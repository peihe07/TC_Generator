# 框界送裁 —— 盲測批（下放包 67 §四 #6，T79b）

> **本批共 6 條。其中若干條附機器建議界，若干條不附 —— **本文不標示哪幾條**。**
> 其名單存於 `docs/fw036/FRAME_BLIND_KEY.md`，**裁定前不得開啟**（分析層已聲明遵守）。
> **判準**：二群之「裁定 ⊃ 建議」比率與其界之位置分佈；
> **否證條件：若二群之界無系統性差異，錨定效應之顧慮不成立。**
> 隨機指派之種子：**6702**（`random.Random(6702).sample`）。

---

## `R-TM40` —— `time_management`，節首行 1437，共 53 行

```
 1437 | **1. A-TM12 降為非阻塞**（§2.1）—— 取值止於物件 id，不再需要
 1438 | 「物件 id → 章節號」之 docx 解析。錨鏈工作不作廢，其角色由「交付欄位
 1439 | 之來源」改為「framework 導航之依據」（Layer 3 主軸章節表、R-TM23 兩條
 1440 | 界線之 spec 依據）。已於 `ANOMALIES.md` A-TM12 條末註記。
 1441 | 
 1442 | **2. 多物件儲存格展開為多條目**（§2.2）—— canon §10.7 允許 string list。
 1443 | 排序改採**物件 id 數值遞增**（本格式無章節層級可比），使同一 leaf 之
 1444 | 條目順序具決定性。
 1445 | 
 1446 | **3. leaf 層之聯集是上限，非每條 TC 之預設值**（§2.3）—— canon §10.7
 1447 | 拘束「List every spec section the TC directly verifies or relies on as
 1448 | setup；Do NOT cite specs only used as background context」。一片 leaf
 1449 | 產生多條 TC 時，各 TC 只列該 TC 實際驗證之物件。
 1450 | 
 1451 | **4. A-TM13 兩片仍阻塞**（§2.4）—— 見 R-TM41。
 1452 | 
 1453 | **5. `feature.yaml` 之 `spec_reference_template` 改寫**（§2.5）—— 已執行。
 1454 | 
 1455 | **6. lint 層新增閘門**（§2.6）—— 每一條目須 (i) 形式符合
 1456 | `CFTS015-\d{7}`、(ii) 其 7 位部分存在於 SYS2 第 5 欄之全集、
 1457 | (iii) 存在於 CFTS015 docx。第 (iii) 項與現存 `lint_tcs.py` 之
 1458 | `lint_spec_reference` 方向相同，**使該閘門由「額外保護」升為
 1459 | 「格式正確性之必要條件」**（G-TM2 項 6 之「不得回退」因此更硬）。
 1460 | 
 1461 | **執行層回報（2026-08-21）**：六項全數登記。第 5 項已執行；第 6 項屬
 1462 | `scripts/`，凍結中未實作。逐 leaf 之候選表已產出於
 1463 | `data/spec_reference_candidates.txt`（22 列全表見上繳）。
 1464 | 
 1465 | **依據訂正（2026-08-21，Pei 於聊天層質疑後實測）**
 1466 | 
 1467 | `04Z-A4` §3 以 `CFTS015-806` / `-1203` / `-1520` 為據，主張文件本身即以
 1468 | `CFTS015-` 前綴指涉其需求物件。**該依據取自短號家族，而本條之取值為
 1469 | 7 位家族**；`CFTS015-<7 位>` 於 CFTS015 全文出現 0 次。
 1470 | 
 1471 | 本條之裁定不受影響（Pei 為工作簿定義之參照體系）。受影響者為其依據：
 1472 | `CFTS015-4813905` 為本專案新定之形式，非沿用文件既有慣例。
 1473 | 兩套編號並存之事實見 A-TM23。
 1474 | 
 1475 | 前綴之三位零填（`CFTS015` 非 `CFTS15`）仍成立 —— 26 個短號值皆用此前綴。
 1476 | 
 1477 | **依據再訂正（2026-08-21，依 `04Z-A5` 上繳 §3.4 之提請）**
 1478 | 
 1479 | `05R` T1 指示將依據改為 canon §10.7 **Rules 第 2 條**
 1480 | （`Use the SourceID format from SYS1 / Polarion when available`）。
 1481 | 
 1482 | **執行層實測後未照該字面執行 —— 該條規則已不存在。**
 1483 | canon `docs/runtime/ASPICE_SWE6_AI_Instruction.md` 之 §10.7 已由他方
 1484 | 整節改寫（工作樹變更，未 commit）：舊節之 `Format per entry` 與六條
 1485 | `Rules` 全數刪除。`grep -c 'Use the SourceID format'` = **0**。
 1486 | 
 1487 | **新 §10.7 為更直接且更強之依據**，逐字：
 1488 | 
 1489 | ```
```

## `R-POP12` —— `popup`，節首行 127，共 14 行｜**建議界：前 0 個非空行**｜其後另有 8 個非訊號行

```
  127 | SWE1-POP-002-02 **不拆** device 軸，理由採「**規格側無此分支**」一說：
  128 | SYS1 5.6 逐字為 `pressing the button a second time`，未區分按鍵型別；
  129 | 037 S11 之 `a physical hard button or a specific UI button on the screen`
  130 | 是 VC 對「button」之列舉性註解，非規格分支；037 本身亦未拆為兩個
  131 | sub-id。依 IN §8.2（RD 為需求單位之權威）與 §8.4.2（規則定義地測試），
  132 | 判一條 TC。
  133 | 
  134 | **本裁同時否決語料 reasoning 之另一套理由**（「是真軸，但 Pop Up List
  135 | 無 hard-button 實例可填」）—— 那套理由蘊含「欠一條待補件」之結論，
  136 | 與本裁不相容。語料之 reasoning 須改寫為本裁之理由（下放包 03 F6）。
  137 | 不開 DR。037 VC 之措辭仍記 Remarks 供 RD-1 順帶確認（不阻斷）。
  138 | 
  139 | Pei 得於審查時否決改裁；若改採「真軸缺件」說，則須開 DR 並將
  140 | -002-02 視為未完成（欠 hard-button 一條）。
```

## `R-POP13` —— `popup`，節首行 144，共 31 行｜**建議界：前 0 個非空行**｜其後另有 25 個非訊號行

```
  144 | TC ID 採 `NR1L-Popup-{NNN}`。依據：分析層 2026-08-27 實測五本交付／
  145 | 產出簿之欄 F（Test Case ID）：
  146 | 
  147 | | 簿 | 前綴 | 列數 |
  148 | |---|---|---|
  149 | | `features/power/delivered/pm_29.xlsx` | `NR1L-PowerManagement` | 389 |
  150 | | SXM 20260813 | `NR1L-SXM` | 215 |
  151 | | UserProfiles 20260819 | `NR1L-UserProfiles` | 189 |
  152 | | TimeManagement 20260825 | `NR1L-TimeManagement` | 59 |
  153 | | Display 20260826 | `TC-DM` | 23 |
  154 | 
  155 | 4/5 為 `NR1L-{FeatureName}-{NNN}`，Display 為離群（不取為基準）。
  156 | pilot 現值 `newR1L-POP-{NNN}` 兩處皆錯（`newR1L`≠`NR1L`；
  157 | 縮寫 `POP`≠全名 `Popup`），全數重排，NNN 序不變。
  158 | 
  159 | **修訂（2026-08-28，上繳包 03 §一-1）**：原末句「feature.yaml 之 `project`
  160 | 模板殘值 `PROJ` 同步修正」**不再適用，此句撤回** —— `features/popup/feature.yaml` 無
  161 | `project` 鍵，全檔亦無 `PROJ` 字串（執行層 grep 實測命中 0）。
  162 | 該句係分析層將 A-POP9(4) 所記之**錯誤述值**連同處分寫入本條，
  163 | 致 A-POP9 之錯誤於其自身之處分條文內再現 —— **分析層之誤**。
  164 | 實際標的為 `tc_id_format`，已由 `"newR1L-POP-{n:03d}"` 改為
  165 | `"NR1L-Popup-{n:03d}"`。
  166 | 
  167 | **量測界之訂正（同上，§一-2）**：下放包 03 §三「全簿掃 `newR1L`／
  168 | `PROJ`／`POP-` 命中須為 0」**不可達成且不應達成**：
  169 | `D2` 為專案名稱欄（母本自帶 `newR1L`，清成 0 等於改動專案名稱）；
  170 | `D10:D14` 為 req_id `SWE1-POP-002-0n`（掃到 0 即 req_id 被抹）。
  171 | 正確量測界採執行層之改定：`newR1L`／`PROJ` 限**本包產出之語料
  172 | 與儲存格**；`POP-` 限 **TC ID 欄 F10:F1411**。
  173 | 順帶：`D2` = `newR1L`（專案名稱）與交付簿欄 F = `NR1L-*`（TC ID）
  174 | 為不同層之值，**不得以 D2 為 TC ID 前綴之權威**。
```

## `R-POP18` —— `popup`，節首行 252，共 30 行

```
  252 | **R-POP16 乙之「首個表格為三簿體例之不變量」一語經實測不成立**，本條取代
  253 | 其中「抽取限於檔內首個表格」一項（其餘兩項不變）。依據：上繳包 03
  254 | §七 A-POP10 實測 —— sxm／audio_mgmt／projection／privacy 之首個表格
  255 | 皆非登記表，power 之登記表又被空行切段；因此丟棄之真陽性為
  256 | **sxm 4／audio_mgmt 7／projection 63**。
  257 | 
  258 | **改採內容判準**（不以位置辨識）：一張表格若其**首欄有 ≥ 2 列
  259 | 匹配 `^(A|DR|R)-[A-Z]+\d+$`**（去除包覆之 `[]`、粗體記號後），
  260 | 即視為登記表，受跳號檢查；一檔可有多張登記表（power 之切段情形），
  261 | 同檔各登記表之編號收為一個序列後再判跳號。
  262 | 非登記表（指紋表、計數表、欄位值表）自然排除 —— `privacy` 之假前綴
  263 | `S` 以本判準亦排除（需實證，不得推定）。
  264 | 
  265 | 另採標題式登記：`## A-XXn` 與 `## [A-XXn]` 兩式皆認
  266 | （audio_mgmt／driver_distraction／sxm 之體例）。
  267 | 
  268 | **本條不及於 `ledger_xref.py`** —— 存在性檢查不需先認定主表，
  269 | 該工具跨全檔收集之現行作法正確，二者分野維持（已記於其檔頭）。
  270 | 
  271 | **實作二項追認（2026-08-28，上繳包 04 §三）**：
  272 | 
  273 | 1. **第二條門檻：單列但佔該表首欄非空格 ≥ 50% 者亦算登記表**。
  274 |    理由正確：power 之 `A-PW` 主表被空行切段，其中有單列續段；
  275 |    只用 ≥ 2 列會把續段丟掉 —— 那是以另一種方式重蹈 A-POP10。
  276 |    兩條門檻皆擋得住 privacy 之欄位值表（`S10` 1/6 = 17%）。
  277 | 2. **標題式登記只作存在性佐證，不參與重複判定**。
  278 |    執行層已實證兩個方向皆會出錯：一併算重複 → popup 之
  279 |    「主表一列 ＋ `## A-POPn` 明細節一節」每號皆變跨表同號（11 筆 note）；
  280 |    有表格就不看標題 → sxm 之 A-SX18／19 又被判跳號（A-POP11 之重現）。
  281 |    先證偽兩方向再定案，而非定案後找支持。
```

## `R-VS36` —— `vehicle_setting`，節首行 672，共 12 行

```
  672 | > 凡以 token 查其在某來源中之出現（覆蓋查詢、引用查詢、值域查詢），
  673 | > **最小須同時試三種形態**，取聯集：
  674 | >
  675 | >   (1) `$X$`（美元包夾）
  676 | >   (2) 裸名 `X`（前後加詞界）
  677 | >   (3) 描述式：`(PROXI parameter|signal|LID|parameter)\s+X`
  678 | >
  679 | > 三者命中數須分別列出。**僅試 (1) 而得 0 者，不得結論為「未出現」。**
  680 | >
  681 | > 理由：本 feature 已兩次因只試 (1) 而誤判
  682 | > （`$HSW_StatFailSts$` 之值域、`$Cooled_Seats$` 之覆蓋）。
  683 | > 兩次皆非資料問題，皆為查法問題。
```

## `R-VS37` —— `vehicle_setting`，節首行 687，共 18 行｜**建議界：前 0 個非空行**｜其後另有 0 個非訊號行

```
  687 | > **【經 R-VS37′ 取代，2026-08-20；原文保留不刪，見 R-TM13】**
  688 | > 取代原因：本條第二分支僅涵蓋「跨越多個**同層**章節」，
  689 | > 未涵蓋「跨**異層**章節」與「**無 reqid**」二情形（A-VS50）。
  690 | 
  691 | > Layer 3 不得純以 SWE ID 中段 token 機械切分。
  692 | > 一個 leaf 之 Layer 3 歸屬，須以其 `reqid_list` 所跨之 CFTS044 章節判定：
  693 | >
  694 | >   該 leaf 之 reqid 全部落在單一章節      → 依該章節之 Layer 3
  695 | >   該 leaf 之 reqid 跨越多個同層章節      → 歸 `Common Features`
  696 | >                                             （其為四側／多側共通需求）
  697 | >
  698 | > SWE ID 中段 token 僅作為**預設值**，與章節判定衝突時**以章節為準**，
  699 | > 並於 `framework.md` 逐筆記明其原始 token 與改判依據。
  700 | >
  701 | > 理由：037 之 SWE ID 命名反映其撰寫時之歸檔，非其需求之實際範圍。
  702 | > 實例：`LeftFrontHeatedSeat-004` 之 reqid 跨 1.3.2.1.3.1～.4 四節
  703 | > （左右 × 加熱通風），其內容為 `the HU shall use $Heated_Seat_Levels$
  704 | > to determine which levels are supported` —— 四側共通。
```
