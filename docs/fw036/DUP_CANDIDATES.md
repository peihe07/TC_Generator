# DUP_CANDIDATES —— 037 原文之字面骨架相似對（候選，非判定）

> **本清單為字面骨架相似度之產物，不是重複之判定。**
> **其召回經回測為不足**（已知 20 對之相似度最低者 0.143，**全召回門檻 ≤0.143，
> 而該門檻命中幾乎所有列對**）；**其精確度亦有反例**
> （`180`／`182` 0.850、`104`／`105` 0.818、`269`／`270` 0.846 **皆已判為可區分**）。
> **其唯一用途為起草該組時之注意提示。**
>
> 方法：`mask_test` 之正規化（去主詞與路由詞）後之 token 集 Jaccard。
> 產生於 2026-08-30（上繳包 63 §1、64 §T76b）。

## 未起草之組（起草時須逐對覆核）

| Jaccard | 對 | 組 |
|---:|---|---|
| 0.927 | `242`／`243` | HU FOTA via TBM |
| 0.882 | `233`／`234` | HU FOTA via TBM |
| 0.533 | `300`／`301` | Bearer Selection |
| 0.462 | `229`／`237` | HU FOTA via TBM |

**未起草組之候選合計 4 對（門檻 0.45）。**

## 已起草之組（供校準：其中多對已判為可區分）

| Jaccard | 對 | 已判 |
|---:|---|---|
| 0.850 | `180`／`182` | 起草時已逐對判定，見各批 `REASONING.md` |
| 0.846 | `269`／`270` | 起草時已逐對判定，見各批 `REASONING.md` |
| 0.818 | `123`／`124` | 起草時已逐對判定，見各批 `REASONING.md` |
| 0.818 | `104`／`105` | 起草時已逐對判定，見各批 `REASONING.md` |
| 0.765 | `121`／`122` | 起草時已逐對判定，見各批 `REASONING.md` |
| 0.765 | `119`／`121` | 起草時已逐對判定，見各批 `REASONING.md` |
| 0.765 | `119`／`120` | 起草時已逐對判定，見各批 `REASONING.md` |
| 0.692 | `003`／`006` | 起草時已逐對判定，見各批 `REASONING.md` |
| 0.677 | `081`／`083` | 起草時已逐對判定，見各批 `REASONING.md` |
| 0.667 | `120`／`122` | 起草時已逐對判定，見各批 `REASONING.md` |
| 0.667 | `119`／`122` | 起草時已逐對判定，見各批 `REASONING.md` |
| 0.656 | `081`／`082` | 起草時已逐對判定，見各批 `REASONING.md` |

**已起草組之候選合計 51 對** —— **其存在即本清單不可作為判定之證據**：**同樣的分數，有的是重複，有的不是。**
