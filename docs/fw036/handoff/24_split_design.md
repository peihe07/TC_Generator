# 拆分設計：A 型複核結果（2026-08-21）

Pei 裁定：A／B 皆拆；新列原位插入、全本 TC ID 重排（政策 b）。
初篩 25 列經逐列複核，**A 型確定 4 列、排除 21 列**。
排除理由逐列記載 —— 排除數多於確定數，證明初篩判別式
（多驅動步驟即多 trigger）過寬。

## A 型確定（拆）

| row | trigger 數 | 拆為 |
|---|---|---|
| **11** | 4（Pre_Start／Start／Cranking／On_EngOn 各自送、各自驗） | 4 列 |
| **12** | 3（SDCARD／BT streaming／phone call 各自選、各自驗） | 3 列 |
| **23** | 3（同 12，Timed 狀態） | 3 列 |
| **179** | 2（來電→FULL OPERATION；掛斷→IDLE，兩個獨立轉移各自驗） | 2 列 |

同型連帶：**row 180**（與 179 逐字同文）→ 同拆 2 列。
**row 181／293**：表面似 179，但其驗證目的為「disclaimer 於通話中
不顯示、通話後首次 Full-Operation 才顯示」—— 通話與掛斷是
**同一情境之建立步驟**，非獨立 trigger，**不拆**。

拆分淨增：(4-1)+(3-1)+(3-1)+(2-1)+(2-1) = **9 列**。

## 排除者與理由（防後續誤拆，逐類記載）

- **同一訊號兩步 transition**（0→1 為一個 trigger）：47、55、57、
  59、66、67、72、79、82、98、106、132、133 —— `Radio_btn0 =0 再 =1`
  是一次按壓，`RemStActvSts 0→1` 是一次啟動。**一 trigger，不拆**
- **環境建立＋單一驗證**：187、191、192、196 —— 兩個 Load Shed
  訊號齊發是 canon §6.1 之環境建立 phase，驗證點單一。**不拆**
- **34**：兩請求（audio＋video control）為**同時併發之一個情境**
  （CFTS 該分支即「兩者皆有」；單獨分支已由 35／36 承載）。**不拆**
- **181／293**：見上。**不拆**

## B 型（30 列，Pei 裁定拆）

判準：每一觀察面向一列；PRE 與 trigger 步驟逐列複製；
test_item 括號下半逐列改寫為該面向之區分 token（維持 sibling 可分）。

| row | 面向數 | row | 面向數 |
|---|---|---|---|
| 10 | 5 | 124–127 | 各 8 |
| 17 | 5 | 157–159 | 各 6 |
| 21 | 6 | 162 | 6 |
| 24 | 7 | 170 | 5 |
| 26 | 7 | 188 | 6 |
| 28 | 5 | 189 | 8 |
| 29 | 6 | 190 | 5 |
| 30 | 7 | 194 | 8 |
| 32 | 5 | 197 | 5 |
| 39 | 5 | 204 | 5 |
| 45 | 7 | 285 | 6 |
| 97 | 5 | 102 | 5 |
| 109 | 5 | | |

30 列 × 各面向 = **186 列**，淨增 **156 列**。

⚠ B 型拆分之**面向定義**須逐列設計（哪一步是共同 setup、哪一步是
面向本體），且 124–127 拆開後將產生 4×8=32 列中大量僅一字之差者，
sibling token 設計為關鍵。**A-PM13 之五列同文（13／265–268）拆分
後將成 5×3=15 列三組全同 —— 拆分前應先併 A-PM13**，否則重複
放大三倍。**建議：A-PM13／A-PM04／A-PM14 之重複列先由 Pei 裁定
併刪，再行 B 型拆分。**

## 拆後規模

283 − 重複併刪（待裁：A-PM04 二列、A-PM13 四列、A-PM14 一列，
至多 −7）＋ A 型 +9 ＋ B 型 +156 ≈ **441–448 列**。
全本 TC ID 依政策 (b) 重排為 001–448 連續。

## 執行順序

1. **Pei 裁定重複列併刪**（A-PM04／13／14，至多 7 列）
2. 分析層出 A 型 4+1 列與 B 型 30 列之逐列拆分內容
   （分批附件，比照 16a–16h 之逐字可貼形式）
3. 執行層套用：插列、四欄寫入、全本 ID 重排、x14 驗證
4. 內容三項（HU 統一、行為化、Front_Panel）於拆分後套用
