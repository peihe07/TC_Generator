# 27 佇列 — 全域收尾包之待辦積累（收 26 後成包）

本檔為 27 包之素材佇列；條文經 Pei 裁定後先落於此，成包時轉入正式下放包。

## Q-1 既定三項（來自 25／26 輪）

1. `RULINGS.sha.tsv` 統一重產（併入 R-VF91–95、R-VF83 但書新 sha、
   C-1 兩條換發、R-VS83；追因工作區 372 列 vs 預期之差額）
2. R-G23′ 入 FO §9.2（每線單一分析來源 + live 取號；V33 §裁定記錄 4 為出處）
3. `canon_refs.py` 之 `verbatim-ruling-text` 判準擴至 feature RULINGS
   條文圍籬（消解 R-VF94 具名 ambiguous 該筆）

## Q-2 行首空格與換行規則（Pei 裁定 2026-08-24，條文即刻可被引用）

**缺陷**：交付內容因換行未做好，行首帶空格 —— test_item 之本文行／
`(...)` 行、各欄之編號項 `1.` `2.` `3.` 前方出現空格。

**條文（入 IN §11，成包時逐字轉入）**：

```
**No leading whitespace / hard-newline discipline**：
`test_item`、`pre_conditions`、`input_test_data`、`test_procedure`、
`expected_result` 之每一實體行自第 0 欄起筆 —— 行首不得有空格或 tab。
適用於：test_item 之上半本文行與下半 `(...)` 行、每一編號項、
以及項內硬換行所產生之續行。test_item 之結構為：上半本文、空一行、
下半 `(...)`（三行皆行首無空格）。
僅有例外：IN §6.1 之子層記法（`a./b./c.` 縮排 3 格、`-` 子彈 6 格）
與 IN §5.4 之 `$` 命令行（縮排 3 格）。
軟換行（顯示折行）屬渲染端，硬換行不得攜帶縮排。
```

**落地三件（27 包工單）**：

1. 寫回 normalizer 於匯出時逐行去除行首空白（例外表同上；冪等，
   一次修好所有未來寫回，不回頭改已生成 JSON）
2. lint 增檢查（字母由執行層依現行表取下一位）：來源 JSON 逐行驗行首；
   test_item 三行結構（本文／空行／括號）驗形
3. **已交付 done region 僅報不擋**（remediation 凍結不變）；新產一律照此

**即刻生效路徑**：各 feature 線（VS 之 V34 起等）下放包引用本節並照抄
條文區塊即生效，不必等 27 成包。

## Q-3 待 26 覆核時一併追問

- tsv 工作區 372 列之來源明細
- expected_numbers `--gate` 之接入時點（裁定 2 之暫緩解除條件是否已成就）
