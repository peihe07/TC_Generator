# 25 — W-P2 下放包（合併 23 包 §E W-P2 + 四項裁定落地）

日期：2026-08-24
所據上繳：`docs/fw036/upstream/24_wp1_continuation.md`（覆核 PASS）
上繳：`docs/fw036/upstream/25_wp2.md`
（原預留之 `23b_wp2.md`／`23c_wp3.md` 廢止；W-P3 屆時另發 26 —— 每輪一個
NN，不再預留，避免 22 之碰撞形態重演）

**裁定記錄**：Pei 2026-08-24 chat「裁完」—— 分析層 24 覆核所提四項
建議全數照准。條文見 §C。另：24 包裁定 3 之 R-VF83~R-VF90 經 Pei commit
確認定案，R-VF1~R-VF8 為永久空號（A-VS02 先例），不補不回收。

---

## A. 禁區

- 全部 git 操作屬 Pei（R-G5@`9814d24c`）
- 歷史 handoff / upstream 檔不改寫（R-G18@`8f61f9fd`）
- 已交付件與 done region 不動；xlsx 不碰
- `features/user_profiles/`、`_intake` 不碰
- 38 處改寫中，實指判不明者**升級不猜**（§E-1）

## B. 背景

W-P1 全部完成並已 commit。本包為改善案之第二工單：預期數字自動化、
閘登錄簿、二項既有缺陷修復（23 包 §E W-P2 原四項），加上 24 覆核
所生四項裁定與一處 canon 修正之落地。

## C. 裁定條文（Pei 2026-08-24 追認；新條文為區塊，既有引用帶 sha8）

### 裁定 A — active-backlog 之處置（採選項 (b)）

```
active-backlog 279 處之處置：僅改寫 features/vehicle_setting/RULINGS.md
與 ANOMALIES.md 之 38 處（逐處語意判別 FO / IN，判不明者升級，不猜）；
改寫後其 waiver 列轉 stale 並自 CANON_REFS_WAIVER.tsv 移除。
其餘 active-backlog 留存，依 R-G18 只減不增。
```

### 裁定 B — R-G13 條文內之 `canon §9`

```
R-G13 條文本體不改寫；24 包 §七-2 之加註 + waiver 列
（reason = verbatim-ruling-text）為定案處置。
```

### 裁定 C — R-G22（新全域條文，併入 FO §9.2；原分析層 [DEFAULT]）

```
R-G22：條文之任何字元變動（含加註、沿革、SUPERSEDED 標記）皆改變其
sha，不設「加註不算改動」之例外。下放包引用之 sha8 與執行層實讀不符時，
執行層停下回報（R-G13），分析層核對變動性質後於下一包換發新 sha8 引用；
變動屬實質修訂者，依通則另出 ′ 版條文。
```

### 裁定 D — R-G14 生效起點（feature-scoped，落 vehicle_setting）

```
R-VS{next}：R-G14 綠色通道於 vehicle_setting 量產線生效，自本裁定後之
下一量產批起算連續綠批（lint 全綠、預期數字全符、全簿基線重現、
獨立判斷無新發現），滿 3 批進入通道。（Pei 裁定 2026-08-24）
```

（`{next}` 由執行層以現行 `RULINGS.md` 實測取號 —— tsv 現示最大為
R-VS81，惟落檔前須重驗無碰撞；本條落 `features/vehicle_setting/RULINGS.md`
並入 tsv。）

### 裁定 E — FO §2 之 fallback chain 引用修正（24 上繳 §五-2，採提案 (a)）

```
FO §2 之無編號子節「BLANK fallback chain (style decisions when no done
region exists)」編為 FO §2.1；Per-state strategy binding 表之
「fallback chain (§3)」改「fallback chain (FO §2.1)」。
```

（落地前先驗全 repo 無既有 `FO §2.1`／`§2.1` 指向他物之引用；有即停。）

## D. 作業清單

1. **裁定落地**：R-G22 併入 FO §9.2（含 §9.2 導覽表加列）；裁定 D 落
   `vehicle_setting/RULINGS.md`（取號 + 錨點）；裁定 E 之 FO 修正；
   `rulings_hash.py` 重產 tsv（R-G22 與 R-VS{next} 首產 sha）
2. **38 處改寫**（裁定 A）：`vehicle_setting` 之 `RULINGS.md`(14) +
   `ANOMALIES.md`(24)，逐處判 FO / IN；判不明者列表升級；
   改後 `canon_refs.py --emit-waiver` 重產、確認 stale 列已除、
   `active-backlog` 由 279 降 241（±升級數）
3. **`expected_numbers.py`**（23 包 W-P2-1）：以 vehicle_setting 最近一批
   下放包手算值為已知全集驗證（FO §5a-12）；逐項相符，不符逐項追因
4. **`GATES.tsv`**（23 包 W-P2-2）：自 lint_defs 生成；近輪命中數可回填者
   回填，不可者標「未知，自本輪起算」，不得以 0 代替未知（G-D）
5. **`--gate` 接入準備**（23 包 W-P2-3）：接入點與觸發時機提案入上繳，
   **準備不啟用**，Pei 裁後生效
6. **`report_stem()` 修復**（23 包 W-P2-4）：tag 併入 feature 名；
   以兩本已知碰撞檔（`20260817_ext`）實測不再覆寫、其餘檔名不變（G-N）
7. 全套 pytest；上繳

## E. 預期數字

| # | 指標 | 預期 |
|---|---|---|
| 1 | `canon_refs.py --waiver` FAIL 數 | 0（維持）|
| 2 | waiver `active-backlog` 列數 | 279 → **241 + 升級數**（升級數逐項列）|
| 3 | waiver stale 列數（改寫後、重產前）| = 38 − 升級數 |
| 4 | `rulings_hash.py --check` | exit 0；錨點數 186 → **188**（+R-G22、+R-VS{next}）|
| 5 | 重複 ruling_id 組數 | 0（維持）|
| 6 | `expected_numbers.py` 重算 vs 手算包 | 逐項相符（不符即 §F-2）|
| 7 | `report_stem` 碰撞數（已知二檔）| 1 組 → **0** |
| 8 | 全套 pytest 失敗數 | **8**（既有，不得增減）|
| 9 | FO `fallback chain (§3)` 字面落點 | 1 → **0** |

## F. 升級條件

1. 38 處中實指判不明者（列表升級，不猜——寧留 waiver 不寫錯引用）
2. `expected_numbers.py` 與手算包不符且追因指向手算包錯誤（既有記錄
   須更正，Tier 2）
3. `R-VS{next}` 取號碰撞、或 `FO §2.1` 既有引用碰撞
4. `GATES.tsv` 回填時發現既有上繳包之命中數自相矛盾

## G. 上繳要求

23 包 §G 全項 + R-G13 引用表（本包既有引用之 sha8：R-G5@`9814d24c`、
R-G13@`abdc56e3`、R-G14@`fb508d10`、R-G16@`2ec55d94`、R-G17@`24cdeec6`、
R-G18@`8f61f9fd`；實讀不符即停，R-G22 生效後依其處置）+ 38 處逐處
判別依據一覽 + `--gate` 接入提案 + 獨立判斷。

## H. 本包產生之新條文清單（自檢表）

- [x] 裁定 A（backlog 處置）— 區塊已列
- [x] 裁定 B（verbatim 定案）— 區塊已列
- [x] 裁定 C／R-G22 — 區塊已列
- [x] 裁定 D／R-VS{next} — 區塊已列
- [x] 裁定 E（FO §2.1）— 區塊已列

本包引用之既有條文：R-G5、R-G13、R-G14、R-G16、R-G17、R-G18（sha8 見 §G）、
R-G9、R-G12、G-D、G-N、A-VS02、FO §5a-12、23 包 §E／§G、24 上繳
§五-2／§五-3／§七-2／§十三-7。
