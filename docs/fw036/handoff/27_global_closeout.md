# 27 — 全域收尾小包（改善案線；V33 §C-3 之順序條件已成立）

日期：2026-08-24。基底：26 上繳收訖、Pei 已 commit。
上繳：`docs/upstream/27_global_closeout.md`。
禁區：git 屬 Pei；VS 線檔案（`features/vehicle_setting/`）**唯讀**
——R-G23′ 單線原則，該線現由其自身 session（V34）發包。

## 一、tsv 重產（V33 §C-3 明令之時點）

`python3 scripts/rulings_hash.py` 重產 `docs/fw036/RULINGS.sha.tsv`，
併入：R-VF91～R-VF95、R-VF83 但書後 `beba78c6`、R-VF73／R-VF74
改指後新值、R-VS83 `1936a6ab`、R-G23′（見 §二落檔後之新值）。
- 驗：`gate_all.py` 之 `rulings_hash` 支轉綠
- 重產後錨點數回報，並**追因 V33 §C-3 所留之差額**（工作區 372 vs
  預期 +2）：逐條列「多出者係何條、何包落檔」，不得只報總數
- `CANON_REFS_WAIVER.tsv` 行號連動更新（§四改鍵前之最後一次行號維護）

## 二、R-G23′ 補 FO §9.2 落點（V33 裁定記錄 4 之待辦）

於 FO §9.2 追加（逐字）：

```
R-G23′（擴充，Pei 裁定 2026-08-24，出處 V33 裁定記錄 4）
單一來源之範圍由全域線擴至每條線：同一 feature／同一線同一時刻
僅一個分析 session 發包。R-VF／R-VS 取號一律「落檔當下 live 取號」
（grep "^### R-{前綴}" 取現行最大 +1），不得預先配號。
```

R-G23 條目加沿革註「′ 版擴充見 FO §9.2」。R-G23 本體一字不動
（R-G22：動了 sha 就換發）。

## 三、mention-not-use（26 上繳 §五-2）

`canon_refs.py` 增「使用／提及」二分：
- **使用**（計入解析義務）：`依`／`照`／`違反`／`適用`／`豁免` 等
  規範性動詞引導之 `§N`／`FO §N`／`IN §N`
- **提及**（不計 unresolved）：`之條文`／`所述`／`該節`／`原文`／
  引號圍籬內、`[SUPERSEDED]` 區內之引用
- 判準表**自 26 上繳 §五-2 所載之四次誤踩實例歸納**，逐例回附
  「該例落入哪一判準」；四例之外如有既有 waiver 列因此可撤，
  列清單（waiver 只減不增之首次兌現）
- 新增單元測試涵蓋四例；`ambiguous` 現值 11 之增減逐筆歸因

## 四、waiver 改內容雜湊為鍵（26 上繳 §五-1；遺留 #10）

`CANON_REFS_WAIVER.tsv` 鍵由 `(path, line_no)` 改
`(path, sha8(line_content))`：
- 一次性遷移 577 列；`line_no` 降為輔助欄（顯示用，不參與比對）
- 舊鍵命中失敗時之 fallback：同檔內容雜湊唯一命中則自動遷移並記錄；
  多命中或零命中列 FAIL
- 測試：行移動不失效、行內容變動即失效（此為鍵之語意，兩向都要驗）

## 五、R-VF83 但書措辭 —— 裁定（落檔屬 VS 線）

**裁定**：採 26 上繳 §四-2 之建議措辭，但書改為
「**現行有效產出未佔用之序號**成為具名缺號」——以狀態界定，
涵蓋重生與配號跳號兩形態，消 FO §9.1 第 10 項之個案名化風險。
**本包不落檔**（VS 條文，單線原則）；裁定文交 VS 線 session 於
其下一包落檔，sha 換發於下次 tsv 重產併入。本包上繳僅轉錄本裁定。

## 六、改善案效果量尺 —— 基線即刻立（26 上繳 §十三-3 第 4 項）

新檔 `docs/fw036/IMPROVEMENT_METRICS.tsv`，三指標、本案值為基線：

| 指標 | 定義 | 本案基線（22→26） |
|---|---|---|
| roundtrips_per_workorder | 下放包數＋重複包數 | 5＋3 |
| violations_caught | 被閘或覆核攔下之規則違反（型別列舉） | 撞號 2、waiver 假性 2、sha 假性 1 |
| gate_first_red | 結案 gate_all 首跑紅支數 | 2 |

量測點：每案結案輪同表追一列。**下一案之成效主張以此表為據，
不再事後點數。**

## 七、預期數字

1. `gate_all.py` 四支全綠（首次）
2. tsv 錨點數 = 實測，差額逐條歸因，無「未知」
3. mention 判準：四例全數落判準；ambiguous 11 之去向逐筆列
4. waiver 遷移 577 列無損；兩向測試綠
5. pytest：8 failed 維持（既有）；新增測試全綠

## 八、上繳要求

R-G13 引用表（全帶 sha）、各節實測、waiver 可撤清單、
`IMPROVEMENT_METRICS.tsv` 全文、獨立判斷（含：§十五-1 之
gate_all 彙總邏輯本輪是否已在多支同紅情境實測）。
