# 下放包 21：台帳修正（PM 最後一包）

純文件，不動工作簿。

## 一、R-13 條文 —— **回復 19 包版本**（分析層錯誤）

20 包 §二之標題「規格訊號名與 DBC 不符時保留原文名」係分析層
為表格欄寬所作之簡寫，**刪去兩個可操作子句**：
「不得代以語意相近之他訊號」（17 包 §五出錯之處）與
「DBC 對應缺漏開 DR 向上游查詢」（DR-PW21 之依據）。

台帳規則「條文全文僅於本台帳落檔一次」意味台帳即唯一權威，
簡寫等同刪條。**執行層指出正確。**

回復為：
```
| R-13 | 2026-08-21 | 規格訊號名與 DBC 不符之處置：規格原文所載之
訊號名，即使 DBC 查無同名，一律保留原文名稱，不得代以語意相近之
他訊號；DBC 對應缺漏登記 DR 向上游查詢 | ACTIVE | 19 | 全案 |
```

成文（防再犯）：
```
R-15 台帳條文之完整性
下放包要求「逐字寫入」台帳之條文，必須為條文全文；
分析層不得為版面或欄寬簡寫。若原條文過長，改列摘要欄並保留
全文於同列，不得以摘要取代全文。
```

## 二、落檔位置表補六列 —— **確認保留**

僅 R-13 有該欄而其餘五條無，確為不一致。補足正確，不移除。

## 三、「同上」鏈缺陷 —— **確認**

19 包由分析層引入（R-13 插於 R-8 與 R-2 之間，破壞「同上」串接
指涉），本包由執行層自行發現並修復。移至 R-5 之後正確。

登記 **A-PM17**：落檔位置表以「同上」串接指涉，插入列將靜默改變
其後各列之指涉對象，無任何檢查可攔。處置見 §五。

## 四、R-1 v3 不在台帳、v2 仍 ACTIVE —— **最高優先，本包處置**

執行層所指成立且嚴重：283 列之訊號寫法全依 v3，台帳所載 ACTIVE
條文卻為 v2，**台帳與交付內容不一致**。

依 R-TM13 處置：
```
| ~~R-1 v2~~ | 2026-08-21 | CAN 訊號採 `Send CAN: <MESSAGE>.<Signal>
= <raw> (<label>)`，PROXI 加 `$` | **SUPERSEDED** | 09 | 全案 |
```
加註：
> 經 CR30580/30581 參考本查證，`$` 為訊號之標記而非 PROXI 之標記；
> v2(c)(d) 之指派相反。由 R-1 v3 取代（12 包）。
> v2(a)(b)(e)(f) 之內容併入 v3。

新增：
```
| R-1 v3 | 2026-08-21 | 訊號一律 `$<MESSAGE>.<Signal>$ = <raw>
(<label>)`，label 取自 DBC VAL_；PROXI 作 `PROXI <Param> = <值>`
不加 `$`；內部訊號 DBC 查無對應者保留來源名不加 `$`，並於 PROC
寫出應設定或應觀察之值 | ACTIVE | 12、17 | 全案 |
```
加註 v3(d) 之修訂沿革（17 包 §三：原「不得留來源名」作廢）。

## 五、其餘三項處置

**R-14 與 S6 之關係**：於 S6 列加註
> 併見 R-14：`PENDING` 說明之語言一律英文；中文描述置於
> DATA_REQUESTS.md。

**R-14 無自動檢查**：執行層所指正確 —— lint K 攔 CJK 屬巧合重疊，
全形拉丁不會觸發。併入 lint feature-scoped 包，新增檢查
`pending_desc_non_ascii`。

**三份文件無結構性檢查**：登記 A-PM17（見 §三）。
併入 lint feature-scoped 包，新增 `docs_structure` 檢查：
台帳／DR／ANOMALIES 之編號連續性、狀態值合法性、
「同上」串接之指涉正確性、條文欄非空。

## 六、執行

1. RULINGS_LEDGER.md：§一 回復 R-13 全文；§四 之 v2 標
   SUPERSEDED 加註 ＋ v3 新增 ACTIVE；§五 之 S6 加註；
   新增 R-15
2. ANOMALIES.md：新增 A-PM17
3. 落檔位置表：R-1 v3、R-15 補列

上繳 `docs/fw036/upstream/21_ledger_fix.md`，附
「本包是否仍有該驗而未驗者」獨立判斷。

**本包後 PM 之分析／執行層工作全部結束**，
餘下為 Pei 之授權與送達（見 DELIVERY_NOTE §8.4）。
