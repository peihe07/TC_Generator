# CDD-10 審閱 —— Revise（R-DIAG22/20 amend）＋ 延伸審計與產出

審閱對象：`up/20260917_CDD-10.md`。審閱層 session：2026-09-17。
**結論：通過。** 85 條讀類值域軸改 unsupported DID 型；常式 4 條帶越界 option；延伸審計 355 配對／12 相異候選 → 新增 12、Revise 8；合併本 556 列，389/389；lint 真違規 0；§9 17/17。
**新增只有 12 條的原因經查證成立，但揭出一個更大的資料缺口（§三），交付前先補。**

---

## 一　逐項核對

| up 節 | 判 | 備註 |
|---|---|---|
| §2.1 85 條三處同改（串／描述／下半 token）；0x2E/0x2F 五條本即合規 | 通過 | |
| §2.2 SID 31 四條：`-278` 以 start 帶 CFTS004-4940476 明載越界值；`-288`／`-341`／`-344` 佔位 | 通過 | 「`31 03` 無資料 byte 可填」之判斷正確 |
| §2.3 `NEG-DIAG` 值域段、14 項自測 | 通過 | |
| §3.1 審計統計：配對數 vs 相異候選之區分 | 通過 | 「列數是配對數不是候選數」之澄清正確；`N` 267 中 243 為引言句 |
| §3.2 `-266` 四態產出（R-DIAG25 撤銷 CDD-07 之收斂） | **追認** | |
| §3.2 Signal bars 0–3 逐值四條、BVA 改限值數 > 8 | **追認為 R-DIAG25(amend)** | 離散值 ≤ 8 → 逐值；> 8 → BVA 兩式 |
| §3.2 Terrestrial／Satellite 引兩錨（VM 情境 ＋ 4939801 三態） | 通過 | R-DIAG24(b) 之範本 |
| §3.2 `-219`／`-223` 改 `$22 50 0B` 讀回斷言 | 通過 | 觀察通道回到本 feature 內，優於原 HMI 面斷言 |
| §3.3 Audio decoder bitrate 非天線態；`$500B` 無 menu items 清單 | 收 | 分析層預判錯（A33）|
| §3.4 環境／持久軸 grep 全 0 | 通過 | 記入 coverage 說明 |
| §3.5 FB-DIAG-s | 通過 | |
| §6.2 coverage 母體錯寫（382）自查改正 | 收 | 「母體與分子一起換」教訓入 profile |

## 二　分析層自報錯誤

| # | 內容 |
|---|---|
| A33 | CDD-10_A §二(b) 兩處預判未查 CFTS004（`-217` VM 非天線態；`$500B` 無清單）|
| A34 | 追補 A 預期 40–120 未先去重「037 列 × 錨」之配對，把配對數當候選數 |
| A35 | **自 CDD-01 起只以 CFTS004 之 Polarion 索引匯出（`Basic Report`）為來源，未取 CFTS004 原始文件**。§8.6 明文「原始規格勝過索引匯出」；引言句之列表（243 個候選配對）就是索引匯出丟掉的內容 |

## 三　資料缺口：CFTS004 原始文件（交付前必補）

up §3.1／§6.1 證實：`$5000` r328、`$5001` r320、`$2843` r?、`$2812` r164、`$280C` 等五句引言之後的**列表未隨匯出帶出**，243 個候選配對因此為 `N`，另 DR-DIAG-4／-5 之 261 行佔位大半亦源於此。
這不是 RD 未寫，是**我們沒拿原始文件**。CFTS024／CFTS092 之原始 docx 皆在 `1_Customer_Requirement/R1LR SR26 ATL-H/<PI>/` 下（CDD-01 up §1.3 曾列該目錄），CFTS004 極可能同在。

裁定（§四 R-DIAG26）：CDD-11 T0 先於 `1_Customer_Requirement` 全樹搜 `*CFTS*004*`／`*General Diagnostic*`（docx／pdf／xlsx），命中即依 R-G66 登錄、抽出五句引言之列表與 `$5000`／`$5001` 之 controlOptionRecord／selectable 列表，**依 R-DIAG23(c) 產出**；未命中則 DR-DIAG-10（文件請求，對 Nik），交付本以現況出。

## 四　審閱裁定（依常規即生效）

```text
R-DIAG25(amend)  離散值之拆法
  錨列舉之離散值 ≤ 8 → 逐值各一條（design_method 功能測試）；> 8 之連續值域 → BVA 兩式（CDD-05 追認）。
```

```text
R-DIAG26  CFTS004 原始文件為來源（IN §8.6）
  CFTS004 之 Polarion 索引匯出（sys2_cfts004_general_diag）不含列舉列表；交付前須取 CFTS004 原始文件
  （docx／pdf）登錄 MANIFEST，引言句之列表以原始文件為錨（`CFTS004-{ObjectID}` 不變，Remarks 不另註），
  依 R-DIAG23(c) 補產。未取得 → DR-DIAG-10，交付本 coverage 說明明記。
```

```text
R-DIAG27  交付形式（沿 R-SEC25 前例）
  單一工作簿；列序依 037 列序（SWE1 ID 升冪，`-340-001`/`-002` 依 037 row）；TC ID 依交付列序重編為 NR1L-DIAG-001 起連號，
  附 `id_map_v08_to_delivery.tsv`；Test Set 欄保留不作排序鍵；佔位列整列著色 FCE4D6（沿 R-SEC 前例，文字不動）；
  檔名 `FM-WI-FSM-036-A01 STLA 測試用例規範與結果_SWQT STLA Test Case Specification & Result_SWQT_Diagnostics_<YYYYMMDD>.xlsx`；
  Cover 版本／日期／作者、Product Document 依 SWC 0708／Home 0809 實測填；核准／審查／中文作者三格留給 Pei。
  specification_reference 維持 §10.7(a) `CFTS004-{ObjectID}`（7 位 Polarion 物件號，同 SWC 0708 之 `CFTS042-4813401` 式）。
```

**待 Pei 確認一點**：Security 線 R-SEC10(c) amend2 用 `CFTS084-{NRL number}`（`NRL-xxxxx` 之數字），本 feature 依 canon §10.7(a) 用 ObjectID（CFTS004 索引 E 欄 7 位號）。兩者不同來源欄；SWC 0708 用的是 ObjectID。**若 Pei 要統一為 NRL，於 CDD-11 上繳後裁，Revise 一次即可**（CFTS004 索引 A 欄即 NRL，逐列可換）。

## 五　待 Pei

1. push（rebase）；GC-16（Security SEC-18 收尾後）。
2. FB-DIAG-a～s（19 項）、`asset_request.md` 五項送出；DR-DIAG-10 視 CDD-11 T0 結果。
3. §四末段 spec_reference 欄源之確認。
4. commit：建議准，沿 up §9。

## 六　CDD-11 隨本審閱落檔

= R-DIAG25(amend)/26/27 ＋ **CFTS004 原始文件搜尋與補產** ＋ **交付候選**（`sandbox/delivery/`）。
