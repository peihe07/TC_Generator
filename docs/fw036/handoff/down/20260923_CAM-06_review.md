# 下放包 CAM-06 審閱（分析層 2026-09-23）

對應上繳包：`docs/fw036/handoff/up/20260923_CAM-06.md`
判定：**§1／§2 全數通過，batch01（含 01b）判過，計 R-G14 第 1 批**；batch02 停於步驟 A 正確（85 > 40）。執行層四處「停下回報」皆為實測所迫，本審閱逐項裁決如下；依 2026-09-17 之常設指示，CAM-07 隨本審閱同時發出。

---

## §一　執行層報告之確認

1. R-CAM13／14 sha8、framework 重開 sha16 量測法（自指欄位以 16 個 0 代入）：接受，量測法記入 profile §8。
2. selfcheck 四項對 33 列回歸：RUN 重複只命中 `-023`（審閱所指同列），無第二例：確認。
3. `RD_FEEDBACK.md` 五條：接受；RDF-03 改題為「三份文件三個值」正確（A-CA22「無單位」不成立，執行層之實測為準）。
4. **A-CA31**（V33 `MAX_SPEED = 13,0 km/h`，raw 208 為整數點）：接受。連帶：**A-CA30 結案**（Atl-Hi 側不可判、2261 側可判）、**A-CA22 結案**（單位皆載）。`-030`／`-031` 供試值 209／208 正確。
5. `batch01b` 12 列一對一、不合併：接受。Toro 2019 本止於 byte 172 → 2261 三參數查無 → 勾 0：接受，但**此為 PROXI 表陳舊，開 DR-CAM-l**（§三-7）。376 之 `CameraDisplaySts` 於 P363 DBC 查無 → `-045` 376 勾 0：接受。
6. `HU screen` → `HU display`（降 X 噪音）：接受；`Controls screen` 保留來源用字：正確。X=2 為 DR-CAM-g 本體，不改。
7. `-002` 無「Absent → 不啟用」之來源 → 不生成 negative：**接受**（§8.4.1 勝於 §7 之配對要求）；登 **RDF-06**「-002 缺 Absent 側需求」。
8. `-005` 格式面 SYS2 查無 → PENDING 不採 037 值：接受。

## §二　裁決（執行層四處停下 ＋ §4 四項 ＋ §5-5）

| # | 項 | 裁決 |
|---|---|---|
| 1 | `-009`／`-010` 之 637 | **不入 8 mph 列**（執行層正確）。637 之 `MAX_SPEED` 與 2261 同為 13,0 km/h、同訊息 `STATUS_CCAN3` → **併入 `-030`／`-031`**：637 勾 1，`specification_reference` 加第二行 V42 常數列之 anchor（`SYS-RA-VF551_V42-656`），reasoning 註 V42 無退出條文、以 V33 條文 ＋ V42 常數承載。R-CAM14 依 R-TM13 加 (b) 段記此 |
| 2 | R-CAM14 之 `BRAKE1` | **修訂**：DBC 為準（R-17），376 之訊息名 `STATUS_CCAN3.VehicleSpeedVSOSig`；`BRAKE1` 為 VF551_V3 文面名，留 reasoning。R-CAM14 加 (c) 段（原文加刪除線） |
| 3 | `-023` Pre-Condition 3（CAN source 行） | **刪**。R-CAM3(e) 之句式為配合 `Send CAN` 步而設，無該步即無代換對象；`$PowerMode$ = IGN_RUN` 已由 `Full-Operation` 行承載 |
| 4 | §4-1 條件側／動作側分屬兩承接列 | **准合一**：同一行為之 IF 與 THEN 來源合為一 TC，`specification_reference` 多行承載兩錨；TC 歸 **SWE ID 較小之承接列**，另一列於 reasoning 委派。R-CAM10 加 (b) 段。plan 之 4 TC 收為 2 |
| 5 | §4-2 batch02 重切 | **准** 02a（`-001` 22 ＋ `-012`／`-014` 4 ＝ 26）／02b（`-002`／`-005`／`-017` ＝ 22）／02c（`-003` 25） |
| 6 | §4-3 `V2-535` 引 VF664 | **開 DR-CAM-k**（中）：VF664 全文件；該 TC 帶 PENDING |
| 7 | §4-4 `Auxiliary Cameras` 由四 SWE 供稿 | **准**，R-CAM13(c) 已涵蓋；framework VIII.2 例外註擴為四列 |
| 8 | A-CA32 `-076`（Rear 節 Controls screen 句） | **生成**，同 `-063`／`-089`，PENDING DR-CAM-g |
| 9 | `-002` negative | 不生成，RDF-06（§一-7） |
| 10 | `RULINGS.sha.tsv` 未重生 | GC 線（既有 GCB） |

## §三　DR 動作（Pei 送）

| DR | 內容 | 級 |
|---|---|---|
| DR-CAM-k（新） | VF664（soft key button controls）全文件 | 中 |
| DR-CAM-l（新） | Toro (2261) 之現行 PROXI 表（2019 本止於 byte 172，缺 byte 177／222 之 camera 參數） | 中 |
| 既有 a／f／g／h／i／j | 不變；f、i 交付前必結 | — |

## §四　RD_FEEDBACK 追加

RDF-06：`SWE-CAM-002` 只有 `Rear_View_Camera = Present` 之成立側，無 Absent 側行為；TC 不得自造 negative。

## §五　備查

- A-CA30／A-CA22 結案、A-CA31 RESOLVED 由 CAM-07 落檔。
- `I-cross` 23／12／10 為基線，不處置。
