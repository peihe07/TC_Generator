# SEC-18 審閱 —— 上繳包 `up/20260917_SEC-18.md`（`security_v12`，spec_reference = `CFTS084-{NRL}`）

審閱層：分析層，2026-09-17。
判定：**通過，不升級**。124 格只動一欄、1,332 行格式 0 錯、NRL 全在 SYS2 表、換行 98／98／98、六本 lint 0。
**出貨本更新為 v12**（`delivered/` 已同步）。

## 一、採納
| 項 | 判 |
|---|---|
| SEC-18_A 全寫：70 列全 OK，CP-001 41 行、SWDL-001 64 行 | 依 Pei 裁；分佈表已給，Pei 若嫌長再裁上限 |
| `category_excluded_if_filtered` 欄保留（全表只 1 號）| 准；證明原 §1(c) 的噪音來自上限而非 category，登分析層 A-44（篩選設計無效）|
| `spec_mode` lint 不存在 → profile 記載＋腳本自驗 | 准；入 lint 歸 GC-16 `S-ORD` |
| `mark()` 加 `src == out` 護欄 | 准 |
| 建議 `mark_placeholder_rows.py` 可指定標的、superseded 命名取實際版本 | **准**，併入 GC-16 後之任一 Security chore；不獨立出包 |

## 二、事故（執行層自報，已修復）
`_all` 截斷與三本重複套色、`_v11` 標籤錯指——均自備份／git HEAD 還原並 `cmp` 6/6。處置正確，登執行層側 ledger。

## 三、一處待執行層一句話確認（不阻）
上繳包 §五-5 稱 70 列 `match_kind` **全為 exact**；SEC-01 上繳包曾記 SAM `_COMP` 尾綴列需 `base` 回退才命中（DR-i）。二者若皆真，表示 `trace_matrix.tsv` 某次重生已把正規化提前（去尾綴後比對即計 exact）。請在下次上繳附一句說明，或在 `trace_matrix.tsv` 加 `match_note` 欄；不影響 spec_reference 結果（兩式取的號相同）。

## 四、狀態
出貨本：`features/security/delivered/` 六本 v12 ＋ 六共用件。Pei 側：commit 一支、六本封面 18 格、`W=4`、GC-16、三封訊息。
Security 線**無下一包**。
