# 下放包 CAM-10 審閱（分析層 2026-09-23）

對應上繳包：`docs/fw036/handoff/up/20260923_CAM-10.md`
判定：**落檔通過；batch03a 29 列判過（第 5 批），三處修正後結案**；執行層於升級條件 3 之「拆五列而未停」判斷正確。CAM-11 同發。

---

## §一　裁決

| # | 項 | 裁決 |
|---|---|---|
| 1 | `V2-486` 1 → 5 列（升級條件 3 觸發未停） | **准**。拆列為 §8.2.2 之正解；升級條件 3 自本包起改寫為「摘句不可行且**不可拆**時停」 |
| 2 | `V3-284` 2 → 4 列（補 c／d Atl-Mi 分支） | **准**，CAM-09 計畫之漏由執行層補正 |
| 3 | `coverage.tsv` 建 25 列全量、沿 security 本格式 | **准**；下放包指 vehicle_setting 為分析層之錯 |
| 4 | `V42-326` 五對映只出兩列 | **擴為五列**。查表對映之每一項為規格明定之獨立輸出，§8.3 等價類不適用（任一項錯即 FP 漏網，§7）；`-145`／`-146` 保留，補三列續 `-147`～`-149`，batch03a → 32 |
| 5 | `systemStatus.ZoomViewRes` 之 LVDS 注入能力（`-131`～`-133`） | 三列保留；**`bench_verify.md` 加一列「LVDS 模擬器注入 RVCM → HU 訊號之能力」**，Pei 交付前確認；若無能力，三列於工作簿 Remarks 註「需 LVDS 模擬器」而非刪 |
| 6 | `-136`～`-138` 只驗「反映 PROXI 值」 | `-136`／`-137` 接受（同 6-25）；**`-138` 退回**：Pre-Condition 3 `PROXI CAN node 27 (ASM/ASCM) is configured as present` 未經 PROXI 表查證 —— 執行層以 `ASCM`／`ASM`／`Air_Suspension`／`Node_27` 掃 Atl-Hi 三本 `forms/proxi/`，有參數名則改 `PROXI <Param> = <值> (<label>)`，查無則該行 `PENDING: DR-CAM-n`（併入 n 之列舉補件）並回報 |

## §二　抽讀（122／131／138／145）之偏差

| # | 型 | 實例 | 修正 |
|---|---|---|---|
| 1 | PENDING 嵌入句中 | `-122` ER2 `… is sent and PENDING: DR-CAM-p it is sent for two …` | PENDING 獨立為子項：ER2 `gridZoomRequest.DynamicGridRQSts = OFF is sent` ＋ `a. PENDING: DR-CAM-p duration of two LVDS message cycles before the steady value follows`；Pre-Condition 3 之同一 PENDING **刪**（缺值只在 ER，§8.4.3 佔位只落於缺值之欄）。`-123` 同。selfcheck 第 8 項：`PENDING:` 出現於行中而非行首／子項首 → 命中 |
| 2 | （無其他） | | |

## §三　備查

- DR 十二條未結；LVDS 注入能力為 bench 事項非 DR。
- R-G14 第 5 批乾淨。
