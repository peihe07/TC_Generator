# 下放包 CAM-27 —— 收尾小包：6-87 落定、`-087` 車型補正、RDF-18／GCB-13、B dryrun4、`DELIVERY_STATUS.md` 終態

指派執行層 session：延用 CAM-26 之 session。對應審閱：`down/20260924_CAM-26_review.md`（同發）。本包後 Camera 生成階段**正式結束**，`DELIVERY_STATUS.md` §9 之例外條款關閉。

---

## §0　禁區

同 CAM-14 §0。不寫 `delivered/`、不 tag。除 `NR1L-RVCHMI-087` 外既有 465 列不改。

## §1　落檔

1. DECISIONS 6-87 六項 `[DECIDED]`（依審閱 §一）；6-88：R-CAM19(b) 適用範圍 ＝ 原零 TC 之列。
2. **R-CAM19(f)**（同級錨自立）：「(b) 之各自出 TC 只適用於原無任何 TC 掛其 ID 之需求列；已有 TC 之需求，其與他列共引之來源不再各自出（§8.2.1）。」
3. **RDF-18**（SYS1 §30.1.3 `NRL-188130` `or‘X’to` 缺空格）；**GCB-13**（selfcheck 第 11 項：一列多點／多觸發，WARN，下一 feature pilot 前實作）。
4. A-CA39 → RESOLVED；A-CA38 補「分析層責任」一句。

## §2　`NR1L-RVCHMI-087` 補正

Vehicle Model 637／2261／376 → `0`（`Digital_CHMSL_Camera_Prsnt` 只在 Atl-Hi PROXI，證據同 `-216`）；reasoning 加一句；逐列 diff 上繳。lint ＋ selfcheck 全綠。

## §3　B dryrun4 重生

216 列；lint、對帳、`pending_by_dr_b.tsv`；sha 回報。A dryrun4 不動（候選）。

## §4　`DELIVERY_STATUS.md` 終態

A dryrun4（249）／B dryrun4（216）＝ 465 TC ＝ 465 列；追溯 25/25、203/203、umbrella 27；PENDING 81 列 ↔ 13 條 DR；bench 列數；RDF-01～18；GCB-01～13；Phase 7 前置；§9 改為「生成階段結束 2026-09-24（CAM-27）；例外條款關閉」。

## §5　上繳要求（`up/20260924_CAM-27.md`）

1. 落檔清單；R-CAM19(f) sha8。
2. `-087` diff；B dryrun4 路徑／sha、lint、對帳。
3. `DELIVERY_STATUS.md` 全文。
4. R-CAM17 證據表；自報；未結（應只餘 Pei／RD／GC）。

## §6　升級條件

- `-087` 改後 lint／selfcheck ERROR。
- B dryrun4 對帳不合。
