# GC-11 審閱（2026-09-05，主線分析層）

對象：`up/20260905_GC-11.md`（執行層 B，sha `3afa0936…`，191 行）。
結果：**收受**。產物全數採用，不作廢。

## 一　先記一件事實（不擴張）
`down/20260905_GC-11.md` 於 21:44:15 由本線落（8,612 B，內容＝主線復歸＋§三 11 項），21:54:54 被 A 線以同名覆寫（3,156 B，內容＝五本 dry-run）。執行層 B 依 21:54 版施作，並如實具名衝突——處置正確。
- 本線 21:44 版之內容已無磁碟副本；其 §三-1/2/3/6/7/8 已由 GC-10 完成，餘 §三-5（pm_29 dry-run，本包 6-1 已做）、§三-9（FO 接手清單）、§三-10（DR 狀態欄提案）三項，移入 GC-12。
- 覆寫事件登 ANOMALIES 一條（次一未佔號）：A 線於 PROTOCOL 增「落檔前證不存在」句之後仍同名覆寫；形態同 A-GC18。**不因此作廢 A 線 GC-11 之任務內容**——其五本 dry-run 與 R-G72 方向一致，做了就是做了。
- 本線自此以 `GC-12` 起號續行；A 線之下放檔本線不再引用。分析層互斥由 Pei 定：本線為主線（Pei 2026-09-05 已裁）。

## 二　請裁三項之裁

### 1　指派衝突
執行層 B 之施作有效，產物全收。補遺 §1「領先線收尾」所指即 B，無衝突實質。

### 2　Revise 本之切分（R-G72(f) 補充）
```text
R-G72(g)：Revise 本分兩批。
  Revise-M（機械批）：只含 mechanical=Y 之轉換（send／recv／buserror／PROXI-old）。
    Y=0 之本不出 Revise-M。本輪：pm_73 33 列、ics_management 14 列、vsm_v42 10 列出 Revise-M；
    sw_update 不出；pm_29 不出 Revise-M —— 其 201 列併入 PM 站 ⑤ 寫回（Revise2），不另發。
  Revise-C（內容批）：X-nav 與 bare-$MSG.Sig$ 之解，逐 TC 查 HMI／CFTS／DBC 後出；
    查無者 PENDING 登 DR。sw_update 之回修全在此批。
  兩批各附 idmap；Revise-C 之 idmap 以 Revise-M 之 new_tc_id 為 old。
```

### 3　bare-$MSG.Sig$（五本 493 處）
不是 DR 就能解的：raw／label 之 **值** 須來自 CFTS 原句（§8.4.1），DBC 只供 label。處置：
- 逐處回查該 TC 之 CFTS ObjectID 原文：原句載有值 → 依 R-G70(c)(d) 轉，附 DBC VAL_ label（機械可做，但前置是人讀原句，故歸 Revise-C）；原句無值 → `PENDING: DR-{n} <signal> value`，登該 feature DR。
- pm_29 之 207 處即 PM 站 ③（389 列人讀審閱）之既定範圍，**不另立包**，併入 ③ 之審閱表加一欄 `bare_value_source`。
- 其餘四本（pm_73 176／ics 66／vsm_v42 41／sw_update 3）於 Revise-C 包處理。

## 三　其餘各節
| 節 | 判 |
|---|---|
| §2 A-GC19／A-GC20、PROTOCOL 附記、R-G70 v4.1 註（lint Y） | 收受 |
| §3 候選表判準已是 lint036 式 | 收受；補遺 §3 之憂不成立，本線之誤在「未先讀 `lint036.py:932` 即斷言」，併入上款 ANOMALIES 條 |
| §4 五本 dry-run、§5 idmap 雛形 | 收受；`test_item` 全程略過正確 |
| §6-1 pm_29 v4.1 重跑 584 vs 舊表 685 | 收受「只報差值」；差 −101 之成因於 GC-12 逐列比對一次（規則集差異），比完舊表加 README 註 |
| §6-2 pm_73 非 pm_29 累積本 | 收受；pm_73 為 288 列 S6 例外本，Revise-M 之母本即它 |
| §7-2 八本基線 P 對照向二度延後 | 准獨立成包，入 GC-12 |

## 四　GC-12 任務（下一包，本線下放；**指派執行層 B**）
1. 八本基線 P／Y 對照向（改前取 `HEAD~` 之 `lint036`，改後現行）。
2. 本線原 §三-9：FO 接手清單加二項（新 feature profile 啟用 X／Y／P(v4)；§5.3 PENDING 二常數於新 feature DR 承接），`[ADD]` 標 R-G71。
3. 本線原 §三-10：`DATA_REQUESTS.md` 狀態欄格式提案（只加標頭說明，不回填），附可機讀節數／總節數。
4. pm_29 二表差 −101 逐列成因；舊表 README 註。
5. Revise-M 三本（pm_73／ics_management／vsm_v42）產出：`_Revise1` 新檔、idmap 填 `new_tc_id`、全閘重過（R-G72(e)）、MANIFEST 加列指前版。**xlsx 只寫新檔，母本不動。**
6. ANOMALIES：§一之覆寫條。
7. 上繳 `up/20260905_GC-12.md`。

## 五　Pei 動作
1. `rulings_hash.py` 重生（GC-10 之 R-G70(e)／R-G72(f)＋本審 R-G72(g)）→ `--check` → commit。
2. `driver_distraction` 二檔 `git mv`（積欠）。
3. DR 草稿取號送出（HMI entry path ×2＋標籤取捨；DR-VS{n} Power_Tailgate_Enable）。
4. 對 A 線分析 session：關閉，或明令其不再向 `down/` 落檔。

## 六　未結 DR
DR-PW25、DR-VS{n}、HMI entry path 草稿 ×3，皆未送出／未結。bare-$ 值查詢之 DR 待 Revise-C 包逐處登。
