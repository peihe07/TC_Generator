# 下放包 SEC-01_E —— 步驟之執行通道必須明確（Pei 2026-09-16 強調）

本檔為 SEC-01 系列第五補充；與 `_D` 同為執行層必讀。
本檔把 R-SEC{live+3}（落地來源）與 R-SEC{live+5}（DID 定式）再收緊一層：**每一步不只要有來源，還要寫明「用什麼執行、怎麼觀察」**。

---

## §1　裁定

```text
R-SEC{live+6}  每一 Procedure 步驟與 ER 必須明示執行通道與觀察手段
  (a) 執行通道為閉合清單，每步擇一並寫出**完整指令或完整操作**：
        ADB   ：`$ adb root` / `$ adb shell <cmd>` / `$ adb push <src> <dst>` / `$ adb pull <src>` /
                `$ adb shell am instrument -w -e class <FQCN>#<method> <runner>` / `$ adb reboot`
        UDS   ：`$ 22 XX XX`、`$ 2E XX XX <data>`、`$ 31 01 XX XX`、`$ 10 XX`（依 R-SEC{live+5}；tester 送 bytes）
        HOST  ：`$ openssl verify …`、`$ openssl x509 -in … -text`、`$ openssl req -in … -noout -verify`、`$ ./logdecrypt_Ver2.sh …`、`$ pytest …`
        CAN   ：`Send CAN: <MESSAGE>.<Signal> = <raw> (<label>)`（§8.7.5(c)）
        PHYS  ：`Insert USB drive containing <資產名> into HU USB port`、`Insert SAM dongle`、`Press H/K "<button>"`、
                `Power cycle HU (IGN OFF → IGN ON)`、`Disconnect network`
        UI    ：`Select "<label>" in Dealer App`（限 HMI L&F／Test Steps PDF 有載之入口）
      指令行依 IN §5.4 以 `$` 起首、獨立一行、不編號；描述行只寫業務意圖，不得只有描述行而無指令行。
  (b) 禁止之寫法（無通道即 FAIL）：
        `Trigger key installation` / `Perform certificate verification` / `Install keys` / `Verify the chain` /
        `Run the test` / `Check the log` / `Corrupt the certificate` —— 皆須改為 (a) 之具體形式，
        例 `Corrupt CERT 0 in ecu.cacert on host (edit PEM body)` ＋ `$ adb push ecu.cacert /data/misc/ecuidentity/`。
  (c) ER 必寫觀察手段，閉合清單：
        LOG   ：`$ adb logcat -s <TAG>` ＋ 期望字串逐字（例 `MelcoCertProviderTest: ECU certificate verification broken chain = ERR_UNABLE_TO_GET_ISSUER_CERT_LOCALLY`）
        FILE  ：`$ adb shell od -t x1 <path>`／`$ adb shell cat <path>`／`$ adb shell ls -l <path>` ＋ 期望值
        UDS   ：`Positive response is received: 62 XX XX <payload>`／`Negative response …: 7F XX <NRC>`
        RC    ：instrument 之 `OK (1 test)`／`FAILURES!!!` ＋ 方法名
        HOST  ：openssl 之 stdout 逐字（`SAMcert.pem: OK`／`error <n> at <depth> depth lookup: …`）
        CAN   ：`<MESSAGE>.<Signal> = <raw> (<label>) is sent`
        UI    ：`The "<title>" screen is displayed`
      不得寫 `verification succeeds`／`system rejects the certificate`／`log shows error` 而無手段與逐字期望值。
  (d) Pre-Condition 必寫連線與權限狀態：`DUT is connected via ADB with root permission (Dev/Eng build)`、
      `Diagnostic session is <xx>`、`Test runner CertProviderServiceManagerTest.apk is installed`（用 apk 時）。
      這三句為 §5.3 常數候選 `ADB_ROOT_READY`／`DIAG_SESSION_<xx>`／`CP_TEST_RUNNER_INSTALLED`。
  (e) 期望字串、路徑、回應 bytes 一律逐字取自既有素材（R-SEC{live+3}(c)）；素材只給語意不給字串者
      （例 CS.212 第 2~6 項只有 tag 範例），該 ER 寫 `PENDING: DR-SEC-h log keyword`，不得自擬字串。
  (f) 一步一通道。同一步既 `adb push` 又 `reboot` → 拆兩步；ER 1:1 對應。
```

## §2　既有素材中可直接沿用之通道句（執行層登入 `step_assets.tsv`，並以此為 §5.3 常數候選）

| 來源 | 通道句（逐字） |
|---|---|
| `Test_Items.txt` | `$ adb shell am instrument -w -e class com.mitsubishielectric.ahu.efw.lib.melcocertprovider.libcertproviderservice.test.CertProviderServiceManagerTest#<method> com.mitsubishielectric.ahu.efw.lib.melcocertprovider.libcertproviderservice.test/androidx.test.runner.AndroidJUnitRunner` |
| Test Steps PDF | `$ adb pull /data/misc/ecuidentity/ecu.crt`；`$ adb push ecu.cacert /data/misc/ecuidentity/`；`$ adb shell "printf '\x01' > /mnt/vendor/oemkeys/ecu/state/ecucertstatus"`；`$ adb shell "od -t x1 /mnt/vendor/oemkeys/ecu/state/ecucertstatus"` |
| CS.165 sheet 第 2~6 項 | `1. corrupt CERT n in file ecu.cacert` → `$ adb push ecu.cacert /data/misc/ecuidentity/`；log `MLog.i(ECU_CERT_SERVICE_FUNCTION_ID, ECU_INSTALLATION_STATUS, status.ordinal()); value : 2 -> 1` |
| CS.98 STEPS | `/vendor/odm/etc/cert_store/`；`adb shell pm list instrumentation`（runner 檢查）；`dcl_baseline.json`／`dcl_revoked_l{0..3}.json` |
| 037 CertProvider | `$ openssl verify -verbose -CAfile RootCert.pem -untrusted L1.pem -untrusted L2.pem -untrusted L3.pem SAMcert.pem`；`$ openssl x509 -in leaf.crt -text` |
| 037 LOGENC-006 | `$ adb shell getsslog`；`$ ./logdecrypt_Ver2.sh HUssss_YYYYMMDD_HHMMSS_XXXX.enc HUssss_YYYYMMDD_HHMMSS_XXXX_enckey.enc` |
| 037 KeyInstall | `installstate` 值 `NOT_INSTALLED (0)`…；`status 0900 0000 0000 0000`；路徑 `/mnt/vendor/oemkeys` → ER 以 `$ adb shell cat`／`od` 讀 |
| 037 SAM | `/data/vendor/dauth`、`/ts`、`/vc`、`/seqId` → 同上 |
| CS.212 第 7/10/11/12 項 | `adb logcat -c`；tag `sec_pki_ki_installer_lib`、`sec_pki_ki_daemon`、`KeyMasterWrapperService`；`invalidKeyBlobEncryptRSAErrorFlow` |
| R1L Diag SWQT | `$ adb root` → `$ adb shell` → `ps -A | grep keyinstall` → `kill <pid>`；UDS `22 FF 02`／`22 29 65`／`10 60` |
| CS.00102 | UDS `22 F1 B6`、`22 20 31`、`22 29 55`／`29 5D`／`29 5E`、`22 29 65`（ADA+）、`31 01 F0 00`、`31 01 FF 01` |

## §3　執行層任務增補

- 任務 3-E `step_assets.tsv` 加欄 `channel(ADB|UDS|HOST|CAN|PHYS|UI)` 與 `observe(LOG|FILE|UDS|RC|HOST|CAN|UI)`。
- 任務 5 profile：lint 候選 **`C`（channel）**：Procedure 之每一編號步驟其後必有 `$` 指令行，或步驟句以 `Insert`／`Press`／`Power cycle`／`Disconnect`／`Select "` 起首；否則 FAIL。ER 每行須含 (c) 七類之一的手段標記；否則 FAIL。Pilot 前先以 pm_29／SWC 0708 跑一次假陽性率回報。
- `trace_summary.md` 加一節：70 列中，依 §2 素材能否為**每一步**配到通道句之預判（Y／部分／N），N 者即 batch 內之 PENDING 候選。

## §4　上繳要求增補

- `step_assets.tsv` 含 channel／observe 欄之計數。
- lint `C` 之假陽性率（對既有兩本）。
- 「每步可配通道」預判表。
