# 下放包 CAM-05 —— pilot 收尾修正 ＋ 全量批①（Display Arbitration）

指派執行層 session：延用。對應審閱：`down/20260916_CAM-04_review.md`；Pei 2026-09-16「准」（§三四項）。

---

## §0　禁區

同 CAM-03 §0。本包觸及：`pilot02/` 原地修正（十列）、`generated/batch01/` 新建。不寫回工作簿。

## §1　裁定落檔

取號現查：`{live}` 期望值 **12**。

```text
R-CAM12  CFTS092 疊層同句之取錨
  CFTS092 於 Cargo/CHMSL（§1.3.5）、Rear Camera（§1.3.6）、Surround View（§1.3.7）、
  Forward Facing（§1.3.8）各節重複出現之逐字同句，Test Group = Rear View Camera 之 TC
  一律取 Rear Camera 節（`SYS-RA-CAM-070`～`-082`，ObjectID 4781635～4781647）之 ObjectID 為錨；
  其他節之同句不得作錨，reasoning 得互參。
```

- **pilot 判過**：DECISIONS 6-11 `[DECIDED]`「pilot02 經 CAM-04 審閱 §二五項修正後判過；全量不跑 v3」。
- **A-CA26** RESOLVED（前提不成立：逾 50 token 依 §4.3.1 摘句，不改驗證點）。
- **profile** 加：§4 Pre-Condition 定序（電源 → PROXI → CAN source → 品牌 → 畫面；開機類 TC 首行為 off 態）；§7 摘句提醒（附 V3-260 案例）；R-CAM12。
- **GC_BACKLOG** 追加 R-TM13 同級錨實作備註。

## §2　pilot02 五項修正（原地）

| TC | 修正 |
|---|---|
| 001 | 錨改 `CFTS092-4781640`（`SYS-RA-CAM-075`）；Pre-Condition 1 改 off 態慣用句（自 VF230／power_moding 交付本抄，回報例句與列號）；刪與之互斥之行 |
| 003 | 錨回 `VF551_V2_PHDCC27_VF_…`（`SYS-RA-VF551_V2-498`），verbatim 摘句 ≤ 50 token（保留 `When … CmdIgnSts = RUN … ShiftLeverPosition = R > Reverse_Deb` 條件與 `display the RVC image … Automatic Display Mode` 結果子句，刪列點符號與中段贅字，摘法回報）；Pre-Condition 回「No camera image is displayed」 |
| 004 | 錨改 `SYS-RA-VF551_V3-260`（§1.10.2.2）同法摘句；Pre-Condition 同 003 |
| 009／010 | Pre-Condition 4 改 `The camera delay setting is set to "On"`；reasoning 註品牌 label 由 -007／-008 承接 |
| 007／008 | ER2 改 `The "Settings" screen is displayed` |

其餘六列以同規則自查（Full-Operation 只用於已上電情境；品牌行只在操作品牌 label 之列）。lint ＋ selfcheck 全綠；上繳附欄級 diff。

## §3　全量批①：Display Arbitration（`-015`／`-016`／`-018`／`-020`），上限 40 TC

**步驟 A — 選列計畫（先於生成，上繳前半）**
每 SWE 列出其 in-scope 來源（排除 Out of Scope 與 R-CAM10 委派他列者）→ 依 Layer 3（VF 章節）聚成驗證點 → 每驗證點標軸（車型／品牌／boundary／mode／none）→ 預估 TC 數。表落 `features/camera/data/batch01_plan.tsv`（`swe_id, source_id, anchor, vf_chapter, verification_point, axis, tc_count, note`）。

分析層預期之驗證點（執行層以來源核實，不受此限）：
- `-015`：自動啟動（Atl-Hi／Atl-Mi 二列，即 pilot 003／004）；手動→自動切換（V2-488／V3-266 二列）；Reverse_Deb 去彈跳（=Deb／<Deb 二列，boundary）；自動模式下退出 R 之 Ttimer1 起算（V2-492～497，車型分寫）；速度門檻於自動模式（V2/V4 `>=`、V3 異名、V33 `>`、V42 待來源；A-CA21 具名）。
- `-016`：App Drawer 開啟／`"X"` 關閉（pilot 005／006）；速度限制之旁路（`VehicleSpeedVSOSig` 高於門檻時手動啟動仍顯示，V2-541～549）；`"X"` 於 R 檔不出現（V2-533／V4-115／V42-299 三本同義 → 一列，車型分寫 CAN source）；controls page／camera app 入口（DR-CAM-g 未結 → `PENDING`，不生成）。
- `-018`：Delay On 10 s（pilot 007／008）；Delay Off 立即退出（品牌二列）；速度邊界 on／off（pilot 009／010）＋ V33 `>` 分支（2261）＋ V3 異名（376）；`"X"` 於延遲期內手動關閉。
- `-020`：`BED_EXTENDER.BedExtenderSts = Active` → 無影像（V33-227，2261 only）；`IncompleteBedExtenderSts = True` 同上。CEH 表該三訊號 Harman=N／Not yet —— **仍依規格生成**（CAN bench 可送），reasoning 記 CEH 狀態，登 ANOMALY「實作可能未接線」；DBC 查無該訊號則 `PENDING: DR-CAM-{n}`。

**步驟 B — 生成**
依計畫生成，ID 自 `NR1L-RVC-011` 起；pilot02 十列不重生（計畫中對應驗證點標 `covered_by: NR1L-RVC-00n`）。全套 canon ＋ profile；Vehicle Model 1/0；R-CAM3(e)／5(e)／11／12。lint（含 Z）＋ selfcheck 全綠。

## §4　上繳要求（`up/20260916_CAM-05.md`）

1. R-CAM12 sha8；DECISIONS／ANOMALIES／profile／GC_BACKLOG 更新。
2. pilot02 五項修正之欄級 diff；off 態慣用句之出處；003／004 摘句前後 token 數。
3. `batch01_plan.tsv` 全表 ＋ 每 SWE 之 TC 數；被排除來源（Out of Scope／委派）逐列。
4. batch01 產物清單、lint、selfcheck；PENDING 列表（DR 號）。
5. 自報錯誤；新 DR。

## §5　升級條件

- 計畫 TC 數 > 40（停於步驟 A，回報切法）。
- `-020` 之 `BED_EXTENDER` 於 P363／637MCA DBC 皆查無。
- 任一驗證點之來源全為 Out of Scope 或全被委派（該 SWE 無可寫列 → 回報，不硬寫）。
- 摘句無法在 50 token 內同時保留條件與結果子句。
