# 下放包 GC-09 —— R-G70 v4 施作、R-TM14 候選表、R-G71 接手清單（2026-09-05）

Pei「追認 准」（2026-09-05）：GC-07 審閱 §一 R-G70（含 (d) PROXI 採 SWC 式）、§二 R-TM14、§三 各裁全數追認；本包准。
前置：本包改 canon 本體（IN §8.7.5）與 lint P 判準，皆 Pei 已准。**不回修任何交付本**（R-TM13／R-TM14：回修只以新版次，且須 Pei 圈選後另包）。

## 一、執行層任務

### 1　R-G70 落檔（R-G23 現查後）
1-1　現查 `### R-G` 最高號（預期 71，70 未佔）；台帳落 `### R-G70 —— 訊號與參數寫法 v4：來源層級與格式（全域）`，條文全文 = GC-07 下放包 §二擬文 ＋ 審閱 §一 之 (d) 修訂（SWC 式 `PROXI <Param> = <value>`；VF230 `is set to` 式列同義舊式）。
1-2　`IN §8.7.5`：本體改 v4；v3 條文依 R-TM13 加 `~~ ~~` 與撤銷註（理由指 GC-07 §二／1-1 節、GC-07 審閱 §一）；沿革節加 v4 段；§8.7.5 開頭之來源分線段指 R-G70(a)。
1-3　`docs/runtime/profiles/vehicle_setting`：`[OVERRIDE §8.7.5]`（R-VS52／R-VS67）標記「2026-09-05 併入全域 v4，本 override 作廢」，條文不刪；`features/vehicle_setting/RULINGS.md` 對應二條加註。
1-4　`rulings_hash --out` 乾跑證 R-G70 入表、撞號 +0；**不寫表**。

### 2　lint P 回 v4 判準
2-1　先量：`scripts/lint036.py` 之 P 檢查現行判準（v2 `Send CAN:` 全域、`--profile` 之 v3 分支）逐行；既有八本基線報告之 P 數。
2-2　改：全域判準 = v4 (b)(c)(d)(e)；`--profile` 之 v3 分支移除；PROXI 判準 SWC 式為合規、`is set to` 式報 WARN（舊式，不 FAIL）。
2-3　對照向：八本基線 P 數改前／改後逐本列（預期 v2 本不變或降；pm_29 之 P 升為 v3 式列數 ~207）；`tests/test_lint036.py` 更新對應測試，增紅 0。

### 3　pm_29 之 207 列 dry-run（不寫回）
`features/power/sandbox/b29/pm_29.xlsx` 逐格掃 v3 式（`$MSG.Sig$`、`Send the signal`、`The signal value … is received`、`is registered without a bus error`），輸出 `features/power/reports/pm29_v4_dryrun_20260905.tsv`：`row / col / old_text / new_text / rule`。轉換規則逐字取 R-G70 (b)(c)；PROXI 85 處若已 SWC 式則不列。附分子分母（掃格數／命中格數／涉及列數）。**xlsx 一位元不動**。

### 4　R-TM14 候選表
母體：`features/*/delivered/*.xlsx`（`git ls-files` 實測列出；`archive/` 不含）。每本以 `--profile` 含 X 與 P(v4) 跑一次，輸出 `docs/reports/rtm14_candidates_20260905.tsv`：`feature / file / sha8 / rows / X / P / P_warn / candidate(Y/N)`，`candidate = (X>0 or P>0)`。報告落各 `features/<f>/reports/`（R-G47）。**只報不改**，圈選屬 Pei。

### 5　R-G71 接手清單
`FEATURE_ONBOARDING.md` 接手清單（§4 或相應節，先量位置）加二項：新 feature 之 profile 一律啟用 lint X 與 P(v4)；`§5.3` 三常數之 PENDING 二條於該 feature 之 DR 內承接（不得複製 PENDING 到新 feature 而不登 DR）。以 `[ADD]` 註標明 R-G71。

### 6　`DATA_REQUESTS.md` 機讀狀態欄（格式先定，不回填）
於 `docs/fw036/templates/`（或 FO 相應節）定格式：每 `## DR-n` 節首行 `status: open|sent|closed（YYYY-MM-DD）`；GC-07 上繳 §5 所列有 DR 之 feature（power、vehicle_setting、time_management、vehicle_category、driver_distraction、power_moding、bed_lowering、sw_update、vsm_v42、vsm_v43）**只加標頭說明，不回填既有節**。附「可機讀節數／總節數」分母。

### 7　上繳 `up/20260905_GC-09.md`：產物 sha、乾跑表、lint P 對照向八本、pm_29 dry-run 統計、候選表摘要、對照向、工具寫入路徑清單、自報。

## 二、Pei 動作（本包後）
1. `rulings_hash.py` 重生（+R-G70）→ `--check` → commit
2. 圈選 `rtm14_candidates_20260905.tsv` 之 `candidate=Y` 列（回覆逐本 Y/N），GC-11 據此出新版次
3. DR 草稿取號送出

## 三、未結 DR
DR-VS{n}（Power_Tailgate_Enable）、DR 草稿（HMI entry path ×2＋HMI 標籤取捨）皆未送出。
