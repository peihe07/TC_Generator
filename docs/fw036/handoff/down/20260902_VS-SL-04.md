# 下放包 VS-SL-04 —— vf230 (3) 分支二次查找＋沙盒稿重生＋DR 稿補節

日期：2026-09-02　層級：Tier 2 → Tier 1
前置：`down/20260902_VS-SL-03_review.md` §2（R1／R2／R3 之令與證據）
上繳寫 `up/20260902_VS-SL-04.md`。BL／VC 沙盒稿本包不動。

## §1　二次查找（只對 v3 (3) 分支 105 列）

依審閱 §2 逐列判 R1／R2／R3，出
`features/vehicle_setting/reports/_v4_branch3_resolution.tsv`：
`row`｜`tc_id`｜`setting`｜`resolution`（R1/R2/R3）｜`proxi_added`｜`evidence`（FIP No.＋proxi_values 鍵）

- **R1 家族閘**：`SWITCH 1–4` → `PROXI AUX_Switch_Types = 1 (Type 1)`；
  `SWITCH 5–6` → `PROXI AUX_Switch_Types = 2 (Type 2)`。label 取 proxi_values 逐字（`Type 1` 含空格）。
  其他名若別名 evidence 指向之家族列亦為變體數閘，同法；無把握者落 R3，不猜。
- **R2 `FIP_ALWAYS_OFF`**：該設定名（或其 evidence 明指之 FIP 列）之 Atlantis 欄為
  `Always false`／`Always return value is 0` → 掛登記旗，Pre 不加 PROXI、不 PENDING。
  已證者：`Suspension Default Ride Height`（No.274）、`Phone Repetition`（No.215）。
- **R3**：其餘維持 `PENDING`，供 DR 第四節。

自檢：R1＋R2＋R3 = 105（assert）；R1 之 54 列逐列有 `proxi_added`。

## §2　vf230 沙盒稿重生

- 先 `cp vf230_vssl.xlsx vf230_vssl.v3.bak.xlsx`，再就地重生
- R1 列補 PROXI 行；R2 列不加行、旗入報告；R3 列維持 PENDING
- lint（括號下半／尾句號／雙引號／Proc-ER 步數）重跑，三項 0 方過
- v4 報告：`vf230_settings_dryrun_v4.{tsv,md}`（分支分布重列）

## §3　DR 稿補第四節

`dr_draft_vssl.md` 加：R3 殘餘之設定名逐名列 TC 引句（逐字）＋兩表查無之證（R-G13 三要件）。
既有三節不動。

## §4　上繳

`_v4_branch3_resolution.tsv`、重生後沙盒稿、v4 報告、補節後 DR 稿之路徑與 SHA256；
自檢（§1 assert、lint）；自報錯誤。上繳准後即送 Pei Excel 抽驗（含 BL／VC 稿）。
