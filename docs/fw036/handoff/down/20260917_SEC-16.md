# 下放包 SEC-16 —— CCVR v2.7 工作中版（20260917）登錄 ＋ ECUCert 路徑衝突 Remarks 註記（無裁定、不跑 `--gate`）

前置：`down/20260917_SEC-15_review.md`；Pei 已落 SEC-12~15 commit。
本包**不動 RULINGS.md／RULINGS.sha.tsv、不跑 `--gate`**（GC-16 落地前 Security 線禁令）。
執行層 session：新開。

---

## §0　禁區
- `down/`／`up/` 既有檔不動；canon／framework／lint 不動；RULINGS 兩檔不動。
- Procedure／ER **不改**（兩套路徑皆非 037 母體，未裁前不擇一）；只動 Remarks、ANOMALIES、MANIFEST、`asset_request.md`。

## §1　登錄
| 檔 | sha16 | 處置 |
|---|---|---|
| `2_Architecture/CCVR/21047_24_00081_…v2.7.xlsx`（mtime 2026-09-17 14:35，112,016 B）| `4e13ee7efbeaf29b` | MANIFEST 新列 `ccvr_v27_wip_20260917`，`note = working copy on v2.7; ECU ID CS.165 status column + 16 engineering note rows (Samuel); Revision sheet unchanged`；原 `SYS2_Mapped` 登錄不動 |
| `Security/REF/…v2.7.xlsx`（112,416 B）| `bb8f5cae7635ec36` | 只記 sha，`note = same as wip except DTC-CS.99 owner column`；不投遞 |

## §2　差異落檔（執行層以三方比對複驗；分析層實測）
`ECU ID CS.165`（wip vs SYS2_Mapped 原 tab）：
- 新增狀態欄；item 1／10／11／13／14 = `out of scope : wait confirm (2026/09/15)`。
- 新增備註 16 行（r23~r38），逐字抽出登 `features/security/data/ccvr_wip_20260917_notes.tsv`（欄 `row | text`）。
- 其餘 15 個 sheet：`Secure Log CS.212` 一格空白差異、`DTC-CS.99` owner 欄（`seure boot(harman)`／`steven(DCL?)`／`samuel`）。

## §3　ANOMALIES
```text
[A-SEC-16] ECUCert 路徑兩說：Test Steps PDF 20260826（TC 依此）寫 /mnt/vendor/oemkeys/ecu/state/ecucertstatus（od -t x1）、
           /data/misc/ecuidentity/ecu.cacert、ecu.crt；CCVR v2.7 工作中版 20260915 備註寫
           /mnt/vendor/ecuidentity/state/ecucertstatus（od -An -t u1）、主路徑 /mnt/vendor/ecuidentity/data/{ecu.cacert,ecu.crt}
           （/data/misc/… 為 back-up）、L0 /odm/etc/cert_store/ecu_chain。037 未載路徑，兩者皆非母體；TC 不改，請 Samuel 確認現行。
[A-SEC-17] CCVR ECU ID item 1／10／11／13／14 於 20260915 標 out of scope wait confirm；涉及 CSR 匯出／STLA script（ECUC CSR TC、X-d/X-k）；
           STLA 確認後可能影響 ECUCert 之 batch 判定與 X-d。
```

## §4　Remarks 註記（Revise → `security_v10`，只 Remarks）
ECUCert 本內凡 Procedure／ER 含 `/mnt/vendor/oemkeys/ecu/state/ecucertstatus`、`/data/misc/ecuidentity/`、`/odm/etc/cert_store` 之 TC，Remarks 加一行：
`path per ECU Cert Test Steps PDF 20260826; CCVR v2.7 working note 20260915 states /mnt/vendor/ecuidentity/... as primary — confirm with Samuel (A-SEC-16)`。
Cert Provider 本內含 `/odm/etc/cert_store` 者加 `L0 chain path per CCVR note 20260915: /odm/etc/cert_store/ecu_chain — confirm (A-SEC-16)`。
v09→v10 逐格 diff 預期只在 `remarks`；六本重生、`_all` 重生；lint 門檻 0。

## §5　`asset_request.md`
`X-d` 段加：`Samuel: also confirm ECU cert partition paths (A-SEC-16) and item 1/10/11/13/14 scope (A-SEC-17)`；DR-m 段加 owner 提示（`DTC-CS.99 owner column: steven(DCL?) / samuel`）。

## §6　上繳要求（`up/20260917_SEC-16.md`）
1. MANIFEST 兩列；`ccvr_wip_20260917_notes.tsv` 16 列全文。
2. 三方比對之逐 sheet 計數（與 §2 相符）。
3. v09→v10 diff（Remarks 格數，預期 ECUC ≤ 20、CP ≤ 6）；六本 sha；lint。
4. 確認 RULINGS 兩檔 sha 前後相同（不動之證）。
5. 自報錯誤。

## §7　升級條件
- diff 落在 `remarks` 以外。
- RULINGS 兩檔 sha 改變。
