# 下放包 CAM-10 —— CAM-09 審閱落檔 ＋ batch03a（`-008`／`-009`／`-010`，23）

指派執行層 session：延用。對應審閱：`down/20260923_CAM-09_review.md`（同發）。

---

## §0　禁區

同 CAM-03 §0。本包觸及：條文／profile／RDF／DR、`-108` reasoning 一句、`batch03a/` 新建、`coverage.tsv`（若已存在則加兩行；不存在則於 `features/camera/data/` 建，格式沿 vehicle_setting 之 `coverage.tsv`）。不寫回工作簿。

## §1　落檔

取號現查：`{live}` 期望值 **16**。

1. **R-CAM16**：全來源委派之 SWE 列，`coverage.tsv` 記 `No TC — all sources delegated to SWE-CAM-nnn (R-CAM10)`，`tc_count = 0`，非缺口；`-007` → `-006`、`-013` → `-004` 兩行。
2. DR-CAM-p（LVDS 訊息週期）。
3. RDF-08（`V3-281` 標的）、RDF-09（`V3-283` 標的）、RDF-10（`V42-590` 同句）。
4. profile §9 加 `TIME_W_RVC` 2,5 s／`TIME_W_RVC2` 50 ms；§7.3 加「DTC 讀取以散文 `Read the DTC list with the diagnostic tool and check that …`，LVDS 以 bus analyzer，皆不造命令」。
5. DECISIONS 6-28～6-33：雙向解讀准、R-CAM16、難點 A～E 處置、`-099`／`-100` 判讀准、切法准。
6. `-108` reasoning 加「判定以 bus analyzer 時戳為準」；交付前實機驗證清單（`features/camera/data/bench_verify.md`，新建）記 `-058`／`-092`／`-108`。

## §2　batch03a 生成：`SWE-CAM-008`（10）＋ `-009`（2）＋ `-010`（11）＝ 23，ID 續 `-118`

依 `batch03_plan.tsv`。要點：
- LVDS 訊號（`gridZoomRequest.*`／`systemStatus.*`／`vehicleUpdate_*`）以 bus analyzer 觀察，label 逐字（profile §7.2）；CAN 側輸入依 §8.7.5(c) 取 DBC `VAL_`。
- `V2-479`／`-480`（兩週期）時序面 `PENDING: DR-CAM-p`。
- `-009`／`-010` 之 Out of Scope（RVCM 端 `systemStatus.*` 之 acquire）不作錨；`V2-578`／`-580` 可作 Pre-Condition 措辭（R-CAM9）。
- `V2-486`（80t）、`V3-284`（78t）、`V42-326`（73t）摘句，保序子序列。
- `-009` 之 `DynamicGridSts`（CEH Not yet）以 HMI 現象（guideline 顯示）為 ER（A-CA16）。
- R-CAM3(f)／R-CAM15／R-CAM16 適用。

## §3　上繳要求（`up/20260923_CAM-10.md`）

1. R-CAM16 sha8；DR／RDF／profile／DECISIONS；`coverage.tsv` 兩行；`bench_verify.md`。
2. batch03a 23 列產物、lint、selfcheck（八項）、PENDING 列表、摘句 token 表。
3. 自報錯誤；未結。

## §4　升級條件

- batch03a > 30。
- `gridZoomRequest`／`systemStatus` 之值於 SYS2 只有訊號名無值（該列 PENDING，回報數）。
- 任一摘句無法在 50 token 內保留條件與結果子句。
