# 下放包 CAM-05 審閱 —— pilot02 修正 ＋ batch01 抽讀（分析層 2026-09-16）

對應上繳包：`docs/fw036/handoff/up/20260916_CAM-05.md`
判定：**pilot02 五項修正通過，pilot 正式判過**；batch01 23 列 lint 全綠、verbatim 33/33 機器核對通過；抽讀 6 列（013／017／021／023／026／030）發現 **1 處重複性瑕疵 ＋ 3 處措辭**（§二），修正後 batch01 判過。R-G14 計數：batch01 為第 1 批（pilot 不計）。

---

## §一　確認

1. R-CAM12 sha8 `852c3725`／`d284ac78`；節界實測「只有一組疊層同句」記入 profile §6。
2. DECISIONS 編號衝突（6-11 → 6-12）：下放包錯，執行層處置正確。
3. `Standby` 首行取自 pm_29（19 例）：接受。`Full-Operation` 有無 `the` 之兩本差異：**沿 VF230（有 `the`）**，因 R-CAM11 已定 VF230 為 Camera 之 VF 型基準；profile §4 記明。
4. 摘句六列皆保序子序列、≤ 50 token；`-016` 補句號之自報已改：接受。「保序子序列」作為摘句之機器判準，記入 profile §5。
5. R-CAM10 委派：`-018` 之「Delay Off 立即退出」由 `-013`／`-014`（req `-015`）承接：接受，plan.tsv 已記。
6. `-020` 依規格生成、PENDING DR-CAM-f、A-CA29：接受。
7. selfcheck 第二次攔下錯錨（`-023` 初稿）：工具價值確立。

## §二　batch01 抽讀瑕疵

| # | TC | 瑕疵 | 依據 | 修正 |
|---|---|---|---|---|
| 1 | **023** | Pre-Condition 1 `Full-Operation` 已含 RUN，步 1 又 `Send CAN: CmdIgnSts = 4 (RUN)` —— 與 pilot 審閱 §二-1 同型，**第二次出現** | §4.4 | 刪步 1（ER 重編）。**全批 23 列以「Pre-Condition 為 Full-Operation 且步 1 為 CmdIgnSts = RUN」為條件掃描**，回報命中清單並一律刪除；此規則併入 selfcheck |
| 2 | 021 | Pre-Condition 只有「Automatic Display Mode 影像已顯示」，退 R 後影像是否續顯取決於 Delay 設定，未定即不可判（執行層 §5-5 自報） | §4.4、§8.7.1 | 加 Pre-Condition `The camera delay setting is set to "On"`（續顯窗因而確定），reasoning 註 Delay 為續顯之前提 |
| 3 | 026 | ER2 `stays displayed until Ttimer2 maxes out` —— 讀取當下不可判「until」 | §6 可判性 | ER2 改 `The rear view camera image is still displayed`；Ttimer2 到期之退出另由 `-029`／或 `-028` 承接（reasoning 已委派） |
| 4 | 030／031 | V33 verbatim 之條件含 `LTM_OperationalModeSts.Info = "Ignition_On_EngOn"`，Pre-Condition 未表達引擎運轉態 | §8.7.1 | 加 Pre-Condition `LTM_OperationalModeSts.Info = "Ignition_On_EngOn"`（來源名保留，§8.7.5(f)）；其 CAN 對應同 DR-CAM-i 之範圍，reasoning 註 |

其餘抽讀列（013／017）無瑕疵；未抽讀之 17 列由執行層依上述四條自查回報。

## §三　待 Pei 裁

| # | 項 | 建議 |
|---|---|---|
| 1 | **A-CA28**：`-016` 引用之 12 列 CFTS092 屬 Cargo／SVC／FFC 節 | **同意不生成**（Test Group 不符，§8.4.2）。惟此為 037 之 RD 缺陷（SWE-CAM-016 混引四節），**登 RD 反饋清單**（`features/camera/RD_FEEDBACK.md`，若無則建）供 Pei 回 RD 作者；TC 側不承接 |
| 2 | **A-CA30**：`>` vs `>=` 於 CAN 解析度下不可判 | **同意**。處置：`-030`／`-031`（2261，V33 `>`）與 `-009`／`-010`（Atl-Hi，`>=`）**維持分列**（verbatim 條文不同，追溯不同）；**376（V3 `BRAKE1`，`>=` 同判準）併入 `-009`／`-010`**，以 R-CAM3(e) CAN source 行寫 `BRAKE1.VehicleSpeedVSOSig (376)` 並勾 376=1；**637（V42 無速度退出條文）**登 coverage 註於 plan.tsv，不硬寫 |
| 3 | 「proc ≥ 2 步」自檢 | **併入 `selfcheck_r_cam10.py`**（更名 `selfcheck_camera.py`，含：R-CAM10 錨、步數 ≥ 2、§二-1 之 RUN 重複、verbatim 保序子序列） |
| 4 | batch01 判過條件 | §二四項 ＋ §三-2 之併列修正後，**判過**，計 R-G14 第 1 批 |
| 5 | 下一批 | batch02 = Startup and Shutdown（`-001`／`-012`／`-014`）＋ Configuration（`-002`／`-005`）＋ State Handling（`-003`／`-017`），上限 40；`-017` 之 RD 錯位（A-CA02）先依 Description 寫，VC/VM 不採 |

## §四　CAM-06 內容（准後發）

§二修正 ＋ §三-2 併列 ＋ selfcheck 擴充 ＋ RD_FEEDBACK 建檔 → batch01 重跑 lint／selfcheck → batch02 選列計畫 ＋ 生成。

## §五　備查

- DR-CAM-a／f／g／h／i 未結；f、i 為交付前必結。
- A-CA30 之根本解需上游確認 `c_VEHSPD_MAX`／`MAX_SPEED` 之標定單位（A-CA22），列 RD_FEEDBACK。
