# CDD-04 審閱 —— Revise（R-DIAG13/14）＋ batch 2

審閱對象：`up/20260917_CDD-04.md`。審閱層 session：2026-09-17。
**結論：通過。** batch 2 = 81 TC，合併本 133 列；PENDING 0；lint 真違規 0；§9 17/17。
**三處內容修正併入 CDD-05**（§三）；兩項判準以裁定調整（§四）。

---

## 一　逐項核對

| up 節 | 判 | 備註 |
|---|---|---|
| §1.2 指紋 58 → 60，無覆寫 | 通過 | 「第五次事件已於 CDD-03 提交前修復」—— CDD-03 上繳記為第四次；執行層於 CDD-05 回報一句：事件累計數與各次時點表，避免計數漂移 |
| §2.1 R-DIAG13 四條 PC-3，`pc_sources` 用不同字串區辨保守套用 | 通過 | 字串區辨是好做法，寫入 profile |
| §2.2 `-043` 改鍵、`-044` 不涉 | 通過 | |
| §2.3 `-006` PENDING 改佔位（超出 T1(a)） | **追認** | 同一合併本對同一欄位不得兩式；理由成立 |
| §2.4 FB-k／l／m | 通過 | 累計 13 項 |
| §2.5 `SEC-DIAG`／`KEY-DIAG` 以號段常數定母體 | 通過 | 9 項自測全 PASS |
| §3.1 `$5000`／`$5001` controlOptionRecord 全未載，以 `<選項名 selection>` 佔位、DR-DIAG-4 補強問句 | 通過 | 未依 ISO IOCP 通用值 —— 正確；**佔位追蹤制見 §四-3** |
| §3.2 sibling plan 81 | 通過 | `-049`／`-106` CFTS004 明載 `$22` 故單條；`-145` Hold 獨立成步 —— 判準一致 |
| §3.3 `-062` `$D013` 無章節 → 以 `$5000` 為受測 DID | **追認**（附條件） | Remarks 不記內部代號；`split_reason` 記 CFTS004-4940296 之位置與 `$D013` 缺章節；FB-l 回覆若指向另一 RID 則 Revise |
| §3.3 `-048` session 軸不產 | **追認** | 無 NRC 可斷言；`0x7F serviceNotSupportedInActiveSession` 不在封閉五碼，不擴 |
| §4.1 `J=2` | 見 §四-1 | |
| §4.1 `SEC-DIAG=19` | 見 §四-2 | |
| §4.2 baseline 重犯 → profile 明文 | 通過 | 兩次同因，profile 規則化正確 |
| §4.3 (d) 所有格撇號誤判 | 通過 | 自檢正則修正合理 |

## 二　分析層自報錯誤

| # | 內容 |
|---|---|
| A16 | CDD-04 §2 T2 定佔位制取代 PENDING，未同時定佔位之追蹤方式；Security 線 R-SEC21 已有前例（`placeholder_summary.tsv`）而未沿用 |
| A17 | CDD-04 T1(d) `SEC-DIAG` 母體寫「I/O Control 之 TC」，未排除 unsupported 型；追補 A 改以母節判定時亦未想到 |

## 三　內容修正（併入 CDD-05）

**(1) `-107`／`-108` 之 `test_item` 上半小寫起首** —— 不改 lint，改用 **R-4**（canon §4.3.1 末句：
verbatim 自原句中段起抄時，句首字母轉大寫屬排版正規化，允許）：`when tester command…` → `When tester command…`。
`J` 不豁免；`[A-DIAG31]` 以 R-4 結案。**全合併本 grep 上半小寫起首者一併正規化。**

**(2) `NR1L-DIAG-102` 之步驟 2／4／6 `Listen to the … speaker output`** —— 為純觀察步驟，無執行動作，
違 IN §5.1（`observe`／`watch`／`monitor` 類不得為主動詞；聽即觀察）。
改為：步驟只留動作與保持 —— `1. Select … $ 2F 50 00 <…>`／`2. Hold for 10 s`／`3. Hold for 10 s`／`4. Hold for 10 s`／`5. Disable … $ 2F 50 00 <Tones Off>`；
ER 對應：1 正回應；2 `The tone is output on the Front Left speaker output for 10 s`；3 `…on the next speaker output in clockwise order for 10 s`；4 `…on the remaining speaker outputs in clockwise order and the rotation repeats`；5 正回應且停止。
**全合併本 grep `Listen to`／`Observe`／`Watch`／`Monitor` 起首之步驟，同法改。**

**(3) 佔位追蹤** —— 見 §四-3 之 R-DIAG15；CDD-05 產 `placeholder_summary.tsv`。

## 四　審閱裁定（依常規即生效）

```text
R-DIAG13(amend)  SEC-DIAG 母體排除 unsupported 型
  R-DIAG13 之「SID 0x2F 之 TC」指實際送出 0x2F 者：positive 與 negative 型；
  unsupported 型送不支援之 SID，不在母體。lint SEC-DIAG 母體加條件 row_kind ≠ unsupported。
  已產之 unsupported 型 TC 不補 PC-3。
```

```text
R-DIAG15  佔位之追蹤（比照 R-SEC21）
  作者側四欄之 `<…>` 佔位一律登 `features/diagnostics/sandbox/placeholder_summary.tsv`
  （欄：TC ID／SWE1 ID／欄位／佔位文字／缺件 token DR-DIAG-n／來源節）。
  交付本可含佔位（U=0）但 `asset_request.md` 須載明「佔位填入前不得執行」；
  DR 回覆到件後依 R-G72 以 Revise 本填值。
```

```text
R-DIAG16  觀察不成步（IN §5.1 之本 feature 適用）
  Procedure 步驟只寫動作（送出／按壓／保持／讀取）；聽、看、觀察只入 ER。
  保持步驟固定句 `Hold for <n> s`，ER 寫該期間之可觀察結果。
```

R-4 之適用（§三-1）不另立條，記於 profile。

## 五　待 Pei

1. GC-16 仍待兩線停止 ＋ commit；Security 線 R-G75 轉達。
2. FB-DIAG-a～m 送出。
3. commit：建議准，訊息沿 up §9。

## 六　CDD-05 隨本審閱落檔

= §三三處 Revise ＋ R-DIAG13(amend)/15/16 落檔 ＋ `placeholder_summary.tsv` ＋ **batch 3 = Layer 2 #13～#17**
（Audio Tone Settings 28／Tuner Control 22／Mode Selection 9／In Motion Menu Control 6／Video Input 5 = 70 列，I/O 母節收尾）。
