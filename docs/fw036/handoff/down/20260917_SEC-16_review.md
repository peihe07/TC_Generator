# SEC-16 審閱 —— 上繳包 `up/20260917_SEC-16.md`（CCVR v2.7 工作中版登錄 ＋ A-SEC-16/17 ＋ `security_v10`）

審閱層：分析層，2026-09-17。
判定：**通過，不升級**。13 格全在 `remarks`；四本 sha 不變為「只動該動處」硬證；RULINGS.md 未動，`RULINGS.sha.tsv` 之變動歸因 CDD 線 `R-DIAG19`。
執行層三處與下放包不符**皆為分析層量測錯誤**，執行層以實測為準正確。

## 一、分析層自報（登台帳）
| # | 錯誤 | 說明 |
|---|---|---|
| A-37 | 「新增備註 16 行 r23~r38」 | 我是拿 wip 與 SYS2_Mapped 之備註區以「首三欄不在對方」粗比，把既有行也算進去；正確為相對 mapped 新增／變更 **11 行**（r25／r30~35／r38／r39／r41／r42）|
| A-38 | item 1 之狀態字樣 | item 1 只有 `out of scope`（`discussing with STLA`），無 `wait confirm` |
| A-39 | `Secure Log CS.212`「一格空白差」| 實為 wip 多出一整段 system-property 描述（未截斷讀完）|
| A-40 | `asset_request.md` 有 DR-m 段 | 該檔只列 `X-`；owner 提示落 `DATA_REQUESTS.md` 正確 |

## 二、採納
- `DTC-CS.99` owner 兩式並存（wip `c5` 有值／mapped `c0` 只表頭）：只引 wip 值，正確。
- 註記規則寫在 `gen_batch1.py`（路徑判準）而非逐 TC 硬編：准，且是對的——路徑異動自動跟隨。
- `EXEC_GUIDE.md` 不重生（不取 Remarks）：准。
- `RULINGS.sha.tsv` 之逐列歸因表：准；此即 GC-16 前的正確報法。

## 三、對 Pei 的實質訊息
ECU ID 狀態欄與 Samuel 備註**9/16 本就有**（wip↔REF 在 `ECU ID CS.165` 無差），9/17 只加了 DTC owner 欄與 CS.212 一段。故路徑衝突（A-SEC-16）早在我們寫 ECUCert TC 之前就存在於 REF 副本——SEC-01 投遞時我只登了 sha 沒打開讀 REF 本，登 **A-41**。問 Samuel 的問題不變。

## 四、狀態
Security 六本 `security_v10`（CertProvider `c3d31bc8257b2798`／ECUCert `173690bf03c5d580`，其餘四本同 v09）。Security 線**無下一包**；觸發條件同前（RD 資產、DR/A-SEC 回覆、`W=4`、自動化 go），另加 **Samuel 答 A-SEC-16 路徑 → Revise 改 ECUCert 7 TC ＋ CP 6 TC 之 Procedure**。
git：一支 `feat(security): SEC-16 …`，帶 pathspec（`sources/MANIFEST.tsv` 含 CDD 未提交列）。
