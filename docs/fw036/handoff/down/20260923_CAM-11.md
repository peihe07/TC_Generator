# 下放包 CAM-11 —— CAM-10 審閱落檔 ＋ batch03a 補三列 ＋ batch03b（`-011`，29）

指派執行層 session：延用。對應審閱：`down/20260923_CAM-10_review.md`（同發）。

---

## §0　禁區

同 CAM-03 §0。本包觸及：`batch03a/`（`-122`／`-123`／`-138` 修正、`-147`～`-149` 新增）、`batch03b/` 新建、`bench_verify.md`、selfcheck 第 8 項、DECISIONS。不寫回工作簿。

## §1　落檔

1. DECISIONS 6-34～6-39：`V2-486` 拆五准；`V3-284` 四列准；coverage 全量准；`V42-326` 擴五；LVDS 注入能力入 bench_verify；`-138` PROXI 查證。
2. profile §7 升級條件 3 改寫：「摘句不可行且不可拆時停」。
3. selfcheck 第 8 項：`PENDING:` 非行首／子項首 → 命中；`PENDING` 同一 DR 同時出現於 Pre-Condition 與 ER 而 Pre-Condition 無缺值 → 命中。
4. `bench_verify.md` 加「LVDS 模擬器注入 RVCM → HU 訊號之能力（`-131`～`-133`）」。

## §2　batch03a 修正

- `-122`／`-123`：PENDING 獨立子項；刪 Pre-Condition 之重複 PENDING。
- `-138`：PROXI 表查證（審閱 §一-6），回報掃描字串與結果。
- 新增 `-147`～`-149`（`V42-326` 之 `Medium_3450_Absent`／`Long_3800_4050B`／`Extra_Long_4050L`），PROXI 值自 `Promaster_ATL_MI` row 327 列舉逐字；`batch03_plan.tsv`／`coverage.tsv` 同步。
- 八目錄 lint ＋ selfcheck（九項）全綠。

## §3　batch03b 生成：`SWE-CAM-011`（29），ID 續 `-150`

依 `batch03_plan.tsv`。要點：
- `-011` 為 `vehicleUpdate_2` 群之 CAN → LVDS gating：每列 Pre-Condition 置 bus analyzer 行；CAN 側 `Send CAN:` 取 DBC `VAL_`／factor；LVDS 側 label 逐字。
- `V3-281`／`-283`（RDF-08／09）：verbatim 逐字，ER 以 `a)` 子句之正確標的。
- 訊號缺失（`is missing` → SNA）之列：以「停送該 CAN 訊息」表達，ER `… = SNA is sent over LVDS`；逾時值若來源有則寫，無則不造。
- 五車型：CAN 訊息名因 EE 而異者依 R-CAM3(e)／(f)；V3 列依 R-CAM15。
- 查表對映（如 `V42-326` 型）全覆蓋，每項一列（審閱 §一-4）；若因此逾 40，停於回報並提切法。

## §4　上繳要求（`up/20260923_CAM-11.md`）

1. 落檔清單；selfcheck 第 8 項對 149 列之命中與修正。
2. `-138` PROXI 掃描結果；`-147`～`-149` 產物。
3. batch03b 產物、lint、selfcheck、PENDING、摘句表；若逾 40 之切法。
4. 自報錯誤；未結。

## §5　升級條件

- batch03b > 40（停於回報）。
- `-011` 任一來源之 CAN 訊號於對應 DBC 查無（PENDING DR-CAM-f 型，回報數）。
- 摘句不可行且不可拆。
