# 審閱 —— 上繳包 VS-SL-07（賦號與前綴正名）

日期：2026-09-02　層級：Tier 2　對象：`up/20260902_VS-SL-07.md`

判定：**准，含 §5-1 之追認。** 賦號正名收訖；工具缺陷另開 VS-SL-08 修（§3）。

---

## §1　收訖

- 三本新 SHA、`.presl07.bak` 鏈 = VS-SL-06 所載、自檢九項全 PASS、`R-VS94`／`R-VS95` 落檔、
  三份 `delivery:` 宣告 yaml 解析 OK —— 收訖
- §5-3 `data_only` 之列數膨脹（1,402）自察自正、備份未污染：收訖
- VF665 自 `NR1L-VS-439` 起編：**確認**（R-VS94 之當然結果）

## §2　§5-1 賦號依列序 —— **追認**

兩項實測成立：本簿 D 欄非非遞減（首逆序在第 1 列）；R-G42 官方比較器 `req_key()`
對本批 D 值拋 TypeError（int／str 混位），「D 欄升冪」**在本批資料上未定義**。
停用未定義之序、改依列序（與 ICS 交付本形制一致）並留可重跑之路 —— 處置正確。
**本階段不重排列**：列序即 SWE1 報告序，重排屬交付時之 R-G42 一(a) 判定，
待比較器修復、可量測後再議（見 §3-2）。

## §3　VS-SL-08（已下放 `down/20260902_VS-SL-08.md`）—— 修 `req_key()` 共用碼

1. **修**：鍵之各段改為型別可比之二元組（數字段 `(0, int)`、字串段 `(1, str)`），
   使混型比較有定義；附單元測試（含 `6AuxSwitches` vs `AutoDoorLocks` 一案）
2. **回歸閘**：修前後對 `delivered/` 既有各本跑 `lint_delivery_spec.py`，
   逐本結果**必須逐字同**（assert diff = 0）；不同即停，不得順手改判
3. **量測**：以修後比較器對 vf230 轉正本乾跑一(a) 列序檢查，報 D 欄逆序對數與樣本 ——
   只量不改，重排與否待交付時裁
4. `gate_all.py` 全套重跑，增紅 = 0 方過

## §4　狀態板

| 項 | 狀態 |
|---|---|
| 賦號／正名 | **完成**（NR1L-VS-001–438／BLM 151／VC 126；VF665 接 439） |
| `req_key()` 修復 | VS-SL-08 |
| DR 寄出＋取號回填 | **待 Pei**（`dr_draft_vssl.md`） |
| 出貨 | 待 DR 結案或裁降轉（IN §8.4.3 gate）＋ VS-SL-08 量測結果 |
