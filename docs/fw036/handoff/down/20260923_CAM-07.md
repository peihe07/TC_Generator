# 下放包 CAM-07 —— CAM-06 裁決落檔 ＋ batch02a 生成（`-001`／`-012`／`-014`）

指派執行層 session：延用。對應審閱：`down/20260923_CAM-06_review.md`（依 2026-09-17 常設指示，本包與審閱同時發出；Pei 於上繳後撤回其不同意者）。

---

## §0　禁區

同 CAM-03 §0。本包觸及：`pilot02/`（-030／-031）、`batch01/`（-023）、`batch01b/`（新增 -046）、`batch02a/` 新建、`RULINGS.md`／`DECISIONS.md`／`ANOMALIES.md`／`DATA_REQUESTS.md`／`RD_FEEDBACK.md`／`framework.md` VIII.2 註。不寫回工作簿。

## §1　裁決落檔（審閱 §二／§三／§四）

取號現查：`{live}` 期望值 **15**（R-CAM13／14 已在）。R-TM13：修訂段以同級錨自立（GCB-06）。

1. **R-CAM14(b)**：637 之速度門檻併入 `-030`／`-031`（`MAX_SPEED = 13,0 km/h`，`STATUS_CCAN3`），`specification_reference` 第二行 `SYS-RA-VF551_V42-656` 之 anchor；V42 無退出條文，reasoning 註。
2. **R-CAM14(c)**：376 訊息名依 DBC 為 `STATUS_CCAN3.VehicleSpeedVSOSig`；原文 `BRAKE1` 加刪除線，留 reasoning。
3. **R-CAM10(b)**：同一行為之 IF 來源與 THEN 來源合為一 TC，多行 `specification_reference`；TC 歸 SWE ID 較小之承接列，另一列 reasoning 委派。
4. DECISIONS：6-15 `-023` Pre-Condition 3 刪；6-16 batch02 三切；6-17 A-CA32 `-076` 生成；6-18 `-002` 無 negative（RDF-06）。
5. ANOMALIES：A-CA30、A-CA22 結案（理由引 A-CA31）；A-CA31 RESOLVED；A-CA32 → 生成。
6. DATA_REQUESTS：DR-CAM-k（VF664）、DR-CAM-l（Toro 現行 PROXI）。
7. RD_FEEDBACK：RDF-06。
8. framework VIII.2 例外註：`Auxiliary Cameras` 由 `-016`／`-017`／`-001`／`-003` 四列供稿。profile §8 記 sha16 量測法。

## §2　既有列修正

| TC | 改動 |
|---|---|
| `-030`／`-031` | Vehicle Model 637＝1；CAN source 行不需（同訊息）；spec_reference 加 V42-656 行；reasoning 補 |
| `-023` | 刪 Pre-Condition 3，餘行重編 |
| `-046`（新，batch01b） | `SYS-RA-CAM-076`（Rear 節 Controls screen softkey），Test Set `Display Arbitration`（Rear 節，非 Auxiliary），PENDING DR-CAM-g，P2 |
| batch02 plan | §4-1 之 4 TC 收為 2（`V33-221+222`、`V33-223+224`，歸 `-001`，`-003` 委派）；`V2-535` 標 PENDING DR-CAM-k |

三目錄重跑 lint ＋ selfcheck 全綠（X 之 WARN 允許）。

## §3　batch02a 生成：`-001`（22）＋ `-012`／`-014`（4），依 `batch02_plan.tsv` 修訂後之列

ID 續 `-047` 起。規則同前；另：
- `-001` 之 LVDS 握手不入（歸 `-007`）；V33/V42 開機序列以 daemon 狀態（adb log）與 HU 側可觀察為 ER；`DynamicGrid_Req` 之 IF+THEN 依 R-CAM10(b) 各一 TC。
- `-012`／`-014`：`PowerShutDownNotifcation.Power_Down = True` 之 LVDS 送出以 §5.4 兩行式，命令自 SWC 交付本之 `$` 例或 SYSAD 抄；查無則 ER 以 bus analyzer 之訊息名／值書寫（升級條件 2 之處置），不造命令。Tpower 保持步驟 `Hold for <Tpower> ms`（值取 SYS2 原文，無則符號）。
- 同源者由 `-012` 承接，`-014` 只 `V3-206`。
- 五車型：Atl-Mi 之 CmdIgnSts 依 R-CAM3(e)；Ignition_Pre_Off 之對應仍 DR-CAM-i → 相關列 PENDING。

## §4　上繳要求（`up/20260923_CAM-07.md`）

1. R-CAM14(b)(c)／R-CAM10(b) sha8；DECISIONS／ANOMALIES／DR／RDF／framework 更新。
2. `-030`／`-031`／`-023`／`-046` 之欄級 diff。
3. batch02a 26 列產物、lint、selfcheck；PENDING 列表（DR-i／k）。
4. LVDS 命令句式之出處（本／列號）或「查無」之回報。
5. 自報錯誤；未結。

## §5　升級條件

- batch02a > 30（plan 修訂後仍逾則停）。
- `-012` 之 `PowerShutDownNotifcation` 於 SYS2 V2 §1.14.1 原句 > 50 token 且無法保序摘句。
- 任一 IF+THEN 合一後之 verbatim 逾 50 token 且兩來源皆非保序子序列可達（則回退為兩 TC，回報）。
