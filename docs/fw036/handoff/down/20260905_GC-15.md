# 下放包 GC-15 —— 三本 Revise1 出貨記錄、R-G72 指紋差異證明、driver_distraction 積欠結案（2026-09-05）

**指派：執行層 A**。Pei「出貨」（2026-09-05）：三本 Revise1 抽驗 9/9 OK（seed 20260905，各 3 列對 dry-run 表），准出貨。
commit `c36635f` 已含 `RULINGS.sha.tsv` 重生（768，`--check` OK）。本包只寫記錄與證明，**不動任何 xlsx，不跑會寫 repo 之產生器**。

## 一、出貨記錄（R-G72(a)(b)(e)）
三本各做：
1. `features/<f>/delivered/MANIFEST.tsv`：Revise1 列 `status` 改 `shipped`，加 `shipped_date=2026-09-05`、`shipped_by=Pei`、`spot_check=9/9 (seed 20260905)`；母本列 `status` 改 `superseded_by=<Revise1 檔名>`。
2. `features/<f>/RULINGS.md`「交付本之取代紀錄」節補出貨日、commit `c36635f`、殘項清單路徑、`unclassified 0`。
3. sha 現查三本與 GC-14 §8 逐字相符（`e9ccddbbd673`／`3554a2b28642`／`70657bdf3f1f`），不符即停手。

| 本 | 新檔 sha12 | 殘項 | 清單 |
|---|---|---:|---|
| pm_73_Revise1 | e9ccddbbd673 | 235（bare 209／ReviseC 26）| `features/power/reports/rg72_residual_pm_73_20260905.tsv` |
| ICSManagement_20260830_Revise1 | 3554a2b28642 | 101（bare）| `features/ics_management/reports/rg72_residual_ics_20260830_20260905.tsv` |
| VSM R1Low_20260902_Revise1 | 70657bdf3f1f | 47（bare）| `features/vsm_v42/reports/rg72_residual_vsm_v42_20260902_20260905.tsv` |

## 二、R-G72 指紋差異證明
GC-14 乾跑預測 `07a74237`，Pei 重生實得 **`a7b9460c`**。commit 前工作區已有 `M RULINGS_LEDGER.md`、`M rg72_residual.py`、`?? tests/test_rg72_residual.py`——推定為 A 於 GC-14 上繳後先做了 GC-14 審閱 §四-1（R-G72(h) 加註＋未知 detail 測試）。
須證：`git diff 7fd0ecd c36635f -- docs/fw036/RULINGS_LEDGER.md` 全文入上繳；差異須限於 R-G72 塊之加註行。若另有他處變動，逐處歸因。上繳一律引 `a7b9460c`，不再引 `07a74237`。
順帶自報：上繳後未經下放即改檔，違反 handoff 節奏（雖內容為審閱所令）；登 ANOMALIES 一條（次一未佔號，形態＝下放前施作）。

## 三、driver_distraction 積欠結案
`git mv` 報 bad source，`lint_paths` PASS。證：`git ls-files features/driver_distraction | grep -E "xlsx|feature.yaml"`、`grep -n "workbook:" features/driver_distraction/feature.yaml`。二者指向 `sandbox/` 即結案，於 GC-06 審閱 §四-3 之積欠項加「已於 <commit> 完成」；否則報現況。

## 四、`rg72_residual.py` 未知 detail 測試（GC-14 審閱 §四-1）
`tests/test_rg72_residual.py` 已在 `c36635f`：驗收，附測試名與 `pytest tests/test_rg72_residual.py -q` 結果。R-G72(h) 加註「新增 lint 代號時分類表同包擴充」若已落，驗收；未落則落（乾跑證入表，Pei 再重生）。

## 五、上繳 `up/20260905_GC-15.md`
§一三本記錄 diff；§二 diff 全文與歸因；§三結果；§四驗收；對照向；工具旗標逐次聲明；自報。

## 六、未結 DR
DR-PW25、DR-VS{n}（Power_Tailgate_Enable）、HMI entry path 草稿 ×3——**皆待 Pei 取號送出**；bare-$ 357 處＋pm_73 26 處字串值待 Revise-C 首包逐處登。

## 七、下一階段預告
Revise-C 首包由分析層先出 pm_73／sw_update 之每 TC 來源錨表，Pei 過目後下放。本包後執行層待命。
