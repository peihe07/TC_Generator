# 審閱 CMF-07 — 通過（交付候選本 v2 成立；本線交付前無下一包）

- 日期：2026-09-22
- 對象：`up/20260922_CMF-07.md`
- 判定：**通過**。對 v1 差異 230 格與預估逐格相符；119 列保留；lint 與 v1 逐項相同。

## 1. 裁定

| 項 | 裁定 |
|---|---|
| `vm_assign.tsv` 就地改值為單一真源 | **核**；R-C62 原判定可自 `vm_split_plan.tsv` 復原，表頭已記 |
| §1-3「範圍裁定 ≠ 車型事實」之區分 | **核，保留**。R-C68 之依據為販售範圍，三台是否為 EMEA ICS 車仍未查明，`vm_unmapped.tsv` 426 列照留 |
| `coverage.tsv` 兩處變動 | 核 |

## 2. 本線狀態

交付候選本 v2：`features/comfort/sandbox/delivery/…_SWQT_ComfortHMI_20260922_v2.xlsx`（sha `33717ddf…`），476 TC／403 leaf 全覆蓋／PENDING 0／lint 0；車型欄 Promaster 295／Toro 16／Fastback 297，三台皆 0 者 177（EMEA 119 ＋ 後排／Tri-Mode 等 58）。

**分析層無下一包**。交付前之動作全在 Pei：commit CMF-07；Excel 三動作（新列列高與格式、`D14` 日期格式、SmartArt 兩舊名）；三格填值；送 `rd_feedback.md` 七項與 DR-47／48／49；決定是否刪 EMEA 列（§1-2 代價）。

之後只剩導航 Revise（R-C66），觸發條件：canon §5.3 增 `ENTER_COMFORT`（Pei 改原檔重貼）＋ DR-49 回覆。屆時 CMF-08 ＝ 依 `nav_rewrite_plan.tsv` 改寫（115 條已明寫導航步驟者優先；290 條 `Climate` 畫面 hop 依 DR-49）。
