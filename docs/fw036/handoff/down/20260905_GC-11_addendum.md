# 下放包 GC-11 補遺 —— 施作指派＋二項量測更正（2026-09-05）

Pei 轉來停手中之執行層 session 報告（下稱「停手線」），另一執行層 session（下稱「領先線」）已完成 GC-11 §三-1/2/3/6/7/8。本補遺定指派並納入兩項更正。

## 1　指派：**領先線收尾，停手線不再寫檔**
- 領先線續作 §三-5（`pm29_v4_dryrun_20260905.tsv`）、§三-9、§三-10、§三-11 上繳。
- 停手線之 `rg70_pm29_dryrun_20260905.tsv` 為其先前產物，領先線於 §三-5 重出正式表後，該表加一行 README 註「被 pm29_v4_dryrun 取代」，不刪。
- 領先線上繳首行仍須記 session 標識；本輪已知有併行寫檔，**如實列併行改動之檔清單**（R-G62(b) 後段），不寫「無他 session 寫檔」。
- 停手線於本補遺後不寫任何檔；其本則報告全文由領先線附於上繳附錄。

## 2　更正一：SWC 之 PROXI 計數標籤（分析層之誤，自報）
GC-07 下放包 §一、GC-07 審閱 §三、GC-11 §一 四度沿用「`PROXI <Param> = <值>`（41 列）」。停手線實測：**41 次出現／26 行／3 相異行／1 個參數名**（`RRM_VPx_Steering_Wheel_Command_Type`；15 行含 `(PROXI-independent)` 致同行出現兩次）。
- 分析層以正則命中次數（41）當列數書寫，形態＝**命中次數代母體計數**（R-G69）。登 ANOMALIES 一條（次一未佔號），內容如上。
- 不推翻 SWC 式裁定（Pei 原話）；R-G70 沿革之數字改為「26 行／1 參數名」，台帳停手線已留之更正註驗收保留。
- 同理複核 GC-07 §一表之 VF230「PROXI 152」、SWC「`$…$` 215」是否亦為次數而非列數；是則一併改標籤，數字本身不動。

## 3　更正二：資料列判準
GC-07／GC-11 以「`No.#` 非空」為資料列。停手線量得 BedLowering 0902、VehicleCategory 0902 之 `No.#` 整欄為空，該判準會把整本讀成 0 列。
- 候選表（§三-6）與所有盤點腳本改用 `lint036` 判準（`test_item`／`proc`／`er` 任一非空）。
- 領先線**確認並回報** `rg72_candidates_20260905.tsv` 所用判準；若為 `No.#`，重出並列出改前／改後各本列數。
- IN／FO 若有「`No.#` 非空」字樣之通用描述，改為指向 `lint036` 判準；GC-07 §一表加註「該表以 `No.#` 判準，四本恰好 `No.#` 皆填，數字成立」。

## 4　Pei 動作
無新增；仍為 GC-11 §五三項。
