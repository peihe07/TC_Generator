# 下放包 CAM-15 —— 落檔 ＋ A 本 dry-run v2 ＋ B01a 補列 ＋ B01b（`Activation and Exit` 後半）

指派執行層 session：延用。對應審閱：`down/20260923_CAM-14_review.md`（同發）。

---

## §0　禁區

同 CAM-14 §0。不寫 `delivered/`、不 tag。

## §1　落檔

取號現查：`{live}` 期望值 **18**。

1. **R-CAM18**：B 本 Vehicle Model 判準（可佈性；預設五款 `1`；PROXI／DBC 缺者 `0` 並具名；598／5210 `0`）。
2. framework 重開：A 本 Test Set `Auxiliary Cameras` → `Additional Cameras`（簽核狀態行加重開紀錄與 sha）；`layer2_assign.tsv`、18 列 json／md、`coverage.tsv` 同步；lint Test Set 拼寫一致。
3. DECISIONS 6-51 `[DECIDED]`（R-CAM18）；6-52 補 4 列 D 欄；6-53 A-CA33 兩欄分立；6-54 B01a 23 列。
4. A-CA33 處置落 b01a 各列（N 欄 `_7.2.n`）。

## §2　A 本 dry-run v2

- 244 列（240 ＋ 4 個 D 欄佔位列，`AH` 註明），D 欄升冪；Test Set 改名後重組；`lint036.py --profile camera` 全表；對帳 244 ↔ `coverage.tsv`（tc_count 240 ＋ 4 個零 TC 列）。
- 檔名 `…_RearViewCamera_A_<date>_dryrun2.xlsx`；sha 回報。

## §3　B01a 補列

`-008-06`（§7.2.6）生成 `NR1L-RVCHMI-023`；b01a_plan／coverage_b 同步。

## §4　B01b：`Activation and Exit` 後半（43 − 23 ＝ 20 leaf），ID 續 `-024`

依 `b_plan.tsv` 章節序；含 `-015`～`-018`（§7.3、§7.3.1～§7.3.3，cache 本獨有，R-CAM6）。規則同 B01a；popup 文字依 `forms/Pop Up List HMI R1 (26PI).xlsx`，查無 `PENDING: DR-CAM-h`；R-CAM18 適用。

## §5　上繳要求（`up/20260923_CAM-15.md`）

1. R-CAM18 sha8；framework 重開紀錄；改名同步之檔清單。
2. dry-run v2 路徑／sha、lint 全表、對帳。
3. `-023` 與 B01b 20 列產物、lint、selfcheck、PENDING；R-CAM17 證據表。
4. 自報錯誤；未結。

## §6　升級條件

- 改名後任一目錄 Test Set 拼寫殘留舊名。
- dry-run v2 之 4 個佔位列被 lint 判 ERROR 且原因在工具（回報，勿改 lint 全域）。
- B01b 任一列 hop label 不可辨 > 3 列。
