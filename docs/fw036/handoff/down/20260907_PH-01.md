# 下放包 PH-01 —— Phone feature intake（scaffold ＋ 來源登錄 ＋ recon）

指派執行層 session：**新開一個 Claude Code session**（R-G62(b)）；派工前 Pei 確認 GC 線之前一 session 已停。
分析層 session：本包由 GC 線之後之單一分析層 session 落檔（R-G62(c)）。

---

## §0　禁區

- **git 一律不動**（add／commit／tag／stash／worktree 皆不得）；Pei 為唯一 git 操作者。
- 不動 `down/`、`up/` 內任何既有檔；不動 `features/` 下其他 feature 目錄。
- 不得生成任何 TC；本包止於 Phase 0–1（intake ＋ recon），Phase 2 以後另包。
- 不得自行對 037 之 911 列做任何範圍篩選（Pei 已裁整份）。
- 不得以 HFP（`CFTS026_HandsFreePhone`）之既有 TC 推定 Phone 之某列「已有人寫」而略過（R-PH{live+2}）。
- `_intake/Phone/` 內原檔不得改名、不得刪。

## §1　背景

新 feature。客戶側名稱為 **Bluetooth, Multiple Phones Paired Simultaneously**（`10_Reviewing/00_TestCase/ASW-R2/` 之資料夾名），
其 SWE.1 母體為 **整本 Phone HMI 之 037 報告**（911 列），非 Multiphone 子集。
分析層於 2026-09-07 之入件量測（本包 §5 之預期數字即出自該量測）後，Pei 裁三條（§2）。
HFP feature（`docs/Report/compare/03_HFP/` 之 CFTS026 交付本）為**獨立 feature**，母體為 CFTS026，
與本 feature（母體 = Phone HMI Logic and Flow）並立；二者可互相參考寫法，追溯各歸各母體。

## §2　裁定（Pei 2026-09-07「用Phone，然後以整份037為主，最後判斷需要增加的再說，HFP是HFP，可以互相參考，但不可以因為HFP寫到了這裡就沒也寫」）

執行層取號：本 feature 之 `RULINGS.md` 由本包 scaffold 產生，故 `{live}` 之期望值為 **1**；
執行層於落檔前現查該檔（R-G23），以現查結果為準並於上繳包回報所代入之號。

```text
R-PH{live}  Test Group = `Phone`
  本 feature 之工作簿 Test Group 欄一律為 `Phone`（不帶 HMI 尾綴，同 `Media`／`Home` 之既例）。
  R-G42 第三項「037 report 之 feature 全名」於本 feature 之適用結果即 `Phone`
  （037 標題 `SWE1-Phone-HMI` 之 feature 名為 Phone；HMI 為文件類別尾綴，非 feature 名之一部分）。
  TC ID 之 ABBR（R-G42 第二項）由 recon 自 037 req_id 提候選，Pei 於 DECISIONS.md 裁一次。
```

```text
R-PH{live+1}  範圍 = 整份 037（911 列），增補後議
  TC 生成之母體為 037 `SWE1-Phone-HMI-V0.1` Analysis Report 全部 911 資料列
  （213 parent `SYS-HMI-RA-PHONE-016`～`271` ＋ 698 child），不以資料夾名稱
  「Multiple Phones Paired Simultaneously」或 Multiphone 關鍵字做子集篩選。
  037 未載而 26PI 變更日誌所列之新增項（MPA11、MPA12）**不併入本輪**；
  其處置（登 DR 要求上游補 037，或另開增補包）於 911 列走完後由 Pei 另裁。
```

```text
R-PH{live+2}  HFP 與 Phone 各為獨立 feature；可互參，不得互相省略
  (a) HFP（CFTS026）之既有 TC 得作為 Phone 之寫法參考（步驟句式、通話情境之 setup 型態）。
  (b) Phone 之任一 037 列，不得因 HFP 已有語意相近之 TC 而不寫；覆蓋完整性以本 feature
      之 037 為唯一判準（FO §5「Every leaf gets a row」）。
  (c) 反向亦同：本 feature 不回頭改 HFP。
  (d) 互參時只參考寫法，不複製其 spec_reference —— 追溯各歸各母體（IN §8.4.2）。
```

## §3　裁決引用（R-G52@dabfa307 之引用制）

執行層自 repo 讀原文並於上繳包回報所讀 sha8；sha 不符即停（FO §8.4）。

| 引用 | 用途 |
|---|---|
| `R-G42@f111d3dc`（ledger 本） | 交付規格：TC ID `NR1L-{ABBR}-{nnn}`、Test Group、D 欄升冪 |
| `R-G63@a25150c1` | `_intake/Phone/` 已由分析層建妥並 `list_directory` 實測（本包 §4 任務 0） |
| `R-G64@34b81800` | 產出物目錄政策 |
| `R-G66@1951347c` | 來源集中制：原檔落 `sources/raw/<doc_id>/`，`feature.yaml` 以 doc_id 引用；feature 端不存副本 |
| `R-G67@823477ed` | intake 時之嵌入物件檢查（本 feature 母體為 HMI L&F 非 CFTS；仍須執行並回報「不適用」之依據） |
| `R-G70@c964dfb8`、`R-G71@72328d57` | profile 一律啟用 lint `P`(v4) 與 `X`（FO §4 [ADD] 二項） |
| R-G23（無 sha 列；見 ledger「綁定須被檢查」節） | 取號現查 |

## §4　執行層任務

**任務 0 — 投遞區確認**
`_intake/Phone/` 已存在（分析層 2026-09-07 建，`list_directory` 實測為空目錄）。
Pei 投遞後應有 5 檔；執行層 `ls` 並逐檔 sha256，與 §5 之三個 sha16 對照（PDF 二件分析層未取 sha，執行層取後首載）。
**5 檔未到齊即停**，上繳「素材未到齊」而不猜。

**任務 1 — scaffold**
```
python scripts/new_feature.py Phone --root .
```
預期產生 `features/phone/`（RUNBOOK／ANOMALIES／RULINGS／DATA_REQUESTS／DECISIONS／feature.yaml／PLAYBOOK／.gitignore ＋ 六子目錄）。
scaffold 之 `abbr` 預設取前二字 `PH`；**此非裁定**，ABBR 由 DECISIONS.md 走 R-G42 第二項。

**任務 2 — 來源登錄（R-G66）**
5 檔以 `scripts/extract_source.py` 登入 `sources/raw/<doc_id>/` 與 `MANIFEST.tsv`（`features` 欄 = `phone`）。
- `SYS1_HMI_Phone_HMI_Logic_and_Flow_R1_SR24_Post 2A_(June_21_2022).xlsx`：`spec-index/cache/` 已有同名 xlsx＋json，
  執行層 sha256 比對二者；**全等**則 MANIFEST note 註「與 spec-index/cache 同體」，**不等**則停，上繳兩個 sha 由 Pei 裁用哪個。
- 43.6 MB 之 `Phone HMI Logic&Flow R1.pdf`：先 `pdfinfo`／`pdffonts` 判文字層（FO §3 spec_mode B／C），
  並抽 Assumptions 章之 **MPA11、MPA12 原文**落 `features/phone/docs/26pi_delta.md`（只抽這兩條，供 Pei 日後裁增補；R-PH{live+1}）。
  若 PDF 無文字層，記 spec_mode C 並回報，不 OCR。
- `MANIFEST.tsv` 手工附加（見既有 note「不得用 --refresh-manifest」）。

**任務 3 — recon（Phase 1）**
依 `features/phone/RUNBOOK.md` Phase 0–1 執行 `scripts/intake.py` ＋ `scripts/recon.py`，產 `RECON.md` ＋ `DECISIONS.md`（[AUTO]／[PROPOSED] 預填）。
recon 至少須回報下列量測，**每項附查詢條件**：
1. 037 資料列數、parent／child 數、Categorization 分佈、FROP 分佈（對照 §5）。
2. 037 parent 之 `Source Requirement ID` 對 SYS1 `Basic Report` 之 **ID／Outline Number 對映率**：
   037 只載文件名（`HMI Source ID` 欄 = 文件名），**無 NRL 號亦無章節號**；
   對映須以 description 文字比對（去除 037 之 `The system shall ` 前綴與句首大小寫後精確比對，再報模糊比對），
   未對映之 parent 逐列列出。此對映為 `spec_reference`（IN §10.7(b) `{檔名}_{章節號}`）之唯一來源，**對映率是本包之關鍵數字**。
3. 037 description 之機械改寫痕跡計數：regex `^The system shall [a-z]` 之列（如 `The system shall the term…`、`The system shall pHCAT3.2`）；
   逐列列出，供 verbatim 上半改取 SYS1 原句（IN §8.6）。此為登 ANOMALIES 之候選，非裁定。
4. HFP 交付本（`docs/Report/compare/03_HFP/…20260316(Refine).xlsx`）之 Test Set 清單與列數 —— 只作 §2 R-PH{live+2}(a) 之參考素材盤點，不做對映。
5. 26PI change log 之列數（預期 1）與 MPA11／MPA12 在 037／SYS1 之查找結果（預期 0／0）。

**任務 4 — profile 骨架**
`docs/runtime/profiles/FW036_R1L_Phone_Profile.md` 建骨架：啟用 lint `P`(v4) 與 `X`；
§5.3 `ENTER_HOME_SCREEN` 之 PENDING 由本 feature DR 承接（FO §4 [ADD] 第二項）——
Phone 之入口為 Menu Bar `"Phone"`，是否需 Home Screen 入口由 recon 判；需要則開 DR，不需要則於 RECON 述明依據。
profile 內容條文留空待 Phase 3。

## §5　預期數字（分析層 2026-09-07 實測，openpyxl read_only）

| 項 | 預期 | 查詢條件 |
|---|---|---|
| 037 sha16 | `2d12fa11a4fc2116` | sha256 前 16 |
| SYS1 xlsx sha16 | `e6d58766cf6ddb19` | 同上 |
| Change Log sha16 | `405eb303c848f0e0` | 同上 |
| 037 資料列 | 911 | `Analysis Report` 列 8 起，A 欄非空 |
| parent／child | 213／698 | A 欄 `-` 數 = 2／3 |
| Categorization | Functional 754／Heading 157 | G 欄 |
| FROP Top-2 | Core HMI 621／Device Manager 197 | H 欄 |
| Source ID 範圍 | `SYS-HMI-RA-PHONE-016`～`271`，唯一 213 | B 欄 |
| SYS1 Basic Report 列 | 273；頂層章 27 | A 欄非空；Outline Number 不含 `.` |
| 帶前綴標籤之 parent | 33（MP 9／PSC 5／MPDND 4／MPS 3／MPP 3／BTP 2／WC 2／MPA、NPL、PPS、MSMS、PW 各 1） | E 欄 `^[A-Z]{2,5}\d*[a-z]?\)` |
| Multiphone 關鍵字子集 | 42 parent／120 child（**登記用，非範圍**） | 前綴 `MPA|MP|MPS|MPP|MSMS|MPDND` 或 `multiphone|two phones|second device|Favorite [12] phone|priorit` |
| `multiple phones|paired simultaneously` | 0 hit（037 與 SYS1 皆） | 全欄 re.I |
| Change Log 列 | 1（26PI，2026-05-20，MPA11＋MPA12，Under Review） | `26PI` 分頁 |

不符者依 FO §8.2 逐項列出，不自行調和。

## §6　上繳要求（`up/20260907_PH-01.md`）

1. 5 檔 sha256 全表；SYS1 與 spec-index/cache 之同體判定。
2. scaffold 產物路徑清單；`RULINGS.md` 落檔後三條 R-PH 之實際號與 body sha8。
3. §5 全表之預期 vs 實測（相符者亦列）。
4. recon 任務 3 之五項量測，第 2 項附未對映 parent 全列。
5. MPA11／MPA12 原文之落點與 PDF spec_mode 判定。
6. `DECISIONS.md` 之 [PROPOSED] 清單（ABBR 候選須在內）。
7. 未結 DR 清單（含 `ENTER_HOME_SCREEN` 承接結果）。
8. 自報錯誤與併行寫檔觀察（R-G62(b) 附記之形態）。

## §7　升級條件（停下，寫入上繳包，不自行處理）

- `_intake/Phone/` 少於 5 檔，或任一 sha 與 §5 不符。
- SYS1 與 spec-index/cache 之 sha 不等。
- `new_feature.py` 因 `features/phone/` 已存在而拒絕。
- 037 parent → SYS1 對映率低於 90%（以精確比對計）。
- 43.6 MB PDF 無文字層且 Assumptions 章無法抽出。
- 任何需改 canon（FO／IN）或全域 ledger 之情形。
