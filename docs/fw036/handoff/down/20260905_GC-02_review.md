# 審閱 GC-02 上繳包（2026-09-05，Tier 2）

對象：`up/20260905_GC-02.md`　下放包：`down/20260905_GC-02.md`
結果：**收受**。六件完成收受；5-2 之中止**正確**，且其 6-2 節之量測推翻了分析層一項裁決，本審閱撤回之。

分析層本輪已自行改動（皆核畢）：`docs/fw036/ANOMALIES.md`（+A-GC10、A-GC11）、
`templates/feature.yaml`（括號改為「不在 R-G1 母本內；另一真實變體 24/145」）、
`INDEX.md` 3 節（孤兒敘述以刪除線保留，加補檔註）。

---

## 一、撤回：審閱 GC-01 §三「R-G48 之補充」之夾具改名裁決（A-GC10）

前提「不存在之表名擴散」為偽：`&Result` 為 24/145 之真實變體，含 `features/power` 在效輸入；
`backend/parser.py:77`／`writer.py:10`／`review_engine.py:1338` 硬編該名。
照原裁改名會使夾具停止覆蓋 power／audio_mgmt 一支，**撤回**。

改裁如下（R-G48 補充 v2，[DEFAULT]）：

```
R-G48(b)：TC 分頁名為工作簿層屬性，兩個真實變體並存
（`Test Case Specification 測試用例規範` 121/145、`Test Case Specification&Result` 24/145，
2026-09-05 census）。任何讀寫工作簿之路徑須以 feature.yaml `workbook.sheet` 為準，
code 內常數只得作「feature.yaml 未載時之後備」。測試夾具須**兩變體各一**，
並加守衛測試：模板 `workbook.sheet` ∈ R-G1 母本之 sheetnames。
```

**先量後改**（GC-03 任務）：`backend/` 五處硬編中，何者已有 feature.yaml 覆蓋路徑、
何者純硬編（即對 121 本會失敗）——執行層以「對母本開檔跑 parser」實測，不由本層推。
純硬編者之改 code 屬 Pei 准之範圍，本審閱只裁方向。

## 二、其餘三項請裁

**1　DBC 一類無一對一版本**（4 節末）—— 裁：R-G45 之「同類」於 DBC／LID 一類以**網段 token**
（`BHCAN`／`FDCAN8`）為配對鍵，版本 token（`R1`／`R4`／`R5`）不入配對；
`vehicle_setting` 之 `R4_BHCAN` ↔ `forms/…R1_BHCAN2`、`R5_FDCAN8` ↔ `forms/…R1_FDCAN8`。
DECISIONS 之四份並列**保留**，加一行配對註。[DEFAULT]

**2　`NOT_SOURCE_DOC` 20 列** —— 裁：**剔除**。MANIFEST 只登原檔；提案表改為 159 列，
統計寫「179 唯一 sha 中原檔 159、結構檔 20（剔除）」（R-G8 分子分母）。
執行層保留旗標不擅刪之處置**正確**。

**3　`upstream/` 舊路徑於 `git mv` 後失效**（5 節）—— 裁：歷史檔**不追改**（同 R-G18(FO)）；
`cleanup_gc02.sh` 段二每行之上加註解 `# was: <old> ; referenced by: <file>`，即為路徑遷移對照，不另出檔。

## 三、9-2 節之提案 —— 併入 R-G50，不另立號

```
R-G50(b)：對照向（反事實量測）須同時回報「被移出／還原之檔數」；
該數為 0 而仍宣稱已建立對照向者，即未量測之斷言。
```

執行層此輪之「指令沒報錯＝指令做了事」與 A-GC 系列同源；提案可測，收受。

## 四、A-GC11（分析層自報）

審閱 GC-01 §三 內裸節號（`§5`、`§0`～`§4`）經 R-G36 逐字進台帳，`canon_refs` +1。
台帳不改字；`rulings_hash` 後以註記指明所指。分析層審閱自此逐檔過 PROTOCOL「文面引用」檢查後再落檔。

## 五、收受清單

台帳新節（R-G44 修訂版在效＋原文刪除線）、提案表（待 §二-2 改 159）、五本 DECISIONS（13 列 PENDING 由分析層判——**GC-03 任務，分析層自做**）、`reports/.gitkeep`、`cleanup_gc02.sh`（待 §二-3 加註）、二張掃描表、`test_lint036` 修正（對照向逐項同名 +0）、ANOMALIES:230 更正、`up/20260901_VS-SL-01.md` 補檔。
`gate_all` +34（不含本包）逐項可歸因，全落 R-G44–50 未入指紋表，**不擋包**。

## 六、Pei 待裁（累計）

| # | 事項 | 狀態 |
|---|---|---|
| 1 | R-G43 保號側 (A)/(B)＋R-G19 台帳內二擇一 | 待裁（GC-01 起）|
| 2 | `_intake/SXM/` 二檔 | 待裁 |
| 3 | R-G48(b)、R-G45 DBC 配對、R-G50(b) [DEFAULT] | 追認 |
| 4 | `backend/` 硬編改 code（GC-03，量測後） | 准否 |
| 5 | `rulings_hash.py`（R-G44–50 入指紋表）＋ R-G30–41 補列 | Pei 作業 |
| 6 | `cleanup_gc02.sh` 專門 commit；`_intake/` 清空 | Pei 作業（先 §二-3 加註） |

## 七、GC-03 預告（待 §六-3／4）

執行層：提案表改 159；`cleanup_gc02.sh` 加註；parser 對母本實測（五處硬編分類）；兩變體夾具＋守衛測試；DECISIONS 配對註。
分析層：13 列「是否影響已交付 TC」逐列判（需讀各 feature 之 delivered 與差異檔——量測後判，不推）。
