# 下放包 VS-SL-09 —— R-G42 一(a) 重排＋F 欄重賦（趁 E 欄仍空之窗口）

日期：2026-09-02　層級：Tier 2 → Tier 1
前置：`down/20260902_VS-SL-08_review.md` §2（Tier 2 裁：遵 R-G42 重排）
上繳寫 `up/20260902_VS-SL-09.md`。

## §1　重排（vf230＋BL；VC 不動）

對 `output/` 二本（先備份 `.presl09.bak`，不覆寫既有備份）：
- 資料列（header 列以下）依修後 `req_key(D 欄)` **穩定排序**：等鍵保持原相對序
- 整列搬移（A–AA 全欄同動），不得只動部分欄
- 排序後跑修後之一(a) 檢查：**相鄰逆序 = 0（assert）**

## §2　F 欄重賦

- vf230：`NR1L-VS-001`–`438` 依新列序連號；BL：`NR1L-BLM-001`–`151` 同法
- 對照表重出為**端到端**：`old_E`｜`old_F`（SL-06 之前原值：vf230 空、BL `newR1L-…`）｜`new_F`（最終）｜`status`（assigned／renamed／RETIRED）；RETIRED 19 列不動、無 new_F
- MANIFEST 更新 SHA

## §3　不動與不回改

- VC 三件（工作簿／對照表／MANIFEST）不動
- 歷史報告與 resolution tsv 之列號引用不回改（對舊列序之紀錄）
- `sandbox/` 與各 `.bak` 鏈不動

## §4　自檢與上繳

- assert：一(a) 相鄰逆序 vf230／BL = 0；列數 438／151 不變；F 唯一且連號
- assert：PENDING 計數不變（vf230 25、BL 之 `Body_Types` 行 151、VC 20 不在本包）
- assert：重排前後**逐列內容集合相同**（除 F 欄外，各列以內容 hash 比對，僅序變）
- lint 四項二本重跑 0；`gate_all.py` 增紅 0
- 上繳：二本新 SHA、二份對照表 SHA、一(a) 複驗數、自檢、自報
