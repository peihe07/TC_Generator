# 審閱 CMF-04 — 通過（四停點全命中，車型欄未填；本線暫停，待 Pei 三問）

- 日期：2026-09-22
- 對象：`up/20260922_CMF-04.md`
- 判定：**通過**。停點判定與「逐條粒度」皆正確；§5 四項不涉車型而照做，正確。候選本 v3 相對 v2 只差 R-C61 之 2 格，如實反映停點。

## 1. 分析層自陳（A 類）

| # | 分析層之誤 |
|---|---|
| A-CMF18 | R-C61 寫「目標 H＝4→2」係以 R-C56 前之計數為底，未量 v2 之現值（H 已為 6）。自身未量即定目標 |
| A-CMF19 | 「補 `-146` Remarks」之裁定以執行層自報為據，未查工作簿 F 與 JSON tc_id 自 R-C46 起即為兩套號碼（分析層自己下的裁定）。**跨層指涉一律以 req_id 為準**，採為本線記法，落 profile |
| A-CMF20 | CMF-04 §1 之軸↔參數對映表由分析層直接給出，未先證條文語與 PROXI label 同一物；執行層證得 ATC／MTC 與 ICS 兩軸無法證，即本表有一半是假設 |

## 2. 裁定（即時生效；Pei 可撤）

| 項 | 裁定 |
|---|---|
| 42 條 `test-setup 蘊含`（無 `Rear_Climate` 之車開不出後排畫面）| **准**其判斷與獨立分支標記；待 Pei 三問後整批處置 |
| `Dec` vs `PROXI Read`（Fastback `Climate_Panel_Type` 相反）| 維持 `Dec`；兩值並列已足。此案併入 §3-1 之問 |
| R-C60 既有 2 處引號 | 依 R-C60「不加」→ **拿掉**，與 11 處未引號一致（合稱非按鍵標籤，已量證）。下一輪 Revise 一併做 |
| `-146` | 無事可做，裁定撤回（A-CMF19）|
| `$Rear_HVAC_cfg$` vs `Rear_Climate` | 與 DR-46 同型：PC 保留 CFTS043 逐字，Remarks 載「PROXI 檔中該參數名為 `Rear_Climate`」；**併入 DR-46 之問**（LID／參數名 ↔ 檔內名之對照來源），不另開 DR |
| `tests/test_comfort_upper_verbatim.py` 之 skip 修正 | 准；CI 收集中斷屬回歸，修正正確 |

## 3. 待 Pei —— 本線在此暫停，CMF-05 不下放

執行層的數字把一件事攤開了：**依 PROXI 逐字，工作簿寫的配置三台都沒有。**

- Toro：`Manual` ＋ `3 knobs` ＋ ICS `Present` → 正是條文 2.14 排除的那一種（無 HVAC 畫面／icon／pop-up）→ 454 條為 0，全本只剩 21 條。
- Promaster／Fastback：`Automatic`、`Rear_Climate = Absent` → 後排 56 條為 0。
- 相乘：**65 條三台皆 0**，含全部後排畫面與 Tri-Mode。

分析層不判 PROXI 對不對、也不判 2.14 之 `3 knob HVAC controls with ICS` 是否即 PROXI 的 `3 knobs` ＋ `Integrated_Climate_Touchscreen`。三問：

1. **Toro 的 PROXI 值是對的嗎？**（`Climate_Type=1`、`Climate_Panel_Type=6`、`Integrated_Climate_Touchscreen=1`，來源 `forms/proxi/Toro`）若對，Toro 於本 feature 幾乎全本不適用，Y 欄 454 條填 0；若您手上有更新的 Toro PROXI，請放進 `forms/proxi/` 我們重跑。
2. **65 條三台皆 0 的 TC 怎麼處理？** 照填出貨（等於宣告本輪不執行）／標註「本輪三台不涉」／移除。分析層建議：**照填出貨**，Remarks 不加字 —— 車型欄本身就是這個資訊。
3. **ATC／MTC ↔ `Climate_Type`、ICS ↔ `Integrated_Climate_Touchscreen`** 這兩個對映條文證不出（條文從不展開縮寫）。接受為約定俗成，或登 DR 向 RD 要「條文用語 ↔ PROXI 參數」對照？這題與 DR-46（LID ↔ CAN 名）、`$Rear_HVAC_cfg$`（CFTS 名 ↔ PROXI 檔名）是同一類：**上游有三套命名，我們沒有任何一份對照表**。分析層建議合為**一個 DR** 問 RD。

另外仍欠一句：**alt-mi ＝ Atlantis（MID）？**（DR-46 四行 PENDING 之鑰）。

您答完 1–3，CMF-05 ＝ 依 `vm_assign.tsv` 填 T–Z ＋ 依 `vm_split_plan.tsv` 拆（72 列，需您准超停點）＋ R-C60 之 2 處 ＋ DR-46 結案，成候選本 v4。

## 4. 請先 commit

CMF-04 產物（三份 plan、`lint036.py` R-C61、測試檔修正、ENTRY 039）。
