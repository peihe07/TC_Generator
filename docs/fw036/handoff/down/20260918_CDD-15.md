# 下放包 CDD-15 —— 佔位重分類：CDD 工具可解者視為可執行（R-DIAG33）→ 交付本 v05

前件：`down/20260918_CDD-14.md`（若 CDD-14 未跑，先跑 CDD-14 再跑本包；本包不依賴其產出）。
Pei 2026-09-18 裁定：**採方案 1** —— 手動執行時由載入 Harman CDD 檔之診斷工具解出位元組，
此類佔位不算缺件。執行層 session：沿用 CDD 線。

---

## §0　禁區
git 不動；`--gate`／`--regen` 不跑；**TC 文字一字不改**（佔位 token 原樣保留）；只動分類、著色、Remarks、tsv、asset_request。
升級條件表自 §5 逐字複製；T0 先取號落條文再產出。

## §1　裁定

```text
R-DIAG33  佔位之兩類：工具可解 vs 真缺件
  (a) 「工具可解」（TOOL_RESOLVED）：值定義於 ECU 之 CDD 檔（CANdela），執行時由載入該檔之診斷工具解出，
      TC 文字保留 `<…>` 佔位即為可執行。屬此類：
      - `<controlState for "…">`／`<out-of-range controlState>`（DR-DIAG-4）
      - `<… byte>`／`<… record>`／`<out-of-range value>` 等資料位元組值域與版面（DR-DIAG-5）
      - `<unsupported SID>`／`<unsupported DID>`（DR-DIAG-6）—— 工具之服務／DID 清單中不存在者即為不支援
  (b) 「真缺件」（ASSET_MISSING）：值不在 CDD 檔，需 RD 另供：
      - `<Package Indication value>`（DR-DIAG-8，sales code → PkgIndex 對照）
      - KeySense 常式行為（DR-DIAG-7，只影響 -290 之 ER 深度，非佔位）
      - SD.00049 附件（DR-DIAG-11，只影響 PC-3 去留，非佔位）
  (c) 著色：只 ASSET_MISSING 之 TC 列著 FCE4D6；TOOL_RESOLVED 不著色。
  (d) Remarks：含 TOOL_RESOLVED 佔位之 TC 加一行定型句
      `Placeholder values resolved at execution by the diagnostic tool loaded with the ECU CDD file`
      （`RM-DIAG` 白名單加第七式；已有 R-DIAG6(amend) 先例）。
  (e) `placeholder_summary.tsv` 加 `class` 欄（TOOL_RESOLVED／ASSET_MISSING）；`asset_request.md` 只列 ASSET_MISSING，
      並另立一節「建議取得 ECU CDD 檔（.cdd）」註明：取得後可將 TOOL_RESOLVED 佔位填為實值以供自動化，非交付前提。
  (f) DR-DIAG-4／-5／-6 Status 改 `TOOL_RESOLVED`（非 CLOSED、非 MISSING），Urgency LOW；DATA_REQUESTS.md 表頭加註此狀態之定義。
  (g) `EXEC_GUIDE.md` 加一節：執行前提 = 診斷工具已載入本 ECU 之 CDD 檔；佔位之解讀方式各舉一例（controlState／值域／不支援 DID）。
```

## §2　任務
- **T0**：取號 `R-DIAG33`（`git grep HEAD` 零命中）→ 落條文 → 指紋依制（R-G75 三數）；`RM-DIAG` 第七式 ＋ 自測 3 項。
- **T1**：佔位分類腳本：對 `placeholder_summary.tsv` 全 591 行依 token 分類，輸出 `class` 欄；預期 TOOL_RESOLVED = DR-4 99 ＋ DR-5 205 ＋ DR-6 269 = **573 行**，ASSET_MISSING = DR-8 **18 行**（≈ 13 條 TC）。分類依 **DR token**，不依字面；任何 token 不在 (a)(b) 清單者 → 停，回報。
- **T2**：Remarks 定型句加入所有含 TOOL_RESOLVED 佔位之 TC（預期 ≈ 429 − 13）；不動其他欄。
- **T3**：著色重做：只 ASSET_MISSING 列（預期 ≈ 13）；其餘列還原無底色（styles 手術法同 SEC-17）。
- **T4**：DATA_REQUESTS.md／asset_request.md／EXEC_GUIDE.md 依 (e)(f)(g) 改寫；`asset_request.md` 之 ASSET_MISSING 節應只剩 DR-7／-8／-11 三件。
- **T5**：merged v11（或 v12，依 CDD-14 是否已出）／交付本 **v05**；lint 十一本；§9；逐格對帳 vs merged（差異只在 Remarks 與 ID/列序）；`coverage.tsv` 不變。

## §3　預期
| 項 | 預期 |
|---|---|
| TC 列數 | 565 不變 |
| 著色列 | 429 → **≈ 13** |
| Remarks 改動 | ≈ 416 條 |
| lint | 真違規 0；`RM-DIAG` 0 |
| asset_request 項目 | 5 → 3（＋ 一節 .cdd 建議） |

## §5　升級條件
- 指紋／`--check`（既存者記「既存」）。
- T1 出現 (a)(b) 清單外之佔位 token → 停，列出，不分類。
- ASSET_MISSING 條數與 DR-8 之 TC 數（CDD-12 記 13 條）不符 → 回報差異。
- 任何 TC 非 Remarks 欄有變動 → FAIL，回報。

## §6　上繳
首段升級條件逐字；sha8；T1 分類表（token × class × 行數）；T2/T3 數字；貼入 **一條 TOOL_RESOLVED TC 全文（含新 Remarks）**；T4 三檔摘要；T5 路徑與對帳；建議 commit ＋ pathspec。
