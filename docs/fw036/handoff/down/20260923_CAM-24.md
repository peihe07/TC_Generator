# 下放包 CAM-24 —— A 本補 4 列 ＋ A 本 dry-run v3 ＋ 全案生成收尾包

指派執行層 session：延用。對應審閱：`down/20260923_CAM-23_review.md`（同發）。本包為 Camera 生成階段之**最後一包**；之後為 Pei 之 DR／bench／Phase 7 閘。

---

## §0　禁區

同 CAM-14 §0。不寫 `delivered/`、不 tag。既有 446 列不改。

## §1　落檔

1. DECISIONS 6-78～6-81：A 本 ＋4；CAM-21 交付物落於 CAM-23（PROTOCOL 例外註）；第 10 項 37 WARN 不逐列覆核；`-204` 殘留接受。
2. `bench_verify.md` 加 `-214` 之亮度邊界列（治具）。

## §2　A 本補 4 列（`batch05/`，ID `NR1L-RVC-241`～`-244`）

| 母列 | 補列 | 驗證點 | 錨 |
|---|---|---|---|
| `-017` | `-241` | Automatic 模式下 `BRAKE_FD_2.VehicleSpeedVSOSig >= c_VEHSPD_MAX` → 退出（供試值依 A-CA30 之 206；五車型依 R-CAM3(e)／R-CAM14 CAN source） | `SYS-RA-VF551_V2-496` anchor |
| `-017` | `-242` | Automatic 模式下 `RVC_ImageDefeat.Req = Pressed` → 退出（內部訊號依 §8.7.5(f) 保留來源名；HMI 面為 `"X"` 或 ImageDefeat 之對應鍵，來源逐字） | 同上 |
| `-201` | `-243` | `Rear_Camera_Repetition.Data` 之複製（adb／dumpsys 兩行式沿 pilot 002） | `SYS-RA-VF551_V42-226` anchor |
| `-214` | `-244` | 亮度 `over 50%`：49%（不顯示）／51%（顯示）兩點 —— 依 §8.3 boundary 每點一列則為 ＋2；**先出 51% 一列（on-point）**，49% 併入同列之對照步不可（§8.3），故若執行層判須兩列，出 `-244`／`-245` 並回報 | `SYS-RA-VF551_V33-234` anchor |

reasoning 具名「拆解審計 CAM-23 §3 #n」與母列；`coverage.tsv` 母 leaf `tc_ids` 追加、`batch` 加 `;batch05`；lint ＋ selfcheck（含第 10 項）全綠（ERROR 級）。

## §3　A 本 dry-run v3

- 248（或 249）列 ＝ 244 ＋ 補列；檔名 `…_A_<date>_dryrun3.xlsx`；lint 全表、對帳、`pending_by_dr.tsv` 重生；sha 回報。DECISIONS 6-58 之交付候選改指 v3。

## §4　全案生成收尾摘要（`features/camera/DELIVERY_STATUS.md`，新建）

一頁：兩本候選（路徑／sha／列數）、TC 總數、零 TC 形制計數、PENDING ↔ DR 表（11 條）、`bench_verify.md` 列數、RDF 17 條、GCB 12 項、未結 DR 15 條之狀態、Phase 7 前置條件（R-G42 七）。供 Pei 作閘之依據；日後 DR 結案時由執行層更新。

## §5　上繳要求（`up/20260923_CAM-24.md`）

1. 落檔清單；`batch05/` 產物、lint、selfcheck。
2. dry-run v3 路徑／sha、lint、對帳；`pending_by_dr.tsv`。
3. `DELIVERY_STATUS.md` 全文。
4. R-CAM17 證據表；自報；未結。

## §6　升級條件

- `-242` 之 ImageDefeat 於 HMI 面無逐字對應鍵（PENDING DR-CAM-g 型，回報）。
- dry-run v3 lint ERROR 非 0 且原因在工具。
