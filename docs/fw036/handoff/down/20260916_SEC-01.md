# 下放包 SEC-01 —— Security feature intake（scaffold ＋ 來源登錄 ＋ 三段追溯矩陣 ＋ framework 草案）

指派執行層 session：**新開一個 Claude Code session**（R-G62(b)）。
分析層 session：2026-09-16 Security intake session（Project「Test Case Design & Development」）。

---

## §0　禁區

- **git 一律不動**；Pei 為唯一 git 操作者。
- 不動 `down/`、`up/` 既有檔；不動 `features/` 下其他 feature 目錄（含 `sw_update/`，見任務 3-8）。
- 不得生成任何 TC；本包止於 intake ＋ 追溯矩陣 ＋ framework 草案驗算，Phase 2 另包。
- 不得自行裁定 §7 之待裁項；只回報量測與 [PROPOSED]。
- 來源原檔位於 `10_Reviewing/00_TestCase/ASW-R3/Security/`（含 `REF/`）與 `2_Architecture/CCVR/`，不在 repo；登錄依 R-G66 落 `sources/raw/`。
- CCVR 三件（§4 任務 0(c)）為 **reference-only**：不得作 `specification_reference` 錨、不得作 Req 來源（IN §8.1／§8.4.2）。

## §1　背景

新 feature。母體為 **6 本 037（SWRA）**，非 CFTS、非 HMI L&F —— 是本專案首例。

| # | 037 | sha16 | 列 | ID 格式 | Verification Method |
|---|---|---|---|---|---|
| 1 | `FM-WI-FSM-037-A03-N1L-SWE1-CertProvider-SWE1R1-V1.0 …_SWRA.xlsx` | `77737ab05339dec4` | 11 | `SWE1-CertProvider-001~011` | 11 Test / Log Observation |
| 2 | `FM-WI-FSM-037-A03-N1L-SWE1-KeyInstall-SWE1R1-V1.0 …_SWRA.xlsx` | `7a98408232c950a7` | 13 | `SWE1-KeyInsyall-001~013`（**原檔拼字**） | 12 Test；012 Doc Review/Static Analysis |
| 3 | `FM-WI-FSM-037-A03-N1L-SWE1-SAM-SWE1R1-V1.1 …_SWRA.xlsx` | `88729029f8c50f0c` | 19 | `SWE1-SAM-0001~0019` | 16 Testing；0001/0008/0019 Peer Review |
| 4 | `SWE1_ECUCert_FM-WI-FSM-037-A03 …_SWRA.xlsx` | `891f75498fd20e26` | 13 | **A 欄全空**，僅 `SYSAD_SEC_ECUCERT_*` | 13 Peer Review |
| 5 | `SWE1_SwdlSecureLib_FM-WI-FSM-037-A03 …_SWRA.xlsx` | `6c628538a79eceed` | 5 | `SWE1-SRA-SECURITY-SWDL-001~005` | 5 Peer Review |
| 6 | `SWE1_libLogEncrypt_FM-WI-FSM-037-A03 …_SWRA.xlsx` | `3ecb0e1ad0cd6094` | 9 | `SWE1-LOGENC-001~009` | 006 Testing；其餘 Peer Review |

合計 70 列。sheet 名 `Analysis Report`，資料列自 **row 9** 起（非 037 常見之 row 8；row 8 為欄名列）。
#1/#2 欄位 18 欄（無 `Sub Categorization`），#3~#6 為 19 欄（有 `Sub Categorization`）；`Verification Criteria`／`Verification Method` 之欄索引因此相差 1（#1/#2 為 Q/R 欄，#3~#6 為 R/S 欄）。

**追溯鏈（分析層 2026-09-16 實測，三段皆接通）**：

```
SWE1 `Source Requirement ID`（SYSAD_*）
  → SYS3 SYSAD docx（REF/ 六本）之 SYSAD ↔ SYS-RA-SEC 對應
      · CertProvider／KeyInstall：每元素一表，`SYSAD_ID` 首列 ＋ `Mapped SYSRA-ID` 列
      · ECUCert／SAM／SWDL／libLogEncrypt：單一「Table 11 – SYS2 ID 對應 SYSAD ID」
  → SYS2 CFTS084（`SYS-RA-SEC-nnn` ↔ Polarion `NRL-nnnnnn`）
  → CCVR v2.7 SYS2_Mapped（`Mapping detail` 356 列 → CCVR 各 sheet 列 → apk JUnit 方法名）
```

**範圍裁定（Pei 2026-09-16，依 9/16 workshop 紀錄）**：本輪只做 CCVR 五個 test item 涵蓋之範圍 ——
`Auth-Boot CS.93`（RD JY/Rivers，workshop 9/17）、`Auth-Prog CS.93`（同）、`Cert Val CS.98`（Steven Hsu，9/10 已開）、
`ECU ID CS.165`（Samuel，9/15 已開）、`Secure Log CS.212`（Shawn，9/15 & 9/17）。QA 側 Max/Iris。
Workshop 紀錄之硬約束：
- CS.165：尚無可用 ECU certificate，只能驗 chain checking ＋ retry；端到端（STLA backend／下載／恢復）不可測；workshop 暫停。
- CS.212：Steven 已整理五個情境（ECU identity/cert verification failure、Unauthorized access to protected data、KeyStore/KeyMint access failure、Key injection sequence/input-data failure、錯誤處理與安全事件紀錄）；package 由 Cybersecurity team 內部 review 後才給 QA；QA 用 Dev-Key build 驗。
- CS.98：STLA 舊 certificate 之 OID／NR1L identification 有誤，修正版未到；DCL 檔由 RD 預先產生各撤銷情境，QA 只選用；DCL readiness 對齊 **9/18 Dev-Key build、9/20 code freeze**。
- CCVR 以 Dev-Key build 完成；Production Key 於 CCVR 通過後才提供（CS.98 No.10 PROD certs not yet）。

## §2　裁定（Pei 2026-09-16）

執行層取號：`RULINGS.md` 由本包 scaffold 產生，`{live}` 期望值 **1**；落檔前現查（R-G23）並於上繳包回報。

```text
R-SEC{live}  母體為六本 037（SWRA），範圍以 CCVR 五 test item 為界
  (a) 六本 037 為 Req 母體；SYS3 SYSAD、SYS2 CFTS084、CCVR 為追溯與作者側素材，不得代位為 Req。
  (b) 本輪 TC 只產出能落到 CCVR 五 test item（CS.93 Auth-Boot／Auth-Prog、CS.98、CS.165、CS.212）
      之 SWE1 列；落不到者列 `features/security/data/out_of_round.tsv` 附理由，不產 TC、不刪列。
  (c) 落到與否以 trace_matrix.tsv 之 `ccvr_sheet` 欄為準（任務 3 產出）；SWE1 Verification Criteria
      本身已指明場景者（如 CertProvider-005 之 ECU CRL）以 SWE1 原文為優先判準。
```

```text
R-SEC{live+1}  Peer Review 列之處置
  Verification Method = Peer Review／Document Review／Static Analysis／Inspection 之 29 列
  （ECUCert 13、SWDL 5、LOGENC 8、SAM 0001/0008/0019）：
  (a) 本包只登記、不產 TC；
  (b) 其中 Verification Criteria 可以黑箱手段觀察者（執行層逐列標 `blackbox_possible = Y/N` 並附觀察手段），
      Pei 於 SEC-02 裁定是否轉為 Testing 類 TC；
  (c) 其餘留 RULINGS 待裁，不得自行標 NA。
```

```text
R-SEC{live+2}  SWE1 原檔異常之處置（R-6 verbatim 原則）
  (a) `SWE1-KeyInsyall-nnn` 拼字保留原字入 `Requirement or Design ID`，Remarks 註 `RD ID spelling as delivered`；登 DR。
  (b) ECUCert 本無 SWE-Requirement ID：暫以 `Source Requirement ID` 欄之 `SYSAD_SEC_ECUCERT_*`（多值時取首值）
      作 `Requirement or Design ID`，Remarks 註 `SWE ID pending DR`；登 DR 要上游補號。
  (c) `Source Requirement ID` 欄之多值以換行、逗號、**空格**三種分隔並存（CertProvider-006/010 為空格串接）；解析須三者皆拆。
```

## §3　裁決引用

| 引用 | 用途 |
|---|---|
| `R-G42` | TC ID `NR1L-{ABBR}-{nnn}`、Test Group、D 欄升冪 |
| `R-G66` | 來源集中制 `sources/raw/<doc_id>/` |
| `R-G67` | intake 嵌入物件檢查（六本 SYSAD docx 含圖／Visio；執行層查並回報，不解包） |
| `R-G70`、`R-G71` | profile 啟用 lint `P`(v4) 與 `X`；本 feature 無 HMI，`X` 之適用性見任務 5 |
| `R-G23` | 取號現查 |
| `R-G13` | 查無不等於不存在 |
| `R-CAM{…}`（CAM-01 §2） | Vehicle Model 七欄 1/0、598/5210 一律 0 —— Security 六本預判**全部車型相同**（middleware，與車型無關；ECUCert 037 R9 明寫「與車型無關」），任務 4 驗證 |
| IN §4.3.1／§8.4.3／§10.7／§11 | test_item 兩段式、PENDING 佔位、spec_ref、格式 |

## §4　執行層任務

**任務 0 — 投遞區確認（R-G44′ sha 同一性 ＋ R-G45／R-G66 不存副本）**

(a) **必投遞 `_intake/Security/`**（分析層以 sha256 比對，repo 內預期無同體；執行層以 `find` ＋ sha 複驗）：

| 群 | 檔 | sha16 |
|---|---|---|
| 037 ×6 | §1 表六本 | 見 §1 |
| SYS3 ×6 | `FM-WI-FSM-035-A02-N1L-SYS3-CertProvider-SYS3R1-V.F …_SYSAD.docx` | `c6517ef2f615670e` |
| | `FM-WI-FSM-035-A02-N1L-SYS3-KeyInstall-SYS3R1-V.D …_SYSAD.docx` | `f1fe71a5e4371da4` |
| | `SYS3_ECUCert_FM-WI-FSM-011-A01 …_SYSAD.docx` | `d5acfcc4c4db88dc` |
| | `SYS3_SAM-FM-WI-FSM-011-A02 …_SYSAD (1).docx` | `e8cf783170039e58` |
| | `SYS3_SWDL_FM-WI-FSM-011-A01 …_SYSAD.docx` | `90526a82c12eb3db` |
| | `SYS3_libLogEncrypt-FM-WI-FSM-011-A01 …_SYSAD (1).docx` | `e47440049c0b00e9` |
| SYS2 ×1 | `REF/SYS2_CFTS084_FM-WI-FSM-035-A02 …_SYSRA_CFTS084_with_NewReq_0331.xlsx`（567.54 KB） | 執行層取 |

(b) **同名異體須回報**：`SYS2_CFTS084_…_0331.xlsx` 在 `Security/REF/`（567.54 KB）與 `CCVR/To Validation team/`（565.47 KB）**大小不同**；執行層兩本取 sha，diff `Basic Report` 列數與 B/D 欄差異，回報後 Pei 裁用哪本。未裁前以 `REF/` 本為準（037 所引者）。
另 `21047_24_00081_…v2.7.xlsx` 在 `Security/REF/`（109.78 KB）、`CCVR/`（109.35 KB）、`CCVR/validation report/`（97.36 KB）三處，取 sha 回報，**不投遞**（SYS2_Mapped 本已內含）。

(c) **reference-only 投遞 `_intake/Security/ref/`**（登 MANIFEST，`note = reference-only`，不登 doc_id 作追溯錨）：

| 檔 | sha16 | 用途 |
|---|---|---|
| `CCVR/To Validation team/21047_24_00081_…v2.7_SYS2_Mapped.xlsx` | `b5470cdf4b210aa6` | SYS2 索引（737 列，`SYS-RA-SEC`↔`NRL`↔原文↔Harman/MD 狀態）＋ `Mapping detail` 356 列 ＋ `Cert Val CS.98` STEPS |
| `CCVR/To Validation team/[NR1L][CCVR]ECU Cert Test Steps.20260826.pdf` | `ed8e0df070fa13ba` | ECUCert 環境建置六步、路徑、`ecucertstatus` 值 |
| `CCVR/To Validation team/Cert Val CS.98/Test_Items.txt` | 執行層取 | 15 條 JUnit 方法名 ＋ `adb shell am instrument` 指令 |

**不投遞**：`CertProviderAndroidInstrumentalTest.apk`、`*_IntegrationTests.zip`、`CertProvider_Test_Demo_20260408.pptx`、`CCVR(from Melco transfer)/*`、`plan.xlsx`、`backup/`（MANIFEST 以 `note` 記外部路徑）。

**任務 1 — scaffold**
```
python scripts/new_feature.py Security --root .
```
ABBR 候選見 §7；六本是一本 workbook 六個 Test Group、還是六本 workbook，列 DECISIONS [PROPOSED]，Pei 裁。

**任務 2 — 來源登錄（R-G66）**
全部投遞檔登 `sources/raw/<doc_id>/` ＋ `MANIFEST.tsv`（`features` 欄 = `security`）。六本 SYSAD docx 依 R-G67 檢查嵌入物件並回報數量，不解包。

**任務 3 — 三段追溯矩陣 `features/security/data/trace_matrix.tsv`**
寫 `features/security/scripts/build_trace_matrix.py`，可重跑，輸出欄：

```
swe1_file | swe1_id | swe1_title | source_ids(raw) | sysad(normalized) | match_kind | sys_ra_sec | nrl | sys2_category | harman_status | md_status | existing_test_rel | ccvr_sheet_rows | new_test_ids | apk_method | verification_method | in_round
```

解析規則（**寫死，不得改**）：
1. SWE1：sheet `Analysis Report`，row 9 起，A 欄或 B 欄任一非空即資料列；B 欄依換行／逗號／空格三分隔拆（R-SEC{live+2}(c)）。
2. SYSAD 正規化：去除**所有**空白字元後比對（SAM 037 之 `SYSAD_SECURITY_ LOGENCRYPT_…` 與 SYSAD docx 皆有內嵌空格變體）。
3. SYS3 style A（CertProvider／KeyInstall）：`python-docx` 逐表，首列首格以 `SYSAD_ID` 起者，`SYSAD_ID` 列取值、`Mapped SYSRA-ID` 列以 regex `SYS-RA-SEC-\d+` 取全部（其中夾雜之 `SYS-RA-SD00126-010` 類非 SEC 家族 ID 另欄 `other_sysra` 保留，不入 sys_ra_sec）。
4. SYS3 style B（ECUCert／SAM／SWDL／libLogEncrypt）：同一表同一列同時含 `SYS-RA-SEC` 與 `SYSAD_` 者，該列全部 SYSAD 對該列全部 SEC。
5. `match_kind`：`exact`／`base`（去 `_COMP`／`_INTF`／`_API`／`_BINDER` 尾綴後命中）／`none`；**`base` 只在 `exact` 落空時試**，且逐筆標明。
6. SYS2 索引：`SYS2_Mapped.xlsx` sheet `SYS2 traceability`，A 欄 = `SYS-RA-SEC`，取 B(NRL)、D(Source category)、G(HARMAN)、H(MD)、I(Existing test relationship)、J、K。
7. CCVR：sheet `Mapping detail`，D 欄 = `SYS-RA-SEC` → A 欄 `<sheet> / <row>` ＋ E 欄 Relationship；`apk_method` 由 `Cert Val CS.98` STEPS 欄（D 欄）以 regex `[a-z][A-Za-z]+Test[A-Za-z]*` 抽取，並與 `Test_Items.txt` 15 條交叉核對。
8. `in_round`：`ccvr_sheet_rows` 含 `Auth-Boot CS.93`／`Auth-Prog CS.93`／`Cert Val CS.98`／`ECU ID CS.165`／`Secure Log CS.212` 任一者 = `Y`，否則 `N`（R-SEC{live}(b)）。

另輸出 `features/security/data/trace_summary.md`：每本 037 之 exact/base/none 計數、SYS2 命中數分佈、in_round Y/N 計數、Harman `Rejected` 命中列表 —— 全部附查詢條件。

**任務 3 附帶回報（不裁定）**：
- 3-1 SAM 037 `Source Requirement ID` 用 `_COMP` 尾綴（19 列全是），SYS3 SAM Table 11 只列基底名；`base` 命中清單逐列。
- 3-2 `SYSAD_SAM_PACKAGE_INTF`（SAM 0005~0008 引用）Table 11 查無；確認 SYSAD docx 全文 grep 結果。
- 3-3 LOGENC 003/007/008/009 之 `*_INTF` 四個 SYSAD 在 libLogEncrypt Table 11 之**原文寫法逐字貼回**（分析層只靠正規化才對上）。
- 3-4 CertProvider-006/010 拆空格後之命中。
- 3-5 Harman 狀態 `Rejected / MD Accepted` 之 SYS2 集中於 SWDL 鏈（335~428、619~664）與 ECUCert 鏈（555/562/563/566/659~664）；逐列列出，附 SYS2 原文首 60 字。分析層預判為責任歸屬（MD 承接），**語意待 Pei 確認**，不得據此排除。
- 3-6 `Secure Log CS.212` 在 `Mapping detail` 僅對 `SYS-RA-SEC-671`（Reference only）；六本 037 之鏈**皆不經 671**。故 CS.212 之五個情境要靠 SWE1 原文之 log 斷言（KeyInstall-010 `log a security violation`、SAM-0004 `logs an error via logdog`、CertProvider-005 log 觀察、KeyInstall-012 avc denied）接上；執行層 grep 六本 037 之 Verification Criteria 含 `log`／`Logdog`／`avc` 者逐列列出。
- 3-7 六本 037 Verification Criteria 含之具體資產／路徑／指令全表（`SecurityAssets/oem-certs/cert-provider/`、`/odm/etc/cert_store`、`/mnt/vendor/oemkeys`、`/data/vendor/dauth/{ts,vc,seqId}`、`installstate` 0~3、`0900 0000 0000 0000`、openssl verify 指令、`getsslog`、`logdecrypt_Ver2.sh`、`HUssss_YYYYMMDD_HHMMSS_XXXX.enc` …），供 DR 清單與 §5.3 常數候選。
- 3-8 `features/sw_update/` 與 SwdlSecureLib 之範圍重疊檢查：列出 sw_update 之 framework Layer 2 與其 037 來源，回報是否已涵蓋 `SWE1-SRA-SECURITY-SWDL-*`；**不動 sw_update**。

**任務 4 — framework 草案驗算（§5）**
以 trace_matrix 回填 §5 各表 leaf 數與 in_round 數；逐列歸屬檔 `features/security/data/layer2_assign.tsv`；不一致逐筆記明，**不自行改 Layer 2 名稱**。Vehicle Model 七欄預判全列相同（五有效車型 `1`、598/5210 `0`）；若任一 037 列含車型／平台條件（grep `HDCC`／`DT`／`Atl`／`platform`／`vehicle line`），逐列回報。

**任務 5 — profile 骨架 `docs/runtime/profiles/FW036_R1L_Security_Profile.md`**
啟用 lint `P`(v4)；`X`（導航路徑）因本 feature 無 HMI 入口，提 [ADD §5.8] 固定入口點擴增候選：`adb shell`／`USB drive`／`DIAG tester`／`Dealer App`（ECU Cert Test Steps 之 CSR 匯出入口），並允許 `$` 指令行為主流程（§5.4）。
[ADD §10.7(c)] 候選：母體為 037 時 `specification_reference` = `{037 檔名 token}_{SWE1-ID}`，例 `SWE1_CertProvider_V1.0_SWE1-CertProvider-004`；ECUCert 依 R-SEC{live+2}(b)。**格式歸 Pei 裁**，列 DECISIONS。
[ADD §5.3] 常數候選由任務 3-7 產出（如 `RUN_CP_INSTRUMENT_TEST`：`$ adb shell am instrument -w -e class com.mitsubishielectric.ahu.efw.lib.melcocertprovider.libcertproviderservice.test.CertProviderServiceManagerTest#<method> com.mitsubishielectric.ahu.efw.lib.melcocertprovider.libcertproviderservice.test/androidx.test.runner.AndroidJUnitRunner`）。

## §5　framework 草案（Layer 1–3；未鎖定，Pei 裁）

### Layer 1

案 A（分析層推薦）：一本 workbook，**六個 Test Group** = 元件名：`Cert Provider`／`Key Install`／`SAM`／`ECU Cert`／`SWDL Secure Lib`／`Log Encrypt`。
案 B：Test Group = `Security`，Test Set 以 CCVR 五 test item 之能力命名（`Certificate Validation`／`ECU Identity`／`Authenticated Boot`／`Authenticated Programming`／`Secure Log`）—— 與本輪範圍裁定同構，但跨元件之 Test Set 會使 §4.2「同 Test Set 同 setup 型態」失效（Cert Provider 與 KeyInstall 之 setup 完全不同）。
Pei 擇。

### Layer 2（案 A 之下；in_round 欄由任務 4 回填）

| Test Group | Test Set | SWE1 | CCVR 落點（分析層實測） |
|---|---|---|---|
| Cert Provider | `Chain Verification` | 001 | Cert Val CS.98、Auth-Boot CS.93 |
| | `Field Matching` | 002, 003, 011 | Cert Val CS.98 |
| | `Revocation` | 004, 005 | Cert Val CS.98 |
| | `Trust Store` | 007, 009 | Cert Val CS.98、Auth-Boot CS.93 |
| | `Client Interface` | 008 | Cert Val CS.98 |
| | （非功能） | 006, 010 | 待 3-4 |
| Key Install | `Temporary Key` | 001 | Cert Val CS.98 |
| | `Installation` | 002, 003, 004, 007 | Cert Val CS.98（007 另 Auth-Boot） |
| | `Overwrite Protection` | 005 | Cert Val CS.98 |
| | `Install State` | 006, 011 | Cert Val CS.98 |
| | `Persistence` | 008 | Cert Val CS.98 |
| | `Crypto Service` | 009, 010 | Cert Val CS.98；010 另接 CS.212（log a security violation） |
| | （非功能） | 012, 013 | 012 接 CS.212（avc denied） |
| SAM | `AuthData Reception` | 0003 | **無** |
| | `AuthData Verification` | 0005, 0006, 0007, 0010, 0012 | **無**（0007 之 SAM cert 經 apk `samCertTestNormalFlow` 間接接 CS.98） |
| | `Installation` | 0009, 0011, 0018 | 0018 → Auth-Prog CS.93 |
| | `Target Notification` | 0013~0017 | **無** |
| | `Error Handling` | 0004 | 接 CS.212（logdog error） |
| | （環境） | 0001, 0002, 0008, 0019 | 0001/0002/0019 → Auth-Prog CS.93 |
| ECU Cert | `Certificate Lifecycle` | R18 CSR export, R19 cert export, R20 cert import, R21 download | ECU ID CS.165 |
| | `Verification` | R9 library, R10 service, R11 online | ECU ID CS.165 |
| | `Diagnostic Access` | R12 diag, R13 dealer | ECU ID CS.165 |
| | （IPC／JNI／IO） | R14, R15, R16, R17 | ECU ID CS.165（白箱，R-SEC{live+1}） |
| SWDL Secure Lib | `Package Decryption` | 003 | Auth-Boot／Auth-Prog CS.93 |
| | `Signature Verification` | 004 | Auth-Boot／Auth-Prog CS.93 |
| | `Key Retrieval` | 002, 005 | Auth-Prog CS.93 |
| | （總則） | 001 | 全 |
| Log Encrypt | `Encryption Procedure` | 006 | **無**（CS.212 是 secure *event* log，非 log *encryption*） |
| | （其餘） | 001~005, 007~009 | 無 |

分析層預判 in_round：Cert Provider 9 列、Key Install 13 列、SAM 5 列（0001/0002/0004/0018/0019）＋ 0007 待裁、ECU Cert 13 列（其中 4 列白箱）、SWDL 5 列、Log Encrypt **0 列**。任務 4 以 trace_matrix 實數取代。

### Layer 3

**不入工作簿**（§4.1.5）。以 **SYSAD ID**（SWE1 `Source Requirement ID` 之首值）為 Layer 3 —— 它同時是 SYS3 章節單位與 SYS2 對應單位，四個下游用途（sequencing／sibling／coverage／scope drift）皆可用。

## §6　預期數字（分析層 2026-09-16 實測，openpyxl read_only ＋ python-docx）

| 項 | 預期 | 查詢條件 |
|---|---|---|
| 037 資料列 | 11／13／19／13／5／9 = 70 | `Analysis Report` row 9 起，A 或 B 欄非空 |
| 037 sheet 數 | 4（`封面`／`ChangeHistory 修訂履歷`／`Product Document 記錄封面頁`／`Analysis Report`） | 六本皆同 |
| 欄數 | #1/#2 18 欄；#3~#6 19 欄 | row 8 非空格數 |
| SYS3 之 SYSAD ID 數 | CP 14／KI 17／ECUCert 26（含空白變體）／SAM 17／SWDL 19／LogEnc 13 | regex `SYSAD_[A-Z0-9_ ]+` 去重 |
| SYS3 之 SEC ID 數 | CP 30+／KI 17／ECUCert 29／SAM 13／SWDL 30+／LogEnc 1 | regex `SYS-RA-SEC-\d+` 去重 |
| SYSAD→SEC 對應之 SYSAD 數（style B 四本） | 40 | 同列共現 |
| SWE1→SEC 命中（exact＋base） | 70 列中 68 命中；CP-006/010 拆空格後應補齊 | 任務 3 |
| 鏈上 SEC ID 總數 | 176（區間 181~189, 198~201, 223~228, 292~295, 335~358, 376~397, 399~428, 459~463, 501, 506~532, 543~544, 552~577, 588, 619, 638~643, 656, 659~664, 683） | 併集 |
| 176 在 `SYS2 traceability` | 176／176，缺 0 | A 欄 |
| 176 之 Existing test relationship | Direct 8／Partial-conditional 42／No existing 98／Reference 16／Heading 10／Out of scope 1／Conflict 1 | I 欄 |
| 176 之 Harman Rejected | 約 55（SWDL 鏈為主） | G 欄 |
| `SYS2 traceability` 總列 | 737 | A 欄非空 |
| `Mapping detail` 總列／Relationship | 356；Supporting catalog 98／Partial 96／Reference only 57／Conditional 54／Direct 33／Supporting catalog reference 14／Unmapped 3／Conflict 1 | A、E 欄 |
| `New test cases` | 47，全 Not executed | A、I 欄 |
| `Test_Items.txt` | 15 條，`fota*` 8／`ecuCert*` 3／`sam*` 1／`secondParty*` 4 | 行計 |
| ECU Cert Test Steps PDF | 6 頁 | pdfplumber |

## §7　待裁項（Pei，SEC-02 前）

| # | 項 | 分析層建議 |
|---|---|---|
| 1 | Layer 1 案 A／案 B | 案 A |
| 2 | ABBR | 案 A：`NR1L-CP`／`NR1L-KI`／`NR1L-SAM`／`NR1L-ECUC`／`NR1L-SWDL`／`NR1L-LOGENC`；案 B：`NR1L-SEC` 單序 |
| 3 | 一本 workbook 或六本 | 一本（本輪 in_round 預判 ≤ 45 列，六本過碎） |
| 4 | §10.7(c) 格式 | `{037 token}_{SWE1-ID}` |
| 5 | Peer Review 29 列之 blackbox 轉換 | 依任務 3 回報後裁 |
| 6 | SAM-0007（SAM cert 驗證）是否納入本輪 | 納入（apk `samCertTestNormalFlow` 可執行） |
| 7 | Harman Rejected 語意（任務 3-5） | 責任歸屬，不排除 |
| 8 | Log Encrypt 本輪 0 列 —— 是否整本延後 | 延後，登 out_of_round |

## §8　DR 清單（本包登記，Pei 送）

| DR | 缺件 | 影響 |
|---|---|---|
| DR-SEC-a | ECUCert 037 無 SWE-Requirement ID | 13 列 ID 暫用 SYSAD |
| DR-SEC-b | `SWE1-KeyInsyall` 拼字 | Remarks 註記 |
| DR-SEC-c | NR1L 正確 OID 之 certificate（CS.98 workshop：舊本 OID 錯） | CertProvider-011 及所有 CS.98 正式 evidence |
| DR-SEC-d | ECU certificate chain（CS.165 workshop：尚無） | ECUCert 端到端、CertProvider-005 全 PENDING |
| DR-SEC-e | 測試資產：valid／broken／revoked／wrong-subject／wrong-issuer／wrong-OID 憑證組（`SecurityAssets/oem-certs/cert-provider/`）；RD 預產之 DCL 檔各撤銷情境 | CertProvider 001~005/011 |
| DR-SEC-f | 測試資產：valid／non-valid／non-platform（R1LRefresh）三組 USB 金鑰 | KeyInstall 002/003/007 |
| DR-SEC-g | 測試資產：各 SAMType 之 AuthData ＋ SAM dongle | SAM 全部 |
| DR-SEC-h | CS.212 五情境之 package（Cybersecurity team review 後） | KeyInstall-010/012、SAM-0004 之 log 斷言關鍵字 |
| DR-SEC-i | `SYSAD_SAM_PACKAGE_INTF` 之 SYS2 對應 | SAM 0005~0008 追溯 |

已結（不登）：SYS3 六本、SYS2 CFTS084、CCVR SYS2_Mapped、Test Steps PDF、Test_Items.txt（REF／CCVR 已有）。

## §9　上繳要求（`up/20260916_SEC-01.md`）

1. 投遞檔 sha256 全表（含任務 0(b) 兩組同名異體之 sha 與 diff）；MANIFEST 登錄；R-G67 嵌入物件計數。
2. scaffold 產物；`RULINGS.md` 三條 R-SEC 實際號與 body sha8。
3. §6 全表預期 vs 實測。
4. `trace_matrix.tsv` ＋ `trace_summary.md` ＋ `build_trace_matrix.py`；任務 3-1~3-8 逐項。
5. §5 Layer 2 表回填 leaf／in_round 數 ＋ `layer2_assign.tsv` ＋ `out_of_round.tsv`；Vehicle Model 條件 grep 結果。
6. profile 骨架 ＋ DECISIONS [PROPOSED]（§7 八項）。
7. 未結 DR、自報錯誤。

## §10　升級條件

- 投遞檔 sha 與 §1／§4 不符。
- `new_feature.py` 因 `features/security/` 已存在而拒絕。
- SWE1→SEC 命中率（exact＋base）< 66/70。
- 176 個 SEC ID 在 `SYS2 traceability` 有任一缺。
- `Test_Items.txt` 15 條與 `Cert Val CS.98` STEPS 抽出之方法名不交集。
- 任何需改 canon 或全域 ledger 之情形。
