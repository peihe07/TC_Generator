# 審閱 GC-06 上繳包（2026-09-05，Tier 2）

對象：`up/20260905_GC-06.md`　下放包：`down/20260905_GC-06.md`＋`_addendum.md`
結果：**收受**。`canon_refs` 789→158、`ruling` unresolved 963→0：A-GC15 之診斷成立且已修。7-4 三項裁於 §一。

## 一、7-4 三項

**1　`gates_tsv` 之 `body_kind`** —— 裁：**補 `DOCS_CHECKS` 再重生** [DEFAULT]。人手列不可持續（產生器一跑即除名，與本次同型）；除名則失去 R-G68 之機檢。改 `scripts/gates_tsv.py` 屬 gate code，Pei 准後由執行層於 GC-08 施作並附前後 diff（預期 48→56：+8 −0）。

**2　`driver_distraction/feature.yaml:17`** —— 由 Pei 於同一 commit 內改（§三命令含之）。

**3　`A-SU7` 盲區** —— sw_update 線補一節交付紀錄（`RULINGS.md`：交付日、commit `ef3ade0`、現行 sha、「無登記前 sha 可對，曾否變更不可證」逐字）。GC-08 執行層抄本裁決。不得寫「未變更」。

## 二、收受清單
`ruling_anchor.py`（錨點常數唯一，R-G43(d)）、`canon_refs.py` 索引面增台帳（canon 側判準不動——首版反升 1682 之修正方向正確，理由入註收受）、同號兩側判 resolved、3 支測試、`gc06_driver_distraction_mv.sh`（未執行）、ics MANIFEST 改指現行 sha＋A-ICS138＋RULINGS 節、sw_update 補登＋A-SU7、R-G36(b)／R-G43(d)／A-GC14／A-GC15 入台帳與 ANOMALIES、judged row 7–16 改註。
`gates_tsv` 不重生之判斷**正確**（逕行重生會除名在效閘）。
2-3 節 waiver `ruling` 母體為 0 之陳述合 R-G50。

## 三、`down/20260905_GC-07.md` 之說明
該包**非本 session 分析層所寫**（本層 GC 系列至 GC-06 補遺止；GC-07 為另一分析層 session 依 Pei 兩則口頭指示所立，mtime 20:58:33）。其 R-G70／R-G71 取號與台帳現況（`### R-G` 最高 R-G69）相容；本層不代審、不重複取號，GC 序號自 GC-08 續。GC-07 §二（§8.7.5 v3→v2 回歸）為 Pei 裁項，本層意見於聊天另陳。

## 四、Pei 動作（順序固定）

```bash
cd /Users/peihe/Work_Projects/TC_Generator
# 1 GC-06 產物＋未追蹤（逐檔）
git add -- scripts/ruling_anchor.py scripts/gc06_driver_distraction_mv.sh \
  docs/fw036/handoff/down docs/fw036/handoff/up docs/fw036/handoff/PROTOCOL.md
git commit -m "feat(fw036): GC-06 canon_refs resolves ledger anchors (R-G43d), delivered MANIFEST fixes, GC-05 closeout" \
  -- scripts/ruling_anchor.py scripts/canon_refs.py scripts/rulings_hash.py scripts/gc06_driver_distraction_mv.sh \
     tests/test_canon_refs.py docs/fw036 docs/reports/rg43_manual_20260905_judged.tsv \
     features/ics_management/delivered/MANIFEST.tsv features/ics_management/ANOMALIES.md features/ics_management/RULINGS.md \
     features/sw_update/delivered/MANIFEST.tsv features/sw_update/ANOMALIES.md
# 2 指紋表再重生（+R-G36(b)、R-G43(d)）
python3 scripts/rulings_hash.py && python3 scripts/rulings_hash.py --check
git commit -m "chore(fw036): regenerate RULINGS.sha.tsv (+R-G36b, R-G43d; 765 rows)" -- docs/fw036/RULINGS.sha.tsv
# 3 driver_distraction 搬移＋feature.yaml 同一 commit
bash -n scripts/gc06_driver_distraction_mv.sh && bash scripts/gc06_driver_distraction_mv.sh
sed -i '' 's#workbook: "workbook/driver_distraction_00.xlsx"#workbook: "sandbox/driver_distraction_00.xlsx"#' features/driver_distraction/feature.yaml
grep -n 'workbook:' features/driver_distraction/feature.yaml     # 應指 sandbox/
git add -- features/driver_distraction/feature.yaml
git commit -m "chore(driver_distraction): move workbooks to sandbox/ per R-G64" -- features/driver_distraction
python3 scripts/lint_paths.py 2>&1 | tail -3                      # 應 0
# 4 stale waiver 1 列（只減）
python3 scripts/canon_refs.py --waiver --gate 2>&1 | grep -i stale
```
第 4 步輸出貼我後給刪列命令（不猜是哪列）。`git status` 若 `features/vsm_v4*` 有 modified，第 1 步 pathspec 已逐檔，不會帶入。

## 五、GC-08 預告（待 §一-1 准）
`gates_tsv.py` 補 `DOCS_CHECKS` 再重生；sw_update 交付紀錄節；`GATES.tsv` 前後 diff。
