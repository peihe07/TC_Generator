# 下放包 CAM-08 —— CAM-07 審閱落檔 ＋ 全批句式修正 ＋ batch02b（`-002`／`-005`／`-017`）

指派執行層 session：延用。對應審閱：`down/20260923_CAM-07_review.md`（同發；Pei 上繳後撤回其不同意者）。

---

## §0　禁區

同 CAM-03 §0。本包觸及：四既有目錄之句式修正（原地）、`batch02b/` 新建、條文／profile／selfcheck。不寫回工作簿。

## §1　落檔

取號現查：`{live}` 期望值 **15**（上輪未消耗）。

1. **R-CAM3(f)**（同級錨自立）：CAN source 行僅於「TC 同時勾 Atl-Hi 與 Atl-Mi 車型 **且** Procedure 含 `Send CAN` 步」時書寫；單一 EE 之列直接以該平台訊息名寫，不加該行。
2. **R-CAM15**：Atl-Mi 車型歸屬 —— V3 之母體為 376；同一驗證點 V42／V33 無對應條文時 V3 列承 637／2261／376，有對應時只承 376；V42 有而 V33 無時 V42 列承 637、V3 列承 376（文面依上繳 CAM-07 §4-4）。
3. **RDF-07**：`SYS-RA-VF551_V33-420` `Rear_Camer_Enable.Info` 拼寫。
4. **profile**：`J` 豁免條（上半逐字）；§9 常數表；A-CA27 加註。
5. **GCB-07**：lint `J` 改以逐字為豁免條件。
6. **selfcheck_camera.py** 加第 5 項（CAN source 行之適用條件：勾兩 EE ∧ 有 `Send CAN` 步，否則命中）、第 6 項（`Select ".+" and set it to` 命中）。
7. DECISIONS 6-19～6-22：selfcheck 放寬准、`-068` 對應准、`-061` 不補第三列、A-CA27 加註。

## §2　全批句式修正（pilot02／batch01／batch01b／batch02a，72 列）

依審閱 §二五型：
- selfcheck 第 5 項掃描 → 命中列刪 CAN source 行（或補正單 EE 訊息名）；
- 第 6 項掃描 → `Set "<label>" = "<Option>"`，Option 逐字取 HMI Settings List 該列選項欄（回報欄位與列號）；
- CAN 讀取句式一律 §8.7.5(d)；`is transmitted` → `is sent`；
- `Hold for <n>` 獨立成步；
- 無 `*` 之設定列刪品牌行。
產出 `DIFF_CAM07_to_CAM08.md`（四目錄合併，逐列欄級）；lint ＋ selfcheck（六項）全綠。

## §3　batch02b 生成：`-002`（19）＋ `-005`（1）＋ `-017`（2）＝ 22，ID 續 `-073`

依 `batch02_plan.tsv`。要點：
- `-002`：PROXI 讀取時機與生效（`Rear_View_Camera = Present`）；`Rear_View_Camera_Type` Digital（`V2-555`）／Analog（`V4-112`）分列且只勾 Atl-Hi（376 無該參數，R-CAM13(c) 同理）；PROXI 值逐字自 `forms/proxi/`（列號回報）；不生成 Absent 側（RDF-06）。
- `-005`：解析度 `1280 x 800`（`V3-254`）可判；格式面 `PENDING: DR-CAM-m`（新開，EVS HAL 支援之影像格式來源）。
- `-017`：`CameraDisplaySts`（CFTS092 `CAM-097`／`-098`，FFC 節）→ Test Set `Auxiliary Cameras`；PROXI `Forward_Facing_Camera = 1`；376 若 DBC 無 `CameraDisplaySts` 則勾 0（同 `-045`）。
- R-CAM3(f)／R-CAM15 自本批起適用。

## §4　上繳要求（`up/20260923_CAM-08.md`）

1. R-CAM3(f)／R-CAM15 sha8；RDF-07；profile；GCB-07；selfcheck 第 5／6 項實作與對 72 列之命中清單。
2. `DIFF_CAM07_to_CAM08.md`；四目錄 lint ＋ selfcheck。
3. batch02b 22 列產物、lint、selfcheck、PENDING（DR-m）。
4. 自報錯誤；未結。

## §5　升級條件

- selfcheck 第 5／6 項命中 > 20 列（停，回報分佈後再改）。
- `-002` 之 PROXI 讀取時機於 SYS2 無原句（只在 037）→ 該驗證點 PENDING，不採 037。
- batch02b > 30。
