# 下放包 CDD-07 —— R-DIAG19／DR-DIAG-7 ＋ batch 5（Layer 2 #3 SXM Subscription ＋ #4 SXM Package Routines，50 列）

前件：`down/20260917_CDD-06.md`、`up/20260917_CDD-06.md`、`down/20260917_CDD-06_review.md`。
依常規：審閱後即發；依據 = CDD-06 審閱 §二～§四。執行層 session：沿用 CDD 線。
**與 GC-16 之先後**：不得並行。GC-16 先 → 指紋依 R-G76(b) 重生；本包先 → R-G75。上繳註明。

---

## §0　禁區

- git 一律不動；push 依審閱 §五-1（rebase，不強推），由 Pei 執行。
- 不動 `down/`、`up/`；不動其他 feature 目錄。
- **只產 batch 5 之 50 列**；其餘列不得生成；framework 已 LOCK，歸屬不改。
- 條文本體不改；R-DIAG19 逐字落。
- 升級條件命中者，上繳包首段必須明記「命中，處置如下」，不得沉默（`[A-DIAG44]` 之教訓）。
- 取 lint 紅數前先判 `Traceback`；子集與全集紅數不一致即停。

## §1　依據

`R-DIAG19`（KeySense HMI L&F 缺件）逐字取自 `down/20260917_CDD-06_review.md` §四；DR-DIAG-7 登 `DATA_REQUESTS.md`（對 Nik：`KeySense HMI Logic and Flow` 匯出，含版本）。
追認：LOCK v01、`-281` PENDING、`-290` ER 限縮、R-DIAG18(1) 148 條就地定型。

## §2　任務

**T0 — 條文與指紋**：R-DIAG19 落 `RULINGS.md`；DR-DIAG-7；指紋依制。`[A-DIAG44]` 登 ANOMALIES。

**T1 — batch 5 sibling plan（先出後產）**

| Layer 2 | DID | 037 列 | 分析層預判 |
|---|---|---:|---|
| #3 SXM Subscription | `$2844`（24）、`$2845`（3）、`$2846`（3）、`$2847`（4）、`$2848`（3）| 37 | `$2844` 8 源三段式（Suspend Year／Month／Day、Reason Text、Reason Code、Phone Number、Subscription Status、…）：**每列只追一子欄位**（GPS 前例）；`-266` Subscription Status 之四狀態（Not Subscribed／Subscribed／Suspend Alert／Suspended）為 CFTS004 明載列舉 → **狀態軸 sibling 四條**（每態一 TC，PC 為該訂閱態，來源 CFTS004-<ObjectID>）；`$2847` 讀寫（`-236`～`-238` 之 apply 成功／失敗為 CFTS004 明載，各 1）；`$2845`／`$2846`／`$2848` 讀 1 ＋ neg 2 ＋ uns 1 |
| #4 SXM Package Routines | `$0307` Validate（5）、`$0309` Select（8）| 13 | 037 VM 明書「Indication Code responses across all retry scenarios／retry paths」：**retry 軸只在 CFTS004 明載重試次數與碼值時拆**，否則單條 ＋ `<Indication Code>` 佔位（DR-DIAG-5）；Start／Results 兩步；PC-3 明文（Routine）|

全部 SXM 列 Region = NAFTA → Vehicle Model `1 1 1 0 0 0 0`（R-DIAG7(amend)）；Harman Status 依 tsv 註 Remarks。
訂閱態之建立方式（如何讓 HU 處於 Suspended）CFTS004 未載 → Pre-Condition 寫該態（狀態即規格觸發，§8.5 例外），**不寫建立步驟**；`pc_sources` 記 CFTS004 錨。

**T2 — batch 5 產出** → `sandbox/batch5/diagnostics_batch5_v01.xlsx`／`batch5.json`／`coverage.tsv`
- ID：新增自 `-291` 起；`-011`／`-012` 仍留號（#1／#2）。
- 其餘規格同前包（位元組式、佔位登錄、Remarks 句式、PC 守門）。

**T3 — lint／自檢／coverage／合併本**：七本各跑（先判 Traceback）；§9 17 項；R-SEC15 型；coverage 50/50；`merged/diagnostics_v05.xlsx`；`placeholder_summary.tsv` 更新。

## §3　預期數字

| 項 | 預期（分析層依 037 列型態推算，未逐列查 CFTS004）|
|---|---|
| batch 5 TC | 50 列 → 60–75（含 `-266` 狀態軸 +3）|
| PENDING | 0（`-156`／`-237` 之 DR-DIAG-1 不在本批）|
| lint 真違規 | 0 |
| 合併本 | ≈ 350–365；覆蓋 18 組／264 列（67.9%）|

## §4　上繳要求（`up/20260917_CDD-07.md`）

1. T0 數字；指紋制。2. sibling plan 全表（含 retry 軸之 CFTS004 查證結果）。
3. TC 清單；貼入 **`-266` 四狀態之一** ＋ **`$0309` Select 一條** 全文。
4. lint 紅數歸因（附 `LINT_STATUS`／Traceback 判定）；自檢；coverage；§3 對表；自報錯誤；未結；建議 commit。

## §5　升級條件（命中必明記）

- 指紋集合不符／`--check` 不綠。
- `-266` 四狀態之 CFTS004 錨不足四個（表示狀態軸無據，改單條 ＋ 佔位並回報）。
- retry 軸 CFTS004 明載而拆分後任一 SWE1 列 > 6 TC。
- 任何需改 canon 之情形。
