# 下放包 SEC-06 —— Revise v03（SAM-0008）＋ 三項裁定落檔 ＋ 封面／Reference sheet 填值 ＋ 交付候選 `security_v02` 三件組

前置：`up/20260916_SEC-05.md`、`down/20260916_SEC-05_review.md`。
Pei 2026-09-16 裁：review §三 三項**准**。git 已修復，SEC-05 四支 commit 落完後開本包。
執行層 session：新開。

---

## §0　禁區

- `down/`／`up/` 既有檔不動；其他 feature 不動；canon 不動；`lint036.py` **本包不動**。
- `framework.md` 本包唯一一次解鎖（Revise v03），只移 SAM-0008 一列；改完重新 LOCKED v03。
- `batch1_v01`／`batch1_v02`／`batch2_v01`／`merged/security_v01` 四本 xlsx 不動（R-TM13）；新本另檔名。
- 封面類 sheet 之填值**只填 SWC 0708 交付本有填之格**，不新增格、不改版面。
- PENDING 124 行不動；不得因交付而改寫或刪除任何 PENDING 行。

## §1　裁定落檔（取號現查；預期 R-SEC19）

```text
R-SEC16 amend  batch 2 六列指派：SAM-0006／0010／0011／0012 = P0（驗證之接受／拒絕決定）；SAM-0003／0009 = P1（介面與儲存）。
               Remarks 之 `priority assigned by analogy; …` 註移除。
R-SEC17 Revise v03  SAM-0008 由 `Service Environment` 移至 `AuthData Verification`；`Service Environment` 剩 SAM-0001／0002／0019。（A-17 更正）
R-SEC18 amend  (a) 刪 `POWER_MODE_STS ↔ PowerModeSts` 對照（A-19：分析層推測橋，DBC 證實無 resume 值）；只留 `IGNITION_STATE ↔ CmdIgnSts`。
               (d) `/data/vehicle/dauth/seqId` 更正為 `/data/vendor/dauth/seqId`（A-14）。
R-SEC19  SAM-0013 之 resume-from-suspend 分支：037 未載觸發手段，VHAL Guide 無對應 → `NR1L-SAM-025`／`-026` 之觸發步驟整行
         `PENDING: X-n suspend/resume trigger method`，ER 對應行同為 PENDING；`BCM_FD_9` 之佔位式移除。`X-n` 登 EXEC_ASSETS（15→16）。
```

## §2　Revise v03 與 v02 產物

1. `framework.md`：加 `REVISE v03 2026-09-16 (R-SEC17 Revise)` 段（一列移組）；LOCKED v03。`layer2_assign.tsv` 同步（1 列）。
2. `gen_batch2.py` 重跑 → `sandbox/batch2/security_batch2_v02.xlsx`：預期與 v01 之逐格 diff = **SAM-0008 兩 TC 之 `Test Set` 2 格 ＋ 六列 Remarks 之類推註移除 ＋ SAM-025/026 之觸發步驟／ER／Remarks 改 PENDING**；差異逐格列回報。
3. `build_merged.py` 重跑 → `sandbox/merged/security_v02.xlsx`：TC 100、PENDING 預期 124 ＋ 4（SAM-025/026 各 Proc＋ER）＝ **128**；lint 門檻 0、`K=0`、selfcheck 0。
4. `coverage.tsv`／`pending_summary.tsv` 更新（token 12）。

## §3　封面類 sheet 與 Reference sheet（先量測後填）

### 3.1　量測（任務先行，回報後填）

對 **SWC 0708** 與 **Home 0809** 兩本交付本，逐 sheet 列出**有填值之格**（座標、值型態、逐字值）：
`Cover 封面`、`ChangeHistory 修訂履歷`、`Product Document 記錄封面頁`、`Reference`。
另量測兩本之**檔名式**（逐字）與 `ChangeHistory` 首列之版本號／日期／作者／描述寫法。
兩本不一致者以 SWC 0708 為準；回報表為 `features/security/data/cover_fields.tsv`（欄 `sheet | cell | swc_value | home_value | security_value(proposed)`）。

### 3.2　填值原則

- 版本：`V0.1`（或量測所得之首版式）；日期 `2026-09-16`；作者依量測之欄位型態（Pei 之署名式逐字沿用）；描述 `Initial release`（或量測所得之首版描述式）。
- 文件名／專案名／feature 名：依量測格之型態填 `Security`（Test Group 六名不入封面）。
- `Reference` sheet：只列**追溯母體與已於 Remarks `source:` 引用之來源**，依量測之欄型（檔名、版本、日期）逐字：
  六本 037（含 sha 不入，檔名含版本）；SYS3 SYSAD 六本；`SYS2_CS00102_SWAD`；`CertProfile_BETA_ROW_CS_SUBCA_G1`；
  `[NR1L][CCVR]ECU Cert Test Steps.20260826.pdf`；`Test_Items.txt`（若量測顯示 SWC 列有非文件類來源，否則不列）；
  `VHAL_User_Guide_R5.pdf`；四本 DBC（依 SWC 先例是否列 DBC 決定）。
  CCVR v2.7、R1L Diag SWQT、DTC Matrix 為 reference-only：**依 SWC 先例**（SWC 有列 reference-only 者則列，否則不列），回報依據。
- 任何量測格為空者，Security 亦留空；不得補 SWC 未填之格。

### 3.3　交付檔名

依量測之檔名式產出 `features/security/sandbox/delivery/<依式命名>.xlsx`（內容 = `security_v02.xlsx` ＋ 封面／Reference 填值），sha 回報；**`security_v02.xlsx` 本身不動**。

## §4　交付候選三件組

`features/security/sandbox/delivery/`：
1. `<依式命名>.xlsx`（含 PENDING；**RD review 草稿**，非出貨）。
2. `coverage.tsv`（70 列追溯：51 產出／19 DEFERRED）。
3. `pending_summary.tsv`（12 token → 給 RD 之資產清單）＋ `EXEC_ASSETS.md` 導出之人讀版 `asset_request.md`（每 token：缺什麼、哪些 TC、找誰——`X-c/d/e/e-2/e-3` Steven、`X-f/f-2` KeyInstall 負責人、`X-g/g-2/n` SAM 負責人、`X-h` Steven/Shawn、`X-i` 執行層列出但由 Pei 決定是否裁降、`X-j/l` JY/Rivers、`X-k` Samuel、`X-m` LogEncrypt 負責人）。

## §5　上繳要求（`up/20260916_SEC-06.md`）

1. R-SEC16 amend／R-SEC17 Revise v03／R-SEC18 amend／R-SEC19 之實際號、sha8、`--gate`。
2. framework v03 diff（一列）、LOCKED v03。
3. batch2 v02 與 merged v02 之逐格 diff 清單；merged v02 之 lint 全項、selfcheck、PENDING 128、`K=0`。
4. `cover_fields.tsv` 全表；檔名式量測結果與最終檔名；`Reference` sheet 逐列來源與依據。
5. 三件組 sha；`asset_request.md` 全文。
6. 自報錯誤。

## §6　升級條件

- batch2 v02 diff 超出 §2-2 所列範圍；merged v02 PENDING ≠ 128。
- 封面類 sheet 出現 SWC 未填之格被填、或版面變動（zip 成員／DV 計數變）。
- `Reference` sheet 列入未於 Remarks 引用之來源，或漏列任一 037。
- 任何需改 canon 或再改 Layer 2 之情形。
