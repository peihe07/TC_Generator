# 下放包 VS-SL-02 —— dry-run v2 ＋ Bed Lowering DT 適用性查證

日期：2026-09-02　層級：Tier 2 → Tier 1
前置：上繳包 VS-SL-01（`features/vehicle_setting/reports/vf230_settings_dryrun.md` 等三本）之審閱，見 `down/20260901_VS-SL-01_review.md`
別名候選：`down/20260902_VS-SL-02_alias_candidates.tsv`
本包仍為 **dry-run，不寫回**。§1 先做，§2 隨後；§1 之結果不阻斷 §2。
交換協定見 `docs/fw036/handoff/PROTOCOL.md`；上繳寫 `up/20260902_VS-SL-02.md`。

---

## §1　Bed Lowering Mode 之 DT 適用性查證（優先）

### 問題

| 來源 | 對 DT (Ram 1500, Body_Types = 7) 之陳述 |
|---|---|
| 总控表 V1.1.0 三張 FeatureSet 表（一致） | Atlantis：`Body_Types` 只認 Type 1 (D2) / Type 4 (DJ)；PNet `DT` 欄 `Always false` → **DT 無** |
| HMI L&F 規格 SR24 1A §Main Use Cases | DT = 前升後降；DJ/D2 = 僅後降 → **DT 有** |

分析層無法裁。執行層以手邊全部文件查證，**只報事實、不下結論**。

### 查證對象（逐檔必查，缺檔亦須報「不在磁碟上」）

| # | 檔 | 查什麼 |
|---|---|---|
| 1 | `find /Users/peihe/Work /Users/peihe/Work_Projects -iname "*Appendix_BV*" -o -iname "*ProSys*" -o -iname "*Architecture_Specification*"` | 总控表 `REF Table` 所載之原始來源 `Sys3_ProSys_SoftwareSystem-Architecture_Specification_Appendix_BV.xlsx` 是否在本機。**在** → 讀其 `FeatureSet list(Gen4_Gen5)` 之 Bed Lowering Mode 列逐字；**不在** → 報缺，附 `REF Table` 之 SharePoint 連結原文供 Pei 開 |
| 2 | `/Users/peihe/Work/02_Project_R1LR/10_Reviewing/00_TestCase/ASW-R2/Vehicle Settings/VF230_V1_R5/SYS3_Vehicle_Settings_FM-WI-FSM-011-A01_System Architectural Design_SYSAD_v1.0.docx`（15.9 MB；`VF665/` 有同名副本，先比 SHA 是否同檔） | 全文搜 `Bed Lowering`／`BLM`／`Body_Types`／`Type 7`／`DT`（逐字命中列出前後各一段） |
| 3 | `forms/SR26 Default Settings and PNet ECU Configuration v1_0.xlsx` | `Default Parameters` 與 `PNET ECU Master Configurations` 兩分頁搜 `Bed Lowering`／`BLM`／`Body_Types`／`ASM`／`ASCM`；有車型欄者逐欄列 DT 與 DJ/D2 之值 |
| 4 | `forms/SR24 R1 Market Configuration Table v1.6.xlsx` | `Market Config - R1` 搜同上；DT 行之值 |
| 5 | `forms/PROXI_HDCC27_R3_20250424.xlsx` `Format` 分頁 | `Body_Types` 之完整值表逐字（含 Type 7 之描述）；`BLM`／`Bed_Lowering` 開頭之參數是否存在；**注意檔名 HDCC27 = HD 車系，非 DT** |
| 6 | `forms/Logical Identifiers and CAN Mapping v1_78.xlsx` `Proxi & Configuration` 分頁 | 搜 `Bed Lowering`／`BLM`／`Body_Types`；`Atlantis High` 欄組之值 |
| 7 | `features/bed_lowering/inputs/` 之 037 報告與 `SYS1_HMI_Bed_Lowering_Mode_…xlsx` | 是否有車型適用欄或 DT／DJ 標註；`Requirement Description` 含 `DT` 之 leaf 列號全列 |
| 8 | `features/bed_lowering/output/…_SWQT_BedLowering_20260827.xlsx` T–Z 車型欄 | 151 列現值（HDCC27／DT27 兩欄各為 0／1／空之計數） |

### 報告格式 `features/bed_lowering/reports/bl_dt_applicability.md`

每檔一節：(a) 檔名＋SHA256 前 16 碼 (b) 查了哪些分頁／哪些字串 (c) 命中逐字（含列號） (d) 該檔對「DT 有無 Bed Lowering」之陳述：**有／無／未提及**。
末段彙總表：8 檔 × 三值。查無者依 R-G13 三要件寫，缺一改記「未查得」。
**不得**把「未提及」寫成「無」；**不得**選邊。

### 停止條件

查完 8 檔即止。不開 DR（DR 由 Pei 於看過彙總表後決定要不要送）。

---

## §2　dry-run v2（依審閱 §2、§5）

### 2.1　PROXI 提議重算（三本）

```text
(1) proxi_now 非空 → 提議 = 形制改寫：$Param$ is set to "label" → Param = raw (label)
                     raw 取 data/_vf230_proxi_values.json；label 查無 raw → RAW_MISSING（不 PENDING）
(2) proxi_now 空、alias 已解、总控表有條件 → 依总控表產出
(3) proxi_now 空、alias 已解、总控表無條件、需求原文無 $var$ → PENDING: DR-{n}
(4) alias UNRESOLVED → 只掛 ALIAS_UNRESOLVED，不掛 PROXI_PENDING、不 PENDING
```

報告 §A 加一列：「(3) 之列數」，並附 D 欄清單。預期遠小於 207；若仍 ≥ 100，停下來報。

### 2.2　別名

- `data/settings_alias.tsv` 併入 `down/20260902_VS-SL-02_alias_candidates.tsv` 之 `tier2_proposal`／`tier2_evidence` 兩欄
- **未經 Pei 逐條認可前，`manual` 者不綁入查找**；v2 先以 `exact` 51 名跑，`manual` 44 名另列「若認可將解鎖之列數」

### 2.3　PATH_ABSENT 分流

92 列「無泛稱句」逐列判：procedure 是否操作某設定項（需路徑）或為行為型（不需）。後者移除 flag；報告列兩者計數與 D 欄清單。

### 2.4　Options 正規化（待 Pei 裁審閱 §4-6；本輪先依此產出，裁定不同再改）

分隔符 `/`、`,`、`+` 兩側各一空格，字詞逐字不動：
`Off/ Only Warning/Warning+ Active Braking` → `"Off" / "Only Warning" / "Warning + Active Braking"`

### 2.5　BedLowering／VehicleCategory

- BL：151 列提議 `PROXI CAN node 27 (ASM/ASCM) = 1 (Present)`；`Body_Types` 行 `PENDING`（待 §1）；7 列 DT 專屬＋16 列「DT 或 DJ/D2」掛 `DT_APPLICABILITY`
- VC：`Camera App` 2 列讀其需求原文，報是 Cam App 還是 Backup Cam，附原文；Specialty／螢幕方向 11 列保留散文、移除 `NO_MAPPING` flag

### 2.6　自檢

- 三本報告列數 457／151／126
- PROXI_PENDING 與 ALIAS_UNRESOLVED 交集 = 0（assert）
- proxi_now 非空而提議 PENDING 之列 = 0（assert）
- 抽 3 條（含 `ForwardCollisionWarning-034`／`-035`）貼 proposed 全文

---

## §3　上繳包內容（寫 `up/20260902_VS-SL-02.md`）

1. `bl_dt_applicability.md`（§1）＋ 8 檔 SHA 表
2. 三本 v2 報告＋摘要（§2）之路徑與 SHA256
3. 2.1(3) 之列數與清單、2.2 之「若認可解鎖列數」
4. 自檢四項結果
5. 自報錯誤（若有）
