# CDD-13 附錄 A —— `1_Customer_Requirement/STLA Standard/` 有 CS 原件：DR-DIAG-9 即刻可結，Annex A 可用

Pei 2026-09-18 指出該目錄。分析層實測（執行層複驗）：

| 原件 | 在 | 備註 |
|---|---|---|
| `CS.00100.pdf` | **有**，81 頁，Change Level D，2022-06-20，sha16 `c1f9c6d7c5a5f0a2` | **Annex A（p.61–75）之 NRC 流程圖為向量文字，pdfplumber 可抽**；全頁疊有斜向浮水印字元（`C 7 1 5 5 7 F : d i r e m s u o c …`），抽取時須依字元座標／字級濾掉 |
| `CS.00099.pdf`、`CS.00102.pdf`、`CS.00101.pdf`、`CS.00025.pdf`、`CS.00042_.pdf`、`CS.00010_03.pdf` | 有 | CS.00102 之 §5.2／5.4 表可望同為向量文字 |
| `ISO 14229-1-2020.pdf` | 有 | 五碼閉集之最終依據可直接引頁 |
| `SD.00049` 及其附件 `Diagnostic Services Secure Access Rights 28JAN2026.xlsx` | **無**（目錄無 SD.00049） | DR-DIAG-10 維持 |
| 另有 `Update/` 子目錄 | 未查 | 執行層 T0 列出 |

## 對 CDD-13 之修正

1. **DR-DIAG-9 改為「內查結案」**：T0 登錄 `CS.00100.pdf`（`spec-index/sources/`，doc_id `cs00100_uds_on_can_D`）＋ `CS.00099.pdf`＋`CS.00102.pdf`；DR-DIAG-11 同結。
2. **T2 擴為 Annex A 逐圖轉錄** `data/cs00100_annex_a.md`：Figure A-1（General）、A-2（$31）、A-4（$22）、A-9（$2F）、A-11（$2E）五圖必轉；其餘（$23/$34/$36/$37/$3D/$2A/$19/$27）只列標題。每圖為「判斷 → No → NRC」之有序表，**順序即優先序**，逐字取節點文字（含 `NRC $XX manufacturer/supplier specific`）。
   分析層初讀（執行層以轉錄為準）：
   - A-1 General：SID 不支援 → `$11`；SID 不在現 session → `$7F`；安全等級 → `$33`；< 2 bytes → `$13`；子功能不支援 → `$12`；子功能不在現 session → `$7E`
   - A-4 $22：最小長度／奇數長 → `$13`；最大長度 → `$13`；DID 不支援 0x22 於現 session（至少一個 DID 支援，否則）→ `$31`；安全 → `$33`；條件 → `$22`；回應過長 → `$14`
   - A-11 $2E：最小長度 → `$13`；DID 不支援 0x2E → `$31`；總長 → `$13`；安全 → `$33`；條件 → `$22`；data record 無效 → `$31`；未寫入 → `$72`
   - A-9 $2F：最小長度 → `$13`；DID 支援 0x2F 且 IOCP 支援，否則 → `$31`；總長 → `$13`；controlState／mask 支援，否則 → `$31`；安全 → `$33`；條件 → `$22`
   - A-2 $31：最小長度 → `$13`；RID 於現 session 支援，否則 → `$31`；RID 安全 → `$33`；子功能對該 RID 支援，否則 → `$12`；總長 → `$13`；routineControlOptionRecord 有效，否則 → `$31`；條件 → `$22`；序列 → `$24`
3. **R-DIAG5(amend3) 改寫**：來源鏈 CFTS004 → 037 → **CS.00100 Annex A** → ISO 14229-1:2020 §7.5。
   (a) DR-DIAG-1 之 3 列（`-156`／`-157`／`-237`）依 Annex A 對應圖之**第一個**適用判斷取碼，PENDING 清零；`U` 應降至 0。
   (b) 既有 223 列：以 `nrc_coverage` 對照 Annex A —— 長度軸 `$13`、值域軸 `$31`、不支援 SID `$11` 預期全合；**不合者才 Revise**，回報差異表。
   (c) 新負向軸**只在** 037 Description／VC 或 CFTS004 同節點述及者產：安全未解鎖 → `$33`（2E/2F/31 類）、session 不符 → `$7F`、子功能不支援 → `$12`（$31 類）。無敘述者不產（R-DIAG8）。
4. R-DIAG29(b) 不變（controlState 仍佔位）；R-DIAG30(a) 之 0x2F 是否需 0x27 仍待 DR-DIAG-10。
5. §5 增一升級條件：Annex A 轉錄與 CFTS004 明載之 NRC 值**衝突** → 回報不改，Pei 裁（CFTS004 為母文件，位階高於 CS.00100）。

## 上繳補列
Annex A 五圖轉錄檔路徑與各圖節點數；(b) 差異表（預期 0 行）；DR-DIAG-1 三列之取碼與依據圖節點；浮水印濾除法一句。
