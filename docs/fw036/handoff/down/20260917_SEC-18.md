# 下放包 SEC-18 —— `specification_reference` 改為 `CFTS084-{Polarion 號}`（對得上者）／純 SWE1 ID（對不上者），一號一行 → `security_v12`

前置：`up/20260917_SEC-17.md`。Pei 2026-09-17 裁：spec_reference 與 SWC 交付本同式；只有 ID 時寫 ID；**必須換行**。
執行層 session：新開；**不跑 `--gate`**；R-SEC10(c) amend2 之條文先落 `RULINGS.md`，sha 由 GC-16 後補（本包回報 body sha8 由 `rulings_hash.py --dry-run` 或等值方式取得，不寫 tsv）。

---

## §0　禁區
- 只動 `specification_reference` 欄（六本＋`_all`）；其餘欄文字 0 diff；填色、DV、zip 成員不變（沿 SEC-17 外科法或重生後再套色，二擇一回報）。
- 不得寫入 `trace_matrix.tsv` 之外的 SYS2 對應；不得自造 NRL 號。

## §1　裁定（落 RULINGS.md，取代 R-SEC10(c)；R-SEC10(d) 之 `spec_mode F` 定義同步改）
```text
R-SEC10(c) amend2  specification_reference（Security）
  (a) 對得上 SYS2 CFTS084 之 SWE1 列：每一對應之 `SYS-RA-SEC-nnn` 寫一行 `CFTS084-{NRL 數字}`（NRL 取 `SYS2 traceability` B 欄，去 `NRL-` 前綴），
      升冪，一號一行，cell 內以換行分隔（`\n`），不串接；同一 SWE1 之所有 sibling 相同。
  (b) 對不上者（`trace_matrix.sys_ra_sec` 為空）：寫該列之 SWE1 ID 一行（例 `SWE1-LOGENC-002`）；ECUCert 無 SWE1 ID 者寫 SYSAD 首值。
  (c) 選號規則（避免 SYS3 粗對應之噪音）：只取 `match_kind = exact` 且 SYS2 `Source category` 非 Information／Heading／Reference 之號；
      該列若仍 > 10 號，執行層以 SYS2 描述與 037 `Verification Criteria` 之關鍵字重疊（名詞 token 交集 ≥ 2）篩選，
      篩後仍 > 10 者**不填**，暫寫 (b) 式並在 `spec_ref_plan.tsv` 標 `REVIEW`，交 Pei 裁。
  (d) lint：`spec_mode F` 之檢查改為「每行匹配 `^CFTS084-\d{6,7}$` 或 `^SWE1-[A-Za-z-]+-\d{3,4}$` 或 `^SYSAD_SEC_ECUCERT_[A-Z_]+$`；升冪；無串接符」。
  (e) 舊 `{037 token}_{SWE1-ID}` 式作廢；v11 依 R-TM13 保留。
```

## §2　實作
1. `build_spec_ref.py`：自 `trace_matrix.tsv`（`swe1_id | sys_ra_sec | nrl | match_kind | sys2_category`）＋ 037 VC 產 `data/spec_ref_plan.tsv`（`swe1_id | candidates_all | candidates_after_filter | lines | status(OK|REVIEW|FALLBACK) | preview`）。
2. 生成器讀 plan 填欄；`SEC_VER=v12` 重跑六本＋`_all`；套色（SEC-17 法）；lint 六本門檻 0。
3. **換行驗證**：逐格讀 `specification_reference`，多行者 cell 值含 `\n` 且 `alignment.wrap_text = True`；回報六本各「多行格數／有 `\n`／wrap 開啟」三數相等。
4. v11→v12 diff：只在 `specification_reference`（124 格）；其餘 0。

## §3　上繳要求（`up/20260917_SEC-18.md`）
1. `spec_ref_plan.tsv` 計數：OK／REVIEW／FALLBACK 各幾列；每 Test Group 之行數分佈（min/median/max）。
2. REVIEW 列全表（swe1_id、候選數、篩後數、前五個號與其 SYS2 描述首 60 字）供 Pei 裁。
3. 六本 sha、diff 只在一欄之證明、換行三數、lint。
4. R-SEC10(c) amend2 body sha8（乾跑取得，未寫 tsv）。
5. 自報錯誤。

## §4　升級條件
- diff 落在 `specification_reference` 以外。
- 任一 NRL 號不在 `SYS2 traceability` 表。
- 換行三數不相等（有 `\n` 但 wrap 未開，或反之）。
