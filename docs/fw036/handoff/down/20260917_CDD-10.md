# 下放包 CDD-10 —— Revise（R-DIAG22(amend)／R-DIAG20(amend)）＋ 延伸審計（R-DIAG23，不產 TC）

前件：`down/20260917_CDD-09.md`、`up/20260917_CDD-09.md`、`down/20260917_CDD-09_review.md`。
依常規：審閱後即發；依據 = CDD-09 審閱 §三～§五。執行層 session：沿用 CDD 線。
指紋制依 GC-16 是否落地。

---

## §0　禁區

- git 一律不動；push 依 CDD-06 審閱 §五-1。
- 不動 `down/`、`up/`；不動其他 feature 目錄。
- `rulings_hash.py` 只得 `--check`／`--out <scratch>`。
- **本包 T2 只產候選清單，不產 TC**；T1 之 Revise 限 R-DIAG22(amend) 讀類值域軸與常式 4 條。
- 候選清單每一列必須帶錨（037 row ＋ 欄位原句，或 CFTS004 ObjectID ＋ 原句）；無錨者不得列。
- 條文本體不改；三條逐字落。升級條件表自 §5 逐字複製。

## §1　依據

`R-DIAG22(amend)`、`R-DIAG20(amend)`、`R-DIAG23` 條文逐字取自 `down/20260917_CDD-09_review.md` §五。

## §2　任務

**T0 — 條文與指紋**：三條落 `RULINGS.md`；指紋依制；`DATA_REQUESTS.md` DR-DIAG-6 題目改「不支援之 SID 與 DID 清單」；`asset_request.md` 同步。

**T1 — Revise → 各批新版 ＋ `merged/v08`**
(a) 讀類值域軸 85 條改「unsupported DID」型（請求 `$ 22 <unsupported DID>`、描述行、ER `7F 22 31 (requestOutOfRange)`、下半 token、佔位登錄 DR-DIAG-6）；逐條表附 tsv。
(b) SID `31` 之 4 條值域軸：CFTS004 有越界值者填之，無者 `<out-of-range value>`；回報各自依據。
(c) `NEG-DIAG` 擴為值域軸：讀類值域軸請求串 == 正向串 → FAIL；自測補 2 正 2 反；`RT-DIAG` 依 R-DIAG20(amend) 豁免明文化。
(d) 全本複驗：lint 真違規 0；§9 17 項（母體 544）。

**T2 — 延伸審計 → `features/diagnostics/docs/extension_audit.tsv` ＋ `extension_audit.md`（摘要）**
逐列（389）比對三來源，每筆候選一行，欄：
`swe1_id`／`row`／`source`（`037-DESC`／`037-VC`／`037-VM`／`CFTS004-INFO`）／`anchor`（row 欄名 或 ObjectID）／`quoted`（原句逐字）／`asserted_by`（現有 TC ID，無則空）／`candidate`（擬拆之 sibling 一句）／`axis`（§8.3 軸名）／`feasible`（`Y`＝可執行且有值；`P`＝需佔位；`N`＝無可觀察現象）。

(a) **037-DESC**：定型句所列失效型態逐一對現有 negative TC —— 預期本項在 T1 後大多已滿足，殘餘者列出。
(b) **037-VC／VM**：逐列抽出情境詞（`boundary`／`timeout`／`varying antenna conditions`／`retry`／`all error scenarios`／`enable/disable states`／`reboot`／`power`…）對現有 TC；未斷言者列候選。VC/VM 為 037 作者文字，**只作情境來源，其值仍須 CFTS004**；VC/VM 提到而 CFTS004 無值者 `feasible = P`。
(c) **CFTS004-INFO**：對 42 個被引 L3 節之 Information 列（預期 ≤ 95 之子集）逐句：含 `shall`／列舉／值域者，若補充的是 037 已引之 FR，列候選；若為 037 未引之獨立 FR 性質，**不列**（R-DIAG8(a)），另記於 `cfts004_uncited_fr.tsv` 補充欄。
(d) **環境／持久軸**：不列候選；於 `extension_audit.md` 末段明記「CFTS004 零載，依 §8.4.1 未延伸」與 grep 條件（`reboot`／`power cycle`／`persist`／`retain`／`ignition`）之命中數。

輸出統計：候選總數、依 source／axis／feasible 分佈、涉及之 L2 組、預估新增 TC 數（Y 與 P 分計）。**前 20 條候選全文貼入上繳包**。

**T3 — 交付前置盤點更新**（承 CDD-09 §5）：候選數併入；DR-DIAG 現況；FB 清單；`framework.md` 不動。

## §3　預期數字

| 項 | 預期（分析層估）|
|---|---|
| T1(a) | 85 條；`NEG-DIAG` 0 |
| 候選（Y）| 20–60（VC/VM 情境詞為主）|
| 候選（P）| 30–80（值缺）|
| 候選（N）| 少量 |
| 合併本 | 544 列不變（本包不產 TC）|

## §4　上繳要求（`up/20260917_CDD-10.md`）

1. 首段升級條件逐字。2. T0 數字。3. T1 逐條表（tsv ＋ 前 10）；lint／自檢。
4. T2 統計表 ＋ 前 20 條候選全文 ＋ (d) 之 grep 命中數。5. T3 盤點。6. §3 對表；自報錯誤；未結；建議 commit ＋ pathspec。

## §5　升級條件（命中必明記）

- 指紋集合不符／`--check` 不綠（既存者記「既存」）。
- T1(a) 讀類值域軸 ≠ 85 ± 5（母體判定有誤，回報後再改）。
- 候選（Y）> 120（表示前七批之產出系統性不足，須回報分析層重審 sibling 判準）。
- 任何需改 canon 之情形。
