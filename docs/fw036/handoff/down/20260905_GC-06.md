# 下放包 GC-06 —— 指紋表重生後之三紅、GC-05 審閱收尾（2026-09-05）

前提（Pei 實測 2026-09-05）：`rulings_hash --check` **PASS（763 條）**；`gate_all` 三紅：`canon_refs` 788、`gates_tsv` 不符、`lint_paths` 4。
本包：三紅先量後裁；GC-05 審閱 §三／§六 之收尾。**改 gate 腳本之 code 屬 Pei 准之範圍**，本包對 `canon_refs.py` 只做任務 1 之量測與提案，改 code 待准（§三-1）。

---

## 一、分析層自報 A-GC15

「`rulings_hash` 重生後 `canon_refs` 之 `ruling` unresolved 即消」——GC-05 上繳 7-1 節所斷、GC-05 審閱 §四照抄。實測重生後 786→788，**未消**。`canon_refs` 之輸出自證：`FO … R-G 編號 50／IN … R-G 編號 0`，即其 `ruling` 型只對 **FO 之 R-G 錨點**解析，不讀台帳、不讀 `RULINGS.sha.tsv`。R-G43(b) 只補了指紋面，沒補解析面。形態：兩層轉抄一句未量測之因果（同 A-GC12）。GC-06 入 `docs/fw036/ANOMALIES.md`。

## 二、執行層任務

### 1　`canon_refs` 之 `ruling` 解析面（先量）
1-1　讀 `scripts/canon_refs.py` 建 R-G 索引之函式：列其掃描檔、錨點正則、所得 50 號之清單（含 `R-G42`、`R-G51`～`R-G68`？各是否在）。
1-2　以 unresolved 648（waiver 外）逐號統計：落在「台帳有錨、FO 無」之號（預期 `R-G12`～`R-G41` 台帳側、`R-G43`(b)(c)、`R-G44`～`R-G50`(b)(c)、`R-G69`）者幾處；其他號幾處（真懸空）。附查詢式。
1-3　waiver 456 列中，`old_id` 為 `R-G12`～`R-G29` 而其所在行已改號者：列出（改號後 waiver 鍵失效之數）。
1-4　**提案**（不改）：索引面增 `docs/fw036/RULINGS_LEDGER.md` 之 `### R-Gnn` 錨點（與 `rulings_hash` 同源，R-G43(c)）；預估解析後 unresolved 數。

### 2　`gates_tsv`
2-1　`python3 scripts/gates_tsv.py --check` 全文；`--diff`（若有）或自行對 `docs/runtime/GATES.tsv` 與現行閘逐列比對，列出不符之閘與欄。
2-2　判因：GC-04 之 `measure_common` 接線／GC-05 之改號是否動到閘之 `selfcheck` 對映或指紋。
2-3　**若僅登錄簿過時**：依 R-G56（原 R-G17，閘登錄簿與除役）重生 `GATES.tsv`，附前後 diff；**若為閘行為改變**：不改，上繳具名待裁。

### 3　`lint_paths` 4 處（皆非 GC 系列所致，先量）
| # | 紅 | 量 |
|---|---|---|
| a | `features/driver_distraction/workbook/*.xlsx` ×2 | `git log --follow --format='%h %ad %s' --date=short` 各檔；`features/driver_distraction/` 之 `feature.yaml`、`RULINGS.md` 有無宣告該落點；線之現況（最後一包日期）|
| b | `ics_management/delivered/…20260830.xlsx` sha 與對照表不符 | 對照表所在檔與其登記 sha8／日期；`git log` 該 xlsx 之 commit 序列與各版 sha8；判「檔被改」或「表登錯」 |
| c | `sw_update/delivered/…20260830.xlsx` 對照表未列 | 同上；該線 RULINGS／DECISIONS 有無交付紀錄與 sha |
**只量不動**：a 之搬移為 `git mv`（Pei）；b、c 觸及交付物與 R-TM13。

### 4　GC-05 審閱收尾
- R-G36 補充（GC-05 審閱 §三）以 `### R-G36(b) — …` 入台帳（R-G43(c) 形式）；A-GC14、A-GC15 入 `ANOMALIES.md`。
- `rg43_manual_20260905_judged.tsv` row 7–16：`judged_side` 以 `~~FO~~ KEEP_ANNOTATE` 改，reason 補「GC-05 審閱重判」。
- `--out` 乾跑證 `R-G36(b)` 入表、撞號 +0；**不寫表**（本輪已重生，下次由 Pei 再跑）。

### 5　上繳 `up/20260905_GC-06.md`：任務 1 統計＋提案、任務 2 判因、任務 3 三表、對照向、工具寫入路徑清單、自報。

## 三、Pei 待裁
1. `canon_refs.py` 索引面增台帳（任務 1-4 提案）—— 准否
2. 任務 3-a：driver_distraction 二檔 `git mv` 至 `sandbox/`，或該線宣告 `workbook/` 為 profile override
3. 任務 3-b／c 依量測結果另裁
4. `?6` 未追蹤：`down/*_review.md` ×？＋新 report——GC-06 一併 commit

## 四、未結 DR
DR-VS{n}（Power_Tailgate_Enable）未送出。
