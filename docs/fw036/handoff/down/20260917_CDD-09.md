# 下放包 CDD-09 —— Revise（R-DIAG22 長度軸／`[A-DIAG50]` PC-3）＋ batch 7（Layer 2 #2 SXM Signal Quality，72 列，最後一批）

前件：`down/20260917_CDD-08.md`、`up/20260917_CDD-08.md`、`down/20260917_CDD-08_review.md`。
依常規：審閱後即發；依據 = CDD-08 審閱 §三／§四。執行層 session：沿用 CDD 線。
**與 GC-16 之先後**：不得並行；指紋制依 GC-16 是否落地（R-G76(b)／R-G75）。上繳註明。

---

## §0　禁區

- git 一律不動；push 依 CDD-06 審閱 §五-1。
- 不動 `down/`、`up/`；不動其他 feature 目錄。
- `rulings_hash.py` 只得 `--check`／`--out <scratch>`。
- **只產 batch 7 之 72 列**；Revise 不重編號；framework 已 LOCK。
- 條文本體不改；R-DIAG22 逐字落。
- 升級條件表自 §5 逐字複製；命中者首段明記。取紅數前判 `Traceback`。

## §1　依據

`R-DIAG22` 條文逐字取自 `down/20260917_CDD-08_review.md` §四。追認：摘句寫法、`pilot01_v06` 不產、`-096`／`-098`～`-100` 不產。

## §2　任務

**T0 — 條文與指紋**：R-DIAG22 落 `RULINGS.md`；指紋依制回報。

**T1 — Revise（跨批）→ 各批新版 ＋ `merged/v07`**
(a) R-DIAG22：全合併本 grep 長度軸 negative（`row_kind = negative` 且 ER 含 `7F .. 13`）之 `$` 請求串，與同 DID／RID 正向 TC 之 `$` 串比對；相同者改為省末 byte，描述行綴 `with the last byte omitted`。逐條表：TC ID／原串／改後串。預估 ≈ 130 條。值域軸（`7F .. 31`）不動，但複核其資料位元組為 `<…>` 佔位或 CFTS004 明載越界值，不得與正向相同。
(b) `NEG-DIAG` lint 實作 ＋ 5 正 5 反自測；全本複驗 0。
(c) `[A-DIAG50]`：batch 5 44 條 SXM 讀類補 PC-3；grep 合併本所有 `$2843`～`$2848` 讀類 TC，PC-3 一致；`pc_sources` 記各列錨。
(d) 產生器守門第五條：長度軸 negative 之請求串 == 同 DID 正向串 → 不出檔。

**T2 — batch 7 sibling plan（先出後產）**

| Layer 2 | DID | 037 列 | 分析層預判（執行層查證為準）|
|---|---|---:|---|
| #2 SXM Signal Quality | `$2840` Antenna Status（10）、`$2841` Signal Quality（35，含 pilot `-189` → `-012`）、`$2842` Overlay Signal Quality（27）| 72 | 三 DID 之子欄位（antenna status／BER／C/N／OBER channel fields／TWER pipe／RS Errs Words／Terr／Sat Symbol／ENS lock 等）各為一列：每列只追一子欄位；positive 讀 1、neg 2（長度軸依 R-DIAG22）、uns 1。CDD-01 §3 VM 見「under varying antenna conditions … RF reception states」—— **天線狀態軸只在 CFTS004 明載狀態列舉時拆**（如 antenna connected／shorted／open），否則單條。`-189`～`-192` VM blank 依 R-DIAG6 Remarks |

Region NAFTA → `1 1 1 0 0 0 0`；Harman 依 tsv；PC-3 chipset 依錨全文。

**T3 — batch 7 產出** → `sandbox/batch7/diagnostics_batch7_v01.xlsx`／`batch7.json`／`coverage.tsv`；ID 自 `-433` 起；pilot `-012` 沿用。

**T4 — lint／自檢／coverage／合併本**：九本各跑；§9 17 項；R-SEC15 型；coverage 72/72；`merged/diagnostics_v07.xlsx`；`placeholder_summary.tsv` 更新；**全 feature coverage 對帳：389 列 = 產出 ＋ OUT_OF_SCOPE 1**，UNCOVERED 0。

**T5 — 交付前盤點（供 CDD-10）**：Layer 2 20 組各列數／TC 數表；PENDING 總數與位置；佔位 token 分佈；DR-DIAG-1～8 現況；FB-a～r 清單；lint 全項終值；`framework.md` 與 `layer2_assign.tsv` 對帳。

## §3　預期數字

| 項 | 預期 |
|---|---|
| T1(a) 命中 | ≈ 120–140 條（長度軸 negative 全數）|
| batch 7 TC | 72 列 → 90–105 |
| PENDING | 0（DR-DIAG-1 餘 2 列不在 #2）|
| lint | 真違規 0；`NEG-DIAG` 0 |
| 合併本 | ≈ 525–540；**20/20 組全產，388/389 列 ＋ 1 OOS = 100%** |

## §4　上繳要求（`up/20260917_CDD-09.md`）

1. 首段升級條件逐字。2. T0 數字。3. T1(a) 逐條表（可以 tsv 附檔，上繳只列統計 ＋ 前 10 條）；`NEG-DIAG` 自測；(c) 44 條清單；守門第五條。
4. T2 sibling plan（含天線狀態軸之 CFTS004 查證）。5. TC 清單；貼入 **一條 Revise 後之長度軸 negative（如 `-002`）** ＋ **`$2840` 任一條** 全文。
6. T4 紅數歸因；自檢；coverage 389 對帳；T5 盤點表；§3 對表；自報錯誤；未結；建議 commit ＋ pathspec。

## §5　升級條件（命中必明記）

- 指紋集合不符／`--check` 不綠（既存者記「既存」）。
- T1(a) 命中 > 160 條（表示值域軸亦有與正向同串者，須擴大並回報）。
- 389 對帳有 UNCOVERED。
- 天線狀態軸拆分後任一 SWE1 列 > 6 TC。
- 任何需改 canon 之情形。
