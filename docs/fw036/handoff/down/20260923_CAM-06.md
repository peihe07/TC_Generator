# 下放包 CAM-06 —— batch01 修正 ＋ framework 重開（Auxiliary Cameras）＋ batch02

指派執行層 session：延用。對應審閱：`down/20260916_CAM-05_review.md`；Pei 2026-09-23 對審閱 §三之答覆：
1「應拆解」、2「637／376 可併」、3 是、4 好、5「-017 所引皆拆」，並確認新 Test Set 名 `Auxiliary Cameras`。

---

## §0　禁區

同 CAM-03 §0。本包觸及：`batch01/` 原地修正、`batch01b/`（-016 之 12 列補生成）、`batch02/` 新建、`features/camera/framework.md` 重開一次。不寫回工作簿。

## §1　裁定落檔

取號現查：`{live}` 期望值 **13**。

```text
R-CAM13  037 所引之來源一律拆解；§8.4.2 只擋「RD 未引」之外部規格
  (a) SWE 列所引之每一 SYS-RA 來源（排除 Out of Scope 與 R-CAM10 委派者）皆須落入至少一個 TC 之
      驗證點；TC 作者不得以 Test Group／節別不符為由略過（§8.2 RD 為需求單位之權威）。
  (b) §8.4.2「不測外部規格」僅適用於 037 未引、只出現在被參考文件中之行為。
  (c) 節別與 Test Group 不符者（Cargo/CHMSL、SVC、FFC 節條文）落 Test Set `Auxiliary Cameras`，
      Pre-Condition 以 PROXI 表達該相機之配備（`Digital_CHMSL_Camera_Prsnt = 1`／
      `Surround_View_Camera = 1`／`Forward_Facing_Camera = 1`），reasoning 註明 Atl-Hi default 為 0。
  (d) VC／VM 欄不是來源；其內容對應得到所引來源者一併生成，對應不到者登 RD_FEEDBACK。
  審閱 §三-1「不生成」與 CAM-05 §3 之 A-CA28 處置作廢。
```

```text
R-CAM14  速度門檻之母錨 = CFTS092-4781643
  Rear View Camera 之速度退出門檻（8 mph）以 `CFTS092-4781643`（SYS-RA-CAM-078）為錨，
  五車型以 R-CAM3(e) CAN source 行分寫訊息名（`BRAKE_FD_2` Atl-Hi／`BRAKE1` 376／
  `STATUS_CCAN3` 637, 2261），VF 條文留 reasoning 互參。
  2261 因 VF551_V33 為 `>(greater)` 而條文相異，維持分列（`-030`／`-031`）。
  `>` 與 `>=` 於 CAN 解析度不可判（A-CA30）之根本解列 RD_FEEDBACK（標定單位）。
```

**framework 重開（Tier 2，Pei 2026-09-23）**：`features/camera/framework.md` A 本 Layer 2 加第 10 組
`Auxiliary Cameras`（Layer 3 = CFTS092 §1.3.5 Cargo/CHMSL、§1.3.7 Surround View、§1.3.8 Forward Facing、§1.3.9 Clearpath），簽核狀態行加一行重開紀錄與新 sha；A 本 9 → 10 組。
`-017`（FFC 節 `CameraDisplaySts`）自 `State Handling` 改歸 `Auxiliary Cameras`；`layer2_assign.tsv` 同步。

**selfcheck**：`selfcheck_r_cam10.py` 更名 `selfcheck_camera.py`，含四項：R-CAM10 錨反查、proc ≥ 2 步、
「Full-Operation 前提 ＋ 步 1 `CmdIgnSts = RUN`」重複、verbatim 保序子序列。

**RD_FEEDBACK.md** 新建（`features/camera/`）：RDF-01 `-016` 混引四節、RDF-02 `-017` 描述／VC 錯位、
RDF-03 `c_VEHSPD_MAX`／`MAX_SPEED` 標定單位、RDF-04 `-018` 之 `e.g.` 措辭、RDF-05 `VF617V5` 拼寫。
供 Pei 回 RD 作者；TC 側不承接。

## §2　batch01 修正（原地）

1. 審閱 §二四項：`-023` 刪步 1；`-021` 加 Delay On 前提；`-026` ER2 改可判；`-030`／`-031` 加 `LTM_OperationalModeSts.Info = "Ignition_On_EngOn"`。
2. 全批以 selfcheck 第三項掃描「RUN 重複」，命中者一律刪步 1，回報清單。
3. `-009`／`-010` 依 R-CAM14 改錨 `CFTS092-4781643`，CAN source 行加 `BRAKE1.VehicleSpeedVSOSig (376) / STATUS_CCAN3.VehicleSpeedVSOSig (637, 2261)`，Vehicle Model 376＝1、637＝1（2261 維持 0，由 -030/-031 承）。
4. `-016` 之 12 列（A-CA28）→ `batch01b/`：先出 `batch01b_plan.tsv`（每列 → 驗證點 → PROXI 配備），生成 ID 續 `-034` 起，Test Set `Auxiliary Cameras`。PROXI 值自 `forms/proxi/` 各平台實測；Atl-Mi 三本無 `Digital_CHMSL`／`Forward_Facing` 參數者，該相機於該平台勾 0 並 reasoning 註。
5. batch01 ＋ 01b 重跑 lint（含 Z）＋ selfcheck 全綠。

## §3　batch02：Startup and Shutdown ＋ Configuration ＋ State Handling ＋ `-017`，上限 40

SWE：`-001`／`-012`／`-014`（Startup and Shutdown）、`-002`／`-005`（Configuration）、`-003`（State Handling）、`-017`（Auxiliary Cameras）。

**步驟 A — 選列計畫** `batch02_plan.tsv`，格式同 batch01；依 R-CAM13 逐來源落驗證點，被排除者只限 Out of Scope／R-CAM10 委派／VF617 查無，逐列記。
分析層預期（執行層以來源核實）：
- `-001`：pilot 001／002 已涵蓋開機預設關閉與 teardown；其餘 V33/V42 之開機序列（LVDS 握手不計，歸 `-007`）。
- `-012`／`-014`：`PowerShutDownNotifcation.Power_Down = True` 之送出時機與 Tpower 保持（V2 §1.14.1）；兩列同源者依 R-CAM10 由 `-012` 承接，`-014` 只承接 RVCM Power Notification Control 獨有者；LVDS 訊息之觀察用 §5.4 兩行式（bus analyzer／`adb` log），命令自 SYSAD 或 SWC 交付本之 `$` 例抄。
- `-002`：`PROXI Rear_View_Camera = 0 (Absent)` → 功能不啟用（negative，全五）；`= 1` → 啟用；`Rear_View_Camera_Type`（Atl-Hi only）Digital／Analog 分列；PROXI 讀取時機（IGN ON）。
- `-005`：EVS HAL 支援之格式／解析度（V2 §1.13.1 與 `Radio_Display_Type`）；供試值只取 SYS2 原文。
- `-003`：一般狀態機（Automatic／Manual／Off）之轉換，與 batch01 重疊者依 R-CAM10 委派，只取 `-003` 獨有錨（含 pilot -006 已改錨之 V2-549 系）。
- `-017`：`CameraDisplaySts` 訊號之送出（CFTS092 CAM-097／098，FFC 節）；VC/VM 之 Air Suspension／4X→1X 若對應到所引來源則生成，否則 RDF-02。

**步驟 B — 生成** 自 batch01b 之末號續編；全套規則同前；lint ＋ selfcheck 全綠。

## §4　上繳要求（`up/20260923_CAM-06.md`）

1. R-CAM13／14 sha8；framework 重開紀錄與新 sha；`RD_FEEDBACK.md` 五條；selfcheck 四項之實作與對 pilot02／batch01 之回歸結果。
2. batch01 修正之欄級 diff；RUN 重複掃描命中清單；`-009`／`-010` 改錨後之 Vehicle Model。
3. `batch01b_plan.tsv` ＋ 產物；PROXI 配備值出處（平台／列號）。
4. `batch02_plan.tsv` ＋ 每 SWE TC 數；排除清單；產物；PENDING 列表。
5. 自報錯誤；未結。

## §5　升級條件

- batch01b ＋ batch02 合計 > 40（停於步驟 A）。
- `-012`／`-014` 之 LVDS 觀察無可抄之命令句式（SWC 交付本與 SYSAD 皆無）→ 回報，該 ER 以 bus analyzer 之訊息名／值書寫，不造命令。
- `-005` 之格式／解析度值於 SYS2 原文缺（只在 037 摘要）→ PENDING，不採 037 值。
- Atl-Mi PROXI 表對 Cargo／FFC 相機無參數且 SYS2 亦無配備條件 → 該列只勾 Atl-Hi，回報。
