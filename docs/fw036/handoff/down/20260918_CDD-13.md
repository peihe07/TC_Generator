# 下放包 CDD-13 —— `REF/` 之 CS 系列規範讀取與適用（DR-DIAG-1／4／6 之 STLA 層來源）

前件：`down/20260917_CDD-12_review.md`。Pei 2026-09-18 問「REF 裡的 CS 會有幫助嗎」；
分析層開檔實測（§1）—— **會，且大**。CDD-01 T0 對 REF 只做 sha 登錄，**從未開讀 CS 系列**，
致 DR-DIAG-1／4／6 與 R-DIAG13 之依據一直停在 CFTS004＋SYSAD（分析層 **A41**）。

---

## §0　禁區

- git 不動；`forms/`、`spec-index/` 依既有登錄政策；`--gate`／`--regen` 不跑。
- **範圍不變（R-DIAG8）**：CS 系列是**值與規則之來源**（同 SYSAD 結 DR-DIAG-3 之先例），
  不是新的需求列；不得因 CS 條文新增 037 未含之驗證對象。新 sibling 只得掛在既有 037 列且 037 VC/VM 或 CFTS004 同節點有可對之敘述。
- 只用 xlsx 匯出本中**逐字可見之文字**；Annex A NRC 流程圖、CS.00102 之 DID/RID 範圍表皆為圖片（匯出本未帶），**不得憑 ISO 常識補其內容**。
- 升級條件表自 §5 逐字複製；命中者首段明記。

## §1　分析層已測（執行層逐項複驗，數字以實測為準）

`/Users/peihe/Work/02_Project_R1LR/10_Reviewing/00_TestCase/ASW-R3/CDD/REF/` 之 CS／SD 系列 9 件：

| 檔 | 主題（自 1.1 Purpose） | 對本 feature |
|---|---|---|
| `SYS2_CS.00099_…` | **Diagnostic Requirement Standard – UDS**（STLA 診斷總則），Analysis Report 808 列，Cat：FR 59／Info 84／OOS 18 | **高** —— §5.5.6.2 S3ECU = 5000 ms 回 default；§5.10.2.1-4 破壞性資料 → NRC request out of range；§5.10.2.3-2 I/O 控制權回收之 6 條件（Return Control／session 轉換／ECU reset／vehicle moving／S3 timeout／internal disable）；§5.10.2.3-5/-6 未配置 I/O 之負回應；§6.1 同步/非同步常式、6.1-3 > 2 s 必非同步、6.1-4 非同步中之 NRC 依 CS.00100；§6.2-8 不需車速之常式於車速 > 0 拒絕；§9.3.3 安全序列 `10 → 27 → 2E/31 etc.`、9.3.3-3 兩次失敗鎖 10 s、9.3.3-6/-7 key 逾 20 s → NRC 0x24 |
| `SYS2_CS.00100_…_146_150_RAR` | **Diagnostic Services（UDS on CAN, STLA 實作）**，351 列 | **最高** —— Table 2 服務適用矩陣（22/2E/2F 為 M、31/27 為 C1）；§5.1.5-4 I/O 中 session 中斷 → ECU 自動收回控制；Table 53 RoutineControl 子功能 01/02/03（證 R-DIAG20）；**Table 51 IOCP**：`00` Return Control to ECU、`01` Reset to Default、`02` Freeze Current State、`03` Short Term Adjustment，NOTE `2F AA AA 03 XX`，`XX` 於 03 時必帶；Control Enable Mask 只用於 packeted DID；NRC：`Parameters Definition` 列明 NRC 處理依 ISO 14229-1:2020 §7.5、`$78` 不可中斷／`$21` 可中斷／30 s 後 `$10`；各服務之 NRC 表一律「Refer to Annex A」，**Annex A 為圖（image9～18），匯出本 media 僅 1 件 → 缺原件** |
| `SYS2_CS00102_SWAD…` | **Standardized Diagnostic Data**（實為 SDD，非 SWAD）：§5.2 標準 DID（$F18x 等）、§5.4 DID/RID 範圍與支援條件（**表皆為圖 image259～274**） | 中 —— 037 未引標準 DID（R-DIAG8 不擴）；範圍表若到原件可校 037 之 DID 落區 |
| `SYS2_CS.000148_…` | **EOL (PROXI) Programming**，131 列 | 低（本 feature 無 PROXI 列） |
| `SYS2_SD.00049_…` | **Diagnostics Access Firewall 角色權限**，12 列；§3 明言權限矩陣在**內嵌 xlsx `Diagnostic Services Secure Access Rights 28JAN2026.xlsx`**（匯出本未帶） | 中 —— 即 FB-DIAG-k／R-DIAG13 之答案所在，但要附件 |
| `SYS2_CS.00101_…` | 5 列，LTM 之 $F180/$F181 | 無 |
| `SYS2_CS00025_…` | CDD 檔開發與安全演算法治理，12 列 | 無 |
| `CS00042_…` | Ethernet 診斷 | 無（R1L 無 Ethernet 診斷，CS.00099 §5.4.2 亦 OOS）|
| `SYS2_CS.00010_03_…` | Diagnostic Design Review 流程，18 列 | 無 |

**CFTS004 是否引用 CS.00099／CS.00100**：分析層未查；執行層 T1 必查（grep `CS.00099`／`CS.00100`／`CS.00102`／`diagnostic protocol definition`）。
即使 CFTS004 未明引，CS.00100 Table 2 為 STLA 全 ECU 之 UDS 實作規範，作為值來源之位階同 SYSAD（R-DIAG3 先例）。

## §2　裁定（依常規落字；Pei 可撤）

```text
R-DIAG29  CS.00100 Table 51 為 I/O Control 之 IOCP 位元組來源
  (a) 0x2F 請求一律書 `2F <DID> <IOCP> [<controlState>]`，IOCP 逐字取 Table 51：
      00 Return Control to ECU／01 Reset to Default／02 Freeze Current State／03 Short Term Adjustment。
      CDD-04 之「ISO IOCP 通用值禁用」係因無 STLA 來源；今 CS.00100 為 STLA 來源，該禁令解除。
  (b) `<controlState>`（Table 51 NOTE 之 `XX`）仍為佔位 —— DR-DIAG-4 題目縮為「各 I/O DID 之 controlState 編碼」，
      不再含 IOCP 本身；`<option-name selection>` 佔位改寫為 `2F <DID> 03 <controlState for "<option-name>">`。
  (c) 037 列之 VC/VM 或 CFTS004 同節點述及「return control」「stop」「reset」者，得依 (a) 產 `00` sibling；
      述及 session 結束／逾時者，得依 CS.00099 §5.10.2.3-2 ＋ CS.00100 §5.1.5-4 產「S3 timeout → 控制權回收」sibling，
      timeout 值 5000 ms 取 CS.00099 Table 8。無此敘述者不產（§0 範圍）。
  (d) Control Enable Mask 只於 packeted DID（CS.00100 §5.6.1.2.1.3-1）；單參數 DID 之 TC 不得帶 mask。

R-DIAG30  Session 與安全序列之來源升級
  (a) `Security access 0x27 has been granted` 之 PC 來源由 SYSAD §4.5 改為併引 CS.00099 §9.3.3（序列 `10 → 27 → 2E/31`）；
      是否含 0x2F 仍**不定** —— SD.00049 附件到件前維持 R-DIAG13 保守；DR-DIAG-10 登記該附件。
  (b) 037 有 session 軸敘述之列（FB-DIAG-m 之 `-048` 類），S3ECU 5000 ms 回 default 取 CS.00099 §5.5.6.2 為來源，
      可產「非 default session 閒置 5000 ms → 回 default session」sibling；ER 之 NRC 值若需 Annex A 者仍佔位。
  (c) 常式類：不需車速之常式於車速 > 0 拒絕（CS.00099 §6.2-8）—— 只對 037 VC 或 CFTS004 同節點述及車速者產 sibling。

R-DIAG5(amend3)  NRC 來源鏈補一級
  CFTS004 → 037 → **CS.00100 Annex A（原件到件後）** → ISO 14229-1 五碼閉集。
  現在 DR-DIAG-1 之 3 列：`-237` 之「appropriate NRC」在 Annex A 到件前**維持 PENDING**；`-156`/`-157` 同。
  已依五碼閉集寫出之 223 列**不回修**，Annex A 到件後對照，衝突者才 Revise。
```

新 DR：
- **DR-DIAG-9**：`CS.00100` 原件（docx/pdf，含 Annex A Figure A-1～A-14 NRC 流程圖）。來源：`1_Customer_Requirement`（CFTS004 原件先例）。
- **DR-DIAG-10**：`SD.00049` 附件 `Diagnostic Services Secure Access Rights 28JAN2026.xlsx`。
- **DR-DIAG-11**：`CS.00102` 原件（§5.2 標準 DID 表、§5.4 DID/RID 範圍表為圖）—— LOW，供 DID 落區校核。
- DR-DIAG-4 改題（R-DIAG29(b)）；DR-DIAG-6「不支援之 SID」—— CS.00100 Table 2 列 `U`（unsupported by default）之服務（`$23`／`$2A`／`$2C`／`$3D`／`$35`／`$87`）為候選，**但 `U` 是「可不支援」非「本 ECU 不支援」**，仍待 RD 確認；先在 asset_request 註明候選。

## §3　任務

**T0**：9 件 REF 之 MANIFEST 狀態盤點（已登者列 doc_id，未登者登錄，sha 覆核）；條文落檔；指紋依制；DR 表更新。
**T1**：CFTS004 對 CS.00099／00100／00102 之引用 grep（三本：index xlsx、原 docx、SYSAD）；結果表。
**T2**：CS.00099／CS.00100 適用條文逐字抽出 `data/cs_uds_rules.md`（只抽 §1 表所列條號，附 Sys-RA ID），圖片處標 `(image)` 不補述。
**T3 — I/O 母節 Revise（R-DIAG29）**：114 列所屬全部 0x2F TC：請求改 `2F <DID> 03 <controlState for "…">`；`<option-name selection>` 佔位全換；placeholder_summary 之 DR-DIAG-4 行**數目不變、題目改**；(c) 之 sibling 先出 plan 再產，每列 ≤ 8。
**T4 — R-DIAG30 sibling plan**：先出 plan（列、依據、預估數）**不產**；量 > 40 條時停在 plan 待審。
**T5**：merged v10、交付本 v03、lint／§9／對帳；`asset_request.md` 增 DR-9/10/11。

## §4　預期

| 項 | 預期 |
|---|---|
| T1 引用命中 | 未知（分析層未查）|
| T3 改寫 TC | ≈ 130（I/O 母節既有 TC 全部）|
| T3(c)／T4 新 sibling | plan 先出；T3(c) 若 ≤ 40 可直接產 |
| 合併本 | 558 → 558 + T3(c) |
| lint | 真違規 0；`NEG-DIAG`／`RT-DIAG` 不受影響 |

## §5　升級條件

- 指紋／`--check`（既存者記「既存」）。
- CFTS004 之 IOCP 或 controlOptionRecord 敘述與 CS.00100 Table 51 **矛盾** —— 回報不改，Pei 裁。
- T3(c) 或 T4 之新 sibling > 40 → 只出 plan。
- 任一 037 列 > 8 TC。
- CS.00100 匯出本 Table 51 以外另載 controlState 值（分析層未見）→ 回報，可能直接解 DR-DIAG-4 一部分。

## §6　上繳

首段升級條件逐字；T0 盤點表；T1 引用表；T2 檔摘要；T3 逐條 tsv ＋ 貼入 **一條 I/O 正向（改寫後）** ＋ **一條 `00` Return Control sibling（若產）** 全文；T4 plan；T5 數字與路徑；§4 對表；自報錯誤；未結；建議 commit ＋ pathspec。
