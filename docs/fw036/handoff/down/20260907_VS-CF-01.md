# 下放包 VS-CF-01 —— CFTS044 工作簿 PENDING 甲類 13 列結案、DR-18 錯號更正、R-VS102 落檔（2026-09-07）

**指派：執行層**。Pei「是準」（2026-09-07）：准分析層審閱所列甲類 13 列之處置。
本包**只動下列列之指定欄位**，不動其他列；不跑會改 repo 之產生器；xlsx 寫回一律 `surgical_save`（直接 openpyxl save 會毀 x14 data validation）。

## 〇、對象簿與其來源（先量測，不符即停手）

| 項 | 值 |
|---|---|
| 母本 | `/Users/peihe/Work/02_Project_R1LR/10_Reviewing/00_TestCase/ASW-R2/Vehicle Settings/CFTS044/FM-WI-FSM-036-A01 STLA 測試用例規範與結果_SWQT STLA Test Case Specification & Result_SWQT_CFTS044_Vehicle Controls_20260819.xlsx` |
| 母本 sha16 | `f39c03bd9cb0f1b3`（121,238 bytes；分頁 `Test Case Specification 測試用例規範` row 10–252，243 資料列） |
| ⚠ | `features/vehicle_setting/inputs/` 之同名檔 sha16 `ebe5a65f30a0d4bc`（108,225 bytes，237 列）**是 intake 骨架，不是本簿**；repo 內以 `**/*CFTS044*Vehicle Controls*.xlsx` 排除 `inputs/` 搜尋命中 **0**。母本目前只在 `10_Reviewing/`。 |
| 處置 | 先 `cp` 母本入 `features/vehicle_setting/sandbox/cfts044/cfts044_20260819.xlsx`，`shasum -a 256` 須 = `f39c03bd…`；寫回產出 `cfts044_20260819_Revise1.xlsx`（R-G72，母本不改）；二者登 `features/vehicle_setting/output/MANIFEST.tsv`。 |
| DBC | 本線綁 `forms/PDT27_E2A_R1_BHCAN2.dbc`（sha16 `46cb73f3db62ac9f`，R-VS101）。本包所引 7 支訊號（`FL/FR_HS_STATSts`、`FL/FR_VS_STATSts`、`HSW_StatSts`、`EngineSts`、`CmdIgnSts`）之 bit 寬與 `VAL_` 於 R1_BHCAN2 與 `inputs/PDT27_E2A_R4_BHCAN.dbc`（`9ef1ec98…`）**逐字相同**，執行層複驗後入上繳。 |
| PROXI | `forms/PROXI_HDCC27_R3_20250424.xlsx`（sha16 `e7c2020f01c3d58d`），分頁 `Format` |

列號皆指母本 Excel 列號；F 欄 tc_id 併列以防位移。

## 一、R-VS102 落檔（`features/vehicle_setting/RULINGS.md` 末尾追加）

取號依 R-G62 live：本檔 tail 實測最大號 `R-VS101`（2026-09-07），故本條 `R-VS102`。**執行層落檔前重查一次最大號**；已被占用則順延並具名。

```text
R-VS102   CFTS044 「宣告有效集之外」型 TC 之處置（Pei 准，2026-09-07）
(1) 訊號之 DBC 值域含宣告有效集之外之已定義 raw 者，以該 raw 為無效值，
    ER 依既有「invalid value is ignored」範式（顯示不變）。
    兩態座椅（Off/Low/High）對 2 bit *_HS_STATSts：無效 raw = 2 (Heated_seat_medium)。
(2) 訊號之 DBC 值域全數落在宣告有效集之內者（2 bit 全定義、1 bit 全定義），
    匯流排上不存在可送之無效 raw —— 不生成並具名 `NOT GENERATED: B7-no-invalid-raw`，
    向上游開型 A 確認型 DR（草稿見 §四，未取號）。上游覆文不能變出第五值，故不掛 PENDING。
(3) `$EngRun_Stat$` 非運轉態（<> [IDLE_STBL//UNLIMITED//LIMITED//RUN]）
    以 `STATUS_CCAN3.EngineSts = 0 (Engine_Off)` 對照，運轉態以 `= 2 (Engine_On)`，
    與 row 10–13 既有寫法一致；全部標 `dr_dependent = DR-19`，覆後複檢。
(4) `$PowerMode$ = [IGN_START]` 對照 `STATUS_BH_BCM2.CmdIgnSts = 5 (START)`
    （同 `[Ignition run] → 4 (RUN)` 之既有前例）；標 `dr_dependent = DR-21`。
    `[IGN_OFF_ACC]` 無 DBC 對應，維持 PENDING，不得以 `1 (IGN_LK)` 代位。
(5) 別名認定（R-13）：`VC_HdRstPrsnt` ≡ PROXI `Headrest_Dump_Present`（Format 列 780）；
    `DSP_SK_PRSNT` ≡ PROXI `Display_OFF_SoftKey`（Format 列 692）。二者值域 0 = Absent／1 = Present。
    ⚠ 二者於 HDCC27 該表標 Not Used／Set to 0；R1L 之適用性於 Remarks 具名，不阻塞。
(6) 工作簿掛 `BLOCKED: DR-18 —— 無效碼之定義待覆` 之 8 列為錯號：台帳 DR-18 之內容為前綴筆誤／
    大小寫確認，全庫無任何 DR 在問無效碼。該 8 列依 (1)(2) 改標，不再引 DR-18。
```

## 二、13 列逐列改寫（只動所列欄位；其餘欄位逐字不動）

### 2.1 R-VS102(1) —— 兩態，補無效 raw（2 列）

| 列 | tc_id | 欄 | 改為 |
|---|---|---|---|
| 63 | NR1L-VehicleSetting-054 | L step 2 | `2. Send CAN: STATUS_CSWM.FL_HS_STATSts = 2 (Heated_seat_medium)` |
| 63 | | M step 2 | `2. STATUS_CSWM.FL_HS_STATSts = 2 (Heated_seat_medium) is sent` |
| 63 | | AH | `R-VS102(1): two-state vehicle (Off/Low/High); raw 2 lies outside the declared valid set` |
| 79 | NR1L-VehicleSetting-070 | L step 2 | `2. Send CAN: STATUS_CSWM.FR_HS_STATSts = 2 (Heated_seat_medium)` |
| 79 | | M step 2 | `2. STATUS_CSWM.FR_HS_STATSts = 2 (Heated_seat_medium) is sent` |
| 79 | | AH | 同 row 63 |

### 2.2 R-VS102(2) —— 三態／1 bit 全定義，不生成並具名（5 列）

row **64**（-055）、**80**（-071）、**151**（-142）、**166**（-157）、**225**（-216）：
I／J／K／L／M／P／R 欄**清空**（與既有 NOT GENERATED 列同形，如 row 56），N 欄保留，
AH 改為：

```
NOT GENERATED: B7-no-invalid-raw —— <signal> 為 <n> bit，raw 全數已定義且皆在宣告有效集內，匯流排上無可送之無效值；R-VS102(2)；DR 草稿見 DATA_REQUESTS.md（未取號）
```

`<signal>`／`<n>` 逐列：64 `STATUS_CSWM.FL_HS_STATSts` 2；80 `STATUS_CSWM.FR_HS_STATSts` 2；
151 `STATUS_CSWM.FL_VS_STATSts` 2；166 `STATUS_CSWM.FR_VS_STATSts` 2；225 `STATUS_CSWM.HSW_StatSts` 1。
（row 225 原 L 欄 `HSW_STATSts` 之大小寫問題隨清空消滅，上繳仍記一筆。）

### 2.3 row 62（-053）—— 對象改為 PROXI `Heated_Seat_Levels`

條文主體為 `$Heated_Seat_Levels$` = [1]/[2]/[3]，原 TC 送 `FL_HS_STATSts` 係驗錯對象。
PROXI Format 列 760：`Heated_Seat_Levels` 2 bit（bit 6–7），`0 = 1 Level／1 = 2 Levels／2 = 3 Levels`，raw 3 未定義。

| 欄 | 改為 |
|---|---|
| J | `1. The vehicle is equipped with the heated seat`⏎`2. The Heated / Vented Seats screen is displayed` |
| K | `NA` |
| L | `1. PROXI Heated_Seat_Levels = 2 (3 Levels)`⏎`2. Power cycle the HU and record the number of heated seat levels shown as Levels_valid`⏎`3. PROXI Heated_Seat_Levels = 3`⏎`4. Power cycle the HU and check that the number of heated seat levels shown is unchanged from Levels_valid` |
| M | `1. PROXI Heated_Seat_Levels = 2 (3 Levels) is accepted`⏎`2. Levels_valid is recorded`⏎`3. PROXI Heated_Seat_Levels = 3 is accepted`⏎`4. The number of heated seat levels shown is unchanged from Levels_valid` |
| R | `負向測試 (Negative / Invalid)` |
| AH | `R-VS102(1)：PROXI Heated_Seat_Levels raw 3 未定義（Format 列 760）；ER 依 invalid-ignored 範式；上游若覆無效 PROXI 之行為另有規定則複檢` |

step 3 之值無 label（未定義 raw，依 §8.7.5 不得造 label）。

### 2.4 R-VS102(3) —— DR-19 對照（2 列＋4 列 Remarks）

| 列 | tc_id | 欄 | 改為 |
|---|---|---|---|
| 14 | -005 | L step 3 | `3. Send CAN: STATUS_CCAN3.EngineSts = 0 (Engine_Off)` |
| 14 | | M step 3 | `3. STATUS_CCAN3.EngineSts = 0 (Engine_Off) is sent` |
| 15 | -006 | L step 2 | `2. Send CAN: STATUS_CCAN3.EngineSts = 0 (Engine_Off)` |
| 15 | | M step 2 | `2. STATUS_CCAN3.EngineSts = 0 (Engine_Off) is sent` |
| 14, 15 | | AH | `dr_dependent = DR-19（R-VS102(3)）：$EngRun_Stat$ 非運轉態以 EngineSts = 0 對照，覆後複檢` |
| 10, 11, 12, 13 | -001～-004 | AH（原空） | `dr_dependent = DR-19（R-VS102(3)）：$EngRun_Stat$ 以 EngineSts 對照，覆後複檢` |

row 14/15 原 step 內之繁中說明句隨改寫消滅。row 16／68／84／155／170 之 `BLOCKED: DR-19` 前綴改 `dr_dependent = DR-19`，餘文不動。

### 2.5 R-VS102(4) —— DR-21（2 列）

| 列 | tc_id | 欄 | 改為 |
|---|---|---|---|
| 26 | -017 | M step 2 | `2. STATUS_BH_BCM2.CmdIgnSts = 5 (START) is sent` |
| 26 | | AH | `dr_dependent = DR-21（R-VS102(4)）：IGN_START 對照 5 (START)，覆後複檢` |
| 28 | -019 | L step 2 | `2. PENDING: DR-21 IGN_OFF_ACC bus value` |
| 28 | | M step 2 | 不動（已為 `PENDING: DR-21`） |
| 28 | | AH | `BLOCKED: DR-21 —— IGN_OFF_ACC 於 DBC 無對應；DR-21 逐實例條文尚未送出（台帳）` |

row 28 原 step 2 之 `1 (IGN_LK)` 為推測，本包撤下。

### 2.6 R-VS102(5) —— DR-22 別名（2 列）

| 列 | tc_id | 欄 | 改為 |
|---|---|---|---|
| 25 | -016 | M step 2 | `2. PROXI VC_HdRstPrsnt = 1 (Present) is accepted` |
| 25 | | AH | `alias（R-13／R-VS102(5)）：VC_HdRstPrsnt ≡ Headrest_Dump_Present（PROXI_HDCC27_R3 Format 列 780，0 Absent／1 Present；HDCC27 標 Not Used）` |
| 44 | -035 | M step 2 | `2. PROXI DSP_SK_PRSNT = 1 (Present) is accepted` |
| 44 | | AH | `alias（R-13／R-VS102(5)）：DSP_SK_PRSNT ≡ Display_OFF_SoftKey（Format 列 692，0 Absent／1 Present；HDCC27 為 Set to 0）` |

別名同時登入本線之別名表（`down/20260902_VS-SL-02_alias_candidates.tsv` 所屬之正式表；執行層先 `grep -rl "alias" features/vehicle_setting/data/` 定位，找不到即報，不新建）。

### 2.7 順手（僅限本包觸及之列）

本包觸及之列其 M 欄若含全形 `；`，一併改半角 `;`（row 25、44、62、63、79）。**其餘 36 列之全形分號不在本包**，留 F3 另包。

## 三、DR-18 錯號更正（`DATA_REQUESTS.md`）

`## DR-18` 節末追加：

```
> **錯號更正（2026-09-07，VS-CF-01）**：CFTS044 工作簿 8 列（row 62／63／64／79／80／151／166／225）
> 以 `BLOCKED: DR-18 —— 無效碼之定義待覆` 掛本 DR，惟本 DR 從未詢問無效碼。
> 該 8 列已依 R-VS102(1)(2) 改標，不再引本 DR。本 DR 之內容與狀態不變。
```

## 四、DR 草稿（未取號，追加於 `DATA_REQUESTS.md` 末）

```
## DR 草稿 —— 「All other states shall be considered invalid」於全定義訊號上無可送之無效值

**未取號**（未送出前不佔號）。型 A — 規格缺陷（確認型，不阻塞）。成對：R-VS102(2)。

CFTS044 之下列條文以「Valid values … All other states shall be considered invalid by the HU」
定義有效集，惟其對應訊號於基線 DBC（PDT27_E2A_R1_BHCAN2／R4_BHCAN 相同）之 raw 全數已定義且皆在有效集內：

| 條文 | 訊號 | bit | VAL_ |
|---|---|---|---|
| 4858317（LeftFrontHeatedSeat-006，三態） | STATUS_CSWM.FL_HS_STATSts | 2 | 0–3 = off/low/medium/high |
| （RightFrontHeatedSeat-024，三態） | STATUS_CSWM.FR_HS_STATSts | 2 | 同上 |
| （LeftFrontVentedSeat-005，三態） | STATUS_CSWM.FL_VS_STATSts | 2 | 0–3 |
| （RightFrontVentedSeat-022，三態） | STATUS_CSWM.FR_VS_STATSts | 2 | 0–3 |
| 4858516（HeatedSteeringWheel-004，兩態） | STATUS_CSWM.HSW_StatSts | 1 | 0 OFF／1 ON |

請確認：(a) 該等「其他狀態」條款於 R1L 不適用（無需驗證）；或 (b) 「無效」另指訊號逾時／缺席，若是請給其判定條件與時限。
我方之處置：該 5 leaf 不生成並具名 `B7-no-invalid-raw`；(b) 成立時再依覆文生成。
ObjectID 空者執行層自 N 欄補入。
```

## 五、上繳 `up/20260907_VS-CF-01.md`

1. §〇 之 sha 三件（母本、sandbox 副本、Revise1）＋ DBC 7 支訊號兩本逐字相符之證。
2. 13 列改寫前後 diff（逐列逐欄）；row 10–13／16／68／84／155／170 之 Remarks diff。
3. R-VS102 落檔行號、live 取號查證；DR-18 追加行號；DR 草稿行號。
4. lint：對 Revise1 跑本線 lint，PENDING 計數須由 25 → **12**（DR-15 ×10、DR-21 row 28、DR-26 row 35），不符即報。
5. 別名表定位結果。
6. 工具旗標逐次聲明；自報。

## 六、未結 DR（本包後）

- DR-15′：已送出待覆（10 列）。
- DR-21 逐實例（IGN_OFF_ACC，row 28）、DR-26（row 35）、DR-25′（row 245/246/249/250 不生成）、§四草稿：**皆待 Pei 取號送出**。

## 七、不在本包

審閱 `CFTS044_review_20260907.md` 之 F1（sibling 括號下半 4 組）、F2（空標籤 3 列）、F3（全形分號其餘 36 列）、F4（導航路徑 14 列）、S1 標籤大小寫、row 252 未生成 —— 待 Pei 指派。
