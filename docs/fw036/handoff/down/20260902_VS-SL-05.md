# 下放包 VS-SL-05 —— 送抽前修正（Pre 矛盾 52 列＋R4 否定式代表值＋DR 縮節）

日期：2026-09-02　層級：Tier 2 → Tier 1
前置：`down/20260902_VS-SL-04_review.md` §2／§3
上繳寫 `up/20260902_VS-SL-05.md`。BL／VC 沙盒稿仍不動。

## §1　Pre「menu is open」矛盾修正

對 `sandbox/vssl/vf230_vssl.xlsx`（先備份 `.v4.bak`，不覆寫既有備份）：
- 命中條件：Pre 含 `The Vehicle Settings menu is open` **且** Procedure 含本輪插入之
  `Press "Settings" on Menu Bar` 導覽段（Tier 2 實測 52 列，實跑為準，差異列明）
- 處置：刪該 Pre 行、重編號；**未插導覽之列不動**
- 產出命中清單 `_v5_pre_fix.tsv`（row／tc_id／刪前 Pre 行號）

## §2　R4 否定式代表值（7 列）

- No.149 系 4 列：Pre 補 `PROXI Country_Code = 2 (United States of America)`
- No.268 系 3 列：Pre 補 `PROXI Trailer_Light_Check = 1 (Type 1 (Radio))`
- 兩系於報告 reasoning 欄註「EP 代表值；兄弟值見值表」（No.268 註 2／3）
- `_v4_branch3_resolution.tsv` 就地改判為 `R4`，evidence 補值表鍵

## §3　DR 第四節縮節

R3 = 25（`NO_FIP_ROW` 22＋`FIP_PARAM_NOT_IN_VALUES` 3）。
第四節重列；`NEGATED_CONDITION` 之 7 名自 DR 移除。既有三節不動。

## §4　v5 報告與 lint

- `vf230_settings_dryrun_v5.{tsv,md}`：分支分布重列（R4 獨立計）
- lint 四項（括號下半／尾句號／雙引號／Proc↔ER 步數）三本重跑，0 方過
- assert：§1 命中數 = 刪行數；R4 = 7；PENDING = 25

## §5　上繳

`_v5_pre_fix.tsv`、v5 報告、重生沙盒稿、縮節 DR 稿之路徑與 SHA256；自檢；自報。
**本包准後即送 Pei Excel 抽驗**（三本沙盒稿＋DR 稿）。
