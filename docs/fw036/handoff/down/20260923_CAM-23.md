# 下放包 CAM-23 —— 拆解審計之補生成（B ＋9）＋ A 本 50 列覆核 ＋ selfcheck 第 10 項 ＋ CAM-21 恢復（B 本 dry-run）

指派執行層 session：延用。對應審閱：`down/20260923_CAM-22_review.md`（同發）。CAM-21 之 §1／§2 併入本包 §4。

---

## §0　禁區

同 CAM-14 §0。不寫 `delivered/`、不 tag。既有 197 列不改（新列另編號）。

## §1　落檔

1. RDF-17（`Make Favorite`／`Edit Favorite`）；DECISIONS 6-71～6-73（CAM-21 §1）。
2. DECISIONS 6-74～6-77：H/M 六列補 9；A 本 50 列覆核；selfcheck 第 10 項 WARN；「1:1 即未拆」撤回、B 本拆解 39.1% 由 037 承擔。
3. GCB-12：pilot 審閱流程加「拆解審計」一步（全域提請）。
4. `selfcheck_camera.py` 第 10 項（WARN 級）：審計器修 boundary（`limit±1` 任一即過）與多觸發（排除 §5.3 常數 hop）後固化；對 A 本 240 ＋ B 本 197 跑一次，命中數回報。

## §2　B 本補生成 9 列（`b06/`，ID 續 `NR1L-RVCHMI-198`～`-206`）

依審閱 §一-1 逐列；每列 reasoning 具名「拆解審計 CAM-22 §5 #n」與母列 TC ID；`sibling` 關係記入 `b_plan.tsv`／`coverage_b.tsv`（母 leaf 之 `tc_ids` 追加）。規則同前；lint ＋ selfcheck（十一項）全綠。

## §3　A 本 50 列逐列覆核

`b_audit.tsv` 同格式產 `a_audit.tsv`（只對 50 個機械命中列逐列覆核 H/M/L，其餘 190 列記 `none`）；H/M 者列清單（逐字證據、追加數）**先回報不生成**，由分析層下包裁補生成範圍。

## §4　B 本 sandbox dry-run（CAM-21 §2 原文，口徑改 206 ＋ 6）

1. 212 列（206 TC ＋ 6 個 D 欄佔位）組簿 → `features/camera/sandbox/b_dryrun/FM-WI-FSM-036-A01 … SWQT_RearViewCamera_B_<date>_dryrun.xlsx`；Cover／Product Document 最小欄；approver／reviewer／中文作者名留空。
2. D 欄升冪（`SWE1-RVC-001`～`-159` 含子項；子項序依 037 列序，回報排序規則）；Test Set 八組拼寫一致。
3. `lint036.py --profile camera` 全表；`Z`；sheet 級問題。
4. `coverage_b.tsv` ↔ 工作簿對帳（206 ＋ 6 ＝ 212）；`pending_by_dr_b.tsv`。
5. sha256 回報；不 tag。

## §5　全案交付前置清單（CAM-21 §3 原文）

A 本候選 dryrun2（244）、B 本候選（本包，212）；阻交付 DR 與列數；`bench_verify.md`；RDF-01～17；Phase 7 待 Pei 准；兩本分交（R-CAM1(a)）。

## §6　上繳要求（`up/20260923_CAM-23.md`）

1. 落檔清單；selfcheck 第 10 項對 437 列之 WARN 數。
2. `b06/` 9 列產物、lint、selfcheck；`coverage_b.tsv` 更新。
3. `a_audit.tsv` ＋ A 本 H/M 清單（不生成）。
4. B dry-run 路徑／sha、lint、sheet 級問題、對帳、`pending_by_dr_b.tsv`。
5. §5 清單實數；R-CAM17 證據表；自報；未結。

## §7　升級條件

- A 本 H/M 合計追加 > 30（停於清單）。
- B dry-run lint ERROR 類非 0 且原因在組簿工具。
- D 欄子項排序無法自 037 列序唯一決定。
