# 下放包 SEC-07 —— PENDING 內查：解禁三個 zip、沿用 power_moding S2R 寫法、挖 Auth-Prog evidence → 128 行重分類 → `security_v03`

前置：`up/20260916_SEC-06.md`、`down/20260916_SEC-06_review.md`。
Pei 2026-09-17 裁：出 SEC-07（不需 RD 回覆即可跑）。
執行層 session：新開。

---

## §0　禁區

- `down/`／`up/` 既有檔不動；canon 不動；`lint036.py` 不動；`framework.md` 不動（LOCKED v03）；其他 feature **唯讀**（power_moding 只讀不寫）。
- 三個 zip **只在 `/tmp` 或 `_intake/Security/unzip/` 拆解、唯讀**；不投遞解壓後之檔到 `sources/raw/`（登 MANIFEST 只記 zip 本身 sha 與其內容清單）；apk 不反編譯、不執行。
- 解得之字串、方法名、路徑、憑證檔名一律**逐字**（R-SEC7(e)）；zip 內查無者仍 PENDING，不得以「應該有」補值。
- 既有 xlsx 全部不動；產物為 `merged/security_v03.xlsx` 與 `delivery/` 更新版。
- `X-h`／`X-c`／`X-d`／`X-g`／`X-j`／`X-k`／`X-m` 之 PENDING **本包不動**（等資產）；`X-i` 不動（等 Pei 裁）。

## §1　內查來源（解禁）

| 來源 | 路徑 | 針對 |
|---|---|---|
| Z1 | `CCVR/To Validation team/Secure Log CS.212/StevenJSHsu/KeyInstall_IntegrationTests.zip` | `X-f-2` |
| Z2 | `CCVR/To Validation team/Cert Val CS.98/IntegrationTests.zip` | `X-e`／`X-e-2`／`X-e-3` |
| Z3 | `CCVR/To Validation team/Secure Log CS.212/StevenJSHsu/CertProvider_IntegrationTests.zip` | 同 Z2（與 Z2 比對是否同體）|
| Z4 | `CCVR/To Validation team/Secure Log CS.212/StevenJSHsu/IntegrationTests/`（目錄）| 同上（可能為 Z3 之解壓）|
| P1 | `features/power_moding/`（profile、framework、交付本 pm_29 或其 sandbox 最終本）| `X-n` |
| C1 | `SYS2_Mapped.xlsx` sheet `Auth-Prog CS.93` 之 Test steps／Results／Evidence 欄（已投遞）| `X-l`、`X-j` |

三 zip 各取 sha16 登 MANIFEST（`note = reference-only; unzipped read-only for SEC-07 inventory`）；Z2／Z3 sha 相同者只登一次。

## §2　任務

### 2.1　zip 清單（Z1～Z4）
`features/security/data/zip_inventory.tsv`：`zip | path_in_zip | size | kind(java|kt|xml|pem|crt|der|json|sig|bp|gradle|other)`。
再抽：
- **測試方法名**：`*Test*.java`／`*.kt` 中 `@Test` 後之方法名，含所屬類 FQCN → `zip_methods.tsv`（`zip | class_fqcn | method | instrument_cmd(組合式，逐字)`）。與 `Test_Items.txt` 15 條交叉：多出者列 `new`，缺者列 `missing`。
- **測試資產**：`.pem`／`.crt`／`.der`／`.json`／`.sig` 檔名與相對路徑 → `zip_assets.tsv`；比對 037 之 `SecurityAssets/oem-certs/cert-provider/`、`{TYPE}/cfgs/`、`{SSN}_SAM_{SAMType}.json` 命名式，命中者標 `matches_037`。
- **runner 宣告**：`AndroidManifest.xml` 之 `<instrumentation android:name=… android:targetPackage=…>` 逐字 → `instrument_cmd` 之依據。

### 2.2　`X-f-2`（18 行）
Z1 若含 KeyInstall 之 instrumentation 類與方法（如 `getStatus`／`installKeys` 類名）：對應之 TC Proc 步驟以 `$ adb shell am instrument -w -e class <FQCN>#<method> <targetPackage>/<runner>` 補值（全式逐字自 manifest 組合），ER 以 `RC` 類 `OK (1 test)`；Remarks `source: Z1 <path_in_zip>`。方法之語意與 037 列不符者**不硬配**，留 PENDING 並在 `zip_methods.tsv` 註 `no 037 match`。

### 2.3　`X-e`／`X-e-2`／`X-e-3`（7＋ 行）
Z2／Z3 之測試憑證：若有 wrong-subject／wrong-issuer／wrong-OID／revoked／broken 之憑證檔或產生腳本，Pre-Condition 之資產描述補檔名（逐字）；若同時有對應 `@Test` 方法（如 `*WrongSubject*`／`*Revoked*`），觸發步驟補 instrument 全式，PENDING 消除。只有憑證沒方法者：Pre-Condition 補檔名，觸發步驟仍 PENDING（改註 `X-e-2: method missing, asset found`）。

### 2.4　`X-n`（4 行）
讀 P1：找 power_moding 之 suspend／resume（S2R）觸發寫法——CAN 訊號、`$ adb shell` 指令或 PHYS 動作，**逐字**取其 Procedure 行與 ER 行；核對 DBC（R-7／R-17）後套入 `NR1L-SAM-025`／`-026`。Remarks `source: power_moding <TC ID>`；沿用需符 R-SEC15(a)（同一 HU 電源行為，非跨場景移植——S2R 是平台行為，允許）。P1 查無者留 PENDING。

### 2.5　`X-l`／`X-j`（14 行）
C1：Auth-Prog 第 2／4／5／6／7 項之 UDS 序列（`31 01 F0 00` → `71 01 F0 00 0x`）與 evidence 欄逐字；能落成 SWDL-003／004 之 Proc（`$ 31 01 F0 00`）＋ ER（`Positive response is received: 71 01 F0 00 01`＋ bit field 語意依 CS.00102）者補值；需要「竄改後的 OTA 映像」本身者（`X-j`）仍 PENDING。

### 2.6　重分類
`pending_summary.tsv` 加欄 `class`：`RESOLVED(Z1|Z2|Z3|P1|C1)`／`AWAIT_ASSET`／`AWAIT_RULING(X-i)`；`asset_request.md` 同步縮減（只剩 AWAIT_ASSET）。
分析層預估：內查可解 **30~45 行**；解後 PENDING **83~98**。

### 2.7　產物
`gen_batch1.py`／`gen_batch2.py`／`build_merged.py` 重跑 → `merged/security_v03.xlsx`；`build_delivery.py` → `delivery/…_SWQT_Security_20260917.xlsx`（封面日期改 `2026-09-17`；`D7`／`D8`／`D9` 中文半仍留空）。lint 門檻 0、`K=0`、selfcheck 0。

## §3　上繳要求（`up/20260917_SEC-07.md`）

1. 三 zip sha16、Z2／Z3／Z4 同體判定、MANIFEST 登錄。
2. `zip_inventory.tsv`／`zip_methods.tsv`（與 `Test_Items.txt` 交叉：new／missing 計數）／`zip_assets.tsv`（`matches_037` 計數）。
3. 各代號解前／解後行數表；`RESOLVED` 逐行列 TC ID、補值內容、`source:`。
4. `X-n`：P1 找到之 TC ID 與逐字行；DBC 核對結果。
5. `X-l`：C1 落值之 TC 與 bit field 語意來源。
6. v03 lint 全項；`pending_summary.tsv` 三類計數；`asset_request.md` 縮減後版。
7. 自報錯誤；zip 內任何**與 037／CCVR 矛盾**之發現（方法名、路徑、憑證命名）另列。

## §4　升級條件

- zip 解壓失敗或含非預期型態（可執行檔以外之 binary 大量）。
- 補值任一處非逐字（執行層自檢：每一 `RESOLVED` 行附 `source:` 之檔案／行號）。
- `RESOLVED` > 60 行（超出預估上限，需檢查是否硬配）。
- lint 任一門檻非 0。
