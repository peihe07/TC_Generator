# 審閱 —— 上繳包 VS-SL-04（二次查找＋沙盒稿重生＋DR 補節）

日期：2026-09-02　層級：Tier 2　對象：`up/20260902_VS-SL-04.md`

判定：**准，含兩項追認；另發現一類 Pre↔Procedure 矛盾（52 列，本層實測）須修後才送抽 —— VS-SL-05 一小輪，非退件。**

---

## §1　覆驗與追認

- 沙盒稿 SHA `eec8445afe104a03` 覆驗相符；438 列；PENDING 32；舊形制 `PROXI $` 殘留 **0**；`.v3.bak` = VS-SL-03 上繳之 SHA。
- **R1b 追認（准）**：No.147 逐字複驗確為引號式 `is "Present"`，`Auto_Power_Folding_Mirrors {1: Present}` 在值表。就地處理、不動共用解析器之取捨**准**；共用碼二缺口（引號式、非 ASCII）留待另包，收訖。
- **`FIP_ALWAYS_ON` 追認（准）**：No.114／115／118 逐字複驗皆 `Always true`。恆顯示者無條件可加，與 `FIP_ALWAYS_OFF` 同不 PENDING、不 DR。R2 全表（含 No.165／199／275，275 之 `Alwatys` 缺字照錄）複驗相符。
- `Phone Repetition` → No.215 之綁定留痕收訖：該綁定出自本層審閱 §2 之明指，責任在本層；若上游 DR 回覆與此相左，回頭改判。
- 三處自報（備份曾存錯已自 git 取回、DR 證據字樣不實已正、`NO_MATCH_EITHER` = 0 之區辨）：收訖。DR 第四節「名字對得上、條件沒人寫」與第一節分問，**對**。

## §2　R3 之再縮 —— `NEGATED_CONDITION` 7 列可解，不入 DR（新判 R4）

本層實測值表：`Country_Code` 無 Australia 且含 `2 (United States of America)`；
`Trailer_Light_Check {0: Absent, 1: Type 1 (Radio), 2: Type 2, 3: Type 3}`。
否定式條件之補集取**規格值表內之代表值**即滿足條件，屬 FO §8.3 之 EP 代表值選取，非造值：

- No.149 `Country_Code is not [Australia]` → `PROXI Country_Code = 2 (United States of America)`（R-VS84 NAFTA 之自然代表）
- No.268 `Trailer_Light_Check is NOT [Absent]` → `PROXI Trailer_Light_Check = 1 (Type 1 (Radio))`，reasoning 註代表值與兄弟值 2／3

→ R3 由 32 縮至 **25**（`NO_FIP_ROW` 22＋`FIP_PARAM_NOT_IN_VALUES` 3），DR 第四節同縮。

## §3　送抽前必修：Pre「menu is open」與插入導覽之矛盾（本層實測 52 列）

v4 沙盒稿實測：Pre 含 `The Vehicle Settings menu is open` 者 90 列，其中 **52 列**之 Procedure 已插入
`Press "Settings" on Menu Bar` 導覽段 —— Pre 斷言之狀態隨後由步驟建立，且該句屬步驟可控狀態，
依 FO §4.4 不得為 Pre。修法：**僅於已插導覽之列**刪該 Pre 行並重編號；未插導覽之 38 列不動。
此為插入邏輯之遺漏，非原稿既有病（原稿無導覽段時該 Pre 行自洽）。

## §4　VS-SL-05（已下放 `down/20260902_VS-SL-05.md`）

1. §3 之 52 列（實跑為準）刪 Pre 行＋重編號
2. §2 R4 套用 7 列＋DR 第四節縮節
3. v5 報告＋lint 重跑（含 Proc↔ER 步數、括號下半、尾句號、雙引號）
4. 上繳准後**即送 Pei Excel 抽驗**（含未動之 BL／VC 稿）
