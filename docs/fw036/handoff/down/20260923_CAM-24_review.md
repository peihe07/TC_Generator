# 下放包 CAM-24 審閱（分析層 2026-09-23）

對應上繳包：`docs/fw036/handoff/up/20260923_CAM-24.md`
判定：**通過。** A 本 245 TC、dry-run v3 249 列合規；`-242` 先查後判正確。惟 §2-2 所揭之形制矛盾須解：三個 B 本前例違反 canon，不是 batch05 違反前例。CAM-25（收尾補正）同發。

---

## §一　裁決

| # | 項 | 裁決 |
|---|---|---|
| 1 | `-244`／`-245` 一列或兩列 | **兩列**（canon §8.3：boundary 每點一 TC；pilot `-009`／`-010` 已依此拆，CAM-03 審閱 §二-6）。**B 本三個前例反向補正**：`NR1L-RVCHMI-036`（205／206）拆為兩列；`-111`（pinch out／pinch in 為兩個觸發，§5.7 不同觸發須拆）拆為兩列；`-142`（OK／`X`／5 s 三條清除路徑，三觸發）拆為三列。B 本 206 → **210**。分析層記一筆：三列皆經批次審閱而未抓（抽讀未及） |
| 2 | `batch05_plan.tsv` 欄位表 | 不另立；A 本以 `spec_reference` ＋ `verbatim_source_pid` 定位即足 |
| 3 | `-245` 不造低於門檻之系統行為 | 准（§8.4.1）；`Remarks` 註「來源未載低於門檻之行為」 |
| 4 | `-243` 之 `J` 豁免 | 確認 |
| 5 | `DELIVERY_STATUS.md` 形制 | 准；CAM-25 後更新為終態 |

## §二　R-CAM17 證據複驗

隨機取證據 2（`RVC_ImageDefeat` 非 DBC 訊號）：分析層以 `/tmp/f.dbc`（FDCAN8）`grep -ic "RVC_ImageDefeat"` → 0；`ImageDefeatRQSts` 命中，屬實。

## §三　備查

- CAM-25 為真正之最後一包（B ＋4、B dry-run v2、DELIVERY_STATUS 終態）。
- 之後全案交 Pei：DR 11 條、bench 101 列、RDF 17 條、Phase 7。
