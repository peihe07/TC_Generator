# 下放包 SEC-20 —— 把 RD 已給之步驟（CCVR Cert Val CS.98／ECU ID CS.165）真正寫進 TC → `security_v14`

前置：`up/20260917_SEC-18.md`（SEC-19 若已跑則接其後）；分析層 2026-09-17 逐 token 審計：CS.98 item 1／2／3／5～8／10 之 RD 步驟在 124 TC 中 **0 命中**，ECU ID item 3～6 之損毀變體未拆。
Pei 目的：「把他們已經提供的寫成測試」——RD 給了可執行步驟就不得留佔位（分析層 A-46：R-SEC8(f) 之「DCL 只在 CS.98 入 TC 時用」條件使 RD 步驟從未落地）。
執行層 session：新開；不跑 `--gate`（GC-16 前禁令）。

---

## §0　禁區
- 只改本包點名之 TC；其餘 0 diff。步驟、路徑、檔名、方法名**逐字取 CCVR v2.7 工作中版**（`4e13ee7efbeaf29b`）之 `Cert Val CS.98` STEPS 欄／`ECU ID CS.165` Test steps 欄；不得改寫、不得補值。
- `test_item` 上半仍為 037 verbatim（CRL 措辭不動）；下半英文括號可改以反映新 sibling。
- 不跑 `--gate`；R-SEC8(f) amend 落 `RULINGS.md`，sha 乾跑。

## §1　裁定
```text
R-SEC8(f) amend  CRL／DCL：test_item 上半沿 037（CRL）；Procedure 依 RD 實作（CCVR Cert Val CS.98 STEPS 之 DCL JSON 流程逐字）；
                 Remarks 加 `term: 037 "CRL" = RD-implemented DCL file /vendor/odm/etc/cert_store/dcl.json (CCVR Cert Val CS.98)`。
                 R-SEC15(a)「同一場景」判準：CS.98 item 2/5~8 之場景 = 撤銷後驗證被拒，與 037 CP-004（非 ECU 撤銷檢查）同場景，可用。
R-SEC26  RD 已給可執行步驟者不得留佔位：CCVR 五 sheet 之 Test steps／STEPS 欄凡有 `$` 可執行行者，對應 037 場景之 TC 必須採用；
         僅資產本身（憑證檔、DCL 檔、chain）仍可為 `<… provided by RD (X-n)>`。
```

## §2　變更清單（逐字來源標於各項）

### 2.1　Cert Provider
| TC | 變更 | 來源 |
|---|---|---|
| CP-004 sibling（撤銷命中→拒絕，現 X-e 佔位）| Procedure 改為 CS.98 item 2 之流程：`$ adb root && adb remount` → `$ adb shell pm list instrumentation \| grep melcocertprovider` → `$ adb push dcl_baseline.json /vendor/odm/etc/cert_store/dcl.json` → 跑 `samCertTestNormalFlow`（baseline 通過）→ `$ adb logcat -c` → `$ adb push dcl_revoked.json /vendor/odm/etc/cert_store/dcl.json` → 再跑 → `$ adb logcat -d \| grep -i "CertProvider"`；ER 依 item 2 Expected（拒絕）；DCL 檔仍為 `<dcl_revoked.json provided by RD (X-e)>` 資產佔位但**步驟不佔位** | CS.98 item 2 STEPS／Expected |
| CP-004 新增 4 sibling：L0／L1／L2／L3 撤銷 | 同上，`dcl_revoked_l0~l3.json`；`distinguishing_axis = revoked level` | CS.98 item 5～8 |
| CP-001 負向新增 1 sibling：self-signed chain | `ecuCertTestSelfSignedChain` 全式 instrument；ER 依 item 3 Expected | CS.98 item 3 |
| CP-007／CP-009 各加前置盤點步驟 | `$ adb shell ls -la /vendor/odm/etc/cert_store/`、`/cfgs/`、`/ecu_chain/`、`/oem_cert_tree/`；ER：列出之目錄／檔案（逐字 item 1 Expected）| CS.98 item 1／10 |
| 涉及 `/odm/etc/cert_store` 之既有步驟 | 路徑依 RD 實作 `/vendor/odm/etc/cert_store/`（037 寫 `/odm`，Remarks 註 `037 /odm = RD /vendor/odm`）| CS.98 全項 |

### 2.2　ECU Cert
| TC | 變更 | 來源 |
|---|---|---|
| ECUC-002／004 | 補第 3 步 `re-connect any internet or reboot`（PHYS：`Reconnect the DUT to the internet, or reboot the DUT` ＋ `$ adb reboot`）；ER 補 `recoveryed` 語意行（`ECU_INSTALLATION_STATUS value : 2 -> 1` 已在）| CS.165 item 2 步驟 3／Expected |
| 新增 4 sibling | corrupt CERT 0,1／0,2／1／2（`distinguishing_axis = corrupted cert index`）；步驟同 item 2 | CS.165 item 3～6 |
| ECUC-016 | 加 case 2 分支 sibling：`dealer mode, export CSR` → 觸發仍 `<Dealer App … (X-k)>`（RD 未給指令）| CS.165 item 12 |

### 2.3　KeyInstall／SAM／SWDL／LOGENC
不動（CCVR 對應 sheet 無新可執行步驟）。

## §3　產出
- `sibling_plan.tsv` 增列；ID 接續各組末號（CP 自 024、ECUC 自 021）；本內仍依 SWE1 序（R-SEC25(b)）— 新 sibling 插入其 SWE1 列之後，**後續 TC 重編號**，附 `id_map_v13_v14.tsv`。
- `SEC_VER=v14` 六本＋`_all`；套色（佔位 TC 集合重算，預期減少）；lint 門檻 0；selfcheck 0；`placeholder_*`／`asset_request.md`／`EXEC_GUIDE.md`（§3 指令索引與 §1.1 路徑）重生；`delivered/` 同步。
- `coverage.tsv` 70／70 不變，`tc_count` 更新。

## §4　上繳要求（`up/20260917_SEC-20.md`）
1. R-SEC8(f) amend／R-SEC26 body sha8（乾跑）。
2. 新增／修改 TC 逐條清單（含來源 item 號）；TC 總數（預期 124 → 132～134）；`id_map`。
3. 佔位行數前後（預期 96 → ≤ 86）；`placeholder_by_token` 之 `X-e` 變化。
4. 逐 token 複驗：CS.98 item 1／2／3／5～8／10 與 CS.165 item 2～6／12 之每一可執行行在 TC 中命中數 ≥ 1（列表）。
5. 六本 sha、lint、換行三數、diff 範圍。
6. 自報錯誤。

## §5　升級條件
- §4-4 任一可執行行命中 0。
- 任何步驟文字非 CCVR 逐字（執行層以 substring 自檢）。
- lint 任一門檻非 0。
