# 下放包 CAM-22 審閱（分析層 2026-09-23）

對應上繳包：`docs/fw036/handoff/up/20260923_CAM-22.md`
判定：**審計通過；結論採「B 本並未較疏」（§4-1 結構性前拆 39.1% 為主因）**，惟六列 H/M 屬實，補 9 TC。升級條件 §4-2 成立之處置：A 本 50 列逐列覆核納入下一包（成本低、可同時驗判準）。CAM-23 同發（併 CAM-21 未執行部分）。

---

## §一　裁決

| # | 項 | 裁決 |
|---|---|---|
| 1 | H 2 列／M 4 列 | **全數補生成，追加 9 列**：`-056` ＋4（CHMSL/Cargo、TRG、FFCTL、SVC 各一，PROXI 配備逐一）、`-044` ＋1（無線側）、`-033` ＋1（10 s 前仍在，取 9 s 形制同 `-035`）、`-085` ＋1（`Apps Page` 作被列舉項）、`-109` ＋1（Drive）、`-196` ＋1（`Fixed Guidelines` Off 側）。皆為**新列**（不於既有列加步，保持一列一驗證點） |
| 2 | A 本 50 列機械命中 | **逐列覆核**（同 B 本 H/M/L 判法），H/M 者補生成；此舉同時驗判準 |
| 3 | 審計判準固化 | 修 §7-2（boundary：`limit±1` 任一出現即通過）與 §7-3（排除 profile §5.3 常數 hop）後，**固化為 selfcheck 第 10 項，等級 WARN**（列出候選，不阻上繳） |
| 4 | 「1:1 即未拆」之推論 | **撤回**；記 DECISIONS：B 本之拆解 39.1% 由 037 承擔，比例不與 A 本相較 |
| 5 | 分析層審閱漏洞 | 記一筆：抽讀 ≤ 4 列不足以覆蓋拆解充分性，後續 feature 之 pilot 審閱加「拆解審計」一步（GCB-12 提請入全域 pilot 流程） |

## §二　R-CAM17 證據複驗

隨機取證據 3（剝除影像 token 前後 58 → 39）：成因（URL 編碼 `%E5%9C%96` 讀成 `96 %`）合理且兩數皆列，屬實。

## §三　備查

- CAM-21 §1／§2 於 CAM-23 恢復。
- 補生成後 B 本預期 206 TC（＋A 本覆核之追加）。
