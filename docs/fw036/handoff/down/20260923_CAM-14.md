# 下放包 CAM-14 —— A 本 sandbox 組簿 dry-run ＋ B 本 B01a（`Activation and Exit` 前半，22）

指派執行層 session：延用。對應審閱：`down/20260923_CAM-13_review.md`（同發）。

---

## §0　禁區

同 CAM-03 §0。**不寫 `features/camera/delivered/`**；sandbox 組簿只落 `features/camera/sandbox/a_dryrun/`。B 本只生成 B01a。

## §1　落檔

1. profile §7.6：生成前先掃該 SWE 列相關之 ANOMALIES；selfcheck 第 9 項（CEH `N`／`Not yet` ＋ ANOMALIES「不走 CAN」→ 命中）。
2. SYS1 檔名 token 化定案（只換空白）：`recon_b_rows.tsv`／`b_plan.tsv` 之 `spec_reference` 重生；GCB-09（同檔名拼寫變體 lint）。
3. DECISIONS 6-47～6-50：§2-2 三處確認；token 化；ANOMALIES 預掃；`est_tc` 基準。

## §2　A 本 sandbox dry-run

1. 240 列（十目錄）組簿 → `features/camera/sandbox/a_dryrun/FM-WI-FSM-036-A01 … SWQT_RearViewCamera_A_<date>_dryrun.xlsx`：Cover／Product Document 依 workflow 之模板事實填最小欄（version／date／author 由 Pei 之既有本例抄；approver／reviewer／中文作者名**留空**）。
2. 跑 `python3 scripts/lint036.py --profile camera <dryrun.xlsx>` 全表；`Z` 全綠；回報 sheet 級問題：`N` 欄多行 spec_reference 之換行、`AH` 欄 remarks、Vehicle Model 七欄、Test Set 十組拼寫一致、D 欄升冪（R-G42）。
3. `coverage.tsv` ↔ 工作簿列數對帳；PENDING 列數 ↔ DR 對照表（`data/pending_by_dr.tsv`）。
4. dry-run 檔 sha256 回報；不 tag。

## §3　B 本 B01a：`Activation and Exit` 前半（22 leaf）

**步驟 A**：自 `b_plan.tsv` 取該組前 22 leaf（依 SYS1 章節序），逐列判軸、hop label 逐字（cache 本 `Description`）、互參入口（`crossref_a_b.tsv`）、PROXI／CAN 前提沿 A 本寫法；`b01a_plan.tsv`。
**步驟 B**：ID 自 `NR1L-RVCHMI-001` 起；Test Group `Rear View Camera`；spec_reference `{檔名}_{章節號}`（§一-2 之 token 化）；`test_item` 上半為 SYS1 `Description` 逐字（≤ 50 token，摘句同法）；Pre-Condition 定序同 A 本；全套 lint（含 Z）＋ selfcheck（十項）。
- `7.3.1`～`7.3.3`（只在 cache 本）之三列依 R-CAM6 生成。
- umbrella 列不生成，`coverage_b.tsv` 記 `UMBRELLA — covered by children`。
- HMI 章節之「refer to popup list」文字以 `forms/Pop Up List HMI R1 (26PI).xlsx` 逐字（PU 號入 reasoning）；查無者 `PENDING: DR-CAM-h`。

## §4　上繳要求（`up/20260923_CAM-14.md`）

1. 落檔清單；token 化重生之前後 sha。
2. dry-run 檔路徑／sha、lint 全表、sheet 級問題清單、對帳結果、`pending_by_dr.tsv`。
3. `b01a_plan.tsv`、22 列產物、lint、selfcheck、PENDING；R-CAM17 證據表。
4. 自報錯誤；未結。

## §5　升級條件

- dry-run lint 出現 ERROR 類且原因在組簿工具（非 TC 內容）。
- B01a 任一列之 hop label 於 cache 本 `Description` 非可辨之畫面標籤（>3 列則停，回報形態）。
- B01a > 30。
