# 下放包 CAM-29 —— 來源覆蓋覆核（A 本 92 ＋ 28 個無紀錄來源）＋ PARTIAL 53 全案重判（只覆核不生成）

指派執行層 session：延用。對應審閱：`down/20260924_CAM-28_review.md`（同發）。本包只產覆核表；補列與否由 Pei 依 `GAP` 清單裁，另包生成。

---

## §0　禁區

同 CAM-14 §0。不改任何 TC、不改兩本 dry-run。

## §1　A 本來源覆蓋覆核

母體：`data/source_disposition_audit.tsv` 之「無紀錄」103 減 `SWE-CAM-024` 之 11 ＝ **92**（`-015`／`-016`／`-018`）＋ `rd_vc_crosscheck` NO-TC 之「無 plan 紀錄」**28** → 去重後之來源集合。

逐來源：
1. 取 SYS2 Description（既有本；Out of Scope 者剔除並記）。
2. 以內容配對全案 465 列（A ＋ B）：Procedure／ER／Pre-Condition／reasoning 任一處驗證該 Description 之行為 → `COVERED-BY <tc_id>`（附配對之逐字依據）；同句之疊層／逐字重複 → `DUPLICATE-OF <SYS-RA>`；hybrid 不可注入等既裁不生成 → `EXCLUDED (A-CAnn)`；皆無 → **`GAP`**。
3. `GAP` 者附：Description 逐字、所屬 Test Set 候選、軸（車型／品牌／boundary）、預估 TC 數。

產 `data/source_coverage_review.tsv`；摘要：`COVERED-BY`／`DUPLICATE-OF`／`EXCLUDED`／`GAP` 計數，`GAP` 按 SWE 分佈。
`COVERED-BY` 者同步回填 `batch01_plan.tsv`／`batch01b_plan.tsv`／`batch02_plan.tsv` 之 `covered_by` 欄（plan 為紀錄檔，非 TC，可改）；`coverage.tsv` 之 `sources_covered` 欄若有則同步。

## §2　PARTIAL 53 之全案重判

`rd_vc_crosscheck.tsv` 中 PARTIAL 且缺漏點**含來源所載**者 53 列：每一缺漏點以全案 465 列重找（不限同錨；含兄弟 TC 與 B 本）→ `COVERED-BY <tc_id>` 或 **`TRUE-GAP`**（附來源逐字、非 RD 自加之證明：該點須能在 SYS2 Description 中逐字定位）。
產 `data/rd_partial_recheck.tsv`；`TRUE-GAP` 清單附補列候選（Test Set／軸／預估數）。

## §3　落檔

1. **RDF-19**：RD 驗證標準含來源未載之點（`[not in source]` 25 項）＋ RD-ONLY 11 之手段，附表，供 RD 決定是否入規格。
2. `bench_verify.md` 加「RD 建議手段」註（RD-ONLY 11 之 TC）。
3. DECISIONS 6-90（RD 車型欄不覆寫既裁）、6-91（PARTIAL 處置）、6-92（覆核包）。
4. ANOMALIES A-CA40（分析層三處誤判）。

## §4　上繳要求（`up/20260924_CAM-29.md`）

1. `source_coverage_review.tsv` 摘要＋ `GAP` 全清單（逐字＋候選）。
2. `rd_partial_recheck.tsv` 摘要＋ `TRUE-GAP` 全清單。
3. plan 回填之 diff 計數。
4. 落檔清單；R-CAM17 證據表（配對之逐字依據抽樣 10 筆附全文）；自報；未結。

## §5　升級條件

- `GAP` ＋ `TRUE-GAP` 合計 > 40（停於清單，先回報分佈）。
- 任一「無紀錄」來源於 SYS2 既有本查無（回報，勿造）。
