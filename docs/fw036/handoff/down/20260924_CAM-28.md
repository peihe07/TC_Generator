# 下放包 CAM-28 —— 來源版本漂移處理（VF551_V2 R1→R2）＋ RD 驗證標準對帳 ＋ 車型欄對帳

指派執行層 session：新開或延用皆可。發包依據：Pei 2026-09-24 對 REF 新增內容之確認（分析層盤點見本包 §1）。
本包為 `DELIVERY_STATUS.md` §9 三種觸發之外之**第四種：來源改版**，DECISIONS 記為新觸發類型；不生成新 TC，只改既有列之 verbatim／錨／車型欄。

---

## §0　禁區

同 CAM-14 §0。不寫 `delivered/`、不 tag。**只改本包 §3 判定須改之列**，其餘不動；改動逐列 diff。

## §1　REF 新增檔盤點（分析層 2026-09-24 實測）

| 檔（`ASW-R3/Camera/REF/`） | sha16 | 判定 |
|---|---|---|
| `VF551_V2/VF551_V2_DT-HDCC_2026-0311_dependency.xlsx`（753 列，Document ID `VF551_V2_R2`） | `dd6352b5e6daec5d` | **VF551_V2 改版 R1 → R2**：與 `sources/raw/` 之 R1 本逐列比 **659 同／93 Description 改／0 增刪**；另含 RD「驗證標準／驗證方法」103 列 |
| `VF551_V2/Video_Parking_Assistance_-_LTM_VF551_V2_R2.docx` | 執行層取 | R2 原文，登錄 |
| `CFTS092_2026-0302_dependency_0305_CAN-PROXI.xlsx`／`_HMI.xlsx`／`-1.xlsx` | `e8186048db32fbbb`／`757d4083ca8821da`／執行層取 | 同一 SYS2 CFTS092 母體（166 列）加 dependency 欄：**七車型適用欄**（col 23–29）、RD 驗證標準／方法（37 列）、FROP 歸屬。三本 Description 相同（執行層以 sha 或逐列證） |
| `VF551_V3/Polarion_SYS2-VF551_V3_dependency.xlsx` | `0cbd30db23eab716` | R6，與 R1 本 **747/747 同、新 1 列**；RD 驗證標準 41 列 |
| `VF551_V9/`、`VF508_V1/`（Trailer Hitch Assist – RVC，1804 列） | `acb0b88fc8ecb4a6` | 037 未引，**不入本案**；登 MANIFEST note「REF 存在、範圍外」 |
| `CameraDependencyTrackingTable.xlsx`、`…CFTS 092…25PI3.5….doc` | — | 進度表／舊版，不登 |

R-CAM17：上表之「93 改」由執行層以 R1 本（`sources/raw/`）與 R2 本逐 `Sys-RA-Feature-ID` 比 Description 重現，附命令與計數。

## §2　來源登錄

依 R-G66：R2 xlsx、R2 docx、CFTS092 dependency 三本、V3 dependency 本登 `sources/raw/`＋`MANIFEST.tsv`（`features = camera`；note 記「R2 取代 R1 為 V2 母體」「dependency 本與原 SYS2 同 Description，附加欄用於對帳」）。R1 本**不刪**（既有 TC 之 verbatim 出處）。

## §3　V2 R1→R2 漂移處理

1. 產 `data/v2_drift.tsv`：93 個 `SYS-RA-VF551_V2-nnn` × R1／R2 Description × 引用該 ID 之 TC（A 本 `spec_reference`／`verbatim_source_pid`）。
2. 對每個被引用者判：(a) 文字差異只在標點／換行／`_x000D_` → `NO-OP`；(b) 語意同、措辭改 → **verbatim 上半改 R2 逐字**（摘句同法），錨不變；(c) 語意改（條件／值／訊號名）→ 依 §8.6 原規格為準，**Procedure／ER 依 R2 重寫**，reasoning 註 R1→R2 差異，Remarks 註「Updated per VF551_V2_R2」；(d) R2 刪去之行為（本次 0 增刪，預期無）。
3. 未被引用之改動列：只列表，不處置。
4. lint ＋ selfcheck 全綠；A dryrun5。

## §4　RD 驗證標準／方法對帳（CFTS092 37 列、V2 103 列、V3 41 列）

`data/rd_vc_crosscheck.tsv`：每一 RD 驗證標準列 → 對應我們的 TC（依 anchor）→ 三態：`COVERED`／`PARTIAL`（RD 標準多出我方未驗之點，逐字列出）／`RD-ONLY`（RD 方法要求之手段我方無）。**只對帳不生成**；`PARTIAL`／`RD-ONLY` 清單供 Pei 裁是否補列。

## §5　七車型適用欄對帳

CFTS092 dependency 本 col 23–29（`SR26_…` 值 = 適用；`　` = 不適用）與 A 本 CFTS092 錨 TC 之 Vehicle Model 1/0 逐列比：`data/vm_crosscheck.tsv`，不一致者列出（附 RD 值與我方依據）。只對帳；改動由 Pei 裁。

## §6　上繳要求（`up/20260924_CAM-28.md`）

1. MANIFEST 登錄；93 改之重現證據。
2. `v2_drift.tsv` ＋ 被引用列之 (a)(b)(c) 計數與逐列 diff；A dryrun5 路徑／sha／lint／對帳。
3. `rd_vc_crosscheck.tsv` 摘要（三態計數）＋ `PARTIAL`／`RD-ONLY` 清單。
4. `vm_crosscheck.tsv` 不一致清單。
5. `DELIVERY_STATUS.md` 更新（沿革加「來源改版」觸發）。
6. R-CAM17 證據表；自報；未結。

## §7　升級條件

- (c) 類 > 15 列（停，回報清單）。
- R2 之 anchor 編號與 R1 不一致（同 `Sys-RA-Feature-ID` 之 `Source Requirement items` 不同）> 0 → 停，回報。
- `RD-ONLY` > 20。
