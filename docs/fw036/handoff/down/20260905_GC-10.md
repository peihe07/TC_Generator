# 下放包 GC-10 —— GC-07 審閱版本裁定後之矯正、GC-09 續行（2026-09-05）

Pei 裁（2026-09-05）：(1) PROXI 採 **SWC 式**；(2) GC 線（內部流程審查）之分析層為本 session（A）；另一分析層 session（B）對 GC-07 之下放屬 Pei 誤派；(3) `down/20260905_GC-07_review_A.md` 為 GC-07 之有效審閱，現路徑 `down/20260905_GC-07_review.md`（B 版，21:22:48 覆寫）**不得再引為裁決依據**，依 PROTOCOL 唯讀不刪不改，加註見任務 1。

**指派**：本包由**一個**執行層 session 施作；上繳首行記 session 標識與「施作期間無他 session 寫檔」（R-G62(b)）。執行層 B（`up/20260905_GC-09_notice.md` 之作者）優先，因其已停手待命；執行層 A 若仍在寫，Pei 先令其停。

---

## 一、先量（改任何字之前）
1-1　`git status --short | sort`；`git log --oneline -5`；三檔 mtime：`RULINGS_LEDGER.md`、`ASPICE_SWE6_AI_Instruction.md`、`scripts/lint036.py`、`tests/test_lint036.py`、`RULINGS.sha.tsv`。
1-2　台帳現況：`R-G70`、`R-G72` 是否已落錨；各條 (d)(e) 之 PROXI 方向逐字（附行號）；`RULINGS.sha.tsv` 是否已含 `R-G70`／`R-G72`（決定矯正形式：未指紋→直接改；已指紋→R-TM13 加註撤銷改 v4.1）。
1-3　IN §8.7.5 現況：v4 是否已寫入、PROXI 例句方向；lint036 `RE_P_PROXI` 現行正則；GC-09 七任務各自完成度（執行層 A 之部分施作）。
1-4　`down/` 內是否另有 B 線之 `20260905_GC-08*.md`（盤點包）或其他同名檔；`docs/fw036/ANOMALIES.md` 之 `A-GC15`／`A-GC16` 現況（誰先寫、內容為何）。

## 二、矯正

### 1　版本標示（唯讀目錄之處置）
- `down/20260905_GC-07_review.md`（B 版）**不動**。
- `docs/fw036/INDEX.md` 3 節加一行：「`20260905_GC-07_review.md` 現內容為 B session 於 21:22:48 覆寫之版，Pei 2026-09-05 裁定無效；有效審閱為 `20260905_GC-07_review_A.md`。」
- `docs/fw036/ANOMALIES.md` 登 **A-GC16**：`down/` 唯讀被覆寫（B session）；分析層 A 之原檔以 `_A` 重落；形態＝同名寫入無互斥。
- PROTOCOL「目錄唯讀」節加：「同一工單同時只得一個分析層 session 落檔；`write_file` 前須 `get_file_info` 證路徑不存在，存在即改名（`_A`／`_B`）不覆寫」（R-G62(c)）。

### 2　R-G70 之 PROXI 方向 → SWC
依 1-2 之形式：(d)／(e) 改為「PROXI：`PROXI <Param> = <值>`（SWC 式，41 列實測）；VF230 之 `PROXI $Param$ is set to "值"`（152 列）為同義舊式，不回修，新產出不採」。IN §8.7.5 例句同步（`PROXI Vehicle_Line_Configuration = 124 (DT)`）。lint036 `RE_P_PROXI`：SWC 式合規、`is set to` 式 WARN（舊式）、其他 FAIL；`test_lint036.py` 對應。
B 版 R-G70 其餘 (a)(b)(c)(f)(g) 與 A 版同向，**保留**。

### 3　R-G72 之處置
回修形態條**保留 R-G72 之號**（已入台帳者不改號），條文以 B 版 (a)–(e) 為本體，**增 (f)**：「候選判準：lint 交付向 X 或 P 紅數 > 0 之交付本為候選，逐本由 Pei 圈選」（A 版 R-TM14 之判準）。A 版之「R-TM14」名稱**作廢**（避免與 time_management 之 R-TM 系列撞名），台帳加註指 R-G72。

### 4　A-GC 編號
`A-GC15` 以 mtime 先者（GC-06 之 canon_refs 未量測因果）保號；B 版之 A-GC15（Home 計數口徑）→ **A-GC17**、A-GC16（例示未查 HMI）→ **A-GC18**；`A-GC16` 為本包任務 1 之覆寫事件。ANOMALIES.md 逐條調整並加改號註。

### 5　GC-09 續行
依 1-3 之完成度，GC-09 §一 1～7 逐項：已由執行層 A 完成且方向正確者**驗收不重做**（附 sha）；方向錯者矯正；未做者做。**任務 4 候選表（`rtm14_candidates` 改名 `rg72_candidates_20260905.tsv`）與任務 3 pm_29 dry-run 須在 PROXI 矯正後才跑**（B notice §4 之指出正確）。
B 線若已出盤點包（1-4），其表**不用**，以本包候選表為準。

### 6　上繳 `up/20260905_GC-10.md`
首行 session 標識；1-1～1-4 量測全文；矯正 diff；GC-09 七項各「驗收／矯正／新做」；乾跑（`--out`，不寫表）證 R-G70(SWC)／R-G72(f) 在表；對照向；工具旗標逐次聲明（R-G50(d)）。

## 三、Pei 動作（本包後）
1. 若 `RULINGS.sha.tsv` 已被執行層 A 寫過（1-2）：本包後重生一次並 commit。
2. 圈選 `rg72_candidates_20260905.tsv`。
3. DR 取號送出。

## 四、未結 DR
同 GC-09。
