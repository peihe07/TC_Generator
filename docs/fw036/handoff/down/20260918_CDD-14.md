# 下放包 CDD-14 —— 收尾雜務（零 TC 內容變更）：R-DIAG31／R-DIAG32、DR 編號對齊、FB-DIAG-u

前件：`down/20260918_CDD-13_review.md`。執行層 session：沿用 CDD 線。

## §0　禁區
git 不動；`--gate`／`--regen` 不跑；**不改任何 TC 之 PC／Procedure／ER／test_item**；升級條件表自 §5 逐字複製。
**T0 先取號落條文，再做任何產出**（CDD-13 之 R-G23 失序不得重演）。

## §1　任務
- **T0**：`R-DIAG31`／`R-DIAG32` 取號（`git grep HEAD` 零命中）→ 落 `RULINGS.md` → 指紋依制（R-G75 三數）。
- **T1**：`data/cs_uds_rules.md` 首段加引用鏈（CFTS004 → CS.00099 §6.1-4／§9.3.3-5 → CS.00100），附 Sys-RA ID 與 CS.00099 列號。
- **T2**：lint `SID-WORD` 落 `lint036.py` `FEATURE_CHECKS`（diagnostics）＋ 產生器守門；對十一本跑，預期 0（A-DIAG68 已清）；自測 3 項（2 正 1 反）。
- **T3**：`DATA_REQUESTS.md` 對齊：DR-DIAG-1 CLOSED（記 Figure A-2／A-11 節點）、-10／-12 CLOSED（原件已登）、-11 MISSING、-4 改題、-6 附候選；`asset_request.md` 同步。分析層 CDD-13 之 9/10/11 編號作廢，以本表為準。
- **T4**：`-156`／`-237` 之 Remarks 是否記所依 Figure 與節點 —— 缺者補（Remarks 一行，不動其他欄；`RM-DIAG` 白名單若需第七式，比照 R-DIAG6(amend) 加並回報）。
- **T5**：`rd_feedback_20260918.md` 增 FB-DIAG-u（`$500B` VC-3 之 5 s override 逾時與 CS.00099 S3ECU 5000 ms 是否同一計時器）。
- **T6**：若 T4 動了 Remarks → merged v11／交付本 v05；否則不重建，明記「產出未變」。

## §2　預期
lint 十一本全 0；`SID-WORD` 0；TC 列數 565 不變；MANIFEST 不變。

## §5　升級條件
- 指紋／`--check`（既存者記「既存」）。
- `SID-WORD` 對現本報出 > 0 → 列出，**不改**，回報（表示 A-DIAG68 未清乾淨）。
- T4 發現 -156／-237 之取碼依據與 Annex A 節點不符 → 回報不改。

## §6　上繳
首段升級條件逐字；sha8 表；T2 自測；T3 DR 表全文；T4 結果；T5 條文；建議 commit ＋ pathspec。
