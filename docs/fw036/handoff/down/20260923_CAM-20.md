# 下放包 CAM-20 —— 落檔 ＋ B 本尾批 B05（B04b 15 ＋ `Warning Banners` ＋ `PAM Integration` ＋ `Camera Settings`，leaf 依 plan 實測，≤ 40）

指派執行層 session：延用。對應審閱：`down/20260923_CAM-19_review.md`（同發）。

---

## §0　禁區

同 CAM-14 §0。A 本不動。B 本組簿 dry-run 不在本包（尾批判過後另包）。

## §1　落檔

1. RDF-15（`PU0456` 按鈕欄 `<Nol>`）。
2. DECISIONS 6-67～6-70：章界切點准；`Aux Cameras` 子項取 SYS1；「來源只給圖」型不造文字不掛 DR（通則）；`X` 不消。
3. GCB-11：lint `X` 固定入口清單納入 profile §5.3 常數。

## §2　B05（尾批），ID 續 `NR1L-RVCHMI-158`

依 `b_plan.tsv` 實測 leaf：B04b（§34.9–§34.10，15）＋ `Warning Banners`（RVC+PAM §8.2–§8.4、§9.2）＋ `PAM Integration`（§6.x、§11.1、§8.8）＋ `Camera Settings`（HU §6.2）。若合計 > 40，依章節序切 B05a／B05b，家族不切開，本包只出 B05a 並回報。

要點：
- B04b：R1 Low 前提以散文（6-64）；§34.9.3 非 leaf 不出；§34.9.4～§34.9.9 名稱編輯流程連續生成。
- `Warning Banners`：文字依 SYS1 逐字（§8.2～§8.4 自載者）與 Pop Up List（PU 號入 reasoning）；查無 → DR-CAM-h（擴題形制）。
- `PAM Integration`：`CVPAM_Presence`（6-56）為配備旗標；PAM 之 CAN 訊號依 A 本記法，DBC 查無 → PENDING DR-CAM-f 型（回報訊號名）。
- `Camera Settings`（HU §6.2）：`ENTER_CAMERA_SETTINGS` ＋ HMI Settings List row 465～473 之 label（`*` 依 R-CAM5）。
- Test Set 各依 framework；R-CAM16(b)(c)／R-CAM18 同前。

## §3　上繳要求（`up/20260923_CAM-20.md`）

1. 落檔清單。
2. `b05_plan.tsv`（leaf 實數、切法）、產物、lint、selfcheck、PENDING；`coverage_b.tsv` 完結對帳（230 列口徑）。
3. R-CAM17 證據表；自報；未結。

## §4　升級條件

- B05a > 43。
- hop label 不可辨 > 3 列。
- `Warning Banners` 任一列文字於 SYS1 與 Pop Up List 皆查無且非「只給圖」型（PENDING 回報數）。
