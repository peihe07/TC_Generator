# 下放包 CAM-18 —— 落檔 ＋ B03（`Camera View Switching` 31 ＋ `Wireless Camera Pairing` 6）

指派執行層 session：延用。對應審閱：`down/20260923_CAM-17_review.md`（同發）。

---

## §0　禁區

同 CAM-14 §0。A 本不動。

## §1　落檔

1. **R-CAM16(c)**（同級錨，不消耗 live）：審閱 §一-1 文面。`coverage_b.tsv` 之 `-086`／`-091`／`-136`／`-139` 註改為該款文面（`cross-reference only`／`verbatim duplicate of §…`）。
2. GCB-10：DR 擴題通則提請全域。
3. DECISIONS 6-62～6-64。

## §2　B03：`Camera View Switching`（HU 28，31 leaf）＋ `Wireless Camera Pairing`（HU 33，6 leaf）＝ 37，ID 續 `NR1L-RVCHMI-099`

規則同 B02。要點：
- HU 28（`More Cams`／view 切換）：入口 `More Cams`／`More AUX` 逐字；view 之名稱（Backup／Cargo／AUX 1～n／SVC）逐字自 Description；配備旗標依 view 種類（`Digital_CHMSL_Camera_Prsnt`／`Auxiliary_Trailer_Camera`／`Surround_View_Camera`），Atl-Mi 缺者 `0` 具名。
- HU 33（無線相機配對）：`Enable Wireless Cameras` 拼法依 DECISIONS 6-61；QR／認證流程之彈窗 `PU0854`／`PU0857`／`PU0454` 已查得；hotspot 能力為 SYS1 硬體要求，不掛 PROXI。
- 逐字同句依 R-CAM16(c) 判；交叉引用句同。
- 兩組各自 Test Set；若合計 > 40 則只出 HU 28 並回報。

## §3　上繳要求（`up/20260923_CAM-18.md`）

1. 落檔清單；R-CAM16(c) sha8。
2. `b03_plan.tsv`、產物、lint、selfcheck、PENDING；`coverage_b.tsv` 兩組完結對帳。
3. R-CAM17 證據表；自報；未結。

## §4　升級條件

- B03 > 40。
- hop label 不可辨 > 3 列。
- 任一 view 之配備旗標六本皆查無且 SYS1 無替代條件。
