# 下放包 VS-SL-01 —— Settings 查找配方 dry-run（**事後補檔**）

> **事後補檔，補檔日 2026-09-05**（GC-01 審閱 R-G49，Pei 2026-09-05 准）。
> 本包原於 2026-09-01 以聊天交付，未入 `down/`。本檔之內容**全數回填自 repo 內留存**，
> 逐節標示來源；無留存者留白，**不重建**（IN §8.4.1）。
> 本檔不得被引為「2026-09-01 已落檔」之證。

回填來源：
- §0／§1 前四條：`features/vehicle_setting/RULINGS.md` L7357–L7442（R-VS84–R-VS88，執行層逐字轉錄，`{live}` 由其代入為 84–88；本檔還原為 `R-VS{live}` 原記法）
- §1 第五條、§2、§3：**無留存**
- §4：`features/vehicle_setting/ANOMALIES.md` A-VS166／A-VS168
- 上繳產物清單：`down/20260902_VS-SL-02.md` L4、`up/20260905_GC-01.md` 7-2 節

---

## §0　裁定（Pei 2026-09-01）

```text
R-VS{live}  市場變體一律 NAFTA
  Vehicle Settings 相關 feature（現行 vehicle_setting；後續 VF665 同）之
  設定項路徑、控件型別、選項文字、PROXI 前置，一律以 NAFTA 變體為準。
  (1) HMI Settings List `Settings` 分頁：同一設定多列時，取 Notes 含 NAFTA
      或無市場標記之列；Notes 標 EMEA／MASAH／MASPN／LATAM 等者不取。
  (2) 总控表 Atlantis 條件式含 Market_Area 分支者，取 NAFTA 分支之回傳值。
  (3) 顯示名取 `Brand-Specific Names` 之 Jeep / Chrysler / Ram / Dodge 欄
      （DT／HDCC27 = RAM）。
  (4) 既有 TC 之非 NAFTA 列（實測 VF230：`Traffic Sign Assist Offset -
      non-NAFTA Setting` 18 列）依本條應移除；**移除屬寫回動作**，
      於 dry-run 報告列出，待 Pei 於報告審閱時確認後執行。
```

## §1　分析層裁決

```text
R-VS{live+1}  `Always false` 之設定項不出負向
  总控表 Atlantis 為 `Always false` 之設定項，不新增「不顯示」之負向 TC ——
  需求本身未必存在該條，出負向即造需求（§8.4.2）。
  既有正向列保留其需求追溯不刪；其 PROXI 前置依本包 §2 任務 1 之來源優先序
  取自需求原文條件式，总控表對此類不提供任何條件。
  執行層仍於報告 §C 列出受影響之設定項與列數（登記，非裁定用）。
```

```text
R-VS{live+2}  PROXI 一律 `PROXI <Param> = <raw> (<label>)`
  無 `$`、無 `is set to`。與 canon §8.7.5 v3(c) 及 CFTS044 交付本（78 處）一致。
  VF230 之 `PROXI $Param$ is set to "label"` 152 處全數改寫（寫回時執行）。
```

```text
R-VS{live+3}  架構只取 Atlantis，車型先以 DT 為主
  总控表只讀主表 `FeatureSet(Gen4-5)` E 欄（Atlantis／DT）。
  `new HW (637)` 分頁與 PNet 各欄不展開。
```

```text
R-VS{live+4}  別名表
  執行層草擬 `data/settings_alias.tsv`，Pei 逐條認可後方綁入配方；
  查無者開 DR，不得以語意相近之名代入（同 R-13）。
```

`R-VS{live+5}`（本輪範圍：VF230 主體，BL／VC 併跑）—— **條文本體無留存**。
僅知其性質為作業範圍宣告，執行層未取號（RULINGS.md L7440–7442）。

## §2　執行層任務 —— **無留存**

僅可自 §1 反推其含「任務 1：PROXI 前置之來源優先序」（R-VS{live+1} 引之）。
其餘不重建。

## §3　—— **無留存**

## §4　參考輸出格式與引數（回填自 A-VS166／A-VS168）

- 逐層導覽路徑之參考輸出：`Press "Settings" → Select "<Category>" → Select "<Parent>" → Check "<Item>"`
- 包內所載三個數（**已由執行層更正，見 A-VS166**）：相異 `"X" customer setting` 名 **106**（實測 107）、non-NAFTA **18** 列（實測 19）、`OR_VALUE` **7**（實測 5）。
  審閱 `down/20260901_VS-SL-01_review.md` §1 記其成因為量測鍵未載。

---

## 對應上繳產物（由 `up/20260901_VS-SL-01.md` 補檔承載，執行層 GC-02 任務）

`features/vehicle_setting/reports/vf230_settings_dryrun.{md,tsv}`、
`features/bed_lowering/reports/bl_settings_dryrun.md`、
`features/vehicle_category/reports/vc_settings_dryrun.md`、
`features/vehicle_setting/data/settings_alias.tsv`、
`features/vehicle_setting/scripts/vs_sl01_dryrun.py`。
