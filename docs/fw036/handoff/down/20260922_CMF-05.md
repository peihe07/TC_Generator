# 下放包 CMF-05 — 車型欄填值 ＋ 車型拆題 ＋ DR-46 結案 ＋ 命名以實際值為準 → 候選本 v4

- 日期：2026-09-22
- 前置：`down/20260922_CMF-04_review.md`；Pei 2026-09-22 四答：(1) Toro PROXI 對；(2) 三台皆 0 照填；(3) 需求與實際 CAN／PROXI 命名本來就不同，**以實際的值當內容**；(4) alt-mi ＝ Atlantis（MID）。
- 產物落點：`features/comfort/sandbox/revise_0922b/`（候選本 v4 `…_ComfortHMI_20260922_v4.xlsx`）。**不入 `delivered/`、不 commit、不跑 `--gate`／`--regen`、不重跑 generator。**
- 開工前 `git status`：CMF-04 產物是否已提交，照列。

## 1. 裁定落帳（Pei 2026-09-22；條文逐字入 `RULINGS.md`，profile 同步）

| # | 條文 |
|---|---|
| R-C48(amend2) | Toro 之 PROXI（`forms/proxi/Toro`）為準：`Climate_Type=1 (Manual)`、`Climate_Panel_Type=6 (3 knobs)`、`Integrated_Climate_Touchscreen=1 (Present)`。帶 2.14 排除式 PC 之 TC，Y 欄＝0 |
| R-C63 | 三台皆 0 之 TC **照填出貨**，不加 Remarks、不移除；車型欄本身即為該資訊 |
| R-C64 | **作者側欄位（PC／ITD／Procedure／ER）之 CAN 訊號名、PROXI 參數名與值，一律取實際檔案之名與值**（DBC、`forms/proxi/`、LID 對照表），不取需求之 `$…$` 名。`test_item` 上半 verbatim 與 reasoning 保留需求原名（canon §8.7.5(b)）。Remarks 不另載對照。**本條於本線覆蓋 canon §8.7.5(g)**；ATC／MTC ↔ `Climate_Type`、ICS ↔ `Integrated_Climate_Touchscreen` 之對映據此採用，不另證 |
| R-C65 | 本交付之平台 ＝ **Atlantis（MID）**；LID → CAN 對映取 `forms/Logical Identifiers and CAN Mapping v1_78.xlsx` 該欄 |
| R-C62(amend) | CMF-04 §3 停點一（> 40）由 Pei 「需要拆題」一語解除；上限改 **120**，實產仍逐條人工複判 |

## 2. 車型欄（依 `vm_assign.tsv`，全列）

- T＝0、U＝0、W＝0、X＝0；V／Y／Z 依 `vm_assign.tsv`（含 42 條 `test-setup 蘊含` 分支照填；65 條全 0 照填）。
- 新拆之 sibling 各標所屬台。
- 填後彙總：三台各自 `1` 條數、全 0 條數（預期 65 ＋ 拆題後之變動）。

## 3. 車型拆題（`vm_split_plan.tsv` 71 條，逐條人工複判）

- 判準：該 TC 之觸發或 ER **確實**隨該軸之 PROXI 值改變（不只是 body 出現字樣）。只有 PC 已宣告該軸者不拆（改車型欄即可）。
- 拆出者：每台一組值一條，PC 寫該台之配置條件（用 PROXI 實際 label，R-C64，例 `Climate_Type = 2 (Automatic)`），下半兩兩相異，Priority 依 R-C47，ID 接尾自 `-477`。原條改為其中一台或瘦身，不得留下三台皆 0 且無內容之殼。
- 同時涉兩軸者拆積，實產數與 plan 之差逐條說明。
- **停點**：實產 > 120。

## 4. DR-46 結案（R-C64／R-C65）

1. 先證三台 DBC（Promaster／Toro／Fastback 各自檔）逐字含 `STATUS_CLIMATE2.HVACRecirc_Sts` 與 `STATUS_CLIMATE2.HVACRearDef_Sts`，附 `SG_`／`VAL_` 行逐字（`grep -a`）。
2. 證得 → 四條之 `PENDING: DR-46` 行改：
   - `006-02`／`012-06`：`Send CAN: STATUS_CLIMATE2.HVACRecirc_Sts = 2 (BLINK)`（label 逐字取 `VAL_`）
   - `013-02`／`114-02`：`Send CAN: STATUS_CLIMATE2.HVACRearDef_Sts = 2 (Blink)`（label 逐字取 `VAL_`，若三台 DBC label 大小寫不一即停）
   ER 依 canon §8.7.5(d)。DR-46 標 CLOSED，載出處。
3. `HVACRearDef_Sts` 於任一台 DBC 查無 → 該兩條維持 PENDING，報 Pei。
4. `$Rear_HVAC_cfg$ = [Present]` 之 5 條 PC → `PROXI Rear_Climate = 1 (Present)`（canon §8.7.5(e)、R-C64）；上半 verbatim 不動。
5. 全本 grep 作者側四欄之 `$…$`（R-C64 之殘留），逐條列出並依同法改；查無實際名者維持 `$…$` 並列表待 Pei。

## 5. 併入

R-C60 之既有 2 處合稱引號拿掉。

## 6. 候選本 v4

母本仍為 0907 本複本；invariant 與逐格對帳同前（對 0907 本、對 v3 皆全數歸因）；lint 兩層 `LINT_STATUS: OK`，目標 U＝0。

## 7. 上繳首段

停點、實產拆題數、TC 總數、三台 `1` 條數與全 0 條數、PENDING 行數、`$…$` 殘留數、對 v3 差異格數。附 R-C64 改寫清單、DBC 證據、產物 sha256、ENTRY。
