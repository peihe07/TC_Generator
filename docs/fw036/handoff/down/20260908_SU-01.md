# 下放包 SU-01 —— SW Update：可獨立改善之部分（不待上游）

Pei 指示（2026-09-08）：「朝著可以改寫的更好的部分去做」。

對象：`10_Reviewing/00_TestCase/ASW-R2/SW Update via USB/FM-WI-FSM-036-A01 … _SWUpdate_20260830.xlsx`
sha16 `d0000d1d96e66f0c`；分頁 `Test Case Specification 測試用例規範` row 10–328，**319 資料列**。
037：`REF/SoftwareUpdate_FM-WI-FSM-037-A03_STLA_Report_SWRA.xlsx`，分頁 `AnalysisReport_FULL`，
`SWE1-FOTA-*` **383 leaf**（Functional 307／Heading 45／Information 25／Non-Functional 4／
Out of scope 1／空 1）。

---

## 〇、先排除一件事：本線**不是** CFTS044 那個錯

CFTS044 之病因是「037 已寫行為而分析層未讀」。本線**已讀** —— 且讀對了。
分析層本輪重驗 037 `Verification Criteria`（Q 欄）：

- **非空 310／383**，`not clear` **0 筆**
- 其語式為 `Examine …flow` / `Monitor …behavior` / `Inspect …handling` / `Analyze …processing`
  —— **指的是內部流程，不是外部可觀測面**

例（`SWE1-FOTA-067`）：
`Examine software download availability detection flow during OTA update processing. /
Monitor vehicle power mode transitions and identify handling of the next IGN_OFF event. …`
`Verification Method: Unit Test / Integration Test / System Test`

**`DATA_REQUESTS.md` 對 DR-SU2 之立論成立** ——「文件說要做系統測，卻沒說系統測時要看哪裡」。
故 195 列之 `PENDING` **不是可以靠補讀來源解掉的**，與 CFTS044 性質相反。
**本包不碰任何 PENDING，不撤任何 DR。**

---

## 一、本包之範圍 —— 三類，皆不依賴上游

| 類 | 列數 | 性質 |
|---|---:|---|
| **A. 追溯欄疑義** | 106 列 | 須先查證再決定改法（§二）|
| **B. Final Step 過長** | 41 列 | 純格式，可直接改（§三）|
| **C. 引號** | 1 列 2 處 | 機械錯（§四）|

**先做 B、C（§三、§四），A 之查證與其處置另包**（§二末）。

---

## 二、A —— 追溯欄（D 欄 `Requirement or Design ID`）之疑義

### 2.1 兩項純 ID 集合之量測（無模糊比對，可完全複算）

以 `SWE1-FOTA-{0 補足三位}` 正規化後取集合差：

```
037 Categorization = Functional Requirement          307 leaf
工作簿 D 欄相異值                                     319
  ├ 落在 Functional 內                               260
  └ 落在 Functional 外                                59
Functional 而工作簿無對應列                            47
```

**59 列之分佈**：Heading **34**／Information **19**／Non Functional Requirement **4**／
Out of scope **1**／Categorization 空白 **1**。

### 2.2 Heading 列之內容不相容（三例，逐字）

| 工作簿列 | D 欄宣告 | 037 該 leaf 之 `Requirement Description`（逐字）| 該列 `test_item` 上半（逐字節錄）|
|---|---|---|---|
| 17 | `SWE1-FOTA-072` | `OTA Client Architecture` | `The HMI shall navigate to the software download via Wi-Fi screen when the user selects the Wi-Fi software download entry…` |
| 18 | `SWE1-FOTA-073` | `Operating Environment` | `If a previously configured Wi-Fi network is available during Body OFF mode, the WiFi Update Service shall request WifiMa…` |
| 19 | `SWE1-FOTA-074` | `Over The Air (OTA) Deployment of Software` | `During automatic FOTA download resume using a previously configured Wi-Fi network, the WiFi Update Service shall suppres…` |

**Heading 之 Description 是章節標題，`test_item` 上半是行為條文** ——
二者不可能是同一個需求單元。**故該 59 列之 D 欄值至少有一部分是錯的。**

### 2.3 ⚠ 分析層**不主張**這是系統性偏移 —— 該假說證據不足

分析層曾以 `test_item` 上半對 037 D 欄做模糊比對，得「75 列宣告錯、另有相似度 ≥0.85 之他列」。
**此結果不可採信**，兩個理由：

1. `test_item` 上半之 verbatim 來源是 **CFTS057**（N 欄為 `CFTS057-{7 位}`），
   037 D 欄是 SWE.1 之改寫本 —— **拿兩份不同文件比對，相似度不構成同一性證據**。
2. `DATA_REQUESTS.md` 之 **DR-SU3** 已載 037 內部有大量**重複表述**
   （`274`/`275`/`276`/`347`/`348` 五列一組等）。實測亦見 row 107／108／109 三列
   同時以 1.0 相似度命中 `SWE1-FOTA-130` —— **037 自身之近似重複使「最佳匹配」不唯一**。

**故本節只提出 §2.1／§2.2 之集合事實與內容不相容，不提出對應表，亦不改任何 D 欄值。**

### 2.4 A 類之處置 —— 先查證，不在本包施作

**查證所需之對照鏈缺一環**：037 之 `Source Requirement ID`（B 欄）實測為
`SYS-RA-FOTA-{nnn}` 格式（如 `SWE1-FOTA-001` → `SYS-RA-FOTA-001`），
**不含 CFTS ObjectID**；而工作簿 N 欄為 `CFTS057-{7 位}`。
`features/sw_update/data/recon_leaf_to_section.tsv` 實測**只有表頭一行、無資料**。
即 repo 內**現無 CFTS057 ObjectID ↔ SWE1-FOTA ID 之對照**。

**下一包（SU-02）之工項，本包不做**：

1. 自 `inputs/SYS3_CFTS_057_…SYSAD_V03_06042026_V3.0.docx` 與
   `inputs/R1LR_Atl-H_25PI4.5 … CFTS_57 Reflash_20251202-2111.docx` 抽出
   ObjectID ↔ 條文之對照，落 `data/cfts057_objectid_map.tsv`。
2. 以該表逐列判定 D 欄之正誤，出報告 `reports/swu_reqid_audit.tsv`
   （欄：工作簿列／D 欄宣告／N 欄 ObjectID／CFTS 條文首 80 字／037 對應 leaf／判定）。
3. **只出報告，不改儲存格** —— 改法待報告出來後由 Pei 裁。

⚠ 47 列未覆蓋之 Functional leaf 是否為真缺口，**取決於 A 之查證結果**：
若 D 欄有錯位，其中一部分可能其實已被生成而只是掛錯號。
**在 SU-02 之前不得將 47 列陳述為覆蓋缺口。**

---

## 三、B —— Final Step 過長（41 列）

判準：`test_procedure` 最末一個編號步驟，去除 `N. ` 序號後 `str.split()` 之詞數 > 18
（IN §5.2B；算法同 `R-POP20` 之訂正版，`(sec)` 類括號計為一詞）。

**41 列 = 不含 PENDING 之 34 列 ＋ 含 PENDING 之 7 列**（後者之過長步驟本身非 PENDING 行）。

改法（同 R-POP20）：**Final Step 只留「動作 ＋ check that ＋ 主要可觀察結果」；
時限、條件子句、對照基準等細節全數留於 ER —— ER 已載，不損資訊。**

範例（row 10，27 詞 → 13 詞）：

```
現行  5. Check that the download progress shown on the head unit does not advance
         after the internet access is restored and before the next Ignition ON event
改為  5. Check that the download progress shown on the head unit does not advance
ER 5  不動（現行已完整承載「after the internet access is restored and before the
      next Ignition ON event」）
```

範例（row 30，27 詞 → 14 詞）：

```
現行  4. Check that the recorded screen content shows the critical update being installed
         and shows no regular update being installed while the critical update session is pending
改為  4. Check that the recorded screen content shows the critical update being installed
ER 4  不動
```

**逐列施作要求**：

- 只動 `test_procedure` 之最末步驟，**ER 一律不動**；若某列之 ER 未承載被移除之細節，
  **該列跳過並回報**（不得為了縮短而丟失驗證條件）。
- 縮短後仍須含 `check that`（IN §5.5）且 ≥ 1 個可觀察標的。
- 縮短後之詞數逐列記入上繳。

**41 列清單**（工作簿列號，括號為現行詞數）：
不含 PENDING —— 10(27) 11(22) 13(19) 14(21) 22(25) 25(20) 30(27) 35(19) 及其餘 26 列；
含 PENDING —— 110(25) 145(19) 148(27) 157(22) 270(24) 297(19) 307(19)。
**執行層以上述判準現算全簿取清單，不採本包之列舉**（本包列舉為抽樣，非全集）。

---

## 四、C —— 引號（row 200，2 處）

IN §11：UI 標籤用雙引號，禁單引號。

```
proc 2. Select the 'Accept' option on the TBM FOTA update notification
er   2. The 'Accept' option is selected on the TBM FOTA update notification
```

`'Accept'` → `"Accept"`，兩處。

⚠ 執行層先確認 `Accept` 為 HMI 來源之標籤（`inputs/SYS1_HMI_Software_Updates_FOTA_…xlsx`
或 `Pop Up List`）；**查無即只改引號形制，不改字串**，並於上繳具名。

**不在本包**：row 72／73 之 `[FOTA FailureRollback Successful]` —— 該括號為
037／CFTS 逐字之值標籤，屬 IN §11 之 `Exception (profile-scoped)`（同 VS-CF-03 §9-2 之判），
**不改**。

---

## 五、經查證後**不改**之項（併記，免下輪重查）

| 項 | 實測 | 判 |
|---|---|---|
| 尾句號、行首尾空白、全形標點、交付欄中文 | 各 **0** | 已合規 |
| Procedure ↔ ER 1:1 | 不等 **0** | 已合規 |
| 括號下半（R-S4）| 缺 **0**；跨 319 列**無任何重複** | 已合規 |
| Priority 值域 | P1 165／P2 135／P3 19，**越域 0** | 已合規 |
| `design_method` | 7 種，皆在 IN §12 內 | 已合規 |
| `input_test_data` | 全 `NA` | 已合規 |
| `specification_reference` | 空 **0**，全為 `CFTS057-{7 位}` | 已合規 |
| ER 情態動詞（shall/will/should/would）| **0** | 已合規 |
| Procedure 禁用主動詞（Verify/Observe…）| **0** | 已合規 |
| `pc`+`proc`+`er` 三欄全同之重複列 | 群組 **0** | 無重複 |
| 單步 TC | 5 列（162／174／224／262／319）| **不改** —— 其唯一步驟即 PENDING（DR-SU6／SU3／SU2 之能力型需求），補步驟即造值 |

---

## 六、上繳 `up/20260908_SU-01.md` 須含

1. 來源本與產出本之 sha256；zip member 差異與 dv 計數（`surgical_save`，母本不改，R-G72）。
2. §三之 41 列現算清單與逐列改前／改後詞數；跳過之列與其理由。
3. §四之 `Accept` 標籤查證結果。
4. 全簿複驗：§五各項數字須不變；`PENDING` 195 → **195**（本包不動）；
   DR 引用數不變（DR-SU2 554／SU6 52／SU3 37／SU7 34／SU4 27／SU1 5／SU5 3）。
5. 工具旗標逐次聲明；自報。

## 七、不在本包

- A 類之查證與 D 欄改動（→ SU-02，§2.4）
- 195 列 PENDING、7 個 DR —— 皆待上游，本包不碰
- 47 列未覆蓋之 Functional leaf —— 其真偽待 A 之查證
- 交付（封面補齊、`delivered/`）—— 出貨 gate 未過（PENDING 195），不議
