# 下放包 CAM-11 審閱（分析層 2026-09-23）

對應上繳包：`docs/fw036/handoff/up/20260923_CAM-11.md`
判定：**落檔與 batch03a 補列通過；batch03b 29 列判過（第 6 批），兩處退回修正後結案**。CAM-12 同發。

---

## §一　裁決

| # | 項 | 裁決 |
|---|---|---|
| 1 | 「PENDING ＋ lint `P`」調和形制（賦值行 `= PENDING (<label>)` ＋ `a. PENDING: DR-… 說明`） | **入 profile §7.5** |
| 2 | `Steering_Ratio_Rack_Pinion_Type` 於 PROXI 查無（`-164`） | 先以同義字（`Steering`／`Rack`／`Pinion`／`Ratio`）重掃 Atl-Hi 三本；仍查無則**併入 DR-CAM-n**，Pre-Condition 該行 `PENDING: DR-CAM-n` |
| 3 | 8b 擴用至六列 | **准** |
| 4 | `-138` 查得 `CAN node 27 (ASM/ASCM)` 為 PROXI 參數名 | 確認；此例證明「以來源措辭逐字掃 PROXI 表」應為 §7.2 之第一步 |
| 5 | `-166` 否定側只判「不為 Type_1」 | 接受（來源只列一項） |

## §二　抽讀（152／161／170）之退回

| # | TC | 問題 | 修正 |
|---|---|---|---|
| 1 | **`-152`／`-153`** | `STEERING1` 查無之掃描以**訊息名** `STEERING` 為字串，違反 profile §7.2「先以**訊號名**自身查」 | 以 `LwsAngle_SCCM`（及 `LwsAngle`）重掃 `PDT27_E2A_R1_FDCAN8.dbc`／`BHCAN2.dbc`；查得則改訊息名並寫 raw／factor（同 `-154` 之 `EPS_FD_1` 型），reasoning 註來源訊息名與 DBC 不同（R-13 保留 verbatim）；仍查無才 PENDING DR-CAM-f。`-175`／`-176`／`-178` 同法以訊號名重掃 P363／637MCA |
| 2 | **`-170`** | `a)` 子句逐字條件為 `STATUS_CCAN3.VehicleSpeedVSOSig is missing`，procedure 卻停送 `STATUS_CCAN5`（執行層判其誤植而改條件） | §8.1／§8.4.1：TC 依來源逐字，不得以判讀代換觸發。procedure 改停送 `STATUS_CCAN3`；reasoning 保留「疑為誤植，RDF-08」；工作簿 Remarks 註 `Condition per source text; suspected typo, see RDF-08`。`STATUS_CCAN5` 缺失 → SNA 之行為**無逐字來源**，不生成，待 RDF-08 回覆 |

其餘：`-161` 兩層 SNA 之具名正確。

## §三　備查

- 升級條件 2 之五列 PENDING 於 §二-1 重掃後回報實數。
- R-G14 第 6 批。
