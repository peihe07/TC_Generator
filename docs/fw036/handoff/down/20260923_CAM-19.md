# 下放包 CAM-19 —— 落檔 ＋ B04（`AUX Camera Settings`，leaf 依 plan 實測，≤ 40）

指派執行層 session：延用。對應審閱：`down/20260923_CAM-18_review.md`（同發）。

---

## §0　禁區

同 CAM-14 §0。A 本不動。

## §1　落檔

1. profile §5.1 加註：`J` 豁免要件為「上半自來源 `Description` 首 token 起」；§4.3.1 註：摘句留孤懸標點且全文 ≤ 50 token 者取全文。
2. `popup_texts.tsv` note 記 `PU1518`／`PU1519` 分辨。
3. DECISIONS 6-65～6-66。

## §2　B04：`AUX Camera Settings`（HU 29／30／31／34.9／34.10；全列 53，leaf 由 `b_plan.tsv` 實測），ID 續 `NR1L-RVCHMI-128`

- 若 leaf > 40，依章節序切 B04a／B04b（家族不切開，≤ 43），本包只出 B04a。
- 設定 hop：`ENTER_CAMERA_SETTINGS` → `Select "Aux Cameras"`（HMI Settings List row 474／475）→ 子項 label 逐字（row 476 起，回報列號）；`*` 標記者依 R-CAM5；設定值 `Set "<label>" = "<Option>"`。
- 相機名稱編輯／刪除／排序等之彈窗依 Pop Up List（`PU085x` 系列已知），查無 → DR-CAM-h。
- §34.9／§34.10（R1 Low 側設定）與 §29～§31（R1 High）之同句依 R-CAM16(c)／父題判。
- R-CAM18 配備旗標同前。

## §3　上繳要求（`up/20260923_CAM-19.md`）

1. 落檔清單。
2. `b04_plan.tsv`（含 leaf 實數與切法）、產物、lint、selfcheck、PENDING、`coverage_b.tsv`。
3. R-CAM17 證據表；自報；未結。

## §4　升級條件

- B04a > 43。
- hop label 不可辨 > 3 列。
- HMI Settings List 之 `Aux Cameras` 子項與 SYS1 §29～§31 之設定名對不上 > 3 項（回報對照表，勿造）。
