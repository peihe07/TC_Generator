# SEC-13 審閱 —— 上繳包 `up/20260917_SEC-13.md`（`security_v08` ＋ `EXEC_GUIDE.md` §1.6）

審閱層：分析層，2026-09-17。
判定：**通過，不升級**。三項升級條件不成立；兩項須裁如下；一項迴歸採納修法；一處指南殘留須修。

---

## 一、裁定

| # | 項 | 裁 |
|---|---|---|
| 1 | A-33「造值」判定 vs 實測（檔名有 CCVR STEPS 來源）| **A-33 撤回「造值」定性，改為 A-33′：分析層未查證即定性**（SEC-01_E 常數之檔名確可溯 CCVR STEPS item 2／5）。**R-SEC24 維持**：目錄裡實際存在的檔案是物理證據，STEPS 文字是作者手誤的機率高；兩名並列於指南 §1.6 已足。CCVR 自身不一致登 **A-SEC-15**，隨 A-SEC 系列回饋 Steven 確認交付檔名。若 RD 答以 STEPS 為準，再 Revise 19 格 |
| 2 | §2 三行刪除／改寫 | **准**。`10 03` 無在庫來源即刪，正確；`Z1 IntegrationTests/README.md` 不存在→改引 `PythonTests/README.md`，正確；`SecurityAssets` 行與 §1.6 自述矛盾→刪，正確 |
| 3 | `gen_batch2.py` 範圍斷言迴歸 | **准修法**（排除集合加 `CONVERT_DOC`）；成因具名（改欄未查全部讀者）。登執行層側 ledger；分析層側登 **A-35**：SEC-11 §2 令改 `disposition` 時未列「讀此欄之全部腳本」為必查項 |
| 4 | `installstate` `0x00` 出處、符號名非檔內常數 | 執行層處置正確（依實測分行、不偽稱常數）；分析層 A-36：SEC-13 §2 之行號與符號名未查即寫 |
| 5 | git 不自動 commit（`CLAUDE.md`）| 正確；分析層下放包之「先落 commit」措辭改為「請 Pei 落 commit 後開工」（本檔起）|

## 二、指南殘留（下包修）
§1.1 引言區塊仍寫 `The APK file name appears in the workbook only for \`CertProviderServiceManagerTest.apk\``——Pre-Condition 已改為 `CertProviderAndroidInstrumentalTest.apk`，該句為生成腳本內寫死字串，未隨 R-SEC24 更新。須改為自 Pre-Condition 抽取。

## 三、通過項
R-SEC24 落檔 35 錨點；19 格全 `pre`；`batch2_v08` 與 v04／v05 逐字節相同為旁證；§1.6 十行皆附檔＋行號（含 Z1 `adb_wrapper.py` L41–42、`keys_install_helper.py` L204–216、VHAL Guide §2.4）；兩驗證 100%；`X-f-2` 註更新。

## 四、分析層自報
A-33′（改定性）、A-35、A-36。

## 五、狀態與下一包
Security 文字版**定稿等資產**（124 TC、70/70、佔位 96／11 token、lint 全 0、`W=4` 待 Pei）。
SEC-14（本檔同時下放）：`gen_batch2.py` 修法 ＋ 指南 §1.1 殘留 ＋ A-SEC-15 落檔；**無 TC 變更、不升版**。
之後 Security 側無包；GC-16（`RULINGS.sha.tsv` 全 repo 重生）待 CDD-02 pilot01 上繳後以單一 session 出。

git（Pei）：SEC-12／13 四支照上繳包 §七；SEC-14 產物再一支 `chore(security)`。
