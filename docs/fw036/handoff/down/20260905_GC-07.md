# 下放包 GC-07 —— canon 修訂：訊號寫法來源層級＋UI 導航路徑明確化（2026-09-05）

Pei 指示（2026-09-05，兩則）：
1. 「測試訊號相關都要明確，像 SWC 這份一樣的寫法；一般手動測項要像 Vehicle Settings 和 Core HMI 的寫法，明確到知道點到哪一層、進哪個 app、設定的哪個選項，這裡需要 HMI 的協助。」
2. 「主要需求是 CFTS；訊號相關參考 VF 文件以及 PROXI／DBC；畫面有關都是 HMI。」

分析層依「格式類裁定先窮舉既有交付本」（台帳 09 §五）先量測，再立條文。本包含一項須 Pei 裁之衝突（§二），其餘准後即施作。

---

## §一　量測（機器讀取，工作簿 `Test Case Specification*` 分頁，header row 9，`No.#` 非空為資料列）

| 本 | 路徑 | sha256 前 12 | 資料列 | `Send CAN:` | `$MSG.Sig$`（v3 式） | `PROXI` | `Send the signal`（v3 式） |
|---|---|---|---|---|---|---|---|
| SWC（Pei 交付，AACP done） | `docs/Report/compare/06_SWC/…_SWC_20260628(done_AACPonly).xlsx` | `7dd8f1737c20` | 145 | **52** | **0** | 41（`PROXI Param = value`，不加 `$`）| 0 |
| VF230（vehicle_setting 基準本） | `features/vehicle_setting/inputs/…_VF230_20260819.xlsx` | `adee5dc462ae` | 457 | **184** | **0** | 152（`PROXI $Param$ is set to "value"`）| 0 |
| Home（Core HMI 類，Pei 交付） | `archive/forms_superseded/…_Home_20260809.xlsx` | `1895fb2a2b44` | 216 | 0 | 0 | 0 | 0 |
| PM 現行工作本 | `features/power/sandbox/b29/pm_29.xlsx` | `35305835796a` | 390 | **0** | **606（207 列）** | 85 | 114 |

補充量測：
- SWC 本之 `$…$` 出現 215 次，全部為 **設定／參數** 包覆（例 `$SteeringWheelButtonGroups$`、`$StWhl_VoiceRecognition$`），**無一** 為 `$MESSAGE.Signal$` 式。
- Home 本 Procedure 首詞：`Press`/`Press the` 158、`Select`/`Select the` 135、`Navigate to` 65、`Menu Bar` 10 次；UI 標籤一律 `"..."`。
- VF230 本 Procedure 首詞：`Read the` 457、`Open the` 365、`Set the` 204、`Send CAN` 184、`Power cycle` 155。**365 次 `Open the Vehicle Settings menu and wait until it is fully rendered`** —— 未寫入口（Menu Bar 何處、哪個 app、哪一層 tab），此即 Pei 所指之不明確處。
- 台帳現查：`### R-G` 錨最高號 **R-G69**；GC-01～GC-06 下放包未新立 R-G（GC-06 補遺明載不立新號）。本包擬立 **R-G70、R-G71**；執行層落檔前再做一次現查（R-G23），有碰撞即順延並於上繳回報。
- `CR30580/30581 參考本`（R-1 v3 之依據）：以 `**/*CR3058*` 全庫搜尋 **查無**。依 R-G13，「查無」只表示不在庫中，非該本不存在；但 v3 之依據目前 **不可複驗**。

---

## §二　衝突：§8.7.5 v3 與 Pei 語料相反 —— **須 Pei 裁**

事實鏈：
1. R-1 v1 撤銷時，Pei 裁「都是照我 SWC 怎麼樣寫就這樣寫」，執行層量得 SWC 0708 之 `Send CAN:` 546 次／273 列 → 立 v2。
2. 下放包 12 依 CR30580/30581 參考本立 v3（`$MESSAGE.Signal$`、`Send the signal`、PROXI 不加 `$`），取代 v2。**12 包未重新量測 SWC 本**。
3. 本次三本 Pei 語料（SWC／VF230／Home，818 列）v3 式出現 **0 次**；v2 式 236 次。唯一採 v3 者為分析層產出之 pm_29。
4. Pei 今日再裁「像 SWC 這份一樣的寫法」。

分析層判斷：v3 係以一本庫外參考本推翻 Pei 本人交付語料，違反 09 §五「語料為權威」。**建議回歸 v2 式**，並將 vehicle_setting 之 `[OVERRIDE §8.7.5]`（R-VS52／R-VS67）升為全域預設，該 override 隨之作廢（內容併入本體）。

擬立 **R-G70（訊號與參數寫法 v4 —— 來源層級與格式，全域）**：

```text
(a) 來源層級（Pei 2026-09-05）：
    需求主體 = CFTS。訊號之名稱、raw 值、VAL_ label = VF 文件（SWRA）＋
    forms/ 之 PROXI 表與 DBC（R-17 DBC 為主、R-13 查無保留原文）。
    畫面之入口、層級、選項標籤 = HMI Logic and Flow（R-G71）。
    三者互相不得代位：CFTS 原句不得當訊號值來源；DBC 不得當畫面標籤來源。
(b) CAN 送出：`Send CAN: <MESSAGE>.<Signal> = <raw> (<label>)`，label 逐字取 DBC VAL_（R-7）。
(c) CAN 觀察（ER）：`<MESSAGE>.<Signal> = <raw> (<label>) is sent <時機>`；
    Procedure 讀取：`Read <MESSAGE>.<Signal> and check that it is <raw> (<label>)`（R-11(b)）。
(d) PROXI：`PROXI $<Param>$ is set to "<值>"`（VF230 152 列式）。
    SWC 本之 `PROXI <Param> = <值>`（41 列）為同義舊式，新產出一律採 (d)。
(e) 設定／VHAL／內部參數：`$<Name>$`，`$` 為參數標記（SWC 215 次實測），
    DBC 查無對應者保留來源名並依 (c) 寫應觀察之值（原 v3(d) 不變）。
(f) Hold／baseline：原 v3(e)(f) 不變。
(g) 規格訊號名與 DBC 不符：R-13、R-17 不變。
沿革：v3 撤銷（R-TM13 加註保留），理由見 GC-07 §二。
lint P 判準：改回 `Send CAN:` 為全域預設，`--profile` 之 v3 分支移除。
```

**Pei 三選一**：
- **甲**：准 R-G70 如上（v2 回歸）。pm_29 之 207 列於 PM 站 ⑤ 寫回時一併轉換（尚未出貨，不涉 R-TM13）；轉換為機械替換，執行層出對照表供 ④ 抽驗。
- **乙**：維持 v3，Pei 提供 CR30580/30581 參考本入庫 `forms/` 並登 FORMS.md，分析層重量測後再議。此選項下 SWC／VF230 既有交付本不回修（R-TM13），新產出採 v3，形成兩式並存。
- **丙**：Pei 另裁。

未裁前，§三、§四 之 UI 導航條文獨立於本衝突，可先行。

---

## §三　新增 §5.8 UI 導航路徑明確化 —— 擬立 **R-G71（全域）**

```text
§5.8 UI Navigation Path (R-G71)
(a) Any step that reaches a screen, app, tab, page, or setting MUST state the
    full path from a fixed entry point, one hop per step or one hop per
    " > " segment inside a single step, every hop label in "...":
      Press "Vehicle" on Menu Bar
      Select "Settings" tab
      Select "Aux Switches"
    Single-step form (when hops are pure navigation with no ER of their own):
      Navigate to "Vehicle" > "Settings" > "Aux Switches"
    Fixed entry points (closed list, extend via §5.3 constants only):
      Menu Bar / App Drawer / Home Screen / Status Bar / H/K "<button>" / Dealer Mode / Eng Mode
(b) Forbidden: `Open the Vehicle Settings menu`, `Go to settings`, `Navigate to
    the page` — any step whose target cannot be located on-screen from the
    step text alone.
(c) Source of path = the feature's HMI Logic and Flow document (spec-index/cache/
    SYS1_HMI_*_HMI_Logic_and_Flow_*.xlsx) or the HMI Settings List
    (forms/HMI Settings List R1 SR25 …). CFTS / VF / DBC are NOT path sources.
    Path hops are quoted verbatim from the HMI source (label text as written;
    R-6 case rule applies).
(d) Path not found in any HMI source → `PENDING: DR-{n} HMI entry path
    <target>`; never guess a hop. DR type "HMI entry path" is registered in the
    feature's DATA_REQUESTS.md (precedent: DR-PW25).
(e) Setting-value hops use the HMI Settings List option label:
    Set "SWITCH 3 Hold Last State" = "Disable"
    (not `to Disable` unquoted; the value is a UI label per §11).
(f) ER for a navigation hop = the reached screen is displayed, named by its HMI
    title: `The "Aux Switches" screen is displayed`.
```

§5.3 增列常數（執行層自 Home／VF230／HMI Settings List 抽取實際字串後填入，**本包不預填**，避免分析層造字串）：
`ENTER_VEHICLE_SETTINGS`、`ENTER_SETTINGS_APP`、`ENTER_HOME_SCREEN` 三條，每條附來源檔＋分頁＋列號。

§9 自檢增第 18 條：`Navigation path complete from fixed entry point, hops quoted from HMI source, no PENDING left unregistered (§5.8)`。

§8.7.5 開頭增一行指回 R-G70(a) 來源層級，使訊號與畫面兩條線各有唯一來源。

---

## §四　執行層任務

1. **量測複核**（先於一切改動）：以 `scripts/` 現有工具重跑 §一四本之計數，數字不符者以執行層量值為準並回報差異（R-G69 理由與數字分別驗證）。
2. **canon 施作（§三，不待 §二裁）**：
   - `docs/runtime/ASPICE_SWE6_AI_Instruction.md` 新增 §5.8、§9 第 18 條、§5.3 三常數（含來源列號）。
   - 台帳 `docs/fw036/RULINGS_LEDGER.md` 落 `### R-G71 —` 錨與條文全文；「條文落檔位置」表加列。
   - `rulings_hash.py`／`canon_refs.py` 重跑，錨可解析。
3. **lint 新檢查 Q（導航路徑）**：Procedure 含 `menu`／`page`／`screen`／`settings`／`tab` 而同 TC 內無固定入口（§5.8(a) 閉合清單）者報 WARN；報告落 `docs/runtime/lint/`（R-G 報告落點規則）。先跑 VF230／BL／VC／PM 四本出基線計數，**只報不改**。
4. **§二 待裁項**：Pei 裁甲後才動 —— §8.7.5 改 v4、台帳 v3 撤銷加註、lint P 判準回 `Send CAN:`、vehicle_setting profile 之 `[OVERRIDE §8.7.5]` 標記作廢併入、pm_29 207 列轉換腳本＋對照表（dry-run 先出計數）。
5. 上繳附：未結 DR 清單（含本包新型 DR「HMI entry path」之登記格式範例一條）、R-G70/71 落檔前之現查號、lint Q 基線四本計數。

## §五　本包不做

- 不回修任何已交付本（R-TM13）。
- 不預填 §5.3 常數字串、不代擬任何 HMI 路徑（§8.4.1）。
- 不在 §二未裁前動 §8.7.5 本體。
