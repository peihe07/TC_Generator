# 下放包 CDD-06 —— Revise（R-DIAG17/18）＋ framework LOCK v01 ＋ batch 4（Layer 2 #5／#18／#19／#20，48 列）

前件：`down/20260917_CDD-05.md`、`up/20260917_CDD-05.md`、`down/20260917_CDD-05_review.md`。
依常規：審閱後即發；依據 = CDD-05 審閱 §三／§四。執行層 session：沿用 CDD 線。
指紋制：GC-16 未 commit → R-G75；已 commit → R-G76(b)。上繳註明。

---

## §0　禁區

- git 一律不動；建議先 commit CDD-05（up §9 訊息）。
- 不動 `down/`、`up/`；不動其他 feature 目錄。
- **只產 batch 4 之 48 列**；Revise 不重編號；其餘列不得生成。
- 條文本體不改；R-DIAG17／R-DIAG18 逐字落。
- framework LOCK 後 Layer 2 名稱與歸屬不再改；改需 Revise v02 ＋ Pei 裁。

## §1　依據

`R-DIAG17`（`X` 豁免）、`R-DIAG18`（Pre-Condition 機械守門）條文逐字取自 `down/20260917_CDD-05_review.md` §四。
追認：BVA 兩式、DR-DIAG-6、`-118` 決策表、`-034` 單條。

## §2　任務

**T0 — 條文與指紋**：兩條落 `RULINGS.md`；指紋依制回報。

**T1 — Revise → `pilot01_v05`／`batch1_v04`／`batch2_v03`／`batch3_v02`／`merged/v04`**
(a) R-DIAG18 (1)：grep 全合併本 Pre-Condition 含 `record`／`recorded`／`read`／`measure`／`send`／`sent`／`press` 者；`*_initial` 移至 Procedure 首步（讀取 ＋ `$` 指令 ＋ `record as`），ER-1 `… is recorded`，後續步號順延；回報命中清單。
(b) R-DIAG18 (2)：grep Pre-Condition 含 `by a diagnostic command`／`has been sent`／`was sent` 者，刪該行（步驟建立該態）；至少 `-012`；回報命中清單。
(c) 產生器守門加第二條（`*_initial` 只得於 Procedure 宣告）；lint `PC-DIAG` 實作 ＋ 5 正 5 反自測。
(d) R-DIAG17：`FEATURE_EXEMPT["diagnostics"] += ["X"]`；複驗 `X=0`。

**T2 — framework LOCK v01**
`features/diagnostics/docs/framework.md` 狀態 `DRAFT` → `LOCKED v01`：Layer 2 20 組（名稱、DID、L3 數、037 列數、pos/neg/uns、已產批次）；Layer 3 42；Part IV 記 LOCK 依據（pilot ＋ batch 1–3 之 162 列驗證判準穩定）。回報 LOCK 前後 diff。

**T3 — batch 4 sibling plan（先出後產）**

| Layer 2 | DID | 037 列 | 含 pilot | 分析層預判 |
|---|---|---:|---|---|
| #5 GPS Signal Data | `$2812` | 34 | — | 14 源三段式（Fix／Ellipsoid Height／Date-Year／Time／Satellites／HDOP 等各一子欄位）：**每 SWE1 列只追一子欄位**（CDD-02 §4.3 前例，不作 a./b. 分項）；positive 讀 1、neg 2、uns 1；`-366` unsupported 之 Title 誤植 `Satellites Currently Tracked` 對 CFTS004 核對 |
| #18 AV Signal Detection | `$0312` | 10 | `-151`／`-154`（pilot `-009`/`-010`）| Routine：Start／Results 兩步；`-155`（1 kHz 0 dB）、`-160`（`$31` 明載，`RESOLVED_CFTS004`）、`-158` 等 |
| #19 Audio Measure AC | `$031B` | 3 | — | Routine：頻率／幅度參數配置；positive 1、neg 2、uns 1 |
| #20 Clear Key Sense PIN | `$030A` | 1 | — | `-347` Do not interrupt when stop is commanded：Start → Stop 請求 → Results；ER 斷言常式仍完成（CFTS004-<ObjectID> 原文）；PC-3 含（Routine）|

回看 CFTS004 各節：GPS 子欄位之單位／值域（度、公尺、UTC）明載者入 ER 用字；未載編碼者 `<…>` 佔位 ＋ R-DIAG15 登錄（token 依缺件類別：資料版面 → DR-DIAG-5）。

**T4 — batch 4 產出** → `sandbox/batch4/diagnostics_batch4_v01.xlsx`／`batch4.json`／`coverage.tsv`
- ID：pilot `-009`／`-010` 沿用；新增自 `-232` 起；`-011`／`-012` 留號（#1／#2）。
- Routine 型 PC-3 依 SYSAD §4.5 明文（非保守套用，`pc_sources` 用明文字串）。
- 其餘規格同前包。

**T5 — lint／自檢／coverage／合併本**：六本各跑；`PC-DIAG`／`X` 0；§9 17 項；R-SEC15 型；coverage 48/48；`merged/diagnostics_v04.xlsx`；`placeholder_summary.tsv` 更新。

## §3　預期數字

| 項 | 預期 |
|---|---|
| batch 4 TC | 48 列 → 55–70 |
| Revise 命中 | R-DIAG18(1) 預估 10–20 條（BVA 越界 ＋ negative 值域軸）；(2) ≥ 1 |
| lint 真違規 | 0；`PC-DIAG`／`X` 0 |
| 合併本 | ≈ 290–305 列；覆蓋 #5～#20（16 組／210 列，54.0%）|
| framework | LOCKED v01 |

## §4　上繳要求（`up/20260917_CDD-06.md`）

1. T0 數字。2. T1 兩份命中清單與改法；守門與 `PC-DIAG` 自測。3. T2 LOCK diff。4. T3 sibling plan。
5. T4 TC 清單；貼入 **`-347` Clear Key Sense PIN 一條** ＋ **GPS 任一 positive 讀一條** 全文。
6. T5 紅數歸因；自檢；coverage；§3 對表；自報錯誤；未結 DR／FB；建議 commit。

## §5　升級條件

- 指紋集合不符／`--check` 不綠。
- R-DIAG18 grep 命中 > 40 條（表示前三批之 PC 型態系統性偏離，須回報分析層重新定型）。
- LOCK 時發現 Layer 2 歸屬須改（不得自改，升級）。
- 任何需改 canon 之情形。
