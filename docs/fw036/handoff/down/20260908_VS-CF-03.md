# 下放包 VS-CF-03 —— CFTS044 出貨版（Revise2）：237/237 全生成、PENDING 0、DR 0

Pei 裁定（2026-09-08）：出貨導向，**每個需求都要有產出**；不留 DR、不留 NOT GENERATED；
`"Headrest Dump"` 標籤改為 HMI 表之名。內容 Pei 事後自行調整。

**本包取代** `down/20260907_VS-CF-02.md` §一、§五 與 `down/20260908_VS-CF-02a.md` **全文**；
該二包依 R-TM13 不刪不改，僅於此具名作廢。VS-CF-02 之 §二／§三／§四併入本包 §三～§五。

基準：`sandbox/cfts044/cfts044_20260819_Revise1.xlsx`，sha256
`f444028d1cd93396738b4225ef6ce5de0ad3d41675d66f16acb0d099c8ef44a1`。
產出 `cfts044_20260819_Revise2.xlsx`（R-G72，Revise1 不改）。落檔一律 `surgical_save`。

## 〇、目標數字（上繳須逐項複算）

| 項 | 目標 |
|---|---|
| 資料列 | 243 → **241**（刪 2 重複列） |
| Requirement 覆蓋 | **237 / 237**，每個 Req 至少 1 條可執行 TC |
| 未生成列 | 23 → **0** |
| PENDING | 12 → **0** |
| 工作簿內 DR 引用（`DR-15`／`21`／`24′`／`25`／`26`／`BLOCKED`） | → **0** |
| `IMPL_GAP` Remarks | → **0**（見 §二.5） |
| tc_id | 重賦連續，附舊→新對照表 |

---

## 一、生成之唯一來源鏈（本包之核心規則）

**037 SWRA `Analysis Report` 之 D 欄（需求分析全文）為第一來源**，Q 欄（Verification
Criteria）為驗證手段之來源，R 欄為方法。CFTS 原句仍為 `test_item` 上半 verbatim。

規則（逐條適用於本包所有新生成／改寫之列）：

(a) **訊號名與值標籤一律用 D 欄之原表示法**，不代換為 DBC 名、不轉 raw 值：
`$FL_HS_RQ$`／`$HeatedSeatFL$`／`$HSW_Stat$`／`[HS_PSD]`／`[HS_NOT_PSD]`／
`[Front Seats]`／`[Fail_Not_Present]` 等。各 leaf 之標籤寫法不一致者（vented 側有寫
`[Medium]`、heated 側寫 `[HS_MED]`），**逐 leaf 保留其自身寫法，不統一**。

(b) **`$*_RQ$`／`$*_Cmd_Tlm$`／`$*_Stat$` 系列為 VHAL／CarProperty API 層訊號**
（D 欄逐字：`via the VHAL interface`／`CarPropertyManager will expose that value to HMI
layer via API interface as get/set methods`）。**不得寫 `Send CAN:` 式、不得寫 message 名、
不得開 bus trace**。設定寫 `Set $Sig$ = [Label]`，讀取寫
`Read $Sig$ and check that it is [Label]`。
CAN 專屬記法（IN §8.7.5(c)(d)）只適用於確為 CAN 之訊號（如 `STATUS_CCAN3.EngineSts`）。

(c) **037 Q 欄若寫 `Requirement is not clear`，不作為擋板** —— 該欄是 RD 對實作細節之意見，
D 欄若已給出可觀察之行為即據以生成。Remarks 具名：
`037 Q 欄為 "Requirement is not clear"；TC 依 D 欄之行為敘述生成`。

(d) **值域缺漏時不開 DR**：D 欄已給之標籤即為值域；D 欄未給者，取該 leaf 之
`test_item` 上半 verbatim 之標籤。兩者皆無時 **才**回報停手（本包預期 0 件）。

---

## 二、19 列由「不生成」改為生成

每列之 I（test_item）／J／K／L／M／P／R 全欄新寫。`test_item` 上半取 N 欄 ObjectID 對應之
CFTS 原句 verbatim（執行層自 `spec-index` 取；取不到則以 037 D 欄首句代之並具名），
下半括號為英文情境標籤，同 Req 多列不得逐字相同（R-S4）。

### 2.1 組態閘控型 —— 9 列

| 列 | Req | D 欄之條件（逐字） |
|---|---|---|
| 93 | OneStageHeatedSeat-039 | `$Heated_Seats$ = [Front Seats] or [Front and Rear Seats]` |
| 107 | TwoStagesHeatedSeat-055 | 同上 |
| 127 | ThreeStagesHeatedSeat-078 | 同上 |
| 179 | TwoStagesVentedSeatsManagement-037 | `PROXI parameter Cooled_Seats == "Front Seats" OR "Front and Rear Seats"` |
| 199 | ThreeStagesVentedSeatsManagement-060 | 同上 |
| 108 | TwoStagesHeatedSeat-056 | `$Heated_Seats$ = [Front Seats] or [Front and Rear Seats]` **and** `$Heated_Seats_Levels$ = [Two Levels]` |
| 128 | ThreeStagesHeatedSeat-079 | 同上，`[Three Levels]` |
| 180 | TwoStagesVentedSeatsManagement-038 | `$Heated_Steats_Levels$ is equal to "Two Levels"`（**原文拼字 `Steats` 照抄**） |
| 200 | ThreeStagesVentedSeatsManagement-061 | 同上，`"Three Levels"` |

範式（正負成對，§7）：

```
Procedure
1. Set $Heated_Seats$ = [Front Seats]
2. Power cycle the HU and press "Comfort" on Menu Bar
3. Read the heated seat control and record its presence as Control_qualifying
4. Set $Heated_Seats$ = [Absent]
5. Power cycle the HU, press "Comfort" on Menu Bar and check that the heated seat control is not displayed

ER
1. $Heated_Seats$ = [Front Seats] is accepted
2. The "Comfort" screen is displayed
3. Control_qualifying is recorded as displayed
4. $Heated_Seats$ = [Absent] is accepted
5. The heated seat control is not displayed
```

step 4 之非合格值取 D 欄未列舉之對立值；D 欄未給對立值者，用
`Set $Heated_Seats$ to a value other than [Front Seats] or [Front and Rear Seats]`
（不造具體值，§8.4.1）。雙條件列（108／128／180／200）之負向側只翻**第二**條件，
第一條件維持合格，以免兩因混淆（§8.3 一次一驗證點）。

### 2.2 故障圖示型 —— 1 列

**row 94**（OneStageHeatedSeat-040）。D 欄末句即完整行為：
`Regardless of the value of $HeatedSeatFR$, if $STATUS_CSWM.FR_HS_STATFailSts$ = [Fail_Present],
the HMI shall update the relative icon on TLM_Display.GUI according to the TLM HMI Document.`

`Regardless of` 以 baseline 覆蓋（§5.6）：先在 `$HeatedSeatFR$` 之一個非 OFF 值下記錄圖示，
再注入 `[Fail_Present]`，檢查圖示改變。ER 只寫「圖示改變」，**不寫改成什麼**
（TLM HMI Document 未附，寫具體外觀即造值）。Remarks 具名該文件未附。

### 2.3 請求／釋放型 —— 5 列

| 列 | Req | D 欄之送出值 |
|---|---|---|
| 87 | RightFrontHeatedSeat-031 | `$FR_HS_RQ$` = `[HS_NOT_PSD]`，於 `[HS_PSD]` 之後 |
| 173 | RightFrontVentedSeat-029 | `$FR_VS_RQ_TGW$` = `[Vented Seat Pressed / VS_PSD]` |
| 233 | HeatedSteeringWheel-013 | `$HSW_RQ_TGW$` = `[NOT Pressed / NOT_PSD]`，於 `[PSD]` 之後 |
| 234 | HeatedSteeringWheel-013 | 同上（同 Req 之第二列，括號下半須與 233 相異） |
| 235 | HeatedSteeringWheel-014 | 依 `$HSW_Stat_2$` 之 toggle |

範式（row 233）：

```
2. Press and release the heated steering wheel button
3. Read $HSW_RQ_TGW$ and check that it is [PSD] followed by [NOT Pressed / NOT_PSD]
```

**row 235 之限制**：D 欄末句為 `as defined in the corresponding signal mapping` ——
該對照表未附。TC **不寫具體送出值**，改驗可觀察之 toggle 結果：
`Read $HSW_Stat_2$ and check that it has changed from the recorded value`。
Remarks 具名：`D 欄指向未附之 signal mapping；本列驗 toggle 之狀態變化，不驗送出值`。

`<Tsend>` 全案不驗（無上限值），Remarks 具名 `<Tsend> 無上限值，本列不驗時限`。
**不開 DR-24′。**

### 2.4 HSW 命令型 —— 4 列

**row 245／246／249／250**（HeatedSteeringWheelManagement-029／030／033／034）。
D 欄已給完整條件→命令邏輯，值域齊備，**DR-25 撤**：

| 列 | 條件（D 欄逐字） | 命令 |
|---|---|---|
| 245 | `$HSW_Stat$ = OFF` ＋ `STATUS_CSWM.$HSW_StatFailSts$ = Fail_Not_Present` ＋ `SteeringWheelHeating.Req = Requested` | `TELEMATIC_VEHICLE_SETUP.HSW_Cmd_Tlm = ON` |
| 246 | `$HSW_Stat$ = ON` ＋ 同上二者 | `HSW_Cmd_Tlm = OFF` |
| 249 | `$HSW_Stat$` transitions to `[ON]` | `HSW_Cmd_Tlm = ON` |
| 250 | `$HSW_Stat$ = [OFF]` | `HSW_Cmd_Tlm = OFF` |

245／246 為三條件合取 → `design_method` = 決策表；249／250 為狀態轉換 → 狀態轉換。
245 與 249、246 與 250 之括號下半須明確區分（前者為使用者請求觸發，後者為狀態變化觸發）。

### 2.5 已生成列之 IMPL_GAP Remarks 一併撤除

44 列 Remarks 現寫 `IMPL_GAP: TELEMATIC_VEHICLE_SETUP.FL_HS_Cmd_Tlm —— 依 R-VS66(a) 開
issue 予 RD`。依 §一(b)，該系列為 API 層訊號，**「DBC 查無」不構成缺陷**，該敘述撤除；
同列之 `BLOCKED: DR-18 —— _mid 對映未解` 一併撤（`_mid` 之值取 D 欄之原標籤）。
撤除後該 44 列之步驟若仍寫 `TELEMATIC_VEHICLE_SETUP.*` 之 message 名，依 §一(b) 改為
`$FL_HS_Cmd_Tlm$` 之 API 記法。

### 2.6 其餘 4 列

- **row 56**（PHEVFeatures-017）：D 欄僅章節描述，依 Q 欄
  `CAN signal to be trigger / System update CAN value to HMI / HMI change the display state to new`
  生成一條薄 TC（PHEV 功能於 HU 可檢視）。Remarks 具名其為章節層條文。
- **row 69／230**（LeftFrontHeatedSeat-011／HeatedSteeringWheel-010）：原「轉指 Stop-Start 節」
  之 HELD OUT 撤；依各自 Q 欄（`Set the Start-Stop available CAN signal to be trigger` 等）生成。
  ⚠ 與 row 10–12 之 Stop-Start 列可能重疊，生成後須跑 §六之重複檢查。
- **row 252**（HeatedSteeringWheel-009）：依 Q 欄
  `Set the Start-Stop is not available CAN signal to be trigger / System update CAN value to HMI` 生成。

---

## 三、既有 12 列 PENDING 之收尾

| 列 | 處置 |
|---|---|
| 71／73／86／88／157／159／172／174 | 依 §一(a)(b) 用 D 欄原表示法改寫；四值循環列（73／88／159／174）之真值表逐條展開，**執行層須讀 D 欄全文**，末行以實讀為準 |
| 233／235 | 見 §2.3 |
| 28 | `$PowerMode$ = [Ign. off & acc. (4 position switch) / IGN_OFF_ACC]` 依 §一(a) **保留原標籤**，寫 `Set $PowerMode$ = [IGN_OFF_ACC]`，不代以 DBC raw。DR-21 撤 |
| 35 | 條文 `selectable only when [IGN_RUN]` 自帶否定側；ER 收斂為可選性。DR-26 撤 |

row 26 之 `CmdIgnSts = 5 (START)` 與 row 10–16 等之 `EngineSts` 屬**確為 CAN** 之訊號，
維持 CAN 記法不動；Remarks 之 `dr_dependent = DR-19／DR-21` 前綴撤除，改為
`$EngRun_Stat$ 依 037 D 欄之對照以 EngineSts 觀察`（037 該 leaf 之 D 欄逐字為據）。

---

## 四、重複列與 sibling

**刪 row 23、row 48**（與 24／49 五欄逐字相同，§10.6 嚴格等價）。
row 49 之 `""` 填 `"Screen Off"`（來源見 §五）；row 24 不改。
刪列後 F 欄 tc_id 依 §10.3 重賦，產出 `reports/cfts044_tcid_remap.tsv`（舊→新，TestRail 用）。

真 sibling 補區分 token：

| Req | 列 | 括號下半改為 |
|---|---|---|
| Stop-StartSystem-002 | 10／11／12 | `(… : heated seat)`／`(… : vented seat)`／`(… : heated steering wheel)` |
| FeaturesEnableCriteria-022 | 58／59 | `(Vehicle line M4)`／`(Vehicle line MP)` |

新生成列之括號下半亦須通過同 Req 不逐字相同之檢查。

---

## 五、導航路徑與標籤

來源：`spec-index/cache/SYS1_HMI_Menu_Bar_and_App_Drawer_HMI_Logic_and_Flow_R1_SR24_3A_(September_11_2023).xlsx`，
sheet `Basic Report`，**row 22**（NRL-127734，§4.1 naming table）。

| 現況 | 改為 |
|---|---|
| `Open the Controls screen from the Menu Bar` | `Press "Controls" on Menu Bar` ／ ER `The "Controls" screen is displayed` |
| `Open the Rear View Camera screen from the Menu Bar` | `Press "Rear View Camera" on Menu Bar` |
| `Open the Heated / Vented Seats screen …`、`Climate screen`（13 列）、`Heated / Vented Seats screen`（205 列） | 全簿統一 `Press "Comfort" on Menu Bar` ／ `the "Comfort" screen` |
| `"Headrest Dump"`（8 處） | **`"3rd Headrest Fold"`**（Pei 裁 2026-09-08；naming table 之 Drawer Name）。`test_item` 上半 verbatim **不動** |
| `Hybrid Electric Pages access button` | `"Hybrid Electric" button` |
| row 49 之 `""` | `"Screen Off"` |

## 六、全簿收尾（Revise2 落檔前）

1. 全形 `；`／`，` → 半形 `; ` ／ `, `，全簿歸零。
2. `Set PROXI` → `PROXI`（v4.1 標準式，IN §8.7.5(e)）。
3. 尾句號、行首尾空白、方括號、`$MESSAGE.Signal$`（作者側四欄＋括號下半）全簿歸零。
4. Procedure ↔ ER 逐列 1:1；每列 ≥2 步。
5. `specification_reference` 格式 `CFTS044-{7 位}`，一行一個，升冪。
6. Priority 值域 P0–P3；`design_method` 依 IN §12。
7. **重複檢查**：新生成 19 列 ＋ row 69／230 與既有列跑 §10.6 嚴格等價比對，命中即回報，不自行合併。
8. row 13 之驗證邏輯（step 3 送 `ESS_ENG_ST = 4`、step 5 要求讀回 `1`，中間無設定；且讀回自注訊號屬 B 類 loop verification）**一併重寫**：驗證點改為 step 5 之 HMI 觀察（開關指示燈狀態），不讀回自注之訊號。

## 七、上繳 `up/20260908_VS-CF-03.md` 須含

1. Revise1／Revise2 之 sha256；zip member 差異與 dv 計數；列數 243 → 241。
2. §〇 之七項目標數字逐項現算。
3. 19 列新生成之逐列全文；每列所用之訊號名與值標籤標明取自 037 哪一 leaf 哪一欄。
4. tc_id 重賦對照表路徑。
5. §六 1–8 之全簿計數。
6. `RULINGS.md`：**撤 `R-VS102(2)`**（B7 之依據已不成立）並加註撤銷理由；
   撤 `vf230_wvf44_writability.py` docstring 之 `B7-no-invalid-raw` 登記行（依 R-TM13 加刪除線與註，不刪行）。
7. `DATA_REQUESTS.md`：DR-15／21／24′／25／26 及 B7 草稿標記為**撤回**（不刪，加註）。
8. 工具旗標逐次聲明；自報。

## 八、分析層自報

**A-{n}　連續四包以 DBC／CFTS 原句為唯一來源，未讀 037 D 欄**
VS-CF-01 §一（R-VS102 全條）、VS-CF-02 §一、VS-CF-02a §一、及本包之前身判斷，
皆未讀 037 `Analysis Report` 之 D 欄。該欄是 SWE.1 對每一 leaf 的行為拆解 ——
即本專案「文件分析」之主體。四包所產生之 B7 新碼、5 個 DR、23 列不生成，
**全部源於此一遺漏**，本包一次撤回。

成因與丙-2 同族但更深：不只是「先斷後量」，而是**反覆量同一份好量的來源（DBC）
來回答一個它答不了的問題**，且每次量完都得到自洽的結論，因而未觸發懷疑。
防再犯：新 feature 之 recon 階段須先落一份「037 D／Q／R 欄可用性清單」，
未落此清單前不得進入 TC 生成（建議條文，待 Pei 裁後入 FO）。
