# 下放包 CDD-16 —— 收尾：`$5100` 兩處描述行、三條 amend（零裁定新增）

前件：`down/20260918_CDD-15_review.md`。執行層 session：沿用 CDD 線。

## §0　禁區
git 不動；`--gate`／`--regen` 不跑；除 §1 兩處外 TC 文字不動；T0 先取號再產出；升級條件表自 §5 逐字複製。

## §1　任務
- **T0**：`R-DIAG6(amend2)`／`R-DIAG32(amend)`／`R-DIAG31(amend)` 依審閱 §三逐字落 `RULINGS.md`；指紋依制（R-G75 三數）。
- **T1**：`NR1L-DIAG-229`／`-230` 描述行 `Send a write request for DID $5100` → `Send an InputOutputControlByIdentifier request for DID $5100`；其他欄不動。
- **T2**：`SID-WORD` 依 R-DIAG32(amend) 措辭校對實作（判準已是指名衝突，只確認註解與條文一致）；十一本跑，預期 **0**。
- **T3**：merged **v14**／交付本 **v07**（版號先 `ls` 確認未被占用）；lint；§9；逐格對帳 vs v13（差異只在兩格 ＋ ID/列序）；`coverage`／`placeholder_summary` 不變。
- **T4**：`cs_uds_rules.md` 首段引文換 CS.00099 r25 句。

## §2　預期
565 列；著色 18；`SID-WORD` 0；lint 全 0；變動格 2。

## §5　升級條件
- 指紋／`--check`（既存者記「既存」）。
- 對帳差異 > 2 格（不含 ID/列序）→ FAIL，回報不改。
- `SID-WORD` > 0 → 列出不改。

## §6　上繳
首段升級條件逐字；sha8 三條；T1 兩格前後；T2/T3 數字與路徑；建議 commit ＋ pathspec。
**本包上繳後 CDD 線無後續包**；Pei 填 Cover 三格即可交付。
