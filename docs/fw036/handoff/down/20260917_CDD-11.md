# 下放包 CDD-11 —— CFTS004 原始文件補產（R-DIAG26）＋ 交付候選（R-DIAG27）

前件：`down/20260917_CDD-10.md`、`_A.md`、`up/20260917_CDD-10.md`、`down/20260917_CDD-10_review.md`。
依常規：審閱後即發；依據 = CDD-10 審閱 §三／§四。執行層 session：沿用 CDD 線。
指紋制依 GC-16 是否落地。

---

## §0　禁區

- git 一律不動；push 依 CDD-06 審閱 §五-1。
- 不動 `down/`、`up/`；不動其他 feature 目錄；`sandbox/delivery/` 為本 feature 專屬。
- `rulings_hash.py` 只得 `--check`／`--out <scratch>`。
- 原始文件搜尋只讀不寫；登錄依 R-G66（sha256 比對、不存副本）。
- 交付本之 Cover 三格（核准／審查／中文作者）**留空**，由 Pei 填。
- `_all`／各批 sandbox 本不刪；交付本另存。
- 升級條件表自 §5 逐字複製；命中者首段明記。

## §1　依據

`R-DIAG25(amend)`、`R-DIAG26`、`R-DIAG27` 條文逐字取自 `down/20260917_CDD-10_review.md` §四。

## §2　任務

**T0 — 條文與指紋**：三條落 `RULINGS.md`；指紋依制。

**T1 — CFTS004 原始文件（R-DIAG26）**
(a) 搜尋：`/Users/peihe/Work/02_Project_R1LR/1_Customer_Requirement/` 全樹（含 `R1LR SR26 ATL-H/<PI>/`、`Reference Documents/`），`find -iname "*CFTS*004*"`、`-iname "*General*Diagnostic*"`；docx／pdf／xlsx 皆列，附 sha16、大小、mtime、PI 版本。
(b) 命中 ≥ 1：選與索引匯出同版（Comments 欄 `CFTS004-xxxx` 之版本號／日期對齊）者，依 R-G66 登錄 `sources/raw/cfts004_<ver>/` ＋ MANIFEST；R-G67 嵌入物件檢查。
(c) 抽出：五句引言之列表（`extension_audit.md` 所列之 5 句，各附 ObjectID）＋ `$5000`／`$5001` selectable 列表 ＋ `$2843`／`$2812`／`$280C` 之 items 列表；逐字落 `features/diagnostics/data/cfts004_lists.md`（每列表附原文件章節與頁碼／段號）。
(d) 依 R-DIAG23(c)／R-DIAG25 補產：列表項屬 037 已引 FR 之列舉者 → 逐值 sibling 或 ER 分項；屬 controlOptionRecord／選項編碼者 → 若原始文件載位元組值，**填值並撤佔位**（更新 `placeholder_summary.tsv`／DR-DIAG-4／-5 現況）；仍無編碼者維持佔位。
(e) 未命中：登 DR-DIAG-10（對 Nik：CFTS004 原始文件，含版本），本包 T2 照常。

**T2 — 交付候選（R-DIAG27）→ `features/diagnostics/sandbox/delivery/`**
(a) 母本：以 SWC 0708 交付本為樣板（`feature.yaml` 之 R-G48 對照已實測）；Cover 版本 `A`、日期 `2026-09-17`、作者欄依 SWC 0708 之填法（英文名／留空）實測後定；Product Document 之專案／版本／類型／機密／初版列依 SWC 0708 逐格照抄形式；`Reference`／`ChangeHistory` 沿樣板不動。
(b) 列序：037 列序（SWE1 ID 升冪；`-340-001` → `-340-002` 依 037 row 347→348）；同列多 TC 依原 ID 序。TC ID 重編 `NR1L-DIAG-001` 起連號；`id_map_v08_to_delivery.tsv`（舊 ID／新 ID／SWE1 ID）。
(c) 佔位列整列 `FCE4D6`（A～AH），文字不動，作法沿 Security SEC-17 之 styles.xml 手術式；Out of Scope 列不著色。
(d) 檔名 `FM-WI-FSM-036-A01 STLA 測試用例規範與結果_SWQT STLA Test Case Specification & Result_SWQT_Diagnostics_20260917.xlsx`。
(e) 共享檔：`coverage.tsv`（389 列）、`placeholder_summary.tsv`、`placeholder_by_token.tsv`、`asset_request.md`（五或六項）、`rd_feedback_20260917.md`、`EXEC_GUIDE.md`（**新增**：一次性環境 —— 診斷工具連線／session／security access 之操作，只寫 SYSAD §4.5 所載，逐行附來源；佔位填入前不得執行之聲明）。
(f) 內容逐格對帳：交付本 vs `merged/v08`（＋T1 補產）cell-for-cell 只差 ID 欄與列序；lint 全項；§9 17 項；R-SEC15 型。

**T3 — 盤點終表**：Layer 2 20 組列數／TC 數；Priority／design_method 分佈；PENDING；佔位；DR-DIAG-1～10；FB-a～s；A-DIAG 全表狀態；`framework.md` LOCKED v01 對帳。

## §3　預期數字

| 項 | 預期 |
|---|---|
| T1(a) 命中 | ≥ 1（分析層依 CFTS024／092 之前例推估；未命中即 DR-DIAG-10）|
| T1(d) 補產 | 命中時 20–80 TC（五句列表之項數未知）；佔位撤銷 0–60 行 |
| 交付本列數 | 556 ＋ 補產 |
| lint | 真違規 0 |
| 對帳 | cell-for-cell 只差 ID／列序 |

## §4　上繳要求（`up/20260917_CDD-11.md`）

1. 首段升級條件逐字。2. T0。3. T1(a) 搜尋結果表；(c) `cfts004_lists.md` 摘要（五句各項數）；(d) 補產清單與佔位撤銷數；或 (e) DR-DIAG-10。
4. T2 交付本路徑、Cover／Product Document 逐格值與其 SWC 0708 依據、id_map 統計、著色列數、對帳結果；貼入 **交付本前 3 列** ＋ **任一 T1 補產 TC** 全文。
5. T3 終表。6. §3 對表；自報錯誤；未結；建議 commit ＋ pathspec。

## §5　升級條件（命中必明記）

- 指紋集合不符／`--check` 不綠（既存者記「既存」）。
- T1(b) 命中之原始文件與索引匯出版本不一致（Comments 欄版本號／日期）且無同版者 —— 回報不登錄，Pei 裁用哪版。
- T2(f) 對帳出現 ID／列序以外之格差。
- 任何需改 canon 之情形。
