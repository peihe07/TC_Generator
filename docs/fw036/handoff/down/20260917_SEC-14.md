# 下放包 SEC-14 —— 收尾 chore：`gen_batch2.py` 範圍修法 ＋ `EXEC_GUIDE.md` §1.1 殘留 ＋ A-SEC-15（無 TC 變更，不升版）

前置：`up/20260917_SEC-13.md`、`down/20260917_SEC-13_review.md`。
**請 Pei 先落 SEC-12／13 四支 commit 再開工**（執行層不自動 commit）。
執行層 session：新開或延用皆可；本包不動 TC、不升版。

---

## §0　禁區
- `down/`／`up/` 既有檔不動；canon／framework／lint 不動；所有 xlsx／JSON 不動（本包**零 TC 變更**，`merged/security_v08.xlsx` sha `bed94fbba2940f96` 前後須相同）。
- 只動：`gen_batch2.py`（一處）、`build_exec_guide.py`（一處）、`EXEC_GUIDE.md`（重生）、`ANOMALIES.md`（一列）、`RULINGS.md`（A-33 定性註）。

## §1　`gen_batch2.py`
`scope` 排除集合 `("D",)` → `("D", "CONVERT_DOC")`，加註 `# CONVERT_DOC rows are produced by gen_deferred.py (batch 3)`。
驗證：三支生成器皆可重跑；`batch2_v08` 輸出 sha 仍 `310a3769814997a2`；`build_merged.py` 重跑後 `security_v08.xlsx` sha 不變。

## §2　`build_exec_guide.py`
§1.1 引言區塊之 apk 檔名改為自 Pre-Condition 集合抽取（regex `Test runner (\S+\.apk) is installed`），不寫死；文句改為
`The APK file name appears in the workbook only as <抽出值> (Pre-Condition above)…`。重生 `EXEC_GUIDE.md`；⊆ 58／58 與 §1.6 10／10 兩驗證仍須 100%；行數回報。

## §3　台帳
- `ANOMALIES.md` 加 `[A-SEC-15] CCVR Cert Val CS.98 STEPS item 2/5 寫 adb install -r -t CertProviderServiceManagerTest.apk，目錄實檔為 CertProviderAndroidInstrumentalTest.apk；工作簿依實檔（R-SEC24），請 Steven 確認交付檔名`。
- `RULINGS.md` R-SEC24 條下加註 `A-33 reclassified: source exists in CCVR STEPS; R-SEC24 kept on physical-file evidence (SEC-13 review)`（註為 amend 外之 note，不改 body sha —— 若 `RULINGS.md` 體例不允許條下註，改記於 `ANOMALIES.md` A-SEC-15 列尾）。
- `asset_request.md` 之 `X-f-2` 段加一句「Steven to confirm CertProvider apk file name (A-SEC-15)」（腳本生成者改腳本）。

## §4　上繳要求（`up/20260917_SEC-14.md`）
1. Pei commit 四支之 hash（執行層回報 `git log -4 --oneline`）。
2. §1 三支重跑之 sha 表（前後相同）。
3. `EXEC_GUIDE.md` 新 sha、行數、兩驗證、§1.1 引言區塊逐字。
4. 台帳三處 diff。
5. 自報錯誤。

## §5　升級條件
- 任一 xlsx／JSON sha 改變。
- 兩驗證任一 < 100%。

## §6　本包後
Security 側**無後續包**；觸發條件不變（RD 資產／DR 回覆／`W=4`／自動化 go）。GC-16 另出。
