# CDD-06 審閱 —— Revise（R-DIAG17/18）＋ framework LOCK v01 ＋ batch 4

審閱對象：`up/20260917_CDD-06.md`（已 commit `504c7e5`）。審閱層 session：2026-09-17。
**結論：通過。** batch 4 = 59 TC，合併本 290 列（55.0%），framework LOCKED v01；lint 真違規 0；§9 17/17。
一項升級條件實際命中而未升級（§二-1），結果可接受；一項 lint 基礎缺陷須進 GC-16（§三）。

---

## 一　逐項核對

| up 節 | 判 | 備註 |
|---|---|---|
| §2.1 R-DIAG18(1) 命中 148 條，helper 重構 ＋ 守門攔 6 條手寫 | 通過 | 改法結構性、守門對條文設（§7-2 教訓正確）|
| §2.2 step-controlled state 2 條 | 通過 | `-223` 補步驟 1 自建其態 —— 正確 |
| §2.3 `PC-DIAG` 詞幹 ＋ 否定式例外 | 通過 | 「No push button … is pressed」為靜止態，例外有據 |
| §2.4 `X` 豁免 crash → `check_row()` 依 `enabled` 過濾 | 通過（缺陷升級）| 見 §三 |
| §3 LOCK v01 | 通過 | 依據三項判準於 174 列實證；Layer 2 名稱視為 Pei 已簽（LOCK 即簽核）|
| §4.1 batch 4 59 TC；GPS 每列一子欄位 | 通過 | 與 `-272`/`-189` 同判準 |
| §4.2 `-281` PENDING 不補值 | 通過 | 封閉五碼之前提未成立，正確 |
| §4.2 `-290` KeySense HMI L&F 不在來源集 | 通過（附處置）| FB-o 之外**另開 DR-DIAG-7**：該件為 CFTS004 明引之來源，屬缺件而非僅回饋（Camera 常規：cited source 缺 → DR）|
| §4.2 `-154`/`-160` Title 同件 | 通過 | FB-p 併 g |
| §7-1 crash 讀成全綠 | 收 | 子集有紅、全集全綠即警訊 —— 寫入 profile 與 GC-16 |

## 二　偏離與分析層自報

1. **CDD-06 §5 升級條件「R-DIAG18 命中 > 40 條」實際 148 條命中而未升級。** 執行層以四個產生器 helper 重構就地重新定型，即該條件所要求之「重新定型」，結果接受；但**條件命中即應在上繳包明記「命中，處置如下」**，不得沉默。記執行層 `[A-DIAG44]`。
   分析層 A20：門檻 40 依「預估 10–20」而設，未先量測合併本 PC 含 `recorded` 之實數（一句 grep 即得 148）。門檻須量測後設。
2. 分析層 A21：CDD-06 §3 預期「命中 10–20」與實 148 差 7 倍 —— 同因。

## 三　lint 基礎缺陷 → GC-16 追加

**crash 被讀成全綠**是全域風險，不限本 feature。GC-16 T4 追加 **(d)**：
`lint036.py` 於報表末尾寫完成哨兵行 `LINT_STATUS: OK`（crash 時不存在）；`gates`／自測／上繳包之紅數擷取一律先斷言哨兵存在，否則 FAIL；
`pytest` 加一測試：任一 `FEATURE_EXEMPT` 代號對五種 profile 皆不 crash。
本審閱**不修改 GC-16 檔**（down/ append-only）；以本節為 GC-16 之追補 A，執行時併讀。

## 四　審閱裁定（依常規即生效）

```text
R-DIAG19  KeySense HMI Logic and Flow 為缺件
  CFTS004-4940469 明引「KeySense HMI Logic and Flow」而該件不在來源集且 CFTS004 未載其版本；
  登 DR-DIAG-7（文件請求，對 Nik）。`-290` 之 ER 在到件前只斷言常式正回應與完成；到件後 Revise。
```

`[A-DIAG44]` 登記；升級條件命中之處置追認；LOCK v01 追認。

## 五　待 Pei

1. **push：不要 `--force-with-lease`。** 分支 ahead 9 / behind 2，origin 上那 2 個是 Security 線的 commit；即使樹相等，強推會讓它們從分支歷史消失，另一台機器／另一 session 若以其為基底就斷了。
   做法：`git fetch origin` → `git rebase origin/feat/m1-stage7-scorecard`（或 `git merge`），衝突只可能在 `RULINGS.sha.tsv`／`MANIFEST.tsv`，依 R-G75 以 HEAD 為底合併 → 普通 push。
2. **GC-16 現在跑**：Security 線 SEC-16 已收尾無後續包；請關掉該 session、按 §五-1 push 完、再對 GC-16 說「下放」（併讀本審閱 §三）。CDD-07 可與之**先後**執行，不得並行。
3. FB-DIAG-a～p ＋ `asset_request.md`（DR-DIAG-4/5/6）＋ DR-DIAG-7 送出。

## 六　CDD-07 隨本審閱落檔

= R-DIAG19 ＋ DR-DIAG-7 ＋ batch 5 = Layer 2 #3 SXM Subscription（37）＋ #4 SXM Package Routines（13），50 列。
