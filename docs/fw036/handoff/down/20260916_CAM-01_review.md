# 下放包 CAM-01 審閱（分析層 2026-09-16）

對應上繳包：`docs/fw036/handoff/up/20260916_CAM-01.md`
判定：**Phase 0–1 通過**；§9 命中之一項（對映率 98.70%）成因確認為下放包前提錯置，非補件事項。Phase 2（framework 鎖定）待 Pei 裁 §三之十項後另包。

---

## §一　下放包自身錯誤之承認（R-G69，數字與理由分別驗證）

| 下放包處 | 錯誤 | 更正 |
|---|---|---|
| §6 A 本 Sub Cat「12／5／7／2／1」 | 合計 27，自相矛盾；分析層以 Description 語意歸類而非 `I` 欄 | 以 `I` 欄逐字為準：**9／6／7／2／1** |
| §6 RVC+PAM「63／11」 | 63 為 `max_row 64 − 1` 之機械值，未濾 A 欄空列；11 為目測 | REF 本 **55／12**、cache 本 **64／12** |
| §6 HeadUnit 1045、CEH 38 | 條件書寫不一（一處含空列、一處含表頭） | 1044／37，資料無差 |
| §4(b)「未裁前以 REF 本為準（037 所引者）」 | 前提反了：037 所引之 `7.3.1–7.3.3` 只存在於 cache 本 | 撤回；見 §三-4 |
| §3.5 預判「32 列皆 RVCM 端」 | 6 列（`V2-578`／`-580` 各被三列引用）為 Radio 端條件 | 見 §三-8 |

## §二　執行層報告之更正

1. **§3.9「Atl-Hi 側無對應之 `CmdIgnSts` 列」—— 不成立。** `CameraEventHal status.xlsx` 第 7 資料列逐字 `BCM_FD_10.CmdIgnSts | input | Atl-H | Y | verified`。故 `-002` 之 Ignition 前提：Atl-Hi 用 `BCM_FD_10.CmdIgnSts`（Y／verified），Atl-Mi 用 `STATUS_BH_BCM2.CmdIgnSts`（N／Could emulate）。未結 10.2-2 **關閉**。
2. **A-CA10（`PowerShutDownNotifcation` 拼寫）—— 為來源原文。** `Video_Parking_Assistance_-_LTM_VF551_V2_R1.docx` Head Unit Requirements 節逐字：「Transmit PowerShutDownNotifcation.Power_Down = True over LVDS to the RVCM」。LVDS 訊息名以來源為準（R-13），verbatim 上半與訊號行皆保留此拼法；ANOMALIES 改標 `RESOLVED — 來源原文，非 037 筆誤`。未結 10.2-4 **關閉**。
3. **§3.9 之 11 個不可注入訊號**：
   - `PT_SYSTEM_FD_1.ShiftLeverPosition_PT`：依 `SYS-RA-VF551_V2-551/-552`，僅 `Hybrid_Type = REP／FCEV` 時取此訊號，其餘取 `TRANSM_FD_4.ShiftLeverPosition`（Y／verified）。`-015` 之主路徑不受影響；hybrid 分支之可測性登 ANOMALIES，**不開 DR**。
   - `STATUS_LIN.DynamicGrid*`：屬 V33／V42 Dynamic Grid Line Overlay 線，A 本 `-023` 引 `V42-230/231/232`；其 ER 若須 `DynamicGridSts` 觀察，改以 HMI 現象（guideline 顯示與否）為觀察面，來源名保留（§8.7.5(f)）。登 ANOMALIES，不開 DR。
   - 其餘（`*FailSts`、`NeutralSwSts`、`LwsAngleType`、`ASCM_Stat`、`LanguageSelection`）：A 本無直接引用者不處置；`LanguageSelection` 命中 `-021` 之 localized overlay，該 TC 以 HMI 語言設定為觸發（§5.8(e)），不走 CAN。
   未結 10.2-3 **關閉**（三條登 A-CA15～17，由執行層落）。

## §三　待 Pei 裁之項目（分析層附建議）

| # | 項 | 執行層提議 | 分析層建議 |
|---|---|---|---|
| 1 | ABBR | A／B 共用 `CAM`，連號 | **同意**。VF230 單本 627 列尚可，兩本合計預估 < 700 |
| 2 | B 本 Test Group | `Camera`／`Camera HMI` | **`Camera`**（R-VF9 前例：兩本同值；Test Set 已足以區分） |
| 3 | `*` 入 hop | 不入，依品牌取 label | **同意不入**。但須先定 **N1L 之品牌**：Jeep／Ram 系 = `ParkView Backup Camera Delay`；Fiat = `Rear View Camera Delay`。**此為新增裁項** |
| 4 | RVC+PAM 用哪本 | cache 本 | **同意 cache 本**（037 所引之 7.2.x／7.3.x 只在此本；REF 本登 MANIFEST note「同名異體，子集」不作追溯母體）。DR-CAM-c 結案 |
| 5 | V4 Layer 3 判法 | D 欄前導號向下繼承 | **同意**（321/321、A 本引用 44 列全落 5 章）。DR-CAM-b 結案 |
| 6 | lint 代號 | `Z` | **同意** |
| 7 | `ENTER_CAMERA_SETTINGS` 第 3 hop | 以 HMI Settings List 類別列為據是否足夠 | **足夠**：canon §5.8(c) 明列 HMI Settings List 為路徑來源；hop = `"Camera"`（`Settings` 分頁 row 464 `13. Camera` 之類別名，序號非 label）。常數定為 `ENTER_SETTINGS_APP` → `Select "Camera"`。DR-CAM-d 結案 |
| 8 | 6 列非 RVCM 之 Out of Scope（`V2-578/-580`） | — | **維持 Out of Scope 不作追溯錨**（SYS2 分類為上游判定，本層不重判）；其「RVC display active」條件可作 `-010/-017/-023` 之 Pre-Condition 措辭來源，於 reasoning 註明出處 |
| 9 | B 本 Layer 2 修訂 | 不提名、不移列 | 見 §四之修訂案 |
| 10 | `SWE-CAM-023` 與 6 列交集（A-CA07） | — | 依 §8.2.1 以 037 為準不合併；生成時每組交集之來源只由一列承接（先到者），reasoning 註明委派。承接順位待 framework 鎖定時定 |

## §四　B 本 Layer 2 修訂案（取代 CAM-01 §5 之 B 本表）

依上繳 §4.2 實測重切；HU 章 1／3／4／18–22／26／32 不入（B 本未引）。

| # | Test Set | 章節（逐字前綴） | 實測列 | 變動 |
|---:|---|---|---:|---|
| 1 | `PAM Integration` | RVC+PAM 6.x、11.1、**8.8** | 10 | ＋8.8（PAM graphic，1） |
| 2 | `Activation and Exit` | HU 6.1、6.5、6.7、**6.10**；RVC+PAM 7.1–7.5、**8.1**、**8.6**、**8.7** | 50 | ＋6.10（1）＋8.1（6）＋8.6（1）＋8.7（1） |
| 3 | `Camera Settings` | HU 6.2 | 5 | — |
| 4 | `Warning Banners` | RVC+PAM 8.2–8.4、9.2 | 13 | 刪 HU 3／4（0 列） |
| 5 | `AUX Camera Access` | HU 27、34.1–34.8 | 62 | 刪 HU 26；HU 28 移出 |
| 6 | `Camera View Switching` | HU 28 | 31 | **新設**（More Cams／More AUX 之切換，setup 型態與 Access 不同） |
| 7 | `Wireless Camera Pairing` | HU 33 | 6 | 更名（原 `Wireless Camera Setup`）；刪 HU 32 |
| 8 | `AUX Camera Settings` | HU 29、30、31、34.9、34.10 | 53 | — |
| — | `Camera App` | — | 0 | **撤組** |
| | 合計 | | **230** | 未歸屬 0 |

決策測試（§4.1.3）：每組篩出者共享 setup（同 PROXI 配備＋同入口）；`Wireless Camera Pairing` 6 列為 genuine outlier（pairing 流程之 setup 與其餘 AUX 組皆異）。
名稱歸 Pei；准則執行層以 `layer2_assign.tsv` 重跑並回填。

## §五　執行層之下一包（CAM-02，待 Pei 裁 §三後發）

1. 依裁定改 `DECISIONS.md` 六項為 `[DECIDED]`；DR-CAM-b／c／d 結案；A-CA10 改 RESOLVED；登 A-CA15～17。
2. `layer2_assign.tsv` 依 §四重跑；`framework.md` 落檔（A 本 9 組不動、B 本 8 組），狀態「草案，待 Pei 簽核」。
3. `lint036.py` 實作 `Z`（Vehicle Model 七欄 1/0、598／5210 恆 0、五欄至少一 1）。
4. `crossref_a_b.tsv` 之 68 筆逐列語意核對，`status` 由 `[PROPOSED]` 改 `[VERIFIED]`／`[REJECTED]`。
5. `PowerShutDownNotifcation`、`CmdIgnSts` 兩處依 §二更正落檔。

## §六　不處置（登記備查）

- `sources/MANIFEST.tsv` 重複 doc_id `r1lr_atl_h_25pi3_5_cabin_cfts_020_ics_and_dcsd_20250910_1124`：歸 GC 線，Camera 不動。
- `RULINGS.sha.tsv` 未重生：全域檔，待 Pei 於 GC 線排程。
- 執行層自 `REF/` 取六件 docx 並代搬 `_intake/Camera/`：來源路徑正確（即 CAM-01 §1 所指資料夾），**不需重做**；`_intake/Camera/` 依 R-G46 由 Pei 清空。
