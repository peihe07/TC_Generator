# SEC-09 審閱 —— 上繳包 `up/20260917_SEC-09.md`（CCVR SYS2_Mapped 落地審計）

審閱層：分析層，2026-09-17。
判定：**通過，不升級**（越權斷言 0；`AXIS_GAP` 5）。審計把「我記了但沒落地」的三件事量化了：**六衝突 0／6 落 Remarks、8 個 CP 負向 TC 0 個有範圍註**——不是執行層漏寫，是**現行裁定沒有要求**（Remarks 體例無此欄）。故處置是新裁定＋Revise，不是返工。

---

## 一、裁定（併 SEC-10 落檔，R-SEC22）

```text
R-SEC22  CCVR 衝突與範圍之 Remarks 定式
  (a) 衝突註：TC 之 SWE1 列落於 CCVR `Mapping notes` 所列衝突者，Remarks 加一行
      `conflict: SYS-RA-SEC-<a> vs <b> — <十字內短述>; unresolved per CCVR Mapping notes row <r>`。
      C1＋C4 同列同落點（CP-007／009）→ 合為一行：`conflict: 543/546 vs 226; 517 vs 520 — root & CA lifecycle; unresolved per CCVR Mapping notes row 12`。
      C2 → SWDL-001~006（`344 vs Auth-Prog item 7 — rollback; row 13`）；C3 → KI-016~019（`376/509/511 vs 510 vs 514 — key strength; row 14`）；
      C5 → CP-007~010（`529/383/532/539 — DCL not deployed; row 10`）；C6 → ECUC-010（`562~565 — CSR format evidence pending; row 15`）。
  (b) 範圍註：Cert Provider 之負向 sibling 與 CP-004／005 全部 sibling（8 TC），Remarks 加
      `scope: service-level validation; reprogramming rejection owned by SWDL (CCVR Cert Val CS.98 rationale)`。
  (c) CCVR 引用一律用 `item <n>`（sheet 編號欄），不用 `row`；`NR1L-SWDL-005`／`-006` 之 `rows 3/5/6/7/8` 改 `items 3/5/6/7/8`。
  (d) 以上皆 Remarks 增行，交付欄不動；lint 不新增判項（Remarks 非檢查面）。
```

## 二、`AXIS_GAP` 五條之裁

| new_id | 裁 | 理由 |
|---|---|---|
| `NEW-CERT-02`（憑證到期／過時清理）| **不拆；轉 RD 回饋 A-SEC-8** | 037 CP-007／009 原文無到期／稽核語意，拆即越 037（§8.4.2）|
| `NEW-ID-01`（recovery 後不得生效）| **執行層先 grep**：ECUCert 037 十三列 Description／VC 含 `reboot`／`recovery`／`restart` 者列出；有 → SEC-10 拆一 environment sibling（IN §8.3 持久化軸）；無 → A-SEC-9 回饋 | 只在 037 有載時拆 |
| `NEW-BOOT-02`（跨簽 bus image 被拒）| **改判 COVERED-by-other**：KI-003 之 non-platform（R1LRefresh）負向 sibling 即此軸 | 落點 SWE1 指錯，非缺 |
| `NEW-NET-01`（外部 TLS）| **改判 `NO_SWE1`**；`trace_matrix.tsv` 之 `new_test_ids` 對 SWDL 之鏈結為 SEC-01 分析層產物之偶然重疊，登 **A-26**，SEC-10 清除該指派 | 題域不同 |
| `NEW-OS-01`（SELinux 關閉失敗）| **改判 COVERED-by-other**：`NR1L-KI-022`（KI-012 sibling 1，avc denied）| 同上 |

→ 新拆 sibling 最多 1 條（視 grep），無系統性漏洞。

## 三、`NO_SWE1` 十一條
CCVR 自有題域（網路／BT／Wi-Fi／OS／診斷權限／流程），不由 Security 承接；列為 **A-SEC-10**「CCVR 新測項無 037 對應」一併回饋 RD／037 作者——這是 SWE.1 層的覆蓋缺口，不是 SWE.6 的。

## 四、執行層自報之處置
初版 `AXIS_GAP` 15 → 5 之判準修正：准，且**正是該做的**（泛詞造成假陽性，修正後有反證）。`assessment` 欄→`Mapping rationale / gaps` 之更正：准，分析層欄名錯登 **A-27**。

## 五、分析層自報
A-26　SEC-01 `trace_matrix` 之 `new_test_ids` 把 `NEW-NET-01` 指到 SWDL——只看 SYS2 ID 交集未看題域。
A-27　SEC-09 §2 指「assessment 欄」，實為 `Mapping rationale / gaps`。

## 六、SEC-10 預告（合併 SEC-08／09 之裁定，一次 Revise → `security_v05`）
1. R-SEC20(amend)、R-SEC7(a)(c) amend（DOC）、R-SEC21(b) amend（步驟型）、R-SEC22 落檔；`H` 之 security 豁免。
2. Remarks 增行：衝突 6 組（19 TC）、範圍 8 TC、`rows→items` 2 TC；CP-008 尾綴。
3. `NEW-ID-01` grep → 0 或 1 sibling；`trace_matrix` 清 `NEW-NET-01` 指派；`audit_newtc.tsv` 重分類（COVERED 32／AXIS_GAP ≤1／NO_SWE1 12／DEFERRED 1）。
4. v05 ＋ 交付候選同名覆蓋；lint `U=0`、`H=0`、門檻 0。
5. ANOMALIES A-SEC-8~10 落 `ANOMALIES.md`，供 Pei 回饋 RD。
