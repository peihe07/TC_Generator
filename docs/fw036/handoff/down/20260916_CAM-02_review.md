# 下放包 CAM-02 審閱（分析層 2026-09-16）

對應上繳包：`docs/fw036/handoff/up/20260916_CAM-02.md`
判定：**六項任務全數完成**；升級條件命中之一項（Market Config 查無品牌欄）為下放包指錯來源，執行層之替代來源可採。framework 已落檔，待 Pei 簽核。

---

## §一　分析層自身錯誤之承認

| 處 | 錯誤 | 更正 |
|---|---|---|
| R-CAM5(c) 指 `SR24 R1 Market Configuration Table` 為品牌來源 | 未先查表即指名；該表軸為市場／國別 | 品牌來源改為 **PROXI `Brand_Configuration_2`**（byte 130 bit 0–4，Format row 566 十六值枚舉）；見 §三-1 |
| CAM-01 上繳 §6／審閱 §三-3「Fiat = `Rear View Camera Delay [CR14730]`」 | 分析層在 CAM-01 下放前自行讀表時同樣略過空欄；審閱未複核即沿用 | D48 = **Maserati**；Fiat／Alfa 空欄；已由執行層四處改寫（A-CA18） |
| CAM-02 §3／§4「五條」 | 六個 code block 而寫五條 | 第六條（Out of Scope 處置）**立號 R-CAM9 成立**，非誤立 |

## §二　執行層報告之確認

1. **品牌實測**：HDCC27／DT27／637 = `11 RAM`、Toro(2261) = `1 Fiat`、Fastback(376) = `5 Abarth`。
   套回 `Brand-Specific Names` 欄別：RAM 系取 `ParkView …` label；Fiat 欄空、Abarth 無欄 → 回落基礎 label。
   **品牌軸實為兩分支**（RAM 系 vs 基礎），與車型軸之關係：RAM 分支 = {HDCC27, DT27, 637}，基礎分支 = {2261, 376}。
   結論：品牌軸與車型軸在 Camera 設定項上**完全對齊**，R-CAM5(d)「車型軸為外層」即足，不會產生車型 × 品牌之交叉列。
2. **lint Z**：`FEATURE_CHECKS = {"camera": ["Z"]}` 之獨立結構**接受**（包內授權成立）；乾跑 11,368 筆命中證其為既有本七欄從未填值，非誤報，故 Z 不得入全域預設之判斷正確。
3. **crossref**：53 VERIFIED／15 REJECTED；B 本 175 列未配對為 HMI 獨有內容，屬正常，Phase 3 以 B 本自身來源生成，不需 A 本對應。
4. **雙 ABBR**：`feature_config.py` 無 schema 驗證，直接可寫；未擴 schema，符合 §5。
5. **9 個既有測試失敗**：逐項查明皆他線（time_management／user_profiles／vehicle_category／vehicle_setting 直呼 `openpyxl.save`、scaffold 夾具、`vs_cf01_selfcheck.py` 未登簿），Camera 不動；登記於 §五 供 GC 線。

## §三　待 Pei 裁

| # | 項 | 建議 |
|---|---|---|
| 1 | R-CAM5(c) 之來源改為 PROXI `Brand_Configuration_2` | **准**。依 R-TM13 原句加刪除線保留、新句另立，body sha 重算回報；DR-CAM-e 改為「確認型」（PROXI 值即證據，可結） |
| 2 | R-CAM9 立號 | **確認成立** |
| 3 | Z 未登 `GATES.tsv`（R-G56） | 登錄須改全域生成器 `gates_tsv.py` 讀 `FEATURE_CHECKS` ＋ 重生全域 tsv → **歸 GC 線排程**；Camera 交付前必須完成，Phase 3 生成不受阻 |
| 4 | `FEATURE_CHECKS` 新結構 | **准**，並補記於 `docs/fw036/RULINGS_LEDGER.md` 待登項（feature 專屬 lint 代號之機制，全域）—— 由 GC 線落條 |
| 5 | **framework 簽核**（Part VIII，行 1466–1644） | 建議簽核：A 本 9 組 25/25、B 本 8 組 230/230、未歸屬 0、歧義 0。簽核不涵蓋 `-023` 交集承接順位（見 6） |
| 6 | `-023` 六組交集之承接順位 | 建議：**先到者承接**（SWE ID 較小者為主列，`-023` 只承接無他列覆蓋之來源），reasoning 註明委派；於 pilot 批實測後再定案 |
| 7 | A-CA20（`-025` 畫面文字：037 `Camera Not in position` vs SYS1 `Camera Out of Position`） | 兩本各依自身來源寫 ER，reasoning 互註衝突；**最終文字以 `forms/Pop Up List HMI R1 (26PI).xlsx` 為準**（HMI L&F 慣例「refer to R1 HMI popup list for final text」），執行層下包查該表並回報 |
| 8 | 下一步 | **CAM-03 pilot 批**：A 本 `Display Arbitration`（`-015`／`-016`／`-018`）＋ `Startup and Shutdown`（`-001`）各出 2–3 TC，合計 ≤ 10，含一組車型軸拆分與一組品牌軸拆分，用以驗 R-CAM2／3／5 之寫法與 lint Z；通過後再開全量 |

## §四　CAM-03 之前置（准 §三-5／8 後由分析層發包）

- R-CAM5(c) 修訂落檔；DR-CAM-e 結。
- Pop Up List 查 `-025` 文字。
- pilot 選列與 sibling 軸設計由分析層在 CAM-03 下放包內指定。

## §五　不處置（登記備查，GC 線）

- `gates_tsv.py` 不讀 `FEATURE_CHECKS`（§三-3）。
- 9 個既有測試失敗（15 腳本直呼 `openpyxl.save`；scaffold 夾具 6 項；`vs_cf01_selfcheck.py` 未登簿 commit `2c08a48`）。
- `sources/MANIFEST.tsv` 重複 doc_id（CAM-01 審閱 §六已記）。
