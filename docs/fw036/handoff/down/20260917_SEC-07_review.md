# SEC-07 審閱 —— 上繳包 `up/20260917_SEC-07.md`（PENDING 內查：128 → 112，`security_v03`）

審閱層：分析層，2026-09-17。
判定：**通過，不升級**。四項升級條件皆不成立。內查只解 16 行（預估 30~45），**落差成因全在分析層之兩個前提錯誤**；執行層對前提錯誤之處置（改抽 pytest、不硬配、不自裁 PHYS 清單）**正確**。
本包最有價值之產出不是 16 行補值，而是 **§5 四項矛盾**——其中兩項可能改變 KeyInstall 的母體理解，須送 RD／037 作者。

---

## 一、通過項

| 項 | 判 |
|---|---|
| Z2 ≡ Z3、Z4 ≡ Z1 解壓樹；MANIFEST 只登 2 | ✅ |
| `Test_Items.txt` 15 條 = Z2 `test_cert_provider.py` 常數表；zip 新增 29 方法（Z1）| ✅ 這解釋了 `Test_Items.txt` 的來歷 |
| `X-f-2` 解 12 行：`getInstalledKeysStatus`（high）、`encryptDecryptNormalFlow`（medium）逐字自 Z1 行號；KI-010／012 不硬配 | ✅ |
| `<installstate file>` 佔位解為 `/mnt/vendor/oemkeys/installstate`；四態 `0x00~0x03` 與 `status/keyinstall` 8 bytes 逐字 | ✅ **DR-SEC-o 據此結案**：四態存於檔、DID 面另計 |
| `X-l` 解 4 行：`31 01 F0 00` → `71 01 F0 00 00/01`，bit field 語意引 `SYS-RA-CS00102-685` | ✅ |
| SWDL-003 解密路徑不移植 Check Program evidence（R-SEC15(a)）| ✅ 正確 |
| `X-n` 找到 pm_73 寫法但不套（PHYS 清單外、且 power_moding 自身 S2R 觀察面亦 PENDING）| ✅ 正確；**不 amend R-SEC7(a)**——沿用一個本身 PENDING 的寫法沒有意義，等 power_moding 的 DR-PW26 結案再議 |
| `apk_pairing.tsv` +2 列（R-SEC15(g) 之必要條件）| ✅ 准 |
| v03 lint 門檻 0、`K=0`、selfcheck 0；`pending_summary.tsv` 加 `class` 欄（96／16）| ✅ |
| `delivery/` 0916 本刪除、`merged/security_v02` 保留 | ✅ 准；交付候選草稿以 merged 本回溯即可 |
| SEC-06 `asset_request` 逐 token 行數手抄錯（總數對）| ✅ 已自檔重算；登執行層自報 |

## 二、分析層自報（登台帳）

| # | 錯誤 | 說明 |
|---|---|---|
| A-22 | SEC-07 §2.1 假設 zip 為 Java／`@Test` instrumentation 專案 | 實為 host 端 pytest 套件驅動 `am instrument`。沒打開就寫 |
| A-23 | SEC-07 §2.3 假設 Z2／Z3 含測試憑證與 wrong-* 方法 | Z2 只有 3 KB、四個檔；`X-e` 系列 0 解，是預估落空主因 |
| A-24 | §2.6 預估 30~45 行 | 建立在 A-22／A-23 之上 |

## 三、§5 四項矛盾之處置（Pei 送 RD／037 作者；分析層建議附後）

```text
DR-SEC-t  KeyInstall 匯入機制與狀態值（合併三問，送 KeyInstall 負責人 ＋ 037 作者）
  (1) Z1 `keys_install_helper.py` L53 註解「USB external storage path removed; raw keys are now staged in the secure
      oemkeys partition」。037 KI-002／003／007 之 GIVEN/WHEN 皆為 USB 插入。**現行實作是否已無 USB 匯入路徑？**
      若是，037 三列須改版，本案 6 個 KI TC 之 PHYS 前提隨之作廢。
  (2) 037 KI-003 之 `status 0900 0000 0000 0000` 不見於 Z1；Z1 之 `status/keyinstall` 成功值為 `00 00 00 00 00 00 00 00`。
      **哪個是現行值？**
  (3) Z1 `test_can_injection.py` 含 RID 0x9001／0x9003／0x9004 完整協定與 9 個案例（DR-SEC-k 之「定義未在手上」前提已變）。
      **CAN 注入是否為 KI 之正式匯入介面（取代 USB）？** 若是，037 應增列，本案再開 CAN 注入 TC 群。
  未答前：TC 依 037 原文不動（R-SEC1(a)）；矛盾登 ANOMALIES A-SEC-4~6。
```

分析層建議：**不先開 CAN 注入 TC**——037 未載即不在 SWE.6 範圍（§8.4.2）；Z1 是 RD 自測，不是需求。但這三問要**一起問**，因為三者指向同一件事：KI 的 037 可能落後實作一版。

第 4 項（`AndroidAuto` 為第三個金鑰客戶）登 A-SEC-7 備查，不阻。

## 四、DR 結案更新

- **DR-SEC-o 結案**（四態存於 `/mnt/vendor/oemkeys/installstate`，Z1 逐字；DID `22 FF 02` 仍只入 Remarks，R-SEC8(e) 不變）。
- **DR-SEC-k 改為併入 DR-SEC-t(3)**。
- 新增 DR-SEC-t。

## 五、現況與下一步

| 項 | 值 |
|---|---|
| 交付候選 | `delivery/…_SWQT_Security_20260917.xlsx`，100 TC，PENDING **112**（AWAIT_ASSET 96／AWAIT_RULING 16）|
| 內查已盡 | 三 zip、power_moding、Auth-Prog evidence 皆查過；**手上素材再無可解者** |
| 等 Pei | ① `X-i` 裁降；② DR-SEC-t 送出；③ `asset_request.md`（11 token）送出 |
| 等 RD | `X-c`／`X-d`（9/18）、`X-e`／`X-g`／`X-h`／`X-k`／`X-j`／`X-m`／`X-n`、DR-t 三問 |
| 等上游 | DR-a／i／r／s（DEFERRED 19 列解凍前提）|

**下一包**：Pei 裁 `X-i` 後出 **SEC-08**（`X-i` 16 行改文件審查定式 R-SEC20；若同時裁 DEFERRED 19 列走同一定式，一併解凍）。資產每到一批，出一次 Revise。
