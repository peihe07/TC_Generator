# 下放包 SEC-10 —— 合併 SEC-08／09 裁定之 Revise → `security_v05`

前置：`up/20260917_SEC-08.md`、`up/20260917_SEC-09.md`、兩份 review。
Pei 2026-09-17 准：兩 review 之全部裁定。
執行層 session：新開。**開工前先 commit SEC-07～09 之工作樹產物**（三支：`feat(security): SEC-07…`、`feat(security)+feat(lint036): SEC-08…`、`feat(security): SEC-09 audit`），不再讓 v03／v04 只存在工作樹。

---

## §0　禁區

- `down/`／`up/` 既有檔不動；canon 不動；`framework.md` 不動（LOCKED v03；本包不動 Layer 2）。
- `lint036.py` 只動 `FEATURE_EXEMPT["security"]`（`H` 之引文豁免）；回歸三路徑 byte 相同。
- 既有 xlsx 不動；v05 為新檔。
- Remarks 增行**只增不改**既有行；交付欄除 §3-3 之 CP-008 尾綴與 §3-4 可能之 1 sibling 外**不動**。

## §1　裁定落檔（取號現查；預期 R-SEC20(amend)、R-SEC7(a)(c)(amend2)、R-SEC21(b)(amend)、R-SEC22）

```text
R-SEC20(amend)  適用面 = 037 VC 以 source code／build files／dependency graph 為受詞之列
                （CP-006 全部 sibling、CP-008 前兩 THEN、KI-012 之 Android.bp／Static Analysis 兩 sibling）；
                KI-013／CP-010 之 VC 為實機量測（procrank／df），不適用。混合型（CP-008）之 test_item 尾綴
                `(document review + log observation)`。
                (c) 補句：ER 引 037 逐字要件時，引文內之關係模糊語不受 IN §6 校準（lint `H` 對 `"…"` 內豁免，限 security）。
R-SEC7(a)(c) amend2  通道加 DOC：`<obtain <artifact> per RD>`／`Review <artifact> against "<要件>"`，免 `$` 行；
                     觀察面加 DOC：`<artifact> are available for review`／`<artifact> satisfy "<要件>"`。
                     出處：037 CP-006／008 VC `Inspect the build files`／`Inspect the source code`；KI-012 Verification Method `Document Review`。
R-SEC21(b) amend  第四型「步驟型」：`<<性質> provided by RD (X-<n>)> is executed` ＋ 次行 `$ <command provided by RD (X-<n>)>`；
                  「性質」逐字取原 PENDING 文字，不補 TC 語境；示例改為 `<log keyword provided by RD (X-h)>`。
R-SEC22  CCVR 衝突與範圍之 Remarks 定式
  (a) `conflict: <SYS2 對照> — <十字內短述>; unresolved per CCVR Mapping notes row <r>`
      CP-007／009 全部 sibling（`NR1L-CP-015`／`-016`／`-019`／`-020`）：`conflict: 543/546 vs 226; 517 vs 520 — root & CA lifecycle; unresolved per CCVR Mapping notes row 12`
      SWDL 全部（`NR1L-SWDL-001`~`-006`）：`conflict: 344 vs Auth-Prog item 7 — rollback; unresolved per CCVR Mapping notes row 13`
      KI-009／010 全部 sibling（`NR1L-KI-016`~`-019`）：`conflict: 376/509/511 vs 510 vs 514 — key strength; unresolved per CCVR Mapping notes row 14`
      CP-004／005 全部 sibling（`NR1L-CP-007`~`-010`）：`conflict: 529/383/532/539 — DCL not deployed; unresolved per CCVR Mapping notes row 10`
      ECUCert EXPORTCSR（`NR1L-ECUC-010`）：`conflict: 562~565 — CSR format evidence pending; unresolved per CCVR Mapping notes row 15`
  (b) `scope: service-level validation; reprogramming rejection owned by SWDL (CCVR Cert Val CS.98 rationale)`
      → `NR1L-CP-002`／`-004`／`-006`／`-007`／`-008`／`-009`／`-010`／`-023`
  (c) CCVR 引用一律 `item <n>`；`NR1L-SWDL-005`／`-006` 之 `rows 3/5/6/7/8` → `items 3/5/6/7/8`。
  (d) 皆為 Remarks 增行；lint 不新增判項。
```

## §2　`lint036.py`

`FEATURE_EXEMPT["security"]` 加 `H`：只豁免命中位置落於 `"…"` 內者（非整項豁免）。回歸三 sha；v04 重跑預期 `H=0`。

## §3　產出變更

### 3-1　Remarks 增行（R-SEC22）
19 TC 加 `conflict:`（4＋6＋4＋4＋1）；8 TC 加 `scope:`；2 TC `rows`→`items`。逐 TC 列於上繳包。

### 3-2　`trace_matrix.tsv`
`new_test_ids` 欄清除 `NEW-NET-01` 對 SWDL-001／002／003／005 之指派（A-26）；`audit_newtc.tsv` 重分類：`NEW-NET-01`→`NO_SWE1`、`NEW-BOOT-02`→`COVERED`（by KI-003）、`NEW-OS-01`→`COVERED`（by `NR1L-KI-022`）；預期 COVERED 32／AXIS_GAP ≤1／NO_SWE1 12／DEFERRED 1。

### 3-3　CP-008 尾綴
`NR1L-CP-017`／`-018` 之 test_item 下半改 `(… (document review + log observation))`。

### 3-4　`NEW-ID-01` 之 grep
ECUCert 037 十三列 `Requirement Description`／`Verification Criteria` grep `reboot`／`recovery`／`restart`／`power cycle`（大小寫不計）；
- 有命中 → 在命中之列下拆 1 environment sibling（R-SEC18／IN §8.3 持久化軸；觸發 `Power cycle HU (IGN OFF → IGN ON)`；ER：損毀鏈於重開機後仍未被使用，觀察面沿 CS.165 `ECU_INSTALLATION_STATUS` log），TC ID 接 ECUC 末號；`sibling_plan.tsv` 加列。
- 無命中 → 不拆，`ANOMALIES.md` 登 A-SEC-9。

### 3-5　`ANOMALIES.md`
登 A-SEC-8（`NEW-CERT-02` 憑證到期／稽核軸，037 無載）、A-SEC-9（依 3-4）、A-SEC-10（`NO_SWE1` 12 條清單，CCVR 新測項無 037 對應，SWE.1 缺口）。

### 3-6　重跑
`SEC_VER=v05` 重跑三支生成 → `merged/security_v05.xlsx`、`delivery/…_SWQT_Security_20260917.xlsx`（同名覆蓋）；`placeholder_summary.tsv`／`by_token` 不變（96／11）除非 3-4 拆列；`coverage.tsv` 51／19 不變（3-4 拆列不改列數，只改 `tc_count`）。

## §4　上繳要求（`up/20260917_SEC-10.md`）

1. commit 三支之 hash。
2. 四條裁定實際號、sha8、`--gate`。
3. `H` 豁免 diff、回歸三 sha、v04→v05 `H` 1→0。
4. Remarks 增行逐 TC 表（29 TC）；v04→v05 逐格 diff（預期只在 `remarks` 29 格 ＋ `test_item` 2 格 ＋ 3-4 可能之新列）。
5. `NEW-ID-01` grep 結果與處置；`audit_newtc.tsv` 四類計數；`trace_matrix` 清除之列。
6. v05 lint 全項（`U=0`、`H=0`、門檻 0、`W=4`）；selfcheck 0。
7. `ANOMALIES.md` 三條全文。
8. 自報錯誤。

## §5　升級條件

- v04→v05 diff 超出 §4-4 所列範圍。
- `H` 豁免使非引文內之 `H` 命中被吞（執行層以構造之反例驗證：一條 ER 在引號外含 `align with` 須仍報）。
- 3-4 拆列時 037 命中句無法逐字引為 Pre-Condition 或 ER。
- lint 回歸 sha 改變。
