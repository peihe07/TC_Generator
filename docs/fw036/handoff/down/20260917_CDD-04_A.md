# CDD-04 追補 A —— 執行層讀包回饋之兩處採納

前件：`down/20260917_CDD-04.md`。執行層讀 CDD-03 審閱後回報兩處量測，分析層採納，隨包生效。

## 一　`SEC-DIAG` 之判定改依母節，不依位元組串

CDD-04 T1(d) 原書「Procedure 含 `$ 2F` 而 PC 無 `0x27` 行 → FAIL」。
執行層實測：pilot `-006` 之觸發步驟整行為 `PENDING: DR-DIAG-4`，**無 `2F` 位元組串**，grep 會漏。

改為：`SEC-DIAG` 之母體 = `layer3_assign.tsv` 中母節為 **I/O Control DIDs** 之 037 列所產之全部 TC
（以 Requirement ID 反查），或 Remarks 含 `SID per 037` 且該 SWE1 列型為 I/O 者；
命中母體而 Pre-Condition 無 `Security access 0x27 has been granted` 行 → FAIL。
R-DIAG13 之 Revise 範圍因此為 pilot **4 條**（`-006`／`-007`／`-008`／`-013`）—— 與審閱所書一致，惟判定法以本追補為準。

同理，`KEY-DIAG` 之母體 = 母節 `$1820`／`$1821` 之 TC，不以 Procedure 有無讀取串為條件。

## 二　R-DIAG14 之 Revise 範圍只有 `-043`

執行層實測 `-044` 無觸發步驟（只讀取後斷言不相容鍵回報 `Not Pressed`），不涉 R-DIAG14。
CDD-03 審閱 §三-2「`-044` 若同用 Power 亦改」之條件不成立，撤該句。**分析層自報 A15**：未查 `-044` 全文即附帶點名。

## 三　GC-16 之時點

執行層回報 Security 工作區仍有 30+ 檔 modified，GC-16 現在跑必再撞。
**GC-16 維持「Pei 確認兩線停止並 commit 後」才開工**；在此之前 CDD-04 照常執行，指紋依 R-G75。
