# 下放包 CAM-12 審閱（分析層 2026-09-23）

對應上繳包：`docs/fw036/handoff/up/20260923_CAM-12.md`
判定：**batch03b 修正通過；batch03c 36 列判過（第 7 批）**。執行層自報 §5-1（CAM-11 之「零命中」未經量測即書寫）為本線最嚴重之一次，本審閱獨立複驗其本輪之同類主張並立條防再犯。CAM-13 同發。

---

## §一　分析層獨立複驗（針對自報 §5-1）

以 `forms/PDT27_E2A_R1_FDCAN8.dbc`（iconv latin1→utf-8 後 grep）複驗本輪 §2-1 之 `-152` 主張：
- `^BO_.*STEERING`：**0**；`LwsAngle_SCCM`：**0**；`SCCM` 僅 `DIAGNOSTIC_REQUEST/RESPONSE_SCCM` 二訊息（非 LWS）；`LwsAngle` 唯一 `SG_` 在 `BO_ 264 EPS_FD_1`。
→ 本輪主張**屬實**；「不以 `EPS_FD_1` 代換」正確（R-13）。

## §二　裁決

| # | 項 | 裁決 |
|---|---|---|
| 1 | **量測主張之證據**（自報 §5-1） | **立 R-CAM17**：上繳包中任何「查無／零命中／全等／逐字相符」之主張，須附**掃描字串、母體檔名、命令與命中數**；無證據之主張視同未量測，審閱不採。分析層每包隨機複驗至少一項（本包已行）。同時登 GCB-08 提請入全域 ledger |
| 2 | HDCC27 之 FD-CAN8 DBC 不在素材 | **DR-CAM-f 擴描述**：加「HDCC27 FD-CAN8 DBC 全本」；`-152`／`-153` 之 PENDING 改指此 |
| 3 | `RESPONSE_TIME` 2,0 s／`Time_POWER` 500 ms／`Tpower`(V42) 5 s | **入 profile §9** |
| 4 | `-024` 全 BLOCKED 之交付台帳形制 | 工作簿**不出列**；`coverage.tsv` 記 `BLOCKED — DR-CAM-a`；交付包附未結 DR 清單（canon §8.4.3）。是否降 NA 由 Pei 於交付時裁 |
| 5 | V3 `-186`～`-188` 一句涵蓋 set／clear、四步走完不拆 | 接受（條文結構相異） |
| 6 | `remarks` 欄新增（json → AH 欄） | 接受；profile §10 記其用途限「來源與 DBC 名稱差異」「疑誤植」「需特殊治具」三類 |
| 7 | DR-CAM-q（DTC 碼值）14 列 PENDING | 接受；`Diagnostics` 組交付前必結 |

## §三　抽讀

本輪未抽讀 TC 內容（§一之複驗替代）；batch03c 之 `A`=6／`X`=2／`L`=1 初稿問題皆由 lint 攔下，工具覆蓋成立。

## §四　備查

- A 本只餘 `HMI Overlays`（`-021`／`-022`／`-023`／`-025`）；`-025` 之畫面文字 DR-CAM-h 未結。
- DR 十三條未結；f／i／q 交付前必結。
- R-G14 第 7 批。
