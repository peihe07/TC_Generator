# 下放包 SEC-13 —— apk 檔名 Revise（A-33）＋ `EXEC_GUIDE.md` 環境知識節 → `security_v08`

前置：`up/20260917_SEC-12.md`、`down/20260917_SEC-12_review.md`（裁定即本包依據；Pei 異議於上繳後撤）。
執行層 session：新開；先落 SEC-12 兩支 commit。

---

## §0　禁區
- `down/`／`up/` 既有檔不動；canon／framework／lint 不動；既有 xlsx 不動（v08 新檔）。
- 本包 TC 內容變更**只限** §1 之 19 格 Pre-Condition；其餘欄不動。
- 指南環境知識節之每一行**必附來源檔與位置**；該節以外仍受 ⊆ 100% 約束。

## §1　R-SEC24 —— apk 檔名更正

```text
R-SEC24  CertProvider 測試 runner 之 apk 檔名（A-33 更正）
  §5.3 常數 `CP_TEST_RUNNER_INSTALLED` 改為 `Test runner CertProviderAndroidInstrumentalTest.apk is installed`
  （檔名逐字取 `CCVR/To Validation team/Cert Val CS.98/CertProviderAndroidInstrumentalTest.apk`，MANIFEST 外部路徑登錄）。
  `CertProviderServiceManagerTest` 仍為 instrumentation class 名，只出現於 `am instrument` 指令與 apk_pairing。
  既有 19 TC 之 Pre-Condition 依 R-G72 Revise；profile §3 常數表同步。
```
執行層：`gen_batch1.py`／`gen_batch2.py` 之常數改；`SEC_VER=v08` 重跑 → `merged/security_v08.xlsx`；v07→v08 diff 預期 **19 格**（皆 `pre`）；交付候選同名覆蓋。RULINGS 落 R-SEC24，`--gate`。

## §2　`EXEC_GUIDE.md` 增節（`build_exec_guide.py`）

在 §1 之後、§2 之前插入：

```text
## 1.6 Environment knowledge (not from test cases)
Each line below is cited to a source outside the workbook; nothing here is asserted by any test case.
- Runner install: `$ adb install CertProviderAndroidInstrumentalTest.apk` — file: CCVR/To Validation team/Cert Val CS.98/ (SEC-01 listing)
- Runner check: `$ adb shell pm list instrumentation` — CCVR SYS2_Mapped, sheet `Cert Val CS.98`, STEPS column
- KeyInstall / KeyMaster runners: install per Z1 `IntegrationTests/README.md` (KeyInstall_IntegrationTests.zip) — apk file name not stated there → `<apk provided by RD>`
- Root shell: `$ adb root` — Z1 `PythonTests/common/adb_wrapper.py` (line n)
- installstate values: 0x00 NOT_INSTALLED / 0x01 VERIFICATION_REQUIRED / 0x02 INSTALLED / 0x03 ERROR — Z1 `keys_install_helper.py` L204–L212
- status/keyinstall 8-byte values: success `00 00 00 00 00 00 00 00`, verification `02 00 00 00 00 00 00 00` — same file
- Diagnostic CAN IDs: Atl-Hi 0x7BF/0x53F, Atl-Mid 0x18DA87F1/0x18DAF187 — VHAL_User_Guide_R5.pdf §2.4
- Architecture DID: `$ 2E 28 50 03` (Atl-Mid) / `$ 2E 28 50 05` (Atl-Hi) — VHAL_User_Guide_R5.pdf §2.4
- Extended session `$ 10 03` — ISO 14229-1 standard value (domain constant)
- Test material location on host: `SecurityAssets/oem-certs/cert-provider/` — 037 CertProvider verification criteria (already in TC)
- pytest harness (RD): `$ pytest` / `$ pytest --html=report.html --self-contained-html` — Z2 `README.md`
```
每行之來源由執行層現查（行號逐字）；查無者該行**刪除**，不留佔位。⊆ 驗證改為「§1.6 以外」；另加驗證「§1.6 每行含 ` — ` 來源片段」。

## §3　其他小修
- §5 自動化表：`PENDING-ASSET` 改為表下註。
- 指南 §1.1 之 apk 檔名隨 R-SEC24 更新。
- `EXEC_ASSETS.md`：`X-f-2` 加註「apk 檔名待 RD」。

## §4　上繳要求（`up/20260917_SEC-13.md`）
1. SEC-12 兩支 commit；R-SEC24 實際號、sha8、`--gate`。
2. v07→v08 diff（預期 19 格 `pre`）；lint 全項；selfcheck。
3. §1.6 逐行來源查得表（含行號）；刪除之行；兩項驗證結果。
4. 交付五件之 sha。
5. 自報錯誤。

## §5　升級條件
- diff ≠ 19 格或落在 `pre` 以外。
- §1.6 任一行無來源片段；或 §1.6 以外 ⊆ < 100%。
- lint 任一門檻非 0。

## §6　本包後之狀態
Security 文字版**定稿等資產**。後續觸發：RD 資產（Revise）、DR-a/i/r/t 回覆（Revise）、`W=4`（Pei）、SEC-14 自動化骨架（Pei 指示時）。
