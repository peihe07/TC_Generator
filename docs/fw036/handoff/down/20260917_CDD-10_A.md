# CDD-10 追補 A —— Pei 裁：延伸「要拆、要詳細、當然要改」；審計改為產出

前件：`down/20260917_CDD-10.md`。Pei 2026-09-17 口頭裁定，本檔落字。**本追補取代 CDD-10 §0「只列不產」與 T2「候選只列不產」**；其餘不變。

## 一　裁定落字

```text
R-DIAG24  SWE1 = 037 全欄；VC／VM 為 sibling plan 之必讀輸入
  (a) 037 之 Verification Criteria／Verification Method 欄與 Description 同為 SWE1 依據；
      sibling plan 逐列抽出 VC/VM 之情境詞（狀態／條件／邊界／逾時／重試／各欄位…），每一情境至少一條 TC。
  (b) 情境之值依序取：該列所引錨 → 同 $XXXX 節內之 CFTS004 列舉／值域（引兩錨）→ `<…>` 佔位（R-DIAG15 登錄）。
      VC/VM 只供情境，不供值（IN §8.4.1）。
  (c) VC/VM 之 unit／integration 字樣不在 SWE.6 層級，略過；純 boilerplate 句（無情境詞）不產。
```

```text
R-DIAG25  詳細度
  (a) 一列所引錨或其 VC/VM 列舉之每一狀態／值，各成一條 sibling（狀態軸），PC 寫該態、ER 斷言該態下之回報；
      態之建立方式 CFTS004 未載者不寫步驟，只寫態（§8.5 例外）。
  (b) 同節其他列已各自成 TC 之欄位，不重複斷言（§8.2.1 不變）；但該列自身之錨列舉多個值／子項者，ER 依 §6.1 逐項。
  (c) 單一 SWE1 列拆分上限 8；超過者回報不產，Pei 裁。
```

分析層自報 **A32**：CDD-01 將 VC/VM 整欄判為 boilerplate、CDD-02 定「Description 字面為範圍」，致 037 之驗證意圖未入 sibling plan。

## 二　T2 改為「審計 ＋ 產出」

(a) 先出 `extension_audit.tsv`（欄位同 CDD-10 T2），**再依其產出**：`feasible = Y`／`P` 者全數產 TC；`N` 者只列不產並註由。
(b) 前例對映（分析層預判，執行層以 CFTS004 查證為準）：
   - `$2840` Terrestrial signal／Satellite signal／Audio decoder bitrate：VM「varying antenna conditions」＋ CFTS004-4939801 之三態（OK／Disconnected／Shorted）→ 各列 3 條，PC 寫天線態，引兩錨。
   - `$500B` `-007`～`-011`：VM「menu enable/disable states」→ enable／disable 各自之 HMI 可觀察斷言分項（CFTS004 明載之 in-motion menu items 逐項 a./b./c.）；`-012` timeout 已有。
   - `$2841`／`$2842` OBER／TWER／C/N／BER「validate each … field」：現行每列一欄位已滿足；**不另拆**，審計記 `asserted_by`。
   - VC「without side effects」：negative TC 已有 `*_initial` 比對；記 `asserted_by`。
   - VC「all error scenarios」：對讀類 = 長度／unsupported DID 二軸（R-DIAG22(amend)），已滿足。
(c) ID 自 `-545` 起連號；Test Set 依 framework（LOCKED，不改組）；各批 Revise 不重編號。
(d) 產出後 lint／自檢／coverage 同前；`merged/diagnostics_v08.xlsx`；`placeholder_summary.tsv` 更新。

## 三　預期數字（取代 CDD-10 §3）

| 項 | 預期 |
|---|---|
| 候選（Y＋P）| 40–120；`N` 少量 |
| 新增 TC | 40–120（`$2840` 9、`$500B` 5–10、其餘視 VC/VM）|
| 單列拆分最大 | ≤ 8 |
| 合併本 | 544 → 585–665 |

## 四　升級條件（取代 CDD-10 §5）

- 指紋集合不符／`--check` 不綠（既存者記「既存」）。
- 任一 SWE1 列拆分 > 8。
- 新增 TC > 150（表示前七批系統性不足，須回報分析層重審全部 sibling 判準後再產）。
- 任何需改 canon 之情形。

## 五　上繳要求增補

上繳包貼入 **`$2840` Terrestrial signal 三態之一** ＋ **`$500B` enable 分項一條** 全文；`extension_audit.md` 附「依 source 之候選數／已產數／N 數」表。
