# 下放包 CDD-01_A —— Diagnostics intake 修訂（037 V1 (3) 重導 ＋ 七項裁定落字 ＋ lint 實作）

前件：`down/20260917_CDD-01.md`、`up/20260917_CDD-01.md`、`down/20260917_CDD-01_review.md`。
Pei 2026-09-17 對審閱 §五 七項**逐項准**，本包為其執行。
執行層 session：沿用 CDD-01 之 session（Pei 已裁同 session 執行，R-G62(b) 對本 feature 不適用）。

---

## §0　禁區

- git 一律不動；Pei 為唯一 git 操作者。**建議先 commit CDD-01 產物**（up §10 訊息），本包產物另一筆。
- 不動 `down/`、`up/` 既有檔；不動其他 feature 目錄。
- **不得生成任何 TC**；本包止於 intake 修訂與 lint 實作，pilot 在 CDD-02。
- `scripts/ruling_anchor.py` **只改 `RE_ANCHOR` 一處**（§2 R-G{live}），其餘全域腳本不動；`new_feature.py` 之 `feature[:2]` 缺陷**本包不修**（列全域待辦）。
- 條文本體不改（R-TM13）；所有變更以 `(amend)` 追加。
- 037 V1 (2) 之 MANIFEST 列不刪（append-only）。

## §1　母體更替

| | 值 |
|---|---|
| 新權威 | `SWE1_Diagnostics_V1 (3).xlsx`，sha16 `d317a38f249b7bd8` |
| 差異 | 僅刪 V1 (2) 末 6 列 `SWE1-Diagnostics-389～394`（`SYS-RA-DIAG-167/168`，GPS Date-Month／Day，`$2812`）；餘 389 列 × 35 欄零格差；封面仍 C 版、履歷無 D 列 |
| 新母體 | **389 列／166 源** |

查詢（分析層已測，執行層複驗）：兩本 `Analysis Report` 列 8 起以 A 欄 ID 對齊，35 欄逐格比對；`set(v2)-set(v3)` = 6、反向 = 0。

## §2　裁定落字（Pei 2026-09-17 逐項准）

`R-DIAG` 條號沿用 CDD-01（1～9）；amend 依 R-SEC16(amend) 前例以括號尾綴命名。
全域條取號 `R-G{live}`：執行層現查 `docs/fw036/RULINGS_LEDGER.md` 末號（R-G23），期望值 ≥ 73。

```text
R-DIAG8(amend)  母體依 037 V1 (3)
  母體為 V1 (3) 之 166 源 389 列。V1 (2) 之 SYS-RA-DIAG-167/168 六列不產；
  167/168 回歸 CFTS004 未收 FR 清單（50 → 52），依 R-DIAG8(a) 不補產。
```

```text
R-G{live}  裁決錨點前綴放寬至四字母
  `scripts/ruling_anchor.py` 之 RE_ANCHOR 由 `R-[A-Z]{0,3}\d+` 改為 `R-[A-Z]{0,4}\d+`。
  解析面與指紋面同源（R-G43(d)），一處改全面生效；既有 18 種前綴全在新範圍。
  改後 `rulings_hash.py` 對全 repo 重跑，既有指紋不得變動（回歸檢查）。
```

```text
R-DIAG5(amend)  SID／NRC 值之來源層級
  (c′) SID／NRC 值依序取：CFTS004 原文 → 037 Description／Title 明載值 → ISO 14229-1 標準碼。
       三者衝突時前者優先；衝突逐筆登 ANOMALIES。
  (d)  取 ISO 14229-1 標準碼者，限下列五碼，且只在 037 已述其失效型態時使用
       （IN §8.4.1 domain constant；CFTS004 自身以 $22／$31 引用同一標準）：
         0x11 serviceNotSupported                    ← 037「Service Not Supported」
         0x12 subFunctionNotSupported                ← 037「unsupported sub-function」
         0x13 incorrectMessageLengthOrInvalidFormat  ← 037「invalid length」／「incorrect format」
         0x22 conditionsNotCorrect                   ← 037「conditions not correct」
         0x31 requestOutOfRange                      ← 037「unsupported DID」／「out of range」
       ER 之 label 用 ISO 名稱；Remarks 註 `NRC per ISO 14229-1 (037 unspecified)`。
       037 未述失效型態者仍 `PENDING: DR-DIAG-1`。
  (e)  I/O Control 之 SID 0x2F 依 (c′) 第二層取 037 明載值（24 列），
       同母節其餘 90 列以同一 DID 之 037 明載值類推，Remarks 註 `SID per 037 <SWE1-ID>`。
```

```text
R-DIAG9(amend)  Layer 2/3 之實測修正
  (a) 被 037 引用之 Layer 3 為 42（R/W 23／I-O 14／Routine 5）；`$5007 - Mid-Range Settings` 未引用。
  (b) Test Set #11 `Audio Output Settings`（單列 SWE1-056）併入 #14 `Audio Tone Settings`；
      #21 `Clear Key Sense PIN` 維持獨立（§4.2 genuine outlier）。Layer 2 定為 20 個。
  (c) 跨母節依 DID 合一者共 6 個：$5000／$5001／$5008／$5009／$500A／$500C；Layer 3 各保留母節。
```

```text
R-DIAG7(amend)  Region → Vehicle Model 填法
  依 PROXI Market_Area 實測（up CDD-01 §7-2 表）：
    Region = All   → HDCC27 1／DT27 1／637 1／598 0／5210 0／2261 1／376 1
    Region = NAFTA → HDCC27 1／DT27 1／637 1／598 0／5210 0／2261 0／376 0
  空值僅 SYS-RA-DIAG-248（Out of Scope 列），不產 TC。
  `Toro_ATL_MI` Market_Area 之 Type = Not Used 記於 ANOMALIES，不影響填法。
```

```text
R-G{live+1}  Commander (598)／Regengade (5210) 全域一律 0
  R-CAM2(b) 由 Camera 兩本升為全域：FW036 Vehicle Model 之 598／5210 欄一律 0。
  已交付之 SWC 0708（202 列填 1）依 R-G72 不回修；其後任何 Revise 本一併改 0。
```

```text
R-DIAG3(amend)  Out of Scope 列之處置欄
  處置欄 = `AF` `Test Result 測試結果`，值 `Out of Scope`；`AH` Remarks 註 `CFTS004 Category: Out of Scope`。
  該列 Requirement ID 照填 SWE1-057，TC ID 照序取號（不跳號），Test Set 依 L3 歸 #10 Screen Test Pattern。
```

RD 回饋 **FB-DIAG-a～f** 六項 Pei 准送；執行層彙整為 `features/diagnostics/docs/rd_feedback_20260917.md`（給 Nik 之版本：中文、每項附 037 row／CFTS004 row／ObjectID），Pei 自行送出。

## §3　執行層任務

**T1 — 正則放寬（R-G{live}）**
改 `RE_ANCHOR`；對全 repo 重跑 `rulings_hash.py`，比對 `RULINGS.sha.tsv` 改前改後：既有列 sha8 零變動、新增 `R-DIAG1～9` 九列。回報 diff 行數。

**T2 — V3 登錄與重導**
(a) `_intake/Diagnostics/` ＋ `sources/raw/diagnostics_037_swra_v1_3/` 投 V1 (3)；MANIFEST 新列，舊列 `note` 加 `superseded by diagnostics_037_swra_v1_3 (Pei 2026-09-17)`。
(b) `feature.yaml` 之 037 指向改 V3。
(c) `data/` 九檔 tsv ＋ `layer2_assign.tsv` ＋ `cfts004_uncited_fr.tsv` 全數以 V3 重導；每檔回報 V2 → V3 列數差（預期只在 `$2812` 相關列）。
(d) `layer2_assign.tsv` 套 R-DIAG9(amend)(b)：#11 併 #14，Test Set 重編為 20 個；framework 文件 `features/diagnostics/docs/framework.md` 依 canon §4.1 寫出（Layer 1／Layer 2 20 個／Layer 3 42 個），狀態 `DRAFT`，待 CDD-02 pilot 後 LOCK。

**T3 — amend 落檔**
七條（R-DIAG8/5/9/7/3 amend ＋ R-G{live}/{live+1}）逐字落 `features/diagnostics/RULINGS.md`（feature 條）與 `docs/fw036/RULINGS_LEDGER.md`（R-G 兩條）；重跑 `rulings_hash.py`，回報全部 sha8。

**T4 — profile lint [ADD] 實作**
`lint036.py` 對 `--profile diagnostics` 啟用（實作方式依 lint036 現行擴充機制，若無機制則以 `features/diagnostics/scripts/lint_diag.py` 獨立實作並在 profile §1 記明）：
- `P-DIAG`：`$XXXX` 白名單 = `layer3_assign.tsv` 之 DID 集合（27 種）；作者側四欄出現白名單外之 `$`+4hex 報 FAIL；`$MESSAGE.Signal$` 式維持 P 原判。
- `U-DIAG`：`test_procedure` 之 UDS 位元組串 —— 每 byte 兩位大寫 hex、單空格分隔；`7F` 後兩 byte 必接 ` (<label>)`；label ∈ R-DIAG5(amend)(d) 五名 ∪ CFTS004 原字。
- `R1-DIAG`：Requirement ID 欄單值，允許 `SWE1-Diagnostics-\d{3}(-00[12])?`。
- `VM-DIAG`：Vehicle Model 七欄 ∈ {0,1}、598／5210 = 0、五有效欄至少一個 1（R-CAM 條文承接）。
- `RM-DIAG`：Remarks 固定句式三種（`Harman Scope: <Need Rework|Need Clarification|Accepted>`／`CFTS004 Category: Out of Scope`／`037 VM blank; procedure derived from Description + CFTS004`）＋ `NRC per ISO 14229-1 (037 unspecified)`／`SID per 037 <SWE1-ID>`。
每項以 5 條正例 ＋ 5 條反例自測，回報結果表。

**T5 — `nrc_coverage.tsv` 重分類**
230 → **226** 列（V3）四類：`RESOLVED_CFTS004`／`RESOLVED_037`／`RESOLVED_ISO`（附五碼之一與 037 觸發字句）／`PENDING_DR1`。預期 PENDING_DR1 顯著低於 185，逐列列出。I/O SID 114 → V3 實數，全數 `RESOLVED_037`。

**T6 — ANOMALIES 與 DR**
分析層 A1～A7（審閱 §四）登 `features/diagnostics/ANOMALIES.md` 為 `[A-DIAG06～12]`，`layer` 欄標 `analysis`。
DR-DIAG-1 依 T5 結果改狀態（阻斷 → 非阻斷，餘列數）；DR-DIAG-2 維持。

**T7 — RD 回饋整包**（§2 末段）。

## §4　預期數字（V3，分析層 2026-09-17 實測）

| 項 | 預期 | 查詢 |
|---|---|---|
| 037 資料列／唯一 ID | 389／388（`-340` ×2 仍在） | A 欄 |
| 來源唯一數／解析率 | 166／100% | strip 後對 CFTS004 B 欄 |
| 每來源列數 | 3 列 ×89、2 列 ×45、1 列 ×32 | |
| Priority | Medium 344／Low 39／High 6 | AG 欄 |
| 列型態 | positive 163／negative 133／unsupported 93 | up 3-8 判準 |
| 母節 | R/W 248／I-O 114／Routine 27 | |
| #5 GPS Signal Data | 34 | `$2812` |
| Layer 2 | 20 個（#11 併入 #14 → 28 列） | |
| Layer 3 被引用 | 42 | |
| CFTS004 未收 FR | 52 | |
| VM 空白／拼字／尾空白 | 4／Caemera 4・senario 3・recevied 1・diagnopstic 1・bightness 2／-315 三列 | 同 CDD-01 |
| `RULINGS.sha.tsv` 新增 | 9 ＋ 7 amend 列 | T1/T3 |
| MANIFEST | 235 → 236 | T2 |

## §5　上繳要求（`up/20260917_CDD-01_A.md`）

1. T1 正則 diff ＋ 指紋回歸表（既有零變動之證明）。
2. T2 V3 sha 複驗、MANIFEST 兩列、十一檔 tsv 之 V2 → V3 差異表、`framework.md` DRAFT。
3. T3 七條 amend 之實際號與 sha8；R-G 實際號。
4. T4 五項 lint 之正反例自測表；實作落點。
5. T5 四類數與 PENDING_DR1 逐列。
6. T6/T7 產物路徑。
7. §4 預期 vs 實測。
8. **CDD-02 前置核對**：framework DRAFT 完成、lint 五項可跑、SWC 0708 欄位對照（up CDD-01 §7-4）可用、`nrc_coverage` 無未分類列 —— 四項皆成立才進 pilot。

## §6　升級條件

- 改正則後既有任一 sha8 變動。
- V3 與 V1 (2) 之差異不止六列。
- T5 出現無法歸四類之列。
- `lint036.py` 無 profile 擴充機制且獨立腳本亦無法接入現有 lint 報表流程。
- 任何需改 canon 之情形。
