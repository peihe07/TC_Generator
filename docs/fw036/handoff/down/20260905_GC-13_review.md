# GC-13 審閱（2026-09-05，主線分析層）

對象：`up/20260905_GC-13.md`（執行層 A）。結果：**收受**。

## 一　裁

### 1　PROXI fallback 誤報（§3）→ **准執行層建議**
```text
R-G70(i)（補）：lint P 之 PROXI fallback 只在該行為賦值形態時報——含 `PROXI` 且同行有
  `=` 或 `is set to` 而 SWC 式／VF230 式皆不中者。純散文提及（"Hold for the PROXI … value"、
  "PROXI parameter reads …"）不報。Y 不受影響。
```
- 正控：pm_73 之 19 處散文樣本取 3 釘為「不報」測試；另造 1 行 `PROXI Foo == "x"`（賦值形態但兩式不中）釘為「報」測試。
- 改後重跑 pm_73 Revise1：預期 P = 209（bare）＋ 26（Revise-C）= 235；不等即停手。

### 2　Revise-M 出貨條件（修 GC-12 審閱 §一末句）
「全為 bare 類」過嚴——Revise-M 本就不解 Revise-C 項。改為：
```text
R-G72(h)：Revise-M 出貨條件 = P_after 之每一命中皆可歸入 {bare-$（待 Revise-C）,
  已登錄之 Revise-C 殘項} 二類之一，且無誤報；殘項清單落
  `features/<f>/reports/rg72_residual_<slug>_<date>.tsv`（row / col / text / class / 去向），
  隨 Revise1 一併入 MANIFEST note。
```
- ics_management、vsm_v42 之 Revise1：**准出貨**（殘項全為 bare，清單仍要出）。
- pm_73 之 Revise1：§一-1 改完重跑後，殘項 209 bare ＋ 26 Revise-C 列清單 → **准出貨**。26 處之 `raw (label)` 回查 CFTS 屬 Revise-C，其 DR 於該包登。

### 3　(h) 範圍含 `test_item` 括號下半（§1-1）
收受；審閱原文「作者側四欄」為分析層口徑不精，R-6 之 P 範圍為準。

### 4　八本基線 P 差 0（§1-4）
收受。「P 升屬預期」為分析層未量測之推定——八本為 0821 本，早於 v3；分析層之誤，形態＝預期未經量測即寫入條文（R-G69）。登 ANOMALIES 一條（次一未佔號），不影響條文。

## 二　其餘
| 節 | 判 |
|---|---|
| §1-2/1-3 實作與五樣本＋二測試 | 收受 |
| §2-2 三道保護攔下工具 bug、未寫檔 | 收受；保護有效之實證，`rg72_revise_m.py` 之去重修正保留並加一測試（同格重複 edit） |
| §2-3 P 降幅逐本等於替換處 | 收受 |
| §2-5/2-6 idmap、MANIFEST、RULINGS | 收受 |
| §4 A-GC23 | 收受 |
| §5 PROTOCOL 二句 | 收受 |
| §6 DR 表格式段 | 收受 |

## 三　GC-14 任務（指派執行層 A）
1. §一-1：fallback 改＋4 測試 → pm_73 Revise1 重跑 lint → P 須 = 235（209+26）。
2. §一-2：三本殘項清單 `rg72_residual_*.tsv`；pm_73 之 26 處逐處標 `class=ReviseC, 去向=CFTS 回查`；MANIFEST note 指清單。
3. `rg72_revise_m.py` 加同格重複 edit 測試。
4. ANOMALIES：§一-4 條。
5. R-G70(i)／R-G72(h) 入台帳（逐字取本審），乾跑證入表。
6. 上繳 `up/20260905_GC-14.md`：三本「准出貨」狀態各一行（sha、殘項數、清單路徑）。

## 四　Pei 動作
1. `rulings_hash.py` 重生（R-G70(h)(i)、R-G72(g)(h)）→ `--check` → commit。
2. GC-14 後：ics／vsm 之 Revise1 各抽 3 列對 dry-run 表 →「出貨」；pm_73 同。
3. DR 取號送出（DR-VS{n}、HMI entry path ×3）；`driver_distraction` `git mv`。

## 五　下一階段預告（不在 GC-14）
Revise-C 首包：對象 pm_73（209 bare ＋ 26 字串值）＋ sw_update（122 X-nav ＋ 3 bare）。逐 TC 查 CFTS 原句取值、查 HMI L&F 取 hop；查無登 DR。此為人讀密集包，分析層先出「每 TC 之來源錨」表再下放。

## 六　未結 DR
同上繳 §9。
