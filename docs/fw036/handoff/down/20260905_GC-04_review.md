# 審閱 GC-04 上繳包（2026-09-05，Tier 2）

對象：`up/20260905_GC-04.md`　下放包：`down/20260905_GC-04.md`
結果：**收受**。六件全收；7-4 四項裁於 §一；DBC 給 Pei 之 [DEFAULT] 於 §二；分析層自報一件於 §三。

---

## 一、7-4 四項

**1　R-G30～R-G41 指紋缺口** —— 裁：**加錨，不改標題，不放寬正則**。
於台帳該九節內，每條圍籬之前插入一行 `### R-Gnn — <條文首行之標題>`（含 `R-G33(d)`、台帳側 `R-G42`），原 `##` 標題一字不動（R-TM13 加註即可）。
放寬正則會把括號內之條號引用（「依 R-G34」）也讀成錨點，反增假錨。
台帳 `R-G42` 加錨後與 FO 摘要條成雙列，屬既有可容之形態（同 `R-G4`／`R-G7`），FO 側已自讓權威，不動。
預期重生後 `R-G30`～`R-G41` 各 1 列、`R-G33(d)` 1 列；GC-05 乾跑逐號回報。

**2　`R-G45` 撞號** —— 裁：補充條改 **`R-G45(b)`**，標題與本體首行同步（本體首行為「R-G45 補充」之字樣，屬 GC-02 審閱之散文而非圍籬條文，得改），台帳加改號註。

**3　R-G43(b) 措辭** —— 裁：條文不改（R-TM13），增 **R-G43(c)** [DEFAULT]：

```
R-G43(c)：指紋之充分條件為「檔在掃描面」且「條號領頭於錨點行」。
凡以敘述式標題（條號在括號內）收錄之條文，須另加條號領頭之子錨點，否則視為無指紋。
台帳新增條文自本條起一律以 `### R-Gnn — 標題` 起首。
```

**4　`tests/test_measure_common.py`** —— 收受，非逾越。「寫成常數而無機檢，第三次仍會犯」與 R-G48(b) 守衛測試同理；自此執行層對「已二犯之規則」加機檢不視為逾越，上繳具名即可。

## 二、DBC 版本（Pei 裁，分析層 [DEFAULT]）

四象限：本簿 48 個 token 全在 FDCAN8 且 R1／R5 皆有；BHCAN 僅 1 個 token 且**只在 R4**（`IPC_VEHICLE_SETUP2.Power_Tailgate_Enable`），47 個兩份 BHCAN 皆無（即它們是 FDCAN8 訊號，BHCAN 查無屬正常）。

[DEFAULT]：**vehicle_setting 綁 forms R1**（R-17 既定權威；就本簿使用面 FDCAN8 無差）。
`Power_Tailgate_Enable` 依 R-13(g) 保留原名、開 DR 向上游查 R1 BHCAN2 是否有對應；DR 未結前該列標 `PENDING: DR-{n}`，不得以 R4 之存在視為 R1 之存在。
GC-05：執行層定位該 token 所在列，登 DR；`inputs/` 之 R4／R5 兩檔 DECISIONS 註「非現行，保留供追溯」。

## 三、13 判之修正（依分母）

| # | 原判 | 改判 |
|---|---|---|
| 3 comfort Pop Up | 待證 | **不影響（條件式）**：該簿 0/1340 不以 PU id 引用；GC-05 以變動 2 id 之彈窗**文字**再 grep 一次，仍 0 則結案 |
| 4 time_management HMI SL | 出貨前必重跑 | **維持**；0 命中而分母 9/316，不足以解除 |
| 6 time_management LID | 待證 | **不影響（條件式）**：分母 1/2548；GC-05 列該 1 個 signal 是否在變動 4 個內，不在即結案 |
| 8 user_profiles Pop Up | 待證 | **不影響**（17/1340 有證據力） |
| 13 vehicle_setting LID | 待證 | **不影響**（18/2548 有證據力） |

**分析層自報 A-GC13**：GC-03 審閱 §四 所定判準（key 級 grep）未先量分母，對 comfort／time_management 兩線為盲測；執行層附分母方顯。形態同 A-GC 系列（判準未量其覆蓋）。GC-05 抄錄。

## 四、分析層未做（具名）

`rg43_manual_20260905.tsv` 130 處判側（GC-04 §二）**尚未做**；下一輪先出 `_judged.tsv`，GC-05 才含改號。

## 五、收受清單與閘況

台帳新節＋R-G43 理由更正＋A-GC12；`rulings_hash.py` 掃描面（+4/−1，34 測試對照向同）；乾跑：+18 列、台帳 16 列、`R-G16`／`R-G19` 由台帳供給——**R-G43(b) 之核心目的達成**；`binding_hits` 141 列附分母；五本 DECISIONS；`measure_common.py`＋12 測試＋`canon_refs` 接線。
`canon_refs` +9 全為未生指紋之號，重生即消。`lint_reports/` 106→23 為 Pei `d362a8d`，歸因正確。

## 六、Pei 動作（本輪）

| # | 事項 | 狀態 |
|---|---|---|
| 1 | R-G43(c)、R-G45(b)、DBC 綁 R1 [DEFAULT] | 追認 |
| 2 | `rulings_hash.py` 重生 | **延至 GC-05 加錨後一次跑**（否則 R-G30–41 仍缺） |
| 3 | `git commit` GC-04 產物 | 可：`git commit -m "feat(fw036): GC-04 ledger in hash scan, binding hits, measure_common" -- docs/fw036 docs/reports scripts tests features/comfort/DECISIONS.md features/time_management/DECISIONS.md features/user_profiles/DECISIONS.md features/vehicle_category/DECISIONS.md features/vehicle_setting/DECISIONS.md` |

## 七、GC-05 預告（待 §六-1 與分析層 judged.tsv）
加錨 14 條、R-G45(b) 改號、R-G43(c)／A-GC13 入台帳、DR 登記（Power_Tailgate_Enable）、#3／#6 補 grep、130 處改號、乾跑逐號回報。
