# GC-07 審閱（2026-09-05）

審閱對象：`docs/fw036/handoff/up/20260905_GC-07.md`。
Pei 於上繳後之裁示（2026-09-05）：「之後都要這樣去做，已繳的有需要再進行修改。」

---

## 一　Pei 裁示之解讀（分析層記錄，Pei 有異議即改）

1. 「之後都要這樣去做」＝ **§二擇甲**（訊號寫法回歸 SWC 之 `Send CAN:` 作者側記法）＋ **R-G71 全域生效**。新產出一律依 R-G70／R-G71。
2. 「已繳的有需要再進行修改」＝ **已交付本納入回修範圍**。此與 R-TM13「交付物不可回溯修改」之字面相衝，處理方式見 §三 R-G72：**不就地改交付檔，發新日期之 Revise 本**（沿 PM Revise 之既有形態），舊本原封留檔。何本回修、何時回修，由 Pei 依 §四 盤點表逐本裁。

---

## 二　上繳包驗收

| 項 | 判 | 備註 |
|---|---|---|
| 1 量測複核 | **准** | P4／全欄兩口徑並列，且反推出下放包未載範圍——此為執行層之正確作法。 |
| 1-1 VF230 `test_item` 欄 81 處 v3 式 | **准，採入 R-G70 沿革** | 「v3 是來源側記法、v2 是作者側記法」之區分成立：`test_item` 上半 verbatim 保留 `$MESSAGE.Signal$`（R-6），步驟／ER 用 `Send CAN:`。兩者各安其位，不再是取捨問題。 |
| 1-2 Home `Press`/`Select` 計數 | **執行層值為準（186／154）** | 下放包之 158／135 係分析層以「首二詞」正則分桶後人工相加，口徑未載且加總有漏。**登 A-GC15**：分析層之誤，形態＝人寫加總代機器計數（R-G69）。 |
| 1-3 台帳現查 | 准 | 70／71 未佔用。 |
| 3-1～3-2 R-G71 落檔 | **准** | 766 錨、unresolved 29→18。 |
| 3-3 §5.3 常數二條 PENDING | **准** | 不臆造 hop 正確。 |
| 3-4 下放包二處例示錯誤 | **准其訂正；登 A-GC16** | 分析層之誤：`Press "Vehicle" on Menu Bar` 與 `Set "SWITCH 3 Hold Last State" = "Disable"` 二例皆未查 HMI 來源即寫入條文，正是 R-G71(c)(d) 所禁之行為，由條文自身之施作者抓出。形態＝條文例示未經量測（同 A-GC 系列「人寫代量測」）。 |
| 4 lint Q→X | **准** | 分析層未查 `CHECK_TITLES` 即指派代號，**併入 A-GC15**（同包、同形態，不另立號）。 |
| 4-3 報告落點改 `features/<f>/reports/` | 准 | 下放包所寫 `docs/runtime/lint/` 與 R-G47 抵觸，執行層依既有裁定為準，正確。 |
| 5 未結 DR 無法全稱 | 准；建議採納，見 §五任務 6 | |
| 6 測試 | 准 | 8 項 HEAD 既紅不在本包；下包處理。 |

---

## 三　裁定（入台帳；號由執行層落檔前現查，R-G23）

### R-G70 —— 訊號與參數寫法 v4：來源層級與作者側記法（全域，Pei 2026-09-05 擇甲）

```text
(a) 來源層級：需求主體 = CFTS；訊號名／raw 值／VAL_ label = VF 文件（SWRA）
    ＋ forms/ 之 PROXI 表與 DBC（R-17、R-13 不變）；畫面之入口／層級／選項標籤
    = HMI Logic and Flow ＋ HMI Settings List（R-G71）。三者不得代位。
(b) 兩側記法各安其位：
    來源側（test_item 上半 verbatim、reasoning 引文）：保留來源原文，含
      `$MESSAGE.Signal$`（VF230 test_item 欄 81 處實測）。
    作者側（pre_conditions / test_procedure / expected_result / input_test_data）：
      一律 SWC／VF230 作者側記法，即 (c)–(f)。
(c) CAN 送出：`Send CAN: <MESSAGE>.<Signal> = <raw> (<label>)`，label 逐字取 DBC VAL_（R-7）。
(d) CAN 觀察 ER：`<MESSAGE>.<Signal> = <raw> (<label>) is sent <時機>`；
    Procedure 讀取：`Read <MESSAGE>.<Signal> and check that it is <raw> (<label>)`（R-11(b)）。
(e) PROXI：`PROXI $<Param>$ is set to "<值>"`（VF230 152 列式）；SWC 之
    `PROXI <Param> = <值>`（41 列）為同義舊式，新產出不用。
(f) 設定／VHAL／內部參數：`$<Name>$`，`$` 為參數標記（SWC 215 次）；DBC 查無對應者
    保留來源名並寫應觀察之值（原 v3(d) 內容不變）。Hold／baseline 同原 v3(e)(f)。
(g) R-13、R-17 不變。
沿革：R-1 v3 撤銷（R-TM13 加註保留）。撤銷理由：v3 依庫外 CR30580/30581 參考本立，
  該本查無不可複驗；三本 Pei 交付本作者側欄位 v3 式 0 次、v2 式 236 次；
  VF230 test_item 欄 81 處 v3 式證明 v3 為來源側記法，12 包誤讀為作者側。
lint P：判準回 `Send CAN:` 全域預設，`--profile` 之 v3 分支移除。
vehicle_setting profile 之 `[OVERRIDE §8.7.5]`（R-VS52／R-VS67）：內容併入本體後作廢，
  台帳加註「升為全域，由 R-G70 承接」。
```

### R-G72 —— 已交付本之回修形態（全域，Pei 2026-09-05「已繳的有需要再進行修改」）

```text
(a) 已交付本不就地改（R-TM13 之「檔不動」保留）；回修以 **Revise 本** 發出：
    同目錄、檔名加 `_Revise{n}`、新日期、新 sha，舊本原封留檔並於該線 RULINGS 記
    「被 <新檔> 取代，取代理由 <R-G 號>」。
(b) 每本 Revise 須附 TestRail 舊 ID→新 ID 對照表（同 PM 站 ⑤ 之形態），
    TC 內容有變者對照表加 `changed` 欄標明變動之欄。
(c) 回修範圍限 canon 條文所定之機械轉換（R-G70 訊號記法、R-G71 導航路徑）
    與 Pei 逐本裁准之內容修正；不得順手改其他欄。
(d) 何本回修、次序：依 GC-08 盤點表由 Pei 逐本裁；未裁者不動。
(e) 回修產出之 Revise 本重新過全部閘（含 X）後方可出貨。
```

---

## 四　下一包 GC-08 —— 盤點，只量不改

對 **所有已交付本**（`features/*/delivered/`、`features/*/output/` 現行本、`docs/Report/compare/*/`
最新一本、AMFM v1 tag 本）產一張表，每本一列：

| 本 | 路徑 | sha12 | 資料列 | v3 式（作者側四欄） | `Send CAN:` | PROXI 舊式（不加 `$`）| lint X | 首個 R-G 違例類別 | 建議 |
|---|---|---|---|---|---|---|---|---|---|

「建議」欄只准三值：`無需回修`／`機械轉換可回修`／`需 Pei 內容裁決`，並附判準（數字為零即「無需」；只有 R-G70/71 機械項即「機械」；涉 HMI 標籤 vs VF 名者即「需裁」）。
盤點腳本落 `scripts/`，dry-run 可重跑。**本包不轉換任何本。**

---

## 五　執行層任務（本審閱＋GC-08 合併一包）

1. R-G70／R-G72 落檔：現查號 → 台帳錨＋全文 → `§8.7.5` 改 v4（沿革保留 v1/v2/v3 撤銷紀錄）→ 條文落檔位置表 → `rulings_hash` 重生 → `canon_refs` 重跑，三數回報。
2. lint P 判準回 `Send CAN:`；移除 v3 profile 分支；`vehicle_setting` profile override 作廢併入；相關單元測試改。
3. `pm_29` 207 列轉換：**先 dry-run 出計數與逐列 diff 對照表**（`features/power/reports/`），不寫入；轉換併入 PM 站 ⑤ 寫回時執行（Pei 於 ④ 抽驗）。
4. ANOMALIES.md 登 A-GC15、A-GC16（自報形態，內容依 §二表）。
5. GC-08 盤點表（§四）。
6. `DATA_REQUESTS.md` 統一機讀關閉欄：**提案不施作** —— 出一行格式（如 `| DR-n | status: open/closed | closed_by: <pkg> |`）與各 feature 現行記法之對照，Pei 裁後再改。
7. 上繳附：未結 DR 清單（依現行人讀口徑，標明口徑）、HEAD 既紅 8 項之歸屬建議。

## 六　待 Pei

1. §一之解讀有無誤（尤其 R-G72 之「Revise 本」而非就地改）。
2. DR 草稿（`features/vehicle_setting/DATA_REQUESTS.md` 檔末，HMI entry path 三問）取號與送出。
3. 上繳 §8-3：新產出畫面 hop 一律 HMI 標籤、VF 名留 test_item 上半——分析層依 R-G70(b) 判為**已定**，不另裁；Pei 有異議再改。
