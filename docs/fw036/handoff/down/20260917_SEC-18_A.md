# 下放包 SEC-18_A —— 取消篩選：SYS3 對應幾個號就寫幾個號

Pei 2026-09-17 裁：「他寫幾個你就對回去」——**不篩、不設上限**。

## §1　取代 SEC-18 §1(c)
```text
R-SEC10(c) amend2 (c)  選號規則：該 SWE1 列經 `trace_matrix.tsv` 對到之**全部** `SYS-RA-SEC-nnn`（`match_kind` = exact 與 base 皆計，
                       不依 SYS2 category 排除），全數寫入，升冪，一號一行。無上限、無 REVIEW 狀態。
```
`spec_ref_plan.tsv` 之 `status` 只剩 `OK`（有對應）／`FALLBACK`（無對應→SWE1 ID 或 SYSAD）；`candidates_after_filter` 欄刪除。

## §2　其餘不變
SEC-18 §0／§2／§3／§4 照舊；§3-1 改回報每 Test Group 之行數分佈（min／median／max）與最大列（預期 CertProvider 某列 ≈ 41 行），供 Pei 看實況。

## §3　升級條件不變
