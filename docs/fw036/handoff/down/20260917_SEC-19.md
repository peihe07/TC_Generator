# 下放包 SEC-19 —— `specification_reference` 之號改為 Polarion **ObjectID（7 位）**，非 NRL → `security_v13`

前置：`up/20260917_SEC-18.md`。Pei 2026-09-17 裁：格式為 `CFTS084-{ObjectID}`（canon §10.7(a)，ObjectID = Polarion 7 位物件號，同 SWC 之 `CFTS042-4813401`）；NRL work item 號**不是** ObjectID（分析層 A-45）。換行一號一行不變。
執行層 session：新開；不跑 `--gate`。

---

## §0　禁區
- 只動 `specification_reference` 欄；其餘欄 0 diff；填色／DV／zip 不變（SEC-17 外科法）。
- ObjectID 只能來自 `sources/raw/` 之 `SYS2_CFTS084_…_0331.xlsx`（`Basic Report`）逐字；不得自 NRL 推算、不得自他檔取。

## §1　量測先行
1. 打開 CFTS084 `Basic Report`：列出表頭全部欄名；找出 **ObjectID 欄**（候選：`Object ID`／`ID`／`Source ID`／`Polarion ID`／`CFTS ID`；值型態 7 位數字，或形如 `CFTS084-nnnnnnn`）。
2. 對 SWC 0708 交付本抽 5 個 `CFTS042-nnnnnnn`，回到 CFTS042 之 SYS2 本確認該號落在哪一欄——**以此定 ObjectID 欄名**，不憑猜。
3. 建 `SYS-RA-SEC-nnn → ObjectID` 對照表 `data/sys2_objectid_map.tsv`（欄 `sys_ra_sec | nrl | object_id | source_row`）；每一 `sys_ra_sec` 必須唯一對到一個 7 位號，缺者列 `MISSING`。

## §2　裁定落檔
```text
R-SEC10(c) amend3  spec_reference 之號 = CFTS084 SYS2 本之 Polarion ObjectID（7 位），格式 `CFTS084-{ObjectID}`；
                   NRL 號作廢（A-45）。一號一行、升冪（依 ObjectID 數值）、cell 內 `\n`、wrap on。
                   對不上 SYS2 者仍寫 SWE1 ID／SYSAD 首值。
                   (d) 格式正則改 `^CFTS084-\d{7}$`。
```
落 `RULINGS.md`，body sha8 乾跑取得，tsv 不動。

## §3　實作
`build_spec_ref.py` 改讀 `sys2_objectid_map.tsv`；`SEC_VER=v13` 重跑六本＋`_all`；套色；lint；換行三數；v12→v13 diff 只在該欄（預期 124 格）。`delivered/` 同步、v12 移 `superseded/`。
`EXEC_GUIDE.md` 不動（不引該欄）。

## §4　上繳要求（`up/20260917_SEC-19.md`）
1. §1-1 表頭全欄名；§1-2 五個 SWC 號之落欄證據；定案之 ObjectID 欄名。
2. `sys2_objectid_map.tsv`：161 個 `sys_ra_sec` 之對到數／`MISSING` 數。
3. 行數分佈（應與 v12 相同）；格式自驗 `^CFTS084-\d{7}$` 0 錯；升冪 0 錯；換行三數。
4. 六本 sha；diff 只在一欄；lint。
5. amend3 body sha8（乾跑）。
6. 自報錯誤。

## §5　升級條件
- CFTS084 `Basic Report` **無** ObjectID 欄（或 §1-2 無法證明欄名）→ 停，回報，不得填 NRL 充數。
- 任一 `sys_ra_sec` `MISSING` → 該列改寫 SWE1 ID 並列出，超過 5 列升級。
- diff 落在該欄以外；換行三數不等。
