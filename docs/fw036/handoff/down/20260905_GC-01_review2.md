# 審閱 GC-01 補遺（2026-09-05，Tier 2）—— §二 重測結案

對象：`up/20260905_GC-01.md` §11（sha8 `17297b8b`）　前審：`down/20260905_GC-01_review.md`
本檔為前審之增補，不覆寫前審（down/ 唯增）。

## 一、§二 結案

案 (b) 成立：表無誤，§10-2 歸納有誤。`Pop Up List` 命中 9 列，
`comfort`／`user_profiles` 二檔在表（`b0827f02`，G019）。
`source_identity_20260905.tsv` **由暫收改為收受**；220／179／206 三數解除保留。

**裁案 2 恢復**，並改寫供裁事實：兩版皆有實體 ——
`R1 (26PI)`：`forms/` ＋ 4 個 `inputs/` 副本，同 sha（G028）；
`R1 SR24 Post 2A (Dec 15, 2023)`：`comfort`、`user_profiles`（G019）。
依 R-G45 [DEFAULT]，`forms/` 版為現行；`comfort`／`user_profiles` 走版本綁定記錄。
Pei 若裁 SR24 版為現行，則 `forms/` 須換檔並記 A 條，且 display／sw_update／vehicle_category 三線已交付 TC 之 popup 引用須重驗——此為裁案 2 之實際代價，供裁時併看。

## 二、R-G45 待記清單改為執行層 11-3 節之重算

**5 個 feature／13 檔次**：comfort 3、time_management 4、user_profiles 1、vehicle_category 1、vehicle_setting 4。
下放包記 1 → 上繳首版記 2 → 重算 5：三方逐次低估，同一成因（以檔名／樣本代 sha 母體查詢）。
分析層自報 **A-GC9**：R-G45 條文所附之「實測待記者」本身即是未跑母體查詢之樣本值。
`time_management` 之 `HMI Settings List R1 SR24 Post 2A (June 15 2023).xlsx` 為證：檔名不同者只有跨 `forms/` 之 sha 比對抓得到，同名異體表本就抓不到 —— R-G44 修訂版（sha 唯一判準）在此再得一證。

## 三、裁決：R-G50 [DEFAULT]（收受上繳包 11-2 之提案）

```
R-G50：上繳包、審閱、下放包內凡對自家產出之表或目錄作全稱否定
（「未出現」「無」「0」「皆」「全部」），須附查詢式（grep／SQL／腳本
與其母體）與命中數，二者缺一即為未量測之斷言（R-G22(LEDGER) 之延伸）。
R-G13(LEDGER) 管外部素材之查無；本條管對自家產出之全稱斷言。
```

本審閱前審 §一 A-GC1～8 與執行層本輪三犯，皆為本條所規制之形態。
自本補遺起，分析層之審閱同受本條拘束。

## 四、閘況

`canon_refs` 572 → 578（§11，+6 unresolved-ruling）→ 597（審閱檔 +19），
分列可歸因，**不擋包**。本補遺再增 R-G50 一號，結清路徑同 GC-01 9-2 節（Pei 追認 → 入台帳 → `rulings_hash.py`）。

## 五、GC-01 結案狀態

上繳包 GC-01 **收受**。未結事項全數轉入前審 §五（Pei 待裁 1、3–7）與 §六（GC-02）。
GC-02 待 Pei 准 §五-7（含測試改動）後下放。
