# 下放包 GC-08 —— `gates_tsv` 之 `body_kind` 閘、sw_update 交付紀錄（2026-09-05）

Pei「准」（2026-09-05）：GC-06 審閱 §一-1 [DEFAULT]（補 `DOCS_CHECKS` 再重生）准；§一-3（A-SU7 交付紀錄）准。
本包改 gate code 一處（`scripts/gates_tsv.py`），其餘為文件。`GATES.tsv` 由本包重生（Pei 已准），`RULINGS.sha.tsv` 不動。

## 一、執行層任務

### 1　`gates_tsv.py::DOCS_CHECKS` 補 `body_kind`
1-1　先量：`DOCS_CHECKS` 七鍵逐字；`GATES.tsv` 現行 `body_kind` 列逐欄（它是人手寫的，欄值即規格）；`body_kind` 閘之實作在哪支腳本（`grep -rn body_kind scripts/ tests/`）。
1-2　改：以 1-1 之人手列為準補入 `DOCS_CHECKS`（鍵名、腳本、旗標、自查對映與現行列一致），使產生器產出之列**與人手列逐欄相同**。
1-3　重生：`gates_tsv.py --out <工作區>` 先比對——預期 48→56（+8 新增、−0 消失、`body_kind` 逐欄同）；符合後才寫 `docs/runtime/GATES.tsv`。附前後 diff。
1-4　`gates_tsv.py --check` PASS；`tests/` 內對 `gates_tsv` 之測試對照向；`selfcheck_map` 若因新增 8 閘要求對映，回報缺對映之閘（**不自造對映**，具名待裁）。

### 2　sw_update 交付紀錄（GC-06 審閱 §一-3 逐字）
`features/sw_update/RULINGS.md` 加一節「交付紀錄（2026-09-05 補登）」：交付檔名、入版控 commit `ef3ade0`、日期、現行 sha256 全值、「無登記前 sha 可對，曾否變更不可證（A-SU7）」。不寫「未變更」。取現場號若立條（R-G62）。

### 3　`gate_all` 全跑
回報六閘狀態；預期 `gates_tsv` PASS、`lint_paths` 0（Pei 已跑搬移）、`rulings_hash` PASS（Pei 已重生）、`canon_refs` 158 或更低（stale 已減）。任何仍紅者逐處歸因。

### 4　上繳 `up/20260905_GC-08.md`：diff、對照向、工具寫入路徑清單（本包 `GATES.tsv` 為准寫之例外，列明）、自報。

## 二、Pei 動作（本包後）
commit：`git commit -m "feat(fw036): gates_tsv registers body_kind gate; GATES.tsv regenerated (56 rows)" -- scripts/gates_tsv.py docs/runtime/GATES.tsv tests features/sw_update/RULINGS.md docs/fw036/handoff`

## 三、未結 DR
DR-VS{n}（Power_Tailgate_Enable）未送出。
