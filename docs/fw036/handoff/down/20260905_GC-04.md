# 下放包 GC-04 —— GC-03 審閱之落實（2026-09-05）

Pei「追認」「准」（2026-09-05）：GC-03 審閱 §二 R-G43(b)、§三 R-G50(c)／R-G16 補充追認；本包准。
仍待 Pei：vehicle_setting DBC（forms R1 vs inputs R4/R5）何者為 R1L 現行——本包 #10／#11 只量不判。

R-G 引用一律用改號後之號（R-G51–R-G69）。`docs/reports/*_YYYYMMDD.tsv` 依 R-G16 補充不入任何母體。

---

## 一、執行層任務

### 1　條文與自報入台帳（R-G36 機器抽取）
- R-G43(b)（`down/20260905_GC-03_review.md` §二-3 圍籬）、R-G50(c)、R-G16 補充（同檔 §三）。
- R-G43 節之理由段：原句以 `~~ ~~` 保留，其後書更正句（審閱 §一：指紋源為 FO＋`features/*/RULINGS.md`，台帳不在掃描面；裁定 (A) 維持之餘一腳理由）。
- `docs/fw036/ANOMALIES.md` 追加 **A-GC12**（審閱 §一 逐字）。

### 2　R-G43(b)：`scripts/rulings_hash.py` 掃描面納入台帳（Pei 准改 code）
2-1　先量：`default_targets` 現行值（行號＋逐字）；`--out`／`--check` 之語意（讀 argparse 定義，不推）。
2-2　改：`default_targets` 增 `docs/fw036/RULINGS_LEDGER.md`。**不動指紋演算法、不動輸出格式。**
2-3　乾跑：`python3 scripts/rulings_hash.py --out /tmp/rg_dry_20260905.tsv`（R-G50(c)：只准 `--out`／`--check`；上繳附本工具寫入 repo 內之路徑清單，應為空）。
2-4　回報（R-G8 分子分母）：
- 重生列數 vs 現行 698；新增列逐號（預期含 R-G30–R-G41、R-G44–R-G50、R-G69、R-G19 台帳側）
- **ID 撞號**：同一 ID 出現 ≥2 列者逐號列（台帳內部已知：R-G13／R-G13 補充、R-G33／R-G33(d)、R-G19 若 L402 改號未淨；跨檔：FO 側 R-G42 摘要 vs 台帳 R-G42 全文）
- 撞號**不得在乾跑中合併或擇一**，列表交分析層裁
- 本體 sha 與 FO 側同號條（R-G42）是否相等
2-5　`tests/` 內若有針對 `rulings_hash` 之測試，跑對照向（改前／改後逐項同名）。
2-6　**`RULINGS.sha.tsv` 不寫**（Pei 於 GC-04 審後跑）。

### 3　§四 待證七列之 grep（附查詢式與命中數）
對象 = 該 feature 現存最新工作簿（`sandbox/`／`generated/` 依 mtime 取最新一本，回報所取路徑與 sha8）：
| # | feature | 查 |
|---|---|---|
| 3 | comfort | Pop Up List：只在 forms 之 1 id、值異之 1 id，grep `PU\d{4}` 命中列 |
| 4 | time_management | HMI Settings List：32 個變值 key＋105 個只在 forms 之 key，逐 key grep 設定名命中列；另回報該線工作簿引用之設定名總數（分母） |
| 6 | time_management | LID：4 個值異 signal，grep `$…$`／裸名命中列 |
| 8 | user_profiles | Pop Up List：同 #3 |
| 10 | vehicle_setting | DBC BHCAN：工作簿內全部 `$MESSAGE.Signal$` token 去重後，逐一對 `forms/PDT27_E2A_R1_BHCAN2.dbc` 與 `inputs/PDT27_E2A_R4_BHCAN.dbc` 各查存否；出四象限（兩邊有／只 R1／只 R4／兩邊無）計數與清單 |
| 11 | vehicle_setting | DBC FDCAN8：同 #10 對 R1_FDCAN8 vs R5_FDCAN8 |
| 13 | vehicle_setting | LID：同 #6 |
輸出 `docs/reports/binding_hits_20260905.tsv`（`feature / n / family / key / hit_rows / query`）。
**#10／#11 只量不判**：四象限交 Pei 裁版。

### 4　13 判抄回五本 DECISIONS
自 `down/20260905_GC-03_review.md` §四 表逐列抄入各 feature `DECISIONS.md` 版本綁定節（取代 `PENDING 分析層判`；原 PENDING 以刪除線保留，R-TM13）。「不影響（key 級）」六列各附審閱所載盲區句。待證列寫「待證，見 GC-04 任務 3」。

### 5　`SELF_OUT` 常數化（R-G16 補充）
`scripts/` 內單一常數（例 `scripts/measure_common.py::MEASUREMENT_OUTPUT_GLOBS = ["docs/reports/*_2026*.tsv", ...]`），本輪與歷輪之掃描腳本改 import 之。附使用處清單。

### 6　上繳 `up/20260905_GC-04.md`
產物 sha、2-4 撞號表、3 之命中表摘要、對照向（含被移出檔數 R-G50(b)）、工具寫入路徑清單（R-G50(c)）、自報。

## 二、分析層任務（本包平行）
`docs/reports/rg43_manual_20260905.tsv` 130 處逐處判側，出 `docs/reports/rg43_manual_20260905_judged.tsv`（原欄＋`judged_side / new_id / reason`），交 GC-05 執行層改號。先判集中六檔 50 處。

## 三、Pei 動作（本包後）
1. GC-04 審後：裁撞號處置 → `python3 scripts/rulings_hash.py`（無參數，寫 `RULINGS.sha.tsv`）→ `git commit -- docs/fw036/RULINGS.sha.tsv scripts/rulings_hash.py`
2. 裁 vehicle_setting DBC 版（依任務 3 四象限）

## 四、未結 DR
無新增。
