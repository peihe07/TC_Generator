# 下放包 PU-02 —— Popup：Pop Up List 基線改版、封面補齊、交付

Pei 裁（2026-09-08）：**「以現在 037 V0.2 為主」**、**Pop Up List 基線取
`Pop Up List HMI R1 SR24 Post 2A (Dec 15, 2023).xlsx`**。

## 〇、本裁之效力 —— 先講清楚不做什麼

「以 037 V0.2 為主」＝ **維持 R-POP2 之原判，生成範圍仍為 5 個 Functional leaf**。
Core §5.1／5.2／5.3／5.4 之缺口**不補生成**，續留 `COVERAGE_GAPS.md` 與 RD-1。

⚠ 此與 CFTS044（VS-CF-03）之處置**刻意不同**，兩者之缺口性質不同：

| | CFTS044 | Popup |
|---|---|---|
| 缺口成因 | 037 D 欄**已寫**行為，分析層未讀 | 037 **沒有 leaf**（V0.2 只做到 §5.5／5.6）|
| 補法 | 讀 D 欄即可生成，屬修正 | 需分析層代上游認定「這是需求」，屬越權 |
| 本輪處置 | 全數補生成 | **不補**，缺口上報 |

`features/popup/RULINGS.md` 之 `R-POP2`／`R-POP7` **不改**。
本包**只做基線改版、封面補齊、交付**，不動任何 TC 之 I–R 欄。

對象：`features/popup/output/FM-WI-FSM-036-A01 … _Popup_20260828.xlsx`
sha256 前 16 `a13559d51d91d369`（與 `10_Reviewing/` 之同名檔逐字相同，分析層複驗）。

---

## 一、Pop Up List 基線改版（R-POP6 修訂）

### 1.1 新檔登記

Pei 於 `10_Reviewing/00_TestCase/ASW-R2/Pop-Up Queue and Priority Management/` 新增 3 件：

| 檔 | sha16 | 處置 |
|---|---|---|
| `Pop Up List HMI R1 SR24 Post 2A (Dec 15, 2023).xlsx` | `de33bcb81e113c8d` | **新基線**，複製入 `forms/`，登 `forms/FORMS.md`（R-POP25-3）|
| `Pop Up List HMI R1 Change Log(26PI).xlsx` | 執行層量 | 複製入 `forms/`，登 FORMS.md；作 delta 佐證 |
| `Core HMI Logic and Flow …(February 2 2023).pdf` | 執行層量 | **不納入** —— 無文字層（21 頁抽取 0 非空白字元，分析層實測），機器可讀源為既有之 `spec-index/cache/SYS1_HMI_Core_…xlsx` |

`Pop Up List HMI R1 (26PI).xlsx` 之 `forms/` 現存本 sha16 `ff47b7be63e5824c`。
⚠ 執行層須**另行核對** `10_Reviewing/` 內之同名檔與 `forms/` 現存本是否逐位元相同
（分析層因檔名相同、複製時覆蓋而未能分辨，**此點未經量測**）；不同即停手回報。

### 1.2 兩版之 delta（分析層實測，執行層複驗）

```
Dec 15 2023 版   Main 資料列 1341   A1 = "SR24 Post 2A CR25802"
26PI 版          Main 資料列 1342   A1 = "SR24 Post 2A CR25802"   ← A1 兩版相同

26PI 獨有        PU1579（Geolocation；Change Log 載 05/20/2026，狀態 Under Review）
Dec 獨有         無
內容相異         PU1578、PU1229（2 筆）
Templates 分頁   兩版完全相同（含 R1 Low 7" 之 13–19，無 Medium）
```

**基線取 Dec 15 2023 版之理由（落入修訂條文）**：其為 SR24 Post 2A 之乾淨對應，
與規格基線同代；26PI 多出之 `PU1579` 狀態為 `Under Review`，未核准之條目不入生成基線。

### 1.3 對現有 5 條 TC 之影響：**零**

TC 引用之 4 筆 PU 於兩版**逐欄逐字相同**（分析層比對 `Timeout (sec)`／`Exit Conditions`／
`Category`／`String/Popup Message`／三個 Template 欄）：

```
PU0942  5 / "Timeout, <Reorder> <X>" / 2 / "<X> Page added! [Reorder]" / 2 / 14 / 22
PU0215  N/A / "<Trks>" / 2 / "Tracks List" / Custom / Custom / Custom
PU0580  "5 seconds" / "Timeout, Touch outside of popup, X" / 2 / '"Welcome [username]", "X"' / 1 / 13 / 21
PU0949  3 / "<OK> <X> " / 3 / "No Phone is Connected.        <OK>    <X> " / 2 / 14 / 23
```

`NR1L-Popup-005` 不引 PU（R-POP14）。
**故本包不改動任何 TC 儲存格**；執行層須複驗上表並在上繳附比對輸出，
若有任一欄相異即停手 —— 那將使「零影響」不成立。

### 1.4 台帳

`features/popup/RULINGS.md` 檔末新增（live 取號；分析層實測現最大號 `R-POP26`，故擬 `R-POP27`，
**執行層落檔前重查**）：

```text
R-POP27 — Pop Up List 生成基線改採 SR24 Post 2A (Dec 15, 2023)（Pei 裁，2026-09-08）

生成基線由 `forms/Pop Up List HMI R1 (26PI).xlsx` 改為
`forms/Pop Up List HMI R1 SR24 Post 2A (Dec 15, 2023).xlsx`。
理由：後者為 SR24 Post 2A 之乾淨對應，與規格基線 `SYS1_HMI_Core_HMI_
Logic_and_Flow_R1_SR24_Post_2A_(February_2_2023)` 同代；26PI 版多出之
`PU1579`（Geolocation）狀態為 `Under Review`，未核准條目不入基線。
26PI 版與 Change Log 續存 `forms/` 作 delta 對照，PU1579 轉核准後再議。

`feature.yaml` 之 `paths.popup_list` 改指新基線。
現有 5 條 TC 所引之 PU0942／PU0215／PU0580／PU0949 於兩版逐欄逐字相同，
**本次改版不改動任何 TC 內容**（比對輸出見上繳包 PU-02）。

R-POP6 之殘留 (a)「CR25802 vs CR22510 之版位關係」**結案** ——
兩版 `Main!A1` 皆逐字為 `SR24 Post 2A CR25802`，與規格基線同代。
R-POP6 之殘留 (b)（`(26PI)` 標記）依 Change Log 判為**程式增量標記**
（單一變更 PU1579，2026-05-20），非車型別；一併結案。
R-POP7（Priority Matrix 不納入）**不變** —— Pei 本次新增之 3 件不含該表，
DR-POP2 維持開啟。
```

`feature.yaml`：`paths.popup_list` 改指新檔。改前後之值寫入上繳。

---

## 二、交付前補齊（比照 VS-CF-04）

`surgical_save` 落檔，全域無 `wb.save()`。

### 2.1 `Cover 封面`

| 儲存格 | 現況 | 填入 |
|---|---|---|
| `D6`（版本） | `C` | 不動（模板版次）|
| `G6` 之值欄（修訂日期） | 空 | `2026-09-08` |
| `D9`（作者） | 空 | `PeiPYHsu` |

⚠ `G6` 之值欄位址以合併儲存格範圍實測後再寫，不得臆測；結果寫入上繳。

### 2.2 `ChangeHistory 修訂履歷` —— **不填**（同 VS-CF-04 之 Pei 裁）

該分頁 A／B／C 為模板修訂，非內容產出。**執行層不得寫入此分頁**，其 sheet xml diff 須為 0。

### 2.3 `Product Document 記錄封面頁`

⚠ 本簿之該分頁**比 CFTS044 那本更空** —— `B3`（專案代號）／`B4`（文件名稱）／
`B5`（版本）／`B6`（部門）／`B7`（分類）／`B8`（日期）／`A13`–`D13`（V1.0 列）**全空**。
CFTS044 本已預填 `new R1L`／`V1.0`／`SW Testing`／`Confidential`。

**填入**（值取自 CFTS044 交付本之同欄，逐字比照；執行層先讀該簿確認後再寫）：

```
B3  new R1L
B4  <本簿交付檔名>
B5  V1.0
B6  SW Testing
B7  Confidential
B8  2026-09-08
A13 V1.0    B13 Initial Release 初版發布    C13 PeiPYHsu    D13 2026-09-08
```

⚠ `B3`–`B7` 為**跨簿套用**，非本簿自有來源。若 Pei 認為不應由執行層代填，
停手回報，只填 `B8` 與 `A13`–`D13`。

---

## 三、交付

### 3.1 檔名

```
FM-WI-FSM-036-A01 STLA 測試用例規範與結果_SWQT STLA Test Case Specification & Result_SWQT_Popup_20260908.xlsx
```

### 3.2 落點

```
features/popup/output/<交付檔名>
features/popup/delivered/<交付檔名>
```

⚠ `features/popup/` **無 `delivered/` 目錄**（分析層實測目錄清單：batches／data／docs／
generated／inputs／output／reports／scripts）。須新建；建前先
`git check-ignore -v features/popup/delivered` 確認未被 ignore，被 ignore 即停手回報。

### 3.3 MANIFEST

`output/MANIFEST.tsv`／`delivered/MANIFEST.tsv` 各追加一列，欄序比照
`features/vehicle_setting/` 之同名表（filename／sha256／source_path／round／status／note；
delivered 側為 delivered_round）。note 記：
`Popup v1；5 條 TC／5 Functional leaf（037 V0.2 全覆蓋）；Pop Up List 基線 SR24 Post 2A (Dec 15, 2023)；Core §5.1–5.4 缺口見 COVERAGE_GAPS.md`

無 tc_id 重賦（本包不刪列不改列），**不產出 remap 表**。

---

## 四、交付前最後驗證

1. 補完後之 zip：相異 member 僅 Cover 封面與 Product Document 對應之 sheet xml **兩件**；
   `Test Case Specification` 與 `ChangeHistory` 之 xml diff 須為 **0**。
   sheet xml 與分頁名之對應以 `workbook.xml` ＋ `workbook.xml.rels` 實測。
2. `Test Case Specification 測試用例規範` 之 5 資料列（row 10–14）與原簿**逐格 diff = 0**。
3. 全簿現算並寫入上繳：資料列 5、Requirement 覆蓋 5/5（037 V0.2 之 Functional leaf）、
   PENDING 0、DR 引用 0、`BLOCKED`／`IMPL_GAP`／`NOT GENERATED` 各 0、
   Procedure↔ER 1:1、每列 ≥2 步、尾句號 0、行首尾空白 0、
   交付四欄全形標點 0、括號下半齊備、Priority ∈ P0–P3、
   Final Step 含 `check that` 且 ≤ 18 words（R-POP20）。
   ⚠ 上列各項分析層已於本輪先跑一次、**全數 0 違規**；執行層須獨立重跑，數字不符即停手。
4. `output/` 與 `delivered/` 兩檔 sha256 相同。

## 五、上繳 `up/20260908_PU-02.md` 須含

1. 三件新檔之 sha256；`10_Reviewing/` 26PI 與 `forms/` 現存本之逐位元比對結果（§1.1 之未測項）。
2. §1.2 delta 與 §1.3 之 4 筆 PU 逐欄比對輸出。
3. `R-POP27` 落檔行號與 live 取號查證；`feature.yaml` 改前後之值。
4. §四 四項驗證之現算；`G6` 合併範圍實測；`delivered/` 之 `git check-ignore` 結果。
5. 兩份 MANIFEST 落檔行號；`forms/FORMS.md` 新增行號。
6. 工具旗標逐次聲明；自報。

## 六、不在本包

- Core §5.1–5.4 之缺口補生成（Pei 裁不做，續留 `COVERAGE_GAPS.md`）
- queue 仲裁行為（全庫無需求；`queue` 於 Core export 167 列命中 0，Priority Matrix 無文字層）
- DR-POP2／DR-POP3／DR-POP4 之送出、RD-1 之上報 —— Pei（Tier 3）之操作
- commit、tag、TestRail 匯入
