# 下放包 CAM-26 —— 追溯補齊：每條需求至少一 TC（R-CAM10 撤銷、零 TC 八列各自出 TC）

指派執行層 session：延用（或新開，`DELIVERY_STATUS.md` §9 之「新增 TC 須有下放包明文指派」即本包）。
發包依據：Pei 2026-09-24 裁定「**有需求就要有產出**」—— 每條 037 leaf 需求在工作簿至少一列 TC 掛自己的 ID；D 欄佔位列（只填需求 ID）不得作為追溯替代。

---

## §0　禁區

同 CAM-14 §0。不寫 `delivered/`、不 tag。既有 455 列不改（新列另編號）。

## §1　裁定落檔

取號現查：`{live}` 期望值 **19**。

```text
R-CAM19  每條 leaf 需求至少一 TC 掛自己的 ID（Pei 2026-09-24）
  (a) 037 之每一 leaf 列（非 umbrella）在工作簿至少有一列 TC，D 欄為該列自身之需求 ID。
  (b) R-CAM10(a) 之「較大 SWE ID 不得再以共引來源作錨」撤銷；共引來源時兩列各自出 TC，
      驗證角度依各自 037 Description，reasoning 互註「同源 SWE-CAM-nnn」。R-CAM10(b)
      （IF/THEN 合一之多行錨）不受影響。
  (c) R-CAM16(a)(c) 之零 TC 形制撤銷（全來源委派／逐字重複／純交叉引用皆須出 TC）；
      R-CAM16(b)（來源自陳 N/A）保留，惟仍須出一列 TC 以其父題／鄰列之可驗事實為驗證點，
      查無可驗事實時 `PENDING: RDF-12` 佔位列（非 D 欄空列）。
  (d) 真缺件（DR-CAM-a 之 SWE-CAM-024）：出一列 `PENDING: DR-CAM-a` 之 TC 列（Test Set
      `AUX Camera`，六欄佔位），不再以 D 欄空列處理。
  (e) D 欄佔位列形制（DECISIONS 6-52／6-73）作廢。
```

R-TM13：R-CAM10(a)、R-CAM16(a)(c) 原文加刪除線並註「撤銷，見 R-CAM19」；DECISIONS 6-86 記本裁定；RDF-12 改題為「`SWE1-RVC-069` 自陳 N/A 但仍須追溯」。

## §2　補生成（`batch06/`、`b08/`）

| 需求 | 現況 | 本包 | 驗證角度 |
|---|---|---|---|
| `SWE-CAM-007`（NCD HAL，LVDS 握手） | 全委派 `-006` | ≥1 TC，ID `NR1L-RVC-246` 起 | 依 -007 Description：LVDS link 建立／握手序列（bus analyzer 觀察），不重抄 -006 之 EVS 串流角度 |
| `SWE-CAM-013`（`diagnosticRequest` LVDS message） | 全委派 `-004` | ≥1 TC | LVDS `diagnosticRequest` 訊息之送出（訊息名／值逐字），不重抄 -004 之 DTC 角度 |
| `SWE-CAM-025`（App 側 Bed Extender／fault banner） | 全委派 `-023` | ≥1 TC | App 側 banner 之顯示（文字 PENDING DR-CAM-h），CAN 側 PENDING DR-CAM-f |
| `SWE-CAM-024`（AUX camera，VF617） | BLOCKED | 1 列 PENDING TC | 六欄 `PENDING: DR-CAM-a`，Test Set `AUX Camera`，Vehicle Model 依 R-CAM18 |
| `SWE1-RVC-069`（`First surface needs: N/A`） | 零 TC | 1 TC，ID `NR1L-RVCHMI-211` 起 | 以父題（§27.3 硬體要求）之可驗事實為驗證點；查無則 PENDING: RDF-12 |
| `SWE1-RVC-086`／`-091`／`-102`（cross-reference only） | 零 TC | 各 1 TC | 依被引章節之行為，在**本列之情境**下驗（引用句本身即前提），reasoning 註被引列 TC ID |
| `SWE1-RVC-136`／`-139`（verbatim duplicate of §34.3.2／§34.4.1） | 零 TC | 各 1 TC | 同句異章（R1 Low 側）→ 前提改 R1 Low（DECISIONS 6-64 散文前提），其餘沿母 TC |

每列 reasoning 具名「R-CAM19 追溯補齊」＋同源／被引 TC ID；`coverage.tsv`／`coverage_b.tsv` 之零 TC 形制全數改為 `tc_count ≥ 1`；lint ＋ selfcheck 全綠（PENDING 列之 `U` 允許）。

## §3　兩本 dry-run 重生

- A 本 dryrun4：D 欄佔位列 4 → 0；列數 = TC 數（預估 249＋）。
- B 本 dryrun3：D 欄佔位列 6 → 0；列數 = TC 數（預估 216＋）。
- 對帳：**工作簿列數 = TC 數 = 有 TC 之 leaf 需求數 ≥ leaf 總數**（每 leaf ≥ 1）；產 `traceability.tsv`（`req_id → tc_ids`，A 25 列 25/25 有 TC、B 203 列 203/203 有 TC；umbrella 27 列另表記子列）。
- `pending_by_dr` 重生；`DELIVERY_STATUS.md` §2 改「每條 leaf 需求 ≥ 1 TC：A 25/25、B 203/203」。

## §4　上繳要求（`up/20260924_CAM-26.md`）

1. R-CAM19 sha8；R-CAM10／16 之刪除線；DECISIONS／RDF-12。
2. 補生成清單（每需求 → TC ID → 驗證角度一句）；lint／selfcheck。
3. 兩本 dry-run 路徑／sha、lint、`traceability.tsv` 對帳（25/25、203/203）。
4. `DELIVERY_STATUS.md` 更新；R-CAM17 證據表；自報；未結。

## §5　升級條件

- 任一需求依其 Description 完全無可驗事實且父題亦無（回報，該列 PENDING: RDF）。
- `traceability.tsv` 任一 leaf 為 0。
