# 下放包 CAM-16 —— 落檔 ＋ B02a（`AUX Camera Access` 前半，30）

指派執行層 session：延用。對應審閱：`down/20260923_CAM-15_review.md`（同發）。

---

## §0　禁區

同 CAM-14 §0。A 本不動（244 列口徑定案，待 Pei 准交付）。

## §1　落檔

1. RDF-11（A-CA34）。
2. profile §7 R-CAM17 註：逐字母體須取全文，不得以截斷輸出判斷。
3. DECISIONS 6-56～6-58：`CVPAM_Presence` 為 PAM 旗標；RVC timeout 兩處同載不開 DR；A 本交付候選 = dry-run v2。

## §2　B02a：`AUX Camera Access` 前 30 leaf（共 60），ID 續 `NR1L-RVCHMI-044`

依 `b_plan.tsv` 章節序（HU 27、34.1～34.8）。要點：
- PROXI 配備：`Auxiliary_Trailer_Camera`（byte 211 bit 2）、`Digital_CHMSL_Camera_Prsnt` 於 Atl-Hi 三本；Atl-Mi 三本查無者依 R-CAM18(b) 該欄 `0` 並具名（CAM-14 證據 5 已定）。
- 入口 hop：`Camera app home page`／`«Camera» shortcut icon`／`More Cams` 等逐字自 cache 本 `Description`；開啟法無來源者沿 DR-CAM-g（不另開）。
- A 本 `SWE-CAM-024` BLOCKED（VF617）：互參只單向，B 列不引 VF617，spec_reference 母體仍 SYS1（R-CAM1(c)）。
- 摘句／popup／R-CAM17 證據／selfcheck 十項同前。
- 章節家族切點：若第 30 列落於同一子節家族內，**併入該家族**（≤ 33），回報。

## §3　上繳要求（`up/20260923_CAM-16.md`）

1. 落檔清單。
2. `b02a_plan.tsv`、產物、lint、selfcheck、PENDING、Toro／Atl-Mi 判 0 之列與證據。
3. R-CAM17 證據表；自報；未結。

## §4　升級條件

- B02a > 35。
- hop label 不可辨 > 3 列。
- 任一列之 PROXI 配備旗標於六本皆查無且 SYS1 無替代條件（回報，不造）。
