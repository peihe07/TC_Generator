# 下放包 CAM-17 —— 落檔 ＋ B02b（`AUX Camera Access` 後半，29）

指派執行層 session：延用。對應審閱：`down/20260923_CAM-16_review.md`（同發）。

---

## §0　禁區

同 CAM-14 §0。A 本不動。

## §1　落檔

取號現查：`{live}` 期望值 **19**（R-CAM18 已在；R-CAM16 增款以同級錨 `R-CAM16(b)` 落檔，不消耗 live）。

1. **R-CAM16(b)**：來源自陳 `N/A` 之零 TC 形制（審閱 §一-1 文面）。
2. DR-CAM-h 擴題（兩項）；`DATA_REQUESTS.md` 更新。
3. RDF-12（`SWE1-RVC-069` 不應為 leaf）、RDF-13（`Enable Wireless Cameras` 大小寫）。
4. profile §7.8：彈窗文字之 ER 形制「逐字文字 ＋ `(PUnnnn)`」，比對判準只在 ER。
5. DECISIONS 6-59～6-61。
6. `coverage_b.tsv` 之 `SWE1-RVC-069` 註改為 R-CAM16(b) 形制。

## §2　B02b：`AUX Camera Access` 後 29 leaf（§27.7.1–§27.8.5、§34.1.1.1–§34.8.4），ID 續 `NR1L-RVCHMI-074`

規則同 B02a。要點：
- §34（R1 Low Wired AUX）之配備條件：PROXI `Auxiliary_Trailer_Camera`／`Digital_CHMSL_Camera_Prsnt`（Atl-Hi only，Atl-Mi 三欄 `0` 具名）；R1 Low 之「Low」為 HU 等級非平台，不以此判車型。
- `Controls screen` 入口 → DR-CAM-g；status bar shortcut → DR-CAM-g(2)。
- popup 依 `forms/Pop Up List HMI R1 (26PI).xlsx`；查無 → `PENDING: DR-CAM-h`（已擴題）。
- 家族切點同法（≤ 33）。

## §3　上繳要求（`up/20260923_CAM-17.md`）

1. 落檔清單；R-CAM16(b) sha8。
2. `b02b_plan.tsv`、產物、lint、selfcheck、PENDING；`coverage_b.tsv`（`AUX Camera Access` 應 60/60 ＋ umbrella）。
3. R-CAM17 證據表；自報；未結。

## §4　升級條件

- B02b > 35。
- hop label 不可辨 > 3 列。
- §34 任一列之配備條件於 PROXI 六本皆查無且 SYS1 無替代條件。
