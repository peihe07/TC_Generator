# 下放包 CDD-02 —— Diagnostics pilot01（12 個 037 列 → 預估 16–20 TC）

前件：`down/20260917_CDD-01_A.md`、`up/20260917_CDD-01_A.md`、
`down/20260917_CDD-01_A_review.md`（主審閱）、`_review_A.md`、`_review_B.md`。
依 Pei 2026-09-17 常規：審閱後即發，本包依據 = 主審閱 §三／§四 ＋ 追補 B §三；Pei 不同意者於上繳後撤回。
執行層 session：沿用 CDD 線 session。

---

## §0　禁區

- git 一律不動；Pei 為唯一 git 操作者。
- 不動 `down/`、`up/` 既有檔；不動其他 feature 目錄。
- **只產 §3 所列 12 個 037 列**之 TC；其餘 377 列不得生成。
- `docs/fw036/RULINGS.sha.tsv`：一律依 **R-G75**（追補 B §三）先 `git diff HEAD` 合併後寫，不得 append／覆寫；上繳記三個列數。
- 條文本體不改；R-DIAG5(amend) 增行以 `R-DIAG5(amend2)` 落。
- `framework.md` 維持 DRAFT，本包不 LOCK。
- Priority／Design Method 值域以 SWC 0708 實測為準（up CDD-01 §7-4），不得自創值。

## §1　依據（審閱裁定，執行層取號現查 R-G23）

```text
R-G75  RULINGS.sha.tsv 併發寫入過渡規則
  （條文逐字取自 down/20260917_CDD-01_A_review_B.md §三；落 RULINGS_LEDGER.md）
```

```text
R-DIAG5(amend2)  0x11 之 037 觸發語含標題句式
  R-DIAG5(amend)(d) 之 `0x11 serviceNotSupported` 觸發語，除 Description 之「Service Not Supported」外，
  含標題句式「Unsupported service requests」。SWE1-Diagnostics-157 由 PENDING_DR1 改 RESOLVED_ISO；
  DR-DIAG-1 餘 2 列（-156、-237）。
```

```text
R-DIAG10  Priority 判準（草案，pilot 後 Pei 裁）
  P0  I/O Control 寫入（0x2F）與 Routine Control（0x31）之正向 TC —— 直接改變車機輸出或狀態
  P1  DID 讀（0x22）／寫（0x2E）之正向 TC；I/O、Routine 之 negative
  P2  DID 讀寫之 negative、全部 unsupported（0x11）
  P3  無
  Out of Scope 列不填 Priority（依 SWC 0708 該列空白慣例，執行層實測後定）
```

`[A-DIAG15]`：GC-16 全 repo 重生排於 SEC 線收斂後另包，本包不做。

## §2　前置（T0，產 TC 前完成）

1. R-G75 落 ledger；R-DIAG5(amend2)、R-DIAG10 落 `features/diagnostics/RULINGS.md`；跑 `rulings_hash.py`，依 R-G75 寫 `RULINGS.sha.tsv`（預期 50 → 53）。
2. `nrc_coverage.tsv` 之 `-157` 改 `RESOLVED_ISO`（0x11）；DR-DIAG-1 改 2 列。
3. 副本實測：`features/diagnostics/data/column_map.tsv` —— SWC 0708 34 欄 ↔ 本 feature 欄位對照（up CDD-01 §7-4 已測，落檔即可），含 `AF`／`AH`、Priority 值域、Design Method 雙語下拉值域、Reference ID 慣例。
4. 讀 canon §5.4／§6.1／§8.7.5、`R-SEC15`（位元組步驟型態）、SWC 0708 前 20 列（欄位風格）後才開寫。

## §3　pilot01 列表（12 個 037 列）

| # | SWE1 ID | 037 row | Source | L3 | 型 | Region | Harman | 驗證重點 |
|---:|---|---:|---|---|---|---|---|---|
| 1 | -001 | 8 | DIAG-146 | `$283F` Camera Level Brightness (R/W) | positive 寫 | All | Need Rework | `2E 28 3F <val>` ＋ `22 28 3F` 讀回；CFTS004 值域 → sibling（R-DIAG8(c)）|
| 2 | -002 | 9 | DIAG-146 | 同 | negative | All | Need Rework | `RESOLVED_ISO`：0x13（長度）／0x31（超範圍）各一 TC（R-DIAG1(b)）|
| 3 | -003 | 10 | DIAG-146 | 同 | unsupported | All | Need Rework | 0x11 |
| 4 | -004 | 11 | DIAG-147 | 同 | positive 讀 | All | — | `22 28 3F`；Harman Scope 不註（status 空）|
| 5 | -112 | 119 | DIAG-323 | `$5000` Buzzer Control | positive I/O | All | — | `2F 50 00 <ctrl> <rec>`（`RESOLVED_037` 0x2F）＋ tone level 隨 volume 之 ER |
| 6 | -113 | 120 | DIAG-323 | 同 | negative I/O | All | — | 0x2F 之 NRC；`7F 2F xx (<label>)` |
| 7 | -151 | 158 | DIAG-363 | `$0312` AV Signal Detection | positive Routine | All | Need Rework | `31 01 03 12 <ch>` Start ＋ `31 03 03 12` Results 兩步；Routine 之 ER 分項（§6.1）|
| 8 | -154 | 161 | DIAG-367 | 同 | negative | All | Need Rework | `RESOLVED_CFTS004`：`$31` 原文 label 用 CFTS004 用字 |
| 9 | -272 | 279 | DIAG-079 | `$2843` X65 Module Version | positive 讀 | NAFTA | — | 多子欄位 DID 之 ER 分項；NAFTA → `1 1 1 0 0 0 0` |
| 10 | -340-001 | 347 | DIAG-264 | `$500D` Video Input Settings | negative | All | Need Rework | R-DIAG2 ID 形式；`R1-DIAG` |
| 11 | -057 | 64 | DIAG-248 | `$1801` Screen Test Pattern | Out of Scope | 空 | Need Clarification | R-DIAG3(amend)：`AF` = `Out of Scope`、`AH` 註；不產 TC 仍佔列、TC ID 順號 |
| 12 | -189 | 196 | DIAG-124 | `$2841` X65 Signal Quality | positive 讀 | NAFTA | — | R-DIAG6 VM blank Remarks 句式；依 -187 同型 |

預估 TC 數：#1 2–3、#2 2、#3 1、#4 1、#5 1–2、#6 1–2、#7 1、#8 1、#9 1–2、#10 1、#11 1（佔列）、#12 1 → **14–18 TC ＋ 1 佔列**。

## §4　產出規格

- 輸出 `features/diagnostics/sandbox/pilot01/diagnostics_pilot01.xlsx`（FW036 樣板，`Test Case Specification 測試用例規範` 分頁，資料 r10 起）＋ `pilot01.json`（10 鍵 ＋ reasoning）。
- TC ID `NR1L-DIAG-001` 起，依 framework 順序（Layer 2 #6 Camera → #11 Buzzer → #18 AV → #1 SXM Module → #2 SXM Signal → #17 Video Input → #10 Screen Test Pattern）連號；pilot 通過後 batch 依 framework 全序重編。
- `test_item` 上半 = 037 Description 第一句 verbatim（R-SEC15 前例），含拼字；下半 `(...)` 英文，區分 token = DID／SID／NRC 值。
- `specification_reference` = `CFTS004-{ObjectID}` 一行一錨（§10.7(a)），家族內升冪；ObjectID 取 `source_resolution.tsv`。
- Pre-Condition：診斷工具連線與 session 狀態只寫 CFTS004／SYSAD 明載者；未載 → `PENDING: DR-DIAG-3 diagnostic session prerequisite`（預期會出現，登 DATA_REQUESTS）。
- Procedure：UDS 位元組串依 R-DIAG5(amend)(b)；工具名寫 `diagnostic tool`（不指定廠牌）；I/O Control 之 controlOptionRecord 值取 037／CFTS004 原文，未載 → PENDING。
- ER：1:1；positive response 位元組串 ＋ 可觀察現象（HU 畫面／音訊／CAN），現象只寫 CFTS004 明載者。
- Remarks：R-DIAG7(a) `Harman Scope:`／R-DIAG6 VM blank／R-DIAG5(amend)(d) `NRC per ISO 14229-1 (037 unspecified)`／(e) `SID per 037 <SWE1-ID>`。
- Vehicle Model 依 R-DIAG7(amend)；Input Test Data `NA`；Priority 依 R-DIAG10 草案；Design Method 依 canon §12 首中。

## §5　lint 與自檢

1. `lint036.py --profile diagnostics` 對 pilot 本實跑：全域檢查項 ＋ `P-DIAG`／`U-DIAG`／`R1-DIAG`／`Z`／`RM-DIAG`；紅數逐項歸因（真違規 vs 判準待調），**不得為降紅改 TC 內容以外之東西而不記錄**。
2. canon §9 自檢 18 項逐 TC 表。
3. R-SEC15 型自檢：無跨情境字串搬移、觀察不代觸發、內部代碼只在 Remarks、字面只用雙引號。
4. `coverage.tsv`（pilot 範圍）：12 列 → TC 對映，-057 記 `OUT_OF_SCOPE`。

## §6　預期數字

| 項 | 預期 |
|---|---|
| TC 數 | 14–18 ＋ 1 佔列 |
| PENDING 行 | ≤ 10（預期集中於 session 前提與 controlOptionRecord）|
| lint 全域項 | K=0、U 除 PENDING 外 0、P=0 |
| `R1-DIAG`／`Z`／`RM-DIAG` | 0 |
| `RULINGS.sha.tsv` | 50 → 53（R-G75、R-DIAG5(amend2)、R-DIAG10）|
| DR-DIAG-1 | 2 列 |

## §7　上繳要求（`up/20260917_CDD-02.md`）

1. T0 四項產物；`RULINGS.sha.tsv` 寫前／寫後／HEAD 三列數（R-G75(c)）。
2. pilot xlsx ＋ json 路徑；TC 清單（ID／SWE1 ID／Test Set／型／Priority／PENDING 數）。
3. lint 五項紅數與逐項歸因；判準待調者附建議改法（不自改判準）。
4. §9 自檢 18 項表；R-SEC15 型自檢。
5. `column_map.tsv`；新 DR（預期 DR-DIAG-3）。
6. §6 預期 vs 實測。
7. 兩個 TC 全文貼入上繳包供分析層細讀：#1 之一條 ＋ #7。
8. 自報錯誤；未結清單。

## §8　升級條件

- `RULINGS.sha.tsv` 寫後 ruling_id 集合不等於 HEAD ∪ 工作區獨有 ∪ 新增。
- 12 列中任一列因來源全缺而整條 PENDING（超過 Pre-Condition 一行）。
- lint 全域項出現非 PENDING 之 U／K／P 紅且無法歸因為判準問題。
- 任何需改 canon 之情形。
