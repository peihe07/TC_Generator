# SEC-11 審閱 —— 上繳包 `up/20260917_SEC-11.md`（`security_v06`，70／70 全覆蓋）

審閱層：分析層，2026-09-17。
判定：**通過，不升級**。四項升級條件不成立；`OpenSSL` 命中判為程式庫名、非 CLI 調用——**執行層判斷正確**（19 列 037 Verification Method 全為 Peer Review 是決定性旁證）。
里程碑：**六本 037 全 70 列皆有 TC，124 TC，PENDING 0，佔位 96，lint 門檻全 0。** Security 文字版至此**產出完成**；後續只剩 Revise（資產／上游／`W=4`）與自動化階段。

---

## 一、裁定（併下一 Revise 落檔）

| # | 項 | 裁 |
|---|---|---|
| 1 | R-SEC22(a) C3 未及 `LOGENC-004` | **R-SEC22(a) amend**：C3 加 `NR1L-LOGENC-007`／`-008`。SEC-09 review 條文只列 KI 是我把「若在產出群」的條件寫丟了（A-30）；執行層照字面辦正確 |
| 2 | `NEW-UPD-01` 由 DEFERRED 轉 `AXIS_GAP` | **裁 `ACCEPTED_GAP`**，登 **A-SEC-11**：SWDL-001 之 037 VC 無 rollback 負向要件，拆即越 037；理由同 `NEW-CERT-02`。`AXIS_GAP` 歸 0 |
| 3 | 037 三處內部矛盾 | **登 ANOMALIES**：**A-SEC-12**（ECUCert STORAGE_IO 之 VC 與 JNI 逐字相同，疑複製貼上）、**A-SEC-13**（LOGENC-008 Description RSA vs VC AES）、**A-SEC-14**（LOGENC-005 `RSA symmetric key` 措辭矛盾）。TC 照 VC 逐字不改（執行層處置正確）；三條隨 A-SEC-8~11 一併回饋 037 作者 |
| 4 | `DOWNLOADCERT_INTF` 拆 2 | **准 2 TC**；文件審查不需實連，§3 之註是我對執行面的顧慮，不適用於文件審查（A-31） |
| 5 | batch1／batch2 不產 `_v06` | **准**；merged 以 JSON 為輸入，三本齊備非形制要求。上繳包記明即可 |
| 6 | LOGENC-003 只 1 TC（API 只一個）| **准**；§3「每 API 一 TC」以 037 為準 |
| 7 | `rule` 欄中文改英文 | **准**；Remarks 全英文入 profile §3 體例（IN §1 之延伸，Remarks 雖非 lint 面仍為交付欄）|

## 二、分析層自報（登台帳）

| # | 錯誤 |
|---|---|
| A-30 | SEC-09 review R-SEC22(a) C3 落點漏 `LOGENC-004`（SEC-09 §1 表有、review 條文無）|
| A-31 | SEC-11 §3 對 `DOWNLOADCERT_INTF` 加「STLA 連線本身不驗」之註——把執行面顧慮套到文件審查 |
| A-32 | SEC-11 §3 之 19 個 artifact 預判，17 個與 037 GIVEN／受詞不符——**未讀 037 就寫**；執行層以 037 逐字覆核為正解 |

## 三、通過項
R-SEC23 逐字落檔、33 錨點；24 sibling／19 列、切分規則清楚（THEN／列舉／單句不切）；artifact 逐列以 037 GIVEN 覆核；ID 接續正確；124 TC、六組分佈、P2 +24；`coverage.tsv` 70／70、`batch_order`／`peer_review_rows` 同步 `CONVERT_DOC`；lint 門檻 0、selfcheck 三批 0、回歸 byte 相同；`ACCEPTED_GAP` 落地、ANOMALIES 66→50 行去重無遺漏；SEC-10 四支 commit 帶 pathspec。

## 四、Security 文字版完成狀態

| 項 | 值 |
|---|---|
| 母體 | 6 本 037，70 列，**70 列全有 TC** |
| TC | **124**（CP 23／KI 25／SAM 33／ECUC 20／SWDL 11／LOGENC 12）|
| Priority | P0 28／P1 53／P2 43 |
| 驗證型態 | 實測 92（含 96 處資產佔位）／文件審查 32（R-SEC20 8 ＋ R-SEC23 24）|
| lint | `U`／`H`／`K`／`P`／`X`／`SC`／`SS` = 0；`W=4` 待 Pei |
| 裁定 | R-SEC1~23 ＋ 9 amend；framework LOCKED v03 |
| 交付候選 | `delivery/…_SWQT_Security_20260917.xlsx`（`ae0fb17354bf95f7`）＋ `coverage.tsv` ＋ `placeholder_*` ＋ `asset_request.md` |
| 未結 | DR-a／b／i／r／s／t；A-SEC-1~14；`X-` 11 token；Cover 三格；`C12`；`W=4` |

## 五、下一包（SEC-12，不等外部）
內容：(1) §一 之 R-SEC22(a) amend、`ACCEPTED_GAP`／A-SEC-11~14 落檔；(2) **`delivery/EXEC_GUIDE.md`**——自 `step_assets.tsv` 生成之一次性環境建置與各通道操作指南（`adb install` 兩支 apk、`pm list instrumentation` 確認、`adb root`、UDSTool 連線與 session、PCAN／DBC 設定、`SecurityAssets/` 放置、logcat 讀法、`od` 讀法、文件審查之取得方式），同時作為日後 `gen_pytest.py` 之 helper 規格；(3) `security_v07` 只帶 Remarks 兩行變更。
Pei 准則下放；自動化骨架（`gen_pytest.py`）依 Pei 指示留待資產到手後。

git：三支照建議落。
