# 下放包 CAM-21 —— 落檔 ＋ B 本 sandbox 組簿 dry-run ＋ 全案交付前置清單

指派執行層 session：延用。對應審閱：`down/20260923_CAM-20_review.md`（同發）。

---

## §0　禁區

同 CAM-14 §0。不寫 `delivered/`、不 tag；A 本 dryrun2 不動。

## §1　落檔

1. RDF-17（`Make Favorite`／`Edit Favorite`）。
2. DECISIONS 6-71～6-73：RDF-16 處置；跨本同句不委派；B 本零 TC 六列補 D 欄、umbrella 不補。

## §2　B 本 sandbox dry-run

1. 203 列（197 TC ＋ 6 個 D 欄佔位）組簿 → `features/camera/sandbox/b_dryrun/FM-WI-FSM-036-A01 … SWQT_RearViewCamera_B_<date>_dryrun.xlsx`；Cover／Product Document 同 A 本 dryrun2 之最小欄；approver／reviewer／中文作者名留空。
2. D 欄升冪（`SWE1-RVC-001`～`-159` 含子項；子項序依 037 之列序，回報排序規則）；Test Group `Rear View Camera`；Test Set 八組拼寫一致。
3. `lint036.py --profile camera` 全表；`Z`；sheet 級問題（`N` 欄多行、`AH`、七欄、`X` 之 GCB-11 類計數）。
4. `coverage_b.tsv` ↔ 工作簿對帳（197 ＋ 6 ＝ 203）；`pending_by_dr_b.tsv`。
5. sha256 回報；不 tag。

## §3　全案交付前置清單（供 Pei）

1. A 本候選 `…_A_20260923_dryrun2.xlsx`（244 列）、B 本候選（本包產出，203 列）。
2. 阻交付 DR：A 本 f／g／i／j／k／m／p／q，B 本 g／h／r；`pending_by_dr.tsv`／`_b.tsv` 各附列數。Pei 裁：結案／降 NA／帶 PENDING 不出貨。
3. `bench_verify.md` 91 列實機驗證。
4. RDF-01～17 回 RD 作者。
5. Phase 7（`--write`／tag）待 Pei 准；交付檔名依 workflow 之模板事實（`_SWQT_RearViewCamera_A_<YYYYMMDD>.xlsx`／`_B_`），是否合併一本或兩本分交由 Pei 定（R-CAM1(a) 為兩本）。

## §4　上繳要求（`up/20260923_CAM-21.md`）

1. 落檔清單。
2. B dry-run 路徑／sha、lint 全表、sheet 級問題、對帳、`pending_by_dr_b.tsv`。
3. §3 清單之實數填齊（DR ↔ 列數、bench 列數、RDF 數）。
4. R-CAM17 證據表；自報；未結。

## §5　升級條件

- B dry-run lint ERROR 類非 0 且原因在組簿工具。
- D 欄子項排序無法自 037 列序唯一決定（回報衝突列）。
