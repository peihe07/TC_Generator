# 下放包 CMF-03 — Comfort HMI Revise 候選本 v2（CMF-02 審閱裁定 ＋ M7 引號 ＋ 內查 DR-46）

- 日期：2026-09-21
- 前置：`down/20260918_CMF-02_review.md`（R-C54～R-C59、R-C49(amend)、R-C50(amend)；編號為暫編，落帳時依 live numbering，附對照）
- 產物落點：`features/comfort/sandbox/revise_0921/`。**不入 `delivered/`、不覆寫 0907 本與 `revise_0918/` 候選本、不 commit、不跑 `--gate`／`--regen`、不手改 `RULINGS.sha.tsv`。**
- 開工前：`git status` 回報 CMF-02 產物是否已提交；未提交則照列，不代為處理。**不得重跑任何 `gen_batch*.py`**（R-C59）。

## 1. 文件層

1. `RULINGS.md` 落 R-C54～R-C59 與兩條 amend（條文逐字取 review §2；R-C49／R-C50 原文加旁標不改）。
2. profile 同步：§3.1.1（摘句首字母大寫）、§3.5（N 欄家族排序）、generator 凍結之註記。刪除線保留。
3. `ANOMALIES.md` 登 A-CMF8～11。`DATA_REQUESTS.md` 登 DR-48（Recirc 自動開 A/C 時是否顯示；2.4 只於 Defrost 句載 `Do not show this change`）。DR-49（`Climate` 畫面之 HMI entry path）**先不登**，待 Pei 對 review §3-3 表態。

## 2. JSON 層（一次性腳本 `cmf03_apply.py`，逐處斷言原值，可自 HEAD 重現）

| 項 | 作法 |
|---|---|
| R-C54 `003-06` front defrost | 補產 1 條 sibling；下半與同 req 各條兩兩相異；原條同步瘦身（若原條含該觸發）|
| R-C55 `005-04` RECIRC | 補產 1 條；ER 取 **037 leaf 逐字**之 `change not shown` 所對應之可觀察句（比照 `json-139` 第 3 步之既有寫法，兩條措辭一致）；Remarks 自足 ambiguity 句，登 `AMBIGUITY_REMARKS`。原條瘦身 |
| R-C56 | -467／-468 之兩行 `PENDING: DR-47` → 條文逐字 ER（與 `json-275` 同句）；三條皆加 Remarks ambiguity 句（英文、自足、≥ 40 字元、無內部 id）|
| R-C50(amend) | 14 條摘句首字母轉大寫；`test-item-upper-verbatim` 之比對對**首字母一字**做大小寫正規化，其餘逐字。gate 改動須附一個負向測試（首字母以外之差異仍 FAIL）|
| R-C49(amend) | `_compact_refs()`：外部／CFTS 行 → leaf 自身節次 → 其餘升冪。**先量**受影響格數並出 before／after；節次集合增減 ≠ 0 即停 |
| R-C58 | 內查 `forms/` 全部 DBC 與 CFTS043 tree view：`RECIRC_STAT`、`EBL_Stat`（含大小寫變體）。報檔名、message、signal、`VAL_` 逐字。**查得** → 四條之 PENDING 行改 `Send CAN: <MESSAGE>.<Signal> = <raw> (<label>)`（canon §8.7.5(c)，label 逐字），DR-46 標 CLOSED 並記出處；**查無或只得其一** → 該條維持 PENDING，不以語意相近之他訊號代入（§8.7.5(g)）|
| M7 引號 | 依 review §2-1 七問套用 `label_quote_plan.tsv` 之 label 類。第 5 問先量 SR24 export 中 `SYNC`／`Sync` 之次數與所在句，取用於按鍵標籤者。第 1 問之 91 列不動。改後重掃，報 label／state／verbatim 三類之 before／after |
| R-C57 | `lint036.py` `FEATURE_CHECKS`：comfort 範圍內 `PENDING:` 起首之整行不入 `P` |
| R-C59 | 各 `gen_batch*.py` 入口加 guard；`test_item.py` `HAND_WRITTEN` 加凍結註記 |

新 sibling 之 TC ID 接 `-475` 起（R-C46），依 req_id 遞增。

## 3. 候選本 v2

- 母本仍為 0907 本之複本（不是疊在 `revise_0918` 上）；`cmf02_revise.py` 之流程沿用，輸出 `…_SWQT_ComfortHMI_20260921.xlsx`。
- invariant 同 CMF-02 §4（zip member、DV sqref ABORT 級；逐格對帳不符 0）。
- 逐格對帳另加一欄：**相對 `revise_0918` 候選本**之差異格數與歸因（每格歸到本包一項）。
- 車型欄：W／X＝0；T／U／V／Y／Z 不動（待 Pei 對 review §3-2 表態）。

## 4. 只出 plan（不改 TC）

`docs/reports/nav_rewrite_plan.tsv`：每條 TC 之導航步驟 → 目的畫面分類（Comfort category／Climate screen／Home widget／其他）→ 可否由 `ENTER_COMFORT`（`Press "Apps" on Menu Bar to open App Drawer` → `Select "Comfort" in the App Drawer`）到達、之後尚缺哪一 hop、該 hop 於 Comfort HMI 條文有無逐字標籤（附節次與逐字）。報各類條數與「改寫後 ER 位移」之條數。**不猜 hop。**

## 5. 停點

- R-C49(amend) 節次集合有增減。
- R-C58 查得之訊號名與條文之 `$…$` 名不同名（不代入，回報）。
- M7 套用後任一條之 `test-item-lower-distinct`／`ui-bracket` 由綠轉紅。
- 停點粒度：逐條（同 CMF-02）。命中寫上繳包首段。

## 6. 上繳

- 首段：停點、PENDING 行數（目標：R-C58 查得則 0，否則 4）、TC 總數（預期 476）、對 0907 本與對 `revise_0918` 之不符數。
- lint 兩層各一次，附 `LINT_STATUS: OK`；報 J／P／U／L 之 before／after（目標 J＝0、P＝0、L＝0）。
- 附：暫編↔實編對照、R-C49(amend) before／after、R-C58 內查結果、M7 三類計數、`nav_rewrite_plan.tsv`、產物 sha256 表、`DELIVERY.sha256` ENTRY（type `candidate`）。
