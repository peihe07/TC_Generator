# 下放包 CMF-04 — 車型欄逐 TC 判定 ＋ 差異拆題（plan-first）

- 日期：2026-09-22
- 前置：`down/20260921_CMF-03_review.md`；Pei 2026-09-22 裁：「如果車型方面有差異，那就需要拆題」—— 即 PROXI 值之差異視為真差異，依 Pei 2026-09-16 規則處置（值／觸發／ER 隨車型而異 → 拆 sibling；共用 → 一條，適用車型標 `1`）。
- 產物落點：`features/comfort/sandbox/revise_0922/`（候選本 v3 只在 §4 通過後產）。**不入 `delivered/`、不 commit、不跑 `--gate`／`--regen`、不重跑 generator。**
- 開工前 `git status`：CMF-03 產物是否已提交，照列。

## 1. R-C62 車型判定規則（分析層裁，Pei 可撤）

以 `vm_axis_plan.tsv` 為底，每條 TC 對三台（V Promaster／Y Toro／Z Fastback）定 `1`／`0`：

| 情況 | 處置 |
|---|---|
| PC 載配置軸，該軸有 PROXI 參數對應，三台之值使該 TC 於某台**不成立** | 該台＝`0`，其餘＝`1`。**不拆**（TC 本身已是該軸之 sibling）|
| 同一 leaf 只有一條 TC，但其觸發／ER 依 PROXI 值在三台間**不同**（例：Toro `Climate_Type=Manual` 使 AUTO 相關步驟不存在，而 leaf 未依 ATC／MTC 拆）| **拆**：每台一組值一條 sibling，各標所屬台＝`1`；ID 接尾自 `-477` |
| PC 無配置軸、內容不涉 PROXI 參數 | 三台＝`1` |
| 軸有但**無 PROXI 參數對應**（Auto Recirc、AUTO ECO 於 PC 從未逐字出現；EMEA ICS 無參數）| 三台維持 `1`，逐條列於 `vm_unmapped.tsv`，待 Pei |

PROXI 值取 `proxi_axis_values.tsv`；`Dec` 與 `PROXI Read` 不一致者，**以 `Dec`（含 label）為準**並在 `vm_assign.tsv` 註記另一值 —— 理由：label 只附於 `Dec`，`PROXI Read` 無法對應條文文字。此為分析層之判斷，請 Pei 核。

**軸 ↔ 參數之對映**（執行層先證條文用語與 PROXI label 為同一物，附逐字；證不出者入 `vm_unmapped.tsv`）：

| PC 軸（條文語） | PROXI 參數 |
|---|---|
| ATC／MTC | `Climate_Type`（2=Automatic／1=Manual）|
| Rear HVAC | `Rear_Climate` |
| knob 型態／`3 knob HVAC controls with ICS`（2.14）| `Climate_Panel_Type` ＋ `Integrated_Climate_Touchscreen` |
| ICS | `Integrated_Climate_Touchscreen` |
| Tri-Mode | `HVAC_Mode_Type` |

## 2. 產出（先 plan，停點後才動候選本）

1. `docs/reports/vm_assign.tsv`：每條 TC × 三台之 `1`／`0`、依據之 PC 行逐字、PROXI 參數與值、規則分支（§1 四列之一）。
2. `docs/reports/vm_split_plan.tsv`：需拆者，逐條列原 TC、拆成幾條、各條之台別與差異之欄位（觸發／ER），預估新增數。
3. `docs/reports/vm_unmapped.tsv`。
4. 彙總：三台各自之 `1` 條數；全 `0` 之 TC 條數（**任一條三台皆 0 即停 —— 代表該 TC 於受測車型皆不適用，需 Pei 裁是否仍出貨**）。

## 3. 停點

- 拆題預估新增 > 40。
- 任一 TC 三台皆 `0`。
- 2.14 排除式 PC 之 454 條：若依 §1 對映使 Toro 全為 `0`，**停**，回報條數與依據，不填。
- 條文語與 PROXI label 無法證同一物之軸 > 2 個。

## 4. 通過停點後

依 `vm_assign.tsv` 填 T–Z，依 `vm_split_plan.tsv` 產 sibling（下半兩兩相異、PC 載該台之配置條件、R-C47 級）；母本仍為 0907 本複本；invariant 與逐格對帳同 CMF-03 §6；產候選本 v3 `…_ComfortHMI_20260922.xlsx`。

## 5. 併入本包（CMF-03_review §2 已裁、不涉車型）

R-C60（合稱不加引號，先量）、R-C61（`H`／`W` 位置條件式豁免，先量 `-035`／`-301`／`-440` 是否同型）、`-146` Remarks、軸 2 兩條維持。DR-46 四行**仍 PENDING**（alt-mi ＝ Atlantis MID 一問 Pei 尚未答）。

## 6. 上繳

首段：停點命中、三台 `1` 條數、拆題數、全 `0` 條數、PENDING 行數（預期 4）。附 §2 三表、R-C61 量測、產物 sha256、`DELIVERY.sha256` ENTRY、lint 兩層 `LINT_STATUS: OK`。
