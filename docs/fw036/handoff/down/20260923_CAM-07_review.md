# 下放包 CAM-07 審閱（分析層 2026-09-23）

對應上繳包：`docs/fw036/handoff/up/20260923_CAM-07.md`
判定：**裁決落檔全數通過；batch02a 26 列 lint／selfcheck 全綠**；抽讀 5 列（048／058／062／068／069）發現 **2 類重複性寫法偏差**（§二），修正後 batch02a 判過，計 R-G14 第 2 批。CAM-08 隨本審閱同發。

---

## §一　確認與裁決（上繳 §2-1、§3-3、§4、§5 之待裁項）

| # | 項 | 裁決 |
|---|---|---|
| 1 | selfcheck 第 1 項放寬（常數錨／合一錨） | **准**。判準逐字如上繳 §2-1；兩類皆逐列印出之作法保留 |
| 2 | `J` = 5 首字小寫 | **准維持逐字**。profile 加豁免條：「`test_item` 上半為來源逐字時，`J` 不適用」；GC_BACKLOG 記 lint 側改為以逐字為豁免條件（GCB-07） |
| 3 | `-068` label 推定（row 470 `Rear View Camera with Rear Door` ↔ `RVC_Swing_Doors_Check.Req`） | **准為來源對應**：HMI Settings List row 470 之 `F` 欄 `VF551` ＋ `H` 欄語意足以定位，非 §8.4.1 之造值；reasoning 保留「對應由 H 欄語意建立」一句。不退 PENDING |
| 4 | `V33-420` `Rear_Camer_Enable.Info` 拼寫 | **登 RDF-07**，逐字不改 |
| 5 | Atl-Mi 車型歸屬判準（V3 於 V42／V33 無對應時承三平台…） | **准並立號 R-CAM15**（文面即上繳 §4-4 三句） |
| 6 | A-CA27「零命中」已不成立 | 原列不改，**加註**「2026-09-23 母體增六本後為 275 條，LVDS 相關仍 0」 |
| 7 | `-061` 只驗 Off 側 | 接受，不生成第三列；reasoning 已具名即足 |
| 8 | LVDS 觀察不造命令、以 bus analyzer 訊息名／值書寫 | 接受 |
| 9 | `T_INITDISPLAY = 5,0 s`／`Tpower = 5 s` 之實測 | 接受，記入 profile §9 常數表 |

## §二　抽讀之寫法偏差（重複性，須全批修正並入 selfcheck）

| # | 型 | 實例 | 依據 | 修正 |
|---|---|---|---|---|
| 1 | **CAN source 行之濫用** | `-058`（只勾 2261，步 1 已直接用 `STATUS_BH_BCM2`）、`-062`（無 `Send CAN` 步）、`-069`（只勾 Atl-Hi） | R-CAM3(e) 之句式只為「跨兩 EE **且**有 `Send CAN` 步」而設（CAM-06 審閱 §二-3 已裁 `-023` 同型） | **R-CAM3(f)**：CAN source 行僅於 TC 同時勾 Atl-Hi 與 Atl-Mi 車型且 Procedure 含 `Send CAN` 步時書寫；單一 EE 之列直接以該平台訊息名寫 `Send CAN`，不加該行。selfcheck 第 5 項 |
| 2 | **設定操作句式** | `-062`／`-068` 步 4／5 `Select "X" and set it to on` | canon §5.8(e) `Set "Type" = "Latching"`；pilot `-007` 已用 `Set "…" = "On"` | 改 `Set "Rear View Camera Active Guidelines" = "On"`；值 `"On"`／`"Off"` 依 HMI Settings List 該列之選項 label（大小寫逐字）。selfcheck 第 6 項（regex `Select ".+" and set it to`） |
| 3 | CAN 讀取句式 | `-062` 步 5 `Read the bus analyzer recording and check TELEMATIC_VEHICLE_SETUP.DynamicGrid_Req` | §8.7.5(d) `Read <MESSAGE>.<Signal> and check that it is <raw> (<label>)` | 改 `Read TELEMATIC_VEHICLE_SETUP.DynamicGrid_Req and check that it is 1 (Dynamic Gridlines ON)`；ER `… = 1 (Dynamic Gridlines ON) is sent`（`is transmitted` 改 `is sent`，對齊語料） |
| 4 | Hold 步驟併入檢查 | `-069` 步 3 `Hold for 5 s and check the supply …` | §8.7.5(f) `Hold for <n>` 獨立成步，ER `The signal is held for <n>`；供電之檢查另成 Final Step | 拆為步 3 `Hold for 5 s`／步 4 `Read the LVDS supply to the RVCM and check that it is still present` |
| 5 | 品牌行之濫用 | `-068`（row 470 無 `*`，仍寫品牌行） | R-CAM5(e) 只為操作帶 `*` 之 label 而設 | 刪 |

全四目錄（72 列）以 selfcheck 第 5／6 項掃描，命中清單入上繳；同型一律改。

## §三　新立條文（CAM-08 落檔）

- **R-CAM3(f)**（§二-1）。
- **R-CAM15** Atl-Mi 車型歸屬判準（上繳 §4-4）。
- **RDF-07** `V33-420` 拼寫。
- profile：`J` 豁免；§9 常數表（`Tpower` 5 s、`T_INITDISPLAY` 5,0 s、`MAX_SPEED` 13,0 km/h、`c_VEHSPD_MAX` 8 mph）。
- selfcheck 第 5／6 項。

## §四　備查

- `-048` 只講「預設 OFF」不指名 label 之處置正確（§8.4.1）。
- `-058` `dumpsys media.camera` 沿 pilot 用例，接受；`Rear_Camera data path is open` 之可判性依 daemon 輸出，交付前由 Pei 實機驗一次。
- DR 八條未結，f／i 交付前必結。
