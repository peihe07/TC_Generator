# 下放包 GC-12 補遺 —— §二-5 之既有答案、join 鍵更正、§二-1 驗收條件之正控（2026-09-05）

來源：執行層 A（已停手）之量測報告，Pei 轉入。三項皆收，B 依本補遺施作。

## 1　§二-5 改為「驗證既有答案」，不重算
A 已算出 −101 之成因：**PROXI 85 ＋ 規則寬窄 16**。
- 85：pm_29 之 PROXI 全數已為標準式（`PROXI Brand_Configuration_2 = Jeep`），舊式 0 行；A 舊表列入係方向未裁時刻意留空 `new_text`，B 新表排除正確。
- 16：buserror 97→86（−11）、send 112→109（−3）、recv 183→182（−1）、bare 208→207（−1）；主因 A 之 `The signal $X$` 樣式較 B 之 bus-error 專式寬。
B 之工作：以 §2 之 join 鍵複驗此二數（85／16）與四項分佈，相符即採；不符報差。README 註內容引本補遺。

## 2　join 鍵更正（§二-5 原文之 `(row, col, old_text)` 作廢）
A 實試三欄 join 回傳「只在舊表 685」＝零命中——兩表 `col` 語意不同（A 用 lint036 欄鍵 pre／input／proc／er；B 自有欄名），`row` 基準亦未確認。照字面 join 會得出「毫無交集」之假結論。
改為：先把兩表 `col` 正規化到 lint036 欄鍵（`pre／input／proc／er`）並確認 `row` 同基準（各取首末三列對母本），再 join；或退而以 `(row, old_text)` 兩欄合併。上繳寫明所用鍵與正規化對照。

## 3　§二-1 驗收條件加正控
「P 預期降至 0 或只餘 bare」之前提是：v4 判準下 bare 式 `$MSG.Sig$`（無 `= raw (label)`）**確會被 P 記為違規**。跑三本之前先以一行樣本正控：造一個只含 bare 式之最小 TC 行，跑 `lint036` 確認 P 命中 1；命中 0 即該條件為空條件（A-VS106 形態：檢查靜默失效與通過不可分辨），停手回報，不得以「P 只餘 bare」交卷。正控結果（命中數、樣本行）入上繳。

## 4　其餘不變
三本母本 sha 三方相符（cedb6b84／cb5697db／abc7f8ae），Revise-M 底本無異議，§二-1 照做。
