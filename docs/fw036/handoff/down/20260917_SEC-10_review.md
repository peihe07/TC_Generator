# SEC-10 審閱 —— 上繳包 `up/20260917_SEC-10.md`（`security_v05`）

審閱層：分析層，2026-09-17。
判定：**通過，不升級**。四項升級條件不成立；六項自報中三項待裁**全准**，兩項為分析層瑕疵登台帳。
**Security 至此進入等待態**：手上素材與內查已盡，工作簿形式可交（`U=0`／`H=0`／門檻 0），實質補值等 RD 資產與 037 作者回覆。

---

## 一、裁定（併下一 Revise 落檔，無需獨立下放包）

| # | 項 | 裁 |
|---|---|---|
| 1 | `FEATURE_EXEMPT_QUOTED` 另立（位置條件之部分豁免）| **准**。`FEATURE_EXEMPT` 語意是整項移出，硬塞會吞引號外命中——執行層讀對了 §5 第二條的意圖。命名與位置可；lint 之 `CHECK_TITLES` 不需動 |
| 2 | `AXIS_GAP` 實測 2（已裁不拆之兩條仍計）| **准**，並定分類：`audit_newtc.tsv` 加值 **`ACCEPTED_GAP`**（軸未拆、已裁不拆、有 A-SEC 號）；`NEW-CERT-02`／`NEW-ID-01` 改計此類，`AXIS_GAP` 歸 0。四類合計 47 |
| 3 | 審計指派改以 `new_test_ids` 為權威面 | **准**；SYS2 反查只留交叉驗證欄 |
| 4 | `ANOMALIES.md` 既存重複段（第 28／47 行起）| **合併**：保留首段，刪重複段；merge 為純整理，不算 Revise，不影響 sha 台帳 |
| 5 | commit 四支而非三支（SEC-06/07 併入 SEC-08）| **准**；成因具名（生成腳本疊改）且帶 pathspec 未夾帶 diagnostics |

## 二、分析層自報（登台帳）

| # | 錯誤 |
|---|---|
| A-28 | SEC-10 §4-4「`remarks` 29 格」實為 29 行／23 格——把行當格 |
| A-29 | SEC-10 §3-2 預期「`AXIS_GAP ≤1`」之算式合計 46 ≠ 47——漏了「已裁不拆者量測面仍是 gap」 |

## 三、通過項
四條裁定逐字落檔、`--gate` 32 錨點；`H` 1→0 且反例八測全過、三路徑 byte 相同；Remarks 29 行落 23 格逐 TC 相符 R-SEC22；`test_item` 2 格；無新列（ECUCert 037 grep 0）；trace 清 `NEW-NET-01` 4 列；審計重跑 C1~C6 全 Y、scope 8/8、越權 0；batch2 sha 與 v04 相同為旁證；A-SEC-8~10 落檔；四支 commit 帶 pathspec。

## 四、Security 現況（等待態）

| 項 | 值 |
|---|---|
| 交付候選 | `delivery/…_SWQT_Security_20260917.xlsx`（`2a61a957fafb05b5`），100 TC |
| 覆蓋 | 51／70 產出；19 DEFERRED（等 DR-a／i／r／s）|
| 佔位 | 96 行／11 token（`asset_request.md`），執行前須補 |
| 裁定 | R-SEC1~22 ＋ 8 amend；framework LOCKED v03 |
| lint | `U`／`H`／`K`／`P`／`X`／`SC`／`SS` = 0；`W=4`（待人裁段）|
| 未結 | DR-a／b／i／r／s／t、A-SEC-1~10、Cover 三格、`C12` |

## 五、觸發下一包之條件（任一即出 Revise）
1. RD 回任一 `X-` 資產 → 補值 Revise。
2. 037 作者回 DR-t（KI 之 USB／CAN／`0900`）→ 可能改 6 個 KI TC 前提或開 CAN 注入群。
3. 037 作者回 DR-a／i／r → DEFERRED 解凍。
4. Pei 裁 `W=4` 四段 → 消除。

本包產物四支 commit 照建議落；小整理（`ACCEPTED_GAP`、ANOMALIES 合併）併入下一 Revise 或以 `chore(security)` 一支先落，執行層擇一回報。
