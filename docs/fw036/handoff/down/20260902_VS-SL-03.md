# 下放包 VS-SL-03 —— 綁別名重跑＋沙盒寫回稿＋DR 併稿

日期：2026-09-02　層級：Tier 2 → Tier 1
前置：`down/20260902_VS-SL-02_review.md`（v2 已准）；Pei 2026-09-02 認可別名 44 名（含 9 名近義，無圈除）
上繳寫 `up/20260902_VS-SL-03.md`。

## §0　決策收束（Pei 免逐項確認之依據，落檔備查）

- **#1 別名 44 名：Pei 認可（本日）。** 執行層將 `settings_alias.tsv` 之 44 列 `tier2_proposal=manual` 升為 `match_type=manual`（evidence 保留），綁入查找。
- **#3 編號：維持 `R-VS84–88`／`A-VS166–168`。** 已落檔且互引，改號徒增台帳搬移；原 `R-VF` 之議作罷，不需裁。
- **#4 BL DT：依審閱 §2 (a)(b)(c) 執行。** (a) 依 canon §8.1（衝突時需求勝）保留 23 列，非新裁；(b) 併入 DR 稿；(c) 車型欄待 DR 回覆。
- **#5 寫回範圍：** 各項皆為既有裁定（R-VS84 NAFTA(4)、R-VS85 PROXI 形制、R-VS86 車型範圍等）之執行，不再逐項請示。**真正之把關點後移**：寫回稿只落沙盒，Pei 於 Excel 實開抽驗後才進 delivered ——與 PM 站④模式同。
- **#2 DR：** 由執行層併稿（§3），**送出仍屬 Pei**；稿成即可寄，無其他待決。

## §1　綁別名重跑（v3 報告，三本）

1. 44 名 manual 綁入後重跑 vf230：預期 `ALIAS_UNRESOLVED` 179 → 約 3（僅 9 名 DR 者殘留之列；實跑為準，差異列明）
2. 分支重算＋自檢同 v2（assert 2、3、列數 457／151／126）
3. 報告：`features/<slug>/reports/*_settings_dryrun_v3.{tsv,md}`；v2 保留不覆寫

## §2　沙盒寫回稿（Revise 稿，不動 output/、不動 delivered/）

落點：`features/<slug>/sandbox/vssl/`，自 `output/` 現本複製後施改。

vf230（`vf230_vssl.xlsx`）：
- 非 NAFTA 19 列移除（列前先存移除清單 tsv：D 欄＋原列號）
- PROXI 形制 152 處改寫 `PROXI <Param> = <raw> (<label>)`
- NEG_CONTRA 3 列 Pre 改 `= 0 (Absent)`
- 分支 (2) 96 列補 PROXI 行；分支 (3) 30 列寫 `PENDING: DR-{n}`（DR 號待 Pei 送出後回填）
- 92＋79 列補導覽路徑與控件句（Options 依正規化；樣張同 v2 §4）
- `test_item` 括號下半逐列檢（硬規）；尾句號、雙引號 lint 過

BL（`bl_vssl.xlsx`）：151 列加 node 27 行；`Body_Types` 行 PENDING；23 列 `DT_APPLICABILITY` 內容不動；車型欄**不填**（待 DR）
VC（`vc_vssl.xlsx`）：Glove Box／Mirror Dimmer 等可對應者補 PROXI；`PROSE_KEPT` 16 列不動；Camera App 2 列 PENDING

每本附 before/after 差異摘要（列數、儲存格改動數、SHA）。

## §3　DR 併稿 `features/vehicle_setting/reports/dr_draft_vssl.md`

單一文面、三節，供 Pei 直接轉寄（DR 號 Pei 送出時依台帳取）：
1. 別名查無 9 名（涉 37 列）：逐名列 TC 引用原句與兩表查無之證（R-G13 三要件）
2. VC Camera App：需求原文 vs 总控表 No.196 `Always false` 之衝突，附逐字
3. BL DT：037 r8–r12 五條 shall vs Appendix BV r41（`41ce5544e1a8c8e7`）之衝突，附兩側逐字；求上游取捨並示車型欄應填值

## §4　上繳

v3 三本報告＋三本沙盒稿＋移除清單＋DR 稿之路徑與 SHA256；自檢結果；自報錯誤。
