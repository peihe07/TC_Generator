# CDD-01_A 審閱 —— Diagnostics intake 修訂上繳包

審閱對象：`up/20260917_CDD-01_A.md`。審閱層 session：2026-09-17。
**結論：T1–T7 全部通過，升級條件五項全未觸發，CDD-02 pilot 四項前置成立。**
待 Pei 兩件裁定（§三）與 CDD-02 範圍之准（§四）後發 pilot 包。

---

## 一　逐項核對

| up 節 | 判 | 備註 |
|---|---|---|
| §1 正則放寬 | 通過 | 一行改；788 共同 id 之 `sha8`／`body_sha8` 零變動，+9 恰為 R-DIAG1～9。子條三式（`R-VS57 之 (4)`）解析不變 —— 回歸面完整 |
| §2 V3 登錄與重導 | 通過 | sha 符；差異恰 6 列、反向 0、共同 ID 零格差；11 檔 tsv 差異全落 `$2812`；MANIFEST 舊列未刪只加 note —— 合 append-only |
| §2.5 framework DRAFT | 通過 | Layer 2 20 組合計 389，`Audio Tone Settings` 28；Layer 3 42 逐字。**維持 DRAFT**，pilot 後 LOCK |
| §3 amend 落檔 | 通過 | `{live}` = 73 現查成立；16 條 sha8 全由工具實跑；併行 Security 線之 `R-SEC23` 干擾已隔離說明 |
| §4 lint 五項 | 通過 | native 於 `FEATURE_CHECKS`；`VM-DIAG` ≡ `Z` 不另立代號**正確**（同判準第二份實作即重複）；自測抓出兩缺陷並修，是自測該有的樣子；白名單 36 依定義取數正確 |
| §5 nrc_coverage | 通過 | 226 列四類、未分類 0；PENDING 205 → 3；I/O SID 114 全解 |
| §6 ANOMALIES／DR／RD 回饋 | 通過 | 16 項含 `layer` 欄；DR-DIAG-1 降 MED；`rd_feedback_20260917.md` 六項齊備，待 Pei 送 |
| §7 §4 對表 | 通過 | 零差異 |
| §10 自報錯誤 | 收 | 第 1 項之根因分析（工具讀不到就手算、手算未比對）是正確的教訓；第 3 項「列數差 ≠ 結論差」亦收入本 feature RUNBOOK 常識段 |

## 二　分析層自報錯誤（續 CDD-01 審閱 §四，登 ANOMALIES `layer = analysis`）

| # | 內容 | 處置 |
|---|---|---|
| A8 | CDD-01_A T4 書「白名單 27 種」：把「037 全文 token 種數」誤當「`layer3_assign.tsv` DID 集合」；定義與數字不一致 | 執行層已依定義取 36（`[A-DIAG14]`）；分析層記 `[A-DIAG17]` |
| A9 | CDD-01 審閱接受了執行層**手算**之 sha8 表（含 `R-DIAG9` 錯值 `802f0427`）而未要求工具實跑或標注「工具不可用」 | 本審閱以 up §3 之工具值為準；記 `[A-DIAG18]`。規則：**sha8 只認工具輸出，工具不可用即升級，不收手算** |

## 三　待 Pei 裁定（兩件，皆不阻斷 CDD-02）

**1. `[A-DIAG15]` `RULINGS.sha.tsv` 範圍** —— 建議 **(甲)** 全 repo 重生（837＋ 列）並納 git，**但不在 CDD 線做**：
它是全域檔，且 Security 線正併行寫入（SEC-11 進行中）。建議待 SEC-11 上繳後，
另發一個獨立 GC 包（`GC-16`，單 session）執行重生 ＋ `--check` 轉綠，避免兩線同時 append 該檔。
CDD 線在此之前維持逐 feature append（現狀，48 列）。

**2. `-157` 之 `unsupported service requests` 是否視同「Service Not Supported」** —— 建議 **視同**：
037 全本之 unsupported 型 93 列，標題句式即為「Handle the Unsupported service requests …」，
Description 定型句才寫「Service Not Supported」；`-157` 的 Description 是標題複製，
失效型態（不支援之 SID）與其餘 92 列同一，只是句式在標題不在描述。
准則 `R-DIAG5(amend)(d)` 增一行註「`0x11` 之 037 觸發語含標題句式 `Unsupported service requests`」，
PENDING 3 → 2（餘 `-156` negative 無型態、`-237` 只書 appropriate NRC）。

另記全域待辦（不需裁定，Pei 排期）：`new_feature.py` `feature[:2]` 缺陷；
`tests/` 既存 9 項失敗（`test_gates_tsv` 1／`test_intake_scaffold` 6／`test_single_write_path` 2），
與本包無關但阻礙「全綠」作為閘門，建議併入 GC-16。

## 四　CDD-02 pilot 範圍提案（Pei 准後發包）

目標：一次驗證五項 lint 校準 ＋ 三段式 TC 型態 ＋ 特殊列處置，**12 個 037 列 → 預估 16–20 TC**。
選列原則：每種型態至少一列、每個母節至少一列、四個特殊處置各一列。

| # | SWE1 ID | L3 | 型 | 驗證什麼 |
|---:|---|---|---|---|
| 1 | -001 | `$283F` Camera Brightness | positive（寫入）| R/W 母節寫入式：`2E 28 3F <val>` ＋ 讀回驗證；CFTS004 值域延伸（R-DIAG8(c)）|
| 2 | -002 | 同 | negative | `RESOLVED_ISO`：0x13／0x31 兩碼各一 TC（sibling 拆法 R-DIAG1(b)）|
| 3 | -003 | 同 | unsupported | `RESOLVED_ISO` 0x11 |
| 4 | -004 | `$283F` | positive（讀）| `22 28 3F` 讀取式 ＋ Harman Scope Remarks（DIAG-147 狀態依 tsv）|
| 5 | -112 | `$5000` Buzzer Control | positive I/O | `0x2F` 之 `RESOLVED_037` 位元組串 ＋ NAFTA Region 填法 |
| 6 | -113 | 同 | negative I/O | 0x2F 之 NRC |
| 7 | -151 | `$0312` AV Signal Detection | positive Routine | `31 01 03 12 …` Start ＋ Results 兩步 |
| 8 | -154 | `$0312` | negative | `RESOLVED_CFTS004`（`$31` 原文）|
| 9 | -201 附近 | `$2843` SXM Module Version | positive 讀 | 多子欄位 DID 之 ER 分項（§6.1 sublevel）|
| 10 | -340-001 | `$500D` Video Input | negative | R-DIAG2 ID 形式 ＋ `R1-DIAG` |
| 11 | -057 | `$1801` | Out of Scope | R-DIAG3(amend)：`AF` = `Out of Scope`、不產 TC 仍佔列 |
| 12 | -189 | `$2841` SXM Signal Quality | positive | R-DIAG6 VM blank Remarks 句式 |

pilot 輸出 `features/diagnostics/sandbox/pilot01/diagnostics_pilot01.xlsx`；
Design Method／Priority 依 SWC 0708 實測值域；Priority 判準提 `R-DIAG10`（P0 = 影響車輛狀態之 I/O Control 寫入與 Routine、P1 = DID 讀寫正向、P2 = NRC／unsupported）供 Pei 裁。
lint 五項於 pilot 本上校準，紅數逐項歸因後才進 batch。

## 五　未結清單

| 項 | 狀態 |
|---|---|
| DR-DIAG-1 | 非阻斷，3 列（§三-2 准後 2）|
| DR-DIAG-2 | 非阻斷 |
| FB-DIAG-a～f | `rd_feedback_20260917.md` 待 Pei 送 Nik |
| `[A-DIAG15]` | 待 §三-1 |
| git | 未動；建議 commit 沿 up §11 訊息 |
