# 下放包 SEC-02 —— 裁定落檔 ＋ 三項重跑 ＋ framework 鎖定 ＋ profile 定稿 ＋ apk 配對 ＋ Pilot 10 TC

指派執行層 session：**延用 SEC-01 session 或新開皆可**（R-G62(b)；本包不依賴前 session 之記憶，全部以 repo 檔為準）。
前置：已讀 `up/20260916_SEC-01.md`（`45dd531f2cffaee7`）與 `down/20260916_SEC-01_review.md`。
Pei 2026-09-16 裁定：**review 四、之 15 項照分析層建議全數准**；另有新增投遞一件（§1）。

---

## §0　禁區

- git 不動；`down/`／`up/` 既有檔不動；其他 feature 不動；canon 與全域 ledger 不動（`RULINGS.sha.tsv` 由 Pei 合併時重生）。
- 本包**只產 Pilot 10 TC**，不得越出 §8 之取樣。
- `scripts/lint036.py` 之修改**限於新增 `FEATURE_CHECKS["security"]` 分支**（§6），不得動既有檢查項與 `PROFILE_CHECKS`。

## §1　新增投遞：CertProvider 之 Polarion 匯出

| 檔 | sha16 | 內容 | 落點 |
|---|---|---|---|
| `Security/SWE1-CertProvider.xlsx`（26,778 B；sheet `Basic Report` 12 列、`Polarion`、`_polarion`） | `beff84c77c6810f1` | SWE1 11 列之 **Polarion work item `NRL-349384`～`NRL-349394`**；`Description`／`Verification Criteria` 與 037 xlsx **11/11 逐字相同**（分析層實測，空白正規化後） | `_intake/Security/` → `sources/raw/swe1_certprovider_polarion/`，MANIFEST `note = Polarion export of swe1_037_certprovider_v1_0; NRL map` |

```text
R-SEC9  SWE1 之 Polarion NRL 對照
  (a) `trace_matrix.tsv` 加欄 `nrl_swe1`；CertProvider 11 列填 `NRL-349384`～`349394`（逐列自 `Basic Report` A 欄），其餘 59 列填 `-`。
  (b) `specification_reference` **不用 NRL**（六本只有一本有匯出，且 037 xlsx 仍為母體）；NRL 入 Remarks 第二行 `Polarion: NRL-nnnnnn`（有者填）。
  (c) 其餘五本之 Polarion 匯出登 DR-SEC-s（低優先，不阻）。
```

## §2　裁定落檔（Pei 2026-09-16 准；R-SEC10 起，取號現查 R-G23）

```text
R-SEC10  framework 與工作簿形制
  (a) Layer 1 = 案 A：一本 workbook，六個 Test Group `Cert Provider`／`Key Install`／`SAM`／`ECU Cert`／`SWDL Secure Lib`／`Log Encrypt`。
  (b) TC ID：`NR1L-CP-nnn`／`NR1L-KI-nnn`／`NR1L-SAM-nnn`／`NR1L-ECUC-nnn`／`NR1L-SWDL-nnn`／`NR1L-LOGENC-nnn`，各組獨立自 001 升冪；
      batch 1 先占號，batch 2 接續（同組內不得交錯）。
  (c) `specification_reference` = `{037 token}_{SWE1-ID}`，一行一筆。token 定義：檔名去副檔名、去前綴 `FM-WI-FSM-037-A03-N1L-`、
      去尾綴 ` STLA 報告_SWRA STLA Report_SWRA`、空白→底線。六個 token 實值（執行層複驗後逐字寫入 profile）：
        `SWE1-CertProvider-SWE1R1-V1.0`／`SWE1-KeyInstall-SWE1R1-V1.0`／`SWE1-SAM-SWE1R1-V1.1`／
        `SWE1_ECUCert_FM-WI-FSM-037-A03`／`SWE1_SwdlSecureLib_FM-WI-FSM-037-A03`／`SWE1_libLogEncrypt_FM-WI-FSM-037-A03`
      ECUCert 之 `{SWE1-ID}` 依 R-SEC3(b) 以 SYSAD 首值代入，例 `SWE1_ECUCert_FM-WI-FSM-037-A03_SYSAD_SEC_ECUCERT_ECUCERT_API`。
  (d) `spec_mode` 新型別 **`F`**（037-SWRA 母體）：錨 = (c)；lint 之 spec_ref 檢查對 `F` 只驗「token ∈ 六值 ＋ `_` ＋ SWE1-ID ∈ 70 列（或 ECUCert 13 個 SYSAD）」。
  (e) Layer 3 = SYSAD ID（SWE1 `Source Requirement ID` 首值），不入工作簿。
  (f) Vehicle Model 七欄：70 列一律五有效車型 `1`、`598`／`5210` `0`（上繳包 7-1 grep 證實無車型條件）。
```

```text
R-SEC11  Peer Review 列之轉換（R-SEC2 之續）
  (a) `peer_review_rows.tsv` 之 `Y` 7 ＋ `Y(部分)` 3 ＋ `Y(間接)` 2 = **12 列轉為 Testing 群**，依其 `observation_means` 產 TC；
      `SWE1-SAM-0017` 依 review 三-3 亦入 Testing 群（Verification Method 空白登 DR-SEC-r）。
  (b) `N` 19 列（LOGENC 001/005/007/008/009 ＋ ECUCert Binder×2／JNI／Storage IO／Online ＋ SWDL 001/002/005 ＋ SAM 0001/0019 ＋ LOGENC 002/003/004）
      不產 TC，`RULINGS.md` 記 `DEFERRED (R-SEC11(b))`，`batch_order.tsv` 標 `batch = D`；不標 NA（R-SEC2(c)）。
  (c) 產出群合計 = 39 ＋ 1（SAM-0017）＋ 12 = **52 列**；`D` 群 18 列；合計 70。
```

```text
R-SEC5(a) amend（CS.212 直連判準補述）
  只計 THEN／`3.x`／`THEN.` 子句內之 `log`／`Logdog`／`logdog`／`avc`／`logcat`，且該子句須宣稱 log 之產生或觀察；
  主詞為 `Log Encryption`／`LogDog` 元件、或 `log` 為受詞（`log file`、`encrypt log`）者不計。大小寫敏感。
  命中 = CertProvider-005/006/008、KeyInstall-010/012、SAM-0004/0015，共 7 列。
```

```text
R-SEC12  batch 人工覆寫
  (a) `SWE1-SAM-0007` 覆寫 `ccvr_batch = 1`，`reason = manual override: apk samCertTestNormalFlow (Test_Items #04; CS.98 STEPS)`。
  (b) 覆寫只此一列；其餘依機械判準。
```

```text
R-SEC13  lint 於 Security 之範圍與代號
  (a) `C`（channel／observe）與 `source:` 標記兩項**只入 `FEATURE_CHECKS["security"]`**，不入 `PROFILE_CHECKS`。
  (b) 代號：若 `lint036.py` 之檢查項代號支援多字元，取 **`SC`**（channel）與 **`SS`**（source:）；若只支援單字元，執行層回報現行占用表，Pei 另指派，本包先以 `SC`／`SS` 落 profile。
  (c) Pilot 門檻：`P`(v4)＋`X`＋`SC`＋`SS` 全綠；`Y`／`Z` 連動照既有。
```

```text
R-SEC14  雜項
  (a) `DR-SEC-j2` 改號 `DR-SEC-r`（SAM-0017 Verification Method 補值）；新增 `DR-SEC-s`（其餘五本 Polarion 匯出）。
  (b) `step_assets.tsv` 剔除 4 列雜訊（`/cfgs`、`/odm`、`/write`、SharePoint 片段）。
  (c) 佔位 `<…>`（§8.4.1）不計 PENDING；`PENDING: DR-…` 才計。lint `SS` 對 `<…>` 不報。
  (d) Harman `Rejected` 解為責任歸屬（MD 承接），ledger 記為推論（53/53 `MD Accepted`），不據此排除任何列。
  (e) R-6 附註：SYSAD docx 之 ID 可帶 U+3000 前綴；正規化去除**所有** Unicode 空白（`\s` 含 U+3000）。
  (f) R-G44′ 實例：`R1L_Diag_SWQT` Desktop 本 vs OneDrive 本 sha 互異，以 `_C §1` 之 sha 為準。
```

## §3　三項重跑（review 三-1／三-2／三-3）

1. `build_trace_matrix.py`：套 R-SEC5(a) amend ＋ R-SEC12 ＋ R-SEC9(a)；預期 `ccvr_batch = 1` = **49**（48 ＋ SAM-0007）、`2` = 21；LOGENC-002/003/004/006 回 batch 2。
2. `batch_order.tsv`：加 `batch = D` 群（R-SEC11(b) 之 18 列）；三群計數 **49／3／18**（batch 2 之 Testing 群只剩 SAM-0003/0005/0006... 執行層以實數取代；此處 3 為分析層粗估，**差異逐列回報**）。
3. `peer_review_rows.tsv`：加 `disposition` 欄（`CONVERT`／`DEFERRED`），12／19。

## §4　framework 鎖定

`features/security/framework.md` 依 SEC-01 §5 之 30 組 Test Set **逐字**寫入（名稱不改），加 leaf／batch 欄（自 `layer2_assign.tsv` 回填），Layer 3 = SYSAD。
`feature.yaml` 回填：`name: Security`、`test_group`: 六值、`abbr`: 六值、`spec_mode: F`、`workbook: one`。
`framework.md` 末尾記 `LOCKED 2026-09-16 (R-SEC10)`。

## §5　apk 15 條 ↔ 產出群之語意配對草表（分析層擬；執行層落 `features/security/data/apk_pairing.tsv`，欄 `method | swe1_ids | basis | confidence`）

| # | 方法 | 配對 SWE1 | 依據 |
|---|---|---|---|
| 01 | `fotaMcpuCertTestNormalFlow` | CP-001（pass）、CP-008（Java client FOTA） | 037 CP-008 明列 FOTA service 為 Java client；CertProfile Code Signing 樹 |
| 02 | `fotaMcpuCertTestBrokenCert` | CP-001（fail） | broken = 鏈驗失敗 |
| 03 | `fotaVcpuCertTestNormalFlow` | CP-001（pass）、CP-008 | 同 01，VCPU 分支 |
| 04 | `samCertTestNormalFlow` | SAM-0007、CP-002／003 SAM 分支 | 037 CP-002/003 GIVEN 含 SAM Dongle |
| 05 | `fotaMcpuCertVerifyStressTest` | CP-010 | 037 CP-010 「series of certificate verification tasks」 |
| 06 | `fotaMcpuCertStressTest` | CP-010 | 同上 |
| 07 | `fotaMcpuVcpuVerifyCertStressTest` | CP-010 | 同上 |
| 08 | `fotaMcpuVcpuCertStressTest` | CP-010 | 同上 |
| 09 | `ecuCertTestNormalFlow` | ECUCert R9／R10（library／service 驗證）、CP-005（ECU 場景） | 037 CP-005 「For ECU verification scenarios」 |
| 10 | `ecuCertTestBrokenChainCert` | ECUCert R9／R10（fail）、CS.212 第 1 項 | CS.212 第 1 項逐字引該 log |
| 11 | `ecuCertTestSelfSignedChain` | ECUCert R9／R10、CS.98 第 3 項 | CS.98 STEPS 逐字 |
| 12 | `secondPartyLevelTwoCertTestNormalFlow` | CP-004（non-ECU CRL，Second Party） | 037 CP-004 列舉 Second Party APKs |
| 13~15 | `secondPartyLevelThreeCert{A,B,C}TestNormalFlow` | CP-004 | 同上；A/B/C 為 sibling 候選（L3 三種 leaf） |

`confidence`：01/02/04/09/10/11/12 = `high`（037 原文或 CCVR 逐字）；03/05~08/13~15 = `medium`（語意）。無配對之 SWE1 列不硬配；`medium` 者 Pilot 內用時 Remarks 標 `apk pairing: medium`。

## §6　profile 定稿

`docs/runtime/profiles/FW036_R1L_Security_Profile.md`：套 R-SEC9~14；`[ADD §10.7(c)]` 六個 token 逐字；`spec_mode F` 定義；lint `SC`／`SS` 之判準（`_E §3`）；常數 15 項去雜訊後定稿。
`scripts/lint036.py`：**新增** `FEATURE_CHECKS["security"] = {SC, SS}`，實作 `_E §3` 判準；對既有兩本（SWC 0708、pm_29）以 `--feature security` 以外之呼叫**不得**觸發（回歸測試：既有呼叫路徑之輸出 sha 不變）。

## §7　DR／EXEC_ASSETS 更新

`DATA_REQUESTS.md`：`j2 → r`；新增 `s`；2.1 文件 DR = a／b／i／r／s。`EXEC_ASSETS.md` 不變。

## §8　Pilot 10 TC（pilot01）

取樣（皆 batch 1）：

| # | SWE1 | sibling | 預期 Design Method | 落地來源 |
|---|---|---|---|---|
| 1 | CP-001 | 鏈驗通過（valid leaf） | Functional Based | apk #01／openssl verify；CertProfile |
| 2 | CP-001 | 鏈驗失敗（broken cert） | Negative / Invalid | apk #02 |
| 3 | CP-002 | Subject 相符 | Functional Based | 037 VC；`openssl x509 -text`；CertProfile r18 |
| 4 | CP-002 | Subject 不符 | Negative / Invalid | 同上 |
| 5 | CP-004 | 本地撤銷清單命中 → 拒絕 | Negative / Invalid | 037 VC（`CRL.r0`，R-SEC8(f)）；apk #12 |
| 6 | CP-011 | OID 相符 | Functional Based | CertProfile r36；佔位 `<…>` |
| 7 | CP-011 | OID 不符／缺 critical extension | Negative / Invalid | 同上 |
| 8 | ECUCert R18（`…EXPORTCSR_INTF`） | CSR 匯出（DID `22 29 65`） | Functional Based | R-SEC6 定式；CS.00102 `-415~418`；PDF Step 2 |
| 9 | KI-011 | `getStatus()` 回 `INSTALLED (2)` | State Transition | 037 VC；DID `22 FF 02` 入 Remarks（R-SEC8(e)） |
| 10 | KI-011 | `getStatus()` 回 `ERROR (3)` | Negative / Invalid | 同上 |

**約束**：
- 至少 **2 列**取自「通道 `N`」池（上繳包 8-1）；若上表六個 SWE1 皆非 `N`，以 batch 1 內之 `N` 列替換 #5 與 #10，並回報替換理由。
- 每 TC 依 R-SEC7：每步 `$` 指令行、ER 觀察手段、Pre-Condition 含 `ADB_ROOT_READY` 等常數；`test_item` 上半 verbatim ≤50 token、下半英文括號；Vehicle Model 依 R-SEC10(f)；`specification_reference` 依 R-SEC10(c)；Remarks 含 `Polarion: NRL-…`（CP 列）與 `apk pairing: …`。
- 產出 `features/security/sandbox/pilot01/security_pilot01.xlsx`（模板取既有交付本，R-G42）＋ 對應 json；lint `P`／`X`／`SC`／`SS` 報告；`PENDING` 計數（預期 0，允許 `<…>` 佔位）。
- 不得產 Pilot 以外之 TC。

## §9　上繳要求（`up/20260916_SEC-02.md`）

1. R-SEC9~14 ＋ R-SEC5 amend 之實際號與 body sha8；`rulings_hash.py --gate` 結果。
2. §3 三項重跑之實數（49／N／18）與逐列差異。
3. `framework.md`（LOCKED）、`feature.yaml`、profile 定稿、`apk_pairing.tsv`、`lint036.py` diff ＋ 回歸 sha。
4. Pilot 10 TC 之 xlsx／json、lint 報告、`N` 池取樣之列與理由、每 TC 之 `source:`／`channel`／`observe` 標記表。
5. DR／EXEC_ASSETS 現況；自報錯誤。

## §10　升級條件

- batch 1 實測 ≠ 49（±1 以外）；`D` 群 ≠ 18。
- `lint036.py` 回歸：既有呼叫路徑輸出 sha 改變。
- Pilot 任一 TC 出現 `PENDING`（非佔位）且無法歸到 EXEC_ASSETS 或 DR。
- token 六值與檔名推導不符。
- 任何需改 canon 之情形。
