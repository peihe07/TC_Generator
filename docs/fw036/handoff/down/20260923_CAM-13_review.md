# 下放包 CAM-13 審閱（分析層 2026-09-23）

對應上繳包：`docs/fw036/handoff/up/20260923_CAM-13.md`
判定：**A 本 25 列全數有處置（PRODUCED 21／240 TC、委派 3、BLOCKED 1）；batch04 判過（第 8 批）；B 本計畫准**。CAM-14 同發。

---

## §一　裁決

| # | 項 | 裁決 |
|---|---|---|
| 1 | §2-2 三處（`-025` 無來源、`-023`／`-021` 實際範圍） | **確認**，皆 R-CAM10 之結果；`-025` 為 R-CAM16 第三例。下放包 §2 之預期為分析層未查 plan 即寫，記一筆 |
| 2 | SYS1 檔名 token 化（`+`、`( )`、`,`、空白） | 依 canon §10.7(b)「空格→底線」**只換空白**，其他字元逐字保留：`…_(February_10th,_2023)_6.1.2`、`…RVC+PAM_…_(June_25_2021)_7.3.1`。`recon_b_rows.tsv` 與 `b_plan.tsv` 之 `spec_reference` 欄一併重生；全案逐字一致（lint 可加同檔名拼寫變體檢查，GCB-09） |
| 3 | 「生成前先掃該 SWE 之 ANOMALIES」 | **入 profile §7.6**，並加 selfcheck 第 9 項：TC 之 `Send CAN:` 訊號若在 `CameraEventHal status.xlsx` 為 `N`／`Not yet` 且 ANOMALIES 有對應之「不走 CAN」項 → 命中 |
| 4 | `-113` 更正、DR-CAM-o 縮 | 接受 |
| 5 | B 本 `est_tc` 1／leaf 基準 | 接受；各批步驟 A 逐列判 |
| 6 | 難點 B 之工作簿 `N` 欄空格 | 由 §一-2 解消 |

## §二　R-CAM17 證據複驗

隨機取證據 2（`-022` 七列無幾何／顏色）—— 分析層以 CAM-01 時所建之 SYS2 對照（V4 `-121`…`-152`）核其 Description 主題皆為類比側顯示條件，無數值型幾何要求：**屬實**。

## §三　A 本交付前置（Phase 5–7）

依 canon §8.4.3 與 R-G14，A 本工作簿含 PENDING（DR-f／i／q 等）不得出貨。前置動作：
1. **sandbox 組簿 dry-run**（不寫 `delivered/`）：以 `lint_batch.py` 之組簿路徑將 240 列寫入 `features/camera/sandbox/a_dryrun/…xlsx`，跑全域 `lint036.py --profile camera` 與 `Z`，回報 sheet 級問題（AH 欄、N 欄多行、Vehicle Model 七欄、Test Set 十組）。
2. `coverage.tsv` ↔ 工作簿列數對帳（240）。
3. 未結 DR 清單附 PENDING 列數對照表，供 Pei 送件與裁「降 NA」。
Phase 7（`--write`／tag）待 Pei 之准，不在本包。

## §四　備查

- DR 十三條；f／i／q 交付前必結。
- R-G14 第 8 批；A 本自 pilot 起累計 240 列、9 批。
