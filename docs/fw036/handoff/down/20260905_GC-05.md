# 下放包 GC-05 —— 加錨、改號收尾、DR 登記（2026-09-05）

Pei「追認」（2026-09-05）：GC-04 審閱 §六-1 三條 [DEFAULT]（R-G43(c)、R-G45(b)、vehicle_setting 綁 forms R1）追認。
分析層 judged.tsv 已落：`docs/reports/rg43_manual_20260905_judged.tsv`（130 列，sha 由執行層實測回報）。
判側分布：FO 80／LEDGER 28／KEEP 10／KEEP_ANNOTATE 12；confidence H 105／M 19／L 6。

---

## 一、執行層任務

### 1　加錨 14 條（R-G43(c)）
台帳內 `R-G30`～`R-G41`、`R-G33(d)`、台帳側 `R-G42`：於各條圍籬之**前一行**插入 `### R-Gnn — <圍籬首行之標題>`，原 `##` 敘述式標題一字不動，其下加一行註「（R-G43(c) 加錨 2026-09-05）」。
`--out` 乾跑逐號回報 14 號各得幾列（預期各 1；`R-G42` 為 2：FO 摘要＋台帳全文）。

### 2　`R-G45` 補充改 `R-G45(b)`
台帳 `:1457` 之標題與本體首行「R-G45 補充」→「R-G45(b)」，加改號註；`features/vehicle_setting/DECISIONS.md` 內之對應引用同步。乾跑證 `R-G45` 撞號歸 0。

### 3　條文與自報入台帳
R-G43(c)（GC-04 審閱 §一-3 圍籬）、A-GC13（同檔 §三）入台帳／ANOMALIES。DBC 裁決（綁 forms R1，`Power_Tailgate_Enable` 依 R-13(g) 保名開 DR）入 `features/vehicle_setting/RULINGS.md`（取現場號 R-VS{live}，R-G62）。

### 4　130 處改號（judged.tsv）
- `FO`（80）：改為 `new_id`；改前逐處重驗 `old_id` 仍在該行（表與檔已漂移者列出不改）。
- `LEDGER`（28）：其中 `new_id=R-G69` 之 2 處（row 33、34）改號；其餘 26 處**不動**。
- `KEEP`（10）：不動。
- `KEEP_ANNOTATE`（12）：不改號，於該行後加 `<!-- R-G43：兩側皆可讀／撞號紀錄，未改號；見 rg43_manual_20260905_judged.tsv row N -->`（`.md`）或 `# R-G43: not renumbered, see judged.tsv row N`（`.py`／`.yaml`）。
- confidence `M`／`L` 之 25 處：改號照做，但上繳逐處列出，供各 feature 線日後覆核。
- row 97／119／120（`canon_refs.py` doc 範例與 `test_canon_refs.py` 夾具）**成對改**；row 112 先驗 FO 內有無 `R-G22′` 錨點。
- pytest 對照向（改前／改後逐項同名，回報被改檔數 R-G50(b)）；`ruff` +0。

### 5　DR 登記
`features/vehicle_setting/DATA_REQUESTS.md` 開 DR-VS{n}：`IPC_VEHICLE_SETUP2.Power_Tailgate_Enable` 於 forms `PDT27_E2A_R1_BHCAN2.dbc` 查無、於 `R4_BHCAN.dbc` 有；請上游確認 R1L BHCAN2 是否含該訊號或其改名。定位該 token 所在 TC 列（最新工作簿），該列標 `PENDING: DR-VS{n}`（寫回時執行，本包只登記與列號）。
`inputs/` 之 `R4_BHCAN.dbc`／`R5_FDCAN8.dbc` 於 DECISIONS 註「非現行（R-G45，綁 forms R1），保留供追溯」。

### 6　#3／#6 補 grep（GC-04 審閱 §三）
- #3 comfort：變動 2 個 PU id 之彈窗**文字**（Pop Up List `Main` 分頁該列之 title／body 欄）於 comfort 最新工作簿 grep；附查詢式與命中數。
- #6 time_management：正式落檔 GC-04 上繳所報之 `DateTmFormat` ∩ {CallAction, EngineRPM, Profile_Delete, Profile_Request} = ∅，附查詢式。
兩者結果抄回各該 DECISIONS。

### 7　全量乾跑
`rulings_hash.py --out <工作區>`：回報總列數、撞號表（預期新增 0）、R-G30–41 各 1 列、R-G45 撞號 0、`R-G44`～`R-G50(c)`、`R-G51`～`R-G69` 逐號在表。**不寫 `RULINGS.sha.tsv`。**

### 8　上繳 `up/20260905_GC-05.md`
產物 sha、任務 4 之改號統計（FO 實改／未改／漂移）、乾跑表、對照向、工具寫入路徑清單（R-G50(c)）、自報。

## 二、Pei 動作（GC-05 審後）
1. `python3 scripts/rulings_hash.py`（無參數，寫表）→ `python3 scripts/rulings_hash.py --check` 應 PASS
2. `git commit -m "chore(fw036): regenerate RULINGS.sha.tsv after R-G43 renumber + ledger anchors" -- docs/fw036/RULINGS.sha.tsv`
3. 其餘 GC-05 產物之 commit（逐檔 pathspec，命令於審閱給）

## 三、未結 DR
新增 DR-VS{n}（任務 5）。
