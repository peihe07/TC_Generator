# SEC-14／SEC-15 審閱 —— `up/20260917_SEC-14.md`（零 TC chore）、`up/20260917_SEC-15.md`（六本 `security_v09`）

審閱層：分析層，2026-09-17。
判定：**兩包通過，不升級**。SEC-14 零變更以五本 xlsx sha 前後相同證實；SEC-15 六本 124 TC、逐格比對 0 格、六本內 SWE1 序非遞減 100%、lint 門檻全 0、`W=4` 集中於 CertProvider 本。
**Security 文字版交付形制定稿**：六本 ＋ 五件共用檔。

---

## 一、通過項
| 項 | 判 |
|---|---|
| SEC-14：`gen_batch2` 排除集合、指南 §1.1 抽取、A-SEC-15、`asset_request` 加句；五本 sha 前後相同；`surgical_save` 逐字節可重現 | ✅ |
| SEC-15：R-SEC25 落檔 `a480e156`；六本檔名／TC 數／sha；`C12` 六式；`D7`/`D8`/`D9` 中文半留空 | ✅ |
| `id_map_v08_v09.tsv` 124 列；Remarks 內 TC ID 引用 0 處 | ✅ |
| 逐格 0 格；單調 100%；TC 號連續 | ✅ |
| 共用五件加 `workbook` 欄、`<Component>/<ID>` 稱謂；指南兩驗證 100% | ✅ |
| 舊單本移 `superseded/` | ✅ |
| `sibling_no` 以「同 SWE1 內舊 TC 號」為等價鍵 | ✅ 准；0 格差異＋連續性已足，不要求 join |

## 二、`RULINGS.sha.tsv` 事故（執行層自報，成因具名）
SEC-13／14 之 `--gate --target features/security/RULINGS.md` **整檔重寫**，抹掉 CDD 線 19 列；SEC-15 依 R-G75 還原 HEAD → 雙 `--target` 重生 → 併回 `RULINGS_LEDGER.md` 三列 → 等式驗證 55＋3＋1＝59。處置正確、自報誠實。
**裁**：(1) 事故登台帳（執行層側，第五次覆寫；Security 線二次）；(2) **在 GC-16 落地前，Security 線不得再執行任何 `--gate`**（本審閱後 Security 已無包，自然成立）；(3) GC-16 之 R-G76 已含「掃描範圍外既有列自動保留」之語意，執行時以本事故作回歸測試案例。
另：無 `--target` 全庫掃描寫出 847 列後即還原——正確，但證明工具預設值危險；併入 GC-16 之 `--check` 預設。

## 三、小裁
- `gen_pilot01.py`／`gen_pilot02.py` 仍持舊 apk 常數：**加 deprecation 註**（`# superseded by gen_batch1.py (SEC-04); do not run — constants frozen per R-TM13`），不刪；併入下一次任何 Security Revise，不獨立出包。
- `data/*_v09.tsv` 為權威、`delivery/` 為快照：准。

## 四、Security 定稿狀態
| 項 | 值 |
|---|---|
| 交付 | `delivery/` 六本（CertProvider 23／KeyInstall 25／SAM 33／ECUCert 20／SwdlSecureLib 11／libLogEncrypt 12 = 124）＋ `coverage.tsv`／`placeholder_summary.tsv`／`placeholder_by_token.tsv`／`asset_request.md`／`EXEC_GUIDE.md` |
| 覆蓋 | 70／70 |
| 佔位 | 96 行／11 token（執行前須補）|
| 裁定 | R-SEC1~25 ＋ 10 amend；framework LOCKED v03 |
| 待 Pei | ① commit 六支（SEC-12~15）；② 六本各填 `D7`／`D8`／`D9` 中文半（18 格）；③ `W=4`（CertProvider 本四段）；④ 送 `asset_request.md`／DR-t／A-SEC-1~15 回饋 |
| 待外部 | RD 資產 11 token；037 作者 DR-a/i/r/t |
| 後續 | 資產到 → Revise；自動化骨架待 Pei go；**GC-16 先行** |

## 五、下一包
Security 線**無**。全 repo 層：GC-16（已下放）須在兩執行 session 皆停、Pei commit 後跑；Security 之六支 commit 屬其前置。
