# 下放包 GC-01 — 全域清理與來源集中（2026-09-05）

Pei 指示：「裁定」（2026-09-05）。本包為分析層對 2026-09-05 內部盤點之裁決落檔。
**本包不含任何刪除、搬移、覆寫**；一切清理動作以本包產出之對照表為前提，另包執行。
Tier 3 事項以 `[DEFAULT]` 標示，Pei 得推翻；未標者為分析層裁決。

---

## 〇、最重要之發現（盤點時新增，前次聊天未列）

**全域裁決 R-G12～R-G29 為雙軌同號異文。**實測：

| ID | `docs/fw036/RULINGS_LEDGER.md` | `docs/fw036/FEATURE_ONBOARDING.md` §9.2 |
|---|---|---|
| R-G12 | 參考素材庫之位置與 manifest（L122） | `git commit` 一律帶 pathspec |
| R-G13 | 查無之成立要件（L144） | 裁決引用制 |
| R-G24 | marker 衝突以較嚴者為準（L516） | 下放指示之路徑實在性 |
| R-G25 | 「宣稱不做 X」之測試須連跑兩次（L546） | 產出物目錄政策 |
| R-G26 | 綠燈須連同其母體一併引用（L581） | 工作區清理制 |
| R-G27 | 概念映射須以確切詞組為據（L598） | 來源集中制（`sources/`） |
| R-G28 | 裁決選單須先驗證其能解題（L624） | CFTS 嵌入物件之逐 feature 檢查 |
| R-G29 | 未驗項分流（L637） | 條文須加框（`body_kind`） |

R-G40／R-G41／R-G42 兩側同題（`交付規格表` 等），R-G43 起兩側皆空。
後果：`canon_refs`（R-G18 唯一可解析）對 R-G12～R-G29 之任何引用**皆歧義**；
`RULINGS.sha.tsv` 釘的是哪一側，本包未量測，不斷言。
**此為 Tier 3 事項，本包不給 DEFAULT**（見 §一 R-G43）。

---

## 一、裁決

### R-G43 — 全域裁決編號之單軌化（**Pei 裁，本包不設 DEFAULT**）

```
R-G43：R-G 編號以一份台帳為唯一發號源。RULINGS_LEDGER.md 與
FEATURE_ONBOARDING.md §9.2 之 R-G12～R-G29 同號異文者，由 Pei 裁定
何側保號；另一側之條文全數改號（自 R-G44 起續號，或改以新前綴），
其原號於條文首行加註「原編號 R-Gnn，2026-09-xx 改號，見 R-G43」。
改號後 rulings_hash.py 重產 RULINGS.sha.tsv；SHA_MIGRATION.tsv 逐條記舊新對照。
```

供裁之兩案：(A) 台帳保號（台帳為 sha 源，FO 側 8 條改號）；
(B) FO 保號（FO 為 runtime 權威，台帳側 8 條改號）。
**在 R-G43 裁定前，本包及其後包引用 R-G12～R-G29 一律寫 `R-Gnn(FO)`／`R-Gnn(LEDGER)`。**

### R-G44 — 來源身分唯一表 [DEFAULT]

```
R-G44：凡 forms/、features/<f>/inputs/、_intake/、sources/raw/ 內之
原檔，一律以 sha256 登記於 sources/MANIFEST.tsv（一 sha 一 doc_id）。
同檔名而 sha 不同者為「同名異體」，各得獨立 doc_id，note 欄互相指名。
feature.yaml 之 paths.* 須可解析至某 doc_id；解析不到者 lint 判紅。
R-G27(FO)「既有 feature 之舊副本不搬」維持，改為「不搬但須登記」。
```

實測異體（大小相異即足證內容相異）：
- `HMI Settings List R1 SR25 Post R1L-R (Feb 13 2026).xlsx`：forms 289.42 KB／vehicle_setting 288.71 KB／comfort 288.52 KB
- `SR24 R1 Market Configuration Table v1.6.xlsx`：forms 268.05 KB／comfort 273.22 KB

### R-G45 — 共用參考檔之現行版 [DEFAULT]

```
R-G45：DBC、LID、PROXI、HMI Settings List、Pop Up List、Market Config
之「現行版」以 forms/ 所存者為準（承 R-1 v2／R-17 之既有指向）。
feature 之 inputs/ 持他版者，於該 feature DECISIONS.md 記「版本綁定」
（R-G15(LEDGER) 之既有機制），並記其與 forms/ 版之差異是否影響已交付 TC；
未記者視為綁定 forms/ 版。
```

實測待記者：vehicle_setting（DBC `R4_BHCAN`／`R5_FDCAN8`、LID `v1_76`）；
Pop Up List 兩版（`R1 (26PI)` vs `R1 SR24 Post 2A (Dec 15, 2023)`）之何者為 R1L 現行，**Pei 裁**。

### R-G46 — `_intake/` 投遞後必清

```
R-G46：_intake/<Feature>/ 為投遞區；intake 完成並經 MANIFEST 登記後，
其原檔須清空，只留 INTAKE.md／intake.json／INTAKE.sha256。清空為 Pei
動作（未入版控，R-G26(FO) 附註不授權執行層）。清空前執行層須出
「_intake ↔ 落點」sha 對照，證每檔已落 sources/raw/ 或 inputs/。
```

實測未清：SW_Update（9 檔）、Display（4）、Popup（3，已入 sources/raw）、
Vehicle_Category（3）、SXM（CFTS_020 ICS .doc／.reqifz，**疑誤投**，Pei 認）。

### R-G47 — lint 報告歸位

```
R-G47：R-G25(FO) 生效（2026-08-24）後產生之 feature 級 lint 報告，
一律落 features/<f>/reports/。docs/fw036/lint_reports/ 凍結為
0821 全域基線（八本 .md/.json 對）＋ pm_29 三檔；其餘依 R-G26(FO)
專門 commit 移除，移除前必跑引用懸空檢查（ANOMALIES／waiver／
DECISIONS／RULINGS 指名者留）。
```

實測：`docs/fw036/lint_reports/` 110 檔；sw_update 約 60 檔日期 0828–0830；
`features/sw_update/reports/` 不存在；`features/sw_update/output/` 非 R-G25(FO) 表列位置。

### R-G48 — 模板即實測值

```
R-G48：docs/fw036/templates/feature.yaml 之 workbook.* 以 R-G1 母本
（forms/…_SWQT_20260817_ext.xlsx）第 9 列實測值為準，並於檔首註明
「本表為母本實測，接手 feature 仍須對其副本重測」。
```

實測誤值三處（vsm_v42/feature.yaml 註解已證）：
sheet 名（模板 `Test Case Specification&Result`，實為 `Test Case Specification 測試用例規範`）、
design_method（模板 Q，實為 R）、author（模板 Z，實為 AA）。

### R-G49 — handoff 鏈之補檔

```
R-G49：down/ 與 up/ 之鏈不得有孤兒審閱檔。協定生效前以聊天交換之包，
須補落同名檔，檔首標「事後補檔，原交換於聊天，補檔日 YYYY-MM-DD」，
不得表述為當時已落檔（同 R-G28(FO) 追溯拘束）。INDEX.md 補列 59–73
並宣告「自 VS-SL-02 起改走 down/up/」。
```

實測孤兒：`down/20260901_VS-SL-01_review.md`（無 `down/…VS-SL-01.md`、無 `up/…VS-SL-01.md`）。

### 記憶更正（分析層自報）

前次聊天所載 R-1 v2 `Send CAN:` 格式已於 2026-08-21 撤銷，現行為 v3
`$MESSAGE.Signal$ = <raw> (<label>)`（IN §8.7.5）。分析層記憶以 canon 為準。

---

## 二、執行層任務（全部為量測與對照，無刪除）

1. **來源身分表** `docs/reports/source_identity_20260905.tsv`：
   欄 `path / size / sha256 / filename / doc_id_or_NONE / dup_group`。
   母體：`forms/*`、`features/*/inputs/**`（含 `derived/`、`cfts057_objects/`）、`_intake/**`、`sources/raw/**`。
   `dup_group` = 同 sha 者同組；同名異 sha 者於 note 標 `NAME_COLLISION`。
   附統計：檔數、唯一 sha 數、同名異體組數、無 doc_id 之檔數（R-G8：分子分母）。
2. **R-G 雙軌對照表** `docs/reports/rg_collision_20260905.tsv`：
   R-G1～R-G42 逐號列兩側標題、所在檔與行號、是否同文；並 grep 全 repo
   （`docs/`、`features/*/`、`scripts/`）對 R-G12～R-G29 之引用處，
   逐處標其上下文可判定為哪一側。**不改任何條文**。
3. **lint_reports 引用懸空檢查**：對 `docs/fw036/lint_reports/` 每檔，grep 全 repo
   之指名處，出 `docs/reports/lint_reports_refs_20260905.tsv`（檔名／被引用處／可移除 Y-N）。
4. **feature.yaml 模板回修**（R-G48）：對母本第 9 列實測後改模板；上繳附實測輸出。
5. **INDEX.md 補列 59–73**＋切換宣告（R-G49）；VS-SL-01 補檔僅出「可尋得之來源」清單，不自造內容。
6. 執行層寫 `up/20260905_GC-01.md`：各表路徑＋sha256＋統計＋自報錯誤。

## 三、Pei 待裁（本包不執行）

| # | 事項 | 選項 |
|---|---|---|
| 1 | R-G43 保號側 | (A) 台帳／(B) FO |
| 2 | Pop Up List 現行版 | `R1 (26PI)`／`R1 SR24 Post 2A` |
| 3 | `_intake/SXM/` 之 CFTS_020 | 誤投（清）／有意（登記） |
| 4 | R-G44～R-G49 [DEFAULT] | 追認／推翻 |

## 四、未結 DR

無新增 DR。既有各 feature 之未結 DR 不變。
