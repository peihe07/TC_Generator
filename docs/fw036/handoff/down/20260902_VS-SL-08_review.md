# 審閱 —— 上繳包 VS-SL-08（req_key 修復＋量測）

日期：2026-09-02　層級：Tier 2　對象：`up/20260902_VS-SL-08.md`

判定：**准。** 修法、測試、回歸閘全數收訖；§7 之重排問題本層裁（§2），VS-SL-09 執行。

---

## §1　收訖

- `req_key()` 型別二元組修法＋10 項新測試＋既有 16 項全綠；回歸 before/after 逐字同
  （四本 SHA 相同、`diff -r` 空、總判同）；不可逆順序遵守 —— 收訖
- 回歸輸出落 `features/vehicle_setting/reports/`（repo 無頂層 `reports/`）：**追認**
- §6-3 檔名切分三版演進之自報（含空白檔名、截斷撞名）：收訖；修前基準未重跑，順序未破 —— 對
- §6-2 `canon_refs` +1 定位至 `vsm_v42` 併行線未提交檔：收訖，**與本線無涉，登記轉知該線**
  （由該線自理，本線不動其檔）

## §2　重排之裁（Tier 2 逕定：**遵 R-G42，重排＋重賦號，趁 E 欄仍空**）

R-G42 一(a) 為全域交付規格，遵規是預設、偏離才須裁 —— 故不上呈 Pei，本層逕定：

1. **vf230（42 處逆序）與 BL（7 處）皆重排**：資料列依修後 `req_key(D)` **穩定排序**
   （等鍵保持相對序，重複 D 之相鄰列不動）；**VC（0 處）不動**
2. 重排後 **F 欄重賦**：`NR1L-VS-001`–`438`／`NR1L-BLM-001`–`151` 依新列序連號 ——
   E 欄（TestRail）三本仍全空，此刻重賦仍零外部成本；**此窗口過後不再重排**
3. 對照表更新為端到端：`old_F`（SL-06 前之原值）→ 最終 `new_F`；RETIRED 19 列不動
4. 歷史報告（dry-run v1–v5、resolution tsv）之列號引用**不回改** —— 其本即對舊列序之紀錄

## §3　VS-SL-09（已下放 `down/20260902_VS-SL-09.md`）

重排＋重賦＋對照表端到端＋lint 與一(a) 複驗（0 方過）＋PENDING 計數不變 assert。

## §4　狀態板

| 項 | 狀態 |
|---|---|
| `req_key()` 修復 | **完成**（回歸 0 差） |
| 重排＋重賦 | VS-SL-09 |
| DR 寄出＋取號回填 | **待 Pei** |
| 出貨 | 待 DR 結案或裁降轉（IN §8.4.3）；VS-SL-09 後交付前置即齊 |
