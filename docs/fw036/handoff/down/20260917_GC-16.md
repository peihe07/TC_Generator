# 下放包 GC-16 —— `RULINGS.sha.tsv` 全 repo 重生 ＋ 單一維護者 ＋ 三項全域待辦

前件：`down/20260917_CDD-01_A_review.md` §三-1、`_review_A.md`、`_review_B.md` §三（R-G75）、
`down/20260917_CDD-03_review.md` §五-1、`docs/fw036/GC_BACKLOG.md`（CDD-03 新建）。
**執行前提（Pei 確認後才開工）**：Security 線與 CDD 線之執行 session 皆已停止對 repo 寫入；
本包**單一 session** 執行；完成並 commit 前，任何線不得動 `docs/fw036/RULINGS.sha.tsv`。

---

## §0　禁區

- git 一律不動；Pei 為唯一 git 操作者。**開工前 Pei 先 commit 兩線既有產物**，使 HEAD 為乾淨起點。
- 不動 `down/`、`up/`；不動任何 feature 之 TC 產物、RULINGS.md 條文本體。
- `ruling_anchor.py` 正則不動（R-G73 已定）。
- 不得為讓 `--check` 轉綠而改任何條文或既有 sha8；差異只能來自「以前沒掃到」。

## §1　背景（事實）

`RULINGS.sha.tsv` 表頭自稱「單一全域檔：重生一律掃描全部 canon 與各 feature 之 RULINGS.md」，
實際只含 security 32 列起家，CDD 線 append 16 ＋ 3；全 repo 錨點 838（CDD-01_A 實測）。
兩線併行 append 同一檔，**四次整檔覆寫**（CDD-01_A ×2、CDD-02 前 ×1、CDD-03 收尾 ×1），
第四次發生於 R-G75 正確寫入之後 —— 過渡規則只防寫前不防寫後。

## §2　裁定（分析層依常規落字，Pei 不同意者撤回）

```text
R-G76  RULINGS.sha.tsv 之單一維護者與重生制
  (a) `docs/fw036/RULINGS.sha.tsv` 為全 repo 唯一指紋表，內容 = `rulings_hash.py` 對全部 canon ＋
      各 feature RULINGS.md ＋ RULINGS_LEDGER.md 之完整輸出；不再以手工 append 維護。
  (b) 任何包新增／修訂裁決後，該包之最後一步一律 `python scripts/rulings_hash.py --regen` 全表重生，
      並以 `--check` 轉綠為上繳條件；上繳包記重生前後之錨點總數與 diff 列數。
  (c) 併行 session 期間，重生只由**先上繳者**執行；後上繳者於其上繳前再重生一次（以 HEAD 為底）。
      任何 session 不得以非重生方式寫該檔。
  (d) R-G75 於本條落地後廢止（其 (b) 集合比對併入 `--check`）。
```

## §3　任務

**T1 — 基線量測**：`--check` 現況（預期 FAIL）；HEAD 之 tsv 列數；全 repo 錨點總數（預期 ≥ 841 = 838 ＋ R-DIAG11/12/10(amend)）；
列出 HEAD tsv 中每一列之 sha8 是否與現行條文相符（預期全符，否則停）。

**T2 — 全表重生**：`rulings_hash.py --regen`（若無此旗標，實作之：輸出完整表、覆寫檔、表頭日期）；
比對重生表 ⊇ HEAD tsv（既有 57 列 sha8 零變動）；`--check` 轉綠。回報：總列數、新增列數、任一既有列變動即升級。

**T3 — 表頭與工具**：tsv 表頭改為 R-G76 所述之維護制；`rulings_hash.py` 加 `--regen`／`--check` 之說明；
`RULINGS_LEDGER.md` 落 R-G76、R-G75 加廢止註（R-TM13 不刪）。

**T4 — GC_BACKLOG 三項**：
(a) `lint036.py` 加全域檢查 `S-ORD`：`specification_reference` 同文件前綴內升冪，違者 FAIL；對 SWC 0708／Home 0809／security_v09／diagnostics_v01 實跑回報紅數（既有交付本之紅只回報，不回修，R-G72）。
(b) `new_feature.py` 加 `--abbr` 參數；未給時仍 `feature[:2]`，給時用之；scaffold 樣板一致。
(c) `tests/` 既存 9 項失敗逐項歸因（`test_gates_tsv` 1／`test_intake_scaffold` 6／`test_single_write_path` 2）：能修者修，不能者標 xfail 並記理由；目標全套綠。

**T5 — 回歸**：`pytest tests/` 全綠（或 xfail 有理由）；`rulings_hash.py --check` 綠；`canon_refs.py` 對任三個 `R-XX@sha8` 引用解析成功。

## §4　預期數字

| 項 | 預期 |
|---|---|
| 重生前 tsv 列 | 57（HEAD）|
| 重生後 tsv 列 | ≥ 841 |
| 既有 57 列 sha8 變動 | 0 |
| `--check` | 綠 |
| `S-ORD` 對 diagnostics_v01 | 0 |
| pytest | 全綠或 xfail 有理由 |

## §5　上繳要求（`up/20260917_GC-16.md`）

T1 基線表；T2 重生 diff 摘要（新增列按前綴分佈）；T3 diff；T4 三項各自產物與紅數；T5 結果；自報錯誤；建議 commit 拆法（tool／ledger／lint／tests 四筆）。

## §6　升級條件

- 既有任一列 sha8 變動。
- 重生後錨點總數 < 838（掃描面比 CDD-01_A 少）。
- `S-ORD` 對 `diagnostics_v01` 非 0（表示 CDD-03 §3.1 之根除不成立）。
- 任何需改 canon 之情形。
