# 下放包 CDD-01 —— Diagnostics feature intake（scaffold ＋ 來源登錄 ＋ recon ＋ framework 草案）

指派執行層 session：**新開一個 Claude Code session**（R-G62(b)）。
分析層 session：本包由 2026-09-17 之 CDD intake 檢查 session 落檔；
檢查全文（395 列量測、CFTS004 交叉比對、REF 盤點）已於同 session 向 Pei 口頭報告並裁定，裁定內容全數收入 §2。

---

## §0　禁區

- **git 一律不動**；Pei 為唯一 git 操作者。
- 不動 `down/`、`up/` 既有檔；不動 `features/` 下其他 feature 目錄。
- **不得生成任何 TC**；本包止於 Phase 0–1（intake ＋ recon）＋ framework 草案之驗算，pilot 另包（CDD-02）。
- 不得補產 037 未收之 CFTS004 條目（Pei 已裁：只產有 SWE1 之 168 源 395 列，缺口不補、不另立工作簿）。
- 來源原檔位於 `10_Reviewing/00_TestCase/ASW-R3/CDD/`（含 `REF/`），不在 repo；登錄依 R-G66 落 `sources/raw/`。
- `SWE1-Diagnostics-057`（Out of Scope）**不產 TC**，但列必須保留於工作簿（§2 R-DIAG{live+2}）。

## §1　背景

新 feature。客戶側資料夾 `ASW-R3/CDD`，**一本 037**：

| | 值 |
|---|---|
| 檔 | `SWE1_Diagnostics_V1 (2).xlsx` |
| sha16 | `b985e09ac42cece7` |
| 版本 | C（2026-02-09；作者 NikKMLiu、審查 StanleyKCWu、核准 AllenACLiu） |
| 列 | 395（`SWE1-Diagnostics-001`～`394`；`-340` 重複兩列） |
| 層 | 全 395 列 Sub Categorization = `Service`；Categorization = `Functional Requirement`；Status = `New` |
| 來源 | 168 個 `SYS-RA-DIAG-xxx`，**168/168 命中 `SYS2_CFTS_004_General_Diagnostic_Requirements`**（repo 已登錄 `sys2_cfts004_general_diag`，sha16 `ffcb202fff92007e`，與 REF 本全等） |
| 內容 | UDS DID 讀寫／I-O Control／Routine；**0 列涉及 DTC** |

037 之列型態為**三段式**：同一來源多拆為（1）正向行為、（2）negative response（長度／格式／不支援 DID → NRC）、（3）Service Not Supported（不支援 SID → NRC，內部狀態不變）。每來源 3 列 × 91、2 列 × 45、1 列 × 32。第 (2)(3) 型之 Description 為全本共用之定型句，唯一差異是所掛之 DID。

REF/ 其餘 17 檔**無任何 SWE1 列指向**（AME Diagnostics SYSRA、CFTS013 Radio Error Management 兩本、CS/SD 系 SYSRA 八本、CFTS024 docx、SYSAD docx 三本）；只供步驟落地之背景（UDS 通道、SYSAD 介面表），不是拆解對象。

## §2　裁定（Pei 2026-09-17）

執行層取號：`RULINGS.md` 由本包 scaffold 產生，`{live}` 期望值 **1**；落檔前現查（R-G23）並於上繳包回報。

```text
R-DIAG{live}  TC ↔ SWE1 為 N:1；三段式各列各自成 TC 群
  (a) 每條 TC 之 Requirement or Design ID 只填一個 SWE1 ID；一條 TC 不得同時掛多個 SWE1 ID。
  (b) 一個 SWE1 ID 依 IN §8.3 sibling 軸可產多條 TC（例：negative response 列拆
      「無效長度」／「超範圍」／「不支援 DID」三條，全部掛同一 SWE1 ID）。
  (c) 三段式之 negative response 列與 Service Not Supported 列各自成 TC 群，
      不併回同 DID 之正向列；近似 TC 以 test_item 括號下半之 DID／SID／NRC 值作區分 token。
```

```text
R-DIAG{live+1}  重複 SWE1 ID 之處置
  `SWE1-Diagnostics-340` 兩列（row 347 SYS-RA-DIAG-264 Audio、row 348 SYS-RA-DIAG-188 Clock Defrost）
  以 `SWE1-Diagnostics-340-001`（row 347）／`SWE1-Diagnostics-340-002`（row 348）區分，
  工作簿 Requirement ID 與 specification_reference 皆用此二號；不發 DR、不改 037。
```

```text
R-DIAG{live+2}  Out of Scope 列不產 TC、保留原內容
  `SWE1-Diagnostics-057`（SYS-RA-DIAG-248，CFTS004 索引 Category = Out of Scope）
  不產 TC；工作簿保留一列，test_item 上半照抄 037 Description，
  Test Set 欄外之處置欄（execution 依 SWC 0708 之對應欄位實測後定）寫 `Out of Scope`，
  Remarks 註 `CFTS004 Category: Out of Scope`。coverage.tsv 以 `OUT_OF_SCOPE` 計，不計入未覆蓋。
```

```text
R-DIAG{live+3}  拼字與空白
  (a) 037 之拼字（Caemera／senario／recevied／diagnopstic／bightness）與 CFTS004 原文
      在 test_item 上半 verbatim 照抄，不修；作者側四欄用正確拼字。
  (b) 037 之來源 ID／標題／描述之首尾空白（SYS-RA-DIAG-315 三列尾端 3 空格等）
      於 MANIFEST／coverage 比對前 strip；工作簿不得帶入。
```

```text
R-DIAG{live+4}  DID 與 UDS 記法（IN §8.7.5 之本 feature [ADD]）
  (a) 敘述層（test_item 上半、pre_conditions、expected_result 散文）：`DID $283F`，
      與 CFTS004 原文及 037 標題一致；037 混寫之 `0x283F` 在上半 verbatim 照抄，作者側一律 `$`。
      `$XXXX` 無尾 `$`，不受 lint P（`$MESSAGE.Signal$`）規制。
  (b) 執行層（test_procedure）：寫 UDS 位元組串，與 security 批次之 UDS 通道同式：
        2. Send UDS request 22 28 3F via diagnostic tool
        3. Check that positive response 62 28 3F <brightness byte> is received
      NRC 寫 `7F <SID> <NRC> (<label>)`，label 逐字取 CFTS004 用字
      （例 `7F 22 31 (Request Out of Range)`）。
  (c) SID／NRC／sub-function 值以 CFTS004 原文為準；原文未載者寫 `PENDING: DR-{n} <缺件>`，
      不得引 ISO 14229 通用表補值（IN §8.4.1）。
```

```text
R-DIAG{live+5}  037 Verification Method 空白之處置
  `SWE1-Diagnostics-189/190/191/192`（SYS-RA-DIAG-124/125）VM 空白；
  依同組 `-187/-188`（SYS-RA-DIAG-123，同型）之步驟型態產出，步驟自 Description ＋ CFTS004 原文推出，
  Remarks 註 `037 VM blank; procedure derived from Description + CFTS004`；不發 DR。
```

```text
R-DIAG{live+6}  Harman Status 與 Region
  (a) CFTS004 索引 `SYS2 HARMAN Status` 非空者（Need Rework 55／Need Clarification 1／Accepted 2），
      該來源之全部 TC Remarks 註 `Harman Scope: <status 原字>`；不影響產出與否。
  (b) Vehicle Model 七欄依 CFTS004 索引 `SYS2 限定地區 Region` 填 1／0（R-CAM 前例：598／5210 一律 0）；
      Region = All → 五個有效車型全 1；Region = NAFTA 系 → 依 forms/proxi 六平台之市場歸屬填，
      歸屬表由 recon 任務 3-7 產出後 Pei 裁。
```

```text
R-DIAG{live+7}  範圍
  (a) 只產 037 所收之 168 源 395 列；CFTS004 之 50 條未收 FR 不補產、不另立工作簿。
  (b) AME Diagnostics SYSRA 與 DTC（CS.00099）不另立工作簿。
  (c) 每列產 TC 前**必須回看 CFTS004 該來源之原文與同 `$XXXX` 標題下之 Information 列**，
      原文載有 037 Description 未展開之值域／子欄位／狀態者，依 IN §8.3 拆 sibling（延伸只向 CFTS004 原文，不向未收條目）。
```

```text
R-DIAG{live+8}  Test Group、TC ID、Layer 2/3
  (a) Test Group = `Diagnostics`；TC ID = `NR1L-DIAG-{nnn}`。
  (b) Layer 2 走 DID 家族聚合（一個或數個相鄰 DID = 一 Test Set），候選見 §5；名稱歸 Pei。
  (c) Layer 3 = CFTS004 之 `$XXXX - <title>` 標題（43 個，含母節 Read/Write DIDs／I-O Control DIDs／Routine IDs），
      逐字，不入工作簿。
```

## §3　裁決引用

| 引用 | 用途 |
|---|---|
| `R-G42`（ledger 本） | TC ID `NR1L-{ABBR}-{nnn}`、Test Group、D 欄升冪 |
| `R-G66` | 來源集中制 `sources/raw/<doc_id>/` |
| `R-G67` | intake 嵌入物件檢查（CFTS024 docx、SYSAD docx 三本） |
| `R-G70`、`R-G71` | profile 啟用 lint `P`(v4) 與 `X` |
| `R-CAM{…}(Vehicle Model 1/0)`、`R-CAM{…}(車型軸拆分)` | Vehicle Model 欄填法與拆分判準之前例（camera/RULINGS.md） |
| `R-SEC15`、`R-SEC16` | UDS 位元組步驟型態、Priority 判準（security/RULINGS.md）之前例 |
| IN §8.2.2 | R-DIAG{live}(a)(b) 之 canon 依據 |
| IN §10.7(a) | `CFTS004-{ObjectID}` 錨 |
| R-G23 | 取號現查 |

## §4　執行層任務

**任務 0 — 投遞區確認（最小投遞集，R-G44′ sha 同一性 ＋ R-G45／R-G66 不存副本）**
分析層 2026-09-17 以 sha256 比對 `sources/MANIFEST.tsv`（224 行）；**已在 repo 者不投遞，只登 MANIFEST `features` 欄加 `diagnostics`**。

(a) **不投遞 —— repo 已登錄同體**（執行層以 sha256 複驗）：

| 檔 | 既有 doc_id | sha16 |
|---|---|---|
| `SYS2_CFTS_004_General_Diagnostic_Requirements_All_Accepted_Done State.xlsx` | `sys2_cfts004_general_diag` | `ffcb202fff92007e` |
| `SYS2_CFTS013_Radio_Error_Management-Associated_Display.xlsx` | `sys2_cfts013_radio_error_management_associated_display` | 執行層複驗 |
| `SYS2_CS00102_SWAD…`／`SYS2_SD.00049…`／`SYS2_CS.00099…`／`SYS2_CS.00100…`／`SYS2_CS.00101…`／`SYS2_CS.000148…` | `sys2_cs00102_swad`／`sys2_sd00049_sysra`／`sys2_cs00099_sysra`／`sys2_cs00100_sysra`／`sys2_cs00101_sysra`／`sys2_cs000148_sysra` | 執行層複驗 |

(b) **必投遞 `_intake/Diagnostics/`**（repo 內無同 sha）：

| 檔 | sha16 | 備註 |
|---|---|---|
| `SWE1_Diagnostics_V1 (2).xlsx` | `b985e09ac42cece7` | 037 主件；doc_id 建議 `diagnostics_037_swra_v1` |
| `SYS2_AME_Diagnostics_SYSRA_STLA_Technical_Safety_Requirement_Analysis_Released_.xlsx` | `b0e0f51cfcb3ecad` | 背景，`note` 記「037 未引用；AME 命令集不在本 feature 範圍」 |
| `SYS2_CFTS_013_Radio Error Management_DCSD_0126_1_V1_Released.xlsx` | 執行層取 | 背景 |
| `CS00042_SYSRA…`／`SYS2_CS.00010_03_SYSRA…`／`SYS2_CS00025_SYSRA…` | 執行層取 | 背景；先查 MANIFEST 有無同 sha |
| `SYS2_CFTS024_Tuner_…SYSRA_CFTS024_Tuner_V01.xlsx` | 執行層取 | 背景 |
| `R1LR_Atl-H_26PI2.5 … CFTS 024_Specific HU Radio Functions_20260608-1206.docx`（2.2 MB） | 執行層取 | 背景；R-G67 檢查 |
| `SYS3_CFTS_004_General Diagnostics_System Architectural Design_SYSAD.docx`（813 KB） | 執行層取 | **步驟落地主要背景**（診斷服務架構、UDS 序列）；R-G67 檢查 |
| `SYS3_AMFM_…SYSAD_v0.2_20260629.docx`（1.3 MB）／`SYS3_SXM_…SYSAD_v0.2_20260629.docx`（1.2 MB） | 執行層取 | 背景；R-G67 檢查 |

投遞前每檔先以 sha256 查 MANIFEST；已存者改歸 (a)。

**任務 1 — scaffold**
```
python scripts/new_feature.py Diagnostics --root .
```
ABBR 已裁 `DIAG`（R-DIAG{live+8}）；`features/diagnostics/` 不存在（分析層 2026-09-17 list 實測）。

**任務 2 — 來源登錄（R-G66）**
全部投遞檔登入 `sources/raw/<doc_id>/` ＋ `MANIFEST.tsv`（`features` 欄 = `diagnostics`；(a) 表既有列加 `,diagnostics`，append-only，不用 `--refresh-manifest`）。
docx 四件依 R-G67 檢查嵌入物件並回報。

**任務 3 — recon（Phase 1）**
每項附查詢條件，對照 §6 預期數字：
1. 037：資料列、唯一 ID 數、重複 ID 列號、Priority 分佈、Release Version 分佈（含 `V1.00.01` 之列號 —— 分析層 Counter 得 1 列但未定位）。
2. 037 `Source Requirement ID`（strip 後）對 CFTS004 `Basic Report` B 欄之解析率；每來源之 037 列數分佈；ObjectID（E 欄）全數有值之確認。
3. CFTS004 側被引用來源之 Category／Region／HARMAN Status／SW-HW-System 分佈；Out of Scope 者逐列。
4. 037 之 VM 空白列、拼字異常列、首尾空白列逐列（預期見 §6）。
5. DID 記法盤點：037 標題＋描述中 `$XXXX` 與 `0xXXXX` 之 token 種數與次數；兩式並存之同列列出。
6. **Layer 3 對映**：每個被引用來源歸屬之 CFTS004 母節（Read/Write DIDs／I-O Control DIDs／Routine IDs）＋ `$XXXX` 標題，逐列輸出 `features/diagnostics/data/layer3_assign.tsv`（欄：SWE1 ID、SYS-RA-DIAG、ObjectID、母節、$XXXX 標題、037 列型態 positive/negative/unsupported/other）。
7. Region → Vehicle Model 歸屬表：CFTS004 Region 值之全部變體（分析層見 23 種排列，皆為 `All` 或 NAFTA 四子項之排列）正規化為 `All`／`NAFTA`；提 NAFTA 對 forms/proxi 六平台之歸屬候選，列 DECISIONS [PROPOSED]，Pei 裁。
8. 037 列型態分類：以標題關鍵字（`negative response`／`Negative senario`／`Unsupported service`／`service not supported`）分 positive／negative／unsupported／other，回報四類數；other 者逐列（預期含 14 列無領域前綴之列）。
9. SYSAD `SYS3_CFTS_004…SYSAD.docx`：抽出診斷服務（MDTDiagService／DiagService）之 UDS 請求／回應序列表與 NRC 表，存 `features/diagnostics/data/sysad_uds_tables.md`；**只抄不改**，作為 R-DIAG{live+4}(b) 位元組步驟之依據。CFTS004 原文與 SYSAD 對 NRC 值不一致者逐筆列出（CFTS004 優先，IN §8.6）。

**任務 4 — framework 草案之驗算（§5）**
以任務 3-6 之實測回填 §5 Layer 2 表之 leaf 欄；Layer 2 歸屬逐列輸出 `features/diagnostics/data/layer2_assign.tsv`；與 §5 草案不一致者逐筆記明依據，**不自行改 Layer 2 名稱**（名稱歸 Pei）。§4.1.3 決策測試：任一 Test Set 篩出 < 3 TC 或 > 80 TC 者標記，附合併／拆分建議。

**任務 5 — profile 骨架**
`docs/runtime/profiles/FW036_R1L_Diagnostics_Profile.md`：啟用 lint `P`(v4)、`X`；
[ADD] R-DIAG{live+4}：`$XXXX` 敘述式與 UDS 位元組式之 lint（位元組串每 byte 兩位 hex、空格分隔；`7F` 後兩 byte 必接 `(<label>)`）；
[ADD] R-DIAG{live}(a)：Requirement ID 欄只得一個 SWE1 ID（含 `-340-001/-002` 形式）；
[ADD] R-CAM Vehicle Model 1/0 檢查（沿用 camera profile 條文）；
[ADD] Remarks `Harman Scope:` 與 `Out of Scope` 之格式檢查。

## §5　framework 草案（Layer 1–3；未鎖定，Pei 裁）

依 canon §4.1.2 步驟 5，只列 Layer 1–3，不列個別 RD；leaf 欄由任務 4 回填。

### Layer 1

`Diagnostics`（已裁）。

### Layer 2（DID 家族聚合；分析層依 CFTS004 43 個 `$XXXX` 標題與 037 列數草擬）

| # | Test Set 候選 | 涵蓋 `$XXXX` | 母節 | 037 列（分析層實測） |
|---:|---|---|---|---:|
| 1 | `SXM Module Version` | $2843 | R/W | 57 |
| 2 | `SXM Signal Quality` | $2840, $2841, $2842 | R/W | 72 |
| 3 | `SXM Subscription` | $2844, $2845, $2846, $2847, $2848 | R/W | 37 |
| 4 | `SXM Package Routines` | $0307, $0309 | Routine | 13 |
| 5 | `GPS Signal Data` | $2812 | R/W | 40 |
| 6 | `Camera Brightness` | $283F | R/W | 6 |
| 7 | `ECU Internal Settings` | $280C | R/W | 8 |
| 8 | `Media Hub Config` | $2870 | R/W | 6 |
| 9 | `Button Interfaces` | $1820, $1821 | R/W | 7 |
| 10 | `Screen Test Pattern` | $1801 | R/W | 4 |
| 11 | `Audio Output Settings` | $180C | R/W | 1 |
| 12 | `Buzzer Control` | $5000 | R/W ＋ I/O | 40 |
| 13 | `Speaker Quadrant Selection` | $5001 | R/W ＋ I/O | 21 |
| 14 | `Audio Tone Settings` | $5002, $5003, $5004, $5005, $5006 | I/O | 27 |
| 15 | `Tuner Control` | $5008, $5009, $500C | R/W ＋ I/O | 22 |
| 16 | `Mode Selection` | $500A | R/W ＋ I/O | 9 |
| 17 | `In Motion Menu Control` | $500B | I/O | 6 |
| 18 | `Video Input` | $500D, $5100 | I/O | 5 |
| 19 | `AV Signal Detection` | $0312 | Routine | 10 |
| 20 | `Audio Measure AC` | $031B | Routine | 3 |
| 21 | `Clear Key Sense PIN` | $030A | Routine | 1 |

合計 395（含 -057 Out of Scope 列，歸 #11 `Audio Output Settings`；該 Test Set 因此為單列，屬 §4.2 genuine outlier，或併入 #14，Pei 擇）。
#12 `$5000` 同時出現於 R/W 與 I/O 兩母節，依 DID 合一（Layer 3 仍各自保留母節）。
單列 Test Set（#11、#21）與 4 列以下者（#10、#18、#20）合併與否列 DECISIONS。

### Layer 3

**不入工作簿**（§4.1.5）。= CFTS004 `<母節> | $XXXX - <title>`，逐字；任務 3-6 產出全表。
同一 Layer 3 內之 037 列依 positive → negative → unsupported 順序連續書寫（§4.1.4 之 TC sequencing）。

## §6　預期數字（分析層 2026-09-17 實測，openpyxl read_only, data_only）

| 項 | 預期 | 查詢條件 |
|---|---|---|
| 037 資料列 | 395 | `Analysis Report` 列 8 起，A／C／D 欄任一非空 |
| 唯一 SWE1 ID | 394 | A 欄 Counter；重複 = `-340` ×2（row 347、348） |
| 來源 ID 唯一數／解析率 | 168／168 (100%) | B 欄 strip 後對 CFTS004 `Basic Report` B 欄 |
| 每來源列數分佈 | 3 列 ×91、2 列 ×45、1 列 ×32 | 同上 |
| Priority | Medium 350／Low 39／High 6 | AG 欄 |
| Release Version | `V1.00.00` 394／`V1.00.01` 1 | F 欄 |
| 標題領域前綴 | SiriusXM 168／Audio 105／GPS 40／AM-FM 31／Steering Wheel 5／Camera+Caemera 6／USB 4／KeySense PIN 3／Display 3／Clock Defrost 2／12-24 Time 2／Front Seat Video 2／無前綴 14 | C 欄 `^(.+?)\s*[-–:]` |
| VM 空白列 | 4（SWE1-189～192） | AI 欄 None |
| 拼字異常列 | Caemera 4／senario 3／recevied 1／diagnopstic 1 | C+D 欄 substring |
| 首尾空白 | 來源 ID 3（-315）／標題 12／描述 21 | `!= .strip()` |
| DID token | `$XXXX` 27 種 / `0xXXXX` 23 種 | C+D 欄 regex |
| DTC 提及列 | 0 | C+D 欄 `\bDTC\b` |
| CFTS004 索引列 | 404（FR 217／Information 95／Heading 78／Out of Scope 14） | `Basic Report` B 欄非空；J 欄 |
| 被引用來源之 Category | FR 167／Out of Scope 1（DIAG-248 → SWE1-057） | J 欄 |
| 被引用來源之 HARMAN Status | 空 110／Need Rework 55／Accepted 2／Need Clarification 1 | O 欄 |
| 被引用來源之 Region | All 89／NAFTA 系 78／空 1 | K 欄正規化 |
| 被引用來源之 ObjectID 空值 | 0 | E 欄 |
| CFTS004 未被引用之 FR | 50（另 Out of Scope 13） | J 欄 ≠ Heading/Information 且 B 欄 ∉ 037 來源集 —— **只回報，不產** |
| Layer 3 標題數 | 43（R/W 23／I-O 15／Routine 5） | Heading 列且 Description 以 `$` 起首，逐列掃描歸屬 |
| REF 檔數 | 17（xlsx 13、docx 4） | `ls REF/` |
| MANIFEST 既有同體 | ≥ 8（CFTS004、CFTS013 Assoc Display、CS00102、SD00049、CS00099、CS00100、CS00101、CS000148） | sha256 比對 |

## §7　DR 清單（本包登記）

無阻斷性 DR。以下為 **RD 回饋草稿**（不阻斷產出，Pei 決定是否送 Nik）：

| 項 | 內容 |
|---|---|
| FB-DIAG-a | `SWE1-Diagnostics-340` 重號（工作簿以 -340-001/-002 區分） |
| FB-DIAG-b | `SWE1-Diagnostics-189～192` VM 空白 |
| FB-DIAG-c | `SWE1-Diagnostics-057` 引用 CFTS004 Out of Scope 條目 |
| FB-DIAG-d | CFTS004 50 條 FR 未收入 037（清單由任務 3 產 `features/diagnostics/data/cfts004_uncited_fr.tsv`） |

## §8　上繳要求（`up/20260917_CDD-01.md`）

1. 投遞檔 sha256 全表；任務 0(a) 之 MANIFEST `features` 欄追加；docx 四件 R-G67 結果。
2. scaffold 產物；`RULINGS.md` 九條 R-DIAG 實際號與 body sha8。
3. §6 全表預期 vs 實測；差異逐筆附查詢。
4. 任務 3 九項量測；3-2、3-3、3-4、3-6、3-8 附逐列 tsv。
5. 任務 4：§5 Layer 2 表回填 ＋ `layer2_assign.tsv`；§4.1.3 標記項與建議。
6. `sysad_uds_tables.md` 與 CFTS004↔SYSAD NRC 不一致清單。
7. `DECISIONS.md` [PROPOSED]：Region→Vehicle Model 歸屬、單列 Test Set 合併、Out of Scope 列之處置欄位名（依 SWC 0708 實測）。
8. 未結 DR（預期 0）、RD 回饋草稿四項之現況、自報錯誤。

## §9　升級條件

- 037 sha 與 `b985e09ac42cece7` 不符；CFTS004 REF 本 sha 與 `ffcb202fff92007e` 不符。
- `new_feature.py` 因 `features/diagnostics/` 已存在而拒絕。
- 037 來源解析率 < 100%。
- §6 任一列數項差 > 2 筆。
- SYSAD 之 UDS 序列與 CFTS004 對同一 DID 之 SID 不一致（非 NRC 值之細節差，而是服務本身不同）。
- 任何需改 canon 或全域 ledger 之情形。
