# 下放包 CAM-02 —— Camera Phase 2：裁定落檔 ＋ framework 落檔 ＋ lint Z ＋ crossref 核對

指派執行層 session：延用 CAM-01 之 session（同 feature 連續作業）。
對應審閱：`down/20260916_CAM-01_review.md`；Pei 裁定 2026-09-16 十項全數到齊。

---

## §0　禁區

同 CAM-01 §0。另：**不得生成 TC**；`framework.md` 落檔後狀態為「草案，待 Pei 簽核」，未簽核前 Phase 4+ 仍 blocked。
`_intake/Camera/` 之清空為 Pei 動作（R-G46），執行層不動。

## §1　裁定（Pei 2026-09-16，逐項對應審閱 §三）

取號現查（R-G23）：`features/camera/RULINGS.md` 現有 R-CAM1～3，`{live}` 期望值 **4**。

```text
R-CAM{live}  Test Group 與 TC ID（取代審閱 §三-1 之「共用 CAM」）
  (a) A、B 兩本之工作簿 Test Group 欄一律為 `Rear View Camera`。
  (b) TC ID 分兩組序號：A 本 `NR1L-RVC-{nnn}`、B 本 `NR1L-RVCHMI-{nnn}`，各自自 001 起連號。
      R-G42 第二項之 ABBR 於本 feature 即為 `RVC`（A）／`RVCHMI`（B）；審閱 §三-1「共用 CAM」作廢。
  (c) 交叉參考索引以 SWE ID 互指，不以 TC ID 互指（兩組序號獨立）。
```

```text
R-CAM{live+1}  品牌軸：label 隨品牌而異者拆列，品牌寫入 Pre-Conditions
  (a) HMI Settings List 標 `*` 之設定項，`*` 不入 hop；hop label 依 `Brand-Specific Names` 分頁取值。
  (b) 同一驗證點若 hop label 因品牌而異 → 拆 sibling（§8.3 mode 軸之品牌分支），
      Pre-Conditions 首行寫明品牌（例：`Vehicle brand is Ram`），Vehicle Model 七欄依該品牌之車型勾 1。
  (c) 品牌 ↔ 車型之對照由執行層自 `forms/SR24 R1 Market Configuration Table v1.6.xlsx` 提候選，
      列 DECISIONS [PROPOSED]，不得自 anchor 前綴或車名推定。
  (d) 本條與 R-CAM3 併讀：車型軸與品牌軸皆為拆分判準，二者同時成立時以車型軸為外層。
```

```text
R-CAM{live+2}  B 本追溯母體 = spec-index/cache 之 RVC+PAM 本
  `SYS1_HMI_RVC+PAM_R1_Low_SR24_1A_(June_25_2021).xlsx` 以 `spec-index/cache/` 本
  （sha16 `1a0bef53c6de975c`，64 列）為 B 本 spec_reference 之母體；REF 本（`5a1c0ab24991dcb1`）
  於 MANIFEST note 記「同名異體，cache 本之真子集，不作追溯母體」。DR-CAM-c 結案。
```

```text
R-CAM{live+3}  V4 Layer 3 以 Description 前導章節號向下繼承
  SYS2 VF551_V4 之 Layer 3 章節取 `D` 欄前導號（heading 列）向下繼承（321/321、60 章），
  不回查 docx。DR-CAM-b 結案。
```

```text
R-CAM{live+4}  ENTER_CAMERA_SETTINGS 常數
  `ENTER_CAMERA_SETTINGS`（3 hops，§5.3）：
    `Press "Apps" on Menu Bar to open App Drawer` → `Select "Settings" in the App Drawer` → `Select "Camera"`
  第 3 hop 之來源：`forms/HMI Settings List R1 SR25 Post R1L-R (Feb 13 2026).xlsx`，`Settings` 分頁 row 464
  `13. Camera`（序號非 label；canon §5.8(c) 明列 HMI Settings List 為路徑來源）。
  ER：`The "Camera" settings screen is displayed`。DR-CAM-d 結案；FO §4 [ADD] 之 §5.3 承接完成。
```

```text
R-CAM{live+5}  Out of Scope 來源之處置
  SYS2 Category 為 `Out of Scope`（含 `Out of scope`）之 32 個來源一律不作 spec_reference 錨；
  其中 `SYS-RA-VF551_V2-578`／`-580`（Radio 端「RVC display active」條件）得作
  `-010`／`-017`／`-023` 之 Pre-Condition 措辭來源，reasoning 註明出處。上游分類不重判。
```

裁定 6（lint 代號 `Z`）、9（B 本 Layer 2 修訂案）、10（`-023` 交集承接順位於 framework 鎖定時定）為 DECISIONS 項，不立 R 號。

## §2　執行層任務

**任務 1 — 裁定落檔**
- `RULINGS.md` 追加 R-CAM4～8（逐字，body sha8 回報）。
- `DECISIONS.md` §6-1～6-7 改 `[DECIDED]`；6-1 之內容以 R-CAM4(b) 覆寫（`RVC`／`RVCHMI`）；新增 6-8 品牌↔車型對照 `[PROPOSED]`（R-CAM5(c)）。
- `DATA_REQUESTS.md`：DR-CAM-b／c／d 結案；DR-CAM-a 維持 MISSING（高）。
- `ANOMALIES.md`：A-CA10 改 RESOLVED（VF551_V2 原文拼法）；新登 A-CA15（`ShiftLeverPosition_PT` 僅 hybrid 分支）、A-CA16（`DynamicGrid*` 改 HMI 觀察面）、A-CA17（`LanguageSelection` 以 HMI 語言設定為觸發）。
- `feature.yaml`：`books.a.abbr = RVC`、`books.b.abbr = RVCHMI`、Test Group 兩本 `Rear View Camera`。若 scaffold 之 schema 不支援單 feature 雙 ABBR，**停下回報**，不自行擴 schema。

**任務 2 — framework.md 落檔**
- Layer 1：`Rear View Camera`（兩本同值）。
- A 本 Layer 2：CAM-01 §5 九組不動（上繳 §4.1 已 25/25）。
- B 本 Layer 2：依審閱 §四八組重跑 `layer2_assign.tsv`，預期 10／50／5／13／62／31／6／53 ＝ 230、未歸屬 0；不符逐筆列。
- Layer 3：A 本 = VF 章節號（V4 依 R-CAM7）＋ CFTS092 §1.3.x 章名；B 本 = SYS1 Outline Number（RVC+PAM 依 R-CAM6 取 cache 本）。
- 附表：SWE-CAM ↔ SWE1-RVC 交叉參考索引（任務 4 之核對結果）。
- 標題註「草案，待 Pei 簽核（R-CAM4～8 已落）」。

**任務 3 — lint Z 實作**
`lint036.py` 新增檢查 `Z`：Vehicle Model 七欄（T–Z）每列須為 `1`／`0`；`Commander (598)`、`Regengade (5210)` 兩欄恆 `0`；其餘五欄至少一個 `1`。以 `--profile` 真值啟用；對既有八本乾跑回報是否誤報（預期既有本全空 → 全 FAIL，故 `Z` **僅在 Camera profile 啟用**，不入全域預設）。附 pytest（依 Pei 偏好）。

**任務 4 — crossref 逐列核對**
`crossref_a_b.tsv` 68 筆逐列讀兩側 Description 語意核對，`status` 改 `[VERIFIED]`／`[REJECTED]`（附一句理由）；另掃 B 本 230 列中未被任何 A 本列配對者之數量（供 Phase 3 判「B 本獨有」）。

**任務 5 — 品牌對照 recon**
自 `forms/SR24 R1 Market Configuration Table v1.6.xlsx` 抽 HDCC27／DT27／637／2261／376 之品牌欄，列 DECISIONS 6-8 候選；`Brand-Specific Names` 分頁 Camera 兩列之各品牌 label 逐字抄入 `data/brand_labels.tsv`。

**任務 6 — 兩處更正**
`-002` Ignition 前提：Atl-Hi `BCM_FD_10.CmdIgnSts`、Atl-Mi `STATUS_BH_BCM2.CmdIgnSts`，記入 profile §5 車型軸。

## §3　預期數字

| 項 | 預期 |
|---|---|
| B 本 Layer 2 八組 | 10／50／5／13／62／31／6／53 ＝ 230 |
| crossref | 68 筆全部有 status；`[REJECTED]` 數回報 |
| lint Z 乾跑 | Camera 無工作簿 → 以 forms 模板空本測 FAIL 路徑、以合成 3 列測 PASS 路徑 |
| RULINGS | R-CAM4～8 五條，`{live}`=4 |

## §4　上繳要求（`up/20260916_CAM-02.md`）

1. R-CAM4～8 實際號與 sha8；DECISIONS 六項 `[DECIDED]` 與 6-8 候選。
2. `framework.md` 路徑與 B 本八組實測；不符逐筆。
3. lint Z 實作位置、pytest 結果、既有八本乾跑之誤報說明。
4. crossref `[VERIFIED]`／`[REJECTED]` 計數；B 本未配對列數。
5. `brand_labels.tsv` 內容。
6. 自報錯誤；未結清單（DR-CAM-a、`-023` 承接順位）。

## §5　升級條件

- `feature.yaml` schema 不支援雙 ABBR。
- B 本八組合計 ≠ 230。
- Market Configuration Table 無五車型之品牌欄。
- 任何需改 canon 或全域 ledger 之情形（含 `Z` 若須入 `CHECK_TITLES` 全域表以外之結構）。
