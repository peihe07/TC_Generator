# 下放包 VS-CF-04 —— CFTS044 交付（Pei 裁「出貨」2026-09-08）

**指派：執行層。** 承 `up/20260908_VS-CF-03.md` 與 `down/20260908_VS-CF-03_review.md`（總判通過）。

**甲-1 之處理**：Pei 覆「好出貨吧」，分析層讀為**採甲案（9 組 19 列並存，不改）**。
理由：三案中只有甲不動 TC 內容，與「出貨」之指令一致。
⚠ 若 Pei 之本意為丙（補 `$Heated_Seat_Levels$` 區分），**本包停手回報** —— 該修改須先於交付。

來源：`sandbox/cfts044/cfts044_20260819_Revise2.xlsx`
sha256 `949fa81e4cd5a5a4b193357de23410ae79b5322f584682189b647d98f71c9db4`（分析層複驗）。
**內容不再改動**；本包只做交付所需之封面／履歷欄位與檔案搬移。

---

## 一、交付前必補之三處欄位（Revise2 實測為空）

`surgical_save` 落檔，不得 `wb.save()`。

### 1.1 `Cover 封面`

| 儲存格 | 現況 | 填入 |
|---|---|---|
| `D6`（版本 Version） | `C` | **`C`**（不動 —— C 為模板版本，非本文件版本；見 §一.3 之 `Product Document`） |
| `H6` 或 `D6` 右鄰（修訂日期 Revision Date，`G6` 之值欄） | 空 | `2026-09-08` |
| `D9`（作者 Author） | 空 | **`PeiPYHsu`**（Pei 指定 2026-09-08） |

⚠ `G6` 之值欄位址請執行層以 `openpyxl` 讀合併儲存格範圍確認後再寫，**不得臆測**；
確認結果寫入上繳。

### 1.2 `ChangeHistory 修訂履歷` —— **不填**（Pei 裁 2026-09-08）

該分頁 `A5`–`A7` 記錄之 A／B／C 為**模板**之修訂（新增欄位、改欄名），非內容之產出，
本次交付不新增列。**執行層不得寫入此分頁**；`sheet4`（其對應之 xml）之 diff 須為 0。

### 1.3 `Product Document 記錄封面頁`

| 儲存格 | 現況 | 填入 |
|---|---|---|
| `B8`（日期 Date） | 空 | `2026-09-08` |
| `C13`（作者 Author，V1.0 列） | 空 | **`PeiPYHsu`** |
| `A14`–`D14` | 空 | 不新增 —— 本次為 V1.0 之首次交付，非改版 |

`B5` 維持 `V1.0`。

---

## 二、檔案搬移與命名

### 2.1 交付檔名

```
FM-WI-FSM-036-A01 STLA 測試用例規範與結果_SWQT STLA Test Case Specification & Result_SWQT_CFTS044_Vehicle Controls_20260908.xlsx
```

與母本同名式，僅日期由 `20260819` 改 `20260908`（母本命名逐字比對後套用，
執行層先 `ls` 母本目錄確認命名式再產生檔名）。

### 2.2 落點（兩處，內容相同）

```
features/vehicle_setting/output/<交付檔名>       ← 工作區留存
features/vehicle_setting/delivered/<交付檔名>    ← 交付本
```

⚠ `output/` 在本 feature `.gitignore` 第 18 行內（VS-CF-01 §3 已具名）。
`delivered/` 是否受版控，執行層先 `git check-ignore -v` 確認，結果寫入上繳。
**若 `delivered/` 亦被 ignore，停手回報** —— 交付本不受版控是重大問題，須 Pei 決定。

### 2.3 MANIFEST

`output/MANIFEST.tsv` 追加一列（欄序：filename／sha256／source_path／round／status／note）：

```
<交付檔名>  <sha256>  features/vehicle_setting/sandbox/cfts044/cfts044_20260819_Revise2.xlsx  VS-CF-04  delivered  CFTS044 v1；241 列／237 Req；PENDING 0；封面與履歷補齊後之 sha
```

`delivered/MANIFEST.tsv` 追加一列（欄序：filename／sha256／source_path／delivered_round／note）：

```
<交付檔名>  <sha256>  features/vehicle_setting/sandbox/cfts044/cfts044_20260819_Revise2.xlsx  VS-CF-04  CFTS044 Vehicle Controls v1；甲-1 採甲案（9 組 19 列並存）；DR-5-B 39 列為 dr_dependent
```

**兩處之 sha256 皆為補完封面欄位後之新 sha**，非 `949fa81e…`。
Revise2 本身不動，保留於 sandbox 作為對照。

### 2.4 TestRail 對照表

`reports/cfts044_tcid_remap.tsv`（VS-CF-03 已產出）一併複製至
`delivered/cfts044_tcid_remap_20260908.tsv`，並於 `delivered/MANIFEST.tsv` 追加其列。
該表為刪 2 重複列後之舊→新 tc_id 對照，TestRail 匯入時需要。

---

## 三、交付前最後驗證（落檔後跑一次，不合即停手）

1. 補完欄位後之 zip：member 數 42；相異之 member 僅 **Cover 封面與 Product Document 記錄封面頁**
   對應之 sheet xml **兩件**（`sheet6`／ChangeHistory 皆須為 0 diff）。
   ⚠ sheet xml 之編號與分頁名之對應由執行層以 `workbook.xml` ＋ `workbook.xml.rels` 實測，**不得臆測**。
   dv 計數不變。
2. `Test Case Specification 測試用例規範` 分頁之 **241 資料列內容零改動** ——
   與 Revise2 逐格比對，diff 須為 **0**。
3. VS-CF-03 §〇 之七項數字重跑一次，全數不變。
4. `output/` 與 `delivered/` 兩檔 sha256 相同。

---

## 四、Pei 已裁（2026-09-08）

1. **作者** = `PeiPYHsu`（Cover `D9`、Product Document `C13` 兩處）。
2. **ChangeHistory 不填**（§1.2）。
3. **甲-1** 依「出貨」讀為甲案（19 列並存，不改內容）。若本意為丙，停手回報。

## 五、上繳 `up/20260908_VS-CF-04.md` 須含

1. 補完前後之 sha256；§三 四項驗證之現算。
2. `G6` 合併儲存格範圍之實測、`delivered/` 之 `git check-ignore` 結果。
3. 兩份 MANIFEST 之落檔行號。
4. 工具旗標；自報。

## 六、不在本包

commit、tag、TestRail 匯入、DR 送出 —— 皆 Pei（Tier 3）之操作，執行層不碰。
`down/20260908_VS-CF-03_review.md` 之乙-1（尾端 2 列空白）／乙-2（row 250 spec 空）／
乙-3（row 29／30 區分軸）已於審閱具名，Pei 未指示變更，**維持現況交付**。
