# 下放包 VS-SL-07 —— 賦號與前綴正名（依 Pei 2026-09-02「裁」採 Tier 2 建議）

日期：2026-09-02　層級：Tier 2 → Tier 1
前置：`down/20260902_VS-SL-06_review.md` §3；Pei 一字裁「裁」= 採本層兩建議
上繳寫 `up/20260902_VS-SL-07.md`。

## §0　裁定（登錄於 features/vehicle_setting/RULINGS.md，號現場取；性質：Pei 授裁採 Tier 2 建議）

```text
R-VS{live}   delivery.tc_id_abbr = VS（feature 級）
  vehicle_setting 之 TC ID 前綴為 NR1L-VS-；VF230 與 VF665 兩本共用同一序列，
  VF665 自 VF230 之最大號 +1 起編。序依 R-G42（D 欄升冪）。
R-VS{live+1} newR1L- 前綴正名為 NR1L-
  BL／VC 之 F 欄 newR1L-{ABBR}-{nnn} 一律改 NR1L-{ABBR}-{nnn}，數字部不動。
  時點依據：兩本 E 欄（TestRail）全空，改號無外部引用成本。
  對照表 status = renamed，old_F → new_F 逐列留痕。
```

## §1　feature.yaml 增 `delivery:` 區塊（三個 feature）

```yaml
# vehicle_setting
delivery:
  tc_id_abbr: VS
# bed_lowering
delivery:
  tc_id_abbr: BLM
# vehicle_category
delivery:
  tc_id_abbr: VC
```
（BLM／VC 取自其 F 欄既有實況，僅補宣告；yaml 既有內容不動，區塊附加於檔尾。）

## §2　賦號與正名（對 `output/` 三本轉正本施作，先備份 `.presl07.bak`，不覆寫既有備份）

1. vf230：438 列 F 欄依 D 欄升冪賦 `NR1L-VS-001` 起連號；`BLOCKED` 全數轉 `assigned`
2. BL 151 列／VC 126 列：`newR1L-` → `NR1L-`，數字不動，`kept` 轉 `renamed`
3. 三份 `testrail_id_map_<slug>.tsv` 重出（vf230 對照表仍 457 列含 19 RETIRED，RETIRED 無 new_F）
4. MANIFEST 更新 SHA；lint 四項重跑（0 方過）

## §3　自檢與上繳

- assert：vf230 賦號數 = 438、無重號、D 欄升冪 ⇔ 號升冪；BL／VC 改前綴數 = 151／126
- assert：對照表 status 分布 = assigned 438＋RETIRED 19／renamed 151／renamed 126
- 上繳：三本新 SHA、三份對照表 SHA、yaml diff、R-VS 兩條落檔號、自檢、自報
- **仍不出貨**（PENDING gate 不變，IN §8.4.3）；DR 寄出取號後另令回填
