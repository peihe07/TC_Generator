# 下放包 VS-SL-08 —— 修共用比較器 `req_key()`（帶回歸閘）

日期：2026-09-02　層級：Tier 2 → Tier 1
前置：`up/20260902_VS-SL-07.md` §5-2；`down/20260902_VS-SL-07_review.md` §3
上繳寫 `up/20260902_VS-SL-08.md`。本包**只動 `scripts/lint_delivery_spec.py` 之 `req_key()` 與其測試**，不動任何工作簿。

## §1　修法

`req_key()` 產出之鍵各段改為型別可比之二元組：數字段 `(0, int(seg))`、字串段 `(1, seg)`。
排序語義：同位段數字先於字串、數字間按值、字串間按字典序；**純同型資料之相對序不得改變**。
新增單元測試 `tests/test_req_key.py`，至少涵蓋：
- `SWE1-VC-6AuxSwitches-002` vs `SWE1-VC-AutoDoorLocks-015`（原 TypeError 案）
- 純數字段序、純字串段序、尾號 002 vs 015、相同鍵（重複 D）

## §2　回歸閘（先跑後改不可逆順序）

1. 修**前**：對 `delivered/` 現存各本跑 `lint_delivery_spec.py`，輸出存
   `reports/_sl08_regress_before/<book>.txt`
2. 修**後**：同組重跑，存 `_sl08_regress_after/`；`diff -r` **必須為空**（assert）
3. 任一本 diff 非空 → **停下來報**，不得改判、不得調整既有基線

## §3　量測（只量不改）

以修後比較器對 `features/vehicle_setting/output/` 之 vf230 轉正本乾跑 R-G42 一(a) 列序檢查：
報 D 欄逆序對數、首末 5 對樣本、若重排所需之移動列數估計。**不重排。**

## §4　自檢與上繳

- 單元測試全綠；回歸 diff = 0；`gate_all.py` 增紅 = 0
- 上繳：修改 diff、測試檔、回歸前後輸出之 SHA、§3 量測數、自報
