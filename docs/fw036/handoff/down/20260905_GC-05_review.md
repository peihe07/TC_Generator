# 審閱 GC-05 上繳包（2026-09-05，Tier 2）

對象：`up/20260905_GC-05.md`　下放包：`down/20260905_GC-05.md`
結果：**收受**。八件全收；7-5 三項裁於 §一；分析層自報一件於 §二。乾跑缺號 0，**可重生指紋表**。

---

## 一、7-5 三項

**1　`A-BLM8` 之 10 處** —— 執行層擋下**正確**，重判：row 7–16 全部改為 `KEEP_ANNOTATE`，理由同 row 17–19。
judged.tsv 該 10 列之 `judged_side` 由執行層以刪除線改為 `KEEP_ANNOTATE`（R-TM13），reason 補「GC-05 審閱重判：該行即撞號紀錄本身」。

**2　`R-G69` 補加錨** —— 覆核成立。下放包 §一-1 之 14 條係抄 GC-04 上繳 3-5 節之九標題而未把 GC-03 改號後之 `R-G69`（同一形態標題）算入；§一-7 之「逐號在表」才是判準。執行層依較嚴者行事，合 R-G24(LEDGER) 精神。

**3　`rulings_hash.py:65` 之 `R-G61′`→`R-G61`** —— 收受。GC-03 機器改號帶出之副作用，judged.tsv 之母體（`rg43_manual`）本就未列該處；執行層一致化正確。

## 二、分析層自報 A-GC14

row 7–16 之判側：分析層讀到之 `context` 欄截斷於 `A-BLM7` 之前半，未見該行後段之 R-G 字樣所在，即援引 bed_lowering 之「取 FO 讀法」宣告判 `FO`。實則該行為 `A-BLM8` 撞號證據本身，與 row 17–19 同質。**判準（feature 宣告）套在未讀之字面上**，形態＝以規則代量測。
另：下放包所載 confidence 分布 H 105／L 6 與實測 H 104／L 7 差一，係分析層產表腳本之統計與寫檔不同源（人寫之數），同 R-G20(LEDGER)。二者併記 A-GC14。

## 三、GC-03 抽取缺陷（7-2）之防再犯

R-G36 補充 [DEFAULT]：機器抽取入台帳後，執行層須逐條回讀所抽本體之**首行與末行**並於上繳列出（首行＝標題即內容者為已知盲區）。GC-06 入台帳。

## 四、收受清單

加錨 15、`R-G45(b)`、R-G43(c)／A-GC13 入台帳、`R-VS101`（DBC 綁 R1）、DR 登記未取號＋受影響二列（r214／r215，`NR1L-VS-205/206`）、130 處處置（改 65／註 22／不動 36／已改 7，定位不到 0，context 重定位法收受）、#3／#6 結案、`binding_hits2`。
對照向 44 檔，pytest／ruff +0。`canon_refs` +19 全為未生指紋之號。

## 五、Pei 動作（本輪，順序固定）

```bash
cd /Users/peihe/Work_Projects/TC_Generator
git status --porcelain | grep -v '^??' | wc -l              # GC-05 之 43 檔＋審閱檔

# 1 先 commit GC-05 產物（指紋表重生前，條文本體須已是最終狀態）
git add -- docs/reports/binding_hits2_20260905.tsv docs/reports/rg43_manual_20260905_judged.tsv
git commit -m "docs(fw036): GC-05 ledger anchors (R-G43c), R-G45b, R-G43 renumber closeout, DR for Power_Tailgate_Enable" \
  -- docs/fw036 docs/reports features scripts tests
git status --porcelain | grep -v '^??' | wc -l              # 0

# 2 重生指紋表（本線首次無參數執行）
python3 scripts/rulings_hash.py
python3 scripts/rulings_hash.py --check                     # 須 PASS
git diff --numstat -- docs/fw036/RULINGS.sha.tsv           # 預期約 +763 / -728 量級
grep -c "R-G3[0-9]\|R-G4[01]" docs/fw036/RULINGS.sha.tsv   # ≥ 12
git commit -m "chore(fw036): regenerate RULINGS.sha.tsv after R-G43 renumber + ledger anchors (763 rows)" \
  -- docs/fw036/RULINGS.sha.tsv

# 3 閘
python3 scripts/gate_all.py 2>&1 | tail -20                 # rulings_hash 應轉綠；canon_refs 之 ruling unresolved 應大降
```

步驟 1 之 pathspec 含 `features`：若併行 vsm 線有未 commit 之改動會被帶入，先 `git status` 看 `features/vsm_v4*` 有無 modified；有則改逐檔。

## 六、後續（GC-06，小包）
R-G36 補充入台帳；A-GC14 抄錄；judged.tsv row 7–16 改註；DR 送出後取號並寫 `PENDING` 至 r214／r215（寫回動作，另裁）。
`_intake/` 五匣清空若未做，仍為 Pei 動作。
