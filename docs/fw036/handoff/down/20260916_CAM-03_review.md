# 下放包 CAM-03 審閱 —— pilot01 逐列（分析層 2026-09-16）

對應上繳包：`docs/fw036/handoff/up/20260916_CAM-03.md`
判定：**§1 六項落檔通過；pilot 9 TC 不出貨**，lint 全綠但人讀發現 6 類寫法瑕疵（§二），修正後為 pilot v2；另 4 項判準待 Pei 裁（§三）。全量生成待 pilot v2 通過。

---

## §一　執行層報告之確認

1. **TRANSM2 不存在於四本 DBC** → 改 `STATUS_CCAN4.ReverseGearSts`（V33 逐字用法）：**接受**；DR-CAM-f（高）維持，637 之複核條件已記。
2. **§6.5.1 無 hop label** → 改走 `ENTER_APP_DRAWER` ＋ `"Rear View Camera"`（Menu Bar §4.1 NRL-127734）：**接受**；DR-CAM-g 維持。CAM-03 §3 #5「Controls 頁」為下放包指錯來源。
3. **Pop Up List 查無兩句 camera 文字**：接受；A-CA20 維持 PENDING、DR-CAM-h 開。
4. **R-CAM10 自檢改錨**（-006、-009）：接受；「R-CAM10 無 lint 覆蓋」→ 列入 §三-4。
5. **R-CAM5 body sha 因刪除線區塊而變、fenced sha 不變**：接受，R-TM13 之意即「原句逐字不動」，以 fenced sha 為證即可；ledger 記兩個 sha。
6. **framework 在 `docs/fw036/framework.md` Part VIII 而非 `features/camera/framework.md`**：下放包路徑寫錯，執行層依行號改對檔。**惟其他 feature 之 framework 皆在 `features/<f>/framework.md`**（vehicle_setting 等 15 本）—— Camera 落在全域檔 Part VIII 是 CAM-02 執行時之選擇，本審閱不改，列 §三-5 由 Pei 定位置。
7. **#9 拆四列**：接受其分析（V2/V4 `>=`、V3 `>=` 異名、V33 `>` 相反、V42 只有 reset 語意）；全量時 -018 之速度邊界依 VF 分寫，637 待 DR-CAM-f／V42 來源補。

## §二　pilot 逐列瑕疵（lint 不抓，人讀抓；依 canon 條號）

| # | TC | 瑕疵 | canon | 修正 |
|---|---|---|---|---|
| 1 | 003、005 | Pre-Condition 已寫「HU in RUN」，Procedure 步 1 又 `Send CAN: CmdIgnSts = 4 (RUN)`——同一狀態既為前提又為步驟 | §4.4「step-controlled state 不入 Pre-Condition」；§4.5 資料單一歸屬 | 刪步 1（前提保留），ER 重編號 |
| 2 | 006 | Pre-Condition 4「image already shown in Manual Display Mode」而步 1–2 又開啟；reasoning 末段仍寫「本列之錨仍為 CFTS092 SYS-RA-CAM-082」與首段「不取 CAM-082」自相矛盾 | §4.4；§10.4 reasoning 須與欄位一致 | 刪 Pre-Condition 4（開啟為 setup 步驟）；reasoning 刪該句 |
| 3 | 004 | verbatim `V3-266` 為「RVC image active … continue to display … switch to Automatic」＝手動→自動之轉換，與本列情境（無影像 → R → 顯示）不符 | §8.1 Test Item ↔ Req | 改取 V3 §1.10.2.2 自動模式進入之原句（執行層自 SYS2 V3 抄，回報 ID）；若 V3 無等價句則取 V42 並回報 |
| 4 | 007、008 | 步 5、6 為 setup／transition 步驟卻帶 `and check that …`；步 5「Shift the vehicle into reverse」未用 §8.7.5(c) CAN 式，且 007 同列跨 Atl-Hi／Atl-Mi（637）而只能寫一個訊號名 | §5.2 A／§5.5 只有 Final Step 帶驗證意圖；§8.7.5(c) | 步 5、6 改純動作，ER 承載觀察；CAN 式依 §三-1 之裁定寫法 |
| 5 | 008 | Pre-Condition 3「The vehicle brand is Fiat (2261, 376)」—— 376 依 PROXI 為 Abarth（R-CAM5(c)′），且該分支之本質是「非 RAM 系回落基礎 label」 | R-CAM5(c)′ | 改「The vehicle brand is not Ram (2261 Fiat, 376 Abarth)」或依 §三-2 之措辭裁定 |
| 6 | 009 | off-point（205）與 on-point（206）綁在同一 TC，步 4、5 各帶 ER 判定，Final Step 6 只重述步 5 | §8.3 boundary 每點一 TC；§8.3 stress-test（兩個獨立部分失敗同落 fail） | 拆二：`-009a` 205 仍顯示（off）、`-009b` 206 移除（on）；pilot v2 以 -009 承 on-point、off-point 排入全量 |
| 7 | 005、006 | design_method = Scenario／Use Case，而步驟 ≤ 4、單一 feature | §12 tie-break：Scenario = ≥3 步跨 feature；單 feature 1–2 步 = Functional | 改 `功能測試 (Functional based)`；006 為狀態轉換亦可，擇一並具名 |
| 8 | 001、002、005 | Pre-Condition「On Atl-Mi vehicles the ignition signal is … instead of …」為註解句，非狀態 | §4.4 | 措辭依 §三-1 裁定後統一 |
| 9 | 002 | `CmdIgnSts = 1 (IGN_LK)` 對應 V33 之 `Ignition_Pre_Off` —— 對應關係未引來源 | §8.4.1 | reasoning 引 V33 I/O 表或 LID 之對應列；查無則該值 `PENDING: DR-CAM-{n}` |
| 10 | 001 | verbatim 取 V33（Toro）而 Procedure 全走 Atl-Hi 訊號，車型勾全五 | 一致性（非 canon 違規） | 全五列之 verbatim 優先取 V2（Atl-Hi 主線）或 CFTS092；V33 留 reasoning 互參 |

其餘：ER 措辭「is accepted」（001／002 ER1）改為可觀察狀態；`Reverse_Deb` 保留符號值可（§8.4.1）。

## §三　待 Pei 裁

| # | 項 | 建議 |
|---|---|---|
| 1 | **R-CAM3 修訂**（執行層提案）：訊息名因 EE 而異但 PROXI 值與 ER 皆同者**不拆**，只在 Pre-Condition 分寫 | **准**，並定固定句式，令 lint 可辨：Pre-Condition 專用一行 `CAN source: <Atl-Hi signal> (HDCC27, DT27) / <Atl-Mi signal> (637, 2261, 376)`；Procedure 一律以 Atl-Hi 訊號寫 `Send CAN:` 式，Atl-Mi 依該行代換。R-CAM3 依 R-TM13 加 (e) 段 |
| 2 | 品牌分支之 Pre-Condition 措辭 | `The vehicle brand is Ram (HDCC27, DT27, 637)`／`The vehicle brand is not Ram (2261 Fiat, 376 Abarth)` |
| 3 | **VF 家族之 `specification_reference` 格式**：pilot 用 `SYS-RA-VF551_V2-498`（SYS2 ID），§10.7 未定 VF 型 | 執行層回報 VF230 交付本之實際寫法，**沿用其前例**；若 VF230 用 VF anchor（`VF551_V2_PHDCC27_VF_484` 型）則 Camera 同 |
| 4 | R-CAM10 之 lint 覆蓋 | 比照 Z 立 feature 代號 `Y2`？——**不立**：R-CAM10 為承接判斷，以 `crossref`／`layer2_assign` 為資料、寫一支 `selfcheck_r_cam10.py` 於 `features/camera/scripts/` 於每批上繳前跑，結果入上繳包，不入 lint 序列 |
| 5 | framework 落點：`docs/fw036/framework.md` Part VIII vs `features/camera/framework.md` | 建議**搬回 `features/camera/framework.md`**（15 本前例），Part VIII 留一行指標；Pei 定 |
| 6 | 邊界值 raw 205／206（A-CA22） | **准**其換算；全量時每個 VF 分支各自換算並具名 DBC SG_ 因子 |

## §四　CAM-04（pilot v2）之任務（待 §三裁後由分析層發包）

1. §二十項逐列修正，重跑 lint（含 Z）＋ `selfcheck_r_cam10.py`。
2. -004 verbatim 改取；-009 拆 on／off，pilot v2 共 10 TC（off-point 列 `-010`）。
3. R-CAM3(e) 落檔；spec_reference 格式依 §三-3 對齊，9 列改寫。
4. 上繳附「修正前後 diff」與逐列 canon 條號。

## §五　不處置（備查）

- DR-CAM-f／g／h 三條待 Pei 送；-024（DR-CAM-a）與 -025（DR-CAM-h）全量時仍 blocked。
- I-cross=9 之基線噪音，依 CHECK_STATUS 非判準，不處置。
