# SEC-02 審閱 —— 上繳包 `up/20260916_SEC-02.md`（22,759 B）＋ `security_pilot01.xlsx`（`cc83c035182dbd95`，分析層複驗相符）

審閱層：分析層，2026-09-16。
判定：**基礎建設通過**（裁定落檔、重跑、framework LOCKED、profile、apk 配對、lint 回歸皆合格）；
**Pilot 10 TC 不通過**，須出 pilot02。lint 全綠但內容有四類實質違規，這正是 Pilot 要抓的。
本審閱不改 repo 檔；修正於 SEC-03 下放（待 Pei 裁 §四）。

---

## 一、基礎建設 —— 通過

| 項 | 判 |
|---|---|
| R-SEC9~14 ＋ R-SEC5 amend 落檔，`--gate` 通過，R-SEC1~8 sha 不變 | ✅ |
| 三項重跑：命中 5／batch 1 = 49／D = 19／產出 51 | ✅ 全符 `_A` |
| framework LOCKED、`feature.yaml`、六 token 逐檔吻合 | ✅ |
| `apk_pairing.tsv` 15 列，high 7／medium 8 | ✅ |
| `lint036.py`：只加 `FEATURE_CHECKS["security"]`；三路徑回歸 byte 相同；27 測試過 | ✅ |
| `C` 已佔用 → 取 `SC`／`SS` | ✅ 准（R-SEC13(b) 之首選成立）|
| `SS` 判準面改 Remarks | ✅ 准；JSON reasoning 之 `source:` 仍為生成側必填，兩面並存 |
| `check_order()` 求值一次之小重構 | ✅ 准（回歸為證）|
| `SAM-0004`／`SAM-0007` 替換 #5／#10 | ✅ 准；代價已具名，CP-004 與 KI-011 ERROR 分支入 batch 1 首批 |
| PENDING 3 筆歸 `X-h` | ✅ token 改法見 §四-3 |
| `W` 2 筆（負向路徑無數值）| ✅ 判可 |
| `I-cross` 10 筆「窗未宣告」 | ✅ **Security profile 豁免 `I-cross`**（ER 為指令輸出斷言，無觀測窗概念；R-SU33/34 為 sw_update 慣例）|

**git 損毀**：Pei 側 `sudo xcodebuild -license`，與本審閱無關；產物在工作樹，SEC-03 前不需 commit。

## 二、Pilot 10 TC —— 不通過，四類實質違規

lint `P`／`X`／`SC`／`SS` 全綠**不等於**內容合規；下列皆為 lint 檢查面外之違規，逐 TC 標示。

### 二-1　造值（IN §8.4.1）—— FAIL

| TC | 位置 | 造值內容 | 正確處置 |
|---|---|---|---|
| `NR1L-KI-001` | Proc 2 | `com.mitsubishielectric.ahu.efw.keyinstall.test.KeyInstallStatusTest#getStatus …AndroidJUnitRunner` —— **手上素材無此 apk／類／方法**（`Test_Items.txt` 只有 CertProvider 15 條；`KeyInstall_IntegrationTests.zip` 未投遞、未拆） | 該步驟無通道 → 依 R-SEC7(b)／R-SEC4(b) 寫 `PENDING: X-f KeyInstall status test runner`；或改以 037 之 DIAG 路徑（`SYSAD_IF_KI_DIAG_STATUS`）但 037 KI-011 原文是 Binder，DIAG 只可作 Remarks |
| `NR1L-KI-001` | Proc 1／ER 1 | `$ adb shell od -t x1 /mnt/vendor/oemkeys` —— 對目錄下 `od`；037 只說「installstate file … in the predefined secure partition」，**檔名未載** | `$ adb shell od -t x1 /mnt/vendor/oemkeys/<installstate file>`，`<…>` 佔位；ER `shows 02` |
| `NR1L-CP-002` | ER 1 | `error 2 at 0 depth lookup: unable to get issuer certificate` —— `error 2`／`0 depth` 為 OpenSSL 執行時值，**素材未載**；只有 `unable to get issuer certificate` 是可引用之 OpenSSL 標準訊息（domain constant） | `The openssl command prints "unable to get issuer certificate" on stdout` |
| `NR1L-CP-002` | ER 3 | `ECU certificate verification broken chain = ERR_UNABLE_TO_GET_ISSUER_CERT_LOCALLY` —— 此字串逐字來自 CS.212 row 1，**但其測項是 `ecuCertTestBrokenChainCert`**，本 TC 跑的是 `fotaMcpuCertTestBrokenCert`；把 ECU 場景的 log 移植到 FOTA 場景即造值 | ER 3 改 `PENDING: X-h log keyword`；或本 TC 改跑 `ecuCertTestBrokenChainCert`（則 SWE1 對應改 CP-005／ECUCert，見 apk_pairing #10）|
| `NR1L-CP-001` | ER 3 | `shows no "ERR_" entry` —— `ERR_` 前綴由單一樣本推得 | 允許，但 Remarks 標 `prefix inferred from CS.212 row 1 sample` |

### 二-2　跨樹污染（`_F §2`：CertProfile 只限 Code Signing 場景）—— FAIL

| TC | 位置 | 問題 | 正確處置 |
|---|---|---|---|
| `NR1L-CP-003`／`-004` | ER 1、Pre-Cond 3、Proc 3 | 037 CP-002 之場景是 **SAM**（`SAM Dongle`、`{TYPE}/cfgs/`、`samCertTestNormalFlow`），ER 1 卻斷言 **Code Signing 樹**之 CN `CS_{SupplierID}_…`（CertProfile r18） | ER 1 依 037 原文粒度：`The openssl output shows a "Subject:" line identical to the subject name in SecurityAssets/oem-certs/cert-provider/{TYPE}/cfgs/`；CertProfile 引用移除（DR-q reference）|
| `NR1L-CP-005`／`-006` | Proc 3／ER | CP-011 是通用列，Proc 3 用 `fotaMcpuCertTestNormalFlow`；`apk_pairing.tsv` #01 只配 CP-001／008，**硬配** | Proc 3 改 `PENDING: X-e OID-variant certificate + runner method`，或 Remarks 標 `apk pairing: none; fota method used as FOTA-scenario instance only`——擇一，Pei 裁（§四-5）|

### 二-3　無觸發之觀察步驟（R-SEC7(a)／IN §5.5）—— FAIL

| TC | 位置 | 問題 |
|---|---|---|
| `NR1L-CP-004` | Proc 3 | host 端 openssl 之後直接 `adb logcat`，**DUT 上無任何步驟觸發 Cert Provider 驗證**，ER 3 卻期待拒絕 log |
| `NR1L-CP-006` | Proc 2 | 同上 |
| `NR1L-KI-001` | ER 3 | `62 FF 02 00` 入 ER 作判準，**違 R-SEC8(e)**（DID 只入 Remarks）；且 037 四態 vs FF02 兩態未解（DR-o 開）|
| `NR1L-ECUC-001` | Pre-Cond 2 vs Proc 1 | Pre-Cond 已宣稱「session is 60」，Proc 1 又送 `10 60`——重複；擇一 |
| `NR1L-ECUC-001` | ER 3 | `ls -l /data/misc/ecuidentity` 「lists the exported CSR file」——`ecu.crt` 於開機即存在（PDF Step 2），**與 DID 讀取無因果**；不能證明介面行為 |
| `NR1L-SAM-001` | ER 2 | ER 引 `adb shell ls -l /data/vendor/dauth`，但 Proc 2 只有「Insert USB」無此指令——ER 引了步驟中不存在之手段（R-SEC7(f) 一步一通道）|

正確處置：負向場景若素材無可觸發之 apk 方法（wrong-subject／wrong-OID 皆無），**該步驟寫 `PENDING: X-e <variant> + trigger`**，不得以 logcat 充數。這正是 `channel_feasible` 該抓出的：Pilot 證明 CP-002／011 負向分支實為 `N`，`trace_summary` 4b 對此二列標 `Y` 有誤，執行層重判。

### 二-4　欄位形制（R-1 v2 基準本 SWC 0708）—— FAIL

| 欄 | 問題 | 處置 |
|---|---|---|
| `Input Test Data` | 10 列全填敘述文字；**R-1 v2 基準：`NA` 為常態**（SWC 285/286），資料內聯 Pre-Condition | 全部改 `NA`，內容併入 Pre-Condition |
| `Test Case Priority` | 全 `High`；IN §10.2 為 `P0`~`P3` | **執行層量測 SWC 0708 與 pm_29 該欄實際值**，依交付本；若交付本為 `P0`~`P3` 則改（CP-001/002 = P0 憑證驗證核心；其餘 P1）|
| `Test Case Design Methods` | 寫 `Functional Based`；本 workbook `下拉選單` sheet 為 `功能測試 (Functional based ; no specific technique)` 等雙語值 | **量測 SWC 0708 該欄實際值**，逐字沿用交付本寫法（含負向 `負向測試 (Negative / Invalid)`）|
| 字面引號 | ER 以反引號 \` 包字串（`` `SAMcert.pem: OK` ``）；IN §11：值與顯示文字一律 `"..."` | 全部改雙引號；lint 加：TC 欄含 \` 者 FAIL（併入 `SC`）|
| Pre-Condition | `Certificate assets of group X-e …`——`X-e` 是內部台帳代號，**不得入交付欄** | 改資產描述（`Valid leaf certificate and trusted chain under SecurityAssets/oem-certs/cert-provider/`），`X-e` 只入 Remarks |
| PENDING 寫法 | `…; the exact keyword is \`PENDING: DR-SEC-h log keyword\`` 混入散文 | 該 ER 行整行為 `PENDING: X-h log keyword`（IN §8.4.3：欄位／行整體佔位）|
| `test_item` 上半 | 用 037 `Requirement Title`（例 `Verification of X.509 Leaf Certificate against Trusted Chain`）| §4.3.1 為「需求原句 verbatim」= `Description` 之句；Title 非需求句。**Pei 裁**（§四-1）；分析層建議 Description 首句 ≤50 token |
| `KI-001` Design Method | `State Transition`，但只讀一個狀態 | `Functional Based`（§12 tie-break）|

### 二-5　可接受並記錄

- `SWE1-KeyInstall-…_SWE1-KeyInsyall-011` 兩拼字並存：准，Remarks 已註。
- `<…>` 佔位 4 處：准。
- `62 29 65 <CSR bytes>, up to 1000 bytes (CS.00102 …)`：准，ER 內引來源可（首例，profile 記入寫法）。
- Vehicle Model 七欄：准。

## 三、Pilot 之結論

lint 四項全綠、PENDING 3、但**內容違規 4 類共 14 處**（10 TC 中 8 TC 至少一處）。`SC`／`SS` 能保證「有指令行、有 source 標」，**不能保證指令是真的、字串是這個場景的**。Pilot 目的達成：抓到造值與跨場景移植兩種新型錯誤。

## 四、待 Pei 裁（SEC-03 前）

| # | 項 | 分析層建議 |
|---|---|---|
| 1 | `test_item` 上半來源：Title vs Description 句 | **Description 首句（≤50 token）**；Title 已在 Test Set／括號下半可見，重複無益 |
| 2 | Priority 與 Design Method 之寫法 | **依 SWC 0708 交付本實測值**（執行層先量測回報，再定）|
| 3 | PENDING token | `PENDING: X-h log keyword`（R-SEC7(e) amend；`X-` 系列與 `DR-` 並列為合法 token）|
| 4 | 負向分支無 apk 方法時 | Proc 之觸發步驟寫 `PENDING: X-e <variant> + trigger`，**不以 logcat 代替觸發**；`trace_summary` 4b 對 CP-002／011 負向分支改判 `N` |
| 5 | CP-011 與 fota 方法之硬配 | **不硬配**：Proc 3 PENDING；apk 只在 `apk_pairing.tsv` 有列者用 |
| 6 | KI-011 Binder 呼叫無素材 | Proc 2 PENDING（`X-f` 加「KeyInstall status test runner」一項）；DID `22 FF 02` 全段入 Remarks |
| 7 | `I-cross` 豁免 | 准 |
| 8 | 反引號 | 禁，改 `"…"`；lint `SC` 加此判 |
| 9 | pilot02 取樣 | 同 10 列重寫（含 CP-004 之 CRL 與 KI-011 ERROR 分支換回 #5／#10？）—— 建議 **12 列**：原 10 ＋ CP-004 ＋ KI-011 ERROR，以驗 R-SEC8(f) 之 CRL 寫法 |

## 五、SEC-03 預告

內容：(1) §四 裁定落 RULINGS（R-SEC15：欄位形制與 pilot 違規類型之明文，含「跨場景字串移植 = 造值」、「logcat 不得代替觸發」、「內部台帳代號不入交付欄」）；(2) `trace_summary` 4b 重判；(3) `EXEC_ASSETS.md` 之 `X-e` 加 wrong-subject／wrong-OID 之**觸發手段**需求、`X-f` 加 runner；(4) lint `SC` 加反引號與內部代號（`X-[a-z]`／`DR-SEC-`）出現於 Pre-Condition／Proc／ER 之 FAIL；(5) **pilot02 12 TC**。
