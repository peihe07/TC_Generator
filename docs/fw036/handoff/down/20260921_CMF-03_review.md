# 審閱 CMF-03 — 通過（候選本 v2 成立；DR-46 之結案卡在一個平台事實，待 Pei）

- 日期：2026-09-22
- 對象：`up/20260921_CMF-03.md`
- 判定：**通過**。J／P／L 三項目標達成；對 0907 本不符 0／21,309；對 `revise_0918` 差異 1,185 格全數歸因；JSON 57/57；pytest 無回歸；停點取「平台不對 ＝ 同一停點」為正確 —— 代入受測車上不存在之 frame 比「不同名」更嚴重。

## 1. 分析層自陳（A 類）

| # | 分析層之誤 |
|---|---|
| A-CMF14 | CMF-01 上繳所稱「4 份 DBC 查無此二訊號」係 `grep` 未加 `-a` 之靜默結果，分析層照收未問「怎麼查的」——違反 R-G50 之精神（全稱否定須附查法與命中數）。執行層本輪自行更正 |
| A-CMF15 | CMF-02_review §3-3 之「導航改寫約 85 條」無量測依據；執行層量得兩個口徑為 336（上界）／115（已明寫導航步驟者）。85 作廢 |
| A-CMF16 | CMF-02_review 引 `R-G78`（僅為 GC-16 候選，未落地）與裸節號 `§8.6`，致 `canon_refs` 各 +1。`down/` 唯讀，不回修；後續下放包引 GC 候選一律寫「R-G78 候選（未落地）」 |

## 2. 逐項裁定（即時生效；Pei 可撤）

| # | 項 | 裁定 |
|---|---|---|
| — | R-C58 停點之適用 | **准**。不取字面代入 `BCM_FD_27.EBL_Stat`。結案路徑見 §3-1 |
| R-C60 | M7 第 4 問合稱 | `Face/Feet`、`Feet plus Windshield` **不加引號**，維持 state 句型（`The Face/Feet airflow mode is active`）；若該行為 Select 動作，寫 `Select the airflow mode "Face" and "Feet"`——**先量**工作簿與條文有無此雙鍵操作之逐字，查無則整行維持原樣並列 plan，不改 |
| — | 軸 2 兩條補 PC ＋ `2.11` | **核**。屬既存違規之顯化，處置與工作簿既有 15 條同句，無新事實 |
| — | `-146` Remarks | **補**，與 `-467`／`-468`／`-275` 同句型，登 `AMBIGUITY_REMARKS`（四條一致） |
| R-C61 | `H`／`W` 之 `matches` | 該四條之 `most closely matches` 為條文逐字（2.3／7.x 同句），**不改寫**。lint036 `H`／`W` 於 comfort 範圍對「已登 `AMBIGUITY_REMARKS` 且 Remarks 載 ambiguity 句」之列豁免，做法比照 Security `FEATURE_EXEMPT_QUOTED`（位置條件式，非全列豁免）。目標 H＝4→2（`-035`／`-301`／`-440` 另三處先量是否同型，同型則一併登錄後豁免）|
| — | A-CMF13、`gen_batch` guard 未及 `cmf0x_apply.py`、`er_shift` 為上界 | 收到，無異議 |

## 3. 待 Pei（本線下一包之前置；分析層不代裁）

### 3-1. DR-46 結案 —— 一個事實題

`Logical Identifiers and CAN Mapping v1_78.xlsx` 把 `RECIRC_STAT`／`EBL_Stat` 定為 **LID**，逐平台對映：

| LID | Atlantis（MID） | Atlantis High |
|---|---|---|
| `RECIRC_STAT` | `STATUS_CLIMATE2.HVACRecirc_Sts` | 同 |
| `EBL_Stat` | `STATUS_CLIMATE2.HVACRearDef_Sts` | `BCM_FD_27.EBL_Stat`（FD CAN8）／`BODY_CNTRL3.EBL_Stat`（CAN-B）|

您 9/21 說本功能只有「alt-mi」有。**請確認：alt-mi ＝ 上表之 Atlantis（MID）？**

- 是 → 分析層即裁：LID 對映表視為 canon §8.7.5(f) 之「對照表」，四條寫 `Send CAN: STATUS_CLIMATE2.HVACRecirc_Sts = 2 (BLINK)`／`STATUS_CLIMATE2.HVACRearDef_Sts = 2 (Blink)`（label 逐字取各自 DBC `VAL_`），`test_item` 上半保留 `$RECIRC_STAT$`／`$EBL_Stat$` 原文，Remarks 載 LID → 訊號之對映出處。**前提**：執行層須先證 `HVACRearDef_Sts` 於 Promaster／Toro／Fastback 三份 DBC 逐字存在（本輪只證了 `HVACRecirc_Sts`）。
- 否 → 回一句是哪一欄。

### 3-2. 車型欄細分

`vm_axis_plan.tsv` 的關鍵一列：**454 條**帶「not 3-knob HVAC controls with ICS」之排除式 PC，而 Toro 之 PROXI 為 `Climate_Panel_Type = 6 (3 knobs)` ＋ `Integrated_Climate_Touchscreen = 1`。若 PROXI label 與條文 2.14 講的是同一件事，Toro 幾乎全本不適用，Y 欄應為 0；若不是同一件事，維持 1。分析層無法判，也不會判。另 Promaster 的 `Rear_Climate = 0` 對 Rear Climate 56 條、`Climate_Type` 對 ATC／MTC 52 條，同題。

### 3-3. 沿 CMF-02_review §3 未決者

R-C44 位階（分析層仍建議維持；值域類 5 條可補產）、`ENTER_COMFORT` 入 canon §5.3（您改原檔後重貼）、DR-49 登與不登、`Climate` 畫面之 hop 標籤條文查無（`nav_rewrite_plan` 290 條 `none`）。

## 4. 下一包

**CMF-04 暫不下放**。可做之事全卡在 §3-1／§3-2；先請您 commit CMF-03 產物（含 17 支 generator guard、`scripts/lint036.py` R-C57、新測試檔），並回答 §3-1 一句。屆時 CMF-04 ＝ DR-46 結案四條 ＋ R-C60／R-C61 ＋ `-146` Remarks ＋ 軸 2，一次 Revise 成候選本 v3；車型細分與導航改寫各自另包，均先 plan。
