# 下放包 CAM-01 —— Camera feature intake（scaffold ＋ 來源登錄 ＋ recon ＋ framework 草案）

指派執行層 session：**新開一個 Claude Code session**（R-G62(b)）。
分析層 session：本包由 2026-09-16 之 Camera background search session 落檔；
背景查核全文另存 Pei 側 `camera_background_search_20260916.md`（非 repo 檔，執行層不需讀）。

---

## §0　禁區

- **git 一律不動**；Pei 為唯一 git 操作者。
- 不動 `down/`、`up/` 既有檔；不動 `features/` 下其他 feature 目錄。
- 不得生成任何 TC；本包止於 Phase 0–1（intake ＋ recon）＋ framework 草案之驗算，Phase 2 以後另包。
- 不得自行對兩本 037 做範圍篩選（Pei 已裁：兩本整份）。
- `forms/proxi/` 六平台十檔為 2026-09-16 Pei 親手搬入之公用資料，**不得改名、不得移動**。
- 來源原檔位於 `10_Reviewing/00_TestCase/ASW-R3/Camera/`（含 `REF/`），不在 repo；登錄依 R-G66 落 `sources/raw/`。

## §1　背景

新 feature。客戶側資料夾 `ASW-R3/Camera`，內含**兩本 037**：

| | A 本（先寫） | B 本（後寫） |
|---|---|---|
| 檔 | `SWE1_CAMERA_FM-WI-FSM-037-A03 STLA 報告_SWRA STLA Report_SWRA_V02.xlsx` | `FM-WI-FSM-037-A03-N1L-SWE1-RVC-HMI-V0.1 STLA 報告.xlsx` |
| sha16 | `cae0df17016b3532` | `6d5812a03da1d148` |
| 列 | 25（`SWE-CAM-001`～`025`） | 230（`SWE1-RVC-001`～`159`，含 `-NN-nn` 子項；umbrella 列 27） |
| 層 | Service：`NormalCameraDaemon`／`EVS HAL`／`NCD HAL`／`NormalCamera App` | HMI：SYS1 HMI L&F 兩份 |
| 來源 | CFTS092 ＋ VF551_V2/V3/V33/V4/V42 ＋ **VF617_V5（缺件）** | `SYS1_HMI_RVC+PAM_R1_Low_SR24_1A_(June_25_2021)` 57 列 ＋ `SYS1_HMI_HeadUnitCameraSystems_HMI_Logic_and_Flow_R1_SR24_Post_2A_v7_(February_10th, 2023)` 173 列 |

兩本**互為步驟來源**：A 本之 HMI 入口與設定 hop 取自 B 本之 SYS1 匯出（§5.8）；B 本之觸發訊號與 PROXI 前提取自 A 本已定之 CAN／PROXI 記法（§8.7.5）。故 framework 為單一份、兩本共用。

## §2　裁定（Pei 2026-09-16）

執行層取號：`RULINGS.md` 由本包 scaffold 產生，`{live}` 期望值 **1**；落檔前現查（R-G23）並於上繳包回報。

```text
R-CAM{live}  兩本 037 皆須產出；A 本先、B 本後；互為步驟來源
  (a) A 本（SWRA_V02，25 列）與 B 本（RVC-HMI-V0.1，230 列）各出一本 FW036 工作簿。
  (b) 生成順序 A → B。A 本之畫面入口／設定 hop 一律引 B 本之 SYS1 HeadUnitCameraSystems 匯出，
      不得自造；B 本之訊號／PROXI 前提一律沿用 A 本已定之寫法。
  (c) 追溯各歸各母體（IN §8.4.2）：A 本 spec_reference 引 CFTS092／VF551，B 本引 SYS1 HMI L&F 章節；
      互參只參考寫法，不複製對方之 spec_reference。
```

```text
R-CAM{live+1}  Vehicle Model 七欄以 1／0 填寫；598／5210 一律 0
  (a) 工作簿 `Vehicle Model 車型` 七子欄（HDCC27 Atl-Hi／DT27 Atl-Hi／VF(ProMaster)637 Atl-Mi／
      Commander (598) Atl-Mi／Regengade (5210) Atl-Mi／Toro(2261) Atl-Mi／Fastack (376) Atl-Mi）
      每列填 `1`（適用）或 `0`（不適用），不留空、不用其他符號。
  (b) `Commander (598)` 與 `Regengade (5210)` 已不支援，Camera 兩本一律 `0`。
  (c) 五個有效車型欄每列至少一個 `1`。lint 新增檢查項（本 feature profile [ADD]）。
```

```text
R-CAM{live+2}  車型軸之拆分判準
  同一驗證點，若 PROXI 前提值、觸發訊號名（Atl-Hi vs Atl-Mi 之 CAN 訊號）、或 ER 可觀察結果
  任一因車型而異 → 拆 sibling，各列只勾自己的車型（§8.3 mode 軸），
  test_item 括號下半以車型／EE 作區分 token；全部相同 → 一列，勾全部適用車型。
```

平台 ↔ VF ↔ PROXI 對照（分析層實測 `forms/proxi/` 六本，`Rear_View_Camera` 六本皆 byte 86 bit 0）：

| Vehicle Model 欄 | forms/proxi | VF551 | EE |
|---|---|---|---|
| HDCC27 Atl-Hi | `HDCC28_ATL_HI`／`HDCC27_initial` | V2 (`PHDCC27` anchor)／V4 | Atl-Hi |
| DT27 Atl-Hi | `DT28_ATL_HI` | V2 (`PDT27` anchor) | Atl-Hi |
| 637 Atl-Mi | `Promaster_ATL_MI` | V42 | Atl-Mi |
| 2261 Atl-Mi | `Toro_ATL_MI` | V33 | Atl-Mi |
| 376 Atl-Mi | `Fastback_ATL_MI` | V3 | Atl-Mi |
| 598／5210 | 無 | 無 | 一律 0 |

## §3　裁決引用

| 引用 | 用途 |
|---|---|
| `R-G42`（ledger 本） | TC ID `NR1L-{ABBR}-{nnn}`、Test Group、D 欄升冪 |
| `R-G66` | 來源集中制 `sources/raw/<doc_id>/` |
| `R-G67` | intake 嵌入物件檢查（CFTS092 docx 含 `{pict}`？執行層查並回報） |
| `R-G70`、`R-G71` | profile 啟用 lint `P`(v4) 與 `X` |
| `R-VF9`（vehicle_setting） | 兩本 workbook 同一 Test Group 值之前例 |
| `R-PH{…}`（PH-01 §2） | 「HMI 為文件類別尾綴，非 feature 名」之前例 |
| R-G23 | 取號現查 |

## §4　執行層任務

**任務 0 — 投遞區確認（最小投遞集，R-G44′ sha 同一性 ＋ R-G45／R-G66 不存副本）**
分析層 2026-09-16 已以 sha256 比對 repo 既有落點；**已在 repo 者不投遞，只由執行層登 MANIFEST 指向既有落點**。

(a) **不投遞 —— repo 已有同體或現行版**：

| 檔 | 既有落點 | 依據 |
|---|---|---|
| `SYS1_HMI_HeadUnitCameraSystems_…_v7_(February_10th, 2023).xlsx` | `spec-index/cache/`（sha16 `0df5da9186d761e3`，與 REF 本**全等**） | R-G44′ |
| `HeadUnitCameraSystems HMI Logic and Flow … v7.pdf`（17 MB） | `spec-index/sources/` | 同上 |
| `RVC+PAM R1 Low SR24 1A (June 25 2021).pdf` | `spec-index/sources/` | 同上 |
| DBC 四本、`PROXI_HDCC27_R3_20250424.xlsx`、`forms/proxi/` 六平台、HMI Settings List | `forms/` | R-G45 現行版 |
| VF551_V9 全套 | — | A 本未引用，不入 |

(b) **必投遞 `_intake/Camera/`**（repo 內無同 sha）：

| 檔 | sha16 |
|---|---|
| A 本 037 | `cae0df17016b3532` |
| B 本 037 | `6d5812a03da1d148` |
| `SYS1_HMI_RVC+PAM_R1_Low_SR24_1A_(June_25_2021).xlsx`（REF 本） | `5a1c0ab24991dcb1` —— **與 `spec-index/cache/` 同名本（`1a0bef53c6de975c`）為同名異體**；執行層 diff 兩者之 `Basic Report` 列數與 Description 差異回報，Pei 裁用哪本；未裁前 B 本追溯以 REF 本為準（037 所引者） |
| `SYS2_CFTS092_…SYSRA_CFTS092_V01.xlsx` | `d3b3393dde8c297c` |
| `SYS2_VF551_V2_…_V01_0525.xlsx` | `e576647a603944a3` |
| `SYS2_VF551_V3_…_V01.xlsx` | `ac0d41b17ddb9f17` |
| `SYS2_VF551_V33_…_V01.xlsx` | `d25fd1a64d77de9f` |
| `SYS2_VF551_V4_…_V01.xlsx` | `b8baac50f1ff480e` |
| `SYS2_VF551_V42_…_V01.xlsx` | `0eaed0b8e6e1272c` |
| `CameraEventHal status.xlsx` | `4a1c3ef6b8168af0` |
| `R1LR_Atl-H_26PI1.5 … CFTS 092_Radio Camera Management_20260310-1555.docx`（63 KB） | 執行層取 |
| VF551 V2/V3/V33/V4/V42 `.docx` 五件（合計 < 400 KB） | 執行層取 |

(c) **選投**：`SYS3_CAMERA_…SYSAD_V02.docx`（6.9 MB）——只供背景（NCD／EVS HAL 定義、UDS 序列），TC 不直接引用；Pei 不投則 MANIFEST 以 `note` 記外部路徑，不登 doc_id。

共 12 檔必投，總量約 2.5 MB。

**任務 1 — scaffold**
```
python scripts/new_feature.py Camera --root .
```
ABBR 候選由 recon 提；A／B 兩本是否共用一組 ABBR 序號，或 B 本另取（如 `CAMH`），列入 DECISIONS.md [PROPOSED]，Pei 裁。

**任務 2 — 來源登錄（R-G66）**
全部投遞檔登入 `sources/raw/<doc_id>/` ＋ `MANIFEST.tsv`（`features` 欄 = `camera`）。
CFTS092 docx 依 R-G67 檢查嵌入物件並回報。

**任務 3 — recon（Phase 1）**
每項附查詢條件，對照 §6 預期數字：
1. A 本：資料列、Sub Categorization 分佈、Source ID 家族分佈。
2. A 本 `Source Requirement ID` 對六本 SYS2 之解析率；未解析者逐列列出（預期全為 `VF617_V5` 系）。
3. A 本引用之 SYS2 項中 Category = `Out of Scope` 者逐列列出（預期 32，皆 RVCM 端行為）。
4. B 本：資料列、umbrella／leaf 數、兩份 SYS1 來源之列數；B 本 `HMI Source ID` 章節號對 SYS1 `Basic Report` `Outline Number` 之對映率（預期 100%）。
5. VF551_V4 SYS2 之 `VF章節` 欄為空 —— 回報其列數並提 Layer 3 對 V4 之替代判法（以 anchor `VF551_V4_PHDCC27_VF_nnn` 回查 docx 章節）。
6. A 本 RD 品質異常（登 ANOMALIES 候選，非裁定）：
   - `SWE-CAM-017` 描述（`CameraDisplaySts`，來源 CFTS092 `SYS-RA-CAM-097/098` Forward Facing Camera 節）與 VC/VM（Air Suspension PROXI、4X→1X reset）不對齊。
   - `SWE-CAM-018` 之 `8 mph / 10 s` 標「e.g.」；CFTS092 `4781643`（`SYS-RA-CAM-078`）原文為 `display timer > 10s AND vehicle speed above 8 mph`；VF551_V2-492 為 Manual mode 退出條件。
   - 拼寫 `VF617V5`／`VF617_V5` 並存（`SWE-CAM-003`）。
   - 重疊列：`-012 ↔ -014`（PowerShutDownNotifcation）、`-004 ↔ -009`（DTC vs systemStatus）、`-020 ↔ -025`（V33-227，Daemon vs App）、`-015 ↔ -018`（V2-488～498）。
7. `CameraEventHal status.xlsx`：列出 `Supported by Harman = N` 或 `MD fake CEH status = Not yet` 之訊號，對映到受影響 SWE（分析層預判：`BED_EXTENDER.*` → -020/-025；`Backup_Cam_Delay` → -015/-018 之 Delay 路徑改走 HMI 設定 hop；`PowerModeSts` → -002 改用 `CmdIgnSts`）。

**任務 4 — framework 草案之驗算（§5）**
以 recon 之實測數字回填 §5 各表之 leaf 欄；Layer 2 歸屬逐列列出（A 本 25 列、B 本 230 列），與 §5 草案不一致者逐筆記明依據，**不自行改 Layer 2 名稱**（名稱歸 Pei）。

**任務 5 — profile 骨架**
`docs/runtime/profiles/FW036_R1L_Camera_Profile.md`：啟用 lint `P`(v4)、`X`；
[ADD] R-CAM{live+1}(c) 之 Vehicle Model 檢查；
[ADD] §5.3 常數候選 `ENTER_CAMERA_SETTINGS`（依 HMI Settings List `Settings` 分頁 row 464 `13. Camera` 節；hop label 帶星號者如 `Rear View Camera Delay*` 之 `*` 是否入 hop 待 Pei 裁，列 DECISIONS）。

## §5　framework 草案（Layer 1–3；未鎖定，Pei 裁）

依 canon §4.1.2 步驟 5，只列 Layer 1–3，不列個別 RD；leaf 欄由任務 4 回填。

### Layer 1

`Camera` —— A、B 兩本同值（R-VF9 前例）。B 本之 037 標題 `SWE1-RVC-HMI` 依 PH-01 R-PH 前例，`HMI` 為文件類別尾綴，feature 名為 RVC；惟 B 本內容涵蓋 AUX／Camera App／CHMSL，`RVC` 名不足以概括，故提 `Camera`。
**替代案**（Pei 擇）：B 本 Test Group = `Camera HMI`。

### Layer 2 —— A 本（Service 層，25 列）

Sub Categorization ∩ VF 章節之交集：

| # | Test Set | SWE-CAM | Sub Cat | 主要 VF 章 |
|---:|---|---|---|---|
| 1 | `Startup and Shutdown` | 001, 012, 014 | Daemon／NCD HAL | V33 1.11.1.2、V2 1.14.1、V3 1.10.2 |
| 2 | `Configuration` | 002, 005 | Daemon／EVS HAL | V2 1.13.1、V3 1.10.2.1、V42 1.11.2.1 |
| 3 | `State Handling` | 003, 017 | Daemon | V42 1.11.1.2、CFTS092 §1.3.8 |
| 4 | `Diagnostics` | 004, 013 | Daemon／NCD HAL | V2 1.10.2.1、V42 1.13.2.1.2.0.x |
| 5 | `LVDS Messaging` | 007, 008, 009, 010, 011 | NCD HAL | V2 1.13.1、V2 1.10.3.2、V42 1.11.2.1 |
| 6 | `Video Pipeline` | 006, 019 | EVS HAL／Daemon | V42 1.11.1.2.0.3.2、V33 1.11.1.2.2 |
| 7 | `Display Arbitration` | 015, 016, 018, 020 | Daemon／App | V2 1.13.2.1.x、V3 1.10.2.2/3、CFTS092 §1.3.6 |
| 8 | `HMI Overlays` | 021, 022, 023, 025 | App | V2 1.13.2.1.4～8、V2 1.13.2.2.x |
| 9 | `AUX Camera` | 024 | App | VF617_V5（缺件） |

註：`-017` 依其描述（`CameraDisplaySts`）暫歸 `State Handling`；RD 對齊後可能改歸 `HMI Overlays`（其 VC 講 4X→1X）。`AUX Camera` 為單列 Test Set，屬 §4.2「genuine outlier」，且全數待 DR-a。

### Layer 2 —— B 本（HMI 層，230 列）

以 SYS1 HeadUnitCameraSystems 之 35 章與 RVC+PAM 之 11 章聚合：

| # | Test Set | SYS1 章（HeadUnitCameraSystems） | RVC+PAM 章 | 037 列（約） |
|---:|---|---|---|---:|
| 1 | `PAM Integration` | — | 6.x, 11.1 | 8 |
| 2 | `Activation and Exit` | 1, 6.1, 6.5, 6.7 | 7.1～7.5 | 45 |
| 3 | `Camera Settings` | 6.2 | — | 6 |
| 4 | `Warning Banners` | 3, 4 | 8.2～8.4, 9.2 | 15 |
| 5 | `Camera App` | 18, 19, 20, 21, 22 | — | 10 |
| 6 | `AUX Camera Access` | 26, 27, 28, 34.1～34.8 | — | 60 |
| 7 | `Wireless Camera Setup` | 32, 33 | — | 12 |
| 8 | `AUX Camera Settings` | 29, 30, 31, 34.9, 34.10 | — | 70 |

「037 列（約）」為分析層依章節號粗估，任務 4 回填實數。§4.1.3 決策測試：每一 Test Set 篩出者皆為同一 setup 型態（同 PROXI 配備 ＋ 同入口）。

### Layer 3

**不入工作簿**（§4.1.5）。
- A 本：以 **VF 章節號**（SYS2 `VF章節` 欄，逐字）為 Layer 3；跨 VF 家族之同章節依平台分列（R-CAM{live+2}）。CFTS092 來源以 §1.3.x 章名（`Cargo/CHMSL Camera`／`Rear Camera`／`Surround View Camera`／`Forward Facing Camera`／`Vehicle Camera App`）為 Layer 3。V4 章節缺，任務 3-5 補判。
- B 本：以 **SYS1 `Outline Number`**（逐字）為 Layer 3，B 本之 `HMI Source ID` 已內建。

### 交叉參考索引（framework 附表，任務 4 產出）

`SWE-CAM ↔ SWE1-RVC` 對照表：以共同之 CFTS092 ObjectID（`SYS-RA-CAM-062～098`）與 HMI 章節之語意配對，供 A 本取 hop、B 本取訊號。分析層預判之強配對：
`-015/-018 ↔ RVC-008/019/020～023/045`（reverse 進出與 Delay）、`-016 ↔ RVC-048/050/051`（手動入口）、`-021/-023 ↔ RVC-026～031`（banner／overlay）、`-024 ↔ RVC-053～062, 084～108`（AUX）、`-025 ↔ RVC-036～039`（fault banner）。

## §6　預期數字（分析層 2026-09-16 實測，openpyxl read_only）

| 項 | 預期 | 查詢條件 |
|---|---|---|
| A 本資料列 | 25 | `Analysis Report` 列 8 起，A 欄非空 |
| A 本 Sub Cat | Daemon 12／App 5／NCD HAL 7／EVS HAL 2／混合 1（-019） | I 欄 |
| A 本 Source ID 引用總數 | 541 | B 欄逗號分割去空 |
| 六本 SYS2 解析成功／查無 | 422／119 | 查無者 prefix 全為 `SYS-RA-VF617_V5`（117）＋ `SYS-RA-VF617V5`（2） |
| 解析成功之 Category | Functional 386／Out of Scope 32／Non Functional 4 | SYS2 K 欄 |
| `SWE-CAM-024` 解析 | 0／12 | 全 VF617_V5 |
| B 本資料列 | 230 | 同上 |
| B 本 Sub Cat | HMI 197／Service 6／空 27 | I 欄；空 = umbrella |
| B 本來源 | RVC+PAM 57／HeadUnitCameraSystems 173 | C 欄檔名前綴 |
| SYS1 HeadUnitCameraSystems 列／頂層章 | 1045／35 | `Basic Report` A 欄非空；Outline Number 不含 `.` |
| SYS1 RVC+PAM 列／頂層章 | 63／11 | 同上 |
| SYS2 各本列 | CFTS092 171／V2 752／V3 747／V33 662／V4 321／V42 801 | `Basic Report` B 欄非空 |
| CameraEventHal 訊號列 | 38（Atl-H 14／Atl-M 24） | Sheet1 A 欄非空 |
| forms/proxi | 6 目錄／10 檔 | `find` |
| HMI Settings List `13. Camera` | `Settings` 分頁 row 464 起 13 項 | A/B 欄 |

## §7　DR 清單（本包登記，Pei 送）

| DR | 缺件 | 影響 |
|---|---|---|
| DR-CAM-a | `VF617_V5`（SYS2 xlsx ＋ VF docx） | A 本 119 個來源不可追；`SWE-CAM-024` 整條 PENDING |
| DR-CAM-b | SYS2 VF551_V4 之 `VF章節` 欄全空 | Layer 3 對 V4 需回查 docx |

已結（不登）：HMI L&F 匯出（REF 已補）、DBC（forms 既有四本）、PROXI（forms/proxi）、HMI Settings List Cameras 項（row 464–478）。

## §8　上繳要求（`up/20260916_CAM-01.md`）

1. 投遞檔 sha256 全表；任務 0(a) 五項之 MANIFEST 登錄（指向既有落點，不複製）；RVC+PAM 同名異體之 diff。
2. scaffold 產物；`RULINGS.md` 三條 R-CAM 實際號與 body sha8。
3. §6 全表預期 vs 實測。
4. 任務 3 七項量測；第 2、3 項附逐列清單。
5. 任務 4：§5 兩張 Layer 2 表回填 leaf 數 ＋ 逐列歸屬檔 `features/camera/data/layer2_assign.tsv`；不一致逐筆。
6. 交叉參考索引草表 `features/camera/data/crossref_a_b.tsv`。
7. `DECISIONS.md` [PROPOSED]：ABBR、B 本 Test Group 名、`*` 入 hop 與否。
8. 未結 DR、自報錯誤。

## §9　升級條件

- 投遞檔 sha 與 §4(b) 不符；`spec-index/cache/` 之 HeadUnitCameraSystems xlsx sha 與 `0df5da9186d761e3` 不等。
- `new_feature.py` 因 `features/camera/` 已存在而拒絕。
- B 本章節號對 SYS1 對映率 < 100%。
- A 本 SYS2 解析率與 §6 差 > 2 筆。
- 任何需改 canon 或全域 ledger 之情形。
