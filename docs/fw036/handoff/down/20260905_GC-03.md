# 下放包 GC-03 —— 「都準都裁定」之落實（2026-09-05）

Pei「都準都裁定」（2026-09-05）：GC-02 審閱 §六 六項全准；R-G43 保號側與 `_intake/SXM/` 二檔**授權分析層裁**。
本包載此二裁與其落實任務。R-G43 裁定前之引用標側寫法（`R-Gnn(FO)`／`R-Gnn(LEDGER)`）自本包起**停用**，改用改號後之號。

---

## 一、分析層裁決（Pei 授權）

### R-G43 落實：**(A) 台帳保號**

理由：`RULINGS_LEDGER.md` 為 `rulings_hash.py` 之指紋源，且其條文附日期、來源與逐字本體；
FO §9.2 之條文為 runtime 摘要。改摘要之號，不改指紋源之號。

改號表（原號升冪，新號自 R-G51 起連號；**條文一字不改**，僅首行加「原編號 R-Gnn，2026-09-05 改號，見 R-G43」）：

| 原號（FO §9.2）| 新號 | | 原號 | 新號 |
|---|---|---|---|---|
| R-G12 | **R-G51** | | R-G21 | **R-G60** |
| R-G13 | **R-G52** | | R-G22 | **R-G61** |
| R-G14 | **R-G53** | | R-G23 | **R-G62** |
| R-G15 | **R-G54** | | R-G24 | **R-G63** |
| R-G16 | **R-G55** | | R-G25 | **R-G64** |
| R-G17 | **R-G56** | | R-G26 | **R-G65** |
| R-G18 | **R-G57** | | R-G27 | **R-G66** |
| R-G19 | **R-G58** | | R-G28 | **R-G67** |
| R-G20 | **R-G59** | | R-G29 | **R-G68** |

（以上 18 號以 `rg_collision_20260905.tsv` 之 `DIFFERENT` 列為準；若 FO 側於 12–29 間有缺號，執行層依實測收縮，**新號仍連號**，並於上繳回報實際對照。）

**R-G19 台帳內二擇一**：L358「寫回作業規定全專案化（Pei, 2026-08-25）」**保號 R-G19**（Pei 親裁）；
L402「理由與數字須分別驗證」改 **R-G69**。

R-G1～R-G11（FO 單邊）、R-G30～R-G42（台帳單邊）不動。

### `_intake/SXM/` 二檔：**非誤投之判定改為「投錯匣」，登記不清**

二檔為 CFTS_020 ICS and DCSD（`.doc`＋`.reqifz`），repo 內無同 sha 落點，而 `features/ics_management/` 存在。
裁：以 `ics_management` 為所屬 feature 登記入 `sources/raw/` 與 `MANIFEST.tsv`（doc_id 依 GC-02 導出規則），
之後 `_intake/SXM/` 之二檔由 Pei 清空（同其他四匣）。**不刪原檔。**

### 追認（GC-02 審閱 §二／§三）
R-G48(b)、R-G45 DBC 網段配對、R-G50(b)：Pei 准，入台帳（任務 1）。

---

## 二、執行層任務

### 1　條文入台帳（R-G36 機器抽取）
自 `down/20260905_GC-02_review.md` 抽 R-G48(b)、R-G45 補充（DBC 配對）、R-G50(b)；自本包抽 R-G43 落實表與 SXM 裁決。
節名 `## 下放包 GC-02／GC-03 之全域條文（Pei 2026-09-05「都準都裁定」）`。

### 2　R-G43 改號（本包最重）
2-1　`FEATURE_ONBOARDING.md` §9.2 之 18 條：標題 `#### R-Gnn` 改新號，條文首行加原編號註；§9.2 表頭之區間字樣同步。
2-2　`RULINGS_LEDGER.md` L402 節：`R-G19` → `R-G69`，首行加註；L358 不動。
2-3　出 `docs/fw036/SHA_MIGRATION_rg43.tsv`：`old_id / new_id / side / file / line / title`，19 列。
2-4　引用遷移，**分三類處置**：
- (a) `rg_refs_20260905.tsv` 判為 `FO` 側（681）＋判為 LEDGER 側且指 L402 條者：**機器改號**。改前對每處重驗判側詞仍在（避免表與檔已漂移）。
- (b) 歧義處（`BOTH_KW`＋`NO_KW`，1,332）落於**歷史檔**（`handoff/NN_*`、`upstream/`、`down/`、`up/`、`features/*/docs/handoff|upstream`、`sandbox/`、`lint_reports/`）者：**不改**（R-G18(FO)→改號後為 R-G57 之歷史檔處置精神）。
- (c) 歧義處落於**在效檔**（canon 三份、PROTOCOL、`features/*/{RULINGS,DECISIONS,PLAYBOOK,RUNBOOK,ANOMALIES,DATA_REQUESTS}.md`、`scripts/`、`tests/`、`backend/`、feature.yaml）者：**不改，逐處列表** `docs/reports/rg43_manual_20260905.tsv`（檔／行／上下文 ±1 行／兩側候選）交分析層判。附三類之分子分母。
2-5　既有 `R-Gnn(FO)`／`R-Gnn(LEDGER)` 標側寫法（EXPLICIT 7 處＋ GC 系列各包）：歷史檔不動；在效檔改為新號。
2-6　**`rulings_hash.py` 不跑**（Pei）。上繳附改號前後 `canon_refs` 三型計數（R-G50(b)：回報被改檔數）。

### 3　MANIFEST 併入
提案表剔除 `NOT_SOURCE_DOC` 20 列 → 159 列；**覆寫** `sources/MANIFEST.tsv`（現行 10 列逐字保留在前，新 149 列在後，`note` 首欄去 `proposed`）。
SXM 二檔：copy 至 `sources/raw/<依現行分層>`，登記 2 列（`features` 欄 `ics_management`），合計 161。改前 `get_file_info`，改後附 sha 與列數。

### 4　`cleanup_gc02.sh` 段二加註 `# was: <old> ; referenced by: <file>`；`bash -n` 重跑；不執行。

### 5　`backend/` 分頁名（R-G48(b)，Pei 准改 code）
5-1　先量：對 R-G1 母本與 `features/power` 工作簿各跑一次現行 `parser.py` 開檔，記成敗與訊息；五處硬編逐處分類「有 feature.yaml 覆蓋」／「純硬編」。
5-2　純硬編者改為：`feature.yaml workbook.sheet` 優先；未載時依序嘗試 `TC_SHEET_NAME_CANDIDATES = (兩變體)`；皆無則報錯列出實際 sheetnames。常數集中一處（`backend/` 內單一定義，其餘 import）。
5-3　夾具：兩變體各一；守衛測試：模板 `workbook.sheet` ∈ 母本 sheetnames（開 `forms/…20260817_ext.xlsx` 實讀）。
5-4　pytest 對照向（改前／改後逐項同名）；`bash -n` 不適用；`ruff` 不增紅。

### 6　DECISIONS DBC 配對註
`features/vehicle_setting/DECISIONS.md` 版本綁定節：四份並列保留，加一行「配對（R-G45 補充）：R4_BHCAN ↔ forms R1_BHCAN2；R5_FDCAN8 ↔ forms R1_FDCAN8」。

### 7　13 列影響判定之素材（分析層判，執行層備料）
對五本 DECISIONS 之 13 檔次，每對（`inputs/` 版 vs `forms/` 版）出 `docs/reports/binding_diff_<feature>_<n>.tsv`：
- xlsx：逐分頁列數差＋變動列之 key 欄（Settings List 取設定名；LID 取 signal；Market Config 取 param；Pop Up List 取 PU id）
- dbc：`BO_`／`SG_`／`VAL_` 三類之增刪改名清單
- 並自該 feature `delivered/` 最新工作簿 grep 變動 key 之命中列號（分子分母）
分析層據此逐列判，寫回 DECISIONS。

### 8　上繳 `up/20260905_GC-03.md`：全部產物 sha、2-4 三類分子分母、5-1 量測、對照向、自報。

## 三、Pei 動作（本包提醒，順序有別）
1. 任務 2 完成後：`rulings_hash.py`（R-G44–50、R-G69、改號 18 條、R-G30–41 補列一併）
2. `cleanup_gc02.sh` 專門 commit（先任務 4）
3. `_intake/` 五匣清空（SXM 待任務 3 登記後）

## 四、未結 DR
無新增。
