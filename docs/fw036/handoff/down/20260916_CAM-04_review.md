# 下放包 CAM-04 審閱 —— pilot02 逐列（分析層 2026-09-16）

對應上繳包：`docs/fw036/handoff/up/20260916_CAM-04.md`
判定：**裁定落檔六項通過；寫法對齊通過**（三種新句型皆有 canon 明文，准）；pilot02 十列中 **3 處實質瑕疵**（§二），修正後 pilot 即可判過，全量不必再跑 v3。

---

## §一　執行層報告之確認

1. VF230 本在 `output/` 非 `delivered/`、錨在 N 欄非 M 欄 —— 下放包兩處寫錯，執行層查對為準。VF 型錨 = SYS2 F 欄 anchor（`VF551_V2_PHDCC27_VF_484` 型）：**確認**，R-CAM11 成立。
2. `style_compare_w97.py` 不適用 → 自寫語料取樣器：接受。
3. Pre-Condition 行序依 VF230-0819 實測「電源 → PROXI → CAN source → 品牌 → 畫面」：**接受**，下放包所擬順序作廢；寫入 profile §4 作 Camera 定序。
4. 進設定三 hop（R-CAM8）與 VF230 之一 hop 並存：接受（來源不同，R-CAM8 已鎖）。
5. R-CAM3(e) 以同級錨自立以保 fenced sha：接受，此作法記入 GC_BACKLOG 作 R-TM13 之實作備註。
6. 臆造錨 `_1368` 被 selfcheck 攔下：`selfcheck_r_cam10.py` 之「反查失敗視同命中」設計**保留**。
7. DR-CAM-i（`IGN_LK ↔ Ignition_Pre_Off`）：接受；-002 帶 PENDING 於 pilot 可，全量交付前須結。

## §二　pilot02 實質瑕疵

| # | TC | 瑕疵 | 依據 | 修正 |
|---|---|---|---|---|
| 1 | **001** | 錨 `CFTS092-4781627`（`SYS-RA-CAM-062`）落在 CFTS092 **§1.3.5 Cargo/CHMSL Camera** 節（heading `SYS-RA-CAM-060`，4781625）；Rear Camera 節之同句為 `SYS-RA-CAM-075`（4781640，heading `-070` Rear Camera）。兩句逐字相同，但本列 Test Group 為 Rear View Camera | §8.4.2 scope | 錨改 `CFTS092-4781640`（來源列 `SYS-RA-CAM-075`）。全量時凡 CFTS092 之「疊層同句」（Cargo／Rear／SVC／FFC 各節重複之條文）一律取 Rear Camera 節 |
| 2 | **001** | Pre-Condition 1「HU is in the Full-Operation state」與 4「ignition is off」互斥，且步 1 才送 RUN | §4.4 | 開機類 TC 之首行不套 Full-Operation；改為 `The HU is in the off state`（VF230 本若有 off／Standby 態之慣用句則抄之，回報） |
| 3 | **003／004** | 改錨 V2-488／V3-266（「影像已顯示 → R → 續顯並切自動」）之理由是 V3-260 逾 50 token —— **但 canon §4.3.1 明文「超限須摘句，全文以 specification_reference 指回」**，逾限不是改驗證點之理由。結果是 pilot 失去 -015 之核心行為（無影像 → R → 顯示） | §4.3.1、§8.1 | 003 回 V2-498、004 取 V3-260，各**摘句 ≤ 50 token**（保留條件與結果子句，摘法回報）；488／266 之「手動→自動切換」為另一驗證點，登入全量之 -015 sibling 清單。A-CA26 以此結案（前提不成立） |
| 4 | 009／010 | Pre-Condition 4 `"Rear View Camera Delay"` 為基礎 label，而兩列只勾 Atl-Hi（Ram 系） | R-CAM5(e) | 改 `The "ParkView Backup Camera Delay" setting is set to "On"`；或改品牌中立句 `The camera delay setting is set to "On"`（不帶 label，因本列不操作該設定）—— 分析層建議後者，reasoning 註品牌 label 由 -007／-008 承接 |
| 5 | 007（含 008） | ER2 `The Settings screen is displayed` 缺引號 | §5.8(f)、§11 | `The "Settings" screen is displayed` |

其餘（002／005／006／008）依同樣規則自查：Full-Operation 行只用於已上電情境；品牌行只在操作品牌 label 之列出現。

## §三　待 Pei 裁

| # | 項 | 建議 |
|---|---|---|
| 1 | §二五項修正後，pilot **判過**，不跑 v3，直接進全量 | 建議准 |
| 2 | 全量批次切法（A 本 25 列） | 以 Test Set 為批：①Display Arbitration（-015/-016/-018/-020）②Startup and Shutdown ＋ Configuration ＋ State Handling ③LVDS Messaging ＋ Diagnostics ＋ Video Pipeline ④HMI Overlays；`-024`（DR-CAM-a）與 `-025`（DR-CAM-h）blocked 留尾批。每批 ≤ 40 TC，R-G14 三批乾淨後自動放行 |
| 3 | CFTS092 疊層同句之取錨規則（§二-1） | 立 R-CAM12：「CFTS092 於 Cargo／Rear／SVC／FFC 各節重複之同句，Rear View Camera 之 TC 一律取 Rear Camera 節（`SYS-RA-CAM-070`～`-082`）之 ObjectID」 |
| 4 | 逾 50 token 之摘句規則 | 不另立號，canon §4.3.1 既有；profile 加一行提醒並附本案例 |

## §四　CAM-05 之內容（准後由分析層發）

§二五項修正 → pilot02 原地改（不另開目錄）→ lint ＋ selfcheck → 上繳 diff；同包附全量批①之選列與 sibling 軸設計。

## §五　備查

- DR-CAM-a／f／g／h／i 五條待 Pei 送；全量交付前 f／i 須結。
- GC_BACKLOG 追加：R-TM13 之「同級錨自立以保 fenced sha」實作備註。
