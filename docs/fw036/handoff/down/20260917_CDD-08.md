# 下放包 CDD-08 —— Revise（R-DIAG20 常式位元組式／R-DIAG21 摘句）＋ batch 6（Layer 2 #1 SXM Module Version，57 列）

前件：`down/20260917_CDD-07.md`、`up/20260917_CDD-07.md`、`down/20260917_CDD-07_review.md`。
依常規：審閱後即發；依據 = CDD-07 審閱 §三／§四。執行層 session：沿用 CDD 線。
**與 GC-16 之先後**：不得並行；GC-16 先則指紋依 R-G76(b)，否則 R-G75。上繳註明。

---

## §0　禁區

- git 一律不動；push 依 CDD-06 審閱 §五-1，由 Pei 執行。
- 不動 `down/`、`up/`；不動其他 feature 目錄。
- **`rulings_hash.py` 只得 `--check`**；`--gate`／`--regen` 禁用（R-G77 候選）。
- **只產 batch 6 之 57 列**；Revise 不重編號；其餘列不得生成；framework 已 LOCK。
- 條文本體不改；R-DIAG20／R-DIAG21 逐字落。
- 升級條件表自本包 §5 逐字複製後逐項判定；命中者首段明記。
- 取 lint 紅數前先判 `Traceback`；子集／全集紅數不一致即停。

## §1　依據

`R-DIAG20`、`R-DIAG21` 條文逐字取自 `down/20260917_CDD-07_review.md` §四；R-G77 候選由 GC-16 落 ledger，本包只依其行事。
追認：`-266` 單條、retry 三碼各 3 條、DR-DIAG-8、`[A-DIAG45]` 還原。

## §2　任務

**T0 — 條文與指紋**：兩條落 `RULINGS.md`；`--check`（預期仍 FAIL，既存）；依制寫入並回報三數。

**T1 — Revise（Routine 母節 ＋ 摘句）→ `pilot01_v06`／`batch4_v02`／`batch5_v02`／`merged/v06`**
(a) R-DIAG20：grep 全合併本 Layer 3 母節 = Routine IDs 之 TC（預期 pilot 2 ＋ batch 4 14 ＋ batch 5 19 = 35 條）之 Procedure 位元組串；逐條表列：TC ID／原串／改後串／改由。至少：
   - `$ 22 03 12`／`$ 22 03 1B`／`$ 22 03 0A` 類 → `$ 31 03 03 12` 等（requestRoutineResults）；對應 ER `62 …` → `71 03 <RID> …`。
   - `-277` 步驟 2 `$ 31 03 12` → `$ 31 03 03 12`。
   - start 缺 option record 者，依 CFTS004 有無 option 判：有 → `<…>` 佔位登錄；無 → 三 bytes 後不加。
(b) `RT-DIAG` lint 實作 ＋ 5 正 5 反自測；七本複驗 0。
(c) R-DIAG21：`-345`～`-348`／`-354`～`-357` 上半摘句（逐字子句，≤ 50 tokens）；同時 grep 全合併本 `L` 紅 0；profile「閾值待 R-3」句改為「R-3 = 50，超限摘句」。
(d) 產生器守門加第三條：上半 > 50 tokens 不出檔。

**T2 — batch 6 sibling plan（先出後產）**

| Layer 2 | DID | 037 列 | 分析層預判（未逐列查 CFTS004，執行層查證為準）|
|---|---|---:|---|
| #1 SXM Module Version | `$2843` | 57（含 pilot `-272` → `-011`）| 19 源三段式，各為一個版本子欄位（SXI Rev Major／Minor／Incremental、RF Rev ×3、Module Type ID ×n、BB Rev ×n…）：**每列只追一子欄位**（`-272` 前例）；positive 讀 1、neg 2、uns 1；預估 19×4 − 1（pilot 已產）= 75 左右。注意 CDD-01 §3 曾見 `SYS-RA-DIAG-098`～`-100`（BB Rev）為 CFTS004 未收之 FR —— 確認 037 引用者與未引用者之界線，未引用者不產（R-DIAG8(a)）|

Region 全 NAFTA → `1 1 1 0 0 0 0`；Harman 依 tsv。子欄位之資料格式（ASCII／BCD／位元組數）CFTS004 明載者入 ER 用字（如 `-268` Radio ID 8 ASCII 前例），未載者 `<…>` 佔位 DR-DIAG-5。

**T3 — batch 6 產出** → `sandbox/batch6/diagnostics_batch6_v01.xlsx`／`batch6.json`／`coverage.tsv`；ID 自 `-358` 起；pilot `-011` 沿用；`-012` 留號（#2）。

**T4 — lint／自檢／coverage／合併本**：八本各跑（先判 Traceback；`LINT_STATUS` 若 GC-16 已落地則斷言之）；§9 17 項；R-SEC15 型（(c) 用詞界式）；coverage 57/57；`merged/diagnostics_v06.xlsx`；`placeholder_summary.tsv` 更新。

## §3　預期數字（分析層推算，執行層以查證數為準）

| 項 | 預期 |
|---|---|
| Revise T1(a) 命中 | 30–35 條（Routine 母節全數）；其中 `$ 22` 誤讀者 ≥ 10 |
| batch 6 TC | 57 列 → 70–80 |
| PENDING | 0（DR-DIAG-1 餘 2 列皆不在 #1）|
| lint | 真違規 0；`L`／`RT-DIAG` 0 |
| 合併本 | ≈ 430；覆蓋 19 組／318 列（81.7%）|

## §4　上繳要求（`up/20260917_CDD-08.md`）

1. 首段：升級條件逐項（自 §5 逐字複製）。2. T0 數字；指紋制。
3. T1(a) 逐條表；`RT-DIAG` 自測；摘句前後對照 8 條；守門第三條。
4. T2 sibling plan（含 CFTS004 子欄位錨表）。5. T3 TC 清單；貼入 **一條 Revise 後之常式 results TC（`-277` 或 `-282`）** ＋ **`$2843` 任一子欄位 TC** 全文。
6. T4 紅數歸因；自檢；coverage；§3 對表；自報錯誤；未結；建議 commit ＋ pathspec。

## §5　升級條件（命中必明記）

- 指紋集合不符（R-G75）／`--check` 不綠（既存者記「既存」不算命中）。
- T1(a) 命中 > 35 條（表示 Routine 母節以外亦有 `$ 22` 誤讀 RID，須擴大 grep 並回報）。
- 摘句後任一上半仍 > 50 tokens 或摘句改寫了原文字句。
- 任何需改 canon 之情形。
