# 下放包 SEC-17 —— 佔位列整列淡橘底色 → `security_v11`（六本，交付本）

前置：`up/20260917_SEC-16.md`、`down/20260917_SEC-16_review.md`。
Pei 2026-09-17 指示：含佔位（`<… provided by … (X-n)>`）之 TC **整列**以淡橘色標示後交付。
執行層 session：新開；**不跑 `--gate`、不動 RULINGS**（GC-16 前禁令）；不改任何 TC 文字。

---

## §0　禁區
- 只動儲存格**填色**；任何欄之文字、TC 序、封面、DV、zip 成員數不得變（`surgical_save`，前後 zip 成員數與 DV 計數相同）。
- 六本皆自 v10 JSON 重生後套色；v10 xlsx 不動，v11 新檔。
- 填色範圍 = `Test Case Specification 測試用例規範` sheet、該 TC 所在列、**自 A 欄到表頭最後一欄**（表頭寬度先量測，不得只塗到 Remarks）。

## §1　規則
```text
判準：TC 之 Pre-Condition／Input Test Data／Procedure／ER 任一欄含 regex `<[^<>]+provided by [^<>]+\(X-[a-z0-9-]+\)>`
      （即 `placeholder_summary.tsv` 之 tc_id 集合；兩者須相等，差異回報）。
顏色：淡橘 `FCE4D6`（Excel「橙色, 輔色 2, 較淺 80%」），solid fill。
不標：文件審查 TC（DOC）、`W=4` 之四段、A-SEC-16 路徑註記之 13 TC（Remarks 註不是佔位）。
```
先量測 SWC 0708／Home 0809 之資料列有無既有填色慣例（Counter of fill colors）；若交付本已用 `FCE4D6` 表他義，改用 `FBE5D6` 並回報。

## §2　實作
`build_delivery.py` 加 `--mark-placeholders`（預設關）；套色在 `surgical_save` 之後以 openpyxl 逐格 `PatternFill` 寫回、再存；驗證：文字逐格 diff v10↔v11 = 0、DV／zip 成員數相同、lint 六本門檻 0。
`SEC_VER=v11`；`delivery/` 六本同名覆蓋（v10 本移 `superseded/`）；`_all` 同套色。

## §3　產物
六本 + `_all`；`placeholder_rows.tsv`（`workbook | tc_id | row | tokens`）；`EXEC_GUIDE.md` §4 加一句 `Rows containing placeholders are shaded light orange (FCE4D6) in the delivery workbooks.`（此句非指令，不影響 ⊆）。

## §4　上繳要求（`up/20260917_SEC-17.md`）
1. 兩本交付本之填色慣例量測；最終色碼。
2. 六本各：塗色列數（預期 CP／KI／SAM／ECUC／SWDL／LOGENC 合計 = `placeholder_summary` 之 TC 數 41）、sha；文字 diff 0；DV／zip 計數前後。
3. lint 六本；`placeholder_rows.tsv` 計數。
4. RULINGS 兩檔 sha 前後（本包不動）。
5. 自報錯誤。

## §5　升級條件
- 文字 diff ≠ 0；DV／zip 計數改變；塗色 TC 集合 ≠ `placeholder_summary` 集合。
