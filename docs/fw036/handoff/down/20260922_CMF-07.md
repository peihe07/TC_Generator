# 下放包 CMF-07 — EMEA ICS 出範圍 ＋ `B5` 版號 → 交付本 v2

- 日期：2026-09-22
- 前置：`down/20260922_CMF-06_review.md`；Pei 2026-09-22：「可以的話不寫 EMEA 也沒問題，因為不含在販售的範圍內」。
- 產物落點：`features/comfort/sandbox/delivery/…_SWQT_ComfortHMI_20260922_v2.xlsx`。**不入 `delivered/`、不 commit、不跑 `--gate`／`--regen`、不重跑 generator。**
- 開工前 `git status`：CMF-06 產物是否已提交，照列。

## 1. 裁定 R-C68（分析層依 Pei 之語所取，Pei 可撤）

EMEA ICS 不在本交付之販售範圍。**列保留、不刪**（R-C46：既有 F／E 不重編，TestRail ID 不丟；canon「每需求必有 TC 輸出」）；處置為：

- 帶正向 EMEA PC（`The vehicle is an EMEA ICS vehicle, whose climate interface is specified in chapter 16`）之 **119 條**：V／Y／Z 全 0（判準機械化：PC 含該句）。
- 依 R-C63，不加 Remarks；車型欄即為該資訊。
- `coverage.tsv` 加欄 `out_of_sales_scope`，該 119 條標 `EMEA`；`rd_feedback.md` 加第七項：EMEA ICS（chapter 16）之 SWE1 需求不在販售範圍，建議 037 標註。
- 307 條否定式 EMEA PC 不動。

**若 Pei 之「不寫」意為刪列**：上繳包報刪列之代價（119 個 TestRail ID 作廢、119 leaf 失覆蓋須改為 Out of Scope 佔列、F 欄不連續），不執行，待 Pei 二選一。

## 2. 其他

- `Product Document!B5`：`Initial Release` → `V1.1`。
- `vm_assign.tsv` 對應列分支改 `R-C68`；R-C67 註記劃線保留。
- 預期：三台皆 0 者 65 → 177；對 v1 交付本差異 ＝ 229 格（車型欄）＋ 1 格（`B5`），逐格歸因。

## 3. 上繳首段

對交付本 v1 差異格數與歸因、三台 `1` 條數與全 0 條數、lint `LINT_STATUS: OK`、sha256、ENTRY（type `delivery-candidate`）。
