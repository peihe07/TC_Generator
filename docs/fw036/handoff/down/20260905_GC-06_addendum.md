# 下放包 GC-06 補遺 —— 裁（2026-09-05）

Pei「裁」（2026-09-05）：GC-06 §三 四項授權分析層裁。裁如下，執行層依本補遺合併執行 GC-06。

## 1　`canon_refs.py` 索引面增台帳 —— **准，隨包執行**（原任務 1-4 由提案改為施作）
- 索引來源增 `docs/fw036/RULINGS_LEDGER.md`，錨點正則與 `rulings_hash` **共用同一常數**（`### R-Gnn` 領頭；R-G43(c)），不得各寫一份。
- 同號兩側皆有錨（`R-G42`、`R-G4`、`R-G7`）者：解析為 **resolved**，不判 ambiguous——條號唯一即已可解析，本體差異由 `rulings_hash` 管。
- 對照向：改前／改後 `unresolved`／`ambiguous`／`mention` 三數與 waiver 命中數逐項回報；`tests/test_canon_refs.py` 加一夾具：台帳錨點可解析、FO 無該號時不紅。
- 改號後失效之 waiver 列（任務 1-3）：**列出不改**，Pei 於審閱後依 R-G57「只減不增」整批減。

## 2　driver_distraction 二檔 —— **`git mv` 至 `sandbox/`（Pei 專門 commit）**
除非任務 3-a 量到該線 `feature.yaml`／profile 已宣告 `[OVERRIDE R-G64] workbook/`；有宣告則不搬，改 `lint_paths` 讀 profile override（另包）。無宣告即無例外。執行層出 `git mv` 兩行（含 `_bak`）供 Pei 跑。

## 3　交付本 sha 不符（ics_management）／未登記（sw_update）—— 先量，量後依下列固定處置
- **檔被改**（`git log` 顯示登記後有 commit 動到該 xlsx）：交付物不可回溯修改（R-TM13）。處置：登 A-ICS 條，**對照表改指現行 sha**（交付物已出，表須反映實物），並於該線 RULINGS 記「登記後變更，變更 commit `<h>`，內容差異 `<diff 摘要>`」；不還原檔。
- **表登錯**（檔自登記後無變動）：改表，登 A 條（人寫之 sha）。
- **未登記**（sw_update）：補登現行 sha，登 A-SU 條；並查該線交付紀錄是否另有 sha 可對，不符者依上款。
三者之改表由執行層做，A 條由執行層落（自報形態），RULINGS 註由執行層抄本裁決。

## 4　`?6` 未追蹤 —— GC-06 上繳後一併 commit（審閱給命令）。

本補遺之裁決入台帳：**不立新 R-G**，以 R-G43(d) 記「解析面與指紋面同源（`canon_refs` 與 `rulings_hash` 共用錨點常數）」，餘為個案處置不入台帳。
