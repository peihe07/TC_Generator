# 下放包 GC-12 —— Revise-M 三本產出、八本 P/Y 對照向、FO 接手清單、DR 狀態欄（2026-09-05）

**指派：執行層 session B**。Pei「審閱結束」（2026-09-05）：GC-11 審閱（`down/20260905_GC-11_review.md`，22:03:34）全數生效，R-G72(g) 入台帳。本包為該審閱 §四 之逐字展開。
**xlsx 只寫 `_Revise1` 新檔，母本一位元不動；不跑會寫 repo 之產生器（`rulings_hash`／`gates_tsv`／`selfcheck_map` 由 Pei 跑）。**

## 一、先量
1-1　`git status --short | sort`、`git log --oneline -3`；`RULINGS.sha.tsv` 是否已含 R-G72(g)（Pei 是否已重生）。未含者：本包不動台帳條文本體，只在 R-G72 條下加 (g) 全文（GC-11 審閱 §二-2 逐字），乾跑 `rulings_hash --out <工作區>` 證入表。
1-2　三本 Revise-M 母本 sha（`features/power/delivered/pm_73.xlsx` `cedb6b84`、ics_management 0830 `cb5697db`、vsm_v42 0902 `abc7f8ae`）現查相符；`delivered/MANIFEST.tsv` 三線現行列逐字。
1-3　`rg72_dryrun_<slug>_20260905.tsv` 三表之 `mechanical=Y` 列數（預期 33／14／10）現查。

## 二、任務

### 1　Revise-M 三本
對 pm_73、ics_management、vsm_v42：
- 以母本為底，只套用 dry-run 表 `mechanical=Y` 之 `old_text→new_text`（rule ∈ send／recv／buserror／PROXI-old），逐格替換；`test_item` 欄不動；其他任何欄不動。
- 寫入 `features/<f>/delivered/<母本檔名去 .xlsx>_Revise1.xlsx`。保留資料驗證者用 surgical_save（丙″）；保護結果欄 Y–AH 逐格斷言等於母本。
- idmap 填 `new_tc_id`：TC ID 不變者填同號（Revise-M 不改 ID，只改內容），`changed_cols` 為實際替換欄；`reason` 為 rule 集合。
- 全閘重過（R-G72(e)）：`lint036 --profile <f>` 含 X／Y／P，`gate_all` 唯讀；報告落 `features/<f>/reports/`。P 預期降至 0 或只餘 bare（bare 屬 Revise-C，列數與 dry-run 之 bare 數相等即合格）。
- `MANIFEST.tsv` 加一列指前版（`supersedes = <母本檔名>`，`reason = R-G72(g) Revise-M`）；該線 `RULINGS.md` 記「母本被 Revise1 取代，內容差異＝<Y 列數> 格機械轉換，母本原封」。
- 每本附：替換格數／涉及列數／母本 sha／新檔 sha／Y–AH 斷言結果。

### 2　八本基線 P／Y 對照向
既有八本（列表以 `docs/runtime/GATES.tsv` 或基線報告目錄現查）：改前＝`git show HEAD~:scripts/lint036.py` 落工作區後逐本跑；改後＝現行。表：`本 / sha8 / rows / P_before / P_after / Y_after / 差 / 差之歸因（v3 分支移除／PROXI 舊式改 WARN／其他）`。只回數字。

### 3　FO 接手清單（R-G71）
`FEATURE_ONBOARDING.md` 接手清單節（先量位置）加二項，`[ADD] R-G71` 標記：
- 新 feature 之 profile 一律啟用 lint X／Y／P(v4)。
- IN §5.3 之 PENDING 二常數（`ENTER_VEHICLE_SETTINGS` 第一 hop、`ENTER_HOME_SCREEN`）於新 feature 之 `DATA_REQUESTS.md` 承接登記；不得複製 PENDING 而不登 DR。

### 4　`DATA_REQUESTS.md` 狀態欄提案（不回填）
`docs/fw036/templates/`（無則 FO 相應節）定格式：每 `## DR-n` 節首行 `status: open|sent|closed（YYYY-MM-DD）`。十個有 DR 之 feature 各只加一段標頭說明。附「可機讀節數／總節數」分母（現況預期 0／N）。

### 5　pm_29 二表差 −101
`rg70_pm29_dryrun_20260905.tsv`（685）vs `_v41.tsv`（584）逐列 join（`row/col/old_text`），出：只在舊表／只在新表／兩表皆有但 new_text 異，各計數並各舉一例。舊表加 README 註「被 v41 取代，差異見 GC-12 上繳 §5」。

### 6　ANOMALIES
登 GC-11 審閱 §一之覆寫條（A 線 21:54 同名覆寫 `down/20260905_GC-11.md`，PROTOCOL 增句之後發生；形態同 A-GC18）；併記本線「未讀 `lint036.py:932` 即斷言判準」之誤。次一未佔號現查。

### 7　上繳 `up/20260905_GC-12.md`
首行 session 標識與觀察依據；一 之量測；三本 Revise-M 各項數字與 sha；八本對照表；FO／DR 格式 diff；§5 三計數；工具旗標逐次聲明；自報。

## 三、Pei 動作（本包後）
1. `rulings_hash.py` 重生 → `--check` → commit（訊息列 GC-10/11/12）。
2. 三本 Revise1 抽驗（各抽 3 列對 dry-run 表）→ 「出貨」。
3. `driver_distraction` `git mv`；DR 草稿取號送出。

## 四、未結 DR
DR-PW25、DR-VS{n}（Power_Tailgate_Enable）、HMI entry path 草稿 ×3；bare-$ 值查詢待 Revise-C 包逐處登。
