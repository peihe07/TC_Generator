# 下放包 CDD-12 —— `forms/CIP_Radio_Tables_v6.7.xlsx` 登錄與補產（DR-DIAG-8 → PARTIAL）＋ 交付本 v02

前件：`down/20260917_CDD-11_review.md`。Pei 2026-09-17 指出 `forms/CIP_Radio_Tables_v6.7.xlsx` 已於 9/16 放置；
分析層於 MCP 中斷前已複製並解析（見 §1）。執行層 session：沿用 CDD 線。指紋制依 GC-16 是否落地。

---

## §0　禁區

- git 一律不動；`forms/*.xlsx` 不入 git（既有政策），FORMS.md／MANIFEST 入。
- 不動 `down/`、`up/`；不動其他 feature 目錄。
- `rulings_hash.py` 只得 `--check`／`--out <scratch>`。
- 流程圖只**轉錄**（逐節點文字，附圖檔 sha 與節點座標序號），不改寫語意；不得由流程圖推算 CFTS004 未載之數值。
- 交付本 v02 依 R-G72 新版號；v01 不動。
- 升級條件表自 §5 逐字複製；命中者首段明記。

## §1　分析層已測事實（執行層複驗）

| 項 | 值 |
|---|---|
| 檔 | `forms/CIP_Radio_Tables_v6.7.xlsx`，5,009,350 bytes，mtime 2026-09-16 09:24，sha16 **`71f5ea4043992475`** |
| 分頁 | 12：`History`／`SEEK Cancel_Stop Transitions`／`TA-PTY31 station list cancel e`／`PI Seek Ordering`／`Default ROW Market Presets`／`Preset Defaults- R1`／`Preset Defaults- VP3&4`／`Predefined Presets -X65 chip`／**`MPFA Validate Process(X65, X40)`**／**`MPFA Select Process(X65, X40)`**／`Travel Link- Weather Icons`／`Sheet1` |
| 版次 | `History` 末列 `v6.7`，2023-03-03，Marcio Luz，`SR24, SR25` |
| MPFA 兩分頁內容 | 各一句 Note ＋ **Visio 流程圖**（`xl/media/image3.emf` Validate、`xl/media/image4.emf` Select；Select 另有 `xl/embeddings/Microsoft_Visio_2003-2010_Drawing.vsd`），標題 `MPFA VALIDATE PROCESS / MPFA SELECT PROCESS — Version 1.2 – Edited by Jim Witek` |
| 流程圖要點（分析層目視轉錄，執行層以 LibreOffice 轉 PNG 逐節點複核）| Retry Flag = 0 → 依 vehicle sales codes 決定 PkgIndex → TX PkgCmd（Validate：TransactionID 0x02／Option 0x02；Select：0x01／0x01）→ 600 ms 內收 PkgInd？否 → Retry Flag < 3 則 Retry++ 重送；是 → IndCode = 0x03？是 → retry；0x0D／0x0F？是 → retry；0x0C／0x0E → 回 IndCode；Validate：0x10 → 回 IndCode 並進 Select；其餘 → 回 IndCode。Select：0x00 → 存 OpCode（PkgInd／TransactionID／IndCode／RadioID／Option／ArrayHash／PkgMAC／BLante／BLpost／BLdisp／OLante／OLpost／OLdisp UPC）→ 回 IndCode → 進 CA Pellet Creation。Retry 耗盡 → 回 $FF 或 IndCode，依 NOTE：3 次 600 ms timeout → $FF；3 次同 0x03 → 0x03；3 次同 0x0D → 0x0D；3 次同 0x0F → 0x0F；混合 → $FF |
| **不含** | DID 位元組版面、controlOptionRecord／controlEnableMask、不支援 SID/DID 清單、sales code → PkgIndex 對照、`SX-9830-0095` 本文 |

分析層 **A38**：該檔 9/16 已在 `forms/`，CDD-01 T0 只以 sha 比對 MANIFEST，未掃 `forms/` 檔名對規格引用之外部文件名（CFTS004 明引「CIP Radio Tables」），致 DR-DIAG-8 於 CDD-07 誤開為缺件。

## §2　裁定（分析層依常規落字）

```text
R-G78（候選，GC-16 併落）  intake 必掃 forms/
  feature intake 之來源盤點除 sha 比對 MANIFEST 外，須列 `forms/` 全部檔名，
  與規格（CFTS／037／SYSAD）明引之外部文件名逐一比對；命中者登 FORMS.md 六欄 ＋ MANIFEST 後才得判「缺件」。
```

```text
R-DIAG28  CIP Radio Tables v6.7 為 $0307／$0309 之流程來源
  (a) `forms/CIP_Radio_Tables_v6.7.xlsx` 登 FORMS.md（六欄）與 MANIFEST（doc_id `cip_radio_tables_v6_7`，features `diagnostics`）。
  (b) MPFA Validate／Select 兩流程圖轉錄為 `features/diagnostics/data/cip_mpfa_flowcharts.md`（逐節點文字＋NOTE 逐字），
      錨記法 `CIP_Radio_Tables_v6.7:MPFA Validate Process`／`…:MPFA Select Process`，於 Remarks 引用；
      specification_reference 維持 CFTS004 ObjectID（CFTS004-4940451/4940462 等已明引該流程）。
  (c) retry 軸之 sibling 依流程圖 NOTE 五種結局各一條：600 ms timeout ×3 → $FF；0x03 ×3 → 0x03；0x0D ×3 → 0x0D；
      0x0F ×3 → 0x0F；混合 → $FF。PC 寫 X65 之回應態（如 `The Sirius XM chipset returns Indication Code 0x03 to each package validate command`），
      不寫建立步驟（§8.5 例外）。
  (d) `<Package Indication value>` 佔位維持（sales code → PkgIndex 對照不在本檔）；DR-DIAG-8 改 PARTIAL：流程圖已到，
      餘 `SX-9830-0095` 本文與 PkgIndex 對照表。
```

## §3　任務

**T0**：條文落檔；指紋依制；FORMS.md 六欄（(b) 涵蓋範圍以實測填：12 分頁、兩張流程圖之圖檔 sha、Visio 嵌入 1）；MANIFEST 一列（`git diff --stat` 覆核 1 insertion）；R-G67 嵌入物件回報（png 28、emf 3、vsd 1）。

**T1 — 流程圖轉錄**：以 `soffice --headless --convert-to png` 轉 `image3.emf`／`image4.emf`，逐節點轉錄（節點序號、形狀、文字逐字、出邊 Yes/No 指向），NOTE 五行逐字；兩圖差異表（TransactionID/Option、0x10 分支、0x00 分支）。**不得補流程圖未畫之節點。**

**T2 — Revise batch 5 `$0307`／`$0309`（19 條）→ `batch5_v04`／`merged/v09`**
(a) 現有 retry 三碼各 3 條（CDD-07）依 (c) 改寫 PC 為流程圖 NOTE 之態；補 600 ms timeout ×3 → $FF 與混合 → $FF 兩條（若 CDD-07 之「differing／timeout → $FF」單條已存在，拆為二）；每列上限 8（R-DIAG25(c)）。
(b) Validate 之 0x10 → 進 Select、Select 之 0x00 → CA Pellet 建立：ER 只斷言本常式之 results 碼（`71 03 03 0x <code>`），流程後續（CA Pellet）不在本列範圍（§8.2.1），Remarks 註流程圖錨。
(c) PC-3 security 明文；`<Package Indication value>` 佔位保留並登錄。
(d) sibling plan 先出後改；逐條表附 tsv。

**T3 — 其餘分頁之適用性一句話盤點**：`Predefined Presets`／`Preset Defaults`／`SEEK`／`PI Seek`／`Travel Link` 對 389 列有無引用（預期 0），記於 `cip_mpfa_flowcharts.md` 末段；不產 TC。

**T4 — DR／FB 更新**：DR-DIAG-8 → PARTIAL（餘二項）；`asset_request.md` 同步；FB-DIAG-r 改述（流程圖已自 CIP Radio Tables 取得，餘 SX-9830-0095）。

**T5 — 交付本 v02**：`…_SWQT_Diagnostics_20260917_v02.xlsx`（R-G72；或依 Pei 慣例改日期戳，執行層沿 Security 之 v-序）；id_map 更新；著色重跑；逐格對帳 vs `merged/v09` 只差 ID／列序；lint／§9／R-SEC15 型；`placeholder_summary.tsv` 更新（預期 DR-DIAG-8 行數不減，token 現況改 PARTIAL）。

## §4　預期數字

| 項 | 預期 |
|---|---|
| 轉錄節點 | Validate ≈ 20、Select ≈ 20；NOTE 5 行 |
| batch 5 Revise | 19 條中 retry 相關 ≈ 12 條改 PC；新增 ≤ 4 條 |
| 合併本 | 556 → 556–560 |
| lint | 真違規 0 |
| MANIFEST | +1；FORMS.md +1 條目 |

## §5　升級條件（命中必明記）

- 指紋集合不符／`--check` 不綠（既存者記「既存」）。
- 流程圖轉錄與 CFTS004-4940465／4940466／4940454／4940455 之敘述矛盾（如 retry 次數非 3、碼值集合不同）—— 回報不改，Pei 裁。
- 任一 SWE1 列 > 8 TC。
- 任何需改 canon 之情形。

## §6　上繳要求（`up/20260917_CDD-12.md`）

首段升級條件逐字；T0 六欄與 sha；T1 轉錄檔摘要 ＋ 兩圖差異表；T2 sibling plan／逐條表／貼入 **timeout×3 → $FF 一條** ＋ **0x03×3 → 0x03 一條** 全文；T3 盤點；T4 現況；T5 對帳與路徑；§4 對表；自報錯誤；未結；建議 commit ＋ pathspec。
