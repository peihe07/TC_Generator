# 下放包 CAM-09 —— CAM-08 審閱落檔 ＋ batch02c（`-003`，23）＋ batch03 選列計畫

指派執行層 session：延用。對應審閱：`down/20260923_CAM-08_review.md`（同發）。R-G14 三批乾淨已達，自本包起批次審閱改為「lint＋selfcheck 全綠 ＋ 分析層抽讀 ≤ 4 列」。

---

## §0　禁區

同 CAM-03 §0。本包觸及：五既有目錄之修正、`batch02c/` 新建、`batch03_plan.tsv`。不寫回工作簿。

## §1　落檔

1. DECISIONS 6-23～6-27：升級條件以列計；`Body_Types` 不寫 raw；`-091` 現狀；`-088` 不拆；`-082` 接受。
2. **DR-CAM-n**：Atl-Hi PROXI `Body_Types`（byte 231）列舉。
3. profile §7 加「訊號 raw 之查證順序：先以訊號名查 DBC `VAL_`，再定 `<raw> (<label>)`」；§4 加「開機類 TC 之判別：步 1 為 `CmdIgnSts = RUN` 者首行必為 Standby」。
4. selfcheck：第 3 項改雙向（重複 ∧ 矛盾）；第 7 項 `Cycle the ignition`／`Turn the ignition`／`Power on the HU` 等非 CAN 式點火動作。
5. `-073`／`-074` PROXI `Body_Types` 改 label 式；`-073`／`-085` 步 1 改 CAN 式、`-073` 首行改 Standby；五目錄以第 3（雙向）／7 項掃描並修正，`DIFF_CAM08_to_CAM09.md`。

## §2　batch02c：`SWE-CAM-003`（23），ID 續 `-095`

依 `batch02_plan.tsv`（合一後 23）。要點：
- 狀態機（Automatic／Manual／Off）之轉換只取 `-003` 獨有錨（與 batch01 重疊者已委派）。
- `V4-117`（ISO 15008 字型／字高）之量測 ER 以來源數值書寫；量測手段若來源未載則 `Read the on-screen text and check that …` 以人工量測，不造工具。
- `V2-535`（VF664）PENDING DR-CAM-k。
- `V33-222`／`-224`（動作側）已合一歸 `-001`，本批不出，plan 記委派。
- R-CAM3(f)／R-CAM15 適用。

## §3　batch03 選列計畫（只計畫不生成）

Test Set `LVDS Messaging`（`-007`～`-011`）＋ `Diagnostics`（`-004`／`-013`）＋ `Video Pipeline`（`-006`／`-019`）。
`batch03_plan.tsv` 同格式；每 SWE 列 in-scope／排除／驗證點／TC 數。預期難點回報：
- LVDS 訊息之觀察全以 bus analyzer（A-CA27 之結論），值語意自 SYS2；`vehicleUpdate_*` 之 raw 若 SYS2 未載則 label 逐字。
- `-004`／`-009` 共引之 32 個 Out of Scope 不計；DTC 之讀取以 `$ adb shell` 或 UDS 工具 —— 交付語料（Diagnostics 本）有 `$` 例者抄之，回報列號。
- `-006`（EVS HAL）與 `-019`（FMVSS 串流終止）之可觀察面。
若合計 > 40，回報切法。

## §4　上繳要求（`up/20260923_CAM-09.md`）

1. 落檔清單；selfcheck 第 3（雙向）／7 項之命中清單與修正 diff。
2. batch02c 23 列產物、lint、selfcheck、PENDING。
3. `batch03_plan.tsv` 全表 ＋ 難點回報 ＋ 切法。
4. 自報錯誤；未結。

## §5　升級條件

- selfcheck 第 3／7 項命中 > 20 相異 TC 列。
- batch02c > 30。
- `-003` 任一驗證點之來源全被委派（回報，不硬寫）。
