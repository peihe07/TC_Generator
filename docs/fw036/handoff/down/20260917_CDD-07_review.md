# CDD-07 審閱 —— R-DIAG19／DR-DIAG-7 ＋ batch 5

審閱對象：`up/20260917_CDD-07.md`。審閱層 session：2026-09-17。
**結論：通過。** batch 5 = 67 TC，合併本 357 列（67.4%），18/20 組全產；lint 真違規 0；§9 17/17。
升級條件兩項命中且首段明記 —— 處置皆正確。**兩處內容修正 ＋ 一處越界缺陷之 Revise 併入 CDD-08**（§三）。

---

## 一　逐項核對

| up 節 | 判 | 備註 |
|---|---|---|
| 首段-1 `--check` FAIL 為既存（stash 複驗）| 通過 | 正確不重生；GC-16 之標的 |
| 首段-2 `-266` 錨數 1 → 單條、ER 直接列舉四態不佔位 | **追認** | 「缺的是如何進入某態，不是態的值」—— 判斷正確；四態列舉為逐字明載 |
| §1 CDD-06 上繳 §6.1 四項條件誤植之更正 | 收 | 「升級條件表自該包 §5 逐字複製」立為常規（§四）|
| §2.2 Indication Code 全表見 CFTS004-4940456 → 不佔位 | 通過 | 下放包預判錯，執行層查證勝（A22）|
| §2.2 retry 三碼各拆 3 條、`$FF` 單條 | 通過 | 依 CFTS004 逐字，≤ 6 |
| §2.2 DR-DIAG-8 流程圖缺件 | 通過 | 與 R-DIAG19 同法 |
| §2.2 `$2847` 首次 `0x2E` 之 PC-3 為 SYSAD 明文 | 通過 | `pc_sources` 字串區辨 |
| §3.1 `L=8`（52 tokens verbatim）判「真違規 0」 | **不採** | 見 §三-2 |
| §5-1 `--gate` 誤用致全表覆為 856 列，已還原 | 收（實質越界） | `[A-DIAG45]`；profile 立規：GC-16 以外**任何包不得跑 `--gate`／`--regen`**（§四）|
| §5-2 自訂 key 重排全檔，已還原 | 收 | 「`git diff --stat` 先對量」入 profile |
| §6.1 `[A-DIAG47]` batch 4 常式結果以 `$ 22` 讀、`-277` 缺 sub-function | **重大，Revise**（§三-1）| 越界未處置正確；審閱層漏審（A24）|
| §6.1 `[A-DIAG46]` `-347`/`-348` Title KeySense 而來源在 `$0309` | 通過 | FB-q |

## 二　分析層自報錯誤

| # | 內容 |
|---|---|
| A22 | CDD-07 §2 預判 Indication Code 需佔位、`-266` 狀態軸四錨 —— 皆未先查 CFTS004 即預判 |
| A23 | CDD-07 §3 預期 PENDING 0，未查 `-237`（row 244，`$2847`）在本批 |
| A24 | **CDD-06 審閱只讀貼入之 `-232`／`-281` 兩條，未 grep 常式母節 TC 之讀取式**；`$ 22` 讀常式結果之系統性錯誤（`-277`／`-282` 等）漏審。審閱抽讀不足以驗「同型多條」，應要求執行層附**型態 grep** |
| A25 | CDD-06 §3 預期 R-DIAG18 命中 10–20，實 148（已記 A20/A21，此為同源）|

## 三　內容修正（併入 CDD-08）

**(1) `[A-DIAG47]` 常式結果讀取式** —— 母體：Layer 3 母節 = Routine IDs 之全部 TC（`$0312`／`$031B`／`$030A`／`$0307`／`$0309`，含 pilot `-009`/`-010`、batch 4、batch 5）。
規則：常式結果一律 `$ 31 03 <RID 2 bytes>`（requestRoutineResults），**不得以 `$ 22 <RID>` 讀**（RID 非 DID）；start 為 `$ 31 01 <RID> [<option>]`、stop 為 `$ 31 02 <RID>`；正回應 `71 0x <RID> …`。
執行層 grep 全合併本 Routine 母節 TC 之 `$ 22 03 xx`／`$ 22 31 xx`／`$ 31 03 xx`（3 bytes）並逐條列出修法；`-277` 補 sub-function。
lint 新增 `RT-DIAG`：Routine 母節 TC 之 Procedure 含 `$ 22 0[3]` 起首之 3-byte 讀，或 `$ 31 0[123]` 後不足 2 bytes → FAIL。

**(2) `L=8` —— 上半 52 tokens 非「不可改」。** canon §4.3.1：上半 token 上限 50（R-3），**超限須摘句**（取與括號下半測試目的直接相關之句／子句），全文以 `specification_reference` 指回。
`-345`～`-348`／`-354`～`-357` 之上半改為摘句（如取「If the three retries all return the same Indication Code, the HU shall report that code」之子句，逐字、不改寫），token ≤ 50；`L` 複驗 0。
執行層引 profile「閾值待 R-3」為不改之由 —— profile 該句已被 canon §4.3.1 明文之 50 取代，profile 同步更正。

**(3) `-266` ER**：維持單條；ER 末加 `, matching the current subscription state of the HU`？—— **不加**（無來源可判定當前態，加即不可觀察）。維持執行層寫法。

## 四　審閱裁定（依常規即生效）

```text
R-DIAG20  常式（RoutineControl）之位元組式
  start `31 01 <RID>` [＋ routineControlOptionRecord]、stop `31 02 <RID>`、results `31 03 <RID>`；
  正回應 `71 <sub> <RID> …`。RID 不得以 0x22 讀。lint RT-DIAG 於 Routine 母節 TC 施檢。
```

```text
R-DIAG21  上半摘句（canon §4.3.1 R-3 之本 feature 適用）
  test_item 上半 > 50 tokens 者，摘與下半測試目的直接相關之句或子句（逐字，不改寫），
  其餘以 specification_reference 指回；lint L 不豁免。
```

```text
R-G77（候選，GC-16 併落）  寫入型工具之禁用範圍
  `rulings_hash.py --gate`／`--regen` 只得於 GC 包執行；feature 包一律 `--check` 或 `--out <scratch>`。
  升級條件表自下放包 §5 逐字複製後逐項判定；命中者於上繳包首段明記。
```

追認：`-266` 單條不佔位、retry 三碼各 3 條、DR-DIAG-8、`[A-DIAG45]` 還原處置。

## 五　待 Pei

1. push 依 CDD-06 審閱 §五-1（rebase，不強推）。
2. GC-16：兩線皆已無新包在跑（Security SEC-17 為最後）；**請關 Security session → push → GC-16 下放**（併讀 CDD-06 審閱 §三 ＋ 本審閱 §四 R-G77 候選）。
3. FB-DIAG-a～r（18 項）、`asset_request.md` 五項（DR-DIAG-4/5/6/7/8）送出。
4. commit：建議准，訊息沿 up §7，pathspec 依其所列。

## 六　CDD-08 隨本審閱落檔

= §三 (1)(2) Revise（Routine 母節全 TC ＋ 8 條摘句）＋ R-DIAG20/21 ＋ `RT-DIAG` lint ＋ **batch 6 = Layer 2 #1 SXM Module Version（57 列，含 pilot `-011`）**。
