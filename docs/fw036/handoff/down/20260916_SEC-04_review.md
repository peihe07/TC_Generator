# SEC-04 審閱 —— 上繳包 `up/20260916_SEC-04.md`（batch 1 正式本 `security_batch1_v01.xlsx`，72 TC）

審閱層：分析層，2026-09-16。
判定：**通過**；升級項（framework Layer 2 含 CJK）**成立且成因在分析層**，於 SEC-05 以 R-G72 Revise 解鎖修正；其餘四項不成立。
執行層對升級項之處置（保留 LOCKED 名稱照寫、`K=10` 如實回報、不自裁）**正確**。

---

## 一、通過項

| 項 | 判 |
|---|---|
| R-SEC16／R-SEC15(f) amend 落檔、`--gate`、R-SEC1~15 不變 | ✅ |
| lint `SC` 第 4 判項散文化；pilot02 重跑 0→2 逐筆即審閱所指；修後 0；三路徑回歸 byte 相同 | ✅ |
| 38 列集合相等；`sibling_plan.tsv` 72（55~85 內）；差 +9 之成因逐列具名（規則 3 對介面列之負向必配）| ✅ 准；預估之 −9 為分析層低估，登 A-13 |
| 正式本 72 TC；`P`／`X`／`SC`／`SS` 0；`selfcheck` 九條 0；pilot01 反證 66 不變 | ✅ |
| PENDING 85 行／10 token 全登 EXEC_ASSETS（9→14 項）| ✅ |
| `W=4` 負向路徑無數值 | ✅ 判可 |
| pilot02 四修落於正式本；ID 對照表 | ✅ |
| `sibling_plan.tsv` 增 `rule` 欄 | ✅ 准（利覆核）|
| selfcheck (b) 納 `PENDING:.*\+ trigger` 為觸發位；(f) 改「Description 逐字子字串」 | ✅ 准 |
| `UI` 首例未產（Dealer App 金鑰更新操作素材無）→ `PENDING: X-k` | ✅ 正確處置 |
| shell 引號誤判之回歸 ✗ 自報 | ✅ 非程式問題 |

## 二、執行層延伸 —— 准（R-SEC7(c) amend，SEC-05 落檔）

`SEC_OBSERVE` 收入：`adb pull` 之 `"1 file pulled"`（歸 `RC`）、`The log buffer is cleared`（歸 `RC`）、`adb shell procrank|df|ps` 之 stdout（歸 `FILE`）。三者皆有素材依據（審閱指定修法／037 verbatim 取樣指令）。條文改為「七類為**觀察面之類別**，各類之具體句式由 profile §3 常數表列舉並可增列，增列須附素材出處」。

## 三、升級項 —— framework Layer 2 名稱含 CJK（成因：分析層）

**A-12（分析層錯誤）**：SEC-01 §5 之 Layer 2 表把五個未命名群以中文註記 `(非功能)`／`(環境)`／`(其餘)`／`(總則)`／`(IPC／JNI／IO)` 佔位，SEC-02 §4 令「30 組逐字寫入」而未先命名，LOCKED 時把佔位鎖了進去。違 IN §1（Test Set 英文）與 §4.2（不得 placeholder）。

**裁（Pei 准則 SEC-05 落檔，R-G72 Revise 解鎖 framework）**：走執行層方向 1（補英文名），但名稱須為**能力**而非分類（§4.2 禁 `Misc`／`General`），逐群定名：

| Test Group | 佔位 | 含列 | **新 Test Set** | 理由 |
|---|---|---|---|---|
| Cert Provider | `(非功能)` | CP-006（Android.bp／log 政策／START_STICKY／安全掃描）、CP-010（stress）| `Service Robustness` | 兩列皆為服務層之健壯性／建置合規 |
| Key Install | `(非功能)` | KI-012（SELinux）、KI-013（Qualcomm HTA）| `Platform Compliance` | 平台安全機制之合規 |
| SAM | `(環境)` | SAM-0001／0002／0008／0019 | `Service Environment` | 執行環境與部署前提（執行層以 037 原文複核四列是否皆屬此義；不合者回報）|
| ECU Cert | `(IPC／JNI／IO)` | R14~R17（Binder×2／JNI／Storage IO）| `Internal Interfaces` | 元件內部介面 |
| SWDL Secure Lib | `(總則)` | SWDL-001 | `Library Scope` | 總則性需求 |
| Log Encrypt | `(其餘)` | LOGENC-001~005／007~009 | `Encryption Services` | 加密服務族 |

排除方向 2（併入既有 Test Set 會使 §4.2「同 Test Set 同 setup」失效）與方向 3（中文仍進交付本）。

## 四、交付狀態之提醒（給 Pei，非執行層）

`security_batch1_v01.xlsx` 含 PENDING 85 行（29 TC）。IN §8.4.3：**含 PENDING 之工作簿不得出貨**，交付前須全數結案或由 Pei 裁定降轉 NA。現況三條路：
1. 送資產（`pending_summary.tsv` 之 10 token，量最大者 `X-f-2` 18 行／`X-i` 16 行／`X-h` 15 行）後 Revise 補值。
2. 以「RD review 草稿」名義給 Steven／Samuel 看 PENDING 位置本身（PENDING 即是對 RD 的問題清單），**不算出貨**。
3. Pei 逐 token 裁降轉。
分析層建議 1＋2 並行，`X-i`（原始碼與建置環境，全在 P2 的 CP-006／KI-012/013）可考慮直接裁降 NA。

## 五、SEC-05 預告（Pei 准 §三 後下放）

1. R-G72 Revise：`framework.md` 解鎖 → 六個 Test Set 改英文名 → 重新 LOCKED（`v02`）；`layer2_assign.tsv`／`feature.yaml` 同步；R-SEC17（Test Set 定名）＋ R-SEC7(c) amend 落檔。
2. `gen_batch1.py` 重跑 → `security_batch1_v02.xlsx`，預期 `K=0`，其餘計數不變（72／85）。
3. **batch 2 產出**（21 列中之產出群 13 列：Testing 11 ＋ CONVERT 2）→ `security_batch2_v01.xlsx`；含 `CAN` 首例（SAM-0013／0017，VHAL Guide §5 之 `CmdIgnSts`／`PowerModeSts`，Atl-Hi／Atl-Mi 拆 sibling，Vehicle Model 各勾）。
4. 合併本 `security_v01.xlsx`（batch 1＋2，51 列產出群全覆蓋；DEFERRED 19 列不入）。
