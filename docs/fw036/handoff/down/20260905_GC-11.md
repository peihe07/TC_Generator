# 下放包 GC-11 —— R-G72 回修：五本候選之轉換 dry-run、GC-10 收尾（2026-09-05）

**指派：執行層 session B**（R-G62(b) 附記；Pei 於下放前確認 A 已停）。
Pei 裁（2026-09-05）：候選表五本**全數圈選**。本包只出 dry-run 與對照表，**不產 Revise 本、不寫回任何 xlsx**；Revise 本之產出待本包審過後另包（R-G72(e) 全閘重過）。

## 一、執行層任務

### 1　GC-10 收尾（先做）
1-1　A-GC19 入 `docs/fw036/ANOMALIES.md`（GC-10 審閱 §二 逐字）；R-G62(b) 附記入 PROTOCOL（GC-10 審閱 §一-4）；R-G70 v4.1 註「PROXI 舊式由 lint Y 報 WARN」。
1-2　八本基線 P 對照向（GC-09 §一-2-3）：既有八本以 v4.1 判準跑 P／Y，改前（v3 分支移除前，取 HEAD~ 之 lint036 或既有基線報告數）／改後逐本列。報告落工作區，只回數字。
1-3　pm_29 v4.1 dry-run 重跑（GC-09 §一-3 規格）→ `features/power/reports/rg70_pm29_dryrun_20260905_v41.tsv`；A 之舊表（方向錯）`git rm --cached` 後留工作區，本包附二表差異計數。

### 2　五本轉換 dry-run（R-G72(c)：只做機械轉換）
對每本 `delivered/*.xlsx`（sha 取 GC-10 5-4 節），全分頁全格掃描，出 `features/<f>/reports/rg72_dryrun_<slug>_20260905.tsv`：
`sheet / row / col / tc_id / rule / old_text / new_text / mechanical(Y/N)`

| rule | 轉換 | mechanical |
|---|---|---|
| P-v3→v4 | `$MSG.Sig$`→`Send CAN: MSG.Sig = raw (label)`（送出）；`The signal value $…$ = … is received`→`MSG.Sig = raw (label) is sent <時機>`；`Send the signal $…$`→`Send CAN: …` | Y（label／raw 原文已有）|
| PROXI-old→SWC | `PROXI $P$ is set to "v"`→`PROXI P = v` | Y |
| X-nav | 步驟無固定入口 | **N**（須 HMI 來源，R-G71(d)；列出但不造 hop）|
| test_item 上半 | **不動**（來源側記法，R-G70(b)）| — |

每本附統計：格數／命中格數／涉及列數／mechanical Y 列數／N 列數（分子分母）。**X 類全為 N**：其解須逐 TC 查 HMI 來源，屬另包（含 DR）。

### 3　TestRail ID 對照表雛形（R-G72(b)）
每本出 `features/<f>/reports/rg72_idmap_<slug>_20260905.tsv`：`old_tc_id / new_tc_id(空) / changed_cols / reason`。`new_tc_id` 留空（Revise 本產出時填）；`changed_cols` 依任務 2 之命中欄；`reason` 為 rule 集合。

### 4　pm_73 來歷
`features/power/RULINGS.md`／`DECISIONS.md`／`delivered/MANIFEST.tsv` grep `pm_73`；回報其交付日、來源批次、與 pm_29 之關係（是否為後續批之累積本）。只量。

### 5　上繳 `up/20260905_GC-11.md`：首行 session 標識與觀察依據；1-2 八本表；1-3 二表差異；2 五本統計；3 五表列數；4 結果；對照向；工具旗標逐次聲明；自報。

## 二、Pei 動作（本包後）
1. 審視五本 dry-run 之 N 列數（X 類），裁：Revise 本是否只做 mechanical Y 先出、X 類留待 HMI DR 後再一版。
2. `RULINGS.sha.tsv`：本包只加 A-GC19（非條文），預期不需重生；`--check` 一次即知。

## 三、未結 DR
同 GC-10（DR-VS{n}、HMI entry path 草稿）。
