# 下放包 GC-02 —— GC-01 追認後之落實（2026-09-05）

Pei「裁 準」（2026-09-05）。本包所依之裁決：
`down/20260905_GC-01.md` §一（R-G44 修訂版–R-G49）、`down/20260905_GC-01_review.md` §三、
`down/20260905_GC-01_review2.md` §三（R-G50）。R-G12～R-G29 引用一律標側（R-G43 未裁）。

**准之範圍（分析層之讀法，Pei 得推翻）**：R-G44（修訂）～R-G50 追認；GC-02 含測試改動；A-GC 落點 `docs/fw036/ANOMALIES.md`（已建）。
**仍未裁**：R-G43 保號側（(A)/(B)＋R-G19 台帳內二擇一）、`_intake/SXM/` 二檔。本包不觸此二項。

分析層本輪已自行落檔（皆 `get_file_info` 核畢）：
`down/20260901_VS-SL-01.md`（事後補檔）、`docs/fw036/ANOMALIES.md`（A-GC1–9）、`PROTOCOL.md` 第四增。

---

## 一、執行層任務

### 1　條文入台帳（R-G36：機器抽取，不轉抄）
自三份 down 檔以腳本抽取 R-G44（**取 review §三 之修訂版，非 GC-01 原文**；原文依 R-TM13 以刪除線保留於其後）、R-G45、R-G46、R-G47（含 review §三 補充）、R-G48、R-G49、R-G50，追加於 `docs/fw036/RULINGS_LEDGER.md` 末，節名 `## 下放包 GC-01 之全域條文（R-G44–R-G50，Pei 2026-09-05「裁 準」）`。
每條附來源檔＋行號。**`rulings_hash.py` 由 Pei 跑**，本包不跑。
R-G30～R-G41 之 sha 台帳缺列（GC-01 上繳 9-2 節）不在本包，屬 Pei。

### 2　`sources/MANIFEST.tsv` 補列 —— 先出提案表，不覆寫
依 R-G44 對 `source_identity_20260905.tsv` 之 179 唯一 sha 出 `sources/MANIFEST_proposed_20260905.tsv`，欄同現行 MANIFEST。
- `doc_id` 由檔名機器導出（family＋version token，小寫底線），**同 dup_group 共一 doc_id**；同名異體加後綴 `_v{sha8}`。
- 現行 10 列之 doc_id 保留不改；新列標 `proposed`。
- `note` 欄：異體者互指；`forms/` 所在者標 `FORMS`。
- **不覆寫 `MANIFEST.tsv`**；提案表經分析層審後另包併入。附統計：提案列數／與現行衝突數。

### 3　R-G45 版本綁定記錄（5 feature／13 檔次）
於 comfort、time_management、user_profiles、vehicle_category、vehicle_setting 各自 `DECISIONS.md` 追加一節「版本綁定（R-G45，2026-09-05）」，逐檔列：`inputs/` 檔名、sha8、`forms/` 同類檔名、sha8、**「是否影響已交付 TC：PENDING 分析層判」**（執行層不判）。
數據全取自 `source_identity_20260905.tsv`，並附查詢式與命中數（R-G50）。

### 4　`lint_reports/` 歸位 —— 出腳本，不執行
- 建 `features/sw_update/reports/`（空目錄＋`.gitkeep`）。
- 出 `scripts/cleanup_gc02.sh`：依 `lint_reports_refs_20260905.tsv` 之 81 檔 `removable=Y` 者寫 `git rm -- <path>`；sw_update 之 25 檔中屬 `N` 且非 0821 基線者（實測後定數）寫 `git mv` 至 `features/sw_update/reports/`。**每行一 pathspec（R-G12(FO)）**，腳本首行註「Pei 專門 commit 用，R-G26(FO)／R-G47」。
- 腳本**只產生，不執行**；上繳附其 sha 與行數。

### 5　測試修正（Pei 准含測試改動）
**5-1 先量測，後改**（禁止未量即改，R-DD26）：
grep 全 repo（`backend/`、`scripts/`、`tests/`、`docs/`、`features/*/`）之
`Test Case Specification&Result` 與 `Test Case Specification 測試用例規範`，逐處列檔／行／類別（code 讀取／code 寫入／test fixture／doc／feature.yaml）。
**若 code 端硬編前者**：不改 code，上繳具名，待分析層裁 —— 改 code 之讀寫路徑非本包授權。
**若 code 端皆自 `feature.yaml` 取名**：續 5-2。
- 5-2 三支夾具（`test_parser.py`／`test_writer.py`／`test_tool_parse.py`）之 `create_sheet(...)` 改為母本實測名；新增守衛測試：讀 `forms/…SWQT_20260817_ext.xlsx` 之 sheetnames，斷言模板 `workbook.sheet` 在其中。
- 5-3 `tests/test_lint036.py::test_existing_report_files_are_never_renamed`：`if report.exists():` 改 `assert report.exists(), ...`；若因此轉紅，紅為真紅（懸空），上繳具名不消紅。
- pytest 跑對照向（改前／改後），逐項同名列表（R-G7(FO)）。

### 6　路徑更正
`features/vehicle_setting/ANOMALIES.md:230` 之 `scripts/vs_sl01_dryrun.py` → `features/vehicle_setting/scripts/vs_sl01_dryrun.py`；依 R-TM13 原文以刪除線留存。改前 `get_file_info`，改後附 diff。

### 7　`up/20260901_VS-SL-01.md` 補檔
檔首標「事後補檔，補檔日 2026-09-05，原交換於聊天」。內容**只列**六件產物（GC-01 上繳 7-2 節 #6–#11）之路徑、現行 sha8、行數；不寫當時之自檢與自報（無留存，IN §8.4.1）。

### 8　上繳 `up/20260905_GC-02.md`
產物清單＋sha256＋每項之查詢式與命中數（R-G50）＋自檢（無刪除／無搬移／`down/` `up/` 未動，除 7 之新增）＋`gate_all` 對照向與交付向＋自報。

## 二、Pei 動作（本包只提醒）
- `rulings_hash.py`（任務 1 之後）；R-G30–41 補列
- `_intake/` 清空（Display／Popup／SW_Update／Vehicle_Category 已證落點，GC-01 上繳 2-2 節）；SXM 待裁
- 跑 `scripts/cleanup_gc02.sh` 之專門 commit

## 三、未結 DR
無新增。
