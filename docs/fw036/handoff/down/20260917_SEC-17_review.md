# SEC-17 審閱 —— 上繳包 `up/20260917_SEC-17.md`（佔位列淡橘 → `security_v11`）

審閱層：分析層，2026-09-17。
判定：**通過，不升級**。文字 diff 0、zip 48、DV (3,1) 不變、六本 lint 門檻 0；50 TC／1,700 格。
執行層兩處不符**皆正確**：50 是對的（分析層 A-42），外科法是對的（下放包 §2 之 openpyxl 寫回會重演 R16 事故，分析層 A-43——條文與自家 `xlsx_surgical.py` 檔頭相悖）。

## 一、裁定
| # | 項 | 裁 |
|---|---|---|
| 1 | 塗色 TC 數 50 ≠ 41 | **50 為準**。41 是 `EXEC_GUIDE` §5「含 `$` 佔位指令行之 TC」，只計 Procedure；96 行佔位分佈於 50 TC（含 ER-only 者）。登 A-42；`EXEC_GUIDE` §4 句改為「50 rows」（下次重生時）|
| 2 | 外科法 ＋ 另立 `mark_placeholder_rows.py` | **准**；R16 先例為依據。下放包 §2 條文作廢 |
| 3 | 色碼 `FCE4D6` vs SWC 先例 `FDEADA`（同族肉眼不可辨）| **維持 `FCE4D6`**——Pei 指示「淡橘」，且 SWC 的橘色標的是特定列群（SWC-167），語意不同；Security 本內無他色，不致混淆。若日後同一本需第二種標記，屆時換色相 |
| 4 | 塗 A~AH（含序號欄）vs SWC 先例 B~AH | **維持 A~AH**（Pei「整列」字面）；Remarks 不加註 |

## 二、交付本
`features/security/sandbox/delivery/` 六本 `security_v11`（CertProvider 11／KeyInstall 4／SAM 23／ECUCert 7／SwdlSecureLib 4／libLogEncrypt 1 塗色列）＋ `coverage.tsv`／`placeholder_summary.tsv`／`placeholder_by_token.tsv`／`placeholder_rows.tsv`／`asset_request.md`／`EXEC_GUIDE.md`。
**這就是給 Pei 的出貨本。** Pei 側仍待：六本封面 18 格、`W=4`、GC-16、三封訊息。

## 三、分析層自報
A-42　41 vs 50：把「含佔位指令行」當「含佔位」。
A-43　§2 令用 openpyxl 寫回，違自家 R16 先例。

## 四、下一包
Security 線**無**。git：一支 `feat(security): SEC-17 — shade placeholder rows, security_v11`。
