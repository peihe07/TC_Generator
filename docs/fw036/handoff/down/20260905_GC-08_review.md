# 審閱 GC-08 上繳包（2026-09-05，Tier 2）

對象：`up/20260905_GC-08.md`　下放包：`down/20260905_GC-08.md`
結果：**收受**。`gates_tsv` FAIL→PASS（56 閘），`body_kind` 復原逐欄同人手列。5-5 四項裁於 §一；併行寫入之處置於 §二（本輪最要緊）。

## 一、5-5 四項

1. **R-G47 vs `lint_paths` 之 `.json`** —— 裁：R-G64（原 R-G25）之落點表增 `reports/` 為 `.md`／`.json`／`.tsv` 之合法第一層目錄（lint 報告成對落此本就是 R-G47 之要求，政策表漏列）。[DEFAULT]。`scripts/lint_paths.py:27` 之表同步；基線不動。GC-10 施作。
2. **`body_kind` 之 `owner`** —— 裁：改為 `rulings_hash`（登錄簿之 owner 須指實作所在，否則除役無所依）；`DOCS_GATE_OVERRIDES` 改該欄，`note` 記「原記 lint_docs036，2026-09-05 依實作更正」。
3. **`R-G29`→`R-G68` 於 `docs/runtime/*`** —— 裁：`docs/runtime/{GATES,SELFCHECK_MAP}.tsv` 與 `profiles/` 納入在效檔，補改號；GC-03 之 `OTHER` 類中屬 `docs/runtime/` 者一併（先量幾處）。
4. **`SELFCHECK_MAP` 12 支缺對映** —— 對映方向為「IN §9 自查項 → 閘」，非每閘必有項；缺對映之閘不是缺陷。裁：不造對映；`--check` 之 FAIL 成因為 `X` 未入 TSV，GC-07 線 21:16 已重生，commit 後複驗即可。

## 二、併行寫入（5-3）

兩條分析線各自下放、同一執行層 session 交錯施作，已造成：`body_kind` 被另一次重生洗掉、`gates_tsv.py` 一度回 HEAD、`SELFCHECK_MAP.tsv` 還原後又被寫、staged 內混兩線。**這不是任一包之缺陷，是流程無互斥。**

```
R-G62(b)（同一 repo 之寫檔互斥 —— 全域）[DEFAULT]
  同一 repo 同時只得有一個執行層 session 在寫檔。第二條線之下放包可先落，
  其施作須待前一包上繳後開始；執行層於上繳包首行記「本包施作期間無其他 session 寫檔」，
  不能如此聲明者，上繳須列併行改動之檔清單（如 GC-08 5-3）。
  分析層兩 session 並存不禁，但下放包互相引用時以 mtime 先者為準。
```

**R-G50(d)**（5-2 提案，收受）：下放包點名工具時**須寫明其唯讀旗標**（`--check`／`--out`）；執行層對未寫明者一律先 `--help` 量旗標，不得推定。上繳之「工具寫入路徑清單」改為逐次執行前聲明。

## 三、Pei 動作 —— **commit 前先分線看**

staged 內已混 GC-06／GC-07／GC-08 之改動且共用檔（台帳、sha 表、`SELFCHECK_MAP`）無法拆線。皆已審過，**一顆 commit 收，訊息列明三包**：

```bash
cd /Users/peihe/Work_Projects/TC_Generator
git status --short | sort                      # 貼我一份再 commit（確認無 vsm 線 modified 混入）
python3 scripts/rulings_hash.py --check        # 須 PASS（766 或更多）
python3 scripts/gates_tsv.py --check           # 須 OK（56）
python3 scripts/selfcheck_map.py --check       # 須 PASS（X 已入）
git add -A -- docs/fw036 docs/runtime docs/reports scripts tests \
  features/sw_update/RULINGS.md features/vehicle_setting features/power/reports \
  features/bed_lowering/reports features/vehicle_category/reports
git commit -m "feat(fw036): GC-06/07/08 — ledger anchors R-G36b R-G43d R-G71, canon §5.8 nav path, lint X, gates_tsv body_kind restored, sw_update delivery record, GATES/SELFCHECK regenerated"
git status --short                             # 應只剩 driver_distraction/workbook 與 vsm 線 untracked
# driver_distraction（GC-06 審閱 §四-3 之四行）
bash scripts/gc06_driver_distraction_mv.sh
sed -i '' 's#workbook: "workbook/driver_distraction_00.xlsx"#workbook: "sandbox/driver_distraction_00.xlsx"#' features/driver_distraction/feature.yaml
git add -- features/driver_distraction/feature.yaml
git commit -m "chore(driver_distraction): move workbooks to sandbox/ per R-G64" -- features/driver_distraction
```

`git status` 第一行貼我看過再跑 `git add -A`——那條會吃該目錄下所有變更，vsm 線若有 modified 在 `features/vsm_v4*` 不在清單內，安全；但 `docs/fw036/handoff` 內若有 vsm 線的 down/up 新檔會一併進，屬歷史紀錄，可接受。

## 四、GC-10 預告
`lint_paths` 落點表增 `reports/`；`body_kind` owner；`docs/runtime` 改號；R-G62(b)／R-G50(d) 入台帳；既有 8 紅測試先量。**GC-10 待 GC-09 上繳後才下放**（R-G62(b)，避免再撞）。
