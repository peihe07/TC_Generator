# 審閱 —— 上繳包 VS-SL-03（綁別名重跑＋沙盒稿＋DR 稿）

日期：2026-09-02　層級：Tier 2　對象：`up/20260902_VS-SL-03.md` 及其 §1 所列產物

判定：**部分准。** v3 報告、lint、BL／VC 沙盒稿、alias 綁定與五處自報皆收訖；
**vf230 沙盒稿退一小輪（VS-SL-04）**——(3) 分支 105 列中約半數可由二次查找解除 PENDING（§2，證據齊），
解完再進 Pei 之 Excel 抽驗，免抽兩次。

---

## §1　收訖事項

- 兩處與包內預期之差異（`ALIAS_UNRESOLVED` 43 非 3；(3) 30→105 且 30 為真子集）：**解釋成立，收訖**。
  43 之成因（176 列中 40 列先落 (1)）數學可驗；105 之成因（新綁者首次被評估）合理。
- `ALIAS_MANUAL_NO_PATH` 5 名不猜路徑：**准**——evidence 逐字「待認」者本不該綁。
- §5-1 `load_settings` 容器判準之修（517 項、少 0 多 0）、§5-2 三處落檔前自正、§5-3 量測失誤自報：收訖。
- §5-4 上游二筆疑似筆誤入 DR 稿：准。
- §5-5 `canon_refs` +2 源於交換包文面裸節號：**協定補一條**（已加 PROTOCOL.md）：
  包文面引 canon 一律帶文件前綴（`FO §x`／`IN §x`），不寫裸 `§x`。+2 屬文面既成，不回修。
- vf230 來源取 `inputs/` 現本（該 feature 無 `output/`）：收訖，寫回包沿用同源。

## §2　vf230 (3) 分支 105 列之二次查找（VS-SL-04 之令，證據皆本層實測）

執行層之判「兩來源皆空」係以**該設定名自身**查总控表；漏掉兩類可解者：

**R1　家族閘（SWITCH 一族 54 列）**
总控表 No.267 `AUX SWITCH Type`：`AUX_Switch_Types is [Type1] → 4`／`[Type2] → 6`；
proxi_values 有 `AUX_Switch_Types {1: Type 1, 2: Type 2}`。
→ `SWITCH 1–4` 之列補 `PROXI AUX_Switch_Types = 1 (Type 1)`；
　`SWITCH 5–6` 之列補 `PROXI AUX_Switch_Types = 2 (Type 2)`。
label 以 proxi_values（`Type 1` 含空格）為準，非 FIP 文面之 `Type1`。
規則化：設定名自身無总控表列、但其別名 evidence 所指之家族列為**變體數閘**者，套家族閘，成員取足以顯示該成員之值。

**R2　FIP 列存在但 Always false／Always return 0 → 不 PENDING、不 DR**
實測：`Suspension Default Ride Height` No.274 = `Always return value is 0`；
`Phone Repetition` No.215 = `Always return value is 0`。
依 R-VS87 之理（Atlantis 不出者不造條件、不出負向），此類改掛登記旗 `FIP_ALWAYS_OFF`，
Pre 不加 PROXI 行、不 PENDING；列保留（追溯需求仍在）。

**R3　真無來源者維持 PENDING**，且——**記 A（分析層之誤）**：VS-SL-03 §3 之 DR 稿規格只令三節，
漏了 (3) 分支「設定之顯示條件兩來源皆未載」一問。DR 稿補**第四節**：R1／R2 處理後之殘餘
（預期含 ParkSense Front/Rear Volume 8 列、Navigation Turn by Turn、Hour Mode、
Suspension Service Mode 與 v2 既有之 30 列），逐設定名列 TC 引句與兩表查無之證。

## §3　VS-SL-04（已下放 `down/20260902_VS-SL-04.md`）

1. 對 v3 (3) 分支 105 列跑 R1／R2／R3，出 `_v4_branch3_resolution.tsv`（每列：R1／R2／R3＋證據欄位）
2. vf230 沙盒稿就地重生（同 `sandbox/vssl/`，v3 稿另存 `.v3.bak`）；lint 重跑
3. DR 稿補第四節
4. 上繳後即進 Pei Excel 抽驗（BL／VC 沙盒稿不動，隨同送抽）
