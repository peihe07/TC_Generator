# 下放包 CMF-01 — Comfort HMI 0907 本之承接、對帳與 Revise 前置（唯讀為主）

- 日期：2026-09-18
- 線別：Comfort（`features/comfort/`，profile `docs/runtime/profiles/FW036_R1L_Comfort_Profile.md`）
- 性質：**承接 ＋ 唯讀對帳 ＋ sibling plan**。本包**不寫回任何工作簿**、不動 `RULINGS.sha.tsv`、不跑 `--gate`／`--regen`。
- 依據：Pei 2026-09-18 要求檢視 0907 本「有無需修改、拆解是否足夠」，分析層審閱後 Pei 回「好」。

## 0. 分析層自陳（A 類，先讀）

分析層之首輪審閱**未先讀 Comfort profile 即下斷言**（型態同既往：量測未窮盡即陳述）。
讀 profile 後，下列首輪所見**撤回或降級**，執行層不得據首輪對話內容動手：

| 首輪所見 | 處置 | 理由 |
|---|---|---|
| 括號下半「末步驟 -> 末 ER」違 R-16 | **撤回** | R-16 為 PM 線裁定；profile §7「其他 feature 之裁決不因類比而適用」。下半形態依 profile §3.1.1；`->` 式是否合規列 §4 待裁 |
| -277／-279（`080-02`／`081-02`）應產出 | **撤回** | R-C24 已裁：該 leaf 全為委派，無獨立餘留；`[BLOCKED-SPEC]` 正確 |
| BLOCKED 列不應佔 TC ID（比照 R-DIAG11）| **撤回** | 跨線類推；profile §5.1–5.3 已定其形 |
| 排除式 PC（3-knob／non-foldable／EMEA）為樣板、屬 §8.5 漂移 | **撤回** | 介面型軸之排除式 PC 為 R-C34 之設計 |
| spec_reference 掛 2.14／16.2／6.3 非直接驗證 | **降級為待裁** | 疑為 PC 出處節次（R-C29）之承載；見 §4 |

**仍成立者**列於 §2、§3。

## 1. 承接 0907 本（本包第一步，做完才往下）

審閱對象不在 repo 世系內：

- 檔：`/Users/peihe/Work/02_Project_R1LR/10_Reviewing/00_TestCase/ASW-R2/Climate Control Interface/FM-WI-FSM-036-A01 …_SWQT_ComfortHMI_20260907.xlsx`
- sha16（分析層容器內量得，執行層須以 `shasum -a 256` 重量，**以執行層之值為準**）：`3550d16d8bd31963`
- repo 內最新為 `output/…_Comfort_20260817_rowsort.xlsx`；`DELIVERY.sha256` 無 0907 之 ENTRY；`delivered/MANIFEST.tsv` 49 bytes

工作項：

1. 依 sha256 身分規則登記 0907 本（`features/comfort/delivered/` ＋ MANIFEST；**不覆寫、不改檔名內容**）。
2. **0907 本 ↔ `generated/*.json` 逐格對帳**（14 個寫入欄），產 `features/comfort/docs/reports/diff_0907_vs_generated.tsv`：列數、TC ID 對映、逐欄不符數。分析層量得 0907 本為 **466 列**（465 個 TC ID ＋ row 116 空殼）、Test Group＝`Climate Control Interface`（profile R-C6 為 `Comfort`）、T–Z 全為 `1`（profile §3.9 為留白）、J 欄無節次括號而帶 `for which …` 子句（profile §3.2 為 `(節次)`）、N 欄缺 `SYS1_` 前綴且以 `、` 串接（profile §3.5）。
3. 據對帳結果，於上繳包**第一段**回答：0907 本是 pipeline 產物之後續版、抑或離線編修本？差異集中於哪些欄？
4. **停點**：若 generated JSON 與 0907 本之不符 > 14 欄 × 50 列，**不得**自行決定以何者為基線，回報待 Pei 裁（§4-1）。

## 2. 必修項（與基線之裁定無關，先備好修法，不寫回）

以下為分析層以 openpyxl 讀 0907 本 `Test Case Specification 測試用例規範` row 10 起所量；執行層逐項複驗後備 patch（JSON 層），**不產新工作簿**，待 §4 裁定後一次 Revise。

| # | 項目 | 量得 | 依據 |
|---|---|---|---|
| M1 | row 116 空殼：`SWE1-HVAC-019-03` 僅 D 欄有值，無 TC ID 無內容 | 1 列 | §8.4.3 不得留空；delivery note 原列為 VF HVAC 文件缺件 → 應為 `[BLOCKED-SPEC]` 列或 `PENDING: DR`，依 R-C24 五問判 |
| M2 | -312／-313（`103-01`／`103-02`）pc＋proc＋er 逐字相同，且皆未執行 037 VC 之「timeout 前再按一次」；兩條都只測逾時消失 | 1 組 | §7 FP；R-C19 之外之真重複 |
| M3 | T–Z 七欄 466 列全 `1` | 466 | R-G74（598／5210 恆 0）；Pei 2026-09-16 車型欄 1／0 裁定晚於 profile §3.9，**以後者為準** —— 但各軸（EMEA ICS 118 列、BEV、Tri-Mode…）對七車型之 1／0 需 PROXI 依據，列 §4-3 |
| M4 | N 欄：缺 `SYS1_` 前綴（profile §3.5 stem）；以 `、`／`;` 串接 | 452 | canon §10.7 一章節一行（Pei 2026-08-21）|
| M5 | PC 內 `[Present]` 方括號＋`shall` 原句：-083／-084／-403／-404 | 4 | profile §3.2.4 `[ext-verbatim]` 容其逐字；**惟 source class 標籤不得外露而此四格外露了引文之外之 `The below requirements shall be implemented when`** → 複驗是否為逐字片段之一部 |
| M6 | `Set the … availability status from the CCM to unavailable`：-033／-069／-071／-390 無執行通道 | 4 | canon §8.7.5(c)；CFTS043／DBC 查得則 `Send CAN:`，查無則 `PENDING: DR` |
| M7 | 標籤引號不一致：`AUTO` 未引 161／已引 43；`Face`／`Feet` 未引 54／55；`RECIRC` 未引 28；`SYNC` 未引 24 ＋ `Sync` 4 | — | canon §11；**hit 數非列數** |
| M8 | Test Set 漂移：`HVAC Pop-ups`（-282，1 條）vs `Climate Popups`（44）；`EMEA ICS Interface` 僅 -403／-404 | 3 | framework Part N 為 15 組；0907 本見 17 個值 |
| M9 | 主動詞 `Note`（25 條）；複合步驟 `, then`／`and <verb>`（102 條，啟發式，需逐條判）| — | canon §5.1／§5.2 |
| M10 | 上半 > 50 token：26 條（-010～-012 為 94）| 26 | canon R-3；profile §3.1.1「該 leaf 對應之那一句」—— 二者衝突時之處置列 §4-4 |

## 3. 拆解不足 —— **plan-first，不產 TC**

依 Pei 2026-09-17 全域標準（037 VC／VM 為必要輸入；VC 點名之情境各 ≥1 TC；列舉值各自 sibling）。
產 `features/comfort/docs/reports/sibling_plan_0907.tsv`（欄：req_id／現有 tc_id／缺之軸／軸值／值之出處節次與逐字片段／預估新增數／是否受 R-C42 三條件所阻）。

候選來源（皆為分析層啟發式，**逐列判讀，不得照單全收**）：

1. **一條 TC 綁兩個控制實體或兩個觸發**（ER 行重複者 15 條）：-031、-049、-061、-064、-072、-103、-113、-119、-178、-201、-204、-308、-362、-438、-444；另 -006（雙向反映）、-083（fan 與 mode）。依 canon §8.2.2 判拆。
2. **touchscreen／hard control 通道軸**：037 VC 或 Description 點名兩通道者約 51 req，其 TC 僅走單通道者約 45（31 純觸控／14 純硬鍵）。037 VC 於同 parent 下為共用樣板，須回條文判該 leaf 是否真含兩通道；硬控型態受 profile §1.1 之 Hard Controls 文件限制（僅得引「型態隨配置而變」）。
3. **列舉未展開**：`003-07` 破 AUTO 之五觸發、`023-01` 之 7 組合（3 鍵）、`024-xx` 之 `press any climate function` 清單。
4. **-210～-219 ↔ -247～-258 十組逐字相同**（ch11 ↔ ch12）：R-C19 與 delivery note 之開放問題（兩章是一個需求或兩個）；plan 內只標記，不處置。

**停點**：plan 之預估新增 > 40 條即停，回報待 Pei 裁。

## 4. 待 Pei 裁定（上繳包須逐項附量測）

1. **基線**：Revise 以 0907 本為準回灌 JSON，抑或以 JSON 為準重產？（§1 對帳後才能裁）
2. **Test Group**：`Climate Control Interface`（0907 本）vs `Comfort`（R-C6）。
3. **車型欄**：七欄 1／0 之依據（`forms/proxi/<platform>/` 可否定出 ATC／MTC、Rear HVAC、ICS、BEV 等軸之各車型值）；定不出者之預設。
4. **下半形態**：`(action -> ER)` 459 條 vs profile §3.1.1 之例式；**上半超 50 token** 與「整句照抄」之衝突處置。
5. **N 欄是否續載 PC 出處節次**（2.14×447、16.2×416、6.3×367）；canon §10.7 為「directly verifies」。
6. **`001-01`／`-02`**：037 寫 `up to 3 tabs`／`Front, Seats, Rear`（VC 前提 DT／HDCC），0907 本 -001～-004 依 SR24 條文測 4 tabs 含 Massage。profile 已裁 Maserati 不在範圍；Massage 是否在範圍未見裁定。
7. **Final Step 無 check target**（229 條，啟發式）、**導航入口**（`Open the comfort category` 類 85 條；profile §3.2「入口未定義」清單僅載 `16.16`／`16.17` 二例，DR #34）：canon §5.5／§5.8（R-G71）晚於 profile，是否回溯適用於本線。
8. **Priority**：P0 325／P1 132／P2 8；本線有無 rubric 裁定（分析層於 profile 未見）。

## 5. RD feedback 候補（執行層比對 `DATA_REQUESTS.md` 後回報是否已載）

- SR24 Basic Report 180 列中 037 未引 51 列；扣除 heading／assumptions 後之實質條文：`18.2`–`18.4`（R-C16 已載）、**`19.1`–`19.3`（7" widget，分析層於 profile §5.4 未見）**、**`20.1`–`20.4.3` LATAM Rear Blower 11 列**（Toro／Fastback 屬 LATAM，與 §4-3 相關）。
- SR25 相對 SR24：新增 §21 L3 HVAC 7 列；EH5 文字變更（`switch to AUTO ECO on`）。R-C1 基線不動，僅記錄。

## 6. 上繳條件

- 第一段：§1 對帳結論 ＋ 任何停點命中。
- 附：`diff_0907_vs_generated.tsv`、`sibling_plan_0907.tsv`、M1–M10 逐項複驗表（分析層數字對／錯／差幾）。
- `LINT_STATUS: OK` 哨兵（若有跑 lint）；未跑則明載未跑。
- 不 commit（Pei 為唯一 git 操作者）。
