# 下放包 SEC-21 —— 收尾 chore：apk 雙名 Remarks 註 ＋ pilot 生成器 deprecation ＋ `mark_placeholder_rows.py` 標的參數 → `security_v15`（只 Remarks）

前置：`down/20260918_SEC-20_review.md`。Pei 先落 SEC-19／20 兩支 commit。
執行層 session：新開或延用；不跑 `--gate`；RULINGS 兩檔不動。

## §0　禁區
- TC 文字只允許 Remarks 增行；其餘欄 0 diff；填色／DV／zip 不變。
- 既有 xlsx 不動；v15 新檔。

## §1　apk 雙名註（R-SEC24 note）
凡 Pre-Condition 同時含 `CertProviderAndroidInstrumentalTest.apk` 與 `CertProviderServiceManagerTest.apk` 之 TC，Remarks 須有一行
`apk name: CCVR STEPS say CertProviderServiceManagerTest.apk; delivered file is CertProviderAndroidInstrumentalTest.apk (A-SEC-15)`。
已有者不重複；缺者補。回報涉及 TC 數與補註數。

## §2　腳本
- `gen_pilot01.py`／`gen_pilot02.py` 檔頭加 `# superseded by gen_batch1.py (SEC-04); do not run — constants frozen per R-TM13`；不刪、不改邏輯。
- `mark_placeholder_rows.py` 加 `--targets <glob>`（預設六本＋`_all`），`superseded/` 命名取實際版本尾綴（沿 SEC-17 review 之建議）。
- `build_spec_ref.py` 之 `missing_nrl` 變數改名 `missing_object_id`（SEC-19 自報 2）。

## §3　產出
`SEC_VER=v15` 重生六本＋`_all`；套色；v14→v15 diff 只在 `remarks`（預期 ≤ 12 格）；lint 門檻 0；`delivered/` 同步、v14 移 `superseded/`。

## §4　上繳要求（`up/20260918_SEC-21.md`）
1. 兩支 commit hash（Pei 落後回報）。
2. §1 涉及 TC 數／補註數；diff 格數與欄。
3. 三腳本 diff 摘要；`mark_placeholder_rows.py --targets` 對單一本之測試（文字 diff 0、members 48）。
4. 六本 sha、lint。
5. 自報錯誤。

## §5　升級條件
- diff 落在 `remarks` 以外；lint 任一門檻非 0。

## §6　本包後
Security 線**無**下一包；等 RD 資產／Samuel 路徑答覆／037 作者 DR／`W=4`／自動化 go；GC-16 先行。
