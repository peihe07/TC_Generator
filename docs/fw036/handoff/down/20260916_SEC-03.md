# 下放包 SEC-03 —— pilot01 違規之裁定落檔 ＋ 形制對齊 ＋ lint 補強 ＋ pilot02（12 TC）

前置：`up/20260916_SEC-02.md`、`down/20260916_SEC-02_review.md`。
Pei 2026-09-16 裁：**review §四 九項照分析層建議全數准**。
執行層 session：延用或新開皆可；git 仍損毀者，產物落工作樹即可（Pei 側修 Xcode license）。

---

## §0　禁區

- git 不動；`down/`／`up/` 既有檔不動；其他 feature 不動；canon 不動。
- `lint036.py` 修改限於 `check_security_channel()`／`check_security_source()` 兩函式與 `FEATURE_CHECKS["security"]`；回歸三路徑 sha 須不變。
- 只產 pilot02 之 12 TC；`pilot01/` 目錄保留不動（R-TM13），pilot02 另目錄。
- `framework.md` LOCKED 不動；`feature.yaml` 只允許回填 §2 量測後之 Priority／Design Method 寫法。

## §1　量測先行（任務 1，回報後才寫 TC）

執行層對 **SWC 0708**（R-1 v2 基準本）與 **pm_29** 量測下列欄之實際值分佈（`Counter`），逐字回報：

| 欄 | 量測 |
|---|---|
| `Test Case Priority` | 值分佈（預期二選一：`P0`~`P3` 或 `High/Medium/Low`）|
| `Test Case Design Methods` | 值分佈（是否為 `下拉選單` sheet 之雙語全文，或英文短式）|
| `Input Test Data` | `NA` 佔比（預期 SWC 285/286）|
| `Test Case Reference ID` | 值分佈（`NEW`？空？）|
| `Estimated Test Time` | 空／數值佔比 |
| `Test Item` 上半 | 取 20 列抽樣：上半是 Requirement Title 還是 Description 句（對照其 037 母體 D/E 欄）|
| ER 內字面引號 | `"…"`／`'…'`／反引號 之計數 |

**規則**：Security 之寫法 = 兩本交付本之**多數值**；兩本不一致者以 SWC 0708 為準。量測結果寫入 profile §3「欄位形制」節並回報，**在 pilot02 寫入前**。

## §2　裁定 R-SEC15（Pei 2026-09-16 准；取號現查）

```text
R-SEC15  pilot01 所揭違規類型之明文（適用 Security 全 70 列）
  (a) 跨場景字串移植 = 造值。任一 log 字串、指令、方法名、路徑，只能用於其來源所載之同一場景
      （同一 apk 方法／同一元件／同一憑證樹）；移用到他場景視同 §8.4.1 造值。
      例：CS.212 row 1 之 `ECU certificate verification broken chain = ERR_UNABLE_TO_GET_ISSUER_CERT_LOCALLY`
      只可用於 `ecuCertTestBrokenChainCert`，不得用於 `fotaMcpuCertTestBrokenCert`。
  (b) 觀察不得代替觸發。ER 期待 DUT 側之結果（log／檔案／狀態）者，Procedure 須有一步在 DUT 側觸發該行為
      （apk 方法、USB 插入、UDS 請求、CAN 送出、reboot）；host 端 openssl 不構成 DUT 觸發。
      無可用觸發手段者，該觸發步驟整行寫 `PENDING: X-<n> <variant> + trigger`，ER 對應行同為 PENDING。
  (c) 內部台帳代號（`X-[a-z]`、`DR-SEC-[a-z]`、`R-SEC\d+`、`A-SE`）不得出現於 Pre-Condition／Input Test Data／Procedure／ER；
      只可入 Remarks。PENDING token 例外：`PENDING: X-<n> <缺件名>` 與 `PENDING: DR-SEC-<n> <缺件名>` 皆合法（R-SEC7(e) amend）。
  (d) 字面值一律 `"…"`（IN §11）；反引號、單引號、`<>` 禁用（`<…>` 佔位除外）。
  (e) `Input Test Data` 一律 `NA`（R-1 v2 基準；§1 量測若證實則落 profile）；資料內聯 Pre-Condition 或 Procedure。
  (f) `test_item` 上半 = 037 `Requirement Description` 之首句 verbatim（≤50 token；長句自句首截至第一個句號），非 Title。
  (g) apk 方法只用於 `apk_pairing.tsv` 有列之 SWE1；無列者不得硬配，觸發步驟依 (b) PENDING。
  (h) R-SEC8(e) 重申：DID 值不入 ER；KI-011 之 Binder 呼叫無素材者，Proc 該步 `PENDING: X-f KeyInstall status test runner`。
  (i) `PENDING` 為整行 token，不與散文混寫；一 ER 行只能是「可觀察斷言」或「PENDING token」二者之一。
  (j) Security profile 豁免 lint `I-cross`。
```

## §3　EXEC_ASSETS／trace 更新

- `EXEC_ASSETS.md`：`X-e` 加兩項 —— `wrong-subject`／`wrong-OID` 憑證之 **DUT 側觸發手段**（apk 方法或 RD 提供之腳本）；`X-f` 加 `KeyInstall status test runner`（Binder `getStatus()` 之可執行入口；`KeyInstall_IntegrationTests.zip` 是否含之——執行層**只列檔名**，不拆 zip）。
- `trace_summary.md` 4b：CP-002／CP-011 之**負向分支**改判 `N`（正向仍 `Y`）；`channel_feasible` 欄改為每 SWE1 兩值 `pos/neg`。
- `apk_pairing.tsv` 不動。

## §4　lint `SC` 補強（`check_security_channel()`）

新增判項（FAIL）：
1. Pre-Condition／Input Test Data／Procedure／ER 任一欄含反引號或單引號包字串。
2. 上述四欄含 `X-[a-z]\b`、`DR-SEC-[a-z]\b`、`R-SEC\d`、`A-SE`，且該行不以 `PENDING:` 起首。
3. ER 行含 `PENDING:` 但非整行以 `PENDING:` 起首。
4. ER 某行引用之指令（`$ …` 或 `adb …`）未出現於同編號之 Procedure 步驟。

回歸：三路徑 sha 不變；對 pilot01.xlsx 重跑，**預期 `SC` 由 0 轉為 ≥ 14**（作為補強有效之證據，數字回報）。

## §5　pilot02 —— 12 TC（`features/security/sandbox/pilot02/security_pilot02.xlsx`）

pilot01 之 10 列**全部重寫**＋ 2 列：

| # | TC ID | SWE1 | sibling | 對 pilot01 之修正要點 |
|---|---|---|---|---|
| 1 | `NR1L-CP-001` | CP-001 | valid → 通過 | Pre-Cond 資產改描述；ER 引號；`ERR_` 推論入 Remarks |
| 2 | `NR1L-CP-002` | CP-001 | broken → 拒絕 | ER1 只留 `"unable to get issuer certificate"`；ER3 `PENDING: X-h log keyword`（不得移植 ECU 字串）|
| 3 | `NR1L-CP-003` | CP-002 | subject 相符 | ER1 依 037：`identical to the subject name in SecurityAssets/oem-certs/cert-provider/{TYPE}/cfgs/`；去 CertProfile |
| 4 | `NR1L-CP-004` | CP-002 | subject 不符 | Proc 3 = `PENDING: X-e wrong-subject certificate + trigger`；ER3 = `PENDING: X-h log keyword` |
| 5 | `NR1L-CP-005` | CP-004 | 本地撤銷命中 → 拒絕（**pilot01 換掉的列換回**）| 依 R-SEC8(f)：CRL 寫法、`CRL.r0` 逐字、Remarks 註 DCL；觸發用 apk #12 `secondPartyLevelTwoCertTestNormalFlow`（apk_pairing 有列）—— **但 NormalFlow 為正向**，撤銷分支之觸發仍 PENDING：Proc 寫 `PENDING: X-e revoked certificate + trigger` |
| 6 | `NR1L-CP-006` | CP-011 | OID 相符 | Proc 3 不用 fota 方法；改 `PENDING: X-e OID certificate + trigger`（(g)）；openssl 兩步保留 |
| 7 | `NR1L-CP-007` | CP-011 | OID 不符／缺 | 同上，PENDING |
| 8 | `NR1L-ECUC-001` | ECUCert R18 | CSR 讀取（DID 2965）| 去 Pre-Cond 2（session）改由 Proc 1 進入；ER3 改 `$ adb pull /data/misc/ecuidentity/ecu.crt` ＋ `$ openssl req -in ecu.crt -noout -verify`（CS.165 第 12 項逐字）→ `verify OK`；DID 讀值與 ecu.crt 之一致性若無比對手段則不斷言 |
| 9 | `NR1L-KI-001` | KI-011 | INSTALLED (2) | Proc 1 `od` 加 `<installstate file>` 佔位；Proc 2 `PENDING: X-f KeyInstall status test runner`；ER 不含 DID；Design Method 依 §1 量測寫法之 Functional |
| 10 | `NR1L-KI-002` | KI-011 | ERROR (3)（**新增**）| Pre-Cond：unsuccessful installation attempt made（037 4.1 逐字）；同 #9 結構 |
| 11 | `NR1L-SAM-001` | SAM-0004 | 驗證失敗、保留既裝 | Proc 加 `$ adb shell ls -l /data/vendor/dauth` 為獨立步驟（before／after 兩步，§5.6 baseline）；ER3 PENDING X-h |
| 12 | `NR1L-SAM-002` | SAM-0007 | SAM 憑證通過 | ER1 依 037 三句粒度；去 CertProfile；引號 |

**每 TC 共同**：Input Test Data `NA`；Priority／Design Method 依 §1 量測寫法；`test_item` 上半依 (f)；Vehicle Model 同 pilot01；Remarks 保留 `source:`／`Polarion:`／`apk pairing:`／`X-` 註。
**門檻**：`P`／`X`／`SC`（補強後）／`SS` 全綠；`I-cross` 豁免；PENDING 計數回報（預期 ≥ 6，全部可歸 X-e／X-f／X-h）。

## §6　上繳要求（`up/20260916_SEC-03.md`）

1. §1 量測表（七欄，兩本，Counter 逐字）與據此定之寫法。
2. R-SEC15 實際號、body sha8、`--gate`。
3. `EXEC_ASSETS.md`／`trace_summary.md` 更新；`KeyInstall_IntegrationTests.zip` 之外部路徑與大小（不拆）。
4. `lint036.py` diff、回歸三 sha、pilot01 重跑之 `SC` 數。
5. pilot02 xlsx／json、lint 報告、PENDING 逐筆歸屬、12 TC 之 channel／observe 表。
6. 自報錯誤。

## §7　升級條件

- §1 量測兩本互斥且 SWC 0708 該欄為空（無法定寫法）。
- lint 回歸 sha 改變。
- pilot02 任一 TC 出現 (a)~(i) 之違規（執行層自檢逐條回報「0 處」，附檢查方法）。
- PENDING 有無法歸 X-／DR- 者。
