# 下放包 CAM-03 —— Camera Phase 2 收尾 ＋ Phase 3 pilot 批（A 本 9 TC）

指派執行層 session：延用 CAM-01／02 之 session。
對應審閱：`down/20260916_CAM-02_review.md`；Pei 2026-09-16「都照建議來做」—— 審閱 §三八項全准。

---

## §0　禁區

- git 一律不動；不動 `down/`、`up/` 既有檔；不動其他 feature。
- **本包只生成 §3 指定之 9 TC**，不得擴列；不得寫回工作簿（Phase 5）。
- `_intake/Camera/` 不動（R-G46，Pei 清）。
- 不得自行擴 `gates_tsv.py`（歸 GC 線）。

## §1　裁定落檔（Pei 2026-09-16，全依審閱 §三）

取號現查：`{live}` 期望值 **10**（R-CAM1～9 已在）。

**(a) framework 簽核** —— `features/camera/framework.md` Part VIII 標題下改為：
```
**狀態：已簽核並鎖定。** Pei 於 2026-09-16 簽核（CAM-02 審閱 §三-5，Tier 2）。
  Layer 1  `Rear View Camera`（R-CAM4(a)）
  Layer 2  A 本 9 組 25／B 本 8 組 230；未歸屬 0、歧義 0
  Layer 3  A 本 = VF 章節號（V4 依 R-CAM7）＋ CFTS092 §1.3.x；B 本 = SYS1 Outline Number（RVC+PAM 依 R-CAM6）
簽核不涵蓋：TC 內容（pilot review）、`-023` 交集承接之實測結果、未結 DR。
重開條件同 vehicle_setting 之例；重開屬 Tier 2（Pei）。
```

**(b) R-CAM5(c) 修訂**（R-TM13：原句加刪除線保留，新句另立，body sha 重算）：
```text
R-CAM5(c)′  品牌 ↔ 車型之對照以 PROXI `Brand_Configuration_2`（byte 130 bit 0–4，Format row 566）
  之各平台實測值為據：HDCC27／DT27／637 = 11 (RAM)、Toro(2261) = 1 (Fiat)、Fastback(376) = 5 (Abarth)。
  `Brand-Specific Names` 分頁無該品牌欄或欄空者，回落基礎 label。
  Camera 設定項之品牌軸實為兩分支：RAM 系 {HDCC27, DT27, 637} vs 基礎 {2261, 376}，與車型軸對齊。
```
DR-CAM-e 結案（確認型）。

**(c) R-CAM{live}  `-023` 交集之承接順位**
```text
R-CAM{live}  多列共引同一來源時，SWE ID 較小者為承接列
  A 本兩列以上共引同一 SYS-RA 來源時，該來源之驗證由 SWE ID 較小者承接；
  較大者不得再以該來源作 spec_reference 錨，reasoning 註明「委派 SWE-CAM-nnn」（§8.2.1）。
  `SWE-CAM-023` 與六列之交集依此全數委派，`-023` 只承接無他列覆蓋之來源。
  pilot 批實測後若有不合理之委派，重開歸 Pei。
```

**(d) A-CA20 處置**（DECISIONS 6-9 `[DECIDED]`）：A、B 兩本各依自身來源寫 ER，reasoning 互註；**最終畫面文字以 `forms/Pop Up List HMI R1 (26PI).xlsx` 為準** —— 執行層本包查該表之 camera 相關列（`Camera Not in position`／`Camera Out of Position`／`Camera System Unavailable`／`Check Entire Surroundings`），逐字抄入 `data/popup_texts.tsv` 並回報 PU 號。

**(e) DECISIONS 6-8 品牌對照** 改 `[DECIDED]`（依 (b)）。

**(f) GC 線待辦登記**（`docs/fw036/handoff/GC_BACKLOG.md` 若無則建）：`gates_tsv.py` 讀 `FEATURE_CHECKS` ＋ 重生 tsv；`FEATURE_CHECKS` 機制之全域條文；9 個他線測試失敗。

## §2　pilot 批之通用規則

- canon 全套（IN ＋ FO）＋ `FW036_R1L_Camera_Profile.md`（lint `P`(v4)、`X`、`Z`）。
- **Vehicle Model 七欄**依 R-CAM2 填 1/0；車型軸依 R-CAM3 拆列；品牌軸依 R-CAM5′。
- **PROXI 前提**（§8.7.5(e)）：Atl-Hi 取 `forms/proxi/DT28_ATL_HI`／`HDCC28_ATL_HI` 之 default 值，Atl-Mi 取各平台 XLSM；至少 `PROXI Rear_View_Camera = 1 (Present)`；Atl-Hi 另 `PROXI Rear_View_Camera_Type = 1 (Digital)`。
- **CAN 記法**（§8.7.5(c)(d)）：Atl-Hi 用 `PDT27_E2A_R1_FDCAN8.dbc`（`TRANSM_FD_4.ShiftLeverPosition`、`BCM_FD_10.CmdIgnSts`、`BRAKE_FD_2.VehicleSpeedVSOSig`），Atl-Mi 用 `P363_BH-CAN…dbc`／`Project__637MCA…dbc`（`TRANSM2.ShiftLeverPosition`、`STATUS_BH_BCM2.CmdIgnSts`、`STATUS_CCAN3.VehicleSpeedVSOSig`）；`VAL_` label 逐字自 DBC；DBC 查無者依 R-13 保留來源名並登 DR。
- **Delay 設定**不走 CAN（CEH 不可注入）：以 `ENTER_CAMERA_SETTINGS`（R-CAM8）＋ `Set "<brand label>" = "On"` 為 setup hop；RAM 系 label `"ParkView Backup Camera Delay"`，基礎 `"Rear View Camera Delay"`（`*` 不入）。
- **門檻值**只取 CFTS092 `4781643`（`> 10s AND > 8 mph`）與 VF 原文，不取 037 之 `e.g.`（A-CA04）。
- 手動入口 hop 取 B 本來源 SYS1 HeadUnitCameraSystems `6.5.1`（controls page／apps drawer／camera app），逐字 label 由執行層自 `Basic Report` 抄出；查無 label 者 `PENDING: DR-CAM-{n} HMI entry path <target>`。
- Daemon／log 觀察步驟用 §5.4 兩行式（`$ adb …`）；命令不入 ER。
- `test_item` 上半 verbatim ≤ 50 token（取 SYS2 原句而非 037 摘要），下半英文括號 sibling token。
- 產物：`features/camera/generated/pilot01/NR1L-RVC-{nnn}.json` ＋ `.md`；ID 自 `NR1L-RVC-001` 起；`lint036.py --profile camera` 全綠方得上繳。

## §3　pilot 選列（9 TC）

| # | SWE | Test Set | 驗證點 | 軸 | 車型 1 |
|---:|---|---|---|---|---|
| 1 | `-001` | Startup and Shutdown | `CmdIgnSts` → RUN 後 NormalCameraDaemon 完成初始化（adb log 可觀察） | — | 全五 |
| 2 | `-001` | Startup and Shutdown | `CmdIgnSts` → OFF 後 daemon teardown（不含 PowerShutDownNotifcation，該點委派 `-012`） | — | 全五 |
| 3 | `-015` | Display Arbitration | `ShiftLeverPosition = R` → RVC 影像顯示 | **車型軸**：Atl-Hi | HDCC27, DT27 |
| 4 | `-015` | Display Arbitration | 同 3 | **車型軸**：Atl-Mi | 637, 2261, 376 |
| 5 | `-016` | Display Arbitration | Controls 頁手動開啟 RVC（hop 自 SYS1 6.5.1） | — | 全五 |
| 6 | `-016` | Display Arbitration | 手動開啟後按 `"X"` 關閉，回前一畫面 | — | 全五 |
| 7 | `-018` | Display Arbitration | Delay = On，R → D，影像維持至 10 s 後退出 | **品牌軸**：RAM label | HDCC27, DT27, 637 |
| 8 | `-018` | Display Arbitration | 同 7 | **品牌軸**：基礎 label | 2261, 376 |
| 9 | `-018` | Display Arbitration | Delay = On，R → D，速度達 8 mph（`= 8` 邊界）於 10 s 內 → 立即退出 | boundary | 全五（CAN 訊號依車型分寫於 Pre-Condition，若 ER 不同則拆並回報） |

#3／#4 之 `ShiftLeverPosition` raw 值與 `VAL_` label 各自逐字自對應 DBC；#9 之速度訊號同理。
Vehicle Model：598／5210 全列 0。

## §4　上繳要求（`up/20260916_CAM-03.md`）

1. §1 六項落檔：framework 狀態行、R-CAM5(c)′ 與 R-CAM10 之 sha8、DR-CAM-e 結案、`popup_texts.tsv`、DECISIONS 6-8／6-9、GC_BACKLOG。
2. 9 TC 之 json＋md 路徑；lint 全綠證據（含 Z）。
3. 每 TC 之 `reasoning` 須具名：來源 ObjectID／VF anchor、委派之 SWE、PROXI 值出處、DBC 檔名與 `VAL_` 列。
4. 執行中遇 DBC 查無、label 查無、Pop Up 查無 → `PENDING: DR-CAM-{n}` 並列表。
5. #9 是否因車型而拆之判斷與依據。
6. 自報錯誤。

## §5　升級條件

- 任一 hop label 於 SYS1 `6.5.1` 查無（不得猜，登 DR 停該 TC）。
- `TRANSM_FD_4.ShiftLeverPosition` 或 `TRANSM2.ShiftLeverPosition` 於對應 DBC 查無 `R` 之 `VAL_` label。
- lint 任一項無法轉綠而原因在 canon／lint 本身。
- 9 TC 之外任何列被觸及。
