# 下放包 CDD-05 —— Revise（R-4／R-DIAG13(amend)/15/16）＋ batch 3（Layer 2 #13～#17，70 列，I/O 母節收尾）

前件：`down/20260917_CDD-04.md`、`_A.md`、`up/20260917_CDD-04.md`、`down/20260917_CDD-04_review.md`。
依常規：審閱後即發；依據 = CDD-04 審閱 §三／§四。執行層 session：沿用 CDD 線。
指紋制：GC-16 未 commit → R-G75；已 commit → R-G76(b)。上繳註明。

---

## §0　禁區

- git 一律不動；建議先 commit CDD-04（up §9 訊息）。
- 不動 `down/`、`up/`；不動其他 feature 目錄。
- **只產 batch 3 之 70 列**；Revise 不重編號；其餘列不得生成。
- 條文本體不改；R-DIAG13(amend)／R-DIAG15／R-DIAG16 逐字落。
- 不為降紅改 TC 內容以外之東西而不記錄；不為降紅補多餘前提。
- `framework.md` 維持 DRAFT（LOCK 於 CDD-06，batch 3 後 I/O 母節全部落地，20 組已產 12 組）。

## §1　依據

`R-DIAG13(amend)`、`R-DIAG15`、`R-DIAG16` 條文逐字取自 `down/20260917_CDD-04_review.md` §四；
R-4（canon §4.3.1 末句）之句首大寫適用記於 profile。
追認：`-006` 佔位化、`-062` 以 `$5000` 為受測 DID、`-048` 不產 session 軸。

## §2　任務

**T0 — 條文與指紋**：三條落 `RULINGS.md`；指紋依 §首段之制回報數字。
另回報 `RULINGS.sha.tsv` 覆寫事件之**累計數與各次時點表**（CDD-03 記第四次、CDD-04 稱第五次）。

**T1 — Revise → `pilot01_v04`／`batch1_v03`／`batch2_v02`**
(a) R-4：合併本 133 列 `test_item` 上半小寫起首者句首轉大寫（至少 `-107`／`-108`）；下半不動。`J` 複驗 0。
(b) R-DIAG16：grep 四欄步驟起首 `Listen`／`Observe`／`Watch`／`Monitor`／`Check whether`／`See if`（不分大小寫），逐條改為動作／保持步 ＋ ER 觀察；至少 `NR1L-DIAG-102` 依審閱 §三-2 之寫法。回報命中清單與改法。
(c) R-DIAG13(amend)：`lint036.py` `SEC-DIAG` 母體加 `row_kind ≠ unsupported`；自測補 2 正 2 反；複驗 `SEC-DIAG` = 0。
(d) R-DIAG15：產 `sandbox/placeholder_summary.tsv`（欄：TC ID／SWE1 ID／欄位／佔位文字／token／來源節）；預期 pilot＋batch1＋batch2 之 `<…>` 全部登錄（batch 2 約 100+ 行）；`asset_request.md` 草稿（給 Nik：DR-DIAG-4 controlOptionRecord／controlEnableMask 全表、DR-DIAG-5 `$283F` 值域、DR-DIAG-1 餘 2 列 NRC）。
(e) profile：R-4 適用、`pc_sources` 字串區辨保守套用、寫入型不立 baseline、R-DIAG16。

**T2 — batch 3 sibling plan（先出後產）**

| Layer 2 | DID | 037 列 | 分析層預判 |
|---|---|---:|---|
| #13 Audio Tone Settings | `$5002`～`$5006` | 28 | volume 0–38、bass/treble/mid −9～+9、fade/balance 9–9：**每列已為單一設定**；positive 寫類依 CFTS004 值域拆 **BVA sibling**（=min／=max／min−1／max+1，只在 CFTS004 明載值域時；`-064`／`-066`／`-071` 之 `Valid values … shall be from` 即值域來源）；negative 二軸；unsupported 1。`-056`（`$180C` 併入本組，R-DIAG9(amend)(b)）讀類 1 |
| #14 Tuner Control | `$5008`、`$5009`、`$500C` | 22 | seek status／frequency step／SDAR station：R/W 讀與 I/O 寫各自成條；`-034`（invalid station → `$31`）CFTS004 明載，單條 |
| #15 Mode Selection | `$500A` | 9 | Mode 1／Mode 2 為 037 已分列；讀 1、寫各 1、negative 2、unsupported 1 |
| #16 In Motion Menu Control | `$500B` | 6 | enable／disable／5 s timeout 回復（`-012`）：timeout 之 `Hold for 5 s` 依 R-DIAG16 成步；**HMI 可觀察現象（menu enable/disable）只寫 CFTS004 明載者**，HMI 入口不在本 feature 範圍，不寫導航 hop |
| #17 Video Input | `$500D`、`$5100` | 5 | `-339` 切換視訊輸入、`-340-001`（pilot 沿用 `-013`）、`$5100` 三列 |

回看 CFTS004 各節：值域明載者 → BVA；controlOptionRecord 未載 → `<…>` 佔位 ＋ R-DIAG15 登錄；I/O 之 PC-3 依 R-DIAG13（unsupported 型不含）。

**T3 — batch 3 產出** → `sandbox/batch3/diagnostics_batch3_v01.xlsx`／`batch3.json`／`coverage.tsv`
- ID：pilot `-013` 沿用；新增自 `-134` 起連號；`-009`～`-012` 留號。
- BVA sibling 之 `test_item` 下半區分 token 為邊界值（`volume step = 38`／`= 39`）。
- 其餘規格同前包。

**T4 — lint／自檢／coverage**：四本各跑；`SEC-DIAG`／`KEY-DIAG`／`J` 皆 0；§9 17 項；R-SEC15 型；coverage 70/70。

**T5 — 合併本** `diagnostics_v03.xlsx` ＋ `placeholder_summary.tsv` 更新（含 batch 3）。

## §3　預期數字

| 項 | 預期 |
|---|---|
| batch 3 TC | 70 列 → 90–115（BVA 拆分視 CFTS004 值域明載列數）|
| PENDING | 0；佔位行登錄 100% |
| lint 真違規 | 0；`J`／`SEC-DIAG` 0 |
| 合併本 | ≈ 225–250 列；覆蓋 Layer 2 #6～#17（12 組／162 列，41.6%）|
| 指紋 | 60 → 63（或 R-G76 重生數）|

## §4　上繳要求（`up/20260917_CDD-05.md`）

1. T0 數字；覆寫事件累計表。
2. T1 五項：R-4 命中清單；R-DIAG16 命中清單與改法；`SEC-DIAG` 複驗；`placeholder_summary.tsv` 行數與 token 分佈；`asset_request.md` 草稿路徑。
3. T2 sibling plan 全表（含 BVA 依據列）。
4. T3 TC 清單；貼入 **一條 BVA sibling（=max 與 max+1 二條）** ＋ **`-012` 5 s timeout 一條** 全文。
5. T4 紅數歸因；自檢；coverage。
6. §3 對表；自報錯誤；未結 DR／FB；建議 commit。

## §5　升級條件

- 指紋集合不符／`--check` 不綠。
- R-DIAG16 grep 命中 > 20 條且改法需改 canon。
- BVA 拆分後任一 SWE1 列 > 6 TC（表示值域拆分過度，須回報不產）。
- 任何需改 canon 之情形。
