# 審閱 GC-07 上繳包 —— 本 session（A）之原版（2026-09-05）

> **落檔說明**：本審閱原以 `down/20260905_GC-07_review.md` 於 2026-09-05 21:18:37 落檔（4,340 B，`get_file_info` 核畢）。
> 21:22:48 該路徑被另一分析層 session（B）**覆寫**（7,680 B），內容為 B 之審閱（R-G70(e) PROXI 採 VF230 式、R-G72、GC-08 盤點包、A-GC15／16）。
> 依 PROTOCOL「目錄唯讀」，`down/` 不得覆寫；本檔以新名重落 A 版原文，**不動 B 版**，兩版並存待 Pei 裁何者為準。
> Pei 於 21:18–21:22 間之「追認 准 down/20260905_GC-07_review.md」所指版本，僅 Pei 可證。

---

（以下為 21:18:37 版原文，逐字）

# 審閱 GC-07 上繳包（2026-09-05，Tier 2）

對象：`up/20260905_GC-07.md`　下放包：`down/20260905_GC-07.md`（另一分析層 session 所立；Pei 2026-09-05 指示本 session 接手後續）
結果：**收受**。§三施作全收；§二 依 Pei 之口頭裁定（「之後都要這樣做，已繳的有需要再修改」）落為 R-G70；「已繳者回修」與 R-TM13 之關係於 §二 明訂。

## 一、§二 裁：**甲（v2 式回歸）**，立 R-G70

Pei 語料三本 P4 範圍 v3 式 0 次（上繳 §1）；VF230 全欄 81 處 v3 式全在 `test_item` verbatim 欄——即 v3 為**來源側**記法，v2 為**作者側**記法（上繳 1-1 之推論收受，明標為推論）。R-1 v3 之依據 CR30580/30581 庫內查無、不可複驗。三者合觀，甲成立。

R-G70 條文以下放包 §二擬文為準，**一處修訂 [DEFAULT]**：
(d) PROXI 之格式——Pei 指名「像 SWC 這份」，SWC 本為 `PROXI <Param> = <value>`（41 列，不加 `$`），故 (d) 改採 SWC 式為新產出之唯一式；VF230 之 `PROXI $Param$ is set to "value"`（152 列）列為既有本之同義舊式，不回修、新產出不採。(e) 之 `$<n>$` 為設定／參數標記維持（SWC 215 次實測）。

施作（GC-09）：§8.7.5 改 v4（v3 依 R-TM13 加註撤銷，理由指 GC-07 §二＋本審閱）；台帳 `### R-G70 —` 錨；lint P 判準回 `Send CAN:` 全域、`--profile` 之 v3 分支移除；`vehicle_setting` 之 `[OVERRIDE §8.7.5]`（R-VS52／R-VS67）標記「併入本體，作廢」；`pm_29` 207 列之轉換**只出 dry-run 對照表**（`old_text / new_text / row / col`），轉換本體在 PM 站 ⑤ 隨 Revise2 一併。

## 二、「已繳的有需要再進行修改」與 R-TM13

Pei 之指示為 Tier 3 裁定，R-TM13 不得擋。但 R-TM13 所護者為**歷史檔之位元不變**，非「交付內容不得演進」。二者並存之形：

```
R-TM14（交付本之回修 —— 全域，Pei 2026-09-05 口頭，分析層落文）
  已交付工作簿之檔案本身永不修改（R-TM13 不變）。回修一律以**新版次**出貨：
  依 REMEDIATION_DELIVERY_MGMT_20260821 §版次命名另立檔，附 old→new TC ID 對照表
  （同 PM 站 ⑤ 之 Revise2 形態），delivered/MANIFEST.tsv 新增一列並指前版。
  「有需要」之判準：以 lint 交付向報告為據 —— X（導航路徑）或 P（訊號寫法）紅數 > 0 之交付本
  為候選，逐本由 Pei 圈選；未圈選者維持原版。
```

執行層 GC-09 出候選表：全部 `features/*/delivered/*.xlsx`（`archive/forms_superseded` 之 Pei 手寫本不含）逐本跑 `--profile` 含 X 與 P（v4 判準），列 `feature / 檔 / sha8 / 資料列 / X / P / 建議`，供 Pei 圈選。**只報不改。**

## 三、上繳各節之裁

| 節 | 裁 |
|---|---|
| 1-2 Home `Press`／`Select` 口徑不符 | 以執行層 186／154 為準（R-G69）；下放包之數無口徑，不追 |
| 3-3 §5.3 二常數 PENDING | 收受；DR 草稿三問合式，**取號與送出屬 Pei** |
| 3-4 二例示訂正 | 收受；「例中每 hop 須有來源」入 §5.8 |
| 4-1 lint 代號 Q→X | 收受 |
| 4-2 X 為 profile-only | 維持至 R-TM14 候選表出爐；**新 feature 之 profile 一律啟用 X 與 P(v4)**，寫入 FO 接手清單 |
| 4-3 四本基線 X 956 | 收受為候選表之首批數字 |
| 5 DR 關閉狀態無機讀欄 | 收受建議：`status: open|sent|closed` 標頭行，格式先定不回填 |
| 6 既有 8 紅 | 非本包；專包先量後修 |
| 8-3 新舊並存 | 依 R-TM14：新產出用 HMI 標籤，舊本經圈選者以新版次轉換，未圈選者並存 |

## 四、待 Pei
1. R-G70 (d) PROXI 採 SWC 式 [DEFAULT]——追認
2. R-TM14 落文——追認
3. DR 草稿取號送出（vehicle_setting）
4. GC-09 准（含改 canon 本體與 lint P）
