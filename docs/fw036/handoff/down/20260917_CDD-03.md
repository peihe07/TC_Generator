# 下放包 CDD-03 —— pilot01 Revise ＋ DR-DIAG-3 內查 ＋ batch 1（Layer 2 #6～#10，31 列）

前件：`down/20260917_CDD-02.md`、`up/20260917_CDD-02.md`、`down/20260917_CDD-02_review.md`。
依常規：審閱後即發，依據 = CDD-02 審閱 §三／§四。執行層 session：沿用 CDD 線。

---

## §0　禁區

- git 一律不動；Pei 為唯一 git 操作者。**建議先 commit CDD-02**（up §11 訊息）。
- 不動 `down/`、`up/` 既有檔；不動其他 feature 目錄。
- `RULINGS.sha.tsv` 一律依 R-G75；上繳記三列數。
- **只產 §3 batch 1 之 31 列**；pilot 9 列以 Revise 方式改，不重新編號；其餘 358 列不得生成。
- 條文本體不改；審閱 §四三條以 `R-DIAG11`／`R-DIAG12`／`R-DIAG10(amend)` 落。
- Pre-Condition 每行必須能標來源；標不出者不得寫（§2 T1-2）。
- `framework.md` 維持 DRAFT；batch 1 上繳通過後 CDD-04 再 LOCK。

## §1　依據

- `R-DIAG11`（OOS 佔列 lint 豁免，列級）、`R-DIAG12`（`I-cross` 豁免）、`R-DIAG10(amend)`（無 P3；OOS 列 Priority 空）—— 條文逐字取自 `down/20260917_CDD-02_review.md` §四。
- R-DIAG10 本體追認；`-001` 不拆、`-272`／`-189` 不分項、`-154` 取 Description 皆追認（審閱 §一）。

## §2　任務

**T0 — 條文落檔與 lint**
(a) 三條落 `features/diagnostics/RULINGS.md`；`rulings_hash.py`；R-G75 寫表（預期 54 → 57）。
(b) `lint036.py`：R-DIAG11 列級豁免（條件 = `AF == "Out of Scope"`，豁免 `I`／`M`／`R`／`Z`）；R-DIAG12 `FEATURE_EXEMPT["diagnostics"] += ["I-cross"]`；`selfcheck_lint_diag.py` 各補 2 正 2 反例；回歸 `tests/`。
(c) 回報：全域 lint 有無 `specification_reference` 升冪檢查；無則只登 `GC_BACKLOG.md`，本包不加。

**T1 — pilot01 Revise → `sandbox/pilot01/diagnostics_pilot01_v02.xlsx`**
1. 13 條 `specification_reference` 家族內升冪複查並修（至少 `NR1L-DIAG-009`）。
2. Pre-Condition 逐行標來源於 JSON `reasoning`（格式 `PC-n: CFTS004-<ObjectID>` ／ `PC-n: SYSAD §x.y` ／ `PC-n: R-SEC<n> 前例` ／ `PC-n: PENDING`）。無來源者：
   - `Ignition ON` → 查 CFTS004／SYSAD；查無 → 併入 DR-DIAG-3 行，或依 power_moding 既有 Pre-Condition 寫法（`features/power_moding/` 交付本實測其 ignition 前提之措辭與來源）沿用並註前例。
   - `OBD-II connector` → 查 SYSAD 4.2.5；只載 ISO-TP over CAN or DoIP 者，改 `The diagnostic tool is connected to the HU diagnostic interface` 並註 SYSAD §；仍屬推論則 PENDING。
   - `-001` PC-4「camera display is active」→ 刪（§8.5）。
3. 同型修正套用至 13 條。

**T2 — DR-DIAG-3 內查（先於 batch 1 寫作）**
CFTS004 `Basic Report` 全欄（Description ＋ Comments ＋ 各 Description/Action 欄）grep：
`session`、`0x10`、`$10`、`extended`、`default`、`security access`、`$27`、`0x27`、`seed`、`key`、`tester present`、`$3E`。
逐命中列回報（row／SYS-RA-DIAG／ObjectID／原句節錄）。SYSAD docx 同 grep。
判定：命中且明載 session／security 要求 → R-DIAG5(amend)(c′) 第一層解，Pre-Condition 改為具體行（如 `The extended diagnostic session is active`，錨 CFTS004-<ObjectID>）；全無 → DR-DIAG-3 維持，行文改 `PENDING: DR-DIAG-3 diagnostic session and security access prerequisite`。
**結果決定 batch 1 之 Pre-Condition 寫法，故 T2 完成前不得開寫 batch 1。**

**T3 — batch 1 產出 → `sandbox/batch1/diagnostics_batch1_v01.xlsx` ＋ `batch1.json` ＋ `coverage.tsv`**

| Layer 2 | DID | 列 | 含 pilot |
|---|---|---:|---|
| #6 Camera Brightness | `$283F` | 6 | -001～-004（4）|
| #7 ECU Internal Settings | `$280C` | 8 | — |
| #8 Media Hub Config | `$2870` | 6 | — |
| #9 Button Interfaces | `$1820`, `$1821` | 7 | — |
| #10 Screen Test Pattern | `$1801` | 4 | -057（佔列）|
| **合計** | | **31** | 5 |

- 先出 sibling plan（每列預估 TC 數與軸，含 CFTS004 回看結果：值域／子欄位／狀態 → sibling 或不拆之理由），再產。
- pilot 5 列沿用 v02 內容，ID 不變（`NR1L-DIAG-001～005`、`-014`）；其餘依 framework 全序自 `-015` 起連號（pilot 之 -006～-013 屬後續批，**本批不重編**，留號）。
- `-340-002`（`$280C` Clock Defrost）在本批：R-DIAG2 ID 形式，`R1-DIAG` 驗。
- `$280C` 為 Read/Write 多設定 DID（Clock Defrost、12/24 h 等）：每 SWE1 列只追其子設定；寫入類依 -001 型立 baseline。
- `$1820`／`$1821` 按鍵狀態 DID：ER 只斷言 CFTS004 該列所述之按鍵集合；「not compatible → Not Pressed」（-055）為 negative 型之 sibling。
- `$1801` Screen Test Pattern R/W：寫入後之畫面現象只寫 CFTS004 明載者。
- 其餘規格同 CDD-02 §4（位元組式、Remarks 句式、Vehicle Model、Input Test Data NA、Priority 依 R-DIAG10(amend)）。

**T4 — lint、自檢、coverage**
`lint036.py --profile diagnostics` 對 v02 與 batch1 各跑；預期 `I`／`M`／`R`／`Z`／`I-cross` 於 OOS 列與全表皆 0；`U` 只餘 PENDING。§9 17 項；R-SEC15 型自檢。`coverage.tsv` 31 列全對映。

**T5 — 合併本** `sandbox/merged/diagnostics_v01.xlsx` = batch1（含 pilot 5 列）＋ pilot 其餘 8 條（`-006～-013`），共 31 ＋ 8 列來源；供後續批累加。

## §3　預期數字

| 項 | 預期 |
|---|---|
| batch 1 TC | 31 列 → 36–48 TC ＋ 1 佔列（pilot 4 列已知 5 TC）|
| PENDING 行 | 視 T2 結果：session 解出 → ≤ 8；未解 → ≈ TC 數 |
| lint | 真違規 0；`Z`／`I-cross` 0（R-DIAG11/12 後）|
| `RULINGS.sha.tsv` | 54 → 57（R-G75 三列數回報）|
| 合併本 | 13 ＋ 1 ＋ 新增 ≈ 45–60 列 |

## §4　上繳要求（`up/20260917_CDD-03.md`）

1. T0：三條 sha8；lint 兩項實作與自測；升冪檢查有無。
2. T1：13 條 Pre-Condition 來源表（TC × PC 行 × 來源）；修改 diff 摘要。
3. T2：grep 命中表與判定；DR-DIAG-3 現況。
4. T3：sibling plan 表；TC 清單；`-340-002` 一條全文 ＋ `$1820` 或 `$1821` 一條全文貼入。
5. T4：lint 紅數歸因；自檢表；coverage。
6. §3 預期 vs 實測；自報錯誤；未結 DR／FB。

## §5　升級條件

- `RULINGS.sha.tsv` 集合不符。
- T2 查得 session／security 要求且與 pilot 已寫之 Pre-Condition 矛盾以外之情形（矛盾即改，不升級）。
- batch 1 任一 Layer 2 組整組因來源全缺而 PENDING 超過 Pre-Condition 一行。
- 任何需改 canon 之情形。
