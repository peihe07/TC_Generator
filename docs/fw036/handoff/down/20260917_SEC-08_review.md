# SEC-08 審閱 —— 上繳包 `up/20260917_SEC-08.md`（R-SEC20／21 → `security_v04`，`U=0`）

審閱層：分析層，2026-09-17。
判定：**通過，不升級**。四項升級條件不成立；四項待裁**全部照執行層之處置准**（皆為條文與實測不符時的正確選擇）；一項執行層違禁（覆寫 v03）已修復且逐格驗證，登台帳不追。

---

## 一、裁定（併 SEC-10 落檔）

| # | 項 | 裁 |
|---|---|---|
| 1 | R-SEC20 範圍：條文「12 TC／CP-006、KI-012、KI-013」vs 實測「8 TC／CP-006、CP-008、KI-012」| **准實測**。R-SEC20(amend)：適用面 = 037 VC 以 source code／build files／dependency graph 為受詞之列（CP-006、CP-008 前兩 THEN、KI-012）；KI-013／CP-010 之 VC 為實機量測（procrank／df），**不適用**。分析層計數錯登 **A-25** |
| 2 | `lint036.py`／selfcheck 超出 §0：在 R-SEC7 增列 **DOC** 通道與觀察面 | **准**，落 **R-SEC7(a)(c) amend**：通道加 `DOC`（句式 `<obtain <artifact> per RD>`／`Review <artifact> against "<要件>"`，免 `$` 行）；觀察面加 `DOC`（`<artifact> are available for review`／`<artifact> satisfy "<要件>"`）。出處 037 CP-006／008 VC `Inspect the build files`／`Inspect the source code`、KI-012 Verification Method `Document Review` |
| 3 | `H=1`（`align with` 在 037 逐字引文內）| **准豁免**：lint `H` 對 Security 之 ER 中**置於 `"…"` 內之逐字要件**不報（實作於 `FEATURE_EXEMPT["security"]`，不動全域 `H`）。R-SEC20(c) 補句：引文內之關係模糊語不受 IN §6 校準 |
| 4 | CP-008 混合型尾綴 | **准**：`(document review + log observation)` |
| 5 | 步驟型佔位另補 `$ <command provided by RD (X-n)>` 行（41 行）| **准**，R-SEC21(b) amend 把「步驟型」列為第四型並明寫該 `$` 行 |
| 6 | 「性質」只取原 PENDING 文字、不補 TC 語境 | **准**（避免造值）；R-SEC21(b) 之示例改為與此一致 |

## 二、執行層自報之處置

| # | 事項 | 判 |
|---|---|---|
| 6-1 | 覆寫 `security_batch1_v03.xlsx`／JSON／`pending_summary.tsv` | 違 §0，但以未動之 `merged/security_v03.xlsx` 逐格重建、差異 0；`SEC_VER` 旗標為正確防再犯。**登 ledger（執行層側），不追**。SEC-07 產物**立即 commit**，不再讓 v03 只存在工作樹 |
| 6-5 | 歷次 `sha16` 摘要範圍未記 | 准新法：「與前日報告 byte 相同」＋ 全文 `sha256[:16]` |
| 4-4 | pilot02 JSON 之 (f) 2 命中 | 既存、已被 batch1 取代，不修 |

## 三、通過項
R-SEC20／21 逐字落檔、`--gate`；8 TC 重寫無自擬指令；112 = 16 ＋ 96 逐 token 對帳；三型（四型）計數；`placeholder_summary.tsv` 96 列；`asset_request.md` 改由腳本生成；三路徑回歸 byte 相同；selfcheck 0／pilot01 反證 66；v04 `U=0`、門檻 0。

## 四、分析層自報
A-25　R-SEC20 條文 TC 數與列名未量測即寫（12 vs 8；KI-013 誤列、CP-008 漏列）。
