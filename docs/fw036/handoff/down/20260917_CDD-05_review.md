# CDD-05 審閱 —— Revise ＋ batch 3（I/O 母節收尾）

審閱對象：`up/20260917_CDD-05.md`。審閱層 session：2026-09-17。
**結論：通過。** batch 3 = 98 TC，合併本 231 列（41.6%），I/O 母節 114 列全部落地；PENDING 0、佔位登錄 100%；lint 真違規 0；§9 17/17。
**兩處內容修正併入 CDD-06**（§三）；`X` 豁免以裁定落（§四）。

---

## 一　逐項核對

| up 節 | 判 | 備註 |
|---|---|---|
| §1.2 覆寫累計表：五次，無漂移 | 通過 | 分析層之疑慮來自只讀 CDD-03 §1.2 首句（A18）；第 4／5 次分別是 R-G75(a)(b) 兩缺口之實例 —— 此結論寫入 GC-16 §1 |
| §2.1 R-4 由產生器施加 | 通過 | 結構修 |
| §2.2 R-DIAG16 16 行／13 條；`-118` 以 §6.1 a./b. 保決策表 | 通過 | |
| §2.3 `SEC-DIAG` 排除集 33 列、未補 PC-3 | 通過 | 10 項自測全 PASS |
| §2.4 `placeholder_summary.tsv` 213 行；新開 DR-DIAG-6 `<unsupported SID>` | **通過，DR-DIAG-6 追認** | 「哪個 SID 不被支援」確為 ECU 實作事實，SYSAD 只正列支援項；`asset_request.md` 三項 |
| §3.1 BVA 兩式（有 negative 列 → 二點；無 → 四點） | **追認** | 越界只在無處可掛時掛回值域列，避免與 negative 值域軸重複 —— 判準成立；`$5005`/`$5006` 無 negative 列 → FB-n |
| §3.2 98 TC；`-034` 單條；#16 不寫導航 hop | 通過 | |
| §4.1 `X=12` 結構性誤報 | 見 §四 | |
| §4.1 `H` `normal operation` 改補集陳述 | 通過 | 正確：以 4940358 之否定為可觀察斷言 |
| §4.2 baseline 第三犯 → 產生器守門 `SystemExit` | 通過 | 「該加的是守門，不是第三次記取」—— 收；同法適用於本審閱 §三-1 |
| §5 刪被取代之 sandbox 工作簿 | 通過 | 只限 sandbox；交付本不適用（R-G72） |

## 二　分析層自報錯誤

| # | 內容 |
|---|---|
| A18 | CDD-05 T0 疑計數漂移，未讀完 CDD-03 §1.2 全段即提問 |
| A19 | CDD-05 T2 書 BVA「四點」未先考慮同 DID 之 negative 列已涵蓋越界；執行層之兩式判準優於下放包 |

## 三　內容修正（併入 CDD-06）

**(1) Pre-Condition 內之「recorded as `*_initial`」** —— `NR1L-DIAG-…`（`-064` 越界條，up §7.2）PC-4
`The fade control step is recorded as Setting_initial`：`recorded` 為動作，違 IN §4.4（Pre-Condition 只述狀態，需 *do／check／record* 者不是 Pre-Condition）。
守門只查「宣告而未用」，未查「宣告之位置」。
改法（§5.6 baseline 型）：Procedure 首步 `Read the fade control step` ＋ `$ 22 50 06` ＋ `record as Setting_initial`；ER-1 `Setting_initial is recorded`；後續步驟號順延。
**全合併本 grep Pre-Condition 含 `record`／`recorded`／`read`／`measured` 者，同法改；產生器守門加第二條：`*_initial` 只得於 Procedure 宣告。**

**(2) `-012`（up §7.3）PC-4 為步驟控制之狀態** —— `The navigation in-motion menu items are enabled by a diagnostic command` 由步驟 1 建立，屬 IN §4.4 禁用之「step-controlled state」。刪 PC-4；步驟 1 即建立該態。
**全合併本 grep Pre-Condition 含 `by a diagnostic command`／`has been sent`／`was sent` 者，同法。**

## 四　審閱裁定（依常規即生效）

```text
R-DIAG17  lint X 之 Diagnostics 豁免
  比照 R-DIAG12：FEATURE_EXEMPT["diagnostics"] 加 `X`。理由：本 feature 不寫導航 hop，X 之命中皆為 DID 名／CFTS004 用詞之字面。
```

```text
R-DIAG18  Pre-Condition 之機械守門（IN §4.4 之本 feature 實作）
  產生器與 lint `PC-DIAG`：Pre-Condition 行含 record／recorded／read／measure／send／sent／press／
  by a diagnostic command 者 FAIL；`*_initial` 只得於 Procedure 宣告並於 ER 使用。
```

BVA 兩式、DR-DIAG-6、`-118` 決策表寫法：追認。

## 五　待 Pei

1. GC-16 仍待兩線停止（Security 已推進至 SEC-15，無後續包，**現在是停 Security session 的時點**）。
2. FB-DIAG-a～n 送出（k 對 SYSAD 作者）；`asset_request.md` 三項（DR-DIAG-4/5/6）送 Nik。
3. commit：建議准，訊息沿 up §9。

## 六　CDD-06 隨本審閱落檔

= §三兩處 Revise ＋ R-DIAG17/18 ＋ **framework LOCK v01** ＋ **batch 4 = Layer 2 #5 GPS Signal Data（34）＋ #18 AV Signal Detection（10）＋ #19 Audio Measure AC（3）＋ #20 Clear Key Sense PIN（1）**，48 列。
後續：CDD-07 = #3 SXM Subscription（37）＋ #4 SXM Package Routines（13）；CDD-08 = #1 SXM Module Version（57）；CDD-09 = #2 SXM Signal Quality（72）；CDD-10 = 交付候選。
