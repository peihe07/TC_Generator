# 下放包 CAM-28 審閱（分析層 2026-09-24）

對應上繳包：`docs/fw036/handoff/up/20260924_CAM-28.md`
判定：**通過。TC 0 列改動，兩本候選不變。** 下放包三處前提錯誤全為分析層之錯（§一）。三項對帳之裁決見 §二；§6-3 附帶量測揭出之 92 個無紀錄來源**須開覆核包**（CAM-29 同發）。

---

## §一　分析層自身錯誤

| 處 | 錯 | 成因 |
|---|---|---|
| 「VF551_V2 R1 → R2」 | `sources/raw/` 既有本 Document ID 本即 `VF551_V2_R2` | 分析層以 CAM-01 時自建之 `sys2_map.json` 為「R1」，未查其 Document ID 欄 |
| 「93 列 Description 改」 | 94 列，全為 `_x000D_`／空白，正規化後 0 | 分析層逐字比對未正規化（違反自己於 R-CAM17 註「取全文」之精神——取了全文卻未去噪） |
| 「V3 新 1 列」 | 0 | 同上，表頭列計入 |

記 A-CA40（分析層三處誤判，執行層依實測更正）。sha16 五個全符、VF508／V9 範圍外之判定屬實。

## §二　裁決

| # | 項 | 裁決 |
|---|---|---|
| 1 | **PARTIAL 78** | (i) `[not in source]` 25 → **不補**（§8.4.1：RD 自加之點非需求）；登 **RDF-19**「RD 驗證標準含來源未載之點（25 項，附表）」供 RD 決定是否入規格。(ii) 含來源所載之點 53 → 執行層**以全案 465 列（非只同錨）重判**，仍缺者為真缺口 → 清單回報，Pei 裁補列。此為 CAM-29 §2 |
| 2 | **RD-ONLY 11** | **不補**：其手段（fault injection 全態、timing 掃描、示波器）超出來源文字所定之驗證點；登 RDF-19 同表，並於 `bench_verify.md` 加註「RD 建議手段」供實機階段參考 |
| 3 | **車型差異 22 TC** | **維持我方勾選**：RD 欄為功能層適用性、我方為該列可佈性（PROXI byte／DBC／品牌），依據逐列具名；DECISIONS 6-90 記「RD 車型欄不覆寫 R-CAM2／3／5／13(c)／18」。Remarks 不加 |
| 4 | **92 個無紀錄來源**（`-015`／`-016`／`-018`） | **開覆核包**（CAM-29 §1）：R-CAM13(a) 要求每來源至少落一 TC 驗證點；plan 未記 ≠ 未覆蓋，但無紀錄即不可證。逐來源以內容配對既有 TC → `COVERED-BY`／`GAP`；`GAP` 為補列候選，Pei 裁 |
| 5 | A dryrun5 不產 | 准 |
| 6 | MANIFEST note 依實測 | 准 |
| 7 | NO-TC 42 中「無 plan 紀錄」28 | 併入 §二-4 之覆核母體（同型問題） |

## §三　R-CAM17 證據複驗

隨機取證據 1（既有本 Document ID 即 R2）：分析層以 CAM-01 時所讀 REF `SYS2_VF551_V2_…_V01_0525.xlsx` 之 F 欄 anchor（`VF551_V2_PHDCC27_VF_484` 型）無法判版次，故當時未察；執行層以 `Document ID` 欄實測為準，屬實。

## §四　備查

- 全案交付閘不變：13 條 DR／105 列 bench／Phase 7。
- CAM-29 為覆核包，不生成；其 `GAP` 清單若 Pei 裁補列，再開生成包（第五種觸發：覆核補列）。
