# 審閱 —— 上繳包 VS-SL-02（dry-run v2 ＋ BL DT 查證）

日期：2026-09-02　層級：Tier 2　對象：`up/20260902_VS-SL-02.md` 及其 §2 所列 12 項產物

判定：**准。** v2 結構收訖，dry-run 階段完成。剩餘全為 Pei 決策項（§3），裁畢即可出 v3（綁別名重跑，機械性）與寫回包。

---

## §1　Tier 2 覆驗（抽驗，非全信上繳自檢）

- `vf230_settings_dryrun_v2.tsv`：SHA256 前 16 `954c7e76ab74566f`、95,970 bytes，與上繳表逐字相符
- 457 列；分支自 flags 反解 = **152／96／30／179**，與 §3 表逐項相符
- assert 2（`PROXI_PENDING` ∩ `ALIAS_UNRESOLVED`）= **0**；assert 3（`proxi_now` 非空而 PENDING）= **0**；PENDING 總數 = **30** —— 皆覆驗屬實
- FCW 抽驗：r150 負向 `= 0 (Absent)`＋`NEG_CONTRA`、r151 正向 `= 3 (…with Mitigation)`，與 §4 樣張一致
- 審閱 VS-SL-01 §2 三缺陷（55 列、165 列、92 列未分流）確認全數消除

執行層三項自報（DR-49 退號、95 列抽取式補正、DT_APPLICABILITY 31→23 更正）皆屬正辦，收訖；`DJD2_ONLY` 8 列另掛之處置**准**。

## §2　BL DT 查證之定性（依 up/ §1，8 檔：有 1／無 1／未提及 6）

衝突現已收斂為**需求側 vs 架構側**，非文件抄錄之誤：

- 架構側：总控表與其原始來源 Appendix BV（在磁碟上，`41ce5544e1a8c8e7`）一致 —— Atlantis 與 PNet 之 DT 欄皆排除 DT
- 需求側：037 報告 r8–r12 五條 DT 專屬 shall、SYS1 use case 逐字分列 DT 與 DJ/D2

**本層建議**（canon §8.1：TC 追溯需求；衝突時需求勝，並向上游 flag）：
(a) DT 專屬 7 列與「DT 或 DJ/D2」16 列**保留不刪**（其追溯之 shall 仍在）；
(b) 開 DR 向上游問 SWE1（037）與 Sys3（Appendix BV r41）對 DT 之取捨，附兩側逐字；
(c) 車型欄 T（HDCC27）／U（DT27）之值待 DR 回覆再填 —— 併同一事實：**BL 工作簿 T–Z 七欄 151 列全空**，本身即為寫回包必修項。
另：`R1L_RTM_V3` 下 SHA 相異之 SYSAD 副本（少 3,166 bytes）不展開，僅記錄。

## §3　待 Pei 之決策（裁畢本案即進 v3）

| # | 事項 | 一句話可裁 |
|---|---|---|
| 1 | 別名 44 名 `manual`（`down/20260902_VS-SL-02_alias_candidates.tsv`，其中 9 名近義標「待認」） | 認可即解鎖 176 列；可整批「准」或圈掉個別名 |
| 2 | DR 草稿送出（9 名別名查無涉 37 列＋VC Camera App 衝突＋BL DT §2(b)） | 「送」即由本層併稿成單一 DR 文面供 Pei 轉寄 |
| 3 | 編號空間：已落檔為 `R-VS84–88`／`A-VS166–168`；本層原建議 `R-VF` | 改號有台帳搬移成本；**維持 R-VS 亦可接受**（已落檔且互引），Pei 一字定案 |
| 4 | BL DT 三步處置（§2 a/b/c） | 准／改 |
| 5 | 寫回範圍確認：非 NAFTA 19 列移除、PROXI 形制 152 處、NEG_CONTRA 3 列改 `0 (Absent)`、Options 正規化、BL node 27 行、車型欄補填 | 此前已裁者之執行確認，隨 v3 併入寫回包 |

## §4　後續序

1. Pei 裁 §3 → 本層出 `down/` v3 包（綁別名重跑＋寫回清單定稿）
2. v3 上繳准後出**寫回包**（Revise；含 TestRail 舊→新 ID 對照，R-G42）
3. VF665 生成自本輪起直接套配方（`settings_lookup` 已可用於 exact＋已認 manual）
