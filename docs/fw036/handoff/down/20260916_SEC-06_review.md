# SEC-06 審閱 —— 上繳包 `up/20260916_SEC-06.md`（Revise v03 ＋ merged v02 ＋ 交付候選三件組）

審閱層：分析層，2026-09-16。
判定：**通過，不升級**。四項升級條件皆不成立。執行層對兩處下放包前提錯誤之處置（不動 `Reference`／`ChangeHistory`、回報而非照辦）**正確**；三個封面格留空不造值**正確**。
**Security 交付候選已成**：100 TC／51 列覆蓋／128 PENDING／12 token；後續動作全在 Pei 側，**暫無下一下放包**。

---

## 一、通過項

| 項 | 判 |
|---|---|
| R-SEC16(amend)／17(v03)／18(amend)／19 落檔，`--gate` 通過 | ✅；**amend 命名式定為括號子號**（`R-SECn(amend)`／`(v03)`），與 `(a)`/`(c)` 同族；profile 記入 |
| framework v03：一列移組、29 組、70 leaf、LOCKED v03 | ✅ |
| batch2 v02 diff 20 格全在範圍；merged v02 PENDING 128 | ✅ |
| PENDING 130→128 之修正（R-SEC19「ER 對應行」只指觸發步驟之 ER）| ✅ 執行層讀法正確 |
| `selfcheck` (b) 改「凡整行 PENDING 之 proc 步驟皆為觸發位」；pilot01 反證 66 不變 | ✅ 准 |
| 封面 11 格填值、zip 48→48、DV 不變 | ✅ |
| 檔名式量測與最終檔名 `…_SWQT_Security_20260916.xlsx` | ✅ |
| `delivery/` 之 tsv 為交付快照、權威在 `data/` | ✅ 准；MANIFEST 不登（非來源）|
| `asset_request.md` 12 token 含急迫序與找誰 | ✅ |

## 二、分析層自報（登台帳）

| # | 錯誤 | 說明 |
|---|---|---|
| A-20 | SEC-06 §3.2 令 `Reference` sheet 列文件來源 | 該 sheet 為模板之設計方法說明表（SWC／Home 35 格逐字相同）。**裁**：交付本**不設**文件參考清單——SWC／Home 先例皆無，來源追溯由 `sources/MANIFEST.tsv` ＋ `coverage.tsv` 承擔，不進 workbook |
| A-21 | SEC-06 §3.1 把 `ChangeHistory` 當 feature 文件史 | 其為表單 FM-WI-FSM-036-A01 之欄位修訂史，逐 feature 不填。**裁**：不動 |

## 三、待 Pei 親填／親裁（不經執行層）

| 格 | 建議 |
|---|---|
| Cover `D7` 核准者、`D8` 審查者 | Pei 依 Security 之實際簽核鏈填；執行層不得代填（正確）|
| Cover `D9` 中文半 | Pei 補中文姓名 |
| Cover `C12` 文件名 | **建議採 Home 之逐 feature 式**：`FM-WI-FSM-036-A01 STLA 測試用例規範和結果_SWQT STLA Test Case Specification & Result_SWQT_Security`。理由：workbook 本就逐 feature 交付，泛用名 = 模板原值，無識別力；「以 SWC 為準」是欄位形制之規則，不適用於文件名 |

## 四、Security 現況總結（交付候選）

| 項 | 值 |
|---|---|
| 母體 | 6 本 037，70 列 |
| 產出 | 51 列 → **100 TC**（CP 23／KI 25／SAM 31／ECUC 12／SWDL 6／LOGENC 3）|
| DEFERRED | 19 列（Peer Review `N`，待上游）|
| Priority | P0 28／P1 53／P2 19 |
| PENDING | 128 行／12 token，全歸 EXEC_ASSETS |
| lint | `K`／`P`／`X`／`SC`／`SS` = 0；selfcheck 九條 0 |
| 交付候選 | `sandbox/delivery/` 三件組 ＋ `asset_request.md` |
| 裁定 | R-SEC1~19 ＋ 5 amend；framework LOCKED v03 |
| 分析層自報錯誤 | A-1~A-21 |

## 五、下一步（全在 Pei 側；下一下放包待觸發）

1. **送 `asset_request.md`**：急迫序 `X-c`／`X-d`（9/18）→ `X-e`／`X-e-2`／`X-e-3`／`X-f-2`／`X-h`（前批）→ 其餘。
2. **裁 `X-i`**（16 行、12 TC，全在 P2 的 CP-006／KI-012/013）：建議**裁降為文件審查**，屆時 SEC-07 把該 12 TC 之 PENDING 改為 Remarks 註記、ER 改「Reviewed against <source>」型（需一條 R-SEC20 定式）。
3. **RD review**：把 `…_SWQT_Security_20260916.xlsx` 給 Steven／Samuel，PENDING 位置即問題清單；不算出貨。
4. 資產到手一批 → **SEC-07 Revise**（PENDING 補值、`Y/N` 重判、lint 全綠）；DEFERRED 19 列依上游回覆分批解凍。
5. 交付前依 IN §8.4.3：PENDING 全結或 Pei 逐 token 裁降；**含 PENDING 不出貨**。

git：本包產物一次 commit，建議 `feat(security): SEC-06 — Revise v03, merged v02 (100 TC), delivery candidate`；`docs(security): SEC-06 package, submission and review` 分開。
