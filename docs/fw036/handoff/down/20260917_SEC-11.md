# 下放包 SEC-11 —— DEFERRED 19 列以文件審查定式產出 → 70／70 全覆蓋（`security_v06`）

前置：`up/20260917_SEC-10.md`、`down/20260917_SEC-10_review.md`。
Pei 2026-09-17 指示「請先繼續產出」——分析層解讀為：**不等上游回覆，DEFERRED 19 列依 R-SEC20 文件審查定式先產出**，使 70 列全部有 TC；上游回 DR-a／i／r 後再 Revise。本解讀寫入 R-SEC23，Pei 若不同意於審閱時撤。
執行層 session：新開；先落 SEC-10 四支 commit。

---

## §0　禁區

- `down/`／`up/` 既有檔不動；canon 不動；`framework.md` 不動（LOCKED v03；19 列之 Test Set 已在 `layer2_assign.tsv`）。
- `lint036.py` 不動；`selfcheck` 不動。
- 既有 xlsx 不動；v06 為新檔。
- 19 列**只能**依 R-SEC20 定式產出；任一列之 037 VC 若含可執行觀察（`adb`／`logcat`／檔案路徑／UDS／openssl），**不得**以文件審查降級——列出並回報，留待裁。
- `W=4` 四段本包不動。

## §1　裁定落檔（取號現查；預期 R-SEC23）

```text
R-SEC23  DEFERRED 列之產出（R-SEC11(b) 之解凍）
  (a) `disposition = D` 之 19 列（LOGENC 001/002/003/004/005/007/008/009；ECUCert SERVICE_BINDER／ECUONLINE_BINDER／
      ECUCERT_JNI／STORAGE_IO／ECUONLINE／ECUONLINE_DOWNLOADCERT_INTF；SWDL 001/002/005；SAM 0001/0019）
      依 R-SEC20 文件審查定式產出 TC；`disposition` 改 `CONVERT_DOC`。
  (b) sibling：037 `Verification Criteria` 之每一可獨立審查之要件（THEN 子句或列舉項）= 1 TC；無 VC 而只有 Description 者，
      以 Description 之每一 `shall` 子句為要件。artifact 依 037 受詞逐字（`source code`／`library`／`interface`／`build files`）。
  (c) Priority 依 R-SEC16 = P2；Design Method `功能測試 (Functional based ; no specific technique)`；`test_item` 下半加 `(document review)`。
  (d) Remarks 首行 `verification: document review (R-SEC23; upstream DR-a/i/r pending)`；ECUCert 六列之 ID 依 R-SEC3(b) 用 SYSAD 首值。
  (e) 上游回覆後之處置：DR-a（ECUCert 補 SWE ID）→ Revise 只改 ID 欄；DR-i／r → 不影響本群；若 037 作者將任一列改為 Testing
      且給出可執行 VC，該列依 R-SEC4 重寫，文件審查 TC 作廢（R-TM13 保留舊本）。
```

## §2　產出

1. `sibling_plan_deferred.tsv` 先出（欄同 SEC-04 4.2；`rule` 欄記 (b) 之要件來源）。預估 19 列 × 1~3 = **25~40 TC**；門檻 20~50。
2. `gen_deferred.py`（冪等，`SEC_VER=v06`）→ `sandbox/batch3/security_batch3_v01.xlsx`（ID 各組自現行末號接續：LOGENC 自 004、ECUC 自 013、SWDL 自 007、SAM 自 032）。
3. `build_merged.py` → `merged/security_v06.xlsx`；`delivery/…_SWQT_Security_20260917.xlsx` 同名覆蓋。
4. `coverage.tsv`：70 列 `tc_count>0` = **70**；`batch_order.tsv` 之 19 列 `disposition` → `CONVERT_DOC`。
5. `peer_review_rows.tsv` 之 19 列 `disposition` 同步。
6. 順帶小整理（SEC-10 review §一 #2／#4）：`audit_newtc.tsv` 加 `ACCEPTED_GAP` 類（`NEW-CERT-02`／`NEW-ID-01`）；`ANOMALIES.md` 合併重複段。

## §3　R-SEC20 定式之逐列 artifact／要件（分析層自 037 逐字預判；執行層以 037 複核，不符者以 037 為準並回報）

| 037 列 | artifact（受詞）| 要件來源 |
|---|---|---|
| LOGENC-001 | the libLogEncrypt library | Description 之 shall 子句 |
| LOGENC-002 | the Logdog daemon integration | `linked by logdog daemon` |
| LOGENC-003 | the log file encryption APIs | VC 之 API 列舉，每 API 一 TC |
| LOGENC-004 | the AES encryption implementation | VC 之演算法／模式要件 |
| LOGENC-005 | the RSA key wrapping implementation | 同上 |
| LOGENC-007 | the KeyAgent interface | 介面要件 |
| LOGENC-008 | the KeyAsset interface（RSA key from KeyInstall）| 介面要件 |
| LOGENC-009 | the storage interface | 介面要件 |
| ECUCert `…SERVICE_BINDER` | the ECU Cert service Binder interface | VC 之 jbyteArray／參數要件 |
| ECUCert `…ECUONLINE_BINDER` | the ECU Online Binder interface | 同上 |
| ECUCert `…ECUCERT_JNI` | the ECU Cert JNI layer | VC 之 uint8_t*／memory 要件 |
| ECUCert `…STORAGE_IO` | the ECU Cert storage I/O implementation | VC 之路徑／權限要件 |
| ECUCert `…ECUONLINE` | the ECU Online module | VC 之連線／重試要件 |
| ECUCert `…ECUONLINE_DOWNLOADCERT_INTF` | the certificate download interface | VC 之下載要件（STLA 連線本身不驗）|
| SWDL-001 | the SwdlSecureLib library | 總則 shall 子句 |
| SWDL-002 | the key retrieval implementation | VC `input parameter range` 要件 |
| SWDL-005 | the key handling implementation | 同上 |
| SAM-0001 | the DebugAuth process configuration | Description 之部署要件 |
| SAM-0019 | the SAM environment configuration | 同上 |

## §4　上繳要求（`up/20260917_SEC-11.md`）

1. SEC-10 四支 commit hash；R-SEC23 實際號、sha8、`--gate`。
2. `sibling_plan_deferred.tsv` 計數（各組）；§3 表與 037 複核之差異逐列。
3. batch3 TC 數；merged v06 TC 總數（預期 100 ＋ batch3）、六組各數、Priority 分佈；`coverage.tsv` 70／70。
4. lint 全項（`U=0`、`H=0`、門檻 0、`W=4`）；selfcheck 0；三本 `surgical_save` 計數。
5. §0 第 4 條之列表（VC 含可執行觀察者，預期 0）。
6. `ACCEPTED_GAP`／ANOMALIES 合併之結果。
7. 自報錯誤。

## §5　升級條件

- batch3 TC 數 < 20 或 > 50。
- 任一 DEFERRED 列之 VC 含可執行觀察而被寫成文件審查（§0 第 4 條）。
- `coverage.tsv` `tc_count>0` ≠ 70。
- lint 任一門檻非 0；三路徑回歸 sha 改變（本包不動 lint，仍跑一次確認）。
