# 下放包 CAM-26 審閱（分析層 2026-09-24）

對應上繳包：`docs/fw036/handoff/up/20260924_CAM-26.md`
判定：**通過。** 十列零 TC 需求各出一列，全案 465 TC、工作簿列 ＝ TC 數、追溯 A 25/25／B 203/203。執行層對下放包五處事實出入之處置（依來源出列、不照錯誤指示）正確。CAM-27（收尾小包）同發。

---

## §一　DECISIONS 6-87 六項裁決

| # | 項 | 裁決 |
|---|---|---|
| 1 | 下放包委派對象寫錯（`-025` → `-020`、`-013` 分委 `-006`／`-004`） | 分析層之錯（未查 `batch03_plan.tsv` 即寫），A-CA38 記實；執行層依實際為準 |
| 2 | `-007`／`-013` 之驗證角度改依來源（無 handshake；`diagnosticRequest` 內容 PENDING DR-CAM-t） | **准**；DR-CAM-t 由 Pei 送 |
| 3 | **R-CAM19(b) 適用範圍** | **只補原零 TC 之列**。追溯要求是「每條需求至少一個 TC 掛其 ID」；已有 TC 之需求（如 `-004`／`-009`）其共引來源不必再各自出，否則為 §8.2.1 之 duplicate traceability。R-CAM19 加 (f) 段明文 |
| 4 | `NR1L-RVCHMI-087` 車型勾錯（`Digital_CHMSL_Camera_Prsnt` 只在 Atl-Hi） | **補正**：637／2261／376 改 `0`，原地改，逐列 diff；A-CA39 RESOLVED。既有列之事實錯誤優先於「不改既有列」之包內禁區 |
| 5 | dryrun4 帶入 `-245` Remarks | 接受（CAM-25 落差之自然收斂） |
| 6 | RDF-18／GCB-13 未落 repo | 分析層漏派；CAM-27 補 |

補充：`-248`（`SWE-CAM-024`）以六欄 PENDING 之 TC 列取代 D 欄佔位 —— 接受（R-CAM19(d)），阻交付 DR-CAM-a 之列數因而 ＋1。

## §二　R-CAM17 證據複驗

隨機取 §1-2 fenced sha 不變之主張：R-CAM10 `1ef47db1`、R-CAM16 `cb146bbb` 與 CAM-11／CAM-17 上繳所記一致，屬實。

## §三　終態（本包後）

| | A | B | 合計 |
|---|---:|---:|---:|
| 需求列 | 25 | 230（leaf 203 ＋ umbrella 27） | 255 |
| 有 TC 之需求 | 25 | 203 | 228 |
| TC ＝ 工作簿列 | 249 | 216 | **465** |
| 含 PENDING 之 TC | 47 | 34 | 81 |
| 阻交付 DR | | | 13 條（a／f／g／h／i／j／k／m／p／q／r／s／t） |

commit 歸 Pei；建議之 pathspec 可用。
