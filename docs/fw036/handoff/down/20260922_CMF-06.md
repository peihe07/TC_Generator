# 下放包 CMF-06 — 交付候選本（EMEA 兩條修正 ＋ 封面／履歷／Product Document）→ `sandbox/delivery/`

- 日期：2026-09-22
- 前置：`down/20260922_CMF-05_review.md`；Pei 2026-09-22：「維持，其他就照你建議」。
- 產物落點：`features/comfort/sandbox/delivery/`。**不入 `delivered/`、不 commit、不跑 `--gate`／`--regen`、不重跑 generator。**
- 開工前 `git status`：CMF-05 產物是否已提交，照列。

## 1. 裁定落帳

| # | 條文 |
|---|---|
| R-C44(confirm) | Pei 2026-09-22「維持」：R-C44 三問為本線拆解之終局判準，2026-09-17 全域標準不覆蓋之；13 條候選不產，476 條定案 |
| R-C65(amend) | DBC 舉證範圍 ＝ 該 TC 車型欄為 1 之車型 |
| R-C60(amend) | 合稱增列 `Face + Feet` |
| R-C66 | 導航改寫（`nav_rewrite_plan.tsv`）**不在本次交付內**；待 canon §5.3 增 `ENTER_COMFORT`（Pei 改原檔）與 DR-49 回覆後另發 Revise。DR-49 於本包登記（文字取 CMF-03 所備），標 open，由 Pei 送出 |
| R-C67 | **EMEA ICS：本交付三台皆非 EMEA ICS 車** —— 此為分析層依 Pei「照你建議」所取之讀法（三台為 NA／LATAM），**未經 Pei 逐字確認**；`ANOMALIES.md` 記為待確認假設。影響僅 2 列 × 3 格，若 Pei 反轉即回填 |

## 2. 內容修正（JSON 層 → 候選本）

- `121-xx` 正向 EMEA ICS 之 2 條（`-403`／`-404`）：V／Y／Z 改 0（R-C67）；`vm_assign.tsv` 對應列更新分支為 `R-C67`。
- 426 條排除式 PC 之車型欄不動。
- 其餘內容與 v4 逐格相同。

## 3. 交付候選本

1. 母本 ＝ 0907 本複本，套 v4 全部變更 ＋ §2。檔名 `…_SWQT_ComfortHMI_20260922.xlsx`（去 `_v4`）。
2. **Cover／ChangeHistory／Product Document／Reference 分頁**：先量 SWC 0708 與 Home 0809 兩本同名分頁之每格（哪些格有值、格式），照其填；`Cover_old`／`ChangeHistory_old` 兩分頁之處置照 0907 本現況（不刪不動）。ChangeHistory 追加一列：日期 2026-09-22、版本依 0907 本之現行版號 +1、內容一句（車型欄、DR-46、命名以實際值、Test Set 修正、sibling 10 條），作者 `PeiPYHsu`。**Approver／Reviewer／中文作者三格留空給 Pei。**
3. `J5` 日期改 2026-09-22（與 Cover 一致）；`Test Case Framework` SmartArt 舊名**只報不改**（Tier 3）。
4. 新列（476–485）之列高與樣式：先量既有列之 row height／style id，複製套用；做不到即報。
5. invariant 與逐格對帳同前；對 v4 差異須 ＝ §2 之 6 格 ＋ 封面類分頁 ＋ `J5`，逐格歸因。
6. 附 `coverage.tsv`（403 req → TC ID 對映，含 5 條 BLOCKED）、`dr_summary.tsv`（DR-47／48／49 open、46 closed）、`rd_feedback.md`（001-01 tabs 衝突、019-03 VF HVAC 缺件、037 VC 前提 DT／HDCC 與實際車型不一致、`$Rear_HVAC_cfg$` 命名、§19／§20／§21 未引用）供 Pei 送 RD。

## 4. 上繳首段

對 v4 差異格數與歸因、封面類分頁之填值清單、留空三格位置、lint `LINT_STATUS: OK`、產物 sha256、ENTRY（type `delivery-candidate`）。
