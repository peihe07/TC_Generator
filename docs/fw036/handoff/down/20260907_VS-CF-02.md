# 下放包 VS-CF-02 —— CFTS044 出貨整備（Revise2）

**指派：執行層。** Pei 指示（2026-09-07）：「整理成有辦法出貨的樣子，把 PENDING 拿掉」。

⚠ **本包不得整包執行** —— §六列有 **4 項須 Pei 先裁**。裁定前只可執行 §一～§四；
§五之 4 項待裁定回填後另行「下放」。

基準：`sandbox/cfts044/cfts044_20260819_Revise1.xlsx`，sha256
`f444028d1cd93396738b4225ef6ce5de0ad3d41675d66f16acb0d099c8ef44a1`（分析層現查）。
產出 `cfts044_20260819_Revise2.xlsx`（R-G72）。落檔一律 `surgical_save`。

---

## 〇、先講清楚：「把 PENDING 拿掉」有三條路，本包只走兩條

| 路 | 內容 | 本包 |
|---|---|---|
| (a) **查得來源後填實** | 值在 DBC／PROXI／HMI 文件裡，只是先前未查 | **走**，7 列 |
| (b) **不生成並具名** | 值確實無來源，該列清空、Remarks 記明缺口與 DR | **走**，5 列 |
| (c) 直接刪字或降 NA | 步驟留白或宣稱不適用 | **不走** |

(c) 會把可追溯的缺口變成看不見的缺口 —— 出貨本乾淨了，但 SWE.6 的追溯鏈上少了一段，
且日後 DR 覆文回來時沒有任何線索指回該列。§8.4.3 之「由 Pei 裁定降轉 NA」限於
**確認不適用**，這 5 列是「規格要求的值在匯流排上編不出來」，不是不適用。
故本包以 (b) 收尾：**工作簿 PENDING 歸零、可出貨，缺口以 `NOT GENERATED` 留在紙面上。**

**代價須明說**：Revise2 之可執行 TC 由 220 降為 **214**（243 列 − 23 既有未生成 − 5 新增未生成
− 2 重複列刪除 ＋ row 252 補生成 1）。覆蓋率之陳述須同步改寫，不得仍稱 237/237 全覆蓋。

---

## 一、PENDING 12 → 0

### 1.1 (a) 查得來源填實 —— 6 列

**新量測（本包之依據）**：`TELEMATIC_VEHICLE_SETUP3`（BO_ 1291）於
`forms/PDT27_E2A_R1_BHCAN2.dbc` 第 424–429 行與 `inputs/PDT27_E2A_R4_BHCAN.dbc`
第 1076–1081 行**兩本皆有**，5 支 `*_Tlm` 訊號皆 1 bit；`VAL_`（R1 第 3355–3359 行）逐字為
`0 "Not_Pressed" 1 "Pressed"`。**先前掛 DR-15 之列並未查過此表。**

| 列 | tc_id | 條文所求 | 改 ER step 3 為 |
|---|---|---|---|
| 71 | -062 | `$FL_HS_RQ$ = [HS_PSD]` | `3. TELEMATIC_VEHICLE_SETUP3.FL_HS_Tlm = 1 (Pressed) is sent` |
| 86 | -077 | `$FR_HS_RQ$ = [HS_PSD]` | `3. TELEMATIC_VEHICLE_SETUP3.FR_HS_Tlm = 1 (Pressed) is sent` |
| 157 | -148 | `$FL_VS_RQ_TGW$ = [VS_PSD]` | `3. TELEMATIC_VEHICLE_SETUP3.FL_VS_Tlm = 1 (Pressed) is sent` |
| 172 | -163 | `$FR_VS_RQ_TGW$ = [VS_PSD]` | `3. TELEMATIC_VEHICLE_SETUP3.FR_VS_Tlm = 1 (Pressed) is sent` |
| 233 | -224 | `$HSW_RQ_TGW$ = [NOT_PSD]` | `3. TELEMATIC_VEHICLE_SETUP3.HSW_Tlm = 0 (Not_Pressed) is sent` |

同 5 列之 Procedure step 3 由「is the one specified for the current state」改為具體值：
`3. Read the CAN-B trace and check that TELEMATIC_VEHICLE_SETUP3.<Sig> is <raw> (<label>)`。
Remarks 改為：
`dr_dependent = DR-24′ —— <Tsend> 之上限未覆，本列不驗時限；請求值取自 DBC VAL_（BO_ 1291）`
（DR-15 前綴移除；`<Tsend>` 本來就沒進步驟，不構成 PENDING）。

**row 35（DR-26）**：條文為 `selectable **only when** $PowerMode$ = [IGN_RUN]`。
「only when」本身即給出否定側，無須上游補件。ER step 2／3 改為只講**可選性**、不講外觀：

```
ER 2. RVC_button_ign_lk is recorded as not selectable
ER 3. The virtual "Rear View Camera" button is selectable
```

Procedure step 2 同步改 `Read whether the virtual "Rear View Camera" button is selectable and record as RVC_button_ign_lk`，
step 3 尾改 `… and check that it is selectable`。
Remarks：`R-VS103(1)：條文之「only when」已給否定側，外觀（灰階／隱藏）不在本列驗證範圍；DR-26 撤`。

### 1.2 (b) 不生成並具名 —— 5 列

| 列 | tc_id | Req | 條文所求 |
|---|---|---|---|
| 73 | -064 | LeftFrontHeatedSeat-015 | `$FL_HS_RQ$` 依現況送 High→Medium→Low→Off **四值** |
| 88 | -079 | RightFrontHeatedSeat-032 | 同，FR |
| 159 | -150 | LeftFrontVentedSeat-013 | 同，FL vented |
| 174 | -165 | RightFrontVentedSeat-030 | 同，FR vented |
| 235 | -226 | HeatedSteeringWheel-014 | 同，HSW |

對應訊號 `TELEMATIC_VEHICLE_SETUP3.*_Tlm` **僅 1 bit（0/1）**，載不動四值 —— 這才是 DR-15
真正在問的事（前述 5 列之「單值 Pressed」被誤併入同一 DR）。

處置：I/J/K/L/M/P/R 清空，N 欄保留，AH 改為

```
NOT GENERATED: B6-value-absent —— 條文要求依現況送 4 個請求值，對應訊號 TELEMATIC_VEHICLE_SETUP3.<Sig> 於基線 DBC 僅 1 bit（VAL_ 0 Not_Pressed／1 Pressed），無可承載之編碼；DR-15（已送出待覆）；R-VS103(2)
```

**沿用既有分類碼 `B6-value-absent`，不新設第八碼**（審閱 VS-CF-01 甲-1 之教訓）。

---

## 二、機械錯與重複列

### 2.1 row 23、row 49 是重複列，不是缺標籤 —— 刪列

實測：row 23 與 row 24 之 `req`／`item`／`pre_conditions`／`expected_result`／
`specification_reference` **五欄逐字相同**，`test_procedure` 僅差 row 23 之標籤為空字串；
row 48 與 row 49 同型（req 皆 `ScreenOFF-052`，僅差 row 48 之 `Audio__initial` 變數名殘缺、
row 49 之按鍵標籤為空）。

依 §10.6 之嚴格等價（同 trigger ＋ outcome ＋ input ＋ verification target），二組皆為
**duplicate**，處置是**刪去重複列**而非補區分 token：

| 保留 | 刪除 | 保留列須修 |
|---|---|---|
| row 24（全欄完整） | **row 23** | 無 |
| row 49（ER 文字完整） | **row 48** | Procedure step 2 之 `""` 改 `"Screen Off"`（來源見 §三） |

刪列後 F 欄 tc_id 依 §10.3 單調遞增重賦（`writeback_036.py` 之 F 欄重賦路徑，同 R-G42 一(a)），
**並產出舊 ID → 新 ID 對照表** `reports/cfts044_tcid_remap.tsv`（TestRail 用；同 PM 線之做法）。

> **分析層自報**：`down/…_review.md` §二 F1 將 23/24、48/49 列為「sibling 括號下半逐字相同」
> 並建議補區分 token —— **錯**。二者是重複列，補 token 等於把重複合法化。
> 成因：只比對了括號下半，未比對整列五欄。登 A 系列一條。

### 2.2 F1 之真 sibling —— 2 組 5 列，補區分 token

| Req | 列 | 現況括號下半 | 改為 |
|---|---|---|---|
| Stop-StartSystem-002 | 10 | (Stop-start enabled, engine stopped) | `(Stop-start enabled, engine stopped: heated seat)` |
| | 11 | 同 | `(Stop-start enabled, engine stopped: vented seat)` |
| | 12 | 同 | `(Stop-start enabled, engine stopped: heated steering wheel)` |
| FeaturesEnableCriteria-022 | 58 | (Two configuration parameters combined) | `(Vehicle line M4)` |
| | 59 | 同 | `(Vehicle line MP)` |

（row 58／59 之 `VC_VEH_LINE` = 98 `553/M4` vs 93 `552/MP`，即區分軸。）

### 2.3 F3 全形分號 —— 28 列

交付四欄之 `；` 全數改 `; `（半形分號 ＋ 一個空格）。母本 38 列，Revise1 已修 5、
清空消滅 5，殘 28；§1.2 再清空 5 之後之殘量由執行層現算，不採本包之推估。

---

## 三、導航路徑（F4）—— 已查得 HMI 來源，全數可填

**新量測**：`spec-index/cache/SYS1_HMI_Menu_Bar_and_App_Drawer_HMI_Logic_and_Flow_R1_SR24_3A_(September_11_2023).xlsx`，
sheet `Basic Report`，**row 22**（NRL-127734，§4.1 naming table）逐字含下列 Feature Name：

| Feature Name | Drawer Name | Feature Type | App Category |
|---|---|---|---|
| `Comfort` | Comfort | Core Feature | Comfort |
| `Controls` | Controls | Core Feature | Vehicle |
| `Rear View Camera` | Rear View Camera | Camera | Vehicle |
| `Screen Off` | Screen Off | Controls Feature | Vehicle, System |
| `PHEV/HEV Pages` | **Hybrid Electric** | Specialty Features | Vehicle |
| `3rd Row Headrest Fold` | **3rd Headrest Fold** | Controls Feature | Vehicle |

改寫規則（§5.8(a)，固定入口 = Menu Bar）：

| 現況（14 列） | 改為 | ER |
|---|---|---|
| `Open the Controls screen from the Menu Bar` | `Press "Controls" on Menu Bar` | `The "Controls" screen is displayed` |
| `Open the Rear View Camera screen from the Menu Bar` | `Press "Rear View Camera" on Menu Bar` | `The "Rear View Camera" screen is displayed` |
| `Open the Heated / Vented Seats screen …` | `Press "Comfort" on Menu Bar` | `The "Comfort" screen is displayed` |

**畫面名統一**：`Climate screen`（13 列）與 `Heated / Vented Seats screen`（205 列）為同一畫面之兩名，
且兩名皆非 HMI 來源。依 Comfort L&F（`SYS1_HMI_Comfort_HMI_Logic_and_Flow_R1_SR25_Post_3A_CR29359_(Feb_24_2025).xlsx`，
`Basic Report` row 78–91，§11「R1 Heated/Vented Seats」）與 Menu Bar row 75（`The Comfort main Menu Bar icon`），
該功能歸屬 **Comfort**。**全簿統一為 `the "Comfort" screen`**，Pre-Condition 之
`The Heated / Vented Seats screen is displayed` 一併改 `The "Comfort" screen is displayed`。

row 49 之 `""` 按鍵標籤填 **`"Screen Off"`**（naming table 逐字）。
row 58／59 之 `Hybrid Electric Pages access button` 改 `"Hybrid Electric" button`（Drawer Name 逐字）。

---

## 四、row 252 補生成

`SWE1-VC-HeatedSteeringWheel-009`，N 欄空。執行層自四本 SWRA 之
`HeatedSteeringWheel` 本取該 leaf 之條文與 ObjectID，依 IN §3 之流程生成，
sibling 參照 row 224–235 之同族列。生成後併入本輪 lint。
**若該 leaf 之條文屬 B4-preamble 或 `valid only if` 前言型，則比照 row 56／94 具名不生成**，
不得為湊數而生成。

---

## 五、須 Pei 先裁（4 項）—— 裁定前不執行

**裁-1　row 28（`IGN_OFF_ACC`）**
`STATUS_BH_BCM2.CmdIgnSts` 之 `VAL_`（兩本 DBC 逐字同）：
`0 Initialization／1 IGN_LK／3 ACC／4 RUN／5 START／7 SNA`，**2 與 6 未定義**。
條文寫 `[Ign. off & acc. (4 position switch) / IGN_OFF_ACC]`。
- **甲**：對照 `3 (ACC)`，與已准之 `[IGN_RUN]→4`、`[IGN_START]→5`（R-VS102(4)）同型，標 `dr_dependent = DR-21`。
- **乙**：`Ign. off **&** acc.` 之連接詞可能指 OFF 與 ACC **兩個位置之聯集**（即 `1` 與 `3` 皆須為可選），
  若如此則甲會漏測一半 → 該列 `NOT GENERATED: B6-value-absent`，DR-21 續掛。

分析層無法從現有文件判別，且此為**唯一**仍會使工作簿留 PENDING 之列。傾向**甲**（可測且有前例），
但乙之風險是漏測，請裁。

**裁-2　`"Headrest Dump"` 之按鍵標籤**
工作簿 8 處寫 `"Headrest Dump"`（源自 CFTS 條文 *Third Row Headrest Dump Softkey button*），
惟 Menu Bar §4.1 naming table **無此字串** —— HMI 側為 Feature Name `3rd Row Headrest Fold` /
Drawer Name `3rd Headrest Fold`。依 §5.8(c)(e)「畫面上之 hop 取 HMI 標籤」應改，
依 §4.3.1「上半 verbatim 保留來源原文」則 test_item 不動。
- **甲**：Procedure／ER 之按鍵標籤改 `"3rd Headrest Fold"`，test_item 上半不動，Remarks 記明兩名。
- **乙**：維持 `"Headrest Dump"`，開 DR 向上游確認 HMI 實際標籤。

同型另一件：`"Rear View Camera"` 於 naming table 之 Drawer Name 即 `Rear View Camera`，無分歧，不受本裁影響。

**裁-3　23 ＋ 5 = 28 列未生成列之去留**
出貨本要「留空列＋Remarks 具名」還是「刪列」？
- **甲（留）**：Req 覆蓋鏈完整，審閱者一眼看得到缺口；但交付本有 28 列空白。
- **乙（刪）**：版面乾淨；但 237 個 Req 只剩 209 個出現在交付本，覆蓋缺口從紙面消失。

分析層傾向**甲** —— SWE.6 之可追溯性優先，且 `NOT GENERATED` 之具名正是為此而寫。
乙若採，須另出一份覆蓋率對照表隨交付本一起送，否則缺口無處可查。

**裁-4　row 13 之驗證邏輯疑義（不是格式問題）**
現況 Procedure：step 3 送 `ESS_ENG_ST = 4 (ENS Stop Pending)`，step 4 送 `EngineSts = 0`，
step 5 **讀回** `ESS_ENG_ST` 並要求它是 `1 (ENS Stopped)`。
兩個問題：(i) 全程沒有任何一步把 `ESS_ENG_ST` 設為 1，步驟 5 必失敗；
(ii) `ESS_ENG_ST` 是測試端注入之訊號，讀回自己注入的值屬 **B 類 loop verification**（禁止）。
條文（`Stop-StartSystem-003`）要求的是「HU **監看** `ESS_ENG_ST` 以判別 `EngRun_Stat` 之變化
是否肇因於進入 stop mode」—— 真正該驗的是 **HU 之外顯行為**（開關指示燈是否熄滅），
而非把訊號讀回來。建議整列重寫，驗證點改為 step 5 之 HMI 觀察。
此屬條文層改寫，請裁是否納入本輪。

---

## 六、上繳 `up/20260907_VS-CF-02.md` 須含

1. Revise1／Revise2 之 sha256；zip member 差異與 dv 計數。
2. 逐列逐欄 diff；刪列前後之列數與 tc_id 重賦對照表路徑。
3. PENDING 計數：12 → **0**（裁-1 取乙則為 1，須具名）。
4. 全形分號、尾句號、行首尾空白、方括號、`$MESSAGE.Signal$`、Proc↔ER 1:1 之全簿計數。
5. 可執行 TC 數與 Req 覆蓋數之**改寫後**陳述（見 §〇 之代價段）。
6. `R-VS103` 落檔（live 取號；本包所引之 (1)(2) 為暫記，實際條號以落檔時為準）。
7. 工具旗標逐次聲明；自報。

## 七、不在本包

S1 之其餘標籤大小寫、乙-1（row 62 ER `is accepted`）、乙-2（row 26／28 baseline）、
`Set PROXI` 舊式 —— 前二者待 Pei 裁定（審閱 VS-CF-01 §三），後者可併 §二.3 同批，
執行層若一併做須於上繳具名。
