# 審閱 GC-03 上繳包（2026-09-05，Tier 2）

對象：`up/20260905_GC-03.md`　下放包：`down/20260905_GC-03.md`
結果：**收受**。八件全數收受；9-6 三項裁於 §二；分析層自報一件（A-GC12）；13 列判定於 §四。

---

## 一、分析層自報 A-GC12：R-G43 之理由未量測即斷言

「`RULINGS_LEDGER.md` 為 `rulings_hash.py` 之指紋源」為偽。實測（上繳 9-3 節）：
掃描面為 FO ＋ `features/*/RULINGS.md`；`RULINGS.sha.tsv` 698 列來源為台帳者 0。
同一事實亦解釋了 GC-01 以來「R-G30–R-G41 缺列」——**非 Pei 漏跑，而是該十二條只在台帳，本就不在掃描面**。
此誤已於 R-G43 §一寫入裁定理由並下放執行；形態＝A-GC 系列（人寫之理由代量測），且為分析層第 12 次。

**裁定 (A) 是否維持**：維持。理由二腳去其一（指紋源），餘一腳仍立（台帳條文附日期、來源、逐字本體；FO 為摘要），
且改號已落 193 處、對照表已出，回退即再動 193 處而所得只是換一側之歧義。
**A-GC12 登 `docs/fw036/ANOMALIES.md`（GC-04 執行層抄錄，R-G36），R-G43 節之理由以刪除線改正，裁定不動。**

## 二、9-6 三項

1. **R-G23′→R-G62′**：覆核**成立**。′ 條之語意繫於母條（「綁定須被檢查」補充），不隨改即重造歧義。收受，改號表以 20 列為準。
2. **R-G43 理由**：併同修訂——台帳 R-G43 節理由段以 `~~ ~~` 保留原句，其後書更正句（§一）。GC-04 抄錄。
3. **R-G19 無指紋 → 台帳納入掃描面** [DEFAULT]：

```
R-G43(b)：`scripts/rulings_hash.py` 之 default_targets 增 `docs/fw036/RULINGS_LEDGER.md`。
台帳為全域條文之本體所在，不入掃描面則 R-G12～R-G42 之台帳側、R-G44 起之新條皆永無指紋。
先以 `--out <工作區>` 乾跑，回報：新增列數、與現行之 ID 撞號（台帳內部同號者，如 R-G13／R-G13 補充、
R-G33／R-G33(d)）、本體 sha 與 FO 側同號條是否相等。撞號處置另裁，不得在乾跑中自行合併。
```

`rulings_hash.py` 為 Pei 之工具，**改其 default_targets 屬 Pei 准之範圍**；GC-04 只做乾跑，改 code 待准。

## 三、9-2／9-4 之提案 —— 併入既有條

- 9-2（跑了會寫檔之工具）→ **R-G50(c)**：包內列為「不跑」之工具，執行層只得 `--check` 或 `--out <工作區路徑>`；上繳附該工具寫入 repo 之路徑清單（空亦寫）。
- 9-4（產出表入母體，二犯）→ **R-G16 補充**（量測與聚合分離）：`docs/reports/*_YYYYMMDD.tsv` 為量測產出，**常設排除**於一切母體；排除集寫入 `scripts/` 之單一常數，不得逐包各記。

## 四、13 列「是否影響已交付 TC」——分析層判（GC-04 抄回五本 DECISIONS）

前提（上繳 7 節）：五個 feature 之 `delivered/` **皆空**，故「已交付 TC」在 repo 內無母體；
本判以「該 feature 現存最新工作簿」為對象，判準為 key 級差異是否觸及其 TC 內容。

| # | feature | 家族 | 判 | 依據／後續 |
|---|---|---|---|---|
| 1 | comfort | HMI Settings List | **不影響（key 級）** | key 逐列相同，差在非 key 分頁 |
| 2 | comfort | Market Config | 不影響（key 級） | 同上 |
| 3 | comfort | Pop Up List | **待證** | 1 PU id 只在 forms、1 值異；GC-04 列該二 id，grep comfort 最新工作簿 `PU\d{4}` 命中 |
| 4 | time_management | HMI Settings List（June 15 2023 版） | **影響待證，出貨前必重跑** | 95／105／32 差異；GC-04 以 32 個變值 key 與 105 個只在 forms 之 key grep 該線最新工作簿；命中 >0 即須改綁 forms 版重產 |
| 5 | time_management | HMI Settings List | 不影響（key 級） | key 相同 |
| 6 | time_management | LID | **待證** | 4 值異；GC-04 列該 4 signal，grep `$…$` 命中 |
| 7 | time_management | Market Config | 不影響（key 級） | key 相同 |
| 8 | user_profiles | Pop Up List | 待證 | 同 #3 |
| 9 | vehicle_category | HMI Settings List | 不影響（key 級） | key 相同 |
| 10 | vehicle_setting | DBC BHCAN（R4 vs R1_BHCAN2） | **高風險，Pei 裁版** | `SG_` 只在 inputs 604、值異 914；forms R1 為 Pei 既定權威（R-17）。GC-04 以該線最新工作簿全部 `$MESSAGE.Signal$` token 對 forms DBC 比對，回報缺失數；**何版為 R1L 現行由 Pei 裁** |
| 11 | vehicle_setting | DBC FDCAN8（R5 vs R1） | 高風險，同 #10 | `SG_` 只在 inputs 122 |
| 12 | vehicle_setting | HMI Settings List | 不影響（key 級） | key 相同 |
| 13 | vehicle_setting | LID | 待證 | 同 #6 |

「不影響（key 級）」之盲區（R-G11）：非 key 分頁（Notes、Brand-Specific Names、Options 文字）之差未查；若該線 TC 引用此類欄位，判須重開。六列各附此句。

## 五、收受清單與閘況

台帳新節、改號 193 處＋`SHA_MIGRATION_rg43.tsv` 20 列、`rg43_manual` 130 處（**分析層待判**，GC-04 前先判集中之六檔 50 處）、MANIFEST 161、SXM 二匣 copy、清理腳本加註、`backend/` 四路徑集中於 `parser.resolve_tc_sheet`＋9 測試（對照向逐項同名，ruff +0）、DBC 配對註、13 張差異表。
`api_server.py:2338` 建檔預設不改：收受其理由。
`canon_refs` **−53**（本線首次降紅）；`rulings_hash` 轉紅為改號之預期後果，Pei 重生後消。

## 六、Pei 待裁／動作（更新）

| # | 事項 | 狀態 |
|---|---|---|
| 1 | R-G43(b)（台帳入掃描面，改 `rulings_hash.py`）[DEFAULT] | 追認；追認後才跑重生 |
| 2 | vehicle_setting DBC：forms R1 vs inputs R4/R5 何者為 R1L 現行 | **Pei 裁**（§四 #10–11） |
| 3 | `rulings_hash.py` 重生（含 R-G44–50、R-G51–69、R-G62′；台帳納入後含 R-G30–41） | 待 1 |
| 4 | `cleanup_gc02.sh` 專門 commit | 可跑 |
| 5 | `_intake/` 五匣清空（SXM 已登記） | 可清 |
| 6 | R-G50(c)、R-G16 補充 [DEFAULT] | 追認 |

## 七、GC-04 預告

執行層：A-GC12 抄錄＋R-G43 理由更正＋R-G50(c)／R-G16 補充入台帳；`rulings_hash --out` 乾跑（台帳納入）；§四 #3／4／6／8／10／11／13 之 grep 命中（附查詢式）；13 判抄回 DECISIONS；`SELF_OUT` 常數化。
分析層：`rg43_manual` 130 處逐處判（先六檔 50 處），輸出 `docs/reports/rg43_manual_20260905_judged.tsv` 交執行層改號。
