# 審閱 —— 上繳包 VS-CF-03（Revise2 出貨版，2026-09-08）

對象：`up/20260908_VS-CF-03.md`（58,599 bytes，mtime 11:53:06）
下放包：`down/20260908_VS-CF-03.md`
方式：分析層直讀 `sandbox/cfts044/cfts044_20260819_Revise2.xlsx` 獨立複算，
未執行 `vs_cf03_lint.py`（不以被驗程式驗自己）。

**總判：通過。** §〇 七項目標全數達成且經獨立複算相符；8 項具名偏差全部成立且處置正確。
另列 **1 項出貨前須決（甲）**、**3 項須知（乙）**、**1 項分析層自報（丙）**。

---

## 一、獨立複算

| 項 | 上繳所稱 | 分析層實測 | 判 |
|---|---|---|---|
| Revise2 sha256 | `949fa81e…f71c9db4` | 同 | ✅ |
| 資料列（F 欄非空） | 241 | **241**（row 10–250） | ✅ |
| 物理列 | 252（尾 2 列真空） | `max_row` 252，>250 之資料列 **0** | ✅ |
| Requirement 覆蓋 | 237/237 | **237** 個相異 Req | ✅ |
| 未生成列 | 0 | `test_item` 空 **0** | ✅ |
| PENDING | 0 | **0** | ✅ |
| `BLOCKED`／`IMPL_GAP`／`NOT GENERATED`／`HELD OUT` | 0 | 各 **0** | ✅ |
| DR 引用 | 僅 `DR-5-B` 39 列 | `DR-5-B` **39**、另有 **`DR-11` 1 處**（row 250 Remarks，即 §7 偏差 5 之具名，非漏網） | ✅ |
| tc_id | -001…-241 連續 | 連續，無跳號 | ✅ |
| Procedure ↔ ER 1:1 | 0 不等 | **0** | ✅ |
| 每列 ≥2 步 | 0 不足 | **0** | ✅ |
| 尾句號／行首尾空白 | 0 | **0** | ✅ |
| `$MESSAGE.Signal$` 三件組 | 0 | **0** | ✅ |
| 全形標點（交付四欄） | 0 | **0**（Remarks 143 列有中文標點，屬 `[OVERRIDE-R5]` 合法） | ✅ |
| `spec_ref` 格式 | 0 不合 | **0**；空值 **1 列**（row 250，具名） | ✅ |
| Priority | P0 24／P1 210／P2 7 | 同 | ✅ |
| `design_method` | 九值內 | 六值，皆在 IN §12 內 | ✅ |
| R-S4 括號下半（存在／無中文／同 Req 相異） | 0 違規 | **0** | ✅ |
| `"Headrest Dump"`／`Climate screen`／`Heated / Vented Seats screen`／`Open the … screen` 殘留 | 0 | 各 **0** | ✅ |
| `TELEMATIC_*` 於作者側四欄 | 0 | **0** | ✅ |
| bus trace 殘留 | 1 列（row 46） | **1**（row 46，`TGW_DISP_STAT` 確為 DBC 訊號） | ✅ |
| `Send CAN:` | 162 列 | **162** | ✅ |
| §一(a) 值標籤 token | 346 | 作者側四欄 **346**（含 `test_item` 為 524） | ✅ |

`STATUS_CSWM.*` 於作者側仍有 122 列 —— 該 10 支訊號皆於基線 DBC 有同名 `SG_`，
依 §10 自報 1 之判準維持 CAN 記法，**正確**，非 §7 偏差 6 之漏網。

### 8 項具名偏差之判

1. **刪列以「內容上移＋尾 2 列清空」為之** —— 成立且為唯一可行解（`surgical_save` 無刪列能力，
   `wb.save()` 會毀 x14 dv）。見甲-1。
2. **「19 列」實為 23 列** —— 下放包之算術錯，執行層之數為準。B7 五列未被 §二指派而依
   §〇／§七.6 一併生成，**判斷正確**；未回問而逕行補齊，在「每個需求都要有產出」之明令下合理。
3. **`DR-5-B` 39 列降為非阻擋** —— 成立。該 DR 待外部文件（TLM HMI Document／HMI requirements），
   非本包可解；ER 只寫可觀察之變化不寫具體樣式，與 row 92 之範式一致。**准**。
4. **刪列依據敘述更正（L／M 欄相異）** —— 我下放包寫「五欄逐字相同」有誤，執行層更正正確。
5. **row 250 無 CFTS044 錨** —— 037 之 Source Requirement ID 為 `SYS-RA-CFTS100`。
   **留空並具名，優於造號**（§8.4.1）。見乙-2。
6. **API 記法適用範圍由 44 擴為 80 列** —— 成立。只改 56 列會造成同簿兩式，擴大正確。
7. **row 92 之 `test_item` 上半改取 D 欄行為句** —— 成立。CFTS 原句為前言式，與該列所驗之
   `Fail_Present` 圖示行為不對應，取 D 欄合乎 §二之 fallback。
8. **`$*_Cmd_Tlm$` 取短名式** —— 成立。§一(a) 與 §一(b)／§2.5 之衝突以較具體者勝出，判斷正確。

### §9 兩項待裁之回覆

- **9-1（row 19／52 重複）**：見甲-1，範圍比上繳所報更大。
- **9-2（方括號與值標籤衝突）**：**執行層之解正確，維持。** §六.3 之規制對象是
  IN §11 所禁之 **UI 標籤式**方括號（`Press [Media]`），而 `[HS_PSD]`／`[Front Seats]`
  是自 037 D 欄逐字引用之**來源 token**，正落在 IN §11 之 `Exception (profile-scoped)` 內。
  下放包 §六.3 未區分兩類，是我寫得不夠精確。**不改 346 處。**

---

## 二、出貨前須決（甲）—— 1 項

### 甲-1　`pc`／`proc`／`er` 三欄逐字相同之群組共 **9 組 19 列**，不只上繳所報的 1 組

上繳 §9-1 只報了 row 19／52。分析層以 `pc+proc+er` 三欄為鍵重掃全簿，實得：

| 列 | Req | 差異 |
|---|---|---|
| **91, 105, 125** | OneStage-039／TwoStages-055／ThreeStages-078 | 三列驗同一件事（`$Heated_Seats$` 組態閘控），僅括號下半寫 One/Two/Three stages |
| 177, 197 | TwoStagesVented-037／ThreeStagesVented-060 | 同上，`Cooled_Seats` |
| 19, 52 | LHD/RHD-010／-013 | 上游條文逐字相同（已報） |
| 20, 53 | LHD/RHD-011／-014 | 同型，**未報** |
| 189, 192 | TwoStagesVented-049／-052 | **未報** |
| 190, 193 | TwoStagesVented-050／-053 | **未報** |
| 191, 194 | TwoStagesVented-051／-054 | **未報** |
| 234, 240 | HSW-015／-021 | **未報** |
| 235, 241 | HSW-016／-022 | **未報** |

漏報之成因：上繳之重複檢查以 §10.6 之**五欄**（含 `test_item`）為鍵，
而 91／105／125 等群組之 `test_item` 括號下半相異，因而未命中。
**但 §10.6 之等價判準是「同 trigger ＋ outcome ＋ input ＋ verification target」——
括號下半是作者生成之標籤，不是驗證目標**；以它入鍵會系統性地遮蔽這類重複。

**這是出貨前必須處理的一件**，理由不是格式：SWQT 審閱者一眼會看到 19 條 TC 只做 9 件事，
而 91／105／125 之三條在同一台車上只能執行一次（`$Heated_Seats$` 是同一個組態參數）。

三案：

- **甲**：全部並存。上游條文確實重複，覆蓋鏈最完整，出貨最快。代價是被問「為什麼三條一樣」。
- **乙**：91／105／125 與 177／197 之組態閘控群**併為一列多錨**（`specification_reference`
  列多個 ObjectID，§10.7 已允許一行一個之多行式），其餘七組並存。列數 241 → 238。
- **丙**：91／105／125 依各自 Req 之 `$Heated_Seat_Levels$` 條件補上區分
  （One Level／Two Levels／Three Levels），使三條真正相異；177／197 同法。
  這是**最合規**的一案 —— D 欄本來就給了該條件，是生成時未取用。其餘七組另議。

分析層建議 **丙**，其次甲。乙會使一個 Req 對應不到自己的 TC，不建議。

---

## 三、須知（乙）—— 3 項，不阻塞

**乙-1　交付本尾端有 2 列視覺空白（物理 row 251／252）**
§7 偏差 1 之必然結果。若 SWQT 交付模板不接受尾端空白列，須另立
`surgical_delete_rows` 能力；本輪不建議為此重跑。**請 Pei 確認模板是否在意。**

**乙-2　row 250 之 `specification_reference` 為空**
IN §8.4.3 定「不得留空」，惟該 leaf 之錨為 `SYS-RA-CFTS100`（非 CFTS044，DR-11）。
執行層留空並於 Remarks 具名，**優於造一個 CFTS044 號**。
若交付檢核要求該欄非空，可寫 `SYS-RA-CFTS100`（037 之逐字來源），
但會使全簿出現一個非 `CFTS044-` 前綴之錨 —— 兩害相權，建議維持留空並在交付說明具名。

**乙-3　row 29／30 之區分軸落在不可觀察處**
二者僅差 `at the CBC` vs `at the FSM`（`$HdRstRelRq$` 為同一訊號）。
自 HU 側讀取同一訊號，無法分辨它「到了 CBC」還是「到了 FSM」——
兩條 TC 之 pass/fail 判定實際上完全相同，屬 §7 之潛在 FP。
上游條文（CFTS044-4858999／-4859000）確實分寫兩個接收端，故不是執行層之錯。
**建議**：交付本維持，Remarks 加一句具名該區分需在對應 ECU 側量測；或列入下一輪。

---

## 四、分析層自報（丙）

**丙-1　下放包 §六.3 未區分兩類方括號，製造了一個假衝突**
§一(a) 令值標籤用 D 欄原表示法（必然帶 `[...]`），§六.3 卻令方括號全簿歸零 ——
同一份下放包內兩條直接相斥，逼執行層在 §9-2 花篇幅自行調和。
IN §11 本身已有 `Exception (profile-scoped)` 區分「來源逐字 token」與「UI 標籤」，
我寫 §六.3 時未援引。**下放包之內部一致性檢查缺項**，登 A 系列。

**丙-2　下放包 §四之「五欄逐字相同」與 §二之「19 列」皆為未複算之推估**
前者實為 L／M 欄相異（§7 偏差 4），後者實為 23 列（§7 偏差 2）。
與 VS-CF-01 丙-2、VS-CF-02 之殘量算術同一形態 —— **下放包內之數字未複算即落筆**，
本輪為第三次。此形態已連續三包出現，建議入 FO 為硬規：
**下放包內之任何列數／處數，須附產生該數之查詢式**，未附者執行層得退回。

---

## 五、狀態

Revise2（`949fa81e…`）在 sandbox，未 commit、未進 `output/`／`delivered/`。
出貨 gate：PENDING 0、DR 阻擋 0、未生成 0 —— **技術面已過**，
餘甲-1 之決定與乙-1／乙-2 之交付模板確認。

台帳落檔（`R-VS102(2)` 撤銷、B7 登記行加刪除線、DR 撤回批次）依 R-TM13 加註不刪，正確。
