# 下放包 SEC-12 —— SEC-11 審閱裁定落檔 ＋ `EXEC_GUIDE.md`（一次性環境建置與通道操作指南）→ `security_v07`

前置：`up/20260917_SEC-11.md`、`down/20260917_SEC-11_review.md`。
Pei 2026-09-17 指示：審閱後直接出下一包，不另等「准」；審閱之裁定即本包依據，Pei 有異議於上繳後撤。
執行層 session：新開；先落 SEC-11 三支 commit。

---

## §0　禁區

- `down/`／`up/` 既有檔不動；canon 不動；`framework.md` 不動；`lint036.py` 不動；既有 xlsx 不動（v07 新檔）。
- `EXEC_GUIDE.md` 之每一指令、路徑、檔名、tag、DID／RID bytes **逐字取自 `step_assets.tsv`／`diag_items.tsv`／TC 本身**；指南不得出現任何 TC 中不存在之指令（它是 TC 之索引，不是新素材）。
- 指南不進 xlsx（交付本無此 sheet）；落 `delivery/EXEC_GUIDE.md`，隨三件組同送。

## §1　裁定落檔（取號現查）

```text
R-SEC22(a) amend  C3（金鑰強度）之落點加 `NR1L-LOGENC-007`／`-008`（SWE1-LOGENC-004 之兩 sibling）；
                  Remarks 增 `conflict: 376/509/511 vs 510 vs 514 — key strength; unresolved per CCVR Mapping notes row 14`。
```
`audit_newtc.tsv`：`NEW-UPD-01` → `ACCEPTED_GAP`，`disposition = A-SEC-11`；`AXIS_GAP` 歸 0。
`ANOMALIES.md` 新增：
```text
[A-SEC-11] CCVR NEW-UPD-01 要求拒絕禁止之 software／key rollback；037 SWE1-SRA-SECURITY-SWDL-001 之 VC
           （keep SWDL config／control KEY-certificate interface）無負向要件。不拆；回饋 037 作者。
[A-SEC-12] 037 SYSAD_SEC_ECUCERT_ECUCERT_STORAGE_IO 之 Verification Criteria 與 …_ECUCERT_JNI 逐字相同
           （jbyteArray／jstring 轉換無記憶體洩漏），與其 Description（經檔案路徑存取 Cert Store）不符；疑複製貼上。
           TC 依 VC 逐字產出（NR1L-ECUC-019）；請 037 作者確認 STORAGE_IO 之正確 VC。
[A-SEC-13] 037 SWE1-LOGENC-008 Description 稱 KeyInstall 為 RSA key 來源，VC THEN 寫 AES key；TC 依 VC 逐字，
           括號下半以中性語 the key is obtained from KeyInstall。請 037 作者定 RSA／AES。
[A-SEC-14] 037 SWE1-LOGENC-005 VC 寫 RSA symmetric key（RSA 為非對稱）；逐字引用未改。請 037 作者更正措辭。
```

## §2　`delivery/EXEC_GUIDE.md`（新腳本 `features/security/scripts/build_exec_guide.py`，自資料檔生成，冪等）

結構（英文，Remarks 體例；不含內部台帳代號，`X-` 只在「資產」節以 `asset_request.md` 之標題引用）：

```text
# Security Test Execution Guide — <delivery workbook 檔名>
Generated <date> from step_assets.tsv / diag_items.tsv / apk_pairing.tsv; every command below appears verbatim in at least one TC.

## 1. One-time environment setup
### 1.1 ADB
  - DUT connection and root: `$ adb root`（列出使用此前提之 TC 數）
  - Test runners: `$ adb install <CertProviderAndroidInstrumentalTest.apk>`；確認 `$ adb shell pm list instrumentation` 含
    `com.mitsubishielectric.ahu.efw.lib.melcocertprovider.libcertproviderservice.test/androidx.test.runner.AndroidJUnitRunner`；
    KeyInstall runner：`com.mitsubishielectric.ahu.efw.lib.testkeyinstalldiagservicemanager/…`（apk 檔名依 Z1 README，查無則寫 `<apk provided by RD>`）
  - Test assets on host: `SecurityAssets/oem-certs/cert-provider/`（層級依 037 逐字）
### 1.2 UDS
  - Tester: NR1L_UDSTool（或等效）；診斷 CAN ID 依 VHAL Guide §2.4（Hi `0x7BF/0x53F`、Mid `0x18DA87F1/0x18DAF187`）
  - Session: `$ 10 60`（SystemSupplierSpecific）／`$ 10 03`；ADA+ 前提（DID 0108 Active Roles）
### 1.3 CAN
  - PCAN／等效；DBC = forms/ 四本；訊號句式 `Send CAN: <MESSAGE>.<Signal> = <raw> (<label>)`
### 1.4 Host tools
  - OpenSSL（`openssl verify`／`x509 -text`／`req -noout -verify`）；`logdecrypt_Ver2.sh`
### 1.5 Document review
  - `<obtain <artifact> per RD>` 之 artifact 清單（自 R-SEC20／23 TC 抽出去重），與 RD 建置環境存取需求

## 2. How to read observations
  - RC：instrument 之 `OK (n test)` / `FAILURES!!!`
  - LOG：`$ adb logcat -s <TAG>`；tag 清單（`MelcoCertProviderTest`、`sec_pki_ki_installer_lib`、`sec_pki_ki_daemon`、`KeyMasterWrapperService`、`Logdog`、`logdog` …，自 TC 抽出去重）
  - FILE：`$ adb shell od -t x1 <path>`／`cat`／`ls -l`；路徑清單（`/mnt/vendor/oemkeys/installstate`、`/mnt/vendor/oemkeys/status/keyinstall`、`/data/vendor/dauth/{ts,vc,seqId}`、`/data/misc/ecuidentity/ecu.crt`、`/vendor/odm/etc/cert_store/` …）；`installstate` 四態表（0x00~0x03，Z1 逐字）
  - UDS：`Positive response is received: <SID+40> …`／`Negative response …: 7F <SID> <NRC>`
  - HOST：openssl stdout 逐字（`<file>: OK`、`unable to get issuer certificate`、`verify OK`）
  - CAN：`<MESSAGE>.<Signal> = <raw> (<label>) is sent`

## 3. Command index
  每一 `$` 指令（去參數變體後去重）→ 出現之 TC ID 清單。自 TC 全表生成。

## 4. Assets still to be provided
  自 `placeholder_by_token.tsv`：每 token 之性質、供給方、涉及 TC 數；指向 `asset_request.md`。

## 5. Automation mapping (for the later script phase)
  通道 → helper 名（`adb()`, `instrument(class, method)`, `uds(bytes)`, `can_send(msg, sig, raw)`, `host(cmd)`, `pause(prompt)`, `doc_review()`）；
  各通道 TC 數；PHYS 步驟句式清單（`Insert …`／`Power cycle …`）→ 將以 `pause()` 停等；DOC → 不生成。
```

## §3　產出

1. `RULINGS.md`：R-SEC22(a) amend；`--gate`。
2. `audit_newtc.tsv`／`ANOMALIES.md` 更新。
3. `SEC_VER=v07` 重跑 → `merged/security_v07.xlsx`（diff 預期只 `NR1L-LOGENC-007`／`-008` 之 Remarks 2 格）；交付候選同名覆蓋。
4. `delivery/EXEC_GUIDE.md` ＋ `build_exec_guide.py`；指南內每一指令以腳本反查 TC，回報「指南指令 ⊆ TC 指令」= 100%。
5. `delivery/` 現為五件：xlsx、`coverage.tsv`、`placeholder_summary.tsv`（＋`by_token`）、`asset_request.md`、`EXEC_GUIDE.md`。

## §4　上繳要求（`up/20260917_SEC-12.md`）

1. SEC-11 三支 commit hash；R-SEC22(a) amend 實際號、sha8、`--gate`。
2. v06→v07 逐格 diff（預期 2 格）；lint 全項；selfcheck。
3. `EXEC_GUIDE.md` 全文；§2 各節之計數（runner 2、tag n、path n、指令 n、artifact n、token 11）；「指南指令 ⊆ TC 指令」驗證結果；apk 檔名查得／佔位。
4. §5 自動化映射之通道 TC 數表（供 SEC-13 估工）。
5. 自報錯誤。

## §5　升級條件

- 指南含任何 TC 中不存在之指令／路徑／tag（⊆ 驗證 < 100%）。
- v06→v07 diff 超出 2 格。
- lint 任一門檻非 0。
