# 下放包 CMF-02 — Comfort HMI Revise 候選本（JSON patch ＋ 23 sibling ＋ CMF-01 裁定落帳）

- 日期：2026-09-18
- 前置：`down/20260918_CMF-01_review.md`（裁定 R-C46～R-C53 暫編、R-C6(amend)；編號但書見該檔 §3）
- 產物落點：`features/comfort/sandbox/revise_0918/`。**不入 `delivered/`、不覆寫 0907 本、不 commit、不跑 `--gate`／`--regen`、不手改 `RULINGS.sha.tsv`**（GC-16 未落地前，裁定只落 `features/comfort/RULINGS.md` 與 profile，sha 表留待 GC 包）。
- 開工前：`git status` 回報 `features/comfort/` 有無未提交之 CMF-01 產物；有則照列，不代為處理。

## 1. 裁定落帳（文件層）

1. `RULINGS.md` 追加 CMF-01_review §3 各條（live numbering，附暫編↔實編對照）。
2. profile 同步：§0 Test Group；§3.5 stem 去 `SYS1_` 並記其出處 ENTRY 032／034；§3.2 J 欄格式改記現行匯出規則（無節次括號）；§3.9 車型欄作廢並指向 R-G74；§5.1「目前之 BLOCKED-SPEC 列」加 `019-03`。**profile 既有文字依 R-TM13 加刪除線保留，不刪。**
3. `DATA_REQUESTS.md` 登 DR-46。`ANOMALIES.md` 登 A-CMF1～7（文字取 review §1）。

## 2. JSON 層 patch（`CMF-01_patch_plan.tsv` 74 列，修正後套用）

| 來源 | 套用方式 |
|---|---|
| M1 `019-03` | `[BLOCKED-SPEC]` 列，Owner＝條文所具名之 VF HVAC document；Remarks 自足、無內部 id |
| M2 `103-01`／`103-02` | **去掉 `3 seconds`**。兩條共同骨架：① 觸發 popup → ② `Press <同一控制> again before the popup times out`。`103-01`（timeout restarts）：③ `Hold for 5 s` 自**第一次**按壓起算之觀察點 —— ER「popup 仍顯示」。`103-02`（additional 5 sec）：③ 自**第二次**按壓起 `Hold for 5 s` —— ER「popup 不再顯示」。若執行層判兩 leaf 於條文上不可分（同一句之兩種說法），**停下回報**，不硬拆 |
| M6 四條 | 該步改為整行 `PENDING: DR-46 …`；其餘步驟不動 |
| M8 三條 | `test_set` 依 framework Part N |
| M9 `Note` 25 | `Note` → `Read … and record`（canon §5.6：recording 步不寫 baseline）|
| M9 複合 35 行 | 拆步，ER 同步補行維持 1:1；拆後新增之 ER 只得陳述該步之可觀察結果，不得引入條文未有之事實 |
| R-C47 | 465 條 `priority` 依 0907 本 P 欄回灌 |
| R-C6(amend) | `test_group` |
| R-C49 | `_compact_refs()` 改為一節一行、升冪、leaf 自身節次置首、每行重述 stem、外部文件各自成行；**先跑全量 before／after 對照**，任何一條之節次集合有增減即停 |
| R-C50 | 26 條上半摘句（連續子字串 ≤ 50 token）。先出 `upper_excerpt_plan.tsv`（原句／摘句／token 數／對應下半），摘句須仍過 `test-item-upper-verbatim`；同 leaf 多條之上半可因下半而異 |
| R-C52 | wb-001～004 Remarks conflict 句（英文、自足、≥ 40 字元、登 `AMBIGUITY_REMARKS`）|
| M7 引號 | **本包不做**（需逐條辨「標籤」與「狀態名」）；只出 `label_quote_plan.tsv`：每個 hit 判 label／state／verbatim 三類，附建議 |

## 3. Sibling 23 條（`sibling_plan_0907.tsv`）

- 依 plan 產出；TC ID 依 R-C46 **接尾端**（`019-03` 取 `-466`，sibling 自 `-467`）。
- `003-07` 分支 (a)：ER「most closely matches」條文無值 → ER 該行 `PENDING: DR-{n}`；分支 (b) 之 4 個 mode 值受 DR #31 所限者同。**PENDING 數須於上繳首段報出。**
- `018-05` 之 `press and hold`：先回條文判是否屬該 leaf；不屬則不產，記於 plan。
- 拆出之原條同步瘦身（刪去移交 sibling 之步驟／ER 行）；wb-006 第 3 步依 plan 刪。
- 每條新 sibling：下半與同 req 既有各條兩兩相異；介面型四軸逐條問（profile §3.2）；Priority 依 R-C47。
- **停點**：實產 > 30 條，或任一條需要條文以外之值才能寫 ER → 停。

## 4. Revise 候選本

1. 母本＝`delivered/…_ComfortHMI_20260907.xlsx` 之**複本**（R-C46）。`xlsx_surgical` 寫入；C／E／O／AA 既有格不動。
2. 既有 465 列：列位、F、E 不變。row 116 填入 `019-03` BLOCKED 列。新列接 row 476 起；新列之 O＝`NEW`、AA＝`PeiPYHsu`、S＝`NA`；C 取 0907 本同 req 既有列之 C 值（Polarion req 號，屬需求側）；E（TestRail ID）留空。
3. 車型欄（R-C48）：W／X 全列 `0`；T／U／V／Y／Z 既有列不動，新列填 `1`。
4. 檔名 `…_SWQT_ComfortHMI_20260918.xlsx`；zip member 數與 DV `sqref` invariant 為 ABORT 級；B 欄公式延伸至新列。
5. 寫後逐格回讀對帳：未列於本包之格，與 0907 本**逐格相同**；不符數須為 0，否則不交。

## 5. 唯讀調查（不改任何 TC）

1. **PROXI 軸值表** `docs/reports/proxi_axis_values.tsv`：五份 PROXI（HDCC27_initial／DT28／Promaster／Toro／Fastback）× `Climate_Type`／`Rear_Climate`／`Climate_Panel_Type`／`Integrated_Climate_Touchscreen`／`Automatic_Recirculation`／`HVAC_AUTO_ECO_Mode`／`HVAC_Mode_Type` 之**值與 label 逐字**；不作「值 → profile 第 N 軸」之對映判讀，只並列。DT27（表頭）vs DT28（資料夾）照實標註，不推定同一。
2. **Comfort 入口**（R-C53）：查 `spec-index/cache/SYS1_HMI_Menu_Bar_and_App_Drawer_…xlsx` §4.1 命名表與 `forms/HMI Settings List …` 有無 Comfort／Climate 列；報檔、sheet、row、逐字。查無 → 提案 DR 文字，不猜 hop。
3. 0907 本 Cover／Product Document／`Test Case Framework` 分頁現況（哪些格有值）；`J5` 日期 `2026/8/17`。只報不改。

## 6. 自檢與上繳

- lint（含 profile gates）於 JSON 與候選本兩層各跑一次，附 `LINT_STATUS: OK` 哨兵；lint 崩潰不得讀成全綠。
- 上繳首段：停點命中與否、PENDING 行數、實產 sibling 數、§4-5 之不符數。
- 附：暫編↔實編裁定對照、`upper_excerpt_plan.tsv`、`label_quote_plan.tsv`、`proxi_axis_values.tsv`、產物 sha256 表、old→new 無（既有 ID 未動，明載）。
- `DELIVERY.sha256` 追加 ENTRY（type `candidate`），checksum 行指向 sandbox 路徑。
