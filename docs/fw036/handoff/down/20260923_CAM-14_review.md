# 下放包 CAM-14 審閱（分析層 2026-09-23）

對應上繳包：`docs/fw036/handoff/up/20260923_CAM-14.md`
判定：**A 本 dry-run 通過（組簿零新違規、對帳 0/0）；B01a 22 列判過（B 本第 1 批）**。五項待裁全裁；CAM-15 同發。

---

## §一　裁決

| # | 項 | 裁決 |
|---|---|---|
| 1 | R-G42 一「037 有列而無 TC 補一列僅填 D 欄」 | **dry-run v2 補 4 列**（`-007`／`-013`／`-024`／`-025`），Test Set／其餘欄空；`AH` 註 `No TC — all sources delegated to SWE-CAM-nnn (R-CAM10)`／`BLOCKED — DR-CAM-a`。交付口徑 244 列 |
| 2 | `AUX Camera` vs `Auxiliary Cameras` 字面相近 | **改名 `Auxiliary Cameras` → `Additional Cameras`**（Cargo/CHMSL、SVC、FFC 為車上其他相機；`AUX Camera` 為 VF617 之外接 AUX 輸入，與 B 本 `AUX Camera Access`／`Settings` 同義域，保留）。framework 重開一次（Tier 2，Pei 可撤），`layer2_assign`／json／coverage 同步 |
| 3 | A-CA33 `SWE1-RVC-008` 家族之錨 | **N 欄寫 `…_7.2.n`**（canon §9-16／§10.7：spec_reference 為 TC 直接驗證之章節）；D 欄仍為 037 之 `SWE1-RVC-008-0n`（R-G42 一之 req_id 權威）。兩欄各安其位，reasoning 註 037 收摺為 `_7.2`。`-008` 本列（排他性驗證點）N 欄 `…_7.2` |
| 4 | B01a 切點 | **改 23 列**（`-008-06` 併入，不切開 §7.2 家族）；`7.3.1`～`7.3.3` 歸 B01b。下放包 §3 之列示為分析層未查章節序即寫，記一筆 |
| 5 | DECISIONS 6-51 B 本 Vehicle Model 判準（可佈性） | **准並立 R-CAM18**：B 本列預設五款皆 `1`；PROXI byte 於該平台本不存在、或觸發訊號於該平台 DBC 不存在者該欄 `0`（reasoning 具名證據）；598／5210 恆 `0` |
| 6 | selfcheck 第 9 項收緊為交集 | 准 |
| 7 | DR-CAM-r（Camera App 配備旗標）／DR-CAM-s（熱保護進入法） | 接受，Pei 送 |

## §二　R-CAM17 證據複驗

隨機取證據 6（`therm` 四本 DBC 零命中）：分析層以 CAM-12 審閱所留之 `/tmp/f.dbc`（FDCAN8）`grep -ic therm` → 0，屬實（其餘三本未複驗，按樣本制）。

## §三　備查

- A 本交付阻於 f／g／i／j／k／m／p／q 八條 DR；B 本新增 r／s。
- `bench_verify.md` 14 列。
- B 本第 1 批乾淨（R-G14 對 B 本另計）。
