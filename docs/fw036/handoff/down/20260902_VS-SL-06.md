# 下放包 VS-SL-06 —— 站⑤ 寫回（轉正＋ID 對照＋台帳落檔）

日期：2026-09-02　層級：Tier 2 → Tier 1
前置：站④ 抽驗通過（Pei 2026-09-02）；`down/20260902_VS-SL-05_review.md`
上繳寫 `up/20260902_VS-SL-06.md`。

## §0　狀態與 gate

- 三本沙盒稿（SHA `49b1bf94…`／`b0ee608d…`／`3a1b5f40…`）經 Pei 抽驗**通過**，即為轉正母本；轉正時逐本先核 SHA，不符即停。
- **出貨 gate 不變**：vf230 含 `PENDING` 25 列、BL 含 `Body_Types` PENDING 與車型欄未填 —— **不得入 `delivered/`**，待 DR 結案或 Pei 裁降轉（IN §8.4.3；**原誤植 FO §8.4.3，VS-SL-06 上繳 §5-3 指正，2026-09-02 更正，記 A**）。本包只到「正式工作位置＋交付前置」，不出貨。
- DR 稿寄出屬 Pei；寄出取號後另令回填 `PENDING: DR-{n}` 之 `{n}`。

## §1　轉正

- 落點依各 feature 既有慣例：`features/vehicle_setting/` 無 `output/` 者**建立**（沿用其 `.gitignore` 現況，勿改）；BL／VC 落各自 `output/`
- 檔名沿原 FM-WI-FSM-036 全名，日期後綴改 `_20260902`
- 沙盒稿與 `.bak` 鏈**保留不刪**（追溯）；各 feature `MANIFEST.tsv` 增列（路徑＋SHA＋狀態 `writeback-pending-DR`）

## §2　TC ID 與 TestRail 對照（R-G42）

先實測三本之 E 欄（TestRail）／F 欄（TC ID）現況再動作（先量再斷）：
1. F 欄空者依 R-G42 賦號（`NR1L-` 前綴、D 欄升冪、Test Group 全名）；F 欄已有者不改號
2. 每本出 `reports/testrail_id_map_<slug>.tsv`：`old_E`｜`old_F`｜`new_F`｜`status`（kept／assigned／**RETIRED**）
3. vf230 移除之 19 列自 `vf230_removed_non_nafta.tsv` 帶入，`status = RETIRED`
4. 上繳附三本 E／F 現況統計（各欄空／非空計數），賦號規則對不上實況時**停下來報**，不硬套

## §3　台帳落檔（編號一律登錄時依 live 台帳取）

1. `features/vehicle_setting/RULINGS.md` 增錄（條文由本包附，號現場取）：
   - 家族閘（R1）：設定名自身無 FIP 列、alias evidence 指向之家族列為變體數閘者套家族閘
   - 引號式條件（R1b）與非 ASCII 參數名之處置（後者依 R-13 不代入）
   - `FIP_ALWAYS_OFF`／`FIP_ALWAYS_ON`：FIP 常數者不 PENDING、不 DR、掛登記旗
   - 否定式代表值（R4）：補集取值表內代表值，reasoning 註兄弟值；市場相關者依 R-VS84 取 NAFTA 代表
   - Pre「menu is open」類步驟可控狀態於插入導覽後必刪（IN §4.4）
2. `ANOMALIES.md` 增錄二筆：A（分析層）`FO §4.4` 誤植；A（執行層）`down/` 覆蓋事件（已復原）
3. `FW036_R1L_VehicleSetting_Profile.md` 增 `[ADD]`：Settings 查找配方指針（HMI Settings List → 路徑／控件／選項；FIP → 條件；proxi_values → raw；R1／R1b／R2／R4 規則；R-VS84 NAFTA）—— VF665 生成即用
4. `forms/LOOKUP_MISSES.md` 依 R-G13 增候選：`Easy_Entry_Menù`（值表拼寫差）、No.275 `Alwatys` 原文缺字
5. **不動全域 `RULINGS_LEDGER.md`**（本輪無全域級新規；R-G42 之 MCP 寫回另案既有）

## §4　自檢與上繳

- 轉正三本 SHA = 沙盒稿 SHA（逐本 assert）；MANIFEST 增列成功
- ID 對照表列數 = 各本現列數＋RETIRED 數（vf230 438＋19）
- lint 四項於轉正本重跑（0 方過）
- 上繳：轉正路徑與 SHA、三份 ID 對照表、E／F 現況統計、台帳 diff 摘要、自檢、自報
