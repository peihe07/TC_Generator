# CDD-02 審閱 —— Diagnostics pilot01

審閱對象：`up/20260917_CDD-02.md`。審閱層 session：2026-09-17。
**結論：pilot01 形式通過、內容有兩處必修（§三），不另開 pilot02；修正併入 CDD-03 batch 1。**
升級條件四項未觸發；lint 真違規 0；§9 自檢 17/17；R-SEC15 型自檢全過。

---

## 一　逐項核對

| up 節 | 判 | 備註 |
|---|---|---|
| §1.2 R-G75(a) 攔下第三次覆寫 | 通過（事故升級）| 過渡規則在落檔前就發揮作用；`[A-DIAG15]` 三次。**GC-16 由「SEC 收斂後」改為「下一個全域包」**（§五-1）|
| §1.3 三列數 35／49／54，集合相符 | 通過 | 對方 `R-SEC22(a)`／`R-SEC24` 保留 |
| §1.5 `column_map.tsv` | 通過 | SWC 0708 `P` 無 P3、`AF` 全空、`O` 全 NEW —— 為 R-DIAG10 與 R-DIAG3(amend) 提供實測底 |
| §2.1 分類器只看標題 | 通過（缺陷已修）| 3 列改判（-106／-160／-340-001），母體 229，Layer 2 組數不變；反查自誤 32 列之教訓（§8-1）收 |
| §2.2 `-154` Title/Description 錯置 | **通過，追認** | 依 §8.6 與 R-SEC15(f) 取 Description 為正向結果回報，`split_reason` 記理由，FB-DIAG-g 送 Nik —— 處置正確 |
| §3 13 TC ＋ 1 佔列 | 通過 | ID 依 framework 順序連號 |
| §4.2 `-001` 未拆值域 | **追認** | CFTS004 `$283F` 四列皆無值域，不造值；DR-DIAG-5 |
| §4.3 `-272`／`-189` 不分項 | **追認** | 一 SWE1 列只追一子欄位，分項即擴入他列（§8.2.1）；§6.1 用於 -154 有據 |
| §5 lint 歸因 | 通過 | 真違規 0；`RM-DIAG` 自身含 `; ` 之缺陷由實本揭出，自測補接合式 —— 正確 |
| §6 自檢 | 通過 | §9 為 17 項，下放包書 18 為分析層錯（A10）|

## 二　分析層自報錯誤（登 `layer = analysis`）

| # | 內容 |
|---|---|
| A10 | CDD-02 §5-2 書 canon §9「18 項」，實 17 |
| A11 | CDD-02 §3 表以 CDD-01 分類器之型別下包（#8、#10 標 negative），未自查 -154 之 Description；且 #1「值域 → sibling」未先查 CFTS004 即預估 2–3 條 |
| A12 | CDD-02 §6 預期 PENDING ≤ 10 未考慮 DR-DIAG-3 為每條 TC 一行之系統性缺件 |

## 三　內容必修（批次前）—— 兩處

**(1) `specification_reference` 未升冪** —— `NR1L-DIAG-009` 列 `4940473 / 4940478 / 4940477 / 4940474`，違 IN §10.7「同文件內 ID 升冪」。
全 13 條複查；同時回報：**全域 lint 有無「錨升冪」檢查項** —— 若無，登全域待辦（本包不加）。

**(2) Pre-Condition 1／2／4 之來源** —— `NR1L-DIAG-001`（與其餘 12 條同型）：
- `1. The vehicle is in Ignition ON state`
- `2. The diagnostic tool is connected to the OBD-II connector`
- `4. The rearview camera display is active on the radio screen`

CDD-02 §4 明文「診斷工具連線與 session 狀態只寫 CFTS004／SYSAD 明載者；未載 → PENDING」。
上繳包只對 session 給了 PENDING（DR-DIAG-3），1／2 之來源未註；4 於 CFTS004 r149 只述「brightness of the rearview camera display」，
未述寫入 DID 須顯示中，屬 §8.5 環境前提而非規格觸發。
處置：每條 Pre-Condition 逐行標來源（CFTS004 ObjectID／SYSAD 章節／R-SEC 前例）於 `reasoning`；
無來源者改 `PENDING: DR-DIAG-3` 併行或刪除（4 建議刪）。SYSAD 4.2.5 Assumptions 若載 ISO-TP over CAN／DoIP，
「OBD-II connector」不得由此推得 —— DoIP 可走乙太。

**(3) DR-DIAG-3（session 前提，13/13 條）之內查先於 DR** —— 執行層先於 CFTS004 全文 grep
`session`／`0x10`／`$10`／`extended`／`default session`／`security access`／`$27` 並回報命中列；
CFTS004 r350（`SYS-RA-DIAG-349`「HU shall implement each diagnostic requirements for enabling and d…」）
與其鄰近列為首要嫌疑。命中則依 R-DIAG5(amend)(c′) 第一層解；全無 → DR-DIAG-3 維持，Pre-Condition 該行改
`PENDING: DR-DIAG-3 diagnostic session and security access prerequisite`（合併 security access 一起問）。

## 四　審閱裁定（依常規即生效，Pei 不同意者撤回）

```text
R-DIAG11  Out of Scope 佔列之 lint 豁免
  `AF` Test Result = `Out of Scope` 之列，lint `I`／`M`／`R`／`Z` 豁免；`R1-DIAG`、`RM-DIAG` 仍檢
  （Requirement ID 須單值、Remarks 須含 `CFTS004 Category: Out of Scope`）。
  實作為 FEATURE_EXEMPT 之列級條件（非全 feature），供其他 feature 之 OOS 列沿用。
```

```text
R-DIAG12  `I-cross` 之 Diagnostics 豁免
  比照 R-SEC15(j)：`FEATURE_EXEMPT["diagnostics"]` 加 `I-cross`。理由：ER 為 UDS 請求／回應斷言，無觀測窗。
```

```text
R-DIAG10(amend)  Priority 值域對齊 SWC 0708
  P3 不用（SWC 0708 值域實測無 P3）；Out of Scope 佔列 Priority 留空。其餘不變。
```

R-DIAG10 本體（P0／P1／P2 判準）**追認**，pilot 之 13 條指派全符。

## 五　待 Pei

1. **GC-16 提前**：三次覆寫已證 R-G75 只是止血。建議 SEC-13 上繳後、任何新包前，單 session 執行全 repo 重生；
   在此之前 Security 線下一包 §0 亦須寫入 R-G75（分析層無法代寫 Security 包，請 Pei 轉達）。
2. FB-DIAG-a～h（新增 g：row 161 Title/Description 錯置；h：row 347 `"Previous Out of Range"`）併送 Nik。
3. commit：建議准，訊息沿 up §11。

## 六　CDD-03 隨本審閱同時落檔

= pilot 兩處必修 ＋ DR-DIAG-3 內查 ＋ R-DIAG11/12/10(amend) 落檔 ＋ **batch 1**（Layer 2 #6～#10，R/W 小群 31 列）。
