# 下放包 SEC-05 —— framework Revise（Test Set 英文定名）＋ batch1 v02 ＋ batch 2 產出 ＋ 合併本 `security_v01`

前置：`up/20260916_SEC-04.md`、`down/20260916_SEC-04_review.md`。
Pei 2026-09-16 裁：review §二（`SEC_OBSERVE` 延伸）與 §三（六個 Test Set 英文定名）**准**。
執行層 session：新開；git 未修復者產物落工作樹。

---

## §0　禁區

- git 不動；`down/`／`up/` 既有檔不動；其他 feature 不動；canon 不動。
- `framework.md` **本包唯一一次解鎖**，僅改 §2 所列六個 Test Set 名稱；其餘 24 組名稱、Layer 1、Layer 3 不動；改完重新 LOCKED。
- `sandbox/batch1/security_batch1_v01.xlsx` 不動（R-TM13）；v02 為新檔。
- `lint036.py` 只動 `SEC_OBSERVE`（§1 R-SEC7(c) amend 之三式，SEC-04 已落者不再改）；回歸三路徑 sha 不變。
- batch 2 只產 **產出群 13 列**（Testing 11 ＋ CONVERT 2）；DEFERRED 8 列不產。

## §1　裁定落檔（取號現查；預期 R-SEC17、R-SEC18）

```text
R-SEC17  Test Set 英文定名（取代 framework v01 之五個中文佔位；A-12 更正）
  Cert Provider   `(非功能)`        → `Service Robustness`     （CP-006、CP-010）
  Key Install     `(非功能)`        → `Platform Compliance`    （KI-012、KI-013）
  SAM             `(環境)`          → `Service Environment`    （SAM-0001／0002／0008／0019；執行層以 037 原文複核四列是否皆屬「執行環境與部署前提」，不合者回報，不自行移組）
  ECU Cert        `(IPC／JNI／IO)`  → `Internal Interfaces`    （R14~R17）
  SWDL Secure Lib `(總則)`          → `Library Scope`          （SWDL-001）
  Log Encrypt     `(其餘)`          → `Encryption Services`    （LOGENC-001~005／007~009）
  名稱為能力層（IN §4.2），非分類；不得再以括號註記作 Test Set。
```

```text
R-SEC7(c) amend  七類為觀察面之類別；各類具體句式由 profile §3 常數表列舉並可增列，增列須附素材出處。
  本次增列：RC 類 `The adb pull command reports "1 file pulled" …`、`The log buffer is cleared`；
            FILE 類 `adb shell procrank|df|ps` 之 stdout（037 KI-013／CP-010／SAM-0002 verbatim）。
```

```text
R-SEC18  batch 2 之 CAN 通道寫法（SAM-0013／0017 首例）
  (a) 觸發以 VHAL Guide R5 之 VHAL→CAN 對照為橋（`forms/VHAL_User_Guide_R5.pdf`）：
      `IGNITION_STATE` ↔ `CmdIgnSts`（Atl-Hi `BCM_FD_10` 0x481 / Atl-Mi `STATUS_BH_BCM2` 0x46C）；
      `POWER_MODE_STS` ↔ `PowerModeSts`（`BCM_FD_9` 0x42A）。
  (b) 步驟依 IN §8.7.5(c)：`Send CAN: <MESSAGE>.<Signal> = <raw> (<label>)`；raw／label 逐字取 `forms/` 之 DBC `VAL_`（R-7、R-17）；
      DBC 查無該訊號或 label 者，raw／label 以 `<…>` 佔位並 Remarks 註 `DBC lookup pending`——不得造值。
  (c) Atl-Hi／Atl-Mi 訊息不同 → 拆 sibling，Vehicle Model 各勾（Hi：HDCC27／DT27 = 1，其餘 0；Mi：VF637／Toro／Fastack = 1，其餘 0）。
  (d) ER：`<MESSAGE>.<Signal> = <raw> (<label>) is sent` 只用於 HU 送出之訊號；SAM 之結果面（`/data/vehicle/dauth/seqId` 歸 0、目標功能 OFF）以 FILE／LOG 類觀察。
```

## §2　framework Revise（R-G72）

1. `framework.md`：加 `REVISE v02 2026-09-16 (R-SEC17)` 段，列六項 old → new；Layer 2 表就地改名；末記 `LOCKED v02 2026-09-16 (R-SEC17)`。
2. `layer2_assign.tsv`、`feature.yaml`（若含 Test Set 列舉）同步；`sibling_plan.tsv` 之 `test_set` 欄（若有）同步。
3. 回報：改名前後 Layer 2 名稱之 CJK 計數（預期 5 → 0）。

## §3　batch1 v02

`gen_batch1.py` 重跑 → `sandbox/batch1/security_batch1_v02.xlsx` ＋ json。
預期：TC 72、PENDING 85、`K=0`、其餘 lint 計數與 v01 相同（`U=85`、`W=4`，門檻四項 0）、selfcheck 0；v01 → v02 之差異**只在 `Test Set` 欄 10 格**（執行層以 cell diff 證明）。

## §4　batch 2 產出（`sandbox/batch2/security_batch2_v01.xlsx`）

### 4.1　範圍（執行層以 `batch_order.tsv` `ccvr_batch=2 ∧ disposition≠D` 取，對帳）

分析層預判 13 列：SAM-0003／0005／0006／0009／0010／0011／0012／0013／0014／0016／0017（Testing 11）＋ CONVERT 2（執行層自 `peer_review_rows.tsv` 取，預判 LOGENC-006 與另一列）。差異逐列回報。

### 4.2　sibling 規則

同 SEC-04 §4.2；另：
- SAM-0012 ValidityCounter 依 IN §8.3 邊界：`0`／`1`／`65535`（037 原文之值域）→ 3 sibling，Design Method `邊界值分析 (Boundary Value Analysis, BVA)`。
- SAM-0010 TimeStamp 舊於已裝者 → 負向 1；新於 → 正向 1。
- SAM-0013／0017 依 R-SEC18(c) 各拆 Hi／Mi。
- SAM-0018 之 SAMType 列舉若在 batch 1 已拆，本批不重複。
- LOGENC-006：`getsslog`／`logdecrypt_Ver2.sh` 兩步為 HOST 通道，檔名格式 `HUssss_YYYYMMDD_HHMMSS_XXXX.enc` 保留佔位（IN §8.4.1）。

`sibling_plan_batch2.tsv` 先出；預估 **20~30**；升級門檻 15~40。

### 4.3　寫法

全部沿 batch1 v02；Priority 依 R-SEC16（SAM-0005/0007 P0、0013~0017 P1、0002/0008 P2）；CAN 首例（Hi、Mi 各一）全文貼回上繳包。
TC ID：SAM 自 batch 1 之末號接續（batch 1 SAM 001~006 → batch 2 自 007）；LOGENC 自 001。

## §5　合併本 `sandbox/merged/security_v01.xlsx`

batch1 v02 ＋ batch2 v01 合併，依 framework Layer 2 順序排列，D 欄升冪（R-G42）；51 列產出群全覆蓋；DEFERRED 19 列不入，於 `Reference` sheet 或 Remarks 不提（交付本不載內部狀態）。
另產 `features/security/data/coverage.tsv`：70 列 × `disposition | batch | tc_count | tc_ids`（DEFERRED 列 `tc_count=0`），供 SWE.6 追溯矩陣。
`pending_summary.tsv` 更新為合併本口徑。

## §6　上繳要求（`up/20260916_SEC-05.md`）

1. R-SEC17／R-SEC18／R-SEC7(c) amend 之實際號、sha8、`--gate`。
2. framework v02 之 old→new 六項、CJK 計數 5→0、LOCKED v02。
3. batch1 v02：72／85／`K=0`、cell diff 只 10 格之證明。
4. batch 2：13 列對帳、`sibling_plan_batch2.tsv` 計數、TC 數、lint／selfcheck、CAN 首例（Hi／Mi）全文、DBC 查得／佔位之計數。
5. 合併本：TC 總數（預期 72 ＋ batch 2）、六組各數、Priority 分佈、`coverage.tsv` 之 51／19、`pending_summary.tsv`。
6. lint 回歸三 sha。
7. 自報錯誤。

## §7　升級條件

- batch1 v02 與 v01 之 cell diff 超出 `Test Set` 欄 10 格。
- `K` 非 0（任一本）。
- batch 2 sibling 總數 < 15 或 > 40；13 列集合不等。
- CAN 步驟出現非 `<…>` 佔位且非 DBC 逐字之 raw／label。
- 合併本 `coverage.tsv` 之 `tc_count>0` 列數 ≠ 51。
- 任何需改 canon 或再改 Layer 2 名稱之情形。
