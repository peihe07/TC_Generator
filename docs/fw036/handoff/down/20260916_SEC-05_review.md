# SEC-05 審閱 —— 上繳包 `up/20260916_SEC-05.md`（framework v02 ＋ batch1 v02 ＋ batch2 28 TC ＋ 合併本 `security_v01.xlsx` 100 TC）

審閱層：分析層，2026-09-16。
判定：**通過，不升級**。六項升級條件皆不成立；執行層十項自報中，**四項成因在分析層**（登台帳）；**三項待 Pei 裁**（併 SEC-06）。
里程碑：**70 列產出群 51 列全覆蓋、100 TC、K=0、門檻全綠**；剩 DEFERRED 19 列與 PENDING 124 行／11 token。

---

## 一、通過項

| 項 | 判 |
|---|---|
| R-SEC17／R-SEC18／R-SEC7(c) amend 落檔，`--gate`，R-SEC1~16 不變 | ✅ |
| framework v02：六名改、CJK 5→0、LOCKED v02；`layer2_assign.tsv` 同步 | ✅ |
| batch1 v02：72／85／`K=0`；v01→v02 逐格 diff = 10 格全在 H 欄 | ✅ |
| batch 2：13 列集合相等；sibling 28（15~40 內）；lint 門檻 0；selfcheck 0 | ✅ |
| CAN 首例 Hi／Mi：`Send CAN: BCM_FD_10.CmdIgnSts = 4 (RUN)`／`STATUS_BH_BCM2.CmdIgnSts = 4 (RUN)`，DBC 逐字，Vehicle Model 各勾 | ✅ 寫法定為 profile 範例 |
| `PowerModeSts` 無 resume label → `<…>` 佔位 ＋ `DBC lookup pending` | ✅ 正確不造值 |
| 合併本 100 TC；`coverage.tsv` 51／19；`pending_summary.tsv` 11 token／124 行；`X-m` 新登 | ✅ |
| `SEC_CMD_LINE` 認 `Send CAN:` 為通道行 | ✅ 准（R-SEC18 之必要條件；越出射程已具名）|
| `SEC_OBSERVE` 增 `getsslog`／`logdecrypt_Ver2.sh` | ✅ 准（037 verbatim）|
| R-SEC18(d) 路徑筆誤（`vehicle`→`vendor`）依 037 原文寫 | ✅ 正確；**A-14 分析層筆誤** |
| batch1 v01 json 被 v02 覆寫 | ✅ 可（json 為重生物；v01 xlsx 在）|

## 二、分析層自報（登台帳）

| # | 錯誤 | 說明 |
|---|---|---|
| A-14 | R-SEC18(d) `/data/vehicle/dauth/seqId` | 筆誤，正確為 `/data/vendor/dauth/seqId` |
| A-15 | SEC-05 §4.1 Testing／CONVERT 預判互換 | SAM-0017 空白 Verification Method 在 SEC-01 審閱三-3 已知屬 CONVERT，我仍列入 Testing；LOGENC-006 反之 |
| A-16 | Layer 2 組數 30 | SEC-01 §5 表我自己數錯（實 29）；執行層寫死 `30` 是沿我的數字，成因共擔 |
| A-17 | R-SEC17 把 SAM-0008 歸 `Service Environment` | SAM-0008 是 AuthData 格式需求，屬 `AuthData Verification`；SEC-01 §5 之 `(環境)` 群本就把它誤放 |
| A-18 | R-SEC16 未列 SAM-0003/0006/0009/0010/0011/0012 | 條文以 batch 1 視角列舉，漏 batch 2 六列 |

## 三、待 Pei 裁（SEC-06 落檔）

| # | 項 | 分析層建議 |
|---|---|---|
| 1 | SAM-0008 移組 | **移至 `AuthData Verification`**，走 R-G72 Revise v03（只此一列；`Service Environment` 剩 0001/0002/0019）。batch2 v02 ＋ 合併本 v02，預期 diff 只在 SAM-0008 之 2 TC 的 Test Set 格 |
| 2 | R-SEC16 之六列類推 | **准**：0006/0010/0011/0012 P0；0003/0009 P1。amend 落檔，Remarks 之 `assigned by analogy` 註可移除 |
| 3 | SAM-0013 resume-from-suspend 觸發 | 037 原文為「resume from suspend」；VHAL Guide 之 `POWER_MODE_STS` 對照為分析層之推測橋，DBC 證實 `PowerModeSts` 無 resume 值。**改判**：resume 之觸發為 `PHYS` 類 `Power cycle HU (IGN OFF → IGN ON)`？——不，那是 boot 分支。**建議**：resume 兩 sibling（SAM-025/026）之觸發步驟改 `PENDING: X-n suspend/resume trigger method`，登 `X-n`；`BCM_FD_9` 佔位式移除，R-SEC18(a) 之 `POWER_MODE_STS` 對照刪除（A-19 分析層推測橋）|

## 四、交付前尚缺（SEC-06 範圍）

合併本目前只有 `Test Case Specification` sheet 填值；交付本尚需：
- `Cover 封面`／`ChangeHistory 修訂履歷`／`Product Document 記錄封面頁`：依 SWC 0708 交付本之填法（執行層量測該三 sheet 之填值格與內容型態，逐格對照回報後填）。
- `Reference` sheet：列六本 037（檔名、版本）、SYS3 六本、CS.00102、CertProfile、CCVR v2.7 —— 只列**追溯母體與已引用之來源**，reference-only 者依 SWC 先例決定是否列。
- 交付檔名：依既有交付本命名式（執行層量測 SWC 0708／Home 0809 之檔名式回報，Pei 定）。
- PENDING 124 行：**不動**，待 Pei 送資產後 Revise；交付前依 IN §8.4.3 全結或 Pei 裁降。

## 五、SEC-06 預告（Pei 准 §三 後）

1. R-SEC16 amend、R-SEC17 Revise v03（SAM-0008）、R-SEC18(a) 刪 `POWER_MODE_STS`／(d) 路徑更正、`X-n` 登記。
2. batch2 v02、合併本 v02（diff 只在 SAM-0008 之 2 格 ＋ SAM-025/026 之觸發步驟改 PENDING）。
3. 三個封面類 sheet ＋ `Reference` sheet 填值（先量測後填）。
4. 交付候選 `security_v02.xlsx` ＋ `coverage.tsv` ＋ `pending_summary.tsv` 三件一組，供 Pei 送 RD review。

git：Pei 側修復後一次 commit SEC-01～05。
