# 下放包 CDD-04 —— Revise（R-DIAG13/14）＋ batch 2（Layer 2 #11 Buzzer Control ＋ #12 Speaker Quadrant Selection，61 列）

前件：`down/20260917_CDD-03.md`、`up/20260917_CDD-03.md`、`down/20260917_CDD-03_review.md`。
依常規：審閱後即發；依據 = CDD-03 審閱 §三／§四。執行層 session：沿用 CDD 線。
**與 GC-16 之先後**：本包可在 GC-16 之前執行，但 **T0 之指紋寫入依 R-G75**（GC-16 尚未落地）；
若 GC-16 已 commit，改依 R-G76(b) 全表重生。上繳包須註明採哪一制。

---

## §0　禁區

- git 一律不動；建議先 commit CDD-03（up §11 訊息）。
- 不動 `down/`、`up/` 既有檔；不動其他 feature 目錄。
- **只產 batch 2 之 61 列**；pilot `-006`～`-008`、`-013` 與 batch 1 `-043`/`-044` 以 Revise 改、ID 不變；其餘列不得生成。
- 條文本體不改。Pre-Condition 每行標來源；R-DIAG13/14 適用。
- `framework.md` 維持 DRAFT（LOCK 留 CDD-05，待 batch 2 證實 I/O 大群之 sibling 判準穩定）。

## §1　依據

- `R-DIAG13`（I/O Control TC 含 PC-3 security）、`R-DIAG14`（觸發鍵不選 Power／Dark）—— 條文逐字取自 `down/20260917_CDD-03_review.md` §四。
- 追認：DR-DIAG-3 結案、`[A-DIAG26]` Ignition ON 刪、`-054` 不拆而分項、讀寫拆二、negative 二軸。

## §2　任務

**T0 — 條文與指紋**：R-DIAG13/14 落 `RULINGS.md`；指紋寫入依 §首段之制；三列數（或重生前後數）回報。

**T1 — Revise**
(a) pilot v02 之 `-006`／`-007`／`-008`／`-013`（I/O Control）加 PC-3 `Security access 0x27 has been granted`，`pc_sources` 記 `SYSAD §4.5（保守套用；列舉未含 I/O Control）` → `pilot01_v03.xlsx`。
(b) batch 1 `-043`（及 `-044` 若同）觸發鍵 `"Power"` → `"Up"`，ER 同步 → `batch1_v02.xlsx`。
(c) `rd_feedback` 新增 **FB-DIAG-k**（SYSAD 作者：I/O Control 是否受 0x27 保護）。
(d) profile 加 R-DIAG13/14 之 lint 候選：`SEC-DIAG`（Procedure 含 `$ 2F` 而 PC 無 `0x27` 行 → FAIL）、`KEY-DIAG`（`$1820`/`$1821` TC 之 Procedure 含 `"Power"`／`"Dark"` → FAIL）；各 5 正 5 反自測。

**T2 — batch 2 sibling plan（先出，後產）**

| Layer 2 | DID | 037 列 | 含 pilot | 分析層預判 |
|---|---|---:|---|---|
| #11 Buzzer Control | `$5000`（R/W 1 ＋ I/O 39）| 40 | `-112`／`-113` | 13 源：tool-controlled／radio-controlled 各頻率（7 kHz、60 Hz、200 Hz、200 Hz 120 mV、1 kHz 120 mV）、entertainment mute、tone level 隨 volume、sequential clockwise routine（`-145`～`-147`）。**頻率／幅度／來源為 037 已分列之軸，不再拆**；每 positive 列 1 TC（controlOptionRecord 取 CFTS004；未載 → `DR-DIAG-4` 佔位）；negative 2、unsupported 1 |
| #12 Speaker Quadrant Selection | `$5001`（R/W 3 ＋ I/O 18）| 21 | — | 四象限（FL／FR／RL／RR）為 037 已分列（`-090`～`-105`）；`-106`（buzzer active → `$22`，`RESOLVED_CFTS004`）為 negative 型；`-049`（`$22` when all outputs functioning）同 |

回看 CFTS004 `$5000`／`$5001` 兩節全部列（含 Information）：controlOptionRecord／controlEnableMask 之位元定義若明載，寫入 Procedure 之位元組串；未載一律 `<controlOptionRecord>` 佔位 ＋ DR-DIAG-4。**不得依 ISO 14229 之 IOCP 通用值（`00`/`01`/`02`/`03`）填 controlOptionRecord** —— R-DIAG5(amend)(d) 之封閉五碼只涵蓋 NRC，不涵蓋 IOCP。

**T3 — batch 2 產出** → `sandbox/batch2/diagnostics_batch2_v01.xlsx`／`batch2.json`／`coverage.tsv`
- ID：pilot `-006`～`-008` 沿用；新增自 `-056` 起連號；`-009`～`-013` 留號。
- 音訊可觀察現象（tone audible on speaker X）只寫 CFTS004 明載者；量測值（120 mV）以 CFTS004 用字入 ER。
- sequential clockwise routine（`-145`）：`Hold for 10 s` 獨立成步（§8.7.5(f) 保持步驟），ER 對應。
- 其餘規格同 CDD-02 §4／CDD-03。

**T4 — lint／自檢／coverage**：v03、batch1 v02、batch2 各跑；`SEC-DIAG`／`KEY-DIAG` 若已實作一併跑；§9 17 項；R-SEC15 型；coverage 61/61。

**T5 — 合併本** `diagnostics_v02.xlsx` = batch1 v02 ＋ batch2 ＋ pilot v03 其餘（`-009`～`-013`）。

## §3　預期數字

| 項 | 預期 |
|---|---|
| batch 2 TC | 61 列 → 75–95 TC |
| PENDING 行 | DR-DIAG-4 類 ≤ 30（controlOptionRecord 未載者每條 2 行）|
| lint 真違規 | 0 |
| Revise 影響 | pilot 4 條 PC ＋1 行；batch 1 兩條步驟改鍵 |
| 合併本 | ≈ 130–150 列 |

## §4　上繳要求（`up/20260917_CDD-04.md`）

1. T0 指紋制與數字。
2. T1 Revise diff 摘要；FB-k；兩項新 lint 自測。
3. T2 sibling plan 全表（含 CFTS004 controlOptionRecord 有無之逐 DID 結果）。
4. T3 TC 清單；貼入 **`-145` sequential routine 一條全文** ＋ **`$5001` 任一象限 positive 一條全文**。
5. T4 lint 紅數歸因；自檢；coverage。
6. §3 預期 vs 實測；自報錯誤；未結 DR／FB；建議 commit。

## §5　升級條件

- 指紋集合不符（R-G75）或 `--check` 不綠（R-G76）。
- CFTS004 `$5000` 節之 controlOptionRecord 完全未載且 037 亦未載，致 #11 39 條 I/O TC 之觸發步驟全數 PENDING —— 此為**回報不升級**，但須附 DR-DIAG-4 之補強問句（要 controlOptionRecord／controlEnableMask 全表）。
- 任何需改 canon 之情形。
