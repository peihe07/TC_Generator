# 審閱 —— 上繳包 PU-02（Popup 交付，2026-09-08）

對象：`up/20260908_PU-02.md`
下放包：`down/20260908_PU-02.md`
方式：分析層直讀 `features/popup/delivered/…_Popup_20260908.xlsx` 與來源本、
`feature.yaml` 獨立複算，未執行 `pu02_deliver.py`。

**總判：通過，可交付。** 複算 11 項全數相符；3 項具名偏差全部成立且處置正確；
§1 之停手回報處理正確。另列 **2 項須知（甲）**、**1 項分析層自報（乙）**。

---

## 一、獨立複算

| 項 | 上繳所稱 | 分析層實測 | 判 |
|---|---|---|---|
| 交付本 sha256 | `ab05bb7b…f774c4df` | 同 | ✅ |
| 來源本 sha256 未變 | `a13559d5…e44ec8d6` | 同 | ✅ |
| zip member | 48 → 48，集合相同 | 同 | ✅ |
| 相異 member | 僅 `sheet3`／`sheet5` 兩件 | **同**（`['xl/worksheets/sheet5.xml', 'xl/worksheets/sheet3.xml']`） | ✅ |
| TC 分頁 xml diff | 0 | `sheet6` 未列入相異 member | ✅ |
| ChangeHistory xml diff | 0 | `sheet4` 未列入相異 member；A/B/C 三列 `datetime` 型別原樣保留 | ✅ |
| **x14 dataValidation** | 1 → 1 | **1 → 1**（classic 3 → 3） | ✅ |
| TC 5 列逐格 diff | 0 | 13 個追蹤欄 × 5 列，**差異格 0** | ✅ |
| Cover `D9` / `G9` | `PeiPYHsu` / `2026-09-08` | 同 | ✅ |
| Product Document 8 格 | B3–B8、A13–D13 | 同；`B4` 為本簿交付檔名全稱 | ✅ |
| `feature.yaml` `popup_list` | 指新基線 | 已改，且舊值與 R-POP27 理由保留於上方註解 | ✅ |

`Pop Up List` 兩版之 delta（PU1579／PU1578／PU1229）、`Templates` 兩版相同、
四筆所引 PU 全欄相同 —— 皆與分析層下放包前之實測逐字吻合，不再重列。

### §1 停手回報之判

**處理正確，且優於條文所求。** 兩個 26PI 本確實相異，執行層停手；
但差異拆到 zip member 層後證明**試算表酬載 63 個 member 逐位元相同**，
`customXml/` 三件為 SharePoint 文件庫 metadata，與取值無關 ——
故 §1.2／§1.3 之結論不受影響。**「停手 ＋ 把唯讀量測跑完 ＋ 回報請裁」是對的做法**，
自報 2 之疑慮不成立：停手條件禁止的是**寫入**，不是禁止繼續量。條文若要連量測都停，
會逼出「不知道差在哪就得回報」的無效往返。此形制記入下一包之通則。

### 3 項具名偏差之判

1. **`B4` 不沿用 CFTS044 之截斷字串** —— **成立且必要**。我在下放包 §2.3 同時寫了
   「`B4 = <本簿交付檔名>`」與「值取自 CFTS044 同欄逐字比照」，二者在 `B4` 上互斥；
   執行層取前者正確 —— 「文件名稱」是逐簿之欄，跨簿套用會指向錯誤文件。
   **這是下放包之內部矛盾**，見乙-1。
2. **`B8`／`D13`／`G9` 為字串而非 `datetime`** —— 成立。`xlsx_surgical._cell_xml()`
   無 `datetime` 分支，未擅自擴充工具正確。見甲-1。
3. **stage 中繼檔用畢即刪** —— 成立，`surgical_save` 之必要形制。

---

## 二、須知（甲）—— 2 項

**甲-1　交付本三格日期為文字，與模板既有列之型別不一致**
`ChangeHistory` 之 A/B/C 三列 `D` 欄為真 `datetime`（分析層複驗：
`datetime.datetime(2025,10,20)` 等，原樣保留未被破壞）；
而本包新寫之 `B8`／`D13`／`G9` 為字串 `'2026-09-08'`，Excel 中靠左顯示。
CFTS044 交付本之 `D13` 亦為 `datetime`（上游預填）。
**不阻塞交付**，但若 SWQT 對日期欄有型別要求，須先擴充 `xlsx_surgical` 之
`datetime` 分支再重落一次。**請 Pei 確認模板是否在意**；VS-CF-04 之
Popup／CFTS044 兩本同此形態，要改就一起改。

**甲-2　`forms/` 之三件不受版控**
`forms/*` 在根 `.gitignore` 第 87 行內，故新基線
`Pop Up List HMI R1 SR24 Post 2A (Dec 15, 2023).xlsx`、Change Log、
以及覆蓋後之 26PI 本**皆不入版控**。受版控者僅 `FORMS.md` 之記錄
（含三者之 sha256）。這是 `forms/` 既有政策（R-POP25），非本包引入；
但本包首次讓**生成基線**落在不受版控處 —— 日後若 `forms/` 內容漂移，
只能靠 `FORMS.md` 之 sha 對帳發現。分析層認為現況可接受（sha 已入版控），
惟記於此供 Pei 知悉。

---

## 三、分析層自報（乙）

**乙-1　下放包 §2.3 內部矛盾，逼執行層自行取捨**
同一節同時要求 `B4 = <本簿交付檔名>` 與「值取自 CFTS044 交付本之同欄，逐字比照」。
我寫該節時**未實讀 CFTS044 之 `B4` 實值**（實為截斷於 `_SWQT_` 之上游預填字串），
只憑「該簿已預填」之印象把整組 B3–B7 寫成「逐字沿用」。
`B3`／`B5`／`B6`／`B7` 恰好沿用無誤，`B4` 則是錯的。

形態與 VS-CF-03 審閱包丙-2 同族（**下放包內之值未複算即落筆**），
本輪為第四次。此形態已跨 CFTS044 與 Popup 兩條線、連續四包出現。
**建議升格為 FO 硬規**：下放包內凡指定「沿用他簿之值」者，須附該簿該格之實測值；
未附者執行層得退回。（VS-CF-03 審閱包丙-2 已提「列數／處數須附查詢式」，
本條為其擴充 —— **值**與**數**同受規制。）

**執行層之自報 1 亦為同族**（`FORMS.md` 之三個 sha 手打捏造後自查更正）。
兩層在同一輪各犯一次同型錯，可見這不是個人疏失而是**流程缺口**：
凡「可量而未量即落筆」之處，目前沒有任何自動閘擋得住。上述 FO 硬規之外，
建議 `lint_docs036.py` 增一檢：包內出現之 sha16／sha256 字串，
若對應檔案存在於 repo，重算比對不符即 FAIL。

---

## 四、狀態

- 交付本 `ab05bb7b…` 已在 `features/popup/delivered/`（受版控，`git check-ignore` rc=1 實測）。
- `output/` 副本 sha 相同；兩份 MANIFEST 為本包新建。
- 5 條 TC 覆蓋 037 V0.2 之 5 個 Functional leaf，PENDING 0、DR 引用 0。
- Core §5.1–5.4 缺口、queue 仲裁 —— 依 Pei 裁不補，續留 `COVERAGE_GAPS.md` 與 RD-1。
- `R-POP2`／`R-POP7` 未改；`R-POP27` 落於 `RULINGS.md` 第 468 行。
- DR-POP2／POP3／POP4 之送出、RD-1 上報、commit、tag、TestRail 匯入 —— 待 Pei。

**待 Pei**：甲-1 之日期型別（改則 CFTS044 與 Popup 兩本一起）；乙-1 之 FO 硬規升格。
