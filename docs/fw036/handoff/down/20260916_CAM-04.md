# 下放包 CAM-04 —— pilot v2（10 TC）：逐列修正 ＋ 寫法對齊已交付本

指派執行層 session：延用。對應審閱：`down/20260916_CAM-03_review.md`；Pei 2026-09-16「都裁，但寫法要遵照之前寫過的格式去寫」。

---

## §0　禁區

同 CAM-03 §0。本包只觸及 pilot01 之 9 列與新增之 1 列（`NR1L-RVC-010`），產物落 `generated/pilot02/`；`pilot01/` 不刪不改（留作 diff 基線）。不寫回工作簿。

## §1　裁定落檔（Pei 2026-09-16，審閱 §三六項全准）

取號現查：`{live}` 期望值 **11**。

**(a) R-CAM3 加 (e) 段**（R-TM13：原 (a)–(d) 不動，body sha 重算，fenced sha 不變）：
```text
R-CAM3(e)  訊息名因 EE 而異，而 PROXI 前提值與 ER 皆相同者，不拆列。
  Pre-Condition 專用一行、固定句式：
    `CAN source: <Atl-Hi signal> (HDCC27, DT27) / <Atl-Mi signal> (637, 2261, 376)`
  Procedure 一律以 Atl-Hi 訊號寫 §8.7.5(c) 之 `Send CAN:` 式；Atl-Mi 依該行代換。
  PROXI 值或 ER 任一相異者仍依 (a)–(d) 拆列。
```

**(b) R-CAM5 加 (e) 段**：品牌分支之 Pre-Condition 固定句式
`The vehicle brand is Ram (HDCC27, DT27, 637)` ／ `The vehicle brand is not Ram (2261 Fiat, 376 Abarth)`。

**(c) R-CAM{live}  VF 家族之 `specification_reference` 格式 = VF230 交付本前例**
```text
R-CAM11  VF551 家族之 spec_reference 寫法沿用 `features/vehicle_setting/delivered/` VF230 交付本
  （`FM-WI-FSM-036-A01 … SWQT_VF230_20260902.xlsx`）之實際格式，逐字同型；
  執行層先抄該本 M 欄 5 個樣本入上繳包，再據以改寫 Camera 之 VF 型錨。
  CFTS092 型維持 §10.7(a) `CFTS092-{ObjectID}`；HMI L&F 型維持 §10.7(b)。
```

**(d) R-CAM10 之自檢**：`features/camera/scripts/selfcheck_r_cam10.py`（讀 `layer2_assign.tsv`＋各 TC json 之 spec_reference，報「錨被 SWE ID 較小者共引」之列），每批上繳前跑，結果入上繳包；不入 lint 序列。

**(e) framework 落點**：`docs/fw036/framework.md` Part VIII（行 1466–1644）整段搬至 `features/camera/framework.md`（含簽核狀態行）；Part VIII 原處留一行指標 `→ features/camera/framework.md`。搬前後兩檔 sha 回報。

**(f) 邊界值換算**（A-CA22）准；DECISIONS 6-10 `[DECIDED]`。

## §2　寫法對齊已交付本（Pei 本次之核心要求）

canon §8.7.5 之基準語料即三本 Pei 交付本（SWC 0628／VF230 0819／Home 0809）。本包要求**每一欄之句式**對齊：

1. 執行層以 `features/vehicle_setting/scripts/style_compare_w97.py`（或等價）對 pilot v2 十列跑欄別比對，基準取：
   - `features/vehicle_setting/delivered/…VF230_20260902.xlsx`（PROXI 前提、設定 hop、Vehicle Model 欄）
   - `features/power_moding/delivered/`（CAN `Send CAN:`／`is sent` 句式、Hold 步驟）
   - `features/home/delivered/`（HMI 導航 hop、`The "X" screen is displayed` ER）
2. 逐欄回報：pilot v2 之每一種句型（Pre-Condition 首行、PROXI 行、CAN 行、hop 行、Final Step、ER 收尾語）在基準本中之最近例句（逐字＋列號），以及 pilot v2 與其之差異；**差異者改成基準句式**，無基準例者具名「新句型」列表供 Pei 裁。
3. 特別對齊項：
   - Pre-Condition 之開頭順序：PROXI 行 → 電源狀態行 → CAN source 行 → 畫面狀態行（依 VF230 本之常見順序實測後定，回報實測）。
   - ER 首句不用 `is accepted`；CAN 送出之 ER 依 §8.7.5(d) 以 HU 側可觀察狀態收尾。
   - adb 兩行式對照 SWC 本之既有 `$` 例。
   - `test_item` 括號下半之 token 形態對照 VF230 本（EE／訊號名／設定值之寫法）。

## §3　逐列修正（審閱 §二十項，逐條對應）

| TC | 修正 |
|---|---|
| 001 | verbatim 改取 V2（Atl-Hi 主線）或 CFTS092 之等價句，V33 留 reasoning；Pre-Condition 5 改 R-CAM3(e) 句式；ER1 改可觀察 |
| 002 | 同 001 之句式；`IGN_LK ↔ Ignition_Pre_Off` 引 V33 I/O 表或 LID 對應列，查無則該值 `PENDING: DR-CAM-{n}` |
| 003 | 刪步 1（RUN 已為前提），ER 重編 |
| 004 | verbatim 改取 V3 §1.10.2.2 自動模式進入之原句（回報 SYS-RA ID）；查無等價句則取 V42 並回報 |
| 005 | 刪步 1；design_method 改 `功能測試 (Functional based)`；Pre-Condition 5 改 R-CAM3(e) 句式 |
| 006 | 刪 Pre-Condition 4；reasoning 刪矛盾句；design_method 改 `狀態轉換` 或 `功能測試`，具名 |
| 007 | 步 5、6 改純動作（CAN 式，Atl-Hi 訊號＋ R-CAM3(e) 行）；ER 承載觀察；只 Final Step 帶 check |
| 008 | 同 007；Pre-Condition 3 改 R-CAM5(e) `not Ram` 句式 |
| 009 | 只承 on-point（206 → 移除）；步 4 之 205 移出；Final Step 收斂 |
| **010（新）** | off-point：205 仍顯示（同前提、同步驟至 205 止），design_method BVA，Vehicle Model 同 009 |

全部十列：spec_reference 依 R-CAM11 改型；`Vehicle Model` 七欄依 R-CAM2；lint（含 Z）全綠；`selfcheck_r_cam10.py` 零命中。

## §4　上繳要求（`up/20260916_CAM-04.md`）

1. R-CAM3(e)／R-CAM5(e)／R-CAM11 之 sha8；framework 搬移前後 sha；DECISIONS 更新。
2. VF230 本 M 欄 5 樣本逐字；十列 spec_reference 改寫前後對照。
3. §2 之欄別比對表：每種句型之基準例（逐字＋來源本＋列號）與 pilot v2 之最終句；「新句型」清單。
4. 十列 `pilot01 → pilot02` 之逐列 diff（欄級）。
5. lint 報告 ＋ selfcheck 結果。
6. 自報錯誤；未結（DR-CAM-a／f／g／h）。

## §5　升級條件

- VF230 交付本 M 欄之 VF 型錨不存在或形制不一（回報樣本，停改寫）。
- `style_compare_w97.py` 不可重用且無等價工具（改以人工抄例，回報）。
- -004 於 V3／V42 皆無「無影像 → R → 顯示」之原句。
- 「新句型」超過 3 種（停，先裁）。
