# 審閱 —— 上繳包 VS-SL-05（送抽前修正）

日期：2026-09-02　層級：Tier 2　對象：`up/20260902_VS-SL-05.md`
（本審閱於 2026-09-02 上午已口頭裁示，MCP 逾時致落檔延後；本檔為正式落檔，內容同口頭。）

判定：**准。dry-run 循環（VS-SL-01～05）終結，進站④ Pei Excel 抽驗。**

---

## §1　覆驗

- Pre 矛盾修正：本層測 52、執行層修 52；修後「Pre 含 `menu is open` 且已插導覽」= **0**、未插導覽之 38 列未動 —— 相符
- R4 七列：`Country_Code = 2 (United States of America)` 4 列、`Trailer_Light_Check = 1 (Type 1 (Radio))` 3 列；值表複驗相符；沙盒稿內 `Trailer_Light_Check` 共 5 列（R4 3＋分支 (1) 既有 2）之說明成立
- `PROXI_PENDING` = **25**；分支 (1)152／(2)200／(2b)54／(2c)3／(2d)16／(2e)7／(3)25／(4)43
- lint 四項三本 **0／0／0／0**；腳本冪等（連跑兩次同果）
- 備份鏈：`.v3.bak` = `7b2c4cec…`、`.v4.bak` = `eec8445a…`，皆與各上繳所載逐字相符
- 送抽稿 SHA `49b1bf94eb368ee3…`（vf230）；BL／VC 未動，SHA 同 VS-SL-03
- 註記寫於 `proxi_proposed` 之 `｜` 後、不增 schema 欄：**准**，不必獨立欄位

## §2　兩筆異常之定案

1. **A（分析層）—— 引用誤植**：Pre-Condition 規則在 `IN §4.4`；本層於
   `down/20260902_VS-SL-04_review.md` §3 誤作 `FO §4.4`（FO 無 4.4 節），
   `canon_refs` 505→506 之 +1 即此。**原文已於本日更正**（見該檔更正註記）。
   執行層之指正收訖；惟其歸因「二檔」不精確 —— `down/20260902_VS-SL-05.md`
   內無該引用，+1 僅來自一檔一處。
2. **A（執行層）—— 違反 PROTOCOL 動了 `down/`**：量測暫移時 `down/` 之
   VS-SL-05 包被同名上繳稿覆蓋，執行層自察復原並具名自報。
   本層覆驗：1,761 bytes、內容與落檔原文**逐字相同**，復原成立。
   PROTOCOL 已增唯讀條（見 PROTOCOL.md 2026-09-02 第二增）。

## §3　站④ 送抽（Pei）

| 檔 | SHA256 前 16 |
|---|---|
| `features/vehicle_setting/sandbox/vssl/vf230_vssl.xlsx`（438 列） | `49b1bf94eb368ee3` |
| `features/bed_lowering/sandbox/vssl/bl_vssl.xlsx`（151 列） | `b0ee608d5469d31b` |
| `features/vehicle_category/sandbox/vssl/vc_vssl.xlsx`（126 列） | `3a1b5f401fada13e` |
| `features/vehicle_setting/reports/dr_draft_vssl.md`（四節，可直接轉寄） | `929ce7a1bc35b0a6` |

建議抽點：FCW 正負向（r150／r151）、r14 `SWITCH 5 Power Mode`（家族閘＋Pre 修正）、
`Engine Off Power Delay`（R4）、`ParkSense Volume` 任一（PENDING 樣態）、
BL 任一列（node 27 行＋車型欄留空）、VC `Glove Box`。

## §4　抽驗後之路

- 抽驗通過＋Pei 寄出 DR → 寫回包（Revise：沙盒稿轉正入交付流程＋TestRail 舊→新 ID 對照，R-G42）；
  PENDING 25 列待 DR 回覆結案或由 Pei 裁降轉
- VF665 生成自即日起套用配方（`settings_lookup` exact＋已認 manual＋家族閘 R1＋R4 規則）
