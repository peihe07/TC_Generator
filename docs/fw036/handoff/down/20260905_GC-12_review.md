# GC-12 審閱（2026-09-05，主線分析層）

對象：`up/20260905_GC-12.md`（執行層 A，22:19 接手）。
結果：**收受**。任務 1 停手正確——驗收條件不可用時交卷等同以空條件宣告合格，本層認同。

## 一　裁：bare-$ 之 lint 覆蓋 → **甲**

```text
R-G70(h)（補）：v3 記法殘留（`$MESSAGE.Signal$` 包覆式，不論其後有無 `= raw (label)`）
  為 P 之 FAIL 類，非 WARN。理由：R-1 v3 已撤銷，殘留即不合規；Revise-C 必須清零，
  WARN 類無法作為出貨閘。
```
- `check_signal_line` 之賦值偵測加 `$` 包覆式樣式；命中報 P，訊息「v3 記法殘留（R-G70）」。
- 八本基線 P 重跑一次，P 升屬預期；對照表加一欄 `P_bare`，使前次表（B 所做）之「不含 bare 貢獻」變成可讀。
- 正控五樣本：三個 v3 殘留樣本 P 須各 ≥1，兩個 v4 正解樣本 P 須 0——**五個一起釘進 `tests/test_lint036.py`**，缺一不算過。
- 施作完成後任務 1 恢復：Revise-M 驗收條件改為 **`P_after == bare 數（取 dry-run 表）且 P_after 全為 bare 類`**，不再用「0 或只餘 bare」之寬句。

## 二　其餘各節
| 節 | 判 |
|---|---|
| 2 R-G72(g) 入台帳、乾跑 a6c5b0e4 | 收受；Pei 重生 |
| 3 三本 sha 三方相符 | 收受 |
| 4 DR 狀態欄 0/96；power 為表格式 | 收受。**裁**：power 線不改為節式，於其表加一欄 `status`（open/sent/closed（日期）），範本加註「表格式 DR 以欄承接」。本包只加標頭，回填仍不做 |
| 5 −101 複驗相符、儲存格層級 join 412/483/413 | 收受；「一列之單位不同」為第二層差異，補遺未見，執行層抓出正確 |
| 6 A-GC21／A-GC22 | 收受。A-GC22 之「行號位移」要點採入：**引用程式碼一律指函式名，不指行號**，加入 PROTOCOL 落檔規則一句 |
| 7 R-VS86 旗標與數字 | 收受；「作者側賦值 0／散文 2；來源側 63」定稿。其 ANOMALIES 條由 A 於 GC-13 登（B 已非執行中 session） |
| 8 ambiguous +1 為 GC-12 下放包自身裸節號 | 收受，分析層之誤（我寫「本包 5 節」）；A-GC11 已涵蓋形態，不另立號；GC-13 起下放包內部指涉用「§二-5」式 |
| 10-2 `git stash push -u`／`pop` | 收受具名。**裁**：共用工作區內禁止 stash（會移走他方未追蹤檔）；歸屬分析改用 `git worktree` 或複製到 scratchpad 後 diff。入 PROTOCOL 一句 |

## 三　GC-13 任務（**指派執行層 A**，續 GC-12 未完項）
1. §一：lint P 加 `$` 包覆式 → 五樣本測試 → 八本基線 P 重跑（附 `P_bare` 欄）→ 對照向。
2. 任務 1 恢復：一-3 `mechanical=Y` 現查 → Revise-M 三本（GC-12 §二-1 原規格）→ 驗收條件依 §一末句 → idmap 填號 → MANIFEST／RULINGS → Y–AH 斷言。
3. ANOMALIES：R-VS86 理由書條（形態＝來源側計入作者側，A-GC20 同族）；次一未佔號現查。
4. PROTOCOL 二句：引程式碼指函式名；共用工作區禁 stash。
5. `docs/fw036/templates/DATA_REQUESTS.md` 加「表格式 DR 以 `status` 欄承接」段；power 線 `DATA_REQUESTS.md` 標頭同步。
6. 上繳 `up/20260905_GC-13.md`。

## 四　Pei 動作
1. `rulings_hash.py` 重生（R-G72(g)、R-G70(h)）→ `--check` → commit（GC-10～13 一併）。
2. GC-13 後三本 Revise1 抽驗（各 3 列對 dry-run 表）→「出貨」。
3. DR 取號送出（DR-VS{n}、HMI entry path ×3）；`driver_distraction` `git mv`。

## 五　未結 DR
同上繳 §9；bare-$ 值查詢待 Revise-C。
