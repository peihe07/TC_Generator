# 審閱 GC-10 上繳包（2026-09-05，Tier 2）

對象：`up/20260905_GC-10.md`（執行層 B，含 §7 補遺）　下放包：`down/20260905_GC-10.md`
結果：**收受**。PROXI 方向已矯正為 SWC（v4.1，R-TM13 加註），候選表出爐，`lint_paths` 6→2、`gates_tsv` PASS（57）。6-4 四項裁於 §一。

## 一、6-4 四項
1. **`Y` 為獨立 profile 檢查** —— 收受。P 為 FAIL 類，WARN 混入即無法只報不改；與 X 同形態一致。R-G70 v4.1 註「PROXI 舊式由 lint Y 報 WARN」。
2. **A-GC 編號** —— 維持現狀（A-GC16／17 為 A 所落、A-GC18 覆寫事件）。二度改號比一次偏離更壞；GC-10 §二-4 之令作廢，台帳不留痕（ANOMALIES 內 B 之三行註即依據）。
3. **未做二項**（八本基線 P 對照向、pm_29 v4.1 dry-run 重跑）—— 併入 **GC-11**，與候選表圈選後之轉換準備同包。
4. **R-G62(b) 可證性** —— 收受提案為 **R-G62(b) 附記**：下放包首行載明指派之執行層 session；派工端（Pei）於指派前確認前一 session 已停；執行層之「未觀察到他方寫入」聲明附觀察依據（最後他方 mtime）即為合式，不要求證明否定。GC-11 入 PROTOCOL。

## 二、另一件自報（代執行層 A 登記）
執行層 A 於 GC-09 逾「不寫表」明文直接寫 `RULINGS.sha.tsv`（21:31:46），且以 B 版審閱之方向落 R-G70(e)。二者皆非 B 之責；登 **A-GC19**（執行層 A：R-G50(c) 違規＋引用無效審閱），GC-11 抄錄。

## 三、候選表（R-G72(f)）—— 供 Pei 圈選
五本全為候選（皆 X>0）：

| # | feature | 本 | rows | X | P | 備註 |
|---|---|---|---:|---:|---:|---|
| 1 | power | pm_29 | 389 | 237 | 40 | PM 站 ⑤ Revise2 本就要出，併入 |
| 2 | power | pm_73 | 287 | 103 | 45 | 本 session 未見其來歷，先查該線 RULINGS |
| 3 | sw_update | …20260830 | 319 | 159 | 0 | 交付紀錄 A-SU7 |
| 4 | ics_management | …20260830 | 31 | 29 | 0 | 已改過一次（A-ICS138）|
| 5 | vsm_v42 | …20260902（b1） | 17 | 6 | 0 | V42 擬 mode C 累積出貨，可等 |

Y 全 0 屬預期（VF230 152 列舊式在 `output/` 非 `delivered/`），非無舊式之證。

## 四、Pei 動作
```bash
cd /Users/peihe/Work_Projects/TC_Generator
git status --short | sort                       # 22 M ＋ untracked；貼我一份
python3 scripts/rulings_hash.py && python3 scripts/rulings_hash.py --check   # v4.1 後重生，須 PASS
python3 scripts/gate_all.py 2>&1 | tail -8      # 預期：canon_refs 158、lint_paths 2，餘 PASS
git add -A -- docs/fw036 docs/runtime docs/reports scripts tests features/vehicle_setting features/power/reports \
  features/bed_lowering/reports features/vehicle_category/reports features/sw_update/RULINGS.md
git commit -m "feat(fw036): GC-09/10 — R-G70 v4.1 (PROXI SWC), R-G72 revise policy, lint Y, rg72 candidates, FO handover items, DR status template; RULINGS.sha.tsv regenerated"
# driver_distraction（GC-06 審閱 §四-3）
bash scripts/gc06_driver_distraction_mv.sh
sed -i '' 's#workbook: "workbook/driver_distraction_00.xlsx"#workbook: "sandbox/driver_distraction_00.xlsx"#' features/driver_distraction/feature.yaml
git add -- features/driver_distraction/feature.yaml
git commit -m "chore(driver_distraction): move workbooks to sandbox/ per R-G64" -- features/driver_distraction
python3 scripts/lint_paths.py 2>&1 | tail -2    # 應 0
```
`git add -A` 之目錄含 A 之未追蹤產物（`gc08_inventory*`、`rg70_pm29_dryrun`）——後者方向已錯，**先 `git rm --cached` 不收**：`features/power/reports/rg70_pm29_dryrun_20260905.tsv`、`docs/reports/gc08_inventory_20260905.tsv`、`scripts/gc08_inventory.py`（三檔留工作區，GC-11 重跑後決定去留）。

**圈選**：回我編號（例「1 3 4」或「全」）；GC-11 據此出轉換 dry-run。
