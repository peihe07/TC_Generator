# 下放包 PU-01 —— Popup 補齊漏失需求（Pei 裁「乙」2026-09-08）

**指派：執行層。** Pei 裁定：Popup 工作簿比照 CFTS044 之出貨處置，
**範圍取乙案** = Core HMI L&F §5.1–5.4 之缺口 ＋ `Pop Up List` 之 Category／Template 規則。

⚠ **本包不得整包執行** —— §五有 3 項須 Pei 先裁。裁定前只可執行 §一之量測落檔，**不得寫工作簿**。

## 〇、既有基準（分析層獨立複算，與 `features/popup/COVERAGE_GAPS.md` 相符）

| 項 | 值 |
|---|---|
| 交付本 | `10_Reviewing/.../Pop-Up Queue and Priority Management/…_Popup_20260828.xlsx`，sha16 `a13559d51d91d369`，203,424 bytes |
| repo 內同名 | `features/popup/output/…_Popup_20260828.xlsx`（執行層先核 sha，不一致即停手） |
| 資料列 | **5**（row 10–14，`NR1L-Popup-001`～`005`），`Test Set` 皆 `Pop-up Close` |
| leaf 覆蓋 | 037 V0.2 之 5 個 `Functional Requirement`（`SWE1-POP-002-01`～`-05`）**全數已生成，無 leaf 側缺口** |
| 037 V0.2 | 7 列：`POP-001`（Heading，註 `Duplicated feature of SWE1-POP-002-02`）、`POP-002`（Heading）、`002-01`～`-05` |
| 規格側缺口 | Core §5.1／5.2／5.3／5.4 **無任何 SWE1 列** |

`COVERAGE_GAPS.md` 之對帳（7 項 = 生成 1 ＋ 缺口 2 ＋ 他項涵蓋 1 ＋ 非行為 2 ＋ 指向外部 1）
分析層複驗**成立**，本包不推翻，只在其上執行 Pei 之乙案。

**R-POP2（不自行擴充）之解除**：Pei 之乙案為 Tier 3 對該條之個案解除，
**限本 feature、限 §5.1–5.4 ＋ Pop Up List**，不及於他 feature。
執行層須落為 `R-POP27`（live 取號；本檔 tail 實測最大為 `R-POP26`）。

---

## 一、本包之新量測（執行層須逐項複驗，不符即停手）

來源：`forms/Pop Up List HMI R1 (26PI).xlsx`（`Main` 1342 資料列、`Templates` 33 列、
`Drop Down Fields`）。執行層先取 sha256 並登 `forms/FORMS.md`
（R-POP25 第 3 點所欠之補登，`grep -i "pop up" forms/FORMS.md` 命中 0）。

### 1.1 §5.3（GP1）—— 兩條獨立證據皆成立，可測

`Templates` 分頁之 `Radio Size` × `Pop Up Size`：

| Radio Size | Small | Medium | Large |
|---|---|---|---|
| `R1 8.4 10.1 12` | 1, 2, 3 | **4, 5, 6** | 7–12 |
| `R1 Low 7"` | 13, 14, 15 | **（無）** | 16, 17, 18, 19 |
| `R1 10.25''/12.3"` | 21, 22, 23 | **24, 25, 26** | 27–32 |

第二證據：`Main` 之 `Template 7"` 欄全部相異值為
`13／14／15／16／17／18／19／20／Custom／N/A`（及少數多值格）——
**Medium 之編號（4/5/6、24/25/26）一次都沒出現**。GP1 於資料側成立。

### 1.2 §5.4（GP2）—— 兩處對不上，須裁

(a) **`Pop Up List` 內查無 Android Dialog／Toast 之列。** 以 `Android|Toast|Dialog`
    不分大小寫掃 `Main` 全欄，命中 **48 列**，逐列判讀**全部為 Projection Device
    之 Android Auto 條目**（PU0250／0252／0253／0254／0329～0332／0347／0356／0357 等），
    無一為 Android 系統對話框或 Toast。
    ⇒ GP2 **無法自 `Pop Up List` 驗證**，只能在裝置上觸發後量測模板尺寸與優先級行為。

(b) **`priority of 1` 於 Category 值域中不存在。**
    `Drop Down Fields` 之 `Category` 值域逐字為 `RVC／1P／1T／2／3／X／VR／SL`；
    `Main` 實際分布為 `2`=1111、`3`=73、`---`=52、`RVC`=29、`SL`=26、`VR`=15、
    `X`=9、`1P`=6、`RVC-X`=5、`2 SL`=4、`1T`=3、`Custom`=2、`RVC\n2`=1、**`1`=1**、`-`=1、空=4。
    GP2 之 `priority 2` 對得上 Category `2`；`priority of 1` **對不上任何值**。
    ⇒ **不得自行推定 `Toast → 1T`**（Pei 2026-09-08：不自行解讀，用他原來的表示方法）。見 §五-3。

### 1.3 落檔

量測寫入 `features/popup/data/popup_list_measure.tsv`（唯讀產出，不改工作簿），
欄位至少 `item`／`source_sheet`／`source_row_or_col`／`measured_value`／`query`。
**每個數字附產生它的查詢式**（審閱 VS-CF-03 丙-2 之建議，本包起試行）。

---

## 二、新生成之 TC（乙案，待 §五 裁定後執行）

### 2.1 §5.3（GAP-POP1）—— 1 條

```
test_item 上半：GP1.) The 7" low radio will not have medium popup templates, but all other radio variants will.
下半：(Medium template absent on the 7" low radio)
```

Procedure 以**兩個 radio 變體對照**書寫（§7 正負配對）：於 7" low radio 觸發
`Pop Up List` 中該列 `Template 7"` 為 Large 之 popup，讀其呈現尺寸；
再於非 7" 變體觸發同一 PU，讀其 `Template 8.4" 10.1" 12"` 對應之 Medium 呈現。
具體 PU 編號由執行層自 `Main` 挑一列**同時**於 `Template 7"` 與
`Template 8.4" 10.1" 12"` 有值、且後者落在 4/5/6 者，**挑選條件寫入 Remarks**，不臆造 PU。
挑不到即停手回報（該情形本身即 GP1 之反證，須上報）。

### 2.2 §5.4（GAP-POP2）—— 2 條

依 §五-3 之裁定書寫。Dialog 側（`large` ＋ `priority 2`）可寫；
Toast 側之 `priority of 1` 依裁定填。二條之驗證皆在裝置側（§1.2(a)），
**不得寫成查 `Pop Up List` 之表格比對**。

### 2.3 §5.1 —— 抽樣一致性 TC（乙案之核心）

§5.1 逐字：`See HM/ Popup List for all popup text strings/timeouts/exit conditions/templates/use cases`。
此句使 `Pop Up List` 成為規範性文件 —— 即乙案所指之「Category／Template 規則」。

**1342 列不可能一列一條 TC**，故本條為抽樣一致性 TC：驗「popup 之呈現與其
`Pop Up List` 該列之 `Timeout (sec)`／`Exit Conditions`／`Template`／`Category` 一致」。
抽樣規則見 §五-2，**未裁定前不生成**。

### 2.4 §5.2 —— 不生成

`Examples of Items That Are Not A Popup`，內容只有一張圖，無可測行為。
`COVERAGE_GAPS.md` 已判其「非可測行為，不列為缺口」，本包維持。
⚠ 此與 CFTS044 之「每個需求都要有產出」不同 —— 該案之 237 leaf 皆有 037 D 欄之行為敘述，
本項則**連 leaf 都沒有且條文只有圖**。若 Pei 仍要求產出，回報後另議。

### 2.5 既有 5 列

**不動**。其 `test_item`／步驟／ER 已對 037 S 欄（Verification Criteria，本份寫有
Precondition／Action／Expected Result 三段，比 CFTS044 之 Q 欄完整）逐條對齊。
本包只加列，不改既有列。

---

## 三、工作簿改動

- 新列接於 row 15 起，`Test Group` = `Popup`（沿用），`Test Set` 見 §五-1。
- `tc_id` 續編 `NR1L-Popup-006` 起，**既有 001–005 不重賦**（無刪列、無位移）。
- `specification_reference` 依 §10.7(b)：`{檔名}_{章節號}`，檔名逐字
  `SYS1_HMI_Core_HMI_Logic_and_Flow_R1_SR24_Post_2A_(February_2_2023)`，與既有 5 列逐字一致。
- 落檔一律 `surgical_save`；產出 `features/popup/sandbox/pu01/popup_20260828_Revise1.xlsx`，
  交付本與 `output/` 既有簿不動（R-G72）。

## 四、全簿收尾（同 VS-CF-03 §六）

尾句號／行首尾空白／全形標點／方括號（UI 標籤式）／Proc↔ER 1:1／每列 ≥2 步／
Priority 值域／`design_method` 值域／R-S4 括號下半（存在、無中文、同 Req 相異）——
新列與既有 5 列一併跑，計數寫入上繳。

---

## 五、須 Pei 先裁（3 項）

**裁-1　新列之 `Requirement or Design ID`（D 欄）用什麼**
§5.1／5.3／5.4 **沒有 SWE1 編號**。既有 5 列該欄為 `SWE1-POP-002-0N`。
- **甲**：用 SYS1 之 NRL 號（`NRL-168283`／`NRL-168285`／`NRL-168286`）——
  真實存在、可追溯、不侵入上游命名空間。
- **乙**：自行編 `SWE1-POP-003`／`-004`／`-005` —— 讀起來一致，但 037 V0.3 若配到同號即撞號，
  且 SWE1 是上游 SWE.1 之命名空間。

分析層建議**甲**，Remarks 具名「037 V0.2 無對應 SWE1 列，錨用 SYS1 NRL 號」。
`Test Set` 一併裁：新列建議 `General Behavior`（既有為 `Pop-up Close`）。

**裁-2　§5.1 抽樣一致性 TC 之抽樣規則**
- **甲**：每個相異 `Category` 值各取 1 列（`2`／`3`／`RVC`／`SL`／`VR`／`X`／`1P`／`1T` 共 8 條）
- **乙**：每個 radio 變體 × 每個 Pop Up Size 各取 1 列（3 × 3 − 1（7" 無 Medium）= 8 條）
- **丙**：甲 ＋ 乙（16 條）
- **丁**：只出 1 條通則型 TC，抽樣列表放 Remarks

分析層建議**乙** —— 它同時驗到 §5.3 之模板規則，與 §5.1 之 `templates` 字面最貼。
甲之 Category 驗的是優先級，但**無任何條文說 Category 值該導致什麼可觀察行為**
（queue 仲裁無需求，見 §六），驗了只能比對表格，接近 loop verification。

**裁-3　§5.4 之 `priority of 1` 如何落字**
見 §1.2(b)：值域無 `1`。
- **甲**：Toast 側 ER 只寫可觀察者（`small template is used`），優先級不寫，Remarks 具名值域不符
- **乙**：照條文逐字寫 `priority of 1`，Remarks 具名 `Pop Up List` 值域為 `1P`／`1T`，執行時以實機為準
- **丙**：認定 `1T`（Transient）為 Toast 之對應 —— **分析層不建議**，此為解讀

分析層建議**乙**（照抄條文＋具名落差），次之甲。

---

## 六、本包不處理，但須 Pei 知悉

**工單名稱之 "Queue" 在整份 Core HMI L&F 匯出內命中 0 次**
（`COVERAGE_GAPS.md` 已載，分析層複驗成立：167 列 `Description` 欄 `re.I` 掃 `queue` = 0）。
即 **Pop-Up Queue Management 這半個 feature 沒有任何上游需求**。
乙案補得了 Priority 之條文側（§5.4）與資料側（Category／Template），
**補不了 queue 仲裁**（兩個 popup 同時觸發時誰勝出）。

其唯一具名來源 `HM/ Popup List Priority Matrix`（§5.1 所指）於 repo 為
`forms/Pop Up List Priority Matrix HMI R1 SR24 1A (May 3 2021).pdf`，**無文字層**
（`COVERAGE_GAPS.md` 載三工具實測 0 非空白字元；本包對同資料夾之
`Core HMI Logic and Flow …pdf` 亦實測 21 頁全空，形態相同）。

三條路，皆非本包可決：
1. 對該 PDF 做 OCR（品質不保證，OCR 出來的規格值不宜直接當 TC 來源）
2. 向上游請 037 V0.3 補 queue／priority 之 SWE1 列（RD-1 具名上報，`COVERAGE_GAPS.md` 已備候選）
3. 交付時具名該範圍不涵蓋，工單名稱與交付內容之落差寫入 `DELIVERY_NOTE.md`

分析層建議 **2 ＋ 3 並行**：先具名交付，同時上報。

## 七、上繳 `up/20260908_PU-01.md` 須含

1. 交付本與 repo `output/` 同名簿之 sha 比對；Revise1 之 sha256。
2. §一 各項量測之複驗結果，**每個數字附查詢式**。
3. 新列逐列全文；`Pop Up List` 之 sha256 與 `forms/FORMS.md` 補登行號。
4. §四 全簿計數；`R-POP27` 落檔行號（live 取號查證）。
5. 工具旗標；自報。
