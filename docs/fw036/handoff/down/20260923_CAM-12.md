# 下放包 CAM-12 —— CAM-11 審閱落檔 ＋ batch03b 修正 ＋ batch03c（`-004`／`-006`／`-019`，36）

指派執行層 session：延用。對應審閱：`down/20260923_CAM-11_review.md`（同發）。

---

## §0　禁區

同 CAM-03 §0。本包觸及：profile、DR、`batch03b/`（`-152`／`-153`／`-164`／`-170`／`-175`／`-176`／`-178`）、`batch03c/` 新建、`coverage.tsv`。不寫回工作簿。

## §1　落檔

1. profile §7.5：PENDING ＋ `P` 調和形制；§7.2 加第一步「以來源措辭逐字掃 PROXI 表 / 以訊號名（非訊息名）掃 DBC」。
2. DECISIONS 6-40～6-42：8b 擴用准；`-170` 依逐字；`Steering_Ratio…` 處置。
3. `-164` 同義字重掃；查無則 DR-CAM-n 範圍擴一項。

## §2　batch03b 修正

- `-152`／`-153`／`-175`／`-176`／`-178`：以訊號名重掃四本 DBC，回報每列之掃描字串與命中；查得者改寫，查無者維持 PENDING。
- `-170`：procedure 改停送 `STATUS_CCAN3`；reasoning 與 Remarks 依審閱 §二-2；json 若無 `remarks` 欄則新增（工作簿 Remarks 欄之來源）。
- 九目錄 lint ＋ selfcheck 全綠。

## §3　batch03c 生成：`SWE-CAM-004`（17）＋ `-006`（17）＋ `-019`（2）＝ 36，ID 續 `-179`

依 `batch03_plan.tsv`。要點：
- **Diagnostics**（`-004`）：DTC 之讀取以散文（profile §7.3），不造命令；`systemStatus.*` 之 set／clear 各一列；`V42-590` 同句（RDF-10）記 covered_by，不造清除側；Out of Scope 之 RVCM 端 acquire 不作錨。
- **Video Pipeline**（`-006`）：EVS HAL 之串流建立／解析度／縮放（`V42-210` 只驗依 `Radio_Display_Type` 縮放）；`V42-263`（73t）／`-226`／`-219`／`-592`／`V3-533` 摘句；觀察以 `dumpsys media.camera` 沿既有用例，新增列入 `bench_verify.md`。
- **`-019`**（FMVSS 串流終止）：來源之時限值有則寫，無則不造；ER 以影像消失為可判面。
- Test Set：`-004`／`-013` → `Diagnostics`；`-006`／`-019` → `Video Pipeline`；跨節條文依 R-CAM13(c)。
- 全覆蓋查表對映；R-CAM3(f)／R-CAM15 適用。

## §4　上繳要求（`up/20260923_CAM-12.md`）

1. 落檔清單；batch03b 重掃結果表（列／字串／命中／改寫）。
2. batch03c 36 列產物、lint、selfcheck、PENDING、摘句表；`bench_verify.md` 更新。
3. `coverage.tsv` 現況（PRODUCED 列數／TC 數）。
4. 自報錯誤；未結。

## §5　升級條件

- batch03c > 40。
- `-004` 任一 DTC 之名稱／值於 SYS2 只見於 037（PENDING，回報數）。
- 摘句不可行且不可拆。
