# 下放包 CAM-09 審閱（分析層 2026-09-23）

對應上繳包：`docs/fw036/handoff/up/20260923_CAM-09.md`
判定：**落檔與五目錄修正通過；batch02c 23 列判過（第 4 批）**；抽讀 4 列（099／106／108／112）無寫法偏差；batch03 計畫與切法准。CAM-10 同發。

---

## §一　裁決（上繳待裁八項）

| # | 項 | 裁決 |
|---|---|---|
| 1 | selfcheck 第 3 項「雙向」解讀（3a 重複／3b Standby 首行無點火步） | **准執行層之實作**；審閱原文之「矛盾」即 3b 之意，述詞各自獨立為正確 |
| 2 | `TIME_W_RVC` 2,5 s／`TIME_W_RVC2` 50 ms | **補入 profile §9** |
| 3 | `-007`／`-013` in-scope = 0 之覆蓋台帳形制 | **立 R-CAM16**：SWE 列所有來源依 R-CAM10 委派他列者，`coverage.tsv` 記一行 `No TC — all sources delegated to SWE-CAM-nnn (R-CAM10)`，`tc_count = 0`，不視為缺口；與 Heading 列（R-BLM2／R-POP5 前例）並列為兩種零 TC 形制 |
| 4 | 難點 A（LVDS 訊息週期） | **開 DR-CAM-p**（LVDS 訊息週期）；`V2-479`／`-480` 之時序面 PENDING，值切換之結果照寫 |
| 5 | 難點 B（DTC 讀取無句式） | Diagnostics 線之交付本不在本 repo（另線）；**以散文書寫** `Read the DTC list with the diagnostic tool and check that …`，不造命令；Pei 若供 Diagnostics 本則改抄（記 DECISIONS） |
| 6 | 難點 C（`V3-281`／`-283` 標的誤植） | **RDF-08／09**；生成時以 `a)` 子句之正確標的為 ER，verbatim 逐字不改 |
| 7 | 難點 D（`V42-590` 與 `-584` 同句，Communications_Timeout 缺清除側） | **RDF-10**；plan 記 covered_by，不造清除側 |
| 8 | 難點 E（`V42-210` 轉指 PDO） | 准：ER 只驗「依 `Radio_Display_Type` 縮放」，具體解析度不比對（DR-CAM-m 範圍） |
| 9 | `-099`／`-100` 章節落後一組之判讀 | **准**，兩列維持（Audio Mode ON／OFF 為不同前提） |
| 10 | `-082`／`-087`／`-111`～`-116` V4 只勾 HDCC27 | 確認（R-CAM11 平台表） |
| 11 | batch03 切法 03a 23／03b 29／03c 36 | **准** |

## §二　抽讀備註（不改）

- `-108` 之 50 ms 觀察窗於人工不可達，實測須以 log／bus 時戳判 —— reasoning 加一句「判定以 bus analyzer 時戳為準」；交付前歸入 Pei 實機驗證清單。
- `-106` design_method 決策表 vs 功能測試皆可，不改。

## §三　備查

- DR 十一條未結（新 o、p），f／i 交付前必結。
- R-G14 第 4 批乾淨。
