# 下放包 CAM-25 —— B 本形制補正（＋4）＋ B 本 dry-run v2 ＋ `DELIVERY_STATUS.md` 終態（生成階段最後一包）

指派執行層 session：延用。對應審閱：`down/20260923_CAM-24_review.md`（同發）。

---

## §0　禁區

同 CAM-14 §0。不寫 `delivered/`、不 tag。A 本不動（dryrun3 為候選）。

## §1　落檔

1. DECISIONS 6-82～6-85：boundary／多觸發一律一點一列（canon §8.3／§5.7 勝於 B 本前例）；`-036`／`-111`／`-142` 補正；`batch05_plan` 欄位不另立；`-245` Remarks。
2. ANOMALIES：A-CA37「B 本三列一列多點經批次審閱未抓」（分析層責任，記事實）。

## §2　B 本補正（`b07/`，ID `NR1L-RVCHMI-207`～`-210`）

| 原列 | 改動 | 新列 |
|---|---|---|
| `-036`（§7.5.3，205／206） | 原列只留 206（on-point，退出）；205 移出 | `-207`：205 仍顯示（off-point） |
| `-111`（§28.7.1.1，pinch out／in） | 原列只留 pinch out | `-208`：pinch in |
| `-142`（§30.1.3，OK／`X`／5 s） | 原列只留 OK | `-209`：`X` 清除；`-210`：5 s 自動清除（4／6 s 邊界依 canon 各一？—— **不**：5 s 為 timeout 非門檻，一列驗「5 s 後清除」即可） |

原三列**原地修改**（Procedure／ER 收斂為單點，reasoning 註「CAM-24 審閱 §一-1 拆出 `-20n`」）；此為既有列之**唯一**改動，逐列 diff 上繳。`coverage_b.tsv` 母 leaf `tc_ids` 追加；lint ＋ selfcheck 全綠。

## §3　B 本 dry-run v2

216 列（210 ＋ 6 佔位）；檔名 `…_B_<date>_dryrun2.xlsx`；lint 全表、對帳、`pending_by_dr_b.tsv` 重生；sha 回報。

## §4　`DELIVERY_STATUS.md` 終態

更新為：A 249（245 TC）／B 216（210 TC），全案 **465 列 455 TC**；PENDING ↔ DR 表重算；bench 列數；RDF／GCB；Phase 7 前置五項。加一節「生成階段結束 2026-09-23；後續變更只因 DR 結案／降 NA／Pei 實機回饋」。

## §5　上繳要求（`up/20260923_CAM-25.md`）

1. 落檔；三列原地 diff；`b07/` 產物、lint、selfcheck。
2. B dry-run v2 路徑／sha、lint、對帳、`pending_by_dr_b.tsv`。
3. `DELIVERY_STATUS.md` 終態全文。
4. R-CAM17 證據表；自報；未結（應只餘 Pei／RD／GC 三類）。

## §6　升級條件

- 三列原地修改後 lint 或 selfcheck 出現 ERROR。
- B dry-run v2 對帳不合。
