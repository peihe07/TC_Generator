# 下放包 CAM-08 審閱（分析層 2026-09-23）

對應上繳包：`docs/fw036/handoff/up/20260923_CAM-08.md`
判定：**落檔全過；全批句式修正 21 列通過（`-008` 之實質缺陷由 selfcheck 第 5 項攔下，記入 R-G14 之「工具先於抽讀」）；batch02b 22 列判過，計第 3 批 → R-G14 三批乾淨，自動放行生效**（後續批次仍逐批審閱，惟不再以 pilot 級逐列讀）。CAM-09 同發。

---

## §一　裁決

| # | 項 | 裁決 |
|---|---|---|
| 1 | 升級條件 1「列 vs 筆」 | 以**列**計，未觸發；條文自本包起明寫「相異 TC 列」 |
| 2 | `Body_Types` raw 由 label 推得（`-073`／`-074`） | **不寫 raw**：`PROXI Body_Types = Type 4 - DJ`（§8.7.5(e) 值取來源 label），reasoning 註 PROXI 表無列舉；開 **DR-CAM-n**（Atl-Hi PROXI `Body_Types` byte 231 之列舉） |
| 3 | `-091` 可判性弱 | 接受現狀（來源即如此），reasoning 已具名 |
| 4 | `-088` 是否拆 14 列 | **不拆**，清單完整性為一驗證點，各值語意已委派 |
| 5 | `-082` ER 兩項 | 接受（§8.4.1） |
| 6 | `P` 初稿 5 筆之查證順序 | 記入 profile §7「先以訊號名查 `VAL_`」 |

## §二　抽讀（073／085／088／092）之偏差

| # | 型 | 實例 | 修正 |
|---|---|---|---|
| 1 | 點火動作未用 CAN 式 | `-073`／`-085` 步 1 `Cycle the ignition to RUN so that the HU reads the PROXI configuration` | 改 `Send CAN: <平台 CmdIgnSts> = 4 (RUN)`（單 EE 直寫，跨 EE 依 R-CAM3(e)）；purpose 子句移 ER：`The HU completes start-up and reads the PROXI configuration` 保留為 ER1。selfcheck 第 7 項：Procedure 命中 `Cycle the ignition`／`Turn the ignition` → 違規 |
| 2 | 開機類 TC 首行仍 Full-Operation | `-073`（步 1 為點火） | 改 `The HU is in Standby state`（profile §4）；全五目錄以「步 1 為 CmdIgnSts = RUN 且首行 Full-Operation」掃描（selfcheck 第 3 項之反向：第 3 項抓重複，本項抓矛盾）—— 併入第 3 項為雙向 |
| 3 | PROXI 巢狀括號 | `-073` `130 (HDCC (82 Hex))` | 依 PROXI 表 label 逐字為 `HDCC (82 Hex)` 者維持；不改（逐字優先） |

## §三　備查

- `-092` PENDING 置於 Pre-Condition 正確（§8.4.3）。
- `-058`／`-092` 之 `dumpsys` 實機驗證項不變。
- DR 九條未結（新 m、n），f／i 交付前必結。
