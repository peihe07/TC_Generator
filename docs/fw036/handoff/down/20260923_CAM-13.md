# 下放包 CAM-13 —— CAM-12 審閱落檔 ＋ A 本尾批 `HMI Overlays`（`-021`／`-022`／`-023`／`-025`）＋ B 本選列計畫

指派執行層 session：延用。對應審閱：`down/20260923_CAM-12_review.md`（同發）。

---

## §0　禁區

同 CAM-03 §0。本包觸及：條文／profile／DR、`batch04/` 新建、`coverage.tsv`、`features/camera/data/b_plan.tsv` 新建。不寫回工作簿；B 本不生成。

## §1　落檔

取號現查：`{live}` 期望值 **17**。

1. **R-CAM17**（量測證據）：上繳包中「查無／零命中／全等／逐字相符」之主張須附掃描字串、母體檔名、命令、命中數；無證據視同未量測。GCB-08 提請入全域。
2. DR-CAM-f 描述擴及 HDCC27 FD-CAN8 DBC；`-152`／`-153` PENDING 指向更新。
3. profile §9 加 `RESPONSE_TIME`／`Time_POWER`／`Tpower`(V42)；§10 `remarks` 用途三類。
4. DECISIONS 6-43～6-46：`-024` 台帳形制；V3 四步不拆；remarks 欄；DR-q 接受。

## §2　batch04 生成：`HMI Overlays`，上限 40

**步驟 A — 選列計畫** `batch04_plan.tsv`（同格式）。分析層預期：
- `-021`：localized overlay 文字（`LanguageSelection` → HMI 語言設定為觸發，A-CA17；§5.8(e) hop 自 HMI Settings List `Language`）；overlay 元素之顯示條件（V2 §1.13.2.1.4～8）。
- `-022`：guideline 幾何／顏色／`Active Guidelines`／`Fixed Guidelines` 設定之顯示（hop 依 `ENTER_CAMERA_SETTINGS` ＋ `Set "…" = "On"`）；靜態圖面之比對以 HMI L&F 圖為據，圖不在素材則 ER 只判「顯示／不顯示」。
- `-023`：`STATUS_LIN.DynamicGridSts`（A-CA16：以 guideline 顯示為 ER）；與 batch01/03 重疊者依 R-CAM10 委派；六組交集（A-CA07）依先到者。
- `-025`：fault banner（`Camera Not in position` 等）—— 畫面文字 PENDING DR-CAM-h；Bed Extender 側 PENDING DR-CAM-f（`BED_EXTENDER` DBC）；banner 出現／消失之條件照寫。

**步驟 B — 生成** ID 續 `-215`；全套規則；lint ＋ selfcheck；`coverage.tsv` 更新至 PRODUCED 22。
若計畫 > 40，停於步驟 A 回報切法。

## §3　B 本選列計畫（只計畫不生成）

`features/camera/data/b_plan.tsv`：B 本 230 列（`SWE1-RVC-001…159` 含子項）依 framework B 本 8 組，每列 → SYS1 章節（cache 本）→ 驗證點 → 軸 → 預估 TC 數；umbrella 27 列標 `UMBRELLA — covered by children`。
分析層預期難點回報：
- hop label 全數逐字自 SYS1 `Basic Report`（`Outline Number` ＋ Description）；查無者 PENDING DR（HMI entry path 型，DR-CAM-g 已開，同型併入）。
- 訊號／PROXI 前提沿 A 本已定寫法（R-CAM1(b)）；`crossref_a_b.tsv` 之 `[VERIFIED]` 53 筆為互參入口。
- AUX 相關（HU 27～34 章，約 150 列）之 PROXI 配備（`Auxiliary_Trailer_Camera`／`Digital_CHMSL_Camera_Prsnt`）與 VF617 缺件（DR-CAM-a）之影響範圍。
- 依 R-CAM11，B 本 spec_reference 為 `{檔名}_{章節號}` 型（§10.7(b)），兩份 SYS1 檔名之底線 token 化回報。
估算總 TC 數與批次切法（每批 ≤ 40）。

## §4　上繳要求（`up/20260923_CAM-13.md`）

1. R-CAM17 sha8；DR-f／profile／DECISIONS。
2. `batch04_plan.tsv` ＋ 產物、lint、selfcheck、PENDING；`coverage.tsv` 現況。
3. `b_plan.tsv` 全表摘要（每組列數、預估 TC、PENDING 預估）＋ 難點 ＋ 切法。
4. 依 R-CAM17，本包所有「查無」主張附證據。
5. 自報錯誤；未結。

## §5　升級條件

- batch04 > 40。
- `-022` 之幾何／顏色值於 SYS2 只見於圖（無文字值）→ PENDING 回報數。
- B 本任一組之 hop label 查無率 > 30%（先回報，不生成）。
