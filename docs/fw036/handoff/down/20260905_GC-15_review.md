# GC-15 審閱（2026-09-05，主線分析層）

對象：`up/20260905_GC-15.md`（執行層 A，commit `9729ba1`）。結果：**收受，GC 線本輪結案**。

## 一　裁
| 項 | 判 |
|---|---|
| 1-2 MANIFEST `status` 為加欄、歷史列留空 | 收受。**裁**：值域採 `shipped`／`superseded`／`pending_ship`／`withdrawn`；歷史列由執行層依 `delivered_round` 與該線 RULINGS 出貨紀錄**逐列提案**（表列 `filename / 提案值 / 依據`），Pei 逐列准後才填；未准前留空，且 `lint_delivery_spec` 不得以該欄機讀。入下一個 GC 小包，非本輪。 |
| 2 R-G72 指紋 diff 14 行限二塊、A-GC27 | 收受；歸因完整。 |
| 3-1 結案註不落 `down/`、不落 FO 條文節 body，改落該線 RULINGS 新節 | **收受，且此為正確之處置**——分析層下放包指錯落點（要求改唯讀審閱檔），分析層之誤。**裁（入 PROTOCOL 一句）**：紀錄類文字不得寫入 `rulings_hash` 掃描面之條文節 body；落各線 RULINGS 之非條文節（節首標「非條文，不立條號」）或 handoff 上繳包。 |
| 4-1 分類覆蓋 4／6，二種既有 detail 維持 `unclassified` | 收受；「落 unclassified 即擋出貨」為該值之本意，維持。GC-14 上繳 7-2 之訂正採入。 |
| 5 不需重生指紋 | 收受。 |

## 二　本輪（GC-07～GC-15）結案狀態
- canon：R-G70 v4.1 (a–i)、R-G71、R-G72 (a–h) 全數落檔，`rulings_hash --check` OK（768）。
- lint：X／Y／P(v4 含 v3 殘留、PROXI 形態)；`rg72_revise_m.py`、`rg72_residual.py` 含測試。
- 交付：pm_73／ics_management／vsm_v42 Revise1 已出貨（`c36635f`／`9729ba1`）。
- 積欠：driver_distraction 結案（`a93ff89`）。
- ANOMALIES：A-GC15～A-GC27。

## 三　待 Pei（不變）
1. DR 取號送出：DR-VS{n}（Power_Tailgate_Enable）、HMI entry path ×3。
2. 關掉 A 線分析 session。

## 四　下一階段
1. Revise-C 首包前置：分析層出 pm_73（209 bare＋26 字串值）／sw_update（122 X-nav＋3 bare）每 TC 來源錨表，Pei 過目後下放。
2. GC 小包（待排）：MANIFEST `status` 歷史列提案；PROTOCOL 加 §一-3 一句。
3. PM 站 ③④⑤ 未動；pm_29 之 207 bare 併入 ③。

執行層待命。
