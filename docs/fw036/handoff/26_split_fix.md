# 25 包覆核 + 下放包 26：拆分缺陷修正

## 覆核判定

規則層**通過**：插列工具（`surgical_insert_rows` 含樣式承接與列位
參照搬移、9 項測試）、ID 重排、存根列處置、括號消歧、lint 全零、
逾越三項之報告——全部正確。執行前就演算法矛盾向 Pei 提問而非
自行擇一，是本包最關鍵的正確動作。

**但 §九-1 屬實質缺陷，PM 不得進入內容三項，先修。**

## 分析層錯誤清單（本包新增三筆）

1. **30 列面向數表與規則 2 不可調和** —— 表按 ER 行數編,規則按
   setup 後步數編,兩者本質不同,分析層未自檢即並列下放
2. **表合計寫 186 實為 184** —— 加總錯誤
3. **基底列數:283 為「有 TC ID 之列數」,資料列實為 284**
   （row 230 存根列）—— 沿用多包未察

規則 1 末步邊界缺陷（4 列 setup 吞全部）之修補正確,追認。

## 一、§九-1 裁決:17 列無觀察步 —— 併入其後觀察步

執行層診斷正確:規則 2 不分驅動步與觀察步,中段驅動步自成一列時
該列驗不到任何東西,且 lint 抓不到（E=0 照樣成立）。

**規則 2 修正（v2）**:
```
setup 段之後,面向之切分以「觀察步」為錨:每一觀察步
（Read／Check that 起首）為一面向之終點;其前之連續驅動步
（Send／Select／Set／Wait／Let／Keep／Run／Place／End 起首）
併入該面向,不自成一列。
面向數 = setup 段後之觀察步數。
```

17 列縮併:各該驅動步併入其後之觀察步列（PROC 成為
setup + 驅動步 + 觀察步）,原自成之列刪除。
全本 407 → **390 列**,ID 重排隨動（001–389 + 存根列）。

⚠ 此為 Pei 前裁「以規則 2 為準」之修正,**須 Pei 追認**
（修正使 17 列從「無效 TC」變為「有效步驟序列」,
方向與 §四「每列可驗」一致）。

## 二、§四-2 裁決:179b／180b PRE 互斥 —— 分析層之錯,修正

下放包 §一 令 b 列增 `An incoming phone call is active on the HU`
而未令移除 `The HU is in IDLE mode`,兩行互斥。修正（逐字）:

179b／180b 之 PRE 第 1 行改:
```
1. The HU is in FULL OPERATION mode due to an active incoming phone call
```
並刪去所增之通話行（狀態行已含通話前提）。其餘行不動。
（179 與 180 之顯示前提行——phone main screen／projection call UI——
維持原列）

## 三、§九-2／3／4 處置

- **§九-2（144 列語意複核）**:分析層職責,排於內容三項完成後之
  全本覆核一併執行（屆時 390 列人讀一輪,含面向獨立性）
- **§九-3（Excel 實開）**:屬 Pei,建議於寫回授權前實開抽驗
  插入列版面（列高 112／126／168 混用區段）
- **§九-4（TestRail 映射）**:ID 重排為 Pei 政策 (b) 之已知代價。
  **[待 Pei 裁]**:是否需要「舊 ID → 新 ID」對照表交付?
  若需,執行層自 §六 對照表延伸產出全表即可,一包可完。

## 四、本包任務（執行層）

基底 `sandbox/b25/pm_25.xlsx`。
1. §一:17 列縮併（清單即上繳 §九-1 之表）,ID 重排 001–389
2. §二:179b／180b（新 rows 271–274 中之 b 列）PRE 修正
3. 驗收:全本 390 資料列;無「setup+驅動步」而無觀察步之列
   （以修正後規則 2 v2 全表重掃 = 0）;E=0;lint A–N 全零;
   括號消歧殘餘碰撞 0;x14 讀回;止於工作副本
4. 上繳 `docs/fw036/upstream/26_split_fix.md`,附該驗未驗獨立判斷

## 五、後續

26 包通過 → 內容三項包（TLM→HU、行為化、Front_Panel）→
全本人讀覆核（§九-2）→ Pei 實開抽驗 + 授權 → 寫回 `(Revise2)`。
