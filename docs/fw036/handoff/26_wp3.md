# 26 — W-P3 下放包（prompt 指紋 + 自查表對映 + 三項裁定落地；改善案末包）

> **[SUPERSEDED by 26_wp3_closeout.md — 2026-08-24]**
> `docs/fw036/handoff/` 下同時存在三份 26 包（本檔 15:12、`26_wp3_final.md` 15:10、
> `26_wp3_closeout.md` 15:16），各指定不同上繳檔名，且**同一新條號 `R-G23`
> 被指派給兩件不同的事**（本檔指「上繳前必跑閘」，closeout 指「單一下放包來源」）。
> Pei 裁定以 `26_wp3_closeout.md` 為準。**內文一字未改**；依 R-G23 標記不刪。

日期：2026-08-24
所據上繳：`docs/fw036/upstream/25_wp2.md`（覆核 PASS，判定記錄見 §B）
上繳：`docs/fw036/upstream/26_wp3.md`
**裁定記錄**：Pei 2026-08-24 chat「1准 2准 3准」——
25 覆核所提三項建議全數照准，條文見 §C。
另三項分析層定案已記於同輪 chat：裁定 D 落點依 25 包為正（R-VS82）；
IN §8.7.5 範圍註記經 commit 視為裁定通過；selection 欄自下一批起記算式
（[DEFAULT]）。

**單一發包聲明**：改善案（23~26）自本包起僅由本對話發包；
其他 session 不寫 `docs/fw036/handoff/`（R-VF66 工單鎖之精神，
23b_wp2_supplement 並行分歧之防再犯）。

---

## A. 禁區

- 全部 git 操作屬 Pei，**含唯讀**（R-G5@`9814d24c`；沿現行慣例）——
  查證需 git log／git show 時，把命令原文列於上繳，由 Pei 執行回貼
- 歷史 handoff / upstream 檔不改寫（R-G18@`8f61f9fd`）；
  **歷史檔中之 `R-VF83@9d5bfa4d` 引用一律不動** —— 但書使 sha 變，
  新 sha 自新引用起用，舊引用是當時實讀之如實記錄
- 已交付件與 done region 不動；`generated/*.json` 之 seq **不重排**（裁定 1）
- xlsx 不碰；`features/user_profiles/`、`_intake` 不碰
- 判不明者升級不猜

## B. 25 之覆核判定（記錄）

PASS。三項不符各自追因至基準、未互相調和；兩處工具自身缺陷
（qualified 行級判準、`source_ruling` 之 `\b`）自攔自修並以字面釘測試；
19 處非 canon 引用未被機械改寫。§十二-2 commit 由 Pei 執行。

## C. 三項裁定條文（Pei 2026-08-24「1准 2准 3准」；可直接貼入）

### 裁定 1 — R-VF83 但書 + 十號缺口具名

R-VF83 條文末加但書（落 `features/vehicle_setting/RULINGS.md`，
依 R-G22 換發新 sha）：

```
但書（Pei 裁定 2026-08-24，26 包）：重生作廢之產出所佔序號成為
具名缺號，不補、不重編（A-VS02 先例）；缺號逐一登記於 ANOMALIES.md。
「連續遞增」之義務範圍為現行有效產出之外的序號不得被重複使用。
```

ANOMALIES.md 新開一條（編號依現行序實測取號）：

```
A-VF{next}：seq 248–257 十號無任何現行產出佔用（25 包 §五-3 首揭；
每批自身連續故逐批檢查必綠，僅跨批聯集可見）。處置：依 R-VF83 但書
具名為缺號。成因查證見 26 包 §D-4；查證結論回填本條，三分
（證實／否證／不可決）皆為合法終態。
```

### 裁定 2 — R-G23（上繳前必跑閘；併入 FO §9.2）

```
R-G23：以下四閘為上繳包提交前必跑項，任一 FAIL（exit ≠ 0）則不得上繳，
除非上繳包內附該 FAIL 之升級說明：
lint_docs036.py --gate（feature close-out 之上繳前）、
canon_refs.py --waiver --gate（同前，R-G18）、
rulings_hash.py --check（任何條文變動之上繳前）、
gates_tsv.py --check（任何閘變動之上繳前）。
expected_numbers.py --gate 暫不列入；其加入於 seq 缺口結案且
基準項（pool 算式）落檔後由 Pei 另裁。
```

### 裁定 3 — 報告檔名 v2（僅及新產報告；工具局部規則，不立全域條文）

```
lint036 報告檔名自本包起採 {tag}_{來源檔 sha8}_{今日}；
既有 16 份報告檔案一律不重命名（歷史不追改）。
回歸向改寫為「既有報告檔案不被重命名」，與 G-N 之字面案例並存。
```

## D. 作業清單

1. **裁定落地**：R-G23 併入 FO §9.2（先驗全 repo 無 `R-G23` 既用）；
   R-VF83 但書 + A-VF{next}（取號先驗無碰撞）；報告檔名 v2 + 回歸改寫；
   `rulings_hash.py` 重產 tsv，列出 sha 變動清單（至少 R-VF83）
2. **R-G19 落地**：`make_batch_context.py` 輸出 prompt 模板 sha256 與
   exemplar 集 sha256 入批次 manifest；以 vehicle_setting 現行批次資料
   實跑一次為證；manifest 格式向後相容（舊 manifest 無此欄不報錯）
3. **R-G21 落地**：IN §9 自查 17 項逐項對映 —— 有對應閘者記 gate_id
   （入 GATES.tsv 之 `selfcheck_item` 欄或對映附表），無者標「人工項」；
   對映之驗證：對 Home A-H10 引號規則確認其對映之閘會轉紅（G-K），
   並附一項「不該紅者不紅」（R-G9）
4. **seq 成因查證**（裁定 1）：先以檔案系統查 —— `output/`、`archive/`、
   `features/vehicle_setting/` 全樹、`generated/` 歷史檔名樣式；
   需 git log／show 者列命令原文於上繳由 Pei 跑。結論三分回填 A-VF{next}
5. **GATES.tsv 補全**：`lint_docs036.py` 之檢查項與各 feature 現行 lint
   （`features/*/scripts/lint_tcs.py` 等）入簿；`effective_date` 以檔案系統
   可得之來源回填（上繳包日期、報告檔 mtime），不可得者維持「未載明」，
   不推測（G-D）
6. **回查**（25 §十三-6）：檢索 23a／24／25 三包（唯讀，歷史檔不改）——
   §十一-1 新入 tsv 之 15 錨點是否曾被 `R-XX@` 形式引用而當時無 sha 可比；
   有者列出，僅記錄不處置
7. 全套 pytest；上繳

## E. 預期數字

| # | 指標 | 預期 |
|---|---|---|
| 1 | FO `^#### R-G23 ` 錨點數 | 0 → **1**；canon R-G 最大號 22 → **23** |
| 2 | `rulings_hash.py --check` | exit 0；錨點 188 → **189**（+R-G23；R-VF83 sha 變、數不變）|
| 3 | sha 變動清單 | ≥1（R-VF83），逐條列 |
| 4 | `canon_refs.py --waiver --gate` | **0**（維持）；本包新寫引用一律帶前綴 |
| 5 | `gates_tsv.py --check` | exit 0；閘數 20 → 實測回報（新入簿數為首量基線）|
| 6 | 自查 17 項對映 | 17 項全數分類（gate_id 或人工項），0 項懸置 |
| 7 | A-VF{next} 取號 | 無碰撞；seq 查證結論三分之一 |
| 8 | 全套 pytest 失敗數 | **8**（既有，不得增減）|
| 9 | 報告檔名 v2 | 18 組同類碰撞 → 新產報告 **0** 組；既有 16 份檔名 byte 不變 |

## F. 升級條件

1. `R-G23`／`A-VF{next}` 取號碰撞
2. 自查 17 項中有無法分類者（既非可對映亦非純人工）
3. seq 查證發現第四種形態（非重生佔用、非預留、非偏移）
4. 回查（§D-6）發現曾有引用因無 sha 而實質讀錯條文（= 既有記錄
   可能須更正，Tier 2）
5. GATES.tsv 補全時發現兩簿之閘定義互相矛盾

## G. 上繳要求

23 包 §G 全項 + R-G13 引用表（既有引用 sha8：R-G5@`9814d24c`、
R-G18@`8f61f9fd`、R-G19@`bd206972`、R-G21@`1230e795`、R-G22@`bca29f8f`、
R-VS82@`12177e4f`、R-VF83@`9d5bfa4d`〔但書前，落地後換發〕）+
seq 查證全記錄（含未跑之 git 命令原文）+ 17 項對映表全文 +
**改善案殘留事項清單**（known-not-done 具名，G-D；至少涵蓋 25 §十三之
未結項與本包新生者）。

## H. 本包產生之新條文清單（自檢表）

- [x] 裁定 1（R-VF83 但書 + A-VF{next}）— 區塊已列
- [x] 裁定 2（R-G23）— 區塊已列
- [x] 裁定 3（報告檔名 v2，工具局部）— 區塊已列

本包引用之既有條文：R-G5、R-G9、R-G18、R-G19、R-G21、R-G22、R-VS82、
R-VF83、R-VF66（精神）、A-VS02、G-D、G-K、G-N、FO §9.2、
25 上繳 §四／§五／§六／§八／§九／§十一／§十三。
