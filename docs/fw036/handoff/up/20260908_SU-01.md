# 上繳包 SU-01 —— SW Update：可獨立改善之部分

下放包：`docs/fw036/handoff/down/20260908_SU-01.md`（Pei 指示 2026-09-08「朝著可以改寫的更好的部分去做」）
執行日：**2026-09-08**。結果分類（FO §8.4）：**完成 B、C 兩類**，
另有 **5 列具名跳過**（§2）、**1 項 §五 之更正**（§5）、**2 項自報**（§7）。

---

## §0　一句話

§三 之母體現算為 **41 列**（與下放包相符），其中 **36 列施作、5 列具名跳過**；
§四 之 row 200 引號 2 處已改。
**PENDING 195 → 195、七個 DR 之引用數一格未變**、§五 各項數字全數不動。
母本未開啟寫入（sha256 落檔後複驗未變，R-G72），`surgical_save` 落檔，
48 個 zip member 僅 `sheet6.xml` 相異，**x14 dataValidation 1 → 1 保住**。

---

## §1　sha256 與結構（§六.1）

| 檔 | sha256 | bytes |
|---|---|---|
| 母本 `10_Reviewing/…_SWUpdate_20260830.xlsx` | `d0000d1d96e66f0cbced1dc8b214499c9d186f7576b721da53102d945b7a3985` | 274,026 |
| 沙盒副本 `sandbox/su01/swupdate_20260830.xlsx` | 同上（逐位元複本） | 274,026 |
| **產出 `sandbox/su01/swupdate_20260830_Revise1.xlsx`** | **`e6c23e5f0398f69169c8489b9811dd1826baaa99decc48d2430540d6e164c8b4`** | — |

母本 sha16 `d0000d1d96e66f0c` 與下放包所載**相符**；落檔後複驗**未變**（腳本末行 assert）。
腳本**冪等**：連跑兩次，Revise1 之 sha256 相同。

```
zip member          48 → 48（增 0／刪 0）
相異之 member        xl/worksheets/sheet6.xml   ← 僅此一件
dataValidation      classic 3 → 3；**x14 1 → 1**（見 §7 自報 3）
surgical_save       sheets_patched = 38 cells
儲存格改動           38 格／37 列 —— L 欄 37（36 列縮短 ＋ row 200 引號）、M 欄 1（row 200 引號）
                    **ER 於 36 列縮短中一格未動**
```

⚠ **repo 內 `features/sw_update/delivered/` 之同名檔非本次之來源**
（sha16 `4f6caa35f2a71ca6`，236,029 bytes，與母本逐位元相異）。
本包依下放包 §〇 取 `10_Reviewing/` 之母本為對象；該落差不在本包範圍，一併回報。

---

## §2　§三 —— Final Step 過長（§六.2）

**判準逐字實作**：`test_procedure` 最末一個編號步驟，
以 `^\s*\d+\.\s*` 去序號後 `str.split()` 之詞數 > 18。

**母體現算（不採下放包之抽樣列舉）**

```
Final Step > 18 詞                    61 列
  末步本身即 PENDING 行（不可縮）        20 列
     29 50 54 117 142 158 177 182 187 188 191 239 242 256 279 282 286 296 304 306
  可施作母體                            41 列   ← 與下放包之「41 列」相符
     其中該列他處含 PENDING              5 列（110 145 157 297 307）
     完全不含 PENDING                   36 列
```

⚠ 下放包 §三稱「41 = 不含 PENDING 34 ＋ 含 PENDING 7」，實測為 **36 ＋ 5**；
**總數 41 相符**，子分類相異。下放包所舉之 `148`／`270` 二列實測**不含 PENDING**。

⚠ 詞數之個別差異：下放包所舉之 row 10 記 27 詞、row 11 記 22、row 22 記 25，
實算為 26／21／24（各少 1）；row 13／14／25／35／110／145／148／157／270／297／307 則相符。
差異源於是否把 `N.` 序號計為一詞。**本包一律不計序號**，且 >18 之判定不受此影響
（41 列之集合兩種算法一致）。

**施作結果：36 列**

一律**純前綴截斷**（不改寫語序），落檔前逐列 assert 四項：
(a) ≤18 詞　(b) 仍含 `check that`　(c) 為原句之前綴　(d) 被移除之詞為同列 ER 末行所承載。

逐列改前／改後詞數見 `features/sw_update/reports/su01_finalstep.tsv`。摘要：

```
最大降幅  r61  30 → 11      r148 27 → 6      r157 22 → 6
最小降幅  r11  21 → 18      r103 21 → 17     r160 23 → 17
全 36 列  改前平均 21.8 詞 → 改後平均 12.3 詞，改後最大 18
```

**(d) 之 5 列例外**：ER 以**不同措辭**承載被移除之語意，逐列具名該 ER 片語並 assert 其存在：

| 列 | 被移除之語意 | ER 之對應片語（已 assert 存在） |
|---|---|---|
| 53 | `and does not connect to the access point that is switched off` | `shows no connection to the access point that is switched off` |
| 61 | `and that the software download over Wi-Fi starts …` | `the software download over Wi-Fi starting` |
| 110 | `and that no opt-in screen is shown in the second run` | `the second run shows no opt-in screen` |
| 148 | `…no SW Update prompt or progress notification appears…` | `contains no SW Update prompt and no progress notification` |
| 157 | `…prompt, progress notification or confirmation screen` | `no progress notification and no confirmation screen` |

**跳過 5 列（下放包 §三「ER 未承載即跳過並回報」）**

| 列 | 詞數 | 理由 |
|---|---|---|
| **22** | 24 | 被移除之 `and the engine auto-stop state is not active` **不在 ER** —— ER 末行只寫 `while the ignition is in accessory`。縮短即丟失一個驗證條件 |
| **175** | 21 | 被移除之 `while the package downloads` **不在 ER** —— ER 末行只寫 `shows the download progress as a percentage or as a status indication` |
| **297** | 19 | ER 末行為 `PENDING: DR-SU2` 佔位；proc 末步是該列唯一之非 PENDING 斷言，縮短即丟失比較對象（`head-unit-started session`）|
| **43** | 19 | 全句即「動作 ＋ `check that` ＋ 主要可觀察結果」，**無可移除之細節**；19 → ≤18 只能改寫語序（`the option to navigate back` → `the back option`），屬改寫非截斷 |
| **56** | 20 | 全句即單一斷言（首個連上之 AP ＝ 訊號格最多者）；任何截斷都會丟失驗證標的 `the one shown with the most signal bars` |

**落檔後之殘量**：Final Step > 18 詞 **61 → 25 列**
（20 列末步即 PENDING ＋ 5 列上表之具名跳過）。
⚠ 22／175 之 ER 缺條件、43／56 之句長，皆為**下一輪可處理之項**：
前二者須先補 ER（屬內容改動，非本包之格式範圍），後二者須 Pei 裁是否允許改寫語序。

---

## §3　§四 —— row 200 之引號（§六.3）

```
改前  proc 2. Select the 'Accept' option on the TBM FOTA update notification
      er   2. The 'Accept' option is selected on the TBM FOTA update notification
改後  proc 2. Select the "Accept" option on the TBM FOTA update notification
      er   2. The "Accept" option is selected on the TBM FOTA update notification
```

全簿單引號掃描（作者側四欄）：改前 **2 處／1 列**，改後 **0**。

**`Accept` 標籤之查證結果 —— 查無，故只改引號形制、不改字串**

| 來源 | 結果 |
|---|---|
| `inputs/SYS1_HMI_Software_Updates_FOTA_HMI_Logic_and_Flow_R1_SR24_post_2A_(Aug_30_2023).xlsx` | 全分頁全欄 `\bAccept\b` **命中 0** |
| `forms/Pop Up List HMI R1 SR24 Post 2A (Dec 15, 2023).xlsx` | 命中 6 處，**全為導航類彈窗**（`New Location destination received`／`Faster Route found`／`Adjust Centerline`），**無 TBM FOTA 情境** |
| `inputs/cfts057_objects/` | 6 個檔全為 `.png`，無文字層，`grep` 命中 0 |

**該單引號之來源已查明**：`test_item` 上半（N 欄 `CFTS057-4907882`）逐字為

```
The TBM FOTA HMI shall detect when the user selects the 'Accept' option for the FOTA update notification.
```

即 **CFTS057 原文本身即單引號**。依 IN §11，四個交付欄用雙引號、
`test_item` 之 verbatim 上半保留來源記法 —— 故 **`test_item` 之 `'Accept'` 未動**
（落檔前 assert 其仍為單引號）。

---

## §4　全簿複驗（§六.4）

```
PENDING          195 → 195      ✔ 一列未動
DR 引用（逐 DR）
  DR-SU2  554 → 554     DR-SU6   52 →  52     DR-SU3   37 →  37
  DR-SU7   34 →  34     DR-SU4   27 →  27     DR-SU1    5 →   5
  DR-SU5    3 →   3                                    ✔ 七個全數不變
```

§五 各項於 Revise1 之現算（下放包所載 → 實測）：

| 項 | 下放包 | Revise1 實測 | 判 |
|---|---|---|---|
| 資料列 | 319 | **319** | ✔ |
| 尾句號／行首尾空白／全形標點／交付欄中文 | 各 0 | 各 **0** | ✔ |
| Procedure ↔ ER 1:1 不等 | 0 | **0** | ✔ |
| 括號下半（R-S4）缺 | 0 | **0** | ✔ |
| 括號下半跨 319 列重複 | 無 | **群組 0** | ✔ |
| Priority | P1 165／P2 135／P3 19 | **同** | ✔ |
| `design_method` 種類 | 7 | **7** | ✔ |
| `input_test_data` 全 `NA` | 是 | 非 `NA` **0** | ✔ |
| `specification_reference` 空 | 0 | **0** | ✔ |
| ER 情態動詞 | 0 | **0** | ✔ |
| Procedure 禁用主動詞 | 0 | **0** | ✔ |
| `pc`+`proc`+`er` 三欄全同 | 群組 0 | **0** | ✔ |
| 單引號 | 1 列 2 處 | **0**（本包已改） | ✔ |
| 單步 TC | 5 列（162／174／224／262／319） | **同**，未動 | ✔ |

---

## §5　§五 之一項更正 —— `specification_reference` 並非全數合規

下放包 §五 記「`specification_reference`：空 **0**，**全為 `CFTS057-{7 位}`**」。
**前半成立，後半不成立。** 實測三列非單一 ID：

```
r85   'CFTS057-4907890\nCFTS057-4907891'                          ← 一行一個，合規（IN §10.7）
r110  'CFTS057-4907453\nCFTS057-4907656'                          ← 合規
r262  'CFTS057-4907667 / 4907668 / 4907669 / 4907670 / 4907671 / 4907672'   ← **不合規**
```

**r262 違反 IN §10.7 兩點**：(a) 以 ` / ` 串接而非一行一個；
(b) 後五個 ID 未重述 `CFTS057-` 前綴（§10.7 定「前綴逐行重述」）。

**本包未改。** 理由：下放包 §一 之範圍限定 A／B／C 三類，
§五 是以「已合規」列入不改之項 —— 該前提被本次量測推翻，
但推翻一個前提不等於取得擴張範圍之授權。
修法為純機械（拆六行、逐行補前綴，ID 一個不增不減），
**建議併入下一包**，一行改動即可。

---

## §6　不在本包（下放包 §七，皆未碰）

A 類之查證與 D 欄改動（→ SU-02）；195 列 PENDING 與七個 DR；
47 列未覆蓋之 Functional leaf（其真偽待 A 之查證）；
交付（封面補齊、`delivered/`）—— 出貨 gate 未過。
`RULINGS.md`／`DATA_REQUESTS.md`／`feature.yaml` **一字未改**。

---

## §7　工具旗標與自報

```
openpyxl                     僅作計算層；全域無 wb.save()
backend.xlsx_surgical
  surgical_save(verify=True)   ← 唯一落檔路徑，verify_structure 未降級
  surgical_insert_rows／restyle  未用
ws.delete_rows()             未用
母本                         唯讀（shutil.copy2 讀出），落檔後複驗 sha256 未變
inputs/ 與 forms/            唯讀（Accept 標籤查證）
寫入路徑                     features/sw_update/sandbox/su01/（2 檔）
                             features/sw_update/reports/su01_finalstep.tsv
                             features/sw_update/scripts/su01_apply.py
```

**自報**

**1. 逐詞比對之 (d) 檢查一開始把 5 列判為失敗，其中 4 列是措辭差異之偽陽性。**
我最初以「被移除之 token 是否全部出現在 ER」為硬規，`does not connect` vs
`shows no connection`、`starts` vs `starting` 這類同義改寫因而落空。
**處置不是放寬檢查**，而是逐列具名 ER 之對應片語並 assert 其實際存在（見 §2 之表），
使該列之豁免可被覆核。另一列（r22）確為真失敗 —— ER 真的沒有那個條件，已跳過。

**3. `x14 dataValidation` 之計數我寫錯，審閱包乙-1 指出後複驗屬實。**
我用 `xml.count("<x14:dataValidation")`（**無尾隨空格**），該子字串同時命中
元素 `<x14:dataValidation ` **與**容器 `<x14:dataValidations `，故得 2；
真正之元素數為 **1**。改前改後皆為 1，**「x14 保住」之結論不變**，錯的只有數字。
複查同一算法對其他兩線之影響：

```
                      我原本之寫法   元素數   容器 count 屬性
SWU 母本／Revise1            2          1            1     ← 本包，已更正為 1 → 1
Popup 來源／交付本            1          1            1     ← 當時用了帶空格之判準，正確
CFTS044 Revise2／交付本       0          0         無容器    ← 該簿無 x14 容器，0 正確
```

即**只有本包之數字錯**，已推之 Popup 與已打 tag 之 CFTS044 未受影響。
今後一律以審閱包乙-1 所建議之 **`<x14:dataValidations count="…">` 屬性值**為準。

**2. 「41 列」之相符是我先算出來、再與下放包對上的，不是照抄。**
第一次現算得 61 列（含末步即 PENDING 者），與下放包之 41 不符；
加上「末步本身非 PENDING 行」之條件後得 41，**與下放包完全一致**。
下放包未把該條件寫進判準（只在括號內註「後者之過長步驟本身非 PENDING 行」），
是我補上的推論。**若 Pei 之本意是連末步即 PENDING 之 20 列也要處理，本包漏了 20 列**
—— 但那 20 列之末步就是 `PENDING: DR-…` 佔位，無從縮短，故判其不在範圍。

---

# 附記（回應審閱包 `down/20260908_SU-01_review.md`，2026-09-08）

## 附-1　乙-1 之更正：`x14 dataValidation` 我數錯，審閱包正確

已於 §1、§7 就地更正為 **1 → 1**，並在 §7 加自報 3 說明算法錯誤與影響範圍
（只有本包之數字錯；Popup 當時用了帶空格之判準得 1，正確；CFTS044 該簿無 x14 容器，0 正確）。
今後採審閱包乙-1 之建議，以 `<x14:dataValidations count="…">` 之屬性值為準。

**未複驗之項**：審閱包記「`su01_apply.py` 冪等之宣稱本輪未複驗」。
執行層側之實測為：連跑兩次，Revise1 之 sha256 相同（`e6c23e5f…`）。
該腳本每次從母本重新複製，不對既有產出做增量修改，故冪等。

---

## 附-2　乙-2 之初判 —— 結果遠超「來歷不明」，**建議改寫 SU-02 之首項**

審閱包建議「比對其資料列數與 PENDING 數即可初判」。照做，結果如下（全程唯讀）。

### 附-2.1　兩本只差三欄

```
                        母本 d0000d1d          delivered 4f6caa35
分頁／zip member          9 / 48                9 / 48
資料列                    319（row 10–328）      319（row 10–328）
PENDING                  195 列                195 列
DR（七個）                 554/52/37/34/27/5/3   **逐 DR 完全相同**
Priority                 P1 165 / P2 135 / P3 19   **完全相同**
```

TC 分頁逐格比對：**相異僅 3 欄、936 格、319 列**

| 欄 | 欄名 | 格數 | 母本 | delivered |
|---|---|---|---|---|
| `D` | Requirement or Design ID | **298** | `SWE1-FOTA-65`（r10） | `SWE1-FOTA-003` |
| `F` | Test Case ID | 319 | `NR1L-SWUpdate-001` | `newR1L-SU-001` |
| `G` | Test Group | 319 | `SW Update via USB` | `SW Update` |

`I`／`J`／`K`／`L`／`M`／`N`／`P`／`R`／`AH` **一格未動** —— 即兩本之 **TC 內容完全相同**，
差別只在**識別欄**。`F`／`G` 是命名慣例之改版（同 VehicleCategory 之
`NR1L-VC-001` → `NR1L-VehicleCategory-001`）。

### 附-2.2　⚠ **`D` 欄之 298 格差異不是改版，是母本那一份錯了**

以 037 `AnalysisReport_FULL` 對帳（`SWE-Requirement ID` 為公式欄，
**須 `data_only=True` 取快取值**，否則讀到 `="SWE1-FOTA-" & TEXT(…)`）：

```
037 leaf 383  ——  Functional 307 / Heading 45 / Information 25 /
                  Non Functional 4 / Out of scope 1 / 空 1

                        D 欄相異值   落在 Functional 內   落在外   Functional 未覆蓋
母本 d0000d1d              319          260             59         **47**
delivered 4f6caa35        311          **307**           4          **0**
```

母本之 59 個「落在外」＝ Heading 34／Information 19／Non Functional 4／空 1／Out of scope 1
—— **與下放包 §2.1 所載逐項相符**（本包複算成立）。
而 **delivered 那一份落在外者只有 4 個，且全部是 `Non Functional Requirement`**
（非 Heading／Information），Functional **307/307 全覆蓋、未覆蓋 0**。

**內容層之驗證（非模糊比對，是身分比對）**：拿每一列宣告之 leaf，
比對該 leaf 在 037 之 `Requirement Description` 與該列 `test_item` 上半（去括號下半）：

```
                     逐字相同    前 80 字相符    合計
母本 d0000d1d             5           0        5 / 319
delivered 4f6caa35      45          82      127 / 319
```

逐列抽查（下放包 §2.2 所舉之三例 ＋ 本包 §3 之 row 200）：

| 列 | `test_item` 上半 | 母本 D → 037 該 leaf | delivered D → 037 該 leaf |
|---|---|---|---|
| 17 | `The HMI shall navigate to the software download via Wi-Fi screen…` | `FOTA-072`＝**Heading**「OTA Client Architecture」 | `FOTA-011`＝Functional，desc **逐字相同** |
| 18 | `If a previously configured Wi-Fi network is available during Body OFF mode…` | `FOTA-073`＝**Heading**「Operating Environment」 | `FOTA-012`＝Functional，**惟 desc 不相符**（相似 0.07，見附2-2）|
| 19 | `During automatic FOTA download resume…` | `FOTA-074`＝**Heading**「Over The Air (OTA) Deployment of Software」 | `FOTA-013`＝Functional，**惟 desc 不相符**（相似 0.36，見附2-2）|
| 200 | `The TBM FOTA HMI shall detect when the user selects the 'Accept' option…` | `FOTA-255`＝Functional，但 desc 為「SWMC shall reliably download the deployment package…」**無關** | `FOTA-239`＝Functional，desc **逐字相同** |

**結論**：下放包 §2.2 之「該 59 列之 D 欄值至少有一部分是錯的」成立，
且**範圍比它推測的更大** —— 母本之 `D` 欄是一份**整體錯位**的對照，
而**正確的對照已經在 repo 裡**（`features/sw_update/delivered/` 那一份）。

⚠ 下放包 §2.3 之兩點保留在此**不適用**：
(1)「拿 CFTS057 與 037 兩份不同文件比對」—— 本次不是跨文件相似度，
是「該列所**宣告**之 leaf，其 037 desc 是否即該列之 `test_item`」之身分比對；
(2)「037 內部近似重複使最佳匹配不唯一」—— 本次沒有做匹配搜尋，
只驗證一個**既有**宣告是否成立。故本節之結論不受 §2.3 之保留所限。

### 附-2.3　對 SU-02 之影響

下放包 §2.4 之工項 1（自兩份 `.docx` 抽 CFTS057 ObjectID ↔ 條文對照）
**可能不必做** —— 其目的是建對照鏈以判 `D` 欄正誤，而
`delivered/` 那一份已提供一個 307/307 全覆蓋、內容可逐列驗證之對照。

**建議 SU-02 改為**：
1. 以 `delivered/` 之 `D` 欄為候選，逐列驗證（037 desc ↔ `test_item` 上半），出
   `reports/swu_reqid_audit.tsv`；不相符者才回頭走 ObjectID 抽取。
2. 若驗證通過，`D` 欄之修法即「以 `delivered/` 之值取代母本之值」，298 格，機械可為。
3. **下放包 §2.4 末之警語隨之解除**：47 列未覆蓋之 Functional leaf
   在 delivered 之對照下為 **0** —— 那 47 列**不是覆蓋缺口，是掛錯號**，
   與下放包所擔心的完全一致。

**本包未改任何 `D` 欄值**（A 類明定屬 SU-02）。以上全為唯讀量測。

⚠ **反向風險須並陳**：也存在「`delivered/` 那份才是舊的、母本之 `D` 欄是上游後來重新指派」
之可能。但若如此，重新指派後之結果會是 59 列指向 Heading／Information、
且 127 → 5 列內容相符 —— **指派品質全面劣化**，不合常理。
判定仍須 Pei 確認兩份之先後與來歷（`F` 欄之 `newR1L-SU-` → `NR1L-SWUpdate-` 方向
與 VehicleCategory 同型，暗示母本較新）。

---

## 附-3　審閱包其餘各項

- **甲-1（r262 spec ref）**：本包未改，維持 §5 之立場，待 Pei 裁併入 SU-02。
- **43／56 是否允許改寫語序**、**22／175 之 ER 補條件**：皆待 Pei，本包未動。
- **丙-1（「已合規表須附判準式」升格 FO 硬規）**：分析層自報，執行層無異議；
  本包 §4 之複驗表已逐項附實測數，可作為該格式之先例。

---

# 附記 2（回應審閱包 2 `down/20260908_SU-01_review2.md`，2026-09-08）

## 附2-1　§二.3 之決定性證據 —— 執行層獨立複算，**全數成立**

```
                    列數  相異值  首值      末值      嚴格遞增  區間內跳號  重複 leaf
母本 d0000d1d        319   319   065(r10)  383(r328)   True        0          0
delivered 4f6caa35   319   311   003(r10)  383(r328)   False      70          6

母本：末值−首值+1 = 383−65+1 = 319 = 列數  → **065–383 之連續整數，與列序一一對應**
delivered 之 6 個重複 leaf：-100(×2) -116(×3) -130(×3) -133(×2) -136(×2) -176(×2)
                            ← 與審閱包所列**逐項相同**
```

**審閱包之結論成立**：母本之 `D` 欄是按列序機械填入之連續整數，
不可能是任何形式的「重新指派」之產物；delivered 則呈現真實指派之特徵
（一 leaf 對多列、略過非 Functional）。**執行層並陳之反向風險就此排除。**

⚠ **這一步我在 SU-01 就該做。** 見附2-5 自報。

## 附2-2　§三.1 之更正 —— **接受**

審閱包所引之「row 17/18/19，delivered 指向的是 desc 逐字相同的 Functional leaf」，
出自我**回報給 Pei 之口頭摘要**，非本上繳包之表；
本包 §附-2.2 之表對 18／19 寫的是「同題材」，未稱逐字相同。

但「同題材」同樣是**未量化之形容**，正是審閱包丙-1 系列所指之形態。
依審閱包實測（相似度 0.07／0.36）就地更正該表：

| 列 | delivered D | 相符程度（實測） |
|---|---|---|
| 17 | `-011` | **逐字相同** |
| 18 | `-012` | **不相符**（相似 0.07）—— 037 desc 為 `monitor the vehicle operational power mode`，`test_item` 為 `If a previously configured Wi-Fi network is available during Body OFF mode…` |
| 19 | `-013` | **不相符**（相似 0.36） |
| 200 | `-239` | **逐字相同** |

**四例中兩例相符。** 結論不受影響（§二.3 之論據不依賴此四例），惟舉例已更正。

## 附2-3　§2.2 之 127 vs 125：兩種取法皆得 127，附逐列證據表

審閱包記 `前 80 字相符 80／合計 125`，本包記 `82／127`。
執行層以**兩種互不相同之 `test_item` 上半取法**重跑：

```
取法 A（剝除所有結尾之 (…) 行，其餘以空格接合）   → 127 列
取法 B（只取第一行）                              → 127 列
兩者之命中列集合**完全相同**（對稱差 0）
```

故該 2 列之差不出在 `test_item` 之取法。為使此項可被逐列覆核，本包產出：

```
features/sw_update/reports/su01_dcol_evidence.tsv
欄：row / tc_id_master / D_master / D_delivered / tier / similarity（319 列全覆蓋）
```

`exact` 之 45 列兩造相同；差異落在 `prefix80` 與相似度分級：

| 分級 | 審閱包 | 執行層 |
|---|---:|---:|
| 逐字相同 | 45 | **45**（相同）|
| 前 80 字相符 | 80 | **82** |
| 相似 ≥0.6 | 57 | **76** |
| 相似 <0.6 | 137 | **116** |

相似度之落差應為**函式不同**（本包用 `difflib.SequenceMatcher` 對全句折疊空白後之
比值；審閱包未載其算法）。**建議以上表 TSV 之 `tier`／`similarity` 逐列對帳定案**，
或由審閱包指定相似度函式後由執行層重算。**任一結果皆不改變 §二.3 之結論。**

## 附2-4　§四 之分級施作 —— 執行層無異議，並確認三項邊界

1. **同意不採「298 格一次取代」。** 審閱包之關鍵區別（「取代母本不需證明 delivered
   每列都對，只需證明母本整欄不可信」＋「但不可在報告中混為已驗證」）**成立且必要**。
2. **`unverified-by-text` 之標記本包已可供給** —— `su01_dcol_evidence.tsv` 之 `tier`
   欄即為該分級之載體，SU-02 直接沿用即可，不需重算。
3. **`F`／`G` 欄本包未動、SU-02 未經裁定前亦不動。**
   補一項實測供 Pei 裁：`features/sw_update/feature.yaml` **確無 `tc_id_format` 鍵**
   （對照 `features/popup/feature.yaml` 有 `tc_id_format: "NR1L-Popup-{NNN}"`）。
   另 VehicleCategory 之 20260907 本已由 Pei 親手改為 `NR1L-VehicleCategory-{NNN}`
   （見 `up/20260908_VC-RECON.md` §2），與母本之 `NR1L-SWUpdate-{NNN}` 同型；
   `newR1L-SU-{NNN}` 則與 popup／VC 兩線之現行式皆不同。**傾向母本式，但仍請 Pei 裁。**
4. **§4.3「47 列不是覆蓋缺口」之解除，執行層確認**：在 delivered 之對照下
   Functional 未覆蓋為 **0**（附-2.2 已實測）。

## 附2-5　自報 —— 我也「否定一條路之後未再找路」

審閱包丙-2 自省「模糊比對判定不可採後，未回頭找更簡單的檢驗」。
**同一件事我在 SU-01 也犯了，而且我比分析層晚一步。**

我在附-2.2 已經把兩本之 `D` 欄逐列取出、算了對 037 之覆蓋與身分比對，
**手上就有那 319 個數字**，卻沒有做最簡單的一件事：排序、看首末、數跳號。
若當時做了，就會直接得到「065–383 連續整數」這個決定性結論，
也不必在上繳裡並陳一個其實可以排除的「反向風險」。

形態：**我做了較複雜的檢驗（覆蓋率、身分比對），就默認已經夠了，
沒有回頭問「有沒有更便宜且更強的檢驗」。** 與丙-2 同族。
防再犯：對任何「兩份候選何者為真」之判定，**先做 ID 層之算術檢驗
（值域、連續性、重複、與列序之相關），再做內容層之比對** —— 前者成本低且常為決定性。
