# 上繳包 CDD-13_A —— CS 原件到手：Annex A 轉錄、DR-DIAG-1／9／11 結案、`U` 歸零

下放包：`down/20260918_CDD-13_A.md`（CDD-13 附錄 A）。執行層 session：沿用 CDD 線，2026-09-18。
**本包為 CDD-13 之續，合併本承 `v10` → `v11`，交付本 `v03` → `v04`。**

## 首段 —— 升級條件（CDD-13 §5 四項 ＋ 附錄 A §5 增一項，逐字複製）

| # | 條件 | 實測 | 觸發 |
|---:|---|---|:-:|
| 1 | 指紋／`--check`（既存者記「既存」） | 寫前 HEAD 79 ＝ worktree 79（CDD-13 未提交，故基準為 HEAD），寫後 **82**（本包新增 0 列、**就地更新 1 列**＝R-DIAG5(amend3) 之改寫）；零漂移。`--check` FAIL → **既存** | **否** |
| 2 | CFTS004 之 IOCP／controlOptionRecord 敘述與 CS.00100 Table 51 矛盾 | CFTS004 該二詞命中 0 | **否** |
| 3 | T3(c)／T4 新 sibling > 40 | 本包 (c) 三軸**零命中**，新 sibling **0** | **否** |
| 4 | 任一 037 列 > 8 TC | 最大 **5** | **否** |
| 5 | **Annex A 轉錄與 CFTS004 明載之 NRC 值衝突 → 回報不改，Pei 裁（CFTS004 為母文件，位階高於 CS.00100）** | 差異表 **0 行**（見 §3.2） | **否** |

**五項皆未命中。**

---

## §1　T0 —— 原件盤點與登錄

`1_Customer_Requirement/STLA Standard/`（**278 檔**）＋ 其 `Update/` 子目錄（**20 檔**）。

| doc_id | 檔 | sha16 | 版 |
|---|---|---|---|
| `cs00100_uds_on_can_D` | `CS.00100.pdf`（81 頁） | `c1f9c6d7c5a5f0a2` | Change Level **D** |
| `cs00099_diag_std` | `CS.00099.pdf` | `1c0da0c9ff705482` | 原件 |
| `cs00102_std_diag_data` | `CS.00102.pdf` | `da5caf89903bb53d` | 原件 |
| **`sd00049_diag_access_firewall_F`** | **`Update/SD.00049.pdf`（7 頁）** | `456413aba708fbdb` | Change Level **F** |
| `iso_14229_1_2020` | `ISO 14229-1-2020.pdf` | `761a6608f7334013` | 2020 |

`sources/MANIFEST.tsv` **+5 列**，`git diff --stat` 覆核 `5 insertions(+)`，無他線列變動。

> **`SD.00049` 實際存在**（`[A-DIAG71]`）—— 附錄 A 稱「目錄無 SD.00049」，實在其 **`Update/` 子目錄**。
> 其 §3 逐字：`The embedded Excel file (Section 4) defines the access rights for each UDS and KWP function and role`。
> **該 xlsx 未附於 PDF**（無 `FileAttachment` 註記、Catalog 無 `Names`）→ **`DR-DIAG-11` 維持 MISSING**，
> 但 PDF 本身提供角色描述（Table 1）與色碼定義，已登錄可用。

---

## §2　T2 —— Annex A 轉錄

`features/diagnostics/data/cs00100_annex_a.md`。Annex A 於 **p61–p75**（NORMATIVE）。

### 2.1　浮水印濾除法（一句）

**浮水印字型為 `Helvetica-Bold`（20.0／25.2／26.3 pt），內文一律 `ArialMT`／`Arial-BoldMT`（5–14 pt）**，
故以 `page.filter(lambda o: o["object_type"]!="char" or not o["fontname"].startswith("Helvetica"))` 濾除 ——
**不依座標、不依字級**，濾後全頁文字連續可讀（`[A-DIAG72]`）。

### 2.2　五圖節點數

| 圖 | SID | 判斷節點數 | 頁 |
|---|---|--:|--:|
| Figure A-1 | General | **12** | 62 |
| Figure A-2 | `$31` | **8**（＋ manufacturer/supplier optional） | 63 |
| Figure A-4 | `$22` | **7**（含多 DID 迴圈） | 66 |
| Figure A-9 | `$2F` | **6**（＋ manufacturer/supplier） | 70 |
| Figure A-11 | `$2E` | **7**（＋ manufacturer/supplier optional） | 72 |

其餘九圖（A-3 `$14`／A-5 `$23`／A-6 `$37`／A-7 `$36`／A-8 `$34`／A-10 `$3D`／A-12 `$2A`／A-13 `$19`／A-14 `$27`）只列標題。
另轉錄 `Table A-1 - Service overview`（p61）與 `Table A-2` 之五個在用碼名（p76–78）。

### 2.3　與本 feature 既有裁定之交叉印證（意外收穫）

| Annex A | 印證 |
|---|---|
| **A-4 第 6 判斷**：`At least one DID is supported in the active session?` → `NRC $31` | **印證 R-DIAG22(amend)**「`0x22` 讀類第二軸為 unsupported DID，ER `7F 22 31`」 |
| **A-9 註 2**：`if IOCP = shortTermAdjustment, 1 byte SI + 2 byte DID + 1 byte IOCP + nth byte controlState…` | **印證 R-DIAG29**：`controlState` 只於 `03` 存在；與 Table 51 NOTE 一致 |
| **A-9 註 1**：`at least 4 (SI + DID + IOCP)` | **印證 R-DIAG22 之長度軸**：`2F <DID> 03`（3 bytes）確實不足 4 → `NRC $13` 成立 |

---

## §3　T3 —— R-DIAG5(amend3) 之落地

### 3.1　(a) DR-DIAG-1 三列之取碼

| TC | 037 列 | 依據圖／節點 | 取碼 |
|---|---|---|---|
| `NR1L-DIAG-281` | `-156`（`$0312` 常式） | **Figure A-2 第 7 判斷 `Condition check`** —— 請求之格式、RID、安全、子功能、長度、`routineControlOptionRecord` 六項皆通過，失效落在條件檢查 | **`7F 31 22` conditionsNotCorrect** |
| `NR1L-DIAG-297` | `-237`（`$2847` 寫入） | **Figure A-11 第 7 判斷 `Was correctly altered into server's memory`** —— 寫入未生效之分支 | **`7F 2E 72` generalProgrammingFailure** |
| （`-157`） | `-157` | 本即 unsupported SID 型，已有 `7F <unsupported SID> 11` | 不需改 |

**全合併本殘餘 PENDING 行：0。lint `U` 由 3 → 0。**

### 3.2　(b) 既有 223 列之對照 —— **差異表 0 行**

| 軸 | SID | NRC | 條數 | Annex A 期望 | 判 |
|---|---|---|--:|---|:-:|
| unsupported SID 軸 | `<unsupported SID>` | `11` serviceNotSupported | 92 | A-1 第 1 判斷 `$11` | ✔ |
| 長度軸 | `22` | `13` | 85 | A-4 第 1/2 判斷 `$13` | ✔ |
| unsupported DID 軸 | `22` | `31` | 85 | A-4 第 6 判斷 `$31` | ✔ |
| 值域軸 | `2F` | `31` | 39＋2 | A-9 第 2/4 判斷 `$31` | ✔ |
| 長度軸 | `2F` | `13` | 35 | A-9 第 1/3 判斷 `$13` | ✔ |
| 長度軸／值域軸 | `2E` | `13`／`31` | 5＋5 | A-11 第 1/3、第 2/6 判斷 | ✔ |
| 長度軸／值域軸 | `31` | `13`／`31` | 4＋5 | A-2 第 1/5、第 2/6 判斷 | ✔ |
| 其他 | `22`／`2F` | `22` Conditions Not Correct | 2 | A-4 第 5／A-9 第 6 判斷 | ✔ |

**與 Annex A 不合者 0 行 —— 無一需 Revise。**

### 3.3　(c) 新負向軸 —— **三軸零命中，不產**

grep 全 389 列之 037 Description／VC 與 CFTS004 同節：

| 軸 | 命中 | 判 |
|---|--:|---|
| 安全未解鎖 → `$33` | 3 → **0** | 命中之 `-346`／`-347`／`-348` 係 `KeySense PIN` 之 `Key` 觸發樣式，**假陽性**（`[A-DIAG73]`） |
| session 不符 → `$7F` | **0** | — |
| 子功能不支援 → `$12` | **0** | — |

依 R-DIAG8 **不產**。

### 3.4　R-DIAG5(amend3) 改寫

條文本體依附錄 A §3 改寫（去掉「（原件到件後）」之但書，補 (a)(b)(c) 三款）。
指紋**就地更新 1 列**：`R-DIAG5(amend3)` sha8 `b0acf8d5` → **`b6eeb923`**，body_sha8 `b33bc24a` → **`fe8e6856`**，9 行。

---

## §4　T5 —— 合併本與交付本

| 項 | 值 |
|---|---|
| `merged/diagnostics_v11.xlsx` | **565 列** |
| 交付本 **v04** | `…_SWQT_Diagnostics_20260918_v04.xlsx`，**565 列／著色 429 列**；`id_map_v11_to_delivery.tsv` |
| 十本批次 | pilot v09／b1 v08／b2 v07／b3 v06／b4 v06／b5 v07／b6 v05／b7 v04／ext v03 |
| **lint** | **合併本與交付本皆 `全 0`** —— 31 項無一命中，**首次**（`U` 由 3 → 0） |
| canon §9 | 17/17 |
| coverage | **389 列，UNCOVERED 0，TC 565** |
| `placeholder_summary.tsv` | **592 行／429 條**（100% 登錄）；`DR-DIAG-6` 269／`DR-DIAG-5` 206／`DR-DIAG-4` 99／`DR-DIAG-8` 18 |

被取代之 sandbox 舊版 **10 本**已刪；交付本 v01～v04 並存（R-G72）。

---

## §5　DR 現況

| # | 前 | 後 |
|---|---|---|
| **1** NRC 權威來源 | MISSING（3 列） | **CLOSED** —— Annex A 取碼，PENDING 清零 |
| **10** CS.00100 原件 | MISSING | **CLOSED** —— 已登錄並轉錄 |
| **12** CS.00102 原件 | MISSING | **CLOSED** —— 已登錄 |
| 11 SD.00049 附件 | MISSING | **MISSING（維持）** —— PDF 已取得，內嵌 xlsx 未附 |
| 4 controlState 編碼 | MISSING | 不變（99 行佔位） |
| 5 資料位元組版面 | MISSING | 不變（206 行） |
| 6 不支援之 SID 與 DID | MISSING | 不變（269 行） |
| 8 `SX-9830-0095` ＋ PkgIndex | PARTIAL | 不變（18 行） |

---

## §6　自報錯誤

| # | 內容 |
|---|---|
| `[A-DIAG71]` | 附錄 A 稱 `SD.00049` 無，實在 `Update/`。我全樹搜尋才找到 —— 但這也代表 **CDD-13 我只搜了 `REF/`，沒搜 `STLA Standard/`**，否則 CS 原件在 CDD-13 就該找到，不必等附錄 A 指路 |
| `[A-DIAG72]` | Annex A 浮水印濾除法（已解） |
| `[A-DIAG73]` | (c) 安全軸之 3 個命中為 `Key` 假陽性 |

---

## §7　未結與待 Pei

| # | 內容 |
|---|---|
| **`DR-DIAG-11`** | SD.00049 之內嵌 xlsx —— **`0x2F` 是否需 `0x27`** 之唯一答案所在；R-DIAG13 保守維持 |
| `DR-DIAG-4`／`-5`／`-6` | 三件仍缺 → **429 條著色列不可派工，可執行 136 條** |
| `[A-DIAG64]` | **CS.00100 未被任一來源明引**（CDD-13 提出），本包之 Annex A 取碼亦依其。**請覆核位階** |
| CDD-13 之三項 | T3(c) 之 12 列未產、T4 兩軸未產（S3ECU 與 5 s override 是否同一計時器無來源） |
| GC-16 | `--check` 第九包記「既存」 |

---

## §8　建議 commit

**本包與 CDD-13 一併提交**（CDD-13 尚未 commit）：

```
feat(diagnostics): CDD-13 + A — CS series and Annex A applied, zero lint hits, 565 TCs

- R-DIAG29/R-DIAG30/R-DIAG5(amend3) landed, amend3 rewritten once the original arrived
- CS.00100/00099/00102, SD.00049 and ISO 14229-1 originals registered from STLA Standard
- Annex A transcribed: five figures node by node, watermark filtered by font
- DR-DIAG-1 closed: -156 takes $22 from Figure A-2, -237 takes $72 from Figure A-11
- all 223 existing NRCs match Annex A, zero rows需 revise
- all 129 I/O requests carry IOCP 03, 7 Return Control siblings, 30 wording fixes
- merged v11 and delivery v04: 565 TCs, lint zero on every check for the first time
- A-DIAG63..73 registered
```

pathspec：

```
features/diagnostics sources/MANIFEST.tsv docs/fw036/RULINGS.sha.tsv \
docs/fw036/lint_reports/diagnostics_*_2026091*.md \
docs/fw036/lint_reports/Diagnostics__diagnostics_*_2026091*.md \
docs/fw036/handoff/up/20260918_CDD-13.md docs/fw036/handoff/up/20260918_CDD-13_A.md \
docs/fw036/handoff/down/20260918_CDD-13.md docs/fw036/handoff/down/20260918_CDD-13_A.md \
docs/fw036/handoff/down/20260917_CDD-12_review.md
```
