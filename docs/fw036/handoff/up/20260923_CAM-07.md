# 上繳包 CAM-07 —— CAM-06 裁決落檔 ＋ batch02a 生成（`-001`／`-012`／`-014`）

對應下放包：`docs/fw036/handoff/down/20260923_CAM-07.md`
對應審閱：`down/20260923_CAM-06_review.md`
執行層 session：延用，2026-09-23。**git 未 commit**；未寫回工作簿；`pilot01/` 未動。

> **升級條件皆未觸發**：(1) batch02a ＝ **26 ≤ 30**；(2) `-012` 之 `V2-550` 原句 **46 token ≤ 50**，
> 無須摘句；(3) 兩列 IF＋THEN 合一後之 verbatim 為 10／9 token，未逾限亦無須回退。
> §1／§2／§3 全數完成，26 列產物 lint ＋ selfcheck 全綠（`J` 五列為逐字所迫，見 §3-3）。

### R-G62(b) 之觀察依據

起點前之最後他方寫入：`down/20260923_CAM-06_review.md` 與 `down/20260923_CAM-07.md`（本輪之下放檔）。
本 session 起點後至落檔止，`features/camera/` 下未觀察到他方寫入；
`docs/fw036/handoff/down/20260922_CMF-07_review.md`（CMF 線）維持未追蹤，本包未觸及。
**不聲稱「無他 session 寫檔」**，可證者只有「未觀察到」。

---

## §1　裁決落檔

### 1.1　取號與 sha8

取號現查：全 repo（排除本包下放檔）之最大 `R-CAM` 號為 **14**，`{live}` ＝ **15** 成立，
與下放包所載相符。**惟本輪三條皆為既有條之增補／修訂**，依 **R-TM13／GCB-06** 以
**同級錨自立**（`### R-CAM14(b)` 等），**未消耗 `{live}`；15 仍未用**。

| 條 | body sha8 | fenced sha8 | 錨點 |
|---|---|---|---|
| **R-CAM14(b)** | `c3f6d57c` | `3ca7ffcd` | `features/camera/RULINGS.md:239` |
| **R-CAM14(c)** | `aa26705f` | `dd1e8b1c` | `features/camera/RULINGS.md:251` |
| **R-CAM10(b)** | `f5c479b2` | `af4fab51` | `features/camera/RULINGS.md:262` |

**同級錨自立之效果實測（GCB-06 之驗證）**：

| 條 | fenced sha8 前 → 後 | body sha8 前 → 後 |
|---|---|---|
| `R-CAM10` | `1ef47db1` → `1ef47db1`（**不變**）| `e50f2445` → `e50f2445`（**不變**）|
| `R-CAM14` | `cbc0f465` → `cbc0f465`（**不變**）| `44043c8c` → **`8525cab2`**（變）|
| `R-CAM13` | `085490df` → `085490df`（不變）| `ac64353f` → `ac64353f`（不變）|

`R-CAM14` 之 body 變動係其標題下新增之**刪除線保留段與作廢理由**（R-TM13 所令），
非 fenced 條文之改動 —— fenced sha 不變即為其證。`R-CAM10` 兩者皆不變，
因 (b) 段之 fenced block 落在自己的錨下。**GCB-06 之作法於本輪得到實證。**

### 1.2　DECISIONS

| § | 內容 |
|---|---|
| **6-15** | `-023` 之 Pre-Condition 3（CAN source 行）刪 —— CAM-06 §5-5 所請之確認結案 |
| **6-16** | batch02 三切：02a **26**／02b 22／02c **23**（合一後總計 73 → **71**）|
| **6-17** | A-CA32 之 `SYS-RA-CAM-076` 改為生成（`NR1L-RVC-046`），Test Set `Display Arbitration` |
| **6-18** | `SWE-CAM-002` 不生成 negative，缺口登 RDF-06 |

> **6-16 之數須更正下放包之預期**：下放包 §3 題為「`-001`（22）＋ `-012`／`-014`（4）」＝ 26，
> 正確。R-CAM10(b) 之合一**減少的是 `SWE-CAM-003`（25 → 23）而非 `-001`** ——
> `V33-221`／`-223` 於原計畫本已各列 1 TC，合一後其對應之 `V33-222`／`-224`（原屬 `-003`）
> 歸零。`-001` 維持 22，batch02a ＝ **26**。

### 1.3　ANOMALIES

| # | 變動 |
|---|---|
| **A-CA22** | PENDING → **CLOSED** —— 標定值與單位皆已查得（V2 §1.8.13 `8`／`mph`；V3 §1.14.1 同數同單位），「無標定單位」之前提不成立 |
| **A-CA30** | PENDING → **CLOSED** —— 不可判只對 Atl-Hi 成立；分歧不在運算子而在兩平台標定本不同數 |
| **A-CA31** | → **RESOLVED**；637 依 R-CAM14(b) 併入 `-030`／`-031` |
| **A-CA32** | → **RESOLVED** —— `-076` 比照生成（`NR1L-RVC-046`）|

### 1.4　DATA_REQUESTS

| DR | 內容 | 級 |
|---|---|---|
| **DR-CAM-k**（新）| VF664 全文件（`SYS-RA-VF551_V2-535` 所引之 soft key button controls）| 中 |
| **DR-CAM-l**（新）| Toro (2261) 之現行 PROXI 表 —— 2019 本止於 byte 172，缺 byte 177／222 之 camera 參數 | 中 |

### 1.5　RD_FEEDBACK

**RDF-06** —— `SWE-CAM-002` 只有配備成立側，無 Absent 側之行為。
**逐字掃描為實測**：該列 20 個 in-scope 來源，`Absent`／`= 0`／`not present` 三串
（含大小寫變體）**零命中**；對照 CFTS092 之 VC 欄有寫 negative（`SYS-RA-CAM-084`），
惟 VC 不是來源（R-CAM13(d)）。索引表同步。

### 1.6　framework VIII.2 例外註

擴為**四列供稿表**（`-016` 12 ／`-017` 2 ／`-001` 1 ／`-003` 3），
並加**反向例**：`SWE-CAM-016` 之 `CAM-076` 其 TC 落 `Display Arbitration` ——
判準是**來源列所屬之節**，不是 TC 所在之目錄。

狀態行之 sha16 沿革：`c4fc9d2e5b7d1f44`（重開前）→ `ee74d26960d6c281`（CAM-06 落檔）→
**`8569aa484b552b7c`**（本輪例外註擴充後）。量測法同前（狀態行**最後一個** sha16 欄位以 16 個 `0` 代入）。

### 1.7　profile §8

新增 `## 8. framework 重開之 sha16 量測法`，內含量測法之文字定義、可貼上之複驗片段與 sha16 沿革；
原 §8（車型軸）順延為 **§9**（含 §8.1 → §9.1）、原 §9（未決）順延為 **§10**。
全 repo 掃描無他檔引用 Camera profile 之 §8／§9（僅 `privacy`／`comfort` 引其**各自**之 profile），
重編號無外部斷鏈。

---

## §2　既有列修正 ＋ `-046`

欄級 diff：`features/camera/generated/batch01/DIFF_CAM06_to_CAM07.md`（三列改動 ＋ 一列新增）。

| TC | 改動 |
|---|---|
| `-023` | Pre-Condition 由 5 行減為 **4 行**（刪 CAN source 行），其後兩行順延 |
| `-030`／`-031` | `VF(ProMaster)637` 由 `0` → **`1`**；`specification_reference` 由單行改為**兩行**（加 `VF551_V42_P637MCA_VF_960`）；Pre-Condition 第 6 行加 `and VF551_V42 1.14.1`；**供試 raw 209／208 不動**（兩平台標定同數）|
| `-046`（新）| `SYS-RA-CAM-076` → `CFTS092-4781641`，Test Set **`Display Arbitration`**，五車型全勾，P2，PENDING `DR-CAM-g` |

### 2.1　selfcheck 之兩項擴充（R-CAM14(b)／R-CAM10(b) 所迫，**須裁**）

下放包之兩條裁決使第 1 項（R-CAM10 錨反查）出現兩類原本必然誤報之錨，本包因而擴充該項。
**這是判準之放寬，逐項具名請 Pei 確認**：

1. **常數錨** —— `R-CAM14(b)` 令 `-030`／`-031` 加第二錨 `SYS-RA-VF551_V42-656`。
   實測該列 **`Category` ＝ `Information`**，且於 `layer3_a_vf_chapters.tsv`（A 本 541 個來源引用之全量展開）
   **零命中 —— 037 從未引用**。R-CAM10 之規範對象為「037 兩列以上共引同一來源」，
   未被引用之列不可能被共引，故不在其射程。判準：`Category = Information` **且** 037 未引 → 另計「常數錨」，不判違反。
2. **合一錨** —— `R-CAM10(b)` 令 IF／THEN 兩來源合一，其承接列必然不同（否則無須本條）。
   判準改為：TC 有 ≥ 2 個非常數錨時，`req_id` 須等於諸錨承接列之**最小者**（即 R-CAM10(b) 之文面）；
   單錨 TC 仍逐錨相等。另計「合一錨」。

本輪實測：常數錨 **2**（`-030`／`-031`）、合一錨 **2**（`-062`／`-063`），命中 **0**。
兩類皆逐列印出，不沉默吞掉。

---

## §3　batch02a 生成（26 列，`NR1L-RVC-047`～`-072`）

### 3.1　產物

`features/camera/generated/batch02a/`：26 × json ＋ 26 × md ＋ `INDEX.md` ＋ `LINT_REPORT.md`。

| SWE | 列數 | TC |
|---|---:|---|
| `SWE-CAM-001` | 22 | `-047`～`-068` |
| `SWE-CAM-012` | 3 | `-069`／`-070`／`-071` |
| `SWE-CAM-014` | 1 | `-072` |

Test Set 除 **`NR1L-RVC-048`**（`SYS-RA-CAM-088`，SVC 節 → `Auxiliary Cameras`，R-CAM13(c)）外，
其餘 25 列皆為 `Startup and Shutdown`。

**摘句三列**（皆為保序子序列，條件與結果子句皆保留）：

| TC | 來源 | 原 token | 摘後 | 摘法 |
|---|---|---:|---:|---|
| `-050` | `V3-252` | 62 | **45** | 刪末句 ISO 15008 字型／字高（17 token）|
| `-054` | `V3-251` | 88 | **42** | 刪 ISO 字型句（17）＋ `a)` 子句（34）|
| `-055` | `V3-251` | 88 | **28** | 只取 `a)` 子句（尾段連續片段）|

ISO 字型與字高之量測由 `SWE-CAM-003` 之 `SYS-RA-VF551_V4-117` 承接（batch02c）。

### 3.2　lint ＋ selfcheck

```
batch02a（26 列）：A=0 B=0 C=0 D=0 E=0 F=0 G=0 H=0 I=0 I-sibling=0 K=0 L=0 M=0 N=0
                  P=0 Q=0 R=0 T=0 V=0 W=0 X=0 Y=0 **Z=0**｜**J=5**  U=4  I-cross=26
batch01b（13 列）：ERROR 類全 0，Z=0｜U=17  I-cross=13  X=3
batch01 （23 列）：ERROR 類全 0，Z=0｜U=4   I-cross=23  X=0
pilot02 （10 列）：ERROR 類全 0，Z=0｜U=1   I-cross=10  X=0
selfcheck_camera（四目錄 72 列）：1 命中 0（常數錨 2／合一錨 2）／2 命中 0／3 命中 0／4 命中 0 —— exit 0
```

- `Z` ＝ 0（72 列 × 7 欄 ＝ 504 格）。
- `U` ＝ 4（batch02a）：`-052` 之 DR-CAM-f 三處、`-064` 之 DR-CAM-i 一處。
- `X` ＝ 0（batch02a）—— `-064` 初稿曾命中，已補 `ENTER_CAMERA_SETTINGS` 三 hop（R-CAM8）為固定入口，
  該補正同時使該列之導航可執行。batch01b 之 `X` ＝ 3 為 `-034`／`-039`／`-046` 之 Controls screen，
  即 DR-CAM-g 本體，WARN 只報不改。
- **`A` ＝ 0** —— `-059` 初稿之 `check whether` 為禁用動詞，已改 `check that no …`。

### 3.3　**`J` ＝ 5：逐字首字小寫（須裁，本包未改字）**

| TC | 來源 | 首 token |
|---|---|---|
| `-051` | `V33-226` | `when` |
| `-052` | `V33-228` | `when` |
| `-055` | `V3-251` `a)` 子句 | `a)` |
| `-056` | `V42-230` | `implement` |
| `-070` | `V2-553` | `a.` |

五列之 test_item 上半**皆為來源逐字**，其首字本即小寫。
改為大寫即破壞 §4.3.1 之逐字忠實，且會使 selfcheck 第 4 項（保序子序列）失敗。

**交付語料之前例（實測）**：`features/*/delivered/` 之 test_item 首行共 **2223 筆**，
其中首字小寫 **10 筆**，分佈於 `power` 與 `vehicle_setting` 兩本 —— **兩本皆已交付**。
故本包**維持逐字**，`J` ＝ 5 列為已知豁免類，五列之 reasoning 已逐列具名。
是否入 profile 為正式豁免條（或改由 lint 側以「上半逐字」為豁免條件），**歸 Pei**。

### 3.4　LVDS 命令句式之出處 —— **查無**（下放包 §4-4）

| 母體 | 量測 | 結果 |
|---|---|---|
| 交付語料 `features/*/delivered/*.xlsx` **17 本** | `^\s*\$\s+\S` 之行 | `$ ` 命令行 **275 條**，其中與 LVDS 相關（`lvds`／`PowerShutDownNotifcation`／`vehicleUpdate`）**0 條** |
| `sources/raw/*sysad*/*.docx` **10 本** | 同三串 | **0 命中** |

> **A-CA27 須更正**：該項記「全語料（三本基準 ＋ 10 本 delivered）掃 `$ ` 起首之指令行，**零命中**」。
> 本輪實測為 **275 條** —— 差異源自 Security 六本於 2026-09-17／18 才落 `delivered/`（CAM-05 量測時未在）。
> **但對本包之結論無影響**：275 條中與 LVDS 相關者仍為 0。
> pilot02 `-001`／`-002` 之 `adb` 句式因而不再是「全案首見」，Security 交付本已有同型 275 例。

**處置**（升級條件 2）：`-069`／`-070`／`-071`／`-072` 之 LVDS 觀察**不造命令** ——
Pre-Condition 寫 `A bus analyzer is connected to the LVDS link between the HU and the RVCM`，
ER 以訊息名與值書寫（`PowerShutDownNotifcation.Power_Down = True／False`）。
`adb` 兩行式只用於 daemon 狀態之觀察（`-058`），其句式沿 pilot02 `-002` 之既有用例。

### 3.5　PENDING 列表

| TC | 欄 | PENDING | DR |
|---|---|---|---|
| `NR1L-RVC-052` | pre ＋ proc ×2 | `BED_EXTENDER` 不在四本 DBC；`Not_Active`／`False` 之 raw 與 label 無來源 | **DR-CAM-f** |
| `NR1L-RVC-064` | pre | `Ignition_Pre_Off` 對 `CmdIgnSts` 之值無來源 | **DR-CAM-i** |

batch02a 之 `U` ＝ 4（`-052` 三處、`-064` 一處）。**batch02a 無新 DR。**

### 3.6　標定值之實測（本輪新查得，供交付前複驗）

| 常數 | 值 | 單位 | 座標 |
|---|---|---|---|
| `T_INITDISPLAY` | **5,0** | `sec` | V33 §1.14.1 `-479`／`-480`／`-483`；V42 `-631`／`-632`／`-635` |
| `Tpower` | **5** | `sec` | V3 §1.14.1 `-583`／`-584`／`-587`；V2 §1.8.13 `-163`／`-162` |

二者使 `-049`～`-057`、`-065`（5 秒）與 `-069`／`-072`（`Hold for 5 s`）之 ER 不再留符號。

---

## §4　結構性回報

1. **HMI Settings List 之三個 Camera 設定項已對上 V33／V42 之內部訊號**（本輪新建立之對應）：

   | 來源訊號 | `Settings` 分頁 | 基礎 label | 品牌 |
   |---|---|---|---|
   | `Rear_Camera_DelayOff.Req` | row 467 | `Rear View Camera Delay*` | RAM ＝ `ParkView Backup Camera Delay` |
   | `Rear_Camera_Grid_Lines_Off.Req` | row 468 | `Rear View Camera Active Guidelines*` | RAM ＝ `ParkView Backup Camera Active Guidelines` |
   | `RVC_Swing_Doors_Check.Req` | row 470 | `Rear View Camera with Rear Door`（無 `*`）| —— |

   **前二者有 `*` 故走品牌軸**；`-067`（637 ＝ RAM）取 RAM label、`-062`／`-063`（2261 ＝ Fiat，欄空）
   回落基礎 label，皆依 R-CAM5(c)′／(e)，未另拆 sibling（各列只勾一個平台，品牌分支單一）。
   **第三者（row 470）之對應為執行層判讀** —— `RVC_Swing_Doors_Check` 於 HMI Settings List 零命中，
   係由 `H` 欄之 `Display Rear View camera while rear cargo…` 推定。
   若審閱認為推定不足，`-068` 可退為 `PENDING`，**歸 Pei**。

2. **四個內部訊號／資料不在任何 DBC**（四本字面掃描零命中）：
   `Rear_Camera_DelayOff.Req`、`Rear_Camera_Grid_Lines_Off.Req`、`RVC_Swing_Doors_Check.Req`、
   `Rear_Camera_Enable.Info`（`SYS-RA-VF551_V3-258` 亦自稱 `internal signal`）。
   本包一律依 §6 以其**可判後果**書寫（影像是否續顯／是否顯示／設定畫面之狀態），不造訊號。
   `Rear_Camera.Data`／`Rear_Camera_Repetition.Data` 同。

3. **`SYS-RA-VF551_V33-420` 之來源含拼寫瑕疵** —— `Rear_Camer_Enable.Info`（少一 `a`），
   與同句之 `Rear_Camera_Enable.Req` 不一致。依逐字原則不更正。
   是否比照 RDF-05 另立一條 RD 反饋，**歸 Pei**（本包未自行加條）。

4. **`SWE-CAM-001` 之 V3／V42／V33 車型歸屬**（本輪之判準，請確認）：
   V3 之母體雖為 376（`P363` anchor，R-CAM11），但當 V42／V33 於同一驗證點**無對應條文**時，
   V3 列承全部三個 Atl-Mi 平台（`-071`／`-072`）；有對應條文時只承 376（`-054`／`-055`）；
   V42 有而 V33 無時承 637 ＋ 376（`-050`）。此與 batch01 `-012`／`-014`／`-016` 之既有作法一致，
   於此明文化供裁。

---

## §5　自報錯誤

1. **CAM-06 上繳之 RULINGS 落實段誤書 `-009`／`-010` 之 Vehicle Model 為「四欄」** ——
   實際落檔一律為**三欄**（HDCC27／DT27／376，637 與 2261 皆 0）。
   json 產物自始正確，錯的只是 `RULINGS.md` 之敘述段；本輪已更正並於該段註明。
2. **`selfcheck_camera.py` 第 1 項之判準經兩次放寬**（§2-1）。兩次皆為下放包裁決所迫，
   非執行層自行鬆綁；但**自檢工具之判準放寬應由 Pei 認可**，於此具名請裁。
3. **A-CA27 之「零命中」已不成立**（§3-4）—— 母體於 CAM-05 之後增加六本。
   本包未改 ANOMALIES 之該列（其為 CAM-05 當時之忠實紀錄），只於此回報；
   是否改記，**歸 Pei**。
4. **`-068` 之設定 label 為推定**（§4-1 第三列），已具名。
5. **`-061` 之「保留」只驗 Off 側** —— 預設即 Off，無法以「設為預設之相反值」構造；
   本包以 `-060`（On 之生效）與 `-061`（Off 之保留）兩列互補涵蓋兩值，
   嚴格之「保留任意值」需第三列，**未生成**，於此回報。
6. **`J` ＝ 5 未改字**（§3-3），依交付語料前例維持逐字，請裁。

---

## §6　未結

| # | 項 | 去處 |
|---|---|---|
| 1 | **DR-CAM-a（高）** `VF617_V5` 缺件 | 補件 |
| 2 | **DR-CAM-f（高）** Atl-Mi 側 DBC（`TRANSM2`／`BED_EXTENDER`）—— `-052` 帶 PENDING | 補件 |
| 3 | **DR-CAM-i（中）** `Ignition_Pre_Off` ↔ `CmdIgnSts` —— `-064` 帶 PENDING；交付前須結 | 補件 |
| 4 | DR-CAM-g（中）Controls screen hop —— `-034`／`-039`／`-046` | 補件 |
| 5 | DR-CAM-h（中）Pop Up List `Camera Not in position` —— `-049`～`-052` 以來源原文書寫 | 補件 |
| 6 | DR-CAM-j（中）SVC 訊號與 `$TGW_DISP_STAT$` 之 SVC 值 | 補件 |
| 7 | **DR-CAM-k（中，新）** VF664 —— batch02c 之 `V2-535` | 補件 |
| 8 | **DR-CAM-l（中，新）** Toro 現行 PROXI 表 | 補件 |
| 9 | §2-1 之 selfcheck 判準放寬（常數錨／合一錨） | Pei 裁 |
| 10 | §3-3 之 `J` 五列豁免是否入 profile | Pei 裁 |
| 11 | §4 之四項（設定 label 推定、V33-420 拼寫、Atl-Mi 車型歸屬判準） | Pei 裁 |
| 12 | §5-3 之 A-CA27 是否改記 | Pei 裁 |
| 13 | **batch02b**（`-002`／`-005`／`-017`，22）與 **batch02c**（`-003`，23） | 下包 |
| 14 | 批③④（LVDS Messaging／Diagnostics／Video Pipeline、HMI Overlays） | 下包 |
| 15 | GC 線六項（`GC_BACKLOG.md`）；`RULINGS.sha.tsv` 未重生 | GC 線 |

---

## §7　本包寫入之檔案（git 未 commit）

```
M  features/camera/RULINGS.md                       R-CAM14(b)(c)／R-CAM10(b)  sha256_16 a7b806e79e991ba0
M  features/camera/framework.md                     VIII.2 四列供稿表          sha256_16 b04b3c9faab600dd
M  features/camera/DECISIONS.md                     6-15 ～ 6-18
M  features/camera/ANOMALIES.md                     A-CA22／A-CA30 CLOSED；A-CA31／A-CA32 RESOLVED
M  features/camera/DATA_REQUESTS.md                 DR-CAM-k／DR-CAM-l
M  features/camera/RD_FEEDBACK.md                   RDF-06                      sha256_16 b7d49575f434d26b
M  features/camera/scripts/selfcheck_camera.py      第 1 項：常數錨／合一錨     sha256_16 b2c4e42e5a068de9
M  features/camera/data/batch02_plan.tsv            合一 ＋ DR-CAM-k            sha256_16 8701a3dcf44e14b3
M  docs/runtime/profiles/FW036_R1L_Camera_Profile.md  新 §8，原 §8/§9 順延      sha256_16 2a8205ed1d8a9272
M  features/camera/generated/batch01/               -023／-030／-031 ＋ LINT_REPORT
M  features/camera/generated/batch01b/              INDEX（13 列）＋ LINT_REPORT
?  features/camera/generated/batch01b/NR1L-RVC-046.{json,md}                    sha256_16 359017ea4220ebaa
?  features/camera/generated/batch02a/              26 × json ＋ 26 × md ＋ INDEX ＋ LINT_REPORT
?  features/camera/generated/batch01/DIFF_CAM06_to_CAM07.md
?  docs/fw036/handoff/up/20260923_CAM-07.md         （本檔）
   features/camera/generated/pilot01/               未動
   features/camera/generated/pilot02/               未動（本輪）
```

`batch02a` 之 26 份 json sha256_16：

```
-047 9c3993508162912f   -048 223f3a4accd7998a   -049 6bd9cc0b942eac41   -050 9a2102123114f6e0
-051 af66ab9fd01cb1c7   -052 e80b8ba1150721d6   -053 acfd9a9d57c3c9b3   -054 05c2d74368c65335
-055 b5dcddb06e8f6eb7   -056 bfc7c241a297875f   -057 b5993eaf1810c6c9   -058 9a727ebf85679783
-059 204be3fa6d30c248   -060 312534db35ac9d54   -061 7ebb2ceb78b54379   -062 6ef8ebb716c0e197
-063 3e6c57107268abe6   -064 63e7ccd7e014330f   -065 705678eebebe4dd9   -066 1162fc49fa64dfbe
-067 d2f6419b31d7ab3b   -068 2612d09b7470383e   -069 4a0f6134c5a41db9   -070 b4e7ed1514e092dd
-071 65b68c1b48a988b0   -072 dfc4dd1a0d46d673
```

### 複驗指令

```bash
python3 scripts/rulings_hash.py --target features/camera/RULINGS.md --out /tmp/cam.sha.tsv
python3 features/camera/scripts/selfcheck_camera.py \
        features/camera/generated/pilot02 features/camera/generated/batch01 \
        features/camera/generated/batch01b features/camera/generated/batch02a
python3 features/camera/scripts/lint_batch.py <暫存目錄> features/camera/generated/batch02a
python3 - <<'EOF'
import hashlib
s = open('features/camera/framework.md', encoding='utf-8').read()
print(hashlib.sha256(s.replace('8569aa484b552b7c', '0'*16).encode()).hexdigest()[:16])
EOF
```
