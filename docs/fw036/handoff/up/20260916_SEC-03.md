# 上繳包 SEC-03 —— 形制量測 ＋ R-SEC15 ＋ lint 補強 ＋ pilot02（12 TC）

對應下放包：`down/20260916_SEC-03.md`（Pei 准 review §四 九項）。
**禁區遵守**：git 未動（且本機 git 仍損毀，見 §9）；`down/`／`up/` 既有檔未動；其他 feature 未動；
canon 未動；`framework.md` LOCKED 未動；`pilot01/` 目錄保留未動（R-TM13）；只產 pilot02 之 12 TC。
`lint036.py` 之修改限於 `check_security_channel()`／`check_security_source()`／`FEATURE_CHECKS`／`FEATURE_EXEMPT`。

---

## 1　§1 欄位形制量測（**先行，pilot02 寫入前完成**）

母體：**SWC 0708**（R-1 v2 基準本，`Test Case Specification 測試用例規範`，286 資料列）
＋ **pm_29**（389 資料列）。`Counter` 逐字。

| 欄 | SWC 0708 | pm_29 | 兩本 | **定之寫法** |
|---|---|---|---|---|
| `Test Case Priority` | `P1` **190**／`P2` 68／`P0` 27／空 1 | `P0` **260**／`P1` 78／`P3` 42／`P2` 9 | 格式一致、分佈相反 | **`P0`~`P3`** |
| `Test Case Design Methods` | 中英雙語全文 **7 值** | 中英雙語全文 **4 值** | 一致 | **`下拉選單` sheet 之雙語全文逐字** |
| `Input Test Data` | `NA` **285／286**（空 1）| `NA` **389／389** | 一致 | **一律 `NA`** |
| `Test Case Reference ID` | `NEW` **285／286**（空 1）| `NEW` **389／389** | 一致 | **`NEW`** |
| `Estimated Test Time` | 空 **286／286** | 空 **389／389** | 一致 | **留空** |
| `Test Item` 上半（20 列抽樣）| 需求句：`HW supplier shall notify the VolumeDown signal via the VHAL interface.` | 條件句：`All TLM, AMP/ICS/DTV functionalities are available.` | **不同型** | 見下 |
| ER 內字面引號 | `"…"` **50**／`'…'` **0**／反引號 **0** | 三者皆 **0** | 一致 | **只用 `"…"`** |

**`Test Case Design Methods` 之逐字全值**（SWC 0708）：

```
86  狀態轉換 (State Transition Testing)
70  決策表 (Decision Table Testing)
50  負向測試 (Negative / Invalid)
48  功能測試 (Functional based ; no specific technique)
20  基礎故障注入 (Fault Injection Lite)
10  邊界值分析 (Boundary Value Analysis, BVA)
 1  情境 / 用例 (Scenario / Use Case Testing)
 1  (空)
```

**七值逐字落在 R-G1 母本 `下拉選單` sheet 之 9 值內**（現查），可證該欄取自下拉選單、非自由輸入。

### 1-1　§7 第一項升級條件之核對

「§1 量測兩本互斥且 SWC 0708 該欄為空」—— **七欄皆非此情形**：
六欄兩本一致；`Test Item` 上半雖兩本不同型，但 **SWC 0708 該欄不為空**，且
**R-SEC15(f) 已直接裁定**取 037 `Requirement Description` 首句（與 SWC 之需求句同族）。
**條件不成立。**

### 1-2　兩處須具名（非形制，屬判斷）

1. **Priority 之逐列值未定** —— 兩本**格式**一致（`P0`~`P3`）而**分佈相反**
   （SWC 以 `P1` 為主、pm_29 以 `P0` 為主）。那是各 feature 之風險判斷，不由「多數值」推得。
   pilot02 一律用 **`P1`**（SWC 0708 多數值）。**逐列 priority 之 rubric 待 Pei 裁。**
2. **`Test Item` 上半兩本不同型** —— 已由 R-SEC15(f) 裁定，量測結果支持之。

量測結果已寫入 `profile` 之新增 **3.0 節「欄位形制」**；量測器
`features/security/scripts/measure_style.py`（唯讀，可重跑）。

---

## 2　§2 R-SEC15 落檔

**取號現查（R-G23）**：`RULINGS.md` 既有錨點至 `R-SEC15` 前為 `R-SEC1`~`R-SEC14` ＋ `R-SEC5(a)`
＋ `R-SEC4(a)`，無 `R-SEC15`。

| 條號 | body sha8 | body_kind | 行數 |
|---|---|---|---|
| **R-SEC15** | `fe4a0ca4` | fenced | 28 |

`rulings_hash.py --gate` **通過**；R-SEC1~R-SEC14 之 body sha8 **逐條不變**。

**(e) 與 (d) 已由 §1 量測證實**（見 1 節表）；**(j) 已於 `lint036.py` 落實**（3-1）。

---

## 3　§4 lint 補強

### 3-1　變更（只動指定面）

| # | 變更 | 依據 |
|---|---|---|
| 1 | 新增 `FEATURE_EXEMPT: dict[str, list[str]] = {"security": ["I-cross"]}`，`check_order()` 自 `CHECK_ORDER + PROFILE_CHECKS` 扣除之 | **R-SEC15(j)** |
| 2 | `lint_sheet()` 之 `if profile:` 改為 `if "I-cross" in enabled:` | 同上 |
| 3 | `check_security_channel()` 加四判項（§4 之 1~4）| SEC-03 §4 |
| 4 | `check_security_channel()` 之編號步驟檢查**略過整行 PENDING 步驟** | **R-SEC15(b)**（觸發步驟整行寫 PENDING）|
| 5 | ER 行為整行 PENDING token 者**免觀察手段檢查** | **R-SEC15(i)** |

`FEATURE_EXEMPT` 為新結構 —— 其必要性：`I-cross` 在 `PROFILE_CHECKS` 內，
**無現成機制可對單一 feature 關閉**；沿用 `FEATURE_CHECKS` 之對稱設計。

### 3-2　回歸（§7 第二項升級條件）

| 呼叫路徑 | base sha16 | now sha16 | 判 |
|---|---|---|---|
| `lint036.py pm_29.xlsx`（無 profile）| `e2ffac8eb9fd6838` | `e2ffac8eb9fd6838` | ✅ **byte 相同** |
| `--profile camera pm_29.xlsx` | `0bd777a9334105d1` | `0bd777a9334105d1` | ✅ |
| `--profile power pm_29.xlsx` | `45b56a6eeca86a29` | `45b56a6eeca86a29` | ✅ |

`tests/test_lint036_z.py` **27 項全過**。
`check_order()` 驗證：`I-cross` **不在** `security`、**仍在** `power`；`SC`／`SS` 只在 `security`。
**升級條件不成立。**

### 3-3　pilot01 重跑（補強有效性之證據）

```
SC=0  →  SC=49
```

§4 預期「≥ 14」，實測 **49**。逐類成因即審閱二-1／二-2 所指者
（反引號 26 處、台帳代號 11 處、ER 指令未見於同步驟等）。

---

## 4　§3 EXEC_ASSETS／trace 更新

### 4-1　`EXEC_ASSETS.md`（sha16 `e9a8e9fa752fa140`）—— 6 → **9** 項

| 新增 | 內容 |
|---|---|
| **`X-e-2`** | **wrong-subject 憑證之 DUT 側觸發手段**。現況：`Test_Items.txt` 15 條**無**對應方法 |
| **`X-e-3`** | **wrong-OID 憑證之 DUT 側觸發手段**。同上 |
| **`X-f-2`** | **KeyInstall status test runner**（Binder `getStatus()` 之可執行入口）|

**`X-f-2` 之候選檔（§6-3；只列檔名，未拆 zip）**：

| 檔 | 大小 | sha16 | 外部路徑 |
|---|---|---|---|
| `KeyInstall_IntegrationTests.zip` | **76,461 B** | `de46aba5dc9b6e9b` | `2_Architecture/CCVR/To Validation team/Secure Log CS.212/StevenJSHsu/` |

**是否含該 runner 未知**（未拆）。同目錄另有 `CertProvider_IntegrationTests.zip`（3,118 B，
`8e97a0413712aaf2`），與 `Cert Val CS.98/IntegrationTests.zip` **sha 相同**（同一檔兩處）。

### 4-2　`trace_summary.md` 4b（sha16 `00e577021eb8d1e3`）

`channel_feasible` 改為每 SWE1 **兩值 `pos/neg`**：

| 值 | 列數 |
|---|---:|
| `Y/Y` | 40 |
| **`Y/N`** | **2**（`SWE1-CertProvider-002`、`SWE1-CertProvider-011`）|
| `部分/部分` | 8 |
| `N/N` | 20 |

`Y/N` 兩列之 `channel_feasible_why` 加註「負向分支 N —— DUT 側無觸發手段（R-SEC15(b)）」。
`apk_pairing.tsv` **未動**（§3 明令）。

---

## 5　§5 pilot02 —— 12 TC

產物：`features/security/sandbox/pilot02/security_pilot02.xlsx`（sha16 `1d1f71c728a6740e`）
＋ 12 個 `NR1L-*.json`。產生器 `gen_pilot02.py`（sha16 `671698cf9cc75a20`）。
寫檔走 `surgical_save`；zip 成員 48 → 48，DV 計數逐 sheet 相同（`sheet6.xml` 之 x14 DV 保留）。

### 5-1　lint 報告（`--profile security`）

`docs/fw036/lint_reports/security_pilot02__security_1d1f71c7_20260916.md`

```
A=0  B=0  C=0  D=0  E=0  F=0  G=0  H=0  I=0  I-sibling=0
J=0  K=0  L=0  M=0  N=0  P=0  Q=0  R=0  T=0  U=14  V=0
W=2  X=0  Y=0  SC=0  SS=0
```

**門檻 `P`／`X`／`SC`（補強後）／`SS` 全綠**；`I-cross` 已豁免（不出現於序列）。

| 非 FAIL 項 | 行計 | 說明 |
|---|---:|---|
| `U` | **14** | PENDING 佔位計數，逐筆歸屬見 5-3 |
| `W` | 2 | `NR1L-CP-003`／`-004` 之 `identical to` —— **該措辭即 §5 #3 所指定**（`identical to the subject name in …`）；負向路徑無數值可比，**建議判可** |

> 首輪曾得 `R=1`（`NR1L-ECUC-001` Pre-Cond「多條件並列於同一行」），已拆為兩行，現為 0。

### 5-2　12 TC 之 channel／observe

| # | TC ID | SWE1 | 步數 | channel | observe | PENDING |
|---:|---|---|---:|---|---|---:|
| 1 | `NR1L-CP-001` | SWE1-CertProvider-001 | 3 | HOST／ADB | HOST／RC／LOG | 0 |
| 2 | `NR1L-CP-002` | SWE1-CertProvider-001 | 3 | HOST／ADB | HOST／RC／PENDING | 1 |
| 3 | `NR1L-CP-003` | SWE1-CertProvider-002 | 3 | HOST／ADB | HOST／RC | 0 |
| 4 | `NR1L-CP-004` | SWE1-CertProvider-002 | 3 | HOST／PENDING | HOST／PENDING | 2 |
| 5 | `NR1L-CP-005` | SWE1-CertProvider-004 | 2 | ADB／PENDING | FILE／PENDING | 2 |
| 6 | `NR1L-CP-006` | SWE1-CertProvider-011 | 3 | HOST／PENDING | HOST／PENDING | 2 |
| 7 | `NR1L-CP-007` | SWE1-CertProvider-011 | 2 | HOST／PENDING | HOST／PENDING | 2 |
| 8 | `NR1L-ECUC-001` | `SYSAD_SEC_ECUCERT_ECUCERT_SRV_EXPORTCSR_INTF` | 4 | **UDS**／ADB／HOST | UDS／FILE／HOST | 0 |
| 9 | `NR1L-KI-001` | SWE1-KeyInsyall-011 | 2 | ADB／PENDING | FILE／PENDING | 2 |
| 10 | `NR1L-KI-002` | SWE1-KeyInsyall-011 | 2 | ADB／PENDING | FILE／PENDING | 2 |
| 11 | `NR1L-SAM-001` | SWE1-SAM-0004 | 4 | ADB／**PHYS** | FILE／LOG／PENDING | 1 |
| 12 | `NR1L-SAM-002` | SWE1-SAM-0007 | 3 | HOST／ADB | HOST／RC | 0 |

七類通道中 **ADB／UDS／HOST／PHYS 四類**出現；`CAN`／`UI` 仍未出現（不在取樣範圍）。

### 5-3　PENDING 逐筆歸屬（14 筆，**全部可歸 X-**）

| TC | 欄 | token | 歸屬 |
|---|---|---|---|
| `NR1L-CP-002` | er 3 | `PENDING: X-h log keyword` | **X-h** |
| `NR1L-CP-004` | proc 3 | `PENDING: X-e wrong-subject certificate + trigger` | **X-e-2** |
| `NR1L-CP-004` | er 3 | `PENDING: X-h log keyword` | **X-h** |
| `NR1L-CP-005` | proc 2 | `PENDING: X-e revoked certificate + trigger` | **X-e** |
| `NR1L-CP-005` | er 2 | `PENDING: X-h log keyword` | **X-h** |
| `NR1L-CP-006` | proc 3 | `PENDING: X-e OID certificate + trigger` | **X-e-3** |
| `NR1L-CP-006` | er 3 | `PENDING: X-h log keyword` | **X-h** |
| `NR1L-CP-007` | proc 2 | `PENDING: X-e OID certificate + trigger` | **X-e-3** |
| `NR1L-CP-007` | er 2 | `PENDING: X-h log keyword` | **X-h** |
| `NR1L-KI-001` | proc 2／er 2 | `PENDING: X-f KeyInstall status test runner` | **X-f-2** |
| `NR1L-KI-002` | proc 2／er 2 | `PENDING: X-f KeyInstall status test runner` | **X-f-2** |
| `NR1L-SAM-001` | er 3 | `PENDING: X-h log keyword` | **X-h** |

§5 預期「≥ 6，全部可歸 X-e／X-f／X-h」—— 實測 **14**，**全部可歸**。
**§7 第四項（PENDING 有無法歸 X-／DR- 者）不成立。**

---

## 6　§7 第三項 —— R-SEC15(a)~(i) 逐條自檢

檢查器 `features/security/scripts/selfcheck_r_sec15.py`（sha16 `4c49d0836a00a8e4`），
對 `pilot02/*.json` 施檢（json 與 xlsx 同源於 `render()`）。

| 條 | 判項 | 命中 | 檢查方法 |
|---|---|---:|---|
| (a) | 跨場景字串移植 | **0** | ECU 專屬 log 字串（`ERR_UNABLE_TO_GET_ISSUER_CERT_LOCALLY`）出現於 ER 而 Procedure 無 `ecuCert*` 方法者 |
| (b) | 觀察不得代替觸發 ＋ 每步須有通道 | **0** | 見 6-1 之兩類分流；另逐編號步驟查其後是否有 `$` 行／實體操作／整行 PENDING |
| (c) | 台帳代號不入四欄 | **0** | 四欄逐行 regex `X-[a-z]｜DR-SEC-[a-z]｜R-SEC\d｜A-SE`，排除 PENDING token 起首之行 |
| (d) | 字面值只用雙引號 | **0** | 四欄逐行查反引號／單引號包字串；另查角括號內容 > 4 token 者（非佔位）|
| (e) | `Input Test Data` = `NA` | **0** | 逐 TC 比對 `input` 欄 |
| (f) | `test_item` 上半 = Description 首句 | **0** | 上半 token 數 ≤ 50 且以句號結尾（完整句，非 Title）|
| (g) | apk 方法須有配對 | **0** | 自 Procedure 抽 `#<method>`，比對 `apk_pairing.tsv` 該 SWE1 之方法集合 |
| (h) | DID 值不入 ER | **0** | ER 逐行查 `62 FF 02` 與 `DID` 字樣 |
| (i) | `PENDING` 整行 | **0** | 四欄逐行：含 `PENDING:` 而非以其起首者 |

**合計 0 處。§7 第三項不成立。**

### 6-1　**(b) 之判準曾過寬，已收斂並反證（自報）**

首版 (b) 把「ER 含任何 DUT 側觀察標記而 Procedure 無 DUT 側觸發」一律判違，得 **3 處**：
`NR1L-CP-005 ER1`、`NR1L-KI-001 ER1`、`NR1L-KI-002 ER1`。

逐筆核之，**三者皆非違規**：
`CP-005 ER1` 為 `adb shell ls` 列出**預裝**之 `"CRL.r0"`；
`KI-001`／`KI-002 ER1` 為 `od -t x1` 讀**由 Pre-Condition 建立**之 `installstate` 值 ——
三者皆是**狀態讀取**，其觀察由**該步驟自身之讀取指令**產生，本無「須先觸發」之問題。

→ (b) 之施檢面收斂為兩類：

| 類 | 定義 | 是否需 DUT 側觸發 |
|---|---|---|
| **出現型** | 斷言「某事件被產生」：log 條目出現／不出現、UDS 回應、runner 結果 | **需要** |
| **狀態讀取型** | 斷言某檔案／目錄／狀態值之現況（`ls`／`cat`／`od`）| 不需要（由其步驟自身之讀取指令產生）|

### 6-2　**收斂後之反證 —— 以同一檢查器掃 pilot01**

為證收斂非「放寬到全過」，以**同一檢查器**掃 `pilot01/*.json`：

| 條 | pilot01 命中 | 對應審閱之指摘 |
|---|---:|---|
| (a) | **1** | 審閱 二-1：`NR1L-CP-002` 把 ECU 場景 log 字串移植到 FOTA 場景 |
| (b) | **2** | 審閱 二-3：`NR1L-CP-004` Proc 3、`NR1L-CP-006` Proc 2 之無觸發觀察 |
| (c) | 11 | 台帳代號 `X-e`／`X-f`／`X-g` 入 Pre-Cond／Input |
| (d) | 26 | 反引號 |
| (e) | 10 | `Input Test Data` 非 `NA` |
| (f) | 10 | `test_item` 上半用 Title 而非 Description 首句 |
| (g) | 2 | CP-011 硬配 `fotaMcpuCertTestNormalFlow` |
| (h) | 1 | `62 FF 02` 入 ER |
| (i) | 3 | PENDING 與散文混寫 |
| **合計** | **66** | — |

**(a) 1 處與 (b) 2 處與審閱二-1／二-3 所指者逐筆相同** —— 判準仍能抓，收斂只去掉了誤報。

---

## 7　升級條件逐項核對（§7）

| # | 條件 | 實測 | 判 |
|---|---|---|---|
| 1 | §1 量測兩本互斥且 SWC 0708 該欄為空 | 六欄一致；`Test Item` 雖不同型但 SWC 不為空且已由 R-SEC15(f) 裁定 | **不成立** |
| 2 | lint 回歸 sha 改變 | 三路徑 byte 相同 | **不成立** |
| 3 | pilot02 任一 TC 出現 (a)~(i) 之違規 | 逐條自檢 **合計 0 處**，附檢查方法（§6）| **不成立** |
| 4 | PENDING 有無法歸 X-／DR- 者 | 14 筆**全部**可歸 X-e／X-e-2／X-e-3／X-f-2／X-h | **不成立** |

**四項皆不成立，本包完成上繳，不升級。**

---

## 8　產物清單（sha256 前 16 碼）

| 檔 | sha16 |
|---|---|
| `features/security/RULINGS.md` | `72548eb7393217ca` |
| `features/security/EXEC_ASSETS.md` | `e9a8e9fa752fa140` |
| `features/security/data/trace_matrix.tsv` | `96549815023a0c42` |
| `features/security/data/trace_summary.md` | `00e577021eb8d1e3` |
| `features/security/scripts/build_trace_matrix.py` | `a130d1e79faa6d92` |
| `features/security/scripts/measure_style.py` | `edbcd5bfd37b9712`（新增）|
| `features/security/scripts/gen_pilot02.py` | `671698cf9cc75a20`（新增）|
| `features/security/scripts/selfcheck_r_sec15.py` | `4c49d0836a00a8e4`（新增）|
| `features/security/sandbox/pilot02/security_pilot02.xlsx` | `1d1f71c728a6740e`（＋12 json）|
| `docs/runtime/profiles/FW036_R1L_Security_Profile.md` | `9836e5991a754a4b`（＋3.0 欄位形制節，307 行）|
| **`scripts/lint036.py`** | `0626836bda624676` |

**未動**：`pilot01/`、`framework.md`、`feature.yaml`、`apk_pairing.tsv`、`diag_items.tsv`、
`step_assets.tsv`、`batch_order.tsv`、`layer2_assign.tsv`、`peer_review_rows.tsv`、
`DECISIONS.md`、`DATA_REQUESTS.md`、`sources/MANIFEST.tsv`、canon、其他 feature、git。

---

## 9　自報錯誤與偏差

| # | 事項 | 性質 | 處置 |
|---|---|---|---|
| 1 | **本機 git 仍損毀** | 環境 | 本輪未執行任何 git 操作。需 Pei 執行 `sudo xcodebuild -license`。SEC-01～SEC-03 之產物皆在工作樹，`3a16c5f` 之後未 commit |
| 2 | (b) 自檢判準首版過寬（3 處誤報）| 執行層判準設計 | 已收斂為出現型／狀態讀取型兩類，並以 pilot01 反證（§6-1、6-2）|
| 3 | pilot02 首輪 `R=1`（Pre-Cond 多條件並列）| TC 書寫瑕疵 | 拆兩行，現 0 |
| 4 | 新增 `FEATURE_EXEMPT` 結構 | **下放包未明令之新結構** | `I-cross` 在 `PROFILE_CHECKS`，無現成機制可對單一 feature 關閉；沿用 `FEATURE_CHECKS` 之對稱設計。回歸 sha 為證（3-2）|
| 5 | `check_security_channel()` 加「略過整行 PENDING 步驟」 | §4 未列，但 R-SEC15(b) 蘊含 | 否則 R-SEC15(b) 所令之 PENDING 觸發步驟必被 `SC` 判 FAIL，兩條文互斥 |
| 6 | `NR1L-ECUC-001` 之 `test_item` 上半取 **英文子句** | **越出 R-SEC15(f) 字面** | 該列 037 `Requirement Description` 為 `產生 CSR\n\nGenerate CSR message.`（中英並列）；逐字全取會觸發 lint `K`（CJK 字元）。取其英文句 `Generate CSR message.`，**仍為原文之逐字子字串**。請 Pei 確認 |
| 7 | `Priority` 逐列一律 `P1` | 量測只定格式，未定逐列值 | 取 SWC 0708 多數值；rubric 待 Pei 裁（1-2）|
| 8 | `W=2` 之 `identical to` | 措辭即 §5 #3 所指定 | 待人裁非 FAIL，建議判可 |
| 9 | pilot02 未覆蓋 `CAN`／`UI` 兩類通道 | 取樣範圍所限 | 具名；建議 batch 1 正式產出時補（SAM-0013／0017 為 `CAN`、ECUCert Dealer App 為 `UI`）|
| 10 | `CertProvider_IntegrationTests.zip` 與 `Cert Val CS.98/IntegrationTests.zip` **sha 相同** | 查證附帶發現 | 同一檔兩處（3,118 B，`8e97a0413712aaf2`），皆未拆 |
