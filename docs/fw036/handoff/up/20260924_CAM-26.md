# 上繳包 CAM-26 —— R-CAM19 追溯補齊：原零 TC 十列各出 TC、兩本 dry-run 重生

對應下放包：`docs/fw036/handoff/down/20260924_CAM-26.md`。
執行層 session：新開（`DELIVERY_STATUS.md` §9 之「新增 TC 須有下放包明文指派」即本包）。日期 2026-09-24。

**一句話**：§1 立 **R-CAM19**（sha8 `8f741740`），R-CAM10／R-CAM16／R-CAM16(c) 依 R-TM13 加刪除線（fenced 指紋不變）；
§2 原零 TC 十列各出一列 —— A 本 `batch06/` **4 列**（`NR1L-RVC-246`～`-249`）、B 本 `b08/` **6 列**（`NR1L-RVCHMI-211`～`-216`），
**全案 465 TC**；§3 A dryrun4 **249 列**／B dryrun3 **216 列**，D 欄佔位 10 → **0**，**列數 ＝ TC 數**，
追溯 **A 25/25、B 203/203**；lint／selfcheck **ERROR 類全 0**。**兩項升級條件皆不成立**。
與下放包有**五處事實出入**（§6-1），產物依來源事實出列，角度提請確認（DECISIONS **6-87** [PROPOSED]）。
未寫 `delivered/`、未 tag；既有 455 列 `generated/` **0 改動**。

---

## §1　裁定落檔

### 1-1　R-CAM19

`features/camera/RULINGS.md`（sha256_16 `014d13c64e4c67d0`）R-CAM18 之後。**R-G23 取號現查**：本檔錨點
`R-CAM1`～`R-CAM18`（`rulings_hash.py` 實測 28 錨點），全 repo `R-CAM19` 之出現只在本下放包與
`up/20260923_CAM-17.md` 之「0 命中」量測句 → `{live}` = **19 成立**。

| 條 | sha8（全本體）| fenced 本體 sha8 |
|---|---|---|
| **R-CAM19** | **`8f741740`** | `a1727b36` |

條文逐字照錄下放包 §1 之 code block；其下另附取號、「(a)」之指涉、執行層落實三段（計入 sha8）。

### 1-2　R-TM13 之刪除線

「R-CAM10(a)」「R-CAM16(a)」指兩條之**原條本文**（兩條皆無 `(a)` 字樣）。刪除線依 R-CAM5(c) 前例落**原條標題之下**，
原 fenced 條文逐字不動 —— `rulings_hash.py` 前後比對（全 28 錨點）：

| 條 | 全本體 sha8 前 → 後 | fenced sha8 |
|---|---|---|
| R-CAM10 | `e50f2445` → `e3424ce3` | `1ef47db1`（**不變**）|
| R-CAM16 | `440b422c` → `0aa13535` | `cb146bbb`（**不變**）|
| R-CAM16(c) | `d9212990` → `0fc02c01` | `65b2946d`（**不變**）|
| 其餘 25 錨點 | 不變 | 不變 |

R-CAM16 之下另附**執行層更正**：其既有「執行層落實」段寫 `SWE-CAM-007` 之唯一來源為 `V42-216`、`-013` 之三來源「全數委派 `-004`」，
皆與 `batch03_plan.tsv` 不合（實為 `V42-210`；`-592`／`-593` 委派 `-006`、`-595` 委派 `-004`）。逐字不改，更正記 **A-CA38**。
R-CAM16(b) 未加註（下放包未令；R-CAM19(c) 已明載其修訂）。

### 1-3　DECISIONS／RDF／DR／ANOMALIES

| 檔 | 項 |
|---|---|
| `DECISIONS.md`（`64522d7eb908e1f2`）| **6-86**（R-CAM19 之落實，七點）；**6-87** `[PROPOSED]`（與下放包之出入六項，§6-1）；6-52／6-73 下加「作廢（R-CAM19(e)）」註 |
| `RD_FEEDBACK.md`（`78f8468e46f317d7`）| **RDF-12** 改題「`SWE1-RVC-069` 自陳 N/A 但仍須追溯」，原題附註保留；總表同步 |
| `DATA_REQUESTS.md`（`8dd57615e31bcfee`）| **DR-CAM-t** 新開：LVDS `diagnosticRequest`／`diagnosticResponse` 之訊息定義（`-247`）|
| `ANOMALIES.md`（`8dfa0b1832f2888d`）| **A-CA35／A-CA36** → RESOLVED；**A-CA38**（下放包與既有紀錄之出入）、**A-CA39**（`-087` 之車型）新登 |

---

## §2　補生成

### 2-1　清單（每需求 → TC → 驗證角度）

| 需求 | TC | Test Set | 驗證角度（一句）| 同源／被引 | 車型 |
|---|---|---|---|---|---|
| `SWE-CAM-007` | `NR1L-RVC-246` | LVDS Messaging | `V42-210` 所載「影像經 LVDS 自 RVCM 送達」之**接收**（bus analyzer）；縮放由 `-200` 驗 | 同源 `-006`：`-200` | 637 |
| `SWE-CAM-013` | `NR1L-RVC-247` | Diagnostics | 讀 DTC 時 LVDS 上之 `diagnosticRequest`／`diagnosticResponse`；訊息內容 `PENDING: DR-CAM-t`；錨 `V42-593` | 同源 `-006`：`-211`／`-212`；`-004`：`-195` | 637 |
| `SWE-CAM-024` | `NR1L-RVC-248` | AUX Camera | **R-CAM19(d) 六欄佔位** `PENDING: DR-CAM-a`（J／K／L／M／N ＋ AH）；上半取 037 Description | — | R-CAM18(a) 五款 |
| `SWE-CAM-025` | `NR1L-RVC-249` | HMI Overlays | App 側：**影像顯示中**收到 bed extender active → 影像移除＋疊層（狀態轉換，與 `-032` 觸發序相反）| 同源 `-020`：`-032`／`-033` | 2261 |
| `SWE1-RVC-069` §27.3.2 | `NR1L-RVCHMI-211` | AUX Camera Access | 父題 §27.3／鄰列 §27.3.1.x 之 R1 High 硬體條件 → `Enable Wireless Cameras` 設定之**提供** | 路徑沿 `-063` | 五款 |
| `SWE1-RVC-086` §27.7.3 | `NR1L-RVCHMI-212` | AUX Camera Access | App Drawer 入口 × §27.5.2（設定 OFF、無投影 → 自動啟用、無彈窗）| 被引 `-071` | 五款 |
| `SWE1-RVC-091` §27.8.5 | `NR1L-RVCHMI-213` | AUX Camera Access | 後視影像 `More Aux` 入口 × §27.6.1（投影中 → `PU1518`）| 被引 `-072` | 五款 |
| `SWE1-RVC-102` §28.6.1.2 | `NR1L-RVCHMI-214` | Camera View Switching | R 檔 `More Cams` 入口 × §27.5.1（休眠 → `connecting`，`PENDING: DR-CAM-h`）| 被引 `-070` | 四款（Toro 0）|
| `SWE1-RVC-136` §34.4.2 | `NR1L-RVCHMI-215` | AUX Camera Access | 沿母列（父題亦全等，來源無可分辨之文字）| 母列 `-086` | 五款 |
| `SWE1-RVC-139` §34.6.1 | `NR1L-RVCHMI-216` | AUX Camera Access | 沿母列之否定命題；父題相異（`Backup and Cargo Cam`）記於 reasoning | 母列 `-087` | HDCC27／DT27 |

每列 reasoning 具名「R-CAM19 追溯補齊」（或 (c)／(d)）與同源／被引 TC ID。計畫檔 `data/batch06_plan.tsv`（`a6529856489cf02a`）、
`data/b08_plan.tsv`（`ef5e43dd3861a8d3`）；兩目錄各含 json ＋ md ＋ `INDEX.md` ＋ `LINT_REPORT.md`。

`coverage.tsv`（`7e1ae30615687aad`）四列改 `PRODUCED`／`batch06`／`tc_count = 1`，零 TC **0**；
`coverage_b.tsv`（`78b47efbdbf28d45`）六列改 `PRODUCED`、`batch` 加 `;b08`，leaf 無 TC **0**。

### 2-2　lint 與 selfcheck

```
lint036 --profile camera（lint_batch.py 暫存簿）
  batch06（4 列）  ERROR 類全 0｜U 11（-247 2／-248 6／-249 3）／I-cross 4
  b08（6 列）      ERROR 類全 0｜U 1（-214）／I-cross 6
selfcheck_camera.py  batch06＋b08（TC 10 筆）  ERROR 級十項全 0｜共引錨 3／缺件佔位 1｜第 10 項 WARN 1（-216 negative，同母列 -087）
```

### 2-3　`selfcheck_camera.py` 之修訂（sha256_16 `69d5da4cf0e5db5f` → `f756218e34dbe057`）

**修訂前**對新 10 列實跑：第 1 項**命中 3**（`-246`／`-247`／`-249`：「承接列為 `-006`／`-006`／`-020`」）、反查失敗 1 ＋ 第 4 項無來源 1 ＋ 第 8b 項 1（皆 `-248`）
—— 即 R-CAM10 之委派判準本身寫在第 1 項內，R-CAM19(b)／(d) 撤銷後必須同步。修訂只加兩個**另計類別**，不放寬其餘判準：

| 類 | 判準 | 不判違反之要件 |
|---|---|---|
| **共引錨**（R-CAM19(b)）| 單錨 TC 之 `req_id` ≠ 承接列 | `req_id` **為 037 引用該來源之列之一**（`layer3_a_vf_chapters.tsv`）；否則仍命中 |
| **缺件佔位**（R-CAM19(d)）| `specification_reference` **每一行**皆以 `PENDING: DR-` 起首 | 免第 1 項反查、第 4 項子序列、第 8b 項；**第 2 項（≥ 2 步）與第 8a 項（行首）照查** |

**反例實測**：把 `-246` 之 `req_id` 改為不引用 `V42-210` 之 `SWE-CAM-001`（scratchpad 副本）→ 第 1 項**命中 1**、exit 1。
**全案回歸**（二十二目錄，`pilot01` 除外）：TC **465**；錨 459 → **468**（＋9）；常數錨 2；共引錨 3／缺件佔位 1 **只出現在新列**；
ERROR 十項全 0；第 10 項 40 → **41**；exit 0。

---

## §3　兩本 dry-run

### 3-1　檔

```
features/camera/sandbox/a_dryrun/FM-WI-FSM-036-A01 STLA 測試用例規範與結果_SWQT STLA Test Case Specification & Result_SWQT_RearViewCamera_A_20260924_dryrun4.xlsx
sha256  a4ad6cf3112d5e31734ecae13c5ebd699e9b1ff716b8e7e4d856d5f5863c8ead
資料列 249（TC 249，D 欄佔位 0）

features/camera/sandbox/b_dryrun/FM-WI-FSM-036-A01 STLA 測試用例規範與結果_SWQT STLA Test Case Specification & Result_SWQT_RearViewCamera_B_20260924_dryrun3.xlsx
sha256  1d8d21b9945d4828fc887a965ddc4c8da3f702233e3abaac63ebf1ca0712f032
資料列 216（TC 216，D 欄佔位 0）
```

組法：以前一候選（A dryrun3／B dryrun2）為底，**列序沿底稿**（已依 R-G42 一升冪），每一 D 欄佔位列**原位**換成同 `req_id` 之新 TC，
**全部 TC 列之內容一律自 json 重寫**，並逐格比對底稿。封面只改日期（`2026-09-24`）與版次字樣。

**既有列之逐格差異**：B 本 **0 格**；A 本 **1 格** —— `NR1L-RVC-245` 之 `AH`（`None` → 6-85 之 Remarks）。
該 Remarks 為 **CAM-25 已寫入 json 而 A dryrun3 未重生**之殘差（CAM-25 下放包令「A 本不動」），本輪一併帶入，非本包之改動。

### 3-2　lint 全表

```
A dryrun4  行計 A=0 B=0 C=0 D=0 E=0 F=0 G=0 H=0 I=0 I-sibling=0 J=37 K=0 L=0 M=0 N=0
           P=0 Q=0 R=0 T=0 U=65 V=0 I-cross=249 W=0 X=3 Y=0 Z=0
           報告 docs/fw036/lint_reports/RearViewCamera_A__camera_a4ad6cf3_20260924.md
B dryrun3  行計 A=0 B=0 C=0 D=0 E=0 F=0 G=0 H=0 I=0 I-sibling=0 J=0 K=0 L=0 M=0 N=0
           P=0 Q=0 R=0 T=0 U=45 V=0 I-cross=216 W=0 X=23 Y=0 Z=0
           報告 docs/fw036/lint_reports/RearViewCamera_B__camera_1d8d21b9_20260924.md
```

**ERROR 類全 0**；`Z` 全綠。與前版之差只在 `U`（A 54 → 65 ＝ ＋11、B 44 → 45 ＝ ＋1，即新列之 PENDING）與 `I-cross`（＝列數）；`J`／`X` 不變。

### 3-3　sheet 級

| 項 | A dryrun4 | B dryrun3 |
|---|---|---|
| D 欄升冪（R-G42 一）| 成立 | 成立（`(母號, 子號)`）|
| D 欄佔位列（F 欄空）| **0** | **0** |
| 序號 `B` 連續 1..n | 成立 | 成立 |
| 相異 TC ＝ 列數 | 249 ＝ 249 | 216 ＝ 216 |
| `N` 欄多行 | 4（同前）| 0 |
| `AH` remarks | 11（前 9 ＋ `-245` ＋ `-248`）| 8（同前）|
| VM 七欄非 0/1 | 0 | 0 |
| Test Set | **10 組**（新增 `AUX Camera` 1）| 8 組（`AUX Camera Access` 61 → 66、`Camera View Switching` 26 → 27）|

### 3-4　追溯對帳（`data/traceability.tsv`，`547352c6e1692c58`）

母體：037 leaf ＝ A 25（`recon.json` `a_data_rows` 25）＋ B 203（`b_leaf` 203）＝ **228**。

```
工作簿列數 = TC 數 = 有 TC 之 leaf 需求數 ≥ leaf 總數
  A  249 = 249 ；有 TC 之 leaf 25/25   ；工作簿 D 欄值 ⊆ leaf 母體
  B  216 = 216 ；有 TC 之 leaf 203/203 ；工作簿 D 欄值 ⊆ leaf 母體
traceability.tsv 228 列，tc_count = 0 者 0 列（升級條件 §5-2 不成立）
```

每列之 `tc_ids` 與 `coverage.tsv`／`coverage_b.tsv` 逐列相等（腳本 assert）。
umbrella 27 列另表 `data/traceability_umbrella.tsv`（`76b0ae11882de51a`）：`req_id → 子 leaf → 子列 TC 數`，27 列皆有子列且子列 TC ≥ 1。
（下放包原句「工作簿列數 = TC 數 = 有 TC 之 leaf 需求數」之中段按字面不成立 —— A 本 25 leaf 對 249 TC；實測之式為上框。）

### 3-5　`pending_by_dr` 重生

算法先以舊母體重現 CAM-24／25 之數字（A 245 列：各 DR 之 `tc_count`／`occurrences` 與 TOTAL 43／54 **全同**；B 210 列：34／44 **全同**），再施於新母體：

| 檔 | sha256_16 | 結果 |
|---|---|---|
| `pending_by_dr.tsv` | `b90e89c8946c1bf1` | **249 列中 46 列**含 PENDING（＋`-247`／`-248`／`-249`）；DR-CAM-a 1、f 10、h 1、t 1 為新增／變動 |
| `pending_by_dr_b.tsv` | `163c94c1927fc2ec` | **216 列中 35 列**（＋`-214`）；DR-CAM-h 3 → 4 |

---

## §4　`DELIVERY_STATUS.md` 更新（sha256_16 `2430bf6ec27f2866`）

| 節 | 改動 |
|---|---|
| §1 | A 候選 → **dryrun4**、B → **dryrun3**；D 欄佔位 **0**；列數 ＝ TC 數 ＝ **465** |
| §2 | 改為「**每條 leaf 需求 ≥ 1 TC：A 25/25、B 203/203**」；原零 TC 四形制表改為十列之去向表；比例 1:10／1:1.06 |
| §3 | 阻交付 DR **11 → 13**（＋DR-CAM-a、＋DR-CAM-t），**81／465 列**（A 46／B 35）；A 本 Test Set 10 組 |
| §4 | bench **105 列**（＋`-246`／`-247`／`-214`／`-216`）|
| §5／§6 | 註明 RDF-18、GCB-13 經 CAM-25 審閱立而**尚未落檔** |
| §7 | 全 465 列之數字；selfcheck 新兩類 |
| 沿革／§9 | 記 CAM-26 為生成結束後唯一之明文指派補生成 |

---

## §5　R-CAM17 證據表

| # | 主張 | 掃描字串／判準 | 母體 | 命令 | 命中 |
|---|---|---|---|---|---|
| 1 | `-007` 之來源無握手 | `handshake`（不分大小寫）| `sources/raw/sys2_vf551_{v2,v3,v33,v4,v42}` ＋ `sys2_cfts092` **六本**，全分頁全格 | openpyxl ＋ `re.I` | **各 0** |
| 2 | `diagnosticRequest` 於來源無載 | `diagnosticRe`（不分大小寫）| 同上六本 | 同上 | **各 0** |
| 3 | `-007` 之唯一來源為 `V42-210` | 037 `SWE-Requirement ID` = `SWE-CAM-007` 之 `Source Requirement ID` | `sources/raw/camera_037_swra_v02` 一本 `Analysis Report` | openpyxl 逐列 | 第 13 列 `SYS-RA-VF551_V42-210`（`batch03_plan.tsv` 第 2 列同）|
| 4 | `Digital_CHMSL_Camera_Prsnt` 只在 Atl-Hi | 字串逐字、分大小寫 | `forms/proxi/` **六本**（含三本 `.XLSM`）| openpyxl 全格 | HDCC27 兩本／DT27 **各 1**；637／2261／376 **各 0** |
| 5 | `Surround_View_Camera` 缺於 Toro | 同上 | 同上六本 | 同上 | Toro **0**；其餘五本各 1 |
| 6 | 既有 455 列 0 改動 | — | `features/camera/generated/` | `git status --short` | 只有 `batch06/`、`b08/` 兩目錄新增；**0 檔 M** |
| 7 | fenced 條文不變 | `rulings_hash.py` 之 `body_sha8` | `features/camera/RULINGS.md` 28 錨點 | 前後兩次 `--target` 比對 | R-CAM10／16／16(c) 之 fenced sha8 **不變**；其餘 25 錨點全不變 |
| 8 | dry-run 既有列 0 改動（除 `-245` `AH`）| 逐格 `!=` | A 245 ／ B 210 既有列 × 25 欄 | openpyxl 對底稿 | A **1 格**／B **0 格** |
| 9 | 追溯無 0 | `tc_count = 0` | `traceability.tsv` 228 列 | csv | **0** |
| 10 | md 與 json 一致 | — | 二十二目錄 465 份 | `render_tc.py --verify` | **相符 465／相異 0** |
| 11 | selfcheck 修訂不放寬 | 不引用該來源之 `req_id` | `-246` 之副本（`req_id` 改 `SWE-CAM-001`）| `selfcheck_camera.py` | 第 1 項**命中 1**、exit 1 |

---

## §6　自報

### 6-1　與下放包之出入（產物依來源；提請確認，DECISIONS 6-87）

1. **`SWE-CAM-025` 之委派對象為 `-020`，非 `-023`**（`batch04_plan.tsv` 第 116 列、`coverage.tsv`、A-CA06）。
2. **`SWE-CAM-013` 非「全委派 `-004`」** —— `-592`／`-593` 委派 `-006`、`-595` 委派 `-004`。
3. **`-007` 之「LVDS 握手序列」無來源**（證據 1）→ 不造握手，改驗來源所載之 LVDS 影像接收。037 之 `Verification Method` 欄（SNA／6 cycles 逾時）亦非該列來源所載，不入。
4. **`-013` 之「訊息名／值逐字」只有名可逐字**（取自 037 題名與 Description）；欄位與值無來源（證據 2）→ `PENDING: DR-CAM-t`（新開）。錨取 `-593` 之由：三來源中唯一使 LVDS 鏈路正常者。
5. **`-136`／`-139` 之「前提改 R1 Low」** —— 兩列與母列同在 §34（R1 Low），母列前提**已為** R1 Low，故全沿母列。
   另 `-139`（§34.6.1）之父題與母列 `-087`（§34.4.1）**不同**，原 R-CAM16(c)(i)「父題亦全等」之要件本不成立 —— CAM-17 之委派有誤，今已出 TC，無後果。

### 6-2　其他

6. **`NR1L-RVCHMI-087` 之車型與 R-CAM18(b) 不合**（證據 4，**A-CA39**）：其前提用之 `Digital_CHMSL_Camera_Prsnt` 只在 Atl-Hi 三本，而該列 637／2261／376 勾 `1`。
   §0 禁改既有列，未動；新列 `-216` 依實測只勾 HDCC27／DT27。
7. **R-CAM16 之「執行層落實」段有誤記**（`-007` 之來源寫成 `V42-216`）—— 為 CAM-10 執行層之筆誤，本包於原處附更正（A-CA38(3)）。
8. `-248` 之「六欄」解讀為 **J／K／L／M／N 五欄 `PENDING: DR-CAM-a` ＋ AH 之 BLOCKED 註**；Procedure／ER 各寫兩行以滿第 2 項（≥ 2 步）。
   `design_method` 與 `priority` 須為詞彙表之值，不能佔位，取 `功能測試`／`P1`。
9. **`-211` 之角度出自鄰列 §27.3.1.x**（037 未收為需求）；升級條件 §5-1 因而**不成立**，未用 `PENDING: RDF-12`。
10. `docs/fw036/RULINGS.sha.tsv` 仍未重生（**GCB-04**，非本線可改）。

### 6-3　升級條件

- §5-1（依 Description 與父題皆無可驗事實）：**不成立** —— 九列皆有來源事實可承；`-248` 為 R-CAM19(d) 明定之缺件佔位，非此條。
- §5-2（`traceability.tsv` 任一 leaf 為 0）：**不成立**（證據 9）。

---

## §7　未結

| # | 項 | 歸屬 |
|---|---|---|
| 1 | **13 條阻交付 DR**（81／465 列；A 46／B 35），含新開之 **DR-CAM-t** | **Pei** |
| 2 | `bench_verify.md` **105 列**實機驗證 | **Pei** |
| 3 | Phase 7（`--write`／tag／`delivered/`）；兩本分交或合併 | **Pei** |
| 4 | **DECISIONS 6-87 六項**：`-246`／`-247` 之角度、DR-CAM-t 是否阻交付、**R-CAM19(b) 之射程**（只及零 TC 列，或亦及已有 TC 之列之被委派來源 —— 後者之補生成量遠逾本包）、`-087` 是否補正（A-CA39）| **分析層／Pei** |
| 5 | **RDF-18**、**GCB-13** 經 CAM-25 審閱立而尚未落入 `RD_FEEDBACK.md`／`GC_BACKLOG.md`（本包未指派）| 分析層指派 |
| 6 | RDF-01 ～ RDF-17（＋18）回 RD 作者 | **RD** |
| 7 | GCB-01 ～ GCB-12（＋13）；GCB-01「Camera 交付前必須完成」| **GC 線** |

---

## §8　落檔清單

```
M  features/camera/RULINGS.md                       R-CAM19 ＋ 三處刪除線 ＋ 更正       sha256_16 014d13c64e4c67d0
M  features/camera/DECISIONS.md                     6-86／6-87；6-52／6-73 作廢註        sha256_16 64522d7eb908e1f2
M  features/camera/ANOMALIES.md                     A-CA35／36 RESOLVED；A-CA38／39      sha256_16 8dfa0b1832f2888d
M  features/camera/DATA_REQUESTS.md                 DR-CAM-t                             sha256_16 8dd57615e31bcfee
M  features/camera/RD_FEEDBACK.md                   RDF-12 改題                          sha256_16 78f8468e46f317d7
M  features/camera/DELIVERY_STATUS.md               §1～§9                               sha256_16 2430bf6ec27f2866
M  features/camera/scripts/selfcheck_camera.py      共引錨／缺件佔位                     sha256_16 f756218e34dbe057
M  features/camera/data/coverage.tsv                4 列 → PRODUCED                      sha256_16 7e1ae30615687aad
M  features/camera/data/coverage_b.tsv              6 列 → PRODUCED                      sha256_16 78b47efbdbf28d45
M  features/camera/data/pending_by_dr.tsv           249 列口徑                           sha256_16 b90e89c8946c1bf1
M  features/camera/data/pending_by_dr_b.tsv         216 列口徑                           sha256_16 163c94c1927fc2ec
M  features/camera/data/bench_verify.md             ＋4 列（105）                        sha256_16 fc9a5ac0490443e3
A  features/camera/data/batch06_plan.tsv            4 列                                 sha256_16 a6529856489cf02a
A  features/camera/data/b08_plan.tsv                6 列                                 sha256_16 ef5e43dd3861a8d3
A  features/camera/data/traceability.tsv            228 leaf                             sha256_16 547352c6e1692c58
A  features/camera/data/traceability_umbrella.tsv   27 umbrella                          sha256_16 76b0ae11882de51a
A  features/camera/generated/batch06/               4 json ＋ 4 md ＋ INDEX ＋ LINT_REPORT
A  features/camera/generated/b08/                   6 json ＋ 6 md ＋ INDEX ＋ LINT_REPORT
A  features/camera/sandbox/a_dryrun/…_A_20260924_dryrun4.xlsx   249 列                sha256 a4ad6cf3112d5e31…
A  features/camera/sandbox/b_dryrun/…_B_20260924_dryrun3.xlsx   216 列                sha256 1d8d21b9945d4828…
A  docs/fw036/lint_reports/RearViewCamera_A__camera_a4ad6cf3_20260924.md
A  docs/fw036/lint_reports/RearViewCamera_B__camera_1d8d21b9_20260924.md
```

**未動**：既有 455 列之 `generated/`（含 `-087`）、`framework.md`、`docs/runtime/profiles/`、`features/camera/delivered/`、
前版 dry-run 各檔、`features/comfort/`（併行 session；其 `delivered/` 之刪除非本包所為）。
