# 上繳包 CAM-24 —— A 本補 5 列、dry-run v3 249 列、`DELIVERY_STATUS.md`（生成階段收尾）

對應下放包：`docs/fw036/handoff/down/20260923_CAM-24.md`（審閱 `down/20260923_CAM-23_review.md` 同發）。
執行層 session：延用。日期 2026-09-23。**本包為 Camera 生成階段之最後一包。**

**一句話**：§1 落檔（DECISIONS 6-78～6-81、`bench_verify` 亮度列）；
§2 A 本補 **5 列**（`batch05/`，`NR1L-RVC-241`～`-245`；下放包預估 4，亮度一項依其指示分兩列）——
A 本 TC 由 240 → **245**；§3 dry-run v3 **249 列**，lint **ERROR 類全 0**、對帳 249 ↔ 249 合；
§4 `features/camera/DELIVERY_STATUS.md` 建檔。
**全案 451 TC**（A 245 ＋ B 206），selfcheck ERROR 級十項全 0、`render_tc --verify` 451/451。
**兩項升級條件皆不成立**（`-242` 之 HMI 面逐字來源已查得，不掛 DR）。
既有 446 列 **0 改動**；未寫 `delivered/`、未 tag。

---

## §1　落檔

### 1-1　DECISIONS 6-78 ～ 6-81

`features/camera/DECISIONS.md`（sha256_16 `cb1b9c39d0095962`）：

| # | 項 |
|---|---|
| 6-78 | A 本拆解審計之補生成（追加 4，**實出 5**：亮度分兩列，形制待裁）|
| 6-79 | CAM-21 之交付物落於 `up/CAM-23`，**不補空殼**（PROTOCOL 同名配對之例外註）|
| 6-80 | 第 10 項之 37 個 WARN **不逐列覆核**（已知偽陽為主；其用處為新批之前置提示）|
| 6-81 | `NR1L-RVCHMI-204` 之殘留 WARN（`列舉(3>2)`）接受不處理 |

### 1-2　`bench_verify.md`

`features/camera/data/bench_verify.md`（sha256_16 `b6e65f7208d36e6c`）加 **A 本 batch05 三組**：
`-243`（`adb dumpsys` 之輸出是否足以判資料路徑）、`-244`／`-245`（亮度計 ＋ 可設定之顯示亮度）、
`-242`（`RVC Image` soft button 之可觸及）。**全案累計 101 列**。

---

## §2　A 本補 5 列（`batch05/`）

TC ID `NR1L-RVC-241`～`-245`｜計畫 `features/camera/data/batch05_plan.tsv`（sha256_16 `81b233ff3af61ad9`）。

| 審計項 | 母列 | 來源列 | 補列 | 驗證點 |
|---|---|---|---|---|
| CAM-23 §3 #1（H）| `-017` | `SYS-RA-VF551_V2-496` | `-241` | any-of 之 `VehicleSpeedVSOSig >= c_VEHSPD_MAX` 退出 |
| 同上 | `-017` | 同上 | `-242` | any-of 之 `RVC_ImageDefeat.Req = Pressed` 退出 |
| §3 #2（M）| `-201` | `SYS-RA-VF551_V42-226` | `-243` | `Rear_Camera_Repetition.Data` 之複製 |
| §3 #3（M）| `-214` | `SYS-RA-VF551_V33-234` | `-244`／`-245` | 亮度 51 %（on-point）／49 %（off-point）|

`coverage.tsv`（sha256_16 `a4a39436f2cdfb25`）之 3 個母 leaf 同步：
`SWE-CAM-015` 14 → 16、`SWE-CAM-006` 17 → 18、`SWE-CAM-019` 2 → 4；`batch` 欄加 `;batch05`；
A 本 `tc_count` 合計 240 → **245**。**既有 240 列 0 改動**。

### 2-1　`-242` 之 ImageDefeat —— **升級條件 §6-1 不成立**

下放包 §6-1 預期「`-242` 之 ImageDefeat 於 HMI 面無逐字對應鍵」→ 掛 DR-CAM-g 型。
**實測 HMI 面之逐字來源已存在**：`SYS-RA-VF551_V2-549` 全文

> `· HU shall acquire the HU touch screen coordinates pressed by the driver to defeat Rear View
> Camera View via **RVC Image soft button** and set internal Signal RVC_ImageDefeat.Req = Pressed.`

故 Procedure 之觸發寫 `Press the RVC Image soft button`，**不掛 DR**，升級條件不成立。
`RVC_ImageDefeat.Req` 本身為**內部訊號**（四本 DBC 之 `SG_` 掃描只得 `ImageDefeatRQSts`／
`TrailerImageDefeatRQSts`，皆非本名），依 §8.7.5(f) 保留來源名於 ER、不寫 `Send CAN` 步。

### 2-2　`-244`／`-245` 出兩列 —— **與既有前例相左，回報**

下放包 §2 明載「49% 併入同列之對照步**不可**（§8.3）」，故**依其指示分兩列**。
**惟本 feature 既有之前例為一列兩點**，且皆已判過：

| 前例 | 章 | 形制 |
|---|---|---|
| `NR1L-RVCHMI-036` | RVC+PAM §7.5.3 | 一列內 `205`（門檻下）與 `206`（門檻上）兩步 |
| `NR1L-RVCHMI-111` | HU §28.7.1.1 | 一列內 pinch out／pinch in 兩步 |
| `NR1L-RVCHMI-142` | HU §30.1.3 | 一列內 OK／`X`／5 秒自動**三**條清除路徑 |

兩形制之取捨請分析層裁：若採一列兩點，`-245` 併入 `-244`，A 本回 244 TC。
本包**未自行合併**（依下放包之明文指示執行）。

`-245` 之 ER 只判「量得之值低於門檻」—— 來源只寫 `must be over 50%` 而**未載低於門檻時之系統行為**，
**不造「影像不顯示」之判準**（§8.4.1）。

### 2-3　lint 與 selfcheck

```
selfcheck_camera.py  batch05（TC 5 筆）   ERROR 級十項全 0｜第 10 項候選 0
lint036 --profile camera（batch05_lint）  ERROR 類全 0｜非致命 I-cross 5／J 1
```

`J` 一列（`-243`）為 **profile §5.1 之逐字豁免** —— 其上半**自來源 `Description` 之首 token 起**
（`SYS-RA-VF551_V42-226` 全文即以小寫 `copy` 起首），符 **6-65(a)** 之要件；reasoning 已具名。

---

## §3　A 本 dry-run v3

```
features/camera/sandbox/a_dryrun/FM-WI-FSM-036-A01 STLA 測試用例規範與結果_SWQT STLA Test Case Specification & Result_SWQT_RearViewCamera_A_20260923_dryrun3.xlsx
sha256  c11e1840a0216d532f1bf921bc1529c92bca9d72d7e07a1bfe55f693188f624c
資料列 249（TC 245 ＋ D 欄佔位 4）
```

### 3-1　lint 全表

```
行計 A=0 B=0 C=0 D=0 E=0 F=0 G=0 H=0 I=0 I-sibling=0 J=37 K=0 L=0 M=0 N=0
     P=0 Q=0 R=0 T=0 U=54 V=0 I-cross=245 W=0 X=3 Y=0 Z=0
報告 docs/fw036/lint_reports/RearViewCamera_A__camera_c11e1840_20260923.md
```

**ERROR 類全 0**；`Z` 全綠。`J` 由 36 → **37**（新增之 `-243`，profile §5.1 豁免）；
`U` 54、`I-cross` 245、`X` 3 —— 與 v2 相較只隨列數變動，**無新類別**。
**升級條件 §6-2 不成立**（無 ERROR）。

### 3-2　sheet 級

D 欄升冪**成立**，`SWE-CAM-001`～`-025` **無缺號**；佔位列 **4**；
Test Set **9 組**、**舊名 `Auxiliary Cameras` 殘留 0**。

### 3-3　對帳

`coverage.tsv` `tc_count` 合計 **245** ＋ 零 TC leaf **4** ＝ **249** ↔ 工作簿 **249** → **合**。

### 3-4　`pending_by_dr.tsv` 重生

`features/camera/data/pending_by_dr.tsv`（sha256_16 `54bba98c14c080b3`）：
**245 列中 43 列含 PENDING**，涉 8 條 DR（f 9／g 5／i 4／j 7／k 1／m 1／p 2／q 14），
合計 54 次 ＋ §7.5 佔位 6 處。**補生成之 5 列皆無 PENDING**，故列數與 v2 相同。

---

## §4　`features/camera/DELIVERY_STATUS.md`（新建）

sha256_16 `cb9798edf91f5c11`。八節：

1. **兩本候選**（路徑／sha／列數）—— A 249／B 212，合計 **461 列 451 TC**；A 之候選自 dryrun2 改指 **dryrun3**
2. **TC 總數與來源覆蓋** —— 含零 TC 之**四形制**計數表
3. **PENDING ↔ DR** —— **11 條阻交付、77／451 列**；Pei 之三選一；R-G42 七
4. **實機驗證** —— `bench_verify.md` **101 列**
5. **RD 回饋** —— RDF-01 ～ 17（17 條）
6. **全域待辦** —— GCB-01 ～ 12（12 項），其中 GCB-01 註「Camera 交付前必須完成」
7. **品質狀態** —— 全 451 列之 selfcheck／lint／render 數字
8. **Phase 7 前置條件** —— 五項

（全文見該檔；本包不重貼以免兩處文字漂移。）

---

## §5　R-CAM17 證據表

| # | 主張 | 掃描字串／判準 | 母體 | 命令 | 命中 |
|---|---|---|---|---|---|
| 1 | `RVC_ImageDefeat.Req` 之 HMI 面逐字來源存在 | `ImageDefeat` | 六本 SYS2 之 `Description` 全文 | `sys2_index()` ＋ 逐列比對 | **25 列命中**；其中 `SYS-RA-VF551_V2-549`（及 V3-207／V4-107）載 `via RVC Image soft button` |
| 2 | `RVC_ImageDefeat.Req` 非 CAN 訊號 | `^\s*SG_\s+\w*[Dd]efeat\w*` | `forms/` **4 本 DBC** | 逐檔 `re.findall` | 只得 `ImageDefeatRQSts`（PDT27 FDCAN8、637MCA）與 `TrailerImageDefeatRQSts`（PDT27 BHCAN2）；**無 `RVC_ImageDefeat`** |
| 3 | `-243` 之 `J` 屬 profile §5.1 豁免 | 來源 `Description` 之**首 token** | `SYS-RA-VF551_V42-226` 全文 | 讀首 token | 首 token 為小寫 **`copy`**，與上半之首 token 相同 → 符 6-65(a) |
| 4 | dry-run v3 對帳 | `coverage.tsv` `tc_count` ＋ 零 TC leaf ↔ 工作簿列數 | 兩者 | csv 加總 ＋ openpyxl | **245 ＋ 4 ＝ 249 ↔ 249，合** |
| 5 | D 欄升冪且無缺號 | req_id 數值 | dry-run v3 249 列 | openpyxl 逐列 | **升冪成立**；`SWE-CAM-001`～`-025` 缺號 **0** |
| 6 | 舊組名無殘留 | `Auxiliary Cameras` | dry-run v3 之 `H` 欄 | `Counter` | **0** |
| 7 | 既有 446 列 0 改動 | — | 十八目錄（`batch05/` 除外）| `git status --short` | **0 列** |
| 8 | md 與 json 一致 | — | 十九目錄 **451** 份 md | `render_tc.py --verify` | **相符 451／相異 0** |
| 9 | 全案 selfcheck | 十項 ERROR ＋ 第 10 項 WARN | 451 列 | `selfcheck_camera.py` | ERROR 十項**全 0**；錨 455／反查失敗 0；第 10 項候選 **37** |

---

## §6　自報錯誤

1. **`-244`／`-245` 之形制與既有前例相左**（§2-2）—— 依下放包 §2 之明文指示分兩列，
   但本 feature 已判過之三個前例（`NR1L-RVCHMI-036`／`-111`／`-142`）皆為**一列多點**。
   **未自行合併**，回報待裁；若採一列形制，A 本回 244 TC。
2. **下放包 §6-1 之預期未成立**（§2-1）—— ImageDefeat 之 HMI 面逐字來源實際存在
   （`SYS-RA-VF551_V2-549`），故未掛 DR。此為**先查後判**之結果，非略過該升級條件。
3. **`batch05` 之 `section`／`verbatim_section` 欄記為 `—`** ——
   `batch05_plan.tsv` 沿用 B 本之欄位表，而 A 本之來源為 SYS2（無 SYS1 章節號），
   其定位由 `spec_reference`（VF 錨）與 `verbatim_source_pid`（SYS-RA id）承載。
   若分析層要求 A 本另立欄位表，請示下。
4. **本輪未動 `features/camera/RULINGS.md`／`framework.md`／`ANOMALIES.md`／`RD_FEEDBACK.md`／
   `DATA_REQUESTS.md`** —— 無新立條、無重開、無新異常、無新 DR。
   `docs/fw036/RULINGS.sha.tsv` 仍未重生（**GCB-04**，非本線可改）。

---

## §7　未結（生成階段結束時之全案狀態）

| # | 項 | 歸屬 |
|---|---|---|
| 1 | **`-244`／`-245` 之一列／兩列形制**（§2-2）| 分析層 |
| 2 | `batch05_plan.tsv` 之欄位表是否另立（§6-3）| 分析層 |
| 3 | **11 條阻交付 DR**（77／451 列）—— 結案／降 NA／帶 PENDING 不出貨 | **Pei（Phase 7 之閘）** |
| 4 | **`bench_verify.md` 101 列**實機驗證 | **Pei** |
| 5 | **RDF-01 ～ 17** 十七條回 RD 作者 | RD |
| 6 | **GCB-01 ～ 12** 十二項；其中 **GCB-01** 註「Camera 交付前必須完成」 | GC 線 |
| 7 | **Phase 7**（`--write`／tag／寫 `delivered/`）—— 五項前置見 `DELIVERY_STATUS.md` §8 | **Pei** |

---

## §8　落檔清單

```
M  features/camera/DECISIONS.md                   6-78 ～ 6-81         sha256_16 cb1b9c39d0095962
A  features/camera/DELIVERY_STATUS.md             交付狀態一頁表        sha256_16 cb9798edf91f5c11
M  features/camera/data/coverage.tsv              母列 3 列追加         sha256_16 a4a39436f2cdfb25
A  features/camera/data/batch05_plan.tsv          5 列                 sha256_16 81b233ff3af61ad9
M  features/camera/data/pending_by_dr.tsv         245 列口徑重生        sha256_16 54bba98c14c080b3
M  features/camera/data/bench_verify.md           batch05 4 列（累計 101）sha256_16 b6e65f7208d36e6c
A  features/camera/generated/batch05/             5 json ＋ 5 md ＋ INDEX ＋ LINT_REPORT
A  features/camera/sandbox/a_dryrun/…_dryrun3.xlsx 249 列              sha256 c11e1840a0216d53…
A  docs/fw036/lint_reports/RearViewCamera_A__camera_c11e1840_20260923.md
```

**未動**：`RULINGS.md`、`framework.md`、`ANOMALIES.md`、`RD_FEEDBACK.md`、`DATA_REQUESTS.md`、
`features/camera/delivered/`、既有 446 列、`sandbox/b_dryrun/`、`features/comfort/`（併行 session）。
