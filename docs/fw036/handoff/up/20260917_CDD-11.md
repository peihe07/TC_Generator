# 上繳包 CDD-11 —— CFTS004 原始文件補產（R-DIAG26）＋ 交付候選（R-DIAG27）

下放包：`down/20260917_CDD-11.md`（依據 `down/20260917_CDD-10_review.md` §三／§四）。執行層 session：沿用 CDD 線，2026-09-17。

## 首段 —— 升級條件逐項（自 §5 逐字複製）

| # | 條件（逐字） | 實測 | 觸發 |
|---:|---|---|:-:|
| 1 | 指紋集合不符／`--check` 不綠（既存者記「既存」） | 寫前 HEAD 75 ＝ worktree 75，寫後 **78**，`git diff` 恰 3 增 0 刪；全生成比對零漂移。`--check` FAIL → **既存**（GC-16 標的） | **否** |
| 2 | T1(b) 命中之原始文件與索引匯出版本不一致（Comments 欄版本號／日期）且無同版者 —— 回報不登錄，Pei 裁用哪版 | ObjectID 空間一致（根節點 `4939389` 相同，抽驗 `4939801`／`4940456` 皆在）；**版本一致** | **否** |
| 3 | T2(f) 對帳出現 ID／列序以外之格差 | 首輪 **1 格差**（OOS 列之處置欄漏寫），**已修**；複驗 **0 格差** | **否**（見 §5-1） |
| 4 | 任何需改 canon 之情形 | 無 | **否** |

**四項皆未命中。** 惟 **R-DIAG26 之前提與原始文件實測不符**，見 §2.3 —— 這是本包最重要的結果，不構成升級條件但須 Pei 裁。

---

## §1　T0 —— 條文與指紋

R-G23 取號現查：`R-DIAG26`／`R-DIAG27` 各零命中，`R-DIAG25(amend)` 未存在。三條逐字落。

**R-G75(c) 三數**：寫前 HEAD **75**／worktree **75**（無差異 → **本次無覆寫事件**，累計仍五次）／寫後 **78**。

| 條 | sha8 | body_sha8 | 行 |
|---|---|---|--:|
| `R-DIAG25(amend)` | `5334731f` | `e5faf417` | 347 |
| `R-DIAG26` | `51acb544` | `15110dc1` | 354 |
| `R-DIAG27` | `fcfe303d` | `330649d4` | 363 |

---

## §2　T1 —— CFTS004 原始文件（R-DIAG26）

### 2.1　(a) 搜尋結果

`/Users/peihe/Work/02_Project_R1LR/1_Customer_Requirement/` 全樹，`-iname "*CFTS*004*"` ＋ `-iname "*General*Diagnostic*"`：

| PI | 檔名 | 型 | 大小 | sha16 |
|---|---|---|--:|---|
| **26PI2.5 Jun** | `R1LR_Atl-H_26PI2.5 Jun Release-…_CFTS_004_General Diagnostic Requirements_20260608-1145.docx` | docx | 110,134 | **`f68f60837fe88851`** |
| 26PI2.5 Jun | 同名 `.doc` | doc | 801,792 | `fe3638037c73ab33` |
| 26PI2.5 Jun | `…_20260605_1356.reqifz` | reqifz | — | — |
| 26PI1.5 Mar | `…_20260310-1509.doc` | doc | 798,208 | `bc15a49e4c755719` |
| 25PI4.5 Dec／25PI3.5 | `.doc`／`.reqifz` 各一 | — | — | — |
| 26PI3.5 | 內含 26PI2.5 之同名 `.doc`／`.reqifz`（未更版） | — | — | — |

**選 26PI2.5 Jun 之 `.docx`** —— 與既有登錄前例 `cfts024_specific_hu_radio_functions_20260608`（同 PI、同 `-1206` 式、同取 docx）一致。

### 2.2　(b) 登錄（R-G66／R-G67）

`sources/MANIFEST.tsv` 第 **239** 列新增 `cfts004_general_diag_20260608`，
sha256 `f68f60837fe888518e6c3344f51709a69aab3d8e140347baddc93d848c47c38e`，version `20260608-1145`，features `diagnostics`。
檔置 `sources/raw/cfts004_general_diag_20260608/`（該目錄為 `.gitignore` 所收，只有 MANIFEST 進版本庫）。

**R-G67 嵌入物件檢查**：docx 共 32 個 part，嵌入媒體僅 `word/media/image1.png`（250×80，3,253 bytes）——
經目視為 **Stellantis 商標圖**。**無 oleObject、無嵌入試算表、無承載資料之物件。**

### 2.3　(c) 五句引言之列表 —— **R-DIAG26 之前提與實測不符**

`features/diagnostics/data/cfts004_lists.md`（逐句核對全表）。

**結論：引言句其後之「列表」不是表格，而是各自獨立、各有 ObjectID 的 FR 段落 —— 且全數已在索引匯出內。**

| 引言句 | DID | 其後 FR 段數 | 在索引匯出內 |
|---|---|--:|--:|
| `The following tones shall be selectable in this DID:` | `$5000` | 14 | **14（全數）** |
| `The following speaker outputs shall be selectable using this DID:` | `$5001` | 5 | **5（全數）** |
| `… report the status of the following items as reported by the Sirius XM chipset …` | `$2841`/`$2842`/`$2843`/`$2844` | 16 | **16（全數）** |
| `… report the following information as provided by the internal GPS receiver.` | `$2812` | 多段 | **全數** |
| `When requested by this DID, the HU shall report the status of the following items:` | `$2840` | 多段 | **全數** |
| `The following data shall be readable from and writeable to this DID:` | `$280C` | 多段 | **全數** |

**索引匯出沒有丟掉任何列表內容。** CDD-10 審閱 §三與 A35 之前提（「引言句之列表是索引匯出丟掉的內容」）不成立。

**更關鍵 —— 位元組編碼根本不在 CFTS004 內。** 對原始文件全文 262,103 字之機械搜尋：

| 樣式 | 命中 |
|---|--:|
| `controlOptionRecord` | **0** |
| `controlEnableMask` | **0** |
| `byte <n>`（版面） | **0** |
| `bit <n>`（位元定義） | **0** |
| `0xXX` | **0** |
| `$XX` | 60 —— 全為 `$0307`／`$0309` 之 Indication Code 與 `$FF` padding，**已在索引匯出內** |

故 **`DR-DIAG-4`／`DR-DIAG-5` 不可能由 CFTS004 解決** —— 須另求來源（DID 資料字典／`CIP Radio Tables`）。

**索引匯出與原始文件之差集**

| 方向 | 數 | 內容 |
|---|--:|---|
| 原始文件有、索引匯出無 | **410**（318 FR ＋ 92 Description） | 內部 cell modem 訊號／CDMA IMSI／HD Sub-channel 等 —— **未被 SYS2 收入本案範圍**之他 ECU／他機種要件。037 自 SYS2 已收範圍導出，故不在母體（R-DIAG8(a)） |
| 索引匯出有、原始文件無 | **13** | 11 個 `Out of  Scope` 類（`$1801` 節之 loss-of-communication）＋ 2 個 `$0301` 節未引用列。其中 `SYS-RA-DIAG-248` 即本 feature 之 OOS 列 `-057`，已依 R-DIAG3(amend) 處置 |

### 2.4　(d) 補產 —— **0 條，佔位撤銷 0 行**

三個依據：(1) 引言句之列表全在索引匯出內，已產；(2) 原始文件零位元組編碼；(3) 410 個差集全屬 SYS2 未收範圍。
**`DR-DIAG-10` 不開** —— 文件已取得且已登錄，不是缺件；缺的是**另一份**資料字典（併 `DR-DIAG-4`／`-5`）。

---

## §3　T2 —— 交付候選（R-DIAG27）

`features/diagnostics/sandbox/delivery/`

| 檔 | 內容 |
|---|---|
| `FM-WI-FSM-036-A01 STLA 測試用例規範與結果_SWQT STLA Test Case Specification & Result_SWQT_Diagnostics_20260917.xlsx` | **556 列** |
| `id_map_v08_to_delivery.tsv` | 556 列（舊 ID／交付 ID／SWE1 ID／037 row） |
| `coverage.tsv` | **389 列，UNCOVERED 0**（TC ID 已換為交付 ID） |
| `placeholder_summary.tsv` | 546 行（交付 ID，依交付列序） |
| `placeholder_by_token.tsv` | 104 列（token × 佔位字串 → 行數／TC 數／TC 清單） |
| `asset_request.md`／`rd_feedback_20260917.md` | 自 sandbox 複製 |
| `EXEC_GUIDE.md` | **新增** |

### 3.1　Cover／Product Document 逐格

| 表 | 格 | 值 | 依據 |
|---|---|---|---|
| Cover 封面 | `D6` 版本 | `A` | Security SEC-15/17 交付本同值（首版） |
| | `G7` 修訂日期 | `2026-09-17` | 同前例 |
| | `D7` 核准者 | **留空** | 下放包 §0：留給 Pei（Security 前例為 `劉安哲 AllenACLiu`） |
| | `D8` 審查者 | **留空** | 同上（Security 前例為 `張愷霏 ErinKFChang`） |
| | `D9` 作者 | `PeiPYHsu` | Security 前例同式；**中文名留空**，留給 Pei |
| | `G9` | `2026-09-17` | 同前例 |
| | `C12` 文件名 | 檔名（不含副檔名） | Security 前例同式 |
| Product Document | `B3` 專案代號 | `NR1L` | SWC 0708 ＋ Security 前例同值 |
| | `B5` 版本 | `V1.0` | 同上 |
| | `B6` 部門 | `SW Testing` | 同上 |
| | `B7` 分類 | `Confidential` | 同上 |
| | `A13`／`B13`／`D13` | `1`／`初版發佈`／`2026-09-17` | 同上 |

`Reference`／`ChangeHistory`／`下拉選單`／`Cover_old`／`ChangeHistory_old` 沿母本不動。

### 3.2　列序與 ID

**列序 = 037 列序。** 實測 **SWE1 ID 升冪與 037 row 升冪完全一致（不一致列數 0）**，故兩種寫法同義；
三段式 `-340-001`（row 347）→ `-340-002`（row 348）順序正確。同一 037 列之多 TC 依原 ID 序。

TC ID 重編 `NR1L-DIAG-001` ～ `NR1L-DIAG-556`，連號無洞；舊／新 ID **各自唯一**，`row037` 單調不遞減。
例：延伸批之 `-556`（`$5009` F+S*N，037 row 42）→ 交付 **`-051`**；`-554`（`$5000` 外部音，row 122）→ 交付 **`-166`**。

### 3.3　著色

佔位列整列 `FCE4D6`（A～AH 共 34 欄），**文字不動**：**386 列**（＝ `placeholder_summary.tsv` 之 386 條 TC）。
Out of Scope 列（交付 `NR1L-DIAG-081`）**不著色**，依下放包 §2(c)。

### 3.4　(f) 逐格對帳與 lint

**交付本 vs `merged/v08` cell-for-cell（B～AH 共 33 欄，556 列）：扣除 B 欄序號與 F 欄 TC ID 後，格差 0。**

lint（`--profile diagnostics`）：無 `Traceback`；`U=3`（DR-DIAG-1 之 PENDING，條文所命），**其餘 30 項全 0**。
canon §9 **17/17**；R-SEC15 型 (a)(b)(e) 555/555、(c) 0、(d) 0、(i) 3（母體扣 Out of Scope 列 1）。

**貼入交付本前 3 列**

```text
B=1  C=NRL-1487  D=SWE1-Diagnostics-001  F=NR1L-DIAG-001  H=Camera Brightness
  I(Test Item)  The MDTDiagService shall interpret the received DID 0x283F request and extract the
                brightness level parameter. (read $283F, SID 22)
  J(Pre-Cond)   1. The ECU is in the default diagnostic session / 2. The diagnostic tool is connected …
  K=NA   L(Proc) 1. Read the current rearview camera brightness level    $ 22 28 3F
  P=P1   R=功能測試 (Functional based ; no specific technique)

B=2  C=NRL-1487  D=SWE1-Diagnostics-002  F=NR1L-DIAG-002  H=Camera Brightness
  I  The SW shall validate the received diagnostic request format and parameters.
     (write $283F, NRC 13 incorrectMessageLengthOrInvalidFormat)
  L  1. Read the rearview camera brightness level and record as Brightness_initial …
  P=P2   R=負向測試 (Negative / Invalid)

B=3  C=NRL-1487  D=SWE1-Diagnostics-002  F=NR1L-DIAG-003  H=Camera Brightness
  I  The SW shall validate the received diagnostic request format and parameters.
     (write $283F, unsupported DID, NRC 31 requestOutOfRange)
  P=P2   R=負向測試 (Negative / Invalid)
```

**T1 補產 TC 全文** —— **無**（§2.4：補產 0 條）。

---

## §4　T3 —— 盤點終表

| # | Test Set | 037 列 | TC | | # | Test Set | 037 列 | TC |
|--:|---|--:|--:|---|--:|---|--:|--:|
| 1 | SXM Module Version | 57 | 76 | | 11 | Buzzer Control | 40 | 54 |
| 2 | SXM Signal Quality | 72 | 119 | | 12 | Speaker Quadrant Selection | 21 | 28 |
| 3 | SXM Subscription | 37 | 51 | | 13 | Audio Tone Settings | 28 | 44 |
| 4 | SXM Package Routines | 13 | 19 | | 14 | Tuner Control | 22 | 30 |
| 5 | GPS Signal Data | 34 | 45 | | 15 | Mode Selection | 9 | 12 |
| 6 | Camera Brightness | 6 | 8 | | 16 | In Motion Menu Control | 6 | 8 |
| 7 | ECU Internal Settings | 8 | 16 | | 17 | Video Input | 5 | 6 |
| 8 | Media Hub Config | 6 | 9 | | 18 | AV Signal Detection | 10 | 12 |
| 9 | Button Interfaces | 7 | 9 | | 19 | Audio Measure AC | 3 | 4 |
| 10 | Screen Test Pattern | 4 | 5 | | 20 | Clear Key Sense PIN | 1 | 1 |
| | | | | | | **合計** | **389** | **556** |

**20/20 組全產；`framework.md` LOCKED v01 之組名／組序／列數與 `layer2_assign.tsv` 對帳相符。**

| 項 | 值 |
|---|---|
| Priority | P0 **65**／P1 **175**／P2 **315**／空 1（OOS） |
| design_method | 負向 353／功能 184／BVA 14／決策表 2／狀態轉換 2／空 1 —— **全數落在母本「下拉選單」表之逐字值內** |
| Vehicle Model | `1 1 1 0 0 1 1` 290／`1 1 1 0 0 0 0` 265／空 1 |
| PENDING | **3 行／2 條**（交付 ID 見 `placeholder_summary.tsv`），皆 `DR-DIAG-1` |
| 佔位 | **546 行／386 條 TC**（100% 登錄）；`DR-DIAG-6` 269／`DR-DIAG-5` 197／`DR-DIAG-4` 64／`DR-DIAG-8` 16 |
| DR-DIAG-1～9 | 1 MISSING／2 PARTIAL／3 **CLOSED**／4 MISSING／5 MISSING／6 MISSING（改題）／7 MISSING／8 MISSING／9 ＝ 6 之獨立列。**DR-DIAG-10 不開**（§2.4） |
| FB-DIAG | a～s，**19 項** |
| A-DIAG | `[A-DIAG01]`～`[A-DIAG56]`，本包新增 `[A-DIAG57]`／`[A-DIAG58]` |

---

## §5　自報錯誤

1. **`[A-DIAG57]` 交付本首版漏寫處置欄（col 32）** —— 逐格對帳時抓到 1 格差：Out of Scope 列之
   `Out of Scope` 處置欄（R-DIAG3(amend) 所命）未寫入。我的交付寫入器沿用 `write_all` 之欄位 map 時漏了 `32`。
   已補並重建，複驗 0 格差。
   **教訓**：對帳不是形式 —— 它正是為了抓這種「新寫入器少一個欄位」的錯；若我當時只比對可見的九欄就會漏掉。
2. **`[A-DIAG58]` `EXEC_GUIDE.md` 之 PC-3 適用範圍**：我最初擬寫「三項環境全 556 條共用」，
   但 SYSAD §4.5 只對 Write DID／Routine execution／DTC clearing 要求 security access，
   純讀取（`0x22`）之 TC 其 Pre-Condition 本就不含該行。已改為分項說明，不將三項一律宣告為共用。

---

## §6　§3 預期 vs 實測

| 項 | 預期 | 實測 | 判 |
|---|---|---|---|
| T1(a) 命中 | ≥ 1 | **多版本命中**，選 26PI2.5 Jun docx | 符 |
| T1(d) 補產 | 命中時 20–80 TC；佔位撤銷 0–60 行 | **0 TC／0 行** | **不符** —— 成因見 §2.3：原始文件無列表可抽、無位元組編碼 |
| 交付本列數 | 556 ＋ 補產 | **556** | 符 |
| lint | 真違規 0 | **0** | 符 |
| 對帳 | cell-for-cell 只差 ID／列序 | **格差 0**（修 1 格後） | 符 |

---

## §7　未結與待 Pei

| # | 內容 |
|---|---|
| **R-DIAG26 之後續** | 條文書「引言句之列表以原始文件為錨…依 R-DIAG23(c) 補產」，而實測**無列表可補**。條文本體未改（§0 禁區），**請裁是否以 `R-DIAG26(amend)` 記明實測結果**，或維持條文並以本上繳包為其履行紀錄 |
| **`DR-DIAG-4`／`-5` 之真正來源** | 已證不在 CFTS004。建議向 Nik 求 **DID 資料字典**或 `CIP Radio Tables`（後者 `DR-DIAG-8` 已列）。`asset_request.md` 待據此改寫 |
| **spec_reference 欄源**（CDD-10 審閱 §四末段） | 本交付本用 ObjectID（`CFTS004-4939794` 式，同 SWC 0708 之 `CFTS042-4813401`）；C 欄另填 NRL（`NRL-1487`）。若要統一為 NRL，逐列可換，**請裁** |
| Cover 三格 | 核准者／審查者／作者中文名**留空**，待 Pei 填 |
| push／GC-16 | `fetch` → `rebase` → 普通 push；GC-16 待 Security 線收尾 |
| FB-DIAG-a～s（19 項）、`asset_request.md` | 待送 Nik |

---

## §8　建議 commit

```
feat(diagnostics): CDD-11 delivery candidate — 556 TCs, CFTS004 source document registered

- R-DIAG25(amend)/R-DIAG26/R-DIAG27 landed
- CFTS004 original docx found and registered: cfts004_general_diag_20260608
- verified the index export drops no list content; byte encodings absent from CFTS004 entirely
- extension production from the original document: 0 TCs, 0 placeholders retired
- delivery workbook in 037 row order, IDs renumbered 001..556, id_map attached
- 386 placeholder rows shaded FCE4D6, Out of Scope row left unshaded
- cell-for-cell reconciliation against merged v08: zero差 outside ID and row order
- EXEC_GUIDE, coverage, placeholder_summary and placeholder_by_token shipped
- A-DIAG57/58 registered
```

pathspec：

```
features/diagnostics sources/MANIFEST.tsv docs/fw036/RULINGS.sha.tsv \
docs/fw036/lint_reports/diagnostics_*_20260917.md \
docs/fw036/lint_reports/FM-WI-FSM-036-A01*Diagnostics*.md \
docs/fw036/handoff/up/20260917_CDD-11.md \
docs/fw036/handoff/down/20260917_CDD-11.md \
docs/fw036/handoff/down/20260917_CDD-10_review.md
```

`sources/MANIFEST.tsv` 為兩線共用檔 —— 本包只**追加一列**（第 239 列），`git diff --stat` 覆核為 `1 insertion(+)`，無他線列變動。
