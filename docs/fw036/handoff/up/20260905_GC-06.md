# 上繳包 GC-06 —— 三紅先量後裁、GC-05 審閱收尾

日期：2026-09-05　層級：Tier 1 → Tier 2
對應下放包：`down/20260905_GC-06.md` ＋ `down/20260905_GC-06_addendum.md`（合併執行）

五件任務全數執行。**`docs/fw036/RULINGS.sha.tsv` 一位元未動**（R-G50(c)）。無刪除、無搬移。

> **本包之核心結果**：`canon_refs` **789 → 158**，其 `ruling` unresolved **963 → 0**。
> A-GC15 之診斷成立且可修 —— 補遺 §1 把「提案」改為「施作」是對的。

---

## §1　產物與 SHA256

| # | 檔 | bytes | SHA256 |
|---|---|---:|---|
| 1-4 | `scripts/ruling_anchor.py`（新）| 1,848 | `9d4dd6a2c9f7f42c22baf0e5465e8201588565cb7c20da4bd81f2c468077c21d` |
| 1-4 | `scripts/canon_refs.py` | 25,909 | `38bf7f1fe276fa80f64260e0795d5b21e8c0da0df3a6050b5b054e8eadc97874` |
| 1-4 | `scripts/rulings_hash.py` | 15,074 | `92ccd61b686e6f2d3938a60592978959d505ffdb3f5bfa4261d29db097bad2b9` |
| 1-4 | `tests/test_canon_refs.py` | 20,319 | `4de307650f0b96b7a8ec2ff7ca154c1caacb82795b4d9b30f16dda72a4cdce07` |
| 3-a | `scripts/gc06_driver_distraction_mv.sh`（新，**未執行**）| 1,485 | `d6d6e5b335e8838e27d7e2d5e85f09fbf1442480e6f51cc827432f4fe34d1ed6` |
| 3-b | `features/ics_management/delivered/MANIFEST.tsv` | 1,033 | `0e41c6eca4b54a70c8004bdd3e0891ac69bb069b3ddcb99b9c3507adaa1203a7` |
| 3-b | `features/ics_management/ANOMALIES.md` | 69,386 | `7614e94c598c31438d448364f0e25dcc5d2526a54d28dbc3ea7002a9d7a8e250` |
| 3-b | `features/ics_management/RULINGS.md` | 126,008 | `079029ee214a7151a428e4b7a618e026276f50383118e195ddc64c2ac778b2a7` |
| 3-c | `features/sw_update/delivered/MANIFEST.tsv` | 594 | `18f35bf196ccd448cd8c829f11f1dc8ff4eb4484ea574c77abc127ec27fc972c` |
| 3-c | `features/sw_update/ANOMALIES.md` | 25,958 | `bf8eea7a617e22d41ab4d40974ead3848dbf0d0115fa470479843350e792a634` |
| 4 | `docs/fw036/RULINGS_LEDGER.md` | 93,763 | `5d84dab15d46f3d5879bc0e787fbcaa9161cffa1b0127379583ca48703af852b` |
| 4 | `docs/fw036/ANOMALIES.md` | 5,441 | `7416e4c7e40295ef8f8c4181016dd3785440fc7a0a97005cea04a8503588d473` |
| 4 | `docs/reports/rg43_manual_20260905_judged.tsv` | 18,842 | `9c13a51eca28cb94bde6849a4b2d52106cf55d9fb03e70f0e8fbeae6cf75a194` |

**11 檔，+132 / −26。**

### 1-1　R-G50(c)：本輪工具寫入 repo 內之路徑清單

| 工具 | 旗標 | 寫入 repo 內之路徑 |
|---|---|---|
| `scripts/rulings_hash.py` ×2 | `--out <工作區>/rg_dry_gc06.tsv` 等 | **無** |
| `scripts/gates_tsv.py` | `--out <工作區>/gates_dry.tsv` | **無** |
| `scripts/canon_refs.py`／`lint_paths.py`／`gate_all.py` | 唯讀 | **無** |
| `scripts/gc06_driver_distraction_mv.sh` | **未執行** | **無** |

**清單為空。** `RULINGS.sha.tsv` 與 `GATES.tsv` 之 `git status` 逐次複核為空。

### 1-2　前提之複測

`rulings_hash --check` **PASS（763 條）**；`gate_all` 三紅：`canon_refs` **789**（下放包載 788，
差一為本包之 down 檔落地所致）、`gates_tsv` 不符、`lint_paths` 4。與下放包所載相符。

---

## §2　任務 1 —— `canon_refs` 之 `ruling` 解析面

### 2-1　先量：索引面現況

`CanonIndex.build`（`scripts/canon_refs.py:145`）以 `RE_RULING`
（`(?<![0-9A-Za-z-])R-G(?P<n>[0-9]+)(?:-[0-9]+)?(?![0-9A-Za-z])`）
掃 **圍籬外之全行**——**不是錨點法，行內提及亦算**。掃描檔只有 `CANONS` 二份：
`docs/fw036/FEATURE_ONBOARDING.md`、`docs/runtime/ASPICE_SWE6_AI_Instruction.md`。

所得 50 號（FO）：`R-G1`～`R-G30`、`R-G42`、`R-G43`、`R-G51`～`R-G68`；IN **0 號**。
逐號探測：`R-G42` ✓、`R-G51` ✓、`R-G68` ✓、`R-G30` ✓；
**`R-G31`～`R-G41` ✗、`R-G44`～`R-G50` ✗、`R-G69` ✗**。

### 2-2　unresolved 逐號歸類

`ruling` unresolved（waiver 前）**963 處**。以台帳之 `RE_ANCHOR`（`rulings_hash` 之常數）
取台帳錨點 **32 個**後比對：

| 類 | 處 | 號數 |
|---|---:|---:|
| 落「台帳有錨、FO 無」 | **963** | 19 |
| 其他號（真懸空）| **0** | 0 |

查詢式：`collect(ROOT)` 之 `kind=ruling and verdict=unresolved`，
其 `target` 與 `ruling_anchor.RE_ANCHOR` 掃台帳所得之錨點集合比對。
**即：963 處全部是同一個成因，補解析面即全消。**

### 2-3　waiver 失效列：**母體為 0**

`docs/fw036/CANON_REFS_WAIVER.tsv` 456 列，其 `kind` 分布為
**`section` 448／`item` 8／`ruling` 0**。
`target` 為 `R-G12`～`R-G29` 之列 **0 列**，故因改號而失效之 waiver 列 **0**。
**rulings 從來就沒有被 waive 過**，下放包 §二-1-3 之「改號後 waiver 鍵失效之數」答案是 0，
且其分母亦為 0 —— 不是「查了沒有」，是「這一類本就不存在」。

### 2-4　施作（補遺 §1，由提案改為施作）

**錨點常數集中一處**：新增 `scripts/ruling_anchor.py`，`RE_ANCHOR` 自
`rulings_hash.py` 逐字移入，另附 `anchor_ruling_numbers(text, prefix)`；
`rulings_hash.py` 與 `canon_refs.py` **皆自該處 import**，不各寫一份（R-G43(d)）。

**索引面增台帳**：`RULING_SOURCES = dict(CANONS, LEDGER="docs/fw036/RULINGS_LEDGER.md")`；
台帳側以**錨點法**建索引（R-G43(c)），canon 二側**維持既有取法不動**。

> **一次量測導致的方向修正，具名**：首版把 canon 側也改成錨點法（「共用同一常數」之字面讀法），
> 實測 unresolved **由 963 升為 1682** —— FO 之索引由 50 號縮成錨點數，反而更多引用解析不到。
> 補遺原文是「索引來源**增**台帳」，故改為只增來源、不動 canon 側之判準。此段留在
> `canon_refs.py` 之註內，供後手不必重犯。

**同號兩側皆有錨者判 `resolved`**：`resolve_ruling` 由「命中 >1 即 ambiguous」改為
「命中即 resolved」。實測 `R-G42`／`R-G4`／`R-G7` 三號皆轉 resolved，`ruling ambiguous` 維持 **0**。

**測試**（`tests/test_canon_refs.py` +36 行，3 支）：
台帳錨點可解析且 FO 無該號時不紅、敘述式標題（條號在括號內）**不算錨點**（範圍向 R-G9）、
同號兩側皆有錨判 resolved。另修 `Resolver` 使**台帳缺檔時視為無錨點**——
不修則最小 repo 之 31 支既有測試全部 collection error。

### 2-5　對照向（R-G7＋R-G50(b)，移出 13 檔）

| 量 | 對照向 | 交付向 | 差 |
|---|---:|---:|---:|
| `canon_refs`（waiver 後）| **789** | **158** | **−631** |
| `unresolved`（waiver 外）| 649 | **18** | −631 |
| `ambiguous`（waiver 外）| 140 | 140 | **+0** |
| `mention` | 563 | 231 | −332 |
| `ruling` unresolved | 963 | **0** | −963 |
| `section` unresolved／ambiguous | 63／748 | 63／748 | **+0／+0** |
| waiver 命中 | — | 462 處／456 列，stale **1** 列 | — |

`mention` 之下降**非行為改變**：其定義為
`[r for r in qualified if r.verdict != "resolved" and r.usage == "mention"]`
（`canon_refs.py:515`）—— ruling 轉 resolved 後即退出該桶。分子分母皆列於上表。

pytest：`10 failed, 1306 passed` → `10 failed, 1309 passed`（**紅 +0、綠 +3**）。
`ruff` 逐檔：`canon_refs.py` 5→5、`rulings_hash.py` 2→2、`tests/test_canon_refs.py` 2→2、
`ruling_anchor.py` **All checks passed**。**增紅 0**。

---

## §3　任務 2 —— `gates_tsv`

`--check` 僅輸出 `FAIL`（無 `--diff` 旗標），故以 `--out <工作區>` 重生後逐列 diff。

**現行 48 列 → 重生 55 列：新增 8、消失 1。**

| 新增（8）| |
|---|---|
| `I-cross`、`W` | `lint036` 之 profile-only 閘 |
| `features/driver_distraction/scripts/selfcheck_pilot_group3.py`、`…/selfcheck_tcs.py` | feature 級「未拆之閘集合」 |
| `features/ics_management/scripts/selfcheck_b01.py` | 同上 |
| `features/vehicle_setting/scripts/vs_sl0{1,2,3}_selfcheck.py` | 同上 |

**消失（1）：`body_kind`。**

### 3-1　判因：**不是 GC 系列所致，而且不能逕行重生**

- 新增之 6 個 feature 級列，其來源腳本之入版控 commit 為 `688c0d1`（2026-08-28）、
  `4bfa2df`（08-30）、`903745e`（09-02）—— **皆早於本 GC 系列**。
  `GATES.tsv` 之最後一次更新為 `e2d9f91`（`lint_delivery_spec` 之閘）。即**登錄簿過時**。
- **`body_kind` 之消失不是過時，是重生會把它除名**：`gates_tsv.py` 之 `DOCS_CHECKS`
  （`scripts/gates_tsv.py:42-50`）是**人手維護之枚舉**，其七鍵**不含 `body_kind`**。
  該閘之列係 commit `519a961`（T71，R-G68〔原 R-G29〕[DEFAULT]）**人手寫入 GATES.tsv**，
  產生器從不知道它。

**依 §二-2-3 之分岔**：這不是「僅登錄簿過時」——逕行重生會**除掉一支在效之閘**。
且要修得對，得在 `DOCS_CHECKS` 補 `body_kind` 再重生，而**改 gate 腳本之 code 屬 Pei 准之範圍**
（下放包前言）。故本包 **`GATES.tsv` 不動、`gates_tsv.py` 不動，上繳具名待裁**。

---

## §4　任務 3 —— `lint_paths` 4 → 2

### 4-1　3-a driver_distraction（**只出腳本，未執行**）

| 量 | 結果 |
|---|---|
| 二檔之 commit 史 | `driver_distraction_00.xlsx`：`2df23ee`(08-27) → `31665d3`(08-27) → `02b8fd4`(08-29)；`_bak`：`2df23ee`(08-27) → `98b3421`(09-02) |
| 該線有無 `[OVERRIDE R-G64]` 宣告 | **無** —— `grep "OVERRIDE R-G" features/driver_distraction/ docs/runtime/profiles/` **命中 0 檔**；`DECISIONS.md:64` 之 `Profile [OVERRIDE] clauses needed` 仍為 `[PROPOSED]` 佔位 |
| `feature.yaml` 之宣告 | `:17 workbook: "workbook/driver_distraction_00.xlsx"` —— 只是路徑宣告，**不是 R-G64 之 override 條款** |
| 該線現況 | 最後一包 `docs/handoff/22_close.md`／`docs/upstream/19_close.md` |

依補遺 §2「無宣告即無例外」→ 出 `scripts/gc06_driver_distraction_mv.sh`（23 行，`bash -n` PASS，**未執行**）。

**腳本內具名兩件**：(1) 該線**目前無 `sandbox/`**，故含 `mkdir -p`；
(2) 搬完**必須**同步改 `features/driver_distraction/feature.yaml:17`，否則該行指向不存在之路徑（R-G63）。
執行層**未代改** feature.yaml —— 它與搬移須在同一個 commit 才不留中間態，而 commit 屬 Pei。

### 4-2　3-b ics_management：**檔被改**（補遺 §3 第一款）

| 版本 | commit | 日期 | sha8 |
|---|---|---|---|
| v1 | `5f1a926` | 08-30 | `d31d81d2` |
| v2 | `b35e651` | 08-30 | **`e2ce5868`**（＝MANIFEST 所載）|
| **現行** | **`ef3ade0`** | **09-01** | **`cb5697db`** |

`ef3ade0` 之訊息為 `chore(delivered): update ICSManagement and add SWUpdate 20260830 workbook`
—— **登記後被改**。逐分頁比對（openpyxl 全格）：
`Test Case Specification 測試用例規範` **32 列相異**（含第 2 列表頭區），
其餘 8 個分頁**逐列相同**。

處置（照第一款，未自行裁）：**檔不還原**（R-TM13）；MANIFEST 之 sha 改指現行 `cb5697db…`
並於 `note` 記變更 commit 與差異；`features/ics_management/RULINGS.md` 加一節抄錄本裁決；
自報 **A-ICS138**（現場號實測最大 `A-ICS137`）。

### 4-3　3-c sw_update：**未登記**（補遺 §3 第三款）

`delivered/MANIFEST.tsv` **只有表頭、0 資料列**；該 xlsx 入版控之 commit 為 `ef3ade0`（與 ICS 同批）。
`features/sw_update/{RULINGS,DECISIONS}.md` 內 grep 交付 sha **命中 0**。

處置：補登現行 sha `4f6caa35…`；自報 **A-SU7**（現場號實測最大 `A-SU6`）。
**盲區（R-G11）**：因無另一組可對之 sha，**不能判其曾否變更**——只能說「未登記」，
不能說「未變更」。

### 4-4　結果

`lint_paths` **4 → 2**（`delivered/ sha 對照：OK`；餘 2 為 3-a，待 Pei 跑腳本）。

---

## §5　任務 4 —— GC-05 審閱收尾

**入台帳**（新節「下放包 GC-05／GC-06 之全域條文」）：
`### R-G36(b) — 機器抽取後須回讀本體首末行`（GC-05 審閱 §三）、
`### R-G43(d) — 解析面與指紋面同源`（補遺末段）。二者皆 `### R-Gnn` 錨點形式（R-G43(c)）。

**入 `docs/fw036/ANOMALIES.md`**：**A-GC14**（judged 之判側讀到截斷 context 即援引該線宣告；
另記 confidence 分布 H105/L6 vs 實測 H104/L7 之差一）、**A-GC15**（未量測之因果兩層轉抄）。
共同形態句由「十三筆」改「十五筆」，並加「以規則代量測」「轉抄未量測之因果」兩型。

**`judged.tsv` row 7–16**：`judged_side` 改為 `~~FO~~ KEEP_ANNOTATE`（R-TM13），
`reason` 補「GC-05 審閱重判：該行即撞號紀錄本身（A-BLM8「R-G12 ~ R-G20 全段撞號」），改號即毀證，同 row 17–19」。**10 列**。

**乾跑（`--out <工作區>`，不寫表）**：763 列／731 ID → **765 列／733 ID**；
`R-G36(b)` 與 `R-G43(d)` **各 1 列在表**；新增 ID 恰為此二者，消失 ID **無**；
撞號組數 **28 → 28（增 0）**。

---

## §6　自檢

| # | 項 | 結果 |
|---|---|---|
| 1 | `RULINGS.sha.tsv`／`GATES.tsv` 未寫 | **PASS**（1-1 節，清單為空）|
| 2 | 無刪除、無搬移 | **PASS** —— 無 `D`／`R` 列；mv 腳本未執行 |
| 3 | 錨點常數只有一份 | **PASS** —— `grep -c "RE_ANCHOR = re.compile"` 全 repo = 1（`ruling_anchor.py`）|
| 4 | pytest 增紅 | **+0**（綠 +3）|
| 5 | `ruff` 增紅 | **+0**（逐檔同數）|
| 6 | 全稱斷言附查詢式與分子分母 | **PASS**（2-2／2-3／4-1／4-3 節）|
| 7 | 對照向回報被移出檔數 | **PASS** —— 13 |
| 8 | 改 gate 腳本之 code | **未做**（`gates_tsv.py` 未動，§3-1）|

---

## §7　自報

### 7-1　閘之四項變化，逐支歸因

| 閘 | 對照向 | 交付向 | 歸因 |
|---|---|---|---|
| `canon_refs` | 789 | **158** | 本包任務 1-4（R-G43(d)）；`ruling` unresolved 963 → 0 |
| `canon_refs`（**含本上繳包**）| 789 | **165** | 本檔自身之 `section` 引用（自指效應，同前輪）。**`ruling` unresolved 由 0 變 10，非本包所致** —— 逐處實測皆落在`docs/fw036/handoff/down/20260905_GC-07.md`（本包作業期間落地，其 `R-G70`／`R-G71` 尚未入台帳）；本上繳包內之 R-G 號逐號重驗**全部 resolved** |
| `rulings_hash` | **PASS** | **FAIL** | 本包於台帳加了 `R-G36(b)`／`R-G43(d)` 二條 → 指紋表需再重生一次（乾跑已證 +2 列、撞號 +0）。**屬 Pei 之作業**，非缺陷 |
| `lint_paths` | 4 | **2** | 3-b／3-c 之改表；餘 2 待 Pei 跑 mv 腳本 |
| `gates_tsv` | FAIL | FAIL | **未動**（§3-1 待裁）|

### 7-2　本輪自造之缺陷一件（交付前改正，具名）

任務 1-4 首版把 canon 側之 ruling 索引也改成錨點法，**unresolved 反升為 1682**。
成因：把補遺「錨點正則與 `rulings_hash` 共用同一常數」讀成「兩側都改用錨點法」，
而該句所修飾者是**新增之台帳來源**，前句已明寫「索引來源**增**台帳」。
量到數字變壞才回頭重讀原文。**改對後之理由已寫入 `canon_refs.py` 之註**，供後手不必重犯。

### 7-3　未做且具名

| 項 | 為何未做 |
|---|---|
| `GATES.tsv` 重生 | 會除名在效之 `body_kind` 閘；正解須改 `DOCS_CHECKS`，屬 Pei 准（§3-1）|
| `RULINGS.sha.tsv` 再重生 | Pei 之作業；本包只乾跑 |
| 跑 `gc06_driver_distraction_mv.sh` | `git mv` 屬 Pei（R-G5）|
| `features/driver_distraction/feature.yaml:17` 之路徑同步 | 須與搬移同一 commit（4-1 節）|
| stale waiver 1 列之整批減 | 補遺 §1 末：Pei 於審閱後依 R-G57「只減不增」處理 |
| `?6` 未追蹤之 commit | 補遺 §4：GC-06 上繳後一併，命令於審閱給 |

### 7-4　請裁三項

1. **`gates_tsv` 之 `body_kind`**（§3-1）：補 `DOCS_CHECKS` 再重生／維持人手列／除名，三擇一。
2. **`driver_distraction` 之 `feature.yaml:17`**（4-1 節）：確認由 Pei 於同一 commit 內改。
3. **`A-SU7` 之盲區**（4-3 節）：sw_update 交付本無可對之 sha，是否需向該線補一份交付紀錄。
