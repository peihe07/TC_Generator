# 上繳包 SEC-10 —— 合併 SEC-08／09 裁定之 Revise → `security_v05`

執行層，2026-09-17。對應下放包 `down/20260917_SEC-10.md`。

**結論**：v05 成。四條裁定落檔、`--gate` 通過；`H` 由 1 → **0**（引文豁免，反例已驗）；
Remarks 增行 **29 行**（19 `conflict:` ＋ 8 `scope:` ＋ 2 `rows→items`）落在 **23 個 remarks 格**；
`test_item` 2 格（CP-008 混合型尾綴）；**無新列**（`NEW-ID-01` 之 grep 命中 0 → 不拆）。
v05 lint `U=0`／`H=0`／門檻 0／`W=4`；selfcheck 0；coverage 51／19、佔位 96／11 皆不變。
**§5 升級條件四條全不成立。**

三項須分析層知悉（§8）：`FEATURE_EXEMPT` 無法表達位置條件故另立表、`AXIS_GAP` 實測 **2**（下放包預期 ≤1，其算式合計僅 46）、
審計之指派路線改以 `new_test_ids` 為權威面（否則 `NEW-NET-01` 無法改判 `NO_SWE1`）。

---

## 一、commit（§4-1）

下放包令「開工前先 commit，三支」。實際落 **四支**（`lint036.py` 之改動與 SEC-09 審計分列，
另 docs 一支；SEC-06／07 之產物本來也還在工作樹，與 SEC-08 併入第一支——
三包之改動疊在同一批生成腳本內，無法乾淨拆開）：

| hash | 訊息 |
|---|---|
| `9890991` | `feat(security): SEC-06~SEC-08 — Revise v03, PENDING resolution, X-i document review (R-SEC20), asset placeholders (R-SEC21), security_v04` |
| `9553b5a` | `feat(lint036): accept R-SEC21 asset placeholders and R-SEC20 document-review steps` |
| `847d497` | `feat(security): SEC-09 — CCVR SYS2_Mapped landing audit (conflicts, scope notes, new test cases)` |
| `1044737` | `docs(security): SEC-06~SEC-09 packages, submissions and lint reports` |

四支皆帶 pathspec，未夾帶並行 session 之 `features/diagnostics/`／`CDD-01` 檔案。
`v03`／`v04` 三本 xlsx 與全部 JSON 已進版控，不再只存在工作樹。

## 二、裁定落檔（§4-2）

| Ruling（錨點 id）| body sha8 | 行數 | 標題 |
|---|---|---:|---|
| `R-SEC20(amend)` | `6329b3bf` | 11 | 適用面之更正與引文豁免（Pei 准 SEC-08 review §一 #1／#3／#4）|
| `R-SEC7(amend2)` | `c3d0f823` | 8 | 通道與觀察面增列 DOC（Pei 准 SEC-08 review §一 #2）|
| `R-SEC21(b)` | `44b0d0f2` | 7 | (amend) 第四型「步驟型」（Pei 准 SEC-08 review §一 #5／#6）|
| `R-SEC22` | `b71e8da1` | 16 | CCVR 衝突與範圍之 Remarks 定式（Pei 裁；SEC-09 review §一）|

- 四條之 fenced 區塊逐字取自下放包 §1，未改一字；各附執行層回報段。
- `python3 scripts/rulings_hash.py --target features/security/RULINGS.md --gate` → **通過**（exit 0）；
  `RULINGS.sha.tsv` 由 28 → **32 錨點**。既有裁定 sha 全部不變
  （`R-SEC20 = 9d24b57a`、`R-SEC21 = bde6bbf3`、`R-SEC19 = e1fdd7fd` …）。
- 命名：`R-SEC7(a)(c) amend2` 之錨點取首個括號，故 id 為 **`R-SEC7(amend2)`**（避開既有之 `R-SEC7(c)`）；
  `R-SEC21(b) amend` 之 id 為 **`R-SEC21(b)`**。兩者皆為新 id，無重複。

## 三、`H` 之引文豁免（§2／§4-3）

### 3-1　實作 —— **未用 `FEATURE_EXEMPT`（請裁）**

`FEATURE_EXEMPT` 之語意是「整項自 `check_order()` 移出」，**無從表達位置條件**；
把 `H` 放進去會連引號外之命中一起吞 —— 正是 §5 第二條所禁。故另立：

```python
# R-SEC20(amend)(c)（SEC-10）：**部分**豁免 —— 只吞「命中位置落於 `"…"` 內」者。
# 施檢面之判定沿用檢查 B 之同一機制（`quoted_spans()` ＋ `inside_spans()`）。
FEATURE_EXEMPT_QUOTED: dict[str, list[str]] = {"security": ["H"]}
```

`check_row()` 之 H 段（唯一改動處）：

```python
    h_quoted = profile is not None and "H" in FEATURE_EXEMPT_QUOTED.get(profile, [])
    for m in RE_H.finditer(er):
        if h_quoted and inside_spans((m.start(), m.end()), spans):
            continue
        ...
    if profile:
        for m in RE_H_RELATION.finditer(er):
            if h_quoted and inside_spans((m.start(), m.end()), spans):
                continue
            ...
```

`spans` 即 B 判項已算好之引號範圍（同一列、同一 ER），不另算一套。

### 3-2　反例（§5 第二條之驗證）—— 新測試 `tests/test_lint036_h_quoted.py`（8 項全過）

| 測試 | 斷言 |
|---|---|
| `test_quoted_relation_word_is_exempt_under_security` | ER 之 `align with` 在 `"…"` 內 → `H` **0** |
| `test_bare_relation_word_still_flagged_under_security` | **同一模糊語在引號外 → `H` 仍報 1**（反例）|
| `test_mixed_line_reports_only_the_bare_hit` | 一列兼有引文內外兩處 → 只報引號外那處 |
| `test_exemption_is_security_only[None/camera/power]` | 非 security（含無 profile）行為不變 |
| `test_reuses_check_b_quote_mechanism` | 引號範圍沿用 B 之 `quoted_spans()`／`inside_spans()` |
| `test_h_is_not_wholly_exempt` | `H` **不在** `FEATURE_EXEMPT["security"]`，且仍在 `check_order("security")` 內 |

`tests/test_lint036_z.py` **27 項全過**（未受影響）。

### 3-3　回歸與 `H` 之變化

| 呼叫路徑 | 判 | 今日報告 `sha256[:16]` |
|---|---|---|
| `lint036.py features/power/delivered/pm_29.xlsx`（無 profile）| ✅ 與 09-16 報告 **byte 相同** | `b651bd35a95dfc3a` |
| `--profile camera` | ✅ byte 相同 | `cf6b5d4e7b1a1e1f` |
| `--profile power` | ✅ byte 相同 | `89eeee086a574b09` |

`H` 之變化：同一本 **`security_v04.xlsx`** 在新 lint 下由 `H=1` → **`H=0`**（豁免生效，非改本）；
`security_v05.xlsx` 亦 `H=0`。被豁免之命中為 `NR1L-CP-012` ER 2 之
`… satisfy "logs must align with Logdog requirements (Error-level only for defects)"`。

## 四、Remarks 增行（§3-1／§4-4）

### 4-1　逐 TC 表（29 行／23 格）

| 增行 | 內容 | TC | 數 |
|---|---|---|---:|
| `conflict:` C1＋C4 | `conflict: 543/546 vs 226; 517 vs 520 — root & CA lifecycle; unresolved per CCVR Mapping notes row 12` | `NR1L-CP-015`／`-016`／`-019`／`-020` | 4 |
| `conflict:` C2 | `conflict: 344 vs Auth-Prog item 7 — rollback; unresolved per CCVR Mapping notes row 13` | `NR1L-SWDL-001`~`-006` | 6 |
| `conflict:` C3 | `conflict: 376/509/511 vs 510 vs 514 — key strength; unresolved per CCVR Mapping notes row 14` | `NR1L-KI-016`~`-019` | 4 |
| `conflict:` C5 | `conflict: 529/383/532/539 — DCL not deployed; unresolved per CCVR Mapping notes row 10` | `NR1L-CP-007`~`-010` | 4 |
| `conflict:` C6 | `conflict: 562~565 — CSR format evidence pending; unresolved per CCVR Mapping notes row 15` | `NR1L-ECUC-010` | 1 |
| `scope:` | `scope: service-level validation; reprogramming rejection owned by SWDL (CCVR Cert Val CS.98 rationale)` | `NR1L-CP-002`／`-004`／`-006`／`-007`／`-008`／`-009`／`-010`／`-023` | 8 |
| `rows`→`items` | `source: CCVR Auth-Prog CS.93 evidence items 3/5/6/7/8; …`（唯一之**改**行，R-SEC22(c)）| `NR1L-SWDL-005`／`-006` | 2 |
| **合計** | | **23 個相異 TC** | **29** |

`NR1L-CP-007`~`-010` 同時落 `conflict:`（C5）與 `scope:`，兩行併存 —— 故 29 行落在 **23 格**
（下放包 §4-4 寫「`remarks` 29 格」，實為 29 **行**／23 **格**，見 §8-3）。

實作面：`conflict:` 依 **SWE1 列**指派（`CONFLICT_NOTE` 表，SwdlSecureLib 五列迴圈生成），
`scope:` 依「CP 之負向 Design Method 或 `req_id ∈ {CP-004, CP-005}`」判 —— 與 SEC-09 審計之口徑同一條規則，
故產出之 8 個 TC ID 與 R-SEC22(b) 所列**逐一相同**。兩者皆**附加於既有 Remarks 之後**，未改既有行。

### 4-2　v04 → v05 逐格 diff（100 TC 同序）

| 欄 | 相異格數 | 成因 |
|---|---:|---|
| `remarks` | **23** | 上表 29 行 |
| `test_item` | **2** | `NR1L-CP-017`／`-018` 之尾綴 `(document review)` → `(document review + log observation)`（R-SEC20(amend)）|
| 其餘 15 欄 | **0** | —— |
| 合計 | **25 格** | 逐格程式比對，無任何非預期增刪（比對器另檢查「新增／移除之 Remarks 行」全部落在上表之式） |

**無新列**（§3-4 之 grep 命中 0）；`coverage.tsv` 51／19、`placeholder_summary.tsv` 96 列／11 token
與 v04 **逐字節相同**（`2b743e69…`／`0f345db2…` 未變）；`security_batch2_v05.xlsx` 之 sha 與 v04 **完全相同**
（`310a3769814997a2`）—— batch 2（SAM／Log Encrypt）本包未觸及，可作旁證。

## 五、`NEW-ID-01` 之 grep 與審計重分類（§3-2／§3-4／§4-5）

### 5-1　grep（§3-4）

對 **ECUCert 037 十三列**之 `Requirement Description`（D 欄）與 `Verification Criteria`（R 欄，本書因多一欄
`Sub Categorization` 而位於索引 17）逐列 grep `reboot|recovery|restart|power cycle|power-cycle`（大小寫不計）：

```text
ECUCert 資料列 13　命中 0
```

→ 走「**無命中**」分支：**不拆 sibling**，登 `A-SEC-9`。TC 數、coverage、佔位數皆不變。

### 5-2　`trace_matrix.tsv` 之 `NEW-NET-01` 清除（A-26）

`build_trace_matrix.py` 之聚合處加抑制（非只改 tsv，否則重生即回填）：

```python
                # A-26（SEC-09 review）：`NEW-NET-01`（對外 TLS）之 SYS2 清單與
                # SwdlSecureLib 諸列偶然交集，題域不同 —— 清除該指派（R-SEC22 併案）。
                ids = [i for i in m["new_test_ids"].split()
                       if not (i == "NEW-NET-01" and row["swe1_id"].startswith(
                           "SWE1-SRA-SECURITY-SWDL-"))]
```

重生後 `trace_matrix.tsv` 之 diff **只有 4 列**（`SWE1-SRA-SECURITY-SWDL-001`／`-002`／`-003`／`-005`），
且各列只少掉 `NEW-NET-01` 一個 token：

```text
-NEW-CERT-11;NEW-NET-01;NEW-UPD-01   +NEW-CERT-11;NEW-UPD-01
-NEW-CERT-11;NEW-NET-01              +NEW-CERT-11        （×3）
```

全表殘留 `NEW-NET-01` 之列數 = **0**。新 sha `947483cfae138f2c`。

### 5-3　`audit_newtc.tsv` 重分類

| 類 | v05 | 說明 |
|---|---:|---|
| `COVERED` | **32** | 30 ＋ `NEW-BOOT-02`（改判：KI-003 之 non-platform 負向 sibling）＋ `NEW-OS-01`（`NR1L-KI-022`，avc denied）|
| `AXIS_GAP` | **2** | `NEW-CERT-02`（boundary；→ A-SEC-8）、`NEW-ID-01`（environment；→ A-SEC-9）|
| `NO_SWE1` | **12** | 11 ＋ `NEW-NET-01` |
| `DEFERRED` | **1** | `NEW-UPD-01` → `SWE1-SRA-SECURITY-SWDL-001`（D 群）|
| 合計 | **47** | |

新增 `disposition` 欄記其處置（`A-SEC-8`／`A-SEC-9`／`A-26`）。
下放包預期 `AXIS_GAP ≤1`，惟 32＋1＋12＋1 = **46 ≠ 47** —— 兩條已裁「不拆」之 NEW 仍以量測面計為 `AXIS_GAP`，
故實測 **2**，見 §8-2。

### 5-4　審計三表重跑（對象為 v05，`cc07dfb1fea8188a`）

| 審計 | v04 | **v05** |
|---|---|---|
| §1 六衝突落 Remarks | C1~C6 全 `N` | **C1~C6 全 `Y`** |
| §2 scope 註 Y | 0／8 | **8／8** |
| §2 越權斷言 | 0 | **0** |
| §3 四類 | 30／5／11／1 | **32／2／12／1** |

`audit_conflicts.tsv` `e3a8c44ebdd321a3`／`audit_scope.tsv` `4d7097f8c8f4a005`／`audit_newtc.tsv` `cc516fa462d51384`。

## 六、v05 產物與 lint（§4-6）

| 檔 | sha256[:16] |
|---|---|
| `sandbox/merged/security_v05.xlsx` | `cc07dfb1fea8188a` |
| `sandbox/batch1/security_batch1_v05.xlsx` | `85534f3dd036b248` |
| `sandbox/batch2/security_batch2_v05.xlsx` | `310a3769814997a2`（＝ v04，未觸及）|
| `sandbox/delivery/FM-WI-FSM-036-A01 …_SWQT_Security_20260917.xlsx` | `2a61a957fafb05b5`（同名覆蓋）|
| `data/trace_matrix.tsv` | `947483cfae138f2c` |
| `data/placeholder_summary.tsv`／`coverage.tsv`／`delivery/asset_request.md` | `2b743e69…`／`0f345db2…`／`18eeae20…`（皆同 v04）|

```text
v05 lint：A=0 B=0 C=0 D=0 E=0 F=0 G=0 H=0 I=0 I-sibling=0 J=0 K=0 L=0 M=0 N=0
          P=0 Q=0 R=0 T=0 U=0 V=0 W=4 X=0 Y=0 SC=0 SS=0
交付候選本 lint：與合併本逐項相同（H=0、W=4，其餘 0）
selfcheck：batch1 0／batch2 0；pilot01 反證 66（判準未放寬）
```

`W=4` 為既裁之待人裁段，與 v01~v04 同。`surgical_save`：三本 `members = 48`、`dv_counts` 不變；
交付本另差 `sheet3`／`sheet5`（封面 11 格，日期仍 `2026-09-17`）。

## 七、`ANOMALIES.md` 三條全文（§4-7）

新增一段「回饋 RD 之 ANOMALIES —— CCVR 落地審計（SEC-09 審計；SEC-10 落檔，2026-09-17）」：

```text
[A-SEC-8]  CCVR `NEW-CERT-02`（Audit minimal TLS CA list）要求「清出未使用／已到期／安全性過時之 root，
           並留定期稽核紀錄」；037 `SWE1-CertProvider-007`（Secure and Persistent Storage for Trusted
           Certificates）與 `-009`（Product and Application Dependent Configuration）之 Description／
           Verification Criteria 無到期、稽核、清理之語意（逐字查無 expire／audit／obsolete）。
           影響：該軸（boundary）在本 feature 無 037 依據，拆 sibling 即越 037（IN §8.4.2）。
           處置：不拆；回饋 037 作者評估是否補 SWE1 要求。

[A-SEC-9]  CCVR `NEW-ID-01` 要求「recovery 之後仍不得生效」；對 ECUCert 037 十三列之 Requirement
           Description 與 Verification Criteria 逐列 grep reboot／recovery／restart／power cycle
           （大小寫不計）命中 0。
           影響：持久化／recovery 軸（IN §8.3）在 037 無載，無從逐字落地。
           處置：不拆（SEC-10 §3-4 之「無命中」分支）；回饋 037 作者。

[A-SEC-10] CCVR `New test cases` 47 條中 12 條落不到 037 任何列（NEW-CERT-10、NEW-REV-05、NEW-BOOT-01、
           NEW-DIAG-01、NEW-NET-01、NEW-NET-02、NEW-FW-01、NEW-FW-02、NEW-BT-01、NEW-WIFI-01、
           NEW-DATA-01、NEW-PROC-01）。
           影響：題域落在網路／藍牙／Wi-Fi／OS／診斷權限／流程治理，非 Security 六元件之射程；
           SWE.1 層之覆蓋缺口，不是 SWE.6 之漏寫。
           處置：不由 Security 承接；整份清單回饋 RD／037 作者
           （`data/audit_ccvr/audit_newtc.tsv` 之 `class = NO_SWE1` 十二列）。
```

## 八、自報錯誤（§4-8）

1. **`H` 之豁免未用 `FEATURE_EXEMPT`（超出 §0 之字面授權）**：見 §3-1。
   `FEATURE_EXEMPT` 是「整項移出檢查序列」之開關，放入 `H` 會使引號外之命中一併消失（§5 第二條）。
   故另立 `FEATURE_EXEMPT_QUOTED`，語意為「位置條件之部分豁免」。請裁此命名與位置是否可。
2. **`AXIS_GAP` 實測 2，下放包預期 ≤1**：32＋1＋12＋1 = 46，少一條；兩條已裁「不拆」之 NEW
   （`NEW-CERT-02`／`NEW-ID-01`）在量測面仍是「軸未拆」，故仍計 `AXIS_GAP`，另以 `disposition` 欄
   記其已裁之處置。若分析層要它們改計為別類（例 `ACCEPTED_GAP`），請明示，執行層不自改分類名。
3. **`remarks` 之「29 格」實為 29 行／23 格**：CP-007~010 四個 TC 同時落 `conflict:` 與 `scope:`。
   本包照行數落地，格數如實回報。
4. **審計之指派路線改動**：原 `swes = via_new or via_sys2`（`new_test_ids` 為主、SYS2 反查為備援）。
   A-26 清除 `NEW-NET-01` 之指派後，備援路線會把 SWDL 四列**再填回來**，`NEW-NET-01` 就改判不成 `NO_SWE1`。
   故改為 `swes = via_new`（權威面）＋ SYS2 反查僅留 `swe1_via_sys2_only` 欄作交叉驗證
   （SEC-09 §5-1 已證兩路一致、備援未曾增列，故此改動不影響其餘 46 條）。
5. **一次函式名覆寫（已修，未影響產物）**：實作 `H` 豁免時我新寫了 `quoted_spans()`／`in_quoted()`，
   與 `lint036.py` 既有之 `quoted_spans()`（檢查 B 用）**同名**，後定義者覆蓋前者。
   自寫之測試立刻抓到（span 值不符），改為**沿用既有** `quoted_spans()` ＋ `inside_spans()`，
   重複定義已刪。教訓：加新 helper 前先 grep 既有同名。
6. **`ANOMALIES.md` 既存重複段**：「回饋 RD 之 ANOMALIES（`_D` §1.3，非裁定）」與其下之三列
   在檔內**出現兩次**（第 28 行與第 47 行起），為本包之前即有。本包**未動**該兩段，
   只在檔末另起一段；是否合併請分析層裁。

## 九、§5 升級條件之逐項判定

| 條 | 判 |
|---|---|
| v04→v05 diff 超出 §4-4 所列範圍 | **不成立**：25 格（`remarks` 23 ＋ `test_item` 2），逐格比對無非預期增刪，無新列 |
| `H` 豁免使非引文內之命中被吞 | **不成立**：`test_bare_relation_word_still_flagged_under_security` 與 `test_mixed_line_reports_only_the_bare_hit` 為反例，皆過 |
| 3-4 拆列時 037 命中句無法逐字引用 | **不適用**：grep 命中 0，未拆列 |
| lint 回歸 sha 改變 | **不成立**：三路徑與 09-16 報告 byte 相同 |

## 十、git

未執行 commit／push（本包產物尚在工作樹）。建議：

```text
feat(security): SEC-10 — R-SEC20/7/21 amends, R-SEC22 CCVR notes, security_v05
feat(lint036): exempt quoted verbatim requirements from H under the security profile
test(lint036): cover the quoted-requirement H exemption
docs(security): SEC-10 package and submission
```
