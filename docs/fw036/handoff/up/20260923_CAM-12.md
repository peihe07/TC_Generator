# 上繳包 CAM-12 —— CAM-11 審閱落檔 ＋ batch03b 修正 ＋ batch03c（`-004`／`-006`／`-019`，36）

對應下放包：`docs/fw036/handoff/down/20260923_CAM-12.md`
對應審閱：`down/20260923_CAM-11_review.md`
執行層 session：延用，2026-09-23。**git 未 commit**；未寫回工作簿；`pilot01/` 未動。

> **升級條件（下放包 §5）**
> 1. batch03c > 40 → **未觸發**（36）。
> 2. `-004` 任一 DTC 之名稱／值於 SYS2 只見於 037 → **條文所設之情形不成立，但缺值成立**：
>    DTC 之碼值**在 037 亦無** —— 來源只寫「as defined in the DTC Criteria Matrix」／
>    「as listed in the "TLM Diagnostic Requirement" document」，兩份文件皆不在素材。
>    **14 列標 `PENDING: DR-CAM-q`**（新開）。
> 3. 摘句不可行且不可拆 → **未觸發**：七列摘句全部達成（見 §3-3）。

### R-G62(b) 之觀察依據

起點前之最後他方寫入：`down/20260923_CAM-11_review.md` 與 `down/20260923_CAM-12.md`。
本 session 起點後至落檔止，`features/camera/` 下未觀察到他方寫入。
**併行改動**：`features/comfort/delivered/…ComfortHMI_20260907.xlsx` 之刪除仍未被該線 stage
（其新本 `…20260922.xlsx` 已入版），本包未觸及。

---

## §1　落檔

### 1.1　profile

| 節 | 內容 |
|---|---|
| **§7.2（擴）** | 加**第一步**：「以來源措辭逐字掃 PROXI 表／以**訊號名**（非訊息名）掃 DBC」。附四個實例 —— `CAN node 27 (ASM/ASCM)` 與 `Steering_Ratio_Rack_Pinion_Type` 之 PROXI 誤判、`TRANSM2 ↔ STATUS_CCAN5`／`ENGINE1 ↔ STATUS_CCAN4`／`GE ↔ STATUS_CCAN5` 之訊息名差異、`LwsAngle ↔ LWSAngle` 之大小寫 |
| **§7.5（新）** | `PENDING` 與 lint `P` 之調和形制（賦值行維持 `= PENDING (<label>)`，說明移 `a.` 子項）|

### 1.2　DECISIONS 6-40 ～ 6-42

6-40 8b 擴用准；6-41 `-170` 依逐字、不以判讀代換觸發；6-42 `Steering_Ratio…` 之處置。

### 1.3　DR-CAM-q（新）

**DTC 之識別碼** —— `DTC Criteria Matrix`（V2 所引）與 `"TLM Diagnostic Requirement"`（V42 所引）
兩份文件不在素材；ER 只判「有／無 DTC」而不指名碼值。**級：高**（`Diagnostics` 組交付前必結）。

**本輪無新條文**（`RULINGS.md` 未動）。

---

## §2　batch03b 修正

### 2.1　重掃結果表（審閱 §二-1，以**訊號名**為字串）

| TC | 來源之訊息.訊號 | 掃描字串 | 命中 | 處置 |
|---|---|---|---|---|
| `-152`／`-153` | `STEERING1.LwsAngle_SCCM` | `LwsAngle_SCCM`（含不分大小寫）| **0** | **維持 PENDING: DR-CAM-f** |
| | | `LwsAngle` | `EPS_FD_1.LwsAngle`（`BO_ 264`，FDCAN8）| **不代換** —— 該訊號為 DT27 之來源（`V2-745`／`-154`），名稱亦不同（§8.4.1）|
| `-175` | `TRANSM2.ShiftLeverPosition` | `ShiftLeverPosition` | **`STATUS_CCAN5`（`BO_ 998`，637MCA）** | 改寫；PENDING 撤除 |
| `-176` | `ENGINE1.ReverseGearSts` | `ReverseGearSts` | **`STATUS_CCAN4`（`BO_ 996`，637MCA）**，`VAL_ 0 "Not_Inserted" 1 "Inserted" 3 "SNA"` | 改寫；PENDING 撤除 |
| `-178` | `GE.LwsAngle` | `LwsAngle`（不分大小寫）| **`STATUS_CCAN5.LWSAngle`（`BO_ 998`，637MCA）** | 改寫；PENDING 撤除 |

三列改寫者：**verbatim 逐字不改**（R-13），procedure／ER 改以 DBC 之承載訊息書寫，
**`remarks` 欄**（工作簿 Remarks 之來源）註 `Source names the message X; the DBC carries <Sig> in Y (BO_ n). See DR-CAM-f.`

> **`-152`／`-153` 維持 PENDING 之根因（新發現）**：`forms/` 只有四本 DBC，
> 其中**唯一之 FD-CAN8 為 `PDT27_E2A_R1_FDCAN8.dbc`（DT27 側）** ——
> **HDCC27 之 FD-CAN8 DBC 不在素材內**，`STEERING1` 很可能只是落在該缺本。
> 建議 DR-CAM-f 之描述擴及此本。

### 2.2　`-164`（`Steering_Ratio_Rack_Pinion_Type`）

以同義字（`Steering`／`Rack`／`Pinion`／`Ratio`）重掃 Atl-Hi 三本 —— **查得**：
PROXI byte 88 bit 0–1，三本 **row 410**，值 `0=Type_1 (Rebel 144.5" WB, 55 C-Factor…)`，
與來源之參數名**逐字相符**。Pre-Condition 改
`PROXI Steering_Ratio_Rack_Pinion_Type = 0 (Type_1)`，**不併入 DR-CAM-n、不標 PENDING**。

### 2.3　`-170`（審閱 §二-2 之退回）

procedure 改回停送 **`STATUS_CCAN3`**（依 `a)` 子句之逐字條件），
reasoning 保留「疑為誤植，RDF-08」，`remarks` ＝
`Condition per source text; suspected typo, see RDF-08`。
**`STATUS_CCAN5` 缺失 → SNA 之行為無逐字來源，不生成**，待 RDF-08 回覆。

### 2.4　`remarks` 欄之新增

json 新增可選 `remarks` 欄；`lint_batch.py` 於組簿時寫入 **`AH` 欄**（Remarks 備註）；
`render.py` 於 `.md` 末加 `## remarks` 區塊。本輪四列有值（`-152`／`-153`／`-170`／`-175`／`-176`／`-178`，共六列）。

---

## §3　batch03c 生成（36 列，`NR1L-RVC-179`～`-214`）

### 3.1　產物與分佈

| SWE | 列數 | TC | Test Set |
|---|---:|---|---|
| `SWE-CAM-004` | 17 | `-179`～`-195` | `Diagnostics` |
| `SWE-CAM-006` | 17 | `-196`～`-212` | `Video Pipeline` |
| `SWE-CAM-019` | 2 | `-213`／`-214` | `Video Pipeline` |

### 3.2　下放包 §3 各要點之落實

| 要點 | 落實 |
|---|---|
| DTC 讀取以散文、不造命令 | 14 列皆寫 `Read the DTC list with the diagnostic tool and check the DTC state`（profile §7.3）|
| `systemStatus.*` set／clear 各一列 | Atl-Hi 六列（`-179`～`-184`）；**V3 三列（`-186`～`-188`）為一句涵蓋兩側**，故以四步走完 True → False 一輪而不拆（條文結構相異）；V42 五列（`-190`～`-194`）|
| `V42-590` 同句記 covered_by，不造清除側 | `-191`（`V42-584`）之 reasoning 具名；RDF-10 承接該覆蓋缺口 |
| Out of Scope 之 RVCM 端 acquire 不作錨 | 未取 |
| `V42-210` 只驗依 `Radio_Display_Type` 縮放 | `-200`；PROXI 值取 `Promaster_ATL_MI` **row 801** 之 `Y = 1`（`1 = 7" 1280x768`）|
| 五列摘句 | 實際**七列**（另含 `-189`／`-206`），見 §3-3 |
| `dumpsys` 新增列入 `bench_verify.md` | 本批未用 `dumpsys`；改列入**兩項新風險**（亮度治具、LVDS 影像鏈路插拔）|
| `-019` 時限值有則寫無則不造 | `-213`：來源無時限值，Delay 設 Off 使「立即停止」可判，ER 只判影像消失 |

### 3.3　摘句表（七列，皆為保序子序列）

| TC | 來源 | 原 | 摘後 | 摘法 |
|---|---|---:|---:|---|
| `-189` | `V33-429` | 65 | **33** | 刪末段之 `Rear_Camera_Enable.Info="TRUE"` 顯示子句 |
| `-199` | `V3-533` | 69 | **33** | 刪回復側子句（由 `-198` 之 Atl-Hi 對應承接語意）|
| `-201` | `V42-226` | 59 | **41** | 刪 `ENGINE1`／`MTX` 之第二分支 |
| `-205` | `V42-263` | 73 | **47** | 刪 `ENGINE1` 分支（含其右中括號）、`AND [ (the signal`、點火之其餘兩個列舉值 |
| `-206` | `V42-266` | 51 | **43** | 刪 `RVC_ACTIVE` 之設定子句 |
| `-210` | `V42-219` | 57 | **31** | 刪 stop-copy 與兩個旗標之設定子句 |
| `-211` | `V42-592` | 58 | **32** | 刪 `Rear_Camera_Enable.Info` 與警示文字之兩子句 |

> `-205` 之摘句**兩度返工**：初稿刪 `)]` 之左半而留下孤立之 `]`（非原句 token），
> 第 4 項攔下；改刪含 `)]` 之整段後為 51 token（逾 1），再刪點火之兩個列舉值才 47。
> **`)]` 一類之黏合符號須整體刪**，記入自報。

### 3.4　標定值（本批所用，皆實測）

| 常數 | 值 | 座標 |
|---|---|---|
| `RESPONSE_TIME` | **2,0 sec** | V42 §1.14.1 `-679`／`-680`／`-683` |
| `Time_POWER` | **500 ms** | V42 §1.14.1 `-739`／`-740`／`-743` |
| `Tpower`（V42）| **5 sec** | V42 §1.14.1 `-703`／`-704`／`-707` —— 與 V2／V3 同值 |
| `TIME_W_RVC`（V42）| **2,5 sec** | V42 §1.14.1 `-667`／`-668`／`-671` |

前三者宜補入 profile §9 常數表（本包未自行補，歸 Pei）。

### 3.5　lint ＋ selfcheck（九目錄）

```
pilot02 （10）U=1  I-cross=10        batch02c（23）J=3 U=8  I-cross=23
batch01 （23）U=4  I-cross=23        batch03a（32）J=1 U=2  I-cross=32
batch01b（13）U=14 I-cross=13 X=3    batch03b（29）J=6 U=3  I-cross=29
batch02a（26）J=5  U=4  I-cross=26   batch03c（36）J=7 U=17 I-cross=36
batch02b（22）U=2  I-cross=22
ERROR 類（A/B/C/D/E/F/G/H/I/K/L/M/N/P/Q/R/T/V/W/Y/Z）九目錄皆 0；`J` 合計 22 列為 profile §5.1 之逐字豁免
selfcheck_camera（九項，214 列）：1/2/3a/3b/4/5/6/7/8a/8b 命中皆 0（常數錨 2、合一錨 2）
```

**batch03c 初稿之 `A`=6（`check whether`）／`X`=2（`screen`）／`L`=1（`-205` 51 token）皆已修正。**

### 3.6　PENDING 列表（batch03c，`U` = 17）

| DR | 列數 | TC |
|---|---:|---|
| **DR-CAM-q**（新）| 14 | `-179`／`-181`／`-183`（set 側）／`-186`／`-187`／`-188`／`-189`／`-190`／`-192`／`-194`／`-195`／`-197`／`-199`／`-211` |
| DR-CAM-f | 2 | `-201`／`-205`（`TRANSM2`）|
| DR-CAM-i | 1 | `-204`（`Ignition_Off` 之 `CmdIgnSts`）|

---

## §4　`coverage.tsv` 現況（下放包 §4-3）

| disposition | 列數 | TC |
|---|---:|---:|
| **PRODUCED** | **18** | **214** |
| `No TC — all sources delegated …`（R-CAM16）| 2 | 0 |
| `No TC — all 12 sources are VF617_V5`（DR-CAM-a）| 1 | 0 |
| `PLANNED — HMI Overlays 尾批` | 4 | 0 |
| **合計** | **25** | **214** |

**A 本只剩 `HMI Overlays` 一組未生成**（`-021`／`-022`／`-023`／`-025`）。

---

## §5　自報錯誤

1. **CAM-11 上繳 §3-3／§4-2 之「`Steering_Ratio_Rack_Pinion_Type` 於 Atl-Hi 三本 PROXI 掃描零命中」
   是假的 —— 我沒有跑那個掃描就寫了結論。** 本輪依審閱之指示重掃即查得（byte 88 bit 0–1，
   三本 row 410）。**未執行之量測不得寫成量測結果**；此為本線最嚴重之一次自報。
   `-164` 已改為實測值，DR-CAM-n 不需擴。
2. **`-205` 之摘句兩度返工**（§3-3 之註）—— `)]` 一類之黏合符號須整體刪，否則留下之 `]`
   非原句 token，第 4 項會攔下。
3. **`dumpsys` 未如下放包 §3 所述用於本批** —— `-006` 之觀察面為 LVDS 鏈路與 DTC，
   非 camera service；`bench_verify.md` 改列入亮度治具與 LVDS 影像鏈路插拔兩項。
4. **`RESPONSE_TIME`／`Time_POWER`／`Tpower`(V42) 未自行補入 profile §9**（§3-4），
   沿 CAM-09 §5-4 之同一作法（該表之內容由審閱所定）。
5. **HDCC27 之 FD-CAN8 DBC 不在素材**（§2-1 末）—— 此前各輪皆以「四本 DBC」為母體陳述，
   未察覺該母體本身有缺。建議 DR-CAM-f 擴及此本。

---

## §6　未結

| # | 項 | 去處 |
|---|---|---|
| 1 | DR-CAM-a／f／g／h／i／j／k／l／m／n／o／p（十二條）| 補件；f／i 交付前必結 |
| 2 | **DR-CAM-q（新，高）** DTC 識別碼 —— `Diagnostics` 組交付前必結 | 補件 |
| 3 | §2-1 末之 HDCC27 FD-CAN8 DBC 缺本，是否擴 DR-CAM-f | Pei 裁 |
| 4 | §3-4 之三個常數是否入 profile §9 | Pei 裁 |
| 5 | **`HMI Overlays` 尾批**（`-021`／`-022`／`-023`／`-025`）—— A 本最後一組 | 下包 |
| 6 | `-024` 全 BLOCKED（DR-CAM-a），交付時之台帳形制 | Pei 裁 |
| 7 | GCB-07 與 GC 線其餘六項 | GC 線 |
| 8 | `bench_verify.md` 六列之實機驗證 | 交付前 Pei |
| 9 | **B 本（`SWE1-RVC-001…159`，230 列）尚未開始** —— R-CAM1(a) 之第二本 | 下包 |

---

## §7　本包寫入之檔案（git 未 commit）

```
M  features/camera/DECISIONS.md                     6-40 ～ 6-42
M  features/camera/DATA_REQUESTS.md                 DR-CAM-q
M  features/camera/data/coverage.tsv                214 TC                 sha256_16 2149ba9158c2f4cf
M  features/camera/data/bench_verify.md             六列                   sha256_16 d0a62d4824c3a621
M  features/camera/scripts/lint_batch.py            AH 欄寫 remarks        sha256_16 86bda55502c675f7
M  docs/runtime/profiles/FW036_R1L_Camera_Profile.md  §7.2 擴／新 §7.5      sha256_16 cdcb8630b86a5c4c
M  features/camera/generated/batch03b/              七列修正 ＋ LINT_REPORT
?  features/camera/generated/batch03c/              36 × json ＋ 36 × md ＋ INDEX ＋ LINT_REPORT
?  docs/fw036/handoff/up/20260923_CAM-12.md         （本檔）
   features/camera/RULINGS.md                       未動                   sha256_16 ca3763cdfaf3ac49
   其餘七個 generated 目錄                           未動（本輪）
```

`batch03b` 修正七列與 `batch03c` 之 json sha256_16：

```
03b  -152 8cc1716c85044a07  -153 8fec38e231befd2f  -164 416af7b4a5da968d  -170 1b0949d7b5ed1239
     -175 c4df2aba6533cc97  -176 4e8cde7ffaf45bdc  -178 6531916085d17508

03c  -179 0df00cd9d2f23396  -180 ae35eba47981567f  -181 41d9df79d0b5c406  -182 a0f0fc577940de91
     -183 d0cbbde4ebd1d154  -184 e6602462a2d07d9d  -185 775c65d747f166ba  -186 27d3e45cdcc0cfc3
     -187 895bfd4f379a65b2  -188 7e70d438d8b91189  -189 2fd258548d0bb557  -190 92d1fc478b45503c
     -191 32e41170534d7bbd  -192 db43cc0fdf7ebf38  -193 0ebf24d8df324715  -194 5f89334bc4537783
     -195 45df489ebf5a0c25  -196 4c56548ff56e87c6  -197 e83b2650bc7ceab4  -198 5d894a8b006db69b
     -199 411883332bddefc2  -200 930abd477ad4abbc  -201 b12e04400f60fd9f  -202 22ddd5933af9a6f5
     -203 3e22a87486894599  -204 5564565e883ac456  -205 053f986371f214d9  -206 0371d9e02d47f8bb
     -207 af20658b405c36a4  -208 c1a2ae2fed823d1f  -209 dc1a89f42ea96b82  -210 969b07999f67bb0c
     -211 9af3526e90c73433  -212 81f79c6d750d0c4b  -213 0f324876ec6602a9  -214 6d251c2ba6aaf3d8
```

### 複驗指令

```bash
python3 features/camera/scripts/selfcheck_camera.py features/camera/generated/{pilot02,batch01,batch01b,batch02a,batch02b,batch02c,batch03a,batch03b,batch03c}
python3 features/camera/scripts/lint_batch.py <暫存目錄> features/camera/generated/batch03c
```
