# 上繳包 VC-RECON —— VehicleCategory 20260907 本之量測（唯讀，未寫任何檔）

觸發：Pei 給路徑
`10_Reviewing/00_TestCase/ASW-R2/Vehicle Settings/VehicleCategory/…_VehicleCategory_20260907.xlsx`
（2026-09-08，無下放包）。Pei 裁：**先出 recon 報告，不寫檔**。

**本包未寫任何工作簿、未改 repo 內任何 feature 檔。** 唯一產出為本文件。

---

## §0　一句話

20260907 本是 Pei 於 **2026-09-07 15:31** 對 repo 內 `20260902_working` 做的一輪
**系統性後製**：TC 內容（`I`／`J` 之敘述、`K`／`L`／`M`）**幾乎未動**，
改的是**識別欄與交付欄** —— Polarion 號、tc_id 前綴、Test Group、spec_ref 檔名式、
7 個車型適用欄、作者欄，另把 `J` 欄之 `PENDING: DR-49` 全數移除。

**覆蓋率是乾淨的**：117/117 個可出 TC 之 leaf 全覆蓋，28 個未覆蓋者
**全部是父層**，其 sub-id 已逐條覆蓋（§8.2.2 之正常型態），**真缺口 0**。

**出貨 gate 未過**：仍有 **14 列 PENDING**（`DR-VC9` ×4／`DR-VC1` ×1／`DR-VC8` ×1／
`DR-VC10` ×8），依 IN §8.4.3「含 PENDING 之工作簿不得出貨」。

---

## §1　量測基準

| 檔 | sha256 | bytes | mtime |
|---|---|---|---|
| **20260907 本**（Pei 給之路徑） | `691c4c334fab39195b9c364a353d3b4a77d2c255e9e8bf79eeceecf6373677c9` | 252,074 | 2026-09-07T15:31 |
| repo `output/…_20260902_working.xlsx` | `f9bbeffc56dc09c80bae6876a93645c6ff59c9db6fff11c934b9835407fc8ac0` | — | — |
| repo `output/…_20260827_working.xlsx` | `c470defd66c5b84c…` | — | — |
| repo `output/…_20260902_working.presl07.bak.xlsx` | `3a1b5f401fada13e…` | — | — |

比對基準取 `20260902_working` —— 它是 `output/MANIFEST.tsv` 唯一登記之產出
（round `VS-SL-07`，source `sandbox/vssl/vc_vssl.xlsx`）。

⚠ **repo 內無任何檔與 20260907 本相符**；`delivered/MANIFEST.tsv` **只有表頭**，
本 feature 至今**未交付過**。即 20260907 本之後製**只存在於 `10_Reviewing/`，repo 未同步**。

`REF/` 內三件（037 V0.1 報告、SYS1 匯出、Vehicle Category HMI L&F PDF）
與 `features/vehicle_category/inputs/` 之同名件並存，本包未逐位元比對（不在量測範圍）。

---

## §2　20260902_working → 20260907 之差異（TC 分頁，1,712 格／128 列）

`I`／`K`／`L`／`M`／`P`／`R`／`AH` 等**內容欄一格未動**。改動集中在識別欄與交付欄：

| 欄 | 欄名 | 格數 | 變更型態 |
|---|---|---|---|
| `C` | Requirement or Design ID (Polarion) | 126 | `NRL-171043` → `NRL-582617`，且**改為逐列相異**（舊值有重複，如 171043 連續三列；新值 582617／582618／582619 逐列遞增）→ 該欄語意由「需求之 Polarion 號」改為「**本 TC 自身之 Polarion 號**」 |
| `D` | Requirement or Design ID | 2 | 其一為表頭區 `D2`：`newR1L` → `NR1L`（專案名）；另一為資料列之單格 |
| `F` | Test Case ID | 126 | `NR1L-VC-001` → `NR1L-VehicleCategory-001`（全 126 條，序號不變）|
| `G` | Test Group | 126 | `Vehicle Category` → **`Vehicle Settings`** |
| `J` | Pre-Conditions | 71（＋表頭 1） | **移除 `PENDING: DR-49` 行**；其餘前置條件逐字不動。表頭區之 1 格為 `J5` 日期 `2025/10/17` → `2026/9/7` |
| `N` | Specification Reference | 126 | 去 `SYS1_HMI_` 前綴：`SYS1_HMI_Vehicle_Category_HMI_Logic_and_Flow_R1_SR24_Post_2A_(December_27_2023)_2.2` → `Vehicle_Category_HMI_Logic_and_Flow_…_2.2` |
| `O` | Test Case Reference ID | 126 | `None` → `NEW` |
| `T`–`Z` | HDCC27／DT27／VF637／Commander／Renegade／Toro／Fastback | 126 × 7 | `None` → `1`（**七個車型全標 1**，無例外） |
| `AA` | Test Case Author | 126 | `None` → `PeiPYHsu` |

**三點須知**

1. **`N` 欄之改動使 spec_ref 與 `feature.yaml` 之 `spec_reference_template` 不再一致**
   （repo 側之樣板含 `SYS1_HMI_` 前綴）。若 repo 要同步，樣板須一併改，
   否則下次重生會把前綴加回去。
2. **`T`–`Z` 全標 `1`** —— 七個車型全適用。若其中有車型不配 Vehicle Category，
   這是需要覆核的一筆；本包無來源可判，僅回報。
3. **`C` 欄語意改變**：新值逐列相異且與 `D` 欄之 `SWE1-HMI-VC-*` 不再同構。
   若該欄之定義仍是「需求之 Polarion 號」，則 126 列各有一個相異需求號，
   與 `D` 欄之 117 個相異 Requirement **對不起來**；若已改為「TC 之 Polarion 號」，
   則欄名（`Requirement or Design ID (Polarion)`）與內容不符。**請 Pei 確認**。

---

## §3　覆蓋率（037 V0.1 對帳）

來源：`features/vehicle_category/inputs/FM-WI-FSM-037-A03-N1L-SWE1-VehicleCategory-HMI-V0.1 STLA 報告.xlsx`
分頁 `Analysis Report`。⚠ 該 037 之欄序與 CFTS044 系列**不同** ——
多一個 `HMI Source ID`（C 欄），故 `Categorization` 落在 **G 欄**而非 F 欄。

```
037 V0.1 leaf 總數            145
其中 Categorization = Functional Requirement   145（全數）
工作簿資料列                   126（row 10–135）
工作簿相異 Requirement          117
覆蓋之 Functional leaf         117 / 145
未覆蓋                         28
  其中「父層，其 sub-id 已全覆蓋」  28   ← 全部
  真孤兒（無任何子 sub-id 覆蓋）     0
```

28 個父層逐一列示（子條數）：

```
VC-001(3) VC-007(5) VC-012(3) VC-013(4) VC-019(2) VC-025(5) VC-026(3) VC-028(2)
VC-033(2) VC-034(2) VC-035(3) VC-036(2) VC-037(2) VC-038(5) VC-042(2) VC-046(5)
VC-047(4) VC-048(2) VC-051(3) VC-052(2) VC-056(2) VC-058(3) VC-059(2) VC-060(2)
VC-062(2) VC-063(2) VC-064(3) VC-065(2)
```

其標題皆為「N-stage／N-rule／N-step …」之複合條文（例：
`VC-038 Five-step language-change flow: (1) display …`），
即上游把一條複合需求拆為父 ＋ 子 sub-id，子項逐條可測。
**父層不另出 TC 屬 §8.2.2 之正常型態，不是缺口。**

**一 Req 出兩列者 9 個**（`VC-038-03`／`-038-05`／`-046-05`／`-048-02`／`-058-03`／
`-062-02`／`-063-02`／`-064-01`／`-064-03`），126 − 117 = 9，對得上。

---

## §4　出貨 gate

```
資料列        126（row 10–135）
Requirement   117
Priority      P0=5 / P1=35 / P2=50 / P3=36     （全在 P0–P3 內）
PENDING       14 列   ← 不可出貨（IN §8.4.3）
BLOCKED       0
IMPL_GAP      0
NOT GENERATED 0
```

**PENDING 14 列之歸屬**

| DR | 列數 | 落點 | 待覆之物 |
|---|---|---|---|
| `DR-VC10` | 8 | `L`／`M` | — |
| `DR-VC9` | 4 | `L` | Dashboard content tab／PDO graphics／Controls table reference |
| `DR-VC1` | 1 | `L` 或 `M` | — |
| `DR-VC8` | 1 | `L` 或 `M` | — |

列號：`26, 29, 33, 34, 42, 61, 121, 122, 123, 124, 125, 126, 127, 134`
（`L` 欄 11 列、`M` 欄 3 列）。

**`DR-49` 之處置須釐清** —— `20260902_working` 有 **20 列**引 `DR-49`（皆在 `J` 欄之
`PENDING: DR-49` 行），20260907 本**全數移除**，且未在他欄留下替代敘述。
PENDING 因而由 34 列降為 14 列。
⚠ 這是**內容層之處置**（把一個未覆之 DR 從前置條件拿掉），不是格式後製。
`DR-49` 是已覆了、判為不適用、還是暫時拿掉待覆 —— **本包無從判定，請 Pei 具名**，
否則 repo 若同步此本，會失去該 20 列曾掛 `DR-49` 的記錄。

---

## §5　封面現況（並為 VS-CF-04／PU-02 之 `G6` 問題定案）

```
Cover 封面
  D6 'C'                    G6 '修訂日期 Revision Date'（欄頭，合併 G6:H6）
  D7 '劉安哲 AllenACLiu'      G7 （空）
  D8 '張愷霏 ErinKFChang'     G8 （空）
  D9 '許珮瑜 PeiPYHsu'        G9  datetime(2026, 9, 7)      ← 值欄確為 G9
Product Document 記錄封面頁
  B3 'NR1L'          B4 <本簿交付檔名全稱>     B5 'Initial Release'
  B6 'SW Testing'    B7 'Confidential'        B8 datetime(2026, 9, 7)
  A13 'V1.0'  B13 'Initial Release 初版發布'  C13 '許珮瑜 PeiPYHsu'  D13 datetime(2026, 9, 7)
```

**VS-CF-04 上繳包 §2 之 open item 就此結案**：修訂日期之值欄**確為 `G9`**，
與作者（`D9`）同列 —— 我在該包之推得正確，且此為 Pei 親填之先例。

同時暴露**已交付兩本（CFTS044 20260908、Popup 20260908）之四處差異**，
Pei 裁「**不修，具名在案**」（2026-09-08），已登於兩 feature 之 `ANOMALIES.md`：

| 欄 | Pei 之慣例（本簿） | 已交付兩本 | 成因 |
|---|---|---|---|
| `D9`／`C13` 作者 | `許珮瑜 PeiPYHsu` | `PeiPYHsu` | 下放包字面為 `PeiPYHsu`，執行層照字面寫，未比對既有交付本之 `D7`／`D8` 寫法 |
| `G9`／`B8`／`D13` 日期 | 真 `datetime` | **字串** | `xlsx_surgical._cell_xml()` 無 `datetime` 分支（已於兩包具名） |
| `B3` 專案代號 | `NR1L` | `new R1L` | PU-02 §2.3 令「逐字沿用 CFTS044 同欄」，而該格為上游母本預填之舊值 |
| `B5` 版本 | `Initial Release` | `V1.0` | 同上 |

⚠ `AA` 欄（Test Case Author）Pei 用 **`PeiPYHsu`**（無中文名），
與 `D9`／`C13` 之 `許珮瑜 PeiPYHsu` **不同** —— 兩欄慣例不一致是本簿實測，非誤記。

---

## §6　結論與可選之下一步（皆未執行）

**本簿之狀態**：內容完整（覆蓋無缺口）、識別欄已後製到交付形制、
封面已填齊，**唯 14 列 PENDING 擋住出貨**。

| 選項 | 前置 |
|---|---|
| (a) 同步 repo 副本至 20260907 | 需先裁 `DR-49` 之處置（§4）與 `C` 欄語意（§2-3）；`feature.yaml` 之 `spec_reference_template` 須一併改（§2-1） |
| (b) 走交付（比照 VS-CF-04） | 需先裁 14 列 PENDING 如何結案或降轉；IN §8.4.3 為硬規 |
| (c) 只結案 `DR-VC1`／`8`／`9`／`10` | 四個 DR 之覆文，本包無 |

**執行層無建議偏好** —— 三項待裁（`DR-49`、`C` 欄語意、`T`–`Z` 全標 1）
皆為上游事實之認定，非執行層可代。

---

## §7　工具旗標與自報

```
openpyxl                 全程 read_only／一般載入，**未 save**、未 surgical_save
寫入路徑                  本文件 ＋ 兩 feature 之 ANOMALIES.md（Pei 裁「具名在案」）
                         —— **未動任何 xlsx、未動 features/vehicle_category/ 之任何檔**
10_Reviewing/ 之 20260907 本   唯讀開啟
REF/ 三件                 未開啟
```

**自報**：§3 之覆蓋率我第一次算錯 —— 誤以 F 欄取 `Categorization`（沿用 CFTS044 系列之欄序），
得到「Functional 0、未覆蓋 117」之荒謬值。發現數字不合理後回讀 037 之 r7 欄名，
確認該檔多一個 `HMI Source ID` 欄、`Categorization` 在 G 欄，重算後為 145/145 Functional。
**教訓**：跨 feature 沿用欄序是 VS-CF-03 那條「量錯來源」的同族錯誤；
037 之欄序須逐檔讀表頭確認，不得沿用。
