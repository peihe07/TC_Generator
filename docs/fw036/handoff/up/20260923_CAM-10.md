# 上繳包 CAM-10 —— CAM-09 審閱落檔 ＋ batch03a（`-008`／`-009`／`-010`）

對應下放包：`docs/fw036/handoff/down/20260923_CAM-10.md`
對應審閱：`down/20260923_CAM-09_review.md`
執行層 session：延用，2026-09-23。**git 未 commit**；未寫回工作簿；`pilot01/` 未動。

> **升級條件（下放包 §4）**
> 1. batch03a > 30 → **未觸發**，惟計畫由 23 修訂為 **29**（§2-2），仍 ≤ 30。
> 2. `gridZoomRequest`／`systemStatus` 之值於 SYS2 只有訊號名無值 → **不成立**：
>    兩者之值於來源皆有逐字（`OFF`／`ON`／`Default`／`SNA`；`1X`／`4X`／`SNA`／`Pressed`／`Not_Pressed`），
>    **該類 PENDING 列數 0**。本批唯一之 PENDING 為 `DR-CAM-p`（LVDS 週期之**時間值**，非訊號值）。
> 3. 任一摘句無法在 50 token 內保留條件與結果子句 → **對 `SYS-RA-VF551_V2-486` 成立**，
>    依 §8.2.2 拆為五列後化解（§2-2），未停於回報。**此處我做了判斷，請確認**。

### R-G62(b) 之觀察依據

起點前之最後他方寫入：`down/20260923_CAM-09_review.md` 與 `down/20260923_CAM-10.md`。
本 session 起點後至落檔止，`features/camera/` 下未觀察到他方寫入。
**不聲稱「無他 session 寫檔」**。

---

## §1　落檔

### 1.1　R-CAM16 取號與 sha8

取號現查：全 repo（排除本包下放檔）之最大 `R-CAM` 號為 **15**，`{live}` ＝ **16** 成立。

| 條 | body sha8 | fenced sha8 | 錨點 |
|---|---|---|---|
| **R-CAM16** | `440b422c` | `cb146bbb` | `features/camera/RULINGS.md:311` |

**既有 19 個錨之 body／fenced sha8 全數未變**（以 `git show HEAD:` 之前一版同工具比對，join 後零差異）。

### 1.2　DATA_REQUESTS：DR-CAM-p

LVDS 訊息之傳送週期 —— `V2-479`／`-480` 之 `two LVDS message cycles` 其時間值於
SYS2 全本與 `forms/` 皆查無。值切換之**結果**照寫，**時序面** PENDING。

### 1.3　RD_FEEDBACK：RDF-08／09／10

| # | 標的 | 摘要 |
|---|---|---|
| RDF-08 | `V3-281` | 主句 gating 標的寫成來源訊號自身（`to LVDS STATUS_CCAN5.LWSAngle`）；另其 `a)` 子句之**條件訊號**亦疑誤植（寫 `STATUS_CCAN3.VehicleSpeedVSOSig is missing`）—— **本包多報一處** |
| RDF-09 | `V3-283` | `a)` 子句之送出標的回寫來源名（`send LVDS STATUS_CCAN4.ReverseGearSts`）|
| RDF-10 | `V42-590` | 與 `-584` 逐字同句，致 `Communications_Timeout` **缺清除側**；附三組 set／clear 之對照表 |

### 1.4　profile

- **§9** 加 `TIME_W_RVC` **2,5 sec**（V33 §1.14.1 `-515`／`-516`／`-519`）與
  `TIME_W_RVC2` **50 ms**（`-587`／`-588`／`-591`，範圍 `[50;100]`、容差 `10`）。
- **§7.3（新）** 無命令句式可抄時之書寫 —— DTC 以
  `Read the DTC list with the diagnostic tool and check that …`、LVDS 以 bus analyzer，
  **皆不造命令**；附兩項之實測依據（交付語料 275 條 `$ ` 中 DTC 同格者 0；
  `features/*/generated/` 全部 json 之 `$ ` 命令行共 9 條全在 camera 本身）。

### 1.5　DECISIONS 6-28 ～ 6-33

6-28 雙向解讀准；6-29 R-CAM16；6-30 難點 A～E 之處置表；6-31 `-099`／`-100` 判讀准；
6-32 切法准；6-33 交付前實機驗證清單。

### 1.6　`coverage.tsv`（新建）與 `bench_verify.md`（新建）

**`features/camera/data/coverage.tsv`** —— A 本 **25 列全量**（非僅兩行）。
欄位沿 `features/security/data/coverage.tsv`（`swe_id, disposition, batch, tc_count, tc_ids`）。

> **須回報**：下放包 §0 寫「格式沿 vehicle_setting 之 `coverage.tsv`」，
> 惟 `features/vehicle_setting/` **無** `coverage.tsv`（全 repo 之 `coverage*.tsv` 實測為
> `security`／`comfort`／`vsm_v43`／`display` 四線，格式各異）。本包取 **security 本**為據 ——
> 其欄位最接近本用途。若 Pei 另有所指，請於審閱指正。

現況（含 batch03a）：

| disposition | 列數 | 說明 |
|---|---:|---|
| `PRODUCED` | 14 | 合計 **146 TC** |
| `No TC — all sources delegated …` | 2 | `-007` → `-006`；`-013` → `-004`（**R-CAM16**）|
| `No TC — all 12 sources are VF617_V5 (DR-CAM-a, missing)` | 1 | `-024` |
| `PLANNED` | 8 | batch03b／03c 四列 ＋ HMI Overlays 尾批四列 |

**`features/camera/data/bench_verify.md`** —— 交付前實機驗證清單，初始三列
（`-058`／`-092` 之 `dumpsys` 輸出可判性、`-108` 之 50 ms 時戳）。

### 1.7　`-108` reasoning

加「判定以 bus analyzer 時戳為準」一句，並指向 `bench_verify.md`。

---

## §2　batch03a 生成（29 列，`NR1L-RVC-118`～`-146`）

### 2.1　產物

`features/camera/generated/batch03a/`：29 × json ＋ 29 × md ＋ `INDEX.md` ＋ `LINT_REPORT.md`。
Test Set 全 29 列 `LVDS Messaging`。

| SWE | 計畫（CAM-09）| **本輪修訂** | TC |
|---|---:|---:|---|
| `SWE-CAM-008` | 10 | **12** | `-118`～`-129` |
| `SWE-CAM-009` | 2 | **6** | `-130`～`-135` |
| `SWE-CAM-010` | 11 | 11 | `-136`～`-146` |
| | **23** | **29** | |

### 2.2　**計畫修訂兩處（須裁）**

1. **`SYS-RA-VF551_V2-486`：1 → 5 列**（`-131`～`-135`）。
   **這是升級條件第 3 項之直接觸發**：該來源 **80 token**，其 `a.`～`e.` 為五個結果子句；
   併為一列時，摘句**無法在 50 token 內同時保留條件與五個結果**（實測：條件段約 39 token，
   任一子句 10～13 token，僅保留一個子句即已 51）。
   依 §8.2.2「一個 sub-id 可需數個 TC，不得反向合併」拆為五列，
   每列之摘句 ＝ 速度條件 ＋ 該子句（32～35 token，皆為原句之保序子序列）。
   **我判斷拆列是該條之正解而非規避，故未停於回報而續做；若 Pei 認為應停，請撤回本五列。**
2. **`SYS-RA-VF551_V3-284`：2 → 4 列**（`-124`～`-127`）。
   該來源 78 token、四個子句（a OFF／b ON／c Default／d never-SNA），
   CAM-09 計畫只列 a／b 兩列，遺漏 c／d 之 Atl-Mi 分支 —— 其行為在 Atl-Hi 側各有專列
   （`-119`／`-120`），Atl-Mi 側不補則該兩行為於 376 無覆蓋。本輪補足。

`batch03_plan.tsv` 已同步修訂（兩列之 `tc_count` 與 note），並為已生成之列補 TC id。
修訂後 batch03 合計 88 → **94**；切法仍成立：**03a 29／03b 29／03c 36**（皆 ≤ 40）。

### 2.3　摘句表（11 列，皆為保序子序列）

| TC | 來源 | 原 token | 摘後 | 摘法 |
|---|---|---:|---:|---|
| `-124`／`-125` | `V3-284` | 78 | **28** | 各刪其餘三個子句 |
| `-126` | `V3-284` | 78 | **31** | 刪 a／b／d |
| `-127` | `V3-284` | 78 | **24** | 刪 a／b／c |
| `-131`／`-132` | `V2-486` | 80 | **35** | 刪 RVC-active 條件列 ＋ 其餘四子句 |
| `-133`／`-135` | `V2-486` | 80 | **32** | 同上 |
| `-134` | `V2-486` | 80 | **33** | 同上 |
| `-145`／`-146` | `V42-326` | 73 | **17** | 各刪其餘四個對映列 |

`-130`（`V2-485`，47 token）、`-139`～`-144`（39／40／31 token）未逾 50，全句書寫。

### 2.4　下放包 §2 各要點之落實

| 要點 | 落實 |
|---|---|
| LVDS 以 bus analyzer、label 逐字；CAN 側取 DBC `VAL_` | 29 列之 Pre-Condition 皆置 bus analyzer 行；CAN 側實測可寫 raw —— `IPC_VEHICLE_SETUP.DynamicGrid` **Atl-Hi `BO_ 1443`（FDCAN8）／376 `BO_ 1468`（P363 BH-CAN）**，兩本之 `VAL_ 0 "Dynamic Gridlines OFF" 1 "Dynamic Gridlines ON"` 同值域 |
| `V2-479`／`-480` 時序面 PENDING | `-122`／`-123`：ER 之「兩個 LVDS 週期」標 `PENDING: DR-CAM-p`，值切換之結果照寫 |
| Out of Scope 不作錨；`V2-578`／`-580` 可作措辭 | `-130`／`-131`～`-135` 之 Pre-Condition 3 `The RVC display is active on the Radio` 即取其措辭，**未作錨**（R-CAM9）|
| `V2-486`／`V3-284`／`V42-326` 摘句 | 見 §2-3 |
| `-009` 之 `DynamicGridSts`（A-CA16）以 HMI 現象為 ER | **本批未觸及** —— A-CA16 所指之 `STATUS_LIN.DynamicGridSts` 屬 `SWE-CAM-023`（V42-230/-231/-232），不在 `-008`／`-009`／`-010` 之 in-scope；本批之 `DynamicGrid` 為 `IPC_VEHICLE_SETUP` 之 CAN 訊號（可注入）。於此回報以免誤解 |
| R-CAM3(f)／R-CAM15／R-CAM16 適用 | 29 列無一寫 CAN source 行（皆單一 EE）；R-CAM15 之 (b)(c) 逐列適用見 INDEX；R-CAM16 見 `coverage.tsv` |

### 2.5　lint ＋ selfcheck

```
batch03a（29 列）：A=0 B=0 C=0 D=0 E=0 F=0 G=0 H=0 I=0 I-sibling=0 K=0 L=0 M=0 N=0
                  P=0 Q=0 R=0 T=0 V=0 W=0 X=0 Y=0 **Z=0**｜**J=1**  U=4  I-cross=29
selfcheck（八項，七目錄 146 列）：1/2/3a/3b/4/5/6/7 命中皆 0（常數錨 2、合一錨 2）
```

`J`=1（`-119`，來源首字為子句編號 `a.`）為 profile §5.1 之逐字豁免，reasoning 已具名。
`U`=4 為 `-122`／`-123` 之 `DR-CAM-p`（pre ＋ er 各一）。

---

## §3　結構性回報

1. **`systemStatus.ZoomViewRes` 為 RVCM → HU 之 LVDS 訊號**（四本 DBC 零命中）——
   `-131`／`-132`／`-133` 須**注入**該訊號才能執行，而注入 LVDS 需模擬器。
   本 feature 之 bench 是否具備該能力**未知**，三列之可執行性因而有風險。
   三列之 reasoning 已具名。`-130`（`V2-485`）不受影響 —— 其條件為
   `for each value of systemStatus.ZoomViewRes`（全稱），任一值皆成立。**歸 Pei。**
2. **`V42-326` 之五個對映只生成兩列**（`Invalid` ＋ `Short_2950`）——
   其餘三值為同一查表判準之等價類，依 §8.3 取邊界與一個有效值。
   **若審閱要求全覆蓋則擴為五列**（batch03a 隨之 29 → 32，仍 ≤ 40）。**歸 Pei。**
3. **`-136`～`-138`（`V2-443`／`-444`／`-445`）之 ER 只驗「反映 PROXI 值」**——
   V2 本無逐值對照（Atl-Mi 之 V3／V42 才有），同 `-091` 之處置（DECISIONS 6-25）。
   `-138` 之 PROXI 參數名為來源之措辭 `CAN node 27 (ASM/ASCM)`，非 PROXI 表之參數名，
   其 raw 無從實測，Pre-Condition 依 §8.7.5(e) 逐字書寫。
4. **`-128`／`-129` 與 `-062`／`-063` 為同一設定之兩條不同條文** ——
   後者驗其對 **CAN**（`TELEMATIC_VEHICLE_SETUP.DynamicGrid_Req`，V33，2261），
   前者驗其對 **LVDS**（`gridZoomRequest.DynamicGridRQSts`，V42，637）。
   標的匯流排、條文、平台皆不同，非重複。

---

## §4　自報錯誤

1. **升級條件 3 觸發而我未停** —— `V2-486` 之摘句確實無法在 50 token 內保留條件與五個結果；
   我判斷「拆五列」是 §8.2.2 之正解而非規避，故續做並具名（§2-2）。**若 Pei 之原意為停，請撤回。**
2. **CAM-09 之 batch03 計畫低估兩處**（`V3-284` 漏 c／d 兩分支、`V2-486` 未察覺摘句不可行）——
   兩者皆為我上一輪造計畫時之疏漏，本輪修訂並具名。
3. **下放包 §0 所指之 `vehicle_setting/coverage.tsv` 不存在**（§1-6），本包改沿 security 本。
4. **`coverage.tsv` 建為 25 列全量而非下放包所令之「加兩行」** —— 下放包寫「若已存在則加兩行；
   不存在則建」，未指定建檔時之範圍。我判斷零 TC 之兩行若無其餘 23 列作背景則無法判讀
   （看不出「其餘列都有 TC」），故建全量。**若 Pei 只要兩行，本檔可裁剪。**
5. **下放包 §2 之 A-CA16 要點與本批無交集**（§2-4 末列），已回報。

---

## §5　未結

| # | 項 | 去處 |
|---|---|---|
| 1 | DR-CAM-a／f／g／h／i／j／k／l／m／n／o（既有十一條）| 補件；f／i 交付前必結 |
| 2 | **DR-CAM-p（新）** LVDS 訊息週期 | 補件 |
| 3 | §3-1 之 `systemStatus.ZoomViewRes` 注入能力 | Pei 裁 |
| 4 | §3-2 之 `V42-326` 是否擴為五列 | Pei 裁 |
| 5 | §4-1 之升級條件 3 處置、§4-3／§4-4 之 `coverage.tsv` 形制 | Pei 裁 |
| 6 | **batch03b**（`-011`，29）與 **batch03c**（`-004`／`-006`／`-019`，36）| 下包 |
| 7 | `HMI Overlays` 尾批（`-021`／`-022`／`-023`／`-025`）；`-024` 全 BLOCKED（DR-CAM-a）| 下包 |
| 8 | GCB-07 與 GC 線其餘六項 | GC 線 |
| 9 | `bench_verify.md` 三列之實機驗證 | 交付前 Pei |

---

## §6　本包寫入之檔案（git 未 commit）

```
M  features/camera/RULINGS.md                       R-CAM16                sha256_16 ca3763cdfaf3ac49
M  features/camera/RD_FEEDBACK.md                   RDF-08／09／10          sha256_16 b08424a16b2e8ab4
M  features/camera/DECISIONS.md                     6-28 ～ 6-33
M  features/camera/DATA_REQUESTS.md                 DR-CAM-p
M  docs/runtime/profiles/FW036_R1L_Camera_Profile.md  §9 兩常數／新 §7.3    sha256_16 bc1383c633410453
M  features/camera/data/batch03_plan.tsv            兩列修訂 ＋ TC id       sha256_16 b1b5f7513a884615
M  features/camera/generated/batch02c/NR1L-RVC-108.{json,md}   reasoning 加一句
?  features/camera/data/coverage.tsv                25 列                  sha256_16 a591d9b437725161
?  features/camera/data/bench_verify.md             3 列                   sha256_16 ee3f7883dd402a95
?  features/camera/generated/batch03a/              29 × json ＋ 29 × md ＋ INDEX ＋ LINT_REPORT
?  docs/fw036/handoff/up/20260923_CAM-10.md         （本檔）
   其餘六個 generated 目錄                           未動（本輪）
```

`batch03a` 之 29 份 json sha256_16：

```
-118 d2d0146de787218f  -119 1fe14fac68759d7c  -120 7f980d739bc6470f  -121 b580008b674e16d9
-122 b2ed01774af72d38  -123 09a7cefd15cd78ee  -124 ea3b2147d8f82053  -125 b04a47f561abe1a3
-126 c80d8c511e9e8e7e  -127 4a4d3b766526414a  -128 2e77d812ff15c75a  -129 310efdb5db7ac710
-130 9824e42b6b285a7c  -131 9d182dbb7941eb54  -132 0356f40bf6e9360f  -133 0db52421d1d970d5
-134 c79084c30e33bbb0  -135 850bc9696aa9cbb7  -136 b102b053c2a23cd5  -137 c9d4c8d3890e64bb
-138 34eec3954796ae05  -139 bcd0e8ebdaf5189b  -140 15dcde914078520c  -141 f4355bb421ded261
-142 67e6d79a012857cd  -143 d9f13b2bf3454291  -144 5329c614aeeb40da  -145 26a7b1c07e6c6ce2
-146 cf341949555c1861
```

### 複驗指令

```bash
python3 scripts/rulings_hash.py --target features/camera/RULINGS.md --out /tmp/cam.sha.tsv
python3 features/camera/scripts/selfcheck_camera.py features/camera/generated/{pilot02,batch01,batch01b,batch02a,batch02b,batch02c,batch03a}
python3 features/camera/scripts/lint_batch.py <暫存目錄> features/camera/generated/batch03a
```
