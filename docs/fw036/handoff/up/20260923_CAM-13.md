# 上繳包 CAM-13 —— CAM-12 審閱落檔 ＋ A 本尾批 `HMI Overlays` ＋ B 本選列計畫

對應下放包：`docs/fw036/handoff/down/20260923_CAM-13.md`
對應審閱：`down/20260923_CAM-12_review.md`
執行層 session：延用，2026-09-23。**git 未 commit**；未寫回工作簿；B 本未生成；`pilot01/` 未動。

> **升級條件（下放包 §5）**
> 1. batch04 > 40 → **未觸發**（26）。
> 2. `-022` 之幾何／顏色值只見於圖 → **不成立**：`-022` 之 7 個 in-scope 來源**完全沒有**
>    幾何／顏色之要求（見 §5 證據 2），該類 PENDING **0 列**。
> 3. B 本任一組之 hop label 查無率 > 30% → **不成立**：**八組皆 0.0%**（見 §5 證據 3）。

### R-G62(b)

起點前之最後他方寫入：`down/20260923_CAM-12_review.md` 與 `down/20260923_CAM-13.md`。
本 session 起點後未觀察到他方對 `features/camera/` 之寫入。
併行改動：`features/comfort/delivered/…20260907.xlsx` 之刪除仍未被 CMF 線 stage（連續第五輪）。

---

## §1　落檔

### 1.1　R-CAM17 取號與 sha8

取號現查：全 repo（排除本包下放檔）之最大 `R-CAM` 號為 **16**，`{live}` ＝ **17** 成立。

| 條 | body sha8 | fenced sha8 | 錨點 |
|---|---|---|---|
| **R-CAM17** | `b4af04e9` | `612baf04` | `features/camera/RULINGS.md:327` |

**既有 20 個錨之 body／fenced sha8 全數未變**
（以 `git show HEAD:features/camera/RULINGS.md` 之前一版同工具產表後 join，零差異）。

併登 **GCB-08**（提請入全域 ledger），附 CAM-11 之實例。

### 1.2　DR-CAM-f 擴描述

改為**三類缺件**：(1) ~~`TRANSM2` DBC~~ —— **CAM-12 已以訊號名重掃解消**；
(2) `BED_EXTENDER` message（四本皆無）；
(3) **HDCC27 之 FD-CAN8 DBC 全本** —— `-152`／`-153` 之 PENDING 改指此。

### 1.3　profile

- **§9** 加 `RESPONSE_TIME` 2,0 sec／`Time_POWER` 500 ms／`Tpower`(V42) 5 sec。
- **§10（新）** `remarks` 欄之用途三類（來源與 DBC 名稱差異／疑似誤植／需特殊治具），
  並明記「`remarks` 不取代 reasoning」；原 §10（車型軸）→ **§11**（含 §10.1 → §11.1）、
  原 §11（未決）→ **§12**。

### 1.4　DECISIONS 6-43 ～ 6-46

6-43 `-024` 台帳形制；6-44 V3 四步不拆；6-45 `remarks` 欄；6-46 DR-CAM-q 接受。

### 1.5　**A-CA17 命中一個既有缺陷（本輪自查，非下放包所令）**

`IPC_VEHICLE_SETUP.LanguageSelection` 依 **A-CA17 不可注入**（CameraEventHal：Atl-H、`N`／`Not yet`），
該項明令「以 HMI 語言設定為觸發，不走 CAN」。
**batch02c 之 `NR1L-RVC-113` 卻寫 `Send CAN: … LanguageSelection = PENDING`** —— 違 A-CA17。

已改為 `ENTER_SETTINGS` ＋ `Language` 之 hop（`HMI Settings List` `Settings` 分頁 **row 146**
`Language`，其選項於 **row 147** 為佔位式 `1) Language 1, 2) Language 2, …`）。
**`PENDING: DR-CAM-o` 撤除**，DR-CAM-o 縮範圍為「支援語言清單與各語言譯文」（級降為低）。
batch04 之 `-233`／`-234` 自始依此寫法。

---

## §2　batch04 生成（26 列，`NR1L-RVC-215`～`-240`）

### 2.1　步驟 A —— 選列計畫

`features/camera/data/batch04_plan.tsv`（**115 列**，四個 SWE 之 115 個來源引用逐列在案）。

| SWE | 全 | 查無(VF617) | OoS | 委派 | in-scope | **TC** |
|---|---:|---:|---:|---:|---:|---:|
| `SWE-CAM-021` | 21 | 4 | 0 | 1 | 16 | **11** |
| `SWE-CAM-022` | 14 | 0 | 0 | 7 | 7 | **7** |
| `SWE-CAM-023` | 79 | 47 | 11 | 13 | 8 | **8** |
| `SWE-CAM-025` | 1 | 0 | 0 | **1** | **0** | **0** |
| | **115** | **51** | **11** | **22** | **31** | **26** |

### 2.2　**三處與下放包 §2 之分析層預期不符（須裁）**

1. **`-025` 之 fault banner 無來源可承** —— 其**唯一**來源 `SYS-RA-VF551_V33-227` 依 R-CAM10
   委派 `SWE-CAM-020`（`NR1L-RVC-032`／`-033`，該兩列之 ER 已含
   `"Camera Not in position"` 之疊加）。**`-025` 為 R-CAM16 之第三例**，
   `coverage.tsv` 記 `No TC — all sources delegated to SWE-CAM-020`。
   下放包所列之「banner 出現／消失之條件照寫」「PENDING DR-CAM-h／DR-CAM-f」因而**無對象**。
2. **`-023` 之 `STATUS_LIN.DynamicGridSts`（A-CA16）不在其 in-scope** ——
   guideline 相關之三個來源（`V2-478`／`-479`／`-480`）依 R-CAM10 **委派 `SWE-CAM-008`**
   （`NR1L-RVC-121`／`-122`／`-123`，batch03a）；`V42-230`／`-258`／`-299` 亦委派他列。
   `-023` 實際之 8 個 in-scope 來源為**警示文字（語言／字型）、`HU_ZOOM.req`、T_INITDISPLAY 前之文字移除**。
3. **`-021` 之 localized overlay 不在其 in-scope** —— 語言相關之 `V2-526`／`V3-253` 屬 **`-023`**；
   `-021` 之 16 個 in-scope 來源為**不中斷影像／退出後之顯示更新／Audio Mode 行為／
   5 秒內之文字移除／不閃爍**。

三者皆為 R-CAM10 委派之結果，非缺漏；plan 逐列在案。**歸 Pei 確認**。

### 2.3　生成要點

- **V4（類比側）十一列**只勾 `HDCC27`（R-CAM11 平台表），
  Pre-Condition 以 `PROXI Rear_View_Camera_Type = 0 (Analogic)` 與 V2 數位側區別。
- **多列與既有 TC 逐字同句而本與承接列不同**（V2 數位 ↔ V4 類比；`SWE-CAM-003` ↔ `-021`／`-022`／`-023`）
  —— 依 R-CAM10「委派只在同一來源被多列共引時成立」，各歸其列，**非重複**。
  逐列 reasoning 已具名其對應之既有 TC。
- `-233`／`-234`（語言）依 **A-CA17** 走 HMI 語言設定，不走 CAN。
- `-239`／`-240` 之觸發訊息名依 profile §7.2 以訊號名重掃後改寫（`TRANSM2` → `STATUS_CCAN5`、
  `ENGINE1` → `STATUS_CCAN4`），`remarks` 具名。

### 2.4　lint ＋ selfcheck

```
batch04（26 列）：A=0 B=0 C=0 D=0 E=0 F=0 G=0 H=0 I=0 I-sibling=0 K=0 L=0 M=0 N=0
                 **P=0** Q=0 R=0 T=0 **U=0** V=0 W=0 X=0 Y=0 **Z=0**｜**J=14**  I-cross=26
selfcheck（九項，十目錄 240 列）：1/2/3a/3b/4/5/6/7/8a/8b 命中皆 0（常數錨 2、合一錨 2）
```

`J`=14 為 profile §5.1 之逐字豁免（來源首字本即小寫），逐列 reasoning 已具名。
**`U`=0** —— 本批無 PENDING。初稿之 `P`=2（`split_reason` 含 `HU_ZOOM.req =` 之賦值形）已改寫。

### 2.5　`coverage.tsv` 現況 —— **A 本完成**

| disposition | 列數 | TC |
|---|---:|---:|
| **PRODUCED** | **21** | **240** |
| `No TC — all sources delegated …`（R-CAM16）| 3 | 0 |
| `BLOCKED — DR-CAM-a`（`-024`）| 1 | 0 |
| **合計** | **25** | **240** |

**A 本 25 列全數有處置，無「PLANNED」剩餘。**

---

## §3　B 本選列計畫（只計畫，未生成）

`features/camera/data/b_plan.tsv`（**230 列**，欄位
`swe_id, test_set, kind, sys1_book, section, spec_reference, title, verification_point, axis, est_tc, note`）。

| Test Set | leaf | umbrella | 建議批 |
|---|---:|---:|---|
| `AUX Camera Access` | 60 | 2 | **B02a 30 ／ B02b 30** |
| `AUX Camera Settings` | 45 | 8 | **B03a 23 ／ B03b 22** |
| `Activation and Exit` | 43 | 7 | **B01a 22 ／ B01b 21** |
| `Camera View Switching` | 25 | 6 | **B04 25** |
| `Warning Banners` | 12 | 1 | **B05 30**（四組併批）|
| `PAM Integration` | 9 | 1 | 同上 |
| `Wireless Camera Pairing` | 5 | 1 | 同上 |
| `Camera Settings` | 4 | 1 | 同上 |
| **合計** | **203** | **27** | **八批，各 ≤ 40** |

**umbrella 27 列**皆記 `UMBRELLA — covered by children`，`est_tc = 0`。

### 3.1　估算之基準（須確認）

`est_tc` 以 **1 TC／leaf 為基準（203）**，**軸之拆分留待各批步驟 A 逐列判**。
軸之訊號分佈（以 SYS1 章節文字掃描）：`PROXI 配備` **20 列**、`車型` **4 列**、其餘 179 列無軸訊號。
以 A 本之實績（in-scope 來源 → TC 約 1.0～1.2）推估，**B 本之實際總數應在 203～230 之間**。

### 3.2　難點回報（下放包 §3）

| # | 項 | 實測／處置 |
|---|---|---|
| **A** | hop label 之可得性 | **八組查無率皆 0.0%** —— 230 列之 SYS1 章節於 cache 本**全數有 `Description`**（證據 3）。DR-CAM-g 型之 PENDING **預估 0**；惟「章節有文字」不等於「文字即 hop label」，實際 hop 之逐字仍須逐列判，於各批步驟 A 回報 |
| **B** | `spec_reference` 之 token 化 | `hmi_source_id` 已為 `{檔名}_{章節號}` 形（§10.7(b)）。**兩份檔名含非底線字元**：RVC+PAM 本含 `+` 與 `( )`（`SYS1_HMI_RVC+PAM_R1_Low_SR24_1A_(June_25_2021)_7.3.1`）；HeadUnitCameraSystems 本另含**逗號與空白**（`…_(February_10th, 2023)_6.1.2`）。**該空白會使工作簿之 `N` 欄出現空格**，是否統一改為底線，**歸 Pei**（改則與 `recon_b_rows.tsv` 之既有值不一致，須一併重生）|
| **C** | 訊號／PROXI 前提沿 A 本 | R-CAM1(b) 成立；`crossref_a_b.tsv` 之 `[VERIFIED]` 於 230 列中對應 **34 個 B 列**，已逐列記於 `note` 欄作互參入口 |
| **D** | AUX 之 PROXI 配備 | `Auxiliary_Trailer_Camera` **查得**：PROXI byte 211 bit 2，Atl-Hi 三本 **row 921** 皆 `0=Absent`（證據 4）。`Digital_CHMSL_Camera_Prsnt` 見 CAM-06 §2-4 之表。**Atl-Mi 三本之該參數待掃**（B 本生成時逐平台核） |
| **E** | VF617 缺件之影響 | B 本之 spec_reference 母體為 SYS1（R-CAM1(c)），**不引 VF617**；DR-CAM-a 對 B 本**無直接影響**。惟 A 本 `SWE-CAM-024`（`AUX Camera`）全 BLOCKED，其與 B 本 `AUX Camera Access`／`AUX Camera Settings` 之互參（`crossref_a_b.tsv` 26 筆 `[VERIFIED]`）因而只能單向 —— B 本可獨立生成，A 本之對應列仍 BLOCKED |

### 3.3　`7.3.1`～`7.3.3` 三列

`SWE1-RVC-016`／`-017`／`-018` 之章節只存在於 `spec-index/cache/` 本（A-CA14／R-CAM6），
`note` 欄已逐列具名；以 cache 本為母體時對映率 100%。

---

## §4　依 R-CAM17 —— 本包所有「查無」主張之證據

| # | 主張 | 掃描字串 | 母體 | 命令／工具 | 命中 |
|---|---|---|---|---|---|
| **1** | `HU_ZOOM.req` 非 CAN 訊號（`-237`／`-238`）| `HU_ZOOM`（不分大小寫）| `forms/*.dbc` **四本**（latin-1 解碼）| `python3` ＋ `re.findall` | **0**（四本各 0）|
| **2** | `-022` 之 7 個 in-scope 來源無幾何／顏色值 | `colou?r｜RGB｜pixel｜width｜height｜radius｜geometry｜guideline｜gridline｜mm\b｜px\b`（不分大小寫）| VF551_V4 之 `-121`／`-133`／`-134`／`-136`／`-140`／`-142`／`-152` 七列 `Description` | `python3` ＋ `re.findall` | **0** |
| **3** | B 本八組之 SYS1 章節文字查無率 0% | 以 `(book, Outline Number)` 對 230 列逐列查表 | `spec-index/cache/` 之 **兩本 xlsx**（HeadUnitCameraSystems 1045 列、RVC+PAM 65 列，合計 1108 個章節）| `openpyxl` ＋ dict 查表 | **查無 0／230** |
| **4** | `Auxiliary_Trailer_Camera` **查得**（非查無，併列以備複驗）| `Auxiliary` | `forms/proxi/` **六本**（Atl-Hi 三本 ＋ Atl-Mi 三本）| `openpyxl` 全格掃描 | **3**（Atl-Hi 三本 row 921，皆 `0=Absent`）|
| **5** | `STEERING1`／`LwsAngle_SCCM`（沿用 CAM-12，本輪未重跑）| —— | —— | —— | 見 CAM-12 §2-1；分析層已獨立複驗（CAM-12 審閱 §一）|

---

## §5　自報錯誤

1. **`NR1L-RVC-113` 違 A-CA17 而我未察**（§1-5）—— A-CA17 於 2026-09-16 即登記
   「以 HMI 語言設定為觸發，不走 CAN」，我在 CAM-09 生成 `-113` 時未查 ANOMALIES 即寫 `Send CAN`，
   並因此開了一個**本不需要**的 DR-CAM-o。本輪自查發現並更正。
   **生成前應先掃該 SWE 列相關之 ANOMALIES**，此點建議入 profile（本包未自行寫入）。
2. **batch04 初稿之 `P`=2** —— `split_reason`（test_item 括號下半）含 `HU_ZOOM.req = Not Pressed`
   之賦值形，被 `P` 攔下。**括號下半亦受 `P` 檢**，此前未意識到。
3. **下放包 §2 之三處預期與實際 in-scope 不符**（§2-2）—— 非我之誤，惟若我於步驟 A 即回報
   而非直接生成，分析層可更早調整；本輪兩者同時交付。
4. **B 本 `est_tc` 為 1／leaf 之基準值而非逐列判讀**（§3-1）—— 其為計畫階段之下限估計，
   已具名；真實數待各批步驟 A。

---

## §6　未結

| # | 項 | 去處 |
|---|---|---|
| 1 | DR-CAM-a／f／g／h／i／j／k／l／m／n／o／p／q（十三條）| 補件；**f／i／q 交付前必結** |
| 2 | §2-2 之三處（`-025` 無來源、`-023`／`-021` 之實際範圍）| Pei 確認 |
| 3 | §3-2 難點 B（SYS1 檔名之空白與 `+`，是否統一底線）| Pei 裁 |
| 4 | §5-1 之「生成前先掃 ANOMALIES」是否入 profile | Pei 裁 |
| 5 | **B 本八批**（B01a/b、B02a/b、B03a/b、B04、B05）| 下包 |
| 6 | A 本之交付（Phase 7）：dry-run ／ `--write` ／ tag | 下包 |
| 7 | GCB-07／GCB-08 與 GC 線其餘六項 | GC 線 |
| 8 | `bench_verify.md` 六列之實機驗證 | 交付前 Pei |

---

## §7　本包寫入之檔案（git 未 commit）

```
M  features/camera/RULINGS.md                       R-CAM17                sha256_16 d296f796f40d1712
M  features/camera/DECISIONS.md                     6-43 ～ 6-46
M  features/camera/DATA_REQUESTS.md                 DR-CAM-f 擴／DR-CAM-o 縮
M  docs/runtime/profiles/FW036_R1L_Camera_Profile.md  §9 三常數／新 §10      sha256_16 c6b69b0136034876
M  docs/fw036/handoff/GC_BACKLOG.md                 GCB-08                 sha256_16 a6ae148bf210420e
M  features/camera/data/coverage.tsv                240 TC                 sha256_16 00917d4c7ec08bf8
M  features/camera/generated/batch02c/NR1L-RVC-113.{json,md}                A-CA17 更正
?  features/camera/data/batch04_plan.tsv            115 列                 sha256_16 bdd0f20175147c9a
?  features/camera/data/b_plan.tsv                  230 列                 sha256_16 0a9b1d3ef431ecc2
?  features/camera/generated/batch04/               26 × json ＋ 26 × md ＋ INDEX ＋ LINT_REPORT
?  docs/fw036/handoff/up/20260923_CAM-13.md         （本檔）
   其餘八個 generated 目錄                           未動（本輪）
```

`batch04` 之 26 份 json sha256_16：

```
-215 3c2b892b73b09566  -216 b085b3905450c2a5  -217 c2c0bf17527df6e0  -218 af50a296aced0d9c
-219 a575101a66c34c05  -220 61ae8a80d0b90c6f  -221 6c17ae26c848a455  -222 4cbf793c8293851f
-223 97afc30684dd02d6  -224 05849f1e8089923f  -225 02a76018802076e1  -226 b6fe7f5759a6ac6b
-227 2a1c21b3848f283d  -228 1ffbe4dd206170c5  -229 3269e62c64e99a9a  -230 02af5358c13ac430
-231 edc30660ca39f633  -232 3fd74def6dfa22bb  -233 868331738debb24f  -234 81f345daf1c6057e
-235 3fb8b948a226be00  -236 0a44afd1cea9e2e4  -237 ac3628b3e714221e  -238 13f862f6a83832ed
-239 6503dce1671cd16a  -240 58eef0363f7bcb92
```

### 複驗指令

```bash
python3 scripts/rulings_hash.py --target features/camera/RULINGS.md --out /tmp/cam.sha.tsv
python3 features/camera/scripts/selfcheck_camera.py features/camera/generated/{pilot02,batch01,batch01b,batch02a,batch02b,batch02c,batch03a,batch03b,batch03c,batch04}
python3 features/camera/scripts/lint_batch.py <暫存目錄> features/camera/generated/batch04
```
