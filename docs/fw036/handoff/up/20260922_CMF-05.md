# 上繳包 CMF-05 — 車型欄填值 ＋ DR-46 結案 ＋ 命名以實際值為準 → 候選本 v4

- 日期：2026-09-22
- 對應下放包：`down/20260922_CMF-05.md`（前置 `down/20260922_CMF-04_review.md`）
- 性質：Pei 四答之落帳、車型欄逐 TC 填值、DR-46 結案、作者側命名改用實際值、候選本 v4。
  **未入 `delivered/`、未覆寫 0907 本與 v1／v2／v3、未 commit、未跑 `--gate`／`--regen`、未重跑 generator。**
- 開工前 `git status`：**CMF-04 產物已提交**於 `1245efb feat(comfort): CMF-04 — vehicle-model plans (all stop points hit), R-C60/R-C61`（28 檔，含三份 plan、`lint036.py` 之 R-C61、測試檔修正、ENTRY 039）。開工時工作區內唯一之未追蹤檔為本下放包 `down/20260922_CMF-05.md`。
- R-G62(b) 之觀察依據：起點前 `features/comfort/` 之最後他方寫入為上列 commit（同日稍早），本輪期間未觀察到他方寫入。**「無他 session 寫檔」不可自證，此處只陳述未觀察到。**
- R-G62(c)：`up/20260922_CMF-05.md` 落檔前已證該路徑不存在。

---

## 0. 首段

| 項 | 結果 |
|---|---|
| **停點** | **未命中**。§3 之停點為「實產 > 120」，**實產 0 條**；§4-2 之停點為「三台 DBC label 大小寫不一」，**兩件 ATL-Mi DBC 皆 `BLINK`，一致** |
| **實產拆題數** | **0**（`vm_split_plan.tsv` 之 71 條逐條複判，全數判為不需拆；理由見 §3） |
| **TC 總數** | **476**（不增減） |
| **三台 `1` 條數** | V Promaster **407**／Y Toro **21**／Z Fastback **409**（自產出檔讀回，與 `vm_assign.tsv` 逐格相符） |
| **全 `0` 條數** | **65**（依 **R-C63** 照填出貨，不加 Remarks、不移除） |
| **PENDING 行數** | **0**（預期目標達成）—— DR-46 結案，四行改為 `Send CAN:`；候選本 `U` 由 4 → **0** |
| **`$…$` 殘留數** | **0**（作者側四欄；`test_item` 上半與下半本即 0） |
| **對 v3 差異格數** | **608**，全數歸因（未歸因 0） |
| 對 0907 本 | 不符 **0**／21,309 格；變更 3,454 格全數歸因 |
| LINT | JSON 層 **57/57 PASS、476 TCs、0 finding**；候選本層 **H=0 W=0 J=0 P=0 L=0 U=0**。`LINT_STATUS: OK` |

**三件需要分析層注意**：

1. **label 是 `BLINK` 不是 `Blink`**（§4）。下放包 §4-2 所書之 `(Blink)` 取自 **Atl-Hi** 之
   `BCM_FD_27.EBL_Stat`（`VAL_ … 2 "Blink"`）；本交付之平台為 Atlantis（MID），
   其兩個訊號於兩件 ATL-Mi DBC **皆為 `"BLINK"`（全大寫）**。依「label 逐字取 `VAL_`」取 `BLINK`。
2. **拆題實產 0 條，不是沒做**（§3）。`vm_split_plan.tsv` 之 71 是**三台全集下**之啟發式上界；
   Toro 依 R-C48(amend2) 為 0 之後，餘下之 V／Z 在兩個相關軸上**同值**
   （`Climate_Type` 皆 2、`Rear_Climate` 皆 0），差異消失。71 條逐條列於 §3-2。
3. **一處超出裁定文字之處置**（§5）：R-C60 只點名 `Face/Feet` 與 `Feet plus Windshield`，
   我把同類之 `Face + Feet`（1 處引號）一併拿掉。回退成本為一行。

---

## 1. 裁定落帳（Pei 2026-09-22 四答）

### 1-1. `RULINGS.md`

落帳前實測現行最大號為 **R-C62**（`grep -oE "^R-C[0-9]+"`），暫編＝實編：
**R-C63／R-C64／R-C65**，加旁標 **R-C48(amend2)**、**R-C62(amend)**。
條文逐字取 CMF-05 §1 表（R19-2），各條下附本輪之落實。sha8 未產（GC-16 未落地）。

### 1-2. profile 同步

| 節 | 處置 |
|---|---|
| §3.9 | 「逐軸細分只出 plan、V／Y／Z 一律先填 1」整段劃線 → **逐 TC 填值**：T＝U＝W＝X＝0 全列，V／Y／Z 依 `vm_assign.tsv`；附 Toro 基線（R-C48(amend2)）之三個 PROXI 值與 2.14 逐字、實測三台條數、65 條全 0 之處置（R-C63）、拆題 0 條、未對映之三軸（R-C62-4） |
| §0 | 新增 **R-C64／R-C65**（命名以實際值為準、平台 ＝ Atlantis MID、上游三套命名而無對照表）與 **A-CMF19 之記法**（跨層指涉一律書 `req_id`；工作簿 F 與 JSON tc_id 自 R-C46 起為兩套號碼，引用時須標明是哪一套） |

### 1-3. `ANOMALIES.md`／`DATA_REQUESTS.md`

- **A-CMF18～A-CMF20** 逐字取 CMF-04_review §1。A-CMF20 下另記：Pei 以 R-C64 把「證不出來」之
  處置由「停」改為「依實際檔案」——**兩者都記，以免日後讀成對映已獲證明**。
- **A-CMF21** 新登（執行層自陳）：`$Rear_HVAC_cfg$` 為第三套命名（§4-3）。
- **DR-46 標 `[CLOSED 2026-09-22]`**，條目下補六點結案紀錄（平台、LID 對映、DBC 逐字、
  label 大小寫、改寫後之四行、`U` 歸零）。**DR-47／DR-48 維持 open；DR-49 仍不登。**

---

## 2. 車型欄（CMF-05 §2）

- T＝0、U＝0、W＝0、X＝0 **全列**（R-C48(amend)／R-G74）。
- V／Y／Z **逐條**取 `docs/reports/vm_assign.tsv`（R-C62 之四分支 ＋ R-C48(amend2) ＋ R-C63）。
- 新拆之 sibling：**無**（實產 0，§3）。

**填後彙總（自候選本 v4 讀回，476 資料列）**：

| 欄 | 表頭 | `1` 之條數 |
|---|---|---|
| T | HDCC27 Atl-Hi | 0 |
| U | DT27 Atl-Hi | 0 |
| **V** | **Promaster** | **407** |
| W | Commander (598) | 0 |
| X | Regengade (5210) | 0 |
| **Y** | **Toro** | **21** |
| **Z** | **Fastback** | **409** |

**V／Y／Z 皆 0 者 65 列**（與 CMF-04 之預期一致 —— 拆題 0 條，故無變動）。
Toro 之 21 條：19 條為 PC 無配置軸之座椅／comfort 類（`076`～`082`、`122`），
2 條為 `The vehicle has 3 knob HVAC controls with ICS` 之**正向** PC（`020-03`／`020-04`，V0Y1Z0）。

---

## 3. 車型拆題（CMF-05 §3）—— **逐條複判後實產 0 條**

### 3-1. 判準與其結果

下放包 §3 之判準：「該 TC 之觸發或 ER **確實**隨該軸之 PROXI 值改變（不只是 body 出現字樣）。
只有 PC 已宣告該軸者不拆（改車型欄即可）。」

把它機械化為一個可查的條件：**該差異軸，在該 TC 之「適用車型集合」（`vm_assign` 為 1 者）內，
是否仍取到兩個以上不同的值。** 若只剩一個值，這條 TC 在本輪三台上不會有兩種行為，拆了也只是
把同一條抄兩份。

三台之值（`Dec`，R-C62）：

| 軸 | Promaster | Toro | Fastback | **V 與 Z 是否同值** |
|---|---|---|---|---|
| `Climate_Type` | 2 (Automatic) | 1 (Manual) | 2 (Automatic) | **同** |
| `Rear_Climate` | 0 (Absent) | 1 (Present) | 0 (Absent) | **同** |
| `Climate_Panel_Type` | 7 (No knobs) | 6 (3 knobs) | 0 (Only blower knob) | 異 |
| `Automatic_Recirculation` | 0 (Absent) | 1 (Present) | 0 (Absent) | 同 |
| `HVAC_Mode_Type` | 0 (No Trimode) | ABSENT | ABSENT | 異 |

**Toro 一旦依 R-C48(amend2) 為 0，`Climate_Type` 與 `Rear_Climate` 這兩個軸就不再有差異** ——
而 71 條候選中有 70 條正是落在這兩軸。

### 3-2. 71 條之逐條歸屬

| 不拆之理由 | 條數 |
|---|---|
| 差異軸於適用集合 `{V, Z}` 內**同值**（`Climate_Type` 38、`Rear_Climate` 32 之絕大多數）| **63** |
| **適用車型集合為空**（三台皆 0，依 R-C63 照填一條即可，無可分之車型）| **8** |
| 合計 | **71** |

逐條之 `tc_id`／`req_id`／軸／適用集合／該軸於集合內之值，已寫回
`docs/reports/vm_split_plan.tsv` 之新增三欄（`applicable_vehicles`／`axis_values_in_set`／`verdict`）。

**唯一之 `Climate_Panel_Type` 候選**（`020-04`，V 與 Z 於該軸異值）之適用集合為**空**
（三台皆 0），故亦不拆。

### 3-3. 這個結果之界線（請核）

- 判準用的是**本輪三台**。若日後加入第四台車，同樣這 71 條可能就需要拆 —— **plan 不作廢**，
  `vm_split_plan.tsv` 保留其 71 列與新增之判定欄。
- 判準假定 `vm_assign.tsv` 之 0／1 正確；其中 **42 條之 `test-setup 蘊含`** 分支是執行層之判斷
  （CMF-04 §2-5，經 CMF-04_review 准）。那 42 條若翻轉，本節之結論要重算。
- **停點（實產 > 120）未命中** —— 實產 0。

---

## 4. DR-46 結案（CMF-05 §4）

### 4-1. DBC 逐字舉證（`grep -a`）

車型 ↔ DBC 之歸屬取 `forms/FORMS.md` §「ATL-Mi DBC 兩件」之 (c) 欄逐字：
**`637MCA = ProMaster（ATL-Mi）BH-CAN`**、**`P363（ATL-Mi）BH-CAN`**；
`PDT27_E2A_R1_BHCAN2`／`R1_FDCAN8` 為 **Atlantis High**，同節 (e) 欄明載「對該線降旁證」。

| DBC | `HVACRecirc_Sts` | `HVACRearDef_Sts` |
|---|---|---|
| `Project__637MCA_BH-CAN_R1_(29_01_2025)_plusCR19670.dbc`（**Promaster**）| `SG_ HVACRecirc_Sts : 1\|2@0+ (1,0) [0\|3] "" ETM,HCPM,TBM`<br>`VAL_ 1482 HVACRecirc_Sts 0 "OFF" 1 "ON" 2 "BLINK" 3 "SNA";` | `SG_ HVACRearDef_Sts : 3\|2@0+ (1,0) [0\|3] "" ETM,HCPM`<br>`VAL_ 1482 HVACRearDef_Sts 0 "OFF" 1 "ON" 2 "BLINK" 3 "SNA";` |
| `P363_BH-CAN [07338]_3A_R2.dbc`（**Fastback**）| `SG_ HVACRecirc_Sts : 1\|2@0+ (1,0) [0\|3] "" ICS,LTM,TBM`<br>`VAL_ 1482 HVACRecirc_Sts 0 "OFF" 1 "ON" 2 "BLINK" 3 "SNA";` | `SG_ HVACRearDef_Sts : 3\|2@0+ (1,0) [0\|3] "" ICS,LTM`<br>`VAL_ 1482 HVACRearDef_Sts 0 "OFF" 1 "ON" 2 "BLINK" 3 "SNA";` |
| **Toro** | **`forms/` 內無 Toro DBC** —— ATL-Mi 只登錄上列兩件 | 同左 |

**Toro 之缺件不構成缺口**：這四條之 **Y 欄皆為 0**（`vm_assign.tsv` 實查，2.14 排除，
R-C48(amend2)），Toro 不執行之。故 §4-3 之「於任一台查無 → 維持 PENDING」不適用 ——
**「任一台」讀為「該 TC 適用之任一台」**。若分析層之意是「三台不分適用與否皆須有 DBC」，
請指正：那樣這四條要退回 PENDING，`U` 會回到 4。

**label 大小寫一致**（兩件皆 `"BLINK"`）→ §4-2 之停點未命中。

### 4-2. 四條之改寫（canon §8.7.5(c)(d)）

| req_id | 原 Procedure 行 | 新 Procedure 行 | 新 ER 行 |
|---|---|---|---|
| `006-02`（步 1）| `PENDING: DR-46 DBC message and raw value for $RECIRC_STAT$ = "not available/Blink"` | `Send CAN: STATUS_CLIMATE2.HVACRecirc_Sts = 2 (BLINK)` | `STATUS_CLIMATE2.HVACRecirc_Sts = 2 (BLINK) is sent` |
| `012-06`（步 2）| 同上 | 同上 | 同上 |
| `013-02`（步 1）| `…for $EBL_Stat$ = "not available/Blink"` | `Send CAN: STATUS_CLIMATE2.HVACRearDef_Sts = 2 (BLINK)` | `STATUS_CLIMATE2.HVACRearDef_Sts = 2 (BLINK) is sent` |
| `114-02`（步 1）| 同上 | 同上 | 同上 |

- 該步原 ER 為 `The climate screen is displayed`（CMF-02 §2 取 `wb-033` 之佔位句），
  改為 §8.7.5(d) 之 `<MESSAGE>.<Signal> = <raw> (<label>) is sent`。
- `test_item` 上半之 `$RECIRC_STAT$`／`$EBL_Stat$` 依 §8.7.5(b) **保留**。
- 四行改寫後以 `lint036` 之 `check_signal_line` 實測：`Send CAN:` 前綴與
  `<MSG>.<Sig> = <raw> (<label>)` 形態皆合規，`P` 無新增。

### 4-3. `$Rear_HVAC_cfg$` 之 5 條 PC（CMF-05 §4-4）

原（`[ext-verbatim]`）：
`The below requirements shall be implemented when the PROXI parameter $Rear_HVAC_cfg$ = [Present] — the vehicle is equipped with rear climate (CFTS043 NEWR1L-53677)`

新（canon §8.7.5(e)）：
`[spec-derived] PROXI Rear_Climate = 1 (Present) — the vehicle is equipped with rear climate (CFTS043 NEWR1L-53677)`

**source class 由 `[ext-verbatim]` 改為 `[spec-derived]`** —— 這一行改寫之後已不是逐字引用，
留著那個標籤是對讀者宣稱一件不成立的事（R-C45 立 `ext-verbatim` 一類的理由正是如此）。
出處 `(CFTS043 NEWR1L-53677)` 保留，`specification_reference` 之 CFTS043 行不動。
**這是本輪唯一一處 source class 之變更，請核。**

實查：`forms/proxi/` **六份檔中並無 `Rear_HVAC_cfg`，只有 `Rear_Climate`**
（三台之命中格 `PROXI Write_Read!R355C6`／Toro `PROXI Write!R355C6`）。登 **A-CMF21**。

### 4-4. `$…$` 全本掃描（CMF-05 §4-5）

作者側四欄（`pre_conditions`／`input_test_data`／`test_procedure`／`expected_result`）之
`$…$` 於改寫前實測 **9 處**（`$Rear_HVAC_cfg$` 5、`$RECIRC_STAT$` 2、`$EBL_Stat$` 2），
改寫後 **0 處**。`test_item` 上半與下半之 `$…$` **本即 0 處**（本 feature 之上半取自
`section_fulltext`，該 spec 之條文不用 `$…$` 記號）。**無「查無實際名而須待 Pei」者。**

---

## 5. R-C60 —— 合稱去引號（CMF-05 §5）

實產 **5 處／4 行**：

| req_id | 欄 | 行 |
|---|---|---|
| `015-01`（`-443`）| test_procedure | `2. Select the "Feet plus Windshield" airflow mode` → 去引號 |
| 同 | expected_result | `2. The "Feet plus Windshield" button is highlighted and its size is increased` → 去引號 |
| 同 | test_item 下半 | 同句兩處 → 去引號 |
| `033-02`（`-405`）| test_procedure | `1. Select the rear "Face + Feet" mode` → 去引號 |

**`Face + Feet` 未見於 R-C60 之條文** —— 它與另兩者同類（兩個模式之合稱，條文 `7.8` 有其字面
而無按鍵標籤），且同樣只有這一處帶引號、其餘 6 處未帶。一併拿掉以求一致，於此具名。
若分析層判 R-C60 只及所列二者，回退即改回這一行。

改後全本合稱之引號數：**0**（原 5 處）。

---

## 6. 候選本 v4（`scripts/cmf05_revise.py`）

- 產出：`features/comfort/sandbox/revise_0922b/…_SWQT_ComfortHMI_20260922_v4.xlsx`
  - sha256 `7b3912fcaf4780ff7490e76c2fba18bc00881f657521615b656266c3a89d6bd4`
- 母本：0907 本（先驗 sha `3550d16d…`）之複本；另先驗 v3 之 sha（`5d372682…`）。

| invariant（★ ABORT 級） | 結果 |
|---|---|
| ★ zip member 數 | 57 ＝ 57 |
| 差異僅目標 sheet | `xl/worksheets/sheet6.xml` |
| ★ DV sqref | 逐字相同 |
| B 欄公式 row 10–601 | 逐列原樣 |
| **對 0907 本逐格對帳** | **不符 0 格**／21,309；變更 **3,454 格**全數歸因 |
| **對 v3 逐格對帳** | 差異 **608 格**，未歸因 **0** |

**對 v3 之 608 格歸因**：R-C48(amend2)/R-C62 車型欄 **580**、M6+R-C64/DR-46 5、
R-C64/PROXI 4、R-C64/DR-46 3、R-C60 3、§3 sibling 12、M7+R-C60 1。

**對 0907 本之 3,454 格歸因**：車型欄 2,444、R-C49(amend) 452、§3 sibling 233、M7 144、
M9-compound 67、M9-Note 22、R-C50+R-C50(amend) 14、R-C50 12、R-C51 10、M2 6、
M7+M9-compound 6、M6+R-C64/DR-46 5、R-C52 4、R-C64/PROXI 4、M8 3、wb-006 3、
R-C55 slim 3、R-C60 3、R-C64/DR-46 3、軸 2 PC 2、M7+M9-Note 2、R-C61 2、M6 1、R-C56 1。

**呈現面（Tier 3）與 CMF-02_review §3 末段之三件仍未動**（新列列高／SmartArt 舊名／`J5` 日期）。

---

## 7. 自檢

| 檢查 | 0907 基線 | v3 | **v4** | 差之成因 |
|---|---|---|---|---|
| **U**（PENDING 佔位）| 0 | 4 | **0** | **DR-46 結案** |
| H／W | 3／3 | 0／0 | **0／0** | R-C61 之豁免 |
| J／L／P | 0／26／0 | 0／0／0 | **0／0／0** | — |
| M／R／I-cross／X | 8／1450／433／319 | 10／1486／443／337 | 同 v3 | 本輪未增列 |
| A–G／I／I-sibling／K／N／Q／T／V／Y | 0 | 0 | **0** | — |

- **JSON 層**：`lint_tcs.py` **57/57 PASS、476 TCs、0 finding**。
- **可重現**：`git checkout HEAD -- generated` 後重跑 `cmf05_apply.py`，129 個 JSON **逐位元組相同**；
  重跑 `cmf05_revise.py` 得**相同 sha256**、`MISMATCH_COUNT: 0`。第二次執行於斷言即 `SystemExit`。
- **`canon_refs.py --report --waiver`**：工作區 **35／178**；以 `git worktree`（**未用 `git stash`**）
  取 HEAD(`1245efb`) ＋ 本下放包之基線，同為 **35／178** → **本包 unresolved／ambiguous 0 筆**。
- **全庫 pytest**：HEAD 基線 10 failed／1,366 passed；工作區 **9 failed／1,389 passed**；
  **只在工作區失敗者 0 項** → 無回歸。
- `DELIVERY.sha256` 追加 **ENTRY 040**；`shasum -a 256 -c --ignore-missing` **61 OK、0 FAIL**。
- **唯讀紀律**：`down/`／`up/` 內無任何檔被移動、改名或覆寫；暫存落 session scratchpad。

### 自報

1. **「三台 DBC」我讀成「該 TC 適用之三台中之適用者」**（§4-1）。Toro 無 DBC 而其 Y 欄為 0，
   我據此結案而非退回 PENDING。這是本輪最關鍵的一個判斷，若讀法不同，四條要退回。
2. **`ext-verbatim` → `spec-derived`**（§4-3）是下放包沒寫的一步，但不改就等於留一個假標籤。
3. **`Face + Feet` 超出 R-C60 之文字**（§5）。
4. **拆題 0 條之結論依賴 `vm_assign.tsv` 之 42 條 `test-setup 蘊含`**（§3-3）。
5. **R-C64 使兩個「無法證同一物」之軸（ATC／MTC、ICS）在候選本中變成已採用之對映**，
   而 A-CMF20 所記的「本表有一半是假設」**這件事本身沒有被證實或推翻，只是被裁定為可用**。
   我在 `ANOMALIES.md` 之 A-CMF20 下加了一句，使兩者不會被讀成同一件事。
6. **EMEA ICS 之 426 條仍為 R-C62-4（三台維持 1）**：本交付三台是否為 EMEA ICS 車，
   PROXI 無參數可判，本輪未處置，仍在 `vm_unmapped.tsv`。

---

## 8. 待分析層／Pei

1. **§4-1 之讀法**：「三台 DBC」是否含不適用之車型？（影響 DR-46 是否真的結案。）
2. **§4-3 之 source class 變更**（`ext-verbatim` → `spec-derived`）核或退。
3. **§5 之 `Face + Feet`** 核或退。
4. **EMEA ICS 426 條**（§7 自報 6）：三台是否為 EMEA ICS 車？否 → 這 426 條之 V／Y／Z 要重算。
5. 沿前幾包未決者：R-C44 位階、`ENTER_COMFORT` 入 canon §5.3、DR-49 登與不登、
   導航改寫（`nav_rewrite_plan.tsv`，290 條之 hop 標籤條文查無）。

---

## 9. 產物與 SHA256

| 路徑 | sha256 |
|---|---|
| `features/comfort/sandbox/revise_0922b/…_SWQT_ComfortHMI_20260922_v4.xlsx`（候選本 v4） | `7b3912fcaf4780ff7490e76c2fba18bc00881f657521615b656266c3a89d6bd4` |
| `features/comfort/scripts/cmf05_apply.py`（新增） | `2476912b86283f2c677ff6ffb59654cdad1e46778eab5cf3a3f08f0882add3fb` |
| `features/comfort/scripts/cmf05_revise.py`（新增） | `39bbf2844f632adbf679fdc9035f69f3ee0a60154de34c010ba5bb784c89d9b4` |
| `features/comfort/docs/reports/cmf05_json_change_log.tsv` | `b2aa15338f9529b5bb6476af095ea6f1df46b018e0c8a7360e3b33696e5772b9` |
| `features/comfort/docs/reports/cmf05_candidate_cell_recon.tsv` | `f9cb2dcf40f16dbd42796684637ee8cf529f6c3bc6076b613c571a29d8a473fb` |
| `features/comfort/docs/reports/cmf05_vs_v3_cell_delta.tsv` | `85211414d87456f7e3974777b34f2d375ba8edb7ceb80409c88acff9add82667` |
| `features/comfort/docs/reports/vm_split_plan.tsv`（補三欄判定） | `ded08dc26b0ae4e6af8e1237c055c4fac9d2e351be21e158470780fc2f53064b` |
| `features/comfort/sandbox/revise_0922b/lint/LINT_STATUS.txt` | `b83d5ef5aea3d5406363f7232513096cf1eb7007edf544b09ef207c123b1d726` |
| `features/comfort/RULINGS.md` | `c88d475103db52b6d9acd0258a5e8aa03f94b357867166f6437f174aad893889` |
| `features/comfort/ANOMALIES.md` | `c97b81b3629889bf4558a8fc9e9d1cc72397312cfd1aadd99d7e73647944a5a7` |
| `features/comfort/DATA_REQUESTS.md`（DR-46 CLOSED） | `180d7d0d09c68bfe26608e3d73c7d730796a00e2c44e2c3328463cc981e93ded` |
| `features/comfort/DELIVERY.sha256`（追加 ENTRY 040） | `edabdae8f1ad57161f8bad9016e9a199c584b9763b895688b6b73bf286bfe258` |
| `docs/runtime/profiles/FW036_R1L_Comfort_Profile.md` | `959b3f3f00c9e567d6ffb5c0e6a3a2c5f3980e99a60e66af3299ecacddb62187` |
| `features/comfort/generated/*.json`（9 檔改動，476 TCs） | 見 `cmf05_json_change_log.tsv`；可由 `cmf05_apply.py` 自 HEAD 逐位元組重現 |

**old→new：無**。**新增 TC：無**（476 → 476）。
