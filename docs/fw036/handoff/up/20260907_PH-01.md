# 上繳包 PH-01 —— Phone feature intake（scaffold ＋ 來源登錄 ＋ recon）

下放包：`docs/fw036/handoff/down/20260907_PH-01.md`
執行層 session：新開（R-G62(b)）。執行日：**2026-09-07**。
結果分類（FO §8.4）：**部分完成 ＋ 一項升級**。

---

## §0　一句話

**Phase 0–1 之可做者全數做完；`§7 升級條件第 2 項成立而停** ——
`_intake/Phone/` 之 SYS1 與 `spec-index/cache/` 之同名檔不只 sha 不等，
**是兩份互不相容之匯出**（章節號在 `5.17`～`17.5` 錯位一格，124 列），
**且兩份皆非 037 之來源**。`spec_reference` 無權威來源，Phase 2 以後未啟動。

另有一項**未預期之發現**：下放包 §4 任務 3-2 之前提「037 無章節號」與實測不符
——037 `HMI Source ID` 欄**本身即載 `{檔名}_{章節號}`**，故存在比 description
文字比對更強之鍵接法；兩法並陳於 §4-2。

---

## §1　5 檔 sha256 全表（§6-1）

`_intake/Phone/` 實測 **5 檔到齊**（另有 `.DS_Store`，非素材）。

| # | 檔名 | bytes | sha256 |
|---|---|---:|---|
| 1 | `FM-WI-FSM-037-A03-N1L-SWE1-Phone-HMI-V0.1 STLA 報告.xlsx` | 196 322 | `2d12fa11a4fc21163148e179b2a64138d6c8412c82f6d9e6a17fbb18e0fbf9eb` |
| 2 | `SYS1_HMI_Phone_HMI_Logic_and_Flow_R1_SR24_Post 2A_(June_21_2022).xlsx` | 94 468 | `e6d58766cf6ddb19e6cdfc93bea01d77c03037123f65fa95386f6238782edd8a` |
| 3 | `Phone HMI Logic&Flow R1 Change Log(26PI).xlsx` | 24 572 | `405eb303c848f0e0746e62b421e9f17a6e6445bb9b92bb7ec3d6ce73caea06d0` |
| 4 | `Phone HMI Logic&Flow R1.pdf` | 45 717 191 | `7180b8aff2eaee309679eaeba93867b45a36417c0833d9ab854c2dd0b2dc5ed8` |
| 5 | `Phone HMI Logic and Flow R1 SR24 Post 2A (June 21 2022).pdf` | 9 327 368 | `46bdbeecaa3729891ed8fccbd65c0b002962fce0a3f5c6c2a9e26a7aaf21fa1c` |

前三者之 sha16 與下放包 §5 **全數相符**。PDF 二件為分析層未取 sha，本包首載。

### SYS1 與 `spec-index/cache` 之同體判定 → **不等，且不同體**

| 來源 | sha256 | docProps `modified` | Basic Report 列 | 頂層章 |
|---|---|---|---:|---:|
| `_intake/Phone/…` | `e6d58766cf6ddb19e6cdfc93bea01d77c03037123f65fa95386f6238782edd8a` | 2026-05-19T03:00:52Z | 273 | 27 |
| `spec-index/cache/…` | `61c3a6a022f8dd18c0e724b411c492fbeafc153e4f814880ace16ed2e278d2f7` | 2026-03-16T06:33:04Z | 273 | 27 |

→ **§7 升級條件第 2 項成立**。詳見 §5。

---

## §2　scaffold 產物與 R-PH 取號（§6-2）

### 2.1　`new_feature.py Phone --root .`

`features/phone/` 未存在，未觸發 §7 第 3 項。產物：

```
features/phone/{.gitignore,ANOMALIES.md,DATA_REQUESTS.md,DECISIONS.md,
                PLAYBOOK.md,RULINGS.md,RUNBOOK.md,feature.yaml}
features/phone/{inputs,data,batches,generated,scripts,docs}/      ← 六子目錄
```

本包另建（非 scaffold 產物，逐項具依據）：

| 路徑 | 依據 |
|---|---|
| `features/phone/sandbox/base/…_SWQT_20260817_ext.xlsx` | R-G1 母本；R-G64 「xlsx 只准在 sandbox 改」。sha256 `6372fb6be02f48dc…`，與 `forms/` 母本**全等** |
| `features/phone/RECON.md` | `recon.py` 產出 |
| `features/phone/data/*.tsv`（4 支） | R-G64 「量測中間物」 |
| `features/phone/docs/26pi_delta.md` | 下放包 §任務 2 第三項 |
| `docs/runtime/profiles/FW036_R1L_Phone_Profile.md` | 下放包 §任務 4 |

`inputs/` **恆空**（R-G66：feature 端不存原檔副本）。

### 2.2　R-PH 取號 —— 現查結果 `{live}` = **1**（與下放包期望值相符）

落檔前掃 `features/phone/RULINGS.md`（scaffold 產出，內容為 `(no rulings recorded yet)`），
無任何既存 `### R-PH`，故自 1 起。

| 條 | 標題 | ruling sha8 | **body sha8** | 落點 |
|---|---|---|---|---|
| **R-PH1** | Test Group | `d7cf79d9` | **`d1f5095d`** | `features/phone/RULINGS.md:17`（14 行，fenced） |
| **R-PH2** | 範圍 | `f7120f43` | **`3e09dd43`** | 同上 `:34`（17 行，fenced） |
| **R-PH3** | HFP 與 Phone 各為獨立 feature | `6ab7759c` | **`3146ca30`** | 同上 `:54`（15 行，fenced） |

`rulings_hash.py --root .` 已重生 `docs/fw036/RULINGS.sha.tsv`：
**771 錨點，來源 21 檔**（前為 20 檔，本 feature 之 `RULINGS.md` 入掃描面）。
`git diff --stat` 實測**只增 3 列，無任何既有列變動** —— 本次無夾帶。

### 2.3　裁決引用之 sha 現查（R-G52@dabfa307）

| 引用 | 下放包所載 | 現查 | 判 |
|---|---|---|---|
| `R-G42`（ledger 本） | `f111d3dc` | `f111d3dc`（`RULINGS_LEDGER.md:1304`，60 行；body `4e5b5b0f`） | ✅ |
| `R-G63` | `a25150c1` | `a25150c1`（`FEATURE_ONBOARDING.md:1059`，39 行） | ✅ |
| `R-G64` | `34b81800` | `34b81800`（`FO:1101`，21 行） | ✅ |
| `R-G66` | `1951347c` | `1951347c`（`FO:1142`，15 行） | ✅ |
| `R-G67` | `823477ed` | `823477ed`（`FO:1160`，48 行） | ✅ |
| `R-G70` | `c964dfb8` | `c964dfb8`（`RULINGS_LEDGER.md:1791`，70 行） | ✅ |
| `R-G71` | `72328d57` | `72328d57`（`RULINGS_LEDGER.md:1729`，46 行） | ✅ |
| `R-G23` | 無 sha 列 | `RULINGS_LEDGER.md:502`「綁定須被檢查」節，已讀，取號依其現查 | ✅ |

**七條全符，未觸發 FO §8.4 之停。**（`R-G42` 另有 `ba593120` 之 FO 側版；
下放包指定 ledger 本，本包取 `f111d3dc`。）

---

## §3　§5 預期數字全表對照（§6-3，相符者亦列）

| 項 | 預期 | **實測** | 判 |
|---|---|---|:--:|
| 037 sha16 | `2d12fa11a4fc2116` | `2d12fa11a4fc2116` | ✅ |
| SYS1 xlsx sha16 | `e6d58766cf6ddb19` | `e6d58766cf6ddb19`（`_intake` 本） | ✅ |
| Change Log sha16 | `405eb303c848f0e0` | `405eb303c848f0e0` | ✅ |
| 037 資料列 | 911 | **911** | ✅ |
| parent／child | 213／698 | **213／698**（A 欄 `-` 數分佈 `{2:213, 3:698}`，無其他值） | ✅ |
| Categorization | Functional 754／Heading 157 | **754／157**（G 欄，僅此二值） | ✅ |
| FROP Top-2 | Core HMI 621／Device Manager 197 | **`Core HMI(System UI,Pop-Up, menu bar...)` 621／`Device Manager` 197** | ✅ |
| Source ID 範圍 | `…-016`～`…-271`，唯一 213 | **`SYS-HMI-RA-PHONE-016`～`-271`，唯一 213** | ✅ |
| SYS1 Basic Report 列 | 273；頂層章 27 | **273／27**（`_intake` 與 `cache` **兩份皆是**） | ✅ |
| 帶前綴標籤之 parent | 33（MP 9／PSC 5／MPDND 4／MPS 3／MPP 3／BTP 2／WC 2／MPA、NPL、PPS、MSMS、PW 各 1） | **33，分佈逐項相符** | ✅ |
| Multiphone 關鍵字子集 | 42 parent／120 child | **35 parent／108 child** | ❌ |
| `multiple phones\|paired simultaneously` | 0 hit（037 與 SYS1 皆） | **0／0**（SYS1 兩份皆查） | ✅ |
| Change Log 列 | 1（26PI，2026-05-20，MPA11＋MPA12，Under Review） | **1**，逐欄相符 | ✅ |

**13 項中 12 項相符，1 項不符。不自行調和（FO §8.2）。**

### 不符項 —— Multiphone 關鍵字子集（A-PH06）

- **本包所用查詢條件（逐字揭露）**：對 `Analysis Report` 列 8 起、A 欄非空之 911 列，
  取 E 欄 `Requirement Description`，先去 `^The system shall ` 前綴，
  再判 `^(MPA|MP|MPS|MPP|MSMS|MPDND)\d`；或 E 欄 `re.I` 命中
  `multiphone|two phones|second device|Favorite [12] phone|priorit`。
  parent 取 A 欄 `-` 數 = 2；child 以「parent 命中則其 child 全帶入」計。
- **實測 35 parent／108 child**（直接命中之 child 為 17）。
- 另試 5 種變體（前綴不要求數字／全欄關鍵字／僅關鍵字／標題＋描述／
  前綴＋全欄關鍵字），**無一還原 42／120**：

  | 變體 | parent | child(繼承) |
  |---|---:|---:|
  | 前綴需數字 ＋ desc 關鍵字（本包） | 35 | 108 |
  | 前綴不需數字 ＋ desc 關鍵字 | 35 | 108 |
  | 前綴 ＋ **全欄**關鍵字 | 35 | 108 |
  | 僅全欄關鍵字 | 22 | 68 |
  | 僅 desc 關鍵字 | 22 | 68 |
  | 前綴 ＋ 標題＋描述關鍵字 | 35 | 108 |

- **不阻擋任何步驟** —— 本項於下放包已標「登記用，非範圍」（R-PH2 定範圍為整份 037）。

---

## §4　recon 五項量測（§6-4，每項附查詢條件）

`recon.py --feature features/phone --root .`：`state=BLANK, leaves=754, sections=213, targets=754`。
產物 `features/phone/RECON.md`。**`DECISIONS.md` 未由 recon 產出** —— 見 §4-6。

### 4-1　037 之量測

已於 §3 逐項列出（911／213／698／754＋157／FROP／Source ID）。補三項：

- **leaf 判準**：`recon.py` 在force 之判準為 `Categorization == Functional Requirement` → **754 leaf**。
  另一判準（id 尾綴 `-NN` 之 child 形）得 698，**會漏掉 56 個 parent 形而本身為 Functional 之列**
  （`SWE1-HMI-001`、`-002`、`-004`…，全表在 `data/recon.json`）。本包採前者。
  **「911 列」（R-PH2 之母體）與「754 leaf」（出 TC 之列）不衝突**，以 DECISIONS §3 之
  Heading 台帳處置橋接。
- **Source ID 不連續**：`016`～`271` 之區間有 256 個號位而唯一值只有 213 個，**非連續**。
  （下放包只載「範圍」與「唯一 213」，未斷言連續；此處揭露以免後續誤推。）
- **每列 B 欄皆非空**（911/911）：child 與其 parent 共用同一 `Source Requirement ID`。

### 4-2　**037 parent → SYS1 對映率（本包之關鍵數字）**

> **⚠ 下放包之前提有一處與實測不符（A-PH05）。**
> 下放包載「037 只載文件名（`HMI Source ID` 欄 = 文件名），**無 NRL 號亦無章節號**」。
> **實測：`HMI Source ID`（C 欄）之值形如
> `Phone HMI Logic and Flow R1 SR24 Post 2A (June 21 2022)_10.14.1` —— 本身即帶章節號**，
> 213 個 parent 對應 213 個相異值，恰為 IN §10.7(b) 之 `{檔名}_{章節號}`。
> 三處獨立佐證：本包逐欄量測；`intake.py`「sources: document citations (need-list derivable)」；
> `recon.py`「citation column: C；distinct sections cited by the leaves: **213**」。
> **「無 NRL 號」一節屬實**（037 無 Polarion ID 欄）。
> 依 FO §8.2 揭露，**不自行調和、不改下放包**。

因此兩法並陳。**兩法皆對兩份 SYS1 各跑一次。**

#### 法一 —— 下放包所指定之 description 文字比對

查詢條件：037 parent 之 E 欄，去 `^The system shall ` 前綴、句首字母還原大寫、
NBSP/彎引號正規化、空白摺疊後，與 SYS1 `Basic Report` D 欄（`Description`，
去 `_x000D_`、空白摺疊）**精確比對**；未中者取 `difflib.SequenceMatcher` 最高相似度。

| SYS1 | 精確 | 累計（含模糊 ≥0.90） | 未對映 |
|---|---:|---|---:|
| `_intake` | **179／213 = 84.0%** | 212／213 = 99.5% | 1 |
| `cache` | **177／213 = 83.1%** | 209／213 = 98.1% | 4 |

> **§7 升級條件第 4 項（「對映率低於 90%，以精確比對計」）在此法下成立。**
> 惟其低係**方法之偽陰性**：SYS1 側之句首帶條號標籤（`MPA01)`、`PHCAT4.)`…）而
> 037 側已剝除，該法未剝 SYS1 側標籤，故 33／32 列落入「模糊」。
> **兩個升級條件（第 2、第 4 項）皆已具名上繳，不自行擇一解釋。**

#### 法二 —— 鍵接（037 `HMI Source ID` ↔ SYS1 `SYSRE_HMI_Source ID`）

查詢條件：037 parent 之 C 欄逐字，對 SYS1 `Basic Report` E 欄
（`SYSRE_HMI_Source ID`，273 個值，**兩份皆無重複**）作精確鍵接；
命中後再把**兩側**之條號標籤（`^[A-Za-z]{1,6}\d*(?:\.\d+)*\.?\)\s*`）剝除後比對 description。

| SYS1 | 鍵值命中 | 去標籤後精確 | 相似 ≥0.90 | **低相似（真錯指）** |
|---|---|---:|---:|---:|
| `_intake` | **211／213 = 99.1%** | 202 | 3 | **6** |
| `cache` | **212／213 = 99.5%** | 208 | 2 | **2** |

全表：`features/phone/data/parent_to_sys1_intake.tsv`／`…_cache.tsv`。

#### 未對映之 parent（逐列）

**`cache` 版 —— 1 列：**

| SWE ID | Source Req ID | 引用之章節 | 說明 |
|---|---|---|---|
| `SWE1-HMI-047` | `SYS-HMI-RA-PHONE-070` | `_5.16.2` | `PHCC8.)`。`cache` 無此需求；`_intake` 有而置於 `5.17` |

**`_intake` 版 —— 2 列：**

| SWE ID | Source Req ID | 引用之章節 | 說明 |
|---|---|---|---|
| `SWE1-HMI-047` | `SYS-HMI-RA-PHONE-070` | `_5.16.2` | 同上 |
| `SWE1-HMI-155` | `SYS-HMI-RA-PHONE-196` | `_17.9` | `_intake` 第 17 章只到 `17.8`（因 `NRL-128107` 併格遺失） |

**`_intake` 版之 6 列低相似（鍵接命中但內容錯指 —— 錯位之直接證據）：**

| SWE ID | 引用章節 | 指到 | 037 之內容 | SYS1 該格之內容 |
|---|---|---|---|---|
| `SWE1-HMI-048` | `5.17` | `NRL-535272` | `PHCC9.` 手機端起話 | `PHCC8.` 同號碼多聯絡人 |
| `SWE1-HMI-049` | `5.18` | `NRL-127983` | `PHDP1.` Keypad | `PHCC9.` |
| `SWE1-HMI-050` | `5.19` | `NRL-127984` | `PHDP2.` Keypad 頂欄 | `PHDP1.` |
| `SWE1-HMI-051` | `5.20` | `NRL-127985` | `PBT1.3` 藍牙串流 | `PHDP2.` |
| `SWE1-HMI-153` | `17.7` | `NRL-128109` | 行進中不顯示訊息 | 下載中提示 |
| `SWE1-HMI-154` | `17.8` | `NRL-128110` | 下載中提示 | 訊息關閉時之 New Message |

**`cache` 版之 2 列低相似**（`5.16.1`、`17.5`）**皆為 `cache` 側儲存格文字被截斷**所致
（037 與 `_intake` 之同格較長），**非錯指**。

**結論（供 §5 之裁決，非本層之裁定）**：以引用鍵計，`cache` 版與 037 一致度較高
（212/213，低相似 2 且皆為截斷）；`_intake` 版有 6 列**系統性錯指**。
**但 `cache` 版仍非 037 之來源**（缺 `5.16.2`）。

### 4-3　037 description 之機械改寫痕跡

- **下放包指定之字面 regex `^The system shall [a-z]`：實測命中 716 列**
  （全 911 列中；754 leaf 之 95%）。**該式不具鑑別力** ——
  合法句 `The system shall allow…` 亦以小寫起始。
- 改以「`The system shall ` 之後首 token」分類（扣除動詞白名單），
  **候選 704 列**，全表 `features/phone/data/mechanical_rewrite_candidates.tsv`
  （欄：`swe_id / source_req_id / hmi_source_id / class / first_token / description_head`）：

| 類 | 列數 | 判準 | 例 |
|---|---:|---|---|
| **L1 標籤殘留** | **80** | 首 token `^[a-z][A-Za-z]{0,5}[\d.]` | `pHCAT3.2)`、`mP04.1)`、`nPL1.1)`、`sMS12.1)`、`bTP04.2)`、`rL7.5.)` |
| **L2 首字母降格** | 12 | 首 token `^[a-z][A-Z]` | `dND`、`rOAMING`、`nAFTA`、`bUX`、`aPAC`、`sENDing` |
| L3 非動詞連接 | 574 | 其餘且非動詞 | `if`(137)、`the`(90)、`when`(40)、`for`(33)、`this`(22)、`during`(20) |
| L4 非字母開頭 | 38 | 首 token `^[^A-Za-z]` | `(image:`、`“Answer”`、`‘+’`、`555-262-4312,`、`.` |

- **L1＋L2（92 列）為最硬之證據** —— 原句首之條號／全大寫詞被機械降格。
- **登 `A-PH04`（候選，非裁定）**。verbatim 上半改取 SYS1 原句（IN §8.6）之處置待 Phase 2，
  且**其可行性繫於 §5 之裁決**（要取哪一份 SYS1 的原句）。

### 4-4　HFP 交付本盤點（R-PH3(a) 之參考素材，**不做對映**）

檔：`docs/Report/compare/03_HFP/…_CFTS026_HandsFreePhone_20260316(Refine).xlsx`
查詢：分頁 `Test Case Specification 測試用例規範`，列 10 起、F 欄（`Test Case ID`）非空。

- **TC 列數 136**；Test Group **一律 `HandsFreePhone`**；TC ID 前綴**一律 `newR1L`**
  （R-G42 生效前之本，不回歸）；`Specification Reference` **136/136 為 `CFTS026-*`**。
- **Test Set 13 組**：Call Log 47／Bluetooth 22／Device Manager 13／AddressBook 11／
  Contact 8／Voice recognition 7／Message 7／Service Control 6／**該欄空白 6**／
  Status Information 4／System State 3／Phone Call 1／
  `12-01需求重點不是只是看電話號碼格式` 1（**該格為中文備註誤入 Test Set 欄；
  屬 HFP 之事，依 R-PH3(c) 本 feature 不回頭改**）。
- 與本 feature 之 `{檔名}_{章節號}` 形制**無交集**，故 R-PH3(d)「不複製 spec_reference」
  在形制上自然成立。

### 4-5　26PI change log 與 MPA11／MPA12

| 項 | 預期 | 實測 |
|---|---|---|
| Change Log 列 | 1 | **1**（分頁 `26PI` 第 9 列；26PI／05/20/2026／Assumptions／`MPA11 and MPA12 created.`／Logic `X`／Under Review） |
| `MPA11` 於 037 | 0 | **0**（全欄 substring） |
| `MPA12` 於 037 | 0 | **0** |
| `MPA11`／`MPA12` 於 SYS1 | — | **0／0**（兩份皆查） |

**原文已抽**（見 §5-2 與 `features/phone/docs/26pi_delta.md`）。

### 4-6　`recon.py` 之斷言 FAIL —— 如實留著

```
- FAIL — SYS1 export available for outline lookup: expected True, measured False
  — paths.sys1_export is null — the 213 cited sections cannot be verified against any baseline
FAILED: 1 ruled-constant assertion(s) do not hold against these files.
DECISIONS.md was NOT written; see RECON.md for the measured values.
```

**`recon.py` 自己拒絕產出 `DECISIONS.md`** —— 與 §7 第 2 項為同一件事之機器側表達。
本包**未繞過**：`features/phone/DECISIONS.md` 為**人工補寫**，其首節具名此事，
凡繫於 SYS1 者一律標 `[BLOCKED: DRAFT-PH-a]` 而**不預填值**。
斷言轉綠後**須重跑 `recon.py` 並以其產出為準**。

---

## §5　**升級項** —— SYS1 兩份匯出互不相容（§6-5 之一，§7 第 2 項）

### 5-1　事實

兩份同檔名之 SYS1 匯出，**列數皆 273、頂層章皆 27**
——**故任何以列數／章數為準之檢查對此一律沉默。**
逐列實測（`features/phone/data/sys1_variant_diff.tsv`）：
**ID 不同 125 列、Outline Number 不同 124 列。**

根因為**兩處單列增刪互相抵銷**：

| # | `_intake`（2026-05-19） | `cache`（2026-03-16） |
|---|---|---|
| 1 | **多** `NRL-535272`（`PHCC8.)` 同號碼多聯絡人），落 Outline **`5.17`** | 無此需求 |
| 2 | **少** `NRL-128107` —— 其正文被**併入前一格 `NRL-128106`（`17.5`）之儲存格**（該格尾端可見 `…the select...NRL-128107 - TTS if TTS currently active)…`） | `NRL-128107` 為獨立列（`17.6`） |
| 結果 | `NRL-127983`～`NRL-128106` 之 Outline **整段 +1**；第 17 章只到 `17.8` | 第 17 章到 `17.9` |

**其後果在追溯，不在字數**：`spec_reference` 為 `{檔名}_{章節號}`（IN §10.7(b)），
在 `5.17`～`17.5` 區間之 **124 列**會指向不同需求，而**在簿面上與正確者長得完全一樣**。

### 5-2　**兩份皆非 037 之來源**

037 引用 `_5.16.2`（`PHCC8.`）—— `_intake` 置於 `5.17`、`cache` 根本沒有；
037 亦引用 `_17.9` —— 只有 `cache` 有。
→ **上游至少存在第三份匯出**，其 `PHCC8.` 為 `5.16` 之子項且第 17 章有 9 項。

### 5-3　本層之處置（**不擇一、不臆造、不繼續**）

- `sources/raw/` **只登錄其餘 4 件**，SYS1 **不登錄**；`sources/MANIFEST.tsv` 只增 4 列。
- `feature.yaml`：`paths.sys1_export: null`，`sources.sys1_export` 註明未登錄與其理由。
- `recon.py` 之斷言 FAIL 保留；`DECISIONS.md` 之 SYS1 相關項一律 `[BLOCKED]`。
- 登 `A-PH01`；DR 草稿 `DRAFT-PH-a`（**未取號**，依 `down/20260901_VS-SL-01_review.md` §2.2）。
- **Phase 2 以後未啟動。**

### 5-4　所求（擇一即可，見 `DATA_REQUESTS.md` §1）

(a) 裁 `cache` 為權威，`_5.16.2` 一列以 PENDING 出；
(b) 裁 `_intake` 為權威，接受 5.17–5.20 與 037 不一致；
(c) **向上游索取 037 V0.1 實際據以撰寫之那一份**（推薦；其 `PHCC8.` 應在 `5.16.2`、第 17 章應有 `17.9`）。

---

## §6　MPA11／MPA12 之落點與 PDF spec_mode（§6-5）

### 6-1　落點

`features/phone/docs/26pi_delta.md`（只抽這兩條，依下放包）。逐字：

```text
MPA11    Awaiting review    LOGICAL
Multiphone not applicable for R1L-R
```
```text
MPA12    Awaiting review    LOGICAL
(R1L‑R only) Refer to the Device Manager HMI L&F for all device management requirements.
```

出處：`Phone HMI Logic&Flow R1.pdf` **p.2**「Assumptions ／ Multiphone Assumptions:」章。
（MPA12 之 `R1L‑R` 用 U+2011 NON-BREAKING HYPHEN，MPA11 用 U+002D；逐字照錄，不統一。）

### 6-2　PDF spec_mode 判定 —— **B（有文字層），不 OCR**

| 檔 | pdfinfo | pdffonts | 文字層 | 判 |
|---|---|---|---|---|
| `Phone HMI Logic&Flow R1.pdf` | PDF24，21 頁，1920×1108 pt | **Type 3 ／ Custom 編碼**（uni yes） | `pdftotext` 得 140 664 字元；`intake.py` 判 21/21 頁有文字層 | **B**（附警示，見下） |
| `Phone HMI Logic and Flow R1 SR24 Post 2A (June 21 2022).pdf` | Visio 2019，42 頁 | Calibri／SymbolMT 等嵌入 TrueType | 165 802 字元；`intake.py` 判 **40/42** 頁有文字層 | **B** |

**警示（`A-PH02`）**：43.6 MB 之 PDF24 檔雖有文字層，`pdftotext -layout` **詞序錯亂** ——
`Unauthori z ed`、`Pro j ection`、`MPA 2 1`（實為 MPA12）、`follow N AFTA`。
改以 `pdfplumber` 依 word 之 `(top, x0)` 重排即正確還原（本包 §6-1 之逐字即此法所得）。
`extract_source.py` 內部亦用 pdfplumber，故 `sources/extracted/phone_hmi_lf_pdf_26pi/` **可用**；
**任何以 `pdftotext` 讀本檔之後續步驟一律不可信。**

### 6-3　一項須具名之事實（`A-PH07`）

`MPA11) Multiphone not applicable for R1L-R` 與客戶側資料夾名
「Bluetooth, Multiple Phones Paired Simultaneously」**方向相反**。
037 V0.1 基線為 SR24 Post 2A（2022-06-21），早於 26PI，故 037 內 Multiphone 條文
（parent 35 列、含 child 143 列）之**現行效力存疑**。
→ DR 草稿 `DRAFT-PH-b`。**本輪仍依 R-PH2 走完整份 037，不預先剔除任何列。**

---

## §7　`DECISIONS.md` 之 `[PROPOSED]` 清單（§6-6）

| § | 項 | 標記 |
|---|---|---|
| 1 | spec_mode | **`[BLOCKED: DRAFT-PH-a]`**（`intake.py` 提案 `A`） |
| 2 | BLANK fallback chain | `[PROPOSED: 以 HFP 交付本為**風格**樣板（R-PH3(a)）；只取寫法，不取 spec_reference 與 Test Set 切法（R-PH3(d)）]` |
| 3 | Heading 157 列之台帳處置 | `[PROPOSED: 不出 TC 列，於覆蓋台帳具名為 Heading（沿 R-POP5 形態）]` |
| 4 | Style authority | `[PROPOSED: BLANK fallback chain]` |
| 4 | Test Item shape | `[PROPOSED — 待樣板裁定後於 Phase 3 落定]` |
| 4 | spec_reference format | **`[BLOCKED: DRAFT-PH-a]`**（形制已定，缺的只有「章節號取自哪一份 SYS1」） |
| 5 | split_mode | `[PROPOSED: standard]` |
| 5 | scope carve-outs | `[PROPOSED: 無]` |
| **6** | **TC ID `{ABBR}`** | **`[PEI]`** —— 見下 |
| 6 | Test Set 表（Part N） | `[PROPOSED: 以 037 之 27 個頂層章為第一版切分]`；章名逐字 **`[BLOCKED: DRAFT-PH-a]`** |
| 6 | Profile [OVERRIDE] | `[PROPOSED — 待 Phase 3]` |
| 7 | Batch plan／model assignment | `[PROPOSED]` |

### `{ABBR}` 為 `[PEI]` 而非 `[PROPOSED]` 之理由（R-G42 二）

037 之 req_id 為 `SWE1-HMI-{nnn}`，其 token **`HMI` 是文件類別而非 feature 縮寫**
（全部 HMI L&F 系 feature 共用，不具鑑別力）。
依 R-G42 二「req_id 無縮寫或有歧義者，由 Pei 裁一次後登 feature.yaml，**不得自定**」，
本項為 `[PEI]`。`feature.yaml` `tc_id.abbr` 現為 `null`。候選（附依據，**不代裁**）：

| 候選 | 依據 | 反面 |
|---|---|---|
| **`Phone`** | `R-POP13`（2026-08-27）之實測：五本交付／產出簿 **4/5** 為 `NR1L-{FeatureName}-{NNN}`。本 repo 現況亦以全名居多（`Popup`／`Privacy`／`ComfortHMI`／`PROJ`…）。與 R-PH1 之 Test Group `Phone` 同字 | 較長 |
| `PH` | `new_feature.py` scaffold 預設（前二字）。全 repo 除本包二檔外無 `NR1L-PH-` 出現（`grep -rl` 實測） | **非裁定**，只是預設值 |
| `HMI` | R-G42 二之字面 | 不具鑑別力，跨 feature 必撞 |

---

## §8　未結 DR 清單（§6-7）

依 `down/20260901_VS-SL-01_review.md` §2.2「未送出前不佔號」，三件皆為**草稿標籤**，非 DR 號。

| 草稿 | 標的 | Status | 阻斷？ | Anomaly | Urgency |
|---|---|---|---|---|---|
| `DRAFT-PH-a` | SYS1 之權威版本（或第三份匯出） | OPEN | **是 —— Phase 2 起全阻** | A-PH01 | **P0** |
| `DRAFT-PH-b` | 037 之 26PI 增補（MPA11／MPA12）或其不適用之裁示 | OPEN | 否 | A-PH07 | P2 |
| `DRAFT-PH-c` | `ENTER_HOME_SCREEN` 之入口逐字 | OPEN | 否 | — | P1 |

### `ENTER_HOME_SCREEN` 之承接結果（FO §4 [ADD] 第二項）

**本 feature 確有標的，已承接、已開草稿，不以「Phone 走 Menu Bar」結案。**
判斷依據（037 全欄 `re.I`）：

| 查詢 | 命中列 |
|---|---:|
| `home\s*screen` | **7**（`SWE1-HMI-144`、`-144-09`、`SWE1-HMI-199`、`-199-03`、`-199-04`、`-199-05`、`SWE1-HMI-204`） |
| `\bwidget\b` | 46 |
| `menu\s*bar` | 621 |
| `main\s*category\s*bar` | 1（`SWE1-HMI-016`） |

第 25 章（Phone Widget，25.6.1／25.9）之多列以 **Home Screen 上之 widget** 為前提，
其 setup 無法只以 Menu Bar 之 `Phone` 入口表達。
標記格式依 R-G71(d)：`PENDING: DR-{n} HMI entry path Home Screen`
（落 `docs/runtime/profiles/FW036_R1L_Phone_Profile.md` §5.3、
`features/phone/DATA_REQUESTS.md` §2）。**不阻斷撰寫。**

### R-G67（嵌入物件）之回報 —— 「已查、無此目錄」

- 掃描對象（**live `ls`，2026-09-07**）：
  `~/Work/02_Project_R1LR/1_Customer_Requirement/R1LR SR26 ATL-H/26PI2.5/Reference Documents/CFTS Embedded Objects/`
- 實測 **9 個目錄**：`CFTS001`(2)、`CFTS013`(13)、`CFTS019`(31)、`CFTS024`(16)、
  `CFTS025`(46)、`CFTS028`(12)、`CFTS036`(2)、`CFTS057`(6)、`CFTS069`(23)。
- **本 feature 之母體為 HMI Logic & Flow，非 CFTS**，故本條在型別上不適用；
  且即便以互參之 HFP 之母 `CFTS026` 查，**該目錄亦不存在**（9 目錄中無 `CFTS026`）。
- → **不適用之依據為「已查、無此目錄」，非「未提及」。** 本 feature 無 `ANCHOR_POOL.md`
  之附記需求（無嵌入物件可記）。

---

## §9　四支 gate 之實跑（FO §8.2 末列）

```
python scripts/gate_all.py --root .
```

| 閘 | exit | 結果 |
|---|---:|---|
| `lint_docs036`（docs_structure） | 0 | PASS |
| `canon_refs` | 1 | **FAIL**：unresolved 18 ＋ ambiguous 141 = **159** |
| `rulings_hash` | 0 | PASS（`RULINGS.sha.tsv` 與現行條文相符，**771 條**） |
| `gates_tsv` | 0 | PASS（`GATES.tsv` 相符，57 閘） |
| `lint_paths` | 0 | PASS（基線外違規 ＋ delivered 不符 = 0） |
| `lint_delivery_spec` | 1 | **FAIL**：基線外判紅 11（掃 8 檔，基線 4 列） |

`gate_all.py` 自身 **exit 1**。

**總判 FAIL —— 升級說明如下（26 包 §C 裁定 2）：**

1. **`canon_refs` 之 159 = 18 unresolved ＋ 141 ambiguous，與 `up/20260905_GC-15.md`
   §所載之基線（18／141）逐數相同**，即**本包貢獻 0**。
   以 `--report --top 400` 逐筆列出後 `grep -i phone` 命中 **0** ——
   本包新增之文件（`features/phone/*.md`、`FW036_R1L_Phone_Profile.md`）之 canon 引用
   **全部為 qualified（`FO §…`／`IN §…`）**，未增任何裸節號。
2. **`lint_delivery_spec` 之 11 列判紅全在 `features/power`／`features/sw_update`／
   `features/vsm_v42` 之 `delivered/`**（Test Group 縮寫、檔名尾綴、缺 DELIVERY_NOTE、
   PENDING 格數等）。本 feature **無 `delivered/` 目錄、無任何交付檔**，
   該閘之掃描面（8 檔）**不含 phone**。本包貢獻 0。
3. 故兩支 FAIL **皆為既有基線，非本包所引入**。本包不修既有 feature 之交付物
   （下放包 §0 禁區：不動 `features/` 下其他 feature 目錄）。

---

## §10　自報錯誤與併行寫檔觀察（§6-8）

### 10-1　自報錯誤

1. **首次量 037 機械改寫痕跡時直接照抄下放包之 regex `^The system shall [a-z]`，
   得 716 列而未察覺其不具鑑別力** —— 該式命中 754 leaf 之 95%，本質上是量了
   「有沒有以 The system shall 起頭」。發現後改以首 token 分類重量（§4-3），
   **兩個數字都報，不以後者取代前者**。
2. **首次量 SYS1 對映時未剝 SYS1 側之條號標籤**，致精確比對率 84.0%／83.1%，
   險些把方法之偽陰性報成上游品質問題。剝除後（法二）為 99.1%／99.5%。
   **兩法並陳，且 §7 第 4 項之升級照樣具名**（它在法一下確實成立）。
3. **一度打算把 `_intake` 版 SYS1 逕行登錄 `sources/raw/`**（理由：其 sha 與下放包 §5
   相符，看似「就是這份」）。該理由不成立 —— §5 所載之 sha 只證明 Pei 投的是這份，
   不證明它是 037 之來源。已改為**不登錄**。

### 10-2　併行寫檔觀察（R-G62(b) 附記形態）

- 本 session 開始時，工作樹已有兩件他線未追蹤檔：
  `docs/fw036/handoff/down/20260905_GC-15_review.md`、
  `features/vsm_v43/docs/handoff/15_dryrun.md`。**本包未動之。**
- 本包唯二觸及**全域檔**者：
  - `docs/fw036/RULINGS.sha.tsv` —— `rulings_hash.py` 重生，實測**只增 3 列**（R-PH1/2/3），
    既有 768 列**一字未動**（`git diff -U0` 過濾後非 R-PH 之異動列為 0）。
  - `sources/MANIFEST.tsv` —— **手工附加 4 列**（未用 `--refresh-manifest`，依該檔既有 note）。
- `_intake/Phone/` 內**原檔 5 件未改名、未刪、未移**；`intake.py` 於該目錄新增
  `INTAKE.md`（其 Tier 0 常規產出，且 `_intake/*` 為 gitignore 範圍）。
- **git 全程未動**（無 add／commit／tag／stash／worktree）。

### 10-3　獨立判斷 —— 「本包是否仍有該驗而未驗者」

| 項 | 狀態 |
|---|---|
| 037 之 27 個頂層章**章名逐字** | **未取** —— 須自 SYS1 取，繫於 `DRAFT-PH-a` |
| 037 圖面（`(image: …)` 之 38 列，L4 類） | **未展開** —— 兩份 PDF 之圖面對應屬 Phase 3，本包未做 |
| `Phone HMI Logic and Flow R1 SR24 Post 2A (June 21 2022).pdf` **2 頁無文字層** | 已知（40/42），**未逐頁具名是哪兩頁** |
| 037 之 `Verification Criteria`／`Verification Method` 兩欄 | **未量** —— 下放包未要求，Phase 3 之素材 |
| `Pop Up List` 是否為本 feature 之素材 | **未查** —— 037 之引用 stem 只有一個（HMI L&F），未見 PU 號；`feature.yaml` `popup_list: null`，如 Phase 3 需用再開 DR |
| 037 之 `Release Version` 欄分佈 | **未量** |
| 本 feature 之 `framework.md` Part N | **未動**（Phase 3） |

---

## §11　落點清單

```
features/phone/                     ← 新（scaffold ＋ 本包產出）
  .gitignore ANOMALIES.md DATA_REQUESTS.md DECISIONS.md PLAYBOOK.md
  RECON.md RULINGS.md RUNBOOK.md feature.yaml
  data/{recon.json, recon_leaf_to_section.tsv, mechanical_rewrite_candidates.tsv,
        parent_to_sys1_intake.tsv, parent_to_sys1_cache.tsv, sys1_variant_diff.tsv}
  docs/26pi_delta.md
  sandbox/base/FM-WI-FSM-036-A01…_SWQT_20260817_ext.xlsx
  inputs/ batches/ generated/ scripts/          ← 空（inputs 恆空，R-G66）
docs/runtime/profiles/FW036_R1L_Phone_Profile.md   ← 新（骨架）
sources/raw/phone_{037_v0_1,hmi_lf_pdf_2022,hmi_lf_pdf_26pi,changelog_26pi}/  ← 新（gitignore）
sources/extracted/phone_{同上四者}/                 ← 新（入版控）
sources/MANIFEST.tsv                ← ＋4 列
docs/fw036/RULINGS.sha.tsv          ← ＋3 列（R-PH1/2/3），無其他異動
_intake/Phone/INTAKE.md             ← intake.py 產出（gitignore 範圍）
```

**git 未動；commit 由 Pei 執行。**
