# 上繳包 GC-05 —— 加錨、改號收尾、DR 登記

日期：2026-09-05　層級：Tier 1 → Tier 2
對應下放包：`down/20260905_GC-05.md`（協定：`docs/fw036/handoff/PROTOCOL.md`）

八件任務全數執行。**`docs/fw036/RULINGS.sha.tsv` 一位元未動**（R-G50(c)）。無刪除、無搬移。
乾跑結果：**R-G30～R-G41、R-G33(d)、R-G42、R-G43～R-G50 系列、R-G51～R-G69 全數在表，缺號 0**。

> **三件請先看**：
> 1. **judged.tsv 內部不一致，本層擋下 10 處改號**（4-2 節）。`features/bed_lowering/ANOMALIES.md`
>    之 `A-BLM8` 內文即「**R-G12 ~ R-G20 之號在兩份權威間全段撞號**」之逐條證據；judged 把該行 10 處
>    判 `FO`（confidence **M**），而緊鄰之 `A-BLM9`／`A-BLM10` 判 `KEEP_ANNOTATE`，理由正是「改號即毀證」。
>    改號會使該紀錄變成假敘述（R-G51~R-G59 並不撞號 —— 那正是 R-G43 的成果）。
>    本層一律降為 `KEEP_ANNOTATE` 並加註，**請分析層重判**。
> 2. **下放包 §一-1 之 14 條清單漏了 `R-G69`**（3-2 節）。首次乾跑逐號核對時它缺列；
>    §一-7 又要求「`R-G51`～`R-G69` 逐號在表」。二者相衝，本層依後者補加第 15 個錨點，具名請覆核。
> 3. **本層自報一件既有缺陷**（7-2 節）：GC-03 把 `R-G45 補充` 抽進台帳時**切掉了本體首行**，
>    台帳所載自「（`BHCAN`／`FDCAN8`）為配對鍵」起 —— 讀者無從知道配對鍵是誰的。本包補回全段。

---

## §1　產物與 SHA256

| # | 檔 | bytes | SHA256 |
|---|---|---:|---|
| 1,2,3 | `docs/fw036/RULINGS_LEDGER.md` | 92,223 | `949da29052ecfa0807870eb8c9168f30079c665e95f9f7f9ca8d6c34b501f872` |
| 3 | `docs/fw036/ANOMALIES.md` | 4,139 | `bde9f9e4cdd86982823d9bb55ef2d3af17a80066d42f2fc94201022c9de3e719` |
| 3 | `features/vehicle_setting/RULINGS.md` | 385,224 | `68d06ea952c7dfa2572656c8953efdc44857243053c1aacb5c1aa522083a2032` |
| 5 | `features/vehicle_setting/DATA_REQUESTS.md` | 90,992 | `7d1a8444fcb1967c00e09a82256d039aaa83bef5f8415c363951d48b7e7f5708` |
| 2,5 | `features/vehicle_setting/DECISIONS.md` | 6,706 | `9e157d9a8558a07587b143125ccfe3a019f830d0022b7a0149979219d7162ac6` |
| 6 | `features/comfort/DECISIONS.md` | 13,977 | `119c608f51dceb273699e504c83e0da1d23eac6899e17d264a224abdfb97c1ab` |
| 6 | `features/time_management/DECISIONS.md` | 7,483 | `5f9dc93d8b18002f77e0e64c452895b4f64e50f1acff91b9477fc56f9151716b` |
| 6 | `docs/reports/binding_hits2_20260905.tsv`（新） | 3,075 | `ed726a854c7064f8169c0f51e68f1cd7a9aad88a1536236bcea991b125d27111` |

**輸入之 judged.tsv 實測**：`docs/reports/rg43_manual_20260905_judged.tsv`
17,352 bytes、130 列、sha256 `81f7ac57333376d2cfa513a84e654a0a2c73311324237f5175d45ff9867897cc`。
其分布實測 **FO 80／LEDGER 28／KEEP 10／KEEP_ANNOTATE 12**（與下放包所載相符）；
confidence **H 104／M 19／L 7**（下放包載 H 105／M 19／L 6，**差一**，以本表實測為準）。

本輪共動 **43 檔，+256 / −74**。

### 1-1　R-G50(c)：本輪工具寫入 repo 內之路徑清單

| 工具 | 旗標 | 寫入 repo 內之路徑 |
|---|---|---|
| `scripts/rulings_hash.py` ×3 次 | `--out <工作區>/rg_dry{,2,3}_20260905.tsv` | **無** |
| `scripts/canon_refs.py` | `--waiver` | **無** |

**清單為空**；`docs/fw036/RULINGS.sha.tsv` 之 `git status` 逐次複核為空。

---

## §2　任務 1、2 —— 加錨與 `R-G45(b)`

**加錨 15 條**（下放包列 14，＋`R-G69`）：`R-G30`～`R-G41`、`R-G33(d)`、台帳側 `R-G42`、**`R-G69`**。
形式為圍籬前一行插 `### R-Gnn — <圍籬首行之標題>`，其下三行註說明「本錨點為指紋而設、
原 `##` 敘述式標題與下框本體一字未動」。**原 `##` 標題與圍籬本體逐字未改。**

`R-G45 補充` → **`R-G45(b)`**：標題改號並加改號註；
`features/vehicle_setting/DECISIONS.md` 內之引用同步改為 `R-G45(b)`。
乾跑實測 `R-G45` **1 列**，撞號歸 0。

---

## §3　任務 7 —— 全量乾跑（`--out <工作區>`）

`rulings_hash.py` 三次乾跑，最終：**763 錨點**（group 10／report 12／ruling 732／superseded 9），來源 20 檔。
現行 `RULINGS.sha.tsv` 728 列／697 個相異 ID → 乾跑 **763 列／731 個相異 ID**。

### 3-1　逐號核對（§一-7）

| 組 | 結果 |
|---|---|
| `R-G30`～`R-G41` | **12/12 在表，各 1 列** |
| `R-G33(d)` | 在表，1 列 |
| `R-G42` | 在表，**2 列**（`FEATURE_ONBOARDING.md:1231` 摘要 ＋ `RULINGS_LEDGER.md:1293` 全文）—— 與 GC-04 審閱 §一-1 之預期一致 |
| `R-G43`／`R-G43(b)`／`R-G43(c)`／`R-G44`～`R-G50(c)` | **14/14 在表** |
| `R-G51`～`R-G69` | **19/19 在表**（補 `R-G69` 之錨後）|
| `R-G62′`／`R-G61` | 皆在表 |
| **缺號** | **0** |

### 3-2　`R-G69` —— 下放包清單之缺漏，具名

首次乾跑（加錨 14 條後）逐號核對即發現 **`R-G51`～`R-G69` 缺 `R-G69`**。
成因與那 14 條完全相同：其標題為
`## 理由與數字、報告與量測（R-G69／R-G20，2026-08-25，全域）`，條號在括號內。

下放包 §一-1 之清單未含它；§一-7 卻要求「`R-G51`～`R-G69` 逐號在表」。
本層依 §一-7 補加第 15 個錨點，並於該錨點註內具名此衝突。**請覆核。**

### 3-3　撞號（未合併、未擇一）

撞號 **28 組**（本體皆不同），其中 `R-G` 者 **3 組**：

| ID | 落點 | 判 |
|---|---|---|
| `R-G42` | `FEATURE_ONBOARDING.md:1231`（摘要）／`RULINGS_LEDGER.md:1293`（全文）| **本輪新增，GC-04 審閱 §一-1 已預先容認**（同 `R-G4`／`R-G7` 之形態；FO 側已自讓權威）|
| `R-G4` | FO:773（條文）／FO:1251（group）／`user_profiles/RULINGS.md:474` | 既有 |
| `R-G7` | FO:795／`user_profiles/RULINGS.md:623` | 既有 |

其餘 25 組為各 feature 內部之同號異文，與本包無涉。**`R-G45` 撞號已歸 0。**

---

## §4　任務 4 —— judged.tsv 之 130 處

### 4-1　處置統計（母體 130，R-G8）

| 處置 | 數 |
|---|---:|
| `RENUM_FO`（FO 側改號）| **63** |
| `RENUM_LEDGER`（`R-G69` 之 2 處）| **2** |
| `ALREADY_GC03`（`old_id` 不在、`new_id` 在該檔 —— GC-03 已改畢）| **7** |
| `ANNOTATED`（`KEEP_ANNOTATE`，含本層擋下之 10 處）| **22** |
| `KEEP`（不動）| 10 |
| `LEDGER_KEEP`（LEDGER 側 `new_id == old_id`，不動）| 26 |
| **合計** | **130** |
| **定位不到（未改）** | **0** |

**定位法（不信 judged.tsv 之行號）**：judged.tsv 之行號早於本層 GC-04 之編輯，已漂移。
本層改以 `rg43_manual_20260905.tsv` 之 `context` 欄逐處重新定位
（CTX **61** 處），context 比不中者退回原行號 ±40 行內就近搜尋（NEAR **4** 處）。
兩者皆不中而 `new_id` 已在該檔者判 `ALREADY_GC03`。
**首版曾直接照行號套用，產生 2 處 DRIFT 與 13 處誤判 ALREADY —— 已全數還原重做**（7-3 節）。

`KEEP_ANNOTATE` 之註解：**同一行合併為一則**（列出所有 row 號）；
落在 markdown 表格列者**內嵌於行末**而非另起新行，以免打斷表格。
形式：`<!-- R-G43：兩側皆可讀／撞號紀錄，未改號；見 …judged.tsv row N -->`（`.md`）／
`# R-G43: not renumbered, see …judged.tsv row N`（`.py`）。

### 4-2　本層擋下之 10 處（**請分析層重判**）

`features/bed_lowering/ANOMALIES.md` 之 `A-BLM8`，其標題逐字為
**「R-G12 ~ R-G20 之號在兩份權威間全段撞號」**，內文逐條列 FO 與台帳對同號給出之不同條文。
judged.tsv 對該行之 **10 處全判 `FO`，confidence 皆 `M`**（row 7–16）；
而同檔次一行之 `A-BLM9`／`A-BLM10`（row 17–19）判 `KEEP_ANNOTATE`，
理由逐字為「**A-BLM8／A-BLM9 為撞號之紀錄本身（並明寫「ledger 版」），改號即毀證**」。

即 judged.tsv **對同一份證據給了兩種相反處置**。照 `FO` 改號後，該紀錄會宣稱
「R-G51 ~ R-G59 在兩份權威間全段撞號」—— 而那正好是**假的**，R-G43 改號的成果就是讓它們不再撞號。

**本層處置**：10 處一律降為 `KEEP_ANNOTATE`，加註指向 judged.tsv row 7–17，**不改號**。
理由：毀證不可逆，而保留可逆。**請分析層重判該 10 處。**

### 4-3　`R-G61′` 之降級（judged row 112 之前提檢核）

下放包要求「先驗 FO 內有無 `R-G22′` 錨點，無則只改本處為 `R-G61` 並註」。
實測 FO 內 `R-G22′` 與 `R-G61′` **命中皆 0**，故 row 112 之 `new_id` 由 `R-G61′` 降為 `R-G61`。

**另具名一處超出 judged.tsv 之修正**：`scripts/rulings_hash.py:65` 之註解在 GC-03 機器改號時
被帶成 `R-G61′`（因 `R-G22′` 之 `R-G22` 段命中），judged.tsv 未列該處。
留著會與 row 112 改後之 `R-G61` 不一致，故一併降為 `R-G61` 並加二行說明。

### 4-4　confidence `M`／`L` 之改號（供各 feature 線日後覆核）

實改 **8 處**（其餘 M 之處落在 4-2 節被擋下，或屬 `ALREADY_GC03`）：

| conf | 處 | 改 |
|---|---|---|
| M | `features/ics_management/ANOMALIES.md:26`、`:76` | `R-G23` → `R-G62` |
| M | `features/ics_management/RULINGS.md:1279`、`:1432` | `R-G23` → `R-G62` |
| M | `features/ics_management/RULINGS.md:427`、`:857` | `R-G13` → `R-G52` |
| M | `features/vehicle_setting/RULINGS.md:5307` | `R-G13` → `R-G52` |
| M | `scripts/rulings_hash.py:123` | `R-G22` → `R-G61`（4-3 節）|

`L`（7 處）**無一落在實改集合內** —— 皆屬 `KEEP`／`LEDGER_KEEP`／`KEEP_ANNOTATE`。

### 4-5　對照向（R-G7＋R-G50(b)）

| 向 | pytest | `ruff`（本輪動到之 15 支 `.py`）| 移出檔數 |
|---|---|---|---:|
| 交付向 | `10 failed, 1306 passed, 15 skipped` | 逐檔 5／1／3／1／7／1／1／2／8／3／2／1／3／2／1 | — |
| **對照向** | `10 failed, 1306 passed, 15 skipped` | **逐檔同數** | **44**（43 已追蹤 ＋ 1 新檔）|

紅 **+0**、綠 **+0**（本輪只改字串與註解，未增測試）；`ruff` **+0**。

---

## §5　任務 3、5 —— 條文、DR

**3　條文入台帳**　`R-G43(c)`（GC-04 審閱 §一-3 圍籬）入台帳新節；
`A-GC13`（判準未量其覆蓋）入 `docs/fw036/ANOMALIES.md`，共同形態句改「十三筆」並加第四種形態。
DBC 裁決入 `features/vehicle_setting/RULINGS.md` 之 **`R-VS101`**
（取號依 R-G62：本檔實測最大號 `R-VS100`）。條內附四象限依據、
並具名「`R-13(g)` 之定義落在 audio_mgmt 線，其跨線適用性出自分析層之裁決，非執行層之認定」。

**5　DR 登記**　`features/vehicle_setting/DATA_REQUESTS.md` 末節，**未取號** ——
依 `down/20260901_VS-SL-01_review.md` §2.2「未送出前不佔號」，下放包所寫之 `DR-VS{n}` 之 `{n}` 保持未填。
受影響 TC 列**已定位**：

| 分頁 | 列 | D 欄 req_id | F 欄 tc_id |
|---|---:|---|---|
| `Test Case Specification 測試用例規範` | 214 | `SWE1-VC-PowerTailgate-027` | `NR1L-VS-205` |
| 同上 | 215 | `SWE1-VC-PowerTailgate-028` | `NR1L-VS-206` |

`PENDING: DR-{n}` 之寫回**不在本包**。`inputs/` 之 `R4_BHCAN`／`R5_FDCAN8` 已於 DECISIONS 註
「非現行（R-VS101），保留供追溯」。

---

## §6　任務 6 —— #3／#6 補 grep，兩列均結案

**#3 comfort（彈窗文字）**　變動之 2 id 為 `PU1578`／`PU1579`。
取 forms Pop Up List `Main` 分頁該二列之 `Description` 與 `String/Popup Message` 共 **4 欄**（皆非空），
對 comfort 最新工作簿（sha8 `235f2ca7`）全分頁全列子字串比對 —— **命中 0/4**。
依 GC-04 審閱 §三「仍 0 則結案」→ **判不影響，結案**。

**#6 time_management（LID）**　該簿引用之 LID signal **1/2548 = `DateTmFormat`**；
變動之 4 個為 `CallAction`／`EngineRPM`／`Profile_Delete`／`Profile_Request`；
**交集為空集** → 依「不在即結案」→ **判不影響，結案**。

兩者之查詢式與逐欄結果落 `docs/reports/binding_hits2_20260905.tsv`，
判定已抄回 `features/comfort/DECISIONS.md` 與 `features/time_management/DECISIONS.md`
（原「待證」依 R-TM13 以刪除線保留）。

---

## §7　自檢與自報

| # | 項 | 結果 |
|---|---|---|
| 1 | `RULINGS.sha.tsv` 未寫 | **PASS**（1-1 節，清單為空）|
| 2 | 撞號未合併、未擇一 | **PASS**（3-3 節）|
| 3 | 原 `##` 標題與圍籬本體未改 | **PASS** —— 加錨只插新行 |
| 4 | 無刪除、無搬移 | **PASS** —— 無 `D`／`R` 列 |
| 5 | pytest／`ruff` 增紅 | **+0／+0**（4-5 節）|
| 6 | 全稱斷言附查詢式與命中數 | **PASS**（3-1／4-1／6 節）|
| 7 | 對照向回報被移出檔數（R-G50(b)）| **PASS** —— 44 |
| 8 | 量測產出未入母體（R-G16 補充）| **PASS** |

### 7-1　`gate_all`：`canon_refs` 759 → 778（+19）

| 向 | `canon_refs` |
|---|---:|
| 對照向（移出 44 檔）| **759** |
| 交付狀態（不含本上繳包）| **778** |
| 交付狀態（含本上繳包）| **786** |

逐型（對照向 → 交付狀態）：`section` 63／748 → 63／748（**+0／+0**）；`item` +0；
`ruling` unresolved 884 → 914。waiver 外 unresolved 619 → 638、ambiguous 140 → 140。
全為 `ruling` unresolved：本包大量指名 `R-G43(c)`／`R-G45(b)`／`R-G51`～`R-G69` 等尚無指紋之號。
**乾跑已證這些號屆時全部入表（3-1 節缺號 0）**，Pei 重生後即消。

### 7-2　自報：GC-03 之抽取缺陷（既有，本包補正）

GC-03 §二-1 之抽取器 `para()` 以「標題之次行」為本體起點。
`R-G45 補充` 之標題**即其本體首行**（`**1　DBC 一類無一對一版本**（4 節末）—— 裁：…以**網段 token**`），
於是首行被切掉 —— 台帳所載自「（`BHCAN`／`FDCAN8`）為配對鍵」起，**讀者無從知道配對鍵是誰的**。
本包以來源逐字補回全段，並於該條加補正註。
**形態**：抽取器對「標題即內容」之形態未設判斷，而 GC-03 上繳未逐條回讀所抽之本體。

### 7-3　自報：本輪首版之三個缺陷（皆已還原重做，錯值未進交付物）

1. **照 judged.tsv 之行號直接套用** —— 該行號早於本層 GC-04 之編輯，已漂移；
   產生 2 處 DRIFT、13 處誤判 `ALREADY`。改以 context 定位後 0 處定位不到。
2. **`KEEP_ANNOTATE` 之註解逐 row 各插一行** —— 同一行插了 10 則，且插在 markdown 表格列之間，
   **把表格打斷**。改為同行合併一則、表格列內嵌行末。
3. **`A-BLM8` 撞號紀錄被機器改號** —— 見 4-2 節；已擋下並改為加註。

三者皆在交付前發現並全數 `git checkout` 還原重做（任務 1–3 以腳本重放，可重現）。

### 7-4　未做且具名

| 項 | 為何未做 |
|---|---|
| `RULINGS.sha.tsv` 重生 | §二-1，屬 Pei；乾跑已證缺號 0，可一次跑 |
| `PENDING: DR-{n}` 寫回工作簿 | §一-5 明文「本包只登記與列號」|
| DR 取號 | 未送出不佔號（`down/20260901_VS-SL-01_review.md` §2.2）|
| `A-BLM8` 之 10 處改號 | 4-2 節，請分析層重判 |
| `_intake/` 清空 | GC-03 審閱 §六，Pei 動作 |

### 7-5　請裁三項

1. **`A-BLM8` 之 10 處**（4-2 節）：judged.tsv 對同一份撞號證據給了兩種相反處置，請重判。
2. **`R-G69` 之補加錨**（3-2 節）：下放包 §一-1 清單漏列，本層依 §一-7 補加，請覆核。
3. **`scripts/rulings_hash.py:65` 之 `R-G61′`→`R-G61`**（4-3 節）：judged.tsv 未列該處，本層一併降級。
