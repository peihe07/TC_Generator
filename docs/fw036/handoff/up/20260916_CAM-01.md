# 上繳包 CAM-01 —— Camera feature intake（scaffold ＋ 來源登錄 ＋ recon ＋ framework 驗算）

對應下放包：`docs/fw036/handoff/down/20260916_CAM-01.md`
執行層 session：2026-09-16，branch `feat/m1-stage7-scorecard`（**git 未動**，R-G62(b)／§0）。
本包止於 Phase 0–1。**未生成任何 TC**。

## §0　禁區遵守

| 禁區 | 遵守情形 |
|---|---|
| git 一律不動 | 未執行 `git add`／`commit`／`push`；只寫工作區檔案 |
| 不動 `down/`、`up/` 既有檔 | 未改動；本包為 `up/` 新檔 |
| 不動 `features/` 下其他 feature | 未改動 |
| 不得生成任何 TC | 未生成 |
| 不自行對兩本 037 做範圍篩選 | 兩本整份入 recon（25 列／230 列全量） |
| `forms/proxi/` 六平台十檔不得改名移動 | 未動；只讀取 sha256 與登 MANIFEST |
| 來源原檔不入 repo，依 R-G66 落 `sources/raw/` | 已落；`sources/raw/` 本即不入 git（`sources/README.md`）|

---

## §1　任務 0 —— 投遞與登錄

### 1.1 投遞（`_intake/Camera/`，16 檔 2.2 MB）

下放包 §4(b) 列 12 列、實檔 16 件（VF551 docx 一列含五件）。
下放包標「執行層取」之六件（CFTS092 docx ＋ VF551 docx 五件）由執行層自
`10_Reviewing/00_TestCase/ASW-R3/Camera/REF/` 取入。
**全部 16 件之 sha 與下放包 §4(b) 所載相符**（下放包未載 sha 者以實測補）。

| doc_id | sha16 | 與 §4(b) 對帳 |
|---|---|---|
| `camera_037_swra_v02` | `cae0df17016b3532` | ✅ 相符 |
| `camera_037_rvc_hmi_v0_1` | `6d5812a03da1d148` | ✅ 相符 |
| `sys1_hmi_rvc_pam_r1_low_sr24_1a_ref` | `5a1c0ab24991dcb1` | ✅ 相符 |
| `sys2_cfts092_sysra_v01` | `d3b3393dde8c297c` | ✅ 相符 |
| `sys2_vf551_v2_sysra_v01` | `e576647a603944a3` | ✅ 相符 |
| `sys2_vf551_v3_sysra_v01` | `ac0d41b17ddb9f17` | ✅ 相符 |
| `sys2_vf551_v33_sysra_v01` | `d25fd1a64d77de9f` | ✅ 相符 |
| `sys2_vf551_v4_sysra_v01` | `b8baac50f1ff480e` | ✅ 相符 |
| `sys2_vf551_v42_sysra_v01` | `0eaed0b8e6e1272c` | ✅ 相符 |
| `camera_event_hal_status` | `4a1c3ef6b8168af0` | ✅ 相符 |
| `cfts092_radio_camera_management_20260310` | `094f619351e616f5` | 下放包未載 sha（標「執行層取」）|
| `vf551_v2_spec_r1` | `38e4a09602dc3af7` | 同上 |
| `vf551_v3_spec_r6` | `ff6b57ad7f427af5` | 同上 |
| `vf551_v33_spec_r2` | `934a56eb5687302c` | 同上 |
| `vf551_v4_spec_r2` | `945222c2382a4317` | 同上 |
| `vf551_v42_spec_r4` | `42aaae78908af2ad` | 同上 |

sha256 全長見 `sources/MANIFEST.tsv`（`features` 欄 = `camera`）。

### 1.2 任務 0(a) 五項 —— 不投遞，只登 MANIFEST 指向既有落點

| doc_id | 既有落點 | sha16 | 依據 |
|---|---|---|---|
| `sys1_hmi_headunitcamerasystems_v7` | `spec-index/cache/` | `0df5da9186d761e3` | R-G44′ —— **與 REF 本全等，§9 之 sha 條件成立** |
| `sys1_hmi_rvc_pam_r1_low_sr24_1a_cache` | `spec-index/cache/` | `1a0bef53c6de975c` | 同名異體，見 §1.3 |
| `headunitcamerasystems_hmi_lf_pdf` | `spec-index/sources/` | `69884c963c08cac5` | R-G44′ |
| `rvc_pam_hmi_lf_pdf` | `spec-index/sources/` | `d16ac02e61c20a63` | R-G44′ |
| `proxi_hdcc28_atl_hi_xlsx`／`_txt`、`proxi_hdcc27_initial_xlsx`／`_txt`、`proxi_dt28_atl_hi_xlsx`／`_txt`、`proxi_promaster_atl_mi`、`proxi_toro_atl_mi`、`proxi_fastback_atl_mi` | `forms/proxi/` | 見 MANIFEST | R-G45 現行版；**不改名不移動** |

既有 MANIFEST 列補掛 `camera`（不新建 doc_id，R-G45／R-G66 不存副本）：
`p363_bh_can_07338_3a_r2`、`pdt27_e2a_r1_bhcan2`、`pdt27_e2a_r1_fdcan8`、
`project_637mca_bh_can_r1_29_01_2025_pluscr19670`（DBC 四本）、
`proxi_hdcc27_r3_20250424`、
`hmi_settings_list_r1_sr25_post_r1l_r_feb_13_2026_v8d04e51a`（sha16 `8d04e51a56d6391d`，
四個同名 doc_id 中以 sha 唯一命中 `forms/` 現行檔者）。

`VF551_V9` 全套未入（A 本未引用），`SYS3_CAMERA_…SYSAD_V02.docx`（6.9 MB）
未投（選投項，Pei 未投）—— 兩者皆未登 doc_id。
MANIFEST 由 166 列增至 194 列。

### 1.3　**RVC+PAM 同名異體之 diff（下放包指定回報項）**

| | REF 本 `5a1c0ab24991dcb1` | `spec-index/cache/` 本 `1a0bef53c6de975c` |
|---|---|---|
| `Basic Report` 資料列（A 欄非空）| **55** | **64** |
| 頂層章（Outline 不含 `.`）| 12 | 12 |
| Outline 集合 | cache 本之真子集 | REF ∪ {`7.2.1`…`7.2.6`, `7.3.1`, `7.3.2`, `7.3.3`} |
| Polarion ID（共有列）| 全數相同（差異 0 列）| 同 |
| Description 差異 | 13 列 | 其中 **2 列為實質差異**（`7.2`／`7.3`：REF 本把條列子項併入本文；cache 本本文只留首句，子項另立列），**11 列僅為換行編碼**（`_x000D_\n` vs `\r\n`）|

**結論**：cache 本為 REF 本之嚴格超集，差別在於 cache 本把 `RVCPAM1)`／`RVCPAM2)`
之條列展開為可定址之 work item。B 本 037 之 `SWE1-RVC-016`／`-017`／`-018`
逐字引用 `…_(June_25_2021)_7.3.1`／`_7.3.2`／`_7.3.3`，**REF 本無此三章**。

→ 下放包 §4(b)「未裁前 B 本追溯以 REF 本為準（037 所引者）」之前提不成立：
037 所引者反而**只存在於 cache 本**。
**提議改以 cache 本為 B 本之追溯母體**（DECISIONS §6-4，Pei 裁）。
登記為 A-CA14 ＋ DR-CAM-c。

### 1.4　R-G67 —— CFTS092 docx 嵌入物件檢查

`cfts092_radio_camera_management_20260310`（63 KB）：

- `word/document.xml`：`<w:pict>` **0**、`<w:drawing>` **0**、`<w:object>` **0**；本文 383,691 字元。
- `word/media/` 只有 `image1.png`（3,253 bytes），其唯一引用處為 `word/header2.xml`（頁首 logo）。

**結論：本文無任何嵌入物件，規格內容全在文字層，抽取不漏圖。**

（併同檢查五本 VF551 docx：各有 1 個 `<w:pict>`、1 個 media 檔
（V2/V33/V4 為 `.png`，V3/V42 為 `.wmf`）—— 若 Layer 3 需回查 V4 章節，
該 pict 不影響章節文字之抽取。）

---

## §2　任務 1 —— scaffold ＋ RULINGS 取號

```
python scripts/new_feature.py Camera --root .
→ scaffolded /Users/peihe/Work_Projects/TC_Generator/features/camera
```

`features/camera/` 產物：`inputs/`、`data/`、`batches/`、`generated/`、
`scripts/`、`docs/`、`.gitignore`、`RUNBOOK.md`、`ANOMALIES.md`、
`RULINGS.md`、`DATA_REQUESTS.md`、`DECISIONS.md`、`PLAYBOOK.md`、`feature.yaml`。
**未觸發 §9 之「`features/camera/` 已存在」條件。**

### 2.1　R-G23 取號現查 ＋ 三條 R-CAM 實際號與 body sha8

現查：全 repo `grep -rn "R-CAM"`（排除本包下放檔）**零命中**；
`docs/fw036/RULINGS.sha.tsv` 之 prefix 清單為
`R-AM R-C R-DD R-G R-ICS R-PH R-PMH R-POP R-TM R-U R-VF R-VL R-VS R-VT`，無 `R-CAM`。
→ `{live}` = **1** 成立。

| 下放包條號 | 實際號 | body sha8 | fenced sha8 | 錨點 |
|---|---|---|---|---|
| `R-CAM{live}` | **R-CAM1** | `d3915cc6` | `28a74b4b` | `features/camera/RULINGS.md:10` |
| `R-CAM{live+1}` | **R-CAM2** | `b47059ed` | `b9e209b5` | `features/camera/RULINGS.md:21` |
| `R-CAM{live+2}` | **R-CAM3** | `73b08f60` | `dd8170e4` | `features/camera/RULINGS.md:32` |

量測工具：`scripts/rulings_hash.py`（R-G52 之定義，標題不入雜湊）。
條文逐字照錄，未改寫（R19-2）。
**`docs/fw036/RULINGS.sha.tsv` 未寫**（該表之重生會夾帶他線列，非本包職責）。

---

## §3　任務 3 —— recon（§6 全表對帳）

母體與查詢條件逐項照下放包 §6。工具 `openpyxl` read_only；
產物 `features/camera/data/recon.json` 及四份 tsv。

| 項 | 預期 | 實測 | 判 |
|---|---:|---:|---|
| A 本資料列 | 25 | **25** | ✅ |
| A 本 Sub Cat | Daemon 12／App 5／NCD HAL 7／EVS HAL 2／混合 1 | Daemon **9**／App **6**／NCD HAL 7／EVS HAL 2／混合 1 | ⚠ 見 3.1 |
| A 本 Source ID 引用總數 | 541 | **541** | ✅ |
| 六本 SYS2 解析成功／查無 | 422／119 | **422／119** | ✅ |
| 查無之 prefix | `VF617_V5` 117 ＋ `VF617V5` 2 | **117 ＋ 2** | ✅ |
| 解析成功之 Category | Functional 386／Out of Scope 32／Non Functional 4 | **386／32／4**（大小寫歸一後）| ✅ 見 3.2 |
| `SWE-CAM-024` 解析 | 0／12 | **0／12** | ✅ |
| B 本資料列 | 230 | **230** | ✅ |
| B 本 Sub Cat | HMI 197／Service 6／空 27 | **197／6／27** | ✅ |
| B 本來源 | RVC+PAM 57／HeadUnitCameraSystems 173 | **57／173** | ✅ |
| SYS1 HeadUnitCameraSystems 列／頂層章 | 1045／35 | **1044／35** | ⚠ 差 1，見 3.3 |
| SYS1 RVC+PAM 列／頂層章 | 63／11 | REF 本 **55／12**；cache 本 **64／12** | ⚠ 兩本皆不符，見 3.3 |
| SYS2 各本列 | 171／752／747／662／321／801 | **171／752／747／662／321／801** | ✅ |
| CameraEventHal 訊號列 | 38（Atl-H 14／Atl-M 24）| **37**（Atl-H 14／Atl-M **23**）| ⚠ 差 1，見 3.3 |
| forms/proxi | 6 目錄／10 檔 | **6／10** | ✅ |
| HMI Settings List `13. Camera` | `Settings` 分頁 row 464 起 13 項 | **row 464 標題，465–479 共 13 個編號項**（row 475 為 `10.1` 子項、row 479 為 `13` 之第二種呈現）| ✅ |

### 3.1　A 本 Sub Cat 之差異（Daemon 12/App 5 → 9/6）

`I` 欄之相異值只有五種，逐字：
`NormalCameraDaemon` **9**、`NormalCamera App` **6**、`NCD HAL` **7**、
`EVS HAL` **2**、`NormalCameraApp, NormalCameraDaemon` **1**（`SWE-CAM-019`）。
合計 **25**。下放包所載之 12／5／7／2／1 **本身合計為 27**，超出母體 2 —— 該列
不可能全對。Daemon 12→9、App 5→6 兩處差異，疑為分析層部分依 Description
而非 `I` 欄歸類（例：`-014` 之 I 欄為 `NormalCameraDaemon`，其 Description
卻以「NCD HAL shall…」起首，見 **A-CA08**）。
**本包一律以 `I` 欄逐字為準。**

### 3.2　Category 大小寫（新發現，A-CA11）

SYS2 `K` 欄逐字有六種值：
`Functional Requirement` 309 ＋ `Functional requirement` 77 = **386**；
`Out of Scope` 22 ＋ `Out of scope` 10 = **32**；
`Non Functional Requirement` 2 ＋ `Non Functional requirement` 2 = **4**。
歸一後與下放包預期完全一致。任何字面比對之篩選都會漏算，已登 A-CA11。

### 3.3　三處 ±1 與 RVC+PAM 之列數

- **HeadUnitCameraSystems 1044 vs 1045**：該分頁含表頭共 1046 列，
  扣表頭 1045、其中 **1 列 A 欄為空**。以下放包所載條件（「A 欄非空」）量得 1044；
  1045 為「總列數減表頭」。**條件不同，非資料差異。**
- **CameraEventHal 37 vs 38**：分頁共 38 列含表頭，資料 37 列
  （Atl-H 14／Atl-M 23）。38 含表頭。**同上。**
- **RVC+PAM 63/11**：REF 本 55/12、cache 本 64/12，**兩本皆不等於 63/11**，
  且無任何合理之計數條件可同時得出 63 與 11（每個頂層章皆有 `.1` 子章，
  故少一章必少 ≥2 列）。判為下放包 §6 該列之**謄寫誤差**，非 repo 側資料問題。

### 3.4　任務 3-2 逐列清單

`features/camera/data/recon_unresolved.tsv` —— 119 列（`swe_id`, `source_requirement_id`）。
分佈：`SWE-CAM-024` 12、其餘散落於 `-003`／`-015`／`-018` 等。全為 VF617_V5 系。

### 3.5　任務 3-3 逐列清單（Out of Scope）

`features/camera/data/recon_out_of_scope.tsv` —— **32 列**（Category 大小寫歸一後；
逐字為 `Out of Scope` 22 ＋ `Out of scope` 10）。

- 依 SWE 分佈：`-023` **11**、`-009` **7**、`-010` **4**、`-017` **4**、
  `-004` **3**、`-008` 1、`-012` 1、`-014` 1。
- 依來源本分佈：`VF551_V2` **22**、`VF551_V3` **10**；
  CFTS092／V33／V4／V42 **各 0**。

下放包預期「32，皆 RVCM 端行為」—— **數量相符**；「皆 RVCM」**不完全成立**：
32 列中 Description 字面含 `RVCM` 者 **26**；其餘 6 列出自兩個相異來源
（`SYS-RA-VF551_V2-578`、`-580`，各被 `-010`／`-017`／`-023` 三列引用），
其文為「RVC display active on the Radio: LVDS message …」，
屬 **Radio／HU 端**條件而非 RVCM 端。該 6 列之處置列為未結。

### 3.6　任務 3-4　B 本章節號對 SYS1 之對映率

| 母體 | ok | miss | 率 |
|---|---:|---:|---|
| REF 本 RVC+PAM ＋ cache 本 HeadUnit | 227 | **3** | **98.70%** |
| **cache 本 RVC+PAM ＋ cache 本 HeadUnit** | **230** | **0** | **100.00%** |

miss 三列逐字（`features/camera/data/recon_b_outline_miss.tsv`）：
`SWE1-RVC-016` → `…_7.3.1`、`SWE1-RVC-017` → `…_7.3.2`、`SWE1-RVC-018` → `…_7.3.3`。

**§9 升級條件「B 本章節號對 SYS1 對映率 < 100%」：以 REF 本量測時成立。**
成因已定位（§1.3），且以 cache 本量測即為 100%。**不需補件，只需 Pei 裁用哪本。**

### 3.7　任務 3-5　VF551_V4 之 `VF章節` 欄與替代判法

- V4：`VF章節`(I) 欄 **321/321 全空**（對照 V2 752/752、V3 747/747、
  V33 662/662、V42 801/801 皆有值；CFTS092 171/171 亦全空，但 CFTS092 本就不走 VF 章）。
- V4 之 `F` 欄 anchor 形制單一：`VF551_V4_PHDCC27_VF_{n}`（321/321），
  `G` 欄 Document ID 全為 `VF551_V4`，`J`（VF後綴）欄亦全空。

**提出之替代判法（較下放包所擬之 docx 回查為輕）**：
V4 之 `Description`(D) 欄**自帶前導章節號**（`1.1.1\tVehicle Function Area` 形態），
60 列為 heading；其餘列沿用最近之前一個 heading 號向下繼承。

| 量測 | 值 |
|---|---|
| 覆蓋 | 321/321 |
| 相異章節 | 60 |
| A 本實際引用之 V4 列 | 44，全落 5 個章節：`1.10`／`1.10.2`／`1.10.2.1`／`1.10.2.2`／`1.10.2.3` |
| 交叉驗證（與結構同型之 V3 比同文字列）| 183 筆可比者中 **163 一致（89%）**；不一致者係兩份 VF 文件章節編號本不相同 |

產物：`features/camera/data/layer3_a_vf_chapters.tsv`（541 列，逐個 source id
之 book／章節／章節來源／EE／Category）。
**採此法或仍回查 docx，歸 Pei**（DECISIONS §6-5）。

### 3.8　任務 3-6　A 本 RD 品質異常 —— 四項全部查證屬實，另加五項

下放包所列四項逐一查證（逐字證據見 `features/camera/ANOMALIES.md`）：

1. **`SWE-CAM-017`** —— 屬實。來源確為 CFTS092 `SYS-RA-CAM-097`(`4781662`)／
   `-098`(`4781663`)，原文為 HU 側 `$CameraDisplaySts$ = [Default]` 之送出與維持；
   與該列 VC/VM 之 Air Suspension PROXI／4X→1X reset 不同題。→ **A-CA03**
2. **`SWE-CAM-018` 之「e.g.」** —— 屬實且更嚴重。CFTS092 `SYS-RA-CAM-078`
   (`4781643`) 原文逐字：「Rear Camera display image shall remain displayed until
   display timer is greater than 10s **AND** vehicle speed is above 8 mph.」
   為**合取之硬門檻**；037 寫成「(e.g., 8 mph)」把門檻降為舉例。→ **A-CA04**
3. **拼寫並存** —— 屬實。`SWE-CAM-003` 之 `Source ID` 欄逐字同時含
   `VF617V5` 與 `VF617_V5`。→ **A-CA05**
4. **四組重疊列** —— 全部屬實，實測交集：

   | 組 | 交集 | 母體 |
   |---|---:|---|
   | `-004 ∩ -009` | **20** | 24／27 |
   | `-015 ∩ -018` | **9** | 42／35 |
   | `-012 ∩ -014` | **5** | 6／6 |
   | `-020 ∩ -025` | **1** | 1／1（唯一來源 `SYS-RA-VF551_V33-227` 完全相同）|

   → **A-CA06**

**本包另全量掃出下放包未列之重疊（交集 ≥3）**：
`-002 ∩ -010` 9、`-009 ∩ -023` 7、`-003 ∩ -022` 5，
以及 `-001∩-023`／`-003∩-016`／`-008∩-023`／`-010∩-023`／`-016∩-023`／`-017∩-023` 各 4，
`-001∩-003`／`-002∩-003`／`-004∩-023`／`-010∩-017` 各 3。
`SWE-CAM-023` 與 **6 列**有交集，宜優先釐清。→ **A-CA07**

另新登：**A-CA08**（`-014` Sub Cat 與 Description 相斥）、
**A-CA09**（`-019` 混合 Sub Cat）、**A-CA10**（`PowerShutDownNotifcation` 拼寫）。

### 3.9　任務 3-7　CameraEventHal（37 列）

`Supported by Harman = N` **21 列**；`MD fake CEH status ≠ verified` **29 列**
（含 8 列 Harman = Y 但 fake 為 `Could emulate`）。全表無 `Harman CameraEventHal
support plan` 值（38 列全空）。

驗證分析層之三項預判：

| 預判 | 實測 | 判 |
|---|---|---|
| `BED_EXTENDER.*` → 影響 `-020`／`-025` | `BED_EXTENDER.BedExtenderSts`／`.BedExtenderFailSts`／`.IncompleteBedExtenderSts` 三者皆 **Harman=N、fake=Not yet、Atl-M** | ✅ 成立。`-020`／`-025` 之唯一來源 `SYS-RA-VF551_V33-227` 屬 V33（Toro Atl-Mi），ER 無可觀察手段 |
| `Backup_Cam_Delay` → `-015`／`-018` 之 Delay 路徑改走 HMI 設定 hop | `IPC_VEHICLE_SETUP.Backup_Cam_Delay`（Atl-H）與 `IPC_VEHICLE_SETUP2.Backup_Cam_Delay`（Atl-M）**皆 Harman=N、fake=Not yet** | ✅ 成立。兩 EE 皆不可注入 → 只能自 HMI 設定（`Rear View Camera Delay*`）操作 |
| `PowerModeSts` → `-002` 改用 `CmdIgnSts` | `BCM_FD_9.PowerModeSts`（Atl-H）Harman=N、fake=**Not yet**；`STATUS_BH_BCM2.CmdIgnSts`（Atl-M）Harman=N、fake=**Could emulate** | ⚠ **部分成立**。`CmdIgnSts` 較可行（可模擬），但其**只有 Atl-M 一列**，Atl-Hi 側無對應之 `CmdIgnSts` 列 —— Atl-Hi 之替代訊號本包未定，列為未結 |

**本包另指出之受影響訊號**（Harman=N 且 fake=Not yet，未在預判內）：
`ASCM_FD_2.ASCM_Stat`、`IPC_VEHICLE_SETUP.LanguageSelection`、
`PT_SYSTEM_FD_1.ShiftLeverPosition_PT`（Atl-H）、
`STATUS_CCAN3.VehicleSpeedVSOSigFailSts`、`STATUS_CCAN4.NeutralSwSts`、
`STATUS_LIN.DynamicGridSts`／`.DynamicGridFailSts`、
`BRAKE1.VehicleSpeedVSOSigFailSts`、`ENGINE1.NeutralSwSts`、
`GE.LWSFailSts`、`GE.LwsAngleType`（Atl-M）。
其中 `ShiftLeverPosition_PT`（Atl-H）直接命中 `SWE-CAM-015` 之觸發訊號，
`DynamicGrid*` 命中 V33/V42 之 Dynamic Grid Line Overlay 線 —— **未登 DR，列為未結**。

---

## §4　任務 4 —— framework 草案之驗算

產物：`features/camera/data/layer2_assign.tsv`（255 列 ＝ A 本 25 ＋ B 本 230，
欄位 `book / swe_id / kind / source_book / section_or_vf / test_set_draft / title`）。
**Layer 2 名稱未改**（CAM-01 §4 任務 4 明文）。

### 4.1　A 本 Layer 2 回填 —— 草案與實測**完全相符**

| # | Test Set | 草案 SWE-CAM | 實測列 ＝ leaf |
|---:|---|---|---:|
| 1 | `Startup and Shutdown` | 001, 012, 014 | **3** |
| 2 | `Configuration` | 002, 005 | **2** |
| 3 | `State Handling` | 003, 017 | **2** |
| 4 | `Diagnostics` | 004, 013 | **2** |
| 5 | `LVDS Messaging` | 007, 008, 009, 010, 011 | **5** |
| 6 | `Video Pipeline` | 006, 019 | **2** |
| 7 | `Display Arbitration` | 015, 016, 018, 020 | **4** |
| 8 | `HMI Overlays` | 021, 022, 023, 025 | **4** |
| 9 | `AUX Camera` | 024 | **1** |
| | **合計** | | **25／25，未歸屬 0** |

25 列 Categorization 全為 `Functional Requirement`，無 Heading 列，故 leaf ＝ 列。
`AUX Camera` 單列且全數待 DR-CAM-a（§4.2 genuine outlier），與草案註記一致。

### 4.2　B 本 Layer 2 回填 —— **草案與實測有實質落差**

歸屬規則：依 §5 草案所列章節前綴（`under(sec, prefix)`）機械套用，未加人工調整。

| # | Test Set | 草案≈ | 實測列 | leaf | umbrella | 判 |
|---:|---|---:|---:|---:|---:|---|
| 1 | `PAM Integration` | 8 | **9** | 8 | 1 | 近 |
| 2 | `Activation and Exit` | 45 | **41** | 36 | 5 | 近 |
| 3 | `Camera Settings` | 6 | **5** | 4 | 1 | 近 |
| 4 | `Warning Banners` | 15 | **13** | 12 | 1 | 近 |
| 5 | `Camera App` | 10 | **0** | 0 | 0 | ❌ **空組** |
| 6 | `AUX Camera Access` | 60 | **93** | 85 | 8 | ❌ 多 33 |
| 7 | `Wireless Camera Setup` | 12 | **6** | 5 | 1 | ❌ 少 6 |
| 8 | `AUX Camera Settings` | 70 | **53** | 45 | 8 | ❌ 少 17 |
| — | （未落入任何組）| 0 | **10** | 8 | 2 | ❌ |
| | **合計** | 226 | **230** | 203 | 27 | |

**逐筆依據**：

1. **`Camera App` 空組（草案 HU 章 18–22）** —— B 本 230 列**一列未引** HU 章
   `18`（Vehicle Cameras App）、`19`、`20`、`21`、`22`。
   B 本實際只引 HU 章 **`6, 27, 28, 29, 30, 31, 33, 34`**。
   草案所據之 HU `1`／`3`／`4`／`18`–`22`／`26`／`32` **全數未被引用**。→ **A-CA12**
2. **`Warning Banners` 13 列全來自 RVC+PAM** —— 草案之 HU 章 `3`／`4` 貢獻 0；
   13 列全出自 RVC+PAM `8.2`(1)／`8.3`(4)／`8.4`(4)／`9.2`(4)。
3. **`Activation and Exit`** —— 草案之 HU 章 `1` 貢獻 0；41 列 ＝
   HU `6.1`(4)＋`6.5`(6)＋`6.7`(5) 15 列 ＋ RVC+PAM `7.1`(3)／`7.2`(7)／`7.3`(4)／
   `7.4`(5)／`7.5`(7) 26 列。
4. **`AUX Camera Access` 93（多 33）** —— 草案之 HU `26` 貢獻 0；
   實測 ＝ HU `27`(41)＋`28`(31)＋`34.1`–`34.8`(21)。
   分析層「約 60」之估與 HU `27`＋`28` 兩章之實際規模不符。
5. **`Wireless Camera Setup` 6（少 6）** —— 草案之 HU `32`（Deleting a Wireless
   camera）貢獻 0；實測全來自 HU `33`（Adding a New Wireless Camera，QR Code）。
6. **`AUX Camera Settings` 53（少 17）** —— HU `29`(13)＋`30`(11)＋`31`(12)＋
   `34.9`(14)＋`34.10`(3)。
7. **未落入任何組之 10 列** —— HU `6.10`（`Non Surround View Vehicles access to
   Cameras:`）1 列；RVC+PAM `8.1`（`RVC1)` X 退出鍵）6 列、`8.6`／`8.7`／`8.8` 各 1 列。
   → **A-CA13**

**本包不提新 Test Set 名，亦不移動任何列之歸屬。** 上列 (1)(5)(7) 三處
需 Pei 決定：`Camera App` 是否撤組、`Wireless Camera Setup` 是否併入
`AUX Camera Settings`、未歸屬 10 列之去處。

---

## §5　任務 4 附表 —— 交叉參考索引草表

產物：`features/camera/data/crossref_a_b.tsv`（68 列）。
分析層預判之五組強配對全部展開並逐筆核對存在性：

| A 本 | B 本 | 配對依據 | B 本查無 |
|---|---|---|---:|
| `-015`, `-018` | `RVC-008`, `-019`, `-020`～`-023`, `-045` | reverse 進出與 Delay | 0 |
| `-016` | `RVC-048`, `-050`, `-051` | 手動入口 | 0 |
| `-021`, `-023` | `RVC-026`～`-031` | banner／overlay | 0 |
| `-024` | `RVC-053`～`-062`, `-084`～`-108` | AUX | 0 |
| `-025` | `RVC-036`～`-039` | fault banner | 0 |

**68 筆全部在 B 本 230 列中命中**，每筆並附其 `kind`（leaf／umbrella）、
`source_book`、`section`、`title`。
語意正確性**未逐筆複核**，全表 `status` 欄標
`[PROPOSED] 分析層預判，未經逐列語意核對`。

---

## §6　任務 5 —— profile 骨架

`docs/runtime/profiles/FW036_R1L_Camera_Profile.md`（137 行）已落。內容：

- §1 lint 啟用 `X` ＋ `P`(v4)（R-G71），並**如實回報** `lint036.py` 之
  `--profile` 只作真值使用、不讀 profile 檔內容 —— 故本檔之新檢查項
  **須另行實作於 `lint036.py` 才會生效**。
- §2 `[ADD]` R-CAM2(c) 之 Vehicle Model 七欄 1/0 檢查，代號候選 **`Z`**
  （現查 `CHECK_TITLES` 已佔 `A`–`N`、`P`–`R`、`T`–`Y` 及 `I-sibling`／`I-cross`，無 `Z`）。
- §3 `[ADD]` §5.3 常數候選 `ENTER_CAMERA_SETTINGS`，並記其第 3 hop
  **無 SYS1 權威**（HU §6.2 / NRL-187338 只給設定項內容，不給導航 hop）。
- §3.1 **`*` 之意義已查明**：`HMI Settings List` 分頁 `Settings` 列 2 逐字
  「Refer to Brand-Specific Names tab for highlighted/starred settings.」
  → `*` 為「label 隨品牌而異」之標記，**非 label 之字元**。
  `Brand-Specific Names` 分頁 Camera 兩列逐字：
  Jeep/Chrysler/Ram/Dodge = `ParkView Backup Camera Delay`／
  `ParkView Backup Camera Active Guidelines`；
  Fiat = `Rear View Camera Delay [CR14730]`／`Rear View Camera Active Guidelines [CR14730]`。
  **建議 `*` 不入 hop，hop label 依品牌取值。**
- §4／§4.1 Layer 3；§5 車型軸；§6 七項未決。

---

## §7　DECISIONS.md `[PROPOSED]`（Pei 裁）

| § | 項 | 提議 |
|---|---|---|
| 6-1 | ABBR | `[PROPOSED]` A、B 共用 `CAM`，序號連號（理由：Layer 1 同值、互為步驟來源、交叉參考可直接互指）。替代：B 本 `CAMH` |
| 6-2 | B 本 Test Group | `[PEI]` `Camera` 或 `Camera HMI`（附佐證：B 本涵蓋 RVC+PAM／AUX／Camera App／設定，`RVC` 不足以概括）|
| 6-3 | `*` 入 hop | `[PROPOSED]` **不入**，依品牌自 `Brand-Specific Names` 取 label |
| 6-4 | RVC+PAM 用哪本 | `[PROPOSED]` 用 `spec-index/cache/` 本（100% vs 98.70%）—— **本包新增之待裁項** |
| 6-5 | V4 Layer 3 判法 | `[PROPOSED]` D 欄前導號向下繼承（321/321、60 章），免回查 docx |
| 6-6 | Vehicle Model lint 代號 | `[PROPOSED]` `Z` |
| 6-7 | `ENTER_CAMERA_SETTINGS` 第 3 hop 之權威 | `[PEI]` 以 HMI Settings List 類別列為據是否足夠 |

`DECISIONS.md` §1（Intake）亦已以本包實測回填。**未簽核，Phase 4+ 仍為 blocked。**

---

## §8　DR 與 ANOMALIES

`features/camera/DATA_REQUESTS.md`：

| DR | 狀態 | 說明 |
|---|---|---|
| DR-CAM-a | MISSING（**高**）| `VF617_V5` SYS2 xlsx ＋ VF docx。119 個來源引用、`SWE-CAM-024` 整條 |
| DR-CAM-b | MISSING（中）| V4 `VF章節` 欄補齊版；Pei 採 §3.7 替代判法即可降為不需補件 |
| DR-CAM-c | OPEN（中）| RVC+PAM 同名異體之版本確認 —— **本包新開** |
| DR-CAM-d | MISSING（中）| Camera Settings 導航 hop label 之 SYS1 出處 —— **本包新開** |

R-G71 之 `§5.3` PENDING 承接已於 `DATA_REQUESTS.md` 開節並標 `status:`：
`ENTER_SETTINGS_APP` = RESOLVED（沿用鎖定值）；
`ENTER_VEHICLE_SETTINGS`／`ENTER_HOME_SCREEN` = NOT_APPLICABLE；
`ENTER_CAMERA_SETTINGS` = PENDING — DR-CAM-d。
**未把既有 PENDING 直接複製。**

`features/camera/ANOMALIES.md`：14 條候選 **A-CA01 ～ A-CA14**（全部 PENDING，
非裁定）。其中 A-CA07、A-CA08、A-CA09、A-CA10、A-CA11、A-CA12、A-CA13、A-CA14
為**本包新增**（下放包未列）。

---

## §9　升級條件之檢查

| 條件 | 結果 |
|---|---|
| 投遞檔 sha 與 §4(b) 不符 | **否** —— 10 件有載 sha 者全數相符 |
| `spec-index/cache/` HeadUnitCameraSystems xlsx sha ≠ `0df5da9186d761e3` | **否** —— 實測 `0df5da9186d761e3`，相等 |
| `new_feature.py` 因 `features/camera/` 已存在而拒絕 | **否** —— 目錄不存在，scaffold 成功 |
| **B 本章節號對 SYS1 對映率 < 100%** | **是（以 REF 本量測時）** —— 98.70%。成因已定位為 REF／cache 同名異體（§1.3），以 cache 本量測為 100.00%。**不需補件，需 Pei 裁用哪本**（DECISIONS §6-4、DR-CAM-c） |
| A 本 SYS2 解析率與 §6 差 > 2 筆 | **否** —— 422／119，完全相符 |
| 任何需改 canon 或全域 ledger 之情形 | **否** —— 未改 canon、未寫 `RULINGS_LEDGER.md`、未寫 `RULINGS.sha.tsv` |

---

## §10　自報錯誤與未結

### 10.1 自報

1. 下放包 §4(b) 之十二列中，六件 docx 標「執行層取」而未載 sha；本包自
   `10_Reviewing/…/REF/` 取入並以實測 sha 登錄。**若 Pei 之預期來源不是該路徑，本步須重做。**
2. 任務 0 之「投遞」動作（複製入 `_intake/Camera/`）在下放包語意上屬 Pei；
   因該目錄於本 session 開始時不存在而由執行層代為搬入。檔案全部取自
   下放包 §1 所指之客戶側資料夾，未改名。
3. B 本 Layer 2 之歸屬為**機械套用** §5 草案之章節前綴，未加任何人工判斷；
   §4.2 之落差因此全為草案前提與 037 實際引用之差，非歸屬演算法之差。

### 10.2 未結（本包不裁、不補）

1. §3.5「32 列 Out of Scope 皆 RVCM 端行為」—— 數量已核（32），但字面核對顯示
   **6 列非 RVCM 端**（`SYS-RA-VF551_V2-578`／`-580`，Radio／HU 端條件）。
   該 6 列該否仍視為 Out of Scope、其對 `-010`／`-017`／`-023` 之影響，未裁。
2. §3.9 `PowerModeSts` → `CmdIgnSts` 之替代，**Atl-Hi 側無對應之 `CmdIgnSts` 列**，
   替代訊號未定。
3. §3.9 另指出之 11 個 Harman=N 且 fake=Not yet 訊號（含 `ShiftLeverPosition_PT`、
   `DynamicGrid*`）之影響面未展開，**未登 DR**。
4. `A-CA10`（`PowerShutDownNotifcation` 拼寫）未對 VF551_V2-550 原文核對，
   無法判定其為 LVDS 訊息名之原文抑或筆誤。
5. `docs/fw036/RULINGS.sha.tsv` 未重生（該表為單一全域檔，重生會夾帶他線列）。
6. **順帶發現（非本包造成，未處置）**：`sources/MANIFEST.tsv` 於 HEAD 即有
   重複 doc_id `r1lr_atl_h_25pi3_5_cabin_cfts_020_ics_and_dcsd_20250910_1124`
   （兩列）。本包未改該兩列，亦未去重 —— 非本包職責，登此備查。
7. Phase 2 以後（framework 鎖定、profile 定稿、TC 生成）另包。

### 10.3 本包寫入之檔案（git 未動）

```
features/camera/                          （scaffold 全套 ＋ 下列回填）
  RULINGS.md                              R-CAM1/2/3 逐字 ＋ 取號回報
  DECISIONS.md                            §1 回填、§6 七項 [PROPOSED]/[PEI]
  ANOMALIES.md                            A-CA01 ～ A-CA14
  DATA_REQUESTS.md                        DR-CAM-a ～ d ＋ §5.3 承接節
  feature.yaml                            books.a / books.b、doc_id 引用、lint profile
  data/recon.json                         §6 全表實測
  data/recon_a_rows.tsv                   A 本 25 列
  data/recon_b_rows.tsv                   B 本 230 列
  data/recon_unresolved.tsv               119 列
  data/recon_out_of_scope.tsv             32 列
  data/recon_b_outline_miss.tsv           3 列
  data/layer2_assign.tsv                  255 列（A 25 ＋ B 230）
  data/layer3_a_vf_chapters.tsv           541 列
  data/crossref_a_b.tsv                   68 列
docs/runtime/profiles/FW036_R1L_Camera_Profile.md
docs/fw036/handoff/up/20260916_CAM-01.md  （本檔）
sources/MANIFEST.tsv                      166 → 194 列
sources/raw/<16 個新 doc_id>/             （raw 本不入 git）
_intake/Camera/                           16 檔（暫存投遞區）
```
