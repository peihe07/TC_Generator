# 上繳包 SEC-05 —— framework Revise v02 ＋ batch1 v02 ＋ batch 2 ＋ 合併本 `security_v01`

對應下放包：`down/20260916_SEC-05.md`（Pei 准 SEC-04 審閱 §二／§三）。
**禁區遵守**：git 未動；`down/`／`up/` 既有檔未動；其他 feature 未動；canon 未動；
`security_batch1_v01.xlsx` **未動**（R-TM13）；`lint036.py` 只動 `SEC_OBSERVE` 與 `SEC_CMD_LINE`（見 §3-3）。

---

## 1　裁定落檔（§6-1）

| 條號 | body sha8 | body_kind | 行數 |
|---|---|---|---|
| **R-SEC17**（Test Set 英文定名）| `d99fa116` | fenced | 25 |
| **R-SEC7(c) amend**（七類為觀察面之類別）| `c884c33e` | fenced | 8 |
| **R-SEC18**（CAN 通道寫法）| `505a625a` | fenced | 15 |

`rulings_hash.py --gate` **通過**；R-SEC1~R-SEC16 之 body sha8 **逐條不變**。

### 1-1　R-SEC17 指定之 SAM 四列複核 —— **三列相符，一列不合**

| SWE1 | 037 Requirement Title | 判 |
|---|---|---|
| `SWE1-SAM-0001` | DebugAuth is under Android 14 environment | ✅ 執行環境 |
| `SWE1-SAM-0002` | DebugAuth should be a native layer daemon service | ✅ 部署形態 |
| `SWE1-SAM-0019` | DebugAuth has following external interfaces: LogDog, Certprovider, BoringSSL, Json library… | ✅ 部署前提 |
| **`SWE1-SAM-0008`** | **Format of AuthData should follow SAM package definition in System design** | **✗ 不合** —— 為**資料格式**需求（manifest 檔名 `<SSN>_SAM_<SAMType>.json`、簽章、憑證），非執行環境或部署前提 |

依條文**回報，不自行移組**。該列在 batch 2 產出群內，本包仍依現行歸屬（`Service Environment`）產 2 TC。
若 Pei 裁定移組（例 `AuthData Verification`），須再走一次 R-G72 Revise。

### 1-2　R-SEC18(d) 之一處筆誤（逐字回報）

條文寫 `/data/vehicle/dauth/seqId`；037 `SWE1-SAM-0015`／`-0017` 原文為
**`/data/vendor/dauth/seqId`**（`vendor`，非 `vehicle`）。
本包依 **037 原文**寫（R-SEC4(c) 逐字取自來源），**不沿用條文之筆誤**。

---

## 2　framework Revise v02（§6-2）

`features/security/framework.md`（sha16 `142d5f7dfc78485c`）加 `REVISE v02 2026-09-16 (R-SEC17)` 段，
列六項 old → new，Layer 2 表就地改名，末記 **`LOCKED v02 2026-09-16 (R-SEC17)`**。

| # | Test Group | old（v01 佔位）| **new（v02）** |
|---:|---|---|---|
| 1 | Cert Provider | `(非功能)` | **`Service Robustness`** |
| 2 | Key Install | `(非功能)` | **`Platform Compliance`** |
| 3 | SAM | `(環境)` | **`Service Environment`** |
| 4 | ECU Cert | `(IPC／JNI／IO)` | **`Internal Interfaces`** |
| 5 | SWDL Secure Lib | `(總則)` | **`Library Scope`** |
| 6 | Log Encrypt | `(其餘)` | **`Encryption Services`** |

**Layer 2 名稱之 CJK 計數：5 → 0**（Part II 表內已無中文組名；REVISE 段之 old 欄保留原字備查）。
`layer2_assign.tsv` 同步（21 列改名，`test_set` 欄 CJK 字元 **38 → 0**）；
`build_layer2_assign.py` 之 `ASSIGN` 表同源同步。
`feature.yaml` **無 Test Set 列舉**、`sibling_plan.tsv` **無 `test_set` 欄** → 兩者無須改。

### 2-1　**併同更正一項計數（執行層自報）**

Layer 2 之組數為 **29**，非先前各包所載之 **30**。

- 成因：`build_layer2_assign.py` 之對帳訊息把組數**寫死為 `30`**
  （`print("  ✓ 30 組 Test Set …")`），該字串經 SEC-02～SEC-04 之上繳包與 `framework.md` v01 沿用。
- 逐組核對 SEC-01 §5 之表：Cert Provider 6／Key Install 7／SAM 6／ECU Cert 4／
  SWDL Secure Lib 4／Log Encrypt 2 = **29**。
- 已改為 `len(ASSIGN)` 計算；`framework.md` 之 Part II 標題、合計列、Part IV 皆更正為 29。
- **leaf 總數 70 與逐組 leaf 數不受影響**；`layer2_assign.tsv` 之 70 列歸屬不變。

---

## 3　batch1 v02（§6-3）

`features/security/sandbox/batch1/security_batch1_v02.xlsx`（sha16 `39c135bcaafbc1b6`）。

| 項 | v01 | **v02** |
|---|---|---|
| TC 數 | 72 | **72** |
| PENDING 行 | 85 | **85** |
| `K` | 10 | **0** |
| `U`／`W` | 85／4 | **85／4** |
| `P`／`X`／`SC`／`SS` | 0／0／0／0 | **0／0／0／0** |
| `selfcheck` 九條 | 0 | **0** |

### 3-1　**v01 → v02 逐格 diff ＝ 10 格，全在 `Test Set`（H）欄**

```
r20 H '(非功能)' → 'Service Robustness'      r30 H '(非功能)' → 'Service Robustness'
r21 H '(非功能)' → 'Service Robustness'      r54 H '(非功能)' → 'Platform Compliance'
r22 H '(非功能)' → 'Service Robustness'      r55 H '(非功能)' → 'Platform Compliance'
r23 H '(非功能)' → 'Service Robustness'      r56 H '(非功能)' → 'Platform Compliance'
                                            r57 H '(非功能)' → 'Platform Compliance'
                                            r58 H '(環境)'   → 'Service Environment'
```

全工作簿逐格比對（三 sheet 全掃）**總 diff = 10**，涉及欄 = `{H}`。
**§7 第一項不成立。**

### 3-2　`lint036.py` 之兩處變更（§0 之射程）

| # | 變更 | 依據 |
|---|---|---|
| 1 | `SEC_CMD_LINE` 由 `^\s*\$\s+\S` 改為 `^\s*(?:\$\s+\S\|Send CAN:\s*\S)` | **R-SEC7(a)** 之 `CAN` 類其指令行為 `Send CAN: …`（IN §8.7.5(c)），非 `$` 起首；不改則 batch 2 之 6 個 CAN 步驟全被判「無執行通道」 |
| 2 | `SEC_OBSERVE` 增列 `adb shell getsslog`、`logdecrypt_Ver2.sh` | **R-SEC7(c) amend** 明訂可增列並附出處；出處為 037 `SWE1-LOGENC-006` 之 VC（兩者皆其 verbatim 工具）|

**自報**：變更 1 超出 §0 所寫之「只動 `SEC_OBSERVE`」。其為 R-SEC18 落地之必要條件
（條文既令 CAN 步驟以 `Send CAN:` 寫，檢查器就須認它是通道行），故仍於本包內為之並具名。

### 3-3　回歸（§6-6）

| 呼叫路徑 | base sha16 | now sha16 | 判 |
|---|---|---|---|
| 無 profile | `e2ffac8eb9fd6838` | `e2ffac8eb9fd6838` | ✅ byte 相同 |
| `--profile camera` | `0bd777a9334105d1` | `0bd777a9334105d1` | ✅ |
| `--profile power` | `45b56a6eeca86a29` | `45b56a6eeca86a29` | ✅ |

`tests/test_lint036_z.py` **27 項全過**。

---

## 4　batch 2（§6-4）

### 4-1　13 列對帳 —— 集合相等，惟**兩列之 Testing／CONVERT 標籤與預判互換**

以 `ccvr_batch=2 ∧ disposition≠D` 取得 **13** 列，與 §4.1 之**集合相等**。

| | §4.1 預判 | 實測 |
|---|---|---|
| Testing 11 | SAM-0003/0005/0006/0009/0010/0011/0012/0013/0014/0016/**0017** | SAM-0003/0005/0006/0009/0010/0011/0012/0013/0014/0016 ＋ **LOGENC-006** |
| CONVERT 2 | **LOGENC-006** ＋ 另一列 | **SAM-0008** ＋ **SAM-0017** |

→ **`SAM-0017` 實為 `CONVERT`**（其 Verification Method 欄空白，故在 `peer_review_rows.tsv` 內，
`blackbox_possible = Y`）；**`LOGENC-006` 實為 `Testing`**（Verification Method = `Testing`）。
另一個 `CONVERT` 為 **`SAM-0008`**。**集合不變，只有標籤互換。§7 第三項之「集合不等」不成立。**

### 4-2　`sibling_plan_batch2.tsv`（sha16 `c03a64fec330b0d6`）

**28** 列（§4.2 預估 20~30 ✔；升級門檻 15~40 ✔）。

| SWE1 | sibling | 依據 |
|---|---:|---|
| SAM-0003 | 2 | 規則 1＋3（root／非 root）|
| SAM-0005 | 2 | 規則 4（三個 THEN 同一觸發合一）＋3 |
| SAM-0006 | 2 | 規則 1＋3 |
| SAM-0008 | 2 | 規則 4＋3；**SAMType 列舉未拆**（§4.2：SAM-0018 於 batch 1 已為單列，不重複拆）|
| SAM-0009 | 2 | 規則 1＋3（Install true 非 Logging／Logging）|
| SAM-0010 | 2 | §4.2 指定（新於→正向、舊於→負向）|
| SAM-0011 | 1 | 規則 1 |
| SAM-0012 | 3 | §4.2 指定（`0`／`1`／`65535`），Design Method `邊界值分析` |
| SAM-0013 | 4 | 規則 1（boot／resume）× **R-SEC18(c)**（Hi／Mi）|
| SAM-0014 | 1 | 規則 1 |
| SAM-0016 | 2 | 規則 2（ON／OFF 兩狀態）|
| SAM-0017 | 2 | **R-SEC18(c)**（Hi／Mi）|
| LOGENC-006 | 3 | 規則 1（getsslog／USB 兩觸發路徑）＋ 4.1.3 之跨迭代比對 |

### 4-3　正式本

`features/security/sandbox/batch2/security_batch2_v01.xlsx`（sha16 `6a7580a3fd569d5a`）＋ 28 json。
TC ID：SAM 自 **007** 接續（batch 1 SAM 001~006）；Log Encrypt 自 **001**。

```
lint：A=0 B=0 C=0 D=0 E=0 F=0 G=0 H=0 I=0 I-sibling=0 J=0 K=0 L=0 M=0 N=0
      P=0 Q=0 R=0 T=0 U=39 V=0 W=0 X=0 Y=0 SC=0 SS=0
selfcheck 九條：0 處
```

Priority：**P0 10／P1 13／P2 5**。

> **R-SEC16 未涵蓋之六列（自報）**：`SAM-0003`／`0006`／`0009`／`0010`／`0011`／`0012`
> 不在 R-SEC16 之三個層級列舉內（該條只點名 SAM-0001/0002/0004/0005/0007/0008/0013~0019）。
> 執行層依其能力面**類推指派**並於每 TC 之 Remarks 標
> `priority assigned by analogy; R-SEC16 does not list this SWE1 row`：
> 驗證之接受／拒絕決定（0006／0010／0011／0012）→ **P0**；介面與儲存（0003／0009）→ **P1**。
> **請 Pei 確認或改派。**

### 4-4　CAN 首例全文（§6-4）

**Atl-Hi（`NR1L-SAM-023`，`SWE1-SAM-0013`）**

```
Test Item 下半：(head unit boots up on Atl-Hi; disabling is notified to all target functions)
Pre-Conditions：
  1. DUT is connected via ADB with root permission (Dev/Eng build)
  2. SAM dongle is inserted into the DUT
Test procedure：
  1. Send the ignition signal on the Atl-Hi bus
  Send CAN: BCM_FD_10.CmdIgnSts = 4 (RUN)
  2. Confirm the dauth log after the previous step
  $ adb logcat -s dauth
Expected Result：
  1. BCM_FD_10.CmdIgnSts = 4 (RUN) is sent
  2. The adb logcat -s dauth output contains the OFF notification to the target functions
Vehicle Model：HDCC27=1 DT27=1 VF637=0 598=0 5210=0 Toro=0 Fastback=0
Remarks：… | CAN signal per VHAL User Guide R5 section 2.4; BCM_FD_10 is 0x481;
          raw and label verbatim from PDT27_E2A_R1_FDCAN8.dbc
```

**Atl-Mi（`NR1L-SAM-024`）**

```
Test procedure：
  1. Send the ignition signal on the Atl-Mi bus
  Send CAN: STATUS_BH_BCM2.CmdIgnSts = 4 (RUN)
  2. Confirm the dauth log after the previous step
  $ adb logcat -s dauth
Expected Result：
  1. STATUS_BH_BCM2.CmdIgnSts = 4 (RUN) is sent
  2. The adb logcat -s dauth output contains the OFF notification to the target functions
Vehicle Model：HDCC27=0 DT27=0 VF637=1 598=0 5210=0 Toro=1 Fastback=1
Remarks：… STATUS_BH_BCM2 is 0x46C; raw and label verbatim from P363_BH-CAN [07338]_3A_R2.dbc
```

### 4-5　DBC 查得／佔位之計數（§6-4）

**CAN 步驟共 6：DBC 逐字 4／`<…>` 佔位 2。**

| TC | 訊息 | raw | label | 判 |
|---|---|---|---|---|
| `NR1L-SAM-023` | `BCM_FD_10` | `4` | `RUN` | DBC 逐字 |
| `NR1L-SAM-024` | `STATUS_BH_BCM2` | `4` | `RUN` | DBC 逐字 |
| `NR1L-SAM-025` | `BCM_FD_9` | `<raw>` | `<label>` | **佔位** |
| `NR1L-SAM-026` | `BCM_FD_9` | `<raw>` | `<label>` | **佔位** |
| `NR1L-SAM-030` | `BCM_FD_10` | `4` | `RUN` | DBC 逐字 |
| `NR1L-SAM-031` | `STATUS_BH_BCM2` | `4` | `RUN` | DBC 逐字 |

**DBC 現查（R-G13）**：四本 DBC 合計 `BO_` 619／`SG_` 3,792。
R-SEC18(a) 之五個名稱**全部查得**：
`BCM_FD_10` = `0x481`（`PDT27_E2A_R1_FDCAN8.dbc`）、
`STATUS_BH_BCM2` = `0x46C`（`P363_BH-CAN [07338]_3A_R2.dbc`）、
`BCM_FD_9` = `0x42A`、`CmdIgnSts`、`PowerModeSts` 皆在。
`CmdIgnSts` 之 `VAL_`：`0 Initialization`／`1 IGN_LK`／`3 ACC`／**`4 RUN`**／`5 START`／`7 SNA`。

**佔位之理由**：`PowerModeSts` 之 `VAL_` 只有
`0 Standard_Power`／`1 Logistic_Mode_ON`／`2 Logistic_Mode_PR`／`3 LogisticModeON_and_EngineON`
—— **無 resume-from-suspend 之 label**。依 R-SEC18(b) 以 `<…>` 佔位並於 Remarks 註 `DBC lookup pending`，
**不造值**。**§7 第四項不成立**（無非佔位且非 DBC 逐字之 raw／label）。

---

## 5　合併本（§6-5）

`features/security/sandbox/merged/security_v01.xlsx`（sha16 `459ddea6c3fce212`）。

| 項 | 值 |
|---|---|
| TC 總數 | **100**（batch1 v02 **72** ＋ batch2 **28**）|
| 逐組 | Cert Provider 23／Key Install 25／SAM **31**／ECU Cert 12／SWDL Secure Lib 6／Log Encrypt 3 |
| Priority | **P0 28／P1 53／P2 19／P3 0** |
| 排序 | framework Layer 2 順序 → SWE1 列序 → TC ID（R-G42 D 欄升冪）|
| DEFERRED 19 列 | **不入**（交付本不載內部狀態）|

```
lint：K=0  P=0  X=0  SC=0  SS=0  U=124  W=4（其餘全 0）
```

### 5-1　`coverage.tsv`（sha16 `0f345db2fcc94e64`）

**70 列**全覆蓋，欄 `swe1_id | disposition | ccvr_batch | tc_count | tc_ids`。

| 項 | 值 |
|---|---|
| `tc_count > 0` | **51** |
| `disposition = D`（`tc_count = 0`）| **19** |
| 合計 | **70** |

**§7 第五項不成立**（`tc_count>0` 列數 = 51）。

### 5-2　`pending_summary.tsv`（合併本口徑，sha16 `22b313c1145c6da6`）

**11 個 token／124 行**（batch1 85 ＋ batch2 39）。本包新登 **`X-m`**
（兩次獨立取樣之 log snapshot ＋ 對稱金鑰比對手段，037 LOGENC-006 4.1.3）。
`EXEC_ASSETS.md` 由 14 → **15** 項（sha16 `dc60b5120e7670a7`）。

---

## 6　升級條件逐項核對（§7）

| # | 條件 | 實測 | 判 |
|---|---|---|---|
| 1 | batch1 v02 與 v01 之 cell diff 超出 `Test Set` 欄 10 格 | diff = **10**，欄 = `{H}` | **不成立** |
| 2 | `K` 非 0（任一本）| v02 **0**／batch2 **0**／合併本 **0** | **不成立** |
| 3 | batch 2 sibling < 15 或 > 40；13 列集合不等 | **28**；集合相等（僅標籤互換）| **不成立** |
| 4 | CAN 步驟出現非 `<…>` 佔位且非 DBC 逐字之 raw／label | 6 步：4 逐字／2 佔位，無第三類 | **不成立** |
| 5 | 合併本 `coverage.tsv` 之 `tc_count>0` ≠ 51 | **51** | **不成立** |
| 6 | 需改 canon 或再改 Layer 2 名稱 | canon 未動；Layer 2 名稱只在本包授權之一次 Revise 內改 | **不成立** |

**六項皆不成立，本包完成上繳，不升級。**

---

## 7　產物清單（sha256 前 16 碼）

| 檔 | sha16 |
|---|---|
| `features/security/RULINGS.md` | `c7fa490d5cf56d43` |
| `features/security/framework.md`（**LOCKED v02**）| `142d5f7dfc78485c` |
| `features/security/EXEC_ASSETS.md`（14 → 15 項）| `dc60b5120e7670a7` |
| `features/security/data/layer2_assign.tsv` | `123be15af4977c44` |
| `features/security/data/sibling_plan_batch2.tsv` | `c03a64fec330b0d6`（新增）|
| `features/security/data/coverage.tsv` | `0f345db2fcc94e64`（新增）|
| `features/security/data/pending_summary.tsv` | `22b313c1145c6da6` |
| `features/security/scripts/gen_batch2.py` | `2e75fc10daefd5fc`（新增）|
| `features/security/scripts/build_merged.py` | `0db0957f6e81afc6`（新增）|
| `features/security/sandbox/batch1/security_batch1_v02.xlsx` | `39c135bcaafbc1b6`（新增）|
| `features/security/sandbox/batch2/security_batch2_v01.xlsx` | `6a7580a3fd569d5a`（新增）|
| `features/security/sandbox/merged/security_v01.xlsx` | `459ddea6c3fce212`（新增）|
| **`scripts/lint036.py`** | `e28c84590739c871` |

**未動**：`security_batch1_v01.xlsx`、`feature.yaml`、`apk_pairing.tsv`、`trace_matrix.tsv`、
`batch_order.tsv`、`peer_review_rows.tsv`、`sibling_plan.tsv`、`step_assets.tsv`、`diag_items.tsv`、
`DATA_REQUESTS.md`、`DECISIONS.md`、profile、`sources/MANIFEST.tsv`、`pilot01/`、`pilot02/`、
canon、其他 feature、git。

---

## 8　自報錯誤與偏差

| # | 事項 | 性質 | 處置 |
|---|---|---|---|
| 1 | **Layer 2 組數 30 → 29** | **執行層錯誤，且已傳播三包** | 成因為 `build_layer2_assign.py` 寫死 `30`；已改為計算值，`framework.md` 三處更正（§2-1）|
| 2 | `lint036.py` 之 `SEC_CMD_LINE` 亦改（§0 只寫 `SEC_OBSERVE`）| **越出射程** | R-SEC18 落地之必要條件；已具名（§3-2）|
| 3 | `SEC_OBSERVE` 增列 `adb shell getsslog`／`logdecrypt_Ver2.sh` | R-SEC7(c) amend 允許 | 出處為 037 LOGENC-006 之 VC |
| 4 | R-SEC18(d) 之路徑筆誤（`vehicle` vs `vendor`）| **下放包筆誤** | 依 037 原文寫，逐字回報（§1-2）|
| 5 | R-SEC17 之 SAM `Service Environment` 含一不合列（SAM-0008）| **分類與能力面不符** | 依條文回報，不自行移組（§1-1）|
| 6 | §4.1 之 Testing／CONVERT 標籤與實測互換 | **下放包預判有誤** | 集合相等，只標籤互換（§4-1）|
| 7 | R-SEC16 未涵蓋 batch 2 之六列 | **條文覆蓋缺口** | 類推指派並逐 TC 於 Remarks 具名，請 Pei 確認（§4-3）|
| 8 | batch2 首輪 `SC=10`／selfcheck 10 處 | 執行層書寫瑕疵 | CAN 通道行未被認（→ §3-2 變更 1）；四列出現型 ER 無觸發 → 補 PENDING 觸發步驟；LOGENC ER 引用跨步驟指令 → 改指本步驟 |
| 9 | `sandbox/batch1/` 之 json 於 v02 產生時被覆寫 | v01 之 json 未保留 | §0 只保護 `security_batch1_v01.xlsx`（未動）；json 為可重生物，且 v01→v02 之差異僅 `Test Set` 一欄 |
| 10 | `PowerModeSts` 無 resume label | 素材缺口 | `<…>` 佔位 ＋ `DBC lookup pending`，不造值（§4-5）|
