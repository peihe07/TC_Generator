# 上繳包 CAM-17 —— R-CAM16(b)／DR-CAM-h 擴題／RDF-12–14／profile §7.8、B02b 29 leaf（產 25 TC）

對應下放包：`docs/fw036/handoff/down/20260923_CAM-17.md`（審閱 `down/20260923_CAM-16_review.md` 同發）。
執行層 session：延用。日期 2026-09-23。

**一句話**：§1 六項落檔完成（取號現查 `{live}` ＝ **19**，與期望值相符；R-CAM16(b) 以同級錨落檔未消耗 live）；
B02b **29 leaf** 產 **25 TC**（`NR1L-RVCHMI-074`～`-098`），
**`AUX Camera Access` 組 60/60 leaf 全數處置**；selfcheck 十項全 0、lint ERROR 類全 0。
**4 列零 TC** 之形制為 R-CAM16 委派款之 B 本類推（**A-CA36**／**RDF-14**，提請裁）。
三項升級條件**皆不成立**。**A 本不動**（十目錄 `git status` 實測 0 改動）；未寫 `delivered/`、未 tag。

---

## §1　落檔

### 1-1　取號現查（R-G23）

落檔前 `rulings_hash.py` 實測 **26 錨點**；全 repo `R-CAM19` **0 命中**（`grep -rn` 於 `*.md`／`*.tsv`／`*.py`）。
**`{live}` ＝ 19**，與下放包 §1 之期望值相符。R-CAM16(b) 為同級增補錨，**不消耗 live**。

### 1-2　R-CAM16(b)

| 錨 | `sha8` | `body_sha8` |
|---|---|---|
| **R-CAM16(b)** | `ed984422` | `dee1de7c` |

零 TC 自此有**三種**形制：委派（R-CAM16）／Heading／**自陳 `N/A`**（R-CAM16(b)）。
**落檔前 26 錨點之 `sha8`／`body_sha8` 逐一比對：改變 0 個**（證據表第 1 項）。

### 1-3　DR-CAM-h 擴題

`features/camera/DATA_REQUESTS.md`（sha256_16 `12d5ae27a0ef6a29`）：
DR-CAM-h 由「Pop Up List 之 camera out-of-position 條目」擴為「**Pop Up List 之 camera 相關缺件**」兩項 ——
(1) out-of-position 兩串零命中；(2) `connecting` 狀態訊息（15 筆含 `connect` 之列逐筆核皆非狀態訊息）。
**原題保留**（與 DR-CAM-g 之擴題同法，DECISIONS 6-60 已立為通則）。
受影響列增列 `SWE1-RVC-080`（`NR1L-RVCHMI-070`）與本批之 `SWE1-RVC-083`（`NR1L-RVCHMI-074`）。

### 1-4　RDF-12／RDF-13／RDF-14

`features/camera/RD_FEEDBACK.md`（sha256_16 `324f6e0768c681c3`），RDF 累計 **14 條**：

- **RDF-12** `SWE1-RVC-069` 來源自陳 `N/A` 而 037 列為 leaf → 請改 `Categorization`
- **RDF-13** `Enable Wireless Cameras` 大小寫三處不一（§27.4.1 全小寫／§27.4.4 句首大寫／§27.4.5 全詞首字大寫；彈窗表同第三種）
- **RDF-14** §34.3／§34.4 之子列與父題皆逐字全等、僅 image token 不同（本包 §2-2 之成因）

### 1-5　profile §7.8（彈窗文字之 ER 形制）

`docs/runtime/profiles/FW036_R1L_Camera_Profile.md`（sha256_16 `d63d61ba8ef0df8f`）新增 **§7.8**：
ER 陳述逐字文字並以 `(PUnnnn)` 附號，**不寫比較句**（`matches` 等為 lint `H`／`W` 之命中詞）；
比對判準**只在 ER**，Procedure 只寫讀取動作。沿革具名 CAM-16 之 B02a 初稿（`H` 4／`W` 3）。

### 1-6　DECISIONS 6-59 ～ 6-61 ＋ `coverage_b.tsv` 註改

`features/camera/DECISIONS.md`（sha256_16 `9558b57ac2325254`）：
**6-59** 零 TC 三形制（立 R-CAM16(b)）；**6-60** DR-CAM-h 擴題並立「沿用既有 DR 而擴題須明文改題並保留原題」之通則；
**6-61** `Enable Wireless Cameras` 取彈窗表拼法。
`coverage_b.tsv` 之 `SWE1-RVC-069` disposition 改為 R-CAM16(b) 之文面
`No TC — source states N/A (A-CA35)`；`b02a_plan.tsv` 之 note 同步。

---

## §2　B02b：`AUX Camera Access` 後 29 leaf

### 2-1　範圍與切點

`SWE1-RVC-083`～`-091`（§27.6.2–§27.8.5，9 leaf）＋ `-129`～`-147`（§34.1.1.1–§34.8.4，20 leaf）＝ **29 leaf**。
§34.8.4 為該組之末列，**切點不落於任何家族內**。29 ≤ 35，**升級條件 §4-1 不成立**。

### 2-2　4 列零 TC（**A-CA36**／**RDF-14**，提請裁）

| 列 | 章 | 事由 | 承接 |
|---|---|---|---|
| `SWE1-RVC-086` | §27.7.3 | 純交叉引用 `For details about wireless cameras connection please refer to ‘Wireless cameras connection’ pages` | §27.5／§27.6 之 TC（`-070`～`-074`）|
| `SWE1-RVC-091` | §27.8.5 | 同上（與 §27.7.3 **非**逐字全等，差一句點）| 同上 |
| `SWE1-RVC-136` | §34.4.2 | 與 §34.3.2 逐字全等**且父題全等**（兩者父題皆 `Accessing Aux Cameras – Backup Cam Only`，只 image token 不同）| `NR1L-RVCHMI-086` |
| `SWE1-RVC-139` | §34.6.1 | 與 §34.4.1 逐字全等 | `NR1L-RVCHMI-087`（章節號較小者承接，沿 R-CAM10(b)）|

**形制之問題（提請裁）**：四列皆為「內容已由他列驗證」之委派款，
惟 **R-CAM16 之文面以 R-CAM10（A 本 SYS-RA 委派）為要件**，不直接及於 B 本之
「逐字同句／純交叉引用」。已按該形制記載於 `coverage_b.tsv`，請裁：擴 R-CAM16 之文面，或另立 B 本之委派條。

**未委派者之理由**：§34.6.2 與 §34.3.2 逐字全等，惟其父題為 `Accessing Aux Cameras – **Backup and
Cargo** Cam`（配置含 Cargo，前提多一個 PROXI 旗標），可分辨 → 出 TC（`-090`）。
§34.8.4 與 §34.6.3 逐字全等，父題分別為 `Accessing AUX – Cameras **Not Connected**` 與
`Accessing Aux Cameras – Backup and Cargo Cam`，情境相反 → 各自出 TC（`-098`／`-091`）。

### 2-3　§27（R1 High）↔ §34（R1 Low）之八群逐字同句

§34 之章標題逐字為 **`R1 Low Wired AUX Cameras`**（`NRL-188152`），
與 §27 之 `R1 High Wired &Wireless Auxiliary Cameras`（`NRL-188027`）成對；
§27.3.1 逐字 `Technical hardware requirements – (R1 High only due to architecture)`。
故八群對位列（`27.2.1.1`↔`34.1.1.1` 等）**各自出 TC**，其前提之 AUX 種類不同
（R1 High 有線＋無線；R1 Low 只有有線）。

**HU 等級無 PROXI 編碼** —— 掃描字串 `Radio_Type`／`Trim_Level`／`Head_Unit_Type`／
`Infotainment_Level`／`R1_Low`／`Uconnect` 於 `forms/proxi/` **六本各 0 命中**，
依下放包 §2 **不以此判車型**，R1 Low 之前提以散文書寫（profile §7.3，三行）。

### 2-4　`Vehicle Model`

| 判 | 列數 | 依據 |
|---|---:|---|
| 五款全 `1` | 18 | R-CAM18(a) |
| 只勾 `HDCC27`／`DT27` | 7 | `-092`（§34.7.1）**來源逐字點名** `DT and HDCC programs` → **R-CAM3**；`-081`／`-085`／`-089`／`-090`／`-091`／`-098` 因 `Digital_CHMSL_Camera_Prsnt`（byte 222 bit 7）於三本 Atl-Mi 零命中 → **R-CAM18(b)** |

**升級條件 §4-3 不成立** —— §34 諸列之配備條件所用之 byte（`Rear_View_Camera`、
`Digital_CHMSL_Camera_Prsnt`、`Surround_View_Camera`）於 PROXI **皆查得**（至少 Atl-Hi 三本）。

### 2-5　hop label（升級條件 §4-2）

所用者：`App Drawer`（§27.2.6.2 逐字＋A 本句式）、`Camera app home page`（§27.2.6.3 逐字）、
`Controls screen`（§27.7.1／§27.7.2 之 `controls page`；進入路徑 → **DR-CAM-g(1)**）、
`Settings`→`Camera`（HMI Settings List row 464）、`More Aux`／`More AUX`（§27.8.1／§27.8.3 逐字）、
`AUX Cam button`／`AUX button`（§34.4.3／§34.8.1 逐字）。
**不可辨者 0 列**，升級條件不成立。本批**未用** status bar shortcut 入口。

### 2-6　PENDING（4 列）

| TC | DR | 事由 |
|---|---|---|
| `NR1L-RVCHMI-074` | **DR-CAM-h** | `connecting` 狀態訊息之逐字文字查無（§27.6.2）|
| `NR1L-RVCHMI-075` | DR-CAM-g | Controls 頁之進入路徑（§27.7.1）|
| `NR1L-RVCHMI-076` | DR-CAM-g／DR-CAM-r | 同上 ＋ `Camera App` 配備旗標（§27.7.2）|
| `NR1L-RVCHMI-092` | DR-CAM-r | `Camera App` 配備旗標（§34.7.1）|

**§34.8.2／§34.8.3 不掛 DR-CAM-h** —— 其畫面色（`blue screen`）與訊息（`“camera system unavailable”`）
**已由來源自載**，依 §4.3.1 直接取其逐字。彈窗表之 `Camera System Unavailable` 命中 6 筆，
module 皆非 `Aux Camera`（`Rearview Camera` ×2、`Interior Camera`、`Turn Signal Activated Blind Spot View`、
`Trailer Surround View Camera` ×2），惟本條不需彈窗表補足。

### 2-7　對帳

| 口徑 | 值 |
|---|---:|
| `b_plan.tsv` 之 `AUX Camera Access` 全列 | 62（leaf 60 ＋ umbrella 2）|
| `coverage_b.tsv` 之該組 | **62** |
| 其中 PRODUCED | 55（B02a 30 ＋ B02b 25）|
| 其中零 TC | 5（`-069` R-CAM16(b)；`-086`／`-091`／`-136`／`-139` 委派款）|
| 其中 umbrella | 2 |
| **判** | **60/60 leaf 全數處置** |

`coverage_b.tsv` 全檔（sha256_16 `1c831649b1308e38`）：PRODUCED 98／零 TC 5／umbrella 9 ＝ **112 列**
（`Activation and Exit` 50 ＋ `AUX Camera Access` 62）。

### 2-8　lint 與 selfcheck

```
selfcheck_camera.py  b02b（TC 25 筆）        十項全 0｜錨 25／反查失敗 0
lint036 --profile camera（b02b_lint.xlsx）   ERROR 類全 0｜非致命 U=7／I-cross=25／X=3｜Z=0／J=0

selfcheck_camera.py  十四目錄（TC 338 筆）    十項全 0｜錨 342／反查失敗 0｜常數錨 2｜合一錨 2
render_tc.py --verify 十四目錄               逐位元相符 338／相異 0
```

---

## §3　R-CAM17 證據表

| # | 主張 | 掃描字串／判準 | 母體 | 命令 | 命中 |
|---|---|---|---|---|---|
| 1 | R-CAM16(b) 未動既有條文指紋 | `sha8`／`body_sha8` 逐錨比對 | `RULINGS.md` 落檔前 **26 錨點** | `rulings_hash.py --target … --out` × 2 ＋ 集合比對 | 既有 26 錨**改變 0**；新增 `R-CAM16(b)` |
| 2 | §27／§34 兩章之逐字同句群 | `Description` 正規化（`_x000D_`→空白、空白摺疊）後分群 | cache 本 `SYS1_HMI_HeadUnitCameraSystems_…` 之 §27／§34 兩章全部列 | openpyxl 全量讀 ＋ `collections.defaultdict` 分群 | **12 群 ≥2 列**（逐群列於 A-CA36）|
| 3 | §34.3.2 vs §34.4.2 連原始格亦全等 | 原始儲存格字串（未正規化）| 同上兩格 | `H[a][1]==H[b][1]` | **全等**（§34.4.1 vs §34.6.1、§34.3.1 vs §34.4.1、§34.6.3 vs §34.8.4、§27.2.1.1 vs §34.1.1.1、§27.2.2.1 vs §34.1.2.1 亦全等）|
| 4 | §27.7.3 vs §27.8.5 **非**全等 | 同上 | 同上兩格 | 同上 | **相異**（差一句點）—— 故非同句重複，而是兩處各自之交叉引用 |
| 5 | HU 等級無 PROXI 編碼 | `Radio_Type`、`Trim_Level`、`Head_Unit_Type`、`Infotainment_Level`、`R1_Low`、`Uconnect`（不分大小寫）| `forms/proxi/` **6 本** | `proxi_have.py` | 六串**六本各 0** |
| 6 | `Camera System Unavailable` 無 AUX module 之條目 | `camera system unavailable`（不分大小寫，比對第 7 欄訊息文字）| `forms/Pop Up List HMI R1 (26PI).xlsx` `Main` 分頁 | openpyxl 逐列 `re.search` | **6 筆**：`PU0169`／`PU0170`（Rearview Camera）、`PU0844`（Interior Camera）、`PU1103`（Turn Signal…）、`PU1240`／`PU1270`（Trailer Surround View Camera）—— **無 `Aux Camera`** |
| 7 | 全 repo 無 `R-CAM19` | `R-CAM19` | `*.md`／`*.tsv`／`*.py` 全 repo | `grep -rn --include=…` | **0** |
| 8 | A 本本輪 0 改動 | — | `features/camera/generated/` 之 A 本十目錄 | `git status --short` | **0 列** |
| 9 | md 與 json 一致 | — | 十四目錄 338 份 md | `render_tc.py --verify` | **相符 338／相異 0** |
| 10 | `AUX Camera Access` 對帳 | `b_plan.tsv` 該組列數 ↔ `coverage_b.tsv` 該組列數 | 兩檔 | csv 計數 | **62 ↔ 62** |

---

## §4　自報錯誤

1. **B02b 初稿之 selfcheck 第 5 項 3 列**（本包內自查自改）——
   `-090` 只勾兩個 Atl-Hi 欄卻寫 `CAN source:` 行（R-CAM3(f) 要求兩 EE 皆勾）；
   `-093`／`-094` 之檔位為**前提**而非動作，無 `Send CAN` 步而寫了該行。三列之 CAN source 行已刪。
2. **B02b 初稿之 lint `R` 18 列**（本包內自查自改）—— R1 Low 之共通前提寫成
   `Only wired AUX cameras are fitted and no wireless AUX feature is offered`（一行兩條件），
   以及 `-097` 之 `AUX 2 is connected and AUX 1 is not connected`。皆已分行。
3. **零 TC 之形制超出既有條文** —— 本批 4 列之委派款不在 R-CAM16 之文面射程內
   （該條以 R-CAM10 為要件）。**未自行擴條**，按該形制記載並登 A-CA36／RDF-14 提請裁（§2-2）。
4. **`coverage_b.tsv` 之 disposition 字串在本輪改過兩次** —— 先由 CAM-16 之
   ``No TC — source states `First surface needs: N/A` (A-CA35)`` 改為 R-CAM16(b) 之文面
   `No TC — source states N/A (A-CA35)`（§1-6），再於 B02b 併入時重生全檔。
   兩次皆為同一輪內，最終值以本包之 sha 為準。
5. **本輪未動 `features/camera/framework.md`** —— 無重開。
   `docs/fw036/RULINGS.sha.tsv` 仍未重生（**GCB-04**，非本線可改）。

---

## §5　未結

| # | 項 | 歸屬 |
|---|---|---|
| 1 | **A-CA36／RDF-14**：B 本委派款零 TC 之條文依據（擴 R-CAM16 或另立條）| 分析層 |
| 2 | **RDF-12**（`SWE1-RVC-069` 不應為 leaf）、**RDF-13**（設定名大小寫）、**RDF-14**（§34.3／§34.4 重複）| RD |
| 3 | B 本餘 **6 組 100 leaf**（`Activation and Exit` 43/43、`AUX Camera Access` 60/60 完結）| 後續批次 |
| 4 | DR 十五條；A 本 f／g／i／j／k／m／p／q 八條阻交付，B 本 g／h／r | Pei |
| 5 | A 本交付（Phase 5–7）待 Pei 之准；候選為 dryrun2（244 列，DECISIONS 6-58）| Pei |
| 6 | GCB-01～GCB-09 九項 | GC 線 |
| 7 | `bench_verify.md` 累計 **45 列**（A 本 11、B01a 3、B01b 8、B02a 11、B02b 12）| Pei 實機 |

---

## §6　落檔清單

```
M  features/camera/RULINGS.md                     R-CAM16(b)           sha256_16 a80ce71970999e40
M  features/camera/RD_FEEDBACK.md                 RDF-12／-13／-14     sha256_16 324f6e0768c681c3
M  features/camera/ANOMALIES.md                   A-CA36               sha256_16 ea5a12450d766a68
M  features/camera/DECISIONS.md                   6-59 ～ 6-61         sha256_16 9558b57ac2325254
M  features/camera/DATA_REQUESTS.md               DR-CAM-h 擴題        sha256_16 12d5ae27a0ef6a29
M  docs/runtime/profiles/FW036_R1L_Camera_Profile.md  §7.8             sha256_16 d63d61ba8ef0df8f
M  features/camera/data/b02a_plan.tsv             note 改 R-CAM16(b)   sha256_16 0b2b51ced6145ac9
A  features/camera/data/b02b_plan.tsv             29 列（產 25）       sha256_16 7f0a548ca6ae64ae
M  features/camera/data/coverage_b.tsv            112 列               sha256_16 1c831649b1308e38
M  features/camera/data/bench_verify.md           B02b 12 列           sha256_16 3b7ef183fc6a06db
A  features/camera/generated/b02b/                25 json ＋ 25 md ＋ INDEX ＋ LINT_REPORT
```

**未動**：`features/camera/framework.md`、`features/camera/delivered/`、
A 本十目錄與 `sandbox/a_dryrun/`、`features/comfort/`（併行 session）。
