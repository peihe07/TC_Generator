# 上繳包 VS-SL-02 —— dry-run v2 ＋ Bed Lowering DT 適用性查證

日期：2026-09-02　層級：Tier 1 → Tier 2
對應下放包：`down/20260902_VS-SL-02.md`（協定：`docs/fw036/handoff/PROTOCOL.md`）
性質：**dry-run。未寫回任何工作簿，未動 `forms/`，未動 `delivered/`，未動 `down/`。**

---

## §1　Bed Lowering DT 適用性查證（8 檔，逐檔）

全文：`features/bed_lowering/reports/bl_dt_applicability.md`（SHA 見 §2）

### 8 檔 SHA 表與判定

| # | 檔 | SHA256 前 16 | 有 | 無 | 未提及 |
|---|---|---|:--:|:--:|:--:|
| 1 | `Sys3_ProSys_…Architecture_Specification_Appendix_BV.xlsx`（`ProjectSystemFeature/SYS3/`） | `41ce5544e1a8c8e7` | | **●** | |
| 1′ | 同名副本（`ProjectSystemDesign/SYS3/Appendix/`） | `637878e1d052fdc7` | | | 同上 |
| 2 | `SYS3_Vehicle_Settings_…SYSAD_v1.0.docx` | `469162b81bf31018` | | | **●** |
| 3 | `forms/SR26 Default Settings and PNet ECU Configuration v1_0.xlsx` | `8f3ae50edd9e8355` | | | **●** |
| 4 | `forms/SR24 R1 Market Configuration Table v1.6.xlsx` | `7e865d557e42c8b0` | | | **●** |
| 5 | `forms/PROXI_HDCC27_R3_20250424.xlsx` | `e7c2020f01c3d58d` | | | **●** |
| 6 | `forms/Logical Identifiers and CAN Mapping v1_78.xlsx` | `a01e1679c706cd45` | | | **●** |
| 7 | `bed_lowering/inputs/` 037 報告 | `8d09ab46e69da3ad` | **●** | | |
| 7′ | `bed_lowering/inputs/` SYS1_HMI 表 | `487354fa935dbc53` | **●** | | |
| 8 | `bed_lowering/output/…_SWQT_BedLowering_20260827.xlsx` | `efa1da4c8f59c98d` | | | **●** |

**計數：有 1／無 1／未提及 6。8 檔全部在磁碟上，無缺檔。**

### 要點（不下結論）

- **檔 1 是總控表 `REF Table` 所載之原始來源，且在磁碟上。** 其
  `FeatureSet list(Gen4_Gen5)` r41（`No` = 36，`Bed Lowering Mode`）之欄位經合併儲存格解出後為：
  **`O` = Atlantis「in DT」**、**`Q` = PNet「in DT」**、**`R` = PNet「in D2/DJ」**。
  `O` 之條件為 `"Body_Types" is ([Type 1] or [Type 4])`（不含 `Type 7`）；
  `Q` 逐字為 `Always false`；`R` 方為 `If "BLM_PRSNT …" is [Set]` 之條件式。
  **兩個架構的 DT 欄都排除 DT。**
- **檔 7 是需求文件**：037 報告 r8–r12 五條 DT 專屬 `shall`（`DT Bed Lowering Entry`／
  前升／後降／協調動作），SYS1_HMI r3 之 use case 亦逐字區分 DT 與 DJ/D2。
- **本層查得之 8 檔中，無任何一檔同時陳述兩者並作出取捨。**
- 依包內停止條件，**本節不開 DR**。

### 包內指令與實際之兩處差異

1. **檔 2 之副本**：包內問「`VF665/` 有同名副本，先比 SHA 是否同檔」——
   **答：同檔**（`VF665/` 與 `VF230_V1_R5/` 皆為 `469162b81bf31018`）。
   全機共 7 份，其中 6 份同 SHA；`Work_Projects/R1L_RTM_V3/…` 之一份
   **SHA 為 `1a4293d96d4a10a4`、bytes 少 3,166**，本層未展開其差異。
2. **檔 8 之「0／1／空之計數」**：T（HDCC27 Atl-Hi）與 U（DT27 Atl-Hi）
   **151 列全空**（空 151、0 為 0、1 為 0）；V–Z 五個車型欄亦同。

---

## §2　v2 產物路徑與 SHA256

| 檔 | bytes | SHA256 |
|---|---:|---|
| `features/bed_lowering/reports/bl_dt_applicability.md` | 14,123 | `75db645d1998c31cf7cf689d64b0ddca041a1e7b0912600564a57f2591028370` |
| `features/vehicle_setting/reports/vf230_settings_dryrun_v2.tsv` | 95,970 | `954c7e76ab74566fc5cc6ebb8782a97cd558f9e36610b661a3080d10d1bc4c81` |
| `features/vehicle_setting/reports/vf230_settings_dryrun_v2.md` | 5,430 | `65c6b0e860520acb05a6644a96393be2a76dd94349f5b8ca9499a115307014a2` |
| `features/bed_lowering/reports/bl_settings_dryrun_v2.tsv` | 37,341 | `f49ec5d0b461ab50230f1790a8b6afad9b13b3851a815fd6bb6be3c59e15cf63` |
| `features/bed_lowering/reports/bl_settings_dryrun_v2.md` | 1,440 | `5486eba405d60961a5373f2e50af4cb28102b2284f3295a104f3772b990239d1` |
| `features/vehicle_category/reports/vc_settings_dryrun_v2.tsv` | 20,944 | `8c21ad11681de0a080a492c2ab90c416b3125e902da4c4e3020b9ab6d4b274ef` |
| `features/vehicle_category/reports/vc_settings_dryrun_v2.md` | 2,177 | `ccc462a4b71158be0c08f7e791a239190997b83f71c8aa018d80345bd01b1c58` |
| `features/vehicle_setting/reports/_v2_branch3.tsv` | 1,761 | `5acfd91469cae1b93538be4520ddcc1997ab576cc9cbd662cb211cafb23b7159` |
| `features/vehicle_setting/reports/_v2_path_split.tsv` | 16,403 | `45cf2dc51592f3a0bf92f90fc67287ab490566034519023ad8edf5ce2fbd8dd3` |
| `features/vehicle_setting/data/settings_alias.tsv`（**7 欄**） | 14,300 | `91ed2d6ed0a75d301b58f05e386ee669740e263732fb8dba3b7c3c0f2ba01a0c` |
| `features/vehicle_setting/scripts/vs_sl02_dryrun_v2.py` | 13,939 | `d8d521d8d1b96ef505df282fcf5cf68f5d62ca0b5493b300ca8c9e16832e3ad8` |
| `features/vehicle_setting/scripts/vs_sl02_selfcheck.py` | 4,531 | `2706006fcd97a283f2de07b789261deb39c9fec5c403d01cd10e9a94cfdb1364` |

v1 之三本報告保留原檔名供對照，未覆寫。
治理文件之改動：`DATA_REQUESTS.md`（**DR-49 退號重擬**，見 §5-1）。

---

## §3　2.1(3) 之列數與清單、2.2 之解鎖列數

### (3)「二來源皆空」= **30 列**（v1 為 207）

包內：「預期遠小於 207；若仍 ≥ 100，停下來報」—— **30，未觸及停止條件。**

D 欄清單：`SWE1-VC-RearGuidanceLightStatus-086/-087/-088`、`-Language-059/-062`、
`-SignatureLighting-025`、`-ForwardCollisionWarningSensitivity-046`、
`-NewSpeedZoneIndication-039/-040/-041/-042`、`-ReadytoDrivePopUp-028/-029/-030`、
`-AutoFoldMirrors-034/-035/-036`、`-DriverEasyExitSeat-040/-041/-042`、
`-EngineOffPowerDelay-046/-048/-049/-050`、`-AutomaticTrailerLight Check-041/-042/-043`、
`-DistanceUnit-021`、`-SpeedUnit-026`、`-ConsumptionUnit-031`。
逐列見 `_v2_branch3.tsv`。

### 四分支分布

| 分支 | 列數 |
|---|---:|
| (1) 形制改寫 | 152 |
| (2) 依总控表／需求原文 | 96 |
| (3) 二來源皆空 → PENDING | **30** |
| (4) 別名未解 → 只掛 `ALIAS_UNRESOLVED` | 179 |

**審閱 §2.1 所指之 55 列與 165 列，v2 皆為 0**（自檢 2、3）。

### 2.2 別名

`settings_alias.tsv` 已併入 `tier2_proposal`／`tier2_evidence`（現 7 欄，107 列）。
**未經 Pei 認可前 `manual` 不綁入查找**，v2 僅以 `exact` 51 名跑。

| Tier 2 提議 | 名數 | 涉及列 |
|---|---:|---:|
| `manual` | 44 | **若認可將解鎖 176 列**（該 176 列現皆無 `path_proposed`：40 列在 (1) 分支、136 列在 (4)） |
| `DR` | 9 | 37 |
| `drop` | 1 | 19 |
| 合計 | 54 | 232 |

### 2.3 PATH_ABSENT 分流 —— **結論與包內預期不同**

**92 列全部「需路徑」，行為型 0 列。**
92 列之 procedure 末步一律為
`Read the Vehicle Settings menu and check that the <X> setting is displayed as <Y>`（92／92），
該步須導覽至該設定項方能執行。逐列清單見 `_v2_path_split.tsv`。

### 2.4 Options 正規化

套用後 `"Off" / "Only Warning" / "Warning + Active Braking"`，
**與下放包 §4 之參考輸出逐字一致**（v1 之空格差已消除）。

### 2.5 BL／VC

- **BL**：151 列提議 `PROXI CAN node 27 (ASM/ASCM) = 1 (Present)`；`Body_Types` 行 `PENDING`。
  `DT_APPLICABILITY` **23 列**（DT 專屬 7 ＋「DT 或 DJ/D2」16）；
  DJ/D2 專屬之 **8 列**另掛 `DJD2_ONLY`，**不掛** `DT_APPLICABILITY`。
- **VC**：`Camera App` 2 列（r23／r24）判為 **`Cam App`，非 `Backup Cam`** ——
  原文逐字寫 `the Camera App` 並指 `see Camera HMI Logic and Flow`，
  `Backup Cam`／`Rear_View_Camera` 在該 2 列原文中未出現。
  **惟总控表 No.196 `Cam App` 為 `Always false`，與「Cameras will appear as a tab」相衝，故仍列 DR。**
  Specialty／螢幕方向等 **16 列**改掛 `PROSE_KEPT`，`NO_MAPPING` 由 53 降至 43。

---

## §4　自檢四項

| # | 項 | 結果 |
|---|---|---|
| 1 | 三本報告列數 457／151／126 | **PASS**（工作簿實測與報告逐本相符） |
| 2 | assert `PROXI_PENDING` ∩ `ALIAS_UNRESOLVED` = 0 | **PASS**（交集 0 列） |
| 3 | assert `proxi_now` 非空而提議 PENDING 之列 = 0 | **PASS**（違反 0 列） |
| 4 | 抽 3 條貼 proposed 全文（含 `-034`／`-035`） | **PASS**，見下 |

`ForwardCollisionWarning-035`（r151，分支 (1)）：

```
Pre-Condition
1. The HU is in the Full-Operation state
2. PROXI Forward_Collision_Mitigation = 3 (Full Speed Forward Collision Warning with Mitigation)

Procedure
1. Power cycle the HU
2. Press "Settings" on Menu Bar
3. Select "Safety & Driving Assistance"
4. Select "Automatic Emergency Braking"
5. Check that "Forward Collision Warning" is displayed with 3 radio buttons "Off" / "Only Warning" / "Warning + Active Braking"

Expected Result
1. The HU completes start-up
2. The Settings screen is displayed
3. The "Safety & Driving Assistance" page is displayed
4. The "Automatic Emergency Braking" page is displayed
5. "Forward Collision Warning" is displayed as 3 radio buttons "Off" / "Only Warning" / "Warning + Active Braking"
```

`ForwardCollisionWarning-034`（r150，負向，分支 (1)）：`Pre` 為
`2. PROXI Forward_Collision_Mitigation = 0 (Absent)`，末步 `Check that "…" is not displayed`。
第三條 r14（`SWITCH5PowerMode-023`，分支 (4)）：別名未解，只出 Pre 第 1 行與前二步，**不出 PENDING**。

---

## §5　自報

1. **`DR-49` 退號**　依審閱 §2.2，原以 54 名為標的之 `DR-49` 判為過寬。
   `DATA_REQUESTS.md` 之該節已改為**未取號之 DR 草稿**，標的縮為 9 名（涉 37 列），
   併同 VC 之二問。**`DR-49` 之號本輪釋出，不佔用。**

2. **執行層自行補正之抽取式（非包內所令）**　v1 只認 `"X" customer setting`，
   漏掉 **95 列**寫成不加引號之 `… check that the X setting is displayed …`。
   v2 加 fallback 後 `setting` 欄為空之列由 95 → **0**，`ALIAS_UNRESOLVED` 由 227 → **179**，
   `path_proposed` 由 66 → **79**。這 95 列所指之 87 個名**全部已在既有 107 名母體內**，
   **母體與別名表列數不變**。此為 v1 之漏，本層自報。

3. **本輪落檔前更正之誤**　`DT_APPLICABILITY` 初版誤把 DJ/D2 專屬之 8 列一併掛旗（31 列），
   與包內 §2.5 所令之「7 ＋ 16」不符。已改為 23 列，8 列另掛 `DJD2_ONLY`。

4. **`gate_all.py` 無增紅**　紅者為 `canon_refs`（503）／`rulings_hash`／`gates_tsv`／
   `lint_paths`（4），**四支與 VS-SL-01 上繳時逐項同數**，皆為本輪之前既有。
   `rulings_hash` 本層仍**未重跑**（重生指紋屬治理動作，且會與既有紅混在一起）。

5. **待 Pei 裁之事項未自裁**　審閱 §4 之 9 項本層皆未代決，其中
   **#1 編號命名空間**（`R-VS84`–`R-VS88` vs `R-VF143`–`R-VF147`）之現況仍為 `R-VS84`–`R-VS88`
   ——Tier 2 建議改 `R-VF`，**惟該建議尚非 Pei 之裁定，本層不自行搬號**。
   anomaly `A-VS166`–`A-VS168` 同。
