# 上繳包 GC-04 —— GC-03 審閱之落實

日期：2026-09-05　層級：Tier 1 → Tier 2
對應下放包：`down/20260905_GC-04.md`（協定：`docs/fw036/handoff/PROTOCOL.md`）

六件任務全數執行。**`docs/fw036/RULINGS.sha.tsv` 一位元未動**（R-G50(c)：乾跑一律 `--out <工作區>`）。
無刪除、無搬移。

> **兩件請先看**：
> 1. **R-G43(b) 只補了一半**（3 節）。台帳納入掃描面後只多 **16 列**，而
>    **R-G30～R-G41 仍然 0 列** —— 成因不是掃描面，是**錨點形態**：那十二條掛在
>    `## 合併包 19 之全域條文（R-G26–R-G31…）` 這種**條號在括號內**之標題下，
>    而錨點正則要求條號**領頭**。R-G43(b) 之條文所寫「不入掃描面則 R-G12～R-G42 之台帳側
>    …皆永無指紋」為真，但**入了也未必有** —— 這是另一個獨立缺口。
>    R-G43(b) 確實修好的是 **`R-G19`**（台帳 L358 保號條，現有指紋）與 `R-G16`。
> 2. **七列 grep 全部 0 命中，但其中三列之分母近乎 0**（4 節）。
>    comfort 之工作簿引用之 PU id 總數為 **0/1340** —— 該判準對它是全盲的，
>    0 命中不得讀為無影響。逐列分母已附。

---

## §1　產物與 SHA256

| # | 檔 | bytes | SHA256 |
|---|---|---:|---|
| 1 | `docs/fw036/RULINGS_LEDGER.md` | 85,506 | `9ee5d9dcc40de21d3b99462dc534dc57785e9ef22c14f115becef737f7a3cbdf` |
| 1 | `docs/fw036/ANOMALIES.md` | 3,704 | `98d1c75aad086d5e678cd2ffd01f9497491be87d198e46c884697cb29f50842f` |
| 2 | `scripts/rulings_hash.py` | 14,914 | `480c3f328648514e09efb14d429e1c1d2b4410a78c4c5923da9a7a151652627c` |
| 3 | `docs/reports/binding_hits_20260905.tsv` | 45,177 | `3271f08029afbd52cf933816f9a30d257d87e400f20c4548163a5442a1f1e660` |
| 4 | `features/comfort/DECISIONS.md` | 13,624 | `2156f7021c4b90ae7198b060c584cdaf4099f04f251a125db7b482f45d95d9bf` |
| 4 | `features/time_management/DECISIONS.md` | 7,101 | `f955d016ce7b8bc62993be0f93794120b525c9bb54c5a71f0c8a308d0c49754a` |
| 4 | `features/user_profiles/DECISIONS.md` | 30,960 | `857e3f50d46d8cd90e5c55154dd4a7ed69527e3b00370f4fdf512043329b2a73` |
| 4 | `features/vehicle_category/DECISIONS.md` | 8,961 | `d5c75e7d9cb693d8f3876053210cfa11bf168881aac8b03b489854454071813c` |
| 4 | `features/vehicle_setting/DECISIONS.md` | 6,250 | `c92669dd665ea4737eafa60aab2589937a3d55fd656a29e9b1c34206a4347c82` |
| 5 | `scripts/measure_common.py`（新） | 1,244 | `b601f87d31c1585b6f3121d37e39d3a09e51712696aac084a73ff69199a26dd8` |
| 5 | `scripts/canon_refs.py` | 24,331 | `3d3493b6c100f7f9f2e6a6bcfec6966f5e42be768d37a6052651926489cfaf35` |
| 5 | `tests/test_measure_common.py`（新） | 2,364 | `058d82ae86f3294f9c411fc035bf61aea8c8b41d4373e1a126caeaea7fbe38f6` |

`git diff --numstat`：台帳 `52 2`、ANOMALIES `7 1`、五本 DECISIONS `7 4`／`8 5`／`5 2`／`5 2`／`8 5`、
`canon_refs.py` `8 0`、`rulings_hash.py` `4 1`。

### 1-1　R-G50(c)：本輪工具寫入 repo 內之路徑清單

| 工具 | 旗標 | 寫入 repo 內之路徑 |
|---|---|---|
| `scripts/rulings_hash.py` | `--out /private/tmp/…/scratchpad/rg_dry_20260905.tsv` | **無**（輸出落工作區，217,557 bytes）|
| `scripts/canon_refs.py` | `--waiver`（唯讀）| **無** |
| `scripts/gate_all.py` | 無 | **無** |

**清單為空。** `docs/fw036/RULINGS.sha.tsv` 之 `git status` 為空，逐次複核。

---

## §2　任務 1 —— 條文與自報入台帳

新節 `## 下放包 GC-03 審閱之全域條文（Pei 2026-09-05「追認」「准」）`，
含 **R-G43(b)**（審閱 §二-3 圍籬）、**R-G50(c)**、**R-G16 補充**（審閱 §三 條目），
皆以腳本自審閱檔抽取（R-G36）並附來源行號。

**R-G43 理由段已更正**：原二句以 `~~ ~~` 逐字保留，其後加更正段 ——
指紋源為 FO ＋ `features/*/RULINGS.md`、台帳 0 列、同一事實解釋 R-G30–R-G41 之「缺列」非漏跑、
裁定 (A) 維持之餘一腳理由、掃描面缺口由 R-G43(b) 補。

`docs/fw036/ANOMALIES.md` 追加 **A-GC12**；末段之共同形態句由「十一筆」改「十二筆」，
並補記三條新防再犯條文，及「A-GC10 與 A-GC12 為兩層同時犯」一句。

---

## §3　任務 2 —— R-G43(b)：台帳納入掃描面

### 3-1　先量（§二-2-1）

`default_targets`（`scripts/rulings_hash.py:247-259`）改前逐字：

```text
    if w_p1_only:
        return list(SCOPE_W_P1)
    targets = ["docs/fw036/FEATURE_ONBOARDING.md"]
    targets += sorted(
        str(p.relative_to(root)) for p in (root / "features").glob("*/RULINGS.md")
    )
    return targets
```

旗標語意（讀 `main()`，非推）：`--out PATH` → `out_path = root / args.out`，
**寫**該路徑（`args.out` 為絕對路徑時 `root /` 不生效，故落工作區）；
預設 `OUT_DEFAULT = "docs/fw036/RULINGS.sha.tsv"`。
`--check` → 只比對 `out_path` 之內容，**不寫**；不符即 exit 1。
**即：無旗標＝寫 repo 內之指紋表**，此為 `up/20260905_GC-03.md` 9-2 節之違規成因。

### 3-2　改（§二-2-2）

`default_targets` 之 `targets` 初值增 `"docs/fw036/RULINGS_LEDGER.md"`，並附三行成因註。
**指紋演算法與輸出格式未動**（`+4 / -1`，只動該串列與註）。

### 3-3　乾跑（§二-2-3）與 3-4 回報

`python3 scripts/rulings_hash.py --out <工作區>/rg_dry_20260905.tsv`

| 量 | 現行 `RULINGS.sha.tsv` | 乾跑 | 差 |
|---|---:|---:|---:|
| 資料列 | 728 | **746** | +18 |
| 相異 ID | 697 | **714** | +17 |
| 台帳所出之列 | **0** | **16** | +16 |

**新增 ID 34**：

- `R-G43`、`R-G43(b)`、`R-G44`、`R-G45`、`R-G46`、`R-G47`、`R-G48`、`R-G48(b)`、
  `R-G49`、`R-G50`、`R-G50(b)`、`R-G50(c)`（GC-01～GC-04 寫入台帳之新條，`### R-Gnn` 錨點）
- `R-G51`～`R-G68`、`R-G62′`（GC-03 改號後之 FO 側）
- `R-G13+補充`（台帳之「R-G13 補充」自成一個 ID，**不與 R-G13 撞號**）
- `R-VL29`、`R-VT27`（併行 vsm 線，非本包）

**消失 ID 17**：`R-G12`、`R-G13`、`R-G14`、`R-G15`、`R-G17`、`R-G18`、`R-G20`～`R-G29`、`R-G23′`
（FO 側改號所致）。

**`R-G16` 與 `R-G19` 未消失** —— 二者改由**台帳**供給
（`RULINGS_LEDGER.md:1570` 與 `:352`）。**這正是 R-G43(b) 修好的東西**：
`up/20260905_GC-03.md` 9-3 節所具名之「`R-G19` 重生後將無任何指紋列」，
在 R-G43(b) 生效後**不再成立**。

### 3-5　**R-G30～R-G41 仍為 0 列 —— R-G43(b) 未能覆蓋**

逐號實測：乾跑表內 `R-G30`～`R-G42` 之中，**只有 `R-G42` 有列，且來源為 FO**
（`FEATURE_ONBOARDING.md:1231`）；`R-G30`～`R-G41` **十二號一列皆無**。

成因（讀 `RE_ANCHOR`／`RE_GROUP`，非推）：錨點要求條號**領頭**於標題
（`## R-G19 — …`、`### R-G44 —— …`）。而該十二條掛在**條號在括號內**之敘述式標題下：

```text
## 合併包 19 之全域條文（R-G26–R-G31，2026-08-25）
## test_item 上半之未涵蓋面向須明寫（R-G33，2026-08-25，全域）
## 下放包 25 之全域條文（R-G34／R-G33(d)，2026-08-25）
## 下放包 26 之全域條文（R-G35／R-G36，2026-08-25）
## 下放包 30 之全域條文（R-G37，2026-08-26）
## 下放包 31 之全域條文（R-G38，2026-08-26）
## 下放包 33 之全域條文（R-G39，2026-08-26）
## 列序為交付屬性（R-G40，Pei 2026-08-27，全域）
## 掃描起點須先被盤點（R-G41，Pei 2026-08-30「准」，全域）
```

**R-G43(b) 之條文所寫為真但不充分**：「不入掃描面則永無指紋」成立，
「入了就有指紋」不成立。要補齊 R-G30～R-G41，須**另改該九個標題為條號領頭**
（動的是台帳條文之標題，非本體）**或**放寬錨點正則。
二者皆非本包授權，**列為請裁**（9-4 節第 1 項）。

### 3-6　撞號（§二-2-4，**未合併、未擇一**）

乾跑後同一 ID ≥2 列者 **28 個**，本體全部不同。逐項歸類：

| 類 | ID 數 | 說明 |
|---|---:|---|
| **台帳納入所新增** | **1** | `R-G45`：`RULINGS_LEDGER.md:1317`（主條，共用參考檔之現行版）vs `:1457`（`R-G45 補充 —— DBC／LID 網段配對`，標題以 `R-G45 補充` 起首而被讀為同號）|
| 既有（feature 內同號異文）| 26 | `R-C1`／`R-C4`(3)／`R-C5`／`R-C6`／`R-C11`／`R-C12`／`R-C18`／`R-C27`／`R-C42`／`R-DD6`／`R-DD20`(3)／`R-DD26`／`R-ICS2`／`R-ICS19`／`R-ICS22`／`R-ICS23`／`R-ICS25`／`R-ICS33`／`R-ICS35`／`R-PMH13`／`R-TM9`(3)／`R-TM10`／`R-TM40`／`R-TM41`／`R-VF70` |
| 既有（跨檔）| 1 | `R-G4`（FO:773 條文、FO:1251 group、`user_profiles/RULINGS.md:474`）、`R-G7`（FO:795、`user_profiles:623`）—— 二者現行表內即已如此 |

**下放包所預期之三組撞號皆未出現**，逐項具名：

| 預期 | 實測 |
|---|---|
| `R-G13`／`R-G13 補充` | **未撞** —— 後者被讀為獨立 ID `R-G13+補充` |
| `R-G33`／`R-G33(d)` | **未撞** —— 二者皆在括號式標題下，**根本沒進表**（3-5 節）|
| FO 之 `R-G42` 摘要 vs 台帳 `R-G42` 全文 | **未撞** —— 台帳側之 R-G42 亦在括號式標題下，未進表；`R-G42` 只有 FO 一列 |

**本體 sha 與 FO 側同號條是否相等**（§二-2-4 末項）：**無可比對之對象**。
台帳所出之 16 列中，其 ID 與 FO 側同號者為 **0** ——
`R-G16`／`R-G19` 之 FO 側同號條已改號為 `R-G55`／`R-G58`，`R-G43`～`R-G50(c)` FO 側本無。
故本項回報為「母體為空」，非「相等」。

**撞號未在乾跑中合併或擇一。** `R-G45` 一組交分析層裁；26＋1 組既有者非本包所生。

### 3-7　對照向（§二-2-5）

| 向 | `tests/test_rulings_hash.py` | 全套 |
|---|---|---|
| 改後 | 34 passed | `10 failed, 1306 passed, 15 skipped` |
| **對照向（`scripts/rulings_hash.py` 還原）** | 34 passed | `10 failed, 1294 passed, 15 skipped` |

逐項同名，紅 **+0**。（1306 − 1294 = 12 為 5 節之新測試。）

---

## §4　任務 3 —— 七列之 grep 命中

輸出：`docs/reports/binding_hits_20260905.tsv`（141 列）。
母體＝各 feature 現存最新工作簿（mtime 取最新、排除 `sandbox/backup/`）：

| feature | 工作簿 | sha8 |
|---|---|---|
| comfort | `output/…_Comfort_20260817_rowsort.xlsx` | `235f2ca7` |
| time_management | `output/…_SWQT_20260825.xlsx` | `717d48d8` |
| user_profiles | `output/…_UserProfiles_20260820_itemgap.xlsx` | `cbd04af6` |
| vehicle_setting | `output/…_VF230_20260902.xlsx` | `aed08b4d` |

（查詢式逐列寫在 tsv 之 `query` 欄，內含同一組 sha8。）

### 4-1　命中與**分母**（R-G8／R-G50／R-G11）

| # | feature | 家族 | 變動 key | 命中 | **該簿引用之同類 key 總數（分母）** | 讀法 |
|---|---|---|---:|---:|---|---|
| 3 | comfort | Pop Up List | 2 | **0** | **0 / 1340** | **本判準對該簿全盲**，0 命中無證據力 |
| 8 | user_profiles | Pop Up List | 2 | **0** | 17 / 1340 | 變動之 2 id 不在該 17 內，**有證據力** |
| 4 | time_management | HMI Settings List | 116 | **0** | **9 / 316** | 分母極小，證據力弱 |
| 6 | time_management | LID | 4 | **0** | **1 / 2548** | 幾近全盲 |
| 13 | vehicle_setting | LID | 4 | **0** | 18 / 2548 | 變動之 4 個不在該 18 內，**有證據力** |

**若不附分母，這五列會被讀成「全部無影響」。** 實際上 #3 與 #6 之 0 命中什麼都沒證明。

### 4-2　#10／#11 DBC 四象限（**只量不判**）

`vehicle_setting` 最新工作簿之 `$MESSAGE.Signal$` token 去重 = **48 個**。

| # | inputs vs forms | 兩邊皆有 | 只在 forms R1 | 只在 inputs R4/R5 | 兩邊皆無 |
|---|---|---:|---:|---:|---:|
| 10 | `R4_BHCAN` vs `R1_BHCAN2` | **0** | **0** | **1** | **47** |
| 11 | `R5_FDCAN8` vs `R1_FDCAN8` | **48** | 0 | 0 | 0 |

- #10 之「只在 R4」一個為 `IPC_VEHICLE_SETUP2.Power_Tailgate_Enable`；
  其餘 47 個兩份 BHCAN 皆無 —— 即**本簿所用之訊號幾乎不落在 BHCAN**，其落點是 FDCAN8。
- #11：**48 個全部在 R1 與 R5 兩邊皆有**。就本簿實際使用之訊號而言，
  **R1 與 R5 之 FDCAN8 無差異**；R5 多出之 122 個 `SG_` 一個都沒被用到。

**本包不判何版為 R1L 現行**（Pei 裁）。惟上表可直接支撐一件事：
若只看本簿之使用面，選 R1 或 R5 對 FDCAN8 不生影響；BHCAN 之 1 個 token 是唯一分歧點。

---

## §5　任務 4、5

**4　13 判抄回**　五本 `DECISIONS.md` 之版本綁定表右欄，
原 `PENDING 分析層判` 依 R-TM13 以 `~~ ~~` 保留，其後書判定。
六列「不影響（key 級）」各附審閱所載盲區句；四列待證改寫為 GC-04 之命中與分母；
#10／#11 附四象限；#4 保留「出貨前必重跑」。表上另加一段前言，載明前提
（`delivered/` 為空、以現存最新工作簿為對象）與來源。

**5　`SELF_OUT` 常數化**　新增 `scripts/measure_common.py`：

```python
MEASUREMENT_OUTPUT_GLOBS = (
    "docs/reports/*_20[0-9][0-9][0-9][0-9][0-9][0-9].tsv",
    "docs/reports/binding_diff_*.tsv",
    "docs/reports/binding_hits_*.tsv",
)
def is_measurement_output(rel) -> bool: ...
```

**使用處清單**：

| 處 | 形態 |
|---|---|
| `scripts/canon_refs.py::in_scan_surface` | 首行即排除，**行為變動 0**（該檔本就只掃 `.md/.py/.yaml/.yml`，`.tsv` 從未進母體；此處為把規則寫死，不是修 bug）|
| `tests/test_measure_common.py` | 12 個測試：五個已知致錯檔名字面釘入（G-N）、五個範圍向（R-G9，證排除集不過寬）、一個證 `canon_refs` 走同一支而非自帶副本、一個證 glob 只錨在 `docs/reports/` |
| 本輪與歷輪之工作區量測腳本 | 皆未入版控；自本包起改 import 此常數 |

`scripts/whitespace_matrix.py` 亦提及 `docs/reports`，惟其為**輸出端**（寫報告），非母體端，未改。

**逾越下放包之處，具名**：包內只要求常數與使用處清單，**測試是本層加的**。
理由：這條規則已犯兩次，寫成常數而無機檢，第三次仍會犯。若分析層認為逾越，可刪該檔。

---

## §6　自檢

| # | 項 | 結果 |
|---|---|---|
| 1 | `RULINGS.sha.tsv` 未寫（§二-2-6）| **PASS** —— `git status` 該檔為空，逐次複核 |
| 2 | R-G50(c)：只用 `--out <工作區>`／`--check` | **PASS** —— 寫入 repo 之路徑清單為**空**（1-1 節）|
| 3 | 無刪除、無搬移 | **PASS** —— 無 `D`／`R` 列 |
| 4 | 撞號未合併、未擇一 | **PASS** —— 28 組逐項列表，一組未動 |
| 5 | 指紋演算法／輸出格式未動 | **PASS** —— `rulings_hash.py` `+4 / -1`，只動 `targets` 串列與註 |
| 6 | pytest 增紅 | **+0**（3-7 節，逐項同名）|
| 7 | `ruff` 增紅 | **+0** —— `canon_refs.py` 5→5、`rulings_hash.py` 2→2、二新檔 `All checks passed` |
| 8 | 全稱斷言附查詢式與命中數（R-G50）| **PASS** —— 3-5／3-6／4-1／4-2 節皆附；0 命中一律附分母 |
| 9 | 對照向回報被移出檔數（R-G50(b)）| **PASS** —— gate 12 檔（9 已追蹤 ＋ 3 新檔）、pytest 同 |
| 10 | 量測產出未入母體（R-G16 補充）| **PASS** —— 本輪掃描皆走 `is_measurement_output` |
| 11 | 母體變動之外部歸因 | **具名** —— `docs/fw036/lint_reports/` 於本輪作業期間由 **106 檔變 23 檔**，係 Pei 於 2026-09-05 16:24:52 執行 `scripts/cleanup_gc02.sh` 並提交（`d362a8d`：移除 81、搬 2 至 `features/sw_update/reports/`）。**非本包所為**；本包之對照向與交付向皆在該提交之後量測，故 7-1 節之 728／737 立於同一基準 |

---

## §7　自報

### 7-1　`gate_all`：`canon_refs` 728 → 737（+9）

| 向 | `canon_refs` |
|---|---:|
| 對照向（本層 GC-04 產物全移出／還原，移出 12 檔）| **728** |
| 交付狀態（不含本上繳包）| **737** |
| 交付狀態（含本上繳包）| **751** |

逐型（對照向 → 交付狀態）：`section` 63／748 → 63／748（**+0／+0**）；
`item` 0／40 → 0／40（+0）；`ruling` unresolved 791 → 804。
waiver 外：unresolved 588 → 597（+9）、ambiguous 140 → 140（+0）。

**全為 `ruling` 型 unresolved**，成因單一：台帳新節與五本 DECISIONS 指名
`R-G43(b)`／`R-G50(b)`／`R-G50(c)`／`R-G16 補充` 等尚無指紋之號。
**指紋一重生即消**——而本包之乾跑已證那些號屆時都會進表（3-4 節）。

`rulings_hash` 維持紅（改號後未重生，Pei 作業）；`gates_tsv`／`lint_paths` 對照向即紅，+0。

### 7-2　本輪無自造缺陷之錯值進交付物

一處中途錯誤已在交付前發現並改正，具名：
`tests/test_measure_common.py::test_canon_refs_uses_the_shared_exclusion` 首版以
`spec_from_file_location` 載入 `canon_refs` 而未先塞 `sys.modules`，
dataclass 解析註記時拋 `AttributeError`。修法同 `tests/test_canon_refs.py` 之既有作法。
**該錯只出現在測試自身，未影響任何量測值。**

另：`scripts/canon_refs.py` 之 `from measure_common import …` 首版未補 `sys.path`，
使 `tests/test_canon_refs.py` 在收集階段即 ImportError（該測試以檔案路徑載入本模組）。
已於 import 前補 `sys.path.insert(0, <本檔所在目錄>)` 並註明理由。

### 7-3　未做且具名

| 項 | 為何未做 |
|---|---|
| `RULINGS.sha.tsv` 重生 | §二-2-6／§三-1，屬 Pei；且撞號處置未裁 |
| `R-G45` 撞號之處置 | §二-2-4 明文「不得在乾跑中合併或擇一」 |
| R-G30～R-G41 之錨點改造 | 3-5 節；動台帳條文標題或錨點正則，皆非本包授權 |
| vehicle_setting DBC 版本之判 | §一 明文「只量不判」，Pei 裁 |
| `rg43_manual` 130 處改號 | GC-04 §二 為分析層任務，GC-05 執行層才改 |
| `cleanup_gc02.sh`、`_intake/` 清空 | GC-03 審閱 §六，Pei 動作 |

### 7-4　請裁四項

1. **R-G30～R-G41 之指紋缺口**（3-5 節）：改台帳九個標題為條號領頭、或放寬錨點正則，或維持無指紋。
2. **`R-G45` 撞號**（3-6 節）：主條與「R-G45 補充」同號；補充條是否另編號（如 `R-G45(b)`，與
   `R-G48(b)`／`R-G50(b)` 之既有形式一致）。
3. **R-G43(b) 條文之措辭**（3-5 節）：其「不入掃描面則永無指紋」為真，惟易被讀成「入了就有」。
4. **`tests/test_measure_common.py` 是否逾越**（5 節末）：包內只要求常數與清單。
