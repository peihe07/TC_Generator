# 上繳包 CAM-19 —— profile §5.1／§5 註、`popup_texts` note、DECISIONS 6-65～6-66、B04a 30 TC

對應下放包：`docs/fw036/handoff/down/20260923_CAM-19.md`（審閱 `down/20260923_CAM-18_review.md` 同發）。
執行層 session：延用。日期 2026-09-23。

**一句話**：§1 三項落檔完成；`AUX Camera Settings` leaf 實測 **45**（> 40）故切兩批，
**B04a 取 §29–§31（R1 High）30 leaf**，產 **30 TC**（`NR1L-RVCHMI-128`～`-157`），
selfcheck 十項全 0、lint ERROR 類全 0。**零 TC 0 列**。
三項升級條件**皆不成立**（其中 §4-3 之對照表為**空集**，見 §2-4）。
**A 本不動**（十目錄 `git status` 實測 0 改動）；未寫 `delivered/`、未 tag。

---

## §1　落檔

### 1-1　profile §5.1 之註 ＋ §5 之摘句註

`docs/runtime/profiles/FW036_R1L_Camera_Profile.md`（sha256_16 `cfca00584998f774`）：

- **§5.1 加註**：`J` 豁免之要件為「上半**自來源 `Description` 之首 token 起**」；
  摘句若自句中某個小寫 token 起首，該小寫只是句中形態，**不在豁免射程**，
  應往前保留至一個大寫 token（仍為保序子序列）。沿革具名 CAM-18 之 `-121`／`-122`。
- **§5 加註**：摘句留孤懸標點（`,`／`and` 列舉中只留一項）且**全文 ≤ 50 token** 者，
  上半**取全文**，各列所驗之項由下半括號句指明。沿革具名 CAM-18 之 §28.7.2 三列（15 token）。

### 1-2　`popup_texts.tsv` 之 `PU1518`／`PU1519` note

`features/camera/data/popup_texts.tsv`（sha256_16 `a1c7c763b5702117`）新增一列，
記兩者之**觸發相異**（`PU1518` ＝ 啟用設定／按無線相機鍵觀看；`PU1519` ＝ **新增**無線相機）
與**文字相異**（`…and enable Wireless Cameras?` vs `…and add a wireless camera?`），
座標 `Main!r1301`／`r1302`。

### 1-3　DECISIONS 6-65 ～ 6-66

`features/camera/DECISIONS.md`（sha256_16 `2ff7546cfc66829c`）：
**6-65** `J` 豁免之要件與摘句留孤懸標點之處置（兩項皆入 profile）；
**6-66** 下放包之列數一律引 `b_plan.tsv` 之 **leaf** 數；執行層每批仍須實測並回報，
不符時**依實測執行並回報**（不自行擴批）。併記 `PU1518`／`PU1519` 之分辨。

---

## §2　B04a：`AUX Camera Settings` 之 §29–§31

### 2-1　leaf 實測與切法

| 口徑 | 值 |
|---|---:|
| `AUX Camera Settings` 全列（`b_plan.tsv`）| 53 |
| **leaf** | **45** |
| umbrella | 8 |

45 > 40，依下放包 §2 切兩批。**切點取 §31.1.6 ↔ §34.9.1 之章界**，即
**§29–§31（`R1 High`，30 leaf）** 對 **§34.9–§34.10（`R1 Low`，15 leaf）**。

**不取「第 40 列」之機械切點**，其由有二：
1. 該處為 `R1 High` ↔ `R1 Low` 之**章界**，與 **DECISIONS 6-64**（兩章之前提相異、各自出 TC）同軸；
2. 第 40 列為 `SWE1-RVC-154`（§34.9.7），**落在 §34.9 之名稱編輯流程中段**
   （§34.9.4 `Edit Favorite` → §34.9.5 字數 → §34.9.6 鍵盤 → §34.9.7 標題更新 → §34.9.8／§34.9.9 各處反映），
   切之會拆散該流程。

30 ≤ 43，**升級條件 §4-1 不成立**。

**跨批之委派風險已排除**：§29／§30／§31 與 §34 兩章之逐字同句群實測 —— 跨章者**僅 1 群**
（`29.1.3`／`34.9.3`，皆 `Note: Wired AUX cameras Cannot be deleted`），
而**該兩列皆非 leaf**（不在 45 之內）。故 B04a／B04b 之間無 R-CAM16(c) 之委派關係。

### 2-2　產物

`features/camera/generated/b04a/` —— 30 份 json ＋ 30 份 md ＋ `INDEX.md` ＋ `LINT_REPORT.md`。
TC ID `NR1L-RVCHMI-128`～`-157`；Test Set 全 30 列 `AUX Camera Settings`；
計畫 `features/camera/data/b04a_plan.tsv`（30 列，sha256_16 `14f94ddd9ea8833a`）。

**零 TC 0 列** —— 30 列皆有獨有驗證點。近似而不委派之三組，其判準逐項如下：

| 組 | 判 | 依據 |
|---|---|---|
| §29.1.1 ↔ §29.2.1 | **各自出 TC**（`-128`／`-133`）| Description 僅 image token 不同（正規化後仍相異）；父題為 `Wired AUX Cam Settings` vs `Wireless AUX Cam Settings` —— **R-CAM16(c)** 父題相異者不適用 |
| §29.1.2 第四項 ↔ §29.2.3 全文 | **各自出 TC**（`-132`／`-138`）| R-CAM16(c)(i) 要求 Description **全文**逐字全等；此處只是**其中一個子項**與另一列之全文相同 |
| §31.1.3 ↔ §34.9.7、§31.1.6 ↔ §34.9.5 | **各自出 TC**（後者屬 B04b）| 逐字分群實測**不同群**（31 vs 31 token、41 vs 37 token），非全等 |

### 2-3　設定路徑

`ENTER_CAMERA_SETTINGS`（**profile §3.0**，三 hop）＋ `Select "Aux Cameras"`
（`forms/HMI Settings List R1 SR25 Post R1L-R (Feb 13 2026).xlsx` `Settings` 分頁
**row 474** `10. Aux Cameras`／**row 475** `10.1 Aux Cameras`）。

### 2-4　`Aux Cameras` 子項 ↔ SYS1 設定名之對照（升級條件 §4-3）

**該表於 `Aux Cameras` 之下無任何子項** —— 實測 `Settings` 分頁：

```
row 474  10     Aux Cameras                    CFTS092/VF617
row 475  10.1   Aux Cameras                    See Head Unit Camera Systems Logic & Flow
row 476  11     Trailer Reverse Guidance       On/Off Checkbox   VF560
```

即 row 475 之後即跳至第 11 項，row 475 之備註逐字為 `See Head Unit Camera Systems Logic & Flow`
—— **子項 label 由本 SYS1 章承載**。全表掃描 `Make Favorite`／`Edit Name`／`Reset Camera`／
`Delete Camera`／`Edit Favorite` 五串：**三個分頁皆 0 命中**。

故對照表為**空集**（0 個子項對 SYS1 之 5 個設定名），**非「對不上 > 3 項」**，
升級條件 §4-3 不成立。設定名一律取 SYS1 之逐字：
`“Make Favorite”`（§30.1.2）／`“Remove as favorite”`（§30.1.4）／`“Edit Name”`（§31.1.1）／
`Reset the selected camera`（§29.1.2）／`Delete the selected camera`（§29.2.2）。

### 2-5　`Vehicle Model`

**30 列五款全 `1`** —— 本批無 PROXI 配備前提（皆為設定與 HMI 行為），
**R-CAM18(b)／(c) 皆不觸發**，依 **R-CAM18(a)** 預設。

### 2-6　hop label（升級條件 §4-2）

| label | 逐字出處 |
|---|---|
| `Apps`／`Settings`／`Camera` | profile §3.0 之 `ENTER_CAMERA_SETTINGS` |
| `Aux Cameras` | HMI Settings List row 474／475 |
| `Make Favorite`／`Remove as favorite`／`Edit Name` | SYS1 §30.1.2／§30.1.4／§31.1.1 逐字 |
| `Delete Camera` | `PU0456` 觸發欄逐字（`Displayed when user selects "Delete Camera"`）|
| `Camera app home page`／`AUX filter`／`favorite filter` | §27.2.6.3／§29.1.2／§30.1.6 逐字 |
| pencil icon／Star | §29.1.2／§30.1.1 逐字 |

**不可辨者 0 列**，升級條件不成立。

### 2-7　popup

`PU0456`（module `Aux Camera`，刪除確認）為本批唯一所需之彈窗，**已查得**。
其按鈕欄第二項拼作 `<Nol>` 而文字欄為 `<Yes>  <No>` —— ER 依文字欄書寫，
差異記於 `-137` 之 `Remarks`（profile §10 第二類）。
§30.1.3 之確認彈窗其內容只在圖（`image159.png`），**不可抽**，故 ER 只判彈窗之出現與清除，
**不造其文字**（§8.4.1）、**不掛 DR-CAM-h**（該條為「Pop Up List 查無」，本處為「來源只給圖」）。

### 2-8　PENDING（7 列，皆 DR-CAM-r）

`-132`／`-138`／`-140`／`-145`／`-146`／`-147`／`-154` —— 其前提或路徑涉 `Camera App`，
該配備旗標六本 PROXI 零命中（CAM-14 已開 DR-CAM-r）。

### 2-9　對帳（`coverage_b.tsv`，sha256_16 `82c94859fa888a03`）

| 組 | `b_plan` 全列 | `coverage_b` | 判 |
|---|---:|---:|---|
| `Activation and Exit` | 50 | 50 | **完結** |
| `AUX Camera Access` | 62 | 62 | **完結** |
| `Camera View Switching` | 31 | 31 | **完結** |
| `Wireless Camera Pairing` | 6 | 6 | **完結** |
| `AUX Camera Settings` | 53 | **36** | B04a 30 ＋ umbrella 6 |
| `Warning Banners` | 13 | 0 | 未生成 |
| `PAM Integration` | 10 | 0 | 未生成 |
| `Camera Settings` | 5 | 0 | 未生成 |
| **合計** | **230** | **185** | 8 組中 **4 組完結** |

`coverage_b.tsv` 全檔：PRODUCED 157／零 TC 6／umbrella 22 ＝ **185 列**。

### 2-10　lint 與 selfcheck

```
selfcheck_camera.py  b04a（TC 30 筆）        十項全 0｜錨 30／反查失敗 0
lint036 --profile camera（b04a_lint.xlsx）   ERROR 類全 0｜非致命 U=7／I-cross=30／X=10｜Z=0／J=0

selfcheck_camera.py  十六目錄（TC 397 筆）    十項全 0｜錨 401／反查失敗 0｜常數錨 2｜合一錨 2
render_tc.py --verify 十六目錄               逐位元相符 397／相異 0
```

`X` 十列之導航標的為 `AUX Cam settings menu`／`settings pop-up` —— 該用語為 SYS1 §29–§31 之逐字，
其入口為 `ENTER_CAMERA_SETTINGS`（profile §3.0）；lint 之啟發式不認該常數為固定入口。
本批之 `X` 為歷來最高（A 本全簿 3、B02a 1、B02b 3、B03 0），成因單一且可解釋，**非新型缺陷**。

---

## §3　R-CAM17 證據表

| # | 主張 | 掃描字串／判準 | 母體 | 命令 | 命中 |
|---|---|---|---|---|---|
| 1 | `AUX Camera Settings` 之 leaf 數 | `test_set` ＋ `kind` 欄計數 | `features/camera/data/b_plan.tsv` 230 列 | csv 計數 | 全 **53**／leaf **45**／umbrella 8 |
| 2 | 跨章逐字同句僅 1 群且皆非 leaf | `Description` 正規化後分群 | cache 本 §29／§30／§31／§34 全部列 | openpyxl 全量讀 ＋ 分群 | 跨章群**僅** `['29.1.3','34.9.3']`；兩列**皆不在 45 leaf 內** |
| 3 | §31.1.3 ↔ §34.9.7、§31.1.6 ↔ §34.9.5 非全等 | 同上分群 | 同上 | 同上 | **不同群**（兩對各為 31/31、41/37 token）|
| 4 | HMI Settings List 之 `Aux Cameras` 無子項 | row 474～476 逐格 | `forms/HMI Settings List …(Feb 13 2026).xlsx` `Settings` 分頁 1015 列 | openpyxl 逐列 | row 475 之後即 row 476 `11. Trailer Reverse Guidance`；**子項 0 個** |
| 5 | 設定名不在 Settings List | `Make Favorite`、`Edit Name`、`Reset Camera`、`Delete Camera`、`Edit Favorite`（不分大小寫）| 同上**三個分頁**全格 | openpyxl 逐列 `re.search` | 五串**皆 0 命中** |
| 6 | `PU0456` 之按鈕欄拼寫 | 按鈕欄逐字 | `forms/Pop Up List HMI R1 (26PI).xlsx` `Main` r459 | openpyxl 逐欄讀 | 按鈕欄 `<X>`／`<Yes>`／**`<Nol>`**；文字欄 `<Yes>  <No>` |
| 7 | 本批無 PROXI 配備前提 | `PROXI ` 前綴於 `pre_conditions` | b04a 30 份 json | `re.findall` | **0 命中**（故 R-CAM18(b)/(c) 不觸發）|
| 8 | A 本本輪 0 改動 | — | A 本十目錄 | `git status --short` | **0 列** |
| 9 | md 與 json 一致 | — | 十六目錄 397 份 md | `render_tc.py --verify` | **相符 397／相異 0** |
| 10 | 四組完結對帳 | `b_plan` 全列 ↔ `coverage_b` | 兩檔 | csv 計數 | 四組逐組相等（50／62／31／6）|

---

## §4　自報錯誤

1. **B04a 初稿之 selfcheck 第 2 項 2 列**（本包內自查自改）——
   `-149`（§31.1.2 鍵盤）與 `-152`（§31.1.3 標題）Procedure 只有 1 步。
   已各加一步：`-149` 加「A–Z 字母鍵齊備」（`full` 之可觀察補強）；
   `-152` 加「另一台之設定選單標題不變」（驗 `NEW NAME` 之單台限定），前提同步加第二台相機。
2. **B04a 初稿之 lint `R` 1 列**（本包內自查自改）—— `-151` 之 Pre-Condition 一行兩條件，已分行。
3. **`X` 10 列未消** —— 曾嘗試把 `-144` 改為自 `ENTER_CAMERA_SETTINGS` 進入以消 `X`，
   但 lint 之啟發式仍以步驟中出現 `settings` 一字為判，改後計數不變，故**還原為前提式起點**
   （與 A 本 batch01b 之 `X` 形制一致）。`X` 為非致命類，成因已於 §2-10 具名。
4. **切點未取「第 40 列」** —— 下放包 §2 寫「若 leaf > 40，依章節序切（家族不切開，≤ 43）」，
   未指定須取滿。已取章界之 30 列並於 §2-1 具名其由；若分析層要求取滿 40（或 43），
   B04a 可擴至 §34.9.6（40 列）或 §34.9.9（42 列），請示下。
5. **本輪未動 `features/camera/RULINGS.md`／`framework.md`／`ANOMALIES.md`／`RD_FEEDBACK.md`／
   `DATA_REQUESTS.md`** —— 無新立條、無重開、無新異常、無新 DR。
   `docs/fw036/RULINGS.sha.tsv` 仍未重生（**GCB-04**，非本線可改）。

---

## §5　未結

| # | 項 | 歸屬 |
|---|---|---|
| 1 | **B04a 之切點是否改取滿 40／43 列**（§4-4）| 分析層 |
| 2 | B 本餘 **4 組 45 列**（leaf 40）：`AUX Camera Settings` 之 §34.9–§34.10 **15 leaf**（B04b）、`Warning Banners` 13、`PAM Integration` 10、`Camera Settings` 5 | 後續批次 |
| 3 | **RDF-11～RDF-14** 四條 | RD |
| 4 | DR 十五條；A 本 f／g／i／j／k／m／p／q 八條阻交付，B 本 g／h／r | Pei |
| 5 | A 本交付（Phase 5–7）待 Pei 之准；候選為 dryrun2（244 列，DECISIONS 6-58）| Pei |
| 6 | GCB-01～GCB-10 十項 | GC 線 |
| 7 | `bench_verify.md` 累計 **71 列**（A 本 11、B01a 3、B01b 8、B02a 11、B02b 12、B03 16、B04a 10）| Pei 實機 |

---

## §6　落檔清單

```
M  docs/runtime/profiles/FW036_R1L_Camera_Profile.md  §5.1 註／§5 註   sha256_16 cfca00584998f774
M  features/camera/DECISIONS.md                   6-65／6-66           sha256_16 2ff7546cfc66829c
M  features/camera/data/popup_texts.tsv           PU1518/PU1519 note   sha256_16 a1c7c763b5702117
A  features/camera/data/b04a_plan.tsv             30 列                sha256_16 14f94ddd9ea8833a
M  features/camera/data/coverage_b.tsv            185 列               sha256_16 82c94859fa888a03
M  features/camera/data/bench_verify.md           B04a 10 列           sha256_16 708271db8e403e18
A  features/camera/generated/b04a/                30 json ＋ 30 md ＋ INDEX ＋ LINT_REPORT
```

**未動**：`features/camera/RULINGS.md`、`framework.md`、`ANOMALIES.md`、`RD_FEEDBACK.md`、
`DATA_REQUESTS.md`、`features/camera/delivered/`、A 本十目錄與 `sandbox/a_dryrun/`、
`features/comfort/`（併行 session）。
