# 上繳包 SEC-19 —— `specification_reference` 之號改 Polarion **ObjectID**（7 位）→ `security_v13`（六本）

執行層，2026-09-17。對應 `down/20260917_SEC-19.md`。**未跑 `--gate`、未寫 `RULINGS.sha.tsv`**。

**結論**：成。ObjectID 欄**以 SWC 先例證實**（§1）；`sys2_objectid_map.tsv` 之 **161 個 `sys_ra_sec` 全數對到 7 位號，`MISSING` 0**；
v12 → v13 逐格 diff **124 格，全在 `specification_reference`**；格式自驗 **1,332 行、`^CFTS084-\d{7}$` 不符 0、非升冪 0、串接符 0**；
換行三數 **98／98／98**；六本 lint 門檻全 0（`W=4` 仍只在 CertProvider）。§5 三條升級條件全不成立。

---

## 一、§1 量測（決定 ObjectID 欄）

### 1-1　CFTS084 `Basic Report` 表頭（前 13 欄；全欄 81 欄）

```text
c0  ID                                   ← NRL work item（`NRL-134186`…）
c1  SYS2 Sys-RA-Feature-ID               ← `SYS-RA-SEC-nnn`
c2  SYS2 Melco ID
c3  Description
c4  SYS2 意見欄位 Comments Field
c5  SYS2 Assignee
c6  SYS2 Grouping
c7  SYS2 來源需求項目ID  Source Requirement items   ← **7 位數字（4949044…）＝ ObjectID**
c8  SYS2 文件識別碼 Document ID           ← `CFTS084`
c9  SYS2 EE Architecture (All,ATL-Hi,…)
c10 SYS2 分類 Category
c11 SYS2 限定地區 Region
c12 （分類細欄）
…   c79 `SYS2_System Requirements Analysis`／c80 `NR1L/NRL-134186`
```

### 1-2　SWC 先例之落欄證據（決定性）

自 SWC 0708 交付本 `Specification Reference 規格參考`（N 欄）取五個號，逐一在
`9_ASPICE/SYS.2 System Requirements Analysis/CFTS_042 Steering Wheel Controls/
CFTS_042 Steering Wheel Controls _SR26_20250813-1632_DeltaAnalysis_Released.xlsx` 搜尋：

| SWC 之號 | 命中位置 |
|---|---|
| `CFTS042-4813401` | `Basic Report` r5，欄 index 4 |
| `CFTS042-4813400` | `Basic Report` r6，同欄 |
| `CFTS042-4813399` | `Basic Report` r7，同欄 |
| `CFTS042-4813398` | `Basic Report` r8，同欄 |
| `CFTS042-4813397` | `Basic Report` r9，同欄 |

該欄之表頭逐字為 **`SYS2 來源需求項目ID  Source Requirement items`**（兩空格，逐字）。
→ **ObjectID 欄名定案**：`SYS2 來源需求項目ID  Source Requirement items`；
CFTS084 同名欄為 **index 7**。`ID`（NRL）與 ObjectID 分屬兩欄，A-45 成立。

### 1-3　`data/sys2_objectid_map.tsv`

| 項 | 值 |
|---|---:|
| CFTS084 `Basic Report` 之 `SYS-RA-SEC` 列 | 737 |
| 本 feature 70 列用到之相異 `sys_ra_sec` | **161** |
| 對到 7 位 ObjectID | **161** |
| `MISSING` | **0** |

欄 `sys_ra_sec | nrl | object_id | source_row`（`source_row` 記 `Basic Report!r<n>`，可回查）。

## 二、裁定

| Ruling（錨點 id）| body sha8 | 行數 |
|---|---|---:|
| `R-SEC10(amend3)`（條文為 R-SEC10(c) amend3）| **`3e8e78ef`** | 12 |
| `R-SEC10(amend2)`（SEC-18 之條文，錨點改名）| `f316b28e`（**未變**）| 15 |

乾跑取得（`--out /tmp/sec19_dry2.tsv`），**正本 `RULINGS.sha.tsv` 本包 0 變動**。

> **錨點改名之理由（自報，§五-1）**：兩條 amend 之標題原皆解析為同一 id `R-SEC10(c)`，
> 將來 `--gate` 必以「重複 ruling_id」失敗。改為 `R-SEC10(amend2)`／`R-SEC10(amend3)`
> （與既有 `R-SEC7(amend2)` 同式）。`body_kind = fenced`，故**兩條之 body sha8 不因改標題而變**（已複驗）。

## 三、v12 → v13

| 項 | 值 |
|---|---|
| 逐格 diff | **124 格，全在 `spec`**；其餘 16 欄 0 |
| 行數分佈 | **與 v12 完全相同**（min 1／median 7／max 64；CertProvider 6／12／41、ECUCert 5／7／24、KeyInstall 13／13／17、SAM 1／3／4、SwdlSecureLib 10／13／64、libLogEncrypt 1／1／1）|
| 格式自驗 | 124 格／1,332 行：`^CFTS084-\d{7}$` 不符 **0**；非升冪 **0**；串接符 **0** |
| 換行三數 | **98／98／98** |
| 塗色 | 六本 ＋ `_all` 重套（CP 11／ECUC 7／KI 4／SAM 23／SWDL 4／LOGENC 1 = 50 列；`_all` 50 列／1,700 格；文字 diff 0、members 48）|

例（`SWE1-CertProvider-001`，v12 → v13）：`CFTS084-134408…` → `CFTS084-4949379`／`-4949380`／`-4949381`…

### 3-1　六本 sha（v13，已套色）

| 本 | sha256[:16] | lint |
|---|---|---|
| CertProvider | `d5d60028c2e1604f` | 門檻 0；`W=4` |
| ECUCert | `fdd85ed6e6f57534` | 全 0 |
| KeyInstall | `0c2e5ac6fdc03c1d` | 全 0 |
| SAM | `25fb2f9b427fcdba` | 全 0 |
| SwdlSecureLib | `9c044555207889a2` | 全 0 |
| libLogEncrypt | `63f62ef7d431a4b6` | 全 0 |
| `merged/security_v13_all.xlsx` | `811afbff77779c0e`（套色前）| —— |

v12 六本已移入 `delivery/superseded/`（`_v12` 尾綴）；`delivered/` 已同步（六本 ＋ 六共用件，逐位元組相同，`MANIFEST.tsv` 重寫）。
`EXEC_GUIDE.md` 未動（不引該欄）。

## 四、自報錯誤

1. **兩條 amend 之錨點 id 相撞（已修）**：見 §二之引註。若非改名，GC-16 之全表重生會在 `--gate` 報重複 id。
2. **`build_spec_ref.py` 之 `missing_nrl` 變數名沿用未改**：其語意已改為「ObjectID 缺號」，
   輸出文字已改為「ObjectID 缺號者」，變數名未重命名（不影響行為）。
3. **SEC-18 之 `nrl` 欄仍留在 `sys2_objectid_map.tsv`**：保留作對照（同一列可同時看到 NRL 與 ObjectID），
   便於回溯 A-45 之判斷；不參與取號。
4. **SEC-20 已落檔但未開工**：本包（v13）與 SEC-20（v14，加 RD 步驟與新 sibling）為相依序列，
   依 SEC-20 前置「SEC-19 若已跑則接其後」，本包先結。

## 五、§5 升級條件

| 條 | 判 |
|---|---|
| CFTS084 無 ObjectID 欄／§1-2 無法證明欄名 | **不成立**：欄名以 SWC 五號落欄逐一證實 |
| 任一 `sys_ra_sec` `MISSING`（>5 列升級）| **不成立**：161／161 對到，`MISSING` 0 |
| diff 落在該欄以外；換行三數不等 | **不成立**：124 格全在該欄；98／98／98 |

## 六、git

未執行 commit／push。建議一支：

```text
feat(security): SEC-19 — specification_reference numbers switch to Polarion ObjectID, security_v13
```
