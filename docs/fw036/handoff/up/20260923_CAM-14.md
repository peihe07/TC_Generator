# 上繳包 CAM-14 —— A 本 sandbox dry-run 判過、B 本 B01a 22 列

對應下放包：`docs/fw036/handoff/down/20260923_CAM-14.md`（審閱 `down/20260923_CAM-13_review.md` 同發）。
執行層 session：延用。日期 2026-09-23。

**一句話**：§1 三項落檔完成；A 本 240 列 sandbox 組簿 dry-run —— **ERROR 類全 0，
組簿未引入任何新違規**（逐批合計與全簿逐項相等），`coverage.tsv` ↔ 240 列全合；
B 本 B01a **22 列**生成，selfcheck 十項全 0、lint ERROR 類全 0。
**未寫 `features/camera/delivered/`，未 tag。**

---

## §1　落檔

### 1-1　profile §7.6 ＋ selfcheck 第 9 項

`docs/runtime/profiles/FW036_R1L_Camera_Profile.md` 新增 **§7.6「生成前之 ANOMALIES 預掃」**
（sha256_16 `046ce8318c950f05`）：每批步驟 A 判軸前先掃該 SWE 列相關之 `ANOMALIES.md`，
命中且處置為「不走 CAN／不可注入」者不得寫成 `Send CAN` 步。沿革具名 CAM-13 之 `-113`。

`selfcheck_camera.py` 新增**第 9 項**（sha256_16 `fbc29926dcfd3001`），判準為**兩條件交集**：

1. 該訊號於 `sources/raw/camera_event_hal_status/*.xlsx` 之 `Supported by Harman` 欄為 `N`
   **且** `MD fake CEH status` 欄含 `Not yet`（A-CA16 所稱之「兩者皆是」）。
2. `ANOMALIES.md` 有**同一列**同時提及該訊號名與「不可注入」或「不走 CAN」。

**初版判準（`N` 或 `Not yet` ＋ 訊號名出現於 ANOMALIES 任一處）於 240 列掃得 58 命中，
經逐筆核對幾乎全為偽陽** —— `STATUS_BH_BCM2.CmdIgnSts`（CEH 註 `could emulate`，
profile §11.1 之 Atl-Mi 點火訊號）、`IPC_VEHICLE_SETUP.DynamicGrid`（CEH 註
`DT25 use Android property and verified`）、`STATUS_CCAN5.ShiftLeverPosition`（`could emulate`）
皆被 `Supported by Harman = N` 一項掃到。收緊後 **240 列命中 0**（證據表第 1 項）。

### 1-2　SYS1 檔名 token 化重生 ＋ GCB-09

依 CAM-13 審閱 §一-2（canon §10.7(b)「只換空白」，其餘字元逐字）重生兩檔之
`spec_reference`／`hmi_source_id` 欄：

| 檔 | 前（HEAD）| 後 | 改動列數 |
|---|---|---|---:|
| `features/camera/data/recon_b_rows.tsv` | `8b6dea66d35d7ff7` | `7909b09d286b9068` | 173 |
| `features/camera/data/b_plan.tsv` | `0a9b1d3ef431ecc2` | `a8840f2e77fcd9ba` | 173 |

重生後 `b_plan.tsv` 之 `spec_reference` 欄**殘留空白 0 個**（證據表第 2 項）。

`docs/fw036/handoff/GC_BACKLOG.md` 登 **GCB-09**（sha256_16 `6c9536ad71c62d2b`）——
`lint036.py` 無「同檔名拼寫變體」檢查；建議把 `spec` 欄之檔名前綴正規化後分群，
同群內 >1 種原始拼法即判。與該檔 GC_BACKLOG 第 1 項（升冪檢查）宜併案。

### 1-3　DECISIONS 6-47～6-50（另加 6-51）

`features/camera/DECISIONS.md`（sha256_16 `9007dd0b8dee352d`）：

- **6-47** CAM-13 §2-2 三處確認（`-025` 為 R-CAM16 第三例）`[DECIDED]`
- **6-48** SYS1 檔名 token 化只換空白 `[DECIDED]`
- **6-49** 生成前 ANOMALIES 預掃、selfcheck 第 9 項取交集 `[DECIDED]`
- **6-50** B 本 `est_tc` 1／leaf 為**規劃基準非承諾** `[DECIDED]`
- **6-51** B 本之 `Vehicle Model` 七欄判準 `[PROPOSED — 待裁]`（見 §3-4）

---

## §2　A 本 sandbox dry-run

### 2-1　檔

```
features/camera/sandbox/a_dryrun/FM-WI-FSM-036-A01 STLA 測試用例規範與結果_SWQT STLA Test Case Specification & Result_SWQT_RearViewCamera_A_20260923_dryrun.xlsx
sha256  eae12a58879a410e7704bbf5e15d2e027f31796385ef0b0a4e5e55df3abdbeb3
大小    59177 bytes｜資料列 240（第 10～249 列）
```

三分頁：`Cover 封面`／`Product Document 記錄封面頁`／`Test Case Specification 測試用例規範`。
Cover／Product Document 依 `features/security/delivered/…_Security_SAM_20260917.xlsx` 之
模板事實填最小欄；**`D7` 核准者、`D8` 審查者、中文作者名留空**（下放包 §2-1）。
列序依 **R-G42 一**：`D` 欄（req_id）數值升冪。**不 tag、不寫 `delivered/`。**

### 2-2　lint 全表

`python3 scripts/lint036.py --profile camera <dryrun.xlsx>`

```
行計 A=0 B=0 C=0 D=0 E=0 F=0 G=0 H=0 I=0 I-sibling=0 J=36 K=0 L=0 M=0 N=0
     P=0 Q=0 R=0 T=0 U=54 V=0 I-cross=240 W=0 X=3 Y=0 Z=0
報告 docs/fw036/lint_reports/RearViewCamera_A__camera_eae12a58_20260923.md
```

- **ERROR 類全 0**；`Z`（Vehicle Model 七欄，R-CAM2）**全綠**。
- `J` 36：全數為 **profile §5.1 之逐字豁免**（`test_item` 上半為來源逐字而該來源首字即小寫），
  逐列 reasoning 已具名承接；全域面為 **GCB-07**。
- `U` 54／`I-cross` 240／`X` 3：非致命類，與逐批結果相同。

**組簿未引入任何新違規** —— 十個批次目錄逐一跑 `lint_batch.py` 之計數合計與全簿逐項相等
（`J` 5+3+1+6+7+14=36、`U` 1+4+14+4+2+7+2+3+17+0=54、`I-cross` 240、`X` 3）。
故下放包 §5 第一項升級條件（ERROR 類且成因在組簿工具）**不成立**。

### 2-3　sheet 級問題

| # | 項 | 實測 | 判 |
|---|---|---|---|
| 1 | `N` 欄多行 spec_reference 之換行 | 240 列中 **4 列**含換行（`-030`／`-031`／`-062`／`-063`，皆 2 行）；`N` 欄含**空白者 0** | 4 列皆為 R-CAM10(b) 合一錨／常數錨之正當多錨，非缺陷 |
| 2 | `AH` 欄 remarks | **9 列**（`-152`／`-153`／`-170`／`-175`／`-176`／`-178`／`-201`／`-239`／`-240`），皆 profile §10 之第一、二類（來源與 DBC 名稱差異／疑似誤植）| 合規 |
| 3 | Vehicle Model 七欄 | 240 列全為 `"0"`／`"1"` 字串（與交付本 `…SAM_20260917.xlsx` 之型別**相同**，實測皆 `str`）；**全 0 之列 0**；各欄計 1：HDCC27 132、DT27 115、637 89、598 0、5210 0、Toro 52、376 73 | 合規（598／5210 全 0 符 R-CAM2）|
| 4 | Test Set 十組拼寫一致 | 工作簿實得 **9 組**，各組拼寫**完全一致無變體**：`Auxiliary Cameras` 18、`Configuration` 20、`Diagnostics` 17、`Display Arbitration` 32、`HMI Overlays` 26、`LVDS Messaging` 61、`Startup and Shutdown` 27、`State Handling` 20、`Video Pipeline` 19 | **第 10 組 `AUX Camera` 0 列** —— 其唯一 SWE 列 `SWE-CAM-024` 為 BLOCKED（DR-CAM-a），非拼寫問題。**另請注意**：`AUX Camera` 與 `Auxiliary Cameras` 為兩個相異組名，字面相近，交付前宜由分析層確認不併 |
| 5 | `D` 欄升冪（R-G42 一）| **升冪成立**；涵蓋 `SWE-CAM-001`～`-023`，**缺 7／13／24／25** | 即 3 個 R-CAM16 委派零 TC ＋ 1 個 BLOCKED。**R-G42 一後半要求「037 有列而無 TC 之需求補一列僅填 D 欄」—— 交付本須另加此 4 列，dry-run 未加**（dry-run 之列數口徑為 240，下放包 §2-1 所定）|
| 6 | 其他欄 | `C`（Polarion）／`E`（TestRail）／`Q`（Est. Time）240 列全空 —— 與交付本 `…SAM_20260917.xlsx` 之 33 列全空**相同**；`O` = `NEW`、`AA` = `PeiPYHsu`、`S` = `NA` 全列填妥 | 合規（`Q` 留空即 R-G42 四之 `[DEFAULT]`）|

### 2-4　對帳

| 口徑 | 值 |
|---|---:|
| `coverage.tsv` 列數（SWE 列）| 25 |
| `coverage.tsv` `tc_count` 合計 | 240 |
| `coverage.tsv` `tc_ids` 相異 | 240 |
| `generated/` 十目錄之 json 相異 | 240 |
| 工作簿資料列 | 240 |
| 差集（兩方向）| **0／0** |

disposition：PRODUCED 21／`No TC — all sources delegated`（R-CAM16）3／BLOCKED 1 ＝ 25。

### 2-5　`data/pending_by_dr.tsv`

新檔（sha256_16 `9601862675794ea2`）。**240 列中 43 列含 PENDING**，涉 8 條 DR：

| DR | 列數 | 出現次數 | 狀態 |
|---|---:|---:|---|
| DR-CAM-f | 9 | 15 | 阻交付 |
| DR-CAM-g | 5 | 10 | 阻交付 |
| DR-CAM-i | 4 | 4 | 阻交付 |
| DR-CAM-j | 7 | 7 | 阻交付 |
| DR-CAM-k | 1 | 1 | 阻交付 |
| DR-CAM-m | 1 | 1 | 阻交付 |
| DR-CAM-p | 2 | 2 | 阻交付 |
| DR-CAM-q | 14 | 14 | 阻交付 |
| DR-CAM-a | 0 | 0 | `SWE-CAM-024` BLOCKED，無 TC 列 |
| b／c／d／e／h／l／n／o | 0 | 0 | A 本無 PENDING 列 |

另有 **6 處 `= PENDING (<label>)` 賦值佔位**（profile §7.5 之形制），其 DR 號記於次一子項，
不另計為未結項；逐筆核對六處皆屬 DR-CAM-f（`BED_EXTENDER.*`／`STEERING1.LwsAngle_SCCM`）。

---

## §3　B 本 B01a

### 3-1　步驟 A —— `features/camera/data/b01a_plan.tsv`

新檔（sha256_16 `fa3a8dcfcf0adccf`），22 列 × 16 欄。
取 `b_plan.tsv` 之 `Activation and Exit` **43 leaf**，依 SYS1 章節序（先 HeadUnitCameraSystems、
後 RVC+PAM）取前 22：`SWE1-RVC-041`～`-052`（§6.1.2–§6.10.1，14 列）
＋ `SWE1-RVC-007-01/-02`、`-008`、`-008-01`～`-05`（§7.1–§7.2，8 列）。

### 3-2　`SWE1-RVC-008` 家族之錨與逐字母體分立（**A-CA33**，自查）

037 B 本 `Analysis Report` 實測：`SWE1-RVC-008` 及其六個子列 `-008-01`～`-06` 之
`HMI Source ID` **同為** `SYS1_HMI_RVC+PAM_R1_Low_SR24_1A_(June_25_2021)_7.2`；
而 cache 本之 §7.2 Description 僅為列舉之標題句 `RVCPAM1) RVC+PAM layout will appear when:`，
六款觸發各自為 §7.2.1–§7.2.6（`NRL-142615`～`NRL-142620`），與六個子列之
`Requirement Title` **一一對應**（六↔六，逐列核對相符）。

處置：`specification_reference` **仍寫 037 之 `…_7.2`**（037 為錨之權威，R-G42 一）；
`test_item` 上半之逐字母體取可定址之 §7.2.n（R-CAM6）。兩者之分立逐列記於 reasoning、
`b01a_plan.tsv` 之 `verbatim_section` 欄與 `ANOMALIES.md` **A-CA33**。
**提請分析層裁是否改錨。**

`SWE1-RVC-008` 本身於 037 為 **leaf**（非 Heading），故必須出 TC。其獨有驗證點判為
**排他性** —— 六款觸發皆不成立時 layout 不出現；六款各支由 `-018`～`-022` 及 B01b 之
`-008-06` 承接，本列不重複任一支。

### 3-3　切點落在 §7.2 家族內（**與下放包 §3 之列示不一致，回報**）

43 leaf 依 SYS1 章節序之第 23～27 列為：`-008-06`（§7.2.6）、`-015`（§7.3）、
`-016`（§7.3.1）、`-017`（§7.3.2）、`-018`（§7.3.3）。

故：

1. 22 之切點**落在 §7.2 家族內**（`-008-05` 之後、`-008-06` 之前）。已依「前 22 leaf」執行，
   `-008-06` 落 B01b；若分析層寧可不切開該家族，B01a 改 23 列即可（仍 < 30，不觸升級條件）。
2. 下放包 §3 所列之「`7.3.1`～`7.3.3` 之三列依 R-CAM6 生成」，依同一章節序為**第 25～27 列**，
   **不在前 22 之內**，本批未生成。俟分析層裁。

### 3-4　產物

`features/camera/generated/b01a/` —— 22 份 json ＋ 22 份 md ＋ `INDEX.md` ＋ `LINT_REPORT.md`。
TC ID `NR1L-RVCHMI-001`～`-022`；Test Group `Rear View Camera`；Test Set 全 22 列
`Activation and Exit`；`specification_reference` = `{SYS1 檔名}_{章節號}`（6-48 之 token 化）。

**`Vehicle Model` 判準**（DECISIONS **6-51** `[PROPOSED]`）：SYS1 兩本之條文不帶平台維度，
故改由**可佈性**判 —— PROXI byte 於該平台之本存在、或觸發訊號於該平台之 DBC 存在則 `1`。
實測 **19 列**五款全 `1`；**3 列**（`-008`／`-012`／`-014`）因 Toro 2019 本缺 byte 177
（**DR-CAM-l**）而 `Toro(2261)` = 0。`598`／`5210` 一律 0（R-CAM2）。

`features/camera/data/coverage_b.tsv`（新檔，sha256_16 `8cb5c03acd6abca2`）：
22 列 PRODUCED ＋ 3 列 umbrella（`SWE1-RVC-007`／`-048`／`-051`）記
`UMBRELLA — covered by children`，合計 25 列。

### 3-5　lint 與 selfcheck

```
selfcheck_camera.py features/camera/generated/b01a   （TC 22 筆）
  1 錨反查 檢查 22／反查失敗 0／命中 0     2 proc≥2 命中 0
  3a 命中 0   3b 命中 0                    4 verbatim 子序列 命中 0（無來源可比 0）
  5 命中 0    6 命中 0    7 命中 0
  8a 命中 0   8b 命中 0   9 命中 0
lint036 --profile camera（b01a_lint.xlsx，22 列）
  ERROR 類全 0｜非致命 U=16、I-cross=22、X=4｜Z=0｜J=0
```

`selfcheck_camera.py` 為 B 本擴充：新增 `sys1_index()`（母體為 `spec-index/cache/` 兩本），
`req_id` 以 `SWE1-RVC-` 起首者，第 1 項只驗錨可反查（**不套 R-CAM10** —— 該委派制以 037 A 本之
SYS-RA 對照為母體，不及於 B 本），第 4 項改以 SYS1 `Description` 為逐字母體。
**A 本 240 列重跑，十項與擴充前逐項相同（全 0、常數錨 2、合一錨 2）。**

`X` 4 列之成因同 batch01b —— 導航標的為 `Controls screen` 而入口本身即 **DR-CAM-g**。

### 3-6　PENDING（B01a）

| DR | 列數 | 說明 |
|---|---:|---|
| **DR-CAM-r**（新開）| 7 | `Camera App`／`Enhanced Camera App` 之配備旗標於六本 PROXI 零命中 |
| **DR-CAM-g** | 4 | Controls 頁之進入路徑無逐字來源（沿 batch01b 形制）|
| **DR-CAM-s**（新開）| 1 | 熱保護關顯示之進入法無來源（四本 DBC `therm` 零命中）|

`features/camera/DATA_REQUESTS.md`（sha256_16 `4fa2a65c1afd3579`）登 DR-CAM-r／-s，合計 15 條。
`features/camera/data/bench_verify.md`（sha256_16 `eb98d21495978b35`）加 B01a 三列
（`-001` 車輛配置、`-016` 熱保護、`-020` bus analyzer 時戳）。

### 3-7　hop label 之可辨性（下放包 §5 第二項升級條件）

22 列所用之 hop label 逐字出處：

| label | 出處（逐字）|
|---|---|
| `Controls screen` | H 本 §12.2.1.1.3、§27.7.1；R 本 §7.2.5 |
| `«Controls» screen (Vehicle Page)` | R 本 §7.2.5 逐字（含法文引號）|
| `App Drawer` | H 本 §18.3.1.1、§27.2.6.2；句式沿 A 本 `NR1L-RVC-113` |
| `Camera app home page` | H 本 §27.2.6.3 |
| `SVC bar` | H 本 §6.7.1 該列 Description 逐字；可選性取 §22.2.13 |
| `Rear View Camera Delay` | `forms/HMI Settings List R1 SR25 Post R1L-R (Feb 13 2026).xlsx` `Settings` r467（`*` 不入，R-CAM5(a)）|

**不可辨者 0 列** —— 升級條件不成立。

### 3-8　popup list

B01a 22 列**無**「refer to popup list」型之文字（`Activation and Exit` 前半為入口與觸發，
無彈窗文案），故本批未用 `forms/Pop Up List HMI R1 (26PI).xlsx`，亦無 `PENDING: DR-CAM-h`。

---

## §4　R-CAM17 證據表

| # | 主張 | 掃描字串 | 母體 | 命令 | 命中 |
|---|---|---|---|---|---|
| 1 | selfcheck 第 9 項收緊後零命中 | 判準見 §1-1 | `features/camera/generated/` 十目錄 240 筆 json | `python3 features/camera/scripts/selfcheck_camera.py <十目錄>` | 初版 **58** → 收緊後 **0** |
| 2 | `b_plan.tsv` 之 `spec_reference` 無殘留空白 | `" "` | `features/camera/data/b_plan.tsv` 第 6 欄 230 列 | `awk -F'\t' 'NR>1 && $6 ~ / /' \| wc -l` | **0** |
| 3 | `Camera App`／`Enhanced Camera App` 無 PROXI 旗標 | `Camera_App`、`Enhanced_Camera` | `forms/proxi/` **6 本**（DT28_ATL_HI／Fastback_ATL_MI／HDCC27_initial／HDCC28_ATL_HI／Promaster_ATL_MI／Toro_ATL_MI）| `proxi_have.py`（全表逐格串接後 `re.findall`，不分大小寫）| 兩串**六本各 0** |
| 4 | Toro 缺 byte 177 之兩名 | `Surround_View_Camera`、`Forward_Facing_Camera` | 同上 6 本 | 同上 | 五本各 1；**`Toro_ATL_MI` = 0** |
| 5 | TRG／CHMSL 之 byte 於 Atl-Mi 三本皆缺 | `Auxiliary_Trailer_Camera`、`Digital_CHMSL_Camera_Prsnt` | 同上 6 本 | 同上 | Atl-Hi 三本各 1；**Fastback／Promaster／Toro 各 0** |
| 6 | 熱保護無 CAN 訊號 | `therm`（不分大小寫）| `forms/` **4 本 DBC** | `grep -ic therm forms/*.dbc` | **四本各 0** |
| 7 | 037 收摺子節者僅 `SWE1-RVC-008` 一族 | 父列 `-NN-nn` 之 section 對 cache 本同層子節 | `recon_b_rows.tsv` 230 列 × cache 兩本 | 逐列比對（見 §3-2）| 同型**僅 1 族**（`-024`／`-031` 子列數與子節數不等，不屬同型）|
| 8 | `render_tc.py` 對既有 md 逐位元相符 | — | `generated/` 十目錄 240 份 md | `python3 features/camera/scripts/render_tc.py --verify <十目錄>` | **相符 240／相異 0** |
| 9 | 組簿未引入新違規 | — | 十批 vs 全簿 | `lint_batch.py` × 10 對 `lint036.py` × 1 | 逐項相等（§2-2）|
| 10 | `coverage.tsv` ↔ 工作簿對帳 | — | `coverage.tsv` 25 列 vs 240 json vs 240 工作簿列 | 集合差 | **0／0** |

---

## §5　自報錯誤

1. **selfcheck 第 9 項初版判準過寬（本包內自查自改）** —— 初版把 CEH 之
   `Supported by Harman = N` **或** `MD fake CEH status = Not yet` 取**聯集**，且
   ANOMALIES 側只比對訊號名是否出現於全文任一處，240 列掃得 58 命中。
   逐筆核對後確認幾乎全為可注入之訊號（CEH 註 `could emulate`／`verified`）。
   已改為**兩條件交集** ＋ ANOMALIES 須同列具「不可注入／不走 CAN」字樣，收緊後 0 命中。
   58 命中之清單未上繳（其為錯誤判準之產物），判準之前後與命中數如上。
2. **B01a 初稿之 lint `R` 6 列、`T` 2 列**（本包內自查自改）—— `R`：Pre-Condition
   把兩個條件寫在同一編號行（`The shift lever is in P and no camera image is displayed` 等）；
   `T`：`PENDING` 說明行含非 ASCII 之法文引號 `«»`。已分行、並把逐字標籤移出 PENDING 行
   （標籤仍逐字保留於步 2 與 ER 2）。改後兩類皆 0。
3. **B01a 初稿之 selfcheck 第 4 項 2 列**（本包內自查自改）—— `-005`／`-006` 之摘句
   在來源中之對應 token 為 `page,`／`drawer,`（逗號黏字），刪逗號即破保序子序列。
   已改為保留逗號（`… via the controls page,`）。
4. **下放包 §3 之兩處與實測不符，未照字面執行**（§3-3）—— 「前 22 leaf」之切點落在
   §7.2 家族內；「`7.3.1`～`7.3.3` 三列」依同一章節序為第 25～27 列而不在前 22。
   已依「前 22 leaf」為準執行並回報，未自行擴批。
5. **`lint_batch.py` 之 glob 原為 `NR1L-RVC-*.json`**，不match B 本之 `NR1L-RVCHMI-*`。
   已改為 `NR1L-*.json`（`selfcheck_camera.py` 原即如此）。A 本十批重跑計數不變。
6. **本輪未動 `features/camera/RULINGS.md`** —— 無新立條；R-CAM17 之證據制已於 CAM-13 立條。
   `docs/fw036/RULINGS.sha.tsv` 仍未重生（**GCB-04**，非本線可改）。

---

## §6　未結

| # | 項 | 歸屬 |
|---|---|---|
| 1 | **R-G42 一之「037 有列而無 TC 補一列僅填 D 欄」** —— 交付本須於 `SWE-CAM-007`／`-013`／`-024`／`-025` 各補一列（dry-run 未加，口徑為 240）| 交付前（Pei 裁）|
| 2 | **`AUX Camera` vs `Auxiliary Cameras` 兩組名字面相近** —— 是否併組或改名 | 分析層 |
| 3 | **`SWE1-RVC-008` 家族之錨是否改寫為 `…_7.2.n`**（A-CA33）| 分析層 |
| 4 | **B01a 是否改 23 列以不切開 §7.2 家族**；`7.3.1`～`7.3.3` 之批次歸屬 | 分析層 |
| 5 | **DECISIONS 6-51**（B 本 Vehicle Model 判準）待裁 | 分析層 |
| 6 | DR 十五條；A 本 f／g／i／j／k／m／p／q 八條阻交付，B 本新增 r／s | Pei |
| 7 | GCB-01～GCB-09 九項 | GC 線 |
| 8 | `bench_verify.md` 累計 14 列（A 本 11、B 本 3）| Pei 實機 |

---

## §7　落檔清單

```
M  docs/runtime/profiles/FW036_R1L_Camera_Profile.md   §7.6                sha256_16 046ce8318c950f05
M  docs/fw036/handoff/GC_BACKLOG.md                    GCB-09              sha256_16 6c9536ad71c62d2b
M  features/camera/DECISIONS.md                        6-47 ～ 6-51        sha256_16 9007dd0b8dee352d
M  features/camera/ANOMALIES.md                        A-CA33              sha256_16 7af6cad65dda4a4e
M  features/camera/DATA_REQUESTS.md                    DR-CAM-r／-s        sha256_16 4fa2a65c1afd3579
M  features/camera/scripts/selfcheck_camera.py         第 9 項＋B 本支援   sha256_16 fbc29926dcfd3001
M  features/camera/scripts/lint_batch.py               glob 改 NR1L-*      sha256_16 4edaff79f972b6a0
A  features/camera/scripts/render_tc.py                json→md 重生        sha256_16 64ccca66438b9634
A  features/camera/scripts/lint_report.py              LINT_REPORT 重生    sha256_16 0da75e72babd40f5
M  features/camera/data/recon_b_rows.tsv               token 化            sha256_16 7909b09d286b9068
M  features/camera/data/b_plan.tsv                     token 化            sha256_16 a8840f2e77fcd9ba
M  features/camera/data/bench_verify.md                B01a 三列           sha256_16 eb98d21495978b35
A  features/camera/data/b01a_plan.tsv                  22 列               sha256_16 fa3a8dcfcf0adccf
A  features/camera/data/coverage_b.tsv                 25 列               sha256_16 8cb5c03acd6abca2
A  features/camera/data/pending_by_dr.tsv              PENDING ↔ DR        sha256_16 9601862675794ea2
A  features/camera/generated/b01a/                     22 json ＋ 22 md ＋ INDEX ＋ LINT_REPORT
A  features/camera/sandbox/a_dryrun/…_dryrun.xlsx      240 列              sha256    eae12a58879a410e…
A  docs/fw036/lint_reports/RearViewCamera_A__camera_eae12a58_20260923.md
```

**未動**：`features/camera/RULINGS.md`、`features/camera/framework.md`、
`features/camera/delivered/`、`features/comfort/`（併行 session）。
