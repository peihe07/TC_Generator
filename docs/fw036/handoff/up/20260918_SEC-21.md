# 上繳包 SEC-21 —— apk 雙檔名 Remarks 註 ＋ 三腳本 chore → `security_v15`

執行層，2026-09-18。前件：`down/20260918_SEC-21.md`、`down/20260918_SEC-20_review.md` §四。
本包**零 TC 步驟／ER 文字變更**，只加 Remarks 一句。

---

## §1　兩支 commit hash（SEC-20 review §五 待 Pei 項）

| 包 | hash | 標題 |
|---|---|---|
| SEC-19 | `4730c53` | feat(security): SEC-19 — specification_reference numbers switch to Polarion ObjectID, security_v13 |
| SEC-20 | `6af6d17` | feat(security): SEC-20 — land the RD-provided CCVR steps, +10 siblings (134 TCs), security_v14 |

（HEAD 另有 `aa0b29a` merge，為 remote 同名分支併入，非本線產物。）

## §2　apk 檔名註（review §四之裁）

判準（`gen_batch1.py`）：該列 Pre-Condition **同時**出現 `CertProviderAndroidInstrumentalTest.apk`（R-SEC24 實檔）
與 `CertProviderServiceManagerTest.apk`（CS.98 GIVEN 逐字）者，Remarks 追加

```
apk name: CCVR STEPS say CertProviderServiceManagerTest.apk; delivered file is CertProviderAndroidInstrumentalTest.apk (A-SEC-15)
```

| 項 | 值 |
|---|---|
| 涉及 TC（兩檔名皆在 Pre-Condition）| **22**（全屬 CertProvider） |
| 補註前已有該註者 | **0** |
| 本包補註數 | **22** |
| v14 → v15 逐格 diff | **22 格**，欄別 100% `remarks`，內容 100% 為上開該句 |

涉及 TC（交付本編號，CertProvider 28 TC 中之 22）：
`NR1L-CP-001`~`013`、`020`~`028`。未命中之 6 列為不需 runner 之文件審查／非 CP-runner 列。

**自報**：下放包 §1 預估 diff ≤ 12 格；實測 22 格。成因為預估以「CS.98 直接引用之列」計，未計入 `PRE` 共用區塊
使全部 CP runner 列皆帶兩檔名。以實測 22 為準（同 SEC-20 §3 之 94 vs ≤86 處置）。

## §3　三腳本 diff 摘要

| 腳本 | 改動 | 行數 |
|---|---|---|
| `gen_batch1.py` | 新增常數 `APK_NAME_NOTE`；`main()` 內依 Pre-Condition 兩檔名同現則 append | +6 |
| `mark_placeholder_rows.py` | `--targets <glob>` 指定標的（未給時仍斷言六本）；`PREV_VER` 不再硬編 `"v11"`，無環境值時退為 `"prev"` | +8 / −3 |
| `build_spec_ref.py` | SEC-19 遺留命名：`missing_nrl` → `missing_object_id`（取號已改 ObjectID，變數名誤導）；純改名，輸出不變 | 4 處 |

`gen_pilot01.py`／`gen_pilot02.py` 之 deprecation 註（SEC-15 review §三）先前已在檔內，本包複查存在，未重複加。

### `--targets` 單本測試
標的：libLogEncrypt 交付本之複本（`/tmp/targets_test.xlsx`），以 `mk.mark(src, out)` 跑。

```
--targets 單本測試：塗色 1 列／34 格｜文字 diff 0｜members 48｜dv [(1, 0), (3, 1)]
```

即：只塗該本應塗之 1 列（34 格＝A~AH），**文字 diff 0**，zip **members 48**，x14 DV 保留 —— 與六本全跑之逐本結果一致。

## §4　六本 sha 與 lint（`SEC_VER=v15`）

| 本 | sha | A | W | X | Y | SC | SS |
|---|---|---|---|---|---|---|---|
| CertProvider | `276f489c5d8e9126` | 0 | 4 | 0 | 0 | 0 | 0 |
| ECUCert | `e793619b3a77a392` | 0 | 0 | 0 | 0 | 0 | 0 |
| KeyInstall | `0c2e5ac6fdc03c1d` | 0 | 0 | 0 | 0 | 0 | 0 |
| SAM | `25fb2f9b427fcdba` | 0 | 0 | 0 | 0 | 0 | 0 |
| SwdlSecureLib | `9c044555207889a2` | 0 | 0 | 0 | 0 | 0 | 0 |
| libLogEncrypt | `63f62ef7d431a4b6` | 0 | 0 | 0 | 0 | 0 | 0 |

門檻全 0；`W=4` 一如既往集中於 CertProvider 本（待 Pei 之 `W=4` 裁定，非本包新增）。
`_all` 塗色前 sha `544cb55b925e2701`；TC 總數 **134**（CP 28／KI 25／SAM 33／ECUC 25／SWDL 11／LOGENC 12），`coverage.tsv` 70／70 不變。
塗色：CertProvider 10／ECUCert 6／KeyInstall 4／SAM 23／SwdlSecureLib 4／libLogEncrypt 1、`_all` 48 列；文字 diff 0；members 48；
`placeholder_rows.tsv` 48 列。v14 六本已移 `superseded/` 並加 `_v14` 尾綴；`publish_delivered.py` 已同步 `delivered/`（6 本 ＋ 6 共用件，逐字節比對通過）。

`id_map_batch_v15.tsv` 134 列（TC 編號與 v14 全同，本包不重編號）。

## §5　自報錯誤

1. **diff 格數超出下放包預估**（§2 末）：22 > ≤12，已具名成因。
2. `mark_placeholder_rows.py` 之 `PREV_VER` 原硬編 `"v11"`，自 SEC-18 起每版皆需手動覆寫環境變數才不致誤標；本包一併修正（`superseded/` 內既有誤標檔名不回溯改，見 SEC-17/18 已自報之 `_v11` 事故）。
3. 本包**未**執行 `--gate`（GC-16 前 Security 線禁令，SEC-15 review §二-2），`RULINGS.sha.tsv` 零觸碰（`git status` 證）。本包無新裁決，無 sha 需登錄。

## §6　狀態

| 項 | 值 |
|---|---|
| 交付 | `delivered/` 六本 **v15**、134 TC ＋ 6 共用件 |
| 佔位 | 94 行／11 token（不變）；塗色 48 列 |
| 裁定 | R-SEC1~26 ＋ amend；framework LOCKED v03 |
| 待 Pei | ① 本包 commit；② 六本封面 `D7`/`D8`/`D9` 中文半 18 格；③ `W=4`；④ 三封訊息（`asset_request.md`／DR-t／A-SEC-1~17，Samuel 多一問 `/vendor/odm/etc/cert_store` vs 037 `/odm`）；⑤ GC-16 前置 |
| 待外部 | RD 資產 11 token；037 作者 DR-a/i/r/t |
