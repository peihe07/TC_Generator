# 上繳包 VS-SL-06 —— 站⑤ 寫回（轉正＋ID 對照＋台帳落檔）

日期：2026-09-02　層級：Tier 1 → Tier 2
對應下放包：`down/20260902_VS-SL-06.md`（協定：`docs/fw036/handoff/PROTOCOL.md`）
**未動 `down/`／`up/` 內任何既有檔（新協定「目錄唯讀」條）；未動 `delivered/`、`inputs/`、`forms/` 之工作簿。**
**未出貨** —— 三本皆含 `PENDING`，依 IN §8.4.3 不得入 `delivered/`。

---

## §1　產物路徑與 SHA256

### 轉正本（站④ 抽驗通過之稿，SHA 逐本核對後複製）

| 檔 | bytes | SHA256 |
|---|---:|---|
| `features/vehicle_setting/output/…_SWQT_VF230_20260902.xlsx` | 187,438 | `49b1bf94eb368ee362378733fe0ad2e36c6f5b8ada3b07fcaec952c476218ac3` |
| `features/bed_lowering/output/…_SWQT_BedLowering_20260902.xlsx` | 230,492 | `b0ee608d5469d31b36a067d0db63f7efd2b40c388e7d58e4fdd6f6a3286f28ec` |
| `features/vehicle_category/output/…_SWQT_VehicleCategory_20260902_working.xlsx` | 231,828 | `3a1b5f401fada13ea39a79e85b8e849a24d1398f8831eba8b4470ec063f65541` |

**三本之 SHA 皆與抽驗通過之沙盒稿逐字相同**（`49b1bf94…`／`b0ee608d…`／`3a1b5f40…`），
轉正前先核、轉正後再核，二次 assert 皆 PASS。沙盒稿與 `.v3.bak`／`.v4.bak` 鏈**保留未刪**。

### MANIFEST ／ ID 對照 ／ 腳本

| 檔 | bytes | SHA256 |
|---|---:|---|
| `features/vehicle_setting/output/MANIFEST.tsv` | 383 | `058f36519d10eeea9fa290404e4fab383dc6de27cef21e0dceb2af5376ae9b45` |
| `features/bed_lowering/output/MANIFEST.tsv` | 383 | `dad4a796d67427a5fe04d1f7c0e8e000f1ece88fa78a157cc2c02dbe81010dc3` |
| `features/vehicle_category/output/MANIFEST.tsv` | 399 | `fac9eecfed479a17a40cc2ce4ed7feee9351eed22447c0ce4fbffbf13cdd1742` |
| `features/vehicle_setting/reports/testrail_id_map_vehicle_setting.tsv` | 81,363 | `4ccd2cd6a007a96b77802e2a32dc04b92d7530f88580ba66ccf65f7f65c14b3a` |
| `features/bed_lowering/reports/testrail_id_map_bed_lowering.tsv` | 30,605 | `636e62b9a5e613510c2f24695ba04a355c8f9492bf8ff71f0b9fc3f9645f182d` |
| `features/vehicle_category/reports/testrail_id_map_vehicle_category.tsv` | 25,038 | `41b0e14598fad1e66addae36d7b42e1bdc9d84c3f9ffa0d13ae3065feb80a128` |
| `features/vehicle_setting/scripts/vs_sl06_promote.py` | 3,778 | `1f3132a7d0e7524ca8dc7b4a85da5cbdacbc2c03684c69453fd5c234c4ba1a48` |
| `features/vehicle_setting/scripts/vs_sl06_idmap.py` | 4,907 | `41b5d94963bf3a375a2a7909e7018961d985c3262bc52f57db6978d249cccc0a` |

### **MANIFEST 之落點與包內字面不同**（自報，理由充分）

包內 §1 令「各 feature `MANIFEST.tsv` 增列」。**若寫入 `features/<slug>/delivered/MANIFEST.tsv`
會立即產生一筆新紅** —— `scripts/lint_paths.py:check_delivered()` 對該檔逐列檢
「**對照表有列而檔不存在**」，而轉正本在 `output/` 而非 `delivered/`。
故改落 **`features/<slug>/output/MANIFEST.tsv`**（`output/` 於 `lint_paths` 之 `EXEMPT_TOPS` 內，
不受落點檢查）。三本各 1 列，欄為
`filename`／`sha256`／`source_path`／`round`／`status`／`note`，`status = writeback-pending-DR`。

---

## §2　TC ID 與 TestRail 對照（R-G42）—— **兩處停下來報，未硬套**

### E／F 欄現況統計（先量再斷）

| feature | 本內列 | E(TestRail) 空／非空 | F(TC ID) 空／非空 | F 樣本 |
|---|---:|---|---|---|
| vehicle_setting | 438 | **438／0** | **438／0** | —— |
| bed_lowering | 151 | 151／0 | **0／151** | `newR1L-BLM-001` |
| vehicle_category | 126 | 126／0 | **0／126** | `newR1L-VC-001` |

**E 欄三本皆全空**（TestRail 尚未回填），故 `old_E` 全為空。

### 阻斷 1 —— `delivery.tc_id_abbr` 全庫無宣告，vf230 之 438 列無法賦號

R-G42 二令 F 欄為 `NR1L-{ABBR}-{nnn}`，且 ABBR 須等於 `feature.yaml` 之 `delivery.tc_id_abbr`。
**實測 `features/*/feature.yaml` 無任一含 `delivery:` 區塊** ——
`vehicle_setting`／`bed_lowering`／`vehicle_category`／`ics_management`／`sw_update` 皆無。
（已在 `delivered/` 之 ICS／SWUpdate 未因此判紅，係其列於
`docs/fw036/DELIVERY_SPEC_BASELINE.tsv`，基線內只計警示。）

**本層不自訂 ABBR**（自訂即造規），vf230 之 438 列一律記 `status = BLOCKED`。
**請示**：`vehicle_setting`（VF230 線）之 `delivery.tc_id_abbr` 應為何？
宣告後本層即可賦號並重出對照表。

### 阻斷 2 —— BL／VC 之 F 欄前綴不合 R-G42

BL／VC 之 F 欄已全數有號，惟前綴為 **`newR1L-`**（`newR1L-BLM-001`／`newR1L-VC-001`），
不合 R-G42 二之 `NR1L-`；現行 `delivered/` 之形制為 `NR1L-ICS-001`（實測 ICS 交付本 1,368 列）。
包內令「F 欄已有者不改號」，故本層記 `status = kept` 並於 `note` 逐列具名其不合形制，**未改號**。
**請示**：`newR1L-` 是否應改為 `NR1L-`？若是，屬改號動作，另令。

### 對照表列數（assert PASS）

| feature | 本內列 | RETIRED | 對照表列 |
|---|---:|---:|---:|
| vehicle_setting | 438 | **19** | **457** |
| bed_lowering | 151 | 0 | 151 |
| vehicle_category | 126 | 0 | 126 |

RETIRED 之 19 列自 `vf230_removed_non_nafta.tsv` 帶入（R-VS84(4) 非 NAFTA，VS-SL-03 移除）。
`status` 之值為 `kept`／`assigned`／`RETIRED`／**`BLOCKED`**（末者為本層新增，供阻斷 1；`assigned` 本輪為 0）。

---

## §3　台帳落檔

| 檔 | 增錄 |
|---|---|
| `features/vehicle_setting/RULINGS.md` | **`R-VS89`–`R-VS93`** 五條（落檔時實測主線最大 `R-VS88`，與所令之 +1 相符） |
| `features/vehicle_setting/ANOMALIES.md` | **`A-VS169`**（分析層 `FO §4.4` 誤植，已結）、**`A-VS170`**（執行層 `down/` 覆蓋事件，已結） |
| `docs/runtime/profiles/FW036_R1L_VehicleSetting_Profile.md` | `[ADD] Settings 查找配方` 一節（三來源→四段之對應、PROXI 來源優先序、R-VS89/90/91/92/93 四規則表、別名與實作指針） |
| `forms/LOOKUP_MISSES.md` | **`B-3`** `Easy_Entry_Menù`（R-G13 三要件齊，判查無）、**`B-4`** No.275 `Alwatys` 原文缺字 |
| `docs/fw036/RULINGS_LEDGER.md` | **未動**（依包內 §3-5） |

五條之要旨：`R-VS89` 家族閘／`R-VS90` 引號式條件與非 ASCII 參數名（後者依 R-13 不代入）／
`R-VS91` FIP 常數不 PENDING 不 DR（`FIP_ALWAYS_OFF`／`FIP_ALWAYS_ON`）／
`R-VS92` 否定式取值表代表值（市場相關者依 R-VS84 取 NAFTA）／
`R-VS93` 步驟可控狀態不得為 Pre（IN §4.4）。**各條皆附 VS-SL-01～05 之實測證據與列數。**

> **`B-4` 之落點與包內字面不同**：包內令入 `LOOKUP_MISSES.md`「候選」。
> 惟 No.275 之 `Alwatys` 為**上游原文缺字**，非任何名稱查詢失敗，
> 不合該檔台帳之「查無」定義，故記於**備註區**（`B-4`）而非台帳表。`B-3` 則入備註區之三要件記載。

---

## §4　自檢

| # | 項 | 結果 |
|---|---|---|
| 1 | 轉正三本 SHA = 沙盒稿 SHA（逐本，轉正前後各一次） | **PASS** |
| 2 | 三本 `output/MANIFEST.tsv` 增列成功（各 1 列） | **PASS** |
| 3 | ID 對照表列數 = 本內列數 ＋ RETIRED（438+19／151+0／126+0） | **PASS** |
| 4 | lint 四項於**轉正本**重跑（括號下半／尾句號／雙引號／Proc↔ER 步數） | **PASS**（0／0／0／0，三本） |
| 5 | 轉正本之 `PENDING` 列數：vf230 **25**、BL **151**、VC **20** | 與 v5／v3 報告相符 |
| 6 | `RULINGS_LEDGER.md` 未動 | **PASS** |

---

## §5　自報

1. **本輪無自造缺陷需更正。** 兩處與包內字面之差異（MANIFEST 落點、`B-4` 落點）皆為
   **避免製造新紅／避免違反該檔自身定義**，理由見 §1／§3，請追認或改令。

2. **`status = BLOCKED` 為本層新增之值**（包內給 `kept`／`assigned`／`RETIRED` 三值），
   供阻斷 1。若不准，請示 vf230 之 438 列應如何記。

3. **分析層之 canon 引用再一處誤植（本層未動 `down/`）**
   `down/20260902_VS-SL-06.md` §0 第二點作「**FO §8.4.3**」。
   實測 **FO 之 `8.4` 為「結果三分法」（第 577 行），其下無 `8.4.3`**；
   `8.4.3 缺件佔位（S6）` 在 `docs/runtime/ASPICE_SWE6_AI_Instruction.md` **第 454 行**。
   **正確引用為 `IN §8.4.3`。** 本上繳包已用正確形。
   此與 `A-VS169`（`FO §4.4`）為**同一類**（FO/IN 混引），建議 Tier 2 於下放前逐條核節號歸屬。

4. **`gate_all.py`：`canon_refs` 505 → 506，其餘三支同數**
   逐檔還原法（依新協定 copy 至工作區、按來源目錄分層，未移動 `down/`／`up/` 任何檔）定位：
   **+1 即上述 `FO §8.4.3` 一處**，來自 `down/20260902_VS-SL-06.md`。
   **本層產物實測 +0**：轉正三本、三份 MANIFEST、三份 ID 對照表、二支腳本、
   `RULINGS.md`／`ANOMALIES.md`／Profile／`LOOKUP_MISSES.md` 之增錄，逐檔還原計數皆不變。
   **`lint_paths` 維持 4** —— 轉正落 `output/`（`EXEMPT_TOPS` 內）未增紅，已複驗其四筆紅仍為
   `driver_distraction/workbook/` 兩檔＋ICS／SWUpdate `delivered/` sha，與本輪無涉。
   `rulings_hash` 本層仍未重跑（重生指紋屬治理動作，且會與既有紅混在一起）；
   **惟本輪新增 `R-VS89`–`R-VS93` 五條，其指紋待該紅結清時一併重生**，於此登記。

### 轉正本不入版控（實測，非缺陷，惟影響追溯）

`output/` 於**三個 feature 之 `.gitignore` 皆列為忽略**：
`features/vehicle_setting/.gitignore:18`、`features/bed_lowering/.gitignore:35`、
`features/vehicle_category/.gitignore:17`。

包內 §1 令「沿用其 `.gitignore` 現況，勿改」，本層照辦、未改任何 `.gitignore`。
**後果**：三本轉正稿與三份 `output/MANIFEST.tsv` **不進 git**，
其存在與內容之唯一書面憑證即本上繳包 §1 之 SHA 表，
以及**已入版控之沙盒稿**（`sandbox/vssl/`，內容逐位元相同：`49b1bf94…` 等三值）。

**登記，不自行處置。** 若須使轉正本可追溯，請示：
(a) 改 `.gitignore` 放行 `output/*.xlsx`；(b) 以 `sandbox/vssl/` 之已入庫稿為追溯本；
(c) 待出貨時直接自 `output/` 進 `delivered/`（該處未被忽略），本階段不另處理。

---

---

## §6　待令

1. `delivery.tc_id_abbr`（阻斷 1）—— 宣告後即可賦號 438 列
2. `newR1L-` → `NR1L-`（阻斷 2）—— 若須改號，另令
3. DR 寄出取號後回填 `PENDING: DR-{n}` 之 `{n}`（vf230 25 列、VC 20 列、BL `Body_Types` 行）
4. 出貨 gate：待 DR 結案或 Pei 裁降轉，方可自 `output/` 進 `delivered/`
