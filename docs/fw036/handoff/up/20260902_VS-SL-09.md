# 上繳包 VS-SL-09 —— R-G42 一(a) 重排＋F 欄重賦

日期：2026-09-02　層級：Tier 1 → Tier 2
對應下放包：`down/20260902_VS-SL-09.md`（協定：`docs/fw036/handoff/PROTOCOL.md`）
**只動 vf230 與 BL 之 `output/` 轉正本、其對照表與 MANIFEST；VC 三件未動；
`sandbox/` 與各 `.bak` 鏈未動；`down/`／`up/` 既有檔未動。**
**仍不出貨**（IN §8.4.3 之 PENDING gate 不變）。

---

## §1　產物路徑與 SHA256

| 檔 | bytes | SHA256 |
|---|---:|---|
| `features/vehicle_setting/output/…_SWQT_VF230_20260902.xlsx` | 192,372 | `aed08b4d83a84732d345dd4d7d5fb11c7a0f28e047cc714ef7420e4ee9fa2eb8` |
| `features/bed_lowering/output/…_SWQT_BedLowering_20260902.xlsx` | 232,887 | `a0331aa6e26334a6ae6667ab7ee931f26780c19d25cb3cdbb90b9edfe981b440` |
| `features/vehicle_setting/reports/testrail_id_map_vehicle_setting.tsv` | 53,693 | `fae77814f5e45525434358256b85c04aecbbf6fc4393147a1388e4fa8739f7b4` |
| `features/bed_lowering/reports/testrail_id_map_bed_lowering.tsv` | 17,619 | `0e09f420ad46fcfaf842b6c3c36d83d7e248c8c2eb32c68372bb628c03a6f9e3` |
| `features/vehicle_setting/output/MANIFEST.tsv` | 371 | `5ed4f3a45844562fa872df04069e01e75bc7fae49515578333ca280e699767f9` |
| `features/bed_lowering/output/MANIFEST.tsv` | 371 | `0d3af5a42bff17850641e8ce792c19d23159c9e81f03266b73124bc21fed6d4a` |
| `features/vehicle_setting/scripts/vs_sl09_reorder.py` | 6,632 | `af615c499c06501979e7c49ac1fd62a51e3ece500c0f209c2798740cbbd82970` |

`.presl09.bak` 二份之 SHA **與 VS-SL-07 上繳所載逐字相同**
（`6c3067ea…`／`fe5b423b…`），備份採「不覆寫既有檔」。
**VC 轉正本未動，SHA 仍為 `f9bbeffc56dc09c80bae6876a93645c6ff59c9db6fff11c934b9835407fc8ac0`**（複驗相符）。

---

## §2　§1 重排

資料列依修後 `req_key(D)` **穩定排序**（`sorted()` 保證等鍵相對序不變），整列搬移全欄同動。

| 本 | 列 | 重排前一(a) 逆序 | **重排後** | 內容集合相同 |
|---|---:|---:|---:|:--:|
| vf230 | 438 | 42 | **0** | **是** |
| BL | 151 | 7 | **0** | **是** |
| VC | 126 | 0 | 0（未動） | —— |

「內容集合相同」之判準：每列取全欄之內容指紋（SHA256），
**排除本包重賦之 F 欄**（BL 另排除 B 欄，理由見下），前後之指紋多重集合逐一相等。

### B 欄（`No.#` 序號）之處置 —— 兩本不同

- **BL**：B 欄為**列相對之自參照公式** `=IF(ISBLANK($D<row>),"",ROW()-9)`（151 列皆是）。
  逐字搬移會使其指向錯列，故**依新列位重寫**（樣式與語義不變，值仍為 `列 − 9`）。
  已逐列 assert 其等於自身列位之公式（151／151 PASS）。
- **vf230**：B 欄為**字面序號**（244–700），依包內「整列搬移」隨列走，**未重編**。

> **待令**：vf230 重排後 B 欄不再遞增（**相鄰逆序 54 處**，首列現為 `520`）。
> B 欄受 **`R-VF83`**（「VF230 之 B 欄序號自 238 起」，Pei 2026-08-23）規範，
> **本包未獲重編 B 欄之令，故未動。** 是否重編、若重編是否自 238 起連號，請裁。

---

## §3　§2 F 欄重賦與端到端對照表

| 本 | F 範圍 | 對照表列 | `status` 分布 |
|---|---|---:|---|
| vf230 | `NR1L-VS-001` – `NR1L-VS-438` | **457** | assigned 438 ＋ RETIRED 19 |
| BL | `NR1L-BLM-001` – `NR1L-BLM-151` | **151** | renamed 151 |

對照表已為**端到端**：`old_F` 為 SL-06 之前之原值 ——
vf230 **438 列皆空**（其本無 TC ID）、BL **151 列皆為 `newR1L-BLM-…`**（複驗 151／151）。
`old_E` 三本仍全空（TestRail 未回填）。RETIRED 19 列不動、無 `new_F`。
MANIFEST 二份已更新 SHA 與 `round = VS-SL-09`。

抽驗（BL 重排後之端到端）：

```
r10  SWE1-HMI-BLM-001-01  old_F='newR1L-BLM-082'  → NR1L-BLM-001
r11  SWE1-HMI-BLM-001-02  old_F='newR1L-BLM-140'  → NR1L-BLM-002
r12  SWE1-HMI-BLM-001-03  old_F='newR1L-BLM-141'  → NR1L-BLM-003
```

---

## §4　§3 不動與不回改（已遵）

- VC 三件（工作簿／對照表／MANIFEST）**未動**，SHA 複驗相符
- 歷史報告（dry-run v1–v5）與 `_v4_branch3_resolution.tsv` 之列號引用**未回改**
- `sandbox/vssl/` 與 `.v3/.v4/.presl07/.presl09` 各 `.bak` 鏈**未動**

---

## §5　自檢

| # | 項 | 結果 |
|---|---|---|
| 1 | 一(a) 相鄰逆序 vf230／BL = 0 | **PASS**（0／0） |
| 2 | 列數不變 438／151 | **PASS** |
| 3 | F 唯一且自 001 連號 | **PASS**（438／151 皆全異且連續） |
| 4 | 重排前後逐列內容集合相同（除 F；BL 另除 B） | **PASS** |
| 5 | BL B 欄自參照公式對齊其列位 | **PASS**（151／151） |
| 6 | PENDING 計數不變 | **PASS**（vf230 **25**、BL **151**、VC **20**） |
| 7 | lint 四項（括號下半／尾句號／雙引號／Proc↔ER 步數）三本 | **PASS**（0／0／0／0） |
| 8 | `.presl09.bak` SHA = VS-SL-07 所載 | **PASS** |
| 9 | VC 三件未動 | **PASS** |

---

## §6　自報

1. **B 欄之處置為本層判斷，請追認**（§2）
   包內只令「整列搬移，不得只動部分欄」。BL 之 B 欄若照此逐字搬移，
   其 `$D<row>` 將指向錯列而生錯值 —— 此非「搬移」而是「搬壞」，
   故本層依新列位重寫該公式，並以獨立 assert 保之。
   vf230 之 B 為字面值，照令隨列走，**其遞增性因此喪失（54 處逆序），已列為待令。**

2. **本輪自造缺陷（已於落檔前更正）**
   內容集合之 assert 初版對兩本一律只排除 F 欄，BL 遂因 B 欄之刻意重寫而誤判
   「重排前後內容不同」。已改為 BL 另排除 B 欄、並新增「B 欄對齊列位」之專屬 assert。
   **BL 已自 `.presl09.bak` 還原後重跑一次**，故其結果為單次乾淨施作。
   vf230 於修 assert 前已成功施作一次，修後再跑一次；**第二次為冪等**
   （已排序之資料再穩定排序不變、F 依同一列序重賦相同），
   內容與對照表相同，僅 xlsx 之 zip 中繼資料使 SHA 相異；
   §1 所載為最終檔之 SHA，`.presl09.bak` 仍為施作前之真實原稿。

3. **`gate_all.py`：`canon_refs` 508 → 510，其餘三支同數；+2 不屬本輪**
   逐檔還原法（copy 至工作區、按來源目錄分層；未觸 `down/`／`up/`）實測：
   移除本輪全部文字產物（`vs_sl09_reorder.py`、二份對照表）後**計數仍為 510**，本輪 **+0**。
   `--report` 之 handoff 範圍內未解析 target 仍為既報之四筆
   （`FO §8.4.3`／`§5-3`／`§8.1`／`§8.6-4`），無新增；
   另一筆 `item §0 第3` 仍唯一出自 `features/vsm_v42/docs/upstream/09_writeback_method.md`。
   **增量出於本線之外之併行作業（vsm_v42／v43 於期間各有 handoff 落檔）**，本線未動其檔。
   `lint_paths` 維持 **4**、`rulings_hash`／`gates_tsv` 同前。

4. **`rulings_hash` 仍未重跑**；`R-VS89`–`R-VS95` 七條之指紋待該紅結清時一併重生（承前登記）。

---

## §7　待令

1. **vf230 之 B 欄**（§2）：是否重編？若重編，是否依 `R-VF83` 自 238 起連號
2. DR 寄出取號後回填 `PENDING: DR-{n}`（vf230 25 列、VC 20 列、BL `Body_Types` 行 151）
3. 出貨：待 DR 結案或裁降轉（IN §8.4.3）
4. VF665 依 `R-VS94` 自 `NR1L-VS-439` 起編（重排未改總數，仍為 438）
