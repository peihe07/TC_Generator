# 上繳包 CMF-04 — 車型欄逐 TC 判定（**四個停點全數命中，一格未填**）＋ R-C60／R-C61 落地

- 日期：2026-09-22
- 對應下放包：`down/20260922_CMF-04.md`（前置 `down/20260921_CMF-03_review.md`）
- 性質：唯讀判定與三份 plan、R-C60／R-C61 之落地、sandbox 候選本 v3。
  **未入 `delivered/`、未覆寫 0907 本與 v1／v2、未 commit、未跑 `--gate`／`--regen`、未重跑 generator。**
- 開工前 `git status`：**CMF-03 產物已提交**於 `da3c8d3 feat(comfort): CMF-03 — R-C54~R-C59 landing, M7 quoting, candidate v2`（89 檔，含 17 支 generator guard、`scripts/lint036.py` 之 R-C57、新測試檔）。
  開工時工作區內唯二之未追蹤檔為分析層自己下放之 `down/20260921_CMF-03_review.md`、`down/20260922_CMF-04.md`。
- R-G62(b) 之觀察依據：起點前 `features/comfort/` 之最後他方寫入為上列 commit（同日稍早），本輪期間未觀察到他方寫入。**「無他 session 寫檔」不可自證，此處只陳述未觀察到。**
- R-G62(c)：`up/20260922_CMF-04.md` 落檔前已證該路徑不存在，故無 `_A`／`_B` 後綴。

---

## 0. 首段

| 項 | 結果 |
|---|---|
| **§3 停點** | **四個全數命中**。逐項之數與依據見 §2-5。**車型欄 T–Z 與車型 sibling 一格未動、一條未產。** |
| 三台 `1` 條數（**依 R-C62 算得之 plan 值，未填入工作簿**） | Promaster **407**／**Toro 21**／Fastback **409**（總 476） |
| 拆題數 | 需拆 **71 條 TC**，**預估新增 72 列**（> 40，停點一） |
| 全 `0` 條數 | **65 條**（停點二） |
| **PENDING 行數** | **4**（預期 4）—— M6 之四條維持，`alt-mi ＝ Atlantis MID？` 一問未答 |
| TC 總數 | **476**（本輪不增減） |
| LINT | JSON 層 **57/57 PASS、476 TCs、0 finding**；候選本層 **H＝0、W＝0、J＝0、P＝0、L＝0、U＝4**。哨兵 `LINT_STATUS: OK`（`sandbox/revise_0922/lint/LINT_STATUS.txt`） |
| 候選本 v3 | 產出。**相對 v2 只差 2 格**（R-C61 之兩條 Remarks）；對 0907 本不符 **0**／21,309 |

**執行層對「停點粒度」之取用（同 CMF-02／CMF-03，經審閱層判為正確）**：**逐條**。
故 §3 之停點只擋下**車型欄與車型 sibling**（§1／§2／§4），
而 §5 所併入之四項（R-C60、R-C61、`-146`、軸 2）**不涉車型**，照做並落入候選本 v3。
若分析層之意是整包停（連 §5 也不做），請指正 —— 回退 v3 只需刪 `sandbox/revise_0922/` 與兩格 Remarks。

**四件需要分析層注意（細節在各節）**：

1. **Toro 幾乎全本不適用**：依 §1 之對映，`2.14` 之排除式 PC 使 Toro 於 **454 條**為 `0`，
   全本只剩 **21 條**為 `1`。這正是 CMF-03_review §3-2 所問而分析層不判之題 —— 數字在此，判斷仍在 Pei。
2. **全 `0` 之 65 條**：其中 **39 條**之成因是「Promaster／Fastback 無 Rear_Climate」ד2.14 使 Toro 為 0」兩個條件相乘；
   **6 條** Tri-Mode（三台皆無或無值）。逐條見 `vm_assign.tsv` 之 `all_zero` 欄。
3. **`-146` 之裁定無事可做，且其來源是我的錯**：CMF-03_review §2 之「補 `-146` Remarks」建立在
   CMF-03 上繳 §8 自報 6 之誤讀上 —— lint036 之 `tc_id` 欄是**工作簿 F**，其 row 156 之
   `Requirement ID` 就是 `SWE1-HVAC-030-04`（`json-275`），Remarks 已於 CMF-03 補齊。**無第四條。** 登 A-CMF17。
4. **CMF-03 留下一個測試回歸，本輪修掉**：我在 CMF-03 新增之 `tests/test_comfort_upper_verbatim.py`
   於**模組層**匯入 comfort 之 `lint_tcs`，而該模組匯入期即讀 `features/comfort/data/recon.json`
   （gitignored 之衍生物）。乾淨 checkout／CI 會 `Interrupted: 1 error during collection`，
   **整個測試套件跑不起來**，不只是這一檔失敗。已改為缺資料時 module-level skip。見 §6。

---

## 1. R-C62 之前置：條文語 ↔ PROXI label 是否同一物（**先證，附逐字**）

下放包 §1 要求「執行層先證條文用語與 PROXI label 為同一物，附逐字；證不出者入 `vm_unmapped.tsv`」。
量測面為 `data/section_fulltext.tsv` 之 **129 節全文**，逐 pattern 報命中節數：

| 待證之物 | 命中節數 | 逐字 |
|---|---|---|
| PROXI 參數名 `Climate_Type` | **0** | — |
| PROXI 參數名 `Climate_Panel_Type` | **0** | — |
| PROXI 參數名 `Integrated_Climate_Touchscreen` | **0** | — |
| PROXI 參數名 `HVAC_Mode_Type` | **0** | — |
| PROXI 參數名 `Automatic_Recirculation` | **0** | — |
| PROXI 參數名 `Rear_Climate`（`rear climate`）| **16** | `2.11`「…or rear climate controls will break SYNC…」 |
| label `Manual` | 7 | **全為 `the manual mode`（氣流模式），非氣候系統型別 —— 同形異義** |
| label `Automatic` | 1 | `14.11`「…not **automatic** system changes to Climate」—— **同形異義** |
| label `Absent` | **0** | — |
| label `Present` | 1 | `3.4`「…will not appear when not **present** in the vehicle」—— 一般英語，非 `Rear_Climate` |
| label `3 knobs` | **0** | 條文寫 `3 knob HVAC controls`（`2.14`，1 節） |
| label `Trimode` | **0** | 條文寫 `Tri-Mode`（`3.1`，1 節） |
| 條文語 `ICS` | 1 | `2.14`「For MTC with **ICS**…」—— **全 129 節僅此一處，且未展開其全稱**；`Integrated` 命中 **0** |
| 條文語 `EMEA` | **0** | —— |

**逐軸之結論**：

| 軸 | 判定 | 依據 |
|---|---|---|
| Rear HVAC ↔ `Rear_Climate` | **可證** | 參數名與條文語 `rear climate` 僅差底線與大小寫 |
| Tri-Mode ↔ `HVAC_Mode_Type` | **可證（附字面差異）** | label `Trimode` 與條文 `Tri-Mode` 僅差一個連字號 |
| knob 型態 ↔ `Climate_Panel_Type` | **半可證** | label `3 knobs` 與條文 `3 knob HVAC controls` 共用 `3 knob`；**惟 2.14 之條件為「MTC ＋ ICS ＋ 3 旋鈕」三者，其 ICS 半邊不可證** |
| **ATC／MTC ↔ `Climate_Type`** | **無法證** | 參數名零命中；label `Manual`／`Automatic` 於條文之命中全為同形異義；條文從不展開 MTC／ATC |
| **ICS ↔ `Integrated_Climate_Touchscreen`** | **無法證** | `ICS` 全 129 節僅 1 處且未展開；`Integrated` 零命中 |
| EMEA ICS | 無 PROXI 參數 | `EMEA` 零字面 → `vm_unmapped.tsv`（R-C62-4）|

**停點四之計數，兩種口徑都給**：
確定無法證者 **2**（ATC／MTC、ICS）；若把 knob 型態之複合對映（其 `with ICS` 半邊不成立即整條不成立）
一併計入則為 **3 > 2，命中**。**我取嚴格計數並回報**——
§1 要求的是「證同一物」，半邊證不出來，這條對映就沒有成立。

**另一個同類事實（順帶查得，只報）**：PC 有一行逐字寫出 PROXI 參數名
`The below requirements shall be implemented when the PROXI parameter $Rear_HVAC_cfg$ = [Present]…`（5 條，出處 CFTS043 NEWR1L-53677）。
**六份 PROXI 檔中並無 `Rear_HVAC_cfg`，只有 `Rear_Climate`**（逐檔實測，三台之命中格為
`PROXI Write_Read!R355C6`／Toro 之 `PROXI Write!R355C6`）。**與 DR-46 同一類名不同名。**

---

## 2. §3 停點 —— 逐項之數與依據

### 2-1. 停點三：`2.14` 之 454 條使 Toro 全為 `0` —— **命中**

- 條文 `2.14` 逐字：「**For MTC with ICS**, there will be no redundant interaction with the screen for
  certain types of physical knobs (**3 knob HVAC controls**) in order to prevent a mismatch between the
  soft and hard controls. In these cases, **no HVAC menu bar icons, no HVAC screens and no HVAC pop ups
  will be displayed**.」
- Toro 之 PROXI（`Dec`）：`Climate_Type = 1 (Manual)`、`Integrated_Climate_Touchscreen = 1 (Present)`、
  `Climate_Panel_Type = 6 (3 knobs)` —— **2.14 所排除之三個條件同時成立**。
- 帶排除式 PC `The vehicle does not have 3 knob HVAC controls with ICS…` 者 **454 條** → Toro 全 `0`。
- 全本之結果：**Toro 只有 21 條為 `1`**（Promaster 407、Fastback 409）。
- 依 §3「**停**，回報條數與依據，不填」——**Y 欄一格未動**。

### 2-2. 停點二：任一 TC 三台皆 `0` —— **命中（65 條）**

| 成因 | 條數 |
|---|---|
| 「後排」ד2.14」相乘：Promaster／Fastback 之 `Rear_Climate = 0 (Absent)`，Toro 被 2.14 排除 | **39**（`The rear climate screen is open…`）＋ 5（`$Rear_HVAC_cfg$ = [Present]`）＋ 1（`Rear Climate screen`）＋ 3（後排 × ATC）|
| Tri-Mode：Promaster `HVAC_Mode_Type = 0 (No Trimode)`、Toro／Fastback **ABSENT** —— 三台皆無 | **6** |
| MTC 條 × 2.14：Promaster／Fastback 為 `Automatic`（非 MTC），Toro 被 2.14 排除 | **11** |
| 其他（含 `one zone MTC with push button TEMPERATURE` 一條 —— Toro 雖為 MTC 但其面板為 3 旋鈕，與「push button TEMPERATURE」相斥）| 餘數 |

逐條見 `vm_assign.tsv` 之 `all_zero` 欄。**這 65 條若照填，等於宣告它們在本輪三台受測車上都不執行。**

### 2-3. 停點一：拆題預估 **72 > 40** —— **命中**

`vm_split_plan.tsv`：需拆 **71 條 TC**（每條只計其第一個命中之軸），預估新增 **72 列**。

| 差異軸 | 條數 | 三台之值 |
|---|---|---|
| `Climate_Type` | 38 | Promaster 2(Automatic)／**Toro 1(Manual)**／Fastback 2(Automatic) |
| `Rear_Climate` | 32 | Promaster 0(Absent)／**Toro 1(Present)**／Fastback 0(Absent) |
| `Climate_Panel_Type` | 1 | 7(No knobs)／6(3 knobs)／0(Only blower knob) —— **三台互異** |

**偵測為啟發式，非證明**（body 命中該軸之字樣且 PC 未宣告該軸），每條須人工複判；
同時涉兩軸者之實際拆數會**高於**本表。已寫入該檔之表頭。

### 2-4. 停點四：無法證同一物之軸 —— **命中（嚴格計 3 > 2）**

見 §1。寬計為 2（不命中）。**兩個數都給，判斷權留給分析層。**

### 2-5. 三份 plan（§2 之產物）

| 檔 | 列數 | 內容 |
|---|---|---|
| `docs/reports/vm_assign.tsv` | 476 | 每條 TC × 三台之 `1`／`0`、依據之 PC 行**逐字**、PROXI 參數與 `Dec` 值（`PROXI Read` 不一致者以 `[PROXI Read=n]` 併列）、規則分支、`all_zero` 旗標 |
| `docs/reports/vm_split_plan.tsv` | 71 | 需拆者之原 TC、差異軸、三台之值、拆成幾條、新增幾列、差異欄位、body 之證據字串 |
| `docs/reports/vm_unmapped.tsv` | 438（涉 426 條 TC） | EMEA ICS **426**、ch9「additional Rear Climate controls」**10**（DR #41）、4 Zone **2** |

**規則分支之分布**：`R-C62-1` 415 條、`R-C62-1(test-setup 蘊含)` 42 條、`R-C62-3（PC 無配置軸）` 19 條。

**一處判斷，請核**：`The rear climate screen is open and the climate system is on`（42 條）之
source class 為 `[test-setup]`，**不是配置軸**，嚴格讀 R-C62-1 不該據以定 `0`。
但無 `Rear_Climate` 之車開不出後排氣候畫面，若不據以定值，這 42 條會拿到 `1／1／1` 而明顯錯。
我**據以定值並另標分支** `R-C62-1(test-setup 蘊含)`，使其可被一眼挑出並整批反轉。

---

## 3. §5 併入之四項（不涉車型）

### 3-1. R-C60 —— **一行未改，只出 plan**

裁定之後半：「**先量**工作簿與條文有無此雙鍵操作之逐字，查無則整行維持原樣並列 plan，不改」。

**條文側量測（129 節全文）**：

| pattern | 命中節數 |
|---|---|
| `select … and … mode` | **0** |
| `press … and …(Face\|Feet\|Windshield)` | **0** |
| `two (mode )?buttons` | **0** |
| `Face/Feet` | 2（`2.12.2`／`16.12.1`）—— **模式循環之組合名，非按鍵標籤** |
| `Feet plus Windshield` | 2（`2.12`／`2.12.2`）—— 同上 |

雙鍵操作之逐字**查無** → 依裁定後半，**Select 動作行亦維持原樣**。
plan：`docs/reports/compound_label_plan.tsv`（42 列，逐處記其今日是否帶引號）。

**附帶查得（更正我在 CMF-03 §4 之措辭）**：我當時寫「工作簿與條文皆無該字面之按鍵標籤」。
條文確無**按鍵標籤**，但 `Face/Feet`／`Feet plus Windshield` 之**字面在條文中是有的**（上表）。
且 **0907 交付本自身即不一致**：L＋M 兩欄之合稱 **已引號 2 處、未引號 11 處**
（0907／`revise_0918`／`revise_0921` 三本實測同數 —— **非 CMF-02／CMF-03 所造**）。
依 R-C60「不加引號」，那 2 處既有引號是否要拿掉？**本輪不動，列入待裁。**

### 3-2. R-C61 —— 落地，**H 6→0、W 5→0**

**先量三處是否同型**（裁定所要求）：

| 工作簿 F | req_id | JSON tc_id | ER 之 `matches` | 今有 Remarks | 同型？ |
|---|---|---|---|---|---|
| `-035` | `SWE1-HVAC-006-04` | `-439` | `…it matches the vehicle's configuration — the model specific icon…` | 無 | **是** —— `2.5` C4 之「as displayed in **the table**」，該表於 129 節全無內容 |
| `-301` | `SWE1-HVAC-096-03` | `-376` | `The style of the HVAC popups matches the type of the hard controls…` | **有**（已登錄） | **是** |
| `-440` | `SWE1-HVAC-122-02` | `-462` | `…an off icon … matches this vehicle's system configuration` | 無 | **是** —— `16.16` ICE15 之「depend on system configuration (see Climate section)」，對照未給 |

三處同型 → 依裁定「同型則一併登錄後豁免」：`-439`／`-462` 補自足之 ambiguity Remarks 並登
`AMBIGUITY_REMARKS`（片段各異，68 §2）；`-376` 早已登錄。

**gate 之實作**（`scripts/lint036.py`）：新增 `FEATURE_EXEMPT_REMARKED = {"comfort": ["H","W"]}`，
只吞**關係類**命中（`RE_H_RELATION`／`RE_W_COMPARE`），`RE_H` 之一般模糊語**不在豁免內**。
**豁免之判準是「該列 Remarks 非空」而非「有 `matches` 這個字」** ——
Comfort 之 JSON 層 `remarks` gate 已強制「非空 Remarks 必為登錄於 `AMBIGUITY_REMARKS` 之句、
≥ 40 字元、英文、無內部代號」，故在本 feature「Remarks 非空」等價於「已登錄之 ambiguity」。
施檢面縮到 feature，不動全域。

**實測**：候選本 **H 6→0、W 5→0**。
裁定所書之「目標 H＝4→2」係以 **R-C56 之前**之計數為底（當時 `-467`／`-468` 尚為 PENDING 行、
`-275` 尚無 Remarks）；R-C56 落地後 H 已是 6，六條全數合於豁免條件，故歸零。

**基線亦隨規則移動，必須一併報**：0907 本在同一支 lint036 下 **H 4→3**
（row 311 早已帶 Remarks，現被豁免），`W` 維持 3。**內容未變，是規則變了。**
`LINT_STATUS.txt` 之 `baseline` 行已改用新規則之值並附註。

### 3-3. `-146` —— **無事可做（裁定之前提有誤，源頭是我）**

`lint036` 報告之 `tc_id` 欄取自**工作簿 F**。實查候選本 v2：

```
row 156   D(Requirement ID) = SWE1-HVAC-030-04     F(TC ID) = NR1L-ComfortHMI-146
          M(ER) = … the manual mode that most closely matches the exited AUTO mode …
          AH(Remarks) = The specification does not name which manual mode the rear system enters …
```

該列就是 `json-275`，其 Remarks **已於 CMF-03 依 R-C56 補齊**。
JSON tc_id `NR1L-ComfortHMI-146` 是另一條（`014-04`，CLIMATE OFF／狀態列破折號），
其 ER 不含 `matches`，也不在 `H` 之命中內。**故無第四條可補，本輪不補。**

成因：依 **R-C46**，既有 466 列之 F **不重編**，工作簿 F 與 JSON tc_id 自 CMF-02 起即為兩套號碼；
我在 CMF-03 §8 自報 6 把前者當成後者。登 **A-CMF17**，並立一條記法：
**跨層指涉一律以 `Requirement ID`（req_id）為準** —— 它是唯一兩層共用且不重編的鍵。

### 3-4. 軸 2 兩條、DR-46 四行 —— 維持

軸 2 之兩條 PC 已於 CMF-03 落地並經本次審閱核可，本輪**未動**。
DR-46 四行維持 `PENDING`（`U＝4`）；CMF-03_review §3-1 之一問（`alt-mi ＝ Atlantis MID？`）未答，
另其「前提」尚未查（`HVACRearDef_Sts` 於三台 DBC 是否逐字存在）—— 見 §7 待辦。

---

## 4. 文件層

- **`RULINGS.md`**：落 **R-C60／R-C61／R-C62**。落帳前實測現行最大號為 **R-C59**
  （`grep -oE "^R-C[0-9]+"`，2026-09-22），暫編＝實編。條文逐字取 review §2 與 CMF-04 §1。
  各條之下附本輪之落實結果（含 R-C60 一行未改、R-C62 未套用之理由）。sha8 未產（GC-16 未落地）。
- **`ANOMALIES.md`**：**A-CMF14～A-CMF16** 逐字取 review §1；**A-CMF17** 為執行層自陳（§3-3）。
- **`DATA_REQUESTS.md`**：本輪**未新增 DR** —— DR-46／47／48 之狀態未變，DR-49 仍依裁定不登。
- **profile**：本輪**未改** —— R-C62 未套用，車型欄之基線仍是 R-C48(amend)（CMF-03 已同步）。

---

## 5. 候選本 v3（`scripts/cmf04_revise.py`）

- 產出：`features/comfort/sandbox/revise_0922/…_SWQT_ComfortHMI_20260922.xlsx`
  - sha256 `5d3726825ff99cb11b39a4538c340c0fc0ffbd5acab116522381e47ba29817d2`
- 母本：`delivered/…_ComfortHMI_20260907.xlsx`（先驗 sha `3550d16d…`）之複本；
  另先驗 v2 之 sha（`98ddf21a…`）以確保對帳對象未被動過。

| invariant（★ 為 ABORT 級） | 結果 |
|---|---|
| ★ zip member 數 | 57 ＝ 57 |
| 差異僅目標 sheet | `xl/worksheets/sheet6.xml` |
| ★ DV sqref（classic ＋ x14） | 逐字相同 |
| B 欄公式 row 10–601 | 逐列原樣（涵蓋新列 476–485）|
| **對 0907 本逐格對帳** | **不符 0 格**／21,309 格；變更 **2,865 格**全數歸因 |
| **對 v2 逐格對帳** | 差異 **2 格**，全數歸因（`R-C61` 2）—— **車型欄零差異，如實反映停點** |

變更格歸因（對 0907 本）：R-C48(amend) 1,864、R-C49(amend) 452、§3 sibling 233、M7 145、
M9-compound 67、M9-Note 22、R-C50+R-C50(amend) 14、R-C50 12、R-C51 10、M2 6、M6 6、
M7+M9-compound 6、R-C52 4、M8 3、wb-006 3、R-C55 slim 3、**R-C61 2**、軸 2 PC 2、
M7+M9-Note 2、R-C56 1。

**呈現面（Tier 3）與 CMF-02_review §3 末段之三件仍未動**（新列列高／SmartArt 舊名／`J5` 日期）。

---

## 6. 自檢

- **JSON 層**：`lint_tcs.py` **57/57 PASS、476 TCs、0 finding**。
- **候選本層**（`lint036.py --profile comfort`）：

| 檢查 | 0907 基線（新規則） | v2 | **v3** | 差之成因 |
|---|---|---|---|---|
| **H** | 3（舊規則 4）| 6 | **0** | R-C61 之豁免；基線亦隨規則移動 |
| **W** | 3 | 5 | **0** | 同上 |
| J／L／P | 0／26／0 | 0／0／0 | **0／0／0** | CMF-03 已達成 |
| U | 0 | 4 | **4** | DR-46 四行維持 |
| M／R／I-cross／X | 8／1450／433／319 | 10／1486／443／337 | 同 v2 | 本輪未增列 |

- **可重現**：`git checkout HEAD -- generated` 後重跑 `cmf04_apply.py`，129 個 JSON **逐位元組相同**；
  重跑 `cmf04_revise.py` 得**相同 sha256**、`MISMATCH_COUNT: 0`。第二次執行於斷言即 `SystemExit`。
- **`canon_refs.py --report --waiver`**：工作區 **35／178**；
  以 `git worktree`（**未用 `git stash`**）取 HEAD(`da3c8d3`) ＋ 本輪兩份下放包之基線，
  同為 **35／178** → **本包新增／改動之檔 unresolved／ambiguous 0 筆**。
- **全庫 pytest**：HEAD 基線 10 failed／1,366 passed；工作區 **9 failed／1,389 passed**；
  **只在工作區失敗者 0 項** → 無回歸。
- `DELIVERY.sha256` 追加 **ENTRY 039**；`shasum -a 256 -c --ignore-missing` **60 OK、0 FAIL**。
- **唯讀紀律**：`down/`／`up/` 內無任何檔被移動、改名或覆寫；量測暫存一律落 session scratchpad。

### 自報

1. **CMF-03 留下之測試回歸，本輪修掉（§0 第 4 點）**。它在我的工作區看不見
   （`data/recon.json` 存在），只在乾淨 checkout 才炸，而我當時的回歸比對用的正是
   worktree —— **基線那一側跑不起來，我卻把它讀成「基線 10 failed」**。
   本輪之比對已先把修正後之測試檔複製進 worktree 再取基線。
2. **`vm_assign.tsv` 之 42 條 `test-setup 蘊含` 是判斷不是規則**（§2-5）。
3. **`vm_split_plan.tsv` 之 71／72 是下界也是啟發式**：每條只計第一個命中軸，
   同時涉兩軸者實際更多；且「body 有該字樣」不等於「該軸真的改變此條之觸發／ER」。
4. **R-C62 之「以 `Dec` 為準」我照辦，但它在 Fastback 之 `Climate_Panel_Type` 上結論相反**：
   `Dec = 0 (Only blower knob)` 而 `PROXI Read = 7`（＝ `No knobs`）。本輪取 `Dec`，
   兩值皆併列於 `vm_assign.tsv`。若改取 `PROXI Read`，Fastback 之旋鈕型態判定會翻轉。
5. **停點三之判定用了 `Climate_Type` 這條「無法證同一物」的對映**（2.14 之「For MTC」半邊）。
   即使把該半邊拿掉、只用「3 旋鈕 ＋ ICS」，Toro 仍落在排除範圍內 —— **結論不變，但依據較弱者已具名。**

---

## 7. 待分析層／Pei

1. **`2.14` 與 Toro**（§2-1）：PROXI 之 `3 knobs + ICS` 與條文 `3 knob HVAC controls with ICS`
   是不是同一件事？是 → Y 欄 454 條為 `0`（全本只剩 21 條）；否 → Y 欄維持 `1`。
2. **全 `0` 之 65 條**（§2-2）：仍出貨？抑或標註為「本輪三台不涉」？
3. **ATC／MTC 與 ICS 兩軸無法證同一物**（§1）：接受以「約定俗成之展開」作為對映依據，
   抑或登 DR 問 RD 要一份 LID／參數 ↔ 條文用語之對照？（與 DR-46 同型。）
4. **`$Rear_HVAC_cfg$` vs `Rear_Climate`**（§1 末）：同一參數之兩個名，六份 PROXI 只有後者。
5. **R-C60 之既有 2 處引號**（§3-1）是否拿掉。
6. **DR-46**：`alt-mi ＝ Atlantis（MID）？` 一句；若是，我再補證
   `HVACRearDef_Sts` 於三台 DBC 之逐字存在（review §3-1 所列之前提），即可結案。
7. 沿 CMF-02_review §3 未決者：R-C44 位階、`ENTER_COMFORT` 入 canon §5.3、DR-49 登與不登。

---

## 8. 產物與 SHA256

| 路徑 | sha256 |
|---|---|
| `features/comfort/sandbox/revise_0922/…_SWQT_ComfortHMI_20260922.xlsx`（候選本 v3） | `5d3726825ff99cb11b39a4538c340c0fc0ffbd5acab116522381e47ba29817d2` |
| `features/comfort/scripts/cmf04_apply.py`（新增） | `0fce7e0257a0ed06e0dfd36160bc0090a54542b153d77b927dd80ae7153341e7` |
| `features/comfort/scripts/cmf04_revise.py`（新增） | `a750eccf700150152b40074ffa470e7a0982ebf6002aa16decb52751a83286a5` |
| `features/comfort/docs/reports/vm_assign.tsv` | `1287a67b8cafef4fc2f37b8e29bbae0ec096e82053a162ce91cb166492207871` |
| `features/comfort/docs/reports/vm_split_plan.tsv` | `baec67fa2aac6d37e6def0988ee45adf17e8112787a088a77bd3d14a38c5cb9f` |
| `features/comfort/docs/reports/vm_unmapped.tsv` | `1fbe9fc6045c573b0537dca976b68d3e3f66cd518aad562e3185da027bf1c0e3` |
| `features/comfort/docs/reports/compound_label_plan.tsv` | `5849d028ef7a42c1bff86c6c7dc19f5bffddaff5b87bb3a158fc6098d3f4fbcb` |
| `features/comfort/docs/reports/cmf04_json_change_log.tsv` | `0a023296a5c64089dfb5af6b0e99c590817695d4563d4cd5e67d3547ae1bd8ff` |
| `features/comfort/docs/reports/cmf04_candidate_cell_recon.tsv` | `1116a9588659a3ac198e25e398fc3fc1360d28f98be65b556616ed1c34860920` |
| `features/comfort/docs/reports/cmf04_vs_0921_cell_delta.tsv` | `cf5850990826ccfdc88630466820500c725ad3faa4f43e653c801fe0214e9f13` |
| `features/comfort/sandbox/revise_0922/lint/LINT_STATUS.txt` | `c050b0119eeae41247cd513274d7d0ce05d8b98a12d7464db266f0d3f3c31c82` |
| `features/comfort/RULINGS.md` | `29ded0b956fb0d90725962f5ca6633dbb5f44acecaa54fef1848ba2b017e7349` |
| `features/comfort/ANOMALIES.md` | `9a441e4c4c673dcb8554e4be189582ead916b29c4421f252ef24882b0130c0ca` |
| `features/comfort/DELIVERY.sha256`（追加 ENTRY 039） | `e8307d47f925e54ae039144070132f0c196ee3b4b8b789787c1e9373d46e3f50` |
| `features/comfort/scripts/lint_tcs.py`（`AMBIGUITY_REMARKS` ＋2） | `69098e6205ffad933e10a75612b04da3ccafc788ef022879d24993d27b24f86a` |
| `scripts/lint036.py`（R-C61 `FEATURE_EXEMPT_REMARKED`） | `a4ecd74156665a8f3a637f6abedc1b10c8168189694ca602920fa7baa565240b` |
| `tests/test_comfort_upper_verbatim.py`（修 module-level 匯入之收集中斷） | `027a4b1beb7b4e5e28a5e9bd63afdab11a064237a8ec09bcef3b4c63c293a88b` |
| `features/comfort/generated/SWE1-HVAC-006.json`／`-122.json` | 見 `cmf04_json_change_log.tsv`；可由 `cmf04_apply.py` 自 HEAD 重現 |

**old→new：無**。**新增 TC：無**（476 → 476）。**車型欄改動：無**。
