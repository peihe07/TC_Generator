# 上繳包 CDD-08 —— Revise（R-DIAG20 常式位元組式／R-DIAG21 摘句）＋ batch 6（Layer 2 #1 SXM Module Version，57 列）

下放包：`down/20260917_CDD-08.md`（依據 `down/20260917_CDD-07_review.md` §三／§四）。執行層 session：沿用 CDD 線，2026-09-17。

## 首段 —— 升級條件逐項（自下放包 §5 逐字複製）

| # | 條件（逐字） | 實測 | 觸發 |
|---:|---|---|:-:|
| 1 | 指紋集合不符（R-G75）／`--check` 不綠（既存者記「既存」不算命中） | 寫前 HEAD 67 ＝ worktree 67，寫後 69，`git diff` 恰 2 列新增 0 刪；全生成逐列比對零漂移。`--check` 仍 FAIL → **既存**（CDD-07 首段已證，HEAD 版同樣 FAIL，差額 756 列全屬他 feature） | **否** |
| 2 | T1(a) 命中 > 35 條（表示 Routine 母節以外亦有 `$ 22` 誤讀 RID，須擴大 grep 並回報） | Routine 母節 TC **35** 條，其中需改 **12** 條／32 處。**另已擴大 grep 至全合併本**：母節以外之 `$ 22 03 xx`／`$ 22 31 xx` **零命中** | **否** |
| 3 | 摘句後任一上半仍 > 50 tokens 或摘句改寫了原文字句 | 8 條摘句後 lint `L` = **0**（八本皆 0）；摘法為「刪去原句前段從屬子句，自 `the retries` 起逐字照抄」，**未改任何一個字**，句首依 R-4 轉大寫 | **否** |
| 4 | 任何需改 canon 之情形 | 無 | **否** |

**四項皆未命中。**

---

## §1　T0 —— 條文與指紋

| 項 | 值 |
|---|---|
| R-G23 取號現查 | `R-DIAG20`／`R-DIAG21` 於全 repo 排除 `down/` 後**各零命中**，號可用 |
| 條文落點 | `features/diagnostics/RULINGS.md`，逐字取自 CDD-07 審閱 §四 |
| 指紋制 | `R-G76` 未見於 `RULINGS_LEDGER.md` → GC-16 未落地 → **R-G75** |

**R-G75(c) 三數**：寫前 HEAD **67**／寫前 worktree **67**（無差異 → **本次無覆寫事件**，累計仍五次）／寫後 **69**。

| 條 | sha8 | body_sha8 | 落點 |
|---|---|---|---|
| `R-DIAG20` | `f73a795c` | `35c37181` | `RULINGS.md:274`，5 行 |
| `R-DIAG21` | `eb0e4065` | `0b7275ef` | `RULINGS.md:282`，5 行 |

`--gate`／`--regen` 全程未執行（§0 禁區，R-G77 候選）。指紋一律以 `--target … --out <scratchpad>` 產生後逐列追加。

---

## §2　T1 —— Revise

### 2.1　R-DIAG20 逐條表（12 條 TC／32 處）

母體依母節反查：Layer 3 母節 = Routine IDs 之 037 列 **27** 列 → 合併本 **35** 條 TC
（pilot 2 ＋ batch 4 14 ＋ batch 5 19）。pilot 之 2 條（`-009`／`-010`）**本就正確**，不動。

| TC ID | 欄 | 原串 | 改後串 | 改由 | 處 |
|---|---|---|---|---|--:|
| `-277` | proc | `$ 22 03 12` | `$ 31 03 03 12` | RID 以 0x22 讀 | 2 |
| `-277` | proc | `$ 31 03 12` | `$ 31 03 03 12` | 缺 sub-function（RID 為 2 bytes）| 1 |
| `-277` | proc | `Send a request for DID $0312 with` | `Request the results of routine $0312 with` | RID 非 DID，用詞改正 | 1 |
| `-278` | proc | 同 `-277` 三項 | | | 4 |
| `-279` | proc | `$ 22 03 12` | `$ 31 03 03 12` | RID 以 0x22 讀 | 2 |
| `-279` | proc | `$ <unsupported SID> 03 12` | `$ <unsupported SID> 01 03 12` | 不支援 SID 之請求比照 start 式 | 1 |
| `-282` | proc | 同 `-279` 兩項 | | | 3 |
| `-284` | proc | 同 `-279` 兩項 | | | 3 |
| `-287` | proc | `$ 22 03 1B` → `$ 31 03 03 1B`／`$ 31 03 1B` → `$ 31 03 03 1B`／用詞 | | | 4 |
| `-288` | proc | 同 `-287` | | | 4 |
| `-289` | proc | `$ 22 03 1B` → `$ 31 03 03 1B`／`$ <unsupported SID> 03 1B` → `… 01 03 1B` | | | 3 |
| `-340`／`-341`／`-343`／`-344` | proc | `$ 31 01 03 09` | `$ 31 01 03 09 <Package Indication value>` | start 缺 option record（CFTS004-4940462 明載有 Package Indication value）| 各 1 |

ER 側無需改：批 4／批 5 之常式 ER 原就寫 `7F 31 <NRC>`／`71 0x <RID>`，無 `62 …` 之誤。

**start 有無 option 之判法**（下放包 T1(a) 所命）：`$0309`／`$0307` 之 CFTS004-4940462／-4940451 明載
「using the Package Indication value provided in the diagnostic command」→ **有**，補 `<…>` 佔位並登錄（新增 4 行）；
`$030A` 之 CFTS004-4940469 未載任何 option → **無**，維持三段不加。

**擴大 grep（條件 2 之查證）**：全合併本 432 列之四欄，`$ 22` 後接 `03 xx`／`31 xx` 者於 Routine 母節以外**零命中** ——
`$ 22` 之其餘用例皆為真正的 DID（`28 xx`／`18 xx`／`50 xx`／`5100`）。

### 2.2　`RT-DIAG` 實作與自測

`scripts/lint036.py`：`DIAG_RT_ROWS`（27 列，以號段 `(148,160)(221,231)(346,348)` 書寫）、
`check_diag_routine()`。母體依母節反查而非依位元組串 —— 依串會漏掉「整行 PENDING」與「以 0x22 誤讀」兩型，
而後者正是本檢查要攔的。`<unsupported SID>` 起首之行不在母體（其服務位元組本就不是 0x31）。

三式機械判準：(a) `$ 22 <RID>` 一律 FAIL；(b) `$ 31 0<sub>` 後不足兩個 RID byte；(c) sub-function 不在 01／02／03。

| # | 案 | 判 |
|---:|---|:-:|
| 正 1 | `$ 31 01 03 12 <AV Channel Number>`（start ＋ option）| PASS |
| 正 2 | `$ 31 03 03 12`（results 四段）| PASS |
| 正 3 | `$ 31 02 03 09`（stop 四段）| PASS |
| 正 4 | `$ <unsupported SID> 01 03 12`（不支援 SID 不在母體）| PASS |
| 正 5 | `SWE1-Diagnostics-001` 之 `$ 22 28 44`（非 Routine 母節，不施檢）| PASS |
| 反 1 | `$ 22 03 12`（RID 以 0x22 讀）| PASS |
| 反 2 | `$ 22 03 1B`（同上，`$031B`）| PASS |
| 反 3 | `$ 31 03 12`（缺 sub-function）| PASS |
| 反 4 | `$ 31 01 09`（RID 只一 byte）| PASS |
| 反 5 | ER 側 `$ 31 04 03 09`（sub-function 不在 01/02/03）| PASS |

**10/10 PASS**；八本複驗 `RT-DIAG` 皆 **0**。

### 2.3　R-DIAG21 摘句（8 條，前後對照）

| TC ID | 前（tokens）| 後（tokens）|
|---|---|---|
| `-345`／`-354` | `If retries are performed as shown on the flowchart, and the retries are the result of differing Indication Codes or as a result of timeout or for another reason, then the HU shall provide the code $FF in the routine results.`（lint 計 **52**）| `The retries are the result of differing Indication Codes or as a result of timeout or for another reason, then the HU shall provide the code $FF in the routine results.`（**≤ 50**，lint `L` = 0）|
| `-346`／`-347`／`-348` | `If retries are performed … three identical instances of Indication Codes $03, $0D, or $0F, then the HU shall provide the respective code $03, $0D, or $0F in the routine results.`（**52**）| `The retries are the result of three identical instances of Indication Codes $03, $0D, or $0F, then the HU shall provide the respective code $03, $0D, or $0F in the routine results.` |
| `-355`／`-356`／`-357` | 同上，`$0307` 版之 `the respective code ($03, $0D, or $0F)`（**52**）| 同上，括號式逐字保留 |

**刪去者僅 `If retries are performed as shown on the flowchart, and`** —— 該子句指向流程圖（DR-DIAG-8 缺件），
與括號下半之測試目的（哪一種重試結局回哪個碼）無關；其餘逐字不動，句首依 R-4 轉大寫。
完整原句以 `specification_reference`（CFTS004-4940465／-4940466／-4940454／-4940455）指回。

### 2.4　產生器守門第三／四條

六個產生器（`gen_pilot`／`gen_batch1`～`gen_batch6`）全數加上：

```python
_b3 = [t["tc_id"] for t in P if "_initial" in t["pre_conditions"]]          # R-DIAG18
_b4 = [(t["tc_id"], tokens) for t in P if 上半 tokens > 50]                  # R-DIAG21
```

`_b4` 之 token 口徑**直接沿用 `lint036.RE_TOKEN` 之字元類**（`[A-Za-z0-9$_.'"-]+`）——
兩處若不同口徑，守門就攔不到 lint 會紅的那批，等於沒守。

### 2.5　profile 更正

CDD-07 我引「`L` 閾值待 R-3」為不改之由，審閱 §三-2 不採。已改：

- `scripts/lint036.py` `CHECK_STATUS["L"]`：`已校準（閾值待 R-3）` → `已校準（R-3 = 50，canon §4.3.1 明文；超限依 R-DIAG21 摘句，不豁免）`。
- `docs/runtime/profiles/FW036_R1L_Diagnostics_Profile.md` 新增 **§1.5a**，記明
  「R-DIAG4(a) 之 verbatim 要求的是『所抄之字不改寫』，不是『必須抄完整句』」，附摘句實例與守門口徑。

**`pilot01_v06` 未產出** —— 下放包 T1 列之，但 pilot 之 2 條常式 TC（`-009`／`-010`）位元組式本就正確、
上半亦未超限，無可 Revise 之處。維持 `pilot01_v05`，不造無變更之新版本（R-G72 之精神）。

---

## §3　T2／T3 —— batch 6

### 3.1　sibling plan（CFTS004 子欄位錨表）

`$2843 - X65 Module Version (Read)` 節（heading CFTS004-4939840）下，**19 個子欄位各一錨**，037 各引為三段式：

| # | 子欄位（CFTS004 逐字）| ObjectID | 037 列（pos/neg/uns）|
|--:|---|---|---|
| 1 | SXI Rev Minor | 4939853 | 276／277／278 |
| 2 | SXI Rev Major | 4939852 | **279**（pilot `-011`）／280／281 |
| 3 | SXI Rev Inc | 4939854 | 282／283／284 |
| 4 | RF Rev Minor | 4939862 | 285／286／287 |
| 5 | RF Rev Major | 4939861 | 288／289／290 |
| 6 | RF Rev Inc | 4939863 | 291／292／293 |
| 7 | Module Type ID C | 4939845 | 294／295／296 |
| 8 | Module Type ID B | 4939844 | 297／298／299 |
| 9 | Module Type ID A | 4939843 | 300／301／302 |
| 10 | Module HW Type C | 4939848 | 303／304／305 |
| 11 | Module HW Type B | 4939847 | 306／307／308 |
| 12 | **Module HW Rev A** | 4939846 | 309／310／311 |
| 13 | Mod SW Rev Minor | 4939850 | 312／313／314 |
| 14 | Mod SW Rev Major | 4939849 | 315／316／317 |
| 15 | Mod SW Rev Inc | 4939851 | 318／319／320 |
| 16 | HDEC Rev Minor | 4939859 | 321／322／323 |
| 17 | HDEC Rev Major | 4939858 | 324／325／326 |
| 18 | HDEC Rev Inc | 4939860 | 327／328／329 |
| 19 | Capability | 4939864 | 330／331／332 |

第 10～12 之命名在 CFTS004 原文即不齊（C/B 為 `Module HW Type`、A 為 `Module HW **Rev**`）——
依 R-DIAG4(a) **逐字照抄，不代為統一**。

**037 引用界線之確認**（下放包 T2 所命）：

| src | ObjectID | Category | 037 引用 | 處置 |
|---|---|---|:-:|---|
| `SYS-RA-DIAG-096` | 4939842 | **Information** | 否 | 不產 —— 為「…the following items…」之引言句，無可斷言；亦因非 FR 而不在 `cfts004_uncited_fr.tsv` |
| `SYS-RA-DIAG-098` BB Rev Minor | 4939856 | Functional Requirement | **否** | 不產（R-DIAG8(a)）；`cfts004_uncited_fr.tsv` 第 99 列 |
| `SYS-RA-DIAG-099` BB Rev Major | 4939855 | Functional Requirement | **否** | 同上，第 100 列 |
| `SYS-RA-DIAG-100` BB Rev Inc | 4939857 | Functional Requirement | **否** | 同上，第 101 列 |

即 `$2843` 節有 **22** 個子欄位錨，037 只引 19 個；BB Rev 三支為 037 之覆蓋缺口（併 FB-DIAG-d）。
登 `[A-DIAG51]`（界線釐清，非缺陷）。

### 3.2　產出

`sandbox/batch6/diagnostics_batch6_v01.xlsx`／`batch6.json`／`coverage.tsv`

| 項 | 值 |
|---|---|
| 新增 TC | **75**（`NR1L-DIAG-358` ～ `-432`，連號無洞、無重複）|
| 組成 | 19 子欄位 × (pos 1 ＋ neg 2 ＋ uns 1) = 76，減 pilot `-011` 已產之 SXI Rev Major positive = **75** |
| Priority | P1 18／P2 57 |
| PENDING | **0** |
| Region／VM | 57 列全 NAFTA → `1 1 1 0 0 0 0` |
| Harman | 3 列 `Need Rework` → Remarks 前綴；其餘空白不寫 |
| `-012` | 留號（#2 SXM Signal Quality，CDD-09）|
| 守門 | 五道全過（PC 動作詞／baseline 未用／baseline 位置／上半 50 tokens／本批不應有常式請求）|

**兩條全文**（下放包 §4-5 所命）

```text
[NR1L-DIAG-277] SWE1-Diagnostics-152 | AV Signal Detection | P2 | All —— Revise 後
Test Procedure
  1. Read the AV signal detection routine results and record as Results_initial
     $ 31 03 03 12
  2. Request the results of routine $0312 with an invalid message length
     $ 31 03 03 12
  3. Read the AV signal detection routine results
     $ 31 03 03 12
Expected Result
  1. Results_initial is recorded
  2. The negative response 7F 31 13 (incorrectMessageLengthOrInvalidFormat) is received
  3. The reported AV signal detection routine results equals Results_initial
```

```text
[NR1L-DIAG-358] SWE1-Diagnostics-269 | SXM Module Version | P1 | NAFTA (1 1 1 0 0 0 0)
Test Item
  Unless otherwise noted, the HU shall report the status of the SXI Rev Minor as reported by the
  Sirius XM chipset when requested by this DID. (read $2843, SXI Rev Minor)
Pre-Condition
  1. The ECU is in the default diagnostic session
  2. The diagnostic tool is connected to the OBD-II diagnostic connector
  3. The Sirius XM chipset is powered and responding
Input Test Data
  NA
Test Procedure
  1. Read the X65 module version information
     $ 22 28 43
Expected Result
  1. The positive response 62 28 43 <module version record> is received, and the record contains
     the SXI Rev Minor as reported by the Sirius XM chipset
Specification Reference
  CFTS004-4939853
Design Method  功能測試 (Functional based ; no specific technique)
Remarks        Harman Scope: Need Rework
```

PC-3 之來源為該列自身之錨（`… as reported by the Sirius XM chipset`），沿 pilot `-011` 之寫法 —— 非保守套用。

---

## §4　T4 —— lint／自檢／coverage／合併本

### 4.1　lint（八本，先判 `Traceback`）

| 工作簿 | `Traceback` | 紅數 |
|---|:-:|---|
| `pilot01_v05`（14）| 無 | 全 0 |
| `batch1_v04`（47）| 無 | 全 0 |
| `batch2_v03`（81）| 無 | 全 0 |
| `batch3_v02`（99）| 無 | 全 0 |
| `batch4_v02`（61）| 無 | `U=2`，其餘 0（含 `L=0`、`RT-DIAG=0`）|
| `batch5_v02`（67）| 無 | `U=1`，其餘 0（**`L` 由 8 → 0**）|
| `batch6_v01`（76）| 無 | **全 0** |
| `merged/diagnostics_v06`（432）| 無 | `U=3`，其餘 0 |

**子集／全集一致性**：`U` 3 ＝ 2（b4）＋ 1（b5）。**一致，不停。**
`U=3` 全為 `DR-DIAG-1` 之 PENDING（`-281` 二行、`-297` 一行），條文所命（R-DIAG5(amend)(d)）。
**真違規 0**；`L`／`RT-DIAG`／`PC-DIAG`／`SEC-DIAG`／`KEY-DIAG`／`RM-DIAG`／`P-DIAG`／`U-DIAG`／`R1-DIAG`／`Z` 八本皆 0。

### 4.2　自檢

canon §9 **17/17**（母體 75）。
R-SEC15 型：(a) 75/75、(b) 75/75、(c) 0、(d) 0、(e) 75/75、(i) 0。
`batch6/coverage.tsv`：**57 列全對映，UNCOVERED 0**。

### 4.3　合併本與佔位

`merged/diagnostics_v06.xlsx` —— **432 列** = pilot v05（14）＋ b1（41）＋ b2（78）＋ b3（98）＋ b4（59）＋ b5（67）＋ **b6（75）**。

`placeholder_summary.tsv`：**394 行／245 條 TC**（登錄率 100%，未登錄 0）。
本包新增 **60 行** = batch 6 之 56 行 ＋ batch 5 Revise 補上之 4 行（`<Package Indication value>`）。
累計：`DR-DIAG-6` 176／`DR-DIAG-5` 139／`DR-DIAG-4` 61／`DR-DIAG-8` 18。

累計覆蓋 **19 組全產／318 個 037 列（81.7%）**。剩 **#2 SXM Signal Quality**（72 列中已產 1）。

### 4.4　被取代之 sandbox 工作簿

依 CDD-05 審閱 §一（只限 sandbox，交付本不適用 —— R-G72）刪除 **4 本**：
`merged/diagnostics_v04.xlsx`／`v05.xlsx`、`batch4/diagnostics_batch4_v01.xlsx`、`batch5/diagnostics_batch5_v01.xlsx`。
各批次目錄現各留一本現行版：pilot v05／b1 v04／b2 v03／b3 v02／b4 **v02**／b5 **v02**／b6 v01／merged **v06**。

---

---

## §5　§3 預期 vs 實測

| 項 | 下放包預期 | 實測 | 判 |
|---|---|---|---|
| Revise T1(a) 命中 | 30–35 條；其中 `$ 22` 誤讀 ≥ 10 | **12 條**／32 處；`$ 22` 誤讀 **8 條／16 處** | **不符（低於預期）** —— 預期把「母節全數 35 條」當成「需改 35 條」；實際 35 條中 23 條（含 pilot 2、batch 5 之 15 條）本就正確 |
| batch 6 TC | 57 列 → 70–80 | **75** | 符 |
| PENDING | 0 | **0** | 符 |
| lint | 真違規 0；`L`／`RT-DIAG` 0 | **皆 0** | 符 |
| 合併本 | ≈ 430；19 組／318 列（81.7%）| **432 列；19 組／318 列（81.7%）** | 符 |
| 指紋 | 依制 | 67 → **69**，零漂移 | 符 |

---

## §6　自報錯誤

1. **`[A-DIAG50]` —— batch 5 之 44 條 SXM 讀類 TC 缺 PC-3「The Sirius XM chipset is powered and responding」。**
   產 batch 5 時我判其為「無來源之多餘前提」而不寫，實則該句就在所引錨的原文裡
   （`… as reported by the Sirius XM chipset`），pilot `-011` 早已以此為 PC-3 並記了 `pc_sources`。
   本包 batch 6 之 75 條依 pilot 前例補立，batch 5 因此與其**不一致**。
   **未處置（越界）** —— 本包 §0 只准產 batch 6，T1 之 Revise 範圍限 R-DIAG20／R-DIAG21。請分析層排 Revise。
   教訓：判「前提有無來源」時，要回讀**同一個錨的全文**，不是只看我為該條摘出來的那半句。
2. CDD-07 我以「profile 寫『閾值待 R-3』」為 `L=8` 不改之由。審閱 §三-2 指出 canon §4.3.1 的 50 是明文，
   profile 那句才是過時的。**教訓**：profile 是 canon 的下位文件，兩者衝突時不能引 profile 推翻 canon；
   而且我引的是 lint 原始碼裡的一行狀態字串，那本來就不是條文。

---

## §7　未結

| # | 內容 | 去向 |
|---|---|---|
| `[A-DIAG50]` | batch 5 缺 PC-3（44 條）| **請分析層排 Revise**（併 CDD-09 或獨立包）|
| `[A-DIAG51]` | `SYS-RA-DIAG-096` 為 Information 引言句；`-098`～`-100` BB Rev 為 037 未引用之 FR | 已處置；BB Rev 三支併 **FB-DIAG-d** |
| `DR-DIAG-7`／`DR-DIAG-8` | 已入 `asset_request.md`（五項）| 待送 Nik |
| push | 依 CDD-06 審閱 §五-1：`fetch` → `rebase` → 普通 push，**不要 `--force-with-lease`** | 待 Pei |
| GC-16 | 兩線皆無新包在跑（Security 至 SEC-17 收尾）。`--check` FAIL 即其標的；R-G77 候選待其落 ledger | 待 Pei |
| FB-DIAG-a～r | 18 項（k 對 SYSAD 作者，其餘對 Nik）| 待送 |

---

## §8　建議 commit

```
feat(diagnostics): CDD-08 batch 6 — 75 TCs, SXM module version; routine byte-form revise

- R-DIAG20 (RoutineControl byte form) + R-DIAG21 (test item excerpting)
- RT-DIAG lint check, 27-row Routine scope, 10/10 self-tests
- revise 12 TCs / 32 sites: RID read via 0x22, missing sub-function, missing option record
- excerpt 8 over-length test items, L drops 8 to 0
- generator guards 3 and 4 across all six generators
- batch 6: 19 version sub-fields, 57 rows into 75 TCs, NR1L-DIAG-358..432
- merged v06: 432 TCs, 19 of 20 sets complete, 318 of 389 rows (81.7%)
- placeholder_summary: 394 rows, 100% registered
- A-DIAG50/51 registered
```

pathspec（併行 session 會 stage 他線檔，`git add` 與 `git commit` 皆須帶；
**不再整包 `docs/fw036/lint_reports`** —— CDD-07 因此夾帶了三份 Security 報表）：

```
features/diagnostics scripts/lint036.py docs/fw036/RULINGS.sha.tsv \
docs/runtime/profiles/FW036_R1L_Diagnostics_Profile.md \
docs/fw036/lint_reports/diagnostics_*_20260917.md \
docs/fw036/handoff/up/20260917_CDD-08.md \
docs/fw036/handoff/down/20260917_CDD-08.md \
docs/fw036/handoff/down/20260917_CDD-07_review.md
```
