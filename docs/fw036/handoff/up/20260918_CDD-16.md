# 上繳包 CDD-16 —— 收尾：`$5100` 兩處描述行、三條 amend（**CDD 線最終包**）

下放包：`down/20260918_CDD-16.md`（依據 `down/20260918_CDD-15_review.md` §三／§四）。執行層 session：沿用 CDD 線，2026-09-18。

## 首段 —— 升級條件逐項（自 §5 逐字複製）

| # | 條件（逐字） | 實測 | 觸發 |
|---:|---|---|:-:|
| 1 | 指紋／`--check`（既存者記「既存」） | 寫前 HEAD **85** ＝ worktree **85**（無差異 → **無覆寫事件**），寫後 **88**，`git diff` 恰 3 增 0 刪，零漂移。`--check` FAIL → **既存**（第十二包） | **否** |
| 2 | 對帳差異 > 2 格（不含 ID/列序）→ FAIL，回報不改 | `merged/v13` → `v14` 之變動：**2 條 TC、2 格**（`NR1L-DIAG-229`／`-230` 之 `test_procedure`） | **否** |
| 3 | `SID-WORD` > 0 → 列出不改 | 十一本**全 0** | **否** |

**三項皆未命中。**

---

## §1　T0 —— 三條 amend

`git grep HEAD` 三號**皆零命中**；**先取號落條文，再做產出**。

| 條 | sha8 | body_sha8 | 行 |
|---|---|---|--:|
| `R-DIAG6(amend2)` | `3e36f4ee` | `86017781` | 466 |
| `R-DIAG32(amend)` | `07b58648` | `e5a4d015` | 474 |
| `R-DIAG31(amend)` | `24e77b9d` | `dee456dc` | 482 |

**R-G75(c) 三數**：寫前 HEAD **85**／worktree **85**／寫後 **88**。全生成比對零漂移。

---

## §2　T1 —— `$5100` 兩處描述行

| TC | 前 | 後 |
|---|---|---|
| `NR1L-DIAG-229` | `2. Send a write request for DID $5100 with the last byte omitted` | `2. Send an InputOutputControlByIdentifier request for DID $5100 with the last byte omitted` |
| `NR1L-DIAG-230` | `2. Send a write request for DID $5100 with a parameter value outside the supported range` | `2. Send an InputOutputControlByIdentifier request for DID $5100 with a parameter value outside the supported range` |

`git diff` 覆核：`batch3.json` 之變動行 **4**（2 刪 2 增），**全部**為 `test_procedure`，其他欄 **0** 變動。
**`[A-DIAG68]` 至此清乾淨**（CDD-13 之修正式以字面前綴 `\$50` 寫成而漏 `$5100`，見 `[A-DIAG74]`）。

---

## §3　T2 —— `SID-WORD` 措辭校對

判準實作本即「指名衝突」（CDD-14 校準後），本包只把註解改為 **R-DIAG32(amend) 之逐字措辭**：

```text
R-DIAG32(amend)（Pei 裁，2026-09-18）：SID-WORD 判準 = 指名衝突 ——
「描述行明確指名某服務（read／write／InputOutputControl／routine …）而其 `$` 行之 SID 為他服務者才 FAIL；
 未指名服務之動作句（Activate／Select／Send a request for DID …）不在檢查範圍。」
```

複驗：反例（`write` ＋ `$ 2F`）攔、正例（`InputOutputControlByIdentifier` ＋ `$ 2F`）不攔。

---

## §4　T3 —— 產出與對帳

版號先 `ls` 確認未占用（`merged/` 最高 v13、`delivery/` 最高 v06）。

| 項 | 值 |
|---|---|
| `merged/diagnostics_v14.xlsx` | **565 列** |
| 交付本 **v07** | `…_SWQT_Diagnostics_20260918_v07.xlsx`，**565 列／著色 18 列**；`id_map_v14_to_delivery.tsv` |
| 九本批次 | pilot v11／b1 v10／b2 v09／b3 v08／b4 v09／b5 v10／b6 v07／b7 v06／ext v05 |
| **lint（十一本）** | **全 31 項皆 0，無一命中；無 `Traceback`** |
| canon §9 | 17/17 |
| 對帳 v13 → v14 | **2 條 TC／2 格**（合 §2 預期） |
| `coverage.tsv` | 389 列，UNCOVERED 0 —— **不變** |
| `placeholder_summary.tsv` | 592 行 —— **不變** |

---

## §5　T4 —— `cs_uds_rules.md` 引文

§〇 引用鏈之第二段改以 **CS.00099 r25** 為準（R-DIAG31(amend)）：

```text
UDS is the "Enhanced Diagnostic Protocol" referenced in this document based on ISO 14229-1
with Stellantis UDS implementation per specification CS.00100.
```

原 §6.1-4／§9.3.3-5 兩處保留於其下之三處規範性指定表，作為佐證。

---

## §6　§2 預期 vs 實測

| 項 | 預期 | 實測 | 判 |
|---|---|---|---|
| TC 列數 | 565 | **565** | 符 |
| 著色 | 18 | **18** | 符 |
| `SID-WORD` | 0 | **0** | 符 |
| lint 全 0 | 全 0 | **全 31 項 0** | 符 |
| 變動格 | 2 | **2** | 符 |

**五項全符。**

---

## §7　自報錯誤

**無。** 本包所有數字均產出後回填；T0 於任何產出前完成；版號先查後用。

---

## §8　Diagnostics 線終態

| 項 | 值 |
|---|---|
| 母體 | 037 V1 (3) **389 列** |
| 交付本 | `sandbox/delivery/…_SWQT_Diagnostics_20260918_v07.xlsx`，**565 條 TC** |
| Test Set | **20 組全產** |
| 覆蓋 | **389/389（100%）**，UNCOVERED 0 |
| lint | **全 31 項 0** |
| canon §9 | **17/17** |
| PENDING | **0** |
| 著色（不可派工） | **18 條**（`DR-DIAG-8`） |
| **可直接執行** | **547 條** |
| 裁決 | `R-DIAG1`～`R-DIAG33` ＋ 16 條 amend，**指紋 88 列零漂移** |
| Anomaly | `[A-DIAG01]`～`[A-DIAG78]` |
| DR | **CLOSED**：1／2／3／9／10／12；**TOOL_RESOLVED**：4／5／6；**開**：7（KeySense）／8（PARTIAL）／11（SD.00049 附件） |
| FB | `FB-DIAG-a`～`u`（**21 項**） |
| 來源登錄 | CFTS004 索引＋原件、SYSAD、CS.00099／00100／00102、SD.00049、ISO 14229-1、CIP Radio Tables v6.7 |
| 衍生文件 | `cs_uds_rules.md`／`cs00100_annex_a.md`／`cip_mpfa_flowcharts.md`／`extension_audit.tsv`／`framework.md`（LOCKED v01） |

**待 Pei 填 Cover 三格（核准者／審查者／作者中文名）即可交付。**

---

## §9　待 Pei（CDD 線無後續包）

1. **交付本 v07 之 Cover 三格** —— 填完即可交。
2. **`asset_request.md` 三件送 Nik**：`KeySense HMI Logic and Flow`（DR-7）／`SX-9830-0095` 本文 ＋ PkgIndex 對照（DR-8，**唯一阻斷 18 條之件**）／`SD.00049` 附件 `Diagnostic Services Secure Access Rights 28JAN2026.xlsx`（DR-11）。
   另附一節「建議取得 `.cdd` 檔」—— **非交付前提**，供自動化用。
3. **`FB-DIAG-a`～`u` 送出**（k 對 SYSAD 作者，其餘對 Nik）。
4. **GC-16 窗口** —— `--check` 已連續十二包記「既存 FAIL」；需 Security 線與 CDD 線同時停。**CDD 線本包後即停**，只待 Security 線。
5. **push** —— 分支現為 ahead 3 / behind 1（Security 線遠端有新 commit），先 `git fetch` 再合。

---

## §10　建議 commit

```
feat(diagnostics): CDD-16 — final cleanup, lint clean on every check, delivery v07

- R-DIAG6(amend2)/R-DIAG32(amend)/R-DIAG31(amend) landed, numbers checked against HEAD first
- the last two A-DIAG68 rows on $5100 corrected, 2 cells changed and nothing else
- SID-WORD comment aligned to the amended wording, behaviour unchanged
- citation chain now quotes CS.00099 r25 as R-DIAG31(amend) directs
- merged v14 and delivery v07: 565 TCs, 18 shaded, all 31 lint checks report zero
- Diagnostics closes at 389 of 389 rows, 547 of 565 TCs directly executable
```

pathspec：

```
features/diagnostics scripts/lint036.py docs/fw036/RULINGS.sha.tsv \
docs/fw036/lint_reports/diagnostics_*_20260918.md \
docs/fw036/lint_reports/Diagnostics__diagnostics_*_20260918.md \
docs/fw036/handoff/up/20260918_CDD-16.md \
docs/fw036/handoff/down/20260918_CDD-16.md \
docs/fw036/handoff/down/20260918_CDD-15_review.md
```
