# 上繳包 SEC-07 —— PENDING 內查：三 zip ＋ power_moding ＋ Auth-Prog evidence → 128 → 112 → `security_v03`

對應下放包：`down/20260917_SEC-07.md`。
**禁區遵守**：`down/`／`up/` 既有檔未動；canon 未動；**`lint036.py` 未動**；
**`framework.md` 未動**（LOCKED v03）；power_moding **只讀**；
三 zip 只在 `_intake/Security/unzip/` 解壓、解壓物**未投遞** `sources/raw/`；**apk 未反編譯、未執行**；
既有 xlsx 全部未動（v03 為新檔）；`X-h`／`X-c`／`X-d`／`X-g`／`X-j`／`X-k`／`X-m`／`X-i` 之 PENDING **未動**。

---

## 1　三 zip 與同體判定（§3-1）

| 代號 | 檔 | sha16 | 大小 |
|---|---|---|---|
| **Z1** | `KeyInstall_IntegrationTests.zip` | `de46aba5dc9b6e9b` | 76,461 B |
| **Z2** | `Cert Val CS.98/IntegrationTests.zip` | `8e97a0413712aaf2` | 3,118 B |
| **Z3** | `CertProvider_IntegrationTests.zip` | `8e97a0413712aaf2` | 3,118 B |

**Z2 ≡ Z3**（sha 相同，同體）。
**Z4**（`Secure Log CS.212/StevenJSHsu/IntegrationTests/` 目錄）**≡ Z1 之解壓樹**（同一 `IntegrationTests/` 結構）。

MANIFEST 新登 **2** 個 doc_id（同體者只登一次）：
`keyinstall_integrationtests_zip`、`certprovider_integrationtests_zip`，
`note = reference-only; unzipped read-only for SEC-07 inventory｜…`。MANIFEST 221 → **223** 列。

---

## 2　三張清單（§3-2）

| 檔 | 列數 | sha16 |
|---|---:|---|
| `features/security/data/zip_inventory.tsv` | **43** | `b4a60a02432376cb` |
| `features/security/data/zip_methods.tsv` | **62** | `0a32813ba9ae3441` |
| `features/security/data/zip_assets.tsv` | **10** | `cd1577c307bc569a` |

`zip_inventory` 之 `kind` 分佈：`other` 23（含 `.pyc`／`.pytest_cache`／`README`）、`py` 16、`pem` 3（`rawkey.p8`）、`sh` 1。

### 2-1　**§2.1 之前提與實測不符 —— 不是 Java instrumentation，是 pytest**

§2.1 令抽「`*Test*.java`／`*.kt` 中 `@Test` 後之方法名」。
**三 zip 內無任何 `.java`／`.kt`／`AndroidManifest.xml`**；其為 **host 端 pytest 套件**，
以 `os.popen("adb shell am instrument …")` 驅動 DUT 上之 instrumentation。

→ 改抽 **pytest 函式（`def test_*`）** 與其**字串常數內之 `am instrument` 全式**，
兩者同列於 `zip_methods.tsv`（欄 `pytest_func` ／ `class_fqcn` ／ `java_method` ／ `instrument_cmd`）。
`instrument_cmd` 為自 zip 內**逐字**組合，非執行層自造。
Z2 之 `README.md` 亦逐字載其執行方式：`$ pytest` ／ `$ pytest --html=report.html --self-contained-html`。

### 2-2　與 `Test_Items.txt` 之交叉

| 項 | 值 |
|---|---|
| `Test_Items.txt` 15 條 ∩ zip | **15／15** |
| **missing**（`Test_Items` 有、zip 無）| **0** |
| **new**（zip 有、`Test_Items` 無）| **29** |

→ **`Test_Items.txt` 即 Z2／Z3 之 `test_cert_provider.py` 之常數表**（15 個 `COMMON_PART % "…"`）。
29 個 new 全在 Z1，分屬五類：

| FQCN | 方法數 |
|---|---:|
| `…testkeymasterwrapper.aes.KeyMasterWrapperAesTests` | 13 |
| `…testkeymasterwrapper.rsa.KeyMasterWrapperRsaTests` | 13 |
| `…testkeymasterwrapper.wrong.KeyMasterWrapperWrongParamsTests` | 5 |
| `…testkeymasterwrapper.swdl.KeyMasterWrapperSwdlTests` | 1 |
| **`…testkeyinstalldiagservicemanager.KeyInstallDiagServiceManagerTests`** | **1**（`getInstalledKeysStatus`）|

（含跨檔重複，相異 java 方法共 44；pytest 函式 61，其中 13 個為純 python 驅動、無 java 方法。）

### 2-3　測試資產

`zip_assets.tsv` 10 列，`matches_037` **7**。
實體憑證素材只有 **3 個 `rawkey.p8`**：`TestKeyMaterial/rawkeys/{Logdog,AndroidAuto,SWDL}/rawkey.p8`，
另有產生腳本 `TestKeyMaterial/generate_material.sh`。
**無 `.pem`／`.crt`／`.der` 之憑證檔**，**無 wrong-subject／wrong-issuer／wrong-OID／revoked 之變體**。

---

## 3　各代號解前／解後（§3-3）

| token | v02（解前）| **v03（解後）** | 差 | class |
|---|---:|---:|---:|---|
| `X-g` | 36 | 36 | 0 | AWAIT_ASSET |
| `X-h` | 19 | 19 | 0 | AWAIT_ASSET |
| **`X-f-2`** | **18** | **6** | **−12** | AWAIT_ASSET |
| `X-i` | 16 | 16 | 0 | **AWAIT_RULING(X-i)** |
| **`X-l`** | **12** | **8** | **−4** | AWAIT_ASSET |
| `X-e` | 7 | 7 | 0 | AWAIT_ASSET |
| `X-d` | 7 | 7 | 0 | AWAIT_ASSET |
| `X-n` | 4 | 4 | 0 | AWAIT_ASSET |
| `X-k` | 4 | 4 | 0 | AWAIT_ASSET |
| `X-j` | 2 | 2 | 0 | AWAIT_ASSET |
| `X-m` | 2 | 2 | 0 | AWAIT_ASSET |
| `X-c` | 1 | 1 | 0 | AWAIT_ASSET |
| **合計** | **128** | **112** | **−16** | |

**`RESOLVED` = 16 行**（§4 第三項之上限為 60，**不成立**）。
惟 §2.6 之預估為「內查可解 30~45 行、解後 83~98」—— **實測解 16、餘 112，低於預估下限**。
成因見 3-3。

### 3-1　`X-f-2` —— 解 12 行（6 TC × Proc＋ER）

| TC | SWE1 | 補值 | `source:` |
|---|---|---|---|
| `NR1L-KI-020`／`-021` | KI-011 | Proc：`$ adb shell am instrument -w -e class com.mitsubishielectric.ahu.efw.lib.testkeyinstalldiagservicemanager.KeyInstallDiagServiceManagerTests#getInstalledKeysStatus com.mitsubishielectric.ahu.efw.lib.testkeyinstalldiagservicemanager/androidx.test.runner.AndroidJUnitRunner`；ER：`"OK (1 test)"` | Z1 `IntegrationTests/PythonTests/KeysInstallationTests/test_java_integration.py` **L72–L77** |
| `NR1L-KI-001`／`-002` | KI-001 | Proc：`…KeyMasterWrapperAesTests#encryptDecryptNormalFlow …`；ER：`"OK (1 test)"` | 同上 **L17–L23** |
| `NR1L-KI-016`／`-017` | KI-009 | 同上 | 同上 |

**併同解除之佔位**：全部 KI TC 之 `<installstate file>` 佔位 → **`/mnt/vendor/oemkeys/installstate`**
（逐字自 Z1 `PythonTests/common/keys_install_helper.py` **L50**）。
狀態值亦逐字取得（同檔 L204–L212）：
`0x00` NOT_INSTALLED／`0x01` VERIFICATION_REQUIRED／`0x02` INSTALLED／`0x03` ERROR；
另一狀態檔 `/mnt/vendor/oemkeys/status/keyinstall`（8 bytes，成功為 `00 00 00 00 00 00 00 00`、
驗證中為 `02 00 00 00 00 00 00 00`）。

**未解之 6 行（3 TC）——不硬配**：

| TC | SWE1 | 為何不配 |
|---|---|---|
| `NR1L-KI-018`／`-019` | KI-010（授權／未授權用戶端）| Z1 最接近者為 `KeyMasterWrapperWrongParamsTests#invalidKeyBlob*` —— 其為**無效金鑰 blob**，非**呼叫端授權**。語意不符 |
| `NR1L-KI-022` | KI-012（SELinux enforcing 下之指定任務）| Z1 無對應方法 |

`zip_methods.tsv` 對此二類註 `no 037 match`。

### 3-2　`X-l` —— 解 4 行（2 TC × Proc＋ER）

| TC | SWE1 | 補值 | `source:` |
|---|---|---|---|
| `NR1L-SWDL-005` | SWDL-004 sibling 2（正確簽章 → 通過）| Proc `$ 31 01 F0 00`；ER `Positive response is received: 71 01 F0 00 00` | CCVR `Auth-Prog CS.93` evidence r7／r8 |
| `NR1L-SWDL-006` | SWDL-004 sibling 3（竄改 → 失敗）| Proc `$ 31 01 F0 00`；ER `Positive response is received: 71 01 F0 00 01` | 同 r3／r5／r6 |

**bit field 語意之來源**：CS.00102 `SYS-RA-CS00102-685` 之 Harman RQMT ——
byte 4 之 bit 0 General Verification Failure／bit 1 CRC Check Failure／bit 2 Signature Check Failure／
bit 7 Read/Write Memory Failure。已入 Remarks。

**未解之 8 行**：SWDL-003 三 sibling（**解密**路徑，Auth-Prog 之 `31 01 F0 00` 為 Check Program＝驗證，
非解密，屬跨場景，依 R-SEC15(a) 不移植）＋ SWDL-004 sibling 1（input parameter range，無 evidence）。

### 3-3　`X-e`／`X-e-2`／`X-e-3` —— **內查未解（0 行）**

Z2／Z3 只含 `test_cert_provider.py`（15 條方法之驅動）、`conftest.py`、`common/adb_wrapper.py`、`README.md`。
**無任何憑證檔、無產生腳本、無 `*WrongSubject*`／`*Revoked*`／`*WrongIssuer*` 之方法。**
→ Pre-Condition 之資產檔名無可補；觸發步驟維持 PENDING。**此即預估落空之主因**
（§2.3 假設 Z2／Z3 內有測試憑證，實測沒有）。

### 3-4　`X-n` —— **內查未解（0 行），惟找到寫法**（§3-4）

P1 查得（`features/power/delivered/pm_73.xlsx`，**只讀**）：

```
TC   : NR1L-PowerManagement-001   （req SWE-PM-071，Test Set `Power Down`，row 182）
Pre  : 1. A suspend-resume boot sequence is available on the bench
Proc : 1. Start the suspend-resume boot sequence
       2. PENDING: DR-PW30 SplashScreen_Time 之值
       3. Read the HU screen and check the "Splash Screen" against that time on the recording
ER   : 1. The TLM display stays blank while the boot sequence runs
```

**兩點使其無法直接套入**：

1. **`Start the suspend-resume boot sequence` 不在 R-SEC7(a) 之 `PHYS` 閉合清單**
   （`Insert`／`Press`／`Power cycle`／`Disconnect`／`Select "`）。逐字沿用會被 `SC` 判「無執行通道」。
   要沿用須先 amend R-SEC7(a) 之 PHYS 句式 —— **屬裁決面，執行層不自裁**。
2. **power_moding 自身之 S2R 觀察面亦為 PENDING**：`2. PENDING: DR-PW26 Suspend-to-RAM 觀察面`、
   `3. PENDING: DR-PW26 Suspend-to-RAM 對應之 PowerSts_Telematic 值`。
   即該 feature 也還沒有 S2R 的可觀察面。

**DBC 核對**：四本 DBC（`BO_` 619／`SG_` 3,792）現查 —— 無 suspend／resume 之訊號或 `VAL_` label；
`PowerModeSts` 之 `VAL_` 仍只有 `Standard_Power`／`Logistic_Mode_ON`／`Logistic_Mode_PR`／
`LogisticModeON_and_EngineON`（與 SEC-05 之 A-19 結論一致）。
→ `X-n` 之 4 行維持 PENDING。

### 3-5　`X-j` —— 未解（2 行）

Auth-Prog 之 evidence 需「竄改後之 OTA 映像」本身，該物即 `X-j`，非文件可解。

---

## 4　產物（§3-6）

| 檔 | sha16 | 備註 |
|---|---|---|
| `sandbox/batch1/security_batch1_v03.xlsx` | `86db24552f4491c8` | 72 TC，PENDING **69**（v02 為 85）|
| `sandbox/batch2/security_batch2_v03.xlsx` | `ba1dbded6e8bb8c4` | 28 TC，PENDING **43**（不變）|
| `sandbox/merged/security_v03.xlsx` | `7ec5cd3d33f27689` | **100 TC**，PENDING **112** |
| `delivery/…_SWQT_Security_20260917.xlsx` | `681d7f8f424bb054` | 封面日期 `2026-09-17`；`D7`／`D8`／`D9` 中文半仍留空 |
| `delivery/coverage.tsv` | `0f345db2fcc94e64` | 51 產出／19 DEFERRED（不變）|
| `delivery/pending_summary.tsv` | `946bdb6fa10c3c38` | **加 `class` 欄**：`AWAIT_ASSET` 11 token／96 行、`AWAIT_RULING(X-i)` 1 token／16 行 |
| `delivery/asset_request.md` | `20158ce723b8e318` | 縮減為 **11 個 AWAIT_ASSET**；另列已解兩代號與三點限度 |
| `data/apk_pairing.tsv` | `bc17031bc7e14dcb` | 15 → **17** 列（新增 Z1 兩配對）|

**merged v03 lint（`--profile security`）**：

```
A=0 B=0 C=0 D=0 E=0 F=0 G=0 H=0 I=0 I-sibling=0 J=0 K=0 L=0 M=0 N=0
P=0 Q=0 R=0 T=0 U=112 V=0 W=4 X=0 Y=0 SC=0 SS=0
```

**門檻 `P`／`X`／`SC`／`SS` 全 0；`K=0`；`selfcheck` 九條 0 處。**
交付候選本之 lint 與 merged v03 相同。zip 成員 48→48、DV 計數不變。

### 4-1　`apk_pairing.tsv` 之兩列新增

`getInstalledKeysStatus` → `SWE1-KeyInsyall-011`（`high`）；
`encryptDecryptNormalFlow` → `SWE1-KeyInsyall-001`／`-009`（`medium`）。
**理由**：R-SEC15(g) 令「apk 方法只用於 `apk_pairing.tsv` 有列之 SWE1」；
補值引入之新方法若不登表，`selfcheck` (g) 即報（實測首輪 6 處）。
兩列之 `basis` 逐字記 Z1 之檔名與行號。

---

## 5　與 037／CCVR 矛盾之發現（§3-7）

| # | 發現（逐字）| 與何者矛盾 | 影響 |
|---|---|---|---|
| 1 | Z1 `keys_install_helper.py` **L53** 註解：`# USB external storage path removed; raw keys are now staged in the secure oemkeys partition.`；其 `raw_keys_root_on_device = "/mnt/vendor/oemkeys/rawkeys"` | **037 KI-002／003／007 之 `GIVEN … USB device with private keys`／`WHEN insert USB device with key material`** | 若 Z1 反映現行實作，則 KI 之 USB 匯入路徑**已被取消**，本包 6 個 KI TC 之 `PHYS` 前提（USB 插入）與實作不符。**執行層未改 TC**（037 為母體，R-SEC1(a)），登此矛盾請 Pei 向 RD 確認 |
| 2 | Z1 `assert_error()` 只驗 `installstate == 0x03`，**未驗 status 值**；`assert_success()` 為 `00 00 00 00 00 00 00 00`、`assert_verification()` 為 `02 00 00 00 00 00 00 00` | 037 KI-003 之 `the status value should be 0900 0000 0000 0000` | 037 之 `0900…` **不見於 Z1 任何處**。本包 `NR1L-KI-005` 仍依 037 原文寫，未改 |
| 3 | Z1 `test_can_injection.py` 含 **RID `0x9001`／`0x9003`／`0x9004`** 之完整協定與回應碼（`0x9001` ZZ `0x00` Success；`0x9003` YY/ZZ `0x00`；`0x9004` YY `0x00` OK／`0x03` WRAP_KEY_PARSE_FAILED；`installstate 0x02`／`0x03`），9 個 pytest 案例 | **DR-SEC-k** 原判「定義文件未在手上」（R-SEC8(g) 降級為 reference）| **DR-SEC-k 之前提已變**：可執行證據在手。惟 037 KI-007 原文為 USB/SDCard 匯入、CAN 注入不在 037 範圍（R-SEC8(g)），故**本包未據此產 TC**；請 Pei 裁是否開 CAN 注入之 TC 群 |
| 4 | Z1 `TestKeyMaterial/rawkeys/` 三個 client：**`Logdog`／`AndroidAuto`／`SWDL`** | 037 KI 各列未列舉 client 名；`SWE1-LOGENC-008` 只提 KeyInstall 為 RSA 金鑰來源 | **`AndroidAuto`** 為 037 未見之第三個金鑰客戶。登記備查 |

---

## 6　升級條件逐項核對（§4）

| # | 條件 | 實測 | 判 |
|---|---|---|---|
| 1 | zip 解壓失敗或含非預期型態（大量 binary）| Z1 35 檔／Z2 4 檔，全為 `.py`／`.md`／`.sh`／`.p8`／`.pyc`；**無大量 binary**；apk 未動 | **不成立** |
| 2 | 補值任一處非逐字 | 16 行之補值逐行附 `source:`（檔名＋行號或 CCVR 列號），見 3-1／3-2 | **不成立** |
| 3 | `RESOLVED` > 60 行 | **16 行** | **不成立** |
| 4 | lint 任一門檻非 0 | `P`／`X`／`SC`／`SS` 全 0 | **不成立** |

**四項皆不成立，本包完成上繳，不升級。**

---

## 7　自報錯誤與偏差

| # | 事項 | 性質 | 處置 |
|---|---|---|---|
| 1 | **SEC-06 §5-1 之 `asset_request` 逐 token 行數有誤** | **執行層前包錯誤** | 該表之 `X-g` 記 11（實為 **36**）、`X-h` 記 15（實為 **19**）—— 我把 SEC-05 之舊值**手抄**進 SEC-06 之表，未自 `pending_summary.tsv` 取。**總數 128 正確**，逐 token 分佈錯。本包之表已自檔案重算 |
| 2 | §2.1 之 Java／`@Test` 前提不成立 | **下放包前提有誤** | 三 zip 為 pytest 套件；改抽 pytest 函式 ＋ 其常數內之 `am instrument` 全式（2-1）|
| 3 | §2.3 假設 Z2／Z3 含測試憑證 | **下放包前提有誤** | 實測無憑證、無 wrong-* 方法；`X-e` 系列 0 解（3-3）|
| 4 | `RESOLVED` 16 行，低於 §2.6 預估之 30~45 | 預估與實測差 | 主因為第 3 項（`X-e` 系列落空 7 行）與 `X-n` 之通道清單問題（4 行）；逐代號成因見 §3 |
| 5 | `apk_pairing.tsv` 新增兩列 | 下放包未明令 | R-SEC15(g) 之必要條件，否則 `selfcheck` (g) 報 6 處。`basis` 逐字記 Z1 行號（4-1）|
| 6 | `X-n` 找到 P1 寫法但未套用 | **需 R-SEC7(a) amend** | `Start the suspend-resume boot sequence` 不在 PHYS 閉合清單；且 power_moding 自身之 S2R 觀察面亦為 PENDING。執行層不自裁（3-4）|
| 7 | 解壓物留在 `_intake/Security/unzip/` | 工作區殘留 | `_intake/` 在 `.gitignore` 內，不進版控；未投遞 `sources/raw/` |
| 8 | `delivery/` 之 20260916 本已刪 | R-TM13 之形式牴觸 | 該本為 SEC-06 之交付候選草稿，已被 20260917 本取代；**`merged/security_v02.xlsx` 仍在**，可回溯。若 Pei 要保留舊交付候選，可自 v02 重生 |
