# 上繳包 GC-08 —— `body_kind` 閘入 `DOCS_CHECKS`、sw_update 交付紀錄

日期：2026-09-05　層級：Tier 1 → Tier 2
對應下放包：`down/20260905_GC-08.md`

三件任務全數執行。**`gates_tsv` 由 FAIL 轉 PASS**（56 閘），`body_kind` 已回到登錄簿且逐欄與原人手列相同。
`RULINGS.sha.tsv` 未動。

> **三件請先看**：
> 1. **`body_kind` 在本包開工前已被除名**（2-1 節）。GC-06 警告的傷害在我讀審閱期間發生了 ——
>    有併行 session 跑了 `gates_tsv` 重生（48→56 行），把那支人手列洗掉。
>    本包的修補因此同時是**復原**：現行 57 行／56 閘，`body_kind` 回來了。
> 2. **本包前提有兩項不成立**（4-1 節）：下放包 §一-3 寫「預期 48→56」、§三 寫
>    「`lint_paths` 0（Pei 已跑搬移）」。實測起點是 **56 行（已被重生過）**，
>    `lint_paths` 是 **6**（搬移未跑，另有 4 個新 `.json` 報告）。
> 3. **我又犯一次 R-G50(c)**（5-2 節）：無旗標跑了 `scripts/selfcheck_map.py`，它會寫檔。
>    已 `git checkout` 還原並複核。

---

## §1　產物與 SHA256

| # | 檔 | bytes | SHA256 |
|---|---|---:|---|
| 1 | `scripts/gates_tsv.py` | 11,684 | `4afd0af9f80dbb7a7bb13dc3b3c3efd972c3bf85e57641db01243a9c2be5e87d` |
| 1 | `docs/runtime/GATES.tsv` | 12,093 | `95d8d537de5230d723a600128da572452e942c637374773c3f46aed56601ccad` |
| 2 | `features/sw_update/RULINGS.md` | 114,122 | `42a42aa6ccbfaedeeea1590f8840142f9397d433acfb50d05d6168ea387634f1` |

`git diff --numstat`：`38 10` gates_tsv.py、`10 1` GATES.tsv、`23 0` sw_update/RULINGS.md。

### 1-1　工具寫入 repo 內之路徑清單（R-G50(c)）

| 工具 | 旗標 | 寫入 repo |
|---|---|---|
| `scripts/gates_tsv.py` | 無（**本包之准寫例外**，下放包前言「`GATES.tsv` 由本包重生（Pei 已准）」）| **`docs/runtime/GATES.tsv`** |
| `scripts/gates_tsv.py --out <工作區>` ×2 | 乾跑 | 無 |
| `scripts/gates_tsv.py --check`／`selfcheck_map.py --check`／`gate_all.py`／`lint_paths.py` | 唯讀 | 無 |
| **`scripts/selfcheck_map.py`（無旗標）** | **誤跑，見 5-2 節** | `docs/runtime/SELFCHECK_MAP.tsv`（**已還原**）|

`RULINGS.sha.tsv` 全程未動。

---

## §2　任務 1 —— `body_kind` 閘

### 2-1　先量：**它已經不在了**

| 量 | 值 |
|---|---|
| `DOCS_CHECKS` 七鍵 | `docs_structure`／`ledger_id`／`ledger_status`／`ledger_text`／`ledger_location`／`ledger_series`／`table_row` —— **不含 `body_kind`** |
| HEAD 之 `GATES.tsv` | 48 行，**有** `body_kind`（人手列）|
| **開工時之工作區** | **56 行，`body_kind` 已消失**；`gates_tsv --check` 當時 **PASS（55 閘）** |
| `body_kind` 閘之實作 | `grep -rn body_kind scripts/ tests/` 只命中 `rulings_hash.py`（欄名、`body_kind == section` 之判定），**`lint_docs036.py` 內查無同名檢查** |

即：**GC-06 §三-1 所預告的除名，在本包開工前已由併行 session 之一次重生完成**。
`--check` 之所以 PASS，正因為檔已與（不含該閘之）產生器一致 —— 閘沒了，檢查器不會抗議。

**另具名**：`body_kind` 之判定實作在 `rulings_hash.py`，不在 `lint_docs036.py`，
而登錄簿把 `owner` 記為 `lint_docs036`。本包**照人手列逐字保留**（下放包 §一-2 之明文），
未改 owner；**該欄與實作不符，請分析層裁**。

### 2-2　改：`DOCS_CHECKS` ＋ 逐欄覆寫表

`DOCS_CHECKS` 補 `body_kind` 一鍵；另加 `DOCS_GATE_OVERRIDES` ——
`docs_gate_rows()` 對其餘七鍵給同一組通用欄值，而 `body_kind` 之人手列不是通用值
（有來源裁決、生效日、未校準狀態與命中數 48/48），故以該表逐欄覆寫。
**欄值逐字取自原人手列**，不改寫。

### 2-3　`source_ruling` 仍書 `R-G29`，具名

人手列之 `source_ruling` 為 `R-G29 [DEFAULT]` —— 該號在 GC-03 之 R-G43 改號後，
FO 側已成 **`R-G68`**。本包**不改**，理由：
(a) 下放包 §一-2 明文「與人手列逐欄相同」；
(b) R-G43 改號經 Pei 裁定**只及於「在效檔」**，`docs/runtime/GATES.tsv` 不在該清單內
（GC-03 之三類分子分母，該檔落 `OTHER`）。
**此為已知之舊號殘留，請分析層裁是否納入下一輪改號範圍。**

### 2-4　重生與驗證

先 `--out <工作區>` 比對再寫（下放包 §一-3 之順序）：

| 比對 | 結果 |
|---|---|
| 重生 vs **開工時之工作區**（56 行）| **+1：`body_kind`**，其餘逐列相同 |
| 重生之 `body_kind` 列 vs **HEAD 之人手列** | **逐欄相同**（`diff` 空）|
| 重生 vs HEAD（48 行）| +9：`I-cross`、`W`、6 支 feature selfcheck 腳本、`body_kind` 位置移動 |

符合後才寫。寫後 `docs/runtime/GATES.tsv` **57 行／56 閘**，
`gates_tsv --check` **OK（56 閘）**。

### 2-5　`selfcheck_map`：缺對映之閘（**不自造**）

`selfcheck_map.py --check`（唯讀）**FAIL** —— 惟其成因**不是本包**：
併行 session 之 `scripts/lint036.py` 新增了閘 `X`，而 `SELFCHECK_MAP.tsv` 尚無該項。

`GATES.tsv` 56 閘中，短 id 之閘未被 `SELFCHECK_MAP` 對映者 **12 支**：
`C`、`I`、`I-cross`、`J`、`K`、`L`、`P`、`Q`、`T`、`V`、`W`、`X`。
已對映者 11 支：`A`、`B`、`D`、`E`、`F`、`G`、`H`、`M`、`N`、`R`、`U`。
**本包不造任何對映**（下放包 §一-4 明文），列表待裁。

---

## §3　任務 2 —— sw_update 交付紀錄

`features/sw_update/RULINGS.md` 末加一節「交付紀錄（2026-09-05 補登）」，**+23 行**，
逐項照 GC-06 審閱 §一-3：檔名、落點、入版控 commit `ef3ade0`、日期 2026-09-01、
現行 sha256 全值 `4f6caa35f2a71ca6fa514bf3c6acfaaba98b01b6607c63f6ba0128085203727b`、
以及逐字之「**無登記前 sha 可對，曾否變更不可證（A-SU7）**」。

**未書「未變更」**（`grep -c "未變更"` = 2，兩處皆在「**不得**據此書『未變更』」之否定句內）。
另明寫盲區：「未變更」與「變更過而無人記錄」在現有證據下不可區分（R-G11）。

**未取條號**：本節為紀錄非裁決，R-G62 之取號只用於立條；下放包 §一-2 之「取現場號若立條」之條件不成立。

---

## §4　任務 3 —— `gate_all` 全跑

| 閘 | 對照向 | 交付向 | 歸因 |
|---|---|---|---|
| `lint_docs036` | PASS | PASS | — |
| `canon_refs` | 158 | **158** | 本包 **+0**。仍紅者為既有 `section` 型（waiver 外 unresolved 18／ambiguous 140）|
| `rulings_hash` | PASS（766） | PASS（766） | 本包未動 |
| **`gates_tsv`** | **FAIL** | **PASS（56 閘）** | **本包任務 1** |
| `lint_paths` | 6 | **6** | 本包 +0；逐處歸因見 4-1 |
| `lint_delivery_spec` | PASS | PASS | — |
| **總判** | 3 支未過 | **2 支未過** | — |

### 4-1　下放包之兩項前提不成立，逐項具名

| 下放包所載 | 實測 |
|---|---|
| §一-3「預期 48→56」 | 起點**不是 48**：開工時工作區已是 **56 行**（併行 session 已重生過並除名 `body_kind`）。本包之實際變化為 **56 → 57 行（55 → 56 閘）**，淨 +1 即 `body_kind` |
| §三「`lint_paths` 0（Pei 已跑搬移）」 | **實測 6**。`features/driver_distraction/workbook/` 二檔仍在（`gc06_driver_distraction_mv.sh` **未執行**），另有 **4 個 `.json`** 落 `features/*/reports/` |

`lint_paths` 6 之逐處：

| 處 | 歸因 |
|---|---|
| `driver_distraction/workbook/*.xlsx` ×2 | GC-06 之 mv 腳本未跑（Pei 動作）|
| `bed_lowering`／`power`／`vehicle_category`／`vehicle_setting` 之 `reports/*_20260905.json` ×4 | **併行 session 於 21:08 產生並已 `git add`**，非本包 |

### 4-2　順帶量到之政策衝突（請裁）

`lint036` 產出之報告為 `.md` ＋ `.json` **成對**；R-G47 令 feature 級報告一律落
`features/<f>/reports/`；而 `lint_paths` 之落點政策對 `.json` 只允許
`data`／`generated`／`inputs`／`sandbox` —— **`reports/` 不在其內**。
於是「照 R-G47 放對地方」與「通過 `lint_paths`」**不可兼得**：
每產一對報告就固定多一個紅。四個新紅正是此形態，且會**隨每次跑 lint 累加**。
本包不改政策（非授權），列為請裁。

---

## §5　自檢與自報

| # | 項 | 結果 |
|---|---|---|
| 1 | `GATES.tsv` 先比對後寫 | **PASS**（2-4 節，`--out` 乾跑二次）|
| 2 | `body_kind` 逐欄與人手列相同 | **PASS**（`diff` 空）|
| 3 | 不自造 selfcheck 對映 | **PASS**（2-5 節只列表）|
| 4 | `RULINGS.sha.tsv` 未動 | **PASS** |
| 5 | pytest | **紅 10 → 9**（`test_gates_tsv::test_check_detects_drift` 轉綠）、綠 1314 → 1315 |
| 6 | `ruff` | `gates_tsv.py` 1 → 1，**+0** |
| 7 | 對照向回報被移出檔數（R-G50(b)）| **PASS** —— 3 |
| 8 | 未寫「未變更」 | **PASS**（§3）|

### 5-1　pytest 之紅減一，具名

`tests/test_gates_tsv.py::test_check_detects_drift` 由紅轉綠 —— 它本就是在測
「`GATES.tsv` 與產生器不符時 `--check` 要抓到」，而登錄簿一直不符。
本包令二者一致，該測試遂綠。**其餘 8 紅**（`test_intake_scaffold` ×6、`test_single_write_path` ×2）
與本包無涉、對照向同名。

### 5-2　**自報：又一次 R-G50(c) 違規（已還原）**

任務 1-4 要「回報缺對映之閘」時，我直接跑了 `python3 scripts/selfcheck_map.py`
—— **該工具無旗標即寫 `docs/runtime/SELFCHECK_MAP.tsv`**（與 `rulings_hash.py`、`gates_tsv.py` 同形）。
發現後 `git checkout -- docs/runtime/SELFCHECK_MAP.tsv` 還原並複核 `git status` 為空，
其後改用 `--check`。

**這是 GC-03 9-2 節同一形態之第二次犯**，而 R-G50(c) 正是為此而立的條 ——
**條文立了，我還是犯了**。差別只在這次「知道要立刻查、立刻還原」。
可測之防法（供分析層裁，本層不自立條）：包內凡點名某工具者，
下放包直接寫明該工具之**唯讀旗標**，執行層不得自行推定；
上繳之「工具寫入路徑清單」由「事後回報」改為「逐次執行前先聲明」。

### 5-3　**並行寫入之現況（影響 commit，請 Pei 先看）**

本包作業期間，併行 session（GC-07 線）持續在動同一個 repo：

- **已 `git add`（staged）19 檔**，其中含 **`docs/fw036/RULINGS_LEDGER.md`** 與
  **`docs/fw036/RULINGS.sha.tsv`** —— 這兩檔**帶著本層 GC-06 尚未提交之改動**
  （`R-G36(b)`／`R-G43(d)` 二條與其指紋）。
- 期間並曾使 `scripts/gates_tsv.py` 一度回到 HEAD 狀態（本層之 `DOCS_GATE_OVERRIDES` 在一次檢查中消失、
  下一次又在），故本包每一步寫入後**立即複驗**，不靠前一步之結論。
- `docs/runtime/SELFCHECK_MAP.tsv` 於本層還原後 **21:16 再度被寫入並 staged**，非本層所為。

**後果**：任何以目錄形 pathspec（如 `-- docs/fw036`）之 commit，都會把兩線之改動混進同一顆。
**建議 commit 逐檔**，且在下 commit 前重看一次 `git status`。

### 5-4　未做且具名

| 項 | 為何未做 |
|---|---|
| `SELFCHECK_MAP.tsv` 之 12 支缺對映 | 下放包明文「不自造對映」|
| `GATES.tsv` 之 `body_kind` owner 欄（記 `lint_docs036`，實作在 `rulings_hash.py`）| 逐欄照人手列（2-1 節末），待裁 |
| `source_ruling` 之 `R-G29` → `R-G68` | 2-3 節，待裁 |
| `lint_paths` 之 4 個 `.json` 紅 | 併行 session 之產物；且成因為政策衝突（4-2 節），待裁 |
| `driver_distraction` 搬移 | Pei 動作（`git mv`）|

### 5-5　請裁四項

1. **R-G47 與 `lint_paths` 之 `.json` 政策衝突**（4-2 節）—— 每跑一次 lint 就多一個紅。
2. **`body_kind` 之 `owner` 欄**：記 `lint_docs036`，實作卻在 `rulings_hash.py`。
3. **`source_ruling` 之舊號 `R-G29`**：是否把 `docs/runtime/*` 納入下一輪改號範圍。
4. **`SELFCHECK_MAP` 之 12 支缺對映**（含併行線新增之 `X`）。
