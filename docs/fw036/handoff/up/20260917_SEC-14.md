# 上繳包 SEC-14 —— 收尾 chore：`gen_batch2.py` 修法 ＋ 指南 §1.1 殘留 ＋ A-SEC-15（零 TC 變更）

執行層，2026-09-17。對應下放包 `down/20260917_SEC-14.md`。

**結論**：四項全成，**零 TC 變更已以 sha 證實** —— 三支生成器 ＋ 合併 ＋ 交付候選全部重跑，
五個 xlsx 之 sha **前後完全相同**（`merged/security_v08.xlsx` 仍 `bed94fbba2940f96`）。
`EXEC_GUIDE.md` 重生（329 行，新 sha `c9f7b58b98ab2a18`），兩項驗證仍 **58／58** 與 **10／10** = 100%。
台帳三處落檔；`R-SEC24` 之 body sha **未動**（`9e93e2c0`，註在 fence 外）。
§5 升級條件兩條皆不成立。

**§4-1 無法回報**：SEC-12／13 之四支 commit **尚未落**（工作樹仍持有 SEC-12／13／14 之產物）。
`git log -4 --oneline` 之現況為並行 Diagnostics session 之三支 ＋ Security 之 `8f58744`（SEC-11）：

```text
162efb4 feat(diagnostics): CDD-02 pilot01 — 13 TCs, R-G75/R-DIAG10, row-kind classifier fix
5c3885b docs(diagnostics): correct CDD-01_A row counts and record the RULINGS.sha.tsv overwrites
b432893 feat(diagnostics): CDD-01_A — 037 V1(3) redirect, seven amends, profile lint
8f58744 docs(security): SEC-11 package, submission and lint reports
```

---

## 一、`gen_batch2.py`（§1）

`scope` 之排除集合 `("D",)` → `("D", "CONVERT_DOC")`，並照下放包所令加英文註：

```python
    # batch 2 之範圍 = CCVR 第二批且非 D。
    # CONVERT_DOC rows are produced by gen_deferred.py (batch 3)
    # —— R-SEC23（SEC-11）改該欄後不排除即誤報（SEC-13 §5-3 自報；SEC-14 §1 定案）。
    scope = {k for k, r in bo.items() if r["ccvr_batch"] == "2"
             and r["disposition"] not in ("D", "CONVERT_DOC")}
```

### 1-1　三支重跑之 sha 表（§4-2）

| 檔 | before | after | 判 |
|---|---|---|---|
| `batch1/security_batch1_v08.xlsx` | `e7e2eba857bd96f6` | `e7e2eba857bd96f6` | ✅ 相同 |
| `batch2/security_batch2_v08.xlsx` | `310a3769814997a2` | `310a3769814997a2` | ✅ 相同（亦同 v04／v05）|
| `batch3/security_batch3_v01.xlsx` | `080db8a4daf70167` | `080db8a4daf70167` | ✅ 相同 |
| `merged/security_v08.xlsx` | `bed94fbba2940f96` | `bed94fbba2940f96` | ✅ 相同（§0 指定值）|
| `delivery/…_SWQT_Security_20260917.xlsx` | `ca34f2253a1c1ba3` | `ca34f2253a1c1ba3` | ✅ 相同 |

跑法：`SEC_VER=v08 gen_batch1 → gen_batch2 → gen_deferred → build_merged → build_delivery`
（五支依序，全部 exit 0；`gen_batch2` 之斷言不再誤報）。124 TC、`coverage.tsv` 70／70、`D` 群 0 不變。
v08 lint 重跑：`U=0 H=0 K=0 P=0 X=0 SC=0 SS=0`、`W=4`。

> 附帶證實：`surgical_save` 對相同輸入之輸出**逐字節可重現**（五本皆同 sha），故「sha 不變」
> 可作為「零 TC 變更」之硬證據，不需逐格比對。

## 二、`build_exec_guide.py` §1.1（§2）

apk 檔名改自 Pre-Condition 抽取（不寫死）：

```python
    # §1.1 之 apk 檔名一律自 Pre-Condition 抽取，不寫死（SEC-13 review §二）
    apk_names = sorted({m.group(1) for p_ in pre_tcs
                        for m in [re.search(r"Test runner (\S+\.apk) is installed", p_)] if m})
```

### 2-1　§1.1 引言區塊逐字（§4-3）

```text
> The APK file name appears in the workbook only as `CertProviderAndroidInstrumentalTest.apk`
> (Pre-Condition above). For the KeyInstall and KeyMaster runners the workbook names the
> instrumentation class but no APK file, so install them from `<apk provided by RD>`.
```

抽出值 = **1 個**（`CertProviderAndroidInstrumentalTest.apk`）；若日後 Pre-Condition 出現多個 apk，
該句自動列為 `` `a.apk` / `b.apk` ``，空集合則為 `` `<none>` ``。

### 2-2　指南現況與兩項驗證

| 項 | 值 |
|---|---|
| 行數 | **329** |
| sha256[:16] | **`c9f7b58b98ab2a18`**（SEC-13 版為 `090b1b62158bb541`）|
| ⊆（§1.6 以外）| **58／58 = 100%** |
| §1.6 每行含 ` — ` 來源 | **10／10 = 100%** |
| 交叉核對 | `apk_pairing` 6／6；`diag_items` 3／3；`step_assets` 30／41（其餘之出處已於句內說明）|

## 三、台帳三處 diff（§4-4）

| 檔 | diff |
|---|---|
| `ANOMALIES.md` | **＋1 列** `[A-SEC-15]`：CCVR `Cert Val CS.98` STEPS item 2／5 逐字寫 `adb install -r -t CertProviderServiceManagerTest.apk`，目錄實檔為 `CertProviderAndroidInstrumentalTest.apk`（`down/20260916_SEC-01.md` 第 125 行）；工作簿依實體檔案（R-SEC24），**請 Steven 確認交付檔名**；A-33 併此改定性為 A-33′ |
| `RULINGS.md` | R-SEC24 條下**加註（fence 外）**：`A-33 reclassified: source exists in CCVR STEPS; R-SEC24 kept on physical-file evidence`。**body sha 未動**：`R-SEC24 = 9e93e2c0`（`--gate` 通過，35 錨點）；錨點 span 由 12 → 14 行（span 非 hash 面）|
| `delivery/asset_request.md` | `X-f-2` 段加 `Steven to confirm CertProvider apk file name (A-SEC-15)`（改 `build_asset_request.py` 之 `ASSET` 表後重生，非手改）。新 sha `205ac51e443a9801`（前 `18eeae20e253a82e`）；96 佔位／11 token 不變 |

## 四、自報錯誤（§4-5）

1. **`gen_batch2.py` 之修法本包實為「追認」**：該修法在 **SEC-13 執行中**即已套用（否則 v08 產不出來），
   本包只是照下放包把註解改為其指定之英文句並補跑驗證。時序上「SEC-13 自報 → SEC-14 定案」，
   非本包新修。
2. **`gen_pilot01.py`／`gen_pilot02.py` 仍持有舊 apk 檔名常數**（`CertProviderServiceManagerTest.apk`）。
   依 R-TM13 不回修已被取代之 pilot；惟若日後有人重跑 pilot 生成器，會產出與 v08 不一致之前提。
   建議由分析層裁：加一行 deprecation 註或刪除兩支腳本。本包未動。
3. **§4-1 無法回報 commit hash**：非執行層可補 —— 四支 commit 待 Pei 落。SEC-14 之產物亦未 commit。

## 五、§5 升級條件之逐項判定

| 條 | 判 |
|---|---|
| 任一 xlsx／JSON sha 改變 | **不成立**：五本 xlsx 前後同 sha；JSON 亦由同一輪生成（內容決定於同一輸入）|
| 兩驗證任一 < 100% | **不成立**：58／58、10／10 |

## 六、git

未執行 commit／push。SEC-12／13／14 建議五支：

```text
feat(security): SEC-12 — R-SEC22(a) amend, A-SEC-11~14, EXEC_GUIDE and security_v07
docs(security): SEC-12 package and submission
feat(security): SEC-13 — R-SEC24 apk file name, EXEC_GUIDE environment knowledge, security_v08
docs(security): SEC-13 package and submission
chore(security): SEC-14 — batch2 scope filter, guide APK name extraction, A-SEC-15
```
