# 上繳包 VS-CF-01 —— CFTS044 甲類 13 列結案、DR-18 錯號更正、R-VS102 落檔

下放包：`docs/fw036/handoff/down/20260907_VS-CF-01.md`（Pei「是準」2026-09-07）
執行日：**2026-09-07**。結果分類（FO §8.4）：**全部完成**，另有 **3 項具名偏差**（§7）。

---

## §0　一句話

§〇 之量測三件（母本 sha、repo 命中、DBC／PROXI 綁定）**全數與下放包相符**，未觸停手條件；
13 列 ＋ 順帶 10 列 Remarks 全數施作，**改動 73 個儲存格、23 列，列數 252 不變**；
`surgical_save` 落檔，42 個 zip member 僅 `sheet6.xml` 相異、data-validation 計數不變；
**PENDING 25 → 12**，與下放包 §五-4 預期之組成（DR-15 ×10、DR-21 row 28、DR-26 row 35）逐列相符。

3 項偏差皆為下放包之筆誤或無法照辦處，已就地更正並具名：§四草稿首列 ObjectID 錯記、
§2.7 之殘量算術、「跑本線 lint」之對象不存在。**無一項改變處置本身。**

---

## §1　量測與 sha（§〇、§五-1）

| 項 | 下放包所載 | 實測 | 判 |
|---|---|---|---|
| 母本 sha16 | `f39c03bd9cb0f1b3` | `f39c03bd9cb0f1b3`（121,238 bytes） | ✅ |
| 母本分頁／列 | `Test Case Specification 測試用例規範` row 10–252，243 資料列 | 同（max_row 252，F 欄非空 243 列） | ✅ |
| `inputs/` 同名檔 | sha16 `ebe5a65f30a0d4bc`，108,225 bytes，非本簿 | 同 | ✅ |
| repo `**/*CFTS044*Vehicle Controls*.xlsx` 排除 `inputs/` | 命中 0 | 命中 0（唯一命中即 `inputs/` 該骨架） | ✅ |
| DBC | `forms/PDT27_E2A_R1_BHCAN2.dbc` sha16 `46cb73f3db62ac9f` | 同 | ✅ |
| 對照 DBC | `inputs/PDT27_E2A_R4_BHCAN.dbc` sha16 `9ef1ec98…` | `9ef1ec9830fc8018` | ✅ |
| PROXI | `forms/PROXI_HDCC27_R3_20250424.xlsx` sha16 `e7c2020f01c3d58d` | 同（743,785 bytes，分頁 `Format`） | ✅ |

**三件產物 sha256（§五-1）**

```
母本      f39c03bd9cb0f1b30cc08b9f447a2a53ef0bbc9b70f5755ea911a52bc17ca924
          /Users/peihe/Work/02_Project_R1LR/10_Reviewing/…_CFTS044_Vehicle Controls_20260819.xlsx
沙盒副本  f39c03bd9cb0f1b30cc08b9f447a2a53ef0bbc9b70f5755ea911a52bc17ca924   ← 與母本逐位元相同
          features/vehicle_setting/sandbox/cfts044/cfts044_20260819.xlsx
Revise1   f444028d1cd93396738b4225ef6ce5de0ad3d41675d66f16acb0d099c8ef44a1
          features/vehicle_setting/sandbox/cfts044/cfts044_20260819_Revise1.xlsx
```

母本**未被開啟寫入**（只 `shutil.copy2` 讀出），落檔後複驗 sha 未變 —— R-G72 守住。

### 1-1　DBC 7 支訊號兩本逐字相符之證

`SG_` 定義與 `VAL_` 逐字比對（`\r\n` 正規化後）：

| 訊號 | R1_BHCAN2 | R4_BHCAN | bit 寬／值域 | `VAL_` |
|---|---|---|---|---|
| `FL_HS_STATSts` | `7\|2@0+ (1,0) [0\|3]` | `7\|2@0+ (1,0) [0\|3]` | **同** | **逐字同**：0 Heated_seat_off／1 low／2 medium／3 high |
| `FR_HS_STATSts` | `2\|2@0+ (1,0) [0\|3]` | 同 | 同 | 逐字同 |
| `FL_VS_STATSts` | `4\|2@0+ (1,0) [0\|3]` | 同 | 同 | 逐字同：0–3 Vented_seat_off／low／medium／high |
| `FR_VS_STATSts` | `15\|2@0+ (1,0) [0\|3]` | 同 | 同 | 逐字同 |
| `HSW_StatSts` | `13\|1@0+ (1,0) [0\|1]` | 同 | 同 | 逐字同：0 OFF／1 ON |
| `EngineSts`（msg 994） | `49\|2@0+ (1,0) [0\|3]` | 同 | 同 | 逐字同：0 Engine_Off／1 Engine_Cranking／2 Engine_On／3 SNA |
| `CmdIgnSts`（msg 1132） | `15\|3@0+ (1,0) [0\|7]` | 同 | 同 | 逐字同：0 Initialization／1 IGN_LK／3 ACC／4 RUN／5 START／7 SNA |

**唯一差異在接收節點清單**（R1 為 `ETM,LTM`；R4 為 `BCM,ICS,SGW` 等）—— 不屬「bit 寬與 `VAL_`」，
下放包之敘述成立。附帶佐證兩條處置：

- `CmdIgnSts` 之 raw **2 與 6 未定義**，`START` 確為 `5`（R-VS102(4) 成立）；
  `IGN_OFF_ACC` 於六個 label 中**確無對應**（R-VS102(4) 後半、row 28 維持 PENDING 成立）。
- `HSW_StatSts` 1 bit **0/1 皆已定義**（R-VS102(2) 於 row 225 成立）。

### 1-2　PROXI 三列之證（分頁 `Format`）

| 列 | Parameter Name | Start/Stop Bit | Coding | Table | Sales Code |
|---|---|---|---|---|---|
| 692 | `Display_OFF_SoftKey` | 5–5（1 bit） | `Table` | `0 = Absent／1 = Present` | `0 = Set to／1 = Never` |
| 760 | `Heated_Seat_Levels` | 6–7（2 bit） | `Table` | `0 = 1 Level／1 = 2 Levels／2 = 3 Levels` | `0 = Never／1 = Never／2 = Set to` |
| 780 | `Headrest_Dump_Present` | 1–1（1 bit） | **`Not Used`** | `0 = Absent／1 = Present` | （空） |

- 列 760 **raw 3 未定義** —— row 62 之改寫（R-VS102(1)）成立。
- 列 760 之 Sales Code `2 = Set to` 佐證下放包以 `= 2 (3 Levels)` 作有效基線之選擇。
- 「HDCC27 標 Not Used／Set to 0」：列 780 之 `Coding = Not Used`（逐字），
  列 692 則是 Sales Code `0 = Set to`（即設為 raw 0 = Absent）。二者敘述皆有據。

---

## §2　13 列改寫前後 diff（§五-2）

**逐列逐欄全文 diff：`features/vehicle_setting/reports/vs_cf01_diff.txt`**（269 行，由
`vs_cf01_selfcheck.py` 讀母本與 Revise1 現算，非施作腳本之自述）。

改動列（23）與下放包指定完全一致，無溢出：

```
10 11 12 13 14 15 16 25 26 28 44 62 63 64 68 79 80 84 151 155 166 170 225
```

| § | 列 | 改動欄 | 儲存格數 |
|---|---|---|---|
| 2.1 兩態補無效 raw | 63、79 | L、M、AH | 6 |
| 2.2 不生成並具名 | 64、80、151、166、225 | I J K L M P R 清空 ＋ AH | 40 |
| 2.3 row 62 改對 PROXI | 62 | J、K(不變故未計)、L、M、R(不變故未計)、AH | 4 |
| 2.4 DR-19 | 14、15（L/M/AH）、10–13（AH）、16/68/84/155/170（AH 前綴） | L、M、AH | 15 |
| 2.5 DR-21 | 26（M、AH）、28（L、AH） | L、M、AH | 4 |
| 2.6 別名 | 25、44 | M、AH | 4 |
| **合計** | **23 列** | | **73** |

（row 62 之 K 欄原即 `NA`、R 欄原即 `負向測試 (Negative / Invalid)`，逐字相符故無 diff；
下放包令其「改為」該值，等同確認 —— 已核對，非漏做。）

要點抽樣：

- **row 63／79**：L/M 之 step 2 由 `PENDING: DR-18` → `= 2 (Heated_seat_medium)`；
  step 1／3 逐字不動，步數仍 3 對 3。
- **row 64／80／151／166／225**：I/J/K/L/M/P/R 全清空，**N 欄保留**（`CFTS044-4858308` 等），
  形制與既有 row 56（`NOT GENERATED: B4-preamble`）同。row 225 原 L 欄之 `HSW_STATSts`
  大小寫問題隨清空消滅（DBC 正名為 `HSW_StatSts`，見 §7-3）。
- **row 62**：L/M 由 3 步改為 4 步（兩者同時改，步數相等）；step 3 之 `= 3` 無 label，
  依 §8.7.5 未造 label。
- **row 14／15**：原 step 內之繁中說明句（`EngRun_Stat 之規格值 … 無法決定滿足不等條件之匯流排值`）
  隨改寫消滅。
- **row 16／68／84／155／170**：AH 僅換前綴 `BLOCKED: DR-19 —— ` → `dr_dependent = DR-19 —— `，
  餘文逐字不動（施作前 assert 前綴，不符即停手）。
- **row 10–13**：AH 原為 `None`（施作前 assert），寫入 `dr_dependent = DR-19（R-VS102(3)）…`。

### 2-1　落檔前之硬 gate（全過）

1. **L／M 步數相等**：對仍有步驟之 8 列（14、15、25、26、28、44、62、63、79）逐列 assert。
2. **PENDING = 12**：不符即 `AssertionError`，不落檔。
3. **列數 252 不變**：本包不刪列，selfcheck 複驗。
4. **`surgical_save` 結構驗證**：

```
sheets_patched : {'Test Case Specification 測試用例規範': 73}
members        : 42 個 zip member，differing 僅 ['xl/worksheets/sheet6.xml']
dv_counts      : sheet5 (1, 0)、sheet6 (3, 0)   ← classic／x14 計數兩側相等
```

全域**無 `Workbook.save()`**（`vs_cf01_apply.py` 只 import `surgical_save`）。

---

## §3　落檔行號（§五-3）

| 標的 | 位置 | 備註 |
|---|---|---|
| `features/vehicle_setting/RULINGS.md` | **行 7650–7688**（`### R-VS102`；檔尾，總 7688 行） | 落檔前重查 live 最大號 = `R-VS101`，`R-VS102` 未被占用，無需順延 |
| `features/vehicle_setting/DATA_REQUESTS.md` | **行 337–343**（DR-18 節末之錯號更正引言塊，緊接 `## DR-19` 之前） | 本 DR 之內容與狀態未動 |
| 同上 | **行 1939–1965**（`## DR 草稿 —— 「All other states…」`，檔尾，總 1965 行） | **未取號**，狀態「未送出」 |
| `features/vehicle_setting/data/can_signal_map.tsv` | **第 51 列** | 見 §5 |
| `features/vehicle_setting/output/MANIFEST.tsv` | 第 3、4 列 | 沙盒副本 `source-copy`；Revise1 `writeback-pending-DR` |

⚠ `features/vehicle_setting/output/` 在本 feature 之 `.gitignore` 第 18 行內，`MANIFEST.tsv` **未受版控**（既有之 VF230 列亦然）—— 該 2 列之登錄只在工作區，不隨 commit 留痕。沙盒二簿則入版控（`sandbox/vssl/` 既有先例）。

`R-VS102` 之條文逐字照下放包 §一，另加「執行層註」4 點（施作範圍、(1) 之「兩態」判準取自條文
而非訊號寬度、(5) 之別名早有據、(6) 之 8 列 vs 全庫 20 列之區辨）。

---

## §4　lint（§五-4）

**`vs_sl03_lint.py` 無法對本簿跑** —— 其 `BOOKS` 綁 vf230／bl／vc 三簿，
列母體自 `*_settings_dryrun_v3.tsv` 取（CFTS044 無此報告）。
故新寫 `vs_cf01_lint.py`，**三項硬規之 regex 逐字取自 `vs_sl03_lint.py`**
（`PAREN_TAIL`／`UNQUOTED`／`UNQUOTED_ER`），列母體改以 F 欄非空之資料列取。

```
Revise1：cfts044_20260819_Revise1.xlsx
資料列 243；本包觸及 23 列

test_item 無括號下半   觸及列 0 PASS ｜全簿   0
行尾句號              觸及列 0 PASS ｜全簿   0
設定項未加雙引號        觸及列 0 PASS ｜全簿   0
全形分號              觸及列 0 PASS ｜全簿  28

PENDING gate：25 → 12  PASS
總判：PASS
```

**PENDING 12 列之組成，與下放包 §五-4 預期逐列相符：**

| DR | 列 |
|---|---|
| DR-15（×10，已送出待覆） | 71、73、86、88、157、159、172、174、233、235 |
| DR-21 | 28 |
| DR-26 | 35 |

---

## §5　別名表定位結果（§2.6）

依下放包指示先 `grep -rl "alias" features/vehicle_setting/data/` —— 只命中
`_w80_meta.json`（15 bytes，非別名表）。改以內容定位，得**兩張既有表**：

| 表 | 已載之別名 | 處置 |
|---|---|---|
| `data/can_signal_map.tsv`（14 欄：token／lid_signal／lid_can／verdict…） | 第 3 列 `$DSP_SK_PRSNT$` → `Display_OFF_SoftKey`，`lid_can = PROXI` | **已在，不動** |
| `data/lid_pairs.tsv`（LID 側，generator 產出） | 第 2684 列 `VC_HdRstPrsnt` → `Headrest_Dump_Present`，`0 = "Absent"／1 = "Present"` | 為產生器輸出，**不手改** |

`VC_HdRstPrsnt` 於 `can_signal_map.tsv` **原缺**，故補入第 51 列（依既有排序置於
`$VC_VEH_LINE$` 之前），`verdict = NOT_IN_DBC`，`detail` 具名 R-VS102(5)／R-13、
PROXI 列 780 與其 `Not Used` 標記、以及 `lid_pairs.tsv:2684` 之交叉來源。
**不新建表**，`docs/fw036/handoff/down/20260902_VS-SL-02_alias_candidates.tsv` 未動。

⚠ **token 形制之具名選擇**：該表既有 token 一律帶 `$…$` 界定符，惟條文（row 25 Test Item 逐字、
`data/leaves.tsv`）寫 `VC_HdRstPrsnt` **不帶 `$`**。為不臆造規格 token，本列取**逐字之裸名**，
並於 `detail` 欄註明。若本線約定以 `$…$` 為鍵，回審閱改 —— 該表現無 `.py` 消費者，改動無下游。

---

## §6　工具旗標與自報（§五-6）

**旗標逐次聲明**

| 動作 | 工具／路徑 | 聲明 |
|---|---|---|
| 母本 → 沙盒 | `shutil.copy2` | 唯讀母本；落檔後複驗母本 sha 未變 |
| 儲存格改動 | `openpyxl`（**計算層**） | 全域無 `wb.save()` |
| 落檔 | `backend/xlsx_surgical.surgical_save()` | 唯一授權路徑（R16／R-G3）；`verify=True` 預設 |
| 產生器 | **未跑任何會改 repo 之產生器** | `lid_parse.py`／`writeback_036.py` 等一律未動 |
| `output/` 既有簿 | **未動**；只在 `MANIFEST.tsv` 追加 2 列 | VF230 交付本 sha 未變 |
| `inputs/` | **未動** | |

**新增檔**

```
features/vehicle_setting/scripts/vs_cf01_apply.py       施作（唯一寫 xlsx 者）
features/vehicle_setting/scripts/vs_cf01_selfcheck.py   逐列逐欄 diff（唯讀）
features/vehicle_setting/scripts/vs_cf01_lint.py        lint ＋ PENDING gate（唯讀）
features/vehicle_setting/reports/vs_cf01_diff.txt       diff 全文
features/vehicle_setting/sandbox/cfts044/cfts044_20260819.xlsx          母本複本
features/vehicle_setting/sandbox/cfts044/cfts044_20260819_Revise1.xlsx  Revise1
```

**自報**

1. §四草稿之首列 ObjectID 我方**逕行更正**（§7-1），未先回問 —— 因其與下放包自載之 leaf 名互斥，
   且五筆之正解皆可自 N 欄與 `leaf_to_reqid.tsv` 唯一決定。若分析層另有本意，回審閱。
2. `vs_cf01_lint.py` 為**新寫之腳本**，非既有「本線 lint」；判準雖逐字沿用，
   但列母體之取法不同（資料列 vs dryrun 報告），此差異可能掩蓋 vf230 線 lint 之其他規則。
3. Revise1 **未進 `output/`、未進 `delivered/`** —— 出貨 gate 未過（DR-15 ×10 等 12 列 PENDING 未結）。

---

## §7　具名偏差（3 項，皆不改處置）

**7-1　§四草稿首列之 ObjectID 錯記**
下放包記 `4858317（LeftFrontHeatedSeat-006，三態）`。實測 `CFTS044-4858317` 於
`data/leaf_to_reqid.tsv:56` 屬 **`SWE1-VC-LeftFrontHeatedSeat-011`**（工作簿 row 69），
非 `-006`。`-006` 之 ObjectID 為 **`CFTS044-4858308`**（`leaf_to_reqid.tsv:51`，
亦即 row 64 之 N 欄）。**五筆一律改自工作簿 N 欄取並與 `leaf_to_reqid.tsv` 對過**，
落檔之草稿表已加 ObjectID 欄與執行層註。其餘四個 leaf 名與下放包所載逐字相符。

**7-2　§2.7 之殘量算術**
下放包稱「其餘 36 列之全形分號留 F3」。實測母本含全形 `；` 者為 **38 列**；
本包 §2.7 修 5 列（25、44、62、63、79），另 5 列（64、80、151、166、225）因清空而消滅，
故 **F3 之殘量為 28 列**，非 36。清單見 `vs_cf01_lint.py` 之全簿計數。

**7-3　row 225 之 `HSW_STATSts` 大小寫**
下放包令「隨清空消滅，上繳仍記一筆」—— 記於此：母本 L/M 欄寫 `HSW_STATSts`，
DBC（兩本皆然）之正名為 **`HSW_StatSts`**。該列已清空，故本簿內**已無**此錯字；
`R-VS102` 之條文與 §四草稿一律用正名。若他簿有同錯字，屬 S1，不在本包。

---

## §8　未結（下放包 §六，實測後不變）

- **DR-15′**：已送出待覆（10 列，見 §4 表）。
- **待 Pei 取號送出**：DR-21 逐實例（`IGN_OFF_ACC`，row 28）、DR-26（row 35）、
  DR-25′（row 245/246/249/250 不生成）、§四之新草稿（`DATA_REQUESTS.md` 行 1939–1965）。

## §9　不在本包（下放包 §七，未動）

F1（sibling 括號下半 4 組）、F2（空標籤 3 列）、F3（全形分號 **28** 列，見 §7-2）、
F4（導航路徑 14 列）、S1（標籤大小寫）、row 252 未生成 —— 待 Pei 指派。

---

## §10　審閱回應（`down/20260907_VS-CF-01_review.md`，2026-09-07）

審閱總判**通過**，獨立複算 16 項全數相符，3 項具名偏差全數獲准。以下逐項回應。

### 甲-1（須補）—— 已補

`B7-no-invalid-raw` 為新設之第七個 blocker 分類碼，原只寫進 5 列 Remarks 與 `R-VS102(2)`。
依 `R-G72(h)` 已於 **`scripts/vf230_wvf44_writability.py` docstring 之分類碼段**登記
（碼義、裁決出處、用例列號、「不在 VF230 母體內故本檔不重跑」皆具名），
並於 **`R-VS102` 執行層註第 5 點**加一行指向該登記位置。
**未重跑 writability 產生器**（該 5 列無標的），依審閱指示。

⚠ 一點具名：該 docstring 上段原文為「**不新設分類**」。新增行以
「本輪之外新設 …**上段之「不新設」係 VF230 該輪之範圍聲明，未被推翻**」措辭，
避免同一 docstring 內自相矛盾。若分析層認為該聲明應改寫而非併存，回審閱。

### 丙-1（分析層自報）—— 已補

`R-VS102` 執行層註第 1 點補入 **Revise1 sha16 `f444028d1cd93396`（全 sha256 一併）**，
並具名「`output/MANIFEST.tsv` 在 `.gitignore` 內、故於此落一份受版控之副本」。
Revise1 之 sha 現有兩處受版控留痕：`RULINGS.md` R-VS102 註 1、審閱檔 §一。

### 乙-1／乙-2／乙-3（建議）—— **未施作，待 Pei 裁定**

三項皆屬審閱自陳之「Pei 裁定範圍」或「隨 F1／F3 同批」，本輪不動 Revise1
（動則 sha 變、審閱之複算失效）。執行層之意見備供裁定：

- **乙-1（row 62 ER step 3 之 `is accepted`）**：**認同，且傾向改寫而非加註**。
  改為 `The result of writing PROXI Heated_Seat_Levels = 3 is recorded as Write_result`
  不需任何新來源，且 step 4 之比較（`unchanged from Levels_valid`）本身即為判準；
  加註則把「工具可能拒寫」留成執行時才炸的分歧。**惟改寫會動 L 欄步 3 之文字**，
  屬條文層，須 Pei 准。
- **乙-2（row 26 缺 baseline）**：**認同**。row 25／44 已有 `record … as Button_*` 之前例，
  本列不對稱。惟 row 28 同型且其 step 2 仍 PENDING（DR-21 未覆），
  建議**兩列併一包待 DR-21 覆後同批**，避免 row 26 先改、row 28 後改而形制再分岔。
- **乙-3（`Set PROXI` 舊式）**：**認同係本包可順手而未做**。當時之取捨為
  下放包 §2.7 明列順手範圍僅「全形分號」，逾此即逾授權；
  但未在上繳包 §7 具名此一克制，**是漏報，登記於此**。列入 F3 同批。

### 丙-2（分析層自報之三處疏失）

執行層無異議，僅補一則量測供 A 系列取號用：本包所修之全形符號**只計 `；`**（38 列）；
若依審閱所述之 `[；，]` 聯集口徑，母本為 **40 列**、Revise1 為 **28 列**
（現算，非推估）。差 12 而非 10 —— 多出之 2 列為 **row 14／15**：
二者只含 `，` 不含 `；`，其全形逗號在 §2.4 改寫 step 時隨繁中說明句一併消滅
（非 §2.7 所為 —— §2.7 之取代只動 `；`，未動 `，`）。
兩口徑之數字併陳，以免下一包再生轉錄差。
