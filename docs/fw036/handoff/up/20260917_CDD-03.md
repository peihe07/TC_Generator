# 上繳包 CDD-03 —— pilot01 Revise ＋ DR-DIAG-3 內查 ＋ batch 1（Layer 2 #6～#10，31 列）

下放包：`down/20260917_CDD-03.md`；依據 `down/20260917_CDD-02_review.md` §三／§四。
執行層 session：沿用 CDD 線。

**§0 禁區遵守**：git 全程未動；未動 `down/`、`up/` 既有檔；未動其他 feature 目錄；
`RULINGS.sha.tsv` 依 R-G75（三列數見 §1.2）；**只產 batch 1 之 31 列**，pilot 9 列以 Revise 改、ID 不變，
其餘 358 列零生成；條文本體未改；**Pre-Condition 每行皆能標來源**（§2 全表）；`framework.md` 維持 `DRAFT`。

**結論**：T0–T5 全部完成。**§5 升級條件四項全未觸發。**

| 項 | 結果 |
|---|---|
| **DR-DIAG-3** | **結案** —— SYSAD §4.5 明載 session／security／OBD-II（§2）|
| pilot01 v02 | 13 TC ＋ 1 佔列；**PENDING 15 → 2**；lint 除 `U=2` 全零 |
| batch 1 | **46 TC ＋ 1 佔列**（新增 41）；**lint 全項 0**；`coverage` 31/31 無 UNCOVERED |
| 合併本 v01 | 55 列；lint 僅 `U=2`（pilot 之 DR-DIAG-4）|
| canon §9 自檢 | **17/17 全過**（母體 46）|

---

## §1　T0 條文落檔與 lint 實作

### 1.1　取號現查（R-G23）

`R-DIAG11`／`R-DIAG12`／`R-DIAG10(amend)` 全 repo 零命中（排除 `down/`），取號成立。條文逐字照錄。

| ruling_id | sha8 | body_sha8 | body 行 |
|---|---|---|---:|
| `R-DIAG11` | `41538bf8` | `8f2c23b5` | 6 |
| `R-DIAG12` | `890cefe6` | `00db2024` | 4 |
| `R-DIAG10(amend)` | `424da598` | `d53f5c55` | 4 |

### 1.2　R-G75 三列數（(c)）與集合比對（(b)）

| 項 | 值 |
|---|---:|
| 寫前（工作區）| **54** |
| HEAD | **54** |
| 寫後 | **57** |

(b)：`HEAD 54 ∪ 工作區獨有 0 ∪ 新增 3 = 57`；寫後 **57**；缺失／多出／重複 **0／0／0 → 相符**。
**§5 升級條件 1 未觸發。**

> **寫入當時無覆寫** —— 寫前檢查顯示工作區與 HEAD 一致（`HEAD 有而工作區無: 無`），
> 此為 R-G75 落檔後之首次乾淨寫入。
>
> **惟本包收尾複驗時查得第四次覆寫**：工作區再度為 35 列、`R-DIAG` **0**，
> HEAD 之 19 列 `R-DIAG*`／`R-G73`～`R-G75` 全數消失（工作區獨有列：無）。
> 依 R-G75(a) 以 HEAD 為底重寫，(b) 複驗 57 = 57、缺失／多出 0，最終 **58 行（57 列 ＋ 表頭）**。
> **修復後之三列數：寫前 35／HEAD 54／寫後 57。**
>
> 四次之形態一致：Security 線整檔覆寫，只留其 35 列 `R-SEC`。
> 本次與前三次之別在**時點** —— 發生於本包已依 R-G75 正確寫入之後，
> 即 **(a) 之寫前檢查無法防範寫後之覆寫**。R-G75 只在「我方要寫」時生效；
> 對方不執行本條時，我方每次收尾都必須再查一次。
> 建議 GC-16 之外，另於 Security 線下一包 §0 明文要求其亦依 R-G75（審閱 §五-1 已請 Pei 轉達）。
>
> **第五次（提交前複查）—— 形態改變**：本次**非整檔覆寫**，資料未遺失，
> 但**表頭 `ruling_id…` 被寫到檔尾**（位置 index 58，應為 0），
> 檔案成為「註解 → 58 列資料 → 表頭」。
> `csv.DictReader` 會把第一列資料當表頭而誤解析整張表，`--check` 亦然。
> 已修正排序為「註解 ＋ 表頭 ＋ 58 列」，並以 `rulings_hash.py` 重生複驗：
> **我方 22 列（19 `R-DIAG*` ＋ `R-G73`／`R-G74`／`R-G75`）sha8 全數相符，無遺失**；
> 他線列亦無不符（其 `R-SEC24` 已更新、新增 `R-SEC25`，皆保留）。
>
> **五次之形態**：1–4 為整檔覆寫（我方列消失），5 為結構破壞（表頭錯位）。
> 後者**在列數上看不出來**（58 列俱在），只有解析時才暴露 ——
> R-G75(b) 之「以 ruling_id 集合比對」恰好會漏掉這一類，
> 建議 GC-16 一併加入**結構檢查**（表頭須為第一個非註解行、不得重複）。

### 1.3　lint 實作（T0(b)）

| 條 | 實作 | 位置 |
|---|---|---|
| `R-DIAG11` | **列級**豁免 —— 新結構 `ROW_EXEMPT_OOS`／`row_exempt()`／`test_result_idx()`；條件為 `AF` Test Result 去空白後 == `Out of Scope`；`I`／`M`／`R` 於 `check_row()` 回傳後濾除，`Z` 跳過分支 | `scripts/lint036.py` |
| `R-DIAG12` | `FEATURE_EXEMPT["diagnostics"] = ["I-cross"]` | 同上 |

**與 `FEATURE_EXEMPT` 之分工記於程式碼註解**：後者整項移出 `check_order()`，前者只對符合條件之列不施檢。
`R1-DIAG`／`RM-DIAG` **不在豁免內**（條文明定）。

自測（`selfcheck_lint_diag.py`，各補 2 正 2 反例，實作為各 5+5）：

| check | 正例（不得命中）| 反例（須命中）| 判 |
|---|---|---|---|
| `P-DIAG`／`U-DIAG`／`R1-DIAG`／`RM-DIAG`／`Z` | 各 5/5 | 各 5/5 | PASS |
| **`R-DIAG11`（列級豁免）** | 5/5（`""`／`NA`／`Pass`／`Fail`／`out of scope` 小寫不豁免）| 5/5（含前後空白、NBSP、換行者仍豁免）| PASS |
| **`R-DIAG12`（`I-cross` 豁免）** | 5/5（`diagnostics`／`security` 不跑）| 5/5（`camera`／`amfm`／`home` 仍跑）| PASS |

> **自測抓出一處我方缺陷**：`R-DIAG12` 之判斷式初版寫反（`[] if "I-cross" in check_order else ["hit"]`），
> 得「假陽性 2／假陰性 3」。已更正並使其與其他項同向（回傳非空 ＝ 有問題）。
> 另修 `main()` 之 print 假設所有 fn 皆回傳 `Violation`（豁免類回傳字串標記）。

回歸：`pytest tests/` **1382 passed／9 failed** —— 該 9 項（`test_gates_tsv` 1、`test_intake_scaffold` 6、
`test_single_write_path` 2）為既存失敗，與本包無關（CDD-01_A §4.4 已以 stash 驗證）。
`camera`／`security` profile 之 `check_order()` 與 `row_exempt()` 皆未受影響（實測：
`camera` 之 OOS 列不豁免，`security` 之 `I-cross` 仍豁免）。

### 1.4　T0(c) 升冪檢查之有無

**全域 lint 無 `specification_reference` 升冪檢查。** `spec` 欄只出現於 `M_FIELDS`（空欄三態）與
`Q_FIELDS`（不可見字元），全檔無任何順序比對。

已登 **`docs/fw036/GC_BACKLOG.md`（本包新建）** 第 1 項，本包未加該檢查。
該檔另收 `new_feature.py` 之 ABBR 取法（第 2 項）與 `RULINGS.sha.tsv` 之維護歸屬／GC-16（第 3 項）。

---

## §2　T2 DR-DIAG-3 內查 —— **結案**

### 2.1　CFTS004 grep（13 筆，**全為假陽性**）

`Basic Report` 全欄（Description ＋ Comments ＋ 各 Description/Action 欄）grep
`session`／`0x10`／`$10`／`extended`／`default`／`security access`／`$27`／`0x27`／`seed`／`key`／
`tester present`／`$3E`：

| CFTS004 列 | SYS-RA-DIAG | 命中詞 | 實際語意 |
|---:|---|---|---|
| r192 | -191 | `default` | `fill remaining as default values` —— 資料填充 |
| r197／r198 | -196／-197 | `default` | `the default RAR link` —— Jira 連結 |
| r211 | -210 | `Key` | `report the Key status` —— **點火鑰匙狀態**（DID 回報內容）|
| r213 | -212 | `key` | `relative to key state, ECU state and standby state` —— 同上 |
| r255 | -254 | `default` | `using default behavior` —— DTC 成熟度章節 |
| r373 | -372 | `Key` | `$030A - Clear Key Sense PIN` —— 常式名稱 |
| r378／r389 | -377／-388 | `$10` | **Package Indication Code `$10`**，非 SID `0x10` |
| r395／r399 | -394／-398 | `key` | `Hard key inputs` —— 使用者輸入源 |
| r401／r402 | -400／-401 | `Default` | `$0301 - Reset Parameters to Default`／`"CIP Default Settings."` |

審閱 §三-3 點名之 **r350（`SYS-RA-DIAG-349`）未命中** —— 其原文為
「The HU shall implement each diagnostic requirements for **enabling and disabling diagnostics**…」，
屬 DTC Enable Criteria 章節（r349 `Enable Criteria` 標題下），**與診斷 session 無關**。

**CFTS004 對診斷 session 與 security access 零載。**

### 2.2　SYSAD grep —— **明載**

`SYS3_CFTS_004…SYSAD.docx` §4.5 Assumptions and Dependencies（para 112–141）逐字：

```text
Session & Security Assumptions
  ECU starts in Default Session.
  Security access (0x27) is required for:
    Write DID
    Routine execution
    DTC clearing
  Valid seed/key algorithm is available and synchronized between tester and ECU.

Dependencies / Hardware Dependencies
  ECU hardware supporting UDS stack.
  CAN / Ethernet controller
  Diagnostic connector (OBD-II)
```

另 §4.8 元件表：MDTDiagService「**Validates diagnostic session, security access and request format**」。

### 2.3　判定

依 **R-DIAG5(amend)(c′)** 第一層解，**DR-DIAG-3 結案**。Pre-Condition 之三行改為具體行：

| 行 | 文字 | 來源 |
|---|---|---|
| PC-1 | `The ECU is in the default diagnostic session` | SYSAD §4.5（ECU starts in Default Session）|
| PC-2 | `The diagnostic tool is connected to the OBD-II diagnostic connector` | SYSAD §4.5 Hardware Dependencies |
| PC-3 | `Security access 0x27 has been granted` | SYSAD §4.5（required for: Write DID / Routine execution / DTC clearing）|

**PC-3 只出現於含寫入（`0x2E`）或常式（`0x31`）之 TC。**

**一處讀法須追認（`[A-DIAG27]`）**：SYSAD 之列舉為 Write DID／Routine execution／DTC clearing 三項，
**未含 I/O Control（`0x2F`）**。本包依「列舉為封閉」讀為 I/O Control 不需 `0x27`，故 I/O 型 TC 不寫 PC-3。
此為由列舉之缺漏反推，請 Pei 追認。

---

## §3　T1 pilot01 Revise → `diagnostics_pilot01_v02.xlsx`

### 3.1　`specification_reference` 升冪（審閱 §三-1 必修）

13 條複查，**多錨者 3 條、未升冪 2 條**（審閱只點名 `-009`，`-010` 亦是）：

| TC ID | 修前 | 修後 |
|---|---|---|
| `NR1L-DIAG-009` | `4940473 / 4940478 / 4940477 / 4940474` | `4940473 / 4940474 / 4940477 / 4940478` |
| `NR1L-DIAG-010` | `4940482 / 4940478 / 4940477` | `4940477 / 4940478 / 4940482` |

修法非逐條手改 —— 產生器之 `tc()` 改為 `sorted({int(o) for o in …})`，**結構上不可能再出現未升冪**。
複驗：13/13 升冪。

### 3.2　Pre-Condition 來源表（審閱 §三-2 必修）

全 13 條、共 **43 行** Pre-Condition，**逐行有據**（`pilot01.json` 之 `pc_sources`）：

| 來源類 | 行數 | 內容 |
|---|---:|---|
| `SYSAD §4.5`（PC-1 session）| 13 | 每條一行 |
| `SYSAD §4.5`（PC-2 OBD-II）| 13 | 每條一行 |
| `SYSAD §4.5`（PC-3 security 0x27）| 5 | `-001`～`-004`（寫入）、`-009`／`-010`（常式）之 6 條 |
| `CFTS004-<ObjectID>` | 7 | `-006` 之 4940297、`-009`／`-010` 之 4940477／4940478／4940482、`-011` 之 4939852、`-012` 之 4939821、`-013` 之 4940355 |
| `IN §8.7.5(f)` baseline 前例 | 5 | `-002`／`-003`／`-004`／`-007`／`-008` |

**三處刪除**：

1. `1. The vehicle is in Ignition ON state` —— **13 條全刪**。
   CFTS004 與 SYSAD 對 ignition／power state **皆零命中**；
   `features/power_moding/` 交付本之 ignition 前提（11 種寫法、15 行）**源於該 feature 自身之規格**
   （其需求即關於 ignition 轉換），非可為 Diagnostics 沿用之來源。
   依 **§8.5**（環境穩定性前提非規格觸發條件）刪除 —— 與審閱對 PC-4 之同一理由。
   **此為第三種處置，非審閱所列之兩個選項**（併入 DR-DIAG-3／沿用 power_moding 前例），
   理由如上，請追認（`[A-DIAG26]`）。若 Pei 認為須保留電源前提，須另開 DR 取得來源。
2. `-001` 之 `The rearview camera display is active on the radio screen` —— 依審閱建議刪（§8.5）。
3. `PENDING: DR-DIAG-3 …` —— 13 行全數由 §2.3 之具體行取代。

**`OBD-II` 之處置**：審閱慮及 SYSAD 只載 ISO-TP over CAN or DoIP 而 DoIP 可走乙太。
實測 SYSAD **Hardware Dependencies 明文列 `Diagnostic connector (OBD-II)`**，故保留 OBD-II 之措辭，
不改為泛稱。錨為 SYSAD §4.5。

### 3.3　v02 之 lint 與 PENDING

```
行計 全項 0，除 U=2
```

PENDING **15 → 2**（僅餘 `DR-DIAG-4` `$5000` controlOptionRecord 之觸發步驟與對應 ER）。

---

## §4　T3 batch 1 —— sibling plan 與產出

### 4.1　sibling plan（31 列 → 46 TC ＋ 1 佔列）

| Layer 2 | 037 列 | TC | 拆分軸與理由（含 CFTS004 回看結果）|
|---|---:|---:|---|
| **#6 Camera Brightness** `$283F` | 6 | 8 | `-001` 寫 1（**CFTS004 四列皆無值域**，不拆，CDD-02 §4.2 已追認）／`-002` negative 2（長度 13、值域 31）／`-003` unsupported 1／`-004` 讀 1／`-005` negative 2／`-006` unsupported 1 |
| **#7 ECU Internal Settings** `$280C` | 8 | 16 | 五個 positive 列（`-052` Cabin EQ Curve、`-053` Automated Loudness、`-334` Front Seat Video、`-340-002` Clock Defeat、`-342` 12/24 Time）**各拆讀／寫二條** —— CFTS004-4939689／4939685／4939688／4939684／4939687 皆書 `readable from and writeable to this DID`，讀（`0x22`）與寫（`0x2E`）為不同服務且可觀察結果不同；三個 negative 列（`-335`／`-341`／`-343`）各 2 |
| **#8 Media Hub Config** `$2870` | 6 | 9 | `-051` **狀態軸拆 2** —— CFTS004-4939923 列舉 `inserted or not inserted`／`-329` 1／`-330` 2／`-331` 1／`-332` 2／`-333` **1（正向，見 §4.3）** |
| **#9 Button Interfaces** `$1820`／`$1821` | 7 | 9 | `-054` **不拆** —— 12 個按鍵為**同一次讀取之回應內容**，ER 依 §6.1 以 `a.`–`l.` 分項；`-055` 1／`-344` 1／`-345` 2／`-386` 1／`-387` 2／`-388` 1 |
| **#10 Screen Test Pattern** `$1801` | 4 | 4 ＋ 1 佔列 | `-057` 佔列（沿用 `-014`）／`-383` 寫 1／`-384` negative 2／`-385` unsupported 1 |
| **合計** | **31** | **46 ＋ 1** | 新增 **41**（pilot 沿用 5 TC ＋ 1 佔列）|

**不拆之判準一致**：`incorrect format` 與 `invalid length` 在 ISO 14229-1 同屬 `0x13`，
拆之即二條 ER 相同，故 negative 一律 2 條（長度軸／值域軸）而非 3 條。

### 4.2　ID 與編號

`NR1L-DIAG-001`～`-005`（#6 之 037 row 8–11）與 `-014`（#10 之 OOS 佔列）**沿用 pilot v02，ID 不變**；
新增自 **`-015`** 起連號至 **`-055`**。
`-006`～`-013`（pilot 之 #11／#18／#1／#2／#17）**屬後續批，本批留號不重編**。

### 4.3　三處依 IN §8.6 取 CFTS004 而與 037 Title／Description 不同（皆登 RD 回饋）

| TC | 037 所書 | CFTS004 所載 | 處置 |
|---|---|---|---|
| `NR1L-DIAG-039`／`-040`／`-041`（`-331`／`-332`）| Title `connected USB devices`（與 `-329`／`-330` **逐字相同**）| CFTS004-4939919 `detected USB Hub devices` | 產為 USB Hub 數；**FB-DIAG-i** |
| `NR1L-DIAG-042`（`-333`）| `return the negative response – Service Not Supported $FF` | CFTS004-4939922 `report $FF as the result if the USB detection is not available` —— `$FF` 為**資料值**非 NRC | 產為**正向回報** TC（非 negative）；**FB-DIAG-j** |
| `NR1L-DIAG-026`／`-027`（`-340-002`）| Title／Description 書 `Clock Defrost` | CFTS004-4939684 書 **`Clock Defeat`** | 作者側四欄用 CFTS004 之 `Clock Defeat`；`test_item` 上半 verbatim 保留 037 之 `Clock Defrost`（R-DIAG4(a)）|

### 4.4　`-340-002` 之 ID 形式

`R1-DIAG` 對 `SWE1-Diagnostics-340-002` **通過**（`R1-DIAG=0`）——
R-DIAG2 之三段式號與裸號之區辨（`DIAG_DUP_REQ_IDS`）在實本上首次生效。

### 4.5　lint 與 coverage

| 本 | lint 行計 |
|---|---|
| `diagnostics_batch1_v01.xlsx`（47 列）| **全項 0** |
| `diagnostics_pilot01_v02.xlsx`（14 列）| 全項 0，除 `U=2` |
| `diagnostics_v01.xlsx`（合併，55 列）| 全項 0，除 `U=2` |

`coverage.tsv`：**31 列全對映**，`UNCOVERED` **0**，`OUT_OF_SCOPE` **1**（`-057`）。

---

## §5　T4 自檢

### 5.1　canon §9（17 項，母體 46 條 TC）

**17/17 全過。**

> **第 9 項（baseline）初跑未過 6 條** —— `rw_pair` 之寫向五條與 `-052` 於 Pre-Condition 立
> `Setting_initial`／`Pattern_initial`，而 ER 只比對「寫入值 ↔ 讀回值」，**初值從未入 ER**。
> 宣告而不用之 baseline 為 §8.5 之多餘前提。已改為寫向不立 baseline，複驗 46/46。登 `[A-DIAG30]`。

### 5.2　R-SEC15 型自檢

| 條 | 結果 |
|---|---|
| (b) 每條含 DUT 側觸發（`$ ` 行）| **46/46** |
| (c) 內部代號入四欄 | **0 處** |
| (d) 反引號／單引號 | **0 處** |
| (e) `Input Test Data = NA` | **46/46** |
| (i) PENDING 整行 token | **0 行**（batch 1 無 PENDING）|

---

## §6　T5 合併本

`features/diagnostics/sandbox/merged/diagnostics_v01.xlsx` —— **55 列**
= batch 1 之 47 列（含 pilot 沿用 6）＋ pilot 其餘 8 條（`-006`～`-013`）。
lint 僅 `U=2`（pilot `-006` 之 `DR-DIAG-4`）。供後續批累加。

---

## §7　§3 預期 vs 實測

| 項 | 預期 | 實測 | 判 |
|---|---|---|---|
| batch 1 TC | 31 列 → 36–48 TC ＋ 1 佔列 | **46 ＋ 1** | 符（區間內）|
| PENDING 行 | session 解出 → ≤ 8 | **batch 1 為 0**；pilot v02 餘 2 | 符（優於預期）|
| lint 真違規 | 0 | **0** | 符 |
| `Z`／`I-cross` | 0 | **0** | 符 |
| `RULINGS.sha.tsv` | 54 → 57 | **54 → 57**（寫前 54／HEAD 54／寫後 57）| 符 |
| 合併本 | 45–60 列 | **55** | 符 |

---

## §8　自報錯誤

1. **`R-DIAG12` 自測判斷式寫反**（§1.3）—— 初版得「假陽性 2／假陰性 3」。
   根因：豁免類檢查之「命中」語意與一般檢查相反，我未先統一方向即寫。已改為同向並加註。
2. **6 條 TC 宣告 baseline 而未使用**（§5.1）—— `rw_pair` 之寫向為模板產生，
   我把讀向之 baseline 前提一併套用而未檢視 ER 是否用到。**canon §9 自檢抓出，lint 抓不到**
   （lint 無此判準）。已修。
3. **審閱只點名 `NR1L-DIAG-009` 未升冪，實為 2 條** —— 我在 CDD-02 未對 `extra_spec` 做排序，
   凡多錨者皆有風險。已於產生器結構上根除，非逐條手改。

---

## §9　升級條件核對（下放包 §5）

| # | 條件 | 實測 | 觸發 |
|---:|---|---|:-:|
| 1 | `RULINGS.sha.tsv` 集合不符 | 57 = 57，缺失／多出／重複 0 | 否 |
| 2 | T2 查得 session／security 要求且與 pilot 已寫之 Pre-Condition 矛盾以外之情形 | 查得且為**矛盾**（PENDING → 具體行），依條文「矛盾即改，不升級」 | 否 |
| 3 | batch 1 任一 Layer 2 組整組因來源全缺而 PENDING 超過 Pre-Condition 一行 | batch 1 **PENDING 0 行** | 否 |
| 4 | 任何需改 canon 之情形 | 無 | 否 |

### 9.1　待 Pei（五件）

1. **`[A-DIAG27]`** I/O Control（`0x2F`）不需 `0x27` 之讀法（由 SYSAD 列舉之缺漏反推）——請追認。
2. **`[A-DIAG26]`** `Ignition ON` 依 §8.5 自 13 條刪除（第三種處置，非審閱所列兩選項）——請追認。
3. **RD 回饋新增 FB-DIAG-i／FB-DIAG-j**（`-331`/`-332` Title 與來源不符；`-333` NRC 與資料值混寫），
   與既有 a～h 併送 Nik（共 **10 項**）。
4. **`GC_BACKLOG.md` 三項**（升冪檢查、`new_feature.py` ABBR、GC-16）之排程。
5. **commit**：建議訊息見 §11。

---

## §10　兩個 TC 全文（下放包 §4-4）

### 10.1　`NR1L-DIAG-026`（`SWE1-Diagnostics-340-002`，R-DIAG2 之三段式 ID）

```text
Requirement or Design ID : SWE1-Diagnostics-340-002
Test Group               : Diagnostics
Test Set                 : ECU Internal Settings
Test Case Priority       : P1
Design Method            : 功能測試 (Functional based ; no specific technique)
Vehicle Model (T..Z)     : 1 1 1 0 0 1 1
Remarks                  : （空）

Test Item
  The MDTDiagService shall support Read and Write of Clock Defrost data via the corresponding DID.
  (read $280C, SID 22)

Pre-Conditions
  1. The ECU is in the default diagnostic session
  2. The diagnostic tool is connected to the OBD-II diagnostic connector

Input Test Data
  NA

Test procedure
  1. Read the Clock Defeat setting
     $ 22 28 0C

Expected Result
  1. The positive response 62 28 0C <Clock Defeat setting byte> is received, and the reported value is
     the Clock Defeat setting currently applied by the radio

Specification Reference
  CFTS004-4939684

split_flag   : True
split_reason : R-DIAG1(b)：037 述本 DID 對該設定同時 readable 與 writeable，讀（0x22）與寫（0x2E）
               為不同服務且可觀察結果不同，依 IN §8.3 拆 sibling；本條為讀向，區分 token 為 SID 22
pc_sources   : PC-1: SYSAD §4.5 Session & Security Assumptions（ECU starts in Default Session）
               PC-2: SYSAD §4.5 Hardware Dependencies（Diagnostic connector (OBD-II)）
```

**說明**：`test_item` 上半 verbatim 取 037 Description 首句，故保留 037 之 `Clock Defrost`；
作者側四欄用 CFTS004-4939684 之 **`Clock Defeat`**（R-DIAG4(a)：上半照抄、作者側用來源用字）。
讀向無 PC-3 —— SYSAD 之 `0x27` 只要求於 Write DID／Routine／DTC clearing。
其寫向為 `NR1L-DIAG-027`，PC 含 PC-3。

### 10.2　`NR1L-DIAG-043`（`$1820` Push Button Control Interface，§6.1 分項）

```text
Requirement or Design ID : SWE1-Diagnostics-054
Test Group               : Diagnostics
Test Set                 : Button Interfaces
Test Case Priority       : P1
Design Method            : 功能測試 (Functional based ; no specific technique)
Vehicle Model (T..Z)     : 1 1 1 0 0 1 1

Test Item
  SW shall support DID $1820 to report the status of the Push Button Control Interface.
  (read $1820, twelve push buttons)

Pre-Conditions
  1. The ECU is in the default diagnostic session
  2. The diagnostic tool is connected to the OBD-II diagnostic connector
  3. No push button on the Push Button Control Interface is pressed

Input Test Data
  NA

Test procedure
  1. Read the Push Button Control Interface status
     $ 22 18 20
  2. Press and hold the "Power" push button
  3. Read the Push Button Control Interface status again
     $ 22 18 20

Expected Result
  1. The positive response 62 18 20 <push button status record> is received, and the record reports
     the status of:
     a. Power        e. Dark      i. Player
     b. Up           f. Mute      j. Nav
     c. Down         g. Back      k. Phone
     d. Select       h. Radio     l. More
  2. The "Power" push button is held pressed
  3. The positive response 62 18 20 <push button status record> is received, and the record reports
     the "Power" push button as pressed

Specification Reference
  CFTS004-4939646

pc_sources   : PC-1/PC-2: SYSAD §4.5
               PC-3: CFTS004-4939646（…report the status of the following push buttons）
```

（工作簿內 `a.`–`l.` 為逐行，上表為版面壓縮示意。）

**說明**：12 個按鍵**未拆 sibling** —— 它們是**同一次 `22 18 20` 讀取之回應內容**，
非獨立驗證點；依 §6.1 以 `a./b./c.` 分項。
步驟 2 之按壓為唯一觸發，步驟 3 為驗證擁有者（§5.5）；步驟 1 之讀取建立「未按下」之基準態。
`-055`（不相容按鍵回報 `Not Pressed`，CFTS004-4939647）另成一條 `NR1L-DIAG-044`，
其 `specification_reference` 含 4939646／4939647 兩錨並升冪。

---

## §11　產物清單

| 路徑 | 動作 |
|---|---|
| `features/diagnostics/RULINGS.md` | 新增 `R-DIAG11`／`R-DIAG12`／`R-DIAG10(amend)` |
| `docs/fw036/RULINGS.sha.tsv` | 依 R-G75 寫，54 → **57**；收尾與提交前各因第四、五次事件再修復（§1.2），最終 **58 列**（含他線 `R-SEC25`）|
| `docs/fw036/GC_BACKLOG.md` | **新建**（3 項全域待辦）|
| `scripts/lint036.py` | R-DIAG11 列級豁免（`ROW_EXEMPT_OOS`／`row_exempt()`／`test_result_idx()`）＋ R-DIAG12 |
| `features/diagnostics/scripts/selfcheck_lint_diag.py` | 新增兩項自測（各 5 正 5 反）；修 print |
| `features/diagnostics/sandbox/pilot01/diagnostics_pilot01_v02.xlsx` | **新增**（Revise）|
| `features/diagnostics/sandbox/pilot01/pilot01.json` | PC 重寫 ＋ `pc_sources` ＋ 錨升冪 |
| `features/diagnostics/sandbox/batch1/diagnostics_batch1_v01.xlsx`／`batch1.json`／`coverage.tsv` | **新增** |
| `features/diagnostics/sandbox/merged/diagnostics_v01.xlsx` | **新增**（55 列）|
| `features/diagnostics/docs/rd_feedback_20260917.md` | 新增 **FB-DIAG-i**／**FB-DIAG-j**（共 10 項）|
| `features/diagnostics/ANOMALIES.md` | 新增 `[A-DIAG25]`～`[A-DIAG30]`（共 30 項）|
| `features/diagnostics/DATA_REQUESTS.md` | `DR-DIAG-3` 結案 |
| `features/diagnostics/feature.yaml` | `row_exempt_out_of_scope`／`feature_exempt` |
| `docs/runtime/profiles/FW036_R1L_Diagnostics_Profile.md` | 五項 lint 標**已校準**；列級／整項豁免記明 |
| `docs/fw036/lint_reports/diagnostics_{batch1_v01,pilot01_v02,v01}__*.md` | 三份（中途版已刪）|
| `docs/fw036/handoff/up/20260917_CDD-03.md` | 本檔 |

**git 未動。** 建議 commit message：

```
feat(diagnostics): CDD-03 batch 1 — 46 TCs, DR-DIAG-3 closed, OOS lint exemption
```
