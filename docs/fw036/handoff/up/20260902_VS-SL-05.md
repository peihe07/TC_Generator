# 上繳包 VS-SL-05 —— 送抽前修正（Pre 矛盾 52 列＋R4 否定式代表值＋DR 縮節）

日期：2026-09-02　層級：Tier 1 → Tier 2
對應下放包：`down/20260902_VS-SL-05.md`（協定：`docs/fw036/handoff/PROTOCOL.md`）
**未動 `down/`、`output/`、`delivered/`、`inputs/`、`forms/`；BL／VC 沙盒稿仍未動。**
（引 canon 帶前綴：`FO §x`／`IN §x`。）

---

## §1　產物路徑與 SHA256

| 檔 | bytes | SHA256 |
|---|---:|---|
| `features/vehicle_setting/reports/_v5_pre_fix.tsv` | 2,830 | `955d5e46e4045e0eb2d5270b877c805b89638dd5b2a176c50f94fbefcf5657fa` |
| `features/vehicle_setting/reports/vf230_settings_dryrun_v5.tsv` | 121,151 | `dea4d5d140198e3ab8e5c511047c808a9307257062267fd574d731ec8ffb9ceb` |
| `features/vehicle_setting/reports/vf230_settings_dryrun_v5.md` | 3,518 | `7951cf6724ca97d47943145d9516b4698746be7d3cf2382bf96f3d3f5ee22bd4` |
| `features/vehicle_setting/reports/_v4_branch3_resolution.tsv`（就地改判 R4） | 20,854 | `3bdd224e62047c46d6fa9dc2bba265e648f4cd4b6f840a23d8ef9a96e2bfcb6e` |
| `features/vehicle_setting/sandbox/vssl/vf230_vssl.xlsx`（**送抽稿**） | 187,438 | `49b1bf94eb368ee362378733fe0ad2e36c6f5b8ada3b07fcaec952c476218ac3` |
| `features/vehicle_setting/sandbox/vssl/vf230_vssl.v4.bak.xlsx` | 187,485 | `eec8445afe104a0397d765ae43b95313829f8cc954116c90b9de617906575b32` |
| `features/vehicle_setting/sandbox/vssl/vf230_vssl.v3.bak.xlsx` | 187,731 | `7b2c4cec3f72479917a90aa15d7dbf1859c2a347a700229a98c055e11e575ea4` |
| `features/vehicle_setting/reports/dr_draft_vssl.md`（第四節縮節） | 13,325 | `929ce7a1bc35b0a632cf9450f54e932b146745dd105e006f64108e1c8006b88d` |
| `features/vehicle_setting/scripts/vs_sl05_fix.py` | 6,841 | `dae456460c4a36a9f1a5a623695106312f130b42fa26df0f82d5e2ddf53218e4` |

**兩份備份之 SHA 皆與其對應上繳包所載逐字相同**：
`.v4.bak` = `eec8445a…`（VS-SL-04 §1）、`.v3.bak` = `7b2c4cec…`（VS-SL-03 §1）。
BL／VC 沙盒稿未動，SHA 仍為 `b0ee608d…`／`3a1b5f40…`。

---

## §2　§1 Pre「menu is open」矛盾修正

| 項 | 列 |
|---|---:|
| Pre 含 `The Vehicle Settings menu is open` | **90** |
| 其中 Procedure 已插導覽段 → 修正 | **52** |
| 未插導覽段 → 不動 | **38** |

**與審閱 §3 所測之 52 相同，無差異。** 修正後複驗：
「Pre 含該句且已插導覽」之列 = **0**；仍含該句者 **38** 列。
每列刪除之行數皆為 **1**（assert PASS）。清單見 `_v5_pre_fix.tsv`
（`row`／`tc_id`／`req_id`／`removed_line_no`／刪前後行數）。

樣本 r14 修正後之 Pre：

```
1. The HU is in the Full-Operation state
2. FD-CAN8 is connected to the bus simulator with signal tracing enabled
3. PROXI AUX_Switch_Types = 2 (Type 2)
```

---

## §3　§2 R4 否定式代表值（7 列）

| 設定項 | 列 | 总控表逐字 | 提議 | 註記 |
|---|---:|---|---|---|
| `Engine Off Power Delay` | 4 | No.149 `If Country_Code is not [Australia], return value is true.` | `PROXI Country_Code = 2 (United States of America)` | EP 代表值；值表 15 值中**無 Australia**，補集即全表 |
| `Automatic Trailer Light Check` | 3 | No.268 `If Trailer_Light_Check is NOT [Absent]), return value is 1.` | `PROXI Trailer_Light_Check = 1 (Type 1 (Radio))` | EP 代表值；兄弟值 `2 (Type 2)`／`3 (Type 3)` |

審閱 §2 之值表本層已複驗：`Country_Code` 15 值、`2 = United States of America`、
逐值掃描**無 Australia**；`Trailer_Light_Check = {0: Absent, 1: Type 1 (Radio), 2: Type 2, 3: Type 3}`。

`_v4_branch3_resolution.tsv` 就地改判 `resolution = R4`、
`subcase = NEGATED_EP_REPRESENTATIVE`，`evidence` 補值表鍵。

沙盒稿複驗：`PROXI Country_Code` **4 列**（皆 R4）；
`PROXI Trailer_Light_Check` **5 列** = R4 之 3 列 ＋ r354／r355（本已於分支 (1) 帶該參數者 2 列）。

> **報告 schema 無 `reasoning` 欄**（`COLUMNS` 十欄自 v1 定），
> 註記寫於 `proxi_proposed` 之 `｜` 之後，與既有 `EP 兄弟` 註記同法。**若須獨立欄位請示。**

---

## §4　§3 DR 稿縮節

第四節由 **11 名 32 列** 縮為 **9 名 25 列**：
(a) FIP 无對應列 8 名 22 列；(b) FIP 參數於值表查無（`Easy_Entry_Menù`）1 名 3 列。
`Engine Off Power Delay`／`Automatic Trailer Light Check` 已解，**自 DR 移除，不再問上游**
（節首載明其去向，以免上游對照舊版時誤以為漏列）。既有一～三節不動。

---

## §5　自檢

| # | 項 | 結果 |
|---|---|---|
| 1 | `assert` R4 = 7 列 | **PASS** |
| 2 | `assert` Pre 修正之每列刪行數 = 1 | **PASS**（52／52） |
| 3 | `assert` v5 `PROXI_PENDING` = 25 | **PASS** |
| 4 | 修正後「Pre 含該句且已插導覽」之列 = 0 | **PASS** |
| 5 | lint 四項（括號下半／尾句號／雙引號／Proc↔ER 步數）三本 | **PASS**（0／0／0／0） |
| 6 | 二份備份 SHA = 其上繳包所載 | **PASS** |
| 7 | `vs_sl05_fix.py` 連跑兩次結果相同（冪等） | **PASS** |

v4 → v5：`PROXI_PENDING` **32 → 25**；分支 **(1) 152／(2) 系列 200／(2b) 54／(2c) 3／(2d) 16／(2e) 7／(3) 25／(4) 43**。

---

## §6　自報

1. **審閱之一處引用有誤，本層曾照抄（已更正）**
   `down/20260902_VS-SL-04_review.md` §3 作「依 **FO §4.4** 不得為 Pre」。
   **實測 `docs/fw036/FEATURE_ONBOARDING.md` 之節號為 `4`／`8`／`8.1`–`8.9`，無 `4.4`**；
   `4.4` 只存在於 `docs/runtime/ASPICE_SWE6_AI_Instruction.md`（其有 `4.1`–`4.6`）。
   **正確引用為 `IN §4.4`。** 本層之 v5 摘要與腳本 docstring 曾照抄該誤，已改。
   `down/` 內之審閱原文屬 Tier 2，本層不動，請自行更正。

2. **本層違反 PROTOCOL「不動 `down/` 內任何檔」（已復原，具名自報）**
   本層為量測 `canon_refs` 之增量，將數檔暫移至同一暫存目錄，
   其中 `down/20260902_VS-SL-05.md` 與 `up/20260902_VS-SL-05.md` **同名**，
   後者覆蓋前者，復原時遂把上繳稿寫回 `down/`。
   **已察覺並逐字復原**：`down/20260902_VS-SL-05.md` 現為 **1,761 bytes**，
   與 Tier 2 落檔時之大小相同，內容逐字還原自本輪收到之原文；
   上繳稿已移回 `up/`。**此為本層之操作缺陷，非產物問題**；
   往後之暫移一律以來源目錄分層存放。

3. **`vs_sl05_fix.py` 初版不可重跑（已修）**
   `_v4_branch3_resolution.tsv` 為就地改判，重跑時 `NEGATED_CONDITION` 已為 0，
   assert 遂失敗。已改為冪等（已為 `NEGATED_EP_REPRESENTATIVE` 者視同命中），
   **連跑兩次結果相同**，審閱端得以重現。

4. **`vs_sl05_fix.py` 之備份採「不覆寫既有檔」**（承 VS-SL-04 §5-2 之教訓），
   本輪複驗 `.v4.bak` 之 SHA 仍為 VS-SL-04 所載之 `eec8445a…`。

5. **`gate_all.py`：`canon_refs` 505 → 506，其餘三支同數**
   逐檔移除法定位：**+1 全部來自 Tier 2 本輪之二檔**
   （`down/20260902_VS-SL-04_review.md`／`down/20260902_VS-SL-05.md`，其內之 `FO §4.4` 無法解析）。
   **本層產物於更正該引用後實測 +0**（v5 報告二檔、`_v5_pre_fix.tsv`、`vs_sl05_fix.py`、
   縮節後 DR 稿、本上繳包，逐檔移除計數皆不變）。
   `lint_paths` 4、`rulings_hash`／`gates_tsv` 同前，皆為本輪之前既有；`rulings_hash` 仍未重跑。

## §7　送抽

本包准後即送 Pei Excel 抽驗，三本沙盒稿與 DR 稿為：

| 檔 | SHA256 |
|---|---|
| `features/vehicle_setting/sandbox/vssl/vf230_vssl.xlsx` | `49b1bf94eb368ee362378733fe0ad2e36c6f5b8ada3b07fcaec952c476218ac3` |
| `features/bed_lowering/sandbox/vssl/bl_vssl.xlsx` | `b0ee608d5469d31b36a067d0db63f7efd2b40c388e7d58e4fdd6f6a3286f28ec` |
| `features/vehicle_category/sandbox/vssl/vc_vssl.xlsx` | `3a1b5f401fada13ea39a79e85b8e849a24d1398f8831eba8b4470ec063f65541` |
| `features/vehicle_setting/reports/dr_draft_vssl.md` | `929ce7a1bc35b0a632cf9450f54e932b146745dd105e006f64108e1c8006b88d` |
