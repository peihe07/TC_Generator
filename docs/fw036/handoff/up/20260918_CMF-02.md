# 上繳包 CMF-02 — Comfort HMI Revise 候選本（JSON patch ＋ sibling ＋ CMF-01 裁定落帳）

- 日期：2026-09-18
- 對應下放包：`down/20260918_CMF-02.md` ＋ `down/20260918_CMF-02_A.md`（同讀，同一 session 執行）
- 性質：JSON 層 patch、裁定落帳、sandbox 候選本、唯讀調查。**未入 `delivered/`、未覆寫 0907 本、未 commit、未跑 `--gate`／`--regen`、未改 `docs/fw036/RULINGS.sha.tsv`。**
- 開工前 `git status`（CMF-02 前言之要求）：`features/comfort/` **無未提交之 CMF-01 產物**。CMF-01 之產物已於 `f55eb2a docs(comfort): CMF-01 — …` 提交，開工時工作區乾淨。

## 0. 首段：停點、PENDING、實產數、不符數

| 項 | 結果 |
|---|---|
| **§3 停點** | **命中一條（停點二）**：`005-04` 之 RECIRC sibling —— 其 ER「A/C 按鈕不亮」之值只在 037 leaf（`(change not shown)`），**條文 2.4 之「(Do not show this change)」只附於 Defrost 一句**，Recirc 一句無呈現之陳述。依停點**不產**該條，原條 `json-139` 亦不瘦身。停點一（實產 > 30）未命中。**停點之適用粒度我取「逐條」**（比照 CMF-01 §1：停點命中之項不決定，其餘照做），若分析層之意是整節停，請指正 |
| **實產 sibling** | **8 條**（計畫 23 條）。未產 15 條：**R-C44 三問答「否」14 條**、停點 1 條。逐條見 §3 與 `features/comfort/docs/reports/sibling_plan_cmf02.tsv` |
| **PENDING 行數** | **6 行**：sibling 之 ER 2 行（`003-07` 分支 (a)，`PENDING: DR-47`）＋ M6 之步驟 4 行（`PENDING: DR-46`）。003-07 分支 (b) 之 4 個模式值 **0 行**（未產，理由見 §3） |
| **§4-5 不符數** | **0**。候選本自產出檔讀回，與 0907 本逐格比對全部工作表（21,309 格）：未列於本包之格**不符 0 格**；列於本包之 1,731 格讀回值 == 意圖值（不符 0），且每格皆歸因到包內之一項 |
| LINT | JSON 層 `lint_tcs.py`：**57/57 gates PASS，474 TCs，0 finding**。候選本層 `lint036.py --profile comfort`：跑完，無 Traceback（計數與 0907 基線之差見 §6）。哨兵 `LINT_STATUS: OK` 見 `sandbox/revise_0918/lint/LINT_STATUS.txt`（**由 wrapper 寫出** —— lint036 自身之哨兵屬 GC-16，尚未落地） |

**需要分析層注意之三件事（細節在各節）**：

1. **sibling plan 與 R-C44 相牴觸**：23 條中 14 條依 R-C44 三問不得拆（其中 `013-03`／`021-02`／`121-02` 三條**在 R-C44 自己的溯源表裡被點名為「不拆」之例**）。CMF-02 §3 寫「依 plan 產出」，但沒有明示要蓋過 R-C44，所以我依既有裁定不產，逐條三問之答寫在 §3。若分析層判某條可拆，照表補產即可。
2. **兩道 lint 相衝**（候選本層）：M6 的 `PENDING` 行，`lint036` 的 P 檢查要值寫成 `[…]`（037 逐字記法），Comfort 的 `lint_tcs` `ui-bracket` 則不准在 procedure 出現 `[…]`。我依 JSON 層 lint 寫成 `"not available/Blink"`，所以 lint036 P = 4。另 R-C50 之摘句以小寫開頭，觸 lint036 J = 14。見 §6。
3. **M2 我判兩個 leaf 可分**，沒有停下（理由見 §2-M2）。若分析層判它們是同一句的兩種說法，這兩條要退回。

## 1. 裁定落帳（文件層）

### 1-1. `RULINGS.md` —— 暫編 ↔ 實編

落帳前實測 `features/comfort/RULINGS.md` 之現行最大號為 **R-C45**（`grep -n "^R-C[0-9]"`）。**暫編 R-C46～R-C53 全數未被佔用，實編＝暫編**：

| 暫編 | 實編 | 備註 |
|---|---|---|
| R-C46 | R-C46 | |
| R-C6(amend) | R-C6(amend) | R-C6 原文不動，加旁標（同 R-C42 → R-C42-1 之處置） |
| R-C47～R-C53 | R-C47～R-C53 | |

- 落於 `## R-C45` 之上（本檔新條置於前之既有排列），條文逐字取 review §3（R19-2），並附 CMF-02_A §2「Pei 未撤回任何一條」。
- **sha8 未產** —— GC-16 未落地，`RULINGS.sha.tsv` 不手改（CMF-02 前言）。

### 1-2. profile 同步（`docs/runtime/profiles/FW036_R1L_Comfort_Profile.md`，R-TM13：刪除線保留，不刪）

| 節 | 處置 |
|---|---|
| §0 | Test Group `Comfort` 劃線 → `Climate Control Interface`（R-C6(amend)） |
| §2 | 同上（該節亦寫 Test Group，一併同步） |
| §3.2（J 欄框） | 「J 欄只寫條件本文與其節次括號」「節次括號保留」劃線；補記**現行匯出規則**：J 欄不帶節次括號（Pei 08-17 編修，ENTRY 032 → ENTRY 034 併入 `write_back.render()`），條件出處改由 N 欄承載（R-C49） |
| §3.5 | 補「工作簿 N 欄之呈現」框：stem 去 `SYS1_`（出處 **ENTRY 032／034**）、一節一行、leaf 自身節次置首、外部文件各自成行；JSON 之格式（`spec-ref-stem` gate 所驗）不變 |
| §3.9（於「3.7／3.8／3.9」條） | 「T–Z 一律留白」整句劃線 → 作廢，指向 **R-G74**／R-C48（W／X＝0；T／U／V／Y／Z 本輪維持 1；CMF-02_A 之 DT27＝DT28 確認一併記） |
| §5.1 | 「目前之 `[BLOCKED-SPEC]` 列」加 `SWE1-HVAC-019-03`（row 116、TC ID -466） |
| §3.2 之 AXIS-VALUES 機讀塊 | 軸 13／EMEA／9／2 之 `negation-users` 補入新列之 tc_id（lint `axis-value-count` 要求名單與實際一致；只增不刪） |

同步（非 profile，但同一裁定之落點）：`framework.md` 之 Layer 1 Test Group（劃線改值）；`feature.yaml` 之 `test_group`；`scripts/verify_partn.py` 之 `TEST_GROUP`，並重跑之以重產 `data/test_set_map.tsv`（129 列之 test_group 欄；Part N 之五項檢查全 PASS）。

### 1-3. `DATA_REQUESTS.md`／`ANOMALIES.md`

- **DR-46** 登錄（`$RECIRC_STAT$`／`$EBL_Stat$` 之 DBC message 與 raw 值；High）。
- **DR-47** 新登（`2.3` 之「most closely matches」無值；Medium；RD-1 候選）—— 為 §3 之 PENDING 所需之號。**另記一個不一致**：後排同句之既有 TC（`030-04`，`json-275`）把「the manual mode that most closely matches the exited AUTO mode」照錄為 ER、沒有 PENDING，與本輪處置不同，待裁。
- **A-CMF1～7** 登錄於 `ANOMALIES.md` 文末新節，文字逐字取 review §1。

## 2. JSON 層 patch（`scripts/cmf02_apply.py`，一次性、逐處斷言原值）

change log：`features/comfort/docs/reports/cmf02_json_change_log.tsv`（1,104 列）。**驗證可重現**：以 `git checkout HEAD -- generated` 還原後重跑腳本，所得 129 個 JSON 與 change log **逐位元組相同**，候選本 sha 亦相同。

| 項 | 落實 | 實量 |
|---|---|---|
| M1 / R-C51 `019-03` | `[BLOCKED-SPEC]` 列，tc_id `-466`；Remarks 取 patch plan 之句（Owner 在首 60 字元內）；PC 取 `019-01` 之四行；N＝`2.13` | 1 |
| M2 `103-01`／`103-02` | 見下 | 2 條 × 3 欄 |
| M6 | 見下 | 4 條 |
| M8 | `json-434` → `Climate Popups`；`json-432`／`-433` → `ICS Climate Modes` | 3 |
| M9-Note | `Note X` → `Read X and record it`（`the size and highlight…` 用 `them`） | 25 步（24 條單改 ＋ `json-147` 併於複合拆步） |
| M9-compound | 35 行拆步，ER 同步補行 1:1；新增之 ER 只陳述該步之結果 | **35 行／32 條**（與 CMF-01 實測同） |
| R-C47 | 465 條 priority 依 0907 本 P 欄回灌（`write_back.row_plan` 對位，D 欄逐列斷言） | P1→P0 325、P2→P1 132、P3→P2 8 |
| R-C6(amend) | 465 條 test_group | 465 |
| R-C50 | 26 條上半摘句 | 26（`upper_excerpt_plan.tsv`，最長 43 token，全數過 `test-item-upper-verbatim`） |
| R-C52 | wb-001～004（`json-435`～`-438`）Remarks conflict 句，四句之尾各異並登 `AMBIGUITY_REMARKS` | 4 |
| wb-006 第 3 步 | 依 plan 刪（`json-032`） | 1 |

**逐項之判斷與偏離（請核）**：

- **M2 —— 判兩個 leaf 可分，未停**。14.18 之句為「Popup will have a 5 sec timeout and restart with additional presses」；037 拆成 `103-01`「timeout restarts」與 `103-02`「remains visible for additional 5 seconds」。我的判斷：前者是「**重計有沒有發生**」（第一次按壓後 5 秒時 popup 仍在），後者是「**重計後的長度**」（第二次按壓後 5 秒時 popup 消失）。兩者可以各自失效（例如有重計但新長度是 3 秒），所以不是同一句的兩種說法。
  - 骨架照 CMF-02 §2：① 按下 icon → ② `Press the driver comfort seat icon again before the popup times out` → ③ 觀察點。
  - **`Hold for 5 s` 寫成 `Wait … 5 seconds …`**：本 spec 有「press and hold」這個手勢，寫 `Hold` 會被讀成按住 icon。`103-01` 寫 `Wait until 5 seconds have passed since the press in step 1`（ER：still shown）；`103-02` 寫 `Wait 5 seconds after the press in step 2 without further interaction`（ER：no longer shown）。
  - `3 seconds` 已去除；下半取 patch plan 之提案句並去掉 3 秒。
- **M6 —— `wb-069` 改為三步**：原第 2 步是「設定 ＋ 讀取」，整行改成 PENDING 會吞掉讀取。所以讀取另成第 3 步，PENDING 步之 ER 取 `wb-033` 同結構之句（`The climate screen is displayed`）。**值寫成 `"not available/Blink"` 而不是 `[not available/Blink]`**，因為 JSON 層 lint `ui-bracket` 會擋（兩道 lint 之衝突見 §6）。
- **M9-compound `json-078`（052）**：原第 2 行「Press the AUTO button once, then change the fan speed…」拆開後，第一步的結果不確定（前一步用風速硬鍵時 AUTO 已經被中斷，再按一次 AUTO 會進入哪一態，條文沒說）。所以改寫成既有的 `until … AUTO ON state is active` 形（`048-02` 已用），使拆出的 ER 只陳述該步自己的結果。
- **M9-compound `json-073`（048-03）**：該 sibling 依 R-C44 三不產（§3），第 4 步仍依 M9 拆成兩步。
- **下半之連動**：只有末步或末行 ER 改變的列才重算下半（`test_item.situation()`），其餘手寫之下半不動。拆步後各 ER 中「step N」之回指已逐條複查（`json-147` 改為 `recorded in step 1`；`json-385` 改為 `step 2`）。
- **`019-03` 之 Priority 取 P1**：它沒有同 leaf 之既有 TC 可取級（R-C47 之 sibling 規則不及於此）。0907 本中三條既有 `[BLOCKED-SPEC]` 列都比同 parent 之可測 leaf 低一級（`080-02` P1 vs `080-01` P0；`081-02` P1；`072` P2），`019-01`／`-02` 是 P0，所以取 P1。**這是判斷，請核**。
- **R-C50 摘句**：`104-0x` 之條文在 SYS1 export 中帶字面 `\n`（不是換行），照錄。`104-01` 取「HVACSB6.) … as follows:\n-Temperature Pop-up: …」；其餘 7 條之項在標頭之後，連續子字串無法同時含標頭與該項，所以只取該項之行。

**連帶修改（為使 lint 綠、敘述不過期）**：

- `scripts/lint_tcs.py`：`MARKER_WHITELIST` 加 `-466`（R-C51）；`AMBIGUITY_REMARKS` 加 `-435`～`-438`（R-C52）。
- `scripts/gen_batch9.py`：`WITHHELD` 移出 `019-03`。原宣告逐字留為註解；註解內括號後加一空格，否則 `withheld-not-generated` 之正規式仍會把註解讀成宣告。
- JSON 之 reasoning 三處改一句（句數不變）：`003`（分支 (a) 已產 PENDING 兩條）、`018`（`018-05` 由「維持一條」改為拆兩條）、`019`（`019-03` 已產 BLOCKED 列）。

## 3. Sibling（計畫 23 → 實產 8）

判讀表：`features/comfort/docs/reports/sibling_plan_cmf02.tsv`（每條之 R-C44 三問與停點之答）。

| 結果 | 條 | 依據 |
|---|---|---|
| **產** | `003-07` 分支 (a) A/C、風速（-467／-468，ER PENDING: DR-47）；`009-04` 觸控箭頭（-469）；`010-03` 硬控（-470）；`015-04` 風速（-471）；`018-05` Climate main 內（-472）；`080-01` 觸控（-473）；`111-04` 觸控箭頭（-474） | 三問皆「是」；原條同步瘦身（`009-04`／`111-04`／`018-05` 刪步後補一個 setup 步以守 §10.5 之兩步） |
| 不產 —— R-C44 一（泛稱） | `013-03`、`021-02`、`121-02`（`any other climate functions`，**R-C44 溯源表明列**）；`003-07` 分支 (b) ×4（`a specific mode button` 為泛稱，四模式列舉在 2.12 而非本句） | R-C44 一 |
| 不產 —— R-C44 二（值域） | `023-01` 組合 ×4（7 種組合是逐鍵 toggle 之結果，其全集已由 `023-02` 之循環條驗）；`050` AUTO ON（`the selected setting` 之取值） | R-C44 二 |
| 不產 —— R-C44 三（舉例） | `048-03`（10.5「e.g. fan speed, airflow mode etc」）；`003-06` front defrost（「(including front defrost)」—— **字面適用**，R-C44 三明列 `including`；若分析層判此處 including 是範圍界定而非舉例則 +1） | R-C44 三 |
| 不產 —— 停點 | `005-04` RECIRC（見 §0） | CMF-02 §3 停點二 |

- **TC ID**：`019-03` 取 -466，sibling 依 req_id 遞增自 -467 至 -474（R-C46）。
- **每條新 sibling**：
  - 下半與同 req 既有各條兩兩相異（`test-item-lower-distinct` 通過）。
  - 介面型四軸取所屬 doc 之 `interface_axis_review`：通道不同時只換該通道之 PC 行（例如 `010-03` 硬控條之 PC 改為 `The vehicle has a fan speed hard control (2.7)`，出處同 `010-04`）。
  - Priority 取同 leaf 既有 TC 之級（R-C47）。
  - `emea_ics_review` 沿用同 leaf 既有條。
- **`018-05` 之 `press and hold`**：回條文判讀，C13.1 之「press and hold of the control will only move one mode over」屬 `018-03`，不屬本 leaf。**不產**，記於判讀表。
- **原條同步瘦身**：`009-04`、`111-04`、`015-04`、`018-05` 各移出拆給 sibling 之步驟。原條之 `split_flag`／`split_reason` 同步設定（`req-id-unique` 要求同 req 之每列皆宣告），`003-07`、`080-01` 亦同。

## 4. Revise 候選本（`scripts/cmf02_revise.py`）

- 產出：`features/comfort/sandbox/revise_0918/…_SWQT_ComfortHMI_20260918.xlsx`
  - sha256 `28a0998c8b36a88b182b3fa209492cc5c6fda3999f48e6031d571d66439a266f`
- 母本：`delivered/…_ComfortHMI_20260907.xlsx`（先驗 sha `3550d16d…`）之複本，`surgical_save` 寫入；C／E／O／AA 既有格不動。

| invariant（ABORT 級者標 ★） | 結果 |
|---|---|
| ★ zip member 數 | 57 ＝ 57 |
| 差異僅目標 sheet | `xl/worksheets/sheet6.xml` 一個 |
| ★ DV sqref（classic ＋ x14） | 逐字相同：`P10:Q601`、`AF10:AF601`、`T10:Z601`、`R10:R601` |
| B 欄公式延伸至新列 | 0907 本之 B 公式本即至 row 601，新列 476–483 已涵蓋；row 10–601 逐列原樣 |
| 新列在 DV 內 | 476–483 皆在 |
| **逐格對帳（§4-5）** | **不符 0 格**／比對 21,309 格（全部工作表）；變更 1,731 格：R-C48 930、R-C49 452、§3 sibling 184、M9-compound 73、R-C50 26、M9-Note 24、R-C51 12、§3 slim 8、M2 6、M6 6、R-C52 4、M8 3、wb-006 3。逐格表 `docs/reports/cmf02_candidate_cell_recon.tsv` |

- **列**：
  - 既有 466 列之列位、F、E 不變；old→new 無（既有 ID 未動）。
  - row 116 = `019-03` BLOCKED 列（F = -466）。
  - 新列 row 476–483，O＝`NEW`、AA＝`PeiPYHsu`、S＝`NA`、E 空，C 取 0907 本同 req 既有列之 C。
- **車型欄（R-C48）**：
  - W／X 在 row 10–475 全改為 `0`（含 row 116）。
  - T／U／V／Y／Z 既有列不動；新列填 `1`，W／X 填 `0`。
- **R-C49**：
  - 改 `write_back._compact_refs()`。
  - 全量 before／after 對照 474 條：**節次集合增減 0**、自身節次置首、其餘升冪皆成立（`docs/reports/cmf02_rc49_refs_before_after.tsv`）。
  - 候選本 N 欄 452 格改寫；另 13 格原本就只有單一節次，寫法不變。
  - **外部文件行排在 Comfort 各行之後**：canon §10.7 之「CFTS 行在前」與 R-C49 之「leaf 自身節次置首行」在此相衝，我依 R-C49。
- **呈現面（Tier 3，未處理）**：
  - 新列 476–483 沿用範本空白列之樣式（列高 14、s=86／87），與 row 475 之資料列樣式（列高 168、s=81／111）不同，Excel 中新列會看起來擠。
  - 程式不改樣式與列高（26 §2 之方向 3 已裁），請 Pei 於 Excel 開檔時判斷。
- **列序**：新列在尾端（R-C46），所以 D 欄自 row 476 起不遞增。這是裁定之結果；`write_back.py` 之 `row-order-by-reqid` assertion 不適用於本候選本。

## 5. 唯讀調查（不改任何 TC）

### 5-1. PROXI 軸值表 —— `features/comfort/docs/reports/proxi_axis_values.tsv`（sha `e6d229b5…`）

- 依 CMF-02_A §1 修正版執行。TSV 表頭註明：**U 欄 ↔ `DT28_ATL_HI` 之對映出自 Pei 2026-09-18 之確認**；T 欄兩份並列、不擇一；W／X 無 PROXI。
- 格式為 long format（來源 × 參數），逐筆附出處格。
- **T 欄 `HDCC27_initial` vs `HDCC28_ATL_HI`：七個參數之 `.xlsx` Data 值完全相同，無差異**。
  - 兩檔整體有 441 列不同（Configuration Code、CAN node 等），都不在這七個參數上。
  - `.txt` 原始 byte 77／78 有差，對照 bit 位置後落在 `Anti_Fog_Sensor_Humidity`／`Mirrors_System`，也不在這七個參數上。
- 附帶：`DT28_ATL_HI` 與 `HDCC28_ATL_HI` 在這七個參數上也相同。

| 參數 | T HDCC27_initial | T HDCC28_ATL_HI | U DT28_ATL_HI | V Promaster | Y Toro | Z Fastback |
|---|---|---|---|---|---|---|
| Climate_Type | 0=Absent | 0=Absent | 0=Absent | 2=Automatic | 1=Manual | 2=Automatic |
| Rear_Climate | 0=Absent | 0=Absent | 0=Absent | 0=Absent | 1=Present | 0=Absent |
| Climate_Panel_Type | 0（檔內無 label） | 0 | 0 | 7=No knobs | 6=3 knobs | 0=Only blower knob |
| Integrated_Climate_Touchscreen | 0 | 0 | 0 | 1=Present | 1=Present | 1=Present |
| Automatic_Recirculation | 0 | 0 | 0 | 0=Absent | 1=Present | 0=Absent |
| HVAC_AUTO_ECO_Mode | 0 | 0 | 0 | ABSENT | ABSENT | ABSENT |
| HVAC_Mode_Type | 0 | 0 | 0 | 0=No Trimode | ABSENT | ABSENT |

- **只並列，不作「值 → profile 第 N 軸」之判讀**（CMF-02 §5-1）。
- 兩件事供分析層參考（只陳述，不判讀）：
  - HDCC／DT 三份之 `Climate_Type` 等皆為 `0=Absent`，而 0907 本 T／U 兩欄全填 `1`。
  - Promaster／Fastback 之 `.XLSM` 中，多個參數之 Y 欄 `Dec` 與 AC 欄 `PROXI Read` 不一致（例：Climate_Type Y=2、AC=3），兩者都照錄。

### 5-2. Comfort 入口（R-C53）

- **查得**：`spec-index/cache/SYS1_HMI_Menu_Bar_and_App_Drawer_HMI_Logic_and_Flow_R1_SR24_3A_(September_11_2023).xlsx`，sheet `Basic Report`，**row 22**（NRL-127734，§4.1 命名表），逐字 `Comfort | Comfort | Comfort | Core Feature | Comfort`。
- `forms/` 下無此檔，只查 `spec-index/cache/` 那份。
- 命名表中**無任何 Feature Name／Drawer Name 為 `Climate` 或 `HVAC`**。
- HMI Settings List（`forms/` 與 `inputs/` 兩份，bytes 不同但命中相同）：有 `18. Seats & Comfort` 等設定類別，**無 Comfort／Climate App 之入口**。
- **提案 `ENTER_COMFORT`**（仿 canon §5.3 `ENTER_SETTINGS_APP`，2 hops，§5.8）：
  ```
  ENTER_COMFORT (2 hops): `Press "Apps" on Menu Bar to open App Drawer`
    → `Select "Comfort" in the App Drawer`
    Source: …Menu_Bar_and_App_Drawer…xlsx, sheet `Basic Report`, row 22 (NRL-127734,
    §4.1 naming table — `Comfort | Comfort | Comfort | Core Feature | Comfort`)
    and row 23 (NRL-127735, ADC1: all configured apps are shown within the App Drawer)
  ```
  - 另 row 11（RMBC4：「If vehicle is not equipped with ICS, remove Comfort from the Default Configuration」）意味有 ICS 時 Comfort 在 Menu Bar 之 Default Configuration 中，可寫成 1 hop。但條文未給 Menu Bar 上之按鈕 label，**未擇此路**，請裁。
- **`Climate` 作為入口則查無** → DR 提案（未登）：`PENDING: DR-{n} HMI entry path Climate` — "Which HMI entry (Menu Bar / App Drawer label) opens the Climate screen? The §4.1 naming table lists `Comfort` but no `Climate`/`HVAC` Feature Name; ADC5 mentions only a 'Climate Category Button'."
- **本輪未改任何 Procedure 之導航步驟**（R-C53）。

### 5-3. 0907 本之 Cover／Product Document／Test Case Framework 現況（只報不改）

| 分頁 | 有值之格 |
|---|---|
| `Cover 封面` | C4 文件名；D6 版本 `C`；D7 核准者 `劉安哲 AllenACLiu`；D8 審查者 `張愷霏 ErinKFChang`；D9 作者 `許珮瑜 PeiPYHsu`；**G9 修訂日期 2026-09-07**（datetime）；C12；H30 `FM-WI-FSM-036-A01` |
| `Product Document 記錄封面頁` | B3 `NR1L`；B4 `…_SWQT_ComfortHMI`；B5 `Initial Release`；B6 `SW Testing`；B7 `Confidential`；**B8 2026-09-07**；A13–D13 `V1.0`／`Initial Release 初版發布`／`許珮瑜 PeiPYHsu`／2026-09-07 |
| `Test Case Framework` | **儲存格全空**（dimension `A1`）；內容只有一個 SmartArt（`drawing7.xml`），其文字含 19 個標籤，**內含 `HVAC Pop-ups` 與 `EMEA ICS Interface`**（即 M8 修掉之兩個錯誤 Test Set 名），以及 `Climate Control Interface`、`Front Climate Anatomy` 重複一次 |

- **`J5`** 在 `Test Case Specification 測試用例規範` 分頁（其餘分頁之 J5 皆空），值為**字串** `'2026/8/17'`（number_format `@`，非日期型別）。
- Cover G9 與 Product Document B8／D13 皆為 2026-09-07，**與 J5 之 2026/8/17 不一致**。候選本未改這些格。

## 6. 自檢

- **JSON 層**：`lint_tcs.py` **57/57 PASS，474 TCs**，輸出 `sandbox/revise_0918/lint/json_layer_lint_tcs.txt`。
- **候選本層**：`lint036.py --profile comfort`（report mode）。0907 本基線與候選本之計數：

| 檢查 | 0907 | 候選本 | 差之成因 |
|---|---|---|---|
| L | 26 | **0** | R-C50 生效（上半 > 50 token 者全數摘句） |
| J（首字小寫） | 0 | **14** | R-C50 之摘句取句中段，以小寫開頭（`a pop-up…`／`for MTC…`／`if the user…`）。連續子字串之限制下無法避免 —— **R-C50 與 J 相衝，請裁**（放寬 J 對 test_item 上半，或允許摘句從句首起算並接受 > 50） |
| P | 0 | **4** | M6 之 PENDING 行：lint036 P 要 `[…]`，Comfort `ui-bracket` 禁 `[…]` —— **兩道 lint 相衝，請裁** |
| U（PENDING 佔位） | 0 | 6 | 即 §0 之 6 行 PENDING |
| M（空欄） | 8 | 10 | `019-03` BLOCKED 列之 L／M 空（與既有 4 條 BLOCKED 列同形） |
| R（PC 並列） | 1450 | 1476 | 新列沿用既有 PC 寫法（既有列同型 1450） |
| X（導航無固定入口） | 319 | 335 | 新列沿用既有導航寫法；R-C53 本輪不改導航 |
| I-cross | 433 | 441 | 新列之「窗未完整宣告」（既有列同型） |
| H／W | 4／3 | 4／3 | 初版 PENDING ER 行用了 `matches` 一詞觸 H／W 各 +2，已改寫措辭，回到基線 |

- `scripts/canon_refs.py --report --top 1000 --waiver`：本包新增／改動之檔案 **unresolved／ambiguous 0 筆**。全庫 34／178，與本包改動前（stash 後重跑）相同，非本包造成。
- `DELIVERY.sha256` 追加 **ENTRY 037**（type `candidate`，checksum 行指向 sandbox 路徑）；`shasum -c --ignore-missing` **58 OK、0 FAIL**。
- **自報**：
  1. **`gen_batch*.py` 不可再跑**：本輪之 patch 直接寫在 `generated/*.json`，而各 parent 之 JSON 由其 generator 整檔產出，重跑任何一支 generator 都會覆寫本輪之改動。`test_item.py` 之 `HAND_WRITTEN` 仍是 `json-192` 之舊下半（未改；只有 generator 會用到它）。
  2. **`write_back.py` 之 `_compact_refs()` 已改為 R-C49 形**。副作用有兩個：`write_back.py --verify-only`（ENTRY 035 之 rowsort 檔）與 `diff_0907.py` 重跑時，N 欄會報不符 —— 那是匯出規則改了，不是內容變了。另 `write_back.py` 之 N 欄節次 assertion 之正規式已補 `\n` 為分隔。
  3. **R-C52 之「入 RD feedback」**：該衝突已由 DR #18（`2.1` 之 037 leaf 與條文於 tab 數與順序不一致，RD-1 候選）承載，**未另開新列**。
  4. **停點命中之 `005-04` RECIRC** 若要處理，可以登一個 DR（「Recirc 自動開 A/C 時是否顯示」），或裁定把既有 `json-139` 第 3 步（沿用 037 之 `change not shown`）也改為 PENDING。**本輪都沒做**。
  5. `upper_excerpt_plan.tsv` 是在 JSON 寫入**之後**才從腳本資料匯出的，不是「先出 plan 再套用」。內容與套用值逐條斷言相同，但順序與 CMF-02 §2 之「先出」不符。

## 7. 產物與 SHA256

| 路徑 | sha256 |
|---|---|
| `features/comfort/sandbox/revise_0918/…_SWQT_ComfortHMI_20260918.xlsx`（候選本） | `28a0998c8b36a88b182b3fa209492cc5c6fda3999f48e6031d571d66439a266f` |
| `features/comfort/generated/*.json`（129 檔，474 TCs） | 見 change log；可由 `cmf02_apply.py` 自 HEAD 重現 |
| `features/comfort/scripts/cmf02_apply.py`（新增） | `8afb38bd536ec6cc3334a36711bf7fe9efbf58d17bd3f0fd9a0d6d67559363a1` |
| `features/comfort/scripts/cmf02_revise.py`（新增） | `cca9ff49530665a1fe219c17b4b7dde662accbd5c9ba46a43e783fd115619c4e` |
| `features/comfort/docs/reports/cmf02_json_change_log.tsv` | `65c5697530f699a9ff83b6e069c694c46174023236e970d0d9d7116ac6ace419` |
| `features/comfort/docs/reports/cmf02_candidate_cell_recon.tsv` | `45c0cb10cb7453e351d87b875655e2a1dd4a32b1826b94a8bbd251e3d0770be5` |
| `features/comfort/docs/reports/cmf02_rc49_refs_before_after.tsv` | `718dbb2dfb252d343200be3a87c4c3025a9b6068c383b851d9a266c78f238519` |
| `features/comfort/docs/reports/upper_excerpt_plan.tsv` | `92a62ab5b5d15bc64f79cff374680265b8ccfbaa653c38b386b7eb6975dc89ee` |
| `features/comfort/docs/reports/sibling_plan_cmf02.tsv` | `eb0dba49192daf58641f93588055eb22a2ae1f4bdee708cba0c60c64190b449d` |
| `features/comfort/docs/reports/proxi_axis_values.tsv` | `e6d229b586f31294a924fd0a2bf3c7309b8e50ac9237fa02ee6f9e662fe9f6eb` |
| `features/comfort/docs/reports/label_quote_plan.tsv` | `34ceb75da0cc2156ed93af3979bc851f61f367fdd38531974eb8eb7015988f33` |
| `features/comfort/sandbox/revise_0918/lint/LINT_STATUS.txt` | `9a4c5276d68e86c2dd4c3487bad0093627cda51dfec9ce02cddc60edd20c03ed` |
| `features/comfort/RULINGS.md` | `adc0361205f2912fa143780ac012a9b5d85ce458a224defd864533f141e46aa5` |
| `docs/runtime/profiles/FW036_R1L_Comfort_Profile.md` | `97d406d3bb8c26d11856568129d73c9d5b5e563785a21ff62909ea69c14b7ce8` |
| `features/comfort/DATA_REQUESTS.md` | `68f94ed231beed2e435848e2dbc29fa132686c0ee77f61155035e4877d1c02f1` |
| `features/comfort/ANOMALIES.md` | `760d7c804036c90d893e0526be6167fbc5f203af56db3074f1ef9c9e8d8b3bce` |
| `features/comfort/DELIVERY.sha256`（追加 ENTRY 037） | `59e53f5b8d0d006730fd88bd1eb74b1137a583275042d9b3822c8b4fe5c2ddb2` |
| `features/comfort/feature.yaml` | `eff3e39feb237fcaa886c5947b7b692fac25576f1f4ee161681dc2c6a1d88663` |
| `features/comfort/framework.md` | `b6af932028fc548b7f1a9c5835557c00291c2980caf43b7cef215b2d2fa70b19` |
| `features/comfort/data/test_set_map.tsv`（`verify_partn.py` 重產） | `00469a89ca06f57c866ab340e2c91020f2b4fbb0cf7b6e1b61a5e2b62694c9ea` |
| `features/comfort/scripts/write_back.py`（R-C49） | `c3edd6f4af883d0b1d2dadc03d2dd82167ab633abbe679f2db7858e2fb170792` |
| `features/comfort/scripts/lint_tcs.py`（白名單／登錄簿） | `c325b76da84e48ee0a56606a2e02e6b12907beb58c11e26517a667d5f6e6ca89` |
| `features/comfort/scripts/gen_batch9.py`（WITHHELD） | `861795753cb81e59cbaa83dbca2be6eadfd3b2185ea5fa45390b4fed2d73c21c` |
| `features/comfort/scripts/verify_partn.py`（TEST_GROUP） | `1142c973a526c5ce21d99c8866d5907c9ba737f7e72a17e686cd913e35c1a2d3` |

**old→new：無**（既有 466 列之 F／E 未動；新增 ID -466～-474 為接尾）。

## 8. M7 引號 —— `label_quote_plan.tsv`（本包不改，只出 plan）

- 來源為**候選本**之 L＋M 兩欄（row 10–483），逐 hit 一列：339 列，分 **label 144／state 184／verbatim 11**。其中 25 列於 `note` 標 `ambiguous:` 並附理由。
- label 列之 `suggestion` 給整行改寫，同一行之 label hit 一併加引號。state／verbatim 列一律 `keep`。
- **計數與 CMF-01 對照**（同一支程式重掃 0907 本得 AUTO 161／43，與 CMF-01 完全一致，所以差異來自內容變動，不是口徑不同）：

| token | 候選本（未引號／已引號） | CMF-01（0907 本） | 差之來源 |
|---|---|---|---|
| AUTO | 173／45 | 161／43 | 新列 8 條帶入（`The AUTO state is AUTO ECO`、PENDING DR-47 ×2 等），加上複合步驟拆步後之重組 |
| Face／Feet／RECIRC／Sync | 54／55／29／4 | 同 | — |
| SYNC | 24（已引號 39） | 24（37） | 新列 |

- **分類之對齊**：label 之判準對齊工作簿內既有之已引號同句型，例如 `Turn "SYNC" on`、`Select the "Feet" airflow mode`。
- **待分析層裁定（影響最大者在前）**：
  1. **airflow mode 名稱（Face／Feet 等）在 `The X airflow mode is active` 句中是否屬「自 UI 讀出之值」（§11）**。目前判 state，共 91 列；若判為值，全數翻為 label。
  2. `read the X state` 之慣例衝突：row 433 L2 已寫 `read the "RECIRC" state`，其餘同型未引號。
  3. `Set RECIRC to <value>`／`Change RECIRC`（6 列）判 label，另一讀法為 state。
  4. `Set the airflow mode to Face …`（018-01 起始條件）判 state，與 `Select the "Face" airflow mode` 不一致。
  5. `Sync`／`SYNC` 之拼寫：R-C2 未定按鍵標籤與內文是否同寫。`Turn Sync on` 之建議同時改拼寫。
  6. `AUTO pop up` 是 pop-up 之類型名，判 verbatim（ambiguous）。
  7. Tri-Mode 之 `MODE`、狀態值 `Auto`／`Manual`／`Open` 不在本次 token 範圍。
