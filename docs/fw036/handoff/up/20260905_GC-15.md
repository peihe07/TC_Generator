# 上繳包 GC-15（2026-09-05）

**session 標識**：執行層 **A**（GC-15 下放包指派）。
**觀察依據（R-G62(b) 附記）**：接手時 HEAD＝`531fc28`；
起點前最後一次他方寫檔為 `down/20260905_GC-15.md`（分析層）。
**施作期間未觀察到他方寫檔。**

**工具旗標逐次聲明（R-G50(d)）**

| # | 工具 | 旗標 | 寫檔 |
|---|---|---|---|
| 1 | `rulings_hash.py` | `--check` | 唯讀 |
| 2 | `rulings_hash.py` | `--out <scratchpad>/dry6.tsv` | **工作區，未寫 repo** |
| 3 | `canon_refs.py` | `--waiver` | 唯讀 |
| 4 | `lint_docs036.py`／`lint_paths.py` | — | 唯讀 |
| 5 | `lint_delivery_spec.load_manifest()` | — | 唯讀（驗加欄後仍可讀）|
| 6 | `pytest` | — | 唯讀 |

**未跑**：`gates_tsv.py`／`selfcheck_map.py`／`rulings_hash --out <repo>`。
**本包未動任何 xlsx。**
⚠ **本包不需要 Pei 重生指紋** —— `--check` 為 **OK（768 條）**，見 5 節。

---

## 1　出貨記錄（下放包 一）

### 1-1　sha 現查 —— 三本逐字相符，未觸發停手

| 本 | 現查 sha12 | GC-14 8 節所載 | 判 |
|---|---|---|---|
| `pm_73_Revise1.xlsx` | `e9ccddbbd673` | `e9ccddbbd673` | ✓ |
| `…_ICSManagement_20260830_Revise1.xlsx` | `3554a2b28642` | `3554a2b28642` | ✓ |
| `…R1Low_20260902_Revise1.xlsx` | `70657bdf3f1f` | `70657bdf3f1f` | ✓ |

### 1-2　MANIFEST —— **`status` 欄為新增，非「改」**

三線之 MANIFEST 原表頭皆為 `filename / sha256 / source_path / delivered_round / note`，
**無 `status` 欄**（GC-15 上繳前一版 2-2 節已提出）。下放包令「`status` 改 `shipped`」，
實為**加欄**。本包加之，並：

- Revise1 列：`status = shipped; shipped_date=2026-09-05; shipped_by=Pei; spot_check=9/9 (seed 20260905)`
- 母本列：`status = superseded_by=<Revise1 檔名>`
- **其餘歷史列一律留空** —— 其狀態逐列指派屬內容判斷，非本包所令。
  留空列數：power **1**、ics_management **0**、vsm_v42 **0**。

**相容性已驗**：`scripts/lint_delivery_spec.py` 之 `load_manifest()` 為表頭驅動
且以 `min(len(head), len(cells))` 取值，**容忍多欄**。加欄後實跑：

```text
features/power/delivered            3 列讀入，Revise1 之 status = shipped; shipped_date=…
features/ics_management/delivered   2 列讀入，Revise1 之 status = shipped; shipped_date=…
features/vsm_v42/delivered          2 列讀入，Revise1 之 status = shipped; shipped_date=…
```

⚠ **留空之欄不是「無狀態」而是「本包未裁」** —— 若日後以 `status` 為機讀依據，
須先補完歷史列，否則會把留空讀成某個預設值（同 `DATA_REQUESTS.md` 之 0/96 形態）。

### 1-3　RULINGS —— 三線各補一段

`features/{power,ics_management,vsm_v42}/RULINGS.md` 之
「交付本之取代紀錄 —— R-G72(g) Revise-M」節各加：
出貨日 **2026-09-05（Pei）**、抽驗 **9/9 OK**（seed `20260905`，各 3 列對 dry-run 表）、
commit `c36635f`、殘項數與 `unclassified 0`、清單路徑、
以及 MANIFEST 之 `status` 兩側值。

---

## 2　R-G72 指紋差異證明（下放包 二）

### 2-1　diff 全文 —— **14 行插入，限二塊，無他處變動**

```text
$ git diff 7fd0ecd c36635f --stat -- docs/fw036/RULINGS_LEDGER.md
 docs/fw036/RULINGS_LEDGER.md | 14 ++++++++++++++
 1 file changed, 14 insertions(+)
```

逐塊歸因：

| 位置 | 插入 | 歸因 |
|---|---:|---|
| `### R-G70` fenced block，`(h)` 之後 | **5 行** | **R-G70(i)**（GC-14 施作，PROXI fallback 只在賦值形態時報）|
| `### R-G72` fenced block，`(g)` 之後 | **9 行** | **R-G72(h)** 5 行（GC-14 施作）＋ **其加註** 4 行（GC-14 審閱 二 所令，本層於下放前先做，見 2-2）|

**無他處變動**（`14 = 5 + 9`，`--stat` 之插入數與逐塊相加相等；刪除 0 行）。

### 2-2　`07a74237` → `a7b9460c` 之成因，與自報

GC-14 上繳 5 節所報之乾跑值 `07a74237` 係在**加註之前**取得；
本層於上繳後、GC-15 下放前，先做了 GC-14 審閱 二 所令之
「R-G72(h) 加註 ＋ 未知 detail 測試」，該 4 行加註使 R-G72 之 body 再變，
Pei 重生遂實得 **`a7b9460c`**。

**痕跡與後果**：Pei commit `c36635f` 前工作區已有
`M RULINGS_LEDGER.md`／`M rg72_residual.py`／`?? tests/test_rg72_residual.py`，
三者被掃入該 commit；本層自己的 `531fc28` 因而只剩上繳包 1 檔。

**登 A-GC27**（形態＝**下放前施作**）。內容雖為審閱所令，
其時序使上繳所報之乾跑值與 Pei 重生值不一致，
而**該不一致在下放包指定複驗前無人可察**。
**本上繳一律引 `a7b9460c`，不再引 `07a74237`。**

---

## 3　`driver_distraction` 積欠結案（下放包 三）

三項查證**全數符合 R-G64**：

```text
git ls-files features/driver_distraction | grep -E "xlsx|feature.yaml"
  features/driver_distraction/feature.yaml
  features/driver_distraction/sandbox/driver_distraction_00.xlsx
  features/driver_distraction/sandbox/driver_distraction_00_bak.xlsx
grep -n "workbook:" features/driver_distraction/feature.yaml
  17:  workbook: "sandbox/driver_distraction_00.xlsx"
python3 scripts/lint_paths.py
  delivered/ sha 對照：OK
  PASS: 基線外違規 + delivered 不符 = 0
```

完成於 commit `a93ff89`。

### 3-1　⚠ 結案紀錄之落點與下放包所指不同 —— 二個理由

下放包令「於 GC-06 審閱 四 之 3 之積欠項加『已於 <commit> 完成』」。**未照做**：

1. `docs/fw036/handoff/down/20260905_GC-06_review.md` 為 **`down/` 唯讀**（PROTOCOL），
   執行層不得改分析層之審閱檔。
2. 改記於 `docs/fw036/FEATURE_ONBOARDING.md` 之 R-G64 節內亦**不可** ——
   該節 body 屬 `rulings_hash` 之雜湊範圍，**實測寫入後
   R-G64 之指紋由 `34b81800`（21 行）變為 `e6d073ca`（29 行），而其條文一字未改**。
   本層已寫入後發現 `--check` 轉紅，**已撤回該寫入**。

**改落 `features/driver_distraction/RULINGS.md` 之新節**
「積欠結案紀錄 —— R-G64 產出物目錄政策」，節首明載「非條文，不立條號」，
並記上述指紋理由。撤回後 `--check` 回 **OK（768 條）**。

⚠ **可推廣之教訓**：凡「紀錄類」文字寫入 `rulings_hash` 掃描面之
**條文節 body** 內，都會改動該條指紋而其條文未變 ——
掃描面為 FO ＋ 台帳 ＋ `features/*/RULINGS.md`（`default_targets()`）。
落點須避開條文節 body，或另起同級標題。

---

## 4　`rg72_residual.py` 未知 detail 測試（下放包 四）—— 驗收

`tests/test_rg72_residual.py` 已在 `c36635f`。**9 項全綠**：

```text
test_known_details_are_classified[v3 記法殘留（R-G70(h)：`$`…]        PASS
test_known_details_are_classified[Procedure 之 CAN 賦值行缺 `Send …]  PASS
test_known_details_are_classified[賦值未寫成 `<MSG>.<Sig> = <raw…]    PASS
test_known_details_are_classified[PROXI 行形態不合 R-G70 v4.1 之…]    PASS
test_unknown_detail_falls_to_unclassified[future]                     PASS
test_unknown_detail_falls_to_unclassified[empty]                      PASS
test_unknown_detail_falls_to_unclassified[v1]                         PASS
test_unknown_detail_falls_to_unclassified[bracket]                    PASS
test_unclassified_is_not_reachable_by_a_known_class                   PASS
```

R-G72(h) 之加註亦已在 `c36635f`（見 2-1 之 9 行中之 4 行）。**二者皆驗收。**

### 4-1　覆蓋率訂正：**4／6，非 4／4**

寫測試時發現，上列 `[v1]` 與 `[bracket]` 二個「未知」樣本，
其實是**現行 lint 今天就會產生**之 P detail：

| P detail | 產生處 | 現行分類 |
|---|---|---|
| `三件組已撤銷（R-1 v1）…` | `RE_P_TRIPLET`（`check_signal_line`）| **unclassified** |
| `車輛屬性之值須以 […] 包覆（037 逐字記法）…` | `check_vehicle_property_line` | **unclassified** |

**處置：維持 `unclassified`**，並於 `scripts/rg72_residual.py` 之 docstring 明載 ——
該二種之去向須人裁，**不得預設歸入 Revise-C**；落 `unclassified` 即擋出貨，正是該值之用途。

**對三本出貨判定無影響**（該二種命中皆 0，`unclassified` 仍為 0）。
但 **GC-14 上繳 7-2 之自報須訂正**：原寫「目前四類已涵蓋」，
應為「**四類涵蓋六類中之四**；三本之 0 是『這三本沒有』，非『本表已涵蓋全部』」。

---

## 5　對照向

| 量 | 本包前 | 本包後 | 說明 |
|---|---|---|---|
| `rulings_hash --check` | OK（768）| **OK（768）** | 本包**不需重生** —— 曾因 3-1 之寫入轉紅，已撤回 |
| `canon_refs` unresolved | 18 | 18 | 不變 |
| `canon_refs` ambiguous | 141 | 141 | 不變 |
| `pytest tests/ -q` | 8 failed／1347 passed | **8 failed／1347 passed** | 紅同一批（HEAD 既紅）；本包未增減測試 |
| `lint_docs036` | PASS | PASS | |
| `lint_paths` | PASS | PASS | |
| 三本 Revise1 sha | `e9ccddbb`／`3554a2b2`／`70657bdf` | **不變** | 本包未動 xlsx |

**指紋現況**（`--check` OK，故現行表即實況）：

```text
R-G70   c964dfb8      R-G72   a7b9460c
```

---

## 6　自報

1. **A-GC27（下放前施作）** —— 見 2-2。本層於 GC-14 上繳後未待下放即做審閱所令之項，
   致上繳所報乾跑值與 Pei 重生值不一致。條文與產物皆無誤，差異已逐行歸因。
   **後續一律待下放再動檔，乾跑值於同一包內回報。**
2. **3-1 之寫入曾使 `--check` 轉紅** —— 本層先把積欠結案註寫入 FO 之 R-G64 節 body，
   R-G64 指紋隨之改變而其條文一字未改。**已撤回並改落點**，
   `--check` 回 OK。此為本包內自行發現並修正，非事後補述。
3. **MANIFEST 之 `status` 欄為新增而非修改**（1-2），且歷史列留空 ——
   留空之語意須在以該欄機讀前先補完。
4. **GC-14 上繳 7-2 之自報不夠精確**，本包 4-1 訂正為 4／6。

---

## 7　未結 DR

`DR-PW25`、`DR-VS{n}`（Power_Tailgate_Enable）、HMI entry path 草稿 ×3
—— **皆待 Pei 取號送出**；bare-$ **357** 處（pm_73 209／ics 101／vsm_v42 47）
＋ pm_73 之 **26** 處字串值，待 Revise-C 首包逐處登。

---

## 8　待 Pei

1. **本包不需重生指紋**（`--check` OK 768）；commit 即可。
2. **1-2 之 `status` 歷史列**：是否補完、由誰補、值域是否採 GC-15 前一版所提之
   `shipped`／`superseded`／`pending_ship`／`withdrawn`。
3. DR 取號送出（`DR-VS{n}`、HMI entry path ×3）。
4. Revise-C 首包：分析層先出 pm_73／sw_update 之每 TC 來源錨表，本層待命。
