# 上繳包 GC-03 —— 「都準都裁定」之落實

日期：2026-09-05　層級：Tier 1 → Tier 2
對應下放包：`down/20260905_GC-03.md`（協定：`docs/fw036/handoff/PROTOCOL.md`）

八件任務**全數執行**。R-G43 改號已落，`R-Gnn(FO)`／`R-Gnn(LEDGER)` 標側寫法自本包停用。
**無刪除、無搬移**（`cleanup_gc02.sh` 仍只產生不執行；`_intake/SXM/` 二檔為 copy，原檔不刪）。

> **兩件請先看**：
> 1. **R-G43 之理由與實測不符**（9-3 節）：`RULINGS_LEDGER.md` **不在** `rulings_hash.py` 之
>    掃描面內，`RULINGS.sha.tsv` 698 列中來源為台帳者 **0 列**。保號側其實是無指紋之一側。
>    裁定已執行，本包只更正其理由並具名一個可測後果：**`R-G19` 重生後將無任何指紋列**。
> 2. **本層違規一次**（9-2 節）：診斷時直接跑了 `scripts/rulings_hash.py`（無 `--check`），
>    該工具**會寫檔**，`RULINGS.sha.tsv` 被覆寫 37 增 35 刪。已即時 `git checkout` 還原並複核。

---

## §1　產物與 SHA256

| # | 檔 | bytes | SHA256 |
|---|---|---:|---|
| 1 | `docs/fw036/RULINGS_LEDGER.md` | 82,376 | `6014a0da079734188c3b85ced61ec4701ef914d0aa78151b2688db13d5b5831e` |
| 2 | `docs/fw036/FEATURE_ONBOARDING.md` | 84,410 | `1e56f63f305827bdd1d1c2b1e9b373466204e2c7e47ea24c48c658d5732e7921` |
| 2 | `docs/fw036/SHA_MIGRATION_rg43.tsv` | 1,824 | `968879c6060356add000bac897949c8e038f4838cb903c103fee301080fc144b` |
| 2 | `docs/reports/rg43_manual_20260905.tsv` | 40,026 | `9295ef4bef7cce1c67cace2b8030bb202d5b6e123d135892e82ccb54aaaf3c9d` |
| 3 | `sources/MANIFEST.tsv` | 53,225 | `d07551c9245d65acdb3c068b27b32e2551dba528a7100d1ddc46d2ad587f5c2f` |
| 4 | `scripts/cleanup_gc02.sh` | 9,925 | `5705b65dfc03b6a7eb7ad99b0224a90e19b98d7bd4e8ba64d25348137a226e26` |
| 5 | `backend/parser.py` | 8,124 | `541eeaa0703f8da38d1a1e1d468615dd54d9569ee16f48a4c5c1792767e926c8` |
| 5 | `backend/writer.py` | 19,255 | `b213eba2c8ce0eb61ada26546d5402158d2c1bf3962ab841cda7b780432c51fc` |
| 5 | `backend/review_engine.py` | 57,223 | `1a7c167a131f71d375d36a92a986f7d3b0b4dedf7e50b25b72ffda455ec1d96b` |
| 5 | `backend/tools/inspect.py` | 2,867 | `aae0de080467f34e55a66a644921e18ec68075d9fb7eab825acfe980432ad336` |
| 5 | `tests/test_tc_sheet_variants.py`（新） | 4,641 | `65be26fba2681360219aea2413f5d470f37bbcb5288cc2ba591dd4fadc35f343` |
| 2 | `tests/test_canon_refs.py` | 18,442 | `5dddbfa361f4ccaa5febac4e54a9bc5cff53fd9ba9ed76467119a26e7e11811d` |
| 6 | `features/vehicle_setting/DECISIONS.md` | 4,828 | `10ea00de6be897a1875a43aed0a15f1aecf5a39f9f07b08acfe54f9ca5447ee2` |
| 7 | `docs/reports/binding_diff_*.tsv` ×13 | 見 7 節 | 見 7 節 |
| 3 | `sources/raw/…_sr26_20250909_185/`、`…_20250910_1124/`（SXM 二匣，新） | 見 4 節 | 見 4 節 |

另有 41 個在效檔因改號而各動 1～6 處（3 節）。

---

## §2　任務 1 —— 條文入台帳

新節 `## 下放包 GC-02／GC-03 之全域條文（Pei 2026-09-05「都準都裁定」）`，台帳 **+102 / -1**。
本體以腳本自 down 檔抽取（R-G36），逐條附來源行號：

| 條 | 來源 |
|---|---|
| R-G48(b) | `down/20260905_GC-02_review.md:21`（圍籬） |
| R-G45 補充（DBC 網段配對）| `…_review.md:47`（散文段） |
| R-G50(b) | `…_review.md:76`（圍籬） |
| R-G43 落實（(A) 台帳保號、改號表、R-G19 二擇一）| `down/20260905_GC-03.md`（§一） |
| `_intake/SXM/` 二檔裁決 | 同上 |

R-G43 節下附執行層之落實與偏差五點（其中第 5 點為 9-3 節之更正）。

---

## §3　任務 2 —— R-G43 改號（本包最重）

### 3-1　範圍：**Pei 於 2026-09-05 裁定「只改在效檔」**

下放包 §二-4(a) 字面為「凡判為 FO 側者一律機器改號」，其母體含歷史包 435 處，
與 §二-4(b)／§二-5「歷史檔不動」相衝。三種讀法相差 435 處，**已請裁**，結果：

| 讀法 | 改號處 | 裁 |
|---|---:|---|
| 只改在效檔 | **193** | **採用** |
| 在效檔＋其他散檔 | 290 | — |
| 照字面全改（含歷史包） | 725 | — |

### 3-2　母體與三類分子分母（R-G8）

引用處 **2,620／443 檔**（母體：`docs/`、`features/`、`scripts/`、`tests/`、`backend/`、
`sources/`、`config/`，排除 `.git`／快取／**本層歷輪之量測產出表**）：

| 類 | 處 | 檔 |
|---|---:|---:|
| (a) 改號（FO 側 ＋ 台帳 L402），落在效檔 | **193** | 43 |
| (a′) 同判側但落歷史／其他檔，**依裁不改** | 532 | 147 |
| (b) 歧義且落歷史／其他檔，不改 | 1,249 | — |
| (c) 歧義且落在效檔，**列表交分析層** | **130** | 48 |
| 台帳側（保號，本不需動） | 516 | — |
| **合計** | **2,620** | — |

(c) 之清單：`docs/reports/rg43_manual_20260905.tsv`（130 列，欄
`path / line / old_id / basis / cand_FO / cand_LEDGER / context`）。
集中處：`features/bed_lowering/ANOMALIES.md` 13、`features/display/RULINGS.md` 10、
`features/display/ANOMALIES.md` 9、`features/vehicle_setting/RULINGS.md` 6、
`features/ics_management/RULINGS.md` 6、`scripts/rulings_hash.py` 6。

### 3-3　「條文一字不改」之落實

**圍籬內之條文本體一律不動**（含其首行之原號）。FO 內因此跳過 26 處、
台帳新節內跳過 5 處（皆為 R-G36 逐字抽取之條文）。
原編號註一律加在 `#### R-Gnn` 錨點之下、圍籬之外，形如
`> **原編號 R-G12，2026-09-05 改號，見 R-G43**（依 R-G43 §一：條文本體一字不改，故下框之首行仍書原號）。`

### 3-4　改號表與兩處偏離

`docs/fw036/SHA_MIGRATION_rg43.tsv` **20 列**（下放包預估 19）：

- 18 列為 R-G12→R-G51 … R-G29→R-G68。**FO §9.2 於 12–29 間無缺號**，新號連號，與下放包之表逐號相符。
- **第 19 列為 `R-G23′`→`R-G62′`** —— 下放包之改號表未列 ′ 條。執行層判其須隨母條改：
  不隨改則 `R-G23′` 將指向台帳側之 R-G23（綁定須被檢查），**即 R-G43 所欲消除之歧義本身**。
  **此推論為執行層所為，請覆核**（FO 之該錨點下已標明此點）。
- 第 20 列為台帳 `R-G19`→`R-G69`（L402 條）；L358 條保號 R-G19，未動。

`### 9.2` 之區間字樣改為「R-G1～R-G11、R-G51～R-G68（含 R-G62′）、R-G42」，
並附改號註與對照表路徑。原名「R-G1 ～ R-G27」記於註內（該名自 R-G30 起本即與實況不符）。

### 3-5　標側寫法之停用

在效檔內之 `R-Gnn(FO)` 尾綴於改號時一併去除（下放包前言「標側寫法自本包起停用」）。
歷史檔內之標側寫法**不動**（§二-5）。

---

## §4　任務 3 —— MANIFEST 併入

| 步 | 數 |
|---|---|
| 提案表 | 179 列 |
| － `NOT_SOURCE_DOC`（結構檔，GC-02 審閱 §二-2 裁「剔除」）| −20 |
| = 原檔 | **159** |
| ＋ `_intake/SXM/` 二檔登記 | +2 |
| **`sources/MANIFEST.tsv` 現行** | **161 列** |

R-G8 分子分母：**179 唯一 sha 中原檔 159、結構檔 20（剔除）**。

改前實測 `sources/MANIFEST.tsv` 3,160 bytes、mtime 2026-09-01 23:02:26、11 行、
sha8 `8c9c20cc`；改後 53,225 bytes、162 行、sha 見 1 節。
**現行 10 列逐字保留在前** —— `git diff` 之刪除行數為 **0**（僅 `---` 檔頭一行以 `-` 起首）。
新 149 列之 `note` 首欄已去 `proposed`。

SXM 二檔依 §一「投錯匣，登記不清」：**copy** 至 `sources/raw/<doc_id>/`（原檔不刪），
copy 後逐檔重算 sha 與來源相符：

| doc_id | 檔 | sha8 |
|---|---|---|
| `r1lr_atl_h_25pi3_5_cabin_cfts_020_ics_and_dcsd_sr26_20250909_185` | `…ICS and DCSD _SR26_20250909-1852.doc` | `f2004bd7` |
| `r1lr_atl_h_25pi3_5_cabin_cfts_020_ics_and_dcsd_20250910_1124` | `…ICS and DCSD_20250910_1124.reqifz` | `c9bbd3b8` |

`features` 欄皆為 `ics_management`；`note` 記其為投錯匣、copy 來源、原檔待 Pei 清空。

---

## §5　任務 4 —— `cleanup_gc02.sh` 加註

段二每筆之上加 `# was: <old> ; referenced by: <file>`（即路徑遷移對照，不另出檔）。
段首加二行說明其為 GC-02 審閱 §二-3 之落實。`bash -n` **PASS**，**未執行**
（`docs/fw036/lint_reports/` 仍 106 檔）。首二行之 R-G 號已隨改號改為 R-G65／R-G51。

---

## §6　任務 5 —— `backend/` 分頁名

### 6-1　先量（§二-5-1）：五處硬編之分類，**與預期不同**

對 R-G1 母本（B 變體）與 `features/power/inputs`（A 變體）各跑一次現行 code 之開檔路徑：

| 處 | 母本（B，121/145）| power（A，24/145）| 判 |
|---|---|---|---|
| `parser.py:77` 常數 ＋ `:134` 解析 | **OK**（prefix 後備命中）| OK | **非純硬編** |
| `writer.py:10` 常數 ＋ `find_tc_sheet_name` | **OK**（prefix 後備）| OK | **非純硬編** |
| `tools/inspect.py:20` `_TC_SHEET_NAMES` 精確集合 | **FAIL → `None`** | OK | **純硬編（真紅）** |
| `review_engine.py:1338` `"sheet": "…&Result"` 字面 | **回報一個它沒讀的名** | 恰巧相符 | **純硬編（真紅）** |
| `api_server.py:2338` `ws.title = TC_SHEET_NAME` | 建新檔用，非讀路徑 | 同 | 寫側預設 |

即：**五處中二處為真硬編**（`inspect` 對 121/145 回報 `tc_sheet_name=None`；
`review_engine` 之 `batch_meta.sheet` 對 121/145 是假的），其餘三處已有 prefix 後備或屬建檔預設。
**`backend/` 全不讀 `feature.yaml`** —— R-G48(b) 之「以 `workbook.sheet` 為準」在 backend 現況下
無取得管道，故實作為「呼叫端可傳 `preferred`，未傳則走候選集」。

### 6-2　改（§二-5-2）

常數與解析**集中於 `backend/parser.py` 一處**，其餘 import：

- `TC_SHEET_NAME_CANDIDATES`（兩變體＋一空格變體）、`TC_SHEET_PREFIX`、
  `resolve_tc_sheet(wb, preferred=None)`：優先序 `preferred` → 候選集 → prefix；
  **皆不中則拋 `KeyError` 並列出實際 `sheetnames`**，不靜默回退。
- `writer.py`：刪除自有常數，改 `from parser import TC_SHEET_NAME, resolve_tc_sheet`；
  `find_tc_sheet_name(wb, preferred=None)` 直接轉呼叫 —— 讀與寫走同一支。
- `tools/inspect.py`：精確集合刪除，改用 `resolve_tc_sheet`。
- `review_engine.py`：`batch_meta.sheet` 改為 `_resolve_report_sheet(workbook_path)` 之實測值，解不出回 `None`（不猜）。

改後複測：**四條路徑對兩變體皆 OK**；`review_engine` 對母本回
`'Test Case Specification 測試用例規範'`、對 power 回 `'Test Case Specification&Result'`。

### 6-3　夾具與守衛（§二-5-3）

新增 `tests/test_tc_sheet_variants.py`（9 個測試，全綠）：
兩變體各一之 resolver／writer／parser 三向、`preferred` 優先於候選順序、
解不出時錯誤訊息須含實際 sheetnames、以及**守衛測試** ——
模板 `workbook.sheet` ∈ R-G1 母本之 `sheetnames`（實開 `forms/…20260817_ext.xlsx` 讀）。

既有 48 處 `&Result` 夾具**未改**：它們測的是**真實的少數變體**，改掉等於讓
power／audio_mgmt 一支失去覆蓋（GC-02 6-2 節之結論，經 GC-02 審閱 §一撤回原裁確認）。

### 6-4　對照向（§二-5-4，R-G7＋R-G50(b)）

| 向 | pytest | 移出檔數 |
|---|---|---:|
| 改後 | `10 failed, 1294 passed, 15 skipped` | — |
| **對照向（改前 = HEAD）** | `10 failed, 1285 passed, 15 skipped` | **47**（46 已追蹤 ＋ 1 新測試檔）|

**逐項同名**（`test_gates_tsv` 1、`test_intake_scaffold` 6、
`test_lint036::test_check_order_extends_only_with_profile` 1、`test_single_write_path` 2）。
紅 **+0**；綠 **+9**（新測試檔）。

`ruff` 逐檔（對照向 → 交付向）：`parser.py` 1→1、`writer.py` 4→4、
`review_engine.py` 9→9、`tools/inspect.py` 2→2、`test_tc_sheet_variants.py` **All checks passed**。
**增紅 0**（首版曾 +5，為未排序 import／未用 import／未用 `noqa`／`except Exception`，已逐項改掉）。

---

## §7　任務 6、7

**6　DBC 配對註**　`features/vehicle_setting/DECISIONS.md` 版本綁定節加 6 行：
以網段 token 配對 —— `PDT27_E2A_R4_BHCAN.dbc` ↔ `forms/PDT27_E2A_R1_BHCAN2.dbc`（`46cb73f3`）、
`PDT27_E2A_R5_FDCAN8.dbc` ↔ `forms/PDT27_E2A_R1_FDCAN8.dbc`（`2a86c4bf`）；四份並列保留。

**7　13 檔次之差異素材**　`docs/reports/binding_diff_<feature>_<n>.tsv`，逐檔含
`meta`／逐分頁列數／key 級增刪改／`delivered` 命中之查詢式與分子分母。

| 檔 | feature | 家族 | 只在 inputs | 只在 forms | 兩邊皆有而值異 |
|---|---|---|---:|---:|---:|
| `binding_diff_comfort_1.tsv` | comfort | HMI Settings List | 0 | 0 | 0 |
| `binding_diff_comfort_2.tsv` | comfort | Market Config | 0 | 0 | 0 |
| `binding_diff_comfort_3.tsv` | comfort | Pop Up List | 0 | 1 | 1 |
| `binding_diff_time_management_4.tsv` | time_management | HMI Settings List | **95** | **105** | **32** |
| `binding_diff_time_management_5.tsv` | time_management | HMI Settings List | 0 | 0 | 0 |
| `binding_diff_time_management_6.tsv` | time_management | LID | 0 | 0 | 4 |
| `binding_diff_time_management_7.tsv` | time_management | Market Config | 0 | 0 | 0 |
| `binding_diff_user_profiles_8.tsv` | user_profiles | Pop Up List | 0 | 1 | 1 |
| `binding_diff_vehicle_category_9.tsv` | vehicle_category | HMI Settings List | 0 | 0 | 0 |
| `binding_diff_vehicle_setting_10.tsv` | vehicle_setting | DBC | `SG_` 只在 inputs 604 | 只在 forms 34 | `SG_` 914 vs 344 |
| `binding_diff_vehicle_setting_11.tsv` | vehicle_setting | DBC | `SG_` 只在 inputs 122 | 只在 forms 1 | `SG_` 2037 vs 1916 |
| `binding_diff_vehicle_setting_12.tsv` | vehicle_setting | HMI Settings List | 0 | 0 | 0 |
| `binding_diff_vehicle_setting_13.tsv` | vehicle_setting | LID | 0 | 0 | 4 |

**key 規格**（逐檔寫入 tsv）：Settings List = `Settings` 分頁第 3 列表頭、key 為 A|B|C 串接；
Pop Up List = `Main` 第 2 列表頭、key 為 `ID Number`；Market Config = `Market Config - R1`
第 6 列表頭、key 為 `Destination Country`；LID = `CAN Mapping` 第 3 列表頭、key 為 `Logical Identifier`；
DBC = 逐行解析 `BO_`／`SG_`／`VAL_`。

**六個檔次之 key 內容完全相同**（comfort 之三項有二、time_management 5、vehicle_category 9、
vehicle_setting 12）—— sha 相異而 key 分頁逐列相同，差異在他處（分頁列數亦同）。
此為分析層判「是否影響已交付 TC」之直接依據。

**`delivered/` 命中：五個 feature 之分母皆為 0。**
查詢式 `glob features/<feat>/delivered/*.xlsx` 逐 feature 命中 0；
全域 `features/*/delivered/*.xlsx` 命中 **5 本**，落於
`ics_management`／`power`／`sw_update`／`vsm_v42` —— **本案五個 feature 一本都沒有**。
故命中率不可計，**不得以「0 命中」表述為「無影響」**（R-G50）；逐檔已於 `note` 欄寫明。

---

## §8　自檢

| # | 項 | 結果 |
|---|---|---|
| 1 | 無刪除、無搬移 | **PASS** —— `git status --porcelain` 無 `D`／`R` 列；SXM 為 copy，`_intake/SXM/` 二檔仍在 |
| 2 | `rulings_hash.py` 未跑（§二-2-6）| **違規一次，已還原** —— 見 9-2 節 |
| 3 | `RULINGS.sha.tsv` 交付狀態與 HEAD 逐位元相同 | **PASS**（`git status` 該檔為空）|
| 4 | 圍籬內條文本體未動 | **PASS** —— FO 跳過 26 處、台帳新節跳過 5 處 |
| 5 | 歷史檔未追改 | **PASS** —— (a′) 532 ＋ (b) 1,249 處未動 |
| 6 | `down/` 未動 | **PASS** —— 本輪只在 `up/` 新增本檔 |
| 7 | `lint_reports/`／`_intake/`／`forms/` 未動 | **PASS**（106／38／17 前後相同）|
| 8 | pytest 增紅 | **+0**（6-4 節，逐項同名）|
| 9 | `ruff` 增紅 | **+0**（6-4 節，逐檔同數）|
| 10 | `gate_all` | `canon_refs` **−53**；`rulings_hash` **由綠轉紅**，歸因見 9-1 節 |
| 11 | 全稱斷言附查詢式與命中數（R-G50）| **PASS** —— 3-2／4／6-1／7 節皆附 |
| 12 | 對照向回報被移出檔數（R-G50(b)）| **PASS** —— pytest 47、gate 47＋16＋2 匣 |

---

## §9　自報

### 9-1　`gate_all`：`canon_refs` **降** 53，`rulings_hash` 由綠轉紅

| 向 | `canon_refs` | `rulings_hash` |
|---|---:|---|
| 對照向（本層 GC-03 產物全移出／還原）| **750** | **PASS** |
| 交付狀態（不含本上繳包）| **697** | **FAIL** |
| 交付狀態（含本上繳包）| **710** | FAIL |

本上繳包自身 **+13**，全為指名 R-G43～R-G50、R-G51～R-G69 等尚無指紋之號所致
（同 GC-01 上繳 11-5 節之自指效應：本節每寫一次條號就多一處）。

逐型（對照向 → 交付狀態）：`section` 63／748 → 63／748（**+0／+0**）；
`item` 0／40 → 0／40（+0）；`ruling` unresolved **813 → 755（−58）**。
waiver 外：unresolved 610 → 557、ambiguous 140 → 140。

**降紅之因**：改號使 FO §9.2 新增 R-G51～R-G68＋R-G62′ 十九個可解析錨點，
而在效檔內原本指向舊號之引用同步改到新號 —— 引用與錨點對齊，`ruling` unresolved 因而下降。
**這是本包唯一一次讓 `canon_refs` 往下走的變更。**

**`rulings_hash` 轉紅之因（以 `--out` 導向工作區暫存檔量測，未寫 repo 內之表）**：
現行 698 列 vs 重生 700 列 —— 新增 21（`R-G51`～`R-G68`、`R-G62′`、
＋`R-VL29`／`R-VT27` 二列屬併行 vsm 線）、消失 19（`R-G12`～`R-G29`、`R-G23′`）、本體變 2。
即**全數為本包之改號所致**，屬 §二-2-6 所預留之 Pei 作業，非缺陷。

### 9-2　本層違規一次：跑了 `rulings_hash.py`（已還原，具名）

診斷 `rulings_hash` 為何轉紅時，我直接執行 `python3 scripts/rulings_hash.py`。
**該工具無參數時會寫入 `docs/fw036/RULINGS.sha.tsv`**，而 §二-2-6 明文「不跑（Pei）」。
實際後果：該檔被覆寫，`git diff --numstat` 為 **37 增 35 刪**。
發現後立即 `git checkout -- docs/fw036/RULINGS.sha.tsv` 還原，並以
`git status --porcelain` 複核為空、sha8 回到 `e079e07f`。其後之量測改用
`--out <工作區暫存檔>`，不再碰 repo 內之表。

**形態**：把「讀一下狀態」與「跑這支工具」當成同一件事，
而該工具之預設行為是**寫**，不是讀（`--check` 才是唯讀）。
與 GC-01 9-3 節、GC-02 9-2 節同屬「未先確認動作之副作用即執行」。
可測之防法（供分析層裁，本層不自立條）：包內列為「不跑」之工具，
執行層若需其輸出，一律只准 `--check` 或 `--out <工作區路徑>`；
上繳須附該工具所寫之 repo 內路徑清單（空亦寫）。

### 9-3　**R-G43 之理由與實測不符**（更正其理由，不動其裁定）

R-G43 §一書：「`RULINGS_LEDGER.md` 為 `rulings_hash.py` 之指紋源，
且其條文附日期、來源與逐字本體；FO §9.2 之條文為 runtime 摘要。改摘要之號，不改指紋源之號。」

實測：

1. `scripts/rulings_hash.py` 之掃描面為 `docs/fw036/FEATURE_ONBOARDING.md`
   ＋ `features/*/RULINGS.md`（`default_targets`，該檔 255–258 行）。**台帳不在其中。**
2. `docs/fw036/RULINGS.sha.tsv` **698 列中，來源欄為 `docs/fw036/RULINGS_LEDGER.md` 者 0 列**。
3. `R-G12`～`R-G29` 之指紋列**全部來自 FO**（例：`R-G19` 之列指
   `docs/fw036/FEATURE_ONBOARDING.md:926`，`fenced`）。

即**保號側（台帳）才是無指紋的一側，改號側（FO）才是指紋所在**，與理由所述相反。

**可測後果一件**：`R-G19` 在指紋表重生後將**不再有任何列** ——
FO 側之 R-G19 已改號為 R-G58，而 Pei 裁保號之台帳 L358 條不被掃描。
「保號」在此保住的是一個沒有指紋的號。

**本層不改裁定**（改號已依裁執行完畢，且 (A)/(B) 之取捨尚有其他理由），
只在台帳 R-G43 節下加註更正其理由（第 5 點），並將此點列為請裁事項。

### 9-4　本輪自造之缺陷一件（已改正，具名）

首次跑改號母體掃描時，**把 GC-01 自己的產出表 `docs/reports/rg_refs_20260905.tsv`
納入了母體**，該檔每列含二個 `R-G` token，於是引用處由 2,620 膨脹為 8,574（+5,936）。
若照此改號，還會把那張已交付之量測表一併改寫。
加 `SELF_OUT` 排除集後重跑得 2,620。
**這與 GC-02 9-3 節是同一個缺陷，本層第二次犯**（該次是 `lint_reports_refs`）。
建議把「本層歷輪產出表不入任何母體」寫成常設排除清單，而非每包各自記得。

### 9-5　未做且具名

| 項 | 為何未做 |
|---|---|
| `rulings_hash.py` 重生（含 R-G30–R-G41 補列）| §二-2-6／§三-1，屬 Pei |
| 跑 `cleanup_gc02.sh` | §三-2，屬 Pei；R-G5 git 操作屬 Pei |
| `_intake/` 五匣清空（含 SXM）| §三-3，屬 Pei |
| (c) 130 處歧義引用之改號 | §二-4(c) 明文交分析層判 |
| 歷史檔之 532＋1,249 處 | Pei 2026-09-05 裁「只改在效檔」 |
| 13 列「是否影響已交付 TC」之判定 | GC-02 審閱 §五：分析層自做，本包只備料 |
| `backend/api_server.py:2338` 之建檔預設名 | 屬寫側預設而非讀失敗；改其預設會改變新建空白模板之分頁名，超出「先量後改」所證之範圍 |

### 9-6　請裁三項

1. **R-G23′→R-G62′** 之推論（3-4 節）—— 改號表未列，執行層自為，請覆核。
2. **R-G43 之理由更正**（9-3 節）—— 是否併同修訂條文，或只留台帳之更正註。
3. **`R-G19` 將無指紋列**（9-3 節）—— 保號側不被掃描，是否要把台帳納入
   `rulings_hash.py` 之掃描面，或接受台帳條文本就無指紋。
