# 上繳包 GC-01 —— 全域清理與來源集中之量測

日期：2026-09-05　層級：Tier 1 → Tier 2
對應下放包：`down/20260905_GC-01.md`（協定：`docs/fw036/handoff/PROTOCOL.md`）

本包依下放包 §二做六件。**無任何刪除、搬移、覆寫** ——
`_intake/`、`lint_reports/`、`forms/`、`features/*/inputs/`、`sources/`、
`down/`、`up/` 之內容一位元未動（assert 見 7-2 節）。
`R-G12`～`R-G29` 之引用一律標側（`R-Gnn(FO)`／`R-Gnn(LEDGER)`），依下放包 §一 R-G43。

---

## §1　產物與 SHA256

| 檔 | bytes | 列 | SHA256 |
|---|---:|---:|---|
| `docs/reports/source_identity_20260905.tsv` | 52,446 | 220＋表頭 | `dda6250de62aebc3c90a1068c018a551a6357872672651eb1695a13232b4df4c` |
| `docs/reports/rg_collision_20260905.tsv` | 5,294 | 42＋表頭 | `4039064fd5a74ed4a93bf2fd3c89b907d36dbba61d0758d861509c858cb33e0d` |
| `docs/reports/rg_refs_20260905.tsv` | 395,376 | 2,527＋表頭 | `b3be4734ad5867a36402f5d2ce4b58499d1784205dcffb0068d07cefbb4f3a62` |
| `docs/reports/lint_reports_refs_20260905.tsv` | 7,932 | 106＋表頭 | `191c678910b04e222232f521cbcbd9a3669456a0dd7612e0b5a1be39e9ef1350` |
| `docs/fw036/templates/feature.yaml` | 2,427 | — | `83818023048f185c8097d05d5a243725df5fb833dfb15556acab757a8ccbf112` |
| `docs/fw036/INDEX.md` | 5,142 | — | `529ae3d94fb9cb17ac49d07cdd47a3843ea91865ce12d697a620c5605fc7cc15` |

`git diff --numstat` 本輪：`33 1 docs/fw036/INDEX.md`、`13 4 docs/fw036/templates/feature.yaml`。
另有 `25 0` 之 `features/vsm_v42/RULINGS.md`／`features/vsm_v43/RULINGS.md` ——
**屬併行作業線，非本包所改**，本包一字未觸。

`rg_refs_20260905.tsv` 為下放包 §二-2 所命之「引用處逐處標側」，
下放包未給檔名，本層另立此名而不塞進 `rg_collision`（R-G16(LEDGER) 量測與聚合分離）。

---

## §2　來源身分表（§二-1，R-G44）

母體：`forms/*`（17）、`features/*/inputs/**`（含 `derived/`、`cfts057_objects/`）、
`_intake/**`、`sources/raw/**`。`.DS_Store` 排除。

| 統計 | 分子／分母（R-G8） |
|---|---|
| 檔數 | **220** |
| 唯一 sha256 | **179** |
| 同 sha 之組（`dup_group`） | **28** 組 |
| 同名異 sha 之組（`NAME_COLLISION`） | **6**（實質 **3**；另 3 為 `INTAKE.md`／`intake.json`／`INPUTS.sha256` 等結構命名，標 `META`）|
| 無 `doc_id` 之檔 | **206 / 220**（93.6%）|

`doc_id` 之解析源為 `sources/MANIFEST.tsv`（現行 **10 列**）。
命中之 14 處全落 `sources/raw/`、`_intake/Popup/` 與
`features/vehicle_setting/inputs/` 之 SYSAD 一檔；`forms/` **17 檔無一有 `doc_id`**。
**R-G44 若追認，缺口即為 206 列。**

### 2-1　實質同名異體（逐檔實測）

| 檔名 | 位置 | bytes | sha8 |
|---|---|---:|---|
| HMI Settings List R1 SR25 Post R1L-R (Feb 13 2026).xlsx | `forms/` | 296,368 | `8d04e51a` |
| 〃 | `features/vehicle_setting/inputs/` | 295,635 | `0a371216` |
| 〃 | `features/time_management/inputs/` | 295,635 | `41daac00` |
| 〃 | `features/vehicle_category/inputs/` | 295,635 | `41daac00` |
| 〃 | `features/comfort/inputs/` | 295,449 | `c706071f` |
| Logical Identifiers and CAN Mapping v1_76.xlsx | `features/vehicle_setting/inputs/` | 622,949 | `ffceac36` |
| 〃 | `features/time_management/inputs/` | 622,158 | `9a751a72` |
| SR24 R1 Market Configuration Table v1.6.xlsx | `forms/` | 274,486 | `7e865d55` |
| 〃 | `features/comfort/inputs/` | 279,779 | `ae4cf0b9` |
| 〃 | `features/time_management/inputs/` | 279,779 | `ae4cf0b9` |

**對下放包之更正三處**（下放包 §一 R-G44「實測異體」）：

1. HMI Settings List 為 **5 份 4 體**，非 3 份 3 體 ——
   下放包漏列 `time_management`、`vehicle_category` 二份。
2. **`vehicle_setting` 與 `time_management`／`vehicle_category` 之三份大小同為 295,635，
   sha 卻分二體。**下放包 R-G44 條文寫「大小相異即足證內容相異」——
   該命題為真且本包三組皆用得上，惟其**逆不成立**，
   而本例正是逆向反證：**同大小不足證同內容**。
   建議 R-G44 落條時把判準明寫為 sha256，不留大小作為替代判準（R-G11(LEDGER) 盲區聲明）。
3. `Logical Identifiers and CAN Mapping v1_76.xlsx` **亦為同名異體**（二體），
   下放包 §一 R-G45 只記 `vehicle_setting` 持 `v1_76`，未記 `time_management` 另持一體。

`forms/` 所存之 LID 為 `v1_78`，與上二者皆非同版。
**R-G45 之「版本綁定」待記清單已於審閱後以機器重算，見 11 節** ——
本節首版所寫之「二個 feature」係只看同名異體表所得，母體錯了（同 9-3 節之形態）。

### 2-2　`_intake/` ↔ 落點之 sha 對照（R-G46 前置）

下放包 §二未列此項，惟 R-G46 明文「清空前執行層須出對照」，
且本表已為表一之導出，故一併出（**清空仍為 Pei 動作，本包不執行**）：

| `_intake/<Feature>/` | 原檔 | 已見落點 | 未見落點 |
|---|---:|---:|---|
| Display | 4 | 4 | — |
| Popup | 3 | 3 | —（全落 `sources/raw/`）|
| SW_Update | **8** | 8 | — |
| Vehicle_Category | 3 | 3 | — |
| SXM | 2 | **0** | `…CFTS_020 ICS and DCSD _SR26_20250909-1852.doc`、`…_20250910_1124.reqifz` |
| AMFM／Bed_Lowering／Comfort／Privacy／Time_Management／Vehicle_Setup_VF665 | 0 | — | 已只剩 `INTAKE.md`／`intake.json` |

**對下放包之更正一處**：SW_Update 為 **8** 原檔，非 9
（該目錄 10 個 entry，其二為 `INTAKE.md`／`intake.json`）。

SXM 二檔在 repo 內無任何同 sha 之落點 —— 與下放包「疑誤投」之判一致，
惟本層只出量測，**誤投與否為 Pei 之裁（下放包 §三-3）**。

---

## §3　R-G 雙軌對照（§二-2）

`rg_collision_20260905.tsv` 逐號列兩側之標題、所在檔與行號、是否同題。
兩側之錨點取法：LEDGER 取條文本體首行 `R-Gnn（標題 —— 全域）`；
FO 取 §9.2 之 `#### R-Gn — 標題`。**不改任何條文。**

| 判 | 號 | 數 |
|---|---|---:|
| `FO_ONLY` | R-G1 ～ R-G11 | 11 |
| `DIFFERENT`（同號異題） | **R-G12 ～ R-G29** | **18** |
| `LEDGER_ONLY` | R-G30 ～ R-G41 | 12 |
| `SAME_TITLE` | R-G42 | 1 |

**對下放包之更正二處**（下放包 §〇 末段）：

1. **R-G40／R-G41 非兩側同題，而是台帳單邊。** FO §9.2 之 `####` 錨點自
   R-G29 直跳 R-G42，中間 R-G30–R-G41 十二條 FO 側全無。
   兩側同號者僅 **R-G42** 一條。
2. **R-G42 兩側非「異文」而是「摘要＋讓與」** —— FO 側之框末明寫
   「全文以 docs/fw036/RULINGS_LEDGER.md 之 R-G42 節為準」，
   已自行讓出權威。故 R-G43 之保號裁定**不必涵蓋 R-G42**：
   它現在就唯一可解析，改動它反而製造新的遷移面。

「R-G43 起兩側皆空」經實測**成立**（兩側 `R-G4[3-9]` 命中皆 0）。

### 3-1　新增之發現：台帳**內部**亦有同號異文

`R-G19` 在 `RULINGS_LEDGER.md` 有**兩條**：

- L358　`R-G19（Pei, 2026-08-25）—— 寫回作業規定全專案化`
- L402　`R-G19（理由與數字須分別驗證 —— 全域）`

即 R-G19 之歧義為**三向**（台帳兩條＋FO 一條「prompt 指紋」），
非下放包所設想之雙軌二向。`R-G43` 之單軌化若只處理跨檔一維，
`R-Gnn(LEDGER)` 之寫法在 R-G19 上**仍然歧義**。此點請分析層併入 R-G43 之供裁案。
（`rg_collision_20260905.tsv` 之 `intra_dup` 欄記此，全表僅 R-G19 一列非空。）

---

## §4　R-G12～R-G29 之引用處逐處標側（§二-2 後半）

母體：`docs/`、`features/*/`、`scripts/` 全檔（排除 `.git`、`__pycache__`、
`.pytest_cache`、`node_modules`、虛擬環境），非只 `.md`。

**引用處 2,527，涉及 423 檔。**

| 判側之依據 | 處數 | 說明 |
|---|---:|---|
| `EXPLICIT` | 7 | 已寫 `R-Gnn(FO)`／`R-Gnn(LEDGER)`（VS-SL 協定後之寫法）|
| `SELF_FILE` | 168 | 引用處即在該側之條文檔內 |
| `CONTEXT_KW` | 1,020 | 上下文 ±2 行命中該側之判別詞、且另一側 0 命中 |
| `BOTH_KW(x/y)` | 44 | 兩側判別詞皆命中 → **歧義** |
| `NO_KW` | 1,288 | 兩側判別詞皆未命中 → **歧義** |

| 統計（R-G8） | 值 |
|---|---|
| **可判定** | **1,195 / 2,527 = 47.3%** |
| 判為 LEDGER | 514 |
| 判為 FO | 681 |
| **歧義** | **1,332 / 2,527 = 52.7%** |

逐號可判定率（低於三成者）：`R-G23` 87/250（34.8%）、`R-G25` 45/148（30.4%）、
`R-G12` 54/186（29.0%）、`R-G28` 40/113（35.4%）。
最高者 `R-G13` 353/578（61.1%），亦是引用處最多者。

**下放包 §〇 所稱「任何引用皆歧義」在措辭上過強**：
就 `canon_refs`（R-G18(FO)）之機器解析而言確實全歧義（該工具只看編號），
但就人讀之上下文而言 **47.3% 可判定**。這兩個數不衝突，
差別在判準：`canon_refs` 之判準是「編號是否唯一」，本表之判準是「上下文是否足以還原」。
改號之遷移成本因此**不是 2,527 處**，而是「1,332 處須人工判」＋「1,195 處可機器換」。

### 4-1　本判法之盲區（R-G11(LEDGER)）

1. **判別詞為啟發式，非證明。** `CONTEXT_KW` 之 1,020 處中，
   人工抽驗 20 處（每 51 列取 1），**20/20 相符**，其中一處
   （`features/vehicle_category/RULINGS.md:300` 之 `R-G15`）初判存疑，
   進一步讀該段之「其變動會使既有產出失效」語，確認為台帳側（版本綁定）。
   抽驗率 20/1,020 = 2.0%，**不足以聲稱錯誤率上界**；只能說未發現反例。
2. **±2 行之窗口為武斷值。** 窗口拉大會把鄰條之詞捲入而增 `BOTH_KW`，
   拉小會增 `NO_KW`。本包未做窗口敏感度掃描。
3. `NO_KW` 之 1,288 處**不等於「不可判定」**，只等於「本判法判不出」——
   其中相當比例為表格列、目錄列、sha 台帳列，本就無上下文可據。

---

## §5　lint 報告之引用懸空檢查（§二-3，R-G47）

`docs/fw036/lint_reports/` 實測 **106 檔**（**下放包記 110，更正為 106**；無子目錄）。
日期分布（取檔名內最後一個日期 token）：0821 **16**、0824 6、0827 1、
0828 6、0829 46、0830 31。

| 判 | 數 | 依據 |
|---|---:|---|
| `N`（不得移除）| **25** | 凍結基線 19＋被引用 6 |
| `Y`（無任何指名，可移除）| **81** | `NO_REF` |

凍結基線 19 = 0821 之 **8 對 `.md`/`.json`（16 檔）** ＋ `pm_29*` 三檔，
與下放包 R-G47 所述之母體**逐檔相符**。

被引用之 6 檔（非凍結）：

| 檔 | 被指名於 |
|---|---|
| `pm_25__power_20260824.md` | `docs/fw036/upstream/25_split.md`、**`tests/test_lint036.py:495`** |
| `pm_26__power_20260824.md` | `docs/fw036/upstream/26_split_fix.md` |
| `pm_27__power_20260824.md` | `docs/fw036/upstream/27_paren_fix.md` |
| `pm_28__power_20260824.md` | `docs/fw036/upstream/28_paren_convergence.md` |
| `SWUpdate__sw_update_7f019b37_20260830.md` | `docs/fw036/upstream/72_delivery.md` |
| `sw_update_20260817_ext__sw_update_a3633876_20260828.md` | `features/sw_update/docs/upstream/25_batch1_review.md` |

**`ANOMALIES`／`waiver`／`DECISIONS`／`RULINGS` 指名者：0 檔。**
R-G47 所設之「留」清單在本次量測下為空集合 —— 該保護條款目前不保護任何檔。

`tests/test_lint036.py` 之 `EXISTING_REPORTS` **字面釘入** `pm_25__power_20260824.md`，
惟該測試（`test_existing_report_files_are_never_renamed`）以 `if report.exists():`
包住斷言，故移除該檔**不會使測試轉紅** —— 會使該 param 靜默空跑。
本層判 `N`（有指名即留），並具名此點供分析層裁：靜默空跑亦是一種懸空。

sw_update 相關者 **63 檔**（下放包記「約 60」，相符）。
`features/sw_update/reports/` **不存在**、`features/sw_update/output/` **存在** ——
與下放包 §一 R-G47 之實測一致。建立 `reports/` 與搬移屬清理動作，本包不執行。

---

## §6　模板回修（§二-4，R-G48）

母本：`forms/FM-WI-FSM-036-A01 STLA 測試用例規範與結果_SWQT STLA Test Case Specification & Result_SWQT_20260817_ext.xlsx`

工作表清單實測 9 個：`Cover_old`／`ChangeHistory_old`／`Cover 封面`／
`ChangeHistory 修訂履歷`／`Product Document 記錄封面頁`／
**`Test Case Specification 測試用例規範`**／`Reference`／`QS Suggestion`／`下拉選單`。
`Test Case Specification&Result` **不在其中**。目標表維度 `A1:AH1411`。

第 9 列實測（B–AH 全欄逐格讀，非抽樣）：

| key | 模板原值 | 母本實測 | 判 | 母本該欄之表頭 |
|---|---|---|---|---|
| `sheet` | `Test Case Specification&Result` | `Test Case Specification 測試用例規範` | **誤** | —（該名不存在）|
| `header_row` | 9 | 9 | 對 | — |
| `req_id` | D | D | 對 | `Requirement or Design ID / 需求/設計 ID` |
| `test_group` … `priority` | G–P | G–P | 對（逐欄相符）| — |
| `design_method` | **Q** | **R** | **誤** | Q 實為 `Estimated Test Time (mins)` |
| `functional_safety` | **R** | **S** | **誤** | R 實為 `Test Case Design Methods` |
| `author` | **Z** | **AA** | **誤** | Z 實為 `Fastack (376) Atl-Mi` |
| `remarks` | AH | AH | 對 | `Remarks / 備註` |

**對下放包之更正一處**：模板誤值為 **四處**，非三處 ——
`functional_safety: "R"` 亦誤（實為 `S`）。
下放包所援引之 `features/vsm_v42/feature.yaml` 註解同樣只列三處，
惟該檔之**值**已寫 `functional_safety: "S"`（L93）——
即 vsm_v42 當時已改對而未計入註解之數。此為 R-G20(LEDGER)
（報告中之摘要數字須為機器輸出）之一例：註解之「三處」是人寫的，值是量出來的。

已改 `docs/fw036/templates/feature.yaml`：四值回修，檔首依 R-G48 加註
「本表之 `workbook.*` 為 R-G1 母本第 9 列之實測值……**接手 feature 仍須對其副本重測**」，
`workbook:` 段內另加註四處原誤值與其實際欄義。`+13 / -4`。

**未做且具名**：`tests/test_parser.py`／`test_writer.py`／`test_tool_parse.py`
以 `wb.create_sheet("Test Case Specification&Result")` 自建 fixture，
即那個不存在的表名已擴散進測試夾具。夾具自洽故全綠，
但它們**不再是母本之鏡像**。改夾具不在本包 §二 之授權內，登此待令。

---

## §7　INDEX 與 VS-SL-01（§二-5，R-G49）

### 7-1　`docs/fw036/INDEX.md`

補列 **59–73** 十五列（逐列取自 `handoff/`／`upstream/` 之實測檔名）：
59–68、70–72 兩側成對；**69（`69_name_audit.md`）與 73（`73_delivery_spec.md`）
下放有、上繳無**，該二列上繳欄記 `—`。

新增 §3「自 `VS-SL-02` 起改走 `down/`／`up/`」，載明 `NN_*.md` 序止於 73、
新包依 `PROTOCOL.md` 落 `down/`／`up/`、既有 `NN_*.md` 保留不搬。

計數行重量並附母數（R-G8）：**下放 77｜上繳 42**
（母數為 `handoff/`／`upstream/` 內以數字起首之 `.md`，不含 `PROTOCOL.md`、`down/`、`up/`）。
舊值「下放 62｜上繳 28（2026-08-30）」保留於同行，不刪。
`down/` 22 檔（含 11 份 `_review.md`）、`up/` 10 檔。

### 7-2　VS-SL-01 補檔 —— **可尋得之來源清單**（不自造內容）

本層**未寫** `down/20260901_VS-SL-01.md` 或 `up/20260901_VS-SL-01.md`。
R-G49 之補檔須標「事後補檔，原交換於聊天」，而其內容之來源如下，
**是否據以補檔為分析層之事**（本層不得代擬他層之包）：

| # | 來源 | 存否 | sha8 | 可還原之部分 |
|---|---|---|---|---|
| 1 | `docs/fw036/handoff/down/20260901_VS-SL-01_review.md`（76 行）| 在 | — | 審閱側全文；其 §1 表列**下放包所載四個數與實測之對照**，即下放包之部分引數 |
| 2 | `docs/fw036/handoff/down/20260902_VS-SL-02.md` L4 | 在 | — | 明文「前置：上繳包 VS-SL-01（`features/vehicle_setting/reports/vf230_settings_dryrun.md` 等三本）」，即上繳包之產物清單 |
| 3 | `features/vehicle_setting/RULINGS.md` L7357–7442 | 在 | — | **`R-VS84`–`R-VS88` 五條逐字轉錄自下放包 VS-SL-01 §0／§1**，L7365 明載「不摘要（FO §8.1）」→ 下放包 §0／§1 之條文本體可據以還原 |
| 4 | `docs/fw036/RULINGS.sha.tsv` L665–669 | 在 | — | 該五條之 sha256 與標題，含「（VS-SL-01 §0）」「（VS-SL-01 §1）」之節號歸屬 |
| 5 | `features/vehicle_setting/ANOMALIES.md` L223–232（`A-VS166`／`A-VS168`）| 在 | — | 下放包所載三個數（106／18／7）與 §4 之參考輸出格式要求 |
| 6 | `features/vehicle_setting/reports/vf230_settings_dryrun.md`（126 行）| 在 | `a2535e5f` | 上繳包產物 |
| 7 | `features/vehicle_setting/reports/vf230_settings_dryrun.tsv`（458 行）| 在 | `065a0bb7` | 上繳包產物 |
| 8 | `features/bed_lowering/reports/bl_settings_dryrun.md`（56 行）| 在 | `bf8656d9` | 上繳包產物 |
| 9 | `features/vehicle_category/reports/vc_settings_dryrun.md`（36 行）| 在 | `aa9aca22` | 上繳包產物 |
| 10 | `features/vehicle_setting/data/settings_alias.tsv`（108 行）| 在 | `781ef0ea` | 上繳包產物（別名 107）|
| 11 | `features/vehicle_setting/scripts/vs_sl01_dryrun.py`（354 行）| 在 | `0e1b28ad` | 產生上列報告之腳本 |
| 12 | `docs/runtime/profiles/FW036_R1L_VehicleSetting_Profile.md` L155 | 在 | — | `[ADD] Settings 查找配方（VS-SL-01～06）` |

**缺口具名**：下放包 VS-SL-01 之 §2、§3 之本體無任何 repo 內留存
（審閱檔只反引其 §0／§1／§4）。補檔若做，該二節須留白並標「原交換於聊天，無留存」，
不得重建（IN §8.4.1）。

**另一路徑實在性缺陷（R-G24(FO)）**：`features/vehicle_setting/ANOMALIES.md:230`
之 `A-VS166` 寫「實測（`scripts/vs_sl01_dryrun.py`）」，該路徑**不存在**；
實際為 `features/vehicle_setting/scripts/vs_sl01_dryrun.py`。改 ANOMALIES 屬條文動作，本包不執行。

---

## §8　自檢

| # | 項 | 結果 |
|---|---|---|
| 1 | 無刪除／搬移／覆寫 | **PASS** —— `git status --porcelain` 無 `D`／`R` 列 |
| 2 | `down/`／`up/` 未動（PROTOCOL 目錄唯讀）| **PASS** —— 本輪只**新增** `up/20260905_GC-01.md` 一檔 |
| 3 | `_intake/`／`lint_reports/`／`forms/`／`sources/` 未動 | **PASS** —— 四處皆不在 `git status` 內 |
| 4 | 量測暫存不落 repo | **PASS** —— 四支腳本落 session scratchpad，未入版控 |
| 5 | 模板改動不增測試紅 | **PASS** —— 見 9-1 節 |
| 6 | `gate_all.py` 增紅 | **+46（canon_refs），全為 unresolved-ruling，逐處歸因見 9-2 節** |
| 7 | 表格之數字皆為腳本輸出（R-G20(LEDGER)）| **PASS** —— 2／3／4／5 節 之每個數皆自四張 tsv 或其產生腳本之 stdout 抄入 |
| 8 | 母體檔數前後不變 | **PASS** —— `lint_reports/` 106、`_intake/` 38（檔）、`forms/` 17；`down/` 22（本包未加）、`up/` 10 ＋本包 1 |

---

## §9　自報

### 9-1　測試之對照向（R-G7(FO)）

`python3 -m pytest tests/ -q` 跑兩次：

| 向 | 結果 |
|---|---|
| 改模板後 | `10 failed, 1285 passed, 15 skipped` |
| **對照向：模板還原為原值** | `10 failed, 1285 passed, 15 skipped` |

**逐項同名同數**（`test_gates_tsv` 1、`test_intake_scaffold` 6、
`test_lint036::test_check_order_extends_only_with_profile` 1、`test_single_write_path` 2）。
本輪 **+0**；該 10 支為本包之前既有，成因與本線無涉。

### 9-2　`gate_all.py` 增紅：全在 `canon_refs`，全為 unresolved-ruling

| 向 | `canon_refs` |
|---|---:|
| 對照向（本包所有產物皆移出／還原）| **526** |
| ＋只還原 `INDEX.md` | 528 |
| ＋只還原 `templates/feature.yaml` | 527 |
| ＋四張 tsv | 529（即 **tsv 貢獻 0**）|
| **本包交付狀態（含本上繳包本身）** | **572** |

增量 **46** 處，`--report` 逐筆檢視結果：**全為 `ruling` 型 unresolved，
`section` 型增量 0**。成因只有兩種，皆非本層可自行結清：

1. **新條尚未入台帳。** `R-G43`～`R-G49` 為下放包 §一之新條與 `[DEFAULT]` 條，
   逐號實測在 `docs/fw036/RULINGS.sha.tsv` 內**命中 0**。
   結清路徑＝下放包 §三-4 之 Pei 追認 → 條文入台帳 → 重跑 `rulings_hash.py`。
2. **既有台帳落後（本包順帶量出之缺口）。**
   `RULINGS.sha.tsv` 內 **R-G30 至 R-G41 十二號一律缺列**
   （逐號實測命中 0；該區間只 `R-G42` 有列）。
   本包 §3 之對照表必須指名該區間，於是照樣增紅。
   **此缺口與本包無關且先於本包存在** —— 任何未來的包只要指名 R-G30～R-G41 都會重現。
   它與 `rulings_hash` 那支既有之紅同源。

**本層不以拿掉引用來消紅**：R-G26(FO) 與 R-G26(LEDGER) 兩側皆要求改動連同其母體一併引用，
而本包每一處新引用皆指向其授權條。消紅要改的是台帳，不是文面。

**自指之效應，具名**：本節每寫一次條號就多一處 unresolved，
故上表之交付值是**對交付檔本身量的**，非對「不含本節之草稿」量的。
本包不追求該數收斂到 0；追求的是它逐處可歸因。

逐型增量（`canon_refs` 之三型，對照向 → 交付狀態）：

| 型 | unresolved | ambiguous |
|---|---|---|
| `section` | 63 → 63（**+0**）| 746 → 746（**+0**）|
| `item` | 0 → 0（+0）| 40 → 40（**+0**，見下）|
| `ruling` | 551 → 601（**+50**）| 0 → 0（+0）|

（`ruling` 毛增 +50，waiver 外之淨增為 +46 —— 差之四處落在既有 waiver 內。
對照向與交付狀態之 waiver 外總計：unresolved 388 → 434、ambiguous 138 → 138。）

裸節號：本包文面之單層節號皆為本包與下放包**自身之節**，非 canon 節號；
canon 節號引用三處，皆帶前綴且實測其所在（`FO §8.1`、`FO §9.2`、`IN §8.4.1`）。
**惟本包首版曾寫「§」加雙層自指節號**，`canon_refs` 之 item 解析器把它讀成
兩份 canon 之同號節，判歧義（實測 `item` ambiguous +1）。已一律改寫為 `9-2 節` 之形，
即**自指之子節不冠 `§`**。此形態 PROTOCOL「文面引用」節未涵蓋（該節只講 canon 前綴），
建議分析層補一句。

`rulings_hash`／`gates_tsv`／`lint_paths` 三支之紅在對照向即已存在，本輪 **+0**。

### 9-3　本輪自造之缺陷一件（已改正，具名）

`lint_reports_refs` 首跑時把**本包自己剛寫出的 tsv 納入了掃描母體**，
於是 106 檔全被判為「有引用」（引用處即那張表自己），`removable=Y` 由 81 掉成 0。
成因：母體以「全 repo」定義而未排除本輪產物 —— 正是 R-G16(LEDGER)
（量測與聚合分離）所規制之形態，本層仍犯。
已加 `SELF_OUT` 排除集後重跑，得 §5 之 81/25。
**§5 之數為修正後之值**；首跑之錯值未進任何交付物。

### 9-4　未做且具名

| 項 | 為何未做 |
|---|---|
| `sources/MANIFEST.tsv` 補 206 列 | R-G44 尚為 `[DEFAULT]`，待 Pei 追認（§三-4）；且登記屬寫入動作，§二 只授權量測 |
| `lint_reports/` 之 81 檔移除 | R-G47 要求「專門 commit」，且 git 操作屬 Pei（R-G5(FO)）|
| `_intake/` 清空 | R-G46 明文為 Pei 動作 |
| `features/sw_update/reports/` 建立與報告搬移 | 同上，屬清理動作，另包 |
| 測試夾具之 `Test Case Specification&Result` | 見 §6 末 |
| `ANOMALIES.md:230` 之路徑更正 | 條文動作，見 7-2 節 末 |
| R-G43 之改號、`SHA_MIGRATION.tsv` | 待 Pei 裁保號側（§三-1）|

---

## §10　回應下放包 §三（Pei 待裁）之量測補充

本層不裁，只把四項各補上本包量出的數：

| # | 事項 | 本包所量出、與該裁相關者 |
|---|---|---|
| 1 | R-G43 保號側 | 遷移面 = 2,527 處／423 檔；其中 1,195 處可機器判側、1,332 處須人工。**另：R-G19 為三向歧義（3-1 節），單一維度之改號解不了它。** R-G42 不必納入（§3 更正 2）|
| 2 | Pop Up List 現行版 | **本列已於 2026-09-05 審閱後補正，原文與成因見 11 節。**　實測：`R1 (26PI)` **5 份同 sha**（`ff47b7be`；`forms/` ＋ `display`／`sw_update`／`vehicle_category` 之 `inputs/` ＋ `_intake/SW_Update/`，`dup_group` G028）；`R1 SR24 Post 2A (Dec 15, 2023)` **2 份同 sha**（`b0827f02`；`comfort`／`user_profiles` 之 `inputs/`，`dup_group` G019）。**兩案皆有實體**，裁案 2 之事實前提成立 |
| 3 | `_intake/SXM/` 之 CFTS_020 | 二檔在 repo 內無同 sha 落點（2-2 節）；`features/sxm/inputs/` 亦無該二檔 |
| 4 | R-G44～R-G49 `[DEFAULT]` 追認 | R-G44 缺口 206/220；**R-G45 待記者為 5 個 feature／13 檔次**（機器重算，見 11 節；非本包首版所寫之 2，亦非下放包之 1）；R-G46 SXM 2 檔未落；R-G47 可移除 81／須留 25；R-G48 誤值 4 處已回修；R-G49 已補 15 列＋切換宣告。**追認前 `canon_refs` 之增紅不消（9-2 節）** |

---

## §11　審閱後補正（2026-09-05，`down/20260905_GC-01_review.md` §二）

### 11-1　重測結果：**案 (b)** —— 表無誤，歸納有誤

審閱 §二 所指之二檔，`source_identity_20260905.tsv` **有列**，
表之 220 檔／206 無 doc_id／179 唯一 sha **全數維持**，2 節不重跑。

`grep "Pop Up List"` 命中 **9 列**，逐列：

| path | bytes | sha8 | dup_group |
|---|---:|---|---|
| `forms/Pop Up List HMI R1 (26PI).xlsx` | 2,951,835 | `ff47b7be` | G028 |
| `features/display/inputs/…R1 (26PI).xlsx` | 2,951,835 | `ff47b7be` | G028 |
| `features/sw_update/inputs/…R1 (26PI).xlsx` | 2,951,835 | `ff47b7be` | G028 |
| `features/vehicle_category/inputs/…R1 (26PI).xlsx` | 2,951,835 | `ff47b7be` | G028 |
| `_intake/SW_Update/…R1 (26PI).xlsx` | 2,951,835 | `ff47b7be` | G028 |
| **`features/comfort/inputs/…R1 SR24 Post 2A (Dec 15, 2023).xlsx`** | **2,954,703** | **`b0827f02`** | **G019** |
| **`features/user_profiles/inputs/…R1 SR24 Post 2A (Dec 15, 2023).xlsx`** | **2,954,703** | **`b0827f02`** | **G019** |
| `forms/Pop Up List Priority Matrix HMI R1 SR24 1A (May 3 2021).pdf` | 1,035,049 | `dc078763` | G025 |
| `features/display/inputs/…Priority Matrix…(May 3 2021).pdf` | 1,035,049 | `dc078763` | G025 |

**10-2 節之原文**（依 R-TM13 不刪，逐字留存）：

> `forms/Pop Up List HMI R1 (26PI).xlsx` 與 `display`／`sw_update`／`vehicle_category`
> 三個 `inputs/` 副本 **sha 相同**（同一 `dup_group`）；`R1 SR24 Post 2A` 一版在本次
> 母體內**未出現**。故此裁之兩案在 repo 現況下只有一案有實體

**該斷言為假**，已於 10 節補正。**Pei 裁案 2 之暫停可解除**：兩版皆有實體，
`R1 (26PI)` 在 `forms/`（依 R-G45 即現行版之預設），`R1 SR24 Post 2A (Dec 15, 2023)`
在 `comfort`／`user_profiles` 二個 `inputs/`。

### 11-2　成因（自報，與 A-GC1 同型）

問的是母體級問題（「該版在母體內有無」），做的卻是**單一檔名之 `dup_group` 檢視** ——
我看的是 `R1 (26PI)` 那一組的五列，沒對「Pop Up List」這個**字串**掃全表。
即：**以一個樣本鍵代替母體查詢**。這與 9-3 節之自造缺陷（母體含本輪產物）
及審閱 A-GC1～A-GC8（以樣本代母體）**是同一形態**，本輪三犯。

可測之防法（供分析層裁，本包不自立條）：凡上繳包內出現「未出現」「無」「皆」
之全稱否定，須附其**查詢式與命中數**（命中 0 亦寫），不得只寫結論。
`R-G13(LEDGER)`（查無之成立要件）已規制「查無」，惟其適用面是外部素材之查找；
本例是**對自家產出之表**做全稱否定，不在該條射程內。

### 11-3　順帶重算：R-G45 待記清單（機器輸出）

既然母體查詢是本次缺陷之根，R-G45 之「待記版本綁定者」亦以同法重算 ——
判準：`features/*/inputs/` 內屬 R-G45 六類共用參考檔者，其 sha 不在 `forms/` 同類之 sha 集合中。

`forms/` 之現行版（10 檔）：DBC 四份（`a51079be`／`46cb73f3`／`2a86c4bf`／`5cac2abc`）、
LID `v1_78`（`a01e1679`）、PROXI（`e7c2020f`）、HMI Settings List（`8d04e51a`）、
Pop Up List（`ff47b7be`）、Pop Up List Priority Matrix（`dc078763`）、Market Config（`7e865d55`）。

| feature | 類 | sha8 | 檔名 |
|---|---|---|---|
| comfort | HMI Settings List | `c706071f` | HMI Settings List R1 SR25 Post R1L-R (Feb 13 2026).xlsx |
| comfort | Market Config | `ae4cf0b9` | SR24 R1 Market Configuration Table v1.6.xlsx |
| comfort | Pop Up List | `b0827f02` | Pop Up List HMI R1 SR24 Post 2A (Dec 15, 2023).xlsx |
| time_management | HMI Settings List | `a2533bf3` | **HMI Settings List R1 SR24 Post 2A (June 15 2023).xlsx** |
| time_management | HMI Settings List | `41daac00` | HMI Settings List R1 SR25 Post R1L-R (Feb 13 2026).xlsx |
| time_management | LID | `9a751a72` | Logical Identifiers and CAN Mapping v1_76.xlsx |
| time_management | Market Config | `ae4cf0b9` | SR24 R1 Market Configuration Table v1.6.xlsx |
| user_profiles | Pop Up List | `b0827f02` | Pop Up List HMI R1 SR24 Post 2A (Dec 15, 2023).xlsx |
| vehicle_category | HMI Settings List | `41daac00` | HMI Settings List R1 SR25 Post R1L-R (Feb 13 2026).xlsx |
| vehicle_setting | DBC | `9ef1ec98` | PDT27_E2A_R4_BHCAN.dbc |
| vehicle_setting | DBC | `51c8fd60` | PDT27_E2A_R5_FDCAN8.dbc |
| vehicle_setting | HMI Settings List | `0a37121f` | HMI Settings List R1 SR25 Post R1L-R (Feb 13 2026).xlsx |
| vehicle_setting | LID | `ffceac36` | Logical Identifiers and CAN Mapping v1_76.xlsx |

**待記 5 個 feature／13 檔次。** 下放包記 1（vehicle_setting），本包首版記 2，
**皆低估**；正解 5。`time_management` 之 `…R1 SR24 Post 2A (June 15 2023).xlsx`
為第三次才浮出者 —— 它與 `forms/` 版**檔名亦不同**，故不進同名異體表，
只有跨 `forms/` 之 sha 比對才抓得到。

`_intake/` 內之副本不計入本表（其歸屬由 R-G46 管，非 R-G45）。

### 11-4　本補正未動之處

11 節只改 10-2 節與 10-4 節之兩格、2-1 節之末段（皆標明補正並留原文），
四張 tsv **位元未動**（sha 見 1 節，逐張複核相符），
`templates/feature.yaml`／`INDEX.md` 未再動。
審閱 §三 所裁之 GC-02 各項（測試修正、夾具表名、`ANOMALIES.md:230`、
VS-SL-01 補檔、PROTOCOL 補句、`lint_reports/` 移除）本包**一項未做** ——
依審閱 §五-7 待 Pei 准後另包。

### 11-5　補正後之閘況（9-2 節之數為補正前所量，於此接續）

| 向 | `canon_refs` |
|---|---:|
| 對照向（本包所有產物皆移出／還原）| 526 |
| 補正前之交付狀態（9-2 節之值）| 572 |
| ＋本 11 節 | 578 |
| ＋審閱檔 `down/20260905_GC-01_review.md`（**Pei 之檔，非本層所寫**）| **597** |

本 11 節之增量 **6 處全為 `ruling` 型 unresolved**（逐型實測：`section` 5,281→5,281、
其 ambiguous 746→746，即 **+0**；`item` +0），成因同 9-2 節第一類。
審閱檔之 **19 處**為該檔自身指名新條所致，與本層之產物無涉，於此分列以免混算。

依審閱 §四「+46 全為 unresolved-ruling，逐處歸因成立，**不擋包**」之判，
本節之增量屬同一類，不另請示。

PROTOCOL 之「自指子節不冠 `§`」（審閱 §三 末，直接生效）本包已遵：
補正時另抓到一處漏網（原 11-1 節之「§2 不重跑」），已改為「2 節不重跑」。
