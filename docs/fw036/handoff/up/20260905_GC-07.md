# 上繳包 GC-07 —— canon 修訂：訊號寫法來源層級＋UI 導航路徑明確化（2026-09-05）

回應 `docs/fw036/handoff/down/20260905_GC-07.md`。
**§二（訊號寫法 v3 vs v2）待 Pei 裁，本包未動 §8.7.5 本體。**
§三、§四 之導航路徑條文已施作完畢。

---

## 1　量測複核（§四.1）—— 四本數字全數複現，惟**量測範圍須明載**

`sha256` 前 12 碼四本全數相符，header row 皆為 **9**，資料列數全數相符。
**下放包 §一之計數在「P4 範圍」下逐格複現；在「全欄範圍」下不成立。**

| 本 | sha12 | 資料列 | `Send CAN:` | `$MSG.Sig$` | `PROXI` | `Send the signal` | `$…$` |
|---|---|---:|---:|---:|---:|---:|---:|
| SWC（下放包載）| `7dd8f1737c20` | 145 | 52 | 0 | 41 | 0 | 215 |
| SWC（P4 實測）| 同 | **145** | **52** | **0** | **41** | **0** | **215** |
| SWC（全欄實測）| 同 | 145 | 52 | 0 | *46* | 0 | *570* |
| VF230（下放包載）| `adee5dc462ae` | 457 | 184 | 0 | 152 | 0 | — |
| VF230（P4 實測）| 同 | **457** | **184** | **0** | **152** | **0** | 152 |
| VF230（全欄實測）| 同 | 457 | 184 | *81* | *215* | 0 | *346* |
| Home（下放包載）| `1895fb2a2b44` | 216 | 0 | 0 | 0 | 0 | — |
| Home（P4 實測）| 同 | **216** | **0** | **0** | **0** | **0** | **0** |
| PM（下放包載）| `35305835796a` | 390 | 0 | 606（207 列）| 85 | 114 | — |
| PM（P4 實測）| 同 | **390** | **0** | **606（207 列）** | **85** | **114** | 643 |

```text
量測條件（複驗用）
  母體：工作簿內 sheet.title.startswith("Test Case Specification") 之全分頁
  資料列：header row 之後，`No.#` 欄非空者
  P4 範圍：pre-conditions / input test data / test procedure / expected result 四欄
           （即 lint036.P_FIELDS；下放包 §一未載範圍，實測反推得之）
  全欄範圍：資料列之所有儲存格
  正則：Send CAN:｜\$[A-Za-z0-9_]+\.[A-Za-z0-9_]+\$｜PROXI｜Send the signal｜\$[^$\n]+\$
  腳本：本包一次性量測腳本，欄位定位 import scripts/lint036.py 之
        find_header_row / build_column_map / P_FIELDS
```

### 1-1　**新事實：VF230 之 v3 式 81 處全在 `test_item` 欄** —— 對 §二 有直接影響

下放包 §二-3 之斷言「三本 Pei 語料 v3 式出現 **0 次**」在 P4 範圍下成立，
**在全欄範圍下不成立** —— VF230 之 `test_item` 欄有 **81 處** `$MESSAGE.Signal$`
（首見 `Test Case Specification 測試用例規範` 列 164 `$TELEMATIC_VEHICLE_SETUP.DRLEnable_Req$`、
列 165／166 `$IPC_VEHICLE_SETUP.DRLEnable$`）。SWC 與 Home 兩本全欄皆 **0**。

`test_item` 上半為**需求原句 verbatim**（`lint036.py` P_FIELDS 之註解、R-6）。
故實測所示為：

- **v3 式（`$MESSAGE.Signal$`）是上游 CFTS／VF 之來源側記法**；
- **v2 式（`Send CAN:`）是 Pei 撰寫測試步驟時之作者側記法**。

二者不是同一層的兩個候選，而是**來源層與作者層各自的記法**。
下放包 12 依 CR30580/30581 參考本立 v3 時，很可能是把**來源側記法**讀成了作者側記法
（該參考本查無，不可複驗，此為推論不是實測 —— 明標於此）。

⚠ **此事實支持下放包 §二之建議（選項甲）**，且給出了 v3 之成因，
但 **v2/v3 之取捨仍屬 Pei 裁**，本包未動 §8.7.5 本體。

### 1-2　補充量測之差異（以執行層量值為準，R-G69）

| 項 | 下放包 | 執行層實測（P4 之 proc 欄，逐行去編號後計行首） | 判 |
|---|---|---|---|
| Home `Navigate to` | 65 | **65** | 相符 |
| Home `Press`／`Press the` | 158 | 行首單詞 `Press` **186**（其中 `Press the` 68、`Press "Edit` 32、`Press and` 21、`Press "+"` 19…）| **不符** |
| Home `Select`／`Select the` | 135 | 行首單詞 `Select` **154**（其中 `Select the` 56、`Select "Reorder` 24、`Select "Add` 18…）| **不符** |
| Home `Menu Bar` | 10 | 未複現（下放包未載其為行首計或全文計）| 口徑不明 |
| VF230 `Read the` 457／`Open the` 365／`Set the` 204／`Send CAN` 184／`Power cycle` 155 | 同左 | **五項全數相符** | 相符 |
| VF230 `Open the Vehicle Settings menu and wait until it is fully rendered` | 365 | **365** | 相符 |

Home 之 `Press`／`Select` 二項差異（186 vs 158、154 vs 135）**未能還原下放包之口徑**；
依 §四.1「數字不符者以執行層量值為準」，本包採 186／154。

### 1-3　台帳現查（R-G23，落檔前）

```text
查詢：ruling_anchor.anchor_ruling_numbers(text, "R-G") 掃 repo 全部 *.md（排除 .git）
結果：R-G 錨點最高號 69；已用尾段 58..69 連續
      "70" 未佔用、"71" 未佔用 → 無需順延
```

---

## 2　§二 待裁 —— 本包未動

`docs/runtime/ASPICE_SWE6_AI_Instruction.md` §8.7.5 本體、lint 檢查 P 之判準、
`vehicle_setting` 之 `[OVERRIDE §8.7.5]`、`pm_29` 之 207 列 —— **全數未動**。
§8.7.5 首段只加了一段**指路**（不改判準）：訊號線歸本節、畫面線歸新 §5.8，
並明載「訊號側之來源層級擬立於 R-G70，待 Pei 裁」。

**R-G70 未立號**（號已現查為未佔用，保留給 Pei 裁後使用）。
請 Pei 於甲／乙／丙擇一；擇甲後之施作清單即下放包 §四.4，本層待命。

---

## 3　§三 施作 —— R-G71 已落檔

### 3-1　canon（`docs/runtime/ASPICE_SWE6_AI_Instruction.md`）

| 位置 | 內容 |
|---|---|
| **§5.8（新）** | UI Navigation Path (a)–(f)，逐字照下放包，**二處例示經訂正**（見 3-3） |
| **§5.3** | 增 `ENTER_SETTINGS_APP`／`ENTER_VEHICLE_SETTINGS`／`ENTER_HOME_SCREEN` 三常數，各附來源檔＋分頁＋列號 |
| **§9 第 18 條（新）** | `Navigation path complete from fixed entry point, hops quoted from HMI source, no PENDING left unregistered (§5.8)` |
| **§8.7.5 首段** | 增「來源分線」段：本節只管訊號與參數；畫面之入口／層級／選項標籤唯一來源為 §5.8。並標明 R-G70 待裁、本節本體未動 |

### 3-2　台帳（`docs/fw036/RULINGS_LEDGER.md`）

- 新增 `### R-G71 —— UI 導航路徑明確化（全域）` 錨與條文全文（fenced）。
- 「條文落檔位置」表加列 `| R-G71 | …§5.8（＋§5.3 三常數、§9 第 18 條、§8.7.5 首段之來源分線） |`。
- `rulings_hash.py` 重生：**766 錨點**（group 10／report 12／ruling 735／superseded 9），來源 20 檔。
  R-G71 指紋 `72328d57`（標題 `d33443c7`），`body_kind = fenced`，
  落 `docs/fw036/RULINGS_LEDGER.md:1727`，46 行。
- `canon_refs.py` 重跑（`--waiver`）：

```text
              施作前   施作後
unresolved      29  →   18    （§5.8 補上 11 處原本懸空之引用）
ambiguous      140  →  140    （不變）
FAIL 合計      169  →  158
ruling unresolved 0 → 0       （R-G71 兩側皆有錨，可解析）
```

### 3-3　**§5.3 三常數：一條有來源，兩條依 R-G71(d) 落 PENDING**

§四.2 令執行層「自 Home／VF230／HMI Settings List 抽取實際字串後填入」。
實測結果是**該三條字串在兩本交付本中都不存在**：

- Home 本之導航行為 `Navigate to the Home Screen page that contains the Shortcuts widget`（22 次）
  —— 是**頁內導航**，不是固定入口。
- VF230 本只有 `Open the Vehicle Settings menu…`（365 次）—— 正是 §5.8(b) 所禁之寫法。

故改自 HMI 來源抽取，結果：

| 常數 | 狀態 | 來源（檔／分頁／列） |
|---|---|---|
| `ENTER_SETTINGS_APP` | **已填** `Press "Apps" on Menu Bar to open App Drawer` → `Select "Settings" in the App Drawer` | Menu Bar and App Drawer L&F `.xlsx`，分頁 `Basic Report`，**列 22**（NRL-127734，§4.1 命名表 `Settings｜Settings｜Settings｜Core Feature｜Vehicle`）＋**列 23**（NRL-127735，ADC1） |
| `ENTER_VEHICLE_SETTINGS` | **第一 hop PENDING**，第二 hop 已填 `Select "Settings" tab` | Vehicle Category L&F `.xlsx`，分頁 `Basic Report`，**列 13**（NRL-171043，VC1.）＋**列 17**（NRL-171047，VC2.0.3「tab 名為 Settings」） |
| `ENTER_HOME_SCREEN` | **PENDING** | 查無（見 §4 之查詢式與命中數） |

二條 PENDING 依 R-G71(d) 寫作 `PENDING: DR-{n} HMI entry path <target>`，
DR 草稿已落 `features/vehicle_setting/DATA_REQUESTS.md`（**未取號** ——
依該檔 standing rule「未送出前不佔號」）。**未臆造任何 hop**（§8.4.1、§五）。

### 3-4　**下放包 §三之二處例示不可照用，已訂正**（皆與條文自身之 (c)(d) 抵觸）

**(a) 之 `Press "Vehicle" on Menu Bar`** ——

```text
母體：Menu Bar and App Drawer L&F .xlsx 分頁 Basic Report 列 22（NRL-127734）之 §4.1 命名表
查詢：逐列比對 Feature Name / Drawer Name / Shortened Name 三欄是否為 "Vehicle"
命中：0（"Vehicle" 只出現於 App Category 欄）
```

例示改為全數有來源者：`"Apps"` → `"Settings"` → `"Aux Switches"`
（末者取 `forms/HMI Settings List R1 SR25 Post R1L-R (Feb 13 2026).xlsx`
分頁 `Settings` **列 564** `21. Aux Switches`）。
§5.8 並加一句：例中每個 hop 皆須有來源，步驟不得示範 (c) 無法溯源之 hop。

**(e) 之 `Set "SWITCH 3 Hold Last State" = "Disable"`** ——
HMI Settings List 分頁 `Settings` 列 564–588 之實際結構為
`21. Aux Switches` → `Aux 1`…`Aux 6` → `Type`／`Power Source`／`Recall Last State`
（`Type` 之 Options 欄為 `Latching, Momentary`，列 566 等）。
**`SWITCH 3 Hold Last State` 與 `Disable` 是 VF230 之寫法，不是 HMI 標籤。**
例示改為 `Set "Type" = "Latching"`，並於 §5.8(e) 加一段警示：
HMI 標籤與 VF／CFTS 參數名經常不同字串，畫面 hop 取 HMI 標籤，
VF 名留在 `test_item` verbatim 上半與訊號行（§8.7.5）。

⚠ **此差異之影響超出本包**：VF230 之 365 列若依 R-G71(c) 改寫，
其設定項名稱將由 VF 名換成 HMI 標籤。**本包不回修任何交付本（R-TM13）**，
但該轉換是否為 Pei 所欲，已列為 DR 草稿之所求第 3 問。

---

## 4　lint 新檢查 —— **代號由 Q 改為 X**（Q 已被佔用）

### 4-1　撞號

```text
查詢：scripts/lint036.py 之 CHECK_TITLES 鍵集
現有：A B C D E F G H I I-sibling J K L M N P Q R T U V W
其中 Q = 「不可見字元（NBSP／全形空格／行尾空白）」（R-10(a)，21 包新增）
```

下放包 §四.3 指定之代號 **Q** 會覆寫既有檢查。執行層改用次一未佔用代號 **X**，
**判準與粒度照下放包**。台帳 R-G71 條下已加註此訂正。

### 4-2　實作（`scripts/lint036.py`）

- `CHECK_TITLES["X"] = "導航路徑無固定入口（§5.8／R-G71）"`，粒度**每行**，
  列入 `PROFILE_CHECKS` → **profile-only**，未指定 `--profile` 時不跑
  （既有八本之報告基線完全不動）。
- 判準：`\b(menu|page|screen|settings|tab)\b`（proc）而**同 TC 內**
  （pre ＋ proc 全文）無 §5.8(a) 閉合清單之入口者報 WARN。
  已標 `PENDING: DR-` 之行跳過（由 U 承擔，不重複報）。
- **入口以整列為範圍而非逐行** —— 入口常寫在 Pre-Condition 或前一步驟，
  逐行判會把正確的多步路徑整條報成違規。
- 新增 5 個單元測試（`tests/test_lint036.py`）：有／無入口、入口在 PC、PENDING 跳過、profile-only。

### 4-3　四本基線（**只報不改**）

報告落 `features/<f>/reports/` —— 依 **R-G47**（feature 級 lint 報告一律落此），
非下放包 §四.3 所寫之 `docs/runtime/lint/`（該目錄不存在，且與 R-G47 抵觸）。

| 本 | sha8 | 資料列 | **X** | 同跑之其他檢查 |
|---|---|---:|---:|---|
| VF230 `output/…_20260902.xlsx` | `aed08b4d` | 438 | **361** | I-cross 278／K 25／P 1003／T 25／U 25 |
| BedLowering `output/…_20260902.xlsx` | `a0331aa6` | 151 | **169** | H 7／I-cross 151／K 151／R 153／W 11 |
| VehicleCategory `output/…_20260902_working.xlsx` | `f9bbeffc` | 126 | **189** | H 1／I-cross 122／K 16／L 5／R 14／U 34／W 4 |
| pm_29 `sandbox/b29/pm_29.xlsx` | `35305835` | 389 | **237** | H 5／I-cross 386／P 10／U 32／W 5 |
| **合計** | | 1104 | **956** | |

產出檔：
`features/vehicle_setting/reports/VF230__vehicle_setting_aed08b4d_20260905.{md,json}`、
`features/bed_lowering/reports/BedLowering__bed_lowering_a0331aa6_20260905.{md,json}`、
`features/vehicle_category/reports/VehicleCategory__vehicle_category_f9bbeffc_20260905.{md,json}`、
`features/power/reports/pm_29__power_35305835_20260905.{md,json}`。

### 4-4　閘與自查表已同步

- `scripts/selfcheck_map.py` 增第 18 項對映：**`partial`／閘 `X`**，
  殘餘人力寫明「hop 標籤是否逐字取自 HMI 來源、路徑是否完整、PENDING 是否已登記 DR」
  三項仍為人讀（R-G60 禁止把 `partial` 記成 `full`）。
- `docs/runtime/SELFCHECK_MAP.tsv` 重生（18 列，對映到之閘 12 支）。
- `docs/runtime/GATES.tsv` 重生，X 列：`profile-only`／`每行`／`未校準`／`selfcheck_items = 18`。

---

## 5　未結 DR 清單（§四.5）

⚠ **無法給出跨 feature 之「未結」全稱數**（R-G50）：各 feature 之 `DATA_REQUESTS.md`
關閉狀態記法不一致（`power` 用 `（High（live））` 標記，其餘用 `## DR-n` 節標題＋內文敘述），
**無統一之機讀關閉欄**。以下為可機讀者，未關閉狀態需人讀：

```text
查詢 A：grep -oE "DR-[A-Z]{0,3}[0-9]+（?[^）]*live" features/*/DATA_REQUESTS.md
查詢 B：grep -c "^## DR-" features/*/DATA_REQUESTS.md
```

| feature | live 標記（查詢 A）| `## DR-` 節（查詢 B）|
|---|---:|---:|
| power | **25** | 0 |
| vehicle_setting | 0 | **39** |
| time_management | 0 | 11 |
| vehicle_category | 0 | 10 |
| driver_distraction | 0 | 9 |
| power_moding | 0 | 8 |
| bed_lowering／sw_update／vsm_v43 | 0 | 5 各 |
| vsm_v42 | 0 | 4 |
| 其餘 10 feature | 0 | 0 |

**建議**（不在本包施作）：`DATA_REQUESTS.md` 之關閉狀態應有統一機讀欄，
否則「未結 DR 清單」在任何一包都只能是人讀結果。

### 5-1　新型別 DR「HMI entry path」之登記格式範例（一條）

已落 `features/vehicle_setting/DATA_REQUESTS.md` 檔末（未取號）。其形態為：

```text
## DR 草稿 —— HMI entry path（新型別，未取號；GC-07 §三 R-G71(d) 開立）

### 標的（2 個固定入口之 hop 標籤）
| 常數 | 缺的 hop | 查詢式與命中數（R-G50） |
| ENTER_HOME_SCREEN | 回到 Home Screen 之入口標籤 |
  母體＝Home Screen L&F 104 entries ＋ Menu Bar §4.1 命名表；
  掃 home button／Home icon／return to the home／access the home —— 命中 0；
  命名表無 Home 列 —— 命中 0 |

### 已查得而不足以構成完整路徑者（G-D 留痕）
  Vehicle Category L&F 列 13／15／17 —— 第二 hop 有來源，第一 hop 沒有

### 所求
  1. Menu Bar 上 Vehicle Category 之按鈕標籤逐字
  2. 回到 Home Screen 之入口逐字
  3. HMI 標籤 vs VF 名之取捨（Recall Last State ≟ SWITCH 3 Hold Last State）

### 本層之處置
  canon §5.3 以 `PENDING: DR-{n} HMI entry path <target>` 佔位，不臆造 hop（§8.4.1）；
  交付本不回修（R-TM13）；VF230 之 365 列由 lint X 報 WARN，只報不改。
```

---

## 6　測試

```text
python3 -m pytest tests/ -q
→ 11 failed, 1308 passed, 15 skipped（施作後）

其中 8 failed 在 HEAD 即為紅（已以 git stash 隔離複驗，非本包造成）：
  tests/test_intake_scaffold.py 6 項、tests/test_single_write_path.py 2 項
本包修好之 3 項：
  test_lint036.py::test_check_order_extends_only_with_profile
      —— 其斷言停在 ["Q","R","T","U","V"]，而 HEAD 之 PROFILE_CHECKS 已含 I-cross、W，
         **在 HEAD 即為紅**。改為對 lint036.PROFILE_CHECKS 斷言，不再逐一列舉。
  test_selfcheck_map.py::test_parses_seventeen_items → 改名 test_parses_all_items，18 項
  test_selfcheck_map.py::test_every_item_is_classified → 改對 len(sm.MAPPING) 斷言
本包新增 5 項（X 之單元測試）全綠。

複驗：pytest tests/test_lint036.py tests/test_selfcheck_map.py
      tests/test_canon_refs.py tests/test_rulings_hash.py -q → 210 passed
```

⚠ `tests/test_intake_scaffold.py`（6）與 `tests/test_single_write_path.py`（2）之既有紅燈
**本包未修**（不在下放包範圍），列此供後續包處理。

---

## 7　本包未做（§五之遵守）

- 未回修任何已交付本（R-TM13）—— VF230 之 365 列、SWC、Home 皆原封。
- 未預填無來源之 §5.3 常數字串、未代擬任何 HMI 路徑（§8.4.1）。
- §二未裁前未動 §8.7.5 本體、未動 lint P 之判準、未動 `vehicle_setting` 之
  `[OVERRIDE §8.7.5]`、未轉換 `pm_29` 之 207 列。
- 未立 R-G70。

---

## 8　待 Pei 之三件

1. **§二 甲／乙／丙**（訊號寫法 v2 回歸 vs 維持 v3）。
   本包 §1-1 之新事實（v3 式 81 處全在 `test_item` 需求原句 verbatim 欄）供裁決參考。
2. **DR 草稿之取號與送出**（`features/vehicle_setting/DATA_REQUESTS.md` 檔末）。
3. **§5.8(c) 之連帶影響**：畫面 hop 一律取 HMI 標籤，
   意味 VF230 之 `SWITCH n Hold Last State` 類名稱在**新產出**中將換成 HMI 標籤。
   本包已依 R-TM13 不回修既有本，但新舊並存之形態請 Pei 確認可接受。
