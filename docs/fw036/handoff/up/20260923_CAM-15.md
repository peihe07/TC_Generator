# 上繳包 CAM-15 —— R-CAM18／R-CAM13(d)、framework 重開二次、dry-run v2 244 列、B01a 補列 ＋ B01b 20 列

對應下放包：`docs/fw036/handoff/down/20260923_CAM-15.md`（審閱 `down/20260923_CAM-14_review.md` 同發）。
執行層 session：延用。日期 2026-09-23。

**一句話**：兩條裁決落檔（取號現查 `{live}` ＝ **18**，與下放包期望值相符）；
framework 重開二次完成，改名 **18 筆** json 同步、舊名殘留 **0**；
A 本 dry-run v2 **244 列**（240 TC ＋ 4 佔位）ERROR 類全 0、對帳 244 ↔ 244；
B01a 補 `-023`（共 **23 列**）、B01b **20 列** —— **`Activation and Exit` 組 43/43 leaf 全數產出**。
**未寫 `features/camera/delivered/`，未 tag。**

---

## §1　落檔

### 1-1　取號現查（R-G23）

`features/camera/RULINGS.md` 落檔前現有 `### R-CAM1`～`R-CAM17`（含 `(b)`／`(c)` 等增補錨），
`rulings_hash.py` 實測 **24 錨點**；全 repo 無 `R-CAM18` 以上。
**`{live}` ＝ 18**，與下放包 §1 之期望值相符。

### 1-2　R-CAM18（B 本 Vehicle Model 之可佈性判準）

| 錨 | `sha8` | `body_sha8` |
|---|---|---|
| **R-CAM18** | `bbf46504` | `9d43e878` |

條文五款：(a) 預設五款已出資平台皆 `1`；(b) PROXI byte 於該平台之本不存在 → `0`；
(c) 觸發訊號於該平台之 DBC 不存在 → `0`；(d) 判 `0` 者 reasoning 須具名證據（R-CAM17）；
(e) `598`／`5210` 恆 `0`（R-CAM2 不變）。A 本不受影響（其車型軸仍由 SYS2 平台錨直接判，R-CAM3）。

### 1-3　R-CAM13(d)（Test Set 改名）＋ framework 重開二次

**改名以同級錨落檔**（R-TM13／**GCB-06**：修訂段自立為同級 `###` 錨，原條 fenced sha 不變）：

| 錨 | `sha8` | `body_sha8` |
|---|---|---|
| **R-CAM13(d)** | `8301fefa` | `0bead1db` |

**既有 24 錨點之 `sha8`／`body_sha8` 逐一比對 HEAD：改變 0 個**（證據表第 1 項）。

framework 重開二次（Tier 2）：

```
重開前 sha16  8569aa484b552b7c   （CAM-07 §1-8 落檔時之值）
重開後 sha16  95b5e5ff567c491b   （本包落檔）
量測法        profile §8 —— 全文中狀態行之最後一個 sha16 欄位以 16 個 0 代入後取 sha256 前 16 碼
複驗          95b5e5ff567c491b（相符）
重開範圍      VIII.2 之組表第 10 列、其註四處、與狀態行；VIII.3～VIII.8 未動
```

**改名同步之檔清單**（實測）：

| 檔 | 改動 |
|---|---|
| `features/camera/generated/**/*.json` | `test_set` 欄 **18 筆**（`NR1L-RVC-034`～`-045`、`-048`、`-093`～`-097`）；另 18 檔之 `reasoning` 內文引用同步 |
| `features/camera/generated/**/*.md` | 改名當時由 `render_tc.py` 重生 **262 檔**（十一目錄，b01b 尚未生成）|
| `features/camera/generated/*/INDEX.md` | batch01b／02a／02b／02c 四份 |
| `features/camera/framework.md` | VIII.2 第 10 列、註四處、狀態行 |
| `features/camera/data/layer2_assign.tsv` | `SWE-CAM-017` 之 `test_set` 欄 |
| `features/camera/data/batch02_plan.tsv` | 5 處 |
| `features/camera/DATA_REQUESTS.md` | DR-CAM-j 之題與影響欄 |
| `features/camera/RD_FEEDBACK.md` | 1 處 |
| **不改** | `features/camera/data/coverage.tsv`（**無 `test_set` 欄**，實測欄序 `swe_id／disposition／batch／tc_count／tc_ids`）；`docs/fw036/handoff/`（唯讀史料）；`RULINGS.md` R-CAM13 之原條（fenced sha 不得變）；framework 中記述 CAM-06 當時決定之兩句（保留原名並加今名指標）|

**升級條件 §6 第一項不成立** —— 改名後 `features/camera/` 與 `docs/`（handoff 除外）
之舊名殘留為 **0**（另有 4 處在 `features/ics_management/docs/reports/`，為他 feature 引用
來源文件目次之字面，非 Camera 之 Test Set 值，不在射程）。

### 1-4　DECISIONS 6-51 ～ 6-55

`features/camera/DECISIONS.md`（sha256_16 `e1dee259be62fc16`）：

- **6-51** B 本 Vehicle Model 判準 `[PROPOSED]` → **`[DECIDED]`**，立 R-CAM18
- **6-52** A 本交付本補 4 列僅填 D 欄，交付口徑 **244 列**
- **6-53** A-CA33 之兩欄分立（N 欄 `…_7.2.n`／D 欄 037 之 req_id）
- **6-54** B01a 改 23 列
- **6-55** Test Set 改名，立 R-CAM13(d)，framework 重開二次

### 1-5　A-CA33 之處置落檔

`b01a` 六列之 `specification_reference` 改寫實測：

| TC | req_id（D 欄）| N 欄 | 逐字母體 |
|---|---|---|---|
| `NR1L-RVCHMI-017` | `SWE1-RVC-008` | `…_7.2` | `NRL-142614`（標題句，排他性）|
| `NR1L-RVCHMI-018` | `SWE1-RVC-008-01` | `…_7.2.1` | `NRL-142615` |
| `NR1L-RVCHMI-019` | `SWE1-RVC-008-02` | `…_7.2.2` | `NRL-142616` |
| `NR1L-RVCHMI-020` | `SWE1-RVC-008-03` | `…_7.2.3` | `NRL-142617` |
| `NR1L-RVCHMI-021` | `SWE1-RVC-008-04` | `…_7.2.4` | `NRL-142618` |
| `NR1L-RVCHMI-022` | `SWE1-RVC-008-05` | `…_7.2.5` | `NRL-142619` |
| `NR1L-RVCHMI-023` | `SWE1-RVC-008-06` | `…_7.2.6` | `NRL-142620` |

`b01a_plan.tsv` 之 `spec_reference` 欄同步，`note` 欄逐列註明；各列 reasoning 註 037 收摺為 `_7.2`。

---

## §2　A 本 dry-run v2

### 2-1　檔

```
features/camera/sandbox/a_dryrun/FM-WI-FSM-036-A01 STLA 測試用例規範與結果_SWQT STLA Test Case Specification & Result_SWQT_RearViewCamera_A_20260923_dryrun2.xlsx
sha256  f194046c923fd85fd898f5f929bb90f00d8e8af6e54108f90ca0b2af021b89d7
大小    59391 bytes｜資料列 244（第 10～253 列）
```

（v1 之 sha256 為 `eae12a58879a…`，240 列；v1 檔保留於同目錄供對照。）

### 2-2　4 個佔位列（R-G42 一／DECISIONS 6-52）

| 工作簿列 | D 欄 | `AH` 註 |
|---:|---|---|
| 111 | `SWE-CAM-007` | `No TC — all sources delegated to SWE-CAM-006 (R-CAM10)` |
| 176 | `SWE-CAM-013` | `No TC — all sources delegated to SWE-CAM-004 (R-CAM10)` |
| 252 | `SWE-CAM-024` | `BLOCKED — DR-CAM-a` |
| 253 | `SWE-CAM-025` | `No TC — all sources delegated to SWE-CAM-020 (R-CAM10)` |

四列**只填 `B`（序號）／`D`（req_id）／`AH`（註）**，Test Set 與其餘欄留空。
`D` 欄升冪**成立**，且自 `SWE-CAM-001` 至 `-025` **無缺號**（v1 缺 7／13／24／25，今補齊）。

### 2-3　lint 全表

`python3 scripts/lint036.py --profile camera <dryrun2.xlsx>`

```
行計 A=0 B=0 C=0 D=0 E=0 F=0 G=0 H=0 I=0 I-sibling=0 J=36 K=0 L=0 M=0 N=0
     P=0 Q=0 R=0 T=0 U=54 V=0 I-cross=240 W=0 X=3 Y=0 Z=0
報告 docs/fw036/lint_reports/RearViewCamera_A__camera_f194046c_20260923.md
```

**ERROR 類全 0；與 v1 之 240 列逐項相同** —— 4 個佔位列未產生任何違規
（`M` 空欄三態 0、`Z` 0、`D` 順序 0）。**升級條件 §6 第二項不成立。**

### 2-4　Test Set 拼寫

工作簿實得 **9 組**，各組拼寫一致無變體；**舊名 `Auxiliary Cameras` 殘留 0**：

```
Additional Cameras 18｜Configuration 20｜Diagnostics 17｜Display Arbitration 32
HMI Overlays 26｜LVDS Messaging 61｜Startup and Shutdown 27｜State Handling 20｜Video Pipeline 19
```

第 10 組 `AUX Camera` 仍為 0 列（`SWE-CAM-024` BLOCKED，其佔位列之 Test Set 欄依 6-52 留空）。

### 2-5　對帳

| 口徑 | 值 |
|---|---:|
| `coverage.tsv` SWE 列 | 25 |
| `coverage.tsv` `tc_count` 合計 | 240 |
| `coverage.tsv` 零 TC 列（`tc_ids` 為 `—`）| 4（`-007`／`-013`／`-024`／`-025`）|
| 240 ＋ 4 | **244** |
| 工作簿資料列 | **244** |
| 判 | **合** |

---

## §3　B01a 補列

`-008-06`（§7.2.6）生成 **`NR1L-RVCHMI-023`**：hop label 逐字取該列之
`«Camera» shortcut icon` 與 `Status bar Shortcut menu`。

入口路徑（如何開啟狀態列捷徑選單）**查無來源** —— `spec-index/cache/` 之
`SYS1_HMI_Status_Bar_…`（99 列）與 `SYS1_HMI_Core_HMI_…`（169 列）掃描字串 `shortcut`：
前者 **0 命中**，後者 **1 命中**而為 Home Screen 之拖曳重排（§13.2.1），皆非開啟動作。
故**沿 DR-CAM-g** 並將該條由「Controls page 之入口」**擴為「HMI 入口路徑之逐字來源」**
（(1) Controls page、(2) Status bar Shortcut menu），不另開新 DR。

`b01a_plan.tsv` 23 列（sha256_16 `8d6caa9844c682f0`）、`coverage_b.tsv` 同步。

---

## §4　B01b：`Activation and Exit` 後 20 leaf

### 4-1　產物

`features/camera/generated/b01b/` —— 20 份 json ＋ 20 份 md ＋ `INDEX.md` ＋ `LINT_REPORT.md`。
TC ID `NR1L-RVCHMI-024`～`-043`；計畫 `features/camera/data/b01b_plan.tsv`（sha256_16 `a44fb0716412ad96`）。

涵蓋 RVC+PAM §7.3（`-015`）、§7.3.1～§7.3.3（`-016`／`-017`／`-018`，**只存在於 cache 本**，
A-CA14／**R-CAM6**）、§7.4（`-019` 四子列）、§7.5～§7.5.3（`-020`～`-023`）、
§8.1／§8.1.1（`-024`／`-025` 四子列）、§8.6（`-033`）、§8.7（`-034`）。

**`Activation and Exit` 組至此 43/43 leaf 全數產出**（`coverage_b.tsv`：
leaf PRODUCED 43 ＋ umbrella 7 ＝ **50**，與 framework VIII.3 第 2 組之 `50｜43｜7` 逐格相符）。

### 4-2　判軸之量測依據

| 事項 | 來源／實測 |
|---|---|
| PAM 配備旗標 | PROXI `CVPAM_Presence`（byte 173 bit 1，`0 = Absent`）。**不取** `PAM_Configuration`（byte 118 bit 4–5）—— 其值域只有 `0 = Rear`／`1 = Front And Rear`，無「無 PAM」之值 |
| HU 尺寸 | PROXI `Radio_Display_Type`（byte 185 bit 0–3）值域逐字：`1 = 7" 1280x768`、`2 = 8.4" 1024x768`、`3 = 10.1" 1920x1200`、`4 = 10.1" 1200x1920`（Portrait 取 `4`，寬 < 高）|
| 速度門檻 | profile §9：`c_VEHSPD_MAX` = 8 mph ＝ 12.874752 km/h；raw 205 ＝ 12.8125（門檻下）、206 ＝ 12.875（門檻上），無恰等於之 raw（**A-CA30**）。來源於 §7.5.3 逐字載 `(8mph)`，與該表相符 |
| RVC timeout | §7.5.3 逐字 `After 10 seconds` ＋ `Timer will start immediately once the vehicle is shifted from REVERSE`；與 Pop Up List `PU0361` 之 `Timeout` 欄 `10` 相符 —— **同一計時器之兩處記載，不另開 DR** |
| gear／speed／ignition 訊號 | 沿 A 本已定之 message 對照（**R-CAM1(b)**）|

**`Toro(2261)` = 0 之 5 列**（`-024`／`-025`／`-026`／`-027`／`-031`）：
其前提所用之 byte 173／185 於 `Toro_ATL_MI` 之 2019 本零命中 → **R-CAM18(b)**，逐列 reasoning 具名。

### 4-3　popup

§7.5.3 引 `PU0361`、§8.1 引 `PU0170`，**兩者皆於 `forms/Pop Up List HMI R1 (26PI).xlsx` 查得**
（非 `PENDING: DR-CAM-h`）。`data/popup_texts.tsv` 補 `PU0361` 一列
（`PU0170` 該檔既有列已載其訊息欄，不重複登）。

**自查得一處來源衝突 → A-CA34**：`X` 退出鍵之位置，SYS1 兩處皆載 **upper right**
（§7.5.3 `NRL-142629`、§8.1 `NRL-142631`），而 `PU0170` 之文字欄載 **bottom right**。
依 §4.3.1 以 SYS1 逐字為 ER（`-038` 判 upper-right），差異記於該列 `Remarks`（profile §10 第二類）。

### 4-4　hop label 之可辨性（升級條件 §6 第三項）

B01b 20 列**無任何 HMI hop**（全為 CAN 觸發、PROXI 配備切換、或畫面上之 soft control／硬鍵），
故「hop label 不可辨」之列數為 **0**，升級條件不成立。

### 4-5　lint 與 selfcheck

```
selfcheck_camera.py  b01b（TC 20 筆）        十項全 0｜錨 20／反查失敗 0
lint036 --profile camera（b01b_lint.xlsx）   ERROR 類全 0｜非致命 I-cross=20（U／X／J／Z 皆 0）

selfcheck_camera.py  b01a（TC 23 筆）        十項全 0｜錨 23／反查失敗 0
lint036 --profile camera（b01a_lint.xlsx）   ERROR 類全 0｜非致命 U=18／I-cross=23／X=5

selfcheck_camera.py  十二目錄（TC 283 筆）    十項全 0｜錨 287／反查失敗 0｜常數錨 2｜合一錨 2
```

**B01b 之 PENDING 為 0 列** —— 本批之配備旗標、計時值、門檻值皆有來源。

---

## §5　R-CAM17 證據表

| # | 主張 | 掃描字串／判準 | 母體 | 命令 | 命中 |
|---|---|---|---|---|---|
| 1 | 新增兩錨未動既有條文指紋 | `sha8`／`body_sha8` 逐錨比對 | `features/camera/RULINGS.md` 落檔前 24 錨點 | `rulings_hash.py --target … --out` × 2 ＋ 集合比對 | 既有 24 錨**改變 0**；新增 `R-CAM13(d)`／`R-CAM18` |
| 2 | framework 自指 sha16 可複驗 | 狀態行最後一個 sha16 欄以 16 個 `0` 代入 | `features/camera/framework.md` 全文 | profile §8 之 python 片段 | `95b5e5ff567c491b`（與所載**相符**）|
| 3 | 改名後舊名殘留 0 | `Auxiliary Cameras` | `features/` ＋ `docs/`（排除 `docs/fw036/handoff/`）| `grep -rn` | **0**（另 4 處在 `features/ics_management/docs/reports/`，為他 feature 引用來源目次之字面）|
| 4 | 工作簿 Test Set 無舊名 | `test_set` 欄逐列比對 | dryrun2.xlsx 244 列 | openpyxl 逐格 | **0** |
| 5 | 佔位列未引入違規 | v1 對 v2 逐項比對 | 240 列 vs 244 列 | `lint036.py --profile camera` × 2 | 22 項計數**逐項相同** |
| 6 | 對帳 | 240 ＋ 4 ↔ 244 | `coverage.tsv` 25 列／工作簿 244 列 | 集合與加總 | **合** |
| 7 | `PAM_Configuration` 無「無 PAM」之值 | 值域逐字 | `forms/proxi/Promaster_ATL_MI/…637_R42_20220204.XLSM` | openpyxl 逐格讀該列 | 值域僅 `0 = Rear`／`1 = Front And Rear`（**2 項**）|
| 8 | `CVPAM_Presence`／`Radio_Display_Type` 缺於 Toro | 兩名逐字，不分大小寫 | `forms/proxi/` **6 本** | `proxi_have.py`（全表串接後 `re.findall`）| 五本各 1；**`Toro_ATL_MI` 各 0** |
| 9 | Status bar Shortcut menu 之開啟法無來源 | `shortcut`（不分大小寫）| `spec-index/cache/SYS1_HMI_Status_Bar_…`（99 列）、`…Core_HMI_…`（169 列）之 `Description` 欄 | openpyxl 逐列 `re.search` | 前者 **0**；後者 **1**（§13.2.1，Home Screen 拖曳重排，非開啟動作）|
| 10 | `PU0361`／`PU0170` 皆查得 | 兩個 PU 號 | `forms/Pop Up List HMI R1 (26PI).xlsx` 三分頁 | openpyxl 全格串接後字串比對 | 各 **1 命中**（`Main` 分頁）|
| 11 | `render_tc.py` 重生後逐位元一致 | — | 十二目錄 **283** 份 md（落檔後之終態）| `render_tc.py --verify` | **相符 283／相異 0** |
| 12 | `coverage_b.tsv` ↔ framework VIII.3 | 第 2 組之 `50｜43｜7` | `coverage_b.tsv` vs `framework.md` | 計數比對 | **逐格相符** |

---

## §6　自報錯誤

1. **改名初次一律掃字面，把 framework 兩句史料也改了**（本包內自查自改）——
   狀態行之「重開一次…加第 10 組 `Auxiliary Cameras`」與 VIII.2 註之「**CAM-06 重開改歸**」
   兩句記述的是 CAM-06 當時之決定，改名即為改寫史料。已還原原名並加今名指標
   （`（今名 `Additional Cameras`，R-CAM13(d)）`），**還原後才重算 sha16**。
2. **改名第一輪只改 json 之 `test_set` 欄，漏了 `reasoning` 內文之 18 處引用**（本包內自查自改）——
   第二輪以整檔字面替換補齊，md 再由 `render_tc.py` 重生。
3. **B01b 初稿三處自查自改**：
   (a) `-037` 之 verbatim 取自 CAM-14 時之**截斷輸出**（`“X” soft control is presse`），
   誤判為來源本身缺字；重讀 cache 本全文後確認原文為 `is pressed (PU0361). …`，已改正並重定摘句。
   **該截斷出自我上一輪之 `print(desc[:300])`，非來源缺陷 —— 屬 R-CAM17 意義下之「未實際量測全文」。**
   (b) `-033` 有 `CAN source:` 行而 Procedure 無 `Send CAN` 步（selfcheck 第 5 項命中），
   已把速度前提改為 `Send CAN` 步。
   (c) `-035` verbatim 51 token 逾 §4.3.1 之 50 上限（lint `L`）；刪
   `or ANY OTHER GEAR (manual gearshift)` 後仍破保序子序列 —— 成因為 `gearshift;` 之分號黏字，
   依 CAM-11 前例整體刪去，`(AGSM)` 後不留分號，45 token 通過。
   (d) `-042` 之 Pre-Condition 兩條件並列於同一行（lint `R`），已分行。
4. **`b01b_plan.tsv` 首版之 `pending` 欄以整份 json 掃 `DR-CAM-[a-z]`，把 reasoning 中的**
   **對照提及也算成 PENDING**（`-042` 被誤標 `DR-CAM-s`）。已改為只掃
   `pre_conditions`／`test_procedure`／`expected_result` 之 `PENDING: DR-…` 形制；重生後 B01b PENDING 為 0。
5. **`-024`／`-031` 首版誤填 `Toro(2261)` = 1**，而其前提所用之 byte 173／185 於 Toro 本零命中，
   與 **R-CAM18(b)** 相違（`-031` 之 reasoning 當時甚至已寫「應為 0」而欄位未改）。已改正為 0。
6. **`popup_texts.tsv` 初次補列時重複登了 `PU0170`**（該檔既有列已載其訊息欄）。已移除重複列，
   位置差異改記於 **A-CA34**。
7. **`RULINGS.md` 之 R-CAM13 原條未動** —— 改名以同級錨 `R-CAM13(d)` 落檔（R-TM13／GCB-06）。
   `docs/fw036/RULINGS.sha.tsv` 仍未重生（**GCB-04**，非本線可改）。

---

## §7　未結

| # | 項 | 歸屬 |
|---|---|---|
| 1 | **A-CA34**：`X` 鍵位置 SYS1（upper right）vs `PU0170`（bottom right），疑為彈窗表舊稿 | RD（**RDF 待補**）|
| 2 | B 本其餘 **7 組 160 leaf** 未生成（`Activation and Exit` 已 43/43）| 後續批次 |
| 3 | DR 十五條；A 本 f／g／i／j／k／m／p／q 八條阻交付，B 本 r／s（B01b 無新增）| Pei |
| 4 | A 本交付（Phase 5–7）待 Pei 之准；dry-run v2 已備 244 列口徑 | Pei |
| 5 | GCB-01～GCB-09 九項 | GC 線 |
| 6 | `bench_verify.md` 累計 **22 列**（A 本 11、B01a 3、B01b 8）| Pei 實機 |

---

## §8　落檔清單

```
M  features/camera/RULINGS.md                     R-CAM18／R-CAM13(d)  sha256_16 212ab2f963c9d933
M  features/camera/framework.md                   重開二次             sha256_16 09f7a3844063f299
M  features/camera/DECISIONS.md                   6-51 ～ 6-55         sha256_16 e1dee259be62fc16
M  features/camera/ANOMALIES.md                   A-CA34               sha256_16 fe0924f7ec8fbbb5
M  features/camera/DATA_REQUESTS.md               DR-CAM-g 擴範圍      sha256_16 28b04bf0bd898c3c
M  features/camera/RD_FEEDBACK.md                 改名同步             sha256_16 00e173e9cd540068
M  features/camera/data/layer2_assign.tsv         改名同步             sha256_16 40ad18e96120e1ac
M  features/camera/data/batch02_plan.tsv          改名同步             sha256_16 398bd3864eca8894
M  features/camera/data/b01a_plan.tsv             23 列／N 欄改寫      sha256_16 8d6caa9844c682f0
A  features/camera/data/b01b_plan.tsv             20 列                sha256_16 a44fb0716412ad96
M  features/camera/data/coverage_b.tsv            43 leaf ＋ 7 umbrella sha256_16 317212e8f86b36e5
M  features/camera/data/popup_texts.tsv           PU0361               sha256_16 f79c4a69dea2db61
M  features/camera/data/bench_verify.md           B01b 8 列            sha256_16 e582d2b35bde1a85
M  features/camera/generated/b01a/                +1 TC（-023）、N 欄改寫、INDEX／LINT_REPORT
A  features/camera/generated/b01b/                20 json ＋ 20 md ＋ INDEX ＋ LINT_REPORT
M  features/camera/generated/{batch01b,batch02a,batch02b,batch02c}/   改名 18 筆 ＋ INDEX
M  features/camera/generated/**/*.md              render_tc.py 重生；終態 283 檔 --verify 全相符
A  features/camera/sandbox/a_dryrun/…_dryrun2.xlsx 244 列             sha256 f194046c923fd85f…
A  docs/fw036/lint_reports/RearViewCamera_A__camera_f194046c_20260923.md
```

**未動**：`features/camera/delivered/`、`features/camera/data/coverage.tsv`、
`docs/fw036/handoff/`（本包自身除外）、`features/comfort/`（併行 session）。
