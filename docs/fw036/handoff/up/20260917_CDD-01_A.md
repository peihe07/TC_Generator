# 上繳包 CDD-01_A —— Diagnostics intake 修訂（037 V1 (3) 重導 ＋ 七項裁定落字 ＋ lint 實作）

下放包：`down/20260917_CDD-01_A.md`；前件 `down/20260917_CDD-01.md`、`up/20260917_CDD-01.md`、
`down/20260917_CDD-01_review.md`。執行層 session：沿用 CDD-01 之 session。

**§0 禁區遵守**：git 全程未動（CDD-01 產物已於 `559b1eb` 由 Pei 授權提交，本包產物未提交）；
未動 `down/`、`up/` 既有檔；未動其他 feature 目錄；**未生成任何 TC**；
`ruling_anchor.py` **只改 `RE_ANCHOR` 一處**；`new_feature.py` 之 `feature[:2]` **未修**（列全域待辦）；
條文本體未改，全部以 `(amend)` 追加；037 V1 (2) 之 MANIFEST 列**未刪**，只加 `note`。

**結論**：T1–T7 全部完成。**§6 升級條件四項全未觸發**。
**DR-DIAG-1 由阻斷降為非阻斷**（205 → **3** 列）。**CDD-02 pilot 前置四項全數成立。**

---

## §1　T1 —— 正則放寬與指紋回歸

### 1.1 改動（一處）

`scripts/ruling_anchor.py`：

```diff
-    r"(?P<base>R-[A-Z]{0,3}\d+[A-Za-z]?)"
+    # R-G73（Pei 裁，2026-09-17，下放包 CDD-01_A §2）：上限由三字母放寬為四，
+    # 以收 `R-DIAG`。本常數為解析面與指紋面之唯一來源（R-G43(d)），一處改全面生效；
+    # 放寬前之 18 種既有前綴（…）全在新範圍內，指紋零變動。
+    r"(?P<base>R-[A-Z]{0,4}\d+[A-Za-z]?)"
```

**diff 行數：1 行改、4 行註解增**（淨 +4）。其餘全域腳本未動。

### 1.2 回歸表（§6 條件 1）

`rulings_hash.py` 全 repo 重跑，改前 vs 改後：

| 項 | 改前 | 改後 | 判 |
|---|---:|---:|---|
| 錨點總數 | 821 | 830 | +9 |
| 分類 | group 10／report 12／ruling 790／superseded 9 | group 10／report 12／ruling 799／superseded 9 | ruling +9 |
| 共同 `ruling_id` | — | 788 | — |
| **既有 `sha8` 變動** | — | **0** | **通過** |
| **既有 `body_sha8` 變動** | — | **0** | **通過** |
| 消失 `ruling_id` | — | **0** | 通過 |
| 新增 `ruling_id` | — | **9**：`R-DIAG1`～`R-DIAG9` | 恰為預期 |

**§6 升級條件 1（既有任一 sha8 變動）未觸發。**

解析驗證：

```
### R-DIAG1 — x            -> R-DIAG1          （改前 NO MATCH）
### R-DIAG5(amend) — x     -> R-DIAG5(amend)
### R-G73 — x              -> R-G73
### R-SEC15 — x            -> R-SEC15          （既有，不變）
### R-PMH12 — x            -> R-PMH12          （既有，不變）
### R-VS57 之 (4) — x       -> R-VS57 之 (4)     （子條三式，不變）
```

---

## §2　T2 —— V3 登錄與重導

### 2.1 V3 sha 複驗與差異（§6 條件 2）

| 項 | 值 |
|---|---|
| 檔 | `SWE1_Diagnostics_V1 (3).xlsx` |
| sha16 實測 | `d317a38f249b7bd8` —— 與下放包 §1 **相符** |
| V2 資料列／唯一 ID | 395／394 |
| V3 資料列／唯一 ID | **389／388** |
| 只在 V2 者 | **6**：`SWE1-Diagnostics-389`～`394`（V2 row 397–402）|
| 只在 V3 者 | **0** |
| 共同 ID 之 35 欄格差 | **0** |

刪除之 6 列（皆 `$2812` GPS Signal Data 1）：

| V2 row | SWE1 ID | Source | 標題 |
|---:|---|---|---|
| 397 | `-389` | `SYS-RA-DIAG-167` | GPS - Report the Date - Month using this DID `$2812` |
| 398 | `-390` | `SYS-RA-DIAG-167` | GPS - Handle the negative response … Date - Month |
| 399 | `-391` | `SYS-RA-DIAG-167` | GPS - Handle the Unsupported service requests … Date - Month |
| 400 | `-392` | `SYS-RA-DIAG-168` | GPS - Report the Date - Day using this DID `$2812` |
| 401 | `-393` | `SYS-RA-DIAG-168` | GPS - Handle the negative response … Date - Day |
| 402 | `-394` | `SYS-RA-DIAG-168` | GPS - Handle the Unsupported service requests … Date - Day |

**§6 升級條件 2（差異不止六列）未觸發。**

### 2.2 MANIFEST 兩列

| 動作 | doc_id | 內容 |
|---|---|---|
| 新列 | `diagnostics_037_swra_v1_3` | sha256 `d317a38f…`、版本 `C`、features `diagnostics`、note 記 389 列／166 源與刪列事實 |
| 舊列 `note` 追加 | `diagnostics_037_swra_v1` | `…；superseded by diagnostics_037_swra_v1_3 (Pei 2026-09-17)`（**列未刪**，append-only）|

MANIFEST **235 → 236 行**，與 §4 預期相符。
原檔落 `sources/raw/diagnostics_037_swra_v1_3/`，投遞區 `_intake/Diagnostics/`（皆 gitignore）。

### 2.3 feature.yaml 重導

`paths.a03_report` 與 `sources.a03` 改指 V3；`sources.a03_superseded` 保留 V2 之 doc_id 與 sha16；
`recon` 區塊全部以 V3 實測改寫（見 §7 全表）；`lint.extra_rules` 填入五項。

### 2.4 十一檔 tsv 之 V2 → V3 差異

| 檔 | V2 | V3 | 差 | 差異之來源 |
|---|---:|---:|---:|---|
| `layer3_assign.tsv` | 395 | **389** | −6 | 刪列（全在 `$2812`）|
| `layer2_assign.tsv` | 395 | **389** | −6 | 同上；另 Test Set 重編為 20 |
| `row_kind.tsv` | 395 | **389** | −6 | 同上 |
| `nrc_coverage.tsv` | 230 | **226** | −4 | 刪列中 negative 2 ＋ unsupported 2 |
| `source_resolution.tsv` | 168 | **166** | −2 | `SYS-RA-DIAG-167`／`-168` 不再被引用 |
| `cited_source_attrs.tsv` | 168 | **166** | −2 | 同上 |
| `cfts004_uncited_fr.tsv` | 50 | **52** | **+2** | `-167`／`-168` 回歸（R-DIAG8(amend)）|
| `data_quality.tsv` | 51 | 51 | 0 | 刪除之 6 列無品質問題 |
| `vehicle_model_region.tsv` | 7 | 7 | 0 | 平台表，不隨母體 |

**差異全部落在 `$2812` 相關列**，與下放包 T2(c) 之預期一致。

### 2.5 `framework.md`（DRAFT）

`features/diagnostics/docs/framework.md`，依 canon §4.1 寫出：

- **Part I Layer 1**：一個 Test Group `Diagnostics`，ABBR `DIAG`。
- **Part II Layer 2**：**20 組**，逐組列 DID／L3 數／列數／pos-neg-uns／OoS／產出／PEND，
  合計 389、產出 388、PEND 3。含 §4.1.3 決策測試與「已合併者」之說明。
- **Part III Layer 3**：**42** 列（`<母節> | $XXXX - <title>` 逐字，附各自 037 列數），不入工作簿。
- **Part IV**：未鎖定三項。狀態 `DRAFT`，待 CDD-02 pilot 後 LOCK。

Layer 2 逐組列數（V3）：

| # | Test Set | 列 | # | Test Set | 列 |
|---:|---|---:|---:|---|---:|
| 1 | SXM Module Version | 57 | 11 | Buzzer Control | 40 |
| 2 | SXM Signal Quality | 72 | 12 | Speaker Quadrant Selection | 21 |
| 3 | SXM Subscription | 37 | 13 | **Audio Tone Settings** | **28** |
| 4 | SXM Package Routines | 13 | 14 | Tuner Control | 22 |
| 5 | **GPS Signal Data** | **34** | 15 | Mode Selection | 9 |
| 6 | Camera Brightness | 6 | 16 | In Motion Menu Control | 6 |
| 7 | ECU Internal Settings | 8 | 17 | Video Input | 5 |
| 8 | Media Hub Config | 6 | 18 | AV Signal Detection | 10 |
| 9 | Button Interfaces | 7 | 19 | Audio Measure AC | 3 |
| 10 | Screen Test Pattern | 4（產 3）| 20 | Clear Key Sense PIN | 1 |
| | | | | **合計** | **389** |

---

## §3　T3 —— 七條 amend 之實際號與 sha8

`R-G` 取號現查（R-G23）：`RULINGS_LEDGER.md` 末號 **R-G72**；
全 repo `grep -rn "R-G73\|R-G74"`（排除本包下放檔）**零命中**。
`{live}` = **73** 成立，與下放包 §2 期望值「≥ 73」相符。

落檔後 `rulings_hash.py` 全 repo 重跑：**837 錨點**；與 T1 後（830）相比，
**既有 `sha8` 變動 0**，新增恰 7 個。

> **併行線之影響**：本包收尾時複跑得 **838** —— 多出之 1 為 `R-SEC23`，
> 由併行之 Security session（commit `ce77a40`，SEC-10）於本包作業期間落檔。
> 與本包無涉；本包之 7 條與既有 788 條之 sha8 皆未因之變動。

| ruling_id | sha8 | body_sha8 | body 行 | 落點 |
|---|---|---|---:|---|
| `R-DIAG1` | `b2bee17e` | `f4b22845` | 8 | `features/diagnostics/RULINGS.md` |
| `R-DIAG2` | `ed3616d3` | `f289eaac` | 6 | 同上 |
| `R-DIAG3` | `4492e16d` | `6ad4f9e6` | 7 | 同上 |
| `R-DIAG4` | `114991db` | `ef03d068` | 7 | 同上 |
| `R-DIAG5` | `ed8ed0ce` | `2633a6ae` | 13 | 同上 |
| `R-DIAG6` | `b16e40e2` | `04efc37d` | 6 | 同上 |
| `R-DIAG7` | `15e271dc` | `8ee0ffa5` | 8 | 同上 |
| `R-DIAG8` | `afd49bf2` | `7dbbef22` | 7 | 同上 |
| `R-DIAG9` | `cc7b5200` | `cfdc9208` | 7 | 同上 |
| **`R-DIAG3(amend)`** | `e13b53ce` | `2a93e41a` | 5 | 同上 |
| **`R-DIAG5(amend)`** | `214e6773` | `193116bd` | 16 | 同上 |
| **`R-DIAG7(amend)`** | `9c7fd33f` | `6ddcfc9e` | 8 | 同上 |
| **`R-DIAG8(amend)`** | `3e23ec90` | `a7d66505` | 5 | 同上 |
| **`R-DIAG9(amend)`** | `7612c975` | `3f9cc4a6` | 7 | 同上 |
| **`R-G73`** | `e64d7231` | `acaf3ae9` | 19 | `docs/fw036/RULINGS_LEDGER.md` |
| **`R-G74`** | `832cde95` | `2ac40b3e` | 16 | 同上 |

`docs/fw036/RULINGS.sha.tsv` **33 → 49 列**（新增 16），與 §4 預期「9 ＋ 7」相符；
**經第二次覆寫與修復後為 50 列**（詳 §3.1）。

### 3.1 併發覆寫事件（兩次）與完整性確認

追補 A §一 要求執行層確認 49 列之內容完整性。**已確認：無資料損失。** 事件經過：

| # | 時點 | 事件 | 我方 16 列之狀態 |
|---:|---|---|---|
| 1 | T3 落檔後、`b432893` 提交前 | Security session（SEC-10／SEC-11）整檔覆寫工作區 | **消失**（工作區 0 列 R-DIAG）|
| — | 提交前複查 | 重跑 `rulings_hash.py` 重新 append，保留對方 33 列（含 `R-SEC23`）| 復原，49 列 |
| — | `b432893` | 提交 | **入 git，49 列** |
| 2 | `b432893` 之後 | Security session 再次整檔覆寫工作區（新增 `R-SEC22(a)`）| **工作區再度消失**（34 列，0 列 R-DIAG）|
| — | 追補 A 之確認作業 | 以 HEAD 版為底、併入對方獨有之 `R-SEC22(a)` | 復原，**50 列** |

**關鍵事實**：第二次覆寫發生於 `b432893` **之後**，故
**HEAD 始終保有我方 16 列**（`git show b432893:…` 實測 49 列／R-DIAG 14／R-G73-74 2）；
損失只在工作區。**但若 Security 線以該工作區狀態提交此檔，16 列即從 HEAD 消失** ——
第二次覆寫時工作區已處於該狀態，只是尚未被提交。

**修復方式**：不採「再 append 一次」（那會再丟對方的新列），改為
**以 HEAD 版為底 ＋ 併入工作區獨有列**，兩線皆不丟。

**完整性確認結果**（追補 A §一 所要求者）：

| 檢項 | 結果 |
|---|---|
| 組成 = 原 32 ＋ 對方 2（`R-SEC23`、`R-SEC22(a)`）＋ 我方 16 | **50 = 50，相符** |
| 缺失／多出／重複 | **0／0／0** |
| 我方 16 列之 `sha8`／`body_sha8`／`sha256`／`source` 對現行條文 | **16/16 相符** |
| 對方 2 列對現行條文 | **2/2 相符** |
| 原 32 列 | **全數相符，無變動** |

**→ 無資料損失，不觸發追補 A §一之升級。**

**惟事件性質須更正記載**：追補 A 判定 `[A-DIAG15]` 為「併發覆寫事件」（單次）。
實測為**兩次，且第二次在提交之後** —— 即
**提交並不終止爭用**，只是把損失擋在 HEAD 之外。
在 GC-16 落地前，任一線每次動此檔都須先與 HEAD 比對後合併，不得逕自 append 或覆寫。

---

## §4　T4 —— lint 五項實作與自測

### 4.1 落點與機制

**native 實作於 `scripts/lint036.py`**（現行擴充機制 `FEATURE_CHECKS`，同 Camera 之 `Z`、
Security 之 `SC`／`SS`），未另立獨立腳本。**§6 升級條件 4 未觸發。**

```python
FEATURE_CHECKS: dict[str, list[str]] = {
    "camera": ["Z"], "security": ["SC", "SS"],
    "diagnostics": ["P-DIAG", "U-DIAG", "R1-DIAG", "Z", "RM-DIAG"],
}
```

**一處與下放包不同 —— `VM-DIAG` ≡ `Z`，未另立代號。**
下放包 T4 書 `VM-DIAG`，其判準逐字即 R-CAM2(a)(b)(c)，與既有 `Z` **完全相同**，
且 **R-G74 已將 598／5210 升為全域**。另立代號即為同一判準之第二份實作。
故沿用 `Z`，於 profile §1 與 `feature.yaml` 記明。`Z` 之違規訊息由
「`Camera 兩本一律 0`」改為「`R-CAM2(b)／R-G74：… 一律 0`」——
既有測試 `tests/test_lint036_z.py` 斷言 `"R-CAM2(b)" in detail`，故保留該字串不動。

### 4.2 五項之判準與自測

自測腳本：`features/diagnostics/scripts/selfcheck_lint_diag.py`（每項 5 正例 ＋ 5 反例）。

| 代號 | 判準 | 出處 | 正例（不得命中）| 反例（須命中）| 判 |
|---|---|---|---|---|---|
| `P-DIAG` | `$XXXX` 白名單；`$MESSAGE.Signal$` 式（帶尾 `$`）維持檢查 `P` 原判 | R-DIAG5(a) | 5/5 通過 | 5/5 命中 | **PASS** |
| `U-DIAG` | 每 byte 兩位大寫 hex、單空格；`7F` 後兩 byte 必接 `(<label>)`，label ∈ ISO 五名 ∪ CFTS004 原字 | R-DIAG5(b)／(amend)(d) | 5/5 通過 | 5/5 命中 | **PASS** |
| `R1-DIAG` | Requirement ID 單值 `SWE1-Diagnostics-nnn(-001/-002)`；`-340` 裸號違規 | R-DIAG1(a)／R-DIAG2 | 5/5 通過 | 5/5 命中 | **PASS** |
| `Z`（≡`VM-DIAG`）| 七欄 1／0；598／5210 = 0；五有效欄至少一 `1` | R-CAM2／R-G74 | 5/5 通過 | 5/5 命中 | **PASS** |
| `RM-DIAG` | Remarks 五種定型句（空值放行）| R-DIAG3(amend)／R-DIAG6／R-DIAG7(a)／R-DIAG5(amend) | 5/5 通過 | 5/5 命中 | **PASS** |

**自測抓出兩個實作缺陷，皆已修**（自測之用即在此）：

1. **`U-DIAG` 假陽性 3 例**：原以單一正則貪婪捕捉整串位元組，
   `… 22 28 3F via diagnostic tool` 之 `via`／`diag` 被當成壞 byte 報出。
   改為「起點正則 ＋ 逐 token 走，遇第一個非位元組候選即止」。
   位元組候選定義為 `^(0x)?[0-9A-Fa-f]{1,4}$` —— 須把 `0x22`、`283F`、`3f` 收進來才報得出。
2. **`R1-DIAG` 假陰性 1 例**：`SWE1-Diagnostics-340`（裸號）被接受，
   因 `(?:-00[12])?` 之 `?` 使尾綴可有可無。增 `DIAG_DUP_REQ_IDS = {"SWE1-Diagnostics-340"}`，
   該號裸寫即違 R-DIAG2。

### 4.3 白名單種數與下放包不符

下放包 T4 書「`$XXXX` 白名單 = `layer3_assign.tsv` 之 DID 集合（**27 種**）」。
**定義與括號內之數不一致**：27 為 CDD-01 §3-5 之「037 全文 `$XXXX` token 種數」，
非 `layer3_assign.tsv` 之 DID 集合。依**定義**實測為 **36 種**：

```
$0307 $0309 $030A $0312 $031B $1801 $180C $1820 $1821 $280C $2812 $283F
$2840 $2841 $2842 $2843 $2844 $2845 $2846 $2847 $2848 $2870 $5000 $5001
$5002 $5003 $5004 $5005 $5006 $5008 $5009 $500A $500B $500C $500D $5100
```

實作依定義取 36 種。登 `[A-DIAG14]`。

### 4.4 回歸與煙霧測試

- `tests/test_lint036.py`／`test_lint036_z.py`／`test_lint036_h_quoted.py`／`test_canon_refs.py`：
  **229 passed**（動工前後相同）。
- 全套 `pytest tests/`：**1382 passed／9 failed**。
  該 9 項（`test_gates_tsv` 1、`test_intake_scaffold` 6、`test_single_write_path` 2）
  經 stash 我方兩檔後**仍全數失敗** —— **既存失敗，非本包造成**。
- 煙霧測試：`lint036.py --profile diagnostics` 對 SWC 0708 實跑，**不崩、各項有輸出**。
  該本為 Steering Wheel Controls 之工作簿，其紅數（`R1-DIAG=286`／`Z=434` 等）
  **為「以 A feature 之 profile 檢 B feature 之本」之當然結果，不是校準值**；
  產生之報表已刪除。五項現況皆標「**未校準**」，待 CDD-02 pilot 之實本。

---

## §5　T5 —— `nrc_coverage.tsv` 四類重分類

母體：V3 之 negative 133 ＋ unsupported 93 = **226** 列。

| 分類 | 列數 | 判準（R-DIAG5(amend)(c′)）|
|---|---:|---|
| `RESOLVED_CFTS004` | **5** | CFTS004 原文明載（第一層）|
| `RESOLVED_037` | **20** | 037 Title／Description 明載值（第二層）|
| `RESOLVED_ISO` | **198** | ISO 14229-1 五碼，且 037 已述失效型態（第三層，(d)）|
| `PENDING_DR1` | **3** | 037 未述失效型態 |
| **合計** | **226** | **未分類 0** —— §6 升級條件 3 **未觸發** |

依列型態：

| 型 | CFTS004 | 037 | ISO | PENDING |
|---|---:|---:|---:|---:|
| negative（133）| 4 | 0 | 127 | 2 |
| unsupported（93）| 1 | 20 | 71 | 1 |

**PENDING_DR1 由 185（R-DIAG5(c) 字面下之預估）降至 3**，遠低於下放包 T5 之預期上限。

### 5.1 PENDING_DR1 逐列（3）

| SWE1 ID | 037 row | 型 | Layer 3 | 未解之由 |
|---|---:|---|---|---|
| `SWE1-Diagnostics-156` | 163 | negative | `$0312 - AV Signal Detection` | Description 為標題之逐字複製，**全無失效型態** |
| `SWE1-Diagnostics-157` | 164 | unsupported | `$0312 - AV Signal Detection` | 同上 |
| `SWE1-Diagnostics-237` | 244 | negative | `$2847 - X65 Index ID (Read/Write)` | 只書「an appropriate negative response code (NRC)」；其 CFTS004 來源 `SYS-RA-DIAG-056`（r57）亦只書「shall provide a negative response」 |

**一處保守判斷須請示**：`-157` 之標題為「Handle **unsupported service requests** for …」。
R-DIAG5(amend)(d) 之 `0x11` 觸發語為 037 之「**Service Not Supported**」——
本包**依條文所書之字面**比對，`unsupported service requests` 不視為命中，故該列留 PENDING。
若 Pei 認為二者語意同一，`-157` 可改判 `RESOLVED_ISO`（`0x11`），PENDING 降為 2。

### 5.2 I/O Control SID（R-DIAG5(amend)(e)）

| 項 | 數 |
|---|---:|
| I/O Control 母節之 037 列 | **114** |
| 其中 037 直接明載 `0x2F` 者 | **24**（23 列在 I/O 母節、1 列在 Routine 母節）|
| 同 DID 類推者 | **91** |
| **PENDING** | **0** —— 全數 `RESOLVED_037` |

---

## §6　T6／T7 —— ANOMALIES、DR、RD 回饋

### 6.1 ANOMALIES（`features/diagnostics/ANOMALIES.md`，16 項，新增 `layer` 欄）

分析層 A1～A7 登為 `[A-DIAG06]`～`[A-DIAG12]`（`layer` = `analysis`）；
CDD-01 之五項改標 `layer` = `execution`；本包另新增三項。

| 標記 | layer | 事由 | 狀態 |
|---|---|---|---|
| `[A-DIAG01]` | execution | NRC／SID 適用空白 | **RESOLVED**（R-DIAG5(amend)；殘 3 列轉 DR-DIAG-1）|
| `[A-DIAG02]` | execution | `R-DIAG` 前綴與錨點正則 | **RESOLVED**（R-G73）|
| `[A-DIAG03]` | execution | SYSAD 無 UDS 序列表 | PENDING（不阻斷，DR-DIAG-2）|
| `[A-DIAG04]` | execution | 598／5210 前例衝突 | **RESOLVED**（R-G74）|
| `[A-DIAG05]` | execution | SWC 0708 欄位錯位 10 列 | 記錄（非本 feature 產物）|
| `[A-DIAG06]` | analysis | 投遞表合併列書寫（A1）| RESOLVED |
| `[A-DIAG07]` | analysis | `-057` 歸屬推定未查 L3（A2）| RESOLVED（R-DIAG3(amend)）|
| `[A-DIAG08]` | analysis | 跨母節只點名 `$5000`（A3）| RESOLVED（R-DIAG9(amend)(c)）|
| `[A-DIAG09]` | analysis | 前綴未查正則（A4）| RESOLVED（R-G73）|
| `[A-DIAG10]` | analysis | 「無阻斷性 DR」未先量測（A5）| RESOLVED |
| `[A-DIAG11]` | analysis | 以 V1 (2) 發包（A6）| RESOLVED（R-DIAG8(amend)）|
| `[A-DIAG12]` | analysis | R-DIAG9(c)「43 個」實為 42（A7）| RESOLVED（條文不改，amend 記 42）|
| `[A-DIAG13]` | execution | Toro `Market_Area` Type = `Not Used` | 記錄（R-DIAG7(amend) 明文不影響填法）|
| `[A-DIAG14]` | execution | 白名單「27 種」實為 36 | **RESOLVED**（依定義實作）|
| `[A-DIAG15]` | execution | `RULINGS.sha.tsv` 自稱全域檔而只含 security 32 列 | **PENDING —— 全域待辦**，見 §8 |
| `[A-DIAG16]` | execution | CDD-01 §2.2 欄名誤標、`R-DIAG9` 自算值不符 | **RESOLVED**（改以工具實跑）|

### 6.2 DR 狀態

| DR | 原 | 現 | 說明 |
|---|---|---|---|
| **DR-DIAG-1** | **阻斷**，205 列 NRC ＋ 114 列 I/O SID，Urgency **HIGH** | **非阻斷**，**3 列**，Urgency **MED** | R-DIAG5(amend) 解出 223/226；I/O SID 全解 |
| DR-DIAG-2 | 非阻斷（SYSAD 補件）| **維持** | 不變 |

### 6.3 RD 回饋整包（T7）

`features/diagnostics/docs/rd_feedback_20260917.md`（132 列，中文），六項
**FB-DIAG-a～f** 各附 037 row／CFTS004 row／ObjectID：

| 項 | 內容 | 關鍵定位 |
|---|---|---|
| a | `-340` 重號 | 037 row 347（`SYS-RA-DIAG-264`，CFTS004 r265，obj `4940355`）／row 348（`-188`，r189，`4939684`）|
| b | `-189`～`-192` VM 空白 | 037 row 196–199（`SYS-RA-DIAG-124`／`-125`，CFTS004 r125／r126）；同型有值者 `-187`/`-188`（r124，obj `4939819`）|
| c | `-057` 引用 Out of Scope | 037 row 64（`SYS-RA-DIAG-248`，CFTS004 r249，obj `4940710`）|
| d | CFTS004 **52** 條 FR 未收 | 附 `data/cfts004_uncited_fr.tsv` 52 列；母節 R/W 36／I-O 6／Routine 10 |
| e | negative／unsupported 定型句未載 NRC | 226 列之四類分佈表 ＋ 殘 3 列逐列 |
| f | V1 (3) 刪 GPS Date-Month／Day 六列 | V2 row 397–402；`SYS-RA-DIAG-167`（r168，obj `4939716`）／`-168`（r169，`4939717`）|

**Pei 自行送出**，本包不送。

---

## §7　§4 預期 vs 實測

| 項 | 預期 | 實測 | 判 |
|---|---|---|---|
| 037 資料列／唯一 ID | 389／388 | **389／388**（`-340` ×2 仍在，row 347/348）| 符 |
| 來源唯一數／解析率 | 166／100% | **166／166 = 100%**，未命中 0 | 符 |
| 每來源列數 | 3 列 ×89、2 列 ×45、1 列 ×32 | **同** | 符 |
| Priority | Medium 344／Low 39／High 6 | **同** | 符 |
| 列型態 | positive 163／negative 133／unsupported 93 | **同** | 符 |
| 母節 | R/W 248／I-O 114／Routine 27 | **同** | 符 |
| #5 GPS Signal Data | 34 | **34** | 符 |
| Layer 2 | 20 個（#11 併入 #14 → 28 列）| **20 個**；`Audio Tone Settings` = **28** | 符 |
| Layer 3 被引用 | 42 | **42** | 符 |
| CFTS004 未收 FR | 52 | **52** | 符 |
| VM 空白 | 4 | **4**（`-189`～`-192`）| 符 |
| 拼字 | Caemera 4・senario 3・recevied 1・diagnopstic 1・bightness 2 | **同**（合計 11）| 符 |
| 尾空白 | `-315` 三列 | **3**（`SWE1-Diagnostics-094`／`-095`／`-096`）；另標題 12／描述 21 | 符 |
| `RULINGS.sha.tsv` 新增 | 9 ＋ 7 amend | **16**（33 → 49；起點因併行線之 `R-SEC23` 由 32 變 33。經第二次覆寫修復後 50 列，含對方 `R-SEC22(a)`）| 符 |
| MANIFEST | 235 → 236 | **236** | 符 |

**全表零差異。**

補充（§4 未列）：Region 分佈 `All` **206**／`NAFTA` **182**／空 **1**
（CDD-01 之 V2 為 212／182／1；差之 6 全在 `All`，即刪除之 GPS 列）。

---

## §8　CDD-02 前置核對（下放包 §5-8）

| # | 項 | 判 | 依據 |
|---:|---|---|---|
| 1 | framework DRAFT 完成 | **成立** | `features/diagnostics/docs/framework.md`，Part I–IV 齊備，狀態 `DRAFT` |
| 2 | lint 五項可跑 | **成立** | 自測 5×5 全 PASS；SWC 0708 煙霧測試不崩 |
| 3 | SWC 0708 欄位對照可用 | **成立** | R-DIAG3(amend) 已定 `AF`；34 欄逐欄對照與 `feature.yaml` 樣板完全一致 |
| 4 | `nrc_coverage` 無未分類列 | **成立** | 226 列，未分類 **0** |

**四項皆成立 → 可進 CDD-02 pilot。**

---

## §9　升級條件核對（下放包 §6）

| # | 條件 | 實測 | 觸發 |
|---:|---|---|:-:|
| 1 | 改正則後既有任一 sha8 變動 | 788 個共同 id，`sha8`／`body_sha8` 變動皆 **0** | 否 |
| 2 | V3 與 V1 (2) 之差異不止六列 | 恰 6 列，反向 0，共同 ID 零格差 | 否 |
| 3 | T5 出現無法歸四類之列 | 226 列，未分類 **0** | 否 |
| 4 | `lint036.py` 無 profile 擴充機制 | 有（`FEATURE_CHECKS`），native 實作完成 | 否 |
| 5 | 任何需改 canon 之情形 | 無 —— `RE_ANCHOR` 之改由 R-G73 授權，非 canon | 否 |

**全數未觸發。**

### 9.1 須 Pei 決之兩件（不阻斷 CDD-02）

1. **`[A-DIAG15]` `RULINGS.sha.tsv` 之範圍**：該檔表頭自稱
   「單一全域檔：其重生一律掃描全部 canon 與各 feature 之 RULINGS.md」，
   而實際只含 `features/security/RULINGS.md` **32 列**；全 repo 重生為 **837** 錨點。
   `rulings_hash.py --check` 因而 FAIL —— **本包動工前以動工前之檔實測亦 FAIL**，非本包造成。
   本包依 §4 之預期只 **append 16 列**（33 → 49），未擅自擴為全 repo（那是 +789 列之變更）。
   出口：(甲) 全 repo 重生為 838 列並納入 git；(乙) 明文改其表頭為「逐 feature append」；
   (丙) 維持現狀。**執行層建議 (甲)**，惟其與併行之 security 線有寫入衝突風險，故不自行為之。

   **該風險已實現兩次，非假設**（逐次經過與完整性確認見 §3.1）：
   第一次在提交前、第二次在 `b432893` **提交之後**。
   **兩條線同時寫同一個無人負責重生之檔，後寫者必然覆蓋先寫者** ——
   且因兩邊都只寫自己那幾列，覆寫在 diff 上不顯眼。
   本項不只是「表過期」，是**併發寫入無保護**；且**提交不終止爭用**。
   兩次皆已修復且無資料損失，但修復靠的是人工比對 HEAD，不可持續。
   建議併 (甲) 一併決定其維護歸屬，並在 GC-16 落地前明令
   「動此檔一律先與 HEAD 比對後合併」。
2. **§5.1 之 `-157`**：`unsupported service requests` 是否視同 R-DIAG5(amend)(d) 之
   「Service Not Supported」。視同則 PENDING 3 → 2。

---

## §10　自報錯誤

1. **CDD-01 §2.2 之欄名誤標**：該表標題書「body sha8」，其九個值實為
   `rulings_hash.py` 之 **`sha8`（section_sha）** 欄，非 `body_sha8`。
   八個值與工具相符，`R-DIAG9` 一個不符（`802f0427` vs `cc7b5200`）——
   手算未複製工具對章節尾界（`---` 分隔線與 `RE_NON_RULING` 段）之處置。
   **根因**：工具當時讀不到錨點（四字母前綴），我改以手算「依 docstring 重實作」，
   而重實作未經任何比對即當作等價。**正解應為當場回報「工具無法產出」並升級**，
   本包已以工具實跑取代全部自算值。登 `[A-DIAG16]`。
2. **`V2 → V3` 比對首次寫法過慢**：以 `ws.cell(r,c)` 在 `read_only` 模式逐格取值，
   為 O(n) 每次存取，跑逾 4 分鐘未完而被中止；改 `iter_rows(values_only=True)` 後數秒完成。
   已損耗一次背景任務。
3. **`sysad_uds_tables.md` 之 V2 殘值與過期結論**：該檔非 T2(c) 所列之十一檔，
   我原判「內容仍成立」而未逐項複核，**判斷有誤** —— 複核後發現兩處：
   (a) §1 表之 R/W「037 列數」欄為 **254**（V2 值），V3 為 **248**；
   (b) §1 末之結論「I/O 母節 114 列之 SID 全數 PENDING」係 R-DIAG5(c) 字面下之舊判，
   已被 **R-DIAG5(amend)(e)** 推翻（實為 PENDING 0）；§5 之 NRC 段亦仍書 230 列／205 無值。
   **已就地更正**（R/W → 248；(e) 之處置；226 列四類），並於檔頭加註 CDD-01_A 更新範圍；
   SYSAD 本身之抽取引文（§1–§4）未動。
   **教訓**：「差異只在 `$2812`」對**列數**成立，對**結論**不成立 ——
   amend 改的是判準，不是母體，兩者須分別複核。

---

## §11　產物清單

| 路徑 | 動作 | 入 git |
|---|---|:-:|
| `scripts/ruling_anchor.py` | 改 `RE_ANCHOR` 一處（R-G73）| 是 |
| `scripts/lint036.py` | 新增 Diagnostics 五項（`FEATURE_CHECKS`）＋ `Z` 訊息補 R-G74 | 是 |
| `docs/fw036/RULINGS_LEDGER.md` | 新增 R-G73／R-G74（含成因段）| 是 |
| `docs/fw036/RULINGS.sha.tsv` | append 16 列（33 → 49）；經二次覆寫修復後 **50 列**（§3.1）| 是 |
| `features/diagnostics/RULINGS.md` | 新增五條 amend ＋ 承接段 | 是 |
| `features/diagnostics/ANOMALIES.md` | 16 項（新增 `layer` 欄）| 是 |
| `features/diagnostics/DATA_REQUESTS.md` | DR-DIAG-1 降級 | 是 |
| `features/diagnostics/feature.yaml` | 重導 V3 ＋ recon 全面改寫 | 是 |
| `features/diagnostics/docs/framework.md` | **新增**（DRAFT）| 是 |
| `features/diagnostics/docs/rd_feedback_20260917.md` | **新增** | 是 |
| `features/diagnostics/scripts/selfcheck_lint_diag.py` | **新增** | 是 |
| `features/diagnostics/data/*.tsv`（9 檔）| 全數 V3 重導 | 是 |
| `features/diagnostics/data/sysad_uds_tables.md` | V2 殘值與過期結論更正（§10-3）| 是 |
| `docs/runtime/profiles/FW036_R1L_Diagnostics_Profile.md` | §0–§7 更新 | 是 |
| `sources/MANIFEST.tsv` | 新列 ＋ 舊列 note（235 → 236）| 是 |
| `sources/raw/diagnostics_037_swra_v1_3/` | V3 原檔 | 否（gitignore）|
| `_intake/Diagnostics/SWE1_Diagnostics_V1 (3).xlsx` | 投遞區 | 否（gitignore）|
| `docs/fw036/handoff/up/20260917_CDD-01_A.md` | 本檔 | 是 |

**git 未動。** 建議 commit message：

```
feat(diagnostics): CDD-01_A — 037 V1(3) redirect, seven amends, profile lint
```
