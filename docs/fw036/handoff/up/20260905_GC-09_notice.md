# 具名報告 —— R-G70(e) 之 PROXI 方向與 Pei 2026-09-05 裁定相反

日期：2026-09-05　層級：Tier 1 → Tier 2
**本檔不是 GC-09 之上繳包。** GC-09 由另一執行層 session 施作中（見 §3），
本 session 依 R-G62(b) 未動 GC-09 之任何任務，只出本報告。
Pei 指示（2026-09-05）：「讓另一線做完 GC-09，把 (e) 之方向差異寫成具名報告，由 GC-10 統一更正。」

---

## §1　裁定與現況相反

**Pei 裁定（2026-09-05，本 session 直接請裁所得）**：

> R-G70(e) 之 PROXI 寫法，**`PROXI <Param> = <值>`（SWC 式）為新產出之標準**；
> `PROXI $<Param>$ is set to "<值>"`（VF230 式）為同義舊式，lint 報 WARN（不 FAIL）。

**repo 現況（實測，2026-09-05 21:29:30）**：

| 落點 | 逐字 | 方向 |
|---|---|---|
| `docs/fw036/RULINGS_LEDGER.md:1807-1808`<br>（sha8 `fef0c3d5`）| ``(e) PROXI：`PROXI $<Param>$ is set to "<值>"`（VF230 152 列式）；SWC 之``<br>``    `PROXI <Param> = <值>`（41 列）為同義舊式，新產出不用。`` | **VF230 為標準** |
| `docs/runtime/ASPICE_SWE6_AI_Instruction.md:632-634`<br>（sha8 `934629e3`）| ``(e) **PROXI 參數**：`PROXI $<Param>$ is set to "<值>"`。``<br>``    例：`2. PROXI $Vehicle_Line_Configuration$ is set to "124 (DT)"``<br>``    SWC 交付本之 `PROXI <Param> = <值>`（不加 `$`）為**同義舊式，新產出不用**；`` | **VF230 為標準** |

**二處皆與裁定相反。**

---

## §2　成因：GC-09 所引之出處與審閱條文不符

| 文件 | 所書 | 方向 |
|---|---|---|
| `down/20260905_GC-07.md:56-58`（§二擬文 (d)）| `PROXI $<Param>$ is set to "<值>"`（VF230 152 列式）…SWC 式為同義舊式，**新產出一律採 (d)** | VF230 |
| `down/20260905_GC-07_review.md` §三 R-G70 **(e)** | 同上（VF230 為標準）| VF230 |
| `down/20260905_GC-09.md:3` | 「GC-07 審閱 §一 R-G70（含 **(d) PROXI 採 SWC 式**）」 | SWC |
| `down/20260905_GC-09.md:9`（§一-1-1）| 「條文全文 = GC-07 下放包 §二擬文 ＋ **審閱 §一 之 (d) 修訂**（SWC 式 `PROXI <Param> = <value>`；VF230 `is set to` 式列同義舊式）」 | SWC |
| `down/20260905_GC-09.md:16`（§一-2-2）| 「PROXI 判準 **SWC 式為合規**、`is set to` 式報 WARN」 | SWC |
| `down/20260905_GC-09.md:20`（§一-3）| 「PROXI 85 處**若已 SWC 式則不列**」 | SWC |

**GC-09 內部一致（四處皆 SWC），但其所引之出處不存在**：
`down/20260905_GC-07_review.md` 之 §一 為「Pei 裁示之解讀」，**無 (d) 項**；
R-G70 之 (d)／(e) 在該檔 **§三**，而 §三 (e) 之方向是 VF230。

即 GC-09 引了一個**沒有的修訂**。而 Pei 之裁定與 GC-09 同向（SWC），
故**須改的是條文，不是 GC-09**。

**Pei 原話之佐證**（`down/20260905_GC-07.md:31,35`）：
「都是照我 SWC 怎麼樣寫就這樣寫」、「像 SWC 這份一樣的寫法」。

---

## §3　施作狀態：GC-09 由另一 session 進行中

本 session 讀取時之 mtime（現在時刻 21:28:24）：

| 檔 | mtime | 對應 GC-09 任務 |
|---|---|---|
| `docs/runtime/ASPICE_SWE6_AI_Instruction.md` | 21:25:33 | §一-1-2（IN §8.7.5 已改 v4）|
| `docs/fw036/RULINGS_LEDGER.md` | 21:27:03 | §一-1-1（R-G70 已落於行 1791）|
| `scripts/lint036.py` | 21:28:02 | §一-2-2（v3 分支已移除；`lint036.py:121` 註「已隨 v3 撤銷而移除（R-G70，GC-08）」）|

依 **R-G62(b)**（GC-08 審閱 §二 [DEFAULT]）「同一 repo 同時只得有一個執行層 session 在寫檔」，
本 session **未施作 GC-09 之任何任務**，亦未修改上表三檔。
本檔為新建檔，與該線之 `up/20260905_GC-09.md` 不同名，不生覆寫。

---

## §4　GC-10 須更正之處（清單，未改）

| # | 落點 | 現行 | 應改為 |
|---|---|---|---|
| 1 | 台帳 `R-G70` (e)（`RULINGS_LEDGER.md:1807-1808`）| VF230 為標準 | **SWC 為標準**，VF230 式列同義舊式；依 R-TM13 原句加 `~~ ~~` 保留並附更正註（引 Pei 2026-09-05 裁定）|
| 2 | `IN §8.7.5 (e)`（`ASPICE_SWE6_AI_Instruction.md:632-634`）| 同上，含例句 `PROXI $Vehicle_Line_Configuration$ is set to "124 (DT)"` | 同上；例句改 SWC 式 |
| 3 | `scripts/lint036.py` 之 P 判準 | 現行 `RE_P_PROXI` 為 `\bPROXI\s+(\$[^$]+\$\|[A-Za-z]…)\s*=`，**兩式皆吃**，不分標準與舊式 | SWC 式合規；`is set to` 式 **WARN 不 FAIL**（GC-09 §一-2-2 之字面）|
| 4 | `tests/test_lint036.py` 之 P 相關案例 | 依 3 之方向 | 同步；字面釘入兩式各一（G-N）|

**另二項受牽連，須在方向定案後重跑，否則其數不可用**：

- **GC-09 §一-3 之 `pm29_v4_dryrun_20260905.tsv`**：其轉換規則取 R-G70 (b)(c)，
  且「PROXI 85 處若已 SWC 式則不列」—— 該句與裁定同向，惟若施作時依台帳現行 (e)（VF230 為標準），
  85 處之納入與否會整個翻面。
- **GC-09 §一-4 之 `rtm14_candidates_20260905.tsv`**：其 `P` 與 `P_warn` 兩欄直接決定
  `candidate(Y/N)`，而 `candidate=Y` 是 **Pei 圈選 Revise 本之母體**（GC-09 §二-2）。
  方向錯則候選集錯 —— **VF230 交付本之 152 列在裁定下應報 WARN，在現行條文下報 0**。
  建議：GC-10 更正條文後**重出候選表**，未重出前不得據其圈選。

---

## §5　本報告未做之事

| 項 | 為何未做 |
|---|---|
| 改台帳 R-G70(e)／IN §8.7.5(e)／`lint036.py` | R-G62(b)：另一 session 正在寫這三檔 |
| GC-09 之七件任務 | 同上；本 session 未施作任何一件 |
| 重出 pm_29 dry-run 與 R-TM14 候選表 | 待方向定案（§4 末）|

## §6　請分析層確認

1. 本報告之 §1／§2 是否成立（若分析層另有 Pei 之相反裁示，以其為準，本檔即作廢）。
2. GC-10 是否採 §4 之四項更正清單，並重出 §4 末之二表。
3. `R-G62(b)` 之施行細節：本輪兩條分析線各自下放至同一執行層，
   而**第二條線之下放包（GC-09）已由另一 session 開工**，本 session 只能停手 ——
   建議下放包首行載明「本包指派給哪一個執行層 session」，否則互斥條無從落實。
