# 上繳包 SEC-16 —— CCVR v2.7 工作中版登錄 ＋ ECUCert 路徑衝突 Remarks 註記 → `security_v10`（六本）

執行層，2026-09-17。對應下放包 `down/20260917_SEC-16.md`。**未跑 `--gate`、未動 RULINGS.md**（GC-16 前禁令）。

**結論**：成。MANIFEST 兩列登錄（sha 與下放包所載逐字相同）；`ccvr_wip_20260917_notes.tsv` 落檔；
`A-SEC-16`／`A-SEC-17` 登 `ANOMALIES.md`；v09 → v10 逐格 diff **13 格，全在 `remarks`**
（ECU Cert **7** ＋ Cert Provider **6**，皆在下放包 §6-3 之上限內）；六本重生後
**四本 sha 與 v09 完全相同**（KeyInstall／SAM／SwdlSecureLib／libLogEncrypt），只有 CertProvider 與 ECUCert 變；
六本與 `_all` lint 門檻全 0。§7 兩條升級條件皆不成立（第二條之說明見 §4）。

**三處實測與下放包不符**（§5）：備註新增行數（16 → **11**）與列號（r23~r38 → **r18~r49 之 11 行**）、
`ECU ID CS.165` item 1 之狀態字樣（非 `wait confirm` 式）、`DTC-CS.99` owner 欄之插入位置（wip 與 SYS2_Mapped 兩式並存）。

---

## 一、登錄（§1／§6-1）

| doc_id | 檔 | sha256[:16] | 實測 |
|---|---|---|---|
| `ccvr_v27_wip_20260917` | `2_Architecture/CCVR/…v2.7.xlsx`（112,016 B，mtime 2026-09-17 14:35）| **`4e13ee7efbeaf29b`** | ✅ 與下放包 §1 相同 |
| `ccvr_v27_wip_ref_20260916` | `10_Reviewing/00_TestCase/ASW-R3/Security/REF/…v2.7.xlsx`（112,416 B，mtime 2026-09-16 16:00）| **`bb8f5cae7635ec36`** | ✅ 相同 |

兩列皆 `reference-only`、外部路徑記於 `note`、**不投遞**；原 `ccvr_v27_sys2_mapped` 列未動。
`note` 依實測改寫：wip 之工程備註為 **11 行新增／變更**（非 16），REF 與 wip 之差為
**`DTC-CS.99` owner 欄 6 格 ＋ `Secure Log CS.212` 一格**。

## 二、三方比對（§2／§6-2）

三本：**wip**（9/17）、**REF**（9/16）、**SYS2_Mapped**（分析層本，`b5470cdf4b210aa6`）。

### 2-1　wip ↔ REF（Samuel 於 9/16→9/17 之增修）

| sheet | 差異 | 內容 |
|---|---:|---|
| `Secure Log CS.212` | **1 格** | `r5 c7` —— wip 於該格多一段文字（`…資安相關 system property 被改，如 ro.debuggable、ro.se…`）|
| `DTC-CS.99` | **6 格** | 新增 `owner` 欄（`c5`）：表頭 `owner`；`r3`／`r4` = `seure boot(harman)`、`r12` = `steven(DCL?)`、`r13`／`r14` = `samuel` |
| 其餘 15 sheet | **0** | 逐格相同 |

→ 全簿差異 **7 格**；`ECU ID CS.165` 在兩本間**無差異**（狀態欄與備註在 9/16 本即已存在）。

### 2-2　wip ↔ SYS2_Mapped（原欄位面）

| sheet | wip 列 | mapped 列 | 差異面 |
|---|---:|---:|---|
| `ECU ID CS.165` | 49 | 40 | wip 於 **index 4 插入一無名狀態欄**（表頭空白），把原 `Added` 欄自 c4 推到 c5；備註區自 20 行增為 28 行 |
| `DTC-CS.99` | 15 | 15 | **兩式並存**：wip 於 **c5 追加 `owner`**（有值）；mapped 於 **c0 插入 `Cyber owner`**（**只有表頭、無值**）|
| `Secure Log CS.212` | 15 | 15 | 同 2-1 之一格 |
| 其餘 14 sheet | —— | —— | 原欄位面 **0 格**差異（mapped 之差異全在其追加之 SYS2 對照欄與 4 個新 sheet）|

mapped 獨有 sheet：`Mapping detail`／`SYS2 traceability`／`New test cases`／`Mapping notes`（分析層產物）。

### 2-3　`ECU ID CS.165` 之狀態欄（逐格實測）

| item | 狀態欄（wip c4）| 備註欄（c6）|
|---:|---|---|
| **1** | `out of scope` | `discussing with STLA` |
| 7／8／9 | —（其 `Out scope` 在 **`Added` 欄**，mapped 本即有，**非新增**）| `confirmed with STLA, out of scope` |
| **10／11／13／14** | `out of scope : ⏎ wait confirm (2026/09/15)` | `discussing with STLA, wait for STLA script` |

### 2-4　`ccvr_wip_20260917_notes.tsv`（§6-1 全文）

欄 `row | text`；**11 列**（相對 SYS2_Mapped 之新增／變更；wip 備註區全體為 r18~r49 共 28 行）：

```text
r25  、網路連上 → 重驗已裝憑證 → 驗不過判 CERT_4_INVALID → 落入重試四種狀態之一 → 啟動線上 GSDP 服務重新下載
r30  、cert chain path :
r31  L0 : /odm/etc/cert_store/ecu_chain
r32  L1, L2 : /mnt/vendor/ecuidentity/data/ecu.cacert,  /data/misc/ecuidentity/ecu.cacert (back-up)
r33  L3 : /mnt/vendor/ecuidentity/data/ecu.crt, /data/misc/ecuidentity/ecu.crt  (back-up)
r34  、Check installation status : adb shell od -An -t u1 /mnt/vendor/ecuidentity/state/ecucertstatus
r35  、確認 DUT 跟 版本 (r5 or r6) 後，用 test.ecu.serialnumber 寫入 serial number 。跟 STLA 申請由 GCSP 下載鍊。
r38  、adb pull ：for key blob, certification chain          （mapped 為 `for CSR, certification chain`）
r39  、adb root ：for install , push , setfenforce 0 , setprop   （mapped 為 `for install , push`）
r41  Precautions                                            （mapped 無此節）
r42  、QFil will erase all partiion. Use fastboot only.
```

sha `3dd15816fdeb00c4`。

## 三、`ANOMALIES.md`（§3）

`A-SEC-16`（ECUCert 路徑兩說，含 `od -t x1` vs `od -An -t u1`、主路徑與 back-up 之互換、L0 `ecu_chain`）
與 `A-SEC-17`（item 1／10／11／13／14 之 out-of-scope 標記與 STLA script 依賴）已落檔，
兩條皆註明 **TC 不改**、**037 未載路徑故兩說皆非母體**，並指名 **Samuel** 確認。
`A-SEC-17` 另註明下放包 §3 所記五項同文與實測不符（item 1 之字樣）。

## 四、Remarks 註記與 v10（§4／§6-3／§6-4）

### 4-1　v09 → v10 逐格 diff

| 欄 | 格數 | 內容 |
|---|---:|---|
| `remarks` | **13** | ECU Cert **7 TC** 加 `path per ECU Cert Test Steps PDF 20260826; CCVR v2.7 working note 20260915 states /mnt/vendor/ecuidentity/... as primary — confirm with Samuel (A-SEC-16)`；Cert Provider **6 TC** 加 `L0 chain path per CCVR note 20260915: /odm/etc/cert_store/ecu_chain — confirm (A-SEC-16)` |
| 其餘 16 欄 | **0** | Procedure／ER **未改**（§0 之要求）|

判準：ECU Cert 本之 TC 其 Procedure／ER 含 `/mnt/vendor/oemkeys/ecu/state/ecucertstatus`、
`/data/misc/ecuidentity`、`/odm/etc/cert_store` 任一者；Cert Provider 本之 TC 含 `/odm/etc/cert_store` 者。
規則寫在 `gen_batch1.py`（非逐 TC 硬編），故日後路徑異動會自動跟隨。

### 4-2　六本 sha（§6-3）

| 本 | v09 | **v10** | 判 |
|---|---|---|---|
| CertProvider | `5bdc9d9bf36b151c` | **`c3d31bc8257b2798`** | 變（6 TC 加註）|
| ECUCert | `f20ca37753a5cc5a` | **`173690bf03c5d580`** | 變（7 TC 加註）|
| KeyInstall | `31cd67cbe1eeee75` | `31cd67cbe1eeee75` | **相同** |
| SAM | `50781cfe453d38d8` | `50781cfe453d38d8` | **相同** |
| SwdlSecureLib | `9d9825f0648b5885` | `9d9825f0648b5885` | **相同** |
| libLogEncrypt | `039044328e88a673` | `039044328e88a673` | **相同** |
| `merged/security_v10_all.xlsx` | —— | `407c75ba85f1e8b7` | 六本串接 |
| `merged/security_v10.xlsx`（舊序合併本）| —— | `f32bd124b1f74b59` | 內部審閱用 |

四本未變即「改動只落在該落之處」之硬證。lint（CertProvider／ECUCert／`_all` 重跑）：
`A`~`Y`／`SC`／`SS` **全 0**，`W=4` 仍只在 CertProvider 本。

### 4-3　§6-4　RULINGS 兩檔

| 檔 | HEAD | 現行 | 判 |
|---|---|---|---|
| `features/security/RULINGS.md` | `66fd2825588b6856` | `66fd2825588b6856` | ✅ **相同**（本包未動）|
| `docs/fw036/RULINGS.sha.tsv` | `fa7fbc9779f33961` | `6f46971e7bf6933c` | ⚠️ **已變 —— 非本包所致** |

`RULINGS.sha.tsv` 之逐列歸因（本包未執行 `rulings_hash.py`）：

```text
HEAD 66 列 → 現行 67 列
新增：R-DIAG19        ← 並行 CDD session（CDD-05）之寫入
消失：無
sha8 變動：無
SEC 相關列之增／減／變動：無
```

→ §7 第二條「RULINGS 兩檔 sha 改變」就**執行層之作為**而言不成立；該檔之變動屬 CDD 線，
且依 R-G75／GC-16 之制，該檔於 GC-16 落地前本就由兩線交替重生。特此具名，免對帳時誤判。

## 五、自報錯誤（§6-5）

1. **備註行數與列號**：下放包 §2 記「新增備註 16 行（r23~r38）」。實測 wip 之備註區為 **r18~r49（28 行）**，
   其中相對 SYS2_Mapped **新增／變更 11 行**（r25、r30~r35、r38、r39、r41、r42）。
   已依實測落 11 列；若分析層之 16 是相對**別本**（例如更早之 v2.7 原始本）所數，請指明基準本，我再重數。
2. **item 1 之狀態字樣**：下放包 §2／§3 記 item **1／10／11／13／14** 皆為
   `out of scope : wait confirm (2026/09/15)`；實測 **item 1 只有 `out of scope`**（無日期與 wait-confirm），
   其備註為 `discussing with STLA`。`A-SEC-17` 依實測分開記載。
3. **`DTC-CS.99` owner 欄兩式**：wip 於 `c5` 追加 `owner`（有值）；SYS2_Mapped 於 `c0` 插入
   `Cyber owner`（**只有表頭、全欄無值**）。兩式並存且位置不同；本包只引 wip 之值（DR-SEC-m 之 owner 提示）。
4. **`Secure Log CS.212` 之「空白差異」**：下放包 §2 稱「一格空白差異」；實測為 `r5 c7`
   **wip 多出一整段文字**（非空白差），內容為資安 system property 變更之描述。
5. **`asset_request.md` 之 DR-m 段不存在**：§5 令「`asset_request.md` 之 `X-d` 段加句；DR-m 段加 owner 提示」。
   該檔只列 `X-` 資產（無 DR 段），故 owner 提示改落 **`features/security/DATA_REQUESTS.md` 之 `DR-SEC-m` 列**
   （該列為 CLOSED，追記為「owner 提示」不改其結案狀態）；`X-d` 段之句已加（`asset_request.md` sha `86b62bad8c8e9ab2`）。
6. **未重生 `EXEC_GUIDE.md`**：指南之內容不取 Remarks（只取 Procedure／ER／Pre-Condition），
   本包未改該三欄，故指南逐字不變、未重生（sha 仍 `c9f7b58b98ab2a18`）。

## 六、§7 升級條件

| 條 | 判 |
|---|---|
| diff 落在 `remarks` 以外 | **不成立**：13 格全在 `remarks` |
| RULINGS 兩檔 sha 改變 | **不成立（就本包作為而言）**：`RULINGS.md` 相同；`RULINGS.sha.tsv` 之唯一增列為 CDD 線之 `R-DIAG19`，SEC 列零變動（§4-3 之歸因表）|

## 七、git

未執行 commit／push。建議一支：

```text
feat(security): SEC-16 — register CCVR v2.7 working copy, A-SEC-16/17 path conflict notes, security_v10
```

（`sources/MANIFEST.tsv` 現含並行 CDD session 之未提交列，commit 時請帶 pathspec。）
