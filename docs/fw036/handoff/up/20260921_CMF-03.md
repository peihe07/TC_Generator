# 上繳包 CMF-03 — Comfort HMI Revise 候選本 v2（CMF-02 審閱裁定 ＋ M7 引號 ＋ 內查 DR-46）

- 日期：2026-09-21
- 對應下放包：`down/20260921_CMF-03.md` ＋ `down/20260921_CMF-03_A.md`（同讀，同一 session 執行）
- 性質：裁定落帳、JSON 層 patch、gate 改動、sandbox 候選本 v2、唯讀調查與兩份 plan。
  **未入 `delivered/`、未覆寫 0907 本與 `revise_0918/` 候選本、未 commit、未跑 `--gate`／`--regen`、未手改 `RULINGS.sha.tsv`、未重跑任何 `gen_batch*.py`。**
- 開工前 `git status`（CMF-03 前言之要求）：**CMF-02 之產物已提交**於
  `e84a79c feat(comfort): CMF-02 — JSON patch, 8 siblings, 019-03 BLOCKED, Revise candidate 0918`；
  開工時工作區內唯三之未追蹤檔為分析層自己下放之
  `down/20260918_CMF-02_review.md`、`down/20260921_CMF-03.md`、`down/20260921_CMF-03_A.md`。
  **無未提交之 CMF-02 產物，不代為處理。**
- R-G62(b) 之觀察依據：起點前 `features/comfort/` 之最後他方寫入為上列 commit（09-18），
  距本輪起點 3 日；本輪期間未觀察到他方寫入。**「無他 session 寫檔」不可自證，此處只陳述未觀察到。**

---

## 0. 首段：停點、PENDING 行數、TC 總數、不符數

| 項 | 結果 |
|---|---|
| **§5 停點** | **命中一條（停點二），影響四條 TC**：R-C58 之內查所得訊號名與條文之 `$…$` 名**不同名** → 依 §8.7.5(g) **不代入**，四條之 `PENDING: DR-46` 行**全數維持**，DR-46 **不結案**。逐條之證據與可結案之條件見 §2。停點一（R-C49(amend) 節次集合增減）**未命中**（增減 0）；停點三（M7 後 `test-item-lower-distinct`／`ui-bracket` 轉紅）**未命中**（兩道 gate 皆 PASS）。停點粒度取「逐條」（同 CMF-02） |
| **PENDING 行數** | **4**（下放包之目標：R-C58 查得則 0，否則 4 —— 取後者）。四行為 M6 之四條：`006-02`／`012-06`（`$RECIRC_STAT$`）、`013-02`／`114-02`（`$EBL_Stat$`）。**DR-47 之兩行已依 R-C56 消除**（改為條文逐字 ER），故 `U` 由 6 降為 4 |
| **TC 總數** | **476**（預期 476）＝ CMF-02 之 474 ＋ R-C54 之 1（`003-06` front defrost，`-475`）＋ R-C55 之 1（`005-04` RECIRC，`-476`） |
| **對 0907 本之不符數** | **0**。候選本 v2 自產出檔讀回，與 0907 本逐格比對全部工作表（21,309 格）：未列於本包之格**不符 0 格**；列於本包之 2,863 格讀回值 == 意圖值（不符 0），且每格皆歸因到本包一項 |
| **對 `revise_0918` 之差異** | **1,185 格**，**全數歸因**（未歸因 0 格）。逐格表 `docs/reports/cmf03_vs_0918_cell_delta.tsv` |
| LINT | JSON 層 `lint_tcs.py`：**57/57 gates PASS，476 TCs，0 finding**。候選本層 `lint036.py --profile comfort`：跑完無 Traceback，**J＝0、P＝0、L＝0**（三項目標達成）。哨兵 `LINT_STATUS: OK` 見 `sandbox/revise_0921/lint/LINT_STATUS.txt` |

**需要分析層注意之四件事（細節在各節）**：

1. **R-C58 之第一步是更正一個量測方法，不是重查**：`grep` 把 ISO-8859 編碼之 `.dbc`
   判為 binary，未加 `-a` 時 `-c` **靜默輸出**、exit 1。CMF-01「4 份 DBC 皆查無此二訊號」
   之結論以同一方式取得。加 `-a` 重查後 `$EBL_Stat$` **有**逐字命中 —— 但落在**錯的平台**（§2）。
2. **M7 第 5 問之拼寫正規化使一個既有違規由隱轉顯**：`Sync is on` → `SYNC is on` 之後，
   profile 軸 2 之 `axis-type-reverse-test` 立即命中兩條。**這不是 M7 造成的違規**，
   是關鍵字表以字面比對而語料字面不一致。處置見 §4，登 A-CMF13。
3. **R-C56 使 `H`／`W` 各 +2**：`most closely matches` 之 `matches` 為 lint036 之模糊語。
   這是裁定之直接後果（與既有 `json-275` 對齊），非新缺陷，逐條具名於 §6。
4. **第 4 問之合稱行未套，逐條回報**（§4 M7-Q4）—— `Face/Feet`／`Feet plus Windshield`
   於工作簿與條文皆無該字面之按鍵標籤，加引號即造出一個不存在之 label。

---

## 1. 裁定落帳（文件層）

### 1-1. `RULINGS.md` —— 暫編 ↔ 實編

落帳前實測 `features/comfort/RULINGS.md` 之現行最大號為 **R-C53**（`grep -n "^R-C[0-9]"`）。
**暫編 R-C54～R-C59 全數未被佔用，實編＝暫編**：

| 暫編 | 實編 | 備註 |
|---|---|---|
| R-C54 ～ R-C59 | R-C54 ～ R-C59 | |
| R-C48(amend) | R-C48(amend) | R-C48 原文不動，加旁標（同 R-C42 → R-C42-1 之處置） |
| R-C49(amend) | R-C49(amend) | 同上 |
| R-C50(amend) | R-C50(amend) | 同上 |

- 落於 `## R-C46 ～ R-C53` 之上（本檔新條置於前之既有排列），條文逐字取 review §2（R19-2）；
  R-C48(amend) 之值逐字取 CMF-03_A §1 之表。
- **sha8 未產** —— GC-16 未落地，`RULINGS.sha.tsv` 不手改（CMF-03 前言）。

### 1-2. profile 同步（`docs/runtime/profiles/FW036_R1L_Comfort_Profile.md`，R-TM13：刪除線保留，不刪）

| 節 | 處置 |
|---|---|
| §0 | 新增「generator 凍結（R-C59）」一條：`generated/*.json` 為唯一語料真源；十七支 generator 入口皆加 guard；`test_item.py` 之 `HAND_WRITTEN` 加凍結註記 |
| §3.1.1 | 補「上半 > 50 token 之摘句，與其首字母（R-C50／R-C50(amend)）」框：摘句首字母轉大寫（canon R-4），gate 對首字母一字正規化、其餘逐字；受影響 14 條；負向測試之路徑一併載明 |
| §3.5（N 欄框） | 「leaf 自身節次置首行，其餘升冪，外部文件列於其後」整句劃線 → **R-C49(amend)**：外部／CFTS 行在前，其後 leaf 自身節次，其後其餘升冪；附實測（7 條、節次集合增減 0）與 before／after 表之路徑 |
| §3.9 | 「T／U／V／Y／Z 本輪維持 `1`，待 PROXI 軸值表另裁」劃線 → **R-C48(amend)** 之七欄表（Pei 2026-09-21）；補記「HDCC／DT 三份 PROXI 之七參數全 0 與此一致」與 `vm_axis_plan.tsv` 之指向 |
| §3.2 之 AXIS-VALUES 機讀塊 | 軸 13／EMEA／9 之 `negation-users` 補入 `-475`／`-476`；**軸 2 補入 `-019`／`-035`**（§4 之連帶，只增不刪） |

### 1-3. `ANOMALIES.md`／`DATA_REQUESTS.md`

- **A-CMF8～A-CMF11** 登錄，文字逐字取 review §1。
- **A-CMF12** 登錄（CMF-03_A §1 明令）：037 之 VC 前提寫 `Vehicle variant is DT or HDCC`，
  與 Pei 之裁定（功能僅 Alt-Mi）不一致；三份 Atl-Hi PROXI 之七參數全 `0=Absent` **與 Pei 之裁定一致而與 037 之 VC 前提相反**。
  列 RD feedback 候補；**TC 不因此改動**。
- **A-CMF13** 新登（執行層自陳）：`Sync`／`SYNC` 之拼寫不一致曾使軸 2 之 `axis-type-reverse-test` 靜默（§4）。
- **DR-48** 新登（R-C55 所指）：`2.4`（C3）之 `Recirc can automatically turn on AC` 是否顯示該變化；Medium、RD-1 候選。
- **DR-46** 之條目補入 R-C58 之內查結果（六點，含可結案之條件）；**狀態維持 open**。
- **DR-47** 之條目補入 R-C56 之處置（PENDING → 條文逐字 ER ＋ Remarks）；**狀態維持 open**。
- **DR-49（`Climate` 畫面之 HMI entry path）依 CMF-03 §1 先不登**，待 Pei 對 review §3-3 表態。
  其文字已於 CMF-02 §5-2 備妥，本輪未動。
- `docs/reports/proxi_axis_values.tsv` 之表頭補記 R-C48(amend) 之出處與「全 0 與裁定一致」一句；
  **表內無任何值被本輪改動**。

---

## 2. R-C58 —— 內查 DR-46（**停點命中**，四條維持 PENDING）

**查得與查無，逐字**：

| 對象 | 結果 |
|---|---|
| `$EBL_Stat$` | **逐字命中 1 處**：`forms/PDT27_E2A_R1_FDCAN8.dbc`<br>`BO_ 1436 BCM_FD_27: 32 SGW`／` SG_ EBL_Stat : 87\|2@0+ (1,0) [0\|2] "" ETM,LTM`<br>`CM_ SG_ 1436 EBL_Stat "Rear Defrost Status";`<br>`VAL_ 1436 EBL_Stat 0 "OFF" 1 "ON" 2 "Blink" 3 "SNA";` |
| `$RECIRC_STAT$` | **四份 DBC 逐字零命中**。三份載 `STATUS_CLIMATE2.HVACRecirc_Sts`（`VAL_ 1482 HVACRecirc_Sts 0 "OFF" 1 "ON" 2 "BLINK" 3 "SNA";`），**名不同** |
| CFTS043 tree view | 二名皆為**條文自己的 `$…$` 名**（`Basic Report` 130 處命中，如 r1412「When the HU receives the `$RECIRC_STAT$` = [not available/Blink] signal …」、r1966「… shall grey-out the RECIRC button …」）。tree view 另載 `$HVACRecirc_Req$`（r392／395/398/401）—— 該名於 DBC **有**逐字命中，即 `TELEMATIC_CLIMATE_SETUP.HVACRecirc_Req`。**故「`$…$` 名 = DBC 訊號名」在本 spec 內成立與不成立者並存** |

**旁證（超出下放包所指之查詢面，故只作陳述不作代入之依據）**：
`forms/Logical Identifiers and CAN Mapping v1_78.xlsx`（sheet `CAN Mapping`）載明此二者為
**LID（邏輯識別碼）**而非 CAN 訊號名，並給出逐平台對映：

| LID | Powernet | CUSW | **Atlantis（MID）** | **Atlantis High** |
|---|---|---|---|---|
| `RECIRC_STAT`（r1566） | `HVAC_A1.RECIRC_STAT` | `HVAC_A1.RECIRC_STATSts` | `STATUS_CLIMATE2.HVACRecirc_Sts` | `STATUS_CLIMATE2.HVACRecirc_Sts` |
| `EBL_Stat`（r587） | `HVAC_A1.EBL_Stat` | `HVAC_A1.EBL_StatSts` | `STATUS_CLIMATE2.HVACRearDef_Sts` | `BODY_CNTRL3.EBL_Stat`（DCSD／CAN-B）＋`BCM_FD_27.EBL_Stat`（ETM／FD CAN8） |

四者之值域皆 `0=Off 1=On 2=Blink 3=SNA`。

**停點之判定（逐條）**：

- `006-02`／`012-06`（`$RECIRC_STAT$`）—— 查得之訊號名 `HVACRecirc_Sts` ≠ 條文之 `$RECIRC_STAT$`。
  **停點二直接命中，不代入。**
- `013-02`／`114-02`（`$EBL_Stat$`）—— 名**逐字相同**，惟該唯一之 DBC 命中
  （`BCM_FD_27.EBL_Stat`，`PDT27_E2A_R1_FDCAN8.dbc`）**只屬 Atlantis High**，
  而 Pei 2026-09-21（CMF-03_A §1）裁定本功能**只有 Alt-Mi 有**（Promaster／Toro／Fastback）。
  依對映表，受測之三台其訊號為 `STATUS_CLIMATE2.HVACRearDef_Sts` —— 與 `$EBL_Stat$` **亦不同名**。
  **代入 `BCM_FD_27.EBL_Stat` 會寫出一個受測車上不存在之 CAN frame**，故同一停點，不代入。

**分析層要注意此處之判斷**：停點條文只寫「訊號名與 `$…$` 名不同名」，`$EBL_Stat$` 之字面是相同的。
我把「平台不對」歸入同一停點，理由是該命中之平台正是 Pei 當日裁定功能**不存在**之平台 ——
若分析層認為應取字面而代入，請明示，改回只需一行。

**結案所需（不必再查）**：只要裁定本交付之平台欄位為 **Atlantis（MID）** 抑或 **Atlantis High**，
上表即足以寫出四條之 `Send CAN: <MESSAGE>.<Signal> = 2 (Blink)` 行。
（Atl-Hi 之 `EBL_Stat` 尚須再擇 ETM／FD CAN8 抑或 DCSD／CAN-B 之一。）

---

## 3. JSON 層（`scripts/cmf03_apply.py`，一次性、逐處斷言原值）

change log：`docs/reports/cmf03_json_change_log.tsv`（189 列）。
**驗證可重現**：以 `git checkout HEAD -- generated` 還原後重跑腳本，所得 129 個 JSON
與本輪之產物**逐位元組相同**，候選本 sha256 亦相同（`98ddf21a…`）。
第二次執行於第一處斷言即 `SystemExit`（實測 exit 1）。

| 項 | 落實 | 實量 |
|---|---|---|
| R-C54 | `003-06` 補產 front defrost sibling `-475`。**原條無須瘦身** —— 2.3 之 `Auto is mutually exclusive with the four airflow modes and front defrost` 明示 front defrost 與四個 airflow mode 為並列之兩物，故同 leaf 之「另一 airflow mode」一條不含本條之觸發 | 1 條 |
| R-C55 | `005-04` 補產 RECIRC sibling `-476`；ER 取 037 leaf `SWE1-HVAC-005-04` 逐字（`The system shall defrost and RECIRC can automatically turn on A/C **(change not shown)**`）所對應之可觀察句，措辭與原條第 3 步一致；Remarks 自足之 ambiguity 句並登 `AMBIGUITY_REMARKS` | 1 條 |
| R-C55 slim | `json-139` 移出第 3 步（其後仍餘兩步，守 §10.5）；下半重算；**tc_title 由 `Defrost and Recirc turn A/C on without showing it` 改為 `Defrost turns A/C on without showing it`**（標題留 Recirc 而內容已無之，即為錯）；**補 `split_flag`／`split_reason`**（`req-id-unique` 要求同 req 之每列皆宣告） | 1 條 × 5 欄 |
| R-C56 | `-467`／`-468` 之 `PENDING: DR-47` 兩行改為與 `json-275` 同句之 ER；三條（`-467`／`-468`／`-275`）各加一句自足之 ambiguity Remarks，登錄簿之片段各異（68 §2） | 2 行 ＋ 3 條 Remarks |
| R-C50(amend) | 14 條摘句首字母轉大寫（逐條斷言其原值仍以小寫起首，不以「掃到就改」替代斷言）；gate `lint_tcs.upper_matches()` 對首字母一字正規化 | 14 條 |
| M7 | 見 §4 | L／M 行改 148、下半段改 46 |
| 軸 2 PC | 見 §4 之連帶 | 2 條 × 2 欄 |
| R-C58 | **不改 JSON**（停點命中） | 0 |

**新 sibling 之細節**：

- TC ID 接 `-475` 起（R-C46），依 req_id 遞增：`-475` = `SWE1-HVAC-003-06`、`-476` = `SWE1-HVAC-005-04`。
- 下半與同 req 既有各條兩兩相異（`test-item-lower-distinct` 通過）。
- PC、`specification_reference`、Priority、`design_method`、`emea_ics_review` 皆沿用同 leaf 既有條。
- `split_reason` 於原文之後附本輪之理由段，**不引 tc_id**（profile §3.6：tc_id 會位移，
  故以「同 leaf 之『另一 airflow mode』一條」指稱；`no-tcid-in-prose` gate 所驗）。

---

## 4. M7 引號（`label_quote_plan.tsv` ＋ review §2-1 七問）

**套用面**：plan 只掃候選本之 L＋M 兩欄，而 `test_item` 下半是 L／M 之複述 ——
不同步即自相矛盾。故本輪**同時套於下半**（以首字母大小寫正規化查表，命中後還原原大小寫），
逐行紀錄於 `docs/reports/cmf03_m7_apply.tsv`。

| 問 | 裁定 | 落實 |
|---|---|---|
| 1 | `The X airflow mode is active` → state，不加 | plan 已判 state，91 列不動 ✓ |
| 2 | `read the RECIRC state` 指按鈕 → 統一為 `read the "RECIRC" button state` | 4 種句型逐行改寫（含既已引號之 `read the "RECIRC" state`） |
| 3 | `Set RECIRC to …`／`Change RECIRC` → label | plan 之 label 類，照套 |
| 4 | `Set the airflow mode to Face` → `Select the "Face" airflow mode` | **只套單一模式名**（`Face`／`Feet`）。**`Face/Feet`、`Feet plus Windshield` 兩行未套** —— 二者為兩個模式之合稱，工作簿與條文皆無該字面之按鍵標籤，加引號即造出一個不存在之 label。**請裁** |
| 5 | `Sync`／`SYNC` 取 SR24 用於按鍵標籤者，全本一致 | **取 `SYNC`**，見下之量測 |
| 6 | `AUTO pop up` → verbatim，不動 | plan 已判 verbatim ✓ |
| 7 | Tri-Mode `MODE` 等 → 本輪不擴 | 未擴 ✓ |

**第 5 問之量測**（SR24 export `SYS1_HMI_Comfort_HMI_Logic_and_Flow_R1_SR24_Post_3A_CR24879_(September_25_2023).xlsx`，全工作表逐格）：

| 拼寫 | 次數 | 代表句 |
|---|---|---|
| `SYNC` | **20** | `C12.) SYNC has on/ off state that is indicated on climate screen (highlight button if SYNC)`（同型另見 CR7、ICE10、C5.1、ICE5.1） |
| `Sync` | 10 | `Sync is not shown for single zone climate configurations`、`turns on Sync`、`Sync button`（BCW1，17.x widget 清單） |
| `sync` | 7 | `sync'd`（內文動詞）、`sync button`（14.x／17.x widget 清單，與同句之 `auto button` 同為小寫散文） |

**取 `SYNC` 之理由**：`highlight button if SYNC` 之句型即其餘按鍵（`AUTO`／`RECIRC`／`MAX A/C`）
之同一句型，且與工作簿既有之 `Turn "SYNC" on` 一致；R-C2 所舉之例（`AUTO`、`RECIRC`）亦皆全大寫。
`Sync button`／`sync button` 出現於 widget 內容清單之散文中，同句之 `auto button` 亦為小寫，
故判其為描述語而非標籤。全本之孤立 token `Sync` 一律正規化為 `SYNC`（含 state 句，第 5 問之「全本一致」）。

**改後重掃（L＋M，row 10–485，同一支掃描器）**：

| | `revise_0918`（before） | `revise_0921`（after） |
|---|---|---|
| 總命中 | 463 | 468 |
| 已引號 | 124 | **280** |
| 未引號 | 339 | **188** |
| 未引號之 plan 分類 —— **label** | **162** | **0** |
| 未引號之 plan 分類 —— state | 169 | 157 |
| 未引號之 plan 分類 —— verbatim | 8 | 6 |
| 未引號 —— 新行（不在 plan 內） | 0 | 25 |

- label 類**歸零**。state 由 169 降為 157 —— 差之 12 全數來自第 2 問與第 4 問之裁定
  （plan 判 state，review 判該處指涉元件）。
- verbatim 8 → 6：兩處為 `-467`／`-468` 之 `PENDING: DR-47` 行，已由 R-C56 改寫而消失。
- 「新行 25」為本輪新產之兩條 sibling、R-C56 之兩行 ER 與第 2／4 問改寫後之行。

### 4-1. 連帶 —— 軸 2 之 PC（**請核**）

`Sync is on` → `SYNC is on` 之後，profile 之 `function-axis-reverse-test` 軸 2 區塊
（其 `removed-interface-keywords` 列 `SYNC is on | SYNC is off` 而**未列 `Sync is on`**）
開始命中 `NR1L-ComfortHMI-019`（`024-02`，MAX DEF 之九步中第 8 步讀 SYNC）與
`NR1L-ComfortHMI-035`（`002-04`，SYNC 抑制乘客側滑桿 pop-up）——
**兩條以 SYNC 指示為可觀察量而未宣告軸 2 之值，這在 CMF-02 及其前皆已成立**，
只是被拼寫差異擋在 gate 之外。

**處置**：兩條各補一行工作簿既有之同一句 PC（15 條在用）
`The vehicle is not a single zone climate configuration, for which Sync is not shown (2.11)`，
並同步補 `2.11` 於其 `specification_reference`（R-C49：PC 出處節次由 N 欄承載）。
profile 軸 2 之 `negation-users` 一併補此二條。

**未擴充關鍵字表** —— 擴之即以另一份清單承擔一致性，與 R-C43 之理由同；
語料之字面一致才是治本。登 **A-CMF13**。

**這是本輪唯一一處「下放包未點名而我改了 TC 內容」之處**，請核。
若分析層判不應補 PC，改法為回退這兩條之 PC 與 N 欄，代價是 `axis-type-reverse-test` 轉紅。

---

## 5. R-C49(amend)、R-C57、R-C59、R-C50(amend) 之 gate／腳本改動

### 5-1. R-C49(amend)（`write_back._compact_refs()`）

- 先量後改：**受影響 7 條**（全為帶外部文件／CFTS 行者：`-382`、`-430`～`-434`、`-471`），
  **節次集合增減 0** → 停點未命中。
- before／after 全量對照：`docs/reports/cmf03_rc49_refs_before_after.tsv`。
- 候選本 N 欄之改動格數 452（與 CMF-02 同 —— 該 452 格為「0907 本之『、』串接 → 一節一行」，
  家族排序只在其中 7 條之行序上再動一次）。

### 5-2. R-C57（`scripts/lint036.py`）

- 新增 `FEATURE_EXEMPT_PENDING_LINE: dict[str, list[str]] = {"comfort": ["P"]}` 與
  `RE_PENDING_LINE`，於 `check_row()` 之三處 `P` 迴圈
  （`check_signal_line`、`check_vehicle_property_line`、`check_proxi_line`）加同一個 `p_skip()`。
- **施檢面縮到 feature**，不動全域；他 feature 之 PENDING 行是否同此形態未經量測。
- 該行仍受 `T`（說明之語言）與 `U`（佔位之可見性）覆蓋，**不會變成沉默**。
- 實測：0907 本基線之 `P` 仍為 **0**（基線無 PENDING 行，故本改動不動基線）；候選本 `P` 由 4 → **0**。

### 5-3. R-C50(amend) 之 gate 與**負向測試**

- `lint_tcs.upper_matches(upper, body)`：對**首字母一字**做大小寫正規化，其餘逐字。
- 負向測試 `tests/test_comfort_upper_verbatim.py`（7 項，全通過）：
  - 正向 3 項（逐字命中／小寫起首之摘句／大寫起首之摘句）；
  - **負向 4 項** —— `A POP-UP …`（首字母以外之大小寫差異）、`A pop-Up …`、
    `A popup …`（字詞差異）、非連續之摘句、空字串，**皆須仍 FAIL**。
- **一處自陳**：該測試檔初版以裸名 `from lint_tcs import …` 匯入，
  使 `sys.modules["lint_tcs"]` 被 comfort 版佔住並交給 `tests/test_lint_tcs.py`（media 版），
  令全庫測試由 10 failed 變成 77 failed。已改為 `importlib` 唯一名載入
  （`tests/test_home_lint_tcs.py` 之註解早已記此陷阱，我先犯了才回去讀）。

### 5-4. R-C59（generator 凍結）

- **17 支**入口加 guard（`gen_batch2`～`gen_batch17` 共 16 支 ＋ `gen_pilot`）：
  無 `--i-know-this-overwrites-cmf` 即 `SystemExit`，訊息指向 R-C59。逐支實測 exit 1、未寫任何檔。
- `test_item.py` 之 `HAND_WRITTEN` 加註「已凍結，勿據此重產」，並明寫其值為 generator 時代之舊值
  （M2 之 `103-01`／`103-02` 已由 `cmf02_apply.py` 改寫於 JSON，本表未同步），
  現行值一律以 `generated/*.json` 為準。

---

## 6. 候選本 v2（`scripts/cmf03_revise.py`）

- 產出：`features/comfort/sandbox/revise_0921/…_SWQT_ComfortHMI_20260921.xlsx`
  - sha256 `98ddf21a616b5f45e0b0cb0d2b163650d1b19090b8b48260cc0032148a31a47f`
- 母本：`delivered/…_ComfortHMI_20260907.xlsx`（先驗 sha `3550d16d…`）之**複本**
  —— **不是疊在 `revise_0918` 上**（CMF-03 §3）。腳本另先驗 `revise_0918` 之 sha
  （`28a0998c…`）以確保對帳之對象未被動過。

| invariant（ABORT 級者標 ★） | 結果 |
|---|---|
| ★ zip member 數 | 57 ＝ 57 |
| 差異僅目標 sheet | `xl/worksheets/sheet6.xml` 一個 |
| ★ DV sqref（classic ＋ x14） | 逐字相同：`P10:Q601`、`AF10:AF601`、`T10:Z601`、`R10:R601` |
| B 欄公式延伸至新列 | row 10–601 逐列原樣，涵蓋新列 476–485 |
| 新列在 DV 內 | 476–485 皆在 |
| **對 0907 本逐格對帳** | **不符 0 格**／比對 21,309 格（全部工作表）；變更 **2,863 格**，全數歸因 |
| **對 `revise_0918` 逐格對帳** | 差異 **1,185 格**，**未歸因 0 格** |

**變更格之歸因（對 0907 本，2,863 格）**：
R-C48(amend) 1,864、R-C49(amend) 452、§3 sibling 233、M7 145、M9-compound 67、
M9-Note 22、R-C50 12、R-C50+R-C50(amend) 14、R-C51 10、M2 6、M6 6、M7+M9-compound 6、
R-C52 4、M8 3、wb-006 3、R-C55 slim 3、軸 2 PC 2、R-C56 1。

**差異格之歸因（對 `revise_0918`，1,185 格）**：
R-C48(amend) 932、M7 145、§3 sibling 72、R-C50+R-C50(amend) 14、R-C49(amend) 8、
M7+M9-compound 6、R-C55 slim 3、M7+M9-Note 2、軸 2 PC 2、R-C56 1。

- **列**：既有 466 列之列位、F、E 不變；old→new 無。row 116 = `019-03` BLOCKED 列（F = -466）。
  新列 row 476–485（F = -467…-476），O＝`NEW`、AA＝`PeiPYHsu`、S＝`NA`、E 空，
  C 取 0907 本同 req 既有列之 C。
- **車型欄（R-C48(amend)）**：T=0 U=0 V=1 W=0 X=0 Y=1 Z=1，**row 10–485 全列**（含 row 116 之 BLOCKED 列與十列新 sibling）。
  這是 1,864 格之來源（476 列 × 7 欄 ＝ 3,332 格中，與 0907 本不同者 1,864）。
- **列序**：新列在尾端（R-C46），D 欄自 row 476 起不遞增 —— 裁定之結果，
  `write_back.py` 之 `row-order-by-reqid` assertion 不適用於本候選本。
- **呈現面（Tier 3，未處理）**：新列 476–485 沿用範本空白列之樣式（列高 14），
  與資料列（列高 168）不同，Excel 中會看起來擠。程式不改樣式與列高（26 §2 方向 3 已裁），請 Pei 於 Excel 判斷。
  CMF-02_review §3 末段所列之另三件（`Test Case Framework` 之 SmartArt 舊名、
  `J5` 與 Cover 日期不一致）本輪同樣**未動**。

---

## 7. 只出 plan（不改 TC）

### 7-1. `docs/reports/nav_rewrite_plan.tsv`（476 列，一條 TC 一列）

欄：`tc_id`／`req_id`／`nav_steps`／`destination`／`entry_fixed_today`／
`enter_comfort_reaches`／`missing_hop`／`hop_label_in_comfort_spec`／`hop_label_evidence`／`er_shift_if_rewritten`。

**目的畫面分類**（掃描面為 `test_procedure` ＋ `pre_conditions`，**扣除三個排除式 PC**
—— 其 `the comfort section is removed from the head unit` 一句會使 374 條全部誤判為 Comfort category）：

| 分類 | 條數 |
|---|---|
| Climate screen | 184 |
| Rear climate screen | 58 |
| Comfort category（Climate main／main category control）| 40 |
| Home widget（Comfort widget／Climate widget）| 29 |
| Seats tab | 8 |
| Lower screen | 7 |
| Controls screen | 5 |
| Head unit menu | 3 |
| Tri-Mode Climate screen | 3 |
| 無導航標的 | 139 |

- **今已具固定入口（§5.8(a) 之閉合清單）者：1 條**（其餘 475 條即 lint036 `X` 之來源）。
- **`ENTER_COMFORT` 可達**：298 條可由該 2 hops 到達 Comfort app；
  39 條**不經** Comfort app（Home widget／Lower screen／Head unit menu）；139 條無導航標的。
- **之後尚缺之 hop 與其標籤 —— 不猜**：

| 目的 | 尚缺之 hop | Comfort HMI 條文有無逐字標籤 |
|---|---|---|
| Climate screen | Comfort app → climate screen | **無**。129 節全文掃描：`2.3`／`2.7`／`2.10`／`2.12` 只寫 `main category control`／`Category button`，無引號標籤 |
| Comfort category | Comfort app → Climate main | **無**。`2.12.2`／`16.12.1` 之 `Climate main category control` 為描述語；`10.8` 之 `The Comfort main Menu Bar icon` 亦無 label |
| Rear climate screen | Comfort app → rear climate screen | **無**。`7.6` 只寫 `REAR CLIMATE OFF screen`／`REAR OFF button` |
| Home widget／Seats tab | Home Screen → Comfort widget（不經 Comfort app） | **有（部分）**：`17.1`／`18.1`「The Comfort widget will have two screens: **Comfort** and **Seats**」為逐字之畫面名；**惟 widget 本身之放置與開啟不在本 spec** |

  合計：`none` 290 條、`verbatim` 37 條、`n/a` 149 條。

- **「改寫後 ER 位移」之條數**：**336**（判準：目的畫面非「無導航標的」且今無固定入口 ——
  插入 hop 必使其後所有步驟與 ER 行重新編號）。
  **另一個較窄之口徑**：今已明寫 `Open …`／`Move to …` 等導航步驟者 **115 條**
  （其餘以 PC 宣告入口）。CMF-02_review §3-3 之「約 85 條」與此二數皆不同，
  口徑之差已具名於此，請以需要之口徑取數。

### 7-2. `docs/reports/vm_axis_plan.tsv`（1,462 列 ＋ 1 列彙總）

欄：`tc_id`／`req_id`／`axis`／`proxi_parameter`／`pc_line_verbatim`／
`proxi_Promaster`／`proxi_Toro`／`proxi_Fastback`／`literal_match`。

- 三台之值逐字取自 `proxi_axis_values.tsv`（CMF-02 §5-1，唯讀）；
  **`Dec` 與 `PROXI Read` 不一致者兩值並列**（`Dec=2 (Automatic) / PROXI Read=3` 之形）。
  實測不一致者：Promaster 之 `Climate_Type`／`Rear_Climate`／`Automatic_Recirculation`、
  Fastback 之 `Climate_Panel_Type`（`Dec=0 (Only blower knob) / PROXI Read=7`）等。
- **不作「PROXI 值 → 該 TC 適用與否」之判定**，只並列。`literal_match` 欄記該台之 PROXI label
  是否**字面**出現於該 PC 行。

**軸之命中**（PC 逐字，不推論）：knob 型態 456、ICS 461、EMEA ICS 426、Rear HVAC 60、
ATC／MTC 52、Tri-Mode 6、**Auto Recirc 0、AUTO ECO 0**。
—— 後二者之 PC **從未逐字載明該軸**（`AUTO ECO` 只出現於 procedure／ER），只陳述，不下結論。

**特別列（彙總列）**：帶排除式 PC `does not have 3 knob HVAC controls with ICS`（2.14）者
**454 條**；與之並列者為 Toro 之 `Climate_Panel_Type Dec=6 (3 knobs)` ＋
`Integrated_Climate_Touchscreen Dec=1 (Present)`。**只陳述，不下結論。**

**依字面之分類（476 條）**：

| 類 | 條數 |
|---|---|
| 依字面三台全適用 | **0** |
| 依字面部分適用 | 5 |
| 無從判定（有軸但無字面相符） | 457 |
| 無從判定（PC 未載任一配置軸） | 14 |

- 候選本 v2 之 **V／Y／Z 一律先填 `1`**（CMF-03_A §2 末句），細分待 Pei 看 plan 後裁。

---

## 8. 自檢

- **JSON 層**：`lint_tcs.py` **57/57 PASS，476 TCs，0 finding**，輸出
  `sandbox/revise_0921/lint/json_layer_lint_tcs.txt`。
- **候選本層**：`lint036.py --profile comfort`（report mode）。0907 本基線、CMF-02 候選本、本輪候選本：

| 檢查 | 0907 基線 | `revise_0918` | **`revise_0921`** | 差之成因 |
|---|---|---|---|---|
| **L** | 26 | 0 | **0** | R-C50 已生效（目標 L＝0 ✓）|
| **J**（首字小寫） | 0 | 14 | **0** | **R-C50(amend)** 之首字母大寫（目標 J＝0 ✓）|
| **P** | 0 | 4 | **0** | **R-C57** 之 PENDING 行豁免（目標 P＝0 ✓）|
| U（PENDING 佔位） | 0 | 6 | **4** | DR-47 之 2 行由 R-C56 消除；DR-46 之 4 行維持（§2 停點）|
| **H** | 4 | 4 | **6** | **R-C56 之直接後果** —— `-467`／`-468` 之新 ER 含 `matches`（既有 4 處：`-035`／`-146`／`-301`／`-440`，其中 `-146` 即與 `json-275` 同句者）|
| **W** | 3 | 3 | **5** | 同上（`-467`／`-468`）|
| M（空欄） | 8 | 10 | 10 | `019-03` BLOCKED 列之 L／M 空（與既有 4 條同形）|
| R（PC 並列） | 1450 | 1476 | 1486 | 新列沿用既有 PC 寫法 ＋ 軸 2 之兩行 PC |
| X（導航無固定入口） | 319 | 335 | 337 | 新列沿用既有導航寫法；R-C53 本輪仍不改導航 |
| I-cross | 433 | 441 | 443 | 新列之「窗未完整宣告」（既有列同型）|
| A–G／I／I-sibling／K／N／Q／T／V／Y | 0 | 0 | **0** | — |

- `scripts/canon_refs.py --report --top 1000 --waiver`：**本包新增／改動之檔案 unresolved／ambiguous 0 筆。**
  歸屬分析依 R-G62 之 10-2 以 `git worktree add`（**未用 `git stash`**）：
  - 乾淨 HEAD：unresolved **34**／ambiguous **178**（＝ CMF-02 所報之數）。
  - HEAD ＋ 三份下放包（`CMF-02_review`、`CMF-03`、`CMF-03_A`）：unresolved **35**／ambiguous 178。
  - 工作區現值：**35／178** —— 與上一列相同，故**增之 1 筆全數來自下放包，非本包產物**。
  - 該筆為 `down/20260918_CMF-02_review.md:26` 之 **`R-G78`**（ruling 未解析）；
    同檔 `:36` 之 `§8.6` 另為一筆 ambiguous（裸節號，PROTOCOL「文面引用」所禁）。**只報不改**（`down/` 唯讀）。
- **全庫 pytest**：乾淨 HEAD 10 failed／1,366 passed；工作區 **9 failed／1,389 passed** ——
  **只在工作區失敗者 0 項**（逐名比對），即本包無回歸。
  （差之 1 項 `test_tc_sheet_variants.py::test_template_sheet_name_exists_in_the_master_workbook`
  只在 worktree 失敗，因其所需之工作簿為 gitignored、worktree 內不存在。）
- `DELIVERY.sha256` 追加 **ENTRY 038**（type `candidate`，checksum 行指向 sandbox 路徑）；
  `shasum -a 256 -c --ignore-missing` **59 OK、0 FAIL**。
- **唯讀紀律**：`down/`／`up/` 內無任何檔被移動、改名或覆寫；
  `up/20260921_CMF-03.md` 落檔前已證該路徑不存在（R-G62(c)），故不需 `_A`／`_B` 後綴。
  量測用之暫存一律落於 session scratchpad，未入專案樹。

### 自報

1. **`git stash` 之禁令被遵守，但我一開始差點用它**：歸屬分析改用 `git worktree add --detach`
   於 scratchpad，用畢 `git worktree remove --force` ＋ `git worktree prune`。
2. **測試檔之模組污染（§5-3 末）**：我先犯了 `tests/test_home_lint_tcs.py` 註解裡早已寫明的錯。
   已修正且全庫測試回到基線，但這是一個**已被前人記下、我沒先讀**的坑。
3. **`vm_axis_plan.tsv` 之 `literal_match` 是字面比對，不是適用性判定**。
   「依字面三台全適用 0 條」不代表沒有 TC 三台都適用 —— 它代表**沒有任何 TC 之 PC 以
   PROXI label 之字面書寫其條件**。這個 0 是口徑之結果，不是結論。
4. **`nav_rewrite_plan.tsv` 之 `er_shift` 為上界**：336 條之判準是「需要入口且今無固定入口」。
   若改寫時把 PC 所宣告之入口保留而只補 hop 於 PC，實際位移之條數會小於此數。
5. **`gen_batch*.py` 之 guard 未加於 `cmf02_apply.py`／`cmf03_apply.py`** ——
   後二者本身即一次性且以斷言自鎖（重跑即 `SystemExit`），R-C59 亦只點名 generator。
6. **`-146`（`014-04`）與 `json-275` 之 ER 含同一句 `most closely matches`**，
   而 R-C56 只點名三條，故 `-146` **未加 Remarks**。其 `H`／`W` 之命中在 0907 基線中即已存在。
   若分析層要一致，第四條補上即可。

---

## 9. 產物與 SHA256

| 路徑 | sha256 |
|---|---|
| `features/comfort/sandbox/revise_0921/…_SWQT_ComfortHMI_20260921.xlsx`（候選本 v2） | `98ddf21a616b5f45e0b0cb0d2b163650d1b19090b8b48260cc0032148a31a47f` |
| `features/comfort/generated/*.json`（129 檔，476 TCs） | 見 change log；可由 `cmf03_apply.py` 自 HEAD 逐位元組重現 |
| `features/comfort/scripts/cmf03_apply.py`（新增） | `87bc7830f8816a2eb7884d31e45360080c1e0231dee7b2acdeeef9d694465fe9` |
| `features/comfort/scripts/cmf03_revise.py`（新增） | `7fa4cec0212035770ec006d3af7da2ea5b14057fd9e46383bfa95e120dc3c2c7` |
| `features/comfort/docs/reports/cmf03_json_change_log.tsv` | `3a9846352381b23e45e74893ac90b1879c6e720e4f29f1ec756a029eab966278` |
| `features/comfort/docs/reports/cmf03_m7_apply.tsv` | `e2d79852aceb0148ce4cc451bea683f6b3177dae4587472a1c9cf3f00cb417b2` |
| `features/comfort/docs/reports/cmf03_candidate_cell_recon.tsv` | `ef6595652610dd0d2bb622dc9a9aa5c274dababb5ebd44f4a50af844fa24356f` |
| `features/comfort/docs/reports/cmf03_vs_0918_cell_delta.tsv` | `c737a0c2720c3a93fb427d14366b90828d28aaa2b810eded9640c5546b36fc57` |
| `features/comfort/docs/reports/cmf03_rc49_refs_before_after.tsv` | `16871541b10bea335a9a9767a9d3973b995065e41e824e3c973e14a81e011185` |
| `features/comfort/docs/reports/nav_rewrite_plan.tsv` | `48fb3c32ce553b551ba6d51099529b49e687275696f58d39bab8b09fcf3d13e6` |
| `features/comfort/docs/reports/vm_axis_plan.tsv` | `59020354404bf47bd8cc9d51c69c8c3882e541493dfcffb752524d68d01701c5` |
| `features/comfort/docs/reports/proxi_axis_values.tsv`（表頭補記） | `3bc4ac4ce5fb6aeebd3dd4f2f438e8a96d998fc53e5737e94847a15e50960ee0` |
| `features/comfort/sandbox/revise_0921/lint/LINT_STATUS.txt` | `3e3e769cf5ce96bd70936081e1e3757eacc960578cc4fee47f0a35cb708fe500` |
| `features/comfort/RULINGS.md` | `e24cd0919e217820d22dcbd4c45720e893d91a1c462e5e0ba75b109e5cfb6fc8` |
| `features/comfort/ANOMALIES.md` | `3fe3d837a67217ca051d4b7dfb64f758cb3061fb3906405e72d18ce5f6b44e43` |
| `features/comfort/DATA_REQUESTS.md` | `1603a6d3e1b07c7e7042ad8140e270cdbeb01bded09bf025de3ab05e347f5a29` |
| `features/comfort/DELIVERY.sha256`（追加 ENTRY 038） | `63bea7398011786d356a27185dd5b906467bfb70131f8cf253dda9016bc160be` |
| `docs/runtime/profiles/FW036_R1L_Comfort_Profile.md` | `56fe825e6c893ce73b828fdf3acf36463bdeb47bc0322b37d0b0ac7fb4f0bff5` |
| `features/comfort/scripts/write_back.py`（R-C49(amend)） | `e9a146f6aee50add051317cd802cb2191cbe808188baa7cde2b7d5d70a0700ff` |
| `features/comfort/scripts/lint_tcs.py`（R-C50(amend) gate ＋ 登錄簿） | `3ba9c6a86549f978ebf26ee00cac30058bd89e1dd64600ddabe2fcd8652f426a` |
| `features/comfort/scripts/test_item.py`（R-C59 凍結註記） | `0f06fcc3398497686d177dc75e6cc6d9c69e5e327ccd679c903e3dd9081bb177` |
| `scripts/lint036.py`（R-C57） | `800ca0136ca88669ee4856afcf0415ae278119175ef9213268c5d1fa3b0a0b59` |
| `tests/test_comfort_upper_verbatim.py`（新增，負向測試） | `fc2a82ed76a831d87b6d67d8d379fb8347d728c3545ee2d5c9e3ee975406af9e` |
| `features/comfort/scripts/gen_batch2.py`～`gen_batch17.py`、`gen_pilot.py`（R-C59 guard，17 支） | 見 `git diff`；改動為各檔尾端同一段 `_frozen_guard()` |

**old→new：無**（既有 466 列之 F／E 未動；新增 ID `-475`／`-476` 為接尾）。

---

## 10. 待分析層／Pei

1. **R-C58 之停點**（§2）：本交付之平台欄位為 Atlantis（MID）抑或 Atlantis High？
   —— 一句裁定即可結 DR-46 並寫出四條之 `Send CAN:` 行。
   若判「取字面而代入 `BCM_FD_27.EBL_Stat`」，亦請明示。
2. **M7 第 4 問之合稱行**（§4）：`Face/Feet`、`Feet plus Windshield` 加不加引號？
3. **軸 2 之兩條補 PC**（§4-1）：核或退。
4. **`-146` 是否補同型 Remarks**（§8 自報 6）。
5. CMF-02_review §3 之三件仍待 Pei：R-C44 vs 09-17 全域標準之位階、
   `ENTER_COMFORT` 入 canon §5.3、DR-49 之登與不登。
