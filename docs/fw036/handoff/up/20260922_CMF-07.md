# 上繳包 CMF-07 — EMEA ICS 出範圍 ＋ `B5` 版號 → 交付候選本 v2

- 日期：2026-09-22
- 對應下放包：`down/20260922_CMF-07.md`（前置 `down/20260922_CMF-06_review.md`）
- 性質：R-C68 落地、`Product Document!B5` 一格、交付候選本 v2、附件更新。
  **未入 `delivered/`、未刪任何列、未改任何 TC 內容、未 commit、未跑 `--gate`／`--regen`、未重跑 generator。**
- 開工前 `git status`：**CMF-06 產物已提交**於 `b544fbd feat(comfort): CMF-06 — delivery candidate with cover sheets and handover files`（19 檔）；
  兩份新下放檔（`CMF-06_review`、`CMF-07`）另以 `6fc23e2 docs(fw036): add CMF-06 review and CMF-07 handoff packages` 入版控。開工時工作區乾淨。
- R-G62(b)：起點前 `features/comfort/` 之最後他方寫入為 `b544fbd`（同日稍早）；本輪期間未觀察到他方寫入。**「無他 session 寫檔」不可自證。**
- R-G62(c)：`up/20260922_CMF-07.md` 落檔前已證該路徑不存在。

---

## 0. 首段

| 項 | 結果 |
|---|---|
| **對交付本 v1 差異格數** | **230**，全數歸因 —— **車型欄 229 ＋ `Product Document!B5` 1**。與 CMF-07 §2 之預估**逐格相符** |
| **三台 `1` 條數** | V Promaster **295**／Y Toro **16**／Z Fastback **297**（v1 為 407／21／409） |
| **全 `0` 條數** | **177**（v1 為 65）—— 占 476 之 **37%** |
| R-C68 之實改 | **112 列／229 格**；**119 條列全數保留，一條未刪** |
| 對 0907 本 | 不符 **0**／21,309 格；變更 **3,693 格**全數歸因 |
| TC 總數 | **476**（不增減）；JSON 語料本輪**未改動** |
| LINT | JSON 層 **57/57 PASS、476 TCs、0 finding**；交付本 v2 層 **H=0 W=0 J=0 P=0 L=0 U=0** —— **與 v1 逐項相同**。`LINT_STATUS: OK` |
| 產物 | `…_SWQT_ComfortHMI_20260922_v2.xlsx`，sha256 `33717ddf…`，242,744 bytes |

**歸因之一處說明**：`cmf07_vs_v1_cell_delta.tsv` 之桶為
`R-C48(amend2)/R-C62/R-C68 車型欄` **227**、`§3 sibling` **2**、`B5` **1**。
那 2 格是 `V483`／`Z483` —— **仍是車型欄**，只因它們落在 sibling 列（row 483），
歸因函式先命中 `§3 sibling` 之分支。**車型欄合計仍為 229 格**（已逐格核對）。

---

## 1. R-C68 —— EMEA ICS 出範圍

### 1-1. 判準與實改

判準機械化為一條可查之條件：**PC 含
`The vehicle is an EMEA ICS vehicle, whose climate interface is specified in chapter 16`**。

| | 數 |
|---|---|
| 命中之 TC（正向 EMEA PC） | **119** |
| 其中今有任一台為 `1`（需改） | **112 列** |
| 需改之格 | **229 格** |
| 其中今已三台全 0 | 7 |
| 否定式 EMEA PC 之 TC（**不動**） | 307 |

- **列保留、一條未刪**（R-C46：既有 `F`／`E` 不重編 → 119 個 TestRail ID 不作廢；
  且 99 個 leaf 仍各有 TC 輸出，覆蓋不破）。
- **不加 Remarks**（R-C63）—— 車型欄本身即為該資訊。
- 值落於 **`docs/reports/vm_assign.tsv`**（單一真源），交付本由該表讀值；
  該表之 `rule_branch` 對應列已改為 `R-C68（EMEA ICS 不在販售範圍；Pei 2026-09-22）`，
  **R-C67 之表頭註記依 R-TM13 劃線保留**。

### 1-2. 「不寫」讀為「保留不刪」—— 刪列之代價（備 Pei 反轉）

CMF-07 §1 末段要求：若 Pei 之「不寫」意為**刪列**，報其代價而不執行。
分析層已在 R-C68 條文內取「保留不刪」之讀法，本輪依條文執行。代價列此備查：

| 刪列之代價 | 量 |
|---|---|
| 作廢之 TestRail ID（E 欄） | **119** —— 且依 R-C46 既有 ID 不重編，作廢後無法回收 |
| 失去覆蓋之 leaf | **99** —— 每個須改為 `Out of Scope` 佔列（比照 Diagnostics 之 R-DIAG3(amend)），否則 `coverage.tsv` 出現 99 個無 TC 之需求 |
| F 欄之連續性 | 刪列後 F 不連續（或須重編，與 R-C46 直接相衝） |
| 工作簿列數 | 476 → 357，且 DV／B 欄公式之涵蓋範圍須重驗 |

**一句話即可反轉**，但上列四項是那句話的代價。

### 1-3. 一件仍未被回答的事（記於 A-CMF24 之結案段）

R-C68 之依據是 Pei 之「**不含在販售的範圍內**」——一個**範圍裁定**。
它與已撤回之 R-C67 所主張的「**三台皆非 EMEA ICS 車**」——一個**車型事實**——**不是同一件事**。

**三台是否為 EMEA ICS 車，至今沒有人查得出來**：PROXI 無對應參數
（`vm_unmapped.tsv` 之 426 列仍在），條文之 `EMEA` 於 129 節**零字面**。
本輪之結果碰巧相同（119 條全 0），但理由不同。已記於 `ANOMALIES.md`，
**以免日後把「不在販售範圍」讀成「已查明三台非 EMEA」**。

---

## 2. `Product Document 記錄封面頁`!B5

`Initial Release` → **`V1.1`**（CMF-06_review §2；與同分頁 A14 之 `V1.1` 一致，
亦與 SWC 0628／Home 0809／Popup 0908 三本該格皆為 `V1.x` 之形制一致）。

---

## 3. 裁定落帳

落帳前實測現行最大號為 **R-C67**，暫編＝實編：**R-C68**。sha8 未產（GC-16 未落地）。

- **`RULINGS.md`**：R-C68 條文逐字（CMF-07 §1）；**R-C67 標撤回**
  （原文依 R-TM13 保留），並載其撤回之由指向 A-CMF25。附本輪之落實數。
- **`ANOMALIES.md`**：**A-CMF25／A-CMF26** 逐字取 CMF-06_review §1；
  **A-CMF24 結案段**記「R-C68 與 R-C67 理由不同」一事（§1-3）。
- **`DATA_REQUESTS.md`**：本輪**無變更**（DR-46 CLOSED；47／48／49 open）。
- **profile**：本輪**未改** —— R-C68 之值落於 `vm_assign.tsv`，profile §3.9 已指向該表。

---

## 4. 附件更新（CMF-07 §1）

| 檔 | 更新 |
|---|---|
| `docs/reports/coverage.tsv` | 加欄 **`out_of_sales_scope`** —— **99 個 leaf／119 條 TC** 標 `EMEA`；`vehicle_V_Y_Z` 欄一併自 v2 重讀 |
| `docs/reports/rd_feedback.md` | 加**第 6 項**「EMEA ICS（chapter 16）之需求不在販售範圍 —— 建議 037 標註」，原第 6 項順延為第 7 項。該項之重點：**037 無任何市場欄位，「不在販售範圍」這件事在需求側沒有痕跡** —— 下一個接手的人只會看到 119 條車型欄全 0 而不知其所以然 |
| `docs/reports/vm_assign.tsv` | 119 條之值與 `rule_branch` 改 R-C68；表頭加三行說明，R-C67 註記劃線保留 |

---

## 5. 交付候選本 v2

- 產出：`features/comfort/sandbox/delivery/…_SWQT_ComfortHMI_20260922_v2.xlsx`
  - sha256 `33717ddfe092642a38011ef5cd6913c0cda93f10b344973140d9a79771bfe5a5`
- 母本：0907 本（先驗 sha `3550d16d…`）之複本；另先驗交付本 v1 之 sha（`c2ddf95d…`）。

| invariant（★ ABORT 級） | 結果 |
|---|---|
| ★ zip member 數 | 57 ＝ 57 |
| 差異僅限所改之四個 sheet xml | `sheet3`／`sheet4`／`sheet5`／`sheet6` —— 與預期集合逐字相同 |
| ★ DV sqref（classic ＋ x14） | 逐字相同 |
| B 欄公式 row 10–601 | 逐列原樣 |
| 留空三格於產出檔仍為空 | PASS（`ChangeHistory!E8`／`Cover!G7`／`Cover!G8`） |
| 寫入格讀回 == 意圖值 | **0 格不符** |
| **對 0907 本逐格對帳** | **不符 0 格**／21,309；變更 **3,693 格**全數歸因 |
| **對交付本 v1 逐格對帳** | 差異 **230 格**，未歸因 **0** |

**仍未做者（同 v1，Tier 3，需 Excel）**：新列 476–485 之列高與樣式
（量測見 `docs/reports/new_row_style_measure.tsv`：列高設 112 ＋ 複製 row 475 格式）；
`Product Document!D14` 之日期格式；`Test Case Framework` 之 SmartArt 兩個舊名。

---

## 6. 自檢

- **JSON 層**：`lint_tcs.py` **57/57 PASS、476 TCs、0 finding**（本輪 JSON 未改動）。
- **交付本 v2 層**：`H=0 W=0 J=0 P=0 L=0 U=0`，`M=10 R=1486 I-cross=443 X=337`
  —— **與 v1 逐項相同**。R-C68 只動車型欄（`T–Z` 不在任何 lint 檢查之施檢面），`B5` 在封面類分頁。
- **可重現**：重跑 `cmf07_delivery.py` 得**相同 sha256**、`MISMATCH_COUNT: 0`。
- **`canon_refs.py --report --waiver`**：工作區 **35／178**；以 `git worktree`（**未用 `git stash`**）
  取 HEAD(`6fc23e2`) 之基線，同為 **35／178** → **本包 0 筆**。
- **全庫 pytest**：HEAD 基線 10 failed／1,366 passed；工作區 **9 failed／1,389 passed**；
  **只在工作區失敗者 0 項** → 無回歸。
- `DELIVERY.sha256` 追加 **ENTRY 042**；`shasum -a 256 -c --ignore-missing` **63 OK、0 FAIL**。
- **唯讀紀律**：`down/`／`up/` 內無任何檔被移動、改名或覆寫。

### 自報

1. **`vm_assign.tsv` 被就地改值**（非只加註記）。它是 CMF-04 之產物，本輪把 119 條之
   V／Y／Z 由判定值改為 R-C68 之 0。這是刻意的 —— 讓交付本與該表**只有一個真源**；
   但也意味著 `vm_assign.tsv` 不再純粹是 R-C62 之判定紀錄。
   R-C62 之原判定仍可自 `vm_split_plan.tsv` 與 CMF-04 上繳包復原，且表頭已記此事。
2. **`coverage.tsv` 之 `vehicle_V_Y_Z` 欄一併重讀**，故該檔相對 CMF-06 版有兩處變動
   （新欄 ＋ 既有欄之值），非只加一欄。
3. **§1-3 之區分**（範圍裁定 vs 車型事實）是我加的，不在下放包之要求內。
   我認為不加會讓下一輪把兩件事當成同一件。若分析層認為多餘，刪 `ANOMALIES.md` 該段即可。

---

## 7. 待 Pei

1. **Excel 內三個動作**：row 476–485 列高 112 ＋ 複製 row 475 格式；
   `Product Document!D14` 設同 D13 之日期格式；`Test Case Framework` SmartArt 之兩個舊名。
2. **三格填值**：`ChangeHistory!E8`（核准者）、`Cover!G7`（核准者日期）、`Cover!G8`（審查者日期）。
3. **送出**：`rd_feedback.md` **七項**、`dr_summary.tsv` 之 DR-47／48／49。
4. **若「不寫 EMEA」其實是要刪列** —— §1-2 之四項代價，回一句即改。
5. 之後本線只剩**導航 Revise**（R-C66）：待 canon §5.3 增 `ENTER_COMFORT`（您改原檔後重貼）
   與 DR-49 之回覆。

---

## 8. 產物與 SHA256

| 路徑 | sha256 |
|---|---|
| `features/comfort/sandbox/delivery/…_SWQT_ComfortHMI_20260922_v2.xlsx`（**交付候選本 v2**） | `33717ddfe092642a38011ef5cd6913c0cda93f10b344973140d9a79771bfe5a5` |
| `features/comfort/scripts/cmf07_delivery.py`（新增） | `9aa0e87b6a86990abac3742965c37e32668960cba9aad20bca006646d1d85478` |
| `features/comfort/docs/reports/cmf07_delivery_cell_recon.tsv` | `f322a676a12b8edb4c4f22e0f622be32d58eed41818dce77860755616d864a9a` |
| `features/comfort/docs/reports/cmf07_vs_v1_cell_delta.tsv` | `1370b1a730a5b5481f209136204936e44b9c4d3eed576892a2d118ac98f2f3ab` |
| `features/comfort/docs/reports/coverage.tsv`（加 `out_of_sales_scope`） | `4c921020eba69d62a0d0131ad409403cfa12fb40c632f0aaa70e17d1ab6e3ac9` |
| `features/comfort/docs/reports/rd_feedback.md`（加第 6 項） | `e31965e65b6952a66860febd05e5b6d5d16bbd0e3c70ac6ba0a17ba1b32138d1` |
| `features/comfort/docs/reports/vm_assign.tsv`（R-C68） | `b0f84f59c7f3fb8f2ef92321440b30e0cdbf58fb08f324c6ef3d2f5f3ffca47d` |
| `features/comfort/sandbox/delivery/lint_v2/LINT_STATUS.txt` | `63caef684fac145f99a739b21316b2364bd41493c51736b9780beedb752c9443` |
| `features/comfort/RULINGS.md` | `86e3c653ecc831f6fc215553ed17e374603bf7761181f224a203f5f0a5cb918a` |
| `features/comfort/ANOMALIES.md` | `8bed8508b11d9c5b8451545f3c14d0bcf13daba7cc30e364c33a2f74407f5c15` |
| `features/comfort/DELIVERY.sha256`（追加 ENTRY 042） | `4958b18a6a4bcb5a0c69fb7754b01d4491c0c7e7e85b0abb53dd86cb075251e7` |

**JSON 語料本輪未改動**（476 TCs，與 ENTRY 040 同）。**old→new：無**。**刪列：無**。
