# 上繳包 CAM-20 —— RDF-15／-16、GCB-11、DECISIONS 6-67～6-70、**B 本尾批 B05 40 TC（B 本 230/230 完結）**

對應下放包：`docs/fw036/handoff/down/20260923_CAM-20.md`（審閱 `down/20260923_CAM-19_review.md` 同發）。
執行層 session：延用。日期 2026-09-23。

**一句話**：§1 三項落檔完成；B05 四組合計 leaf **實測 40**（＝ 上限，不切批），產 **40 TC**
（`NR1L-RVCHMI-158`～`-197`），selfcheck 十項全 0、lint ERROR 類全 0、**零 TC 0 列**；
**`coverage_b.tsv` 230/230 全數處置 —— B 本八組全部完結**。
三項升級條件**皆不成立**。自查得一處來源衝突 → **RDF-16**。
**A 本不動**（十目錄 `git status` 實測 0 改動）；未寫 `delivered/`、未 tag；B 本組簿 dry-run 不在本包。

---

## §1　落檔

### 1-1　RDF-15（`PU0456` 按鈕欄 `<Nol>`）

`features/camera/RD_FEEDBACK.md`（sha256_16 `494f7dea1b44f784`）：
`PU0456` 之按鈕欄第二項拼作 `<Nol>` 而文字欄為 `<Yes>  <No>`；
同表之同型彈窗（`PU1518`／`PU1519`）按鈕欄皆作 `<No>`，故為**孤例**。
TC 側依文字欄書寫，差異註於 `NR1L-RVCHMI-137` 之 `Remarks`。

### 1-2　GCB-11（lint `X` 之固定入口清單）

`docs/fw036/handoff/GC_BACKLOG.md`（sha256_16 `138279f8add91fd5`）登 **GCB-11**：
`X` 之固定入口判定未納各 feature profile §5.3 之導航常數
（Camera 側 `ENTER_SETTINGS_APP`／`ENTER_CAMERA_SETTINGS`），致 B04a 30 列中 10 列命中且改寫無效。
建議改為「Procedure 含該 feature §5.3 任一常數之完整 hop 串」即認，或提供 per-feature 白名單。

### 1-3　DECISIONS 6-67 ～ 6-70

`features/camera/DECISIONS.md`（sha256_16 `c3fd62ba64858121`）：
**6-67** 批次切點取章界／流程界，下放包之列數上限為**上限非目標**；
**6-68** `Aux Cameras` 之子項 label 取 SYS1 逐字（Settings List 無子項，非缺件，不開 DR）；
**6-69** **「來源只給圖」型之通則** —— ER 只判結構性可觀察項、**不造文字**、**不掛 DR-CAM-h**
（該條為「Pop Up List 查無」，兩者成因相異），reasoning 須具名圖檔名與「內容不可抽」；
**6-70** lint `X` 之 10 列不消（非 TC 內容缺陷），併登 GCB-11。

---

## §2　B05（B 本尾批）

### 2-1　leaf 實測與切法

| Test Set | 章 | 全列 | **leaf** | umbrella |
|---|---|---:|---:|---:|
| `AUX Camera Settings`（B04b）| HU §34.9.1–§34.10.3 | —— | **15** | —— |
| `Warning Banners` | RVC+PAM §8.2–§8.4.1、§9.2–§9.2.3 | 13 | **12** | 1 |
| `PAM Integration` | RVC+PAM §6.2–§6.5、§8.8、§11.1 | 10 | **9** | 1 |
| `Camera Settings` | HU §6.2.2.1–§6.2.2.3 | 5 | **4** | 1 |
| **合計** | | | **40** | |

**40 ＝ 下放包 §2 之上限，不切批**，升級條件 §4-1 不成立。
ID `NR1L-RVCHMI-158`～`-197`，四組各依 framework VIII.3 之 Test Set。

### 2-2　零 TC：0 列

本批四組之逐字同句掃描（母體：cache 本 §29／§30／§31／§34 全部列 ＋ RVC+PAM 全本）
**未得任何含本批章節之同句群**，R-CAM16(b)／(c) 皆不觸發。
三組近似而**不**委派者，其判準逐項如下（皆依 **R-CAM16(c)** 之「父題須亦全等」要件）：

| 對 | 判 | 依據 |
|---|---|---|
| §34.9.1 ↔ §29.1.1／§29.2.1 | 各自出 TC（`-158`／`-128`／`-133`）| Description 僅 image token 不同（正規化後仍相異）；父題三異（`AUX Cam Settings`／`Wired`／`Wireless`）|
| §34.9.8 ↔ §31.1.4 | 各自出 TC（`-168`／`-153`）| Description **逐字全等**（皆 17 token），惟父題相異（§34.9 R1 Low vs §31.1 R1 High）|
| §34.10.3 ↔ §34.7.1 | 各自出 TC（`-172`／`-092`）| 差一個 image token（22 vs 24）；父題相異（`Camera app` vs `from Surround View`）|
| §8.3.3 ↔ H §6.1.2 | 各自出 TC（`-177`／`-001`）| 本列有 `RVC2.4)` 編號前綴；**跨兩本**且分屬兩組 —— R-CAM16(c) 以「同組另一列」為要件，不適用 |

### 2-3　自查得之來源衝突 → **RDF-16**

`Check Entire Surroundings` 之時限：**SYS1 三處皆 5 秒**
（RVC+PAM §8.2 `NRL-142633`、§9.2 `NRL-142646`；HU §28.2.1 `NRL-188092`），
而 `forms/Pop Up List HMI R1 (26PI).xlsx` `Main` r365 之 **`PU0362` Timeout 欄為 `10`**；
同表之相機類同訊息列（`PU0447`／`PU0467`）亦皆 10，**只有** `PU1102`（Turn Signal）為 5。

§8.2 本身即逐字引 `(PU0362)`，故非兩條獨立需求之差異，而是**同一需求之兩處記載不一**（同 RDF-11 之型）。
TC 側依 **§4.3.1** 以 SYS1 為準（`-173` 以 4／6 秒驗 5 秒窗），差異註於該列 `Remarks`。

### 2-4　`Vehicle Model`

| 判 | 列數 | 依據 |
|---|---:|---|
| 五款全 `1` | 29 | R-CAM18(a) |
| 只勾 `HDCC27`／`DT27` | 1（`-172`）| §34.10.3 **來源逐字點名** `DT and HDCC programs` → **R-CAM3** |
| `Toro(2261)` = `0` | 9（`-185`～`-193`）| 前提用 `CVPAM_Presence`（byte 173 bit 1，**DECISIONS 6-56**），該 byte 於 `Toro_ATL_MI` 零命中 → **R-CAM18(b)** |

**升級條件 §4-4 之對照**：PAM 之 CAN 訊號**皆查得**，無需 DR-CAM-f 型 ——
Atl-Hi `BCM_FD_12.PAMRequestSts`（`PDT27_E2A_R1_FDCAN8.dbc`，`VAL_ 0 "Not_Pressed" 1 "Pressed"`）、
Atl-Mi `STATUS_PAM.PAMSystemSts`（`P363` 與 `637MCA` 兩本，
`VAL_ 0 "OFF" 1 "ON_Active" 2 "ON_Inactive" 3 "ON_Disabled"`）。

### 2-5　hop label（升級條件 §4-2）

`ENTER_CAMERA_SETTINGS`（profile §3.0）＋ `Aux Cameras`（Settings List row 474／475）＋
`Rear View Camera Delay`（row 467）／`Rear View Camera Active Guidelines`（row 468）／
`Rear View Camera Fixed Guidelines`（row 469，**該列無 `*`**）＋
`“Edit Favorite”`（§34.9.4）／`“Edit Name”`（§31.1.1）／`Reset Camera`（§34.9.2.2）＋
`Camera app home page`／`AUX filter`（§27.2.6.3／§29.1.2）。
**不可辨者 0 列**，升級條件不成立。

**用語差異一則**：同一「設為最愛」之動作，R1 High 側 §30.1.2 作 `“Make Favorite”`，
R1 Low 側 §34.9.4 作 `“Edit Favorite”` —— 兩列各依其來源之逐字書寫（`-141`／`-162`），
差異記於 `b05_plan.tsv` 之 note。

### 2-6　popup（升級條件 §4-3）

| SYS1 | PU | 狀態 |
|---|---|---|
| §8.2／§9.2 `Check Entire Surroundings` | `PU0362` | 查得（時限相左 → RDF-16）|
| §9.2.1／§9.2.2 `Camera System Unavailable` | `PU0169` | 查得；`PU0169` 之觸發欄與 §9.2.2 **逐字對應** |
| §9.2.3 `Camera Out of Position` | —— | **查無 → `PENDING: DR-CAM-h`**（`-184`，1 列）|
| §30.1.3 之確認畫面（B04a）｜§34.9.7／§6.5／§11.1 之圖 | —— | **「來源只給圖」型**，依 **DECISIONS 6-69** 不造文字、不掛 DR |

**升級條件 §4-4 之判**：`Warning Banners` 12 列中，文字於 SYS1 與 Pop Up List 皆查無**且非「只給圖」型**者
**僅 1 列**（`-184`，已標 PENDING），不成立。

### 2-7　PENDING（5 列）

`-169`／`-170`／`-171`／`-172`（`Camera App` 配備旗標，**DR-CAM-r**）；
`-184`（`Camera Out of Position` 文字查無，**DR-CAM-h**）。

### 2-8　對帳 —— **B 本 230/230 完結**

| 組 | `b_plan` 全列 | `coverage_b` | 判 |
|---|---:|---:|---|
| `Activation and Exit` | 50 | 50 | 完結 |
| `AUX Camera Access` | 62 | 62 | 完結 |
| `AUX Camera Settings` | 53 | 53 | 完結 |
| `Camera View Switching` | 31 | 31 | 完結 |
| `Wireless Camera Pairing` | 6 | 6 | 完結 |
| `Warning Banners` | 13 | 13 | 完結 |
| `PAM Integration` | 10 | 10 | 完結 |
| `Camera Settings` | 5 | 5 | 完結 |
| **合計** | **230** | **230** | **全數完結** |

`coverage_b.tsv`（sha256_16 `0fc77beaa799cc23`）：**PRODUCED 197／零 TC 6／umbrella 27 ＝ 230**。
**未處置之 leaf：0**（逐列比對 `recon_b_rows.tsv` 之 230 列，無遺漏）。

### 2-9　lint 與 selfcheck

```
selfcheck_camera.py  b05（TC 40 筆）         十項全 0｜錨 40／反查失敗 0
lint036 --profile camera（b05_lint.xlsx）    ERROR 類全 0｜非致命 U=5／I-cross=40／X=4｜Z=0／J=0

selfcheck_camera.py  十七目錄（TC 437 筆）    十項全 0｜錨 441／反查失敗 0｜常數錨 2｜合一錨 2
render_tc.py --verify 十七目錄               逐位元相符 437／相異 0
```

---

## §3　R-CAM17 證據表

| # | 主張 | 掃描字串／判準 | 母體 | 命令 | 命中 |
|---|---|---|---|---|---|
| 1 | 本批四組之 leaf 數 | `test_set` ＋ `kind` 欄計數 | `features/camera/data/b_plan.tsv` 230 列 | csv 計數 | 15／12／9／4 ＝ **40** |
| 2 | 本批無逐字同句群 | `Description` 正規化後分群，取含本批章節者 | cache 本 §29／§30／§31／§34 全部列 ＋ RVC+PAM 全本 | openpyxl 全量讀 ＋ 分群 | **0 群** |
| 3 | `PU0362` Timeout 為 10 而 SYS1 為 5 | Timeout 欄（第 3 欄）逐格 | `forms/Pop Up List HMI R1 (26PI).xlsx` `Main` r365 | openpyxl 逐欄讀 | `PU0362` **10**；同訊息之 `PU0447`／`PU0467` 亦 10；`PU1102` 為 5 |
| 4 | `Camera Out of Position` 查無（重測）| `out of position`、`not in position`（不分大小寫）| 同上 `Main` 分頁全欄 | openpyxl 逐列 `re.search` | **0 命中**（與 `popup_texts.tsv` 首兩列之既有記載一致）|
| 5 | PAM 之 CAN 訊號查得 | `PAMSystemSts`、`PAMRequestSts` 之承載 message 與 `VAL_` | `forms/` **4 本 DBC** | 逐檔 `re` 掃 `BO_`／`SG_`／`VAL_` | `STATUS_PAM.PAMSystemSts`（P363／637MCA，四值）；`BCM_FD_12.PAMRequestSts`（PDT27 FDCAN8，二值）|
| 6 | `PAM_Configuration` 值域二項（6-56 複驗）| 值域逐字 | `forms/proxi/Promaster_ATL_MI/…637_R42_20220204.XLSM` byte 118 | openpyxl 讀該列 | `0 = Rear`／`1 = Front And Rear` |
| 7 | Settings List row 467–469 之 `*` | 設定名逐字 | `forms/HMI Settings List …(Feb 13 2026).xlsx` `Settings` 分頁 | openpyxl 逐列 | row 467 `Rear View Camera Delay*`、row 468 `… Active Guidelines*`、**row 469 `… Fixed Guidelines`（無 `*`）** |
| 8 | B 本無未處置之 leaf | `recon_b_rows.tsv` 230 列逐列比對 `prod`／`ZERO`／umbrella | 兩檔 | 集合差 | **0 列未處置** |
| 9 | A 本本輪 0 改動 | — | A 本十目錄 | `git status --short` | **0 列** |
| 10 | md 與 json 一致 | — | 十七目錄 437 份 md | `render_tc.py --verify` | **相符 437／相異 0** |

---

## §4　自報錯誤

1. **B05 初稿之 selfcheck 第 4 項 3 列**（本包內自查自改）——
   `-160`／`-161`（§34.9.2.2）之摘句破保序子序列，成因為 `2”.(“AUX` 之黏字（句點與括號黏於前字）；
   依 **DECISIONS 6-65(b)**（全文 29 token ≤ 50）**兩列皆改取全文**，所驗之項由下半括號句指明。
   `-171`（§34.10.2）之末字 `screen` 而來源為 `screen,`，已保留逗號。
2. **B05 初稿之 selfcheck 第 2 項 3 列**（本包內自查自改）—— `-182`／`-184`／`-186` Procedure 只有 1 步，
   已各加一步對照（恢復後畫面／回正後畫面／互不抑制）。
3. **B05 初稿之 selfcheck 第 5 項 2 列**（本包內自查自改）—— `-189`／`-190` 有 `CAN source:` 行
   而其 PAM 狀態由**實體障礙物**觸發（無 `Send CAN` 步），已刪該行；
   PAM 之 CAN 對照移入 reasoning 供 `-191` 引用。
4. **B05 初稿之 lint `A` 1 列、`H` 1 列**（本包內自查自改）——
   `-180` 用禁用動詞 `check whether`（已改 `check where`）；
   `-192` 之 ER 用關係模糊語 `matches`（已依 **profile §7.8** 改為直述）。
5. **本輪未動 `features/camera/RULINGS.md`／`framework.md`／`ANOMALIES.md`／`DATA_REQUESTS.md`**
   —— 無新立條、無重開、無新異常、無新 DR（`-184` 沿用已擴題之 DR-CAM-h）。
   `docs/fw036/RULINGS.sha.tsv` 仍未重生（**GCB-04**，非本線可改）。

---

## §5　未結

| # | 項 | 歸屬 |
|---|---|---|
| 1 | **B 本組簿 dry-run**（230 列口徑之工作簿、lint 全表、對帳）—— 下放包 §0 明載不在本包 | 後續包 |
| 2 | **RDF-11～RDF-16** 六條 | RD |
| 3 | DR 十五條；A 本 f／g／i／j／k／m／p／q 八條阻交付，B 本 g／h／r | Pei |
| 4 | A 本交付（Phase 5–7）待 Pei 之准；候選為 dryrun2（244 列，DECISIONS 6-58）| Pei |
| 5 | GCB-01～GCB-11 十一項 | GC 線 |
| 6 | `bench_verify.md` 累計 **91 列**（A 本 11、B 本 80）| Pei 實機 |

---

## §6　落檔清單

```
M  features/camera/RD_FEEDBACK.md                 RDF-15／RDF-16       sha256_16 494f7dea1b44f784
M  features/camera/DECISIONS.md                   6-67 ～ 6-70         sha256_16 c3fd62ba64858121
M  docs/fw036/handoff/GC_BACKLOG.md               GCB-11               sha256_16 138279f8add91fd5
A  features/camera/data/b05_plan.tsv              40 列                sha256_16 b594c5a1736a42a2
M  features/camera/data/coverage_b.tsv            230 列（全數完結）   sha256_16 0fc77beaa799cc23
M  features/camera/data/bench_verify.md           B05 20 列            sha256_16 1770a43935e57ce6
A  features/camera/generated/b05/                 40 json ＋ 40 md ＋ INDEX ＋ LINT_REPORT
```

**未動**：`features/camera/RULINGS.md`、`framework.md`、`ANOMALIES.md`、`DATA_REQUESTS.md`、
`features/camera/delivered/`、A 本十目錄與 `sandbox/a_dryrun/`、`features/comfort/`（併行 session）。

---

## §7　B 本總計（供交付規劃）

| 口徑 | 值 |
|---|---:|
| B 本 037 列 | 230（leaf 203 ＋ umbrella 27）|
| PRODUCED（leaf）| **197** |
| 零 TC（leaf）| 6（R-CAM16(b) 1、R-CAM16(c) 5）|
| umbrella | 27 |
| **B 本 TC 總數** | **197**（`NR1L-RVCHMI-001`～`-197`，號段連續無跳號）|
| A 本 TC 總數 | 240（`NR1L-RVC-001`～`-240`）|
| **全案 TC** | **437** |
