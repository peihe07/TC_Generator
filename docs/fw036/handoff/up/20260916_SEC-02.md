# 上繳包 SEC-02 —— 裁定落檔 ＋ 三項重跑 ＋ framework LOCKED ＋ profile 定稿 ＋ apk 配對 ＋ Pilot 10 TC

對應下放包：`down/20260916_SEC-02.md` ＋ `down/20260916_SEC-02_A.md`。
走法：**走法 2（照實數開工）** —— `_A` 已確認執行層之開工前查核三處皆為分析層條文錯誤，
本包即以 `_A` 之修正數字落地，`framework.md` 據此 LOCKED。

**禁區遵守**：`down/`／`up/` 既有檔未動；其他 feature 未動；canon 與全域 ledger 未動
（`RULINGS.sha.tsv` 待 Pei 合併時重生）；只產 Pilot 10 TC，未越出 §8 取樣；
`scripts/lint036.py` **只新增 `FEATURE_CHECKS["security"]` 分支與其兩個檢查函式**，未動既有檢查項與 `PROFILE_CHECKS`。

> **git**：本輪**未執行任何 git 操作** —— 作業中 `git` 於本機損毀
> （連 `git --version` 都回 `You have not agreed to the Xcode license agreements`），
> 需 Pei 於終端機執行 `sudo xcodebuild -license` 後方能 commit。見 §10。

---

## 1　R-SEC9 ~ R-SEC14 ＋ R-SEC5(a) amend 之落檔（§9-1）

**取號現查（R-G23）**：`features/security/RULINGS.md` 之既有錨點為 `R-SEC1`~`R-SEC8`
（`grep "^### R-SEC"` 實測），故 `{live}` = **9**，與下放包相符。
（另有兩處 `R-SEC9`／`R-SEC11` 字串出現於 `peer_review_rows.tsv` 之 `disposition` 欄，
為本包自身之前向引用，非錨點。）

| 條號 | body sha8 | body_kind | 行數 |
|---|---|---|---|
| **R-SEC9**（Polarion NRL 對照）| `13193baa` | fenced | 13 |
| **R-SEC10**（framework 與工作簿形制）| `8fc10b7b` | fenced | 17 |
| **R-SEC11**（Peer Review 轉換，**修正版**）| `6c1e8b60` | fenced | 28 |
| **R-SEC12**（batch 人工覆寫）| `e9a57e13` | fenced | 8 |
| **R-SEC13**（lint 範圍與代號）| `eec34fd5` | fenced | 6 |
| **R-SEC14**（雜項）| `5e48ce44` | fenced | 12 |
| **R-SEC5(a) amend**（CS.212 判準，[A-SE12] 結案）| `dfa9ada8` | fenced | 24 |

`python scripts/rulings_hash.py --target features/security/RULINGS.md --gate` → **通過**
（無重複 id 之本體歧異）。R-SEC1~R-SEC8 之 body sha8 **逐條不變**。

---

## 2　§1 新增投遞 —— CertProvider 之 Polarion 匯出

| 項 | 下放包所載 | 實測 | 判 |
|---|---|---|---|
| sha16 | `beff84c77c6810f1` | `beff84c77c6810f1` | ✅ |
| 大小 | 26,778 B | 26,778 B | ✅ |
| sheet | `Basic Report` 12 列、`Polarion`、`_polarion` | `Basic Report` **13 列**（表頭 ＋ 11 資料列 ＋ 1 空列）、`Polarion`、`_polarion` | ⚠ 列數口徑差 1，資料列 11 相符 |
| NRL 區間 | `NRL-349384`～`NRL-349394` | 相同，對 `SWE1-CertProvider-001`~`-011` **逐列順序對應、無缺無跳** | ✅ |
| **「11/11 逐字相同」** | 分析層實測 | **執行層複驗成立**：`Description` **11/11**、`SWE1 Verification Criteria` **11/11**（空白正規化後逐字相等）| ✅ |

登錄：`sources/raw/swe1_certprovider_polarion/`，MANIFEST doc_id `swe1_certprovider_polarion`。
MANIFEST 由 220 → **221** 列；`features` 含 `security` 者 **27** 列。

**R-SEC9(a) 已落**：`trace_matrix.tsv` 新增 `nrl_swe1` 欄，CertProvider 11 列填 NRL、
其餘 **59 列填 `-`**（合 70）。
註：該匯出之欄名為 `SEW1 SWE-Requirement ID`（**原檔拼字 `SEW1`**），解析逐字照用不更正（R-6）。

---

## 3　§3 三項重跑（§9-2）

### 3-1　R-SEC5(a) amend —— 命中 **5 列**

| 版本 | 判準 | 命中 |
|---|---|---|
| SEC-01 初版 | 全 VC 逐字含五詞 | 11 |
| SEC-01 審閱 三-1 | WHEN∪THEN ＋ 否定濾網 | 7 |
| **SEC-02_A §2（現行）** | **THEN-only ＋ 須宣稱資安事件被寫入 log** | **5** |

命中逐列（與條文指名者**逐列相符**）：

| SWE1 ID | THEN 子句（逐字）|
|---|---|
| `SWE1-CertProvider-008` | `THEN. Confirm successful end-to-end verification through system logs (Logdog) for both client types.` |
| `SWE1-KeyInsyall-010` | `6.1. THEN the system must REFUSE the request and log a security violation.` |
| `SWE1-KeyInsyall-012` | `- THEN no SELinux denial logs (avc: denied) related to KeyInstall should be present.` |
| `SWE1-SAM-0004` | `3.1. THEN DebugAuth logs an error via logdog, which corresponds to fail reason` |
| `SWE1-SAM-0015` | `3.2. AND DebugAuth prints the log about sending message with sequence ID mentioned` |

剔除之二列：`CertProvider-005`（`Monitor system logs` 在 WHEN）、
`CertProvider-006`（`logs must align with Logdog requirements` 為 logging **政策**）。
二者 `ccvr_batch` **不受影響**（經 SYS2 落 `Cert Val CS.98`，機械判準已為 1），
`ccvr_items` 已移除 `Secure Log CS.212`。

**`[A-SE12]` 結案。**

### 3-2　R-SEC12 —— `SWE1-SAM-0007` 人工覆寫

`batch_order.tsv` 該列逐字：

```
SWE1-SAM-0007	1	-	Cert Val CS.98	manual override: apk samCertTestNormalFlow（審閱 三-2）
```

R-SEC12(b)「只此一列」以 `MANUAL_BATCH1` 集合之基數（1）保證。

### 3-3　R-SEC11 —— `disposition`／交叉表

`peer_review_rows.tsv` 之 `disposition` 欄：**CONVERT 12／DEFERRED 19**。
`batch_order.tsv` 新增 `disposition` 欄，`D` 群 **19** 列，與 `peer_review_rows.tsv` 之
`blackbox_possible = N` 集合**兩集合相等**（程式互驗）。

| 群 | batch 1 | batch 2 | 計 |
|---|---:|---:|---:|
| Testing（原生）| 28 | 11 | 39 |
| CONVERT | 10 | 2 | 12 |
| DEFERRED | 11 | 8 | 19 |
| **合計** | **49** | **21** | **70** |

**產出群 51／D 群 19**，與 `_A` §1(c) 相符。
§3-2 之「batch 2 之 Testing 群 3」為粗估已作廢；**實測 11**（＝原生 Testing 之 batch 2），
若連 CONVERT 計則 13。`_A` §1 已定「以 11 為準」，本包採之。

---

## 4　§4 framework 鎖定 ＋ `feature.yaml`

`features/security/framework.md`（sha16 `ba61681bad096c33`）——
Part I Layer 1 六 Test Group、Part II Layer 2 **30 組逐字**（名稱未改）、Part III Layer 3 = SYSAD、
Part IV 鎖定狀態。末記 **`LOCKED 2026-09-16 (R-SEC10)`**。

leaf／batch／D／產出四欄自 `layer2_assign.tsv` ＋ `batch_order.tsv` **程式回填**，
合計 `70 / 49 / 21 / 19 / 51`。

`feature.yaml`（sha16 `2ccf6892c3695f69`）回填：`workbook_count: one`、
`test_group` 六值、`abbr` 六值、`spec_ref_tokens` 六值、`spec_mode: F`、
`paths` 改為 doc_id（R-G66，`inputs/` 不存副本）、`sys1_export`／`spec_pdf`／`popup_list` = `null`。
**YAML 可解析**（`yaml.safe_load` 通過）。

### 4-1　R-SEC10(c) 六個 token —— 逐檔複驗

| Test Group | token（實測推導結果）| 與條文 |
|---|---|---|
| Cert Provider | `SWE1-CertProvider-SWE1R1-V1.0` | ✅ |
| Key Install | `SWE1-KeyInstall-SWE1R1-V1.0` | ✅ |
| SAM | `SWE1-SAM-SWE1R1-V1.1` | ✅ |
| ECU Cert | `SWE1_ECUCert_FM-WI-FSM-037-A03` | ✅ |
| SWDL Secure Lib | `SWE1_SwdlSecureLib_FM-WI-FSM-037-A03` | ✅ |
| Log Encrypt | `SWE1_libLogEncrypt_FM-WI-FSM-037-A03` | ✅ |

**六值全部吻合**，§10 之「token 六值與檔名推導不符」不成立。
註：`Key Install` 之 token 為 `KeyInstall`（檔名正確拼字），而其 SWE1-ID 為 `KeyInsyall`（原檔錯字）——
`specification_reference` 因而形如 `SWE1-KeyInstall-SWE1R1-V1.0_SWE1-KeyInsyall-011`，**兩種拼字並存**。
此為 R-SEC3(a)／R-SEC8(b) 之必然結果，非錯誤，具名備查。

---

## 5　§5 apk 配對草表

`features/security/data/apk_pairing.tsv`（sha16 `a9718d57320af26e`），**15 列**，
欄 `no | method | swe1_ids | basis | confidence`。

- `confidence`：**high 7**（01/02/04/09/10/11/12）／**medium 8**（03/05~08/13~15）—— 與 §5 末段**逐項相符**。
- **複驗**：15 條方法**全部**在 `Test_Items.txt` 之 15 條內；所引之 SWE1 列**全部**在 `trace_matrix.tsv` 70 列內。
- 被配對之 SWE1 列共 **10** 個：CP-001／002／003／004／005／008／010、SAM-0007、
  ECUCert `…ECUCERT_API`／`…ECUCERT_SERVICE`。**無配對者不硬配**（§5 明令）。

---

## 6　§6 profile 定稿 ＋ `lint036.py`

### 6-1　profile

`docs/runtime/profiles/FW036_R1L_Security_Profile.md`（sha16 `328850d8769b2ff0`，268 行）——
標題改「**定稿**」；0 節之 `[PEI]` 全數改為 R-SEC 條號；
3.2 `[ADD §10.7(c)]` 六 token 逐字 ＋ `spec_mode F` 之 lint 判準；
3.4-a 代號與實作狀態；3.4-b `SS` 判準面偏差；常數 15 項（去雜訊後）。

### 6-2　**R-SEC13(b) 所要之代號占用表 —— `C` 已被佔用**

| 層 | 已占用代號 |
|---|---|
| `CHECK_ORDER`（全 feature 生效）| `A B C D E F G H I I-sibling J K L M N P` |
| `PROFILE_CHECKS`（任意 `--profile`）| `Q R T U V I-cross W X Y` |
| `FEATURE_CHECKS`（指名 profile）| `Z`（camera）|

**`C` = `hedge (test_item 括號下半)`，在 `CHECK_ORDER` 內對所有 feature 生效** ——
故 `_E §3` 所命名之 lint `C` **不可用**。
`lint036.py` 之代號為 dict key（字串），**多字元有既有先例**（`I-cross`／`I-sibling`），
依 R-SEC13(b) 之首選取 **`SC`**（channel）與 **`SS`**（source:）。
單字母剩餘只有 `O`／`S`（`O` 易與 `0` 混淆），**`SC`／`SS` 不佔用該空間**。

### 6-3　`lint036.py` 之變更（diff 摘要）

新增，**未刪未改任何既有行為**：

1. `CHECK_TITLES["SC"]`／`["SS"]`、`CHECK_STATUS["SC"]`／`["SS"]`、`CHECK_GRANULARITY["SC"]`／`["SS"]`
2. `FEATURE_CHECKS = {"camera": ["Z"], "security": ["SC", "SS"]}`
3. `SEC_*` 常數群 ＋ `security_remarks_idx()` ＋ `check_security_channel()` ＋ `check_security_source()`
4. `lint_sheet()` 內：`enabled = check_order(profile)` 提前求值（原為就地呼叫兩次）、
   `remarks_idx` 於 `"SS" in enabled` 時解析、兩個 `if ... in enabled` 之逐列呼叫

> **具名之小重構**：原 `lint_sheet()` 兩處各自呼叫 `check_order(profile)`，
> 本包改為求值一次存 `enabled` 並沿用。**行為等價**（同一函式、同一引數、無副作用），
> 回歸 sha 亦證其等價。

### 6-4　**回歸（§10 之硬條件）**

| 呼叫路徑 | before sha16 | after sha16 | 判 |
|---|---|---|---|
| `lint036.py pm_29.xlsx`（無 profile）| `e2ffac8eb9fd6838` | `e2ffac8eb9fd6838` | ✅ **byte 相同** |
| `--profile camera pm_29.xlsx` | `0bd777a9334105d1` | `0bd777a9334105d1` | ✅ |
| `--profile power pm_29.xlsx` | `45b56a6eeca86a29` | `45b56a6eeca86a29` | ✅ |

`tests/test_lint036_z.py` **27 項全過**。
`check_order()` 驗證：`SC`／`SS` **只在 `--profile security` 出現**，
於 `None`／`camera`／`power` 皆不出現。

**§10「既有呼叫路徑輸出 sha 改變」不成立。**

### 6-5　**`SS` 之判準面偏差（自報，待 Pei 覆核）**

R-SEC4(a) 原文為「於 **reasoning** 註明來源與位置」，reasoning 在生成側 JSON、
**不在工作簿**，而 `lint036.py` 之施檢面是 xlsx。
→ `SS` 改以 **`Remarks` 欄之 `source:` 標記**為判準面。
若 Pei 認為應對 JSON reasoning 施檢，`SS` 須移出 `lint036.py`、另掛生成側 selfcheck。

（實作上另有一處：`remarks` 不在 `FIELD_HEADERS` 內，`SS` 自行解析其欄索引，
**不動 `FIELD_HEADERS`** —— 動之會改變所有既有檢查之 `fields` 內容。）

---

## 7　§8 Pilot 10 TC

產物：`features/security/sandbox/pilot01/security_pilot01.xlsx`（sha16 `cc83c035182dbd95`）
＋ 10 個 `NR1L-*.json`。
產生器 `features/security/scripts/gen_pilot01.py`（sha16 `9917c96d2247417f`）。

**寫檔路徑**：`backend.xlsx_surgical.surgical_save`（R18-3；未用 openpyxl `wb.save()`）。
**結構驗證**：zip 成員 48 → 48；DV 計數逐 sheet 相同
（`sheet6.xml` 之 `(legacy 3, x14 1)` 保留），`differing` 只有 `xl/worksheets/sheet6.xml`。

### 7-1　**`N` 池取樣之替換（§8 約束）**

§8 表之六個 SWE1（CP-001／002／004／011、ECUCert R18、KI-011）之
`channel_feasible` **全為 `Y`**，故依 §8 約束以 batch 1 之 `N` 列替換 **#5** 與 **#10**。

batch 1 之 `N` 池共 5 列：`SAM-0001`、`SAM-0002`、`SAM-0004`、`SAM-0007`、`SAM-0019`。
其中 **`SAM-0001`／`SAM-0019` 在 D 群**（R-SEC11(b) DEFERRED，不產 TC），**不可取**。
可取者為 `SAM-0002`／`SAM-0004`／`SAM-0007`。

**替換與理由**：

| 原 | 改為 | 理由 |
|---|---|---|
| #5 `CP-004`（本地撤銷清單）| **`SAM-0004`** | 同時驗 **R-SEC5(a) amend 之命中列**（log 斷言）與 R-SEC7(c) 之 `LOG` 觀察面，並驗 `PENDING: DR-SEC-h` 之寫法 |
| #10 `KI-011` ERROR(3) sibling | **`SAM-0007`** | 同時驗 **R-SEC12 之人工覆寫列**與 **apk 配對 #04**（`samCertTestNormalFlow`），並驗 R-SEC8(h) 之「037 原文粒度」 |

**替換之代價（具名）**：本輪 Pilot 因而**未覆蓋** (1) CP-004 之撤銷清單路徑
（R-SEC8(f) 之 CRL 寫法與 `TERM:CRL|DCL` 標記）、(2) KI-011 之 `ERROR (3)` 負向 sibling。
二者建議列入 batch 1 正式產出之首批。

### 7-2　10 TC 一覽

| # | TC ID | SWE1 | Test Set | Design Method | 通道 | 觀察 |
|---|---|---|---|---|---|---|
| 1 | `NR1L-CP-001` | CP-001 | Chain Verification | Functional Based | HOST／ADB | HOST／RC／LOG |
| 2 | `NR1L-CP-002` | CP-001 | Chain Verification | Negative / Invalid | HOST／ADB | HOST／RC／LOG |
| 3 | `NR1L-CP-003` | CP-002 | Field Matching | Functional Based | HOST／ADB | HOST／RC |
| 4 | `NR1L-CP-004` | CP-002 | Field Matching | Negative / Invalid | HOST／ADB | HOST／LOG |
| 5 | `NR1L-SAM-001` | **SAM-0004**（`N` 池）| Error Handling | Negative / Invalid | ADB／**PHYS** | LOG／FILE |
| 6 | `NR1L-CP-005` | CP-011 | Field Matching | Functional Based | HOST／ADB | HOST／RC |
| 7 | `NR1L-CP-006` | CP-011 | Field Matching | Negative / Invalid | HOST／ADB | HOST／LOG |
| 8 | `NR1L-ECUC-001` | ECUCert R18 | Certificate Lifecycle | Functional Based | **UDS**／ADB | UDS／FILE |
| 9 | `NR1L-KI-001` | KI-011 | Install State | State Transition | ADB／**UDS** | FILE／RC／UDS |
| 10 | `NR1L-SAM-002` | **SAM-0007**（`N` 池）| AuthData Verification | Functional Based | HOST／ADB | HOST／RC |

七類通道中 **ADB／UDS／HOST／PHYS 四類**已在 Pilot 出現；`CAN`／`UI` 未出現
（`CAN` 只服務 SAM-0013／0017，`UI` 只服務 ECUCert Dealer App，皆不在本取樣內）。

### 7-3　lint 報告（`--profile security`）

`docs/fw036/lint_reports/security_pilot01__security_cc83c035_20260916.md`

```
A=0  B=0  C=0  D=0  E=0  F=0  G=0  H=0  I=0  I-sibling=0
J=0  K=0  L=0  M=0  N=0  P=0  Q=0  R=0  T=0  U=3  V=0
I-cross=10  W=2  X=0  Y=0  SC=0  SS=0
```

**R-SEC13(c) 之 Pilot 門檻 `P` ＋ `X` ＋ `SC` ＋ `SS` 全綠（0／0／0／0）。**
`Y`／`Z` 未啟用（`FEATURE_CHECKS["security"]` 未含 `Z`，見 8-2）。

**非 FAIL 之三項，逐項說明**：

| 檢查 | 行計 | 性質 | 說明 |
|---|---:|---|---|
| `U` | 3 | 計數用 | `PENDING: DR-SEC-h log keyword` ×3（`CP-004`／`CP-006`／`SAM-001`），見 7-4 |
| `W` | 2 | **待人裁非 FAIL** | `CP-004`／`CP-006` 之 ER 含 `differs from` 而 test_item 上半無數值 —— 負向路徑之「主體不符」本無數值可比，**建議判可** |
| `I-cross` | 10 | **警示器非判準** | **10 列全為「窗未完整宣告」**（`起 availability-check → 訖 未載`），**非跨列衝突**。該檢查源於 sw_update 之觀測窗慣例（R-SU33/34），Security 之 ER 為指令輸出斷言、無「窗」概念。**建議 Pei 裁：Security profile 豁免 `I-cross`，或另定窗之慣例** |

> 首輪 lint 曾得 `A=6 E=4 H=1 N=58 SS=10`，皆已修正：
> `A` 步驟句改不以 `Verify` 起首（`Run the OpenSSL chain verification of …`）；
> `E` 之 proc 步數與 ER 行數改為 **1:1**（R-SEC7(f) 本即要求）；
> `H` 之 `corresponds to` 改寫；`N` 去除行尾句號；
> `SS=10` 為**執行層 lint 實作之 bug**（`remarks` 不在 `FIELD_HEADERS`，恆取空字串），已修（6-5 末段）。

### 7-4　PENDING 計數（§8：預期 0，允許 `<…>` 佔位）

| 類 | 數 | 內容 |
|---|---:|---|
| `<…>` 佔位（R-SEC14(c)，**不計 PENDING**）| 4 | `1.3.6.1.4.1.57872.<…>` ×3、`62 29 65 <CSR bytes>` ×1 |
| **`PENDING: DR-…`** | **3** | `PENDING: DR-SEC-h log keyword`（`CP-004`／`CP-006`／`SAM-001` 之 ER）|

**三筆 PENDING 之歸屬與一處請裁**：

其依據為 **R-SEC7(e)** —— 「素材只給語意不給字串者，該 ER 寫 `PENDING: DR-SEC-h log keyword`」，
條文**逐字指定**此 token。三個場景（subject 不符、OID 不符、SAM AuthData 驗證失敗）
在 `Secure Log CS.212` 之第 1/7/10/11/12 項**皆無對應字串**，故無逐字可取。

**惟 `DR-SEC-h` 已由 R-SEC8(k) 記為 `CLOSED`**，其資產面改列 `EXEC_ASSETS.md` 之 **`X-h`**。
→ 以一個 CLOSED 之 DR 作 PENDING 理由，字面上矛盾。
**請 Pei 裁**：是否將 R-SEC7(e) 之 token 改為 `PENDING: X-h log keyword`。
執行層**照條文逐字寫，不自行改**。

§10 之「Pilot 任一 TC 出現 `PENDING`（非佔位）且**無法歸到** EXEC_ASSETS 或 DR」——
三筆皆可歸到 `X-h`（CS.212 正式 package），**條件不成立**。

---

## 8　DR／EXEC_ASSETS 現況（§7）

### 8-1　R-SEC14(a) 已落

- `DR-SEC-j2` → **`DR-SEC-r`**（`SWE1-SAM-0017` Verification Method 補值）。
  該列已依 R-SEC11(a) 入 Testing 群（CONVERT），**不阻產出**。
- 新增 **`DR-SEC-s`**（其餘五本 037 之 Polarion 匯出；59 列之 `nrl_swe1` 填 `-`）。
- 2.1 文件 DR = **`a`／`b`／`i`／`r`／`s`**（前三者已從寬結，仍送上游）。

### 8-2　R-SEC14(b) 已落

`step_assets.tsv` 由 156 → **152** 列（剔除 `/cfgs`、`/odm`、`/write`、SharePoint 片段）。
**`/odm/etc/cert_store` 為真路徑，未受影響**（只剔除裸 `/odm`）。

### 8-3　`EXEC_ASSETS.md` 不變

`X-c`~`X-h` 六項，sha16 `2d3239064b6275d7`。其「現況」欄仍為下放包與 workshop 之轉述，
**非執行層直接查證**（該檔已具名此盲區）。

---

## 9　升級條件逐項核對（§10 ＋ `_A` §3 修正）

| # | 條件 | 實測 | 判 |
|---|---|---|---|
| 1 | batch 1 實測 ≠ 49（±1 以外）| **49** | **不成立** |
| 2 | `D` 群 ≠ **19**（`_A` §3 修正）| **19** | **不成立** |
| 3 | **R-SEC5 命中 ≠ 5**（`_A` §3 新增）| **5** | **不成立** |
| 4 | `lint036.py` 回歸：既有呼叫路徑輸出 sha 改變 | 三路徑 **byte 相同** | **不成立** |
| 5 | Pilot 任一 TC 出現 `PENDING`（非佔位）且無法歸到 EXEC_ASSETS 或 DR | 3 筆，皆歸 `X-h` | **不成立** |
| 6 | token 六值與檔名推導不符 | 六值全吻合 | **不成立** |
| 7 | 任何需改 canon 之情形 | 未改 canon；`RULINGS.sha.tsv` 未動 | **不成立** |
| 8 | CS.00102 `Basic Report` ≠ 732／1.1 表 ID 查無（SEC-02 §10 承 `_D`）| 732；55/55 全在 | **不成立** |

**八項皆不成立，本包完成上繳，不升級。**

---

## 10　自報錯誤與偏差

| # | 事項 | 性質 | 處置 |
|---|---|---|---|
| 1 | **`git` 於本機損毀** —— 連 `git --version` 都回 Xcode license 錯誤 | 環境故障（於 `git worktree add` 後出現）| **本輪未執行任何 git 操作**；需 Pei 於終端機 `sudo xcodebuild -license`。本包產物全部落在工作樹，未 commit |
| 2 | `SS` 首版恆報（10/10）| **執行層 lint 實作 bug** —— `remarks` 不在 `FIELD_HEADERS`，`fields.get("remarks")` 恆為空 | 改為自行解析 `Remarks` 欄索引，不動 `FIELD_HEADERS` |
| 3 | `SS` 之判準面由 reasoning 改為 Remarks | **越出 R-SEC4(a) 字面** | 6-5 具名，待 Pei 覆核 |
| 4 | `lint_sheet()` 之 `check_order()` 由呼叫兩次改為求值一次 | 小重構，非下放包所令 | 行為等價，回歸 sha 為證（6-3）|
| 5 | Pilot 首輪 lint `A=6 E=4 H=1 N=58` | 執行層之 TC 書寫瑕疵 | 全數修正至 0（7-3）|
| 6 | §8 六個 SWE1 皆非 `N`，替換 #5／#10 | 依 §8 約束 | 替換為 SAM-0004／SAM-0007，理由與**代價**見 7-1 |
| 7 | `DR-SEC-h` 已 CLOSED 而 R-SEC7(e) 仍以其為 PENDING token | **條文之間不一致** | 照條文逐字寫，請 Pei 裁是否改 `X-h`（7-4）|
| 8 | `I-cross` 對 Security 10 列全報「窗未完整宣告」 | 檢查面不適用 | 建議豁免或另定窗慣例（7-3），**執行層不自裁** |
| 9 | 下放包 §1 記 `Basic Report` 12 列，實測 13 列 | 計數口徑差（含空列）| 資料列 11 相符，不影響 |
| 10 | `specification_reference` 內兩種拼字並存（`KeyInstall` token ＋ `KeyInsyall` ID）| R-SEC3(a)／R-SEC8(b) 之必然結果 | 具名備查，非錯誤（4-1）|
| 11 | Pilot 未覆蓋 `CAN`／`UI` 兩類通道 | 取樣範圍所限 | 具名（7-2）；建議 batch 1 正式產出時補 |
| 12 | 全測試套件有 9 項 failed | **既有失敗，非本輪造成** | 逐項為 `test_intake_scaffold`（`new_feature.py` 無 `--adopt-existing` 旗標）與 `test_single_write_path`（camera／time_management／user_profiles／vehicle_category／vehicle_setting 之 openpyxl save 基線），**無一提及 security 或 lint036** |

---

## 11　產物清單（sha256 前 16 碼）

| 檔 | sha16 |
|---|---|
| `features/security/RULINGS.md` | `b630a6130fe6231e` |
| `features/security/framework.md`（**LOCKED**）| `ba61681bad096c33` |
| `features/security/feature.yaml` | `2ccf6892c3695f69` |
| `features/security/DATA_REQUESTS.md` | `145df6a0fe521202` |
| `features/security/ANOMALIES.md` | `26ac0a6ba7ad3788` |
| `features/security/EXEC_ASSETS.md` | `2d3239064b6275d7` |
| `features/security/DECISIONS.md` | `2db6cb5b751a7c85`（未變）|
| `features/security/data/trace_matrix.tsv` | `e03637e0c8aad63b` |
| `features/security/data/trace_summary.md` | `053b7b5d3fcc1899` |
| `features/security/data/batch_order.tsv` | `d59a85ecc6fa553e` |
| `features/security/data/peer_review_rows.tsv` | `95ceddf95bd455a0` |
| `features/security/data/layer2_assign.tsv` | `9962f1b21ac85b5e` |
| `features/security/data/step_assets.tsv` | `2f9ab508ded20186` |
| `features/security/data/diag_items.tsv` | `e46ddd43c57c87a4`（未變）|
| `features/security/data/apk_pairing.tsv` | `a9718d57320af26e` |
| `features/security/scripts/build_trace_matrix.py` | `de5873c1d97f09b7` |
| `features/security/scripts/build_step_assets.py` | `eb884be5d466e453` |
| `features/security/scripts/gen_pilot01.py` | `9917c96d2247417f` |
| `features/security/sandbox/pilot01/security_pilot01.xlsx` | `cc83c035182dbd95` |
| `docs/runtime/profiles/FW036_R1L_Security_Profile.md` | `328850d8769b2ff0` |
| **`scripts/lint036.py`** | `0f5d91f675d88483` |
| `sources/MANIFEST.tsv` | 220 → **221** 列（`security` 27 列）|

**未動**：`scripts/` 其餘既有檔、`docs/fw036/` 之 canon 與 ledger、`features/` 下其他 feature、
`down/`／`up/` 既有檔、git。
