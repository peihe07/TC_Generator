# 上繳包 VS-SL-03 —— 綁別名重跑＋沙盒寫回稿＋DR 併稿

日期：2026-09-02　層級：Tier 1 → Tier 2
對應下放包：`down/20260902_VS-SL-03.md`（協定：`docs/fw036/handoff/PROTOCOL.md`）
**未動 `down/`、未動 `output/`、未動 `delivered/`、未動 `inputs/`、未動 `forms/`。**
寫回稿只落 `features/<slug>/sandbox/vssl/`。

---

## §1　產物路徑與 SHA256

### v3 報告（§1）

| 檔 | bytes | SHA256 |
|---|---:|---|
| `features/vehicle_setting/reports/vf230_settings_dryrun_v3.tsv` | 122,963 | `0f2bf0e86ad9e246638d2d8f188f06bf72e5bf686e4a691f543c81f8af8cbfaa` |
| `features/vehicle_setting/reports/vf230_settings_dryrun_v3.md` | 4,694 | `16dceaa6942a1038573aec433ef05513f504057d6ceeae525b12151402d20fe8` |
| `features/bed_lowering/reports/bl_settings_dryrun_v3.tsv` | 37,341 | `f49ec5d0b461ab50230f1790a8b6afad9b13b3851a815fd6bb6be3c59e15cf63` |
| `features/bed_lowering/reports/bl_settings_dryrun_v3.md` | 817 | `10ad92ea9a9484e8a150f1ebf1489982d0f68bef89ae2ff4ea0e971a2cf02ce6` |
| `features/vehicle_category/reports/vc_settings_dryrun_v3.tsv` | 20,944 | `8c21ad11681de0a080a492c2ab90c416b3125e902da4c4e3020b9ab6d4b274ef` |
| `features/vehicle_category/reports/vc_settings_dryrun_v3.md` | 748 | `7bd8061ef7511e93f84339d99b321d48b261d65f74dea05161525aeabf881544` |
| `features/vehicle_setting/reports/_v3_branch3.tsv` | 5,815 | `d04979e2f72c280b93f7f530695f79d8787c075db5cc5d5d5141e56e5045f90d` |
| `features/vehicle_setting/data/settings_alias.tsv`（44 名已升 `manual`） | 17,896 | `781ef0eabffc9bda29f70252e1df28f17b14a6e790c3a4644b2850584aec2415` |

### 沙盒寫回稿（§2）＋移除清單

| 檔 | bytes | SHA256 |
|---|---:|---|
| `features/vehicle_setting/sandbox/vssl/vf230_vssl.xlsx` | 187,731 | `7b2c4cec3f72479917a90aa15d7dbf1859c2a347a700229a98c055e11e575ea4` |
| `features/bed_lowering/sandbox/vssl/bl_vssl.xlsx` | 230,492 | `b0ee608d5469d31b36a067d0db63f7efd2b40c388e7d58e4fdd6f6a3286f28ec` |
| `features/vehicle_category/sandbox/vssl/vc_vssl.xlsx` | 231,828 | `3a1b5f401fada13ea39a79e85b8e849a24d1398f8831eba8b4470ec063f65541` |
| `features/vehicle_setting/reports/vf230_removed_non_nafta.tsv` | 2,440 | `741fed555ac7354911a733b4b02bad0a3adaf1992ca0d6895359692066d83498` |

### DR 併稿（§3）

| 檔 | bytes | SHA256 |
|---|---:|---|
| `features/vehicle_setting/reports/dr_draft_vssl.md` | 9,205 | `1ef0669221d76199c9c245d839197a751ffd4f2e15b703c371f2891b2e7b9f4e` |

### 腳本

| 檔 | bytes | SHA256 |
|---|---:|---|
| `features/vehicle_setting/scripts/vs_sl03_bind.py` | 4,052 | `7b8e0da1a71cae0649c34e90f423b9e8498d494c02969c5f7ff7991bb52c1442` |
| `features/vehicle_setting/scripts/vs_sl03_dryrun_v3.py` | 9,263 | `8bced74a6915510336d210096a8fefe3b46cb73bc053306916962becf7f59b98` |
| `features/vehicle_setting/scripts/vs_sl03_writeback.py` | 9,614 | `9e478600767511c59289cc16a75eb31b092e6a8395e5e30934b36be4df762fe4` |
| `features/vehicle_setting/scripts/vs_sl03_lint.py` | 4,266 | `15a966e67eed302415122f0628bc5c0bf8ab4aeaa64d57ff1e4b788334adb55f` |
| `features/vehicle_setting/scripts/vs_sl03_selfcheck.py` | 5,037 | `bf03dcd31263ad88eb3cb488821d5790e4d0f380fdcb99b13c547792ba4e6bf6` |
| `features/vehicle_setting/scripts/settings_lookup.py`（**已修，見 §5-1**） | 18,314 | `a397f1adc11ba948de9e9ad0f95e4a532430825e88b1547bab941ec5d3c27c97` |

---

## §2　§1 綁別名重跑

`settings_alias.tsv`（107 列）：**exact 51／manual 46／UNRESOLVED 10**
（manual 46 = Tier 2 之 44 ＋ v1 自判之 2；UNRESOLVED 10 = DR 之 9 ＋ drop 之 1）。

綁定**一律取自 `tier2_evidence` 之明指**，不以名稱相似度推算：
形式 A（`HMI rNNN`）22 名、B（`（rNNN）`）6 名、C（`Aux n > X`，n 取自 `SWITCH n`）12 名、
D（只給分類或標「待認」）4 名。**39／44 綁到 Settings 列**；
**5 名不綁 path**（`6 Aux Switches`、`AUX Switches`、`4 AUX Switches`、
`Rearview Camera Dynamic Guidelines`、`Trailer Number`）—— 其 evidence 逐字載
「對應物待認」或只給分類，本層不猜，掛 `ALIAS_MANUAL_NO_PATH`（15 列）。

| 項 | v2 | **v3** |
|---|---:|---:|
| `ALIAS_UNRESOLVED` | 179 | **43** |
| `path_proposed` | 79 | **240** |
| (3) 二來源皆空 | 30 | **105** |
| 分支 | 152／96／30／179 | **152／157／105／43** |

### 兩處與包內預期之差異（包內令「實跑為準，差異列明」）

1. **`ALIAS_UNRESOLVED` 為 43，非「約 3」。**
   包內之 179 − 176 = 3 係列數相減，惟該 176 列並非全在 (4)：其中 **40 列 `proxi_now` 非空**，
   依 §2.1 優先落 **(1) 形制改寫**，本就不掛此旗。實際落 (4) 者 136 列，179 − 136 = **43**。
   43 列即 DR 之 9 名與 drop 之 1 名所涉。

2. **(3) 由 30 升至 105。** v2 之 30 列為 v3 之**真子集**（`v2 有而 v3 無 = 0`）。
   新增 75 列全為「原本卡在 (4)、綁定後才第一次被真正評估」之名：
   `SWITCH 1–6 Type／Power Mode／Hold Last State`（18 名 54 列）、
   `Park Sense Front/Rear Volume`（8 列）、`Suspension Default Ride Height／Service Mode`（6 列）、
   `Navigation Turn by Turn／Phone Repetition`（6 列）、`Hour Mode`（1 列）。
   **成因**：此族之條件在需求原文中以 **CAN 訊號**表述（`IPC_VEHICLE_SETUP2.AUX5_PWRMD` 等），
   非 `$var$ = [label]`；且 SWITCH 一族於总控表無對應列。依 (3) 落 `PENDING`，不猜值。

   > **提請 Tier 2 裁**：此 105 列之 `PENDING` 所指之問題（「別名已解，但兩來源皆未載其 PROXI 條件」）
   > 與 DR 稿之三節**均不同**。DR 稿依包內 §3 只寫三節，**未擅自擴充**。是否另立一節或另開 DR，請裁。

---

## §3　§2 沙盒寫回稿

**來源現本之差異**：`features/vehicle_setting/` **無 `output/`**（且 `output/` 在其 `.gitignore` 內），
故 vf230 依 `inputs/` 之現本複製；BL／VC 依包內所指之 `output/` 複製。

| 本 | 列 前→後 | Pre 改 | Proc 改 | ER 改 | 合計 |
|---|---|---:|---:|---:|---:|
| vf230 | **457 → 438** | 412 | 343 | 343 | **1,098** |
| bl | 151 → 151 | 151 | 0 | 2 | 153 |
| vc | 126 → 126 | 71 | 0 | 0 | 71 |

vf230 之改動：非 NAFTA **19 列移除**（清單另檔，含 D 欄與原列號）、
PROXI 形制改寫（**嚴格式舊形制殘留實測 0**）、`NEG_CONTRA` 3 列改 `= 0 (Absent)`、
分支 (2) 補 PROXI 行、分支 (3) 寫 `PENDING`、有真路徑者補導覽與控件句（Options 依 §2.4 正規化）。
BL：151 列加 node 27 行、`Body_Types` 行 PENDING、**車型欄不填**（待 DR）。
VC：可對應者補 PROXI（71 列）、`PROSE_KEPT` 16 列不動、`Camera App` 2 列 PENDING。

### 硬規 lint（`vs_sl03_lint.py`）

| 本 | 列 | `test_item` 無括號下半 | 尾句號 | 設定項未加雙引號 | |
|---|---:|---:|---:|---:|---|
| vf230 | 438 | 0 | 0 | 0 | **PASS** |
| bl | 151 | 0 | 0 | 0 | **PASS** |
| vc | 126 | 0 | 0 | 0 | **PASS** |

`Procedure` 與 `Expected Result` 之步數逐列相等 —— **由構造保證**（同索引插入同長度之導覽段），
且落檔前 assert；來源本實測 457 列 0 筆不一致。

---

## §4　自檢

| # | 項 | 結果 |
|---|---|---|
| 1 | 三本報告列數 457／151／126 | **PASS** |
| 2 | assert `PROXI_PENDING` ∩ `ALIAS_UNRESOLVED` = 0 | **PASS**（交集 0） |
| 3 | assert `proxi_now` 非空而提議 PENDING = 0 | **PASS**（違反 0） |
| 4 | 抽 3 條貼 proposed 全文 | **PASS** |
| 5 | 沙盒三本硬規 lint | **PASS** |

抽驗之第三條改取 r14（`SWE1-VC-SWITCH5PowerMode-023`，本輪新綁），沙盒稿逐字為：

```
Procedure
1. Send CAN: IPC_VEHICLE_SETUP2.AUX5_PWRMD = 1 (BATTERY)
2. Send CAN: IPC_VEHICLE_SETUP2.AUX5_PWRMD = 0 (IGNITION)
3. Press "Settings" on Menu Bar
4. Select "Aux Switches"
5. Select "Aux 5"
6. Read the Vehicle Settings menu and check that the "SWITCH 5 Power Mode" setting is displayed as IGNITION

Expected Result
1. IPC_VEHICLE_SETUP2.AUX5_PWRMD = 1 (BATTERY) is sent
2. IPC_VEHICLE_SETUP2.AUX5_PWRMD = 0 (IGNITION) is sent
3. The Settings screen is displayed
4. The "Aux Switches" page is displayed
5. The "Aux 5" page is displayed
6. The "SWITCH 5 Power Mode" setting is displayed as IGNITION
```

---

## §5　自報錯誤

1. **`settings_lookup.load_settings` 之容器判準有誤（v1 起既有，本輪修正）**
   原只認 D 欄為 `>` 者為第一層容器。實測 `21. Aux Switches` 之 `Aux 1`–`Aux 6`（r565–r588）
   **D 欄為空**卻確有 `N.M` 子項，致其子項之 `parent` 落空、路徑少一層。
   已改為「D 欄為 `>` 或為空者皆令其名成為後續 `N.M` 列之 parent」。
   **攤平後項數不變（517）**，逐項比對「少掉 0、多出 0」，只有 `parent` 改對；
   FCW 路徑複驗不變。另更正：v1 §A 所載「攤平 519」係 `--list-settings` 之輸出**行數**
   （選項字串含換行），**正確數為 517**。

2. **本輪落檔前更正之三處自造缺陷**
   - `rewrite_pair` 初版之 `Expected Result` 錨點與 `Procedure` 不對稱，
     致 r14 出現「Procedure 6 步 vs Expected 3 步」。已改為同索引插入，長度由構造保證。
   - 控件句初版誤套在**本層新造之導覽行**上，生出
     `The Settings screen is displayed as 3 radio buttons …` 之誤句。已改為只套原有之行。
   - `path_proposed` 初版未辨真偽，BL／VC 之欄值為說明字串「（本 feature 不套 path 段）」，
     被當成路徑而在**全部 277 列**塞入假的 `Press "Settings" on Menu Bar`。
     已加 `is_real_path()`（須以 `Settings > ` 起首且至少三節）；重跑後 BL Proc 改 0 列、VC Proc 改 0 列。

3. **一次量測失誤（未影響產物）**　曾以寬鬆式 `"is set to" in Pre` 判舊形制殘留而得 189，
   誤以為改寫不全。以嚴格式 `PROXI $…$ is set to` 複量為 **0**；189 為
   `The "X" customer setting is set to BATTERY` 之類的無關句，本就不該改。

4. **DR 稿附帶回報之二處上游疑似筆誤**（已寫入 DR 稿，非本層自造）
   `SWE1-VC-WarningsforLowFuelInverterShutdown - **Visual**Warning-121` 之內容為 Audible；
   `SWE1-VC-MaxPowerLevel140` 缺序號前之連字號。

5. **`gate_all.py`：`canon_refs` 由 503 升至 505，其餘三支同數**
   逐檔移除法定位：**+2 全部來自交換包文面之裸節號歧義**（`§x` 未標 FO／IN，
   `8.1` 等 17 個節號為兩 canon 共用）——
   一來自 Tier 2 之 `down/20260902_VS-SL-03.md`（`canon §8.1`），
   一來自本協定新增之上繳／下放文面。
   **本輪之產物逐檔實測各自 +0**：v3 三本報告、`_v3_branch3.tsv`、移除清單、
   `settings_alias.tsv`、五支腳本、三本沙盒稿，移除任一者計數皆不變。
   `lint_paths` 仍為 4（`driver_distraction/workbook/` 兩檔＋ICS／SWUpdate `delivered/` sha），
   **沙盒三本落於 `sandbox/`，屬合法落點，未增紅**。
   `rulings_hash` 本層仍未重跑（重生指紋屬治理動作，且會與既有紅混在一起）。
