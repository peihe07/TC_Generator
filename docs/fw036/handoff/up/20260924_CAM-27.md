# 上繳包 CAM-27 —— 收尾：6-87 落定、R-CAM19(f)、`-087` 車型補正、RDF-18／GCB-13、B dryrun4、`DELIVERY_STATUS.md` 終態

對應下放包：`docs/fw036/handoff/down/20260924_CAM-27.md`（審閱 `down/20260924_CAM-26_review.md` 同發）。
執行層 session：延用 CAM-26。日期 2026-09-24。**本包後 Camera 生成階段正式結束。**

**一句話**：§1 六項 `[DECIDED]`、**6-88**、立 **R-CAM19(f)**（sha8 `6f14d226`）、**RDF-18**／**GCB-13** 落檔、A-CA38 補責任句、**A-CA39 RESOLVED**；
§2 `NR1L-RVCHMI-087` 之 637／2261／376 → `0`（json 三欄 ＋ reasoning 末句），lint／selfcheck **ERROR 全 0**；
§3 B dryrun4 **216 列**，對既有列只 **3 格**差異（即 `-087` 之 V／Y／Z），lint 計數與 dryrun3 全同，對帳 203/203；
§4 `DELIVERY_STATUS.md` 終態、§9 例外條款關閉。**兩項升級條件皆不成立**。TC 數不變（465）。未寫 `delivered/`、未 tag。

---

## §1　落檔

| 檔 | 項 | sha256_16 |
|---|---|---|
| `features/camera/DECISIONS.md` | **6-87** `[PROPOSED]` → `[DECIDED]`，附審閱 §一 之裁決表；**6-88** R-CAM19(b) 之射程 ＝ 原零 TC 之列 | `ec5e19d9cea7f78e` |
| `features/camera/RULINGS.md` | **R-CAM19(f)**（同級錨自立，逐字照錄下放包 §1-2）＋ 執行層落實一段 | `ba551d278fa76f6d` |
| `features/camera/RD_FEEDBACK.md` | **RDF-18** 全節 ＋ 索引列 | `9b2c7f6c41bba3eb` |
| `docs/fw036/handoff/GC_BACKLOG.md` | **GCB-13**（selfcheck 第 11 項：一列多點／多觸發，WARN，下一 feature pilot 前實作）| `062bca0084c61d9e` |
| `features/camera/ANOMALIES.md` | **A-CA38** 補「(1)(2) 為分析層之錯」並轉 RESOLVED（6-87 已裁）；**A-CA39** → RESOLVED | `144dcfa190ddef4b` |

### 1-1　R-CAM19(f) 之指紋

`rulings_hash.py --target features/camera/RULINGS.md` 前後比對（31 → 32 錨點）：

| 條 | sha8（全本體）| fenced 本體 |
|---|---|---|
| **R-CAM19(f)** | **`6f14d226`** | `be1d9fd6` |
| R-CAM19 | `8f741740`（**不變**）| `a1727b36`（不變）|
| 其餘 30 錨點 | 全不變 | 全不變 |

同級錨自立（GCB-06），故原條 R-CAM19 之兩個 sha 皆不動。

---

## §2　`NR1L-RVCHMI-087` 補正

### 2-1　逐列 diff（`generated/b02b/NR1L-RVCHMI-087.json`，sha256_16 `aab3b00d67db381e`）

```
@@ vehicle_model
-    "VF(ProMaster)637": "1",
+    "VF(ProMaster)637": "0",
-    "Toro(2261)": "1",
-    "Fastack (376)": "1"
+    "Toro(2261)": "0",
+    "Fastack (376)": "0"
@@ reasoning（末附一句）
+ **車型補正（CAM-27 §2，A-CA39）**：前提所用之 `Digital_CHMSL_Camera_Prsnt` 於 `forms/proxi/` 六本之實測
+ 為 HDCC27 兩本／DT27 各 1、637／2261／376 各 0 → 依 **R-CAM18(b)** 637、2261、376 改判 `0`（原為 `1`），
+ 與 `-090`／`-216` 一致。
```

`test_item`／Procedure／ER／`specification_reference` 等其餘欄**不動**。`render_tc.py` 重生 b02b 25 檔，**只有 `-087.md` 有差異**
（其 Vehicle Model 行與 reasoning）；其餘 24 檔逐位元相同。

同步之附屬文字（非 TC 列）：`b02b/INDEX.md` 之 `-087` 列車型、`b02b_plan.tsv` 之 `vehicle_model_1`（`6f78da18b1c10fc4`）、
`bench_verify.md` 之 `-216` 列說明（`b0bd5a0d952c8e6c`）、`b08/INDEX.md` 之出入說明。

### 2-2　lint 與 selfcheck（**升級條件 §6-1 不成立**）

```
lint036 --profile camera  b02b（25 列）  ERROR 類全 0｜U 7／I-cross 25／X 3 —— 與改前相同；LINT_REPORT.md 重生後逐位元無差異
selfcheck_camera.py       二十二目錄（TC 465 筆）  ERROR 級十項全 0｜錨 468／常數錨 2／共引錨 3／缺件佔位 1｜第 10 項 WARN 41（不變）
render_tc.py --verify     二十二目錄 465 份  相符 465／相異 0
```

---

## §3　B dryrun4

```
features/camera/sandbox/b_dryrun/FM-WI-FSM-036-A01 STLA 測試用例規範與結果_SWQT STLA Test Case Specification & Result_SWQT_RearViewCamera_B_20260924_dryrun4.xlsx
sha256  a279e6fa2106541b5d7b7162a1a489f1488ef5d450df02811fb56ed5bfd87c1a
資料列 216（TC 216，D 欄佔位 0）
```

組法同 CAM-26 §3-1：以 **B dryrun3** 為底、列序沿之、全部 TC 列自 json 重寫並逐格比對 —— **既有列之差異 3 格**：
`NR1L-RVCHMI-087` 之 `V`／`Y`／`Z`（`1` → `0`），此外 0 格。封面只改版次字樣（`dry-run v4`）。**A dryrun4 不動。**

### 3-1　lint 全表

```
行計 A=0 B=0 C=0 D=0 E=0 F=0 G=0 H=0 I=0 I-sibling=0 J=0 K=0 L=0 M=0 N=0
     P=0 Q=0 R=0 T=0 U=45 V=0 I-cross=216 W=0 X=23 Y=0 Z=0
報告 docs/fw036/lint_reports/RearViewCamera_B__camera_a279e6fa_20260924.md
```

**ERROR 類全 0**，各項計數與 dryrun3 **逐項相同**。

### 3-2　sheet 級與對帳（**升級條件 §6-2 不成立**）

| 項 | 實測 |
|---|---|
| 列數／相異 TC | 216 ／ 216 |
| D 欄升冪（`(母號, 子號)`）| 成立 |
| D 欄佔位列 | 0 |
| 序號 `B` 連續 | 成立 |
| `AH` remarks | 8 |
| VM 七欄非 0/1 | 0 |
| 有 TC 之 leaf | **203/203**；每 leaf 之 `tc_ids` 與 `coverage_b.tsv`、`traceability.tsv` 逐列相等；工作簿 D 欄 ⊆ leaf 母體 |

`pending_by_dr_b.tsv` 重生：與 CAM-26 版**逐位元相同**（`163c94c1927fc2ec`；車型不入 PENDING 之計）。
`traceability.tsv` 只改首行之母體註記（B dryrun3 → dryrun4），資料列不變（`2aa8b703b037b03f`）。

---

## §4　`DELIVERY_STATUS.md` 全文（sha256_16 `a1c3efb38bd911e4`）

````markdown
# Camera（FW036 R1L）—— 交付狀態

**一頁式閘表**，供 Pei 作 Phase 7 之依據。日期 2026-09-24（CAM-24 §4 建檔；CAM-26 §3 更新；**CAM-27 §4 終態**）。
日後 DR 結案／bench 驗畢時由執行層更新本檔，並於該輪上繳包記其新 sha。

---

## 1　兩本候選

| 本 | 檔（`features/camera/sandbox/`）| 列數 | TC | D 欄佔位 | sha256 |
|---|---|---:|---:|---:|---|
| **A** | `a_dryrun/…_RearViewCamera_A_20260924_dryrun4.xlsx` | **249** | 249 | 0 | `a4ad6cf3112d5e31…` |
| **B** | `b_dryrun/…_RearViewCamera_B_20260924_dryrun4.xlsx` | **216** | 216 | 0 | `a279e6fa2106541b…` |
| | **合計** | **465** | **465** | 0 | |

**工作簿列數 ＝ TC 數**（**R-CAM19(e)**：D 欄佔位列形制作廢）。
**兩本分交**（**R-CAM1(a)**）；合併與否由 Pei 定。
交付檔名依 workflow 模板事實：`…_SWQT_RearViewCamera_A_<YYYYMMDD>.xlsx`／`…_B_<YYYYMMDD>.xlsx`
（`delivered/` 只放客戶檔名定稿、無 `dryrun` 尾綴，**R-G42 五**）。
候選沿革：A 本 dryrun2（244 列）→ dryrun3（249 列，6-78）→ **dryrun4**（CAM-26）；
B 本 dryrun2 → dryrun3（CAM-26）→ **dryrun4**（CAM-27：`NR1L-RVCHMI-087` 之車型補正，三格）。

## 2　TC 總數與來源覆蓋

| 本 | 037 列 | leaf | **有 TC 之 leaf** | umbrella | **TC** |
|---|---:|---:|---:|---:|---:|
| A（SWRA_V02）| 25 | 25 | **25/25** | 0 | **249** |
| B（RVC-HMI_V0.1）| 230 | 203 | **203/203** | 27 | **216** |
| | | **228** | **228/228** | | **465** |

**每條 leaf 需求 ≥ 1 TC：A 25/25、B 203/203**（**R-CAM19(a)**，Pei 2026-09-24「有需求就要有產出」）。
逐列對照：`data/traceability.tsv`（228 leaf → tc_ids）；umbrella 27 列另表 `data/traceability_umbrella.tsv`
（037 `Categorization = Heading`，不入 R-CAM19(a) 之母體，其子 leaf 皆有 TC）。
**R-CAM19(f)**：已有 TC 之需求，其與他列共引之來源不再各自出 TC（§8.2.1）—— 補生成之射程止於原零 TC 之十列。

**A 本之 1:10**（25 SWE 列 → 249 TC）因其拆解全在執行層；
**B 本之 1:1.06**（203 leaf → 216 TC）因 037 已自行前拆 39.1%（**DECISIONS 6-77**）。

### 原零 TC 之十列（R-CAM19 後各出一列）

| 原形制 | 原條（已撤銷／修訂）| 列 | 本輪之 TC |
|---|---|---|---|
| 全來源委派 | R-CAM16 → R-CAM19(b)(c) | `SWE-CAM-007`／`-013`／`-025` | `NR1L-RVC-246`／`-247`／`-249` |
| BLOCKED（缺件）| 6-52 → R-CAM19(d) | `SWE-CAM-024` | `NR1L-RVC-248`（六欄 `PENDING: DR-CAM-a`）|
| 來源自陳 `N/A` | R-CAM16(b) → R-CAM19(c) | `SWE1-RVC-069` | `NR1L-RVCHMI-211` |
| 逐字重複／純交叉引用 | R-CAM16(c) → R-CAM19(c) | `-086`／`-091`／`-102`／`-136`／`-139` | `NR1L-RVCHMI-212`～`-216` |

## 3　PENDING ↔ DR（**Phase 7 之閘**）

**13 條阻交付**，共 **81／465 列**（A 46／B 35）：

| DR | 題 | A 列 | B 列 | 合計 |
|---|---|---:|---:|---:|
| DR-CAM-a | SYS2 VF617_V5 缺件 | 1 | 0 | 1 |
| DR-CAM-f | 缺件之 DBC 三類（`BED_EXTENDER`／HDCC27 之 FD-CAN8 全本 等）| 10 | 0 | 10 |
| DR-CAM-g | HMI 入口路徑之逐字來源（Controls page／Status bar Shortcut menu）| 5 | 7 | 12 |
| DR-CAM-h | Pop Up List 之 camera 相關缺件（out-of-position／`connecting`）| 1 | 4 | 5 |
| DR-CAM-i | `LTM_OperationalModeSts` ↔ `CmdIgnSts` 值對應表 | 4 | 0 | 4 |
| DR-CAM-j | Auxiliary Cameras 之訊號定義（四項）| 7 | 0 | 7 |
| DR-CAM-k | VF664 全文件 | 1 | 0 | 1 |
| DR-CAM-m | EVS HAL 支援之影像格式 | 1 | 0 | 1 |
| DR-CAM-p | LVDS 訊息之傳送週期 | 2 | 0 | 2 |
| DR-CAM-q | DTC 之識別碼 | 14 | 0 | 14 |
| DR-CAM-r | `Camera App`／`Enhanced Camera App` 配備旗標 | 0 | 26 | 26 |
| DR-CAM-s | 熱保護關顯示之進入法 | 0 | 1 | 1 |
| DR-CAM-t | LVDS `diagnosticRequest`／`diagnosticResponse` 之訊息定義（CAM-26 新開；**由 Pei 送**，CAM-26 審閱 §一-2）| 1 | 0 | 1 |
| **合計（相異 TC 列）** | | **46** | **35** | **81** |

**DR 共 20 條**（`DR-CAM-a` ～ `-t`）；阻交付 13 條，其餘 7 條（b／c／d／e／l／n／o，含已結案者）於現行 465 列無 PENDING。
A 本 Test Set 由 9 組增為 **10 組**（`AUX Camera` 1 列，即 `-248`）。
逐列對照：`data/pending_by_dr.tsv`（A）／`data/pending_by_dr_b.tsv`（B）。

**Pei 之裁**（三選一，可逐 DR 不同）：① 結案 ② 裁降 NA ③ 帶 PENDING 不出貨。
**R-G42 七**：`delivered/` 內 PENDING 須為 **0**；例外須有 R- 號記入 MANIFEST note。

## 4　實機驗證

`features/camera/data/bench_verify.md` —— **105 列**
（A 本 11 ＋ batch05 4 ＋ batch06 2、B01a 3、B01b 8、B02a 11、B02b 12、B03 16、B04a 10、B05 20、B06 6、b08 2）。
非缺陷清單；其 lint 與 selfcheck 皆已全綠，此處記「紙面可判、實機待證」者。

## 5　RD 回饋

**RDF-01 ～ RDF-18，18 條**（`features/camera/RD_FEEDBACK.md`）。**RDF-18**（SYS1 §30.1.3 `NRL-188130` 之 `or‘X’to` 缺空格）
於 CAM-27 落檔；**RDF-12** 於 CAM-26 改題「`SWE1-RVC-069` 自陳 N/A 但仍須追溯」。
**回 RD 作者為交付前置**（不阻 Phase 7）。

## 6　全域待辦

**GCB-01 ～ GCB-13，13 項**（`docs/fw036/handoff/GC_BACKLOG.md`），皆 **OPEN**。
**GCB-13**（selfcheck 第 11 項「一列多點／多觸發」，WARN，下一 feature pilot 前實作；Camera 不回跑）於 CAM-27 落檔。
其中 **GCB-01**（`gates_tsv.py` 未登 `Z` 檢查）於註記「**Camera 交付前必須完成**」；
**GCB-04**（`RULINGS.sha.tsv` 未重生）與 **GCB-11**（lint `X` 之入口清單）為非本線可改。

## 7　品質狀態（全 465 列）

```
selfcheck_camera.py   ERROR 級十項  全 0（錨 468／反查失敗 0／常數錨 2／共引錨 3／缺件佔位 1）
                      第 10 項（WARN）候選 41（8.8%），已知以偽陽為主（6-80 不逐列覆核）
lint036 --profile camera
  A dryrun4（249 列）  ERROR 類全 0｜J 37（profile §5.1 逐字豁免）／U 65／I-cross 249／X 3
  B dryrun4（216 列）  ERROR 類全 0｜J 0／U 45／I-cross 216／X 23（GCB-11）
render_tc.py --verify  二十二目錄 465 份 md 逐位元相符 465／相異 0
```

「共引錨」（R-CAM19(b)）與「缺件佔位」（R-CAM19(d)）為 CAM-26 於 selfcheck 第 1 項新增之計數類別，非違反。

## 8　Phase 7 前置條件

1. §3 之 13 條 DR **結案或裁降 NA**（**R-G42 七**）。
2. §4 之 **105 列實機驗證**（其結果可能改寫 ER 或新增 PENDING）。
3. **GCB-01** 完成（`Z` 檢查入閘登錄簿，R-G56）。
4. Pei 之准：`--write`／tag／寫 `features/camera/delivered/`；
   內容物依 **R-G42 六**：xlsx ＋ MANIFEST ＋ DELIVERY_NOTE ＋ 未結 DR 清單 ＋（PARTIAL）tc_id 對照表。
5. 兩本分交或合併之決定（**R-CAM1(a)** 為兩本）。

## 沿革

- 2026-09-23 建檔（CAM-24 §4）。A 245 ＋ B 206 ＝ 451 TC；11 條 DR 阻交付。
- 2026-09-23 更新為**終態**（CAM-25 §4）。B 本形制補正 ＋4（`b07/`，**A-CA37**），
  A 245 ＋ B 210 ＝ **455 TC**；B 候選改指 `dryrun2`。
- 2026-09-24 **R-CAM19 追溯補齊**（CAM-26，§9 之「下放包明文指派」）。原零 TC 十列各出一列（`batch06/` 4、`b08/` 6），
  A 249 ＋ B 216 ＝ **465 TC**，D 欄佔位 10 → 0；A 候選改指 `dryrun4`、B 改指 `dryrun3`；
  阻交付 DR 11 → 13（DR-CAM-a 之 `-248` 由佔位列轉為 TC 列、DR-CAM-t 新開）。
  A dryrun4 另帶入 CAM-25 已改而 dryrun3 未含之 `NR1L-RVC-245` `Remarks`（6-85）。
- 2026-09-24 **終態**（CAM-27）。6-87 落定、立 **R-CAM19(f)**（6-88）；`NR1L-RVCHMI-087` 車型補正（A-CA39 RESOLVED）；
  B 候選改指 `dryrun4`；RDF-18／GCB-13 落檔。TC 數不變（465）。

## 9　生成階段結束

**Camera 之 TC 生成階段結束於 2026-09-24（CAM-27）**（下放包 CAM-01 ～ CAM-27，執行層 27 輪；
CAM-26 為 R-CAM19 之追溯補齊、CAM-27 為收尾）。**CAM-26 時開立之「下放包明文指派」例外條款自此關閉。**
本檔自此**只因下列三者變更**：

1. **DR 結案或裁降 NA** —— §3 之 13 條；結案時該列之 `PENDING` 改為實值、本檔重算；
2. **Pei 之實機回饋** —— §4 之 105 列；其結果可能改寫 ER 或新增 PENDING；
3. **Phase 7 之執行** —— §8 之五項前置齊備後，寫 `features/camera/delivered/` 並 tag。

**不再因生成而變更。**
````

---

## §5　R-CAM17 證據表

| # | 主張 | 掃描字串／判準 | 母體 | 命令 | 命中 |
|---|---|---|---|---|---|
| 1 | `-087` 之 json 只改 VM 三欄與 reasoning | 行級 diff | `NR1L-RVCHMI-087.json` | `git diff -U0` | 3 個 hunk：VM 637、VM 2261＋376、reasoning |
| 2 | 其餘既有 464 列 0 改動 | — | `features/camera/generated/` | `git status --short` | 只有 `b02b/NR1L-RVCHMI-087.{json,md}` 與兩份 `INDEX.md`；其他 TC 檔 0 |
| 3 | B dryrun4 對 dryrun3 只 3 格差異 | 逐格 `!=` | 216 列 × 25 欄 | openpyxl | **3**（`-087` V／Y／Z）|
| 4 | `or‘X’to` 為單一 token（RDF-18）| `‘X’` | cache 本 §30.1.3 `NRL-188130` 之 `Description` | `str.split()` | 23 token 中含 `‘X’` 者 **1**：`or‘X’to` |
| 5 | R-CAM19(f) 同級錨自立不動原條 | `rulings_hash.py` 之 sha8／body_sha8 | `RULINGS.md` 32 錨點 | 前後兩次 `--target` 比對 | 只新增 R-CAM19(f) 一列；其餘 31 錨點兩個 sha 皆不變 |
| 6 | 追溯無 0 | `tc_count = 0` | `traceability.tsv` 228 列；B dryrun4 216 列 | csv ＋ openpyxl | **0**；B 203/203 |
| 7 | 含 PENDING 之 TC 列數 | 五欄（Pre／Input／Proc／ER／Spec）任一行含 `PENDING: DR-CAM-x`，或 `= PENDING (` 之賦值佔位 | A 十二目錄 249 份 json／B 十目錄 216 份 | `pending_by_dr.py`（先以 CAM-24／25 之母體重現其 43／34 全同，再施於新母體）| **A 46／B 35**，合 81 |

---

## §6　自報

1. **審閱 §三 之「含 PENDING 之 TC：A 47／B 34」與實測不合** —— 實測 **A 46／B 35**（證據 7；合計同為 81）。
   A 為 CAM-25 時之 43 ＋ `-247`／`-248`／`-249`；B 為 34 ＋ `-214`（`PENDING: DR-CAM-h`，CAM-26 §2-1 已列）。
   `DELIVERY_STATUS.md` §3 依實測書寫。
2. **A-CA38 一併轉 RESOLVED** —— 下放包 §1-4 只令「補分析層責任一句」；惟其所提之事項已由 6-87 全數裁定，狀態留 OPEN 將與 6-87 矛盾，故併轉。如不宜，可逆。
3. **`b08/INDEX.md`（CAM-26 產物）之出入說明同步改寫** —— 原句「母列不改，登 A-CA39」已不成立。
4. `docs/fw036/RULINGS.sha.tsv` 仍未重生（**GCB-04**）。

---

## §7　未結（只餘 Pei／RD／GC）

| # | 項 | 歸屬 |
|---|---|---|
| 1 | **13 條阻交付 DR**（81／465 列；A 46／B 35）—— 結案／裁降 NA／帶 PENDING 不出貨；DR-CAM-t 由 Pei 送 | **Pei** |
| 2 | `bench_verify.md` **105 列**實機驗證 | **Pei** |
| 3 | **Phase 7**（`--write`／tag／`delivered/`）—— 五項前置見 `DELIVERY_STATUS.md` §8；兩本分交或合併 | **Pei** |
| 4 | **RDF-01 ～ RDF-18** 回 RD 作者 | **RD** |
| 5 | **GCB-01 ～ GCB-13**；GCB-01「Camera 交付前必須完成」 | **GC 線** |

**執行層側之生成工作至此無未結項。**

---

## §8　落檔清單

```
M  features/camera/DECISIONS.md                         6-87 [DECIDED]；6-88            sha256_16 ec5e19d9cea7f78e
M  features/camera/RULINGS.md                           R-CAM19(f)                      sha256_16 ba551d278fa76f6d
M  features/camera/ANOMALIES.md                         A-CA38 責任句；A-CA38／39 RESOLVED  sha256_16 144dcfa190ddef4b
M  features/camera/RD_FEEDBACK.md                       RDF-18                          sha256_16 9b2c7f6c41bba3eb
M  docs/fw036/handoff/GC_BACKLOG.md                     GCB-13                          sha256_16 062bca0084c61d9e
M  features/camera/DELIVERY_STATUS.md                   終態；§9 例外條款關閉            sha256_16 a1c3efb38bd911e4
M  features/camera/generated/b02b/NR1L-RVCHMI-087.{json,md}   車型補正                  json sha256_16 aab3b00d67db381e
M  features/camera/generated/b02b/INDEX.md              -087 之車型
M  features/camera/generated/b08/INDEX.md               出入說明同步
M  features/camera/data/b02b_plan.tsv                   -087 之 vehicle_model_1         sha256_16 6f78da18b1c10fc4
M  features/camera/data/bench_verify.md                 -216 列說明                     sha256_16 b0bd5a0d952c8e6c
M  features/camera/data/traceability.tsv                首行母體註記                    sha256_16 2aa8b703b037b03f
A  features/camera/sandbox/b_dryrun/…_B_20260924_dryrun4.xlsx   216 列                 sha256 a279e6fa2106541b…
A  docs/fw036/lint_reports/RearViewCamera_B__camera_a279e6fa_20260924.md
```

**未動**：A dryrun4 與其餘 dry-run 各檔、`-087` 以外之既有 464 列、`pending_by_dr*.tsv`（重生後逐位元同）、
`selfcheck_camera.py`、`framework.md`、profile、`delivered/`、`features/comfort/`（併行 session）。
