# 上繳包 CAM-16 —— RDF-11／profile §7.7／DECISIONS 6-56～6-58、B02a 31 leaf（產 30 TC）

對應下放包：`docs/fw036/handoff/down/20260923_CAM-16.md`（審閱 `down/20260923_CAM-15_review.md` 同發）。
執行層 session：延用。日期 2026-09-23。

**一句話**：§1 三項落檔完成；B02a 依下放包 §2 末之指示**併入 §27.6.1 家族**取 **31 leaf**，
產 **30 TC**（`NR1L-RVCHMI-044`～`-073`），selfcheck 十項全 0、lint ERROR 類全 0；
**`SWE1-RVC-069`（§27.3.2）不生成** —— 其 Description 逐字為 `First surface needs: N/A`（**A-CA35**，提請裁）。
三項升級條件**皆不成立**。**A 本不動**；未寫 `delivered/`、未 tag。

---

## §1　落檔

### 1-1　RDF-11（A-CA34 之 RD 回饋）

`features/camera/RD_FEEDBACK.md`（sha256_16 `2b8b92edfd0ea158`）新增 **RDF-11**：
`X` 退出鍵之位置，SYS1 兩處皆 upper right（§7.5.3 `NRL-142629`、§8.1 `NRL-142631`），
`PU0170` 載 bottom right。TC 側依 §4.3.1 以 SYS1 為準（`NR1L-RVCHMI-038` 判 upper-right），
差異註於 `Remarks`。索引表同步（RDF 累計 11 條）。

### 1-2　profile §7.7（逐字母體須取全文）

`docs/runtime/profiles/FW036_R1L_Camera_Profile.md`（sha256_16 `e037b030ed0f8518`）新增 **§7.7**，
為 R-CAM17 之延伸：逐字母體之讀取**須取 `Description` 全文**，不得以截斷輸出
（`print(desc[:N])`、終端折行、表格欄寬）判斷來源內容。
沿革具名 CAM-15 之 `NR1L-RVCHMI-037`（`“X” soft control is presse` 之誤判）。
作法條款：讀取時不切片；需摘要輸出時**先印全文長度與 token 數**，摘句之依據為全文。

### 1-3　DECISIONS 6-56 ～ 6-58

`features/camera/DECISIONS.md`（sha256_16 `cee4344afdfee6fd`）：

- **6-56** `CVPAM_Presence`（byte 173 bit 1）為 PAM 配備旗標；不取 `PAM_Configuration`
- **6-57** RVC timeout 10 s 兩處同載（§7.5.3 逐字 ＋ `PU0361` Timeout 欄），不另開 DR
- **6-58** A 本交付候選 ＝ `…_dryrun2.xlsx`（244 列，sha256 `f194046c923fd85f…`）；
  Phase 7 待 Pei 准且須先結八條 DR 或裁降 NA

---

## §2　B02a：`AUX Camera Access` 前 31 leaf

### 2-1　切點（下放包 §2 末之指示）

`AUX Camera Access` 共 **60 leaf**。依 SYS1 章節序（HU 27 → 34.1～34.8），
第 30 列為 `SWE1-RVC-082-01`、第 31 列為 `SWE1-RVC-082-02`，**兩者同屬 §27.6.1**。
依「若第 30 列落於同一子節家族內，併入該家族（≤ 33）」取 **31 leaf**（`§27.1.1`～`§27.6.1`）。
其餘 **29 leaf**（§27.7.1–§27.8.5、§34.1.1.1–§34.8.4）落 B02b。**B02a ＝ 31 ≤ 35，升級條件 §4-1 不成立。**

### 2-2　`SWE1-RVC-069`（§27.3.2）零 TC —— **A-CA35，提請裁決**

該列之 Description **全文 4 token**：`First surface needs: N/A`（`NRL-188058`）。
其同層鄰居實測為 §27.3.1（`Technical hardware requirements – (R1 High only due to architecture)`）、
§27.3.3（Pop-up Messages）、§27.3.4（Lock out conditions）、§27.3.5（Signals from head unit）——
即 §27.3 之子節為 HMI 規格之**結構欄位**，`First surface needs` 一欄於本 feature 填 `N/A`。
037 仍將該列列為 leaf（`Categorization` 非 Heading）。

**處置**：不生成 TC，不造來源未載之判準（§8.4.1）。
`coverage_b.tsv` 記 ``No TC — source states `First surface needs: N/A` (A-CA35)``。
**其形制與 R-CAM16 不同** —— R-CAM16 為「全來源委派」之零 TC，本列非委派而是**來源自陳無需求**。
提請分析層裁：入 R-CAM16 之第二款，或另立條。

### 2-3　產物

`features/camera/generated/b02a/` —— 30 份 json ＋ 30 份 md ＋ `INDEX.md` ＋ `LINT_REPORT.md`。
TC ID `NR1L-RVCHMI-044`～`-073`；Test Set 全 30 列 `AUX Camera Access`；
計畫 `features/camera/data/b02a_plan.tsv`（31 列，sha256_16 `0b9737a17c3fcd47`）。

`coverage_b.tsv`（sha256_16 `88b87348cefa321d`）：PRODUCED 73／零 TC 1／umbrella 8 ＝ **82 列**
（`Activation and Exit` 50 全數完結、`AUX Camera Access` 32）。

### 2-4　`Vehicle Model`（R-CAM18）與其證據

| 判 | 列 | 依據 |
|---|---|---|
| 五款全 `1` | 28 列 | R-CAM18(a) 預設 |
| 三個 Atl-Mi 欄 `0` | `NR1L-RVCHMI-050`（§27.2.1.1）、`-056`（§27.2.6.1）| 其前提用 `Digital_CHMSL_Camera_Prsnt`（byte 222 bit 7）與 `Auxiliary_Trailer_Camera`（byte 211 bit 2）——**只有 Atl-Hi 三本有**，`Fastback_ATL_MI`／`Promaster_ATL_MI`／`Toro_ATL_MI` **各 0**（R-CAM18(b)）|

**無線 AUX 相機無 PROXI 配備旗標** —— 掃描字串 `Wireless_Camera`／`Wireless_Aux`／`Aux_Camera`／
`Hotspot`／`WiFi_Hotspot` 於 `forms/proxi/` **六本各 0 命中**。
惟 SYS1 **有替代條件**：§27.3.1.1～§27.3.1.3 之三項硬體要求（觸控螢幕、WiFi hotspot 能力、
MOPAR 相機）與 §27.4.1 之 `Enable Wireless Cameras` 設定。
故依 **R-CAM18(a)** 五款全 `1`，**不掛 DR**，且**升級條件 §4-3 不成立**（該條之成立要件為
「六本皆查無**且** SYS1 無替代條件」）。

### 2-5　hop label（升級條件 §4-2）

| label | 逐字出處 |
|---|---|
| `App Drawer` | H 本 §27.2.6.2 Description 全文即 `Apps Drawer`；句式沿 A 本 `NR1L-RVC-113` |
| `Camera app home page` | H 本 §27.2.6.3 Description 全文（4 token）|
| `Settings` → `Camera` → `Aux Cameras` | `forms/HMI Settings List R1 SR25 Post R1L-R (Feb 13 2026).xlsx` `Settings` 分頁 row 464 `13. Camera` → row 474 `10. Aux Cameras` → row 475 `10.1 Aux Cameras` |
| `Enable Wireless Cameras` | SYS1 §27.4.5 ＋ `PU1517`／`PU1518` 之拼法（§27.4.1／§27.4.4 寫小寫 `‘enable wireless cameras’`，三處大小寫不一，取彈窗表）|

**不可辨者 0 列**，升級條件不成立。
本批**未用 DR-CAM-g** —— 所用入口皆有逐字來源（不含 Controls page）。

### 2-6　popup

| SYS1 | PU | 狀態 |
|---|---|---|
| §27.3.3（彈窗文字以 popup list 為準）| `PU0459` | 查得，文字逐字入 ER |
| §27.4.2／§27.6.1（投影中之確認彈窗）| `PU1518` | 查得（module `Camera`；按鈕 `<X>`／`<Yes>`／`<No>`，與 §27.4.2.1／.2 之 `presses yes`／`no` 相符）|
| §27.4.5（`Wireless Cameras disabled.`）| `PU1517` | 查得，條文逐字與彈窗文字**全等** |
| §27.1.5.1／§27.3.5.1（QR 認證流程）| `PU0854`／`PU0857`／`PU0454` | 查得 |
| §27.2.3.2（無線不可用）| `PU0459` | 查得 |
| **§27.5.1／§27.6.2 之 `connecting` 狀態訊息** | —— | **查無 → `PENDING: DR-CAM-h`**（`NR1L-RVCHMI-070`）|

`data/popup_texts.tsv`（sha256_16 `c55a32fb4c016d06`）補 4 列（含查無之一列）。

### 2-7　PENDING（3 列）

| TC | DR | 事由 |
|---|---|---|
| `NR1L-RVCHMI-044` | DR-CAM-r | `Enhanced Camera App` 配備旗標（CAM-14 已開）|
| `NR1L-RVCHMI-058` | DR-CAM-r | `Camera App` 配備旗標 |
| `NR1L-RVCHMI-070` | **DR-CAM-h** | `connecting` 狀態訊息之逐字文字查無 |

### 2-8　lint 與 selfcheck

```
selfcheck_camera.py  b02a（TC 30 筆）        十項全 0｜錨 30／反查失敗 0
lint036 --profile camera（b02a_lint.xlsx）   ERROR 類全 0｜非致命 U=3／I-cross=30／X=1｜Z=0／J=0

selfcheck_camera.py  十三目錄（TC 313 筆）    十項全 0｜錨 317／反查失敗 0｜常數錨 2｜合一錨 2
render_tc.py --verify 十三目錄               逐位元相符 313／相異 0
```

`X` 一列（`-068`）之導航標的為 `screen focus` —— 該詞為 SYS1 §27.4.5 之逐字（`must launch
CP/AA screen focus for a wired connection`），不改寫。

---

## §3　R-CAM17 證據表

| # | 主張 | 掃描字串／判準 | 母體 | 命令 | 命中 |
|---|---|---|---|---|---|
| 1 | 無線 AUX 無 PROXI 旗標 | `Wireless_Camera`、`Wireless_Aux`、`Aux_Camera`、`Hotspot`、`WiFi_Hotspot`（不分大小寫）| `forms/proxi/` **6 本** | `proxi_have.py`（全表逐格串接後 `re.findall`）| 五串**六本各 0** |
| 2 | TRG／CHMSL 之 byte 只在 Atl-Hi | `Auxiliary_Trailer_Camera`、`Digital_CHMSL_Camera_Prsnt` | 同上 6 本 | 同上 | `DT28_ATL_HI`／`HDCC27_initial`／`HDCC28_ATL_HI` 各 1；`Fastback`／`Promaster`／`Toro` **各 0** |
| 3 | `connecting` 訊息查無 | module 欄為 `Camera`／`Aux Camera` 之列，其訊息欄（第 7 欄）含 `connect` 者 | `forms/Pop Up List HMI R1 (26PI).xlsx` `Main` 分頁 | openpyxl 逐列 `re.search` | **15 筆**，逐筆核皆為 Add／Authenticating／Connection Complete／Connection ERROR／Delete 等流程畫面，**無一為 `connecting` 狀態訊息** |
| 4 | `PU1517` 與 §27.4.5 文字全等 | `Wireless Cameras disabled` | 同上 `Main` 分頁 | openpyxl 全格串接後字串比對 | **1 命中**（r1300），文字欄逐字 `Wireless Cameras disabled.` ＋ `<OK>` |
| 5 | `PU1518` 之按鈕與 §27.4.2.1/.2 相符 | 按鈕欄逐字 | 同上 r1301 | openpyxl 逐欄讀 | `<X>` `<Yes>` `<No>` —— 與 `presses yes`／`presses no` 相符 |
| 6 | §27.3.2 全文即 `N/A` | `Description` **全文**（依 profile §7.7 不切片）| cache 本 §27.3.2（`NRL-188058`）| openpyxl 讀該格 ＋ 印長度 | 全文 **4 token**：`First surface needs: N/A` |
| 7 | 設定路徑之逐字 | `13. Camera`／`Aux Cameras` | `forms/HMI Settings List R1 SR25 Post R1L-R (Feb 13 2026).xlsx` `Settings` 分頁 1015 列 | openpyxl 逐列 | row 464 `13. Camera`；row 474 `10. Aux Cameras`；row 475 `10.1 Aux Cameras` |
| 8 | lockout 之逐字形制 | `UNAVAILABLE WHILE VEHICLE IS IN MOTION` | 同上 | openpyxl 逐列 | **1 命中**（row 457）|
| 9 | `PAM_Configuration` 值域二項（6-56 之依據，複驗）| 值域逐字 | `forms/proxi/Promaster_ATL_MI/…637_R42_20220204.XLSM` | openpyxl 讀該列 | `0 = Rear`／`1 = Front And Rear`（**2 項**）|
| 10 | 本批未動 A 本 | — | `features/camera/generated/{pilot02,batch01,…,batch04}` | `git status --short` | A 本十目錄 **0 改動** |
| 11 | md 與 json 一致 | — | 十三目錄 313 份 md | `render_tc.py --verify` | **相符 313／相異 0** |

---

## §4　自報錯誤

1. **B02a 初稿之 lint `H` 4 列、`W` 3 列**（本包內自查自改）—— ER 寫
   `…, which matches PU0459 in the R1 HMI pop-up list` 之句式，`matches` 為關係模糊語。
   已改為「逐字文字 ＋ `(PU0459)`」之形制（陳述事實而非比較），兩類皆 0。
   同時把 `-060` 之 Procedure 中 `compare it word for word with …` 一步簡化為
   `Read the pop-up text on the HU display` —— 比對之判準本應只在 ER。
2. **`b02a_plan.tsv` 之零 TC 列** —— `SWE1-RVC-069` 於計畫中以 `tc_id = —` 記，
   與其餘 30 列同表；若分析層希望零 TC 列另立表，請示下。
3. **DR-CAM-h 之範圍擴大**：該條原題為「Pop Up List 之 camera out-of-position 條目」
   （`Camera Not in position`／`Camera Out of Position` 兩串零命中）。
   本批之 `connecting` 狀態訊息為**同型缺件**（彈窗表無該文字），已沿用該條而**未擴其題**——
   與 CAM-15 對 DR-CAM-g 之處理（明文擴題）不一致。提請分析層裁：擴題或另開。
   `DATA_REQUESTS.md` 本輪**未改**。
4. **本輪未動 `features/camera/RULINGS.md`／`framework.md`** —— 無新立條、無重開。
   `docs/fw036/RULINGS.sha.tsv` 仍未重生（**GCB-04**，非本線可改）。

---

## §5　未結

| # | 項 | 歸屬 |
|---|---|---|
| 1 | **A-CA35**：`SWE1-RVC-069` 之零 TC 形制（R-CAM16 第二款或另立條）| 分析層 |
| 2 | **DR-CAM-h 是否擴題**（含 `connecting` 狀態訊息）| 分析層 |
| 3 | B 本餘 **6.5 組 129 leaf**（`Activation and Exit` 43/43 完結；`AUX Camera Access` 31/60）| 後續批次 |
| 4 | **RDF-11**（`X` 鍵位置）| RD |
| 5 | DR 十五條；A 本 f／g／i／j／k／m／p／q 八條阻交付，B 本 r／h | Pei |
| 6 | A 本交付（Phase 5–7）待 Pei 之准；候選為 dryrun2（244 列，DECISIONS 6-58）| Pei |
| 7 | GCB-01～GCB-09 九項 | GC 線 |
| 8 | `bench_verify.md` 累計 **33 列**（A 本 11、B01a 3、B01b 8、B02a 11）| Pei 實機 |

---

## §6　落檔清單

```
M  features/camera/RD_FEEDBACK.md                 RDF-11               sha256_16 2b8b92edfd0ea158
M  features/camera/ANOMALIES.md                   A-CA35               sha256_16 afab20e0795186a1
M  features/camera/DECISIONS.md                   6-56 ～ 6-58         sha256_16 cee4344afdfee6fd
M  docs/runtime/profiles/FW036_R1L_Camera_Profile.md  §7.7             sha256_16 e037b030ed0f8518
A  features/camera/data/b02a_plan.tsv             31 列（產 30）       sha256_16 0b9737a17c3fcd47
M  features/camera/data/coverage_b.tsv            82 列                sha256_16 88b87348cefa321d
M  features/camera/data/popup_texts.tsv           +4 列                sha256_16 c55a32fb4c016d06
M  features/camera/data/bench_verify.md           B02a 11 列           sha256_16 541b0bbf089e76d4
A  features/camera/generated/b02a/                30 json ＋ 30 md ＋ INDEX ＋ LINT_REPORT
```

**未動**：`features/camera/RULINGS.md`、`features/camera/framework.md`、
`features/camera/DATA_REQUESTS.md`、`features/camera/delivered/`、
A 本十目錄與 `sandbox/a_dryrun/`、`features/comfort/`（併行 session）。
