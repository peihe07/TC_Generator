# 上繳包 CDD-15 —— 佔位重分類（R-DIAG33）→ 交付本 v06

下放包：`down/20260918_CDD-15.md`。執行層 session：沿用 CDD 線，2026-09-18。
**CDD-14 已於本 session 先行跑完**（未 commit），本包承其產出。

## 首段 —— 升級條件逐項（自 §5 逐字複製）

| # | 條件（逐字） | 實測 | 觸發 |
|---:|---|---|:-:|
| 1 | 指紋／`--check`（既存者記「既存」） | 寫前 HEAD 82／worktree 84（差 2 為**本 session CDD-14 未提交之新增**，非覆寫事件），寫後 **85**，零漂移。`--check` FAIL → **既存**（第十一包） | **否** |
| 2 | T1 出現 (a)(b) 清單外之佔位 token → 停，列出，不分類 | 592 行之 token 全在清單內（`DR-DIAG-4`／`-5`／`-6`／`-8`），**無例外** | **否** |
| 3 | **ASSET_MISSING 條數與 DR-8 之 TC 數（CDD-12 記 13 條）不符 → 回報差異** | **命中** —— 實測 **18 條**，非 13 | **是** |
| 4 | 任何 TC 非 Remarks 欄有變動 → FAIL，回報 | `git diff` 逐檔檢視：變動行**全部**為 `"remarks"`，非 Remarks 欄 **0** 行 | **否** |

**條件 3 命中，處置如下：**
差異為**分析層引數誤讀**（`[A-DIAG76]`）—— CDD-12 上繳包記「13 個 **037 列**（19 條 TC）」，下放包引為「13 條 TC」。
實際帶 `<Package Indication value>` 佔位者 **18 條**（#4 之 19 條中，`-350` unsupported 型不帶該佔位）。
**以實測 18 為準**，著色 18 列。

---

## §1　T0 —— 條文與指紋

`R-DIAG33` `git grep HEAD` **零命中**；**先取號落條文，再做產出**（CDD-13 之失序不重演）。

| 項 | 值 |
|---|---|
| `R-DIAG33` | sha8 `24a1e123`／body_sha8 `674dc223`／`RULINGS.md:443`，20 行 |
| **R-G75(c) 三數** | 寫前 HEAD **82**／worktree **84**／寫後 **85** |

> **三數之說明**：HEAD 82 與 worktree 84 之差為 **CDD-14 本 session 新增之 `R-DIAG31`／`R-DIAG32`**（尚未 commit），
> **不是覆寫事件**。本包只追加 1 列（`R-DIAG33`）。全生成比對零漂移。

**`RM-DIAG` 白名單**：R-DIAG33(d) 書「加第七式」，惟白名單此時**已有七式**
（原五 ＋ CDD-12 流程圖錨 ＋ CDD-14 Annex A 錨），故本包所加實為**第八式**（`[A-DIAG77]`）。
自測 3 項（2 正 1 反）通過。**`R-DIAG6` 之「五種定型句」實已為八種，請一併裁。**

---

## §2　T1 —— 佔位分類表

分類**依 DR token 不依字面**（R-DIAG33(a)(b)）。`placeholder_summary.tsv` 加 `class` 欄，**592 行**。

| token | class | 行數 | 條數 |
|---|---|--:|--:|
| `DR-DIAG-4`（controlState） | `TOOL_RESOLVED` | **99** | |
| `DR-DIAG-5`（值域／record 版面） | `TOOL_RESOLVED` | **206** | |
| `DR-DIAG-6`（不支援之 SID／DID） | `TOOL_RESOLVED` | **269** | |
| **小計** | `TOOL_RESOLVED` | **574** | **427** |
| `DR-DIAG-8`（`<Package Indication value>`／`<out-of-range Package Indication value>`） | `ASSET_MISSING` | **18** | **18** |

**清單外之 token：0**（條件 2 未命中）。

`ASSET_MISSING` 之 18 條：`-339`／`-341`／`-342`／`-344`～`-349`／`-351`～`-357`／`-557`／`-558`（皆 `$0307`／`$0309`）。

> 下放包預期 TOOL_RESOLVED 573 行，實測 **574** —— `DR-DIAG-5` 為 206 行非 205（CDD-13 之 `<in-motion menu status byte>` 等新增所致）。

---

## §3　T2 —— Remarks 定型句

**427 條** TOOL_RESOLVED TC 之 Remarks 加：

```text
Placeholder values resolved at execution by the diagnostic tool loaded with the ECU CDD file
```

**§5 條件 4 複驗**：`git diff` 各 JSON 之變動行**全部**為 `"remarks"`，非 Remarks 欄 **0** 行。

**貼入一條 TOOL_RESOLVED TC 全文**

```text
[NR1L-DIAG-057] SWE1-Diagnostics-109 | Buzzer Control | P0 | All
Test Item
  The DiagService shall interpret the received 0x2F (InputOutputControlByIdentifier) request for DID
  0x5000 and extract the buzzer control activation parameter.
  ($5000, entertainment audio muted while a buzzer control tone is active)
Pre-Condition
  1. The ECU is in the default diagnostic session
  2. The diagnostic tool is connected to the OBD-II diagnostic connector
  3. Security access 0x27 has been granted
  4. An entertainment audio source is playing
Input Test Data
  NA
Test Procedure
  1. Activate a buzzer control tone that is not an externally generated tone
     $ 2F 50 00 03 <controlState for "buzzer control tone">
Expected Result
  1. The positive response 6F 50 00 is received, and the entertainment audio source is muted
Specification Reference   CFTS004-4940298
Design Method  功能測試 (Functional based ; no specific technique)
Remarks        Placeholder values resolved at execution by the diagnostic tool loaded with the ECU CDD file
```

**TC 文字一字未改** —— `<controlState for "buzzer control tone">` 原樣保留（§0 所命）。

---

## §4　T3 —— 著色

| 項 | 前 | 後 |
|---|--:|--:|
| 著色列 | **429** | **18** |

只 `ASSET_MISSING` 之 18 條整列 `FCE4D6`；其餘 547 條**無底色**。

---

## §5　T4 —— 三檔改寫

| 檔 | 改動 |
|---|---|
| `DATA_REQUESTS.md` | 表頭加 `TOOL_RESOLVED` 狀態之定義；第 5（controlState）／6（值域版面）／9（不支援 SID 與 DID）三列 Status → **`TOOL_RESOLVED`**、Urgency → `LOW`。現況：CLOSED 8／TOOL_RESOLVED 3／PARTIAL 2／MISSING 2 |
| `asset_request.md` | **全檔改寫**：§⚠ 執行前提（兩類分佈表）／§一 `ASSET_MISSING` 三件（DR-8 18 行、DR-7 非佔位、DR-11 非佔位）／§二 **建議取得 `.cdd`**（三 token 對應 CDD 內容，註明非交付前提）／§三 到件後處置。**5 項 → 3 件 ＋ 一節** |
| `EXEC_GUIDE.md` | §一 改寫為「執行前提：診斷工具須已載入本 ECU 之 CDD 檔」，含兩類分佈、**三型佔位之解讀各一例**（controlState／值域版面／不支援 DID），並記 `PENDING` 行數為 **0** |

---

## §6　T5 —— 產出與對帳

| 項 | 值 |
|---|---|
| `merged/diagnostics_v13.xlsx` | **565 列**（下放包書 v11／v12，因 CDD-14 已出 v12 而順延，`[A-DIAG78]`） |
| 交付本 **v06** | `…_20260918_v06.xlsx`，**565 列／著色 18 列**（下放包書 v05，CDD-14 已出 v05 而順延） |
| 十本批次 | pilot v10／b1 v09／b2 v08／b3 v07／b4 v08／b5 v09／b6 v06／b7 v05／ext v04 |
| **逐格對帳** vs `merged/v13` | **ID／列序以外之格差 0** |
| lint（十一本） | 除 **`SID-WORD=2`**（CDD-14 之真違規，依條文未改）外**其餘 31 項全 0**；`RM-DIAG` **0**；無 `Traceback` |
| `coverage.tsv` | **389 列，UNCOVERED 0** —— **不變**（合 §2 T5 預期） |

---

## §7　§3 預期 vs 實測

| 項 | 預期 | 實測 | 判 |
|---|---|---|---|
| TC 列數 | 565 不變 | **565** | 符 |
| 著色列 | 429 → ≈ 13 | 429 → **18** | **近符**（差因見條件 3） |
| Remarks 改動 | ≈ 416 條 | **427 條** | 近符 |
| lint | 真違規 0；`RM-DIAG` 0 | `RM-DIAG` **0**；`SID-WORD` 2 為 CDD-14 之已報真違規 | 符 |
| asset_request 項目 | 5 → 3（＋ .cdd 節） | **3 ＋ 一節** | 符 |

---

## §8　自報錯誤

| # | 內容 |
|---|---|
| `[A-DIAG76]` | 條件 3 之差異：ASSET_MISSING 18 條 vs 預期 13 —— 分析層把 CDD-12 之「13 個 037 列」引為「13 條 TC」 |
| `[A-DIAG77]` | R-DIAG33(d) 書「第七式」，實為**第八式**（白名單已有七式） |
| `[A-DIAG78]` | 下放包命交付本 `v05`／合併本 `v11`，皆已被 CDD-14 用掉，順延為 **v06**／**v13** |
| — | T1 首版統計用錯欄位索引（`class` 在 index 6 而我取 5），一度印出 `TOOL_RESOLVED 0 行`。檔案本身寫對，只是統計錯；已重算。**教訓：加欄後，所有依位置取值之處都要同步** |

---

## §9　未結與待 Pei

| # | 內容 |
|---|---|
| **`NR1L-DIAG-229`／`-230`** | CDD-14 之 A-DIAG68 殘留 2 行，依條文未改，待裁 |
| `R-DIAG6` 之定型句數 | 實已為**八**種，請以 `R-DIAG6(amend)` 記明 |
| `DR-DIAG-8` | **唯一阻斷交付之缺件**（18 條）——`SX-9830-0095` 本文 ＋ PkgIndex 對照 |
| `DR-DIAG-7`／`-11` | 非佔位，只影響 `-290` 之 ER 深度與 I/O 之 PC-3 去留 |
| **`.cdd` 檔** | 非交付前提，但取得後可將 574 行 TOOL_RESOLVED 佔位填為實值以供自動化 |
| FB-DIAG-a～u（21 項） | 待送 |
| GC-16 | `--check` 第十一包記「既存」 |

**交付本現況：565 條中 547 條可直接執行，18 條待 `SX-9830-0095` 到件。**

---

## §10　建議 commit

**與 CDD-14 一併提交**：

```
feat(diagnostics): CDD-14 + 15 — SID-WORD check and placeholder reclassification

- R-DIAG31/32/33 landed, number checks done against HEAD before any output
- citation chain recorded: CFTS004 to CS.00099 to CS.00100, r25 is the normative one
- SID-WORD implemented, recalibrated to naming conflict, 103 to 2
- the 2 remaining are genuine A-DIAG68 residue on $5100, left unchanged per the ruling
- R-DIAG33: placeholders split into TOOL_RESOLVED 574 lines and ASSET_MISSING 18
- shading drops from 429 rows to 18; 427 TCs gain the CDD-file Remarks phrase
- asset_request cut from five items to three plus a .cdd recommendation
- EXEC_GUIDE rewritten: 547 of 565 TCs are directly executable
- merged v13 and delivery v06, cell-for-cell reconciliation clean
- A-DIAG74..78 registered
```

pathspec：

```
features/diagnostics scripts/lint036.py docs/fw036/RULINGS.sha.tsv \
docs/fw036/lint_reports/diagnostics_*_20260918.md \
docs/fw036/lint_reports/Diagnostics__diagnostics_*_20260918.md \
docs/fw036/handoff/up/20260918_CDD-14.md docs/fw036/handoff/up/20260918_CDD-15.md \
docs/fw036/handoff/down/20260918_CDD-14.md docs/fw036/handoff/down/20260918_CDD-15.md \
docs/fw036/handoff/down/20260918_CDD-13_review.md
```
