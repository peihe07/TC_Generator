# 上繳包 CAM-04 —— pilot v2（10 TC）：逐列修正 ＋ 寫法對齊已交付本

對應下放包：`docs/fw036/handoff/down/20260916_CAM-04.md`
對應審閱：`down/20260916_CAM-03_review.md`
執行層 session：延用，2026-09-16。**git 未動**；未寫回工作簿；`pilot01/` 不刪不改。

---

## §1　任務 1 —— §1 六項落檔

### 1.1　取號現查與 sha8

現查：`features/camera/RULINGS.md` 原有 `R-CAM1`～`R-CAM10`（含 `R-CAM5(c)′`／`R-CAM5(e)` 之 prime 錨）。
`{live}` = **11** 成立。

| 條 | body sha8 | fenced sha8 | 錨點 |
|---|---|---|---|
| **R-CAM3(e)**（增補，自立 `###` 錨）| `e9ad2043` | `616af257` | `RULINGS.md:43` |
| **R-CAM5(e)**（增補，自立 `###` 錨）| `3e719a71` | `7fe6beef` | `RULINGS.md:94` |
| **R-CAM11** | `35cc02b7` | `14557e5c` | `RULINGS.md:153` |

**R-TM13 之「fenced sha 不變」已達成**：

| 條 | body sha8（前 → 後）| fenced sha8 |
|---|---|---|
| R-CAM3 | `73b08f60` → **`7c5879c4`** | `dd8170e4` → **`dd8170e4`（不變）** |
| R-CAM5 | `35a26509` → `35a26509`（不變）| `91f752d4`（不變）|
| R-CAM5(c)′ | `10dbe3c6` → **`43e46c82`** | `df45f7ab`（不變）|

作法須回報：**(e) 段一律自立為同級 `###` 錨**（沿 `features/audio_mgmt/RULINGS.md`
之 `R-AM2′` 前例）。初稿曾把 R-CAM3(e) 之 code block 寫在 R-CAM3 標題轄下，
`rulings_hash.py` 會把同一錨下的多個 fenced block 併雜湊，
使 R-CAM3 之 fenced sha 由 `dd8170e4` 變為 `95faafe5` —— 與下放包 §1(a)
「fenced sha 不變」相違，已改為自立錨並復原。
`R-CAM5(c)′` 之 body sha 變動係因其轄下段落被新插入之 `R-CAM5(e)` 標題截短，
條文本體逐字未動（fenced 不變可證）。

### 1.2　(a) R-CAM3(e) ／ (b) R-CAM5(e) ／ (c) R-CAM11

三條逐字照錄（R19-2），內容同下放包 §1。R-CAM11 下另附執行層之落實說明（見 §2.1）。

### 1.3　(d) `selfcheck_r_cam10.py`

`features/camera/scripts/selfcheck_r_cam10.py`（93 行）。
資料來源 `features/camera/data/layer3_a_vf_chapters.tsv`（A 本 541 個來源引用之全量展開）；
owner = 引用該來源之最小 SWE ID。

**R-CAM11 之後 `specification_reference` 寫 SYS2 `F` 欄之 VF anchor，故工具另建
「錨 → SYS-RA id」之反查表**（掃六本 SYS2 之 `B`／`F` 兩欄），
反查失敗亦視為命中並 exit 1 —— 這一步在本包實測中抓到一個錯值（見 §6-2）。

```
R-CAM10 自檢：批次 ['…/features/camera/generated/pilot02']
  檢查錨 10 個；無法反查 0；**命中 0**
exit=0
```

### 1.4　(f) 邊界值換算

DECISIONS **6-10** `[DECIDED]`；併記「全量時每個 VF 分支各自換算並具名其 DBC `SG_` 因子」。

### 1.5　(e) framework 搬移

| 檔 | 搬移前 sha16 | 搬移後 sha16 | 行數 |
|---|---|---|---|
| `docs/fw036/framework.md` | `94baca2fac2e82e0` | `bac7bff2526c623e` | 1653 → **1468** |
| `features/camera/framework.md` | （不存在）| `c4fc9d2e5b7d1f44` | — → **195** |

Part VIII 之 189 行整段搬入（含簽核狀態行），逐字未改；新檔加 5 行檔頭說明搬移由來。
全域檔原處留：

```
## Part VIII — Camera (two books: SWRA Service + RVC-HMI)

→ `features/camera/framework.md`（CAM-04 §1(e)，Pei 2026-09-16 裁；
沿其他 15 本 feature 之前例，framework 落於各 feature 目錄）。
```

DECISIONS **6-11** 記此裁定。

---

## §2　任務 2 —— 寫法對齊已交付本

### 2.1　R-CAM11 —— VF230 本 M 欄 5 樣本 ＋ 十列改寫對照

**先報路徑差異**：下放包指 `features/vehicle_setting/delivered/…SWQT_VF230_20260902.xlsx`，
該目錄下**只有 CFTS044 一本**；VF230 本實際落於
`features/vehicle_setting/output/…SWQT_VF230_20260902.xlsx`（sha12 `aed08b4d83a8`，438 列）。
另 canon §8.7.5 之基準語料 `VF230 0819`（sha12 `adee5dc462ae`，457 列）位於
`features/vehicle_setting/inputs/`。**兩本皆抄，形制一致。**

`specification_reference` 欄（母本第 9 列欄序為 `N`，非下放包所寫之 `M`）之樣本：

| # | 本 | 列 | 逐字 |
|---:|---|---:|---|
| 1 | VF230-0819 | r10 | `VF230_V1_PDT27_VF_6222` |
| 2 | VF230-0819 | r41 | `VF230_V1_PHDCC27_VF_8670` |
| 3 | VF230-0902 | r10 | `VF230_V1_PDT27_VF_6120` |
| 4 | VF230-0902 | r13 | `VF230_V1_PHDCC27_VF_2114` |
| 5 | VF230-0902 | r25 | `VF230_V1_PHDCC27_VF_3509` |

**形制單一**：VF230-0819 之 457 列只有兩種形態（`PDT27` 236 ／ `PHDCC27` 221），
VF230-0902 之 438 列同樣只有兩種。**即 SYS2 `F` 欄（`SYS2 來源需求項目ID`）之 VF anchor，
不是 SYS-RA ID。** 升級條件「VF 型錨不存在或形制不一」**不成立**。

十列 spec_reference 改寫前後（pilot01 → pilot02）：

| TC | 來源列（SYS-RA）| pilot01（SYS-RA 式）| pilot02（R-CAM11 式）|
|---|---|---|---|
| 001 | `SYS-RA-CAM-062` | `SYS-RA-VF551_V33-213` | `CFTS092-4781627` |
| 002 | `SYS-RA-VF551_V33-220` | `SYS-RA-VF551_V33-220` | `VF551_V33_P226MCA_VF_1141` |
| 003 | `SYS-RA-VF551_V2-488` | `SYS-RA-VF551_V2-498` | `VF551_V2_PHDCC27_VF_484` |
| 004 | `SYS-RA-VF551_V3-266` | `SYS-RA-VF551_V3-266` | `VF551_V3_P363_VF_484` |
| 005 | `SYS-RA-VF551_V2-545` | `SYS-RA-VF551_V2-545` | `VF551_V2_PHDCC27_VF_532` |
| 006 | `SYS-RA-VF551_V2-549` | `SYS-RA-VF551_V2-549` | `VF551_V2_PHDCC27_VF_548` |
| 007 | `SYS-RA-CAM-078` | `SYS-RA-CAM-078` | `CFTS092-4781643` |
| 008 | `SYS-RA-CAM-078` | `SYS-RA-CAM-078` | `CFTS092-4781643` |
| 009 | `SYS-RA-VF551_V2-490` | `SYS-RA-VF551_V2-490` | `VF551_V2_PHDCC27_VF_1577` |
| 010 | `SYS-RA-VF551_V2-490` | （不存在）| `VF551_V2_PHDCC27_VF_1577` |

json 另加頂層欄 `source_object_id` 保留 SYS-RA id，使錨與來源列兩者皆可追。

**附帶佐證**：`V2-488` 之 anchor 為 `VF551_V2_PHDCC27_VF_484`、`V3-266` 為
`VF551_V3_P363_VF_484` —— **同一 VF 序號 484**，逐句對應之雙生條文，
支持 §3 對 `-003`／`-004` 之配對選擇。

### 2.2　Pre-Condition 之行序 —— 實測結果與下放包所擬不同

下放包 §2-3 擬「PROXI 行 → 電源狀態行 → CAN source 行 → 畫面狀態行」。
VF230-0819 之 457 列逐序位實測：

| 序位 | 最常見句型 | 次數 |
|---|---|---:|
| 第 1 行 | `The HU is in the Full-Operation state` | **457／457（100%）** |
| 第 2 行 | `FD-CAN8 is connected to the bus simulator with signal tracing enabled` | 296 |
| 第 2 行 | `PROXI $<Param>$ is set to "<值>"` | 10 |
| 第 3 行 | `The Vehicle Settings menu is open` ／ `The "<X>" customer setting is set to <值>` | 92 ／ 42 |

→ **電源狀態行在前、PROXI／CAN 行在後、畫面狀態行最後**，與下放包所擬相反。
pilot02 依**實測**採：`Full-Operation` → `PROXI` → `CAN source` → 品牌行 → 畫面／檔位狀態。

另：VF230-0902 r13／r25 之 PROXI 行已是 v4.1 標準式
（`PROXI Heated_Seats = 0 (Absent)`），與 VSM-0902 一致；VF230-0819 之
`PROXI $Param$ is set to "…"` 為舊式（lint `Y`／WARN），**新產出不採**（R-G70 v4.1）。

### 2.3　逐欄句型對照表

| 句型 | 基準例（逐字 ＋ 本 ＋ 列）| pilot02 之最終句 | 判 |
|---|---|---|---|
| Pre 首行（電源）| `1. The HU is in the Full-Operation state`（VF230-0819 r10，457/457）| 同 | 對齊 |
| Pre PROXI | `2. PROXI Heated_Seats = 0 (Absent)`（VF230-0902 r25）／`1. PROXI EPB_Maintenance_Menu = 0 (Absent)`（VSM-0902 r10）| `2. PROXI Rear_View_Camera = 1 (Present)` | 對齊 |
| Pre 設定值 | `3. The "SWITCH 5 Power Mode" customer setting is set to BATTERY`（VF230-0819 r12）| `4. The "Rear View Camera Delay" setting is set to "On"` | 對齊（值加引號依 §5.8(e)）|
| proc CAN | `1. Send CAN: IPC_VEHICLE_SETUP2.AUX5_PWRMD = 1 (BATTERY)`（VF230-0819 r14）| `1. Send CAN: TRANSM_FD_4.ShiftLeverPosition = 2 (R)` | 對齊 |
| ER CAN | `1. IPC_VEHICLE_SETUP2.AUX5_PWRMD = 1 (BATTERY) is sent`（VF230-0819 r14）| `1. TRANSM_FD_4.ShiftLeverPosition = 2 (R) is sent` | 對齊（**取代 pilot01 之 `is accepted`**）|
| proc 導航 hop | `2. Press "Settings" on Menu Bar`（VF230-0902 r13）| `1. Press "Apps" on Menu Bar to open App Drawer` | 對齊（入口式；hop 數見下）|
| ER 導航 hop | `2. The Settings screen is displayed`／`3. The "Doors & Locks" page is displayed`（VF230-0902 r13）；`2. The "Select Shortcut Type" screen is displayed`（Home-0809 r167）| `2. The Settings screen is displayed`／`3. The "Camera" settings screen is displayed` | 對齊 |
| Final Step | `3. Read the Vehicle Settings menu and check that the "6 Aux Switches" customer setting is not displayed`（VF230-0819 r10）| `3. Read the HU display and check that the rear view camera image is displayed in Automatic Display Mode` | 對齊 |
| ER 末行 | `3. The "6 Aux Switches" customer setting is not displayed`（VF230-0819 r10）| `3. The rear view camera image is no longer displayed and the previous screen is shown again` | 對齊 |
| 等待步驟 | `3. Hold for 200 ms`（SWC-0628 r21）；`3. Wait for 10 minutes without switching the ignition off`（sw_update r10）| `7. Wait for 10 s` | 對齊 |
| `test_item` 括號下半 | `(read the Vehicle Settings menu and check that the "Auto Door Locks" customer setting is not displayed)`（VF230-0902 r13 —— 小寫起首、為 Final Step 之複述）| `(Atl-Hi, read the HU display and check that the rear view camera image is displayed in Automatic Display Mode)` | 對齊＋**前綴區分 token**（R-CAM3／R-CAM5 之 sibling 需求，見 §6-3）|

**hop 數之差異須回報**：VF230-0902 進設定為**一 hop** `Press "Settings" on Menu Bar`；
R-CAM8 已鎖定 `ENTER_CAMERA_SETTINGS` 為**三 hop**（`Apps` → `Settings` → `Camera`）。
pilot02 依裁定採三 hop；兩者形制（`Press "<X>" on Menu Bar` / `Select "<X>"`）一致，
差別只在中介之 App Drawer。

### 2.4　「新句型」清單 —— **3 種，未逾 §5 之上限**

以三本基準語料 ＋ 10 本 `features/*/delivered/` 全掃（逐行 regex）判定。

| # | 句型 | 交付語料 | canon | 處置 |
|---:|---|---|---|---|
| 1 | `Select "<X>" in the App Drawer` | **零命中** | §5.8(a) 範例逐字、§5.3 `ENTER_SETTINGS_APP` 已鎖定 | 採用（canon 有據）|
| 2 | `Set "<X>" = "<Y>"` | **零命中** | §5.8(e) 範例 `3. Set "Type" = "Latching"` | 採用（canon 有據）|
| 3 | §5.4 兩行式 `   $ adb …` | **零命中**（全語料無任何 `$ ` 指令行）| §5.4 全節 | 採用（canon 有據）；登 **A-CA27** |

另兩種為**本包裁定所定之固定句式**，非執行層自創，不計入「新句型」：
`CAN source: …`（R-CAM3(e)）與 `The vehicle brand is (not) Ram (…)`（R-CAM5(e)）。

---

## §3　任務 3 —— 逐列修正（下放包 §3 十列逐條）

| TC | 下放包要求 | 落實 |
|---|---|---|
| 001 | verbatim 改 V2 或 CFTS092；Pre 5 改 R-CAM3(e)；ER1 改可觀察 | 錨改 **`CFTS092-4781627`**（`SYS-RA-CAM-062`，21 token，平台中立）。**須回報**：`-001` 之 28 個來源中，V2 側僅 `-528`／`-531`（Camera Not in Position 疊層），與開機無涉；描述開機序列者只有 V33／V42，故取 CFTS092，V33-213 留 reasoning 互參。Pre 第 3 行為 R-CAM3(e) 固定句；ER1 改 `… is sent and the HU completes start-up` |
| 002 | 同 001 句式；`IGN_LK ↔ Ignition_Pre_Off` 引來源，查無則 PENDING | **查無** —— V33／V42／V2 三本全文搜尋同時含 `OperationalModeSts` 與 `CmdIgnSts`／`IGN_LK`／`BCM_FD` 之列，零命中。Pre 第 4 行 `PENDING: DR-CAM-i …`（lint `U` = 1，`T` = 0）。verbatim 仍取 V33-220 —— `-001` 之來源無任何 V2／CFTS092 條文描述關機序列 |
| 003 | 刪步 1，ER 重編 | 刪 `Send CAN: CmdIgnSts = 4 (RUN)`（RUN 由 Pre 之 Full-Operation 涵蓋，§4.4）；步驟由 4 減為 **2**，ER 同步重編。錨另改 **`V2-488`**，見 §6-3 |
| 004 | verbatim 改 V3 §1.10.2.2 之自動模式進入原句；查無則取 V42 並回報 | **兩者皆逾 lint L 上限**：V3 §1.10.2.2 之 `-260` = **55** RE_TOKEN；V42 之 `-225` = 61、`-302` = 51（L = 50）。故維持 **`V3-266`**（§1.10.2.3，30 token），並把 `-003` 之錨同步改為其雙生句 `V2-488`（同為「RVC 影像已顯示 → 進 R → 續顯並切自動」），使兩列情境完全對齊 —— 此即審閱 §二-3 所要求之「情境一致」。登 **A-CA26**，Pei 裁 |
| 005 | 刪步 1；design_method 改功能測試；Pre 5 改 R-CAM3(e) | 全數落實；步驟 4 → **3** |
| 006 | 刪 Pre 4；reasoning 刪矛盾句；design_method 具名 | Pre 由 4 → 4 行但刪「影像已顯示」一行（開啟改為 setup 步驟）；reasoning 之「本列之錨仍為 CFTS092 SYS-RA-CAM-082」已刪；design_method 取 **狀態轉換**（顯示態 → 非顯示態）並於 reasoning 具名 |
| 007 | 步 5、6 改純動作（CAN 式 ＋ R-CAM3(e)）；ER 承載觀察 | 步 5／6 改 `Send CAN: TRANSM_FD_4.ShiftLeverPosition = 2 (R)` ／ `= 4 (D)`；另加步 7 `Wait for 10 s`；只有 Final Step（步 8）帶 `and check that`。Pre 第 3 行為 gear 之 CAN source 行 |
| 008 | 同 007；Pre 3 改 `not Ram` 句式 | 同上；Pre 第 4 行 `The vehicle brand is not Ram (2261 Fiat, 376 Abarth)`（R-CAM5(e)）|
| 009 | 只承 on-point；步 4 之 205 移出；Final Step 收斂 | 步驟 **3** 步（gear D → 206 → Final），ER 3 行，只驗 on-point |
| **010（新）** | off-point 205 仍顯示 | 新增，同前提同步驟至 205 止，BVA，Vehicle Model 同 009（Atl-Hi）|

逐列欄級 diff：`features/camera/generated/pilot02/DIFF_pilot01_to_pilot02.md`（51 列）。

---

## §4　lint ＋ selfcheck

```
python scripts/lint036.py --profile camera <暫存簿>
行計 A=0 B=0 C=0 D=0 E=0 F=0 G=0 H=0 I=0 I-sibling=0 J=0 K=0 L=0 M=0 N=0
      P=0 Q=0 R=0 T=0 U=1 V=0 I-cross=10 W=0 X=0 Y=0 Z=0
```

報告全文 `features/camera/generated/pilot02/LINT_REPORT.md`。

- **ERROR 類全 0**，含 `Z` = 0（十列七欄共 70 格）。
- `U` = 1 為 `NR1L-RVC-002` 之 **DR-CAM-i 佔位**（依審閱 §二-9 之指示所加；`T` = 0 表其說明為 ASCII）。
  `CHECK_STATUS["U"]` 為「計數用」，非 FAIL。
- `I-cross` = 10 為結構性基線（交付本 100% 同樣半窗），`CHECK_STATUS` 明載非判準。
- `selfcheck_r_cam10.py`：檢查錨 10 個、反查失敗 0、**命中 0**，exit 0。
- 十列 `test_item` 上半與其來源列之 SYS2 `Description` 欄**逐字全等**（機器比對），
  token 數 7～40，全數 ≤ 50。

---

## §5　升級條件之檢查

| 條件 | 結果 |
|---|---|
| VF230 交付本 M 欄之 VF 型錨不存在或形制不一 | **否** —— 兩本共 895 列只有兩種形態（`PDT27`／`PHDCC27`），即 SYS2 `F` 欄之 VF anchor。惟**路徑與欄名須更正**：本在 `output/` 非 `delivered/`；欄為 `N` 非 `M`（§2.1）|
| `style_compare_w97.py` 不可重用且無等價工具 | **可重用性未採** —— 該腳本存在（`features/vehicle_setting/scripts/`）但係 vehicle_setting 專用之欄別比對；本包改以自寫之語料取樣器（逐行 regex 掃三本基準 ＋ 10 本 delivered）取句型最近例，結果見 §2.3。**回報此替代**，未改動他 feature 之腳本（§0）|
| `-004` 於 V3／V42 皆無「無影像 → R → 顯示」之原句 | **實質成立但另有解** —— V3 §1.10.2.2 之 `-260` 存在且語意正確，惟 **55 token 逾 L=50**；V42 之 `-225`(61)／`-302`(51) 亦逾限。改以 V3 §1.10.2.3 之 `-266` ＋ 將 `-003` 錨同步改為雙生句 `V2-488`，兩列情境對齊且 lint 全綠。登 A-CA26 待裁 |
| 「新句型」超過 3 種 | **否** —— 恰 3 種，且三者皆有 canon 明文（§5.8(a)／§5.8(e)／§5.4），缺的只是交付語料前例（§2.4）|

---

## §6　自報錯誤

1. **R-CAM3(e) 初稿寫在 R-CAM3 標題轄下**，使 R-CAM3 之 fenced sha 由 `dd8170e4`
   變為 `95faafe5`，與下放包 §1(a)「fenced sha 不變」相違。已改為自立 `###` 錨並復原（§1.1）。
2. **`VF551_V2_PHDCC27_VF_1368` 為臆造之錨值**。初稿未查 SYS2 `F` 欄即填，
   由 `selfcheck_r_cam10.py` 之「反查失敗」攔下，實際值為 `VF551_V2_PHDCC27_VF_484`。
   —— 此即 §1(d) 之工具在本包內首次發揮作用；**反查失敗視同命中**之設計是刻意的。
3. **`-003` 之錨由 `V2-498` 改為 `V2-488`，超出下放包 §3 對 `-003` 之要求**（原只要求刪步 1）。
   理由：審閱 §二-3 要求 `-004` 之 verbatim 與其情境相符，而 Atl-Mi 側可用之 ≤50 token 條文
   只有 `V3-266`（「影像已顯示 → 進 R → 續顯並切自動」）；若 `-003` 續用 `V2-498`
   （「無影像 → 進 R → 顯示」），車型軸兩列之驗證點即不同，違 R-CAM3(a) 之「同一驗證點」。
   **此為執行層之判斷，非裁定所令，請於審閱確認或退回。**
4. `NR1L-RVC-001` 之驗證點由「daemon 完成初始化」調整為「開機後相機影像依預設為關閉」，
   以配合 CFTS092 錨之語意（§8.1）。adb 兩步保留為輔助觀察。**此亦為執行層之判斷。**
5. `NR1L-RVC-007`／`-008` 之 `reasoning` 仍為同一模板填品牌差異（CAM-03 §8-5 已自報，本包未改善）。

---

## §7　未結

| # | 項 | 去處 |
|---|---|---|
| 1 | **DR-CAM-a（高）** `VF617_V5` 缺件 | 補件 |
| 2 | **DR-CAM-f（高）** `TRANSM2` DBC | 補件 |
| 3 | DR-CAM-g（中）controls page hop | 補件 |
| 4 | DR-CAM-h（中）Pop Up List out-of-position 文字 | 補件 |
| 5 | **DR-CAM-i（中，本包新開）** `LTM_OperationalModeSts` ↔ `CmdIgnSts` 值對應 | 補件 |
| 6 | A-CA26 `-004` 之 L 上限（放寬 L／接受現案／另指定來源）| Pei 裁 |
| 7 | A-CA27 §5.4 兩行式無語料前例 | Pei 裁 |
| 8 | A-CA21 VF／CFTS 邊界判準相斥 | Pei 裁 |
| 9 | `-018` 速度邊界之四 VF 分支拆分（全量）| Pei 裁 |
| 10 | §6-3／§6-4 兩處執行層判斷 | pilot v2 review |
| 11 | GC 線五項（`GC_BACKLOG.md`）| GC 線 |

## §8　本包寫入之檔案（git 未動）

```
M  docs/fw036/framework.md                   Part VIII 搬出，留一行指標（1653 → 1468 行）
?  features/camera/framework.md              Part VIII 全文（195 行，新）
M  features/camera/RULINGS.md                R-CAM3(e)／R-CAM5(e)／R-CAM11
M  features/camera/DECISIONS.md              新增 6-10（邊界換算）、6-11（framework 落點）
M  features/camera/ANOMALIES.md              新登 A-CA25／A-CA26／A-CA27
M  features/camera/DATA_REQUESTS.md          新開 DR-CAM-i
?  features/camera/scripts/selfcheck_r_cam10.py   R-CAM10 自檢（新）
?  features/camera/generated/pilot02/        10 × json ＋ 10 × md ＋ INDEX ＋ LINT_REPORT ＋ DIFF（新）
?  docs/fw036/handoff/up/20260916_CAM-04.md  （本檔）
   features/camera/generated/pilot01/        未動（diff 基線）
```
