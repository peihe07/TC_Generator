# 上繳包 VS-CF-03 —— CFTS044 出貨版 Revise2：237/237 全生成、PENDING 0、DR 0

下放包：`docs/fw036/handoff/down/20260908_VS-CF-03.md`（Pei 裁定 2026-09-08）
執行日：**2026-09-08**。結果分類（FO §8.4）：**全部完成**，另有 **8 項具名偏差／擴充**（§7）
與 **2 項待裁**（§9）。

---

## §0　一句話

§〇 之七項目標數字**全數達成**：資料列 243 → **241**、Requirement 覆蓋 **237/237**、
未生成 23 → **0**、PENDING 12 → **0**、撤回之 DR（DR-15／21／24′／25／26）與 `BLOCKED`
→ **0**、`IMPL_GAP` → **0**、tc_id 重賦連續且附對照表。

工作簿內**僅存 `DR-5-B` 一類 DR 引用（39 列）** —— 該 DR 不在 §〇 之枚舉、亦不在 §七.7 之
撤回清單，其 TC 皆可執行，故降為 `dr_dependent = DR-5-B：…` 之非阻擋具名（§7 偏差 3）。

`surgical_save` 落檔，42 個 zip member **僅 `sheet6.xml` 相異**，data-validation 計數不變。
內容改動 **810 個儲存格、235 列**（以身分對位計，已扣除因刪列而全簿位移之 No.#／tc_id）。

⚠ **一項重複命中須 Pei 裁**：Revise2 row 19 與 row 52（不同 Req）之五欄逐字相同（§9-1）。

---

## §1　量測與 sha（§七.1）

| 項 | 值 |
|---|---|
| Revise1（基準，不改） | `f444028d1cd93396738b4225ef6ce5de0ad3d41675d66f16acb0d099c8ef44a1`　108,247 bytes |
| **Revise2（本包產出）** | `949fa81e4cd5a5a4b193357de23410ae79b5322f584682189b647d98f71c9db4`　142,757 bytes |

落檔後複驗 Revise1 之 sha256 **未變** —— R-G72 守住（腳本末行 assert）。
母本（`10_Reviewing/…_20260819.xlsx`）**本包未開啟**。

**zip member 差異與 dv 計數**

```
zip member 數        42 → 42（增刪皆 0）
相異之 member        xl/worksheets/sheet6.xml  ← 僅此一件
dataValidation       sheet6: 3 → 3；x14:dataValidation 0 → 0
                     sheet5: 1 → 1
surgical_save 回報   sheets_patched = 1847 cells（含 No.#／tc_id 之重賦）
```

**列數**

```
資料列（F 欄非空）    243 → 241     （刪 Revise1 row 23、row 48）
物理列               252 → 252     （見 §7 偏差 1：surgical_save 無刪列能力，
                                    以「內容上移 ＋ 尾 2 列清空」為之，
                                    row 251／252 為真空列）
```

---

## §2　§〇 七項目標數字之逐項現算（`vs_cf03_lint.py` 實跑）

```
資料列 243 → 241                        241            PASS
Requirement 覆蓋                        237 / 237      PASS（237 個皆命中 037 之 leaf）
未生成列 23 → 0                          0  []          PASS
PENDING 12 → 0                          0  []          PASS
撤回 DR（15／21／24′／25／26／18／22）
  ＋ BLOCKED 之工作簿引用 → 0             0  []          PASS
IMPL_GAP → 0                            0  []          PASS
tc_id 重賦連續                           NR1L-VehicleSetting-001 … -241  PASS
仍具名之未結 DR                          DR-5-B，39 列（§7 偏差 3）
```

`NOT GENERATED`／`HELD OUT` 兩個分類詞於全簿亦歸 **0**。

---

## §3　覆蓋之來源歸屬（§七.3 之總表）

23 列由「不生成」轉為生成（下放包 §二 標題作「19 列」，實為 **23 列**，見 §7 偏差 2），
逐列全文見 §4。每列所用之訊號名與值標籤之來源：

| Revise2 row | Revise1 row | Requirement | 訊號／值標籤取自 |
|---|---|---|---|
| 91 | 93 | OneStageHeatedSeat-039 | 037 **HeatedSeat** D 欄 r46：`$Heated_Seats$` / `[Front Seats]` |
| 105 | 107 | TwoStagesHeatedSeat-055 | 037 HeatedSeat D 欄 r62：同上 |
| 125 | 127 | ThreeStagesHeatedSeat-078 | 037 HeatedSeat D 欄 r85：同上 |
| 177 | 179 | TwoStagesVentedSeatsManagement-037 | 037 **VentedSeat** D 欄 r44：`Cooled_Seats` / `"Front Seats"` |
| 197 | 199 | ThreeStagesVentedSeatsManagement-060 | 037 VentedSeat D 欄 r67：同上 |
| 106 | 108 | TwoStagesHeatedSeat-056 | 037 HeatedSeat D 欄 r63：`$Heated_Seats_Levels$` / `[Two Levels]` |
| 126 | 128 | ThreeStagesHeatedSeat-079 | 037 HeatedSeat D 欄 r86：`[Three Levels]` |
| 178 | 180 | TwoStagesVentedSeatsManagement-038 | 037 VentedSeat D 欄 r45：`$Heated_Steats_Levels$` / `"Two Levels"`（拼字照抄） |
| 198 | 200 | ThreeStagesVentedSeatsManagement-061 | 037 VentedSeat D 欄 r68：`"Three Levels"` |
| 92 | 94 | OneStageHeatedSeat-040 | 037 HeatedSeat D 欄 r47 末句：`$STATUS_CSWM.FR_HS_STATFailSts$` / `[Fail_Present]`；baseline 之 `[HS_HI]` 取自同節 leaf -024 |
| 243 | 245 | HeatedSteeringWheelManagement-029 | 037 **Heated_Steering_Wheel** D 欄 r36：`$HSW_Stat$`=OFF／`$HSW_StatFailSts$`=Fail_Not_Present／`HSW_Cmd_Tlm`=ON |
| 244 | 246 | HeatedSteeringWheelManagement-030 | 037 HSW D 欄 r37：同上，ON → OFF |
| 247 | 249 | HeatedSteeringWheelManagement-033 | 037 HSW D 欄 r40：`$HSW_Stat$` transitions to `[ON]` |
| 248 | 250 | HeatedSteeringWheelManagement-034 | 037 HSW D 欄 r41：`$HSW_Stat$` = `[OFF]` |
| 54 | 56 | PHEVFeatures-017 | 037 **Common Features** D 欄 r24（章節層）＋ **Q 欄**（CAN trigger → HMI display） |
| 67 | 69 | LeftFrontHeatedSeat-011 | 037 HeatedSeat D 欄 r18 ＋ Q 欄；`$EngRun_Stat$` 以 `STATUS_CCAN3.EngineSts` 觀察 |
| 228 | 230 | HeatedSteeringWheel-010 | 037 HSW D 欄 r17 ＋ Q 欄：同上 |
| 250 | 252 | HeatedSteeringWheel-009 | 037 HSW D 欄 r16 ＋ Q 欄；**無 CFTS044 錨**（見 §7 偏差 5） |
| 62 | 64 | LeftFrontHeatedSeat-006 | 037 HeatedSeat D 欄 r13：`$HeatedSeatFL$` / `[HS_OFF]`…`[HS_HI]`；上半 verbatim 取 CFTS044-4858308 |
| 78 | 80 | RightFrontHeatedSeat-024 | 037 HeatedSeat D 欄 r31：`$HeatedSeatFR$`；CFTS044-4858338 |
| 149 | 151 | LeftFrontVentedSeat-005 | 037 VentedSeat D 欄 r12：`$VentedSeatFL$`；CFTS044-4858368 |
| 164 | 166 | RightFrontVentedSeat-022 | 037 VentedSeat D 欄 r29：`$VentedSeatFR$`；CFTS044-4858399 |
| 223 | 225 | HeatedSteeringWheel-004 | 037 HSW D 欄 r11：`$HSW_Stat$` / `[OFF]`、`[ON]`；CFTS044-4858516 |

**§三 之 12 列 PENDING** 亦全數收尾（Revise2 row 69／71／84／86／155／157／170／172＝
請求／真值表型；row 231／233＝HSW；row 27＝`$PowerMode$`；row 34＝`[IGN_RUN]` 可選性）。

---

## §4　新生成 23 列之逐列全文


### §2.1 組態閘控型（9 列）

#### Revise2 row 91（Revise1 row 93）　`NR1L-VehicleSetting-082`　SWE1-VC-OneStageHeatedSeat-039

```text
test_item:
  Following requirements are valid only if PROXI parameter Heated_Seats == "Front Seats" OR "Front and Rear Seats".
  
  (One stage config gate: heated seats present vs absent)
pre_conditions:
  1. The HU is in its delivered configuration
  2. The configuration parameters are writable through the VHAL interface
input_test_data:
  NA
test_procedure:
  1. Set $Heated_Seats$ = [Front Seats]
  2. Power cycle the HU and press "Comfort" on Menu Bar
  3. Read the heated seat control and record its presence as Control_qualifying
  4. Set $Heated_Seats$ to a value other than [Front Seats]
  5. Power cycle the HU, press "Comfort" on Menu Bar and check that the heated seat control is not displayed
expected_result:
  1. $Heated_Seats$ = [Front Seats] is accepted
  2. The "Comfort" screen is displayed
  3. Control_qualifying is recorded as displayed
  4. $Heated_Seats$ is set to a value other than [Front Seats]
  5. The heated seat control is not displayed
specification_reference:
  CFTS044-4859360
priority:
  P1
design_method:
  等價劃分 (Equivalence Partitioning, EP)
remarks:
  037 Q 欄為 "Requirement is not clear"；TC 依 D 欄之行為敘述生成（下放包 §一(c)）。訊號名與值標籤取自 037 HeatedSeat D 欄；step 4 之非合格值不造具體值（§8.4.1）
```

#### Revise2 row 105（Revise1 row 107）　`NR1L-VehicleSetting-096`　SWE1-VC-TwoStagesHeatedSeat-055

```text
test_item:
  Following requirements are valid only if PROXI parameter Heated_Seats == "Front Seats" OR "Front and Rear Seats".
  
  (Two stages config gate: heated seats present vs absent)
pre_conditions:
  1. The HU is in its delivered configuration
  2. The configuration parameters are writable through the VHAL interface
input_test_data:
  NA
test_procedure:
  1. Set $Heated_Seats$ = [Front Seats]
  2. Power cycle the HU and press "Comfort" on Menu Bar
  3. Read the heated seat control and record its presence as Control_qualifying
  4. Set $Heated_Seats$ to a value other than [Front Seats]
  5. Power cycle the HU, press "Comfort" on Menu Bar and check that the heated seat control is not displayed
expected_result:
  1. $Heated_Seats$ = [Front Seats] is accepted
  2. The "Comfort" screen is displayed
  3. Control_qualifying is recorded as displayed
  4. $Heated_Seats$ is set to a value other than [Front Seats]
  5. The heated seat control is not displayed
specification_reference:
  CFTS044-4859376
priority:
  P1
design_method:
  等價劃分 (Equivalence Partitioning, EP)
remarks:
  037 Q 欄為 "Requirement is not clear"；TC 依 D 欄之行為敘述生成（下放包 §一(c)）。訊號名與值標籤取自 037 HeatedSeat D 欄；step 4 之非合格值不造具體值（§8.4.1）
```

#### Revise2 row 125（Revise1 row 127）　`NR1L-VehicleSetting-116`　SWE1-VC-ThreeStagesHeatedSeat-078

```text
test_item:
  Following requirements are valid only if PROXI parameter Heated_Seats == "Front Seats" OR "Front and Rear Seats".
  
  (Three stages config gate: heated seats present vs absent)
pre_conditions:
  1. The HU is in its delivered configuration
  2. The configuration parameters are writable through the VHAL interface
input_test_data:
  NA
test_procedure:
  1. Set $Heated_Seats$ = [Front Seats]
  2. Power cycle the HU and press "Comfort" on Menu Bar
  3. Read the heated seat control and record its presence as Control_qualifying
  4. Set $Heated_Seats$ to a value other than [Front Seats]
  5. Power cycle the HU, press "Comfort" on Menu Bar and check that the heated seat control is not displayed
expected_result:
  1. $Heated_Seats$ = [Front Seats] is accepted
  2. The "Comfort" screen is displayed
  3. Control_qualifying is recorded as displayed
  4. $Heated_Seats$ is set to a value other than [Front Seats]
  5. The heated seat control is not displayed
specification_reference:
  CFTS044-4859400
priority:
  P1
design_method:
  等價劃分 (Equivalence Partitioning, EP)
remarks:
  037 Q 欄為 "Requirement is not clear"；TC 依 D 欄之行為敘述生成（下放包 §一(c)）。訊號名與值標籤取自 037 HeatedSeat D 欄；step 4 之非合格值不造具體值（§8.4.1）
```

#### Revise2 row 177（Revise1 row 179）　`NR1L-VehicleSetting-168`　SWE1-VC-TwoStagesVentedSeatsManagement-037

```text
test_item:
  Following requirements are valid only if PROXI parameter Cooled_Seats == "Front Seats" OR "Front and Rear Seats".
  
  (Two stages config gate: cooled seats present vs absent)
pre_conditions:
  1. The HU is in its delivered configuration
  2. The configuration parameters are writable through the VHAL interface
input_test_data:
  NA
test_procedure:
  1. Set $Cooled_Seats$ = "Front Seats"
  2. Power cycle the HU and press "Comfort" on Menu Bar
  3. Read the vented seat control and record its presence as Control_qualifying
  4. Set $Cooled_Seats$ to a value other than "Front Seats"
  5. Power cycle the HU, press "Comfort" on Menu Bar and check that the vented seat control is not displayed
expected_result:
  1. $Cooled_Seats$ = "Front Seats" is accepted
  2. The "Comfort" screen is displayed
  3. Control_qualifying is recorded as displayed
  4. $Cooled_Seats$ is set to a value other than "Front Seats"
  5. The vented seat control is not displayed
specification_reference:
  CFTS044-4859438
priority:
  P1
design_method:
  等價劃分 (Equivalence Partitioning, EP)
remarks:
  037 Q 欄為 "Requirement is not clear"；TC 依 D 欄之行為敘述生成（下放包 §一(c)）。訊號名與值標籤取自 037 VentedSeat D 欄；step 4 之非合格值不造具體值（§8.4.1）
```

#### Revise2 row 197（Revise1 row 199）　`NR1L-VehicleSetting-188`　SWE1-VC-ThreeStagesVentedSeatsManagement-060

```text
test_item:
  Following requirements are valid only if PROXI parameter Cooled_Seats == "Front Seats" OR "Front and Rear Seats".
  
  (Three stages config gate: cooled seats present vs absent)
pre_conditions:
  1. The HU is in its delivered configuration
  2. The configuration parameters are writable through the VHAL interface
input_test_data:
  NA
test_procedure:
  1. Set $Cooled_Seats$ = "Front Seats"
  2. Power cycle the HU and press "Comfort" on Menu Bar
  3. Read the vented seat control and record its presence as Control_qualifying
  4. Set $Cooled_Seats$ to a value other than "Front Seats"
  5. Power cycle the HU, press "Comfort" on Menu Bar and check that the vented seat control is not displayed
expected_result:
  1. $Cooled_Seats$ = "Front Seats" is accepted
  2. The "Comfort" screen is displayed
  3. Control_qualifying is recorded as displayed
  4. $Cooled_Seats$ is set to a value other than "Front Seats"
  5. The vented seat control is not displayed
specification_reference:
  CFTS044-4859464
priority:
  P1
design_method:
  等價劃分 (Equivalence Partitioning, EP)
remarks:
  037 Q 欄為 "Requirement is not clear"；TC 依 D 欄之行為敘述生成（下放包 §一(c)）。訊號名與值標籤取自 037 VentedSeat D 欄；step 4 之非合格值不造具體值（§8.4.1）
```

#### Revise2 row 106（Revise1 row 108）　`NR1L-VehicleSetting-097`　SWE1-VC-TwoStagesHeatedSeat-056

```text
test_item:
  Also the requirements are valid only if $Heated_Steats_Levels$ == "Two Levels"
  
  (Two stages level gate: two levels vs other level count)
pre_conditions:
  1. The HU is in its delivered configuration
  2. The configuration parameters are writable through the VHAL interface
input_test_data:
  NA
test_procedure:
  1. Set $Heated_Seats$ = [Front Seats]
  2. Set $Heated_Seats_Levels$ = [Two Levels]
  3. Power cycle the HU and press "Comfort" on Menu Bar
  4. Read the heated seat control and record its presence as Control_qualifying
  5. Set $Heated_Seats_Levels$ to a value other than [Two Levels]
  6. Power cycle the HU, press "Comfort" on Menu Bar and check that the heated seat control is not displayed
expected_result:
  1. $Heated_Seats$ = [Front Seats] is accepted
  2. $Heated_Seats_Levels$ = [Two Levels] is accepted
  3. The "Comfort" screen is displayed
  4. Control_qualifying is recorded as displayed
  5. $Heated_Seats_Levels$ is set to a value other than [Two Levels]
  6. The heated seat control is not displayed
specification_reference:
  CFTS044-4859377
  CFTS044-4859378
priority:
  P1
design_method:
  決策表 (Decision Table Testing)
remarks:
  037 Q 欄為 "Requirement is not clear"；TC 依 D 欄之行為敘述生成（下放包 §一(c)）。雙條件列之負向側只翻第二條件，第一條件 $Heated_Seats$ = [Front Seats] 維持合格（§8.3）；非合格值不造具體值（§8.4.1）。CFTS 原句作 `Heated_Steats_Levels`，037 D 欄作 `Heated_Seats_Levels`；上半從 CFTS verbatim，步驟從 D 欄（§一(a)）
```

#### Revise2 row 126（Revise1 row 128）　`NR1L-VehicleSetting-117`　SWE1-VC-ThreeStagesHeatedSeat-079

```text
test_item:
  Also the requirements are valid only if $Heated_Steats_Levels$ == "Three Levels"
  
  (Three stages level gate: three levels vs other level count)
pre_conditions:
  1. The HU is in its delivered configuration
  2. The configuration parameters are writable through the VHAL interface
input_test_data:
  NA
test_procedure:
  1. Set $Heated_Seats$ = [Front Seats]
  2. Set $Heated_Seats_Levels$ = [Three Levels]
  3. Power cycle the HU and press "Comfort" on Menu Bar
  4. Read the heated seat control and record its presence as Control_qualifying
  5. Set $Heated_Seats_Levels$ to a value other than [Three Levels]
  6. Power cycle the HU, press "Comfort" on Menu Bar and check that the heated seat control is not displayed
expected_result:
  1. $Heated_Seats$ = [Front Seats] is accepted
  2. $Heated_Seats_Levels$ = [Three Levels] is accepted
  3. The "Comfort" screen is displayed
  4. Control_qualifying is recorded as displayed
  5. $Heated_Seats_Levels$ is set to a value other than [Three Levels]
  6. The heated seat control is not displayed
specification_reference:
  CFTS044-4859401
priority:
  P1
design_method:
  決策表 (Decision Table Testing)
remarks:
  037 Q 欄為 "Requirement is not clear"；TC 依 D 欄之行為敘述生成（下放包 §一(c)）。雙條件列之負向側只翻第二條件，第一條件 $Heated_Seats$ = [Front Seats] 維持合格（§8.3）；非合格值不造具體值（§8.4.1）。CFTS 原句作 `Heated_Steats_Levels`，037 D 欄作 `Heated_Seats_Levels`；上半從 CFTS verbatim，步驟從 D 欄（§一(a)）
```

#### Revise2 row 178（Revise1 row 180）　`NR1L-VehicleSetting-169`　SWE1-VC-TwoStagesVentedSeatsManagement-038

```text
test_item:
  Also the requirements are valid only if $Heated_Steats_Levels$ == "Two Levels"
  
  (Two stages vented level gate: two levels vs other level count)
pre_conditions:
  1. The HU is in its delivered configuration
  2. The configuration parameters are writable through the VHAL interface
input_test_data:
  NA
test_procedure:
  1. Set $Cooled_Seats$ = "Front Seats"
  2. Set $Heated_Steats_Levels$ = "Two Levels"
  3. Power cycle the HU and press "Comfort" on Menu Bar
  4. Read the vented seat control and record its presence as Control_qualifying
  5. Set $Heated_Steats_Levels$ to a value other than "Two Levels"
  6. Power cycle the HU, press "Comfort" on Menu Bar and check that the vented seat control is not displayed
expected_result:
  1. $Cooled_Seats$ = "Front Seats" is accepted
  2. $Heated_Steats_Levels$ = "Two Levels" is accepted
  3. The "Comfort" screen is displayed
  4. Control_qualifying is recorded as displayed
  5. $Heated_Steats_Levels$ is set to a value other than "Two Levels"
  6. The vented seat control is not displayed
specification_reference:
  CFTS044-4859439
  CFTS044-4859440
priority:
  P1
design_method:
  決策表 (Decision Table Testing)
remarks:
  037 Q 欄為 "Requirement is not clear"；TC 依 D 欄之行為敘述生成（下放包 §一(c)）。雙條件列之負向側只翻第二條件，第一條件 $Cooled_Seats$ = "Front Seats" 維持合格（§8.3）；非合格值不造具體值（§8.4.1）。CFTS 原句之拼字 `Steats` 與 037 D 欄一致，照抄不改
```

#### Revise2 row 198（Revise1 row 200）　`NR1L-VehicleSetting-189`　SWE1-VC-ThreeStagesVentedSeatsManagement-061

```text
test_item:
  Also the requirements are valid only if $Heated_Steats_Levels$ == "Three Levels"
  
  (Three stages vented level gate: three levels vs other level count)
pre_conditions:
  1. The HU is in its delivered configuration
  2. The configuration parameters are writable through the VHAL interface
input_test_data:
  NA
test_procedure:
  1. Set $Cooled_Seats$ = "Front Seats"
  2. Set $Heated_Steats_Levels$ = "Three Levels"
  3. Power cycle the HU and press "Comfort" on Menu Bar
  4. Read the vented seat control and record its presence as Control_qualifying
  5. Set $Heated_Steats_Levels$ to a value other than "Three Levels"
  6. Power cycle the HU, press "Comfort" on Menu Bar and check that the vented seat control is not displayed
expected_result:
  1. $Cooled_Seats$ = "Front Seats" is accepted
  2. $Heated_Steats_Levels$ = "Three Levels" is accepted
  3. The "Comfort" screen is displayed
  4. Control_qualifying is recorded as displayed
  5. $Heated_Steats_Levels$ is set to a value other than "Three Levels"
  6. The vented seat control is not displayed
specification_reference:
  CFTS044-4859465
priority:
  P1
design_method:
  決策表 (Decision Table Testing)
remarks:
  037 Q 欄為 "Requirement is not clear"；TC 依 D 欄之行為敘述生成（下放包 §一(c)）。雙條件列之負向側只翻第二條件，第一條件 $Cooled_Seats$ = "Front Seats" 維持合格（§8.3）；非合格值不造具體值（§8.4.1）。CFTS 原句之拼字 `Steats` 與 037 D 欄一致，照抄不改
```


### §2.2 故障圖示型（1 列）

#### Revise2 row 92（Revise1 row 94）　`NR1L-VehicleSetting-083`　SWE1-VC-OneStageHeatedSeat-040

```text
test_item:
  Regardless of the value of $HeatedSeatFR$, if $STATUS_CSWM.FR_HS_STATFailSts$ = [Fail_Present], the HMI shall update the relative icon on TLM_Display.GUI according to the TLM HMI Document.
  
  (Fail_Present overrides the seat state icon)
pre_conditions:
  1. The vehicle is equipped with the right front heated seat
  2. The "Comfort" screen is displayed
  3. CAN-B is connected to the bus simulator
input_test_data:
  NA
test_procedure:
  1. Send CAN: STATUS_CSWM.FR_HS_STATFailSts = 0 (Fail_Not_Present)
  2. Set $HeatedSeatFR$ = [HS_HI]
  3. Read the right front heated seat icon and record it as Icon_no_fail
  4. Send CAN: STATUS_CSWM.FR_HS_STATFailSts = 1 (Fail_Present)
  5. Read the right front heated seat icon and check that it differs from Icon_no_fail
expected_result:
  1. STATUS_CSWM.FR_HS_STATFailSts = 0 (Fail_Not_Present) is sent
  2. $HeatedSeatFR$ = [HS_HI] is accepted
  3. Icon_no_fail is recorded
  4. STATUS_CSWM.FR_HS_STATFailSts = 1 (Fail_Present) is sent
  5. The right front heated seat icon differs from Icon_no_fail
specification_reference:
  CFTS044-4859361
priority:
  P1
design_method:
  基礎故障注入 (Fault Injection Lite)
remarks:
  test_item 上半取 037 HeatedSeat D 欄末句（N 欄 ObjectID 4859361 之 CFTS 原句為前言式，與 D 欄之可測行為不對應，依下放包 §二 之 fallback 取 D 欄之行為句並具名）。`Regardless of` 以 baseline 覆蓋（§5.6）；baseline 之非 OFF 值 [HS_HI] 取自同節 leaf -024 之 D 欄與 CFTS 原句（§一(d)）；TLM HMI Document 未附，ER 只寫圖示改變，不寫改成什麼
```


### §2.4 HSW 命令型（4 列）

#### Revise2 row 243（Revise1 row 245）　`NR1L-VehicleSetting-234`　SWE1-VC-HeatedSteeringWheelManagement-029

```text
test_item:
  IF ($HSW_Stat$ == "OFF" AND STATUS_CSWM.HSW_StatFailSts == "Fail_Not_Present" AND SteeringWheelHeating.Req passes to "Requested")
  THEN
  TLM shall set TELEMATIC_VEHICLE_SETUP.HSW_Cmd_Tlm = "ON".
  
  (User request with $HSW_Stat$ = OFF and no fault)
pre_conditions:
  1. The vehicle is equipped with the heated steering wheel
  2. The "Comfort" screen is displayed
  3. CAN-B is connected to the bus simulator
input_test_data:
  NA
test_procedure:
  1. Set $HSW_Stat$ = OFF
  2. Send CAN: STATUS_CSWM.HSW_StatFailSts = 0 (Fail_Not_Present)
  3. Press the heated steering wheel control to drive SteeringWheelHeating.Req to Requested
  4. Read $HSW_Cmd_Tlm$ and check that it is ON
expected_result:
  1. $HSW_Stat$ = OFF is accepted
  2. STATUS_CSWM.HSW_StatFailSts = 0 (Fail_Not_Present) is sent
  3. SteeringWheelHeating.Req is driven to Requested
  4. $HSW_Cmd_Tlm$ reads ON
specification_reference:
  CFTS044-4859496
priority:
  P1
design_method:
  決策表 (Decision Table Testing)
remarks:
  三條件合取，值域取自 037 Heated_Steering_Wheel D 欄（$HSW_Stat$／$HSW_StatFailSts$／SteeringWheelHeating.Req）。$HSW_StatFailSts$ 於基線 DBC 有同名訊號，維持 CAN 記法；$HSW_Stat$／$HSW_Cmd_Tlm$ 為 API 層（§一(b)）
```

#### Revise2 row 244（Revise1 row 246）　`NR1L-VehicleSetting-235`　SWE1-VC-HeatedSteeringWheelManagement-030

```text
test_item:
  IF ($HSW_Stat$ == "ON" AND STATUS_CSWM.HSW_StatFailSts == "Fail_Not_Present" AND SteeringWheelHeating.Req passes to "Requested")
  THEN
  TLM shall set TELEMATIC_VEHICLE_SETUP.HSW_Cmd_Tlm = "OFF".
  
  (User request with $HSW_Stat$ = ON and no fault)
pre_conditions:
  1. The vehicle is equipped with the heated steering wheel
  2. The "Comfort" screen is displayed
  3. CAN-B is connected to the bus simulator
input_test_data:
  NA
test_procedure:
  1. Set $HSW_Stat$ = ON
  2. Send CAN: STATUS_CSWM.HSW_StatFailSts = 0 (Fail_Not_Present)
  3. Press the heated steering wheel control to drive SteeringWheelHeating.Req to Requested
  4. Read $HSW_Cmd_Tlm$ and check that it is OFF
expected_result:
  1. $HSW_Stat$ = ON is accepted
  2. STATUS_CSWM.HSW_StatFailSts = 0 (Fail_Not_Present) is sent
  3. SteeringWheelHeating.Req is driven to Requested
  4. $HSW_Cmd_Tlm$ reads OFF
specification_reference:
  CFTS044-4859497
priority:
  P1
design_method:
  決策表 (Decision Table Testing)
remarks:
  三條件合取，值域取自 037 Heated_Steering_Wheel D 欄（$HSW_Stat$／$HSW_StatFailSts$／SteeringWheelHeating.Req）。$HSW_StatFailSts$ 於基線 DBC 有同名訊號，維持 CAN 記法；$HSW_Stat$／$HSW_Cmd_Tlm$ 為 API 層（§一(b)）
```

#### Revise2 row 247（Revise1 row 249）　`NR1L-VehicleSetting-238`　SWE1-VC-HeatedSteeringWheelManagement-033

```text
test_item:
  IF ($HSW_Stat$ passes to "ON")
  THEN
  TLM shall set TELEMATIC_VEHICLE_SETUP.HSW_Cmd_Tlm = "ON".
  
  (State transition to ON drives the command)
pre_conditions:
  1. The vehicle is equipped with the heated steering wheel
  2. The "Comfort" screen is displayed
input_test_data:
  NA
test_procedure:
  1. Set $HSW_Stat$ = [OFF]
  2. Set $HSW_Stat$ = [ON] without pressing any control
  3. Read $HSW_Cmd_Tlm$ and check that it is ON
expected_result:
  1. $HSW_Stat$ = [OFF] is accepted
  2. $HSW_Stat$ = [ON] is accepted
  3. $HSW_Cmd_Tlm$ reads ON
specification_reference:
  CFTS044-4859500
priority:
  P1
design_method:
  狀態轉換 (State Transition Testing)
remarks:
  狀態變化觸發（非使用者請求），與 row 245／246 之觸發來源相異；值域取自 037 Heated_Steering_Wheel D 欄
```

#### Revise2 row 248（Revise1 row 250）　`NR1L-VehicleSetting-239`　SWE1-VC-HeatedSteeringWheelManagement-034

```text
test_item:
  IF ($HSW_Stat$ == "OFF")
  THEN
  TLM shall set TELEMATIC_VEHICLE_SETUP.HSW_Cmd_Tlm =  "OFF".
  
  (State observed as OFF drives the command)
pre_conditions:
  1. The vehicle is equipped with the heated steering wheel
  2. The "Comfort" screen is displayed
input_test_data:
  NA
test_procedure:
  1. Set $HSW_Stat$ = [ON]
  2. Set $HSW_Stat$ = [OFF] without pressing any control
  3. Read $HSW_Cmd_Tlm$ and check that it is OFF
expected_result:
  1. $HSW_Stat$ = [ON] is accepted
  2. $HSW_Stat$ = [OFF] is accepted
  3. $HSW_Cmd_Tlm$ reads OFF
specification_reference:
  CFTS044-4859501
priority:
  P1
design_method:
  狀態轉換 (State Transition Testing)
remarks:
  狀態變化觸發（非使用者請求），與 row 245／246 之觸發來源相異；值域取自 037 Heated_Steering_Wheel D 欄
```


### §2.6 其餘 4 列

#### Revise2 row 54（Revise1 row 56）　`NR1L-VehicleSetting-045`　SWE1-VC-PHEVFeatures-017

```text
test_item:
  This section defines features for plug-in hybrid electric vehicles (PHEV) that can be modified or displayed in the HU, Smartphone app, or Web, generated by ePowertrain, in the HU.
  
  (PHEV pages viewable on the Head Unit)
pre_conditions:
  1. The vehicle is a plug-in hybrid electric vehicle
  2. The HU is in its delivered configuration
input_test_data:
  NA
test_procedure:
  1. PROXI Hybrid_Type = 3 (Plugin Hybrid Electric Vehicle)
  2. Power cycle the HU
  3. Press "Hybrid Electric" button and check that the PHEV feature pages are displayed on the HU
expected_result:
  1. PROXI Hybrid_Type = 3 (Plugin Hybrid Electric Vehicle) is accepted
  2. The HU completes start-up
  3. The PHEV feature pages are displayed on the HU
specification_reference:
  CFTS044-4859529
priority:
  P2
design_method:
  功能測試 (Functional based ; no specific technique)
remarks:
  037 D 欄僅章節描述；依 Q 欄（CAN signal to be trigger／System update CAN value to HMI／HMI change the display state to new）生成一條薄 TC，本列為章節層條文（下放包 §2.6）
```

#### Revise2 row 67（Revise1 row 69）　`NR1L-VehicleSetting-058`　SWE1-VC-LeftFrontHeatedSeat-011

```text
test_item:
  For vehicles equipped with the Stop-Start feature, when the vehicle engine is not running ($EngRun_Stat$ <> [IDLE_STBL//UNLIMITED//LIMITED//RUN] && $Hybrid_Type$ <> [[PHEV] OR [BEV] OR [HEV] OR [REPB] OR [FCEV]]), please see Stop-Start System Feature section {CFTS044-443} for switch behavior.
  
  (Engine stopped with Stop-Start equipped)
pre_conditions:
  1. The vehicle is equipped with the Stop-Start feature
  2. The "Comfort" screen is displayed
  3. CAN-B is connected to the bus simulator
input_test_data:
  NA
test_procedure:
  1. Send CAN: STATUS_CCAN3.EngineSts = 2 (Engine_On)
  2. Read the left front heated seat control and record its state as Control_running
  3. Send CAN: STATUS_CCAN3.EngineSts = 0 (Engine_Off)
  4. Read the left front heated seat control and check that its state differs from Control_running
expected_result:
  1. STATUS_CCAN3.EngineSts = 2 (Engine_On) is sent
  2. Control_running is recorded
  3. STATUS_CCAN3.EngineSts = 0 (Engine_Off) is sent
  4. The left front heated seat control state differs from Control_running
specification_reference:
  CFTS044-4858317
  CFTS044-4858347
  CFTS044-4858377
  CFTS044-4858408
priority:
  P1
design_method:
  狀態轉換 (State Transition Testing)
remarks:
  轉指條文之不生成處置撤回；依 037 HeatedSeat Q 欄（Set the Start-Stop available CAN signal to be trigger／System update CAN value）生成。$EngRun_Stat$ 依 037 D 欄之對照以 STATUS_CCAN3.EngineSts 觀察；開關行為之具體樣式未附，ER 只寫狀態相異
```

#### Revise2 row 228（Revise1 row 230）　`NR1L-VehicleSetting-219`　SWE1-VC-HeatedSteeringWheel-010

```text
test_item:
  For vehicles equipped with the Stop-Start feature, when the vehicle engine is not running ($EngRun_Stat$ <> [IDLE_STBL//UNLIMITED//LIMITED//RUN] && $Hybrid_Type$ <> [[PHEV] OR [BEV] OR [HEV] OR [REPB] OR [FCEV]] ), please see Stop-Start System Feature section for switch behavior.
  
  (Engine stopped with Stop-Start equipped: steering wheel)
pre_conditions:
  1. The vehicle is equipped with the Stop-Start feature
  2. The "Comfort" screen is displayed
  3. CAN-B is connected to the bus simulator
input_test_data:
  NA
test_procedure:
  1. Send CAN: STATUS_CCAN3.EngineSts = 2 (Engine_On)
  2. Read the heated steering wheel control and record its state as Control_running
  3. Send CAN: STATUS_CCAN3.EngineSts = 0 (Engine_Off)
  4. Read the heated steering wheel control and check that its state differs from Control_running
expected_result:
  1. STATUS_CCAN3.EngineSts = 2 (Engine_On) is sent
  2. Control_running is recorded
  3. STATUS_CCAN3.EngineSts = 0 (Engine_Off) is sent
  4. The heated steering wheel control state differs from Control_running
specification_reference:
  CFTS044-4858529
priority:
  P1
design_method:
  狀態轉換 (State Transition Testing)
remarks:
  轉指條文之不生成處置撤回；依 037 Heated_Steering_Wheel Q 欄（Set the Start-Stop available CAN signal to be trigger／System update CAN value）生成。$EngRun_Stat$ 依 037 D 欄之對照以 STATUS_CCAN3.EngineSts 觀察；開關行為之具體樣式未附，ER 只寫狀態相異
```

#### Revise2 row 250（Revise1 row 252）　`NR1L-VehicleSetting-241`　SWE1-VC-HeatedSteeringWheel-009

```text
test_item:
  For vehicles not equipped with Stop-Start feature (i.e., $Hybrid_Type$ ≠ [PHEV, BEV, HEV, REPB, FCEV]) and when $EngRun_Stat$ ≠ [IDLE_STBL / UNLIMITED / LIMITED / RUN], the HMI shall:
  
  (Stop-Start not equipped: control greyed-out)
pre_conditions:
  1. The vehicle is not equipped with the Stop-Start feature
  2. The "Comfort" screen is displayed
  3. CAN-B is connected to the bus simulator
input_test_data:
  NA
test_procedure:
  1. Send CAN: STATUS_CCAN3.EngineSts = 2 (Engine_On)
  2. Read the heated steering wheel control and check that it is selectable
  3. Send CAN: STATUS_CCAN3.EngineSts = 0 (Engine_Off)
  4. Read the heated steering wheel control and check that it is disabled and shown greyed-out
expected_result:
  1. STATUS_CCAN3.EngineSts = 2 (Engine_On) is sent
  2. The heated steering wheel control is selectable
  3. STATUS_CCAN3.EngineSts = 0 (Engine_Off) is sent
  4. The heated steering wheel control is disabled and shown greyed-out
specification_reference:
  (空)
priority:
  P1
design_method:
  狀態轉換 (State Transition Testing)
remarks:
  本 leaf 之 037 Source Requirement ID 為 SYS-RA-CFTS100（非 CFTS044），無 CFTS044 ObjectID 可錨（DR-11），specification_reference 留空並具名；test_item 上半依下放包 §二 之 fallback 取 037 D 欄之行為句。依 Q 欄生成
```


### §〇 追加：B7 五列（R-VS102(2) 撤回後）

#### Revise2 row 62（Revise1 row 64）　`NR1L-VehicleSetting-053`　SWE1-VC-LeftFrontHeatedSeat-006

```text
test_item:
  Valid values for the $HeatedSeatFL$ are shown below. All other states shall be considered invalid by the HU.
  
  (Valid value set accepted, others ignored)
pre_conditions:
  1. The vehicle is equipped with the left front heated seat
  2. The "Comfort" screen is displayed
  3. The signal is writable through the VHAL interface
input_test_data:
  NA
test_procedure:
  1. Set $HeatedSeatFL$ = [Heated Seat Off / HS_OFF]
  2. Read the left front heated seat control and check that its state is Off
  3. Set $HeatedSeatFL$ = [Heated Seat Low / HS_LO]
  4. Read the left front heated seat control and check that its state is Low
  5. Set $HeatedSeatFL$ = [Heated Seat Medium / HS_MED]
  6. Read the left front heated seat control and check that its state is Medium
  7. Set $HeatedSeatFL$ = [Heated Seat High / HS_HI]
  8. Read the left front heated seat control and check that its state is High
  9. Set $HeatedSeatFL$ to a value outside the valid set above
  10. Read the left front heated seat control and check that its state is unchanged from the last valid value
expected_result:
  1. $HeatedSeatFL$ = [Heated Seat Off / HS_OFF] is accepted
  2. The left front heated seat control state is Off
  3. $HeatedSeatFL$ = [Heated Seat Low / HS_LO] is accepted
  4. The left front heated seat control state is Low
  5. $HeatedSeatFL$ = [Heated Seat Medium / HS_MED] is accepted
  6. The left front heated seat control state is Medium
  7. $HeatedSeatFL$ = [Heated Seat High / HS_HI] is accepted
  8. The left front heated seat control state is High
  9. $HeatedSeatFL$ is set to a value outside the valid set
  10. The left front heated seat control state is unchanged from High
specification_reference:
  CFTS044-4858308
priority:
  P1
design_method:
  等價劃分 (Equivalence Partitioning, EP)
remarks:
  本 leaf 之 $HeatedSeatFL$ 為 API／CarProperty 層訊號（037 D 欄逐字 `via the VHAL interface`），非匯流排 raw，「raw 全數已定義」不構成不生成之依據；值標籤逐字取 037 D 欄與同列 CFTS 原句；無效值不造具體值（§8.4.1）
```

#### Revise2 row 78（Revise1 row 80）　`NR1L-VehicleSetting-069`　SWE1-VC-RightFrontHeatedSeat-024

```text
test_item:
  Valid values for the $HeatedSeatFR$ are shown below. All other states shall be considered invalid by the HU.
  
  (Valid value set accepted, others ignored: passenger side)
pre_conditions:
  1. The vehicle is equipped with the right front heated seat
  2. The "Comfort" screen is displayed
  3. The signal is writable through the VHAL interface
input_test_data:
  NA
test_procedure:
  1. Set $HeatedSeatFR$ = [Heated Seat Off / HS_OFF]
  2. Read the right front heated seat control and check that its state is Off
  3. Set $HeatedSeatFR$ = [Heated Seat Low / HS_LO]
  4. Read the right front heated seat control and check that its state is Low
  5. Set $HeatedSeatFR$ = [Heated Seat Medium / HS_MED]
  6. Read the right front heated seat control and check that its state is Medium
  7. Set $HeatedSeatFR$ = [Heated Seat High / HS_HI]
  8. Read the right front heated seat control and check that its state is High
  9. Set $HeatedSeatFR$ to a value outside the valid set above
  10. Read the right front heated seat control and check that its state is unchanged from the last valid value
expected_result:
  1. $HeatedSeatFR$ = [Heated Seat Off / HS_OFF] is accepted
  2. The right front heated seat control state is Off
  3. $HeatedSeatFR$ = [Heated Seat Low / HS_LO] is accepted
  4. The right front heated seat control state is Low
  5. $HeatedSeatFR$ = [Heated Seat Medium / HS_MED] is accepted
  6. The right front heated seat control state is Medium
  7. $HeatedSeatFR$ = [Heated Seat High / HS_HI] is accepted
  8. The right front heated seat control state is High
  9. $HeatedSeatFR$ is set to a value outside the valid set
  10. The right front heated seat control state is unchanged from High
specification_reference:
  CFTS044-4858338
priority:
  P1
design_method:
  等價劃分 (Equivalence Partitioning, EP)
remarks:
  本 leaf 之 $HeatedSeatFR$ 為 API／CarProperty 層訊號（037 D 欄逐字 `via the VHAL interface`），非匯流排 raw，「raw 全數已定義」不構成不生成之依據；值標籤逐字取 037 D 欄與同列 CFTS 原句；無效值不造具體值（§8.4.1）
```

#### Revise2 row 149（Revise1 row 151）　`NR1L-VehicleSetting-140`　SWE1-VC-LeftFrontVentedSeat-005

```text
test_item:
  Valid values for the $VentedSeatFL$ are shown below. All other states shall be considered invalid by the HU.
  
  (Valid value set accepted, others ignored: vented driver side)
pre_conditions:
  1. The vehicle is equipped with the left front vented seat
  2. The "Comfort" screen is displayed
  3. The signal is writable through the VHAL interface
input_test_data:
  NA
test_procedure:
  1. Set $VentedSeatFL$ = [Vented Seat Off / VS_OFF]
  2. Read the left front vented seat control and check that its state is Off
  3. Set $VentedSeatFL$ = [Vented Seat Low / VS_LO]
  4. Read the left front vented seat control and check that its state is Low
  5. Set $VentedSeatFL$ = [Vented Seat Medium / VS_MED]
  6. Read the left front vented seat control and check that its state is Medium
  7. Set $VentedSeatFL$ = [Vented Seat High / VS_HI]
  8. Read the left front vented seat control and check that its state is High
  9. Set $VentedSeatFL$ to a value outside the valid set above
  10. Read the left front vented seat control and check that its state is unchanged from the last valid value
expected_result:
  1. $VentedSeatFL$ = [Vented Seat Off / VS_OFF] is accepted
  2. The left front vented seat control state is Off
  3. $VentedSeatFL$ = [Vented Seat Low / VS_LO] is accepted
  4. The left front vented seat control state is Low
  5. $VentedSeatFL$ = [Vented Seat Medium / VS_MED] is accepted
  6. The left front vented seat control state is Medium
  7. $VentedSeatFL$ = [Vented Seat High / VS_HI] is accepted
  8. The left front vented seat control state is High
  9. $VentedSeatFL$ is set to a value outside the valid set
  10. The left front vented seat control state is unchanged from High
specification_reference:
  CFTS044-4858368
priority:
  P1
design_method:
  等價劃分 (Equivalence Partitioning, EP)
remarks:
  本 leaf 之 $VentedSeatFL$ 為 API／CarProperty 層訊號（037 D 欄逐字 `via the VHAL interface`），非匯流排 raw，「raw 全數已定義」不構成不生成之依據；值標籤逐字取 037 D 欄與同列 CFTS 原句；無效值不造具體值（§8.4.1）
```

#### Revise2 row 164（Revise1 row 166）　`NR1L-VehicleSetting-155`　SWE1-VC-RightFrontVentedSeat-022

```text
test_item:
  Valid values for the $VentedSeatFR$ are shown below. All other states shall be considered invalid by the HU.
  
  (Valid value set accepted, others ignored: vented passenger side)
pre_conditions:
  1. The vehicle is equipped with the right front vented seat
  2. The "Comfort" screen is displayed
  3. The signal is writable through the VHAL interface
input_test_data:
  NA
test_procedure:
  1. Set $VentedSeatFR$ = [Vented Seat Off / VS_OFF]
  2. Read the right front vented seat control and check that its state is Off
  3. Set $VentedSeatFR$ = [Vented Seat Low / VS_Lo]
  4. Read the right front vented seat control and check that its state is Low
  5. Set $VentedSeatFR$ = [Vented Seat Medium / VS_MED]
  6. Read the right front vented seat control and check that its state is Medium
  7. Set $VentedSeatFR$ = [Vented Seat High / VS_HI]
  8. Read the right front vented seat control and check that its state is High
  9. Set $VentedSeatFR$ to a value outside the valid set above
  10. Read the right front vented seat control and check that its state is unchanged from the last valid value
expected_result:
  1. $VentedSeatFR$ = [Vented Seat Off / VS_OFF] is accepted
  2. The right front vented seat control state is Off
  3. $VentedSeatFR$ = [Vented Seat Low / VS_Lo] is accepted
  4. The right front vented seat control state is Low
  5. $VentedSeatFR$ = [Vented Seat Medium / VS_MED] is accepted
  6. The right front vented seat control state is Medium
  7. $VentedSeatFR$ = [Vented Seat High / VS_HI] is accepted
  8. The right front vented seat control state is High
  9. $VentedSeatFR$ is set to a value outside the valid set
  10. The right front vented seat control state is unchanged from High
specification_reference:
  CFTS044-4858399
priority:
  P1
design_method:
  等價劃分 (Equivalence Partitioning, EP)
remarks:
  本 leaf 之 $VentedSeatFR$ 為 API／CarProperty 層訊號（037 D 欄逐字 `via the VHAL interface`），非匯流排 raw，「raw 全數已定義」不構成不生成之依據；值標籤逐字取 037 D 欄與同列 CFTS 原句；無效值不造具體值（§8.4.1）
```

#### Revise2 row 223（Revise1 row 225）　`NR1L-VehicleSetting-214`　SWE1-VC-HeatedSteeringWheel-004

```text
test_item:
  Valid values for the $HSW_Stat$ are shown below. All other states shall be considered invalid by the HU.
  
  (Dual state valid value set accepted, others ignored)
pre_conditions:
  1. The vehicle is equipped with the heated steering wheel
  2. The "Comfort" screen is displayed
  3. The signal is writable through the VHAL interface
input_test_data:
  NA
test_procedure:
  1. Set $HSW_Stat$ = [Heated steering wheel off / OFF]
  2. Read the heated steering wheel control and check that its state is Off
  3. Set $HSW_Stat$ = [Heated steering wheel on / ON]
  4. Read the heated steering wheel control and check that its state is On
  5. Set $HSW_Stat$ to a value outside the valid set above
  6. Read the heated steering wheel control and check that its state is unchanged from the last valid value
expected_result:
  1. $HSW_Stat$ = [Heated steering wheel off / OFF] is accepted
  2. The heated steering wheel control state is Off
  3. $HSW_Stat$ = [Heated steering wheel on / ON] is accepted
  4. The heated steering wheel control state is On
  5. $HSW_Stat$ is set to a value outside the valid set
  6. The heated steering wheel control state is unchanged from On
specification_reference:
  CFTS044-4858516
priority:
  P1
design_method:
  等價劃分 (Equivalence Partitioning, EP)
remarks:
  本 leaf 之 $HSW_Stat$ 為 API／CarProperty 層訊號（037 D 欄逐字 `via the VHAL interface`），非匯流排 raw，「raw 全數已定義」不構成不生成之依據；值標籤逐字取 037 D 欄與同列 CFTS 原句；無效值不造具體值（§8.4.1）
```


---

## §5　tc_id 重賦對照表（§七.4）

```
features/vehicle_setting/reports/cfts044_tcid_remap.tsv
欄：old_row  new_row  old_tc_id  new_tc_id  requirement_id
243 列（241 列重賦 ＋ 2 列 new_tc_id = DELETED）
```

規則：刪 Revise1 row 23／48 後，其後之列依序上移；`tc_id` 依 §10.3 重賦為
`NR1L-VehicleSetting-{001..241}`，`No.#`（B 欄）同步重賦為 1..241。
舊 → 新之位移：`row < 23` 不變；`23 < row < 48` 減 1；`row > 48` 減 2。

**刪列之依據複核**：兩對之 D／G／H／I／J／K／N／P／R／S／AH 逐字相同，
差異只在 L（與 M）之一個空標籤：

```
row 23 L step2  … the ""            softkey button …     ← 刪
row 24 L step2  … the "Headrest Dump" softkey button …   ← 留
row 48 L step2  Press the "Screen Off" soft button … Audio__initial（變數名筆誤）
        M step2  The touchscreen display is off playback continues …（漏字）  ← 刪
row 49 L step2  Press the ""          soft button … Audio_track_initial      ← 留，填 "Screen Off"
```
**保留者為完整之一列**，與下放包 §四之指定（刪 23／48、留 24／49）一致；
下放包所述「五欄逐字相同」實為「除該空標籤外逐字相同」，見 §7 偏差 4。

---

## §6　§六 1–8 之全簿計數（`vs_cf03_lint.py` 全綠）

| § | 項 | 結果 |
|---|---|---|
| 六.1 | 全形 `；`／`，`（作者側四欄） | **0**　PASS |
| 六.2 | `Set PROXI` | **0**　PASS（34 處全改為 `PROXI`） |
| 六.3 | 行尾句號 | **0**　PASS |
| 六.3 | 行首／行尾空白 | **0**　PASS（含 `test_item`／`specification_reference`／`remarks`） |
| 六.3 | `$MESSAGE.Signal$` 三件組 | **0**　PASS |
| 六.3 | 方括號（**UI 標籤式**） | **0**　PASS |
| 六.3 | └ §一(a) 之值標籤 token | **346 處**（IN §11 例外＋profile `[OVERRIDE §11]`，不計違規 —— 見 §9-2） |
| 六.4 | Procedure ↔ ER 逐列 1:1 | **0 不等**　PASS |
| 六.4 | 每列 ≥2 步 | **0 不足**　PASS |
| 六.5 | `specification_reference` 格式 `CFTS044-{7 位}` | **0 不合**　PASS |
| 六.5 | 一行一個、升冪 | **0 亂序**　PASS |
| 六.5 | └ 無 CFTS044 錨之列 | row 250（具名，見 §7 偏差 5） |
| 六.6 | Priority ∈ P0–P3 | **0 越域**　PASS（P0 24／P1 210／P2 7） |
| 六.6 | `design_method` ∈ IN §12 之九值 | **0 越域**　PASS |
| 六.7 | §10.6 嚴格等價重複 | **命中 1 組：row 19／row 52**（回報，不自行合併 —— §9-1） |
| 六.8 | row 13 讀回自注訊號 | **0**　PASS（驗證點改為 HMI 指示燈觀察） |
| R-S4 | 括號下半存在／無中文／同 Req 相異 | **0 違規**　PASS |

**§五 導航與標籤之全簿計數**

```
Press "Comfort" on Menu Bar        22 處 / 13 列   （新生成之組態閘控列）
"Comfort" screen                  229 處 / 206 列
"Controls" screen / "Rear View Camera"  沿 §五 改寫，Open the … from the Menu Bar 全數轉為 Press … on Menu Bar
"3rd Headrest Fold"                28 處 / 13 列
"Hybrid Electric" button            9 處 /  5 列
row 49 之 ""                        → "Screen Off"

殘留檢查（皆為 0）
  "Headrest Dump"                   0        Climate screen        0
  Heated / Vented Seats screen      0        Open the … screen     0
  TELEMATIC_* message 名（作者側）    0
bus trace 殘留                       1 列（row 46，`TGW_DISP_STAT` 確為 DBC 訊號，依 §一(b) 保留）
Send CAN:（確為 CAN 之訊號）        278 處 / 162 列
```

---

## §7　具名偏差與擴充（8 項）

**1. `surgical_save` 無刪列能力 —— 以「內容上移 ＋ 尾 2 列清空」實現刪 2 列**
下放包定「落檔一律 `surgical_save`」，而該模組為 cell-diff 落檔器（`patch_sheet_xml`
只改儲存格，無 row 刪除）；`ws.delete_rows()` 須配 `wb.save()`，而 `wb.save()` 會毀
x14 dataValidation（R16／R-G3），二者不可得兼。
**處置**：保留物理列 252，內容整體上移，row 251／252 清空為真空列。
資料列（F 欄非空）因而為 **241**，符合 §〇。**未改處置本身**。
⚠ 交付本尾端將有 2 個視覺空白列，若不可接受須另立刪列能力（`surgical_delete_rows`）。

**2. 下放包 §二 標題之「19 列」實為 23 列**
§二各小節之列數為 §2.1 九 ＋ §2.2 一 ＋ §2.3 五 ＋ §2.4 四 ＋ §2.6 四 = **23**，
其中 §2.3 之五列（87／173／233／234／235）本即 PENDING 列而非「不生成」列。
Revise1 之「不生成」列（`test_item` 空）實測 **23 列**：18 列由 §2.1／2.2／2.4／2.6 覆蓋，
**餘 5 列（64／80／151／166／225）為 VS-CF-01 §2.2 之 B7 列，§二未指派**。
**處置**：依 §〇「未生成列 → 0」與 §七.6「撤 `R-VS102(2)`（B7 之依據已不成立）」，
該 5 列一併生成（見 §3 表末五列、§4 末組）。其生成依據為 037 D 欄逐字之
`via the VHAL interface`／`CarPropertyManager … get/set methods` —— 訊號為 API 層，
非匯流排 raw，故「有效集之外可注入」成立，以 EP 之正負配對書寫。

**3. `DR-5-B` 之 39 列不撤，降為非阻擋具名**
§〇 之枚舉為「DR-15／21／24′／25／26／BLOCKED」，§七.7 之撤回清單亦同 ——
**`DR-5-B` 兩處皆未列**。該 DR 之內容為「畫面層之具體樣式待 TLM HMI Document／
HMI requirements」，屬未到件之外部文件，非本包可解。
**處置**：39 列之 `BLOCKED:` 前綴撤除，改標 `dr_dependent = DR-5-B：…`；
其 TC 皆可執行（ER 只寫可觀察之變化，不寫具體樣式，與 §2.2 對 row 94 之範式同）。
`DR-18`（12 列）與 `DR-22`（2 列）同理降為非阻擋，惟其**具體待覆點已由 D 欄解掉**
（`_mid` 取 D 欄原標籤、`$HdRstRelRq$ = [Pressed]`），故工作簿內不再引其號。

**4. 刪列之依據敘述更正**
下放包 §四稱 row 23／48 與 24／49「五欄逐字相同」；實測 **L 欄（row 48 另含 M 欄）相異**，
差異即該空標籤與變數名筆誤。刪除之對象與保留之對象不變（保留者為完整之一列），
**未改處置本身**，僅更正敘述。

**5. row 252（Revise2 row 250）無 CFTS044 ObjectID**
`SWE1-VC-HeatedSteeringWheel-009` 之 037 `Source Requirement ID` 為 **`SYS-RA-CFTS100`**
（非 CFTS044），`data/leaf_to_reqid.tsv` 標 `BLOCKED（R-VS17）—— 即 DR-11`。
**處置**：TC 照生成（§〇「每個需求都要有產出」），`specification_reference` **留空**並於
Remarks 具名 —— 造一個 CFTS044 號即為造值（§8.4.1）。`test_item` 上半依 §二 之 fallback
取 037 D 欄之行為句。§六.5 之格式檢查對空值不判違規。

**6. §一(b) 之適用範圍由「44 列」擴為全簿之 API 層訊號列**
§2.5 稱 `IMPL_GAP` 為 44 列，實測為 **56 列**；且 `TELEMATIC_*` message 名於作者側四欄
共出現於 **76 列**（含 row 18／72／96–99／158／232 等非 `IMPL_GAP` 列）。
§一(b) 之條文為「逐條適用於本包所有新生成／改寫之列」，若只改 56 列，
**同一訊號將在同一本工作簿內有兩種寫法**。
**處置**：76 列全數改為 API 記法，另加 row 30／31／32／33（`$HdRstRelRq$`）共 **80 列**。
`TELEMATIC_*` 於作者側四欄之殘留為 **0**。

**7. `test_item` 上半之 fallback 用於 row 94（Revise2 row 92）**
§二 定上半取 N 欄 ObjectID 之 CFTS 原句 verbatim；row 94 之 ObjectID `4859361` 原句為
`Also the requirements are valid only if $Heated_Seat_Levels$ == "One Level"`（前言式），
**與 §2.2 所定之可測行為（Fail_Present 圖示）不對應**。
**處置**：依 §二之 fallback 取 037 D 欄之行為句（即 §2.2 所引之 `Regardless of …` 全句），
Remarks 具名。row 250 同此路徑（無 ObjectID）。

**8. `$*_Cmd_Tlm$` 之書寫取 §2.5 之短名式**
037 D 欄逐字為 `$TELEMATIC_VEHICLE_SETUP.FL_HS_Cmd_Tlm$`（含 message 名），
而 §一(b) 定「不得寫 message 名」、§2.5 明給 `$FL_HS_Cmd_Tlm$`。
**處置**：從 §2.5 之短名式。此為 §一(a)「用 D 欄之原表示法」與 §一(b)／§2.5 之衝突，
以較具體者勝出。

**另二項順帶更正（不改處置）**
- 037 `OneStageHeatedSeat-043` 之 D 欄寫 `FR_HS_Cmd_Tlm`，而其 N 欄 CFTS 原句
  （`CFTS044-4859364`）寫 `FL_HS_Cmd_Tlm`。Revise2 row 95 從 CFTS 原句之側別（FL），
  Remarks 具名待覆。
- row 96／98（Revise2 row 94／96）之 D 欄只有 `.Req` 一個條件，單步不足 §六.4 之 ≥2 步；
  以同節 leaf -043／-045 之 `[Not_Pressed]` 補一步基線讀取，Remarks 具名。

---

## §8　台帳落檔（§七.6、§七.7）

**`features/vehicle_setting/RULINGS.md`**（依 R-TM13：原條文不刪不改，於檔末具名撤銷）

```
新增段  ### R-VS102 之部分撤銷（VS-CF-03，2026-09-08）
        撤銷 R-VS102(2) —— `B7-no-invalid-raw` 不生成之依據
        理由：該 5 leaf 之訊號於 037 D 欄逐字為 API／CarProperty 層
              （`via the VHAL interface`），不是匯流排上之 raw；
              「2 bit／1 bit raw 全數已定義」量的是 DBC，答的卻是 API 層之值域。
        效力：(1)(3)(4)(5)(6) 不受影響；(3)(4) 之 dr_dependent 前綴已自工作簿撤除。
        施作：五列已於 Revise2 row 62／78／149／164／223 全數生成。
```

**`features/vehicle_setting/scripts/vf230_wvf44_writability.py`**（docstring 分類碼段）

```
B7-no-invalid-raw 之登記行加刪除線（~~…~~）保留，其下加撤銷註 ——
「B7 不再是有效之 blocker 分類碼」，依 R-TM13 不刪行。
```

**`features/vehicle_setting/DATA_REQUESTS.md`**（檔末新增「撤回批次 —— VS-CF-03」）

```
撤回（不刪，加註）  DR-15、DR-21、DR-24′、DR-25、DR-26、B7 草稿（未取號）
共同理由            問錯了來源 —— 五問皆以 DBC／PROXI／LID 為值域之唯一來源而發，
                    而 037 D 欄已逐 leaf 給出訊號名與值標籤，且該系列於 D 欄
                    逐字為 API／CarProperty 層訊號，不是匯流排上之編碼。
降為非阻塞（DR 仍開）DR-18、DR-22（工作簿內不再引其號）
不在撤回清單         DR-5-B（39 列改標 dr_dependent，待外部文件到件）
逐 DR 附「Revise2 之處置」欄，指出各自落在哪些新列號。
```

---

## §9　待 Pei 裁（2 項）

**9-1　§六.7 之重複命中：Revise2 row 19 與 row 52**

```
row 19  NR1L-VehicleSetting-010   SWE1-VC-SwitchLHD/RHDConfiguration-010   CFTS044-4858560
row 52  NR1L-VehicleSetting-043   SWE1-VC-SwitchLHD/RHDConfiguration-013   CFTS044-4859509
```

二者之 `test_item`／`pre_conditions`／`input_test_data`／`test_procedure`／`expected_result`
**五欄逐字相同**（連括號下半 `(Right hand drive configuration)` 亦同），僅 Requirement ID
與 ObjectID 相異 —— 兩個 CFTS 物件之條文本身即逐字相同
（`When $DriverSide$ = [Right hand drive] the HMI shall be modified as defined by HMI
requirements.`）。二者皆掛 `dr_dependent = DR-5-B`（右駕版面之具體定義待 HMI requirements）。

依 §六.7「命中即回報，不自行合併」**未動**。可能之處置：
(a) 視為上游條文重複，兩列並存（覆蓋完整性優先，現況）；
(b) 併為一列而以 `specification_reference` 兩行同錨（列數降為 240，牴觸 §〇 之 241）；
(c) 待 DR-5-B 覆文後以右駕版面之不同面向拆分。**建議 (a)，待覆再議。**

**9-2　§六.3 之「方括號全簿歸零」與 §一(a) 之值標籤衝突**

§六.3 要求作者側四欄＋括號下半之方括號歸零；Revise1 實測該類**本已為 0**。
而 §一(a) 為本包之核心規則，明定值標籤用 D 欄原表示法（`[HS_PSD]`／`[Front Seats]`／
`[Fail_Present]` 等），§2.1／§2.3 之範式亦逐字如此書寫 —— 二者在同一份下放包內直接相斥。

**本包之解**：依 IN §11 之 `Exception (profile-scoped)`（「tokens quoted verbatim from a
cited source … may retain the source's notation when the feature profile says so」）
與 profile 之 `[OVERRIDE §11]`，將 §一(a) 之值標籤視為**來源逐字 token**，
與 §六.3 所禁之**UI 標籤式方括號**分兩類計數：

```
UI 標籤式方括號（§六.3 之規制對象）    0 處    ← 歸零達成
§一(a) 值標籤 token                346 處    ← 依核心規則書寫
```

若 Pei 認為 §六.3 應涵蓋值標籤，則須改以雙引號書寫（`Set $Heated_Seats$ = "Front Seats"`），
一次改 346 處，**請裁**。

---

## §10　工具旗標與自報

**工具旗標（逐次聲明）**

```
openpyxl                  僅作計算層；全域無 wb.save()
backend.xlsx_surgical
  surgical_save(verify=True)  ← 唯一落檔路徑，verify_structure 未降級
  surgical_insert_rows        未用
  surgical_restyle            未用
ws.delete_rows()          未用（見 §7 偏差 1）
母本                      未開啟、未寫入
inputs/、output/          未寫入
寫入路徑                  features/vehicle_setting/sandbox/cfts044/ 一處
                          ＋ reports/cfts044_tcid_remap.tsv
                          ＋ RULINGS.md／DATA_REQUESTS.md／scripts/（台帳與腳本）
```

**新增檔**

```
features/vehicle_setting/scripts/vs_cf03_apply.py    施作（可重跑，冪等）
features/vehicle_setting/scripts/vs_cf03_lint.py     §六／§〇 之檢查與現算
features/vehicle_setting/reports/cfts044_tcid_remap.tsv
features/vehicle_setting/sandbox/cfts044/cfts044_20260819_Revise2.xlsx
```

**自報**

1. **§一(b) 之邊界由我判定，非下放包明給。** §一(b) 枚舉 `$*_RQ$／$*_Cmd_Tlm$／$*_Stat$`
   為 API 層，並定 CAN 記法「只適用於確為 CAN 之訊號」。我以**基線 DBC 是否有同名
   `SG_` 定義**為「確為 CAN」之判準：`*_STATSts`／`*_STATFailSts`／`Tri_Level_HSW_StatSts`／
   `EngineSts`／`ESS_ENG_ST`／`CmdIgnSts`／`TGW_DISP_STAT` 命中而維持 `Send CAN:`；
   `$HeatedSeatFL/FR$`／`$VentedSeatFL/FR$`／`$HdRstRelRq$`／`$PowerMode$` 未命中同名而轉
   API 記法。⚠ **`FL_HS_Tlm`／`HSW_Tlm` 於 DBC 確有同名**，但 037 D 欄稱該請求為
   `$FL_HS_RQ$`／`$HSW_RQ_TGW$` —— 依 §一(a)「不代換為 DBC 名」，我**未**沿用 `*_Tlm`。
   此一判準若與 Pei 之本意不同，影響 80 列，**請核**。
2. **037 D 欄之解析為程式化萃取。** `$*_Cmd_Tlm$` 58 列之條件與命令值以 regex 自 D 欄
   萃取，逐列 assert 命中（未命中即停手）；萃取結果已逐列列印複核。
   其餘列為逐列手寫。
3. **未跑既有 lint。** `vs_cf01_lint.py` 綁 Revise1 之 `vs_cf01_apply.TOUCHED`，
   對 Revise2 之列號已不成立；`vs_sl03_lint.py` 綁 VS-SL-03 之三簿。
   故另立 `vs_cf03_lint.py`，其三項硬規之判準逐字沿用 `vs_cf01_lint.py`
   （`PAREN_TAIL`／尾句號／全形分號），並加 §六 之其餘各項。
4. **本包未觸及交付流程。** 未 commit、未打 tag、未寫 `output/`。
   Revise2 仍在 sandbox，待 Pei 覆 §9 之二項後再議出貨。
