# 上繳包 GC-13（2026-09-05）

**session 標識**：執行層 **A**（GC-12 審閱 三 指派）。
**觀察依據（R-G62(b) 附記）**：接手時工作區乾淨（`git status --short` 無 M 行），HEAD＝`50472ce`；
起點前最後一次他方寫檔為 `22:32:50`（`down/20260905_GC-12_review.md`，分析層），距起點 ~1 分。
**施作期間未觀察到他方寫檔。**

**工具旗標逐次聲明（R-G50(d)）**

| # | 工具 | 旗標 | 寫檔 |
|---|---|---|---|
| 1 | `rulings_hash.py` | `--out <scratchpad>/dry3.tsv` | **工作區，未寫 repo** |
| 2 | `canon_refs.py` | `--waiver` | 唯讀 |
| 3 | `lint_docs036.py` | — | 唯讀 |
| 4 | `lint036.py` | `--profile <f> --report-dir features/<f>/reports --json` | 寫報告（六份：三本母本＋三本 Revise1）|
| 5 | `scripts/rg72_revise_m.py` | `--src/--dryrun/--out/--idmap` | **只寫新檔與 idmap；母本唯讀** |
| 6 | `pytest tests/ -q` | — | 唯讀 |

**未跑**：`gates_tsv.py`／`selfcheck_map.py`／`rulings_hash --out <repo>`（屬 Pei）。
**未用 `git stash`**（PROTOCOL 新句）。

---

## 1　lint P 加 v3 記法式（R-G70(h)）

### 1-1　條文落檔

- canon `docs/runtime/ASPICE_SWE6_AI_Instruction.md` §8.7.5 加 **(h)**。
- 台帳 `### R-G70` fenced block 加 **(h)**（逐字取自 GC-12 審閱 一）。

**範圍措辭之訂正**：審閱原文只寫「作者側四欄」，而 R-6 所定之 P 範圍為
**四欄 ＋ `test_item` 之括號下半**（括號下半亦為作者生成內容）。
實測即由此驗出（見 1-4 之 206 vs 209）。條文改為「範圍同 R-6 之 P 範圍」，
`test_item` **上半** verbatim 仍依 (b) 保留。

### 1-2　實作

`scripts/lint036.py` 加 `RE_P_V3_DOLLAR`，並置於 `check_signal_line` 之
**提早返回之前**：

```python
RE_P_V3_DOLLAR = re.compile(r"\$[A-Z][A-Z0-9_]{2,}\.[A-Za-z][A-Za-z0-9_]*\$")
...
for m in RE_P_V3_DOLLAR.finditer(line):
    add(f"v3 記法殘留（R-G70(h)：`$` 包覆式已撤銷）{m.group(0)!r}")
assignments = list(RE_P_ASSIGNMENT.finditer(line))
if not assignments:
    return out          # ← 原本在此之前就返回，故 `$` 包覆式永不被計
```

### 1-3　正控五樣本 —— **全過**，並已釘入 `tests/test_lint036.py`

| 樣本 | 期望 | 實測 |
|---|---|---|
| `Send the signal $STATUS_BH_BCM1.OperationalModeSts$ = 2 (Ignition_Off)` | P≥1 | **1** |
| `Read the signal $STATUS_TELEMATIC.PowerSts_Telematic$ and check that it is 4 (…)` | P≥1 | **1** |
| `The signal value $STATUS_TELEMATIC.PowerSts_Telematic$ = 4 (…) is received` | P≥1 | **1** |
| `Send CAN: STATUS_BH_BCM1.OperationalModeSts = 2 (Ignition_Off)` | P=0 | **0** |
| `Read STATUS_TELEMATIC.PowerSts_Telematic and check that it is 4 (…)` | P=0 | **0** |

五個以 `parametrize` 釘為五個測試（缺一即紅），另加二：
非 profile 向亦報（(h) 為全域判準）、`test_item` **上半** verbatim 不報（(b)）。
`tests/test_lint036.py` 由 127 增為 134 項，全綠。

### 1-4　八本基線 P 重跑 —— **P 差 0，`P_bare` 全 0**

落 `docs/reports/p8_baseline_rg70h_20260905.tsv`。

| book | 來源存在 | rows | P_before | P_after | **P_bare** | 差 | profile | P_prof_before | P_prof_after | P_prof_bare |
|---|---|---:|---:|---:|---:|---:|---|---:|---:|---:|
| AMFM_20260821 | Y | 298 | 0 | 0 | **0** | 0 | amfm | 16 | 16 | 0 |
| BT_20260821 | **N** | — | — | — | — | — | — | — | — | — |
| CFTS012_DealerMode_20260821 | Y | 125 | 0 | 0 | **0** | 0 | — | — | — | — |
| CFTS026_HandsFreePhone_20260821 | Y | 159 | 0 | 0 | **0** | 0 | — | — | — | — |
| Home_20260821 | Y | 216 | 0 | 0 | **0** | 0 | home | 0 | 0 | 0 |
| MediaHMI_20260821 | **N** | — | — | — | — | — | — | — | — | — |
| PowerManagement_20260821 | Y | 283 | 35 | 35 | **0** | 0 | power | 45 | 45 | 0 |
| Projection_20260821 | **N** | — | — | — | — | — | — | — | — | — |

**審閱預期「P 升屬預期」—— 實測未升，差為 0。** 成因：
八本為 **0821 基線本**，成於 R-1 v3 採用之前，本就無 v3 記法；
v3 殘留集中於**分析層產出本**（pm_29／pm_73／ics／vsm）。
`P_before` 之量法：`git show HEAD:scripts/lint036.py` 落工作區後載為獨立模組，
與現行版同 process 各跑一次，同一母體。
三本來源簿不存在（B 於 GC-12 已記，本包複查一致），其路徑在 repo 外之
`/Users/peihe/Work/02_Project_R1LR/…`。

---

## 2　Revise-M 三本

### 2-1　一-3　`mechanical=Y` 現查 —— 三表皆與下放包預期相符

| 本 | dry-run 表總列 | `mechanical=Y` 列 | 涉及資料列 | 下放包預期 |
|---|---:|---:|---:|---:|
| pm_73 | 342 | 63 | **33** | 33 ✓ |
| ics_management | 113 | 18 | **14** | 14 ✓ |
| vsm_v42 | 60 | 13 | **10** | 10 ✓ |

rule 分布（Y）：pm_73 `recv` 44／`send` 19；ics `recv` 18；vsm `recv` 9／`send` 4。

### 2-2　產出

工具 `scripts/rg72_revise_m.py`（新）。**母本一位元不動** ——
以 `openpyxl` 載為記憶體物件改格，再由 `backend.xlsx_surgical.surgical_save`
以母本 zip 為底逐格 patch 寫**新檔**，`verify_structure` 通過。

| 本 | 母本 sha12 | 新檔 | 新檔 sha12 | 替換處 | 格 | 列 | Y–AH 斷言 |
|---|---|---|---|---:|---:|---:|---|
| pm_73 | `cedb6b84d422` | `pm_73_Revise1.xlsx` | `e9ccddbbd673` | **63** | 41 | **33** | PASS |
| ics_management 0830 | `cb5697db79a6` | `…_ICSManagement_20260830_Revise1.xlsx` | `3554a2b28642` | **18** | 14 | **14** | PASS |
| vsm_v42 0902 | `abc7f8aecd23` | `…R1Low_20260902_Revise1.xlsx` | `70657bdf3f1f` | **13** | 12 | **10** | PASS |

替換處數與各表 `mechanical=Y` 列數**逐本相等**（63／18／13）。

**三道保護（缺一即中止，未寫任何檔）**：
(1) 目標格落在結果欄 Y–AH（第 25–34 欄）即拒絕；
(2) `old_text` 於該格不出現即中止並列出；
(3) 結果欄逐格斷言等於母本；(4) `surgical_save(verify=True)`。

⚠ **保護 (2) 於首跑實際攔下 4 處**，成因為本層工具之 bug（同格內重複之
`(old,new)` 被逐列套用，第一次已替換全部出現處，第二次遂判 miss）。
已改為**每格去重後以「出現次數」為單位替換**。**該次未寫任何檔** ——
保護有效，非事後補救。

### 2-3　閘重過（R-G72(e)）

| 本 | P 母本 | P Revise1 | 降 | v3殘留 母本 | v3殘留 Revise1 | Y | X |
|---|---:|---:|---:|---:|---:|---:|---:|
| pm_73 | 317 | **254** | −63 | 272 | **209** | 0 | 103 |
| ics_management | 119 | **101** | −18 | 119 | **101** | 0 | 29 |
| vsm_v42 | 60 | **47** | −13 | 60 | **47** | 0 | 6 |

**P 之降幅逐本等於替換處數**（63／18／13），無多改無少改。

### 2-4　驗收條件之核對（審閱 一 末句：`P_after == bare 數 且 P_after 全為 bare 類`）

先解單位歧義 —— dry-run 表之 `bare(N)` 為**表列數**，P 為**逐次命中**。
以 like-for-like（Revise1 本之作者側 `$MSG.Sig$` 實際出現次數）核對：

| 本 | 作者側四欄 `$MSG.Sig$` 出現 | ＋`test_item` 括號下半 | P 之 v3殘留 | 相等？ | P 總 | 非 v3殘留者 |
|---|---:|---:|---:|---|---:|---:|
| pm_73 | 206 | +3 = **209** | **209** | ✓ | 254 | **45** |
| ics_management | 101 | +0 = **101** | **101** | ✓ | 101 | **0** |
| vsm_v42 | 47 | +0 = **47** | **47** | ✓ | 47 | **0** |

- **`P_after == bare 數`：三本皆成立**（單位對齊後逐本相等）。
- **`P_after 全為 bare 類`：ics_management ✓、vsm_v42 ✓、pm_73 ✗**（45 例外）。

**pm_73 之 45 例外，兩類，皆非 Revise-M 所能解**：

| 類 | 數 | 樣本 | 判 |
|---|---:|---|---|
| PROXI 行形態不合（B 之 PROXI fallback）| **19** | `3. Hold for the PROXI Switch_Off_Time value`；`2. The PROXI parameter "Switch_Off_Time" is at 20 minutes`；`2. The Rear_View_Camera PROXI parameter reads "Present"` | ⚠ **全部為散文提及 PROXI，非 PROXI 賦值 —— FAIL 類之誤報**，見 3 節 |
| 真實 v4 違規 | **26** | `1. Send the signal STATUS_BH_BCM2.RemStActvSts = "Remote Start Active"`（缺 `Send CAN:`、值為引號字串非 `raw (label)`）| 屬 **Revise-C** —— 其值須回查 CFTS 原句 |

**結論**：ics_management 與 vsm_v42 之 Revise1 **合格**；
pm_73 之 Revise1 **機械轉換本身正確且完整**（63/63、Y–AH 不動），
但**不滿足「全為 bare 類」**，其 45 例外 19 為誤報、26 屬 Revise-C。
本層**不宣告 pm_73 出貨** —— 待 3 節之裁。

### 2-5　idmap（R-G72(b)）

`features/<f>/reports/rg72_idmap_*_20260905_filled.tsv`，欄
`old_tc_id / new_tc_id / row / changed_cols / reason`。
**Revise-M 不改號**，故 `new_tc_id` 填同號；`changed_cols` 為實際替換欄；
`reason` 為該列之 rule 集合。列數＝各本涉及列數（33／14／10）。

### 2-6　MANIFEST 與 RULINGS

三線 `delivered/MANIFEST.tsv` 各加一列（`supersedes` 記於 note 與 `source_path`），
三線 `RULINGS.md` 各加「交付本之取代紀錄 —— R-G72(g) Revise-M」節，
載母本原封、差異格數、Y–AH 斷言、TC ID 不變、X-nav 與 bare-$ 屬 Revise-C。

---

## 3　⚠ 待裁：PROXI fallback 於 FAIL 類之誤報（19 處／pm_73）

GC-10 所加之 PROXI 檢查，其 fallback 為「兩式皆不中而含 `PROXI` 者 → 記 **P**」。
實測該 fallback 命中之 19 處**全數為散文提及**，無一為 PROXI 賦值：

```text
  3x  3. STATUS_TELEMATIC.PowerSts_Telematic = 1 (Standby) is sent after the PROXI Switch_Off_Time…
  2x  3. Hold for the PROXI Switch_Off_Time value
  2x  5. The PROXI Switch_Off_Time value elapses
  2x  2. The Rear_View_Camera PROXI parameter reads "Present"
  1x  2. The PROXI parameter "Switch_Off_Time" is at 20 minutes
  1x  1. Apply the configuration: PROXI Car_Shape_Configuration and PROXI Number_of_Doors…
  （餘 8 處同型）
```

**P 為 FAIL 類**（R-G70(h) 所確立），誤報直接擋出貨閘，
且使「P_after 全為 bare 類」對任何含 PROXI 散文之本恆不成立。

**建議（未施作，動之會再移 P 基線）**：
fallback 只在該行**看起來是賦值**時才報 —— 即含 `PROXI` 且該行有
`=` 或 `is set to` 而二式皆不中者；純散文提及不報。
`Y`（舊式 WARN）不受影響。

**未裁前 pm_73 之 Revise1 不宣告出貨**；ics_management 與 vsm_v42 不受影響。

---

## 4　ANOMALIES

現查 `A-GC` 最高為 **A-GC22** → 本包用 **A-GC23**。

**A-GC23**　R-VS86 之理由書以來源側記法計入作者側計數（「CFTS044 交付本 78 處」）。
自量（未照抄 GC-12）：036 交付本（`ebe5a65f30a0`）全分頁全欄 **15**，
作者側四欄僅 **2 行**且皆為散文、**賦值 0**；原始 docx（`87fe31774eea`）**63**，
形態 `PROXI parameter Rear_Sun_Shade = [1h:Present]`（**來源側**）。
15＋63＝78 對得上，但構成不是「交付本 78 處」。
形態＝**來源側記法計入作者側計數**（A-GC20 同族，即 R-G70(b) 所防者）。
**R-VS86 結論不動，理由更正**（GC-12 已改，本包登記）。

---

## 5　PROTOCOL 二句（審閱 二 之 6／10-2）

`docs/fw036/handoff/PROTOCOL.md` 之「目錄唯讀」節末加：

1. **引用程式碼一律指函式名，不指行號** —— 並行改檔期間行號會位移，
   且其失效**不報錯**（實例：`lint036.py:932` 於 GC-12 現查時已為空行，
   判準實體在 `lint_sheet()` 內；登 A-GC22）。
2. **共用工作區內禁止 `git stash`** —— `push -u` 會連他方未追蹤檔一併移走，
   `pop` 之還原非原子；歸屬分析改用 `git worktree` 或複製到 scratchpad 後 diff
   （實例：`up/20260905_GC-12.md` 8 節，該包 10-2 已具名）。

---

## 6　`DATA_REQUESTS.md` 表格式（審閱 二 之 4）

- 範本 `docs/fw036/templates/DATA_REQUESTS.md` 加「表格式 DR 以 `status` 欄承接」段：
  判別式（`## DR-` 節 0 而 DR 號 > 0 即表格式）、欄位形態、
  並明載「機讀時二形皆須支援 —— 只掃節首行會把表格式全線讀成 0」。
- `features/power/DATA_REQUESTS.md` 標頭同步改為表格式說明
  （`## DR-` 節 **0**、DR 號 **30**）。**欄與回填皆未做。**

---

## 7　對照向

| 量 | 本包前 | 本包後 | 說明 |
|---|---|---|---|
| `canon_refs` unresolved | 18 | 18 | 不變 |
| `canon_refs` ambiguous | 141 | 141 | 不變 |
| `pytest tests/ -q` | 8 failed／1319 passed | **8 failed／1326 passed** | 紅同一批（`test_intake_scaffold` 6／`test_single_write_path` 2，HEAD 既紅）；綠 **+7**（本包新增測試）|
| `lint_docs036` | PASS | PASS | |
| `rulings_hash` 錨數 | 768 | 768（乾跑）| |

**指紋乾跑**（`--out <工作區>`，未寫 repo）：

```text
R-G70   現行 4bfccb46  →  乾跑 76014d85   （加 (h)）
R-G72   現行 f0b96263  →  乾跑 a6c5b0e4   （GC-12 加 (g)，尚未重生）
```

**Pei 重生 `RULINGS.sha.tsv` 後二值應為 `76014d85`／`a6c5b0e4`**；
現行表在重生前為過期（`--check` 會紅）。

---

## 8　自報

1. **`scripts/rg72_revise_m.py` 首跑有 bug**（同格重複 edit 逐列套用致誤判 miss）。
   保護 (2) 攔下，**未寫任何檔**，修正後重跑。具名於此。
2. **pm_73 之 Revise1 未宣告出貨**（2-4／3 節）。其機械轉換正確且完整，
   但驗收條件之「全為 bare 類」因 19 處誤報 ＋ 26 處 Revise-C 項而不成立。
   本層未自行改 PROXI fallback —— 動之會再移 P 基線，且非本包所令。
3. **八本基線 P 差為 0**，與審閱「P 升屬預期」之預期不同。
   非實作失效 —— 正控五樣本全過，且三本 Revise-M 之 P 各降 63／18／13。
   成因為母體選擇：八本無 v3 記法。
4. 六份 lint 報告寫入 `features/<f>/reports/`（三本母本＋三本 Revise1），依 R-G47。

---

## 9　未結 DR

同 `up/20260905_GC-12.md` 9 節：`DR-PW25`、`DR-VS{n}`（Power_Tailgate_Enable）、
HMI entry path 草稿 ×3；bare-`$` 值查詢待 Revise-C 逐處登。
**本包新增之待登**：pm_73 之 26 處真實 v4 違規（`Send the signal X = "字串"` 型），
其 `raw (label)` 須回查 CFTS 原句，查無者登 DR —— 屬 Revise-C 包。
