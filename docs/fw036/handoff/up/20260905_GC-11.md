# 上繳包 GC-11 —— 五本候選之轉換 dry-run、GC-10 收尾

**執行層 session：B**（GC-11 首行指派）。
**R-G62(b) 附記之聲明**：施作區間 **21:57 – 22:0x**。
起點前之最後一次他方寫入為 `RULINGS.sha.tsv` **21:31:46**（執行層 A），距起點 25 分餘；
A 已於 `1e539cb` 提交後停止。施作期間所見之新檔皆為本 session 或分析層之下放檔。
**依附記，本聲明只附觀察依據，不主張證明否定。**

日期：2026-09-05　層級：Tier 1 → Tier 2
對應下放包：`down/20260905_GC-11.md`（21:54）＋ `down/20260905_GC-11_addendum.md`（21:47）

> **指派衝突，具名**：補遺（21:47）§1 令「領先線收尾、停手線不再寫檔」，
> 而 GC-11 本體（21:54，**較後**）首行令「指派：執行層 session B……Pei 於下放前確認 A 已停」。
> 二者相反。本 session 依**較後且載明 Pei 已確認**之 GC-11 本體施作，
> 並先驗 A 確已停（見首行）。補遺 §2／§3 之二項更正**照納**（見 3 節、6-2 節）。

---

## §1　產物與 SHA256（前 16 碼）

| 節 | 檔 | bytes | SHA256 |
|---|---|---:|---|
| 2 | `docs/fw036/ANOMALIES.md` | 9,967 | `2514912cec54566b` |
| 2 | `docs/fw036/handoff/PROTOCOL.md` | 3,552 | `10b7a43e40f80d3c` |
| 2 | `docs/fw036/RULINGS_LEDGER.md` | 105,144 | `a6451ef658d6e452` |
| 4 | `features/power/reports/rg72_dryrun_pm_29_20260905.tsv` | 131,731 | `37b0461e27311d2c` |
| 4 | `features/power/reports/rg72_dryrun_pm_73_20260905.tsv` | 53,808 | `c33a9f40d929ab1d` |
| 4 | `features/sw_update/reports/rg72_dryrun_swupdate_20260830_20260905.tsv` | 31,514 | `51d49682aa535880` |
| 4 | `features/ics_management/reports/rg72_dryrun_ics_20260830_20260905.tsv` | 16,613 | `dca4cc6b8a4ba922` |
| 4 | `features/vsm_v42/reports/rg72_dryrun_vsm_v42_20260902_20260905.tsv` | 9,262 | `69639300283b5223` |
| 5 | `features/*/reports/rg72_idmap_*_20260905.tsv` ×5 | — | 列數見 5 節 |
| 3 | `features/power/reports/rg70_pm29_dryrun_20260905_v41.tsv` | 73,908 | `c601e1093d70e022` |

**工具旗標逐次聲明（R-G50(d)）**：`lint036.py` 唯讀（`--report-dir` 指工作區、`--json`）**6 次**；
`lint_paths.py`／`gate_all.py` 唯讀。**本包未跑任何會寫 repo 之產生器** ——
`rulings_hash.py`／`gates_tsv.py`／`selfcheck_map.py` 一次都沒跑。
**所有 `.xlsx` 只以 `read_only=True` 開啟，一位元未寫。**

---

## §2　任務 1-1：GC-10 收尾

- **`A-GC19`**（代執行層 A 登記，GC-10 審閱 §二 指定）：A 逾 GC-09「不寫表」明文寫
  `RULINGS.sha.tsv`（mtime 21:31:46，R-G50(c) 違規）＋ 以無效之 B 版審閱方向落 R-G70(e)。
  條內明記**非 B 之責**。
- **`A-GC20`**（補遺 §2 之更正）：`PROXI <Param> = <值>（41 列）` 之 **41 為命中次數非列數**；
  實測 **26 行／3 相異行／1 個參數名**（`RRM_VPx_Steering_Wheel_Command_Type`）。
  形態＝命中次數代母體計數（R-G69）。**不推翻 SWC 式裁定**。
  共同形態句由「十八筆」改「二十筆」。
- **PROTOCOL**「目錄唯讀」節增 **R-G62(b) 附記**：下放包首行載明指派之 session；
  派工端於指派前確認前一 session 已停；執行層之聲明**只附觀察依據，不要求證明否定**；
  已知併行之輪次改為如實列併行改動之檔清單。
- **台帳 R-G70 v4.1** 增註：**WARN 之落點為 lint `Y`**（profile 專屬，比照 `X`），
  理由為 `P` 屬 FAIL 類、WARN 混入即無法只報不改。

---

## §3　補遺 §3 之回答：候選表所用之資料列判準

**答：已是 `lint036` 判準，非 `No.#`；不需重出。**

`rg72_candidates_20260905.tsv` 之 `rows` 欄取自 lint036 報告之「資料列數」一行，
而 lint036 之判準在 `scripts/lint036.py:932`：

```python
if not any(fields[k].strip() for k in ("test_item", "proc", "er")):
    continue
```

即**「`test_item`／`proc`／`er` 任一非空」**，與補遺 §3 所令者逐字相同。
故補遺所憂之「`No.#` 整欄為空即讀成 0 列」在本表**不會發生**；
五本之 `rows`（31／389／287／319／17）皆為 lint036 口徑。**改前／改後無差異可列。**

---

## §4　任務 2：五本轉換 dry-run（只做機械轉換，xlsx 不動）

欄：`sheet / row / col / tc_id / rule / old_text / new_text / mechanical(Y/N)`。
`test_item`（I 欄）**全程略過** —— 來源側記法，R-G70(b) 明文不動。

| 本 | sha8 | 掃格數 | 命中格數 | 涉及列數 | mechanical **Y** 列 | **N** 列 |
|---|---|---:|---:|---:|---:|---:|
| `pm_29` | `35305835` | 6,238 | 498 | 307 | 201 | 292 |
| `pm_73` | `cedb6b84` | 4,881 | 239 | 192 | 33 | 191 |
| sw_update 0830 | `4f6caa35` | 4,786 | 3 | 122 | **0** | 122 |
| ics_management 0830 | `cb5697db` | 403 | 84 | 28 | 14 | 28 |
| vsm_v42 0902 | `abc7f8ae` | 270 | 54 | 17 | 10 | 16 |

逐 rule（命中數）：

| 本 | `send` | `recv` | `bare-$MSG.Sig$` | `PROXI-old→SWC` | `X-nav` |
|---|---:|---:|---:|---:|---:|
| `pm_29` | 109 | 182 | **207** | **0** | 237 |
| `pm_73` | 19 | 44 | 176 | **0** | 103 |
| sw_update | 0 | 0 | 3 | **0** | 159 |
| ics_management | 0 | 18 | 66 | **0** | 29 |
| vsm_v42 | 4 | 9 | 41 | **0** | 6 |

**三件要看的**：

1. **`PROXI-old→SWC` 五本全為 0** —— 與候選表之 `Y=0` 一致。VF230 之 152 列舊式在
   `features/vehicle_setting/output/`，不在任何 `delivered/`。**故 PROXI 轉換在本輪五本上無事可做。**
2. **`bare-$MSG.Sig$` 為 `mechanical=N`** —— 該形態只有 `$MSG.Sig$` 而其後無 `= raw (label)`，
   **缺 raw／label 即不可機械轉**，須查 DBC（R-7／R-17）。pm_29 之 207 恰為 GC-09 §一-3 所稱之「207 列」。
3. **`X-nav` 全為 N** —— 依 R-G71(d)「不造 hop」，只列不轉；其解須逐 TC 查 HMI 來源，屬另包（含 DR）。
   sw_update 之 `mechanical=Y` 列為 **0**：其 122 涉列**全部**是 X-nav 與 3 個 bare，
   即該本之回修**沒有任何可機械完成之部分**。

---

## §5　任務 3：TestRail ID 對照表雛形

`features/<f>/reports/rg72_idmap_<slug>_20260905.tsv`，欄
`old_tc_id / new_tc_id(空) / changed_cols / reason`。`new_tc_id` 依 R-G72(b) 留空（Revise 本產出時填）。

| 本 | 列數（相異 `old_tc_id`）|
|---|---:|
| `pm_29` | 307 |
| `pm_73` | 192 |
| sw_update | 122 |
| ics_management | 28 |
| vsm_v42 | 17 |

`changed_cols` 為該 TC 之命中欄號集合（`nav` 代表 X-nav 之列級命中）；`reason` 為其 rule 集合。

---

## §6　任務 1-3、任務 4

### 6-1　pm_29 v4.1 dry-run 重跑

來源 `features/power/sandbox/b29/pm_29.xlsx`（sha8 `35305835`，與 delivered 本同 sha）。
輸出 `features/power/reports/rg70_pm29_dryrun_20260905_v41.tsv`。

**掃格數 6,238／命中格數 584／涉及列數 207。**
逐 rule：`bare-$MSG.Sig$` 207、`recv` 182、`send` 109、`buserror` 86、`PROXI-old→SWC` **0**。

**與 A 之舊表差異**：舊 `rg70_pm29_dryrun_20260905.tsv` **685 列**，本表 **584 列，差 −101**。
差異之成因**本包未逐列比對**（舊表產於 PROXI 方向矯正前，其規則集不同），
只回報二表列數與差值。舊表依補遺 §1 **不刪**，留工作區。

### 6-2　pm_73 之來歷（任務 4，只量）

| 項 | 值 |
|---|---|
| MANIFEST 之 `delivered_round` | **74 包 R-P399（列序訂正後重出）** |
| `source_path` | `features/power/sandbox/b73/pm_73.xlsx` |
| note | S6 例外（Pei 2026-08-30）；**288 列**；列序依 Requirement or Design ID 遞增、同 leaf 內依 tc_id 遞增（四道皆過）|
| 入版控 | `2bbd385`／`51e7526`／`1deb45e`（皆 2026-08-30）|
| 與 `pm_29` 之關係 | **非其累積本** —— `pm_29` 之 `delivered_round` 為「27 包 §D-5」，二者為不同批次、不同 sandbox 來源（`b29` vs `b73`）|

---

## §7　自檢與自報

| # | 項 | 結果 |
|---|---|---|
| 1 | 未產 Revise 本、未寫回任何 xlsx | **PASS**（全部 `read_only=True`）|
| 2 | 未跑會寫 repo 之產生器 | **PASS**（1 節末）|
| 3 | `test_item` 上半未列入轉換 | **PASS**（I 欄全程略過）|
| 4 | `X-nav` 全為 `mechanical=N` | **PASS**（不造 hop，R-G71(d)）|
| 5 | 每本附分子分母 | **PASS**（4 節、6-1 節）|
| 6 | `lint_paths` | **2**（本包新增之 `.tsv` 報告落 `reports/`，GC-10 7-1 已放行）|
| 7 | R-G62(b) 附記之聲明附觀察依據 | **PASS**（首行）|

### 7-1　閘況

`lint_paths` **2**（餘 driver_distraction 二檔，待 Pei）。
本包未動條文本體以外之產生器輸出，`rulings_hash --check` 之狀態**未變**——
GC-10 所改之 R-G70(e)／R-G72(f) 仍待 Pei 重生一次（GC-11 §二-2 之預期成立與否，於重生時即知；
本包新增之 `A-GC19`／`A-GC20` 為 ANOMALIES 表列，非台帳條文，**不影響指紋**）。

### 7-2　未做且具名

| 項 | 為何未做 |
|---|---|
| **GC-11 §一-1-2 八本基線 P 對照向** | 需取 `HEAD~` 之 `lint036` 版本逐本重跑並與現行比對；本輪時間用於五本 dry-run 與 idmap，**未做**。此為第二次延後（GC-10 6-3 亦列），建議獨立成包 |
| 補遺 §2 末「VF230 152／SWC 215 是否亦為次數」之複核 | **未做**；已於 `A-GC20` 內具名 |
| 舊 `rg70_pm29_dryrun_20260905.tsv` 之 README 註 | 補遺 §1 令領先線加；本 session 非領先線，**未加**，該表原封留工作區 |
| `RULINGS.sha.tsv` 重生 | 屬 Pei |
| `driver_distraction` 搬移 | 屬 Pei（`git mv`）|

### 7-3　請裁三項

1. **指派衝突**（前言）：補遺 §1 與 GC-11 本體相反，本包依後者。若分析層認為應由領先線收尾，
   本包之產物可作廢或併入，請明示。
2. **Revise 本之切分**（GC-11 §二-1）：五本之 `mechanical=Y` 列數為 201／33／**0**／14／10 ——
   **sw_update 之 Y 為 0**，其回修全數卡在 X-nav（須 HMI 來源）。
   若裁「只做 mechanical Y 先出」，sw_update 這一本**無事可做**，等同不回修。
3. **`bare-$MSG.Sig$` 之處置**：五本合計 493 處缺 raw／label，機械不可轉。
   是否併入 HMI／DBC 查詢之同一包（與 X-nav 一起），或另立 DR。
