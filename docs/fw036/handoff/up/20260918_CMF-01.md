# 上繳包 CMF-01 — Comfort HMI 0907 本之承接、對帳與 Revise 前置

- 日期：2026-09-18
- 對應下放包：`down/20260918_CMF-01.md`
- 性質：唯讀對帳 ＋ 登記 ＋ plan。**未寫回任何工作簿、未動 `RULINGS.sha.tsv`、未跑 `--gate`／`--regen`、未改 `generated/*.json`、未 commit。**
- 觀察依據（R-G62(b)）：起點前之最後他方寫入為 `down/20260918_CMF-01.md`（mtime 15:24:16）；本 session 期間未觀察到他方寫入 `features/comfort/`。

## 1. 第一段：對帳結論與停點

**0907 本 = ENTRY 035 之 pipeline 產出（`…_Comfort_20260817_rowsort.xlsx`，`235f2ca7…`）＋ 離線填欄。語料側內容未被離線編修。**

`scripts/diff_0907.py` 三方比對（A＝0907 本、G＝`generated/*.json` 經 `write_back.row_plan()／cell_of()`、R＝rowsort 檔），row 10–475 共 466 列：

| 欄 | A≠G | A≠R | R≠G | 內容 |
|---|---|---|---|---|
| D F H I J K L M N R S AH（12 欄） | 0 | 0 | 0 | 逐格相同 |
| G | 466 | 466 | 0 | `Comfort` → `Climate Control Interface`（含留空列 row 116） |
| P | 465 | 465 | 0 | P1→P0（325）、P2→P1（132）、P3→P2（8）：整體降一級，相對次序不變 |
| C／E／O／T–Z／AA（NEVER_WRITE 欄） | — | 各 465–466 | — | C＝Polarion `NRL-4342xx`；E＝TestRail `C74501…`；O＝`NEW`；T–Z＝全 `1`；AA＝作者 |

- 列數：兩側皆 466（465 TC ＋ row 116 `SWE1-HVAC-019-03` 留空列）。TC ID 對映：F 欄依列位編號，與 JSON 之 tc_id 不同（ENTRY 035 已講明；本包一律寫 `wb-NNN`／`json-NNN`）。
- R≠G 之 14 欄皆 0 → 語料於 0817 之後未前進。
- **差異全在 2 個語料欄（G、P）＋ 非語料欄**，且皆為整欄機械式改寫。
- **分析層於 1 節列為「與 profile 不符」之 J 欄（無節次括號、帶 `for which …`）與 N 欄（無 `SYS1_`、以 `、` 串接），都不是離線編修**：J 之括號移除與 N 之 stem 去前綴是 Pei 於 2026-08-17 自行改的（DELIVERY ENTRY 032），之後由 ENTRY 034 併入 pipeline 匯出規則（`write_back.render()`／`_compact_refs()`）。也就是 profile 落後於 pipeline，0907 本並沒有偏離 pipeline。

**停點 1 節-4：字面上命中。** 不符格數 931（G 466 ＋ P 465）> 14 × 50 = 700。依包內規定**未自行決定基線**，列入 5 節-1。不過實際差異只有 2 個欄位，而且都是單一映射，裁定所需的資料已經齊備，見 5 節-1。

## 2. 登記（1 節-1）

| 項 | 結果 |
|---|---|
| 來源 sha256（執行層實測） | `3550d16d8bd319633d872856ae51ba63a7d2a928d22fab69bff6dd1963736aff`（與分析層之 16 碼一致）｜253,539 bytes｜mtime 2026-09-08 09:56 |
| 複製 | `features/comfort/delivered/…_SWQT_ComfortHMI_20260907.xlsx`（檔名與內容不改；`cmp` 逐位元組一致） |
| MANIFEST | `delivered/MANIFEST.tsv` 追加一列（delivered_round＝`0907 (external edit, observed)`） |
| 台帳 | `DELIVERY.sha256` 追加 **ENTRY 036**（type `observed`，比照 ENTRY 032 之型）＋ checksum 行；`shasum -c --ignore-missing` 57 OK、0 FAIL |

## 3. M1–M10 逐項複驗（分析層數字 對／錯／差幾）

| # | 分析層所量 | 執行層實測 | 判 | 備 patch |
|---|---|---|---|---|
| M1 | row 116 空殼 1 列 | 1 列（`019-03`，D 以外 13 欄全空） | **數對；成因補正**：`gen_batch9.py` 之 2.13 reasoning 明載 019-02／-03 屬 `[BLOCKED-SPEC]`，因白名單增列須裁定（R-C26）而**停下未產**；後來 019-02 另外補產了（json-458），019-03 則留空至今。ENTRY 035 說「比照 080-02」，但 080-02 實際上已產出 BLOCKED row，兩者並不一致 | 已備 BLOCKED row 之內容；**待裁（5 節-9）** |
| M2 | -312／-313 PC＋proc＋ER 逐字同，皆未測再按 | J／K／L／M 四欄逐字同；I 下半不同。037 VC 兩 leaf 皆為「timeout 前再按一次」 | **對**。另查得 json-191 之 `test_item_authored` 寫的是「5 sec timeout」，也就是把 103-01 當成單純逾時；但 037 的 103-01 是「timeout restarts」 | 已備改寫（兩條皆改為 timeout 前再按）。`3 seconds` 為作者自取之 <5 s 測點，**請分析層確認是否屬 IN §8.4.1 造值** |
| M3 | T–Z 466 列全 1 | 7 欄 × 466 列全 `1`（含留空列 row 116） | **對** | 不在 JSON 層（NEVER_WRITE 欄），無 patch；待 5 節-3 |
| M4 | 缺 `SYS1_` 且以 `、`／`;` 串接 452 | 無 `SYS1_`：465（全部 TC）；含 `、`：452；含 `;`：6 | **數對（`、` 452）；缺前綴應為 465**。成因同 1 節（Pei 之 08-17 編修併入匯出規則）。**但 IN §10.7 之「一節一行、禁用 `、`／`;`」為 Pei 2026-08-21 所裁，晚於 08-17，故串接確實違反現行 canon**；`SYS1_` 前綴屬 R-C1 基線檔名之問題，另立 | 待 5 節-5（決定 N 欄的承載範圍後，一次改 `_compact_refs()`） |
| M5 | 4 格外露 `The below requirements shall be implemented when` | 4 格（wb-083／084／403／404 = json-430～433） | **數對，判斷錯**：該句**整句即 CFTS043 `NEWR1L-53677` 之逐字原文**（`…$Rear_HVAC_cfg$ = [Present].`，tree view 實查）。匯出時只剝除 `[ext-verbatim]` 標籤，引文之外並未外露任何文字，符合 profile 3.2.4 節 | 不需修改 |
| M6 | 4 條無執行通道 | 4 條（wb-033／069／071／390） | **對**。CFTS043 有訊號名：`$RECIRC_STAT$`（NEWR1L-50234，R1L 適用）、`$EBL_Stat$`（NEWR1L-50051，R1L 適用），值 `[not available/Blink]`；**`forms/` 內 4 份 DBC 皆查無**（含寬鬆 pattern）→ IN §8.7.5(c) 無法寫 `Send CAN:` | 已備 `PENDING: DR-46 …`。**DR-46 尚未登入 `DATA_REQUESTS.md`（本包為唯讀，僅提案）** |
| M7 | AUTO 未引 161／已引 43；Face 54、Feet 55；RECIRC 28；SYNC 24＋Sync 4 | L＋M 兩欄：AUTO 161／43、Face 54、Feet 55、**RECIRC 29**、SYNC 24、Sync 4 | **對（RECIRC 差 1）**。計數範圍為 L＋M；若含 I／J 則數字全數加倍以上（見 patch plan 之附記） | 不逐條列出；須先分辨「作為標籤」與「作為狀態名」（如 `AUTO ECO`、`the AUTO state`），機械式加引號會誤傷，Revise 時逐條處理 |
| M8 | 17 值；`HVAC Pop-ups` 1、`EMEA ICS Interface` 2 | 17 值，同 | **對**。framework Part N 2 節：14.1 → `Climate Popups`（#10）、16.11 → `ICS Climate Modes`（#12） | 已備（json-434／432／433 之 `test_set`） |
| M9 | `Note` 25；複合 102（啟發式） | `Note` 25 步／25 TC。**真正的雙動作步驟 35 行／32 TC**（排除 `Press and hold`、`press the handle and drag` 等單一手勢 11 行） | **Note 對；102 高估**：「`<動作> and read <對象>`」共 94 行，是 IN §5.5 容許的「ACTION + check target」形，不算複合 | 已備（`Note`→`Record`；複合 35 行逐條列出） |
| M10 | 26 條；-010～-012 為 94 | 26 條，同。**最大值為 118**（wb-315～322，8 條）；其次 94（-010～-012）、79（-331～-333）、74（-410～-416） | **數對；「94」並非最大值** | 待 5 節-4 |

patch 全表：`features/comfort/docs/reports/CMF-01_patch_plan.tsv`（74 列：M1 1、M2 6、M6 4、M8 3、M9-Note 25、M9-compound 35）。**全部停在 JSON 層之提案，未套用。**

## 4. 拆解不足 —— sibling plan（3 節）

`features/comfort/docs/reports/sibling_plan_0907.tsv`。**預估新增 23 條，未達停點（40）。**

| 候選來源 | 分析層 | 逐條判讀後 | 新增 |
|---|---|---|---|
| 1. 雙實體／雙觸發（17 條） | 17 條候選 | 拆 10：005-04、009-04、013-03、015-04、018-05、021-02、048-03、050、111-04、121-02。不拆 7：002-01（TS→HC 向另由 002-08 承接，建議刪去 wb-006 第 3 步）、010-05（power 實體屬 setup）、012-02、023-02、036-05（wb-179 已是 sibling）、101-01、123 | 10 |
| 2. 通道軸（約 45） | 約 45 | 以 037 VC 認定「兩通道」者 57 leaf，其中 TC 只走單通道者 33。**回到條文句逐 leaf 判讀，只有 2 個 leaf 的句子本身點名兩通道**：010-03（2.7 C6 `or use Hard Control`；ICS 側 112-04 已有 HC 條 wb-371，前排缺）、080-01（13.4 LS3 `or on the touch screen itself`）。其餘 31 個 leaf 的句子為顯示／狀態陳述，或屬單通道明文（2.2／16.2 的 popup 限 HC、7.5 只有 TS）。VC 的「via touchscreen or hard control」確實是同 parent 共用的樣板 | 2 |
| 3. 列舉未展開 | 003-07、023-01、024-xx | 003-06 補 front defrost（+1）；003-07 分支 (b) 補 4 個 mode 值（+4，值集受 DR #31 影響）、分支 (a) 全無 TC（+2，**ER「most closely matches」無值 → 產出時 ER 須 PENDING**）；023-01 補雙鍵／三鍵組合（+4）；**024 之 7 項已由 024-04～07 全數涵蓋（0）** | 11 |
| 4. ch11 ↔ ch12 | 10 組 | **實測 9 對**（J／L／M 逐字相同）：210↔247、211↔248、212↔249、214↔251、215↔252、216↔253、217↔256、218↔257、219↔258；213 無對。只做標記，不處置 | 0 |

受 R-C42 所阻者：**0**（R-C42 管的是配置條件的軸化，本表各軸都是觸發或列舉值）。

## 5. 待 Pei 裁定（附量測）

1. **基線**：量測支持「**以 JSON 為準重產，並把 0907 本的非語料欄帶進 Revise**」—— 語料 12 欄兩側完全相同，差異只在 G／P 兩欄（整欄單一映射）。**但 C（Polarion）、E（TestRail `C745xx`）、O、AA 四欄只存在於 0907 本**，`write_back.py` 目前把它們列為 NEVER_WRITE，而母本是空白範本 → **直接重產會丟失 TestRail ID**。所以 Revise 無論以哪一側為基線，都需要先決定這四欄的承載方式（進 JSON，或改以 0907 本為母本）。
2. **Test Group**：0907 本 466 格皆為 `Climate Control Interface`；R-C6／framework Part N 為 `Comfort`。
3. **車型欄**：表頭 T `HDCC27 Atl-Hi`、U `DT27 Atl-Hi`、V `VF(ProMaster)637`、W `Commander (598)`、X `Regengade (5210)`、Y `Toro(2261)`、Z `Fastack (376)`。`forms/proxi/` 有 6 個資料夾：HDCC27_initial、DT28_ATL_HI、HDCC28_ATL_HI、Promaster、Toro、Fastback；**598／5210 無 PROXI**（R-G74 恆為 0，與 0907 本的 `1` 衝突）。五份 PROXI 皆載有 `Climate_Type`／`Rear_Climate`／`Climate_Panel_Type`／`Integrated_Climate_Touchscreen`／`Automatic_Recirculation`（HDCC／DT 另有 `HVAC_AUTO_ECO_Mode`、`HVAC_Mode_Type`）→ **參數名可用來定軸，參數值本包未讀**。另：U 欄表頭為 DT**27**，資料夾卻是 DT**28**，須確認兩者是否為同一車型。
4. **下半形態／上半長度**：`->` 下半 **461** 條（分析層 459，差 2；扣除 4 條 BLOCKED 即為 461）；上半 > 50 token 共 26 條，最長 118。
5. **N 欄**：2.14 × 447、16.2 × 416、6.3 × 367（與分析層相同）。同時須決定 IN §10.7 之「一節一行」與 `SYS1_` 前綴（M4）。
6. **`001-01`／`-02`**：037 為 `up to 3 tabs`／無 Massage；0907 本 wb-001～004 依 SR24 條文測 4 tabs 並含 Massage。profile 對 Massage 只記載「委派文件已查而不解」（第 105 行），**未見範圍裁定**。
7. **Final Step 無 check target**：229 條（與分析層相同，啟發式）。**導航入口**：以 `Open the <comfort|climate|rear climate|tri-mode|seats>` 開頭者 60 條（分析層 85，pattern 不同）；`Open the comfort category` 5 條；R-G71 之 `" > "` 路徑形 **0 條**。
8. **Priority**：0907 本 P0 325／P1 132／P2 8，與分析層相同。**分析層「未見 rubric」只對一半**：profile 確實沒有本線專屬的 rubric，但全域有 `docs/runtime/TEST_CASE_PRIORITY.md`（P0–P3，核心主流程預設 P0）。0907 本整體降一級，看起來就是依這份 rubric 所做的修正，須確認是否據此回灌 JSON。
9. **（新增）`019-03` 之 `[BLOCKED-SPEC]` 白名單增列**（R-C26）：內容已備妥（見 M1）。

## 6. RD feedback 候補（比對 `DATA_REQUESTS.md`）

- **`19.1`–`19.3`（7" widget）：已載**，見 DR #6（「3 節仍未生成，037 未分析」）。分析層「profile 5.4 節未見」一說屬實，但 DR 已經登載。
- **`20.1`–`20.4.3` LATAM Rear Blower：已載**，見 DR #8（「10 節／0 leaf，DEFERRED，Pei 對 RD」）。分析層記為 11 列，是把 heading `20` 也算進去了。
- SR25 相對 SR24 之差異（§21 L3 HVAC、EH5 文字）：**本包未複驗**，依包內指示只做記錄。
- **提案新增 DR-46**：`$RECIRC_STAT$`／`$EBL_Stat$` 之 DBC message 與 raw 值（M6）。未登錄。

## 7. 產物與 SHA256

| 路徑（`features/comfort/` 下） | sha256 |
|---|---|
| `delivered/…_SWQT_ComfortHMI_20260907.xlsx`（新增，複製） | `3550d16d8bd319633d872856ae51ba63a7d2a928d22fab69bff6dd1963736aff` |
| `delivered/MANIFEST.tsv`（追加 1 列） | `38dc21e36d4f33be7c798043820251fe1fec0dd50f03140088daf92415e7c938` |
| `DELIVERY.sha256`（追加 ENTRY 036） | `87d6cb857e1b7279e0efdff5b0aba4ebfca1e7d94429db19bb6d7dddb939a63f` |
| `docs/reports/diff_0907_vs_generated.tsv`（新增） | `70d601b587f6ad026a7abe7f349e4c3a8cb891626cc906bdbc95624a732ef737` |
| `docs/reports/sibling_plan_0907.tsv`（新增） | `943abc0cf27bdd654d815a6d66750d8957a94964b66e455e3dce2e68462e53af` |
| `docs/reports/CMF-01_patch_plan.tsv`（新增） | `560e0b9c650e9e427e733b032c946a91b9c3bb5f3586d6de9ece9aeda511ab49` |
| `scripts/diff_0907.py`（新增，唯讀比對；import `write_back` 之 `row_plan`／`cell_of`，不呼叫 `main`） | `112815e003d2154a540334a8adfa08b8a249c0c6082620b063320b06117b1a32` |

## 8. 自檢與自報

- **LINT：未跑**（本包未改語料，也未產出工作簿）。無 `LINT_STATUS` 哨兵。
- 未跑 `gate_all.py`。`scripts/canon_refs.py --report --top 1000 --waiver`：本檔 unresolved／ambiguous **0 筆**（全庫 34／176 為既有數，非本包造成）。
- 自報：
  1. 1 節停點採字面解讀（931 格 > 700），並未以「只差 2 欄」為由改判；是否適用請分析層確認。
  2. M7 之計數範圍（L＋M）是反推分析層數字而來，並非依規定所定的範圍。
  3. sibling plan 之 2 節（通道）與 M9 之分類都以 regex 先篩、再人工判讀；**regex 沒篩到的 leaf 沒有逐一讀過**，所以「只剩 2 個」是在候選集之內的結論，不代表全體 leaf 都已檢查。
  4. 4 節 018-05 之 VC `press and hold` 須回條文判斷是否屬於本 leaf，未計入估數。
  5. 本 session 曾嘗試讀 `leaf_clause_sentence.tsv`，但誤以 req_id 為鍵（該檔以 tc_id 為鍵），查無資料；通道判讀改以 037 leaf 句與 `section_fulltext.tsv` 為據，不影響結論。
