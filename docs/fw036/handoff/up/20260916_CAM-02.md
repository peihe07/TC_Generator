# 上繳包 CAM-02 —— Camera Phase 2：裁定落檔 ＋ framework 落檔 ＋ lint Z ＋ crossref 核對

對應下放包：`docs/fw036/handoff/down/20260916_CAM-02.md`
對應審閱：`down/20260916_CAM-01_review.md`
執行層 session：延用 CAM-01 之 session，2026-09-16。**git 未動**（未 `add`／`commit`／`push`）。
**未生成任何 TC。** `framework.md` Part VIII 狀態為「草案，待 Pei 簽核」。
`_intake/Camera/` 未動（清空為 Pei 之 R-G46 動作）。

---

## §1　任務 1 —— 裁定落檔

### 1.1　R-G23 取號現查與實際號

現查：`features/camera/RULINGS.md` 有 `### R-CAM1`～`R-CAM3`；
全 repo 無 `R-CAM4` 以上；`docs/fw036/RULINGS.sha.tsv` 無 `R-CAM` 列（該表仍未重生）。
**`{live}` = 4 成立。**

| 下放包 §1 之條 | 實際號 | body sha8 | fenced sha8 | 錨點 |
|---|---|---|---|---|
| `R-CAM{live}` Test Group 與 TC ID | **R-CAM4** | `831c9ac2` | `a34710bb` | `RULINGS.md:41` |
| `R-CAM{live+1}` 品牌軸 | **R-CAM5** | `094c4d3e` | `91f752d4` | `RULINGS.md:51` |
| `R-CAM{live+2}` B 本追溯母體 | **R-CAM6** | `7a1f2817` | `1d21b22a` | `RULINGS.md:63` |
| `R-CAM{live+3}` V4 Layer 3 | **R-CAM7** | `6f3bc5a6` | `47738105` | `RULINGS.md:72` |
| `R-CAM{live+4}` ENTER_CAMERA_SETTINGS | **R-CAM8** | `9b443fdf` | `3e01768d` | `RULINGS.md:80` |
| `R-CAM{live+5}` Out of Scope 之處置 | **R-CAM9** | `fb789b49` | `eb9ae04c` | `RULINGS.md:91` |

**取號之一處自裁須回報**：下放包 §1 之第六個 code block（Out of Scope 之處置）
其標題寫 `R-CAM{live+5}`，而 §4-1 之上繳要求與 §3 預期數字皆只寫「R-CAM4～8 五條」。
執行層依 code block 之實際數目落 **六條**，第六條取 **R-CAM9**。
若 Pei 原意為該條不立號（併入 DECISIONS），請於審閱指明，執行層改為作廢 R-CAM9。

R-CAM1～R-CAM3 之 body sha8 落檔後仍為 `d3915cc6`／`b47059ed`／`73b08f60`，
與 CAM-01 上繳所載相同 —— 確認未誤改既有條文。

### 1.2　DECISIONS.md

| § | 狀態 | 內容 |
|---|---|---|
| 6-1 ABBR | `[DECIDED — R-CAM4(b)]` | A 本 `NR1L-RVC-{nnn}`、B 本 `NR1L-RVCHMI-{nnn}`，兩組序號各自自 001 起。CAM-01 之「共用 `CAM`」與審閱 §三-1 之同意**一併作廢** |
| 6-2 Test Group | `[DECIDED — R-CAM4(a)]` | 兩本 `Rear View Camera`。CAM-01 之 `Camera`／`Camera HMI` 與審閱 §三-2 之 `Camera` 皆作廢 |
| 6-3 `*` 入 hop | `[DECIDED — R-CAM5(a)]` | 不入；hop label 依 `Brand-Specific Names` 取值。**內含 A-CA18 之更正，見 §5.2** |
| 6-4 RVC+PAM | `[DECIDED — R-CAM6]` | 用 `spec-index/cache/` 本。DR-CAM-c 結案 |
| 6-5 V4 Layer 3 | `[DECIDED — R-CAM7]` | D 欄前導號向下繼承。DR-CAM-b 結案 |
| 6-6 lint 代號 | `[DECIDED]` | `Z`。落檔前再查一次現況（A-GC16）：`A`–`N`、`P`–`R`、`T`–`Y` 及 `I-sibling`／`I-cross` 已佔用，`O`／`S`／`Z` 未佔用 |
| 6-7 第 3 hop 權威 | `[DECIDED — R-CAM8]` | 足夠（canon §5.8(c)）。DR-CAM-d 結案 |
| **6-8 品牌 ↔ 車型** | `[PROPOSED]` **新增** | **指定來源查無此欄**；改提 PROXI `Brand_Configuration_2` 之實測，見 §5 |

### 1.3　DATA_REQUESTS.md

| DR | 變動 |
|---|---|
| DR-CAM-a | 維持 **MISSING（高）** —— `VF617_V5`，119 個來源引用、`SWE-CAM-024` 整組 |
| DR-CAM-b | **CLOSED**（R-CAM7）|
| DR-CAM-c | **CLOSED**（R-CAM6）|
| DR-CAM-d | **CLOSED**（R-CAM8）|
| **DR-CAM-e** | **新開（中）** —— 車型 ↔ 品牌之權威對照表；R-CAM5(c) 指定之 Market Config Table 無此欄 |

R-G71 之 `§5.3` 承接節四項全部有 `status:`：
`ENTER_SETTINGS_APP` = RESOLVED（沿用鎖定值）、
`ENTER_VEHICLE_SETTINGS`／`ENTER_HOME_SCREEN` = NOT_APPLICABLE、
`ENTER_CAMERA_SETTINGS` = **RESOLVED**（R-CAM8 定稿）。**承接完成，無 PENDING 複製。**

### 1.4　ANOMALIES.md

| # | 變動 |
|---|---|
| A-CA02 | PENDING → **RESOLVED**（R-CAM7）|
| A-CA10 | PENDING → **RESOLVED**（審閱 §二-2：`PowerShutDownNotifcation` 為 VF551_V2 docx 原文，非 037 筆誤，依 R-13 照抄）|
| A-CA12 | PENDING → **RESOLVED**（審閱 §四 重切八組，本包重跑全合）|
| A-CA13 | PENDING → **RESOLVED**（10 列已各有歸屬）|
| A-CA14 | PENDING → **RESOLVED**（R-CAM6）|
| **A-CA15** | 新登 —— `ShiftLeverPosition_PT` 僅 hybrid 分支（`Hybrid_Type = REP／FCEV`）取用；`-015` 主路徑改取 `TRANSM_FD_4.ShiftLeverPosition`（`Y`／`verified`），不受影響。不開 DR |
| **A-CA16** | 新登 —— `DynamicGrid*` 改以 HMI 現象（guideline 顯示與否）為觀察面，來源名保留（§8.7.5(f)）。不開 DR |
| **A-CA17** | 新登 —— `LanguageSelection` 以 HMI 語言設定為觸發（§5.8(e)），不走 CAN |
| **A-CA18** | 新登（**本包自報之誤載更正**）—— 品牌欄別誤讀，見 §5.2 |
| **A-CA19** | 新登 —— Abarth（Fastack 376）不在 `Brand-Specific Names` 四欄之內 |
| **A-CA20** | 新登 —— `SWE-CAM-025` 之畫面文字 037 作 `Camera Not in position`、SYS1 §9.2.3 作 `Camera Out of Position` |

### 1.5　feature.yaml —— **schema 支援雙 ABBR，不觸發升級條件**

`scripts/feature_config.py::load_feature_config` 為 `yaml.safe_load` ＋
自 `workbook.columns` 推導 `col`，**無 schema 驗證**，未限制 `books.*` 之鍵。
故 `books.a.abbr = RVC`／`books.b.abbr = RVCHMI` 直接可寫，**未擴 schema**。
實測載入通過（`col` 15 欄、雙 ABBR 取得）。

同時落：`test_group: "Rear View Camera"`（頂層與兩 book 各一份）、
`books.b.sys1_doc_ids` 之 RVC+PAM 改註 R-CAM6、`lint.profile: camera`、
`lint.extra_rules: ["Z"]`（由 CAM-01 之 `[PROPOSED] 未實作` 改為已實作之代號）。

---

## §2　任務 2 —— `framework.md` Part VIII 落檔

路徑：`docs/fw036/framework.md`，**Part VIII — Camera**（行 1466 起，全檔 1644 行）。
標題下第一行即「**狀態：草案，待 Pei 簽核（R-CAM4～R-CAM9 已落）**」。

小節：VIII.1 Layer 1／VIII.2 A 本 Layer 2／VIII.3 B 本 Layer 2／
VIII.4 Layer 3／VIII.5 軸／VIII.6 交叉參考索引／VIII.7 已知異常與未決／
VIII.8 Workbook sync。

### 2.1　B 本八組實測 —— **與修訂案逐組全合，無不符**

`layer2_assign.tsv` 依審閱 §四重跑（255 資料列 ＝ A 本 25 ＋ B 本 230）：

| # | Test Set | 預期 | 實測列 | leaf | umbrella | 判 |
|---:|---|---:|---:|---:|---:|---|
| 1 | `PAM Integration` | 10 | **10** | 9 | 1 | ✅ |
| 2 | `Activation and Exit` | 50 | **50** | 43 | 7 | ✅ |
| 3 | `Camera Settings` | 5 | **5** | 4 | 1 | ✅ |
| 4 | `Warning Banners` | 13 | **13** | 12 | 1 | ✅ |
| 5 | `AUX Camera Access` | 62 | **62** | 60 | 2 | ✅ |
| 6 | `Camera View Switching` | 31 | **31** | 25 | 6 | ✅ |
| 7 | `Wireless Camera Pairing` | 6 | **6** | 5 | 1 | ✅ |
| 8 | `AUX Camera Settings` | 53 | **53** | 45 | 8 | ✅ |
| | **合計** | **230** | **230** | **203** | **27** | ✅ |

**未歸屬 0、歧義 0。** 不符逐筆：**無**。
（下放包 §5 升級條件「B 本八組合計 ≠ 230」未觸發。）

A 本九組不動，實測仍 25/25、未歸屬 0。

Part VIII 另逐組列出章節細分（如 `AUX Camera Access` = HU 27.1(6)／27.2(10)／
27.3(4)／27.4(7)／27.5(2)／27.6(4)／27.7(3)／27.8(5)／34.1(5)／34.3(1)／34.4(3)／
34.6(4)／34.7(4)／34.8(4)），供 Pei 簽核時逐章核對。

---

## §3　任務 3 —— lint `Z` 實作

### 3.1　實作位置

| 項 | 位置 |
|---|---|
| 常數 | `scripts/lint036.py`：`VEHICLE_MODEL_HEADERS`（七欄逐字）、`VEHICLE_MODEL_ZERO`、`VEHICLE_MODEL_ALLOWED` |
| 欄位對照 | `build_vehicle_model_columns()` —— 取標頭**首行**去空白比對（母本第 9 列 T–Z 為 `HDCC27\nAtl-Hi\n` 之形，第二行之 EE 不入鍵）|
| 檢查 | `check_vehicle_model()` |
| 接線 | `lint_sheet()` 內逐列施檢；七欄不齊時**每 sheet 記一筆**，不逐列複述 |
| 報告三表 | `CHECK_TITLES`／`CHECK_STATUS`／`CHECK_GRANULARITY` 各加 `Z` |
| 啟用 | **新增 `FEATURE_CHECKS = {"camera": ["Z"]}`**，`check_order()` 僅於 `profile == "camera"` 附加 |
| 測試 | `tests/test_lint036_z.py`（**27 項**）|

七欄逐字（母本 T–Z 實測，R-G48）：`HDCC27`／`DT27`／`VF(ProMaster)637`／
`Commander (598)`／`Regengade (5210)`／`Toro(2261)`／`Fastack (376)`。

判準與粒度：

| 施檢面 | 粒度 | 訊息 |
|---|---|---|
| (a) 值非 `1`／`0`（含空、空白、`X`、`2`…）| 每列每欄 | `R-CAM2(a)：七欄每列須填 1 或 0，不留空、不用其他符號` |
| (b) `Commander (598)`／`Regengade (5210)` 非 `0` | 每列每欄 | `R-CAM2(b)：… 已不支援，Camera 兩本一律 0` |
| (c) 五個有效欄無任何 `1` | 每列一筆 | `R-CAM2(c)：五個有效車型欄每列至少一個 1` |
| 七欄不齊 | **每 sheet 一筆** | `R-CAM2：本 sheet 無完整之 Vehicle Model 七子欄，Z 無法施檢` |

(a) 命中之欄不再判 (b)；(a) 有任一命中時不判 (c)。
`\xa0` 與前後空白先剝除（不可見字元歸 `Q` 管）；儲存格為數值 `1`／`0` 亦合規。

### 3.2　**為何另立 `FEATURE_CHECKS` 而非併入 `PROFILE_CHECKS`**

`PROFILE_CHECKS` 隨**任意** `--profile` 值生效。`Z` 若併入，任何 feature 之
profile 執行都會整本 FAIL。故另立只認 feature 名之 `FEATURE_CHECKS`。
此為 `lint036.py` 內之新模組級 dict，**未改 canon、未改全域 ledger**。

### 3.3　既有交付本乾跑 —— 無誤報，且證實必須 feature 專屬

對 `features/*/delivered/*.xlsx` **10 本**（非下放包所稱之八本）逐本乾跑：

| profile | Z 命中合計 |
|---|---:|
| `camera` | **11,368** |
| （無 profile）| **0** |
| `power`（他 feature）| **0** |

**誤報判定：無誤報。** 逐筆檢視（以 `pm_29.xlsx` 為例）：389 資料列 × 7 欄
＝ 2,723 筆，**全部**為 `R-CAM2(a)`、snippet 全為 `(空)`。
即既有交付本**具備**該七欄（欄位對照命中，非「七欄不齊」之單筆），
但七欄**從未填值**。這是既有本之事實，不是檢查之誤判 ——
亦即下放包所預期之「既有本全空 → 全 FAIL」成立，
**`Z` 僅在 Camera profile 啟用**之設計因此是必要的，不是保守。

`Z(none)` 與 `Z(power)` 皆 0 → **既有八本之報告基線完全未動**。

### 3.4　pytest

```
tests/test_lint036_z.py            27 passed
tests/test_lint036.py（既有）      137 passed   ← 含本包修改之 1 項＋新增之 1 項
全 lint 相關                       164 passed
```

**修改既有測試一項（須回報）**：`tests/test_lint036.py::
test_every_check_has_status_and_granularity` 原斷言
`set(CHECK_TITLES) == set(check_order("power"))`，`Z` 加入後多出一項而失敗。
已改為涵蓋「通用 ＋ profile ＋ 全部 `FEATURE_CHECKS`」，並加一條
`feature_only.isdisjoint(generic)`（防日後誤把 feature 專屬者混入通用序列）。
另新增 `test_feature_checks_only_fire_for_their_own_profile`。

### 3.5　**`Z` 不入 `GATES.tsv`（R-G56）—— 未處置，回報**

`scripts/gates_tsv.py:185-186` 之閘登錄只列舉 `CHECK_ORDER` 與 `PROFILE_CHECKS`，
**不讀 `FEATURE_CHECKS`**，故 `Z` 不會出現在 `docs/runtime/GATES.tsv`。
要登錄須改 `gates_tsv.py` ＋ 重生該全域 tsv —— 觸及下放包 §5 之
「需改全域 ledger」，**本包不做，列為升級事項**（見 §7）。

---

## §4　任務 4 —— crossref 逐列核對

`features/camera/data/crossref_a_b.tsv` 由 68 列 × 10 欄擴為 68 列 × 12 欄
（新增 `relation`／`status`／`verdict_reason`）。

**核對準則**（本包自定，已寫入腳本 docstring）：
`same-point`（同一驗證點）與 `precondition-or-hop`（非同一驗證點，
但為該 A 列之 TC 所需之前置或導航 hop 之逐字來源）判 `[VERIFIED]`；
其餘判 `[REJECTED]`，各附一句理由。

### 4.1　計數

| status | 筆數 |
|---|---:|
| `[VERIFIED]` | **53**（`same-point` 45 ＋ `precondition-or-hop` 8）|
| `[REJECTED]` | **15** |
| 合計 | 68 |

依 A 本列：`-015` 7/0、`-018` 7/0、`-016` 3/0、`-021` 4/2、`-023` 4/2、
`-024` 26/9、`-025` 2/2（格式：VERIFIED／REJECTED）。

### 4.2　`[REJECTED]` 15 筆

| A 本 × B 本 | 理由 |
|---|---|
| `-021` × `RVC-029` | §8.3.2 為關閉 RVC 後回到前一 HU 狀態，屬退出行為；`-021` 為影像無閃爍與在地化疊層 |
| `-021` × `RVC-030` | §8.3.3 為「無法顯示 X 時不支援 camera delay」之支援性約束，屬 `-015`／`-018` 之 delay 線 |
| `-023` × `RVC-029` | 同上；非 guideline／疊層／觸控之渲染 |
| `-023` × `RVC-030` | 同上 |
| `-024` × `RVC-056` | §27.1.4 core HU lockout 之承接，屬 Driver Distraction 線 |
| `-024` × `RVC-057` | §27.1.5 無線 AUX 數量上限（4 具 MOPAR／WiFi），屬配對線 |
| `-024` × `RVC-058` | §27.1.5.1 QR code token 認證，屬配對線 |
| `-024` × `RVC-086` | §27.7.3 純交叉引用（指向 Wireless cameras connection 頁），無可驗證行為 |
| `-024` × `RVC-091` | §27.8.5 同上 |
| `-024` × `RVC-102` | §28.6.1.2 同上 |
| `-024` × `RVC-103` | §28.7.1.1 pan／zoom 手勢，屬影像編輯線 |
| `-024` × `RVC-104` | §28.7.1.2 影像重設與 latch，屬影像編輯線 |
| `-024` × `RVC-105` | §28.7.2 Edit image 之 Rotate 90／Mirror／Reset，屬影像編輯線 |
| `-025` × `RVC-037` | §9.2.1 為「Camera System Unavailable」（影像無法顯示），`-025` 之訊息為 out of position |
| `-025` × `RVC-038` | §9.2.2 為藍屏條件之「Camera System Unavailable」，訊息不同 |

### 4.3　`precondition-or-hop` 8 筆（判 `[VERIFIED]` 但非同一驗證點）

`-018`×`RVC-008`（活化為退出 TC 之前置）、
`-016`×`RVC-048`／`-050`／`-051`（手動入口之 hop 及其條件分支）、
`-015`×`RVC-045`、`-018`×`RVC-045`（Delay 設定項為前置）、
`-024`×`RVC-084`／`-085`（AUX 入口之 hop 及其條件分支）。

### 4.4　B 本未配對列數

B 本 230 列中，被 A 本任一列配對者 **55**，**未配對 175 列**，
逐列落 `features/camera/data/crossref_b_unpaired.tsv`。
此 175 列即「B 本獨有」之候選母體，供 Phase 3 判定。

### 4.5　核對中發現之新異常

`-025` 之 037 Description 逐字「display a "**Camera Not in position**" warning
overlay」，而其唯一 `[VERIFIED]` 之同點配對 `RVC-039`（SYS1 §9.2.3）逐字
「Display "**Camera Out of Position**" message」。兩側畫面文字不一致 ——
登 **A-CA20**，verbatim 上半引哪一側待裁。

---

## §5　任務 5 —— 品牌對照 recon　**（觸發下放包 §5 升級條件）**

### 5.1　R-CAM5(c) 指定之來源 —— **查無該欄**

`forms/SR24 R1 Market Configuration Table v1.6.xlsx`（8 分頁）全表逐格掃描：

- `HDCC27`／`DT27`／`637`／`2261`／`376` 五個車型代號 **零命中**。
- 品牌字樣僅三處，皆為散文，非對照欄：
  `Revision Log!D25`「Feedback from RAM teams (GIM 21023031)」、
  `Revision Log!D26`「Feedback from Alfa」、
  `Market Config - R1!E6`「Only for EMEA/Maserati Variants with HU Internal
  Antenna Homologation needs」。
- 該表之軸為**市場／國別**（`Geographical Region`／`Destination Country`／
  `Distribution Country`／Radio Variant），與車型無關。

→ **升級條件「Market Configuration Table 無五車型之品牌欄」成立。**
R-CAM5(c) 另明文「不得自 anchor 前綴或車名推定」，故執行層**不推定**，
改提下列**實測**來源，列 DECISIONS 6-8 `[PROPOSED]`，請 Pei 核可其權威性。

### 5.2　**A-CA18 —— CAM-01 之品牌欄別誤讀，本包更正**

`brand_labels.tsv`（逐格，含空欄；經 `openpyxl` 非 read-only 複查，無合併儲存格）：

| 列 | `A` | `B` Jeep / Chrysler / Ram / Dodge | `C` Fiat / Fiat Commercial | `D` Maserati | `E` Alfa |
|---:|---|---|---|---|---|
| 48 | `Camera` | `ParkView Backup Camera Delay` | **（空）** | `Rear View Camera Delay [CR14730]` | **（空）** |
| 49 | `Camera` | `ParkView Backup Camera Active Guidelines` | **（空）** | `Rear View Camera Active Guidelines [CR14730]` | **（空）** |

基礎 label（`Settings` 分頁）：row 467 `Rear View Camera Delay*`、
row 468 `Rear View Camera Active Guidelines*`。

**CAM-01 上繳 §6、profile §3.1、DECISIONS 6-3、以及 CAM-01 審閱 §三-3
四處皆把 `[CR14730]` 兩項歸為 Fiat —— 實為 Maserati；Fiat 與 Alfa 兩欄為空。**
成因：CAM-01 之取值以「非空儲存格依序列印」為之，空欄被略過而使欄位右移一格。
本包已於 DECISIONS 6-3／6-8、profile §3.1 更正，逐字表落
`features/camera/data/brand_labels.tsv`。

### 5.3　替代來源 —— PROXI `Brand_Configuration_2`（實測）

參數定義（`forms/PROXI_HDCC27_R3_20250424.xlsx` 分頁 `Format` **row 566**）：
`Infotainment_Configuration_13`，byte **130**，bit **0–4**，Coding = `Table`，
枚舉逐字 `0 = Unbranded / 1 = Fiat / 2 = Lancia / 3 = Alfa Romeo /
4 = Fiat Professional / 5 = Abarth / 6 = Maserati / 7 = Ferrari / 8 = Chrysler /
9 = Dodge / 10 = Jeep / 11 = RAM / 12 = SRT / 13 = PSA (Citroen) /
14 = PSA (Peugeot) / 15 = Ford`。

各平台實測（`features/camera/data/brand_proxi.tsv`）：

| Vehicle Model 欄 | forms/proxi 平台 | 值（逐字）| 品牌 | 來源格 |
|---|---|---|---|---|
| HDCC27 Atl-Hi | `HDCC28_ATL_HI` | `11=RAM` | **RAM** | `Sheet1!I565` |
| DT27 Atl-Hi | `DT28_ATL_HI` | `11=RAM` | **RAM** | `Sheet1!I565` |
| VF(ProMaster)637 Atl-Mi | `Promaster_ATL_MI` | `11` | **RAM** | `PROXI Write_Read!Y570` |
| Toro(2261) Atl-Mi | `Toro_ATL_MI` | `1` | **Fiat** | `PROXI Write!Y570` |
| Fastack (376) Atl-Mi | `Fastback_ATL_MI` | `5` | **Abarth** | `PROXI Write_Read!Y570` |
| （範本，非車型）| `HDCC27_initial` | `0=Unbranded` | Unbranded | `Sheet1!I565` |

兩種 PROXI 檔格式之取值欄不同，已各自驗明表頭：`Sheet1` 格式取 `I` 欄
（`11=RAM` 之複合字串）；`PROXI Write*` 格式取 **`Y` 欄（表頭 `Dec`）**。

### 5.4　由 5.2 ＋ 5.3 推得之 hop 影響

| 車型 | 品牌 | 命中之 Brand-Specific 欄 | hop label |
|---|---|---|---|
| HDCC27／DT27／637 | RAM | `B`（Jeep / Chrysler / Ram / Dodge）| `ParkView Backup Camera Delay`／`ParkView Backup Camera Active Guidelines` |
| Toro(2261) | Fiat | `C` —— **該欄為空** | 回落基礎 label（去 `*`）|
| Fastack (376) | Abarth | **無對應欄** | 回落基礎 label（去 `*`）|

→ **R-CAM5(b) 之品牌軸於 Camera 範圍內實際只有兩分支**（RAM 系 vs 其餘），
非逐品牌。`Commander (598)`／`Regengade (5210)` 依 R-CAM2(b) 恆 `0`，不參與。

**未決**（A-CA19、DECISIONS 6-8(iv)）：
Abarth 不在四欄之內，「無欄即回落基礎 label」為執行層之讀法，**未經裁定**；
且 Fastback 之 `5 = Abarth` 與該平台檔名所示之 363/376 車系是否確為 Abarth 版，
本包未另尋佐證。

---

## §6　任務 6 —— 兩處更正落檔

| 更正 | 落點 |
|---|---|
| `-002` Ignition 前提：Atl-Hi `BCM_FD_10.CmdIgnSts`（`Y`／`verified`）、Atl-Mi `STATUS_BH_BCM2.CmdIgnSts`（`N`／`Could emulate`）；**不得用 `BCM_FD_9.PowerModeSts`** | profile **§5.1**（新節）。CAM-01 上繳 §3.9「Atl-Hi 側無對應之 `CmdIgnSts` 列」之誤已於該節具名撤回 |
| `PowerShutDownNotifcation` 為 VF551_V2 docx 原文，依 R-13 照抄 | `ANOMALIES.md` A-CA10 改 **RESOLVED**，並載來源逐字 |

同時，`sources/MANIFEST.tsv` 兩列之 `note` 依 R-CAM6 改寫：
`sys1_hmi_rvc_pam_r1_low_sr24_1a_ref` 記「同名異體，為 cache 本之真子集，
**不作追溯母體**」；`sys1_hmi_rvc_pam_r1_low_sr24_1a_cache` 記「**B 本
spec_reference 之追溯母體**，對映率 100.00%」。MANIFEST 列數不變（194 資料列）。

---

## §7　升級條件之檢查

| 條件 | 結果 |
|---|---|
| `feature.yaml` schema 不支援雙 ABBR | **否** —— `feature_config.py` 無 schema 驗證，`books.*.abbr` 直接可寫，實測載入通過（§1.5）。**未擴 schema** |
| B 本八組合計 ≠ 230 | **否** —— 八組逐組全合，合計 230，未歸屬 0、歧義 0 |
| **Market Configuration Table 無五車型之品牌欄** | **是** —— 全表零命中（§5.1）。已改提 PROXI `Brand_Configuration_2` 之實測為替代來源，列 DECISIONS 6-8 `[PROPOSED]` ＋ DR-CAM-e，**未推定**（R-CAM5(c) 明文所禁）|
| 需改 canon 或全域 ledger（含 `Z` 須入 `CHECK_TITLES` 以外之結構）| **部分** —— 見下 |

**關於最後一項，兩件須 Pei 裁：**

1. **`FEATURE_CHECKS` 之新增**：`Z` 之啟用面須縮到 feature，`CHECK_TITLES`
   本身容得下（已加），但**啟用序列**須另立 dict。該 dict 位於
   `scripts/lint036.py` 之模組層，**非 canon、非全域 ledger**，且下放包
   §2 任務 3 明文要求「`Z` 僅在 Camera profile 啟用，不入全域預設」——
   執行層判為包內授權範圍，已實作。若 Pei 認為仍屬須升級之結構變更，
   本包可回退為「只加 `CHECK_TITLES` 不接線」。
2. **`Z` 不入 `GATES.tsv`（R-G56）**：`gates_tsv.py` 只讀 `CHECK_ORDER`／
   `PROFILE_CHECKS`，故 `Z` 不在閘登錄簿內。要登錄須改該全域生成器並重生
   `docs/runtime/GATES.tsv` —— **本包未做**，請 Pei 決定是否併入 GC 線。

---

## §8　gate 與測試現況

| 項 | 結果 |
|---|---|
| `scripts/lint_paths.py` | **PASS**（基線外違規 ＋ delivered 不符 = 0）|
| `tests/test_lint036.py` ＋ `test_lint036_z.py` | **164 passed** |
| 全 `tests/` | 1374 passed、9 failed、15 skipped |

**9 項失敗全部與本包無關，逐項查明**（本包未修，非本線之責）：

| 失敗 | 成因 |
|---|---|
| `test_single_write_path.py` 2 項 | 15 個既有腳本（`features/time_management`／`user_profiles`／`vehicle_category`／`vehicle_setting`）直呼 `openpyxl.save` 而未入 grandfather 名單。無一為本包檔案 |
| `test_intake_scaffold.py` 6 項 | `new_feature.py` 於 pytest tmp dir 以 `--adopt-existing` 執行時 exit 2；為既有測試夾具問題，與 Camera 無關 |
| `test_gates_tsv.py` 1 項 | `GATES.tsv` 漂移 —— 重生後之**唯一**差異為新增一列 `features/vehicle_setting/scripts/vs_cf01_selfcheck.py`（該檔於 commit `2c08a48` 落，未登簿）。**本包之 `Z` 不造成漂移**（`gates_tsv.py` 不讀 `FEATURE_CHECKS`，已逐列驗明）。歸 vehicle_setting／GC 線，本包不動（沿 CAM-01 審閱 §六之形制）|

---

## §9　自報錯誤與未結

### 9.1　自報

1. **A-CA18 —— CAM-01 之品牌欄別誤讀為 Fiat（實為 Maserati）**，且該誤已被
   CAM-01 審閱 §三-3 沿用。成因為取值時略過空欄致欄位右移。本包以逐格
   （含空欄）複查更正，四處落點全部改寫。**同類形態（非空即印）日後須避免。**
2. **R-CAM9 為執行層自裁之取號**（下放包 §1 有六個 code block，而 §3／§4 只寫
   「五條」）。若原意為不立號，請指明。
3. B 本 Layer 2 之歸屬仍為**機械套用**審閱 §四之章節前綴，未加人工判斷；
   本輪全合，故無須人工調整之處。
4. crossref 之 `[VERIFIED]`／`[REJECTED]` 為**執行層之語意判讀**，準則已明載
   （§4 開頭），但判讀本身未經第二人覆核；`-024` 之 35 筆中判 9 筆 REJECTED，
   為全表爭議最集中處。

### 9.2　未結

| # | 項 | 去處 |
|---|---|---|
| 1 | **DR-CAM-a（高）** `VF617_V5` 缺件 —— 119 個來源引用、`SWE-CAM-024` 整組 BLOCKED | 補件 |
| 2 | `SWE-CAM-023` 與 6 列交集之**承接順位** | 審閱 §三-10：待 framework 鎖定時定 |
| 3 | 車型 ↔ 品牌之權威來源（DR-CAM-e）；Abarth 回落讀法（A-CA19）| Pei 裁 |
| 4 | `-025` 畫面文字兩側不一致（A-CA20）| Pei 裁 |
| 5 | `Z` 是否入 `GATES.tsv`（§7-2）| Pei 裁／GC 線 |
| 6 | `docs/fw036/RULINGS.sha.tsv` 仍未重生（全域檔）| GC 線排程 |
| 7 | `framework.md` Part VIII **未簽核** → Phase 4+ 仍 blocked | Pei 簽核 |

### 9.3　本包寫入之檔案（git 未動）

```
M  scripts/lint036.py                          Z：常數／欄位對照／檢查／接線／FEATURE_CHECKS
M  tests/test_lint036.py                       既有 1 項改寫 ＋ 新增 1 項
?  tests/test_lint036_z.py                     27 項（新）
M  docs/fw036/framework.md                     Part VIII（1466–1644 行，新）
M  docs/runtime/profiles/FW036_R1L_Camera_Profile.md   §2 實作／§3.0 常數／§3.1 更正／§3.2 品牌軸／§5.1 Ignition／§6 未決改寫
M  features/camera/RULINGS.md                  R-CAM4～R-CAM9 ＋ 取號回報
M  features/camera/DECISIONS.md                6-1～6-7 改 [DECIDED]；新增 6-8
M  features/camera/ANOMALIES.md                A-CA02/10/12/13/14 改 RESOLVED；新登 A-CA15～A-CA20
M  features/camera/DATA_REQUESTS.md            DR-CAM-b/c/d 結案；新開 DR-CAM-e；§5.3 承接完成
M  features/camera/feature.yaml                雙 ABBR／test_group／lint.profile／extra_rules
M  features/camera/data/layer2_assign.tsv      依審閱 §四重跑（255 列）
M  features/camera/data/crossref_a_b.tsv       68 列 × 12 欄（＋relation/status/verdict_reason）
?  features/camera/data/crossref_b_unpaired.tsv   175 列（新）
?  features/camera/data/brand_labels.tsv          8 列（新）
?  features/camera/data/brand_proxi.tsv           6 列（新）
M  sources/MANIFEST.tsv                        RVC+PAM 兩列之 note 依 R-CAM6 改寫（列數不變）
?  docs/fw036/handoff/up/20260916_CAM-02.md    （本檔）
```
