# 上繳包 CDD-01 —— Diagnostics feature intake（scaffold ＋ 來源登錄 ＋ recon ＋ framework 驗算）

下放包：`down/20260917_CDD-01.md`。執行層 session：2026-09-17（Pei 指示於分析層同 session 直接執行，
未另開 session —— 下放包標頭之 R-G62(b) 指定作廢，此事實記此）。

**§0 禁區遵守**：git 全程未動（未執行 `git add`／`commit`／`push`）；未動 `down/`、`up/` 既有檔；
未動 `features/` 下其他 feature 目錄；**未生成任何 TC**；未補產 CFTS004 未收之 50 條 FR。

**結論**：任務 0–5 全部完成。**升級兩項**（§9），**阻斷 CDD-02 一項**（§7-1）。

---

## §1　投遞與來源登錄（任務 0、2）

### 1.1 全 19 件 sha256 全表

sha256 前 16 位；全 19 件皆已登 `sources/MANIFEST.tsv`（224 → **235 行**，新增 11 列）。

| doc_id | sha16 | features 欄 | 檔名 |
|---|---|---|---|
| `diagnostics_037_swra_v1` | `b985e09ac42cece7` | `diagnostics` | SWE1_Diagnostics_V1 (2).xlsx |
| `sys2_ame_diagnostics_sysra` | `b0e0f51cfcb3ecad` | `diagnostics` | SYS2_AME_Diagnostics_SYSRA_…_Released_.xlsx |
| `sys2_cfts013_radio_error_management_dcsd_v1` | `1c11d4de6e529ec3` | `diagnostics` | SYS2_CFTS_013_Radio Error Management_DCSD_0126_1_V1_Released.xlsx |
| `sys2_cs00042_sysra` | `1992d4fe6c2e0eab` | `diagnostics` | CS00042_SYSRA …_Released.xlsx |
| `sys2_cs00010_03_sysra` | `0fb728893314d0b4` | `diagnostics` | SYS2_CS.00010_03_SYSRA … Released.xlsx |
| `sys2_cs00025_sysra` | `0b69505f3974362c` | `diagnostics` | SYS2_CS00025_SYSRA …_SYSRA_Released.xlsx |
| `sys2_cfts024_tuner_sysra_v01` | `e5004e46835a6bda` | `diagnostics` | SYS2_CFTS024_Tuner_…_CFTS024_Tuner_V01.xlsx |
| `cfts024_specific_hu_radio_functions_20260608` | `c7d2b53cf9cc6a14` | `diagnostics` | R1LR_Atl-H_26PI2.5 … CFTS 024_…_20260608-1206.docx |
| `sys3_cfts004_general_diag_sysad` | `bc0bad3dbb0710a4` | `diagnostics` | SYS3_CFTS_004_General Diagnostics_…_SYSAD.docx |
| `sys3_amfm_sysad_v0_2` | `679dfc256b6f47a7` | `diagnostics` | SYS3_AMFM_…_SYSAD_v0.2_20260629.docx |
| `sys3_sxm_sysad_v0_2` | `2db8d29834675e69` | `diagnostics` | SYS3_SXM_…_SYSAD_v0.2_20260629.docx |
| `sys2_cfts004_general_diag` | `ffcb202fff92007e` | `security,diagnostics` | SYS2_CFTS_004_General_Diagnostic_Requirements_…xlsx |
| `sys2_cfts013_radio_error_management_associated_display` | `1036b2af9f655441` | `display,diagnostics` | SYS2_CFTS013_Radio_Error_Management-Associated_Display.xlsx |
| `sys2_cs00102_swad` | `6c20f3be4299d8e7` | `security,diagnostics` | SYS2_CS00102_SWAD …_Released.xlsx |
| `sys2_sd00049_sysra` | `ee086d9044e5c666` | `security,diagnostics` | SYS2_SD.00049 _SYSRA …_Released.xlsx |
| `sys2_cs00099_sysra` | `caffcd100c3d8657` | `security,diagnostics` | SYS2_CS.00099_SYSRA …_Released.xlsx |
| `sys2_cs00100_sysra` | `b2dc81d7f92ab278` | `security,diagnostics` | SYS2_CS.00100_SYSRA …_146_150_RAR.xlsx |
| `sys2_cs00101_sysra` | `570af91e2082dd21` | `security,diagnostics` | SYS2_CS.00101_SYSRA …_Released.xlsx |
| `sys2_cs000148_sysra` | `d3cb892261dcb84c` | `security,diagnostics` | SYS2_CS.000148_SYSRA …_Released.xlsx |

### 1.2 任務 0 之複驗結果（§9 前兩項）

- 037 sha16 = `b985e09ac42cece7` **符**；CFTS004 REF 本 sha16 = `ffcb202fff92007e` **符**。
  §9 之升級條件 1 **未觸發**。
- 下放包 §4(a) 列「不投遞」8 件，實測 **8 件全數命中 MANIFEST 同 sha**，
  `features` 欄已 append `,diagnostics`（append-only，未用 `--refresh-manifest`）。
  §6 預期「MANIFEST 既有同體 ≥ 8」**符（恰 8）**。
- 下放包 §4(b) 列「必投遞」，實測 **11 件**（下放包表列 8 列，其中三列為合併書寫：
  `CS00042`／`CS.00010_03`／`CS00025` 一列三檔、兩本 SYSAD docx 一列兩檔）。全數 repo 內無同 sha。
- 落點：`_intake/Diagnostics/`（投遞區，11 件）→ `sources/raw/<doc_id>/`（R-G66 原檔落點，11 件）。
  二者皆在 `.gitignore` 內（`_intake/*`、`sources/raw/`），**不入 git**。

### 1.3 R-G67 嵌入物件檢查（docx 四件）

**母 CFTS = CFTS004 → 已查、無此目錄。**

`1_Customer_Requirement/R1LR SR26 ATL-H/` 下四個 PI 之 `Reference Documents/CFTS Embedded Objects/`
逐一列舉，**無 CFTS004 子目錄**：

| PI | 該目錄下之 CFTS 子目錄 |
|---|---|
| 26PI1.5 | CFTS013, CFTS019, CFTS024, CFTS025, CFTS036, CFTS069 |
| 26PI2.5 | CFTS001, CFTS013, CFTS019, CFTS024, CFTS025, CFTS028, CFTS036, CFTS057, CFTS069 |
| 26PI3.5 | CFTS013, CFTS019, CFTS024, CFTS025, CFTS069 |
| 25PI4.5 | （`Embedded Objects/`）含 CFTS024 |

`find … -iname "*004*" -path "*mbedded*"` 全庫 **零命中**。

四件 docx 之內部 `word/embeddings/` 亦全部為 **0**（只有 `word/media/` 圖片：
CFTS024 docx 15、AMFM SYSAD 12、CFTS004 SYSAD 8、SXM SYSAD 13）。

**附記**：`CFTS024`（背景檔之母 CFTS）於 26PI2.5 下有 16 個嵌入物件
（6 個 `.rtf` inline 圖、10 個 `.xls`）。**CFTS024 非本 feature 之拆解對象**
（037 零引用，R-DIAG8 範圍外），故不出「由圖找列」二欄表；事實記此。

---

## §2　scaffold 與 RULINGS（任務 1）

- `python scripts/new_feature.py Diagnostics --root .` **成功**；`features/diagnostics/` 先前不存在。
  §9 之升級條件 2 **未觸發**。產物：6 目錄（`inputs`/`data`/`batches`/`generated`/`scripts`/`docs`）
  ＋ 8 檔（`RUNBOOK.md`／`ANOMALIES.md`／`RULINGS.md`／`DATA_REQUESTS.md`／`DECISIONS.md`／
  `feature.yaml`／`PLAYBOOK.md`／`.gitignore`）。
- **scaffold 之 ABBR 瑕疵已修**：`new_feature.py` 以 `feature[:2].upper()` 取 abbr，
  產出 `[A-DInn]`；依 R-DIAG9(a) 之 `DIAG` 改為 `[A-DIAGnn]`（`ANOMALIES.md`、`PLAYBOOK.md` 各一處）。
  該公式對任何 ABBR ≠ 前二字之 feature 皆會產生同一瑕疵，**未改腳本**（全域工具，屬 §9）。

### 2.1 R-G23 取號現查

`grep -rn "R-DIAG"` 於全 repo（排除本包下放檔）**零命中**；
`docs/fw036/RULINGS.sha.tsv` 無 `R-DIAG` 列（現僅含 `R-SEC` 前綴）。
**`{live}` = 1 成立**，與下放包 §2 期望值一致。九條實際號為 **R-DIAG1～R-DIAG9**。

### 2.2 九條之實際號與 body sha8

`rulings_hash.py` 讀不到本檔錨點（原因見 §9-2），故 sha8 依該腳本 docstring 所載之
本體定義自算（錨點次行起至下一同級標題前一行止，首尾空行去除、行尾空白去除、
`\n` 接合、UTF-8 取 sha256）。

| 條號 | body_sha8 | body 行數 | slug |
|---|---|---:|---|
| `R-DIAG1` | `b2bee17e` | 8 | TC ↔ SWE1 為 N:1；三段式各列各自成 TC 群 |
| `R-DIAG2` | `ed3616d3` | 6 | 重複 SWE1 ID 之處置 |
| `R-DIAG3` | `4492e16d` | 7 | Out of Scope 列不產 TC、保留原內容 |
| `R-DIAG4` | `114991db` | 7 | 拼字與空白 |
| `R-DIAG5` | `ed8ed0ce` | 13 | DID 與 UDS 記法 |
| `R-DIAG6` | `b16e40e2` | 6 | 037 Verification Method 空白之處置 |
| `R-DIAG7` | `15e271dc` | 8 | Harman Status 與 Region |
| `R-DIAG8` | `afd49bf2` | 7 | 範圍 |
| `R-DIAG9` | `802f0427` | 9 | Test Group、TC ID、Layer 2/3 |

條文本體**一字未改**，逐字照錄自下放包 §2（R19-2）。

---

## §3　§6 預期 vs 實測全表

母體：037 `395 列`／037 來源 `168`／CFTS004 索引 `404 列`。

| 項 | 預期 | 實測 | 判 |
|---|---|---|---|
| 037 資料列 | 395 | **395** | 符 |
| 唯一 SWE ID | 394 | **394**（重複 `-340` @ row 347/348）| 符 |
| 來源唯一數／解析率 | 168／168 (100%) | **168／168 (100%)**，未命中 0 | 符 |
| 每來源列數分佈 | 3列×91、2列×45、1列×32 | **同** | 符 |
| Priority | Medium 350／Low 39／High 6 | **同** | 符 |
| Release Version | V1.00.00 394／V1.00.01 1 | **同**；V1.00.01 定位為 **row 336 = `SWE1-Diagnostics-329`**（USB 連接數回報）| 符＋補定位 |
| Status／Categorization／Sub Cat | 全 New／全 FR／全 Service | **同**（395/395/395）| 符 |
| 標題領域前綴 | SiriusXM 168／Audio 105／GPS 40／AM-FM 31／Steering Wheel 5／Camera+Caemera 6／USB 4／KeySense PIN 3／Display 3／Clock Defrost 2／12-24 Time 2／Front Seat Video 2／無前綴 14 | **全數相符**（Caemera 4 ＋ Camera 2 = 6；AM/FM 31；無前綴 14）| 符（附註 A）|
| VM 空白列 | 4（SWE1-189～192）| **4**（row 196–199 = `SWE1-Diagnostics-189/190/191/192`）| 符 |
| 拼字異常 | Caemera 4／senario 3／recevied 1／diagnopstic 1 | **同**；另 `bightness` **2**（row 8、11）| 符＋補 |
| 首尾空白 | 來源ID 3(-315)／標題 12／描述 21 | **3**（row 101–103，皆 `'SYS-RA-DIAG-315   '` 尾 3 空格）／**12**／**21** | 符 |
| DID token | `$XXXX` 27 種／`0xXXXX` 23 種 | **27**（232 次）／**23**（94 次）；同列並存 **74 列** | 符＋補 |
| DTC 提及列 | 0 | **0** | 符 |
| CFTS004 索引列 | 404（FR 217／Information 95／Heading 78／Out of Scope 14）| **404 / 217 / 95 / 78 / 14** | 符（附註 B）|
| 被引用來源 Category | FR 167／Out of Scope 1 | **同**（Out of Scope = `SYS-RA-DIAG-248`）| 符 |
| 被引用來源 HARMAN Status | 空 110／Need Rework 55／Accepted 2／Need Clarification 1 | **同** | 符 |
| 被引用來源 Region | All 89／NAFTA 系 78／空 1 | **同** | 符 |
| Region 原值變體 | 23 種排列 | **24 種** = `All` ＋ **22 種** NAFTA 排列 ＋ `''`（空）| 差 1，定義差（附註 C）|
| 被引用來源 ObjectID 空值 | 0 | **0** | 符 |
| 被引用來源 SW/HW/System | （未給）| SW 165／System 2／Out of Scope 1 | 補 |
| CFTS004 未引用 FR | 50（另 Out of Scope 13）| **50**（另 Out of Scope **13**）| 符 |
| Layer 3 標題數 | 43（R/W 23／I-O 15／Routine 5）| 總 **63**（R/W 38／I-O 15／Routine 10）；**被 037 引用 42**（R/W 23／I-O 14／Routine 5）| 差 1，定義差（附註 D）|
| REF 檔數 | 17（xlsx 13、docx 4）| **17**（xlsx 13、docx 4）| 符 |
| MANIFEST 既有同體 | ≥ 8 | **恰 8** | 符 |

**§9 之「§6 任一列數項差 > 2 筆」未觸發** —— 唯二差異各為 1，且皆為定義差非資料差。

### 附註

- **A（標題前綴）**：13 個具名前綴 ＋「無前綴 14」合計 **385**，不足 395。
  餘 **10 列**為標題含連字號／冒號而被切出長偽前綴者
  （如 `Valid values for treble tone control step shall be from`）。
  下放包 §6 之表亦合計 385，故**分析層之母體與本包一致**，非新差異。
  該 10 列與「無前綴 14」同性質（皆為整句式標題），逐列見 `data/row_kind.tsv`。
- **B（Category 原字）**：`Out of  Scope` 之 `of` 與 `Scope` 間為**兩個空格**。
  過濾一律以原字比對，不得寫 `"Out of Scope"`（本包首次量測即因此誤得 0，已修正）。
- **C（Region 變體 24 vs 23）**：分析層之 23 = `All` ＋ 22 種 NAFTA 排列，**未計空值**。
  空值僅 1 列，即 Out of Scope 之 `SYS-RA-DIAG-248`（不產 TC）。正規化後三類數全符。
- **D（Layer 3 43 vs 42）**：分析層之 43 = R/W **被引用** 23 ＋ I-O **全部** 15 ＋ Routine **被引用** 5。
  差之 1 為 **`$5007 - Mid-Range Settings`** —— CFTS004 I/O 母節有該節，**037 未引用**。
  R-DIAG9(c) 條文所書之「43 個」據此應讀為 42（條文本體不改，事實記於 `RULINGS.md` 執行層回報段）。

---

## §4　任務 3 九項量測

逐列 tsv 全部落 `features/diagnostics/data/`（9 檔）：

| 任務 | 產物 | 列數 |
|---|---|---:|
| 3-1／3-4 | `data_quality.tsv`（vm_blank／misspelling／whitespace 逐列）| 51 |
| 3-2 | `source_resolution.tsv`（raw／strip／037 列數／解析／CFTS004 row／ObjectID／錨／Cat／L3）| 168 |
| 3-3 | `cited_source_attrs.tsv`（Category／Region 原值＋正規化／HARMAN／SW-HW／母節／L3／037 列數）| 168 |
| 3-6 | `layer3_assign.tsv`（SWE1 ID／037 row／SYS-RA-DIAG／ObjectID／母節／`$XXXX` 標題／DID／列型態／Cat／Region／HARMAN）| 395 |
| 3-7 | `vehicle_model_region.tsv`（七欄 × PROXI 平台 × `Market_Area` 實測 × 證據格）| 7 |
| 3-8 | `row_kind.tsv`（列型態／標題前綴／標題全文）| 395 |
| 3-9 | `sysad_uds_tables.md` | — |
| 任務 4 | `layer2_assign.tsv` | 395 |
| §7 | `cfts004_uncited_fr.tsv`（50 條未收 FR，含錨與母節）| 50 |
| §2 衍生 | `nrc_coverage.tsv`（230 列 negative／unsupported 之 NRC 值來源）| 230 |

### 3-5　DID 記法盤點

`$XXXX` **27 種 / 232 次**；`0xXXXX` **23 種 / 94 次**；**同列並存 74 列**。
並存之典型為標題用 `$500A`、描述用 `0x500A`
（例 `SWE1-Diagnostics-058`：「audio source selection using DID $500A … request for DID 0x500A」）。
R-DIAG5(a) 之「上半 verbatim 照抄、作者側一律 `$`」對此 74 列必須逐欄判別，
**不得全檔取代**（已寫入 profile §2.1）。

### 3-8　列型態分類

| 型 | 判準（標題關鍵字）| 列數 |
|---|---|---:|
| positive | 不含下列關鍵字者 | **165** |
| negative | `negative response`／`Negative senario`／`negative scenario` | **135** |
| unsupported | `Unsupported service`／`service not supported` | **95** |
| other | — | **0** |

合計 395。母節分佈：R/W **254**／I/O **114**／Routine **27**。

**`other` 為 0 之說明**：下放包預期「other 含 14 列無領域前綴之列」。
實測該 14 列**皆可由關鍵字判型**（如 `SWE1-Diagnostics-048`「negative senario for This DID shall…」
→ negative；`SWE1-Diagnostics-333`「USB service not supported. 2870」→ unsupported）。
「無領域前綴」與「無法判型」是兩件事；前者 14 列（另 10 列整句式，見附註 A），後者 0 列。

### 3-9　SYSAD 抽取 —— **負面結果**

全文詳見 `data/sysad_uds_tables.md`。**SYSAD 無 UDS 位元組序列表、無 NRC 值表。**

量測：`UDS` 6 次、`NRC` **1 次（僅縮寫表）**、`0x7F` 0、`SID` 0、
`sub-function`／`subfunction` 0、`RoutineControl`／`InputOutputControl` 0。

SYSAD 所載之全部 UDS 資訊為 4.2.5 Assumptions 三行：
`0x22 – ReadDataByIdentifier`／`0x2E – WriteDataByIdentifier`／`0x31 – Routine Control`，
外加 `ISO 14229 (UDS)`、`ISO 15765-2 (ISO-TP) over CAN or DoIP`。
唯一之序列表（Table 10）為 **DTC 寫入流程**（`UpdateDTCStatus()` → `onDtcStatus()`
→ `SendProperty()` → `ipc_write()`），而 **037 之 DTC 提及列 = 0**，且 R-DIAG8(b)
已裁 DTC 不另立工作簿 → **該表不進步驟落地**。

**CFTS004 ↔ SYSAD 之 NRC 不一致清單 = 空集**，且其為「一方無表」，
**不是「已比對且一致」**。

**§9 之升級條件 5 未觸發** —— SYSAD 無序列表，故無「同一 DID 之 SID 不一致」可言。
惟 I/O Control 母節（114 列）所需之 `0x2F` **SYSAD 未載、CFTS004 亦未載**，詳見 §7-1。

---

## §5　任務 4 —— framework 草案之驗算

### 5.1 §5 Layer 2 表回填 —— **21/21 逐列吻合，合計 395，零差異**

| # | Test Set 候選 | DID | 母節 | 草案 | 實測 | 判 |
|---:|---|---|---|---:|---:|---|
| 1 | SXM Module Version | `$2843` | R/W | 57 | **57** | 符 |
| 2 | SXM Signal Quality | `$2840,$2841,$2842` | R/W | 72 | **72**（10+35+27）| 符 |
| 3 | SXM Subscription | `$2844`–`$2848` | R/W | 37 | **37**（24+3+3+4+3）| 符 |
| 4 | SXM Package Routines | `$0307,$0309` | Routine | 13 | **13**（5+8）| 符 |
| 5 | GPS Signal Data | `$2812` | R/W | 40 | **40** | 符 |
| 6 | Camera Brightness | `$283F` | R/W | 6 | **6** | 符 |
| 7 | ECU Internal Settings | `$280C` | R/W | 8 | **8** | 符 |
| 8 | Media Hub Config | `$2870` | R/W | 6 | **6** | 符 |
| 9 | Button Interfaces | `$1820,$1821` | R/W | 7 | **7**（2+5）| 符 |
| 10 | Screen Test Pattern | `$1801` | R/W | 4 | **4** | 符 |
| 11 | Audio Output Settings | `$180C` | R/W | 1 | **1** | 符 |
| 12 | Buzzer Control | `$5000` | R/W ＋ I/O | 40 | **40**（R/W 1 ＋ I/O 39）| 符 |
| 13 | Speaker Quadrant Selection | `$5001` | R/W ＋ I/O | 21 | **21**（3+18）| 符 |
| 14 | Audio Tone Settings | `$5002`–`$5006` | I/O | 27 | **27**（9+9+5+2+2）| 符 |
| 15 | Tuner Control | `$5008,$5009,$500C` | R/W ＋ I/O | 22 | **22**（9+6+7）| 符 |
| 16 | Mode Selection | `$500A` | R/W ＋ I/O | 9 | **9**（3+6）| 符 |
| 17 | In Motion Menu Control | `$500B` | I/O | 6 | **6** | 符 |
| 18 | Video Input | `$500D,$5100` | I/O | 5 | **5**（2+3）| 符 |
| 19 | AV Signal Detection | `$0312` | Routine | 10 | **10** | 符 |
| 20 | Audio Measure AC | `$031B` | Routine | 3 | **3** | 符 |
| 21 | Clear Key Sense PIN | `$030A` | Routine | 1 | **1** | 符 |
| | **合計** | | | **395** | **395** | **符** |

草案未涵蓋之 DID：**無**。草案列出而無 037 列之 DID：**無**。
逐列歸屬見 `data/layer2_assign.tsv`。**Layer 2 名稱未改**（名稱歸 Pei）。

### 5.2 §5 散文之兩處更正

**(1) Out of Scope 列之 Layer 2 歸屬**

§5 稱「合計 395（含 -057 Out of Scope 列，歸 #11 `Audio Output Settings`；
該 Test Set 因此為單列）」。

實測：`SWE1-Diagnostics-057`（`SYS-RA-DIAG-248`，CFTS004 r249，ObjectID `4940710`）之
母節為 **Read/Write DID Requirements**、L3 為 **`$1801 - Screen Test Pattern (Read/Write)`**
→ 屬 **#10 Screen Test Pattern**，非 #11。

`#11` 之單列實為 **`SWE1-Diagnostics-056`**（`SYS-RA-DIAG-222`，Category = Functional Requirement，
**會產 TC**）。

後果：
- `#11` 為單列**與 Out of Scope 無關**，其合併判斷不得以「該列不產 TC」為由。
- `#10` 之 4 列中 1 列不產 TC → **實產 3 列**，正落在 §4.1.3 之 `< 3` 邊界上。

**(2) 跨母節之 DID 不只 `$5000`**

§5 稱「#12 `$5000` 同時出現於 R/W 與 I/O 兩母節，依 DID 合一」。
實測被 037 引用之 DID 中，**跨兩母節者共 6 個**：
`$5000`、`$5001`、`$5008`、`$5009`、`$500A`、`$500C`。
「依 DID 合一（Layer 3 仍各自保留母節）」之處置對這 6 個一體適用，草案之聚合已如此為之，
故 §5.1 之列數無誤；僅散文只點名 `$5000` 一個。

（`$500B` 在 R/W 母節另有 `$500B - In-Motion Menu Control (Read)` 一節，**037 未引用**，
故不列入跨母節者。）

### 5.3 §4.1.3 決策測試

| 判準 | 命中 |
|---|---|
| `< 3 TC` | **#11 `Audio Output Settings`（1）／#21 `Clear Key Sense PIN`（1）** |
| `> 80 TC` | **無**（最大 #2 = 72）|

4 列以下但未觸發者：#10（4，實產 3）／#18（5）／#20（3）。

**合併／拆分建議**（詳 `DECISIONS.md` §4）：
- **#11 → 併入 #14 `Audio Tone Settings`**（同為音訊輸出設定域，併後 28 列）。
- **#21 維持獨立** —— §4.2 genuine outlier：Routine 母節下唯一之非 SXM／非 AV 常式，無同域可併。
- 無須拆分。

### 5.4 Layer 3 全表

`data/layer3_assign.tsv` 395 列，欄含母節與 `$XXXX - <title>` 逐字。
被引用 L3 **42** 個（R/W 23／I-O 14／Routine 5）；CFTS004 總數 63，未被引用 21（逐列見同檔反查）。
Layer 3 **不入工作簿**（§4.1.5）。

---

## §6　profile 骨架（任務 5）

`docs/runtime/profiles/FW036_R1L_Diagnostics_Profile.md`（161 列）已寫出，含：

- §1 lint `P`(v4)、`X` 啟用（R-G71）；**並如實記載 `lint036.py --profile` 只作真值使用、
  不讀 profile 內容**，故 §2–§6 之 [ADD] 必須另行實作才會生效 —— **本包一項未實作**。
- §2 [ADD] R-DIAG5：`$XXXX` 敘述式（含 lint `P` 之白名單需求，否則 27 種 token 全誤報）
  ＋ UDS 位元組式（每 byte 兩位 hex、空格分隔、大寫；`7F` 後兩 byte 必接 `(<label>)`）。
  **§2.3 標 `[BLOCKED]`** —— 見 §7-1。
- §3 [ADD] R-DIAG1(a)：Requirement ID 欄單值（含 `-340-001/-002` 形式）。
- §4 [ADD] R-CAM2 Vehicle Model 1/0（條文逐字承接）＋ 本 feature 之 Region 填法表。
- §6 [ADD] Remarks 之 `Harman Scope:`／`Out of Scope`／VM blank 三種固定句式與其母體。
- §7 未決項六項之去向。

---

## §7　DECISIONS [PROPOSED]（下放包 §8-7）

全文見 `features/diagnostics/DECISIONS.md` §1–§6。摘要：

### 7-1　**[PEI] R-DIAG5(c) 之 NRC／SID 適用空白 —— 阻斷 CDD-02 pilot**

| 項 | 母體 | CFTS004 有值 | 僅 037 有值 | 兩者皆無 |
|---|---:|---:|---:|---:|
| NRC（negative 135 ＋ unsupported 95）| **230** | 5 | 20 | **205（89%）** |
| I/O Control SID `0x2F` | **114** | **0** | 24 列明載 | — |

證據：
- CFTS004 全 404 列中含 `negative response` 者僅 **7** 列，帶值者 **5**
  （`$22, Conditions Not Correct` ×2 @ r32/r321；`"Request Out of Range", code $31` ×3 @ r267/r271/r372）。
- 037 之 negative／unsupported 為共用定型句，措辭為
  「shall respond with an appropriate negative response code (NRC)」—— **不帶值**；
  僅 20 列寫成 `Service Not Supported (NRC $11)` 形式。
- CFTS004 全文 `0x2F`／`$2F` **零命中**（僅四處 `I/O Control` 字面）；SYSAD 亦無。
  唯一載明者為 037 自身之 24 列（`0x2F (InputOutputControlByIdentifier)`）。

依 R-DIAG5(c) 字面，**205 列 NRC ＋ 114 列 I/O SID 全數 `PENDING: DR-{n}`**，
`test_procedure` 之 `7F <SID> <NRC> (<label>)` 與 positive response 位元組串皆無值可寫，
profile §2 之 lint 亦無從啟用（無值可檢）。

**這不是資料缺件，是條文之適用空白** —— 值在 037 內明載且無歧義，只是 037 非 CFTS004。
三個出口（DECISIONS §2 甲／乙／丙），執行層建議 **(甲)**：
R-DIAG5(c) 增列「037 所載之 SID／NRC 值視同原文，其與 CFTS004 衝突時以 CFTS004 為準」。
可解 I/O SID 全部 114 列與 NRC 20 列；餘 185 列 NRC 仍 PENDING，
需 (乙) 之 ISO 14229 例外或 DR 回覆方能全解。

登記：`[A-DIAG01]`、`DR-DIAG-1`（DATA_REQUESTS #1，Urgency **HIGH**）。

### 7-2　**[PROPOSED]** Region → Vehicle Model 歸屬表

不以車名推定（同 R-CAM5(c) 之作廢理由）。依 PROXI `Market_Area`（byte 160、bit 0–2）逐平台實測：

| Vehicle Model 欄 | PROXI 平台 | `Market_Area` | 解 | 證據格 |
|---|---|:-:|---|---|
| HDCC27 Atl-Hi | `HDCC28_ATL_HI` | `0` | NAFTA | `Sheet1!A677` |
| DT27 Atl-Hi | `DT28_ATL_HI` | `0` | NAFTA | `Sheet1!A677` |
| VF(ProMaster)637 Atl-Mi | `Promaster_ATL_MI` | `0` | NAFTA | `PROXI Write_Read!F682` |
| Commander (598) / Regengade (5210) | （無 PROXI）| — | — | R-CAM2(b) |
| Toro(2261) Atl-Mi | `Toro_ATL_MI` | `2` | **LATAM** | `PROXI Write!F682` |
| Fastack (376) Atl-Mi | `Fastback_ATL_MI` | `2` | **LATAM** | `PROXI Write_Read!F682` |

填法提案：`All`（89 源／**212 列**）→ `1 1 1 0 0 1 1`；
`NAFTA` 系（78 源／**182 列**）→ `1 1 1 0 0 0 0`；空值 1 源即 Out of Scope 列，不產 TC。

兩項保留事項：
1. `Toro_ATL_MI` 之 `Market_Area` 於 PROXI Type 欄記為 `Not Used`（其餘五本為 `Table`）。
2. **598／5210 之前例衝突**：R-DIAG7(b) 引 R-CAM2(b)「一律 0」，
   但已交付之 **SWC 0708 工作簿實測填 `1`**（285 列中 202 列）——
   其將 598／5210 視為 Atl-Mi 群成員。R-CAM2(b) 明文限「Camera 兩本」且晚於 SWC 0708 兩個月。
   **本包不自行仲裁**，上表仍依 R-DIAG7(b) 字面填 `0`。登 `[A-DIAG04]`。

### 7-3　**[PROPOSED]** 單列 Test Set 合併

見 §5.3。#11 併入 #14；#21 維持獨立。

### 7-4　**[PROPOSED]** Out of Scope 列之處置欄位名 —— **已實測，非 [PEI]**

R-DIAG3 稱「execution 依 SWC 0708 之對應欄位實測後定」。本包已取得並實測 SWC 0708
（`10_Reviewing/00_TestCase/ASW-R1/SWC/FM-WI-FSM-036-A01 …_SWQT_SWC_20260708.xlsx`，
`Test Case Specification 測試用例規範`，表頭 r9，34 欄，資料 r10–r295）。

**[PROPOSED: 處置欄 = `AF` `Test Result 測試結果`]**，依據：
- `AF` 是 r7 之 `Test Result 測試結果` 區塊（`AB`–`AH`）中唯一記載「執行結論」之欄；
  `AG` 為缺陷號、`AH` 為備註（R-DIAG3 已指定 `AH` 寫 `CFTS004 Category: Out of Scope`）。
- `下拉選單` 分頁僅一欄 11 列，**全部是 `R` Test Case Design Methods 之值域**；
  `AF` 無下拉限制，寫入 `Out of Scope` 無值域衝突。
- SWC 0708 之 `AF` 全欄空白（尚未執行），無既有用法可牴觸。

**附帶實測**：SWC 0708 之欄位對照與 `feature.yaml` 樣板（R-G48 母本實測值）**完全一致**
（`D`/`G`/`H`/`I`/`J`/`K`/`L`/`M`/`N`/`O`/`P`/`R`/`S`/`AA`/`AH`），可為 CDD-02 之副本實測基線。

---

## §8　DR 與 RD 回饋現況

### 8.1 DR（本包新登）

| DR | 內容 | 阻斷 | 狀態 |
|---|---|---|---|
| **DR-DIAG-1** | NRC／SID 值之權威來源（205 列 NRC ＋ 114 列 I/O SID）| **是** —— 阻斷 negative／unsupported 全部批次（230 列）；positive 165 列不受影響 | **PENDING**，Urgency HIGH |
| DR-DIAG-2 | SYSAD 之 UDS 序列補件（現本無序列表、無 NRC 表）| 否 | PENDING，Urgency MED |

下放包 §7 稱「無阻斷性 DR」—— **本包實測後不成立**，DR-DIAG-1 為阻斷性。

### 8.2 RD 回饋草稿四項之現況

| 項 | 下放包所載 | 本包實測 | 狀態 |
|---|---|---|---|
| FB-DIAG-a | `-340` 重號 | **確認**：row 347 `SYS-RA-DIAG-264`（→ `$500D` Video Input Settings）／row 348 `SYS-RA-DIAG-188`（→ `$280C` ECU Current Internal Settings）| 可送 |
| FB-DIAG-b | `-189～192` VM 空白 | **確認**：037 row 196–199 | 可送 |
| FB-DIAG-c | `-057` 引用 Out of Scope 條目 | **確認**：`SYS-RA-DIAG-248`，CFTS004 r249，ObjectID `4940710`，L3 `$1801 - Screen Test Pattern (Read/Write)`，Region 亦空白 | 可送 |
| FB-DIAG-d | CFTS004 50 條 FR 未收 | **確認 50**：清單已產 `data/cfts004_uncited_fr.tsv`（母節分佈 R/W 36／I-O 6／Routine 8；另未引用 Out of Scope 13）| 可送 |

**新增建議項 FB-DIAG-e**：037 之 negative／unsupported 定型句（230 列）未載 NRC 值，
為 DR-DIAG-1 之根因。若一併回饋 Nik，可於 037 下一版根除，優於逐列 PENDING。

### 8.3 ANOMALIES（本包登記 5 項）

`[A-DIAG01]` NRC／SID 適用空白（PENDING，阻斷）／
`[A-DIAG02]` `R-DIAG` 前綴與錨點正則不相容（PENDING，升級）／
`[A-DIAG03]` SYSAD 無 UDS 序列表（PENDING，不阻斷）／
`[A-DIAG04]` 598／5210 前例衝突（PENDING）／
`[A-DIAG05]` SWC 0708 自身欄位錯位 10 列（記錄，非本 feature 產物，不修）。

### 8.4 自報錯誤

1. **Category 字串誤判**：首次量測以 `"Out of Scope"`（單空格）過濾，
   得「被引用之 Out of Scope = 0、未引用 Out of Scope = 0」，與 §6 預期不符。
   原字為 `Out of  Scope`（**雙空格**）。已修正並複驗（1／13，與預期相符）。
   **教訓**：Category 值一律以原字比對，不得重打。已記於 §3 附註 B。
2. **`sysad_uds_tables.md` §1 初稿誤述**：初稿寫「I/O Control 母節之 SID SYSAD 未載」，
   未同時查 037。補查後 037 有 24 列明載 `0x2F`，已更正為
   「SYSAD 未載、CFTS004 亦未載、僅 037 載明」，並改列為條文適用空白而非資料缺件。
3. **profile §4 表之 037 列數初填錯誤**：初稿依「89 源／78 源」推得 217／177，
   未實際按列統計。實測為 **212／182**。已更正。
4. **scaffold ABBR**：`new_feature.py` 產出 `[A-DInn]`，與 R-DIAG9(a) 之 `DIAG` 不符，
   已於本 feature 兩檔改為 `[A-DIAGnn]`；**未改腳本**（全域工具）。

---

## §9　升級（§9 條件命中）

### 9-1　逐項核對

| # | §9 條件 | 實測 | 觸發 |
|---:|---|---|:-:|
| 1 | 037 sha ≠ `b985e09ac42cece7`；CFTS004 REF sha ≠ `ffcb202fff92007e` | 兩者皆符 | 否 |
| 2 | `new_feature.py` 因 `features/diagnostics/` 已存在而拒絕 | 成功建立 | 否 |
| 3 | 037 來源解析率 < 100% | 168/168 = 100% | 否 |
| 4 | §6 任一列數項差 > 2 筆 | 唯二差異各為 1，且皆定義差 | 否 |
| 5 | SYSAD 之 UDS 序列與 CFTS004 對同一 DID 之 SID 不一致 | SYSAD 無序列表，無從不一致 | 否 |
| 6 | **任何需改 canon 或全域 ledger／工具之情形** | **命中 ×2** | **是** |

### 9-2　**升級項 (1)：`R-DIAG` 四字母前綴不被全域錨點正則接受**

`scripts/ruling_anchor.py` 之 `RE_ANCHOR` 為 `R-[A-Z]{0,3}\d+`，**上限三字母**：

```
### R-DIAG1 — x            -> NO MATCH
### R-DIA1  — x            -> R-DIA1
### R-SEC15 — x            -> R-SEC15
```

後果：`python scripts/rulings_hash.py --target features/diagnostics/RULINGS.md`
回報 **「0 錨點」**，九條 R-DIAG 無法進 `docs/fw036/RULINGS.sha.tsv`，
**R-G52 之 `R-XX@<sha8>` 引用制對本 feature 失效**
（下放包引 sha8，執行層無從自 tracked 表查證）。

全 repo 既有 **18 種前綴皆 ≤3 字母**：
`R-PMH 153／R-VF 135／R-VS 97／R-TM 88／R-G 71／R-ICS 65／R-C 40／R-DD 31／R-VL 29／R-SEC 28／
R-VT 27／R-POP 27／R-CAM 15／R-AM 7／R-PH 3／R-U 1／R-P 1`。`R-DIAG` 為首例。

**本包未改該正則**。三個出口（DECISIONS §6），執行層建議 **(甲)**：
放寬為 `R-[A-Z]{0,4}\d+`。影響面單一 —— `ruling_anchor.py` 之 `RE_ANCHOR` 為
`rulings_hash.py` 與 `canon_refs.py` 之共同來源（R-G43(d)：解析面與指紋面同源），
一處改即全面生效；既有 18 種前綴全在新範圍內，**無回歸風險**。
(乙) 改本 feature 條號為三字母（與下放包 §2 所書不符，九條須改號）；
(丙) 維持現狀，九條不入指紋表。

### 9-3　**升級項 (2)：R-DIAG5(c) 之適用需增補條文**

見 §7-1。出口 (甲)(乙) 皆為**修改 R-DIAG5(c) 條文本體**，屬 Pei 專有。
本包未改條文，僅登 `[A-DIAG01]` 與 DR-DIAG-1，並於 `RULINGS.md` 執行層回報段記載其適用結果。

### 9-4　未觸發但須記明者

- **R-DIAG9(c) 條文所書之「43 個」與實測 42 不符**（§3 附註 D）。條文本體未改。
- **下放包 §5 散文兩處與實測不符**（§5.2）。Layer 2 聚合本身無誤，21/21 吻合。
- **下放包 §7「無阻斷性 DR」不成立**（§8.1）。

---

## §10　產物清單

| 路徑 | 說明 | 入 git |
|---|---|:-:|
| `features/diagnostics/`（scaffold 8 檔 ＋ 6 目錄）| 其中 `RULINGS.md`／`DECISIONS.md`／`ANOMALIES.md`／`DATA_REQUESTS.md`／`feature.yaml` 已填 | 是 |
| `features/diagnostics/data/*.tsv`（9 檔）＋ `sysad_uds_tables.md` | 逐列量測產物 | 是 |
| `docs/runtime/profiles/FW036_R1L_Diagnostics_Profile.md` | profile 骨架 161 列 | 是 |
| `sources/MANIFEST.tsv` | 224 → 235 行；8 列 `features` 欄追加 | 是 |
| `sources/raw/<doc_id>/`（11 目錄）| R-G66 原檔落點 | 否（gitignore）|
| `_intake/Diagnostics/`（11 檔）| 投遞區 | 否（gitignore）|
| `docs/fw036/handoff/up/20260917_CDD-01.md` | 本檔 | 是 |

**git 未動** —— 未執行 `git add`／`commit`／`push`。建議 commit message：

```
feat(diagnostics): CDD-01 — scaffold, source registration, Phase 0-1 recon
```
