# 上繳包 VS-SL-11 —— R-VF129 撤回加註

日期：2026-09-02　層級：Tier 1 → Tier 2
對應下放包：`down/20260902_VS-SL-11.md`（協定：`docs/fw036/handoff/PROTOCOL.md`）
**工作簿零改動**（assert 見 §3）；只動 `features/vehicle_setting/RULINGS.md` 之 `R-VF129` 條，
依 R-TM13 **加註不刪**。未動 `down/`／`up/`、`sandbox/`、`.bak` 鏈、對照表、MANIFEST、`output/`。

---

## §1　產物與 SHA256

| 檔 | bytes | SHA256 | 變更 |
|---|---:|---|---|
| `features/vehicle_setting/RULINGS.md` | 383,514 | `ed30a34af91ce0cd4b564d319187e552b78016ebfb91992a5a266ad5794eaede` | **+10 行，0 刪除** |

`git diff --numstat` 實測：`10  0  features/vehicle_setting/RULINGS.md` —— **純新增**。
本輪之改動僅此一檔（`forms/FORMS.md` 之改動屬併行作業線，非本包）。

---

## §2　§1　加註內容與落點

逐字轉錄自下放包 §1，落於 `R-VF129` 條之**執行層註之後**
（即該條原文與其「本層不實寫，待 Pei 之裁定」之註記皆保留在前，撤回加註接於其後）：

```text
> **撤回（分析層 2026-09-02，VS-SL-10 審閱 §2）**：本案為分析層 2026-08-25
> 之裁定草案，執行層當時明載「不實寫，待 Pei 之裁定」，從未生效、從未施行。
> 茲撤回，理由三：
> (1) 位階 —— Pei 之 R-VF83 但書二（2026-09-02）明文修訂該條，即確認其在效；
> (2) 論證缺失 —— 本案「事實前提為假」之論證正文在簿上未落（轉錄止於「實測：」），
>     無從審據，不可重建、不得補造（IN §8.4.1）；
> (3) 實測反證 —— VS-SL-10 §4 之二本量測（repo 237／交付路徑 243 → 起點 244）
>     與 R-VF114 之導出相符。
> R-VF83（含二但書）與 R-VF114 維持在效。原文依 R-TM13 保留不刪。
```

**效果**：`R-VF83`（含但書一、但書二）與 `R-VF114` 之在效狀態自此無歧義；
VS-SL-10 §7-1 所列之待令**即由本加註結清**，本層無其他待決條文。

---

## §3　自檢

| # | 項 | 結果 |
|---|---|---|
| 1 | 三本工作簿 SHA **不變** | **PASS**（`aed08b4d…`／`a0331aa6…`／`f9bbeffc…` 前後逐本相同，`diff` 空） |
| 2 | `git diff` 僅 `RULINGS.md` 一檔且純新增 | **PASS**（10 新增／0 刪除） |
| 3 | 原文未刪（R-TM13）—— `R-VF129` 之條文與其執行層註仍在 | **PASS**（加註接於其後） |
| 4 | `gate_all.py` 增紅 | **0**（見 §4-1） |
| 5 | `output/`／`sandbox/`／`.bak`／對照表／MANIFEST 未動 | **PASS** |

---

## §4　自報

1. **`gate_all.py` 無增紅**　`canon_refs` 維持 **509**：逐檔還原法
   （copy 至工作區、按來源目錄分層；未觸 `down/`／`up/`）實測 ——
   還原 `RULINGS.md` 後計數**仍為 509**，本輪 **+0**。
   `lint_paths` 維持 **4**（`driver_distraction/workbook/` 兩檔＋ICS／SWUpdate `delivered/` sha，
   皆與本線無涉）；`rulings_hash`／`gates_tsv` 同前。

2. **`rulings_hash` 待重生清單再 +1，累計九處**（承 VS-SL-06 §5-4 起之登記）：
   `R-VS89`／`R-VS90`／`R-VS91`／`R-VS92`／`R-VS93`／`R-VS94`／`R-VS95`
   ＋ `R-VF83`（但書二）＋ **`R-VF129`（撤回加註）**。
   本層仍未重跑 —— 重生指紋屬治理動作，且會與該支既有之紅混在一起。

3. **本輪無自造缺陷。** 單檔單處加註，一次到位。

---

## §5　機器側收斂

本包為 VS-SL 線機器側之最後一件。其後之三項**皆非執行層可自行推進**：

| 項 | 待 |
|---|---|
| DR 寄出＋取號回填 `PENDING: DR-{n}` | **Pei**（`features/vehicle_setting/reports/dr_draft_vssl.md`，四節可直接轉寄） |
| 出貨（`output/` → `delivered/`） | DR 結案或 Pei 裁降轉（IN §8.4.3 之 PENDING gate） |
| `rulings_hash` 九處指紋重生 | 該支既有之紅結清（其成因非本線） |

**VF665 隨時可開**：配方在 `docs/runtime/profiles/FW036_R1L_VehicleSetting_Profile.md` 之
`[ADD] Settings 查找配方`，TC ID 依 `R-VS94` 自 **`NR1L-VS-439`** 起編。
