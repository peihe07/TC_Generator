# 上繳包 VS-SL-10 —— R-VF83 但書二落檔＋具名缺號登記

日期：2026-09-02　層級：Tier 1 → Tier 2
對應下放包：`down/20260902_VS-SL-10.md`（協定：`docs/fw036/handoff/PROTOCOL.md`）
**未動任何工作簿內容**（案 (A) 之要義即零改動，assert 見 §5）；
未動 `down/`／`up/`、`sandbox/`、各 `.bak` 鏈、對照表、MANIFEST。

---

## §1　產物與 SHA256

| 檔 | bytes | SHA256 | 變更 |
|---|---:|---|---|
| `features/vehicle_setting/RULINGS.md` | 382,763 | `af4deefa924acc1ffc31b3dd1336b1e07b26a69c88db4d09b6f53fdf6bf0a6ba` | +30 行（R-VF83 但書二） |
| `features/vehicle_setting/ANOMALIES.md` | 210,691 | `cd050801a1cb0c658722e5e460016541fee496b8f90d4428dcfa87acaf04e107` | +69 行（`A-VS172` 及其複驗） |

`git diff --stat`：2 檔、**99 行新增、0 刪除**（R-TM13：不刪除、加註）。

---

## §2　§0　但書二落檔

逐字轉錄自下放包 §0，落於 `RULINGS.md` 之 `R-VF83` 條下、但書一之後，
並依但書一之先例附 sha 註記：

- 但書二前之 sha8 **`beba78c6`**（`docs/fw036/RULINGS.sha.tsv` 實測，body_sha8 `ebfa087c`）
- 但書一前之 sha8 `9d5bfa4d`（既載）
- 歷史 handoff／upstream 以舊值所作之引用**一律不動**（26 包 §A 先例）
- `rulings_hash` 本輪未重跑；**本但書之指紋與 `R-VS89`–`R-VS95` 七條一併待該紅結清時重生**

**適用效果**：vf230 重排後 B 欄 54 處逆序，依但書二**不再違反 `R-VF83`**；
序號集合之完整性另由但書一之具名缺號程序保之（§3）。

### ⚠ 與 `R-VF129` 之關係未決（**本層不自裁，具名待裁**）

落檔時實測 `RULINGS.md` 尚有 **`R-VF129`**（分析層裁定 2026-08-25，L6689）：

```
R-VF-甲（B 欄模型之撤銷，分析層裁定 2026-08-25）
**`R-VF83`（VF230 之 B 欄自 238 起）與 `R-VF114`（B 欄起始號由實測導出，
取二本 CFTS044 末號之最大 +1）**，其**事實前提為假，一併撤銷**。
```

其執行層註逐字為「**本層不實寫，待 Pei 之裁定**」—— **該撤銷未曾施行**。

本層之處置：但書二為 **Pei 2026-09-02 之裁**，時點在後且明文修訂 `R-VF83`，故依令落檔；
**惟 `R-VF129` 之撤銷仍在簿上且未經 Pei 處置**。二者關係（撤銷是否隨本裁而失效、
或本裁是否即默示推翻該撤銷）**本層不自裁**，已於 `R-VF83` 條下具名，待令。

> 另記：`R-VF129` 之逐字轉錄於簿上**於「**實測**：」後即結束**，其論證正文未落，
> 讀之無從得知「事實前提為假」所指為何。一併具名。

---

## §3　§1　具名缺號登記

`A-VS172`（主線最大實測 `A-VS171`，取 +1）：**19 個具名缺號 `634`–`652`**（連續）。

| assert | 結果 |
|---|---|
| 19 號互異 | **PASS** |
| 皆在 `244`–`700` 內 | **PASS** |
| 與現簿 B 值集合不相交 | **PASS** |

### 值域完整性複驗（但書二之判準）

| 項 | 值 |
|---|---:|
| 值域 `244`–`700` | 457 |
| 現簿 B 值 | 438 |
| 具名缺號 | 19 |
| 438 ＋ 19 | **457** |
| **除具名缺號外仍懸空之號** | **0** |

**PASS** —— 值域無洞。

### B 值之來源與包內字面不同（自報）

包內 §1-1 令自 `.bak` 鏈（`.presl07.bak` 或 sandbox v3 稿）取 B 值。
**該二稿皆為移除後之 438 列**：其第 400–418 列裝的是位移後之他列內容 ——
本層逐列實測其 D 值與 `vf230_removed_non_nafta.tsv` **相符 0／19**，自其取值必取到錯號。
故改自**移除前之原稿** `features/vehicle_setting/inputs/…_SWQT_VF230_20260819.xlsx`
（457 列，D 值 **19／19 相符**）取。

| 稿 | 列 | 含 19 原列號 | D 值相符 |
|---|---:|:--:|---:|
| `inputs/…VF230_20260819.xlsx`（移除前） | 457 | 是 | **19／19** |
| `sandbox/vssl/vf230_vssl.v3.bak.xlsx` | 438 | 是（但為位移後之他列） | **0／19** |
| `output/…VF230_20260902.presl07.bak.xlsx` | 438 | 同上 | **0／19** |

---

## §4　§2　238–243 六號之查證（只查不猜）

**結論：238–243 不是缺號，無須登記。其為 Part 1（CFTS044）交付路徑本所佔用。**

依 `R-VF114`（分析層裁定 2026-08-24）「取 repo 內與交付路徑**二本** CFTS044 之 B 欄末號，
取其最大 +1」，本層逐本實測：

| 本 | 列 | B 範圍 | 缺口 | sha256 前 16 |
|---|---:|---|---|---|
| repo 內 `features/vehicle_setting/inputs/…CFTS044…20260819.xlsx` | 237 | `1`–`237` | 無 | `ebe5a65f30a0d4bc` |
| **交付路徑** `…/ASW-R2/Vehicle Settings/CFTS044/…20260819.xlsx` | **243** | `1`–`243` | 無 | `f39c03bd9cb0f1b3` |

`max(237, 243) + 1 = 244`，即現行起點。
包內 §2 所附之「CFTS044 交付本 B 末值 **237** 之複核」——
**237 為 repo 內那本之末值；交付路徑本為 243**。
`A-VF33` 所載「CFTS044 交付本末號 243 + 1」指的是**交付路徑本**，本層複驗成立。
**故無「未登記之缺號」，不觸發包內之停手條件，亦未自行補登。**

---

## §5　自檢

| # | 項 | 結果 |
|---|---|---|
| 1 | 三本工作簿 SHA **不變**（(A) 零改動） | **PASS**（`aed08b4d…`／`a0331aa6…`／`f9bbeffc…` 逐本前後相同） |
| 2 | 19 號互異／皆在 244–700／與現簿不相交 | **PASS**（三項） |
| 3 | 序號集合值域無洞（438 ＋ 19 = 457） | **PASS** |
| 4 | 但書二逐字轉錄、落於 R-VF83 條下、未刪除既有文字 | **PASS**（99 行純新增） |
| 5 | `gate_all.py` 增紅 | **0**（見 §6-3） |
| 6 | 對照表／MANIFEST／`sandbox/`／`.bak` 鏈未動 | **PASS** |

---

## §6　自報

1. **B 值來源改自 `inputs/` 原稿**（§3 末）——包內所指之 `.bak` 鏈為移除後之稿，取值必錯。

2. **`R-VF129` 之撤銷未決**（§2）——本層依 Pei 之新裁落檔但書二，
   **不自行判定該撤銷是否失效**；另記其逐字轉錄在簿上不完整。

3. **`gate_all.py`：`canon_refs` 510 → 509（下降 1），其餘三支同數；本輪 +0**
   逐檔還原法（copy 至工作區、按來源目錄分層；未觸 `down/`／`up/`）實測：
   還原本輪二檔後計數**仍為 509**，故本輪 **+0**。
   計數之下降出於本線之外之併行作業（vsm 線於期間有落檔）。
   `lint_paths` 維持 **4**、`rulings_hash`／`gates_tsv` 同前。

4. **`rulings_hash` 仍未重跑**：待重生之指紋現為 **`R-VS89`–`R-VS95` 七條 ＋ `R-VF83`（但書二）**，共八處。

---

## §7　待令

1. **`R-VF129` 與 `R-VF83` 但書二之關係**（§2）：撤銷是否隨本裁失效？
2. DR 寄出取號後回填 `PENDING: DR-{n}`（vf230 25 列、VC 20 列、BL `Body_Types` 行 151）
3. 出貨：待 DR 結案或裁降轉（IN §8.4.3）
