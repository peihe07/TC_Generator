# 上繳包 CMF-06 — 交付候選本（封面／履歷／Product Document ＋ 三份附件）

- 日期：2026-09-22
- 對應下放包：`down/20260922_CMF-06.md`（前置 `down/20260922_CMF-05_review.md`）
- 性質：裁定落帳、交付候選本、交付附件三份。
  **未入 `delivered/`、未 commit、未跑 `--gate`／`--regen`、未重跑 generator。**
- 開工前 `git status`：**CMF-05 產物已提交**於 `13001e1 feat(comfort): CMF-05 — vehicle-model columns, DR-46 closed, actual names`（30 檔）。開工時工作區內未追蹤檔為 `down/20260922_CMF-05.md`、`down/20260922_CMF-05_review.md`、`down/20260922_CMF-06.md`（皆分析層下放），三者已隨該 commit 入版控。
- R-G62(b) 之觀察依據：起點前 `features/comfort/` 之最後他方寫入為上列 commit（同日稍早）；本輪期間未觀察到他方寫入。**「無他 session 寫檔」不可自證。**
- R-G62(c)：`up/20260922_CMF-06.md` 落檔前已證該路徑不存在。

---

## 0. 首段

| 項 | 結果 |
|---|---|
| **對 v4 差異格數** | **11**，全數歸因：封面類分頁 **10** ＋ `J5` **1**。**TC 分頁之車型欄與內容與 v4 逐格相同** |
| 對 0907 本 | 不符 **0**／21,309 格；變更 **3,465 格**全數歸因 |
| **留空三格** | `ChangeHistory 修訂履歷!E8`（核准者）、`Cover 封面!G7`（核准者日期）、`Cover 封面!G8`（審查者日期）—— 產出檔實測仍為空 |
| TC 總數 | **476**（不增減）；403 leaf 全覆蓋，含 5 條 BLOCKED 佔列 |
| LINT | JSON 層 **57/57 PASS、476 TCs、0 finding**；交付本層 **H=0 W=0 J=0 P=0 L=0 U=0**。`LINT_STATUS: OK` |
| 產物 | `features/comfort/sandbox/delivery/…_SWQT_ComfortHMI_20260922.xlsx`，sha256 `c2ddf95d…`，242,916 bytes |

**三件必須先看**：

1. **R-C67 未套用 —— 其涵蓋面被低估 38 倍**（§2）。條文書「影響僅 2 列 × 3 格」，
   實測為 **112 列／229 格**，三台皆 0 之 TC 會由 65 升為 **177（占 476 之 37%）**。
   且該條自書「**未經 Pei 逐字確認**」。我不以一個未確認之假設在交付本上做這麼大的改動。
   **這是本包唯一未照做的一項**，回填只需一個指令。
2. **`-403`／`-404` 又是工作簿 F**（§2）。其 req 為 `116-03`／`116-04`（非條文所書之 `121-xx`），
   且**已為 V=Y=Z=0**，無格可改。**A-CMF19 之同一坑，第二次**。登 A-CMF24。
3. **§3-4 新列列高與樣式：做不到，已報**（§3-4）。`backend/xlsx_surgical` 之寫入路徑依設計
   只 patch 儲存格**值**；另開 XML 編修路徑會破壞本 repo 之單一寫入路徑紀律。
   量測值已落 `docs/reports/new_row_style_measure.tsv`，Excel 內兩個動作可完成。

**另一件本輪自行修掉的**：`surgical_save` 把 `datetime` 序列化為**字串** ——
若不處理，交付本之四個日期格會變成文字型日期（Excel 不認）。改寫 **Excel 序列值**後，
四格讀回皆為 `datetime`。見 §3-5。

---

## 1. 裁定落帳（CMF-06 §1）

落帳前實測現行最大號為 **R-C65**，暫編＝實編。條文逐字取 CMF-06 §1 表（R19-2）。

| # | 落實 |
|---|---|
| **R-C44(confirm)** | 落帳。476 條定案，13 條候選不產 —— 本輪無新動作 |
| **R-C65(amend)** | 落帳（DBC 舉證範圍 ＝ 該 TC 車型欄為 1 之車型）。追認 CMF-05 §4-1 之讀法 |
| **R-C60(amend)** | 落帳（合稱增列 `Face + Feet`）。追認 CMF-05 §5 之處置 |
| **R-C66** | **DR-49 已登記**（`DATA_REQUESTS.md`，標 open，問句逐字取 CMF-02 §5-2）；導航步驟**一行未改** |
| **R-C67** | **未套用** —— 見 §2。登 **A-CMF24**，並依其明令於 `ANOMALIES.md` 記為「待確認之假設」 |

`ANOMALIES.md` 另落 **A-CMF22／A-CMF23**（逐字取 CMF-05_review §1）。
sha8 未產（GC-16 未落地）。**profile 本輪未改** —— 無新的 profile 面裁定。

---

## 2. R-C67（EMEA ICS）—— 未套用，逐條回報

### 2-1. 條文所指之兩條不是它以為的那兩條

CMF-06 §2 寫「`121-xx` 正向 EMEA ICS 之 2 條（`-403`／`-404`）：V／Y／Z 改 0」。實查交付本：

```
row 413   F = NR1L-ComfortHMI-403   D(Requirement ID) = SWE1-HVAC-116-03   V=0 Y=0 Z=0
row 414   F = NR1L-ComfortHMI-404   D(Requirement ID) = SWE1-HVAC-116-04   V=0 Y=0 Z=0
```

- `-403`／`-404` 是**工作簿 F**，其 req 為 `116-03`／`116-04`，**不是 `121-xx`**。
- 兩列**已為 V=Y=Z=0**（其 PC 帶 `PROXI Rear_Climate = 1 (Present)` → V／Z＝0；2.14 → Y＝0），
  **無格可改**。
- 依 **A-CMF19** 於 CMF-04_review 自己定下的記法「跨層指涉一律以 `req_id` 為準」，
  這是**同一個坑的第二次**。登 **A-CMF24**。

### 2-2. R-C67 若實際套用，涵蓋面是 119 條不是 2 條

R-C67 之判準為「本交付三台皆非 EMEA ICS 車」。凡 PC 帶
`The vehicle is an EMEA ICS vehicle, whose climate interface is specified in chapter 16` 者
皆須三台全 0。實測：

| | 條數 |
|---|---|
| 帶**正向** EMEA PC 之 TC | **119** |
| 其中今為 (V,Y,Z)=(1,0,1) | 107 |
| 其中今為 (1,1,1) | 5 |
| 其中今已 (0,0,0) | 7 |
| **需改之列／格** | **112 列／229 格** |
| **三台皆 0 之 TC** | 65 → **177**（占 476 之 **37%**） |

（另 307 條帶**否定**式 EMEA PC，其條件於三台皆成立，不受影響。）

### 2-3. 為什麼不套

- R-C67 **自書**「此為分析層依 Pei『照你建議』所取之讀法…**未經 Pei 逐字確認**」。
- 其對影響面之估計（2 列／6 格）與實測（112 列／229 格）差 **38 倍** ——
  作出該裁定時所根據的規模與實際不同，裁定人未必是在對這個規模說「可以」。
- 套用後之交付本會宣告 **37% 之 TC 於本輪三台皆不執行**。這是交付面的重大陳述，
  不該由一個具名未確認之假設帶進去。
- **回填成本低**：判準已機械化（PC 含該句者三台全 0），`vm_assign.tsv` 與交付本
  可於一個指令內重產。Pei 一句話即可。

**車型欄因此維持候選本 v4 之值**（V 407／Y 21／Z 409，三台皆 0 者 65）。
`vm_assign.tsv` 表頭已加四行註記此事。

---

## 3. 交付候選本（CMF-06 §3）

### 3-1. 封面類分頁之量測（§3-2「先量兩本同名分頁」）

**`SWC 0708` 交付本不在本 repo** —— `find` 全庫只得 `SWC 20260628`／`SWC 20260529`
（`docs/Report/compare/06_SWC/`）。故以 **SWC 20260628** 代之，並加量 **Home 20260809**、
最近之 **Popup 20260908** 與母本 **Comfort 20260907**，共四本。逐格結果：

| 分頁 | 四本之一致性 | 本輪之處置 |
|---|---|---|
| `Reference` | **四本 35 格完全相同** | **不動** |
| `ChangeHistory 修訂履歷` | 三本（SWC／Popup／Comfort）之 A5–E7 為**範本之三列履歷**（A／B／C，記欄位變更，作者張愷霏、核准劉安哲），完全相同；**Home 0809 例外** —— 它把 row 7（版本 C）**覆寫**成自己的交付說明 | 依 §3-2「**追加一列**」：新增 row 8，不覆寫既有三列 |
| `Cover 封面` | 四本皆有 C4／C6／D6／G6／C7／D7／C8／D8／C9／C12／H30。**G7／G8（核准者／審查者日期）只有 Home 0809 有值**；SWC／Popup／Comfort 皆空 | G7／G8 **留空**（與最近三本一致，且屬 §3-2 之「留給 Pei」） |
| `Product Document 記錄封面頁` | 四本皆有 A1–B7、A11–D12、A13；**B4／B8／C13／D13 只有 Comfort 0907 與 Popup 0908 有值** | 依既有形制追加 row 14 |

### 3-2. 實際寫入之 11 格

| 分頁 | 格 | 值 | 依據 |
|---|---|---|---|
| `Cover 封面` | G9 | `2026-09-22`（datetime） | §3-2；與 J5 一致 |
| `ChangeHistory 修訂履歷` | A8 | `D` | §3-2「版本依 0907 本之現行版號 +1」（C → D） |
| 同 | B8 | 雙語一句（見下） | §3-2 |
| 同 | C8 | `PeiPYHsu` | §3-2「作者 PeiPYHsu」 |
| 同 | D8 | `2026-09-22`（datetime） | §3-2 |
| `Product Document 記錄封面頁` | B8 | `2026-09-22`（datetime） | 與 Cover 一致 |
| 同 | A14 | `V1.1` | 0907 本之 A13＝`V1.0` +1 |
| 同 | B14 | 同一句 | §3-2 |
| 同 | C14 | `許珮瑜 PeiPYHsu` | 與 C13 同形 |
| 同 | D14 | `2026-09-22`（**字串**，見 §3-5） | §3-2 |
| `Test Case Specification` | J5 | `2026-09-22` | §3-3 |

B8／B14 之一句（逐字）：

```
車型欄依 PROXI 逐條填值、DR-46 結案（CAN 訊號改用實際名與值）、Test Set 名稱修正、新增 sibling 10 條
Filled the vehicle model columns per PROXI configuration, closed DR-46 by using the actual CAN signal names and values, corrected the Test Set names, and added 10 sibling test cases
```

### 3-3. 留空之三格（§4 所要求之位置）

| 分頁 | 格 | 欄義 | 為何留空 |
|---|---|---|---|
| `ChangeHistory 修訂履歷` | **E8** | 核准者 Approver（新列 D） | §3-2「Approver 留空給 Pei」 |
| `Cover 封面` | **G7** | 核准者日期 | 0907 本即空；四本中只有 Home 0809 有值 |
| `Cover 封面` | **G8** | 審查者日期 | 同上 |

**一處讀法請核**：§3-2 寫「Approver／Reviewer／**中文作者**三格留空給 Pei」。
`Cover!D7`（核准者 劉安哲）、`D8`（審查者 張愷霏）、`D9`（作者 許珮瑜 PeiPYHsu）在 0907 本
**都已有值**。我把「留空」讀為「**不填新值**」而非「清除既有值」—— 清除會丟掉母本已有的資料，
且 §3-2 同時要求「照 SWC／Home 兩本填」，兩者相衝時我取不破壞。
故實際留空者為上表三格（皆為母本本來就空、且需人判斷者）。腳本另加一道 assertion：
這三格於母本非空即中止。

### 3-4. 新列 476–485 之列高與樣式 —— **做不到，已報**（§3-4 之明文允許）

- **原因**：`backend/xlsx_surgical.surgical_save()` 之 `diff_cells()` 只比對並寫入
  **儲存格值**；列高（`<row ht=…>`）與樣式（`<c s=…>`）不在其 patch 範圍，
  且母本之 476–485 為範本空白列（`ht=14.0`、style 86/87/89/92）。
  另開一條 XML 編修路徑會破壞本 repo 之**單一寫入路徑**紀律
  （`tests/test_single_write_path.py` 所守），不為此一項而破。
- **量測已附**：`docs/reports/new_row_style_measure.tsv`
  - 資料列 10–475 之列高分布：`112.0 ×131`、`140.0 ×76`、`126.0 ×70`、`98.0 ×65`、
    `84.0 ×34`、`154.0 ×32`、`70.0 ×19`、`168.0 ×13`（**眾數 112.0**）
  - 新列現為 `height=14.0, customHeight=True`
  - **22 欄之 style id 與資料列不同**（逐欄列於該檔，例 `L`：資料列 81 vs 新列 86）
- **Excel 內兩個動作**：選取 row 476–485 →（1）列高設 112；（2）以 row 475 為來源「複製格式」。

### 3-5. `datetime` 之序列化（本輪自行查出並修掉）

初版把四個日期格寫成 `datetime`，產出檔讀回全是**字串** `'2026-09-22 00:00:00'` ——
`surgical_save` 之 XML 寫出把 `datetime` 當一般物件轉字串，落到 Excel 即**文字型日期**。
改寫 **Excel 序列值**（`to_excel(2026-09-22) = 46287.0`）後，三格（`Cover!G9`、`ChangeHistory!D8`、
`Product Document!B8`）讀回皆為 `datetime`，且沿用各自既有之 number_format。

**一格例外**：`Product Document!D14` 之 number_format 為 `General`（範本空白列），非日期式；
寫序列值會顯示成 `46287`。故該格寫**字串** `2026-09-22`。
其上一列 `D13` 之格式為 `m"月"d"日"`，兩列呈現因而不同 —— **具名於此**，
Pei 於 Excel 內把 D14 設為與 D13 相同之格式即可。

### 3-6. invariant 與逐格對帳

| invariant（★ ABORT 級） | 結果 |
|---|---|
| ★ zip member 數 | 57 ＝ 57 |
| 差異僅限本包所改之**四個** sheet xml | `sheet3`（Cover）／`sheet4`（ChangeHistory）／`sheet5`（Product Document）／`sheet6`（TC）—— 與預期集合逐字相同 |
| ★ DV sqref（classic ＋ x14） | 逐字相同 |
| B 欄公式 row 10–601 | 逐列原樣 |
| 留空三格於產出檔仍為空 | PASS |
| 寫入格讀回 == 意圖值 | **0 格不符** |
| **對 0907 本逐格對帳** | **不符 0 格**／21,309；變更 **3,465 格**全數歸因 |
| **對候選本 v4 逐格對帳** | 差異 **11 格**，未歸因 **0** |

對 v4 之 11 格：`CMF-06 §3-2 封面類分頁` 10、`CMF-06 §3-3 J5` 1 —— **與 §3-5 之預期一致**
（§3-5 原寫「§2 之 6 格 ＋ 封面類分頁 ＋ J5」，而 §2 之 6 格實為 **0 格**，見 §2-1）。

### 3-7. `Test Case Framework` 之 SmartArt —— 只報不改（Tier 3）

該分頁儲存格全空，內容只有一個 SmartArt（`drawing7.xml`），其 19 個標籤中仍含
**`HVAC Pop-ups`** 與 **`EMEA ICS Interface`** —— 即 M8 已於 JSON／工作簿修掉之兩個錯誤 Test Set 名
（現為 `Climate Popups`／`ICS Climate Modes`），另 `Climate Control Interface`、
`Front Climate Anatomy` 各重複一次。**本輪未改**（§3-3 明令只報）。

---

## 4. 交付附件（§3-6）

| 檔 | 內容 |
|---|---|
| `docs/reports/coverage.tsv` | **403 leaf → TC** 之對映，403 列。欄：`req_id`／`outline`／`tc_count`／`workbook_rows`／**`workbook_tc_ids`**／**`json_tc_ids`**／`vehicle_V_Y_Z`／`blocked`。合計 **476 TC**，**5 條 BLOCKED**（`080-02`、`081-02`、`044-02`、`072`、`019-03`）。**兩套號碼並列**，正是 A-CMF19／A-CMF24 那個坑的解 |
| `docs/reports/dr_summary.tsv` | DR-46 **CLOSED**（附結案依據）；DR-47／48／49 **open**，各載其阻塞面與問句 |
| `docs/reports/rd_feedback.md` | 六項供 Pei 送 RD：①`2.1` tabs／Massage 與 037 不一致；②`019-03` VF HVAC 缺件；③037 之 VC 前提（DT/HDCC）與 Pei 裁定（Alt-Mi）相反；④**上游三套命名而無對照表**（需求 `$…$`／CFTS `$…$`／DBC 與 PROXI 實際名，附實測 0 命中之證據）；⑤`§19`／`§20`／`§21` 未被 037 引用；⑥DR-47/48/49 之摘要 |

---

## 5. 自檢

- **JSON 層**：`lint_tcs.py` **57/57 PASS、476 TCs、0 finding**（本輪 JSON 無改動）。
- **交付本層**：`H=0 W=0 J=0 P=0 L=0 U=0`，其餘 `M=10 R=1486 I-cross=443 X=337`
  —— **與候選本 v4 完全相同**（本輪對 TC 分頁只改 `J5` 一格）。
- **可重現**：重跑 `cmf06_delivery.py` 得**相同 sha256** `c2ddf95d…`、`MISMATCH_COUNT: 0`。
- **`canon_refs.py --report --waiver`**：工作區 **35／178**；以 `git worktree`（**未用 `git stash`**）
  取 HEAD(`13001e1`) ＋ 本下放包之基線，同為 **35／178** → **本包 0 筆**。
- **全庫 pytest**：HEAD 基線 10 failed／1,366 passed；工作區 **9 failed／1,389 passed**；
  **只在工作區失敗者 0 項** → 無回歸。
- `DELIVERY.sha256` 追加 **ENTRY 041**（type `delivery-candidate`）；
  `shasum -a 256 -c --ignore-missing` **62 OK、0 FAIL**。
- **唯讀紀律**：`down/`／`up/` 內無任何檔被移動、改名或覆寫。

### 自報

1. **R-C67 未套用**（§2）—— 本包唯一未照做者，理由與回填成本已具名。
2. **「留空三格」之讀法**（§3-3）：我取「不填新值」而非「清除既有值」。
3. **`Product Document!D14` 是字串日期**（§3-5），與同欄 D13 之呈現不同。
4. **`ChangeHistory` 版號取 `D`**：0907 本之 A5–A7 為 `A`／`B`／`C`，該三列記的是**範本欄位之變更**
   （新增欄位、重新命名 O 欄），不是本文件之交付履歷。我依 §3-2「現行版號 +1」取 `D` 並追加一列，
   但**這條履歷的語意與前三列不同類**（前三列是表單改版，本列是內容交付）。
   Home 0809 之做法是覆寫 row 7；我沒有覆寫。若分析層認為應另起一個履歷區或不記於此，請指正。
5. **`Product Document` 之 `B5`（版本）仍為 `Initial Release`** —— 0907 本如此，而 SWC／Home／Popup
   三本皆為 `V1.0`。本次是第二次交付，該欄是否應改為 `V1.1`，**未改、只報**（§3-2 未提及此格）。
6. **`SWC 0708` 以 `SWC 20260628` 代量**（§3-1）—— 檔不在 repo。若 0708 之封面類分頁與 0628 不同，
   §3-1 之結論要重驗。

---

## 6. 待分析層／Pei

1. **R-C67**（§2）：三台是否皆非 EMEA ICS？是 → 我重跑，119 條全 0（三台皆 0 升至 177）；否 → 回哪一台。
2. **Cover 之 D7／D8／D9 是否要清空**（§3-3 之讀法）。
3. **`ChangeHistory` 新列之版號與位置**（自報 4）；**`Product Document!B5`** 是否改 `V1.1`（自報 5）。
4. **`Product Document!D14`** 之日期格式（自報 3）—— 由 Pei 於 Excel 設定。
5. **新列 476–485 之列高與樣式**（§3-4）—— 由 Pei 於 Excel 兩個動作完成，量測已附。
6. 沿未決：`ENTER_COMFORT` 入 canon §5.3（Pei 改原檔）、DR-49 送出（R-C66 之前置）。

---

## 7. 產物與 SHA256

| 路徑 | sha256 |
|---|---|
| `features/comfort/sandbox/delivery/…_SWQT_ComfortHMI_20260922.xlsx`（**交付候選本**） | `c2ddf95dcbe1656ef6f16746243fec7e89a0f0dd9f4be27e76d5814df57dbebf` |
| `features/comfort/scripts/cmf06_delivery.py`（新增） | `dfd0b6a87f43a2da9118c45ce987a61eb0ff22ef73c98dbee5de2a7ff10f3d44` |
| `features/comfort/docs/reports/coverage.tsv`（新增） | `cf406376ec2c3e67dc93e691c4b58b24d84c5fa21c6600793837e660850f0d8f` |
| `features/comfort/docs/reports/dr_summary.tsv`（新增） | `498d32a4ac137d4e650e6098a6c457ff3dec087a8444987cef0afb294e049fc1` |
| `features/comfort/docs/reports/rd_feedback.md`（新增） | `a119c9d156a35cef2eb29082fe5e5dc7902e76c0b3e8afcf15cee61696d1b078` |
| `features/comfort/docs/reports/new_row_style_measure.tsv`（新增） | `573d629d9617d8f37f48ee41270cd8625cc4df12e4811b1e83b546e7c236c3cf` |
| `features/comfort/docs/reports/cmf06_delivery_cell_recon.tsv` | `b2cbeba86542d0c3d4f72f6f5fbfa6745a74c61725e19998a63100ca94ee9469` |
| `features/comfort/docs/reports/cmf06_vs_v4_cell_delta.tsv` | `e0c808056bf85361045a9ba10450693d1542042b7f6ac810be2c81a329711af7` |
| `features/comfort/docs/reports/vm_assign.tsv`（加 R-C67 註記） | `766893fc20200a5c44362246eb2f2e5cc38b2f890ec114df397caefa3a95f416` |
| `features/comfort/sandbox/delivery/lint/LINT_STATUS.txt` | `be5f26d60659d6258d5b2df8a27612b0274b3cc5a984666aa1e508715373d95a` |
| `features/comfort/RULINGS.md` | `f8e9ef78f714bb2725e81a6e0576acaeee8e0959d829751f04041afebc5f91f8` |
| `features/comfort/ANOMALIES.md` | `49d0715dcb0f6700f0ffee3fb4d109daaa34b2660c2c8a0323f41d6f257813b4` |
| `features/comfort/DATA_REQUESTS.md`（DR-49 登記） | `b9975281a0194a6fa49662e0bb3436fc1ae2761ae0d14f23e02c4715161158b3` |
| `features/comfort/DELIVERY.sha256`（追加 ENTRY 041） | `105f1259fd693178eeb644ea40eb33b3b9edafc64731f63eff7d8f817dd473c8` |

**JSON 語料本輪未改動**（476 TCs，與 ENTRY 040 同）。**old→new：無**。
