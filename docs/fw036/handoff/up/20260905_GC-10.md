# 上繳包 GC-10 —— GC-07 版本裁定後之矯正、GC-09 續行

**執行層 session：B**（`up/20260905_GC-09_notice.md` 之作者）。
**R-G62(b) 之聲明**：本包施作區間為 **21:40:41 – 21:47:04**，
其間本 session 為唯一寫檔者 —— 依據：起點前之最後一次他方寫入為
`docs/fw036/RULINGS.sha.tsv` **21:31:46**（執行層 A），距起點 8 分 55 秒；
施作期間逐檔複驗 mtime 皆為本 session 之寫入。
**惟本聲明只能證「未觀察到」**，不能證他方未寫（R-G11 盲區）——
互斥須由派工端保證，見 6-4 節之請裁。

日期：2026-09-05　層級：Tier 1 → Tier 2　對應下放包：`down/20260905_GC-10.md`

---

## §1　產物與 SHA256（前 16 碼）

| 節 | 檔 | bytes | SHA256 |
|---|---|---:|---|
| 2-1 | `docs/fw036/INDEX.md` | 6,136 | `3d0c68cf4d03810b` |
| 2-1 | `docs/fw036/handoff/PROTOCOL.md` | 3,034 | `3d2cdb92ed8fb991` |
| 2-1／2-4 | `docs/fw036/ANOMALIES.md` | 8,763 | `0e9f8d224d35ad9b` |
| 2-2／2-3 | `docs/fw036/RULINGS_LEDGER.md` | 104,946 | `1ada4a140b901cd1` |
| 2-2 | `docs/runtime/ASPICE_SWE6_AI_Instruction.md` | 49,687 | `ffb21c128b397b53` |
| 2-2 | `scripts/lint036.py` | 53,022 | `aa5304ba4b4a9015` |
| 2-2 | `docs/runtime/GATES.tsv` | 12,391 | `7526d8c1a4135fc5` |
| 2-5 | `docs/reports/rg72_candidates_20260905.tsv` | 613 | `f04021614508b77e` |

工作區合計 **22 檔，+768 / −372**（含執行層 A 未提交之部分）。

**工具旗標逐次聲明（R-G50(d)）**：`gates_tsv.py` 無旗標 **2 次**（准寫 `GATES.tsv`，GC-08 授權）；
`gates_tsv.py --check` 3 次、`rulings_hash.py --check` 1 次、`lint036.py`（唯讀，`--report-dir` 指工作區）5 次、
`gate_all.py`／`canon_refs.py --waiver` 唯讀。**除 `GATES.tsv` 外未寫任何 repo 內之產生器輸出。**

---

## §2　任務一：先量（改任何字之前）

### 2-1（1-1）基準

`git log --oneline -5` 之首為 `d23d69a`（GC-07，執行層 A 已提交）。起點 mtime：

| 檔 | mtime | 意義 |
|---|---|---|
| `ASPICE_SWE6_AI_Instruction.md` | 21:25:33 | GC-09 §一-1-2 已由 A 做 |
| `RULINGS_LEDGER.md` | 21:27:03 | GC-09 §一-1-1 已由 A 做 |
| `scripts/lint036.py` | 21:28:02 | GC-09 §一-2-2 已由 A 做 |
| `tests/test_lint036.py` | 21:28:27 | 同上 |
| `RULINGS.sha.tsv` | 21:31:46 | **A 已重生並寫入 repo 內之表** |

`git status --short` 起點：20 個 modified、23 個未追蹤。

### 2-2（1-2）台帳與指紋

`### R-G70` 於 `RULINGS_LEDGER.md:1791`、`### R-G72` 於 `:1836`（皆已落錨）。
`RULINGS.sha.tsv` **768 列**，`R-G70`／`R-G71`／`R-G72` **三號皆已在表**。

> **矯正形式因而定為「加註撤銷改 v4.1」**（GC-10 §一-2 之分岔：已指紋 → R-TM13 加註）。

起點 (e) 逐字（`:1807-1808`）：`PROXI $<Param>$ is set to "<值>"`（VF230）為標準、SWC 式為舊式
—— **與 Pei 裁定相反**。

### 2-3（1-3）IN 與 lint 現況、GC-09 完成度

`IN §8.7.5` 標題已為 `（R-1 v4 …，R-G70）`，(e) 之例句為 `PROXI $Vehicle_Line_Configuration$ is set to "124 (DT)"`（VF230 向）。
`scripts/lint036.py:118` 之 `RE_P_PROXI` 為 `\bPROXI\s+(\$[^$]+\$|[A-Za-z][A-Za-z0-9_]*)\s*=`
—— **兩式皆吃，且該常數全檔無使用處**（`grep` 命中僅定義行）：即**當時根本沒有 PROXI 檢查**。

GC-09 七任務完成度（起點）：

| 任務 | 狀態 |
|---|---|
| 1-1 台帳 R-G70 | A 已做（方向錯）|
| 1-2 IN §8.7.5 v4 | A 已做（方向錯）|
| 1-3 profile override 作廢 | A 已做（`FW036_R1L_VehicleSetting_Profile.md`、`vehicle_setting/RULINGS.md` 皆 M）|
| 1-4 乾跑 | A 已做，且**已寫 repo 內之 `RULINGS.sha.tsv`**（逾 GC-09「不寫表」之明文）|
| 2 lint P | A 已移除 v3 分支；**PROXI 判準未實作** |
| 3 pm_29 dry-run | A 已出 `features/power/reports/rg70_pm29_dryrun_20260905.tsv`（未追蹤）|
| 4 候選表 | **未做**（`rtm14_`／`rg72_` 皆不存在）|
| 5 FO 接手清單 | **未做**（`FEATURE_ONBOARDING.md` 內 `R-G71` 命中 0）|
| 6 DR 機讀狀態欄 | **未做**（`status: open\|sent\|closed` 命中 0）|

### 2-4（1-4）B 線盤點包與 A-GC 編號

`down/` 內之 `20260905_GC-08*.md` 為**本線（B）之包**，非另一盤點包；
A 另出 `docs/reports/gc08_inventory_20260905.tsv` 與 `scripts/gc08_inventory.py`（未追蹤）。

`ANOMALIES.md` 起點：`A-GC15`（GC-06 之未量測因果，本線所落，mtime 先）、
`A-GC16`（Home 計數＋lint 代號）、`A-GC17`（例示未查 HMI）—— 後二者由 A 依
`features/power/DATA_REQUESTS.md` 之 standing rule「不覆寫、改取次一可用號」所落，
並已於檔內具名其偏離。

---

## §3　任務二-1：版本標示

- `down/20260905_GC-07_review.md`（B 版）**未動**（PROTOCOL 目錄唯讀）。
- `INDEX.md` 新增 **3-1 節**：載明該檔為 21:22:48 覆寫之版、Pei 裁定無效、
  有效審閱為 `_A` 版，並給引用判準。
- `ANOMALIES.md` 登 **A-GC18**（唯讀目錄被同名覆寫；含「已入台帳之 R-G70(e) 取自 B 版」之後果）。
- `PROTOCOL.md`「目錄唯讀」節增 **R-G62(c)**：`write_file` 前須 `get_file_info` 證路徑不存在，
  已存在即改名（`_A`／`_B`）不覆寫；並註明其與執行層側 R-G62(b) 之對應。

---

## §4　任務二-2：PROXI 方向 → SWC（v4.1）

### 4-1　台帳

`R-G70` (e) **原二行加 `~~ ~~` 保留**，其後書 **v4.1**：
`PROXI <Param> = <值>`（SWC）為標準；VF230 式為同義舊式，既有本不回修（R-TM13），
回修依 R-G72；lint P 對舊式報 **WARN**。
另加一則方向更正註，載明成因（條文取自 B 版審閱、GC-09 所引之「審閱 §一 (d)」不存在）、
指向 `up/20260905_GC-09_notice.md`，並聲明 (a)(b)(c)(d)(f)(g) 與沿革段**未動**。

### 4-2　IN §8.7.5 (e)

改為 v4.1，例句改 `2. PROXI Vehicle_Line_Configuration = 124 (DT)`；
VF230 式列同義舊式；附 v4.0→v4.1 更正註。

### 4-3　lint036：**新增 PROXI 檢查**（原本沒有）

| 形態 | 判 |
|---|---|
| `PROXI <Param> = <值>`（SWC）| **合規**（無違規）|
| `PROXI $<Param>$ is set to "<值>"`（VF230）| **`Y`（WARN 只報不改）** |
| 兩式皆不中而含 `PROXI` | **`P`（FAIL 類）** |

`Y` 為**新增之 profile 專屬檢查**（`PROFILE_CHECKS` 末位，`CHECK_ORDER` 不變 ——
既有八本之非 profile 基線因而完全不動）。其 `CHECK_STATUS` 逐字比照 `X` 之
「**WARN 只報不改**」形態，並註明「既有交付本不回修（R-TM13），回修依 R-G72」。

**逐式直驗**（合成三行，非取語料）：

```
PROXI Vehicle_Line_Configuration = 124 (DT)                 -> 合規
PROXI $Vehicle_Line_Configuration$ is set to "124 (DT)"     -> Y（PROXI 舊式）
PROXI something weird here                                  -> P（形態不合亦非既知舊式）
```

`GATES.tsv` 隨之重生：**56 → 57 閘**（新增 `Y`），`gates_tsv --check` **OK**。

> **具名之設計選擇**：GC-10 §二-2 只寫「`is set to` 式 WARN（舊式）」，未指定其落點。
> 本包以**新增 profile 檢查 `Y`** 實作，理由：`P` 為 FAIL 類，混入即無法只報不改；
> 而 repo 既有之 WARN 形態就是 `X` 那一支。**此為執行層之判斷，請覆核**；
> 若分析層要求併入 `P` 並以狀態文字區分，改一處即可。

---

## §5　任務二-3／二-4／二-5

### 5-1　R-G72 增 (f)、R-TM14 名稱作廢

`R-G72` 於 (e) 後增 **(f) 候選判準**：交付向 `X` 或 `P` 紅數 > 0 者為候選，逐本由 Pei 圈選；
**`Y`（WARN）不單獨構成候選**，惟已因 X／P 入選者其 Y 併入轉換範圍；
候選表落 `docs/reports/rg72_candidates_<YYYYMMDD>.tsv`。
另加註：**「R-TM14」之名作廢** —— `R-TM` 為 time_management 線之系列前綴，全域條以之取號會撞名；
凡引 R-TM14 者一律改指 R-G72。

### 5-2　A-GC 編號：**與下放包不同，具名**

GC-10 §二-4 令「B 版 A-GC15→A-GC17、A-GC16→A-GC18，A-GC16 留給覆寫事件」。
**實測起點**：A 已把該二條落為 `A-GC16`／`A-GC17`（同一「先者保號」原則）。
若照令再改一次，等於把已落之二條**二度改號**，而 GC-10 §二-4 之依據正是「mtime 先者保號」。

**本包之處置**：`A-GC15`／`A-GC16`／`A-GC17` **維持現狀不動**，
覆寫事件登**次一可用號 `A-GC18`**。共同形態句由「十七筆」改「十八筆」。
**此為對下放包之偏離，請分析層裁**；要照原令改號，本包之三行註記即為改號依據。

### 5-3　GC-09 續行

| 任務 | 本包處置 |
|---|---|
| 1-1／1-2／1-3 | **矯正**（4-1／4-2）；1-3 之 profile 作廢由 A 完成，本包驗收未改 |
| 1-4 乾跑 | A 已逾「不寫表」直接寫 `RULINGS.sha.tsv`（2-3 節）—— 本包**未再寫**；現行表與矯正後之條文**不符**（見 6-1） |
| 2 lint P | **矯正並補實作**（4-3）|
| 3 pm_29 dry-run | **未驗收**：A 之表產於 PROXI 方向矯正前，其「PROXI 若已 SWC 式則不列」之判當時反向；**須重跑**（6-3）|
| 4 候選表 | **新做**（5-4）|
| 5 FO 接手清單 | **未做**（6-3）|
| 6 DR 機讀狀態欄 | **未做**（6-3）|

### 5-4　`rg72_candidates_20260905.tsv`

母體：`git ls-files "features/*/delivered/*.xlsx"` → **5 本**。逐本以 `--profile <feature>` 跑，
報告落工作區（**repo 內未新增報告檔**）。

| feature | sha8 | rows | X | P | P_warn(Y) | candidate |
|---|---|---:|---:|---:|---:|---|
| ics_management | `cb5697db` | 31 | 29 | 0 | 0 | **Y** |
| power (`pm_29`) | `35305835` | 389 | 237 | 40 | 0 | **Y** |
| power (`pm_73`) | `cedb6b84` | 287 | 103 | 45 | 0 | **Y** |
| sw_update | `4f6caa35` | 319 | 159 | 0 | 0 | **Y** |
| vsm_v42 | `abc7f8ae` | 17 | 6 | 0 | 0 | **Y** |

**五本全為候選**（皆 X > 0）。`Y` 全為 0 —— VF230 之 152 列舊式**不在交付本內**
（VF230 落 `features/vehicle_setting/output/`，非 `delivered/`），故本表之 Y 欄為 0 屬預期，
非「無舊式」之證。**圈選屬 Pei**（R-G72(f)）。

---

## §6　自檢與自報

| # | 項 | 結果 |
|---|---|---|
| 1 | 被覆寫之 `down/` 檔未動 | **PASS**（只加註於 INDEX）|
| 2 | 台帳原句加刪除線保留，未就地改寫 | **PASS**（指紋已生，依 R-TM13）|
| 3 | `RULINGS.sha.tsv` 本包未寫 | **PASS**（僅 `--check` 1 次）|
| 4 | 候選表之報告落工作區 | **PASS**（repo 內未新增 lint 報告）|
| 5 | pytest | 紅 **10 → 8**、綠 1314 → **1319** |
| 6 | `ruff`（`lint036.py`）| 19 → 19，**+0** |
| 7 | `gates_tsv` | **PASS（57 閘）** |
| 8 | 工具旗標逐次聲明（R-G50(d)）| **PASS**（1 節末）|

### 6-1　`gate_all`：`rulings_hash` 轉紅，成因明確

| 閘 | 值 | 歸因 |
|---|---|---|
| `canon_refs` | 158 | 本包 +0 |
| `rulings_hash` | **FAIL** | 本包改了 `R-G70`(e)、`R-G72`(f) 之本體，而 `RULINGS.sha.tsv` 是 **A 於 21:31:46 依舊本寫的**。**須重生一次**（GC-10 §三-1 已預留）|
| `gates_tsv` | **PASS（57）** | 本包新增 `Y` 閘並重生 |
| `lint_paths` | 6 | 本包 +0（2 為 driver_distraction 未搬、4 為 A 之 `.json` 報告；後者之政策衝突已由 GC-08 審閱裁定 GC-10 施作 —— **本包未做**，見 6-3）|

pytest 之紅由 10 降為 8：`test_gates_tsv::test_check_detects_drift`（GC-08 已修）與
`test_lint036::test_check_order_extends_only_with_profile` 皆轉綠，後者因 `Y` 加在
`PROFILE_CHECKS` 而 `CHECK_ORDER` 未動，正合該測試之不變式。

### 6-2　自報：候選表首版全零，交付前抓到

首版之報告解析取「`|` 後第一個數字」，而統計表之欄序為
`| 檢查 | 項目 | 行計 | 列計 | 粒度 | 校準 |` —— 數字在第 3 欄。
於是五本 X／P／Y **全被讀成 0**，`candidate` 全為 `N`。
**若照此交付，Pei 會據一張「無候選」的表停止回修** —— 而實測五本全是候選。
改以逐欄切分後重跑得 5-4 節之值。形態：**把「解析成功」當成「解析正確」**，
全零未觸發任何警覺 —— 若非五本皆零之巧合太整齊，可能就漏過去了。

### 6-3　未做且具名

| 項 | 為何未做 |
|---|---|
| GC-09 §一-2-3 八本基線 P 對照向 | 需逐本跑既有八本並比對報告；本包時間用於方向矯正與候選表，**未做** |
| GC-09 §一-3 pm_29 dry-run 之重跑 | A 之表產於方向矯正前，須重跑；**未做** |
| GC-09 §一-5 FO 接手清單、§一-6 DR 機讀狀態欄 | **未做** |
| GC-08 審閱 §一-1 之 `lint_paths` 落點表增 `reports/` | GC-10 §二未列，但 GC-08 審閱指定 GC-10 施作 —— **本包未做**，`lint_paths` 之 4 個 `.json` 紅因而仍在 |
| `RULINGS.sha.tsv` 重生 | GC-10 §三-1，屬 Pei |
| `A-GC16`／`A-GC17` 之二度改號 | 5-2 節，具名偏離 |

### 6-4　請裁四項

1. **`Y` 之落點**（4-3 節末）：新增 profile 檢查，或併入 `P` 以狀態文字區分。
2. **A-GC 編號**（5-2 節）：維持 A 已落之 `A-GC16`／`A-GC17` ＋ 覆寫事件用 `A-GC18`，
   或照 GC-10 §二-4 再改一次號。
3. **6-3 之五項未做**：是否併入 GC-11，或另包。
4. **R-G62(b) 之可證性**（首行）：本 session 只能證「未觀察到他方寫入」。
   建議下放包載明「本包指派給哪一個執行層 session」，並由派工端在指派前確認前一 session 已停。

---

## §7　補遺（Pei「收尾＋補遺」2026-09-05，同一施作區間之延伸）

三項自 6-3 之未做清單取出補做；其餘二項（八本基線 P 對照向、pm_29 dry-run 重跑）仍未做。

### 7-1　`lint_paths` 落點表增 `reports/`（GC-08 審閱 §一-1 [DEFAULT]）

`scripts/lint_paths.py` 之 `ALLOWED[".json"]` 由
`{generated, data, inputs, sandbox}` 增為 `{generated, data, inputs, **reports**, sandbox}`，
並附五行成因註（R-G47 令報告落 `features/<f>/reports/`，而 `.json` 之政策表漏列該目錄，
於是「照 R-G47 放對地方」與「通過本閘」不可兼得）。
`.md` 不在本表三種副檔名內，原本就不受限。**基線一列未動**。

**實測：`lint_paths` 6 → 2。** 消掉的四紅即 A 於 21:08 產生之四對報告之 `.json` 半；
餘二為 `features/driver_distraction/workbook/*.xlsx`，待 Pei 跑
`scripts/gc06_driver_distraction_mv.sh` 與 `feature.yaml:17` 之同步。

### 7-2　FO 接手清單二項（GC-09 §一-5）

`FEATURE_ONBOARDING.md` §4 末以 `**[ADD] …（R-G71，GC-09 §一-5）**` 加二項：

1. **profile 一律啟用 lint `X` 與 `P`(v4)** —— 並明寫「未啟用時 `P=0`／`X=0` 是沉默不是核可」。
2. **`§5.3` 三常數之 `PENDING` 由該 feature 之 DR 承接** —— 不得複製 `PENDING` 而不登 DR。

### 7-3　DR 機讀狀態欄之格式（GC-09 §一-6）

新增 `docs/fw036/templates/DATA_REQUESTS.md`（1,368 bytes）：
定 `## DR-<n>` 節首行 `status: open|sent|closed（YYYY-MM-DD）`；
值域三選一、缺日期視為未標；**未取號之草稿節不寫 `status:`**（未送出不佔號）。
**既有節一律不回填**（GC-09 §一-6 明文）。

**分子／分母（R-G8）**：十線之 `## DR-` 節共 **96 節，具 `status:` 者 0 節 → 0/96**。
逐線：vehicle_setting 39、time_management 11、vehicle_category 10、driver_distraction 9、
power_moding 8、bed_lowering 5、sw_update 5、vsm_v43 5、vsm_v42 4、power 0。

> `power` 之 0 節係其 DR 記於 `features/power/DATA_REQUESTS.md` 而未用 `## DR-` 標題形態
> —— 該線之形態與其餘九線不同，回填前須先定其對映。**本包只量不改。**

### 7-4　補遺後之閘況

| 閘 | 本包起點 | 補遺後 |
|---|---|---|
| `lint_paths` | 6 | **2** |
| `gates_tsv` | FAIL | **PASS（57 閘）** |
| `canon_refs` | 158 | 158 |
| `rulings_hash` | PASS（A 於 21:31:46 之表）| **FAIL** —— 本包改了 R-G70(e)／R-G72(f) 之本體，須重生（Pei，GC-10 §三-1）|
| `lint_docs036`／`lint_delivery_spec` | PASS | PASS |

### 7-5　補遺仍未做者

| 項 | 為何 |
|---|---|
| GC-09 §一-2-3 八本基線 P 對照向 | 需逐本跑既有八本並比對；本輪未做 |
| GC-09 §一-3 pm_29 dry-run 重跑 | A 之表產於方向矯正前，須在 v4.1 下重跑；本輪未做 |
| `RULINGS.sha.tsv` 重生 | 屬 Pei（GC-10 §三-1）|
| `driver_distraction` 搬移 | 屬 Pei（`git mv`）|
