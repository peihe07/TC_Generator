# 上繳包 CAM-23 —— 補生成 B ＋9、A 本 50 列覆核、selfcheck 第 10 項、B 本 dry-run 212 列

對應下放包：`docs/fw036/handoff/down/20260923_CAM-23.md`（審閱 `down/20260923_CAM-22_review.md` 同發）。
執行層 session：延用。日期 2026-09-23。CAM-21 之 §1／§2 依 CAM-23 §1／§4 恢復並執行。

**一句話**：§1 四項落檔（RDF-17、DECISIONS 6-71～6-77、GCB-12、**selfcheck 第 10 項 WARN**）；
§2 補生成 **9 列**（`b06/`，`NR1L-RVCHMI-198`～`-206`），B 本 TC 由 197 → **206**；
§3 A 本 50 列逐列覆核 → **H 1／M 2／L 47**，追加預估 **4 列**（**< 30，升級條件 §7-1 不成立**）；
§4 B 本 dry-run **212 列**（206 ＋ 6 佔位），**lint ERROR 類全 0**、對帳 212 ↔ 212 合，
**D 欄排序規則唯一決定**（§7-3 不成立）。三項升級條件**皆不成立**。
**A 本十目錄 0 改動**；未寫 `delivered/`、未 tag。

---

## §1　落檔

### 1-1　RDF-17（CAM-21 §1-1）

`features/camera/RD_FEEDBACK.md`（sha256_16 `5f619c0e5a72ab57`，RDF 累計 **17** 條）：
同一動作於兩章異名 —— §30.1.2（R1 High）`“Make Favorite”` vs §34.9.4（R1 Low）`“Edit Favorite”`，
兩句之句型**逐字相同**（`From the AUX Cam settings menu, the user presses “…”`），只按鍵名相異。
另指出 §34.9 側**無**按下之後果說明（§30 側有 §30.1.3～§30.1.6 四列）。

### 1-2　DECISIONS 6-71 ～ 6-77

`features/camera/DECISIONS.md`（sha256_16 `c4ad918ec758eb58`）：

| # | 項 | 來源 |
|---|---|---|
| 6-71 | RDF-16 之處置（時限依 SYS1）| CAM-20 審閱 §一-1（CAM-21 §1-2）|
| 6-72 | 跨兩本之同句不委派 | CAM-20 審閱 §一-2 |
| 6-73 | B 本零 TC 六列補 D 欄；**umbrella 27 列不補** | CAM-20 審閱 §一-5 |
| 6-74 | H/M 六列全數補生成（＋9），**皆為新列** | CAM-22 審閱 §一-1 |
| 6-75 | A 本 50 列逐列覆核，H/M 先回報不生成 | CAM-22 審閱 §一-2 |
| 6-76 | 審計判準固化為 selfcheck 第 10 項（**WARN 級**）| CAM-22 審閱 §一-3 |
| 6-77 | 「1:1 即未拆」撤回；B 本 39.1% 由 037 承擔 | CAM-22 審閱 §一-4 |

### 1-3　GCB-12

`docs/fw036/handoff/GC_BACKLOG.md`（sha256_16 `a8c3f9fb09d3c4ac`）：
pilot 審閱流程加「拆解審計」一步 —— 抽讀 ≤ 4 列可查單列寫法而**查不出拆解充分性**；
建議之機械掃描與已知偽陽清單（影像 token、`limit±1` 之正當寫法、導航 hop）併附。

### 1-4　`selfcheck_camera.py` 第 10 項（WARN 級）

`features/camera/scripts/selfcheck_camera.py`（sha256_16 `69d5da4cf0e5db5f`）。
依 **DECISIONS 6-76** 修兩類偽陽後固化：

```
(a) boundary  改為「limit 與 limit±1 **任一**出現即通過」（A-CA30 之故）
(b) 多觸發    排除 profile §5.3 之導航常數 hop（NAV_HOP，GCB-11 同因）
(c) 母體      Description 全文（profile §7.7）先剝除影像 token（CAM-22 §7-1）
(d) 兄弟列    以 source_object_id 分群，037 已前拆者其軸由兄弟列承接
```

**等級 WARN** —— 列出候選、**不阻上繳**（`sys.exit` 不計入）；其餘十項維持 ERROR 級。

**對全案 446 列跑一次**：ERROR 級十項**全 0**；**第 10 項候選 37 列（8.3%）**。
（A 本 240 ＋ B 本 206 ＝ 446；補生成前之 437 列亦為 37 候選 ——
`-056` 之列舉候選因 `-198`～`-201` 而解消，`-204` 新增一個 `列舉(3>2)` 候選，淨值不變。）

---

## §2　B 本補生成 9 列（`b06/`）

TC ID `NR1L-RVCHMI-198`～`-206`｜計畫 `features/camera/data/b06_plan.tsv`（sha256_16 `ea15fe4db7d663da`）。

| 審計項 | 母列 | 章 | 補列 | 驗證點 |
|---|---|---|---|---|
| §5 #1（H）| `-056` | §27.2.6.1 | `-198`／`-199`／`-200`／`-201` | CHMSL/Cargo、TRG、FFCTL、SVC 各一（PROXI 配備逐一）|
| §5 #2（H）| `-044` | §27.1.1 | `-202` | 無線側之 latching／non-persistent |
| §5 #3（M）| `-033` | §7.5.1 | `-203` | 10 秒窗之**下界**（9 秒仍在），形制同 `-035` |
| §5 #4（M）| `-085` | §34.1.5.1 | `-204` | `Apps Page` 作**被列舉之項** |
| §5 #5（M）| `-109` | §28.6.1.1 | `-205` | `Neutral or Drive` 之 **Drive** 側 |
| §5 #6（M）| `-196` | §6.2.2.3 | `-206` | `or no guidelines` 之 **Off** 側 |

各列 reasoning 皆具名「拆解審計 CAM-22 §5 #n」與母列 TC ID；
`b06_plan.tsv` 之 `note` 欄同記。`coverage_b.tsv`（sha256_16 `a1563082de0cd078`）之
**6 個母 leaf** 其 `tc_id` 欄追加本批 ID、`batch` 欄加 `;b06`：

```
SWE1-RVC-021   b01b;b06   NR1L-RVCHMI-033;NR1L-RVCHMI-203
SWE1-RVC-047-01 b05;b06   NR1L-RVCHMI-196;NR1L-RVCHMI-206
SWE1-RVC-053   b02a;b06   NR1L-RVCHMI-044;NR1L-RVCHMI-202
SWE1-RVC-065   b02a;b06   NR1L-RVCHMI-056;NR1L-RVCHMI-198;-199;-200;-201
SWE1-RVC-101   b03;b06    NR1L-RVCHMI-109;NR1L-RVCHMI-205
SWE1-RVC-133   b02b;b06   NR1L-RVCHMI-085;NR1L-RVCHMI-204
```

**既有 197 列 0 改動**（`git status` 實測）。
lint：ERROR 類全 0（非致命 `U` 1／`I-cross` 9）；selfcheck ERROR 級十項全 0。

---

## §3　A 本 50 列逐列覆核（不生成）

`features/camera/data/a_audit.tsv`（sha256_16 `4b1ead43634c101d`，**240 列全表**，格式同 `b_audit.tsv`）。

| 口徑 | 值 |
|---|---:|
| 機械命中 | 50（20.8%）|
| `confidence = H` | **1** |
| `confidence = M` | **2** |
| `confidence = L` | **47（94%）** |
| **追加 TC 預估** | **4** |

**H／M 清單（先回報不生成，待分析層裁）**：

| # | TC | 來源列 | 軸 | 逐字證據 | 追加 | 判 |
|---|---|---|---|---|---:|---|
| 1 | `NR1L-RVC-017` | `SYS-RA-VF551_V2-496` | 列舉 | any-of 退出條件**四款**：`BCM_FD_10.CmdIgnSts != RUN`／`BRAKE_FD_2.VehicleSpeedVSOSig >= c_VEHSPD_MAX`／`Shift Lever … "Park"`／`RVC_ImageDefeat.Req = Pressed`；該來源列只有 `-017`（點火）與 `-018`（P 檔）**兩個** TC —— **速度**與 **ImageDefeat** 兩支未驗 | **2** | **H** |
| 2 | `NR1L-RVC-201` | `SYS-RA-VF551_V42-226` | 列舉 | 兩個 `The rearview image shall be displayed …` 子句；現有 TC 只驗 `RESPONSE_TIME` 之時限，未驗 `Rear_Camera_Repetition.Data` 之複製 | 1 | M |
| 3 | `NR1L-RVC-214` | `SYS-RA-VF551_V33-234` | boundary | `the setted brightness must be over 50%`；TC 只出現 `50`，無 `49`／`51`（惟亮度量測本即需治具，`bench_verify.md` 已列）| 1 | M |

**判準過寬之結論已可判**（CAM-22 §3 之兩解讀）：
A 本 47/50（94%）、B 本 33/39（85%）為 L，**兩本之機械命中皆以偽陽為主** ——
即 CAM-22 §3 之解讀 **(1)「判準過寬」為真**，非 A 本有缺口。
此亦為 selfcheck 第 10 項訂為 **WARN 級**（而非 ERROR）之實證依據。

---

## §4　B 本 sandbox dry-run

### 4-1　檔

```
features/camera/sandbox/b_dryrun/FM-WI-FSM-036-A01 STLA 測試用例規範與結果_SWQT STLA Test Case Specification & Result_SWQT_RearViewCamera_B_20260923_dryrun.xlsx
sha256  3724671b88fbe17c05ba625cd60a02207499968f370d4ad45c818b09ba0ba1df
大小    56396 bytes｜資料列 212（TC 206 ＋ D 欄佔位 6）
```

### 4-2　D 欄排序規則（升級條件 §7-3）

規則：**`(母號 int, 子號 int)` 升冪，母列（無子號尾綴）排於其子列之前**。

**實測該規則與 037 之列序逐列相同** —— 以 `recon_b_rows.tsv` 之 230 列（037 原序）
對該規則排序後之序列比對，**逐列相等、無一相異**。
故排序**唯一決定**，升級條件 §7-3 **不成立**。
工作簿之 D 欄升冪**成立**（212 列逐列驗）。

### 4-3　6 個 D 欄佔位列（DECISIONS 6-73）

| 工作簿列 | D 欄 | `AH` 註 |
|---:|---|---|
| 101 | `SWE1-RVC-069` | `No TC — source states N/A (A-CA35)` |
| 119 | `SWE1-RVC-086` | `No TC — cross-reference only (A-CA36)` |
| 124 | `SWE1-RVC-091` | `No TC — cross-reference only (A-CA36)` |
| 137 | `SWE1-RVC-102` | `No TC — cross-reference only (A-CA36)` |
| 194 | `SWE1-RVC-136` | `No TC — verbatim duplicate of §34.3.2 (A-CA36)` |
| 197 | `SWE1-RVC-139` | `No TC — verbatim duplicate of §34.4.1 (A-CA36)` |

**umbrella 27 列不補**（6-73：R-G42 一之「有列而無 TC」指 leaf 需求）。

### 4-4　lint 全表

```
行計 A=0 B=0 C=0 D=0 E=0 F=0 G=0 H=0 I=0 I-sibling=0 J=0 K=0 L=0 M=0 N=0
     P=0 Q=0 R=0 T=0 U=44 V=0 I-cross=206 W=0 X=23 Y=0 Z=0
報告 docs/fw036/lint_reports/RearViewCamera_B__camera_3724671b_20260923.md
```

**ERROR 類全 0**（`J` 亦為 0 —— B 本無首字小寫之逐字上半）；`Z` 全綠。
非致命：`U` 44／`I-cross` 206／`X` 23。
`X` 23 列之成因為 **GCB-11**（lint 之固定入口清單未納 profile §5.3 之導航常數），
**非組簿工具所致**，升級條件 §7-2 不成立。

### 4-5　sheet 級問題

| # | 項 | 實測 | 判 |
|---|---|---|---|
| 1 | `N` 欄多行／含空白 | **各 0** | 合規（B 本之錨為單一 `{檔名}_{章節號}`）|
| 2 | `AH` 欄 remarks | **8 列**（`-002`／`-037`／`-038`／`-054`／`-079`／`-084`／`-137`／`-173`），皆 profile §10 之第一、二類 | 合規 |
| 3 | Vehicle Model 七欄 | 206 列全為 `"0"`／`"1"` 字串；**全 0 之列 0**；各欄計 1：HDCC27 206、DT27 206、637 193、598 0、5210 0、Toro 171、376 193 | 合規（598／5210 全 0 符 R-CAM2）|
| 4 | Test Set 八組拼寫一致 | **8 組、各組無變體**：`AUX Camera Access` 61、`AUX Camera Settings` 45、`Activation and Exit` 44、`Camera View Switching` 25、`Warning Banners` 12、`PAM Integration` 9、`Camera Settings` 5、`Wireless Camera Pairing` 5 | 合規 |
| 5 | Test Group | 全 206 列 `Rear View Camera` | 合規 |
| 6 | `C`／`E`／`Q` 欄 | 212 列全空 —— 與 A 本 dryrun2 及交付本 `…SAM_20260917.xlsx` 相同 | 合規（`Q` 留空即 R-G42 四之 `[DEFAULT]`）|

### 4-6　對帳

| 口徑 | 值 |
|---|---:|
| `coverage_b.tsv` 列數 | 230 |
| ├ `tc_id` 相異 | **206** |
| ├ 零 TC leaf | 6 |
| └ umbrella | 27（不入工作簿）|
| `generated/` 八目錄之 json | **206** |
| 差集（兩方向）| **0／0** |
| 206 ＋ 6 | **212** |
| 工作簿資料列 | **212** |
| **判** | **合** |

### 4-7　`pending_by_dr_b.tsv`

`features/camera/data/pending_by_dr_b.tsv`（sha256_16 `36496b45d30446ea`）。
**206 列中 34 列含 PENDING**：

| DR | 列數 | 次數 |
|---|---:|---:|
| DR-CAM-r | 26 | 26 |
| DR-CAM-g | 7 | 14 |
| DR-CAM-h | 3 | 3 |
| DR-CAM-s | 1 | 1 |
| **合計** | **34** | **44** |

其餘 15 條 DR 於 B 本無 PENDING 列。`§7.5 佔位`（`= PENDING (label)`）**0 處**。

---

## §5　全案交付前置清單（CAM-21 §3／CAM-23 §5）

### 5-1　兩本候選

| 本 | 檔 | 列數 | sha256 |
|---|---|---:|---|
| **A** | `sandbox/a_dryrun/…_RearViewCamera_A_20260923_dryrun2.xlsx` | **244**（TC 240 ＋ 佔位 4）| `f194046c923fd85f…` |
| **B** | `sandbox/b_dryrun/…_RearViewCamera_B_20260923_dryrun.xlsx` | **212**（TC 206 ＋ 佔位 6）| `3724671b88fbe17c…` |
| | **全案 TC** | **446** | |

**兩本分交**（**R-CAM1(a)**）；是否合併一本由 Pei 定。
交付檔名依 workflow 之模板事實：`…_SWQT_RearViewCamera_A_<YYYYMMDD>.xlsx`／`…_B_<YYYYMMDD>.xlsx`
（`delivered/` 只放客戶檔名定稿，無 `dryrun` 尾綴，**R-G42 五**）。

### 5-2　阻交付之 DR ↔ 列數（Pei 裁：結案／降 NA／帶 PENDING 不出貨）

| DR | 題 | A 本列數 | B 本列數 | 合計 |
|---|---|---:|---:|---:|
| DR-CAM-f | 缺件之 DBC 三類 | 9 | 0 | 9 |
| DR-CAM-g | HMI 入口路徑之逐字來源（二項）| 5 | 7 | 12 |
| DR-CAM-h | Pop Up List 之 camera 相關缺件（二項）| 0 | 3 | 3 |
| DR-CAM-i | `LTM_OperationalModeSts` ↔ `CmdIgnSts` 對應表 | 4 | 0 | 4 |
| DR-CAM-j | Auxiliary Cameras 之訊號定義（四項）| 7 | 0 | 7 |
| DR-CAM-k | VF664 全文件 | 1 | 0 | 1 |
| DR-CAM-m | EVS HAL 支援之影像格式 | 1 | 0 | 1 |
| DR-CAM-p | LVDS 訊息之傳送週期 | 2 | 0 | 2 |
| DR-CAM-q | DTC 之識別碼 | 14 | 0 | 14 |
| DR-CAM-r | `Camera App`／`Enhanced Camera App` 配備旗標 | 0 | 26 | 26 |
| DR-CAM-s | 熱保護關顯示之進入法 | 0 | 1 | 1 |
| **合計（相異 TC 列）** | | **43／240** | **34／206** | **77／446** |

另 **DR-CAM-a**（VF617_V5 缺件）阻 `SWE-CAM-024` 之 `AUX Camera` 組（A 本 0 TC，已佔位）。
逐列對照：`features/camera/data/pending_by_dr.tsv`（A）／`pending_by_dr_b.tsv`（B）。

### 5-3　實機驗證

`features/camera/data/bench_verify.md`（sha256_16 `8f5e67fed8b42a68`）：**累計 97 列**
（A 本 11、B01a 3、B01b 8、B02a 11、B02b 12、B03 16、B04a 10、B05 20、B06 6）。

### 5-4　RD 回饋

**RDF-01 ～ RDF-17，共 17 條**（`features/camera/RD_FEEDBACK.md`）。
其中本階段新開者：RDF-11（X 鍵位置）、-12（`N/A` 列不應為 leaf）、-13（設定名大小寫）、
-14（§34.3／§34.4 重複）、-15（`PU0456` 之 `<Nol>`）、-16（橫幅時限 5 vs 10）、
-17（`Make Favorite`／`Edit Favorite`）。

### 5-5　Phase 7

`--write`／tag／寫 `delivered/` **待 Pei 之准**；本包未執行（§0 禁區）。
前置：§5-2 之 11 條 DR 結案或裁降 NA（canon §8.4.3、**R-G42 七**：`delivered/` 內 PENDING 須為 0）。

---

## §6　R-CAM17 證據表

| # | 主張 | 掃描字串／判準 | 母體 | 命令 | 命中 |
|---|---|---|---|---|---|
| 1 | D 欄排序唯一決定 | `(母號, 子號)` 升冪對 037 原序 | `recon_b_rows.tsv` 230 列 | 逐列比對兩個序列 | **逐列相等、相異 0** |
| 2 | 工作簿 D 欄升冪 | 同上規則 | dry-run 212 列 | openpyxl 逐列 | **成立** |
| 3 | 對帳 | `coverage_b` `tc_id` 相異 ↔ `generated/` json | 兩者 | 集合差 | **206 ↔ 206，差集 0／0**；206 ＋ 6 ＝ **212 ↔ 212** |
| 4 | Test Set 八組無變體 | `H` 欄逐列 | dry-run 206 個 TC 列 | `collections.Counter` | **8 組**，計數見 §4-5 |
| 5 | A 本本輪 0 改動 | — | A 本十目錄 | `git status --short` | **0 列** |
| 6 | 既有 197 列 0 改動 | — | `b01a`～`b05` 七目錄 | `git status --short` | **0 列**（僅 `b06/` 為新增）|
| 7 | md 與 json 一致 | — | 十八目錄 446 份 md | `render_tc.py --verify` | **相符 446／相異 0** |
| 8 | selfcheck 第 10 項對全案 | 修後之四項判準（§1-4）| A 240 ＋ B 206 ＝ 446 | `selfcheck_camera.py` | ERROR 十項**全 0**；第 10 項候選 **37（8.3%）** |
| 9 | A 本覆核之 L 率 | 逐列覆核 | 50 個機械命中列 | 人工 ＋ `a_audit.tsv` | **47/50 ＝ 94% 為 L** |
| 10 | B 本 PENDING 列數 | `PENDING: DR-CAM-[a-z]` | `b01a`～`b06` 八目錄 206 筆 json 之六欄 | `re.findall` | **34 列／44 次**，逐 DR 見 §4-7 |

---

## §7　自報錯誤

1. **`-204` 之 `test_set` 初填 `AUX Camera Settings` 而其母列 `-085` 屬 `AUX Camera Access`**
   （本包內自查自改）—— `coverage_b.tsv` 之母列列印時查出，已改為 `AUX Camera Access`，
   `b06_plan.tsv` 之 note 同步。若未查出，dry-run 之 Test Set 計數會與 `coverage_b` 不一致。
2. **`-205` 初稿之 verbatim 破保序子序列**（本包內自查自改）—— 末字 `Drive` 而來源為 `Drive,`
   （逗號黏字），已保留逗號。
3. **selfcheck 第 10 項對 `-204` 仍報 `列舉(3>2)`** —— §34.1.5.1 之三項由 `-085`（前兩項合一 ER）
   與 `-204`（第三項）承接，故兄弟數 2 < 3。**分析層裁定為 ＋1 而非 ＋2**（DECISIONS 6-74），
   該 WARN 為**已裁之殘留**，非新缺口；此即第 10 項訂為 WARN 級之用處（列出而不阻）。
4. **CAM-21 之 §1／§2 於本包執行** —— 其 §3（交付前置清單）亦併入本包 §5。
   CAM-21 本身**無對應之 `up/20260923_CAM-21.md`**；依 PROTOCOL 之同名配對制，
   該包之交付物落於本包，請分析層確認是否須補一份指向本包之空殼（本包未自行補）。
5. **本輪未動 `features/camera/RULINGS.md`／`framework.md`／`ANOMALIES.md`／`DATA_REQUESTS.md`**
   —— 無新立條、無重開、無新異常、無新 DR。
   `docs/fw036/RULINGS.sha.tsv` 仍未重生（**GCB-04**，非本線可改）。

---

## §8　未結

| # | 項 | 歸屬 |
|---|---|---|
| 1 | **A 本 H/M 三列之補生成範圍**（追加 4 列）—— §3 之清單，本包未生成 | 分析層 |
| 2 | CAM-21 之 up 包是否須補（§7-4）| 分析層 |
| 3 | selfcheck 第 10 項之 37 個 WARN 候選是否逐列覆核（已知多為偽陽）| 分析層 |
| 4 | **RDF-01 ～ RDF-17** 十七條 | RD |
| 5 | **11 條阻交付 DR**（77／446 列）—— 結案／降 NA／帶 PENDING 不出貨 | Pei |
| 6 | Phase 7（`--write`／tag／`delivered/`）| Pei |
| 7 | GCB-01 ～ GCB-12 十二項 | GC 線 |
| 8 | `bench_verify.md` **97 列** | Pei 實機 |

---

## §9　落檔清單

```
M  features/camera/RD_FEEDBACK.md                 RDF-17               sha256_16 5f619c0e5a72ab57
M  features/camera/DECISIONS.md                   6-71 ～ 6-77         sha256_16 c4ad918ec758eb58
M  docs/fw036/handoff/GC_BACKLOG.md               GCB-12               sha256_16 a8c3f9fb09d3c4ac
M  features/camera/scripts/selfcheck_camera.py    第 10 項（WARN）      sha256_16 69d5da4cf0e5db5f
A  features/camera/data/b06_plan.tsv              9 列                 sha256_16 ea15fe4db7d663da
A  features/camera/data/a_audit.tsv               240 列               sha256_16 4b1ead43634c101d
M  features/camera/data/coverage_b.tsv            母列追加 6           sha256_16 a1563082de0cd078
A  features/camera/data/pending_by_dr_b.tsv       34 列／44 次         sha256_16 36496b45d30446ea
M  features/camera/data/bench_verify.md           B06 6 列（累計 97）  sha256_16 8f5e67fed8b42a68
A  features/camera/generated/b06/                 9 json ＋ 9 md ＋ INDEX ＋ LINT_REPORT
A  features/camera/sandbox/b_dryrun/…_dryrun.xlsx 212 列               sha256 3724671b88fbe17c…
A  docs/fw036/lint_reports/RearViewCamera_B__camera_3724671b_20260923.md
（前包已落未 commit）features/camera/data/b_audit.tsv                    sha256_16 288340d93afd082f
```

**未動**：`features/camera/RULINGS.md`、`framework.md`、`ANOMALIES.md`、`DATA_REQUESTS.md`、
`features/camera/delivered/`、A 本十目錄與 `sandbox/a_dryrun/`、B 本既有 197 列、
`features/comfort/`（併行 session）。
