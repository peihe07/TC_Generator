# 上繳包 CAM-25 —— B 本形制補正（＋4）、B dry-run v2 216 列、`DELIVERY_STATUS.md` 終態

對應下放包：`docs/fw036/handoff/down/20260923_CAM-25.md`（審閱 `down/20260923_CAM-24_review.md` 同發）。
執行層 session：延用。日期 2026-09-23。**本包為 Camera 生成階段之最後一包。**

**一句話**：§1 落檔（DECISIONS 6-82～6-85、**A-CA37**）；
§2 三列**原地收斂**為單點、拆出 **4 列**（`b07/`，`NR1L-RVCHMI-207`～`-210`）——
B 本 206 → **210**，**全案 455 TC**；§3 B dry-run v2 **216 列**，lint **ERROR 類全 0**、對帳 216 ↔ 216 合；
§4 `DELIVERY_STATUS.md` 更新為終態並加「生成階段結束」一節。
**兩項升級條件皆不成立**。未寫 `delivered/`、未 tag；A 本不動。

---

## §1　落檔

### 1-1　DECISIONS 6-82 ～ 6-85

`features/camera/DECISIONS.md`（sha256_16 `d0a702285c18855e`）：

| # | 項 |
|---|---|
| 6-82 | **canon §8.3／§5.7 勝於本 feature 之既有寫法** —— boundary 每點一列、不同觸發必拆；`NR1L-RVC-244`／`-245` 維持兩列 |
| 6-83 | B 本三列之形制補正（原地收斂 ＋ 拆出 `-207`～`-210`），登 **A-CA37** |
| 6-84 | `batch05_plan.tsv` 之欄位表不另立 |
| 6-85 | `NR1L-RVC-245` 之 `Remarks`（來源未載低於門檻之行為）|

### 1-2　ANOMALIES A-CA37

`features/camera/ANOMALIES.md`（sha256_16 `feb5f59acf41f939`）：
B 本三列「一列多點／多觸發」**經批次審閱而未抓**，狀態 **RESOLVED**（本包補正）。
其發現路徑記實：CAM-24 上繳 §2-2 把三列**當作前例**回報，
分析層於 CAM-24 審閱 §一-1 判為「三個前例違反 canon，不是 batch05 違反前例」而**反向補正**。
責任歸屬記為**分析層之抽讀式審閱**（同 **GCB-12** 之成因）；
並記執行層側之後衛限制 —— selfcheck 第 10 項只查拆解**不足**、**不查一列多點**，其擴充提請分析層裁。

---

## §2　B 本形制補正

### 2-1　三列之原地 diff（**既有列之唯一改動**）

```
NR1L-RVCHMI-036（b01b，§7.5.3）
- proc: 1. Send CAN: … = 205 (12.8125 km/h) / 2. … still displayed / 3. … = 206 (12.875 km/h) / 4. … turned off
+ proc: 1. Send CAN: … = 206 (12.875 km/h) / 2. … turned off
- title: auto dismissal when the vehicle speed reaches the 8 mph threshold during the delay
+ title: auto dismissal at the 8 mph on-point during the delay
+ reasoning 末附「CAM-24 審閱 §一-1 之補正 … 拆出 NR1L-RVCHMI-207（205，off-point）」

NR1L-RVCHMI-111（b03，§28.7.1.1）
- proc: 1. Pinch outwards … / 2. … zoomed in / 3. Pinch inwards … / 4. … zoomed out
+ proc: 1. Pinch outwards … / 2. … zoomed in
- title: the zoom functionality is a pinch on the wireless AUX video feed
+ title: the pinch outwards gesture zooms the wireless AUX video feed in
+ reasoning 末附「… 拆出 NR1L-RVCHMI-208（pinch in）」

NR1L-RVCHMI-142（b04a，§30.1.3）
- proc: 1. Press OK … / 2. … cleared / 3. … ‘X’ … / 4. … cleared / 5. … record timestamp / 6. … 6 seconds …
+ proc: 1. Press OK on the confirmation pop-up / 2. … cleared
- title: the favorite confirmation clears on OK on X or automatically after 5 seconds
+ title: the favorite confirmation is cleared by pressing OK
+ reasoning 末附「… 拆出 NR1L-RVCHMI-209（‘X’ 清除）與 -210（5 秒自動清除）」
```

**`test_item_verbatim`／`specification_reference`／`vehicle_model`／`design_method`／`priority` 皆不動**；
改動只及 `tc_title`／`split_reason`／`test_item` 下半／`test_procedure`／`expected_result`／`reasoning`。
**其餘 449 列 0 改動**（`git status` 實測：`generated/` 下只 8 檔改、1 目錄新增）。

### 2-2　拆出之 4 列（`b07/`）

| TC | 母列 | 章 | 驗證點 |
|---|---|---|---|
| `-207` | `-036` | §7.5.3 | raw **205**（12.8125 km/h，門檻 off-point）→ 仍顯示 |
| `-208` | `-111` | §28.7.1.1 | **pinch in** → zoom out（前提加「影像已放大」）|
| `-209` | `-142` | §30.1.3 | **`‘X’`** 清除 |
| `-210` | `-142` | §30.1.3 | **5 秒逾時**自動清除 |

`b07_plan.tsv`（sha256_16 `67f5c1964a79db8b`）4 列；
`coverage_b.tsv`（sha256_16 `846923671485a238`）之 3 個母 leaf 追加：

```
SWE1-RVC-023-02  b01b;b07  NR1L-RVCHMI-036;NR1L-RVCHMI-207
SWE1-RVC-103-02  b03;b07   NR1L-RVCHMI-111;NR1L-RVCHMI-208
SWE1-RVC-116     b04a;b07  NR1L-RVCHMI-142;NR1L-RVCHMI-209;NR1L-RVCHMI-210
```

**`-210` 不夾 4／6 兩點** —— 5 秒為 **timeout 而非門檻**（下放包 §2 明載），只取逾時後之 6 秒。

### 2-3　lint 與 selfcheck（**升級條件 §6-1 不成立**）

```
selfcheck_camera.py  b01b/b03/b04a/b07（TC 83 筆）  ERROR 級十項全 0
lint036 --profile camera
  b07（4 列）    ERROR 類全 0｜非致命 I-cross 4（U／X／J／Z 皆 0）
  b01b（20 列）  I-cross 20            b03（29 列）  U 3／I-cross 29
  b04a（30 列）  U 7／I-cross 30／X 10  batch05（5 列）J 1／I-cross 5
```

三列原地修改後**無任何 ERROR**；`b01b`／`b03`／`b04a` 之非致命計數與改前相同。

---

## §3　B 本 dry-run v2

```
features/camera/sandbox/b_dryrun/FM-WI-FSM-036-A01 STLA 測試用例規範與結果_SWQT STLA Test Case Specification & Result_SWQT_RearViewCamera_B_20260923_dryrun2.xlsx
sha256  7f06b61cb999b847ffd0c63583c509d00d5c2f5b80f5460b5c806e6360b1c8fb
資料列 216（TC 210 ＋ D 欄佔位 6）
```

### 3-1　lint 全表

```
行計 A=0 B=0 C=0 D=0 E=0 F=0 G=0 H=0 I=0 I-sibling=0 J=0 K=0 L=0 M=0 N=0
     P=0 Q=0 R=0 T=0 U=44 V=0 I-cross=210 W=0 X=23 Y=0 Z=0
報告 docs/fw036/lint_reports/RearViewCamera_B__camera_7f06b61c_20260923.md
```

**ERROR 類全 0**；`Z` 全綠；`J` 0。`X` 23 之成因為 **GCB-11**，與 v1 相同（非組簿工具）。

### 3-2　sheet 級

D 欄升冪**成立**（規則同 v1：`(母號, 子號)` 升冪、母列先）；佔位列 **6**；
Test Set **8 組**（`AUX Camera Access` 61／`AUX Camera Settings` 47／`Activation and Exit` 45／
`Camera View Switching` 26／`Warning Banners` 12／`PAM Integration` 9／`Camera Settings` 5／
`Wireless Camera Pairing` 5）；`N` 欄多行 **0**；`AH` remarks **8 列**；VM 七欄非 0/1 **0**。

### 3-3　對帳（**升級條件 §6-2 不成立**）

`coverage_b.tsv` 之 `tc_id` 相異 **210** ＋ 零 TC leaf **6** ＝ **216** ↔ 工作簿 **216** → **合**。

### 3-4　`pending_by_dr_b.tsv` 重生

sha256_16 `5fe1c3ffba1fd9b6`：**210 列中 34 列含 PENDING**
（DR-CAM-r 26／g 7／h 3／s 1，合計 44 次）。**補正之 4 列皆無 PENDING**，故列數與 v1 相同。

---

## §4　`DELIVERY_STATUS.md` 終態

sha256_16 `c22362544725edef`。更新項：

| 節 | 改動 |
|---|---|
| §1 兩本候選 | B 改指 `dryrun2`（216 列／210 TC）；合計 **465 列 455 TC** |
| §2 覆蓋 | B 本 TC 206 → **210**；1:1.02 → **1:1.07** |
| §3 DR | 分母改 455（11 條、77 列不變）|
| §7 品質 | 全 455 列之數字（selfcheck 錨 459／第 10 項候選 40／render 455-455）|
| **§9（新增）** | **「生成階段結束」** —— 本檔自此只因 ① DR 結案／降 NA ② Pei 實機回饋 ③ Phase 7 執行 而變更；**不再因生成而變更**；任何新增 TC 須有下放包之明文指派 |

---

## §5　R-CAM17 證據表

| # | 主張 | 掃描字串／判準 | 母體 | 命令 | 命中 |
|---|---|---|---|---|---|
| 1 | 三列之原地 diff 只及六個欄位 | `json.dumps` 前後逐行 diff | `-036`／`-111`／`-142` | `difflib.unified_diff` | 逐列之異動行皆為 `tc_title`／`split_reason`／`test_item`／`test_procedure`／`expected_result`／`reasoning` |
| 2 | 其餘 449 列 0 改動 | — | `generated/` 二十目錄 | `git status --short` | `generated/` 下只 **8 檔改**（三列之 json ＋ md、兩份 LINT_REPORT 等）、**1 目錄新增**（`b07/`）|
| 3 | `or‘X’to` 為單一黏字 token | `Description` 全文之 token 化 | cache 本 §30.1.3（`NRL-188130`）| `.split()` 後逐項檢視 | token 序列含 `'or‘X’to'`（`or`、引號、`to` 相連）→ `-209` 之 verbatim 整體保留該 token |
| 4 | dry-run v2 對帳 | `coverage_b` `tc_id` 相異 ＋ 零 TC leaf ↔ 工作簿列數 | 兩者 | 集合 ＋ openpyxl | **210 ＋ 6 ＝ 216 ↔ 216，合** |
| 5 | D 欄升冪 | `(母號, 子號)` 升冪、母列先 | dry-run v2 216 列 | openpyxl 逐列 | **成立** |
| 6 | 全案 selfcheck | 十項 ERROR ＋ 第 10 項 WARN | 二十目錄 **455** 列 | `selfcheck_camera.py` | ERROR 十項**全 0**；錨 **459**／反查失敗 0／常數錨 2；第 10 項候選 **40** |
| 7 | md 與 json 一致 | — | 二十目錄 455 份 md | `render_tc.py --verify` | **相符 455／相異 0** |
| 8 | B 本 TC 數 | json 檔數 | `b01a`～`b07` 九目錄 | glob 計數 | **210** |

---

## §6　自報錯誤

1. **`-209` 初稿之 verbatim 破保序子序列**（本包內自查自改）——
   原寫 `User presses ‘X’to clear`，而來源之 token 為 `or‘X’to`（單一黏字）。
   已改為 `User presses or‘X’to clear,`（只刪 `OK` 一字），該讀法為逐字忠實之代價（§4.3.1），
   reasoning 已具名。
2. **`-208` 初稿之 lint `R` 1 列**（本包內自查自改）—— Pre-Condition 一行兩條件，已分行。
3. **第 10 項對 `-036`／`-207`／`-142`／`-209` 續報 `boundary` WARN** ——
   其成因為 raw 值（205／206）與門檻值（8 mph）不同單位，偵測器無從對應；
   `-142`／`-209` 之 `5` 亦因觀察點寫 6 秒而未見 `5`。
   皆為**已知偽陽**（**DECISIONS 6-80** 不逐列覆核），非新缺口。
4. **本輪未動 `features/camera/RULINGS.md`／`framework.md`／`RD_FEEDBACK.md`／`DATA_REQUESTS.md`／
   `profile`** —— 無新立條、無重開、無新 DR。
   `docs/fw036/RULINGS.sha.tsv` 仍未重生（**GCB-04**，非本線可改）。

---

## §7　未結（生成階段結束；應只餘 Pei／RD／GC 三類）

| # | 項 | 歸屬 |
|---|---|---|
| 1 | **11 條阻交付 DR**（77／455 列；A 43／B 34）—— 結案／裁降 NA／帶 PENDING 不出貨 | **Pei** |
| 2 | **`bench_verify.md` 101 列**實機驗證 | **Pei** |
| 3 | **Phase 7**（`--write`／tag／寫 `delivered/`）—— 五項前置見 `DELIVERY_STATUS.md` §8 | **Pei** |
| 4 | 兩本分交或合併之決定（**R-CAM1(a)** 為兩本）| **Pei** |
| 5 | **RDF-01 ～ RDF-17** 十七條回 RD 作者 | **RD** |
| 6 | **GCB-01 ～ GCB-12** 十二項；**GCB-01** 註「Camera 交付前必須完成」 | **GC 線** |
| 7 | （分析層可選）selfcheck 第 10 項是否擴為亦查「一列多點」（A-CA37 之後衛缺口）| 分析層 |

**執行層側之生成工作至此無未結項。**

---

## §8　落檔清單

```
M  features/camera/DECISIONS.md                   6-82 ～ 6-85         sha256_16 d0a702285c18855e
M  features/camera/ANOMALIES.md                   A-CA37               sha256_16 feb5f59acf41f939
M  features/camera/DELIVERY_STATUS.md             終態 ＋ §9           sha256_16 c22362544725edef
A  features/camera/data/b07_plan.tsv              4 列                 sha256_16 67f5c1964a79db8b
M  features/camera/data/coverage_b.tsv            母列 3 列追加         sha256_16 846923671485a238
M  features/camera/data/pending_by_dr_b.tsv       210 列口徑重生        sha256_16 5fe1c3ffba1fd9b6
A  features/camera/generated/b07/                 4 json ＋ 4 md ＋ INDEX ＋ LINT_REPORT
M  features/camera/generated/b01b/NR1L-RVCHMI-036.{json,md}   原地收斂
M  features/camera/generated/b03/NR1L-RVCHMI-111.{json,md}    原地收斂
M  features/camera/generated/b04a/NR1L-RVCHMI-142.{json,md}   原地收斂
M  features/camera/generated/batch05/NR1L-RVC-245.{json,md}   Remarks（6-85）
M  features/camera/generated/{b01b,b03,b04a,batch05}/LINT_REPORT.md
A  features/camera/sandbox/b_dryrun/…_dryrun2.xlsx 216 列              sha256 7f06b61cb999b847…
A  docs/fw036/lint_reports/RearViewCamera_B__camera_7f06b61c_20260923.md
```

**未動**：`RULINGS.md`、`framework.md`、`RD_FEEDBACK.md`、`DATA_REQUESTS.md`、
`docs/runtime/profiles/`、`features/camera/delivered/`、A 本十一目錄（`batch05/NR1L-RVC-245` 之
`Remarks` 除外）與 `sandbox/a_dryrun/`、`features/comfort/`（併行 session）。
