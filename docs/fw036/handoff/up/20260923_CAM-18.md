# 上繳包 CAM-18 —— R-CAM16(c)／GCB-10／DECISIONS 6-62～6-64、B03 30 leaf（產 29 TC）

對應下放包：`docs/fw036/handoff/down/20260923_CAM-18.md`（審閱 `down/20260923_CAM-17_review.md` 同發）。
執行層 session：延用。日期 2026-09-23。

**一句話**：§1 三項落檔完成；B03 **30 leaf** 產 **29 TC**（`NR1L-RVCHMI-099`～`-127`），
**`Camera View Switching` 與 `Wireless Camera Pairing` 兩組皆完結**；
selfcheck 十項全 0、lint ERROR 類全 0（`J`／`X` 亦為 0）。
**下放包 §2 之「31 ＋ 6 ＝ 37」為全列數，實測 leaf 為 25 ＋ 5 ＝ 30**（回報，見 §2-1）。
三項升級條件**皆不成立**。**A 本不動**（十目錄 `git status` 實測 0 改動）；未寫 `delivered/`、未 tag。

---

## §1　落檔

### 1-1　R-CAM16(c)

| 錨 | `sha8` | `body_sha8` |
|---|---|---|
| **R-CAM16(c)** | `d9212990` | `65b2946d` |

以同級錨落檔（**不消耗 live**，`{live}` 仍為 19）；
**落檔前 27 錨點之 `sha8`／`body_sha8` 逐一比對：改變 0 個**（證據表第 1 項）。
零 TC 自此**四種**形制：委派（R-CAM16）／Heading／自陳 `N/A`（R-CAM16(b)）／
逐字重複或純交叉引用（R-CAM16(c)）。

`coverage_b.tsv` 之四列 disposition 已改為該款文面：

```
SWE1-RVC-086  27.7.3   No TC — cross-reference only (A-CA36)
SWE1-RVC-091  27.8.5   No TC — cross-reference only (A-CA36)
SWE1-RVC-136  34.4.2   No TC — verbatim duplicate of §34.3.2 (A-CA36)
SWE1-RVC-139  34.6.1   No TC — verbatim duplicate of §34.4.1 (A-CA36)
```

`b02b_plan.tsv` 之 note 同步（sha256_16 `b316239f2e231399`）。

### 1-2　GCB-10

`docs/fw036/handoff/GC_BACKLOG.md`（sha256_16 `fbef6198f83a7c14`）登 **GCB-10**：
沿用既有 DR 而擴其範圍時，須於 `DATA_REQUESTS.md` 明文改題並保留原題。
立條之由為本線兩次實例（DR-CAM-g 已明文改題；DR-CAM-h 於 CAM-16 沿用未擴題、CAM-17 補正），
並給出建議形制（原題入括號、逐項編號、補列 `Leaves served`）。

### 1-3　DECISIONS 6-62 ～ 6-64

`features/camera/DECISIONS.md`（sha256_16 `4abb61e61473c1ec`）：
**6-62** 零 TC 四形制（立 R-CAM16(c)；判準關鍵為**父題是否亦全等**）；
**6-63** DR 擴題通則提請全域（GCB-10）；
**6-64** §27 ↔ §34 八群同句各自出 TC、HU 等級以散文前提書寫（R-CAM18 不適用）。

---

## §2　B03

### 2-1　列數口徑之更正（回報）

下放包 §2 寫「`Camera View Switching`（HU 28，**31 leaf**）＋ `Wireless Camera Pairing`（HU 33，**6 leaf**）＝ 37」。
實測 `b_plan.tsv`：

| 組 | 全列 | **leaf** | umbrella |
|---|---:|---:|---:|
| `Camera View Switching` | 31 | **25** | 6 |
| `Wireless Camera Pairing` | 6 | **5** | 1 |
| 合計 | 37 | **30** | 7 |

即下放包之 31／6 為**全列數**（與 framework VIII.3 之表一致），leaf 為 25／5。
**30 ≤ 40，升級條件 §4-1 不成立**，兩組皆出。

### 2-2　1 列零 TC（R-CAM16(c)(ii)）

`SWE1-RVC-102`（§28.6.1.2，`NRL-188107`）全文為
`For details about wireless cameras connection please refer to ‘Wireless cameras connection’ pages`，
標的 §27.5／§27.6（`Wireless AUX Camera Connection …`）已有 TC（B02a `-070`～`-073`、B02b `-074`）
→ 依 **R-CAM16(c)(ii)** 記零 TC。

**對照（不適用之例）**：§33.1.1（`-123`）末有 `(please refer to next pages).` 之括號句，
惟其**非全文交叉引用**（前有 39 token 之完整需求句），故仍出 TC。

### 2-3　產物

`features/camera/generated/b03/` —— 29 份 json ＋ 29 份 md ＋ `INDEX.md` ＋ `LINT_REPORT.md`。
TC ID `NR1L-RVCHMI-099`～`-127`；計畫 `features/camera/data/b03_plan.tsv`（30 列，sha256_16 `e44d27468b6cb9e9`）。
**Test Set 分兩組**：`-099`～`-122` 為 `Camera View Switching`（24 TC）；
`-123`～`-127` 為 `Wireless Camera Pairing`（5 TC）。

### 2-4　`Vehicle Model`

27 列五款全 `1`；**2 列**（`-099` §28.1、`-109` §28.6.1.1）其前提用
`Surround_View_Camera`（byte 177 bit 0，父題分別引 `PU0853` 之 surround view 情境與
§28.6 之 `UI – In Reverse with “Surround View and Camera App”`），
該 byte 於 `Toro_ATL_MI` 零命中 → **R-CAM18(b)** `Toro(2261)` 判 `0`。
**升級條件 §4-3 不成立** —— 本批所用之配備旗標（`Surround_View_Camera`、`Rear_View_Camera`、
`Radio_Display_Type`）於 PROXI 皆查得。

### 2-5　hop label（升級條件 §4-2）

| label | 逐字出處 |
|---|---|
| `More Cams` | §28.1／§28.6.1.1 逐字 |
| `More AUX` | §28.1（`More AUX row of buttons`）／§28.3.2 逐字 |
| `Edit image` | §28.7.2 逐字 |
| `Rotate 90`／`Mirror Image`／`Reset` | §28.7.2 逐字（三控制項）|
| `Add camera` | §33.1.1／§33.1.2 逐字（`‘Add camera’ button`）|
| `App Drawer` | §27.2.6.2 逐字＋A 本 `NR1L-RVC-113` 句式 |
| `Enable Wireless Cameras` | DECISIONS **6-61**（取彈窗表拼法）|

**不可辨者 0 列**，升級條件不成立。本批**未用 Controls page**，故無 DR-CAM-g。

### 2-6　popup（六個 PU 皆查得，無 DR-CAM-h）

| SYS1 | PU | module | 用於 |
|---|---|---|---|
| §28.1 | `PU0853` | Aux Camera | `More Vehicle Cameras` 彈窗之內容（`-099`）|
| §28.2.3 | `PU0852` | Aux Camera | `<Insert Camera Name Here> <Battery><WiFi>`（`-102`）|
| §28.4.2 | `PU0851` | Aux Camera | `Camera Unavailable . Please go to camera settings …`（`-108`）|
| §33.1.1／§33.1.2 | `PU0854` | Aux Camera | `Add Aux Camera …` QR Code Setup Process（`-123`／`-125`）|
| §33.1.2 | `PU1519` | Camera | `Do you want to disconnect Apple CarPlay and add a wireless camera?`（`-124`）|
| §28.4.1 | `PU0459`／`PU0851` | Aux Camera | 不可用態之逐字依據（`-107`）|

**§28.2.1／§28.3.1／§28.3.2 不掛 DR-CAM-h** —— 其文字（`Check Entire Surroundings`／
`camera system unavailable`）與時限（`5 seconds`）皆由來源自載，依 §4.3.1 取其逐字。
`Check Entire Surroundings` 於彈窗表命中 **5 筆**，module 皆非 `Aux Camera`（見證據表第 4 項），
惟不需彈窗表補足。

**`PU1518` vs `PU1519` 之分辨**（本批新得）：兩者皆為投影衝突之確認彈窗，
觸發欄逐字不同 —— `PU1518` 為「啟用設定」或「選取無線相機」（B02a `-064`／`-072`），
`PU1519` 為「**新增**無線相機」（本批 `-124`）。

### 2-7　PENDING（3 列，皆 DR-CAM-r）

| TC | 章 | 事由 |
|---|---|---|
| `NR1L-RVCHMI-099` | §28.1 | `Camera App` 配備旗標 |
| `NR1L-RVCHMI-106` | §28.3.2 | 同上（`with or without camera app` 之有 camera app 側）|
| `NR1L-RVCHMI-118` | §28.7.3 | `Enhanced Camera App` 配備旗標 |

### 2-8　對帳（`coverage_b.tsv`，sha256_16 `e27e28bb44aff1a7`）

| 組 | `b_plan` 全列 | `coverage_b` | 判 |
|---|---:|---:|---|
| `Activation and Exit` | 50 | 50 | **完結** |
| `AUX Camera Access` | 62 | 62 | **完結** |
| `Camera View Switching` | 31 | 31 | **完結** |
| `Wireless Camera Pairing` | 6 | 6 | **完結** |
| `AUX Camera Settings` | 53 | 0 | 未生成 |
| `Warning Banners` | 13 | 0 | 未生成 |
| `PAM Integration` | 10 | 0 | 未生成 |
| `Camera Settings` | 5 | 0 | 未生成 |
| **合計** | **230** | **149** | 8 組中 **4 組完結** |

`coverage_b.tsv` 全檔：PRODUCED 127／零 TC 6／umbrella 16 ＝ **149 列**。

### 2-9　lint 與 selfcheck

```
selfcheck_camera.py  b03（TC 29 筆）         十項全 0｜錨 29／反查失敗 0
lint036 --profile camera（b03_lint.xlsx）    ERROR 類全 0｜非致命 U=3／I-cross=29｜Z=0／J=0／X=0

selfcheck_camera.py  十五目錄（TC 367 筆）    十項全 0｜錨 371／反查失敗 0｜常數錨 2｜合一錨 2
render_tc.py --verify 十五目錄               逐位元相符 367／相異 0
```

---

## §3　R-CAM17 證據表

| # | 主張 | 掃描字串／判準 | 母體 | 命令 | 命中 |
|---|---|---|---|---|---|
| 1 | R-CAM16(c) 未動既有條文指紋 | `sha8`／`body_sha8` 逐錨比對 | `RULINGS.md` 落檔前 **27 錨點** | `rulings_hash.py` × 2 ＋ 集合比對 | 既有 27 錨**改變 0**；新增 `R-CAM16(c)` |
| 2 | B03 兩組之 leaf 數 | `test_set` ＋ `kind` 欄計數 | `features/camera/data/b_plan.tsv` 230 列 | csv 計數 | `Camera View Switching` 全 31／leaf **25**；`Wireless Camera Pairing` 全 6／leaf **5** |
| 3 | §28.6.1.2 全文為交叉引用句 | `Description` **全文**（profile §7.7 不切片）| cache 本 §28.6.1.2（`NRL-188107`）| openpyxl 讀該格 ＋ 印 token 數 | 全文 **13 token**，即該交叉引用句本身 |
| 4 | `Check Entire Surroundings` 無 AUX module 條目 | `check entire surroundings`（不分大小寫，比對訊息欄）| `forms/Pop Up List HMI R1 (26PI).xlsx` `Main` 分頁 | openpyxl 逐列 `re.search` | **5 筆**：`PU0362`／`PU0467`（Surround View Camera）、`PU0446`（Trailer Reverse Guidance）、`PU0447`（Rearview Camera with all features）、`PU1102`（Turn Signal…）—— 無 `Aux Camera` |
| 5 | `PU1518` 與 `PU1519` 之觸發欄相異 | 觸發欄逐字 | 同上 r1301／r1302 | openpyxl 逐欄讀 | `PU1518`：`tries to enable wireless cameras` ＋ `presses a wireless camera button`；`PU1519`：`tries to **add a new** wireless camera` |
| 6 | `PU0852`／`PU0851`／`PU0853`／`PU0854` 皆 module `Aux Camera` | module 欄 | 同上 | openpyxl 逐列 | 四者**皆 `Aux Camera`**（`PU0851`／`PU0459` timeout 欄皆 `5 seconds`）|
| 7 | `Radio_Display_Type` 之 12 吋直式值 | 值域逐字 | `forms/proxi/Promaster_ATL_MI/…637_R42_20220204.XLSM` byte 185 | openpyxl 讀該列 | `6 = 12" 1920x1200`／**`7 = 12" 1200x1920`**（寬 < 高即 Portrait）|
| 8 | `Surround_View_Camera` 缺於 Toro | `Surround_View_Camera` | `forms/proxi/` **6 本** | `proxi_have.py` | 五本各 1；**`Toro_ATL_MI` 0** |
| 9 | A 本本輪 0 改動 | — | A 本十目錄 | `git status --short` | **0 列** |
| 10 | md 與 json 一致 | — | 十五目錄 367 份 md | `render_tc.py --verify` | **相符 367／相異 0** |
| 11 | 四組完結對帳 | `b_plan` 全列 ↔ `coverage_b` 列數 | 兩檔 | csv 計數 | 四組**逐組相等**（50／62／31／6）|

---

## §4　自報錯誤

1. **B03 初稿之 selfcheck 第 2 項 1 列、第 4 項 1 列**（本包內自查自改）——
   `-107`（§28.4.1）Procedure 只有 1 步（已加「有線側不灰階」之對照步，該對照亦使
   「灰階繫於無線不可用」可判）；`-114`（§28.7.2）摘句成 `the “Rotate 90” soft controls`
   而來源之 token 為 `“Rotate` `90”,`（逗號黏字），破保序子序列。
2. **§28.7.2 三列改用全文 verbatim**（承上）—— 該句之三個控制項以 `,`／`and` 相連，
   逐字摘句必留孤懸標點；三列 verbatim 一律取**全文 15 token**，
   各列所驗之控制項改由 `test_item` 下半之括號句指明。
3. **B03 初稿之 lint `R` 3 列**（本包內自查自改）—— Pre-Condition 一行兩條件，已分行。
4. **B03 初稿之 lint `J` 2 列**（本包內自查自改）—— `-121`／`-122` 之 verbatim 自 §28.8.1 之
   `however,` 起首（小寫）。**profile §5.1 之豁免不適用** —— 該豁免之要件為
   「上半與來源 `Description` 之**首 token** 相同」，而該兩列非自首 token 起。
   已改為保留首片語 `From the AUX camera video feed,`（仍為保序子序列），`J` 歸 0。
5. **下放包 §2 之列數口徑與實測不符**（§2-1）—— 「31 leaf ＋ 6 leaf」實為全列數。
   已依實測之 leaf 數執行並回報，未自行擴批。
6. **本輪未動 `features/camera/framework.md`／`DATA_REQUESTS.md`／`ANOMALIES.md`／`RD_FEEDBACK.md`**
   —— 無重開、無新 DR、無新異常（A-CA36 已涵蓋本批之零 TC 形制）。
   `docs/fw036/RULINGS.sha.tsv` 仍未重生（**GCB-04**，非本線可改）。

---

## §5　未結

| # | 項 | 歸屬 |
|---|---|---|
| 1 | B 本餘 **4 組 81 列**（leaf 70）：`AUX Camera Settings` 53、`Warning Banners` 13、`PAM Integration` 10、`Camera Settings` 5 | 後續批次 |
| 2 | **RDF-11～RDF-14** 四條 | RD |
| 3 | DR 十五條；A 本 f／g／i／j／k／m／p／q 八條阻交付，B 本 g／h／r | Pei |
| 4 | A 本交付（Phase 5–7）待 Pei 之准；候選為 dryrun2（244 列，DECISIONS 6-58）| Pei |
| 5 | GCB-01～GCB-10 十項 | GC 線 |
| 6 | `bench_verify.md` 累計 **61 列**（A 本 11、B01a 3、B01b 8、B02a 11、B02b 12、B03 16）| Pei 實機 |

---

## §6　落檔清單

```
M  features/camera/RULINGS.md                     R-CAM16(c)           sha256_16 c8ed10478891b4f0
M  features/camera/DECISIONS.md                   6-62 ～ 6-64         sha256_16 4abb61e61473c1ec
M  docs/fw036/handoff/GC_BACKLOG.md               GCB-10               sha256_16 fbef6198f83a7c14
M  features/camera/data/b02b_plan.tsv             note 改 R-CAM16(c)   sha256_16 b316239f2e231399
A  features/camera/data/b03_plan.tsv              30 列（產 29）       sha256_16 e44d27468b6cb9e9
M  features/camera/data/coverage_b.tsv            149 列（4 組完結）   sha256_16 e27e28bb44aff1a7
M  features/camera/data/bench_verify.md           B03 16 列            sha256_16 1b87da4f049a8aeb
A  features/camera/generated/b03/                 29 json ＋ 29 md ＋ INDEX ＋ LINT_REPORT
```

**未動**：`features/camera/framework.md`、`DATA_REQUESTS.md`、`ANOMALIES.md`、`RD_FEEDBACK.md`、
`features/camera/delivered/`、A 本十目錄與 `sandbox/a_dryrun/`、`features/comfort/`（併行 session）。
