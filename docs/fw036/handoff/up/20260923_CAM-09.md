# 上繳包 CAM-09 —— CAM-08 審閱落檔 ＋ batch02c（`-003`，23）＋ batch03 選列計畫

對應下放包：`docs/fw036/handoff/down/20260923_CAM-09.md`
對應審閱：`down/20260923_CAM-08_review.md`
執行層 session：延用，2026-09-23。**git 未 commit**；未寫回工作簿；`pilot01/` 未動。

> **升級條件（下放包 §5）**
> 1. selfcheck 第 3／7 項命中 **>20 相異 TC 列** → **未觸發**：第 7 項 12 列、第 3b 項 3 列，
>    相異聯集 **12 列**（3b 之三列皆在第 7 項內）。
> 2. batch02c **23 ≤ 30** → 未觸發。
> 3. `-003` 任一驗證點之來源全被委派 → **成立一例**（§2-4），已回報、未硬寫。

### R-G62(b) 之觀察依據

起點前之最後他方寫入：`down/20260923_CAM-08_review.md` 與 `down/20260923_CAM-09.md`。
本 session 起點後至落檔止，`features/camera/` 下未觀察到他方寫入；
`down/20260922_CMF-07_review.md`（CMF 線）維持未追蹤。**不聲稱「無他 session 寫檔」**。

---

## §1　落檔

### 1.1　DECISIONS 6-23 ～ 6-27

6-23 升級條件以**相異 TC 列**計；6-24 `Body_Types` 不寫 raw（開 DR-CAM-n）；
6-25 `-091` 接受現狀；6-26 `-088` 不拆 14 列；6-27 `-082` ER 兩項接受。

### 1.2　DATA_REQUESTS

| DR | 內容 | 級 |
|---|---|---|
| **DR-CAM-n**（新）| Atl-Hi PROXI `Body_Types`（byte 231 bit 0–2）之列舉 —— 三本 row 1018 只載現值 `7 = Type 7` | 中 |
| **DR-CAM-o**（新，本輪生成中發現）| `IPC_VEHICLE_SETUP.LanguageSelection` 之語言 ↔ raw 對照與 HU 支援語言清單 | 低（單列 `-113`）|

### 1.3　profile

| 節 | 內容 |
|---|---|
| **§4.1（新）** | 開機類 TC 之判別 —— 步 1 為點火者首行必 `Standby`；反向（`Standby` 首行而無點火步）亦禁；點火動作一律 CAN 式，目的子句移 ER；**電池斷電不在此列**（`-064` 之 NVM 驗證所需） |
| **§7.2（新）** | 訊號 raw 之查證順序 —— 先以**訊號名自身**查四本 DBC 之 `VAL_`，不得以同名近似之訊號代查；附 CAM-08 `P` 5 筆之反例與 `TGW_CAMERA_DISP_STAT` 之實測值域；LVDS 訊號無 DBC，一律 label 逐字 |

### 1.4　selfcheck 第 3 項（雙向）與第 7 項

| # | 判準 |
|---|---|
| **3a** | Pre-Condition **首行**為 `Full-Operation` 而步 1 送 `CmdIgnSts = RUN` —— 二者擇一處置（非開機類刪步 1；開機類首行改 `Standby`）|
| **3b** | Pre-Condition **首行**為 `Standby` 而 Procedure **無任何點火步** —— 該態下不送點火則其後各步不可執行 |
| **7** | Procedure 以散文寫點火（`Cycle the ignition`／`Turn the ignition`／`Power on the HU`／`Power cycle`）|

> **「雙向」之解讀須確認**：審閱 §二-2 寫「第 3 項抓重複，本項抓矛盾…併入第 3 項為雙向」。
> 若照字面（**步 1 RUN ∧ 首行 Full-Operation**）則與原第 3 項為**同一述詞**，只是處置有二；
> 本包因而把「矛盾」實作為**另一個方向**（`Standby` 首行 ∧ 無點火步），使雙向各有獨立述詞。
> 原第 3 項另收緊為「**首行**為 Full-Operation」（原為全文含 `Full-Operation` 即算）。
> **若 Pei 之原意為前者，請於審閱指正**；兩種實作對本輪之命中集合無差（3a 修正後皆為 0）。

### 1.5　§1-5 之修正（見 §3）

---

## §2　batch02c 生成（23 列，`NR1L-RVC-095`～`-117`）

### 2.1　產物與分佈

`features/camera/generated/batch02c/`：23 × json ＋ 23 × md ＋ `INDEX.md` ＋ `LINT_REPORT.md`。
Test Set：`-095`～`-097` **`Auxiliary Cameras`**（來源屬 Cargo/CHMSL 與 SVC 節，R-CAM13(c)）；
`-098`～`-117` `State Handling`。23 列之 test_item 上半**全部未逾 50 token，無摘句**。

### 2.2　下放包 §2 各要點之落實

| 要點 | 落實 |
|---|---|
| 狀態機轉換只取 `-003` 獨有錨 | `-106`（`V2-534`）為其唯一與 batch01 重疊者之**承接列**（`-023` 當時即因 R-CAM10 改錨 CFTS092）；本列取**不成立側**（離開 RUN）與 `-023` 分工 |
| `V4-117` ISO 字型／字高之量測 | `-112`：ER 以來源數值逐字（`ISO font 15008`／`greater than 18 arc minutes`）；**量測手段來源未載即不造工具**，procedure 只寫 `Read … and check its font and its size` |
| `V2-535`（VF664）PENDING | `-107`：Pre-Condition ＋ ER 皆標 `PENDING: DR-CAM-k` |
| `V33-222`／`-224` 已合一歸 `-001` | 本批不出，plan 之該二列 `tc_count = 0`、note 記委派 |
| R-CAM3(f)／R-CAM15 適用 | 23 列無一寫 CAN source 行（皆單一 EE 或無 `Send CAN` 步）；V42 列（`-117`）依 R-CAM15(c) 承 637 |

### 2.3　lint ＋ selfcheck

```
batch02c（23 列）：A=0 B=0 C=0 D=0 E=0 F=0 G=0 H=0 I=0 I-sibling=0 K=0 L=0 M=0 N=0
                  P=0 Q=0 R=0 T=0 V=0 W=0 X=0 Y=0 **Z=0**｜**J=3**  U=11  I-cross=23
selfcheck（八項，本批）：全 0
```

**初稿曾有 `A`=4／`H`=2／`W`=1／`X`=3，皆已改正**：
`Observe` 為禁用動詞（`-104`／`-105`，改 `Read … and check`）；
`matches`／`corresponds to` 為 ER 模糊語（`-107` 改 `is as defined in`、`-113` 改
`is shown in the second language that was selected in step 1`，該改寫同時解消 `W`）；
`screen` 之導航誤判（`-101`／`-104`／`-105`，改 `display`）。

`J`=3（`-099`／`-100`／`-101`）為 **profile §5.1 之逐字豁免** —— 來源首字即子句編號 `a.`／`c.`，
三列 reasoning 已逐列具名。

### 2.4　**升級條件 3 成立：一個驗證點之來源全被委派**

`SWE-CAM-003` 之 **Dynamic Gridlines 動作側**（`SYS-RA-VF551_V33-222`／`-224`）——
其兩個來源依 **R-CAM10(b)** 已於 CAM-07 合一歸 `SWE-CAM-001`（`NR1L-RVC-062`／`-063`）。
該驗證點於 `-003` 因而**無來源可承**，本批**不硬寫**，plan 之該二列記委派。
此為 R-CAM10(b) 之預期效果而非缺漏，於此依升級條件回報。

另有六個來源整列委派（`V2-536`／`-555`／`V42-209` → `-002`；`V42-217`／`-221`／`-223` → `-001`），
其驗證點皆由承接列涵蓋，不屬本條。

### 2.5　PENDING 列表（`U` = 11）

| TC | DR | 說明 |
|---|---|---|
| `-096`／`-097` | DR-CAM-j | `SVC_SoftBtn_Rq` 不在四本 DBC；`<Tsend>` 無值 |
| `-107` | DR-CAM-k | VF664 查無 |
| `-108` | DR-CAM-i | `LTM_OperationalModeSts` SNA 之 `CmdIgnSts` 對應無來源 |
| `-109`／`-110` | DR-CAM-f | `BED_EXTENDER` 不在四本 DBC |
| `-113` | **DR-CAM-o** | `LanguageSelection` 之語言 raw 無來源 |

### 2.6　本批新查得之標定值

| 常數 | 值 | 單位 | 座標 |
|---|---|---|---|
| `TIME_W_RVC` | **2,5** | `sec` | V33 §1.14.1 `-515`／`-516`／`-519` |
| `TIME_W_RVC2` | **50** | `ms` | V33 §1.14.1 `-587`／`-588`／`-591`（範圍 `[50;100]`、容差 `10`）|

二者使 `-108`（50 ms 觀察窗）與 `-110`（停送逾 2,5 s）之 procedure 不留符號。
**兩值宜補入 profile §9 常數表** —— 本包未自行補（該表為審閱 §一-9 所定之內容），請裁。

---

## §3　五目錄之修正（§1-5）

欄級 diff：`features/camera/generated/DIFF_CAM08_to_CAM09.md`。**變動 14 列**
（batch01b 2／batch02b 12）；pilot02／batch01／batch02a 三目錄第 3／7 項零命中，逐字未動。

### 3.1　第 7 項之命中清單（12 列）

`-037`／`-044`（batch01b）、`-073`／`-074`／`-075`／`-076`／`-077`／`-078`／
`-085`／`-086`／`-087`／`-091`（batch02b）—— 全數 `Cycle the ignition to RUN …`。
改 `Send CAN: <平台 CmdIgnSts> = 4 (RUN)`，目的子句移 ER1。
`-037`／`-044` 另刪步 1 之 `Set PROXI … and` 前綴（PROXI 已在 Pre-Condition）。

### 3.2　第 3b 項之命中清單（3 列）

`-085`／`-086`／`-087` —— `Standby` 首行而無點火步；**由第 7 項之修正一併解消**
（步 1 改為送 RUN 後即有點火步）。修正後 3a／3b 皆 0。

### 3.3　連帶之三項

- **首行改 Standby（9 列）**：`-037`／`-044`／`-073`～`-078`／`-091`（profile §4.1）。
- **補 CAN source 行（3 列）**：`-037`／`-044`／`-085` —— 取得 `Send CAN` 步後兩條件皆成立（R-CAM3(f)）。
- **`Body_Types` 改 label 式（2 列）**：`-073`／`-074`（DECISIONS 6-24）；
  `Vehicle_Line_Configuration = 130 (HDCC (82 Hex))` 之巢狀括號**不改**（審閱 §二-3）。

### 3.4　五目錄之 lint ＋ selfcheck（修正後）

```
pilot02 （10）U=1  I-cross=10                    batch02a（26）J=5  U=4   I-cross=26
batch01 （23）U=4  I-cross=23                    batch02b（22）U=2  I-cross=22
batch01b（13）U=17 I-cross=13 X=3                batch02c（23）J=3  U=11  I-cross=23
ERROR 類（A/B/C/D/E/F/G/H/I/K/L/M/N/P/Q/R/T/V/W/Y/Z）六目錄皆 0；`J` 為逐字豁免
selfcheck_camera（八項，117 列）：1/2/3a/3b/4/5/6/7 命中皆 0（常數錨 2、合一錨 2）
```

---

## §4　batch03 選列計畫（只計畫，未生成）

`features/camera/data/batch03_plan.tsv`（**142 列**，欄位
`swe_id, test_set, source_id, anchor, vf_chapter, verification_point, axis, tc_count, note`）——
九個 SWE 列之 **142 個來源引用逐列在案**。

| SWE | Test Set | 全 | 查無 | OoS | 委派 | in-scope | **計畫 TC** |
|---|---|---:|---:|---:|---:|---:|---:|
| `SWE-CAM-007` | LVDS Messaging | 1 | 0 | 0 | 1 | **0** | **0** |
| `SWE-CAM-008` | LVDS Messaging | 12 | 0 | 1 | 1 | 10 | **10** |
| `SWE-CAM-009` | LVDS Messaging | 27 | 0 | 7 | 18 | 2 | **2** |
| `SWE-CAM-010` | LVDS Messaging | 20 | 0 | 4 | 9 | 7 | **11** |
| `SWE-CAM-011` | LVDS Messaging | 30 | 0 | 0 | 0 | 30 | **29** |
| `SWE-CAM-004` | Diagnostics | 24 | 0 | 3 | 0 | 21 | **17** |
| `SWE-CAM-013` | Diagnostics | 3 | 0 | 0 | 3 | **0** | **0** |
| `SWE-CAM-006` | Video Pipeline | 22 | 1 | 0 | 0 | 21 | **17** |
| `SWE-CAM-019` | Video Pipeline | 3 | 0 | 0 | 0 | 3 | **2** |
| | | **142** | **1** | **15** | **32** | **94** | **88** |

依 Test Set：`LVDS Messaging` **52**／`Diagnostics` **17**／`Video Pipeline` **19**。

### 4.1　**兩個 SWE 列之 in-scope 為 0**（升級條件之同類，回報）

- **`SWE-CAM-007`**（`Other LVDS / General Protocol`）：唯一來源 `SYS-RA-VF551_V42-216`
  依 R-CAM10 委派 `SWE-CAM-006`（`-006` 之 SWE ID 較小）。該列**整條無 TC**。
- **`SWE-CAM-013`**（`Message: diagnosticRequest / Response`）：三個來源全數委派
  `SWE-CAM-004`。該列**整條無 TC**。

二者之覆蓋台帳須記「No TC — all sources delegated」，**歸 Pei** 確認其形制
（bed_lowering R-BLM2／popup R-POP5 為 Heading 列之前例，此處為**委派**而非 Heading，形制不同）。

### 4.2　切法建議（88 > 40）

| 批 | 範圍 | TC |
|---|---|---:|
| **batch03a** | `SWE-CAM-008`（10）＋ `-009`（2）＋ `-010`（11） | **23** |
| **batch03b** | `SWE-CAM-011`（29） | **29** |
| **batch03c** | `SWE-CAM-004`（17）＋ `-006`（17）＋ `-019`（2） | **36** |

三批皆 ≤ 40。`03a`／`03b` 同屬 `LVDS Messaging`（52 一批過大，依訊息群切）；
`03c` 併 `Diagnostics` 與 `Video Pipeline` —— 二者共用 `systemStatus.*` 之 DTC 語意，
併批可使 R-CAM10 之委派關係在同一批內可見。

### 4.3　難點回報（下放包 §3 所令）

| # | 難點 | 實測／處置建議 |
|---|---|---|
| **A** | `-008` 之 `two LVDS message cycles`（`V2-479`／`-480`）| LVDS 訊息週期之值於 SYS2 查無；ER 若要判「兩週期」須先知週期。建議該二列之時序面標 PENDING（新 DR），值切換之**結果**仍可判 |
| **B** | **DTC 之讀取無命令句式可抄** | 實測：交付語料 17 本之 275 條 `$ ` 命令行中，與 `DTC` 同格者 **0**；`features/*/generated/` 全部 json 之 `$ ` 命令行共 **9 條，全在 camera 本身**（`adb logcat`／`dumpsys media.camera`）。**故無可抄之 UDS／DTC 句式** —— 建議 `-004`／`-006` 之 DTC 面以「以診斷工具讀取 DTC 並檢查其狀態」之散文書寫，**不造命令**（同 LVDS 之處置，A-CA27 結論之延伸）。若 Pei 可提供 Diagnostics 線之交付本，改抄其式 |
| **C** | `-011` 之兩列來源疑似誤植 | `V3-281` 寫 `gate BH-CAN STATUS_CCAN5.LWSAngle to LVDS STATUS_CCAN5.LWSAngle`（標的應為 `vehicleUpdate_2.LwsAngle`，其 `a)` 子句即如此寫）；`V3-283` 之 `a)` 寫 `send LVDS STATUS_CCAN4.ReverseGearSts = [SNA]`（應為 `vehicleUpdate_2.ReverseGearSts`）。**逐字不改**，生成時以 `a)` 子句之正確標的為 ER，並登 RD_FEEDBACK（建議 RDF-08／09），**歸 Pei** |
| **D** | `-004` 之 `V42-590` 內容誤植 | 其節為 `Communications_Timeout`（`-589`）之清除側，但句子寫 `Once the systemStatus.InternalErrorStatus signal is set to "False"` —— 與 `-584` 逐字同句。plan 記 covered_by `-584`，該 Test Set 因而**缺 Communications_Timeout 之清除側**。建議一併登 RD_FEEDBACK |
| **E** | `-006` 之 `V42-210` 轉指 HMI／PDO | `scale the image … according to PROXI Radio_Display_Type. Refer to HMI and PDO documents for the correct aspect ratio and resolutions` —— PDO 不在素材（同 `-092` 之 DR-CAM-m 範圍）。建議 ER 只驗「依 `Radio_Display_Type` 縮放」而不比對具體解析度 |
| F | 摘句量多 | `-009` 之 `V2-486`（80t）、`-006` 之 `V42-263`（73t）／`V42-226`（59t）／`V42-219`（57t）／`V42-592`（58t）／`V3-533`（69t）、`-010` 之 `V42-326`（73t）、`-008` 之 `V3-284`（78t）、`-004` 之 `V33-429`（65t）—— **九列須摘句**，皆逾 50 token |

`vehicleUpdate_*` 之 raw：四本 DBC 皆無 LVDS 訊號，故一律以來源 label 逐字（profile §7.2 已明文）。

---

## §5　自報錯誤

1. **CAM-08 之 `-082`／`-087` 誤勾 DT27**（V4 本只對應 HDCC27，R-CAM11）。本輪自查發現並更正，
   一併適用於 batch02c 之六列 V4（`-111`～`-116`，皆只勾 HDCC27）。
   **此為 CAM-08 落檔時之判斷疏漏，非審閱所指**。
2. **selfcheck 第 3 項「雙向」之解讀為本包所定**（§1-4），與審閱字面可能不同，已具名請裁。
3. **batch02c `-099`／`-100` 之章節歸屬為判讀** —— V2 本 `VF章節` 欄於 `-499`～`-511` 區
   **落後一組**（`-502` 標 `1.13.2.1.5` 而其為該節首句）。本包據此判 `-501` 屬 Audio Mode ON、
   `-507` 屬 Audio Mode OFF；若審閱認定另一讀法，兩列可合為一列。**歸 Pei。**
4. **`TIME_W_RVC`／`TIME_W_RVC2` 未自行補入 profile §9** —— 該表之內容由審閱 §一-9 所定，
   本包只於 §2-6 回報，未逕自增列。
5. **DR-CAM-o 為生成中新發現**（`-113` 之語言 raw），非下放包所令，已補登。

---

## §6　未結

| # | 項 | 去處 |
|---|---|---|
| 1 | DR-CAM-a／f／g／h／i／j／k／l／m（既有九條）| 補件；f／i 交付前必結 |
| 2 | **DR-CAM-n（新）** Atl-Hi PROXI `Body_Types` 列舉 | 補件 |
| 3 | **DR-CAM-o（新）** `LanguageSelection` 語言 ↔ raw | 補件 |
| 4 | §1-4 之「雙向」解讀 | Pei 裁 |
| 5 | §2-6 之兩個常數是否入 profile §9 | Pei 裁 |
| 6 | §4-1 之兩個 in-scope ＝ 0 之 SWE 列，其覆蓋台帳形制 | Pei 裁 |
| 7 | §4-3 難點 A（LVDS 週期）／B（DTC 命令）／C（兩列誤植 → RDF-08/09）／D（`V42-590` 誤植）／E（PDO 轉指）| Pei 裁 |
| 8 | §5-3 之 `-099`／`-100` 章節判讀 | Pei 裁 |
| 9 | **batch03a／03b／03c**（23／29／36） | 下包 |
| 10 | `HMI Overlays`（`-021`／`-022`／`-023`／`-025`）與 `AUX Camera`（`-024`，DR-CAM-a 全 BLOCKED）| 下包 |
| 11 | GCB-07 與 GC 線其餘六項 | GC 線 |
| 12 | `-058`／`-092` 之 `dumpsys` 輸出實機驗證 | 交付前 Pei |

---

## §7　本包寫入之檔案（git 未 commit）

```
M  features/camera/DECISIONS.md                    6-23 ～ 6-27
M  features/camera/DATA_REQUESTS.md                DR-CAM-n／DR-CAM-o
M  features/camera/scripts/selfcheck_camera.py     第 3 雙向 ＋ 第 7 項   sha256_16 781cfb2d3f79deaa
M  docs/runtime/profiles/FW036_R1L_Camera_Profile.md  新 §4.1／§7.2       sha256_16 b452935a975a0946
M  features/camera/generated/batch01b/             -037／-044 ＋ LINT_REPORT
M  features/camera/generated/batch02b/             12 列 ＋ LINT_REPORT
?  features/camera/generated/batch02c/             23 × json ＋ 23 × md ＋ INDEX ＋ LINT_REPORT
?  features/camera/generated/DIFF_CAM08_to_CAM09.md                       sha256_16 a0b028c26724e92c
?  features/camera/data/batch03_plan.tsv           142 列                 sha256_16 91a1edf62bc5f608
?  docs/fw036/handoff/up/20260923_CAM-09.md        （本檔）
   features/camera/RULINGS.md                      未動（本輪無新條文）   sha256_16 8015aa6fcfe69c8a
   features/camera/generated/pilot02／batch01／batch02a                    未動
```

`batch02c` 之 23 份 json sha256_16：

```
-095 2afc6e02ba60bc83  -096 5a5b97233fd62352  -097 7daacaa5e83ba4da  -098 9476d5950954727a
-099 b394475ca541431d  -100 de6480434bd797b8  -101 7c553cee78890e13  -102 7dbc26ffdd961142
-103 add13570efae8d5c  -104 f536ca13d96c7914  -105 c5f4c6c051d0a63c  -106 e38a91b1cfc361b1
-107 0305c815a0faf30d  -108 e49ad6a1d08d6399  -109 75bb8ceb52128691  -110 6b18cb2371f84908
-111 d26427ac84fab99c  -112 e14187dbb8fff9a6  -113 e7e19066e66c9fae  -114 0ec864eb79cd375c
-115 5ccc6693c6456738  -116 3f481b135f41ed98  -117 b0b7e9ce908689c4
```

### 複驗指令

```bash
python3 features/camera/scripts/selfcheck_camera.py \
        features/camera/generated/pilot02  features/camera/generated/batch01 \
        features/camera/generated/batch01b features/camera/generated/batch02a \
        features/camera/generated/batch02b features/camera/generated/batch02c
python3 features/camera/scripts/lint_batch.py <暫存目錄> features/camera/generated/batch02c
```
