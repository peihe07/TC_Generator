# 26 — W-P3 下放包（改善案收尾：prompt 指紋 + 自查表對映 + 三項裁定落地）

> **[SUPERSEDED by 26_wp3_closeout.md — 2026-08-24]**
> 同輪三份 26 包之最早一份（15:10）。Pei 裁定以 `26_wp3_closeout.md` 為準。
> **內文一字未改**；依 R-G23（26 包 §C [DEFAULT]）標記不刪。

日期：2026-08-24
所據上繳：`docs/fw036/upstream/25_wp2.md`（覆核 PASS）
上繳：`docs/fw036/upstream/26_wp3_final.md`
本包為流程改善案（23~26）之**最後一包**。

**裁定記錄**：Pei 2026-08-24 chat「1准 2准 3准」——
25 覆核之三項建議全數照准，條文見 §C。另三件已於覆核時定案：
裁定 D 落點依 25 包（R-VS82）為正；IN §8.7.5 範圍註記經 commit 視為
裁定通過；selection 欄自下一批起記算式而非結果（[DEFAULT]）。

**單一發包源**：本案（`docs/fw036/handoff/`）自本包起僅由本對話發包，
並行 session 不得寫入本目錄（R-VF66 之形態於 fw036 層適用；
`23b_wp2_supplement.md` 分歧事件為其成因）。

---

## A. 禁區

- 全部 git 操作屬 Pei（R-G5@`9814d24c`），**除 §D-2 之窄口唯讀授權**
- 歷史 handoff / upstream 檔不改寫（R-G18@`8f61f9fd`）
- 已交付件與 done region 不動；`generated/*.json` 之既有內容不動；xlsx 不碰
- `features/user_profiles/`、`_intake` 不碰
- 既有 16 份 lint 報告檔**不重命名**（裁定 3 之邊界）

## B. 背景

W-P1／W-P2 已 commit。本包收掉：W-P3 原兩項（R-G19 prompt 指紋、
R-G21 自查表對映）、25 覆核三裁定之落地、GATES.tsv 補全、
與 25 上繳獨立判斷之可收項。

## C. 裁定條文（Pei 2026-08-24 追認；可直接貼入）

### 裁定 1a — R-VF83 之但書（落 `features/vehicle_setting/RULINGS.md`，錨點 `R-VF83+但書`）

```
R-VF83 但書：重生作廢之產出所佔序號成為具名缺號 —— 不補、不重編、
不回收（A-VS02 先例）。缺號須開異常登記，列明號段與成因查證結果；
「連續遞增」之檢查對具名缺號豁免，豁免以異常編號為據。
（Pei 裁定 2026-08-24，25 上繳 §五-3）
```

### 裁定 1b — A-VF{n}：seq 248–257 十號缺口（落 `vehicle_setting/ANOMALIES.md`，取號依現行實測，防碰撞）

```
A-VF{n}：vf230 產出之 seq 於 248–257 連續十號無任何產出佔用
（pilot1 = 238–247，pilot2 = 258–267；跨批聯集實測，25 上繳 §五-3）。
每批自身連續故逐批檢查必然全綠，僅跨批聯集可見。成因查證見本條附記
（§D-2 之查證結果回填）。處置：依 R-VF83 但書具名為缺號，不補不重編。
狀態：RESOLVED（但書落檔＋本條登記即為處置完成；成因欄允許「查無」）。
```

### 裁定 2 — R-G23：閘之接入（落 FO §9.2）

```
R-G23：`lint_docs036.py --gate`、`canon_refs.py --waiver --gate`、
`rulings_hash.py --check`、`gates_tsv.py --check` 四支為上繳前必跑項，
其輸出附於上繳包；任一 exit 非 0，該包不得上繳，先修或升級。
`expected_numbers.py --gate` 之接入延至其對照基準歸零後另裁 ——
已知會紅而被容忍之閘不得接入（G-D）。
（Pei 裁定 2026-08-24，25 上繳 §八）
```

### 裁定 3 — 報告檔名（實作指示，不立條號；Pei 准 25 覆核建議 3）

新產報告採 `{tag}_{來源檔 sha8}_{今日}`；**既有 16 份報告檔一律不動**。
回歸向由「其餘檔案之報告檔名不變」改寫為「**既有報告檔案不被重命名**」
（G-N 之字面案例隨之更新）。

## D. 作業清單

1. **裁定落地**：§C 四塊入檔（1a／1b／2；A-VF 取號先實測現行最大號，
   碰撞即停）；`rulings_hash.py` 重產 tsv；G-N 回歸案例更新（裁定 3）
2. **seq 成因查證**（裁定 1，一次性）：
   **窄口唯讀 git 授權（本包限定，Pei 2026-08-24）**：得執行
   `git log --oneline --all -- 'features/vehicle_setting/generated/*'` 與
   `git show <sha>:<path>`，僅此二形；一切改狀態 git 仍禁。
   另查 `output/`（gitignored，檔案系統直讀）。
   結論三分：證實 pilot #2 v1 佔用／查無佔用證據／其他成因。
   **若指向寫回偏移（成因 c），停下升級** —— 那不適用但書。
   查證結果回填 A-VF{n} 成因欄
3. **R-G19 落地**：`make_batch_context.py` 輸出 `prompt_sha8` 與
   `exemplar_set_sha8` 入批次 manifest；自下一批起生效，既有 manifest 不回填
4. **R-G21 落地**：IN §9 自查 17 項逐項對映 —— 有對應閘者記 `gate_id`
   入 `GATES.tsv` 新欄 `selfcheck_items`，無者標「人工項」；
   對映之驗證取 Home A-H10（引號規則 → 自查第 15 項）確認其對映閘
   實測轉紅（G-K），另取一項人工項確認無閘可對映（R-G9 範圍向）
5. **GATES.tsv 補全**：`lint_docs036.py` 之檢查項與
   `features/*/scripts/lint_tcs.py` 現存之閘入簿（掃描面回報）；
   `effective_date` 自上繳包與 `lint_reports/` 回填，**查無者維持「未載明」**，
   不用 git 推測（§D-2 之授權不及於此）
6. **裁定 3 實作**：`lint036.py` 報告檔名改制；以 25 上繳 §九-2 之 18 組
   中抽 2 組實測新報告不互覆；既有 16 份檔案 byte 不動之驗證附上
7. **25 上繳獨立判斷之收項**：(a) 第 6 項 —— 全 repo 掃 15 個新入 tsv
   錨點之編號是否曾被 `@sha8` 形式引用（預期 0，實測回報）；
   (b) 第 4 項 —— `expected_numbers.py` 對 `cfts044` 側 smoke 跑一次，
   只回報可跑性與 selection 欄格式一致性，不作為驗收
8. 全套 pytest；上繳

## E. 預期數字

| # | 指標 | 預期 |
|---|---|---|
| 1 | `RULINGS.sha.tsv` 錨點數 | 188 → **190**（+`R-VF83+但書`、+`R-G23`）|
| 2 | 重複 ruling_id 組數 | **0** |
| 3 | A-VF 新開 | **1** 條（取號回報）|
| 4 | 四支 gate（R-G23）實跑 | **全部 exit 0** |
| 5 | GATES.tsv 閘數 | 20 → **20 + 新入簿數**（未知，回報實測；G-D）|
| 6 | 自查 17 項對映 | 閘對映數 + 人工項數 = **17**，餘數 0（R-G10）|
| 7 | 新命名報告互覆組數（抽測 2 組）| **0** |
| 8 | 既有 16 份報告檔 | **byte 不動** |
| 9 | 15 錨點之 `@sha8` 既有引用 | 預期 **0**（實測回報）|
| 10 | 全套 pytest 失敗數 | **8**（既有，不得增減）|
| 11 | seq 查證結論 | 三分之一，逐證列出 |

## F. 升級條件

1. A-VF 取號碰撞、或 `R-G23` 編號碰撞
2. seq 查證指向寫回偏移（成因 c）
3. 自查 17 項中出現「無閘可對映且無法歸人工項」者
4. GATES.tsv 補全時發現同 gate_id 於不同工具語意不同
5. manifest 新欄影響既有批次之比對（預期不影響 —— 既有不回填）

## G. 上繳要求

23 包 §G 全項 + R-G13 引用表（本包引用：R-G5@`9814d24c`、
R-G13@`abdc56e3`、R-G17@`24cdeec6`、R-G18@`8f61f9fd`、R-G19@`bd206972`、
R-G21@`1230e795`、R-G22@`bca29f8f`、R-VF83@`9d5bfa4d`、R-VS82@`12177e4f`；
實讀不符即停）+ 四支 gate 輸出 + seq 查證之證據鏈 + 對映表全文 +
獨立判斷（含「本案 23~26 收尾後，改善案是否仍有該做而未做者」之總結項）。

## H. 本包產生之新條文清單（自檢表）

- [x] 裁定 1a（R-VF83 但書）— 區塊已列
- [x] 裁定 1b（A-VF{n}）— 區塊已列
- [x] 裁定 2（R-G23）— 區塊已列
- [x] 裁定 3（實作指示，不立條號）— 已列
- [x] 窄口唯讀 git 授權（§D-2，本包限定）— 已列

本包引用之既有條文：sha8 見 §G；另 G-D、G-K、G-N、R-G9、R-G10、
A-VS02、R-VF66（精神）、25 上繳 §四／§五／§六／§八／§九／§十三、
23 包 §G。
