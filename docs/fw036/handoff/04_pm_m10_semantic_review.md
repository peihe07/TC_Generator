# PM M10 摘句語意覆核（分析層，2026-08-21）

量測條件：對 `features/power/sandbox/b02/pm_remediated.xlsx` 之
test_item 欄，取 L 原違規之 71 列，與交付本原值逐列比對。
下半定義 = **最後一個整行被括號包住之行（`^\(.+\)$`）**；
上半 = 其前之全部行。

## 零、分析層自身工具缺陷之更正（§5a）

初次覆核採「以 `(` 起首之行即為下半」，將 verbatim 原文中之
`(DEFAULT)$VC_VEH_BRAND$ = [Lancia]...` 誤判為括號下半，
據以產生「M15 覆寫六列括號、品牌張冠李戴」之結論。
**該結論為工具缺陷所致之假象，全數撤回。**
複驗確認 rows 212–217 之括號下半
（`(read the displayed font -> The HU displays the Chrysler font)` 等）
逐列完好、未被改動。此即 §5a「抽取型工具之缺陷不會報錯，
須以已知全集驗證」之實例；已改以全集自我驗證（71/71 皆解出下半）。

## 一、分類結果

**defect：2 組 4 列**（非 M10 所致，為既有重複 TC）

| 列 | 內容 |
|---|---|
| 210 / 225 | test_item 上半、括號下半、pre/input/proc/er **四欄全同** |
| 211 / 226 | 同上 |

兩組於回修**前**即完全相同，M10 未造成、亦未掩蓋。屬 §10.6
`duplicate_of` 情形（strict equivalence）。登記 **A-PM04**：
PM 存在 2 組完全重複 TC，須刪列或補區分軸。
**此為內容缺陷，非本批引入，不阻塞批 1 寫回**，另案處置。

**style-divergence：3 列**（rows 239、240、241）

摘句以連接詞起首（`Or if HU changes mode...`）。上繳所列 9 列中，
rows 47、55、58、59、136、137 經複驗實際不以連接詞起首，
真值為 3 列。R-4 僅授權首字母轉大寫、未授權刪詞，執行層未處置正確。

**裁定**：維持現狀，不擴充 R-4。理由：刪除起首連接詞即改動
verbatim，破壞可回指性（同 R-6 之理據）；連接詞起首雖不美觀，
不影響測試目的之判讀。登記為 note，不修。

**note：21 列**（摘句掉了與括號目的有交集之詞）

逐列檢視後，掉落詞多屬列舉項或狀態鍵之其一（如 row 189 掉
`hvac/phone/display`、row 194 掉 `beeps/alerts`），
其保留段落仍涵蓋測試目的之核心行為。**無一列因摘句而無法判讀
測試目的**。row 32（下放包所指弱例）之括號目的 `"Sleep"` 於
verbatim 上半原文即不存在，屬 037 與 036 之目的敘述落差，
非 M10 造成。

**52 列摘句後上半逐字相同（20 組）**：經正確解析複驗，
其中 18 組之括號下半**逐列相異、可完全區分**（符合 canon §4.3
「the tag IS the distinguishing token」）；僅上述 2 組為既有重複。
**M10 未破壞 sibling 可分性。**

## 二、判定

M10 之 71 列摘句**通過語意覆核**。無 defect 歸因於 M10。
批 1 之交付前阻塞項**解除**。

## 三、後續登記

- **A-PM04**：rows 210/225、211/226 完全重複 TC（2 組 4 列）
- **M17-PM**：A 禁用動詞 20 列（見 03_batch1_review §一）
- **M16-PM**：spec_reference R-2 家族遷移 283 列
- 連接詞起首 3 列：note，不修，不再列入後續批次
