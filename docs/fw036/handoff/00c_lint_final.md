# 下放包 00c：N 豁免裁定 + M/J 基準補校（批 0b 覆核後）

批 0b 判定：**通過**。13 項校準全中；J 8→0、N 7093→2 兩項修訂
各自生效；交叉驗證之 39,745 行 / 6,704 尾句號與分析層實測逐項
等同；兩項質疑撤回並固化為正向測試。唯讀自證充分。

## 裁定 1：N 子步驟豁免 —— 採 (b)，但限定範圍（分析層自裁）

19 行差額之處置屬「量測與掃描定義」，分析層得自裁，不上呈 Pei。

**裁定**：a./b./c. 縮排子步驟為實質測試步驟，非續行。canon §6.1
明訂子層以 `a./b./c.` 表達測試內容；§11「Applies to every
numbered item」涵蓋之。N 應計入 → **N = 6,704**。

**限定（重要，勿全域套用）**：僅將 `^\s*[a-z][.)]` 加入
**N 檢查之自身行定義**。**編號行之全域定義不動**，E 檢查沿用
`^\s*\d+[.)]`。

**理由（實測）**：若全域併入，E 將漂移 —— DealerMode 1→16、
Media 1→10、HFP 5→8、BT 1→2、PM 0→1（Projection 5→5 不變）。
Media 之 E 校準基準將由 1 變 10，破壞既有校準錨。§6.1 之 ER
子層本就不與 proc 步驟一一對應，全域併入會產生系統性假違規。

`$` 指令行豁免維持不動（8 本中無帶句號之 `$` 行，故不影響數值）。

## 裁定 2：M 基準補校（分析層實測，可校準）

M 定義（pre/proc/er/spec 四欄，strip 後空、非 NA/N/A、
非 `PENDING:` 前綴）實測基準：

| 本 | pre | proc | er | spec | 小計 |
|---|---|---|---|---|---|
| BT | 11 | 1 | 2 | 0 | 14 |
| DealerMode | 54 | 0 | 0 | 0 | 54 |
| HFP | 15 | 0 | 1 | 23 | 39 |
| Home | 5 | 0 | 0 | 0 | 5 |
| Projection | 1 | 1 | 2 | 92 | 96 |
| AMFM / Media / PM | — | — | — | — | 0 |
| **總計** | | | | | **208** |

與上繳 M=208 相符 → **M 改標「已校準」**。Media M=0 無區辨力之
判斷正確，故改以全語料分佈為基準（三本為 0、五本有值，
逐本比對即具區辨力）。

## 裁定 3：J 剩餘 20 筆基準（計數口徑釐清）

分析層列計得 12、行計得 20，與上繳 20 相符 → **口徑為行計**，
J 改標「已校準」。全 20 筆分佈（除此之外應為零，可證不漏報）：

- BT 行計 14（rows 160/172/180/184 `create exception...`、
  438 `verify Android FW...`、439 `during Android FW...` 等）
- PM 行計 2（rows 20、204，test_item，verbatim 中段起抄型 → R-4）
- Projection 行計 4（rows 429–432，test_item `if the current...`
  → R-4）
其餘 5 本 J=0。

**口徑統一要求**：報告須於表頭標明各檢查係列計或行計；
現行 D=275（列計）與 J=20（行計）並列易誤讀。全部改為
**行計為主、附列計**。

## 執行指令

```bash
cd /Users/peihe/Work_Projects/TC_Generator
# 1. N：加入 ^\s*[a-z][.)] 至 N 自身行定義（勿動全域編號行定義）
# 2. 報告表頭增列「計數口徑」欄（行計／列計）
# 3. M、J 由「未校準」改「已校準」，基準值照本包表格寫入
pytest tests/test_lint036.py -q   # 新增：E 不受 N 豁免變更影響之迴歸測試
python3 scripts/lint036.py <8 本> --report-dir docs/fw036/lint_reports
```

## 校準基準（最終版）

Media 0625：A=0 B=0 C=1 D=0 E=1 F=0 G=0 H=0 I=2 J=0 K=0 L=0 N=2 M=0
全語料：N=6,704 M=208 J=20 K=1,736；E 逐本 {BT1, DM1, HFP5, Media1,
PM0, Proj5, AMFM0, Home0}（**E 值不得變動，變動即 N 豁免外溢**）
總違規：10,659（= 10,640 + 19）；以重跑實測為準。

仍無基準者：L（待 R-3 閾值）、K 分級（待 R-5）、I-sibling
（PM=104 已登記 M15，待回修時逐列覆核）。

## 上繳

`docs/fw036/upstream/00c_lint_final.md`：最終校準表、E 迴歸證明、
總違規分項、「該驗未驗」獨立判斷、引用裁決清單。新規 0 條。
