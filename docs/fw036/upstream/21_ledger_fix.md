# 上繳包 21：台帳修正（PM 最後一包）

下放：`docs/fw036/handoff/21_ledger_fix.md`
日期：2026-08-21　　**純文件，工作簿未動。**

| 項 | 值 |
|---|---|
| 工作副本 | `features/power/sandbox/b19/pm_19.xlsx`（`b4dd5ca0…`）—— 雜湊未變 |
| 改動 | `RULINGS_LEDGER.md`、`features/power/ANOMALIES.md` |

---

## 一、§一 R-13 回復全文 ＋ R-15 新立

R-13 已回復為 19 包之全文（含「不得代以語意相近之他訊號」與
「DBC 對應缺漏登記 DR 向上游查詢」兩個可操作子句）。
R-15 已新增（ACTIVE，源 21）。

## 二、§四 R-1 v2 → SUPERSEDED、R-1 v3 → ACTIVE

主表：
```
| ~~R-1 v2~~ | … | ~~CAN 訊號採 `Send CAN: …`，PROXI 加 `$`~~ | **SUPERSEDED** | 09 | 全案 |
| R-1 v3     | … | 訊號一律 `$<MESSAGE>.<Signal>$ = <raw> (<label>)`… | ACTIVE | 12、17 | 全案 |
```
兩段加註（R-1 v2 撤銷理由、R-1 v3(d) 修訂沿革）逐字寫入。
**台帳與交付內容至此一致** —— 283 列所依之 v3 已為台帳之 ACTIVE 條文。

## 三、§五 S6 加註

S6 列後加 `↳ **併見 R-14**：`PENDING` 說明之語言一律英文；
中文描述置於 `DATA_REQUESTS.md`」。

## 四、§三 A-PM17 登記

已入 `ANOMALIES.md` 之 A-PM 系列表，含成因、實例（19 包 R-13 插入
破壞 R-2 之指涉）、「無任何檢查可攔」與後續納入 `docs_structure` 檢查。

---

## 五、⚠ 一項逾下放字面之處置：落檔位置表之「同上」全部展開

**§三僅令登記 A-PM17，未令改表。但本包一寫就複現了該缺陷。**

§四要求 `R-1 v2` 標撤銷、`R-1 v3` 新增於其後。落檔位置表原第一列
`R-1 v2 | docs/runtime/ASPICE_SWE6_AI_Instruction.md §8.7.5` 為
其下 `R-6 | 同上 §8.7.5 末段` 之指涉來源；插入指向 handoff 檔之
`R-1 v3` 列後，**`R-6` 之「同上」即錯指為 handoff 檔** ——
與 19 包所犯者同一形態，時隔兩包再犯。

執行層之處置：**將該表 7 處「同上」全部展開為完整路徑**
（R-6／R-8／R-2／R-3／R-4／R-5／R-10），並於表首加禁用註記：

> **本表不得使用「同上」串接指涉**（A-PM17）—— 插入列會靜默改變
> 其後各列之指涉對象且無檢查可攔。各列一律書寫完整路徑。

理由：A-PM17 之處置若僅止於「登記＋待 lint 包加檢查」，
則在該 lint 包完成前，**每一次插入列都會再犯一次**，而本包已是
第二次。移除「同上」即移除該缺陷之成因，非僅偵測之；
展開後各列指涉逐一核對無誤，語意零變動。

**請追認。** 若分析層認為表格應維持簡寫形式，可回退，
惟屆時 A-PM17 之風險持續存在至 lint 包完成為止。

---

## 六、本包是否仍有該驗而未驗者 —— 執行層獨立判斷

**有，三項。**

1. **`ASPICE_SWE6_AI_Instruction.md` §8.7.5 之本文未同步。**
   台帳已將 R-1 v2 標 SUPERSEDED，但落檔位置所指之
   `docs/runtime/ASPICE_SWE6_AI_Instruction.md` §8.7.5 **仍載 v2 條文**
   （執行層未改該檔）。台帳與 canon 本文因而不一致 ——
   **與本包所修者為同一形態，只是換了一份檔案。**
   R-1 v3 之全文目前僅存於 handoff 檔，未進 canon。
   建議：v3 條文寫入 canon §8.7.5，v2 段落依 R-TM13 加註保留。
2. **`lint036.py` 之 module docstring（S3 落檔處）與檢查 P 仍為 v2。**
   §五之 lint feature-scoped 包尚未執行；在其完成前，
   **lint 之 P 判準與台帳之 ACTIVE 條文相牴觸**。
   現況為 P=10 全落在明令不動之 `test_item` 括號下半，故不影響驗收，
   但該一致性缺口仍在。
3. **R-15 本身無法自動檢驗。**
   「條文全文不得簡寫」須比對下放包字串與台帳字串是否等同，
   而下放包為自由格式 markdown。§五之 `docs_structure` 檢查
   所列四項（編號連續、狀態值合法、指涉正確、條文欄非空）
   **不涵蓋 R-15**。建議該包一併評估可行性，或明記為人工項。

---

## 七、引用之裁決編號

**R-15（本包新立）**、R-1 v2（本包標 SUPERSEDED）、
**R-1 v3（本包入 ACTIVE）**、R-13（本包回復全文）、R-14、
R-TM13（撤銷加註不刪除）、S5（裁決台帳制）、S6（本包加註）。

**新登記異常**：**A-PM17**。

---

## 八、未做之事

- **未改工作簿** —— `pm_19.xlsx` 雜湊 `b4dd5ca0…` 未變。
- 未改 `scripts/lint036.py`、未改 `docs/runtime/ASPICE_SWE6_AI_Instruction.md`。
- 未送達、未覆寫任何交付本。

---

> **PM 之分析／執行層工作至此結束。**
> 餘下為 Pei 之授權與送達 —— 依 `DELIVERY_NOTE.md` §8.4，
> 前次授權**不及於**四個長欄位改寫後之版本，須重新取得。
