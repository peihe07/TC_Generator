# 上繳包 20：PM 收尾（台帳補記、DR 證據補強、lint 缺口）

下放：`docs/fw036/handoff/20_pm_closeout.md`
日期：2026-08-21　　**本包不改工作簿內容，僅文件。**

| 項 | 值 |
|---|---|
| 工作副本 | `features/power/sandbox/b19/pm_19.xlsx`（`b4dd5ca0…`）—— **未動，雜湊未變** |
| 改動 | 三份文件，無 `.xlsx` 寫入、未執行任何 `apply.py` |

---

## 一、§二 —— `RULINGS_LEDGER.md` 補記六列

六列已逐字寫入主表（R-8 之後）：

```
| R-9  | 2026-08-21 | Pre-Condition 一條件一行一編號 | ACTIVE | 13 | 全案 |
| R-10 | 2026-08-21 | 空白與字元正規化（分區適用） | ACTIVE | 13 | 全案 |
| R-11 | 2026-08-21 | 一觀察點一步驟／須寫出應觀察值／Input 一律 NA | ACTIVE | 14 | 全案 |
| R-12 | 2026-08-21 | Pre-Condition 句式與排序（工具行置末） | ACTIVE | 15 | 全案 |
| R-13 | 2026-08-21 | 規格訊號名與 DBC 不符時保留原文名 | ACTIVE | 19 | 全案 |
| R-14 | 2026-08-21 | PENDING 佔位說明一律英文 | ACTIVE | 20 | 全案 |
```

R-12 之加註亦逐字寫入。

### ⚠ 三處須追認

1. **R-13 之標題被覆寫。** 19 包時執行層所寫者為
   「規格原文所載之訊號名，即使 DBC 查無同名亦保留原名，
   **不得代以語意相近之他訊號**；**DBC 對應缺漏開 DR**」。
   本包依「逐字寫入」改為下放所給之
   「規格訊號名與 DBC 不符時保留原文名」。
   **後者較短，且刪去了兩個可操作子句** —— 而「不得代以語意相近之他訊號」
   正是 17 包 §五出錯之處，「缺漏開 DR」則是 DR-PW21 之依據。
   台帳規則載「條文全文僅於本台帳落檔一次」，若標題即全文，
   本次覆寫實質削弱了條文。**請確認採何者。**
   （條文全文另在 `19_pm_final.md` §一，落檔位置欄已指向該處，
   故操作意涵未失，惟台帳自身不再承載。）

2. **「條文落檔位置」表補了六列，逾下放字面。**
   下放僅指定主表六列與 R-12 加註，未提落檔位置表。
   執行層補入 R-9～R-14 之落檔指向（13／13／14／15／19／20 各包），
   理由：R-13 於 19 包已有該欄，僅其一有而其餘五條無即不一致。
   **屬執行層自行補足，請追認或指示移除。**

3. **19 包所引入之一處缺陷，本包順手修正。**
   19 包將 `| R-13 | …19_pm_final.md §一 |` 插在落檔位置表之
   `R-8` 與 `R-2` 之間，而該表以「同上」串接指涉
   （`R-8 同上 §10.7` → 指 `ASPICE_SWE6_AI_Instruction.md`）。
   插入一個指向他檔之列後，其下 `R-2 | 同上 §10.7` 之「同上」
   即錯指為 handoff 檔。本包已將 R-13 連同新五列移至 `R-5` 之後，
   「同上」鏈回復。**此為執行層前包之疏失，於此自陳。**

---

## 二、§三 —— DR-PW21 證據補強

分析層之實測段已逐字補入 `DATA_REQUESTS.md` 之 DR-PW21 條文，
含 DBC `VAL_ 854 PowerModeSts` 全列舉、CFTS009-4941562 原文、
「二值逐字相符（僅 ON/On 大小寫）」之研判、以及**若上游確認後
row 72 之應改寫法**（`$STATUS_BH_BCM1.PowerModeSts$` = 0／1）。

一併載明分析層之立場：**現行寫法在 R-13 下正確，維持不動**；
「規格名 = DBC 某訊號」之認定屬上游職權（§8.4.1）。
row 72 內容**未動**。

---

## 三、§四 —— A-PM16 登記

已入 `ANOMALIES.md` 之 A-PM 系列表：

> **A-PM16** `verify.py` 之 `read_without_value` 未覆蓋 ER 側。
> 該檢查僅施於 `proc`。row 291 之 ER 2 現為純
> `PENDING: DR-PW22 (…)` 行，不含 `check that`，**屬未覆蓋而非通過**
> —— 若日後 ER 側加檢，該列須另計。俟 lint feature-scoped 改寫時納入。

---

## 四、§一／§五 —— 追認與排入，無執行項

- §一 R-14 成文：已入台帳（見 §一）。PENDING 措辭之處置獲追認。
- §五 lint feature-scoped 改寫：**另立包**，本包未動 `scripts/lint036.py`。

---

## 五、本包是否仍有該驗而未驗者 —— 執行層獨立判斷

**有，四項。**

1. **`R-1 v3` 完全不在台帳上，而 `R-1 v2` 仍列 ACTIVE。**
   12 包以 v3 取代 v2 之 (a)(c)(d)（訊號一律 `$MESSAGE.Signal$`、
   PROXI 不加 `$`、內部訊號處置），17 包 §三又修訂 v3(d)。
   **本次 283 列之訊號寫法全部依 v3**，而台帳所載之 ACTIVE 條文為 v2 ——
   **台帳與交付內容不一致，且方向與 R-8 那次（條文對抗已交付工作）相同。**
   本包補了 R-9～R-14 卻未觸及此項，因下放未列。
   **建議下一包一併處置**：v2 標 SUPERSEDED 並加註，v3 入 ACTIVE。
2. **R-14 與既有 S6 之關係未載。** S6 為「缺件 PENDING 佔位」，
   R-14 規範其說明之語言。二者未互相指涉，台帳亦無層級欄。
   後續若有人只讀 S6 而未讀 R-14，即可能再犯本次之錯。
3. **R-14 無任何自動檢查。** lint K 會攔下 CJK，但那是巧合的重疊 ——
   K 檢查的是「欄位含 CJK」而非「PENDING 說明之語言」。
   若說明改以他種非英文書寫而不含 CJK（例如全形拉丁），K 不會叫。
   併入 §五之 lint 包時建議一併考慮。
4. **本包所改三份文件無任何自動驗證。**
   工作簿有 `verify.py` 與 lint036 兩道，文件層則全靠人眼。
   本次即有一處（§一-3 之「同上」鏈）是執行層自行回查才發現，
   非任何檢查所攔。台帳／DR／ANOMALIES 之結構性檢查目前為零。

---

## 六、引用之裁決編號

**R-14（本包新立）**、R-9／R-10／R-11／R-12／R-13（本包補記台帳）、
R-TM13（撤銷加註不刪除）、S5（裁決台帳制）、S6（缺件 PENDING 佔位）、
§8.4.1（不得代為認定）、canon §1（TC 欄位 English only）。

關聯：**DR-PW21**（證據補強）、**DR-PW22**、**A-PM16**（新登記）。

---

## 七、未做之事

- **未改工作簿** —— `pm_19.xlsx` 雜湊 `b4dd5ca0…` 未變，未執行任何 `apply.py`。
- 未改 `scripts/lint036.py`（§五另立包）。
- 未處置 R-1 v2／v3 之台帳不一致（見 §五-1，逾本包所令）。
- 未送達、未覆寫任何交付本。
