# DECISIONS — Security (FW036)

Generated by recon (Phase 1). Marker semantics per FO §4:

- `[AUTO]` — machine-determined, recorded for audit, no action needed
- `[PROPOSED: value — rationale]` — Pei edits if disagreeing; untouched at
  sign-off = binding as proposed
- `[PEI]` — cannot be proposed; must be filled before sign-off

An unsigned sheet blocks Phase 4+.

---

## 1. Intake（SEC-01 回填，2026-09-16）

- spec_mode: `[PROPOSED: 不適用既有 A–E]` —— 本 feature 無 SYS1 HMI 匯出、無 spec PDF、
  無 popup list；母體為 037（SWRA）＋ SYS3 SYSAD ＋ SYS2 CFTS084 三段鏈。
  FO §3 之 A–E 皆以「規格文件之可錨性」分型，本形態未在其列。Pei 裁是否新增型別。
- Source files present: `[AUTO]` —— **26 個 doc_id**（SEC-01 16 ＋ _C 2 ＋ _D 7 ＋ _F 1；另 `DTCs Matrix` 為既有列補掛 `security`），sha256 全表見
  `sources/MANIFEST.tsv`（`features` 欄 = `security`）與上繳包 §1。
  13 件為追溯母體／素材，其餘為 **reference-only**（CCVR 三件、CDD 群七件、R1L Diag SWQT、CertProfile、VHAL Guide、DTC Matrix）。
- Missing referenced specs: `[AUTO]` —— DR-SEC-a～q 共 18 項（含 `j2`），見上繳包 §7；
  其中 c～h、q 為**測試資產／規格本**（憑證組、USB 金鑰、SAM dongle、CS.212 package），
  非文件，不列 ANOMALIES 之 spec 缺件而列 DATA_REQUESTS。
- Spec release/version pinned: `[AUTO]` —— CertProvider V1.0／KeyInstall V1.0／
  SAM V1.1／ECUCert（無版號）／SwdlSecureLib（無版號）／libLogEncrypt（無版號）。

## 2. Workbook survey

- workbook_state: `[AUTO]` (BLANK / PARTIAL_CLEAN / PARTIAL_INTERLEAVED / FULL)
- Header row index: `[AUTO]`
- Column mapping: `[AUTO — header match n/n]`
- Done-region segments (rows, author, count): `[AUTO]`
- Regen-region segments: `[AUTO]`
- Ambiguous rows (if any): `[PROPOSED: disposition per row — evidence]`
- Draft-region disposition: `[PROPOSED: discard & regenerate — lint
  consistency cheaper than row salvage]`
- Design-method vocabulary: `[AUTO — from 下拉選單 sheet]`

## 3. Coverage（SEC-01 回填，2026-09-16）

- 037 leaf count: `[AUTO: 70]` —— 11／13／19／13／5／9，六本合計，與下放包 §6 相符。
- Covered by done region: `[AUTO: 0]` —— 本 feature 無既有交付本。
- Regen targets: `[AUTO: 70]`，分兩批 —— `ccvr_batch=1` **52 列**、`=2` **18 列**（含 R-SEC5 之 CS.212 直連 11 列）
  （`features/security/data/batch_order.tsv`）。
- Leaves covered nowhere: `[AUTO: 0]` —— 70 列全數歸入 Layer 2（`layer2_assign.tsv`）。
- Parent/child both-leaf duplications: `[AUTO: 0]` —— 037 之 `Analysis Report` 為平表，
  無 Heading／leaf 兩層（與 sw_update 之 `AnalysisReport_FULL` 不同型）。

## 4. Style bindings

- Style authority: `[AUTO: done region | PROPOSED: fallback chain (BLANK)]`
- Test Item shape: `[PROPOSED]`
- Test Group / Test Set columns: `[PROPOSED: blank | fill per framework]`
- spec_reference format: `[PROPOSED — done-region sample or spec_mode template]`
- Author value on new rows: `[PROPOSED: PeiPYHsu]`
- Exemplar source: `[PROPOSED — own done region, or cross-feature style-only]`
- Priority rubric deviations: `[AUTO — report done-region blanks/anomalies]`

## 5. Split & scope

- split_mode: `[PROPOSED: standard]`
- Granularity precedent to follow: `[PROPOSED — cite done-region rows or
  docs.md §8.3 if BLANK]`
- Known scope carve-outs (struck text, external refs): `[PROPOSED per case]`

## 6. Framework & profile —— SEC-01 §7 待裁八項（執行層只提 [PROPOSED]，不自裁）

- Test Set table (Part N draft): 見下放包 SEC-01 §5 案 A；逐列歸屬
  `features/security/data/layer2_assign.tsv`，**30 組 leaf 數與 §5 表逐組相符**。
- Profile 骨架：`docs/runtime/profiles/FW036_R1L_Security_Profile.md`。

| # | 項 | 分析層建議 | 執行層 [PROPOSED] ＋ 實測依據 |
|---|---|---|---|
| 6-1 | Layer 1 案 A／案 B | 案 A | **[PROPOSED: 案 A]**。案 B 之跨元件 Test Set 會使 IN §4.2「同 Test Set 同 setup 型態」失效 —— 實測 setup 差異可證：Cert Provider 走 `adb shell am instrument`（apk），Key Install 走 USB 實體插入，ECU Cert 走 DIAG／Dealer App，三者無共用 precondition |
| 6-2 | ABBR | 案 A 六組／案 B 單序 | **[PROPOSED: 六組 `CP`／`KI`／`SAM`／`ECUC`／`SWDL`／`LOGENC`]**，隨 6-1 案 A 連動。全 repo 現查無此六個 ABBR 之既有佔用 |
| 6-3 | 一本 workbook 或六本 | 一本 | **[PROPOSED: 一本]**。**惟其前提已變** —— addendum §1 改為 70 列全產，非原「in_round ≤ 45 列」；一本 70 列仍在既有交付本之列數範圍內（sw_update 單本逾 200 列），前提不因此翻轉 |
| 6-4 | §10.7(c) `specification_reference` 格式 | `{037 token}_{SWE1-ID}` | **[PROPOSED: 同分析層]**。ECUCert 13 列無 SWE ID，依 **R-SEC3(b)** 以 `Source Requirement ID` 首值代入，例 `SWE1_ECUCert_SYSAD_SEC_ECUCERT_ECUCERT_API` —— 該 13 列之錨在 DR-SEC-a 結案前皆為暫用 |
| 6-5 | Peer Review 列之 blackbox 轉換 | 依任務 3 回報後裁 | **[PROPOSED: `Y` 7 列＋`Y(部分)` 3 列＋`Y(間接)` 2 列轉 Testing；`N` 19 列維持]**。逐列觀察手段與依據見 `features/security/data/peer_review_rows.tsv` |
| 6-6 | SAM-0007 是否納入本輪 | 納入（apk `samCertTestNormalFlow`）| **[PROPOSED: 納入，但須 Pei 明示]** —— 機械判準下 SAM-0007 為 `ccvr_batch=2`（其 SEC 在 `Mapping detail` 無 `Cert Val CS.98` 落點）。`samCertTestNormalFlow` 確在 `Cert Val CS.98` STEPS 內，惟該落點掛在別列 SEC 上。**納入即為人工覆寫 batch，須逐列具名** |
| 6-7 | Harman `Rejected` 語意 | 責任歸屬，不排除 | **[PROPOSED: 同分析層]**。實測：鏈上 161 個 SEC 中 `Accepted` 88／`Rejected` 53／空 20。`Rejected` 逐列見上繳包 §4-5 —— 集中於 SWDL 與 ECUCert 兩鏈，與「MD 承接」之解釋相容，惟 **SYS2 原文未明寫其語意，本項仍屬推論** |
| 6-8 | Log Encrypt 本輪 0 列 —— 是否整本延後 | 延後，登 out_of_round | **[已由 addendum §1 取代]** —— 70 列全產，Log Encrypt 9 列改為 `ccvr_batch=2`，不延後、不排除。`batch_order.tsv` 已如此落。本項無須再裁，列此備查 |

- Contested attributions: `[PEI]` —— 6-6（SAM-0007）為唯一機械判準與分析層建議相左者。
- Profile [OVERRIDE] clauses needed: `[PROPOSED]` ——
  (1) `[ADD §5.8]` 固定入口點擴增四項（`adb shell`／`USB drive`／`DIAG tester`／`Dealer App`），
  否則 `X` 對 70 列整批報 WARN 而無訊號；
  (2) `[ADD §10.7(c)]` 見 6-4；
  (3) `[ADD §5.3]` 常數候選見 profile 3.3；
  (4) `[ADD]` `step_sources` lint（R-SEC4(a) 之 `source:` 標記），代號待指派。

## 7. Execution

- Batch plan (count, order, pilot batch): `[PROPOSED]`
- Model assignment per batch: `[PROPOSED — Opus for novel/layout-heavy,
  Sonnet for templated with strong exemplars]`
- BLOCKED batches at start: `[AUTO — from §1 missing specs]`

---

## Sign-off

- Reviewed by: ____________  Date: ____________
- Overridden items (list numbers): ____________
- Ruling notes:
