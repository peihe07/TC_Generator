# 下放包 03 — R16 執行覆核、證據更正、凍結解除判準

分析層 → 執行層。2026-08-13。承接上繳包 `docs/upstream/02_integrity.md`。

**權限標示**：§1（R17-1 ~ R17-3）為分析層自裁，即時生效。
§2（R17-4 ~ R17-8）**須 Pei 裁定，尚未簽署，不得執行**。

---

## 1. 分析層自裁（即時生效）

```text
[RULING] R17 — R16 執行覆核（分析層自裁部分，2026-08-13）

R17-1  §4.4 停手範圍之解讀 —— 執行層正確，追認
  事實：R16 下放包 §4.4 寫「3.4 發現 Home 受損 → 回報後停手，
        重產與否由 Pei 裁」。執行層讀為「停 Home 之重產動作」，
        續行 §3.5 登記與 §3.6 草案，未動 Home 任何一列、未跑 --write。
  裁：追認。§3.5/§3.6 為回報之載體且不動交付件，續行正確。
  歸因：**分析層條文缺陷** —— 「停手」未標明標的。
  立為通則：**停手條件之條文必須明列停手標的**，格式為
        「停止 <具體動作>，續行 <具體動作>」。僅寫「停手」者，
        執行層得依最小停止範圍解讀，且該解讀優先。

R17-2  R16 證據數字更正 —— 執行層發現正確，根因已定位
  事實：R16 §2 寫「x14 dataValidation（sheet6）: 6 → 0」。
        執行層實測 x14 元素 2、sqref 群組 2、range 總數 7、
        classic(sheet6) 3、classic(全簿) 4，指出 6 對不上任一量法，
        推測為「兩類相加誤標」。
  根因（分析層複現，非執行層之推測）：
        分析層原始量法為 t.count("x14:dataValidation")，
        **無詞界之子字串計數**。實測拆解 A.xlsx sheet6：
          <x14:dataValidation 開始標籤      2
          </x14:dataValidation> 結束標籤    2
          <x14:dataValidations 容器開始     1
          </x14:dataValidations> 容器結束   1
                                    合計 = 6
        即開閉標籤各計一次，且複數容器元素亦被子字串命中。
        與「兩類相加」無關。
  裁：正確值為 **x14 dataValidation 元素 2 個、涵蓋 2 個 sqref 群組
        共 7 個 range，全數於 v1 消失**。RULINGS.md 之 R16 原文不改，
        於其後附本更正（執行層已如此處置，追認）。
  §5a 新增：**以子字串計數量測 XML 元素數量，必須加詞界與標籤形狀約束**
        （至少排除結束標籤與同前綴之複數容器）。此即 canon「詞彙型工具
        之缺陷不會報錯，須以已知全集驗證」之具體實例，記為第 11 例。
  裁決結論不受影響：21 lost / 10 added 之成員清單獨立成立且已複現。

R17-3  R16-2「SXM 尚未寫回」為錯誤前提 —— 歸因分析層
  事實：執行層實測 features/sxm/output/ 早有交付件與 .sha256，
        tag fw036-sxm-v1 存在，且已受損（lost 11 / added 10、
        x14 DV 2 → 0，失去者正是 R 欄設計方法下拉）。
  歸因：分析層撰寫 R16-2 時以**先前對話中之印象**為來源，未回到 repo
        實測 output/ 與 refs/tags。此違反 canon §5a
        「不以自身先前輸出為來源；回到 repo 現行記載或當下實測」。
  裁：R16-2 之「SXM 尚未寫回，攔得住」一句作廢，以本條取代。
        凍結令本身不受影響（其標的為未來之 --write）。
  §5a 強化：**任何關於「某 feature 目前處於何階段」之陳述，
        撰寫當下必須以 output/ 內容 + refs/tags + PLAYBOOK §6
        三者實測為據，三者不一致時停手回報。**
        本次為該類錯誤之第二次（前次為 AMFM P7 之 tag/§6 不一致）。

R17-4  Projection 對照組之證明力 —— 追認執行層之限縮
  事實：執行層指出 Projection output/ 與 inputs/ 位元完全相同，
        零差異證明的是「沒被寫過」，非 writer 安全。
  裁：追認。R16 §2 之 Projection 對照組敘述，其結論限縮為
        「該檔未經寫回路徑」，不得作為 writer 安全性之任何佐證。
```

---

## 2. 待 Pei 裁定（不得執行）

```text
[PENDING RULING] R17-5 — 凍結解除判準

事實：R16-2 之解除條件寫「§3.1 完成且 §3.2 invariant 上線」，
      但未定義「完成」由誰認定、憑何證據。兩者實質皆已完成，
      執行層未自行解除，五個 PLAYBOOK 橫幅仍掛「待 Pei 裁示」——
      處置正確。

分析層建議之判準（三項全中方得解除）：
  (a) 探針對 AMFM 客戶原件與 FW036 空白範本兩次驗證皆 LOSSLESS
      —— 已達成（上繳包 §2 附原文輸出）
  (b) §3.2 之 ABORT 級 invariant 已上線且經反向測試
      —— 需補：以一份「刻意破壞的輸出」驗證 invariant 確實 ABORT，
         而非只驗證正常情形不 ABORT（不可能失敗之檢查項不算 PASS）
  (c) AMFM v2 經人在 Excel 實開確認無修復提示（見 R17-9）

裁：[ ] 照建議三項判準，(a) 已達成，(b)(c) 完成後解除
    [ ] 僅以 (a) 為據，即刻解除
    [ ] 其他：____

[PENDING RULING] R17-6 — SXM 重產

事實：SXM 已受損（lost 11 / added 10、x14 DV 2 → 0），
      為 append 形態，不受外科手術之「不得中間插列」限制。
      與 Privacy 同一份 rev C 範本。

建議：比照 AMFM v2 重產。tag fw036-sxm-v1 保留為歷史，
      另立新 tag。內容不動，只換寫回方法。

裁：[ ] 照建議  [ ] 暫不重產  [ ] 其他：____

[PENDING RULING] R17-7 — Home 重產路徑

事實：Home 受損（lost 14 / added 10，含整組 SmartArt），
      但 x14 DV 為 0 → 0（該檔本無 DV），故 DV 判準看不見它。
      Home 為 interleaved 重寫，現行外科手術僅支援 append，
      擴充工作量不小。

分析層另提第三條路徑（成本遠低於擴充 append 路徑）：
  **whole-sheet splice** —— 取 openpyxl 重新產生之「目標 sheet XML」
  整片替換，其餘 zip 成員逐 byte 複製。
  理據：Home 之 14 項缺損（SmartArt、printerSettings、
  sharedStrings 等）**沒有一項位於目標 sheet XML 內**，全部是
  其他 zip 成員；而目標 sheet 內 openpyxl 會破壞的資產
  （x14 DV）Home 為 0。故整片替換不損失任何已知資產。
  前置驗證（必辦，任一不成立則此路徑不適用）：
    - 目標 sheet 之 classic DV 計數 == 0
    - 目標 sheet 無 x14 DV
    - 目標 sheet 之合併儲存格與 shared formula 於 openpyxl 往返後
      計數與 ref 皆不變
  此模式須與 append 外科手術並列為獨立模式，於 feature.yaml 指定，
  不得自動選用。

裁：[ ] 照建議，先做前置驗證，通過則以 whole-sheet splice 重產 Home
    [ ] 擴充 append 外科手術以支援 interleaved
    [ ] 暫不重產 Home，僅登記缺損
    [ ] 其他：____

[PENDING RULING] R17-8 — v2 第 243–310 列無儲存格樣式

事實：範本 template tail 只到第 242 列，故 243–310 共 68 列無框線
      格式。v1 亦然，非本次造成，獨立於 R16。

建議：**不在 v2 修補**。v2 之價值在於「與 v1 逐格相同、僅修結構」，
      混入樣式修改會破壞該可比性，也使 §4.3 之「v2 與 v1 不符即停手」
      失去判準。另立條目，於下一次內容變動時一併處理。

裁：[ ] 照建議  [ ] v2 一併補樣式  [ ] 其他：____

[PENDING RULING] R17-9 — AMFM v2 之 Excel 實開驗證（Tier 3，僅 Pei）

事實：v2 全部驗證均在程式層。外科手術寫顯式 <f> 公式，而新增之
      B243–B310 不在被逐 byte 保留的 calcChain.xml 內。Excel 通常
      靜默重建，但此為推論非實測。執行層無法執行此項。

需 Pei 在 Excel 開啟 v2 並確認四點：
  1. 無「檔案已損毀，Excel 已修復」提示
  2. R 欄設計方法下拉可用（點得開、選項為 下拉選單 分頁 9 條）
  3. SmartArt 圖存在
  4. 列印預覽之版面設定與客戶原件一致

裁：[ ] 已實開，四點全過   [ ] 發現問題：____
```

---

## 3. 執行層作業（僅 R17-5(b) 一項可先辦）

R17-5(b) 之反向測試不依賴任何待裁項，可即刻執行：

- 製造一份刻意破壞之輸出（如以 openpyxl 存檔路徑產生），
  確認 §3.2 之 invariant 確實 ABORT 並印出差異清單
- 再製造一份僅 DV 計數不符者，確認同樣 ABORT
- 回報兩次之實際輸出原文

**其餘一律待 Pei 裁定，不得動作。** 特別是：
不得重產 SXM、不得動 Home 任何一列、不得解除凍結、
不得修補 243–310 列樣式、不得打任何 tag。

---

## 4. 上繳包 02 之覆核結論

- §3.1–§3.6 全數完成，952 測試全過
- AMFM v2 三方比對表（59 members / 0 lost 0 added / 4 classic + 2 x14 DV
  / 301 列 / lint PASS）與 v1、客戶原件並列，格式正確
- v1 vs v2 十分頁逐格比對、連跑兩次 SHA256 相同 —— **此為本包最有力之
  單項證據**，證明重產具決定性且未夾帶內容變動
- 探針改為呼叫生產模組而非自帶平行實作 —— **正確且重要**。
  「測自己那份 copy 證明不了出貨的 writer」一語應納入 canon
- §5.6 六項該驗未驗之獨立判斷：**其中兩項（Excel 實開、凍結解除判準
  未定義）為本輪最關鍵之發現，皆非下放包所要求**
- 三處對分析層之更正（數字、SXM 前提、Projection 證明力）全部成立

---

## 5. 本包產生之新條文清單（自檢表）

- [x] R17-1 停手條文須明列標的 —— §1，區塊形式
- [x] R17-2 子字串計數之詞界約束（§5a 第 11 例）—— §1，區塊形式
- [x] R17-3 feature 階段陳述須三源實測（§5a 強化）—— §1，區塊形式
- [x] R17-4 Projection 對照組證明力限縮 —— §1，區塊形式
- [ ] R17-5 凍結解除判準 —— §2，**未簽署**
- [ ] R17-6 SXM 重產 —— §2，**未簽署**
- [ ] R17-7 Home 重產路徑（whole-sheet splice 提案）—— §2，**未簽署**
- [ ] R17-8 243–310 列樣式 —— §2，**未簽署**
- [ ] R17-9 Excel 實開驗證（Tier 3）—— §2，**未簽署**

以上均以可直接貼入之區塊形式出現，非夾敘於段落中。
