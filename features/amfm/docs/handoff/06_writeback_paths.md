# 下放包 06 — 寫回路徑違規點之處置（ratchet 入庫 + 一項待裁）

分析層 → 執行層。2026-08-13。承接上繳包 `docs/upstream/05_wrapup_review.md`。

**權限標示**：§1（R20-1 ~ R20-3、R20-5 ~ R20-7）為分析層自裁，即時生效。
§2（R20-4，`api_server.py:2410` 就地覆寫來源檔）**須 Pei 裁定**。

**與 Pei 裁示之關係**：Pei 已裁「做過的都不重產，只專心在現在」（R18）。
本包嚴格遵守 —— **不改寫任何既有 write_back 腳本、不重產任何交付件**。
本包處理的是「未來會產生新受損檔」之路徑，與重產無關。

---

## 1. 分析層自裁（即時生效）

```text
[RULING] R20 — 寫回路徑違規點之處置（分析層自裁部分，2026-08-13）

R20-1  03 包權限標示落差 —— 歸因分析層，補登有效
  事實：03 包權限標示寫「§1（R17-1 ~ R17-3）為分析層自裁」，
        但 §1 區塊實際含 R17-4，§5 自檢表亦將四條同列為已簽署。
        執行層依 R19-2 之「逐字取自 03 §1」補了整個 §1 區塊（含
        R17-4），落差照實記載，未自裁何者為準 —— 處置正確。
  裁：**四條同為分析層自裁項**，權限標示之「R17-1 ~ R17-3」為
        分析層漏寫範圍上界，非有意排除 R17-4。補登有效，全數生效。
  §5a：條文區塊與其權限標示之範圍必須逐項對齊；標示以列舉而非
        區間表示時，須與區塊內實際條文數一致。

R20-2  R19-3 之測試以 ratchet 形式入庫 —— 不待違規點清零
  事實：停手條件 3 觸發（現況即 FAIL，11 個違規呼叫點），執行層
        未入庫、未動生產程式碼 —— 完全正確。
  裁：測試改以 **ratchet（棘輪）** 形式入庫，即刻執行：
        (1) 將現況 11 個呼叫點以「已知既存違規」清單寫入測試，
            逐點附檔名、行號、性質，作為 grandfathered baseline
        (2) 測試之通過條件為「掃描結果 ⊆ 白名單 ∪ 既存違規清單」
        (3) 新增任何不在兩份清單內之呼叫點 → **FAIL**
        (4) 既存違規清單為**只減不增**；移除一項時同步從清單刪除，
            清單本身即進度計量
        (5) 保留執行層已設計之白名單三項與陽性對照
  理由：規則之價值在於阻止新增，不在於一次清零。以「必須先清零才
        能入庫」為條件，等於讓最需要防護的期間完全無防護。
  §5a：**機制之導入不得以「現況已合規」為前提**；現況不合規時，
        正確作法是凍結現況並禁止惡化，而非延後機制。

R20-3  四支 feature write_back 腳本 —— 封存，不改寫、不得執行
  事實：features/{home,sxm,media,projection}/scripts/write_back.py
        （及 writeback.py）仍在 openpyxl 存檔路徑上。四者所屬
        feature 之交付件皆已產出，缺損已依 R18-1 裁為 DEFERRED。
  裁：依 R18「做過的都不重產」，**不改寫此四支腳本**。
        改以封存處置：
        (1) 四支腳本檔頭加入封存標頭（逐字）：
            「QUARANTINED (R20-3, 2026-08-13) — this script writes via
             openpyxl save and will destroy zip members and data
             validations. It must not be executed. The feature's
             delivered artefact is frozen; see ANOMALIES A-H27 /
             A-SX28 / A-AM18 and RULINGS R18-1.」
        (2) 各 feature PLAYBOOK 加註該腳本已封存
        (3) 四點自 R20-2 之既存違規清單中另標 `QUARANTINED`，
            與「仍可被呼叫之違規」區分
        (4) **不加程式層 guard**（加 guard 屬修改生產程式碼，
            且該腳本本就不應被執行；標頭與登記已足）
  Media 附註：features/media 未在 R16/R18 之檢測範圍內，其交付件
        是否受損**未量**。本包不擴大檢測範圍，僅封存腳本並登記
        「交付件狀態未量」。

R20-5  Privacy 之 write_back 自始建於 xlsx_surgical
  裁：Privacy 尚未寫回，**不得複製任一既有 feature 之 write_back
        腳本作為起點**（四支皆已封存）。其寫回路徑自始建於
        backend/xlsx_surgical.py，並受 R18-3 之 ABORT 級 invariant
        拘束。此為 R18-3 規則 1 之首次正向適用。

R20-6  canon 草案落點 —— 追認
  裁：判準型通則置於 §6a 之末，追認。執行層之理由（前文規範如何
        不製造缺損，本條規範缺損既成後怎麼辦）正確。

R20-7  A-AM19 之未實測標記內容 —— 追認並嘉許
  裁：執行層除照條文標註外，另寫明「未驗的具體是什麼」
        （s= 索引跨列沿用是否對所有欄成立；第 242 列為 template
        tail 最後一列，其樣式未必等同資料列常態；新增 <row> 屬性後
        spans 與 customFormat 是否需同步）。
        追認，並立為體例：**未實測標記須指名待驗之具體命題**，
        僅寫「未實測」者不合格 —— 無命題者日後無從驗起。
```

---

## 2. 待 Pei 裁定

```text
[PENDING RULING] R20-4 — backend/api_server.py:2410 就地覆寫來源檔

事實（執行層 AST 掃描，已排除 pixmap.save / document.save 誤判）：
  該行為 wb.save(source_path)，即以 openpyxl 存檔路徑
  **覆寫來源檔本身**，而非產生可丟棄之輸出。

分析層評估 —— 此點與其餘十點性質不同，嚴重一級：
  其餘十點產生的是受損的「輸出」，可丟棄重來；本點損毀的是
  **後續一切比對所依賴的基準**。AMFM v2 之全套驗證能成立，
  前提正是 inputs/ 那份客戶原件未被動過（59 zip members 完整）。
  若此路徑曾被觸發於任一 inputs/ 檔，該 feature 之所有結構比對
  結論同時失效，且無從察覺 —— 因為基準與受測物一起被改。

  此點亦直接威脅 Privacy：其客戶原件（FW036 空白範本、V6_R2 等）
  尚在 inputs/，而 Privacy 正要開始使用 app 路徑。

選項 A（建議）：
  (a) 停用該分支 —— 改為寫入新檔並回傳新路徑，永不覆寫來源
  (b) 在 R20-2 之既存違規清單中標為 `HAZARD`，與其餘區分
  (c) 立即實測：對五個 feature 之 inputs/ 全部檔案計 SHA256，
      與客戶端來源目錄之同名檔比對，確認是否已有被覆寫者
      —— 此為唯讀量測，不動任何檔

選項 B：僅做 (c) 實測與 (b) 登記，暫不動程式碼
選項 C：其他 ____

裁：[ ] A（建議）  [ ] B  [ ] C：____

註：選項 A(a) 屬修改生產程式碼，逾越「不重產」之外的另一類動作，
    故不自裁。A(c) 之實測本身為唯讀，若 Pei 僅簽 (c) 亦可先行。
```

---

## 3. 執行層作業

1. 貼入 §1 全文至 `features/amfm/RULINGS.md`（R20）
2. 依 R20-2 以 ratchet 形式入庫該測試（含 11 點既存違規清單、
   白名單三項、陽性對照、只減不增之註記）
3. 依 R20-3 為四支腳本加封存標頭、各 PLAYBOOK 加註、
   清單內標 `QUARANTINED`
4. `features/media/ANOMALIES.md` 登一條：write_back 腳本已封存、
   交付件結構狀態**未量**（依 R19-1，既有條目存在時更新之）
5. R20-4 **不執行任何一項**，包括 (c) 之實測 —— 待簽

**不做**：不改寫四支 write_back、不加程式層 guard、不動 api_server.py、
不擴大檢測至 Media 交付件、不動 canon 本體、不執行任何 git 操作。

---

## 4. 停手條件

1. `RULINGS.md` R20 編號已占用 → 停止貼入 R20，續行第 2–4 項
2. 第 2 項入庫後測試仍 FAIL（即掃描結果超出兩份清單）→
   停止入庫，續行第 3–4 項，回報超出項
3. 第 3 項加標頭時發現某腳本已有相衝突之標頭 → 停止該檔之處理，
   續行其餘檔與第 4 項，回報內容

---

## 5. 覆核結論

- 停手條件 3 之處置完全正確：現況 FAIL 即不入庫、不動生產程式碼、
  照實回報 —— 這正是「不可能失敗之檢查項不算 PASS」的反面：
  **會失敗的檢查項不得以修改受測物來讓它通過**
- AST 實作而非字串比對，且主動驗證 `pixmap.save` / `document.save`
  未誤判 —— 符合 R17-2 之詞界紀律
- 第 10 項之風險分級（損毀基準 vs 產生可丟棄輸出）為本輪最重要之
  單項發現，且非下放包所要求
- 第 1–4 項之分布觀察（規則 1 實際只覆蓋五分之一交付路徑）成立，
  已由 R20-3 封存與 R20-5 正向適用處理

---

## 6. 本包產生之新條文清單（自檢表）

- [x] R20-1 權限標示與條文範圍須逐項對齊 —— §1，區塊形式
- [x] R20-2 ratchet 入庫 + 機制導入不以現況合規為前提 —— §1，區塊形式
- [x] R20-3 四支 write_back 封存（不改寫、不執行）—— §1，區塊形式
- [ ] R20-4 api_server.py:2410 就地覆寫 —— §2，**未簽署**
- [x] R20-5 Privacy write_back 自始建於 xlsx_surgical —— §1，區塊形式
- [x] R20-6 canon 草案落點追認 —— §1，區塊形式
- [x] R20-7 未實測標記須指名具體命題 —— §1，區塊形式
- [x] 停手條件三項（已依 R17-1 明列標的與續行標的）—— §4

以上均以可直接貼入之區塊形式出現，非夾敘於段落中。
