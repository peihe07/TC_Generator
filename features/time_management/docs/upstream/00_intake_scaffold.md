# 上繳包 00 — Time Management：Phase 0 Intake + Scaffold

執行層 → 分析層。對應下放包 `docs/handoff/00_intake_scaffold.md`（rev B）。
執行日期 2026-08-20。本包止於 scaffold，**未跑 `recon.py`**（依下放包 §3）。

## 0. 與下放包之偏離（先講）

**下放包 §3 步驟 1 之前提未成立**：素材未投放至 `_intake/Time_Management/`，
Pei 直接放進 `features/time_management/inputs/`。

處置：執行層將三檔**移**（非複製）至 `_intake/Time_Management/` 後照 §3
序列執行；步驟 4 之 `--scaffold` 以 `shutil.move` 將其搬回 `inputs/`，
淨結果檔案回到原位且完整走過分類。

用 move 而非 copy 之理由：`intake.py:452` 之 `shutil.move` 對已存在之
同名目標會靜默覆蓋，copy 會在 `inputs/` 留下待覆蓋之同名檔。

此偏離不觸及 §6 之升級條件（該條為「`_intake/` 為空」，本例素材齊備、
僅位置不同），故未停止。

## 1. INTAKE.md 與 intake.json

路徑：`_intake/Time_Management/INTAKE.md`、`_intake/Time_Management/intake.json`
（`--scaffold` 後該目錄僅存此二產物）。全文：

```
# INTAKE — Time_Management (generated by intake.py)

## Classified files
- `R1LR_Atl-H_25PI3.5_Cabin_CFTS _015 Time and Date _SR26_20250909-1851.docx` → **cfts_doc** — CFTS/Word candidate (spec_mode D)
- `SWE1_Secure_Date&Time.xlsx` → **swra_report** — req family SWE-RA-TIME&DATE, 22 rows, sources: component/architecture ids (trace via SYS3)
- `SYS2_CFTS_015_Time and Date _SR26_V1_Including_Delta_Released.xlsx` → **polarion_export** — SYS2 safety-analysis export (Sys-RA ids)

## Documents cited by the requirement report (need list)
- not derivable: source column holds component/Polarion ids, not document names — need list not derivable from this template; trace runs through the architecture/export files instead

## Proposed spec_mode: **D** — CFTS/Word is the spec source; spec_reference by lookup

## Action: complete — run `--scaffold` then Phase 1 recon
```

**對照下放包 §2 之預期分類：三檔逐項相符，無不一致。** `spec_mode` 提議
為 D，即 §6 之預期值（非 D 才需升 Tier 2）。need list 判為 not derivable，
腳本明講且未編造清單，符合 §4(4)。

## 2. 目錄樹與 feature.yaml

```
features/time_management/
├── .gitignore        ├── batches/        ├── feature.yaml
├── ANOMALIES.md      ├── data/           ├── generated/
├── DATA_REQUESTS.md  ├── docs/           ├── inputs/
├── DECISIONS.md      ├── PLAYBOOK.md     ├── scripts/
├── RULINGS.md        └── RUNBOOK.md
```

`ls -d features/time_management` 命中；
`ls -d "features/time management"` **無命中**（步驟 5 通過，無空格孿生目錄）。

`inputs/` 三檔及其 SHA256：

| 檔案 | SHA256 |
|---|---|
| `R1LR_..._015 Time and Date _SR26_20250909-1851.docx` | `564b86517ce240b2699af2d702e838687fd276be3201de879054b57e0b6d3758` |
| `SWE1_Secure_Date&Time.xlsx` | `3d692e75b3bf1dc7359443483a551145f4696b3f2c509d3019569ef7139ee279` |
| `SYS2_CFTS_015_..._Released.xlsx` | `ecf665a11eaf859c0e8ea2a9caba41cf94196dabe46772cfd3f9e805c432dec1` |

`feature.yaml` 全文見該檔（已含 §3 之兩處修正）。關鍵值：
`feature: "Time Management"`、`test_group: "Time Management"  # [PROVISIONAL] 見 R-TM2`、
`spec_mode: "D"`、`a03_report: "inputs/SWE1_Secure_Date&Time.xlsx"`、
`sys1_export: "inputs/SYS2_CFTS_015_..._Released.xlsx"`。
`workbook` / `spec_pdf` / `popup_list` 仍為佔位符。

## 3. 三處手動修正之逐項確認

| 檔案 | 改前 | 改後 | 依據 |
|---|---|---|---|
| `feature.yaml` | `feature: "Time_Management"` | `feature: "Time Management"` | R-TM1 / R-TM3(2) |
| `feature.yaml` | `test_group: "Time_Management"　# framework-internal; workbook write per profile` | `test_group: "Time Management"　# [PROVISIONAL] 見 R-TM2；framework-internal; workbook write per profile` | R-TM2 |
| `ANOMALIES.md` L4、L14；`PLAYBOOK.md` L97 | `A-TInn` | `A-TMnn` | R-TM3(3) |

改後 `grep -rn "A-TI" ANOMALIES.md PLAYBOOK.md` 無殘留。

## 4. Anomaly 寫入確認

`ANOMALIES.md` 已建索引表 + 逐條全文。狀態全部 PENDING（登記，未裁定）。

| # | 來源 | 狀態 |
|---|---|---|
| A-TM01…A-TM05 | 下放包 §5 指派 | 已寫入，各含執行層複核段 |
| A-TM06…A-TM09 | **執行層自行登記** | 已寫入 |

### A-TM03 之重測（§7(4) 要求，對 `inputs/` 原始 binary）

`Basic Report`，列 2 起共 **227** 列，區分大小寫精確相等計數：

| 第 7 欄 EE Architecture | 沙箱 | 重測 | 第 10 欄 Category | 沙箱 | 重測 |
|---|---|---|---|---|---|
| `ATL-Hi` | 101 | **101** | `Functional Requirement` | 126 | **126** |
| `All` | 61 | **61** | `Heading` | 70 | **70** |
| `ATL-Mid` | 55 | **55** | `Information` | 30 | **30** |
| `Atl-Hi` | 10 | **10** | `Out of Scope` | 1 | **1** |

**兩組逐項相符，無差異可報。** 沙箱副本與原始 binary 一致，A-TM03 之事實
基礎成立。Atlantis High 之正確全集為 `ATL-Hi` ∪ `Atl-Hi` = 111 列；只取
`ATL-Hi` 之判準漏 10 列（9.0%）。

### 執行層新登記之四條

- **A-TM06** — `a03_report` 路徑含 `&`。YAML 層安全，但未加引號傳入 shell
  時指令會在 `inputs/SWE1_Secure_Date` 處被切斷，**靜默失敗**。附帶：
  下放包 §2 與 R-TM1 記本檔為 `SWE1_Secure_DateTime.xlsx`，與實測之
  `SWE1_Secure_Date&Time.xlsx` 不符（別名字面差異，不動 R-TM1 裁定）。
- **A-TM07** — 036 缺件，`workbook_state` 無法判定。已逐項列出所阻塞之
  五項 recon 產出。特別提請注意：`workbook.columns` 現值全為模板預設，
  同型先例 `vehicle_setting` 實測後 `design_method` Q→R、`author` Z→AA，
  本 feature 極可能同樣需位移。
- **A-TM08** — SYS2 第 7 欄**表頭宣告**之值域 `{All, ATL-Hi, ATL-Mi}` 與
  **實測值集** `{All, ATL-Hi, ATL-Mid, Atl-Hi}` 不符：無任何一列之值為
  `ATL-Mi`。由表頭自動推導 enum 會產生零命中之 `ATL-Mi` 並同時漏掉
  `ATL-Mid` 與 `Atl-Hi`。與 A-TM03 同屬 canon §5a 形態但成因不同
  （後設資料與資料不一致，非資料端大小寫不一致）。
- **A-TM09** — 見 §6，本包最重要之發現。

## 5. DATA_REQUESTS.md 初版

| # | 檔案 | Status | Anomaly | Urgency |
|---|---|---|---|---|
| 1 | FW036 工作簿（本 feature 客戶原件） | MISSING | A-TM07 | **High** |
| 2 | FW036-037-A03 正式釋出件 | UNCONFIRMED | A-TM02 | **High** |
| 3 | Pop Up List | NOT REQUESTED（intake 未命中，依 §2(3) 不預先索取） | — | 低 |
| 4 | 涵蓋 126 筆 FR 之 037，或「51 筆不在範圍」之書面依據 | MISSING / 待答 | A-TM09 | **High** |

各列均附逐項說明與解鎖條件。#2 與 #4 同源，建議合併一次查詢。

## 6. §7(6) — 該驗而未驗者之獨立判斷

**盤點方式**：以「本包產出之每一個數字、每一個路徑值、每一份檔案」為全集
逐一問「此值之依據是實測、是模板預設、還是轉述」，未實測者即列入下表。
非以「想到什麼寫什麼」為之。

### 盤點時發現可驗、遂於本包內補驗者（1 項）

**037 與 SYS2 之交叉比對 —— 下放包全篇未觸及，已補驗，結果立為 A-TM09。**

037 引用 75 筆 `SYS-RA-TIME&DATE` 功能需求；SYS2 共 126 筆 FR；
**懸空引用 0 筆，51 筆 FR 無對應 SWE leaf，覆蓋率 59.5%（上界）。**
037 已引用之 75 筆 Category **全部**為 `Functional Requirement`，
且 75 + 51 = 126 恰等於 FR 總數 —— 此完美互補即解析正確性之驗證，
誤中之序號不可能全數避開 70 筆 Heading 與 30 筆 Information。

此結果與 A-TM02 相互強化：Reviewer 欄空白 + 模板佔位列未清 +
日期 2020/09/05 早於 SR26 + 只覆蓋六成 FR，四項合觀，**本件為未完成
工作稿之可能性高於其為權威釋出件**。

**分析層請注意**：下放包 §2 將 037 之缺件問題定位為「是否另有正式版」
（來源權威問題），本次補驗顯示問題更深一層 —— 縱使 Pei 裁定本件即為
權威 037，51 筆 FR 之缺口依然存在，那是**內容缺口而非版本問題**。
兩者需分別裁決。

### 仍未驗、且本包內不可驗者（4 項，全部因 036 缺件）

| 未驗項 | 現值來源 | 為何不可驗 |
|---|---|---|
| `workbook.columns` 全 14 欄 | 模板預設 | 036 缺件，無表頭可 match。**先例顯示極可能需位移** |
| `workbook.header_row: 9` | 模板預設 | 同上 |
| `workbook.sheet` 名稱 | 模板預設 | 同上 |
| `done_region.author_value: "Arif"` | 模板預設 | 同上 |

**以上四項現值一律不得引為基線**，036 落地後須逐項實測覆寫。

### 仍未驗、可驗但超出本包範圍者（2 項）

| 未驗項 | 為何未驗 | 建議時機 |
|---|---|---|
| CFTS docx 之內文（outline 結構、是否含 PU 編號） | 本包止於 scaffold；`intake.py` 僅做 text-layer 分類，未讀內文 | Phase 1 recon。**其結果決定 DATA_REQUESTS #3 之 Urgency 與 `spec_reference_template` 之 `{outline}` 可否成立** |
| R-G1 之母本 `forms/…_SWQT_20260817_ext.xlsx` 是否存在於 repo | 下放包稱其「已固定」，執行層未複核 | Phase 1 起始時順帶確認 |

### 判定為「無」者及其全集依據（1 項）

**037 之 leaf 無缺號** —— 依據為 `Analysis Report` 列 9–30 之第 1 欄全集
逐列讀出，得 `SWE-RA-TIME&DATE-001` 至 `-022` 連續 22 筆。此非抽樣，
為全集列舉，故「無缺號」之判定成立。同時確認 `intake.py` 報告之 22 rows
未把列 6 之模板佔位說明列誤算入分母（該列在表頭列 8 之前）。

## 7. 待分析層／Pei 裁決事項（依急迫序）

1. **A-TM09 之覆蓋分母** — Tier 2，阻塞 Phase 3 以後全部覆蓋率產出
2. **A-TM02 之 037 身分** — Tier 2，與上項同源但需分別裁決
3. **A-TM07 之 036 取件** — Tier 3，阻塞 Phase 1 全部
4. **A-TM01 之 `vehicle setting` 孤兒目錄處置** — Tier 3，不阻塞本 feature
5. **A-TM04 / A-TM05 之腳本修法** — Tier 2，不阻塞本 feature（迴避方式有效）

## 8. 本包未動之事項

未動 git（無 `git add` / `commit`）。未改任何腳本。未動其他 feature 目錄
（含 A-TM01 之兩個 `vehicle setting` 目錄）。未跑 `recon.py`。
`_intake/Time_Management/` 保留 `INTAKE.md` 與 `intake.json` 未刪。
