# 上繳 02R — framework 鎖定稿落檔

執行層 → 分析層。對應 `docs/handoff/02R_framework_lock.md`。
**僅回報差異。** 2026-08-20。

## 1. T4 四項驗證 —— 全數相符

```
R-TM 條數       : 18    期望 18   ✅
A-TM 條數       : 16    期望 16   ✅（T2 不增條）
'下游影響' 命中  : 1     A-TM13 條內（ANOMALIES.md:701）  ✅
framework.md    : 存在，57 行  ✅
```

## 2. T1–T3 寫入確認

| T | 動作 | 位置 | 確認 |
|---|---|---|---|
| T1 | R-TM14 / R-TM15 逐字追加 | `RULINGS.md` 末尾 | 16 → **18** |
| T2 | A-TM13「下游影響」段 | A-TM13 條末（原「判準精確性」段之後） | 已追加 |
| T3 | `features/time_management/framework.md` | feature 目錄，**非** `docs/fw036/` | 已建，§1–§4 全文 |

T2 之首次 `assert` 失敗（anchor 字串取自 A-TM09 而非 A-TM13），
已改以 A-TM13 條末實際字串為錨並加 `t.count(a)==1` 唯一性檢查後成功。
**此即 assert 前置之作用**：錯誤錨點被擋下，而非靜默寫到錯的位置。

## 3. T5(3) — 該驗而未驗者（續用 02 上繳 §5.1 之五全集）

### 3.1 依全集 2（寫入後複查）

四項全部複查，見 §1。三處寫入全部前置 `assert`，其一如上被擋下。

### 3.2 依全集 5（草案設計說明之逐項可驗性）

| 02R § | 斷言 | 本包是否已驗 |
|---|---|---|
| 2.1 | 「005/006/016/021 主要動詞與受詞同型：maintain internal {clock/time signal/calendar/counters}」 | **未驗** —— 見 3.3 |
| 2.1 | 「`1.5.2.2` 為條件章節非能力章節」 | **已驗**：標題 `Time Indication management on Key Off Status`，依情境分章，執行層 02 上繳已實測該標題 |
| 2.2 | 「010 收端、022 送端」 | **未驗**（依賴 leaf 描述） |
| 2.3 | 「020 語意軸為訊息傳輸」 | **未驗**（依賴 leaf 描述） |
| 3.3 | Layer 3 主軸章節表 | **已驗**：逐組比對 `data/leaf_to_section_probe.txt`，七組主軸章節皆為該組 leaf 實際命中之章節，無憑空列入者 |
| 3.4 | 三條相鄰組界線 | **未驗**（依賴 leaf 描述） |

### 3.3 仍未驗之核心項 —— **leaf 描述全文，執行層仍未讀**

02R §2 之全部語意複核（Set 3 維持、Set 7 成立、020 歸屬、019 定案、
三條界線）**建立在 037 `Requirement Description` 欄之上，而該欄由分析層
讀取，執行層本包並未複驗**。

02R §2 開頭稱此為「本次最有價值的一句」而採納執行層之提請，並已補測 ——
但補測結果同樣是單方的。依 R-TM4（雙向適用）與 `00Z` §2 所立常態
（上游給的數字同樣受查證義務拘束），**此項本應由執行層複驗**。

**未逕行之理由**：02R 之 T1–T3 未指派閱讀 leaf 描述，且該欄位讀取需
開啟 037（`inputs/SWE1_Secure_Date&Time.xlsx`），不在本包指令範圍內。
成本極低（單欄 22 列，唯讀）。

**提請**：下一包指派一次 `Requirement Description` 欄之逐筆讀取與比對。
**在此之前，02R §2 之語意複核與 §3.4 之三條界線應視為單方實測。**
三條界線已寫入 `framework.md` §4 並將於 TC 生成時逐條適用（R-TM17），
其未經雙方確認一事值得在生成前解決。

### 3.4 其餘未驗項（狀態未變）

交付路徑 Home 複本內容（刻意不驗，R-TM10-A1）、PU 陽性對照（待 Pei 裁）、
`write_back` 兩值（Phase 3）、`1.5.3.*` 與 A-TM09 之關聯（不主張）。

## 4. 本包未動之事項

未動 git。**未寫 `docs/fw036/framework.md`**（T3 明令，全域檔待 Pei 裁
是否併入）。未改七組分組。未生成任何 TC。未填 `D5`。未援引他 feature
樣式。未以 openpyxl 存回任何工作簿。**未跑 `recon.py`**。
