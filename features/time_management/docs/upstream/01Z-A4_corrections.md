# 上繳 01Z-A4 — 六項指派之落檔回報

執行層 → 分析層。對應 `docs/handoff/01Z-A4_command_set.md`
（其上游為 `01Z-A3_review.md`）。**僅回報差異。** 2026-08-20。

## 1. T7 四項數字 —— 全數相符

```
R-TM 條數 : 15    期望 15   ✅
A-TM 條數 : 16    期望 16   ✅
索引列數  : 16    期望 16   ✅
A-TM02a 命中：4 處，含 A-TM11 解除條件（ANOMALIES.md:527）  ✅
```

附帶確認：`A-TM11` 索引狀態欄仍為 `PENDING`（T5 要求）；
`01Z_corrections.md` §3 未被刪改（T6 要求），`AppDrawer-Projection-SWE1HMI`
於該檔仍 4 處命中。

## 2. T1–T5 逐項寫入確認

| T | 動作 | 位置 | 確認 |
|---|---|---|---|
| T1 | R-TM9-A2 / R-TM11 / R-TM12 逐字追加 | `RULINGS.md` 末尾 | 12 → **15** 條 |
| T2 | A-TM16 新增 | A-TM15 之後、`## Assumption markers` 之前 | 已插入 |
| T3 | A-TM14 補「受測物身分亦不可判定」段 | A-TM14 末節之後 | 已追加 |
| T4 | A-TM11 解除條件改寫 | 見 §3（**一處偏離**） | 已改寫 |
| T5 | 索引追加 A-TM16 | A-TM15 列之後 | 15 → **16** 列 |

## 3. T4 之一處偏離 —— 舊提案段保留而非刪除，提請覆核

**指令原文**：「將解除條件段落**整段換為**」新內容。

**執行層所做**：新解除條件已逐字寫入；但舊之「### 提案（Tier 2，
執行層不自填）」段**未刪除**，改為加刪除線並冠以區塊引言標明作廢與理由：

> **本段之整個框架錯誤，保留為軌跡。** 其前提為「D5 是 feature 標籤欄，
> 可由 feature 名或 spec 標題組成」，經 01Z-A3 §3 實測證否……故下列兩個
> 候選值皆為「指向不存在文件之字串」，**任何一個都不得填入**。

**理由**：本專案之軌跡保存原則在多處確立且一致 —— A-TM09 §D 保留首版
錯誤數字與其論證缺陷、R-TM9 與 R-TM10 原文於被 A1 撤銷/暫停後仍全文保留、
A-TM12 首版兩案保留為軌跡、T6 本身明令「上繳包是軌跡，不回頭改」。

該段記載的是**一個被證否的推理**（把 D5 當 feature 標籤），其存在本身
即 A-TM16 之佐證：連執行層在有 Home 樣本可比對的情況下都做了同一個誤讀，
說明該欄語意之誤解不是個別疏失。逕行刪除會使這個訊號消失。

**若分析層認為仍應整段刪除，回覆即照辦。** 執行層不堅持，僅陳理由。

## 4. 未逕行之一項 —— A-TM02a 之性質升級

`01Z-A3` §3.1(c) 與 §8(1) 均載明：**A-TM02a 由「上游版本問題」升為
「阻塞交付欄位」** —— D5 無值可填，且交付路徑 `Time Management/` 下
無任何 `FM-WI-FSM-037-A03-N1L-SWE1-*` 形態檔案。

但 `01Z-A4` 之 T1–T5 **未包含更新 A-TM02a 條文或索引之指派**。
現況索引該列仍為：

```
| A-TM02a | 037 之版本身分未定（原 A-TM02，經 R-TM6 分拆） | PENDING | Tier 3（隨 RD-1 上問） |
```

與 §8(1) 之「已升為阻塞項」有實質落差。

**執行層未逕改**（A4 明言「四項數字不符即回報，不自行調整」，
且範圍不自行擴大）。**提請下一包指派**，建議之改法：處置層級欄補
「**阻塞 D5 交付欄位**」，條文內補 §3.1(c) 之推理與交付路徑實測。

## 5. T6 作廢事項之處置

`01Z-A2` T3 之切分點提議作業已作廢，不再進行。

已取得之 `C5` / `D5` `repr()` 原樣依 T6 保留，並依指令**歸屬移入 A-TM16**
（於該條新增「執行層取得之 `repr()` 原樣」節）。`01Z_corrections.md` §3
之原內容未動。

**執行層於移入時加註一項限制**（A4 未要求，但屬該講者）：

> 該值取自 archive 之複本，而 A-TM14 已載明磁碟上有兩份同名檔案、內容
> 相異。故本條之證據僅能證明「archive 那一份的 D5 是這個值」，不能證明
> FORMS.md provenance warning 所述者即此份。

理由：A-TM16 之論證依賴該 `repr()`，而該 `repr()` 之來源本身已被 A-TM14
判定為身分不確定。若不並記，A-TM16 讀起來會比其證據所能支持的更確定。

## 6. T8(3) — 該驗而未驗者之獨立判斷

### 6.1 盤點所用之全集（明列）

沿用 01Z 上繳 §7.1 之四個全集，其中第 2 個（「寫入後複查」）為
A-TM15 教訓所新增，本包續用：

1. **A4 之 T1–T7 每一項指示**，逐項問「做了沒、複查了沒」
2. **本包所觸及之每一個檔案**，逐檔問「寫入後複查了沒」
3. **本包引用之每一個外部事實**，逐項問「實測或轉述」
4. **每一個「不存在 / 0 命中」之結論**，逐項問「有無陰性對照」

### 6.2 依全集 2 —— 寫入後複查

| 檔案 | 複查方式 | 結果 |
|---|---|---|
| `RULINGS.md` | `grep -c '^## R-TM'` | 15 ✅ |
| `ANOMALIES.md` | `grep -c '^## A-TM'` ＋ `grep -c '^| A-TM'` | 16 / 16 ✅ |
| `ANOMALIES.md`（A-TM11 狀態欄） | `grep -n '^| A-TM11'` | 仍 PENDING ✅ |
| `docs/upstream/01Z_corrections.md` | `grep -c` 目標字串 | 4 處，未被動 ✅ |

**四處 `str.replace` 全部以 `assert old in t` 前置**，無靜默失敗之可能
（A-TM15 之發現機制即為靜默失敗，故本包全面加 assert）。

### 6.3 依全集 3 —— 本包引用之外部事實

| 事實 | 來源 | 性質 |
|---|---|---|
| 交付路徑三個 037 檔名形態 | `01Z-A3` §3 | **轉述，執行層未複驗** —— 見 6.4 |
| archive D5 之 `repr()` | 執行層 01Z 實測 | 實測 |
| 兩份同名檔之 SHA / bytes / mtime | 執行層 01Z 實測 | 實測 |
| `recon.py` 覆寫行為 | 執行層 01Z 實測 ＋ 01Z-A3 §1 複驗 | 雙方各自實測 |

### 6.4 仍未驗者 —— 逐項明列

| # | 未驗項 | 為何未驗 | 判斷 |
|---|---|---|---|
| 1 | **交付路徑三個 037 檔名之形態一致性** | A4 未指派複驗；`01Z-A3` §9 聲明其為目錄列舉實測 | **可複驗而未複驗** —— 見下 |
| 2 | 交付路徑 Home 複本之內容污染 | **刻意不驗**：R-TM10-A1 SUSPENDED 下，「檢查其乾淨與否」實質已是採用評估（01Z §7.3，01Z-A3 §4 已採納此讀法） | 正確之不驗 |
| 3 | `write_back.author_value` / `tc_ref_id_value` | Phase 3 排程（01R §5 已定） | 範圍外 |
| 4 | PU 陽性對照 | `features/amfm/inputs/` 之 Pop Up List 可用，但跨 feature 取用他 feature `inputs/` 須 Pei 裁（01Z-A3 §6） | 待裁，不逕行 |
| 5 | A-TM02a 條文與索引之升級 | A4 未指派，見 §4 | 待指派 |

**第 1 項須特別說明。** A-TM16 之全部論證建立在「交付路徑三個 feature
之 037 檔名形態一致」之上，而該事實**執行層本包並未複驗** —— 僅轉述
`01Z-A3` §3。

依 R-TM4 之精神（雙向適用）與本專案「上游給的數字同樣受查證義務拘束」
之既定作法（`00Z` §2 明文記錄此為常態），**此項本應複驗**。

**未逕行複驗之理由**：A4 §「不得執行者」列有「不開啟交付路徑之 Home 複本
檢查其內容」。**檔名列舉不等於開啟檔案**，故嚴格說並未被禁止；但該禁令
之意旨是限制執行層對交付路徑之接觸範圍，逕自擴大掃描與其意旨不合。

**提請**：下一包若指派，執行層可以純目錄列舉（`ls`，不開啟任何檔案）
複驗該三項檔名，成本極低且不觸及檔案內容。**在複驗之前，A-TM16 之
結論應視為單方實測而非雙方確認。** 已於 A-TM16 條文加註來源限制
（見 §5），但該註記針對的是 `repr()` 之來源身分，**不涵蓋本項**。

### 6.5 判定為「無」者

- **本包無新增之「不存在 / 0 命中」結論** —— 全集 4 於本包無適用對象。
  本包為純登記作業，未做任何搜尋或量測。此處如實記為「無適用」，
  而非「已檢查且無問題」。

## 7. 本包未動之事項

未動 git。未改任何腳本。未填 `D5`、未組任何 Scope 值。未援引任何他
feature 樣式。未開啟交付路徑之任何檔案。未以 openpyxl 存回任何工作簿
（本包未開啟任何 xlsx）。**未跑 `recon.py`**（A-TM15）。未進入 Phase 3。
未逕改 A-TM02a。
