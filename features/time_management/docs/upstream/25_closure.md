# 25 上繳 —— R-TM80 / A-TM29 落檔，條數增量回報

執行層，2026-08-22。對應下放包 `docs/handoff/25_closure.md`。
**本包僅落檔與回報，無其他動作**（依該包 §6）。

---

## 0. 逐項對照表（R-TM74）

| 項 | 內容 | 狀態 | 備註 |
|---|---|---|---|
| 1 | R-TM80 落 `RULINGS.md` | **完成** | 見 §1 |
| 2 | A-TM29 落 `ANOMALIES.md` | **完成** | 見 §1 |
| 3 | 條數增量回報 | **完成** | 見 §2 |

**未做者：無。不適用者：無。**

## 1. 落檔

- **R-TM80**（寫回指令一律帶 `--out`）→ `RULINGS.md` 末，接 R-TM79 之後。
  裁定原文照錄，另加執行層回報段：`24` 之寫回已以 `--out` 重跑至 `output/`，
  誤落 `inputs/` 之檔已刪，兩次 SHA256 相同。
- **A-TM29**（`write_back.py` 輸出預設落在輸入目錄）→ `ANOMALIES.md` 末，
  接 A-TM28 之後。狀態 PENDING、Tier 2、跨 feature。**未改腳本。**

## 2. 條數增量（R-TM46 + R-TM79 —— 兩個數）

| 區塊 | 前 | 後 | 增量 |
|---|---|---|---|
| `## R-TM`（**不含**撤回，`^## R-TM`） | 80 | **81** | **+1** |
| `## R-TM`（**含**撤回，`^## (~~)?R-TM`） | 82 | **83** | **+1** |
| `## A-TM`（`ANOMALIES.md`） | 28 | **29** | **+1** |
| `## G-TM` | 3 | **3** | 0 |

含撤回與不含撤回之差額維持 **2**（R-TM62 / R-TM63 之刪除線標題）。

## 3. 自檢 —— 不得執行者

未動 git、未改任何腳本、未改任何 TC、未觸交付路徑、未動 `output/` 內容
（工作簿與 `DELIVERY_NOTE.md` 皆維持 `24` 之狀態）、未碰 `features/vehicle_setting/`。

## 4. 現況

`features/time_management/output/` 之兩件維持不變：

```
工作簿  SHA256 2afd87be418e85599a99670db74457c3a629220583d39db195870a61093833c1
        179,991 bytes，59 條（rows 10–68），zip member 48
DELIVERY_NOTE.md   [PROPOSED]
```

**`25` §4 之四項待 Pei，執行層無可代勞者。** 其中 §4.3（A-TM28 頁名）
若確認，執行層可重跑寫回 —— 屆時依 R-TM80 帶 `--out`。

## 5. 本包產生之新條文清單（自檢 —— R-TM14）

**無。** 本包為落檔與回報。
