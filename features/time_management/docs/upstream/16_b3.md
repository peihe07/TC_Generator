# 16 上繳 —— 三條文、四項修正、**B3 生成 14 條（lint 零發現）**

執行層，2026-08-22。對應下放包 `docs/handoff/16_b3.md`。

---

## 0. 執行結果一覽

| 任務 | 內容 | 狀態 |
|---|---|---|
| T1 | R-TM69 / 70 / 71 入 `RULINGS.md` | 完成 —— **一度漏做，見 §6.1** |
| T2 | 四項修正（B3 判準、齊全性閘、B4 範圍、context PROXI） | 完成，**自驗 49 → 56** |
| T3 | B2 之 002 補 DR-5 | 完成，**另補 B1 之 001 一項真遺漏** |
| T4 | **B3 生成五片 14 條** | 完成，**B3 之 lint 發現 0 項** |
| T5 | 本包 | 完成 |

**增量（R-TM46）**：`## R-TM` **+3**（71 → 74）；`## A-TM` **0**（27）；
`## G-TM` **0**（3）。與下放包所訂增量相同。

**本包三項須先看**：**§2 之齊全性閘判準**（32 項發現中僅 1 項為真遺漏）、
§1 之**停止條件判定我一度看錯**、§4 之「掃 reasoning 誤判」已第三次出現。

---

## 1. T2 停止條件 —— 未觸發，但我一度判成觸發

T2 訂「重跑後期望抓到 002 之兩條，若仍只抓到一條即回報並停」。

**我首次以 `tail -20` 看輸出，只見 `B2.json#3` 一條 spec-gap，正要回報停止。**
完整輸出（39 行）顯示：

```
[spec-gap] B2.json#2: SWE-RA-TIME&DATE-002 之 Remarks 未含 `PENDING: DR-5` 佔位
                      （現有 ['DR-11']，但那是別的缺件）…
[spec-gap] B2.json#3: SWE-RA-TIME&DATE-002 為 A-TM13 之受影響 leaf，其 Remarks 為空…
```

**兩條都在，停止條件未觸發。** `tail` 之截斷把 `#2` 切掉了 ——
新閘同時產生 35 項 `placeholder-completeness`，把 spec-gap 擠出視窗。

**可記取者**：判定一個「有沒有抓到」的停止條件時，不能用截斷過的輸出。
本包起改以 `grep <類別>` 取該類之全部命中，再判定。

---

## 2. **齊全性閘之判準 —— 32 項發現中僅 1 項為真遺漏**

R-TM69(2) 訂「應有集合由 `arch.placeholders` + `SPEC_GAP` 推得」。
`arch.placeholders` 是該 **leaf** 之全部 Atl-Mid 物件。

### 2.1 三種判準，兩種可實作

| | 判準 | 可實作 | B1+B2 之發現數 |
|---|---|---|---|
| (a) | **逐 TC 比對該 leaf 全集**（現行實作，依條文） | ✓ | **32** |
| (b) | 逐 TC 比對「該 TC 所涉」 | **✗** | — |
| (c) | 以 **leaf 為單位取聯集** | ✓ | **1** |

**(b) 無法實作**：`spec_reference` 只含真值（Atl-Hi），Atl-Mid 物件不在
其中，無從推出「該 TC 涉及哪些 Atl-Mid」。

### 2.2 兩種可實作判準之鑑別力差距

生成時我把每個 leaf 的 Atl-Mid 物件**分散**於該 leaf 的各條 TC
（如 003 的 5 個分為 2+3）。故：

```
判準 (a)：32 項，其中 31 項為「該條沒列全 leaf 的所有 Atl-Mid」
判準 (c)：1 項 —— 001 之物件 4814069 **從未被任何一條 TC 宣告**
```

**那 1 項是真遺漏**，(a) 也抓得到，但被 31 項形式不符淹沒。

**執行層之意見**：缺口是 **leaf 層級的事實**（「該需求在 Atl-H 無對應」
與哪一條 TC 無關），佔位只要在該 leaf 出現過就能追。判準 (a) 要求每條
TC 重複列出，Remarks 會膨脹，而膨脹的部分不帶新資訊。

**未逕改為 (c)** —— 條文明文為「逐 TC」，改判準屬條文範圍。
**現行實作維持 (a)**，故 B1/B2 現有 32 項發現未清。**提請裁定。**

### 2.3 已補之一項真遺漏（超出 T3 指派，但無爭議）

T3 只指派補 002 之 DR-5。**執行層一併補了 001 之 4814069** ——
該項在 (a)(c) 兩判準下皆為缺，是本次揭出的唯一實質缺漏，補之無爭議。
已於該條 reasoning 註明補於何處與理由。

補後：判準 (c) 下 B1/B2 **歸零**；spec-gap **歸零**。

---

## 3. T4 —— B3 生成

`features/time_management/generated/B3.json`，**五片 14 條，lint 零發現**。

| leaf | 題名 | 條數 | 特殊處置 |
|---|---|---|---|
| 009 | Time Signal Validation | 5 | **9/9 Atl-Hi —— 無佔位之乾淨樣本** |
| 011 | Time Format Handling | 3 | **B-6 界線**；`$DateTmFormat$` 值域取自 LID 表 |
| 017 | Date Transmission | 2 | **B-5 界線**；3 個 Atl-Mid；**五個 TLM LID 依 R-TM62 不寫** |
| 019 | Proxi-Based Behavior | 2 | PROXI 值域入前置條件 |
| 020 | IPC Synchronization | 2 | **Atl-Hi 錨點為零 → R-TM64 零真值例外** |

### 3.1 下放包 T4 之六項注意，逐項證據

| # | 項目 | 實測 |
|---|---|---|
| 1 | 020 依 R-TM64 零真值例外 | ✓ 兩條之 `spec_reference` 皆恰為 `PENDING: DR-11 Atl-H 對應需求` |
| 2 | 017 之 3 個 Atl-Mid、019 之 1 個 | ✓ 判準 (c) 下齊全（017: 3/3、019: 1/1、020: 4/4） |
| 3 | **B-5（017 不得涵蓋 GPS 來源值送出）** | ✓ 內容欄無 `GPSDateTm`；前置條件明訂 `"Sync Time with GPS" is set to OFF`，使日期來自手動設定而非 GPS |
| 4 | **B-6（011 不得驗時間值之送出時機）** | ✓ 三條之內容欄對 008 之 `not_ours` 零命中；兩條 reasoning 明寫「不驗時間值之送出」 |
| 5 | 011 之 `$DateTmFormat$` | ✓ `TELEMATIC_FD_10.DateTmFormat` on FD，來源列 407；**值域 `2 = FORMAT_24H` 取自 LID 表 Format 欄**，非自擬 |
| 6 | 009 為乾淨樣本 | ✓ 五條**零佔位**（除一條 DR-20 之注入操作）；spec_reference 全為真值 |

### 3.2 009 之乾淨樣本值得記一項

009 之五條涵蓋：24h 下界（00）、上界（23）、分鐘上界（59）、
越界值（24）之接收端處置、以及**格式轉換在接收端**（4814009/4814010：
`the receiving module shall convert the hour to be displayed based upon the
$DateTmFormat$ signal`）。

**最後一條是 B-6 界線的正面樣本**：匯流排恆為 24h、轉換在接收端 ——
故 009 驗「送出恆為 24h」，011 驗「格式訊號之保存與廣播」，
**兩者不重疊**，而不是靠避開對方的訊號名達成。

### 3.3 017 之五個 TLM LID

context 對該五 LID 輸出 `segment: None` 與 `excluded`（R-TM62）。
**生成時完全未寫入任何訊號斷言**，改以 `TELEMATIC_TIME_DATE`
（4814019 逐字之 MESSAGE）與 IPC 顯示值為判準。

---

## 4. **「掃 reasoning 誤判」已第三次出現**

| # | 情形 | 出處 |
|---|---|---|
| 1 | 掃到「本條不涵蓋 SNA」之聲明 → 誤判 014 涵蓋 SNA | `15` §5.3 |
| 2 | 本包 B-2 複驗，同一情形 | 本包 |
| 3 | 掃到「五個 TLM LID 依 R-TM62 不寫」→ 誤判 017 寫了 TLM LID | 本包 §3.1 |

**三次皆為執行層自寫之臨時判準**，非 lint —— lint 自 R-TM70 起已排除
reasoning。**臨時判準無條文拘束，是同型錯誤反覆之處。**

三次都在報出結論前就自己抓到，但**靠的是「覺得不對再查一次」而非任何
機制**。列入未驗清單，提請是否值得約束（如：自檢判準一律排除 reasoning）。

---

## 5. T2 之其餘三項

### 5.1 B3 判準（R-TM69(1)）

改驗 `PENDING: DR-{SPEC_GAP_DR}`，並在訊息中列出「現有哪些別的 DR」，
使讀者一眼看出是「宣告了別的缺件」而非「完全沒宣告」。

### 5.2 B4 掃描範圍（R-TM70）

排除 `test_item` 上半與 `reasoning`。紅綠雙向已證：
同一訊號 `$DateTmHour$` 置於 011 之上半 → 豁免；置於下半括號 → 仍報。

### 5.3 context 之 PROXI（§3）

新增 `load_proxi_table()`（`tm_rulings`），PROXI 參數改輸出
`not_a_can_signal` 與其值域、來源分頁，**不再誤標 `PENDING: DR-6`**。

**缺檔不 raise**（與 `load_lid_table` 相反），理由寫入 docstring：
LID 表缺檔會使 segment 無來源而靜默填錯；PROXI 表缺檔只會使該參數落回
「無此 LID」之佔位路徑，該路徑已標明「須回報」，**不會產出看似正常的錯值**。

### 5.4 base_tc 因新閘轉紅，已補

`base_tc`（001）帶一個 Atl-Mid 物件，新閘使「應全綠之最小 TC」轉紅。
已補其 Remarks，**動態取自 `auth["ee_arch"]` 不寫死物件 id** ——
寫死會使該 vector 在 tsv 更新後仍然全綠，而那正是它該偵測的漂移（R-TM52）。

---

## 6. 自我更正

### 6.1 **T1 一度漏做**

我依 T2 → T3 → T4 順序執行，**跳過了 T1**（R-TM69/70/71 之落檔），
直到最終驗證時發現 `R-TM` 計數仍為 71 而非 74 才補做。

**成因**：T2 之停止條件使我把注意力全放在「先跑通兩閘再說」，
而 T1 是純文件工作、無執行結果可看，遂被略過。
**R-TM46 之增量檢查抓到了它** —— 若無該項，這一漏會直接進交付。

### 6.2 §1 之停止條件判定看錯（已於 §1 詳述）

---

## 7. 驗證輸出

```
lint（三檔）                       檔 3；發現 32 項 —— **全部為 B1/B2 之
                                   placeholder-completeness（判準 (a)）**
  其中 B3.json                     **0 項**
  其中 spec-gap                    **0 項**（補 DR-5 後歸零）
判準 (c) 下之 B1/B2/B3             **全部齊全**
lint_tcs --self-test               56 / 56（49 + 7）
build_batch_context --self-test    13 / 13
grep -c '^## R-TM' RULINGS.md      74
生成累計                            B1 19 + B2 16 + B3 14 = **49 條，14/22 片**
```

---

## 8. 未驗清單（R-TM54 三分）

### A. 可驗而未驗

| # | 項目 |
|---|---|
| A1 | **B1/B2 之 32 項 `placeholder-completeness` 未清**（判準 (a) 下），待 §2 裁定 |
| A2 | **自檢之臨時判準無約束**（§4，同型誤判已三次） |
| A3 | B2/B3 之 30 條**未經任何獨立覆核**（pilot 之結構性限制） |
| A4 | `10`–`15` 遺留：G1（9 片）、G2、G4、PROXI 設定方式、89 筆 docx 無標籤物件、`section` 未交叉驗證、A-TM26 閘只驗記錄存在 |
| A5 | 07/08/09 遺留六項 |

### B. 待 Pei

| # | 項目 |
|---|---|
| B1 | **DR 之上游查詢 —— 現況 B1 30 處、B2 25 處、B3 22 處，合計 77 處佔位** |
| B2 | 常數表 v3；A-TM25；`DateTmFormat2`；A-TM26 判準擴充 |
| B3 | RD-1 送出 |
| B4 | **B4 批次（013 / 015 / 022）之下放** —— 剩最後三片 |

### C. 已解決

| # | 項目 | 解決於 |
|---|---|---|
| C1 | B3 閘假陰性（`15` §5.2） | R-TM69(1)，重跑抓到兩條 |
| C2 | 「該有的佔位有沒有」無閘門（`15` §5.2） | R-TM69(2)，但判準待裁（§2） |
| C3 | verbatim 與界線之張力（`15` §5.3） | R-TM70，紅綠雙向已證 |
| C4 | context 誤標 PROXI 為 CAN 訊號（`15` §4.3） | 本包 T2(4) |
| C5 | R-TM67 之回溯範圍（`15` A3） | R-TM71，不回溯 |

---

## 9. 未執行者（下放包所禁，逐項確認）

- 未動 git；**未寫回工作簿**
- 未建 `tm_constants.py`
- 未自 Atlantis High 以外之架構欄取值
- **未縮減任何 leaf 之覆蓋** —— 020 雖無 Atl-Hi 錨點仍生成兩條
- **未改寫 test_item 上半之 verbatim**（R-TM70 之豁免不等於可改）
- **未預先修正 B3 之 lint 發現**（B3 本身零發現）
- 未碰 `features/vehicle_setting/`

---

## 10. 提請裁定

1. **§2 —— 齊全性閘之判準**：(a) 逐 TC 全集（現行，32 項發現，31 項為
   形式不符）vs (c) leaf 聯集（1 項，正是唯一真遺漏）。
   執行層意見：缺口是 leaf 層級的事實，(c) 之鑑別力高得多。
2. **§4 —— 自檢之臨時判準是否值得約束**（如一律排除 reasoning）。
   同型誤判已三次，每次都靠「覺得不對再查」而非機制。
3. **§6.1 —— T1 漏做由 R-TM46 之增量檢查抓到**。是否值得在上繳格式中
   固定一項「逐 T 項對照表」，使漏做在自檢階段就現形。
