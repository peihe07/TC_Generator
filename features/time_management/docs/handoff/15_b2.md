# 下放包 15 — 三項裁定、pilot 獨立抽驗、B1 放行

分析層 → 執行層。往返編號 `15`。對應上繳 `docs/upstream/15_b2.md`。
`14` 受理。

---

## 1. §2.4 不一致 —— 六條一併改 `NA`

判準是「該資料是否為互動操作」，不是佔位字串長什麼樣。
GPS 位置設定與無效訊號注入**同為互動操作**，依 canon §4.5 皆屬 Procedure。

```
R-TM66（分析層裁定，2026-08-22）—— §4.5 之判準為資料性質，非字串形態

input_test_data 之歸屬依 canon §4.5 之資料性質判定
（環境資料 → Pre-Condition／互動操作 → Procedure／獨立資料集 → 本欄），
**不得以佔位字串之字面形態為篩選依據**。

本 feature 之落實：TC#4 / #5 / #18（GPS 位置，DR-10）與
TC#15 / #16 / #17（訊號注入，DR-20）**六條之 input_test_data 一律改 `NA`**，
該操作寫在 Procedure。

執行層以字串匹配掃出前三條而漏後三條，成因即以字面代替判準
（`14` §2.4 自陳）。**同型風險**：日後新增之 DR 號若字串不同，
同一問題會再度漏掃。
```

## 2. §4.2 —— 分析層獨立抽驗：**執行層之複核成立，10 項 style 全部不成立**

執行層既寫判準又判其成立與否，故請獨立抽驗。分析層對 CFTS015 原文實查：

| 前置條件 | 分析層獨立查證 | 判定 |
|---|---|---|
| `The VES screens are powered on` | **物件 4813966 逐字**：`the time shall always be displayed in the VES screens **when the screens are powered on**` | **spec 明文條件，非預設狀態。保留** |
| `The CAN bus is asleep` | 匯流排睡眠為 008 wakeup 測試刻意造出之前提；無任何條文將其列為預設 | **保留** |
| `The CAN bus is awake` | 4813953（`Upon each CAN wakeup`）、4813960（`For each periodic message of 1000 msec`）—— 週期送出之驗證以喚醒為必要前提 | **保留**（同 S2 之邊界情形） |
| 「共同實詞」判準 | `Wake the CAN bus` ↔ `The CAN bus is awake`：`bus` 因長度 ≤3 被濾、`wake`≠`awake` | **判準無效，非 TC 之問題** |

**四項複核全部與執行層一致。10 項 style 不成立，不列入 pilot 發現。**

「共同實詞不是語意對應之有效代理」之自我定性精確 —— **該判準正是
「以結構特徵代替內容判斷」，而它出現在為檢查該形態而寫的判準裡**。
此為本 feature 第三個同型實例（前二：`01Z-A4` 之負向後查 regex 自我抵銷、
`06Z` 之 `len()` 守衛）。**併入 R-TM31 之既有註記，不另立條文。**

## 3. §3.2 紅向構造 —— 立為條文

紅向構造錯誤已兩次（`13` §1.3 之不存在物件 id、`14` §3.2 之 dict 展開序）。
**兩次現象皆為「紅向未 raise」，而成因是壞值根本不壞。**

```
R-TM67（分析層自裁，2026-08-22）—— 紅向須先證明壞值確實壞

self-test 之紅向須於斷言守衛反應之前，先以一行複驗**證明所構造之輸入
確實違反該守衛所檢查之條件**（如「改名後之 TC 含舊鍵者 0 條」）。

理由：紅向未觸發時，「守衛失效」與「壞值不壞」在現象上完全相同，
而後者會使一個實際無效之守衛被記為已驗。

依據：13 §1.3（用了不存在之物件 id）、14 §3.2（`{**r}` 先展開致新舊鍵
並存）—— 兩次皆為構造錯誤，兩次皆一度被讀為守衛問題。

本條與 R-TM42（受測物須有鑑別力）同族：前者管受測物，本條管壞值。
```

## 4. A4 —— Remarks 之佔位排列

```
R-TM68（分析層裁定，2026-08-22）—— Remarks 內多種佔位之排列

Remarks 為缺口宣告之單一落點（R-TM64）。一條 TC 若有多個佔位：

1. **一佔位一行**，不合併
2. **依 DR 號數值升冪**（DR-5 在 DR-11 之前）
3. 每行格式不變：`PENDING: DR-{n} {缺件描述}`
4. **不加小計、不加前言** —— Remarks 只承載逐列事實（R-TM47）

B2 之 002 將同時承載 DR-5（CFTS015 缺件物件）與 DR-11（Atl-Mid 引用），
依本條排列。lint 增一項：Remarks 之佔位行若非升冪即報。
```

## 5. **B1 pilot —— 放行**

盤點（逐項）：

| 項 | 狀態 |
|---|---|
| lint 對 B1.json | **0 項發現** |
| defect（分析層 7 條） | defect-1 已改（收斂為只主張外觀，理由更完整）；defect-2 見 §1 |
| defect（執行層 12 條） | 3 項，皆 §4.5 同型，§1 一併處置 |
| style-divergence | 分析層 3 項已處置；執行層 10 項經雙方複核**全部不成立** |
| 內容正確性 | 分析層覆核 7 條、執行層自檢 12 條 |

**B1 於 §1 之六條修正完成後放行。**

**須明記其限制**（A2 之自陳正確且重要）：**執行層之自檢不等於獨立覆核**。
本 feature 無 done region，pilot 為唯一人工閘，而該閘之 12/19 由產出者
自檢。**此為結構性限制，非本輪可解** —— 記入 Part VII 之 pilot 段，
使交付時該限制可見。

## 6. 指令

### T0

```bash
cd /Users/peihe/Work_Projects/TC_Generator
```

### T1 — `RULINGS.md`：追加 R-TM66 / R-TM67 / R-TM68

內文為 §1 / §3 / §4 之區塊全文。R-TM31 條末追加 §2 之「第三個同型實例」註記。
**增量**：`## R-TM` **+3**。

### T2 — 六條 `input_test_data` 改 `NA`（R-TM66）

TC#4 / #5 / #18 / #15 / #16 / #17。**改後跑 lint，回報發現數。**

### T3 — 兩個 lint 閘

1. **Remarks 佔位升冪檢查**（R-TM68）
2. **§4.5 閘**：`input_test_data` 與 `test_procedure` 若有逐字相同之行即報
   —— 此即 R-TM66 之自動化，使日後不再依賴字串匹配之人工掃描

**各附 red-green，紅向依 R-TM67 加構造複驗。**

### T4 — Part VII 增記 pilot 之結構性限制（§5）

於 Part VII 之 `Workbook sync` 節前增一段：

```markdown
### Pilot 之結構性限制

本 feature 無 done region，pilot 為唯一人工閘（canon §1.1 第三層不存在）。
B1 之 19 條中，7 條由分析層覆核，**12 條由產出者自檢** ——
自檢不等於獨立覆核。B2–B4 沿用同一形態。

此限制於交付時須為讀者可見。
```

### T5 — **B2 生成**

`002, 004, 005, 014, 016, 018, 021` 七片。

**與 B1 之差異，逐項注意**：

1. **002 / 005 含 A-TM13 缺口** —— Remarks 將同時有 DR-5 與 DR-11
   兩種佔位，依 R-TM68 排列
2. **021 之 Atl-Hi 錨點為零**（`12` §2.2）—— 其 spec_reference 依 R-TM64
   寫單一佔位 `PENDING: DR-11 Atl-H 對應需求`，明細入 Remarks
3. **014 之 5 物件中 4 個為 Atl-Mid**；018 之 3 個中 2 個
4. **B-2 界線（014 ↔ 022）無自動判準**，022 不在 B2 而在 B4 ——
   **014 之 TC 不得涵蓋 SNA 送出規則**，人工確認
5. 018 之 `ECU_RESET` 為 `PENDING: DR-8`；004 之 GPS 可用性為 `DR-10`

**生成後跑 lint，回報全部發現，不預先修正。**

### T6 — 上繳

`docs/upstream/15_b2.md`。依 R-TM54 三分列未驗清單。

### 不得執行者

- 不動 git；**不寫回工作簿**
- 不建 `tm_constants.py`
- 不自 Atlantis High 以外之架構欄取值
- 不縮減任何 leaf 之覆蓋
- 不預先修正 B2 之 lint 發現
- 不碰 `features/vehicle_setting/`

---

## 7. 呈報 Pei

**B1 pilot 放行**（六條 `input_test_data` 修正後）。lint 已歸零。

三項待你之項**開始影響 B2**：

1. **常數表 v3** —— B2 之 018 需 `ECU_RESET`、004 需 GPS 操作，
   兩者皆為 v3 之佔位條目。**不阻塞生成**（寫 `PENDING`），
   但 B2 之佔位密度會高於 B1。
2. **DR-8/9/10/11/12/20 之上游查詢** —— B1 已產生 29 處佔位，
   B2 預估更多。**佔位可寫入交付件，但每一處都是 test 執行時的斷點。**
3. **A-TM25** —— 交付件外觀是否與既有一致。

另請留意 §5 之結構性限制：**pilot 之 12/19 由產出者自檢**，
本 feature 無 done region 可仲裁。若你要更強的閘，唯一的辦法是你親自抽看。

## 8. 本包產生之新條文清單（自檢 —— R-TM14）

| 編號 | 形態 | 區塊 | 指令段指派 |
|---|---|---|---|
| R-TM66 | 分析層裁定，§4.5 判準為資料性質 | §1 | ✅ T1 + T2 + T3 |
| R-TM67 | 分析層自裁，紅向須證明壞值確實壞 | §3 | ✅ T1 + T3 |
| R-TM68 | 分析層裁定，Remarks 佔位排列 | §4 | ✅ T1 + T3 |
| R-TM31 註記 | 第三個同型實例 | §2 | ✅ T1 |

分析層本包未動 git、未改任何腳本、未改任何 TC。
