# 25 — 結案包：寫回完成，交付待 Pei

分析層。對應上繳 `docs/upstream/24_writeback.md`。**受理，本 feature 之
分析／執行工作至此結束。**

---

## 1. §2 落點偏離 —— **我的指令錯了**

`24` T2 之指令未帶 `--out`，而 `write_back.py:452` 之預設落點為
`src.with_name(...)`，即**來源檔同目錄（`inputs/`）**。
同一節之文字要求「輸出落 `output/`」——**指令與要求不一致**，
照指令執行必然落錯。

執行層之處置正確：未觸禁令（仍在 feature 目錄內、未及交付路徑）、
以 `--out` 重跑、兩次 SHA256 相同（證明寫回為決定性、落點不影響內容）、
刪除前確認該檔為本次所生且 gitignored、刪除後 `inputs/` 14 檔俱在。

**主動揭露刪除行為亦正確** —— 刪除屬不可逆，即使是自己剛產出的檔案。

```
R-TM80（分析層裁定，2026-08-22）—— 寫回指令一律帶 --out

下放包之 `write_back.py --write` 指令**一律明寫 `--out`**，
指向 `features/<feature>/output/`。不得依賴腳本預設。

理由：`write_back.py:452` 之預設為來源檔同目錄（`inputs/`），
與「輸出落 output/」之慣例相反。**預設值與慣例相衝時，
沉默地依賴預設即等於指令與要求不一致。**

腳本預設之修改（改為 `feature-dir/output/`）**跨 feature，Tier 2**，
本條不逕改，登記為 A-TM29。
```

```
A-TM29（PENDING，Tier 2 —— 跨 feature）

`write_back.py:452` 之輸出預設為 `src.with_name(src.stem + "_regen-v1.xlsx")`
即來源檔同目錄（`inputs/`），而本專案之慣例為輸出落 `output/`。

**後果**：未帶 `--out` 時，產出物落入輸入目錄。其危害有二：
一為輸入目錄混入產出物；二為若該目錄之檔案日後被當成輸入重讀，
會形成自我循環。本次因 `inputs/` 為 gitignored 且執行層即時發現，
未造成實害。

修法（改預設為 `feature-dir/output/`）影響全部 feature，Tier 2。
在其修畢前，以 R-TM80 之作業紀律迴避。
```

## 2. 驗證結果 —— 全數通過

| 項 | 結果 |
|---|---|
| dry-run 六項（R-TM78） | **全符** |
| G-TM3 正向驗證 | **PASS**（腳本內建 + 獨立重開取樣） |
| `tc_id` F10:F68 | **59 值全數互異且與序列逐列相等** |
| zip member 數 | **48**（非 47）—— x14 下拉存活，2 條 `x14:dataValidation` |
| 內容相異之 member | **僅 `xl/worksheets/sheet6.xml`**（即目標分頁），無連動 |

**`surgical_save` 之寫入路徑至此為首次實際執行，且證實與讀碼推論相符。**

§3.1 之補強（`tc_id` 逐列互異且與序列相等）**超出指派而有價值** ——
它把「取樣三列通過」升為「全 59 列之 `tc_id` 欄逐列驗證」，
排除了取樣可能漏掉的中間錯位。

---

## 3. 交付物現況

```
features/time_management/output/
  FM-WI-FSM-036-A01 …_20260817_ext_regen-v1.xlsx
      SHA256  2afd87be418e85599a99670db74457c3a629220583d39db195870a61093833c1
      大小     179,991 bytes
      內容     59 條（rows 10–68），member 48
  DELIVERY_NOTE.md   [PROPOSED]
```

### 3.1 交付件之數字（全部實測）

| 項 | 值 |
|---|---|
| TC | **59** |
| leaf 覆蓋 | **22 / 22** |
| SYS2 功能需求覆蓋 | **78 / 126 = 61.9%** |
| lint 發現 | **0** |
| `PENDING` 佔位 | **51 處**（DR-12b 25、DR-10 10、DR-20 9、DR-5 4、DR-6/8/9 各 1） |
| 獨立覆核比例 | 7 / 59 |

---

## 4. **待 Pei —— 四項，全部只有你能做**

### 4.1 交付說明之兩處定稿（`24` §1）

| 處 | 內容 | 分析層意見 |
|---|---|---|
| 「未及事項」第一點 | 揭露 7/59 獨立覆核 | **建議保留** —— 該限制交付後仍存在，先說比被問到好 |
| 48 筆之措辭 | 現行指名 SWE.1 未補件 | 若不宜指名上游，可去成因寫「尚未分解為 SWE leaf」。**但事實不宜改** |

### 4.2 交付路徑之複製與 git

`output/` 之工作簿與 `DELIVERY_NOTE.md` 複製至
`/Users/peihe/Work/02_Project_R1LR/10_Reviewing/00_TestCase/ASW-R2/Time Management/`，
其後 git、tag、`DELIVERY.sha256`（append-only）。

**分析層與執行層皆未觸及交付路徑，亦未動 git。**

### 4.3 A-TM28（`Clock` / `Clock & Date`）—— **25 處**

寫回已完成，該 25 處之 DR-12b 記號在工作簿內。
**現在改要改工作簿，成本高於改 JSON。** 若你能確認頁名，
執行層可重跑一次寫回（決定性，SHA 可預期）。

### 4.4 RD-1 與 DR 之上游查詢

Q-TM1–3 + N-TM1 仍 DRAFT 未送。DR-8/9/10/20 之 21 處為**問測試團隊
當天可解**者；DR-5 四處與 DR-6 一處卡在上游文件。

---

## 5. 本 feature 之收束

| 階段 | 結果 |
|---|---|
| 素材 | 三份 → 十四份（LID 表、HMI Settings List、SYS3 等由 Pei 陸續補入） |
| 條文 | R-TM 82 條（含 2 條撤回）、A-TM 29 條、G-TM 3 條 |
| 往返 | 25 包 |
| 產出 | **59 條 TC，22/22 leaf，lint 零發現，已寫回** |

**三項在過程中被實際攔下之錯誤**（皆會直接損及交付件）：

1. **架構範圍誤判**（R-TM75）—— 我由檔名推得「本 feature 僅 Atl-H」，
   致 40 處佔位宣告一個不存在的缺件。由你一句「本專案有 Atl-H 也有
   Atl-M」推翻。
2. **LID 表架構欄取錯**（A-TM26）—— Powernet 欄之值形態合理而實質錯誤，
   取錯不報錯。
3. **備份檔進入寫回路徑**（R-TM78）—— dry-run 顯示 114 列而應為 59，
   若直接寫回，交付件每條重複而 lint 不會報。

**三項之共同形態**：錯誤實作會產出**外觀合理之值**，
既有檢查全綠。這是本 feature 反覆記錄之主線。

## 6. 本包產生之新條文清單（自檢 —— R-TM14）

| 編號 | 形態 | 區塊 | 指令段指派 |
|---|---|---|---|
| R-TM80 | 分析層裁定，寫回指令一律帶 `--out` | §1 | 由執行層於下次寫回時適用 |
| A-TM29 | anomaly，PENDING，Tier 2（跨 feature） | §1 | 登記即可 |

**執行層本包僅需**：R-TM80 / A-TM29 落檔，回報條數增量（`## R-TM` +1、
`## A-TM` +1）。**無其他動作。**

分析層本包未動 git、未改任何腳本、未改任何 TC、未觸交付路徑。
