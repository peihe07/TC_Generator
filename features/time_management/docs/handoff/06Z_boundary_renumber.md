# 06Z — `06` 往返結案：界線改以鄰接對編號、未驗清單分類、執行期守衛

分析層 → 執行層。覆核對象：`docs/upstream/06R_corrections.md`。**受理。**

**兩項提請全部成立**，且 §4.4 之觀察值得立條。

---

## 1. §2.1 界線計數 —— 提請成立，改以鄰接對編號

「五條」是以**條文**為單位數（R-TM17 三條 + R-TM25 兩條），
而 R-TM25 之第一條（`014 ↔ 008/017`）內含兩個鄰接對。
**兩種數法各自成立，混用時不精確** —— 且不精確之處恰好落在
「哪幾對有自動判準」這個要靠計數對照的問題上。

```
R-TM53（分析層裁定，2026-08-21）—— §8.2.1 界線改以鄰接對編號

Part VII 之相鄰組界線改以**鄰接對**為單位編號 B-1 … B-6，
取代原先以條文為單位之「五條」說法：

  B-1  004 ↔ 010     GPS fallback vs 收到之無效訊號（觸發源）
  B-2  014 ↔ 022     GPS 送出 vs SNA 規則歸屬
  B-3  018 ↔ 011     重開之後：時間值 vs 格式
  B-4  014 ↔ 008     GPS 來源值 vs 送出時機與觸發
  B-5  014 ↔ 017     GPS 來源值 vs 日期通道
  B-6  011 ↔ 008     格式訊號保存重送 vs 時間值傳輸

B-1…B-3 出自 R-TM17（02R §3.4），B-4…B-6 出自 R-TM25（03Z §2）。
**條文來源不變，只改編號單位。**

每一對須明標其**有無 lint 自動判準**（`BOUNDARY_SIGNALS` 有訊號名者為有，
僅在 `BOUNDARY_NOTES` 者為無）。該對照由執行層依 `tm_rulings.py` 之
實際內容產出，**分析層不預先指定**——`06R` 上繳之表與本包之配對存在
差異（其列 `008↔014` 與 `014↔008/017` 兩列），須以實際資料為準。

無自動判準者，其驗證責任明歸 B1 pilot，且須於 pilot 檢查表**逐對列出**。
```

## 2. §4.4 —— 未驗清單須分類，立為條文

> 3 項結構性不可複驗者若續掛於「待辦」，會使清單永遠清不完 ——
> 而它們本就不是執行層能清的。8 項已解決者亦應自清單移除，
> 否則累積成噪音。

**這是 R-TM50 之作業面後果，我立該條時只想到「標示要正確」，
沒想到「標錯會使清單失效」。**

```
R-TM54（分析層自裁，2026-08-21）—— 未驗清單須三分，不得混列

每包之「該驗而未驗」清單須分三區，不得混為一列：

  A. 可驗而未驗 —— 執行層能清，列為待辦
  B. 結構性不可複驗 —— 來源在 repo 之外（Project 層指令、聊天層歷史、
     他方未提交之工作樹、Pei 之決定）。**移出待辦，另立「待 Pei」區**
  C. 已解決 —— 註明解決之包號後**自清單移除**

理由：三者混列時，清單長度不再反映剩餘工作量。B 類永遠清不完而使清單
看似停滯，C 類累積成噪音而使 A 類被淹沒 —— 兩者都使該清單失去
「還剩什麼」之指示功能。

依據：06R §4.4 之三包重判（19 項中已解決 8、可驗未驗 8、
結構性不可複驗 3、已失效 1）。
```

**「已失效」為第四種**（canon §10.7 Rules 3 隨整節改寫而消滅）——
併入 C 區處理，註明失效原因。

## 3. §3.2 C-2 退化紅向 —— R-TM42 之第二次應驗

首次 12/13，失敗於「抽空後仍綠」，成因為 **B1 不含 A-TM13 兩片**，
退化操作無物可抽而 `if not gaps: return c["batch"] != "B2"` 恆真。

**這正是 R-TM42 所述之情形**，且比母本 sheet 顯示序那例更隱蔽 ——
後者是資料巧合，本例是**受測批次之組成使該類根本不存在**。

兩處修正皆正確：判準改 `bool(gaps) and all(...)`（不再以「無 gap 即通過」
為滿足），self-test 自動取含 A-TM13 之批次並印出切換。
**B1 與 B2 皆 13/13 之雙批驗證**，是「不論以哪一批跑都真的測到」之證明。

## 4. §3.4 執行期守衛 —— 提請成立，本包指派

執行層之初步判斷（C-3 / C-4 可加執行期守衛；C-1 / C-2 / C-6 之退化情形
在資料層即為 `tm_rulings` 常數被清空，以模組載入時斷言攔截較合適）
**分析層同意**，且其區辨正確：**守衛要加在錯誤發生的那一層**，
加在下游只是把同一個問題往後推。

`tm_rulings.py` 之模組載入斷言尤其恰當 —— 該模組「只含裁決值、不含邏輯」，
其常數被清空即代表裁決值遺失，**那是任何下游都無法補救的失效**。

## 5. 指令

### T0

```bash
cd /Users/peihe/Work_Projects/TC_Generator
```

### T1 — `RULINGS.md`：追加 R-TM53 / R-TM54

標題行 `## R-TM53 — §8.2.1 界線改以鄰接對編號`、
`## R-TM54 — 未驗清單須三分，不得混列`，內文為 §1 / §2 之區塊全文。

R-TM23 / R-TM25 / G-TM1 項 4 之「五條」字樣，**依 R-TM13 加註**
（不改原文）指向 R-TM53 之新編號。

**增量**：`## R-TM` **+2**；`## G-TM` **0**；`## A-TM` **0**。

### T2 — 界線對照表（依實際資料，非依本包之配對）

依 `tm_rulings.py` 之 `BOUNDARY_SIGNALS` / `BOUNDARY_NOTES` **實際內容**
產出 B-1…B-6 之對照表，每對標：

- 有無 lint 自動判準
- 若有，其 owns / not_ours 之訊號名（**逐項列出，不只計數** —— R-TM31）
- 若無，其區辨依據（觸發源／規則歸屬）

**若實際資料之配對與 `06Z` §1 之 B-1…B-6 不一致，以實際資料為準並回報差異**
—— 本包之配對由分析層依條文推得，未對 `tm_rulings.py` 實測。

表落 `tm_rulings.py` 之檔頭註解與 `RULINGS.md` 之 R-TM53 回報段兩處，
**兩處須逐字相同**（單一來源之同一精神；若無法逐字相同，說明原因）。

### T3 — 執行期守衛（§3.4）

- `tm_rulings.py`：模組載入時斷言 —— `TEST_SETS` 七值、`BOUNDARY_SIGNALS`
  三條且各非空、`SPEC_GAP` 兩片、`TEST_SET_OF` 覆蓋 22 片。
  **任一不成立即 raise**，訊息指明遺失之常數名
- `build_batch_context.py`：C-3（`rendered` 格式）與 C-4
  （`upper_verbatim` 逐字出現於來源）之執行期守衛

**各附 red-green**：紅向以 monkeypatch 清空該常數／改寫該值構造，
**不觸及來源檔**。

### T4 — 驗證（R-TM31 列明細；R-TM46 增量）

```bash
grep -n '^## R-TM5[34]' features/time_management/RULINGS.md
grep -n 'B-1\|B-6' features/time_management/RULINGS.md features/time_management/scripts/tm_rulings.py
python3 features/time_management/scripts/build_batch_context.py --self-test
python3 features/time_management/scripts/lint_tcs.py --self-test
python3 -c "import sys; sys.path.insert(0,'features/time_management/scripts'); import tm_rulings; print('module assertions OK')"
```

回報兩支 self-test 之總數變化（以實測為準）。

### T5 — 上繳

`docs/upstream/06Z_corrections.md`。**依 R-TM54 之三分格式**列未驗清單。
須含 T4 全部輸出、T2 之對照表、T3 之 red-green。

### 不得執行者

- 不動 git（除非 Pei 直接指示）
- **不生成任何 TC**
- 不改 `backend/`、不改 canon、不改 `docs/fw036/framework.md`
- 不修改任何既有上繳包或下放包
- 不填 `functional_safety`、不杜撰 CAN 網段
- 不動 `TODO(R-TM10-A1)` 之步驟措辭常數與 ER 樣板
- 不碰 `features/vehicle_setting/`
- 不送出 RD-1

---

## 6. 呈報 Pei —— 同兩項，第三次

管線技術面已備妥（context 13/13、lint 21/21、單一來源模組、寫回六項、
執行期守衛待本包）。**B1 之阻塞只剩兩個，都不是技術問題：**

1. **`functional_safety` 之值**（A-TM24）—— 來源 1 已實測否定。
2. **步驟措辭常數與 ER 樣板**（R-TM10-A1）—— (a) 解除並指定樣式來源，
   或 (b) 維持 SUSPENDED、本 feature 自擬並接受與他 feature 不一致。
   **若選 (b)，我下一包直接擬常數表供你過目。**

依 R-TM54，此二項自即日起**不再列於執行層之待辦清單**，
改列「待 Pei」區 —— 它們本就不是執行層能清的。

## 7. 本包產生之新條文清單（自檢 —— R-TM14）

| 編號 | 形態 | 區塊 | 指令段指派 |
|---|---|---|---|
| R-TM53 | 分析層裁定，界線改鄰接對編號 | §1 | ✅ T1 + T2 |
| R-TM54 | 分析層自裁，未驗清單三分 | §2 | ✅ T1 + T5 |

分析層本包未動 git、未改任何腳本、未改 canon、未改 `backend/`。
