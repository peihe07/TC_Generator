# 下放包 17 — 齊全性判準改 (c)、自檢判準約束、逐 T 對照表、B4 生成

分析層 → 執行層。往返編號 `17`。對應上繳 `docs/upstream/17_b4.md`。
`16` 受理。**B3 零發現，009 之乾淨樣本尤其好。**

---

## 1. §2 齊全性判準 —— **改採 (c) leaf 聯集**

執行層之論證成立，且其後果已在數字上顯現：**32 項發現中 31 項為形式不符，
把唯一的真遺漏（001 之 4814069）淹掉了。**

這正是 `06` §4.3 所立之形態：**誤報比漏抓惡劣，因其主動製造雜訊使真發現
不可見。** 判準 (a) 之 31 項雜訊率為 97%。

```
R-TM72（分析層裁定，2026-08-22）—— 齊全性閘改以 leaf 為單位

R-TM69(2) 之 `lint_placeholder_completeness` 判準由「逐 TC 比對該 leaf
全集」改為 **「以 leaf 為單位取聯集比對」**：

  該 leaf 之全部 TC 之 Remarks 佔位聯集，須等於該 leaf 應有之佔位集合。
  缺與多皆報，**報在 leaf 層而非逐 TC**。

理由三點：
1. **缺口是 leaf 層級之事實** —— 「該需求在 Atl-H 無對應」與哪一條 TC
   驗證它無關。逐 TC 重複列出，Remarks 膨脹而不帶新資訊。
2. **判準 (b)（逐 TC 比對該 TC 所涉）不可實作** —— `spec_reference`
   只含真值，無從推出該 TC 涉及哪些 Atl-Mid 物件（執行層 §2.1 已證）。
3. **鑑別力**：(a) 32 項中 31 項為形式不符，雜訊率 97%，
   真遺漏被淹沒；(c) 恰好 1 項且即為該真遺漏。

**可讀性之保證**：同一 leaf 之 TC 於工作簿中相鄰（tc_id 依序），
故讀者掃該 leaf 之數列即見全部佔位，不因分散而漏看。

**B1/B2 之 32 項發現隨本條歸零**（判準 (c) 下已齊全，執行層 §7 實測）。
001 之 4814069 已補，該補正確且應保留。
```

## 2. §4 自檢臨時判準 —— 立為條文

同型誤判三次（`15` §5.3、本輪兩次），**三次皆為執行層自寫之臨時判準，
而 lint 自 R-TM70 起已排除 reasoning**。即：**規則已修，而人工檢查
沒跟上同一條規則。**

```
R-TM73（分析層自裁，2026-08-22）—— 自檢之臨時判準須與對應 lint 閘同範圍

執行層為自檢而臨時撰寫之掃描判準，其**掃描範圍須與 lint 中對應閘門
一致**。lint 已排除者（`reasoning`、`test_item` 上半 —— R-TM70），
臨時判準亦須排除。

理由：三次同型誤判（掃 reasoning 而把「本條不涵蓋 X」之聲明本身
當成違規）皆發生於臨時判準，而非 lint —— **lint 受條文拘束並經紅綠雙向，
臨時判準兩者皆無。**

**最低要求**：臨時判準之掃描範圍須在該次回報中明列
（掃哪些欄、排除哪些），使誤判之成因可被對造判讀，
而非只在「覺得不對再查一次」時偶然發現。

依據：15 §5.3、16 §4 之三次同型誤判。
```

## 3. §6.1 T1 漏做 —— 上繳格式固定逐 T 對照表

`R-TM46` 之增量檢查抓到了它，**但那是間接的** —— 若該包無條文新增，
增量為 0，漏做就不會現形。

```
R-TM74（分析層自裁，2026-08-22）—— 上繳須含逐 T 項對照表

上繳包之 §0 須列**下放包之全部 T 項編號**，逐項標
「完成／未做／不適用」，未做者附理由。

不得只列已做者 —— 只列已做者時，漏做之項不會出現在表上，
而讀者無從察覺其缺席（**缺席不是可見的狀態**）。

依據：16 §6.1 —— T1 被跳過，靠 R-TM46 之增量檢查間接抓到；
若該包無條文新增，增量為 0，該漏做不會現形。
```

**另採納執行層之作業改進**（§1）：判定「有沒有抓到」之停止條件時，
不得用截斷過的輸出（`tail`）；改以 `grep <類別>` 取該類全部命中再判。
**此為作業紀律，併入 R-TM74 之回報要求，不另立條文。**

## 4. B3 之三項正面，記錄備查

**(1) 009 §3.2 之界線論證** —— 匯流排恆為 24h、轉換在接收端
（4814009/4814010 逐字），故 009 驗「送出恆為 24h」、011 驗「格式訊號之
保存與廣播」，**兩者不重疊是因為 spec 本身如此切分，不是靠避開對方的
訊號名達成**。這是 B-6 界線的正面樣本，**比任何 lint 閘更能說明界線為何
成立**。

**(2) §5.3 之 PROXI 缺檔不 raise** —— 其理由（LID 缺檔會靜默填錯，
PROXI 缺檔只會落回已標明須回報之佔位路徑）**是 R-TM39 之正確應用**：
兩個缺檔之後果不對稱，故處置不對稱。

**(3) §5.4 之 `base_tc` 動態取值** —— 不寫死物件 id，因寫死會使該 vector
在 tsv 更新後仍全綠。**R-TM52 之正確應用。**

## 5. 指令

### T0

```bash
cd /Users/peihe/Work_Projects/TC_Generator
```

### T1 — `RULINGS.md`：追加 R-TM72 / R-TM73 / R-TM74

內文為 §1 / §2 / §3 之區塊全文。**增量**：`## R-TM` **+3**。

### T2 — 齊全性閘改判準 (c)（R-TM72）

改後對 B1/B2/B3 重跑 lint，**期望三檔皆零發現**。
非零即回報並停（以 `grep placeholder-completeness` 取全部命中再判 —— §3）。

**red-green**：紅向以「某 leaf 之某 Atl-Mid 物件在該 leaf 全部 TC 中皆未
宣告」構造，依 R-TM67 加構造複驗。

### T3 — **B4 生成（最後一批）**

`013, 015, 022` 三片。

**注意事項**：

1. **022 SNA Handling** —— B-2 界線之另一側。014 已在 B2，
   **022 得涵蓋 SNA 送出規則**（該規則屬 022），
   但**不得涵蓋 GPS 值之送出正確性**（屬 014）。
   014 之寫法可作對照。
2. **013 DST Handling** —— 依 R-TM60，無手動 DST 開關；
   觸發為 `CROSS_DST_BOUNDARY`（`PENDING: DR-10`）。
   其 spec 錨為 4813995（`adjusted automatically`），**單一物件**。
3. **015 Manual Date Handling** —— 9 個物件中 4 個為 Atl-Mid；
   與 001（Manual Time Setting）為 `Manual Setting` 組之另一片，
   **001 之寫法可作對照**，但**日期設定項之 UI 標籤須查 HMI Settings
   List**，無來源者寫 `PENDING: DR-12`。

**生成後跑 lint，回報全部發現，不預先修正。**

### T4 — 上繳

`docs/upstream/17_b4.md`。**依 R-TM74 列逐 T 對照表**（本包 T1–T4 全列）。
依 R-TM54 三分列未驗清單。

### 不得執行者

- 不動 git；**不寫回工作簿**
- 不建 `tm_constants.py`
- 不自 Atlantis High 以外之架構欄取值
- 不縮減任何 leaf 之覆蓋
- 不改寫 test_item 上半之 verbatim
- 不預先修正 B4 之 lint 發現
- 不碰 `features/vehicle_setting/`

---

## 6. 呈報 Pei —— **B4 之後即為寫回**

B4 三片完成後，22 片全覆蓋，累計約 60 條 TC。**其後之工作性質改變**：

| 階段 | 內容 | 誰 |
|---|---|---|
| 寫回 | `surgical_save` 首次實跑（**該路徑至今從未執行**）+ G-TM3 正向驗證 | 執行層 |
| 交付 | git、tag、DELIVERY.sha256 | **你** |

**兩件在寫回前必須有答案**：

1. **DR 之上游查詢** —— 現況 77 處佔位，B4 後預估破 85。
   **佔位會寫進交付件。** 若上游能在寫回前答覆其中幾項，
   該批佔位可替換為真值；否則交付件帶著佔位出去，
   每一處都是測試執行時的斷點。
2. **A-TM25** —— 交付件之引號與措辭是否與既有交付件一致。
   **寫回後再改的成本遠高於現在** —— 現在改 JSON，寫回後要改工作簿。

其餘：常數表 v3、`DateTmFormat2`、RD-1 送出。

## 7. 本包產生之新條文清單（自檢 —— R-TM14）

| 編號 | 形態 | 區塊 | 指令段指派 |
|---|---|---|---|
| R-TM72 | 分析層裁定，齊全性閘改 leaf 聯集 | §1 | ✅ T1 + T2 |
| R-TM73 | 分析層自裁，臨時判準須與 lint 同範圍 | §2 | ✅ T1 |
| R-TM74 | 分析層自裁，上繳須含逐 T 對照表 | §3 | ✅ T1 + T4 |

分析層本包未動 git、未改任何腳本、未改任何 TC。
