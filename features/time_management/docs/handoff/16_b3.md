# 下放包 16 — B3 閘修正、verbatim 與界線之張力、B3 批次生成

分析層 → 執行層。往返編號 `16`。對應上繳 `docs/upstream/16_b3.md`。
`15` 受理。**四項提請全部成立，其中一項改變 lint 之結構。**

---

## 1. §5.2 B3 假陰性 —— 條文變更使既有閘門靜默失效

**診斷正確且是本包最重要的一項。** R-TM64 之前，Remarks 只承載 DR-5，
故「Remarks 含 `PENDING: DR-`」等值於「該 leaf 之缺口已宣告」。
**R-TM64 使 Remarks 成為全部佔位之單一落點後，該等值關係破裂，
而 B3 未隨之調整。**

```
R-TM69（分析層裁定，2026-08-22）—— 佔位之閘門須驗特定 DR 號，並增齊全性閘

1. **B3（lint_spec_gap）之判準改為**：該 leaf 若在 `SPEC_GAP` 內，
   其 Remarks 須含 `PENDING: DR-{該 leaf 之特定 DR 號}`
   （002 / 005 為 DR-5），**非「含任一 PENDING: DR-」**。

2. **新增齊全性閘 `lint_placeholder_completeness`**：
   逐 TC 比對「應有之佔位集合」與「Remarks 實有之佔位集合」——
   應有集合由 context 之 `arch.placeholders` + `SPEC_GAP` 推得，
   兩集合不等即報，**缺與多皆報**。

   理由：R-TM68 只驗排列、B3 只驗特定號之存在，
   **「該有的都有沒有」在本包之前無任何閘門負責**（執行層 §5.2 自陳）。

3. **一般化**：條文變更若改變某欄位之語意（如「Remarks 承載什麼」），
   **須逐一檢查以該欄位為判準之既有閘門**。R-TM64 未做此檢查，
   致 B3 靜默失效兩輪。

   本條與 R-TM61（搜尋須兼搜字面量）同族：前者管找得全，
   本條管改得全。
```

## 2. §5.3 verbatim 與界線之張力 —— **界線不及於 verbatim 上半**

執行層之處置（procedure/ER 不驗 SNA + reasoning 明寫不涵蓋）**足夠**，
但其所疑之「張力」本身有更根本之解：

```
R-TM70（分析層裁定，2026-08-22）—— §8.2.1 界線不及於 verbatim 上半

§8.2.1 之相鄰組界線拘束的是**作者所撰之內容**（test_item 下半、
pre_conditions、input_test_data、test_procedure、expected_result），
**不及於 test_item 上半之 verbatim 需求原文**。

理由：上半為逐字照錄之上游文字（canon §4.3.1，來源限
`leaf_descriptions.txt`），作者無權改寫。若界線及於上半，
唯一的遵守方式就是改寫需求原文 —— **那正是 §4.3.1 所禁**。
兩條規則不可能同時要求「照錄」與「不得出現某詞」。

先例同源：既有之「verbatim 上半豁免於作者品質檢查」為同一原理，
本條為其在 §8.2.1 上之延伸。

**lint 之 B4（界線閘）隨之調整**：掃描範圍排除 `test_item` 之上半
（第一段），只掃下半括號與其餘內容欄。
**reasoning 亦排除** —— 執行層 §5.3 之自我更正已證：掃 reasoning 會把
「本條不涵蓋 SNA」這句聲明本身當成違規。

014 之現行處置**維持不變**，其 reasoning 之明寫予以保留 ——
該註記使人工覆核者不必重新推導。
```

## 3. §4.3 PROXI 參數 —— 指派修正

`$NAV_Presence$` 等被 `SIGNAL_RE` 抓為 CAN 訊號而標 `PENDING: DR-6`，
**語意錯誤**（其為 PROXI 參數，值域已在 `proxi_atlantis_high.tsv`）。

執行層生成時已正確寫入前置條件而未寫三件組，但 **context 之輸出仍誤導**。
指派修正（§5 T2）。

## 4. A3 回溯構造複驗 —— **只對新增者與曾失敗者適用**

```
R-TM71（分析層自裁，2026-08-22）—— R-TM67 之適用範圍

構造複驗（R-TM67）適用於：
  (a) 本條立訂後**新增**之紅向；
  (b) 既有紅向中**曾經出現過構造錯誤者**（13 §1.3、14 §3.2 二處）。

**既有 42 項不回溯補做。** 理由：回溯之成本與其發現機率不成比例 ——
該 42 項皆已通過紅綠雙向且多數經多輪重跑；而構造錯誤之特徵是
「紅向不觸發」，該 42 項若有此問題，早已在歷次重跑中顯示為失敗。

**但若某既有紅向日後出現不觸發**，須先依 R-TM67 複驗構造，
不得逕判為守衛失效。
```

## 5. 指令

### T0

```bash
cd /Users/peihe/Work_Projects/TC_Generator
```

### T1 — `RULINGS.md`：追加 R-TM69 / R-TM70 / R-TM71

內文為 §1 / §2 / §4 之區塊全文。**增量**：`## R-TM` **+3**。

### T2 — 四項修正

1. **B3 判準改驗特定 DR 號**（R-TM69(1)）
2. **新增 `lint_placeholder_completeness`**（R-TM69(2)），缺與多皆報
3. **B4 掃描範圍排除 test_item 上半與 reasoning**（R-TM70）
4. **context 對 PROXI 參數不輸出 `PENDING: DR-6`**（§3）——
   改輸出其 PROXI 值域與來源分頁，供前置條件使用

**各附 red-green，紅向依 R-TM67 加構造複驗。**

**T2(1)(2) 完成後對 B2.json 重跑 lint** —— 期望抓到 002 之
**兩條**（TC#2 與 TC#3），非一條。**若仍只抓到一條，回報並停。**

### T3 — B2 之修正

依重跑結果補 002 之缺漏 DR-5 佔位（TC#2 / TC#3），
Remarks 依 R-TM68 升冪。改後 lint 應歸零。

### T4 — **B3 批次生成**

`009, 011, 017, 019, 020` 五片。

**與前兩批之差異**：

1. **020 之 Atl-Hi 錨點為零**（同 021）—— R-TM64 零真值例外
2. **017 之 4 物件中 3 個為 Atl-Mid**；019 之 3 個中 1 個
3. **B-5 界線（014 ↔ 017）** —— 017 不得涵蓋 GPS 來源值之送出；
   014 已在 B2，**其寫法可作為對照**
4. **B-6 界線（011 ↔ 008）** —— 011 不得驗時間值之送出時機；
   008 已在 B1，**其寫法可作為對照**
5. **011 為唯一涉 12H/24H 格式者**（`09` §5.3 之更正），
   其 `$DateTmFormat$` 在 Atl-Hi 為 `TELEMATIC_FD_10.DateTmFormat` on FD
6. **009 為 B1/B2/B3 中 Atl-Hi 錨點最完整者**（9/9）——
   **可作為「無佔位之乾淨樣本」**，其寫法宜特別留意

**生成後跑 lint，回報全部發現，不預先修正。**

### T5 — 上繳

`docs/upstream/16_b3.md`。依 R-TM54 三分列未驗清單。

### 不得執行者

- 不動 git；**不寫回工作簿**
- 不建 `tm_constants.py`
- 不自 Atlantis High 以外之架構欄取值
- 不縮減任何 leaf 之覆蓋
- **不改寫 test_item 上半之 verbatim**（R-TM70 之反面：豁免不等於可改）
- 不預先修正 B3 之 lint 發現
- 不碰 `features/vehicle_setting/`

---

## 6. 呈報 Pei

**B2 16 條已生成，B3 五片同時下放。** 進度：22 片中已生成 14 片（B1 7 + B2 7），
B3 五片後剩 B4 三片。

**§5.2 值得你知道**：R-TM64（我裁的）使一個既有 lint 閘門靜默失效兩輪 ——
**條文之間也會互相破壞，不只程式之間。** 已立 R-TM69 要求條文變更時
逐一檢查以該欄位為判準之既有閘門。

仍待你（**已開始影響交付品質，非只是流程**）：

1. **DR-5/8/9/10/11/12/20 之上游查詢** —— B1 29 處佔位、B2 25 處，
   B3/B4 後預估破 70。**每一處都是測試執行時的斷點**，
   而交付件會帶著這些佔位出去。
2. **常數表 v3** —— B2 已用到其三個佔位條目。
3. A-TM25、RD-1 送出。

## 7. 本包產生之新條文清單（自檢 —— R-TM14）

| 編號 | 形態 | 區塊 | 指令段指派 |
|---|---|---|---|
| R-TM69 | 分析層裁定，佔位閘驗特定 DR 號 + 齊全性閘 | §1 | ✅ T1 + T2 |
| R-TM70 | 分析層裁定，界線不及於 verbatim 上半 | §2 | ✅ T1 + T2 |
| R-TM71 | 分析層自裁，R-TM67 適用範圍 | §4 | ✅ T1 |

分析層本包未動 git、未改任何腳本、未改任何 TC。
