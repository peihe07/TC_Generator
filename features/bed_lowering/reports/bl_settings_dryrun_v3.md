# BedLowering dry-run **v3** 摘要（VS-SL-03 §1）

日期：2026-09-02　明細：`bl_settings_dryrun_v3.tsv`（151 列）

本 feature 之列不經別名綁定（入口非 Settings List），**v3 與 v2 之報告內容相同**：

| 項 | 數 |
|---|---:|
| 報告列 | **151**（自檢 PASS） |
| `PROSE_PRECOND` | 151 |
| `DT_APPLICABILITY` | **23** = DT 專屬 7 ＋「DT 或 DJ/D2」16 |
| `DJD2_ONLY` | 8 |

提議（151 列一致）：`PROXI CAN node 27 (ASM/ASCM) = 1 (Present)` ＋
`PROXI Body_Types = PENDING（待裁定）`。

DT 適用性之查證見 `bl_dt_applicability.md`（8 檔：有 1／無 1／未提及 6）；
DR 稿第三節見 `features/vehicle_setting/reports/dr_draft_vssl.md`。
沙盒寫回稿：`features/bed_lowering/sandbox/vssl/bl_vssl.xlsx`（車型欄**不填**，待 DR）。
