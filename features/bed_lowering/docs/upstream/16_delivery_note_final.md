# 上繳包 16 — Bed Lowering Mode：交付說明定稿

日期：2026-08-27
對應下放包：`features/bed_lowering/docs/handoff/16_delivery_note_final.md`
（sha256 `387d8306b4ba7de35047bc3f179c2a516ae4bf9be2ac300c3540738907a139eb`）
執行層：Tier 1

**結論：B 類文法修正 4 格已寫回（工作簿 `10` → `11`），`DELIVERY_NOTE.md` 四節更新完畢，
定稿對帳 11 項全數相符。三項交付前置全數完成，`DELIVERY_NOTE.md` 狀態已改為
「交付授權可請求」。**

**執行層未請求授權、未預作交付**（下放包 §四）。

---

## 一、B 類修正（§一-1）

### 1.1 命中確認

全批掃描 `\binformation\s+are\b`，**命中 4 處，與下放包所載逐處相符**：

| req_id | 欄 | 行 |
|---|---|---|
| `SWE1-HMI-BLM-013-01` | `test_procedure` | 2 |
| `SWE1-HMI-BLM-013-01` | `expected_result` | 2 |
| `SWE1-HMI-BLM-014-01` | `test_procedure` | 2 |
| `SWE1-HMI-BLM-014-01` | `expected_result` | 2 |

改後全批殘留 **0**；工作簿內殘留亦 **0**（逐列複掃 151 列）。

### 1.2 寫回 —— 只動 4 格

`patch_cells.py` 原只能清空（上繳 07 §四-4 已具名此缺口）。
本包補上 `--set-from`，自 batch json 取指名 `req_id` 之指名欄覆寫既有列：

```
set 列149 SWE1-HMI-BLM-013-01 test_procedure: 已改
set 列149 SWE1-HMI-BLM-013-01 expected_result: 已改
set 列152 SWE1-HMI-BLM-014-01 test_procedure: 已改
set 列152 SWE1-HMI-BLM-014-01 expected_result: 已改
sheets_patched {'Test Case Specification 測試用例規範': 4}
set round-trip：4 格，不符 0
```

**`sheets_patched` = 4，恰為指名之四格。** 不重跑 `write_back` 之理由同上繳 09 §1.2：
那會由 batch json 重建每一格，使 batch 內任何他處改動搭順風車混進本次修訂。

**列號由 `req_id` 反查而非推算** —— 列號隨批次順序而定，
推算會在批次順序有任何變動時錯位。

| 檢查 | 結果 |
|---|---|
| 保全計數（`10` vs `11`）| zip 48／sheet 9／legacy DV 4／**x14 DV 1**／extLst 3 全等，**差異欄位僅 `sha256`**（size 亦同）|
| set round-trip | 4 格，不符 0 |
| 全簿 lint（151 列）| **clean — 0 findings** |

新工作簿 `bed_lowering_11.xlsx`
sha256 `353b87f59a19121d23d1018330b043b8957699f156273b675f979f31995574a1`

---

## 二、`DELIVERY_NOTE.md` 更新（§二）

| 節 | 更新 |
|---|---|
| 標頭 | 工作簿指向 `bed_lowering_11.xlsx`，**並補上完整工作簿鏈**（`00` … `11`，11 段）；狀態由「尚未可交付」改為「三項前置全數完成，授權可請求」|
| §3 | 新增 **§3.2**：B7 全查所見之 013/014 vs 015/016 驗證重疊，註明成因為上游切分維度不同（內容類型 vs 顯示裝置）、保留未合併未製造差異 |
| §4 | **全節重寫**：加入同儕基準對照表、結構性事實（訊號一律落 ER 中段）、**基準之四項限制**、以及 §4.4 之數字更正始末 |
| §5 | 加註四筆 DR 皆 2026-08-27 送出 |
| §8 | 三項前置逐項記其完成方式；併記本包之 B 類修正 |

### 2.1 §4.3 之限制照實寫入

下放包 §二-1 要求併載基準之限制。已寫入四項，其中最要緊者：

> **唯一之非本管線人寫本（CFTS044，191 列）套不上判準** ——
> ER 為自由散文，同一句同時是訊號斷言與畫面斷言。
> **故本基準未涵蓋「人手寫的 HMI 工作簿長什麼樣」。**

**這一項不能省。** §4.1 之結論（80.8% 是常態形狀）建立在兩本**同管線**產出上；
若讀者以為它也涵蓋人寫本，會把結論推得比證據遠。

### 2.2 §4.4 之更正始末保留原文脈絡

依 R-TM13，上繳 13 §六-2 之原文未刪、標明為錯，
`DELIVERY_NOTE.md` §4.4 記其始末（估值非量測、各批判準不一致、逐批數字亦全錯）。

### 2.3 一處排版修正

§6 轉載 `COVERAGE_GAPS.md` 之「判準與其模稜之處」時，該節原為 `##` 層級，
會打斷交付說明之節次編號（§6 之後直接冒出一個無編號 `##`）。
已降為 `### 6.1` 並註明「轉載自 `COVERAGE_GAPS.md`」。

---

## 三、定稿對帳（11 項全數相符）

```
OK  工作簿 151 列
OK  附表 vs 工作簿逐列差異 0        ← 讀 bed_lowering_11.xlsx 實檔，非讀 batch json
OK  nonbus 122        OK  pending 12        OK  gap 13
OK  151 + 12 + 13 = 176
OK  §4 載 122（80.8%）              OK  §4 載同儕 100.0%
OK  §8 載三項完成                   OK  §5 載送出日
OK  標頭指向 workbook_11
```

---

## 四、執行層自陳

1. **B7 全查為分析層執行，本包未複驗其結論**。§一之 B 類修正依其結果照做，
   §一-2 之重疊觀察依其描述轉入交付說明。**若該全查本身有誤，本包不會發現。**
2. **`patch_cells.py --set-from` 為本包新增，僅本次一用**。
   其 round-trip 驗證覆蓋指名之 4 格，**未驗證「未指名之格確實沒被碰到」** ——
   該保證來自 `surgical_save` 之 `verify_structure`（非目標成員不得差異）
   與 `sheets_patched: 4` 之計數，非來自逐格比對。
3. **交付說明 §7 之操作化判斷清單仍為蒐集所得、非窮舉**（同上繳 14 §四-3）。
   本包未再擴充該清單。
4. **未請求交付授權、未預作任何交付動作**（下放包 §四）。

---

## 五、未結 DR（皆已送出 2026-08-27，待回覆）

| DR | 待回覆後動作 |
|---|---|
| DR-1 | 7 條 PENDING 代入 + 各批 `provisional_inputs` 全數複驗 |
| DR-2 | 041-01／02 收緊為具體值（不阻斷）|
| DR-3 | 033-04 代入 + 033-02／034-02 連動複驗 |
| DR-4 | 4 條 PENDING 代入；017-05 不重新分流（除非 X-01 實為螢幕規則）|

---

## 六、停點

**已停。** 交付說明定稿，工作簿 `11` 為現行交付本。

**此後為 Pei 之交付授權（Tier 3）。執行層不請求、不預作。**
