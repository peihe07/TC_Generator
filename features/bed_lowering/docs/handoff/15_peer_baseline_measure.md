# 下放包 15 — Bed Lowering Mode：同儕基準量測（判定方式口徑）

日期：2026-08-27
取號：落檔當下 `list_directory` 實測 `docs/handoff/` 有 01–14，取 15
對象：執行層（Tier 1）
性質：**單一量測，不含 TC 生成、不含寫回。** 為 Pei 之交付前置第 2 項
（判定方式量級之處置）提供比較基準。

---

## 一、要回答的問題

上繳 14 §〇 實測：本 feature 151 列中 **122 條（80.8%）之最終判定需要人**
（B+C 口徑）。執行層建議據此重評分析層先前之裁定（「目視判定不構成新形態」）。

**但「80.8% 高不高」無法自本 feature 內部回答** —— 它需要同類交付本作分母。
HMI 測試規範之最終判定本就以人眼為主；若同儕交付本亦在此量級，
則 80.8% 是 HMI 工作簿之常態形狀，不是本 feature 之異常。
**這是可量的，不該用推論代替。**

## 二、量測

1. 取**已交付且經 Pei 認可**之同儕工作簿至少二本（建議 AMFM v1 tagged，
   加一本 Arif／Wilson 之人寫本以取得非本管線之基準；實際取哪本依
   磁碟現況決定，逐本具名）
2. 以**與上繳 14 §〇 完全相同之 A／B／C 判準**分類其交付列：
   - A：最終 ER 之判定對象為訊號
   - B：最終 ER 判定對象為人，流程含訊號注入／讀取
   - C：全程不出現任何訊號
   判準之套用差異（如他本之訊號寫法不帶 `$`、或用 `Send CAN:` 前綴式）
   **逐本記錄其識別規則**，不得默默調整判準後直接給數字
3. 輸出對照表：本 feature 151 列 vs 各同儕本，A／B／C 三口徑與百分比，
   分母逐欄標明

## 三、判準之已知盲區（沿用，不重新設計）

上繳 14 §四-2 具名之盲區（最終 ER 同時含訊號與目視斷言者會被計入 A）
於同儕本上**命中率可能更高**（他本之 ER 寫法未必與本 feature 同慣例）。
逐本回報該樣態之命中條數；命中即人工複核，不估。

## 四、不做的事

- **不改任何 TC、不改任何工作簿。** 本包只產一份量測報告
- **不下結論說 80.8% 是否可接受** —— 該判定屬 Pei（台架能力為其領域）
- 同儕本若判準套不上（例如非 HMI feature、或訊號寫法差異過大以致
  分類不可靠），**具名回報套不上，不勉強給數字**

## 五、順帶一項（版控）

上繳 14 §四-5 具名：`docs/runtime/profiles/FW036_R1L_BedLowering_Profile.md`
之 `[OVERRIDE IN §10.4]` 自包 07 起生效但該檔未入版控。
**git 操作專屬 Pei**，執行層不得自行 commit；本包僅要求：
列出本 feature 相關之未入版控檔案清單（路徑 + 是否生效中之規則），
交 Pei 一次處理。

## 六、停點

量測報告產出後停，上繳包 15。
