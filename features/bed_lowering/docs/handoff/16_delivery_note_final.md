# 下放包 16 — Bed Lowering Mode：交付說明定稿（B7 全查結果 + 同儕基準 + DR 送出）

日期：2026-08-27
取號：落檔當下 `list_directory` 實測 `docs/handoff/` 有 01–15，取 16
對象：執行層（Tier 1）
性質：**文件更新，不含 TC 生成、不含新寫回**（§二之文法修正除外）。

---

## 〇、Pei 之三項前置狀態

| 前置 | 狀態 |
|---|---|
| R-G14 抽樣覆核 | **已完成**——Pei 指定 B7 全查；分析層執行完畢，結果見 §一 |
| 判定方式量級處置 | **已由上繳 15 之同儕量測解消**，無需裁定 |
| 四筆 DR 送出 | **Pei 已於 2026-08-27 全數送出**，`DATA_REQUESTS.md` 送出日已回填 |

**三項前置全數完成。交付授權可請求（Tier 3，仍屬 Pei）。**

---

## 一、B7 全查結果（分析層，2026-08-27）

16 條生成 TC 與 13 條 coverage gap 逐條讀畢。

**判定：通過。無 A 類項。** 分流之兩條分界（017-05 vs 016/023；013/014 之
-01~03 vs -04~07）逐條複核成立，與 `COVERAGE_GAPS.md` 所載一致；
`coverage_gap` 陣列 13 筆與該表 13 列逐 req_id 相符。
PENDING 四條之 `has_pending` 標記與 DR-4 對應正確。
per-TC reasoning 僅出現於 016-04／023-01 兩條委派條，符合 profile §4 之定式。

### 1.1 B 類（機械修正，隨本包執行）

**文法：`information` 為不可數名詞，`are` 應為 `is`。** 命中 4 處，
皆於 013-01／014-01 之 `test_procedure` 第 2 步與 `expected_result` 第 2 行：

```
The Bed Lowering status information are legible  →  ... information is legible
```

兩條均已寫回（B7 段），故須 patch 已寫回列。

### 1.2 具名觀察（不修，記錄於此）

`013-01~03`／`014-01~03`（依內容類型：資訊／軟鍵／訊息，分日夜）與
`015-03`／`016-03`（HU／EVIC 文字之日夜可讀性）在驗證面上有重疊——
同一塊畫面內容在日夜兩條件下之可讀性被兩組 leaf 各驗一次。

**不合併、不製造差異**，理由同 A-BLM11：切分來自上游 037（013/014 依
內容類型切、015/016 依顯示裝置切），兩組各自追溯其 leaf。
括號下半已載區分 token。**此形態與 A-BLM11 同族，於交付說明 §3 併同揭露。**

---

## 二、`DELIVERY_NOTE.md` 更新

### 2.1 §4 換上同儕基準（**本包主要修改**）

現行 §4 僅載本 feature 之 122/151（80.8%）。加入上繳 15 之對照，
使該節讀得出「這是 HMI 工作簿之常態形狀」：

| 本 | 母體 | A | B | C | B+C |
|---|---|---|---|---|---|
| bed_lowering | 151 | 29（19.2%）| 80 | 42 | **122（80.8%）**|
| audio_mgmt B1 | 70 | 0（0.0%）| 6 | 64 | **70（100.0%）**|
| vehicle_setting VF230 | 457 | 0（0.0%）| 296 | 161 | **457（100.0%）**|

併載三項：
1. 分子定義逐口徑標明（A／B／C 之判準原文）
2. **基準之限制**（上繳 15 §六-2）：可分類同儕僅 2 本且皆本管線產出；
   唯一之非本管線人寫本（CFTS044）因 ER 為自由散文而套不上判準，
   **故本基準未涵蓋「人手寫的 HMI 工作簿長什麼樣」**
3. 上繳 15 §四之結構性發現：兩本同儕之訊號一律落 ER 中段，
   最末行一律為 HMI 觀察（VF230 296 列含訊號、落最末行者 0）

**並記上繳 14 §〇 之更正始末**：原載 38 條／25.2% 為估值非量測、
低報逾三倍，已更正為逐條實測。**不刪原文脈絡**（R-TM13）。

### 2.2 §3 併入 B7 之重疊觀察（§一-2）

A-BLM11 之 006/020 兩對外，加記 013/014 vs 015/016 之重疊形態，
同樣註明「保留未合併、未製造差異」。

### 2.3 §8 前置三項改為已完成

逐項記其完成方式（B7 全查、同儕量測解消、DR 四筆 2026-08-27 送出）。

### 2.4 §5 PENDING 12 條加註 DR 送出日

四筆 DR 皆 2026-08-27 送出；回覆後依各 DR 之結案動作清單處理。

## 三、B 類修正之寫回

§一-1 之四處文法修正：`patch_cells.py` 只改指名儲存格
（013-01／014-01 之 `test_procedure` 與 `expected_result`，共 4 格）。
round-trip、保全計數、全簿 lint（151 列）照跑。
**工作簿版號進位，鏈結記入 `DELIVERY_NOTE.md`。**

## 四、停點

上繳包 16。**此後即為 Pei 之交付授權（Tier 3）**，執行層不請求、不預作。

## 五、未結 DR（皆已送出，待回覆）

| DR | 送出日 | 待回覆後動作 |
|---|---|---|
| DR-1 | 2026-08-27 | 7 條 PENDING 代入 + 各批 `provisional_inputs` 全數複驗 |
| DR-2 | 2026-08-27 | 041-01／02 收緊為具體值（不阻斷）|
| DR-3 | 2026-08-27 | 033-04 代入 + 033-02／034-02 連動複驗 |
| DR-4 | 2026-08-27 | 4 條 PENDING 代入；017-05 不重新分流（除非 X-01 實為螢幕規則）|
