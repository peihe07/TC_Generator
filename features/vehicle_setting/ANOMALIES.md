# ANOMALIES — FW036 Vehicle Setting

登記格式 `[A-VSnn]`。**Tier 1：登記，不裁定。**
本輪（00 包）之登記全部為執行層實測；標「沿用分析層」者為未複驗即照錄之項。

| id | 內容 | 證據 | 狀態 |
|---|---|---|---|
| ~~A-VS01~~ | ~~SYS-RA 指向 SYS2 之 `Heading` / `Information` 列~~ | 逐引用實測 273：`Functional Requirement` **239**／`Heading` **25**／`Information` **9** | **除役（02 包 §1.4）** —— 以 037 之 `Categorization` 對 SYS2 之 `Category` **逐 leaf 交叉列表，零錯配**（Functional↔FR 236、Heading↔Heading 25、Information↔Information 9、CFTS100 那筆 NO_REF 1）。**那 25 個不是錯配，是同一批非需求列在兩份文件裡各自被正確標記。**當初登記為異常之成因：只讀了 SYS2 側，未讀 037 自己的 `Categorization`（第 6 欄） |
| A-VS03 | 四份 037 封面完全相同，無法區分 | 沿用分析層（00B §1）。**本輪未複驗** | 登記 |
| A-VS04 | CFTS044 內未填佔位 `{CFTS044-xxxx}` | 沿用分析層（00B §1.1）。**本輪未複驗** | 登記 |
| A-VS05 ⟷ **A-VS53** | `Heated_Seat_Levels` / `Heated_Seats_Levels` / `Heated_Steats_Levels` 三種拼寫並存 | **實測成立**：三者皆為 037 之 `$var$` token；對 3,000 個相異 LID 作**不分大小寫全字串**比對，`Heated_Steats_Levels` **無逐字對應** | 登記，RD-1 候選 |
| ~~A-VS06′~~ | body heading 270 對相異 `{7位數}` 254，差額 16 未追因 | **實測推翻其前提**：以 `word/styles.xml` 解出 heading 1–7 樣式後，body heading **270** 個，其 `{7位數}` **逐處 270、相異 270、差額 0**。254 係分析層在**轉檔文字**上以較寬形態量得（本輪同形態量得 逐處 444／相異 259，亦非 254） | **除役（05 包 §11）**。原記之差額 16 為轉檔文字之產物：原差額 16 於原始 docx 上不重現；254 為轉檔文字之產物 <br>**關聯未驗項已收（08 輪 W-32(d)）**：其「差額 0」所依之樣式階層量測，於 W-32(d) 之逐 reqid 交叉驗證中再度成立（236 / 236 章節一致），**位置法與樣式階層在 reqid 層級亦重合**。 |
| A-VS07′ | DBC 檔名 `R4`／`R5` 在 `PDT27_E2A` 組指網段、在 `PDT25_E3A` 組指版本週次 | 沿用分析層（00H §1.1）。本輪複驗其 `PDT27_E2A` 側：兩檔 `VersionYear`=25、`VersionWeek`=50 **完全相同**，`BusType` 僅 FDCAN8 有且逐字為 `"CAN FD"` | 登記（FYI 類） |
| A-VS08 | `PDO Graphics Release` PDF 車型與主題皆與本 feature 無交集 | 沿用分析層（00C §3.1）。**本輪未複驗** | 登記 |
| A-VS09 | 26PI 版 Pop Up List 較 Comfort／User Profiles 基線新 | 沿用分析層（00D §5）。**本輪未複驗** | 登記 |
| A-VS10 | CFTS044 指名之 `TLM HMI Document` 於客戶 HMI 目錄無同名檔 | 沿用分析層（00D）。**本輪未複驗**（W-13 未執行） | 登記 |
| A-VS11 | 無 `PDT27_E2A` 組之跨版本比對表 | 沿用分析層（00H §2） | 登記 |
| **A-VS12** | **`SWE1-VC-HeatedSteeringWheel-009` 之 `Source Requirement ID` 逐字為 `SYS-RA-CFTS100`** —— 指向 **CFTS100**（非 CFTS044），且**無 `-N` 序號**，於 SYS2 解析不到任何列 | 實測。其為 271 leaf 中唯一取不到 7 位數 Polarion ID 者 | **新開**。下放包 §5.2 記其原因為「SYS2 該列 `Source Requirement items` 為空」——**原因不符**：SYS2 全 538 列之該欄無一為空，且此 leaf 根本解析不到列 |
| **A-VS13** | **`SYS-RA-CFTS\d+-\d+` 之正則使 `SYS-RA-CFTS100` 隱形** | 嚴格式得 273 引用、寬鬆式（`-N` 可省）得 **274**，多出者即 `SYS-RA-CFTS100`。故「273 全部指向 CFTS044」為真，但其為真之原因是**抽取式看不見反例** | **新開**。canon §5a 第 12 條之標準形態 |
| **A-VS14** | **5 個 leaf 對映到 >1 個 CFTS044 章節** | `SWE1-VC-LeftFrontHeatedSeat-004`／`-011` 各對映 4 章節（`1.3.2.1.3.1`–`.4`）；`SWE1-VC-HeatedSteeringWheelManagement-025`／`-026`／`-027` 各對映 2 章節（`1.3.2.1.3`；`1.3.3.3.6.1`） | **新開**。`specification_reference` 之單值形式對此 5 leaf 不成立 |
| **A-VS15** | **`Proxi & Configuration` 分頁無獨立之 `Atlantis High` 欄組** | 該分頁列 2 之欄組標題逐字為 **`Atlantis & Atlantis High`**（第 16 欄），而 `CAN Mapping` 分頁為 `Atlantis`(16) 與 `Atlantis High`(26) **分列** | **新開**。00G §4 之「10 個 Atlantis High 空欄」全數落在此分頁；**其非空欄，而是該表一欄兼管兩者且已自述** |
| **A-VS16** | **`Format` = `See Proxi Table` 之 LID 實測為 8，非 6** | 全簿（含十張車型專屬分頁）掃 `Atlantis High` 與 `Atlantis` 之 `Format` 欄：`Cooled_Seats`／`DSP_SK_PRSNT`／`EC_Mirror_HK_Prsnt`／`Heated_Seat_Levels`／`Heated_Seats`／`Heated_Steering_Wheel`／**`VC_TIRE_CIRCUMF`**／**`VC_VEH_BRAND`** | **新開**。多出之二者非本 feature 之 token，故「本 feature 用得到 4」不變 |
| **A-VS17** | **兩份 DBC 之 141 個共有 signal 中，128 個之起始位元不同** | 逐屬性比對 `SG_ name : start|len@order sign (factor,offset)`：128 / 141 之 `start` 不同（例 `ACV_FailType` BHCAN 55 vs FDCAN8 165） | **新開**。00H §5-3 自陳「同名不同定義本篇看不到」——**實測為常態而非例外**；TC 引用訊號時若不指明網段／DBC，起始位元即不確定 |
| ~~A-VS18~~ | ~~`recon.py` 之 leaf 數與 W-2 不符~~ | recon 46 vs W-2 56（Common Features）；全體 237 vs 271 | **除役（01 輪 W-16）** —— `scripts/recon.py:602` 為 `is_leaf = cat.lower().startswith("functional")`，依 037 之 `Categorization` 過濾；下放包 §5.1 之判準為「A 欄非空」，無此過濾。**二者非對同一件事給出不同答案，是兩個判準在數兩件不同的事**，且 recon 之判準與 036 之實際投影一致（036 之 237 列 = 237 個 Functional 列）。**recon 未錯。** |
| A-VS19 | **`new_feature.py` 之名稱正規化缺陷** | `scripts/new_feature.py:144` 為 `feature.lower()`，**不轉空白**；以 R-VS3 原記之 `"Vehicle Setting"` 呼叫會產生含空白之目錄名 | **措辭改寫（W-26）**：原記為「誤建目錄待刪」——**該目錄本 feature 未落地，無待刪標的**（實測：`ls -d` 與 `find` 皆無輸出、`git ls-files` 未曾匹配，**P1 已關閉**）。**工具缺陷維持登記，不在本 feature 修**；R-VS3′ 已將 scaffold 參數改為 `vehicle_setting` |
| **A-VS20** | **037 之 `Categorization` 大小寫不一致** —— `HeatedSeat` 有一列為小寫 `information` | 逐字值分布：`Functional Requirement` 237／`Heading` 25／`Information` 8／`information` **1** | **新開（02 包 §1.3）**。⚠ **方向須說明白**：其影響 **`Information` 側之計數**（區分大小寫 8、不分 9），**不影響 Functional 母體之界定**（`startswith('Functional')` 區分與不分大小寫**皆得 237**）——該筆是 `information`，兩種寫法都不會被誤收進 Functional。`recon.py` 之 `.lower()` 在此**不是救了它，是恰好不需要救**。惟任何以 `== 'Information'` 分類之下游會少計一筆。RD-1 FYI 類，我方以不分大小寫吸收，不待上游修正 |
| **A-VS21** | **分析層經 MCP 讀取含中文之 repo 檔時，偶發單字元顯示為替代字元** | 曾兩度回報「檔案疑似毀損」（`ANOMALIES.md`「相異 259」後、`00 §1 第 6 點`「沙」後）；執行層位元層實測：嚴格 UTF-8 解碼**通過**、`U+FFFD` **= 0**，「沙」後三處皆為「箱」 | **新開（05 包 §11，工具類）**。⚠ **其危險不在顯示，在於它偽裝成資料之缺陷** ——若執行層照辦改寫，會把一個好的檔案改壞。**通則：跨層回報「檔案毀損」前須先以位元層確認，不得以讀取結果之外觀為據。** |
| **A-VS22** | **CFTS044 之 `$VentedSeatFL$` 值中出現 `Vented Seat Off / HS_OFF`**（`HS_` 為 Heated 之前綴，應為 `VS_OFF`） | 逐 token 抽值時命中；同一 `= [值]` 內之左式已定其為 VentedSeat | **新開（07 包 §3.2，W-21）**。規格筆誤，RD-1 FYI 類。**不影響取值** —— 左式已定其歸屬 |
| **A-VS23** | **LID 表之 `TGW_DISP_STAT` 值域有拼字錯誤 `diplay closed`** | W-19 逐值比對：DBC 作 `Display_closed`，LID 表 `Format` 欄作 `Diplay_closed`（缺 `s`） | **新開（03 輪 W-19）**。RD-1 FYI 類。⚠ **若以 LID 表之字串為 ER 之逐字值，會寫出一個匯流排上不存在的狀態名** |
| **A-VS24** | **CFTS044 以 `IGN OFF` 描述 `$PowerMode$`，而其匯流排訊號 `CmdIgnSts` 之值域無 OFF** | CFTS044 in-scope 值含 `IGN OFF`／`IGN OFF ACC`；DBC 與 LID 表之 `CmdIgnSts` 值域為 `Initialization`／`IGN_LK`／`ACC`／`RUN`／`START`／`SNA` —— **無任何 OFF** | **新開（03 輪 W-19）**。⚠ **非拼字差異，是狀態不存在** —— 引用 `$PowerMode$ = IGN OFF` 之條文，其 ER 無法以 `CmdIgnSts` 之單一值表達。待判：是否對映至 `IGN_LK`，或另有他訊號承載。**登記待判，不自行對映** |
| **A-VS25** | **「00–02 三輪產物不在版控」之陳述為誤** | 實測 `git ls-files features/vehicle_setting/ | wc -l` = **51**；`git log -- features/vehicle_setting` = **6 個 commit**，最早 `c780d5d`（00 輪 30 檔） | **新開（W-25）**。⚠ **兩層皆有責**：分析層於 07 §7／09 包以推測書寫；**執行層於 02 輪上繳原樣重述，而那六個 commit 正是執行層自己跑的** ——手上有直接證據而未查。真正未入庫者僅 `inputs/INPUTS.sha256`（已於 `2c6c9b3` 解決） |
| ~~A-VS26~~ | ~~LID 表與 DBC 之 message 歸屬矛盾~~ | LID `CAN Mapping` r695 之 Atlantis High 欄組實測：`Signal` 欄逐字為 `STATUS_CCAN3.ESS_ENG_ST\nENGINE_FD_2.ESS_ENG_ST`，`CAN` 欄為 `CAN-B\nFD` | **降級為我方解析產物（14 包 §1）**。該儲存格以換行分隔載**兩個 message 對應兩個網段**；`STATUS_CCAN3` 在 BHCAN、`ENGINE_FD_2` 在 FDCAN8，**LID 與 DBC 完全一致，兩者皆對**（與 R-VS8「兩份並用」同型）。執行層未展開單格多值即報矛盾。**DR-13 撤銷。** ⚠ **此為 C3 缺陷之第三次現身**（W-8 值域切分／W-15b′ 交叉配／本項），故 W-23 之解析式修正不再是可延項 |
| **A-VS27** | **LID 表之 signal 名大小寫與 DBC 不符** | LID `HSW_STATSts`／`HSW_STATFailSts`；DBC **`HSW_StatSts`／`HSW_StatFailSts`**（`STAT` vs `Stat`） | **新開（W-15b′）**。⚠ **推翻 00G §2 之「LID 表與 DBC 一致」**。依 R-VS9(1) 照 LID 逐字寫入 TC，即寫出匯流排上不存在之 signal 名 ——與 R-VS9(5) 所防之 `$PowerMode$`／`PowerModeSts` 同型，**惟此次錯的是第一權威本身** |
| **A-VS28′** | **LID 表載有基線 DBC 所無之訊號 —— 範圍縮為 1 支** | 逐支複驗（`SG_\s+<name>\b`，兩份 DBC）：`FL_HS_Tlm`／`FL_VS_Tlm`／`HSW_Tlm` **皆在 BHCAN**（執行層漏查）；`HeatLeftSeatTgl`／`FL_HS_Cmd_Tlm_Req`／`HSW_Cmd_Tlm` 屬 **Atlantis 欄組（非 in-scope）**，依 R-VS9(1)′ 本就不取用（其 message `TGW_HVAC_CTRL_M` 兩份 DBC 皆無）| **範圍縮為 1 支（14 包 §2）**：**`HDRstRelRq_3rdRow`** 為唯一真缺 —— `RADIO_B3` 存在於 BHCAN 而其 4 支 signal（`ManDispCtrl`／`PowerSideStep_Req`／`RQ_DISP_INTS`／`VR_Blower_Req`）**無任何頭枕相關**；兩份 DBC 全域僅 `Driver_Headrest_Req`／`Passenger_Headrest_Req`，**無第三排**。配對 **DR-14′** |
| **A-VS29** | **C3 缺陷之第四次現身 —— 發生於宣稱修好它的同一輪** | `scripts/lid_parse.py` 已修（錨點 4/4），惟寫出 `data/lid_pairs.tsv` 時對 `Format` 欄做 `" ".join(r[fc].split())`，**再度壓平換行**，使 `parse_format_cell()` 輸入有損 | **新開（W-23(b)）**。**由判準 C3 自動捕獲，計 6 筆** —— 判準化之價值在此得證：若無 C3 判準，該 6 筆會混入「待判 16」而看不出是回歸。歷次面貌：W-8 值域切分／W-15b′ 交叉配／A-VS26 單格多值／本次中介檔寫出端，**每次下移一層**。**本輪未修**（R-VS25 三項上限已滿），排 06 輪頭部 <br>**補記（18 包 §1 之 W-27(a)′）**：錨點 (a) 之判準更正為**條目數 = 11**；鍵值對 10 為附帶量。原措辭「11 個鍵值」把條目與鍵值對混為一談 —— `4 bit signal` 為位元寬宣告，不含 `=`，本就不是鍵值對。 |
| **A-VS30** | **in-scope 之 CFTS044 與 in-scope 之 LID／DBC 對請求訊號之值域與 bit 寬相衝突** | CFTS044 `4858356`／`4858386`（`[EE Architecture:Atlantis High]`）載循環降階四值；基線 DBC 之 `FL_HS_Tlm`／`FR_HS_Tlm`／`FL_VS_Tlm`／`HSW_Tlm` **皆 1 bit**，`VAL_` 為 `Not_Pressed`／`Pressed`；LID 之 Atlantis High 欄組亦對映至該等 1 bit 訊號 | **新開（17 包 §2）**。⚠ **兩造皆 in-scope，R-VS19 之架構差異不適用** ——四階之 `Pressed/Not Pressed` 對照見於 `4857991` 等 CUSW 條文，依 R-VS19 本就不取用。**配對 DR-15（High）**；阻塞 Heated Seat 88 ＋ Vented Seat 72 = **160 leaf** 之分支結構與設計方法 |
| **A-VS31** | **LID 表之 Fail 值寫法在兩列間不一致** | `VentedSeatFL` 之 Format 逐字為 `1= Fail Present`（**`=` 前缺空格、`Fail_Present` 之底線遺漏**）；`HeatedSeatFL` 之對應行為 `1 = Fail_Present`。二者同指一值 | **新開（19 包 D-3）**。⚠ **足以使字面比對誤判為不同值** —— `Fail Present` 與 `Fail_Present` 於未正規化底線之比對下為兩個字串；本 feature 之值比對已對 `_` 做正規化故未受害，**但任何逐字引用該欄之下游會受害**。RD-1 FYI 類 |
| **A-VS32** | **CFTS044 以 `{CFTS043}` 顯式引用 Comfort 之三處，其主題與重疊最多之 43 條無交集** | 三處分別為 Climate 捷徑類別、`I am cold` 例程、`I am hot` 例程（皆設 `$HVACJumpVAL$`）；而 06 輪之 43 條重疊全為座椅加熱／通風／方向盤加熱 | **新開（20 包 §4）**。即**重疊最多者，CFTS044 反而未顯式引用 Comfort** ——徵候為**分層可能是隱含而非文件明示**。FYI 類，**不排作業**：R-VS7 已裁定，且其佐證不只此三處（另有 CFTS044 之 16 處 `Refer to TLM HMI Document`）|
| ~~A-VS33~~ | ~~R-VS7(a) 與 R-VS7(b) 對同一 leaf 皆適用而條文未定優先序~~ | 3 筆：`HeatedSteeringWheelManagement-026`（引 PDO graphics）／`-031`／`-035`（引 TLM HMI Document）；其 Layer 3 同時落在 06 輪之委派對應內 | **開立於 09 輪，關閉於 10 輪（26 包 §1）** —— R-VS7 原文已定其序：**(b) 之首字為「例外」**，例外條款之定義即於其適用範圍內排除主規則，故 **(b) 優先於 (a)**。實質理由：委派之前提為「該行為由 Comfort 擁有」，而該 3 筆之 CFTS044 條文**明文將該行為指給 TLM HMI Document／PDO graphics** —— 規格作者已宣告其歸屬，而該歸屬不是 Comfort。⚠ **執行層漏看該二字，把例外條款讀成平行條文。** |
| **A-VS34** | **06 輪之 Layer 3 清單漏列五個「階數型」Layer 3，致單向委派表漏 84 個應委派之 leaf** | 漏列者：`OneStageHeatedSeat`（12）／`TwoStagesHeatedSeat`（16）／`ThreeStagesHeatedSeat`（20）／`TwoStagesVentedSeatsManagement`（16）／`ThreeStagesVentedSeatsManagement`（20），合計 **84** | **新開（09 輪 W-29）**。成因：06 輪之七條正則映到之 Layer 3 清單只列 6 個，其側別判定分支以 `LeftFront`／`RightFront` 為條件，**階數型之名稱不含側別詞故接不到**。⚠ **canon §5a 條 7「假陰性源自詞彙不全」** —— 而 06 輪之報告當時寫「43/43 全數具名，無具名 0%」，**那個 0% 是對一個由不全清單選出之母體說的**。處置（26 包 §2）：06 輪單向表降為證據並加 SUPERSEDED 標記；`delegation_lookup.tsv` 為委派之唯一來源 |
| **A-VS35** | **W-32(d) 之交叉驗證兩側皆以 7 位數 id 為鍵** | 其驗的是「該 id 之章節歸屬於兩條路徑上一致」，**非「該 id 於 SYS2 與 docx 指向同一條文」** | **新開（26 包 §3）**。後者之獨立驗證須另一份**載有條文正文之匯出**，現無此素材。⚠ **具名為已知界線，非缺口** —— 「未做」與「做不了」在上繳包上須可分辨 |
| **A-VS36** | **03／04 上繳之獨立判斷節未回填 09 輪之發現** | 其所列未驗項與 W-29／W-30 之結果有重疊 | **新開（26 包 §3）**。**依裁定不回填** —— 上繳包為**當輪之快照**，回填會使其失去時序意義；改於 `docs/INDEX.md` 註明「其未驗項已由 09 輪之 W-29／W-30 涵蓋」 |
| **A-VS37** | **`TRUNCATED_ENUM` 之現行偵測涵蓋不足** | 現行判準「`# = Not Used` 結尾」命中 **13**；而**鍵序有缺口者 102**（列舉之數字鍵不連續）；另 `reserved` 38、`TBD/TBC` 1 | **新開（10 輪 W-17(2)）**。截斷之主要形態為**跳號**而非尾綴標記，現行旗標低估約 8 倍。⚠ **102 為上界非截斷數** —— 跳號亦可能是規格本就不使用該值。**未改判準**（屬新作業）|
| **A-VS38** | **同一值同時出現於 include 與 exclude** | `$VC_VEH_LINE$` 之 `WS`／`M240`／`M182/M189`；`$PowerMode$` 之 `IGN_RUN`／`Ignition run` | **新開（10 輪 W-33）**。**非矛盾** —— 出自不同條文，一條 `= X` 一條 `!= X`，適用情境不同。**惟以「該值是否在值域內」為判準之查詢會得到「兩者皆是」**；值域查詢須連同極性與其條文一起看。影響 framework 之分支設計 |
| **A-VS39** | **27 包之 in-scope 判準漏 `ECU` 與 `Radio` 兩屬性** | 補入後：已覆蓋 130 筆之「ECU 含 LTM/ETM/RRM 且 Radio 含 R1L/R1L-R」為 **130/130（100%）**；未覆蓋 39 筆中僅 **6（15%）** 滿足 —— 其餘 33 之 ECU 為 `CCDMF`(20)／`CCDMR`(5)／`ETM` 單獨(3)，或 Radio 不含 R1L 系列 | **新開（11 輪 W-37）**。**33 / 39 之「缺口」為判準過寬所致，非資料缺**。⚠ **R-VS34 之形態，本次犯者為分析層** —— 以自訂 in-scope 形態掃描而未先確認「本專案之適用範圍由哪些屬性界定」。若該 39 筆不追因即送 RD-1，會浪費上游一輪 |
| **A-VS40** | **`$Cooled_Seats$` 之覆蓋查詢一度誤判為真遺漏** | 首查 `$Cooled_Seats$` 命中 0 leaf；改查 `Cooled` 任意形態得 `TwoStagesVentedSeatsManagement-037`／`ThreeStagesVentedSeatsManagement-060`，其描述寫 `PROXI parameter Cooled_Seats` 而非 `$var$` 形態 | **新開（11 輪 W-37）**。**R-VS34 之形態，本輪第二次，犯者為執行層**。若止於首查，`4858294` 會被報為 (a) 真遺漏而觸發升級條件 |

| **A-VS41** | **純交叉參照條文無機器判準** | `4859544`／`4859545` 判為「純交叉參照無可測內容」係人讀（11 輪 W-37）；未建立可機器判定之判準 | **新開（28 包 §4 登記，不排作業）**。風險為「已覆蓋側可能混有純參照條文」——惟已覆蓋側之條文皆有 leaf 引用，**即使為純參照亦已在母體內，不影響覆蓋結論** |
| **A-VS42** | **in-scope 判準漏 `EE Architecture = Atlantis Mid`** | 251 個已覆蓋 reqid 中 **121 筆**落在 in-scope 之外，逐筆查其失敗條件**全數為 `EE Architecture`**，其值**全數為 `Atlantis Mid`**（`Artifact Type` 全數合格）。全 2,030 條之該欄值域為 `Atlantis High` 825／`PowerNet` 498／`CUSW` 388／`All` 354／**`Atlantis Mid` 247** | **新開（12 輪 W-39(2)）。⚠ 升級條件命中**。那 121 筆是 037 之 237 leaf **實際引用**之條文 —— **被覆蓋卻不在母體內**，判準與事實直接矛盾。連鎖：27 包之 1,128／998／169／39／76.9% 皆須重述；**11 輪之「(a) = 0，母體完整」不再安全**（敏感度觀測：加入 Mid 後未覆蓋為 8，較已讀之 6 多 2 筆從未檢視）。**判準之修訂屬分析層** |
| **A-VS43** | **階數之掃描形態只試 `stage`，未試 `Level`** | 08 包記「明示階數 1 / 27」，其判準為 `one／two／three stage(s)`。依 28 包放寬判準複核得 **5 / 17**：`SWE1-HVAC-054/055/067` 為 `Multi-Level Heated/Vented seats`、`-062` 為 `Multi-Level Heated steering wheel`、`-063` 為 `Single-Level heated steering wheel` | **新開（12 輪 W-40(2)）。⚠ 升級條件命中**。**R-VS34 形態第三次**（前為 `$HSW_StatFailSts$`、`$Cooled_Seats$`）。**後果最重**：08 包據「1 / 27」判「資料本身沒有可收斂之維度」，而資料其實有，只是寫成 `Multi-Level`。**W-34(1) 之 0/174 收斂失敗，其成因診斷可能整個是錯的**。且此維度正是 **DR-15 之維度**，其問法應否改寫屬分析層 |
| **A-VS44** | **Comfort 側母體數字互不一致 —— 口徑已裁定（29 包 §3）** | 「委派所引之相異 Comfort leaf」：08 包 §2.2 記 **27**、08 包另處記 **22**、本輪自 `delegation_lookup.tsv` 之 174 列 `delegate=yes` 實測 **17**。「Comfort leaf 全母體」：06 輪記 **498**、本輪自 037 `Analysis Report` 分頁實測相異 `SWE1-HVAC-` id **129** | **新開（12 輪 W-40(2)）；29 包 §3 已裁口徑，13 輪 D-2 標明**。爾後一律以：相異 `SWE1-HVAC-` id = **129**、`delegate=yes` 所引之相異 Comfort leaf = **17**。06 輪之 **498 為資料列數**（含同一 id 之多列）、08 包之 **27／22 為當時不同口徑之計數**，**二者皆降為證據**。凡引 Comfort 母體須標明口徑（「資料列」或「相異 id」）。分子 5 vs 1 與分母無關，A-VS43 之結論不動搖 |
| **A-VS45** | **分析層於 17／18 包引 DR-15 之條文 id 錯誤** | 17 包 §2.1 與 18 包 §2 稱 DR-15 之 CFTS044 證據為 `4858356`／`4858386`；實測該二條為**接收側**（`When the HU receives a $HeatedSeatFR$ = [HS_OFF] signal…`）。循環降階表之實際出處為四條：**`4858325`（`$FL_HS_RQ$`）／`4858355`（`$FR_HS_RQ$`）／`4858385`（`$FL_VS_RQ_TGW$`）／`4858416`（`$FR_VS_RQ_TGW$`）**，皆 `[EE Architecture:Atlantis High]` | **新開（29 包 §2.2 指定登記，13 輪 D-2）**。成因：以正則抓 `$FR_HS_RQ$ = [Medium]` 之命中位置，再取「前一個 7 位數」為其 reqid —— **取到前一個區塊的號（差 1），未以區塊邊界驗證歸屬**。DR-15 **尚未送出**，13 輪 D-3 已更正引用，無外部代價 |
| **A-VS46** | **Comfort 側無「單階座椅」條文，12 個 `OneStageHeatedSeat` leaf 之委派標的明文排除單階** | 174 列 `delegate=yes` 逐列對照本側 Layer 3 階數與 Comfort 側階數標記：**One → Multi 12 列**、Two → Multi 32、Three → Multi 40。該 12 列所引 `SWE1-HVAC-054`／`-067`，二者開頭逐字為 `For Multi-Level Heated/Vented seats`。Comfort 全母體 129 中唯一 `Single-Level` 者為 `-063`，其主詞為 `heated steering wheel`；以 `single[\s-]?level` 且含 `seats?` 交叉查詢**命中 0** | **新開（13 輪 W-43(2)）。⚠ 升級條件命中**。**非對映筆誤，為委派之實質缺口** —— 委派標的所述行為明文排除單階座椅。連帶：08 包「資料本身沒有可收斂之維度」須撤回（維度存在，Single↔One／Multi↔{Two,Three}，惟 Two 與 Three 不可分）。**配對之 RD 屬分析層，本層不代擬** |
| **A-VS47** | **`LeftFrontHeatedSeat-004`／`-011` 為四側共通需求卻掛在左側名下** | CFTS044 §1.3.2.1.3.1 與 §1.3.2.1.3.2 皆 29 條，逐位對照引用狀態，不一致者恰 2：`4858334`（`$Heated_Seat_Levels$`）與 `4858347`（Stop-Start），二者未被右側 leaf 引用，實由 `LeftFrontHeatedSeat-004`／`-011` 引用，且該二 leaf 之 `section` 逐字為 `1.3.2.1.3.1;1.3.2.1.3.2;1.3.2.1.3.3;1.3.2.1.3.4` | **新開（13 輪 W-41′）**。**非 037 遺漏，升級條件未命中** —— Layer 3 之 17 vs 15 差 2 即源於此。**對 framework 之後果**：該二 leaf 依實質應屬 `Common Features`，而 `framework.md` 以 SWE ID 中段 token 機械切分會放錯層。**未自行搬動**（framework 不鎖定，屬 Pei） |
| **A-VS48** | **W-42 之四屬性對照在條文層級去重，未查 leaf 內一致性** | 13 輪 W-42 之 `Radio`／`ECU`／`Market`／`Model Year` 對照以條文為單位去重後計數，**未查同一 leaf 之多條 reqid 其屬性是否一致**。`Model Year` 為唯一兩組有實質差異者（純 Mid 各年 7 vs 無 Mid 各年 33、另有 2019:30） | **新開（30 包 §4 D-3 指定登記，14 輪）**。該差異**未追因**。若某 leaf 之數條 reqid 分屬不同 `Model Year`，現行表看不出來 |
| **A-VS49** | **`4858393` 將通風座椅之值寫為加熱前綴 `HS_HI`** | CFTS044 §1.3.2.1.3.4 之 `4858393` 與 `4858394` 去方括號後同文，方括號值僅差 `[Vented Seat High / **HS_HI**]` vs `[Vented Seat High / **VS_HI**]`。LF 側對應條文（`4858363`／`4858367`／`4858368`）一律為 `VS_HI`。RF Vented 之 30 條 vs 其餘三節 29 條，差 1 即源於此（兩節正規化後相異條文皆 28，來源對稱） | **新開（14 輪 W-46(3)）**。**該錯值已進入 `data/spec_variables.tsv`**：`$VentedSeatFR$` 之 `cfts044_include` 含 `Vented Seat High / HS_HI`、`Vented Seat High/HS_HI`、`Vented Seat Off / HS_OFF`；`$VentedSeatFL$` 含 `Vented Seat Off / HS_OFF`。反向（`Heated` 配 `VS_`）掃描命中 0，污染單向，範圍 4 值／2 token。**不自行清除**（屬 TC 內容，且須先確認為 typo 而非別名）。**配對之 RD 屬分析層** |
| **A-VS50** | **R-VS37 未涵蓋「跨異層章節」與「無 reqid」二情形** | R-VS37 第二分支逐字為「跨越多個**同層**章節」。`HeatedSteeringWheelManagement-025`／`-026`／`-027` 之 `section` 為 `1.3.2.1.3;1.3.3.3.6.1` —— 四段 vs 五段，**不同層**；`HeatedSteeringWheel-009` **無 reqid**，R-VS37 未規定其歸屬 | **新開（14 輪 W-46(2)）**。W-46 之 6 筆不一致中，**僅 2 筆（`LeftFrontHeatedSeat-004`／`-011`）在 R-VS37 文義內**。本輪暫依「多章節即 Common Features」處理該 3 筆、無 reqid 者單列，**該處理無條文依據**，已於 `framework.md` 以 ⚠ 標明。**不代擬條文** —— 請補 R-VS37 之第三、四分支 |
| **A-VS51** | **`4858413` 之值退化為 `[ Pressed]`** | CFTS044 §1.3.2.1.3.4 之 `4858413` 逐字為 `$CCDMF_FR_VS_RQ$ = [ Pressed]`（含前導空白）；LF 同位條文 `4858382` 為 `$CCDMF_FL_VS_RQ$ = [Vented Seat Pressed / VS_PSD]`。**標籤 `Vented Seat` 與代碼 `VS_PSD` 皆遺失** | **新開（15 輪 W-48）。⚠ 升級條件命中** —— 其不對稱型與 Heated 兩節（引用歸屬，A-VS47）**不同**：Vented 兩節之引用狀態不一致為 **0**，不對稱在**值**。影響：`$CCDMF_FR_VS_RQ$` 之第一階值域少一值、多一個字面為 ` Pressed` 之值。**併入 DR-18** |
| **A-VS52** | **值域之大小寫雙寫致同一值重複計數** | CFTS044 對同值有多種大小寫並存：`[Vented Seat Low / VS_LO]`(5) ／ `[Vented Seat Low / VS_Lo]`(4)；`[Vented Seat Medium / VS_MED]`(2) ／ `[Vented seat Medium / VS_MED]`(2) ／ `[Vented seat medium / VS_MED]`(4)；`[Not Pressed]` ／ `[Not pressed]`。致 `$VentedSeatFR$` 之 `cfts044_include` 同一值算成五個 | **新開（15 輪 W-48）**。後果為**值域基數虛高，直接影響 TC 之列舉分支數**。**不自行合併**（屬 TC 內容）。**其餘 29 個 token 之同型重複未掃** |
| **A-VS53** ⟷ **A-VS05**（32 包 §2：**併入 A-VS05**，兩者皆保留，R-TM13） | **`$Heated_Steats_Levels$` token 本身為 typo** | `spec_variables.tsv` 之 30 個 token 中並存 `$Heated_Seats_Levels$` 與 `$Heated_Steats_Levels$`（`Steats`） | **新開（15 輪 W-47）；16 輪依 32 包 §2 併入 A-VS05**（同一 typo，00B §5-5 已登記）。其正規化鍵依 **R-VS39** 與 `$Heated_Seats_Levels$` 相同。**不自行合併或改名**（原值為 CFTS044 逐字，改之即失去可追溯性）。**併入 DR-18** |
| **A-VS54**（**已關閉**，16 輪依 R-VS38） | **31 包 §5 之 typo／別名判據不足以分辨** | 該判據逐字為「對稱側一律用另一前綴者判 typo；對稱側亦用同前綴者判別名」。`4860015`(LF)／`4860021`(RF) **兩側一致地寫 `HS_OFF`**，依該判據應判別名；但同章節內 `4860011`／`4860013`／`4860017`／`4860019` 皆寫 `VS_OFF`，全文頻次 `VS_OFF` 15 次 vs `HS_OFF` 2 次 | **新開（15 輪 W-47(3)）**。**該判據無法分辨「兩側一致地抄錯」與「真雙軌命名」**。本輪改以「章節內並存 ＋ 全文頻次」為基礎判 typo。**已關閉（16 輪 D-3）**：32 包 §1 之 **R-VS38** 將該三項（對稱側／章節內並存／全文頻次）定為聯合判據，並明訂「別名須為系統性雙軌命名，不得由少數例外構成」。原判據之缺陷已具名 —— **左右兩節係複製後修改，複製錯誤必然對稱** |
| **A-VS55**（**已關閉**，17 輪依 R-VS19″） | **R-VS19′ 之主文與 (a) 段互斥** | 主文逐字為「`EE Architecture` 降為輔助資訊，**不作為排除判準**」；(a) 段逐字為「縮限為 `CUSW`／`PowerNet` **專屬者**（即 `Radio`／`ECU` 判準不通過者）」。**括號內之等式在資料上不成立**：全 2,030 條中「他架構專屬且通過 `Radio`／`ECU` 判準」者 **270 條**。兩讀之差：in-scope **425 vs 695**、21 節內未覆蓋 **8 vs 51**、**(a) 類 0 vs 43 未歸因** | **新開（16 輪 D-5）。⚠ 升級條件命中（需要判斷而無條文）**。新增之 43 條**全數為 `PowerNet` 專屬**，內容為實質可測需求（`If $EngineRPM$ < …greyed-out`、`Valid values for the $HeatedSeatFL$…`、`grey-out the Third Row Headrest Dump Softkey`），非適用性宣告、非交叉參照。**可供釋義之實測**：251 個已覆蓋條文中，`CUSW`／`PowerNet` 專屬者 **0** —— 037 從未引用任何純他架構條文。**本層不裁定採哪一讀**（R-VS19′ 為 Pei 所裁）。**已關閉（17 輪 D-2）**：34 包 §1 之 **R-VS19″** 自陳為分析層起草缺陷，原意為讀法一，並將排除段明訂為 `NOT（EE Architecture ⊆ {CUSW, PowerNet}）`。定案數字：in-scope 425／21 節內 259／未覆蓋 8／96.9%／落外 0／**(a) = 0**。43 條不歸因，其為 out-of-scope |
| **A-VS56** | **值域大小寫重複影響 12 / 30 個 token** | 15 輪 §6-3 只量過 `$VentedSeat*$`。全 30 token 重量：`$VentedSeatFR$` 23→18、`$VentedSeatFL$` 23→19、**`$ESS_ENG_ST$` 18→15**、**`$PowerMode$` 19→16**、`$FR_VS_RQ_TGW$`／`$HSW_RQ_TGW$`／`$HSW_Stat$` 各減 2、`$FL_HS_RQ$`／`$FL_VS_RQ_TGW$`／`$FR_HS_RQ$`／`$HdRstRelRq$`／`$PrplsnSysAtv$` 各減 1。全體 345 → 319 | **新開（16 輪 W-50(3)）。⚠ 升級條件命中**（逐字為「影響 `$VentedSeat*$` 以外之 token」）。`$ESS_ENG_ST$` 與 `$PowerMode$` **與座椅無關** —— 大小寫雜訊不限於座椅節族，**是 CFTS044 全文性質**。已依 **R-VS39** 增 `normalized_key` 欄處理，**原值一個未改**。**併入 DR-18 三** |
| **A-VS57**（**已關閉**，18 輪依 R-VS41） | **訊號書寫形式：R-VS9(3) 三件組 vs TC 規則書 §8.7.5 v3 互斥** | 34 包 §3 逐字令 `procedure 訊號斷言須 <signal> in <message> on <網段>（R-VS9(3)）`，同時令本輪起適用 `ASPICE_SWE6_AI_Instruction.md`；該文件 §8.7.5 逐字稱「**v1 之三件組 `<Signal> in <MESSAGE> on <segment>` … 已撤銷**」，改採 `$<MESSAGE>.<Signal>$ = <raw> (<label>)`。二者不可同時滿足 | **新開（17 輪 W-53）。⚠ 影響本批每一條 procedure 與 ER**。本層採 R-VS9(3)，理由具名：(1) R-VS9 為 **Pei 裁定**，§8.7.5 之撤銷署名為分析層下放包且為 **time_management 之包**；(2) 34 包 §3 為針對本輪之最具體指令且明引 R-VS9(3)；(3) §8.7.5 §0 之衝突條款為「feature profile 之 override 勝過通則」，而 **`docs/runtime/profiles/` 下 9 份無 VehicleSetting**，R-VS9 實質承擔該角色。**本層不裁定**。**已關閉（18 輪 D-4）**：35 包 §1 判 **canon 勝，R-VS9(3) 撤回**（R-VS41(1)），L-VS1 一併撤回。決定性理由為「書寫慣例專由 SWC 0708 交付本推導，分析層自訂之慣例不予採用」—— **三件組是分析層自訂的**。網段改由 Pre-Condition 末位承載（R-VS41(2)）。batch01 之 10 條 procedure 與 ER 全數改寫為 `batch01_v2.json` |
| **A-VS58** | **`EngRun_Stat` 之規格值於 LID 與 DBC 皆無對應** | CFTS044 `4858551`／`4858553`／`4858555` 以 `$EngRun_Stat$ = [IDLE_STBL//UNLIMITED//LIMITED//RUN]`（或其否定）為觸發；LID `Format` 與基線 DBC `EngineSts`（`STATUS_CCAN3`, id 994）之值域皆為 `Engine_Off`／`Engine_Cranking`／`Engine_On`／`SNA`。**四個規格值在兩者中皆不存在** | **新開（17 輪 W-53）。⚠ 升級條件命中**（某 leaf 之 TC 無法在不違反 §8.4.1 下寫出）。**具名 leaf**：`SWE1-VC-Stop-StartSystem-004`／`-005`／`-006`。依 **R-VS9(2)**「兩者不一致時停下回報，不自行調和」，**未把 `RUN` 讀為 `Engine_On`**。三條 TC 之相應步驟依 §8.4.3 填 `PENDING: DR-19` |
| **A-VS59** | **`4858560` 交叉參照未具名之 HMI 需求** | 條文逐字 `When $DriverSide$ = [Right hand drive] the HMI shall be modified as defined by HMI requirements.` —— 未具名任何文件、章節或需求 ID | **新開（17 輪 W-53）。⚠ 升級條件命中**（canon §9 自檢有項目不通過而無法修正）。`SWE1-VC-SwitchLHD/RHDConfiguration-010` 之末步驟無可寫之驗證目標：寫具體修改項違 §8.4.1／§8.4.2，寫「HMI is modified」違 §6。依 §8.4.3 填 `PENDING: DR-20`；**§9 檢查 5 記為不通過**，DR-20 答覆前無法修正 |
| **A-VS60**（**已關閉**，18 輪 W-54） | **`test_item` 上半之逐字方括號與 §11 衝突，無 profile 可援引例外** | §11 逐字「Square brackets `[...]` … MUST NOT appear in TC output fields」，其例外為 **profile-scoped**（「when the feature profile says so」）。而 R-VS6／R-S4 要求 `test_item` 上半為來源逐字，CFTS044 原文含 `[ENS_DSBL]`／`[SNA]`／`[Right Drive]`／`[Pressed]` 等 token | **新開（17 輪 W-53）**。本 feature **無 profile 檔**。本層採「逐字優先」：四個交付欄已全數無方括號（機械驗證 0 違規），方括號僅存於 `test_item` 上半之逐字段。**已關閉（18 輪 W-54）**：`docs/runtime/profiles/FW036_R1L_VehicleSetting_Profile.md` 之 `[OVERRIDE §11]` 落檔，先例 Home A-H10。lint 改以「與所引來源列逐字相符」驗證保留 token |
| **A-VS61**（**已關閉**，18 輪 W-54） | **Layer 2 `Common Features` 觸 §4.1.3 之「too coarse」反樣式** | §4.2／§4.1.3 禁止 catch-all 吸收互不相關能力，判準為「以此 Test Set 過濾工作簿是否得到有意義之群集」。`Common Features` 為 **037 之檔界**，其 46 個 leaf 含 Stop-Start System、Switch LHD/RHD、Third Row Headrest Dump、ScreenOFF、PHEV Features、Features Enable Criteria | **新開（17 輪 W-53）**。R-VS4 為 Pei 所裁且 framework 已建，**本層不改**。惟 §4.2 之「same Test Set implies a shared setup pattern and UI entry path」在本批 10 條上明顯不成立（Stop-Start 走 Climate 畫面＋CAN-B，Third Row Headrest Dump 走軟鍵＋實體致動）。**已關閉（18 輪 W-54）**：profile 之 `[OVERRIDE §4.1.3／§4.2]` 落檔，理由為「037 檔界即上游作者選定之能力叢集邊界」 |
| **A-VS62**（**已關閉後撤回**，經 R-VS52 取代，Pei 2026-08-22） | **ER 之送出型措辭 `is registered without a bus error` 未能自交付本枚舉** | 該措辭出現於 `batch01_v2.json` 之 **8 處**，為 CAN 送出型步驟之 ER。分析層自陳未能自既有交付本枚舉其 house style：`features/comfort/inputs/` 之 036 為近空白樣板（59 列、TC 內容全空），無可枚舉之樣本；SWC 0708 不在本 feature 之 `inputs/` | **新開（38 包 §2，19 輪 D-11 登記）**。依「**格式裁決須先枚舉既有交付本之實際書寫樣式**」之原則，**分析層不自訂此處之措辭**。兩條路徑擇一：(a) Pei 提供 SWC 0708 或任一已交付本之 CAN 送出步驟樣本 → 據以對齊；(b) 逕以 pilot review 認可現行措辭 → 記為本 feature 之既定寫法。**不阻擋放行**（38 包 §2）—— 若日後對齊，為單欄字串替換。**pilot 第一批已由 Pei 判通過（2026-08-22），故現行措辭依 (b) 暫為既定寫法**。**已關閉（Pei 2026-08-22，53 包 §2）**：採路徑 (a) —— `The signal $X$ = <raw> (<label>) is registered without a bus error` **認可為本 feature 之既定寫法**，適用於 `Send the signal …` 型步驟之 ER；讀取型仍用 `reads <raw> (<label>)`。**feature-scoped，不得援引至他 feature**；日後取得 SWC 0708 或已交付本樣本而措辭不同者，以該樣本為準逐條替換。懸置 25→31 輪共七輪。**（a) 之認可旋即撤回（34 輪 D-3）**：54 包 §1 之 **R-VS52** 依 SWC 0708 交付本裁 —— 送出型 ER 改為 `<MESSAGE>.<Signal> = <raw> (<label>) is sent`，訊號名**不加 `$` 包夾**。二選一之答案最終由**交付本樣本**（路徑 (b)）給出，非由認可現行措辭給出 |
| **A-VS63** | **`$PowerMode$` 之 `IGN_START` 與 `IGN_OFF_ACC` 於基線 DBC 無對應值** | LID 對映 `PowerMode` → `STATUS_BH_BCM2.CmdIgnSts`，其 Format 與兩份 DBC 之 `VAL_` 皆為 `0 Initialization／1 IGN_LK／3 ACC／4 RUN／5 START／7 SNA`。CFTS044 全文實測：`[Ignition lock / IGN_LK]` 11 次（**逐字相符**）；`[4h:Ignition run]` **5 次 —— CFTS044 自載原始碼值 `4h`**，故 `IGN_RUN` 可解為 `4 (RUN)`；`[Ignition start / IGN_START]` 3 次、`[Ign. off & acc. … / IGN_OFF_ACC]` 4 次，**二者無對應且無原始碼值** | **新開（19 輪 W-57）→ DR-21**。依 **R-VS9(2)** 未把 `IGN_START` 讀為 `START`（**該調和看似顯然，正是 A-VS58 之同型**）。**`IGN_RUN` 之可解不靠推理，靠 CFTS044 自己寫的 `4h`**。阻塞 `ThirdRowHeadrestDump-029`／`-031`。37 包送件文第 8 項已就 `4858978`（Second Row）提出 `IGN_OFF_ACC` 同一問題，**惟該項未送出且無 DR 編號** |
| **A-VS64** | **`VC_HdRstPrsnt` 於 LID／DBC／值域資料三處皆 0 命中** | CFTS044 `4858989` 以 `VC_HdRstPrsnt = [Present]` 為顯示軟鍵之觸發。實測：`lid_pairs.tsv`（2,710 列）**0**、兩份基線 DBC **0**、`spec_variables.tsv`（30 token）**0** | **新開（19 輪 W-57）→ DR-22**。**R-VS34 形態**：該 token 於 CFTS044 寫作**裸名，無 `$...$`**，故早輪以 `$var$` 形態建 `spec_variables.tsv` 時未收入；本輪依 **R-VS36** 三形態試法方發現。阻塞 `ThirdRowHeadrestDump-028` |
| **A-VS65** | **`4859032` 交叉參照未具名之 HMI Logic and Flow** | 條文逐字 `the HU shall follow the HMI Logic & Flow to update the state of the Rear View Camera soft button` —— 未具名任何文件、章節或需求 ID | **新開（19 輪 W-57）→ DR-23**。**與 A-VS59／`4858560` 同型**，惟所指文件不同（該條為「HMI requirements」，本條為「HMI Logic & Flow」）—— **同一 feature 內第二次**。阻塞 `ThirdRowHeadrestDump-039` |
| **A-VS66** | **分析層於下放包開立之 DR 未逐筆確認其入登記簿** | 37 包送件文第 7 項（`$VC_VEH_LINE$` 車型碼）與第 8 項（`$PowerMode$` 之 `IGN_OFF_ACC`）實為 **DR-8**（00G §5）與 **DR-12**（29 包 §1.2），二者於下放包開立**但未寫入 `DATA_REQUESTS.md`**。19 輪執行層記「第 7、8 項在登記簿裡沒有 DR 編號」，即此 | **新開（39 包 §4，20 輪 D-3／D-4）**。**R-VS35 所立之核對機制只覆蓋執行層側，未覆蓋分析層側**。39 包 §4 補：分析層須於次輪下放包列「上輪開立 N／登記簿現有 M」兩數。20 輪已補登記 DR-8、DR-12；**DR-12 依 R-VS42 併入 DR-21** |
| **A-VS67** | **B1 判準過寬 —— 65 條中僅 8 條為真阻塞** | W-58 初測以「`as defined by`／`refer to`／`follow the`／`per the`／`as specified by`／`according to` ＋ 未帶文件名或章節號」為 B1 判準，得 **65 個相異條文**。逐條讀後：**57 條為尾綴修飾**（參照之前已有具體結果動詞，如 `shall be shown as greyed-out, per the HMI`），**8 條為整個結果被外推**（參照之前無結果動詞） | **新開（20 輪 W-58）**。措辭分布：`as specified by` 23／`follow the` 13／`according to` 10／`per the` 9／`refer to` 7／`as defined by` 2／`as defined in` 1。**若止於初測，writable = no 會報 117（49.4%）而非 88（37.1%）**，且會把 57 個實際可寫之 leaf 誤列為阻塞。**與 A-VS39（分析層之 in-scope 判準過寬）同型，本次犯者為執行層** |
| **A-VS68** | **`$HSW_Stat$` 於 `4858516` 之 `$` 不對稱** | CFTS044 `4858516` 逐字為 `$HSW_StatS = [Heated steering wheel off / OFF] $HSW_StatS = [Heated steering wheel on / ON]` —— **缺右側 `$`**，正確應為 `$HSW_Stat$`（同條文前段即寫 `Valid values for the $HSW_Stat$ are shown below`） | **新開（20 輪 W-58）**。致 token 掃描抓出不存在之 `HSW_StatS`，並使 `HeatedSteeringWheel-004` 被判為 B3。**非資料缺口，為來源書寫 typo** —— 應併入 DR-18（座椅值域之書寫問題）之同型，惟 DR-18 未送出 |
| **A-VS69**（**已關閉**，21 輪依 R-VS43） | **`$DriverSide$ = [Right Drive]` 之對映為執行層之判斷，且已入已放行之 TC** | LID Format 為 `0 = Left Side`／`1 = Right Side`。CFTS044 全文之 `$DriverSide$` 值：`[Right Side]` 10／`[Left Side]` 7／**`[Right Drive]` 4**／`[1h: Right Side]` 4／`[Right hand drive]` 2／`[0h: Left Side, 1h: Right Side]` 2／`[1h: Right hand]` 1。**`Right Drive` 本身無 `Nh:` 原始碼值錨點** | **新開（20 輪 W-58）**。17 輪之 `SwitchLHD/RHD-009`／`-011`（**已於 2026-08-22 由 pilot review 放行**）以 `PROXI Driver_Side = 1 (Right Side)` 承載 `[Right Drive]`。本層之依據為「該 token 之值域為二值且已由 `1h: Right Side` 錨定，`Right Drive` 非 `Left Side` 故為 `Right Side`」——**此為二值域之演繹，非發明**，惟仍屬本層之判斷。**與 `IGN_START → START` 之差別在於後者之值域有六值，目標非唯一**。請分析層裁定該演繹是否成立；若不成立，該二條須改為 PENDING。**已關閉（21 輪 D-3）**：40 包 §1 裁**演繹成立**，`-009`／`-011` **維持已放行不改**。決定性實測為 **`Left Drive` 與 `Left hand drive` 全文各 0 命中** —— `Right Drive`／`Right hand drive`／`Right hand` 三者若為平行命名體系，必有其左側對偶；且 `4858559` 稱 `LF` 為 `Passenger`，右駕語意自證。判準立為 **R-VS43** 之三條件 |
| **A-VS70** | **W-58 之單條文比對設計致 B2 為上界** | W-58 之 `value_matched()` 只在**該值自身**帶 `Nh:` 時 credit 原始碼值，**不做跨條文之錨點聚合**。而 CFTS044 之 `Nh:` 錨點**跨條文散佈**（`[4h:Ignition run]` 出現於 5 個他條文）。致 `ThirdRowHeadrestDump-032`／`-038` 被標 B2 而實為假陽性 | **新開（40 包 §2，21 輪 D-3 登記）**。**成因在分析層**：W-58 之規格由 39 包 §2 所定，**未要求跨條文聚合**。後果為 19 輪之「88 / 149」兩數皆為上界／下界而非實數，**不可引為決策依據**。21 輪 W-59 依 18 輪 §6-1 自陳之驗法修正 |
| **A-VS71** | **`== "值"` 之語法形態完全未掃 —— W-58 只掃 `= [值]`** | 21 輪 W-61(1) 追四個未命中 token 時發現：`STATUS_CSWM.HSW_StatFailSts == "Fail_Not_Present"`、`$Heated_Steats_Levels$ == "Two Levels"` 等以**雙引號 ＋ `==`／`passes to`** 承載值，**W-58 之 `TOKEN_CMP` 只匹配 `= [值]`，該形態一條未掃**。全量重掃得：**20 個 token、95 個 leaf**；其值對照 DBC 後 **5 個 token 解出**（`*_STATFailSts` 之 `Fail_Present`／`Fail_Not_Present`），**14 個 token 未解**，涉 **79 個原判 `writable = yes` 之 leaf** | **新開（21 輪 W-61(1)）。⚠ 升級條件命中**（「四 token 有任一承載值域而未被抽出」）。未解之 14 中，`HeatedSeatFL`／`FR`・`VentedSeatFL`／`FR` 為 `Heated_seat_mid` vs DBC `Heated_seat_medium` 之實質不符；`Cooled_Seats`／`Heated_Seats`／`Heated_Seat_Levels`／`Heated_Steering_Wheel` 為 PROXI 參數，**其值域在 LID 而本輪未逐一回查 LID**。**故 165／72 兩數仍為上界／下界，未逕自改寫**；受影響列已於 `writability.tsv` 標 `quoted_form_risk = yes` |
| **A-VS72**（**已關閉**，22 輪依 R-VS17） | **B1 之結果動詞表漏 `has to show`，B1 = 8 可能為高估** | W-61(2) 反向抽樣兩向皆 0 反例（尾綴修飾 10/10 有結果動詞、整個外推 8/8 無）。**惟該抽樣以同一張動詞表判定，對表本身之漏詞不具鑑別力**。逐條讀 8 條「整個外推」者：`4859386`／`4859387`／`4859448`／`4859449`／`4859498` **五條之措辭為 `TLM has to show an informative popup relative to the failure`** —— `show` 為具體且可觀察之結果，僅其內容（popup 文字）未具名。表中有 `shall show` 而**無 `has to show`** | **新開（21 輪 W-61(2)）**。若計入該措辭，**B1 由 8 降為 3**（僅 `4858560`／`4859509`／`4859032`）。**本層不逕自改判** —— 屬 TC 內容層之判斷。**已關閉（22 輪 D-3）**：41 包 §2 指出該問題**已由 R-VS17 裁定**（DR-5-B 未到位期間，受影響 leaf 仍產 TC，ER 寫至訊號層為止，畫面層標 BLOCKED 於 Remarks）。該 5 條之觸發條件在訊號層完全可寫（`Fail_Present` 由 LID 與 DBC 雙來源給出）。**B1 由 8 降為 3**；動詞表**不需補 `has to show`** —— 其判定由 R-VS17 決定，非由動詞表決定。**此為 canon §5a 條 17 之形態：立新規則前先查既有政策** |
| **A-VS73** | **21 輪產物入庫於他 feature 之 commit** | 21 輪之 12 個 `features/vehicle_setting/` 檔案實際落於 `1f95205 feat(time_management): package 09 …`，**訊息與內容不符**。成因：執行層 `git add` 完成後、`commit` 之前，另一 session 以**無 pathspec 之 add** 將暫存區內之本 feature 檔案一併帶入其 commit；執行層隨後之 pathspec commit 得 `nothing to commit` | **新開（2026-08-22）**。**內容完整未遺失**。Pei 裁定採「不改寫歷史，補一筆說明」，詳見 `docs/reports/COMMIT_ATTRIBUTION.md`。**同型事件先前發生過一次**（Power，`4d623ef`）。**防護之界線須記明**：執行層之 pathspec commit **只約束本 session**，暫存區為 repo 層級共用，**擋不住他 session 之無 pathspec `add`** —— 真正之防護須各 session 於 `add` 時亦帶 pathspec，**非執行層可單方保證** |

| **A-VS74** | **B1 之「具名」正則漏帶空白之文件名** | 五條 informative popup 條文之全文為 `… popup relative to the failure. **Refer to TLM HMI Document.**` —— **其有具名**。W-58 之 `NAMED` 正則為 `(CFTS\s*\d{3}\|\{\d{7}\}\|PU\d+\|\d+\.\d+(\.\d+)+\|[A-Z][A-Za-z]*_[A-Za-z_]*(?:Document\|Spec\|List))`，末式**要求含底線**，故 `TLM HMI Document`（空白分隔）不命中 | **新開（22 輪 D-3）**。該五條因此被誤判為「未具名交叉參照」而入 B1。41 包 §2 由 **R-VS17**（DR-5-B 之既有政策）另行解決之，結論相同（可寫），**惟其成因不同** —— 分析層之依據是政策，本項是正則漏字。**B1 剩餘之 3 條已逐條複查**：`4858560`／`4859509` 為 `as defined by HMI requirements`（泛稱，非文件名）、`4859032` 為 `follow the HMI Logic & Flow`（文件類型名，本 feature 之 `inputs/` 無此檔），**三者確為未具名或具名而不存在** |
| **A-VS75** | **R-VS43(3) 之極性對照表召回率 7 / 16** | 22 輪 W-62(3) 以**普查**（非抽樣）掃 30 個 token 之全部 351 個值，含極性詞者 **156 對**，出現之相異極性詞 **16 個**。表內 5 對／10 詞，**9 個不在表內**：`pressed`(43 次)／`high`(19)／`low`(19)／`start`(7)／`disabled`(6)／`lock`(2)／`true`(1)／`stop`(1)／`invalid`(1) | **新開（22 輪 W-62(3)）**。`pressed` 為**出現最多者**卻不在表內。後果：R-VS43(3) 於無對偶詞可測時**保守判不成立**，故 **derivable 被低估**（現為 1 組）。**本輪未補表** —— 補表會改變 derivable 計數，其新增者須先過 **R-VS44** 之未結 DR 交叉（`pressed` 直接落在 DR-15 之 1 bit vs 階數爭點上），**故補表不是單純的字典擴充** |
| **A-VS76** | **`writable = yes` 不保證可寫 —— B4 = 0 為掃描盲區所致** | `4858310`／`4858340`（`The HU shall ignore **invalid** $HeatedSeatFL/FR$ signals`）**無方括號值**，故 B1/B2/B3 三類皆不命中，判為 `writable = yes`。惟其可寫性取決於「何謂 invalid」：基線 DBC 之 `FL_HS_STATSts` 為 2 bit 且 **0–3 四個編碼全部已定義**，**無現成之無效編碼可注入** | **新開（22 輪 W-64）**。本輪之所以能寫，是因 **`4858307` 逐字給出二階配置之有效值**（`HS_OFF`／`HS_LO`／`HS_HI`），故 `2 (Heated_seat_medium)` 於二階下為無效 —— **來源自載，非造值**。**但該解法是人讀他條文得來，掃描看不見**。故 **20 輪之 B4 = 0 應讀為「本掃描無法偵測 B4」，而非「B4 不存在」** |
| **A-VS77**（**已關閉**，29 輪依 R-VS49） | **PROXI Table 已找到，惟為他車型 —— 待裁是否採用** | 23 輪 W-65 唯讀搜尋：R1LR 目錄下**無** PROXI 檔（264 個 xlsx/xlsm/docx 內容掃描 ＋ 全庫 5,304 檔檔名掃描）。惟他車型之 `PROXI_DT27_R2_20250110.xlsx` 與 `PROXI_HDCC27_R3_20250424.xlsx` 之 `Format` 分頁**四個參數全數有值域且兩表逐字一致**：`Cooled_Seats`／`Heated_Seats` = `0 Absent／1 Front Seats／2 Front And Rear Seats／3 Not Used`；`Heated_Steering_Wheel` = `0 Absent／1 Present`；`Heated_Seat_Levels` = `0 1 Level／1 2 Levels／2 3 Levels` | **新開（23 輪 W-65）。⚠ 正向升級**。**三項佐證指向同一定義**：(1) 兩份獨立 PROXI 表逐字一致；(2) `Heated_Seat_Levels` **僅引 `VF664`**，正是 R1LR LID 欄 20 之 `664`；(3) R1LR LID 自身之 `Heated_Steering_Levels` 有實 Format `0 = 1 Level／1 = 2 Levels／2 = 3 Levels`，**與 PROXI 表之 `Heated_Seat_Levels` 形態相同**。**本層不採用**（跨車型代入屬裁定事項）。若裁為可採，**79 個 leaf 之阻塞解除**。**已關閉（29 輪 D-4）**：49 包 §1 之 **R-VS49** 裁採用，依據為**版本層級之對應**（該表之 `Heated_Seat_Levels` 引 `LTM (VF664_V2); ETM (VF664_V3)`，與 R1LR LID 之 `VFs=664`、`ECU=LTM/ETM` 三項一致）；44 包之拒絕理由（Toro226 未提及）不成立，因其所引為 `V42` 非 `V2/V3`。**惟「79 leaf」之預估為錯** —— 實測**僅 8 條 leaf 引用該四參數**，W2 轉出 **7**，見 A-VS95 |
| **A-VS78** | **R-VS44 之閘有漏：DR-15 之範圍以值樣式宣告，涵蓋不到 `Pressed`** | 42 包 §3.1 令補極性表後以 `pressed` 相關之 derivable 測閘，**預期全數 `DR-CONFLICT`**。實測補表後新增 derivable **4 組**（`FL_HS_RQ`／`FR_HS_RQ` 之 `Heated Seat Pressed / HS_PSD`、`FL_VS_RQ_TGW`／`FR_VS_RQ_TGW` 之 `Vented Seat Pressed / VS_PSD`），**4 組全數過閘，被攔 0 組** | **新開（23 輪 W-66(3)）。⚠ 升級條件命中**。成因：`dr_conflict.py::OPEN_DR["DR-15"]` 之值樣式為 `^\s*(high\|low\|medium\|off)\s*$`，`Heated Seat Pressed / HS_PSD` 不匹配。**但 DR-15 問的是該訊號之編碼（1 bit vs 承載階數），其標的涵蓋該 token 之全部值，不只 High/Low/Medium** —— 把 `Pressed` 對映為 `1 (Pressed)` **即預設了 1 bit 之答案**。**本層未採用該 4 組**，亦**未逕自放寬 DR-15 之範圍宣告**（其為 DR 範圍之詮釋，屬分析層）。修法具名：`DR-15` 之值樣式改為 `.`（涵蓋該五個 token 之全部值） |
| **A-VS79** | **第二個時間符號 `<Tdisplay>`，DR-24 未涵蓋** | 23 輪 W-66(2) 全量掃 `<T…>` 形態，得 **43 個 leaf**、兩個符號：**`Tsend` 15 次／`Tdisplay` 28 次**。42 包 §3.2 之 DR-24 僅以 `<Tsend>` 為訴求 | **新開（23 輪 W-66(2)）**。**`Tdisplay` 之引用數為 `Tsend` 之近兩倍**，且同受 canon §8.7.1「門檻須為具體值」之規制。**DR-24 之提問文須併入 `<Tdisplay>`**，否則答覆只解 15 個 leaf 而留 28 個。**提問文之修訂屬分析層** |
| **A-VS80** | **LID 版本落後兩版** | 我方 `inputs/` 用 `Logical Identifiers and CAN Mapping v1_76.xlsx`（客戶目錄之 `26PI1.5/Reference Documents/`）；客戶目錄之 `26PI2.5/Reference Documents/` 已有 **v1_78** | **新開（23 輪 W-65）**。**就本輪之四個 PROXI 參數而言 v1_78 無差異**（其 `Format` 欄同為 `See Proxi Table`、`VFs` 欄同為 `664`，逐欄實測），故不影響 A-VS77 之結論。**惟其他 token 是否有差異未掃** —— 素材更新屬 Pei，本層不補入 |
| **A-VS81** | **`TLM HMI Document` 於客戶目錄中確不存在 —— 僅有 15 處引用它** | 24 輪 W-68(2)：檔名掃描 `*TLM*HMI*`／`*telematic*` **命中 0**；R1LR 目錄下 **1,362 個 pdf/doc/rtf/xls 之內容掃描**，`TLM HMI` **命中 15**，**全數為 CFTS 文件中之引用**（`Refer to TLM HMI Document`／`see TLM HMI Specification`），**無一為該文件本身** | **新開（24 輪 W-68(2)）**。**W-68(2) 之正向升級條件未命中** —— 該文件確不在客戶需求目錄。**DR-20／DR-23 之型 B 訴求成立且無法以搜尋解除**，須向上游索取。影響 17 個 leaf（R-VS17／DR-5-B）。**已窮盡搜尋範圍見上繳 22 §4** |
| **A-VS82** | **VF664 找到，但其不定義 PROXI 參數之值域** | 24 輪 W-68(1)：全庫檔名含 `664` 者 **47 筆**，其中 `Vehicle_Controls_Management_by_VP_-_LTM_VF664_*`（docx／pdf，四車型五版本）為 VF664 本文。逐檔抽其文字層（8,936／13,663／7,797 字元）：**四個參數之名稱出現於「本 VF 所用之 PROXI 參數」清單中，但無任何值域定義** | **新開（24 輪 W-68(1)）**。**W-68(1) 之正向升級條件（「找到 VF664 → 79 leaf 可解」）之前提不成立** —— VF664 只列參數名。值域之定義仍在 **PROXI 表**，而 R1LR 之 PROXI 表不存在，僅有他車型之兩份轉錄（A-VS77）。**故 A-VS77 之裁定仍待，且不因 VF664 之尋獲而改變** |
| **A-VS83** | **DR-15 之標的涵蓋已生成之三條 TC** | R-VS44′ 令 DR-15 改為 **token 級**後，`FL_HS_RQ`／`FR_HS_RQ`／`FL_VS_RQ_TGW`／`FR_VS_RQ_TGW`／`HSW_RQ_TGW` 之**任何值**皆在其範圍內。實測 CFTS044 **自載** `1h: pressed`／`0h: not pressed` 五個 token 之錨點，**而他處條文（`4858325` 等）又述循環階數** —— **該內部矛盾正是 DR-15 之提問標的** | **新開（24 輪 W-70(2)）**。24 輪選入之 `LeftFrontVentedSeat-012`／`HeatedSteeringWheel-012` **已由閘攔下並移出 batch05**。**惟 batch03 之 `LeftFrontHeatedSeat-014`、batch04_v2 之 `RightFrontHeatedSeat-031` 已生成且斷言 `FL_HS_Tlm`／`FR_HS_Tlm` 之 0/1** —— 其在 R-VS44′ 生效前產出。**是否須一併移出，屬裁定事項，本層不逕自撤回已生成之 TC** |
| **A-VS84** | **第三種引號形態：彎引號 `“…”`（U+201C/201D）** | 25 輪 W-73 讀 `4859504` 時發現其條件為 `STATUS_CCAN3.ESS_ENG_ST != “ENS_DSBL”` —— **彎引號**。20 輪之 `= [值]` 與 21 輪之 `== "值"`（直引號）**兩式皆抓不到**。全量重掃得 **2 個 token ／ 3 個 leaf**：`EngineSts`（`Engine_Off`／`Engine_On`，**皆解**）、**`ESS_ENG_ST`（`ENS_DSBL`，未解，2 leaf）** | **新開（25 輪 W-73）**。`ENS_DSBL` 於 DBC 為 `ENS disabled`，**逐字無對應** —— 與 **A-VS58 同型**。`StopStartSystemBehavior-054`／`-055` 因此移出 batch07。**規模有界（2 token／3 leaf），故 21 輪 §6-2 之「第三種形態存在與否未知」就此關閉為「存在，且已量」**。惟**第四種形態（如 `'值'` 單引號、全形引號）仍未掃** |
| **A-VS85** | **037 有兩個 leaf 承載同一需求，其 TC 為嚴格等價** | 25 輪 W-73 之 sibling 比對查出**兩對**：`HeatedSteeringWheel-021`（`4858544`，`$HSW_Stat$ = [1h: On]`）≡ `-015`（`4858538`，`= [On]`）；`-022`（`4858545`，`[0h: Off]`）≡ `-016`（`4858539`，`[Off]`）。四條之觸發、結果、輸入、驗證對象**全同**，差別僅在來源條文之記法（帶不帶 `Nh:` 原始碼值） | **新開（25 輪 W-73）**。依 §10.6 標 `duplicate_of` 且 `distinguishing_axis.axis = none`。**成因在 CFTS044**：`4858538`／`4858544` 為同一需求之兩次書寫。**037 依 R-VS15 為母體，本層不得合併 leaf**（§8.2.1 之反向禁令：TC 作者不得合併 RD sub-id）。**是否向上游反映屬分析層** |
| **A-VS86** | **pilot 已放行之 batch01_v3 中有一條 DR-15 曝險** | 25 輪 W-71 全量掃 42 條已生成 TC，得曝險 **3** 條 —— 已知 2 條外，**新增 `SwitchLHD/RHDConfiguration-009`（batch01_v3，pilot 於 2026-08-22 PASS）**。其 ER 逐字為 `$TELEMATIC_VEHICLE_SETUP3.FL_HS_Tlm$ is 1 (Pressed), the same as recorded in step 2` | **新開（25 輪 W-71）**。**曝險 3 ≤ 10，升級條件未命中**。惟其中一條**已通過 pilot review** —— 若 DR-15 覆為「承載階數」，該條之 ER 須改寫，**而其已列為已放行**。44 包 §3 之裁定為「不撤回、標記、覆後複檢」，本層依此標 `dr15_exposed = yes`，**未撤回** |
| **A-VS87** | **穩定核心已耗盡 —— 產能歸零** | 26 輪 W-75／W-76：穩定核心 **72** 已用 **71**，餘量 **1**（`HeatedSteeringWheel-009`，**無 reqid**，DR-11 之 `UNRESOLVED-SOURCE`）。batch08 交付 7 條後，**batch09 無 leaf 可選**。累計交付 **65 條 TC**，母體 237 中 **165 條不可生成** | **新開（26 輪 W-76）。⚠ 升級條件命中**（「W-75／W-76 之交付合計 < 12」，實測 7）。不可生成之成因分布：`writable=B2 + quoted_form_risk` **69**／`writable=B2` **62**／`delegate=blocked` **17**／`B2+pending+risk` **10**／其餘 7。**即：除 17 條 R-VS17 之 blocked 外，其餘 148 條全數卡在值域類 DR（DR-21／DR-22′）上**。**在 DR 覆之前，本 feature 之產能為 0** |
| **A-VS88** | **docx 表格內之值未掃 —— R-VS46 所要求之未測者具名** | 26 輪 W-74 依 R-VS46 逐式測 12 種形態，得承載值者僅 **①方括號**（173 命中／27 token／94 leaf）、**②直引號**（223／20／95）、**③彎引號**（5／2／3）；`passes to` 48 命中但**無新 (token,值) 對**（其已為②所涵蓋）；④⑤⑥⑦⑧⑨⑩⑫ **全數 0**。**未測者**：`blocks_with_sec()` 之抽取為 `word/document.xml` 之**段落文字流**，**docx 表格之儲存格被攤平為段落**，故「值在相鄰儲存格／同列並置」之表格形態**無法以本抽取法辨識** | **新開（26 輪 W-74）**。依 **R-VS46**，本輪之「12 式已測」**不得讀為窮盡** —— 其涵蓋清單如上，**未測者為 docx 表格式**。其驗法為改以 `word/document.xml` 之 `<w:tbl>` 結構抽取並保留列／欄關係。**規模未知** |
| **A-VS89** | **R-VS47 只恢復 3 條 —— 「判準過嚴壓抑大量可寫 leaf」之假設不成立** | 27 輪 W-77 依 R-VS47 全量分級 237 leaf：**W0 91／W1 3／W2 143**。舊判 `writable = yes` 91 → 新 W0 87 ＋ W1 2 ＋ W2 2；舊 `no` 146 → 新 W0 4 ＋ W1 1 ＋ W2 141。**淨恢復 5 − 2 = 3 條** | **新開（27 輪 W-77）。⚠ 升級條件命中**（「W1 < 20」，實測 **3**）。46 包 §1 之理由為「W-58 之判準比 R-VS17 與 22 包嚴，且該嚴格從未經裁定」—— **該診斷正確，但其量級預估錯誤**。成因：多數受阻 leaf 之未解值**位於後件**（驗證目標即該值）或**前件無其他已解條件**（扣除 PENDING 後不足 2 步）。**且 5 條新升級者中 4 條為 `OneStageHeatedSeat-047`~`-050`，其 `delegate = pending`（DR-17）** —— 仍不可生成。**batch10 之池為 0** |
| **A-VS90**（**已關閉**，28 輪依 47 包 §1） | **`ENS_DSBL` 之處置跨輪不一致，且不一致落在 pilot 已放行之作品中** | `batch01_v3` 之 `Stop-StartSystem-005`（**pilot PASS 2026-08-22**）步驟 3 逐字為 `Send the signal $STATUS_CCAN3.ESS_ENG_ST$ = 7 (ENS disabled)`，其承載規格之 `[ENS_DSBL]`。而 **25 輪 W-73 將 `StopStartSystemBehavior-054`／`-055` 因同一 token 同一值移出 batch07**。`$ESS_ENG_ST$` 之值域為 **11 值**，**R-VS43(1)（二值域或目標唯一）不成立** → **不可演繹** | **新開（27 輪 W-77）**。W-77 之驗收錨點二（`-005` 須判 W1）**失敗，判為 W2** —— **分級器無誤，是跨輪處置不一致**。二者必有一錯：若 `ENS_DSBL → 7` 成立，則 25 輪之移出過嚴；若不成立，則 `-005` 含未經授權之演繹且已放行。**本層不逕自改任一方**，請裁。**已關閉（28 輪 D-3）**：47 包 §1 裁 **`ENS_DSBL → 7 (ENS disabled)` 成立** —— R-VS43 三條件逐項：(1) 依 **R-VS48(a) 縮寫關係**，11 值中唯一命中 `ens disabled`；(2) `[7h: ENS disabled]` 全文 5 處；(3) `ens enabled`／`ENS_ENBL` 全文各 0。推論：`-005` 之寫法**獲追認**；**25 輪移出之 `-054`／`-055` 過嚴，已移回並入 batch10** |
| **A-VS91** | **W-78 之驗收錨點失敗 —— `4859495` 之序列不在 docx 表格中** | 27 輪 W-78 以 `<w:tbl>` 保留列欄重抽：全文 **25 個表格**（分屬 8 個實質條文 ＋ 17 個修訂履歷表）。**`4859495` 之表格數為 0**；文件順序上其後緊接 `4859496` 之區塊，**中間無表格節點**。其 `described below` 所指之序列**可能為圖片**（docx 之 `<w:drawing>`），本抽取法與 OCR 皆未取 | **新開（27 輪 W-78）。⚠ 升級條件命中**。表格抽取本身有效（`4858264` 之 20×3 `Feature／PROXI parameters／Radio functionality`、`4859174` 之 12×3 dual/tri state 表等），得 **41 token／45 新 (token,值) 對**，**惟解開既有阻塞 0** —— 表格給的是**使用條件**（`Heated_Seats = Front Seats or Front and Rear`）而非**匯流排編碼**，故不解 DR-22′。**未測者**：`<w:drawing>` 內嵌圖片之內容 |
| **A-VS92** | **兩條已生成 TC 經重分級為 W2** | `SwitchLHD/RHDConfiguration-012`（batch08）與 `ThirdRowHeadrestDump-038`（batch04_v2）舊判 `writable = yes`，新分級為 **W2**（未解值位於後件或前件無已解條件），`dr_dependent = DR-21` | **新開（27 輪 W-77）**。二者皆**已生成且已交付**。**本層不逕自撤回**（比照 A-VS83／A-VS86 之處置），標記待 DR-21 覆後複檢。**與 A-VS86 合計，已生成之 65 條中至少 5 條須於 DR 覆後回溯**（3 條 `dr15_exposed` ＋ 本 2 條） |
| **A-VS93**（**已關閉**，29 輪依 R-VS48′） | **R-VS48(b) 產生一個假陽性：`IGN_OFF → IGN_LK`** | 28 輪 W-80 全量套用 R-VS48 後得 20 個可對映對，其中 `$PowerMode$` 之 `[Ignition off (5 position switch) / IGN_OFF]` 依 (b) 共享實詞唯一命中 `ign_lk`。**其共享者為命名前綴 `ign`，非實詞** —— `$PowerMode$` 之值域（`Initialization`／`IGN_LK`／`ACC`／`RUN`／`START`／`SNA`）**無任何「off」語意之值**，故 `IGN_OFF` 實無對應 | **新開（28 輪 W-80）**。**本層未採用該對**，並於 §2 具名其理由。**R-VS48(b) 之「實詞」未排除命名前綴** —— `ign` 於 `IGN_OFF` 與 `IGN_LK` 中皆為 token 命名之慣用前綴，其共享不具鑑別力。**判準之精化屬分析層**；本層不逕自擴充停用詞表。**已關閉（29 輪 D-4）**：48 包 §1 之 **R-VS48′** 將 (b) 路改為**不得自動採用**、須逐對人讀。分析層自陳三種自動化修法皆無法分辨其與 `Right Drive → Right Side`。29 輪 W-83(1) 之複查：15 對中 (b) 路 **14 對，人讀 14/14 通過**（12 對為分隔符差異、2 對語意核心逐字相符），`IGN_OFF → IGN_LK` **不在採用表內** |
| **A-VS94** | **`4859495` 之 `described below` 序列不在 docx 中 —— 既非表格亦非圖片** | 28 輪 W-81：`word/media/` **全檔僅 1 張 png（3 KB）**，且**不屬任何條文**（以 `r:embed` 逐段定位，含圖之條文數 **0**）。27 輪 W-78 已證其不在 `<w:tbl>` 中 | **新開（28 輪 W-81）**。**「內容存在但抽取法看不見」之假設就此排除** —— 該序列**根本不在檔案裡**。此為**來源缺陷**：CFTS044 之 `4859495` 引用一個同文件內不存在之序列。**28 輪之 batch08 `HeatedSteeringWheelManagement-028` 只斷言「離開 off」與「回到 off」兩點之處置，經此證實為正確** |
| **A-VS95** | **DR-22′ 之「影響 79 leaf」為錯 —— 實為 8 條** | 29 輪 W-83(4)：併入 R-VS49 之四參數值域後，**W2 轉出僅 7 條**（W0 101 → 108）。追因：**全母體中引用該四參數者僅 8 條 leaf**。「79」之來源為 21 輪之 `quoted_form_risk` 標記，其把**引號形態下全部未解 token 之受影響 leaf**一併計入，而該 79 條中僅 8 條涉及 PROXI 參數 | **新開（29 輪 W-83(4)）。⚠ 升級條件命中**（「W2 轉出數 < 40」）。**真正之大宗為 `*_Cmd_Tlm` 四者**：`FL_HS_Cmd_Tlm` 17 leaf／`FR_HS_Cmd_Tlm` 16／`FL_VS_Cmd_Tlm` 14／`FR_VS_Cmd_Tlm` 14 —— **合計 61 leaf**，其於 LID 記為 `NOT_IN_DBC`。次為 `Hybrid_Type` 11／`HeatedSeatFL`・`FR` 各 8／`EngRun_Stat` 7。**DR-21 之影響應以此重估**，DR-22′ 之撤回不改變產能 |
| **A-VS96**（**已關閉**，31 輪 W-87） | **四條適用性前言無自身可測內容** | 29 輪 W-85：`TwoStagesHeatedSeat-055`（`4859376`）／`ThreeStagesHeatedSeat-078`（`4859400`）／`TwoStagesVentedSeatsManagement-037`（`4859438`）／`ThreeStagesVentedSeatsManagement-060`（`4859464`），其條文逐字皆為 `Following requirements are valid only if PROXI parameter X == "…"` —— **僅界定後續條文之適用範圍，自身無觸發亦無可觀察之結果** | **新開（29 輪 W-85）**。與 13 輪判為 (c) 之 `4859399`／`4859463`（`applicable for R1 Low only`）**同型**。四者經 R-VS49 而由 W2 轉為 W0（其 PROXI 值已可解），**惟「值可解」不等於「有可測內容」** —— **分級器看得見前者，看不見後者**。**與 A-VS76 同源**（B4 之偵測盲區）。四者移出 batch11。**已關閉（31 輪 W-87）**：四者改判 **W2**／`blocker_class = B4-preamble`，惟本 anomaly **漏列第五條 `OneStageHeatedSeat-039`（`4859360`）**，見 **A-VS99** |
| **A-VS97**（**已裁**，32 輪依 R-VS38） | **LID 內 `FL_VS_Cmd_Tlm` 之值域自相矛盾** | 31 輪 W-86(2)：`CAN Mapping` 列 769（LID `FL_VS_RQ_TGW`）之 `Atlantis` 欄組 Format 為 `Atlantis / 2 bit signal / 0 = Vented_Seat_Off / 1 = Vented_Seat_Low / 2 = Vented_Seat_Medium / 3 = V…`；**列 770（LID `FL_VS_RQ_TGW2`）之同一 Signal Name `TELEMATIC_VEHICLE_SETUP2.FL_VS_Cmd_Tlm` 之 Format 卻為 `0 = Heated_seat_off … 3 = Heated_seat_high`**。列 790 之 `FR_VS_Cmd_Tlm`（Vented）亦寫 `Heated_seat_*` | **新開（31 輪 W-86(2)）**。**同一訊號兩個值域**，且通風座椅掛加熱座椅之標籤 —— 與 **A-VS49 同型**（`HS_HI` 出現於 VentedSeat 章節）。**本層不擇一**。本輪因 W-86(3) 判不採用（見 A-VS98）而未觸及此矛盾，**已裁（32 輪 D-4）**：52 包 §3 依 **R-VS38** 三項聯合判準（(a) 對稱側列 769 為 `Vented_Seat_*`；(b) 同表內兩形態並存，自相矛盾；(c) 與 A-VS49 同型，該案已判 typo）裁 —— **值域取 `Vented_Seat_*`**，列 770／790 之 `Heated_seat_*` **判為 LID 之轉錄錯誤**。`spec_variables.tsv` 增 `suspect_prefix` 標記，**不改原值**；**併入 DR-18**（確認型），不另開 DR |
| **A-VS98**（**已關閉**，32 輪依 R-VS51） | **R-VS19″ 與 R-VS20 之交集：65 個 leaf 恆在範圍卻恆不可寫** | 31 輪 W-86(3)：引用 `*_Cmd_Tlm` 四者之條文 **65 條，`EE Architecture` 100% 為 `Atlantis Mid`**（實測，無其他值）。其值域僅存在於 LID **`Atlantis` 欄組（欄 16–20）**，而 R-VS20 第二階指定之欄組為 **`Atlantis High`（欄 26–30）**，且 R-VS20 逐字禁「他架構條文（CUSW／PowerNet／**Atlantis Mid**）之值域一律不取用」。同列之 `Atlantis High` 欄組所給者為**另一訊號** `TELEMATIC_VEHICLE_SETUP3.FL_VS_Tlm`（`0 = Not_Pressed / 1 = Pressed`）**即 DR-15 之標的** | **新開（31 輪 W-86(3)）。⚠ 結構性，待裁**。**R-VS19″ 使 `Atlantis Mid` 在母體內、R-VS20 使其值域不可取用** —— 兩條之交集此前未曾被指出，後果是 **60 個相異 leaf（B2 群）恆為在範圍卻恆不可寫**。**本層不併入 `spec_variables.tsv`**：併入即以 Atlantis Mid 之值域描述 Atlantis High，且會同時解掉 DR-15（觸犯 **R-VS44**）。分級維持 → 見 A-VS99。**50／51 包所稱之「正向升級（61 leaf 可解）」不成立** —— 其值域確為實值域，但架構欄組不符。**已關閉（32 輪 D-4）**：52 包 §1 之 **R-VS51** 裁 —— **不是條文矛盾，是 R-VS20 之排除清單已於 33 包經 R-VS19″(a) 縮限為 `CUSW`／`PowerNet` 專屬者，而 `RULINGS.md` 之 R-VS20 條目未加註**（分析層自陳該疏漏在其側）。`Atlantis Mid` 條文之值域**得取用**，其欄組依 R-VS51(2) 分流至 LID 之 `Atlantis` 欄組。**惟 R-VS44 優先** —— DR-15 之五個 token 仍由 `guard()` 攔下，不因本條而解 |
| **A-VS99** | **W-87 之修正幅度：W0 虛高 5 條，非 4 條** | 31 輪 W-87：四式全掃得**條文層 9 條**（`valid-only-if` 6／`section-applicable` 3／`applicable-only` 3，有重疊）。對映至 leaf 後：**唯一來源即前言者 5 個 leaf**（A-VS96 之四條 ＋ **`OneStageHeatedSeat-039`（`4859360`）**）；**併引者 1 個**（`HeatedSteeringWheelManagement-025` = `4858296` ＋ `4859492`）。三條前言條文（`4859343`／`4859399`／`4859463`）**無任何 leaf 引用**。分級 **108／2／127 → 103／2／132** | **新開（31 輪 W-87）**。**A-VS96 依此關閉**（四條轉 W2／`B4-preamble`），惟其漏列第五條 `OneStageHeatedSeat-039`。**`-025` 不改判** —— 其併引實體條文 `4858296`，pilot #2 §1 #7 已確認其 spec_ref 併列前言為正確（§10.7 明文含 `relies on as setup`）。**升級條件「同型總數 > 20」未命中**（實測 9 條文／5 leaf） |
| **A-VS100** | **DR-21 之優先序數字兩度與實測不符（R-VS50 第二次命中）** | 31 輪 D-3 回查：DR-21 現載「82 個 leaf、104 次命中」，實測為 **137 leaf、215 次命中、27 個相異 token**。且 A-VS95／50 包／51 包所指之「單一最大解鎖 `*_Cmd_Tlm` 61 leaf」實為 **60 相異 leaf**，而**真正最大群為 `HeatedSeatFL`／`FR`／`VentedSeatFL`／`FR` 四者之 84 相異 leaf**（DR-21 現載將此四者各記為 6 次） | **新開（31 輪 D-3）**。**R-VS50 立於 50 包，本輪首次適用即再命中一次** —— 且這次錯的是 R-VS50 自己所根據之替代數字（「61」）。成因同型：以**累加次數**代替**相異 leaf 數**，且未隨 W-59／W-77／W-80 三次改判更新。**已於 `DATA_REQUESTS.md` 之 DR-21 改寫，並列出全 27 token 之逐項分解與三個群組之相異 leaf 數**；歷史記載依 R-TM13 不追改 |
| **A-VS101**（**已關閉**，34 輪 W-94） | **`writability.tsv` 不可自 repo 重現 —— 無任何腳本產出之** | 32 輪 W-90：`grep -l writability.tsv scripts/*.py` **命中 0**。該表係 20／27／29／31 四輪以 inline heredoc 逐次修改產物而成，歷輪之 R-VS43 演繹、R-VS48 對映表、R-VS49 PROXI、W-87 前言改判**只寫進產物，未寫進驅動**。以現有模組（`clause_tokens` ＋ `bus_domain` ＋ `grade`）重建之驅動得 **62/2/173**（三形態抽取）或 **82/3/152**（補收 token 後），與 103/2/132 **逐 leaf 不一致 52 筆** | **新開（32 輪 W-90）。⚠ 方法層**。後果：**W-90(4) 之「與 103／2／132 之對照」無法以同一驅動作出** —— 本輪改以**受控比較**（同一驅動、僅切換 R-VS51）給出增量，並以**增量法**套回產物（僅升級、不降級，沿用歷輪作法）。**該表之絕對數自 20 輪起即不可稽核**；若日後需重算全量，須先補寫驅動腳本並逐輪回放各裁定。**已排入 W-94（34 輪）**：55 包 §1 之 **R-VS53** 令交付論述之產物須可自 repo 重現，過渡期間 `writability.tsv` 之**絕對值標「不可稽核」**。**已關閉（34 輪 W-94）**：`scripts/writability_driver.py` 建立，回放 R-VS36／39／43／47／48′／49／51／44 ＋ W-59 ＋ W-87，產物由其重生，**驅動輸出與產物逐 leaf 差異 0**。重生前之版本保留為 `docs/reports/writability_pre_w94.tsv` |
| **A-VS102** | **`spec_variables.tsv` 漏收 5 個 DBC 已有完整值域之 token** | 32 輪 W-90：237 leaf 所引 251 條文內之 41 個 token 中，**5 個未收於 `spec_variables.tsv` 而 DBC 之 `VAL_` 有完整值域** —— `FR_HS_STATFailSts`(23 次)／`FL_HS_STATFailSts`(21)／`FL_VS_STATFailSts`(19)／`FR_VS_STATFailSts`(16) 皆為 `0 "Fail_Not_Present" 1 "Fail_Present"`；`EngineSts`(3) 為 `0 "Engine_Off" 1 "Engine_Cranking" 2 "Engine_On" 3 "SNA"` | **新開（32 輪 W-90）**。**R-VS34 形態之再現**（以假定之形態掃描而判其不存在）—— 該 5 者一直被記為未解值，實則 **DBC 早有其值域**。**非演繹、非造值**：DBC 為值域之權威（R-VS9(1)′ ＋ R-VS39），補收屬填補掃描遺漏。5 者**皆不在任何未結 DR 之範圍內**（已逐一過 `conflict()`）。全庫另有 38 個同型 token，經查**屬他 feature 條文**（Massage／Lumbar／Bolster／Mirror／SideStep），已排除 |
| **A-VS103** | **`FR_VS_Cmd_Tlm` 之 `Vented_seat_*` 值須跨列引入，R-VS38 之 typo 裁定未涵蓋** | 32 輪 W-90：條文寫 `FR_VS_Cmd_Tlm = "Vented_seat_high／low／off／mid"`（14 次），而 LID **列 790 之 Format 只有 `Heated_seat_*`**。52 包 §3 已裁該 `Heated_seat_*` 為 typo，**但 `FR_VS_Cmd_Tlm` 在 LID 中無任何 `Vented_seat_*` 之列** —— 其正確值域須自 **`FL_VS_Cmd_Tlm` 之列 769** 跨列引入 | **新開（32 輪 W-90）**。**本層未跨列引入**。R-VS38 之裁定為「該值判為轉錄錯誤」，**未及於「以對稱側之列補其值域」** —— 後者是**跨列推論**，非 typo 更正。條文之側寫（FR 對 FL）雖強，仍屬裁定事項。**併入 DR-18 之確認範圍**（32 輪 D-3 已將 A-VS97 併入）。影響：`FR_VS_Cmd_Tlm` 之 14 次命中仍記未解 |
| **A-VS104** | **`_mid` 與 LID 之 `_medium` 不合，R-VS48(a) 之子序列判準不涵蓋** | 32 輪 W-90：條文寫 `Heated_seat_mid`／`Vented_seat_mid`（6 次），LID／DBC 為 `..._medium`。**R-VS48(a) 之縮寫判準為字元子序列** —— `mid` 之 `d` 在 `medium` 中位於 `i` 之前（m-e-**d**-i-u-m），故 `m→i→d` 不成子序列，**判不成立** | **新開（32 輪 W-90）**。判準無誤，是**縮寫形態超出其涵蓋範圍**（`mid` 為 `medium` 之**截斷式**縮寫，非抽字母式）。**本層未擴充 R-VS48(a)** —— 擴充會改變全量對映結果，須先過 R-VS44 之未結 DR 交叉（`FL_HS_RQ` 等五 token 之階數值正是 DR-15 標的）。修法具名：於 (a) 增「前 N 字元前綴」一路，或於別名表逐對登錄。**已裁（34 輪 D-4）**：55 包 §3 判**不擴充判準** —— `mid` 非 `medium` 之前綴（`m-i-d` vs `m-e-d`），亦非其字元子序列之前綴段，**其為變母音之縮寫，無結構性判準可依**。**併入 DR-18**（確認型） |
| **A-VS105** | **32 輪 W-90 之增量法誤覆蓋 W-87 之刻意降級（5 leaf）** | 34 輪 W-94：驅動與產物之 18 筆差異中，**5 筆為 31 輪 W-87 判 W2／`B4-preamble` 之適用性前言 leaf**（`OneStageHeatedSeat-039`／`TwoStagesHeatedSeat-055`／`ThreeStagesHeatedSeat-078`／`TwoStagesVentedSeatsManagement-037`／`ThreeStagesVentedSeatsManagement-060`），於 32 輪 W-90 之增量套用中**被改回 W0** 並清空 `blocker_class` | **新開（34 輪 W-94）。⚠ 本層之回歸**。成因：32 輪採「僅升級、不降級」之增量法（因基線不可重現而不得不然，見 A-VS101），**該規則對「刻意之降級」無免疫** —— W-87 之 W2 是判斷結果，不是待修正之低估。**由 W-94 之驅動查出**，即 R-VS53 之立法目的本身。已於本輪重生產物時更正。**已更正（35 輪 D-4 確認）**：35 輪 W-91 重跑 `writability_driver.py --diff` 得「W0 129／W1 2／W2 106，與 `writability.tsv` 逐 leaf 不一致 0」，5 筆之 W2／`B4-preamble` 於現行產物中成立 |
| **A-VS106** | **`selfcheck_w53.py` 之 L-VS2／R-VS39 檢查於 R-VS52 形態下靜默失效** | 34 輪 W-93：改寫後全批次自檢「0 項」。追查其實作 —— `re.search(r"\$[A-Z0-9_]+\.(\w+)\$\s*=…")`，而 R-VS52 已令訊號名**不加 `$` 包夾**，故該 regex **匹配 0 次**，L-VS2（訊號名須在 DBC）與 R-VS39（標籤須逐字）**兩項皆未執行**。「0 項」不構成證據 | **新開（34 輪 W-93）**。**與 `guard()` 之靜默直通（55 包 §2）同一失效類型**：一個檢查在輸入形態改變後停止檢查，而其輸出與「檢查通過」不可分辨。已修正 regex 為 `\b[A-Z][A-Z0-9_]+\.(\w+)\s*=…`，並追加兩項 R-VS52 形態檢查。**可失敗性已驗**：新版 0 項、舊版（`batch01_v3`／`batch03_v2`／`batch10`）各自報出 R-VS52 違規。**已更正（35 輪 D-4 確認）**：其修法於本輪升格為通則 **R-VS54**，並以 `scripts/selfcheck_anchored.py` 令錨點與被檢輸入同批執行 —— 35 輪實測「新版 0 項／錨點（改寫前舊版）69 項」，十一批中十批之錨點皆命中 |
| **A-VS107** | **42 條讀取步驟以 `record it` 記錄一個後續從未被引用之值** | 34 輪 W-93：新版全批次中，procedure 含 `record` 而非 `record as` 者 **42 處**（如 `2. Read the displayed state of the left front heated seat and record it`）。因 `the same as recorded` 已由 (d) 掃為 0，**其所記錄之值在該 TC 內無任何後續引用** | **新開（34 輪 W-93）**。**非本輪造成** —— 該形態自 batch03 起即存在。R-VS52(2) 之讀取型形式為 `Read <對象> and record as <變數名>` ＋ ER `<變數名> is recorded`；此 42 處既無變數名亦無被引用，**其正解可能是刪除該記錄子句而非為之命名**。**本層未擅改** —— 54 包 §2 之改寫類型 (d) 限於 baseline 比較（已改之 5 條），42 處不在其列。**須裁**：命名或刪除。**已關閉（35 輪 W-95，依 56 包 §2）**：42 處逐處判其記錄值於該 TC 內有無後續引用 —— **無引用 22 處**（刪除 record 子句，改為 `Read … and check that it is <值>` ＋ ER `<X> reads <值>`）、**用於後續比較 20 處**（命名為 `*_initial` 等具名變數，比較步驟改引用變數名）。改寫後全批次 `record` 而無變數名者 **0**、`recorded in step N` 者 **0**；九欄未受影響（變動 0 處）、149 處訊號斷言逐字不變。**其中 1 處（`ScreenOFF-051`）非值之記錄而為匯流排 trace 之記錄**，見上繳 31 §2 |
| **A-VS108** | **產物曾授 W0 予 DR-15 標的之 leaf，其中 2 條已交付且過 pilot #2** | 34 輪 W-94：驅動之 12 筆差異中 **5 筆為 DR-15 之 token**（`FL_HS_RQ`／`FR_HS_RQ`／`*_RQ_TGW`）—— 其值經 W-59 之 `Nh:` 錨點可解，**但 `guard_new_conclusion()` 依 R-VS44 判 DR-CONFLICT**，驅動故判 W2；產物則為 W0。其中 `LeftFrontHeatedSeat-014` 與 `RightFrontHeatedSeat-031` **已生成 TC 且經 pilot #2 通過** | **新開（34 輪 W-94）**。**產物之 W0 早於 R-VS44′ 之 token 級擴張，且從未回放** —— 此即 A-VS101 所述「裁定只寫進產物」之反面：**裁定寫了條文卻沒寫進產物**。本輪重生產物後該 5 筆已為 W2。**已交付之 2 條 TC 不撤回**（依 44 包 §3 之既有處置：標記、覆後複檢），其 `dr15_exposed` 標記早由 A-VS83／A-VS86 記載。**與 A-VS86 合計，DR-15 覆後須回溯之已交付 TC 為 5 條** |
| **A-VS109** | **`PHEVFeatures-017` 為節定義式前言，W-87 之四式未涵蓋** | 35 輪 W-91：`CFTS044-4859529` 逐字為 `This section defines features for plug-in hybrid electric vehicles (PHEV) that can be modified or displayed in the HU, Smartphone app, or Web, generated by ePowertrain, in the HU.` —— **其僅界定本節之範圍，無觸發亦無可觀察之結果**。而 `writability_driver.py::PREAMBLE` 之四式為 `Following requirements are valid only if`／`The requirements in this section are applicable`／`applicable (for\|to) … only`／`This section applies`，**`This section defines` 不在其列**，故該 leaf 判 W0／generatable=yes | **新開（35 輪 W-91）**。**與 A-VS106／`guard()`／31 輪 W-87 同一失效類型之第四例**：判準以形態列舉，形態一變即靜默脫落。**本層未擴充 PREAMBLE**（擴充會改變全量分級，且 W-87 之四式係分析層所定）。**依 R-VS54，其修法須附必命中錨點**。影響：`generatable` 池虛增 1。**已處置（36 輪 W-98(3)）**：`writability_driver.PREAMBLE` 補第五式 `This\s+section\s+defines`，該 leaf 由 W0 轉 **W2／B4-preamble**。**錨點命中**（驅動 --diff 逐筆列出）。依 R-VS46 之涵蓋清單見上繳 32 §4 |
| **A-VS110** | **剩餘池 33 leaf 之斷言目標訊號不在基線 DBC，L-VS2 必 FAIL** | 35 輪 W-91：池 36 中 **33 個**之 `THEN` 賦值目標為 `TELEMATIC_VEHICLE_SETUP.FL_HS_Cmd_Tlm`(11)／`.FR_HS_Cmd_Tlm`(11)／`TELEMATIC_VEHICLE_SETUP2.FL_VS_Cmd_Tlm`(11)。三者於基線 DBC（`PDT27_E2A_R4_BHCAN.dbc` ＋ `PDT27_E2A_R5_FDCAN8.dbc`）之 `SG_` 命中**各為 0**。LID `CAN Mapping` 列 763（`FL_HS_RQ2`）之 **`Atlantis` 欄組**確有該對映與值域（`0 = Heated_seat_off` … `3 = Heated_seat_high`），且該列之 `Atlantis High` 欄組標 `ATL_HIGH_EMPTY` | **新開（35 輪 W-91）。⚠ 阻塞交付**。成因非錯判：該 33 條之 `EE Architecture` 皆為 **`Atlantis Mid`**，依 R-VS19″ 在母體內、依 R-VS51(2) 其值域取 LID `Atlantis` 欄組 —— **惟基線 DBC 為 Atlantis High 之網段檔，不含 Atlantis Mid 之命令訊號**。故「在母體內、值域可得、而訊號不存在於基線 DBC」三者同時成立。**R-VS9(1)′ 令拼寫以 DBC 為權威，L-VS2 令不存在者 FAIL** —— 依現行條文，該 33 leaf **不可寫出通過自檢之 TC**。`writability`／`generatable` 未含「訊號存在於基線 DBC」一項，故判其 W0／yes。**已開 DR-25**。**已處置（36 輪 W-98(1)，依 R-VS57）**：L-VS2 改三分 —— 該 33 條判 **WARN**（不在 DBC 而 LID `Atlantis` 欄組有逐字來源），**不判 W2**，標 `dr_dependent = DR-25`，照常撰寫 TC。二錨點皆過：編造之訊號名 `FL_HS_NOPE_Tlm` 判 FAIL；33 條判非 WARN 者 0。**本輪 W-100 已自其中取 10 條交付（batch13）** |
| **A-VS111** | **`guard_new_conclusion()` 僅在「值需演繹」之路徑上被呼叫，條文自帶值者完全不過閘** | 35 輪 W-91：`HeatedSteeringWheel-012`（`CFTS044-4858531`）逐字為 `… the HU shall send an on-change $HSW_RQ_TGW$ = [Pressed / PSD] signal …`。`HSW_RQ_TGW` 為 **DR-15 之 token 級標的**，直接呼叫 `conflict("HSW_RQ_TGW", "Pressed / PSD")` 回傳 **DR-15**；而 `writability.tsv` 該列之 `driver_reason` 為「**無未解值**」、`writable = W0`、`dr_id` 空 | **新開（35 輪 W-91）**。**R-VS44 之適用範圍脫落**：guard 設於「產生新結論」之輸出點，而條文自帶值者不產生新結論，故整條路徑不經過 guard —— **其輸出與「已過閘」不可分辨**，即 R-VS54 所述之形態。**與 A-VS108 同源而不同路**：A-VS108 是裁定未回放，本項是**閘根本沒被呼叫**。**本層未改驅動**（改法會動全量分級，須先過 R-VS44 之交叉）。影響：本輪 W-91 已將該 leaf 排除，不生成 TC。**已處置（36 輪 W-98(2)，依 R-VS44(4)）**：guard 改為於**每一個進入 TC 之 (token, 值)** 呼叫。**錨點命中**：`HeatedSteeringWheel-012` 由 W0 轉 **W2**。**連帶查出 `SwitchLHD/RHDConfiguration-009` 亦轉 W2**（`FL_HS_RQ = Pressed`，DR-15）—— 見 A-VS114 |
| **A-VS112** | **`FeaturesEnableCriteria-023` 之「all other signal values」須跨條文取值，而該 token 為 DR-8 標的** | 35 輪 W-91：`CFTS044-4859543` 逐字為 `The HU shall disable and not display the features for all other signal values.` —— **其自身無 token**（`clause_pairs()` 回傳 `{}`），故過閘。其「signal values」之所指須自同節（1.3.4.2.2）之兄弟條文取得：`4859538`～`4859542` 之 `$VC_VEH_LINE$ = [RU / VEH_RU]`／`[K8 / VEH_K8]`／`[JL / VEH_JL]` 等。**`VC_VEH_LINE` 為 DR-8′ 之 token 級標的**（`OPEN_DR["DR-8"]`，狀態待送） | **新開（35 輪 W-91）**。**A-VS111 之變體**：不是閘沒被呼叫，是**閘看不到跨條文之依賴** —— 一個條文之可寫性取決於兄弟條文之 token，而 `clause_pairs()` 逐條文取值。**本層未跨條文引入**（與 A-VS103 之處置一致：跨列／跨條文引入屬裁定事項，非執行層自裁）。影響：本輪 W-91 已將該 leaf 排除 |
| **A-VS113** | **`CFTS044-4859031` 未載 `$PowerMode$ ≠ [IGN_RUN]` 時 Rear View Camera 按鍵之狀態** | 36 輪 W-101(3)：條文逐字為 `The Rear View Camera soft button shall be selectable only when the signal $PowerMode$ = [IGN_RUN].` —— 載明 `IGN_RUN` 時可選，**未載其餘點火狀態下該按鍵為「灰階」、「不顯示」或「顯示而不回應」**，三者皆與 `selectable only when` 相容 | **新開（36 輪 W-101(3)）**。35 輪本層曾自 tc_title 推得 `not selectable`，**57 包 §3.3 判其為造值**（§8.4.1：推論即造值）並令退回。本項即該退回所留下之缺口。`ThirdRowHeadrestDump-038` 步驟 2 已退回記錄形態並命名為 `RVC_button_ign_lk`，其比較之期望值標 **`PENDING: DR-26`**。**已開 DR-26**（型 A，Urgency Low）。**處置維持（37 輪 D-4）**：DR-26 仍未送出（60 包 §7 之十份待送之一），`-038` 之 `PENDING: DR-26` 佔位不動 |
| **A-VS114** | **R-VS44(4) 擴大呼叫範圍後，已交付之 `SwitchLHD/RHDConfiguration-009` 由 W0 轉 W2** | 36 輪 W-98(2)：其唯一來源 `CFTS044-4858559` 之未解值為 **`FL_HS_RQ = Pressed`** —— `FL_HS_RQ` 為 **DR-15 之 token 級標的**。舊路徑因該值由條文自帶而未過閘，故判 W0 | **新開（36 輪 W-98(2)）**。**與 A-VS111 同因、不同 leaf**：A-VS111 由分析層自池中查出（未交付），本項為**已交付且已過 pilot #2 之 TC**。**不撤回**（依 44 包 §3 之既有處置：標記、覆後複檢）。**與 A-VS108／A-VS86 合計，DR-15 覆後須回溯之已交付 TC 由 5 增為 6**。**處置維持（37 輪 D-4）**：60 包 §7 逐字確認「覆後回溯已交付 **6** 條（A-VS86 3 ＋ A-VS108 2 ＋ A-VS114 1）」，與本簿相符 |
| **A-VS115** | **DR-25 之提問範圍（3 個訊號）小於 L-VS2 WARN 類之實際範圍（5 個訊號）** | 36 輪 W-98(1)：全母體之 WARN 類逐字為 `FL_HS_Cmd_Tlm`(17 leaf)／`FR_HS_Cmd_Tlm`(16)／`FL_VS_Cmd_Tlm`(14)／**`FR_VS_Cmd_Tlm`(14)**／**`HSW_Cmd_Tlm`(4)**，合計受影響 leaf **65**。而 DR-25（57 包 §2）只列前三者，其影響段亦只稱 33 leaf | **新開（36 輪 W-98(1)）**。**本層未擅自擴大 DR-25 之提問**（其條文逐字轉錄自 57 包 §2，不代擬）。`FR_VS_Cmd_Tlm` 另受 A-VS103／DR-18 牽制（其 LID 值域為 `Heated_seat_*`，跨列引入未獲裁）。**本輪 W-100 之 batch13 未取用 `FR_VS_Cmd_Tlm` 與 `HSW_Cmd_Tlm` 之任何 leaf**，故交付物不受此缺口影響；惟 DR-25 覆後之處置範圍須由分析層補齊。**已關閉（37 輪 D-3）**：60 包 §3 之 **DR-25′** 將提問擴為 5 訊號／65 leaf，逐字轉錄入簿，原 DR-25 條文保留 |
| **A-VS116** | **`*_STATFailSts` 四 token 於 CFTS044 引用 83 次，而 86 條 TC 中以 `Fail_Present` 為驗證標的者 0** | 36 輪 W-101(1)：全 86 條（76 ＋ batch13 10）之 procedure／ER 中，含 `STATFailSts` 之斷言 **10 處**（皆為 batch13 之 `= 0 (Fail_Not_Present)` 前提設定），含 **`Fail_Present` 者 0** | **新開（36 輪 W-101(1)）**。**R-VS56 之 P0(b) 有半邊完全沒有 TC** —— 其逐字為「加熱座椅、加熱方向盤之『開啟』**與 `*_STATFailSts = Fail_Present` 之處置**」。該四 token 之值域早由 **A-VS102** 補收（DBC `0 = Fail_Not_Present`／`1 = Fail_Present`），**故非不可寫，是從未被選入任何 batch**。本輪 P0 重判得 6 條，全數落在「開啟」半邊。**建議下輪之選池優先序納入之**。**已追因（37 輪 W-105(3)）—— 成因非選池歷史，見 A-VS121**：含 `Fail_Present` 之 16 個 leaf **全數 `delegate = blocked`**，故不在 `generatable` 池內，**R-VS58 之優先序對其無效**。**分析層追認（61 包 §1，38 輪 D-4）**：60 包 §4 之追因段**作廢**；R-VS58 維持有效惟不解本缺口，須先解 **DR-5-B**。**惟 38 輪 W-109 實測 DR-5-B 之可解鎖數為 0**（其 5 leaf 另有阻塞），見上繳 34 §2.3 |
| **A-VS117** | **本輪新寫之 `SIG_REF` 抽取式初版有偽陽性，使 WARN 類虛增 171 → 65** | 36 輪 W-98(1)：初版 `\b([A-Z][A-Z0-9_]{2,})\.(\w+)` 命中 `TLM.Display.GUI` 之 `Display`(6) 與一處數字段 `100`(5)，二者皆非 CAN 訊號引用。收緊為 `\b([A-Z][A-Z0-9_]{2,})\.([A-Za-z]\w{2,})\b(?!\.)`（排除三段式路徑與純數字段）後，WARN 之相異 signal 由 7 降為 **5**，受影響 leaf 由 171 降為 **65** | **新開（36 輪 W-98(1)）。⚠ 方法層，未進產物** —— 於 `--write` 之前查出並修正，`writability.tsv`／`generatable.tsv` 從未含虛增值。登記之理由：**其為 R-VS54 所述形態之再現**（新判準之適用範圍在第一版即過寬），且「三段式路徑非訊號引用」之判據須留痕，否則下一個抽取式會再犯。**處置維持（37 輪 D-4）**：收緊後之 `SIG_REF` 於本輪 W-106 再度使用，18 leaf 之訊號抽取無偽陽性 |
| **A-VS118** | **`HSW_Cmd_Tlm` 於 LID 兩個欄組皆無值域 —— DR-25′ 之前提對該訊號不成立** | 37 輪 W-106：`data/lid_pairs.tsv` 之 `signal == HSW_Cmd_Tlm` 命中 **0**；`data/_lid_argroups.json` 之 `Atlantis` 與 `Atlantis High` 兩組皆為 `None`。其條文（`CFTS044-4859496` 等 4 條）之賦值為 `TELEMATIC_VEHICLE_SETUP.HSW_Cmd_Tlm = "ON"` | **新開（37 輪 W-106）**。**DR-25′（60 包 §3）逐字稱「而 LID 之 `Atlantis` 欄組確載其值域」—— 該句對 `HSW_Cmd_Tlm` 不成立。** R-VS57 之 WARN 判定只問**訊號名**有無逐字來源（其於 CFTS044 條文中有，故判 WARN），**未問值域有無來源** —— 二者可分離，本項即其分離之實例。故該 4 leaf 之訊號名可寫而值無從書寫，分級為 **W2**（未解值位於後件）。**本層未跨訊號引入 `HSW_StatSts` 之 `0 = OFF／1 = ON`**（跨訊號引入與 A-VS103 之跨列引入同型，屬裁定事項）。**已處置（38 輪 W-108(1)，依 R-VS57(4)）**：L-VS2 之 WARN 判定加問值域來源 —— 該 4 leaf 判 **W2／`B6-value-absent`**，自 `dr_dependent` 之 65 扣為 **61**。**二錨點皆過**：`HSW_Cmd_Tlm` 必命中 B6、`FL_HS_Cmd_Tlm` 之 17 leaf 維持 WARN |
| **A-VS119** | **四個 HSW leaf 之條文僅差 `[On]` vs `[1h: On]`，致兩組已交付 TC 三欄全同** | 37 輪 W-105 之唯一性掃描：`-016`／`-022` 一組、`-021`／`-015` 一組，其 `pre_conditions`／`test_procedure`／`expected_result` **三欄逐字相同**，僅 `specification_reference` 相異。追其來源：`4858538`／`4858544` 僅差 `[On]` 與 `[1h: On]`，`4858539`／`4858545` 僅差 `[Off]` 與 `[0h: Off]`，後半段逐字相同且 `EE Architecture` 相同 | **新開（37 輪 W-105）。非本輪造成** —— 該四條分屬 25／29 輪之 batch06／batch07。依 §8.2.2「一 leaf 得對多 TC，**反向不可**」，四 leaf 不得共用一份 TC；而其可測內容無法分辨，**故無從產生四份相異之 TC**。**本層未合併、未刪除**。**已開 DR-27**（型 A，Urgency Medium）。該掃描已立為錨點（`scripts/uniqueness_anchor_w105.py`，二組必命中）。**已量得其母體規模（38 輪 W-107）**：母體 237 leaf 之條文正規化後，**可測內容無法分辨者 34 組／72 leaf**；其中 **Layer 3 相同（即無任何可分辨之軸）者 3 組／6 leaf** —— 本項之二組 ＋ **`SwitchLHD/RHDConfiguration-010`／`-013`（新，未交付）**。其餘 31 組／66 leaf 之組內 Layer 3 相異（階數配置可分辨），已以 pre_conditions 處理 |
| **A-VS120** | **batch13 之 `pre_conditions` 未指明階數配置，與 batch14 之 ThreeStages 各列不可分辨** | 37 輪 W-105：batch14 之 `ThreeStagesHeatedSeat-081` 與 batch13 之 `TwoStagesHeatedSeat-058`，其來源條文**逐字相同**（僅節號 `1.3.3.3.3.1` vs `1.3.3.3.2.1` 相異）。batch13 之 pre_conditions 第 1 項為「The vehicle is equipped with heated front seats」，**未指明階數** | **新開（37 輪 W-105）**。**已於本輪更正**：產 `batch13_v2`，10 條之 pre_conditions 第 1 項改為「configured for two heated/vented seat states」，**其餘九欄不動**，原版保留。既有慣例來自已交付之 `ThreeStagesHeatedSeat-080`／`TwoStagesHeatedSeat-057`。更正後唯一性掃描之錨點以外重複組 **0**。**已追認（61 包 §3，38 輪 D-4）**：其為 W-105 之 Sibling Rows 要求所迫，只動一欄、其餘九欄不動、原版保留，「具名供裁而未逕行主張正當」獲判正確 |
| **A-VS121** | **A-VS116 之成因為委派阻斷，非選池歷史 —— R-VS58 對該缺口無效** | 37 輪 W-105(3)：全母體含 `Fail_Present` 之 leaf **16 個**，其 `delegate` **全數為 `blocked`**（其中 5 條標 `blocked_layer = screen`／`blocked_ref = DR-5-B`），故 `generatable` 全數為 `no`，**不在池內**。池 24 條中含 `Fail_Present` 者 **0** | **新開（37 輪 W-105(3)）**。**60 包 §4 逐字稱「成因為選池一路依 reqid 升冪 ＋ 逐 Layer 2 輪流，與風險無關」—— 該追因不成立。** 真正成因為 **R-VS7 之委派界線**（失效狀態之畫面行為屬 Comfort）＋ **R-VS17**（R-VS7(b) 之 16 leaf 於 DR-5-B 到位前標 BLOCKED）。**R-VS58 之優先序只能排序池內之 leaf，對池外者無效** —— 即 W-105(3)「`Fail_Present` 類須優先納入」**無可納入之標的**。P0(b) 之失效半邊要有 TC，須先解 **DR-5-B**，非改選池順序 |
| **A-VS122** | **W-107 之正規化式兩處失準，三度改判組數 36 → 40 → 34** | 38 輪 W-107：初版 (a) 未去除**區塊尾端吸入之下一節標題**（`blocks_with_sec()` 以區塊 id 切割，節標題落在前一區塊之尾）—— 實例 `4858545` 吸入 `1.3.2.1.3.12 Stop-Start System Feature … {4858546}`，致其不與 `4858539` 成組；(b) 以 `\[[^\]]*\]` 去除**全部方括號**，而值之方括號（`[Off]`／`[High]`）為可測內容之一部分 —— 致 `LeftFrontVentedSeat-015`／`-016`／`-017` 被誤判為同組。修正 (a) 得 40 組、再修正 (b)（改為只去值內之 raw 碼前綴 `[1h: On]` → `[On]`）得 **34 組** | **新開（38 輪 W-107）。⚠ 方法層，未進產物** —— 三個數字皆在同一輪內產生，最終數 34 方入上繳。**其被抓到的方式值得記**：TC 層之唯一性掃描（37 輪 W-105）已知 `-016`／`-022` 為一組，而母體層初版**未命中該組** —— **兩個掃描對同一事實不一致，即其中之一失準**。此為 R-VS54 之錨點精神在「兩個獨立掃描互為錨點」形態下之應用 |
| **A-VS123** | **`SwitchLHD/RHDConfiguration-010`／`-013` 之條文可測內容無法分辨，且二者皆未交付** | 38 輪 W-107：二者之正規化條文鍵相同且 **Layer 3 相同**（`SwitchLHD/RHDConfiguration`），即無任何可分辨之軸。與 A-VS119 之二組同型 | **新開（38 輪 W-107）**。**A-VS119 之第三例，且為首個「兩者皆未交付」者** —— 前二組已交付故其代價已付；本組尚可在生成前處置。**本層未合併、未刪除**（§8.2.2 之反向禁令）。**併入 DR-27 之提問範圍**（其問「`[1h: On]` 與 `[On]` 是否同一需求」，本組之形態需另問，故**具名而不逕行併入**，待分析層裁） |
| **A-VS124** | **61 包所開之 W-102–W-107 與 DR-27 與 Part 1 既有編號全面撞號** | 38 輪 VF230 進場：Part 1 之 W 號已用至 **W-109**，其中 **W-104／W-105／W-106／W-107** 於 60、61 輪各有所指（`docs/upstream/33_priority_pool.md`、`34_redundancy_and_batch15.md`、`docs/handoff/60`／`61_review_round37.md`）；**W-102／W-103 為空號**。`DATA_REQUESTS.md` 之 **DR-27 已為 37 輪 W-105 之唯一性提問所用**（成對 A-VS119） | **新開（38 輪 VF230 進場）**。**成因為 R-VS60 之直接後果**：併入既有 feature 即共用編號空間，而 61 包以「Part 2 之第一批工單」自 W-102 起編，未查 Part 1 之現況。**本層之處置分二**：(1) **DR 已逕行改號為 DR-28** —— DR 為對外提問，同號兩義會使覆文無從對應，其代價高於改號；(2) **W 號未改，登記待裁** —— W 號為內部工單標籤，本輪產物一律以中性檔名落檔（`scripts/vf230_leaves.py`／`scripts/vf230_layer2.py`），不寫入撞號。**建議**：VF230 之六工單改編為 **W-110–W-115**（接續 Part 1 之 W-109），即 W-102→W-110、W-103→W-111、W-104→W-112、W-105→W-113、W-106→W-114、W-107→W-115。**待 Pei 裁**。另：61 包 §4.2 標「處置（W-103）」而 §5.4 之 Layer 2 為 W-105、§4.5 標「處置（W-105）」而 §5.2 之 leaf 母體為 W-103 —— **61 包 §4 與 §5 之 W 號互不一致**，本層以 §5／§8 之清單為準 |
| **A-VS125** | **`C-VF230_V1_R5_PDT27.doc` 實為 OOXML，61 包 §6 第 2 項「需轉檔」之前提不成立** | 38 輪 VF230 進場：`file` 回 `Microsoft Word 2007+`；`zipfile` 開得 **26 個 member**，含 `word/document.xml`；`python-docx` 直讀得 **3954 段落／6 表／192 個 Heading 段落**。副檔名為 `.doc` 而內容為 `.docx` | **新開（38 輪 VF230 進場）**。61 包 §6 第 2 項逐字稱「需轉檔；`.doc` 之解析路徑與 Part 1 不同」—— **二句皆不成立**，解析路徑與 Part 1 之 `spec_pdf`（亦為 docx）**相同**。**未轉檔、未改名**：改副檔名會使其 sha256 之檔名對應失效，且原始交付即為此名。`feature.yaml` 之 `paths.spec_pdf_vf230` 逕指 `.doc`，並於註解記明其實為 OOXML。**此為「以檔名推得格式」之失效**，與 A-VS106／A-VS109 之「以形態列舉推得範圍」同族。**已複驗（Pei 補入 `.docx` 後）**：Pei 於 2026-08-23 另補一份 `C-VF230_V1_R5_PDT27.docx`（轉檔產物，sha256 `79aea849…`，356457 bytes，與 `.doc` 之 352747 相異）。以 `python-docx` 逐段落比對：**二者之段落數（3954）／表數（6）／Heading 數（192）／全文逐字（sha1 `5096965efbec`）完全相同**，目次亦逐字相同。→ **`.docx` 不含 `.doc` 所無之任何內容**，轉檔未帶來新資訊，本項之結論不變。二檔皆已入 `INPUTS.sha256`；`paths.spec_pdf_vf230` **維持指向 `.doc`**（其為原始交付之檔名，改指會使既有量測之檔名對應失效）。**建議刪除冗餘之 `.docx`，惟刪檔屬 Pei 之權；本層未刪。** |
| **A-VS126** | **11 份 037 之分頁名不一致，2 份為 `Sheet1` —— 以分頁名定位會靜默漏 2 份** | 38 輪 VF230 進場：11 份分三形態 —— **4 份**為全模板（`封面`／`ChangeHistory`／`Product Document`／`Analysis Report`／`Instructions`／`下拉選單設定處`）、**5 份**僅 `Analysis Report` ＋ `Instructions`、**2 份**僅 **`Sheet1`**（`Daytime_Running_Light…`／`Pressure_Unit , Power_Unit…`）。該 2 份之版面與其餘 9 份**逐欄相同**（18 欄，表頭列 index 6） | **新開（38 輪 VF230 進場）**。**首版腳本以 `wb["Analysis Report"]` 定位，對該 2 份直接拋錯** —— 拋錯是幸運：若首版改以 `wb.sheetnames[0]` 或 `wb.active` 取代，該 2 份會讀到正確分頁而其餘 9 份讀到 `封面`，**其輸出與「這 9 份沒有 leaf」不可分辨**。已改為**以版面定位**（掃描各分頁尋含 `Requirement Description` 之表頭列），與 `recon.py` 之「欄以表頭文字解析、不以字母」同一原則。該 2 份合計 **29 leaf**（12 ＋ 17），佔母體 4.7% |
| **A-VS127** | **canon §4.1.2 之交集法於 VF230 不產生可用之 Layer 2 粒度** | 38 輪 VF230 Layer 2 候選：spec 之 192 個 Heading 中，L5 有 **99** 個，其中 **95** 掛於同一 L4 章（`LTM or ETM Algorithm Requirements`），其餘 4 個分屬 `IPC Algorithm Requirements`(2)／`IPC Algorithim Requirements for Remote Operation`(2)。037 之 106 個 Requirement Title 簇與 spec 目次之交集為 **exact 103 ／ 無對應 3**（交集本身極強），**但其 L4 歸屬塌成 603 leaf（97.4%）對 16 leaf（2.6%）之二分** | **新開（38 輪 VF230 Layer 2）**。**非判準失效，是 spec 之章節結構不承載功能分群** —— 其 L2 為文件結構章（`Introduction`／`Functional Requirements`／`Diagnosis and Recovery`…），功能簇一律落在 L5／L6。canon §4.1.2 步驟 2 假定「spec 目次 = spec 作者之分章方式」可作切分軸，該假定於 VF230 不成立。**本層未自創 Layer 2 標籤**（canon §4.1.2 步驟 3 明令「用 spec 自己的 section ID，不得自造」）。**另備兩個切分源呈報待裁**：037 之 11 份分報告族群（12–131 leaf，中位 52）與 037 之 Sub Categorization（7 值）。另有 **2 簇同名歧義**（`Speed Unit`／`Charge Power Level` 於 spec 目次各有兩處同名章，分屬不同 L4 分支），其歸屬未定 |
| **A-VS128** | **VF230 之 Test Group 之三個來源互不相同 —— W-104 之停下項有三個候選而非二個** | 38 輪 VF230 進場，三源逐字：(a) spec 之 L1 Heading = **`Vehicle Setup Management [VF230_V1_]`**；(b) 037 之 Sub Categorization（僅 leaf）以 **`Vehicle Setting Management (VSM)`** 為多數 **388/619（62.7%）**，其餘為 `Vehicle Setup Management(PSVF230)` 76、`Vehicle Controls Management by VP (VC)` 70、`Vehicle Control` 51、`Camera` 25、`CAN (including VHAL)` 5、`Time_and_Date (PSCFTS015)` 4；(c) `feature.yaml` 現行之 `test_group` = **`Vehicle Setting`** | **新開（38 輪 VF230 進場）**。61 包 §4.3 只問「是否等同 `Vehicle Setting`」，即預設為二選一；**實測有三個互異之候選**，且 (b) 內部亦不齊一（7 值）。依 **R-C6**（feature 身分取自 spec 模組名，非交付資料夾標籤）應取 (a)，惟 (a) 之逐字含 `[VF230_V1_]` 尾綴，取用時須決定是否截去 —— **截去本身即一項裁定**。**本層不決**（61 包 §4.3 明列為停下項）。`feature.yaml` 之 `profiles.vf230.test_group` 落為 **`null`** 並附三源之註，**不以 Part 1 之值預填** —— 預填會使「未裁定」與「已裁定為同值」不可分辨 |
| **A-VS132** **【VF230 線舊制編號，R-VF10 前所開，保留】** | **037 之 8 列判 `Heading` 而 035 SYSRA 判 `Functional Requirement` —— VF230 之 leaf 母體可能為 627 而非 619** | 38 輪 VF230 跨源驗核：037 之 745 列**全數命中** 035（`FM-WI-FSM-035-A02_…_SYSRA_VF230_V4_Released.xlsx` 之 `Basic Report`，2655 列），其中 leaf 側 **619 / 619 全數為 `Functional Requirement`（零錯配，反向錯配亦 0）**；heading 側 126 列中 **118 為 `Heading`、8 為 `Functional Requirement`**。該 8 列之 037 條文逐字為 `The HMI layer shall capture the customer selection for …`（5 條）與 `HW supplier shall notify the IPC_VEHICLE_SETUP2.* signal via VHAL interface …`（3 條），**皆為需求形態，非節標題**。逐一為 `SWITCH3PowerMode-014`／`SWITCH6PowerMode-026`／`SWITCH3Type-039`／`SWITCH5Type-045`／`SWITCH6Type-051`／`SWITCH2HoldLastState-058`／`SWITCH3HoldLastState-063`／`SWITCH6HoldLastState-076` | **新開（38 輪 VF230 跨源驗核）**。**此即 DR-28 所稱「A-VS01 型之錯配無從偵測」之標的 —— 已偵得，且 DR-28 之前提本身已不成立**（見 A-VS134）。**與 Part 1 之 A-VS01 同型**：037 之 Categorization 與其自身條文形態不符。**本層未改 leaf 母體**，`data/vf230_leaves.tsv` 維持 **619** —— 改判為 627 會動及 Layer 2 候選（§4 之 106 簇）、R-VS59 之續號末值與全部下游計數，且 Part 1 之 A-VS01 亦係經裁定方除役。**請裁**：是否將該 8 列納入 leaf 母體。八者集中於 SWITCH 族（Power Mode／Type／Hold Last State），非隨機散布，**疑為上游填表時之整段複製誤植** |
| **A-VS133** **【VF230 線舊制編號，R-VF10 前所開，保留】** | **037 之 11 份分報告僅涵蓋 035 之 `Functional Requirement` 之 57.7%，460 條無任何 SWE.1 分析** | 38 輪 VF230 跨源驗核：035 之 `Functional Requirement` 合計 **1087**，為 037 之 745 列所收者 **627**（619 leaf ＋ A-VS132 之 8），**未收 460（42.3%）**。未收者之 `EE Architecture` 全數 `ATL-Hi`、`VF章節` 全數落於 `01.10.…` 之下，**與已收者同一分支**。**同一章節內既有收錄亦有未收**（`01.10.01.01.74` 收 14、未收 33），故非「整章委派他 feature」之乾淨切分。樣本：`SYS-RA-VF230_V1-785`～`-787`（`Auto_Power_Folding_Mirrors` 之顯示與設定）等 | **新開（38 輪 VF230 跨源驗核）。⚠ 涉及交付範圍之定義。** **非本層漏收**：全樹搜尋確認 VF230 之 037 分報告**僅此 11 份**（`find /Users/peihe/Work -iname '*FM-WI-FSM-037*' -iname '*VF230*'` 命中 11），故未收之 460 條**在上游尚無 SWE.1 分析**。**本層未擴大 leaf 母體** —— canon 之 leaf 母體來源為 037（R-VS4／R-C3 之判準皆立於 037），以 035 之 Category 直接造 leaf 屬換來源，非本層可自裁。**其後果須具名**：若 VF230 之交付範圍為「該 VF 之全部功能需求」，則現行 619（或 627）**只是 1087 之 57.7%**，交付時「覆蓋率」一詞將有兩個分母而答案相差 42.3 個百分點。**請裁**：VF230 之交付範圍以 037 為界，抑或以 035／SYS2 之 `Functional Requirement` 全集為界（後者須先取得其餘 460 條之 037 分析） |
| **A-VS134** **【VF230 線舊制編號，R-VF10 前所開，保留】** | **DR-28 之前提不成立 —— 可替代 SYS2 之跨源已在 `inputs/` 內，且較 `SYS2_VF230.xlsx` 完整** | 38 輪：Pei 於 2026-08-23 補入之 `FM-WI-FSM-035-A02_VF230_…_SYSRA_VF230_V4_Released.xlsx`，其 `Basic Report` 分頁之欄位與 CFTS044 之 SYS2 export **同型**（`SYS2 Sys-RA-Feature-ID`／`SYS2 分類 Category`／`SYS2 VF章節 Chapter for VF`／`SYS2 EE Architecture`／`SYS2 限定地區 Region`），2655 列，**涵蓋 037 之全部 745 列（0 未命中）**。另於 `9_ASPICE/SYS.2 System Requirements Analysis/Z.QS YuShen 260423/08.[SYS2]Vehicle Settings/SYS2_VF230.xlsx` 尋得真正之 SYS2 export（2626 列，schema 相同），**惟其缺 037 之 6 個 `E-Save` leaf**（`SYS-RA-VF230_V1-2660`～`-2665`） | **新開（38 輪）**。DR-28（61 包 §7 開立、本層改號）逐字稱「VF230 缺此來源者，其 leaf 之 Functional/Heading 判定將無第二來源可核，A-VS01 型之錯配無從偵測」—— **兩句皆已不成立**：035 提供了第二來源，錯配亦已偵得 8 個（A-VS132）。**成因為「以檔名族群推得素材有無」** —— 61 包 §2.1 之清冊只掃 `VF230_V1_R5/` 一層，未及 `9_ASPICE/SYS.2` 分支，亦未問「有無其他檔承載同一欄位」。**與 A-VS125（以檔名推得格式）同族**。**DR-28 之處置**：其「請提供 SYS2」之請求**不撤回**（`SYS2_VF230.xlsx` 為原生來源，且其與 035 之 6 列差異本身待解），惟 **Urgency 由「待定」降為 Low**，且其「影響」段作廢。**`E-Save` 之 7 列（6 leaf ＋ 1 heading）於 spec 目次（A-VS127 §2.1）與 `SYS2_VF230.xlsx` 皆無對應，而 035 有** —— 三源不一致，另待判 |
| **A-VS129** **【VF230 線舊制編號，R-VF10 前所開，保留】** | **5 個 `swe_id` 與其 `title` 內嵌換行（分析層開立，62 包 §2.3）** | 38 輪 W-116 之執行層複驗**成立**：`data/vf230_leaves.tsv` 之 619 列中，`swe_id` 異常 **5**（皆為 `SWE1-VC-RearGuidanceLightswith\nCargoLights-014`～`-018`）、`title` 異常 **5**（皆為 `Rear Guidance Lights with\nCargo Lights`）、`family` 異常 0；相異 title **106**，與上繳 61 §4 相符。**回原始 037 儲存格實測**：該換行為**上游所書**（`_STLA_Suspension_Flash_Lights_With_Lower…` 一檔，含 heading 列 `-013` 共 6 列），非本層抽取所致 | **分析層開立（62 包 §2.3），執行層複驗成立**。其「可能即無對應之 3 簇之一之成因」之推論**經 W-116 證實**：以 R-VS66 之正規化重跑交集，`Rear Guidance Lights with Cargo Lights` 由「無對應」轉為 exact，**103／3 → 104／2**。故該簇之不匹配為**正規化產物，非規格缺口**。另二簇（`E-Save` 6 leaf、`CHMSL CAMERA DYNAMIC CENTERLINE` 5 leaf）不受此解釋，**為真缺口**。**本層未改上游之值**（改值即造值）；`vf230_leaves.tsv` 之序列化形式亦不改，改為於比對端解序列化（見 A-VS135） |
| **A-VS130** **【VF230 線舊制編號，R-VF10 前所開，保留】** | **17 個 `swe_id` 缺數字前之連字號（分析層開立，62 包 §2.4）** | 38 輪 W-117／W-118 之執行層複驗**成立**：619 個 `swe_id` 中不符 `-\d+$` 者 **17** —— `SWE1-VC-TrailerBrakeType025`～`037`（13）與 `SWE1-VC-MaxPowerLevel140`～`143`（4）；其餘 602 皆符；619 個皆以 `SWE1-VC-` 起首。**W-118 回原始 037 儲存格逐字實測**：二族之**原始儲存格即無連字號**（另含 heading 列 `TrailerBrakeType024`／`MaxPowerLevel139`，故 037 內共 19 列同型），皆出自 `_STLA_Trailer_Name - Max_Power_Level_Report.xlsx` 一檔 | **分析層開立（62 包 §2.4），執行層複驗成立，且確認為上游書寫瑕疵**（非本層抽取所致）。**W-117 之受影響清單（3 處，未改）**：(1) `features/vehicle_setting/scripts/layer3_w46.py:41` 之 `re.match(r"SWE1-VC-(.+)-\d+$", swe_id)` —— 對該 17 個回 `None`，**family 取不到**；Part 1 之 271 leaf 受影響 **0**。(2)(3) `scripts/recon.py:628-636` 之 `-\d\d$` —— 該式要求「連字號 ＋ 恰二位數字」，而兩 feature 之 ID 皆為三位數（`-001`），故其於 **Part 1 之 271 與 VF230 之 619 皆命中 0**；該處為 R-C3 所禁之啟發式與判準並陳之**證據測量**，非選取路徑，**其數字本即恆為 0，與本項無關**。**Part 1 之既有產物未受影響，不修**（62 包 §5.2）。**已開 DR-29**（W-118） |
| **A-VS135** **【VF230 線舊制編號，R-VF10 前所開，保留】** | **W-116／W-119 之判準三度修正 —— R-VS66 之正規化清單與 R-VS65 之 token 定義皆不足以達成其自身目的** | 38 輪。**(a) W-116**：依 R-VS66 逐字所列之正規化（NFKC ＋ `[\r\n\t]` 摺疊 ＋ 大小寫不分）重跑，結果**仍為 103／3，與複驗前無異**。追因：`vf230_leaves.tsv` 將換行序列化為**字面兩字元** `\n`，`[\r\n\t]` 不匹配，而後續之 `[^a-z0-9]+` 摺疊會把 `n` 當英數字保留，致 `with\nCargo` → `with ncargo`。加入解序列化後方得 **104／2**。**(b) W-119**：R-VS65 令「token 由 DR 正文取得，不自創」，初版依此三式抽取，得 DR-15 波及 14／DR-21 波及 17／DR-25 波及 **174**；逐一查其命中 token 為 `High`(9)／`Medium`(5)／`START`(10)／`Radio`(2)／`TELEMATIC_VEHICLE_SETUP`(172) —— **前四為英文常用詞，第五為訊息名而非該 DR 所問之訊號**。收緊後（`A.B` 取全稱；反引號式加「含 `_` 或 `.`」之識別式判準）得波及 1／不波及 10／待判 2。**(c)** 該識別式判準初版套用於全部三式，致 `$DriverSide$`（無底線）被誤濾、DR-20 空手；改為僅套於反引號式，得最終之**波及 1／不波及 11／待判 1** | **新開（38 輪 W-116／W-119）。⚠ 方法層，未進產物** —— 三個版本皆在同輪內產生，僅最終數入上繳。**與 A-VS117／A-VS122 同族之第三例**：新判準之第一版適用範圍必過寬或過窄。**本項之特殊處在於判準係由條文逐字所定** —— R-VS66 明列正規化項目、R-VS65 明列 token 取得式，**二者皆為分析層所書，而皆不足以達成其自身宣示之目的**。故此非執行層之實作疏失，是**條文之可執行性缺口**。**建議**：條文所定之判準須附「必命中／必不命中」之錨點（R-VS54 之精神），否則其充分性無從在落筆時檢驗 |
| **A-VS136** **【VF230 線舊制編號，R-VF10 前所開，保留】** | **A-VS118 之 4 leaf 之值域逐字載於 037 之 `Verification Method` 欄 —— 其「無來源」之結論係搜尋範圍所致** | 38 輪 W-120：A-VS118（37 輪 W-106）判 `HSW_Cmd_Tlm` 於 LID 之 `Atlantis`／`Atlantis High` 兩欄組皆無值域、DBC 之 `SG_` 命中 0，其 4 leaf 於 38 輪 W-108(1) 判 **W2／`B6-value-absent`**。W-120 實測：CFTS044 之 037 中 `Verification Criteria`／`Verification Method` 提及 `HSW_Cmd_Tlm` 者**恰為同 4 個 leaf**（`HeatedSteeringWheelManagement-029`／`-030`／`-033`／`-034`，reqid `4859496`／`4859497`／`4859500`／`4859501`），其 `Verification Method` 末行逐字為 `* verify that TELEMATIC_VEHICLE_SETUP.HSW_Cmd_Tlm = "ON"`／`= "OFF"`／`* HMI shall update to TELEMATIC_VEHICLE_SETUP.HSW_Cmd_Tlm = "ON"`／`= "OFF"` | **新開（38 輪 W-120）**。**A-VS118 之判斷未誤 —— LID 與 DBC 確實無 —— 誤在搜尋範圍未含該欄**，而該欄之未被搜尋非疏忽，是**全庫從未有人知其存在**（W-120 §2：63 條 R-VS／`framework.md`／全部腳本／canon／本 feature 之 profile 對 `Verification Criteria`／`Method` 命中皆為 **0**）。**與 A-VS131 同族**：以既有之搜尋範圍推得「來源不存在」。**本層未改該 4 leaf 之分級**（W2 維持）—— 轉 W0 須先裁定該欄之位階，而 R-VS9(1)′（拼寫以 DBC 為權威）與 R-VS57(4)（WARN 須名與值域皆有來源）**皆未預設 037 之 VC/VM 為值域來源**。**請裁**。另：該欄之內容品質不齊一（CFTS044 有 14 leaf、VF230 有 90 leaf 之 VC/VM 逐字為「Requirement is not clear」），**不可整欄一體採信** |
| **A-VS137** | **`HSW_StatFailSts` 之值域於 `spec_variables.tsv` 與 LID 兩欄組皆為空，而基線 DBC 有其完整 `VAL_`** | 40 輪 W-111：`bus_domain()['HSW_StatFailSts']` 與 `lid_column_domain()['HSW_StatFailSts']` **皆為空集**，而 DBC 之 `VAL_` 為 `0 "Fail_Not_Present" 1 "Fail_Present"`。致 `SWE1-VC-HeatedSteeringWheelManagement-035` 判「未解值位於後件」而維持 **W2**，成為 16 個 `Fail_Present` leaf 中**唯一未由 `no` 轉 `yes`** 者 | **新開（40 輪 W-111）**。**A-VS102 之同型遺漏**：該 anomaly 補收了 `FL/FR_HS_STATFailSts`／`FL/FR_VS_STATFailSts`／`EngineSts` 五者，**漏收 `HSW_StatFailSts`**。**BACKLOG 之 B-02**（`$HSW_StatFailSts$` 之 R-VS20 階梯重查）其解凍條件逐字為「(b) HSW 相關 leaf 進入生成時」——**本輪該條件已成立**（R-VS40(b)，具名 leaf 為 `HeatedSteeringWheelManagement-035`）。**本層未補收**：補收會改動全量分級，而本輪之作業範圍為 R-VS59～R-VS62 四條新裁，補收不在其列。**已補收並關閉（41 輪 W-114）**：`writability_driver.dbc_value_backfill()` 以回放形式自 DBC `VAL_` 補其值域（R-VS53(2)，不手改 `spec_variables.tsv`）。`HeatedSteeringWheelManagement-035` 由 **W2 轉 W0**，分級 137/2/98 → **138/2/97**，**錨點 1 由 15/16 轉 16/16**。**W-114(2) 之同型遺漏全量量測**：237 leaf 所引 41 個 token 中，DBC 有值域者 8，而「DBC 有而 `spec_variables` 與 LID 兩欄組皆空」者 **僅本項 1 個** —— 升級門檻「> 5」**未命中**，A-VS102 之補收非系統性不足 |
| **A-VS138** | **5 個 leaf 於通風座椅／加熱方向盤之節內引用加熱座椅之 `*_HS_STATFailSts`** | 40 輪 W-113：`HeatedSteeringWheelManagement-031`（`4859498`）引用 `FL_HS_STATFailSts`；`TwoStagesVentedSeatsManagement-047`（`4859449`）／`-056`（`4859459`）／`ThreeStagesVentedSeatsManagement-081`（`4859488`）引用 `FR_HS_STATFailSts` | **新開（40 輪 W-113）**。**DR-18 之「加熱／通風前綴交叉」家族之新實例**，惟此處交叉者為**訊號名本身**而非值之前綴。**本層逐字轉錄，不更正** —— 「判某訊號為 typo」與「以所在節之實體功能推其應為何訊號」是兩事，後者為跨欄推論（同 A-VS103 之處置）。batch16 已取用其中 3 條，其 `test_procedure` 依條文寫 `*_HS_STATFailSts`。**併入 DR-18 之確認範圍**。**41 輪 W-116 再取 1 條**（`ThreeStagesVentedSeatsManagement-081`，其於通風座椅節內引用 `FR_HS_STATFailSts`），累計 4 條已交付。**仍逐字轉錄，未更正** |
| **A-VS139** | **W-112 之「查得 174／查無 0」為 Layer 3 群層級之命中，非逐 leaf 之畫面層對應** | 40 輪 W-112：`delegation_lookup.tsv` 之 `comfort_leaf_ids` 為 R-VS7(a)′ 之**功能群指名**，故本表之比對粒度為 Layer 3。174 個標的之 `comfort_hit_count` 僅三種值（1／5／17），相異 `comfort_leaf_ids` 組合 **3 種** —— 即 174 個 leaf 實際只對應 3 組 Comfort 條文。**且 Comfort 037 之 403 條 Functional 中，含 heated／vented seat 者 20 條，其中含 fail／error 者 0 條** | **新開（40 輪 W-112）**。**升級條件「查無數 > 查得數」未命中，惟該數不可讀為「Comfort 素材足以支撐 R-VS59(2)」** —— 其只證明「該功能群於 Comfort 有條文」，未證明「該 leaf 之特定畫面行為有逐字來源」。**batch16 之 10 條即其反例**：全數為失效狀態，而 Comfort 之 seat 條文含 fail 者 0，故其畫面層斷言一律標 `PENDING: DR-5-B`（R-VS59(4)）。**建議 W-112 之判準改為逐 leaf 之行為層比對，並附錨點**。**已改（41 輪 W-115(2)）**：判準改為「對象 ∧ 行為」雙命中之逐 leaf 比對，**錨點（`Fail_Present` 類 16 leaf 須全數判查無）命中**。新判準得 **查得 2／查無 72／行為未抽出 117**（標的 191，已納入 R-VS59 廢除之 `blocked`）——舊判準之「查得 174／查無 0」因而確認為失準。**惟新判準之行為抽取式只涵蓋 74/191**，其餘 117 條列為第三類而非計入查無 |
| **A-VF2** | **W-VF14 之 RD-1 判準初版以全等比對 `layer3`，命中 0** | 38 輪 W-VF14：「已交付」候選判準 (c)（已於 RD-1 送出之項次）初版以 `layer3 in ("HeatedSeat","VentedSeat","Heated Seat","Vented Seat")` 辨識，**命中 0**。實際之 `layer3` 值為族名形態 —— `ThreeStagesHeatedSeat`(22)／`ThreeStagesVentedSeatsManagement`(22)／`TwoStagesHeatedSeat`(20)／`LeftFrontVentedSeat`(15) 等 **11 種**，無一與四個全等字串相符。改為 `re.search(r"(HeatedSeat\|VentedSeat)")` 並明排除 `SteeringWheel`（`HeatedSteeringWheel*` 非 RD-1 之標的）後得 **158**（RD-1 自述 160，差 2 為未入 `writability.tsv` 者） | **新開（38 輪 W-VF14）。⚠ 方法層，未進產物** —— 兩版皆在同輪內產生，僅 158 入上繳。**A-VS135 之同族第四例，且為 R-VF11 立法後之首例** —— 若當時已依 R-VF11 為判準 (c) 附錨點（必命中：任一 `*HeatedSeat*` leaf；必不命中：任一 `HeatedSteeringWheel*` leaf），初版之「命中 0」在落筆時即可見。**本例反證 R-VF11 之必要性**：命中 0 與「RD-1 未涵蓋任何有 VC/VM 值域之 leaf」不可分辨，若未追查即會據以作出錯誤之交付判定。本輪已追查 |
| **A-VS140** | **`$VC_VEH_LINE$` 於 237 leaf 僅 2 處引用，其值為 `WL` 與 `M4 OR MP` —— R-VS62 之七個車型碼於母體內全數命中 0** | 41 輪 W-115(1)：以 `clause_pairs()` 逐 leaf 取 `VC_VEH_LINE` 之值，得 **2 leaf／2 相異值**：`WL` ×1、`M4 OR MP` ×1。R-VS62 所裁之已解四碼（`332`／`WS`／`DT`／`HDCC`）與未解三碼（`M182`／`M189`／`M240`）**於本母體之引用皆為 0** | **新開（41 輪 W-115(1)）**。**R-VS62 之二錨點兩側皆無標的**，依 R-VS54(2) 不得記為通過 —— 該條之解鎖與其縮限**於本 feature 內皆無從驗證**。成因：DR-8′ 所載之「103 處引用」係就 **CFTS 全文**量得，非就 237 個 in-scope leaf；R-VS62 因而是**以母體外之證據所裁**。**且 DR-8′ 縮限後之三碼不涵蓋本母體實際使用之 `M4 OR MP`** —— 該值於 LID v1.76 與 PROXI 表列 466 皆未查得。**本層未擴充 DR-8′**（其範圍屬裁定事項），具名待裁。**已關閉（42 輪 D-3，依 R-VS62′）**：分析層查 PROXI 表列 466，母體實際引用之二值皆已解（`WL` → 101 (65 Hex)；`M4 OR MP` → 98 (62 Hex) OR 93 (5D Hex)）。**DR-8′ 撤回，不送出**；R-VS62 之四碼部分作廢 |
| **A-VS141** | **`Also the requirements are valid only if …` 為適用性前言之第六式，W-87 之五式未涵蓋** | 42 輪 W-119：`SWE1-VC-OneStageHeatedSeat-040`（`CFTS044-4859361`）逐字為 `Also the requirements are valid only if $Heated_Seat_Levels$ == "One Level"` —— **其僅界定後續條文之適用範圍，無觸發亦無可觀察之結果**。而 `writability_driver.PREAMBLE` 之五式為 `Following requirements are valid only if`／`The requirements in this section are applicable`／`applicable (for\|to) … only`／`This section applies`／`This section defines`，**`Also the requirements are valid only if` 不在其列**，故該 leaf 判 W0／generatable=yes | **新開（42 輪 W-119）**。**與 A-VS109 完全同型**（第五式即由 A-VS109 補入），亦即 **R-VS54 所述形態之第六例**：判準以形態列舉，形態一變即靜默脫落。**本層未擴充 PREAMBLE**（擴充會改動全量分級，且 W-87 之式係分析層所定）。**本輪已將該 leaf 列入 `held_out` 並具名**，未生成 TC。**建議之修法**：以「valid only if／applicable only」之語義段為判準，並依 R-VS46 列其涵蓋清單與未測形態 |
| **A-VS142** | **pilot #4 之分層維度 `dr_dependent`（有／無）退化 —— 43/43 皆為「有」** | 42 輪 W-118：交叉格矩陣實測 `batch14/有 10・無 0`／`batch15/有 13・無 0`／`batch16/有 10・無 0`／`batch17/有 10・無 0` —— **「無」側之四格全為 0**。故八格之設計實際只有四格，分層之 7 條中 4 條來自四個 batch、**3 條係自最大格（batch15）補足** | **新開（42 輪 W-118）**。**抽樣維度失去區分力**：`dr_dependent` 自 batch14 起即全批標記（R-VS57 之 WARN 類 ＋ R-VS59(4) 之 PENDING 類），故該維度對此母體恆為單值。**本層照 67 包之抽法執行並具名回報**，未自行改維度。**建議之替代維度**：`screen_source`（found／PENDING／unextracted）或 `priority`（P0／P1），二者於本母體皆非單值。**已處置（68 包 §4，43 輪 D-5）**：分析層裁定分層維度改為 `batch × priority`（P0 24／P1 109 於各 batch 皆非單值），**不重抽** —— 已出之 15 條維持，其分層 7 條之代表性限制記入 pilot 之結論 |
| **A-VS143** | **W-120 之升級門檻「(7) AH 欄非空應為 21」隨每批交付而變，寫死之數不可持續** | 42 輪 W-120：dry-run 實測 AH 欄非空 **26** —— 其為 `delivery_disclosure.md` 之數，而該數於本輪 W-119 之 batch18 產出後由 **21 增為 26**（batch18 新增 5 條畫面層 `PENDING`）。67 包之升級條件逐字為「(7) 非 21」 | **新開（42 輪 W-120）**。**非實作缺陷**：AH 欄之非空數 = 畫面層 `PENDING` 之 TC 數，其隨每輪之交付內容變動；門檻若寫死為某輪之值，**次輪必然「命中」而其命中不表示異常**。**本層之處置**：重生 `delivery_disclosure.md` 使其含 batch18，二數同為 **26**，並將門檻讀為「AH 欄非空數 ≠ `delivery_disclosure.md` 所載之數」。**該讀法為本層所擇，具名待裁** |
| **A-VF3** | **RD-1 之 158 vs 160 —— 代理判準與實質判準之定義不同，非資料有誤（分析層開立，V07 §6.1）** | 38 輪 W-VF18 之執行層追查：**實質判準**（RD-1 逐字標的 `Heated Seat` 88 ＋ `Vented Seat` 72，其成員以 `framework.md` 之 Layer 2 → Layer 3 對照表為準）自 `writability.tsv` 重算得 **160**（Heated 88／Vented 72），**與 RD-1 自述完全相符**。**代理判準**（W-VF16 所用之 `layer3` 字串含 `HeatedSeat`／`VentedSeat`）得 **158**。差額 **2** 為 `SWE1-VC-LeftFrontHeatedSeat-004`／`-011`，其 `layer3` 為 **`CrossZone Common`** —— 該值為 `framework.md` 明列之 Heated Seat 之 Layer 3，惟其名不帶族名字串 | **分析層開立（V07 §6.1），執行層追查結案為 (iii) 二者定義本不相同**。**非 (i)**：`writability.tsv` 無遺漏，該 2 leaf 皆在其內且 `layer3` 值正確。**非 (ii)**：RD-1 之 160 無計數誤。A-VF3 所慮之「`writability.tsv` 非該範圍之全集，其作為代理判準即不成立」**前半不成立、後半成立** —— 其為全集，是取用方式錯了。canon §5a「代理判準不得凌駕實質判準」於此獲正面驗證。**W-VF16 之判準 (c) 應以 160 為準**，惟**此更正不改變 W-VF16 之結論**：A-VS118 之 4 leaf 於 158 與 160 兩版皆判「未交付」。**本輪未改任何檔** |
| **A-VF4** | **錨點本身可以選錯 —— 選了不在被掃描集合內之實例，其值恆為 `None`** | 38 輪：**(a) W-VF18** 首版以 `SWE1-VC-LeftFrontHeatedSeat-001` 為必命中錨點，實測「實質=False 代理=False」而停；追因：該 leaf **不在 `writability.tsv` 之 237 列內**（`leaves.tsv` 有 271 列，二者差 34）。**(b) R-VF17 施行** 首版以 `SWE1-VC-HeatedSteeringWheelManagement-023` 為必不命中錨點，其於 `writability.tsv` 內不存在，取值為 `None`；施行前後皆 `None`，**「錨點通過」與「錨點不存在」不可分辨** | **新開（38 輪 W-VF18／R-VF17 施行）**。**R-VF11／R-VF19 之盲點**：二條文令「附必命中／必不命中錨點」，**未令「先驗錨點存在於被掃描之集合內」**。一個不存在之必不命中錨點**恆為通過**，其提供之保證為零 —— 此與 A-VS106（檢查在輸入形態改變後靜默停止檢查）同型：**驗證機制自身之失效與其通過不可分辨**。已改正：W-VF18 改用 `LeftFrontHeatedSeat-003`（在檔內）並另加**鑑別錨點** `LeftFrontHeatedSeat-004`（實質須命中、代理須不命中，即差額本身）；R-VF17 改用 `-031` 並加 `raise SystemExit` 於錨點不在檔內時。**已寫入 PLAYBOOK 檢查表第 3 項**（R-VF19） |
| **A-VS144** | **W-121 之新判準對 `ThirdRowHeadrestDump-027` 誤判為前言，而該 leaf 已交付** | 43 輪 W-121：`CFTS044-4858988` 逐字為 `The Third Row Headrest Dump Softkey button will also be accessible from the Rear View Camera screen, if applicable.` —— 新判準判其為前言，因 (a) 其動詞為 `will also be accessible`（**繫詞結構**），不在 `shall <verb>`／`has to <verb>` 之抽取範圍內；(b) 其含條件性措辭 `applicable`。而該 leaf 已於 batch02 交付（`Headrest Dump softkey reachable from the Rear View Camera screen`），且經 pilot #1／#2 | **新開（43 輪 W-121）**。**新判準之兩個構件各有一處不足**：其一，可測動詞之抽取只認 `shall`／`has to`，漏 `will`／繫詞；其二，**「if applicable」為附加於可測需求之限定語，非適用性前言** —— 二者形似而語法位置不同（前者句末、後者句首且統轄後續條文）。**本輪未套用新判準**（升級條件已命中，見上繳 38 §2.1），故產物未受影響。**修法之方向**：可測動詞之抽取須涵蓋 `will`／`shall be`／`is`＋形容詞；條件性措辭須限於**句首且統轄後續**者 |
| **A-VS145** | **`dr_dependent` 之實際覆蓋為 69/143，非 68 包 §2 所載之 139/139；本層上繳 37 §6-4 亦誤記為「全部」** | 43 輪 D-4：逐批實測 —— batch01–12 之 `dr_dependent` 非空者 **2**（batch03／batch04 各 1），batch13–19 之七批**全批標記**（10＋10＋13＋10＋10＋10＋4 ＝ 67）。合計 **69**／143。`screen_pending` 為 **26**。二者範圍差 **43** 條，非上繳 37 所載之 113 條 | **新開（43 輪 D-4）。⚠ 兩層皆誤**：68 包 §2 之「139/139」與本層上繳 37 §6-4 之「139 條中標 `dr_dependent` 者為全部」皆為**以 pilot 母體（batch14–17 之 43 條，該範圍內確為 43/43）外推至全母體**。**成因同型於 A-VS140／R-VS50′**：以子集之實測代全集之陳述。二標記之分離（68 包 §2）不因本項而改，**惟其差額之數須以 43 為準** |
| **A-VF5** | **`writability.tsv` 237 vs `leaves.tsv` 271，差 34 —— 結案為既有規則之正當排除（R-VS15）** | 38 輪 W-VF22：`leaves.tsv` 271 減 `writability.tsv` 237 = **34**（反差 0）。該 34 **與 `data/non_functional_leaves.tsv` 之 34 列逐一相等**（集合全等，實測 `True`），其 `categorization` 為 `Heading` 25 ／ `Information` 9，family 分布 `Common Features` 10／`HeatedSeat` 11／`VentedSeat` 9／`Heated Steering Wheel` 4 | **分析層開立（V08 §6.1），執行層追查結案為 (i) 依既有規則之正當排除**。**明文出處：R-VS15** —— `docs/upstream/06_comfort_overlap.md:81` 逐字「本 feature 母體 = `data/leaves.tsv` 減去 `data/non_functional_leaves.tsv` = **237**（R-VS15）」；`RULINGS.md:187` 另令「**271 僅用於描述 037 之列數，不得作為任何比率之分母**」。故 **237 即為裁定之母體，非 271 之殘缺子集**。A-VF5 所慮之「一個非全集之來源被當作全集使用」**不成立** —— 其為裁定之全集。**本線既有以 `writability.tsv` 為來源之各判準（W-VF16 (c)／W-VF18／W-VF14）皆未因此失真。** 附帶：本輪 W-VF18 與 R-VF17 首版之錨點失敗（A-VF4）於此獲完整解釋 —— 所選之 `LeftFrontHeatedSeat-001`／`HeatedSteeringWheelManagement-023` **皆為該 34 之成員**（Heading／Information），本即不在 237 內。**`leaves.tsv` 之檔名與其內容不符**（含 34 個非 leaf）為一項命名瑕疵，**本輪未改名**（其為 Part 1 之產物，改名會斷全庫引用） |
| **A-VF6** | **行號型錨點會被同檔他處之編輯位移 —— 位置不是識別** | 38 輪 W-VF20 重跑：R-VF22 施行後，`scripts/vf230_wvf20_619.py` 之兩處錨點皆失效。**(a)** 必為「須改」錨點指向 `feature.yaml:29`，該行已改為 `627`，不再含 `619` → 錨點所指之行不存在，腳本停。**(b)** 逐行覆寫之鍵為 `("feature.yaml", 118)`，而 (a) 之編輯使該行**自 118 移至 119**，覆寫鍵失配，`388/619` 一行遂由「不改」誤判回「須改」 | **新開（38 輪 W-VF20 重跑）**。**(a) 為 R-VF21 第 1 項之正面作用** —— 存在性先驗使「世界已變」被察覺而非被忽略，**該次停下是正確行為，非缺陷**。**(b) 才是缺陷**：以 `(檔, 行號)` 為覆寫鍵，其在同檔任一處增刪行後即失配，**且失配之結果是靜默改判**（該行由「不改」變「須改」），與 A-VS106 同型。已改為 `(檔, 內容片段)` 鍵並以 `frag in txt` 比對。**通則**：判準之覆寫、例外、錨點一律以**內容**定錨，不以行號 —— 行號是位置，內容才是識別。建議納入 R-VF21 |
| **A-VF7** | **`data/leaves.tsv` 名實不符 —— 含 34 列非 leaf（分析層開立，V10 §8）** | `leaves.tsv` 271 列中 **34 列非 leaf**（Heading 25 ／ Information 9），依 R-VS15 已由母體排除，母體為 237；`RULINGS.md` 另令「271 不得作為任何比率之分母」。**檔名為 `leaves`，內容為「037 之列」。** 其已致一次實際失誤：A-VF4 之錨點選了該 34 之成員（`LeftFrontHeatedSeat-001`／`HeatedSteeringWheelManagement-023`）為 leaf 錨點 —— **檔名使人合理推定其內容皆為 leaf** | **分析層開立（V10 §8）**。**不改名**（Part 1 產物，改名斷全庫引用），本條已核可本層之判斷。**惟其所令之處置「改以檔內首行加註」本層實測會破壞消費者，未施行** —— `scripts/writeback_036.py:59` 等以 `csv.DictReader(f, delimiter="\t")` 讀本檔，**首行即表頭**；加一行 `#` 註記後 `fieldnames` 變為該註記字串本身（實測），`swe_id` 等欄全數取不到。**改以旁註檔 `data/leaves.tsv.README` 履行同一目的**（名實對齊、低成本、不動引用），`leaves.tsv` **未動一個位元**（加註後已自 git 原樣還原並驗；該檔為 CRLF＋LF 混用，任何重寫皆會全檔正規化）。**通則**：對既有資料檔之「低成本註記」須先驗其解析路徑 —— 註記之載體本身可能即為資料格式之一部分 |
| **A-VF8** | **R-VF20 之覆寫層於裁定後一輪內攔到真實回歸 —— driver 重跑已 commit** | 38 輪 W-VF27：`grade_overrides.py --check` 於本輪首次執行即報 **8 項不符並 exit 1** —— R-VF17 之 4 leaf 全數回復 `writable=W2`／`blocker_class=B6-value-absent`。追因：**併行線之 commit `70b75d0`（rounds 43/46）重跑 `writability_driver.py --write`**，覆寫被靜默回復，**且已進入 git 歷史**（非僅工作區）。`--apply` 修復後四欄復原 | **新開（38 輪 W-VF27）**。**非新缺陷，是已預告之風險成真** —— 上繳 V07 §2.1 與 §7 第 1 項逐字稱「driver 若重跑，本變更會被回復為 W2」「其存續繫於『沒有人跑 driver』，這不是一個可依賴之狀態」。**R-VF20 之立法目的因而獲得一次完整驗證**：若無 `--check`，該回歸會**靜默通過**（Pei 之裁定被一次例行重跑抹除而無人察覺）。**兩項後續**：(1) `RUNBOOK.md`／`PLAYBOOK.md` 已載「跑 driver 後必跑後置步驟」，而併行線**未跑** —— 該文件之可見度不足以構成保證，**建議改為 driver 自身於結束時呼叫或提示**（本層未改 driver，R-VF20 明令）；(2) 本輪之修復同樣可能被下一次 driver 重跑抹除，**在 (1) 落實前，本狀態仍不可依賴** |
| **A-VS146** | **節標題落入 `test_item` 之上半段** | 46 輪 W-130：全母體掃 `\d+(\.\d+)+ … \{\d{7}\}$` 之尾段，命中 **3 條 5 處**（`blocks_with_sec()` 以區塊 id 切割，節標題落在前一區塊之尾）。34 輪 W-107 已具名該形態（A-VS122），**而未察其已入交付物** | **新開（46 輪 W-130）。已修**：D-4 之修正**反覆**去尾，修正前 3 → 修正後 **0**，八條 pilot 指名者之上半段皆餘一行。**登記遲於上繳 39 §5 一輪**（47 輪 D-5 補登） |
| **A-VS147** | **HTML 實體未還原（`&lt;`／`&gt;`）落入交付物** | 46 輪 W-130：全母體命中 **5 條 9 處**，如 `within a time period of &lt;Tdisplay&gt;`。**抽取缺陷落入交付物** | **新開（46 輪 W-130）。已修**：修正前 5 → 修正後 **0**。**登記遲於上繳 39 §5 一輪**（47 輪 D-5 補登） |
| **A-VS148** | **R-VS67 之 (a)(c) 相衝：44 條已交付 TC 之四階斷言於 1 bit 訊號上無法成立，分級崩落 48 leaf** | 46 輪 W-131：改取 `Atlantis High` 欄組之 `*_Tlm`（1 bit，`0 = Not_Pressed`／`1 = Pressed`）後，`= 3 (Heated_seat_high)` 之類斷言無法成立 —— **44 條**；分級 138/2/97 → **94/2/141**；`generatable` 140 → **96**；池 6 → **2**；§9 自檢 0 → **69 項**（R-VS39 64／§5.5 5） | **新開（46 輪 W-131）。⚠ 阻塞交付**。R-VS67(c) 自身已預告該衝突，惟其量級（過半）46 輪始實測。**已處置（47 輪 W-133，依 R-VS67′）**：欄組改依「能承載」擇之 —— 不能承載者取 `Atlantis` 欄組之 `*_Cmd_Tlm` 並依 R-VS66(a) 標 `impl_gap`。**44 條之斷言全數回復成立**（`dr15_exposed` 保留）；分級回 **138/2/97**；`generatable` 回 **140**；自檢 **0 項** |
| **A-VS149** | **錨點語意在「本輪使其變差」時反轉，9 批誤判為「檢查已失效」** | 46 輪：`selfcheck_anchored.py` 之錨點為各批之**前一版**，其假設為「舊版較差」。W-131 使新版產生 69 項違規而舊版為 0 —— **9 批之錨點回報 0**，被判為「檢查已失效」，實際為「輸入變差」 | **新開（46 輪）**。**已處置（47 輪 W-134，依 73 包 §2）**：錨點改用**固定之刻意違規樣本**（`tests/anchor_samples/selfcheck_anchor.json`，**20 項**逐一對應 §9 之檢查 ＋ L-VS2 ＋ R-VS39 ＋ R-VS52 ＋ R-VS56 ＋ 56§2），**不再以前一版為錨**。驗收：**20/20 必命中**，被檢輸入 0 項 |
| **A-VS150** | **72 包 D-3 之 `check whether` 與 canon §5.5 之末步驟驗證意圖相衝** | 46 輪 W-132：彈窗類 5 條依 72 包 §2 之 D-3 選項一改為 `check whether …`，致 §9 自檢報 **5 項 §5.5 違規**（末步驟無 `check that`／`to verify`／`confirm that`） | **新開（46 輪 W-132）**。**已處置（47 輪 W-135，依 73 包 §3）**：改採**選項二**之最弱斷言 —— procedure `check that an informative popup … is shown`／ER `An informative popup relative to the failure is shown`／內容與樣式以 `remarks` 標 `BLOCKED: DR-5-B`。**5 項 §5.5 違規全數消解**；`screen_pending` 15 → **10** |
| **A-VS151** | **LID `fmt` 之解析兩處失準，致 W-133 之錨點兩側初版皆未命中** | 47 輪 W-133：(a) `lid_pairs.tsv` 之 `fmt` 其換行為**字面之 `\n` 兩字元**（非真換行），未還原者 `0 = Not_Pressed\n1 = Pressed` 被讀成單一值，值域基數由 2 誤為 1；(b) LID 之**單一儲存格常綑綁兩訊號之列舉**（`2 bit signal (FL_HS_STATSts)` … `1 bit signal (FL_HS_STATFailSts)` …），未逐段切分者 raw 碼互相覆寫 —— `HeatedSeatFL` 之四階值域只剩 2 值 | **新開（47 輪 W-133）。⚠ 方法層，未進產物** —— 二者皆於錨點階段查出並修正，`--write` 於修正後方執行。**其被抓到的方式即 R-VS54 之設計**：兩側錨點皆有明確預期，初版**兩側皆回報「兩欄組皆不能承載」**，該一致之錯誤即解析失準之訊號。**若無必不命中側，(b) 之偽陰性會被讀為「High 欄組確實不能承載」而逕行降欄組**。**48 輪 W-136(3) 複驗**：固定錨點 20/20 必命中，判準未再失準 |
| **A-VS152** | **D-3 令補入之 PDT24 兩檔不在 `inputs/`，與本輪禁區「不補素材」相衝** | 47 輪 D-3：`inputs/` 之 PDT24 命中 **0**；兩檔位於 repo 之外（`…/SQA_AUTO_R1LR_SWE6/cantool/dbc/NOW/`）。而 47 輪禁區逐字含「**不補素材**」 | **新開（47 輪 D-3）**。**本層未複製** —— 素材之補入涉 repo 外檔案之搬入，其形態同 R-VS61（VF230 素材補入由 Pei 執行）。**已就地量得二檔之 sha256**（`877e4cbb…` BHCAN2／`defc65d0…` FDCAN8）並備妥 `INPUTS.sha256` 之補列，待 Pei 複製後即可核對。現行 `INPUTS.sha256` 之 `shasum -c` **32/32 全 OK**；Part 1 現有 **16** 檔，補入後為 **18**，與 D-3 所載相符。**已備妥（48 輪 W-138）**：`docs/reports/pdt24_prep.md` 載其補列文字、複製指令與核對指令；二檔之 sha256 與 **R-VS68′ 所載完全相符**。**複製仍屬 Pei** |
| **A-VF9** | **併行線不知本線裁定之存在（治理缺口；分析層開立，V11 §3）** | A-VF9（分析層登記 2026-08-23；來源：A-VF8 之追因）　　A-VF8 之直接成因為併行線重跑 driver 而未跑後置步驟。　**其根因非併行線之疏失，而是本線之裁定（R-VF17／R-VF20）　從未以任何機制送達併行線。**　　現況：兩條分析線共用 `features/vehicle_setting/` 之全部工具與資料檔，　而 `RULINGS.md` 中 `R-VF*` 與 `R-VS*` 並存、無任何跨線通知機制。　併行線之作業者無從知悉某 4 個 Part 1 之 leaf 受一條 `R-VF` 條文保護。　　**此為 R-VF2（併入單一 feature）之結構性後果**，　與 R-VF10 所解決之命名撞號為同源之第二種表現：　**前者為「同名兩義」，後者為「異名同物而不相知」。**　　處置：**不自裁**。跨線通知機制屬工作形態，須 Pei 裁（見 §7）。　本層之最小提案：`R-VF*` 中凡效果落在 Part 1 之產物者　（現為 R-VF17 一條），於 `RULINGS.md` 之 `R-VS` 區塊加一行交叉指標。 | **分析層開立（V11 §3）**。**本輪已處置**：依 R-VF38 建 `CROSSLINE.md`（兩線共用，「受保護」3 項／「未受保護」6 項），`PLAYBOOK.md` 之 `接手` 讀取清單加入本檔且置首，並依 R-VF39 令 `writability_driver.py --write` 之尾段自動呼叫覆寫層 —— **機械檢查方為保證，文件之可見度已證不足**（A-VF8：`RUNBOOK`／`PLAYBOOK` 已載而併行線未跑）。**全集盤點**：22 條 R-VF 中 16 條效果落於他線產物，僅 3 條有能失敗之檢查，**13 條為「未受保護」**，逐條列於 `CROSSLINE.md` 並依 R-VF38 三於每次上繳具名 |
| **A-VF10** | **`R-VS59`–`R-VS66` 八條各有兩個定義 —— R-VF10 所欲防之撞號至今仍活在 `RULINGS.md` 內** | 38 輪 W-VF34：R-VF40 之檢查二（編號唯一性）於**現行全檔**即失敗，報 8 筆。逐一實查為真違反，例：`### R-VS59 ——` 出現於行 **1366**（「VF230 之 B 欄序號自 238 起」，V01 包，Pei 裁定）與行 **1486**（「委派不等於不寫」，Part 1 之 63 包，Pei 裁定）—— **同號兩義，皆為 Pei 裁定，皆在生效中**。R-VS60–66 同型。引用面實測：R-VS59 **124 處**／R-VS62 80／R-VS66 52／其餘各 33–40，**八號合計 433 處**，散於 `RULINGS.md`(59)／`docs/handoff/63_rulings_round39.md`(31)／`docs/upstream/35_delegation_reopen.md`(29)／`V00_numbering_collision.md`(26)／`ANOMALIES.md`(23) 等 | **新開（38 輪 W-VF34）。⚠ 阻塞級。** **非新缺陷，是 V04 §3.1 之改編（`R-VS59`→`R-VF1` … `R-VS66`→`R-VF8`）自始未執行** —— 其屬 **W-VF13**，而 W-VF13 之改名部分至今未動（上繳 V09 §5 第 1 項、V10 §6 第 4 項、V12 皆已具名，未獲排入）。**R-VF40 之檢查二於首次執行即攔到之，與 A-VF8 同型**：機械檢查抓出一項文件與口頭皆已知、卻無人處置之違反。**本層未改號** —— 433 處之引用面，且兩線之條文皆在生效中，改號須同時處理兩線之交叉引用，非本輪工單所涵蓋。**後果須具名**：任何文件寫「依 R-VS59」者，**其所指為何義不可由編號決定**，須由上下文推斷 —— 此正為 R-VF10 立法時所稱「同號兩義會使覆文歸屬不明」之實現，惟其發生於內部條文而非對外 DR |
| **A-VS153** | **openpyxl 存檔丟棄 `x14` 擴充之 dataValidation —— R 欄之受控下拉失效** | 48 輪 W-137：實寫後以 raw XML 比對備份，`xl/worksheets/sheet6.xml`（`Test Case Specification`）之 `x14:dataValidation` 由 **2 降為 0**；一般 `dataValidation` **4 保留**、分頁序不變、drawing／chart 13 不變。所失者為 **`R10:R132` 之下拉**，其來源為 `Reference!$C$4:$C$12`（即受控 9 值） | **新開（48 輪 W-137）。⚠ 逾越禁區之連帶損失** —— 禁區令「表外之欄一格不動」，本項非儲存格之值而是**工作簿之驗證設定**，其為 openpyxl 之已知限制（存檔時警告 `Data Validation extension is not supported and will be removed`）。**資料本身不受影響**：R 欄 143 列全數落在受控 9 值內（W-136(1) 實測 0 出域）。**修復之嘗試已回退**：本層曾以 raw XML 注入還原該區塊，因 `x14:`／`xm:` 之命名空間宣告一併被移除而**使檔案不可開啟**，即依 W-137(3) 自備份還原（sha256 回 `ebe5a65f…5089`，237 列與 x14 皆完好），**再重跑實寫**。**現行母本仍缺該下拉**。**修復之建議（屬 Pei）**：於 Excel 內對 `R10:R152` 重設資料驗證，來源 `Reference!$C$4:$C$12` —— 其範圍原僅至 R132，**本 feature 之 143 列已逾之**，故一併擴至 R152 為宜 |
| **A-VS154** | **W-145 之拆分判準三版收斂 90 → 71 → 14，前二版皆違「不得為使拆分數上升而拆」** | 50 輪 W-145：v1「TC 內相異狀態值 ≥2」得**應拆 90 leaf**——其把**同一情境之前置步驟**誤為多值（`Stop-StartSystem-002` 先送 `ESS_ENG_ST = 3` 再送 `EngineSts = 2`，二者非兩個驗證點）；v2「ER 之驗證性斷言標的相異 ≥2」得 **71**——其把**同一轉換之前後態**誤為兩失效（「indicators are lit」與「indicators are turned off」）；v3 依 canon §8.2.2 之明文軸「**不同控制實體 → 拆**」得 **14**，另去除畫面名 `Heated / Vented Seats screen` 之偽陽性後為 **10**（9 leaf） | **新開（50 輪 W-145）。⚠ 方法層，未進產物** —— 三版皆於稽核階段量得，拆分之執行（W-143(2)）用的是 v3。**禁區逐字「不得為使拆分數上升而拆：拆分須通過 §8.3 之壓力測試」** —— 前二版之數若逕行採用，會產出 76 條不必要之 TC。**其被抓到的方式是逐條抽驗**：v1／v2 之樣本一讀即見其非兩個驗證點。**判準之數大不等於判準之對**。**已升格為條文（51 輪 D-4）**：78 包 §2 之 **R-VS73**「判準之調整使數上升者，須逐條抽驗其增量」，與 R-VS29（下降須逐筆判定差集）對稱。50 輪之禁區句因而不再是臨時條款 |
| **A-VS155** | **W-142 錨點所列之四個子類，其標的數與現行產物不符（B6／B7 為 0）** | 50 輪 W-142(3)：指令列「必命中 ＝『未解值位於後件』**24** ＋『前件無已解條件』**16** ＋ `HSW_Cmd_Tlm` 之 B6 **4**；必不命中 ＝ B4-preamble **6** ＋ B7-indistinguishable **2**」。實測：未解值位於後件 **68**／前件無已解條件 **18**／**B6 0**／B4 **6**／**B7 0** | **新開（50 輪 W-142）**。B6 之 4 條已於 **47 輪 W-133 依 R-VS67′ 處置**（`HSW_Cmd_Tlm` 改取能承載之欄組）；**B7-indistinguishable 從未進產物**（A-VS119／A-VS123 之不可分辨組僅登記，未成為 `blocker_class`）。**兩側各仍有標的且皆命中**（必命中 86/86、必不命中 B4 6/6），故錨點整體成立；**惟該二子類依 R-VS54(2) 不得記為通過**。前二類之數 24／16 vs 68／18 之差，係 R-VS71 生效前後之母數不同（指令所據為 46 輪之數）。**分析層追認（78 包 §4，51 輪 D-4）**：其成因在分析層 —— 「50 輪指令之錨點清單抄自 62 包而未回查其現況（**R-VS50′ 第四次**）」 |
| **A-VF1** | **`recon.py` 之證據測量恆為 0（分析層登記 V04 §4；本輪補落）** | A-VF1（分析層登記 2026-08-23；來源：上繳 62 §4 之「附帶發現」）　　`scripts/recon.py:628-629` 與 `:635-636` 之　`re.search(r"-\d\d$", rid)` 要求「連字號 ＋ 恰二位數字」。　　實測（上繳 62 §4）：Part 1（271 leaf）命中 0；VF230（619 leaf）命中 0。　兩 feature 之 reqid 尾綴皆為三位（`-001`），故該式**於兩 feature 恆為 0**。　　該處係作為「判準之差距」之證據測量（R-C3 所禁之啟發式與判準並陳）。　**一個恆為 0 之測量不提供任何區辨力，其作為證據之力為零** ——　惟其不會報錯，故此缺陷至今未被察覺。　　執行層將其列為「附帶發現，未列為 anomaly」；**分析層登記為 anomaly**　（anomaly 之登記與分類屬分析層自裁範圍）。　　處置：**不改**。其修正屬 Part 1 之事（W-VF13 只查不改）。 | **分析層開立（V04 §4），自始未落檔，本輪由 R-VF48 之檢查攔出**。與 **R-VF9** 同型 —— 一條分析層已登記之項目，其正文從未進入登記簿，而其編號被引用（`RULINGS.md`）。**成因同 R-VF9**：V04 之後無對應上繳（`docs/upstream/V04_*` 為缺號），條文遂未落。**R-VF48 之檢查於首次執行即攔到二例（本項與 `feature.yaml` 之過時 `A-VS131`）** |
| **A-VF11** | **R-VF48 之檢查判準兩度過窄，且其首次執行即攔到二項真缺漏** | 38 輪 W-VF42：`check_rvf48()` 首版只認 `ANOMALIES.md` 之 `\| **A-VSnn** \|` 表列形態，致 **(a)** 早期列之 `\| A-VS03 \|`（不加粗）與 `\| ~~A-VS01~~ \|`（除役加刪除線）被誤報；**(b)** 依 R-VF10 加了「【VF230 線舊制編號…】」標記之 `A-VS129`–`A-VS136`，因編號後不再緊接 `\|` 而被誤報。兩度放寬後（`84 → 73 → 57` 項）方止。**同一檢查首次執行亦攔到二項真缺漏**：`A-VF1` 自始未落檔、`feature.yaml` 兩處仍引用已讓號之 `A-VS131` | **新開（38 輪 W-VF42）。⚠ 方法層，且為 A-VF6 之同族**。**(b) 之成因尤須記**：**R-VF10 所令之標記，使 R-VF48 之抽取式失效** —— 兩條規則各自正確，其**交互**產生誤報。此形態於本線為首見：既有之 A-VS117／A-VS122／A-VS135 皆為單一判準過寬或過窄，**本項為兩條並存之規則互相干擾**。**修法**：抽取式改為「取列首之第一個編號，不限定其後之修飾」—— 以**位置**（列首）而非**鄰接**（緊接 `\|`）為錨。**與 A-VF6「行號是位置，內容才是識別」互補**：此處反之，**欄位之位置才是識別，其後之修飾不是**。**檢查現仍 FAIL 57 項**，逐類具名於上繳 V16 §4，未抑制 |
| **A-VS156** | **§7 讀法 B 之採用，其出處未經下放／上繳包流通，分析層查無** | 51 輪 D-4（78 包 §1）：上繳 43 §2.1 記「讀法 B —— **Pei 裁定 2026-08-23**」，而分析層於其 thread 內查無該裁定，亦未見於任何下放包。**執行層之第一手記載**：該裁定確為 Pei 所裁 —— 50 輪本層以**選項式直接徵詢**（列 A／B 兩讀及其應補數與總量），Pei 擇「讀法 B：逐列舉型訊號」。**其場合為執行層 thread，非下放包** | **新開（51 輪 D-4）**。**非誤植、非將沉默讀為裁定** —— 78 包所列三種可能中屬 **(a)**「Pei 於他處裁定，本 thread 未記」，其「他處」即執行層與 Pei 之直接往返。**成因為流通路徑之缺**：經執行層 thread 產生之裁定**無機制回流至分析層**。依 78 包 §1「該讀法之採用暫掛」，**本輪 D-3 已回復兩讀並列**，`split_audit.tsv` 不以任一為準。**其量級本輪重量得 84 條**（非 78 包所載之 72，見上繳 44 §2.1），即最終 TC 總量 **245（讀法 B）vs 329（讀法 A）**。**R-VS35 之延伸（跨層引用之裁定其出處須具名）本層照辦**：本條即其具名。**已處置並關閉（52 輪 D-3）**：79 包 §1 之 **R-VS74**（Pei）追認其結論（§7 取讀法 B、應補 0、總量以 245 為準）；80 包 §2 之 **R-VS75** 立其通則 —— 「執行層與 Pei 之直接往返所生之裁定，須於次輪上繳包之 §5 具名列出：徵詢之內容、所列選項、Pei 之擇定、日期」。**本輪上繳 45 §5 即其首次照辦** |
| **A-VS157** | **D 型 probe `-014` 為 false pass —— 以「有送出」代替「送出正確之值」** | 51 輪 W-146：其 test_item 為四階真值表（High→Medium／Medium→Low／Low→Off／Off→High），而 procedure 之 ER 只驗 `TELEMATIC_VEHICLE_SETUP3.HSW_Tlm = 1 (Pressed)`。與同型之 `-012` 對比：procedure 僅差 step 1 之值，**ER 逐字相同** —— 設 `off` 得 Pressed、設 `high` 亦得 Pressed，**HU 送錯階數亦通過**。其 `reasoning` 稱「本條驗 High 一列」而實未驗之 | **新開（52 輪 D-3，80 包 §1 判其不通過）。⚠ 本層之 false pass**。成因：`HSW_RQ_TGW` 為 **DR-15 之 token 級標的**，其逐狀態之送出值於來源無逐字對應；本層在「值未解」與「驗證目標即該值」之間**取了可寫的那一半**，而該半不構成驗證。**已改寫（52 輪 W-149）**：驗證步驟之 ER 改 `PENDING: DR-15`，前置步驟照寫（R-VS71：值於來源亦無 → 該處 PENDING，其餘照寫）。**D 型待其過分析層抽驗方得放量**。**已處置並關閉（53 輪 D-3）**：81 包 §1 追認本層之自陳（「其自述『照寫了其餘，卻沒把該處寫成 PENDING，而是換了一個能通過的斷言』，即 A-VS157 之實質」）；81 包 §2 解除逐型抽驗閘，**本輪 D 型 11 條全依改寫後之形態產出**（ER 為 `PENDING: DR-15`，前置步驟照寫） |
| **A-VS158** | **W-150 之 14 條中有 2 條其觸發非「收到狀態訊號」，分型判準漏認無 `$` 之 token** | 52 輪 W-150：`OneStageHeatedSeat-042`（`IF DrvSeatHeating.Req passes to "Requested" THEN TLM shall set TELEMATIC_CLIMATE_SETUP.FL_HS_Cmd_Tlm = "Pressed"`）與 `-044`（同型，`FR_HS_Cmd_Tlm`）**其觸發為按壓請求、標的為命令送出**，屬 A 型而非 E 型。成因：51 輪之分型式以 `IF \(?\$\w+\$` 認 A 型，而該二條之 token `DrvSeatHeating.Req`／`PsngrSeatHeating.Req` **無 `$` 包夾**，故落入 E 型之殘餘類 | **新開（52 輪 W-150）**。**升級條件「W-150 之 14 條有任一其觸發非『收到狀態訊號』」逐字命中**。**本層已排除該 2 條並具名**，batch22 實產 12 條。**另記一項**：`-042` 之標的為 **`TELEMATIC_CLIMATE_SETUP`**（非 `TELEMATIC_VEHICLE_SETUP`）—— 其為本母體首見之 message，其欄組對映未查，**併入該 2 條之待處置**。**已處置並關閉（53 輪 W-152(c)）**：該 2 條依 81 包 §3(c) 於本輪補寫為 **A 型**；其 message 之不符另立 **A-VS159**（LID 查得 `TELEMATIC_CLIMATE_SETUP` 無任何 `_HS_` 訊號），依 R-VS9(1)′ 取 LID 之歸屬 |
| **A-VS159** | **`TELEMATIC_CLIMATE_SETUP` 於 LID 無任何 `_HS_` 訊號，與條文所載之 message 不符** | 53 輪 W-152(c)：`OneStageHeatedSeat-042`／`-044` 之條文逐字為 `TLM shall set TELEMATIC_CLIMATE_SETUP.FL_HS_Cmd_Tlm = "Pressed"`。實測 LID：`TELEMATIC_CLIMATE_SETUP` 有 **20 列，其中無任何 `_HS_` 訊號**；而 `FL_HS_Cmd_Tlm` 於 LID **僅一列（列 764），其 message 為 `TELEMATIC_VEHICLE_SETUP`**。即**條文之 message 與 LID 之 message 不符** | **新開（53 輪 W-152）**。依 **R-VS9(1)′「message 歸屬以 LID 為第一權威」**取 LID 之歸屬，其不符具名於此。其值 `"Pressed"` 依 **R-VS67′** 之能承載判準：`Atlantis High` 欄組之 `SETUP3.FL_HS_Tlm`（`0 = Not_Pressed`／`1 = Pressed`）二值**能承載**條文所述之二狀態，故取之、**不標 `impl_gap`**。**待覆之事由已具名，未逕行調和**：條文所載 message 是否為誤植，或 `TELEMATIC_CLIMATE_SETUP` 之 LID 列不全，本層分不出 —— **併入 DR-15′ 之標的**。**已處置並關閉（54 輪 D-3）**：82 包 §3 確認本層之處置正確（依 R-VS9(1)′ 取 LID 歸屬、依 R-VS67′ 判 `SETUP3.FL_HS_Tlm` 二值能承載、不標 `impl_gap`、不逕行調和而併入 DR-15′） |
| **A-VS160** | **W-152 之分型母數漏 2 leaf；且 `latest_batches()` 之檔名式丟棄整個 `batch21_probe.json`** | 53 輪 W-152：**(a)** 分型時以 53 leaf 為母數，而 81 包 §3 所令為 **64**；實際母體點算得 `ThirdRowHeadrestDump-033`／`-034`（皆 `generatable = yes`、D 型送出）**未入任一段**，係本輪自檢外之點算方發現。**(b)** `writeback_036.py:47` 之 `latest_batches()` 其式為 `(batch\d+)(?:_v(\d+))?\.json$`，**`batch21_probe.json` 不匹配而整檔被丟棄** —— 該檔之 7 條（6 leaf：`-012`／`-014`／`-017`／`-018`／`-021`／`-022`）**不在 54 輪寫回之來源集內** | **新開（53 輪 W-152）。⚠ 二者皆為靜默之漏**，其共同形態為「以名單或檔名為母數，而未回頭與母體點算對照」。**(a) 已補**：本輪加 D 型 2 條（`-033`/`-034`，其區辨軸為接收端 CBC／FSM），batch23 由 45 → **47** 條。**(b) 未修** —— 其屬 `writeback_036.py` 之修改，**本輪禁區為「不寫回母本」而非不得修腳本**，惟該修改會改變 54 輪之寫回列數（**+7**），**依 R-VS73「使數上升者須逐條抽驗增量」與本輪一項作業之限，本層不逕改，具名待 54 輪處置**。**母體點算現況**：237 leaf 中有 TC 者 **219**、無 TC 者 **18**（held_out 7 具名 ＋ `generatable = no` 11）。**已處置並關閉（54 輪 W-153／W-154）**：**(a)** 82 包 §1 就其立 **R-VS76**（每輪另跑以母體為輸入之完整性檢查），本輪 `completeness_w154.py` 實作之，實測 219 ＋ 7 ＋ 11 ＝ **237、無未歸類、PASS**。**(b)** `latest_batches()` 之式改為 `(batch\d+(?:_[a-z]+)?)(?:_v(\d+))?\.json$`，`batch21_probe.json` 已入來源集；依 **R-VS73** 逐條抽驗其 7 條增量：**皆為該 leaf 之唯一版本、無跨批重複、`-022` 之 2 條即 probe 依 §8.3 所拆者** —— **真陽性 7／偽陽性 0** |
| **A-VF12** | **`RULINGS.md` 並非條文之全集 —— 多數 Part 1 條文之正文在 handoff 包內，由檔首對照表指路** | 38 輪 W-VF49：`RULINGS.md` 僅 **45 個 `### R-VS{n} ——` 條文起始**，而全庫引用之 `R-VS{n}` 遠多於此。檔首之「來源／條」對照表逐字載明「`R-VS1 ~ R-VS6` 之正文見 `docs/handoff/00_intake_and_rulings.md` §3，依 canon §8.7 其下放包已落檔且入版控，**本檔不重複轉錄以免二處分岔**」。R-VF48 之首版只認條文起始，遂將 R-VS1–R-VS53 等**大量誤報為「引用而無定義」**（57 項中逾三分之二屬此） | **新開（38 輪 W-VF49）**。**R-VF48 之前提須修正** —— 其立法時假定「條文簿內應有全部定義」，而本簿之設計本即為**部分轉錄 ＋ 指路**。**該假定之錯不使 R-VF48 失效**：其首跑仍攔到 `A-VF1` 與過時之 `A-VS131` 二項真缺漏（上繳 V16 §4）。**修法**：定義之全集 = 條文起始 ∪ 檔首對照表所列 ∪ fenced block 起始。**修正過程中又錯一次**：`head = r.split("---", 1)[0]` 被表格之分隔列 `\|---\|---\|` 截斷，對照表之列全數落空（21 → 15 即修此）—— **`---` 於 Markdown 有兩義（水平線／表格分隔），以之切檔即為以歧義符號切檔**。與 A-VF6（行號是位置非識別）同族：**分隔符之選擇亦須問「它在此語境是否唯一」** |
| **A-VS161** | **拆分時改寫 `test_item` 上半段，違 R-VS6（逐字）** | 55 輪 D-3（83 包 §2 D-5）：50 輪 W-143 之拆分產出中，2 leaf 之 `test_item` 上半段非條文逐字。`ThirdRowHeadrestDump-027`（`4858988`）三處改寫：`will also be` → `shall also be`、`if applicable` → `when supported`、`Softkey button` → `softkey`。`Stop-StartSystem-002`（`4858514`）二處：`When the stop-start system` → `If the stop-start system`、`the HU/CCDMR shall` → `the HMI shall` | **新開（55 輪 D-3）。⚠ 本層之改寫**。**拆分只應窄化其實體，不得改其文字** —— 窄化屬括號內之下半段，上半段為 037／條文之逐字，其改寫使可追溯性斷裂。成因：W-143 之拆分器在複製上半段時套了「規範化」（`will`／`If` → `shall`／`When` 之類），**該規範化本身從未被指令要求，是我自己加的**。**已修（55 輪 W-157 D-5）**：上半段回復條文逐字，窄化改記於下半段；`earlyfix_w157.py` 之 D-5 掃描以**條文為錨**逐條比對。**全母體掃描結果：`split_flag = true` 之 7 條中不符者 2 leaf（5 列）**，升級門檻「> 2 則為系統性」**未逾**（恰為 2）。**惟其非隨機** —— 二者皆為 `will`／`When` 開頭之條文，**該規範化只在條文以非 `shall` 語氣起首時觸發**，故其覆蓋率取決於母體中此類條文之數，非取決於拆分之次數 |
