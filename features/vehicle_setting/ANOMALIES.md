# ANOMALIES — FW036 Vehicle Setting

登記格式 `[A-VSnn]`。**Tier 1：登記，不裁定。**
本輪（00 包）之登記全部為執行層實測；標「沿用分析層」者為未複驗即照錄之項。

| id | 內容 | 證據 | 狀態 |
|---|---|---|---|
| ~~A-VS01~~ | ~~SYS-RA 指向 SYS2 之 `Heading` / `Information` 列~~ | 逐引用實測 273：`Functional Requirement` **239**／`Heading` **25**／`Information` **9** | **除役（02 包 §1.4）** —— 以 037 之 `Categorization` 對 SYS2 之 `Category` **逐 leaf 交叉列表，零錯配**（Functional↔FR 236、Heading↔Heading 25、Information↔Information 9、CFTS100 那筆 NO_REF 1）。**那 25 個不是錯配，是同一批非需求列在兩份文件裡各自被正確標記。**當初登記為異常之成因：只讀了 SYS2 側，未讀 037 自己的 `Categorization`（第 6 欄） |
| A-VS03 | 四份 037 封面完全相同，無法區分 | 沿用分析層（00B §1）。**本輪未複驗** | 登記 |
| A-VS04 | CFTS044 內未填佔位 `{CFTS044-xxxx}` | 沿用分析層（00B §1.1）。**本輪未複驗** | 登記 |
| A-VS05 ⟷ **A-VS53** | `Heated_Seat_Levels` / `Heated_Seats_Levels` / `Heated_Steats_Levels` 三種拼寫並存 | **實測成立**：三者皆為 037 之 `$var$` token；對 3,000 個相異 LID 作**不分大小寫全字串**比對，`Heated_Steats_Levels` **無逐字對應** | 登記，RD-1 候選 |
| ~~A-VS06′~~ | body heading 270 對相異 `{7位數}` 254，差額 16 未追因 | **實測推翻其前提**：以 `word/styles.xml` 解出 heading 1–7 樣式後，body heading **270** 個，其 `{7位數}` **逐處 270、相異 270、差額 0**。254 係分析層在**轉檔文字**上以較寬形態量得（本輪同形態量得 逐處 444／相異 259，亦非 254） | **除役（05 包 §11）**。原記之差額 16 為轉檔文字之產物：原差額 16 於原始 docx 上不重現；254 為轉檔文字之產物 <br>**關聯未驗項已收（08 輪 W-32(d)）**：其「差額 0」所依之樣式階層量測，於 W-32(d) 之逐 reqid 交叉驗證中再度成立（236 / 236 章節一致），**位置法與樣式階層在 reqid 層級亦重合**。 |
| A-VS07′ | DBC 檔名 `R4`／`R5` 在 `PDT27_E2A` 組指網段、在 `PDT25_E3A` 組指版本週次 | 沿用分析層（00H §1.1）。本輪複驗其 `PDT27_E2A` 側：兩檔 `VersionYear`=25、`VersionWeek`=50 **完全相同**，`BusType` 僅 FDCAN8 有且逐字為 `"CAN FD"` | 登記（FYI 類） |
| A-VS08 | `PDO Graphics Release` PDF 車型與主題皆與本 feature 無交集 | 沿用分析層（00C §3.1）。**本輪未複驗** | 登記 |
| A-VS09 | 26PI 版 Pop Up List 較 Comfort／User Profiles 基線新 | 沿用分析層（00D §5）。**本輪未複驗** | 登記 |
| A-VS10 | CFTS044 指名之 `TLM HMI Document` 於客戶 HMI 目錄無同名檔 | 沿用分析層（00D）。**本輪未複驗**（W-13 未執行） | 登記 |
| A-VS11 | 無 `PDT27_E2A` 組之跨版本比對表 | 沿用分析層（00H §2） | 登記 |
| **A-VS12** | **`SWE1-VC-HeatedSteeringWheel-009` 之 `Source Requirement ID` 逐字為 `SYS-RA-CFTS100`** —— 指向 **CFTS100**（非 CFTS044），且**無 `-N` 序號**，於 SYS2 解析不到任何列 | 實測。其為 271 leaf 中唯一取不到 7 位數 Polarion ID 者 | **新開**。下放包 §5.2 記其原因為「SYS2 該列 `Source Requirement items` 為空」——**原因不符**：SYS2 全 538 列之該欄無一為空，且此 leaf 根本解析不到列 |
| **A-VS13** | **`SYS-RA-CFTS\d+-\d+` 之正則使 `SYS-RA-CFTS100` 隱形** | 嚴格式得 273 引用、寬鬆式（`-N` 可省）得 **274**，多出者即 `SYS-RA-CFTS100`。故「273 全部指向 CFTS044」為真，但其為真之原因是**抽取式看不見反例** | **新開**。canon §5a 第 12 條之標準形態 |
| **A-VS14** | **5 個 leaf 對映到 >1 個 CFTS044 章節** | `SWE1-VC-LeftFrontHeatedSeat-004`／`-011` 各對映 4 章節（`1.3.2.1.3.1`–`.4`）；`SWE1-VC-HeatedSteeringWheelManagement-025`／`-026`／`-027` 各對映 2 章節（`1.3.2.1.3`；`1.3.3.3.6.1`） | **新開**。`specification_reference` 之單值形式對此 5 leaf 不成立 |
| **A-VS15** | **`Proxi & Configuration` 分頁無獨立之 `Atlantis High` 欄組** | 該分頁列 2 之欄組標題逐字為 **`Atlantis & Atlantis High`**（第 16 欄），而 `CAN Mapping` 分頁為 `Atlantis`(16) 與 `Atlantis High`(26) **分列** | **新開**。00G §4 之「10 個 Atlantis High 空欄」全數落在此分頁；**其非空欄，而是該表一欄兼管兩者且已自述** |
| **A-VS16** | **`Format` = `See Proxi Table` 之 LID 實測為 8，非 6** | 全簿（含十張車型專屬分頁）掃 `Atlantis High` 與 `Atlantis` 之 `Format` 欄：`Cooled_Seats`／`DSP_SK_PRSNT`／`EC_Mirror_HK_Prsnt`／`Heated_Seat_Levels`／`Heated_Seats`／`Heated_Steering_Wheel`／**`VC_TIRE_CIRCUMF`**／**`VC_VEH_BRAND`** | **新開**。多出之二者非本 feature 之 token，故「本 feature 用得到 4」不變 |
| **A-VS17** | **兩份 DBC 之 141 個共有 signal 中，128 個之起始位元不同** | 逐屬性比對 `SG_ name : start|len@order sign (factor,offset)`：128 / 141 之 `start` 不同（例 `ACV_FailType` BHCAN 55 vs FDCAN8 165） | **新開**。00H §5-3 自陳「同名不同定義本篇看不到」——**實測為常態而非例外**；TC 引用訊號時若不指明網段／DBC，起始位元即不確定 |
| ~~A-VS18~~ | ~~`recon.py` 之 leaf 數與 W-2 不符~~ | recon 46 vs W-2 56（Common Features）；全體 237 vs 271 | **除役（01 輪 W-16）** —— `scripts/recon.py:602` 為 `is_leaf = cat.lower().startswith("functional")`，依 037 之 `Categorization` 過濾；下放包 §5.1 之判準為「A 欄非空」，無此過濾。**二者非對同一件事給出不同答案，是兩個判準在數兩件不同的事**，且 recon 之判準與 036 之實際投影一致（036 之 237 列 = 237 個 Functional 列）。**recon 未錯。** |
| A-VS19 | **`new_feature.py` 之名稱正規化缺陷** | `scripts/new_feature.py:144` 為 `feature.lower()`，**不轉空白**；以 R-VS3 原記之 `"Vehicle Setting"` 呼叫會產生含空白之目錄名 | **措辭改寫（W-26）**：原記為「誤建目錄待刪」——**該目錄本 feature 未落地，無待刪標的**（實測：`ls -d` 與 `find` 皆無輸出、`git ls-files` 未曾匹配，**P1 已關閉**）。**工具缺陷維持登記，不在本 feature 修**；R-VS3′ 已將 scaffold 參數改為 `vehicle_setting` |
| **A-VS20** | **037 之 `Categorization` 大小寫不一致** —— `HeatedSeat` 有一列為小寫 `information` | 逐字值分布：`Functional Requirement` 237／`Heading` 25／`Information` 8／`information` **1** | **新開（02 包 §1.3）**。⚠ **方向須說明白**：其影響 **`Information` 側之計數**（區分大小寫 8、不分 9），**不影響 Functional 母體之界定**（`startswith('Functional')` 區分與不分大小寫**皆得 237**）——該筆是 `information`，兩種寫法都不會被誤收進 Functional。`recon.py` 之 `.lower()` 在此**不是救了它，是恰好不需要救**。惟任何以 `== 'Information'` 分類之下游會少計一筆。RD-1 FYI 類，我方以不分大小寫吸收，不待上游修正 |
| **A-VS21** | **分析層經 MCP 讀取含中文之 repo 檔時，偶發單字元顯示為替代字元** | 曾兩度回報「檔案疑似毀損」（`ANOMALIES.md`「相異 259」後、`00 §1 第 6 點`「沙」後）；執行層位元層實測：嚴格 UTF-8 解碼**通過**、`U+FFFD` **= 0**，「沙」後三處皆為「箱」 | **新開（05 包 §11，工具類）**。⚠ **其危險不在顯示，在於它偽裝成資料之缺陷** ——若執行層照辦改寫，會把一個好的檔案改壞。**通則：跨層回報「檔案毀損」前須先以位元層確認，不得以讀取結果之外觀為據。** |
| **A-VS22** | **CFTS044 之 `$VentedSeatFL$` 值中出現 `Vented Seat Off / HS_OFF`**（`HS_` 為 Heated 之前綴，應為 `VS_OFF`） | 逐 token 抽值時命中；同一 `= [值]` 內之左式已定其為 VentedSeat | **新開（07 包 §3.2，W-21）**。規格筆誤，RD-1 FYI 類。**不影響取值** —— 左式已定其歸屬 |
| **A-VS23** | **LID 表之 `TGW_DISP_STAT` 值域有拼字錯誤 `diplay closed`** | W-19 逐值比對：DBC 作 `Display_closed`，LID 表 `Format` 欄作 `Diplay_closed`（缺 `s`） | **新開（03 輪 W-19）**。RD-1 FYI 類。⚠ **若以 LID 表之字串為 ER 之逐字值，會寫出一個匯流排上不存在的狀態名** |
| **A-VS24** | **CFTS044 以 `IGN OFF` 描述 `$PowerMode$`，而其匯流排訊號 `CmdIgnSts` 之值域無 OFF** | CFTS044 in-scope 值含 `IGN OFF`／`IGN OFF ACC`；DBC 與 LID 表之 `CmdIgnSts` 值域為 `Initialization`／`IGN_LK`／`ACC`／`RUN`／`START`／`SNA` —— **無任何 OFF** | **新開（03 輪 W-19）**。⚠ **非拼字差異，是狀態不存在** —— 引用 `$PowerMode$ = IGN OFF` 之條文，其 ER 無法以 `CmdIgnSts` 之單一值表達。待判：是否對映至 `IGN_LK`，或另有他訊號承載。**登記待判，不自行對映** |
| **A-VS25** | **「00–02 三輪產物不在版控」之陳述為誤** | 實測 `git ls-files features/vehicle_setting/ | wc -l` = **51**；`git log -- features/vehicle_setting` = **6 個 commit**，最早 `c780d5d`（00 輪 30 檔） | **新開（W-25）**。⚠ **兩層皆有責**：分析層於 07 §7／09 包以推測書寫；**執行層於 02 輪上繳原樣重述，而那六個 commit 正是執行層自己跑的** ——手上有直接證據而未查。真正未入庫者僅 `inputs/INPUTS.sha256`（已於 `2c6c9b3` 解決） |
| ~~A-VS26~~ | ~~LID 表與 DBC 之 message 歸屬矛盾~~ | LID `CAN Mapping` r695 之 Atlantis High 欄組實測：`Signal` 欄逐字為 `STATUS_CCAN3.ESS_ENG_ST\nENGINE_FD_2.ESS_ENG_ST`，`CAN` 欄為 `CAN-B\nFD` | **降級為我方解析產物（14 包 §1）**。該儲存格以換行分隔載**兩個 message 對應兩個網段**；`STATUS_CCAN3` 在 BHCAN、`ENGINE_FD_2` 在 FDCAN8，**LID 與 DBC 完全一致，兩者皆對**（與 R-VS8「兩份並用」同型）。執行層未展開單格多值即報矛盾。**DR-13 撤銷。** ⚠ **此為 C3 缺陷之第三次現身**（W-8 值域切分／W-15b′ 交叉配／本項），故 W-23 之解析式修正不再是可延項 |
| **A-VS27** | **LID 表之 signal 名大小寫與 DBC 不符** | LID `HSW_STATSts`／`HSW_STATFailSts`；DBC **`HSW_StatSts`／`HSW_StatFailSts`**（`STAT` vs `Stat`） | **新開（W-15b′）**。⚠ **推翻 00G §2 之「LID 表與 DBC 一致」**。依 R-VS9(1) 照 LID 逐字寫入 TC，即寫出匯流排上不存在之 signal 名 ——與 R-VS9(5) 所防之 `$PowerMode$`／`PowerModeSts` 同型，**惟此次錯的是第一權威本身** |
| **A-VS28′** | **LID 表載有基線 DBC 所無之訊號 —— 範圍縮為 1 支** | 逐支複驗（`SG_\s+<name>\b`，兩份 DBC）：`FL_HS_Tlm`／`FL_VS_Tlm`／`HSW_Tlm` **皆在 BHCAN**（執行層漏查）；`HeatLeftSeatTgl`／`FL_HS_Cmd_Tlm_Req`／`HSW_Cmd_Tlm` 屬 **Atlantis 欄組（非 in-scope）**，依 R-VS9(1)′ 本就不取用（其 message `TGW_HVAC_CTRL_M` 兩份 DBC 皆無）| **範圍縮為 1 支（14 包 §2）**：**`HDRstRelRq_3rdRow`** 為唯一真缺 —— `RADIO_B3` 存在於 BHCAN 而其 4 支 signal（`ManDispCtrl`／`PowerSideStep_Req`／`RQ_DISP_INTS`／`VR_Blower_Req`）**無任何頭枕相關**；兩份 DBC 全域僅 `Driver_Headrest_Req`／`Passenger_Headrest_Req`，**無第三排**。配對 **DR-14′** |
| **A-VS29** | **C3 缺陷之第四次現身 —— 發生於宣稱修好它的同一輪** | `scripts/lid_parse.py` 已修（錨點 4/4），惟寫出 `data/lid_pairs.tsv` 時對 `Format` 欄做 `" ".join(r[fc].split())`，**再度壓平換行**，使 `parse_format_cell()` 輸入有損 | **新開（W-23(b)）**。**由判準 C3 自動捕獲，計 6 筆** —— 判準化之價值在此得證：若無 C3 判準，該 6 筆會混入「待判 16」而看不出是回歸。歷次面貌：W-8 值域切分／W-15b′ 交叉配／A-VS26 單格多值／本次中介檔寫出端，**每次下移一層**。**本輪未修**（R-VS25 三項上限已滿），排 06 輪頭部 <br>**補記（18 包 §1 之 W-27(a)′）**：錨點 (a) 之判準更正為**條目數 = 11**；鍵值對 10 為附帶量。原措辭「11 個鍵值」把條目與鍵值對混為一談 —— `4 bit signal` 為位元寬宣告，不含 `=`，本就不是鍵值對。 |
| **A-VS30** | **in-scope 之 CFTS044 與 in-scope 之 LID／DBC 對請求訊號之值域與 bit 寬相衝突** | CFTS044 `4858356`／`4858386`（`[EE Architecture:Atlantis High]`）載循環降階四值；基線 DBC 之 `FL_HS_Tlm`／`FR_HS_Tlm`／`FL_VS_Tlm`／`HSW_Tlm` **皆 1 bit**，`VAL_` 為 `Not_Pressed`／`Pressed`；LID 之 Atlantis High 欄組亦對映至該等 1 bit 訊號 | **新開（17 包 §2）**。⚠ **兩造皆 in-scope，R-VS19 之架構差異不適用** ——四階之 `Pressed/Not Pressed` 對照見於 `4857991` 等 CUSW 條文，依 R-VS19 本就不取用。**配對 DR-15（High）**；阻塞 Heated Seat 88 ＋ Vented Seat 72 = **160 leaf** 之分支結構與設計方法 |
| **A-VS31** | **LID 表之 Fail 值寫法在兩列間不一致** | `VentedSeatFL` 之 Format 逐字為 `1= Fail Present`（**`=` 前缺空格、`Fail_Present` 之底線遺漏**）；`HeatedSeatFL` 之對應行為 `1 = Fail_Present`。二者同指一值 | **新開（19 包 D-3）**。⚠ **足以使字面比對誤判為不同值** —— `Fail Present` 與 `Fail_Present` 於未正規化底線之比對下為兩個字串；本 feature 之值比對已對 `_` 做正規化故未受害，**但任何逐字引用該欄之下游會受害**。RD-1 FYI 類 |
| **A-VS32** | **CFTS044 以 `{CFTS043}` 顯式引用 Comfort 之三處，其主題與重疊最多之 43 條無交集** | 三處分別為 Climate 捷徑類別、`I am cold` 例程、`I am hot` 例程（皆設 `$HVACJumpVAL$`）；而 06 輪之 43 條重疊全為座椅加熱／通風／方向盤加熱 | **新開（20 包 §4）**。即**重疊最多者，CFTS044 反而未顯式引用 Comfort** ——徵候為**分層可能是隱含而非文件明示**。FYI 類，**不排作業**：R-VS7 已裁定，且其佐證不只此三處（另有 CFTS044 之 16 處 `Refer to TLM HMI Document`）|
| ~~A-VS33~~ | ~~R-VS7(a) 與 R-VS7(b) 對同一 leaf 皆適用而條文未定優先序~~ | 3 筆：`HeatedSteeringWheelManagement-026`（引 PDO graphics）／`-031`／`-035`（引 TLM HMI Document）；其 Layer 3 同時落在 06 輪之委派對應內 | **開立於 09 輪，關閉於 10 輪（26 包 §1）** —— R-VS7 原文已定其序：**(b) 之首字為「例外」**，例外條款之定義即於其適用範圍內排除主規則，故 **(b) 優先於 (a)**。實質理由：委派之前提為「該行為由 Comfort 擁有」，而該 3 筆之 CFTS044 條文**明文將該行為指給 TLM HMI Document／PDO graphics** —— 規格作者已宣告其歸屬，而該歸屬不是 Comfort。⚠ **執行層漏看該二字，把例外條款讀成平行條文。** |
| **A-VS34** | **06 輪之 Layer 3 清單漏列五個「階數型」Layer 3，致單向委派表漏 84 個應委派之 leaf** | 漏列者：`OneStageHeatedSeat`（12）／`TwoStagesHeatedSeat`（16）／`ThreeStagesHeatedSeat`（20）／`TwoStagesVentedSeatsManagement`（16）／`ThreeStagesVentedSeatsManagement`（20），合計 **84** | **新開（09 輪 W-29）**。成因：06 輪之七條正則映到之 Layer 3 清單只列 6 個，其側別判定分支以 `LeftFront`／`RightFront` 為條件，**階數型之名稱不含側別詞故接不到**。⚠ **canon §5a 條 7「假陰性源自詞彙不全」** —— 而 06 輪之報告當時寫「43/43 全數具名，無具名 0%」，**那個 0% 是對一個由不全清單選出之母體說的**。處置（26 包 §2）：06 輪單向表降為證據並加 SUPERSEDED 標記；`delegation_lookup.tsv` 為委派之唯一來源 |
| **A-VS35** | **W-32(d) 之交叉驗證兩側皆以 7 位數 id 為鍵** | 其驗的是「該 id 之章節歸屬於兩條路徑上一致」，**非「該 id 於 SYS2 與 docx 指向同一條文」** | **新開（26 包 §3）**。後者之獨立驗證須另一份**載有條文正文之匯出**，現無此素材。⚠ **具名為已知界線，非缺口** —— 「未做」與「做不了」在上繳包上須可分辨 |
| **A-VS36** | **03／04 上繳之獨立判斷節未回填 09 輪之發現** | 其所列未驗項與 W-29／W-30 之結果有重疊 | **新開（26 包 §3）**。**依裁定不回填** —— 上繳包為**當輪之快照**，回填會使其失去時序意義；改於 `docs/INDEX.md` 註明「其未驗項已由 09 輪之 W-29／W-30 涵蓋」 |
| **A-VS37** | **`TRUNCATED_ENUM` 之現行偵測涵蓋不足** | 現行判準「`# = Not Used` 結尾」命中 **13**；而**鍵序有缺口者 102**（列舉之數字鍵不連續）；另 `reserved` 38、`TBD/TBC` 1 | **新開（10 輪 W-17(2)）**。截斷之主要形態為**跳號**而非尾綴標記，現行旗標低估約 8 倍。⚠ **102 為上界非截斷數** —— 跳號亦可能是規格本就不使用該值。**未改判準**（屬新作業）|
| **A-VS38** | **同一值同時出現於 include 與 exclude** | `$VC_VEH_LINE$` 之 `WS`／`M240`／`M182/M189`；`$PowerMode$` 之 `IGN_RUN`／`Ignition run` | **新開（10 輪 W-33）**。**非矛盾** —— 出自不同條文，一條 `= X` 一條 `!= X`，適用情境不同。**惟以「該值是否在值域內」為判準之查詢會得到「兩者皆是」**；值域查詢須連同極性與其條文一起看。影響 framework 之分支設計 |
| **A-VS39** | **27 包之 in-scope 判準漏 `ECU` 與 `Radio` 兩屬性** | 補入後：已覆蓋 130 筆之「ECU 含 LTM/ETM/RRM 且 Radio 含 R1L/R1L-R」為 **130/130（100%）**；未覆蓋 39 筆中僅 **6（15%）** 滿足 —— 其餘 33 之 ECU 為 `CCDMF`(20)／`CCDMR`(5)／`ETM` 單獨(3)，或 Radio 不含 R1L 系列 | **新開（11 輪 W-37）**。**33 / 39 之「缺口」為判準過寬所致，非資料缺**。⚠ **R-VS34 之形態，本次犯者為分析層** —— 以自訂 in-scope 形態掃描而未先確認「本專案之適用範圍由哪些屬性界定」。若該 39 筆不追因即送 RD-1，會浪費上游一輪 |
| **A-VS40** | **`$Cooled_Seats$` 之覆蓋查詢一度誤判為真遺漏** | 首查 `$Cooled_Seats$` 命中 0 leaf；改查 `Cooled` 任意形態得 `TwoStagesVentedSeatsManagement-037`／`ThreeStagesVentedSeatsManagement-060`，其描述寫 `PROXI parameter Cooled_Seats` 而非 `$var$` 形態 | **新開（11 輪 W-37）**。**R-VS34 之形態，本輪第二次，犯者為執行層**。若止於首查，`4858294` 會被報為 (a) 真遺漏而觸發升級條件 |

| **A-VS41** | **純交叉參照條文無機器判準** | `4859544`／`4859545` 判為「純交叉參照無可測內容」係人讀（11 輪 W-37）；未建立可機器判定之判準 | **新開（28 包 §4 登記，不排作業）**。風險為「已覆蓋側可能混有純參照條文」——惟已覆蓋側之條文皆有 leaf 引用，**即使為純參照亦已在母體內，不影響覆蓋結論** |
| **A-VS42** | **in-scope 判準漏 `EE Architecture = Atlantis Mid`** | 251 個已覆蓋 reqid 中 **121 筆**落在 in-scope 之外，逐筆查其失敗條件**全數為 `EE Architecture`**，其值**全數為 `Atlantis Mid`**（`Artifact Type` 全數合格）。全 2,030 條之該欄值域為 `Atlantis High` 825／`PowerNet` 498／`CUSW` 388／`All` 354／**`Atlantis Mid` 247** | **新開（12 輪 W-39(2)）。⚠ 升級條件命中**。那 121 筆是 037 之 237 leaf **實際引用**之條文 —— **被覆蓋卻不在母體內**，判準與事實直接矛盾。連鎖：27 包之 1,128／998／169／39／76.9% 皆須重述；**11 輪之「(a) = 0，母體完整」不再安全**（敏感度觀測：加入 Mid 後未覆蓋為 8，較已讀之 6 多 2 筆從未檢視）。**判準之修訂屬分析層** |
| **A-VS43** | **階數之掃描形態只試 `stage`，未試 `Level`** | 08 包記「明示階數 1 / 27」，其判準為 `one／two／three stage(s)`。依 28 包放寬判準複核得 **5 / 17**：`SWE1-HVAC-054/055/067` 為 `Multi-Level Heated/Vented seats`、`-062` 為 `Multi-Level Heated steering wheel`、`-063` 為 `Single-Level heated steering wheel` | **新開（12 輪 W-40(2)）。⚠ 升級條件命中**。**R-VS34 形態第三次**（前為 `$HSW_StatFailSts$`、`$Cooled_Seats$`）。**後果最重**：08 包據「1 / 27」判「資料本身沒有可收斂之維度」，而資料其實有，只是寫成 `Multi-Level`。**W-34(1) 之 0/174 收斂失敗，其成因診斷可能整個是錯的**。且此維度正是 **DR-15 之維度**，其問法應否改寫屬分析層 |
| **A-VS44** | **Comfort 側母體數字互不一致 —— 口徑已裁定（29 包 §3）** | 「委派所引之相異 Comfort leaf」：08 包 §2.2 記 **27**、08 包另處記 **22**、本輪自 `delegation_lookup.tsv` 之 174 列 `delegate=yes` 實測 **17**。「Comfort leaf 全母體」：06 輪記 **498**、本輪自 037 `Analysis Report` 分頁實測相異 `SWE1-HVAC-` id **129** | **新開（12 輪 W-40(2)）；29 包 §3 已裁口徑，13 輪 D-2 標明**。爾後一律以：相異 `SWE1-HVAC-` id = **129**、`delegate=yes` 所引之相異 Comfort leaf = **17**。06 輪之 **498 為資料列數**（含同一 id 之多列）、08 包之 **27／22 為當時不同口徑之計數**，**二者皆降為證據**。凡引 Comfort 母體須標明口徑（「資料列」或「相異 id」）。分子 5 vs 1 與分母無關，A-VS43 之結論不動搖 |
| **A-VS45** | **分析層於 17／18 包引 DR-15 之條文 id 錯誤** | 17 包 §2.1 與 18 包 §2 稱 DR-15 之 CFTS044 證據為 `4858356`／`4858386`；實測該二條為**接收側**（`When the HU receives a $HeatedSeatFR$ = [HS_OFF] signal…`）。循環降階表之實際出處為四條：**`4858325`（`$FL_HS_RQ$`）／`4858355`（`$FR_HS_RQ$`）／`4858385`（`$FL_VS_RQ_TGW$`）／`4858416`（`$FR_VS_RQ_TGW$`）**，皆 `[EE Architecture:Atlantis High]` | **新開（29 包 §2.2 指定登記，13 輪 D-2）**。成因：以正則抓 `$FR_HS_RQ$ = [Medium]` 之命中位置，再取「前一個 7 位數」為其 reqid —— **取到前一個區塊的號（差 1），未以區塊邊界驗證歸屬**。DR-15 **尚未送出**，13 輪 D-3 已更正引用，無外部代價 |
| **A-VS46** | **Comfort 側無「單階座椅」條文，12 個 `OneStageHeatedSeat` leaf 之委派標的明文排除單階** | 174 列 `delegate=yes` 逐列對照本側 Layer 3 階數與 Comfort 側階數標記：**One → Multi 12 列**、Two → Multi 32、Three → Multi 40。該 12 列所引 `SWE1-HVAC-054`／`-067`，二者開頭逐字為 `For Multi-Level Heated/Vented seats`。Comfort 全母體 129 中唯一 `Single-Level` 者為 `-063`，其主詞為 `heated steering wheel`；以 `single[\s-]?level` 且含 `seats?` 交叉查詢**命中 0** | **新開（13 輪 W-43(2)）。⚠ 升級條件命中**。**非對映筆誤，為委派之實質缺口** —— 委派標的所述行為明文排除單階座椅。連帶：08 包「資料本身沒有可收斂之維度」須撤回（維度存在，Single↔One／Multi↔{Two,Three}，惟 Two 與 Three 不可分）。**配對之 RD 屬分析層，本層不代擬** |
| **A-VS47** | **`LeftFrontHeatedSeat-004`／`-011` 為四側共通需求卻掛在左側名下** | CFTS044 §1.3.2.1.3.1 與 §1.3.2.1.3.2 皆 29 條，逐位對照引用狀態，不一致者恰 2：`4858334`（`$Heated_Seat_Levels$`）與 `4858347`（Stop-Start），二者未被右側 leaf 引用，實由 `LeftFrontHeatedSeat-004`／`-011` 引用，且該二 leaf 之 `section` 逐字為 `1.3.2.1.3.1;1.3.2.1.3.2;1.3.2.1.3.3;1.3.2.1.3.4` | **新開（13 輪 W-41′）**。**非 037 遺漏，升級條件未命中** —— Layer 3 之 17 vs 15 差 2 即源於此。**對 framework 之後果**：該二 leaf 依實質應屬 `Common Features`，而 `framework.md` 以 SWE ID 中段 token 機械切分會放錯層。**未自行搬動**（framework 不鎖定，屬 Pei） |
| **A-VS48** | **W-42 之四屬性對照在條文層級去重，未查 leaf 內一致性** | 13 輪 W-42 之 `Radio`／`ECU`／`Market`／`Model Year` 對照以條文為單位去重後計數，**未查同一 leaf 之多條 reqid 其屬性是否一致**。`Model Year` 為唯一兩組有實質差異者（純 Mid 各年 7 vs 無 Mid 各年 33、另有 2019:30） | **新開（30 包 §4 D-3 指定登記，14 輪）**。該差異**未追因**。若某 leaf 之數條 reqid 分屬不同 `Model Year`，現行表看不出來 |
| **A-VS49** | **`4858393` 將通風座椅之值寫為加熱前綴 `HS_HI`** | CFTS044 §1.3.2.1.3.4 之 `4858393` 與 `4858394` 去方括號後同文，方括號值僅差 `[Vented Seat High / **HS_HI**]` vs `[Vented Seat High / **VS_HI**]`。LF 側對應條文（`4858363`／`4858367`／`4858368`）一律為 `VS_HI`。RF Vented 之 30 條 vs 其餘三節 29 條，差 1 即源於此（兩節正規化後相異條文皆 28，來源對稱） | **新開（14 輪 W-46(3)）**。**該錯值已進入 `data/spec_variables.tsv`**：`$VentedSeatFR$` 之 `cfts044_include` 含 `Vented Seat High / HS_HI`、`Vented Seat High/HS_HI`、`Vented Seat Off / HS_OFF`；`$VentedSeatFL$` 含 `Vented Seat Off / HS_OFF`。反向（`Heated` 配 `VS_`）掃描命中 0，污染單向，範圍 4 值／2 token。**不自行清除**（屬 TC 內容，且須先確認為 typo 而非別名）。**配對之 RD 屬分析層** |
| **A-VS50** | **R-VS37 未涵蓋「跨異層章節」與「無 reqid」二情形** | R-VS37 第二分支逐字為「跨越多個**同層**章節」。`HeatedSteeringWheelManagement-025`／`-026`／`-027` 之 `section` 為 `1.3.2.1.3;1.3.3.3.6.1` —— 四段 vs 五段，**不同層**；`HeatedSteeringWheel-009` **無 reqid**，R-VS37 未規定其歸屬 | **新開（14 輪 W-46(2)）**。W-46 之 6 筆不一致中，**僅 2 筆（`LeftFrontHeatedSeat-004`／`-011`）在 R-VS37 文義內**。本輪暫依「多章節即 Common Features」處理該 3 筆、無 reqid 者單列，**該處理無條文依據**，已於 `framework.md` 以 ⚠ 標明。**不代擬條文** —— 請補 R-VS37 之第三、四分支 |
| **A-VS51** | **`4858413` 之值退化為 `[ Pressed]`** | CFTS044 §1.3.2.1.3.4 之 `4858413` 逐字為 `$CCDMF_FR_VS_RQ$ = [ Pressed]`（含前導空白）；LF 同位條文 `4858382` 為 `$CCDMF_FL_VS_RQ$ = [Vented Seat Pressed / VS_PSD]`。**標籤 `Vented Seat` 與代碼 `VS_PSD` 皆遺失** | **新開（15 輪 W-48）。⚠ 升級條件命中** —— 其不對稱型與 Heated 兩節（引用歸屬，A-VS47）**不同**：Vented 兩節之引用狀態不一致為 **0**，不對稱在**值**。影響：`$CCDMF_FR_VS_RQ$` 之第一階值域少一值、多一個字面為 ` Pressed` 之值。**併入 DR-18** |
| **A-VS52** | **值域之大小寫雙寫致同一值重複計數** | CFTS044 對同值有多種大小寫並存：`[Vented Seat Low / VS_LO]`(5) ／ `[Vented Seat Low / VS_Lo]`(4)；`[Vented Seat Medium / VS_MED]`(2) ／ `[Vented seat Medium / VS_MED]`(2) ／ `[Vented seat medium / VS_MED]`(4)；`[Not Pressed]` ／ `[Not pressed]`。致 `$VentedSeatFR$` 之 `cfts044_include` 同一值算成五個 | **新開（15 輪 W-48）**。後果為**值域基數虛高，直接影響 TC 之列舉分支數**。**不自行合併**（屬 TC 內容）。**其餘 29 個 token 之同型重複未掃** |
| **A-VS53** ⟷ **A-VS05**（32 包 §2：**併入 A-VS05**，兩者皆保留，R-TM13） | **`$Heated_Steats_Levels$` token 本身為 typo** | `spec_variables.tsv` 之 30 個 token 中並存 `$Heated_Seats_Levels$` 與 `$Heated_Steats_Levels$`（`Steats`） | **新開（15 輪 W-47）；16 輪依 32 包 §2 併入 A-VS05**（同一 typo，00B §5-5 已登記）。其正規化鍵依 **R-VS39** 與 `$Heated_Seats_Levels$` 相同。**不自行合併或改名**（原值為 CFTS044 逐字，改之即失去可追溯性）。**併入 DR-18** |
| **A-VS54**（**已關閉**，16 輪依 R-VS38） | **31 包 §5 之 typo／別名判據不足以分辨** | 該判據逐字為「對稱側一律用另一前綴者判 typo；對稱側亦用同前綴者判別名」。`4860015`(LF)／`4860021`(RF) **兩側一致地寫 `HS_OFF`**，依該判據應判別名；但同章節內 `4860011`／`4860013`／`4860017`／`4860019` 皆寫 `VS_OFF`，全文頻次 `VS_OFF` 15 次 vs `HS_OFF` 2 次 | **新開（15 輪 W-47(3)）**。**該判據無法分辨「兩側一致地抄錯」與「真雙軌命名」**。本輪改以「章節內並存 ＋ 全文頻次」為基礎判 typo。**已關閉（16 輪 D-3）**：32 包 §1 之 **R-VS38** 將該三項（對稱側／章節內並存／全文頻次）定為聯合判據，並明訂「別名須為系統性雙軌命名，不得由少數例外構成」。原判據之缺陷已具名 —— **左右兩節係複製後修改，複製錯誤必然對稱** |
| **A-VS55** | **R-VS19′ 之主文與 (a) 段互斥** | 主文逐字為「`EE Architecture` 降為輔助資訊，**不作為排除判準**」；(a) 段逐字為「縮限為 `CUSW`／`PowerNet` **專屬者**（即 `Radio`／`ECU` 判準不通過者）」。**括號內之等式在資料上不成立**：全 2,030 條中「他架構專屬且通過 `Radio`／`ECU` 判準」者 **270 條**。兩讀之差：in-scope **425 vs 695**、21 節內未覆蓋 **8 vs 51**、**(a) 類 0 vs 43 未歸因** | **新開（16 輪 D-5）。⚠ 升級條件命中（需要判斷而無條文）**。新增之 43 條**全數為 `PowerNet` 專屬**，內容為實質可測需求（`If $EngineRPM$ < …greyed-out`、`Valid values for the $HeatedSeatFL$…`、`grey-out the Third Row Headrest Dump Softkey`），非適用性宣告、非交叉參照。**可供釋義之實測**：251 個已覆蓋條文中，`CUSW`／`PowerNet` 專屬者 **0** —— 037 從未引用任何純他架構條文。**本層不裁定採哪一讀**（R-VS19′ 為 Pei 所裁）。`framework.md` 列為阻塞項第 1 |
| **A-VS56** | **值域大小寫重複影響 12 / 30 個 token** | 15 輪 §6-3 只量過 `$VentedSeat*$`。全 30 token 重量：`$VentedSeatFR$` 23→18、`$VentedSeatFL$` 23→19、**`$ESS_ENG_ST$` 18→15**、**`$PowerMode$` 19→16**、`$FR_VS_RQ_TGW$`／`$HSW_RQ_TGW$`／`$HSW_Stat$` 各減 2、`$FL_HS_RQ$`／`$FL_VS_RQ_TGW$`／`$FR_HS_RQ$`／`$HdRstRelRq$`／`$PrplsnSysAtv$` 各減 1。全體 345 → 319 | **新開（16 輪 W-50(3)）。⚠ 升級條件命中**（逐字為「影響 `$VentedSeat*$` 以外之 token」）。`$ESS_ENG_ST$` 與 `$PowerMode$` **與座椅無關** —— 大小寫雜訊不限於座椅節族，**是 CFTS044 全文性質**。已依 **R-VS39** 增 `normalized_key` 欄處理，**原值一個未改**。**併入 DR-18 三** |
