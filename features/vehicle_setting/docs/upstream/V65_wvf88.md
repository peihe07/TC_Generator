# 上繳 V65 — W-VF88（收尾）；**VF230 之執行層工作結束**

**所據下放包**：`docs/handoff/V64_lid_accepted.md`　**日期**：2026-08-25

---

## 〇、工單各項之已辦／未辦（R-VF131）

| 項 | 事項 | 狀態 |
|---|---|---|
| W-VF88.1 | 2 鍵人讀（`CallAction`／`VC_HdRstPrsnt`） | **已辦，二者逐字相符** |
| W-VF88.2 | 交付說明二數複驗（460／8） | **已辦，二數皆成立；且更正本層一項誤讀** |
| W-VF88.3 | `lid_pairs_v178.tsv` 刪除 | **已辦** |
| W-VF88.4 | 備份檔於 Pei 二步後刪 | **未辦（如令）** —— 二步未執行，保留 |

**編號回報（R-VF102 一）**：實測最大 `R-VF135` → 二條實得 **`R-VF136`／`R-VF137`**，
與所令之 +1／+2 相符；**未逐字入者皆 0**。**現行已開而未落檔之條文：0。**

---

## 一、依 `R-VF136` 逐項列其名與射程（**不以「n 項全綠」概括**）

**自檢 20 項**（其射程為 `generated/` 之 438 列 × 各項所指之欄）：

```
 1 canon 判準（引 wvf62）      2 Send CAN 合 R-1 v2       3 訊號/msg/raw→label 與 DBC 相符
 4 ER「is sent」/proc「is transmitted」  5 proc↔ER 步數一一對應   6 Input Test Data 為 NA 或 R-VF91 佔位式
 7 priority 與判準模組相符      8 spec_reference 出自 R-VF68 錨鏈   9 可執行欄位無實作層名詞
10 writable 與 dr_dependent    11 seq 唯一且為連號        12 R-VF101 必要句（佔位式）
13 同家族 tc_title 不得逐字相同（**跨批**）  14 家族>1 之標題須含區辨 token（**跨批**）
15 句界切分未因無空格而漏刪管路句  16 test_item 具括號下半    17 上半 token ≤ 50
18 同上半之下半不得逐字相同（**同 leaf 內**）  19 括號下半含 " -> "  20 上半不得為排除清單之形態
```
**九批 ＋ pilot #3 皆 0 筆失敗；無法失效之項 0**（每項皆經破壞式實測其能失敗）。

**其射程外者已具名**：項 13／14／18 之跨批視野依 `data/vf230_batches.tsv`；
**`test_item` 之「節錄取捨是否恰當」為人讀之標的，機械判準不及於此**。

---

## 二、W-VF88.1：2 鍵人讀 —— 二者逐字相符

```
CallAction（CAN Mapping 列 207，scope=Atlantis High）
  原儲存格 Z  'TELEMATIC_VEHICLE_SETUP.CallAction'  → message/signal 正確拆分
  原儲存格 AA 'FDCAN8'                              → can 相符
  原儲存格 AB '0: No_Action\n1: Iniziate…'          → fmt 相符（逸出後）

VC_HdRstPrsnt（Proxi & Configuration 列 294，scope=Atlantis(&High)）
  原儲存格 P  'Headrest_Dump_Present'   → **無點號故 message 為空**，正確
  原儲存格 Q  空                        → can 為空，正確
  原儲存格 R  '0 = "Absent"\n1 = "Present"' → fmt 相符
```

**`R-VF137` 之「無對照物」自 2706 降為 **0**。**

---

## 三、W-VF88.2：460 與 8 皆複驗成立，**且更正本層一項誤讀**

```
035 Basic Report 之 Functional Requirement       **1087**
037 之 11 份所收（619 Functional ＋ 8 偏離）      **627**
未收                                              **460**（42.3%）✅

037 之 745 列   Heading **126** ／ Functional **619**  ✅
037 判 Heading 而 035 判 Functional：**8**  ✅（全數 SWITCH 族）
619 ＋ 8 = **627**，與 `leaves.tsv` 之 627 **逐一對上**（還原逸出後 627/627）
```

### 3.1 複驗過程中之二錯，皆自查自改

**其一：漏解 2 份 037。** 首版硬編分頁名 `Analysis Report`，
**而 11 份中有 2 份之分頁為 `Sheet1`** → 只解出 711 列（記載 745）、
Functional 590（記載 619）。**改為不限分頁名、以「表頭列首格 = `SWE-Requirement ID`」定位**後，
745／126／619 皆與記載相符。

**其二：本層先前之誤讀。** 上繳 V63 曾稱「037 之 leaf 為 627 列不是 745，與 R-VF12 之 745 不符」——
**實為 745 是 037 之總列數（含 heading 側 126），627 是 leaf 數，二者不矛盾。**
A-VS132／A-VS133 已載此分解，**本層未細讀其記載即報為不符。**

### 3.2 五筆「不在 037 內」為逸出之表示差異，非缺漏

`SWE1-VC-RearGuidanceLightswith\nCargoLights-014`～`-018` 五筆，
其 ID 含換行，於 `leaves.tsv` 序列化為字面二字元 `\n`。
**還原逸出後 627/627 全數對上。**

**交付說明之 §七已改寫**：二數自「未複驗」升為「已自來源複驗」，其具名已去。

---

## 四、W-VF88.3：中介檔已刪

`data/lid_pairs_v178.tsv` 與 `lid_pairs.tsv` 內容相同，**保留二份同內容之檔
即 `R-VF125` 所防之分岔形態**；其產生之痕已在 `scripts/vf230_wvf87_lid_parse.py`
與上繳 V63。**已刪。**

---

## 五、現況（**執行層之 VF230 工作至此結束**）

```
已寫入工作簿   438 列（B 欄 244–681），repo 內副本 169.71 KB
               四閘全過｜重讀比對 438 × 16 欄差異 0
交付路徑 036   **仍為 0 資料列** —— 其複製屬 Pei（R-VF112）
選池 470｜隔離 84｜去重 35（可還原）
DBC   forms/ 之 BHCAN2 R1 ＋ FDCAN8 R1（換本零影響，且修正一處既有污染）
LID   forms/ v1_78（分級 627 列逐列相異 0）
自檢  20 項全綠、無法失效之項 0（逐項名見 §一）
交付說明草稿  docs/reports/vf230_delivery_note_draft.md（**未送出**，R-VF27）
```

---

## 六、本包是否仍有該驗而未驗者（canon FO §8.2）

**有，三項。**

1. **Excel 中之下拉可用性與版面仍未驗** —— 屬 Pei 之第一步。
   **本層之驗證止於「x14 之 XML 合法」**，其與「Excel 中可用」為二事（`R-VF136` 之同型）。
2. **交付路徑之複製未執行** —— 屬 Pei 之第二步。**其誤覆蓋之風險已具名**
   （CFTS044 二本分岔 237／243）。
3. **438 列之 `test_item` 節錄取捨仍只人讀 25 條**（8 ＋ 17，5.7%）——
   **其為機械判準射程外之標的**，`R-VF108` 已裁不提高至 20%，
   **而其未讀之 413 條，其節錄是否恰當為未知。**
