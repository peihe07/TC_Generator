# 上繳 V63 — W-VF87（LID 換本、`.gitignore`、交付說明草擬）

**所據下放包**：`docs/handoff/V62_write_accepted.md`　**日期**：2026-08-25

---

## 〇、工單各項之已辦／未辦（R-VF131）

| 項 | 事項 | 狀態 |
|---|---|---|
| W-VF87.1 | LID 依 R-VF134 四步施行 | **已辦，分級零差異 → 工作簿不動** |
| W-VF87.2 | `.gitignore` 增備份之形態 | **已辦** |
| W-VF87.3 | 交付說明草擬 | **已辦**（`docs/reports/vf230_delivery_note_draft.md`，未送出） |
| W-VF87.4 | 備份檔於 Pei 二步後刪除 | **未辦（如令）** —— 二步未執行，本輪保留 |

**編號回報（R-VF102 一）**：實測最大 `R-VF133` → 二條實得 **`R-VF134`／`R-VF135`**，
與所令之 +1／+2 相符；**未逐字入者皆 0**。**現行已開而未落檔之條文：0。**

---

## 一、【阻斷已解】`lid_pairs.tsv` 之產生者亦不在 repo

`R-VF134` 二令「改 `lid_parse.py` 之來源路徑 → 重產 `lid_pairs.tsv`」。
**其前提不成立**：`lid_parse.py` **為解析函式庫 ＋ 自驗，本無來源路徑**；
且 **`grep` 全 repo，無任何腳本寫入 `lid_pairs.tsv`** ——
**其產生者不在 repo，與 `_dbc_parsed.json` 同型（第二例）。**

**已新寫驅動** `scripts/vf230_wvf87_lid_parse.py`，
**其解析核心逕用 `lid_parse.py` 之既有函式**（`parse_signal_cell`／
`parse_format_cell`），**不另寫解析**（R-VF74）。

---

## 二、自驗**五度不等**，逐次攔下一個不等價點

**其自驗為「以本驅動重建 v1_76，與現行 `lid_pairs.tsv` 比對；全等方採信」。**

| 次 | 差異 | 其成因 |
|---|---|---|
| 1 | 僅現行有 **550** | **讀錯欄組** —— 列 2 為欄組標籤（F `Powernet`／K `CUSW`／P `Atlantis`／U `Compact`／**Z `Atlantis High`**），而列 3 之欄名各組共用；取第一個 `Signal Name` 即讀到 `Powernet` |
| 2 | 僅現行有 **318** | **`Proxi & Configuration` 之欄組標籤不同** —— 其 P 欄組為 `Atlantis & Atlantis High`（該分頁無 `Atlantis High` 一組）。**逐字寫死之欄組名，其變體即漏** |
| 3 | 僅本驅動有 **3** | **回退之條件錯** —— 應為「`Signal Name` 儲存格為空」，而非「其解析結果為空」。列 282／832 之 Z 欄為 `N/A`／`Not Used`（**非空而解不出訊號**），原產生者視其為「已表態」而不回退 |
| 4 | `fmt` 欄相異 **1250** | **換行須逸出**（現行為字面二字元 `\\n`）。**且此次揭露自驗本身之缺陷**：其時只比鍵集而宣稱「全等」，**其射程不及於值**。已改為**鍵集 ＋ 逐欄** |
| 5 | `fmt` 欄相異 **134 → 4 → 0** | 前導換行不得 `strip()`（134）；Tab 須轉空白（4，其為 tsv 分隔符） |

**第 4 次為本輪最重要之一項** —— **一個宣稱「全等」之自驗，其本身之射程未經檢驗。**
若其時即據以產生 v1_78，**1250 筆 `fmt` 之差將無人察覺**。

**最終**：`2710 vs 2710`，**鍵集雙向差 0，八欄逐欄相異皆 0**。

---

## 三、v1_76 vs v1_78 之實質差異：**4 筆，觸及 VF230 者 0**

**以不含 `row` 之鍵比對**（v1_78 刪 2 列使其後列號全部上移，
以 `row` 為鍵會產生大量假差異）：

```
僅 v1_76  EngineRPM ／ ENGINE_FD_2.EngRPM
          EngineRPM ／ STATUS_CCAN5.EngineSpeed（重複列）
僅 v1_78  CallAction    ／ TELEMATIC_VEHICLE_SETUP.CallAction
          VC_HdRstPrsnt ／ Headrest_Dump_Present
同鍵而 `fmt` 相異：0
**其差異觸及 VF230 所用之 158 訊號者：0**
```

### 3.1 分級之複驗（R-VF134 三）：**627 列逐列，相異 0**

以新 tsv 重跑 `vf230_wvf44_writability.py`，與原 `writability.tsv` 逐列比對
**相異 0** → **依 R-VF134 四，工作簿不動。**

**其驗證後已還原原檔**（`git status` 對 `writability.tsv` 與 `lid_pairs.tsv` 無輸出），
**而後方施行換本**。

### 3.2 更正本層 V49 §五之一項過強宣稱

V49 §五稱「v1_78 **移除** `EngineRPM` 映射」。**實測**：
```
v1_76  EngineRPM 9 列   v1_78  EngineRPM 7 列
```
**移除者為 2 列**（`ENGINE_FD_2.EngRPM` 與一列重複之 `STATUS_CCAN5.EngineSpeed`），
**該 lid 仍在且其餘 7 列不變。**

**其連帶削弱本層當時所提之「反證據」** ——
V49 §五曾以「LID 移除 EngineRPM 而 DBC 增加 `EngRPM_Tach`，方向相反」
為 `R-VF120` 之反證。**該反證之前半不成立**（LID 未移除該 lid），
**故其力較本層當時所述更弱。**（`R-VF120` 已由 `R-VF127` 撤銷，本項不影響其結論。）

---

## 四、交付說明草擬（W-VF87.3）

`docs/reports/vf230_delivery_note_draft.md`，**草稿，未送出**（R-VF27）。其含：

- 交付之數（438 列／B 244–681）與隔離 84 之逐類具名
- **三類須知悉之標記**：佔位式 85（其值非條文所指定）／具名退化 23／`Remarks` 65
- **⚠ B 欄自動編號機制已失效**（237 格公式覆寫，`R-VF132` 之代價，**已入交付說明**）
- 值域來源之版本（DBC 換本之零影響 ＋ 其一併修正之 `LanguageSelection` 污染；LID 換本之零差異）
- 已知而未解者 4 項（`90ec`／35 對去重／`Greeting_Light`／x14 之可用性未驗）

---

## 五、本包是否仍有該驗而未驗者（canon FO §8.2）

**有，四項。**

1. **交付說明之 `R-VF12`（460 條揭露）與 `R-VF16`（8 列偏離）未複驗** ——
   其原始數據為前輪之產物，**草稿中已具名其未複驗**。
2. **Excel 中之下拉可用性、版面（欄寬／換行／對齊）仍未驗** —— 屬 Pei 之二步。
3. **`lid_pairs_v178.tsv` 為中介產物，其是否應保留未定** ——
   現行 `lid_pairs.tsv` 已為 v1_78 之內容，**二檔內容相同**；
   **本層保留之以留其產生之痕，具名待裁。**
4. **本驅動之自驗只驗「重建 v1_76 = 現行檔」，未驗「其對 v1_78 之解析亦正確」** ——
   **後者無對照物可比**（v1_78 之 tsv 從未存在）。
   **其正確性繫於「同一驅動對二本行為一致」之推定**，該推定未經獨立驗證。
