# 上繳 34 —— 母體層冗餘掃描、R-VS57(4) 重跑、產能終局盤點

執行層寫入。依據：`docs/handoff/61_review_round37.md` §5（38 輪指令）。canon §8.2 六節。

> **包號衝突之處置**：`docs/handoff/` 內有兩份 NN=61 之下放包
> （`61_review_round37.md` 與 `61_vf230_intake.md`），且其 W 編號、DR 編號、
> 上繳編號三處互撞。**Pei 裁定 2026-08-23**：
> 38 輪以 **`61_review_round37.md`** 為準；VF230 包 §7 之 DR-27 草稿**改編為 DR-28**。
> VF230 之素材尚未補入 `inputs/`（R-VS61 由 Pei 執行），該線本輪未動。詳見 §2.5。

| 項 | 內容 | 狀態 |
|---|---|---|
| D-1 | 依 R-VS18 先建本檔（六節先留空） | ✅ |
| D-2 | 轉錄 R-VS50′／R-VS57(4)；60 包 §4 追因段標作廢 | ✅ |
| D-3 | DR-25′ 影響段改為四訊號／61 leaf | ✅ |
| D-4 | batch14 FR 側四條 reasoning 改記依據；A-VS116／120 標處置 | ✅ `batch14_v2` |
| D-6 | 骨架 ⬜／✅ 對照 | ✅ |
| **W-107** | 母體層之冗餘掃描（237 leaf） | ✅ **34 組／72 leaf** —— 升級條件命中 |
| **W-108** | 依 R-VS57(4) 重跑分級 ＋ batch15 | ✅ 二錨點全過；batch15 **13 條** |
| **W-109** | 產能終局之盤點 | ✅ **池見底（餘 1）** |

**交付累計 109 條**（96 ＋ batch15 13）。**池餘 1。**

---

## 1. 預期 vs 實測（相符者亦列出）

| # | 預期 | 實測 | 判 |
|---|---|---|---|
| 1 | W-107(2)：列出可測內容無法分辨之組數與 leaf id | **34 組／72 leaf** | 相符 |
| 2 | 升級門檻：W-107(2) 之組數 > 10 | **34** | **升級條件命中** |
| 3 | W-107(3)：已交付／未交付分列 | 全已交付 18 組／全未交付 15 組／部分 1 組 | 相符 |
| 4 | W-107 必列：其對「237 母體」之意涵 | 237 leaf → **199 個相異驗證點**（198 相異鍵 ＋ 無條文 1） | 相符 |
| 5 | W-108(1) 錨點：`HSW_Cmd_Tlm` 之 4 leaf 須轉 W2／B6 | **4/4 命中**，`blocker_class = B6-value-absent` | PASS，可失敗 |
| 6 | W-108(1) 錨點：`FL_HS_Cmd_Tlm` 之 17 leaf 須維持 WARN | **17/17 維持**，不符 0 | PASS |
| 7 | 61 包 §4 之預期：`dr_dependent` 自 65 扣為 **61** | **61** | 相符 |
| 8 | W-108(2)：W0／W1／W2 與 **126/2/109** 之對照 | **126／2／109** —— 逐 leaf 差 **0** | 相符 |
| 9 | W-108(3)：batch15，池不足則取全部並回報 | 池 **14**，扣 A-VS112 之 1 → **全取 13** | 相符 |
| 10 | 升級門檻：W-108(3) 之池為 0 | 本輪為 14（**本批後為 1**） | 升級**未命中** |
| 11 | batch15：預判與定稿後之判定一致 | **13/13 一致**（全 P1；本池預判 **P0 = 0**） | 相符 |
| 12 | §9 十七項自檢 ＋ 錨點 | 十五批**新版 0 項／錨點 166 項** | 相符 |
| 13 | 唯一性掃描（承 37 輪） | 錨點 2 組必命中**全命中**；錨點以外重複組 **0** | 相符 |
| 14 | W-109：逐 DR 之解鎖量，降冪 | 已列（§3.2）。**最大解鎖量之 DR 為 DR-25′（23）** | 相符 |
| 15 | 升級門檻：W-109 之最大解鎖量 DR 非 DR-5-B／DR-18／DR-25′ 三者之一 | **依字面命中**（最大者為非 DR 之「未解值位於後件」24）；**依「DR」字面則未命中**（DR-25′ 23 為最大之 DR） | **兩讀並列，不自行擇一** → §2.4 |
| 16 | R-VS35 兩數 | 新增 2／登記簿 122；DR 新增 0／登記簿 19 | 相符，差額 0 |

## 2. 不符項目（不自行調和）

### 2.1 W-107 之正規化式兩處失準，同輪內三度改判

| 版 | 處置 | 組數 | 涉及 leaf |
|---|---|---:|---:|
| 初版 | 去全部方括號 ＋ 只去前導節號 | 36 | 77 |
| 修正 (a) | 另去**區塊尾端吸入之下一節標題** | 40 | 89 |
| **定版 (b)** | 方括號**不去**，只去值內 raw 碼前綴（`[1h: On]` → `[On]`） | **34** | **72** |

**(a) 之成因**：`blocks_with_sec()` 以區塊 id 切割，節標題落在前一區塊之尾 ——
`4858545` 吸入 `1.3.2.1.3.12 Stop-Start System Feature … {4858546}`，
致其不與 `4858539` 成組。**每節之最後一個 leaf 恆受此害。**

**(b) 之成因**：值之方括號（`[Off]`／`[High]`）**是可測內容**，去之則
`LeftFrontVentedSeat-015`／`-016`／`-017` 被誤判為同組。

**其被抓到的方式須記**：TC 層之唯一性掃描（37 輪 W-105）已知 `-016`／`-022` 為一組，
而母體層初版**未命中該組** —— **兩個掃描對同一事實不一致，即其中之一失準**。
此為 R-VS54 之錨點精神在「兩個獨立掃描互為錨點」形態下之應用。→ **A-VS122**

**三個數字皆在同一輪內產生，僅定版 34 入本包。**

### 2.2 34 組中，真正「無可分辨之軸」者只有 3 組

| 類 | 組數 | leaf | 判 |
|---|---:|---:|---|
| 組內 **Layer 3 相異**（階數／族群配置可分辨） | **31** | 66 | 已以 `pre_conditions` 之階數配置分辨（batch13_v2／14／15 皆如此） |
| 組內 **Layer 3 相同**（**無任何可分辨之軸**） | **3** | 6 | A-VS119 之二組 ＋ **`SwitchLHD/RHD-010`／`-013`（新）** |

**「34 組」不可直接讀為「34 組不可分辨之 TC」** —— 其 31 組之分辨資訊
存在於**節號與 Layer 3**，只是不在條文正文內，而正規化式依指令去除了節號。

第三組 `-010`／`-013` **二者皆未交付**，是首個尚可在生成前處置者。→ **A-VS123**

### 2.3 DR-5-B 之可解鎖數為 0 —— 與 61 包 §1 之推論不符

61 包 §1 逐字：「P0(b) 之失效半邊要有 TC，**須先解 DR-5-B**」。

W-109 實測：**DR-5-B 之標的 5 個未交付 leaf，其可解鎖數為 0** ——
該 5 者除 DR-5-B 外另有阻塞因子，單解 DR-5-B 不足以使其成為 `generatable`。

且含 `Fail_Present` 之 16 leaf 中，**只有 5 個標 `blocked_ref = DR-5-B`**，
其餘 11 個為 `delegate = blocked` 而無 `blocked_ref`（即委派予 Comfort 而無 DR 承載）。

**即：P0(b) 之失效半邊，其阻塞主體是委派本身，不是 DR-5-B。**
解 DR-5-B 亦不足以使該 16 leaf 回到池內。**本層未改 `delegate`。**

### 2.4 升級條件之「最大解鎖量 DR」有兩讀

| 讀法 | 最大者 | 是否三者之一 | 升級 |
|---|---|---|---|
| 「最大解鎖量」（不限 DR） | **（無 DR：未解值位於後件）24** | 否 | **命中** |
| 「最大解鎖量**之 DR**」 | **DR-25′ 23** | 是 | 未命中 |

**本層不自行擇一。** 併此一併回報：**`DR-18` 之標的數為 0** ——
其於升級條件中被列為三個預期之一，而實測其未阻塞任何未交付 leaf
（`FR_VS_Cmd_Tlm` 之 14 leaf 係因值域未解而 W2，`conflict()` 未將其歸於 DR-18）。

### 2.5 VF230 線本輪未動 —— 素材未補入

`61_vf230_intake.md` 之 W-102 及其下游需 `features/vehicle_setting/inputs/` 內有 VF230 素材；
實測 **vf230 相關檔 0 份**。R-VS61 明訂補入由 Pei 執行。
來源根目錄 `VF230_V1_R5/` 可讀，11 份 037 分報告在位。**本層未搬檔。**

## 3. 結果三分法（canon §8.4）

### 3.1 已驗且相符

- W-108(1) 二錨點全過且皆可失敗；`dr_dependent` 65 → **61**，與 61 包 §4 逐字相符
- W-108(2) 分級 **126/2/109**，與 61 包所載對照數逐 leaf 差 **0**
- batch15 之預判／定稿一致性 **13/13**；§9 自檢十五批 **0 項**、錨點 **166 項**
- 唯一性掃描：錨點 2 組全命中、錨點以外 **0**
- 母體 237 leaf → **199 個相異驗證點**（本 feature 首次量得此數）

### 3.2 W-109 —— 逐 DR 之解鎖量（降冪）

**已交付 109／母體 237／未交付 128。**

| DR／阻塞因子 | 標的數（未交付） | **可解鎖數** |
|---|---:|---:|
| （無 DR：未解值位於後件 —— 驗證目標即該值） | 25 | **24** |
| **DR-25′** | 27 | **23** |
| （無 DR：前件無已解條件，扣 PENDING 後不足 2 步） | 16 | **16** |
| **DR-15** | 13 | **13** |
| **DR-19** | 7 | **7** |
| （無 DR：`HSW_Cmd_Tlm` 值域無來源 —— B6） | 4 | **4** |
| **DR-8** | 2 | **2** |
| **DR-21** | 2 | **2** |
| （無 DR：未載） | 1 | **1** |
| **DR-22** | 1 | **1** |
| （無 DR：唯一來源條文為適用性前言 —— B4） | 6 | **0** |
| **DR-5-B** | 5 | **0** |

**送 DR 之優先序（依可解鎖數）：DR-25′ 23 → DR-15 13 → DR-19 7 → DR-8 2 ＝ DR-21 2 → DR-22 1。**
**DR-5-B 0、DR-18 未列（標的 0）、DR-26／DR-27 未列（其不阻塞可生成性）。**

**兩個最大項不是 DR**：「未解值位於後件」24 與「前件無已解條件」16 ——
**合計 40 leaf 之阻塞為分級判準本身**（R-VS47），無 DR 可送。
依 R-VS50′，此處分列兩數：**有 128 個未交付標的，其中僅 1 個為可作業標的。**

### 3.3 未驗

- **batch15 之 13 條未過 pilot review。** pilot #3 之 13 條（60 包 §5）為 batch13 ＋ 3；
  batch14（10）與 batch15（13）**共 23 條同形態而不在 pilot 範圍內**。
- **「未解值位於後件」之 25 leaf 未逐一追其未解值為何。** 其為最大解鎖項而無 DR 承載，
  **本輪未做逐 leaf 之未解值清單** —— 指令未令，但該表若無，這 24 條解不開。
- VF230 線全未動（§2.5）。
- §9 十七項中不可機械化之七項本輪未逐條人讀。

## 4. 本輪實際使用之掃描條件（canon §5a 條 1／2／4／5）

| 項 | 條件 |
|---|---|
| 母體 | `generatable.tsv` 全 **237** 列；無條文可取者 **1**（A-VS12 之 `SYS-RA-CFTS100`） |
| 條文之正規化 | 捨去區塊第一行（id ＋ 屬性段）→ 去前導 `\d{7}:` → 去前導節號 → 空白壓縮 → **反覆去尾端之下一節標題**（`\d+(\.\d+)+ … \{\d{7}\}$`）→ **值內 raw 碼前綴 `[Nh: X]` → `[X]`** → casefold → 數詞→數字（zero–ten） |
| **不**做之正規化 | **不去值之方括號**（`[Off]`／`[High]` 為可測內容）；不去 `$var$`；不做 typo 前綴修正（R-VS38 之表未涵蓋本用途） |
| 多 reqid 之 leaf | 其鍵為各 reqid 之正規化文以 `\n@@\n` 串接（順序依 `reqid_list`） |
| 已交付之判定 | `generated/batch*.json` 各 batch 之最新版之 `leaf_id` 聯集，得 **109** |
| L-VS2 之值域來源（R-VS57(4)） | DBC `VAL_` ∪ LID `Atlantis` 欄組 ∪ LID `Atlantis High` 欄組。**條文內嵌值不計** —— 見 §2 之註：其只給標籤不給 raw 碼，無從寫成 R-VS52 之 `= <raw> (<label>)`；依據為 61 包 §4 之實例本身（`HSW_Cmd_Tlm` 之內嵌值為 `"ON"` 而該條裁其為 B6） |
| 解鎖量之判準 | 標的數 = 該 DR 所涉之未交付 leaf；**可解鎖數 = 該 DR 為其唯一阻塞因子者**（`delegate` 非 blocked/pending、非 B4、無其他 DR、無 B6） |
| batch15 之參數 | **自條文解析**（`parse()`），不手寫：`IF ($<tok>$ (==\|passes to) "<階>"` ＋ `set <MSG>.<Cmd> = "<階>"` |
| 錨點（R-VS54） | §9：各批改寫前版本，batch15 為首版故用刻意違規樣本；W-108(1)：`HSW_Cmd_Tlm` 必命中 B6／`FL_HS_Cmd_Tlm` 必維持 WARN；唯一性：二組必命中 |

## 5. 新開 anomaly 與 DATA_REQUESTS（成對）

| anomaly | 內容 | 成對之 DR |
|---|---|---|
| **A-VS122** | W-107 之正規化式兩處失準，同輪內三度改判 36 → 40 → 34 | 無（方法層，未進產物） |
| **A-VS123** | `SwitchLHD/RHD-010`／`-013` 可測內容無法分辨，且**二者皆未交付** | **DR-27**（已在簿；**其形態需另問，本層具名而不逕行併入**） |

**R-VS35 之兩數**：**本輪（W-107／W-108／W-109）新增 2 筆**（A-VS122／A-VS123）。

**登記簿之核對於上繳前重跑，得 127 個相異 id —— 與本輪之 122 不符，差 5。**
逐筆查其成因：**A-VS124 ～ A-VS128 為並行之 VF230 線所寫入**
（`61_vf230_intake.md` 之工單，另一 session 執行中），**非本輪產出**。
同批寫入者另有 **R-VS59 ～ R-VS63**（`RULINGS.md`）與 **DR-28**（`DATA_REQUESTS.md`）。

依 R-VS35「上繳包之表為當輪之陳述，登記簿為跨輪之登記簿，二者不得互相替代」，
**本層不將該 5 筆計入本輪之新增數**，亦不追改其內容。

  本輪新增 **2**（A-VS122／A-VS123）
  並行 VF230 線新增 **5**（A-VS124 ～ A-VS128）
  登記簿現有 **127** 個相異 id —— 2 ＋ 5 ＋ 前輪之 120 = 127，**差額 0**

DR：本輪新增 **0**；並行 VF230 線新增 **1**（DR-28，即 Pei 裁定改編之 VF230 缺 SYS2 一件）；
登記簿現有 **20 個相異 id**（19 ＋ DR-28）。差額 0。

**列此以免「登記簿多出 5 筆」被讀為本包漏登。**

**已標處置者**：A-VS116（61 包 §1 追認；**惟 DR-5-B 之可解鎖數實測為 0**）／
A-VS118（W-108(1) 依 R-VS57(4) 判 B6，二錨點過）／A-VS119（母體規模已量：3 組／6 leaf）／
A-VS120（61 包 §3 追認）。

## 6. 獨立判斷：本包是否仍有該驗而未驗者

**有，五項：**

1. **池見底了。** 未交付 128，其中**可作業者 1**（`FeaturesEnableCriteria-023`，
   而其實質受 DR-8 牽制 —— A-VS112）。**下一輪沒有 batch 可產。**
   這不是預測，是 W-109 之實測。

2. **最大的兩個阻塞項沒有 DR 可送。**「未解值位於後件」24 ＋「前件無已解條件」16
   ＝ **40 leaf，佔可解鎖總量之最大宗，而其阻塞是分級判準本身**。
   送再多 DR 也解不開這 40 條 —— 要解須回頭問「這些未解值究竟缺什麼」，
   **而該清單本輪未做**。建議次輪之唯一作業即為此。

3. **DR-5-B 解了也沒用（可解鎖 0）。** 61 包 §1 指其為 P0(b) 缺口之鑰匙，
   實測其 5 個標的另有阻塞，且 16 個 `Fail_Present` leaf 中 11 個根本無 DR 承載，
   只有 `delegate = blocked`。**委派本身才是主體。**

4. **23 條交付物（batch14 ＋ batch15）不在任何 pilot 範圍內。**
   pilot #3 之抽樣裁定（60 包 §5）作成時 batch14／15 尚不存在。

5. **A-VS123 是首個「可在生成前處置」的不可分辨組。** 前二組已交付、代價已付；
   `-010`／`-013` 尚未生成。**若 DR-27 之答覆晚於次輪之選池，該組會重蹈覆轍。**

**本輪無「該驗而漏驗」之作業項** —— 三項作業（W-107／W-108／W-109）皆執行且皆有結果。

---

### D-6 骨架對照

| 節 | 骨架要求 | 本包 |
|---|---|---|
| §1 | 預期 vs 實測，相符者亦列 | ✅ 16 列，相符 12／升級門檻 4 皆列 |
| §2 | 不符項目，不自行調和 | ✅ 五項（2.1–2.5）；§2.4 之兩讀並列未擇一 |
| §3 | 三分法 | ✅ 已驗相符／W-109 全表／未驗 三段俱全 |
| §4 | 掃描條件揭露 | ✅ 10 列，含正規化式逐步與「**不**做之正規化」 |
| §5 | 新開 anomaly 與 DR 成對 | ✅ 2 anomaly／0 新 DR，R-VS35 兩數已列 |
| §6 | 獨立判斷 | ✅ 五項，含「池見底」之實測結論 |
