# 上繳 41 —— 036 母本之實寫

執行層寫入。依據：`docs/handoff/75_writeback_go.md` §2（48 輪指令）
＋ `66_writeback_procedure.md`（貼回程序）。canon §8.2 六節。

**三道 gate 全過**：G1 dry-run（47 輪）／G2 pilot（Pei 2026-08-23）／
G3 母本備份（47 輪，`ebe5a65f…5089`）。

| 項 | 內容 | 狀態 |
|---|---|---|
| D-1 | 依 R-VS18 先建本檔 | ⬜ |
| D-2 | 轉錄 R-VS68′／R-VS69／R-VS35 之補充 | ⬜ |
| D-3 | `delivery_disclosure.md` 依 R-VS69 重列（§A／§B-1／§B-2） | ⬜ |
| D-4 | A-VS151／152 標處置；R-VS35 分線列；骨架對照 | ⬜ |
| **W-136** | 實寫前之最終核對 | ⬜ |
| **W-137** | 實寫 | ⬜ |
| **W-138** | PDT24 兩檔之補列準備（**不執行複製**） | ⬜ |

---

## 1. 預期 vs 實測（相符者亦列出）

（本節於作業完成後填入）

## 2. 不符項目（不自行調和）

（本節於作業完成後填入）

## 3. 結果三分法（canon §8.4）

（本節於作業完成後填入）

## 4. 本輪實際使用之掃描條件（canon §5a 條 1／2／4／5）

（本節於作業完成後填入）

## 5. 新開 anomaly 與 DATA_REQUESTS（成對）

（本節於作業完成後填入）

## 6. 獨立判斷：本包是否仍有該驗而未驗者

（本節於作業完成後填入）

---

# 實測結果 —— **實寫已完成**

## 1. 預期 vs 實測（相符者亦列出）

| # | 預期 | 實測 | 判 |
|---|---|---|---|
| 1 | W-136(2)：實寫前母本 sha256 ＝ `ebe5a65f…5089` | **相等** | PASS |
| 2 | W-136(3)：固定錨點 20 項全數必命中 | **20/20**，被檢輸入 0 項 | PASS |
| 3 | W-136(1)：列數 ＝ leaf 聯集數 | **143 ＝ 143** | 相符 |
| 4 | W-136(1)：I 欄上下段結構 ＝ 列數 | **143／143**（含空行、括號收尾） | 相符 |
| 5 | W-136(1)：K 欄非 `NA` ＝ 0 | **0** | 相符 |
| 6 | W-136(1)：R 欄不在受控 9 值內 ＝ 0 | **0** | 相符 |
| 7 | W-136(1)：N 欄一 ObjectID 一行，無 `,` `;` | 1 行 **136**／2 行 **7**；含 `,;` 者 **0** | 相符 |
| 8 | W-136(1)：AA 欄 ＝ `PeiPYHsu` | 全表單一值 | 相符 |
| 9 | W-136(1)：AH 欄非空 ＝ §A＋§B-1＋§B-2 | **76 vs 32** | **不符** → §2.1（**Pei 裁定續行**） |
| 10 | W-137(1)：清空現有 237 列之 B–AH | **清空 237 列** | 相符 |
| 11 | W-137(2)：自列 10 起 append | **143 列，列 10–152**；B 欄 1–143 連續 | 相符 |
| 12 | W-137(3)：重讀逐列比對十六欄 | **不符 0 處** | PASS |
| 13 | W-137(4)：實寫後 sha256 | `6d3797d4b5c07b5fb29a47686feac4b86d122107a0ce49deb866c44e61bb911b` | 相符 |
| 14 | 禁區：列 1–9、其他分頁、表外之欄一格不動 | 列 9 表頭原樣；分頁序與備份相同；E／O／Q／S 四欄 **全 0 非空** | 相符（**惟見 §2.2**） |
| 15 | W-138：PDT24 兩檔之 sha256 與 R-VS68′ 相符 | **二檔皆相符** | PASS |
| 16 | W-138：71 輪補充證據之重量 | `*_Cmd_Tlm` 不存在 ✅／`SETUP2` 不存在 ✅／`*_Tlm` 為 1 bit **字面不成立** | **不符** → §2.3 |
| 17 | D-3：三節條數與合計，及與 `screen_pending = 10` 之差額 | §A **16**／§B-1 **0**／§B-2 **16**／合計 **32**；另 §C **44**；差額 **+6** | 相符（**含指令未列之 §C** → §2.1） |
| 18 | R-VS35 兩數（分線列） | 主線本輪新增 **1**（A-VS153）；登記簿 **152 個相異 id**，最大 **A-VS153** | 相符 |

## 2. 不符項目（不自行調和）

### 2.1 AH 欄非空 76 ≠ §A＋§B-1＋§B-2 之 32 —— 差額即 §C

D-3 依 R-VS69 重列後：

| 節 | 意涵 | 條數 |
|---|---|---:|
| §A | 單一缺值待補（非畫面層之 `BLOCKED`，如 DR-24′ 時限） | **16** |
| §B-1 | 畫面層**完全**未驗（`DR-5-B` ＋ ER 為 `PENDING`） | **0** |
| §B-2 | 畫面層**部分**未驗（已驗其變更、樣式／內容待補） | **16** |
| §A＋§B-1＋§B-2 | —— 48 輪指令之對照數 | **32** |
| **§C** | **實作缺口**（`IMPL_GAP`，R-VS66(a)／R-VS67′） | **44** |
| **AH 欄非空（聯集）** | | **76** |

**32 ＋ 44 ＝ 76，逐條可對。** §C 為 **47 輪 W-133 依 R-VS67′ 新增之註記**，
**而 48 輪指令之對照式寫於其之前** —— 其為 R-VS64 所指之「門檻之關係式過時」。

**本層未調和**，於實寫前停下並具名回報；**Pei 裁定續行**（2026-08-23）。
**建議之關係式**：`AH 欄非空 ＝ delivery_disclosure.md 之各節聯集`。

### 2.2 `x14` 擴充之 dataValidation 於存檔時被丟棄 —— **逾越禁區之連帶損失**

實寫後以 raw XML 比對備份：

| 項 | 備份 | 實寫後 |
|---|---:|---:|
| 一般 `dataValidation` | 4 | **4** |
| **`x14` 擴充 `dataValidation`** | **2** | **0** |
| 分頁序／drawing／chart | 同 | **同** |

所失者為 `Test Case Specification` 之 **`R10:R132` 下拉**，來源 `Reference!$C$4:$C$12`
（即受控 9 值）。其為 openpyxl 之已知限制（存檔時警告
`Data Validation extension is not supported and will be removed`）。

**資料本身不受影響**：R 欄 143 列全數落在受控 9 值內（W-136(1) 實測出域 0）。

**修復之嘗試已回退**：本層曾以 raw XML 注入還原該區塊，
因 `x14:`／`xm:` 之命名空間宣告一併被移除而**使檔案不可開啟**；
即依 **W-137(3) 自備份還原**（sha256 回 `ebe5a65f…5089`，237 列與 x14 皆完好），
**再重跑實寫**。**本層不再手改 XML。** → **A-VS153**

**現行母本仍缺該下拉。修復屬 Pei**：於 Excel 內對 `R10:R152` 重設資料驗證，
來源 `Reference!$C$4:$C$12` —— 原範圍僅至 R132，**本 feature 之 143 列已逾之**。

### 2.3 W-138 之第三項主張，字面不成立而實質相符

| 主張（71 輪） | 重量結果 |
|---|---|
| 四份 DBC 中 `*_Cmd_Tlm` 皆不存在 | ✅ 四檔命中皆 0 |
| 四份 DBC 中 `TELEMATIC_VEHICLE_SETUP2` 皆不存在 | ✅ 四檔命中皆 0 |
| `*_Tlm` 為 1 bit | **⚠ 字面不成立** —— 位元寬集合 [1,2,3] |

逾 1 bit 者為 `MassageIntensity_D/P_Tlm`（2 bit）與 `MassageType_D/P_Tlm`（3 bit），
**皆為按摩座椅之訊號，不在本 feature 範圍**。

**本 feature 之五個標的**（`FL_HS_Tlm`／`FR_HS_Tlm`／`FL_VS_Tlm`／`FR_VS_Tlm`／`HSW_Tlm`）
**於 PDT24 與 PDT27 兩代皆為 1 bit** —— **R-VS67′ 之前提成立**。

**本層不擇一讀法**（升級條件依字面命中、依實質未命中），兩讀並列。

## 3. 結果三分法（canon §8.4）

### 3.1 已驗且相符 —— **036 母本之實寫已完成**

| 項 | 值 |
|---|---|
| 母本 | `…/CFTS044/FM-WI-FSM-036-A01 …_20260819.xlsx` |
| 分頁 | `Test Case Specification 測試用例規範` |
| 清空 | 現有 **237** 列之 B–AH |
| 寫入 | **143** 列，列 **10–152**，B 欄 1–143 連續 |
| 重讀比對 | 十六欄逐列 **不符 0 處** |
| 實寫前 sha256 | `ebe5a65f30a0d4bcf9e46b51a43145ce222027ac49ad523fe5c2d2b6566a5089` |
| **實寫後 sha256** | **`6d3797d4b5c07b5fb29a47686feac4b86d122107a0ce49deb866c44e61bb911b`** |
| 備份 | `REF/036_pre_writeback_20260823.xlsx`（未動，仍為實寫前之本） |

其餘已驗：P 欄 P0 **24**／P1 **113**／P2 **6**；AH 非空 **76**；
AA 全表 `PeiPYHsu`；E／O／Q／S 四欄全空；固定錨點 20/20。

### 3.2 已驗而不符 —— 已於 §2 逐項說明，未調和

§2.1（AH 對照式過時）／§2.2（x14 下拉遺失，**逾越禁區之連帶損失**）／
§2.3（W-138 第三項主張之兩讀）。

### 3.3 未驗

- **x14 下拉之修復未做**（屬 Pei，§2.2 已列其指令）。
- **PDT24 兩檔未複製**（禁區；`pdt24_prep.md` 已備補列與指令）。
- **`impl_gap` 之 44 條未開 issue 予 RD**（R-VS66(a)，屬 Pei）。
- **DR-15′ 仍未送出**；47 條 `dr15_exposed` 之標記保留。
- **未抽樣之 28 條未經人工關卡**（75 包 §1 明言不因本次通過而視為已 review）。
- 44／45 輪之作業面（W-124～W-129）仍未執行。

## 4. 本輪實際使用之掃描條件（canon §5a 條 1／2／4／5）

| 項 | 條件 |
|---|---|
| 寫入之母體 | `generated/batch*.json` 各 batch 之**最新版**，依 batch 名升冪，得 **143** 列 |
| 清空之範圍 | 資料列（列 10 起）之 **B–AH**（欄 2–34）；**列 1–9、其他分頁、B–AH 以外之欄不觸** |
| AH 之來源 | **`tc["remarks"]`**（48 輪 D-3 改；42 輪以 `dr_dependent` 推導者為權宜） |
| `screen_pending` 之判準 | **R-VS69**：AH 載有畫面層之 `BLOCKED`（`DR-5-B`），**與 ER 是否 `PENDING` 無關** |
| 重讀比對 | `openpyxl` 以 `data_only=True` 重開，逐列比對十六欄之字串形式 |
| 母本雜湊之閘 | 實寫前必驗 ＝ `ebe5a65f…5089`，不等即 `SystemExit` |
| x14 之偵測 | 直接讀 `xl/worksheets/sheet*.xml` 之 raw XML，計 `<dataValidation` 與 `x14:dataValidation` |
| W-138 之重量 | 四份 DBC（PDT24 兩檔就地讀、PDT27 兩檔自 `inputs/`）以 `latin-1` 讀；`SG_ \w+_Tlm : \d+\|(\d+)@` 取位元寬 |

## 5. 新開 anomaly 與 DATA_REQUESTS（成對）

| anomaly | 內容 | 成對之 DR |
|---|---|---|
| **A-VS153** | openpyxl 存檔丟棄 `x14` 擴充之 dataValidation，R 欄受控下拉失效 | 無（工具限制；修復屬 Pei） |

**R-VS35 之兩數（依 R-VS63 分線列）**：
主線本輪新增 **1**（A-VS153）；登記簿現有 **152 個相異 id**，最大 **A-VS153**。
**已標處置者**：A-VS151（48 輪 W-136(3) 複驗，錨點 20/20）／
A-VS152（W-138 已備補列與指令，複製仍屬 Pei）。

DR：本輪新增 **0**。

## 6. 獨立判斷：本包是否仍有該驗而未驗者

**有，五項：**

1. **實寫完成，而母本已與備份不同。** 實寫後 sha256 `6d3797d4…`；
   **備份 `REF/036_pre_writeback_20260823.xlsx` 仍為實寫前之本**（`ebe5a65f…`）。
   **任何後續之改寫應以現行母本為基準另備份** —— 現有備份已不是「上一版」。

2. **§2.2 是本輪唯一一次逾越禁區之改動，且是我造成的。**
   禁區令「表外之欄一格不動」，x14 之遺失非儲存格而是驗證設定 ——
   **其非我所欲，但確為我之寫入所致**。更該記的是：
   我試圖修復時**把檔案寫壞了**，靠 W-137(3) 之還原路徑救回。
   **該路徑本輪首次被實際用到，且有效。**

3. **AH 之對照式過時（§2.1）是 R-VS64 之第四次實例**（前三為 129／21／「預期命中」）。
   R-VS64 立於 42 輪，**其回溯清單（43 輪 D-3 之 51 行）仍未改寫** ——
   本輪之 §2.1 即該未改寫之直接後果。

4. **143 條中僅 51 條經人工關卡（36%）。** 75 包 §1 明言未抽樣之 28 條
   不因 pilot 通過而視為已 review —— **而母本已寫入全部 143 條**。
   交付論述須連同此比例。

5. **`impl_gap` 之 44 條已寫入母本**，其 AH 載 `IMPL_GAP: …`。
   **RD 之 issue 尚未開** —— 即交付本中有 44 條之訊號在基線 DBC 中不存在，
   而其應有之後續動作（R-VS66(a)）未啟動。

---

### D-6 骨架對照

| 節 | 骨架要求 | 本包 |
|---|---|---|
| §1 | 預期 vs 實測，相符者亦列 | ✅ 18 列，相符 15／不符 2／PASS 4 皆列 |
| §2 | 不符項目，不自行調和 | ✅ 三項（2.1–2.3）；§2.1 經 Pei 裁定續行，§2.3 兩讀並列 |
| §3 | 三分法 | ✅ 已驗相符（含實寫之逐項數據）／已驗不符／未驗 |
| §4 | 掃描條件揭露 | ✅ 8 列，含清空範圍、AH 之來源、與 x14 之偵測法 |
| §5 | 新開 anomaly 與 DR 成對 | ✅ 1 anomaly／0 新 DR，R-VS35 分線列 |
| §6 | 獨立判斷 | ✅ 五項，含「備份已非上一版」與自身之逾越 |
