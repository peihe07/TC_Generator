# 上繳 U72 — W-VF92：457 列已依 Requirement ID 重排；**B 起點不從 V72 之「自 001」，其與 R-VF83 衝突**

**所據**：`docs/handoff/V72_reorder_dispatch.md`（`R-G32`，W-VF92 六項）
**日期**：2026-08-25

---

## 〇、工單各項之已辦／未辦（R-VF131）

| 項 | 事項 | 狀態 |
|---|---|---|
| W-VF92.1 | 解封（問一已裁、問二未裁＝不載） | **已受領** |
| W-VF92.2 | 補入 19 條 ＋ 全表重排 ＋ B 全重寫 ＋ 對照表 | **已辦**，惟 **B 起點取 244 而非 001**（§2） |
| W-VF92.3 | 重跑閘 3／閘 4 ＋ 新增「D 欄非降序」閘 | **已辦** —— 全過（§3） |
| W-VF92.4 | `Test Case Framework` 分頁載對帳表 | **未辦** —— 依 V72 §1 問二未裁，本輪跳過 |
| W-VF92.5 | repo 內副本複製至交付路徑 | **屬 Pei** —— **且現二本已分岔**（§5） |
| W-VF92.6 | W-VF91 其餘各項 | **狀態不變**（§6） |

**落檔之條文**：**無新條** —— V72 §1 明令不另立 R-VF 號，由 `R-G32` 承接。
**新開 anomaly：`A-VF33`**（實測最大 `A-VF32`，實得 +1），見 §2。

**本包對交付物之推進**（R-G31）：**457 列已重排＋B 全重寫之 036 repo 內本**，
**＋ `data/vf230_id_remap.tsv`（457 列）**。

---

## 一、重排之結果

### 1.1 施行前之實測（閘 0）

```
資料列  **457**（列 10–466）｜B **244–700** 連號｜相異 D **454**
D 之數字尾碼位數   三位零填 450 ｜ 無數字尾碼 7 （如 `SWE1-VC-MaxPowerLevel140`）
**同一 family 內位數不一者 0** → **字串序即數值序**（V71 §4 問一(a) 之前提於此獲實測支持）
```

**該檢查為新立**：V71 稱「皆三位零填故字串序即數值序」，
**其為推定**；本檔以逐條實測驗之，**family 內位數不一者即停**（本輪 0）。

### 1.2 重排

```
排序鍵    D 欄整字串升冪，唯一鍵
穩定性    stable sort（Python `sorted` 保證）—— 同一 D 值之多列保持原相對序
          相異 D 454 而資料列 457，**差 3 為 2 個 D 之拆列**
          （`DaytimeRunningLights-006` × 2、`HeadlightsOffDelay-014` × 3）
B 欄      依重排後之位置全部重寫，**244–700 連號**
```

### 1.3 family 段數 —— **回報實得之數，與 V72 §3 之預期不符**

```
family 相異     **110**（V71 §1 實測 438 列時為 109）
重排前之段數    **424**（V71 §1 實測 438 列時為 405）｜平均每 family 散在 3.85 處
重排後之段數    **110** —— **= family 數，即每 family 恰一段**
```

**V72 §3 令「重排後 family 段數應 = 109」。實得 110。**

**其差之成因為補入之 19 條帶入一個新 family** ——
V71 §1 之 109／405 為**補入前之 438 列**所量，V72 §3 沿用該數為預期值，
**而本輪之標的為補入後之 457 列。**

**其判準未受影響**：V72 所欲驗者為「**每 family 恰一段**」，
**110 段 / 110 family 即其成立**。**本層依 V72 §2.3「不得自行調帳」不改任何數，
逐字回報實得。**

---

## 二、**B 欄之起點取 244，不從 V72 §2.2 之「自 001 起」**

### 2.1 其為下放包與既有裁定之直接衝突

V72 §2.2-2 逐字：「B 欄依重排後之位置**全部重寫，自 001 起連續**，格式循既定 ID 規格」。

**`R-VF83`（Pei 裁定 2026-08-23）逐字**：

> VF230 之 036 workbook，B 欄「No.#/序號」自 **238** 起連續遞增，**不自 1 起**。
> 配套 2：**CFTS044 之 B 欄（1–237）為凍結欄**，任何寫回不得觸及。
> 配套 3：續號為**跨 workbook 之連續序號**。

**起點其後由 Pei 於 2026-08-24 依實測改裁為 244**（CFTS044 交付本末號 243 + 1）。

**照 V72 之字面施行，VF230 之 B 1–243 將與 CFTS044 之 B 1–243 逐號相同**，
**而二本同屬一次交付之 Part 1／Part 2。**

### 2.2 本層之處置

**不從其字面**：B 取**實測之現行起點 244**，重排本身（stable sort by D、
B 依位置全部重寫、產對照表）**逐字照辦**。

**其為 `U71 §2` 之同一處置形態** —— 下放包之前提與實測（或與既有裁定）相左者，
**不予執行該項並具名回報，不靜默照辦亦不靜默不辦。**

**已登記 `A-VF33`。**

### 2.3 **其可低成本翻案**

`scripts/vf230_wvf92_reorder.py` 之 B 起點**取自實測而非寫死**
（`R-VF114` 之精神）。**若 Pei 裁「確自 001 起」**，改該起點之來源重跑即可：
**重排之結果（列序）完全不變，只有 B 之值與對照表重生。**

**惟其須再動交付本一次**（§5），故**請於本輪一併裁畢**。

### 2.4 **通則**

**下放包所引之數若與既有裁定相左，執行層不得以「令在後故其勝」自決。**
**裁定之修改須經裁定，不得經下放包之措辭。**

---

## 三、閘之結果（W-VF92.3）

### 3.1 施行工法 —— **XML 手術式，openpyxl 未參與存檔**

依 V72「寫回工法」：只改目標分頁之 `<sheetData>`
（分頁名 → `xl/workbook.xml` → rels → **`xl/worksheets/sheet6.xml`**，
**不寫死 part 名**），**其餘 part 逐位元原樣寫出**。

**實測：全 48 個 part 中內容相異者恰 1 個，即該分頁。**
**故 x14 之 `<extLst>` 無被剝離之機會，`R-VF113` 之補回程序本輪不需要。**

**未用 `backend/xlsx_surgical.surgical_save`（R18-3 所指之單一寫出路徑）之理由**：
該模組之模型為「openpyxl 改**格值** → 對源 XML 逐格套用」，
**而本輪之操作非改值，是列之置換** —— 逐格套用須重寫 445 列 × 16 欄之長字串，
且**其不搬 `<c>` 之 `s`（樣式索引）**，樣式與內容遂脫鉤。
**故搬移 `<c>` 區塊（樣式隨內容），而結構不變式改用該模組之 `verify_structure`**
—— **檢查仍走共用之權威實作，不另造判準**（閘 3a）。
**本檔無任何 openpyxl 存檔呼叫**，`tests/test_single_write_path.py` 之掃描不命中。

**本輪之寫出為位元可重現**：自備份還原後重跑，
產出之 sha256 與首次相同（`67256f58…07d4cb`）。

### 3.2 閘

```
[閘 0] 資料列 457（列 10–466）｜B 244–700 連號｜尾碼位數一致｜資料列無空洞
[閘 3a] `backend/xlsx_surgical.verify_structure`（**共用之權威實作，非本檔自造**）
         zip member 集合相同｜逐分頁 dv 數（classic／x14）相同｜
         **內容相異之 part 恰 1：`xl/worksheets/sheet6.xml`**   → **通過**
[閘 3b] part 級對帳（前 → 後，本檔之獨立實作，**全一致**）
         <dataValidation           6 →   6
         x14:dataValidation        2 →   2      （另以 probe_x14 獨立實作複驗 = 2）
         <conditionalFormatting    1 →   1
         <extLst                   1 →   1
         分頁數                   10 →  10
         drawing rel               6 →   6      chart rel  0 → 0
         part 總數                48 →  48
[閘 4] 重讀比對 **457 列 × 16 欄**（B 除外，其為本輪所改）：**差異 0**
[新閘] **D 欄逐列非降序：違者 0**
[新閘] B 欄連號 244–700：**True**
```

### 3.3 獨立複驗（不經本檔之 XML 路徑）

```
全 part XML 合法（嚴格 parser）：不合法者 **0**
`<extLst>` 仍在目標分頁：**True**
列數 457（重排前後同）｜相異 D 454
**D 之多重集合重排前後相同：True**
**內容多重集合（B 除外）重排前後相同：True** ← 重排未增未減未改任何值
D 非降序 True｜B == range(244, 701) True
含公式 **0**｜多行而無 `wrap_text` **0**
對照表 457 列｜old_b 唯一｜new_b 唯一｜**與重排前之本一致 True**｜**與重排後之本一致 True**
```

### 3.4 列數對帳（V72 §2.3 之口徑）

```
資料列 **457** = 438 ＋ 19                                    ✔
已寫 454 ＋ 隔離 84 ＋ 去重 35 ＋ DR-48 剔除 1 = **574**        ✔
  （`vf230_wvf91_recon.py` 之口徑為「去重 dropped 36」，
    其 = 35 ＋ DR-48 之 1，**同一數之二種寫法**；PASS，574/574）
```

**與 V72 所載相符，未調任何帳。**

---

## 四、ID 對照表（W-VF92.2-3）

```
data/vf230_id_remap.tsv    **457 列**    欄 = old_b / new_b / req_id
**B 之位置未變者僅 12 列** —— 即 445 列之 B 號改變
```

**其必要性**：重排使 B 幾乎全變，**pilot 紀錄、DR、隔離表內凡以 B 號指稱某列者，
無此表即全部斷鏈。** 該表已與重排前之本、重排後之本**雙向逐條驗證一致**。

---

## 五、**交付路徑之本已再度分岔** —— W-VF92.5 之前提須先具名

**實測**（非推定）：

```
交付路徑之本   191,421 bytes   08-25 16:49
repo 內副本（重排前）之備份    191,421 bytes
  sha256  b93ee1c0…9b457c   **二者逐位元相同**
repo 內副本（重排後）          189,846 bytes
  sha256  67256f58…07d4cb
```

**故：Pei 已於 16:49 將「補入 19 條而未重排」之本複製至交付路徑，
而本輪之重排在其後。二本現已分岔。**

**W-VF92.5 之複製須對重排後之新本再為之一次**（`R-VF112`，屬 Pei）。
**且其 Excel 開檔確認亦須對新本為之**（`R-VF116`）。

**惟請先裁 §2 之 B 起點** —— **若裁「自 001 起」，本層須再重寫 B 一次，
則現在複製即為白做。**

---

## 六、W-VF92.6 —— W-VF91 其餘各項之狀態（**皆不變，逐項具名**）

| W-VF91 項 | 狀態 | 說明 |
|---|---|---|
| .2 `H10:H132` → `H10:H1000` | **仍不予執行** | `U71 §2` 已具名其前提為假（母元素為 `conditionalFormatting` 之 colorScale，非 `dataValidation`）。**V72 未推翻該回報，本層維持。** |
| .4 交付說明更新 | **已辦（本輪再更新）** | `docs/reports/vf230_delivery_note_draft.md`：§四改寫（B 之重寫已非「244–681 ＋ 682–700」）、**增 §十（交付列序，R-G32）** |
| .6 `A-VF32` 之對治 | **維持** | `vf230_wvf91_recon.py` 每次驗之，本輪重跑 **PASS 574/574** |
| .7 備份刪除 | **仍未辦，其條件未成就** | Pei 之 Excel 開檔確認至今未回報；**且本輪又多一次寫入，確認須對重排後之本為之** |

**備份現為三個**（本輪新增第三個）：

```
_vf230_036_prewrite_backup.xlsx     82,467 bytes   寫入前（0 列）
_vf230_036_append_backup.xlsx      186,784 bytes   補入前（438 列）
_vf230_036_reorder_backup.xlsx     191,421 bytes   **重排前（457 列，未排序）**
```

**第三個之保留為必要**：**重排為不可逆之全表改動**，
且其與交付路徑現存之本逐位元相同 —— **其為現行交付本之唯一 repo 內對應物。**

---

## 七、未結 DR 清單（V72 §3 之要求；S6）

**未結 37 條**（`DR-22′` 已撤回不計）。

**已送出、待覆 —— 6 條**：

```
DR-14′  DR-15   DR-15′  DR-17   DR-19   DR-20     （皆 2026-08-22 送出）
```

**已登記、未送出 —— 31 條**：

```
Urgency High    DR-21  DR-37  DR-39  DR-46  DR-47
Urgency Medium  DR-18  DR-23  DR-25  DR-25′ DR-27  DR-34  DR-36  DR-38
                DR-40  DR-41  DR-42  DR-43  DR-44  DR-48
Urgency Low     DR-26  DR-28  DR-29  DR-30  DR-31  DR-32  DR-33  DR-35  DR-45
其他／待擬提問文  DR-8′  DR-12  DR-24′
```

**本輪未新開 DR** —— 重排不觸及任何素材之解讀。

**與交付本直接相關者**：`DR-44`（35 條去重之還原）／`DR-48`（1 條之剔除）——
**二者若獲覆，選池 574 之分法即變，交付列數隨之變。**

---

## 八、本包是否仍有該驗而未驗者（canon FO §8.2）

**有，四項。**

1. **重排後之工作簿未經人以 Excel 開啟確認** ——
   本輪之全部驗證皆為機械判準（XML 合法、多重集合相同、逐欄比對）。
   **`R-VF136`「合法 ≠ 可用」之同型，第五次。**
   **具體之未知**：457 列重排後，Excel 是否仍正常渲染（下拉、色階、列高）。
   **只有人眼能驗。**

2. **`<row>` 之列高屬性留在原位，未隨內容移動** ——
   實測列 10–246 有 `ht="14" customHeight="1"`、列 247–466 無（220 列）。
   本層判其為**位置之屬性**而留之，**其為刻意且已具名**（交付說明 §10.4）。
   **未驗者**：重排後「哪一條需求落在有列高之列」已全變，
   **其於 Excel 中之視覺效果是否可接受，未經人看過。**
   **另一選項**（隨內容移動列高）本層未採，其將使 19 條之無列高散入全表。
   **二者孰優本層無判準，待人眼。**

3. **`autoFilter ref="A9:AH132"` 仍未動，而資料至列 466** ——
   `U71 §六` 第 3 項已具名，**其是否屬本層射程仍未經裁定**，本輪亦未動。
   **重排使其影響加大**：篩選只及列 132 者，於重排後所涵蓋之 family 已全變。

4. **19 條之 `test_item` 內容本身仍未經人讀** —— `U71 §六` 第 1 項，狀態不變。

**已於本輪查證而不再列為未驗者**：
選池對帳（重跑 PASS）、x14 之保全（part 級對帳前後一致）、
D 之尾碼格式（實測而非推定）。

---

## 九、給分析層／Pei 之待裁

| # | 事項 | 阻塞 |
|---|---|---|
| **1** | **B 欄之起點：244（本層所採，依 `R-VF83`）抑或 001（V72 §2.2 之字面）** | **阻塞 W-VF92.5 之複製** —— 裁 001 則須再重寫一次 |
| 2 | `autoFilter` 之延伸是否屬本層射程（未驗項 3） | 不阻塞 |
| 3 | 列高屬性之處置（未驗項 2） | 不阻塞，惟須人眼 |

**§1 裁畢前，本層建議 Pei 暫不複製至交付路徑。**
