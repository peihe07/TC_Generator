# 上繳 V52 — W-VF82（全量括號下半、拆列、五項隔離）

**所據下放包**：`docs/handoff/V50_split_ruling.md`／`V51_writeback_authorized.md`
**日期**：2026-08-25

---

## 〇、實寫之二條件：**條件一已成就，條件二非本層可成就**

`R-VF124` 令實寫須二條件成就：**(一) W-VF82 完成；(二) 分析層抽樣覆核通過。**

**條件一本輪成就**（本報告即其交付）。**條件二非本層可自行成就 —— 故本輪不實寫。**
`R-VF115`／`R-VF116` 之實作為實寫腳本內之閘，**實寫未行則其無載體**，仍具名待辦。

---

## 一、編號回報（R-VF102 一）＋ 常設清單（W-VF82 §4）

實測最大 `R-VF120` → 四條實得 **`R-VF121`–`R-VF124`**，與二包所令相符；
逐字轉錄以程式比對，**未逐字入者皆 0**。

**現行已開而未落檔之條文：0**（`RULINGS.md` 現有 R-VF 條文 115 條，最大 `R-VF124`）。
**本項為 R-VF122 所令之常設清單，此後每包皆附，直至連續二輪為 0。**

---

## 二、括號下半：**0 / 440 → 438 / 438**

canon §4.3.1 逐字「缺括號下半 = FAIL，不得出貨」。**全量施行完成。**

```
具括號下半      438 / 438
上半 token > 50   **0**（施行前 197／440，44.8%）
下半含 " -> "   438 / 438
```

### 摘句之規則：「取末句」為**錯**之規則

初版擬取末句作上半。**實測 440 條中 230 條之末句不宜作上半**：

```
末句為伴隨句者（`Any invalid value shall be considered invalid by HMI.`）   83
末句無結論動詞者                                                          147
```

**改為**：取**含結論動詞且與 `tc_title` 之關鍵詞重疊最多**之句；
同分取最後者（最接近結論）。**其恆為條文之某一整句，不截斷**（A-VS161 只刪句不改字）。

**結果**：258 條取得結論句；**182 條無結論句而退回末句，已具名**。

---

## 三、拆列：5 列，而非預期之 6 —— 其第 6 列缺席之因為 DBC 錯字

```
DaytimeRunningLights-006   False／True                    → 2 列 ✅
HeadlightsOffDelay-014     0sec／30sec／60sec／**90sec**   → 3 列（**應 4**）
```

**`90sec` 對不上 DBC 之 `90ec`（缺 `s`）** —— 見 §五乙。
**不自行對應**（R-VF92 二），第四列缺席並具名。**已開 DR-47 乙。**

### 實作層之二錯，皆本輪自查自改

**其一：不得以字串替換拆列。** 首版於 `batches` 層以 regex 替換值，實測二錯：
`seq` 與原列衝突；**且僅改 label 而未改 raw，產出 `is 0 (30sec)` 之自相矛盾標題**。
**改於 `facts` 層展開**，`raw`／`label` 由 DBC 一併取得，`build()` 自然產出正確之對。

**其二：展開之列被同層之去重吃掉。** 「上游條文逐字相同」之去重以 `text` 之 sha1 為鍵，
**而同一 leaf 展開之各列其 `text` 本就相同**（同一條文），遂只剩 1 列。
**已排除 `multi_of` 之列** —— `R-VF122` 已裁重複判準為「四欄逐字相同」，
**而四欄於 `build()` 後方存在，故該層不得以 `text` 判之**。

---

## 四、多值形態補掃（W-VF82 §3）：**0 命中，倍率不變**

除已認之二式外，掃 `one of`／`X or Y`／方括號內逗號列舉／二方括號四式，
**於 440 條之條文上命中 0**。**故 1.01 之倍率其下界為緊。**

---

## 五、五條隔離 ＋ 一處 DBC 錯字 → **DR-47**

### 甲、`pre_conditions` 不可執行之 PROXI 參數名（5 條）

```
ParkSenseRearVolume-070／-071      條文 `retrieve the **the PROXI parameter** PROXI configuration`
ParkSenseFrontVolume-077／-078     （同上）
SuspensionAutoEntryorExit-090      條文 `Hybrid_Type **and** SRT (VC_SRT_PRSNT)` —— 併寫二參數
```

其所生之前置為 `PROXI $the PROXI parameter$ is set to "Absent"` ——
**測試員無從得知須設哪一個參數，該前置不可執行。**
**不自行取其一**（`Hybrid_Type` 雖在表內，取之即改取他標的，R-VF92 二）。

**其未被更早發現之因**：`vf230_selfcheck_wvf62.py:classify()` 之首個分支
**將 `PROXI $…$` 之列一律歸「系統版本或模式」，不驗參數名是否為有效參數** ——
**故該五條之前置十九輪來皆判合規。** 本輪由 `title_param()` 之表內比對方揭露。

### 乙、DBC 標籤錯字 `90ec`（見 §三）

---

## 六、`title_param()` 之前綴匹配 —— **本層造出一個被明文禁止之推定，又自行攔下**

**擴充之緣由**：實測 15 條之 PROXI 參數名不在表內，其中 9 條**表內實有其完整名**：

```
CAN node 95              → 表內 `CAN node 95 (ITBM/ITCM)`
CAN node 97              → 表內 `CAN node 97 (PSSM)`
CAN Node 27 (ASM or ASCM) → 表內 `CAN node 27 (ASM/ASCM)`（`or` vs `/`）
```

遂加「前綴唯一匹配」式。**其首版立即誤命中**：

```
Greeting_Light  →  Greeting_Lights_Menu
```

**而該二者正是 DR-34 所問、且 `R-VF92` 二明令「不得以名近推定其對應」者** ——
**本層親手在 DR-34 具名過該禁令，而新判準恰好造出了它所禁之推定。**

**已加守衛**：前綴匹配之剩餘部分**須以空白或 `(` 起首**（即表內名為「條文名 ＋ 獨立之別名段」），
而非續字。`Greeting_Light` 之剩餘為 `s_Menu`（續字）→ 不匹配。
**七個錨點固定其二側，全過。**

---

## 七、自檢：**19 項全綠，19 項皆可失效**

新增四項（`R-VF118`／V47 §5.3）：括號下半存在／上半 token ≤ 50／
同上半之下半不得逐字相同／下半含 ` -> `。

### 項 18 之射程經實測收窄

初版報 **22 對假陽**：`TrafficSignAssistOffset-060` 與 `-073` 之
**條文不同、四欄不同、`tc_title` 不同，僅摘句碰巧收斂至同一句**。
**`R-VF122` 已裁重複之判準為「四欄逐字相同」**，其由去重機制與項 13 管；
**本項不得越俎**。故鍵自「上半」改為 **`(leaf_id, 上半)`** ——
其射程為「同一原條文所拆出之列」。

**摘句碰撞之量（不 FAIL，僅回報）**：跨 leaf 而 `test_item` 全同者 **19 組 38 條**，
**其中四欄亦逐字相同者 0 組**。

---

## 八、數字

```
量產         435 條（seq 268–702）＋ pilot #3 之 3 條 = **438**
B 欄         244 – 681（實測導出：CFTS044 repo 237／交付 243，取最大 +1）
可直接書寫    470   隔離 84   拆列 +3（5 列 − 原 2 條）
括號下半     438/438   上半 token > 50   0
自檢         19 項全綠、無法失效之項 0
```

**未寫回**（R-VF124 條件二未成就）。**交付路徑 036 仍為 0 資料列。**

---

## 九、本包是否仍有該驗而未驗者（canon FO §8.2）

**有，四項**（第 4 項於撰寫時補驗，其結果為 0）。

1. **182 條之上半為「退回末句」** —— 其是否與括號下半之測試目的直接相關，
   **未逐條人讀**；canon §4.3.1 令「以與測試目的直接相關之句為限」，
   **而退回末句者，其相關性未經確認。**
2. **19 組摘句碰撞未人讀**（§七末）—— 四欄相異故非重複，
   **但「不同條文摘出同一句」是否表示摘句過度收斂，本層未判。**
3. **拆列之標的以白名單寫死**（`MULTI` 二個 leaf）—— 其由本輪之量測導出，
   **而該量測之判準只認二式**（V49 §七-1 已具名）；**白名單與量測之分岔無檢查在管。**
4. **（本項已於撰寫本報告時補驗，其結果為 0，故非未驗項）**
   以同一表內比對式（含前綴唯一匹配）掃 pilot #1／#2 之 `pre_conditions`，
   **PROXI 參數名不可對應者 0** —— 二 pilot 無同型缺陷。
5. **`R-VF115`／`R-VF116` 仍未實作**（實寫腳本內之閘），其為實寫之前置。
