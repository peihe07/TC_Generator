# 上繳 V54 — W-VF83（摘句補正）

**所據下放包**：`docs/handoff/V53_excerpt_blocker.md`
**日期**：2026-08-25　**不寫回**（R-VF124 條件二本輪由分析層驗收）

---

## 〇、驗收條件：**上半為排除清單形態者 0 / 438**（施行前 174，39.7%）

```
上半為 (a)(b) 二形態者     174 → **0**
上半 distinct              265 → **427**
退回末句者                 182 → **178**
括號下半                   438 / 438（未動，如令）
同一 (leaf, 上半) 之下半重複  **0**（上半變動後複驗，如令）
自檢                       **20 項全綠、無法失效之項 0**
```

---

## 一、編號回報 ＋ 常設清單（R-VF122）

實測最大 `R-VF124` → 實得 **`R-VF125`**，與所令之 +1 相符；**未逐字入者 0**。
**現行已開而未落檔之條文：0。**

---

## 二、二層判準：排除先行，評分次之

**本條之診斷正確且為要害**：「**評分只能排序，不能排除**」——
伴隨句同時滿足「含結論動詞」與「與 `tc_title` 重疊」二條件，
故上輪之評分式判準**反而優先選中它**。

**已實作**：`excluded_upper()` 於評分之前剔除，故評分無從再選。
**排除後全數被剔除者，退回原集合再由評分處理**（避免上半為空）。

---

## 三、三錨點之實測 —— 其一與本條所引不符，以實測為準

| 錨點 | 結果 |
|---|---|
| **必命中** `B252` `Any invalid value shall be considered invalid by HMI.` | 排除 ✅ |
| **必不命中** `B390` | **本條引其上半為 `The LTM or ETM application layer shall update the Headlights Off Delay setting information displayed on the HMI.`，而實測為 `The update of Headlights Off Delay setting information is displayed .`**（`seq 414` 相符，上半不符）。**以實測為準 → 保留 ✅** |
| **鑑別** 含 `maintain/update` 而主詞確為驗證對象者 | **實測不存在** —— 全庫含 `maintain/update` 之句**僅 1 種**，即 (b) 所欲排除者。**依令具名「不存在」，未以他句充數。** |

**鑑別錨點不存在之副效**：**(b) 之 pattern 不可能誤殺** —— 無他句可殺。
**該資訊只有在確認其不存在後方得知**，故「不得以他句充數」之令有其實效。

---

## 四、本條之 174 未含一個變體

```
Any invalid **signal** value shall be considered invalid by the HMI.   2 條
```

**逐字寫死之排除式，其變體即漏。** 已改為
`^Any invalid\b.{0,40}?shall be considered invalid`，並加**假陽之防**錨點
（`The invalid value is displayed as Off.` 不得排除）。**五錨點全過。**

**故實際排除者為 176，非 174。**

---

## 五、實作層自查一錯，於施行前被錨點攔下

排除式首版之 `\b` **於外層字串中被解為退格字元（`\x08`）寫入檔案**，
致 (a) 之 pattern 恆不命中，而 (b)（無 `\b`）正常。

**其未進入產出** —— 本條令「三錨點先實測，錨點不符即停」，
**錨點於施行前即回報 (a) 二例皆 `排除=False` 並停**。

**此為「先驗錨點」之令首次於本層攔下一個實作錯**（R-VF11／R-VF17 之設計目的）。

---

## 六、白名單分岔已對治（V53 §4 待辦一）

**改為由量測直接導出**：以 `receives the value as X via signal` 掃全量 facts，
相異值 ≥2 者即為拆列標的；**原白名單降為交叉驗證之期望值，不符即停**。

**可失效已實測**：竄改期望值（加一個 `Maybe`）→ `exit 1`，
訊息載「**其一已過時**：條文若增減多值形態，期望值須隨之更新並具名」。

---

## 七、自檢增項

**項 20：上半不得為排除清單之形態。** 其判準**自 `batches.EXCLUDE_UPPER` 匯入**
（單一權威，二處各寫一份則其分岔無檢查在管）。**可失效已實測**（回報 1 筆）。

**20 項全綠、20 項皆可失效、跨批視野 10 批 / 438 條。**

---

## 八、數字

```
438 條（435 量產 seq 268–702 ＋ pilot #3 之 3）
B 欄 244–681 連號｜隔離 84｜拆列 5 列（第 6 列缺席之因為 DBC 錯字 `90ec`，DR-47 乙）
```

**交付路徑 036 仍為 0 資料列。未寫回。**

---

## 九、本包是否仍有該驗而未驗者（canon FO §8.2）

**有，三項**（第 3 項於撰寫時補測，其結果為 0）。

1. **178 條之「退回末句」其相關性仍未逐條人讀。**
   **上輪本層具名此項時，其射程被本包 §2.2 正確指出為不足**
   （174 條中多數並非退回末句者，乃新規則主動選中者）。
   **本輪之 178 條為排除後之退回，其性質與上輪不同，而其相關性同樣未經確認。**
2. **排除清單為已知集合（R-VF95 二）**，其為二形態。
   **全庫上半 distinct 427 種，本層未逐一人讀其餘 425 種是否另有伴隨句形態。**
3. **（本項已於撰寫本報告時補測，其結果為 0，故非未驗項）**
   摘句碰撞（跨 leaf 而 `test_item` 全同）：**19 組 38 條 → 0 組 0 條**。
   **其歸零非偶然**：上輪之碰撞多因不同條文之末句同為伴隨句
   （`Any invalid value …` 46 條共用一句），**排除層一併消除之。**
4. **`R-VF115`／`R-VF116` 仍未實作**（實寫腳本內之閘）。
