# 上繳 V18 —— 委派範圍補足（11 候選，仍 0）、R-VF48 誤報 15→2、158 處判線別

執行層寫入。依據：`docs/handoff/V18_delegation_scope.md` §5（4 項工單）。canon §8.2 六節。

**本輪未生成任何 TC、未執行任何寫回**（R-VF26）、**`framework.md` 未動**。
**批次生成之閘本層未自行解除**（其解除屬分析層）。

---

## 1. 交付總表

| 工單 | 狀態 | 產物 |
|---|---|---|
| W-VF50 委派範圍補足（**P4 前置**） | **完成 —— 11 候選，委派仍 0** | `docs/reports/vf230_delegation.tsv`、`scripts/vf230_wvf46_delegation.py` |
| W-VF43 158 處判線別（**不得再順延**） | **完成** | `docs/reports/wvf43_lineside.md`、`scripts/vf230_wvf43_lineside.py` |
| W-VF51 R-VF54 之實作 | **完成 —— 15 → 2** | `scripts/grade_overrides.py` |
| W-VF52 DR 兩套編號之對照 | **完成（只列不改）** | `docs/reports/wvf52_dr_numbering.md` |
| 條文落檔 | **完成** | R-VF53／54／55（R-VF 現 **48 條**） |

---

## 2. W-VF50 —— 11 個候選逐一比對，委派仍 **0**

候選以 `features/` 之**實有目錄**為準（R-VF53 二），得 11 個。

| feature | 狀態 | 037 簇 | 與 VF230 同名 |
|---|---|---:|---:|
| comfort | 已比對 | 127 | 0 |
| sxm | 已比對 | 202 | 0 |
| user_profiles | 已比對 | 207 | 0 |
| power | 已比對 | 99 | 0 |
| power_moding | 已比對 | 56 | 0 |
| amfm／home／media／privacy／projection／time_management | **無 037 素材，無法比對** | — | — |

**已比對之 5 個 feature 合計 691 簇，與 VF230 之 106 簇同名 0。**
**未以「推定無關」代替比對**（6 個無素材者逐一具名）。

**逐 feature 之鑑別錨點（W-VF50 第 4 項）**：

```
comfort        2 個  Auto On Driver Comfort - 2/3 Option              → no ✅
power          6 個  Power Tailgate／Max Power Level／Charge Power Level／
                     Power Unit／Power Side Step／Engine Off Power Delay → no ✅
power_moding   4 個  SWITCH 1–4 Power Mode                            → no ✅
user_profiles  —     無近名簇，**具名為不存在**
sxm            —     無近名簇，**具名為不存在**
```

**`power` 之六個尤為關鍵**：VF230 有大量 `Power *` 簇而恰有 `power` feature ——
**一條以 feature 名為鍵之規則會把六者全部誤配。**

**委派結果不變：627 全數 `no`。** R-VF53 二之前置因而完成。
**閘之解除屬分析層，本層不自行解除。**

---

## 3. W-VF43 —— 146 處判線別

**依上下文，非依編號**。各號之兩義主題詞取自 `RULINGS.md` 之條文標題逐字，
非本層自擬；取上下文 260 字元比對。

```
VF230 線   31
Part 1 線  42
待人工     73        ← 兩義皆命中或皆不命中者，**不臆測**
```

**待人工佔 50%** —— 主題詞法對半數之引用無決斷力。其須逐處讀全段，
**本輪未讀**（W-VF43 令只判不改，未令讀全段）。

**錨點**：必命中（`R-VS59` 之引用含 `238`）判 VF230 ✅；
必不命中（含「不寫」）判 Part 1 ✅；
**鑑別 —— `docs/handoff/` 之 Part 1 檔而引用 VF230 線者 5 處**，
皆為歷史包，**全域取代必誤傷之**。階段二之逐處取代須以其為必不取代之標的。

---

## 4. W-VF51 —— 15 → 2，**惟驗收條件未達且方向與預期相反**

R-VF54 令「FAIL 應僅餘 A-VF10 之撞號集，其數應為 **8**」。
**實得 2，且不含撞號集。**

**成因**：`R-VF1`／`R-VF2`／`R-VF8` 之引用**全部位於限縮後不掃之三簿內**
（`RULINGS.md`／`ANOMALIES.md`／`DATA_REQUESTS.md`）。

→ **第二項之限縮使 R-VF48 不再攔截撞號集。**
其仍由 **R-VF10 之檢查攔截（8 項）**，故整體覆蓋未失 ——
**惟二檢查之分工已改變，而本條之驗收條件係以舊分工所寫。**

**限縮不損攔截力之驗證仍成立**：`R-VF9`（來自 `framework.md`）與
`A-VS131`（來自 `feature.yaml`）皆在限縮後之範圍內。

### 4.1 殘留 2 項，依 R-VF55 分列

| 類 | 項 | 具名 |
|---|---|---|
| **甲（結構性不可精確化）** | **0** | 無 |
| **乙（可精確化而未為之）** | `DR-11` | 其定義為「仍開啟」表之 `9（新）`，非 `## DR-11`。**精確化所需之工作**：W-VF52 之對照關係入碼。**未為之理由**：該對照尚待核可，先入碼即以未核可之判斷驅動檢查 |
| **乙** | `A-VS2` | `docs/INDEX.md` 之 `A-VS2、` 為列舉之截斷（其後接他號）。**精確化所需之工作**：引用抽取式加「其後不得緊接頓號之列舉」之判準。**未為之理由**：成本低而收益僅一項，本輪未為 |

**甲類為 0，即現無已知之結構性盲區。**

---

## 5. W-VF52 —— 兩套編號：**(iii) 歷史遺留之不一致**

```
甲、表列編號   1 2 3 4 4b 5c 5-A 5-B 7 8 9（新） 10（新）   —— 素材／缺件之項次
乙、內文 DR-n  DR-8 DR-12 DR-14 … DR-34（22 條）           —— DR 之正式編號
```

**非 (i)**：成員不一一對應（12 vs 22），表列 `1`／`2`／`3` 從未有 DR 號。
**非 (ii)**：二者**有重疊而非平行** —— 表列 `8` 與 `DR-8` 內容逐字相同。

**可考之對照**：`8`↔`DR-8`／`9（新）`↔`DR-11`（行 136 逐字「即 DR-11」）／
`7`↔`DR-7`／`5-A`·`5-B`·`5c`↔`DR-5` 之子項。
**`10（新）` 無對應**；`1`／`2`／`3`／`4`／`4b` 皆為已關閉之素材項，從未獲配 DR 號。

**另具名二事**：`DR-13`（14 包）／`DR-16`（24 包）**已撤銷** ——
其號存在於敘述而無正文，為正常之撤銷狀態；
**`DR-10` 全庫查無其定義與其所指**，待查。

**二套皆未改。**

---

## 6. 本包是否仍有該驗而未驗者 —— 獨立判斷

**有，四項。**

1. **6 個 feature 無 037 素材而無法比對**（§2）。其是否確實與 VF230 無關，
   **現無任何證據** —— 「無法比對」不等於「無關」。
   **委派之結論（627 全 `no`）因而只在 5 個已比對之 feature 上成立。**
   若該 6 個之一擁有 VF230 之某功能，本層無從得知。

2. **W-VF43 之待人工 73 處未讀全段**（§3）。其為判線別之殘餘，
   **佔全體之半** —— 階段二不得於其未判前執行。

3. **R-VF54 之驗收條件係以舊分工所寫**（§4）。本層依其字面判「未達」，
   **惟實質上覆蓋未失** —— 二者之差異須由分析層確認何者為準。

4. **`DR-10` 查無定義與所指**（§5）。其於全庫僅以編號出現，
   **本層未追其來源**。

**另**：本輪之 `power` 鑑別錨點（6 個 `Power *` 簇對 `power` feature）
**是本線至今最接近「差一點就錯」之一次** —— 若委派判準以 feature 名為鍵
而非以簇名全等，六簇皆會被誤委派，而其誤在生成時不報錯、
於 pilot review 亦難辨（reviewer 讀到的是一段看似合理之畫面敘述，
正是 R-VF53 所述之形態）。**錨點在此不是驗證，是攔截。**
