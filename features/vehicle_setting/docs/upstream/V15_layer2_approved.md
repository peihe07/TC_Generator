# 上繳 V15 —— framework 已鎖（P3 完成）、R-VF9 自始未落檔、檢查點改逐項報告

執行層寫入。依據：`docs/handoff/V15_layer2_approved.md` §7（4 項工單）。canon §8.2 六節。

**本輪未生成 TC、未執行任何寫回**（R-VF26）。
**`framework.md` 已依 R-VF44 寫入並鎖定 —— P3 完成。**

---

## 1. 交付總表

| 工單 | 狀態 | 產物 |
|---|---|---|
| W-VF38 framework 寫入與鎖定（**最優先**） | **完成，已鎖** | `framework.md`（+215 行）、`scripts/vf230_wvf38_framework.py` |
| W-VF40 檢查點報告形態 | **完成，逐項可失敗性實測** | `scripts/grade_overrides.py` |
| W-VF41 二 DR 開立 | **完成（未送出）** | **DR-32**／**DR-33** |
| W-VF39 改號範圍分類（只列不改） | **完成，且揭出更嚴重者** | `docs/reports/wvf39_renumber_scope.md`、`scripts/vf230_wvf39_renumber.py` |
| R-VF44–47 ＋ R-VF42 但書落檔 | **完成** | `RULINGS.md`（R-VF 現 **40 條**） |
| **R-VF9 補落（自始未落檔）** | **完成** | `RULINGS.md`、`feature.yaml` |

---

## 2. W-VF38 —— framework 已鎖，P3 完成

`framework.md` 附加 VF230 節（`<!-- VF230-BEGIN -->`…`<!-- VF230-END -->`），
178 → **393 行**。

**Part 1 之段落逐位元未動** —— 實測：附加前之全檔與附加後之前段字串相等。
腳本**冪等**（重跑不重複附加，`VF230-BEGIN` 恆為 1）。

**四項附帶條件逐項落實**：

1. **R-VF43 標註**：SWITCH 1–4 之 Power Mode 4 簇標「含兩種條文形態
   （顯示／HW 通知／HMI 送出）—— 其 leaf 不得因同簇而逕作 sibling」；
   **SWITCH 5／6 不加**（W-VF36 實測其無兩種形態）。
2. **R-VF34 留空且可見**：`E-Save`／`CHMSL CAMERA DYNAMIC CENTERLINE`
   之 Layer 3 標「**（無 spec 對應）**」並附理由，未以鄰近章名填充。
3. **Part 1 之 Layer 1／2／3 未動。**
4. **Test Set 名凍結**已載於節內並登錄 `CROSSLINE.md`；
   7 筆異質性殘留逐一列出並註明「不得作為 pilot review 之 defect」。

**錨點實測**：合計 627 ✅／Test Set 9 ✅／二不成立之名已消失 ✅／
鑑別錨點 `Power Unit` 在 `Measurement Units` ✅。

**鎖定註記三項**（R-VF47 二所令，只記一次）：分組立於本層判斷而非上游準則
（DR-32 待覆）／R-VF34 之 2 簇留空／SWITCH 5／6 無 HMI 送出且成因未查（DR-33 待覆）。

### 2.1 P4 之啟動條件

| 條件 | 狀態 |
|---|---|
| Layer 1／2／3 已鎖 | ✅ 本輪 |
| leaf 母體確定（627） | ✅ R-VF16 |
| Test Group 確定 | ✅ R-VF9（**本輪補落**） |
| `feature.yaml` 之 profile 齊備 | ✅ 含 `test_group`（**本輪補上**） |
| 寫回 | **凍結中**（R-VF26），P4 之產出不寫回 |
| B 欄續號基準 | ✅ R-VF1（238 起）—— **惟其現以 `R-VS59` 落檔，見 §4** |

**P4 可啟動。** 惟 §4 之編號問題須先知悉。

---

## 3. W-VF40 —— 檢查點改逐項報告

各項獨立執行、各自輸出、末尾總表；整體 exit code 仍為「任一失敗即非 0」。

```
---- 總表 ----
  PASS                   覆寫層（R-VF17／R-VF20；4 筆 × 4 欄）
  PASS                   R-VF23 檔名合規
  FAIL ← 已知未解 A-VF10   R-VF10 編號唯一性（8 項）
  —— 2 PASS ／ 1 FAIL（其中新失敗 0）
```

**可失敗性逐項實測**：人為破壞覆寫層之一欄 → 總表顯示
「FAIL ← **新** 1」與「FAIL ← **已知未解** A-VF10」**二者可分辨**，
且 `R-VF23` 仍各自輸出其 PASS。**未以抑制、跳過、白名單解決。**

---

## 4. W-VF39 —— 揭出比 A-VF10 更嚴重者：**R-VF9 自始未落檔**

**第 1 項之實測**：`R-VS67` **不撞號**（`RULINGS.md` 內僅 1 個條文起始，
為 Part 1 之 71 包）。

**但同一次掃描揭出**：`R-VF1`–`R-VF9` **九號於 `RULINGS.md` 全為 0**。

- `R-VF1`–`R-VF8` 以 `R-VS59`–`R-VS66` 存在 → 即 **A-VF10** 之撞號
- **`R-VF9`（Test Group）自始未以任何編號落檔** ——
  其原號 `R-VS67` 已為 Part 1 所用，而其正文**從未進入條文簿**

**其嚴重性**：`R-VF9` 被 `RULINGS.md` 引用 **3 處**，且
**`framework.md` 之 Layer 1 即立於其上** ——
**一條被據以行動之裁定，其正文不在條文簿內。**

**本輪已補落**（逐字，原編號 `R-VS67` 保留於區塊內），並復查發現
其所令之 `profiles.vf230.test_group` 自 `null` 改為 `"Vehicle Setting"`
**亦自始未施行**，本輪一併補上。

### 4.1 改號範圍之分類

```
516 處 ／ 60 檔
  現行有效（須改）   158
  歷史紀錄（不追改） 358
  待人工               0
```

**鑑別錨點（Part 1 檔名而引用 VF230 線）確實存在** ——
`docs/handoff/64_review_round40.md`。**故機械取代之誤傷風險為實，非理論。**

**現行 158 處尚須逐處判其所指為兩義之何者** —— 該判斷不可由編號決定，
須讀上下文。**本層未判、未改**（V15 §7 第 3 項令只列不改）。

對照表（`R-VS59`→`R-VF1` … `R-VS67`→`R-VF9`）已備於報告 §3，供 R-VF45 三置入。

---

## 5. W-VF41 —— 二 DR 已登記

**DR-32**（037 分報告之切分依據）／**DR-33**（SWITCH 5／6 是否本不需要
HMI 送出類需求）。開號前已查最大已用號為 DR-31。
**狀態皆未送出，本層不送出、不催覆**（R-VF27）。

依 R-VF47 二，「分組未經上游查證」之風險已記於 `framework.md` 鎖定註記**一次**，
**本上繳起不再逐輪重複具名**。

---

## 6. 本包是否仍有該驗而未驗者 —— 獨立判斷

**有，四項。**

1. **`R-VF9` 之遺漏揭出一個本層與分析層皆無之檢查**：
   **「被引用之條文編號是否存在於條文簿」**。R-VF10 之檢查驗「同號不得兩義」，
   **未驗「有引用而無定義」**。`R-VF9` 被引用 3 處逾三包而無人察覺 ——
   **若 framework 之 Layer 1 未逼我去查 V03 原文，其至今仍不會被發現。**
   建議補立此檢查（其可機械：掃 `R-V[SF]\d+` 之引用，比對條文起始之集合）。

2. **A-VF10 未解，且改號之現行 158 處尚須逐處判線別。** `--check` 之
   `R-VF10` 項將持續 FAIL。**惟其已由 R-VF46 之總表與其餘各項區隔**，
   不再遮蔽。

3. **11 條跨線條文未受保護**（R-VF38 三之具名義務）。本輪新增 `R-VF9`／
   `R-VF44` 一列受保護（framework 之錨點腳本），故實為 **10 條**；
   其中 4 條為程序義務、不可機械檢查。

4. **P4 啟動前，B 欄續號基準（R-VF1，238 起）現以 `R-VS59` 落檔** ——
   即其為 A-VF10 之八號之一。**P4 若引用該條，其所指須以線別判之。**
   本層已知，具名。

**另**：本輪之 `R-VF9` 補落，其發現路徑為 **W-VF38 寫 framework 時須引用
Layer 1 之依據** —— 即**一項產出逼出一項缺漏**。與 A-VF8（覆寫層攔到 driver
重跑）、A-VF10（編號檢查攔到八條撞號）並列，**本線至今之三個重要缺漏，
皆非由審視文件而得，皆由「做一件具體之事」而得。**
