# 上繳 V59 — B 欄裁定之輸入（W-VF85.5）＋ 三條落檔；**實寫仍未執行**

**所據下放包**：`docs/handoff/V58_bcol_model_wrong.md`
**日期**：2026-08-25

---

## 〇、本輪之範圍

`R-VF129` 令「**在 Pei 裁定前，實寫不得執行**」，W-VF85 待裁後啟動。
**本輪只做二事**：三條落檔；**W-VF85.5 先行**——其為**裁定之輸入**而非工單之執行。

**未寫入任何工作簿。**

---

## 一、編號回報 ＋ 常設清單（R-VF122）

實測最大 `R-VF128` → 三條實得 **`R-VF129`／`R-VF130`／`R-VF131`**，
與所令之 +1／+2／+3 相符；**未逐字入者皆 0**。
**現行已開而未落檔之條文：0。**

### 本輪工單各項之已辦／未辦（**R-VF131 所立之新義務，首次履行**）

| 項 | 事項 | 狀態 |
|---|---|---|
| W-VF85.1 | 依裁定處置 B 欄 | **未辦 —— 待裁** |
| W-VF85.2 | 實寫 438 列 | **未辦 —— 待裁** |
| W-VF85.3 | 實寫後之 Excel 提示 | **未辦 —— 待裁** |
| W-VF85.4 | LID／PROXI 路徑改指 | **部分**：R-VF33 之消費者清單已產（§三），**未改** |
| W-VF85.5 | CFTS044 之 B 欄轉硬值之時點與原因 | **已辦**（§二） |

---

## 二、【問二之答】交付本之 B 欄公式**已被移除**，其為 Pei 於 8/24 之操作

**逐位元證據（讀 sheet XML，非經 openpyxl 之值介面）**：

```
交付路徑  <c r="B10" s="156"><v>1</v></c>
            ← **無 <f> 元素** —— 公式已被移除，只剩值；且樣式為 s=156

repo 內   <c r="B10" s="78"><f t="shared" ref="B10:B73" si="0">IF(ISBLANK($D10),"",ROW()-9)</f><v>1</v></c>
            ← 公式 ＋ 其快取值；樣式為 s=78
```

**檔案屬性**：

```
交付路徑  118.40 KB｜mtime 2026-08-24 11:40｜lastModifiedBy PeiPYHsu(許珮瑜)｜Microsoft Macintosh Excel
repo 內   105.69 KB｜mtime 2026-08-19 14:45｜lastModifiedBy PeiPYHsu(許珮瑜)｜Microsoft Macintosh Excel
```

**判**：交付本之 B 欄於 **2026-08-24 11:40 由 Pei 以 Excel 操作後成為硬值**
（公式與其樣式一併改變，其形態符合「複製→選擇性貼上為值」）。
**故「公式轉硬值」為該交付本之既成狀態，且係 Pei 親為。**

**其對問一之意義**：選項 (a)（寫硬值 244–681）**與交付本之既成狀態一致**；
選項 (b)（留空由公式自算）**則使 repo 內副本與交付本在 B 欄之機制上分歧**。

**本層仍不自行裁定** —— 上述為事實之呈報，非對 Pei 原始指示之解釋。

### 一項本層先前之不精確，於此更正

上繳 V57 §二稱「交付 CFTS044 B10 = '1'（**硬值**）」——**該判斷正確**，
惟其時本層係以 `openpyxl` 之值介面得之，**未驗其 XML 是否確無 `<f>`**。
**本輪以 XML 直讀確認之** —— 其為「先前之結論正確而其證據不足」，
**非結論之更正**。

---

## 三、W-VF85.4 之 R-VF33 消費者清單（**已產，未改**）

### PROXI（5 處，皆寫死 `inputs/`）

```
scripts/vf230_wvf44_writability.py:108   openpyxl.load_workbook(ROOT/"inputs"/"PROXI_HDCC27_R3_20250424.xlsx")
scripts/vf230_wvf69_batches.py:135       zipfile.ZipFile(FEAT/"inputs"/"PROXI_HDCC27_R3_20250424.xlsx")
scripts/vf230_wvf70_proxi_values.py:14   BOOK = FEAT/"inputs"/"PROXI_HDCC27_R3_20250424.xlsx"
scripts/writability_driver.py:11         （註解引用，非讀取點）
scripts/writability_w58.py:91            （註解引用，非讀取點）
```

**惟 PROXI 之二本經實測為同一檔**（sha256 前 16 碼皆 `e7c2020f01c3d58d`，
743,785 bytes，上繳 V49 §七-6 已測）——
**故其「改指」為零差異之操作**，**本層建議不改**：
改之只增一處跨目錄依賴而無任何收益。**具名待裁。**

### LID（1 處，且其為間接）

```
scripts/writability_driver.py:166   讀 data/lid_pairs.tsv（**非 xlsx**）
scripts/lid_parse.py                自 xlsx 產生 lid_pairs.tsv（其路徑未寫死於本檔）
```

**LID 之讀取鏈為 `xlsx → lid_pairs.tsv → writability_driver`** ——
**現行各腳本讀的是 tsv，非 xlsx**。
故 `R-VF123`（准換 v1_78）之施行**須先以新 xlsx 重產 `lid_pairs.tsv`**，
**而該 xlsx 現仍在 `forms/`，未搬入**（搬入屬 Pei，V50 §6）。

**故 W-VF85.4 之實質前置為「LID v1_78 之搬入」，其屬 Pei。**
本層在其到位前**無法施行**該項，具名。

---

## 四、B 欄三問之現況（**待 Pei，本層不自決**）

| 問 | 狀態 |
|---|---|
| 一：硬值 244–681 抑或公式自算 1–438 | **待裁** |
| 二：覆寫公式為硬值是否為所欲 | **其事實輸入已備**（§二）：交付本之公式已由 Pei 於 8/24 移除 |
| 三：列 247–447 無公式 | 若採 (a) 全寫硬值則自解；若採 (b) 須補公式（本層不建議動樣板） |

---

## 五、本包是否仍有該驗而未驗者（canon FO §8.2）

**有，四項。**

1. **B 欄三問未裁** —— 實寫之唯一阻斷。
2. **實寫腳本之閘 3（x14 保全）與閘 4（寫入後重讀比對）從未執行** ——
   其在閘 2 即停。**二閘之程式碼未經運行驗證**，
   B 欄裁定後之首次實寫將是它們第一次真正運行。
3. **`LanguageSelection` 之 `22: Slovenian` 於新 DBC 消失，其原因未查** ——
   本層只確認其消失並據以重生成，**未問「該值是否應消失」**
   （其或為 BHCAN→BHCAN2 改組所致，或為新本之遺漏）。
4. **LID v1_78 未搬入，其換用（R-VF123）遂未施行** —— 屬 Pei（§三）。
