# 上繳 V33 —— 編號整治收尾（§C 三處置交付；§D 之歸屬待確認）

> **[SUPERSEDED by V33a_c_only.md — 2026-08-24]**
> Pei 裁示 (a)：§D 歸 VS 線 session，上繳改為僅含 §C 之 `V33a_c_only.md`。
> 本檔保留不刪（R-VF18、R-G23）；內文一字未改。

日期：2026-08-24
下放包：`docs/handoff/V33_number_hygiene_wvf71.md`
執行層：Claude Code（改善案 session）
結論：**§C 三處置全數交付**，§E-1／§E-2 達標、§E-3 不符（歸因見 §四）。
**§D（W-VF71 六項）未執行** —— 下放包於本層作業期間更正裁定記錄 3 為
「VS 線維持於其自身 session 發包……主對話僅管改善案（docs/fw036 全域線）」，
§D 之歸屬因而改變；**未自行認定，見 §六**。

**git**：未執行任何 git 操作（V33 §A）。
**`docs/fw036/` 與共用腳本**：全程唯讀，未改一字（§A）。
**`RULINGS.sha.tsv`**：未動、未重產、未以其跑 `--check`（§A、§C-3）。

---

## 一、R-G13 引用回報表（§G）

全域條文帶 sha；VS 條文依 §C-3 標 `tsv-pending`。

| ruling_id | 下放引用 | 實讀 | 判 |
|---|---|---|---|
| `R-G5` | `9814d24c` | `9814d24c` | **符** |
| `R-G13` | `abdc56e3` | `abdc56e3` | **符** |
| `R-G18` | `8f61f9fd` | `8f61f9fd` | **符** |
| `R-G22` | `bca29f8f` | `bca29f8f` | **符** |
| `R-VF18`／`R-VF91`／`R-VF92`／`R-VF93`／`R-VF94` | —— | `tsv-pending` | 依 §C-3 凍結 |
| `R-VF95`（本包新產）| —— | `tsv-pending` | 同上 |

**四項全域條文全符，無不符。**

---

## 二、預期數字對照（§E；相符者亦列）

| # | 指標 | 預期 | 實測 | 判 |
|---|---|---|---|---|
| 1 | C-1 live 取號 | **R-VF95** | **R-VF95** | **符** |
| 2 | 改指後 `R-VF71 之一`／`R-VF71 三` 於現行效力區之引用 | **0** | **0** | **符**（§三-3）|
| 3 | `canon_refs --waiver`（唯讀）| unresolved 0；ambiguous **11 維持** | unresolved **0**；ambiguous **21** | unresolved **符**；ambiguous **不符**（§四）|
| 4 | 解除隔離生成數 100 | —— | **未執行** | §D 歸屬待確認 |
| 5 | 三批自檢 | —— | **未執行** | 同上 |
| 6 | 錨點複驗 | 逐一列 | **部分交付**（§五）| 同上 |
| 7 | 進度三數 | —— | **未執行** | 同上 |
| 8 | 全套 pytest | 8 failed 維持 | **8 failed／1176 passed** | **符**（§F-3 未觸發）|

---

## 三、§C 三處置

### 三-1 C-1 取號（§F-1 未觸發）

```
$ grep '^### R-VF' features/vehicle_setting/RULINGS.md | grep -oE 'R-VF[0-9]+' \
    | sed 's/R-VF//' | sort -n | tail -3
92
93
94
→ 現行最大 R-VF94，live 取號 = R-VF95
```

與 §E-1 預期相符。**依 R-G23′「落檔當下 live 取號，不得預先配號」**，
取號時點為落檔前一刻，其實測輸出已記於 `RULINGS.md` 之 V33 節標頭。

### 三-2 C-1 逐字比對輸出（程式，非目視 —— §C-1 所令）

抽取以**內容定錨**（R-VF21／R-VF28：不以行號），自作廢區之
`### R-VF71 —— ER 與 procedure 之動詞取自條文；量測須機械執行` 之
fenced block 內取 `**一、`～`**二、` 與 `**三、`～ 段：

```
一款：      12 行 vs 12 行 → 逐字相同
二款(原三)：10 行 vs 10 行 → 相異 1 行
    來源: **三、`PRE_FORBIDDEN` 等列舉須標其性質。**
    本條: **二、`PRE_FORBIDDEN` 等列舉須標其性質。**
```

**全文唯一之差異為款號**（原「三、」於本條為第二款，改為「二、」），
其餘每一字元相同。該差異已於 `R-VF95` 之註記內具名。

**原二款不入本條**：其已由 `R-VF73`（分析層不列無法機械執行之量）
逐字撤銷取代（該條載「R-VF71 二撤銷，以本條取代」）。
**作廢區之原文一字不動**（R-VF18；§A）。

### 三-3 兩處引用改指（§E-2）

| 處 | 所屬條文 | 改後 |
|---|---|---|
| `R-VF73` 之末段 | 分析層不列無法機械執行之量 | 「**R-VF71 二撤銷，以本條取代。** 其餘二款不受影響 —— **現行落點為 R-VF95 之一、二款**（V33 §C-1 自作廢區逐字補落；原號維持作廢不重用）。」|
| `R-VF74` 之內文 | 範例查證之義務 | 「非全集（**R-VF95 二**之同一性質；該款於 V33 §C-1 自作廢區補落，原號維持作廢）。」|

**改指之作法**：**移除 `R-VF71 之一`／`R-VF71 三` 之字面 token，
而保留其事實陳述**。若僅在其後補一句指路（本層初版之作法），
舊 token 仍在，§E-2 之 0 不成立 —— **已改為真正之改指**。

**§E-2 實測 = 0**（現行效力區）。餘三處落點及其豁免依據：

| 行 | 區 | 依據 |
|---|---|---|
| 5331、5332 | **「執行層註（V32 改號輪）」** | **裁定記錄 1 明令「升格為正式記載，不動」** |
| 5409 | `R-VF95` 之沿革句 | §E-2 逐字豁免（「僅餘作廢區與 R-VF95 沿革句」）|

> **具名一項**：§E-2 之豁免文字只列「作廢區與 R-VF95 沿革句」，
> **未列 V32 執行層註**。而裁定記錄 1 令該註不動，二者於此相接
> —— 本層依裁定記錄 1（較具體且為 Pei 直裁）不動之，**具名回報**。

### 三-4 兩條之 sha 將變（§C-1 末項所令之具名）

因引用改指而文字變動者：

| 條 | 變動 | 現況 |
|---|---|---|
| **`R-VF73`** | 末段一句改寫 | sha 將於 tsv 重產時換發 |
| **`R-VF74`** | 內文一處改寫 | 同上 |

**本包未重產 tsv**（§A、§C-3），故未計其新值。
重產時另須併入：`R-VF95`（新條）、`R-VF91`–`R-VF94`、
`R-VF83` 但書後新 sha、`R-VS83`。

### 三-5 C-2 R-VF94 之 `canon §8.7` 判定

**條文本體不動一字**（逐字轉錄，FO §8.1）；於其 fenced block 後加註，
記判定為 **FO §8.7（落檔與封存）** 及其三項依據（IN §8.7 為
Cross-Domain Behavioral Patterns 與對照表執行無涉；本檔同句三處
於 25 輪均判 FO）。

**§F-5 未觸發** —— 本層獨立實讀 R-VF94 之上下文：該句為
「其正文見 `docs/handoff/V25_pilot1v3_review.md` §4，依 canon §8.7 不重複轉錄」，
其語境為**落檔位置與不重複轉錄**，與 FO §8.7 之內容一致，
與 IN §8.7 無任何交集。**判定與上下文相符，未見矛盾。**

**ambiguous 該筆暫留不抑制**（§C-2 所令）。其消解須把
`verbatim-ruling-text` 判準擴至 feature `RULINGS.md` 之條文圍籬，
而該判準在 `scripts/canon_refs.py` 內 —— §A 令不碰。**已具名，未抑制。**

### 三-6 C-3 tsv 凍結

`docs/fw036/RULINGS.sha.tsv` **全程未動、未重產、未跑 `--check`**。
本上繳之 VS 條文 sha 欄一律 `tsv-pending`。

---

## 四、§E-3 之不符 —— ambiguous 21（預期 11 維持）

**unresolved = 0，符。** ambiguous 實測 **21**，預期 11。

**歸因（逐檔，唯讀）**：增量**全數在本包禁區之外之他人改動**，
本包所改之檔（`RULINGS.md` 三類改動）**零命中**：

| 來源 | 說明 |
|---|---|
| `features/power_moding/` 之 `ANOMALIES.md`／`docs/INDEX.md`／`docs/{handoff,upstream}/12_phase4_batch1.md` | 併行 session（power_moding 線）於本包作業期間持續改動 |
| `features/vehicle_setting/docs/handoff/V32_renumber_ruling.md` 及 `RULINGS.md` 之 V31／V32 新增段 | 併行 session（VS 線）於本包作業期間落檔 |

**「11 維持」之基準已失效**：該 11 為 26 包成文時之值，
而 canon_refs 掃全 repo，**其數受任何 session 之任何改動影響**。
**未以重產 waiver 消解之**（§A 令 `docs/fw036/` 不碰，且重產屬「增」，違 R-G18）。

> **具名一項判準問題**：`canon_refs --waiver` 之 FAIL 數為**全 repo 之單一數字**，
> 而 repo 現由多個 session 並行改動。**一個 feature 之下放包無法為該數負責** ——
> 其預期值在成文與交付之間必然漂移。
> **建議**：該指標於 feature 級下放包應改為「**本包所改之檔之 FAIL 數**」
> （本包實測 **0**），全 repo 之數留給全域線。**待裁。**

---

## 五、§D-5 錨點複驗（部分交付；唯讀）

`R-VF92 一` 令「全庫所有現行之『必不命中』錨點」須獨立確認其不命中之原因。

**已查（唯讀，未改任何檔）**：

| 項 | 結果 |
|---|---|
| 全庫「必不命中」錨點之落點 | `RULINGS.md` 10 處、`PLAYBOOK.md` 1 處、`CROSSLINE.md` 1 處 |
| **A-VF27 之 `max_row` 型缺陷是否復現於現行腳本** | **未復現** —— `features/vehicle_setting/scripts/*.py` 全掃，無任何 `max_row=`／`max_col=`／`range(n, NNN)` 型之寫死掃描上限；命中之 `[:NN]` 皆為列印截斷，非掃描界 |
| **§F-4（波及已核可交付物）** | **未觸發** |

**未做**：逐一「於不經被驗程式之另一路徑確認其不存在」——
其為 §D 之一項，歸屬待確認（§六）。

---

## 六、§D 之歸屬 —— 未自行認定，回報待確認

下放包 V33 於本層作業期間被更正。**裁定記錄 3 之現行文字**：

```
3. **VS 線維持於其自身 session 發包**（Pei 2026-08-24 更正）——
   本包由改善案側代擬一次，交 VS 線 session 受領後即為其下放包；
   後續 V34 起由 VS 線自行產出。主對話僅管改善案（docs/fw036 全域線）。
```

本層為**改善案 session**（主對話）。依上文，`features/vehicle_setting/` 之
執行工作歸 VS 線 session。**§D 六項（W-VF71）其性質為 feature 執行**：

| § | 事項 | 量 |
|---|---|---|
| D-1 | 100 條解除隔離並生成 | 100 TC |
| D-2 | `reasoning` 必要句列自檢增項 | 判準修改 |
| D-3 | B 類 11 條開 DR | 11 DR |
| D-4 | `PLUMBING` 應刪未刪側實測 | 逐條人讀 |
| D-5 | 全庫錨點複驗 | 12 處（本包已部分交付，§五）|
| D-6 | 續生成第 2 組 3 批 150 條 | 150 TC |

**合計 250 條 TC 之生成。**

**本層未執行，亦未自行認定其不歸本層** —— 兩種讀法皆說得通：
(a)「代擬一次」= 只擬不做，§D 交 VS 線；
(b)「代擬一次」= 本輪連做一次，V34 起才交出去。

**§C 已全數交付**（其為 R-G23′ 所生之編號整治，與改善案直接相關），
故無論何種讀法，本上繳所載者皆為已完成且可覆核之工作。
**請裁示 §D 由誰執行。**

---

## 七、獨立判斷 —— 本包是否仍有該驗而未驗者

1. **`R-VF95` 之 sha 未產**（§C-3 凍結），故本條**現在無法被 `R-XX@sha8` 引用**。
   在 tsv 重產前，任何引用它的下放包只能以編號引用 —— **即 R-G13 生效前之狀態**。
2. **逐字比對只比了「一、三款」兩段**，未比 R-VF71 fenced block 之其餘部分
   （首行標題句、二款）。二款依裁定不入本條，首行為條文識別行 ——
   **但「未入本條之部分是否確實未被誤帶入」本層未反向驗證**。
3. **`R-VF73`／`R-VF74` 之改寫未經第三方複讀。** 二者之 sha 將變，
   而其變動之**語意等價性**（改指後意思是否不變）只有本層自己讀過。
4. **§E-2 之「現行效力區」未有機械定義。** 本層以「非作廢區、非
   V32 執行層註、非 R-VF95 沿革句」判之，**該分類是人讀而非程式**。
   若要它可重測，須先定義「現行效力區」之機械判準。
5. **§三-3 所揭之 §E-2 豁免清單與裁定記錄 1 之相接**（V32 執行層註
   未列於 §E-2 之豁免文字內）—— 本層依裁定記錄 1 處置並具名，
   **但該相接本身未經裁定**。
