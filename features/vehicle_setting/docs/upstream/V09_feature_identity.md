# 上繳 V09 —— R-VF23 落檔、收斂清單、一處實質衝突

執行層寫入。依據：`docs/handoff/V09_feature_identity.md` §2（1 項工單）＋ §1 之 R-VF23。
canon §8.2 六節。

**本輪未生成 TC、未寫回工作簿、未搬移任何檔、未改任何檔名。**

---

## 1. 交付總表

| 項 | 狀態 | 產物 |
|---|---|---|
| R-VF23 落檔 | **完成** | `RULINGS.md`（逐字） |
| W-VF24 收斂前置清單 | **完成（只列不改）** | `scripts/vf230_wvf24_converge.py`、`docs/reports/wvf24_converge_plan.md` |

**先行更正 V09 §3 之一列**：該表列 **W-VF21／W-VF22／W-VF23 為「執行層待辦」**
—— **三者已於上繳 V08（commit `9ad4fef`）完成**。V09 成文時未見上繳 V08。
現行待辦僅餘 **W-VF13**（改名／舊制標記）與 **W-VF15**（R-VF12 之揭露文字），
二者皆自始未執行。

---

## 2. W-VF24(1) —— 現況復測（以當下檔案系統為據）

```
docs/handoff/          89 檔，其中屬 VF230 線 10
docs/upstream/ 一層    42 檔，其中 V* 3（V06／V07／V08）
docs/upstream/vf230/    2 檔（00_intake.md／01_recon.md）
```

**VF230 線之 handoff 10 檔**：`61_vf230_intake.md`／`62_vf230_recon_review.md`／
`63_test_group_ruling.md`／`ZZ_vf230_numbering_collision.md`／
`V04`–`V09`。

### 2.1 撞號於 handoff 側**仍為現行狀態**

| 號 | Part 1 線 | VF230 線 |
|---|---|---|
| 61 | `61_review_round37.md` | `61_vf230_intake.md` |
| 62 | `62_review_round38.md` | `62_vf230_recon_review.md` |
| 63 | `63_rulings_round39.md` | `63_test_group_ruling.md` |

**upstream 側已無同號**（VF230 線為 `V*` 或 `vf230/`）。
即 R-VF10 之分離**只在 upstream 側生效，handoff 側未施行** —— 其屬 W-VF13。

---

## 3. W-VF24(2)(3) —— 搬移表與交叉引用

搬移表 **6 列**（4 handoff ＋ 2 upstream），逐條列於
`docs/reports/wvf24_converge_plan.md` §3，含舊路徑／新路徑／依據／現況。
另須移除空目錄 `docs/upstream/vf230/`。Part 1 之 `00_`–`38_` 未列入。

交叉引用 **67 處 ／ 22 檔**：

```
須同步更新   12   （RULINGS.md／docs/INDEX.md 等現行有效之陳述）
不追改       55   （docs/handoff／docs/upstream／docs/reports —— R-VF18）
```

---

## 4. W-VF24(4) —— 與併行線之協調點：**一處實質衝突**

`docs/upstream/vf230/` 二檔係併行線於 `7a7747e`／`942f0d7` 所搬。

**衝突**：`docs/handoff/64_review_round40.md`（**Part 1 線之下放包**）
已引用 `docs/upstream/vf230/…` 之新路徑（2 處）。

- 依 **R-VF23 第四項**搬回 → 該引用指向不存在之路徑
- 依 **R-VF18** 該檔為下放包、屬歷史紀錄 → **不得追改**

**兩者不可兼得。** 本層不擇一，列出兩種處置：

1. **搬回，接受 Part 1 下放包內留有失效路徑。** 與 R-VF18「歷史不追改」
   一致，代價為連結斷。**可加一行對照表於 `docs/INDEX.md` 說明舊路徑之去向**，
   使失效連結可追。
2. **不搬回，改令收斂方向為 `vf230/`。** 與 **R-VF23 第三項**
   （「R-VF10 維持原文，不修訂」）直接牴觸，且 V09 §1 已明言子目錄提案撤回。

**本層之觀察（非建議）**：處置 1 之代價為兩處失效連結；
處置 2 之代價為推翻本包剛作成之裁定。**二者量級不同。**

**另**：本清單**有效期止於下一次併行線提交**。搬移前須由 Pei 復查
`git status`，本層未查併行線是否有未提交之作業正在動該二檔。

---

## 5. 本包是否仍有該驗而未驗者 —— 獨立判斷

**有，三項。**

1. **W-VF13 未執行，而其正是撞號殘留之解**（§2.1）。R-VF10 之分離於
   handoff 側從未施行，故 `61`／`62`／`63` 三號至今各有兩義。
   **本包之 W-VF24 只備清單，不含施行** —— 施行仍待 W-VF13 與 Pei 之搬移。

2. **R-VF10 所令之 `ANOMALIES.md` 舊制標記仍未加**（A-VS129／130／132–136
   各須一行「VF230 線舊制編號，R-VF10 前所開，保留」）。
   其不涉檔案搬移，**本可於本輪施行而未行** —— 因其屬 W-VF13 之範圍，
   本層不逕行擴張工單。**惟其為本輪唯一可做而未做者，具名於此。**

3. **W-VF15（R-VF12 之揭露文字）逾期兩包**。R-VF12 明言
   「範圍之界定與該界之揭露為兩件事，裁定只解決前者」，
   而揭露文字自 V05 至今未撰。

**另有一項非「未驗」而是本包之結構觀察**：V09 §3 將已完成之
W-VF21／22／23 列為待辦，成因為分析層成文時未見上繳 V08。
**此與 V08 §1 所記之分析層自省（「以 13:11 之目錄列表推稱現況，
未於下放前復測」）為同一形態** —— 下放包之現況陳述落後於執行層之產出。
**本層之對策已內建於 W-VF24 第 1 項（復測，不以先前輸出為來源），
建議該項升為通則。**
