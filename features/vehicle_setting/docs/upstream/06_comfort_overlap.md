# 06 上繳 — Comfort 逐條對照（R-VS7 委派句之來源表）

執行層寫入。往返 NN = 06。
**本輪未生成任何 TC，未寫回任何工作簿，未執行任何 git 寫入性操作。**

> 依 R-VS18 於開工第一個動作建立，六節先留空，逐項完成即填。

## 本輪作業清單（**W-9 為唯一作業，R-VS28**）

| # | 項 | 狀態 |
|---|---|---|
| D-1 | 建立本檔 | ✅ |
| D-2 | R-VS27／R-VS28 逐字轉錄 | ✅ 2 / 2 |
| D-3 | A-VS31 新開 ＋ W-27 錨點措辭補記 | ✅ |
| **W-9** | Comfort 逐條對照（四小項） | ✅ 附件 `reports/comfort_overlap.md` |

**W-22 排 08 輪（依 R-VS27 之新判準）；W-17／W-24／DR-14′／`unesc()` 併模組排 09 輪。**

---

## 1. 預期 vs 實測（相符者亦列出）

| 項 | 預期 | 實測 | 判定 |
|---|---|---|---|
| Comfort 037 leaf 總數 | 498 | **498** | 符 |
| Comfort 命中（子字串、無詞界） | 43（00D 之上界） | **43** | 符 |
| **Comfort 命中（詞界）** | 未給 —— 本輪須重測 | **43**（v2）；集合與子字串版**完全相同** | **00D 之上界實為精確值** |
| 本 feature 母體 | 237 Functional leaf | **237** | 符 |
| `{CFTS043}` 引用處數 | 3 | **3** | 符 |
| A-VS31 之依據 | `VentedSeatFL` 作 `1= Fail Present` | 逐字 `'1= Fail Present'`；`HeatedSeatFL` 作 `'1 = Fail_Present'` | 符 |
| 配對具名率 | 升級門檻為「無具名者 > 三成」 | **43 / 43 具名，無具名 0（0%）** | **未觸及** |
| Layer 3 全集 | 未給 | **18** 個 | 新測 |

---

## 2. 不符項目（不自行調和）

### 2.1 我方之假陰性：詞界 v1 得 30 而非 43

首版以 `\bseat\b` 重測得 **30**，較子字串少 **13**。逐筆檢視後
**13 筆全為複數形 `seats`**（`heated/vented seats` 一類）——
`\bseat\b` 於 `seats` 內不匹配。

**這不是「詞界濾掉偽陽性」，是我的正則產生假陰性**（canon §5a 條 7）。
v2 改為 `seats?` 後復原為 43，且與子字串版集合完全相同。

> **若止於 v1 而回報「詞界重測得 30，00D 高估 13」，那會是一個錯誤的更正** ——
> 且因為方向看起來「更嚴謹」而不易被質疑。

### 2.2 配對依據之一與之二於原理上不成立（**非不符，是前提有誤**）

下放包列三種依據：共同訊號名／共同 CFTS044 章節／共同 UI 元件。
實測 Comfort 037 之訊號層探針**全為 0**，`Source Requirement ID` 命名空間亦不相交。

**故前二者不可能成立。** 本輪改以**同一實體功能**（R-VS4 之 Layer 3）為依據
—— 其可具名、可逐條驗證，且不是「僅關鍵詞相同」。

**此非自行調和**：未改任何數字，而是回報「原擬之依據於此資料上不存在」，
並具名替代依據與其判準式（報告 §3 之七條正則）。

---

## 3. 結果三分法（canon §8.4）

| 分類 | 項目 |
|---|---|
| **改對了** | 詞界正則由 v1 修為 v2（複數形）；配對依據由「共同訊號名」改為「同一實體功能」並具名其判準式；R-VS27／R-VS28 轉錄；A-VS31 開立；W-27 錨點措辭補記 |
| **核實無誤** | Comfort 498 leaf、43 命中、237 Functional、`{CFTS043}` 三處、A-VS31 之逐字證據 |
| **正確地不動** | 「未分左右」之 36 條**未強行判側別** —— Comfort 原文即未指明；`{CFTS043}` 三處**未列入配對來源** —— 其主題與本輪 43 條無交集，僅為分層原則之佐證 |

---

## 4. 本輪實際使用之掃描條件（canon §5a 條 1／2／4／5）

| 對象 | 條件 |
|---|---|
| Comfort 037 | `Analysis Report`；表頭列 7；資料自列 8；**A 欄非空 = leaf**（498） |
| 命中判定之欄位 | **A–E 欄合併**（SWE ID／Source Requirement ID／HMI Source ID／Title／Description），**不分大小寫** |
| 子字串版（00D 之條件） | `heated seat`／`vented seat`／`heated steering`／`seat heat`／`ventilat`，**無詞界** |
| **詞界 v2** | `\bheated\s+seats?\b`／`\bvented\s+seats?\b`／`\bheated\s+steering\b`／`\bseat\s+heat`／`\bventilat` —— 名詞允許複數；`ventilat` 為詞幹故只要求**左**詞界 |
| 本 feature 母體 | `data/leaves.tsv` 減去 `data/non_functional_leaves.tsv` = **237**（R-VS15） |
| Layer 3 | `SWE1-VC-(.+?)-\d+$` 之中段（R-VS4） |
| 配對式 | **七條具名正則 → Layer 3 清單**（報告 §3）；側別判定要求方位詞與功能詞**相距 ≤ 24 字元** |
| 訊號層探針 | `\$[A-Za-z0-9_]+\$`／`\bSTATUS_[A-Z0-9_]+`／`\b\w+(?:Sts\|_Req)\b`／`\bCAN\b`／`\b[A-Z][A-Z0-9_]{3,}\.\w+\b` |
| `{CFTS043}` | CFTS044 原始 docx 之 body 段落文字，字面比對，各取前後 200 字元 |

---

## 5. 新開 anomaly 與 DATA_REQUESTS（成對）

| anomaly | 配對 DR | 內容 |
|---|---|---|
| **A-VS31** | —（RD-1 FYI） | LID 表之 `VentedSeatFL` 作 `1= Fail Present`（`=` 前缺空格、底線遺漏），`HeatedSeatFL` 作 `1 = Fail_Present`。**足以使字面比對誤判為不同值** |

**無新開 DR。** A-VS29 條目已補記 W-27 錨點 (a) 之措辭更正（判準為條目數 = 11）。

---

## 6. 獨立判斷：本包是否仍有該驗而未驗者 —— **有，五項**

1. **「未分左右」之 36 / 43 未逐條讀原文確認其確實涵蓋兩側。**
   本輪之判定為「措辭未指明側別 → 對應兩個 leaf」，
   **未驗證該 36 條之畫面行為是否真的左右對稱**。若某條實際只涉一側，本表會多指一個 leaf。

2. **配對為 Comfort → 本 feature 之單向。** 反向未做 ——
   **本 feature 之 237 個 Functional leaf 中，有哪些應委派而本表未涵蓋，未查。**
   R-VS7(a) 之委派句是從本 feature 側發起的，故反向才是使用時的查法。
   **本表目前只支援「已知 Comfort leaf，查其對應」，不支援「已知本 feature leaf，查該不該委派」。**

3. **Comfort 之 43 條未逐條讀其 `Requirement Description` 全文。**
   配對依據取自標題 ＋ 描述之正則命中，**未人讀確認其畫面行為與本 feature 之訊號行為確為同一實體功能之兩面**。

4. **R-VS7(b) 之 16 個 BLOCKED leaf 與本表之關係未交叉。**
   報告 §6 記其「不走本表之委派」，**但未列出該 16 leaf 之 id**，
   亦未驗證其是否與本表之對應 leaf 重疊。**重疊者其處置為何，未定。**

5. **`{CFTS043}` 三處與 43 條無交集，此事實本身未追因。**
   CFTS044 顯式引用 Comfort 之三處，主題皆非座椅／方向盤加熱；
   **即「重疊最多之 43 條，CFTS044 反而未顯式引用 Comfort」**。
   其可能意味分層是隱含的而非文件明示 —— **未驗，登記。**
