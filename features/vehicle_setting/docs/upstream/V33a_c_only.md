# 上繳 V33a —— V33 §C 三處置（僅 §C；§D 由 VS 線 session 執行）

日期：2026-08-24
下放包：`docs/handoff/V33_number_hygiene_wvf71.md` **§C**
執行層：Claude Code（改善案 session，依裁定記錄 3「代擬一次」）
範圍：**僅 §C 三處置**。§D 六項（W-VF71，含 250 條 TC 生成）依
Pei 2026-08-24 裁示 (a) **歸 VS 線 session**，本包不執行、不預作。

**git**：未執行任何 git 操作（V33 §A）。
**`docs/fw036/` 與共用腳本**：全程唯讀，未改一字（§A）。
**`RULINGS.sha.tsv`**：未動、未重產、未以其跑 `--check`（§A、§C-3）。
**`RULINGS.md` 之改動**：僅 §A 所列三類 —— 檔尾追加 V33 節、
R-VF94 區塊後加註、R-VF73／R-VF74 兩處引用改指。**三類之外未動一行。**

---

## 一、R-G13 引用回報表

全域條文帶 sha；VS 條文依 §C-3 之凍結標 `tsv-pending`。

| ruling_id | 下放引用 | 實讀 | 判 |
|---|---|---|---|
| `R-G5` | `9814d24c` | `9814d24c` | **符** |
| `R-G13` | `abdc56e3` | `abdc56e3` | **符** |
| `R-G18` | `8f61f9fd` | `8f61f9fd` | **符** |
| `R-G22` | `bca29f8f` | `bca29f8f` | **符** |
| `R-VF18`／`R-VF91`～`R-VF95` | —— | `tsv-pending` | §C-3 凍結 |

**四項全域條文全符，無不符。**

---

## 二、§C-1 取號實測（§E-1）

**取號時點為落檔前一刻**（R-G23′：落檔當下 live 取號，不得預先配號）：

```
$ grep '^### R-VF' features/vehicle_setting/RULINGS.md \
    | grep -oE 'R-VF[0-9]+' | sed 's/R-VF//' | sort -n | tail -3
92
93
94
→ 現行最大 R-VF94，live 取號 = R-VF95
```

**實測 `R-VF95`，與 §E-1 預期相符 —— §F-1 未觸發。**
該實測輸出已逐字記於 `RULINGS.md` 之「VF230 線 —— V33 包」節標頭，
使其可事後覆核。

---

## 三、§C-1 逐字比對輸出（程式，非目視）

**抽取以內容定錨**（R-VF21／R-VF28：不以行號定位）：自作廢區之
`### R-VF71 —— ER 與 procedure 之動詞取自條文；量測須機械執行` 之
fenced block 內，取 `**一、` 至 `**二、` 之前為一款、`**三、` 至末為三款。

比對輸出：

```
一款：      12 行 vs 12 行 → 逐字相同
二款(原三)：10 行 vs 10 行 → 相異 1 行
    來源: **三、`PRE_FORBIDDEN` 等列舉須標其性質。**
    本條: **二、`PRE_FORBIDDEN` 等列舉須標其性質。**
```

**全文唯一之差異為款號** —— 原「**三、**」於本條為第二款，改為「**二、**」。
其餘每一字元相同。該差異已於 `R-VF95` 之註記內逐字具名。

**原二款不入本條**：其已由 `R-VF73`（分析層不列無法機械執行之量）
逐字撤銷取代 —— 該條載「R-VF71 二撤銷，以本條取代」。
**作廢區之原文一字不動**（R-VF18；§A）。

**`R-VF95` 與作廢區並存，不互相取代**：本條為其現行效力之落點，
作廢區為其歷史之落點。

---

## 四、§C-1 兩處引用改指（§E-2）

| 所屬條文 | 改後文字 |
|---|---|
| **`R-VF73`** —— 分析層不列無法機械執行之量 | 「**R-VF71 二撤銷，以本條取代。** 其餘二款不受影響 —— **現行落點為 R-VF95 之一、二款**（V33 §C-1 自作廢區逐字補落；原號維持作廢不重用）。」|
| **`R-VF74`** —— 範例查證之義務及於每一種將被書寫之形態 | 「下放包所列之形態為**已知者**，非全集（**R-VF95 二**之同一性質；該款於 V33 §C-1 自作廢區補落，原號維持作廢）。」|

**改指之作法須具名**：本層初版僅於原句後補一句指路，
**舊 token 因而仍在，§E-2 之 0 不成立**。已改為真正之改指 ——
**移除 `R-VF71 之一`／`R-VF71 三` 之字面 token，保留其事實陳述**。

**§E-2 實測 = 0**（現行效力區）。餘三處落點與其豁免依據：

| 區 | 處 | 依據 |
|---|---|---|
| 「執行層註（V32 改號輪）」 | 2 | **裁定記錄 1 明令「升格為正式記載，不動」** |
| `R-VF95` 之沿革句 | 1 | §E-2 逐字豁免 |

> **具名一項相接**：§E-2 之豁免文字只列「作廢區與 R-VF95 沿革句」，
> **未列 V32 執行層註**；而裁定記錄 1 令該註不動。二者於此相接，
> 本層依裁定記錄 1（較具體且為 Pei 直裁）不動之。**該相接本身未經裁定。**

---

## 五、兩條之 sha 將變 —— 具名（§C-1 末項所令）

因引用改指而**文字變動**者二條，依 R-G22@`bca29f8f` 其 sha 將換發：

| 條 | 變動內容 | 現況 |
|---|---|---|
| **`R-VF73`** | 末段一句改寫（改指 R-VF95）| **sha 將變**，本包未計其新值 |
| **`R-VF74`** | 內文一處改寫（改指 R-VF95）| **sha 將變**，本包未計其新值 |

**本包未重產 tsv**（§A、§C-3），故二者之新 sha 未產。

**tsv 重產時另須併入**（供全域收尾包參照）：
`R-VF95`（新條，首產）、`R-VF91`～`R-VF94`、`R-VF83` 但書後之新值、
`R-VS83`，以及本節之 `R-VF73`／`R-VF74`。

---

## 六、§C-2 R-VF94 之節號判定

**判定：`FO §8.7`（落檔與封存）。** 條文本體**不動一字**（逐字轉錄，FO §8.1），
判定以 R-VF94 區塊後之加註記錄。

**三項依據**：
1. `IN §8.7` 為 Cross-Domain Behavioral Patterns（門檻／相似操作／變體標籤），
   與對照表之執行無涉
2. 本檔同句三處（`RULINGS.md` 之 L12／3731／3794）於 25 輪均判 `FO §8.7`
3. **本層獨立實讀其上下文**：該句為「其正文見
   `docs/handoff/V25_pilot1v3_review.md` §4，……不重複轉錄」，
   其語境為**落檔位置與不重複轉錄**，與 `FO §8.7` 之內容一致

**§F-5 未觸發** —— 判定與上下文相符，未見矛盾。

**ambiguous 該筆暫留不抑制**（§C-2 所令）。其消解須把
`verbatim-ruling-text` 判準擴至 feature `RULINGS.md` 之條文圍籬，
而該判準在 `scripts/canon_refs.py` 內 —— §A 令不碰。**已具名，未抑制。**

---

## 七、§C-3 tsv 凍結

`docs/fw036/RULINGS.sha.tsv` **全程未動、未重產、未跑 `--check`**。
本上繳之 VS 條文 sha 欄一律 `tsv-pending`。

---

## 八、§E-3 之實測 —— **不是 11，是 23**（不自行調和）

| 指標 | 預期 | 實測 |
|---|---|---|
| `canon_refs --waiver` unresolved | 0 | **0** —— **符** |
| 同 ambiguous | **11 維持** | **23** —— **不符** |

**「11 維持」之基準已失效，且其失效與本包無關。**
`canon_refs` 掃**全 repo**，其數受任何 session 於任何檔之任何改動影響；
11 為 26 包成文時之值。逐檔歸因（唯讀）：

| 處 | 檔 | 屬誰 |
|---|---|---|
| 9 | `features/power_moding/`（`ANOMALIES.md`／`docs/INDEX.md`／`docs/{handoff,upstream}/12_phase4_batch1.md`）| 併行 session（power_moding 線）|
| 1 | `features/vehicle_setting/docs/handoff/V32_renumber_ruling.md` | 併行 session（VS 線）|
| 4 | `features/vehicle_setting/docs/handoff/V33_number_hygiene_wvf71.md` | **本下放包自身**（其述及 §C-2 之歧義筆）|
| 4 | `features/vehicle_setting/RULINGS.md` | 1 為 R-VF94 條文本體（既有，§C-2 令暫留）；3 為 V32 執行層註（裁定記錄 1 令不動）|
| 2 | `features/vehicle_setting/docs/upstream/V33_number_hygiene_wvf71.md` | **本層之前一版上繳**（見 §九）|
| 2 | `docs/fw036/upstream/26_wp3_closeout.md` | 改善案 26 包（另線處理）|
| 1 | `features/power_moding/docs/upstream/12_phase4_batch1.md` | 併行 session |

**未以重產 waiver 消解之** —— §A 令 `docs/fw036/` 不碰，且重產屬「增」，違 R-G18。

### 八-1 一項更正：本層先前所報之「本包所改之檔零命中」**不準確**

實測後更正：本層自撰之 **C-2 加註**曾命中一次 —— 其為描述該歧義筆而
**逐字複寫了那個歧義字串**，工具遂把「描述」當成「引用」。**已改為
不觸閘之表述**（以「canon」加裸節號 8.7 述之），現 `RULINGS.md` 之 4 處
皆為既有或裁定令不動者。

> **其可一般化，且與 `verbatim-ruling-text` 同源**：
> `canon_refs.py` **分不出「提及」與「使用」** ——
> 一篇說明某引用為何歧義的文字，其本身就成為一個歧義引用。
> 本層於 23a～26 各包已數次繞開它（改寫為「加裸節號」式），
> **但那是人記得繞，不是工具知道**。
> **建議**：判準加一類 `mention-not-use`，或於 waiver 之 reason 增此類。
> **屬共用腳本，本包不碰（§A）；具名待裁。**

### 八-2 一項判準問題（建議，待裁）

`canon_refs --waiver` 之 FAIL 為**全 repo 之單一數字**，而 repo 現由
多個 session 並行改動。**一個 feature 之下放包無法為該數負責** ——
其預期值在成文與交付之間必然漂移（本包實測即由 11 漂到 23）。

**建議**：feature 級下放包之該指標改量「**本包所改之檔之 FAIL 數**」；
全 repo 之數留給全域線。本包依該定義之實測為 —— `RULINGS.md` 4 處，
**其中 0 處為本包新引入**（1 為既有條文本體、3 為裁定令不動之記載）。

---

## 九、前一版上繳之處置

本層於裁示前已寫出 `docs/upstream/V33_number_hygiene_wvf71.md`
（含 §D 歸屬之討論）。依 Pei 裁示 (a)，上繳改為本檔（僅 §C）。

**前一版保留不刪**，已於其檔頭標 `[SUPERSEDED by V33a_c_only.md]`
（R-VF18、R-G23 之處置：另一份標 [SUPERSEDED] 不刪）。

---

## 十、獨立判斷 —— 本包是否仍有該驗而未驗者

1. **`R-VF95` 之 sha 未產**（§C-3 凍結），故本條**現在無法被
   `R-XX@sha8` 引用** —— 在 tsv 重產前，引用它只能用編號，
   即 R-G13 生效前之狀態。**VS 線於 V34 引用本條時須注意此點。**
2. **逐字比對只比了一、三款兩段。** 未反向驗證「未入本條之部分
   （首行標題句、原二款）是否確實未被誤帶入」。
3. **`R-VF73`／`R-VF74` 之改寫未經第三方複讀。** 其 sha 將變，
   而**改指後語意是否等價**只有本層自己讀過。
4. **§E-2 之「現行效力區」無機械定義。** 本層以「非作廢區、非 V32
   執行層註、非 R-VF95 沿革句」判之 —— **人讀而非程式**。
   若要可重測，須先定義其機械判準。
5. **§D 未執行亦未預作。** 本包未觸碰 `data/vf230_isolated.tsv`、
   `generated/`、任何生成腳本 —— **VS 線受領時之狀態與 V33 成文時相同**。
