# 上繳 V10 —— 八條落檔、Layer 2 提案、覆寫層攔到真實回歸

執行層寫入。依據：`docs/handoff/V10_four_rulings.md` §9（4 項工單）＋
§2–§8 之 R-VF24–R-VF31、A-VF7。canon §8.2 六節。

**本輪未生成 TC、未執行任何寫回**（R-VF26）、**`framework.md` 未寫入**。
唯一之產物變更為覆寫層之修復（見 §3），其為回復既有裁定之狀態，非新變更。

---

## 1. 交付總表

| 項 | 狀態 | 產物 |
|---|---|---|
| R-VF24–R-VF31 落檔（8 條） | **完成** | `RULINGS.md`（逐字，現 R-VF 共 22 條） |
| A-VF7／A-VF8 | **完成** | `ANOMALIES.md` |
| W-VF25 Layer 2／3（**最優先**） | **完成（提案待核）** | `docs/reports/wvf25_layer2_proposal.md`、`scripts/vf230_wvf25_layer2.py` |
| W-VF26 reasoning gate | **完成（設計待核，未實作）** | `docs/reports/wvf26_reasoning_gate.md` |
| W-VF27 覆寫層補強 | **完成（含逐欄實測）** | `scripts/grade_overrides.py` |
| W-VF28 揭露文字 ＋ 舊制標記 | **完成（草稿，未送出）** | `docs/reports/wvf28_disclosure_draft.md`；`ANOMALIES.md` 7 筆標記 |

---

## 2. W-VF25 —— Layer 2／Layer 3 提案

### 2.1 ⚠ 一項 R-VF25 作成時未有之量測：**12 個簇被邊界對切**

```
跨兩個分報告族群之 Title 簇   12 個 ／ 72 leaf
每一個之分裂                  恰 3 / 3
標的                          SWITCH 1–4 之 Power Mode／Type／Hold Last State
```

**依粒度 D 之字面（11 份分報告），該 12 個功能各被切成兩個 Test Set 各 3 條。**
R-VF25 與 V07 §5.3 皆以「12–131 leaf、中位 52」論其均勻度，**未測簇之跨界**。

**將三個 SWITCH 相關族群合併後，跨界簇歸零**（131 leaf）。

### 2.2 兩案並陳（本層不擇一）

| | Test Set 數 | 最大 | 最小 | 跨界簇 | 偏離 R-VF25 字面 |
|---|---:|---:|---:|---:|---|
| **A（11 族群，字面）** | 11 | 131 | 12 | **12** | 無 |
| **B（三 SWITCH 族群合併）** | 9 | 131 | 12 | **0** | 有 |

11 個 Test Set 名之提案、逐族群之命名依據、Layer 3 對照、
canon §4.1.3 兩型態之實數，見 `docs/reports/wvf25_layer2_proposal.md`。

**命名合規**：11 個名皆 1–3 字名詞片語、無 `Vehicle Setting` 前綴、
無括號標籤、無 `Report`／`features`／`^`／` - ` 之檔名殘留。

**須具名之異質性**：`Trailer and Signage`(131)／`Driver Convenience`(99)／
`Approach and Tailgate`(57) 三者族群內最大簇僅佔 25%／6%／14%。
**名只能取其較大之主題。** 若不可接受，須改粒度而非改名。

### 2.3 Layer 3 之一處無解

無 spec 章可掛者 **2 簇**：`E-Save`(6 leaf)／`CHMSL CAMERA DYNAMIC CENTERLINE`(5)。
依 R-VF25 配套 3「不得自創標籤」，**本層未為其造章名**。
處置待裁：(a) Layer 3 留空／(b) 待 spec 修訂／(c) 循 DR 確認。

**`framework.md` 未寫入、未鎖**（R-VF25：本條只裁粒度）。

---

## 3. W-VF27 —— 覆寫層補強，**且於本輪攔到真實回歸**

### 3.1 回歸（A-VF8）

`--check` 於本輪首次執行即報 **8 項不符並 exit 1**：R-VF17 之 4 leaf
全數回復 `W2`／`B6-value-absent`。

**追因：併行線之 `70b75d0`（rounds 43/46）重跑 `writability_driver.py --write`，
且已 commit。** 覆寫被靜默回復並進入 git 歷史。`--apply` 已修復。

**此為已預告之風險成真** —— 上繳 V07 §2.1／§7 第 1 項逐字稱
「driver 若重跑，本變更會被回復為 W2」「其存續繫於『沒有人跑 driver』，
這不是一個可依賴之狀態」。**R-VF20 之立法目的因而獲完整驗證**：
若無 `--check`，Pei 之裁定會被一次例行重跑抹除而無人察覺。

### 3.2 R-VF29 之兩項

**第 6 項（檢查面 = 變更面）**：`--apply` 與 `--check` 現**共用單一之
`expected_row()`** 定義四欄終態 —— 二者之相等**由結構保證**，
非由兩處各自維護欄位清單。

逐欄可失敗性實測：

| 欄 | 改壞後 `--check` |
|---|---|
| `writable` | exit 1，訊息命中該欄 |
| `blocker_class` | exit 1，訊息命中該欄 |
| `blocker_detail` | exit 1，訊息命中該欄 |
| `evidence_note` | exit 1，訊息命中該欄 |

**第 5 項（註記不累積）**：改為依清單重生，重跑三次仍為一筆。

### 3.3 一項本條未能解決者

`RUNBOOK.md`／`PLAYBOOK.md` 已載「跑 driver 後必跑後置步驟」，
而**併行線未跑**。**文件之可見度不足以構成保證。**
建議改由 driver 自身於結束時呼叫或提示 —— **本層未改 driver**（R-VF20 明令）。
**在此落實前，本輪之修復同樣可能被下一次重跑抹除。**

---

## 4. W-VF26／W-VF28

**W-VF26**：`reasoning` gate 之設計見 `docs/reports/wvf26_reasoning_gate.md`。
擬掛於 `lint036.py`，規則以覆寫清單為判準來源（非內嵌條件式），
附**三個**錨點（含鑑別錨點 —— 若無之，一條過寬之「所有 TC 皆須含 reqid」
規則亦會通過前兩個）。**未實作**（V10 §9 令先提設計）。
三項未決具名：規則編號是否與 `L-VS*` 衝突、掛於共用 lint 抑或另立、FAIL 抑或 WARN。

**W-VF28**：揭露段落草稿見 `docs/reports/wvf28_disclosure_draft.md`。
三項約束逐項對照（不得寫成「委派他 feature」／不得以 1087 為分母／
8 列偏離須揭露）。**草稿未併入任何交付包**。
其中「已另行向上游確認」一句指 DR-30，依 R-VF27 其送出屬 Pei —— **本層不預設其已送出**。

`ANOMALIES.md` 之舊制標記（R-VF10 所令）已補：A-VS129／130／132–136 共 **7 筆**
各加「**【VF230 線舊制編號，R-VF10 前所開，保留】**」。此為上繳 V09 §5 第 2 項
自承之「本可做而未做者」，本輪補上。

---

## 5. A-VF7 —— 其所令之處置本層實測會破壞消費者，未施行

V10 §8 令「改以檔內首行加註」。**實測該做法會破壞 `leaves.tsv` 之消費者**：

```
scripts/writeback_036.py:59  csv.DictReader(f, delimiter="\t")
→ 加一行 `#` 註記後，fieldnames 變為該註記字串本身（實測），
  swe_id／family／src_ref／title／desc 全數取不到
```

**改以旁註檔 `data/leaves.tsv.README` 履行同一目的**（名實對齊、低成本、
不動引用）。`leaves.tsv` **未動一個位元** —— 加註後已自 git 原樣還原並驗；
該檔為 **CRLF ＋ LF 混用**，任何重寫皆會全檔正規化（實測 272 增 272 刪）。

**通則**：對既有資料檔之「低成本註記」須先驗其解析路徑 ——
**註記之載體本身可能即為資料格式之一部分。**

---

## 6. 本包是否仍有該驗而未驗者 —— 獨立判斷

**有，四項。**

1. **本輪之覆寫修復可能再度被抹除**（§3.3）。文件之可見度已證不足
   （併行線讀過 RUNBOOK 與否不可知，但確實未跑）。**在 driver 自身
   呼叫後置步驟之前，R-VF17 之效果仍不可依賴。** 此為本輪最須處置者。

2. **W-VF25 之 12 個跨界簇未經上游查證。** 本層只測其「跨兩份分報告」，
   **未查上游為何將同一功能之 6 條分置兩份** —— 其可能為分報告切分之
   偶然，亦可能有本層未知之理由。**在該理由未明前，提案 B（合併）
   之正當性只立於「跨界為害」，未立於「上游本意」。**

3. **`docs/INDEX.md` 之「舊路徑對照」一節未寫**（R-VF31 二所令）——
   其內容為搬移之結果，而搬移屬 Pei 且未發生。**依賴順序正確，
   但此為一項已受令而未產出者，具名。**

4. **W-VF13 之改名部分仍未執行**，`61`／`62`／`63` 三號於 handoff 側
   各有兩義。本輪只補了其 `ANOMALIES.md` 標記部分。

**另有一項為本輪之方法教訓**：A-VF7 之處置與 W-VF20 之逐行覆寫（A-VF6）
為**同一形態之第二例** —— 一個看似低成本之修改（加一行註記／以行號為鍵），
其失效路徑不在修改者之視野內（解析器把註記當表頭／編輯使行號位移）。
**二者皆為「載體之語義未被檢查」。** 建議：凡對既有檔之修改，
落筆前先問「誰在讀它、怎麼讀」。
