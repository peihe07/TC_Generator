# V60 — B 欄採 (a) 硬值 244–681；LID 之前置更正；實寫啟動

下放包 **V60**。所據上繳：`docs/upstream/V59_bcol_inputs.md`（2026-08-25）。
新增 **二條**（開號取實測最大 +1／+2，**回報實得之號**）、**W-VF86**（實寫）。

---

## 1. 三處具名嘉許

1. **§二 —— 以 sheet XML 直讀取代值介面，得出可辯護之結論。**
   ```
   交付路徑  <c r="B10" s="156"><v>1</v></c>              ← 無 <f>，樣式 156
   repo 內   <c r="B10" s="78"><f t="shared" …>…</f><v>1</v></c>  ← 有 <f>，樣式 78
   ```
   **公式與樣式一併改變**，其形態符合「複製 → 選擇性貼上為值」；
   配合 `mtime 2026-08-24 11:40`／`lastModifiedBy PeiPYHsu`，
   **判為 Pei 親為之操作**。
2. **§二末之自我更正分寸準確** —— 「先前之結論正確而其證據不足」，
   **非結論之更正**。二者之別多被混為一談。
3. **§三 —— 建議「不改」並附理由。** PROXI 二本逐位元相同，
   **「改之只增一處跨目錄依賴而無任何收益」** —— 其為對本層之令提出反對，
   且理由立於實測。

---

## 2. 甲 —— B 欄採 (a)：硬值 244–681

```
R-VF-甲（B 欄之處置，分析層裁定 2026-08-25，
為 Pei 於 2026-08-24（上繳 V45 §〇-A）之裁定之延續）
【落檔取實測最大 +1】

**B 欄寫入硬值 244–681（438 列，列 10–447）。採 V58 §2.1 之選項 (a)。**

**其非新裁定，而是既有裁定於模型更正後之續存判斷（R-VF58）**：

  Pei 之裁定為「**VF230 之 B 欄自 244 起**」（V45 §〇-A，明示；
  其前於 V01 之原始指示「編號順序要從 CFTS044 的最後一份開始」為同一意旨）。
  **該裁定所指者為「交付本上顯示之編號」，非「工作簿內部之計算機制」。**

  `R-VF129` 所撤銷者為**機制之模型**（二本共用一個 `ROW()-9` 序號空間），
  **而該模型之為假，不觸及「VF230 之編號應自 244 起」此一意旨。**
  **理由被更正，而結論仍被新事實所支撐** —— 見下三項。

**支撐之三項實測**：
  一、**交付本之 B 欄已為硬值 1–243，且係 Pei 於 8/24 親為**（上繳 §二）
      —— 「公式轉硬值」為該交付系列之既成狀態，(a) 與之一致。
  二、**選項 (b) 之 1–438 將使 VF230 與 CFTS044 之 B 欄重疊 1–243**，
      二本若被併看，其序號不可分辨。
  三、選項 (b) 尚須補列 247–447 之公式，**其為對 Pei 之交付母本之結構性修改**。

**代價須寫入交付說明，不得省略**：寫硬值將覆寫 repo 內副本之 **237 格公式**，
**該本之自動編號機制自此失效**（日後增刪列不再自動重編）。

**Pei 之否決權保留**：若其原意為「留公式自算」，
**回「公式」二字即撤本條**，改補列 247–447 之公式並留 B 欄空白。
**本層不再就此另問** —— 已問二輪，續問之成本高於其收益。
```

---

## 3. 乙 —— LID 之施行前置更正；PROXI 不改指

```
R-VF-乙（LID／PROXI 路徑之處置，分析層裁定 2026-08-25）
【落檔取實測最大 +2】

**一、PROXI 不改指，維持 `inputs/`。核可執行層 §三之建議。**
  二本 sha256 逐位元相同（`e7c2020f01c3d58d`／743,785 bytes），
  **改指為零差異之操作，其只增一處跨目錄依賴而無收益。**
  `R-VF125`（`forms/` 引用不複製）之立法目的為**防分岔**，
  **而二本既為同一檔，其無分岔可防。**
  **本項自 W-VF85.4 移除。**

**二、LID 之施行前置為「改 `lid_parse.py` 之來源路徑」，非「搬入 `inputs/`」。**

  **上繳 §三之前提有誤**：其稱「LID v1_78 現仍在 `forms/`，未搬入；
  搬入屬 Pei，本層無法施行」——
  **而 V50 §6 之「請 Pei 搬入」已由 `R-VF125` 撤回**
  （Pei 逐字：「v1_78 我就放在 forms 是全專案共用」）。
  **本層不再搬檔；`forms/` 之素材由各 feature 引用。**

  **故其施行為**：
  ```
    lid_parse.py 之來源 xlsx 改指 forms/Logical Identifiers and CAN Mapping v1_78.xlsx
      → 重產 data/lid_pairs.tsv
      → writability_driver.py 讀之（其讀取點不變）
  ```
  **不需 Pei 之任何動作。** 依 `R-VF33` 先產 `lid_parse.py` 之消費者清單
  （誰呼叫它、其產物被誰讀），無清單即退回。

  **重產後須比對新舊 `lid_pairs.tsv`**，差異逐項回報；
  **其影響 438 列者具名。R-VF123 已准換本，本項為其施行，不另求裁定。**
```

---

## 4. W-VF86 — 實寫（**啟動**）

**W-VF85 之各項依 `R-VF131` 逐項編號，其未辦者移入本工單。**

- **W-VF86.1** **閘 3（x14 保全）與閘 4（寫入後重讀比對）之空跑驗證**
  （上繳 §五-2：二閘從未執行，其程式碼未經運行驗證）。
  **於 scratch 副本上先行**：人為破壞 `<extLst>` → 閘 3 須停；
  人為改一格使其與預覽表不符 → 閘 4 須停。
  **二閘皆證其可失效後，方得進入 W-VF86.2。**
- **W-VF86.2** B 欄依 `R-VF-甲` 寫硬值 244–681；
  **寫入前後之 B 欄逐格比對，載明覆寫之公式格數**（預期 237）。
- **W-VF86.3** 實寫 438 列至 `inputs/` 之 repo 內副本（`R-VF112`）；
  x14 依 `R-VF113` 補回並複驗 `x14dv` 之數；
  寫入後重讀該檔與預覽表**逐欄比對（438 × 14）**，差異即停。
- **W-VF86.4** 實寫完成時**明示提示 Pei 之 Excel 確認**（`R-VF116`），不得靜默結束。
- **W-VF86.5** LID 依 `R-VF-乙` 二施行（改 `lid_parse.py` 來源、重產、比對）。
- **W-VF86.6** **`LanguageSelection` 之 `22: Slovenian` 消失之原因**（上繳 §五-3）
  —— 查其於 `BHCAN`／`BHCAN2` 二側之歸屬與 message 之改組；
  **只查不改**；若查無定論則開 DR（登記，送出屬 Pei）。

**交付說明須載 `R-VF-甲` 之代價**（237 格公式被覆寫，自動編號機制失效）。

---

## 5. 給 Pei

**B 欄我裁了，實寫啟動。**

**其非新裁定**：你於 V45 親裁「244」，其所指為**交付本上顯示之編號**，
而我所撤銷的是**工作簿內部之計算機制之模型** —— **模型為假，不觸及你的意旨。**
且三項實測支撐它：交付本已是硬值 1–243（**8/24 你親自轉的**）／
留公式會使二本 B 欄重疊 1–243／留公式還須改你的交付母本結構。

**代價**：會覆寫 repo 內副本的 **237 格公式**，該本的自動編號機制自此失效。
**此已令寫入交付說明。**

**若你的原意是留公式自算，回「公式」二字即撤銷** ——
**我不再就此另問，已問二輪。**

**另一件已自解，不需你動手**：LID v1_78 之換用。
執行層以為要等你搬檔，**而 `R-VF125`（你說 forms 是全專案共用）已撤回搬檔之要求** ——
其施行改為「`lid_parse.py` 之來源改指 `forms/`」，不搬檔。

**實寫之後仍是那兩件**：以 Excel 開 repo 內副本確認下拉可用；
確認後**逐字具名檔名**複製至交付路徑。

---

## 6. 新條文清單（自檢）

| 暫稱 | 內容 |
|---|---|
| 甲 | B 欄硬值 244–681；為 Pei V45 裁定於模型更正後之續存；代價須入交付說明；Pei 回「公式」即撤 |
| 乙 | PROXI 不改指（二本同檔，無分岔可防）；LID 之施行為改 `lid_parse.py` 來源，不搬檔 |

**⚠ 二條之編號於落檔時取實測最大 +1／+2，並回報實得之號。**

**工單**：W-VF86（六項；閘 3／4 之空跑驗證為 W-VF86.2 之前置）。

**執行層上繳時須附「本包是否仍有該驗而未驗者」之獨立判斷。**
