# V34 — §D 歸本線；W-VF71 重發；全 repo 指標之範圍限縮

下放包 **V34**（VS 線自行產出，依 Pei 2026-08-24 之裁定記錄 3 更正）。
所據上繳：`docs/upstream/V33_number_hygiene_wvf71.md`（改善案 session 交付 §C）
＋ `V33a_c_only.md`。
新增 **R-VF96**（1 條）。**W-VF71 六項重發，執行者為本線之執行層。**

**編號現況（實測依據：上繳 §三-1）**：`RULINGS.md` 現行最大為 **R-VF95**，
故本包取 **R-VF96**。

---

## 1. §D 之歸屬 —— 歸本線，即刻執行

上繳 §六提二讀法待裁。**裁定：讀法 (a)** ——
「代擬一次」為**只擬不做**，§D 之六項（W-VF71）為 feature 執行工作，
**歸 VS 線之執行層**，非改善案 session。

**理由**：Pei 之更正逐字為「主對話僅管改善案（`docs/fw036` 全域線）」。
§D 之六項全數落於 `features/vehicle_setting/`，其產出為 250 條 TC，
**與 `docs/fw036` 全域線無交集**。

**改善案 session 之處置正確** —— 其未自行認定歸屬、未執行、且已交付
與其職掌相符之 §C。**§C 三處置本層核可**（取號、逐字補落、改指），
不重複覆核。

---

## 2. R-VF96 —— 全 repo 指標不作為 feature 級下放包之驗收條件

```
R-VF96（跨 session 指標之範圍限縮，分析層裁定 2026-08-24，
採納上繳 §四之建議）

`canon_refs --waiver` 之 FAIL／ambiguous 數為**全 repo 之單一數字**，
而 repo 現由多個 session 並行改動。

**一個 feature 級下放包無法為該數負責** —— 其預期值在成文與交付之間必然漂移。
實例：本輪 ambiguous 預期 11、實測 21，**增量全數來自
`features/power_moding/` 與 VS 線併行落檔，本包所改之檔零命中**。

**故：feature 級下放包之驗收條件，凡涉全 repo 之聚合數者，
一律改為「本包所改之檔之該數」。**

  ✗ 「`canon_refs --waiver` 之 ambiguous 維持 11」
  ✓ 「本包所改之檔之 ambiguous 為 0」

全 repo 之數留予全域線（`docs/fw036`）管理。

**本條不放寬任何判準** —— 其只更正歸屬。
一個無人能為之負責之數字，其作為驗收條件時只會被解釋掉，
**而解釋掉之後，真正的增量與併行之增量即不可分辨。**
```

---

## 3. 上繳 §七 之五項未驗者 —— 逐項處置

| # | 事項 | 處置 |
|---|---|---|
| 1 | `R-VF95` 無 sha，現無法以 `R-XX@sha8` 引用 | **認可**。tsv 凍結為 §A 所令，其解凍屬全域線。**在其重產前，VS 線之引用以編號為之** |
| 2 | 逐字比對只比二段，未反向驗證「未入本條者是否被誤帶入」 | **列入 W-VF71 第 0 項**：以 `R-VF95` 之 fenced block 反查，確認其不含 `R-VF71` 二款之任一行 |
| 3 | `R-VF73`／`R-VF74` 之改寫未經第三方複讀 | **本層即第三方**。已讀其改後文字，**語意等價**：二者皆保留原事實陳述、僅換指路 token。**核可，不需再驗** |
| 4 | 「現行效力區」無機械定義 | **暫不定義**。其為一次性之改號作業所需，作業已畢；**若日後再有改號需求方定之**，現在定義是為一個不再發生的動作立規 |
| 5 | §E-2 豁免清單與裁定記錄 1 之相接未經裁定 | **追認**：V32 執行層註不動之處置正確。裁定記錄 1（Pei 直裁、較具體）勝於 §E-2 之列舉遺漏 |

---

## 4. W-VF71 重發（**本線執行層，即刻**）

**新增第 0 項**（上繳 §七-2）：

0. 以 `R-VF95` 之 fenced block 反查，確認其**不含** `R-VF71` 二款之任一行；
   不符者回報並停。

原六項不變：

1. **100 條依 R-VF91 二解除隔離並生成**（送出型 28 條維持隔離）。
2. `reasoning` 之必要句（「條文未指定所收之值；值域取自 DBC，本條驗其代表值」）
   **列為自檢增項，缺之即 FAIL**。
3. **B 類 11 條開 DR**（PROXI 無值或無參數名，含 2 條連參數名都抽不出者），
   登記，未送出。
4. **`PLUMBING` 之「應刪而未刪」側實測**：以 `test_item` 長度離群者為線索逐條人讀。
5. **全庫「必不命中」錨點複驗**（R-VF92 一）——
   **已查者不重查**：上繳 §五已測「`max_row` 型缺陷未復現於現行腳本」，
   **餘者為 12 處落點之逐一獨立確認**（`RULINGS.md` 10／`PLAYBOOK.md` 1／
   `CROSSLINE.md` 1），於不經被驗程式之另一路徑確認其不命中之原因。
6. 重跑三批自檢（含新增項），**canon 判準與增項皆須為 0**。
7. **續生成第 2 組（3 批 150 條）**，與第 1 組併為一次上繳。

**驗收條件依 R-VF96 改寫**：`canon_refs` 之數以**本包所改之檔**計，須為 0；
全 repo 之數僅回報、不作為條件。

---

## 5. 抽樣覆核 —— **本次上繳必抽，不再順延**

V31 §5 已明訂終點。下次上繳時本層依 R-VF76 抽 20%
（每批 10 條、依 `form_index` 之形態分布取條），**以當時之最終產出為對象**，
不再等待組成穩定。

---

## 6. 給 Pei

**進度三數**（依 R-VF93，不換算期程）：
```
已生成待覆核    150
現行可直接書寫  476（376 ＋ R-VF91 解除之 100）
隔離             81
```

**待你**：DR-34（已縮為 2 個標的）／35／36／37／38／39／40 及 B 類 11 條之新 DR，
**皆未送出**。**DR-37（A 類 46 條之訊號名）仍為最實質者。**

---

## 7. 新條文清單（自檢）

| 編號 | 型別 | 區塊 |
|---|---|---|
| R-VF96（全 repo 聚合數不作為 feature 級驗收條件；改計本包所改之檔） | 分析層裁定 | ✅ §2 |

**工單**：W-VF71（重發，新增第 0 項；執行者為本線執行層）。

**執行層上繳時須附「本包是否仍有該驗而未驗者」之獨立判斷。**
