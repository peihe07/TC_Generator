# V62 — 實寫核可；LID 時序自解；交付僅餘 Pei 之二步

下放包 **V62**。所據上繳：`docs/upstream/V61_wvf86_written.md`（2026-08-25）。
新增 **二條**（開號取實測最大 +1／+2，**回報實得之號**）、**W-VF87**。

---

## 1. 實寫核可

```
閘 1  B 欄起始號 244（repo 237／交付 243）           ✅
閘 2  目標之列：資料 0｜公式 237                      ✅
閘 3  x14dv 2 → 0 → **2**（補回並以嚴格 parser 自驗）  ✅
閘 4  重讀比對 438 列 × 16 欄：**差異 0**              ✅

獨立複驗  169.71 KB｜資料列 438｜B 欄 244–681 連號
          D/G/H 非空 438｜F 非空 0｜AH 非空 65
          sheet6.xml x14dv 2、XML 合法
          B10 之 XML 無 `<f>`，237 格公式已覆寫
```

**`R-VF132` 之代價已具名**：repo 內副本之自動編號機制自此失效。
**其須入交付說明，本層再確認一次。**

---

## 2. 三處具名嘉許

1. **§二 —— 空跑不是「驗證通過」，是「驗證查出問題」。**
   閘 4 首跑即崩（`unbound prefix: line 3618`）——
   openpyxl 重寫 `<worksheet>` 根元素時丟棄 `xmlns:x14`／`xmlns:xm` 宣告，
   **只補 `<extLst>` 而不補宣告，其 XML 不合法。**
   **若無 V60 §W-VF86.1 之空跑，此缺陷將於首次實寫時發生於正式檔上。**
2. **§二末 —— 更正本層之宣稱，且指出其過強之處。**
   V45 §三稱替代路徑「已驗其形，未驗其用」——
   **而其時只驗了 `x14dv` 之元素數與 openpyxl 可讀（其解析較寬容），
   未驗 XML 之合法性。** **「已驗其形」本身就不成立。**
   本輪新增嚴格 parser 自驗，方使該語為真。
3. **§三 —— 閘 2 之修正明引 `R-VF130` 自問並自答「答否，故其為放寬」，
   再指其「經 `R-VF132` 裁定，非逕行」。**
   **一條本層立來管束他人之判準，被用來管束一次自己想做的修改，
   且結論是「這確實是放寬」而非為其開脫。**

---

## 3. 甲 —— LID 之時序自解：先換後驗，工作簿不動

```
R-VF-甲（LID 換本之時序，分析層裁定 2026-08-25）
【落檔取實測最大 +1】

**上繳 §六所慮之二難，其前提已由既有實測消解。**

執行層之顧慮為：LID 為值域來源鏈第一位，其換本**可能**改變 `writability.tsv`
之分級 → 選池 → 438 列之組成，**而 438 列已寫入工作簿**。

**而該「可能」已被量測過**（上繳 V49 §五，逐列人讀而非僅聚合數）：
```
  v1_76 vs v1_78 之差異觸及 VF230 之 158 訊號／56 PROXI 參數：**0 ／ 0**
  其變動之性質為**補全既有項之值域**（`CallAction`／`VC_HdRstPrsnt`），
  非增減 VF230 所用者
```

**故其施行順序為**：

  一、**先換**（`lid_parse.py` 來源改指 `forms/` → 重產 `lid_pairs.tsv`）
  二、**比對新舊 `lid_pairs.tsv`**，差異逐項回報
  三、**以新 tsv 重跑 `writability_driver.py`**，比對分級之逐級差異
  四、**差異為 0 者**（預期，依 V49 §五）→ **工作簿不動，於上繳具名「零差異」**
      **差異非 0 者** → **停手回報，不自行重寫工作簿**，其處置由本層另裁

**不預設其為 0** —— V49 §五之量測為 xlsx 層之列集合比對，
**而本項為 `lid_pairs.tsv` 與分級層之比對，二者非同一量測。**
**「已測過」不等於「這一層已測過」**（V55 §2 之誤即此形態：
本層以 token 層比對誤報值域相異 0，而執行層以解析鏈測得 1）。
```

---

## 4. 乙 —— 寫入前備份不入版控

```
R-VF-乙（`_vf230_036_prewrite_backup.xlsx` 之處置，分析層裁定 2026-08-25）
【落檔取實測最大 +2】

**不入版控，且得刪除。**

**其為冗餘**：寫入前之本（80.53 KB）**已在 git 歷史中**
（`inputs/…_SWQT_VF230_20260819.xlsx` 為已提交之檔），
**其還原以 `git checkout <commit> -- <path>` 為之，較旁檔可靠** ——
旁檔無版本資訊，其「是哪一次寫入之前」不可考。

**惟刪除之時點**：待 Pei 完成 §5 之二步（Excel 確認 ＋ 複製至交付路徑）**之後**。
**在其之前保留** —— 其為零成本之保險，而該二步尚未執行。

**`.gitignore` 增一行** `inputs/_*_prewrite_backup.xlsx`，
**使其形態化而非個案處理**（日後其他 feature 之寫回同受其益）。
```

---

## 5. 給 Pei —— **交付路徑上只剩你的兩步**

**438 列已寫入 `repo` 內副本，四閘全過，重讀比對差異 0。**
**交付路徑之 036 仍為 0 資料列** —— 其複製屬你（`R-VF112`）。

**第一步：以 Excel 開啟**
```
features/vehicle_setting/inputs/
  FM-WI-FSM-036-A01 STLA 測試用例規範與結果_SWQT STLA Test Case
  Specification & Result_SWQT_VF230_20260819.xlsx
```
**確認二事**：
- **下拉選單可用** —— x14 之 XML 已補回**且經嚴格 parser 驗其合法**
  （本輪新增；先前只驗元素數與 openpyxl 可讀，**其宣稱過強，已更正**）。
  **「合法」與「Excel 中可用」仍為二事，後者只有你能驗。**
- **版面** —— 438 列 × 16 欄之比對為機械，
  **其「在 Excel 中看起來是否正確」（欄寬、換行、對齊）未經人眼**（上繳 §八-3）。

**第二步：確認後複製至交付路徑**
```
/Users/peihe/Work/02_Project_R1LR/10_Reviewing/00_TestCase/ASW-R2/
  Vehicle Settings/VF230_V1_R5/
```
**逐字具名檔名，不得以萬用字元或目錄複製** ——
CFTS044 二本現分岔（repo 237 列／交付 243 列），
**誤覆蓋會使 Part 1 之 238–243 六列消失。**

**一項須入交付說明者**：B 欄之 237 格公式已被覆寫為硬值，
**該本之自動編號機制自此失效**（日後增刪列不再自動重編）。

---

## 6. W-VF87 — 收尾

- **W-VF87.1** LID 依 `R-VF-甲` 四步施行；**差異非 0 即停手回報**。
- **W-VF87.2** `.gitignore` 增 `inputs/_*_prewrite_backup.xlsx`（`R-VF-乙`）。
- **W-VF87.3** **交付說明之草擬**（其積欠已久：`R-VF12` 之 460 條揭露、
  `R-VF16` 之 8 列偏離、具名退化 27 條之彙總、隔離 84 條、
  本輪之 B 欄公式覆寫）—— **草稿，不送出**（`R-VF27`）。
- **W-VF87.4** `_vf230_036_prewrite_backup.xlsx` 於 Pei 完成二步後刪除；
  **在其之前保留**。

---

## 7. 一項本層之數已過時，記之

上繳 §七：AH 之數自 **69 降為 65**（`not clear` 63 ＋ 條文矛盾 2）。
其非缺漏 —— W-VF82 所隔離之 5 條中有 4 條屬 `not clear`。

**本層於 V52 §五所報之 69 為當時之數，其後未隨隔離更新** ——
**其正為 `R-VF119`（取得新事實時須檢驗既有結論）所指之形態，
而本層未對自己已驗過之數施行之。**

---

## 8. 新條文清單（自檢）

| 暫稱 | 內容 |
|---|---|
| 甲 | LID 先換後驗；差異 0 則工作簿不動，非 0 則停手待裁；不預設為 0（「已測過」≠「這一層已測過」） |
| 乙 | 寫入前備份不入版控、得刪（git 歷史較旁檔可靠）；`.gitignore` 形態化；刪除待 Pei 二步之後 |

**⚠ 二條之編號於落檔時取實測最大 +1／+2，並回報實得之號。**

**工單**：W-VF87（LID 施行、`.gitignore`、交付說明草擬、備份之刪除時點）。

**執行層上繳時須附「本包是否仍有該驗而未驗者」之獨立判斷。**
