# 76 下放包 — 實寫完成之覆核、R-VS70、49 輪（交付準備）

分析層寫入，2026-08-23。**036 母本已寫入 143 列，重讀比對不符 0 處。**

---

## 1. §2.2 —— **是我違了既有政策**

x14 之 dataValidation 遺失，其成因不在 openpyxl 之限制，
而在 **66 包 §4 令以 openpyxl 存檔母本** ——
**既有政策明令「xlsx 不得以 openpyxl 存檔」**（其理由正是 x14 之下拉會被丟棄）。

**我在寫貼回程序時未查該政策**（canon §5a 條 17：立新規則前先查既有政策），
且 repo 已有 `xlsx_surgical.py` 之作法可循。

```
R-VS70（分析層裁定 2026-08-23）
036 母本之寫回**不得以 openpyxl 存檔**。
其寫入須以 **XML 層之外科式修改**（開 zip → 改 `xl/worksheets/sheet*.xml`
之目標儲存格 → 原樣重打包），**不觸其餘 part**。

**寫回前後須逐項比對 raw XML 之下列計數，任一不等即中止並還原**：
    `<dataValidation`／`x14:dataValidation`／`<conditionalFormatting`
    ／分頁數／`drawing` 與 `chart` 之 rel 數／`sharedStrings` 之項數變動方向

**本次之 x14 遺失已發生**（母本 `6d3797d4…`）：
其修復不以工具為之 —— **由 Pei 於 Excel 內對 `R10:R152` 重設資料驗證**，
來源 `Reference!$C$4:$C$12`。**原範圍僅至 R132，本 feature 之 143 列已逾之**，
故重設時須同時延展其範圍。

**A-VS153 之歸因更正**：其非「openpyxl 之工具限制」，
而是**分析層指定了不該用的工具**。
```

**執行層之處置正確且值得記**：其試圖以 raw XML 注入還原而**把檔案寫壞**，
隨即依 W-137(3) 自備份還原並重跑實寫。
**該還原路徑本輪首次被實際用到，且有效。**

---

## 2. 三項處置

### 2.1 §2.1 AH 對照式過時 —— R-VS64 之第四次實例

執行層之判斷正確：`AH 非空 76` 之差額即 §C（`IMPL_GAP` 44），
**32 ＋ 44 ＝ 76 逐條可對**。

**對照式改為**：`AH 欄非空 ＝ delivery_disclosure.md 之各節聯集`。

**且 R-VS64 之回溯清單（43 輪 D-3 之 51 行）仍未改寫** —— 本項即其直接後果。
→ **W-140**。

### 2.2 §2.3 兩讀 —— 取實質，並限縮升級條件之措辭

```
分析層裁定
W-138 第三項之升級條件依**實質**未命中：
本 feature 之五個標的（`FL_HS_Tlm`／`FR_HS_Tlm`／`FL_VS_Tlm`／`FR_VS_Tlm`／
`HSW_Tlm`）於 PDT24 與 PDT27 兩代皆為 1 bit，**R-VS67′ 之前提成立**。

逾 1 bit 者為按摩座椅之 `MassageIntensity_*`／`MassageType_*`，
**不在本 feature 之 237 leaf 內**。

**71 輪之補充證據段須改寫**：其「`*_Tlm` 為 1 bit」之主詞
限定為本 feature 之五個標的，不及於全部 `*_Tlm`。
—— 該段將隨 DR-15′ 送出，**措辭之過寬會使上游據以反駁**。
```

### 2.3 備份已非上一版

實寫後母本為 `6d3797d4…`，而 `REF/036_pre_writeback_20260823.xlsx`
仍為實寫前之 `ebe5a65f…`。

```
分析層裁定
**任何後續之改寫前，須以現行母本另備份**，其命名為
`REF/036_pre_<動作>_<YYYYMMDD>.xlsx`，並記其 sha256。
現有備份保留（其為「未寫入前」之唯一存本），**不覆蓋、不改名**。
```

---

## 3. 49 輪指令 —— **交付準備**

```text
你是 FW036 管線之執行層。repo: /Users/peihe/Work_Projects/TC_Generator
本輪為 Vehicle Setting 之第 49 輪：**交付包之彙整**。母本已寫入，不再動它。

## 先讀

  docs/fw036/FEATURE_ONBOARDING.md
  features/vehicle_setting/RULINGS.md
  features/vehicle_setting/docs/handoff/76_writeback_review.md   ← 本輪依據

## 禁區

- git 不執行。**不再改動 036 母本**（x14 之修復屬 Pei，於 Excel 內為之）。
- 不補素材、不代擬條文、不自行調和數字。各版保留不刪。
- **不得以 openpyxl 存檔任何 xlsx**（R-VS70）。

## 文書

D-1  依 R-VS18 建 docs/upstream/42_delivery_pack.md，六節先留空。
D-2  逐字轉錄 76 包 §1 之 **R-VS70** 入 RULINGS.md；
     A-VS153 之歸因依 §1 更正為「分析層指定了不該用之工具」。
D-3  依 R-VS35 分線列兩數。D-6 骨架對照照做。

## 作業（三項）

W-139  **交付包之彙整** → `docs/reports/DELIVERY.md`
       一份，供客戶與內部同時使用，須含：
       (1) **交付範圍**：143 條 TC／母體 237 個 Functional leaf；
           未生成之 94 條逐條列其阻塞類別與所待之 DR
       (2) **驗證覆蓋**：pilot #1 8 ＋ #2 15 ＋ #3/#4 28 ＝ **51 條（36%）**
           經人工關卡；**未抽樣之 92 條僅經機械自檢**
       (3) **待補清單**（依 R-VS69 之分節）：
             §A 單一缺值 16／§B-1 畫面層完全未驗 0／
             §B-2 畫面層部分未驗 16／§C 實作缺口 44
       (4) **未結 DR 十一份**及其影響條數
       (5) **已知限制**：`dr15_exposed` 47 條之形態可能因 DR-15′ 之答覆而變；
           `impl_gap` 44 條之訊號不在基線 DBC
       (6) 母本之 sha256（實寫前 `ebe5a65f…`／實寫後 `6d3797d4…`）
       **不得以「143 條已交付」一句涵蓋** —— 三個分母（237／143／51）須並列。

W-140  **R-VS64 之回溯清單改寫**（43 輪 D-3 之 51 行，至今未改）
       逐行改為關係式，寫入 `threshold_audit.md` 之新欄；
       **不改各下放包之原文**（R-TM13）。
       **必列**：51 行之「原文 → 關係式」對照，及其中已因過時而實際出錯者
       （已知 4 例：129／21／「預期命中」／AH 32）。

W-141  **x14 修復之驗收準備**（**不執行修復**）
       (1) 備妥修復後之驗收指令：raw XML 中 `x14:dataValidation` 計數 ≥ 2、
           其 `sqref` 涵蓋 `R10:R152`、來源為 `Reference!$C$4:$C$12`
       (2) 備妥修復後之 sha256 記錄欄位
       (3) **依 R-VS70 備妥「寫回前後之 raw XML 比對」腳本**，
           供日後任何母本改動使用；**以本次之遺失為必命中錨點**
           （以實寫前後兩本為輸入，須報出 x14 由 2 降 0）

## 升級條件

W-139 之三個分母未並列；
W-140 之 51 行有未改寫者；
W-141(3) 之錨點未報出 x14 之遺失。
```

---

## 4. 待 Pei

| # | 事項 |
|---|---|
| **1** | **x14 下拉之修復** —— Excel 內對 `R10:R152` 重設資料驗證，來源 `Reference!$C$4:$C$12`（**原範圍僅至 R132，須延展**） |
| 2 | PDT24 兩檔複製入 `inputs/`（`pdt24_prep.md` 已備補列與指令） |
| 3 | 實寫後之入庫（git 屬你；母本在 repo 外，僅 repo 側之產物需入庫） |

（`impl_gap` 之 44 條依你之指示不再列。）

---

## 5. 本包產生之新條文清單（自檢）

| 條 | 主題 | 裁定者 |
|---|---|---|
| **R-VS70** | 母本寫回不得以 openpyxl 存檔；須 XML 外科式並前後比對驗證設定 | 分析層（本輪額度用畢） |
| W-138 第三項之取捨 | 取實質；71 輪補充證據段之主詞須限縮為五個標的 | 分析層 |
| 後續改寫前之另備份 | 現有備份為「未寫入前」之唯一存本，不覆蓋 | 分析層 |
