# 82 下放包 — 生成收尾覆核、R-VS76、54 輪（寫回 ＋ pilot sheet）

分析層寫入，2026-08-23。**生成完成：225 TC／219 leaf；237 全數具名。**

---

## 1. R-VS76 —— 「該生成而沒生成」須有專屬檢查

§6-1 之判斷成立且為本輪最重要者：

> §9 檢既有條目之品質，錨點檢檢查本身是否失效，**二者皆以「已有之產物」為輸入**。
> 本輪之所以攔到，是因為算總量與 245 之差時**被迫回頭點母體**。

```
R-VS76（分析層裁定 2026-08-23）
每輪生成後，**除品質檢查外，另跑一項完整性檢查**：

  母體 237 leaf 逐一歸入三類，**其和須等於 237**：
    有 TC ／ held_out（附條文逐字與不生成之理由）／`generatable = no`
  **任一 leaf 未歸類即為漏**，停下回報。

該檢查以**母體**為輸入，非以產物為輸入 —— 其為 §9 與錨點所不能代者。
本輪之 `ThirdRowHeadrestDump-033`／`-034` 即其標的（漏 2，事後補入）。
```

---

## 2. 交付基準由 245 改為 **225**

差額 20 之三項歸屬（11 `generatable = no` ＋ 7 held_out ＋ 2 拆分預估過寬）
**加總即 20，逐項可對**。

**分析層覆核 held_out 之 7 條**：

| 群 | 條 | 判 |
|---|---|---|
| **G1 適用性前言** | 5 | **成立**。`Also the requirements are valid only if …` 為 A-VS141 之第六式，其無觸發亦無可觀察標的（W-121 之結構判準已定其為 B4） |
| **G4 轉指他節** | 2 | **成立**。`please see Stop-Start System Feature section for switch behavior` —— 其行為屬他節所有，依 **§8.2.1** 不得擴張至本 leaf |

```
分析層裁定 2026-08-23
**交付基準由 245 改為 225。**
245 為 W-147／W-151 之預估，其 v3′ 判準施於條文而**未先判該 leaf 是否應生成**，
致 `generatable = no` 之 11 與無可觀察標的之 7 皆被計入。**預估過寬，非生成之漏。**

母體 237 之最終歸類（**和為 237，無殘餘**）：
    有 TC        **219** leaf ／ **225** TC
    held_out       **7** leaf（G1 5 ＋ G4 2）
    generatable=no **11** leaf（W2 池，待 DR 覆）
```

---

## 3. 三項確認

| 項 | 確認 |
|---|---|
| **§2.2** `TELEMATIC_CLIMATE_SETUP` | **處置正確**。依 R-VS9(1)′ 取 LID 之歸屬、依 R-VS67′ 判 `SETUP3.FL_HS_Tlm` 二值能承載、不標 `impl_gap`；不逕行調和而併入 DR-15′，正確 |
| **§2.4** `latest_batches()` 丟棄 probe | **本輪不改正確**（R-VS73：使數上升者須逐條抽驗增量）。**54 輪修，其 7 條併入 pilot #6 之母體** |
| **§9 首跑 22 項** | **消解正確且未改判準**。`= 7 (SNA)` 之 18 項為 A-VS157 同族（「來源沒有」而「需要寫點什麼」），**攔住它的是 R-VS39 之逐字核對** —— 一條為別的目的所立之規則 |

---

## 4. 54 輪指令

```text
你是 FW036 管線之執行層。repo: /Users/peihe/Work_Projects/TC_Generator
第 54 輪。**全量寫回 ＋ pilot sheet。**

讀：docs/handoff/82_generation_done.md（本輪依據）
    ＋ docs/handoff/66_writeback_procedure.md（欄位對映）＋ RULINGS.md

## 文書
D-1  建 docs/upstream/47_fullwrite2.md，六節先留空。
D-2  逐字轉錄 82 包 §1 之 R-VS76 入 RULINGS.md。
D-3  A-VS159／160 標處置。依 R-VS35 分線列兩數。D-6 骨架對照。

## 作業（三項）

W-153  **`latest_batches()` 之修正**
       其式 `(batch\d+)(?:_v(\d+))?\.json$` 丟棄 `batch21_probe.json`。
       修正後之來源集為 **232 條**（225 ＋ probe 7）。
       **依 R-VS73**：增量 7 條逐條列出並抽驗（其已過 §9 與錨點，此處驗其
       是否確為最新版、有無與 batch23 重複之 leaf）。

W-154  **R-VS76 之完整性檢查**
       母體 237 逐一歸類（有 TC／held_out／`generatable = no`），**和須為 237**。
       **必列**：三類之數與其和；未歸類者即停下回報。

W-155  **全量寫回**（依 R-VS70／R-VS72）
       (1) 寫前另備份 `REF/036_pre_fullwrite2_<YYYYMMDD>.xlsx`，記 sha256；
           既有二備份不覆蓋、不改名
       (2) **XML 外科式**（不得以 openpyxl 存檔）
       (3) 全量：一 leaf 一列（拆分者多列），未生成者 C／D／G／H／N 照填、
           I/J/K/L/M/P/R 留空、AH 記其阻塞（held_out 7 之理由亦入 AH）
       (4) 寫前後比對 raw XML 七項，任一下降即中止並還原
       (5) 重讀逐列比對十六欄，不符即中止並還原
       (6) 實寫後 sha256 記入上繳
       **必列**：總列數（預期 ＝ 232 TC ＋ 未生成之 leaf 數）

W-156  **pilot #5＋#6 之合併 sheet** → `docs/reports/pilot6_sheet.md`
       母體：本輪新增而未經人工關卡者（batch20 12 ＋ 拆分 7 ＋ probe 7
       ＋ batch22 12 ＋ batch23 47 ＝ **85 條**）
       抽樣 **18 條**：
         **必檢 10** —— G 型八子形態各 1（G1 為 held_out 不計）＋ D 型
                        `PENDING: DR-15` 形態 2 ＋ G2「顯示不變」形態 1
         **分層 8** —— 維度 `test_set`（四值）× `screen_pending`（有／無）
                      **先驗該維度於本母體非單值**（A-VS142／§2.4 之教訓）
       每條含十六欄全文 ＋ 來源條文逐字 ＋ 其形態分類。
       **列交叉格矩陣。**

## 禁區
git 不執行。不補素材、不代擬條文。各版保留不刪。
不得以 openpyxl 存檔 xlsx。**不得為使列數符合預期而改動任何判定。**

## 升級條件
W-154 之三類和 ≠ 237；
W-155(4) 之任一計數下降／(5) 之比對不符；
W-156 之分層維度於本母體為單值。
```

---

## 5. 其後

| 輪 | 內容 |
|---|---|
| 54 | 寫回（**約 232 TC**）＋ pilot sheet |
| **55** | pilot #5＋#6 覆核 → **交付** |

x14 之修復範圍屆時依實際列數定，**54 輪之上繳會列出**。

---

## 6. 本包產生之新條文清單（自檢）

| 條 | 主題 | 裁定者 |
|---|---|---|
| **R-VS76** | 每輪另跑完整性檢查，以母體為輸入；三類之和須為 237 | 分析層 |
| 交付基準 245 → 225 | 預估過寬（`generatable = no` 11 ＋ held_out 7 ＋ 拆分 2） | 分析層 |
| held_out 7 之覆核 | G1 5 依 A-VS141／W-121；G4 2 依 §8.2.1。**皆成立** | 分析層 |
