# 61 下放包 — A-VS121 之追認（我的追因錯）、乘客側裁定、38 輪

分析層寫入，2026-08-22。**交付 96 條。池剩 14，兩輪見底。**

---

## 1. A-VS121 —— **60 包 §4 之追因是錯的，R-VS58 解的不是那件事**

我在 60 包 §4 寫：

> 成因為選池一路依「reqid 升冪 ＋ 逐 Layer 2 輪流」，**與風險無關**。

**該追因不成立。** 實測：含 `Fail_Present` 之 16 個 leaf 其 `delegate` **全為 `blocked`**
（5 個 `screen`／`DR-5-B`，11 個空），故 `generatable = no`，**根本不在池內**。

**真正成因為 R-VS7 之委派界線 ＋ R-VS17 之 BLOCKED 處置。**
選池順序無論怎麼排，都排不到池外之 leaf。

```
分析層更正 2026-08-22
R-VS58（選池優先序）**維持有效** —— 其為正確之規則，
且 W-105(4) 之預判／定稿一致性 10/10 證其可行。

**惟其不解 A-VS116 之缺口。** P0(b) 之失效半邊要有 TC，
須先解 **DR-5-B**（其 16 leaf 中 5 個因畫面層 BLOCKED、11 個因委派），
**非改選池順序**。

A-VS116 之成因改指向 **A-VS121**，60 包 §4 之追因段作廢。
```

**這是我第二次以推論代替實測而排錯優先序**（前次為 50 包之「79」，R-VS50 因之而立）。
R-VS50 令「作為優先序依據之數字須回查組成」——
**本次我回查了數字（83 次引用為實）卻未回查其可及性（`delegate` 狀態）。**

```
R-VS50′（範圍之補正，分析層裁定 2026-08-22；本輪唯一新條文）
R-VS50 之回查義務，除該數字之**組成**外，
另及於其標的之**可及性**：該批 leaf 是否在當前池內、
其 `delegate`／`writable`／`guard` 三者是否容許其被選入。

**「有 N 個標的」與「有 N 個可作業之標的」為兩數，須分列。**
以前者排定優先序而後者為 0 者，該優先序無效。
```

---

## 2. 乘客側之表述 —— **成立，但依據與駕駛側不同**

執行層列此為與 35 輪 `-038` 同型之風險，**正確地不自行擴張**。

分析層實測：

| 形態 | 命中 |
|---|---:|
| `PsngrSeatHeating.Req` | **21** |
| `DrvSeatHeating.Req` | 22 |
| `selects DrvSeatHeating.Req`（60 包 §1 之依據） | **1** |
| **`selects PsngrSeatHeating.Req`** | **0** |
| `the right heated seat` | **0** |

**乘客側無 `or` 並列，60 包 §1 之依據不涵蓋它。**

**但其成立之依據另有其物**：`4859365` 逐字

> `IF PsngrSeatHeating.Req passes to "Requested" THEN TLM shall set
> TELEMATIC_VEHICLE_SETUP.FR_HS_Cmd_Tlm = "Pressed"`

與駕駛側 `4859364` 之結構**逐字對稱**（僅 `Drv`↔`Psngr`、`FL`↔`FR`）。

```
分析層裁定 2026-08-22
`PsngrSeatHeating.Req` 以「按壓右前座椅加熱圖示」表述**成立**，
其依據為 **`4859365` 與 `4859364` 之逐字對稱**，
**非** 60 包 §1 所引之 `4859508`（該文僅及駕駛側）。

batch14 之 FR 側四條（`-061`／`-062`／`-063`／`-085`）**不退回**，
惟其 `reasoning` 之依據須改記為
「`CFTS044-4859365`；其與駕駛側 `4859364` 逐字對稱，
 駕駛側之觸發表述由 `4859508` 之 `or` 並列確立」。

**與 `-038` 之別**：後者於來源中無任何對應文字；
本項於來源中有對稱條文，其對稱性為實測（`Drv`↔`Psngr` 逐字替換即得）。
```

---

## 3. 三項確認

| 項 | 確認 |
|---|---|
| **§2.4 batch13_v2 之改動** | **追認**。其為 W-105 之 Sibling Rows 要求所迫（不改則 5 條不可分辨），且只動 `pre_conditions` 一欄、其餘九欄不動、原版保留。**具名供裁而未逕行主張正當，正確** |
| **A-VS118** `HSW_Cmd_Tlm` 值域無來源 | **R-VS57 之缺口成立**：其 WARN 判定只問訊號名有無逐字來源，未問值域。**該 4 leaf 依 R-VS57 判 WARN 而實不可寫** —— 見 §4 之 R-VS57 補正 |
| **A-VS119** 四 leaf 不可分辨 | 未合併、未刪除、已開 DR-27，**正確**。§8.2.2 之反向禁令使其無解於本層 |

---

## 4. R-VS57 之補正（併入 §1 之條文額度，不另編號）

```
R-VS57 之(4)（分析層裁定 2026-08-22）
L-VS2 之 WARN 判定，除訊號**名**須有逐字來源外，
其**值域**亦須有來源（CFTS044 條文之內嵌值、LID 對應欄組、或 DBC `VAL_`）。

  名有來源 ∧ 值域有來源 → **WARN**（照寫，標 `dr_dependent`）
  名有來源 ∧ 值域無來源 → **W2**，`blocker_class = B6-value-absent`
  名無來源              → **FAIL**（不得寫）

實例：`HSW_Cmd_Tlm` 之名見於 CFTS044，其值域於 LID 兩欄組皆為 `None`
→ 由 WARN 改判 **W2／B6**，其 4 leaf 自 `dr_dependent` 之 65 中扣除為 **61**。
```

---

## 5. 38 輪指令

```text
你是 FW036 管線的執行層。repo: /Users/peihe/Work_Projects/TC_Generator

讀：
  docs/fw036/FEATURE_ONBOARDING.md
  docs/runtime/ASPICE_SWE6_AI_Instruction.md
  docs/runtime/profiles/FW036_R1L_VehicleSetting_Profile.md
  features/vehicle_setting/RULINGS.md
  features/vehicle_setting/docs/handoff/61_review_round37.md   ← 本輪依據

## 文書

D-1  依 R-VS18 建 docs/upstream/34_redundancy_and_batch15.md，六節先留空。
D-2  逐字轉錄 61 包 §1 之 **R-VS50′** 與 §4 之 **R-VS57(4)** 入 RULINGS.md；
     **60 包 §4 之追因段標「作廢，見 A-VS121」**。
D-3  `DATA_REQUESTS.md`：DR-25′ 之影響段依 R-VS57(4) 改為
     **四訊號／61 leaf**（`HSW_Cmd_Tlm` 移出，改列於 A-VS118 之 B6 類）。
D-4  batch14 之 FR 側四條，其 `reasoning` 依 61 包 §2 改記依據條文。
     ANOMALIES.md：A-VS116 改指向 A-VS121；A-VS120 標「追認（61 包 §3）」。
     依 R-VS35 列兩數。D-6 照做。

## 作業（三項，R-VS25）

W-107 **母體層之冗餘掃描**（A-VS119 之未量部分）
      現行唯一性掃描只施於**已交付之 96 條**。改施於**母體 237 leaf 之來源條文**：
      (1) 逐 leaf 取其 reqid 之條文正文，去除 `[…]` 屬性段與節號後正規化
          （R-VS39 之鍵 ＋ 數詞↔數字）
      (2) 兩兩比對，列出**可測內容無法分辨**之組數與其 leaf id
      (3) 其中已交付者、未交付者分列
      **必列**：組數、涉及 leaf 數、及其對「237 母體」之意涵
      —— 該數決定交付時「237 個 leaf 對應幾個相異驗證點」

W-108 **依 R-VS57(4) 重跑分級 ＋ batch15**
      (1) 併入值域來源之檢查，`HSW_Cmd_Tlm` 之 4 leaf 須由 WARN 轉 **W2／B6**
          **錨點（R-VS54）**：該 4 leaf 必命中；`FL_HS_Cmd_Tlm` 之 17 leaf 須維持 WARN
      (2) 全量重跑，列 W0／W1／W2 與 **126/2/109** 之對照
      (3) batch15 —— 依 R-VS58 之優先序自重跑後之池取，**池不足則取全部並回報**

W-109 **產能終局之盤點**（§6-2 之延伸）
      池見底在即。逐一列出**剩餘 141 個未交付 leaf** 之阻塞歸屬：
        leaf_id / writable / blocker_class / delegate / dr_dependent /
        **解鎖所需之 DR**
      並列**逐 DR 之解鎖量**（某 DR 覆後可增加幾條可生成 leaf），
      **依解鎖量降冪排序**。
      **該表即送 DR 之優先序依據**，取代分析層歷輪之估計。

## 禁區

git 不執行。不寫回工作簿。不代擬條文。各版保留不刪。
不得跨列引入 `FR_VS_Cmd_Tlm` 之值域；不得合併 A-VS119 之 leaf。
**不得為使池不見底而放寬任何判準。**

## 升級條件

W-107(2) 之組數 > 10（則 237 母體之相異驗證點數須重述）；
W-108(1) 之錨點未命中；
W-108(3) 之池為 0；
W-109 之最大解鎖量 DR 非 DR-5-B／DR-18／DR-25′ 三者之一。
```

---

## 6. 待 Pei —— **DR 之送出已成唯一瓶頸**

池剩 14，batch15 後見底。**W-109 將給出逐 DR 之解鎖量**，屆時送 DR 之優先序有實測依據。

現有待送 **十一份**：DR-25′／DR-26／DR-27／DR-21／DR-17／DR-24′／DR-18／DR-11／
DR-20／DR-23／DR-8′。DR-15 待覆（覆後回溯 6 條）。

**pilot #3 之 13 條**：分析層於次包出建議分類。

---

## 7. 本包產生之新條文清單（自檢）

| 條 | 主題 | 裁定者 |
|---|---|---|
| R-VS50′ | 回查義務及於標的之可及性；「有 N 個標的」與「N 個可作業標的」須分列 | 分析層（本輪額度用畢） |
| R-VS57(4) | WARN 須名與值域**皆**有來源；值域無來源者判 W2／B6 | 分析層（R-VS57 之補正） |
| 乘客側之裁定 | 依 `4859365`／`4859364` 之逐字對稱，非 `4859508` | 分析層 |
