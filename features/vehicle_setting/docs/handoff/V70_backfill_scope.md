# V70 — §3.3 裁定：採全欄改寫；交付本現況之實測；19 條補入

下放包 **V70**。所據上繳：W-VF90 之停手回報（2026-08-25）
＋ **分析層對交付路徑之 036 之獨立實測**。
新增 **一條**（開號取實測最大 +1，**回報實得之號**）、**W-VF91**。

---

## 1. 分析層之實測：交付路徑之本已為延伸後之狀態

**量測**：逕讀 `/Users/peihe/…/VF230_V1_R5/…_SWQT_VF230_20260819.xlsx`。

```
sha256（前 16）  73e28c740fe79055     186,784 bytes     mtime 13:14:16
資料列 438｜B 244–681 連號｜AH 非空 65
sheet6.xml   XML 合法
  一般 sqref   AF10:**AF1000**｜P10:**P1000** Q10:Q11｜T10:**Z1000**｜H10:H132
  x14  sqref   R10:**R1000**
```

**即 `R-VF141`（四處延伸至 1000）已施行，且該本已在交付路徑上。**
**repo 內副本 12:21:45／186,784 bytes 與之同大小，二者一致。**

**上繳稱「工作簿未動（仍 438 列，B 244–681）」** ——
**列數與 B 欄之陳述為真，而 `sqref` 已改，其非「未動」。**
**此差異須於次一次上繳具名其時點與執行者**（W-VF91.5）。

**另實測 `H10:H132` 一處**（上繳與本層前輪皆未載）——
**其為第五處同型，仍停於列 132。** 其欄為 Test Set，
**現有 438 列之值皆由本層寫入且經自檢**，故與 P／R 同理無資料風險；
**惟其為 `R-VF139`（四處一併）所漏列之第五處**，見 §3。

---

## 2. 甲 —— §3.3 採全欄改寫；補入 19 條

```
R-VF-甲（補入之改寫範圍，分析層裁定 2026-08-25）
【落檔取實測最大 +1】

**`R-VF140` 之「重寫該欄，不重寫其餘欄位」撤銷該限制，改採全欄改寫。**

**其二令在本批料上不可兼得，執行層之判斷正確**：
```
  須改者  test_item 19｜tc_title 13｜expected_result 12｜test_procedure 11
          pre_conditions 3｜input_test_data 2｜writable 2
  不改者  spec_ref／priority／priority_class／design_method／value_source  皆 0
```
**「須過現行自檢」為 canon 之要求，「只改一欄」為本層之便利性設定** ——
**二者相衝時前者勝。** `R-VF140` 之限制係為保全 verdict 通過之內容，
**而 verdict 之通過本即對舊判準而言**（`R-VF140` 自身已載此），
**故其保全之物並不存在。**

**`tc_title` 之 13 條尤不可免** —— 補入使 10 個家族之計數改變，
**`R-VF98`（家族區辨 token）遂對其生效**。其非改寫之選擇，是判準之後果。

**補入之數為 19，非 20**：`SWITCH1Type-002` 與已寫入之 `-029`
**可執行四欄及 `tc_title` 逐字相同**，依 `W-VF74` 剔除。
**其 `test_item` 相異**（顧客動作層 vs `CarPropertyManager.setProperty()` 屬性層）
—— 與 `SWITCH1PowerMode-005/-030` 同型第二例，**為上游需求集之重複**，已開 DR-48。

**候選產出 `generated/vf230_backfill.json` 予以核可**：
19 條、事實可抽出 20/20、現行 20 項自檢 **0 筆失敗、無法失效之項 0**、
九批回歸 0。**得逕行補入。**
```

---

## 3. `R-VF139` 之範圍再擴：第五處 `H10:H132`

**`R-VF139` 令「P／R／T–Z／AF 四處一併」，而實測第五處 `H10:H132`（Test Set）
同型且未被涵蓋。**

**其現況為已延伸之四處至 1000、而 H 停於 132** ——
**即 `R-VF139` 所欲避免之「同一份交付本內二種驗證範圍」已然發生**，
只是其邊界自 P／R 移至 H。

**處置**：`H10:H1000` 一併延伸，**與其餘四處同時為之**，
使五處一致。**其與補入同輪施行，不另立輪次。**

---

## 4. 三處具名嘉許

1. **§三其一 —— 自列之「未驗」逐字人讀後即為第四例。**
   其上繳 §九自列「未逐字人讀該 2 條之條文，不能排除其為同族第四例」；
   **逐字人讀，它就是第四例** —— `[A-Za-z0-9_ ]` 不含 `"`，
   `value as "Absent"` 比對失敗而回報成「條文未帶值」。
   **一個自己寫下的『未驗』，被自己驗掉，且結論與自己所擔心者相同。**
   放寬依 `R-VF82` 量兩側（偽陰回收 2／偽陽 0／量產 501 條逐條相異 0），
   **且第 3 條 `PowerUnit-013` 之非受害者身分經查證而非推定。**
2. **§三其二 —— 只比四欄即剔除，而其自列之未驗第 3 項正是「未比 `test_item`」。**
   **比了，相異，遂自「本層之重複」改判為「上游需求集之重複」** ——
   二者之處置完全不同（前者剔除，後者剔除並開 DR）。
3. **§三.3 之連帶影響已量測而非推定** ——
   將 20 條納入跨批視野後重跑九批，`batch08` 唯一 1 筆即該重複對，
   **證已寫入之 438 條不因補入而失效。**

---

## 5. W-VF91 — 補入與最終複製（**本層對 VF230 之最後一項**）

- **W-VF91.1** 補入 19 條至 **repo 內副本**；**B 欄續號前實測其現行末號**
  （不採信 681 之推算）；預期 682–700，**實數以實測為準**。
- **W-VF91.2** `H10:H132` → `H10:H1000`（§3），**與其餘四處複驗其一致**。
- **W-VF91.3** **重跑閘 3（x14 保全）與閘 4（重讀比對，457 × 16）**，差異即停。
  **457 = 438 + 19**，其實數以實測為準。
- **W-VF91.4** 交付說明更新：測項 **438 → 457**、需求 **435 → 454**、
  §八增五處 `sqref` 之延伸、§六增補入一項與 DR-48。
  **其數繫於 W-VF91.1／.3 之實測，不得先寫。**
- **W-VF91.5** **具名 `sqref` 已改與「工作簿未動」之陳述差異**（§1）：
  其施行之時點、執行者、與 repo 內副本／交付路徑二本之一致性。
- **W-VF91.6** `A-VF32` 之通則已入，**核可**；
  **其對治（扣除方與納入方讀同一份清單）之施行併入本工單**：
  `facts.py:389` 與 `isolate.py:99` 之 `pool - pilots` 改讀
  `data/vf230_batches.tsv`，**或另立對帳使其差可見**。
  **依 `R-VF33` 先產消費者清單，無清單即退回。**
- **W-VF91.7** 備份檔刪除（`R-VF135`）—— **Pei 之二步已完成（§1 實測），
  其條件成就。**

**補入完成後，repo 內副本須再複製至交付路徑一次**（`R-VF112`：複製屬 Pei；
**逐字具名檔名**）。

---

## 6. 給 Pei

**你的二步已完成** —— 本層實測交付路徑之本為 186,784 bytes／13:14:16，
438 列、B 244–681、XML 合法、`sqref` 已延伸至 1000（四處）。
**其與 repo 內副本一致。**

**其後仍有一次複製** —— 19 條補入後，repo 內副本會變為 457 列，
**須再複製一次至交付路徑**（逐字具名檔名）。

**補入後之數（推算，以實測為準）**：

```
需求  435 → 454      測項  438 → 457      B 欄  244 – 700
```

**補入之數為 19 而非 20**：`SWITCH1Type-002` 與已寫入之 `-029` 四欄逐字相同，
**而其 `test_item` 相異** —— **是上游需求集之重複，非本層之重複**，已開 DR-48。

**另測到第五處** `H10:H132`（Test Set 欄）同型未延伸 ——
已令與其餘四處一併延至 1000，**否則「同一份交付本內二種驗證範圍」仍在，
只是邊界移了位置。**

---

## 7. 新條文清單（自檢）

| 暫稱 | 內容 |
|---|---|
| 甲 | `R-VF140` 之「只改該欄」撤銷，採全欄改寫；補入 19 條；`vf230_backfill.json` 核可 |

**⚠ 編號於落檔時取實測最大 +1，並回報實得之號。**

**工單**：W-VF91（七項）。

**分析層本輪之錯**：`R-VF139` 令「四處一併」而漏第五處 `H10:H132`；
`R-VF140` 之「只改該欄」與「須過現行自檢」在本批料上不可兼得，其未經量測即令之。
另 V68 §2 指名 `generated/vf230_pilot1_v4.json`，**該檔不存在**
（v4 於 W-VF63 就地覆蓋 `vf230_pilot1.json`）。

**執行層上繳時須附「本包是否仍有該驗而未驗者」之獨立判斷。**
