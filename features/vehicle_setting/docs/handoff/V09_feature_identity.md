# V09 — feature 歸屬重申、子目錄提案撤回、R-VF10 原制維持

下放包 **V09**。對應上繳：`docs/upstream/V09_*.md`。
本包新增 **R-VF23**（1 條）、**W-VF24**（1 項工單）。**本包不新增任何開放項。**

裁定依據：Pei 於 2026-08-23 之答覆 —— **「還是 Vehicle Setting 不變」**，
經釐清後確認為對 **feature 歸屬**（V08 §5.1 讀法 C）之答覆。

---

## 1. R-VF23 —— feature 歸屬不變；檔案擺放不涉 feature 身分

```
R-VF23（feature 歸屬之重申與子目錄提案之撤回，Pei 裁定 2026-08-23）

**一、重申 R-VF2。** VF230 併入 `features/vehicle_setting/`，
與 CFTS044 同屬一個 feature。此不因任何 `docs/` 之檔案擺放而改變：
`features/` 目錄結構不動、`framework.md` 之 Layer 1 不分、
Test Group 依 R-VF9 兩本同值為 `Vehicle Setting`。

**二、V08 §5.1 之修訂建議撤回。** 本層曾建議將 R-VF10 之
「handoff／upstream 用 `VNN_`」改為「用 `vf230/{NN}_` 子目錄」，
並同步建 `docs/handoff/vf230/`。**該建議撤回，不再提起。**

理由：該提案在結構上讀為分家之訊號，而其所欲解決者
（命名空間撞號）**R-VF10 之原制已足以解決** ——
以檔名前綴分線，不需目錄分線。以較強之手段解決較弱之問題，
其副作用（讀為分家）大於其收益。

**三、R-VF10 維持原文，不修訂。** 五個命名空間之分配不變：

  handoff／upstream 檔名   `V{NN}_` 平鋪
  裁決                     `R-VF{n}`
  anomaly                  `A-VF{n}`
  工單                     `W-VF{n}`
  DR                       不分離

**四、目標狀態為單一套。** 現況為兩套並存 ——
`docs/upstream/vf230/00_intake.md`＋`01_recon.md`（併行線所搬）
與 `docs/upstream/V06_*.md`＋`V07_*.md`（本線）。
**兩套並存即撞號問題之殘留形態**，須收斂為一套。
依本條第三項，收斂方向為 `V{NN}_` 平鋪：

  `docs/upstream/vf230/00_intake.md` → `docs/upstream/V01_vf230_intake.md`
  `docs/upstream/vf230/01_recon.md`  → `docs/upstream/V02_vf230_recon.md`
  收斂後移除空目錄 `docs/upstream/vf230/`

**W-VF19 已實測二檔為純搬移、逐位元相同（368/368、247/247 行），
無獨有內容，故此收斂無資料損失之虞。**

**全部檔案搬移與 git 操作屬 Pei。** 兩層只備清單，不執行。
```

---

## 2. W-VF24 — 收斂之前置清單（只列不改）

1. 復測現況（**不以 V07／V08 之目錄列表為來源**）：
   `docs/upstream/` 與 `docs/upstream/vf230/` 之逐檔清單、
   `docs/handoff/` 內屬 VF230 線之逐檔清單。
2. 產出可供 Pei 逐條核對之搬移表：舊路徑 → 新路徑 → **屬哪一線**。
   Part 1 之 `00_`–`37_` 一律不列入。
3. 掃描全庫對 `vf230/00_intake.md`／`vf230/01_recon.md`／
   `61_vf230_intake.md`／`62_vf230_recon.md` 之交叉引用，逐處列出
   （搬移後須同步更新者）。
4. **與併行線之協調點須具名**：該二檔係併行線於 `7a7747e`／`942f0d7`
   所搬。回報搬回是否會與併行線現行作業衝突；有衝突者標明，**不逕行**。
5. **不執行任何搬移、不改名、不 `git mv`、不刪目錄。**

---

## 3. 未受本包影響之開放項（原樣延續，本包未新增）

| 項 | 狀態 |
|---|---|
| **「已交付」判準 ＋ R-VF15 之 AH 轉錄** | 待 Pei。**時窗正在關**（見 §4） |
| **Layer 2 起點核可** | 待 Pei。實數已備：106 簇／104 exact／11 族群／627 leaf；建議粒度 D |
| driver 是否全面採 VC/VM | 待 Pei。本層不建議於 Part 1 寫回進行中變更 |
| DR-28／DR-29／DR-30 | 三件皆未送出 |
| W-VF21（補落 R-VF10–13 並複驗 R-VF17） | 執行層待辦，**最優先** |
| W-VF22（271 vs 237 之 34 leaf）／W-VF23（覆寫層實作） | 執行層待辦 |

---

## 4. 時窗提醒（不重複論證，只記時點）

分析層於 2026-08-23 復測 CFTS044 交付路徑之 036
（分頁 `Test Case Specification 測試用例規範`，列 10–246）：

```
F 0 ／ G 0 ／ J 0 ／ K 0 ／ O 0 ／ P 0 ／ R 0 ／ AA 0 ／ AH 0
```

→ 寫回**尚未發生**，「已交付」判準 (a) 仍為 0。
`docs/upstream/37_pilot_and_writeback.md` 已存在。
**此裁定若落於寫回之後，同一判準將給出不同答案。**

---

## 5. 本包產生之新條文清單（自檢）

| 編號 | 型別 | 是否以可貼入區塊出現 |
|---|---|---|
| R-VF23（重申 R-VF2；撤回子目錄提案；R-VF10 維持原文；收斂為單一套） | Pei 裁定 | ✅ §1 |

**工單**：W-VF24（收斂前置清單，只列不改，含併行線衝突判斷）。

**本包未新增任何待裁項。** §3 所列為先前各包之延續。

**執行層上繳時須附「本包是否仍有該驗而未驗者」之獨立判斷。**
