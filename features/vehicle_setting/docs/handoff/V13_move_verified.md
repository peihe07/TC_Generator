# V13 — 搬移完成之驗證、R-VF39 確認、兩條檢查之補立

下放包 **V13**。對應上繳：`docs/upstream/V13_*.md`。
本包新增 **R-VF40**（1 條）、**W-VF33／W-VF34**（2 項工單）。

依據：Pei 於 2026-08-23 之答覆 —— **「一是」**（R-VF39 之解讀正確）／
**「二 搬好了」**（R-VF23 第四項之搬移已執行）。

---

## 1. 搬移之驗證（分析層實測，非採信回報）

**量測條件**：`Filesystem:list_directory` 對
`features/vehicle_setting/docs/handoff/` 與 `docs/upstream/`，2026-08-23 搬移後。

### 1.1 handoff 側

```
V00_numbering_collision.md      V01_vf230_intake.md
V02_vf230_recon_review.md       V03_test_group_ruling.md
V04 … V12                       共 13 檔，V00–V12 連續無缺號
```

**舊檔名皆已不存在**：`ZZ_vf230_numbering_collision.md`／`61_vf230_intake.md`／
`62_vf230_recon_review.md`／`63_test_group_ruling.md` **各 0 命中**。

**撞號已解**：

| 號 | 搬移前 | 搬移後 |
|---|---|---|
| 61 | `61_review_round37.md` ＋ `61_vf230_intake.md` | **僅** `61_review_round37.md` |
| 62 | `62_review_round38.md` ＋ `62_vf230_recon_review.md` | **僅** `62_review_round38.md` |
| 63 | `63_rulings_round39.md` ＋ `63_test_group_ruling.md` | **僅** `63_rulings_round39.md` |

→ handoff 側之 `NN_` 序列自此全歸 Part 1（現至 `74_pilot34_ruling.md`），
VF230 線全在 `V{NN}_`。

### 1.2 upstream 側

```
V01_vf230_intake.md   V02_vf230_recon.md
V06 … V10             共 7 檔
docs/upstream/vf230/  不存在（0 命中）
```

**V03／V04／V05 為缺號**，非漏搬 —— 該三包無各自之上繳，
其內容由上繳 V06 一併回覆。依 `A-VS02` 先例，**不補不重編**。

### 1.3 結論

**R-VF23 第四項（收斂為單一套）已完成，R-VF10 之命名空間分離
於 handoff 與 upstream 兩側均已生效。**

W-VF13 之改名部分至此結案；其 `ANOMALIES.md` 舊制標記部分已於上繳 V10 §4 補齊。
**W-VF13 全項結案。**

---

## 2. R-VF39 之解讀確認

Pei 答「**一是**」→ V12 §4 之解讀成立，**R-VF39 維持原文，不修訂**。

即：R-VF33 本即拘束兩線，driver 為共用檔一事**依 R-VF33 行事即為所需之協調**，
無須另設程序；`scripts/writability_driver.py` 之**尾段呼叫／提示**已獲授權。

**四項約束不因本確認而放寬**（V12 §4）：限尾段、施行前須依 R-VF33
產出消費者清單（無清單即修改者退回）、影響面須登錄 `CROSSLINE.md`、
施行前實測 `git status`。**W-VF30(b) 仍應先落。**

---

## 3. R-VF40 —— R-VF23 與 R-VF10 由「未受保護」轉為可檢查

```
R-VF40（兩條跨線條文之機械檢查，分析層裁定 2026-08-23）

R-VF38 三令「凡效果落於他線產物之條文，須同時具備 `CROSSLINE.md` 之登錄
與一項能失敗之檢查；缺者標『未受保護』並於每次上繳具名」。

V12 §3 首批登錄中，**R-VF10 與 R-VF23 皆標「無檢查 → 未受保護」**。
搬移完成後，二者之終態已可機械表述，故補立檢查：

**檢查一（R-VF23 ／ 檔名合規）**
  `docs/handoff/` 與 `docs/upstream/` 內，**不得存在 VF230 線之 `NN_` 檔**，
  亦不得存在 `docs/upstream/vf230/` 目錄。
  判準以檔名為之：凡檔名含 `vf230`／`test_group_ruling`／`numbering_collision`
  而不以 `V` 起首者即為違反。
  **錨點**（R-VF21／R-VF28，以內容定錨）：
    必命中   人為建 `docs/handoff/99_vf230_test.md` → 檢查須失敗
    必不命中 `docs/handoff/61_review_round37.md`（Part 1，含 `61_` 但非 VF230 線）
    鑑別錨點 `docs/handoff/V01_vf230_intake.md` —— 含 `vf230` 而合規；
             一條過寬之「檔名不得含 vf230」規則會誤殺之

**檢查二（R-VF10 ／ 編號唯一性）**
  `RULINGS.md`／`ANOMALIES.md` 內，**同一編號不得有兩個定義**。
  掃 `R-VS{n}`／`R-VF{n}`／`A-VS{n}`／`A-VF{n}` 之條文起始行，
  同號出現 ≥2 次即失敗。
  **錨點**：
    必命中   人為插入重複之 `R-VF17` 標題 → 檢查須失敗
    必不命中 現行 `RULINGS.md` 全檔 → 應通過
    鑑別錨點 `A-VS129`–`A-VS136` 之「【VF230 線舊制編號…】」標記行 ——
             其為註記而非條文起始，**不得被計為第二次定義**

**二者併入 W-VF30(b) 之同一檢查點**，不另立入口。
通過後於 `CROSSLINE.md` 將該二條自「未受保護」改標其檢查指令。

**本條不擴及其餘跨線條文** —— 其盤點與補檢查屬 W-VF32。
```

---

## 4. 工單

### W-VF33 — `docs/INDEX.md` 之「舊路徑對照」節

依 **R-VF31 二**（處置 1：搬回並接受失效連結，以對照表使其可追）。

1. 實測 `docs/handoff/64_review_round40.md` 內引用
   `docs/upstream/vf230/…` 之處，**逐處列出行與逐字內容**
   （V11 記為 2 處，須復測，不沿用）。
   **同時掃全庫其餘檔案**是否另有引用舊路徑者 —— 不假定只有該檔。
2. 於 `docs/INDEX.md` 新增一節「舊路徑對照（R-VF23 搬移，2026-08-23）」，
   列 6 列：舊路徑 → 新路徑。
3. **不得修改 `64_review_round40.md` 或任何 handoff／upstream 之歷史檔**
   （R-VF18：歷史紀錄不追改）。失效連結是已裁定接受之代價。
4. 若第 1 項掃出**現行有效之陳述**引用舊路徑（非歷史紀錄），
   依 R-VF18 該類須改 —— **逐句判別後列出待核，不逕改。**

### W-VF34 — R-VF40 之兩條檢查實作

1. 依 R-VF40 實作檢查一與檢查二，併入 W-VF30(b) 之檢查點。
2. **兩條各三個錨點逐一實測**（含鑑別錨點），錨點不符即停並回報。
3. 可失敗性須以人為破壞實測，非以「目前通過」推定其能失敗
   （canon：不可能失敗者標「未實測」而非 PASS）。
4. 通過後更新 `CROSSLINE.md`：R-VF10、R-VF23 二列自「未受保護」
   改標其檢查指令。

---

## 5. 現行工單狀態（依實測，非沿用）

| 工單 | 狀態 |
|---|---|
| W-VF13 | **結案**（改名 §1.3 ＋ 舊制標記已於上繳 V10 補齊） |
| W-VF29 | 非阻塞，未執行（12 跨界簇之可測代理） |
| W-VF30 | (b) 待落 **最優先**；(a) 已獲授權，前置為消費者清單 |
| W-VF31 | 待執行（提案 C 之表；符合 R-VF37(a) 者逕鎖 framework） |
| W-VF32 | 待執行（`CROSSLINE.md` ＋ 跨線全集盤點） |
| W-VF33／W-VF34 | 本包新開 |

**優先序**：W-VF30(b) → W-VF31 → W-VF32 → W-VF34 → W-VF33 → W-VF29。

W-VF30(b) 居首之理由不變：**R-VF17 之效果在其落實前不可依賴**，
而 A-VF8 已證該風險非假設。

---

## 6. 待 Pei 裁定

**無。** 本包未新增待裁項；V12 §6 所列二項皆已由本輪之答覆結案。

**下一個須你裁者，預期為 W-VF31 之 framework 鎖定**——
若其判為 R-VF37(b)（出現新名或既有名語義範圍實質改變）則回報待核；
判為 (a) 則逕鎖，不再經你。

---

## 7. 本包產生之新條文清單（自檢）

| 編號 | 型別 | 區塊 |
|---|---|---|
| R-VF40（R-VF23／R-VF10 之兩條檢查；各三錨點；併入同一檢查點） | 分析層裁定 | ✅ §3 |

**工單**：W-VF33（`INDEX.md` 舊路徑對照，兼掃全庫舊路徑引用）／
W-VF34（R-VF40 兩檢查之實作與錨點實測）。

**結案**：W-VF13（全項）／R-VF23 第四項（搬移，已實測驗證）。

**未解除之風險，須於每次上繳具名**：R-VF32／R-VF39 之機械化落實前，
R-VF17 之效果不可依賴。

**執行層上繳時須附「本包是否仍有該驗而未驗者」之獨立判斷。**
