# V51 — 實寫授權（附條件成就即生效）

下放包 **V51**。所據：Pei 於 2026-08-25 之答覆「**授權准許 繼續吧**」。
新增 **一條**（開號取實測最大 +1，**回報實得之號**）。**本包不新增工單** ——
W-VF82 維持 V50 §5 之六項不變。

---

## 1. 授權之範圍與其條件

```
R-VF-甲（VF230 之實寫授權，Pei 裁定 2026-08-25）
【落檔取實測最大 +1】

**實寫予以授權。R-VF110（實寫須為獨立之一次裁定）由本條滿足，
不再另求裁定。**

**惟其為附條件之授權，二條件成就前不得實寫**：

  **條件一：W-VF82 完成。**
    現行 440 條之 `test_item` **無一具括號下半**（V46 §1 實測 0/440），
    **且上半 token 逾 50 者 197 條**（44.8%）。
    canon §4.3.1 逐字：「**缺括號下半 = FAIL，不得出貨**」。
    **今日實寫即寫入 440 條違反 canon 硬規則者**，
    而其一經寫入，`R-VF14`（不回溯已交付）即生效。

  **條件二：分析層對 W-VF82 之產出抽樣覆核通過。**
    抽樣依 `R-VF76`，並含 V47 §5.3 之四項新判準
    （括號下半存在／上半 token ≤ 50／同上半之下半不得逐字相同／下半含 " -> "）。

**二條件皆成就者，執行層得逕行實寫，不再回 Pei。**
**任一條件未成就而實寫者，為違反本條。**

**本條不放寬既有之寫回拘束**，下列各條全數維持：
  `R-VF114`  B 欄起始號由實測導出（二本 CFTS044 末號取最大 +1），不符即停
  `R-VF115`  複製標的逐字具名檔名；**複製前實測目標檔非 0 列即停手回報**
  `R-VF116`  實寫後由 Pei 以 Excel 開啟 repo 內副本確認下拉可用，
             **確認前不得複製至交付路徑**
  `R-VF112`  寫回之目標為 repo 內副本；**交付路徑之對應本由 Pei 複製，兩層皆不寫**
  `R-VF113`  x14 之替代寫入路徑（openpyxl 存檔後補回 `<extLst>`）

**`R-VF115`／`R-VF116` 現尚未實作**（上繳 V49 §六具名為待辦）——
**其實作為實寫之前置**，併入 W-VF82。
```

---

## 2. 為何不即刻實寫 —— 一句話

**現在寫進去的 440 條，每一條都缺 canon §4.3.1 所令之括號下半，
而 canon 對其之處置逐字為「不得出貨」。**

W-VF82 正是補此者；其完成後實寫**不再需要你的第二次裁定**。
**本包之作用即為省去那一次往返** —— 授權已在，條件成就即生效。

---

## 3. 現況

```
已生成          440 條（seq 268–704 ＋ pilot #3 之 901–903）
W-VF82 後       444 列
括號下半         0 / 440   ← W-VF82 之主要工作
上半 token > 50  197 / 440
B 欄            依 R-VF114 實測重算（上輪為 244–683）
交付路徑 036     實測 0 資料列，可續行
```

**W-VF82 之六項不變**（V50 §5）：全量補括號下半與摘句／拆 2 條為 6 列／
多值形態補掃／未落檔條文常設清單／自檢全量綠／不寫回。
**併入 R-VF115、R-VF116 之實作**（本包 §1 末）。

---

## 4. 給 Pei

**授權已落檔，你不必再裁第二次。**

W-VF82 完成、本層抽樣通過後，執行層即逕行實寫至 **repo 內副本**。
**其後有一件仍需你動手**：

1. 以 **Excel** 開啟 `features/vehicle_setting/inputs/…_SWQT_VF230_20260819.xlsx`，
   確認下拉選單可用（openpyxl 會移除 x14，替代路徑已驗其形未驗其用）
2. 確認後，將該檔複製至交付路徑
   `/Users/peihe/Work/…/Vehicle Settings/VF230_V1_R5/`
   **逐字具名檔名，不得以萬用字元或目錄複製**（CFTS044 二本現已分岔，
   誤覆蓋會使 Part 1 之 238–243 六列消失）

**另一件仍待你**：`forms/Logical Identifiers and CAN Mapping v1_78.xlsx`
搬入 `inputs/`（V50 §6，素材補入屬你）。

---

## 5. 新條文清單（自檢）

| 暫稱 | 內容 |
|---|---|
| 甲 | 實寫授權；二條件（W-VF82 完成 ＋ 抽樣通過）成就即生效，不再回 Pei；既有寫回拘束全數維持 |

**⚠ 編號於落檔時取實測最大 +1，並回報實得之號。**

**工單**：無新增。W-VF82 維持，併入 R-VF115／R-VF116 之實作。

**執行層上繳時須附「本包是否仍有該驗而未驗者」之獨立判斷。**
