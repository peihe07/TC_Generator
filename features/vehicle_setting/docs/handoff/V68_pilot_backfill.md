# V68 — 20 條 pilot leaf 補入；`sqref` 採 (c)；A-VF 開立

下放包 **V68**。所據：Pei 於 2026-08-25 之答覆「**(c) 直接補入**」
＋ 分析層對交付本、`leaves.tsv`、`writability.tsv`、`vf230_isolated.tsv`、
`vf230_dedup_restorable.tsv` 五檔之**對帳**。
新增 **二條**（開號取實測最大 +1／+2，**回報實得之號**）、**W-VF90**。

---

## 1. 分析層之對帳：交付本少 20 個需求，其成因未查

**量測條件**：逕讀交付本之 D 欄（438 列）與四檔之 `leaf_id`，
還原 `\n` 逸出後以集合比對。**非採信任何前輪之數。**

```
035 之 Functional Requirement                     1087
  為 037 之 11 份所收（＝ leaf 母體）               627   ← leaves.tsv 實測
  未收                                              460

627 leaf
  W2 不可寫                                          53
  W0 ＋ W1 選池                                     574   ← writability.tsv 實測
      隔離（vf230_isolated.tsv）                      84
      去重（vf230_dedup_restorable.tsv 之 dropped）    35
      **既未寫入亦不在上二表者**                       **20**
      已寫入交付本                                    435
      （84 ＋ 35 ＋ 20 ＋ 435 = 574，**恰足**）
```

**該 20 個 leaf 逐一為 pilot #1 與 pilot #2 之標的**：

```
BlindSpotAlert-002/003/004                Blind Spot with Trailer Detection-045/046/049
PowerLiftgate/TailgateAlert-016/017/018   SuspensionServiceMode-002/003/006
LaneSenseWarning-014/015                  ParkSense-084/085
IlluminatedApproach-004  Language-059  SWITCH1Type-002  TimeandDateSettings-002
```

**其 TC 皆已寫成且 verdict 皆通過**
（pilot #1 v4 定稿於 `R-VF124` 之條件一；pilot #2 通過於 V29 §1），
**產物在 `generated/vf230_pilot1_v4.json` 與 `vf230_pilot2.json`。**

**其未進入工作簿一事，不在任何一份隔離表、去重表或雙方之「已知而未解」清單內。**
**其被發現係因 Pei 問「一共多少需求寫成多少測項」而本層逐檔對帳，
非因任何機制。**

---

## 2. 甲 —— 20 條補入；成因須查明且登記

```
R-VF-甲（pilot leaf 之補入，Pei 裁定 2026-08-25：「直接補入」）
【落檔取實測最大 +1】

**pilot #1 與 pilot #2 之 20 個 leaf 之 TC 補入交付本。**

**其 TC 之來源**：**不重新生成** —— `generated/vf230_pilot1_v4.json` 與
`vf230_pilot2.json` 之現有內容，**惟須先以現行之 20 項自檢全量驗過**。

**理由**：二 pilot 之產出成於 `R-VF118`（括號下半）與 `R-VF133`（摘句二層判準）
**之前**，其 `test_item` 可能不具括號下半、其上半可能為排除清單之形態。
**「verdict 通過」係對其當時所適用之判準而言，不蘊含其合於現行判準。**
**未過現行自檢者，依現行判準重寫該欄，不重寫其餘欄位。**

**補入後之數**：438 ＋ 20 = **458 列**，B 欄 244 – **701**。
**其實數以執行層之實測為準**，本層之 458／701 為推算。

**成因須查明並登記為 anomaly（不因已補入而免除）**：
20 個 leaf 全數在選池（W0/W1）內、全數可寫、TC 早已寫成且通過覆核，
**而量產之選池未涵蓋之。** 其為「已消耗之 leaf 被自選池扣除，
而其產出未被納入交付」之形態 —— **該形態若不登記，其於他 feature 必再現。**
```

---

## 3. 乙 —— `sqref` 採 (c)

```
R-VF-乙（`sqref` 之延伸，Pei 裁定 2026-08-25：「(c)」）
【落檔取實測最大 +2】

**P／R／T–Z／AF 四處之 `sqref` 一併延伸至列 1000。**

  一般 `dataValidation`   `P10:P1000`／`T10:Z1000`／`AF10:AF1000`
  x14 `dataValidation`    `R10:R1000`
  **`Q10:Q11` 之處置須具名** —— 其與 `P` 共用一條 `dataValidation`
  而其範圍僅 2 列；**其為原樣板之設定，本裁定不改之，惟須於上繳具名其現況。**

**四處同時改，不得只改其一**（`R-VF139`）——
同一份交付本內二種驗證範圍，其不一致本身即為日後之疑點來源。

**施行**：於 scratch 副本先行 → 以嚴格 parser 自驗 XML 合法 →
**重跑閘 4（重讀比對，其列數依補入後為 458 × 16）** → 差異即停。
**`x14dv` 之數於改前改後須相同。**

**其代價須入交付說明**：交付母本之驗證設定經修改；
**其上限 1000 為刻意留餘裕，非現行列數**（458），**該選擇之理由一併載明** ——
一個依列數而定而寫死之設定，其過時不會被任何檢查攔下（B 欄與本輪皆其實例）。
```

---

## 4. W-VF90 — 補入與延伸（**本層對 VF230 之最後一項**）

- **W-VF90.1** 20 條 pilot TC **以現行 20 項自檢全量驗**；
  **逐條回報其通過與否**；未過者依現行判準重寫該欄並具名所改之欄。
- **W-VF90.2** 補入 20 條，**B 欄續 682 起**；
  **補入前實測交付本現行末號**（`R-VF114` 之精神：不採信 681 之推算）。
- **W-VF90.3** `sqref` 四處延伸至 1000（`R-VF-乙`），含 `Q10:Q11` 之現況具名。
- **W-VF90.4** **重跑閘 3（x14 保全）與閘 4（重讀比對 458 × 16）**，差異即停。
- **W-VF90.5** **成因查明**：該 20 個 leaf 為何未入量產選池 ——
  查選池腳本之扣除邏輯；**開立 anomaly 並登記**（不因已補入而免除）。
- **W-VF90.6** 交付說明更新：測項數 438 → 458、需求數 435 → 455、
  §八增 `sqref` 之延伸與其上限之理由、§六增 20 條補入一項。
- **W-VF90.7** 備份檔於 Pei 二步後刪除（`R-VF135`，仍未辦）。

**完成後 Pei 之二步（Excel 確認 ＋ 複製至交付路徑）方為最終步驟。**

---

## 5. 給 Pei

**補入後之預期數（推算，以執行層實測為準）**：

```
需求  435 → 455      測項  438 → 458      B 欄  244 – 701
```

**那 20 條不是新寫的** —— 其 TC 早已寫成且 pilot verdict 通過，
**只是從未進入工作簿**。**惟其成於「括號下半」與「摘句二層判準」二條裁定之前**，
故須先過現行自檢；未過者只補正該欄，不重寫其餘。

**`sqref` 依你所裁延伸至 1000，四處一併** ——
上限取 1000 而非 458 是刻意留餘裕，**其理由（依列數而定之寫死設定，
其過時不會被任何檢查攔下）已令載入交付說明。**

**一件我要據實說**：那 20 條之缺席，**不在任何一份表、任何一份「已知而未解」清單裡**。
**它被發現是因為你問了「一共多少需求寫成多少測項」而我逐檔對帳** ——
**不是因為任何機制。** 故其成因已令查明並登記為 anomaly，
**否則同一形態於他 feature 必再現。**

---

## 6. 新條文清單（自檢）

| 暫稱 | 內容 |
|---|---|
| 甲 | 20 條 pilot leaf 補入；不重生成但須過現行自檢；成因須查明並登記，不因已補入而免除 |
| 乙 | `sqref` 四處一併延至 1000；scratch 先行、嚴格 parser、重跑閘 4；上限之理由入交付說明 |

**⚠ 二條之編號於落檔時取實測最大 +1／+2，並回報實得之號。**

**工單**：W-VF90（七項）。

**執行層上繳時須附「本包是否仍有該驗而未驗者」之獨立判斷。**
