# V32 — 改號裁定：採方向 1；另二項撞號之處置

下放包 **V32**。所據：執行層之撞號回報 ＋ 分析層對 `RULINGS.md` 之實測
（5134 行、166 個條文標題，2026-08-24）。
新增 **R-VF94**（1 條）。**本包不含新工單**；W-VF71 六項全數維持。

---

## 1. 撞號回報 —— 核可，採**方向 1**（執行層直接改號）

實測確認：`R-VF83`（B 欄 238，L1436）／`R-VF84`（併入，L1479）／
`R-VF85`（素材補入，L1498）皆已被佔用，
且 `R-VF86`（`output/` 位階）／`R-VF87`（REF 代用）／
`R-VF88`（W 號改編）／`R-VF89`（token 掃描）／`R-VF90`（Layer 2 前置複驗）亦然。
**現行最高正式條文為 `R-VF90`；`R-VF99` 為 `lint036.py` docstring 之錨點示例，非條文** ——
二判斷皆與實測相符。

**V31 之三條改編為**：

```
R-VF83（R-VF81 射程更正）  → R-VF91
R-VF84（錨點二則補強）      → R-VF92
R-VF85（停止宣告批數）      → R-VF93
```

改編範圍：`V31_rvf81_scope_fix.md` 之 §2／§3／§4 標題、§8 自檢表、
及全檔內對該三號之交叉引用。**檔頭加改編註記，比照 `R-VF83` 現行條文之改編註寫法。**
`W-VF71` 無撞號，維持。

**條文內文對 `R-VF81`／`R-VF76`／canon §8.4.1 之引用不受影響。**

---

## 2. 分析層另測到之二項（執行層未報）

### 2.1 `R-VF70` 現有**兩個定義**

```
L4412  R-VF70 —— tc_title 採純句式；括號別名不入標題        （V25 §3）
L4546  R-VF70 —— 判準以允許型別之白名單為之                （V25 §4）
```

**二者皆為 V25 包所出，同包內即撞號。** 本層當時將 §3／§4 分別標為
R-VF70／R-VF71，而 §4 之內容於落檔時被拆為兩條，**其一沿用了 §3 之號**。

**處置**：L4546 之「判準以允許型別之白名單為之」改編為 **`R-VF94`**。
L4412 之 `R-VF70` 維持（其被 V26 以後多次引用）。

### 2.2 `R-VF1` – `R-VF8` **在 `RULINGS.md` 中不存在**

V04 §3.1 之對照表令 `R-VS59`→`R-VF1` … `R-VS67`→`R-VF9`。
**實測：僅 `R-VF9` 依該表落檔（L3258）；`R-VS59`–`R-VS66` 實際改編為
`R-VF83`–`R-VF90`。**

即 **V04 §3.1 之對照表未被執行**，而本層於其後三十包中持續以 `R-VF1`／`R-VF2`
指稱其內容（如「R-VF1（B 欄序號自 238 起）」）。**該等引用為懸空引用。**

**R-VF48（引用而無定義之檢查）攔不到它** —— 其掃描面依 R-VF54 二限縮為
「會被據以行動之處」，而該等引用幾乎全在 `docs/handoff/`（豁免範圍內）。
**豁免是對的，缺口在別處：本層從未核對過自己的對照表是否被執行。**

---

## 3. R-VF94 —— 對照表之執行須經回報確認；不追改歷史

```
R-VF94（編號對照表之執行確認，分析層裁定 2026-08-24）

**一、`R-VF1`–`R-VF8` 之處置**：**不新建、不回填。**
其內容現存於 `R-VF83`–`R-VF90`，為現行有效之唯一定義。

**於 `RULINGS.md` 檔首之對照表增列**：
```
  V04 §3.1 曾令 R-VS59–R-VS67 → R-VF1–R-VF9，
  實際執行為 R-VS59–R-VS66 → R-VF83–R-VF90、R-VS67 → R-VF9。
  故 docs/handoff/ 內出現之 R-VF1–R-VF8 一律改讀為 R-VF83–R-VF90（順位對應）。
```
**歷史包不追改**（R-VF18）—— 對照表使其可解，即為足夠。

**二、V25 §4 之 `R-VF70` 重號改編為 `R-VF94`**（本條自身即該號之新定義處，
其正文見 `docs/handoff/V25_pilot1v3_review.md` §4，依 canon §8.7 不重複轉錄）。

**三、通則**：**分析層所發之編號對照表，其執行結果須於次一次上繳中逐項回報確認**，
不得假定已執行。

  **成因**：V04 §3.1 之表未被執行，而本層於其後三十包中未曾核對。
  **一張未經確認之對照表，與未發出者不可分辨。**

  **與 R-VF48 之關係**：R-VF48 攔「引用而無定義」，其掃描面已依 R-VF54 二
  正當地排除 `docs/handoff/`；**本條所補者為「對照表之執行」此一動作本身**，
  非擴大 R-VF48 之掃描面。
```

---

## 4. 執行順序（答執行層之提問）

**先改號，後跑工單。** 順序：

1. V31 之三條改編為 R-VF91／92／93（§1）
2. L4546 之 `R-VF70` 改編為 R-VF94（§2.1）
3. `RULINGS.md` 檔首對照表增列 R-VF1–R-VF8 之改讀規則（§3 一）
4. 落檔 R-VF91／92／93／94
5. **W-VF71 六項全數照跑**，順序不變：
   100 條解除隔離 → `reasoning` 必要句列自檢增項 → B 類 11 條開 DR →
   `PLUMBING` 反向測 → 全庫「必不命中」錨點複驗 → 重跑自檢 →
   續生成第 2 組並與本組併為一次上繳

**1–4 為 5 之前置，但四項合計為機械改字，得於同一輪與 5 併行完成。**

---

## 5. 新條文清單（自檢）

| 編號 | 型別 | 區塊 |
|---|---|---|
| R-VF94（對照表執行須經回報確認；R-VF1–R-VF8 不回填；V25 §4 重號改編） | 分析層裁定 | ✅ §3 |

**改編**：R-VF83→91／R-VF84→92／R-VF85→93（V31）；R-VF70(L4546)→R-VF94。

**分析層本輪之錯**：V04 §3.1 之對照表未經確認即被視為已執行，
致三十包中之 `R-VF1`–`R-VF8` 為懸空引用（§2.2）；
V25 同包內 §3／§4 之編號重複（§2.1）。

**執行層上繳時須附「本包是否仍有該驗而未驗者」之獨立判斷。**
