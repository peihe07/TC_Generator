# 80 下放包 — probe 抽驗結果、R-VS75、52 輪

分析層寫入，2026-08-23。

---

## 1. probe 抽驗：E 通過／F 通過須拆／**D 不通過**

### D 型 `-014` —— false pass，退回

test_item 為四階真值表（High→Medium／Medium→Low／Low→Off／Off→High），
而 procedure 只驗 `HSW_Tlm = 1 (Pressed)`。

與 `-012` 對比：procedure 僅差 step 1 之值，**ER 逐字相同**。
設 `off` 得 Pressed、設 `high` 亦得 Pressed ——
**HU 送錯階數亦通過**。`reasoning` 稱「驗 High 一列」而實未驗之。

```
分析層裁定 2026-08-23
D 型中其驗證目標本身為 DR-15 標的者（真值表之送出值），
**驗證步驟之 ER 寫 `PENDING: DR-15`**，前置步驟照寫
（R-VS71：值於來源亦無 → 該處 PENDING，其餘照寫）。
**不得以「有送出」代替「送出正確之值」** —— 後者為條文所述之驗證目標。
`-014` 退回改寫；D 型待其過檢方得放量。
```

### F 型 —— 通過，惟二處須修

- `-022` 之 test_item 為 `[M4 OR MP]`，TC 只驗 `M4 (98)`，**未驗 `MP (93)`**
  → 依 §8.3 為兩個獨立之部分失效，**拆二條**（F 型由 2 → 3）
- `-021` 之 pre_condition 1「The vehicle is a plug-in hybrid」與 step 2 之
  `Set PROXI Hybrid_Type` 重複 → 違 §4.5 欄位歸屬，**刪 pre_condition**

### E 型 —— **通過，放量**

baseline 具名變數（`HSW_display_before`）、最弱斷言、雙 BLOCKED、
`-017`／`-018` 互為對稱。**餘 14 條放量。**

---

## 2. R-VS75 —— 直接往返之裁定須回流

```
R-VS75（分析層裁定 2026-08-23）
執行層與 Pei 之直接往返（選項式徵詢等）所生之裁定，
須於**次輪上繳包之 §5** 具名列出：徵詢之內容、所列選項、Pei 之擇定、日期。
**未回流者不得作為判準之依據**（R-VS35 之延伸）。

理由：兩層間唯一之流通物為下放／上繳包；
直接往返之裁定分析層看不到，其判準基礎因而分岔。
§7 讀法為第一個撞上者，凡以選項式徵詢取得者皆有此問題。
```

---

## 3. 三項確認

| 項 | 確認 |
|---|---|
| §2.1 兩讀差 84 vs 72 | **不影響交付基準** —— R-VS74 已裁讀法 B，應補 0，總量仍以 **245** 為準。84 僅為讀法 A 之數，供對照 |
| §2.3 母數 76 vs 82 | **正確**。以作業後之母數計，差 6 即 probe 之 leaf |
| §2.4 分層維度單值 | **A-VS142 形態再現**。**分層維度須先驗其於該母體非單值**，否則退化為順序取樣 —— 記為 pilot 抽樣之通則 |

---

## 4. 52 輪指令

```text
你是 FW036 管線之執行層。repo: /Users/peihe/Work_Projects/TC_Generator
第 52 輪。**本輪不寫回母本。**

讀：docs/handoff/80_probe_verdict.md（本輪依據）＋ RULINGS.md
    ＋ docs/runtime/ASPICE_SWE6_AI_Instruction.md §8.3／§4.5

## 文書
D-1  建 docs/upstream/45_probe_fix.md，六節先留空。
D-2  逐字轉錄 80 包 §2 之 R-VS75 入 RULINGS.md。
D-3  A-VS156 標「依 R-VS75 處置」。依 R-VS35 分線列兩數。D-6 骨架對照。

## 作業
W-149  probe 之修正
       (1) `-014` 依 80 包 §1 改寫：驗證步驟之 ER 改 `PENDING: DR-15`
       (2) `-022` 依 §8.3 拆二條（`M4 (98)`／`MP (93)`），
           `split_flag = true`、`split_reason` 記其軸
       (3) `-021` 刪 pre_condition 1（§4.5）
       §9 自檢 ＋ 固定錨點 20 項

W-150  **E 型放量** —— 餘 14 條 → `batch22.json`
       套 profile ＋ 各現行條文 ＋ Sibling Rows；§9 自檢 ＋ 錨點
       **D／F 二型待 W-149 過分析層抽驗後方放量**

W-151  W-147 之預估更新
       依本輪之拆分（F 型 +1）重算母體 237 之最終 TC 總量，
       **以讀法 B 為基準**（R-VS74）

## 禁區
git 不執行。不寫回母本。不補素材、不代擬條文。各版保留不刪。
不得以 openpyxl 存檔 xlsx。
**D 型不得以「有送出」代替「送出正確之值」**。

## 升級條件
W-149 之 §9 自檢非 0；
W-150 之 14 條有任一其觸發非「收到狀態訊號」（則其非 E 型）；
W-151 之總量與 245 差 > 10。
```

---

## 5. 本包產生之新條文清單（自檢）

| 條 | 主題 | 裁定者 |
|---|---|---|
| **R-VS75** | 直接往返之裁定須於次輪上繳 §5 回流具名 | 分析層 |
| D 型之 ER 處置 | 驗證目標為 DR 標的者，ER 寫 PENDING；不得以「有送出」代替 | 分析層 |
| 分層維度之通則 | 抽樣前須驗其於該母體非單值 | 分析層 |
