# V72 — 排序鍵已裁：W-VF92 解封施行

下放包 **V72**。所據：Pei 裁定 2026-08-25
「回覆(a) 都是依照 requirement ID 順序啊 你需要寫回專案適用等級」
＋「那要幫我處理回去這裡啊」（即令施行）。

**本包對交付物之推進：457 列重排＋19 條補入後之 036 repo 內本**（R-G31）。

---

## 1. 裁定落檔

### 問一：裁 (a) —— 依 Requirement ID 升冪，且升格為專案適用等級

- V71 §4 之 `R-VF-甲` **不另立 R-VF 號**，由全域條文承接：
  **`R-G32`（交付列序 = Requirement ID 升冪 —— 全域）**，
  全文已落 `docs/fw036/RULINGS_LEDGER.md`（引用即據，R-G13）。
- 要點：交付列序一律依 Requirement or Design ID 升冪（family 字母序、
  尾碼數值序；三位零填故字串序即數值序）；同一 ID 之多列相鄰；
  選池序（R-VS58）射程限於批次組成，寫回時必須重排。
  適用全部 feature；既有已交付本於下一次寫回時套用。

### 問二：未裁 —— 本輪不施行

- `Test Case Framework` 分頁**維持空**，W-VF92.4 本輪跳過。
- 涵蓋對帳仍存 `docs/reports/` 與 `data/*.tsv`。
- 若 Pei 日後裁「載」，屬**新一輪寫回**，須承擔再動檔一次之代價
  （V71 §6「不分次」之警語即為此而設）。

---

## 2. W-VF92 解封 —— 施行規格

### W-VF92.1 已解除
問一已裁；問二以未裁＝不載處置（§1）。

### W-VF92.2 補入＋重排（同輪一次完成，不分次）
1. **先補入 19 條**（W-VF91.1，已核可之 pilot 缺口補入）。
2. **全表重排**：
   - 排序鍵 = **D 欄整字串升冪**（唯一鍵）
   - **stable sort** —— 同一 D 值之多列保持原相對序
   - ~~B 欄依重排後之位置**全部重寫**，自 001 起連續，格式循既定 ID 規格~~
     **撤銷（V73 §1）**：「自 001 起」為分析層筆誤，與 R-VF83（自 238 起、
     08-24 改裁 244）及 CFTS044 之 B 欄凍結配套直接衝突。
     現行：**B 欄自 244 起連續遞增**，起點取自實測不寫死。
     執行層取 244 為正辦。
3. **產出 ID 對照表** `data/vf230_id_remap.tsv`：
   欄 = 舊 B 值、新 B 值、D 值。重排使 B 欄全變，無此表則
   pilot 紀錄、DR、隔離表之舊 B 引用全部斷鏈。

### W-VF92.3 閘
- 重跑閘 3、閘 4（457 列 × 16 項）
- **新增一閘**：D 欄逐列非降序，違者即停
- 列數對帳：資料列 457（438 ＋ 19）；對帳口徑 已寫 454 ＋ 隔離 84
  ＋ 去重 35 ＋ DR-48 剔除 1 ＝ 574。若實測與此不符，**停，回報，不得自行調帳**

### 寫回工法（沿用既定，66_writeback_procedure）
- XML 手術式：僅動 `xl/worksheets/sheet*.xml` 目標範圍，其餘 part 不觸
- **openpyxl 不得存檔**（x14 命名空間剝離之既知缺陷）
- 前後驗證：`<dataValidation`、`x14:dataValidation`、`<conditionalFormatting`
  計數、分頁數、drawing/chart rel 計數，一致方過

### W-VF92.5（屬 Pei）
完成並經上繳覆核後，repo 內副本由 **Pei 複製至交付路徑**（R-VF112）。

### W-VF92.6
W-VF91 其餘各項（`H10:H1000`、A-VF32 對治、交付說明更新、備份刪除）
維持，隨本輪同批。

---

## 3. 上繳要求

- 附未結 DR 清單（S6）
- 附「本包是否仍有該驗而未驗者」之獨立判斷
- 回報：實測列數、閘結果、對照表列數（應 = 457）、
  重排前後 family 段數（重排後應 = 109，即每 family 恰一段）
