# V66 — 收尾核可；**下拉之涵蓋範圍只到列 132**（分析層獨立複驗所得）

下放包 **V66**。所據上繳：`docs/upstream/V65_wvf88.md`（2026-08-25）
＋ **分析層對 `inputs/…_SWQT_VF230_20260819.xlsx` 之獨立複驗**
（不經寫回腳本之介面，逕讀 xlsx 與其 sheet XML）。
新增 **一條**（開號取實測最大 +1，**回報實得之號**）、**W-VF89**。

---

## 1. 分析層之獨立複驗：資料面全數相符

**逐項列其名與射程（`R-VF136`），不以「全綠」概括**：

```
資料列                              438
逐欄非空  B/C/D/G/H/I/J/K/L/M/N/P/R/AA  各 438     AH  65     F  0
B 欄                                244 – 681，連號
D 欄唯一                            435（3 組為拆列，與 §三所報相符）
G 欄 distinct                       {'Vehicle Setting'}
H 欄 distinct                       9（已鎖之九名）
sheet6.xml                          XML 合法（嚴格 parser）
extLst                              512 字元｜`xmlns:x14` ✅｜`xmlns:xm` ✅
x14:dataValidation                  2（與寫入前之原檔相同）
```

**上繳 §五所報之數，本層以獨立路徑複驗相符。**

---

## 2. 【新測】下拉之 `sqref` 只涵蓋至列 132，而資料至列 447

**逐字自 `sheet6.xml` 抽出全部 `sqref`**：

```
一般 dataValidation
  AF10:AF13              "Pass, Fail, Pending, Block, NA"
  **P10:P132** Q10:Q11   "P0,P1,P2,P3"          ← Priority
  **T10:Z132**           "0,1"
x14 dataValidation
  **R10:R132**           Reference!$C$4:$C$12   ← Design Method
```

**438 列佔列 10–447。**

| 欄 | 下拉涵蓋 | 已寫入之列 | **無下拉之列數** |
|---|---|---|---:|
| P（Priority） | 10–132 | 10–447 | **315** |
| R（Design Method） | 10–132 | 10–447 | **315** |
| T–Z | 10–132 | — | （本次未寫入該欄組） |
| AF（結果） | 10–13 | — | （測試執行時填，非本次） |

**即：P 與 R 二欄之下拉，於第 124 列之後（B 欄 367 起）不存在。**

**其非本輪寫入所造成** —— `sqref` 為樣板之既有設定（其原設計為 123 列之容量），
**寫入 438 列並不會使 `sqref` 自動延伸。**

**其對 Pei 第一步之意義**：開檔後前 123 列之下拉可用、其後 315 列無下拉。
**若不預先知悉，該現象極易被讀為「x14 之補回失敗」** ——
**而 x14 之補回實為成功（`x14dv` 2、XML 合法、`xmlns` 宣告齊備）。二者須分辨。**

---

## 3. 甲 —— `sqref` 之延伸須經裁定，不得逕行

```
R-VF-甲（資料驗證範圍之延伸，分析層裁定 2026-08-25）
【落檔取實測最大 +1】

**`sqref` 之延伸為對交付母本結構之修改，須經 Pei 裁定，不得由任一層逕行。**

**三項選擇，其代價不同**：

  (a) **不延伸** —— 交付本之 P／R 二欄於列 133 之後無下拉。
      **其值已由本層寫入且經 20 項自檢驗證**（項 7 驗 priority、
      項 1 之 canon 判準驗 design_method 之值域），**故資料正確而輸入輔助缺席。**
      **代價**：後續人工編輯該二欄時無下拉可選，易生拼寫變體。

  (b) **延伸至 R447／P447** —— 改 `sheet6.xml` 之 `sqref`。
      **代價**：修改交付母本之驗證設定；其為 XML 層之手術，
      **且 `x14` 之 `sqref` 與一般 `dataValidation` 之 `sqref` 須一併改**，
      漏其一即二欄行為不一致。

  (c) **延伸至一個有餘裕之上限**（如 R1000）——
      同 (b)，**惟其對日後之增列亦有效**。

**本層建議 (c)**，其理由與 `R-VF132` 之 B 欄同型：
**一個依列數而定之設定，若其上限寫死於樣板，其過時不會被任何檢查攔下** ——
本輪即其實例（樣板之 123 列容量，寫入 438 列而無人察覺，
**直至本層逐字讀 `sqref` 方見**）。

**在裁定前不動。** 現況（(a)）為交付本之既成狀態，其資料正確。
```

---

## 4. 三處具名嘉許

1. **§三.1 —— 複驗過程中之二錯皆自查自改，其一更正本層之誤讀。**
   本層 V63 曾稱「037 之 leaf 為 627 不是 745，與 `R-VF12` 之 745 不符」——
   **實為 745 是總列數（含 heading 126），627 是 leaf 數，二者不矛盾。**
   **本層未細讀 A-VS132／A-VS133 之既有記載即報為不符。**
2. **§三.1 之「漏解 2 份 037」為 A-VF126 之同族第三例** ——
   硬編分頁名 `Analysis Report`，而 11 份中有 2 份為 `Sheet1`。
   **改以「表頭列首格 = `SWE-Requirement ID`」定位，即以內容定錨而非以名定錨**
   （`R-VF128` 之精神）。
3. **§一 —— 依 `R-VF136` 逐項列出 20 項之名與射程，並具名射程外之標的。**
   **「`test_item` 之節錄取捨為人讀之標的，機械判準不及於此」** ——
   一份自檢報告主動劃出自己管不到的地方。

---

## 5. W-VF89 — 待裁項與最終收尾

- **W-VF89.1** **`sqref` 之處置待 Pei（§3）**；裁 (b)／(c) 者，
  **其修改須於 scratch 副本先行並以嚴格 parser 自驗**，
  且**改後重跑閘 4（重讀比對 438 × 16 欄）**，差異即停。
- **W-VF89.2** 備份檔 `_vf230_036_prewrite_backup.xlsx` 於 Pei 二步後刪除（如令）。
- **W-VF89.3** 交付說明草稿增一節：**下拉之涵蓋範圍**（其現況、其非補回失敗、
  及 `sqref` 之裁定結果）。

**本工單為 VF230 之最後一項；其完成後本層之工作結束。**

---

## 6. 給 Pei —— 開檔前請先讀這一段

**你的第一步是「確認下拉可用」。** 本層先告知其預期現象，以免誤判：

```
P（Priority）與 R（Design Method）二欄
  列 10–132（B 欄 244–366）   下拉**可用**
  列 133–447（B 欄 367–681）  **無下拉**   ← 315 列
```

**這不是 x14 補回失敗。** x14 之補回為成功：`x14dv` 2、XML 合法、
`xmlns:x14` 與 `xmlns:xm` 宣告齊備 —— 本層以嚴格 parser 逐字驗過。

**其成因是樣板之 `sqref` 原設定只到列 132**（原設計容量 123 列），
**而寫入 438 列不會使 `sqref` 自動延伸。**

**請裁**：不延伸／延伸至 447／延伸至一個有餘裕之上限（如 1000）。
**本層建議第三者** —— 理由與 B 欄同：一個依列數而定而寫死於樣板之設定，
**其過時不會被任何檢查攔下**，本輪即其實例。

**二欄之值已由本層寫入並經自檢驗證**（項 7 驗 Priority、項 1 之 canon 判準驗
Design Method 之值域），**故資料正確，缺席者為輸入輔助。**

**其餘二步不變**：確認版面（欄寬／換行／對齊）；
確認後**逐字具名檔名**複製至 `…/VF230_V1_R5/`。

---

## 7. 新條文清單（自檢）

| 暫稱 | 內容 |
|---|---|
| 甲 | `sqref` 之延伸須經 Pei 裁定；三選項及其代價；建議 (c)；裁定前不動 |

**⚠ 編號於落檔時取實測最大 +1，並回報實得之號。**

**工單**：W-VF89（`sqref` 待裁、備份刪除、交付說明增節）。

**分析層本輪之錯**：V63 §之「745 與 627 不符」為未細讀既有記載所致之誤讀
（已由執行層更正，§4-1）。

**執行層上繳時須附「本包是否仍有該驗而未驗者」之獨立判斷。**
