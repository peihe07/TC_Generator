# V17 — 委派判定為 P4 前置、P0 對映之部分核可、R-VF48 判準精確化

下放包 **V17**。對應上繳：`docs/upstream/V17_*.md`。
本包新增 **R-VF50–R-VF52**（3 條）、**W-VF46–W-VF49**（4 項工單）。

覆核對象：`docs/upstream/V16_p3_done_p4_prep.md`
＋ `docs/reports/vf230_priority_batches.md`。

---

## 1. 覆核結論：**核可**。**批次生成暫不啟動**（理由見 §2）

具名嘉許三處：

1. **§2.1 之自我更正兩度且逐次具名** —— 首版 621/6 之低估，成因為
   「鏈 3（PROXI）宣告而未實作」；PROXI 之量測本身又因掃描範圍過窄
   （前 400 列／前 6 欄）而誤報「46 中僅 4 在表」，改掃 `Format` 分頁
   800 列後為「35 在表／11 無」。**兩次皆為執行層自查而得，非本層指出。**
   其鑑別錨點 `Blind_Spot_Monitoring`（在表）vs
   `Blindspot_Trailer_Detection`（不在）尤其精準 —— 二名近似，
   一條子字串比對之規則必誤判其一。

2. **鑑別錨點答「不存在」而非略過** —— W-VF44 第 5 項令取一個
   「LID／DBC 皆無而 VC-VM 有」之 leaf；實測 **0 個**。
   執行層據實回報「A-VS118 之形態於 VF230 無對應者，R-VF13 之來源開放
   於 VF230 現無實際作用」。**一條裁定在此 feature 無作用，是可回報之結論，
   不是失敗。**

3. **A-VF11 —— 兩條各自正確之規則交互產生誤報。** R-VF10 所令之
   `ANOMALIES.md` 舊制標記，使 R-VF48 之抽取式失效。
   **此形態於本線首見**，其記錄價值高於該次誤報本身。

**分析層本輪之錯**：`A-VF1` 自始未落檔（`upstream/V04_*` 為缺號），
與 `R-VF9` 同型之第二例。本層於 V05–V16 十二包中同樣從未查證。
**R-VF48 立法後首跑即攔下，其立法時點晚了十包。**

---

## 2. R-VF50 —— 委派判定為 P4 之前置，批次生成暫停

```
R-VF50（VF230 之委派判定，分析層裁定 2026-08-23）

上繳 V16 §6 第 2 項自判為「本輪最大之未驗項」，本層同意，並升其位階：

**委派判定（R-VS7／blocker 碼 B1–B3）於 VF230 之 627 leaf 完全未做。**
Part 1 有 **99 個 B2**（畫面層委派 Comfort）。VF230 之 627 leaf 中
是否有應委派者未查；未查者現一律判 W0，**將被寫成 TC**。

**其後果不對稱**：
  漏判委派 → 寫出本不屬本 feature 之 TC，違反 canon §8.2.1（不得擴張至
             sibling Req 之範圍）與 §8.4.2（不得測本規格未擁有者），
             且該 TC 已進入批次後，回收成本遠高於事前判定
  誤判委派 → 該 leaf 暫不產 TC，事後補回，成本低

**故：委派判定完成前，VF230 不得開始批次生成。** 包含 pilot 批。

**此非新增阻塞，是把既有之未驗項移到其應在之位置** ——
canon §3 之工作流第 1 步即為「理解需求（行為、觸發、結果）」，
而「此需求是否屬本 feature」先於該步。

**W-VF45 之批次規劃（63 批 × 10、pilot 為第 1 批）不作廢**，
其於委派判定完成、池數確定後重算即可。
```

---

## 3. R-VF51 —— P0 對映部分核可，須補 canon §10.2 之 safety 一類

```
R-VF51（VF230 之 P0 對映，分析層裁定 2026-08-23）

**一、已提之九簇予以核可。** 其依 R-VS56 之原則（實體致動且具傷害可能／熱源）
對映，逐簇附「驅動何機構、乘員何以可能在其行程內」，判準可辯護：

  Power Tailgate ／ Power Liftgate/Tailgate Alert ／ Power Side Step ／
  Suspension Auto Entry or Exit ／ Suspension Default Ride Height ／
  Suspension Flash Lights With Lower ／ Suspension Sound Horn With Lower ／
  Suspension Service Mode ／ Driver Easy Exit Seat

  **鑑別錨點 `Suspension Display Messages` 判非 P0 正確** ——
  其為訊息顯示而非車身致動；以 `Suspension` 為鍵之規則會誤判之。

  **P0(b)（熱源）= 0** 亦核可：VF230 無加熱功能。

**二、對映之涵蓋面不足，須補。**

R-VS56 之二類係**以 Part 1 之內容界定**（頭枕下放、加熱元件）。
以其原則外推至 VF230 為正確作法，**但 R-VS56 非 P0 之全部定義** ——
canon §10.2 之 P0 為「safety, boot/recovery, connection, audio output,
eCall, vehicle-critical CAN signal, data-loss risk」，
**`safety` 一詞不限於實體致動。**

VF230 含下列 ADAS 相關設定，其開關直接改變安全警示系統之行為：

  Forward Collision Warning ／ Forward Collision Warning Sensitivity ／
  Pedestrian Emergency Braking or Warning & Active Braking ／
  Blind Spot Alert ／ Blind Spot with Trailer Detection ／
  Lane Sense Warning ／ Lane Sense Strength ／
  Park Sense ／ Park Sense Front Volume ／ Park Sense Rear Volume ／
  Traffic Sign Warning

**該類現全數落於 P1／P2。** 其是否應為 P0，**本層不逕裁** ——
須依 canon §10.2 之 `safety` 逐簇具名判定，見 W-VF47。

**判定之界線建議（非結論）**：
  改變「警示是否發出」者，其失效使駕駛失去一次警示 → 傾向 P0
  改變「警示之音量／靈敏度／樣式」者 → 傾向 P1

**三、在 W-VF47 完成前，選池順序不得定案** ——
R-VS58 令選池依 P0→P1→P2，**而 pilot 批正是自池首取**（上繳 V16 §6 第 3 項
已具名此連動）。P0 集合若變，pilot 批之內容隨之變。
```

---

## 4. R-VF52 —— R-VF48 之誤報以判準精確化消除，不以白名單

```
R-VF52（R-VF48 判準之精確化原則，分析層裁定 2026-08-23）

R-VF48 之檢查現 FAIL 57 項，執行層已逐類具名為 (a) 真缺漏／
(b) 判準過窄之殘留／(c) 偽陽性。

**三類之處置不同，不得以同一手段處理**：

  **(a) 真缺漏**（`R-VF1`／`R-VF2`／`R-VF8` 等，即 A-VF10 之撞號集）
      → 待 R-VF49 之改號。**其 FAIL 為正確之報告，不得消除。**

  **(b) 判準過窄之殘留**（`R-VS1`／`DR-5`／`DR-11`）
      → **修正定義偵測之抽取式**，使其涵蓋條文簿之實際形態
        （早期條文之標題形態、`DATA_REQUESTS.md` 之表列編號如 `5-A`／`9`）。
      **不得以「豁免該編號」處理** —— 豁免會使日後該號真正缺漏時無法被察覺。

  **(c) 偽陽性**（`A-VS001`／`A-VS199`／`R-VF99`）
      → **精確化引用之抽取式**：排除程式碼字串與 docstring、
        排除數列片段（如 `A-VS001` 之三位零填形態於本簿不存在）。
      **同樣不得以白名單處理。**

**通則**：檢查之誤報須以**判準之精確化**消除，不以**例外清單**消除。
例外清單使檢查在該處永久失明，而失明處不會報錯（A-VS106／A-VF1 之形態）。

**驗收條件**：修正後之 FAIL 應**僅餘 (a) 類**，其數應等於 A-VF10 之
撞號集大小。若仍有 (b)(c) 殘留，逐項具名其不可精確化之理由。

**R-VF46 第 4 項之「已知未解」標記適用於 (a) 類**，
使其與新失敗可分辨；(b)(c) 不得使用該標記。
```

---

## 5. 工單

### W-VF46 — VF230 之委派判定（**最優先，P4 前置**）

1. 沿用 Part 1 之委派判準（R-VS7／Part 1 之 R-VS59「委派不等於不寫」／
   B1–B3 碼）。**不新設判準**；Part 1 之定義為既有政策，優先。
   **注意**：此處之 `R-VS59` 為 **Part 1 線**之條文，非 VF230 線之 B 欄續號條
   —— 即 A-VF10 之撞號實例，引用時須以線別判之。
2. 對 627 leaf 逐一判是否應委派他 feature（Comfort／Home／Power Moding 等），
   產出 `docs/reports/vf230_delegation.tsv`。
3. **逐 Test Set 報委派數**，並報委派之目的 feature 分布。
4. 依 R-VF21／R-VF28 附三錨點；**鑑別錨點須取一個「簇名近似 Comfort 之標的
   而實不應委派」者**（Part 1 之 99 個 B2 為畫面層委派，其誤判形態即在此）。
   不存在則具名「不存在」。
5. **不生成 TC。** 委派者於 `writability.tsv` 標其 blocker 碼，不改其他欄。

### W-VF47 — P0 之 safety 一類補判（R-VF51 二）

1. 對 §3 所列 11 簇（ADAS 相關）逐簇依 canon §10.2 之 `safety` 判 P0 歸屬。
2. **逐簇具名**：其失效之後果為何、駕駛因而失去什麼。
3. 建議之界線（改變「是否發出警示」vs「音量／靈敏度／樣式」）
   **為建議非判準**，採否須具名理由。
4. 產出修訂後之 P0／P1／P2 計數，**與現行 49／288／290 並陳其差異**。
5. **不重排選池**（待 W-VF46 之池數確定後一併重算）。

### W-VF48 — W1 之 DR 衝突閘實測（上繳 V16 §6 第 1 項）

執行層自述「VF230 只受 DR-21 波及 7 leaf，故影響應小，**但『應小』是推論，
未實測**」。**依 canon §5a，推論不得代替量測。**

對 VF230 之訊號值域跑 `guard_new_conclusion()` 之 DR 衝突閘，回報實數：
受衝突之 leaf 數、其現行分級、閘後是否應改判。**只測不改。**

### W-VF49 — R-VF48 判準之精確化（依 R-VF52）

修正定義偵測與引用抽取二式，使 FAIL 僅餘 (a) 類。
**不得使用白名單或豁免清單。** 修正後重跑並附三錨點實測；
殘留之 (b)(c) 逐項具名其不可精確化之理由。

### W-VF43 — 維持原範圍（158 處判線別，只判不改）

本輪未執行，範圍已由 W-VF39 備妥，不重述。

---

## 6. 優先序

```
W-VF46  →  W-VF47  →  W-VF48  →  W-VF49  →  W-VF43
```

W-VF46 居首：**批次生成之前置**（R-VF50）。
W-VF47 次之：其改變選池順序，而 pilot 批自池首取。
W-VF48／W-VF49 為分級與檢查之補強，不阻塞。
W-VF43 殿後：寫回凍結期間不阻塞（R-VF49）。

---

## 7. 待 Pei 裁定

**無。**

**下一個預期回到你手上者**：W-VF46／W-VF47 完成、選池重算後之
**pilot 批範圍與時點**。本層先前預告該項會在本輪回到你手上，
**因委派判定未做而順延** —— 順延之理由為 R-VF50，非遺忘。

---

## 8. 本包產生之新條文清單（自檢）

| 編號 | 型別 | 區塊 |
|---|---|---|
| R-VF50（委派判定為 P4 前置；批次生成暫停；後果不對稱之理由） | 分析層裁定 | ✅ §2 |
| R-VF51（P0 九簇核可；須補 canon §10.2 safety 一類；選池待定案） | 分析層裁定 | ✅ §3 |
| R-VF52（誤報以判準精確化消除，不以白名單；三類處置不同） | 分析層裁定 | ✅ §4 |

**工單**：W-VF46（委派判定，**最優先**）／W-VF47（P0 safety 補判）／
W-VF48（DR 衝突閘實測）／W-VF49（R-VF48 判準精確化）／W-VF43（維持）。

**分析層本輪之錯**：`A-VF1` 未落檔，十二包未查證（§1）；
V16 §4 列 P4 前置三項，**未列委派判定** —— 與 V15 §2.1 之問法過窄同型，
**連續兩包漏列同一類前置**。

**未解除之風險，須於每次上繳具名**：10 條跨線條文未受保護
（其中 4 條為程序義務、不可機械檢查）。A-VF10 未解前，
`R-VF10` 與 `R-VF48` 二項持續 FAIL。

**執行層上繳時須附「本包是否仍有該驗而未驗者」之獨立判斷。**
