# `DELIVERY.md` —— FW036 Vehicle Setting（CFTS044）交付說明

執行層產出，W-162（56 輪）。依 `docs/handoff/84_delivery.md` §5。

> **交付狀態：定稿（57 輪 W-165）。**
> 56 輪 R-VS77 回掃所得之 17 條 `test_item` 修正**已寫入母本**。
> 母本現為 **`c72b9556…`，243 列**；`generated/` 與母本**無差**。

---

## 1. 三個分母

| 分母 | 數 | 說明 |
|---|---:|---|
| **母體** | **237** leaf | CFTS044 之 in-scope leaf 全量 |
| **交付** | **225** TC ／ **219** leaf | 一 leaf 一列，拆分者多列 |
| **經人工關卡** | **69** 條（225 之 **31%**） | pilot #1 8 ＋ #2 15 ＋ #3/#4 28 ＋ #5/#6 18 |

**三者不可互推**：225 非 237 之子集（拆分使 TC 多於 leaf），
69 為 225 之抽樣（**其餘 156 條未經人工覆核**）。

## 2. 母體之三類（R-VS76，和須為 237）

| 類 | leaf | 說明 |
|---|---:|---|
| 有 TC | **219** | 其 TC 合計 225 |
| `held_out` | **7** | G1 適用性前言 5（無觸發亦無可觀察標的）＋ G4 轉指他節 2；**各附條文逐字與理由**，並寫入母本之 AH 欄 |
| `generatable = no` | **11** | W2 池，待 DR 覆 |
| **合計** | **237** | 無未歸類（`completeness_w154.py` 每輪驗之） |

## 3. 待補清單（依 R-VS69 分節）

| 節 | 條 | 定義 |
|---|---:|---|
| **§A** 單一缺值待補 | **25** | 部分步驟之 ER 為 `PENDING`，其餘步驟已可驗 |
| **§B-1** 畫面層完全未驗 | **0** | 全部 ER 皆 `PENDING` 者 —— **本交付無此類** |
| **§B-2** 畫面層部分未驗 | **39** | 已驗其變更，樣式／內容待補（AH 載 `BLOCKED: DR-5-B`） |
| **§C** `IMPL_GAP` | **56** | 依 R-VS67′(2)／R-VS66(a)：取能承載之欄組，其訊號不在基線 DBC。**預期於執行時發現實作缺口，應開 issue 予 RD** |

§A 與 §B-2 有重疊（同一 TC 可兼具），故不加總。

## 4. 未結 DR 及其影響條數

| DR | 影響之 TC | 標的 |
|---|---:|---|
| `DR-24′` | **49** | 時限（`<Tsend>` 等）之上限值 |
| `DR-25` | **44** | （見 `DATA_REQUESTS.md`） |
| `DR-5-B` | **39** | 畫面層之具體樣式與內容（TLM HMI Document） |
| `DR-18` | **20** | 2 bit 狀態訊號無未用碼 —— 無效值之定義 |
| `DR-15` | **11** | 請求訊號之編碼（逐狀態之送出值） |
| `DR-19` | **5** | `$EngRun_Stat$` 四值與 `STATUS_CCAN3.EngineSts` 之對應 |
| `DR-22` | **4** | `HDRstRelRq_3rdRow` 之值域（LID `fmt` 空、DBC 無） |
| `DR-21` | **2** | （見 `DATA_REQUESTS.md`） |
| **受影響之相異 TC** | **144**（225 之 **64%**） | |

`DATA_REQUESTS.md` 現列 DR-5／7／8／8′／11–34。**未送出者見該檔之狀態欄。**

## 5. 已知限制

1. **`dr15_exposed` 之 58 條**，其形態可能因 **DR-15′** 之答覆而變 ——
   D 型之驗證步驟現寫 `PENDING: DR-15`（80 包 §1），
   若 DR-15′ 覆得逐狀態之送出值，該 58 條須改寫為實值斷言。
2. **`duplicate_of` 8 對／11 條** —— 其內容逐字相同而各自追溯不同 leaf。
   依 §8.2.2 **不合併**（兩個 requirement leaf 各須有其 TC），
   覆核者見重複屬正常，其 `duplicate_of` 欄記其對象。
3. **x14 下拉現為 `0`** —— 於 48 輪之 openpyxl 存檔時失去（A-VS153），
   **其修復範圍為 `R10:R252`**，屬 Pei，於 Excel 內對 R 欄重設。
   **本 repo 之外科式 emit 不會再失之**（R-VS70），惟亦不會將其復原。
4. **156 條未經人工覆核**（225 − 69）。55 輪之 42 處 defect 修正與本輪之
   17 條 R-VS6 回復，**亦無一經人工覆核** —— 其驗證為機械檢查
   （§9 十七項 ＋ 固定錨點 20 ＋ 五項 defect 掃描 ＋ R-VS77 回掃）。
5. **R-VS6 之檢查層級為 L1**（R-VS79）—— 產物與**解析結果**比對，
   兩側皆經 `inscope_w39.blocks_with_sec()`。**其結論應讀為「與解析結果一致」，
   非「與來源逐字相符」。** 58 輪 W-167 已以 **L3** 抽驗該解析層：
   自 225 條依 `leaf_id` 排序等距取 10，以 `zipfile` 直讀 `word/document.xml`
   逐字比對，**10/10 命中、不符 0** —— L1 之前提於該抽樣中成立。
   **惟抽樣為 10/225（4.4%）**，其餘未驗。
6. **R-VS6 之 27 條已收為 PASS**（86 包 §1）—— 其上半段與條文僅差 XML 實體解碼
   （`&lt;&gt;` → `<>`）與不斷行空白。**逐字之比較基準為來源文件所顯示之字元**，
   實體只是其編碼，故屬解碼而非改寫。彎引號 → 直引號**不在此列**（屬改寫，已修）。
7. **11 批（約 80 條）之首版生成過程不可重放** —— 見 §7 之 `REGEN_ORDER.md`：
   `batch01`–`batch12` 之原生成器未入庫（另 `W-149` 之修正層腳本亦不存在）。
   依 86 包 §3，其 **R-VS53 以產物之 sha256 凍結滿足**：
   凍結點為各批現存最早版之 JSON（合計 **78** 條，其現行為 **80** 條），
   其後之每一層修正皆有腳本且順序已記。

   > **可稽核之範圍為「自凍結點起之變更」，非「自需求起之產出」。**
   > 該範圍須如此表述，**不得以「可重製」一語涵蓋**。

## 6. 母本之 sha256 沿革

| 時點 | sha256 | 列數 |
|---|---|---:|
| 寫回前（原始） | `ebe5a65f30a0d4bcf9e46b51a43145ce222027ac49ad523fe5c2d2b6566a5089` | 237 |
| 48 輪寫回後 | `6d3797d4…` | 143 |
| 50 輪全量後 | `a96e0318d0ecba30355da152d3cdb7f60256bdaec59d9aa270874b49fca7f647` | 241 |
| 54 輪全量後 | `d5d2d3af70f43a7e34d339520481d6e05281198edcfa8a4012d17b6f77625dfd` | 243 |
| 55 輪 defect 修正後 | `83dbef7abce41090c3aac88eb7f619358e97e63e70bd02872d5502778e5c4430` | 243 |
| **57 輪 R-VS6 回復後（交付版）** | **`c72b95560cefb06e94380e795ac6e1eff0c23546f817509060c116314be1221c`** | **243** |

備份五份（`REF/`）：`036_pre_writeback_20260823`／`036_pre_fullwrite_20260823`／
`036_pre_fullwrite2_20260823`／`036_pre_fullwrite3_20260823`／
**`036_pre_final_20260824`**。**各版保留不刪。**

**母本與 `generated/` 無差** —— 57 輪之重讀逐列比對十六欄，243 列 × 16 欄，
**0 項不符**；結構七項全數持平。

## 7. 附件

| 檔 | 內容 |
|---|---|
| `docs/reports/writability.tsv` | 237 leaf 之分級與其理由 |
| `docs/reports/REGEN_ORDER.md` | 各批之重製順序（原生成器 → 修正層 → 現行版本 → sha256） |
| `DATA_REQUESTS.md` | 未結 DR 之全文與狀態 |
| `docs/reports/delivery_disclosure.md` | §A／§B-1／§B-2 之逐條清單 |
| `docs/reports/x14_fix_prep.md` | A-VS153 之修復步驟與三項驗收判準 |
| `ANOMALIES.md` | 159 項 anomaly 之登記（主線，最大 `A-VS161`）＋ 13 項（VF230 線） |
| `RULINGS.md` | R-VS1–**R-VS79** 之逐字條文 |

## 8. 「檢查全數通過」之二分（87 包 §4）

**「不符數 0」有兩種來源，其意義不同，不得以一語涵蓋。**

| 類 | 輪 | 條 | 產物是否改動 |
|---|---|---:|---|
| **經修正而通過** | 55 輪 pilot 五項 defect | **42 處** | ✅ 改動 |
| **經修正而通過** | 56 輪 R-VS6 之實質改寫 | **17 條** | ✅ 改動 |
| **經判準定案而通過** | 57 輪 R-VS6 之實體解碼類 | **27 條** | ❌ **一個位元組未動** |

第三列之通過，係 86 包 §1 裁定「逐字之比較基準為所顯示之字元」——
**變的是判準之解釋，不是產物。**

## 9. 重製鏈之實跑驗證（58 輪 W-166）

取鏈長最長之 `batch17`（6 層）自凍結點按 `REGEN_ORDER.md` 之順序重跑：

| 項 | 結果 |
|---|---|
| 重跑 | **成功**，五層全數執行、產出 `batch17_v6.json` |
| sha256 | **不等** —— 重跑 `48233e9c…` vs 現行 `e4f094ec…` |
| 差異 | **單一欄位 `screen_pending`，6 條**（重跑 `no`／現行 `yes`） |
| 成因 | **48 輪之 R-VS69 重列係就地改動已定版之 JSON**，未產新版、未更新 `revision`，**故該層不在鏈上** |

**其涉 5 檔**：`batch01_v6`／`batch02_v4`／`batch16_v4`／`batch17_v5`／`batch18_v4`。

> **鏈之可稽核性因此有一個缺口**：`REGEN_ORDER.md` 所載之順序
> **不足以重製現行產物**，其缺一層 48 輪之 R-VS69 就地改動。
> **產物本身正確**（R-VS69 之判準以現行產物驗之為 0），
> **不可重製者為其產生路徑。**

## 10. 驗證之重跑

```
cd features/vehicle_setting
python3 scripts/selfcheck_w53.py generated/<批>.json   # canon §9 十七項
python3 scripts/selfcheck_anchored.py                  # 固定錨點 20 項（須全數必命中）
python3 scripts/defect_scan_w157.py                    # pilot 五項 defect（須 0）
python3 scripts/backscan_w160.py                       # R-VS77 全母體回掃（須 0）
python3 scripts/completeness_w154.py                   # R-VS76 完整性（須 237）
python3 scripts/rvs6_verdict_w163.py                   # R-VS6 三分（兩側錨點須皆有標的）
```
