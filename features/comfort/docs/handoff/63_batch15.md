# 63 — Comfort HMI / 批次 15：`Heated Vented Seats`

- 產出層：分析層｜2026-08-16｜對象：執行層
- 承接：下放包 62（同一次覆核，拆檔）

---

## 1. 批次 15 —— `Heated Vented Seats`（最後一組）

| 項 | 值 |
|---|---|
| Test Set | `Heated Vented Seats` |
| 節次與各節 leaf 數 | **自 `framework.md` 導出，逐節列出**（48 §2）|
| leaf | 22 節／59 leaf；**DR #11 影響 11 leaf 之產出形態，其餘 48 不受影響** |
| tc_id | 續編 |

**採納執行層之建議：先產不含 popup 片語之 leaf。**

依 61 §1 之訂正，「該組有阻塞」不等於「該組不能開工」；
本組為十五組之最後一組，其完成使 Part N 之切分全數落地。

### 1.1 DR #11 之 11 leaf —— 形態受影響，非不可產出

執行層 §4 之 leaf 級實測：11 個 ch11 leaf 直接受 DR #11 影響，
**其中三對之差異恰為 popup 片語本身**。

指示：

- **48 leaf 先產**，不受 DR #11 影響
- **11 leaf 逐一套 R-C28 三問**（同 62 §4 之 ch9 作法）：
  其可觀察量若不依賴該片語之差異，該 leaf 可生成，
  其差異於 `reasoning` 具名並指向 DR #11；否則停下
- **不整批停** —— 「形態受影響」須逐 leaf 化為「可生成／不可生成」

### 1.2 ch11 ↔ ch12 之比對須以母節為準

執行層 §4.4 指出 037 之 leaf 描述**省略主詞**，故 leaf 對 leaf 之比對
會失真。比對一律回**母節之 full_text**（`section_fulltext.tsv`），
不以 leaf 描述代之。

**此即 R-C18 之同型**（截斷／省略之欄位不得用於判讀），
於本組為首次以「省略主詞」形式出現。

### 1.3 其他已知交會點

- **ch11／ch12 之合併為 Part N 之裁定**（12 §1）：兩章同屬本組，
  其 sibling 判定於組內進行；`HVS1`／`HVS2`／`HVS4`／`HVS5`／`HVS6`
  五個條款標籤跨兩章重複（A-CF13 第三項），**引用一律以 outline 節次為鍵**
- **`12.3`（Active/Inactive button 顏色）為 `122-02` 條件三之第二個候選節**
  （47 §1 之預裁）。該節生成後，須就 `122-02` 作 R-C39 條件三之逐句比對；
  **兩個候選皆不成立時，預裁啟動**（不產列、不入分母、DR #32 升 High）
- **`11.11`（`configured with hard buttons for comfort controls`）** ——
  33 §4.1 曾以之為「實體鍵之有無為配置項」之證據；本組生成時須判其是否
  須登記為軸（三條件）

### 1.4 照舊

R-C34 生成時義務（目的版反面檢驗）、R-C36-1、R-C40、R-C28 三問、
**R-C41**（比對之陳述須註明方法）、兩份檢查清單、`no-tcid-in-prose`、
`prose-reqid-exists`、stop-and-report 條件同前。

寫回依 46 §3：照常執行、標「範本容量待擴充」、**不送 Excel 確認**。

---

## 2. 現況

| | 數 |
|---|---|
| 驗證單位 | 403 leaf |
| 已生成 | **298 leaf / 303 TC（73.9%）** |
| Test Set 完成 | 9 / 15 |
| lint gate | 44 道（＋本輪二道） |
| 條文 | R-C1 ～ R-C41 |
| DR | #42 |

**本組完成後**：十五組全數開過，剩餘者為各組內因 DR 而停下之 leaf，
以及 `Front Climate Anatomy`／`Airflow and Defrost`／`Home Screen Widget`／
`Climate Popups`／`Climate Modes` 五組之未竟部分。

**屆時 pipeline 之進度將完全繫於 DR 之回覆** —— 這是本 feature 首次
所有可自行推進之工作皆已完成。該狀態一旦到達，須明白告知 Pei，
不以「還有零星可做」掩蓋。

---

## 3. 執行層作業指示

1. 執行批次 15（§1），節次與 leaf 數自 `framework.md` 導出。
2. 依 §1.1 先產 48 leaf；11 leaf 逐一套三問並回報。
3. 依 §1.2 比對一律回母節 full_text。
4. 依 §1.3 於 `12.3` 生成後就 `122-02` 作條件三比對並回報；
   `11.11` 之軸依三條件判。
5. 全批重跑 lint 與 §9 自評；寫回依 46 §3。
6. 上繳 `docs/upstream/41_batch15.md`。git 不執行。

---

## 4. 本包產生之新條文清單（自檢）

無新條文。
