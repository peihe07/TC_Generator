# 59 — Comfort HMI / 批次 13：`ICS Airflow and Defrost`

- 產出層：分析層｜2026-08-15｜對象：執行層
- 承接：下放包 58（同一次覆核，拆檔）

---

## 1. 批次 13 —— `ICS Airflow and Defrost`

| 項 | 值 |
|---|---|
| Test Set | `ICS Airflow and Defrost` |
| 節次與各節 leaf 數 | **自 `framework.md` 導出，逐節列出**（48 §2）|
| tc_id | 續編 |

**選此批之理由**：

- **無已知阻塞**（執行層 §9.6 實測）
- 完成後 **ch16 四組全數到位**（102 leaf 之族），
  `ch16_mirror_map` 之兩側皆有 TC，其 `no-counterpart` 與 `partial`
  之判定即可全面以 TC 對 TC 覆核
- 其餘兩大組皆待外部：`Heated Vented Seats`（59，DR #11）、
  `Rear Climate`（46，DR #31）

### 1.1 本批之已知交會點

- **其對造 `Airflow and Defrost`（ch2）僅 14/23** —— DR #31 使
  `2.12`／`2.12.2` 共 9 leaf 停下。故本批之 R-C36-1 比對中，
  **部分對造無 TC**，須落回對條文比對，並於 `provisional` 標 true
- **`16.12`／`16.12.1` 與 `2.12`／`2.12.2` 為鏡射對** ——
  若 ch2 側停下之依據為條文性質，依 **R-C40** 本批對應之 leaf 亦須停下。
  **生成前先套 R-C40 之一問**，不得因「ch16 側條文完整」逕行生成
- **`16.8`（MAX DEF，12 leaf）為本組最大節** —— 其對造 `2.8` 已生成，
  可 TC 對 TC 比對
- **`16.15`（mirror defrost）與 DR #40** ——
  該 DR 問的是 ch2 側 `2.15`／`2.9` 之不對稱；ch16 側之對應須獨立判定，
  不以 ch2 側之未決為由停下，亦不以其為由生成

### 1.2 照舊

R-C34 生成時義務（目的版反面檢驗）、R-C36-1、**R-C40**、R-C28 三問、
「入口或操作方式未定義」與「對照關係未定義」兩份清單、
stop-and-report 條件同前。

寫回依 46 §3：照常執行、標「範本容量待擴充」、**不送 Excel 確認**。

---

## 2. 現況

| | 數 |
|---|---|
| 驗證單位 | 403 leaf |
| 已生成 | **233 leaf / 238 TC**（57.8%）|
| Test Set 完成 | 7 / 15 |
| lint gate | 43 道 |
| 條文 | R-C1 ～ R-C40 |
| DR | #40 |

**本批完成後之預期**：ch16 四組全數到位，已生成 leaf 約 262 / 403（65%）。

**其後之排程繫於兩個外部待答**：DR #11（59 leaf）與 DR #31（9 leaf ＋
`Rear Climate` 之鏡射效用）。**兩者皆不在本 pipeline 可解之範圍。**

---

## 3. 執行層作業指示

1. 執行批次 13（§1），節次與 leaf 數自 `framework.md` 導出。
2. 依 §1.1 於 `16.12`／`16.12.1` 生成前先套 R-C40 之一問，回報其結論。
3. 全批重跑 lint 與 §9 自評；寫回依 46 §3。
4. 上繳 `docs/upstream/39_batch13.md`。git 不執行。

---

## 4. 本包產生之新條文清單（自檢）

無新條文。
