# 51 — Comfort HMI / DR #6 之成本更新、批次 9

- 產出層：分析層｜2026-08-15｜對象：執行層／Pei
- 承接：下放包 50（同一次覆核，拆檔）

---

## 1. DR #6 之影響範圍更新 —— 由 3 節擴為 3 節 ＋ 7 leaf

DR #6（R1LR ATL-H 之螢幕配置，7" 是否在交付範圍內）自下放包 09 開至今，
其原記影響為 **`19.1`～`19.3` 三節**。

批次 8 實測後應更新為：

| 阻塞 | 內容 |
|---|---|
| `19.1`～`19.3` | 3 節（7" Home screen Comfort Widget，037 未分析） |
| **批次 8 之 7 leaf** | **`Home Screen Widget` 組內，卡於螢幕／widget 尺寸軸** |

`Home Screen Widget` 之 coverage 因此為 **10/21（48%）**，
為十五組中**首次過半停下**者。

**此為狀況變更之如實回報，非重提問題**：DR #6 開列時之成本為 3 節，
現為 3 節 ＋ 7 leaf，且該組已因之停在半數。分析層前次已言明
「不再猜候選檔案」，故本包**不提出新候選**，僅更新其成本。

處置由 Pei 決定：解答、指認來源、或明示接受該組停在 10/21 而續行其他組。
**現行作法（續行其他組）不需任何動作即維持。**

---

## 2. 批次 9 —— `Climate Modes`

| 項 | 值 |
|---|---|
| Test Set | `Climate Modes` |
| 節次與各節 leaf 數 | **自 `framework.md` 導出，逐節列出**（48 §2）|
| tc_id | 續編 |

**選此批之理由**：

- **避開 DR #37**（`Climate Popups`，42 leaf）—— 該組於 §2 之實測有結論前
  不開工
- **避開 DR #31**（`Airflow and Defrost` 之 `2.12`／`2.12.2`）
- 其節次於 `ch16_mirror_map` 多為 `partial`，**R-C36 之逐條判定於本批
  將密集適用** —— 而該表之 `partial` 分界欄已於下放包 37 §2 要求備妥

### 2.1 本批之已知交會點

- **`2.11`（SYNC）與已生成之 `2.6.1` 重疊**（14 §2）：兩側屆時皆有 TC，
  `pending-sibling` 之 `provisional` 列將到期，**R-C36-1 之 TC 對 TC 比對
  於此對可全面適用**
- **`2.3`／`2.12` 與 ch7 之對造**已由 `ch2_ch7_mirror_map` 記為逐字重述
  （37 §3 之抽樣所得）—— 其 sibling 判定應引該表，不重推
- **`2.13`（MAX A/C）**：`-021`～`-023` 已測其破壞 MAX DEF 之行為（3.2 側），
  本批測其自身行為，§8.2.1 之界線須具名

### 2.2 照舊

R-C34 生成時義務（含第十六軸若判為介面型）、R-C36-1、R-C28 三問、
「入口未定義」檢查清單、stop-and-report 條件同前。

寫回依下放包 46 §3：照常執行、標「範本容量待擴充」、**不送 Excel 確認**。

---

## 3. 執行層作業指示

1. 更新 DR #6 之影響範圍（§1），不新增候選檔案。
2. 執行批次 9（§2），節次與 leaf 數自 `framework.md` 導出。
3. 全批重跑 lint 與 §9 自評；寫回依 46 §3。
4. 上繳 `docs/upstream/35_batch9.md`。git 不執行。

---

## 4. 本包產生之新條文清單（自檢）

無新條文。
