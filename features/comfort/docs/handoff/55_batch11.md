# 55 — Comfort HMI / 批次 11：`Climate Popups`

- 產出層：分析層｜2026-08-15｜對象：執行層
- 承接：下放包 54（同一次覆核，拆檔）

---

## 1. 批次 11 —— `Climate Popups`

| 項 | 值 |
|---|---|
| Test Set | `Climate Popups` |
| 節次與各節 leaf 數 | **自 `framework.md` 導出，逐節列出**（48 §2）|
| tc_id | 續編 |

**選此批而非 `Rear Climate` 之理由**：

- `Rear Climate`（46 leaf）之價值在於 `ch2_ch7_mirror_map` 之對造已有 TC，
  **而 DR #31 使 `2.12`／`2.12.2` 共 9 leaf 停下** —— ch2 家族尚未到位，
  其鏡射效用未達最大。等 DR #31 有解再排較划算
- `Climate Popups` 現已解封至 **41/42**（僅 `14.12` 停，53 §1），
  為現時可動之最大一組
- **本批可望解一個既有待決項**：`14.16.1` 為 DR #32 之
  `122-02` 條件三之兩個候選節之一（47 §1 之預裁對象）。
  該節生成後，`122-02` 之預裁即有實測依據

### 1.1 本批之已知交會點

- **`14.16.1` 與 `122-02`**：生成後須依 R-C39 條件三**逐句比對**其 ER
  是否涵蓋 `122-02` 之內容（configuration → icon 之對照）。
  結論若為否，依 47 §1 之預裁處置（不產列、不入分母、DR #32 升 High），
  **不創造第五類 marker**
- **`14.12` 停下**，理由記 DR #37 之三問（35 §2.1）
- **`15.1`（HVAC Pop-ups Chart）** 與 ch14 之關係須先判：
  該節若為對照表而非行為條文，其 leaf 之處置比照「條文要求一組對照關係」
  類（DR #32）之判準，**不得將表格內容當作行為驗證**
- **popup 之樣式**（radial／vertical）依 35 §2.1 為 `14.12` 之範圍，
  **其餘節之 TC 不得引用該樣式**（§8.2.1）

### 1.2 照舊

R-C34 生成時義務（含 §1 之目的版反面檢驗）、R-C36-1、R-C28 三問、
「入口或操作方式未定義」與「對照關係未定義」兩份檢查清單、
stop-and-report 條件同前。

寫回依 46 §3：照常執行、標「範本容量待擴充」、**不送 Excel 確認**。

---

## 2. 現況（供上繳包對照）

| | 數 |
|---|---|
| 驗證單位 | 403 leaf |
| 已生成 | 依上繳 36 為 **153 leaf / 166 TC** |
| Test Set 完成 | 6 / 15 |
| lint gate | 43 道 |
| 條文 | R-C1 ～ R-C39 |
| DR | #39 |

**交付端**：DR #35（範本容量）仍為硬阻塞，現 166 列而 116 列無下拉。
該項待 Pei 於 Excel 擴充，與生成無關。

---

## 3. 執行層作業指示

1. 執行批次 11（§1），節次與 leaf 數自 `framework.md` 導出。
2. 依 §1.1 於 `14.16.1` 生成後，就 `122-02` 之 R-C39 條件三作逐句比對並回報。
3. 依 §1.1 先判 `15.1` 之性質（對照表抑或行為條文），再決定其 leaf 之處置。
4. 全批重跑 lint 與 §9 自評；寫回依 46 §3。
5. 上繳 `docs/upstream/37_batch11.md`。git 不執行。

---

## 4. 本包產生之新條文清單（自檢）

無新條文。
