# 39 — Comfort HMI / R-C40 之首次適用、DR #11 之範圍實測、批次 13

- 產出層：執行層｜2026-08-16｜對象：分析層
- 承接：下放包 58／59
- 結果：六項全數落實。**R-C40 首次適用即撤下已生成之兩條**（`16.11` 之
  `-224`／`-225`），並使 `16.13` 以後之 tc_id **首次位移**。批次 13 產
  **29 條**（`-237`…`-265`），**無停下項**；`16.12`／`16.12.1` 於生成前
  先答 R-C40 之一問，結論為**前件不成立**（非逐字相同），兩節照生成。
  本輪另實測出**四對逐字等價之 TC**（`16.9`↔`2.9`、`16.15`↔`2.15`），
  兩對 sibling 由 `deferred` 改判為 **`sibling`**。
  lint **43/43 PASS，265 條**。ENTRY 010 已產出，3 項 FAIL 同源，不可交付。

---

## 0. 下放包六項之對照

| # | 指示 | 狀態 |
|---|---|---|
| 1 | R-C40 貼入 `RULINGS.md` | ✅ §1 |
| 2 | 回報 `015-04`／`015-05` 之停下理由，套 R-C40 之一問並處置；撤下之一側登記依據 | ✅ §2 —— **撤下 ch16 側** |
| 3 | DR #32 之 recirc 成員加交叉註記；`RUNBOOK` 記「依成因歸類，依後果註記」 | ✅ §3 |
| 4 | `16.13` 六條補 `distinguishing_axis` 與 `duplicate_of` 判定；`RUNBOOK` 記 §4.5／§4.6 分界 | ✅ §4 |
| 5 | 回報 DR #11 之內容與阻塞範圍 | ✅ §5 —— **單位錯誤一處** |
| 6 | 執行批次 13；`16.12`／`16.12.1` 生成前先套 R-C40 之一問 | ✅ §6 |
| — | 上繳 39 | 本件 |

---

## 1. R-C40 已貼入

`RULINGS.md` 第 903 行起，逐字照錄，未加註、未改寫。其一問：

> **這個停下，是基於「條文本身之性質」，還是基於「該章之脈絡」？**

前者則兩處同停或同生成；後者則不一致為實然，**惟兩側各須具名其理由**。

---

## 2. `015-04`／`015-05` —— 停下理由、一問之答、處置

### 2.1 原停下理由（批次 9，`gen_batch9.py` 之 `WITHHELD`）

> 「Adjusting Fan speed and Mode will alter the **Front and Rear** passengers」
> —— 可觀察量在後排，而「車輛是否配備後排氣候」**不在十六軸內**；
> 其來源即 `2.1` 之 tab 集合問題（`DATA_REQUESTS` #17，未解）。

`015-05` 同理（後排調整中斷 SYNC）。

### 2.2 套 R-C40 之一問

**實測**：ICE10（`16.11`）與 C12（`2.11`）之該二句以 `in` 比對**逐字命中**，
前件成立。

該停下之依據是「**該軸在既有十六軸內未登記**」——
**軸之未登記是條文本身之性質**（兩處條文同樣指涉後排，該軸在兩處同樣未登記），
**不是 ch2 所特有之脈絡**。故**兩側應同停**。

### 2.3 處置：撤下已生成之 ch16 側

`SWE1-HVAC-116-03`／`-04`（原 `-224`／`-225`）**撤下**，依據登記於
`scripts/gen_batch12.py` 之 `WITHHELD`（逐字保留，含反面考量）：

> **反面之考量已檢查**：ch16 十八節無後排專章（ch7 不屬 ICS 側），
> 若論脈絡，ch16 側之後排介面比 ch2 側**更**未定義 ——
> 該考量只會加強撤下而非反對之。

### 2.4 其後果：tc_id 首次位移

`tc-id-sequence` 要求 `1..N` 無缺號，故撤下二條使 **`16.13` 以後之 tc_id 全數前移二號**
（`16.13` 由 `-227..-238` 變為 `-225..-236`）。**這是本專案首次 tc_id 位移。**

**其連帶損害於本輪被找到並修正**：`16.13` 之 `reasoning` 與 `distinguishing_axis`
內以 tc_id 具名之處（`-227`～`-232`、`-233`～`-238`、`-238`、`-232`）
**在位移後全部指錯條**。lint 之任何 gate 都不檢查散文內之 tc_id 引用，
**這正是 profile §5.5「推導欄重算風險」所預告者，本輪首次實現**。
已逐處訂正，並於 `gen_batch12.py` 之註解標明「本串號為撤下後之號」。

> **給分析層**：以 tc_id 具名之散文引用是**推導值**，其正確性繫於編號不變。
> 撤下任何一條，都須連帶複查全 corpus 之散文引用 —— 目前**無 gate 覆蓋此事**。

---

## 3. DR #32 之 recirc 成員 —— 交叉註記已加

成員 **(b)**（`2.5`／`16.5` 之 recirc icon）條目內加註：本成員亦符合 **#34(h)**
之形態（可執行性依賴測試員已知答案，`NR1L-ComfortHMI-213` 之 ER 只寫
「該車型之圖示」），**惟其成因為對照未定義** —— 測試員之所以須自行知道，
正是因為對照不存在。故籍屬 #32，**#34 不重複列之**。

`RUNBOOK.md` 已記「一項落在兩類之交會時，依成因歸類，依後果註記」，
並與同源二則（**處置相同者不分類**、**標題與清單須隨成員更新**）合為一組：
三者都在防同一個失誤 —— **讓「這一類有多大」這個數字說謊**。

---

## 4. `16.13` 六條退出途徑 —— 逐對判定

`distinguishing_axis.axis` = 「MAX A/C 之退出途徑（trigger）與其落點（outcome）」。
六條之 §10.6 四項逐項比對：

| TC | trigger | outcome |
|---|---|---|
| `-231` | 改風速 | MAX A/C 解除，**其餘狀態維持** |
| `-232` | 改溫度／recirc／模式 | 回前一手動模式，**除該被改之元素外** |
| `-233` | 按 MAX A/C | 回前一手動模式 |
| `-234` | 按 A/C | 回前一手動模式且 **A/C 關閉** |
| `-235` | 按 AUTO | 系統**進入 AUTO** |
| `-236` | 按 MAX DEF | 系統**進入 MAX DEF** |

**trigger 於六條全異，outcome 於其中四條亦異，故無一對構成 `duplicate_of`。**
`input` 六條皆 NA、`verification target` 各為該退出途徑本身。

`RUNBOOK.md` 已記 **§4.5 是欄位歸屬（一條之內），§4.6 是 sibling（跨條）**，
並標明這是**第二次**把跨條之相似誤記為 §4.5（前次 `17.1` 三條，50 §4）。
其要害不是分類錯，是**處置錯**：§4.5 之處置是搬欄位，而搬欄位解決不了
「兩條 TC 是否重複」。

---

## 5. DR #11 —— 內容與阻塞範圍（實測）

### 5.1 內容

| 項 | 值 |
|---|---|
| 名稱 | **HMI Pop Up List** —— 定義 Comfort 各 popup 之內容與行為 |
| 狀態 | ❌ 未入 `inputs/`，`paths.popup_list` 為 `null` |
| 問句 | `ch11.1`／`11.2` 之 `opens popup and` 是 ch11 與 ch12 之唯一實質差異；該 popup 究竟是**進入路徑**（中介畫面）或**回饋**（狀態提示），決定 ch11／ch12 應**合併**或**拆回兩組** |
| 等級 | **High** |
| 關聯 | A-CF13 |

### 5.2 阻塞範圍 —— **表內「59 節」是單位錯誤**

實測（`framework.md`）：

| 章 | 節數 | leaf 數 |
|---|---|---|
| ch11 | 13 | 37 |
| ch12 | 9 | 22 |
| **合計** | **22 節** | **59 leaf** |

**59 是 leaf 數，不是節數。** 表格「影響範圍」欄應為「**22 節／59 leaf**」。

### 5.3 兩章之相近程度（實測）

以節全文之 `SequenceMatcher` 比對，五對近重複：

| 對 | 相似度 |
|---|---|
| `11.1` ↔ `12.1` | 0.981 |
| `11.2` ↔ `12.2` | 0.983 |
| **`11.3` ↔ `12.4`** | **1.000（逐字相同）** |
| **`11.4` ↔ `12.5`** | **1.000（逐字相同）** |
| `11.5` ↔ `12.6` | 0.930 |

五對共涵蓋 **59 leaf 中之 24 leaf**。
`opens popup` 出現於 `11.1`／`11.2`／`11.8`／`11.9`（另 `14.16` 亦有）。

**故 DR #11 之答案影響的不只是分組**：若該 popup 是回饋，兩章之差異僅在措辭，
24 leaf 將面臨與 §6.4 同型之「逐字等價 TC」問題；若是進入路徑，
則 ch11 之 TC 須多驗一個中介畫面。**兩種答案產出的 TC 不同，故不能先生成再修。**

---

## 6. 批次 13 —— `ICS Airflow and Defrost`

### 6.1 產 29，**無停下項**

| 節 | 父 | leaf | tc_id |
|---|---|---|---|
| `16.8` | `SWE1-HVAC-113` | 12 | `-237`…`-248` |
| `16.9` | `SWE1-HVAC-114` | 2 | `-249`／`-250` |
| `16.12` | `SWE1-HVAC-117` | 3 | `-251`…`-253` |
| `16.12.1` | `SWE1-HVAC-118` | 10 | `-254`…`-263` |
| `16.15` | `SWE1-HVAC-121` | 2 | `-264`／`-265` |

`framework.md` 宣告 29 leaf，實測 29 emitted ＋ 0 withheld ＝ 29。

### 6.2 R-C40 之一問，**於生成前作答**（59 §6 之指示）

| 對 | 前件（逐字相同？） | 依據 |
|---|---|---|
| `16.12` ↔ `2.12.1` | **否** | C13.0 有「**In some non-tri mode equipment types,** airflow modes has 5 states」之配置限定語，ICE11 **無** |
| `16.12.1` ↔ `2.12.2` | **否** | C13.1 之循環為 `Face > Face/Feet > Feet > Feet plus Windshield`＋「**Defrost will not be included**」（**四模式**）；ICE11.1 為 `Face > Face/Feet > Feet > Feet/Windshield > **Windshield**`（**五模式**） |

**前件不成立 → R-C40 不適用。** ch2 側之停下（DR #31）源於
**ch2 陳述三個氣流模式集合而其一（4 模式）無正面適用條件**，
**ch16 只陳述一組**，該問題於此不存在 —— 依 R-C40 之語彙，這是
**章節脈絡**而非條文性質，故不一致為實然。**兩節照生成，各具名其理由。**

> 注意此處與 §2 之對比：同一個問題，`16.11` 之答案是「條文性質」故撤下，
> `16.12` 之答案是「章節脈絡」故生成。**R-C40 不是一個一律同停的規則。**

### 6.3 本輪最重要之實測：**四對逐字等價之 TC**

兩側 TC 落地後逐欄比對，發現：

| ch16 | ch2 | `test_item` | `test_procedure` | `expected_result` | `pre_conditions` |
|---|---|---|---|---|---|
| `-249` | `-159` | 同 | 同 | 同 | **異（第九軸值）** |
| `-250` | `-160` | 同 | 同 | 同 | **異** |
| `-264` | `-165` | 同 | 同 | 同 | **異** |
| `-265` | `-166` | 同 | 同 | 同 | **異** |

**處置**：`2.9`↔`16.9` 與 `2.15`↔`16.15` 之 verdict 由 `deferred` 改判為
**`sibling`**，`equivalent_tc_pairs` 記其對，兩側 doc 之 `distinguishing_axis`
回填為「第九軸（EMEA ICS 車型）之值」。**依 §10.6，軸值即可區辨，故
`duplicate_of` 不填**（且該欄為節級並帶工具側注入之列號，跨節等價不在其射程）。

**`2.15`↔`16.15` 是全 corpus 唯一兩側 leaf 數相等且逐條等價者**，
故「兩份 TC 是否應合併為一份並以軸值參數化」在此最赤裸 —— 已隨 037 分解案登 **DR #38**。

**另記不等勢**：`2.9` 尚有 `-161`（獨立性）與 `-162`（鏡面除霜連動）二條為 ICE8 所無 ——
**ICE8 連引用鏡面除霜都沒有**（C8 尚有「if this feature available」一句），
ch16 側對該配置**零陳述**。此為 **DR #40** 之不對稱在 ch16 側之實測，比 ch2 側更明顯。

### 6.4 `16.8` 之鏡射對是 `3.2` 而非 `2.8`

鏡射表記 `16.8 ↔ 3.2`（七項連動逐字相同）。實測兩側 TC 之
`SequenceMatcher` 比值：`16.8`↔`3.2` = 0.07、`16.8`↔`2.8` = 0.097 ——
**兩者皆低**，因 ch16 側之 ER 皆以 ICS 控件為錨。
**不因其為鏡射而互相移植**（§8.2.1）。

### 6.5 `16.15` 與 DR #40 之獨立判定

`16.15` 之 `-264`／`-265` **不補配置式 PC**，依據為 **ICE14 自身無任何配置限定語** ——
與 `2.15` 之 `-165`／`-166` 結論相同，**而其依據為本節自身之條文，非承襲 ch2 側之處置**。
（59 §6 之要求：不以 ch2 側之未決為由停下，亦不以其為由生成。）

### 6.6 135 列 provisional 之重新確認 —— **其中 100 列原為不得終判之 verdict**

批次 13 使五節落地，`provisional-sibling` gate 報 **135 列**兩側皆已生成。

**第一次處置錯了，且是機器把它抓回來的**：我把 135 列一律標 `provisional=false`
並保留原 verdict。重跑 `sibling_candidates.py --rebuild` 後，其中 **100 列自動翻回
`true`** —— 因 `NEVER_FINAL` 規定 `deferred` 與 `not-broken-by-3-samples (class)`
**永不得為非 provisional**（41 §4／42 §1）。

**正確處置**：那 100 列（93 `deferred` ＋ 7 類級）之兩側 TC 皆已在，
**必須逐對判定**而非僅解旗 —— 逐對比對其 `expected_result` 之可觀察量後，
100 列全數改判為 **`not-sibling`**，另 2 對改判 `sibling`（§6.3）、
33 列 `not-sibling` 維持。重跑後 gate 靜默、rebuild 冪等。

> **這一則值得記**：`deferred` 解旗**不是**「確認完畢」，
> 而是「現在才輪到你判」。前者只需一個動作，後者需要看 TC。
> **機器攔下了我，只因為那條規則早先被寫成了不變式而不是提醒。**

表現況：1668 列；`not-sibling` 988／`deferred` 568／類級 99／`sibling` 13；
`provisional` false 855／true 813。

---

## 7. lint 與 §9 自評

```
43 / 43 gates PASS; 0 finding(s) across 265 TCs
```

反向驗證三支全 PASS（`verify_provisional_gate` 6/6、`verify_axis_gate`、
`verify_axis_type_gate`）。

TC 238 → **265**；已生成 leaf 233 → **260**；已生成節 75 → **80**。
**已生成 leaf 260 / 403 ＝ 64.5%**。

**§9 十七項**：新增 29 條（批次 13）。

| # | 項目 | 變動 | 獨立依據 |
|---|---|---|---|
| 3 | Pre-Condition | 變 | EMEA **正向**值（16.2）＋第五軸（MAX DEF，`16.8` 十二條）＋第十軸（REAR DEFROST，`16.9` 二條與 `-243`）＋第二軸（`-242`）＋第一軸 ATC 值（`-247`）＋第四軸（`-248`）＋軸 13（2.14）＋軸 9（6.3，全數） |
| 5–8 | 步驟 | 變 | 每條 2–3 步，末步持驗證 |
| 9 | Baseline | 變 | `-244`～`-248`（中斷途徑）需前後對照，首步建立基線 |
| 10 | Procedure ↔ ER 1:1 | 變 | 29 條全數 1:1 |
| 11 | FP／FF | 變 | `-244`（改風速**不**中斷 MAX DEF）為否定式，與 `-245`（其他變更中斷）成正反兩側 |
| 12 | 溯源、§8.2.1 | 變 | 29 leaf 各溯其 037 req_id；`16.8`↔`3.2` 不互相移植（§6.4）；`16.15` 獨立判定（§6.5） |
| 13 | Design Method | 變 | 29 條皆功能測試（`16.12.1` 之循環序另加狀態轉換） |
| 16 | `specification_reference` | 變 | 各條含自身節次＋16.2＋2.14＋6.3；適用者另加 3.2／3.4／16.11／16.3／16.13 |
| 17 | §8.6／§8.7 | 變 | `-251` **不驗按鈕數** —— ICE11 自稱 5 states 而其呈現句寫 `for the four airflow modes`（A-CF13 同型），故 ER 只驗「所選之鍵高亮且變大」 |
| 其餘 | — | 不變 | |

---

## 8. 「本包是否仍有該驗而未驗者」（R-C30）

1. **批次 13 之 29 條未經 §7 之 FP／FF 人工複核**，只經 lint。
2. **散文內之 tc_id 引用無 gate 覆蓋**（§2.4）——
   本輪已知有位移而逐處手改，**下一次位移時沒有東西會提醒我**。
3. **§6.3 之四對等價只在「兩側皆已生成」時才驗得到** ——
   尚未生成之節與已生成節之間若有同型等價，**現在看不見**，
   而 `sibling_candidates` 只比詞彙不比 TC 全文。
4. **`16.12.1` 之 `-262`（後排畫面下之作用）** 判為觀察**前排**故不涉未登記之後排軸；
   該判斷**依賴條文明寫 `alters the front Mode`**，若條文另有他處述及後排回饋，本條之免除即不成立 —— **未全章掃描**。
5. **`16.8` 十二條之中斷途徑與 `16.13` 六條同型** ——
   §4 之逐對判定只做了 `16.13`，`16.8` 之五條（`-244`～`-248`）
   **本輪一併寫入 `distinguishing_axis`，但未經分析層覆核**。
6. **DR #11 之 §5.3 相似度為全文比對** —— 未逐 leaf 比對，
   故「24 leaf」是節級推得之數，非 leaf 級實測。

---

## 9. 待分析層

1. **§2.4** —— tc_id 位移之連帶複查是否應成為一個 gate（散文引用之 tc_id
   須存在且其所指之條與描述相符）。目前**全靠人記得**。
2. **§6.3 / DR #38** —— 四對逐字等價之 TC：是否維持兩份，
   抑或改為一份以軸值參數化。**現行為兩份**（§8.2.2 禁 TC 作者合併 leaf）。
3. **DR #40** —— 鏡面除霜之不對稱，ch16 側之零陳述使「實然抑或疏漏」更難由本 feature 內部判定。
4. **DR #11** —— 影響範圍欄之單位錯誤請訂正為「22 節／59 leaf」（§5.2）；
   其答案影響 24 leaf 之產出形態（§5.3）。
5. **DR #35 / A-CF26** —— 範本容量：現 265 列，263 列無 P 下拉。
   **補救仍為常數，不因此升級**。
6. **批次 14 之授權**；剩餘者為 `Heated Vented Seats`（59 leaf，待 DR #11）、
   `Rear Climate`（46 leaf，待 DR #31）。
   **兩組皆有已知阻塞，故無「無阻塞之下一批」** —— 這是首次。
