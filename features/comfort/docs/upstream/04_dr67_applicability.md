# 上繳包 04 — R-C12/13/14 落實 ＋ DR #6・#7 判讀

執行層 → 分析層。2026-08-14。回應下放包 `07_upstream03_review.md` §6
與 `08_dr67_material.md` §4。

**結論：五項作業全部完成。現行判讀 4 `in_scope`／13 `undetermined`／
0 `out_of_scope`。** `DECISIONS.md` 未簽署，Phase 3 未開始。

---

## 0. 置頂：兩件分析層需要知道的事

| # | 事項 |
|---|---|
| **甲** | **DR #7 已解、DR #6 限縮至 7"，但都不是靠 08 供入的素材解的。** Market Configuration Table 對 `R1L-R` 與螢幕尺寸皆 0 命中；解答來自 **037 自身的引用結構**（ch16 引 18/19 得 99 leaves、ch18 引 1/4 得 3 leaves、ch19 引 0/3）。這正是 R-C13「零命中應觸發換路徑」的直接應用 |
| **乙** | **判讀結果之淨變動為「±」而非單向保守**：20.x 十節依 R-C12 降級，同時 16.1 與 18.2–18.4 四節依結構證據升為 `in_scope`。淨值 17 節中 4 節有定論、13 節待料 |

---

## 1. §6.1 條文貼入

`RULINGS.md` 現載 **R-C1 ~ R-C14 + R-C4-1 + R-C5-1**，共 16 條，全部原文照錄。
R-C12/13/14 另立「下放包 07 §1」小節，並標注 R-C13／R-C14 適用全 feature、
R-C13 與 Privacy R22-2 同構待合併處置。

## 2. §6.2 R-C12 降級 —— 依據一字未刪

`data/sr24_substantive_applicability.tsv` 加第五欄 `pending_on`。

20.1 ~ 20.4.3 十節：`in_scope` → **`undetermined`**，
`pending_on` = `DR #8 — CFTS043 4803259 NOTE vs its own Radio attribute (A-CF12)`。

`basis` 欄原有依據**全部保留**（機械檢查：10 列全部仍含 `Scope=Yes` 字樣、
仍含 items 編號、仍含 `Market=All` 與 EE 說明），僅前綴：

```
[R-C12: downgraded from in_scope 2026-08-14; evidence below retained in full,
 not retracted]
```

並依 07 §3 追加層級訂正段（見下）。

### A-CF12 已併記 07 §3 之層級訂正

上繳 03 §3.4 曾稱 `Scope` 欄是「本檔對『什麼在 R1L-R 範圍內』最直接的表態」。
**該理由與 §8.6 不合，已訂正。** tree view 是 SYS.1 階段之 traceability
**index export**，非原始 spec 來源；CFTS043 主檔 `.doc` 才是。index 與原始
來源不一致時原始來源勝出，故 `Scope` 欄降為**索引層佐證，不參與選邊**。

排除索引層後，矛盾的正確描述是 **CFTS043 主檔內部之矛盾**：

| 層級 | 來源 | 陳述 |
|---|---|---|
| 原始．散文 | 4803259 NOTE | below 之需求只適用 R1H |
| 原始．屬性 | 同 item `Radio` | `R1L-R, R1L, R1H` |
| ~~索引層~~ | ~~tree view `Scope`~~ | 佐證，不參與選邊 |

**接受 07 §3 之後果判斷**：若今日被迫選邊，canon 重心偏向散文，即偏向
`out_of_scope` —— 與上繳 03 所選方向相反。故 R-C12 之降級不只是形式保守，
而是修正了一個**實質上偏錯方向**的暫定值。這點我在上繳 03 沒看出來：
我把「兩個來源打架」處理成「挑一個並揭露這是選擇」，但沒有先問「這兩個
來源是不是同一層級」。層級判斷應在選邊之前，不是選邊之後的補充。

## 3. §6.3 A-CF11 已註記升格 R-C13

A-CF11 頂端加註升格、與 R22-2 同構、以及**升格後當輪即再次應用**之紀錄。

## 4. 08 §2 —— DR #6／#7 判讀

### 4.1 Gate：先驗 hash 再判讀

| 項 | 值 | 結果 |
|---|---|---|
| bytes | 279,779（與 08 §1 相符） | ✅ |
| SHA256 | `ae4cf0b929b033ac3baabf9d2e6e7497da5539d3f774be30d52d636a67816cfc` | —— |
| 對 25PI3.5 `ae4cf0b9…` | 前綴相符 | **PASS** |

同時對 08 §1 所列另三個 release 之 hash 前綴（`9efae74f…`／`2e66a6d9…`／
`7e865d55…`）逐一排除。Gate 通過方進入判讀。

### 4.2 該素材**沒有**回答本題 —— 這點必須明記

實測 Market Configuration Table 全 8 工作表：

| 檢索 | 命中 |
|---|---|
| `R1L-R` | **0** |
| 任何螢幕尺寸（7"／10.25"／12.3"／inch…） | **0** |
| `widget` | **0** |
| `EMEA` | 158 |
| `LATAM` | 26 |

其 variant 軸是**市場別**（ROW 102／ECE 64／ROW+ 40／US-CAN 7／CHN／JPN／
MEX／KOR 各 1），**非機型別**；地理分組為 EMEA 149／APAC 37／NAFTA 19／
LATAM 19 國。它回答「哪個國家屬哪個市場、拿到什麼語言／單位／tuner 設定」，
**不回答「本次交付涵蓋哪些螢幕與市場」**。

08 §2 已預先警告「不得以『AMFM 用過』即認定其適用於本題」—— 實測證實這個
警告是對的：AMFM 用它解 tuner／語言，那是它的軸；螢幕與交付範圍不是。

### 4.3 依 R-C13 換路徑，第三路才出結果

| 路徑 | 作法 | 結果 |
|---|---|---|
| 一 | 結構化欄位篩選（radio variant／地理分組欄之相異值） | 得知 variant 軸是市場別，不承載 R1L-R |
| 二 | 全 8 表 token 掃描 | 見上表，螢幕尺寸與 R1L-R 皆 0 |
| 三 | **037 自身之引用結構**（不依賴任何字串檢索） | **決定性** |

第三路：

| 章 | 標題 | 037 引用 | leaves |
|---|---|---|---|
| 16 | ICS CLIMATE EMEA – CARRYOVER | **18 / 19** | **99**（全 feature 之 25%） |
| 18 | 10.25" Home screen - Comfort Widget | **1 / 4**（18.1） | **3** |
| 19 | 7" Home screen - Comfort Widget | **0 / 3** | 0 |

### 4.4 判讀結果與其推論方向

| 節 | verdict | 依據 |
|---|---|---|
| **16.1** | **`in_scope`** | 037 不可能在 EMEA 章產出 99 leaves 而該市場不在交付範圍內。16.1 是該章唯一未被引用之子節 → in-scope 章內之**覆蓋缺口**（A-CF08），非範圍問題 |
| **18.2–18.4** | **`in_scope`** | 037 引用 18.1 得 `SWE1-HVAC-129-01/-02/-03`。**18.1 與 19.1 條文文字相同**（皆 `W0.)`），037 分析的是 10.25" 實例 —— 無論該選擇解讀為「10.25" 是交付螢幕」或「作者對重複條款擇一」，**被分析的都是 10.25" 那個**，故判定對兩種解讀皆成立 |
| **19.1–19.3** | `undetermined` | 無來源。**037 未引用 ch19 不作為排除證據** |

**推論方向是單向的，這是本節的核心**：

- **有引用 → 是證據**（不在範圍內就產不出那些 leaves）
- **無引用 → 不是證據**

後者正是 A-CF01／R-C5 的錯誤步驟（「037 沒引用」被當成「SR24 沒有」用），
已由 R-C5-1 訂正。ch19 的沉默在任一方向都不算數，所以它 `undetermined`，
不是 `out_of_scope`。

### 4.5 08 §3 次要候選 —— 先驗後用，驗不過

08 §3 要求「取用前須先驗其確實承載該資訊，不得以『名稱像』即採」。

- **`Vehicle Category HMI Logic and Flow R1 SR24 Post 2A`**：已在
  `spec-index/sources/`。以 `pdftotext` 抽出全文（19,052 bytes）實測 ——
  它只載「**文件**涵蓋哪些機種」（R1 Low: 7"／8.4"／10.1"／10.25"／12.3"），
  與 SR24 §1.1 同型，而 06 §3 已裁此不構成交付範圍證據；且 `R1L-R` 0 命中、
  `Atlantis` 0 命中、無任何配置表。**驗不過，不採用。**
- **`VINtoArchitecture decoding v3.xlsx`**：全 repo 搜尋**不存在**。
  已列 `DATA_REQUESTS.md` #10。

### 4.6 附帶量測：A-CF06 可結案一半

判讀期間發現 `pdftotext` 已安裝，順手實測 SR24 PDF：**具 text layer**，
抽出 62,874 bytes 文字。A-CF06 所記「『PDF 具 text layer』目前不可陳述」
之限制**解除**。

未解者僅工具面：`recon.py` 之 `survey_spec_text_layer()` 只試 `pymupdf`，
故 `RECON.md` 仍印 `unknown (pymupdf not installed)`。該行**不是錯誤陳述**
（pymupdf 確實未裝），但已與可得事實不同步。加 `pdftotext` fallback 是小
改動，**惟不在 07／08 之作業範圍內，本輪未動**，列為建議。

## 5. §6.5 —— 未簽署、Phase 3 未開始

- `DECISIONS.md` **未簽署**。Sign-off 仍為空白範本，recon 每跑仍發 R-C10
  警告，屬正確狀態。05 §4 之兩項調整建議（exemplar 具名 `home`、
  pilot 取第 13 章）仍**未代為寫入**。
- **Phase 3 未開始**：未寫 framework Part N、未寫 profile、未建 Test Set 表。
- 07 §5 之預留要求已記入 `RUNBOOK.md`：章 20（Alternate Rear Blower）與
  home-screen widget 兩塊須留為**可插入之邊界**，不得切成必須重整才容納
  得下的形狀。

---

## 6. 本包是否仍有該驗而未驗者 —— 獨立判斷

### 6.1 已驗

1. Market Configuration Table 之 SHA256 gate（含排除另三個 release）。
2. 該檔全 8 表之 token 掃描與 variant／地理分組欄之相異值。
3. 037 對 ch16／ch18／ch19 之引用結構與 leaf 數。
4. 18.1 與 19.1 條文文字相同（`W0.)`）。
5. `Vehicle Category` PDF 之內容（19,052 bytes 全文），驗證其驗不過。
6. `VINtoArchitecture` 全 repo 不存在。
7. TSV 之 `basis` 保留性（10 列全含 `Scope=Yes`）、`pending_on` 欄存在、
   17 節與 A-CF08 substantive 集合對稱差為空、`out_of_scope` 計數為 0。
8. SR24 PDF text layer（62,874 bytes）。

### 6.2 該驗而未驗

| # | 未驗事項 | 為何 | 風險 |
|---|---|---|---|
| 1 | **DR #8（4803259 NOTE 效力）** | 需上游確認，非本地可驗 | **高** —— 13 節中 10 節待此。07 §3 已指出 canon 重心偏向散文，即偏向 `out_of_scope` |
| 2 | **7" 是否屬交付螢幕配置** | 無來源（DR #6／#10） | 中 —— 3 節待此 |
| 3 | **16.1／18.x 之 `in_scope` 是否應更保守** | 我判定其來源無矛盾，故 R-C12 不觸發 | **中** —— 見下 |
| 4 | `$Indipendent_Rear_Fan$` 實際配置值 | 不在任何已有素材中 | 中 —— 不影響 verdict，影響 TC 之 Pre-Condition 能否寫具體值 |
| 5 | A-CF02 交付夾 | 該樹本 session 不可達，狀態未變 | 低 |

**第 3 項需要說清楚，因為它是本包最可能被推翻的判斷。**

我對 16.1 與 18.2–18.4 記 `in_scope`，依據是「037 引用了同章兄弟節」。
這是**間接證據** —— 037 的引用行為反映了它對範圍的理解，而不是一份直接
聲明「本交付涵蓋 EMEA／10.25"」的文件。R-C12 的觸發條件是「來源存在未解
之內部矛盾」，此處我判定無矛盾（MCT 沉默不等於矛盾），故未降級。

但若分析層認為 **R-C12 的精神應擴及「依據為間接證據」而不只是「來源有
矛盾」**，這四節就該一併記 `undetermined`。這個界線我判不了 —— 條文寫的是
矛盾，不是間接性。我照條文字面執行，並在此標明界線所在，供分析層決定是否
需要一條新條文。

### 6.3 未做、亦未偷做者

- 未簽署 `DECISIONS.md`；未代寫 05 §4 之調整建議。
- Phase 3 未開始。
- 未對任何一節做 TC 處置（不產 TC、不入分母、不列 BLOCKED、不補 RD）。
- 未改 R-C5、R-C5-1 或任何既有條文原文；20.x 之 `basis` 一字未刪。
- 未動 `recon.py`（pdftotext fallback 僅列為建議）。
- 未重跑任何既有 feature 之 recon（R-C8）；對其目錄零寫入。
- 未執行任何 git 操作。

### 6.4 執行層對「本包可否結案」之判斷

**可結案。** 五項作業完成，判讀有依據、缺料有具名、推論方向已標明。

**D-C10 仍不宜裁**，且理由比上繳 03 更強：07 §3 之層級訂正顯示，若今日
選邊，canon 重心偏向 `out_of_scope` —— 也就是說先前那個暫定的 `in_scope`
不只是「未經裁決」，而是**方向可能相反**。DR #8 一個問句可解，現在更該先問。

**Phase 3 可否開始，取決於一件事**：403 leaves 之部分不因 17 節未定而停滯
（07 §5 已言明），但 Part N 若要切，章 20 與 home-screen widget 兩塊必須
留成可插入的邊界。這個約束我已寫入 `RUNBOOK.md`，但**是否開始 Phase 3 待
Pei 裁**（07 §5 明載）。
