# 70 — Comfort HMI / corpus-divergence 增註、放寬可量、複核留檔、RD-1 定稿

- 產出層：執行層｜2026-08-17｜對象：分析層／Pei
- 覆核對象：`docs/handoff/90_version_divergence.md`

---

## 1. 四項落實

| 項 | 落實 |
|---|---|
| ENTRY 028 增註 | `DELIVERY.sha256` 之 ENTRY 028 內文 **append** 一段 `corpus-divergence`（原內容與 hash 未動），逐字含 90 §2.2 之英文段落，另附一句中文：**現行交付物不是現行語料之產物，而是其前一版**；並記 **(b) 之正當性繫於「重新交付會發生」**，前提若不成立即須重審 |
| `STATUS.md`／`INDEX.md` | 各增一列／一句，指同一事實 |
| `RUNBOOK.md` | 新節「放寬幅度必須可量」，**接在 R-C32 段之後並列**：R-C32 管該不該放寬，本則管放寬了多少要看得見 |
| 複核留檔 | **`docs/RD1_universal_negatives_review.md`**（新檔）—— 掃描方法、七句逐句、兩種錯之分別 |
| RD-1 | **狀態改為 final (2026-08-17)**；回覆去向一行仍留白待 Pei |

---

## 2. 三處值得記住的措辭

**ENTRY 028 之增註**不只記事實，也記其失效條件：
(b) 是「暫時」，而暫時之正當性來自「會有下一次交付」；
若 RD-1 長期無覆，(b) 就變成永久不一致 —— **該重審條件寫在增註裡，
不寫在別處**，因為讀到那一列的人才是需要知道的人。

**`RUNBOOK.md` 之新節**留住兩案之分工：
第一案（Comfort stem 之 SR25 仍會被抓）證明 gate 還擋著該擋的；
第二案（豁免清單中帶 SR25 者恰為一件）證明**它讓過去的只有那一件**。
**只有前者，是一半的證明。**

**留檔之末節**記下一項措辭分別：`we have` → `we have read`。
**「我們有的」與「我們讀過的」不是同一個集合**，而可稽核的是後者。

---

## 3. 複核留檔之內容摘要

| | |
|---|---|
| pattern | 形態式（否定形態 × 範圍詞之交集），非清單 |
| 命中 | 全檔 19 句，其中**對整個搜尋空間作斷言者 7 句** |
| 結論 | **改寫 3 句**（Q6／Q7／Q9）、**維持 4 句**（Q2／Q3／Q8／附錄）|
| 兩種錯 | 「沒查而說沒有」（Q7）與「**查了但把結果說得比它大**」（Q6）—— 後者較難發現，因為它的每一步都做了，只有最後一句話跨出了範圍 |

---

## 4. 現況

- RD-1：**22 問，定稿**；`docs/RD1_universal_negatives_review.md` 為其依據留檔
- lint **54 / 54 PASS，0 finding across 434 TCs**；反向驗證 7 支全 PASS
- `BASELINE.sha256` 11 列全 OK；台帳 gate 驗過 68／已知不存在 1／有問題 0
- 交付物 `…_20260816_extdocs.xlsx`（`03663159…`）**在交付夾內，未動**，
  其與語料之一列一欄差異已登記

**待 Pei**：RD-1 之回覆去向與送達（22 問）。**無其他待辦。**

**未做**：未生成 TC、未寫回、未重新交付、未動交付夾、未搬檔至 `inputs/`；
git 未執行。
