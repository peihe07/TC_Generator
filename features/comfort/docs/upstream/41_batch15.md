# 41 — Comfort HMI / R-C41、斷言盤點、批次 15（最後一組）

- 產出層：執行層｜2026-08-16｜對象：分析層
- 承接：下放包 62／63
- 結果：七項全數落實。R-C41 貼入。既有斷言盤點歸三類，**(b) 類實作為兩道 gate**
  —— 其中 `mirror-map-verified` **立刻找出一個會使 gate 自己誤判的量測缺陷**
  （difflib 之 `autojunk`，§2.3）。等價掃描增 PC 報告欄：**22 組中 4 組連
  `pre_conditions` 亦相同**。ch9 之 8 leaf 逐一套三問 → **7 leaf 可生成**，
  惟與 DR #38 之現行處置不一致，**本輪不逕行生成，待裁**。
  批次 15 產 **57 條**（`-304`…`-360`），停 2（`11.5`／`12.6`，白名單未裁）。
  **`122-02` 之預裁條件已滿足並啟動**（DR #32 升 High）。
  lint **48/48 PASS，360 條**。ENTRY 012 已產出。
  **十五組全數開過 —— 詳見 §10 之明白告知。**

---

## 0. 下放包七項之對照

| # | 指示 | 狀態 |
|---|---|---|
| 1 | R-C41 貼入 `RULINGS.md` | ✅ §1 |
| 2 | 盤點既有斷言歸三類；(b) 類實作為 gate 並回報首次結果 | ✅ §2 |
| 3 | 等價掃描增 PC 報告欄 | ✅ §3 |
| 4 | 輸出 3 組 ≥0.9 之逐字 diff，聲明門檻僅用於產生候選 | ✅ §4 |
| 5 | ch9 之 8 leaf 逐一套三問，回報可生成者 | ✅ §5 —— **7 可生成，惟與 DR #38 相衝** |
| 6 | 加 `prose-reqid-exists` gate 並反向驗證 | ✅ §6 |
| 7 | 執行批次 15：`Heated Vented Seats` | ✅ §7 |
| — | 上繳 41 | 本件 |

---

## 1. R-C41 已貼入

`RULINGS.md` 第 928 行起，逐字照錄。本件之所有比對陳述**逐處標
`[machine]`／`[manual]`**；未標者依 R-C41 視為 `[manual]`。

---

## 2. 既有斷言之盤點與 (b) 類之實作

### 2.1 盤點（方法 `[machine]`：對 41 份上繳包 grep 斷言詞）

| 斷言詞 | 出現次數 |
|---|---|
| 實測 | 237 |
| 掃描 | 64 |
| 逐一 | 33 |
| `N 命中` | 28 |
| 已驗 | 18 |
| 逐句／逐條／逐字比對 | 13 |

**逐句歸類不成比例（392 處），故以斷言之「種類」為單位歸類**，並記其量。
這一步本身是 `[manual]`。

| 類 | 種類 | 處置 |
|---|---|---|
| **(a)** 現由 gate 覆蓋 | Procedure↔ER 1:1、modal、行尾句點、`design_method` 字串、`priority` 值域、tc_id 連續、`spec_ref` 格式、EMEA 逐條答、interface-axis 逐節答、軸值數與否定式覆蓋、marker 白名單與 Remarks、JSON 鍵覆蓋、殘留掃描、reasoning 句數、標題字數、`no-tcid-in-prose`、`prose-reqid-exists`、pending／provisional sibling、function-axis 反向檢驗 | 標 `[machine]`，具名該 gate；無須重驗（**共 48 道**）|
| **(b)** 機器可覆核而尚無 gate | **①「逐字相同」之鏡射標籤**（18 處，R-C40 之前件由此讀出）；**②「已停下」與「已產出」之互斥**（44 個 withheld leaf）；**③ 等價對是否在 sibling 表內**（§7.5 之發現）| **本輪實作為三道 gate**，見 §2.2 |
| **(c)** 本質為判斷 | 「無共用可觀察量」「非 sibling」「刻意略過之理由」「軸之類別（R-C34）」「R-C40 之章節脈絡判斷」 | 標 `[manual]`，其有效範圍為**判定當時之語料規模**（現 129 節／403 leaf／360 TC）；語料增長後須重判 |

### 2.2 (b) 類之三道新 gate 與首次結果

| gate | 首次結果 |
|---|---|
| `mirror-map-verified` | 18 個 `mirrored` 列**全數通過**（共同連續字串 74～624 字元），逐列印出其長度與片段 |
| `withheld-not-generated` | 44 個 withheld leaf，**無一同時被產出**；全數存在於 037 之 403 內 |
| `equivalence-in-sibling-table`（63 §1）| **首次即紅** —— 4 對等價節不在 sibling 表內，見 §7.5 |

反向驗證 `verify_b_gates.py`（九向全 PASS），含**失效側**：不相干之兩節
（`7.3`↔`2.5`，21 字元）會 FAIL、不存在之 outline 會 FAIL。

### 2.3 **`mirror-map-verified` 的第一次執行差點誤判三列**

首次實作用 `difflib.SequenceMatcher(None, a, b)` 之預設值，量得：

| 對 | 預設（autojunk=True）| 訂正（autojunk=False）|
|---|---|---|
| `16.13` ↔ `2.13` | **1 字元** | **100 字元** |
| `16.17` ↔ `2.16` | 2 字元 | 86 字元 |
| `7.8` ↔ `2.12` | 19 字元 | 79 字元 |

`autojunk` 於序列長度 >200 時，把出現率 >1% 之元素當雜訊丟棄 ——
**在英文段落上那是大半個字母表**。三列會被判為「`mirrored` 卻無共同字串」。

> **一道用來檢查「有沒有做過量測」的 gate，自己的量測是錯的。**

**連帶訂正**：凡在**長文**上取過的相似度皆受影響。上繳 39 §6.4 之數已重測：

| 對 | 原記 | 訂正 |
|---|---|---|
| `16.8` ↔ `2.8` | 0.097 | **0.317** |
| `16.8` ↔ `3.2` | 0.070 | **0.365** |
| `16.12` ↔ `2.12.1` | 0.211 | **0.576** |

**結論方向不變**（皆非高相似），惟兩者之**大小關係翻轉**：訂正後鏡射對
（`16.8`↔`3.2`）比非鏡射對（`16.8`↔`2.8`）更相似，**這反而支持鏡射表**。
§4.2 之 ch11／ch12 短文比對**不受影響**（已重測，數值相同）。

`verify_b_gates.py` 之第四向即**把這個回歸釘住**：若有人把 `autojunk`
改回預設，該向會紅。

---

## 3. 等價掃描 —— PC 之報告欄（62 §2）

鍵維持三欄；每組另報其 `pre_conditions` 為 `same`／`partial`／`no`。
**手驗之結論（`106-04`≡`110-06` 之 PC 相同）自此機械化。**

本輪 **22 組**（批次 15 使其自 13 增為 22）：

| PC 判定 | 組數 | 意義 |
|---|---|---|
| `same` | 3 | `11.3`≡`12.4`、`11.4`≡`12.5`（兩組）—— **同一台車上跑兩次一樣的測試** |
| `partial (同行不同序)` | 1 | `16.2`≡`16.6`（DR #42） |
| `no` | 18 | 其中 12 組跨第九軸（兩側互斥），**6 組為 ch11↔ch12** |

**須具名之限制**：ch11↔ch12 那 6 組之 `no`，**其差異只在 PC 之出處標註**
（`(11.1)` vs `(12.1)`），語意上兩者是同一台車。**故 `no` 會高估差異** ——
PC 欄比對的是字串，不是意義。**方法 `[machine]`，其限制 `[manual]` 判讀。**

---

## 4. 3 組 ≥0.9 之逐字 diff（62 §3）

**門檻聲明**：`SequenceMatcher` 之 0.9 **僅用於產生候選**，判定依 diff 之內容。
10 組逐字相同者不經門檻。（方法 `[machine]`：leaf 文字取自 037 之
`Requirement Description` 欄。）

| 對 | 相似度 | **逐字差異** | 判讀 |
|---|---|---|---|
| `054-02` ↔ `067-02` | 0.962 | ch12 多 `/LEDs` | **實質差異**：ch12 之控制另有 LED，ch11 只寫箭頭 |
| `054-03` ↔ `067-03` | 0.968 | ch12 多 `/LED` | 同上 |
| `058` ↔ `072` | 0.934 | ch11 `HMI **Settings List**` ／ ch12 `HMI **Notes**` | **委派對象是兩份不同文件** —— 見 §7.3 與 DR #43 |

第三列最重要：**它不是措辭差異，是兩個不同的外部文件**。
若只看 0.934 而不看 diff，會把它當成同一句話的兩種寫法。

### 4.1 DR #11 之影響範圍訂正

11 個帶 popup 片語之 leaf **全數可生成**（§7.2），故 DR #11 **阻塞 0 leaf**；
其影響為**分組與 `duplicate_of`**（ch11／ch12 是否同一需求）。表已訂正。

---

## 5. ch9 之 8 leaf 逐一套 R-C28 三問（62 §4）

| leaf | 一、出處 | 二、資格 | 三、落點 | 結論 |
|---|---|---|---|---|
| `039`（9.1）| ✅ 本節自身 | — | — | **不可生成** —— 全句為配置陳述，**無可觀察之行為** |
| `040-01`／`040-02`（9.2）| ✅ CR11 之逐字原文，本節以「in these variants」回指（R-C29）| ✅ 車輛配置，§8.5 例外 | pre_conditions | **可生成** |
| `041`（9.3）| ✅ 同上 | ✅ | pre_conditions | **可生成** |
| `042-01`～`-03`（9.4）| ✅ 同上 | ✅ | pre_conditions | **可生成** |
| `043`（9.4.1）| ✅ 同上 | ✅ | pre_conditions | **可生成** |

**7 可生成、1 不可。** 其 PC 為 §8.5 之**條文自帶觸發條件**，非 profile 軸
—— 與 `2.9` 之「if this feature available」（批次 10 之 `013-04`）同型。

### 5.1 **但這個結論與 DR #38 之現行處置相衝，故本輪不逕行生成**

`17.5` 之 `128-01`／`128-02` 因「dual airflow modes 有無」之否定值無字面
而停下（軸不可登記）—— **其正向值同樣是條文自帶之限定語**。
兩者形態相同：

| | 正向值字面 | 否定值字面 | 現行處置 |
|---|---|---|---|
| `9.2`／`9.4`（ch9）| ✅ CR11 | ❌ | 上輪停下（41 §5.4）→ 本輪判為可生成 |
| `17.5`（DR #38）| ✅ CW4 | ❌ | 停下 |
| `2.9` 之 `013-04` | ✅ C8 | ❌ | **已生成**（批次 10）|

**三者處置不一致，而其差別無人裁過。** 若「條文自帶觸發條件」足以生成，
則 `17.5` 之二 leaf 亦應解封；若不足，則 `013-04` 之 PC 須撤。
**本層不自行擇一** —— 這正是 R-C40 所治之「同型不同命」，
只是此處不是逐字相同之條文，而是**同一種缺口形態**。請裁。

---

## 6. `prose-reqid-exists`（62 §5）

散文欄位命中之 req_id 須存在於 037 之 403 leaf 集合（該集合**已含**
section-level leaf 與全部已停下者，故「引用已停下之 leaf」不會誤判）。
首次結果：**126 個相異 req_id 引用，全數存在**。

反向驗證併入 `verify_no_tcid_gate.py`（十向全 PASS），其中兩向為要害：
- **宇宙必須是 037 之 403**，不得由 corpus 自身導出（**自導出之宇宙永遠不會 FAIL**）
- 已停下之 leaf（`128-01`／`122-02`／`125-08`／`016-01`）在宇宙內 → 引用它們維持靜默

---

## 7. 批次 15 —— `Heated Vented Seats`（最後一組）

### 7.1 產 57，停 2

| 節 | leaf | tc_id | | 節 | leaf | tc_id |
|---|---|---|---|---|---|---|
| `11.1` | 4 | `-304`…`-307` | | `11.11` | 3 | `-336`…`-338` |
| `11.2` | 4 | `-308`…`-311` | | `11.11.1` | 1 | `-339` |
| `11.3` | 1 | `-312` | | `12.1` | 4 | `-340`…`-343` |
| `11.4` | 2 | `-313`／`-314` | | `12.2` | 4 | `-344`…`-347` |
| `11.6` | 6 | `-315`…`-320` | | `12.3` | 2 | `-348`／`-349` |
| `11.6.1` | 2 | `-321`／`-322` | | `12.4` | 1 | `-350` |
| `11.7` | 4 | `-323`…`-326` | | `12.5` | 2 | `-351`／`-352` |
| `11.8` | 4 | `-327`…`-330` | | `12.7` | 2 | `-353`／`-354` |
| `11.9` | 2 | `-331`／`-332` | | `12.8` | 3 | `-355`…`-357` |
| `11.10` | 3 | `-333`…`-335` | | `12.9` | 3 | `-358`…`-360` |
| — | — | — | | **`11.5`／`12.6`** | **2** | **停下** |

57 emitted ＋ 2 withheld = 59（framework.md：22 節／59 leaf）。

### 7.2 DR #11 之 11 leaf —— **全數可生成**（63 §1.1）

11 leaf：`054-01`／`055-01`／`059-02`／`061-02`／`061-03`／`061-04`／
`062-01`／`063-01`／`064-01`／`064-02`／`065-02`。

**同一個依據**：每條之 ER **只用 ch11 自身之字**（`opens popup`、
«Driver Seat Zone»、`3 seconds`、`status bar and temperature/comfort popup`），
**無一條斷言 popup 之內容** —— 而 popup 內容正是未入案之 Pop Up List 所定者。
R-C28 第一問由本節滿足。

> DR #11 決定的是**兩章是否同一需求**（分組），不是**這條 ER 寫不寫得出來**。
> 「形態受影響」逐 leaf 化之後，是 11 個「可生成」。

### 7.3 `11.5`／`12.6` 停下 —— 白名單為裁定，非生成時之決定

兩節全句皆為「Refer to HMI … for the details on the Auto Comfort Settings
options」，**整條委派予本 spec 以外之文件**，形態即 `[BLOCKED-SPEC]`。
**惟 profile §5.1 之白名單是裁定（R-C26），二者不在其上**，故停下回報、
**不自行標 marker**。已登 **DR #43** 請裁（列入白名單產 BLOCKED row，
抑或維持停下並登為 coverage gap）。

**委派對象不同**（Settings List vs Notes，§4 之 diff）—— 故 R-C40 之前件
不成立，兩側之停下各自成立而非互相援引。

### 7.4 `11.11` 之「硬鍵配置」—— 三條件不齊，不登記為軸

| 條件 | 結果 |
|---|---|
| 1. 兩值逐字 | ❌ 正向值逐字（`configured with hard buttons for comfort controls`），**否定值於 129 節無字面** |
| 2. 互斥且窮盡或明示並列 | ❌ |
| 3. 無值由推論 | ❌ |

**不增軸**；三 leaf 仍生成，以**本節自身之句子**為 §8.5 條文自帶觸發條件
（與 §5 之 ch9 同一形態 —— 故 §5 之請裁一併適用於此，本層已在此使用該作法，
**其一致性由本節之 `reasoning` 具名**）。

### 7.5 **本輪最大的發現：同組內之 sibling 從未被產生為候選**

`sibling_candidates.py` 依設計排除同一 Test Set 內之配對
（`group[a] == group[b]`）。而 Part N 把 ch11 與 ch12 **併為同一組**。

> **合併兩章，等於把它們從 sibling 偵測裡拿掉。**

結果：**全 corpus 最強之四對等價，從未被判過**——

| 對 | 等價之 TC | PC |
|---|---|---|
| `11.1`↔`12.1` | 3 對（`054-02`～`-04` ↔ `067-02`～`-04`）| no（僅出處標註之別）|
| `11.2`↔`12.2` | 3 對 | no（同上）|
| **`11.3`↔`12.4`** | 1 對 | **same** |
| **`11.4`↔`12.5`** | 2 對 | **same** |

後兩對之兩節**全文逐字相同**，其 TC **四欄全同** —— 與 DR #42 同型。

處置：四列補入 `pending_sibling.tsv`（`source = identical-tc-scan`），
verdict `sibling`，`equivalent_tc_pairs` 記其對，兩側 doc 之
`distinguishing_axis` 回填；**新增 gate `equivalence-in-sibling-table`**
使「等價對不在表內」不再可能。**已登 DR #44**：
gate 只覆蓋「TC 逐字相同」者，**同組內非逐字相同之 sibling 仍無機制可發現**。

### 7.6 `12.3` 生成後 —— **`122-02` 之預裁條件已滿足，預裁啟動**

R-C39 條件三之兩個候選節皆已生成：

| 候選 | 所定者 | 條件三 |
|---|---|---|
| `14.16.1`（批次 11）| 座椅分區圖示之三態循環與關閉時之灰化 | ❌ |
| **`12.3`（本批）** | **active／inactive 之文字與圖示顏色（白／灰）** | ❌ —— 依**控制之作用狀態**，非**車輛配置** |

`122-02` 所缺者為 `system configuration → icon` 之對照，**與兩者皆不同層**。

依 47 §1 之預裁：`SWE1-HVAC-122-02` **不產列、不指派 tc_id、不入 coverage 分母**；
**DR #32 升 High**，問句改為「`16.16` 所指之 Climate section 為何節？
該節是否定義 configuration 與 icon 之對照？」

### 7.7 第十二軸首次以排除式使用

ch11／ch12 之座椅控制位於 comfort category 之 **Seats 分頁**內，
而 `2.1` 於僅前排氣候之車輛使 **tabs 不顯示** → 該介面消失，故補排除式 PC。
**狀態列與 temperature/comfort popup 不隨 tabs 消失**，故 `056`／`070`／`066`／
`065-02`／`065-03` **五條不補**（`interface_axis_review` 之 `axis_12` 欄逐節記其分野）。
profile 之 `axis-values` 增第七個區塊（軸 12），其 scan 實測：
pattern 三句命中**全在 `2.1`**。

### 7.8 223 列 provisional 之逐對判定

全部逐對判定（不僅解旗）：223 列改判 `not-sibling`，其理由皆附
**`[machine]` 之佐證**（等價掃描之 22 組**與該對無一相關**）。
表現況：1672 列；`not-sibling` 1425／`deferred` 202／類級 27／`sibling` 18；
`provisional` false 1371／true 301；`equivalent_tc_pairs` 已填 **12 列**。

---

## 8. lint 與 §9 自評

```
48 / 48 gates PASS; 0 finding(s) across 360 TCs
```

反向驗證六支全 PASS（`no_tcid`＋`prose-reqid`／`equiv_invariant`／`b_gates`／
`provisional`／`axis`／`axis_type`）。

TC 303 → **360**；已生成 leaf 298 → **355**；已生成節 91 → **111**。
**已生成 leaf 355 / 403 ＝ 88.1%**。

**§9 十七項**：新增 57 條（批次 15）。

| # | 項目 | 變動 | 獨立依據 |
|---|---|---|---|
| 3 | Pre-Condition | 變 | 第八軸（Multi-Level／Standard 座椅）＋第七軸（Multi／Single 方向盤）＋第六軸（座椅分區）＋第十六軸（17.3）＋`11.11` 之條文自帶觸發條件；排除式：軸 13（2.14）、EMEA（16.2）、軸 9（6.3）、**軸 12（2.1，首次）** |
| 5–8 | 步驟 | 變 | 每條 2–3 步，末步持驗證 |
| 9 | Baseline | 變 | 按壓循環之每一段皆需前一段為基線，故首步為設定該段 |
| 10 | Procedure ↔ ER 1:1 | 變 | 57 條全數 1:1 |
| 11 | FP／FF | 變 | `059-02`（不顯示 popup）、`059-05`（不可關閉）、`061-04`（灰化時不開）、`065-01`／`065-03`（不顯示）為否定式，各配正向對照 |
| 12 | 溯源、§8.2.1 | 變 | 57 leaf 各溯其 037 req_id；**ch11↔ch12 之比對一律回母節 full_text**（63 §1.2）—— 037 之 leaf 描述省略主詞，`062-02`～`-04`（方向盤）與 `067-02`～`-04`（座椅）逐字相同即其證 |
| 13 | Design Method | 變 | 34 條狀態轉換（按壓循環）、23 條功能測試 |
| 16 | `specification_reference` | 變 | 各條含自身節次＋2.14＋16.2；comfort section 者另加 6.3＋2.1；取第十六軸者另加 17.3 |
| 17 | §8.6／§8.7 | 變 | `«Driver Seat Zone»`／`«Passenger Seat Zone»` 為 profile §3.4 之 source-quoted token，照錄；`3 seconds` 為條文明值，寫入 ER |
| 其餘 | — | 不變 | |

---

## 9. 「本包是否仍有該驗而未驗者」（R-C30）

1. **批次 15 之 57 條未經 §7 之 FP／FF 人工複核**，只經 lint。
2. **§2.1 之盤點以「種類」為單位，不是逐句** —— 392 處斷言中，
   我實際讀過的約 40 處；**其餘是依詞頻歸類的**，這一步是 `[manual]` 且抽樣。
3. **`autojunk` 之連帶訂正只重測了我記得的三個數**（§2.3）——
   **上繳包中其他長文相似度數字未逐一重測**，其誤差方向已知（低估）。
4. **§3 之 PC 欄比對字串而非語意**，故 ch11↔ch12 之 6 組 `no` 是高估；
   反過來說，**若兩條 PC 語意相同而字串不同，本欄不會告訴你**。
5. **§7.4 之 `11.11` 已使用「條文自帶觸發條件」而 §5 之 ch9 未用** ——
   兩者同型，**本輪之處置在同一份上繳包裡就不一致**。
   我選擇在本組使用（因其為本批之 leaf）、在 ch9 不使用（因其非本批且已停），
   **但這是作業邊界之理由，不是原則之理由**。
6. **`equivalence-in-sibling-table` 只覆蓋逐字相同者** —— 同組內
   「幾乎相同」之 sibling 仍無機制（DR #44）。

---

## 10. 待分析層 —— **並告知：所有可自行推進之工作已完成**

十五組全數開過。**剩餘 48 leaf 全數繫於 DR 之回覆**，逐項列出：

| 停下之 leaf | 節 | 繫於 |
|---|---|---|
| 2 | `2.1` | DR #17（tab 集合）|
| 2 ＋ 2 | `2.3.1`／`2.5.1` | DR #38（dual airflow modes）／DR #32 |
| 1 | `2.5` | DR #32（recirc 圖示對照）|
| 2 ＋ 2 | `2.11`／`16.11` | 後排氣候軸（DR #17 之同源，R-C40 已裁兩側同停）|
| 3 ＋ 6 | `2.12`／`2.12.2` | **DR #31** |
| 2 | `2.13` | VF HVAC 文件（DR #16）|
| 8 | `9.1`～`9.4.1` | **DR #41 ＋ §5 之請裁**（7 leaf 之可生成性）|
| 2 | `11.5`／`12.6` | **DR #43**（白名單裁准）|
| 1 ＋ 3 ＋ 1 ＋ 1 | `14.1`／`14.12`／`14.14`／`14.15` | DR #37／#34／#32 |
| 1 | `16.16` | **DR #32（已升 High，預裁啟動）** |
| 1 ＋ 1 ＋ 2 ＋ 2 | `17.2`／`17.3`／`17.4`／`17.5` | DR #6（螢幕尺寸）／DR #38 |
| 3 | `18.1` | R-C17（與 `17.1` 逐字相同，章標題不是條文）|

**本 feature 首次到達此狀態**：pipeline 之進度完全繫於 DR 之回覆，
**不以「還有零星可做」掩蓋**。若無 DR 回覆，本層可做者僅剩：
複核既有 TC（§9.1 之 FP／FF 人工複核）、以及分析層另行指派之查核。

**優先度建議（依解封 leaf 數）**：DR #31（9）→ DR #41／§5（7）→
DR #6（5）→ DR #38（4）→ DR #34（3）→ R-C17（3）。

**另請裁三事**：
1. **§5 之三者不一致**（ch9／`17.5`／`013-04`）—— 條文自帶觸發條件足不足以生成。
2. **DR #44** —— 候選產生器是否應涵蓋同組內配對。
3. **DR #43** —— `11.5`／`12.6` 之白名單。
