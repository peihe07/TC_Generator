# 54 上繳 — 空行判準訂正與收尾

執行層 → 分析層 | 往返 NN = 54 | **最後一個工作包**

## 一、§J 自檢（含 R-P348 之十對）

抄錄前重驗（R-P200(c)）：
**§A 頂層 block = 5、§J 列數 = 5、§H 步驟 9 =「五條」、相容性 C(5,2) = 10 對齊備。四處一致。**

**⚠ 十對中有一對之理由寫鬆**：§J 載 `R-P347×R-P349` 為
「無交集（後者為 TC 產出，前者為格式）」—— **不精確**。
R-P349(b) 新增之三條 TC 亦有 `test_item`，二者確實共用同一產物。
惟 R-P348(b) 所問為「**是否存在同時滿足二者之解**」，
答案為有（新 TC 依 R-P347 格式寫即可），**故結論成立而理由不精確**。
其餘九對確為無共用產物。已依 R-P348 之判準逐對獨立核對，非僅數對數。

## 二、§D 全表自驗

| # | 期望 | 實測 | 判定 |
|---|---|---|---|
| G237 | 全批全數 PASS；fixture 含首段含空行之正例與末段前非恰一空行之反例 | fixture **8 / 8**；全批 **283 / 283** | **PASS** |
| G239 | 三項皆有 TC；受阻三項已入驗證邊界 | `-281` / `-282` / `-283`；受阻三項入交付說明 §3.2 | **PASS** |
| G243 | 涵蓋補測後全數；2 條鍵斷裂已補齊 | **283 列**；鍵斷裂殘留 **0** | **PASS** |
| G244 | 十項全數 PASS | **10 / 10**，寫入 284 列（TC 283 ＋ 留白 1） | **PASS** |
| G70 | 全 PASS | 阻斷類 PASS（exit 0）；待裁 323 項為 R-P42(b) 既有 | **PASS** |
| 素材層 | 依 R-P327 重跑 | G0 9/9、G99 111/111、G94 111/111、G103 111/111、G240 8/8 | **PASS** |

**§D 全表過。** 惟 G94 / G240 之數字須連同 §四之強度界線一併讀。

## 三、B1 —— G237 訂正（R-P347）

分隔符改以**最後一個空行**認定，首段內部之空行不計。

**未放寬攔截力之佐證**：紅向 fixture 六個訂正後仍全叫 ——
無空行、**末段前二個空行**、末段無括號、末段無 ` -> `、
首段不在 `source_clause` 內、首段帶自加前綴。
綠向二個，其一即 R-P347 之訂正標的「首段內含空行」（`4942354` 之形狀）。

53 包之 11 項 FAIL 全數轉綠，**其成因確為判準之分隔符定義錯，非二條互斥**
—— A-PW333 之判定依本條訂正（A-PW335）。

## 四、B2 —— 可行三項補測（R-P349(b)）

| tc_id | leaf | 標的 | 首段片段起訖 |
|---|---|---|---|
| `NR1L-PowerManagement-281` | `SWE-PM-007` | `Bench` 列之八個輸出取值 | 3374–3880 |
| `NR1L-PowerManagement-282` | `SWE-PM-001` | `Full-Operation` 含 SDCARD / BT / Phone Call 之列 | 651–1194 |
| `NR1L-PowerManagement-283` | `SWE-PM-004` | `Timed` 含 SDCARD / BT / Phone Call 之列 | 2311–2845 |

首段依 R-P344 取該狀態列在攤平串流中之連續片段，
**照原分隔逐字取，未加 `=`、未加 `,`、未重排欄序**；起訖字元位置入 `reasoning_note`。

**`tc_id` 自 281 起附加，既有 280 條之號碼不變** ——
與 R-P322 所記之 100% 位移不同性質，舊號在本次不再位移。

⚠ **三條首版之末步皆逾 §5.2B 之 18 字上限**（22 / 21 / 21 字），已縮短末步後重寫，
**驗證目標未改，僅措辭濃縮**（A-PW339）。
其與 14 包所記「18 字上限已有三條頂到上限」為同一結構性問題之第二度顯現。

## 五、B3 —— 承接 53 包 B6

- **三代對照表重建** → `data/tcid_three_gen_54.md`（283 列，鍵斷裂 0）
- **50 包提案版已加標「提案版，已由重建版取代」並保留不刪**（R-P329）
  ⚠ 重建腳本 `tcid_three_gen_50.py` **會就地覆寫 50 包版** ——
  本次已自備份還原並另存新檔；**該腳本之輸出路徑未改，下次重跑會再覆寫一次**
- **R-P330 三處加註**：R-P322 / R-P325 / R-P326 各於其下加註指向 R-P330，原文不改
- **交付說明條數** 280 條 / 281 列 → **283 條 / 284 列**
- **全量 dry-run** 十項全數 PASS

## 六、B4 —— 驗證邊界三項增列

交付說明 §三之未驗證性質自**六項增為八項**：
第 7 項「`source_clause` 之段落切分是否正確」、
第 8 項「其餘交付欄位之格式規則是否落實」。
新增 §3.1（R-P350 逐字）與 §3.2（三個受阻狀態）。

## 七、B5 —— 加註（依 R-P36 原文不改）

`R-P342` 加註指向 R-P347；`R-P250` 加註指向 R-P351。

## 八、20 條重新檢視之處置（R-P328(c)）

51 包 B2 所令之「20 條重新檢視」，其實質已由本包與 53 包分項完成：

- **新增錨點是否使涵蓋不足** → R-P340(b) 之六項，三項補測、三項受阻（§四、§六）
- **判準有誤** → `4941453` 之逐輸出值與現行泛稱 ER 之落差已載於
  `data/g239_state_table_53.md`
- **應測而未測** → 同上

**⚠ 惟「逐條」之形式未做** —— 20 條未一條一條列出結論，
而是以標的（狀態／輸出組合）為單位處理。
**據實回報：若分析層要的是 20 條各有一行結論，本包未提供。**

## 九、DR-PW11 之時程風險（分析層 §K 第 2 項）

`SWE-PM-008` 之解阻繫於 DR-PW11 之回覆，**該 DR 自 22 包開出至今未獲回覆**。
其影響面於本包擴大 —— 原為 1 個 leaf 之 TC，現另加三個狀態之補測。
**時程不在本專案控制內**，已入交付說明 §3.2。

## 十、執行層獨立判斷

1. **本包五條中有二條是在修前三包所立條文之錯**（R-P347 修 R-P342、R-P351 修 R-P250），
   另一條是在補一個查不到該類錯的機制（R-P348 補 R-P335）。
   **51–54 四包中，用於修正分析層自身條文之往返多於用於產出 TC 之往返。**
   R-P346 已界定其不違 R-P341（查證 vs 修正內部矛盾），該界定成立；
   **惟其代價是四次往返只補了 3 條 TC 與 280 條 `test_item`。**
2. **R-P348 之機制查得到 A-PW333 那類錯嗎 —— 未必。**
   其要求「取二條要件之極端案例，問是否存在同時滿足二者之解」。
   而 A-PW333 之極端案例為「**錨點文字自身含空行**」——
   該案例之存在只有實際掃過 220 個錨點才知道。
   **§J 之十對檢查若僅憑條文推想而不觸語料，同型之錯仍會漏。**
   建議 55 包令該檢查須**引語料實例**，否則其與 R-P335 之六處檢查同為形式。
3. **本包唯一未達成分析層字面要求者為 §八之「逐條」**。據實記明，不飾。
4. **寫回前尚缺二事**（§E）：Pei 就 283 條版本明示授權、Pei 指定客戶目錄路徑。
   **G244 已十項全數 PASS，技術面無阻。**
