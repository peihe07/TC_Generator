# 01 — Power Management Phase 0 Intake（上繳）

上繳包 | 執行層 → 分析層 | 往返 NN = 01
結果：**停於步驟 2（素材台帳核對）。步驟 3–7 未執行。**

---

## 0. 結論摘要

| 步驟 | 狀態 |
|---|---|
| -1 檔案完整性掃描 | PASS —— **0 處損壞**，已知兩處皆完好，未修改任何位元組 |
| 0 全檔一致性實測（八項） | PASS —— 八項全數相符 |
| 0.5 §K 第 2 項註記 | DONE |
| 1 `new_feature.py Power --adopt-existing` | DONE，未覆寫 `01_intake.md` |
| 2 素材落入 `inputs/` 並重測 SHA256 | **FAIL —— 7 份中 3 份不符。停。** |
| 3–7 | **未執行** |

---

## 一、步驟 -1：損壞掃描結果

掃描方式：`open(p,"rb").read()`，逐段 `decode("utf-8")`，
遇 `UnicodeDecodeError` 記錄位移與前後 60 bytes，自 `e.end` 續掃。

```
size 12636 bytes
0 處損壞
```

**全檔 strict UTF-8 解碼一次通過，無任何非法位元組序列。**

補充掃描（位元組合法但字元可疑者）：

- U+FFFD REPLACEMENT CHARACTER：**0 個**
- Private Use Area U+E000–U+F8FF：**0 個**
- U+FFFE / U+FFFF：**0 個**

指定之兩處已知損壞點，**實測皆完好，未做任何修復**：

| 指定損壞點 | 實測 | 判定 |
|---|---|---|
| §F「原樣保留，不進 lint」與「不進 trace、不寫回）」之間 | `（原樣保留，不進 lint、不進 trace、不寫回）` —— 頓號 U+3001 在位 | 完好 |
| §A R-P7「547 」與「）\*\*不追、不問…」之間 | `CFTS009 本文未被引用之 547 條）**不追、不問、不列 RD-1**。` —— 「條」U+689D 在位 | 完好 |

**是否全部修復：不適用 —— 無損壞可修。**
(-1.C) 之停止條件（掃到已知兩處以外之損壞）未觸發。
(-1.D) 驗證：`open(p,"rb").read().decode("utf-8")` 不拋例外。

> 對分析層之提示：本檔寫入結果正確。若 Filesystem MCP 曾破壞過此路徑，
> 該損壞不存在於執行層所見之磁碟內容。已知兩處之描述與磁碟現況不符，
> 此一「已知事實」本身即為未經實測之陳述。

---

## 二、步驟 0：八項實測值對照表

| # | 檢項 | 期望值 | 實測值 | 判定 |
|---|---|---|---|---|
| 1 | §A fenced block 數 | 8 | 8 | PASS |
| 1b | §A 編號無缺無重 | R-P1…R-P8 | `1,2,3,4,5,6,7,8` | PASS |
| 2 | §D 節標題含「十一項」 | 含 | 含（`## D. 閃點（實測十一項不符即停…`） | PASS |
| 2b | §D 閃點列數／編號 | 11、G1…G11 | 11、`G1…G11` 無缺無重 | PASS |
| 3 | §E 節標題含「已定版」 | 含 | 含（`## E. framework Layer 2 / Layer 3（**已定版**）`） | PASS |
| 3b | §E 表列數 | 5 | 5 | PASS |
| 3c | §E leaf 欄依序 | 64 / 24 / 16 / 7 / 3，合計 114 | `[64, 24, 16, 7, 3]`，sum **114** | PASS |
| 4 | §G 表列數 | 4 | 4 | PASS |
| 4b | §G Urgency 分布 | 撤回 2（DR-PW2、DR-PW4）；live 2（DR-PW1 High、DR-PW3 Medium） | DR-PW1 `High`／DR-PW2 `**撤回**`／DR-PW3 `Medium`／DR-PW4 `**撤回**` | PASS |
| 5 | §H 步驟 4 | 寫「十一項」 | `4. 以 §D 十一項閃點自驗；任一不符即停並上繳。` | PASS |
| 5b | §H 步驟 5 | 寫「八條」 | `5. 將 §A 八條裁決逐字抄入 RULINGS.md；…` | PASS |
| 6 | §I 提及「兩項 Test Set 邊界」 | 不含 | 不含（且全節無「邊界」二字） | PASS |
| 7 | §J 表列數 | 8 | 8 | PASS |
| 8 | 全檔 strict UTF-8 解碼 | 通過 | 通過（見 §一） | PASS |

**八項全數與指定狀態完全一致。**
分析層未能當下實測之 §D／§E 兩節，執行層實測結果與其寫入意圖相符。

---

## 三、步驟 0.5：§K 註記

§K 第 2 項末已加註，第 1、3 項未動：

```
   此為 DR-PW2(b) 之證據強度上限 —— 現階段只能說「可疑」，
   不能說「遺漏」。（R-P7 後已無追查必要，保留作為當時判斷之紀錄）
```

---

## 四、步驟 1：scaffold

```
$ python3 scripts/new_feature.py Power --adopt-existing
scaffolded /Users/peihe/Work_Projects/TC_Generator/features/power
```

`--adopt-existing` 語義為「填補缺口，絕不覆寫既存檔案」。
`features/power/docs/handoff/01_intake.md` 未被覆寫（§K 註記仍在，第 239 行）。

產生之檔案：`.gitignore`、`ANOMALIES.md`、`DATA_REQUESTS.md`、
`DECISIONS.md`、`PLAYBOOK.md`、`RULINGS.md`、`RUNBOOK.md`、`feature.yaml`、
空目錄 `inputs/`。皆為骨架，**內容尚未填寫**（步驟 5、6 未執行）。

---

## 五、步驟 2：素材台帳核對 —— **FAIL，停**

素材原始位置（唯一命中之目錄）：

```
/Users/peihe/Work/02_Project_R1LR/10_Reviewing/00_TestCase/ASW-R2/Power Management/
```

搜尋方式：以 §B 之 7 組 `(bytes, sha256前16碼)` 為索引鍵，
對 `/Users/peihe/Work`、`/Users/peihe/Work_Projects`、`Desktop`、`Downloads`、
`Documents` 全樹掃描；三份未命中者再對 `/Users/peihe` 全家目錄補掃。

### 5.1 相符者（4 / 7）

| §B SHA(16) | §B bytes | 實測全 64 碼 SHA256 | 判定 |
|---|---|---|---|
| `ce93174794d0d43c` | 78607 | `ce93174794d0d43c03d25dcd577c2811b85a8ebb2fd754a5201e5d6979297eda` | **相符** |
| `2284abf5e6c17e4d` | 85210 | `2284abf5e6c17e4d1a082cf70d676b6551e16e023387c642f3fd1959c3478c8d` | **相符** |
| `6af7bfd314a28b39` | 227822 | `6af7bfd314a28b3925636b642dc80c87918b50136fb8fa264563b3dc117eb592` | **相符** |
| `f318b14623fcbf97` | 64422 | `f318b14623fcbf972c6d0428408a6cf5297cbe7d0a710c7fafb8a0e05f538e61` | **相符** |

四份 xlsx magic bytes 皆為 `50 4B 03 04`（ZIP/OOXML），與 §B「真實格式」欄一致。

（`6af7bf…` 與 `f318b1…` 另於
`/Users/peihe/Work_Projects/R1L_RTM_V3/data/9_ASPICE/02_SYS.2 …/` 有位元組相同之副本。）

### 5.2 不符者（3 / 7）—— 停止原因

§B 對此三份記為「純文字」，並據以立 R-P3 與 A-PW07。
**磁碟上之原始檔皆為真正的二進位文件格式，且大小相差一至兩個數量級。**

| §B 記載 | §B bytes | §B 格式 | 磁碟實檔 | 實測 bytes | magic bytes | 實測 SHA(16) |
|---|---|---|---|---|---|---|
| `691e5ca17bcfa112` …CFTS_009_Wake-up_and_Power-up_SR26….docx | 412654 | 純文字 | `R1LR_Atl-H_25PI3.5_Activation and Configuration_CFTS_009_Wake-up and Power-up_SR26_20250909-1658.docx` | **154588** | `50 4B 03 04` = **ZIP/OOXML** | `eb3eb0861363fc1d` |
| `d13919341c810b15` …CFTS_010_Power_Down__SR26….docx | 81064 | 純文字 | `R1LR_Atl-H_25PI3.5_Activation and Configuration_CFTS_010_Power Down _SR26_20250909-1658.**doc**` | **245248** | `D0 CF 11 E0 A1 B1 1A E1` = **OLE2 (.doc)** | `47c402a01b1a2e3a` |
| `019b3d7a51211cd3` SYS3_CFTS_009_…_SYSAD_v1_1_0.docx | 51264 | 純文字 | `SYS3_CFTS_009_Power_Management_FM-WI-FSM-011-A01_System Architectural Design_SYSAD_v1.1.0.docx` | **3474091** | `50 4B 03 04` = **ZIP/OOXML** | `cb6bf7d81030abc8` |

§B 所載之三個雜湊值，**於整個 `/Users/peihe` 家目錄內不存在任何符合之檔案**
（以 bytes 為索引先過濾，再逐一比對 SHA256 前綴；`Library`／`.Trash`／
`node_modules`／`.git`／虛擬環境已排除）。

### 5.3 此一不符所推翻之條文與數字

不是單純的「檔案找錯了」。分析層測的是**沙箱內某種純文字衍生物**，
不是這三份原始文件；而該衍生物之產生方式、產生者、轉換是否無損，
台帳中無任何記載。連帶影響：

1. **R-P3 之事實前提為假。**
   「三份 .docx 經 magic bytes 實測皆非 OOXML 亦非 OLE」——
   對原始檔實測：兩份是 OOXML，一份是 OLE2。R-P3 據此禁用
   `python-docx / olefile / zipfile`，而這三者正是讀取原始檔的正當途徑。
   照 R-P3 執行將**無法讀取任何一份 CFTS 本文**。
2. **A-PW07 為假。** 「三份 .docx 實為 Markdown 純文字，副檔名與內容不符」——
   副檔名與內容其實是相符的。
3. **CFTS010 之副檔名根本不是 `.docx`，是 `.doc`。**
   §B、R-P3、A-PW07、§I 禁區皆稱「三份 .docx」，此一描述在檔名層即為錯誤。
4. **G8 / G9 失去已驗基礎。**
   （CFTS009 需求錨點 904／章節錨點 172；CFTS010 需求錨點 148／章節錨點 92）
   此四數係在該純文字衍生物上量得。原始檔文字層若與衍生物不同，
   四數即無效。執行層無法在不解開原始檔的前提下驗證，而解開之路已被 R-P3 封死。
5. **§K 第 1 項之「50,296 字元、40 個章節號」失去基礎。**
   SYS3 原始檔 3,474,091 bytes，§B 記 51,264 bytes ——
   差 **67.8 倍**。淺層探測所探之物不是這份文件。
6. **§C 三段錨點鏈之第 1、2 步（CFTS 本文正則）無法起跑。**
   G1／G2／G6／G7／G10 之來源是四份相符的 xlsx，尚可執行；
   G3／G4／G5／G8／G9／G11 全部依賴 CFTS 本文。

**依 §B「不一致即停」與 §H.4「任一不符即停並上繳」，停於此。**
`features/power/inputs/` 保持為空 —— 四份相符者亦未複製，
因為「素材台帳整體通過」才是入 `inputs/` 的前提，
且 §I 明定「素材補入超出 `features/power/inputs/` 需 Pei 裁定」。

### 5.4 待裁項

- **Q1**：R-P3 是否撤回或改寫？三份原始文件之讀取方式為何？
  （若允許 `zipfile`／`python-docx` 讀 OOXML，`.doc` 那份仍需 `olefile`
  或外部轉檔；轉檔即引入新的衍生物，其雜湊須重新入台帳。）
- **Q2**：分析層之三份純文字衍生物請直接下放（附產生指令），
  或明示其已不可得、應以原始檔重新產生。
- **Q3**：G8／G9 四數在新讀取路徑下重測後若與 §D 不同，
  是以實測為準改寫 §D，或視為停止條件？
- **Q4**：A-PW07 撤回並改登為「台帳格式欄與原始檔不符」之新 anomaly？

---

## 六、獨立判斷：本包是否仍有該驗而未驗者

**有。§K 現列三項之外，另有五項；且 §K 第 1 項之敘述本身已因 §5.2 失效。**

### 6.1 §K 三項之現況

| §K | 內容 | 執行層判定 |
|---|---|---|
| 1 | SYS3 SYSAD 未讀 | **仍在，且比 §K 所述更嚴重** —— §K 稱「已做淺層探測（50,296 字元）」，但該探測對象非本文件（原始檔 3,474,091 bytes）。實為**完全未讀**，且連「已淺層探測」這句都不成立。 |
| 2 | 140 個 Atlantis 錨點未做 ECU×EE 聯合過濾 | **同意 R-P7 後無追查必要**，已於 §K 加註。附帶：其計數來源同為失效之衍生物，即使日後翻案亦須重數。 |
| 3 | Layer 2 聚類未交叉驗證 | **仍在。** 見 6.2 第 3 項——§K 未指出此項與 G11 互為循環。 |

### 6.2 §K 未列者（執行層自判）

1. **素材身分本身從未驗證 —— 這是本次停止的根因，而 §K 未列。**
   §B 標題寫「SHA256 為分析層沙箱副本實測值」，卻未記載沙箱副本
   與原始檔之對應關係、轉換步驟、或轉換後雜湊變動之事實。
   台帳的功能是讓下游能認出「是同一份東西」，此處三份做不到。
   **凡經轉換之素材，台帳須同時登記原始檔雜湊與衍生物雜湊**，
   否則執行層的重測必然失敗——如本次。

2. **§D 十一項閃點無一項驗證素材身分。**
   G1–G11 全部是「內容數量對不對」，沒有一項是「讀到的是不是該讀的檔」。
   十一項閃點在本次全數未能起跑，正因為擋在它們前面的那道檢查不在表上。

3. **§E 與 G11 互為同源，不構成交叉驗證。**
   §E 表宣告 64/24/16/7/3，G11 期望值亦為 64/24/16/7/3。
   以 G11 驗 §E 是循環論證。§K 第 3 項只說「未交叉驗證」，
   未指出 §D 內已有一項閃點被誤當成獨立驗證。
   真正的獨立方法（例如以 037 `Requirement Title` 或 SYS2 章節標題
   獨立聚類再比對）尚未設計——而 §E「本分組之已知弱點」已自承
   `Requirement Title` 無分組價值，等於承認獨立方法不存在。
   **§E 標「已定版」與「本表實際只由單一來源支撐、不是交集」並存，
   此二陳述之張力應由 Pei 裁決，不宜由執行層以實測默默覆蓋。**

4. **G5 只驗 leaf 身分，未驗其解析結果。**
   G5 期望「需 CFTS010 之 leaf 恰為 SWE-PM-071/072/073」，
   但 §E 進一步宣告這三顆解析到 `010 §1.7.1、§1.7.2`。
   後者無對應閃點。CFTS010 域是全案唯一的跨文件分支
   （且 A-PW04 已記「CFTS010 全域未進 037 追溯分頁」），
   風險最高的一段反而驗得最淺。

5. **§C 之讀取座標未經執行層實測。**
   「037 表頭 r7、資料 r8–r145、138 實體列其中 23 列全空」、
   「SYS2 CFTS009 r2–r338 / CFTS010 r2–r74」、
   「FW036 表頭 r9、資料 r10–r221」——
   這些是 §C 自己的輸入假設，不在 §D 之列。四份 xlsx 雜湊既已相符，
   本可當場驗完；因停於步驟 2 而未做。
   **建議下一包把座標實測併入 §D**，否則它們永遠是未驗的前提。

6. **（記錄用）§B 之 `bytes` 欄在本次成為唯一有效的偵錯線索。**
   若台帳只記雜湊不記大小，本次只會得到「找不到檔案」，
   而不會得到「找到了，但它是 154KB 的 OOXML 而非 412KB 的純文字」。
   此欄應保留並在所有 feature 沿用。

---

## 七、禁區遵守聲明

| 禁區 | 遵守情形 |
|---|---|
| 不得寫回 FW036 workbook | 未開啟、未寫入 |
| 不得執行任何 git 操作 | 未執行任何 git 指令 |
| 不得以 openpyxl save 寫任何 xlsx | 未載入 openpyxl |
| 不得補齊 SWE-PM-089 | 未進入步驟 3，無任何 leaf 對應動作 |
| 不得以 python-docx / olefile / zipfile 讀三份 .docx | **未讀取內容**。僅讀取檔案前 8 bytes 作 magic bytes 判定與計算 SHA256，係 §B、§H.2 明令之核對動作，非以上述任一函式庫解析文件結構 |
| §C 數字自行重跑，不得沿用 | 未沿用；亦未產出（停於步驟 2） |
| 素材補入超出 inputs/ 需 Pei 裁定 | `inputs/` 保持為空，未複製任何素材 |
| 不得自行猜測原文補字 / 修改裁決條文 | 僅執行 0.5 指定之 §K 註記一處；R-P3、A-PW07 雖經實測為假，**未改動一字**，僅上繳待裁 |

---

## 八、下一步

待 Pei 就 §5.4 之 Q1–Q4 裁決後，自步驟 2 續跑。
四份 xlsx 已驗明，可在裁定後立即入 `inputs/`。
