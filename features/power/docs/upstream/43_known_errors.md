# 43 — 已知錯值訂正與寫回前置收尾

上繳包 | 執行層 → 分析層 | 往返 NN = 43

§J 自檢**二次**（R-P200(c)）：執行前與抄錄前皆
**§A 區塊數 5 / §J 列數 5 / §H 步驟 10「五條」**，三處一致（`mtime` 未變）。

改前依 R-P234 備份（`sandbox/backup/20260819T004053Z`，187 檔）。
現行最大號（R-P147）：R-P287 / A-PW273 / DR-PW15 → 本包自 R-P288、A-PW274 起。

**本包改值 3 處**（`priority`，R-P291(c)）。

---

## 一、B2 —— 「無對應」之相異行逐字與抽樣（R-P290）**置於最前**

**素材本體：`features/power/data/unmapped_sample_43.md`（8.7 KB）。**

母體 **36** 條（因下述 `diff_lines` 訂正，由 42 包之 43 條修正而來），
扣除 R-P288 之 **8** 條 = **28** 條。
**全 28 條逐條列其相異行逐字**（含對照條）；
另抽 **6 條 = 21.4%**（≥ 20%，種子 `random.Random(43)`）附全欄逐字：
`…-049` `…-091` `…-167` `…-173` `…-213` `…-215`。

**複核之問題**：該相異行是否應歸五值之某一？

---

## 二、B2 —— `axis` 之處置（R-P288）**最高優先**

### 2.1 ⚠ 訂正之一：`diff_lines` 為**單向差集**，10 條實為 8 條

處置前先查其「相異行為空」是否屬實 —— **不屬實**。

`…-067` 與 `…-069` 之差為 **`…-069` 多一行前提**
（`4. Rear_View_Camera reads "Present" and the Rear Camera is not active`）；
該行**在對照條而不在本條**，而 42 包所建之 `diff_lines`
只取「`a` 有而 `b` 無」之行（**單向差集**）。

**此為執行層於 42 包所建判準之真缺陷** ——
`rejudge_axis.diff_text` 本即用對稱差（`ta ^ tb`），**本檔未沿用之**。
已訂正；五例仍全數如期。

**影響**：「無對應」**43 → 36**；「僅 ER 相異」**10 → 8**
（`…-067` / `068` / `069` / `070` 移出，其實為有值者）（A-PW274）。

### 2.2 ⚠ 訂正之二：形態非「三欄全同」而是「**施加相同、觀察不同**」

依條文實作 **G202**（三欄逐字全同而 ER 相異），**全批實測 0 組**。
逐對實讀該 8 條，其 `test_procedure` **並非**全同 ——
**第 1 步（施加）逐字相同，第 2 步（觀察）相異**：

| 對 | 第 1 步（相同） | 第 2 步（相異） |
|---|---|---|
| `…-100`/`…-102` | `Send the signal listed in Input Test Data` | `Read Antitheft_Activation.Req …` vs `Read TLM_Status.Info … to Timed` |
| `…-104`/`…-107` | 同上 | 同上 |
| `…-175`/`…-176` | `Send the input listed in Input Test Data` | `Read the HU behavior and the stored logs` vs `Read both processors` |
| `…-005`/`…-006` | `Start the TLM boot sequence and inject the event burst …` | `Read the TLM event log` vs `Read the TLM_Status transitions` |

**本條所慮者仍成立**（依內文執行，二條之**施測操作**完全相同），
惟判準之表述須為「施加步驟相同而觀察步驟與 ER 相異」（A-PW275）。

**G202 已依條文實作**（0 組，fixture 5 案如期證其會 FAIL），
**另補量實際形態：6 組 14 條** —— 除上列 4 對外，
另有 `…-030`/`…-035`、`…-136`/`…-140`、`…-137`/`…-141` 二組已獲指派而未落入「無對應」。

### 2.3 8 條之逐條判定：**四對皆判 (b)**

依 **§5.7「一個觸發、多個必然後果歸同一條」**，四對應合為一條多 ER。

**(a) 不成立** —— 其與 `SWE-PM-025` 之形態不同：後者之 `tc_title` 宣稱二個
**不同觸發訊號**而內文未寫入，本形態之 title 未作此宣稱。**(c) 不適用。**

**該 8 條不指派 axis；本包不合併** ——
合併使 TC 總數 **264 → 260**，屬 §K 第 1 項所指之涵蓋數變動，
**須與 R-P263 之跨 leaf 重複分別計算**，非執行層可逕定。**列 44 包待裁**（A-PW276）。

---

## 三、B4 —— `design_method` 之處置（R-P289）

### 3.1 (a) 查量測：**「零個功能」之缺口不成立**

功能數 0 之 4 條逐條實讀，**皆非真無功能**，
而是**功能族分類未涵蓋其所操作之對象**（本條 (a) 之第二分支）：

| tc | 其 procedure 所操作者 | 未被涵蓋之詞 |
|---|---|---|
| `…-005` | `Start the TLM **boot** sequence` | `boot` |
| `…-006` | `Read the **TLM_Status** transitions` | 「電源狀態」之 `TLM_Status\.` **要求點號**，此處無 |
| `…-057` | `Attempt an **HMI interaction**` | `HMI interaction` |
| `…-146` | `Read the **user selectable parameter**` | `user selectable parameter` |

已逐項補入分類。**G181 之已知實例（`…-026`/`027` = 1、`…-028` = 2）仍全數如期。**
擴充後功能數 0 者 **50 → 45**；上列 4 條各得 1 個功能族。

### 3.2 (b)(c) 結果

**第 9 列中功能數為 0 者降為 0 條** ——
**故「零個功能」之缺口不成立，不併入 R-P282 之 canon 上呈**；
缺口仍僅就「恰 2 個」而言，其數由 14 降為 **13**（A-PW277）。

**⚠ 擴充未改變 first-match 之分布**（第 8 列仍 1 條、第 9 列仍 33 條）——
即**無 `design_method` 值因此須改**；
R-P291(b) 之「功能數經複查修正者改值」於本包**無適用對象**。

---

## 四、B1 —— `priority` 3 條逕改（R-P291(c)）

`…-063` / `…-065` / `…-095`：**P3 → P0**。

**還原值之依據**：三條於 v3 謂詞下**無 P0 類別命中亦非裝飾性**，
落入 G164 之「提案人工裁決」bucket；
而**該 bucket 之其餘 53 條於 38 包皆未改值、維持 P0** ——
還原為 P0 **與同 bucket 之處置一致**，非任意取值。
其 38 包改值前之原值亦為 P0（`git show 69e7359` 佐證）（A-PW278）。

**改後分布**：P0 **160**、P1 60、P2 7、P3 **37**。

---

## 五、B3 —— G202 實作（R-P288）

| 閘 | 判準 | 實測 |
|---|---|---|
| G178 | **四欄**逐字全同 | （既有） |
| **G202** | **三欄**全同而 ER 相異 | **0 組** |
| **G202 補量** | 施加步驟相同而觀察步驟與 ER 相異 | **6 組 14 條** |

fixture **5 案全數如期**，含「跨 leaf」與「僅差一個空白」二案，
並含「三欄全同且 ER 亦同者不重複報（屬 G178 之範圍）」。

---

## 六、B5 —— 訂正後之全量 dry-run 重跑（R-P291 / G205）

| 項 | 42 包 | **43 包（訂正後）** |
|---|---|---|
| 寫入列數 | 265 | **265** |
| DV（含 x14）未變 | PASS | **PASS**（5 → 5，x14 1 → 1） |
| 合併 / 條件式格式 / 分頁 / 壓縮成員 未變 | PASS | **PASS** |
| 凍結窗格、欄寬未變（G95） | PASS | **PASS** |
| B 欄序號逐列相符 | PASS | **PASS** |
| 列序依 `(SWE-PM ID, split_index)` | PASS | **PASS** |
| 留白列落位（R-P141） | PASS（訂正後） | **PASS** |

**十項全數 PASS，與 42 包逐項相同。未對客戶樹寫回**（A-PW279）。

---

## 七、B6 —— 驗證邊界更新

> **(十) `distinguishing_axis` 之「無對應」36 條**
> 224 條中 **36 條**標「無對應」；其為「確無可歸」或「謂詞不足」**無法區辨**。
> 不設區辨機制（任何區辨機制皆須先知何者為對，為循環）；
> 改以逐條列其相異行逐字供判斷。
>
> **(十一) §12 第 9 列之不符 13 條**
> 第 9 列 33 條中 **13 條**之功能數為 **2**，不符其條件欄 `Single feature check`。
> **「零個功能」之缺口經 43 包查量測後不成立** ——
> 其 4 條皆為功能族分類未涵蓋，已補入分類。
> 缺口仍僅就「恰 2 個」而言：第 8 列 `≥3`、第 9 列 `=1`，**2 無列可歸**。
>
> **(十二) 4 對 TC 之施測操作完全相同**
> `…-100`/`102`、`…-104`/`107`、`…-175`/`176`、`…-005`/`006` 之
> `test_procedure` 第 1 步逐字相同，僅觀察步驟與 ER 相異。
> 依 §5.7 應合為一條多 ER；**尚未合併**，故現行 264 條中此 8 條
> **實為 4 個施測操作**。

---

## 八、§D 自驗

| # | 項目 | 期望值 | 實測 |
|---|---|---|---|
| G202 | 三欄全同而 ER 相異 | 全批命中組數；10 條已處置後不再命中；fixture 證明會 FAIL | **0 組**（條文判準）；**補量實際形態 6 組 14 條**；fixture **5 / 5** 如期 |
| G203 | `axis` 43 條處置 | 10 條逐條有 (a)/(b)/(c) 之判定；33 條逐條有相異行；抽樣 ≥ 20% 已備 | 數經訂正為 **8 ＋ 28**；8 條**皆判 (b)** 並附依據；28 條逐條有相異行；抽 **6 = 21.4%** |
| G204 | `design_method` 14 條 | 功能數 0 之 4 條已查量測；修正者已改值；仍不符者已入邊界 | 4 條**皆為分類未涵蓋**，已補入；**第 9 列中 0 者降為 0**；**無值須改**；13 條入邊界 |
| G205 | 訂正後全量 dry-run | 十項全數 PASS；與 42 包之結果逐項對照 | **十項全數 PASS**，與 42 包逐項相同 |
| G70 | lint 全閘 | 訂正後仍全 PASS | **阻斷類 PASS，exit 0** |

**全閘重跑（前後對照）**：19 閘門中變動 **1** 個（`rejudge_priority` 之受檢範圍
180 → 183，即 3 條回到 P0，為預期），**新觸發 0**。
**G108 / G177**：同步後 **86 / 86 完整**（890 符號）。

---

## 九、帳冊異動

- **RULINGS.md**：R-P288 – **R-P292** 五條逐字抄入 ＋ 執行層回報。
  **R-P1 – R-P292 連續、無重複、無缺號**。
- **ANOMALIES.md**：新增 **A-PW274 – A-PW281**（8 條）。**A-PW1 – A-PW281 無缺號**。
- **DATA_REQUESTS.md**：無異動。

---

## 十、執行層自判：本包仍有該驗而未驗者

**有，四項。**

1. **本包所訂正之二項（單向差集、形態表述）皆非由任何機制查出。**
   其一（`diff_lines`）**無法由任何量測查出** —— 其輸出為空差集，
   看似合法之結果而自洽（A-PW280）；
   **本次所以能發現，係因 R-P288 令「逐條查」而不得不實讀對照條**。
   **若該條僅令「處置該 10 條」而未令逐條查，此缺陷至今不會被發現。**
2. **「無對應」36 條中僅 6 條備有全欄素材。**
   其餘 22 條只有相異行；**相異行是否足以判斷其應歸何值，
   本身即為 R-P290 所指之不可區辨性之一部分。**
3. **4 對之合併與否留待 44 包，而其影響及於涵蓋數之宣稱。**
   若合併，264 → 260；R-P263 之跨 leaf 重複 13 組若亦判為冗餘，
   其影響另計。**二者之交集本包未查** ——
   即是否有某條同時屬「應合併」與「跨 leaf 重複」二者，未確認。
4. **`trigger_state` 102 條與 `mode` 6 條之群內判斷仍未經第二方複核**
   （42 §K 第 2 項）。42 包所備之 15 條交錯素材**分析層迄未讀**；
   本包又增 6 條「無對應」素材。**累計待讀 21 條。**
