# 38 — 第 8 列謂詞訂正與第二級改值

上繳包 | 執行層 → 分析層 | 往返 NN = 38

§J 自檢**二次**（R-P200(c)）：執行前與抄錄前皆
**§A 區塊數 6 / §J 列數 6 / §H 步驟 9「六條」**，三處一致（`mtime` 未變）。

**第二級改值 204 處**，改前依 R-P234 備份（`sandbox/backup/20260818T113858Z`，157 檔）。
現行最大號（R-P147）：R-P258 / A-PW229 / DR-PW15 → 本包自 R-P259、A-PW218 起。

---

## 一、B1 —— 第 8 列謂詞訂正（R-P259 / G181）**最高優先**

### 1.1 「功能」之界定（R-P250：先量再寫）

`test_procedure` 全批之子系統／訊號族詞頻實測：
`TLM` 137、`screen` 97、`HU` 90、`call` 55、`audio` 30、`display` 23、`antitheft` 23、
`theme` 19、`panel` 16、`logo` 15、`ignition` 11、`menu` 11、`phone` 11、`volume` 10、
`bus` 9、`CAN` 8、`AUD_LVL` 6、`ICS` 5、`camera` 5、`ETM` 4、`HVAC` 2、`backlight` 2、`LIN` 2。

**11 個功能族**：音訊輸出／電話／畫面顯示／品牌與主題／後視攝影機／防盜／
電源狀態／設定與選單／實體控制／HVAC／ICS 模組。

三項界定原則：
1. **`TLM` / `HU` / `ETM` 不計** —— 受測件本身，幾乎每條皆有，**不具區辨力**。
2. **`CAN` / `LIN` / `bus` 不計** —— **施測手段**而非被測功能。
3. **同族詞歸一** —— `audio` / `volume` / `AUD_LVL` / `speaker` / `mute` / `chime` 計為一。

### 1.2 已知實例之驗證（判定為條文給定，非執行層自擬）

| tc | 條文所判 | 實測 |
|---|---|---|
| `…-026` | 僅一個功能（逾時設定） | **1**：設定與選單 |
| `…-027` | 同上 | **1**：設定與選單 |
| `…-028` | 跨二個功能，未達三 | **2**：電話、音訊輸出 |

**三案全數如期。**

### 1.3 (b)(c) 命中數與續判

**第 8 列 7 → 2**（`…-007`、`…-056`）。
功能數分布：0 個 **50**、1 個 **124**、2 個 **68**、3 個 **20**、4 個 **2**；
獨立命中（≥ 3）**22** 條，first-match 後餘 2。

| tc | 新落點 |
|---|---|
| `…-007` | **維持第 8 列** —— 跨 3 個功能：ICS 模組、電源狀態、音訊輸出 |
| `…-008` `…-016` `…-025` `…-026` `…-027` `…-028` | 第 9 列（`…-026` / `…-027` 另依 R-P260(a) 改判第 4 列） |

落底 86 → **90**。

---

## 二、B3 —— G154 漏檢查證（R-P260 / G183）**未漏**

`NO_TRANSITION_RE` 現行即含 `remains? in`。語料中 `remains in` 共 **5** 條
（`…-028` / `031` / `032` / `034` / `035`），**五條皆為 G154 命中**。

`…-028` 確在 G154 之 **26 條**清單內 —— `data/g155_design_method_rejudge.md`
第 19 行逐字：`| 028 | remains in | 3. Both calls were served and the TLM remains in Timed state |`；
其第 52 行之裁決清單**早已提案第 9 列**，與 R-P260(b) 一致。

**故 G154 無漏檢。** 真正之缺口在**其產出未被套用** ——
該提案自 **33 包**存在，歷 34–37 包五包未改值（A-PW210），至本包方落實。
**閘門有效而結果被擱置，與閘門失效在後果上難以分辨**（A-PW219）。

---

## 三、B2 —— 第二級改值（R-P261 / G182）

`git diff --stat`：**204 insertions / 204 deletions**（序列化已驗位元組還原）。

### 3.1 改值前後之分布

**`design_method`**

| 值 | 前 | 後 |
|---|---|---|
| 狀態轉換 (State Transition Testing) | **253（95.8%）** | **91（34.5%）** |
| 功能測試 (Functional based ; no specific technique) | 1 | **88** |
| 決策表 (Decision Table Testing) | 9 | **84** |
| 基礎故障注入 (Fault Injection Lite) | 1 | **1** |

**單一值集中度 95.8% → 34.5%**，三大值 91 / 88 / 84 近乎均勻 ——
**R-P236 所慮之「翻為另一方向之一致偏向」未發生**（A-PW229）。

**`priority`**

| 值 | 前 | 後 |
|---|---|---|
| P0 | **193（73.1%）** | **157（59.5%）** |
| P1 | 63 | 63 |
| P2 | 8 | 4 |
| P3 | **0** | **40** |

### 3.2 ⚠ 乾跑中攔下一項回歸

首次乾跑得 `基礎故障注入` **1 → 0**：
`…-008` 經 **R-P239 明裁「成立，維持」**，卻因第 2 列謂詞之**已知偽陰性**
（A-PW178：其措詞為 `Stop the broadcast`）未被命中，
**一個經明文裁定為正確之值被機械結果覆寫**。

已列入逐條裁定並自備份還原後重跑。
**通則性教訓：以 first-match 全量改值時，凡曾經明文裁定之值皆須列為逐條裁定**，
否則裁定會被謂詞之已知缺陷抹除（A-PW220）。

### 3.3 ⚠ 13 條因標籤缺漏而未改（停並回報）

§12 九列之標籤僅**四個**見於本語料（第 2 / 3 / 4 / 9 列）；
**第 1、5、6、8 列與「矛盾」無既有標籤**，§12 之完整標籤表不在本庫。
依 34 包 G167 / 36 包 §4.6 之先例，**不自行擬定 canon 值**。

| 應為 | 條數 | tc |
|---|---|---|
| 第 1 列 | 1 | `…-024` |
| 第 5 列 | 2 | `…-015`、`…-222` |
| 第 6 列 | 6 | `…-010`、`…-011`、`…-259`、`…-260`、`…-261`、`…-262` |
| 第 8 列 | 2 | `…-007`、`…-056` |
| 矛盾 | 2 | `…-034`、`…-071` |

**待分析層提供該五類之標籤逐字。**

### 3.4 ⚠ `priority` 受檢範圍收斂

對全 264 條套用得 **45** 條，條文載「40 條」——
差額 5 條（`…-156` / `157` / `200` / `201` / `226`，皆 P1）**不在 G164 之受檢範圍**
（全部 P0 ＋ Branding and Theme = 201 條）內。
**依條文範圍收斂為 40 條**，逾範圍者列出供裁定，本包不改（A-PW228）。

### 3.5 G182 —— 全閘重跑（前後對照）

**17 個閘門中 15 個數值全同**；變動 2 個：

| 閘門 | 變動 | 判定 |
|---|---|---|
| `rejudge_priority` | 受檢範圍 201 → 180；P3 提案 40 → 0 | **預期**（提案已套用） |
| `audit_design_method`（G150） | exit 0 → **1**（`KeyError`） | **新觸發 1 個** |

**新觸發之查明**：G150 以硬編之 `WALK` 表（32 包**人工**走查結果）逐條比對；
改值後母體改變，抽樣取到未走查之 TC（`…-234` / `…-245`）。
**非資料缺陷，而是人工走查結果之時點相依性** —— 依 R-P214 不可機械重生，
故**未補寫該表**，改為明確標示「未走查」並回報其數（本次 **2 / 4**），
並載明其不符率**自此僅對交集有效**（A-PW222）。

### 3.6 限度（條文所令之明載）

改值本身無閘門驗其正確；分析層複核涵蓋率為**第 4 列 2 / 14、第 8 列 3 / 7**。
G182 只證「未變壞」，不證「已改對」——
**164 條 `design_method` 新值中，僅第 8 列 3 條與第 4 列 2 條經第二方讀過。**

---

## 四、B5 —— 跨 leaf 重複之逐組判定（R-P263 / G184）

判準：二 leaf 之 `source_anchor` 是否相同。

| 型 | 組數 | 組 |
|---|---|---|
| **(a) 共用錨點 → 維持（R-P186）** | **8** | `152`/`196`、`153`/`197`、`154`/`198`、`155`/`199`、`156`/`200`、`157`/`201`、`165`/`229`、`168`/`230` |
| **(b) 錨點不同而內容相同** | **5** | `100`/`104`、`101`/`105`、`102`/`107`、`235`/`250`、`236`/`251` |

### 4.1 ⚠ (a) 之 `tc_title` 未改 —— 停並回報

條文令加入「其 leaf **所側重之語**」。**實測：無任何側重之別可資取用。**

`SWE-PM-054` / `SWE-PM-101` 之 `section`（皆 `1.6.2.1.16`）、
`source_anchor`（皆 `4941673`–`4941676`）、`source_clause`（**逐字相同**）三者全同；
二 TC 之 `test_set` 亦皆為 `Startup Display`。唯一相異者為其**批次**，而批次非語義。

**更關鍵者**：該五對正是 **DR-PW12（Medium，live）**之標的 ——
其逐字問上游「該五對是否為 037 之重複登載，抑或各自代表相異之 SW 需求」，**RD 尚未答覆**；
二 leaf 之 `reasoning` 亦自載「重疊登記（A-PW137 / R-P186）」。

**若由執行層造出區別語，即等同代 RD 回答「二者為相異需求」** ——
正是 §8.2.2 與 R-P186 所禁者。**故不改**；
覆核者難以分辨之問題屬實，**惟其解在 DR-PW12 之回覆，不在 TC 作者之措詞**（A-PW226）。

### 4.2 (b) 之成因，分二類

**(b-1) `SWE-PM-028` / `029`**（`100`/`104`、`101`/`105`、`102`/`107`）——
二 leaf 之 `source_clause` **相異**（相似度 0.75）：
`028` 獨有 `or ( If Auto_SwitchOn_Setting.Req =="Active ", when Timeout1 == 00 MIN" for LTM High Radio…`；
`029` 獨有 `IF SwitchOff_Timeout_Setting.Req == 00 min THEN TLM has to set Timeout1 to the value speci…`。
**成因：TC 只寫了共同部分，未就各自獨有之子句分化**（A-PW224）。

**(b-2) `SWE-PM-080` / `086`**（`235`/`250`、`236`/`251`）——
`source_clause` **逐字相同**而 `source_anchor` **相異**（`4942017` vs `4942041`）。
**成因：同一段規格文字登載於二個不同錨點** ——
與 DR-PW12 同型，**惟 DR-PW12 以「錨點相同」為條件掃出，本形態不在其涵蓋內**，
為一項新發現（A-PW225）。

### 4.3 涵蓋數

(a) 型不下修（待 DR-PW12）；(b) 型**本包不改值，裁定於 39 包**。
(b-1) 若補寫分化則涵蓋數不變；(b-2) 若確為重複登載則其一為冗餘。

---

## 五、B4 —— `axis` 複核素材備料（R-P262）

`data/axis_review_38.md`（39 KB，種子 `random.Random(38)`）：

| 群 | 母體 | 抽樣 | 率 |
|---|---|---|---|
| `input_data` | 92 | **16** | 17.4% |
| `trigger_state` | 68 | **12** | 17.6% |
| `mode` | 42 | **8** | 19.0% |

每條逐字呈現：`tc_title` / 執行層之依據 / 對照條 / **四欄 token 差** /
**每一相異欄位之本條與對照條全文**。不作判定、不作摘要。

**「無對應」40 條之三個選項與其後果**（只呈不裁）：

| 選項 | 後果 |
|---|---|
| **新增列舉值**（如 `single`） | 須改 §4.6，**屬 canon 層**，影響全部 feature；語義正確、不犧牲既有契約 |
| **以 `none` 表之** | **與 `none` ⇔ `duplicate_of` 雙向契約衝突** —— 該 40 條並非重複，設 `duplicate_of` 即為不實；不設則 C4 觸發 40 次 |
| **留空** | 違反 C2（`axis` 非空字串），40 條觸發；且「留空」與「未填」無從分辨 |

**本包未改 `axis` 之任何值。**

---

## 六、B6 —— R-P256 加註（R-P264）

原文不改，加註指向 R-P264。
原文 SHA256 `9bc651a2bce18f635946af088a607f51`，**加註前後同一（UNCHANGED）**。
`…-015` 維持未動，其不一致依 A-PW199 由對帳表側查明。

---

## 七、§D 自驗

| # | 項目 | 期望值 | 實測 |
|---|---|---|---|
| G181 | 第 8 列功能數謂詞 | 界定已量語料並附三實例；命中 7 → ? | 11 個功能族，詞頻實測已列；**三實例全數如期**；**7 → 2** |
| G182 | 第二級改值後全閘重跑 | 全閘重跑；新觸發數；前後分布已回報 | **17 閘門 15 個數值全同**；**新觸發 1 個**（G150，已查明並處置）；分布見 §三 |
| G183 | G154 漏檢 | `…-028` 是否曾觸發；未觸發者之成因與影響面 | **曾觸發** —— 在 26 條清單內（報表第 19 行）；**G154 未漏**，缺口在其產出被擱置五包 |
| G184 | 跨 leaf 重複逐組判定 | 13 組皆判 (a)/(b)；(a) 之 `tc_title` 已改且不再逐字相同 | 13 組**皆已判**（(a) 8 / (b) 5）；**(a) 之 `tc_title` 未改** —— 無語義材料且屬 DR-PW12 之待答事項，理由見 §4.1 |
| G70 | lint 全閘 | 改值後仍全 PASS | **阻斷類 PASS，exit 0** |

**G108 / G177**：同步後 **72 / 72 完整**（797 符號）。全腳本語法 72 檔、錯誤 0。

---

## 八、帳冊異動

- **RULINGS.md**：R-P259 – **R-P264** 六條逐字抄入 ＋ 執行層回報；
  **R-P256 依 R-P36 加註**（SHA256 UNCHANGED）。
  **R-P1 – R-P264 連續、無重複、無缺號**。
- **ANOMALIES.md**：新增 **A-PW218 – A-PW229**（12 條）。**A-PW1 – A-PW229 無缺號**。
- **DATA_REQUESTS.md**：無新增（惟 A-PW225 之形態為 DR-PW12 未涵蓋者，列 39 包考慮）。

---

## 九、執行層自判：本包仍有該驗而未驗者

**有，四項。**

1. **164 條 `design_method` 新值中僅 5 條經第二方讀過**（第 8 列 3、第 4 列 2）。
   其餘 159 條之正確性僅依 first-match 謂詞，而該謂詞於本包內即被查出
   **第 8 列用錯條件欄**（R-P259）—— **同型錯誤在其他列尚未被查**。
   §12 之逐列條件欄與 tie-break 是否還有其他被混用者，**本包未逐列核對**。
2. **`…-026` / `…-027` 之裁定與機械結果相異，執行層依裁定改值而未追究其分歧。**
   R-P260(a) 判其為第 4 列（二條件共同決定），而 `substantive_conditions` 只數到 1 項 ——
   **該分歧顯示第 4 列之代理判準可能系統性低估**，
   惟本包未量其影響面（第 4 列現為 84 條，若代理判準低估則實際應更多）。
3. **G150 之 `WALK` 交集自本包起持續縮小。** 本次 2 / 4 未走查；
   往後每次改值皆會再縮，**該閘門之有效性將逐步歸零而不會報錯** ——
   與 A-PW219 之「閘門有效而結果被擱置」互為鏡像。
4. **(b-2) 之形態（錨點不同而內容相同）全批未掃。**
   本包僅由 G178 之 13 組中辨出 2 組，**未反向以 `source_clause` 逐字相同為條件全批掃描** ——
   若另有 clause 相同而 TC 不同者，現行無任何機制查之。
