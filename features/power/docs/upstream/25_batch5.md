# 上繳包 25 —— 批次定義訂正、全量對帳與第五批

> 對應下放包：`features/power/docs/handoff/25_batch5.md`
> 執行層：Claude（TC_Generator）
> **§J 自檢已先驗**：§A fenced block = **6**（R-P177–R-P182 連續無缺）、
> §J 列數 = **6**、§H 步驟 8 = 「**六條**」—— **三處一致，未停。**
> 本包**未執行任何 git 子命令**（R-P149）；**未對任何 workbook 呼叫 `save()`**；
> **未觸碰客戶樹與 `inputs/`**；
> **未以 SWE-PM ID 區間定義任何批次範圍**（R-P177(b)）；
> **未重做第二批已產出之六個 leaf**（R-P177(a)）；
> **未自行合併或拆分 `SWE-PM-025` 之八條**（R-P179）；
> **G121 完成後方啟動第五批**（R-P181）；
> **未自行調整 §E**；**未以「事前未知缺口 0」作為品質證據**（R-P180）。

**G0 前置閘：7 / 7 素材 SHA256 相符 —— PASS。**

**編號查核（R-P147，先查後開）**：
**A-PW 最大 132、R-P 最大 176、DR-PW 最大 11、閘門最大 G120。**
本包新號自 **A-PW133 / R-P177 / G121** 起；
**新開 DR-PW12（Medium）** —— 執行層 Tier 1 登記，見 §五。

---

## 一、B1 —— G121 全量對帳表（R-P178）

腳本 `scripts/build_reconciliation.py`；產物
`data/leaf_batch_reconciliation.tsv`（逐 leaf 一列）＋ `data/g121_reconciliation.md`。

### 1.1 各 Test Set 合計 —— **PASS**

| Test Set | §E 定版 | 實測 | 相符 |
|---|---|---|---|
| Power State | 63 | **63** | 是 |
| Startup Display | 24 | **24** | 是 |
| Branding and Theme | 16 | **16** | 是 |
| Timeout Settings | 8 | **8** | 是 |
| Power Down | 3 | **3** | 是 |
| **合計** | **114** | **114** | 是 |
| ＋ `SWE-PM-089`（R-P141 留空）| 1 | 1 | 是 |
| **總計** | **115** | **115** | 是 |

**五個 Test Set 全數相符，未觸及 §E。**

### 1.2 產出狀態（本包產出第五批**之前**之基準）

| Test Set | leaf | 已產出 | 未產出 | 其中受阻斷 | 未產出且未阻斷 |
|---|---|---|---|---|---|
| Power State | 63 | 43 | 20 | 10 | **10** |
| Startup Display | 24 | 4 | 20 | 0 | **20** |
| Branding and Theme | 16 | 0 | 16 | 0 | **16** |
| Timeout Settings | 8 | 8 | 0 | 0 | 0 |
| Power Down | 3 | 3 | 0 | 0 | 0 |

### 1.3 **R-P178 之推導有誤 —— 實測 10，非 6**

§K 第 1 項明令「不得僅訂正數字」，故連同推導過程檢討。**二處錯誤，同一根因**：

| 推導語句 | 本條所載 | 實測 | 差異來源 |
|---|---|---|---|
| 「`001`–`063` 中 6 個屬 Timeout Settings，故 **57** 屬 Power State」 | 57 | **53** | 該區間內非 Power State 者實為 **10** —— 6 個 Timeout Settings ＋ **4 個 Startup Display**（`053`–`056`）|
| 「第三批 22 ＋ 第四批 **25** ＋ 阻斷 10 = 57」 | 已產出 47 | **43** | 第四批 25 leaf 中僅 **21** 屬 Power State，另 4 屬 Startup Display |
| 「63 − 57 = **6**」 | 6 | **10** | 63 − 43 − 10 = **10** |

**根因即 R-P177 所訂正之錯誤本身 —— 以 ID 區間推定 Test Set 成員資格。
該錯誤於立下 R-P177 之同一包內，在 R-P178 之推導中再犯一次。**

**方向結論成立**：區間內帳目 `10 阻斷 ＋ 22 第三批 ＋ 21 第四批 = 53`，
區間內未觸及者 **0**；10 個未觸及者之 ID **全部大於 063**。

---

## 二、B2 —— R-P174 加註（R-P177 / G122）

依 R-P36 加註，**原文一字未改**：

| | SHA256 | bytes |
|---|---|---|
| 加註前 | `3683828ff411a6c28e6f34421419af00527b9b8e26949973bbd3884746bbcf75` | 755 |
| 加註後 | `3683828ff411a6c28e6f34421419af00527b9b8e26949973bbd3884746bbcf75` | 755 |

**G122 = UNCHANGED。**

---

## 三、B3 —— `SWE-PM-025` 觸發訊號原文（R-P179）

產物 `data/b3_swepm025_triggers.md`。**執行層不作判斷、未合併、未拆分。**

### 3.1 三對之逐字並列

| 對 | `4941569` / `4941570` / `4941571` 側 | `4941572` / `4941573` / `4941574` 側 |
|---|---|---|
| 一 | `Front_Panel_OnOff.Req has a transition from “Not_Pressed” value to “Pressed” value` | `CLIMATIC_PANEL.Radio_Btn0 has a transition from “Not_Pressed” value to “Pressed” value` |
| 二 | **不含觸發訊號** —— `In this case, IF user accepts, TLM shall set …` | **與左側逐字相同** |
| 三 | **不含觸發訊號** —— `IF Phone_Call.Info == Not_ActiveTLM has to stop …` | **與左側逐字相同** |

**即：三對之中僅第一對含觸發訊號；第二、三對為其後續行為，二側一字不差。**

### 3.2 中性證據（不作判斷）

| 項 | 值 |
|---|---|
| `Front_Panel_OnOff.Req` 出現之錨點數 | **13** |
| `CLIMATIC_PANEL.Radio_Btn0` 出現之錨點數 | **10** |
| 二者同時出現於同一錨點者 | **0** |
| 章節分布 | 二者相同（§1.6.2.1.15、§1.6.7.1）|

**證據方向已於產物中寫明，以免誤讀**：

- 若二者曾**於同一錨點內並列**，即為 (a) 之**強證據** —— 本次為 0，故無此證據。
- **「從未共現」傾向 (b) 之弱證據** —— 同一訊號之不同稱法本就不會被並列書寫。
- **惟「從未共現」亦與 (a) 相容** —— 二個相異訊號分屬不同硬體來源，各自成段亦屬常態。

**故本節不足以獨立裁定。裁定於 26 包。**

---

## 四、B4 —— 第五批（R-P181 / G123）

### 4.1 範圍 —— **leaf ID 全集逐一列出（R-P181(e)，不以區間表述）**

範圍**取自 G121 對帳表之 Test Set 成員清單**；
`build_b5_material.py` 與 `gen_batch05.py` 皆自
`leaf_batch_reconciliation.tsv` 讀取，**程式碼中無任何 ID 區間**。

**納入 —— 29 leaf**

- Power State（10）：`SWE-PM-066`、`SWE-PM-067`、`SWE-PM-068`、`SWE-PM-069`、
  `SWE-PM-070`、`SWE-PM-076`、`SWE-PM-095`、`SWE-PM-103`、`SWE-PM-114`、`SWE-PM-115`
- Startup Display（19）：`SWE-PM-074`、`SWE-PM-075`、`SWE-PM-093`、`SWE-PM-094`、
  `SWE-PM-097`、`SWE-PM-098`、`SWE-PM-099`、`SWE-PM-100`、`SWE-PM-101`、`SWE-PM-102`、
  `SWE-PM-104`、`SWE-PM-105`、`SWE-PM-106`、`SWE-PM-107`、`SWE-PM-108`、`SWE-PM-109`、
  `SWE-PM-110`、`SWE-PM-111`、`SWE-PM-113`

**排除 —— 15 leaf，逐一列明理由**

| leaf | Test Set | 理由 |
|---|---|---|
| `SWE-PM-053`、`SWE-PM-054`、`SWE-PM-055`、`SWE-PM-056` | Startup Display | 已於第 004 批產出（**R-P177(a) 不重做**）|
| `SWE-PM-112` | Startup Display | **撞上 live DR 影響面：DR-PW9**（其範圍歸屬未定）—— R-P165 / R-P181(c) |
| `SWE-PM-001`–`SWE-PM-009` | Power State | 受阻斷：**DR-PW6**（`008` 另受 DR-PW11）|
| `SWE-PM-010` | Power State | 受阻斷：**DR-PW11** |

### 4.2 素材前置檢查（R-P172）

29 leaf 之全部 item 於 CFTS 文字層**皆有內文**，無 R-P144(b) 之阻斷情形。

### 4.3 產出

| 項 | 值 |
|---|---|
| 檔案 | `features/power/generated/batch_005_startup_display.json` |
| leaf | **29** |
| TC | **66**（臨時號 `158`–`223`，接續第四批之末 `157`，遞增無跳號）|
| Test Set | Startup Display 51 / Power State 15（逐 leaf 符對帳表，G40 PASS）|
| priority | P0 48 / P1 18 |
| `spec_reference` | **66 / 66 指向 CFTS009** |

**G94 87 / 87 逐字相符、G99 87 / 87 相等、G103 87 / 87 相等、lint 阻斷類 PASS（exit = 0）。**

---

## 五、第五批之二項素材發現（執行層 Tier 1 登記）

### 5.1 五對 leaf 之 `source_anchor` 完全相同（A-PW137 / **DR-PW12**）

全量掃描：238 個被引用錨點中 **28 個被 >1 個 leaf 引用**。其中五對之錨點集合**完全重合**：

| 本批 leaf | 對造 | 共用錨點 | 對造之批次 |
|---|---|---|---|
| `SWE-PM-097` | `SWE-PM-056` | `4941680` | 第四批 |
| `SWE-PM-101` | `SWE-PM-054` | `4941673`–`4941676` | 第四批 |
| `SWE-PM-102` | `SWE-PM-055` | `4941678` | 第四批 |
| `SWE-PM-114` | `SWE-PM-068` | `4941876` | **本批** |
| `SWE-PM-115` | `SWE-PM-070` | `4941878` | **本批** |

屬性比對**不適用** —— 二側為**同一個錨點**，屬性必然相同。

**處置**：比照 R-P167 對 `SWE-PM-025` 之處置 ——
**未合併、未省略**，依 037 之 leaf 母體逐一產出以維持追溯，
於 `reasoning` / `reasoning_note` 標記；**已開 DR-PW12（Medium）**。
**是否應改依 §8.2.1 委由對造承擔（將減少約 9 條），呈請裁定於 26 包。**

### 5.2 `SWE-PM-093` 之二錨點逐字相同而屬性相異五欄（A-PW138）

`4941301`（§1.3.5）與 `4941941`（§1.9.8）**內文逐字相同**，
以 R-P173(a) 之集合正規化比對後**仍相異五欄**：

| 屬性 | `4941301` | `4941941` |
|---|---|---|
| ECU | `ETM, LTM, RRM` | `ETM, LTM` |
| EE Architecture | `Atlantis High, PowerNet, CUSW, Atlantis Mid` | `All` |
| Model Year | `Default` | `2024, 2025, 2022, 2023` |
| Radio | `allSys` | `R1H, R1L-R, R1M, R1L` |
| State | `New` | `Under Review` |

**為 R-P136 所定之「屬性相異 → 停並上繳」形態。**
二者之 ECU **皆含 `LTM`**、Radio **皆涵蓋本專案**（`allSys` / `R1L`），
故適用性未變 —— 比照 R-P135 之處置**產出 TC、未合併亦未拆分**。
`source_clause` 因而含同一段落二次（G94 仍逐字相符）。
**是否為變體登載，呈請裁定於 26 包。**

---

## 六、G124 —— 第五批之反向涵蓋 ＋ G113

### 6.1 G113 分桶（R-P171）

| 範圍 | 分支 | `規格未定義該支` | `已由他條或他 leaf 涵蓋` | `真缺口` |
|---|---|---|---|---|
| 全量（五批）| **74** | 27 | 47 | **0** |

**第五批之真缺口歷程（依 R-P182 並陳各版數值）**：

| 版 | 真缺口 | 分支總數 | 說明 |
|---|---|---|---|
| 初判（機械）| **6** | 76 | `093` 四列、`105` 一列、`110` 一列 |
| 逐項裁決 ＋ 補測後 | **2** | 76 | 5 個為真缺口已補 **9 條**；`110` 判為措詞不完整非缺口，已補齊前提之逐字標記名 |
| 訂正分桶腳本後 | **0** | **74** | 見下 |

**訂正之理由（不得以「改後通過」為由，故寫成結構性依據）**：
分桶腳本之 `_strip_connectives()` 初版以**無邊界之 `re.sub`** 剝除連接詞，
致 `Door_Ajar_Status` 之 `oor` 內之 `or` 被當作連接詞剝掉，
殘餘 `Do _Ajar_Status` **永不可能見於任何 TC**，**憑空虛構出一個真缺口**
（`DOOR_OFF` 亦同型）。已改為三種明確之剝除位置
（獨立詞元／小寫後之大寫黏連／字串起首之大寫黏連）並**迭代至不動點**。
**此為分詞器之錯誤分類（同 R-P169 之判別），非門檻調整。**

### 6.2 逐項裁決之真缺口（已補測 9 條）

| leaf | 分支 | 裁決 | 補測 |
|---|---|---|---|
| `SWE-PM-093` | 模式變更至 **TIMED MODE** 取消動畫（OR 右支）| **真缺口** | 補 1 條 |
| `SWE-PM-093` | 車門 **OPEN** 時之模式變更**略過**動畫 | **真缺口** | 補 1 條 |
| `SWE-PM-105` | 例外清單之 **FOTA pop ups** 等類別 | **真缺口** | 補 5 條（incoming / outgoing / climate / SOS 與 Assist / FOTA）|
| `SWE-PM-099` | 「新的一天」之**跨越午夜**與**時區／DST 自動調整** | **真缺口**（透鏡 1 捕獲）| 補 2 條 |
| `SWE-PM-110` | `$Country_Code$` 標記名 | **非缺口** —— 分支已由 TC 覆蓋，缺者為前提措詞未逐字引標記名 | 補齊措詞 |

### 6.3 反向涵蓋三透鏡

透鏡 1 之「無對應」補測後餘 **3 項**，逐項裁決皆**非缺口**：

| leaf | 項 | 裁決 |
|---|---|---|
| `SWE-PM-074` | `See HMI for pop-up details` | **交叉參照**，非可獨立驗證之行為 |
| `SWE-PM-105` | `See HMI logic and Flow "Startup" requirements for details` | **交叉參照**，同上 |
| `SWE-PM-105` | 例外清單整句 | **透鏡 1 之粒度限制** —— 該句被切為一個行為項而由 8 條 TC 分擔，逐類別皆有對應 TC（證據見 §6.2）|

### 6.4 盲測指標（R-P180）

**第五批之「事前未知缺口數」標為「不適用」，不記為 0。**
G113 於本批撰寫時**已在手**，OR 兩支被當成拆分軸
（`069` / `074` / `093` / `110` / `111` 皆按 OR 分支各成條）。

**惟本批之覆核仍捕獲撰寫者漏掉之 5 個真缺口並補測 9 條** ——
證據方向與第四批相反：**工具在手仍漏，「已在手」不等於「該形態已被窮盡」。**

---

## 七、§D 全表自驗

| # | 項目 | 期望值 | 實測 | 判定 |
|---|---|---|---|---|
| G121 | 全量對帳表 | 各 Test Set = 63 / 24 / 16 / 8 / 3；＋ `SWE-PM-089` = 115 | **63 / 24 / 16 / 8 / 3**，總計 **115** | **PASS** |
| G122 | R-P174 加註後原文位元組未變 | UNCHANGED | `3683828f…`，755 bytes，前後相同 | **UNCHANGED** |
| G123 | 第五批產出 | 【實測填入】leaf ID 全集、TC 數、排除清單與 DR 編號 | **29 leaf / 66 條**；全集見 §4.1；排除 15 leaf（DR-PW6 / DR-PW11 / DR-PW9 / R-P177(a)）| **已填** |
| G124 | 第五批反向涵蓋 ＋ G113 | 【實測填入】三桶計數、真陽性率；盲測指標依 R-P180 標示 | 全量三桶 **27 / 47 / 0**；本批真缺口 **6 → 2 → 0**（補測 9 條）；**盲測指標＝不適用** | **已填** |
| G70 | lint 全閘 | 全 PASS | 223 TC，**阻斷類 PASS，exit = 0** | **PASS** |

**無 MISMATCH。**

補驗（非 §D 所列，一併回報）：**G0 7 / 7**、**G94 87 / 87**、**G99 87 / 87**、
**G103 87 / 87**、**G108 7 / 7 完整**、**G113 self-test 5 / 5**、**G38 連號遞增無跳號**。

**Phase 4 累計：五批 87 leaf / 223 條。**

---

## 八、§F Anomaly 異動

**A-PW133 ~ A-PW138**，已入 `ANOMALIES.md`（A-PW01 – A-PW138 連續無缺）：

| 號 | 摘要 |
|---|---|
| A-PW133 | 分析層以 ID 區間定義批次範圍，二次致錯（R-P177）|
| A-PW134 | R-P178 之推導錯誤（6 → 實測 10），**根因即 R-P177 所禁者，同包內再犯** |
| A-PW135 | 分析層未經查證即書寫斷言第五次（R-P179）|
| A-PW136 | 盲測指標於工具進入撰寫階段後失效（R-P180）|
| A-PW137 | **五對 leaf 共用完全相同之錨點**（新開 DR-PW12）|
| A-PW138 | **`SWE-PM-093` 二錨點逐字相同而屬性相異五欄**（R-P136 形態）|

---

## 九、§G DATA_REQUESTS

DR-PW9（High）、DR-PW10（Medium）、DR-PW11（High）沿用。
**新增 DR-PW12（Medium）** —— 執行層 Tier 1 登記（A-PW137）。
下放包 §G 載「無新增」，係在 §五之發現作成之前；**據實回報**。

---

## 十、裁決條文與台帳

**§A 六條逐字抄入 `RULINGS.md`**（逐條以字串比對確認與 §A 之 fenced block 相同），
各附執行層回報。`RULINGS.md` 現為 **R-P1 – R-P182 連續無缺**。
「待裁」節已更新。§F 六項已入 `ANOMALIES.md`。

---

## 十一、執行層自判：本包仍有該驗而未驗者

**有，四項。**

1. **`SWE-PM-112` 之排除是執行層的判斷，不是條文的明文。**
   R-P181(c) 令「依 R-P165 扣除 live DR 影響面」，而 DR-PW9 於
   `DATA_REQUESTS.md` 中記為「不阻斷已產出之 33 leaf」——
   **它從未說「阻斷 `SWE-PM-112`」**。執行層讀 R-P165 之「撞上者排除」
   而將其排除，取保守解。**若裁定認為該 leaf 應納入，本批即少一個 leaf**，
   而這個判斷沒有第二人看過。

2. **五對共用錨點之處置（逐一產出）同樣是執行層選的。**
   另一條路是逕依 §8.2.1 委由對造承擔，那會少約 9 條。
   選擇產出的理由是「缺一條 TC 比多一條危險」（R-P121 之教訓），
   **但這是類比，不是本案之裁決。**

3. **本包第三次以「分詞器錯誤分類」為由修改自己的量測腳本。**
   24 包已記過一次（該次三度）。理由確為結構性、三個數字也都列了，
   **但「這是 bug 不是遷就」仍然是被修改的人自己說的。**
   值得注意的是：這次的 bug **會虛構缺口**（方向對執行層不利），
   前次的 bug **會虛增缺口**（方向亦不利）——
   二次皆非「往好看的方向」，惟此為事後觀察，非機制保證。

4. **第三、四、五批合計 180 條，分析層一條都沒看過。**
   §K 第 2 項已自判為「連續四包之積欠」，本包使其成為**五包**。
   閘門全綠已數次被證明不代表品質（A-PW64 / A-PW92 / A-PW114），
   而本批之 5 個真缺口正是撰寫者自己漏掉、由工具事後抓回的 ——
   **工具抓得到分支缺漏，抓不到語義錯誤。**
