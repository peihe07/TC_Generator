# 上繳包 31 —— design_method 分布、分層取樣與陳舊產物全掃

> 對應下放包：`features/power/docs/handoff/31_design_method.md`
> 執行層：Claude（TC_Generator）
>
> **§J 自檢二次皆一致（R-P200(c)）**：開工時與抄錄條文前皆為
> §A block **5** / §J 列數 **5** / §H 步驟 9「**五條**」；
> 檔案 **11709 bytes、mtime 08-18 16:34:04 均未變**。
>
> 本包**未執行任何 git 子命令**（惟依 R-P225 之重跑毀損二檔後以 `git restore` 還原，見 §三）；
> **未變更任何 `design_method` 之值**（R-P223(c)）；**未節錄 B2 取樣之任何欄位**；
> **未僅以逐位元組比對認定產物非陳舊**；**未以 R-P221 為由結案 `SWE-PM-064`**（sibling 明文已尋得）。

**G0 前置閘：7 / 7 素材 SHA256 相符 —— PASS。**
**編號查核（R-P147）**：R-P 最大 **220**、A-PW 最大 **159**、DR-PW 最大 **15**、閘門最大 **G149**。
本包新號：**R-P221–R-P225**、**A-PW160–A-PW164**、**G150–G153**。**DR 無新增**（R-P221(b) 不適用）。

---

## 一、B1 —— `design_method` 分布與 §12 走查（R-P223 / G150）**最高優先**

### 1.1 全批分布（264 條）

| design_method | 條 | 佔比 |
|---|---|---|
| 狀態轉換 (State Transition Testing) | **253** | **95.8%** |
| 決策表 (Decision Table Testing) | 9 | 3.4% |
| 功能測試 (Functional based) | 1 | 0.4% |
| 基礎故障注入 (Fault Injection Lite) | 1 | 0.4% |

### 1.2 逐 Test Set

| Test Set | 條 | 最大值 | 佔比 | ≥ 60% |
|---|---|---|---|---|
| Power State | 128 | 狀態轉換 | **100%** | 是 |
| Startup Display | 59 | 狀態轉換 | **100%** | 是 |
| Branding and Theme | 34 | 狀態轉換 | **100%** | 是 |
| Timeout Settings | 26 | 狀態轉換 | **100%** | 是 |
| **Power Down** | 17 | 決策表 | 52.9% | 否 |

**唯一分布多樣者為第一批（Power Down）** —— 其後五批幾近全為狀態轉換。

### 1.3 走查 —— **不符率 60.5%**

抽樣 **43 / 247 = 17.4%**（種子 `random.Random(31)`，率 ≥ 16.7%）；
§12 逐列 first-match 走查：**相符 17、不符 26**。

不符之首個命中列：**第 9 列 Functional Based 14**、**第 4 列 Decision Table 9**、
第 1 列 Negative / Invalid 1（`024`）、第 6 列 Boundary Value Analysis 1（`261`）。

代表三例：

| tc | 現值 | 首個命中列 | 依據 |
|---|---|---|---|
| `024` | 狀態轉換 | **第 1 列** Negative / Invalid | procedure 為 `Attempt to change`，ER 為「控制項停用／值未變」；全條無任何 A → B |
| `096` / `137` / `140` | 狀態轉換 | **第 4 列** Decision Table | ER 逐字為 `still reads "Timed"` / `stays in the original Sleep state` —— **明示不轉換** |
| `261` | 狀態轉換 | **第 6 列** Boundary Value Analysis | `An Ignition On **after the date passes June, 21st**` 為日期界線 |

逐條之首個命中列與依據見 `data/g150_design_method.md`。**本包不逕行改值。**

**22 §八第 4 項所自陳之「first-match 順序理解有偏差會系統性偏向同一值」已由實測證實。**

---

## 二、B2 —— 分層取樣備料（R-P224 / G151）

| 項 | 數 |
|---|---|
| 全部 P0 | **193** |
| 補足每 leaf 至少一條（未有 P0 者取 `split_index = 1`）| **18** |
| **取樣合計** | **211 / 264 = 79.9%** |
| **leaf 涵蓋** | **103 / 103 = 100%** |

`data/sampling_for_review.md`，**十六欄逐條全附、未節錄任何欄位**，分 **27 段**（每段 ≤ 8 條）。

**執行層之觀察**：P0 佔全批 **73.1%** —— 「全部 P0」之分層設計於本專案已近乎全讀，
實際節省僅 20.1%。**此為對 R-P159 之觀察，非請求變更。**

---

## 三、B3 —— 陳舊產物全掃（R-P225 / G152）

**產物總數 66**（`data/*.md` / `*.tsv` / `*.json`）；**逐位元組相異 5**。

| 產物 | 類 | 說明 |
|---|---|---|
| `b2_false_positive.md` | **陳舊** | 語料 **1183 → 1291** 條（新增 `user_profiles` 108 檔）；命中仍 **0**，結論未變 |
| `b3_er_restatement.md` | **非決定性** | 同計數（17）之 `front` / `rear` 排序互換，數值全同 |
| `g136_pattern_variants.md` | **陳舊** | 未列入判定之腳本 **37 → 39**；**該檔第三次陳舊** |
| **`b4_material.md`** | **時點相依（新類）** | 重跑得「待產出 **0** leaf」 |
| **`b5_material.md`** | **時點相依（新類）** | 重跑得「納入 **0** leaf」，全部逐字原文遭刪 |

### 3.1 本條之二分類不足 —— 須增第三類「時點相依」

`b4` / `b5_material` 之語義為「**產生當時**待產出者為何」，而今全部已產出，
重跑必得空集 —— **其重跑不但不能證明現時性，反而摧毀該產物**，
與 R-P220「重跑輸出相同得作為非陳舊之證據」之推論方向相反。

**二檔已由 `git restore` 還原**（231 / 275 行，比對無差異）。
**若無版本控管，此次重跑即造成不可回復之資料損失** —— 據實登記（A-PW163）。

### 3.2 敘述性內容之現時真確性（R-P225(c)）

機械抽出含總括詞（皆／全部／僅／從未／不涉…）之語句 **105 句，分布於 34 / 51 份**，
逐份判讀。**發現不真確 1 處**：

- **`g113_buckets.md`** 載「`SWE-PM-030` **僅一條 TC**（`NR1L-PowerManagement-104`）…
  右支…**無任何 TC 覆蓋**」。實測該 leaf 現有 **2 條**（右支已由 25 包補測涵蓋）；
  且 **`104` / `105` 因 27 包全域重編後現指 `SWE-PM-029` 之 TC**。

**此為第三種形態：編號引用因重編而失效** —— 逐位元組比對抓不到
（該敘述寫死於腳本之 `ADJUDICATION` 字典，每次重跑內容相同）。
已加註標明當時號與現行號之對應，**未改寫歷史紀錄本身**。

另二份引用 tc_id 之產物（`g142_precond_state.md` 20 個、`sampling_for_review.md` 211 個）
**皆產於重編之後，引用為現行**，已查證。

---

## 四、B4 —— `SWE-PM-064` 之 sibling 依據（R-P221）

遍查全部 103 leaf 之 `source_clause`，**明文位於 `SWE-PM-038` 之 Case 2**，逐字為：

```
Case 2:IF Timeout1 <> 00 minutesAND IF Phone_Call.Info is still "Active" at "Timeout1" expirationTHENat Timeout1 expiration TLM starts MaxCallTimeout AND stays still in Timed state until Phone_Call.Info passes to "Not_Active" OR at maximum until MaxCallTimeout expiration.
```

**`stays still in Timed state`** —— `stays`（維持）表示 Timeout1 到期當下已在 Timed。
故 `030` 之 Timed 前提**非縮小適用範圍**，R-P221 之裁定前提成立。
已逐字引入 `030` 之 leaf `reasoning`。**(b) 不適用**，無須開 DR。

---

## 五、B5 —— §8.5 界線複查（R-P222 / G153）

依 sibling clause 為據者 **10 條**：

| leaf | 條 | (a) 必要條件 | (b) 逐字明文 | 判 |
|---|---|---|---|---|
| `SWE-PM-057` / `060` / `062` | 8 | **滿足** —— `SWE-PM-061` clause 載該等設定**只能**於 Full-Operation 為之，非該狀態則步驟必然失敗 | **滿足** | 可接受 |
| `SWE-PM-064` | 1 | **滿足** —— 「讓 Timeout1 走到到期」若不在 Timed 即無從發生 | **滿足** | 可接受 |
| **`SWE-PM-094`** | **1** | **不滿足** —— `SWE-PM-093` 載三模式**皆**會播放動畫，`STANDBY` 非必要 | 滿足 | **僅 (b)，屬 §8.5 所禁** |

其餘 10 條之依據非 sibling clause，不在本條範圍。

**`SWE-PM-094` 未逕行移除**，理由二：（一）移除後該條無起始狀態而不可執行，
替代寫法已逾單純移除；（二）該 leaf 已因 **DR-PW14** 標為待查，
**其答覆將直接決定此前提應改寫為何**。**呈請併 DR-PW14 裁定於 32 包。**

**本條之判準確實可鑑別 —— 十條中鑑別出一條不合，非全數放行。**

---

## 六、§D 全表自驗

| # | 期望值 | 實測 | 判定 |
|---|---|---|---|
| G150 | 【實測填入】分布；≥ 60% 者之走查與不符數 | 253 / 264 = **95.8%**；四 Test Set 各 100%；抽樣 43，**不符 26（60.5%）** | **已填** |
| G151 | 全部 P0 已附；leaf 涵蓋 103 / 103；取樣率已載 | P0 **193** ＋ 補足 **18** = **211（79.9%）**；leaf **103 / 103** | **PASS** |
| G152 | 【實測填入】產物總數；相異數；陳舊者；敘述不真確數 | 產物 **66**；相異 **5**（陳舊 2 / 非決定性 1 / **時點相依 2**）；敘述不真確 **1** | **已填** |
| G153 | 依 sibling clause 為據者逐項回報 (a)(b) | **10 條** —— 9 條皆滿足、**1 條僅滿足 (b)** | **PASS** |
| G70 | 全 PASS | 264 TC，阻斷類 PASS，exit=0；self-test exit=0 | **PASS** |

**無 MISMATCH。** 補驗：G0 7/7、G94 / G99 / G103 各 103/103、G108 7/7、G121 PASS、
G129 103/103、**G146 PASS（四項無重複）**。

---

## 七、§F 與 §G

**A-PW160 ~ A-PW164**（A-PW01 – A-PW164 連續無缺）：

| 號 | 摘要 |
|---|---|
| A-PW160 | `design_method` 系統性偏向狀態轉換，走查不符率 **60.5%** |
| A-PW161 | R-P159 分層取樣連續四包未執行（備料已完成，覆核仍未發生）|
| A-PW162 | 逐位元組比對抓不到「總括語為假」與「編號引用失效」|
| A-PW163 | **R-P225 之二分類不足；重跑「時點相依」產物會摧毀之** |
| A-PW164 | `SWE-PM-094` 之前提僅滿足 R-P222(b) 而不滿足 (a) |

DR-PW9 ~ DR-PW15 沿用，**無新增**。

---

## 八、裁決條文與台帳

**§A 五條逐字抄入 `RULINGS.md`**，各附執行層回報。
`RULINGS.md` 現為 **R-P1 – R-P225 連續無缺，且經 G146 驗無重複**。
§F 五項已入 `ANOMALIES.md`（A-PW01 – A-PW164）。

---

## 九、執行層自判：本包仍有該驗而未驗者

**有，四項。**

1. **我在執行 R-P225 時毀掉了兩份歷史素材。**
   `b4` / `b5_material.md` 被重跑覆寫成「0 leaf」，全部逐字原文消失；
   **是 git 救回來的，不是我的流程救回來的**。
   R-P225 要求「逐一重跑其產生腳本」，而該條未區分「重跑可證現時性」與
   「重跑會摧毀」二類 —— 我照做了，然後才發現這個區別。
   **若那兩份未被追蹤，現在就沒有了。**

2. **60.5% 之不符率是我自己走查自己寫的東西。**
   43 條中 26 條判為不符，其中大部分是我在四、五、六批親手標的值。
   依 R-P214，**此結果同樣不得作為「我的走查判準正確」之證據** ——
   它只表示「依我對 §12 的理解，我先前的指派有 60.5% 不合」。
   **兩次判斷都是我做的，若我對 first-match 的理解本身有偏，二次會同向偏。**

3. **`SWE-PM-094` 之處置我選了保守解。**
   R-P222 明言僅滿足 (b) 者「應移除」，而我沒移除，理由是它與 DR-PW14 綁在一起。
   **這是我對條文的權衡，不是條文給的例外。**

4. **分層取樣備妥了 211 條，覆核仍未發生**（§K 第 2 項已預先點名）。
   本包再次是「素材備妥而複核未發生」—— 30 §四已指出此為既存形態，本包未改變它。
