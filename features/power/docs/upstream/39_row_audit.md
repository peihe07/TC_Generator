# 39 — §12 逐列核對、axis 收尾與反向重複掃描

上繳包 | 執行層 → 分析層 | 往返 NN = 39

§J 自檢**二次**（R-P200(c)）：執行前與抄錄前皆
**§A 區塊數 6 / §J 列數 6 / §H 步驟 9「六條」**，三處一致（`mtime` 未變）。

改前依 R-P234 備份（`sandbox/backup/20260818T135200Z`，163 檔）。
現行最大號（R-P147）：R-P264 / A-PW229 / DR-PW15 → 本包自 R-P265、A-PW230 起。

---

## 一、B1 —— §12 全九列核對（R-P266 / G185）**最高優先**

**條件欄逐字之佐證**：九列之條件欄**皆見於權威原文**（9 / 9，
`docs/runtime/ASPICE_SWE6_AI_Instruction.md` 第 572–580 行）；
tie-break 句亦逐字可見（第 582 行），**僅三句**，對應第 3、8、9 列。

### 1.1 核對結果 —— **不一致 4 / 9**

| 列 | 條件欄逐字 | tie-break | 現行謂詞所依 | 一致 |
|---|---|---|---|---|
| 1 | `Invalid input / illegal op` | — | `attempt to\|invalid\|illegal\|not allowed` | 是 |
| 2 | `Simulated fault (disconnect, timeout)` | — | `disconnect\|inject…fault\|fault injection` | **否** |
| 3 | `State A → State B transition` | `state-change focus` | `POSITIVE_RE` | 是 |
| 4 | `Multiple conditions → outcome` | — | `substantive_conditions(pre) >= 2` | **否** |
| 5 | `Input partitioned valid / invalid` | — | `a value other than\|other than "\|out of range` | 是 |
| 6 | `Boundary (=limit, limit±1)` | — | `…\|limit(\b\|±)\|greater than` | **否** |
| 7 | `Multi-parameter combination` | — | **無謂詞** | 是 |
| 8 | `End-to-end flow, ≥3 features` | `≥3 steps crossing features` | 功能數 ≥ 3 | 是（R-P259 已訂正） |
| 9 | `Single feature check` | `1–2 steps single feature` | catch-all | **否** |

**R-P259 之同型錯誤確實不只一處。**

### 1.2 第 2 列 —— 不一致，惟**不予訂正**

條件欄括號明列 `disconnect`、`timeout`；現行謂詞只取前者。

**惟語料實測：`timeout` 之 49 次出現無一為「注入之逾時故障」** ——
全為設定名稱（`Open the timeout setting entry in the TLM menu`、
`SwitchOff_Timeout_Setting.Req`、`Timeout1`）。
逕加將產生約 **26 條偽陽性、0 條真陽性**。

依 **R-P250**「一個都取不到者，該謂詞不得使用」——**不加入**，
列為謂詞與條件欄之**已知落差**：本 feature 無注入型逾時故障（A-PW231）。

### 1.3 第 6 列 —— 不一致，**已訂正並改值**

`limit(\b|±)` 命中**裸詞 `limit`**：3 條偽陽性（`…-010` / `…-011` / `…-015` 之
「Read the volume **limit**」）。
**與 37 包 `rejudge_axis` 所修者為同一缺陷，而本檔當時未同步訂正** ——
即「同一缺陷修於一處而未及他處」之實例。

v2 只留顯式界線形態；條件欄之 `limit±1` 形態於語料 **0 次**，故不設其謂詞（R-P250）。

**依 §E 之阻斷條件已重判改值**：第 6 列 **6 → 4**；
`…-010` / `…-011` → 第 9 列（已改值）、`…-015` → 第 5 列（無標籤仍未改）；
落底 90 → **92**；**標籤缺漏而未改者 13 → 11**（A-PW232）。

### 1.4 第 9 列 —— **既有裁決與 canon 之分歧**

條件欄為 `Single feature check`（**實質判準**），現行為 catch-all（**無判準**），
而 **R-P231(c) 明訂第 9 列為 catch-all**。

實測：落底 **92** 條之功能數分布為 0 個 **25**、1 個 **50**、2 個 **17** ——
**17 條不符 `Single feature check`**。
**執行層不自行裁定裁決與 canon 何者優先**，列出供處置（A-PW233）。

---

## 二、B2 —— `axis` 40 條省略欄位（R-P265 / G186）

### 2.1 依據之查證（直接查權威檔，非僅依副本）

§K 第 3 項指出下放包之副本自 01 包起未與權威版本比對，而本裁定之依據正繫於此。
執行層直接查 `docs/runtime/ASPICE_SWE6_AI_Instruction.md`：

- **§10.1**（第 508 行）：`also carries \`reasoning\` (§10.4) and optional
  \`keywords\`, \`duplicate_of\`, \`distinguishing_axis\`.` —— **`optional` 屬實**
- **§4.6**（第 168 行）：`On \`## Sibling Rows\` injection, output \`duplicate_of\` …
  and \`distinguishing_axis\` …` —— **輸出時機為 sibling 注入時，屬實**

**依據於權威原文成立**；§K 第 3 項於此二節範圍內已解消（A-PW235）。

### 2.2 處置

- **(a)** 40 條已**移除** `distinguishing_axis` 欄（非設為空字串）。
  實測：無該欄者 **40** 條，其中 leaf 產出 ≥ 2 條者 **0**。
- **(b)** C2 訂正為「**若該欄存在則須非空且為六值之一**」。
- **(c)** 新增 **C8**：leaf 產出 ≥ 2 條者該欄**必須存在**；全批觸發 **0**。
- **(d)** 未改 §4.6、未改 §10.1。

**fixture 12 案全數如期**（含二新案）。
**G168 結構違規 227 → 187**（減 40）；其餘 187 條待 40 包依 R-P262 之複核改值。

---

## 三、B3 —— 第 4 列代理判準擴充（R-P267 / G187）**不改值**

**成因確認**：`…-026` 之第二條件（所選之值）在 `test_procedure` 之
`Select "Not_Active" for …`，**不在 `pre_conditions`**。

| 來源 | 計法 |
|---|---|
| `pre_conditions` | 實質條件項數（扣除 bench 樣板列） |
| `test_procedure` | 設定／施加具名參數之值之步驟 —— 措詞實測：`Set … to`（37）、`Select "…" for`（12）、`Send … signal`（41）、`Apply …`（6）、`Keep … at`（4） |
| `input_test_data` | 非 `NA` 之每一列 |

**第 4 列 80 → 141（新增 61）。**
新增者之現落點：**第 9 列 59**、第 8 列 1、第 5 列 1 ——
即現行落底 92 條中 **59 條（64.1%）**在擴充後應為第 4 列。
R-P267 所舉之 `…-026` / `…-027` **皆被納入**。

**⚠ 執行層之保留**：新增量占全批 23.1%，而其判準為執行層自設自實作自回報（R-P214）；
**「何謂一個條件」之界定未經第二方確認**（例：`Send the two Load Shed signals`
計為一抑或二，本檔計為一）（A-PW234）。

**本包未因此改值。**

---

## 四、B4 —— `source_clause` 反向全批掃（R-P269 / G188）**不改值**

| 項 | 數 |
|---|---|
| `source_clause` 逐字相同之 leaf 群 | **6** |
| 　其 TC 集合**亦相同** | **5** |
| 　其 TC 集合**不同 —— 新形態** | **1** |

**新形態**：`SWE-PM-056` / `SWE-PM-097` —— **錨點亦相同**（`4941680`），
clause 逐字相同，各 1 條 TC 而**無一相同**。
實讀其差異**僅措詞**：`which logo the HU displays` vs `which logo appears`；
標題差 `startup animation` vs `startup animation **selection**`。
**判定：同一測試之二次撰寫，其差異為偶然措詞。**

**⚠ 本項之意義：G188 補上了 G178 之盲區。**
該對為 **DR-PW12 五對中的第五對**，G178 因其措詞微差而未命中（A-PW236）。

**執行層之補充**：本掃描以 clause **逐字**相同為條件，**故亦有盲區** ——
clause 僅措詞微差者（如 `SWE-PM-028` / `029`，相似度 0.75）不會命中。
**逐字判準於規格側與 TC 側皆有同型限度**（A-PW237）。

---

## 五、B5 —— 閘門代表性盤點（R-P268 / G189）

**(a)** G150 已回報有效樣本／母體（38 包即實作）。
**(b)** 本次 **2 / 4 = 50.0%**，**恰在門檻上** —— 尚未低於，**惟下次改值即跌破**。
**(c)** 盤點 **8** 項：

| 閘門 | 型 | 現況 | 風險 |
|---|---|---|---|
| G150 | **甲＋乙** | 有效樣本 **2 / 4** | **最高** |
| G162 | **甲＋乙** | 母體已於 38 包變動（P0 193 → 157）而 `CLASSIFY` 未更新 | **高** |
| —（`build_review_material_36`） | 甲 | 第 4 列抽 14 / 79，**實際複核 2 條** | **高** |
| G154 / G155 | 乙 | 鍵為 tc_id 不失配；惟裁決係對舊值所作 | 中 |
| —（`build_axis_review_38`） | 甲 | 複核尚未執行 | 中 |
| G113 / G131–G133 | 乙 / 甲 | 母體為規格側，TC 改值不影響 | 低 |
| G156 | 乙（**有預設值**） | `data/` 88 檔全數已分類 | **低（訂正）** |

**最實質之發現**：**G162 與 G150 同型，其母體已於 38 包變動而未加裝有效樣本回報** ——
其「38.2% 無法歸類」之結論**已對現值失效，而報表不會顯示此事**（A-PW238）。

**⚠ G156 之風險評估經查為誤判，已訂正**：其有 `DEFAULT = ("a", …)`，
新產物自動歸類，**無靜默缺口**。**偏誤方向為誇大風險清單**，依 R-P187 明載。
其**反向風險為預設過寬**（新增之 (c) 型若未入 `VERDICT` 會被誤歸 (a) 而遭重跑）（A-PW239）。

---

## 六、B6 —— `reasoning` 查補與 DR-PW12 更新（R-P270）

**(a)** 8 組所涉之**十個 leaf**，其 `reasoning` **全數已載**共用錨點與 DR-PW12
（`SWE-PM-054` / `101` / `055` / `102` / `068` / `114` / `070` / `115` / `056` / `097`）。
**R-P186 之要求早已滿足，無一須補**（A-PW240）。

**(b)** DR-PW12 已增列待答項，逐字載明：
該五對之 TC 內文逐字全同（八組，另 `158`/`189` 僅措詞微差），六組連 `tc_title` 亦同；
二 leaf 之 `section` / `source_anchor` / `source_clause` 三者全同，
**無語義材料可資區別，逕加即為造值（§8.4.1）**；
**本 DR 之答覆將決定該 8 組標題是否需分化**。

**(c)** 交付說明之驗證邊界已增列（見 §七）。

---

## 七、交付說明之驗證邊界（增列）

於 35 §7.2 之六項永久限制、38 之 `design_method` 值域邊界外，**增列三項**：

> **(一) 8 組同標題而不同 Requirement ID**
> 工作簿中存在 8 組 `tc_title` 逐字相同而 Requirement ID 不同之列
> （`152`/`196`、`153`/`197`、`154`/`198`、`155`/`199`、`156`/`200`、`157`/`201`、
> `165`/`229`、`168`/`230`）。
> **此為已知且待答**（DR-PW12），**非疏漏** ——
> 其二 leaf 共用相同錨點與逐字相同之 clause，依 §8.2.2 不得由 TC 作者合併。
>
> **(二) `distinguishing_axis` 欄之有無**
> 40 條 TC **無**該欄 —— 其 leaf 僅產出 1 條 TC，無 sibling，
> 依 §10.1（optional）與 §4.6（sibling 注入時輸出）其輸出條件本即不成立。
> **欄位缺漏為文義所許，非遺漏。**
> 另 187 條之 `axis` 值不在 §4.6 之六值內，**重判提案已出而尚未套用**。
>
> **(三) `design_method` 之謂詞與 §12 條件欄之已知落差**
> 第 2 列：條件欄舉 `timeout` 為例，**本 feature 無注入型逾時故障**，該例無從建立。
> 第 4 列：代理判準經擴充後應為 141 條（現 80 條），**尚未套用**。
> 第 9 列：條件欄為 `Single feature check` 而現行為 catch-all，
> 落底 92 條中 **17 條功能數 ≥ 2**，即不符該條件欄。

---

## 八、§D 自驗

| # | 項目 | 期望值 | 實測 |
|---|---|---|---|
| G185 | §12 全九列核對 | 九列皆回報三事；不一致者已列出並重判 | 九列皆回報；**不一致 4 / 9**；第 6 列**已重判改值**，第 2 列不予訂正（附理由），第 4 / 9 列列出待處置 |
| G186 | `axis` 40 條省略 | 40 條無 `axis` / `delta` 欄；C2 訂正後不觸發；≥ 2 條之 leaf 皆有 `axis` | **40 條已移除**；C2 訂正、C8 新增；**≥ 2 條者缺欄 0**；fixture **12 / 12**；違規 227 → **187** |
| G187 | 第 4 列擴充 | 84 → ?；新增者逐條有二條件 | **80 → 141**（新增 61，逐條已列其條件）；**不改值** |
| G188 | `source_clause` 反向掃 | clause 相同之 leaf 對數；其 TC 相同／不同之分布 | **6 群**；TC 亦相同 **5**、**不同 1**（`SWE-PM-056`/`097`）；**不改值** |
| G189 | 閘門代表性 | G150 回報有效樣本／母體；此類閘門已盤點 | G150 **2 / 4 = 50.0%**；盤點 **8 項**，高風險 3 項 |
| G70 | lint 全閘 | 全 PASS | **阻斷類 PASS，exit 0** |

**改值後全閘重跑**（以本包備份前後對照）：**18 個閘門中 17 個數值全同**，
變動 1 個（`verify_axis` 227 → 187，恰為省略之 40 條）—— **新觸發 0**。

**G108 / G177**：同步後 **76 / 76 完整**（817 符號）。

---

## 九、帳冊異動

- **RULINGS.md**：R-P265 – **R-P270** 六條逐字抄入 ＋ 執行層回報。
  **R-P1 – R-P270 連續、無重複、無缺號**。
- **ANOMALIES.md**：新增 **A-PW230 – A-PW240**（11 條）。**A-PW1 – A-PW240 無缺號**。
- **DATA_REQUESTS.md**：**DR-PW12 依 R-P270(b) 增列待答項**；無新增編號。

---

## 十、執行層自判：本包仍有該驗而未驗者

**有，四項。**

1. **第 4 列擴充後之 141 條與現行 80 條之差，本包未改值而下包須改** ——
   其量為 61 條，**大於 38 包第二級改值中 `design_method` 之 164 條的三分之一**。
   而其判準（「何謂一個條件」）**未經第二方確認**，
   若該界定有誤，61 條將一致地錯 —— 與 A-PW220 所記之形態同型。
2. **第 9 列之分歧本包只量未裁，而其影響面為 17 條。**
   若採條件欄之 `Single feature check`，該 17 條須改判；
   **惟改判至何列尚無謂詞**（其功能數為 2，未達第 8 列之 3）——
   即 §12 於「2 個功能」處**無列可歸**，此為 canon 之覆蓋缺口，本包未提請裁定。
3. **G185 之核對為執行層自行判讀條件欄之語義**（如判 `attempt to` 為
   `illegal op` 之語料措詞），**未經第二方確認**。
   九列中判為「一致」之五列，其一致性同樣僅為執行層之判讀。
4. **本包三度自查出自身之偏誤**（第 6 列同型缺陷未同步、G156 風險誤判、
   G188 逐字判準之盲區），**皆非由任何機制攔下**。
   與 36 §十第 1 項所記之形態相同 —— **自查仍是唯一之攔截手段**。
