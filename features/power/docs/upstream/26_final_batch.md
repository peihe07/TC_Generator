# 上繳包 26 —— 裁定積案、數字標註機制與末批

> 對應下放包：`features/power/docs/handoff/26_final_batch.md`
> 執行層：Claude（TC_Generator）
>
> **§J 自檢：一次不符，已停並回報，經確認後以 15 條續行** —— 見 §十一。
> 執行層於自檢時實測 §A fenced block = **14**、§J 列數 = **14**、§H 步驟 9 =「**十四條**」，
> 三處一致而開工；作業至步驟 9 之前，實測 §A 已增為 **15**（新增 `[R-P197]`），
> 與自檢所據之 14 不符，**依指示停於抄錄裁決條文之前並回報**，
> 未自行補寫任何條文。經確認後以 15 條續行。
>
> 本包**未執行任何 git 子命令**（R-P149）；**未對任何 workbook 呼叫 `save()`**；
> **未觸碰客戶樹與 `inputs/`**；
> **未採信下放包內未標註來源型別之數字**（R-P183，二處已自行實測取代）；
> **未自行合併或拆分 `SWE-PM-093` / `SWE-PM-025` / 重疊對之任何 TC**；
> **未擴充 G113 之辨識規則**（R-P192）；
> **未自行推定本專案之品牌適用性**（R-P193(c)；品牌由 R-P197 裁定，
> `Brand_Configuration_2` 之本專案配置值實測不到，未推定）；
> **未以模板套語填充 leaf `reasoning`**（R-P190(ii)）。

**G0 前置閘：7 / 7 素材 SHA256 相符 —— PASS。**

**編號查核（R-P147，先查後開）**：
**A-PW 最大 132 → 本包新號自 A-PW139 起**（現行最大實測為 **138**，25 包已用至此）、
**R-P 最大 182**、**DR-PW 最大 12**、**閘門最大 G124**。
本包新號：**R-P183–R-P197**、**A-PW139–A-PW142**、**G125–G133**；
**DR-PW13 曾開而於同包內撤回**（R-P197 明令不開）。

---

## 一、B5 —— R-P183 首次適用（G128）

掃描範圍依 §B5 為 §前言 / §A（**條文內引述既有紀錄者豁免**）/ §B / §D；
豁免編號（R-P / A-PW / G / DR-PW / leaf ID / 章節號 / 包次 / 往返 NN）。

### 1.1 未標註之數字 —— **2 處**

| 位置 | 數字 | 執行層獨立實測 | 結論 |
|---|---|---|---|
| §D 之 G126 期望值 | `115` | G121 實測：114 定版 ＋ `SWE-PM-089` 留空 | **相符** |
| §B6(a) / §D 之 G129 | 「**五個**批次檔」 | `generated/` 當時實測 5 個（本包產出末批後為 6）| **相符** |

另二處為**引述既有條文之項數**，出處已具於句中，判為已具來源並實測：
「R-P158 所列**四**項」→ 該條之上繳須含項實測為 **4**；
「§10.4 **四**項順序」→ 該節實測為 **4** 個編號項。

### 1.2 `[推導]` 之結論實測（本條明令）

| 標註 | 推導式 | 實測 | 相符 |
|---|---|---|---|
| R-P190(i) | 54 leaf = 第四批 25 ＋ 第五批 29 | 25 ＋ 29 = **54** | **是** |
| §K 第 1 項 | 180 條 = 223 − 43（首二批）| 223 − 43 = **180** | **是** |

### 1.3 一項措辭疑義（呈報）

本條載「未標註之數字 → **停並回報**，不得逕行採信」，
而 §B5 載「分析層可能有遺漏，執行層須據實指出」且 §H 步驟 3 之後尚有步驟 4–10。
**執行層取「不得逕行採信」為其效力核心** —— 不停止全包，
而是對該二數字不採信、改以自行實測取代。若原意為硬停，請於 27 包更正本判讀。

**G128：本包全部數字皆已檢查；未標註者 2 處已回報並實測，二者皆正確。**

---

## 二、B2 —— DR-PW9 阻斷欄修訂（R-P185）

阻斷欄現逐字載「**阻斷 `SWE-PM-112` 之 TC 撰寫**（R-P185，26 包修訂）」，
並記明「該阻斷意旨原未為本欄之措辭所承接，致 25 包執行層須以保守解自行排除」。
**G121 對帳表同步** —— `SWE-PM-112` 由諮詢性移入**阻斷**，
故末批之範圍自對帳表取得時即自動排除之，不再依賴執行層之判讀。

---

## 三、B10 / R-P197(d) —— 二處加註（R-P194 / R-P197）

| 條文 | 加註前 SHA256 | 加註後 | bytes |
|---|---|---|---|
| **R-P184** | `b286c4ecf4defa17b4839b1275345fa78894065bd0f03bb83f0f42d3c682a231` | **相同** | 1038 |
| **R-P193** | `ab5977772b343a4f83306e69c1bc79555705c76e538ecfac5a0b3f91d5a69733` | **相同** | 996 |

**二者原文位元組皆未變。**

---

## 四、B1 —— 末批（R-P189 / G125 / G126）

### 4.1 範圍 —— leaf ID 全集逐一列出，不以區間表述

範圍取自 **G121 對帳表之「未產出且未受阻斷」全集**。

**納入 —— 16 leaf（Branding and Theme 全數）**：
`SWE-PM-077`、`SWE-PM-078`、`SWE-PM-079`、`SWE-PM-080`、`SWE-PM-081`、`SWE-PM-082`、
`SWE-PM-083`、`SWE-PM-084`、`SWE-PM-085`、`SWE-PM-086`、`SWE-PM-087`、`SWE-PM-088`、
`SWE-PM-090`、`SWE-PM-091`、`SWE-PM-092`、`SWE-PM-096`

**排除 —— 12 leaf，逐一列明 DR 編號**：

| leaf | Test Set | 阻斷之 DR |
|---|---|---|
| `SWE-PM-001` ~ `SWE-PM-007`、`SWE-PM-009` | Power State | **DR-PW6** |
| `SWE-PM-008` | Power State | **DR-PW6、DR-PW11** |
| `SWE-PM-010` | Power State | **DR-PW11** |
| `SWE-PM-112` | Startup Display | **DR-PW9**（本包依 R-P185 修訂後為明文阻斷）|
| `SWE-PM-089` | （留空 —— R-P141）| **DR-PW1** |

### 4.2 產出（G125）

| 項 | 值 |
|---|---|
| 檔案 | `features/power/generated/batch_006_branding_theme.json` |
| leaf | **16** |
| TC | **34**（臨時號 `224`–`257`，接續第五批之末 `223`，遞增無跳號）|
| Test Set | Branding and Theme 34 / 34 |
| `spec_reference` | 34 / 34 指向 CFTS009 |
| leaf `reasoning` | **16 / 16 於產出時即依 §10.4 四項寫入**，無延後 |

素材前置檢查（R-P172）：16 leaf 之全部 item 於 CFTS 文字層皆有內文。

### 4.3 最終對帳（G126）

| 項 | 數 |
|---|---|
| 已產出 | **103** |
| 受阻斷 | **11**（`SWE-PM-001`–`010`、`SWE-PM-112`）|
| 留空（`SWE-PM-089`）| **1** |
| **合計** | **115** |

**相符。** 五個 Test Set 之「未產出且未阻斷」**全部為 0** —— **Phase 4 之產出面完成**。

**Phase 4 累計：六批 103 leaf / 257 條。**

---

## 五、B6 —— leaf `reasoning` 補寫與 G129（R-P190）

### 5.1 非空率實測 —— 本條之字面與實測有出入，據實更正

| 批次 | 非空 / 總數 |
|---|---|
| 批次一 | 3 / 3 |
| 批次二 | 8 / 8 |
| 批次三 | 22 / 22 |
| **批次四** | **1 / 25**（非空者：`SWE-PM-050`）|
| **批次五** | **6 / 29**（非空者：`093` / `097` / `101` / `102` / `114` / `115`）|

本條稱「批次四、五之 leaf `reasoning` **皆為空字串**」——
實測為**完全空 47**，另 **7 個有內容但僅為個案註記**
（共用錨點之呈請、`ELSE` 前件之說明）。
**本條之判斷成立**（該等註記非 §10.4 四項之稽核軌跡），
**惟「皆為空字串」之字面與實測不符**，更正為 47 空 ＋ 7 註記 = 54 需處理。

### 5.2 補寫

批次四 **25**、批次五 **29** 逐 leaf 依 §10.4 四項寫入；末批 16 於產出時即寫入。
**非模板套語** —— 各條之「刻意略過」皆載具體對象：
委由之 sibling leaf ID（§8.2.1）、外部文件名（§8.4.2）、規格未載之數值（不造容差值）。

### 5.3 G129

`scripts/verify_reasoning.py`。門檻由 `--calibrate` **實測校準** ——
批次一 ~ 三之最短 `reasoning` 為 **26 字**（`SWE-PM-022`），向下取整至十位 → **20**。

fixture 四案（R-P190(iii) 之「刻意置空證明其會 FAIL」）：

| 案 | 期望 | 實際 |
|---|---|---|
| 現況之批次一 | 通過 | 通過 |
| **刻意將第一個 leaf 之 `reasoning` 置空** | FAIL | **FAIL** |
| 以套語充數（8 字，短於門檻）| FAIL | **FAIL** |
| 整個欄位不存在 | FAIL | **FAIL** |

**全量：103 / 103 合格。**

**門檻偏低據實登記** —— 20 字只攔得住「根本沒寫」，攔不住「寫得太薄」；
26 字之樣本本身即為薄弱軌跡，而該批已為分析層判為合格。
提高門檻須先重評批次一 ~ 三之既有 `reasoning`，非本閘可單方為之。

---

## 六、B7 —— 補測同步（R-P191 / G130）

**需更新 5 leaf（批次三），已更新 5 —— G130 = 5 / 5。**

| leaf | 前 | 後 | 同步之內容 |
|---|---|---|---|
| `SWE-PM-011` | 268 | 430 | 「短按與長按等價」之刻意略過**已被 22 包盲測推翻** |
| `SWE-PM-014` | 303 | 458 | 補記 23 包 G113 攔下之 `Ignition Pre Off` OR 支（第八例）|
| `SWE-PM-018` | 51 | 198 | 「單一行為，不拆」已不成立，現為二條（第九例）|
| `SWE-PM-028` | 73 | 273 | 補記 LTM High OR 支之補測 ＋ R-P196 之重疊登記 |
| `SWE-PM-030` | 123 | 319 | 「單一行為」已不成立，25 包補 OR 右支（第十例）|

批次五之補測（25 包 G113 與透鏡 1 所攔：`093` / `099` / `105` / `110`）
於本包補寫 `reasoning` 時**同步寫入**，非另一步。

---

## 七、B8 —— 逗號列舉規模（R-P192 / G131）

判準先寫定後執行（`scan_clause_patterns.py`）：冒號後（或 `In the following …:` 型）
以逗號分隔**三項以上**者計一個列舉。

- **出現 5 次 / 5 個 leaf**：`SWE-PM-013`、`028`、`041`、`042`、`103`
- **判別率 5 / 5 = 100%**（母體僅 5 筆，抽樣無意義；種子 `random.Random(26)` 仍載明）
- 判別採**與 G113 相同之「各項獨有實詞」判準**

**屬「各項皆應測而 TC 只取其一」者：2 / 5 leaf（40%），合計 6 個未覆蓋項**

| leaf | 未覆蓋之列舉項 |
|---|---|
| `SWE-PM-013` | `Ignition Pre_Start`、`Ignition Cranking`、`Ignition On Engine On` |
| `SWE-PM-103` | `Ignition Pre_Start`、`Ignition Start`、`Ignition Cranking` |

其餘三者：`041` / `042` 之列舉項全數覆蓋；`028` 之未覆蓋項為散文，非可測項。

**執行層未補測該 6 項** —— 本條明訂「先量規模，不先立閘門」「規模明確後方議」。
已於 `SWE-PM-103` 之 `reasoning` 明載其未測與理由；`SWE-PM-013` 登記於 A-PW142。
**此為交付件中已知而未補之缺口，呈請裁定於 27 包。**

---

## 八、B9 / B11 / B12

### 8.1 適用性條件盤點（R-P193 / G132）

**含品牌 / 車型 / 市場 / 配備條件之 leaf：40 / 103**，清單見 `data/b8_b9_b12_scans.md`。
各該 leaf 之 `reasoning` 已載其處理。

**品牌一項由 R-P197 直接裁定為 `Jeep` 並明令不開 DR** ——
執行層曾依 R-P193(c) 開立 `DR-PW13`，**於同包內撤回**。
未被 R-P197 涵蓋之三類（`$VC_VEH_LINE$` 之 `DT` / `M240`、螢幕尺寸 `7 inch`、
機型 `LTM High Radio`）**不另開 DR**，登記於 **A-PW141** 待裁 ——
執行層不以同型問題另開 DR 而規避 R-P197 之「不開 DR」。

**一項前提之實測不符**：R-P193 稱 `SWE-PM-014` 與 `SWE-PM-026` 之 `source_clause`
「皆含 `Brand_Configuration_2 == "Jeep"` 之分支與其 ELSE」。實測：
`026` 含 **1 次且無 ELSE**；**`014` 含 0 次** ——
該參數完全不在其錨點原文內，而其三條 TC 之 `pre_conditions` 引用之
（且拼寫為 `Brand_Configuration _2`，多一空格）。
為 §8.4.2 之依據越界（**A-PW139**，與 A-PW115 同族）；**G82 攔不住** ——
其只掃 ER 之專有標的，不掃 `pre_conditions`。**執行層未逕改該三條。**

### 8.2 `SWE-PM-026` 車門 OR 查證（R-P195）

| 問 | 答 |
|---|---|
| （a）G113 是否觸發？ | **是** —— 組 1 支 1 `DriverDoorSts`、支 2 `PsngrDoorSts`，**二支皆判「已覆蓋」**，故未入待裁清單 |
| （b）漏檢形態 | **無漏檢** —— 標準 `A OR B` 結構，辨識規則正常命中 |
| （c）是否補測 | **不需** —— `NR1L-PowerManagement-092` 之 `input_test_data` 逐字為 `STATUS_BH_BCM1.PsngrDoorSts = "Open"` |

**本條之前提有一處與實測不符**：其稱三條「**無一條提及副駕駛門**」，而 `092` 提及。

**惟本條之疑慮方向正確，只是落點不同** —— 三條之**拆分軸**為前狀態與通話狀態，
副駕駛門僅出現於通話中那一條、駕駛門僅出現於其餘二條，
**二個車門訊號之覆蓋係附帶而非設計**；若日後改寫該三條，該覆蓋可能無聲消失。

**規格原文之矛盾登記於 A-PW140** —— OR 對之後**又單獨重複一次**
`DriverDoorSts passes to "Open"`，且有連續二個 `AND`。
**未合入現有 DR**（其為書寫瑕疵而非資料缺漏，且不阻斷任何 TC）；是否另開呈請裁定。

### 8.3 重疊對掃描（R-P196 / G133）

**門檻：Jaccard(實詞集合) ≥ 0.60**（實詞集合取自 `reverse_coverage.words()`，
含詞幹化與停用詞移除）。

**重疊對合計 27** —— 錨點**相同**者 **5**（A-PW137 已登記）、**不同**者 **22**（本條之對象）。

| 對 | 相似度 |
|---|---|
| `SWE-PM-041` / `042` | **1.00** |
| `SWE-PM-109` / `110` | 0.88 |
| `SWE-PM-098` / `100` | 0.88 |
| `SWE-PM-028` / `029` | 0.85 |
| `SWE-PM-027` / `052` | 0.84 |

**一項門檻之限度須據實登記**：`041` / `042` 相似度 **1.00**（實詞集合完全相同）
**而二者實質不同** —— 差別為 `TLM OFF with Network **on**` 對 `**off**`，
`on` 為停用詞而遭移除。**本門檻對「差一個停用詞」之對造完全失明**；
此非本次調整所致，而是度量本身之性質。

各該 TC 之 `reasoning` 已註明對造 leaf ID 與相似度。**未合併亦未刪除任何 TC。**

---

## 九、B3 / B4 —— 共用錨點註記與 `SWE-PM-093` 屬性（R-P186 / R-P188 / G127）

**B3**：五對之 `reasoning` 已註明對造 leaf ID 與 DR-PW12，
並逐字引 **§8.2.2**「TC 作者不得代 RD 合併需求單位」為依據（不再以 R-P121 之類比為據）。

**B4 / G127** —— `4941301` vs `4941941`，正規化後**仍相異五欄**：

| 屬性 | `4941301` | `4941941` | 集合相同 | 差集 |
|---|---|---|---|---|
| Artifact Type | Subsystem Functional Requirement | 同左 | 是 | — |
| **ECU** | ETM, LTM, RRM | ETM, LTM | **否** | 左獨有 {rrm} |
| **EE Architecture** | Atlantis High, PowerNet, CUSW, Atlantis Mid | All | **否** | 左 {atlantis high, atlantis mid, cusw, powernet}／右 {all} |
| Market | All | All | 是 | — |
| **Model Year** | Default | 2024, 2025, 2022, 2023 | **否** | 左 {default}／右 {2022, 2023, 2024, 2025} |
| **Radio** | allSys | R1H, R1L-R, R1M, R1L | **否** | 左 {allsys}／右 {r1h, r1l, r1l-r, r1m} |
| **State** | New | Under Review | **否** | 左 {new}／右 {under review} |

二者之 ECU **皆含 `LTM`**、Radio **皆涵蓋本專案**，適用性未變。**未合併亦未拆分。**

---

## 十、R-P197 —— 品牌裁定之處置

（a）`SWE-PM-026` **整個 leaf 在範圍內**，三條維持。
（b）`SWE-PM-014` 之 Jeep 分支在範圍內 —— 實測 `059` 驗 Jeep 支、`060` 驗 ELSE 支，二支皆覆蓋。
（d）R-P193 已加註指向本條，原文位元組未變（§三）。

**（c）`Brand_Configuration_2` 值域實測**：

| 來源 | 結果 |
|---|---|
| CFTS 本文（`anchor_bodies()` 全量，8 處引用）| **無值域宣告** |
| 037（全表格，7 處引用）| **無值域宣告** |
| **CFTS 本文實際使用之相異值** | **2 個** —— `"Jeep"`（`4941575` / `4941740`）、`"Fiat"`（`4941737`）|

即**該參數於規格層面得取 ≥ 2 值，並非恆為 `Jeep`**；
**而「本專案之實際配置值」實測不到**（PROXI 配置屬車輛配置檔，不在 CFTS 或 037 內）。

依本條末句「**實測不到值域者停並上繳，不得推定**」：**ELSE 分支未補測**，
執行層**未推定**該參數於本專案恆為 `Jeep`。**裁定於 27 包。**

---

## 十一、§J 自檢之一次不符 —— 停並回報之經過

| 時點 | 事實 |
|---|---|
| 開工前自檢 | §A fenced block **14**、§J 列數 **14**、§H 步驟 9「**十四條**」—— **三處一致，開工** |
| 步驟 9 之前 | 實測 §A 已增為 **15**（新增 `[R-P197]`），§J 亦為 15、§H 步驟 9 改為「十五條」|
| 處置 | **停於抄錄裁決條文之前**，未寫入任何條文，回報後經確認以 15 條續行 |

**原 14 條之內容未變** —— 其起始行號（20 / 45 / 66 / 82 / 102 / 124 / 136 / 150 /
189 / 208 / 230 / 249 / 264 / 282）與自檢時完全相同，新增者僅追加於 §A 之末。

**一項連帶影響**：執行層已依 R-P193(c) 開立 `DR-PW13`，
而 R-P197 明令**不開 DR** —— 該 DR 於同包內撤回，未送上游。

---

## 十二、§D 全表自驗

| # | 項目 | 期望值 | 實測 | 判定 |
|---|---|---|---|---|
| G125 | 末批產出 | 【實測填入】 | **16 leaf / 34 條**（`224`–`257`）；全集見 §4.1；排除 12 leaf 及其 DR | **已填** |
| G126 | 最終對帳 | 已產出 ＋ 阻斷 ＋ `SWE-PM-089` = 115 | **103 ＋ 11 ＋ 1 = 115** | **PASS** |
| G127 | `SWE-PM-093` 屬性正規化比對 | 【實測填入】 | 正規化後**仍相異五欄**，差集見 §九 | **已填** |
| G128 | R-P183 首次適用 | 全部數字皆已標註；未標註者已回報 | **未標註 2 處已回報**，二者經實測皆正確；`[推導]` 二處實測相符 | **已填** |
| **G129** | leaf `reasoning` 非空 | 皆非空；刻意置空 → FAIL | **103 / 103 合格**；fixture 四案如期 | **PASS** |
| **G130** | 補測同步更新 | 【實測填入】 | 需更新 **5**、已更新 **5** | **已填** |
| **G131** | 逗號列舉規模 | 【實測填入】 | 出現 **5 次 / 5 leaf**；判別率 **100%**（種子 26）；真缺口比例 **2 / 5 leaf、6 個列舉項** | **已填** |
| **G132** | 適用性條件盤點 | 【實測填入】 | **40 / 103 leaf**，清單與處理見 §8.1 | **已填** |
| **G133** | 重疊對掃描 | 【實測填入】 | 重疊對 **27**（錨點相同 5 / 不同 22），門檻 **0.60** | **已填** |
| G70 | lint 全閘 | 全 PASS | **257 TC，阻斷類 PASS，exit = 0** | **PASS** |

**無 MISMATCH。**

補驗：**G0 7 / 7**、**G94 103 / 103**、**G99 103 / 103**、**G103 103 / 103**、
**G108 7 / 7**、**G113 self-test 5 / 5**、**G121 PASS**、**G38 連號遞增無跳號**。

---

## 十三、§F Anomaly 異動

**A-PW139 ~ A-PW142**（A-PW01 – A-PW142 連續無缺）：

| 號 | 摘要 |
|---|---|
| A-PW139 | `SWE-PM-014` 三條 TC 以不在其 `source_clause` 內之參數為前提；G82 攔不住 pre_conditions |
| A-PW140 | `SWE-PM-026` 規格原文自我矛盾（OR 對後又單獨重複駕駛門、連續二個 `AND`）|
| A-PW141 | 三類適用性條件無可賴來源且未為 R-P197 涵蓋；**不另開 DR**，登記待裁 |
| A-PW142 | 逗號列舉型「只取其一」實測存在，**6 個列舉項未覆蓋且本包未補** |

§F 另二項（R-P183 / R-P185 所生者）已由該二條之執行層回報承接，未另開號。

---

## 十四、§G DATA_REQUESTS

DR-PW9（**已依 R-P185 修訂阻斷欄**）、DR-PW10、DR-PW11、DR-PW12 沿用。
**DR-PW13 曾開而於同包內撤回**（R-P197 明令不開）。

---

## 十五、裁決條文與台帳

**§A 十五條逐字抄入 `RULINGS.md`**（逐條以字串比對確認相同），各附執行層回報。
`RULINGS.md` 現為 **R-P1 – R-P197 連續無缺**。
R-P184 與 R-P193 已加註且原文位元組未變。§F 四項已入 `ANOMALIES.md`。

---

## 十六、執行層自判：本包仍有該驗而未驗者

**有，五項。**

1. **交付件中留有 6 個已知而未補之缺口**（A-PW142）。
   R-P192 說「先量規模，不先立閘門」，我照做了 ——
   **但這意味著末批交付時，`SWE-PM-013` 與 `SWE-PM-103` 的六個點火狀態沒有 TC，
   而我知道。** 這是本包最該被優先裁定的一項。

2. **R-P183 之機制，本包就出現它擋不住的情形。**
   該條要求執行層在 §J 自檢時攔下未標註之數字 ——
   而本包真正出問題的不是數字沒標註，是**下放包在自檢之後被改了**。
   §J 自檢只在開工時做一次，R-P183 亦然；**二者都沒有涵蓋「執行期間變更」**。
   本次是我在步驟 9 才偶然發現（因為要抄條文而重讀 §A），不是機制攔下的。

3. **`SWE-PM-014` 之依據越界（A-PW139）是我在查 R-P193 前提時順帶發現的，不是閘門抓的。**
   G82 只掃 ER，不掃 `pre_conditions` —— **同一形態在 `pre_conditions` 裡至今無人量過**，
   本包也沒量（只查了這一個 leaf）。

4. **六批 257 條之中，分析層看過的仍只有前 43 條。**
   §K 第 2 項已自判為六包積欠，本包再加 34 條。
   我補寫的 103 份 `reasoning` 使覆核**變得可能**，但覆核本身沒有發生。

5. **G129 之門檻是我用現有語料校準的，而現有語料本身可能太薄。**
   26 字之樣本能通過，代表這道閘攔不住「寫了等於沒寫」。
   我照實把門檻和它的限度都寫出來了，但**沒有能力判斷 20 字是不是夠** ——
   那需要重讀批次一 ~ 三的 22 份 `reasoning` 並重評，本包未做。
