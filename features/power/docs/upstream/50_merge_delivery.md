# 50 — 併入第七批、三代對照與交付說明更新

上繳包 | 執行層 → 分析層 | 往返 NN = 50

§J 自檢**二次**（R-P200(c)）：執行前與抄錄前皆 **§A 5 / §J 5 / §H 步驟 9「五條」**。

現行最大號（執行前實測）：R-P326 / A-PW320 / DR-PW15 → 本包 A-PW321 – A-PW328。
備份 `sandbox/backup/20260819T022421Z`（208 檔，R-P234）。

**本包未寫回**（R-P323）。`output/` 現無交付檔 —— 舊副本已作廢移入 `superseded/`。

---

## ⚠ 一、本包最重要之發現 —— **51 包寫回之阻斷項**

### G99 於第七批 FAIL：**103 / 111**（A-PW321）

| | |
|---|---|
| 舊 103 個 leaf | **全數通過** |
| **第七批 8 個 leaf** | **全數 FAIL** |
| 缺漏之錨點 | **15 個**，其中 **13 個有實際內文** |
| 最大者 | **`4941453`，4,259 字元**（TLM 內部狀態對照表） |

即：**第七批之 `source_clause` 材料上不完整** ——
037 歸屬於該 8 個 leaf 之規格文字，有相當部分未被抄入。

**⚠ 此為執行層之漏。** 49 包 §D 稱「G0–G228 沿用，期望值不變」，
而該包**未執行 G99**，致缺陷未被發現即上繳。

**G94 為何 PASS**：其比對之對象為**已宣告之** `source_anchor` ——
宣告窄，抄錄自然對得上。**自洽而不完整**，
與 **R-P293** 所指者同型：能自洽者唯逐條查方可見。

**處置：本包不自行修補。** 修補須重寫 8 個 leaf 之
`source_anchor` / `source_clause` 並重產其 TC，
其後果為 **TC 數再變、最終 `tc_id` 再重編、本包所建之三代表作廢** ——
**此非執行層可自裁之範圍**（R-P320 亦限制可自該等錨點取用之內容）。
**停並上繳，待下放包裁定。**

> **在其解決之前，即使 Pei 授權亦不應寫回。**

---

## 二、B1 / G229 —— G113 78 支逐支裁決（R-P325）

### ⚠ 二項訂正

**（甲）78 為全語料合計，非第七批。** 49 §九第 3 項寫成「本批 78」為**執行層之誤**，
R-P325 承襲之。實測逐批：`001` 4、`002` 31、`003` 16、`004` 8、`005` 15、`006` 1、
**`007` 僅 3**。

**（乙）桶名之出入。** R-P118(d) 第三桶為 `已由他條涵蓋`，R-P325 改為 `已由他 leaf 涵蓋`；
本批絕大多數為**同一 leaf 之他 TC** 涵蓋，依後者字面無桶可入。
**以 R-P118(d) 之桶為準，「他 leaf」另計為子桶，未改任一條之文字。**

### 裁決結果 —— **78 / 78，無一沉默**

| 桶 | 計數 | 佔比 |
|---|---|---|
| 已由他條涵蓋（同 leaf 之他 TC） | **65** | 83.3% |
| 已由他 leaf 涵蓋 | **3** | 3.8% |
| 規格未定義該支 | **10** | 12.8% |
| **真缺口** | **0** | **0%** |

**`真缺口` = 0 → R-P325 之「須補測後方得寫回」未觸發。**

> **此 0 不是目標，是結果。** 78 支**每一支皆附完整理由**（非 ≥16.7% 抽樣，而是 100%），
> 其中 **42 支**（實測）指名一條具體 TC 之標題、ER 逐字內容或 pre_condition。
> 已寫入 **25 個 leaf** 之 `reasoning`。

`規格未定義該支` 之 10 支集中三型：外部文件參照（HMI Spec ×2、VF210、See HMI）、
說明性舉例之並列（`for example … rather than USB OR BT streaming audio` ×4）、
**SIS 之電壓門檻 ×2**（即 49 包 R-P320(c) 已上繳者）。

### ⚠ 遭驗證條件否決之謂詞二版（A-PW325）

**版本一 df 門檻**：`equal` df=13 / `user` 12 / `due` 11，
**而已知真缺口標記 `pre`=8、`ltm`=8**，與 `case`/`logic`/`need` 同帶 ——
**任何濾得掉 `equal` 之門檻都貼著 `pre`/`ltm` 過**。**不得使用**（R-P250）。

**版本二子謂詞 (b) 詞幹前綴**：驗證條件**當場攔下** ——
**`pre` 被判為 `pres` 之前綴變體而排除**。
`mod` ⊂ `mode`（切詞產物）與 `pre` ⊂ `pres`（真缺口）**詞形同型，無法分辨**，故刪。

**版本三（採用）**：只留標點變體與黏連產物 —— **只解 1 支，77 支落人工**。
機械層之貢獻極小，此為實情。

**驗證環境本身亦曾為誤**：初版以現況全語料為環境，
而 `off-tim`/`ltm` 於現況 TC 中本就在（缺口已補齊），必然命中而無意義；
訂正為**當時快照**（與 G113 自驗同源）。

---

## 三、B2 / G230 —— 三代 `tc_id` 對照表（R-P324）

`data/tcid_three_gen_50.md` ＋ `data/final_tc_id_map_50.tsv`，**280 列**。

| 代 | 涵蓋 | 號段 |
|---|---|---|
| （一）歷史臨時號 | 3 個時點 | — |
| （二）交付副本最終號 | **260** | 001–260 |
| （三）新最終號 | **280** | 001–280 |

### ⚠ R-P322 之「後移約 20 號」低估 —— 實測 **260 / 260 = 100%**（A-PW328）

排序鍵 `(SWE-PM ID, split_index)` 使 `SWE-PM-001`–`009` 全部插在最前，
**其後每一條皆位移，無一條維持原號**。
**舊副本之任一號碼於新版皆指向另一條 TC。**

### 2 條鍵斷裂已人工補齊，殘留 0

`SWE-PM-091`「keeps the Day theme regardless of the day night signal」→「uses the Day theme」；
`SWE-PM-092` 同構。依據為同 `req_id` ＋ 同 `split_index` ＋ 同一行為之標題改寫，
以 `ALIAS` 表明列於腳本內。補齊後歷史涵蓋 **266 / 266**（前 264 / 266）。

**判準三版並陳（A-PW326）：29 → 3 → 0。**
初版含第七批 20 條與 27 包補測所增者（皆非斷裂），且過濾式**誤取 `NR1L` 之 `1` 為號碼**；
再訂正「被併入者以**鍵**辨識而非號碼」後歸零。

---

## 四、B3 / G231 —— 舊交付副本作廢（R-P322）

移至 `output/superseded/SUPERSEDED-20260819__…xlsx`，**未刪除**；
移動前後 SHA256 **位元組相同**（`dc0d2ee0…`，106,319 bytes）。
`output/superseded/README.md` 載作廢日、理由、取代者（**51 包，尚未產出**）與換算指引。

對照表之作廢記於 `data/SUPERSEDED_47_maps.md`；
**`final_tc_id_map_47.tsv` 與 `tcid_final_map_47.md` 二原檔一字不改** ——
其價值在記錄 47 包當時之指派（R-P233 之 (c) 型），改寫即摧毀該紀錄。

---

## 五、B4 / G232 —— 全量 dry-run **281 列，十項全數 PASS**

| 項 | 50 包（281 列） | 47 包實際寫回（261 列） |
|---|---|---|
| DV（含 x14）未變 | PASS | PASS |
| 合併儲存格未變 | PASS | PASS |
| 條件式格式未變 | PASS | PASS |
| 分頁清單未變 | PASS | PASS |
| 壓縮成員清單未變 | PASS | PASS |
| 凍結窗格未變（G95） | PASS | PASS |
| 欄寬未變（G95） | PASS | PASS |
| B 欄序號逐列相符 | PASS | PASS |
| 列序依 `(SWE-PM ID, split_index)` | PASS | PASS |
| 留白列落於 `SWE-PM-089` | PASS | —（47 包以「最終 tc_id 逐列相符」與「來源未改動」二項代之） |

DV 5 → 5；x14 1 → 1。**僅對 `sandbox/` 副本為之，未觸客戶樹與 `inputs/`。**

---

## 六、B5 —— 交付說明更新（R-P326）

### ⚠ (b) 之二處錯誤，依實測書寫（A-PW327）

本條列 `SWE-PM-003`（DR-PW5）、`008`、`089`、`112`。實測：

- **`SWE-PM-003` 已於第七批產出** —— DR-PW5 為 **advisory**，不阻斷
  （**49 包上繳已逐一確認並載明**，本條未採而重述舊狀）
- **漏列 `SWE-PM-010`** —— 受 **DR-PW11** 阻斷

**實測未產出者 `008` / `010` / `089` / `112`，共 4 個；已產出 111 / 115。**

五項落實：(a) 三分布重測（Power State 125 → **145**、P0 157 → **177**、決策表 139 → **159**）；
(b) 依實測；(c) DR-PW6 之結案界線；(d) 作廢紀錄 ＋ 100% 位移警語 ＋ 三代表；
(e) 圖形界線並明載**「未被用於推想」係執行層自述、無閘門可證**。
另更新 §六（通讀母體 260 → 280，仍 **0%**）與 §八（送達前置增列 (e) 寫回授權）。

---

## 七、G0 之二處處置（A-PW322）

49 包依 R-P319 新建之 `inputs/derived/` 使 `gate0()` 拋 `IsADirectoryError` —— **完全不產出結果**。
另 `q1`–`q4.jpg`（**49 包所產之 O829 圖四象限切圖**，經目視確認）留在 `inputs/` 未登記。

**R-P187 之並陳**：**崩潰（無結果）** → 只跳目錄後 **7/7 相符但 6 個「多」而 FAIL**
→ 補入 49 包 R-P319 已登記於 §B 之二份 RTF（`LEDGER` 7 → 9）後 **9 / 9 PASS**。

**其方向有利於通過，故明載結構性理由**：程式內之 `LEDGER` 是 §B 台帳之**副本**，
**落後於台帳者是副本不是台帳**；且**嚴格集合相等之判準一字未動**，
任何**未登記**之檔仍會 FAIL。四個切圖已歸位 `inputs/derived/` 並登記。

另實測 8 個 `find()` pattern 對 `inputs/` 全部檔名**無多重匹配**（A-PW323）——
`find()` 取第一個子字串匹配，而新入庫之 `4942177-…` 排序在全部素材之前，**若碰撞會靜默取錯檔**。

---

## 八、§D 全表自驗

| # | 項目 | 實測 |
|---|---|---|
| G0 | 素材身分 | **9 / 9 PASS**（見 §七） |
| G229 | G113 78 支裁決 | **78 / 78**；三桶 65 / 3 / 10；**真缺口 0** —— **PASS** |
| G230 | 三代號對照表 | 280 列、三代齊備、2 條斷裂補齊、**殘留 0** —— **PASS** |
| G231 | 交付副本作廢 | 已移入 `superseded/`、**未刪除**、檔名已加註、SHA256 相同 —— **PASS** |
| G232 | 全量 dry-run 281 列 | **十項全 PASS** |
| G70 | lint 全閘 | **exit 0** |
| G94 | `source_clause` 保真 | exit 0（**⚠ 其對象為已宣告之錨點，見 §一**）|
| **G99** | **錨點集合相等** | **103 / 111 —— FAIL（§一，阻斷 51 包）** |
| G108/G177 | 腳本語法與符號 | PASS |
| G113 | OR 分支 | 78（不判 FAIL，已全數裁決）|
| G121 | 全量對帳 | PASS |
| — | `verify_content_distinct` | exit 1 —— **既有項**，即 DR-PW12 之五對重複登載，非本包所生 |

---

## 九、執行層自判：本包有該驗而未驗者

1. **G99 之修補範圍未評估。** 本包只發現與上繳，**未量測**修補後
   第七批之 TC 數會如何變、`4941453` 之 4,259 字元會產生多少新測項。
   該評估需重讀該表並重判 §5.7 之切分，非本包 §H 所涵蓋。
2. **G113 之 78 支裁決由執行層一人為之。** 其中 65 支之依據為
   「同 leaf 之他 TC 已涵蓋」，而**該判斷本身即是一次通讀** ——
   惟其為執行層自判，**不構成第二方複核**（已載入交付說明 §六之註）。
3. **280 條之第二方通讀仍為 0%。** 連續多包之積欠，本包未改變。
