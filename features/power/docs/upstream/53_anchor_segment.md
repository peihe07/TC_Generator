# 53 上繳 — test_item 首段改以錨點為單位

執行層 → 分析層 | 往返 NN = 53

## 一、錨點歸屬複核素材（R-P345(b)）—— **置於最前**

`features/power/data/anchor_review_53.md`

母體 **155** 條（另 125 條之 leaf 只有一個錨點，歸屬唯一，不入母體）。
抽 **31 / 155 = 20.0%**，種子 **53**，**按 leaf 交錯**（round-robin）涵蓋 **31 / 40** 個 leaf。
依序抽只會集中於少數 leaf —— R-P316 之教訓已套用。

**風險（R-P345(d)）**：改值先於複核，280 條之 `test_item` 已寫入。
提案器自驗 **5 / 6**，以之推 155 條中約 **26** 條可能有誤，本抽樣預期捕獲約 **5** 條。

## 二、§J 自檢

抄錄前重驗（R-P200(c)）：**§A 頂層 block = 5、§J 列數 = 5、§H 步驟 10 =「五條」，三處一致。**

## 三、裁決條文抄錄

| 批 | 條文 | 逐字核對 |
|---|---|---|
| 51 包（52 包補抄） | R-P327 – R-P335 | 9 / 9 |
| 52 包（本包步驟 3 補抄） | R-P336 – R-P341 | **6 / 6** |
| 53 包（本包步驟 10） | R-P342 – R-P346 | **5 / 5** |

R-P337 / R-P338 依 R-P36 原文不改，各加註指向 R-P342 – R-P344。
§F 六條入 `ANOMALIES.md`：**A-PW329 – A-PW334**（開新號前實測現行最大為 A-PW328）。

## 四、§D 全表自驗

| # | 期望 | 實測 | 判定 |
|---|---|---|---|
| G242 | 中位數逾 735 即停 | min 33 / 中位數 **188** / P95 432 / max 4,259 | **PASS**，不停 |
| G237 | 三判準；fixture 驗明會 FAIL | fixture **7 / 7**（六紅一綠）；全批 **269 / 280** | **FAIL 11**，見 §六 |
| G238 | 首段無自加前綴 | 全批 0 違規 | PASS |
| G241 | 全批有主要錨點與依據；抽樣 ≥ 20% 按 leaf 交錯 | 280 / 280；31 / 155 = 20.0%，種子 53 | PASS |
| G239 | 四狀態 ＋ 二組 Source 皆有 TC；ER 已精確化 | **未產出** | **未完成**，見 §五 |
| G65′ | 擴及 ER；逐條回報；命中者已處置 | fixture 三向 PASS（含新增之 ER 面）；全批命中 **0** | PASS |
| G240 | 差異已列出並判定 | **8 / 8 相符** | PASS |
| G70 | 全 PASS | 阻斷類 PASS（exit 0）；319 項 R-P42(b) 待裁為既有 | PASS |
| 素材層 | 依 R-P327 重跑 | G0 9/9、G99 111/111、G94 111/111、G103 111/111 | PASS |

### G240 之獨立性界線（R-P339(b)）

獨立者為**錨點辨識與內文歸屬之邏輯** —— 正則於 `reextract_clause_53.py`
內獨立宣告，不 import `lint_tcs`、不呼叫 `anchor_bodies()`。
**⚠ 段落切分非獨立** —— 其輸入 `data/textlayer/*_plain.txt` / `*_bold.txt`
係 `extract_textlayer.py` 自 CFTS 原始檔所產，與 `anchor_bodies()` 共用該層。
**若段落切分本身有誤，本閘驗不出來。** 此界線已寫入該檔 docstring，
不得以 8 / 8 宣稱「完全獨立驗證」——**否則即重蹈 R-P339 所指之錯。**

## 五、G239 未完成 —— 其中三項受 DR-PW11 阻斷

材料見 `data/g239_state_table_53.md`。

R-P340(b) 之六項補測，**三項受既有 blocking DR 擋住**：

`Logistic Idle` / `Logistic Standby` / `Logistic Sleep` 之定義錨點
`4941427` / `4941432` / `4941435` **全屬 `SWE-PM-008`**，
而該 leaf 受 **DR-PW11（High, live）** 阻斷 ——
其被引用錨點 `4941425` / `4941430` / `4941433` 於兩份 CFTS 文字層皆無內文段落，
`source_clause` 於原理上無法完整。該 leaf 自第三批起排除，
為 111 / 115 之未產出四者之一。

**故該三項非「不補」，而是其 leaf 尚未解阻** —— 不觸 R-P328(e)。
本包 §G 載「DR-PW11 blocking」而未指出其與 R-P340(b) 之交集；
**其形態與 R-P330(c)「未回查 DR 之 blocking 屬性」同型，為同一形態之第二次**（A-PW334）。

**可行之三項未產出**：`Bench`（`SWE-PM-007`）、
`Full-Operation` 二列與 `Timed` 二列之 `Source` 分辨（`SWE-PM-001` / `SWE-PM-004`）。
其未產出之理由**非號碼穩定**（R-P328(e) 所禁者），而是本包工作量已逾一次往返所能承載 ——
據實回報，不飾。新增 TC 將使 `tc_id` 再變並連動三代對照表，宜與 B6 同批處理。

## 六、G237 FAIL 11 項 —— 本包二條新條文互斥

11 條全部源於**同一個錨點** `4942354`（`SWE-PM-073`）：**其單一段落內含一個空行**。

- **R-P343(a)**：首段 = 錨點之文字，**逐字**，不切句、不省略、不重組
- **R-P342(a)**：`test_item` 含**恰一個空行**

對該錨點二者無同時成立之解。**執行層未自行折衷** ——
摺除空行違反「逐字」，改判準則屬自行改寫本包所立之閘。
故**保留逐字、令 G237 據實叫 11 項**，待 54 包裁。

**此為同一包內二條新條文互斥之第一例**（A-PW333）；
前三例（A-PW329 / A-PW330 / A-PW331）皆為跨包。
其共同形態為**一次立多條要件而未相互檢查**，51–53 三包連續三次。

## 七、B1 之實作與量

首段依 R-P343 逐字取自主要錨點，**不切句**；22 條之錨點逾 1,000 字元，
依 (c) 取連續片段並將**起訖字元位置**記入 `reasoning_note`。
`4941453` 之片段依 R-P344 照原分隔逐字取 —— **未加 `=`、未加 `,`、未重排欄序**。

後段 `(<操作> -> <預期結果>)` 之形態取自 Comfort 實測：
465/465 以 `(` 起 `)` 止、**461/465** 含 ` -> `、0 筆含換行、0 筆末句點、
**465/465 之操作段首字為小寫**（末項 R-P333 未定，依實測採用）。

⚠ **Comfort 自身有 4 列不含 ` -> `** —— G237(c) 對其參照語料有 4 個反例。
與 A-PW329 同型（判準取自語料而語料自身不全然符合），惟數量小，記之。

## 八、選句器之處置（R-P345 追認）

`scripts/pick_first_segment.py` **未套用**，保留為紀錄。
其自驗 4 / 6、全批覆蓋率 < 0.30 者 103 / 280。
二個未過者屬**無寫法可分辨之類**：肯定 vs 否定、二句皆述同一轉入而區辨全在條件子句
——與 R-P332 之 `mod ⊂ mode` 同型，**非調參可解**。
調整二次後停手（IDF 加權、合併改為語義條件），未再調 ——
**再調即為「先看答案再定門檻」**（本 feature G73 已有調三次之前科）。

## 九、未完成，承接至 54 包

- **B3**（G239）—— 可行之三項未產出；受阻之三項待 DR-PW11
- **B6** 承接 51 包 B2–B6 —— 20 條重新檢視、三代對照表重建、
  R-P330 三處訂正與加註、全量 dry-run、驗證邊界更新
- 驗證邊界應增列：Comfort 首段長度對照（R-P342）、
  首段以錨點為單位之依據（R-P343）、
  **其餘交付欄位之格式規則未查**（§K 第 3 項）、
  G240 之獨立性界線（§四）

## 十、執行層獨立判斷

1. **`test_item` 三包未成，其成因每次都是同一件事** ——
   分析層一次立多條要件而未相互檢查。51 包（逐字／不省略／長度上限）、
   52 包（重組形式／連續子字串）、53 包（逐字／恰一個空行）。
   **R-P335 令就地增補須檢視六處，惟該機制查的是「節與節之間同步」，
   查不到「同一節內二條要件是否相容」。**
2. **提案器 5 / 6 與選句器 4 / 6 之檻不同，係因其後續處置不同，非因準確率不同。**
   R-P345 為前者配了抽樣複核，R-P250 對後者要求的是直接可用。
   **若 54 包為選句器補上同等的複核機制，其 4 / 6 亦不必然不可用** —— 此點供裁。
3. **本包之閘全綠者中，G94 與 G240 皆有已知的效力界線**（產生器同源、段落切分共用）。
   二者合計覆蓋 8 個重建 leaf，而該 8 個 leaf 之 `source_clause` 佔全語料字元數之多數。
   **「素材層四閘全 111/111」之陳述，其強度低於其數字所示。**
