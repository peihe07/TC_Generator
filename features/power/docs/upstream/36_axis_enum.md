# 36 — axis 列舉值、死列裁定與樣式謂詞通則

上繳包 | 執行層 → 分析層 | 往返 NN = 36

§J 自檢**二次**（R-P200(c)）：執行前與抄錄前皆
**§A 區塊數 5 / §J 列數 5 / §H 步驟 9「五條」**，三處一致（`mtime` 未變）。

**本包未改任何 TC 值**：`git diff --stat -- features/power/generated/` 為空。
現行最大號（R-P147）：R-P247 / A-PW200 / DR-PW15 → 本包自 R-P248、A-PW201 起。

---

## 一、B1 —— 第 4 / 8 列之複核素材（R-P252）**置於最前**

**素材本體：`features/power/data/review_material_36.md`（55 KB）。**

- **第 4 列**：母體 **79** 條（80 − `…-099`），抽 **14 條 = 17.7%**，
  種子 `random.Random(36)`（種子＝包號）。
  抽中 `…-009` `…-014` `…-029` `…-031` `…-032` `…-075` `…-096` `…-103`
  `…-106` `…-133` `…-139` `…-191` `…-223` `…-228`。
- **第 8 列**：**全 7 條** —— `…-007` `…-008` `…-016` `…-025` `…-026` `…-027` `…-028`。

每條逐字呈現：`tc_title` / **`source_clause` 全文（未截斷）** / clause 之條件子句 /
`pre_conditions` / 實質前提 / `input_test_data` / `test_procedure` /
`expected_result` / 執行層所判之「二個以上條件」/ 現值 `design_method`。

**本檔不作判定、不作摘要** —— 摘要會把複核者之判斷替換成執行層之判斷，
而本素材之目的正是取得**獨立**之第二方判斷（R-P214）。

第 8 列之複核問題另列：**該 ≥ 3 步是否確為「跨功能」**（現行謂詞只驗步數）。

---

## 二、B2 —— `axis` 全批重判（R-P248 / G173 / G174）**最高優先**

### 2.1 現行違規：**254 / 264 = 96.2%**

| 現行值 | 條數 | 合法 |
|---|---|---|
| `behaviour` | 245 | **否** |
| `trigger_state` | 6 | 是 |
| `branch` | 6 | **否** |
| `timing` | 3 | 是 |
| `trigger` | 3 | **否** |
| `input_data` | 1 | 是 |

⚠ 裁決條文列舉 `behaviour` 與 `trigger`，**未提及 `branch`（6 條）** ——
非法值實為**三種**而非二種（A-PW201）。

### 2.2 判準之選擇先經語料量測（R-P250）

`delta` ＋ `split_reason` 之詞頻實測：「分支」**112**、「狀態」30、「Standby」25、
「觸發」17、「模式」10、「界線」8、「時序」3；而「輸入」/「boundary」/「duplicate」皆 **0**。
**最高頻之「分支」不對應任一 axis** → 散文不足為判準基礎。

改以**該 TC 相對其同 leaf 姊妹之 token 層結構差異**為判準
（`axis` 之本義即「此二條藉何者區分」）。
判序：`none` → `boundary` → `timing` → `trigger_state` → `mode` → `input_data`。
`boundary` 先於 `timing`（界線值多以時間表述）；
`trigger_state` 先於 `mode`（模式常為觸發之後果，以主動施加者為軸）。

### 2.3 重判提案

| 提案 axis | 條數 |
|---|---|
| `input_data` | **92** |
| `trigger_state` | **68** |
| `mode` | **42** |
| **無對應** | **40** |
| `timing` | 17 |
| `boundary` | 5 |

**無對應之 40 條**：其 leaf 僅產出 1 條 TC，無他條可區分，
而六值皆預設「與他條之區分」。**未新增列舉值**；處置屬分析層（A-PW203）。

### 2.4 ⚠ 本包最實質之發現：`SWE-PM-025` 二對 TC 四欄逐字全同

`…-087` ≡ `…-091`、`…-088` ≡ `…-092` ——
`pre_conditions` / `input_test_data` / `test_procedure` / `expected_result` **逐字相同**，
而 `tc_title` 分載 `Front_Panel_OnOff.Req` 與 `CLIMATIC_PANEL.Radio_Btn0`
（即 R-P179 所裁之二觸發訊號）。

**該二觸發訊號未寫入 TC 內文** —— 非重複 TC，而是
**TC 內文未實現其宣稱之區分**：依現行內文執行，二條之操作完全相同。
正確 axis 為 `trigger_state`；內文須於 37 包補入該觸發。

**`G168` 之 C5 抓不到此形態**（C5 只比 `delta`），係由重判之四欄比對所得（A-PW202）。

### 2.5 雙向契約（G174）

C4（`axis="none"` ⇒ `duplicate_of` 已設）＋ **C7（`duplicate_of` 已設 ⇒ `axis="none"`）**。
fixture **10 案全數如期**（含 C6 二案、C7 一案、六值皆合法一案）。

⚠ **全批 `axis="none"` 為 0、`duplicate_of` 為 0，故 C4 / C7 於本批語料皆不觸發** ——
其正確性僅由 fixture 支撐，**未經語料檢驗**，據實標明。

### 2.6 已知限度（量化）

`input_data` 之 92 條中，其 token 差**全為裸數字**者 **3 條** ——
該形態下參數名二條相同故不入 token 差。
例：`…-018` vs `…-019` 之差為 `20 60`，實為 `Switch_Off_Time` 20 vs 60 分鐘，
**真值應為 `timing`**。**不回頭調整判準**（17 §I），標為已知偏低估（A-PW209）。

**本包未改任何 `axis` 值。**

---

## 三、B3 —— first-match 壓抑之查（R-P249 / G175）

| 列 | 獨立命中 | first-match 實得 | **被前列吸收** | 落點 |
|---|---|---|---|---|
| 5 等價劃分 | 21 | 2 | **19（90.5%）** | 第 4 列 14、第 3 列 4、矛盾 1 |
| 6 界線值 | 7 | 6 | 1 | 第 5 列 1 |
| 7 組合 | —（無謂詞） | **0** | 9 對候選、6 對真組合 | 第 3 / 4 / 6 / 8 / 9 列 |
| 8 情境 | 16 | 7 | **9（56.3%）** | 第 4 列 6、矛盾 1、第 5 列 1、第 6 列 1 |

**第 5 列受壓抑之比例最高（90.5%），高於第 8 列（56.3%）。**
**吸收者以第 4 列為主**（第 5 列 14 / 19、第 8 列 6 / 9）——
與第 7 列同源：第 4 列「多條件 → 結果」涵蓋面極廣。

裁定（第 7 列非缺陷、係 first-match 之設計行為）**逐字接受**。

---

## 四、B4 —— 樣式謂詞之形態量測（R-P250 / G176）

### 4.1 本條在本包內攔下三次真缺陷（皆於產出任何結果之前）

| # | 缺陷 | 攔下之方式 | 訂正 |
|---|---|---|---|
| 1 | `diff_text` 取**行層**差異，二條**共有**之 token 混入 —— `…-002`/`…-003` 僅差 boot target，而差異行同含二條皆有之 `SplashScreen_Time` | 實例驗證：`…-002` 期望 `mode` 實測 `timing` | 改 **token 層** |
| 2 | `boundary` 沿用 G6 之 `ROW6_RE`，命中裸詞 `limit`（多來自「the volume limit returns to its normal maximum」之偶然共現） | 逐條核對 7 條：**3 條為偽**（token 差 45 / 40 / 48）、4 條為真（token 差僅 7） | 另立 `BOUNDARY_RE`，只取顯式比較形態 |
| 3 | 稽核之**量測設計**二誤：單一語料測全部謂詞；摺疊空白連換行摺掉 | v1 誤報 **30** 個謂詞「不得使用」 | 分五類語料、行首錨定加 `re.M`、摺疊保留換行 → 實為 **7** |

### 4.2 ⚠ 另攔下二次，其錯在**執行層之期望值**而非謂詞

`…-001` 與 `…-008` 皆被**未讀即推想**為 `timing`，送驗後皆是**期望值錯而謂詞對**。
定版之 16 案期望值**皆經實讀該 TC 與其對照條後確認**，腳本內逐案載明實讀依據。

**此印證 §K 第 2 項之疑慮**：實例之選取由同一方為之，
其品質取決於該方是否真的讀了（A-PW206）。

### 4.3 既有 82 個謂詞之回溯稽核

語料五類：TC 六欄 / `source_clause` / 識別子 / **文字層**（`data/textlayer/`）/ 檔案路徑。

| 項 | 數 |
|---|---|
| **五類皆命中 0 —— 取不到已知應命中實例** | **7** |
| **大小寫敏感** | **9** |
| **空白敏感** | **21** |

**（甲）命中 0 之 7 個皆為 `PD_RE`**（`Sys-RA-PD[_-]\d+`），分布於
`build_b1` / `build_b2` / `build_layer3` / `verify_gates` / `verify_gates_03` / `verify_layer3`。
依 R-P250 **不得使用**；惟其可能係上游 `Sys-RA-PD` 類需求未進入本 feature ——
**判定屬分析層**，執行層不逕行移除。

**（乙）大小寫敏感之前五**：

| 模組.謂詞 | 語料 | 現行 | 加 `re.I` | 增幅 |
|---|---|---|---|---|
| `or_branch_coverage.GLUED_OR_RE` | 文字層 | **2** | **2408** | +2406 |
| `build_residual_sample.GLUE_RE` | 文字層 | 130 | 7022 | +6892 |
| `audit_precond_state.MODE_RE` | 文字層 | 12538 | 69022 | +56484 |
| `lint_tcs.ER_PROPER_RE` | 文字層 | 31324 | 128707 | +97383 |
| `reverse_coverage.NAMED_RE` | 文字層 | 37005 | 69327 | +32322 |

**`GLUED_OR_RE` 之 2 → 2408 最須注意**（23 包 OR 黏連之偵測謂詞）。
**惟增幅大不等於應改** —— `ER_PROPER_RE` / `NAMED_RE` 刻意以大寫辨識具名標的；
`rejudge_priority` 之 `CAN` 更是刻意區分（34 包 v1 之缺陷即誤加 `re.I`）。
**逐一裁決屬 37 包，本包只出量測不改謂詞**（17 §I）。

**（丙）空白敏感 21 個**，含本包新建之 `rejudge_axis.MODE_RE` / `BOUNDARY_RE`
與 `confirm_row4.COND_RE`。

---

## 五、B5 —— G108 符號層修正（R-P251 / G177）

**修正前後之實測對比即為 R-P251(c) 之直接證據**：

| | 修正前 | 修正後 |
|---|---|---|
| 輸出 | `G108：66 / 66 完整` | `完整 63 / 66；FAIL 0；**無基線未驗 3**` |
| `exit` | **0** | **1** |

同一份檔案、同一時點。舊版之 `symbols_ok is not False` 使無基線者**預設通過**。

**（b）修正當下之基線缺漏 3 檔**（皆本包新建）：
`audit_style_predicates.py` / `build_review_material_36.py` / `rejudge_axis.py`。
同步後 **66 / 66 完整、FAIL 0、無基線 0、exit 0**（759 符號）。

**（c）本包再增一同型實例**：G174 之 C4 / C7 於本批語料皆不觸發
（`axis="none"` 與 `duplicate_of` 皆 0 條），其「無違規」同樣不是「已驗證」（A-PW208）。

---

## 六、B6 —— 交付說明之驗證邊界更新（R-P249）

於 35 §7.2 之六項永久限制外，**增列 `design_method` 值域之三項邊界**：

> **`design_method` 欄之值域邊界（R-P249）**
>
> §12 為 **first-match** 九列，惟本批之實際值域為：
>
> | 列 | 實得 | 說明 |
> |---|---|---|
> | 1 Negative / Invalid | 1 | |
> | 2 Fault Injection | **0** | **謂詞偽陰性** —— 語料中確有 1 條真故障注入（`…-008`），其措詞為 `Stop the broadcast`（A-PW178） |
> | 3 State Transition | 81 | |
> | 4 Decision Table | 79 | 79 條經逐條人工確認（35 包）；**分析層抽樣複核未完成** |
> | 5 Equivalence Partitioning | 2 | **殘餘值** —— 獨立命中 21，**90.5% 被前列吸收** |
> | 6 Boundary Value Analysis | 6 | 獨立命中 7 |
> | 7 Combinatorial | **0** | **結構上不可達** —— 反向查得 9 對候選、6 對真組合，於 first-match 序下全數被前列吸收 |
> | 8 Scenario / Use Case | 7 | **殘餘值** —— 獨立命中 16，**56.3% 被前列吸收**；且「跨功能」未機械化 |
> | 9 Functional Based | 86 | |
> | 矛盾 | 2 | 正向與明示不轉換同時命中 |
>
> **收件方若以「值域涵蓋九列」或以各列數字為「該類測試之總數」理解，將誤判其分布。**
> 第 2 / 7 列之 0 意義不同：第 2 列為謂詞漏判，第 7 列為序之結構結果。

另增列 **`distinguishing_axis` 之邊界**：
> 現行 **254 / 264 = 96.2%** 之值不在 §4.6 之六值列舉內；
> 重判提案已出而**尚未套用**；另有 **40 條**其 leaf 僅產出 1 條 TC，
> **六值皆無對應**，屬契約之覆蓋缺口。

---

## 七、§D 自驗

| # | 項目 | 期望值 | 實測 |
|---|---|---|---|
| G173 | `axis` 六值列舉 | 現行違規數；重判後全批皆映至六值；無可映者已列出 | 現行違規 **254 / 264**（非法值**三種**）；重判提案已出；**無可映 40 條已列出**，未新增列舉值 |
| G174 | `axis="none"` ⇔ `duplicate_of` 雙向契約 | 全批相符；fixture 證明二向皆會 FAIL | 全批相符（二者皆 0 條）；fixture **10 / 10** 如期，C4 / C7 二向皆證會 FAIL。⚠ **語料中不觸發，未經語料檢驗** |
| G175 | first-match 壓抑 | 第 5、7、8 列各自被前列吸收之候選數 | 第 5 列 **19（90.5%）**、第 8 列 **9（56.3%）**、第 6 列 1；第 7 列 **9 對候選（6 對真組合）全數被吸收** |
| G176 | 樣式謂詞形態量測 | 現行謂詞皆已補量；無已知應命中實例者已列出 | **82 個**全數量測；**命中 0 之 7 個已列出**（皆 `PD_RE`）；大小寫敏感 9、空白敏感 21 |
| G177 | G108 符號層 | `None` 不計入完整；無基線者單獨計數 | 已修正；修正前 `66/66、exit 0` → 修正後 `63/66、無基線 3、exit 1`；同步後 **66 / 66、exit 0** |
| G70 | lint 全閘 | 全 PASS | **阻斷類 PASS，exit = 0**；待人工裁決 275 項（R-P42(b) / R-P67） |

**不改值之佐證**：`git diff --stat -- features/power/generated/` 為空。
**全腳本語法**：66 檔，錯誤 0。

---

## 八、帳冊異動

- **RULINGS.md**：R-P248 – **R-P252** 五條逐字抄入 ＋ 執行層回報。
  **R-P1 – R-P252 連續、無重複、無缺號**。
- **ANOMALIES.md**：新增 **A-PW201 – A-PW209**（9 條）；
  **A-PW197 依 R-P248 結案**（§4.6 原文已取得）；
  **A-PW190 依 R-P251 更新**（見 A-PW208）。**A-PW1 – A-PW209 無缺號**。
- **DATA_REQUESTS.md**：無新增（DR-PW1 – DR-PW15 沿用）。

---

## 九、執行層自判：本包仍有該驗而未驗者

**有，四項。**

1. **`axis` 重判之 264 條提案僅 16 條經實讀驗證**（各軸 2–4 例）。
   其餘 248 條之提案**未經逐條人工確認** —— 與 35 包第 4 列之情形同型，
   而該次逐條確認查出 12 條疑似偽陽性中之 1 條為真偽陽性。
   **本次未做等量之確認**，據實標明；若 37 包逕以本提案改值，
   即等於接受一個未經第二方複核之自動判定改動 264 條中之 254 條。
2. **`…-087` / `…-091` 之形態未經全批掃描。**
   本次係由 `axis` 重判之四欄比對**偶然**發現二對；
   **未回頭問「是否另有三欄相同而一欄微差者」** —— 該形態同樣是
   「內文未實現其宣稱之區分」，而現行無任何閘門查之。
3. **G176 之三項量測（命中數 / 大小寫 / 空白）皆為粗篩，非判定。**
   「加 `re.I` 後上升」只說明存在大小寫變體，不說明該變體是否為該謂詞應抓者；
   R-P250 所要求之「已知應命中實例」**未對既有 82 個謂詞逐一取得** ——
   本包只對新建之 `rejudge_axis` 諸謂詞做到。
4. **改值已推遲至 37 包，自 32 包起累計五包未改值**（§K 第 3 項）。
   本包再增 `axis` 254 條與 `…-087` / `…-088` / `…-091` / `…-092` 之內文補寫，
   **改值面較 35 包更大**。若 37 包一次改畢，其改動量將是本專案單包之最大者，
   **而改值本身無任何閘門驗其正確** —— 此風險於此標明。
