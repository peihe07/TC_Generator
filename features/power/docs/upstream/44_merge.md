# 44 — 合併裁定、指令設計通則與寫回

上繳包 | 執行層 → 分析層 | 往返 NN = 44

§J 自檢**二次**（R-P200(c)）：執行前與抄錄前皆 **§A 5 / §J 5 / §H 步驟 9「五條」**。

改前依 R-P234 備份（`sandbox/backup/20260819T005433Z`，191 檔）。
現行最大號：R-P292 / A-PW281 / DR-PW15 → 本包自 R-P293、A-PW282 起。

**TC 264 → 260。本包未對客戶樹寫回。**

---

## 一、B5 —— 待讀素材彙整（R-P297(c)）**置於最前**

**`features/power/data/review_pending_44.md`（23 KB）** ——
42 包交錯素材 **15** 條 ＋ 43 包「無對應」抽樣 **6** 條 = **21** 條，
二份原檔逐字併入，**未作任何摘要或改寫**（原檔仍在）。

⚠ 分析層自 38 包起已四度推遲 `axis` 群之複核；R-P297(c) 已列其為寫回之前置。

---

## 二、B1 —— 四對合併（R-P294 / G206）**最高優先**

合併前先依 **R-P293 逐條查其標定**：四對之 `pre_conditions` / `input_test_data`
**逐字全同**、施加步驟**逐字相同**、**皆同 leaf** —— 標定屬實。

| 保留 | 併入 | leaf | procedure | ER |
|---|---|---|---|---|
| `…-005` | `…-006` | `SWE-PM-072` | 2＋2 → **3** | 2＋2 → **3** |
| `…-100` | `…-102` | `SWE-PM-028` | 2＋2 → **3** | 2＋2 → **3** |
| `…-104` | `…-107` | `SWE-PM-029` | 2＋2 → **3** | 2＋2 → **3** |
| `…-175` | `…-176` | `SWE-PM-076` | 2＋2 → **3** | 2＋2 → 4 → **3** |

### 2.1 ⚠ 合併觸發 §6 之 1:1 衝突（G70 阻斷類 FAIL）

`…-175`/`…-176` 之二 ER **無共同行**，合併後 **4 行 ER 對 3 步 procedure**。
其餘三對因其 ER 首行同為「施加被接受」之共同斷言而恰為 3 對 3 ——
**該巧合掩蓋了此衝突之普遍性**。

**調和**：原 `…-176` 之二 ER 行（`The main CPU resets` / `The CAN micro resets`）
**皆為其「讀二處理器」同一步之後果**，故併為一行
（`The main CPU and the CAN micro reset at the time of the reset`）——
**二斷言逐字保留，未新增亦未刪除事實**，已載於該條 `reasoning_note`。
調和後 **G70 恢復 PASS**（A-PW284）。

### 2.2 ⚠ 臨時號重排方式之訂正

初版以 `(SWE-PM ID, split_index)` 重排，實測**使 260 條全數變號**，
將摧毀既往各包對 `…-NNN` 之引用（§K 第 3 項所慮者）。
改為**保留原 `tc_id` 序、僅補四條被併入者所留之缺口** ——
仍為 001–260 連號，位移最多 4 號。
**舊 → 新對照表附於 `data/merge_44.md`**（A-PW287）。

### 2.3 (d) 另三組經逐條查：**皆跨 leaf，不得合併**

`…-030`/`035`（`SWE-PM-064` vs `SWE-PM-038`）、
`…-136`/`140`、`…-137`/`141`（皆 `SWE-PM-045` vs `SWE-PM-047`）——
其施加步驟確亦逐字相同，**惟三組皆跨 leaf**。
依 §I 與 R-P186（不得代 RD 合併需求單位），**一律不得合併**。
**R-P294(d) 之「是者一併合併」無適用對象。**

### 2.4 連鎖處置

`SWE-PM-072` 合併後僅存 1 條 → 依 **R-P265** 移除其 `axis` 欄（224 → **219**）；
`split_flag` / `split_index` 依 R-P115 重算 **14** 條。

---

## 三、B2 —— 合併與跨 leaf 重複之交集（R-P295 / G207）

**交集 2 條**：舊組 `…-102`/`…-107` 之二條皆為本次被併入者 ——
G178 組數 **13 → 12**（該組因二成員皆被併入而消失）。

**涵蓋數之變動分別計算**：

| 來源 | 性質 | leaf 涵蓋數 | TC 數 |
|---|---|---|---|
| 合併（R-P294） | 同一 leaf 內二條併為一條 | **不變**（103 leaf） | **264 → 260** |
| 跨 leaf 重複（R-P263） | 依 R-P186 **不合併** | 不變 | **不變** |

**故最終條數之變動僅由合併所致：260。**

其餘 12 組於新號下之對應：`099`/`102`、`100`/`103`、`149`/`192`、`150`/`193`、
`151`/`194`、`152`/`195`、`153`/`196`、`154`/`197`、`162`/`225`、`165`/`226`、
`231`/`246`、`232`/`247`。

---

## 四、B3 —— ⚠ R-P296 之事實前提二處不實，處置無適用對象

依 R-P293 逐條查後回報，**未依「處置」措詞逕行補判準**。

**不實之一**：43 包所訂正之 3 條為 `…-063`/`065`/`095`（新號 `…-062`/`064`/`094`），
其 P3 係 `COSMETIC_RE` 命中**參數名** `Brand_Configuration _2` 之偽陽性所致。
**`…-110`/`138`/`139`（後視攝影機）從未被改為 P3**，其自 34 包起即為 P0。

**不實之二**：`rejudge_priority` **未將任一條判為 P3**：

| tc（新號） | 現值 | 判定 |
|---|---|---|
| `…-062` / `064` / `094` | P0 | **提案人工裁決**（無 P0 類別命中，亦非裝飾性） |
| `…-070` / `071` / `081` / `107` / `135` / `136`（後視攝影機 6 條） | P0 | **P0 成立**，命中 `safety` |

**佐證**：現行 P3 **37 條**中命中任一 §10.2 P0 類別者為 **0 條**。

⚠ 43 包全閘重跑之 `rejudge_priority` 180 → 183，其成因為**該 3 條回到 P0
而使受檢範圍擴大 3**，**非該謂詞將其判為 P3** ——
**二者於報表上皆為該閘之數字變動而意義相反**（A-PW285）。

**未改 `rejudge_priority` 之判準。** 受檢範圍（合併後）180 條：
P0 成立 108、提案人工裁決 53、提案 P3 19。

---

## 五、B4 —— 合併後之全量 dry-run（R-P297(b) / G209）

**261 列（TC 260 ＋ 留白 1）**，十項全數 PASS：

| 項 | 43 包（265 列） | **44 包（261 列）** |
|---|---|---|
| DV（含 x14）未變 | PASS | **PASS**（5 → 5，x14 1 → 1） |
| 合併 / 條件式格式 / 分頁 / 壓縮成員 未變 | PASS | **PASS** |
| 凍結窗格、欄寬未變（G95） | PASS | **PASS** |
| B 欄序號逐列相符 | PASS | **PASS** |
| 列序依 `(SWE-PM ID, split_index)` | PASS | **PASS** |
| 留白列落位（R-P141） | PASS | **PASS** |

---

## 六、R-P297 之四項前置

| 前置 | 狀態 |
|---|---|
| (a) 合併完成，260 條 | **已完成** |
| (b) 合併後 dry-run 十項全 PASS | **已完成** |
| (c) 分析層讀畢 21 條素材並出具結果 | **未完成**（素材已備） |
| (d) R-P295 之交集已查 | **已完成**（2 條） |

**本包未對客戶樹寫回**；寫回之實際執行須 Pei 明示授權 —— 逐字接受。

---

## 七、§D 自驗

| # | 期望值 | 實測 |
|---|---|---|
| G206 | 264 → 260；四條各有多 ER 且 `reasoning` 載明依據；另三組已逐對查 | **260**；四條皆載 §5.7 依據；另三組**皆跨 leaf 不得合併** |
| G207 | 交集條數；涵蓋數變動已分別計算 | **2 條**；已分別計算 |
| G208 | 3 條不再判 P3；補入後之受檢範圍 | ⚠ **其前提不實** —— 該謂詞本即未判其為 P3；**未改判準**；受檢範圍 **180** |
| G209 | 260 ＋ 1 列；十項全 PASS；與 43 包對照 | **261 列，十項全 PASS** |
| G70 | 合併後仍全 PASS | **調和後 PASS，exit 0**（初次因 §6 1:1 而 FAIL） |

**全閘重跑**：20 閘門中 10 個數值變動 —— 皆為 260 條之新母體所致（條數、tc_id 位移），
**無新觸發之異常類別**。**G108 / G177**：87 / 87 完整（899 符號）。

---

## 八、帳冊異動

- **RULINGS.md**：R-P293 – **R-P297** 五條 ＋ 執行層回報。**R-P1 – R-P297 無缺號**。
- **ANOMALIES.md**：新增 **A-PW282 – A-PW287**（6 條）。**A-PW1 – A-PW287 無缺號**。

---

## 九、執行層自判：本包仍有該驗而未驗者

**有，三項。**

1. **R-P293 於本包即查出二項標定不實，而該通則不回溯**（R-P255）——
   **先前各包以「處置」措詞下達者，其標定是否屬實均未經查**，其數不可知。
2. **§6 之 1:1 衝突僅因一對而顯現。**
   其餘三對之 ER 首行恰為共同斷言而掩蓋之 ——
   **若四對皆有共同首行，此衝突至本包結束仍不會被發現**，
   而其為合併操作之普遍風險。
3. **合併之 ER 調和由執行層逕為**（`…-176` 之二行併為一行）。
   其未新增亦未刪除事實，惟**「二斷言可併為一行」本身為語義判斷**，
   未經第二方確認。
