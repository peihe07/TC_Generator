# 上繳包 21 —— SYS2 `Need rework`、git 禁區釐清與切片規則擴充

> 對應下放包：`features/power/docs/handoff/21_need_rework.md`
> 執行層：Claude（TC_Generator）
> **本包為補執行**（R-P164，於 23 包）—— 21 包當時未執行，
> 23 包依該條補執行其 §H **步驟 3–9**（步驟 1–2 之 G0 與編號查核併於 23 包）。
> 本包**未執行任何 git 子命令**；**未重開 R-P7 之範圍裁定**；
> **未以本包之新資訊調整已產出之任何 TC**。

**時序聲明**：本上繳包撰於 23 包當下，其所述之狀態為**該時點**之狀態，
非 21 包原訂執行時點之狀態。**凡受此影響者於各節逐一標明。**

---

## 一、B1 —— DR-PW9 與 R-P7 加註（R-P148 / G107）

### 1.1 DR-PW9（High）已開

| 項 | 內容 |
|---|---|
| 問題 (a) | SYS2 CFTS009 匯出之收錄條件為何？檔名之 `All_Accepted` 所指者為哪一欄？ |
| 問題 (b) | 037 引用之 `Sys-RA-PM-0293`（`HARMAN Status` = `Need rework`）對應之 `SWE-PM-112` 是否仍在範圍內？ |
| 阻斷 | `SWE-PM-112` 之範圍歸屬。**不阻斷已產出之 33 leaf** |

### 1.2 `SWE-PM-112` 所引該列之逐字值

| 欄 | 值 |
|---|---|
| `SYS2 Sys-RA-Feature-ID` | `Sys-RA-PM-0293` |
| `SYS2 HARMAN Status`（第 15 欄）| **`Need rework`** |
| `SYS2 MD Status`（第 17 欄）| **（空）** |

CFTS009 全表：`HARMAN Status` 為 `Accepted` **168**、`Need rework` **4**；
`MD Status` 為 `Accepted` 168。四列為
`Sys-RA-PM-0021` / `0291` / `0292` / `0293`，**037 僅引用其中之 `0293`**。

### 1.3 G107 —— R-P7 加註後原文位元組佐證

| 項目 | 值 |
|---|---|
| 加註前 SHA256 | `8ba43d3f3ef866c8da59f6bff2cd84932a957dbc7bf4260ae156ff8da00e0cd4` |
| 加註後 SHA256 | 同 |
| bytes | **388**（前後相同）|
| **G107** | **UNCHANGED** |

加註置於裁決區塊**之外**，內容為：該條之範圍裁定作成時不知 `All_Accepted`
與內容不符，已開 DR-PW9，**本條不因此重開**（R-P148 明令）。

---

## 二、B2 —— R-P150 之編輯規則落實（G108）

### 2.1 形式：**腳本**，非 checklist

`features/power/scripts/check_edit_integrity.py`。
選腳本而非 checklist 之理由：**checklist 依賴記得執行，腳本可被其他步驟呼叫**。

三層檢查：

| 層 | 方法 | 攔得住什麼 |
|---|---|---|
| 語法 | `ast.parse` | 括號、縮排等破壞 |
| 載入 | `importlib` 實際載入 | 語法正確而符號消失致 `NameError` 者 |
| **符號** | 頂層函式／類別／常數集合對**基準快照** | **「刪掉四個函式而語法仍正確」** |

基準快照 `data/edit_integrity_baseline.json` —— **7 檔、163 個符號**。

### 2.2 fixture（G108 之驗證條件同 G33）

| 案例 | 期望 | 實測 |
|---|---|---|
| 現況原檔 | PASS | **PASS** |
| **刻意刪去四個函式**（20 包之實際損壞形態）| FAIL | **FAIL —— 語法 True 而符號層攔下**，缺 `check_er_clause_coverage` / `check_misread_terms` / `check_s52b_final_step_intent` / `check_source_clause` |
| 語法破壞 | FAIL | **FAIL —— 語法層攔下** |

### 2.3 「同一步內完成」之確保方式 —— **據實回報：本腳本不保證**

R-P150 要求回報「如何確保同一步內完成」。
**答案是：不保證。** 本腳本是一道**可執行之檢查**，
**何時執行仍靠執行者**。

本包（及 22 / 23 包）之實際作法：每次編輯後立即跑 lint
（其 import 即涵蓋載入層），並於包結束前跑全檔檢查。
**這是紀律加工具，不是機制** —— 與 R-P118(e) 之「沉默不算裁決」同一性質。

---

## 三、B3 —— A-PW02 / DR-PW3 / R-P151 之互相標註

| 處 | 已加之交叉指引 |
|---|---|
| **A-PW02** | 指向 DR-PW3 與 G103 / G114；並載 23 包全量掃描之結果 |
| **DR-PW3** | 指向 A-PW02、G103 / G114、DR-PW11 |
| **R-P151**（本包條文回報）| 載三處關係，並載全量掃描使該關聯不再是推測 |

**23 包 B2 之全量掃描證實該形態會實際丟東西**：
115 leaf 中 **2 個不相等**，四個被丟棄之 item
（`4941425` / `4941430` / `4941433` / `4941984`）
**於兩份 CFTS 文字層皆無內文段落**。已併入 DR-PW11。

---

## 四、B4 —— 第二批之現行狀態快照

`features/power/data/b4_batch2_snapshot.md`，含檔案 SHA256 / bytes、
26 條之 `tc_id` / `req_id` / `split_index` 對照、待人工裁決類逐條清單。

> **一項不可回溯之事實**：該 SHA256 取自 **23 包當下**，
> **非 21 包當時之值** —— 22 包已依 R-P153 ~ R-P157 改動第二批
> （`remarks` 標記、`reasoning` 補列、六條 ER 修正、二條越界斷言刪除）。
> **21 包當時之值已不可回溯取得。**
> 此快照對「覆核 21 包當時之第二批」此一原始用途**已無效**；
> 其現有用途為「覆核第二批之現況」。

另：21 包原文寫「待人工裁決類 44 項」，係 20 包當時之**全批**數；
本快照僅列第二批之項數（**29**）。

---

## 五、§D 全表自驗

| # | 項目 | 期望值 | **實測** | 判定 |
|---|---|---|---|---|
| **G107** | R-P7 加註後原文位元組未變 | UNCHANGED | SHA256 前後同，388 bytes | **UNCHANGED** |
| **G108** | 語法檢查步驟已納入；刻意刪函式之案例證明會攔下 | 三案如期 | 三案如期；符號層攔下語法正確之刪函式 | **PASS** |
| **G70** | lint 全閘 | 全 PASS；**leaf 11；TC 43** | `exit=0`；阻斷類 PASS；**leaf 33；TC 106** | **PASS（數值已非 21 包當時）** |

> §D 之 G70 期望值「leaf 11；TC 43」為 **21 包當時**之狀態。
> 補執行時之實測為 leaf 33 / TC 106（22 包產出第三批、23 包補二條）。
> **此差異為補執行之必然結果，非異常。**

---

## 六、執行層對「本包是否仍有該驗而未驗者」之獨立判斷

**（甲）補執行本身所產生者 —— 三項**

1. **B4 快照之原始用途已失效**（見 §四）。
2. **§D 之 G70 期望值已不可能達成** —— 其所寫之 leaf / TC 數為 21 包當時之狀態。
   我照實回報現值並標明差異，**但這意味著本包之 §D 有一格從一開始就無法對上**。
3. **R-P152 之啟動條件於本包落檔前已被使用** ——
   第三批係依 22 包之宣告啟動，非依本條落檔後之判定。
   **條文與其效力之間有一段空窗**，即 R-P164 所指者。

**（乙）已驗而應標明其強度不足者 —— 一項**

4. **G108 不保證「同一步內完成」**（見 §2.3）。
   它把 20 包那次損壞攔得住，**攔不住「編輯完沒跑它」**。

**（丙）本包自身之作業瑕疵**

5. 無。本包之編輯皆以 JSON 載入或精確字串替換為之，
   未使用寬切片；每次改動後立即跑 lint。

---

## 七、產出檔案

| 檔案 | 說明 |
|---|---|
| `features/power/scripts/check_edit_integrity.py` | G108（新增）|
| `features/power/data/edit_integrity_baseline.json` | G108 之基準快照（新增）|
| `features/power/data/b4_batch2_snapshot.md` | 第二批狀態快照（新增）|
| `features/power/DATA_REQUESTS.md` | DR-PW9、DR-PW3 交叉指引（改）|
| `features/power/RULINGS.md` | R-P148 ~ R-P152 抄入、R-P7 加註（改）|
| `features/power/ANOMALIES.md` | A-PW02 / A-PW110 / A-PW111 交叉指引與更新、A-PW121（改）|
| `features/power/docs/upstream/21_need_rework.md` | 本檔（新增）|
