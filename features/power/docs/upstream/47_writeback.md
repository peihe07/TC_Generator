# 47 — 殘留 6 條人工指派與寫回

上繳包 | 執行層 → 分析層 | 往返 NN = 47

§J 自檢**二次**（R-P200(c)）：執行前與抄錄前皆 **§A 5 / §J 5 / §H 步驟 8「五條」**。

現行最大號：R-P306 / A-PW301 / DR-PW15 → 本包自 **R-P308**（見下）、A-PW302 起。

> ⚠ **R-P307 為缺號** —— 本包 §A 之編號自 R-P308 起，而 46 包之末為 R-P306。
> **執行層不自行補寫裁決條文**（§I），據實登記為 **A-PW302**。
> 抄入後 `RULINGS.md` 之缺號為 **[307]**，此為本專案首次出現 R-P 缺號。
> 其為編號跳號抑或有一條未下放，執行層無從判斷。

---

## 一、B1 —— 6 條人工指派（R-P308 / G217）

**依 §K 第 3 項先核對六號之實指**（46 包已實測分析層引用舊號之情形確實發生）——
`…-001` / `004` / `163` / `164` / `203` / `204` **六號皆為現行有效號**，
與謂詞判定之「無對應」6 條**完全相符**，無須依實指改換。

| tc | JSON 既有值 | **人工指派** | 依據 |
|---|---|---|---|
| `…-001` | `mode` | **`timing`** | 與 `…-004` 觀察於開機序列之不同時點（`SplashScreen_Time` 對 `StandardScreen_Time`） |
| `…-004` | `timing` | **`timing`** | 同上，對照方向相反 |
| `…-163` | `input_data` | **`trigger_state`** | `on the phone main screen` 對 `on the phone projection call UI` —— 顯示中之畫面為系統狀態 |
| `…-164` | `input_data` | **`trigger_state`** | 同上 |
| `…-203` | `mode` | **`trigger_state`** | `Bring the HU to Timed mode` 對 `to Full Operation mode` —— 電源模式為系統狀態 |
| `…-204` | `mode` | **`trigger_state`** | 同上 |

各條之 `reasoning_note` 皆註明其為**人工指派**及其依據。

**JSON 之 `axis` 值分布**：`input_data` 86、`trigger_state` 71、`mode` 39、
`timing` 18、`boundary` 5 = **219** 條（另 41 條依 R-P265 無該欄）；
**「無對應」之值為 0**。

### ⚠ 一項區別須明載

**謂詞未改**（依 §I），故 `propose()` 之判定仍為「無對應 6 條」——
**JSON 之值與謂詞之判定自此分離**。重跑謂詞仍會將該 6 條判為無對應。
**此為 R-P308「不再擴充謂詞，改以人工指派」之必然結果，非缺陷**；
惟其使「以謂詞重跑驗證 JSON 值」之作法對該 6 條失效（A-PW303）。

---

## 二、B3 —— R-P303 加註（R-P312）

原文不改，加註載明其 (b) 之慮**經 46 包實測不成立**，
立條理由訂正為「合併之結構後果因條而異」。
原文 SHA256 `7546b2f91cd7be397151513b9f4d40ac`，**加註前後同一（UNCHANGED）**。

**執行層之補充觀察**：本條所指之形態為
「**分析層引執行層之自陳措辭為證據，而未查其實際結構**」——
44 包之自陳本身即為推想（當時未逐對查 ER 結構），經引用後成為裁決之立條理由。
與 R-P302 所診斷之第九次同源，**惟其鏈長一節：推想 → 自陳 → 被引為證據 → 立條**（A-PW307）。

---

## 三、B4 —— 寫回（R-P310 / G218 / G219）**本包主產出**

### （一）前置確認 —— 四項全數 PASS

| 項 | 結果 |
|---|---|
| (a) `axis` 之「無對應」= **0**；TC **260** 條 | **PASS** |
| (b) 全量 dry-run 十項全數 PASS（261 列） | **PASS** |
| (c) 目標檔 SHA256 與 `inputs/` 原始檔相符 | **PASS** —— `ce93174794d0d43c…`，78,607 bytes |
| (d) 依 R-P228 備份至 `sandbox/` | **PASS** —— `sandbox/backup/20260819T013425Z`，**199 檔** |

### （二）寫入

| 項 | 值 |
|---|---|
| 路徑 | `xlsx_surgical.surgical_save()`；**全域無 `Workbook.save()`** |
| 來源（**唯讀**） | `features/power/inputs/FM-WI-FSM-036-A01 …_20260816.xlsx` |
| **交付副本** | **`output/FM-WI-FSM-036-A01 …_20260816.xlsx`**（106,319 bytes） |
| 產生方式 | 自來源**位元組複製**為 `output/.work_*`（SHA256 比對通過），寫入後由 `surgical_save` 產出副本，工作檔即刪 |
| 寫入列數 | **261**（TC 260 ＋ 留白 1），第 10 – 270 列 |
| 最終 `tc_id` | 於此刻依 R-P113(c) 指派，序為 `(SWE-PM ID, split_index)`（R-P115）—— **001–260 連號** |
| 留白列 | `SWE-PM-089` 依 R-P141 保留，僅填 `req_id` |

**存放路徑之理由**：`output/` 為本專案既有輸出目錄（全域規約「輸出放 `output/`」），
**非客戶樹、非 `inputs/`**。

### （三）寫入後驗證 —— **十一項全數 PASS**

| 項 | 結果 |
|---|---|
| DV（含 x14）未變 | **PASS**（5 → 5，x14 1 → 1） |
| 合併儲存格 / 條件式格式 / 分頁清單 / 壓縮成員清單 未變 | **PASS** |
| 凍結窗格、欄寬未變（G95） | **PASS** |
| B 欄序號逐列相符（261 列） | **PASS** |
| 列序依 `(SWE-PM ID, split_index)`（261 列） | **PASS** |
| **最終 `tc_id` 逐列相符（001–260 連號）** | **PASS** |
| **來源原始檔未被改動** | **PASS** —— 寫入前後 SHA256 同一 |

**XML 層 diff**：壓縮成員之增減 **無**。

### （四）不得為之 —— 逐項確認未為

**未送達客戶目錄**；**未執行任何 git 操作**；**未以 openpyxl `save()` 為之**。
**送達為另一動作，須 Pei 另行授權** —— 未代為。

---

## 四、B5 —— 最終號對照表（R-P311 / G220）

`data/tcid_final_map_47.md` 逐條列五欄（27 前 / 27 後 / 44 前 / 現行 / **最終**）。

| 項 | 數 |
|---|---|
| 逐條列數 | **266** |
| 具最終號者 | **260** |
| 被併入而無最終號者 | **4**（欄指向併入對象之最終號） |
| **鍵斷裂經人工確認補齊** | **2** |

**2 條之人工對照**：

| req_id | 27 後 | 現行 | **最終** |
|---|---|---|---|
| `SWE-PM-091`（`keeps the Day theme…` → `uses the Day theme`） | `…-257` | `…-253` | **`…-200`** |
| `SWE-PM-092`（`keeps the Night theme…` → `uses the Night theme`） | `…-258` | `…-254` | **`…-201`** |

**補齊後之涵蓋：266 / 266（100%）。**
⚠ 其限度仍在 —— 本次係因條數不符方查出該斷裂（A-PW306）。

---

## 五、B2 —— 驗證邊界更新

> **(十三) `distinguishing_axis` 之判定以措辭形態為代理判準**
> 其**無法窮盡語義**；本 feature 之 260 條中 **6 條由人工指派**，
> 其餘由謂詞判定，**謂詞之語義覆蓋率不可知**。
> 二度實證：情境型輸入（45 包）、施加動作型（47 包）。
> **`axis` / `delta` 不寫入工作簿**，故其不影響交付內容。
>
> **(十四) 授權之效力範圍**
> R-P309 之授權所及者為**授權當時之 260 條內容 ＋ 6 條 `axis` 指派**。
> **若其後內容再有實質變動**（TC 增減、四個長欄位之改寫、`req_id` 或
> Test Set 之變動），**本授權不及於變動後之版本，須重新取得**。
>
> **(十五) 交付範圍**
> 本次交付為 **260 條 TC ＋ `SWE-PM-089` 之留白列**（R-P141）。
> **11 個 leaf 受 blocking DR 阻斷而未產出**（DR-PW1 ~ DR-PW15 之 live 項），
> 其狀態見 `DATA_REQUESTS.md`。

---

## 六、§D 自驗

| # | 期望值 | 實測 |
|---|---|---|
| G217 | `axis` 之「無對應」= 0；6 條之 `reasoning` 皆註明人工指派 | **0**；**6 條**皆註明 |
| G218 | 四項逐項回報；任一不符即停 | 四項**全數 PASS**，未觸發停止 |
| G219 | 五項逐項回報；DV / merges / cf / pane / 欄寬皆未變；B 欄與列序逐列相符 | **十一項全數 PASS**；另驗最終 `tc_id` 與**來源原始檔未被改動** |
| G220 | 涵蓋 266 條（含 2 條人工補齊）；最終號欄已補 | **266 / 266** |
| G70 | 指派後仍全 PASS | **阻斷類 PASS，exit 0** |

**G108 / G177**：同步後 **90 / 90 完整**（923 符號）。

---

## 七、帳冊異動

- **RULINGS.md**：R-P308 – **R-P312** 五條 ＋ 執行層回報；**R-P303 依 R-P36 加註**。
  ⚠ **R-P1 – R-P312 之缺號為 [307]**（見 A-PW302）。
- **ANOMALIES.md**：新增 **A-PW302 – A-PW307**（6 條）。**A-PW1 – A-PW307 無缺號**。

---

## 八、執行層自判：本包仍有該驗而未驗者

**有，四項。**

1. **寫回為不可逆，而其驗證僅及於機制與結構**（§K 第 2 項）。
   G219 之十一項皆驗「未變」與「對得上」，**不驗所寫之 260 條內容是否正確** ——
   內容之正確性由歷 47 包之各次複核承擔，其涵蓋率已逐項記於驗證邊界。
2. **R-P308 之人工指派由分析層一人所為，無第二方複核**（§K 第 1 項自載）。
   執行層另指出：**該指派之依據為分析層自撰**，
   而執行層之核對僅及於「六號之實指」與「指派已落實」，**未複核其軸之判定是否正確**。
3. **交付副本置於 `output/`，其為執行層依全域規約所選** ——
   R-P310(二)(b) 令「其產生方式與存放路徑須先回報」，
   **而本包之回報與寫入於同一次作業內完成**，
   分析層未及於寫入前確認該路徑。若其認為不妥，須於 48 包指正並重出。
4. **R-P307 之缺號**（A-PW302）。
   歷來各包之帳冊完整性驗證皆以「連續、無重複、無缺號」為項，
   **本包起該項首次不成立**；其成因不明，執行層不自行補寫。
