# 35 — 人工確認、G108 擴充與謂詞驗證

上繳包 | 執行層 → 分析層 | 往返 NN = 35

§J 自檢**二次**（R-P200(c)）：執行前 **§A 區塊數 6 / §J 列數 6 / §H 步驟 9「六條」**；
抄錄前重驗，三處仍一致（`mtime` 未變）。

**本包未改任何 TC 值**：`git diff --stat -- features/power/generated/` 為空。
現行最大號（R-P147）：R-P241 / A-PW189 / DR-PW15 → 本包自 R-P242、A-PW190 起。

---

## 一、B1 —— G108 擴充（R-P242 / G169）**先行**

| | 33 包 | 35 包擴充 | 期末同步 |
|---|---|---|---|
| 基線檔數 | 7 | **59** | **63** |
| 涵蓋率 | **11.9%** | **100.0%** | **100.0%** |
| 符號數 | 163 | 704 | 728 |

`WATCHED` 改為 `sorted(p.name for p in SCRIPTS.glob("*.py"))` ——
**往後新增之腳本自動納入**。舊 7 檔符號**無流失**。**既存損壞 0**（34 包已修畢）。

**載入層安全性先實測再擴充**：`exec_module` 會執行模組層程式碼，
若有模組層檔案寫入即與 **R-P227(c)** 衝突。AST 掃描 59 檔，
排除 `sys.path.insert` 後**僅 2 檔**另有陳述
（`build_reconciliation.py`、`rejudge_design_method.py`），**皆為記憶體內字典操作，無寫入**。

**基線為 (c) 型受保護產物**：首次 `--update-baseline` 遭 `ProtectedWrite` **如期拒寫**；
因重寫將摧毀「33 包時點之 7 檔基線」，**先另存 `edit_integrity_baseline_pre35.json`**
保全該紀錄，再帶 `--overwrite-protected` 重設；二次重設皆入 `edit_integrity_reset_log.tsv`。

**⚠ 新發現之限度**：符號層**僅對有基線之檔生效** ——
無基線者 `symbols_ok` 為 `None` 而仍計入「完整」。
**故擴充當下之「59 / 59 完整」在更新基線前是虛的**（A-PW190）。

---

## 二、B2 —— 第 4 列 80 條逐條確認（R-P243 / G170）**最高優先**

`confirm_row4.py` → `data/g170_row4_confirmation.md`。本檔**只蒐證不判定**。

### 2.1 三項證據之交叉分布（80 條）

| 甲 規格條件子句 ≥ 2 | 乙 有姊妹 | 條數 | 意義 |
|---|---|---|---|
| 是 | 是 | **27** | 規格為多條件，且該條件確實改變結果 |
| 是 | 否 | **37** | 規格為多條件，惟未以姊妹枚舉 |
| 否 | 是 | **4** | 規格僅一條件子句，惟姊妹顯示條件改變結果 |
| 否 | 否 | **12** | 二證據皆無 → 送人工逐條判 |

證據乙＝同 leaf 內「僅差一實質前提而 ER 不同」之姊妹 TC —— 決策表之結構特徵。

### 2.2 ⚠ 證據甲之謂詞於本包內訂正，並陳兩版（R-P187 / R-P182）

v1 之 `COND_RE` 未加 `re.I`，而語料主要條件措詞為 **`IF`（全大寫，42 次）**
與小寫 `if`（35）/ `when`（14）/ `while`（5）——
`SWE-PM-044` 之 `IF … THENTLM` 完全落空。

| 版 | 二證據皆無者 |
|---|---|
| v1 | **37 / 80** |
| v2 | **12 / 80** |

**偏誤方向：膨脹「疑似偽陽性」** —— 誇大本層發現，且會把 TC 推回落底、
抵銷 R-P236 之效果，屬對執行層有利之方向。
結構性理由：規格以 `IF … THEN` 全大寫書寫，**條件詞之大小寫與其是否為條件無關**。

### 2.3 12 條之逐條判定（實讀 clause 之 AND 鏈）

| tc | leaf | clause 之條件結構 | 判定 |
|---|---|---|---|
| `…-029` | `SWE-PM-064` | `Timeout1 == 00 min` AND `Phone_Call.Info == Active` AND Full-Operation AND ignition 切換 | **成立** |
| `…-030` | `SWE-PM-064` | `Timeout1 <> 00 min` AND 到期 AND `Phone_Call.Info` 仍 Active | **成立** |
| `…-031` | `SWE-PM-065` | `IF Timeout1 <> 00` AND `IF Phone_Call.Info` 於到期前轉 Not_Active | **成立** |
| `…-032` | `SWE-PM-065` | 同上二條件 | **成立** |
| `…-072` | `SWE-PM-017` | Full-Operation AND `Rear_View_Camera == Present` AND Rear Camera 轉 inactive；結果另依二訊號 | **成立**（最強） |
| `…-082` | `SWE-PM-021` | Idle AND `Rear_View_Camera == Present` AND `Rear_Camera_Enable.Info` False→True | **成立** |
| `…-110` | `SWE-PM-031` | `Rear_View_Camera == Present` AND 依 `Rear_Camera_Enable.Info` 之值顯示與否 | **成立** |
| `…-109` | `SWE-PM-030` | `Auto_SwitchOn_Setting.Req == Recall_Last` AND `VPLastStatus == On` | **成立** |
| `…-123` | `SWE-PM-039` | Ignition Working Conditions AND `LTM_OperationalModeSts.Info == SNA` | **成立（弱）** |
| `…-130` | `SWE-PM-043` | Standby AND **except if** 需顯示 HMI 畫面 | **成立** |
| `…-138` | `SWE-PM-046` | `Rear_View_Camera == Present` AND `Rear_Camera_Enable.Info == True` | **成立** |
| `…-099` | `SWE-PM-027` | clause 僅 `IF Antitheft_Result.Info == "Not_Successfully"` —— **單一條件**；前提屬情境建構 | **不成立** |

`…-099` 續判：第 5 / 6 列不命中、第 7 列無謂詞、第 8 列 procedure 僅 2 步
→ **落至第 9 列 Functional Based**。

### 2.4 確認後之數字

**第 4 列 80 → 79；落底 85 → 86。**

### 2.5 ⚠ 二項須明載

（甲）**證據甲只數條件子句之起首詞，不數 AND 鏈** ——
`SWE-PM-021` 之「IF A AND B AND C」僅計 1，使三角化偏保守（送更多條進人工）。
**人工判定為最終權威（R-P236(b)），故不影響結果，僅影響分流；判準已用於量測，本包不回頭調整**（17 §I）。

（乙）**R-P243(b) 之分析層抽樣複核尚未執行**（§K 定其時點為 36 包）。
故 79 / 86 仍為**執行層自判**，依 R-P214 未經第二方複核，**不得作為改值之最終依據**。

---

## 三、B3 —— 第 7 / 2 列之反向查（R-P244 / G171）

`reverse_probe_rows.py` → `data/g171_reverse_probe.md`。自**結構特徵**出發。

### 3.1 第 7 列：得候選 **9 對**（非「已查而無」）

判準：同一 leaf 內二條 TC，**二個以上**獨立參數同時取不同值
（單一參數變動者為決策表之逐列，不計）。

| leaf | TC 對 | 同時相異之參數 | 人工判定 |
|---|---|---|---|
| `SWE-PM-073` | `…-007`/`…-011`、`…-011`/`…-012`、`…-011`/`…-016` | `PN14_LS_Actv`、`PN14_LS_Lvl7` | **非組合** —— 二訊號恆同步變動，屬單一邏輯條件之二訊號 |
| `SWE-PM-038` | `…-038`/`…-040`、`…-038`/`…-043`、`…-039`/`…-040`、`…-039`/`…-043` | `Timeout1`、`Phone_Call.Info` | **真組合枚舉** |
| `SWE-PM-014` | `…-064`/`…-065` | `Auto_SwitchOn_Setting.Req`、`Timeout1` | **真組合枚舉** |
| `SWE-PM-026` | `…-095`/`…-097` | `Brand_Configuration_2`、`PhoneCall.Info` | **真組合枚舉** |

**9 對中 6 對為真組合枚舉。**

### 3.2 ⚠ 第 7 列之 0 為 **first-match 序之結構性結果**

候選 TC 之實際落點：
`…-038`/`…-039`/`…-040`/`…-043`/`…-064` → **第 3 列**；
`…-065`/`…-095`/`…-097` → **第 4 列**；
`…-011` → 第 6 列；`…-007`/`…-016` → 第 8 列；`…-012` → 第 9 列。
**無一條能抵達第 7 列。**

原因：任何多參數組合測試**必**同時滿足第 4 列之「多條件 → 結果」，
而第 4 列在序上先於第 7 列。
**故於現行 §12 之 first-match 序下，第 7 列結構上不可能命中** ——
此非本語料之特性，而是 §12 序之特性。

**須由分析層裁定**：（甲）接受第 7 列為死列；或（乙）調整 §12 之序或第 4 / 7 列之判準。
**執行層不自行調整 §12。**

### 3.3 第 2 列之同法複驗：得候選 **2 條**

| tc | 命中之結構 | 判定 |
|---|---|---|
| `…-008` | `Stop the broadcast of the two Load Shed signals on the bus` | **成立**（現值即 Fault Injection Lite，34 包已依 R-P232 確認） |
| `…-011` | 同一措詞 | **不成立** —— 步驟 2 隨即以復原值恢復廣播，**所驗為復原而非故障** |

**反向查未發現新的故障注入**；惟確認詞彙謂詞之盲區為真且**有界**（264 條中結構候選僅 2）。

---

## 四、B4 —— `priority` 謂詞 fixture（R-P245 / G172）

`fixture_priority.py` —— **13 案全部自撰之虛構 TC，不取自本語料**。**13 / 13 如期。**

七類正例各一（safety / boot·recovery / connection / audio output / eCall /
vehicle-critical CAN signal / data-loss risk）＋
三個 P3 負例（品牌 logo 置中 / 使用者換 icon pack / 季節動畫）＋
**三個對抗案**：

| # | 對抗案 | 針對之 v1 缺陷 | 結果 |
|---|---|---|---|
| 11 | bench 樣板句不得使 connection 命中 | v1 之 `connected` / `network` 吃到樣板句 | 如期 |
| 12 | 英文字 “can” 不得使 CAN 命中 | v1 之 `\bCAN\b` 誤加 `re.I` | 如期 |
| 13 | `audio and video` 之 audio 為偶然共現 | — | 如期 |

每一 fixture 之 `pre_conditions` 皆帶與本語料同形之 bench 樣板句，故第 11 / 12 案為真對抗。

### ⚠ 本 fixture 之效力邊界

（甲）**第 13 案不是正確性測試，是回歸護欄** ——
其期望值即為現行行為（34 §5.1 已判該 `audio` 為偶然共現），
它防止該行為悄悄改變，**不證明該行為為對的**。

（乙）依 **R-P214**，fixture 由執行層自撰、自定期望、自回報，
**其全數 PASS 不足以證判準正確**；其所證者為
「判準對執行層**所能想到**之案例表現如預期」。
**v1 之缺陷正是執行層當初想不到的**，故此邊界為實質限制而非形式保留。

---

## 五、B5 —— `remarks` / `split_flag` 逐條清單（R-P246）

### 5.1 `remarks` —— 9 條

| tc | leaf | 現值 | 建議值 |
|---|---|---|---|
| `…-033` `…-034` `…-035` `…-036` `…-038` `…-040` `…-041` `…-043` | `SWE-PM-038` | （空） | 「DR-PW10 待範圍確認」 |
| `…-015` | `SWE-PM-073` | 載 DR-PW8 | **待人工確認**（見下） |

**⚠ `…-015`**：其 `remarks` 所載之 DR-PW8 **確為 live DR**，
惟 `leaf_batch_reconciliation.tsv` 之 `SWE-PM-073` 列**未載該 DR** ——
**不一致者可能是對帳表而非該 TC**。執行層不擅改任一方（A-PW199）。

### 5.2 `split_flag` / `split_index` —— 13 條

| tc | leaf | 現值 | 建議值 |
|---|---|---|---|
| `…-028` `…-071` `…-072` `…-082` `…-083` `…-084` `…-085` `…-110` `…-111` | `063`/`016`/`017`/`021`/`022`/`023`/`024`/`031`/`032` | `split_flag=True` 而該 leaf 僅 1 條 | `split_flag=False`、`split_index=1` |
| `…-053` `…-054` `…-055` | `SWE-PM-013` | `split_index=0`（leaf 共 6 條） | 依 R-P115 改為 1..6 之出現序 |
| `…-097` | `SWE-PM-026` | `split_index=0`（leaf 共 4 條） | 依 R-P115 改為 1..4 之出現序 |

**本包不改值。**

---

## 六、B6 —— G168 實作（R-P247）

`verify_axis.py` → `data/g168_axis_contract.md`。

### 6.1 ⚠ 契約原文之缺口

R-P247 稱「§4.6 已定其契約（`axis` ∈ 列舉值；`axis="none"` ⇔ `duplicate_of` 已設）」,
惟 **`§4.6` 之原文於本庫查無**（`RULINGS.md` / `PLAYBOOK.md` / `DECISIONS.md` /
`RUNBOOK.md` / `docs/INDEX.md` 逐一搜尋皆無），
且 **`duplicate_of` 欄不存在於任何 TC**（264 條之鍵組合恆為 `{axis, delta}`）。
**執行層不自行擬定列舉值**，比照 34 包對 G167 之處置（A-PW197）。

### 6.2 可查證之結構（C1–C5）

| 項 | 判準 | 全批實測 |
|---|---|---|
| C1 | 鍵組合恰為 `{axis, delta}` | 0 違規 |
| C2 | `axis` 非空字串 | 0 違規 |
| C3 | `delta` 非空字串 | 0 違規 |
| C4 | `axis == "none"` ⇒ `duplicate_of` 已設 | **語料中 `axis="none"` 為 0 條 → 本項現不觸發**；其正確性**未經本批語料檢驗** |
| C5 | 同 leaf 內二條之 `delta` **逐字相同** | **觸發 1 組** |

`axis` 值分布（**實測，非契約**）：`behaviour` 245、`trigger_state` 6、`branch` 6、
`timing` 3、`trigger` 3、`input_data` 1。

**fixture 6 案全數如期**，含「應 FAIL C1 / C3 / C4」與「應觸發 C5」——
**證明本閘確實會 FAIL**（R-P247 之要求）。

### 6.3 C5 之 1 組觸發，人工判定

`SWE-PM-071` 之 `…-002` / `…-003`，`delta` 皆為
「本條驗抑制分支：轉往 Standby 或 Bench 時不得顯示 splash，與 -01 為互斥條件」。
逐條實讀：`…-002` 之 boot target 為 **Standby**、`…-003` 為 **Bench**，
`test_procedure` / `expected_result` / `split_reason` **皆已區分**。

→ **二條確為真正互斥，非重複 TC；缺陷在 `delta` 欄沿用同一句樣板**。
建議 36 包改寫該二條之 `delta`。**本包不改值**（A-PW198）。

---

## 七、B7 —— 交付說明之「已驗／未驗」草稿（R-P247）

> **本批 264 條 TC 之驗證邊界**
>
> 下列閘門全數 PASS **不代表已全面驗證**。已驗與未驗之性質如下。

### 7.1 已驗證之性質（附其閘門）

| 性質 | 閘門 |
|---|---|
| `source_clause` 對 CFTS 原文之**逐字保真** | G94 |
| 錨點集合之完整與一致 | G0 / G99 |
| 必填欄位之存在與型態 | G79 / G70 |
| ER 之具名標的存在於 `source_clause` | G82 |
| `source_clause` 之行為項有無 TC 對應（**詞彙層**） | 反向涵蓋三透鏡 |
| OR 分支之涵蓋 | G113 |
| 前提之六欄型態與狀態描述 | G142 / G147 |
| 末步 ER 之可證偽性（**逐條人工裁決，非自動**） | R-P142 / R-P67 |
| `design_method` 之 §12 first-match 判定 | G154 / G155 / G163 / G170 |
| `priority` 之 §10.2 歸類 | G164（謂詞經 G172 之 13 案 fixture） |
| `distinguishing_axis` 之結構契約與 `delta` 逐字重複 | G168 |
| 腳本之語法、載入與符號完整（63 / 63） | G108 / G169 |
| 全量對帳（103 產出 ＋ 11 阻斷 ＋ 1 留空 = 115） | G121 |

### 7.2 **未**驗證之性質（六項永久已知限制）

| # | 未驗證之性質 | 為何無法機械化 |
|---|---|---|
| 1 | ER 是否為 `source_clause` 之**語義蘊含** | 需語義判斷；三透鏡皆詞彙層，門檻 0.45 為人工設定 |
| 2 | `test_procedure` 各步之**可執行性** | 需對 bench 能力之實體知識 |
| 3 | `tc_title` 是否**忠實**描述該 TC | 需語義判斷 |
| 4 | TC 內**數值門檻**是否等於規格所載 | 需理解該數值在二處之語義角色 |
| 5 | 前提條件是否**充分** | 需推論「執行此步驟還缺什麼」 |
| 6 | **測項之遺漏** | 需知道規格沒寫但應測者 |

**R-P241(d) 之第 3 項（多條 TC 是否真正互斥）由 G168 部分涵蓋** ——
「部分」須明載：G168 只抓 `delta` **逐字相同**者，
**措詞不同而語義相同者仍抓不到**，該部分仍屬語義判斷（A-PW200）。

### 7.3 尚待處置者

- 第 4 列之 79 條與落底 86 條為**執行層自判**，分析層抽樣複核於 36 包
- `remarks` 9 條、`split_flag` / `split_index` 13 條、`delta` 2 條待改值（36 包）
- 第 8 列之「跨功能」未機械化，7 條命中者未確認
- DR-PW1 ~ DR-PW15 中之 live DR 影響面

---

## 八、§D 自驗

| # | 項目 | 期望值 | 實測 |
|---|---|---|---|
| G168 | `distinguishing_axis` 契約 | 全批符 §4.6；`delta` 逐字相同者觸發；fixture 證明會 FAIL | **結構違規 0**；C5 觸發 **1 組**（已人工判定）；fixture **6 / 6** 如期。⚠ **§4.6 原文查無，列舉值未驗**（A-PW197） |
| G169 | G108 擴充 | 基線涵蓋 59 / 59 = 100%；既存損壞已列出並修復 | **63 / 63 = 100%**（728 符號）；既存損壞 **0**（34 包已修畢）；新增限度見 §一 |
| G170 | 第 4 列人工確認 | 80 條逐條有條件與依據；確認後數字已回報 | **80 條皆有三項證據**；12 條逐條判定 → **79 成立 / 1 不成立**；**第 4 列 79、落底 86** |
| G171 | 第 7 / 2 列反向查 | 結構特徵之命中數；第 7 列之 0 已說明為「已查而無」 | 第 7 列 **9 對候選**（6 對真組合）；第 2 列 **2 條候選**（1 成立）。⚠ **第 7 列之 0 非「已查而無」，係 first-match 序之結構性結果**（A-PW193） |
| G172 | `priority` 謂詞 fixture | 十案（七正三負）皆如期；fixture 未取自本語料 | **13 案**（另加三對抗）**13 / 13 如期**；**全部自撰**。效力邊界見 §四 |
| G70 | lint 全閘 | 全 PASS | **阻斷類 PASS，exit = 0**；待人工裁決 275 項（R-P42(b) / R-P67，不判 FAIL） |

**不改值之佐證**：`git diff --stat -- features/power/generated/` 為空。
**全腳本語法**：63 檔，錯誤 0。

---

## 九、帳冊異動

- **RULINGS.md**：R-P242 – **R-P247** 六條逐字抄入 ＋ 執行層回報。
  **R-P1 – R-P247 連續、無重複、無缺號**；既有條文前綴 SHA256
  `c6673a4af8fe17f7af2b7ecb0a7992b0`（34 包之 R-P159 加註仍在）。
- **ANOMALIES.md**：新增 **A-PW190 – A-PW200**（11 條）。**A-PW1 – A-PW200 無缺號**。
- **DATA_REQUESTS.md**：無新增（DR-PW1 – DR-PW15 沿用）。

---

## 十、執行層自判：本包仍有該驗而未驗者

**有，四項。**

1. **「誇大本層發現」型偏誤已連續二包各出現一次**（A-PW192）——
   34 包 `split_flag` v2 之 88.3%、35 包 `COND_RE` 之大小寫。
   二者皆由執行層自查而出，**皆非由任何機制攔下**。
   **執行層不主張其為偶發**；現行無任何機制能在自查之外攔住此形態。
2. **第 4 列之 79 條仍為執行層自判。** R-P243(b) 之分析層抽樣複核未執行，
   依 R-P214 尚未取得第二方複核。
3. **G168 之 C4 未經本批語料檢驗** —— `axis="none"` 為 0 條，
   其正確性僅由自撰 fixture 支撐，與 R-P245 所指之同型限制。
4. **第 7 列之處置需分析層裁定，本包只呈證據。**
   若裁定為「死列」，則 §12 之九列實為八列，**該事實應寫入交付說明之驗證邊界**；
   本包之 B7 草稿尚未納入該項，因裁定未下。
