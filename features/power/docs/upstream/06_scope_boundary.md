# 06 — 錨點層範圍上界與懸空參照 DR（上繳）

上繳包 | 執行層 → 分析層 | 往返 NN = 06
結果：**十二步全部完成，無停止。G28–G31 全數量測完成。**
三張 DR 已發。惟過程中發生一次檔案損傷並修復 —— 見 §六，須覆核。

---

## 0. 結論摘要

| 步驟 | 狀態 |
|---|---|
| 建立 `handoff/06_scope_boundary.md` | DONE（§A 10 區塊 / §J 10 列 / §H 12 步，自檢一致） |
| 1 G0 前置閘 | **PASS 7 / 7** |
| 2 九章依 R-P42 / R-P43 落地，登記 A-PW27 | DONE —— 13 個未被引用錨點逐一登記 |
| 3 B1 懸空參照 leaf 層交叉，驗 G29 | DONE —— **影響面大幅收窄** |
| 4 CFTS010 OLE2 上界，驗 G30 | DONE —— **上界 = 0** |
| 5 VC/VM 品質，驗 G28，登記 A-PW28 | DONE —— **二欄合觀 0 / 115 不可執行** |
| 6 三張 DR | DONE（DR-PW5 High / DR-PW6 Medium / DR-PW7 Low） |
| 7 §E 補列兩章（R-P46），驗 G13b | DONE —— **G13b 仍 46，G21 未變** |
| 8 §E 弱點段加註（R-P47），驗 G31 | DONE —— **PASS，惟過程中曾損傷並修復，見 §六** |
| 9 G17 自閘門表移除 | DONE（本包未列 G17、未做任何章節層新量測） |
| 10 §D 全表自驗 | DONE |
| 11 §A 十條抄入 RULINGS.md、§F 入 ANOMALIES.md、§G 入 DATA_REQUESTS.md | DONE（RULINGS R-P1–R-P51；ANOMALIES A-PW01–A-PW29；DR-PW1–DR-PW7） |
| 12 上繳 ＋ 更新 INDEX.md | DONE（本檔） |

### 三項最重要之結果

1. **R-P45「先交叉再發 DR」直接改變了 Urgency 判定。** 31 處 / 16 章之表面數字
   看似 High；leaf 層交叉後**僅 2 處落在被引用錨點下**，影響 9 個 leaf、
   1 個 Test Set → 定為 **Medium**。
2. **R-P48 取得的上界為 0。** CFTS010 之 OLE2 目錄無 `ObjectPool`、無 `\x01Ole`。
   **二份 CFTS 皆零嵌入物件** —— A-PW23 之訂正形態就此確立為確定值。
3. **G28 基線：二欄合觀 0 / 115 不可執行。** VC 單欄 2 筆、VM 單欄 0 筆。
   → DR-PW7 定為 **Low**，不阻斷任何 leaf。

---

## 一、B1 —— 懸空參照之 leaf 層交叉（R-P45 / G29，**上繳項一之一**）

`data/b1_dangling_refs.md`，由 `scripts/build_dangling.py` 產生。
歸屬規則：一處參照歸屬於其位置之前最近之**需求錨點**（與 §C rule 2 同構）。

| 指標 | 實測 |
|---|---|
| 懸空參照總處數 | **31**（CFTS009 16 + CFTS010 15） |
| 分布章節數 | **16** |
| 可歸屬至某需求錨點者 | **31 / 31**（無不可判定者） |
| **受影響之被引用錨點數** | **2** |
| **受影響之 leaf 數** | **9 / 114** |
| **受影響之 Test Set** | **1 個 —— Power State（9 leaf）** |

受影響之 leaf：`SWE-PM-001`–`SWE-PM-009`。

### 1.1 逐章彙總（16 章）

| CFTS | 章節 | 標題 | 處數 | 受影響之被引用錨點 | 受影響 leaf |
|---|---|---|---|---|---|
| 009 | §1.3.1.1 | BODY OFF and BODY ON MODE GROUPS | 2 | — | 0 |
| 009 | §1.3.2 | ECU CAN Architecture Configuration | 1 | — | 0 |
| 009 | §1.3.3.5 | Power up Sequence | 2 | — | 0 |
| 009 | **§1.6.2.1** | **TLM algorithm requirements** | **2** | **`4941354`, `4941355`** | **9** |
| 009 | §1.6.4.1 | TLM algorithm requirements | 4 | — | 0 |
| 009 | §1.9.1 | Loss of Communication Behavior | 1 | — | 0 |
| 009 | §1.9.7 | Passenger Display Power Moding | 2 | — | 0 |
| 009 | §1.9.16 | Contextual Theme | 2 | — | 0 |
| 010 | §1.1 | Revision Notes | 1 | — | 0 |
| 010 | §1.4.1.1 | Voltage Level Behavior | 1 | — | 0 |
| 010 | §1.5.2.2.1.1 / .2 | System Voltage / ECU Local Voltage | 2 / 2 | — | 0 |
| 010 | §1.5.3.2.1.1 / .2 | System Voltage / ECU Local Voltage | 3 / 2 | — | 0 |
| 010 | §1.8.2.1.1 / .2 | System Voltage / ECU Local Voltage | 2 / 2 | — | 0 |

**29 / 31 處落在未被引用之錨點下，依 R-P42 不在測試範圍內。**

R-P44 稱 §1.6.2.1「為影響面最大之單一章節」—— 實測為**唯一有影響面之章節**。

---

## 二、B2 —— CFTS010 OLE2 目錄清點（R-P48 / G30，**上繳項一之二**）

`data/b2_cfts010_ole.md`，由 `scripts/build_ole_census.py` 產生。

> **實作說明**：`olefile` 未安裝於本環境。依 CLAUDE.md「優先用內建函式庫」，
> 以標準函式庫自行解析 MS-CFB 之標頭、FAT 與目錄鏈。
> **只讀目錄項（名稱、型別、大小），未讀任何 stream 之內容位元組** ——
> 符合 R-P48 之「列出 storage 清單屬清點，非解內容」。

容器標頭：sector 512 B、mini sector 64 B、FAT 1 扇區、DIFAT 無延伸。
目錄項 **14 個**：root 1、stream 10、storage 3。

| 判準 | 實測 |
|---|---|
| `ObjectPool` storage | **否（不存在）** |
| 名稱以 `_` 起始之 storage（MS-DOC 物件容器慣例） | **0** |
| `\x01Ole` 標記 stream（每個嵌入 OLE 物件必有） | **0** |

目錄內容為標準 MS-DOC 結構：`WordDocument`(156,320)、`1Table`(17,067)、
`Data`(36,952)、`\x05SummaryInformation`、`\x05DocumentSummaryInformation`、
`MsoDataStore` 及其 customXml 項。root 層之 `\x01CompObj` 為容器自身標記，
任何 OLE2 文件皆有，非嵌入物件。

### G30 對照

| 來源 | 數量 |
|---|---|
| 05 包下界（`textutil html` 之 `WrapperResource` 字樣） | 15 |
| **06 包上界（OLE2 目錄之嵌入物件容器）** | **0** |
| 差額 | **−15** |

**上界 0 < 下界 15 並非矛盾** —— 二者量的不是同一件事：
下界量的是**文字層中的參照字樣**，上界量的是**容器中實際存在的嵌入物件**。
兩數併觀即 A-PW23 之訂正形態：**參照存在，資源不存在**。

**故 CFTS010 之嵌入物件數為 0，此為確定值而非下界。**
與 CFTS009（05 包實測同為 0）一致 —— **二份 CFTS 皆零嵌入物件**。

---

## 三、三張 DATA_REQUEST 之全文（**上繳項二**）

已寫入 `DATA_REQUESTS.md`。

### DR-PW5 —— High（live）

> **CFTS009 §1.6.2.1.4 Stolen Vehicle Mode —— 範圍內而未被涵蓋。**
> `SWE-PM-003`（Partial Operation）經 `Sys-RA-PM-0031` 引用該章之 `4941400`，
> 其全文為「the R1 HU shall not enter stolen vehicle mode under any condition」
> （`Radio` 欄含本專案車型 `R1L`，`Model Year` 2021–2025）。
> 而 `SWE-PM-003` 之 Requirement Description 全文為 Partial Operation 之電源政策
> （Display / audio / BT / Tuner / USB / AUX），**無一字涉及 stolen vehicle mode**。
> 請上游澄清：此否定需求應由哪一個 SWE leaf 承接？或確認其不需 TC。
>
> 阻斷：`SWE-PM-003` 之 TC 是否須涵蓋該否定需求。Anomaly：A-PW16、R-P43(c)。

落差逐句：`4941400` 要求「**不得進入**該模式」；`SWE-PM-003` 之 VC/VM 為
`Change STATUS_BH_BCM2.RemStActvSts to "Remote Start Active"` 後
觀察 `$Telematic_Power$ = "Partial_Operation"` —— 二者為不同行為。
未被引用之 `4941399`（描述**進入**條件，`Radio` 欄 `VP4R7, VP4R84` 不含 R1L）
依 R-P42 不在範圍內，故本 DR 只問否定需求之承接，不問防盜功能本身。

### DR-PW6 —— Medium（live）

> **31 處懸空 `WrapperResource` 參照 —— 請補提供缺漏之 RTF 資源或其等效內容。**
> CFTS009 16 處、CFTS010 15 處，分布 **16 個章節**。二份文件經實測**皆無任何嵌入物件**
> （CFTS009 無 `word/embeddings/`、`w:object`/`w:drawing`/`w:pict`/`o:OLEObject` 各 0；
> CFTS010 之 OLE2 目錄無 `ObjectPool`、無 `\x01Ole`），故該等
> `…inline.rtf WrapperResource` 為**純字面之懸空參照**，所指資源未隨文件匯出。
> **影響面（依 R-P45 先完成 leaf 層交叉）**：31 處中**僅 2 處**落在被引用之錨點下 ——
> 皆位於 **CFTS009 §1.6.2.1 `TLM algorithm requirements`**（錨點 `4941354` / `4941355`），
> **觸及 9 個 leaf**（`SWE-PM-001`–`009`），全屬 **Power State**。
> 其餘 29 處落在未被引用之錨點下，依 **R-P42** 不在測試範圍內。
>
> 阻斷：§1.6.2.1 之 9 個 leaf 其 TC 之 `specification_reference` 無可引之規格文字
> （B2 v2 判為「無法判定」）。Anomaly：A-PW23、A-PW26、R-P44、R-P45。

**Urgency 判定依據（下放包要求說明）**：定為 **Medium**，理由三點 ——
（a）§E 已定版，本 DR 不阻斷 framework；
（b）影響 9 / 114 leaf，且僅影響其 `specification_reference` 之可引文字，
不影響該 9 個 leaf 之 Test Set 歸屬或其他 Layer 3 章節之 TC 撰寫
（該 9 個 leaf 各自另有 2–5 個章節可引）；
（c）29 / 31 處依 R-P42 已排除。
**若依 31 處 / 16 章之表面數字，本應判為 High —— R-P45 之要求直接改變了此判定。**

### DR-PW7 —— Low（live）

> **`Verification Criteria` 欄品質。** G28 基線實測：VC 單欄不可執行 **2 / 115**、
> VM 單欄不可執行 **0 / 115**、**二欄合觀不可執行 0 / 115**。
> 兩筆為 `SWE-PM-007`（「Vehicle not equiped with CAN or engineering line is active」）
> 與 `SWE-PM-008`（「Vehicle equiped with CAN」），皆僅為泛稱環境陳述而無可操作條件。
> 二者之 VM 欄均為可執行，故**不阻斷任何 leaf 之 TC 撰寫**。
>
> 阻斷：不阻斷。僅影響該二 leaf 之 Pre-Conditions 欄品質。Anomaly：A-PW28、R-P49。

**Urgency 判定依據**：定為 **Low** —— 二欄合觀之不可執行數為 0。

---

## 四、§D 全表實測值對照（**上繳項三**）

**G17 依 R-P37 自本包起移除，未列、未驗、未做任何章節層新量測。**

| # | 項目 | 期望值 | 實測值 | 判定 |
|---|---|---|---|---|
| G0 | 七份原始檔 SHA256 | 7 / 7 | 7 / 7 | PASS |
| G13 | 跨多章節 leaf 數 | 11 | 11 | PASS |
| G13b | Layer 3 相異章節總數 | **46**（補列後仍 46） | **46** | **PASS** |
| G21 | §E 定版分布 | 63 / 24 / 16 / 8 / 3 = 114 | 相同 | PASS |
| G22 | 兩條 leaf 之 Test Set | Power State / Timeout Settings | 相同 | PASS |
| G18 / G19 / G20 / G23 / G24 / G25 / G26 / G27 | 沿用 | — | 未變 | PASS |
| **G28** | VC / VM 可執行性 | 【首次量測即基線】 | VC 單欄 **2 / 115** 不可執行；VM 單欄 **0 / 115**；**二欄合觀 0 / 115** | 基線已立 |
| **G29** | 懸空參照 leaf 層影響面 | 【實測填入】 | 受影響被引用錨點 **2**、leaf **9 / 114**、Test Set **1（Power State）** | 已填空 |
| **G30** | CFTS010 嵌入物件上界 | 【實測填入】 | **0**（下界 15，差額 −15） | 已填空 |
| **G31** | R-P46 / R-P47 原文位元組未變 | 兩處雜湊 UNCHANGED | §E 弱點段 `1f737d7c…7d1ca6` 前後同值 → **UNCHANGED** | **PASS**（見 §六） |

**無 MISMATCH。**

> **G31 之範圍說明**：R-P46 為**補列**（分析層明令之內容變更），其性質是修改而非加註，
> 故不適用「原文不變」之驗證 —— G31 之雜湊驗證僅適用於 R-P47 之加註。
> 補列後之效力以 G13b（46 未變）與 G21（63/24/16/8/3 未變）驗證，二者皆 PASS。
> 此一區分於下放包 §D 之 G31 條目未明示，執行層據 R-P46 / R-P47 之性質差異自行判讀。

---

## 五、R-P46 / R-P47 之加註前後全文（**上繳項四**）

### R-P46 —— §E Layer 3 補列（內容變更，非加註）

**補列前**：

| Test Set | Layer 3 |
|---|---|
| Power State | 009 §1.6.2.1.1–.15、§1.7.1、§1.8.1.1.1、§1.9.3–.5、§1.9.12 |
| Timeout Settings | 009 §1.6.3、§1.6.4、§1.6.7 |

**補列後**：

| Test Set | Layer 3 |
|---|---|
| Power State | 009 **§1.6.2.1**、§1.6.2.1.1–.15、§1.7.1、§1.8.1.1.1、§1.9.3–.5、§1.9.12 |
| Timeout Settings | 009 **§1.6.2.1.17**、§1.6.3、§1.6.4、§1.6.7 |

驗證：`build_layer3.py` 重跑 → 140 列、**G13b = 46**（未變）；
`build_testsets.py` 重跑 → **63 / 24 / 16 / 8 / 3 = 114**（未變）。
證條文所稱「缺漏僅在 §E 之人工清單，該二章原已在 `layer3_full.tsv` 內」為真。

### R-P47 —— §E 弱點段加註（原文不改）

**原文**（SHA256 `1f737d7c1498002e56a4c50e4b33d5d130608d4d7c479e614a6839f7437d1ca6`，
加註後同值）：

> §4.1.2 要求取「規格目錄」與「RD 分析報告分組」之交集。
> 本案中後者**無分組價值**：037 `Requirement Title` 於 115 leaf 中
> 出現 20+ 種，多數僅出現 1 次（`Timeout` 7、`Phone Call` 5 為僅有例外）。
> 因此上表實際只由 CFTS 章節單一來源支撐，**不是交集**。

**新增之註記段落**：

> **註記（R-P47，06 包加註；依 R-P36 之精神，原文不改）**
>
> 上段之 `Requirement Title` 統計為失效值。05 包 G26 以原始檔實測：
>
> - 相異值 **99 種**（非「20+ 種」）
> - 僅出現 1 次者 **94 種**
> - 出現 > 1 次者共 5 組：`Timeout` 7、`Phone Call` 5、
>   `Splash Screen logo visualization` 4、`Power down` 3、`FOTA` 2
>
> 即「20+ 種」大幅低估，「`Timeout` 7、`Phone Call` 5 為**僅有例外**」為誤 ——
> 另有三組重複值。
>
> **本段之結論（`Requirement Title` 無分組價值）不受影響，反而更強**：
> 99 種比 20+ 種更無分組價值。保留原文以資對照。見 A-PW25。

---

## 六、執行層自陳：一次檔案損傷與修復（須覆核）

**經過**：執行 R-P47 加註時，腳本先取得 §E 弱點段之起訖偏移量，
接著執行 R-P46 之表格取代（改變了字串長度），**再以取代前的舊偏移量插入註記**。
結果註記被插在句中，`docs/handoff/01_intake.md` 之弱點段一度成為：

```
因此上
> **註記（R-P47…）**
…見 A-PW25。表實際只由 CFTS 章節單一來源支撐，**不是交集**。
```

即「因此上表實際只由…」被切成「因此上」與「表實際只由…」兩段。

**偵測**：G31 之雜湊比對當場 **FAIL** —— 這正是 R-P36 所要求之雜湊驗證的作用。
若無此驗證，該損傷會沉默地留在檔案中。

**修復**：以字串手術還原原句，並將註記整段移至段後。
修復後重驗：原文字串完整出現於檔中，SHA256 與加註前**完全相同**
（`1f737d7c…7d1ca6`），G31 改判 **PASS**。

**現況**：`docs/handoff/01_intake.md` 之 §E 弱點段與其註記已如 §五所示，
全檔 strict UTF-8 通過。

**請分析層覆核**：本包對 `01_intake.md` 之三處改動（R-P46 表格兩列、
R-P47 註記一段）之外，該檔應無其他變動。執行層已逐段確認，
但因曾發生一次偏移錯誤，建議由分析層獨立比對。

**自我檢討**：此為與 03 §九第 4 項（把排版簡寫誤讀為實測值）同型之錯誤 ——
**在同一個處理流程中混用了「取代前」與「取代後」的狀態**。
教訓與 R-P38 所訂正者相同：單位／基準必須在操作前固定。
往後對已落檔文件之多處改動，應逐處獨立完成並各自驗證，不共用偏移量。

---

## 七、獨立判斷：本包是否仍有該驗而未驗者（**上繳項五**）

05 上繳包 §8.2 之五項，本包處置：第 1 項→R-P42 / R-P43（已落地，A-PW27 登記 13 個
未被引用錨點）；第 2 項→R-P45 / B1（已交叉，影響面大幅收窄）；
第 3 項→R-P49 / G28（基線已立）；第 4 項→R-P51（登記為已知限制）；
第 5 項→R-P48 / B2（上界已取，為 0）。**五項全部處置。**

### 新增未驗項（五項）

**1.（最重）R-P42 已立，但「不測」與「不寫入 reasoning」之**執行期**檢查機制不存在。**
   R-P42 是本包最重要的條文，它規範的是 **Phase 4 撰寫 TC 時的行為**。
   但目前只有條文，沒有任何閘門能在 TC 產出後驗證「是否真的沒測未被引用之錨點」。
   A-PW27 已列出 13 個未被引用錨點 —— **這正是一份可機械檢查的黑名單**：
   若某 TC 之內容或 `specification_reference` 命中其中任一錨點，即為違反 R-P42。
   建議 Phase 4 之 lint 增設此閘。**現況：條文有，執行期驗證無。**

**2. B1 之歸屬規則本身未經交叉驗證。**
   「參照歸屬於其前最近之需求錨點」是我依 §C rule 2 之同構性自訂的。
   實測 31 / 31 皆可歸屬、0 個不可判定 —— 這個 100% 的結果**看起來太乾淨**。
   若某處參照實際上屬於其**後**之錨點（例如圖表置於該錨點文字之前），
   歸屬就會錯，而 DR-PW6 之影響面（2 處 / 9 leaf）會隨之改變。
   本包未以任何獨立方法檢驗此規則。

**3. G28 之 `SIGNALS` 正則有 6 筆需人工覆寫，覆寫率 5.2%（6 / 115）—— 未評估其代表性。**
   4 筆為偽陰性（`CS.00244`、`Timeout1`、`HIL` ×2 未被識別），2 筆為確認不可執行。
   偽陰性全部集中在「命名慣例不符我預設之識別式」。
   **這意味著 `SIGNALS` 對本 feature 之命名慣例調校不足**，
   若日後套用於其他 feature，偽陰性率不可預期。基線可用，但其精度未經量化。

**4. DR-PW6 之影響面計算假設「9 個 leaf 各自另有其他章節可引」，但未逐一確認。**
   §三之 Urgency 判定依據（b）稱「該 9 個 leaf 各自另有 2–5 個章節可引」——
   此數字取自 `layer3_full.tsv` 之每 leaf 章節數分布，**未逐一確認其餘章節
   之被引用錨點是否也落在懸空參照上**。B1 已知只有 §1.6.2.1 受影響，
   故推論成立，但「成立」與「已逐一確認」是兩件事。

**5. 本包新增之四支腳本無任何自我檢查。**
   `build_dangling.py`、`build_ole_census.py`、`build_vcvm.py` 皆為一次性量測，
   輸出即基線，**無任何斷言可在下次執行時偵測回歸**。
   相較之下 `verify_gates_03.py` 與 `build_testsets.py` 內建期望值比對。
   §六之損傷正是「無自我檢查即沉默失敗」之實例（該次是 G31 的雜湊救了它）。
   建議把 G28–G30 之基線值寫成常數，使腳本自身成為回歸閘。

---

## 八、禁區遵守聲明

| 禁區 | 遵守情形 |
|---|---|
| 不得寫回 FW036 workbook | 僅 `read_only=True` 開啟 |
| 不得執行任何 git 操作 | 本包執行期間未執行任何 git 指令 |
| 不得以 openpyxl save 寫任何 xlsx | 未呼叫 `save()` |
| 不得補齊 `SWE-PM-089`（R-P1） | 未補 |
| 不得沿用純文字衍生物之任何數字（R-P10） | 全部數字自原始檔重生 |
| 不得自行調整 §C 正則 | `SEC_RE` / `REQ_RE` 一字未改 |
| 不得修改任何已落檔裁決條文之內文（R-P36） | 未動任何 `[R-Pnn]` 區塊內文。R-P47 之加註以雜湊證原文未變（G31）；**惟過程中曾損傷 §E 弱點段並修復，已於 §六自陳** |
| **不得測試未被引用之錨點（R-P42）** | 本包不寫 TC。B2 v2 之判讀已限於被引用錨點；A-PW27 將 13 個未被引用錨點登記為**不測** |
| **不得解析任何 RTF 或 OLE stream 之內容** | 未讀任何 RTF。OLE2 僅解析標頭、FAT 與目錄鏈，**未讀任何 stream 之資料扇區** |
| 不得續行章節層反向缺口調查（R-P37） | 本包未對 288 章做任何量測；§D 未列 G17 |
| 不得變更 §E 之分布數字（R-P35） | 63/24/16/8/3 未動（G21 複驗 PASS）。R-P46 之補列僅動 Layer 3 章節清單 |
| 素材補入超出 `inputs/` 需 Pei 裁定 | 未補入任何素材。`olefile` **未安裝**，改以標準函式庫實作 |

---

## 九、待裁

- **Q1（新，最重）§七第 1 項：Phase 4 之 lint 是否增設 R-P42 執行期閘門。**
  A-PW27 之 13 個未被引用錨點即為現成黑名單。
  現況條文有而執行期驗證無 —— 這與 05 包「判讀單位錯誤產出實質錯誤」為同型風險。
- **Q2 §七第 2 項：B1 之歸屬規則（參照歸屬於其前最近錨點）是否需獨立驗證。**
  31/31 之 100% 歸屬率可疑；若規則有誤，DR-PW6 之影響面隨之改變。
- **Q3 A-PW29：EE Architecture 分布與既有政策 R30-3 / R30-4（車型欄留白）之併存關係。**
  Mid 單值 13 個、High 單值 1 個 —— 是否於 Phase 4 據以填欄，或維持留白。
- **Q4 §六之檔案損傷請覆核** —— `01_intake.md` 除本包三處改動外應無其他變動。
- **Q5 §七第 5 項：G28–G30 之基線是否寫回腳本成為回歸閘。**
- **Q6 三張 DR 之 Urgency 判定是否認可**（DR-PW5 High / DR-PW6 Medium / DR-PW7 Low）。
