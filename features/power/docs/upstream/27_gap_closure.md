# 上繳包 27 —— 已知缺口補測、下放包凍結與實測訂正

> 對應下放包：`features/power/docs/handoff/27_gap_closure.md`
> 執行層：Claude（TC_Generator）
>
> **§J 自檢二次皆一致（R-P200(c) 首次適用）**：
> 開工時 §A fenced block **6** / §J 列數 **6** / §H 步驟 10「**六條**」；
> **抄錄裁決條文前重驗**同為 6 / 6 /「六條」，
> 檔案 **13216 bytes、mtime 08-18 14:59 均未變** —— **本包下放後未被修改**。
>
> 本包**未執行任何 git 子命令**（R-P149）；**未對任何 workbook 呼叫 `save()`**；
> **未觸碰客戶樹與 `inputs/`**；**寫回仍未開放**；
> **未以「閘門尚未建立」為由不補缺口**（R-P199）；
> **未為使 G129 門檻好看而刪改任何既有 `reasoning`**（R-P203(c)，補寫全為追加）；
> **未另開 DR 繞過 R-P197**；
> **未自行縮小 G82 擴充之判準**（方向對執行層有利者依 R-P187 呈請裁定）。

**G0 前置閘：7 / 7 素材 SHA256 相符 —— PASS。**

**編號查核（R-P147，先查後開）**：
**R-P 最大 197（R-P1–197 連續）、A-PW 最大 142、DR-PW 最大 13、閘門最大 G133。**
本包新號：**R-P198–R-P203**（R-P198 為 26 包遺落者，依原編號補入不重編）、
**A-PW143–A-PW147**、**G134–G137**。**DR 無新增**。

---

## 一、B1 —— R-P198 補落檔與其三項處置

**已依原編號抄入 `RULINGS.md`，接於 R-P197 之後；R-P1 – R-P203 連續無缺。**

### （a）`SWE-PM-014` —— 查證後**不需補測**

| 支 | TC | 前提 | ER |
|---|---|---|---|
| 肯定側 | `059` | `Brand_Configuration _2 reads "Jeep"` | 轉 Standby |
| **ELSE 側** | `060` | `Brand_Configuration _2 reads a value other than "Jeep"` | 轉 Timed 且維持當前音源 |

二支與原文逐一對應，**二支皆已涵蓋**。

### （b）`SWE-PM-026` —— 已補否定側一條

`NR1L-PowerManagement-097`：非 Jeep 時開啟車門。
**其 ER 逐字僅述**「`TLM_Status.Info` does not pass to "Standby" through the transition of this clause」
—— **未斷言其他行為**（§8.4.1）。

### （c）含 `Brand_Configuration_2` 守衛條件者之涵蓋（以**容忍空白**之樣式掃全 103 leaf）

| leaf | 守衛條件 | 肯定側 | 否定側 |
|---|---|---|---|
| `SWE-PM-014` | `IF Brand_Configuration _2 == "Jeep" AND …` | `059` | **`060`** |
| `SWE-PM-026` | `AND proxi parameter Brand_Configuration_2 == "Jeep"` | `094` / `095` / `096` | **`097`（本包補）** |

另 `SWE-PM-053` / `054` / `101` 含該參數而**非守衛條件**（為 logo 之資料來源）。

**本條僅拘束 `Brand_Configuration_2`** —— 其餘適用性參數維持 A-PW141 之待裁，**未類推**。

---

## 二、B2 —— 六個已知缺口補測（R-P199 / G134）

| leaf | 補測之列舉項 | 補 |
|---|---|---|
| `SWE-PM-013` | `Ignition Pre_Start`、`Ignition Cranking`、`Ignition On Engine On` | **3 條** |
| `SWE-PM-103` | `Ignition Pre_Start`、`Ignition Start`、`Ignition Cranking` | **3 條** |

**G134 重跑（以 G131 之同一判準）**：五組列舉中全未覆蓋之項合計 **1** ——
`SWE-PM-028` 之 `only for this case`。
**其不補之獨立理由**：該段為**散文而非可測項**（前一子句之範圍限定語，
無獨有之可觀察標的），非「閘門尚未建立」。

**tc_id 全域重編** —— 補測插入於批次三中段，其後號段全部位移。
新增 `scripts/renumber_tc_ids.py`，依批次序重新指派 `001` 起之連號
（`tc_id` 為臨時號，R-P113(b)）；三個產生器之 `START_ID` 同步。

| 批 | TC | 號段 |
|---|---|---|
| 1 | 17 | 001–017 |
| 2 | 26 | 018–043 |
| 3 | **68**（+4）| 044–111 |
| 4 | 50 | 112–161 |
| 5 | **69**（+3）| 162–230 |
| 6 | 34 | 231–264 |

**Phase 4 累計：103 leaf / 264 條**（26 包為 257）。

---

## 三、B3 / B4 —— G82 擴充與樣式變體（R-P201 / G135 / G136）

### 3.1 分析層之訂正 —— **經執行層獨立重掃確認**

以容忍空白之樣式 `Brand[_\s]*Configuration[_\s]*2` 掃全部 103 leaf：

| leaf | 容忍樣式 | 嚴格樣式 | 原文逐字形態 |
|---|---|---|---|
| **`SWE-PM-014`** | **1** | **0** | **`Brand_Configuration _2`** |
| `SWE-PM-026` / `053` / `054` / `101` | 1 | 1 | `Brand_Configuration_2` |

**26 包之「0 次」係樣式未涵蓋空白所致；原文確載該參數，§8.4.2 未越界。**
A-PW139 已依 R-P201(a) 改記為「掃描樣式未涵蓋空白變體」。

### 3.2 G135 —— G82 擴充

擴至 `pre_conditions` 與 `input_test_data`，並加識別子空白摺除（`_fold_ident()`）。

**fixture 五案全數如期**：三欄皆在則通過／`pre_conditions` 越界 FAIL／
`input_test_data` 越界 FAIL／三欄同時越界 FAIL／帶空格與不帶空格視為同一則通過。

**擴充後於現況資料觸發 25 項**：

| 類 | 數 | 執行層之判別 |
|---|---|---|
| `pre_conditions` 引用 leaf 未載之訊號（`TLM_Status` / `Telematic_Power` / `STANDBY MODE`）| 14 | **真陽性之候選** —— 該等為建構前提之機制，其定義在他章；是否屬 §8.4.2 之「測試」待裁 |
| `input_test_data` 之純數值（`100` / `20` / `25` / `15`）| **9** | **偽陽性之候選** —— 測試者所選之注入值，依 §4.5 本就不必見於規格 |
| `input_test_data` 引用 leaf 未載之訊號（`Day_Night_Mode`）| 2 | **真陽性之候選** —— `SWE-PM-091` / `092` 之原文未載該訊號 |

**執行層未縮小判準** —— 排除純數值會使發現由 **25 降為 16**，
**方向對執行層有利，依 R-P187 須由分析層裁定**。
本包之處置：新增二欄之發現**列入待人工裁決類**（比照 R-P42(b) / G73），
`expected_result` 維持阻斷，lint 之 exit code 不受影響。**呈請裁定該判準之校準。**

**另修一項久存之分流缺陷**：`run_batch_gates()` 之結果原**直接併入阻斷類**，
致 `ADJUDICATE_RULES` 對批次層閘門（G79 / G81 / G82）**完全無效** ——
此缺陷存在已久，由本次擴充首度暴露。已改為與 `run_all_gates()` 同一介面。

### 3.3 G136 —— 字串樣式變體逐腳本檢查

判定 **10 檔**，明細見 `data/g136_pattern_variants.md`。
**空白變體未涵蓋者原 1** —— `scan_clause_patterns.py` 之 `APPLICABILITY` 逐字比對。

**已修**（加 `_fold()`）；其方向為**增加發現（對執行層不利）**，
依 R-P187 自行修正並回報：G132 之 leaf 數 **40 → 40 不變**
（`SWE-PM-014` 原已由 `Jeep` / `LTM High` 命中），惟其命中詞由 2 增為 3。

**刻意未放寬者**：`verify_source_clause.py` 之大小寫 —— R-P125(a) 明令
「不做大小寫正規化，那些差異是真差異」；`lint_tcs.py` 之 `ER_PROPER_RE` ——
其判準即為「大寫識別子」，放寬即失其義。

---

## 四、B5 —— `PsngrDoorSts` 之涵蓋自附帶轉為設計（R-P202）

`NR1L-PowerManagement-095`（重編前之 `092`）之 `split_reason` 現載
「**並承擔 OR 之右支 `STATUS_BH_BCM1.PsngrDoorSts`**（R-P202）」；
`reasoning_note` 另載「`094` / `096` / `097` 承擔 `DriverDoorSts` 支；
**改寫本條時須保留副駕駛門之輸入，否則該支之涵蓋將無聲消失**」。
`SWE-PM-026` 之 leaf `reasoning` 依 R-P191 同步。

---

## 五、B6 —— 批次一至三 `reasoning` 重評（R-P203 / G137）

### 5.1 母體數與條文不符

本條載「22 份」，實測 **批次一 3 ＋ 批次二 8 ＋ 批次三 22 = 33 份**（22 應僅指批次三）。

### 5.2 重評與補寫

| §10.4 項 | 重評前 | 補寫後 |
|---|---|---|
| 1 驗證目標 | 33 / 33 | 33 / 33 |
| **2 關鍵情境條件** | **3 / 33** | **33 / 33** |
| 3 為什麼這樣切（有依據）| 24 / 33 | **25 / 33**（見 5.3）|
| 4 刻意略過（optional，不計入分母）| 17 / 33 | 17 / 33 |

**補寫 31 份，全為追加，未刪改任何既有文字**（R-P203(c)）。
第 2 項之內容取自各該 leaf 之實際觸發條件（其 TC 之 `pre_conditions` 與
`input_test_data`），**非套語**。

### 5.3 第 3 項仍有 8 份判為「空語」—— 判定式之取段限制，非內容缺漏

判定式取**首個** `為什麼這樣切：` 段落，而該處為原有之「單一行為，不拆」
（依 R-P203(c) 不得刪改），本包所補之實質依據位於其後；`**` 標記另使擷取提前截斷。

八份之補依據段皆具實質內容，例：

- `SWE-PM-016`：「停留與影像管理為同一步之二個後果，以二個 ER 承接而非拆條 ——
  拆條會造出規格未載之時序（§8.4.1）」
- `SWE-PM-032`：「單一觸發、單一終態；原文之 `Standby OR Sleep` 二支中，
  本錨點僅載 Standby 側，Sleep 側不在本 leaf 之錨點內」

**放寬判定式之方向對執行層有利，依 R-P187 未自行修改，呈請裁定。**

### 5.4 G129 門檻重校（依 R-P182 並陳）

| 版 | 校準語料 | 最短 | 門檻 |
|---|---|---|---|
| 26 包 | 批次一 ~ 三（重評前）| 26 字（`SWE-PM-022`）| **20** |
| 27 包（候選）| 批次一 ~ 三（補寫後）| 201 字（`SWE-PM-060`）| 200 |
| **27 包（採用）** | **全六批** | **131 字（`SWE-PM-094`）** | **130** |

**未取 200 之理由**：批次四 ~ 六尚有 131 / 169 / 177 / 181 / 184 字者，
以 200 為門檻將迫使該等 `reasoning` **為過門檻而加字** ——
該形態與 R-P203(c) 所禁者方向相反而性質相同（一為刪、一為灌水）。

G129 現為 **103 / 103 合格**，fixture 四案仍如期。
**長度仍為弱代理** —— §10.4 四項之實質涵蓋由 G137 判，非 G129。

---

## 六、B7 —— A-PW139 訂正

已改記為「掃描樣式未涵蓋空白變體，致誤判為 §8.4.2 越界（已訂正）」，
**§8.4.2 越界之判定撤回**，並載明執行層之獨立重掃結果與 G135 / G136 之處置。

---

## 七、§D 全表自驗

| # | 項目 | 期望值 | 實測 | 判定 |
|---|---|---|---|---|
| G134 | 六個已知缺口 | 補測或逐一具獨立不補理由；重跑後未補者為 0 或全數具理由 | **六個全數補測**（6 條）；重跑後全未覆蓋 **1**，具獨立理由（散文非可測項）| **PASS** |
| G135 | G82 擴充 | 涵蓋 `pre_conditions` 與 `input_test_data`；fixture 證明會 FAIL | 已擴充 ＋ 空白摺除；**fixture 五案如期**；現況觸發 25 項（判別見 §3.2）| **PASS** |
| G136 | 字串樣式變體 | 【實測填入】腳本清單與變體涵蓋；含 `SWE-PM-014` 之獨立重掃 | 判定 **10 檔**；空白變體未涵蓋者 **1 → 0**（已修）；`SWE-PM-014` 容忍 **1** / 嚴格 **0** | **已填** |
| G137 | 批次一至三 `reasoning` 重評 | 【實測填入】四項涵蓋率；補寫數；門檻是否重校 | 母體 **33**（非 22）；第 2 項 **3 → 33**；補寫 **31**；門檻 **20 → 130** | **已填** |
| G70 | lint 全閘 | 全 PASS | **264 TC，阻斷類 PASS，exit = 0**；self-test exit = 0 | **PASS** |

**無 MISMATCH。**

補驗：**G0 7 / 7**、**G94 103 / 103**、**G99 103 / 103**、**G103 103 / 103**、
**G108 7 / 7**、**G113 self-test 5 / 5**、**G121 PASS**、**G129 103 / 103（門檻 130）**、
**G38 連號遞增無跳號（001–264）**。

---

## 八、§F Anomaly 異動

**A-PW143 ~ A-PW147**（A-PW01 – A-PW147 連續無缺）；**A-PW139 已依 R-P201(a) 訂正**。

| 號 | 摘要 |
|---|---|
| A-PW143 | G82 漏掃二個欄位（已擴充 G135）；**另修 `run_batch_gates()` 之分流缺陷** |
| A-PW144 | R-P192 之推遲被讀為含補測，6 個缺口隨末批交出（已全數補測）|
| A-PW145 | 分析層執行期間修改下放包二次，未告知重驗（已立 R-P200，本包二次自檢皆一致）|
| A-PW146 | `092` 對 `PsngrDoorSts` 之涵蓋為附帶（已轉為設計）|
| A-PW147 | G129 校準語料未評估、門檻攔不住「寫了等於沒寫」（已重評並重校至 130）|

---

## 九、§G DATA_REQUESTS

DR-PW9 ~ DR-PW12 沿用；DR-PW13 維持 26 包之撤回。**無新增，未繞過 R-P197。**

---

## 十、裁決條文與台帳

**§A 六條逐字抄入 `RULINGS.md`**（逐條字串比對確認），各附執行層回報。
`RULINGS.md` 現為 **R-P1 – R-P203 連續無缺**（含補入之 R-P198）。
§F 五項已入 `ANOMALIES.md`，A-PW139 已訂正。

---

## 十一、執行層自判：本包仍有該驗而未驗者

**有，四項。**

1. **G82 擴充觸發之 25 項，我只做了分類，沒有逐條裁決。**
   14 項 `pre_conditions` 之訊號越界尤其懸而未決 ——
   若判為真陽性，受影響者橫跨批次四、五、六，
   **那會是一次比本包大得多的返工**，而我把它擱在待裁類裡繼續交付。

2. **本包三次遇到「放寬判準會對我有利」，三次都沒放寬** ——
   G82 之純數值、G137 之取段、以及 26 包遺留之 G129 門檻。
   R-P187 在這三處都生效了。**但這也意味著本包交出的三個數字
   （25 項、25 / 33、130 字）都不是它們該有的樣子**，
   而是「未經校準的判準」下的讀數。裁定前它們不宜被引用。

3. **`SWE-PM-013` 與 `SWE-PM-103` 之補測，我沒有第二人覆核。**
   六條 TC 的內容是照既有同型條複製再改前提 ——
   形式上合規，但那六個點火狀態之間是否真有可觀察差異，
   **規格沒說，我也沒有查證的來源**。

4. **264 條 TC 中，分析層目視者仍為少數**（§K 第 2 項）。
   本包又加 7 條、改動 212 個 tc_id、重寫 31 份 `reasoning` ——
   **改動量遠大於覆核量，且此比例已連續七包擴大。**
