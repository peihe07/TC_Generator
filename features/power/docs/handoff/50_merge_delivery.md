# 50 — 併入現行交付與重新寫回

下放包 | 分析層 → 執行層 | 往返 NN = 50

前置：49 包上繳已覆核，判定 **ACCEPT**。
語料現況 **280 條 TC / 111 leaf**，`tc_id` 1–280 連號；
`output/` 之交付副本未被觸碰（261 列）。

**Pei 已裁 R-P321 之（甲）併入現行交付**，逐字：「甲：併入」。

沿用不變之節：`§C 抽取規格` 同 02 包；`§I 禁區` 同 49 包 §I，另增列見本包 §I。

## A. 本包裁決條文（逐字，抄入 RULINGS.md）

```
[R-P322] R-P321 裁定（甲）併入現行交付；其代價明載並接受。
         Pei 逐字：「甲：併入」。

         **其代價已由執行層於 A-PW320 指出，本條逐字記明並接受**：
         最終 `tc_id` 依 `(SWE-PM ID, split_index)` 指派，
         而本批 leaf 為 `SWE-PM-001`–`009`，**其序在現行全部 leaf 之前** ——
         **併入後現行 260 條之最終號將全部後移約 20 號，
         既有交付副本與其對照表全部失效。**

         **分析層於 49 §K 建議（甲）時未載此代價**，
         係執行層查出後方得並陳。此為分析層第十次未經查證即書寫之陳述 ——
         其形態為**建議而未推算其後果**，與前九次之「陳述而未查證」同族。

         處置：
         （a）既有 `output/` 交付副本**標記為作廢**，
              移至 `output/superseded/` 並保留（不刪除），
              其檔名加註作廢日與被何者取代
         （b）其對照表同標作廢
         （c）新交付副本涵蓋 **281 列**（TC 280 ＋ 留白 1）
         裁決者 Pei，逐字依據：「甲：併入」。
```

```
[R-P323] 重新寫回之授權：Pei 之原授權不及於本次，須重新取得。
         R-P309 逐字載：「若其後內容再有實質變動（TC 增減、
         四個長欄位之改寫、`req_id` 或 Test Set 之變動），
         **本授權不及於變動後之版本，須重新取得**」。

         本次為 TC 增加（260 → 280）且最終 `tc_id` 全數重新指派，
         **屬實質變動，R-P309 之授權不及於之**。

         **本包不寫回。**
         其產出為「可供 Pei 審視之最終狀態」——
         全量 dry-run 與最終 `tc_id` 對照表；
         **待 Pei 就 281 列之版本另行授權後，於 51 包寫回。**

         此非形式主義：Pei 所點頭之 260 條與本次之 280 條
         **內容不同、編號不同**，其授權不得沿用。
         裁決者 Pei，逐字依據：「甲：併入」。
```

```
[R-P324] 最終 `tc_id` 對照表須涵蓋三代號。
         本次重新指派後，同一條 TC 將歷經三代號：
         （一）歷史臨時號（09 包以來，含 27 包重編前後、44 包合併前後）
         （二）**現行交付副本之最終號**（001–260，47 包所指派）
         （三）**本次之新最終號**（001–280）

         對照表須以 `(req_id, tc_title)` 為鍵，涵蓋三代。
         **46 包已建者為（一）→ 現行臨時號**，其涵蓋為 264 / 266
         （2 條因 `tc_title` 改寫而鍵斷裂，A-PW299）——
         **該 2 條於本次須一併人工補齊**，否則其斷裂將延續至第三代。

         **另須明載**：現行交付副本之最終號（二）
         **已對外存在**（`output/` 之 261 列），
         其與（三）之對應為**本次作廢之核心資訊** ——
         若有人已引用（二）之號碼，須憑此表換算。
         裁決者 Pei，逐字依據：「甲：併入」。
```

```
[R-P325] G113 之 78 個未覆蓋分支：本包裁決，不再積欠。
         49 §九第 3 項：本批 78 個未覆蓋分支依 R-P118(c) 不判 FAIL，
         **惟其積欠仍在**。

         **裁定：本包逐項裁決，不得帶入寫回。**
         依 R-P118(d) 三選一（`真缺口` / `規格未定義該支` /
         `已由他 leaf 涵蓋`），並依 R-P171 分三桶回報計數；
         **僅 `真缺口` 需完整理由，前二桶給計數與抽樣例示（≥ 16.7%）**。

         **`真缺口` 若有，須補測後方得寫回** ——
         其為 R-P199 之直接適用（已發現之缺口不得以「閘門未建立」為由不補）。
         裁決者 Pei，逐字依據：「甲：併入」。
```

```
[R-P326] 交付說明更新：涵蓋範圍、作廢紀錄與未產出之 4 leaf。
         DELIVERY_NOTE 須更新：
         （a）**涵蓋範圍** —— 280 條 TC ＋ 留白列；**111 / 115 leaf**
         （b）**未產出之 4 leaf 逐一列出**：
              `SWE-PM-003`（DR-PW5）、`SWE-PM-008`（DR-PW11）、
              `SWE-PM-089`（R-P141 留白）、`SWE-PM-112`（DR-PW9）
         （c）**DR-PW6 之結案界線**（49 §六 / A-PW318）——
              就其阻斷面結案，非就全部 31 處參照結案；
              其餘 29 處依 R-P42 不在測試範圍內
         （d）**前一版交付副本之作廢**（R-P322(a)）及三代號對照表之存在
         （e）**圖形資料之使用界線**（R-P320）——
              二份 Visio 圖僅用於確認行為有依據，
              **其連線關係無工具可查；「未被用於推想」為執行層自述，
              無閘門可證**（49 §九第 1 項）
         裁決者 Pei，逐字依據：「甲：併入」。
```

## B. 本包須產出

### B1. G113 之 78 個未覆蓋分支裁決（R-P325）—— **最高優先**

三選一逐項裁決；三桶計數；`真缺口` 完整理由並補測。

### B2. 最終 `tc_id` 重新指派與三代對照表（R-P324）

依 `(SWE-PM ID, split_index)`；三代號齊備；
**2 條鍵斷裂者人工補齊**。

### B3. 既有交付副本作廢（R-P322(a)(b)）

移至 `output/superseded/`，檔名加註作廢日與取代者；對照表同標。

### B4. 全量 dry-run（281 列）

十項逐項回報；與 45 / 47 包之結果對照。

### B5. 交付說明更新（R-P326）

(a)–(e) 五項。

### B6. **不寫回**（R-P323）

本包產出「可供 Pei 審視之最終狀態」；寫回於 51 包。

## D. 閃點

G0 為前置閘。G0–G228 沿用（G17 已移除；G109 不實作），期望值不變。

| # | 項目 | 期望值 |
|---|---|---|
| G229 | G113 78 分支裁決（R-P325） | 78 項皆有裁決；三桶計數；`真缺口` 已補測 |
| G230 | 三代號對照表（R-P324） | 涵蓋 280 條 ＋ 已併入者；2 條鍵斷裂已人工補齊；三代皆有 |
| G231 | 交付副本作廢（R-P322） | 舊副本已移至 `superseded/` 且未刪除；檔名已加註 |
| G232 | 全量 dry-run（281 列） | 十項全數 PASS；與 45 / 47 包逐項對照 |
| G70 | lint 全閘 | 全 PASS |

## E. framework

§E 已定版（R-P35），本包不動。
**寫回於 51 包**，其條件為 Pei 就 281 列之版本另行授權（R-P323）。

## F. Anomaly 異動

開新號前先查現行最大號並回報（R-P147）。

- 新增：分析層建議（甲）而未推算其後果，第十次（R-P322）
- A-PW320 → 依 R-P322 處置，其指出正確
- A-PW299 → 依 R-P324 於本次人工補齊

## G. DATA_REQUESTS

DR-PW6 → 已結案（就阻斷面）。
DR-PW5 / DR-PW9 / DR-PW11 仍 live，其阻斷之 3 leaf 不在本次交付。

## H. 作業指示

1. G0 前置閘
2. 查現行最大號並回報
3. B1 G113 78 分支裁決，驗 G229 —— **最高優先；`真缺口` 須補測**
4. B2 最終 `tc_id` 重新指派與三代對照表，驗 G230
5. B3 既有交付副本作廢，驗 G231
6. B4 全量 dry-run，驗 G232
7. B5 交付說明更新
8. 以 §D 全表自驗
9. **抄錄裁決條文前重驗一次 §J 自檢**（R-P200(c)）；
   §A 五條逐字抄入 RULINGS.md；§F 入 ANOMALIES.md
10. 上繳 `features/power/docs/upstream/50_merge_delivery.md`，更新 `docs/INDEX.md`

## I. 禁區

沿用 49 包 §I 全部條目，另增列：

- **不得寫回**（R-P323）
- **不得刪除既有交付副本**（R-P322(a)）
- **`真缺口` 未補測前不得宣告本包完成**（R-P325）
- **不得沿用 R-P309 之授權**（R-P323）

## J. 本包產生之新條文清單（自檢）

1. R-P322 裁定（甲）併入；其代價明載並接受
2. R-P323 重新寫回之授權須重新取得；本包不寫回
3. R-P324 最終 `tc_id` 對照表須涵蓋三代號
4. R-P325 G113 之 78 個未覆蓋分支本包裁決
5. R-P326 交付說明更新五項

逐條確認：**五條**，皆以獨立 fenced block 呈現於 §A，未夾於敘述中。
自檢：§A 區塊數 = 5、§J 列數 = 5、§H 步驟 9 寫「五條」，三處一致。
本次已逐一數過 §A 之 fenced block。
**本包下放後分析層不再修改**（R-P200(a)）。

## K. 分析層自判：本包是否仍有該驗而未驗者

**有，三項。**

1. **分析層之第十次失** —— 其形態自「陳述而未查證」變為
   **「建議而未推算後果」**。R-P183（數字須標來源）與 R-P250（先量再寫）
   皆不涵蓋此類；**唯 R-P293 之「逐條查」與執行層之自查可撞見**，
   而本次確由執行層撞見。
2. **280 條中經第二方通讀者仍為 0%**（49 §九第 2 項）。
   歷次複核皆為抽樣或欄位層，**TC 全文之通讀從未發生**；
   R-P241 已裁改為目標式深挖，其代價已入驗證邊界。
3. **二份 Visio 圖之「未被用於推想」無閘門可證**（49 §九第 1 項）。
   R-P320(b) 明禁自標籤推想轉換關係，**而該禁令之遵守僅由執行層自述** ——
   本包未設任何驗證，亦無可設之驗證。
