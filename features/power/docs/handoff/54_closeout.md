# 54 — 空行判準訂正與收尾

下放包 | 分析層 → 執行層 | 往返 NN = 54

前置：53 包上繳已覆核，判定 **ACCEPT**。
**G242 過** —— Power 錨點中位數 188 vs Comfort 首段中位數 114、max 735，
**R-P343 之錨點類比成立**；280 條 `test_item` 已寫入；
三批裁決條文補齊（R-P327–R-P346 共 20 條，逐字核對全符）。

**本包為最後一個工作包。** 其完成後即進寫回與交付。

沿用不變之節：`§C 抽取規格` 同 02 包；`§I 禁區` 同 53 包 §I，另增列見本包 §I。

## A. 本包裁決條文（逐字，抄入 RULINGS.md）

```
[R-P347] G237 之空行判準訂正：末段為括號區塊，其前恰一空行；首段得含空行。
         53 包 G237 FAIL 11 項，**全部源於同一錨點 `4942354`
         （`SWE-PM-073`），其單一段落內含一個空行** ——
         R-P343(a)「逐字」與 R-P342(a)「恰一個空行」**無同時成立之解**。

         **執行層未折衷、保留逐字、讓閘據實叫 —— 處置正確。**
         摺除空行違反逐字；改判準等於自行改寫該包剛立之閘。

         **訂正：問題不在二者互斥，在分隔符之定義選錯。**
         「恰一個空行」以**全文之空行總數**為判準，
         而其真正用途為**分隔首段與末段** ——
         當首段自身含空行時，該定義即失效。

         新判準（G237 三項）：
         （a）**末段**為 `(` 起 `)` 止且含 ` -> ` 之區塊，
              且其**前恰有一個空行**
         （b）**首段為該空行之前的全部文字**，**其內部得含空行**
         （c）首段為 `source_clause` 之連續子字串（空白正規化後）

         此訂正**不放寬攔截力** —— 末段之格式要件未變，
         首段之連續子字串要件未變；所變者僅分隔符之認定方式。
         R-P342 依 R-P36 原文不改，於其下加註指向本條。
         裁決者 Pei，逐字依據：「儘快收尾」。
```

```
[R-P348] 同一節內二條要件之相容性須檢查（R-P335 之補充）。
         53 包指出：A-PW333 為**同一包內二條新條文互斥之第一例**；
         A-PW329 / 330 / 331 三例皆為跨包。
         **共同形態為「一次立多條要件而未相互檢查」，
         51–53 連續三包皆有。**

         **R-P335 令就地增補須檢視六處，而其所查者為「節與節之間之同步」，
         查不到「同一節內二條要件是否相容」。**

         補充規則：分析層於同一包內立二條以上要件者，
         **須逐對檢查其相容性**，並於 §J 之自檢中列明已檢查之對數；
         其檢查方式為：
         （a）取二條之要件各自之**極端案例**（最長、最短、含特殊字元者）
         （b）問「是否存在同時滿足二者之解」
         （c）無解者，於下放前即訂正，不得留待執行層攔下

         **本包立三條要件以上時，§J 須列出 C(n,2) 對之檢查結果。**
         裁決者 Pei，逐字依據：「儘快收尾」。
```

```
[R-P349] G239 三項受 DR-PW11 阻斷；可行三項於本包補測。
         53 包查得：`Logistic Idle` / `Logistic Standby` / `Logistic Sleep`
         之定義錨點 `4941427` / `4941432` / `4941435`
         **全屬 `SWE-PM-008`** —— 該 leaf 受 **DR-PW11（High, live）**阻斷，
         其被引用錨點於兩份 CFTS 文字層皆無內文，自第三批起排除。

         **該三項不是「不補」，是其 leaf 尚未解阻**，不觸 R-P328(e)。

         **分析層之失**：53 包 §G 寫「DR-PW11 blocking」
         **卻未指出其與 R-P340(b) 之交集** ——
         與 R-P330(c)「未回查 DR 之 blocking 屬性」同型，**第二次**。

         處置：
         （a）該三項**登記為受阻**，入驗證邊界；
              DR-PW11 獲解後與 `SWE-PM-008` 一併產出
         （b）**可行三項於本包補測**：`Bench`、
              `Full-Operation` 二列之 `Source` 分辨、
              `Timed` 二列之 `Source` 分辨
         （c）新增 TC 使 `tc_id` 再變，**與 B6 之三代對照表重建同批**
         裁決者 Pei，逐字依據：「儘快收尾」。
```

```
[R-P350] 素材層「四閘全 111/111」之強度界線，寫入驗證邊界。
         53 包自陳二項：
         （a）`G94` 111/111 —— 對 8 個重建 leaf，**產生器與驗證器同源**
              （R-P339 已認）
         （b）`G240` 8/8 —— 獨立者為錨點辨識與內文歸屬之邏輯，
              **段落切分共用**（輸入之 txt dump 由 `extract_textlayer.py` 所產）；
              **若段落切分本身有誤，該閘驗不出來**

         **二者合計覆蓋之 8 個 leaf，其 `source_clause` 佔全語料字元數之多數。**

         **裁定：「素材層四閘全 111/111」不得單獨引為素材已驗之證據。**
         驗證邊界須逐字載明：
         「素材層閘門（G0 / G94 / G99 / G103 / G240）之結果為全數通過，
          **惟其中 G94 對 8 個重建 leaf 之產生器與驗證器同源、
          G240 與其共用段落切分**；
          該 8 個 leaf 之 `source_clause` 佔全語料字元數之多數。
          **段落切分若有誤，現行任何閘門皆驗不出。**」

         執行層已將該界線寫入 `reextract_clause_53.py` 之 docstring
         與上繳 §四 —— **追認其處置**，其自陳
         「不寫的話就是重蹈 R-P339 指的那個錯」為正確之判斷。
         裁決者 Pei，逐字依據：「儘快收尾」。
```

```
[R-P351] R-P250 之「不得使用」應區分直接套用與附複核之提案。
         53 包指出：**提案器 5/6 可用、選句器 4/6 不可用，
         差別不在準確率，在後續處置** ——
         R-P345 為前者配了 20% 抽樣複核，
         而 R-P250 對後者要求的是**直接可用**。

         **該指摘成立。R-P250 之「一個都取不到者不得使用」
         其立意為「不得以未經驗證之判準直接套用於無人覆核之產出」，
         而非「任何準確率不足者一律不得存在」。**

         訂正：
         （a）**無複核機制之直接套用** —— 維持 R-P250 之原要求
              （須有已知應命中之實例，一個都取不到者不得使用）
         （b）**附第二方抽樣複核之提案** ——
              其準確率不必達直接套用之標準，
              **惟其抽樣率須與其準確率成反比**：
              準確率愈低，抽樣率須愈高；
              且**其實測準確率須載明於該提案之回報中**

         **本包不啟用選句器** —— R-P343 已改以錨點為單位，
         首段無須選句，該器已無用途。
         其保留未刪（`pick_first_segment.py`）為正確處置：
         **其實測資料（4/6、覆蓋率 < 0.30 者 103/280）為本條之依據。**
         R-P250 依 R-P36 原文不改，於其下加註指向本條。
         裁決者 Pei，逐字依據：「儘快收尾」。
```

## B. 本包須產出

### B1. G237 訂正與全批重驗（R-P347）—— **最優先**

依新三項判準實作；`4942354` 之 11 條應轉 PASS；
fixture 須含「首段內含空行」之正例與「末段前非恰一空行」之反例。

### B2. `4941453` 可行三項補測（R-P349(b)）

`Bench`、`Full-Operation` ×2 Source、`Timed` ×2 Source；
首段依 R-P344 取連續片段。

### B3. 承接 53 包之 B6（全部）

20 條重新檢視、三代對照表重建、R-P330 三處訂正與加註、
全量 dry-run、驗證邊界更新。

### B4. 驗證邊界之三項增列

（a）R-P350 之素材層強度界線（逐字）
（b）R-P349(a) 之三個受阻狀態
（c）其餘交付欄位之格式規則未查（53 §K 第 3 項）

### B5. R-P342 / R-P250 加註（R-P347 / R-P351）

依 R-P36 原文不改；雜湊佐證。

## D. 閃點

G0 為前置閘；素材層閘門依 R-P327 重跑。

| # | 項目 | 期望值 |
|---|---|---|
| G237 | `test_item` 三項判準（R-P347） | 全批 **280 + 新增 / 全數 PASS**；fixture 含首段含空行之正例與末段前非恰一空行之反例 |
| G239 | `4941453` 可行三項（R-P349(b)） | 三項皆有 TC；受阻三項已入驗證邊界 |
| G243 | 三代對照表重建（B3） | 涵蓋補測後全數；2 條鍵斷裂已補齊 |
| G244 | 最終全量 dry-run | 十項全數 PASS |
| G70 | lint 全閘 | 全 PASS |

## E. framework

§E 已定版（R-P35）。
**本包完成後即進寫回**（55 包），其條件為
G244 全數 PASS ＋ **Pei 就最終版本明示授權** ＋ **Pei 指定客戶目錄路徑**。

## F. Anomaly 異動

開新號前先查現行最大號並回報（現行 A-PW334）。

- 新增：R-P342 之「恰一個空行」以全文空行總數為判準，而其用途為分隔首末段（R-P347）
- 新增：R-P335 查節與節之同步，查不到同一節內二條要件之相容性（R-P348）
- 新增：分析層未回查 DR 之 blocking 屬性，**第二次**（R-P349）
- 新增：R-P250 之「不得使用」未區分直接套用與附複核之提案（R-P351）
- A-PW333 → 依 R-P347 處置

## G. DATA_REQUESTS

DR-PW5 advisory。
**DR-PW9 / DR-PW11 blocking** —— DR-PW11 另阻斷
`Logistic Idle` / `Logistic Standby` / `Logistic Sleep` 三項補測（R-P349(a)）。
無新增。

## H. 作業指示

1. G0 前置閘（素材層依 R-P327 重跑）
2. 查現行最大號並回報
3. B1 G237 訂正與全批重驗，驗 G237 —— **最優先**
4. B2 可行三項補測，驗 G239
5. B3 承接 53 包 B6，驗 G243
6. B4 驗證邊界三項增列；B5 加註
7. 最終全量 dry-run，驗 G244
8. 以 §D 全表自驗
9. **抄錄裁決條文前重驗一次 §J 自檢**（R-P200(c)）；
   §A 五條逐字抄入 RULINGS.md；§F 入 ANOMALIES.md
10. 上繳 `features/power/docs/upstream/54_closeout.md`，更新 `docs/INDEX.md`

## I. 禁區

沿用 53 包 §I 全部條目，另增列：

- **不得摺除 `test_item` 首段內之空行**（R-P347）
- **不得產出受 DR-PW11 阻斷之三個狀態之 TC**（R-P349(a)）
- **不得單獨以「素材層四閘全 111/111」為素材已驗之證據**（R-P350）
- **不得啟用選句器**（R-P351）
- **不得寫回**（§E）

## J. 本包產生之新條文清單（自檢）

1. R-P347 G237 之空行判準訂正：末段為括號區塊，首段得含空行
2. R-P348 同一節內二條要件之相容性須檢查
3. R-P349 G239 三項受 DR-PW11 阻斷；可行三項於本包補測
4. R-P350 素材層四閘之強度界線，寫入驗證邊界
5. R-P351 R-P250 應區分直接套用與附複核之提案

逐條確認：**五條**，皆以獨立頂層 fenced block 呈現於 §A，未夾於敘述中。
自檢：§A 頂層區塊數 = 5、§J 列數 = 5、§H 步驟 9 寫「五條」，三處一致。

**依 R-P348 之相容性檢查（C(5,2) = 10 對）**：
R-P347×R-P348 無交集；R-P347×R-P349 無交集（後者為 TC 產出，前者為格式）；
R-P347×R-P350 無交集；R-P347×R-P351 無交集；
R-P348×R-P349/350/351 皆無交集（R-P348 為分析層作業規則）；
R-P349×R-P350 無交集；R-P349×R-P351 無交集；R-P350×R-P351 無交集。
**十對皆無共同要件，故無互斥之虞。**

**本包下放後分析層不再修改**（R-P200(a)）。

## K. 分析層自判：本包是否仍有該驗而未驗者

**有，三項。**

1. **R-P347 之訂正未經 R-P348 之檢查即立** ——
   本包 §J 之十對檢查為事後補作，而 R-P348 要求者為**下放前**。
   **本包為該規則之首次適用，其自身即為例外**，據實記明。
2. **R-P349 使受阻項增為三個狀態**，
   而 `SWE-PM-008` 之解阻繫於 DR-PW11 之回覆 ——
   **該 DR 自 22 包開出至今未獲回覆**，其時程不在本專案控制內。
3. **R-P350 所載之界線意味：段落切分若有誤，現行任何閘門皆驗不出。**
   其唯一之外部檢查為人讀 CFTS 原文與 `source_clause` 之對照，
   **而該對照從未執行** —— 分析層歷次所讀者皆為 `source_clause`，
   非其與原文之比對。**登記為永久已知限制。**
