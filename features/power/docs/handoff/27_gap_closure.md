# 27 — 已知缺口補測、下放包凍結與實測訂正

下放包 | 分析層 → 執行層 | 往返 NN = 27

前置：26 包上繳已覆核，判定 **ACCEPT**。
Phase 4 產出面完成 —— 六批 103 leaf / 257 條 `[引用：26 §H 步驟 7/7b]`；
最終對帳 103 ＋ 11 ＋ 1 = 115 相符 `[引用：26 §H G126]`。

§J 自檢首次觸發停止、R-P187 首次生效，二機制皆有效。
**惟造成 §J 觸發者為分析層** —— 於執行期間修改下放包二次（增 R-P197、R-P198）。

沿用不變之節：`§C 抽取規格` 同 02 包；`§I 禁區` 同 26 包 §I，另增列見本包 §I。

## A. 本包裁決條文（逐字，抄入 RULINGS.md）

```
[R-P198] `Brand_Configuration_2` 得逐車配置，ELSE 分支須測。
         （本條原載於 26 包 §A，因分析層於執行期間增補而未及落檔；
           依原編號抄入，不重編號。）

         R-P197(c) 令實測該 PROXI 參數之可取值域以定 ELSE 之處置。
         Pei 直接裁定：得逐車配置。
         故 ELSE 分支為另一可發生情境，不得以「恆為 Jeep」為由不測。
         R-P197(c) 之實測要求因而解除。

         26 包之實測獨立佐證此裁定：CFTS 本文實際用到二個值
         （`Jeep` 於 `4941575` / `4941740`、`Fiat` 於 `4941737`），
         即該參數規格上得取 ≥ 2 值。

         逐 leaf 之處置：
         （a）`SWE-PM-014` Behaviour 2 —— 規格明載二支：
              Jeep 且駕駛門開且無通話 → Standby；ELSE → Timed。
              二支皆須有 TC；查證現有涵蓋，缺者補測。
         （b）`SWE-PM-026` —— 其錨點未載 ELSE，非 Jeep 時規格未定義。
              依 §7 補一條否定側 TC：非 `Jeep` 時車門開啟不導致該轉換。
              **ER 僅得述「未發生該轉換」**，不得斷言其他行為（§8.4.1）。
         （c）掃全部 leaf，凡含 `Brand_Configuration_2` 守衛條件者，
              逐一回報肯定側與否定側之涵蓋情形。

         本條僅拘束 `Brand_Configuration_2`；其他適用性參數之值域
         仍依 R-P193 盤點，實測不到者停並上繳，不得類推本條。
         裁決者 Pei，逐字依據：「可逐車配置」。
```

```
[R-P199] R-P192 之推遲僅及於立閘門，已發現之缺口須補測。
         R-P192 載「先量規模，不先立閘門」，
         執行層讀為「補測亦一併推遲」，
         致 G131 量出之 6 個已知缺口
         （`SWE-PM-013` 與 `SWE-PM-103` 各 3 個點火狀態無 TC）
         隨末批一併交出。

         **分析層之條文不夠明確** —— 所推遲者為「立閘門」，
         非「已發現之缺口不補」。

         通則：任何「先量規模、暫不立閘門」之裁決，
         **不免除補測該次量測所實際發現之缺口**；
         推遲僅及於機制之建立，不及於已具體指出之缺陷。
         若某缺口不補，須有獨立之不補理由（規格未定義、
         已由他 leaf 涵蓋、範圍外），不得以「閘門尚未建立」為由。

         處置：補測該 6 個缺口，或逐一給出不補之獨立理由。
         裁決者 Pei，逐字依據：「上繳」。
```

```
[R-P200] 下放包一經下放即凍結；增補須明示重驗。
         26 §五第 2 項：R-P183 之機制與 §J 自檢皆只在開工時做一次，
         二者皆不涵蓋「執行期間下放包被變更」。
         本次係執行層於步驟 9 重讀 §A 方偶然發現。

         **成因為分析層於執行期間修改下放包二次**（增 R-P197、R-P198），
         且未告知執行層重驗自檢。

         規則：
         （a）下放包一經交付執行層，分析層不得再修改
         （b）確需增補者，二擇一：
              （i）另開新包；或
              （ii）修改後明示告知「下放包已修訂，請重驗 §J 自檢」，
                   並載明修訂處與新的三處計數
         （c）執行層於**抄錄裁決條文之前**須重驗一次 §J 自檢
              —— 該時點為條文落檔之最後關卡；
              與開工時之自檢併為二次，不得省略
         裁決者 Pei，逐字依據：「上繳」。
```

```
[R-P201] A-PW139 之實測有誤，撤回其 §8.4.2 越界之判定。
         26 §五第 3 項稱 `SWE-PM-014` 之 `source_clause`
         完全不含 `Brand_Configuration_2`（0 次），
         而其三條 TC 之 `pre_conditions` 引用之
         「還拼成 `Brand_Configuration _2`」，判為 §8.4.2 依據越界。

         分析層直讀該 leaf 之 `source_clause`，實測其載：
         `IF Brand_Configuration _2 == "Jeep" AND
          STATUS_BH_BCM1.DriverDoorSts == "Open" AND
          PhoneCall.Info == "Not_Active"`
         —— **帶空格之 `Brand_Configuration _2` 即在原文中**。

         TC 非拼錯，係忠實抄錄規格之排版錯誤。
         掃描所用之樣式為無空格之 `Brand_Configuration_2`，
         故於 source 掃得 0、於 TC 掃得有 ——
         二處實為同一帶空格字串。§8.4.2 未越界。

         此為 §5a 第 12 條之實例：詞彙型工具之缺陷不會報錯。

         處置：
         （a）A-PW139 之 §8.4.2 判定撤回，改記為「掃描樣式未涵蓋空白變體」
         （b）**其附帶發現成立且重要**：
              G82 只掃 `expected_result`，不掃 `pre_conditions` ——
              依據越界之檢查漏了一整個欄位。
              擴充 G82 至 `pre_conditions` 與 `input_test_data`，
              並以 fixture 證明擴充後確實會攔下越界案例
         （c）全部以字串樣式比對之腳本，須檢查其是否涵蓋
              空白、大小寫、全半形之變體；逐一回報
         裁決者 Pei，逐字依據：「上繳」。
```

```
[R-P202] R-P195 之前提部分不符，其疑慮方向仍成立。
         R-P195 稱 `SWE-PM-026` 之三條「無一條提及副駕駛門」，
         而 26 包實測該分支已由 `092` 覆蓋 —— 前提不符。

         惟執行層自陳「那個覆蓋是附帶的，不是設計的」——
         該疑慮方向成立：一個分支被覆蓋到，與該分支被有意識地涵蓋，
         不是同一件事；前者於後續修改中隨時可能失去。

         處置：`092` 之 `split_reason` 或 `reasoning` 須明載
         其涵蓋 `PsngrDoorSts` 分支，使該涵蓋自附帶轉為設計。
         `SWE-PM-026` 之 `reasoning` 併同更新（R-P191 之要求）。
         裁決者 Pei，逐字依據：「上繳」。
```

```
[R-P203] G129 門檻之限度追認，並重評批次一至三之 `reasoning`。
         26 §五第 5 項自陳：G129 之門檻 20 字係以現有語料校準
         （批次一至三最短之 `reasoning` 為 26 字），
         該閘攔得住「根本沒寫」，攔不住「寫了等於沒寫」。

         追認其限度並登記。惟校準語料本身未經評估 ——
         批次一至三之 22 份 `reasoning` 是否皆具實質內容，本包未驗。

         處置：
         （a）重評批次一至三之全部 `reasoning`，逐份判其是否
              涵蓋 §10.4 之四項（驗證目標／關鍵情境條件／
              為什麼這樣切／刻意略過）
         （b）未涵蓋者補寫；若因此使最短長度上移，
              G129 之門檻得據以重校，並依 R-P182 並陳新舊值
         （c）**不得為使門檻好看而刪改既有 `reasoning`**
         裁決者 Pei，逐字依據：「上繳」。
```

## B. 本包須產出

### B1. R-P198 補落檔

依原編號 R-P198 抄入 `RULINGS.md`，接於 R-P197 之後；
更新台帳缺口記與 `INDEX.md`。其 (a)(b)(c) 三項處置一併執行。

### B2. 六個已知缺口補測（R-P199）—— 最高優先

`SWE-PM-013` 與 `SWE-PM-103` 各 3 個點火狀態；
補測或逐一給出獨立之不補理由。

### B3. G82 擴充（R-P201(b)）

擴至 `pre_conditions` 與 `input_test_data`；
fixture 證明擴充後確實攔得下越界案例。

### B4. 字串樣式變體檢查（R-P201(c)）

全部以字串比對之腳本，逐一檢查其是否涵蓋空白、大小寫、全半形變體；
回報清單與其涵蓋情形。
**另：以帶空格之樣式獨立重掃 `SWE-PM-014`，回報實測**
—— 分析層之訂正僅讀一次、未經複驗（§K 第 1 項）。

### B5. `092` 與 `SWE-PM-026` 之註記（R-P202）

`092` 明載其涵蓋 `PsngrDoorSts` 分支；`SWE-PM-026` 之 `reasoning` 同步更新。

### B6. 批次一至三 `reasoning` 重評（R-P203）

22 份逐份判 §10.4 四項之涵蓋；補寫未涵蓋者；
門檻若重校，依 R-P182 並陳新舊值。

### B7. A-PW139 訂正（R-P201(a)）

改記為「掃描樣式未涵蓋空白變體」，撤回 §8.4.2 之判定。

## D. 閃點

G0 為前置閘。G0–G133 沿用（G17 已移除；G109 不實作），期望值不變。

| # | 項目 | 期望值 |
|---|---|---|
| G134 | 六個已知缺口（R-P199） | 補測或逐一具獨立不補理由；G131 重跑後未補者為 0 或全數具理由 |
| G135 | G82 擴充（R-P201(b)） | 涵蓋 `pre_conditions` 與 `input_test_data`；fixture 證明會 FAIL |
| G136 | 字串樣式變體（R-P201(c)） | 【實測填入】腳本清單與其變體涵蓋情形；含 `SWE-PM-014` 之獨立重掃 |
| G137 | 批次一至三 `reasoning` 重評（R-P203） | 【實測填入】22 份之四項涵蓋率；補寫數；門檻是否重校 |
| G70 | lint 全閘 | 全 PASS |

## E. framework

§E 已定版（R-P35），本包不動。
Phase 4 產出面已完成；**寫回仍不開放** ——
阻斷條件為 R-P199 之六個缺口與 R-P198 之 ELSE 分支補測。

## F. Anomaly 異動

開新號前先查現行最大號並回報（R-P147）。

- A-PW139 → 依 R-P201(a) 訂正，撤回 §8.4.2 判定
- 新增：G82 只掃 `expected_result`，漏 `pre_conditions` 與 `input_test_data`（R-P201(b)）
- 新增：R-P192 之推遲被讀為含補測，致 6 個已知缺口隨末批交出（R-P199）
- 新增：分析層於執行期間修改下放包二次，未告知重驗（R-P200）
- 新增：`092` 對 `PsngrDoorSts` 之涵蓋為附帶而非設計（R-P202）
- 新增：G129 門檻攔不住「寫了等於沒寫」，且校準語料未評估（R-P203）

## G. DATA_REQUESTS

DR-PW9 ~ DR-PW12 沿用。DR-PW13 已於 26 包內撤回（R-P197 明令不開），
其未涵蓋之三類條件登於 A-PW141 待裁 —— 本包不另開 DR 繞過該裁定。

## H. 作業指示

1. G0 前置閘
2. 查現行最大號並回報
3. B1 R-P198 補落檔並執行其三項處置
4. B2 六個已知缺口補測，驗 G134 —— 最高優先
5. B3 / B4 G82 擴充與樣式變體檢查，驗 G135 / G136
6. B5 `092` 與 `SWE-PM-026` 註記
7. B6 批次一至三 `reasoning` 重評，驗 G137
8. B7 A-PW139 訂正
9. 以 §D 全表自驗
10. **抄錄裁決條文前重驗一次 §J 自檢**（R-P200(c)）；
    §A 六條逐字抄入 RULINGS.md；§F 入 ANOMALIES.md
11. 上繳 `features/power/docs/upstream/27_gap_closure.md`，更新 `docs/INDEX.md`

## I. 禁區

沿用 26 包 §I 全部條目，另增列：

- **不得以「閘門尚未建立」為由不補已發現之缺口**（R-P199）
- **不得為使 G129 門檻好看而刪改既有 `reasoning`**（R-P203(c)）
- **不得另開 DR 繞過 R-P197 之不開 DR 裁定**（§G）
- **寫回仍不開放**（§E）

## J. 本包產生之新條文清單（自檢）

1. R-P198 `Brand_Configuration_2` 得逐車配置，ELSE 分支須測（補落檔）
2. R-P199 R-P192 之推遲僅及於立閘門，已發現之缺口須補測
3. R-P200 下放包一經下放即凍結；增補須明示重驗
4. R-P201 A-PW139 之實測有誤，撤回 §8.4.2 判定；G82 擴充
5. R-P202 R-P195 之前提部分不符，其疑慮方向仍成立
6. R-P203 G129 門檻之限度追認，並重評批次一至三

逐條確認：**六條**，皆以獨立 fenced block 呈現於 §A，未夾於敘述中。
自檢：§A 區塊數 = 6、§J 列數 = 6、§H 步驟 10 寫「六條」，三處一致。
本次已逐一數過 §A 之 fenced block。
**本包下放後分析層不再修改**（R-P200(a)）。

## K. 分析層自判：本包是否仍有該驗而未驗者

**有，三項。**

1. **R-P201 之訂正由分析層直讀 `source_clause` 而得，
   而該讀取亦僅一次、未經第二方複驗。**
   若分析層之讀取有誤，則撤回 A-PW139 反而是錯的。
   執行層須依 B4 以帶空格之樣式獨立重掃並回報。
2. **257 條 TC 中，分析層目視者仍為少數。**
   R-P159 之分層取樣（每 leaf 至少一條 ＋ 全部 P0）至今未完整執行；
   本次積欠覆核所讀者為 leaf 層之 `source_clause` 與 `reasoning`，
   TC 本體僅讀首二批與第三批之一部分。
3. **R-P200 規範了往後，未處理本次已造成之後果** ——
   R-P198 之遲落檔已由 B1 補，而執行期間變更是否另致其他不一致，本包未查。
