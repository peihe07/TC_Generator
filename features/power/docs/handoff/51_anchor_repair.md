# 51 — G99 缺漏修補與第七批重產

下放包 | 分析層 → 執行層 | 往返 NN = 51

前置：50 包上繳已覆核，判定 **ACCEPT**。
G229 / G230 / G231 / G232 皆 PASS；**G99 FAIL（103 / 111）**，
執行層據以停並上繳，**未自行修補、未寫回 —— 處置正確**。

沿用不變之節：`§C 抽取規格` 同 02 包；`§I 禁區` 同 50 包 §I，另增列見本包 §I。

## A. 本包裁決條文（逐字，抄入 RULINGS.md）

```
[R-P327] leaf 母體變動時，素材層閘門須重跑；「沿用，期望值不變」不適用於其。
         49 包 §D 載「G0–G228 沿用，期望值不變」，
         **而該包新增 8 個 leaf** —— 素材層閘門之母體因而改變，
         `G99` 本應重跑。**分析層未察，此為其漏。**

         執行層之診斷逐字採納：
         「`G94` 之所以 PASS，是因為它比對的對象是**已宣告的
          `source_anchor`** —— **宣告窄，抄錄自然對得上，自洽而不完整**。」
         **此為「自洽而不完整」之第五例**
         （A-PW280 單向差集、EMF 1% 解析、LibreOffice 空白頁、
          WMF 26 bytes 填充、本次）。

         **通則**：閘門分二類，其「沿用」之效力不同 ——
         （a）**驗產出者**（TC 內容、格式、涵蓋）——
              其母體為該批之 TC，前批之結果得沿用
         （b）**驗素材者**（`G94` 保真、`G99` 錨點集合、
              `G103` layer3 完整性、`G0` 素材身分）——
              **其母體為 leaf 全集；凡 leaf 母體有增減，一律重跑**，
              **不得以「沿用」帶過**

         此為 R-P172（地基閘門須於首批全量執行）之延伸 ——
         該條規範首次，本條規範其後之每次母體變動。
         裁決者 Pei，逐字依據：「包 50 完成」。
```

```
[R-P328] 第七批 15 個缺漏錨點之補齊與重產。
         第七批 8 個 leaf 之 `source_anchor` **缺 15 個錨點**，
         其中 **13 個有實際內文**，最大者 `4941453` 有 **4,259 字元**
         （TLM 內部狀態對照表）。
         **即第七批之 `source_clause` 於材料上不完整**，
         其所產之 20 條 TC 係依不完整之規格所寫。

         處置（依序，不得跳步）：
         （a）以 `layer3_full.tsv` 為準，補齊該 8 leaf 之 `source_anchor` 全集
         （b）依 §C 之抽取規格重建其 `source_clause`；
              **依 R-P109 完整、依 R-P125 保真** —— 二閘皆須重跑
         （c）**重新檢視該 20 條 TC**：
              新增之錨點內容是否使其涵蓋不足、判準有誤、或有應測而未測者
         （d）依 R-P118(d) 對新增錨點跑反向涵蓋，逐項裁決
         （e）TC 數若因此變動，**據實回報，不得為維持號碼穩定而不補**

         **`4941453` 之 4,259 字元須特別檢視** ——
         其為狀態對照表，若含具名狀態與其對應值，
         可能使現有 TC 之 `expected_result` 有更精確之依據，
         亦可能揭露未測之對照項。
         裁決者 Pei，逐字依據：「包 50 完成」。
```

```
[R-P329] 三代對照表之重建不構成第四代；其理由。
         50 包已建三代對照表（280 列，2 條鍵斷裂已補齊）。
         R-P328 之重產將使 TC 數與 `tc_id` 再變。

         **惟第三代號尚未落於任何交付物** ——
         50 包依 R-P323 未寫回，`output/` 之現行副本仍為第二代（001–260）。
         故第三代僅為**提案**，其重建**不構成第四代**。

         處置：R-P328 完成後**重建三代對照表**，
         其第三代改為重產後之最終號；
         **50 包所建之版本標記為「提案版，已由重建版取代」**，
         保留不刪（比照 R-P322(a) 對舊副本之處置）。

         **併記**：若 R-P328 之後再有 leaf 母體變動，
         其第三代將再度重建 —— **只要未寫回，代數不增**。
         代數之增加以「落於交付物」為界，非以「重算次數」為界。
         裁決者 Pei，逐字依據：「包 50 完成」。
```

```
[R-P330] 50 包下放包之三處不符：逐項訂正。
         執行層依實測書寫而非依下放包字面，處置正確。三處皆為分析層之誤：

         （a）**R-P325 之「78 支」為全語料合計，非第七批** ——
              第七批僅 **3 支**。其源為分析層 49 §九之誤述，
              R-P325 承襲而未查。
         （b）**R-P322 之「後移約 20 號」低估** ——
              實測 **260 / 260 = 100%**，
              **舊副本無任何一條維持原號**。
              「約 20 號」係分析層自 TC 增量推想，未實算。
         （c）**R-P326(b) 之四個 leaf 有二處錯** ——
              列入已產出之 `SWE-PM-003`（DR-PW5 為 advisory 而非 blocking），
              **漏列 `SWE-PM-010`**。
              實測未產出者為 **`SWE-PM-008` / `010` / `089` / `112`**。

         **三處皆為分析層未經查證即書寫**，合計已達第十三次。
         其形態分別為：承襲自身舊誤（a）、以增量推想比例（b）、
         未回查 DR 之 blocking 屬性（c）。

         R-P322 / R-P325 / R-P326 依 R-P36 原文不改，
         各於其下加註指向本條。
         **交付說明中之「後移約 20 號」須改為實測之 100%。**
         裁決者 Pei，逐字依據：「包 50 完成」。
```

```
[R-P331] 寫回暫緩；R-P323 之授權條件更新。
         R-P323 定「待 Pei 就 281 列之版本另行授權後，於 51 包寫回」。
         **G99 FAIL 使該 281 列之版本不再是最終狀態** ——
         R-P328 之重產將改變其內容與條數。

         **執行層之判斷逐字採納**：
         「在其解決前，**即使你授權也不該寫回**。」

         **寫回之條件更新為**：
         （a）R-P328 之補齊與重產完成，`G99` 為 **111 / 111**
         （b）`G94` / `G109`（`source_clause` 保真與完整）重跑通過
         （c）重建後之全量 dry-run 十項全數 PASS
         （d）三代對照表重建完成
         （e）**Pei 就重產後之版本明示授權**

         **本包不寫回。**
         裁決者 Pei，逐字依據：「包 50 完成」。
```

```
[R-P332] R-P250 之驗證條件於本包二度攔下謂詞：追認其有效。
         50 包：二個謂詞被 R-P250 之驗證條件當場擋下 ——
         `df` 門檻版（`pre`/`ltm` 與 `case`/`logic`/`need` 同一帶，
         濾不掉 `equal` 又不誤傷）、
         詞幹前綴版（`pre` 被判為 `pres` 之變體而排除，
         **與 `mod ⊂ mode` 詞形完全同型，無寫法可分辨**）。

         **追認 R-P250 之有效性** —— 其「須以已知應命中之實例驗證，
         一個都取不到者不得使用」之要件，
         於本次攔下二個看似合理而實際不可用之謂詞。

         **併記其結果**：最後只留標點與黏連二個結構性子謂詞，
         **只解 1 支，77 支落人工** ——
         即該類判準之機械化上限已近，
         **人工裁決之量不會因謂詞改良而顯著下降**。
         此結論寫入驗證邊界。
         裁決者 Pei，逐字依據：「包 50 完成」。
```

## B. 本包須產出

### B1. 第七批錨點補齊與 clause 重建（R-P328(a)(b)）—— **最高優先**

補齊 15 個錨點；重建 `source_clause`；`G94` / `G99` / `G109` 重跑。

### B2. 20 條 TC 之重新檢視（R-P328(c)(d)(e)）

新增錨點是否使其涵蓋不足／判準有誤／有應測未測；
反向涵蓋逐項裁決；TC 數變動據實回報。
**`4941453` 之 4,259 字元須特別檢視並回報其內容摘要。**

### B3. 三代對照表重建（R-P329）

50 包版標「提案版，已由重建版取代」，保留不刪。

### B4. 三處訂正與加註（R-P330）

R-P322 / R-P325 / R-P326 加註；交付說明之「約 20 號」改為 **100%**；
未產出 leaf 改為 `008` / `010` / `089` / `112`。

### B5. 全量 dry-run（重產後）

十項逐項回報。

### B6. 驗證邊界更新（R-P332）

增列「反向涵蓋之機械化上限已近，人工裁決量不會因謂詞改良而顯著下降」。

## D. 閃點

G0 為前置閘。G0–G232 沿用（G17 已移除；G109 不實作）——
**惟依 R-P327，素材層閘門（G0 / G94 / G99 / G103）一律重跑，不得沿用**。

| # | 項目 | 期望值 |
|---|---|---|
| G99 | 錨點集合完整性 | **111 / 111** |
| G94 | `source_clause` 保真 | 111 / 111 |
| G103 | layer3 token 完整性 | 111 / 111 |
| G233 | 第七批錨點補齊（R-P328） | 15 個已補；`4941453` 之內容已檢視並回報 |
| G234 | 20 條重新檢視（R-P328(c)） | 逐條有結論；TC 數變動已據實回報 |
| G235 | 三代對照表重建（R-P329） | 涵蓋重產後全數；50 包版已標提案並保留 |
| G236 | 重產後 dry-run | 十項全數 PASS |
| G70 | lint 全閘 | 全 PASS |

## E. framework

§E 已定版（R-P35），本包不動。
**寫回於 R-P331 之五項齊備後**；本包不寫回。

## F. Anomaly 異動

開新號前先查現行最大號並回報（R-P147）。

- 新增：分析層以「沿用」帶過素材層閘門，致 G99 未跑（R-P327）
- 新增：「自洽而不完整」第五例 —— 宣告窄則抄錄必對（R-P327）
- 新增：第七批 `source_clause` 材料不完整，缺 15 錨點（R-P328）
- 新增：分析層三處未經查證即書寫，合計第十三次（R-P330）
- 新增：反向涵蓋之機械化上限已近（R-P332）

## G. DATA_REQUESTS

DR-PW5 之屬性訂正為 **advisory**（非 blocking）——
其所涉之 `SWE-PM-003` 已產出。
DR-PW9 / DR-PW11 維持 blocking。

## H. 作業指示

1. G0 前置閘 —— **依 R-P327 重跑，不得沿用**
2. 查現行最大號並回報
3. B1 錨點補齊與 clause 重建，驗 G99 / G94 / G103 / G233 —— **最高優先**
4. B2 20 條重新檢視，驗 G234
5. B3 三代對照表重建，驗 G235
6. B4 三處訂正與加註
7. B5 重產後 dry-run，驗 G236
8. B6 驗證邊界更新
9. 以 §D 全表自驗
10. **抄錄裁決條文前重驗一次 §J 自檢**（R-P200(c)）；
    §A 六條逐字抄入 RULINGS.md；§F 入 ANOMALIES.md
11. 上繳 `features/power/docs/upstream/51_anchor_repair.md`，更新 `docs/INDEX.md`

## I. 禁區

沿用 50 包 §I 全部條目，另增列：

- **不得寫回**（R-P331）
- **不得以「沿用」帶過素材層閘門**（R-P327）
- **不得為維持號碼穩定而不補 TC**（R-P328(e)）
- **不得刪除 50 包所建之三代對照表**（R-P329）

## J. 本包產生之新條文清單（自檢）

1. R-P327 leaf 母體變動時素材層閘門須重跑
2. R-P328 第七批 15 個缺漏錨點之補齊與重產
3. R-P329 三代對照表重建不構成第四代
4. R-P330 50 包下放包之三處不符：逐項訂正
5. R-P331 寫回暫緩；授權條件更新為五項
6. R-P332 R-P250 二度攔下謂詞：追認；機械化上限已近

逐條確認：**六條**，皆以獨立 fenced block 呈現於 §A，未夾於敘述中。
自檢：§A 區塊數 = 6、§J 列數 = 6、§H 步驟 10 寫「六條」，三處一致。
本次已逐一數過 §A 之 fenced block。
**本包下放後分析層不再修改**（R-P200(a)）。

## K. 分析層自判：本包是否仍有該驗而未驗者

**有，三項。**

1. **G99 之漏使第七批之 20 條 TC 係依不完整規格所寫** ——
   其中有多少須修改，**R-P328(c) 之檢視方知**。
   **若修改面大，本次「併入」之決定（R-P322）之代價將高於裁定當時所評估者。**
2. **分析層之失已達第十三次，且本包三處為同一包內併發。**
   歷來所立之防範（R-P183 標註來源、R-P250 先量再寫、R-P293 逐條查）
   **皆未攔下本次三處** —— 其分別為承襲舊誤、以增量推想、未回查屬性，
   **三者皆非數字或樣式問題**。
3. **`4941453` 之 4,259 字元內容分析層未讀。**
   其為狀態對照表，**可能使現有 TC 之 ER 有更精確依據，
   亦可能揭露未測之對照項** —— 二者皆會改變第七批之條數。
   本包僅令執行層檢視並回報，**分析層須於 52 包讀其摘要後方能判斷影響面**。
