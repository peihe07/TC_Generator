# 37 — 流程矯正與第一級改值

下放包 | 分析層 → 執行層 | 往返 NN = 37

前置：36 包上繳已覆核，判定 **ACCEPT**。
R-P250 於本包內即攔下三次真缺陷、二次期望值錯誤 —— 預防性機制立即生效。

**本包同時做兩件事**：流程矯正（R-P253 ~ R-P255）與**第一級改值**（R-P256）。
自 32 包起連續五包未改值，責任在分析層 ——
每包皆以「改值於下一包」推遲，致修改面愈積愈大。

沿用不變之節：`§C 抽取規格` 同 02 包；`§I 禁區` 同 36 包 §I，另增列見本包 §I。

## A. 本包裁決條文（逐字，抄入 RULINGS.md）

```
[R-P253] 改值須於發現之同包完成；推遲者須明列其前提。
         自 32 包起連續五包未改值。
         每包皆由分析層以「改值於下一包」推遲，
         而其間新發現持續累積（`design_method` / `priority` / `remarks` /
         `split_flag` / `axis` / `delta`），**修改面愈積愈大**。

         **成因為分析層迴避不可逆動作** ——
         惟迴避不可逆不等於謹慎；拖至修改面失控本身即為風險。

         規則：
         （a）某包所發現且**其判定前提已具備**者，
              **於同包內完成改值**，不得推遲
         （b）前提未具備而須推遲者，**須明列該前提為何、何時可具備**；
              未明列者不得推遲
         （c）連續二包對同一項目推遲者，須說明其何以未能具備前提
         裁決者 Pei，逐字依據：「同時做」。
```

```
[R-P254] 發現須先分流：事實錯誤者修，非事實錯誤者入限制清單。
         現況每個發現皆開裁決條文與 anomaly，
         其中相當比例為**登記而非處置**。
         R-P247 已確立正確去處 —— 未驗證之性質入交付說明之限制清單 ——
         而分析層未將其作為常規出口。

         分流判準：
         （a）**會使某條 TC 於事實上錯誤者**（違反明文契約、
              引用不存在之依據、內文與其宣稱不符）→ **修**
         （b）**不會者**（某性質無機制可驗、某閘門有涵蓋限度、
              某判準之效力邊界）→ **入 R-P247 之限制清單**，
              不開裁決條文、不開往返

         (b) 類仍須登記於 ANOMALIES，惟**不生新往返**。
         裁決者 Pei，逐字依據：「同時做」。
```

```
[R-P255] 新通則僅拘束往後；回溯適用須另有明文並先估返工面。
         近十包所立之通則（R-P214 / R-P220 / R-P227 / R-P250 等）
         皆自單一事件歸納，而**立完即要求全批回溯套用** ——
         此為返工之主要來源。

         規則：
         （a）新立之通則**僅拘束其後之作業**
         （b）欲回溯適用者，須於該條文內**另以明文載明**，
              並**先估其返工面**（受影響之條目數）後方得下達
         （c）例外：若該缺陷會使**已產出內容於事實上錯誤**，
              回溯為必要，不受本條限制 —— 惟仍須估返工面
         裁決者 Pei，逐字依據：「同時做」。
```

```
[R-P256] 改值分二級；本包完成第一級。
         **第一級 —— 事實明確者**：現值違反明文契約，其錯誤無須判斷即可確認。
         **本包一次改畢：**
         （a）`axis` 之三種非法值（`behaviour` 245 / `branch` 6 / `trigger` 3）
              —— 皆不在 §4.6 之六值內，**其為錯誤係事實**；
              **惟改成哪一值屬判斷，故本級只改「明確可映者」**：
              執行層之重判提案中，凡其 leaf 之區分軸
              **逐字見於 `split_reason` 或 `tc_title`** 者得改；
              其餘（含「無對應」40 條）留待第二級
         （b）`split_index = 0`（4 條）—— 違反 R-P115 之「自 1 起」
         （c）`split_flag = True` 而該 leaf 僅 1 條 TC（9 條）
         （d）`remarks` 無 DR 而註記（8 條）
         （e）`delta` 逐字重複（2 條）

         **第二級 —— 判斷所得者**：須第二方複核方得改。
         `design_method`（第 4 列 79 / 落底 86）、`priority`（建議 P3 40）、
         `axis` 之其餘映射。
         **其前提為分析層之抽樣複核**（36 包已備料：第 4 列 14 條、第 8 列 7 條）；
         **依 R-P253(b) 明列：該前提於 38 包具備，改值於 38 包。**

         **第一級改值後須重跑全部閘門**，回報有無因改值而新觸發者。
         裁決者 Pei，逐字依據：「同時做」。
```

```
[R-P257] `SWE-PM-025` 之 `087`≡`091`、`088`≡`092` 內文須寫入其區分。
         36 §B2 實測：該四條之四欄**逐字全同**，
         而 `tc_title` 分載二個不同觸發訊號
         （`Front_Panel_OnOff.Req` 對 `CLIMATIC_PANEL.Radio_Btn0`）——
         **TC 內文未寫入其所宣稱之區分，依現行內文執行二條操作完全相同。**

         **此為 R-P184 之落實缺口。**
         R-P184 裁維持八條，其理由為
         「二訊號為不同硬體來源，故障可只存於其中一條路徑」——
         **該理由僅於 TC 內文確實分別操作二訊號時方成立**。
         現況下二條測的是同一件事，八條之中有四條為重複。

         **裁定：裁決維持，落實補正。**
         二對之 `test_procedure` 須分別指名其觸發訊號；
         `pre_conditions` 與 `expected_result` 如有涉該訊號者一併具名。
         **此為事實錯誤（內文與其宣稱不符），依 R-P254(a) 屬第一級，本包改。**

         併查：全批是否另有「四欄逐字全同而 `tc_title` 相異」者 ——
         G168 之 C5 只比 `delta`，抓不到此形態。補設 **G178** 驗之。
         裁決者 Pei，逐字依據：「同時做」。
```

```
[R-P258] `GLUED_OR_RE` 之大小寫敏感須查；G113 之既有結論或須重估。
         36 §B4 之回溯稽核：既有 82 個謂詞中
         **大小寫敏感 9 個，最劇者 `GLUED_OR_RE` 由 2 增至 2408**。

         `GLUED_OR_RE` 為 G113（OR 分支涵蓋）之組成。
         **若其確實漏檢，則 G113 自 23 包起之全部結論皆須重估** ——
         含其「前瞻攔下第八、九、十例」與各批之未覆蓋分支計數。

         處置：
         （a）查明 2408 之組成：其中多少為真 `OR` 並列、
              多少為誤命中（如詞中之 `or`、`for` / `word` 之類）
         （b）若真 `OR` 之增量顯著，**重跑 G113 全批**並回報
              新增之未覆蓋分支數
         （c）**依 R-P255(c)，本項屬「已產出內容於事實上錯誤」之虞**，
              回溯為必要 —— 惟須先估返工面後方改值
         （d）其餘 8 個大小寫敏感謂詞同法查明，回報其增量
         裁決者 Pei，逐字依據：「同時做」。
```

## B. 本包須產出

### B1. 第一級改值（R-P256）—— **最高優先**

（a）–（e）五項一次改畢；`axis` 只改明確可映者，其餘留待第二級。
**改值後重跑全部閘門**，回報有無新觸發。

### B2. `SWE-PM-025` 四條之內文補正（R-P257）

二對之 `test_procedure` 分別指名觸發訊號；相關欄位一併具名。

### B3. G178 實作（R-P257）

驗「四欄逐字全同而 `tc_title` 相異」；全批實測；fixture 證明會 FAIL。

### B4. `GLUED_OR_RE` 與其餘 8 個大小寫敏感謂詞之查明（R-P258）

2408 之組成分析；真 `OR` 增量；若顯著則重跑 G113 並估返工面。
**本包不因此改值** —— 返工面估出後於 38 包裁定。

### B5. 條文訂正登記

R-P248 漏列 `branch`（第三種非法值）—— 依 R-P36 於其下加註，原文不改。

## D. 閃點

G0 為前置閘。G0–G177 沿用（G17 已移除；G109 不實作），期望值不變。

| # | 項目 | 期望值 |
|---|---|---|
| G178 | 四欄逐字全同而 `tc_title` 相異（R-P257） | 【實測填入】全批命中數；`087`/`091`、`088`/`092` 修正後不再命中；fixture 證明會 FAIL |
| G179 | 第一級改值後之閘門重跑（R-P256） | 全部閘門重跑；【實測填入】新觸發數；有新觸發者逐條說明 |
| G180 | `GLUED_OR_RE` 組成分析（R-P258） | 【實測填入】2408 之真 `OR` 數與誤命中數；G113 之預估返工面 |
| G70 | lint 全閘 | 改值後仍全 PASS |

## E. framework

§E 已定版（R-P35），本包不動。
**寫回仍不開放** —— 第二級改值於 38 包，其前提為分析層之抽樣複核。

## F. Anomaly 異動

開新號前先查現行最大號並回報（R-P147）。

- 新增：分析層連續五包推遲改值，修改面愈積愈大（R-P253）
- 新增：`SWE-PM-025` 四條內文與其宣稱不符，R-P184 之落實缺口（R-P257）
- 新增：`GLUED_OR_RE` 大小寫敏感，G113 既有結論或須重估（R-P258）
- R-P248 → 依 B5 加註，漏列 `branch`

## G. DATA_REQUESTS

DR-PW9 ~ DR-PW15 沿用。無新增。

## H. 作業指示

1. G0 前置閘
2. 查現行最大號並回報
3. B1 第一級改值，驗 G179 —— **最高優先**
4. B2 `SWE-PM-025` 內文補正
5. B3 G178 實作，驗 G178
6. B4 `GLUED_OR_RE` 查明，驗 G180 —— **本包不因此改值**
7. B5 R-P248 加註
8. 以 §D 全表自驗
9. **抄錄裁決條文前重驗一次 §J 自檢**（R-P200(c)）；
   §A 六條逐字抄入 RULINGS.md；§F 入 ANOMALIES.md
10. 上繳 `features/power/docs/upstream/37_first_tier_edit.md`，更新 `docs/INDEX.md`

## I. 禁區

沿用 36 包 §I 全部條目，另增列：

- **第二級改值於本包不得為之**（R-P256）
- **`axis` 之「無對應」40 條不得於本包改值**（R-P256(a)）
- **不得因 `GLUED_OR_RE` 之查明結果於本包改任何值**（R-P258(c)）
- **新立之通則不得於本包回溯適用**（R-P255）

## J. 本包產生之新條文清單（自檢）

1. R-P253 改值須於同包完成；推遲者須明列前提
2. R-P254 發現須先分流：事實錯誤修，非事實錯誤入限制清單
3. R-P255 新通則僅拘束往後；回溯須明文並先估返工面
4. R-P256 改值分二級；本包完成第一級
5. R-P257 `SWE-PM-025` 四條內文須寫入其區分（R-P184 之落實）
6. R-P258 `GLUED_OR_RE` 之大小寫敏感須查；G113 或須重估

逐條確認：**六條**，皆以獨立 fenced block 呈現於 §A，未夾於敘述中。
自檢：§A 區塊數 = 6、§J 列數 = 6、§H 步驟 9 寫「六條」，三處一致。
本次已逐一數過 §A 之 fenced block。
**本包下放後分析層不再修改**（R-P200(a)）。

## K. 分析層自判：本包是否仍有該驗而未驗者

**有，三項。**

1. **`axis` 之 264 條提案僅 16 條經實讀驗證**（36 §九）。
   本包只改「明確可映者」即為因應 —— 其餘 254 條之映射
   仍待第二方複核，**改值本身亦無閘門驗其正確**。
   G179 只驗改值後閘門有無新觸發，不驗改對了沒有。
2. **R-P254 之分流判準由分析層自定，其寬嚴影響後續往返數。**
   判「非事實錯誤」愈寬，往返愈少而限制清單愈長 ——
   **該傾向對「加快進度」有利**，依 R-P187 之精神明載。
   分流之逐項結果須列出供覆核。
3. **第二級改值之前提為分析層之抽樣複核，而該複核尚未執行。**
   36 包已備料（第 4 列 14 條、第 8 列 7 條）。
   依 R-P253(b) 明列：**該前提於 38 包具備** ——
   分析層須於 38 包執行，不得再推遲。
