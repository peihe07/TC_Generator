# 70 — 69 回報覆核；複合目標改走「原子 token 字典」

下放包 | 分析層 → 執行層 | 往返 NN = 70

前置：`data/69_report.md` 已覆核，判定 **ACCEPT**。依 R-P394(c) 停手正確；
三閘殘留為判準歸屬問題之診斷正確；標記自身觸發判準之自陳與修正正確；
丁案於 B5 範圍 0 條之嚴格判法正確；「99 → 80」定義差指出正確，69 包 §0 該格採 80。
寫回移至 71 包，續受 S6 阻斷。

## 0. 分析層判斷

104 條之殘留全為「一步多目標，至少一目標非白名單」。66 包對 39 名之人讀顯示：
非白名單目標之**原子種類有限**（音源／畫面／音量／TLM 態／通話／網路…），
每一原子對應之白名單代理量在 66 包 §1 表已有先例。
故不必再逐條人讀 104 條，改為：**執行層把複合目標拆成原子 token 去重，分析層對 token 建字典，機器套用**。
此為既有裁決之整理，非新判準；字典每一條目引 66 包 §1 表或錨點。

## A. 本包裁決條文（逐字，抄入 RULINGS.md）

```
[R-P395] 複合觀察目標以「原子 token 字典」處置；B5 剩餘 72 條先跑同型規則。
         （a）執行層對未歸零之 104 條（及剩餘 72 條跑完後之同型殘留）：
              取 `Read <X> …` / `Check that <X> …` 之 <X>，以 ` and ` / ` against ` / `, ` 拆為原子 token，
              全案去重，每 token 列：出現次數、所在 tc_id 清單、一個代表錨點段落全文；
              落 `data/composite_tokens_70.md`，**不判定**
         （b）分析層於 71 包對每 token 裁一個白名單代理量（引 66 包 §1 表先例或錨點），
              成 `data/proxy_dictionary_71.md`：`token | 代理量 | 類別 | 依據`；
              查無先例且錨點無可觀察面者記 `PENDING: DR-PW{n}`，不造
         （c）機器套用字典：一步多 token → 每 token 一句 check（同一步內以 `;` 續列或拆步，ER 對齊）；
              套用後重跑該三閘，殘留者為字典未覆蓋 token，回 (a) 補一輪
         （d）B5 剩餘 72 條以 69 包八條規則**先跑**，其殘留併入 (a) 之 token 母體
         （e）丁案於 B5 範圍結案：R-P394(b) 丁案項記「0 條，依 R-P376(c) 嚴格判準」
         裁決者：分析層（Tier 2）。
```

## H. 作業指示

1. 抄 R-P395；R-P394(b) 丁案項加註結案
2. 剩餘 72 條跑八條規則，出該批六閘
3. `composite_tokens_70.md` 供料（母體 = 104 ＋ 72 之殘留）→ **停**
4. 上繳 `features/power/docs/upstream/70_tokens.md`，附 G255 表

## I. 禁區

沿用 69 包 §I。token 拆分不得合併同義（`HU mode` 與 `TLM state` 為二 token，由分析層判是否同物）；供料不得預填代理量。

## J. 自檢

一條。對既有 canon：R-P353 — 字典為其代理量指定之集中實作，合；R-P381/R-P384 — 人讀單位由「名」改「token」，非取消人讀，加註；R-P394(b)(c) — (d) 為其續行，合；§8.4.1 — (b) 不造，合。

本包數字（R-P379(a)）：104／72／83／119（`69_report.md` §5–§6）。

## K. 待 Pei

無阻斷項。站④ 抽查 75 條（`reviewed_75_69.md`）仍在你手上。
