# 31 — design_method 分布、分層取樣與陳舊產物全掃

下放包 | 分析層 → 執行層 | 往返 NN = 31

前置：30 包上繳已覆核，判定 **ACCEPT**。
G146 已實作（四項重複皆 0）；DR-PW15 已開；R-P220 當場揪出二項陳舊。

**分析層已完成 R-P217 之複核**：20 項逐項讀畢，**同意 20 / 20**
（對照 14 項那次為同意 12 / 不同意 2）——
差別在於本批之依據皆指得出來，其中第 13–18 項之
「clause 載『set … **back to** `False`』蘊含起始為 `True`」
為實質推論而非套語。

**分析層接受執行層之二項更正**：
（甲）T24 與 R-P159 連續三包未動，其原因為**下放包未要求**，
      非工作量排擠 —— 分析層於 30 §K 所書之理由為推託。本包二項皆排入。
（乙）R-P220 所揪出之二項陳舊「同型問題在別處還有多少，我沒有查」——
      本包全掃。

沿用不變之節：`§C 抽取規格` 同 02 包；`§I 禁區` 同 30 包 §I，另增列見本包 §I。

## A. 本包裁決條文（逐字，抄入 RULINGS.md）

```
[R-P221] `SWE-PM-064` 之 Timed 前提維持，但 `reasoning` 須補依據。
         執行層指出 `4941718` 之第一條件載 `in TLM Full-Operation state`，
         **第二條件通篇無狀態名**，疑其 Timed 前提縮小適用範圍。

         **裁定：維持 Timed。**
         第二條件之觸發為「`Timeout1` 到期」，
         而 `Timeout1` 僅於 Timed 狀態下計時 ——
         依 `SWE-PM-014` 之 Behaviour 1 / 2（點火轉換時進 Timed）
         與 `SWE-PM-038` 之 Case 1–3（Timed 下 `Timeout1` 到期）。
         故 Timed **非縮小範圍，而係該觸發唯一能發生之狀態**。

         **惟該推論依賴 sibling leaf，而 `030` 之 `reasoning` 未載。**
         處置：
         （a）於 `030` 之 `reasoning` 載明係**哪一條 sibling clause**
              建立「`Timeout1` 僅於 Timed 計時」，逐字引之
         （b）**若遍查 sibling clause 皆無此明文**
              → 本裁定之前提不成立，改標待查並列入 DR 候選，
                不得以本條為由結案
         裁決者 Pei，逐字依據：「那你要給我指令」。
```

```
[R-P222] 引用 sibling clause 作為 Pre-Condition 依據之界線（§8.5 張力登記）。
         20 項中第 1–9 項之 `Full-Operation` 前提，
         其依據為 `SWE-PM-061` 之 clause
         「These settings could be only done in TLM Full-Operation Status」。
         §8.5 禁止「他 RD 所有之環境穩定性前提」進 Pre-Condition。

         **分析層判定可接受**，理由：該狀態非環境穩定性，係**閘門條件** ——
         無之則 procedure 之「於 menu 選值」根本無法執行，
         省略反致 §7 之 false fail。

         **惟該界線甚細，登記以免被過度推廣。**
         判準：引用 sibling clause 作為 Pre-Condition 依據者，
         須同時滿足二事：
         （a）該狀態為 procedure 得以執行之**必要條件**
              （缺之則步驟無法完成或必然失敗），且
         （b）該必要性有 sibling clause 之**逐字明文**，非推定

         僅滿足 (b) 而不滿足 (a) 者 —— 即該狀態只是「通常如此」——
         仍屬 §8.5 所禁之環境穩定性前提，應移除。

         處置：以本判準複查 20 項中依 sibling clause 為據者，
         逐項回報其是否同時滿足 (a)(b)。
         裁決者 Pei，逐字依據：「那你要給我指令」。
```

```
[R-P223] `design_method` 之分布須量測（T24）。
         分析層於批次四尾段讀四條，見二條可疑：
         `155`（讀出廠預設值，procedure 為「讀參數、讀參數」，
          ER 為「讀到某值」，**無任何 A → B 之轉換**）
         與 `154`（送 DID 值 → logo 取代）皆判為「狀態轉換」。
         依 §12 first-match，`155` 應落於末列
         「Single feature check → Functional Based」。

         **此為推測非實測**（四條之樣本，R-P64）。
         且執行層於 22 §八第 4 項自陳
         「若我的 first-match 順序理解有偏差，**會系統性地偏向同一值**」。

         現行 G72 只驗 `design_method` 是否為下拉選單九詞條之一，
         **不驗其選得對不對**。

         處置：
         （a）量全 264 條之 `design_method` 值分布，逐 Test Set 呈現
         （b）凡某一值於某 Test Set 佔比 ≥ 60% 者，
              自該群抽樣（率 ≥ 16.7%，種子載明）
              做 §12 **逐列 first-match 走查**，
              回報每條之首個命中列與其依據
         （c）走查所得之不符者逐條列出；**本包不逕行改值** ——
              §12 之指派影響交付內容，裁定於 32 包
         裁決者 Pei，逐字依據：「那你要給我指令」。
```

```
[R-P224] R-P159 之分層取樣覆核由執行層備料，分析層執行。
         R-P159 裁定分析層每批之覆核範圍為
         「全部 leaf 之 `source_clause` 與 `reasoning`（全讀）＋
          反向涵蓋報告（全讀）＋ TC 全文（分層取樣：
          每 leaf 至少一條 ＋ 全部 P0）」。
         **該分層取樣至今未完整執行** —— 批次五、六之 TC 本體未讀。

         處置：執行層備料，分析層讀。
         上繳須附 `data/sampling_for_review.md`，內含：
         （a）**全部 P0 之 TC**（十六欄逐條）
         （b）**每 leaf 至少一條**（P0 已涵蓋者不重複；
              未有 P0 之 leaf 取其 `split_index = 1` 者）
         （c）取樣清單與其涵蓋率（leaf 涵蓋數 / 103、TC 數 / 264）
         分段附上，每段不逾 8 條，**不得節錄欄位**。
         裁決者 Pei，逐字依據：「那你要給我指令」。
```

```
[R-P225] 陳舊產物全掃（R-P220 之擴大適用）。
         30 §一：R-P220 當場揪出二項陳舊
         （`g136_pattern_variants.md` 之總括語對 `audit_precond_state.py`
          已為假；`g145_gate_triggers.md` 之 G137 擷取式因改口徑而失配），
         **而二者皆為執行層自身之產物**；
         執行層自陳「同型問題在別處還有多少，我沒有查」。

         處置：對 `features/power/data/` 下**全部**腳本產物：
         （a）逐一重跑其產生腳本，比對輸出是否逐位元組相同
         （b）不同者列出差異並判其為
              「陳舊」（資料已變而產物未更新）或
              「非決定性」（含時戳等每次必異之內容）
         （c）**另驗其敘述性內容之現時真確性** ——
              二項陳舊之一（`g136`）並非數值過期，
              而是**總括語句對新增之腳本已為假**；
              逐位元組比對抓不到此類，須人工逐份判讀其總括語句
         （d）逐份回報其判讀結果與依據
         裁決者 Pei，逐字依據：「那你要給我指令」。
```

## B. 本包須產出

### B1. `design_method` 分布與 first-match 走查（R-P223）—— **最高優先**

逐 Test Set 之值分布；佔比 ≥ 60% 者抽樣走查；
不符者逐條列出。**不逕行改值。**

### B2. 分層取樣備料（R-P224）

`data/sampling_for_review.md`；全部 P0 ＋ 每 leaf 至少一條；
分段附於上繳，每段不逾 8 條，不得節錄欄位。

### B3. 陳舊產物全掃（R-P225）

`data/` 下全部產物；逐位元組比對 ＋ 敘述性內容之人工判讀；
逐份回報。

### B4. `SWE-PM-064` 之 `reasoning` 補依據（R-P221）

引 sibling clause 逐字；遍查皆無者改標待查並列 DR 候選。

### B5. §8.5 界線複查（R-P222）

20 項中依 sibling clause 為據者，逐項回報是否同時滿足 (a)(b)。

## D. 閃點

G0 為前置閘。G0–G149 沿用（G17 已移除；G109 不實作），期望值不變。

| # | 項目 | 期望值 |
|---|---|---|
| G150 | `design_method` 分布（R-P223） | 【實測填入】逐 Test Set 之值分布；≥ 60% 者之抽樣走查結果與不符數 |
| G151 | 分層取樣備料（R-P224） | 全部 P0 已附；leaf 涵蓋 103 / 103；取樣率已載 |
| G152 | 陳舊產物全掃（R-P225） | 【實測填入】產物總數；逐位元組相異數；判為陳舊者；敘述性內容判讀之不真確數 |
| G153 | §8.5 界線複查（R-P222） | 依 sibling clause 為據者逐項回報 (a)(b) 之滿足情形 |
| G70 | lint 全閘 | 全 PASS |

## E. framework

§E 已定版（R-P35），本包不動。
**寫回仍不開放** —— 阻斷條件為 R-P223 之走查結果與 R-P224 之分析層覆核。

## F. Anomaly 異動

開新號前先查現行最大號並回報（R-P147）。

- 新增：`design_method` 疑系統性偏向「狀態轉換」，G72 不驗其正確性（R-P223）
- 新增：R-P159 之分層取樣連續四包未執行，原因為下放包未要求（R-P224）
- 新增：逐位元組比對抓不到「總括語句對新增內容已為假」之陳舊（R-P225(c)）
- 新增：引用 sibling clause 作為 Pre-Condition 依據之界線（R-P222）

## G. DATA_REQUESTS

DR-PW9 ~ DR-PW15 沿用。
R-P221(b) 若成立，開新 DR（編號查後開，R-P147）。

## H. 作業指示

1. G0 前置閘
2. 查現行最大號並回報
3. B1 `design_method` 分布與走查，驗 G150 —— **最高優先；不逕行改值**
4. B2 分層取樣備料，驗 G151
5. B3 陳舊產物全掃，驗 G152
6. B4 `SWE-PM-064` 補依據
7. B5 §8.5 界線複查，驗 G153
8. 以 §D 全表自驗
9. **抄錄裁決條文前重驗一次 §J 自檢**（R-P200(c)）；
   §A 五條逐字抄入 RULINGS.md；§F 入 ANOMALIES.md
10. 上繳 `features/power/docs/upstream/31_design_method.md`，更新 `docs/INDEX.md`

## I. 禁區

沿用 30 包 §I 全部條目，另增列：

- **不得逕行變更任何 `design_method` 之值**（R-P223(c)）
- **不得節錄 B2 取樣之任何欄位**（R-P224）
- **不得僅以逐位元組比對認定產物非陳舊**（R-P225(c)）
- **`SWE-PM-064` 遍查無 sibling 明文者不得以 R-P221 結案**（R-P221(b)）

## J. 本包產生之新條文清單（自檢）

1. R-P221 `SWE-PM-064` 之 Timed 維持，`reasoning` 須補 sibling 依據
2. R-P222 引用 sibling clause 作為 Pre-Condition 依據之界線
3. R-P223 `design_method` 之分布須量測（T24）
4. R-P224 R-P159 之分層取樣：執行層備料、分析層執行
5. R-P225 陳舊產物全掃，含敘述性內容之人工判讀

逐條確認：**五條**，皆以獨立 fenced block 呈現於 §A，未夾於敘述中。
自檢：§A 區塊數 = 5、§J 列數 = 5、§H 步驟 9 寫「五條」，三處一致。
本次已逐一數過 §A 之 fenced block。
**本包下放後分析層不再修改**（R-P200(a)）。

## K. 分析層自判：本包是否仍有該驗而未驗者

**有，二項。**

1. **R-P223 之 60% 門檻由分析層憑印象所訂，無依據。**
   依 R-P64 標為**推測**。若某 Test Set 之分布本就應集中
   （例如 Power State 多為狀態轉換屬合理），
   60% 會造出無意義之走查；若偏向更隱蔽，60% 又攔不到。
   **本門檻僅為首次量測之起點，不得作為判準沿用**；
   實測分布出來後應據以重訂。
2. **R-P224 之取樣覆核，分析層尚未執行。**
   本包僅要求備料。**若 32 包分析層仍未讀完，
   須說明其何以再度延宕** —— 30 §四已指出
   「素材備妥而複核未發生」為既存形態。
