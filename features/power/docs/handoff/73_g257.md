# 73 — 72 回報與站④-1 覆核；§5.2 之適用欄位訂正

下放包 | 分析層 → 執行層 | 往返 NN = 73

前置：`data/72_report.md`、`data/station4_findings_72.md` 已覆核，判定 **ACCEPT**。
G256 判準（同名時而加引號時而不加）設計正確；sandbox MANIFEST 取徑正確（交付台帳不登未交付檔）；
站④ 結構／欄位／一致性三節全過；站④-1 之成因分析正確；R-P366(b) 前提已變之指出正確。
S6 由 Pei 明文裁甲（執行層 session 逐字「甲」），記入本包。

## 0. 站④-1 之覆核 —— 執行層量錯了欄位

IN §5.2 之字數限制（A ≤12、B ≤18）**約束 Procedure 步驟**，ER 無字數限制（IN §6 僅要求 1:1 對齊、可觀察；
§6.1 允許 `a./b./c.` 子項）。站④-1 量的是 **ER 末步**，非 §5.2B 之標的。
故七條**不構成違規**；惟同一成因（ITD 內聯、`;` 串接）**很可能同樣落在 Procedure**，該處才受 §5.2 約束，本包令重量。

## A. 本包裁決條文（逐字，抄入 RULINGS.md）

```
[R-P398] S6 裁甲記錄；站④-1 撤銷並改量 Procedure；`;` 串接之處置；R-P366(b) 前提訂正。
         （a）S6：Pei 裁甲。146 條含 PENDING 者不出貨，寫回順延至七張 DR 回覆；
              乙／丙未開。`sandbox/b72/pm_72.xlsx` 為站④ 通過版，不入 delivered/
         （b）站④-1 撤銷：IN §5.2 約束 Procedure，ER 無字數上限；七條 Remarks 之 `(站④-1 …)` 移除。
              新增 G257：Procedure 非末步 ≤12 字、末步 ≤18 字（§5.2A/B；含 `to …` 例外者依 §5.2C ≤18），
              全案重量；逾限者依 R-P366(b) 拆步、ER 對齊，**不得刪減資料**
         （c）`;` 串接：Procedure 內一律改拆步（每 token 一步，§5.7 之 Final Step 仍為單一驗證擁有者，
              前面各 check 為 setup 證明步）；ER 內保留 `;` 或改 §6.1 `a./b./c.` 子項，二擇一全案一致，取子項式
         （d）R-P366(b) 之「回指步從來不是末步（0/163）」實測結論作廢：B5 R3 改動步序後前提不成立；
              R-P366 依 R-P36 原文不改，加註
         （e）MANIFEST：`sandbox/b72/MANIFEST.tsv` 取徑採；R-P397(a)「SHA 記 MANIFEST」自此指 sandbox 之台帳
         裁決者：分析層（Tier 2；(a) 為 Pei 裁定之記錄）。
```

## H. 作業指示

1. 抄 R-P398；R-P366 加註；七條 Remarks 移除站④-1 標記
2. G257 實作，全案重量 Procedure，逾限者拆步；`;` 串接依 (c) 改
3. 六閘＋G256＋G257 全跑；`pm_73.xlsx` 重出（sandbox/b73，MANIFEST）；目視包重出
4. 上繳 `features/power/docs/upstream/73_g257.md`，附 G255／G256／G257 表

## I. 禁區

沿用 72 包 §I。ER 不得為字數拆行（R-P398(b)）；Procedure 拆步不得刪資料（R-P366(b)）。

## J. 自檢

一條。對既有 canon：IN §5.2／§6／§6.1 — (b)(c) 為其正確適用；R-P366(b) — (d) 訂正前提，加註；R-P395(c) — (c) 收斂其「`;` 或拆步」為全案一致；S6／R-P374(a) — (a) 合。

本包數字（R-P379(a)）：7／48／41（`station4_findings_72.md` §C）；146／287／10／3（`72_report.md` §4）。

## K. 待 Pei

無阻斷項。**DR 是否已送**仍未答（72 包 §K-2）——甲案下這是唯一推進途徑。
