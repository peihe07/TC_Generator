# 24 — G113 訂正、地基閘門時序與第四批

下放包 | 分析層 → 執行層 | 往返 NN = 24

前置：23 包上繳已覆核，判定 **ACCEPT**。
執行層於 G113 = 2/7 時停並上繳、未啟動第四批，處置正確。
G113 已主動攔下第八、第九例（`SWE-PM-014` / `SWE-PM-018`）——
**首次由閘門攔下而非事後補救。**

**分析層三項錯誤，於本包處置**：
（甲）R-P161 稱「已重複七次」為錯誤歸納 —— 其中二項非 OR 結構；
（乙）G103 全量掃描應於首批即為之，排序錯誤；
（丙）R-P135 之 ECU 比對未經正規化即採信「相異」。

沿用不變之節：`§C 抽取規格` 同 02 包；`§I 禁區` 同 23 包 §I，另增列見本包 §I。

## A. 本包裁決條文（逐字，抄入 RULINGS.md）

```
[R-P169] 分隔符集合之修正屬實作瑕疵訂正，授權為之。
         23 §G113：`OR` 右運算元以 `( If …` 起首，
         而 `IF` 誤列於分隔符集合內，致運算元被截斷丟棄。

         判別：
         - 調**門檻**或**判準**使已知案例通過 → 遷就答案，禁止
         - 修**分詞器**之錯誤分類 → 訂正實作瑕疵，授權

         `"Active" (If LTM High is present: …)` 中之 `If`
         為運算元之一部分，非分隔符 —— 其被列為分隔符係客觀錯誤，
         與首版「反轉字串配正向詞邊界永不匹配」之 0/7 bug 同類，
         而該 bug 已修且分析層接受。

         處置：
         （a）自分隔符集合移除 `IF` 及同類之條件引導詞，
              **其理由須寫成結構性依據**（為何該詞不是分隔符），
              不得以「改後三項通過」為理由
         （b）修正後重跑全部實例，逐項回報
         （c）**修正不得觸及 OR 之辨識規則本身**，僅限運算元定界
         裁決者 Pei，逐字依據：「一次下放」（回應 23 §G113）。
```

```
[R-P170] R-P161 之「已重複七次」為錯誤歸納，訂正。
         23 §G113 實測：17 包 `greater` 與 22 包 VR 長按
         **根本不是 OR 結構**（`and if` / `both … and`），
         不應由 G113 捕獲。

         分析層將三種不同形態歸為同一類，
         再以該錯誤歸納設定 G113 之驗證條件（七項全數重現）——
         **驗證條件本身建立於錯誤前提上**。
         此為 R-P64 所拘束之未經查證量化陳述，第三次發生
         （前二次：A-PW30 之「最大」、R-P157 之「應可機械檢查」）。

         訂正後之 G113 驗證條件：**五項 OR 結構實例全數重現**
         （16 包 `BODY OFF-TIMED`、18 包 `Ignition Pre Off`、
          22 包 LTM High ×3）；
         第八、第九例（`SWE-PM-014` / `SWE-PM-018`）為前瞻捕獲，另計。

         `greater` 與 VR 長按所屬之形態（條件並列 / 操作變體）
         **登記為未機械化之缺陷類**，不併入 G113，
         其捕獲仍賴反向涵蓋與人讀。
         R-P161 依 R-P36 原文不改，於其下加註指向本條。
         裁決者 Pei，逐字依據：「一次下放」（回應 23 §G113）。
```

```
[R-P171] G113 之未覆蓋分支採分桶，降低裁決負擔。
         23 實測：未覆蓋分支 55、真陽性 2（3.6%）。
         R-P166 方將透鏡 1 之裁決負擔降下，G113 立即加回一份
         （23 §八第 2 項）。

         處置比照 R-P127 對殘差詞之作法：
         未覆蓋分支歸入三桶並給計數 ——
           `規格未定義該支`／`已由他條或他 leaf 涵蓋`／`真缺口`
         僅 `真缺口` 需完整理由並依 R-P118(d) 裁決；
         前二桶給計數與抽樣例示（抽樣率不低於 16.7%）。
         回報真陽性率，逐批追蹤。
         裁決者 Pei，逐字依據：「一次下放」（回應 23 §八第 2 項）。
```

```
[R-P172] 地基閘門須於首批即全量執行（通則，canon 候選）。
         23 §G114：全量掃描得不相等 2 leaf / 4 item
         （`SWE-PM-008` 缺 `4941425`/`4941430`/`4941433`、
          `SWE-PM-010` 缺 `4941984`）。

         執行層之判斷逐字採納：
         「`SWE-PM-008` 缺三個 item，而它早因 DR-PW6 排除 ——
          **若沒有 DR-PW6，22 包就會拿一份缺三個錨點的
          `source_clause` 去寫它，而 G94/G99 全綠。
          那是運氣不是流程。**」

         G103 應於首批即全量執行，而非於第四批方為之 ——
         此為分析層之排序錯誤（R-P144 立條時僅要求對已產出 leaf 執行）。

         通則：凡驗證**素材本身**而非驗證產出之閘門
         （素材身分、錨點鏈完整性、來源保真度一類），
         一律於**首批之前對全量素材執行**，不得隨批次逐步擴大。
         驗證產出之閘門（TC 內容、格式、涵蓋）方隨批次執行。
         裁決者 Pei，逐字依據：「一次下放」（回應 23 §G114）。
```

```
[R-P173] ECU 差異須以集合正規化後比對。
         23 §八第 3 項：`SWE-PM-025` 三對僅 ECU 相異，
         執行層自陳無能力判斷該差異是否重要。

         分析層查 20 §二之 R-P135 屬性表：其 ECU 值為
         `LTM, ETM, RRM` 對 `RRM, LTM, ETM` ——
         **同一集合之不同排列**，非真差異。
         分析層當時採信「相異」而未正規化即比對，為第三項錯誤。

         處置：
         （a）ECU / EE Architecture / Radio / Market 等**多值欄**
              一律以**集合**比對（去空白、統一大小寫、排序後比較），
              不以字串比對
         （b）以此重跑 `SWE-PM-025` 三對與 R-P135 三對，
              回報正規化後之相同／相異
         （c）若正規化後 `SWE-PM-025` 三對之 ECU 相同，
              則其八條之拆分依據僅存「觸發訊號不同」，
              依 §5.7 成立，**不需合併**，R-P167 就此結案
         （d）若正規化後仍相異，附各對之集合差集，裁定於 25 包
         裁決者 Pei，逐字依據：「一次下放」（回應 23 §八第 3 項）。
```

```
[R-P174] 第四批啟動：Power State 之 `SWE-PM-033`–`063`（31 leaf）。
         23 已依 R-P165 逐一比對全部 live DR 之影響面，
         該 31 leaf 不受任何 live DR 影響。

         啟動條件更新為：
         （a）G113 依 R-P169 修正後，**五項 OR 實例全數重現**（R-P170）
         （b）R-P172 之全量地基閘門已執行完畢（23 已完成 G103 全量）
         二者齊備即啟動，不待 R-P173 之裁定
         （其影響限於 `SWE-PM-025` 等已產出者，不影響第四批之新 leaf）。

         `SWE-PM-001`–`010` 仍一律不納入（R-P168）。
         tc_id 沿用臨時編號，接續第三批之末。
         裁決者 Pei，逐字依據：「一次下放」。
```

```
[R-P175] B4 快照失效之處置：不重建，登記。
         23 §21 包補執行：B4 快照之 SHA256 取自 23 包當下，
         非 21 包當時之值（22 包已改動第二批），該值**已無法取得**。
         執行層據實回報其對原始用途已失效。

         處置：**不重建、不以現值冒充**。
         登記為 anomaly，記明其成因為
         「下放包 21 未即時執行，而其間產物已變動」。
         此為 R-P164 所處置之台帳缺口之**衍生損失** ——
         補執行可補條文，補不回當時之狀態。
         裁決者 Pei，逐字依據：「一次下放」（回應 23 §21 包補執行）。
```

```
[R-P176] G108 之限度明載：紀律加工具，非機制。
         23 §G108：`check_edit_integrity.py` 三層檢查
         （語法／載入／符號）已實作，fixture 以 20 包之實際損壞形態建構
         （刪四函式時語法仍 True 而符號層攔下）。
         **惟其不保證「同一步內完成」—— 何時執行仍靠執行者。**

         追認該自陳，不強令改為機制 ——
         於當前工具形態下無法強制執行時點。
         登記為已知限制：R-P150 之「同一步內完成」為紀律要求，
         其違反不會被任何機制攔下。
         裁決者 Pei，逐字依據：「一次下放」（回應 23 §G108）。
```

## B. 本包須產出

### B1. G113 修正與重驗（R-P169 / R-P170）—— **第四批前置**

- 自分隔符集合移除 `IF` 及同類條件引導詞，**理由寫成結構性依據**
- 修正不得觸及 OR 之辨識規則本身
- 重跑：**五項 OR 實例須全數重現**；`greater` 與 VR 長按不列入
- 為 R-P161 加註（依 R-P36 原文不改），雜湊佐證原文未變

### B2. G113 分桶（R-P171）

未覆蓋分支歸三桶並給計數；`真缺口` 完整理由；
前二桶抽樣率 ≥ 16.7%；回報真陽性率。

### B3. ECU 集合正規化比對（R-P173）

`SWE-PM-025` 三對 ＋ R-P135 三對，正規化後回報相同／相異；
相異者附集合差集。

### B4. 第四批（R-P174）

B1 完成後啟動；31 leaf；上繳內容依 R-P158 所列四項。

## D. 閃點

G0 為前置閘。G0–G116 沿用（G17 已移除；G109 不實作），期望值不變。

| # | 項目 | 期望值 |
|---|---|---|
| G113 | OR 分支涵蓋（R-P170 訂正後） | **五項 OR 實例全數重現**；第八、第九例另計 |
| G117 | ECU 集合正規化（R-P173） | 【實測填入】六對之正規化後相同／相異 |
| G118 | R-P161 加註後原文位元組未變 | UNCHANGED |
| G119 | 第四批產出（R-P174） | 31 leaf；`spec_reference` 全部指向 CFTS009 |
| G120 | 第四批反向涵蓋 ＋ G113 | 【實測填入】三桶計數、真陽性率、事前未知缺口數 |
| G70 | lint 全閘 | 全 PASS |

## E. framework

§E 已定版（R-P35），本包不動。

## F. Anomaly 異動

開新號前先查現行最大號並回報（R-P147）。

- 新增：R-P161 之「七次同型」為錯誤歸納，驗證條件建於錯誤前提（R-P170）
- 新增：G103 全量應於首批即為之，分析層排序錯誤（R-P172）
- 新增：分析層採信未正規化之 ECU「相異」（R-P173）
- 新增：B4 快照失效，為台帳缺口之衍生損失，不可重建（R-P175）
- 新增：G108 之「同一步內完成」無機制可攔（R-P176）

## G. DATA_REQUESTS

DR-PW9（High）、DR-PW10（Medium）、DR-PW11（High，已擴大至 4 item / 2 leaf）沿用。
無新增。

## H. 作業指示

1. G0 前置閘
2. 查現行最大號並回報
3. 執行 B1 G113 修正與重驗 —— **五項須全數重現**
4. 執行 B2 分桶
5. 執行 B3 ECU 正規化比對，驗 G117
6. B1 達成後啟動 B4 第四批，驗 G119 / G120
7. 以 §D 全表自驗
8. §A 八條裁決逐字抄入 RULINGS.md；§F 入 ANOMALIES.md
9. 上繳 `features/power/docs/upstream/24_batch4.md`，更新 `docs/INDEX.md`

## I. 禁區

沿用 23 包 §I 全部條目，另增列：

- **G113 之修正僅限運算元定界，不得觸及 OR 之辨識規則**（R-P169(c)）
- **不得以「改後通過」作為分隔符修正之理由**（R-P169(a)）
- **不得將 `greater` / VR 長按併入 G113**（R-P170）
- **不得重建或以現值冒充 B4 快照**（R-P175）
- **不得於五項 OR 實例未全數重現前啟動第四批**（R-P174(a)）

## J. 本包產生之新條文清單（自檢）

1. R-P169 分隔符集合之修正屬實作瑕疵訂正，授權
2. R-P170 R-P161 之「七次同型」為錯誤歸納，訂正為五項
3. R-P171 G113 未覆蓋分支採分桶
4. R-P172 地基閘門須於首批即全量執行（canon 候選）
5. R-P173 ECU 差異須以集合正規化後比對
6. R-P174 第四批啟動：`SWE-PM-033`–`063`（31 leaf）
7. R-P175 B4 快照失效不重建，登記
8. R-P176 G108 之限度明載：紀律加工具，非機制

逐條確認：**八條**，皆以獨立 fenced block 呈現於 §A，未夾於敘述中。
自檢：§A 區塊數 = 8、§J 列數 = 8、§H 步驟 8 寫「八條」，三處一致。
本次已逐一數過 §A 之 fenced block。

## K. 分析層自判：本包是否仍有該驗而未驗者

**有，三項。**

1. **本包三條裁決皆為訂正分析層自身之錯誤**（R-P170 / R-P172 / R-P173），
   而三者形態相同：**未經查證即書寫斷言**。
   R-P64 已立於 08 包，至今於分析層身上失效四次。
   本包未提出任何機制改善之，僅逐次訂正。
2. **第三批 63 條之內容，分析層尚未覆核。**
   22、23 兩包皆只覆核上繳摘要。依 R-P159 為分層取樣，
   而取樣本身也還沒做。
3. **G113 之真陽性率 3.6% 若於第四批仍不改善，
   其裁決成本將超過收益** —— 31 leaf 之未覆蓋分支數可能數倍於 55。
   本包未預設該情形之處置。
