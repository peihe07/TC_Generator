# 23 — OR 分支閘門、台帳補洞與第四批

下放包 | 分析層 → 執行層 | 往返 NN = 23

前置：22 包上繳已覆核，判定 **ACCEPT**。
G103 首次真實命中（`SWE-PM-010` 缺 `4941984`），R-P144 兌現。

**分析層三項錯誤，於本包逐一處置**：
（甲）22 §前言宣告 R-P148 ~ R-P152「維持有效」，
      而該五條從未抄入 RULINGS.md —— charter 載「未寫入 repo 之裁決等於沒發生」，
      分析層替未落檔之條文宣告效力。
（乙）第三批範圍撞上 DR-PW5 / DR-PW6，
      而 R-P124(d) 當初擇 Timeout Settings 之逐字理由正是避開該二 DR。
（丙）G109 之可行性判斷有誤 —— 見 R-P163。

沿用不變之節：`§C 抽取規格` 同 02 包；`§I 禁區` 同 22 包 §I，另增列見本包 §I。

## A. 本包裁決條文（逐字，抄入 RULINGS.md）

```
[R-P161] OR 分支涵蓋閘門（G113）—— 本包最高優先。
         22 §八第 1 項：「原文以 `OR` 並列而 TC 只取其一」已重複**七次**
         （16 包 `BODY OFF-TIMED`、17 包 `greater`、18 包 `Ignition Pre Off`、
          22 包四項），**七次全靠反向涵蓋事後抓到，至今無閘門可攔**。

         該形態有明確語法特徵，且為本專案唯一
         「反覆出現、可機械化、卻無人做」之缺陷類。

         G113 定義：
         （a）自各 leaf 之 `source_clause` 抽出含
              `OR` / `or` / `either … or` / `nor` 之並列結構，
              以其為**分支對**（不限二元，`A OR B OR C` 拆為三支）
         （b）逐支比對該 leaf 之全部 TC，判其是否有 TC 覆蓋
         （c）任一支無 TC 覆蓋 → 列入輸出，**不判 FAIL**，
              入 R-P76 之待人工裁決類，逐支裁決三選一：
              `真缺口`（補 TC）／`規格未定義該支`（登記不補）／
              `已由他 leaf 涵蓋`（註明 leaf 與 tc_id）
         （d）**驗證條件**：以七次已知實例為對照，
              對各該批之**修補前**資料重跑，須能重現全部七項。
              **不得為求重現而調整抽取規則**（17 §I 之教訓）；
              未能重現者須說明漏檢形態

         `OR` 之大小寫、標點黏連（`minutesOR`）須一併處理，
         其正規化限於分隔符層，不得擴及語義。

         **G113 為第四批之前置條件** —— 未就位不得啟動第四批。
         裁決者 Pei，逐字依據：「一次下放」（回應 22 §八第 1 項）。
```

```
[R-P162] layer3 靜默丟棄之全量掃描（G103 擴及 115 leaf）。
         22 §八第 2 項：layer3 之「靜默丟棄」為普遍機制，
         而 G103 迄今只驗了已產出 TC 之 33 leaf，**其餘 81 leaf 未測**。
         `SWE-PM-010` 之 `4941984` 證明該機制會實際丟東西。

         處置：對**全部 115 leaf** 執行 G103，回報：
         （a）不相等之 leaf 清單與其差集
         （b）每個被丟棄之 item id 於兩份 CFTS 文字層之存在情形
         （c）與 A-PW02（`4942087`）、DR-PW3、DR-PW11（`4941984`）之關聯

         此為一次全量掃描即可得之數字，執行層已指出而未做
         （逾越 22 包範圍，處置正確）。
         **凡不相等者一律開 DR 或併入既有 DR，不得自行補齊 layer3。**
         裁決者 Pei，逐字依據：「一次下放」（回應 22 §八第 2 項）。
```

```
[R-P163] G109 之不可行判斷追認，並訂正分析層之立條依據。
         22 §B4：G109 評估為不可行，理由為
         「越界的不是標的而是規則」——
         `MaxCallTimeout` 確實出現於 `038` 之 `source_clause` 內，
         越界者為其啟動條件之**規則**（出自 `4941718`，屬 `SWE-PM-064`），
         token 層無法辨識。該判斷正確，追認。

         分析層於 R-P157 末段寫「此形態應可機械檢查：
         ER 之具名標的若不出現於本 leaf 之 `source_clause`，即為候選」——
         **該判準即現行 G82，且 G82 實測為 0**；
         分析層提出一個已存在且已證無效之判準，且未查證即書寫「應可」。
         此為 R-P64 所拘束之推測性修飾語，未標推測。

         G109 不實作。ER 依據越界之形態，往後**僅能靠取樣與人讀**，
         登記為已知限制。
         裁決者 Pei，逐字依據：「一次下放」（回應 22 §B4）。
```

```
[R-P164] 21 包之台帳缺口處置：補執行，不代抄、不重編號。
         `handoff/21_need_rework.md` 存在而 upstream 不存在；
         R-P148 ~ R-P152 從未抄入 RULINGS.md。
         執行層拒絕代抄並記明缺口，處置正確。

         處置：**補執行 21 包之 §H 步驟 3–9**，
         其五條裁決依原編號 R-P148 ~ R-P152 抄入，不重編號。
         其中：
         - R-P152（第三批啟動條件）已由 22 包實質達成（分析層完成第二批覆核），
           抄入時於其下加註「已由 22 包達成」，**原文不改**（R-P36）
         - R-P149 / R-P150 拘束執行層之作業，22 包已遵行，追認
         - DR-PW9 之編號已由 22 包保留，維持

         RULINGS.md 之缺口記與 INDEX 之未執行列，於補執行後更新為已補。
         裁決者 Pei，逐字依據：「一次下放」（回應 22 §一）。
```

```
[R-P165] 批次啟動前須查 live DR 之影響面。
         22 §二：第三批範圍 `SWE-PM-001`–`009` 正為 DR-PW6 之影響面，
         其阻斷欄逐字載「§1.6.2.1 之 9 個 leaf 其 TC 之
         `specification_reference` 無可引之規格文字」；
         `SWE-PM-003` 另受 DR-PW5（High）影響。
         **而 R-P124(d) 擇 Timeout Settings 之逐字理由正是避開該二 DR** ——
         分析層三包之後自撞，且下放包隻字未提。

         規則：任何批次之範圍界定前，須先查全部 live DR 之影響面，
         逐 leaf 比對；撞上者排除並於下放包**明載其排除理由與 DR 編號**。
         未查而撞上者，執行層應如 22 包一般停並回報，不得逕行撰寫。
         裁決者 Pei，逐字依據：「一次下放」（回應 22 §二）。
```

```
[R-P166] 透鏡 1 存續：保留計算，降低裁決負擔。
         22 §八第 3 項：透鏡 1 連續兩批零命中；
         四項真缺口全來自透鏡 3 之殘差詞；
         其 14 項「無對應」全裁為已涵蓋或範圍外。

         惟首批（17 包）之透鏡 1 曾捕獲「通話轉移」與
         「voltage out of range」二項，非全無價值。

         處置：
         （a）透鏡 1 **保留計算**，其輸出仍列於報告
         （b）其「無對應」項**不再要求逐項裁決**，改以三桶計數呈現
              （比照 R-P127 對殘差詞之處置）
         （c）透鏡 2 / 透鏡 3 之輸出**仍須逐項裁決**，不變
         （d）若透鏡 1 再連續兩批零命中（即累計四批），提請裁撤
         裁決者 Pei，逐字依據：「一次下放」（回應 22 §八第 3 項）。
```

```
[R-P167] `SWE-PM-025` 之八條拆法送分析層覆核。
         22 §八第 4 項：該 leaf 之兩組錨點文字幾乎逐字相同、
         只差觸發訊號，執行層依 §5.7 各拆四條，共八條，無第二人覆核；
         若二者實為同一行為之兩個入口，該 leaf 即多出四條。

         本包上繳須附：二組錨點之**逐字原文**與其**全部屬性**
         （比照 R-P143 之作法，含 `ECU` / `Model Year` / `Radio` /
          `State` / `EE Architecture` / `Market`），
         及八條 TC 之 `tc_title` / `split_reason` 對照表。
         **不得自行合併或拆分**，裁定於 24 包。
         裁決者 Pei，逐字依據：「一次下放」（回應 22 §八第 4 項）。
```

```
[R-P168] 第四批範圍：Power State 之**未受 live DR 影響**者。
         第三批實產 22 / 32（`SWE-PM-001`–`009` 因 DR-PW6 停、
         `SWE-PM-010` 因 G103 停）。

         第四批依 R-P165 先查 live DR 影響面，取 Power State 剩餘
         且不受 live DR 影響之 leaf；受影響者逐一列出並排除。
         **`SWE-PM-001`–`010` 一律不納入**，待 DR-PW5 / DR-PW6 / DR-PW11 解。

         **G113（R-P161）須先就位**方得啟動第四批。
         tc_id 沿用臨時編號，接續第三批之末。
         裁決者 Pei，逐字依據：「一次下放」。
```

## B. 本包須產出

### B1. G113 OR 分支閘門（R-P161）—— **最高優先，第四批前置**

`scripts/or_branch_coverage.py`；依 R-P161(a)–(d) 實作。
**驗證條件為七項已知實例之重現** —— 對各該批修補前資料重跑，
逐項回報「重現／未重現」，未重現者說明漏檢形態。
**不得為求重現而調整抽取規則。**

### B2. G103 全量掃描（R-P162）

115 leaf 全數；輸出 (a)(b)(c) 三項；不相等者開 DR 或併入既有 DR。
**不得自行補齊 layer3。**

### B3. 21 包補執行（R-P164）

執行 21 包 §H 步驟 3–9；R-P148 ~ R-P152 依原編號抄入；
R-P152 加註「已由 22 包達成」，原文不改；
更新 RULINGS 缺口記與 INDEX 未執行列。

### B4. `SWE-PM-025` 之裁定素材（R-P167）

二組錨點逐字原文 ＋ 全部屬性 ＋ 八條之 `tc_title` / `split_reason` 對照。
**不得自行合併或拆分。**

### B5. 第四批（R-P168）

**G113 就位後**方得啟動。先依 R-P165 查 live DR 影響面並明載排除清單。
上繳內容依 R-P158 所列四項（全 leaf 之 `source_clause` ＋ `reasoning`、
反向涵蓋全文、`§D` 全表、分層取樣 TC 全文含全部 P0）。

## D. 閃點

G0 為前置閘。G0–G112 沿用（G17 已移除；G109 依 R-P163 不實作），期望值不變。

| # | 項目 | 期望值 |
|---|---|---|
| G113 | OR 分支涵蓋（R-P161） | **七項已知實例全數重現**；未重現者說明形態；輸出入待裁類不判 FAIL |
| G114 | G103 全量（R-P162） | 115 leaf 掃畢；不相等清單與差集；每個被丟棄 id 之存在情形 |
| G115 | 21 包補執行（R-P164） | R-P148 ~ R-P152 已入 RULINGS，編號無重無缺；缺口記已更新 |
| G116 | 第四批產出（R-P168） | 【實測填入】leaf 數、TC 數、排除清單與其 DR 編號 |
| G70 | lint 全閘 | 全 PASS |

G113 之驗證條件同 G33 且加嚴：須以**七項已知實例**證明其確實會捕獲。

## E. framework

§E 已定版（R-P35），本包不動。

## F. Anomaly 異動

開新號前先查現行最大號並回報（R-P147）。

- 新增：分析層宣告未落檔之裁決「維持有效」，違 charter（R-P164）
- 新增：分析層批次規劃撞上 live DR，且該規避理由為其自身三包前所立（R-P165）
- 新增：分析層於 R-P157 提出一個已存在且已證無效之判準（G82），未查證即書「應可」（R-P163）
- 新增：`OR` 並列只取其一之形態重複七次而無閘門（R-P161）
- 新增：layer3 靜默丟棄之全量情形未測（R-P162）

## G. DATA_REQUESTS

DR-PW9（21 包保留）、DR-PW10（Medium）、DR-PW11（High）沿用。
R-P162 若掃出新的不相等 leaf，開新 DR 或併入既有（編號查後開，R-P147）。

## H. 作業指示

1. G0 前置閘
2. 查現行最大號並回報
3. 實作 B1 G113，驗七項重現 —— **最高優先**
4. 執行 B2 G103 全量，驗 G114
5. 執行 B3 21 包補執行，驗 G115
6. 產出 B4 `SWE-PM-025` 素材 —— **不得自行合併或拆分**
7. **G113 就位後**啟動 B5 第四批，驗 G116
8. 以 §D 全表自驗
9. §A 八條裁決逐字抄入 RULINGS.md；§F 入 ANOMALIES.md
10. 上繳 `features/power/docs/upstream/23_or_branch.md`，更新 `docs/INDEX.md`

## I. 禁區

沿用 22 包 §I 全部條目，另增列：

- **不得於 G113 就位前啟動第四批**（R-P161 / R-P168）
- **不得為求 G113 重現七項而調整抽取規則**（R-P161(d)）
- **不得自行補齊 layer3 之被丟棄 item**（R-P162）
- **不得代抄未執行包之裁決條文**（R-P164，追認 22 包之處置）
- **`SWE-PM-001`–`010` 一律不納入任何批次**，待相關 DR 解（R-P168）
- **不得自行合併或拆分 `SWE-PM-025` 之八條**（R-P167）

## J. 本包產生之新條文清單（自檢）

1. R-P161 OR 分支涵蓋閘門 G113，第四批前置
2. R-P162 layer3 靜默丟棄之全量掃描
3. R-P163 G109 不可行追認，並訂正分析層之立條依據
4. R-P164 21 包台帳缺口：補執行，不代抄不重編號
5. R-P165 批次啟動前須查 live DR 影響面
6. R-P166 透鏡 1 保留計算，降低裁決負擔
7. R-P167 `SWE-PM-025` 八條拆法送分析層覆核
8. R-P168 第四批範圍：Power State 未受 live DR 影響者

逐條確認：**八條**，皆以獨立 fenced block 呈現於 §A，未夾於敘述中。
自檢：§A 區塊數 = 8、§J 列數 = 8、§H 步驟 9 寫「八條」，三處一致。
本次已逐一數過 §A 之 fenced block。

## K. 分析層自判：本包是否仍有該驗而未驗者

**有，三項。**

1. **G113 是七次事後補救之後才立的。** 其立條依據來自執行層之觀察，
   非分析層自行發現 —— 分析層讀了七次同型缺陷而未歸納出形態。
   本條解決往後，未解決「為何七次都沒看出這是同一件事」。
2. **第三批 22 leaf / 61 條之分層取樣為 57/61，未取樣 4 條皆 P1。**
   依 R-P159 此為刻意取捨，但本包未覆核該 57 條之內容 ——
   分析層對第三批之覆核尚未開始，僅覆核了 22 包之上繳摘要。
3. **R-P167 之 `SWE-PM-025` 若裁為應合併，第三批之 TC 數將變動，
   而該 leaf 已在第三批內。** 屆時之處置（改寫或補記）本包未預設。
