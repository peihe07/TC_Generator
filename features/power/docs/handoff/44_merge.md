# 44 — 合併裁定、指令設計通則與寫回

下放包 | 分析層 → 執行層 | 往返 NN = 44

前置：43 包上繳已覆核，判定 **ACCEPT**。
訂正後全量 dry-run 十項全數 PASS 且與 42 包逐項相同；
「零個功能」之缺口經查量測**不成立**（4 條皆為分類未涵蓋，已補入）——
R-P289(a)「先查量測」之要求生效。

沿用不變之節：`§C 抽取規格` 同 02 包；`§I 禁區` 同 43 包 §I，另增列見本包 §I。

## A. 本包裁決條文（逐字，抄入 RULINGS.md）

```
[R-P293] 指令須令「逐條查」而非「處置」；自洽之缺陷唯此可撞見。
         43 §十第 1 項：`diff_lines` 之單向差集缺陷
         **無法由任何量測查出** —— 其輸出為空差集，
         **看似合法之結果而自洽**。
         執行層自陳：本次所以能發現，係因 R-P288 令「**逐條查**」
         而不得不實讀對照條；
         **若該條僅令「處置該 10 條」而未令逐條查，此缺陷至今不會被發現。**

         **通則**：凡下放包令執行層處理某一組已標定之項目時，
         其指令須令「**逐條查其標定是否屬實**」，
         而非僅令「處置該組」。
         二者之差別在於：後者接受該組之標定為前提，
         **前者使該標定本身成為被檢驗之對象**。

         其代價為工時；其收益為**能撞見自洽之缺陷** ——
         該類缺陷不會於任何報表上呈現，
         亦不會被任何閘門攔下（本專案已三度遇之：
         A-PW139 之空白變體、G113 之 `IF` 分隔符、本次之單向差集）。

         **本條回溯適用之範圍**：不回溯（R-P255）——
         惟往後之下放包一律適用。
         裁決者 Pei，逐字依據：「上繳」。
```

```
[R-P294] 四對 TC 合併為四條多 ER；`…-030`/`035` 等三組另查。
         43 §2.3：`…-100`/`102`、`…-104`/`107`、`…-175`/`176`、`…-005`/`006`
         四對之 `test_procedure` **第 1 步（施加）逐字相同**，
         僅觀察步驟與 ER 相異，執行層皆判 (b)。

         **裁定：合併。**
         依 §5.7「一個觸發、多個必然後果歸同一條 ——
         觸發為驗證單位，必然隨之而來之後果為待查之事實，非另一條 TC」。
         四對之施加步驟逐字相同，即其觸發相同；
         其觀察之差異為同一觸發之不同後果，應以多 ER 承接。

         處置：
         （a）四對各合為一條：保留第 1 步，
              二條之觀察步驟併為第 2、3 步，其 ER 對應併列
         （b）合併後 **264 → 260**；`tc_id` 依 R-P113(c) 於寫回時統一指派，
              本階段之臨時號重新連號
         （c）合併條之 `reasoning` 須載明其係由何二條合併及其依據（§5.7）
         （d）**43 §2.2 所補量之另三組**（`…-030`/`035`、`…-136`/`140`、
              `…-137`/`141`）已獲指派而未落入「無對應」——
              **逐對查其施加步驟是否亦逐字相同**；
              是者一併合併，否者說明其差異所在
         裁決者 Pei，逐字依據：「上繳」。
```

```
[R-P295] 合併與跨 leaf 重複之交集須查；涵蓋數之變動分別計算。
         43 §十第 3 項：合併影響涵蓋數（264 → 260），
         而 R-P263 之跨 leaf 重複 13 組 26 條若亦判為冗餘，其影響另計；
         **二者之交集未查** —— 即是否有某條同時屬「應合併」與「跨 leaf 重複」。

         處置：
         （a）查該交集並回報
         （b）**涵蓋數之變動分別計算**：
              合併所致者為「同一 leaf 內二條併為一條」——
              **leaf 涵蓋數不變，TC 數減少**；
              跨 leaf 重複若判冗餘則為「二 leaf 各有其 TC 而內容相同」——
              **依 R-P186 不合併，故 TC 數不變**
         （c）故最終條數之變動**僅由合併所致**；
              R-P263 之 13 組不改條數，其處置為 `tc_title` 之區別
              （R-P270 已裁其待 DR-PW12）
         裁決者 Pei，逐字依據：「上繳」。
```

```
[R-P296] `priority` 3 條回到 P0：追認其為訂正而非退步。
         43 §八：全閘重跑之唯一變動為 `rejudge_priority` 之受檢範圍
         180 → 183，即 3 條回到 P0。

         **追認。** 該 3 條為後視攝影機影像輸出
         （`…-110` / `…-138` / `…-139`），
         R-P240 已裁其 **P0 成立**，其依據為 §10.2 第一類 `safety`
         （倒車輔助為安全相關功能）。
         `rejudge_priority` 之新謂詞將其判為 P3，係該謂詞未涵蓋
         「影像輸出經 `safety` 歸類」之路徑 —— **謂詞之限度，非值之錯誤**。

         處置：於 `rejudge_priority` 之判準中補入 R-P240 之依據，
         使其不再將該 3 條判為 P3；並回報補入後之受檢範圍。
         裁決者 Pei，逐字依據：「上繳」。
```

```
[R-P297] 真實寫回之授權與其前置。
         42 / 43 包之全量 dry-run 十項皆 PASS 且結果一致；
         R-P291 之三項前置已完成（`priority` 3 條已改、
         `axis` 43 → 36 已處置、`design_method` 14 → 13 已入邊界）。

         **惟本包新增合併（R-P294），其後須再跑一次全量 dry-run。**

         **真實寫回之前置條件**：
         （a）R-P294 之合併完成，260 條
         （b）合併後之全量 dry-run 十項全數 PASS
         （c）分析層讀畢累計 21 條素材
              （42 包交錯 15 ＋ 43 包無對應 6）並出具複核結果
         （d）R-P295 之交集已查

         **四項齊備後，寫回於 45 包**。
         **本包不對客戶樹寫回。**

         併記寫回之不可逆性：客戶樹之 FW036 為交付標的，
         其寫入依 R-P228 須先備份至 `sandbox/`；
         **且依 charter，交付形式與送達執行屬 Pei，
         寫回之實際執行須 Pei 明示授權，不得以本條為據逕行為之。**
         裁決者 Pei，逐字依據：「上繳」。
```

## B. 本包須產出

### B1. 四對合併（R-P294）—— **最高優先**

四對各合為一條；`reasoning` 載明依據；臨時號重新連號；
另三組逐對查施加步驟是否亦逐字相同。

### B2. 合併與跨 leaf 重複之交集（R-P295）

查交集並回報；涵蓋數變動分別計算。

### B3. `rejudge_priority` 判準補入 R-P240 之依據（R-P296）

補入後回報受檢範圍。

### B4. 合併後之全量 dry-run（R-P297(b)）

260 條 ＋ 留白列；十項逐項回報並與 43 包對照。

### B5. 待讀素材彙整（R-P297(c)）

42 包交錯 15 條 ＋ 43 包無對應 6 條，**合併為一份置於上繳最前**，
供分析層一次讀畢。

## D. 閃點

G0 為前置閘。G0–G205 沿用（G17 已移除；G109 不實作），期望值不變。

| # | 項目 | 期望值 |
|---|---|---|
| G206 | 四對合併（R-P294） | 264 → 260；四條合併條各有多 ER 且 `reasoning` 載明依據；另三組已逐對查 |
| G207 | 合併與跨 leaf 之交集（R-P295） | 【實測填入】交集條數；涵蓋數變動已分別計算 |
| G208 | `rejudge_priority` 補判準（R-P296） | 3 條不再判 P3；【實測填入】補入後之受檢範圍 |
| G209 | 合併後全量 dry-run（R-P297(b)） | 260 ＋ 1 列；十項全數 PASS；與 43 包逐項對照 |
| G70 | lint 全閘 | 合併後仍全 PASS |

## E. framework

§E 已定版（R-P35），本包不動。
**真實寫回於 45 包**，其前置為 R-P297 之四項；
**且須 Pei 明示授權方得執行**。

## F. Anomaly 異動

開新號前先查現行最大號並回報（R-P147）。

- 新增：指令令「處置」而非「逐條查」時，自洽之缺陷不會被撞見（R-P293）
- 新增：四對 TC 之施測操作相同，應合併（R-P294）
- 新增：`rejudge_priority` 未涵蓋「影像輸出經 `safety` 歸類」之路徑（R-P296）

## G. DATA_REQUESTS

DR-PW9 ~ DR-PW15 沿用。無新增。

## H. 作業指示

1. G0 前置閘
2. 查現行最大號並回報
3. B5 待讀素材彙整 —— **置於上繳最前**
4. B1 四對合併，驗 G206 —— **最高優先**
5. B2 交集查證，驗 G207
6. B3 `rejudge_priority` 補判準，驗 G208
7. B4 合併後全量 dry-run，驗 G209
8. 以 §D 全表自驗
9. **抄錄裁決條文前重驗一次 §J 自檢**（R-P200(c)）；
   §A 五條逐字抄入 RULINGS.md；§F 入 ANOMALIES.md
10. 上繳 `features/power/docs/upstream/44_merge.md`，更新 `docs/INDEX.md`

## I. 禁區

沿用 43 包 §I 全部條目，另增列：

- **不得對客戶樹之 FW036 寫回**（R-P297）
- **不得合併跨 leaf 重複之任何 TC**（R-P295(b)、R-P186）
- **不得於 R-P297 四項齊備前執行寫回，亦不得無 Pei 明示授權而寫回**

## J. 本包產生之新條文清單（自檢）

1. R-P293 指令須令「逐條查」而非「處置」
2. R-P294 四對 TC 合併為四條多 ER；另三組另查
3. R-P295 合併與跨 leaf 重複之交集須查；涵蓋數變動分別計算
4. R-P296 `priority` 3 條回到 P0 為訂正；`rejudge_priority` 補判準
5. R-P297 真實寫回之授權與其四項前置

逐條確認：**五條**，皆以獨立 fenced block 呈現於 §A，未夾於敘述中。
自檢：§A 區塊數 = 5、§J 列數 = 5、§H 步驟 9 寫「五條」，三處一致。
本次已逐一數過 §A 之 fenced block。
**本包下放後分析層不再修改**（R-P200(a)）。

## K. 分析層自判：本包是否仍有該驗而未驗者

**有，三項。**

1. **累計 21 條素材，分析層迄未讀**（42 包 15 ＋ 43 包 6）。
   R-P297(c) 已將其列為寫回之前置 ——
   **分析層須於 45 包前讀畢並出具結果，此為不可再推遲之項**。
   自 38 包起分析層已四度推遲 `axis` 群之複核。
2. **R-P293 之通則不回溯**（依 R-P255），
   故**先前各包中以「處置」措詞下達者，其標定是否屬實均未經查** ——
   43 包所查出之單向差集缺陷即為一例，而該類缺陷數不可知。
3. **R-P294 之合併使 `tc_id` 重新連號**，
   而 `tc_id` 依 R-P113 於寫回時方最終指派 ——
   **本階段之重新連號不影響最終值，惟其與既有紀錄之對照關係將改變**；
   45 包之寫回須附「臨時號 → 最終號」之完整對照表，
   使既往各包所引之 `tc_id` 得以追溯。
