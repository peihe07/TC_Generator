# 08 — lint 閘門補齊與偽陽性量測

下放包 | 分析層 → 執行層 | 往返 NN = 08

前置：docs/upstream/07_phase4_gates.md 已覆核，判定 ACCEPT。
九步全完成、G32–G36 無 MISMATCH。

**Phase 4 現為阻斷狀態**，阻斷條件為 R-P60（lint 閘門不對稱）。
本包解除該阻斷。本包仍不撰寫任何 TC。

## A. 本包裁決條文（逐字，抄入 RULINGS.md）

```
[R-P60] **lint 閘門補齊為 Phase 4 之阻斷條件。**
        現況 59 條裁決中僅 R-P42 有執行期閘門（07 §7.2 第 5 項）。
        前七包所建立之全部價值在於「條文可被機械驗證」，
        而最新之條文有閘門、最早之條文僅靠紀律 —— 此不對稱恰為反向：
        早期條文被引用最多次、最易被遺忘。
        Phase 4 開始前須為下列可機械檢查之條文補設閘門：
          G37 R-P1  —— `SWE-PM-089` 之 TC 不得存在，
                       且其來源欄不得被任何值填補
          G38 R-P2  —— tc_id 須符 `NR1L-PowerManagement-\d{3}`，
                       且於同群內單調遞增、無重複、無跳號
          G39 R-P8  —— priority 須為 P0–P3 之一；
                       且不得與 037 `Priority` 欄呈現一對一映射
                       （若全數 High→Px、Medium→Py，即為違反）
          G40 R-P35 —— 每 TC 之 Test Set 須為五個定版值之一，
                       且逐 leaf 之歸屬須與 `data/leaf_testset.tsv` 相符；
                       全體分布須為 63 / 24 / 16 / 8 / 3
        四閘皆須以合成 fixture 驗證其**確實可能失敗**（同 G33 之要求）。
        裁決者 Pei，逐字依據：「是」（回應 07 Q3、Q6）。
```

```
[R-P61] 閘門偽陽性率須量測，且**先於任何門檻調整**。
        G33 僅驗「該觸發時會觸發」，未驗「不該觸發時不會觸發」；
        PASS fixture 僅一個且為執行層自撰之短句。
        1398 條特徵字串中若有任一條為通用語句，
        所有合法 TC 將被誤殺。
        量測方式：
        （a）**主要**——以合成之合法 Power TC 語料（不少於 20 條，
             涵蓋五個 Test Set，內容取自**被引用**錨點之改寫）
             量測誤觸發率。依既有原則，測試控制須用合成 fixture，
             不得以 repo 現況為對照。
        （b）**次要，非權威**——以其他 feature 之已交付 TC 語料試跑。
             該等 TC 屬不同規格，理應 0 命中；
             若有命中即證特徵字串過於通用。
             此為訊號非判準，須明確標示為非權威。
        裁決者 Pei，逐字依據：「是」（回應 07 Q2）。
```

```
[R-P62] `MIN_FINGERPRINT = 40` **維持不變**，81 個無特徵字串之
        黑名單錨點**登記為已知缺口，本包不調門檻**。
        理由：降低門檻直接推高偽陽性，而偽陽性率現為未知數；
        於未知數上調參數，係以一個未量測之風險換另一個。
        俟 R-P61 之量測有結果後另案再議。
        該 81 個仍受閘門 (a) 保護，且 §10.7 要求 `specification_reference`
        必填 —— 缺口實為「逐字抄錄內容」且「未列來源」二重違規之交集，
        非單一漏洞。
        裁決者 Pei，逐字依據：「是」（回應 07 Q1）。
```

```
[R-P63] 黑名單之 315 個「位於已觸及章節」者**分兩層**，不細分。
        第一層：與任一被引用錨點**同章且同一父節**者 —— 最高風險
        第二層：其餘位於已觸及章節者
        reviewer 所需者為「這批要特別小心」，非一份排序精美之長清單；
        過細之分層徒增維護成本而不增判斷力。
        裁決者 Pei，逐字依據：「是」（回應 07 Q4）。
```

```
[R-P64] **量化修飾語之標註義務為雙向通則**（取代 R-P58 之單向範圍）。
        R-P58 原文僅拘束分析層，係自「四次皆為分析層所犯」之樣本歸納，
        非自原理推導。A-PW32（執行層將懸空參照誤稱為「RTF 資源」，
        實測試算表 16 處為多數、RTF 僅 14 處）證明同型錯誤兩層皆會犯。
        通則：任一方於下放包、上繳包、裁決條文、anomaly 描述中
        書寫量化或排他性修飾語（「最大」「唯一」「多數」「全部」
        「僅有」「最」等），須標明其依據為**實測**或**推測**；
        無實測依據者一律寫「推測」。
        依 R-P36，R-P58 原文不改，於其下加註指向本條。
        裁決者 Pei，逐字依據：「是」（回應 07 Q5）。
```

```
[R-P65] Phase 4 之開始條件明定為：
        （a）R-P60 之四閘（G37–G40）皆就位且通過 fixture 測試
        （b）R-P61 之偽陽性量測完成且結果已登記
        （c）本包上繳並經分析層覆核 ACCEPT
        三者全備方得開始。R-P62 之 81 個缺口與 R-P63 之分層
        **不構成阻斷**。
        裁決者 Pei，逐字依據：「是」（回應 07 Q6）。
```

（以上**六條**裁決條文，抄入 RULINGS.md 時逐字保留，
 每條獨立區塊，不得夾於敘述中。）

## B. 本包須產出

### B1. 四道新閘門與其 fixture（R-P60）

於 `scripts/lint_tcs.py` 增設四閘，每閘須有：
  正常案例（應 PASS）
  違規案例（應 FAIL，且**實際觸發**）

各閘之違規 fixture 至少涵蓋：
  G37 —— 一條 tc_id 指向 `SWE-PM-089` 之 TC
  G38 —— 格式錯誤一例、重複一例、跳號一例
  G39 —— priority 為 `High` 一例；全體呈 037 一對一映射一例
  G40 —— Test Set 非五個定版值一例；
          某 leaf 之歸屬與 `leaf_testset.tsv` 不符一例；
          分布非 63/24/16/8/3 一例

**fixture 一律合成，不得讀 `generated/`。**
執行 `python features/power/scripts/lint_tcs.py --self-test` 須全數通過。

### B2. 偽陽性量測（R-P61）

  合成 20 條以上合法 Power TC（涵蓋五個 Test Set），
    內容取自**被引用**錨點之改寫，非逐字抄錄
  以現行 1398 條特徵字串試跑，回報誤觸發數與逐條明細
  另跑其他 feature 已交付 TC 語料，標明為**非權威訊號**
  若誤觸發 > 0，逐條列出該特徵字串並判斷其是否為通用語句

輸出至 `data/b2_false_positive.md`。

### B3. 黑名單分層（R-P63）

  於 `data/unreferenced_anchors.tsv` 增欄 `risk_tier`：
    `1` = 與任一被引用錨點同章且同一父節
    `2` = 其餘位於已觸及章節者
    `3` = 所屬章節未被觸及者（即原 499）
  回報三層之計數

### B4. R-P58 之加註（R-P64）

  依 R-P36，R-P58 原文不改，於其下加註：
    「註記（R-P64，08 包）：本條原文僅拘束分析層，
     係自『四次皆為分析層所犯』之樣本歸納，非自原理推導。
     A-PW32 證明同型錯誤兩層皆會犯。
     標註義務已由 R-P64 擴為雙向通則。原文保留。」
  加註後須以雜湊佐證原文位元組未變

## C. 抽取規格

  §C rule 1 / 2 / 3 / 4 正則不變。
  R-P17 文字層定義不變。
  `MIN_FINGERPRINT = 40` 不變（R-P62）。

## D. 閃點

G0 為前置閘。G0–G16、G13b、G18–G36 沿用（G17 已移除），期望值不變。

| # | 項目 | 期望值 |
|---|---|---|
| **G37** | R-P1 閘門（`SWE-PM-089` 不得有 TC） | fixture 正常 PASS、違規實際 FAIL |
| **G38** | R-P2 閘門（tc_id 格式與單調遞增） | 同上，三類違規皆實際觸發 |
| **G39** | R-P8 閘門（priority 值域與非映射） | 同上，兩類違規皆實際觸發 |
| **G40** | R-P35 閘門（Test Set 定版符合） | 同上，三類違規皆實際觸發 |
| **G41** | 偽陽性率（R-P61(a)） | 【實測填入】合成合法語料之誤觸發數；期望 0 |
| **G42** | 跨 feature 訊號（R-P61(b)） | 【實測填入】非權威；理應 0 命中 |
| **G43** | 黑名單分層計數（R-P63） | 【實測填入】tier 1 / 2 / 3；三者之和須 = 814 |
| **G44** | R-P58 加註後原文位元組未變 | UNCHANGED |

G37–G40 之驗證條件同 G33：**須確認其在該階段確實可能失敗**；
不可能失敗者標「未實測」而非 PASS。

## E. framework

§E 已定版（R-P35），本包不動。

## F. Anomaly 異動

  A-PW27 → 依 R-P52(c) 已改為腳本導出；本包依 R-P63 增 `risk_tier` 欄
  A-PW30 → 依 R-P64 標註為雙向通則之首例（分析層側）
  A-PW32 → 依 R-P64 標註為雙向通則之首例（執行層側）
  新增 A-PW33：81 個黑名單錨點無特徵字串，閘門 (b) 無法涵蓋
               （R-P62，已知缺口，不阻斷）
  新增 A-PW34：`點號分隔識別式` 之 308 個 token 含大量偽陽性
               （前四名 `notification.` `path.` `actions.` `displayed.`
                皆為句末單字加句點）；G28 基線未受影響，
                但該識別式之實際貢獻被高估（07 §7.2 第 3 項）
  新增 A-PW35：G33 之 fixture 於首次執行抓到真 bug
               （`\b\d{6,8}\b` 於底線前不成立，而
                `specification_reference` 慣以底線串接，
                致閘門 (a) 完全不觸發）；
               為 canon「檢查項須確認其確實可能失敗」之首次實質捕獲

## G. DATA_REQUESTS

  DR-PW1 → live，High
  DR-PW3 → live，Medium
  DR-PW5 → live，High
  DR-PW6 → live，Medium（描述已依 A-PW32 訂正）
  DR-PW7 → live，Low
  DR-PW2、DR-PW4 → 維持撤回
  無新增

## H. 作業指示

  1. G0 前置閘
  2. 依 R-P60 實作四閘並建 fixture，驗 G37–G40
  3. 依 R-P61 產出 B2 偽陽性量測，驗 G41 / G42
  4. 依 R-P63 為黑名單增 `risk_tier` 欄，驗 G43
  5. 依 R-P64 為 R-P58 加註，驗 G44
  6. 以 §D 全表自驗
  7. §A 六條裁決逐字抄入 RULINGS.md；§F 入 ANOMALIES.md
  8. 上繳 features/power/docs/upstream/08_lint_parity.md，更新 docs/INDEX.md
  9. **回報 R-P65 之三項開始條件是否全備**，逐項列出依據

## I. 禁區

  不得寫回 FW036 workbook
  不得執行任何 git 操作（全數屬 Pei）
  不得以 openpyxl save 寫任何 xlsx（R16 凍結）
  不得補齊 SWE-PM-089（R-P1）
  不得沿用純文字衍生物之任何數字（R-P10）
  不得自行調整 §C 正則
  不得修改任何已落檔裁決條文之內文（R-P36）
  不得測試未被引用之錨點（R-P42）
  不得解析任何 RTF 或 OLE stream 之內容（R-P39、R-P48）
  不得續行章節層反向缺口調查（R-P37）
  不得變更 §E 之分布數字（R-P35）
  不得以 A-PW29 之存在逕行填寫車型欄（R-P54）
  **不得調整 `MIN_FINGERPRINT`（R-P62）**
  **不得以 repo 現況作為任何 fixture 之測試對照**
  **本包不撰寫任何 TC**
  素材補入超出 features/power/inputs/ 需 Pei 裁定

## J. 本包產生之新條文清單（自檢）

  R-P60 lint 閘門補齊（G37–G40）為 Phase 4 阻斷條件
  R-P61 偽陽性率須量測，先於任何門檻調整
  R-P62 `MIN_FINGERPRINT = 40` 維持，81 個登記為已知缺口
  R-P63 黑名單分兩層（tier 1 / 2），不細分
  R-P64 量化修飾語標註義務為雙向通則，取代 R-P58 之單向範圍
  R-P65 Phase 4 開始條件明定為三項

  逐條確認：**六條**，皆以獨立區塊呈現於 §A，未夾於敘述中。
  自檢：§A 區塊數 = 6、§J 列數 = 6、§H 步驟 7 寫「六條」，三處一致。
