# 64 — 63 回報覆核；R-P377 限縮；G252 之六名交分析層人讀

下放包 | 分析層 → 執行層 | 往返 NN = 64

前置：`data/63_report.md` 已覆核，判定 **ACCEPT**。G255 3/3 首次運作即過；
丁案二條入 corpus 驗證完整；R-P377×R-P375(c) 衝突之指出正確；
「本輪仍非人讀」之揭露正確。寫回移至 65 包，續受 S6 阻斷。

## 0. 分析層之誤（自陳）

R-P377(a) 撤 PENDING 未區分靜態組態與運行時用法，直接違 R-P375(c)。
63 包 §J 對既有 canon 之檢查寫「R-P377 對 R-P368(b) 合」，**漏查 R-P375(c)** ——
R-P364(d) 立於 57 包，本包即為其首次失效實例：分析層列了引用條，未列觸及條。登 A-PW。

## A. 本包裁決條文（逐字，抄入 RULINGS.md）

```
[R-P380] R-P377(a) 限於靜態組態用法（甲）；運行時用法維持 PENDING。
         （a）強、中候選之撤除僅及 Pre-Condition 之靜態組態用法
              （`-062` / `-076` / `-077`）；Procedure／ER 之運行時讀取或轉移
              （`-017` / `-018` / `-019` / `-070` / `-081` / `-107`）維持 `PENDING: DR-PW23`
         （b）`SwitchOffSetting.Req` 二條皆運行時，一條不撤；其候選記入附表供上游確認
         （c）PENDING 數以 `pending_recount_63.tsv` 甲情形為準（依該檔計數為 99）
         （d）運行時用法之解消途徑：上游回覆該 `.Req` 之 CAN／UI 觀察面，
              或 HMI Settings List 補查得對應設定名（白名單 (ii)）；後者本輪已查無，記明
         R-P377 依 R-P36 原文不改，加註。
         裁決者：分析層（Tier 2，訂正自身條文以合 R-P375(c)）。
```

```
[R-P381] G252 之六個「查無」由分析層人讀；執行層供料，不再機掃。
         （a）執行層對六名各出一頁：所屬 tc_id、`test_item` 上半 verbatim、
              其錨點 ObjectID 之段落全文、TC 之 Procedure／ER 現行文；
              落 `data/g252_six_63.md`，**不判定、不查詢**
         （b）分析層逐名人讀，於 65 包裁：有錨（指明段落與代理量）／
              查無（分析層自負 R-G13 三要件，開 DR、登 M-n）／
              TC 措辭問題（`after each one`、`again after Timeout1` 類非觀察目標名，
              屬 Procedure 改寫，回 R-P353 處理）
         （c）G252 期望值改為：「有錨者入代理量表；六名之處置見 65 包；無第四類」
         裁決者：分析層（Tier 2）。
```

```
[R-P382] 39 個有錨名即可填代理量表；R-P367 之覆核於本包完成。
         `proxy_reachability_63.md` 之 39 名，其查詢詞落在規格用語且錨點在 G0 台帳，
         覆核通過。執行層填 `data/observable_proxy_64.md`
         （`<原 X> | <代理量（白名單類別）> | <錨點> | <影響列>`），
         代理量須為白名單四類之一；填不出白名單類者不得硬填，回報。
         裁決者：分析層（Tier 2）。
```

## H. 作業指示

1. 抄 R-P380–R-P382（依 R-P379 先重算：3 條、6 條、2 條、99、39）；R-P377 加註；§0 登 A-PW
2. PENDING 依 R-P380 回退六條 → `pending_recount_64.tsv`
3. `g252_six_63.md` 供料 → **停**
4. `observable_proxy_64.md` → **停，待覆核**
5. 上繳 `features/power/docs/upstream/64_rulings.md`，附 G255 表

## I. 禁區

沿用 63 包 §I，另增列：運行時用法不得撤 PENDING（R-P380(a)）；六名不得再機掃（R-P381(a)）；代理量不得非白名單類（R-P382）。

## J. 自檢

三條。C(3,2)：R-P380×R-P382 — 代理量表不涉內部訊號，無交集；餘無交集。
對既有 canon —— **引用條與觸及條並列**（R-P364(d)）：
R-P380 對 R-P375(c) — 合（即為其落實）；對 R-P377 — 限縮，加註；對 R-P353(ii) — (d) 引用，合。
R-P381 對 R-G13 — 分析層自負三要件，合；對 R-P372(a) — 「人讀」改由分析層，加註；對 A-PW355 教訓 — 合。
R-P382 對 R-P367 — 為其覆核，合；對 R-P353 白名單 — 合。無違反。

本包數字（R-P379(a)）：3／6（`63_report.md` §4 表）；99（`pending_recount_63.tsv` 甲）；39／6（`proxy_reachability_63.md`）。

## K. 待 Pei

無阻斷項。B5 續凍。
