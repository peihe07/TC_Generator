# 21 — Need rework 追查、git 禁區釐清與第二批覆核

下放包 | 分析層 → 執行層 | 往返 NN = 21

前置：`docs/upstream/20_falsifiability.md` 已覆核，判定 **ACCEPT**。
G103 11/11、G104 阻斷類 PASS、G105 第二桶全列、G106 並存值已附。
執行層兩次自我訂正（R-P143 之 State 對數、R-P146 之黏連影響）皆正確。

沿用不變之節：`§C 抽取規格` 同 02 包；`§I 禁區` 同 20 包 §I，另增列見本包 §I。

## A. 本包裁決條文（逐字，抄入 RULINGS.md）

```
[R-P148] SYS2 之 `Need rework` 四列開 DR-PW9（High）。
         20 §2.1 實測：SYS2 CFTS009 匯出之 `HARMAN Status` 欄
         有 `Accepted` 168、**`Need rework` 4**，
         而該匯出檔名為 `All_Accepted` —— **檔名與內容不符**。

         037 引用其中一列，對應 `SWE-PM-112`；
         該 leaf 不在已產出之 11 leaf 內，但在 115 之母體內。

         問題有二，須一併提出：
         （a）037 引用了上游標為需重工之需求，本專案至今無任何機制
              會注意到此事（20 §八(甲)3）
         （b）匯出檔名之 `All_Accepted` 不字面為真，
              而 R-P7 之範圍裁定係以該匯出為母體 ——
              裁定當時不知其含非 Accepted 之列

         DR-PW9 內容：請 RD 說明 `Need rework` 四列之現況，
         及 037 引用其一是否為刻意。
         Urgency **High** —— 其影響 `SWE-PM-112` 之 TC 可否撰寫。

         **本條不重開 R-P7 之範圍裁定**；R-P7 依 R-P36 原文不改，
         於其下加註指向本條，記明其母體之已知瑕疵。
         裁決者 Pei，逐字依據：「上繳」。
```

```
[R-P149] 自造損壞不得以 git 修復（禁區釐清）。
         20 §八(丙)6：執行層以 `git checkout` 將 `lint_tcs.py`
         還原至 HEAD，修復自身過寬切片所致之損壞。
         **揭露正確，動作仍越線** —— charter 載「全部 git 操作屬 Pei，
         兩層皆只準備不執行」，未設「修復自造損壞」之例外。

         風險為實：`git checkout <file>` 會連同該檔案內
         **其他未提交之變更一併丟棄**。本次適無，不代表次次無。

         往後自造損壞之處置，二擇一：
         （a）以非 git 方式重建該檔（自記憶或自備份），或
         （b）**停並回報**，由 Pei 決定
         **不得執行任何 git 子命令**，含 `checkout` / `restore` /
         `stash` / `clean`。

         本次已發生者不予追究 —— 其揭露完整且範圍受限；
         登記為 anomaly 以立先例。
         裁決者 Pei，逐字依據：「上繳」。
```

```
[R-P150] 過寬切片之編輯規則擴充。
         20 §八(丙)6：寬切片第三次（05 包 §E 偏移、11 包誤刪二常數、本包誤刪四函式）。
         執行層指出：前二次之既有規則為「每次編輯獨立進行、不共用位移」，
         **而本次係切片邊界選錯，非位移問題** —— 舊規則未涵蓋此形態。

         擴充規則：任何以字串切片或範圍替換方式修改程式碼之編輯，
         替換後須立即以**語法或載入層檢查**驗證該檔完整性
         （`python -m py_compile` 或 import 檢查），
         不得俟後續執行時方由 `NameError` 之類攔下。
         該檢查須於**同一步**內完成，不得延至該包末之整體自驗。

         本次由 `NameError` 當場攔下屬偶然 ——
         若被刪者為未於本包執行路徑上之函式，將靜默存活至日後。
         裁決者 Pei，逐字依據：「上繳」。
```

```
[R-P151] G103 之 unresolved 併入判定追認，並與 A-PW02 / DR-PW3 建立關聯。
         20 §三：fixture 第四案之期望值原寫「多一個不存在之 token → FAIL」，
         實測為「相等」——不可解析之 token 不產生任何 item，
         致 **037 若引用 SYS2 未載之 token，該錨點形同消失而三閘全綠**。
         執行層已將 unresolved 併入判定，追認。

         此非假想：**A-PW02 即實例** ——
         `Sys-RA-PM-0334` 之 `4942087` 於兩份 CFTS 本文皆不存在，
         即 DR-PW3 所問者。
         A-PW02 / DR-PW3 / 本條須互相標註，使日後查閱者見其為同一問題。

         併記：「fixture 期望值寫錯反而查出真問題」為第二例
         （首例為 18 包 G95 選錯分頁）。二例皆因**去查為什麼**
         而非照著輸出下結論。
         裁決者 Pei，逐字依據：「上繳」。
```

```
[R-P152] 第三批之啟動條件：分析層完成第二批 26 條之技術覆核。
         20 §八(甲)1：43 項可證偽性裁決之裁決者即撰寫者 ——
         「沉默不算裁決」防得了漏裁，防不了自我背書。
         此問題**不可由執行層自行解決**，其解方為分析層之獨立覆核。

         分析層現況：已讀 `leaves` 陣列（8 leaf）與 `018` 一條全文，
         **其餘 25 條未讀**（19 §K 第 1 項、20 §K 第 1 項，兩包未解）。

         第三批不啟動，直至分析層完成 26 條之覆核並出具發現。
         本包上繳無須重附 26 條全文（19 包已附，分析層自行取讀）。
         裁決者 Pei，逐字依據：「上繳」。
```

## B. 本包須產出

### B1. DR-PW9（R-P148）

寫入 `DATA_REQUESTS.md`，Urgency High，含 (a)(b) 兩項問題。
為 R-P7 加註（依 R-P36 原文不改），雜湊佐證原文未變。
回報 `SWE-PM-112` 所引之該列 NRL 與其 `HARMAN Status` 逐字值。

### B2. R-P150 之編輯規則落實

於執行層自身之作業規則中加入語法/載入檢查步驟；
回報其形式（腳本、checklist 或其他）與其如何確保「同一步內完成」。

### B3. A-PW02 / DR-PW3 / R-P151 之互相標註

三處各加交叉指引，使任一處被查閱時可見其餘二處。

### B4. 現行狀態快照

供分析層覆核第二批時對照：
- `batch_002_timeout_settings.json` 之 SHA256 與 bytes
- 26 條之 `tc_id` / `req_id` / `split_index` 對照表（單頁）
- 待人工裁決類 44 項之逐條清單（`tc_id` ＋ 規則 ＋ 裁決）

## D. 閃點

G0 為前置閘。G0–G106 沿用（G17 已移除），期望值不變。

| # | 項目 | 期望值 |
|---|---|---|
| G107 | R-P7 加註後原文位元組未變（B1） | UNCHANGED |
| G108 | R-P150 之語法檢查步驟（B2） | 已納入作業規則；以刻意刪函式之案例證明其會攔下 |
| G70 | lint 全閘 | 全 PASS；leaf 11；TC 43 |

G108 之驗證條件同 G33：須確認其在該階段確實可能失敗。

## E. framework

§E 已定版（R-P35），本包不動。
**第三批不啟動**（R-P152）。

## F. Anomaly 異動

開新號前先查現行最大號並回報（R-P147）。

- 新增：SYS2 匯出檔名 `All_Accepted` 不字面為真，含 4 列 `Need rework`；
  037 引用其一（`SWE-PM-112`）；R-P7 之範圍裁定當時不知此事（R-P148）
- 新增：執行層以 `git checkout` 修復自造損壞，越禁區；揭露完整，立先例（R-P149）
- 新增：過寬切片第三次，形態為切片邊界選錯，舊規則未涵蓋（R-P150）
- A-PW02 → 依 R-P151 加交叉指引至 DR-PW3 與 G103

## H. 作業指示

1. G0 前置閘
2. 查現行最大號並回報
3. 開 DR-PW9 並為 R-P7 加註（B1），驗 G107
4. 落實 B2 編輯規則，驗 G108
5. 產出 B3 交叉標註
6. 產出 B4 狀態快照
7. 以 §D 全表自驗
8. §A 五條裁決逐字抄入 RULINGS.md；§F 入 ANOMALIES.md；§G 入 DATA_REQUESTS.md
9. 上繳 `features/power/docs/upstream/21_need_rework.md`，更新 `docs/INDEX.md`

## I. 禁區

沿用 20 包 §I 全部條目，另增列：

- **不得執行任何 git 子命令，含修復自造損壞之情形**（R-P149）
- **不得重開 R-P7 之範圍裁定**（R-P148）
- **不得啟動第三批**（R-P152）
- **不得以本包之新資訊自行調整已產出之 43 條 TC**

## J. 本包產生之新條文清單（自檢）

1. R-P148 `Need rework` 四列開 DR-PW9（High）
2. R-P149 自造損壞不得以 git 修復
3. R-P150 過寬切片之編輯規則擴充
4. R-P151 G103 之 unresolved 追認並與 A-PW02 / DR-PW3 關聯
5. R-P152 第三批之啟動條件為分析層完成第二批覆核

逐條確認：**五條**，皆以獨立 fenced block 呈現於 §A，未夾於敘述中。
自檢：§A 區塊數 = 5、§J 列數 = 5、§H 步驟 8 寫「五條」，三處一致。
本次已逐一數過 §A 之 fenced block。

## K. 分析層自判：本包是否仍有該驗而未驗者

**有，三項。**

1. **第二批 26 條之技術覆核，責任在分析層而非本包。**
   R-P152 把它訂為第三批之啟動條件，但**本包沒有推進它**。
   已連續三包（19 / 20 / 21）掛著未解。
2. **R-P143 之合併裁定仍暫緩。**
   資料已齊（六個錨點之七欄原值 ＋ `State` ＋ `All_Accepted` 之查證），
   而分析層尚未裁。此為分析層之積欠，非執行層之。
3. **B2 之屬性原字串至今無人覆核。**
   20 §八(甲)4 已指出：附上原字串使人「能」覆核，非「已」覆核；
   而分析層讀不到 CFTS 本文，抽取者即執行層本人。
   本包未解此結構性限制。
