# 13 — ER 品質修正與首批覆核完成

下放包 | 分析層 → 執行層 | 往返 NN = 13

前置：docs/upstream/12_pilot_fixes.md 已覆核，判定 **ACCEPT**。
R-P92 已取得結論 —— `Test Case Framework` 分頁非空儲存格 0，不構成權威來源，
§E 未動。

**惟 R-P87 之修正引入新缺陷。** 分析層讀畢 001–005 修正後全文，
發現為湊 1:1 而將 procedure 動作複述為 ER，已讀五條全數命中。
**根因為分析層之條文不完整** —— R-P87 只寫「步數 = 行數」，
未寫「補出之 ER 行仍須為可觀察結果」。

**寫回仍不開放**，阻斷條件為 R-P96 與 R-P98。

## A. 本包裁決條文（逐字，抄入 RULINGS.md）

[R-P96] **ER 之每一行須為可觀察且可判讀之結果，
        不得為 procedure 動作之複述（G73）。**
        R-P87 僅規定「procedure 步數 = ER 行數」，
        未規定補出之行須具判讀價值，致修正引入新缺陷：

          `001` ER2「The elapsed time is recorded from boot start」
          `002` ER1「The boot target status is Standby」
          `003` ER1（同 002）
          `004` ER2（同 001）
          `005` ER1「The TLM boot sequence starts」

        該等行**永遠為真** —— 只要測試員執行了該步驟即成立，
        不構成任何判準，違反 §6「observable, judgeable」。

        §6 明訂 setup / transition 之 ER 係為
        「prove condition established」—— 證明條件已建立，
        **非複述執行者做了什麼**。

        判準：若某 ER 行在「該步驟被執行」之外不含任何額外資訊，
        即為複述，FAIL。
        若某步驟確無可觀察結果，**應合併該步驟**，
        不得為湊 1:1 而造 ER。

        十條全查。補設閘門 G73 —— 偵測 ER 行與其對應 procedure 步驟
        之複述關係（詞彙重疊率 ＋ 無新增可觀察標的）。
        判準之詞彙基礎須有經驗來源（比照 R-P83 / R-P88），
        不得憑印象列舉。

        **R-P87 依 R-P36 原文不改**，於其下加註指向本條。
        裁決者 Pei，逐字依據：「是」（回應 12 Q1）。

[R-P97] **時間量測之 ER 不得寫數值相等。**
        `001` ER3 與 `004` ER3 寫「The recorded time **equals**
        SplashScreen_Time / StandardScreen_Time」。
        實測時間不會精確等於配置值，該 ER 嚴格執行必然 fail（§7 之 FF）。
        §8.4.1 禁止造容差值，故不得改寫為「±100ms」等。
        正確做法為描述**觀察到之行為**：到期前不顯示、到期後顯示。
        現成範式即本批 `002` / `003` 之
        「through SplashScreen_Time」寫法。
        裁決者 Pei，逐字依據：「是」（回應 12 Q2）。

[R-P98] **首批覆核須完成十條，分析層現僅讀五條。**
        12 包上繳之逐條差異全文中，分析層讀畢 `001`–`005`，
        `006`–`010` 未讀（MCP 讀取上限）。
        T1 於已讀五條中五條命中，推測全批皆然 ——
        **但推測不等於實測**（R-P64）。
        本包上繳須附 `006`–`010` 之修正後全文，
        並附 `001`–`005` 之再修正後全文供對照。
        寫回於分析層完成十條覆核前不開放。
        裁決者 Pei，逐字依據：「是」（回應 12 Q3、Q4）。

[R-P99] **合成 fixture 通過不足以佐證閘門正確 —— 逐字採納執行層之結論。**
        12 §七(乙) 第 7 項：四次「合成 fixture 過、真實資料出問題」
        （A-PW35 `\b` bug、A-PW39 檔名空格、A-PW50 `[spec-derived]`、
        A-PW58 誤殺）之後，該結論已具實證基礎，且第四次方向首次相反。
        現況：G64 / G66 / G71 / G72 四閘**僅有合成證據**。
        （a）G64 依 R-P83 之作法補經驗語料
             （Comfort / Privacy 已交付之 `pre_conditions`），
             量測其完備性與偽陽性
        （b）G66 / G71 / G72 之真實實測**須於寫回包一次補齊**，
             與 12 §七(甲) 第 2 項合併處理
        （c）往後任何新閘之上繳，須標明其證據為
             「合成」或「合成＋真實」，不得混稱 PASS
        裁決者 Pei，逐字依據：「是」（回應 12 Q5）。

[R-P100] 12 §七之其餘各項處置：
        （甲）第 4 項（`Test Case Framework` 為何存在、為何獨有）
              **登記不阻斷**。執行層已誠實標為「無資料可判別，不臆測」，
              該處置正確，不強令臆測。
        （甲）第 5 項（colorScale `H10:H145` 語義）
              **登記不阻斷**，依 R-P95 可與寫回並行。
        （乙）第 6 項（B6 範圍限定致看不見全貌）
              **接受該代價並明載**：R-P94 之窄範圍為刻意選擇，
              Comfort O 欄之矛盾係於該窄範圍內偶然撞見；
              其餘欄位若有同型問題，本方法查不到。
              此限制登記，不擴大範圍。
        裁決者 Pei，逐字依據：「是」（回應 12 Q5）。

（以上**五條**裁決條文，抄入 RULINGS.md 時逐字保留，
 每條獨立區塊，不得夾於敘述中。）

## B. 本包須產出

### B1. R-P87 之加註（R-P96）

依 R-P36 原文不改，於 R-P87 下加註：

  「註記（R-P96，13 包）：本條僅規定 procedure 步數 = ER 行數，
   未規定補出之 ER 行須為可觀察結果，致 12 包之修正引入新缺陷 ——
   五條已讀 TC 中五條將 procedure 動作複述為 ER。
   判讀價值之要求已由 R-P96 補足。原文保留。」

加註後須以雜湊佐證原文位元組未變。

### B2. 十條之 ER 再修正（R-P96 / R-P97）

逐條檢查並修正：
  複述型 ER 行（R-P96）—— 改為可判讀之結果，或合併該 procedure 步驟
  時間量測之 `equals` 表述（R-P97）—— 改為行為描述

**須逐條列出「12 包修正後 / 本包再修正後」對照**，
不得只給最終版本，亦不得以「未變更」帶過而不列出該欄現值。

修正後重跑完整 lint，含 G63（1:1 仍須成立）。

**注意**：若依 R-P96 合併步驟致 procedure 步數減少，
G63 之 1:1 仍須成立，且 §10.5「至少 2 個編號步驟」不得違反。

### B3. G73 實作（R-P96）

  偵測 ER 行與其對應 procedure 步驟之複述關係
  判準須含：詞彙重疊率、該 ER 行是否引入 procedure 未述之可觀察標的
  詞彙基礎須有經驗來源（比照 R-P83 / R-P88），不得憑印象列舉
  合成 fixture ＋ **對本批十條之真實實測**，二者皆須回報
  依 R-P99(c)，明標其證據為「合成＋真實」

### B4. G64 之經驗語料補測（R-P99(a)）

  以 Comfort / Privacy 已交付之 `pre_conditions` 為語料
  量測 G64 之偽陽性率與完備性（是否存在第三類環境穩定性表述）
  依 R-P80，僅用其結構性事實，不引用任何內容裁決
  回報：語料行數、偽陽性數、新發現之形態（若有）

### B5. 十條 TC 全文（再修正後）—— **上繳包必附**

十三欄 × 10 條，逐條含 `reasoning`。
**不得節錄、不得省略換行、不得以摘要代替。**

**輸出順序須為 `006`–`010` 在前、`001`–`005` 在後** ——
分析層已讀 001–005 之 12 包版本，優先取得未讀者可降低
再次因讀取上限而覆核不全之風險（A-PW55）。

## C. 抽取規格

  §C rule 1 / 2 / 3 / 4 正則不變。
  R-P17 文字層定義不變。
  `MIN_FINGERPRINT = 40` 不變（R-P62）。

## D. 閃點

G0 為前置閘。G0–G16、G13b、G18–G72 沿用（G17 已移除），期望值不變。

| # | 項目 | 期望值 |
|---|---|---|
| **G73** | ER 複述偵測（R-P96） | fixture 正常 PASS、違規實際 FAIL；**且對本批十條之真實實測結果須回報**；再修正後 0 findings |
| **G74** | 時間量測 ER 無 `equals` 型表述（R-P97） | 十條中涉時間量測者皆改為行為描述；【實測填入】受影響條數 |
| **G75** | G64 經驗語料（R-P99(a)） | 【實測填入】語料行數、偽陽性數、是否存在第三類形態 |
| **G76** | R-P87 加註後原文位元組未變（B1） | UNCHANGED |
| **G63** | Procedure ↔ ER 1:1（沿用） | 再修正後仍 10 / 10 |
| **G70** | 修正後 lint 全閘（沿用） | 全 PASS；leaf 仍 3；TC 仍 10 |

G73 之驗證條件同 G33，且依 R-P99(c) 須明標證據型別。

## E. framework

§E 已定版（R-P35），本包不動。
`Test Case Framework` 分頁經 R-P92 判讀為空，不構成權威來源，§E 不受影響。

## F. Anomaly 異動

  A-PW56 → 依 R-P92 之判讀結果更新：該分頁為空，不構成權威來源；
           「為何存在、為何獨有」依 R-P100 登記不阻斷
  新增 A-PW59：R-P87 之修正引入新缺陷 —— 為湊 1:1 而將 procedure
               動作複述為 ER，已讀五條中五條命中。
               根因為分析層條文不完整（只規定行數，未規定判讀價值）。
               **此為分析層裁決造成實質退步之首例** ——
               有別於 A-PW30（修飾語誤述）與 A-PW37（論證前提不成立）
  新增 A-PW60：時間量測 ER 寫數值相等，嚴格執行必然 fail（§7 之 FF）
  新增 A-PW61：四閘（G64 / G66 / G71 / G72）僅有合成證據；
               依 R-P99 補實測或明標證據型別

## G. DATA_REQUESTS

  DR-PW1 → live，High
  DR-PW3 → live，Medium
  DR-PW5 → live，High
  DR-PW6 → live，Medium
  DR-PW7 → live，Low
  DR-PW2、DR-PW4 → 維持撤回
  無新增

## H. 作業指示

  1. G0 前置閘
  2. 依 R-P96 為 R-P87 加註（B1），驗 G76
  3. 依 R-P96 / R-P97 再修正十條（B2），逐條列前後對照
  4. 實作 B3 G73，含合成 fixture 與本批十條之真實實測
  5. 依 R-P99(a) 補測 G64 經驗語料（B4），驗 G75
  6. 再修正後重跑完整 lint，驗 G63 / G70 / G73 / G74
  7. 輸出 B5 十條全文（`006`–`010` 在前）
  8. 以 §D 全表自驗
  9. §A 五條裁決逐字抄入 RULINGS.md；§F 入 ANOMALIES.md
 10. 上繳 features/power/docs/upstream/13_er_quality.md，更新 docs/INDEX.md

## I. 禁區

  **不得寫回 FW036 workbook（R-P98 未完成前一律不開放）**
  **不得對任何 workbook 呼叫 `save()`**（R-G3）
  不得執行任何 git 操作（全數屬 Pei）
  不得補齊 SWE-PM-089（R-P1）
  不得沿用純文字衍生物之任何數字（R-P10）
  不得自行調整 §C 正則
  不得修改任何已落檔裁決條文之內文（R-P36）
  不得測試未被引用之錨點（R-P42）
  不得解析任何 RTF 或 OLE stream 之內容（R-P39、R-P48）
  不得續行章節層反向缺口調查（R-P37）
  不得自行調整 §E（R-P35）
  不得因 A-PW46 / A-PW51 改變車型欄之留白處置（R-P54、R-P81）
  不得修改 Comfort 之任何交付物；查核範圍限 `NEVER_WRITE` 各欄（R-P94）
  不得調整 `MIN_FINGERPRINT`（R-P62）
  不得擴大批次範圍超出 `Power Down` 3 leaf
  **不得為湊 1:1 而造 ER；無可觀察結果之步驟應合併（R-P96）**
  **不得以造容差值解決 R-P97（§8.4.1）**
  **G73 / G64 之詞彙不得憑印象列舉，須有經驗來源**
  不得以 repo 現況作為任何 fixture 之測試對照
  素材補入超出 features/power/inputs/ 需 Pei 裁定

## J. 本包產生之新條文清單（自檢）

  R-P96 ER 每行須為可觀察結果，不得為動作複述（G73）
  R-P97 時間量測之 ER 不得寫數值相等
  R-P98 首批覆核須完成十條，寫回於完成前不開放
  R-P99 合成 fixture 通過不足以佐證閘門正確（逐字採納執行層結論）
  R-P100 12 §七其餘各項之處置

  逐條確認：**五條**，皆以獨立區塊呈現於 §A，未夾於敘述中。
  自檢：§A 區塊數 = 5、§J 列數 = 5、§H 步驟 9 寫「五條」，三處一致。
