# 52 — test_item 定案、狀態表補測與收尾

下放包 | 分析層 → 執行層 | 往返 NN = 52

前置：51 包已停於 R-P334(b) 之攔阻點，**處置正確**。
其已完成者：G99 103 → **111 / 111**、G94 111/111、G103 111/111、
G233 15 / 15；`source_clause` 3,363 → 39,574 字元；備份 216 檔。
**其 §H 步驟 3 以後未執行，裁決條文未抄** —— 併入本包。

**51 包不再修改**（分析層已就地修訂三次，逾 R-P200 之立意）。
本包承接其未完成之 B2–B6 與全部裁決條文之抄錄。

沿用不變之節：`§C 抽取規格` 同 02 包；`§I 禁區` 同 51 包 §I，另增列見本包 §I。

## A. 本包裁決條文（逐字，抄入 RULINGS.md）

```
[R-P336] `test_item` 首段**不加前綴**；前綴為原句自帶而非格式要求。
         R-P334(b) 之查證結果具決定性：
         Comfort 之前綴**逐字出現於該列 `Specification Reference` 者 0 / 149**；
         **465 列中 317 列（68%）根本沒有前綴**；
         其字母族有 11 種（`R` `C` `CM` `CR` `EH` `W` `LS` `HVACP`
         `HVACSB` `ICE` `CW`），且編號與節次系統性錯開
         （`C1)` ↔ 節 2.2、`C2.1)` ↔ 2.3.1）。

         **判定：該前綴非 TC 作者所加，係規格原句自帶。**
         Comfort 之來源文件（`HMI_Comfort_HMI_Logic_and_Flow`）
         於正文即標有需求標籤，逐字引句自然帶入；
         **68% 無前綴者，即其原句本無標籤**。

         **Power 之來源為 CFTS009 / CFTS010，其正文無此類標籤** ——
         故逐字引 `source_clause` 即無前綴，**不得自行加上 `section`**。
         加 `section` 為發明一個 Comfort 所無之形式（§8.4.1 之同類）。

         R-P334(b) 之「回報並停」就此結案：**答案為不加。**
         裁決者 Pei，逐字依據：「儘快收尾」。
```

```
[R-P337] `test_item` 首段之取句判準（R-P333(a) 之補充）。
         R-P333(a) 令首段「逐字自 `source_clause` 取」，
         **而未給「取哪一句」之判準**；
         51 包重建後 `SWE-PM-009` 之 `source_clause` 達 5,933 字元
         （含 `4941453` 整張表），該缺口因而顯著。

         判準：
         （a）**取該 TC 之 `expected_result` 所斷言之行為
              在 `source_clause` 中之對應句**；
              此與 §10.7 之「`specification_reference` lists every spec
              section the TC **directly verifies**」同一原則
         （b）**一句為度**；一句不足以承載該 TC 所驗之完整行為時，
              取**最小完整句組**（連續、不跳接）
         （c）**若該 TC 所驗者為表格中之某列**（如 `4941453`），
              取**該列之欄名與值**，**不取整表**；
              其形式為 `<狀態>: <欄名>=<值>, <欄名>=<值>…`，
              各欄以逗號分隔，**逐字自表取**
         （d）取句後**不得改寫、不得補字、不得省略中間文字**
         裁決者 Pei，逐字依據：「儘快收尾」。
```

```
[R-P338] G237 之首段上限自 Comfort 實測導出，不憑印象訂。
         51 包指出：`source_clause` 母體增大 20 倍後，
         **G237 之「首段為 `source_clause` 之子字串」判準變得極寬鬆**，
         其攔截力須重估。

         處置：
         （a）量 **Comfort 465 列之 I 欄首段長度分布**
              （首段 = 第一個空行之前），回報其 min / 中位數 / P95 / max
         （b）**以 P95 為 Power 之首段長度上限**，寫入 G237
         （c）**不得憑印象訂數值**（R-P64 / R-P250）——
              本條之上限須為 (a) 之實測值
         （d）G237 之判準因而為三項：
              首段長度 ≤ P95、首段為 `source_clause` 之**連續**子字串、
              且首段後恰一個空行、後段以 `(` 起 `)` 止且含 ` -> `
         裁決者 Pei，逐字依據：「儘快收尾」。
```

```
[R-P339] G94 對 8 個重建 leaf 之效力降級；52 包以獨立實作重抽。
         51 包自陳：`verify_source_clause.py` 之比對對象為
         `anchor_bodies()` 之串接，**而重建 `source_clause` 用的是
         同一函式、同一串接式** ——
         **對該 8 個 leaf，G94 於原理上不可能 FAIL**。

         **此為「自洽而不完整」之第六例**，
         而其窄處**不在宣告而在產生器與驗證器同源**
         —— 較前五例更隱蔽：前五例之窄可由讀者看出，本例須讀二段程式方知。

         處置：
         （a）**上表之 G94 111/111 不得被讀為
              「8 個重建 leaf 已獨立驗證」**；其效力僅及於其餘 103 個手寫 leaf
         （b）以**獨立實作**重抽該 8 個 leaf 之 `source_clause`
              （比照 G103 對 layer3 之作法：不讀原產生器之任何中間產物，
               自 CFTS 本文以 §C 之正則獨立重建），與現行比對
         （c）不同者逐一列出並判其何者為是
         裁決者 Pei，逐字依據：「儘快收尾」。
```

```
[R-P340] `4941453` 狀態表之處置：ER 精確化與未測對照項補測。
         51 包實測該表為 **13 個具名狀態 × 8 個輸出欄 = 104 組狀態—輸出對**，
         其值為具體可測者（`ON` / `OFF` / `ON (Muted)` / `OFF (*)`）。

         **R-P328 所預判之二事皆成立，且其面大於裁定當時之評估**：

         （a）**ER 精確化** —— 例：`SWE-PM-005`（Standby）現行 ER 為
              「無任何功能可用」，而該表給出逐輸出之值。
              **凡其 leaf 之 `source_anchor` 含 `4941453` 者，
              其 ER 之泛稱表述須改為該表所載之具體值**；
              取值依 R-P337(c) 之形式。
         （b）**未測對照項補測** ——
              `Logistic Idle` / `Logistic Standby` / `Logistic Sleep` / `Bench`
              之輸出組合於現行 20 條中無對應 TC；
              `Full-Operation` 與 `Timed` 各有二列
              （差異在 `Source` 是否含 `SDCARD` / `BT` / `Phone Call`），
              現行未分辨。
              **依 §8.3（mode 為拆分軸）逐一補測。**

         **TC 數將因此增加，據實回報，不得為維持號碼穩定而不補**（R-P328(e)）。
         裁決者 Pei，逐字依據：「儘快收尾」。
```

```
[R-P341] §4.5 欄位歸屬全批實測；本包為最後一個查證包。
         Pei 要求確認 `input_test_data` 之內容是否寫入
         `pre_conditions` 或 `test_procedure` / `expected_result`。

         **分析層實測批次一全 16 條，結果為無重複**：
         無獨立資料集者 `input_test_data` = `NA`，操作值寫於 procedure；
         有獨立資料集者其值只寫於 `input_test_data`，
         **procedure 以 `listed in Input Test Data` 引用而不重述**；
         `pre_conditions` 僅放環境與狀態。
         **此為 12 包 R-P89 修過之結果（G65），非本來即對。**

         **惟分析層僅讀 16 / 280** —— 依 R-P327 之教訓須全批實測。

         處置：
         （a）**全批跑 G65**，逐條回報；命中者列其重複之欄位與逐字內容
         （b）**擴及 `expected_result`** —— G65 現只驗 `test_procedure`
              與 `pre_conditions`，而 Pei 之問含 ER；
              擴充後以 fixture 證明其會 FAIL
         （c）命中者依 §4.5 處置：環境資料 → `pre_conditions`、
              互動資料 → procedure、獨立資料集 → `input_test_data`；
              已屬前二者而重複列於 `input_test_data` 者改 `NA`

         **本包為最後一個查證包。**
         自本條起，新發現一律依 R-P254 分流 ——
         **會使某條 TC 於事實上錯誤者修；其餘一律入 R-P247 之限制清單，
         不開裁決條文、不開往返。**
         裁決者 Pei，逐字依據：「全部檢查過然後這裡我需要儘快收尾了」。
```

## B. 本包須產出

**先抄 51 包之 R-P327 – R-P335 九條**（其未抄錄），再抄本包六條。

### B1. `test_item` 全批重寫（R-P336 / R-P337 / R-P338）—— **最優先**

不加前綴；依 R-P337 取句；G237 三項判準（上限自 Comfort 實測導出）。

### B2. `4941453` 之 ER 精確化與補測（R-P340）

含該錨點之 leaf 逐一處理；四個未測狀態與二組 Source 分辨補測；TC 數據實回報。

### B3. §4.5 全批實測（R-P341）

G65 擴及 `expected_result`；全批逐條回報；命中者依 §4.5 處置。

### B4. 8 個 leaf 之獨立重抽（R-P339）

獨立實作，不讀原產生器之中間產物；與現行比對；不同者列出。

### B5. 51 包未完成之 B2–B6

20 條重新檢視、三代對照表重建、三處訂正與加註、全量 dry-run、驗證邊界更新。

## D. 閃點

G0 為前置閘。素材層閘門（G0 / G94 / G99 / G103）依 R-P327 重跑。

| # | 項目 | 期望值 |
|---|---|---|
| G237 | `test_item` 三項判準（R-P338） | 上限為 Comfort P95 實測值；全批通過；fixture 驗明會 FAIL |
| G238 | 首段無前綴（R-P336） | 全批首段皆不含 Power 自加之 `section` 前綴 |
| G239 | `4941453` 補測（R-P340） | 四個未測狀態與二組 Source 分辨皆有 TC；ER 已精確化 |
| G65′ | §4.5 全批（R-P341） | 擴及 ER；全批逐條回報；命中者已處置 |
| G240 | 8 leaf 獨立重抽（R-P339） | 獨立實作；與現行比對之差異已列出並判定 |
| G70 | lint 全閘 | 全 PASS |

## E. framework

§E 已定版（R-P35）。**寫回於 53 包**，其條件為本包全數完成
＋ Pei 就最終版本明示授權。

## F. Anomaly 異動

開新號前先查現行最大號並回報（現行 A-PW328）。

- 新增：Comfort 之 `test_item` 前綴為原句自帶，非格式要求（R-P336）
- 新增：R-P333(a) 未給取句判準（R-P337）
- 新增：**「自洽而不完整」第六例 —— 產生器與驗證器同源**（R-P339）
- 新增：`4941453` 揭露 4 個未測狀態與 2 組未分辨之 Source（R-P340）
- 新增：G65 原不驗 `expected_result`（R-P341）

## G. DATA_REQUESTS

DR-PW5 advisory；DR-PW9 / DR-PW11 blocking，其 leaf 不在本次交付。無新增。

## H. 作業指示

1. G0 前置閘（依 R-P327 重跑素材層閘門）
2. 查現行最大號並回報
3. **先抄 51 包之 R-P327 – R-P335 九條入 RULINGS.md**
4. B1 `test_item` 全批重寫，驗 G237 / G238 —— **最優先**
5. B2 `4941453` 之 ER 精確化與補測，驗 G239
6. B3 §4.5 全批實測，驗 G65′
7. B4 8 leaf 獨立重抽，驗 G240
8. B5 51 包未完成之 B2–B6
9. 以 §D 全表自驗
10. **抄錄裁決條文前重驗一次 §J 自檢**（R-P200(c)）；
    §A 六條逐字抄入 RULINGS.md；§F 入 ANOMALIES.md
11. 上繳 `features/power/docs/upstream/52_test_item.md`，更新 `docs/INDEX.md`

## I. 禁區

沿用 51 包 §I 全部條目，另增列：

- **不得為 `test_item` 首段自加 `section` 或任何前綴**（R-P336）
- **不得憑印象訂 G237 之首段上限**（R-P338(c)）
- **不得以 G94 之 111/111 宣稱 8 個重建 leaf 已獨立驗證**（R-P339(a)）
- **不得為維持號碼穩定而不補 TC**（R-P340）
- **不得寫回**（§E）

## J. 本包產生之新條文清單（自檢）

1. R-P336 `test_item` 首段不加前綴
2. R-P337 首段之取句判準
3. R-P338 G237 之上限自 Comfort 實測導出
4. R-P339 G94 對 8 個重建 leaf 效力降級；獨立重抽
5. R-P340 `4941453` 之 ER 精確化與未測對照項補測
6. R-P341 §4.5 全批實測；本包為最後一個查證包

逐條確認：**六條**，皆以獨立頂層 fenced block 呈現於 §A，未夾於敘述中。
自檢：§A 頂層區塊數 = 6、§J 列數 = 6、§H 步驟 10 寫「六條」，三處一致。
**本包下放後分析層不再修改**（R-P200(a)）。

## K. 分析層自判：本包是否仍有該驗而未驗者

**有，三項。**

1. **`4941453` 使第七批之 TC 數增加，其面大於 R-P322 裁定「併入」時所評估者。**
   §K 之疑慮於 51 包確立。**惟不重開該裁定** ——
   併入之理由（客戶比對二份同名工作簿之成本）不因條數增加而改變。
2. **「自洽而不完整」已第六例，且其隱蔽度遞增。**
   前五例之窄可由讀者看出，本例須讀二段程式方知
   （產生器與驗證器同源）。**無通用防範**；
   其唯一之發現途徑仍為執行層之自查。
3. **本包為最後一個查證包（R-P341），
   其代價為：此後之發現除事實錯誤外皆不再處理。**
   該取捨由 Pei 所定，其風險為
   **交付物中可能存在已知而未修之非事實性缺陷** ——
   該等一律入限制清單，於交付說明中可見。
