# 39 — §12 逐列核對、axis 收尾與反向重複掃描

下放包 | 分析層 → 執行層 | 往返 NN = 39

前置：38 包上繳已覆核，判定 **ACCEPT**。
第二級改值 204 處完成；`design_method` 之狀態轉換由 253（95.8%）降至 91（34.5%）；
第 8 列 7 → 2；G154 經查**無漏檢**。

沿用不變之節：`§C 抽取規格` 同 02 包；`§I 禁區` 同 38 包 §I，另增列見本包 §I。

## A. 本包裁決條文（逐字，抄入 RULINGS.md）

```
[R-P265] `axis` 無對應 40 條：**省略該欄**，不新增列舉值、不留空。
         38 §五所呈三選項皆有代價。**分析層指出第四選項，且有明文依據：**

         **§10.1 逐字**：
         「Top-level response also carries `reasoning` (§10.4) and
          **optional** `keywords`, `duplicate_of`, `distinguishing_axis`.」
         —— `distinguishing_axis` 為 **optional**。

         **§4.6 逐字**：
         「**On `## Sibling Rows` injection**, output `duplicate_of` … and
          `distinguishing_axis` …」
         —— 其輸出之時機為 **sibling 注入時**。

         該 40 條之 leaf 僅產出 1 條 TC，**無 sibling 可注入**，
         故 §4.6 之輸出條件本即不成立；§10.1 復明訂其為 optional。
         **省略該欄為文義所許，非權宜。**

         處置：
         （a）該 40 條**移除 `axis` 與 `delta` 二欄**（非設為空字串）
         （b）**C2 之判準訂正**：由「`axis` 須存在且非空」
              改為「**若該欄存在則須非空且為六值之一**」；
              欄位不存在者不觸發
         （c）另立檢查：**leaf 產出 ≥ 2 條 TC 者，其 `axis` 欄必須存在** ——
              以免 (a) 之省略被誤用於有 sibling 之情形
         （d）**不改 §4.6、不改 §10.1** —— 本裁定係依其文義，非變更 canon
         裁決者 Pei，逐字依據：「上繳」。
```

```
[R-P266] §12 全九列之條件欄與 tie-break 須逐列核對。
         38 §九第 1 項：R-P259 查出第 8 列之謂詞取 tie-break 之措詞
         （`≥3 steps`）而非該列條件欄（`≥3 features`）；
         **§12 之逐列條件欄與 tie-break 是否另有被混用者，本包未逐列核對。**

         而 164 條新值中僅 5 條經第二方讀過 ——
         其餘 159 條之正確性僅依 first-match 謂詞。
         **若另有一列同型錯誤，其影響面與第 8 列相當或更大。**

         處置：逐列核對全九列，逐列回報三事：
         （a）該列**條件欄之逐字**
         （b）現行謂詞**實際所依之措詞**
         （c）二者是否一致；不一致者列出其差異並重判該列

         §12 之 tie-break 僅三句
         （`State Transition = state-change focus`；
          `Scenario = ≥3 steps crossing features`；
          `Functional = 1–2 steps single feature`），
         **其餘六列無 tie-break**，故其謂詞不得引用任何 tie-break 措詞。
         裁決者 Pei，逐字依據：「上繳」。
```

```
[R-P267] 第 4 列之代理判準系統性低估，須量其影響面。
         38 §九第 2 項：R-P260(a) 判 `…-026` / `…-027` 為第 4 列
         （`LTM High` 是否存在 ＋ 所選之值，二條件共同決定），
         而執行層之 `substantive_conditions` **只數到 1 項** ——
         **該分歧顯示第 4 列之代理判準可能系統性低估**。

         成因：代理判準數的是 `pre_conditions` 之實質前提項數，
         而 `…-026` 之第二個條件（所選之值）**在 `test_procedure` 而非前提**。
         **條件不必然全數落於 `pre_conditions`。**

         處置：
         （a）將條件來源擴及 `test_procedure` 與 `input_test_data`，
              重跑第 4 列之代理判準，回報 84 → ?
         （b）新增者逐條列出其二條件為何，供分析層抽樣複核
         （c）**代理判準仍不得凌駕實質判準**（R-P236(b)）——
              其結果仍為提案
         （d）本包**不因此改值**；改值於 40 包，其前提為分析層之複核
         裁決者 Pei，逐字依據：「上繳」。
```

```
[R-P268] G150 之有效性遞減須有機制察覺。
         38 §九第 3 項：G150 之 `WALK` 交集自本包起持續縮小
         （本次 2 / 4 未走查），**往後每次改值皆會再縮，
         該閘門之有效性將逐步歸零而不會報錯**。

         此與 A-PW219（閘門有效而結果被擱置）**互為鏡像**：
         一者為閘門有效而輸出未用，一者為閘門形式存在而實質失效；
         **二者於報表上皆呈現為「無異常」。**

         處置：
         （a）G150 每次執行須回報其**有效樣本數**與**母體數**，
              而非僅回報結果
         （b）有效樣本數低於母體之 50% 時，**其結果標為「代表性不足」**，
              不得作為該欄無異常之證據
         （c）此規則推及**所有以交集或抽樣為基礎之閘門** ——
              逐一盤點現行閘門中屬此類者並回報
         裁決者 Pei，逐字依據：「上繳」。
```

```
[R-P269] 反向重複掃描：以 `source_clause` 逐字相同為條件全批掃。
         38 §九第 4 項：(b-2) 之形態（**錨點不同而內容相同**）全批未掃；
         本包僅由 G178 之 13 組中辨出 2 組，
         **未反向以 `source_clause` 逐字相同為條件全批掃描** ——
         若另有 clause 相同而 TC 不同者，現行無任何機制查之。

         處置：
         （a）以 `source_clause` 正規化後逐字相同為條件，全批掃出所有 leaf 對
         （b）逐對回報其 TC 是否亦相同；
              **clause 相同而 TC 不同者為新形態**，逐對列出其差異
         （c）該形態之意義：同一規格文字產出不同之驗證 ——
              或為刻意（不同錨點側重不同面向），或為不一致
         （d）本包只掃與呈，**不改值**；裁定於 40 包
         裁決者 Pei，逐字依據：「上繳」。
```

```
[R-P270] (a) 型 8 組之 `tc_title` 不改，追認執行層之處置。
         R-P263(a) 令 `tc_title` 逐字相同者加以區別；
         執行層未改，理由為**無語義材料且屬 DR-PW12 之待答事項**（G184）。

         **追認。** 其理由成立：在 DR-PW12 未答前，
         二 leaf 之側重為何**尚不可知**；
         逕加「側重之語」即為造值（§8.4.1）——
         而 R-P263(a) 明禁「僅加編號」，故無合規之改法。

         **分析層之 R-P263(a) 未察此前提**，責在分析層。

         改以：
         （a）該 8 組之 `reasoning` 明載其與對造 leaf 共用錨點
              （R-P186 已要求，查其是否已載；未載者補）
         （b）`tc_title` 之區別**列為 DR-PW12 之待答項**，
              於該 DR 內明載「其答覆將決定 8 組標題是否需分化」
         （c）DR-PW12 獲答前，工作簿中將存在 8 組同標題而不同 Requirement ID 之列 ——
              **此事實須載入交付說明之驗證邊界**，使收件方知其為已知且待答，
              非疏漏
         裁決者 Pei，逐字依據：「上繳」。
```

## B. 本包須產出

### B1. §12 全九列核對（R-P266）—— **最高優先**

逐列回報條件欄逐字、現行謂詞所依措詞、是否一致；
不一致者列出差異並重判該列。

### B2. `axis` 40 條省略欄位（R-P265）

移除 `axis` 與 `delta`；C2 判準訂正；新增「≥ 2 條 TC 者必須有 `axis`」之檢查。

### B3. 第 4 列代理判準擴充（R-P267）

條件來源擴及 `test_procedure` 與 `input_test_data`；84 → ?；
新增者逐條列出二條件；**不改值**。

### B4. 以 `source_clause` 反向全批掃（R-P269）

逐對回報；clause 相同而 TC 不同者逐對列出差異；**不改值**。

### B5. 閘門代表性盤點（R-P268）

G150 回報有效樣本／母體；盤點所有以交集或抽樣為基礎之閘門。

### B6. (a) 型 8 組之 `reasoning` 與 DR-PW12 更新（R-P270）

`reasoning` 查補；DR-PW12 增列待答項；交付說明之驗證邊界增列。

## D. 閃點

G0 為前置閘。G0–G184 沿用（G17 已移除；G109 不實作），期望值不變。

| # | 項目 | 期望值 |
|---|---|---|
| G185 | §12 全九列核對（R-P266） | 九列皆回報三事；不一致者已列出並重判 |
| G186 | `axis` 40 條省略（R-P265） | 40 條無 `axis` / `delta` 欄；C2 訂正後不觸發；≥ 2 條之 leaf 皆有 `axis` |
| G187 | 第 4 列代理判準擴充（R-P267） | 【實測填入】84 → ?；新增者逐條有二條件 |
| G188 | `source_clause` 反向掃（R-P269） | 【實測填入】clause 相同之 leaf 對數；其 TC 相同／不同之分布 |
| G189 | 閘門代表性（R-P268） | G150 回報有效樣本／母體；此類閘門已盤點 |
| G70 | lint 全閘 | 全 PASS |

## E. framework

§E 已定版（R-P35），本包不動。
**寫回仍不開放** —— 阻斷條件為 R-P266 之核對結果
（若另有一列同型錯誤，須先重判改值）。

## F. Anomaly 異動

開新號前先查現行最大號並回報（R-P147）。

- 新增：§12 其餘八列之條件欄與 tie-break 未逐列核對（R-P266）
- 新增：第 4 列代理判準只數 `pre_conditions`，系統性低估（R-P267）
- 新增：以交集或抽樣為基礎之閘門會靜默歸零（R-P268）
- 新增：clause 相同而 TC 不同之形態全批未掃（R-P269）
- 新增：R-P263(a) 未察「無語義材料」之前提，責在分析層（R-P270）

## G. DATA_REQUESTS

DR-PW12 依 R-P270(b) 增列待答項。其餘沿用。

## H. 作業指示

1. G0 前置閘
2. 查現行最大號並回報
3. B1 §12 全九列核對，驗 G185 —— **最高優先**
4. B2 `axis` 40 條省略，驗 G186
5. B3 第 4 列代理判準擴充，驗 G187 —— **不改值**
6. B4 `source_clause` 反向掃，驗 G188 —— **不改值**
7. B5 閘門代表性盤點，驗 G189；B6 `reasoning` 與 DR-PW12 更新
8. 以 §D 全表自驗
9. **抄錄裁決條文前重驗一次 §J 自檢**（R-P200(c)）；
   §A 六條逐字抄入 RULINGS.md；§F 入 ANOMALIES.md
10. 上繳 `features/power/docs/upstream/39_row_audit.md`，更新 `docs/INDEX.md`

## I. 禁區

沿用 38 包 §I 全部條目，另增列：

- **不得新增 §4.6 之列舉值，亦不得改 §4.6 / §10.1**（R-P265(d)）
- **不得將 `axis` 設為空字串**（R-P265(a)）
- **不得因第 4 列代理判準之擴充而改值**（R-P267(d)）
- **不得因反向掃描結果而改值**（R-P269(d)）
- **不得逕改 (a) 型 8 組之 `tc_title`**（R-P270）

## J. 本包產生之新條文清單（自檢）

1. R-P265 `axis` 無對應 40 條省略該欄，依 §10.1 之 optional 與 §4.6 之輸出條件
2. R-P266 §12 全九列之條件欄與 tie-break 須逐列核對
3. R-P267 第 4 列代理判準系統性低估，須量影響面
4. R-P268 G150 之有效性遞減須有機制察覺
5. R-P269 以 `source_clause` 逐字相同為條件反向全批掃
6. R-P270 (a) 型 8 組之 `tc_title` 不改，追認執行層之處置

逐條確認：**六條**，皆以獨立 fenced block 呈現於 §A，未夾於敘述中。
自檢：§A 區塊數 = 6、§J 列數 = 6、§H 步驟 9 寫「六條」，三處一致。
本次已逐一數過 §A 之 fenced block。
**本包下放後分析層不再修改**（R-P200(a)）。

## K. 分析層自判：本包是否仍有該驗而未驗者

**有，三項。**

1. **164 條 `design_method` 新值中僅 5 條經第二方讀過**（38 §九第 1 項）。
   R-P266 之逐列核對可查出**謂詞層**之錯，
   **查不出逐條層之錯** —— 二者為不同層次。
   若九列皆核對無誤，159 條之正確性仍僅依謂詞，未經第二方逐條複核。
2. **A-PW219 之形態（閘門有效而結果被擱置）成因為分析層連續五包推遲改值。**
   R-P253 已改流程，惟**該五包之其他閘門輸出是否亦有被擱置者，本包未查**。
3. **R-P265 之依據為 §10.1 與 §4.6 之文義**，
   而該二節之文字來自下放包首段之副本；
   **該副本與 `docs/runtime/ASPICE_SWE6_AI_Instruction.md` 之一致性
   自 01 包起未經比對**（36 §K 第 1 項已提，至今未處置）。
   若副本與權威版本有異，本裁定之依據即不成立。
