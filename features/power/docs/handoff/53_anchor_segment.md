# 53 — test_item 首段改以錨點為單位

下放包 | 分析層 → 執行層 | 往返 NN = 53

前置：52 包已停於 B1，**處置正確**。
其已完成者：51 包九條補抄（R-P327–R-P335，程式核對 9/9 逐字相符）、
R-P338(a) 之 Comfort 首段長度實測、選句器之 R-P250 驗證。

**52 包三項發現皆成立，且指向分析層同一個錯** ——
**以「句子」為首段之單位，而 Comfort 用的是「一個需求陳述」。**

**52 包裁決條文之處置**：
`R-P336`（不加前綴）、`R-P339`（G94 降級）、`R-P340`（狀態表補測）、
`R-P341`（§4.5 全批；最後一個查證包）**維持有效**；
`R-P337` / `R-P338` **依本包訂正**。
**六條連同本包五條，於本包一併抄入。**

沿用不變之節：`§C 抽取規格` 同 02 包；`§I 禁區` 同 52 包 §I，另增列見本包 §I。

## A. 本包裁決條文（逐字，抄入 RULINGS.md）

```
[R-P342] R-P338 之硬上限撤回：P95 為統計量之誤用。
         52 包實測 Comfort 465 列首段長度：
         min 26 / 中位數 114 / P95 282 / max 735；**其中 23 列超過 282**。

         **執行層之指摘成立**：P95 依定義即有 5% 在其上；
         以之為 G237 之 FAIL 門檻，
         **等於宣告客戶已交付且 Pei 明示為所要者之 23 列不合格**。

         **撤回 R-P338(b)(d) 之長度上限。首段長度不作閘** ——
         Comfort 未設，分析層不得發明。

         G237 之判準因而為三項（去長度）：
         （a）`test_item` 含**恰一個空行**
         （b）首段為 `source_clause` 之**連續**子字串（空白正規化後）
         （c）後段以 `(` 起 `)` 止且含 ` -> `

         R-P338(a) 之實測值保留為紀錄，用途改為
         「Power 首段長度分布與 Comfort 之對照」，入驗證邊界；**不作判準**。
         裁決者 Pei，逐字依據：「儘快收尾」。
```

```
[R-P343] 首段之單位改為「該 TC 所直接驗證之錨點」，取代取句判準。
         **R-P337 之根本錯誤**：以**句子**為首段之單位，
         致 52 包三項發現 —— 14 條無句 ≤ 上限、
         機械選句 4 / 6 不合 R-P250、表格型 clause 無句可取。

         **Comfort 之首段實為「一個需求陳述」，非「一個句子」** ——
         其來源文件之每個需求標籤（`C1)` `HVACSB` 等）各領一段陳述，
         長度 26–735 字元不等，**該段落即其首段，未經切句**。

         **Power 之對應物為「一個錨點」** ——
         而 `source_clause` 為**多個錨點之串接**
         （51 包重建後最長 5,933 字元、含 8–11 個錨點）。
         **分析層將串接體誤作單一陳述，故須切句；改以錨點為單位即無須切句。**

         新判準：
         （a）首段 = **該 TC 所直接驗證之錨點之文字，逐字**，
              不切句、不省略、不重組
         （b）驗多個錨點者取其**主要者**，其餘記於 `reasoning_note`
         （c）錨點文字逾 **1,000 字元**者（如 `4941453` 之 4,259 字元），
              得取該錨點中**對應該 TC 之連續片段**，
              並於 `reasoning_note` 記明起訖字元位置。
              **1,000 為分界而非品質門檻**，其唯一作用為區分
              「整段可讀」與「須取片段」二種處置，**不作 FAIL 判準**
         （d）首段仍須為 `source_clause` 之連續子字串（G237(b)）——
              錨點文字本即其組成，此條自動成立
         裁決者 Pei，逐字依據：「儘快收尾」。
```

```
[R-P344] R-P337(c) 之重組形式撤回；表格取連續片段。
         R-P337(c) 令表格列之首段寫為
         `<狀態>: <欄名>=<值>, <欄名>=<值>…`，
         **而其中之 `=` 與 `,` 為重建時所加，原文無之** ——
         與「連續子字串」要件直接衝突（52 包指出）。

         **撤回該重組形式。**
         `4941453` 於文字層為攤平之 126 段儲存格串流；
         某狀態之首段 = **該狀態列在串流中之連續片段**，
         依原分隔逐字取，**不加 `=`、不加 `,`、不重排欄序**。

         其可讀性由規格原文之形態決定 ——
         **原文如此攤平，即照樣引；不得為求可讀而重組**（§8.4.1 之同類）。
         欄名與值之對應關係於 `reasoning_note` 說明，不寫入 `test_item`。
         裁決者 Pei，逐字依據：「儘快收尾」。
```

```
[R-P345] 錨點歸屬由執行層提案、分析層抽樣複核；選句器不用。
         R-P343 之「該 TC 所直接驗證之錨點」仍需逐 TC 判斷，
         **惟其難度遠低於選句** —— 自 N 個錨點中擇一，
         而非自連續文字中切句；且其判斷有既有依據
         （該 TC 之 `split_reason` 與 `reasoning` 多已言明其所驗之面向）。

         **選句器（`pick_first_segment.py`）不用** ——
         52 包實測 4 / 6，全批覆蓋率 < 0.30 者達 103 / 280，
         依 R-P250「一個都取不到者不得使用」，**追認其不套用**。
         其二個未過者屬**無寫法可分辨之類**
         （肯定 vs 否定；二句皆述同一轉入而區辨全在條件子句），
         與 R-P332 之 `mod ⊂ mode` 同型 —— **非調參可解**。

         處置（比照 R-P272 之先例）：
         （a）執行層逐 TC **提案**其所驗之主要錨點，並列其依據
              （引 `split_reason` / `reasoning` 之逐字）
         （b）**分析層抽樣複核 ≥ 20%**，種子載明；
              素材**按 leaf 交錯排列**（R-P316 之教訓：
              依序讀取會集中於單一群）
         （c）不符者於 54 包訂正
         （d）**明載其風險**：改值先於複核，若複核不符則須二次改值
         裁決者 Pei，逐字依據：「儘快收尾」。
```

```
[R-P346] 52 包五條之效力與「最後一個查證包」之界線。
         `R-P336`（首段不加前綴）—— **維持**；其依據為
         「前綴係規格原句自帶」，不因單位改為錨點而變。
         `R-P339`（G94 對 8 個重建 leaf 降級；獨立重抽）—— **維持**。
         `R-P340`（`4941453` 之 ER 精確化與四個未測狀態補測）—— **維持**，
         其首段依 R-P344 取連續片段。
         `R-P341`（§4.5 全批實測；**本包為最後一個查證包**）—— **維持**。
         `R-P337` / `R-P338` —— 依 R-P342 / R-P343 / R-P344 訂正，
         其原文依 R-P36 不改，各於其下加註指向本包對應條文。

         **界線明確化**：R-P341 所指者為**查證**（找出未知之缺陷），
         而 52 / 53 二包所為者為**修正分析層自身條文之內部矛盾** ——
         二者不同，本包不違 R-P341。
         **自本包起，新查證一律依 R-P254 分流。**
         裁決者 Pei，逐字依據：「儘快收尾」。
```

## B. 本包須產出

**先抄 52 包之 R-P336 – R-P341 六條，再抄本包五條**（合計 11 條）。

### B0. Power 錨點文字長度分布（R-P343 之前提）—— **先行**

量全部錨點之文字長度，回報 min / 中位數 / P95 / max，與 Comfort 對照。
**若其中位數逾 Comfort 之 max（735），R-P343 之類比不成立 —— 停並上繳。**

### B1. `test_item` 全批重寫（R-P343 / R-P344 / R-P336）—— **最優先**

錨點歸屬提案（含依據）；首段逐字取錨點文字；
逾 1,000 字元者取連續片段並記起訖；不加前綴、不重組。
G237 依 R-P342 三項判準實作，fixture 驗明會 FAIL。

### B2. 錨點歸屬複核素材（R-P345(b)）—— **置於上繳最前**

抽 ≥ 20%，種子載明，**按 leaf 交錯排列**。

### B3. `4941453` 之 ER 精確化與補測（R-P340）

四個未測狀態 ＋ 二組 Source 分辨；首段依 R-P344。

### B4. §4.5 全批實測（R-P341）

G65 擴及 `expected_result`；全批逐條回報；命中者依 §4.5 處置。

### B5. 8 leaf 獨立重抽（R-P339）

### B6. 承接 51 包之 B2–B6

20 條重新檢視、三代對照表重建、三處訂正與加註、全量 dry-run、驗證邊界更新。
驗證邊界增列：Comfort 首段長度對照（R-P342）、
首段以錨點為單位之依據（R-P343）、其餘交付欄位之格式規則未查（§K 第 3 項）。

## D. 閃點

G0 為前置閘；素材層閘門依 R-P327 重跑。

| # | 項目 | 期望值 |
|---|---|---|
| G242 | Power 錨點長度分布（B0） | 【實測填入】min / 中位數 / P95 / max；中位數逾 735 即停 |
| G237 | `test_item` 三項判準（R-P342） | 恰一空行；首段為連續子字串；後段 `(` … ` -> ` … `)`；**無長度判準**；fixture 驗明會 FAIL |
| G238 | 首段無前綴（R-P336） | 全批首段皆不含自加前綴 |
| G241 | 錨點歸屬（R-P345） | 全批逐 TC 有主要錨點與依據；抽樣 ≥ 20% 已備且按 leaf 交錯 |
| G239 | `4941453` 補測（R-P340） | 四狀態 ＋ 二組 Source 皆有 TC；ER 已精確化 |
| G65′ | §4.5 全批（R-P341） | 擴及 ER；逐條回報；命中者已處置 |
| G240 | 8 leaf 獨立重抽（R-P339） | 差異已列出並判定 |
| G70 | lint 全閘 | 全 PASS |

## E. framework

§E 已定版（R-P35）。**寫回於 54 包**，其條件為本包全數完成
＋ 分析層之錨點歸屬抽樣複核 ＋ Pei 明示授權。

## F. Anomaly 異動

開新號前先查現行最大號並回報（現行 A-PW328）。

- 新增：R-P338 以 P95 為硬上限係統計量之誤用，其導出語料自身即有 5% 反例（R-P342）
- 新增：分析層以「句子」為首段單位，而 Comfort 用「一個需求陳述」；Power 之對應物為「一個錨點」（R-P343）
- 新增：R-P337(c) 之重組形式與連續子字串要件直接衝突（R-P344）
- 新增：選句器 4 / 6，其二個未過者屬無寫法可分辨之類（R-P345）

## G. DATA_REQUESTS

DR-PW5 advisory；DR-PW9 / DR-PW11 blocking。無新增。

## H. 作業指示

1. G0 前置閘（素材層依 R-P327 重跑）
2. 查現行最大號並回報
3. **先抄 52 包之 R-P336 – R-P341 六條入 RULINGS.md**
4. B0 Power 錨點長度分布，驗 G242 —— **中位數逾 735 即停**
5. B1 `test_item` 全批重寫，驗 G237 / G238 —— **最優先**
6. B2 錨點歸屬複核素材，驗 G241 —— **置於上繳最前**
7. B3 `4941453` 補測，驗 G239；B4 §4.5 全批，驗 G65′；B5 獨立重抽，驗 G240
8. B6 承接 51 包 B2–B6
9. 以 §D 全表自驗
10. **抄錄裁決條文前重驗一次 §J 自檢**（R-P200(c)）；
    §A 五條逐字抄入 RULINGS.md；§F 入 ANOMALIES.md
11. 上繳 `features/power/docs/upstream/53_anchor_segment.md`，更新 `docs/INDEX.md`

## I. 禁區

沿用 52 包 §I 全部條目，**惟 R-P338 之長度上限依 R-P342 撤回**；另增列：

- **不得為 `test_item` 首段設任何長度判準**（R-P342）
- **不得切句** —— 首段以錨點為單位（R-P343）
- **不得重組表格之欄值形式**（R-P344）
- **不得使用選句器**（R-P345）
- **不得寫回**（§E）

## J. 本包產生之新條文清單（自檢）

1. R-P342 R-P338 之硬上限撤回；首段長度不作閘
2. R-P343 首段之單位改為「該 TC 所直接驗證之錨點」
3. R-P344 R-P337(c) 之重組形式撤回；表格取連續片段
4. R-P345 錨點歸屬由執行層提案、分析層抽樣複核；選句器不用
5. R-P346 52 包五條之效力與「最後一個查證包」之界線

逐條確認：**五條**，皆以獨立頂層 fenced block 呈現於 §A，未夾於敘述中。
自檢：§A 頂層區塊數 = 5、§J 列數 = 5、§H 步驟 10 寫「五條」，三處一致。
**本包下放後分析層不再修改**（R-P200(a)）。

## K. 分析層自判：本包是否仍有該驗而未驗者

**有，三項。**

1. **R-P343 之「一個錨點 ≈ 一個需求陳述」為分析層之類比，未經實測。**
   其依據為 Comfort 之首段長度分布與其來源文件之標籤結構，
   **而 Power 之錨點文字長度分布未量** —— B0 即為此設。
   **若其分布與 Comfort 顯著不同，該類比不成立。**
2. **52 / 53 二包皆為修正分析層自身條文之內部矛盾，未產生任何 `test_item`。**
   R-P333 立於 51 包，至今三包未寫成一條 ——
   **其成因為分析層一次立三條互相衝突之要件**（逐字、不省略、長度上限）。
3. **`test_item` 之規則自 01 包至 51 包從未被檢視**，Pei 指出後方發現。
   **同類風險**：其餘交付欄位（`test_group` / `test_set` /
   `functional_safety` / `estimated_test_time` / `remarks`）
   是否亦有未落實之格式規則，**本包未查亦不查**（R-P341）——
   **登記為已知缺口，入驗證邊界。**
