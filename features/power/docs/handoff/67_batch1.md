# 67 — 66 回報覆核；`$VC_*$` 處置；刺激訊號選法；分批施作

下放包 | 分析層 → 執行層 | 往返 NN = 67

前置：`data/66_report.md` 已覆核，判定 **ACCEPT**。主體未做之二理由成立，停手正確。
A-PW366（顯示截斷作資料鍵）之根因分析與修正正確；`FUNC_STATE` 之註腳併入為重要發現；
四個 `PROXI <Param>` 不存在之拒收正確。寫回移至 68 包，續受 S6 阻斷。

## 0. 分析層之誤（自陳）

66 包 §1 表把 `$VC_VEH_BRAND$`、`$VC_VEH_LINE$`、`$VC_SpecialPKG$`、`$TBM_Present$` 寫成 `PROXI <Param>`，
**未查 PROXI `Format` 即指定參數類別**。規格原文即寫 `$…$`（訊號記法），分析層把「品牌組態」
之語意等同於 PROXI，是 R-P368(b) 所禁之語意跳接，且發生在裁決文本。登 A-PW。

## A. 本包裁決條文（逐字，抄入 RULINGS.md）

```
[R-P389] `$VC_*$` 與 `$TBM_Present$`：先逐字查 DBC `SG_`；查無者 R-13 保留原名並開 DR。
         （a）四名先以規格原名逐字查 forms 二本 DBC 之 `SG_`（R-P371(a) 型：`SG_` 逐字即段 3 查得，
              不以 LID 段 2 為必經），並查 `SR26 Default Settings`、`SR24 Market Configuration`
              之全部分頁（執行層 66 包丙案）
         （b）查得者依所在檔寫 `$MESSAGE.Signal$`（DBC）或 `PROXI <Param>`（PROXI）
         （c）查無者依 R-13 保留規格原名，**不加 `$`、不加 `PROXI`**，寫
              `Set VC_VEH_BRAND = <值> (DR-PW{n})`，並開一張 DR 問四名之實體載體
              （CAN 訊號／PROXI／DID）與其值域；上游回覆前 TC 以此寫，**不算 PENDING**
              （原名與值皆規格明載，缺的是載體，非資料）
         （d）近似名（`Brand_Configuration_2`、`Vehicle_Line_Configuration`）不採，
              執行層之拒收正確；記入 DR 附註供上游對照
         裁決者：分析層（Tier 2，訂正 66 包 §1 表之類別指定）。
```

```
[R-P390] 刺激訊號（規格未指名者）之選法：以 DBC `CM_` 為據，執行層列候選、分析層擇定。
         chime 觸發、ICS 觸控回應等，規格只述「有 chime」「ICS 可用」而未指名訊號，
         此為**測試刺激之設計選擇**，非規格名之識別，R-P368(b) 之「逐字」要件不適用，
         但仍不得憑訊號名語意擇定。
         （a）執行層對每一候選列：`MESSAGE.Signal`、`CM_` 註解全文、發送／接收節點、`VAL_`
         （b）分析層以 `CM_` 是否**明述**該功能（如「requests chime」「touch coordinate」）擇定；
              `CM_` 無明述者不選；全無者開 DR
         （c）本輪：chime 候選（含 `GW_B_5.Chime_Priority`）與 ICS 觸控候選
              （含 `DIS_CENTERSTACK.DCSD_DISP_STAT`）依 (a) 供料 `data/stimulus_candidates_67.md`
         裁決者：分析層（Tier 2）。
```

```
[R-P391] 66 包 §H 第 3 步主體分二批；`4941453` 雙列取聯集並登 DR 低優先。
         （a）第一批（本包即做）：不依賴 R-P389／R-P390／未解得項者 ——
              電源狀態類之 `-100/101/103/104`、`-119/120`、`-118`、`-123`、`-169`、`-218`、
              開機與復位類 `-004/-005/-049/-050`、音訊類 `-011/-012`、
              品牌視覺類中僅依 PROXI 已查得參數者 `-148`、`-149~152/192~195`、`-155/185`、
              設定類 `-216/217`、`-020/021`、`-143/156`、`-121`、`-017/018/019`；
              以上依 66 包 §1 表改寫，含所列拆分
         （b）第二批（待 R-P389 (a) 結果與 R-P390 擇定）：`-233~238`、`-228~230`、`-242/243`、
              `-249/250`、`-222/223/224` 之 `TBM_Present` 項、`-055`、`-202`、`-081`、
              `-125`／`-182`（繫 ENTER_SLEEP）、`-186/187/191`（`$Themed_Sound$`）、`-281`
         （c）`4941453` 之二列 `Full-Operation`／`Timed`：`FUNC_STATE` 取聯集之作法**採**；
              Source 欄差異登 DR（低優先）問二列是否對應不同 HU 變體；
              TC 之 Source 子項寫「TLM plays the audio active source」，不列舉音源
         （d）執行層 66 包對「67 條目／65 條相異 TC」之讀法採；本包起以相異 tc_id 計
         裁決者：分析層（Tier 2）。
```

## H. 作業指示

1. 抄 R-P389–R-P391；§0 登 A-PW
2. R-P389(a) 查詢，回報四名各自結果；查無者開 DR（先查現行最大號）
3. R-P390 供料 `stimulus_candidates_67.md` → 該項**停**
4. R-P391(a) 第一批改寫，驗 G245／G250／G251 於該批範圍
5. `4941453` Source 差異登 DR；PENDING 重算 → `pending_recount_67.tsv`
6. 上繳 `features/power/docs/upstream/67_batch1.md`，附 G255 表

## I. 禁區

沿用 66 包 §I，另增列：`$VC_*$` 不得寫成 `PROXI`（R-P389）；刺激訊號不得憑名擇定（R-P390(b)）；第二批不得先做（R-P391(b)）。

## J. 自檢

三條。C(3,2)：R-P389×R-P391 — 第二批之界定即依 R-P389 未決項，一致；R-P390×R-P391 — 同；R-P389×R-P390 — 前者為識別、後者為刺激設計，範圍互斥，無交集。
對既有 canon：R-P389 對 R-13 — (c) 合；對 R-P368(b) — (d) 合；對 R-P371(a) — (a) 引用其 `SG_` 逐字判準，合；對 S6 — (c) 明示不算 PENDING，理由載明（缺載體非缺資料），**此為對 S6 適用範圍之解釋**，Pei 得否決。R-P390 對 R-P368(b) — 明示不適用之理由；對 §8.4.1 — `CM_` 為規格外之機讀依據，非造值，合。R-P391 對 R-P374(a) — 第一批非 B5，B5 續凍，合；對 §8.4.1 — (c) 不擇一，合。無違反。

本包數字（R-P379(a)）：第一批 tc_id 數由執行層依 (a) 清單計，本包不預寫。

## K. 待 Pei

無阻斷項。R-P389(c)「缺載體不算 PENDING」是我對 S6 範圍的解釋，你不認可就撤，該類回 PENDING。
