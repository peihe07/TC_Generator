# 19 — 第二批覆核與錨點完整性

下放包 | 分析層 → 執行層 | 往返 NN = 19

前置：`docs/upstream/18_source_fidelity.md` 已覆核，判定 **ACCEPT**。
第二批 8 leaf / 26 條完成，累計 11 leaf / 43 條。
G94 11/11 逐字相符；G98 盲測抓到 1 項事前未知缺口。

**R-P116（`SWE-PM-089` 佔不佔列）Pei 仍未裁定**，本包不代填、不實作。

沿用不變之節：`§C 抽取規格` 同 02 包；`§G DATA_REQUESTS` 同 18 包；
`§I 禁區` 同 18 包 §I，另增列見本包 §I。

## A. 本包裁決條文（逐字，抄入 RULINGS.md）

```
[R-P133] G73 對末步 ER 之比對，須先剝除 R-P101 所強制之驗證意圖子句。
         A-PW92：第二批 26 條中 12 條觸發 R-P96(a)，全為狀態回讀，
         三條 overlap 達 1.00。
         成因非任一閘門有瑕疵，而係兩條裁決彼此拉扯 ——
         R-P101 要求末步指名所檢查者，而 ER 述及同一標的時
         其實詞必然多已見於該步驟。
         此重疊為 R-P101 之產物，非撰寫者之複述。

         處置：G73 比對末步 ER 時，先自該 procedure 步驟剝除
         `to check that ...` / `and check that ...` / `to verify ...`
         子句，再計 overlap。
         此非放寬判準 —— 剝除者恰為本專案自己強制加入之文字。
         非末步之 ER 行不適用本條，仍以完整步驟比對。

         剝除後若某條仍觸發，即為真複述，依 R-P76 分流人工裁決。
         裁決者 Pei，逐字依據：「A 順便處理上繳」。
```

```
[R-P134] 錨點清單完整性補閘（G99）。
         18 §七(乙)7：G94 比對 `source_clause` 與執行層所填之
         `source_anchor`，**若某個該被引用之錨點根本未被列進
         `source_anchor`，G94 一樣會全綠**。
         錨點清單本身之完整性來自 layer3 之 `item_ids` 欄，
         而 layer3 之正確性係 03–06 包所驗，非本閘所驗。

         G99：逐 leaf 比對其 `source_anchor` 集合與
         `layer3_full.tsv` 所載該 leaf 之 `item_ids` 集合。
         不相等即 FAIL，並列出兩側之差集。
         以刻意刪去一個錨點之 fixture 證明其會失敗。

         G94 驗「抄對了」，G99 驗「抄全了該抄的」——
         二者缺一，反向涵蓋之地基仍不完整。
         裁決者 Pei，逐字依據：「A 順便處理上繳」。
```

```
[R-P135] `SWE-PM-038` 三組成對錨點之屬性查證。
         18 §七(甲)2：`4941727`/`4941728`、`4941729`/`4941730`、
         `4941735`/`4941736` 三組各為「不含／含 RemStartFail 處置」
         之成對錨點，執行層各自成條，無第二人覆核；
         若三組實為同一行為在不同車型變體下之登載，即為三條多餘之 TC。

         查證方法：CFTS 本文之需求錨點標頭載有
         `[ECU:...]` / `[Market:...]` / `[Model Year:...]` /
         `[Radio:...]` / `[EE Architecture:...]` 等屬性。
         逐對比對該等屬性：
         （a）屬性相同 → 二者為同一適用範圍下之不同行為，各自成條正確
         （b）屬性相異 → 二者為變體登載，須停並上繳，由 Pei 裁定是否合併

         **不得自行合併或拆分** —— 該判斷影響 TC 數與追溯。
         裁決者 Pei，逐字依據：「A 順便處理上繳」。
```

```
[R-P136] 逐字相同之跨章節錨點須以屬性佐證其為重複登載。
         18 §七(甲)5：`4941814`/`4941815`/`4941817` 與
         `4941692`/`4941693`/`4941695` 逐字相同，分屬
         §1.8.1.1.1 與 §1.6.2.1.17 兩章節；
         執行層判為「重複登載」而未另拆條，**自陳無證據**。

         同 R-P135 之方法：比對兩組錨點標頭之屬性。
         屬性全同 → 重複登載成立，維持現行處置；
         屬性相異 → 停並上繳。
         裁決者 Pei，逐字依據：「A 順便處理上繳」。
```

```
[R-P137] `SWE-PM-063` 之單條處置須列出其所委由之 leaf 與錨點。
         18 §七(甲)3：該 leaf 僅寫一條，理由為
         「其細部邏輯由他 leaf 承載」，而該定義係執行層自下。
         §8.2.1 要求：當行為委由 sibling Req 承擔時，
         `reasoning` 須明確列出承擔該行為之 sibling Req ID。
         現行 `reasoning` 未列。

         補列：逐項指出被委出之行為、承擔之 leaf ID、
         承擔之具體 TC id；無法指出者即非「已由他 leaf 涵蓋」，
         而是未涵蓋，須依 R-P118(d) 重裁。
         裁決者 Pei，逐字依據：「A 順便處理上繳」。
```

```
[R-P138] 殘差詞之「措詞差異」判定須抽樣覆核。
         18 §七(甲)4：信噪比 0.7% 係以「執行層判其為措詞差異」算得；
         145 個殘差中 123 個由同一人判為無妨，判別者與被判別之工作同源。
         分桶降低回報成本，未降低誤判風險。

         處置：自第二批之 123 個「措詞差異」桶中隨機抽 20 個
         （固定亂數種子，種子須載明），逐一列出：
         殘差詞、所屬行為項、最佳對應 TC、判為措詞差異之理由。
         由分析層覆核。抽樣率約 16%。
         裁決者 Pei，逐字依據：「A 順便處理上繳」。
```

```
[R-P139] G95 之凍結窗格類別登記為永久未實測。
         18 §七(甲)6：本 workbook 無 `<pane>`，該類別
         **在本 feature 之整個生命週期內都不會有證據**。
         標「未實測」而非 PASS 之處置正確，不得改標。
         若日後客戶範本加入凍結窗格，該支仍為未驗狀態，
         須於當時補測，不得以「歷來 PASS」為由略過。
         裁決者 Pei，逐字依據：「A 順便處理上繳」。
```

```
[R-P140] 第二批 26 條全文送分析層覆核。
         18 §七(甲)1：26 條全數出自執行層一人，無任一條經他人讀過；
         分層取樣 18/26 意味 **8 條連上繳包都未出現全文**。
         首批之品質係分析層讀畢十七條、歷三輪缺陷方逐步得到。

         處置：本包上繳附 `batch_002_timeout_settings.json` 之
         **全部 26 條**，以 JSON 原文分段附上（非散文報告），
         使分析層得以最低讀取成本取得全文。
         在分析層覆核前，第二批狀態為**暫定**。
         裁決者 Pei，逐字依據：「A 順便處理上繳」。
```

```
[R-P141] SWE-PM-089 於工作簿中保留一列空白（R-P116 之裁定，甲案）。
         該列僅填 `req_id = SWE-PM-089`，其餘欄一律留白。
         B 欄序號照常計入該列。

         理由：DR-PW1 目前 live 且 Urgency 為 High。
         待決項若於交付物中完全不可見，即僅存在於
         `DATA_REQUESTS.md`，而客戶不讀該檔。
         保留空白列使「此項尚未解決」於交付物本身可見。
         此與 R-P121 對 `015` 之處置同一邏輯 ——
         待決狀態須於工作簿內可見，不得僅存於 DR 檔。

         素材之限度須一併記明：Comfort / Home / Privacy 三份
         已交付件共 693 列，「僅填 req_id 其餘全空」者為 0；
         該素材僅證「已交付件中無此形態」，
         **不證「此形態不被接受」**（18 §四之限制聲明）。
         本裁決係基於上開理由，非基於先例。

         DR-PW1 獲解後，該列依當時之裁決填寫或移除，
         不得以本條為由保留空白。
         裁決者 Pei，逐字依據：「甲」。
```

## B. 本包須產出

### B1. 第二批 26 條全文（R-P140）—— 置於上繳包最前

以 `batch_002_timeout_settings.json` 之原文分段附上，每段不逾 8 條。
不得以散文摘要代替。首批 17 條不重附。

### B2. G99 錨點清單完整性（R-P134）

`scripts/verify_anchor_set.py`；逐 leaf 比對 `source_anchor` 與
`layer3_full.tsv` 之 `item_ids`；輸出差集；刻意刪錨點之 fixture 證明會 FAIL。
**11 leaf 有任一不相等即停並上繳。**

### B3. 成對／重複錨點之屬性查證（R-P135 / R-P136）

自 CFTS 本文抽出各該錨點標頭之全部屬性，逐對併表比對。
輸出至 `data/b3_anchor_attributes.md`。
**不得自行合併或拆分任何 TC。**

### B4. `SWE-PM-063` 之委出清單（R-P137）

逐項列出被委出之行為、承擔之 leaf ID 與 TC id；
無法指出者依 R-P118(d) 重裁為真缺口。

### B5. 殘差詞抽樣（R-P138）

固定種子隨機抽 20 個「措詞差異」，逐一列出四欄。
種子值須載明於報告與程式碼。

### B6. G73 剝除子句（R-P133）

實作剝除；回報剝除前後之觸發數；
剝除後仍觸發者依 R-P76 分流並逐項裁決。

## D. 閃點

G0 為前置閘。G0–G98 沿用（G17 已移除），期望值不變。

| # | 項目 | 期望值 |
|---|---|---|
| G99 | 錨點清單完整性（R-P134） | 11 leaf 之 `source_anchor` 與 `item_ids` 全數相等；刻意刪除 → FAIL |
| G100 | 成對錨點屬性（R-P135 / R-P136） | 【實測填入】各對之屬性相同或相異 |
| G101 | G73 剝除後觸發數（R-P133） | 【實測填入】剝除前 12；剝除後之數與逐項裁決 |
| G102 | 殘差詞抽樣（R-P138） | 20 個，種子已載明 |
| G70 | lint 全閘 | 全 PASS；leaf 11；TC 43（除非 R-P135/136/137 導致增減） |

G99 之驗證條件同 G33：須確認其在該階段確實可能失敗。

## E. framework

§E 已定版（R-P35），本包不動。
**第三批不啟動** —— 待分析層完成第二批 26 條之覆核（R-P140）。

## F. Anomaly 異動

開新號前先查現行最大號並回報。

- 新增：G94 不驗錨點清單完整性，反向涵蓋地基仍缺一半（R-P134）
- 新增：`SWE-PM-038` 三組成對錨點之拆法無第二人覆核（R-P135）
- 新增：跨章節逐字相同錨點判為重複登載而無證據（R-P136）
- 新增：`SWE-PM-063` 之委出未列 sibling Req ID，違 §8.2.1（R-P137）
- 新增：信噪比 0.7% 之分母由同一判別者產生（R-P138）
- A-PW92 → 依 R-P133 處置後更新
- G95 凍結窗格類別 → 依 R-P139 標為永久未實測

## H. 作業指示

1. G0 前置閘
2. 查現行最大號並回報
3. 輸出 B1 第二批 26 條全文 —— 置於最前
4. 實作 B2 G99 —— 不相等即停
5. 產出 B3 屬性查證 —— 不得自行合併或拆分
6. 產出 B4 `SWE-PM-063` 委出清單
7. 產出 B5 殘差詞抽樣
8. 實作 B6 G73 剝除，驗 G101
9. 以 §D 全表自驗
10. §A 九條裁決逐字抄入 RULINGS.md；§F 入 ANOMALIES.md
11. 上繳 `features/power/docs/upstream/19_batch2_review.md`，更新 `docs/INDEX.md`

## I. 禁區

沿用 18 包 §I 全部條目，另增列：

- **不得啟動第三批**（R-P140 覆核完成前）
- **不得自行合併或拆分 `SWE-PM-038` / 跨章節重複之任何 TC**（R-P135 / R-P136）
- **不得以「已由他 leaf 涵蓋」結案而未列出 sibling Req ID**（R-P137）
- **不得改標 G95 凍結窗格類別為 PASS**（R-P139）
- **G73 之剝除僅限本專案自己強制加入之驗證意圖子句，不得擴大**（R-P133）

## J. 本包產生之新條文清單（自檢）

1. R-P133 G73 對末步 ER 先剝除 R-P101 所強制之子句
2. R-P134 錨點清單完整性補閘 G99
3. R-P135 `SWE-PM-038` 三組成對錨點之屬性查證
4. R-P136 跨章節逐字相同錨點須以屬性佐證
5. R-P137 `SWE-PM-063` 之委出須列 sibling Req ID
6. R-P138 殘差詞「措詞差異」須抽樣覆核
7. R-P139 G95 凍結窗格類別登記為永久未實測
8. R-P140 第二批 26 條全文送分析層覆核
9. R-P141 `SWE-PM-089` 保留一列空白（R-P116 之裁定，甲案）

逐條確認：**九條**，皆以獨立 fenced block 呈現於 §A，未夾於敘述中。
自檢：§A 區塊數 = 9、§J 列數 = 9、§H 步驟 10 寫「九條」，三處一致。
本次已逐一數過 §A 之 fenced block。

## K. 分析層自判：本包是否仍有該驗而未驗者

**有，三項。**

1. **分析層對第二批之覆核尚未開始。** 本包只是把全文要過來，
   真正的技術覆核在 20 包。首批的三輪缺陷都是在讀全文時才出現的。
2. **R-P135 / R-P136 之屬性比對，分析層無法獨立複驗。**
   沙箱之 Project 附件已換為 Comfort 素材（A-PW44），
   分析層讀不到 CFTS 本文原始檔，只能接受執行層之回報。
3. **`Ignition Pre Off` 之補測（`043`）分析層未看過。**
   它是透鏡 3 盲測抓出之唯一真缺口，其補測品質未經覆核。
