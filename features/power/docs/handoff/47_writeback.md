# 47 — 殘留 6 條人工指派與寫回

下放包 | 分析層 → 執行層 | 往返 NN = 47

前置：46 包上繳已覆核，判定 **ACCEPT**。
`…-172` 已依 §6.1 子項化；歷史臨時號對照表已建（涵蓋 264 / 266）；
dry-run 261 列十項全 PASS。

**分析層已完成 R-P305 之複核** —— 6 條全數讀畢，**三組皆有可辨之軸**。
**惟不再要求擴充謂詞**，見 R-P308。

**Pei 已就寫回為明示授權**（R-P309）。

沿用不變之節：`§C 抽取規格` 同 02 包；`§I 禁區` 同 46 包 §I，另增列見本包 §I。

## A. 本包裁決條文（逐字，抄入 RULINGS.md）

```
[R-P308] 殘留 6 條人工指派；謂詞之語義極限登記，不再擴充。
         分析層讀畢 6 條，**三組皆有可辨之軸**：

         （a）`…-001` / `…-004` → **`timing`**
              相異行為 `Start the suspend-resume boot sequence` 對
              `… and let it progress …`；
              二條觀察於**開機序列之不同時點**
              （`SplashScreen_Time` 對 `StandardScreen_Time`），
              正合 R-P298 訂正後之定義
              「事件發生之相對位置不同」。

         （b）`…-163` / `…-164` → **`trigger_state`**
              相異行為 `on the phone main screen` 對
              `on the phone projection call UI`；
              **顯示中之畫面為系統狀態**。

         （c）`…-203` / `…-204` → **`trigger_state`**
              相異行為 `Bring the HU to Timed mode` 對
              `to Full Operation mode`；**電源模式為系統狀態**。

         **執行層之疑點成立且揭露謂詞之極限**（A-PW300）：
         其形態近於系統狀態，**惟為 `test_procedure` 之施加動作
         而非前提之狀態陳述** ——
         **軸為語義問題，非欄位問題**；
         謂詞比對之為**措辭形態**（`is in X state`）而非**語義**
         （該差異是否為一個狀態），而措辭形態無窮盡。

         **裁定：不再擴充謂詞，改以人工指派此 6 條。**
         第三輪擴充僅會再漏第四種措辭 ——
         其已有二度實證（R-P299 之情境型、本次之施加動作型）。

         **登記為永久限制並寫入驗證邊界**：
         「`distinguishing_axis` 之判定以措辭形態為代理判準，
          其無法窮盡語義；本 feature 之 260 條中 6 條由人工指派，
          其餘由謂詞判定，**謂詞之語義覆蓋率不可知**。」

         處置：6 條依 (a)(b)(c) 指派；**不改謂詞**；
         各該條之 `reasoning` 註明其為人工指派及其依據。
         裁決者 Pei，逐字依據：「上角」。
```

```
[R-P309] 寫回授權：Pei 已明示，其效力範圍限於現行 260 條。
         Pei 逐字：「授權啊」。

         **其效力於 R-P306 之條件 1 成就時發生** ——
         該條件為「分析層對 6 條之複核結果」，
         現已由 R-P308 完成（人工指派，非維持無對應）。

         **效力範圍**：本授權所及者為**授權當時之 260 條內容**
         加上 R-P308 之 6 條 `axis` 指派。
         **若其後內容再有實質變動**（TC 增減、四個長欄位之改寫、
         `req_id` 或 Test Set 之變動），
         **本授權不及於變動後之版本，須重新取得** ——
         因屆時所寫者已非 Pei 所點頭之內容。

         `axis` / `delta` **不寫入工作簿**（§4.6 為 JSON 層之欄位），
         故 R-P308 之指派不影響交付內容，仍在本授權範圍內。
         裁決者 Pei，逐字依據：「授權啊」。
```

```
[R-P310] 寫回之執行程序。
         **（一）前置確認**（寫入前逐項回報，任一不符即停）：
         （a）R-P308 之 6 條已指派，`axis` 之「無對應」為 **0**
         （b）全量 dry-run 十項全數 PASS（261 列）
         （c）目標檔之 SHA256 與 `inputs/` 之原始檔相符
              —— 確認寫入對象未被他人改動
         （d）依 R-P228 備份至 `sandbox/`，回報備份路徑與檔數

         **（二）寫入**：
         （a）路徑為 `xlsx_surgical.py`（唯一授權之寫回路徑，R16 / R-G3）
         （b）**不得對 `inputs/` 之原始檔寫入** ——
              寫入對象為交付副本；其產生方式與存放路徑須先回報
         （c）最終 `tc_id` 依 R-P113(c) 於此刻統一指派，
              依 `(SWE-PM ID, split_index)` 序（R-P115）
         （d）`SWE-PM-089` 之留白列依 R-P141 保留，僅填 `req_id`

         **（三）寫入後驗證**（逐項回報）：
         （a）G66 / G71 / G72 / G95 全量實測
         （b）DV（含 x14）、合併儲存格、條件式格式、分頁清單、
              欄寬、凍結窗格之寫入前後逐項比對
         （c）XML 層 diff：列出全部相異之 part
         （d）B 欄序號與列序逐列驗證
         （e）「歷史臨時號 → 最終號」對照表補齊最終號欄

         **（四）不得為之**：
         不得送達客戶目錄、不得執行任何 git 操作、
         不得以 openpyxl `save()` 為之。
         **送達為另一動作，須 Pei 另行授權。**
         裁決者 Pei，逐字依據：「授權啊」。
```

```
[R-P311] 46 包所引之號為舊號：實測確認，追認執行層之依實指處置。
         R-P303 所引之 `…-175` / `…-176` 為 **44 包合併前**之號；
         現行號下該二號指向另二條 TC，其實指者為合併後之 **`…-172`**。

         **執行層依實指處置而非依字面號處置，正確**（A-PW297）。

         分析層於 46 §K 第 2 項已預載此風險，**實測確認其發生** ——
         即該自判為有效之預警，非事後補述。

         **併記對照表之限度**（A-PW299）：
         `data/tcid_history_46.md` 以 `(req_id, tc_title)` 為鍵，
         **涵蓋 264 / 266**；缺口 2 條係 `tc_title` 於 27 包後被改寫
         致鍵斷裂，**非遺漏**。
         寫回時之最終號欄一併補齊該 2 條，其對照以人工確認。
         裁決者 Pei，逐字依據：「上角」。
```

```
[R-P312] 「其餘三對非因巧合而免」：修正分析層之推想。
         R-P303(b) 慮「三對之首行恰為共同斷言，掩蓋衝突之普遍性」。
         46 §三(b) 實測：其餘三對之 ER **本即各對應一步**，
         **非因巧合而免** —— 該慮於本批僅及於一對。

         **分析層之推想不成立**（其源為 44 包自陳之措辭，
         而分析層未查其實際 ER 結構即引為普遍性之證據）。

         **惟該慮於通則層仍成立**：ER 行數與步驟數是否相等，
         取決於被合併二條之 ER 結構，而該結構因條而異。
         R-P303(c) 之通則因此仍有其必要，**其立條理由則須訂正** ——
         非「三對之巧合」，而是「合併之結構後果因條而異」。

         R-P303 依 R-P36 原文不改，於其下加註指向本條。
         裁決者 Pei，逐字依據：「上角」。
```

## B. 本包須產出

### B1. 6 條人工指派（R-P308）—— **寫回前置**

依 (a)(b)(c) 指派；`reasoning` 註明人工指派及依據；
**不改謂詞**；回報 `axis` 之「無對應」為 0。

### B2. 驗證邊界更新（R-P308）

增列謂詞之語義極限與 6 條人工指派之事實。

### B3. R-P303 加註（R-P312）

依 R-P36 原文不改；雜湊佐證。

### B4. 寫回（R-P310）—— **本包主產出**

依 R-P310 之四段程序；每段逐項回報。

### B5. 最終號對照表補齊（R-P310(三)(e) / R-P311）

歷史臨時號 → 最終號；2 條鍵斷裂者以人工確認補齊。

## D. 閃點

G0 為前置閘。G0–G216 沿用（G17 已移除；G109 不實作），期望值不變。

| # | 項目 | 期望值 |
|---|---|---|
| G217 | 6 條人工指派（R-P308） | `axis` 之「無對應」= 0；6 條之 `reasoning` 皆註明人工指派 |
| G218 | 寫回前置確認（R-P310(一)） | 四項逐項回報；任一不符即停 |
| G219 | 寫入後驗證（R-P310(三)） | 五項逐項回報；DV / merges / cf / pane / 欄寬皆未變；B 欄與列序逐列相符 |
| G220 | 最終號對照表（R-P311） | 涵蓋 266 條（含 2 條人工補齊）；最終號欄已補 |
| G70 | lint 全閘 | 指派後仍全 PASS |

## E. framework

§E 已定版（R-P35），本包不動。
**寫回於本包執行；送達為另一動作，須 Pei 另行授權。**

## F. Anomaly 異動

開新號前先查現行最大號並回報（R-P147）。

- 新增：謂詞以措辭形態為代理判準，無法窮盡語義（R-P308）
- 新增：分析層之「三對巧合」推想不成立（R-P312）
- A-PW297 → 依 R-P311 追認其處置
- A-PW299 → 依 R-P311 記其限度
- A-PW300 → 依 R-P308 處置，其疑點成立
- R-P303 → 依 R-P312 加註

## G. DATA_REQUESTS

DR-PW9 ~ DR-PW15 沿用。無新增。
**該 11 個 leaf 不進本次交付**，其狀態於交付說明中載明。

## H. 作業指示

1. G0 前置閘
2. 查現行最大號並回報
3. B1 6 條人工指派，驗 G217
4. B3 R-P303 加註；B2 驗證邊界更新
5. **B4 寫回** —— 依 R-P310 四段程序，驗 G218 / G219
6. B5 最終號對照表補齊，驗 G220
7. 以 §D 全表自驗
8. **抄錄裁決條文前重驗一次 §J 自檢**（R-P200(c)）；
   §A 五條逐字抄入 RULINGS.md；§F 入 ANOMALIES.md
9. 上繳 `features/power/docs/upstream/47_writeback.md`，更新 `docs/INDEX.md`

## I. 禁區

沿用 46 包 §I 全部條目，另增列：

- **不得擴充 `axis` 之任何謂詞**（R-P308）
- **不得對 `inputs/` 之原始檔寫入**（R-P310(二)(b)）
- **不得送達客戶目錄**（R-P310(四)）
- **不得執行任何 git 操作**
- **前置四項任一不符即停，不得續行寫入**（R-P310(一)）

## J. 本包產生之新條文清單（自檢）

1. R-P308 殘留 6 條人工指派；謂詞之語義極限登記
2. R-P309 寫回授權；效力範圍限於現行 260 條
3. R-P310 寫回之執行程序（四段）
4. R-P311 46 包所引為舊號：追認實指處置；對照表限度
5. R-P312 「三對非因巧合而免」：修正分析層之推想

逐條確認：**五條**，皆以獨立 fenced block 呈現於 §A，未夾於敘述中。
自檢：§A 區塊數 = 5、§J 列數 = 5、§H 步驟 8 寫「五條」，三處一致。
本次已逐一數過 §A 之 fenced block。
**本包下放後分析層不再修改**（R-P200(a)）。

## K. 分析層自判：本包是否仍有該驗而未驗者

**有，三項。**

1. **R-P308 之人工指派由分析層一人所為，無第二方複核。**
   其與歷次所批評之「自判自報」同構 ——
   惟其僅 6 條且各附逐字依據，**其代價低於再一輪謂詞擴充**。
   取捨記明。
2. **寫回為不可逆，而其驗證僅及於「機制」與「結構」。**
   G219 驗 DV / merges / 列序皆未變，
   **不驗所寫之 260 條內容是否正確** ——
   內容之正確性由歷 47 包之各次複核承擔，
   而該等複核之涵蓋率已逐項記於驗證邊界。
3. **本包所引之 `…-001` / `…-004` / `…-163` / `…-164` /
   `…-203` / `…-204` 六號，其現行有效性未查。**
   46 包已實測分析層引用舊號之情形確實發生（R-P311）。
   **執行層須先以 `(req_id, tc_title)` 核對該六號之實指，
   不符者依實指處置並回報。**
