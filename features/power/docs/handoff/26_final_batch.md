# 26 — 裁定積案、數字標註機制與末批

下放包 | 分析層 → 執行層 | 往返 NN = 26

前置：25 包上繳已覆核，判定 **ACCEPT**。
G121 五個 Test Set 全數相符（63/24/16/8/3 ＋ `SWE-PM-089` = 115）`[引用：25 §1.1]`；
第五批 29 leaf / 66 條 `[引用：25 §七 G123]`；
**Phase 4 累計 87 leaf / 223 條** `[引用：25 §七]`。

**分析層錯誤第六次** `[引用：25 §1.3、A-PW134]`：
R-P178 之推導（6）實測為 10，差額 4 為 `053`–`056`（屬 Startup Display 而非 Power State）；
其根因即 R-P177 所禁之「以 ID 區間推定 Test Set 成員資格」，
且發生於立下 R-P177 之同一包內。

沿用不變之節：`§C 抽取規格` 同 02 包；`§I 禁區` 同 25 包 §I，另增列見本包 §I。

## A. 本包裁決條文（逐字，抄入 RULINGS.md）

```
[R-P183] 下放包之數字須標註來源型別（機制，非勸誡）。
         分析層之未經查證斷言已達六次，形態同一：
         A-PW30「最大」、R-P157「應可機械檢查」、R-P161「重複七次」、
         R-P172 之排序歸因、R-P173(丙) 之前提、R-P178 之推導。
         R-P64 立於 08 包而於分析層身上失效六次，
         每次皆由執行層抓出；歷來處置僅為逐次訂正，未立機制。

         機制：**下放包內任何數字、比例、計數或推導結論，
         其後須以方括號標註來源型別**：
           `[實測]` —— 附量測條件（掃描範圍、欄位、大小寫、詞界）
           `[推導]` —— 附推導式與其所依據之各項數字及其來源
           `[引用]` —— 附出處（包次、節次、檔名）
         三者皆不適用者不得書寫該數字。

         **執行層於 §J 自檢時一併驗證**：
         下放包內出現而未標註之數字 → **停並回報**，不得逕行採信。
         標註為 `[推導]` 者，執行層須實測其結論並回報相符與否。

         此標註僅適用於下放包之敘述性內容，
         不適用於裁決條文內引述既有紀錄之數字（該等已有出處），
         亦不適用於編號（R-P、A-PW、G、DR-PW、章節號、leaf ID）。
         裁決者 Pei，逐字依據：「上繳」。
```

```
[R-P184] `SWE-PM-025` 裁定：維持八條，R-P167 結案。
         25 §三之逐字並列顯示：
         三對之中僅第一對含觸發訊號 ——
         `Front_Panel_OnOff.Req` 對 `CLIMATIC_PANEL.Radio_Btn0`；
         第二、三對不含觸發訊號且二側一字不差。

         中性證據：二訊號於全部錨點中從未共現，
         章節分布相同（§1.6.2.1.15、§1.6.7.1）。

         裁定依據 §8.3 之壓力測試：
         二者為不同硬體來源之相異訊號（前面板開關 vs 空調面板按鍵），
         其後續行為雖逐字相同，然**故障可只存在於其中一條路徑** ——
         僅測一側時，另一側之失效無法被判讀。
         依 §5.7「不同 trigger 即拆分」，八條成立。

         第二、三對之逐字相同，係二觸發共用之下游行為，
         不構成合併理由；其重複已於各條之 `split_reason` 可見。
         裁決者 Pei，逐字依據：「上繳」（回應 25 §三）。
```

```
[R-P185] `SWE-PM-112` 之排除追認。
         25 §十一第 1 項：執行層以保守解排除該 leaf，
         而 `DATA_REQUESTS.md` 中 DR-PW9 記為
         「不阻斷已產出之 leaf」，未明言阻斷 `SWE-PM-112`。

         追認其排除。依據為 R-P148 之逐字：
         「Urgency High —— 其影響 `SWE-PM-112` 之 TC 可否撰寫」——
         **該阻斷意旨已載於立條理由，DR 條目之措辭未完整承接**。
         責在分析層之 DR 撰寫，非執行層之判讀。

         處置：修訂 DR-PW9 之阻斷欄，明載
         「阻斷 `SWE-PM-112` 之 TC 撰寫」。
         裁決者 Pei，逐字依據：「上繳」（回應 25 §十一第 1 項）。
```

```
[R-P186] 五對 leaf 共用完全相同錨點之處置追認：逐一產出。
         25 §十一第 2 項：執行層選擇為五對各自產出而非委由對造承擔，
         其理由為「缺一條 TC 比多一條危險」（R-P121 之類比）。

         追認，但依據改為條文而非類比：
         §8.2.2 明訂「Inverse direction is forbidden：
         TC authors must not merge multiple RD sub-ids into one TC.
         RD-level consolidation belongs to the RD authors」。
         二個 leaf 引用完全相同之錨點集合，
         係 RD 將同一行為分解為二個需求單位；
         TC 作者不得代其合併，故逐一產出為唯一合規之處置。

         其重複追溯係 RD 之決定，非本專案所生 ——
         各該 TC 之 `reasoning` 須註明其共用錨點之對造 leaf ID，
         使覆核者見其重複之來源。
         DR-PW12 維持，由 RD 決定是否合併需求單位。
         裁決者 Pei，逐字依據：「上繳」（回應 25 §十一第 2 項）。
```

```
[R-P187] 量測腳本之自我修改須回報 bug 之影響方向。
         25 §十一第 3 項：執行層自陳
         「『這是 bug 不是遷就』仍然是被修改的人自己說的」，
         並指出歷次之 bug 方向皆對執行層不利
         （虛構缺口、虛增缺口），惟此為事後觀察非機制保證。

         處置：往後任何對量測腳本之自我修改，
         上繳時須一併回報：
         （a）修正前後之數值（依 R-P182 並陳各版）
         （b）該 bug 若未修正，其影響方向 ——
              對執行層有利（少報缺口／少報觸發）
              或不利（多報缺口／多報觸發）
         （c）修正之結構性依據（為何該分類客觀錯誤）

         方向為「有利」者，**須停並上繳，由分析層裁定是否採信**；
         方向為「不利」者，執行層得自行修正並回報。
         此非推定執行層之動機，而係使該資訊可被檢驗 ——
         事後觀察若不成為常規回報項，下次即無從比對。
         裁決者 Pei，逐字依據：「上繳」（回應 25 §十一第 3 項）。
```

```
[R-P188] `SWE-PM-093` 之屬性差異裁定暫緩，取資料。
         A-PW138：其二錨點逐字相同而屬性相異五欄，
         屬 R-P136 之形態（跨章節逐字相同須以屬性佐證）。

         上繳二錨點之全部屬性逐欄原值，
         依 R-P173(a) 以集合正規化後比對（去空白、統一大小寫、排序），
         列出正規化後仍相異之欄與其集合差集。
         不得自行合併或拆分。裁定於 27 包。
         裁決者 Pei，逐字依據：「上繳」。
```

```
[R-P189] 末批範圍：G121 對帳表所列之全部未產出且未阻斷者。
         依 R-P177(b)，自 `data/leaf_batch_reconciliation.tsv`
         取「未產出」且無阻斷 DR 者之 leaf ID 全集，
         逐一列出，不以區間表述。
         阻斷者逐一列出其 DR 編號並排除。

         本批為 Phase 4 之末批 —— 其完成後，
         全部未阻斷之 leaf 應已產出 TC。
         上繳須附最終對帳：已產出 ＋ 阻斷 ＋ `SWE-PM-089` = 115，
         不符即停。
         裁決者 Pei，逐字依據：「上繳」。
```

```
[R-P190] **批次四、五之 leaf `reasoning` 全數缺漏，阻斷寫回。**
         分析層於積欠覆核中實讀 `generated/` 之五個批次檔：
         批次一、二、三之 leaf `reasoning` 皆具完整 §10.4 稽核軌跡；
         **批次四、五之 leaf `reasoning` 皆為空字串 `""`**
         `[實測：直讀 batch_003 / 004 / 005 之 `leaves` 陣列，
           `SWE-PM-033` / `034` / `035` / `036` / `066` / `067` 等皆 `""`]`。

         TC 層之 `reasoning_note` 仍在，惟其為個案註記
         （例：共用錨點之呈請），**非 §10.4 所要之稽核軌跡**。

         三項後果：
         （a）§10.4 明文要求該欄，其用途為
              「reviewers can align on the AI's interpretation
              without re-reading the source」
         （b）該等 leaf **無任何「刻意略過了什麼、依據哪一條」之紀錄**
              —— §8.2.1 之委出與 §8.4.2 之略過全無跡可循
         （c）**R-P159 之覆核方式對該等 leaf 失效** ——
              該條裁定「全讀 `source_clause` 與 `reasoning`」，
              而分析層本次所獲之 T13 / T14 / T16 / T18 / T19
              **全數來自 `reasoning`**。

         **現行閃點皆無法發現此事** —— G19 所量者為 037 之欄位，
         非產出 JSON 之欄位。

         處置：
         （i）**阻斷寫回** —— 54 leaf `[推導：第四批 25 ＋ 第五批 29，
              依 24 §六與 25 §七 G123；實測為準]`
              無稽核軌跡而交付，即交出無法覆核之物，
              而 ASPICE 所要者正為可追溯
         （ii）逐 leaf 補寫 `reasoning`，依 §10.4 四項順序；
              **不得以模板套語填充** —— 其價值在於
              「刻意略過」一項之具體內容
         （iii）**補設閃點 G129** —— 產出 JSON 之每一 leaf
              `reasoning` 須非空且長度不低於一定門檻；
              以刻意置空之 fixture 證明其確實會 FAIL
         裁決者 Pei，逐字依據：「寫在 26 包」。
```

```
[R-P191] 補測後 `reasoning` 未同步更新。
         `SWE-PM-011` 之 `reasoning` 仍載
         「刻意略過：`4941376` 所述之短按與長按等價」，
         而 22 包盲測已將「VR 長按」列為事前未知缺口並補測；
         `SWE-PM-030` 之 `reasoning` 仍載「單一行為」，
         而 25 包已補其 OR 右支。

         **補了 TC，reasoning 沒改** —— 覆核者讀 `reasoning`
         會以為該分支被刻意排除。

         處置：
         （a）逐一掃查全部批次，凡因反向涵蓋或 G113 而補測者，
              其 leaf `reasoning` 須同步更新，明載該分支已補
         （b）往後任何補測，**須於同一步更新 `reasoning`**，
              不得延至其他步驟（比照 R-P107 之全欄掃描）
         裁決者 Pei，逐字依據：「寫在 26 包」。
```

```
[R-P192] G113 不涵蓋逗號列舉，須量其規模。
         `SWE-PM-013` 之 `4941391` 列舉七個 Ignition Working Conditions
         （`Ignition On, Ignition Pre_Start, Ignition Start,
           Ignition Cranking, Ignition On Engine On,
           Ignition Off, Ignition Pre-Off`），
         被略過為「非行為陳述」；
         Partial Operation 於該七個狀態下皆應存在，而 TC 只取其一。

         **前五批之「只取其一」缺口全為 OR 型，
         因為 G113 只看得到 OR** —— 逗號型有多少，至今無人量過。

         處置：先量規模，不先立閃點 ——
         掃全部 leaf 之 `source_clause`，統計
         「冒號或冒號後以逗號分隔三項以上之列舉」之出現次數與所在 leaf，
         並抽樣判別其中有幾成屬「各項皆應測而 TC 只取其一」。
         **抽樣率不低於 16.7%，種子載明。**
         規模明確後方議是否擴充 G113；
         **本包不擴充 G113 之辨識規則**。
         裁決者 Pei，逐字依據：「寫在 26 包」。
```

```
[R-P193] 品牌條件（`Brand_Configuration_2 == "Jeep"`）須於 reasoning 處理。
         `SWE-PM-014` 與 `SWE-PM-026` 之 `source_clause` 皆含
         `Brand_Configuration_2 == "Jeep"` 之分支與其 ELSE，
         而二者之 `reasoning` **隻字未提**：
         R1LR 是否為 Jeep 品牌、非 Jeep 時之 ELSE 分支要不要測，皆無交代。
         此與 A-PW29（EE Architecture 之適用性）同一類問題。

         處置：
         （a）掃全部 leaf 之 `source_clause`，
              列出含品牌、車型、市場、配備等**適用性條件**者
         （b）逐一於 `reasoning` 明載其處理：
              本專案適用哪一支、另一支是否需測、依據為何
         （c）**本專案是否為 Jeep 品牌，分析層無資料可查** ——
              若無可賴來源，開 DR（編號查後開，R-P147），
              **不得自行推定**
         裁決者 Pei，逐字依據：「寫在 26 包」。
```

```
[R-P194] R-P184 之裁定範圍擴及 `SWE-PM-015` 與 `SWE-PM-019`。
         二者之 `source_clause` 同為
         `Front_Panel_OnOff.Req` 對 `CLIMATIC_PANEL.Radio_Btn0`
         之成對結構，與 `SWE-PM-025` 同型；
         23 包已載「若裁為應合併，`SWE-PM-015`/`019` 同受影響」，
         **而分析層於 R-P184 只寫了 `SWE-PM-025`**。

         裁定：R-P184 之理由逐字適用於 `SWE-PM-015` 與 `SWE-PM-019` ——
         二訊號為不同硬體來源，故障可只存於其中一條路徑，
         依 §5.7 拆分成立，**三個 leaf 之現有拆法全數維持**。
         R-P184 依 R-P36 原文不改，於其下加註指向本條。
         裁決者 Pei，逐字依據：「寫在 26 包」。
```

```
[R-P195] `SWE-PM-026` 之車門 OR 分支須查證。
         其 `source_clause` 載
         `AND (STATUS_BH_BCM1.DriverDoorSts passes to "Open"
               OR STATUS_BH_BCM1.PsngrDoorSts passes to "Open")`，
         **而規格原文緊接著又單獨重複一次 `DriverDoorSts passes to "Open"`** ——
         原文本身即有矛盾。
         該 leaf 之三條拆分依前狀態與通話狀態，
         **無一條提及副駕駛門**。

         查證：
         （a）G113 是否曾對該錨點觸發？若有，其裁決為何？
         （b）若未觸發，說明其漏檢形態
         （c）`PsngrDoorSts` 分支是否需補測，依 R-P118(d) 裁決
         規格原文之矛盾登為 anomaly，並考慮合入現有 DR 或另開。
         裁決者 Pei，逐字依據：「寫在 26 包」。
```

```
[R-P196] 內容高度重疊而錨點不同之 leaf 須登記。
         `SWE-PM-028`（錨點 `4941580`-`4941582`）與
         `SWE-PM-029`（錨點 `4941586`-`4941589`）之 `source_clause`
         內容近乎重複（防盜成功 → 請求復歸 → Timeout1 取值 → 轉 Timed），
         `029` 多一個 `$PwrAccDelayAct$` 來源；二者各拆三／四條。

         **錨點不同而內容重複** —— 為 A-PW137
         （五對 leaf 共用完全相同錨點）之近親形態，
         而 A-PW137 僅登記了「錨點完全相同」那一型。

         處置：掃全部 leaf，以 `source_clause` 之
         正規化文字相似度找出重疊對（門檻載明），
         逐對登記於 ANOMALIES，並於各該 TC 之 `reasoning`
         註明其重疊之對造 leaf ID（比照 R-P186）。
         **不得自行合併或刪除**；是否並入 DR-PW12 裁定於 27 包。
         裁決者 Pei，逐字依據：「寫在 26 包」。
```

```
[R-P197] 本專案之品牌為 **Jeep**。
         R-P193(c) 載「本專案是否為 Jeep 品牌，分析層無資料可查」
         並令無可賴來源時開 DR。Pei 直接裁定，**不開 DR**。

         效果：
         （a）`Brand_Configuration_2 == "Jeep"` 之分支**在範圍內**，須測；
              `SWE-PM-026` 全條段皆以此為前提，故整個 leaf 在範圍內
         （b）`SWE-PM-014` Behaviour 2 之 Jeep 分支在範圍內
         （c）**ELSE 分支之處置仍需一項實測**：
              `Brand_Configuration_2` 為 PROXI 參數。
              若其於本專案恆為 `Jeep`，ELSE 分支永不發生，
              不測並於 `reasoning` 明載其理由；
              若該參數得逐車配置而可取其他值，
              ELSE 分支即為另一可發生情境，**須補測**。
              執行層須自 CFTS 本文或 037 實測該參數之可取值域並回報；
              **實測不到值域者停並上繳，不得推定**。
         （d）R-P193 依 R-P36 原文不改 ——
              其所記「分析層無資料可查」為當時之事實，
              於其下加註指向本條。
         裁決者 Pei，逐字依據：「Jeep是」。
```

```
[R-P198] `Brand_Configuration_2` 得逐車配置，ELSE 分支須測。
         R-P197(c) 令實測該 PROXI 參數之可取值域以定 ELSE 之處置。
         Pei 直接裁定：**得逐車配置**。
         故 ELSE 分支為另一可發生情境，**不得以「恆為 Jeep」為由不測**。
         R-P197(c) 之實測要求因而解除，不須再測值域。

         逐 leaf 之處置：
         （a）**`SWE-PM-014` Behaviour 2** —— 規格明載二支：
              Jeep 且駕駛門開且無通話 → Standby；
              ELSE → Timed（維持當前音源）。
              **二支皆須有 TC。**
              查證現有 TC 是否已涵蓋二支；缺者補測。
         （b）**`SWE-PM-026`** —— 其錨點未載 ELSE，
              非 Jeep 時之行為規格未定義。
              依 §7（列舉之支援項須配對至少一條否定測試），
              **補一條否定側 TC**：
              `Brand_Configuration_2` 非 `Jeep` 時，
              車門開啟**不導致**該狀態轉換。
              **ER 僅得述「未發生該轉換」**，
              不得斷言非 Jeep 時發生何種其他行為（§8.4.1）。
         （c）以同一判準掃全部 leaf：
              凡含 `Brand_Configuration_2` 之守衛條件者，
              逐一回報其肯定側與否定側之涵蓋情形。

         **本條僅拘束 `Brand_Configuration_2`**；
         其他適用性參數（車型、市場、配備）之值域仍依 R-P193 盤點，
         實測不到者停並上繳，不得類推本條。
         裁決者 Pei，逐字依據：「可逐車配置」。
```

## B. 本包須產出

### B1. 末批（R-P189）—— 主產出

leaf ID 全集逐一列出；阻斷者列明 DR 編號；
上繳內容依 R-P158 所列四項，並依 R-P180 標示盲測指標適用性。
**最終對帳不符即停。**

### B2. DR-PW9 阻斷欄修訂（R-P185）

明載「阻斷 `SWE-PM-112` 之 TC 撰寫」。

### B3. 五對共用錨點之 `reasoning` 註記（R-P186）

各該 TC 之 `reasoning` 註明共用錨點之對造 leaf ID。

### B4. `SWE-PM-093` 屬性資料（R-P188）

二錨點全部屬性逐欄原值 ＋ 集合正規化後之比對結果與差集。

### B5. R-P183 之驗證回報

本包 §前言、§A、§B、§D 內之數字，逐一檢查是否已依 R-P183 標註；
**未標註者停並回報** —— 本包為該機制之首次適用，
分析層可能有遺漏，執行層須據實指出。

### B6. leaf `reasoning` 補寫與 G129（R-P190）—— **寫回之阻斷項**

（a）實測五個批次檔之 leaf `reasoning` 非空率，逐批回報
（b）逐 leaf 補寫，依 §10.4 四項順序；**不得以模板套語填充**
（c）實作 G129 並以刻意置空之 fixture 證明其會 FAIL

### B7. 補測同步更新（R-P191）

掃全部批次，凡因反向涵蓋或 G113 而補測者，同步更新其 leaf `reasoning`；
逐項列出更新前後。

### B8. 逗號列舉規模量測（R-P192）

統計出現次數與所在 leaf；抽樣率 ≥ 16.7%，種子載明；
判別其中屬「各項皆應測而 TC 只取其一」之比例。
**不擴充 G113 之辨識規則。**

### B9. 適用性條件盤點（R-P193 / R-P197 / R-P198）

列出含品牌、車型、市場、配備條件之 leaf；
逐一於 `reasoning` 明載其處理。
**品牌已由 R-P197 裁定為 Jeep；且依 R-P198 得逐車配置，
`Brand_Configuration_2` 之值域不需再測。**
依 R-P198(a)(b)(c) 完成三項：
  （a）`SWE-PM-014` Behaviour 2 之二支涵蓋查證，缺者補測
  （b）`SWE-PM-026` 補一條否定側 TC
  （c）全部含 `Brand_Configuration_2` 守衛條件之 leaf 逐一回報
其他適用性參數之值域實測不到者停並上繳。
並依 R-P36 為 R-P193 加註指向 R-P197 / R-P198，雜湊佐證原文未變。

### B10. R-P184 加註（R-P194）

依 R-P36 原文不改，加註指向 R-P194；雜湊佐證原文未變。

### B11. `SWE-PM-026` 車門 OR 查證（R-P195）

依（a）（b）（c）回報；規格原文矛盾登為 anomaly。

### B12. 重疊對掃描（R-P196）

以正規化文字相似度找出重疊對（門檻載明）；
逐對登記並於 `reasoning` 註明對造 leaf ID。
**不得自行合併或刪除。**

## D. 閃點

G0 為前置閘。G0–G124 沿用（G17 已移除；G109 不實作），期望值不變。

| # | 項目 | 期望值 |
|---|---|---|
| G125 | 末批產出（R-P189） | 【實測填入】leaf ID 全集、TC 數、排除清單與 DR 編號 |
| G126 | 最終對帳（R-P189） | 已產出 ＋ 阻斷 ＋ `SWE-PM-089` = 115；不符即停 |
| G127 | `SWE-PM-093` 屬性正規化比對（R-P188） | 【實測填入】正規化後相異之欄與差集 |
| G128 | R-P183 首次適用（B5） | 本包全部數字皆已標註來源型別；未標註者已回報 |
| **G129** | leaf `reasoning` 非空（R-P190） | 五個批次檔之每一 leaf `reasoning` 皆非空；刻意置空 → FAIL |
| **G130** | 補測同步更新（R-P191） | 【實測填入】需更新之 leaf 數與已更新數 |
| **G131** | 逗號列舉規模（R-P192） | 【實測填入】出現次數、所在 leaf、抽樣率與種子、真缺口比例 |
| **G132** | 適用性條件盤點（R-P193） | 【實測填入】含適用性條件之 leaf 清單與其處理 |
| **G133** | 重疊對掃描（R-P196） | 【實測填入】重疊對數、相似度門檻 |
| G70 | lint 全閘 | 全 PASS |

## E. framework

§E 已定版（R-P35），本包不動。

## F. Anomaly 異動

開新號前先查現行最大號並回報（R-P147）。

- 新增：分析層未經查證斷言第六次，且根因即同包內所立之 R-P177（R-P183）
- 新增：DR-PW9 之阻斷欄未承接 R-P148 之立條意旨（R-P185）
- A-PW137 → 依 R-P186 追認處置，其依據改為 §8.2.2
- A-PW138 → 依 R-P188 待資料

## G. DATA_REQUESTS

DR-PW9（依 R-P185 修訂阻斷欄）、DR-PW10、DR-PW11、DR-PW12 沿用。無新增。

## H. 作業指示

1. G0 前置閘
2. 查現行最大號並回報
3. 執行 B5 —— 驗本包數字是否皆已標註（R-P183 首次適用）
4. 執行 B2 DR-PW9 修訂
5. 執行 B3 `reasoning` 註記
6. 產出 B4 `SWE-PM-093` 屬性資料
7. 產出 B1 末批，驗 G125 / G126
7b. 執行 B6 – B12（R-P190 – R-P196），驗 G129 – G133 —— **B6 為寫回之阻斷項**
8. 以 §D 全表自驗
9. §A 十六條裁決逐字抄入 RULINGS.md；§F 入 ANOMALIES.md
10. 上繳 `features/power/docs/upstream/26_final_batch.md`，更新 `docs/INDEX.md`

## I. 禁區

沿用 25 包 §I 全部條目，另增列：

- **不得採信下放包內未標註來源型別之數字**（R-P183）
- **不得自行合併或拆分 `SWE-PM-093` 之 TC**（R-P188）
- **量測腳本之自我修改若其 bug 方向對執行層有利，須停並上繳**（R-P187）
- **末批之範圍一律自對帳表取全集，不以區間表述**（R-P189）
- **不得以模板套語填充 leaf `reasoning`**（R-P190(ii)）
- **不得擴充 G113 之辨識規則**（R-P192）
- **不得自行推定其他適用性參數之可取值域**（R-P198）
- **`SWE-PM-026` 否定側 TC 之 ER 僅得述「未發生該轉換」**（R-P198(b)）
- **不得自行合併或刪除重疊對之任何 TC**（R-P196）

## J. 本包產生之新條文清單（自檢）

1. R-P183 下放包之數字須標註來源型別（機制）
2. R-P184 `SWE-PM-025` 維持八條，R-P167 結案
3. R-P185 `SWE-PM-112` 之排除追認；DR-PW9 阻斷欄修訂
4. R-P186 五對共用錨點逐一產出追認，依據改為 §8.2.2
5. R-P187 量測腳本自我修改須回報 bug 之影響方向
6. R-P188 `SWE-PM-093` 屬性差異裁定暫緩，取資料
7. R-P189 末批範圍：對帳表所列全部未產出且未阻斷者
8. R-P190 批次四、五之 leaf `reasoning` 全數缺漏，阻斷寫回（G129）
9. R-P191 補測後 `reasoning` 須同步更新（G130）
10. R-P192 G113 不涵蓋逗號列舉，先量規模（G131）
11. R-P193 品牌等適用性條件須於 reasoning 處理（G132）
12. R-P194 R-P184 之裁定擴及 `SWE-PM-015` 與 `SWE-PM-019`
13. R-P195 `SWE-PM-026` 之車門 OR 分支須查證
14. R-P196 內容高度重疊而錨點不同之 leaf 須登記（G133）
15. R-P197 本專案之品牌為 Jeep；ELSE 分支之處置待參數值域實測
16. R-P198 `Brand_Configuration_2` 得逐車配置，ELSE 分支須測

逐條確認：**十六條**，皆以獨立 fenced block 呈現於 §A，未夾於敘述中。
自檢：§A 區塊數 = 16、§J 列數 = 16、§H 步驟 9 寫「十六條」，三處一致。
本次已逐一數過 §A 之 fenced block。

## K. 分析層自判：本包是否仍有該驗而未驗者

**有，二項。**

1. **第三、四、五批合計 180 條** `[推導：223 − 43（首二批）= 180]`
   **分析層一條未看**，本包使其成為六包之積欠。
   執行層 25 §十一第 4 項之判斷成立：
   「工具抓得到分支缺漏，抓不到語義錯誤」，
   而語義錯誤僅分析層讀得出。
   R-P159 之分層取樣至今未執行。
   **本包仍未推進之** —— 分析層將於末批上繳後以獨立輪次為之，
   不再併入下放包，以免再度延宕。
2. **R-P183 為六次錯誤後方立之機制，其有效性未經驗證。**
   其依賴執行層於 §J 自檢時攔下未標註之數字；
   若執行層未攔，該機制即失效。本包為其首次適用，
   G128 之結果即為其第一份證據。
