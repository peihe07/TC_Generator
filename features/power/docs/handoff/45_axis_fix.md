# 45 — axis 複核發現訂正

下放包 | 分析層 → 執行層 | 往返 NN = 45

前置：44 包之執行狀態不影響本包；其五條裁決（R-P293 ~ R-P297）維持有效。

**分析層已履行 R-P297(c)** —— 讀畢 `axis_moved_sample_42.md`，
9 條具足夠細節可判，**同意 7、不同意 2**。
自 38 包起四度推遲之複核就此完成。

沿用不變之節：`§C 抽取規格` 同 02 包；`§I 禁區` 同 44 包 §I，另增列見本包 §I。

## A. 本包裁決條文（逐字，抄入 RULINGS.md）

```
[R-P298] `…-259` / `…-260` 之 `axis` 應為 `input_data`，非 `timing`。
         二條之相異為 **December 21st 對 March 20th**，
         而其事件結構**完全相同** —— 二者之 `input_test_data` 皆為
         `An Ignition On after the date passes <日期>`。

         **與 R-P283(1) 之裁定必須一致**：該條逐字載
         「`timing` 指事件發生之時點不同，
          **參數之單位為分鐘不等於其軸為時序**」。
         **同理：值之單位為日期，不等於其軸為時序。**
         二條之相對時點皆為「日期越過之後」，所異者為哪一個日期 ——
         與 `…-018` / `…-019`（20 分對 60 分）同形。

         若判 `timing` 成立，則 `…-018` / `…-019` 亦應為 `timing`，
         而該二條已裁定為 `input_data`。**二者不得並存。**

         處置：
         （a）`…-259` / `…-260` 及其同群者（`…-261` / `…-262` 等
              季節起始日之對照組）改回 `input_data`
         （b）**訂正 `TIMING_RE` 之判準**：其現行將
              「日期／時刻之取值差異」誤判為時點差異。
              `timing` 之正確語義為**事件發生之相對位置不同**
              （如 `boot … completed` 對 `is not ended`），
              非「值恰為時間量」
         （c）以訂正後之判準重跑 `timing` 群全 19 條，回報變動
         裁決者 Pei，逐字依據：「出啊」。
```

```
[R-P299] `…-216` / `…-212` 非「無對應」；五謂詞未涵蓋「情境型輸入」。
         二條之相異行逐字為：
         ```
         A climate pop-up at the moment of the transition
         An ongoing call  at the moment of the transition
         ```
         **二者皆為轉換當下所施加之情境**，其軸明確可辨。

         **判「無對應」為誤** —— 其成因非「確無可歸」，
         而係**五個正向謂詞未涵蓋「情境型／事件型輸入」**
         （pop-up、通話、中斷等非數值、非訊號名之施加項）。

         **此實證執行層 42 §八第 1 項之疑慮**：
         「無對應」之二義性中，本條屬「謂詞不足」一側。

         處置：
         （a）擴充 `INPUT_DATA_RE` 或另立謂詞，涵蓋情境型輸入；
              其詞彙**須先量語料**（R-P250）並附三個已知應命中之實例
         （b）以擴充後之判準**重跑全部 36 條「無對應」**，回報其變動
         （c）仍為「無對應」者，**逐條列其相異行逐字**（43 包已備 28 條，
              本包補齊至 36 條），供分析層再次複核
         （d）**不得以「已擴充謂詞」為由認定其餘 36 條皆已解決** ——
              抽樣 1 條即中，不足以推論其比例，
              亦不足以推論擴充後即無殘留（R-P64）
         裁決者 Pei，逐字依據：「出啊」。
```

```
[R-P300] 分析層複核之結果與其涵蓋：據實記明。
         分析層讀 `axis_moved_sample_42.md` 15 條，
         **其中 9 條具足夠細節可判**（其餘 6 條之素材未含對照條全欄或
         相異行過簡），**同意 7、不同意 2**。

         **同意者**：
         `…-005`/`006`（無對應，屬 R-P294 四對之一）、
         `…-021`/`022`（`mode`，與分析層 42 包之判定一致 ——
          **併答 43 §K 第 3 項：該二條確在 `mode` 僅存之 6 條內**）、
         `…-036`/`041`、`…-108`/`109`、`…-116`/`115`、
         `…-138`/`139`、`…-206`/`202`（皆 `trigger_state`）。

         **不同意者**：`…-259`/`260`（R-P298）、`…-216`/`212`（R-P299）。

         **涵蓋之限度明載**：
         抽樣 15 / 64 = 23.4%，其中可判者 9 = 14.1%；
         `trigger_state` 群（母體 32）讀 5 條、`mode` 群（母體 2）讀 1 條、
         `timing` 群（母體 7）讀 1 條、無對應群（母體 23）讀 2 條。
         **`trigger_state` 之 5 條全數同意，其群內判斷之信心較高；
         其餘三群之樣本過小，不足以支持群層之結論。**
         裁決者 Pei，逐字依據：「出啊」。
```

```
[R-P301] 寫回之前置更新。
         R-P297 所定四項中，(c)（分析層讀畢素材）**已完成**（R-P300）。
         **惟本包新增二項訂正**（R-P298 / R-P299），其後須再驗。

         **寫回之前置條件更新為：**
         （a）R-P294 之合併完成，260 條（44 包）
         （b）R-P298 之 `timing` 群重跑完成
         （c）R-P299 之「無對應」36 條重跑完成，
              且仍為無對應者已備素材
         （d）R-P295 之交集已查（44 包）
         （e）**最終全量 dry-run 十項全數 PASS**
         （f）**Pei 明示授權**（charter：交付形式與送達執行屬 Pei）

         六項齊備後，寫回於 46 包。
         **本包不對客戶樹寫回。**

         併記：分析層於 44 §K 第 1 項稱該複核「不可再推遲」，
         **已於本包履行**；(c) 項自此不再為阻斷。
         裁決者 Pei，逐字依據：「出啊」。
```

## B. 本包須產出

### B1. `TIMING_RE` 訂正與 `timing` 群重跑（R-P298）—— **最高優先**

判準改為「事件發生之相對位置不同」；
`…-259` ~ `…-262` 等季節組改回 `input_data`；
`timing` 群全 19 條重跑並回報變動。

### B2. 情境型輸入謂詞擴充與 36 條重跑（R-P299）

詞彙先量語料並附三個已知實例；
36 條重跑並回報變動；仍為無對應者逐條列相異行逐字。

### B3. 訂正後之全量 dry-run

若 44 包之合併已完成，以 260 條為母體；否則以現行條數並標明。

## D. 閃點

G0 為前置閘。G0–G209 沿用（G17 已移除；G109 不實作），期望值不變。

| # | 項目 | 期望值 |
|---|---|---|
| G210 | `TIMING_RE` 訂正（R-P298） | `…-259` ~ `262` 已回 `input_data`；【實測填入】`timing` 群 19 → ? |
| G211 | 情境型輸入謂詞（R-P299） | 詞彙已量語料並附三實例；【實測填入】36 → ?；仍無對應者已備素材 |
| G212 | 訂正後全量 dry-run | 十項全數 PASS |
| G70 | lint 全閘 | 訂正後仍全 PASS |

## E. framework

§E 已定版（R-P35），本包不動。
**真實寫回於 46 包**，其前置為 R-P301 之六項；**須 Pei 明示授權**。

## F. Anomaly 異動

開新號前先查現行最大號並回報（R-P147）。

- 新增：`TIMING_RE` 將「值恰為時間量」誤判為時點差異（R-P298）
- 新增：五謂詞未涵蓋情境型／事件型輸入，致誤判「無對應」（R-P299）
- 新增：分析層複核之群層涵蓋不均，三群樣本過小（R-P300）

## G. DATA_REQUESTS

DR-PW9 ~ DR-PW15 沿用。無新增。

## H. 作業指示

1. G0 前置閘
2. 查現行最大號並回報
3. B1 `TIMING_RE` 訂正與重跑，驗 G210 —— **最高優先**
4. B2 情境型輸入謂詞擴充與 36 條重跑，驗 G211
5. B3 訂正後全量 dry-run，驗 G212
6. 以 §D 全表自驗
7. **抄錄裁決條文前重驗一次 §J 自檢**（R-P200(c)）；
   §A 四條逐字抄入 RULINGS.md；§F 入 ANOMALIES.md
8. 上繳 `features/power/docs/upstream/45_axis_fix.md`，更新 `docs/INDEX.md`

## I. 禁區

沿用 44 包 §I 全部條目，另增列：

- **不得以「已擴充謂詞」為由認定其餘 36 條皆已解決**（R-P299(d)）
- **不得使 `…-018`/`019` 與 `…-259`/`260` 之 axis 判定相左**（R-P298）
- **不得對客戶樹之 FW036 寫回**（R-P301）

## J. 本包產生之新條文清單（自檢）

1. R-P298 `…-259`/`260` 應為 `input_data`；`TIMING_RE` 判準訂正
2. R-P299 `…-216`/`212` 非「無對應」；謂詞未涵蓋情境型輸入
3. R-P300 分析層複核之結果與其涵蓋限度
4. R-P301 寫回之前置更新為六項

逐條確認：**四條**，皆以獨立 fenced block 呈現於 §A，未夾於敘述中。
自檢：§A 區塊數 = 4、§J 列數 = 4、§H 步驟 7 寫「四條」，三處一致。
本次已逐一數過 §A 之 fenced block。
**本包下放後分析層不再修改**（R-P200(a)）。

## K. 分析層自判：本包是否仍有該驗而未驗者

**有，三項。**

1. **抽樣 1 條即中「謂詞不足」，不足以推論 36 條中之比例。**
   R-P299(d) 已明禁以擴充為由結案，
   **惟擴充後仍為無對應者之複核，本包只令備素材，未令分析層再讀** ——
   其讀取時點為 46 包，**而該包為寫回包**。
   若屆時再發現同型，寫回將再延。
2. **`trigger_state` 群 32 條中讀 5 條全數同意，**
   而 `mode`（母體 2）、`timing`（母體 7）、無對應（母體 23）
   三群之樣本各為 1、1、2 條 —— **不同意之 2 條皆出自小樣本群**。
   **小樣本群之誤判率是否較高，抑或僅為抽樣所致，無從判斷。**
3. **本包為分析層之複核所生，而該複核延宕四包。**
   若其於 38 包即為之，`axis` 之二次訂正可與 41 包之套用合併，
   **本包與 42–43 包之部分工作可免**。
   延宕之成本於此具體可見，記明。
