# 25 — 批次定義訂正、全量對帳與第五批

下放包 | 分析層 → 執行層 | 往返 NN = 25

前置：24 包上繳已覆核，判定 **ACCEPT**。
G113 五項全數重現且 `OR_TOKEN_RE` 一字未動；
前瞻攔下第十例（`SWE-PM-030`）；分桶真陽性率 3.6% → 1.9%。

**分析層二項錯誤，於本包處置**：
（甲）以 SWE-PM ID 區間定義批次範圍 —— 第三批因此撞上 DR-PW6，
      第四批因此與第二批重疊六個 leaf。**同一錯誤二次。**
（乙）R-P173(丙) 之前提為假 —— 執行層之 `norm_set()` 自 20 包起
      即對全部屬性做集合比對，未正規化之採信發生於呈現層。
      **此為分析層未經查證即書寫斷言之第五次。**

沿用不變之節：`§C 抽取規格` 同 02 包；`§I 禁區` 同 24 包 §I，另增列見本包 §I。

## A. 本包裁決條文（逐字，抄入 RULINGS.md）

```
[R-P177] 批次範圍一律以 Test Set 成員資格定義，不得以 SWE-PM ID 區間定義。
         R-P174 載「Power State 之 `SWE-PM-033`–`063`（31 leaf）」，
         而其中 `038` / `057` / `060`–`063` 六個屬 **Timeout Settings**，
         已於第二批產出 —— **Test Set 成員資格不是 ID 區間**。

         R-P158 之第三批亦以 ID 區間定義（「前半 32」），
         因而撞上 DR-PW5 / DR-PW6（R-P165 所處置者）。
         **同一錯誤二次**，且第二次發生於第一次已立規則之後。

         處置：
         （a）第四批範圍**訂正為 25 leaf**，不重做該六個 leaf ——
              重做將使同一 leaf 有二組 TC，違 §10.3 之單調遞增與追溯唯一性
         （b）往後任何批次之範圍，一律自 `data/leaf_testset.tsv`
              取該 Test Set 之成員清單，再依 R-P165 扣除 live DR 影響面；
              **下放包須逐一列出該批之 leaf ID 全集**，不得以區間表述
         R-P174 依 R-P36 原文不改，於其下加註指向本條。
         裁決者 Pei，逐字依據：「一次下放」。
```

```
[R-P178] 全量對帳表（G121）—— 第五批之前置。
         分析層自定版數字直接相減得：
         Power State 定版 **63** leaf；
         ID 區間 `001`–`063` 共 63 個 ID，其中 6 個屬 Timeout Settings，
         故該區間內僅 **57** 個屬 Power State；
         第三批 22 ＋ 第四批 25 ＋ 阻斷 10（`001`–`009` 由 DR-PW6、
         `010` 由 G103）= **57**。
         **63 − 57 = 6 個 Power State leaf 之 ID 大於 063，二批皆未觸及。**

         此為算術推導，非實測 —— 分析層無 leaf→Test Set 對照表可查，
         **不宣稱該六個 leaf 為何者**。

         G121：產出全量對帳表，逐 leaf 一列：
           `SWE-PM ID` / `Test Set`（依 §E 定版）/
           `批次`（001–004 或 `未產出`）/ `TC 數` /
           `阻斷之 DR 編號`（若有）
         合計須滿足：
           各 Test Set 之 leaf 數 = 63 / 24 / 16 / 8 / 3，合計 114
           ＋ `SWE-PM-089`（R-P141 留空）= 115
         **任一 Test Set 之合計不符即停並上繳。**

         此表為往後全部批次之範圍依據（R-P177(b)）。
         裁決者 Pei，逐字依據：「一次下放」。
```

```
[R-P179] R-P173(丙) 訂正；`SWE-PM-025` 之裁定待觸發訊號原文。
         執行層訂正：`build_anchor_attributes.py` 之 `norm_set()`
         自 20 包起即對全部屬性做集合比對，
         故 R-P135 之 ECU 早已判為「相同」；
         未正規化之採信發生於**呈現層**（23 §四之表格），非比對邏輯。
         分析層於 R-P173(丙) 所述之前提為假 ——
         **此為未經查證即書寫斷言之第五次**
         （前四次：A-PW30「最大」、R-P157「應可機械檢查」、
          R-P161「重複七次」、R-P172 之排序歸因）。

         `SWE-PM-025` 三對之 ECU 為 `LTM, RRM` vs `LTM, RRM, ETM`，
         **真集合差 `{ETM}`**，非排列。R-P167 不結案。

         惟本專案為 LTM（Atlantis High）——
         **二組錨點之 ECU 集合皆含 LTM**，
         故就本專案而言該差異不改變適用性。
         真正之爭點在 22 §八第 4 項所述之「只差觸發訊號」。

         處置：上繳二組錨點之**觸發訊號逐字原文**並列。
         （a）觸發訊號確為不同訊號 → 依 §5.7 拆分成立，八條維持
         （b）為同一訊號之不同稱法 → 併為四條
         裁定於 26 包。**不得自行合併或拆分。**
         裁決者 Pei，逐字依據：「一次下放」。
```

```
[R-P180] 盲測指標於工具進入撰寫階段後失效，明載。
         24 §另記：第四批「事前未知缺口 0」**不是品質證據** ——
         本批撰寫時 G113 已在手，OR 兩支被當成拆分軸，
         **盲測抓不到同型缺口是設計使然**。

         逐字採納。通則：任一工具一旦用於**撰寫階段**，
         其於**覆核階段**之捕獲數即不再構成該工具有效性之證據，
         亦不構成產出品質之證據。
         往後回報「事前未知缺口數」時，須同時標明
         該工具於本批撰寫時是否已在手；
         已在手者，該數字標為**不適用**而非 0。

         R-P128（第二批為透鏡 3 之第一次盲測）之結論
         僅對第二批有效，不得外推至後續批次。
         裁決者 Pei，逐字依據：「一次下放」。
```

```
[R-P181] 第五批範圍：依 G121 對帳表，取 Power State 未產出者
         ＋ Startup Display（24 leaf）。
         （a）G121 完成後，Power State 之未產出 leaf 逐一列出並納入
              （分析層推導為 6 個，實測為準）
         （b）併入 Startup Display 全 24 leaf
         （c）依 R-P165 扣除 live DR 影響面，排除者列明其 DR 編號
         （d）`SWE-PM-001`–`010` 仍一律不納入
         （e）下放包／上繳包須**逐一列出 leaf ID 全集**，不得以區間表述

         **G121 為本批之前置條件**，未完成不得啟動。
         tc_id 沿用臨時編號，接續第四批之末。
         裁決者 Pei，逐字依據：「一次下放」。
```

```
[R-P182] 分桶初版之三個數字並陳，追認為常規。
         24 §另記：分桶初版把 `IF` / `ELSE IF` / `THENTLM`
         當成「具名標的」，虛增真缺口為 10；
         排除連接詞後 4；逐項裁決後 1 ——
         **三個數字都列在上繳包裡，未只呈現最終值。**

         追認並立為常規：凡經多次修正而變動之量測值，
         上繳時須並陳其各版數值與修正理由，
         不得僅呈現最終值。
         理由：僅呈現最終值者，讀者無從判斷該值係量測所得
         或係調整所得（13 §七(丙)7 之教訓）。
         裁決者 Pei，逐字依據：「一次下放」。
```

## B. 本包須產出

### B1. G121 全量對帳表（R-P178）—— **第五批前置，最高優先**

逐 leaf 一列，欄位依 R-P178；各 Test Set 合計須為 63 / 24 / 16 / 8 / 3。
**任一不符即停並上繳。**
輸出至 `data/leaf_batch_reconciliation.tsv` ＋ 摘要 md。

### B2. R-P174 加註（R-P177）

依 R-P36 原文不改，加註指向 R-P177；雜湊佐證原文未變。

### B3. `SWE-PM-025` 觸發訊號原文（R-P179）

二組錨點之觸發訊號逐字並列；
**不得自行合併或拆分**；裁定於 26 包。

### B4. 第五批（R-P181）

G121 完成後啟動；leaf ID 全集逐一列出；
上繳內容依 R-P158 所列四項，並依 R-P180 標明盲測指標之適用性。

## D. 閃點

G0 為前置閘。G0–G120 沿用（G17 已移除；G109 不實作），期望值不變。

| # | 項目 | 期望值 |
|---|---|---|
| G121 | 全量對帳表（R-P178） | 各 Test Set 合計 = 63 / 24 / 16 / 8 / 3；＋ `SWE-PM-089` = 115 |
| G122 | R-P174 加註後原文位元組未變 | UNCHANGED |
| G123 | 第五批產出（R-P181） | 【實測填入】leaf ID 全集、TC 數、排除清單與 DR 編號 |
| G124 | 第五批反向涵蓋 ＋ G113 | 【實測填入】三桶計數、真陽性率；盲測指標依 R-P180 標為不適用或實測 |
| G70 | lint 全閘 | 全 PASS |

## E. framework

§E 已定版（R-P35），本包不動。
**G121 之結果若顯示某 Test Set 之 leaf 數與定版不符，
停並上繳，不得自行調整 §E。**

## F. Anomaly 異動

開新號前先查現行最大號並回報（R-P147）。

- 新增：分析層以 ID 區間定義批次範圍，二次致錯（R-P177）
- 新增：Power State 有 6 leaf 疑未觸及（算術推導，待 G121 實測）（R-P178）
- 新增：分析層未經查證即書寫斷言第五次（R-P179）
- 新增：盲測指標於工具進入撰寫階段後失效（R-P180）

## G. DATA_REQUESTS

DR-PW9（High）、DR-PW10（Medium）、DR-PW11（High）沿用。無新增。

## H. 作業指示

1. G0 前置閘
2. 查現行最大號並回報
3. 產出 B1 G121 全量對帳表 —— **最高優先；不符即停**
4. 執行 B2 加註，驗 G122
5. 產出 B3 觸發訊號原文 —— **不得自行合併或拆分**
6. G121 完成後啟動 B4 第五批，驗 G123 / G124
7. 以 §D 全表自驗
8. §A 六條裁決逐字抄入 RULINGS.md；§F 入 ANOMALIES.md
9. 上繳 `features/power/docs/upstream/25_batch5.md`，更新 `docs/INDEX.md`

## I. 禁區

沿用 24 包 §I 全部條目，另增列：

- **不得以 SWE-PM ID 區間定義任何批次範圍**（R-P177(b)）
- **不得重做第二批已產出之六個 leaf**（R-P177(a)）
- **不得自行合併或拆分 `SWE-PM-025` 之八條**（R-P179）
- **不得於 G121 完成前啟動第五批**（R-P181）
- **G121 結果與 §E 定版不符時不得自行調整 §E**（§E）
- **不得以「事前未知缺口 0」作為品質證據**（R-P180）

## J. 本包產生之新條文清單（自檢）

1. R-P177 批次範圍以 Test Set 成員資格定義，不以 ID 區間
2. R-P178 全量對帳表 G121，第五批前置
3. R-P179 R-P173(丙) 訂正；`SWE-PM-025` 待觸發訊號原文
4. R-P180 盲測指標於工具進入撰寫階段後失效
5. R-P181 第五批範圍：Power State 未產出者 ＋ Startup Display
6. R-P182 多次修正之量測值須並陳各版數值

逐條確認：**六條**，皆以獨立 fenced block 呈現於 §A，未夾於敘述中。
自檢：§A 區塊數 = 6、§J 列數 = 6、§H 步驟 8 寫「六條」，三處一致。
本次已逐一數過 §A 之 fenced block。

## K. 分析層自判：本包是否仍有該驗而未驗者

**有，三項。**

1. **R-P178 之「6 個 leaf」為算術推導，非實測。** 依 R-P64 標為推導。
   若 G121 實測不為 6，本條之推導即有誤，須連同其推導過程一併檢討 ——
   不得僅訂正數字。
2. **第三批 64 條與第四批 50 條之內容，分析層皆未覆核。**
   累計 114 條未經目視，而依 R-P159 應做分層取樣，取樣本身也未做。
   此為連續四包之積欠。
3. **分析層之錯誤已達五次且形態相同。**
   R-P64 立於 08 包，於分析層身上失效五次，
   每次皆由執行層抓出。本包仍未提出機制改善之。
