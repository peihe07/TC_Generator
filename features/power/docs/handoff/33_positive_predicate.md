# 33 — 正向轉換謂詞、050 裁定與不可再生產物

下放包 | 分析層 → 執行層 | 往返 NN = 33

前置：32 包上繳已覆核，判定 **ACCEPT**。
G154 全批觸發 26 條、三個已知實例皆重現；
產物判類 (a) 52 / (b) 4 / **(c) 12**；備份機制已實作。

**分析層已複核 32 條裁決：同意 32 / 32**（`050` 待裁，由本包 R-P232 裁定）。
`259`–`262` 之季節起始日確為界線值；
18 條 Decision Table 之邏輯正確（ER 明示不轉換 → 第 3 列不命中 → 第 4 列）；
`010` / `011` 之 `volume limit` 偽陽性判斷正確。

沿用不變之節：`§C 抽取規格` 同 02 包；`§I 禁區` 同 32 包 §I，另增列見本包 §I。

## A. 本包裁決條文（逐字，抄入 RULINGS.md）

```
[R-P231] `ROW3_RE` 不放寬；改建「明示有轉換」之正向謂詞。
         32 §九第 1 項：執行層自陳
         「放寬 `ROW3_RE` 會把大批『無法判定』變成『相符現值』，
          **等於我用一個我自己寫的謂詞去證明我自己先前的指派是對的**」，
         並將套用與否交由分析層。**該克制正確。**

         **裁定：不放寬。** 放寬使 174 條落回現值，
         而現值正是 95.8% 偏向之來源 —— 該作法以判準遷就既有答案。

         **改以對稱之正向謂詞：**
         （a）建 `明示有轉換` 詞彙表，**自本語料導出**
              （比照 G154 之作法：`passes to` / `enters` / `transitions to` /
                `goes to` / `is in … state` 之轉入形態等，實測為準）
         （b）命中者 → §12 第 3 列**正向確認**，非默認
         （c）**未命中且第 1、2、4–8 列皆不命中者 → 落至第 9 列
              Functional Based** ——
              §12 為 first-match 且第 9 列為 catch-all，
              **「機械無法判定」不應存在為一個結果類別**；
              其真正意義為「未命中任何前列，故落底」
         （d）G154（明示不轉換）與本條之正向謂詞**互為對照** ——
              二者同時命中者為矛盾，須逐條人工裁決並列出成因

         此非放寬亦非收緊，係將「以現值為默認」改為「以文本為依據」。
         裁決者 Pei，逐字依據：「先繼續做」。
```

```
[R-P232] `050` 裁定：情境建構之斷電非 §12 第 2 列之 fault injection。
         32 §3.2：`050` 命中之 `disconnect` 出自前提
         `The battery is disconnected`；§12 第 2 列逐字列 `disconnect`，
         惟執行層指出「本條之斷電為情境建構而非注入故障」，
         二讀皆有據而不自行決定。

         **裁定：非 fault injection。**
         判準：§12 第 2 列之 `Simulated fault` ——
         **其故障須為驗證之對象**，即 TC 所觀察者為系統對該故障之反應。
         作為情境建構之前提者不適用 ——
         其故障不被觀察，僅用以建立待驗行為之起始條件。

         `050` 所屬之 `SWE-PM-012` clause 為
         「After a battery reconnection … it has to restore the last user
          settings … setting TLM_Status.Info to "Sleep" first」——
         **所驗者為重接後之還原與起始狀態，非斷電本身之反應**。
         故第 2 列不命中，依 first-match 續判其後各列。

         此判準立為通則，適用於全批之第 2 列判定。
         裁決者 Pei，逐字依據：「先繼續做」。
```

```
[R-P233] 不可再生產物須加寫入保護，並全掃同類。
         32 §一與 §九第 3 項：`edit_integrity_baseline.json` 屬 (c) 型 ——
         **其為 G108 之基準，重跑即等於重設基準，
         其後任何損壞都不會被攔下，且不會有人發現**。
         31 包之全掃未及於它，**係因其產生腳本不在當時之清單內
         —— 是運氣不是流程**。
         執行層自陳「同類之『一碰就失效』之物還有沒有，我沒有系統性地找過」。

         處置：
         （a）掃全部 `scripts/`，找出**會寫入 (c) 型產物**之腳本，
              逐一列出「腳本 → 其所寫入之 (c) 型產物」
         （b）為全部 (c) 型產物加**寫入保護** ——
              其產生腳本執行前須檢查該產物是否已存在；
              已存在者**拒寫並回報**，除非帶明示之覆寫參數
         （c）`edit_integrity_baseline.json` 另加一層：
              其重設須留下**重設紀錄**（時點、重設前後之雜湊），
              使「基準何時被重設」可事後查核
         （d）判準：凡「其價值來自記錄某一時點」者皆屬之 ——
              baseline、before-snapshot、dry-run 結果、對照表
         裁決者 Pei，逐字依據：「先繼續做」。
```

```
[R-P234] 備份範圍擴及 `generated/` 與 `scripts/`。
         32 §二：現行備份涵蓋 `data/` 68 檔，
         未涵蓋 `generated/`（理由：皆經產生器且已入版控）
         與 `scripts/`（理由：另由 G108 掌管）。

         **二理由皆不足：**
         （a）`generated/` 之 TC 內容為本 feature 之**主產出** ——
              「已入版控」意味著損壞後須以 git 還原，
              而 R-P228 正為使 git 不再是唯一退路而設
         （b）`scripts/` 由 G108 掌管者為**語法與符號完整性**，
              非其內容之正確性；G108 攔得住刪函式，
              攔不住「改壞了但仍能載入」

         處置：備份擴及 `generated/` 與 `scripts/`；
         回報擴充後之檔數與備份耗時。
         裁決者 Pei，逐字依據：「先繼續做」。
```

```
[R-P235] P0 佔 73.1% 之合理性須量測。
         32 §A-PW169 / 31 §二：P0 佔全批 73.1%（193 / 264）。
         §10.2 之 P0 定義為窄類：
         「safety, boot/recovery, connection, audio output, eCall,
          vehicle-critical CAN signal, data-loss risk」。
         **近四分之三之 TC 落入該窄類，其合理性未經檢驗。**

         此與 `design_method` 為同型風險：
         一個由執行層逐條指派、無閘門驗其正確性、且高度集中於單一值之欄位。
         R-P8 令 priority 依 TC 實際測項套 §10.2 判定 ——
         而該判定之結果從未被複核。

         處置：
         （a）量 P0 之逐 Test Set 分布
         （b）自 P0 抽樣（率 ≥ 16.7%，種子載明），
              逐條對照 §10.2 之七個類別，回報其所屬類別與依據
         （c）無法歸入任何類別者逐條列出
         **本包不改任何 priority 值** —— 裁定於 34 包。
         裁決者 Pei，逐字依據：「先繼續做」。
```

## B. 本包須產出

### B1. 正向轉換謂詞與全批重判（R-P231）—— **最高優先**

（a）詞彙表自語料導出並列出；（b）全批重跑機械提案；
（c）落至第 9 列者逐條列出；（d）與 G154 同時命中者逐條人工裁決。
回報：正向命中數 / 落底第 9 列數 / 矛盾數 / 相異於現值者。
**仍不逕行改值。**

### B2. 第 2 列判定依 R-P232 重跑

以「故障是否為驗證對象」為判準重判全批之第 2 列命中；
回報變動數。

### B3. 不可再生產物保護（R-P233）

（a）腳本 → (c) 型產物之對照；（b）寫入保護實作；
（c）`edit_integrity_baseline.json` 之重設紀錄；（d）判準套用結果。

### B4. 備份擴充（R-P234）

擴及 `generated/` 與 `scripts/`；回報檔數與耗時。

### B5. P0 抽樣（R-P235）

逐 Test Set 分布 ＋ 抽樣對照 §10.2 七類；無法歸類者列出。
**不改任何值。**

## D. 閃點

G0 為前置閘。G0–G157 沿用（G17 已移除；G109 不實作），期望值不變。

| # | 項目 | 期望值 |
|---|---|---|
| G158 | 正向轉換謂詞（R-P231） | 【實測填入】正向命中 / 落底第 9 列 / 與 G154 矛盾 / 相異於現值；矛盾者皆有人工裁決 |
| G159 | 第 2 列重判（R-P232） | 【實測填入】變動數；`050` 已依裁定處置 |
| G160 | 不可再生產物保護（R-P233） | (c) 型 12 份皆有寫入保護；已存在時拒寫並回報；fixture 證明 |
| G161 | 備份擴充（R-P234） | 涵蓋 `data/` ＋ `generated/` ＋ `scripts/`；回報檔數與耗時 |
| G162 | P0 分布與抽樣（R-P235） | 【實測填入】逐 Test Set 分布；抽樣之七類歸屬；無法歸類數 |
| G70 | lint 全閘 | 全 PASS |

## E. framework

§E 已定版（R-P35），本包不動。
**寫回仍不開放** —— 阻斷條件為 R-P231 之重判結果（34 包改值後方解除）。

## F. Anomaly 異動

開新號前先查現行最大號並回報（R-P147）。

- 新增：`ROW3_RE` 放寬將以判準遷就既有答案，已否決（R-P231）
- 新增：「機械無法判定」不應為結果類別，§12 第 9 列為 catch-all（R-P231(c)）
- 新增：不可再生產物之寫入保護缺如，`edit_integrity_baseline.json` 之未受損為運氣（R-P233）
- 新增：備份未涵蓋 `generated/` 與 `scripts/`（R-P234）
- 新增：P0 佔 73.1% 未經檢驗，與 `design_method` 同型風險（R-P235）

## G. DATA_REQUESTS

DR-PW9 ~ DR-PW15 沿用。無新增。

## H. 作業指示

1. G0 前置閘
2. 查現行最大號並回報
3. B3 不可再生產物保護**先行** —— 保護就位前不得執行任何會寫入 (c) 型產物之腳本
4. B4 備份擴充，驗 G161
5. B1 正向轉換謂詞與全批重判，驗 G158 —— **不逕行改值**
6. B2 第 2 列依 R-P232 重判，驗 G159
7. B5 P0 抽樣，驗 G162 —— **不改任何值**
8. 以 §D 全表自驗
9. **抄錄裁決條文前重驗一次 §J 自檢**（R-P200(c)）；
   §A 五條逐字抄入 RULINGS.md；§F 入 ANOMALIES.md
10. 上繳 `features/power/docs/upstream/33_positive_predicate.md`，更新 `docs/INDEX.md`

## I. 禁區

沿用 32 包 §I 全部條目，另增列：

- **不得放寬 `ROW3_RE` 或任何使「無法判定」落回現值之調整**（R-P231）
- **保護就位前不得執行任何會寫入 (c) 型產物之腳本**（R-P233）
- **不得逕行變更任何 `design_method` 或 `priority` 之值**（R-P231 / R-P235）

## J. 本包產生之新條文清單（自檢）

1. R-P231 `ROW3_RE` 不放寬；改建正向轉換謂詞；第 9 列為 catch-all
2. R-P232 `050` 裁定：情境建構之斷電非 fault injection
3. R-P233 不可再生產物須加寫入保護，並全掃同類
4. R-P234 備份範圍擴及 `generated/` 與 `scripts/`
5. R-P235 P0 佔 73.1% 之合理性須量測

逐條確認：**五條**，皆以獨立 fenced block 呈現於 §A，未夾於敘述中。
自檢：§A 區塊數 = 5、§J 列數 = 5、§H 步驟 9 寫「五條」，三處一致。
本次已逐一數過 §A 之 fenced block。
**本包下放後分析層不再修改**（R-P200(a)）。

## K. 分析層自判：本包是否仍有該驗而未驗者

**有，三項。**

1. **R-P231(c) 之「落底第 9 列」為分析層對 §12 之解讀。**
   §12 未明文其為 catch-all，係分析層自「first-match ＋ 末列為
   Single feature check」推得 `[推導]`。
   若該解讀有誤，174 條將一致地落向錯誤之值 ——
   **與現行 95.8% 之偏向為同型風險，只是方向不同**。
   執行層若見反證，須停並回報。
2. **分析層對 211 條取樣仍未讀**（31 包已備料，27 段）。
   本包仍未推進 —— 因 R-P231 之重判為寫回之阻斷項且面更大。
   **34 包若仍未讀，須說明何以再度延宕**（31 §K 第 2 項、32 §K 第 3 項
   已二度預先要求）。
3. **P0 73.1% 若判為不合理，其返工面可能大於 `design_method`** ——
   priority 影響交付件之優先序判讀，而 193 條為 P0。
   本包僅量測，未預設處置。
