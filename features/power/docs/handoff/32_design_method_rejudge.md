# 32 — design_method 全數重判與時點相依產物

下放包 | 分析層 → 執行層 | 往返 NN = 32

前置：31 包上繳已覆核，判定 **ACCEPT**。

**T24 經實測證實且遠重於分析層之推測**：
`狀態轉換` 佔 253 / 264 = 95.8% `[引用：31 §1.1]`；
五個 Test Set 中四個為 100%；
抽樣 43 條走查，**不符 26 條、不符率 60.5%** `[引用：31 §1.3]`。
分析層讀四條見二條可疑，實測為 26 / 43。

**分析層下達了破壞性指令**：R-P225 令「對全部產物逐一重跑」，
而 `b4_material.md` / `b5_material.md` 之語義為「產生**當時**待產出者為何」，
重跑必得空集 —— **不但不能證明現時性，反而摧毀該產物**。
執行層以 `git restore` 還原，其成因為分析層之指令，非自造損壞。

沿用不變之節：`§C 抽取規格` 同 02 包；`§I 禁區` 同 31 包 §I，另增列見本包 §I。

## A. 本包裁決條文（逐字，抄入 RULINGS.md）

```
[R-P226] `design_method` 全數重判，並補設「明示不轉換」閘門（G154）。
         31 §1.3 實測不符率 60.5%（26 / 43，抽樣率 17.4%，種子 31）。
         抽樣之不符率不得僅套用於抽中之 26 條 ——
         其餘 204 條之不符率應與之相當，**故須全數重判**。

         **重判不得由同一理解逕行為之** ——
         產生 95.8% 之理解與重判之理解若為同一，將重演偏向。
         方法：
         （a）將 §12 之**可機械判定者**實作為逐列謂詞
              （如第 1 列之「invalid / illegal / attempt to」、
                第 2 列之「disconnect / timeout / inject fault」、
                第 6 列之「limit / limit±1 / boundary / after … passes」），
              產出**首個命中列之提案**
         （b）機械提案與現值相同者，標「相符」，不改
         （c）**二者相異者，逐條人工裁決**並列出
              首個命中列、其謂詞命中之字串、以及改或不改之依據
         （d）機械無法判定者（如第 4 列「多條件 → 結果」），
              全數入人工裁決，不得由機械預設

         **G154（低成本高訊號）**：`expected_result` 含
         「明示不轉換」之措詞（`still reads` / `stays in` / `remains in` /
          `unchanged` / `does not change` 等，詞彙須自本語料導出，
          比照 R-P83）而 `design_method` 為 `狀態轉換` 者 → 觸發。
         以 `096` / `137` / `140` 為已知實例，須能重現。

         本包**產出提案與裁決**，**不逕行改值** ——
         改值影響交付內容，於 33 包依裁決結果一次為之。
         裁決者 Pei，逐字依據：「上繳」。
```

```
[R-P227] 腳本產物增第三類「時點相依」；重跑前須先判類。
         R-P220 立「重跑輸出位元組相同得作為產物非陳舊之證據」，
         R-P225 據以令全掃重跑 —— **二條皆隱含一個未言明之前提：
         該產物反映現況。**

         31 §3.1 之反例成立：`b4_material.md` / `b5_material.md`
         之語義為「產生當時待產出者為何」，
         **其重跑必得空集，不但不證現時性，反摧毀該產物**。

         增第三類，並訂重跑前之分類義務：
         （a）**現況型** —— 反映當下資料之狀態。
              重跑並比對為有效；輸出相同即證非陳舊（R-P220 適用）
         （b）**非決定性** —— 含時戳、排序不定等每次必異者。
              比對其實質內容而非位元組
         （c）**時點相依** —— 反映某一時點之狀態（如「當時待產出者」）。
              **一律不得重跑**；其現時性由「產生時點 ＋ 其後之異動紀錄」
              判定，非由重跑判定

         **任何全掃重跑之指令，執行前須先逐產物判類**，
         判為 (c) 者跳過並回報；
         **判類有疑義者一律跳過並上繳，不得試跑**。

         R-P220 / R-P225 依 R-P36 原文不改，各於其下加註指向本條。
         **本條之成因為分析層之指令未考慮產物語義**，責在分析層。
         裁決者 Pei，逐字依據：「上繳」。
```

```
[R-P228] 依指令執行所致之損壞：先備份，不得以 git 修復。
         31 包執行 R-P225 時毀損二檔，執行層以 `git restore` 還原。
         R-P149 禁「自造損壞以 git 修復」，
         **而本次之損壞係執行分析層指令所致，非自造** ——
         其性質有別，且執行層已據實揭露、範圍受限（二檔、比對無差異）。

         **本次不追究**，成因登記於分析層（R-P227）。

         往後之通則：
         （a）任何可能改寫既有產物之操作（重跑、批次改寫、格式轉換），
              **執行前須先將受影響檔案複製至 `sandbox/`**（不入版控）
         （b）損壞時**自該備份還原**，不動 git
         （c）無備份而已損壞者 → **停並回報**，由 Pei 決定
         （d）R-P149 對「自造損壞」之禁令不變

         此非加重執行層之負擔，而係使 git 不再是唯一之退路 ——
         **版本控管本不應承擔作業安全網之角色**。
         裁決者 Pei，逐字依據：「上繳」。
```

```
[R-P229] R-P159 之分層取樣於本專案近乎全讀，追認執行層之觀察。
         31 §二實測：P0 佔全批 73.1%（193 / 264）；
         「全部 P0 ＋ 每 leaf 至少一條」之取樣為 211 / 264 = 79.9%，
         實際節省僅 20.1%。

         追認該觀察。R-P159 立條時假設分層取樣可顯著節省覆核量，
         **該假設於本 feature 不成立** —— 因 P0 比例過高。

         處置：**不改變取樣範圍**（211 條仍須備料並由分析層讀），
         但明載其實為「近乎全讀」，
         使 R-P159 所稱之「以覆核深度換速度」在本 feature 不再成立 ——
         **本 feature 之覆核實為全讀，其代價為時間而非深度。**

         另記：P0 佔 73.1% 本身可疑 ——
         §10.2 之 P0 限於「安全、開機／復原、連線、音訊輸出、eCall、
         車輛關鍵 CAN 訊號、資料遺失風險」。
         此比例是否合理，**本包不裁**，列為 33 包之候選議題。
         裁決者 Pei，逐字依據：「上繳」。
```

```
[R-P230] 反覆陳舊之產物須改為自動產生或標為不可引用。
         `g136_pattern_variants.md` 為**第三次**判為陳舊
         （31 §三）。其陳舊形態非數值過期，
         而係總括語句對新增之腳本已為假 —— 逐位元組比對抓不到。

         同一產物三次陳舊，即證其維護方式不可靠。
         處置二擇一：
         （a）改為**完全自動產生** —— 其總括語句亦由腳本依實測生成，
              不得有人工撰寫之概括斷言
         （b）於檔首標明「**本檔含人工撰寫之概括語句，
              引用前須逐句複核其現時真確性**」，並禁止其被
              其他文件引用為證據

         擇 (a) 者須說明其總括語句如何自實測導出；
         擇 (b) 者須列出現行全部人工概括語句供逐句複核。
         裁決者 Pei，逐字依據：「上繳」。
```

## B. 本包須產出

### B1. `design_method` 全數重判之提案與裁決（R-P226）—— **最高優先**

（a）§12 逐列謂詞實作；（b）機械提案 vs 現值之比對；
（c）相異者逐條人工裁決並列首個命中列、命中字串、依據；
（d）機械無法判定者全數入人工裁決。
**不逕行改值。**

### B2. G154 實作（R-P226）

詞彙自本語料導出；以 `096` / `137` / `140` 為已知實例證明可重現；
fixture 證明會 FAIL。

### B3. 產物分類（R-P227）

`data/` 下 66 個產物逐一判 (a)/(b)/(c)；
判為 (c) 者列出並註明其時點語義；
為 R-P220 / R-P225 加註（依 R-P36），雜湊佐證原文未變。

### B4. `g136_pattern_variants.md` 處置（R-P230）

擇 (a) 或 (b) 並實施；擇 (b) 者列出全部人工概括語句。

### B5. 備份機制（R-P228）

實作「改寫前先備份至 `sandbox/`」；回報其形式與覆蓋範圍。

## D. 閃點

G0 為前置閘。G0–G153 沿用（G17 已移除；G109 不實作），期望值不變。

| # | 項目 | 期望值 |
|---|---|---|
| G154 | 明示不轉換而標狀態轉換（R-P226） | `096` / `137` / `140` 皆能重現；fixture 證明會 FAIL；【實測填入】全批觸發數 |
| G155 | `design_method` 機械提案 vs 現值（R-P226） | 【實測填入】相符數 / 相異數 / 機械無法判定數；相異者皆有人工裁決 |
| G156 | 產物分類（R-P227） | 66 個逐一判類；(c) 型已列出並註明時點語義 |
| G157 | 備份機制（R-P228） | 改寫前備份已實作；回報其覆蓋範圍 |
| G70 | lint 全閘 | 全 PASS |

## E. framework

§E 已定版（R-P35），本包不動。
**寫回仍不開放** —— 阻斷條件為 R-P226 之重判結果（33 包改值後方解除）。

## F. Anomaly 異動

開新號前先查現行最大號並回報（R-P147）。

- 新增：`design_method` 不符率 60.5%，全批偏向狀態轉換（R-P226）
- 新增：R-P220 / R-P225 隱含「產物反映現況」之未言明前提（R-P227）
- 新增：分析層之全掃重跑指令具破壞性，致二檔毀損（R-P227）
- 新增：`g136` 第三次陳舊，其形態為人工概括語句失真（R-P230）
- 新增：P0 佔 73.1%，其合理性未裁（R-P229）

## G. DATA_REQUESTS

DR-PW9 ~ DR-PW15 沿用。無新增。

## H. 作業指示

1. G0 前置閘
2. 查現行最大號並回報
3. **B3 產物分類先行** —— 判類完成前不得執行任何重跑（R-P227）
4. B5 備份機制實作，驗 G157
5. B1 `design_method` 全數重判之提案與裁決，驗 G155 —— **不逕行改值**
6. B2 G154 實作，驗 G154
7. B4 `g136` 處置
8. 以 §D 全表自驗
9. **抄錄裁決條文前重驗一次 §J 自檢**（R-P200(c)）；
   §A 五條逐字抄入 RULINGS.md；§F 入 ANOMALIES.md
10. 上繳 `features/power/docs/upstream/32_design_method_rejudge.md`，更新 `docs/INDEX.md`

## I. 禁區

沿用 31 包 §I 全部條目，另增列：

- **判類完成前不得執行任何產物重跑**（R-P227）
- **時點相依型產物一律不得重跑；判類有疑義者跳過並上繳**（R-P227(c)）
- **不得逕行變更任何 `design_method` 之值**（R-P226）
- **依指令所致之損壞不得以 git 修復；無備份而已損壞者停並回報**（R-P228）

## J. 本包產生之新條文清單（自檢）

1. R-P226 `design_method` 全數重判，補設 G154
2. R-P227 產物增第三類「時點相依」；重跑前須先判類
3. R-P228 依指令所致之損壞：先備份，不得以 git 修復
4. R-P229 R-P159 之分層取樣於本專案近乎全讀，追認
5. R-P230 反覆陳舊之產物須自動產生或標為不可引用

逐條確認：**五條**，皆以獨立 fenced block 呈現於 §A，未夾於敘述中。
自檢：§A 區塊數 = 5、§J 列數 = 5、§H 步驟 9 寫「五條」，三處一致。
本次已逐一數過 §A 之 fenced block。
**本包下放後分析層不再修改**（R-P200(a)）。

## K. 分析層自判：本包是否仍有該驗而未驗者

**有，三項。**

1. **R-P226 之機械謄詞由執行層實作，其與 §12 之對應由執行層自判。**
   與 95.8% 之成因為同一環節。機械化降低了逐條之隨意性，
   **未消除「對 §12 之理解有偏差」之風險** ——
   若謂詞本身寫偏，全批會一致地偏向另一個值。
   分析層將於 33 包以原始素材複核相異之逐條裁決。
2. **P0 佔 73.1% 之合理性未裁**（R-P229 末段）。
   §10.2 之 P0 定義為窄類，而本 feature 近四分之三為 P0 ——
   若該比例不合理，將影響交付件之優先序判讀。列 33 包候選。
3. **分析層對 B2 之 211 條取樣仍未讀。**
   31 包已備料（27 段、十六欄全附），
   **分析層本包未讀** —— 因 R-P226 之重判面更大且為寫回之阻斷項。
   若 33 包仍未讀，須說明其何以再度延宕（31 §K 第 2 項已預先要求）。
