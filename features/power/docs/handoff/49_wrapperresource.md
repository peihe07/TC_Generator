# 49 — WrapperResource 入庫、禁區解除與 SWE-PM-001–009 產出

下放包 | 分析層 → 執行層 | 往返 NN = 49

前置：48 包之執行狀態不影響本包。

**Pei 已提供 DR-PW6 所缺之二份資源**，分析層已於沙箱完成轉檔。
`SWE-PM-001`–`009` 之阻斷因而解除。

沿用不變之節：`§C 抽取規格` 同 02 包；`§I 禁區` 同 48 包 §I，**惟依 R-P318 解除二項**。

## A. 本包裁決條文（逐字，抄入 RULINGS.md）

```
[R-P317] DR-PW6 所缺之二份資源已到，其來源與轉檔紀錄。
         Pei 提供二檔：
         `4942177-_CFTSMV009_CIP_R4_O829_4_inline.rtf`（10,083,195 bytes）
         `4942178-_CFTSMV009_CIP_R4_O1584_5_inline.rtf`（1,048,356 bytes）

         **其檔名前綴 `4942177` / `4942178` 為 Polarion 之附件 work item 號，
         與其所屬之需求錨點 `4941354` / `4941355` 不同** —— 二者不得混用。

         內容經轉檔後確認：**皆為 Visio 圖（`Visio.Drawing.11`）**，
         以 OLE 物件內嵌於 RTF；
         `O829` 為 **TLM 完整電源狀態機圖**
         （含 `FULL OPERATION` / `TIMED` / `IDLE` / `SLEEP` / `STANDBY` /
          `BENCH` / `PARTIAL` / `LOGISTIC` / `LOGISTIC IDLE` 全部狀態、
          轉換條件、圖例與註記）。

         **轉檔路徑（分析層於沙箱執行，供入庫登記）**：
         ```
         RTF → 抽 \*\objdata 之 hex → OLE2 compound file
             → OlePres000 stream → 自 offset 40 之標準 WMF 簽章
               （01 00 09 00）切出
             → LibreOffice --convert-to png → JPEG
         ```
         工具：`olefile 0.47`、`LibreOffice`（`/usr/bin/soffice`）、
         `Pillow 12.1.1`；OS：沙箱 Linux。

         **產出之雜湊（前 16 碼）**：
         `O829_std.wmf` 1,570,764 bytes `c28c19e4ebac0336`
         `O1584_std.wmf` 131,198 bytes `0ed3550c5287d384`

         **轉檔曾三度失敗，其形態記明**（A-PW 待編）：
         （a）EMF 記錄鏈只解析到 **1.0%** 即斷（起點偏移錯）
         （b）LibreOffice 直接讀外層 EMF 得**空白頁**（5,983 bytes）
         （c）`WMFC` 標頭欄位對齊失敗
         **有效之作法為「忽略外層包裝，直接找 `01 00 09 00` 標準 WMF 簽章」。**
         此三失敗皆**不報錯而產出看似合法之結果**（空白 PNG、1% 解析），
         與 A-PW280 之單向差集同型。
         裁決者 Pei，逐字依據：「你入庫之後自己轉檔」。
```

```
[R-P318] R-P39 / R-P48 之禁區於本 feature 解除，其範圍限定。
         R-P39 明禁「不解 RTF」，R-P48 明禁「解析 RTF 或 OLE stream 之內容」。
         **分析層於本包所為正是其所禁者。**

         **Pei 之「自己轉檔」構成授權**，惟須落為條文，
         否則執行層依禁區辦事時將與現況矛盾。

         **解除之範圍限定**：
         （a）僅及於**Pei 所提供之 WrapperResource 資源檔**
              （現為該二份；日後另有提供者一併適用）
         （b）僅及於**為取得其可讀內容之轉檔**；
              **不及於**對 CFTS 本文其餘 29 處懸空參照之主動解析
              —— 該 29 處依 R-P42 不在測試範圍內（06 包已裁）
         （c）R-P39 / R-P48 之其餘效力不變

         R-P39 / R-P48 依 R-P36 原文不改，各於其下加註指向本條。
         裁決者 Pei，逐字依據：「你入庫之後自己轉檔」。
```

```
[R-P319] 二份資源入庫；依 R-P13 ＋ R-P23 登記。
         **原始檔**入 `features/power/inputs/`（不入版控）。
         **轉出物**入 `features/power/inputs/derived/`。

         台帳須登記（R-P13 三欄 ＋ R-P23 第四欄）：
         （a）原始檔之完整路徑、bytes、SHA256 **全 64 碼**
         （b）衍生物之 bytes、SHA256 全 64 碼
         （c）轉換工具與**完整轉換指令**
         （d）轉換工具之版本與 OS 版本

         **分析層所報之雜湊為沙箱副本實測值**（前 16 碼）；
         **執行層須於入庫後重測全 64 碼**，
         **不一致即停並上繳** —— 此為 02 包 A-PW08 之教訓
         （沙箱副本曾與原始檔差 67.8 倍）。

         **轉出物之可重現性**：執行層須以 §R-P317 所載之路徑
         **獨立重跑一次**，比對其輸出與分析層所產之 WMF 是否位元組相同；
         不同者列出差異並說明。
         裁決者 Pei，逐字依據：「你入庫之後自己轉檔」。
```

```
[R-P320] `SWE-PM-001`–`009` 之產出：圖形資料之使用界線。
         DR-PW6 之阻斷解除，該 9 個 leaf 得以產出 TC。

         **惟圖形資料之使用有其界線，須明載**：
         轉出之 WMF / JPEG 為**向量圖**，其文字標籤可抽取，
         **而標籤之間的關係（何狀態經何條件轉至何狀態）在圖形之連線中**，
         **不在文字層**。

         故：
         （a）**得為之**：確認該 9 個 leaf 之行為於規格中確有依據；
              抽出具名訊號與狀態名；產生 `specification_reference`
         （b）**不得為之**：自文字標籤之散列**推想轉換關係** ——
              例如同時抽到 `TIMED`、`RemoteStart becomes Active`、
              `FULL OPERATION` 三個標籤，
              **不足以斷定三者為同一條轉換**
         （c）轉換關係之判讀**須以人讀圖為之**；
              執行層於撰寫 TC 時如需某轉換之確切條件而圖中不明者，
              **停並上繳，不得推想**

         此界線與 §8.4.1「不得造值」同源 ——
         自散列標籤推想關係即為造一個規格未明載之事實。
         裁決者 Pei，逐字依據：「你入庫之後自己轉檔」。
```

```
[R-P321] 本批之產出與寫回之關係。
         現行交付副本（`output/`，261 列）已依 R-P309 之授權寫回，
         **其內容為 260 條 ＋ 留白列，不含 `SWE-PM-001`–`009`**。

         **R-P309 之授權不及於本批** ——
         其逐字載「若其後內容再有實質變動（TC 增減…），
         本授權不及於變動後之版本，須重新取得」。

         故本批之處置二擇一，**由 Pei 裁定**：
         （甲）**併入現行交付** —— 產出後與現行 260 條合併重排、
              重新寫回、重新取得授權；交付為單一份 112 leaf 之工作簿
         （乙）**另批交付** —— 現行 260 條維持不動，
              `SWE-PM-001`–`009` 之 TC 另成一批，日後另行寫回

         **分析層建議（甲）**：其理由為 —— 客戶收到二份不同時點、
         不同涵蓋範圍之同名工作簿，其比對成本高於一次重寫；
         且 R-P113(d) 已定「寫回為單次操作」。
         **惟此屬交付形式，分析層不自裁。**

         **本包只產出 TC，不寫回。**
         裁決者 Pei，逐字依據：「你入庫之後自己轉檔」。
```

## B. 本包須產出

### B1. 二份資源入庫與雜湊重測（R-P319）—— **先行**

原始檔入 `inputs/`、轉出物入 `inputs/derived/`；
台帳四欄齊備；**重測全 64 碼，不一致即停**。

### B2. 轉檔之獨立重跑（R-P319）

依 R-P317 之路徑獨立重跑；比對輸出是否與分析層所產位元組相同。

### B3. R-P39 / R-P48 加註（R-P318）

依 R-P36 原文不改；雜湊佐證。

### B4. `SWE-PM-001`–`009` 之 TC 產出（R-P320）

依 §1–§13 全部 TC 內容規則；
`specification_reference` 指向 CFTS009 §1.6.2.1；
**轉換關係不明者停並上繳，不得推想**；
tc_id 為臨時號，自現行末號之後接續。

### B5. 反向涵蓋與全閘

本批跑反向涵蓋（三透鏡 ＋ G113）與完整 lint；
依 R-P127 分桶、依 R-P180 標示盲測指標之適用性。

### B6. DR-PW6 之結案登記

其影響面（9 leaf / 2 錨點）已解除；於 `DATA_REQUESTS.md` 標記結案並註明來源。

## D. 閃點

G0 為前置閘。G0–G224 沿用（G17 已移除；G109 不實作），期望值不變。

| # | 項目 | 期望值 |
|---|---|---|
| G225 | 資源入庫（R-P319） | 二份原始檔 ＋ 轉出物已入庫；台帳四欄齊備；全 64 碼相符 |
| G226 | 轉檔重跑（R-P319） | 【實測填入】獨立重跑之輸出與分析層所產是否位元組相同 |
| G227 | 本批 TC 產出（R-P320） | 9 leaf；`spec_reference` 全部指向 CFTS009 §1.6.2.1；停並上繳之項已列 |
| G228 | 本批反向涵蓋（R-P321） | 【實測填入】行為項／已覆蓋／無對應；三桶計數 |
| G70 | lint 全閘 | 全 PASS |

## E. framework

§E 已定版（R-P35）—— **本批之 9 leaf 屬 Power State，其 leaf 數不變**
（該 9 leaf 本即計入 63，僅先前未產出）。
**寫回不在本包**（R-P321）。

## F. Anomaly 異動

開新號前先查現行最大號並回報（R-P147）。

- 新增：轉檔三度失敗而皆不報錯（空白 PNG、1% 解析、欄位對齊）；
  與 A-PW280 之單向差集同型（R-P317）
- 新增：附件 work item 號與需求錨點號不同，不得混用（R-P317）
- 新增：圖形資料之標籤散列不足以斷定轉換關係（R-P320）
- DR-PW6 → 依 B6 結案

## G. DATA_REQUESTS

**DR-PW6 → 結案**（資源已到）。
DR-PW5 / DR-PW9 / DR-PW11 等仍 live，其阻斷之 leaf 不在本批。

## H. 作業指示

1. G0 前置閘
2. 查現行最大號並回報
3. B1 入庫與雜湊重測 —— **先行；不一致即停**
4. B2 轉檔獨立重跑，驗 G226
5. B3 R-P39 / R-P48 加註
6. B4 `SWE-PM-001`–`009` 之 TC 產出，驗 G227 —— **不得推想轉換關係**
7. B5 反向涵蓋與全閘，驗 G228
8. B6 DR-PW6 結案登記
9. 以 §D 全表自驗
10. **抄錄裁決條文前重驗一次 §J 自檢**（R-P200(c)）；
    §A 五條逐字抄入 RULINGS.md；§F 入 ANOMALIES.md
11. 上繳 `features/power/docs/upstream/49_wrapperresource.md`，更新 `docs/INDEX.md`

## I. 禁區

沿用 48 包 §I 全部條目，**惟依 R-P318 解除 R-P39 / R-P48 對本批資源之限制**；
另增列：

- **不得自文字標籤之散列推想轉換關係**（R-P320(b)）
- **轉換關係不明者停並上繳**（R-P320(c)）
- **不得寫回**（R-P321）
- **不得主動解析其餘 29 處懸空參照**（R-P318(b)）
- **不得混用附件 work item 號與需求錨點號**（R-P317）

## J. 本包產生之新條文清單（自檢）

1. R-P317 DR-PW6 資源已到；其來源與轉檔紀錄
2. R-P318 R-P39 / R-P48 之禁區限定解除
3. R-P319 二份資源入庫；雜湊須重測全 64 碼
4. R-P320 `SWE-PM-001`–`009` 產出；圖形資料之使用界線
5. R-P321 本批與寫回之關係；併入或另批由 Pei 裁定

逐條確認：**五條**，皆以獨立 fenced block 呈現於 §A，未夾於敘述中。
自檢：§A 區塊數 = 5、§J 列數 = 5、§H 步驟 10 寫「五條」，三處一致。
本次已逐一數過 §A 之 fenced block。
**本包下放後分析層不再修改**（R-P200(a)）。

## K. 分析層自判：本包是否仍有該驗而未驗者

**有，三項。**

1. **分析層之轉檔由其一人所為，無第二方複核。**
   R-P319 令執行層獨立重跑並比對，**惟其重跑之依據為分析層所載之路徑** ——
   若該路徑本身有誤（例如切出之 WMF 少了尾段），
   重跑會一致地錯。**其唯一之外部檢查為人讀該圖是否完整。**
2. **`O1584`（錨點 `4941355`）之內容分析層未定性。**
   僅確認其為 Visio 圖並轉出，**未判讀其主題**。
   其對 9 個 leaf 之貢獻為何，本包未查。
3. **DR-PW6 結案後，仍有 3 張 live DR 阻斷 2 個 leaf**
   （`SWE-PM-003` 另受 DR-PW5、`SWE-PM-008` 另受 DR-PW11）——
   **該 2 leaf 雖在本批範圍內，其阻斷未解除**；
   執行層須逐一確認其是否仍應排除，並於上繳回報。
