# 07 — Phase 4 前置閘門與量測工具強化

下放包 | 分析層 → 執行層 | 往返 NN = 07

前置：docs/upstream/06_scope_boundary.md 已覆核，判定 ACCEPT。
Q4（檔案損傷覆核）由分析層獨立完成，**通過** ——
`01_intake.md` §E 弱點段句子連續無斷點、註記落於句後、
§E 補列與定版分布皆相符、未見其他非預期變動。

本包不寫 TC。任務為：把「有條文而無執行期驗證」的缺口補上，
並強化量測工具之可回歸性，使 Phase 4 得以開始。

## A. 本包裁決條文（逐字，抄入 RULINGS.md）

```
[R-P52] **R-P42 之執行期閘門**（Phase 4 lint，跨 feature 通則候選）。
        R-P42 規範 Phase 4 撰寫 TC 之行為，現況為「有條文、無驗證」。
        A-PW27 所登記之未被引用錨點即為可機械檢查之黑名單。
        Phase 4 之 lint 須增設閘門：
        （a）任一 TC 之 `specification_reference` 命中黑名單錨點 → FAIL
        （b）任一 TC 之內容（procedure / ER / pre_conditions）
             逐字引用黑名單錨點之特徵字串 → FAIL
        （c）黑名單須由腳本自 `layer3_full.tsv` 與 037 引用集自動導出，
             **不得人工維護** —— 人工清單會隨 leaf 增修而失效。
        本閘須於 Phase 4 開始**之前**就位，非事後補設。
        依據：05 包已證判讀單位錯誤會產出實質錯誤之結論
        （§1.6.2.1.15.1 由「部分涵蓋」翻為「涵蓋」）；
        同型風險於 TC 撰寫階段無任何攔截。
        裁決者 Pei，逐字依據：「好出」（回應 06 Q1）。
```

```
[R-P53] B1 懸空參照之歸屬規則須獨立驗證。
        現行規則「一處參照歸屬於其位置之前最近之需求錨點」
        為執行層依 §C rule 2 之同構性自訂，實測 31/31 全數可歸屬、
        0 個不可判定 —— 此 100% 之結果本身即為可疑訊號。
        驗證方法：對每一處 `WrapperResource`，量測其在原始 XML 中
        與**前一個**需求錨點及**後一個**需求錨點之段落索引距離。
        若存在任一處距離後者顯著近於前者，該規則即有反例。
        DR-PW6 之影響面（2 處 / 9 leaf / 1 Test Set）繫於此規則。
        裁決者 Pei，逐字依據：「好出」（回應 06 Q2）。
```

```
[R-P54] A-PW29 之 EE Architecture 分布**維持留白，判準留存**。
        既有政策 R30-3 / R30-4（Functional Safety 欄填 `NA`、
        車型欄 T–Z 留白）為 Privacy feature 所立，依 §5a 第 17 條
        既有政策優先，本包不予推翻。
        A-PW29 所登記者為「G25 具備填欄判準之能力」，
        非「現在即據以填欄」。
        Phase 4 若需改填，走另案裁定，不得以 A-PW29 之存在
        逕行視為授權。
        裁決者 Pei，逐字依據：「好出」（回應 06 Q3）。
```

```
[R-P55] G28 / G29 / G30 之基線值寫回腳本，使腳本自身成為回歸閘。
        `build_dangling.py`、`build_ole_census.py`、`build_vcvm.py`
        現為一次性量測，輸出即基線，無任何斷言可偵測回歸。
        06 §六之損傷即「無自我檢查即沉默失敗」之實例 ——
        救它的是 G31 之雜湊驗證，非腳本自身。
        三支腳本須內建期望值常數與比對，不符即非零退出。
        裁決者 Pei，逐字依據：「好出」（回應 06 Q5）。
```

```
[R-P56] 三張 DR 之 Urgency 認可：
        DR-PW5 **High**（§1.6.2.1.4 Stolen Vehicle Mode，
          範圍內而未被涵蓋，直接影響 `SWE-PM-003` 之 TC 可否撰寫）
        DR-PW6 **Medium**（懸空參照，leaf 層交叉後影響面收窄為
          2 處 / 9 leaf / 1 Test Set）
        DR-PW7 **Low**（VC/VM 二欄合觀 0/115 不可執行，不阻斷任何 leaf）
        三者之判定依據皆有實測支撐，認可。
        另記：R-P45「先交叉再發 DR」直接改變了 DR-PW6 之 Urgency ——
        表面 31 處/16 章看似 High，交叉後為 Medium。該條之價值就此驗證。
        裁決者 Pei，逐字依據：「好出」（回應 06 Q6）。
```

```
[R-P57] G28 之 `SIGNALS` 正則精度須量化。
        現況偽陰性 4 筆（`CS.00244`、`Timeout1`、`HIL` ×2 未被識別）、
        人工覆寫率 5.2%（6/115），代表性未評估。
        偽陰性全部集中於「命名慣例不符預設識別式」，
        意味該正則對本 feature 之命名慣例調校不足，
        跨 feature 套用時偽陰性率不可預期。
        須以 037 全欄之實際 token 分布重新推導識別式，
        並回報調校後之覆寫率。
        基線（0/115 不可執行）在精度量化前**維持有效**，
        但不得作為跨 feature 之通用基線。
        裁決者 Pei，逐字依據：「好出」（回應 06 §七第 3 項）。
```

```
[R-P58] 分析層於 R-P44 稱 §1.6.2.1 為「影響面最大之單一章節」，
        實測為**唯一有影響面之章節**（29/31 處落在未被引用錨點下）。
        登記為 A-PW30。此為分析層第四次高估／誤述
        （前三次：A-PW18 證據誤植、A-PW19 數值失效、A-PW21 來源誤植）。
        四次皆非結論錯誤，而是**修飾語或出處與實測不符**。
        往後分析層於下放包中之量化修飾語（「最大」「唯一」「多數」等）
        須標明其依據為實測或推測；無實測依據者一律寫「推測」。
        裁決者 Pei，逐字依據：「好出」。
```

```
[R-P59] R-P36 之實際效用超出立條理由，登記備忘。
        立條理由為「保持逐字記錄之意義」；
        06 §六顯示其實際作用為**偵測靜默之檔案損壞** ——
        G31 之雜湊比對當場攔下註記插入句中之損傷。
        與 A-PW29（R-P40 之實際用途超出立條理由）同型。
        登記為 A-PW31：條文之價值可能超出其立條理由，
        評估是否裁撤既有閘門時，不得僅以立條理由是否仍成立為判準。
        裁決者 Pei，逐字依據：「好出」。
```

（以上**八條**裁決條文，抄入 RULINGS.md 時逐字保留，
 每條獨立區塊，不得夾於敘述中。）

## B. 本包須產出

### B1. R-P42 黑名單之自動導出（R-P52(c)）

  腳本 `build_blacklist.py`，自 `layer3_full.tsv` 與 037 引用集導出
  輸出 `data/unreferenced_anchors.tsv`：
    anchor_id / cfts / chapter_num / chapter_title / 所屬章節是否被任一 leaf 觸及
  須涵蓋**全部** CFTS 章節，不限於 A-PW27 之九章
    —— A-PW27 之 13 個為九章內之子集，非全集
  回報全集數量，並與 A-PW27 之 13 個對照說明差額

### B2. Phase 4 lint 閘門實作（R-P52(a)(b)）

  於 lint 增設 `check_rp42_unreferenced_anchor`
  （a）`specification_reference` 命中黑名單 → FAIL
  （b）TC 內容逐字引用黑名單錨點之特徵字串 → FAIL
       特徵字串之抽取方式由執行層自裁，須於上繳包說明並舉例
  **以合成 fixture 測試該閘**（不得以 repo 現況為測試對照）：
    至少一個應 PASS、一個應因（a）FAIL、一個應因（b）FAIL
  本包不執行 Phase 4，僅令閘門就位並通過 fixture 測試

### B3. 懸空參照歸屬規則之獨立驗證（R-P53）

  對 31 處逐一量測：與前一需求錨點、後一需求錨點之段落索引距離
  列出距離後者較近者（若有）
  若存在反例，重算 DR-PW6 之影響面並更新該 DR
  若無反例，說明 100% 歸屬率之成因

輸出至 `data/b3_dangling_rule_check.md`。

### B4. `SIGNALS` 正則調校（R-P57）

  自 037 之 `Verification Criteria` / `Verification Method` 兩欄
    抽取全部 token，統計命名形態分布
  據此重新推導識別式，涵蓋 `CS.00244` / `Timeout1` / `HIL` 等形態
  回報調校前後之人工覆寫率
  重跑 G28，回報基線是否改變

輸出至 `data/b4_signals_calibration.md`。

### B5. 三支腳本內建回歸斷言（R-P55）

  `build_dangling.py`：31 處 / 16 章 / 受影響 2 錨點 / 9 leaf
  `build_ole_census.py`：目錄項 14、ObjectPool 不存在、`\x01Ole` = 0
  `build_vcvm.py`：二欄合觀 0/115 不可執行（若 B4 改變基線，以新值為準）
  不符即非零退出並列出差異

## C. 抽取規格

  §C rule 1 / 2 / 3 / 4 正則不變。
  R-P17 文字層定義不變。
  B4 所調校者為 G28 之 `SIGNALS` 判準，非 §C 正則。

## D. 閃點

G0 為前置閘。G0–G16、G13b、G18–G31 沿用（G17 已移除），期望值不變。

| # | 項目 | 期望值 |
|---|---|---|
| **G32** | R-P42 黑名單全集（R-P52(c)） | 【實測填入】全 CFTS 未被引用錨點數；與 A-PW27 之 13 個之關係須說明 |
| **G33** | lint 閘門 fixture 測試（R-P52(a)(b)） | 三個 fixture：1 PASS、1 因(a)FAIL、1 因(b)FAIL，**三者皆須實際觸發** |
| **G34** | 懸空參照歸屬規則（R-P53） | 【實測填入】距離後錨點較近者之處數；若 > 0 則 DR-PW6 影響面須重算 |
| **G35** | `SIGNALS` 調校後覆寫率（R-P57） | 【實測填入】調校前 5.2%（6/115）→ 調校後之值 |
| **G36** | 三支腳本回歸斷言（R-P55） | 三支皆內建期望值且不符時非零退出，以刻意改壞之輸入驗證其確實會失敗 |

G33 與 G36 之驗證條件：**該檢查項須確認其在該階段確實可能失敗**；
不可能失敗者標「未實測」而非 PASS。

## E. framework

§E 已定版（R-P35），本包不動。
R-P46 之補列已由 06 包落實並經分析層覆核通過。

## F. Anomaly 異動

  A-PW23 → 依 R-P48 / 06 §二確立為終值：二份 CFTS 皆零嵌入物件，
           形態為「參照存在而資源不存在」
  A-PW26 → 影響面依 06 §一更新為 2 處 / 9 leaf / 1 Test Set
  A-PW27 → 依 R-P52(c) 由人工清單改為腳本導出，標註來源變更
  新增 A-PW30：分析層第四次量化修飾語與實測不符（R-P58）
  新增 A-PW31：條文之價值可能超出其立條理由（R-P59）

## G. DATA_REQUESTS

  DR-PW1（SWE-PM-089 來源）→ live，High
  DR-PW3（4942087 歸屬）→ live，Medium
  DR-PW5 → live，High（R-P56 認可）
  DR-PW6 → live，Medium（R-P56 認可；影響面若因 G34 改變則重評）
  DR-PW7 → live，Low（R-P56 認可）
  DR-PW2、DR-PW4 → 維持撤回
  無新增

## H. 作業指示

  1. G0 前置閘
  2. 產出 B1 黑名單全集，驗 G32
  3. 實作 B2 lint 閘門並以 fixture 測試，驗 G33
  4. 產出 B3 歸屬規則驗證，驗 G34；若有反例，重算並更新 DR-PW6
  5. 產出 B4 `SIGNALS` 調校，驗 G35；若基線改變，更新 G28 與 DR-PW7
  6. 依 R-P55 為三支腳本加回歸斷言，驗 G36
  7. 以 §D 全表自驗
  8. §A 八條裁決逐字抄入 RULINGS.md；§F 入 ANOMALIES.md
  9. 上繳 features/power/docs/upstream/07_phase4_gates.md，更新 docs/INDEX.md

## I. 禁區

  不得寫回 FW036 workbook
  不得執行任何 git 操作（全數屬 Pei）
  不得以 openpyxl save 寫任何 xlsx（R16 凍結）
  不得補齊 SWE-PM-089（R-P1）
  不得沿用純文字衍生物之任何數字（R-P10）
  不得自行調整 §C 正則（B4 所調者為 G28 判準，非 §C）
  不得修改任何已落檔裁決條文之內文（R-P36）
  不得測試未被引用之錨點（R-P42）
  不得解析任何 RTF 或 OLE stream 之內容（R-P39、R-P48）
  不得續行章節層反向缺口調查（R-P37）
  不得變更 §E 之分布數字（R-P35）
  **不得以 A-PW29 之存在逕行填寫車型欄（R-P54）**
  **不得以 repo 現況作為 lint fixture 之測試對照（B2）**
  **本包不撰寫任何 TC**
  素材補入超出 features/power/inputs/ 需 Pei 裁定

## J. 本包產生之新條文清單（自檢）

  R-P52 R-P42 之執行期閘門，黑名單須腳本導出
  R-P53 懸空參照歸屬規則須獨立驗證
  R-P54 EE Architecture 維持留白，判準留存，既有政策優先
  R-P55 三支腳本內建回歸斷言
  R-P56 三張 DR 之 Urgency 認可
  R-P57 `SIGNALS` 正則精度須量化
  R-P58 分析層量化修飾語須標明實測或推測
  R-P59 條文價值可能超出立條理由，登記備忘

  逐條確認：**八條**，皆以獨立區塊呈現於 §A，未夾於敘述中。
  自檢：§A 區塊數 = 8、§J 列數 = 8、§H 步驟 8 寫「八條」，三處一致。
