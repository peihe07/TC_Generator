# 17 — 反向涵蓋、寫回路徑閘門與第二批前置

下放包 | 分析層 → 執行層 | 往返 NN = 17

前置：docs/upstream/16_write_order.md 已覆核，判定 **ACCEPT**。
dry-run 三閘證據升為「合成＋真實」；五條 DV 全數存活（含 x14）；
XML 層僅 `xl/worksheets/sheet6.xml` 一個 part 相異。

**分析層之兩項記載錯誤**（16 §八(丁)所指，逐字接受）：
  §J 自檢寫「四條」而追加 R-P117 後實為五條 ——
    此為分析層第三次於條數自檢出錯（前二次：四條寫成八條、六條寫成七條），
    而該自檢機制正為防此而設。
  A-PW73 / 74 / 75 與 15 包既有編號衝突 ——
    開新 anomaly 前未查現行最大號。執行層順延為 A-PW75 / 76 / 77，處置正確。

**本包三項（R-P118 / R-P119 / R-P120）為第二批之前置條件。**

## A. 本包裁決條文（逐字，抄入 RULINGS.md）

[R-P118] **反向涵蓋檢查：做成報告與人工裁決，不做成 pass / fail 閘門。**
        現況：G82 驗「ER 之具名標的是否出現於 `source_clause`」，
        **不問「`source_clause` 中有而 ER 中沒有的是什麼」**。
        R-P117 之三項缺口於 G67 覆蓋率 **21 / 21 = 100%** 之今日
        依然無任何閘門會發現；查出者為人讀規格原文逐句對照。
        此為覆蓋率與品質正交之第三次實證（首次為 A-PW64）。

        實作 `reverse_coverage.py`：
        （a）將每 leaf 之 `source_clause` 逐句拆為**行為項**，編號列出
        （b）逐項比對現有 TC，標為「已覆蓋（列出 tc_id）」或「無對應」
        （c）「無對應」者**不判 FAIL、不使 exit=1**，另列一節輸出
        （d）每一項「無對應」須**人工裁決**並登記，三選一：
             `真缺口`（須補 TC）／
             `規格未給門檻，不可獨立驗證`（不補 TC，登記）／
             `已由他條涵蓋`（註明 tc_id 與理由）
        （e）沉默不算裁決 —— 未經裁決之「無對應」項使該批不得結案

        機械化不了「涵蓋與否」，但機械化得了「把該問的問題列出來」。
        處置形態比照 R-P67 對 R-P42(b) 之分流。
        裁決者 Pei，逐字依據：「是」（回應 16 Q2）。

[R-P119] **`surgical_save` 補設閘門（G89）。**
        16 §八(甲)5 自陳：`verify_structure()` 係「未拋例外即當通過」，
        其三個檢查（zip member 集合、逐分頁 DV 計數、相異 part 集合）
        本身未經獨立驗證。
        **本專案唯一授權之寫回路徑，至今無任何閘門在驗它。**
        （a）為 `verify_structure()` 之三項檢查各建刻意弄壞之案例，
             證明其**確實會拋 `StructureError`**
        （b）另建正常案例證明其不誤拋
        （c）三項檢查若有任一無法以刻意弄壞觸發，標「未實測」而非 PASS
        裁決者 Pei，逐字依據：「是」（回應 16 Q3）。

[R-P120] **append 邊界保護為真實寫回之前置（G90）。**
        dry-run 僅驗 **BLANK 工作簿、十列、單次寫回**。
        真實寫回為 N 列、單次，且屆時工作簿內可能已有他人之列
        （Home 之 Arif 144 列即為先例）。
        **既有列之保護完全未測。**
        以合成之非 BLANK 副本（預先填入若干列並記錄其逐格內容與雜湊）
        執行寫回，驗證：
        （a）既有列之每一格內容位元組不變
        （b）新列自既有列之後起始，無覆蓋
        （c）B 欄序號與既有列銜接正確
        （d）刻意令新列起始位置重疊既有列 → 須 FAIL
        裁決者 Pei，逐字依據：「是」（回應 16 Q4）。

[R-P121] **`015` 之門檻值缺口開 DR-PW8（High）。**
        R-P117(c) 所補之 `015` 依 `voltage out of range conditions`
        撰寫，而該錨點僅載條件之**名稱**，未載電壓值；
        §8.4.1 不許造值，R-P42 不許往他錨點取值。
        結果為**一條寫得出來卻執行不了之 TC**。
        此類 TC 若以「已覆蓋」之姿態進入交付物，
        **較缺一條 TC 危險** —— 它使涵蓋率報表為真而測試無法執行。
        （a）開 DR-PW8，Urgency **High**
        （b）於 `015` 之 `remarks` 標明其待門檻值，
             使該狀態於工作簿內可見，不僅存於 DR 檔
        裁決者 Pei，逐字依據：「是」（回應 16 Q1）。

[R-P122] **`SWE-PM-072` 之「as soon as possible」登記為不可驗證。**
        `4942338` 載 `process them **as soon as possible**,
        depending on boot timings`，未給任何門檻。
        `006` 所測者為「開機期間即依轉換處理」，
        而「as soon as possible」在規格原文之寫法下**不可獨立驗證** ——
        依 §8.4.1 不得造值，亦無可判讀之判準。
        **不補 TC、不開 DR**，登記為 anomaly，
        使日後查閱者不致誤認為漏測。
        分析層已以 `source_clause` 原文獨立複驗
        `SWE-PM-071`（4 項行為 / 4 條 TC，全覆蓋）與
        `SWE-PM-072`（除本項外全覆蓋），
        16 §八(甲)3 所指之「同一判斷來源」風險就此解除。
        裁決者 Pei，逐字依據：「是」（回應 16 Q5）。

[R-P123] **R-P116 之裁定素材母體擴大至 Home 與 Privacy。**
        16 之 B4 僅查 Comfort，得「無此形態之先例」。
        15 包正因加入 **Home（Arif 144 列）** 而使 G77 之結論翻轉 ——
        同一教訓不得犯第二次。
        擴查 Home 與 Privacy 之已交付件，是否存在
        「僅填 `req_id` 而其餘欄留空」之列。
        **仍不得實作、不得建議** —— R-P116 屬交付形式，待 Pei 裁定。
        裁決者 Pei，逐字依據：「是」（回應 16 Q6）。

[R-P124] **第二批範圍：`Timeout Settings`（8 leaf）。**
        擇此為第二批之理由：
        （a）次小（8 leaf），續行「小批驗管線」之原則
        （b）**首次跨 CFTS009** —— 首批三 leaf 全在 CFTS010，
             CFTS009 之錨點鏈、章節解析、`specification_reference`
             產生路徑於真實 TC 撰寫中從未走過
        （c）含 `SWE-PM-057` —— R-P34 所裁之爭議 leaf
             （名稱指向 Power State、行為指向 Timeout Settings），
             其歸屬之正確性於此批可獲實際檢驗
        （d）不觸及 DR-PW5（`SWE-PM-003`，屬 Power State）
             與 DR-PW6（§1.6.2.1，屬 Power State）

        **R-P118 / R-P119 / R-P120 三項為本批之前置條件**，
        未全備不得開始第二批 ——
        否則第二批將以同一套看不見反向缺口之流程再產八個 leaf。
        裁決者 Pei，逐字依據：「是」（回應 16 Q2 / Q3 / Q4 之次序建議）。

（以上**七條**裁決條文，抄入 RULINGS.md 時逐字保留，
 每條獨立區塊，不得夾於敘述中。
 **抄入前須先查 RULINGS.md 與 ANOMALIES.md 之現行最大號**，
 避免重演 16 包之編號衝突。）

## B. 本包須產出

### B1. 反向涵蓋（R-P118）—— **最重要**

  `features/power/scripts/reverse_coverage.py`
  對現有三 leaf 之 `source_clause` 逐句拆行為項並比對 15 條 TC
  輸出 `data/reverse_coverage_batch1.md`：
    逐 leaf 之行為項編號表、覆蓋狀態、無對應項清單
  **對三 leaf 之無對應項逐一提出，並依 R-P118(d) 三選一裁決並登記**
  回報：行為項總數、已覆蓋數、無對應數、三類裁決之分布

  **驗證條件**：以 R-P117 之三項已知缺口為對照 ——
  若該腳本對**修補前**之資料跑不出那三項，即為判準不足，
  須說明其漏檢形態，不得逕行調整拆句規則使結果好看
  （比照 13 §七(丙)7 所指之「先看答案再定門檻」）。

### B2. `surgical_save` 閘門（R-P119 / G89）

  為 `verify_structure()` 三項檢查各建刻意弄壞之案例：
    zip member 集合不等、逐分頁 DV 計數不等、相異 part 集合過大
  證明其確實拋 `StructureError`；另建正常案例證明不誤拋
  無法以刻意弄壞觸發者，標「未實測」

### B3. append 邊界（R-P120 / G90）

  以合成之非 BLANK 副本（預填若干列，記錄逐格內容與雜湊）
  驗 (a)–(d) 四項
  **既有列之保護以逐格位元組比對為準**，不以「看起來沒變」為準

### B4. DR-PW8 與 `015` 標記（R-P121）

  寫入 `DATA_REQUESTS.md`：DR-PW8，High
  `015` 之 `remarks` 標明待 `voltage out of range` 門檻值
  重跑 lint（`remarks` 由留白改為有值，須確認不觸發任何閘）

### B5. R-P116 素材擴查（R-P123）

  Home 與 Privacy 之已交付件，`read_only=True`
  是否存在「僅填 `req_id` 而其餘欄留空」之列
  三個 feature 之結果併表
  **不得實作、不得建議**

### B6. A-PW 編號整理

  抄入本包之 anomaly 前，先列出現行最大號並回報

## C. 抽取規格

  §C rule 1 / 2 / 3 / 4 正則不變。
  R-P17 文字層定義不變。
  `MIN_FINGERPRINT = 40` 不變（R-P62）。

## D. 閃點

G0 為前置閘。G0–G16、G13b、G18–G87 沿用（G17 已移除），期望值不變。

| # | 項目 | 期望值 |
|---|---|---|
| **G91** | 反向涵蓋報告（R-P118） | 【實測填入】行為項總數／已覆蓋／無對應；**且對修補前資料須能重現 R-P117 之三項缺口** |
| **G89** | `surgical_save` `verify_structure`（R-P119） | 三項檢查各有刻意弄壞之案例確實拋 `StructureError`；正常案例不誤拋 |
| **G90** | append 邊界保護（R-P120） | 既有列逐格位元組不變；新列不覆蓋；B 欄銜接正確；重疊案例 FAIL |
| **G92** | `015` remarks 標記（R-P121） | `remarks` 有值且不觸發任何既有閘門 |
| **G93** | R-P116 素材三 feature 併表（R-P123） | 【實測填入】Comfort / Home / Privacy 各自之結果 |
| G70 | lint 全閘 | 全 PASS；leaf 仍 3；TC 15 |

G89 / G90 之驗證條件同 G33：**須確認其在該階段確實可能失敗**。

## E. framework

§E 已定版（R-P35），本包不動。
第二批範圍依 R-P124 為 `Timeout Settings`（8 leaf），**本包不產出其 TC**。

## F. Anomaly 異動

  **開新號前須先查現行最大號並回報（B6）。**
  新增：反向涵蓋方向於覆蓋率 100% 時仍無閘門（R-P118）
  新增：`surgical_save` 為唯一授權寫回路徑而無閘門（R-P119）
  新增：append 邊界保護未測（R-P120）
  新增：`015` 為可撰寫而不可執行之 TC（R-P121）
  新增：`SWE-PM-072` 之「as soon as possible」不可驗證（R-P122）
  新增：分析層第三次於條數自檢出錯（本包 §前言）

## G. DATA_REQUESTS

  **新增 DR-PW8**（High）—— `voltage out of range` 之門檻值
  DR-PW1 → live，High（併 R-P116 之待裁）
  DR-PW5 → live，High
  DR-PW3 / DR-PW6 → live，Medium
  DR-PW7 → live，Low
  DR-PW2、DR-PW4 → 維持撤回

## H. 作業指示

  1. G0 前置閘
  2. 依 B6 查現行 anomaly 最大號並回報
  3. 實作 B1 反向涵蓋，驗 G91 —— **最重要；須能重現 R-P117 三項缺口**
  4. 依 R-P118(d) 對三 leaf 之無對應項逐一裁決並登記
  5. 實作 B2 `surgical_save` 閘門，驗 G89
  6. 實作 B3 append 邊界，驗 G90
  7. 依 R-P121 開 DR-PW8 並標記 `015`（B4），驗 G92
  8. 依 R-P123 擴查 R-P116 素材（B5），驗 G93
  9. 以 §D 全表自驗
 10. §A 七條裁決逐字抄入 RULINGS.md；§F 入 ANOMALIES.md；§G 入 DATA_REQUESTS.md
 11. 上繳 features/power/docs/upstream/17_reverse_coverage.md，更新 docs/INDEX.md

## I. 禁區

  **不得開始第二批 —— R-P118 / R-P119 / R-P120 三項未全備前（R-P124）**
  **不得寫回客戶樹之 FW036 或 `inputs/` 之原始檔**
  **不得以 openpyxl `save()` 寫任何 xlsx**（R16 / R-G3）
  **不得指派最終 tc_id（R-P113(c)）**
  **不得實作或建議 R-P116 之任一處置（R-P123）**
  **不得為求 G91 結果好看而調整拆句規則（B1 驗證條件）**
  不得執行任何 git 操作（全數屬 Pei）
  不得補齊 SWE-PM-089（R-P1）
  不得沿用純文字衍生物之任何數字（R-P10）
  不得自行調整 §C 正則
  不得修改任何已落檔裁決條文之內文（R-P36）
  不得測試未被引用之錨點（R-P42）
  不得自行調整 §E（R-P35）
  不得依 Arif 素材自行改動 G77 或任何 TC（Q3 屬 Pei）
  不得以 repo 現況作為任何 fixture 之測試對照
  素材補入超出 features/power/inputs/ 需 Pei 裁定

## J. 本包產生之新條文清單（自檢）

  R-P118 反向涵蓋做成報告與人工裁決，不做 pass / fail
  R-P119 `surgical_save` 補設閘門（G89）
  R-P120 append 邊界保護為真實寫回之前置（G90）
  R-P121 `015` 之門檻值缺口開 DR-PW8（High）
  R-P122 `SWE-PM-072` 之「as soon as possible」登記為不可驗證
  R-P123 R-P116 素材母體擴大至 Home 與 Privacy
  R-P124 第二批範圍 `Timeout Settings`；三項前置未全備不得開始

  逐條確認：**七條**，皆以獨立區塊呈現於 §A，未夾於敘述中。
  自檢：§A 區塊數 = 7、§J 列數 = 7、§H 步驟 10 寫「七條」，三處一致。
  **本次自檢已逐一數過 §A 之 fenced block**（前三次於此出錯）。

---

## 上繳包必附

  一、B1 反向涵蓋報告，含「對修補前資料能否重現 R-P117 三項缺口」之結果
  二、三 leaf 無對應項之逐項裁決（三選一 ＋ 依據）
  三、B2 / B3 之刻意弄壞證明
  四、B5 三 feature 併表
  五、§D 全表實測值對照（含 G89–G93）
  六、**明確回答：R-P124 之三項前置是否全備，第二批可否開始**
  七、「本包是否仍有該驗而未驗者」之獨立判斷。
      16 §八列九項，本包處置：(甲)1→R-P121、(甲)2→R-P118、
      (甲)3→R-P122（分析層已獨立複驗）、(甲)4→R-P120、(甲)5→R-P119、
      (甲)6→R-P123、(乙)7 已妥、(乙)8 待 114 leaf 完成、(丙)9 已妥。
      執行層須自行判斷是否另有該驗而未驗者，不得只覆述此九項。
