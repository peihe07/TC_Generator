# 交付說明 —— Power Management（FW036）

依 **R-P314**（48 包）產出，**50 包依 R-P326 更新**。

> ## ⚠ 狀態更新（2026-08-24）：最新交付版為 §8.1h（389 條）
>
> §8.1h 以**新檔名並存**送達 `…_20260824.xlsx`，前二版原地保留。
> **③ 人讀覆核與 ④ Excel 實開抽驗皆經 Pei 明示跳過** —— 其未驗項
> 列於 §8.1h 之「本版之已知限制」表，須與本表並讀。
>
> ## ⚠ 狀態更新（2026-08-21）：§8.1g 為 283 條版之最終交付
>
> 送達沿革：§8.1（20260816）→ §8.1b（20260820，未登錄）→
> §8.1a（20260821 Revise）→ §8.1c（20260821 Revise2）→
> §8.1e（20260821）→ §8.1f（20260821）→
> **§8.1g（20260821，現行；四個長欄位全面改寫，覆蓋前一版）**。
> §8.1d「已知未完成項」除 A5 一項外仍然有效，須與 §8.1e 並讀。
> **§8.1g 另附「本版之已知限制」表**（PENDING 二處、DR-PW21、
> A-PM13／14／15、lint P=10），須一併讀。
> 註：`(Revise)` 與 `(Revise2)` 二檔已由 Pei 於 2026-08-21 17:06 前後
> 分別刪除與更名為 `…_20260821.xlsx`，故目錄現存二檔。
>
> 以下「尚未產出」之警語為 50 包當時之狀態，保留不刪。
>
> ## ⚠ 對應之交付副本**尚未產出**（已由 §8.1a 取代）
>
> 前一版副本（260 條）**已於 2026-08-19 作廢**（R-P322(a)），
> 移至 `output/superseded/SUPERSEDED-20260819__…xlsx`（**保留，不刪除**）。
> 新版（**283 條 / 284 列**）**待 Pei 就該版本明示授權後於 55 包寫回**（R-P323 → R-P331 → 54 包 §E）——
> **R-P309 之原授權不及於本次**（TC 260 → 280 且最終 `tc_id` 全數重新指派，屬實質變動）。
>
> 本說明所述之數字**皆為現行語料之實測值**，非既存副本之內容。

> **本說明之首要用途，是讓收件方知道這批測試案例「驗過什麼」與「沒驗過什麼」。**
> **全數閘門 PASS 不代表已全面驗證**（R-P247）——
> 各閘門所涵蓋之性質列於 §二，其未涵蓋者列於 §三，二者須並讀。

---

## 一、交付範圍

| 項 | 值 |
|---|---|
| 測試案例（TC） | **280** 條，`tc_id` `NR1L-PowerManagement-001` – `280` 連號 |
| 覆蓋之 leaf 需求 | **111 / 115** 個 |
| 工作簿列數 | **284**（283 條 ＋ `SWE-PM-089` 之留白列） |
| 列序 | 依 `(SWE-PM ID, split_index)`（R-P113 / R-P115） |

**Test Set 分布**：Power State **145**、Startup Display 59、Branding and Theme 34、
Timeout Settings 26、Power Down 16。

**`priority` 分布**：P0 **177**、P1 59、P3 37、P2 7。
**`design_method` 分布**：決策表 **159**、狀態轉換 88、功能測試 32、基礎故障注入 1。

（與前一版之差為第七批 20 條，全屬 Power State。）

### 1.1 未產出之 leaf —— **4 個**（前一版為 12 個）

> ⚠ **R-P326(b) 所列之四個與實測不符**，本表依實測書寫：
> 其列 **`SWE-PM-003`（DR-PW5）**，而該 leaf **已於第七批產出**
> —— DR-PW5 為 **advisory**，不阻斷；
> 其**漏列 `SWE-PM-010`（DR-PW11）**。
> 實測未產出者為 **`008` / `010` / `089` / `112`**
> （111 / 115 leaf 已產出，`SWE-PM-001`–`007`、`009` 之八個已由第七批解除）。

| SWE-PM ID | blocking DR | 說明 |
|---|---|---|
| `SWE-PM-008` | **DR-PW11** | 其被引用錨點 `4941425` / `4941430` / `4941433` 於 CFTS 文字層皆無內文段落。DR-PW6 雖已結案，此阻斷仍在 |
| `SWE-PM-010` | **DR-PW11** | 其被引用錨點 `4941984` 於 CFTS 文字層無內文段落 |
| `SWE-PM-089` | **DR-PW1** | 其上游來源不明（**於工作簿中保留一列空白**，見下） |
| `SWE-PM-112` | **DR-PW9** | 其範圍歸屬未定（`HARMAN Status` 為 `Need rework`） |

**`SWE-PM-089` 之留白列**（R-P141）：該列僅填 `req_id`，其餘欄留白，
B 欄序號照常計入。其用意為使「此項尚未解決」於交付物本身可見，
而非僅存於 `DATA_REQUESTS.md`。**DR-PW1 獲解後，該列依當時之裁決填寫或移除。**

### 1.2 DR-PW6 之結案界線（R-P326(c)）

DR-PW6 已於 49 包**結案**，惟**僅就其阻斷面** ——
其原載 **31 處**懸空 `WrapperResource` 參照，Pei 所提供者為
**阻斷 `SWE-PM-001`–`009` 之二份**（錨點 `4941354` / `4941355` 之實體）。
**其餘 29 處未獲提供**；依 **R-P42**（06 包）該 29 處落在未被引用之錨點下，
**不在測試範圍內**。

**即：DR-PW6 就其阻斷面結案，非就其全部 31 處參照結案。**

### 1.3 圖形資料之使用界線（R-P326(e)）

上開二份資源為 **Visio 狀態機圖**（`O829` / `O1584`），已轉出 WMF / JPEG。
其用途**限於確認第七批 8 個 leaf 之行為於規格中確有依據**；
**第七批之 TC 全部取自 CFTS009 §1.6.2.1.x 之文字錨點，未使用圖之連線關係**
（R-P320(b) 明禁自標籤之散列推想轉換關係）。

> **⚠ 此界線之遵守無閘門可證。** 圖之連線關係無工具可查，
> 「未被用於推想」**係執行層之自述**，本專案未設亦無可設之驗證（49 §九第 1 項）。
> 收件方於評估第七批時應知此限制。

**因而刻意不驗者二項**（規格未給門檻，非真缺口）：
`SWE-PM-004` 之 **Timeout1 確切時長**（其節不在該 leaf 之錨點內）；
`SWE-PM-009` 之**電壓門檻與進出 INIT 之時間**（原文明載 `refer to SIS`，**SIS 不在本專案素材內**）。

### 1.4 前一版交付副本之作廢與三代號對照（R-P326(d)）

| 項 | 值 |
|---|---|
| 作廢者 | `output/superseded/SUPERSEDED-20260819__…xlsx`（261 列，`tc_id` 001–260） |
| SHA256 | `dc0d2ee0d46e75180add963be130eb59e66669202477716cfe44d7e46eefdb99`，106,319 bytes（移動前後位元組相同） |
| 作廢理由 | Pei 裁 R-P321（甲）併入第七批 |
| 取代者 | **54 包之 284 列版本 —— 已產出，尚未寫回** |

> ## ⚠ 號碼不得以相認 —— 實測 **260 / 260（100%）之最終號改變**
>
> 最終 `tc_id` 依 `(SWE-PM ID, split_index)` 指派，而第七批之 leaf 為
> `SWE-PM-001`–`009`，**其序在原有全部 leaf 之前**。
> 故舊副本之 `001`–`260` **無一條在新版維持同號**。
> **若已有人引用舊號碼，該號碼於新版指向另一條 TC。**
>
> 三代號對照表：`features/power/data/tcid_three_gen_50.md`
> ＋ `data/final_tc_id_map_50.tsv`（鍵為 `(req_id, tc_title)`）——
> 涵蓋（一）歷史臨時號（含 27 包全域重編、44 包合併）、
> （二）舊副本之最終號 001–260、（三）新最終號 001–280。
> A-PW299 之 **2 條鍵斷裂**（`SWE-PM-091` / `092` 之標題改寫）**已人工補齊**，
> 補齊後歷史階段之涵蓋為 **266 / 266**。

各 DR 之內容與狀態見 `features/power/DATA_REQUESTS.md`。

---

## 二、已驗證之性質

| 性質 | 閘門 |
|---|---|
| 素材身分與錨點集合之完整、一致 | G0 / G99 |
| `source_clause` 對 CFTS 原文之**逐字保真** | G94 |
| Layer 3 章節對應之一致 | G103 |
| 必填欄位之存在與型態、格式規約 | G70 / G79 |
| ER 之具名標的存在於 `source_clause` | G82 |
| `source_clause` 之行為項有無 TC 對應（**詞彙層**） | 反向涵蓋三透鏡 |
| OR 分支之涵蓋 | G113 |
| 前提之六欄型態與狀態描述 | G142 / G147 |
| 末步 ER 之可證偽性（**逐條人工裁決，非自動**） | R-P142 / R-P67 |
| `design_method` 之 §12 first-match 判定 | G154 / G155 / G163 / G170 / G181 |
| `priority` 之 §10.2 歸類（謂詞經 13 案 fixture 驗證） | G164 / G172 |
| `distinguishing_axis` 之結構契約與六值列舉 | G168（C1–C8） |
| 四欄逐字全同之偵測（跨 leaf） | G178 |
| 三欄全同而 ER 相異之偵測 | G202 |
| 全量對帳（103 產出 ＋ 12 未產出 = 115 leaf） | G121 |
| 腳本之語法、載入與符號完整（90 / 90） | G108 / G177 |
| **寫入後之工作簿結構**：DV（含 x14）、合併儲存格、條件式格式、分頁清單、壓縮成員、凍結窗格、欄寬皆未變；B 欄序號與列序逐列相符 | G66 / G71 / G72 / G95 / G219 |

---

## 三、**未**驗證之性質 —— 六項語義限制

現行工具皆為**詞彙層或結構層**之比對，下列六項需語義判斷，**無機制可驗**：

| # | 未驗證之性質 | 為何無法機械化 |
|---|---|---|
| 1 | ER 是否為 `source_clause` 之**語義蘊含** | 三透鏡皆詞彙層；其 0.45 門檻為人工設定。詞彙重疊高而語義相反者無機制可攔 |
| 2 | `test_procedure` 各步之**可執行性** | 需對 bench 能力之實體知識 |
| 3 | `tc_title` 是否**忠實**描述該 TC | 需語義判斷 |
| 4 | TC 內**數值門檻**是否等於規格所載 | G94 只驗 `source_clause` 之保真；TC 內之 `20` / `10 seconds` 與 clause 之對應無機制 |
| 5 | 前提條件是否**充分** | 需推論「執行此步驟還缺什麼」 |
| 6 | **測項之遺漏** | 需知道規格沒寫但應測者 |
| 7 | **`source_clause` 之段落切分是否正確** | 見下方之素材層強度界線（54 包 R-P350）—— **現行任何閘門皆驗不出** |
| 8 | **其餘交付欄位之格式規則是否落實** | `test_group` / `test_set` / `functional_safety` / `estimated_test_time` / `remarks` 五欄之格式規則**從未被檢視**（54 包 §K 第 3 項）—— `test_item` 之規則亦是自 01 包至 51 包無人查，經 Pei 指出方發現 |

### 3.1 素材層閘門之強度界線（R-P350，逐字）

> 素材層閘門（G0 / G94 / G99 / G103 / G240）之結果為全數通過，
> **惟其中 G94 對 8 個重建 leaf 之產生器與驗證器同源、
> G240 與其共用段落切分**；
> 該 8 個 leaf 之 `source_clause` 佔全語料字元數之多數。
> **段落切分若有誤，現行任何閘門皆驗不出。**

**「素材層四閘全 111/111」不得單獨引為素材已驗之證據。**
其唯一之外部檢查為人讀 CFTS 原文與 `source_clause` 之對照，
**而該對照從未執行** —— 歷次所讀者皆為 `source_clause`，非其與原文之比對。
登記為**永久已知限制**。

### 3.2 `4941453` 狀態表之三個受阻狀態（R-P349(a)）

`4941453` 之 13 個具名 TLM 狀態中，
**`Logistic Idle` / `Logistic Standby` / `Logistic Sleep` 三者無 TC**。

其定義錨點 `4941427` / `4941432` / `4941435` **全屬 `SWE-PM-008`**，
該 leaf 受 **DR-PW11（High, live）** 阻斷 ——
其被引用錨點 `4941425` / `4941430` / `4941433` 於兩份 CFTS 文字層皆無內文段落。

**該三項非未測之疏漏，而是其 leaf 尚未解阻**；
DR-PW11 獲解後與 `SWE-PM-008` 一併產出。
可行之三項（`Bench` 之輸出組合、`Full-Operation` 與 `Timed` 各二列之 `Source` 分辨）
**已於 54 包補測**（`NR1L-PowerManagement-281` – `283`）。

---

## 四、三層品保之聲明

> 本專案之品質保證由三層構成 —— **閘門**、**分析層之覆核**、**執行層之自查**。
> 其中**執行層之自查無任何機制保障其發生**，
> 而歷次實質發現中有相當比例出自該層。
> **該層若不作為，其缺失不會於任何報表上呈現。**

**併記一項結構性不對稱**（R-P316）：
歷次分析層對執行層之判定皆要求第二方複核（R-P214 / R-P243 / R-P252 / R-P262 等），
**而分析層自身之判定未受同一要求** —— 其歷 48 包之各次裁決從未經第二方複核，
唯一之外部檢查為執行層之上繳自判，而該自判之發生無機制保障。
**此為本專案品保結構之最外層缺口。**

---

## 五、已知落差

| # | 落差 | 說明 |
|---|---|---|
| 1 | **§12 之覆蓋缺口** | `test_procedure` 觸及**恰 2 個功能族**者 **13 條**落於第 9 列。§12 第 8 列為 `≥3 features`、第 9 列為 `Single feature check` —— **「恰 2 個」無列可歸**。該 13 條暫標第 9 列，**其不符該列之條件欄** |
| 2 | **§12 第 7 列結構不可達** | Combinatorial 於 first-match 序下**無法命中** —— 任何多參數組合必先滿足第 4 列。反向查得 9 對候選、6 對真組合，全數被前列吸收。**本批之 `design_method` 值域實為八列** |
| 3 | **§12 第 2 列無實例** | 條件欄舉 `timeout` 為例，而本 feature 之 `timeout` 全為設定名稱，**無注入型逾時故障** —— 該例之謂詞無從建立。第 2 列僅 1 條（Fault Injection Lite） |
| 4 | **`distinguishing_axis` 之語義覆蓋率不可知** | 其判定以**措辭形態**為代理判準，無法窮盡語義（二度實證：情境型輸入、施加動作型）。260 條中 **6 條由人工指派**，其餘由謂詞判定。**該欄不寫入工作簿**，故不影響交付內容 |
| 5 | **8 組同標題而不同 Requirement ID** | 其二 leaf 共用相同錨點與逐字相同之 clause；依 §8.2.2 不得由 TC 作者合併。**此為已知且待答**（DR-PW12），非疏漏 |
| 6 | **4 對 TC 曾為同一施測操作** | 已依 §5.7 合併為 4 條多 ER（264 → 260）。合併後之 ER 行數等於步驟數；同一步之多後果以 §6.1 子項承接 |

---

## 六、複核涵蓋率

**「複核」指由第二方（分析層）以原始素材逐條讀過並出具判定。**

| 欄位／項目 | 母體 | 經第二方複核 | 比率 |
|---|---|---|---|
| `design_method` 第 4 列 | **138** | **13** | 9.4% |
| `design_method` 第 8 列 | 1 | 3（複核當時之 7 條中） | — |
| `distinguishing_axis`（移出 `input_data` 者） | 64 | **9**（15 條中可判者） | 14.1% |
| `distinguishing_axis`（殘留無對應） | 6 | **6** | **100%** |
| `priority`（P0 ＋ Branding） | 180 | 34（32 包抽樣） | 18.9% |
| TC 內容之通讀 | **280** | **0** | **0%** |
| G113 OR 未覆蓋分支之裁決 | **78** | **78**（執行層逐支，非第二方） | **100%** |

> **末列須特別注意**：**無任何一條 TC 之四個長欄位曾被第二方逐字通讀**。
> 依 R-P241，通讀取樣已改為目標式深挖 —— 其代價即為自由文字欄位無人目視，
> 僅由詞彙層透鏡與 lint 之閃點涵蓋。**第七批之 20 條亦在此列。**

> **末二列之別**：G113 之 78 支**已 100% 逐支裁決**（50 包，R-P325），
> 惟其裁決者為**執行層**而非第二方 —— 該列不構成第二方複核之證據。
> 裁決結果：已由他條涵蓋 65、已由他 leaf 涵蓋 3、規格未定義該支 10、**真缺口 0**。
> 逐支理由見 `data/g113_adjudication_50.md`（78 / 78 皆附理由，其中 42 支指名具體 TC）。

---

## 七、執行層之補充 —— 六項之外應載而未載者

依 §K 第 2 項之要求，執行層獨立檢視後，**另指出三項**：

1. **謂詞判定與 JSON 值已就 6 條分離**（A-PW303）——
   `axis` 之 6 條由人工指派而**謂詞未改**，故重跑 `propose()` 仍會將其判為「無對應」。
   **「以謂詞重跑驗證 JSON 值」之作法對該 6 條失效。**
2. **寫回之驗證僅及於機制與結構**（47 §八第 1 項）——
   G219 之十一項皆驗「未變」與「對得上」，**不驗所寫 260 條之內容是否正確**。
   內容之正確性由 §六之複核涵蓋率承擔。
3. **授權之效力範圍有界**（R-P309）——
   本次交付所本之授權，其所及者為**授權當時之 260 條內容**。
   **若其後內容再有實質變動**（TC 增減、四個長欄位之改寫、`req_id` 或 Test Set 之變動），
   **該授權不及於變動後之版本，須重新取得**。

---

## 八、送達狀態

**已送達（2026-08-20）。**

| 前置 | 狀態 |
|---|---|
| (a) 交付說明產出 | **完成**（本檔，54 包更新） |
| (b) 三代號對照表 | **完成**（`data/tcid_three_gen_54.md`，283 列，鍵斷裂 0；50 包版標「提案版」保留） |
| (c) 客戶目錄之路徑由 Pei 指定 | **完成** —— `…/10_Reviewing/00_TestCase/ASW-R2/Power Management` |
| (d) Pei 明示授權送達 | **完成** —— Pei 逐字「授權」 |
| (e) **284 列版本之寫回授權**（R-P323 → R-P331 → 54 包 §E） | **完成**，同 (d) |

### 8.1 送達紀錄

| 項 | 值 |
|---|---|
| 交付副本 | `output/FM-WI-FSM-036-A01 …_PowerManagement_20260816.xlsx` |
| SHA256 | `def0983b8ca89c171166345cfb509c28d56b72416a24f4a73b7cad659b8dc328` |
| 位元組 | 120,515 |
| 內容 | **284 列 —— TC 283 條（`NR1L-PowerManagement-001`–`283`）＋ `SWE-PM-089` 留白列 1** |
| 送達目的地 | `/Users/peihe/Work/02_Project_R1LR/10_Reviewing/00_TestCase/ASW-R2/Power Management/` |
| 送達後核對 | **來源與目的地位元組相同**（同一 SHA256） |
| 送達前備份 | `features/power/sandbox/delivery_backup/20260820T034803Z/`（原目錄二檔，R-P228） |

**寫回路徑**為 `xlsx_surgical.py` 之 `surgical_save()`（唯一授權之路徑，R16 / R-G3）；
**全域無 `Workbook.save()`**。**`inputs/` 之原始檔未被改動** ——
寫入前後 SHA256 皆為 `ce93174794d0d43c…`（G0 台帳之「FW036-A01 交付標的」）。

### 8.1a 送達紀錄（第二次，2026-08-21）—— 五項回修版

| 項 | 值 |
|---|---|
| 交付副本 | `…_SWQT_PowerManagement_20260821(Revise).xlsx` |
| SHA256 | `f59de2e7b9dd7ef10d3f3e089bbb2ff62af428e386662c22961e84af05ea8e4d` |
| 位元組 | 128,130 |
| 內容 | 283 列（`NR1L-PowerManagement-001`–`283`），列數／`tc_id`／`req_id` 與前一版逐列相符 |
| 回修項目 | M3 訊號記法（R-1）、M15 sibling 區分 token（S4）、M10 test_item 摘句（R-3）、M11 首字大寫（R-4）、M17 禁用動詞（§5.1）；合計 174 列 |
| 送達目的地 | `/Users/peihe/Work/02_Project_R1LR/10_Reviewing/00_TestCase/ASW-R2/Power Management/` |
| 送達方式 | **新檔名並存** —— 前一版 `…_20260820.xlsx` 原地保留，未覆蓋 |
| 送達後核對 | **來源與目的地位元組相同**（同一 SHA256） |
| 送達前備份 | `features/power/sandbox/delivery_backup/20260821T080541Z/`（原目錄二檔，R-P228） |
| 授權 | Pei 逐字「授權」（2026-08-21），效力範圍為**授權當時之 283 條內容**，含批 1（154 列）與批 2（20 列）之變動 |
| ChangeHistory | **本次未增列**（Pei 指示） |

寫回路徑為 `xlsx_surgical.py` 之 `surgical_save()`；全域無 `Workbook.save()`。
基底為客戶目錄現行之 `…_20260820.xlsx`（SHA256 `c80ee6da…`），
該檔於本次作業全程唯讀，mtime 未變。

**送達後驗證 12 項全數 PASS**：壓縮成員清單（42）未變、DV 含 x14 未變、
分頁清單（10）未變、合併儲存格（5 ranges）未變、凍結窗格未變、欄寬（36 欄）未變、
條件式格式未變、資料列數 283 相符、B 欄序號逐列相符、`tc_id` 逐列相符、
`req_id` 逐列相符、**lint036 全 16 項檢查為零**。

> ### ⚠ 8.1b 前次送達（2026-08-20）未登錄於本檔
>
> §8.1 所載之交付副本為 `…_20260816.xlsx`／SHA256 `def0983b…`／120,515 bytes；
> 惟客戶目錄於本次作業前實際持有者為 `…_20260820.xlsx`／
> SHA256 `c80ee6da…`／132,332 bytes —— **二者非同一檔**。
> 即 8/20 之送達未留紀錄，本檔 §8.1 於該時點起即為過期。
> 該版內容經實測為 283 條、`-001`–`-283`，與 §一所述相符。
> **其送達當時無備份**；本次作業之備份
> （`delivery_backup/20260821T080541Z/`）為其首次留存。
> 由執行層於 2026-08-21 具名回報，未追改 §8.1 之原記載。

### 8.1c 送達紀錄（第三次，2026-08-21）—— R-1 v2 訊號寫法版

| 項 | 值 |
|---|---|
| 交付副本 | `…_SWQT_PowerManagement_20260821(Revise2).xlsx` |
| SHA256 | `0ab0e6039ea069f19104…`（全值見 `docs/fw036/upstream/10a_pm_full.md`） |
| 位元組 | 121,095 |
| 內容 | 283 列，列數／`tc_id`／`req_id`／`test_item` 與前一版逐列相符 |
| 回修項目 | 下放包 10a A 段：A1 訊號記法改 R-1 v2(a)(b)、A6 DBC `VAL_` 標籤、A4 PROXI 前綴與 `$` 式 19 行、A2/A3 Input 內聯 57 列、6 列 transition 拆步（ER 同步） |
| 變動量 | 154 格／81 列，全數落於 `pre`／`input`／`proc`／`er` 四欄 |
| 送達目的地 | `…/10_Reviewing/00_TestCase/ASW-R2/Power Management/` |
| 送達方式 | **新檔名並存** —— `…_20260820.xlsx` 與 `…_20260821(Revise).xlsx` 均原地保留 |
| 送達後核對 | **來源與目的地位元組相同**（同一 SHA256） |
| 送達前備份 | `features/power/sandbox/delivery_backup/20260821T084928Z/`（原目錄三檔，R-P228） |
| 授權 | Pei 指示「一樣寫回」（2026-08-21），比照 §8.1a 之流程 |
| ChangeHistory | **未增列**（比照 §8.1a） |

送達後驗證 13 項全數 PASS：壓縮成員 42、DV 含 x14、分頁 10、合併儲存格 5、
凍結窗格、欄寬、條件式格式、資料列 283、B 欄序號、`tc_id`、`req_id`、
**`test_item` 逐列相符**（R-6 verbatim 未動）、lint036 之 A–N 十五項為零。

> ### ⚠ 8.1d 本版之已知未完成項 —— 交付前須並讀
>
> 本版**非全綠**：lint036 檢查 P（訊號寫法，R-1 v2）為 **23**
> （前一版於同一規則下為 51；十五項 A–N 均為 0，無回歸）。
> 下放包 10a 六項作業中**兩項保留未做**，理由為「硬做即造值」：
>
> | 未完成項 | 範圍 | 保留理由 |
> |---|---|---|
> | **A5** spec_reference R-2 遷移 | 283 列全數 | 錨點對照僅 **125/283 為確定值**；155 列為 2–13 個候選中之最佳猜測（f1 低至 0.2）。寫入即以猜測冒充 traceability 錨點（§8.4.1） |
> | **A2/A3** Input 內聯 | 158 列中之 **101 列** | free／PROXI／混合類之步驟改寫需內容判斷；機械代入實測產生病句。101 列清單見 `features/power/sandbox/b10/edits.json` 之 `log.held` |
>
> 因此本版仍有 **101 列之 Input Test Data 非 `NA`、其步驟仍以
> `listed in Input Test Data` 回指該欄**，且 **spec_reference 全 283 列
> 維持 `R1LR_…_{章節號}` 舊格式**。
>
> P=23 之組成：`input` 欄 13 筆（屬上述 101 保留列）、
> `test_item` 括號下半 10 筆 —— 後者為批 1 之 sibling token 逐字引用當時
> proc 文字所致，因 10a 驗收明訂「`test_item` 零變動」而未修，
> **該限制與 R-6 相牴觸，待裁定**。
>
> 完整說明見 `docs/fw036/upstream/10a_pm_full.md` §5–§8。

### 8.1e 送達紀錄（第四次，2026-08-21）—— A5 spec_reference 遷移，**覆蓋前一版**

| 項 | 值 |
|---|---|
| 交付副本 | `…_SWQT_PowerManagement_20260821.xlsx` |
| SHA256 | `7ff2501b87eb5fd5170d…`（前一版 `0ab0e603…`） |
| 位元組 | 見檔；前一版 121,095 |
| 變更 | **A5 完成**：`spec_reference` 283 列全數遷移為 `CFTS009/010-{ObjectID}` 首行、原 `{檔名}_{章節號}` 參照保留為次行 |
| 變動量 | 283 格，**全數落於 `spec`（N）欄**；`test_item`／`pre`／`input`／`proc`／`er` 五欄零變動 |
| 送達方式 | **覆蓋**（Pei 指示）—— 前一版即本包同一輪之產出，內容被本版完全涵蓋 |
| 送達前備份 | `features/power/sandbox/delivery_backup/20260821T093234Z/`（原目錄三檔，R-P228） |
| 送達後核對 | 來源與目的地位元組相同 |
| 授權 | Pei 指示「覆蓋」（2026-08-21） |

送達後驗證 15 項全數 PASS：壓縮成員 42、DV 含 x14、分頁 10、合併儲存格 5、
凍結窗格、欄寬、條件式格式、資料列 283、B 欄序號、`tc_id`、`req_id`、
**五個內容欄未變**、**spec 首行 283/283 合式**、**PENDING 0 列**、
lint036 之 A–N 十五項為零。

**來源鏈（A5）**：`req_id` → `layer3_full.tsv` 之 `tokens`（Sys-RA-PM-nnnn）
→ SYS2 Polarion 匯出之 `Source Requirement ID`（7 位 ObjectID）
→ SYS2 `Document ID`。鏈驗證：140 筆 layer3 記錄之 `item_ids` 與 tokens 經
SYS2 解出者**逐筆相等、0 筆不符**；tokens 缺於 SYS2 者 0；
leaf 跨兩份 CFTS 者 0。ObjectID 取該 leaf 之全集，不由章節號反推、不挑選其一
（§10.7 允許多值以 `, ` 並列）。

> **§8.1d 之更正**：該節列 A5 為「未完成」，**已不適用** ——
> A5 於本次（§8.1e）完成，283/283、PENDING 0。
> 初判「前置資料不足」係執行層取錯來源（以語意歸屬檔代替 SYS2 事實對照），
> 經 Pei 指出後改依 SYS2 重做。詳見 `docs/fw036/upstream/10a_pm_full.md` §5.1。
> **§8.1d 其餘各項（A2/A3 之 101 列保留、P=23）仍然有效。**

### 8.1f 送達紀錄（第五次，2026-08-21）—— spec_reference 改一值一行（R-8）

| 項 | 值 |
|---|---|
| 交付副本 | `…_SWQT_PowerManagement_20260821.xlsx` |
| SHA256 | `94419a2f4186f25ab4bc…`（前一版 `7ff2501b…`） |
| 位元組 | 122,934 → 見檔 |
| 變更 | `spec_reference` 改為**一個 ObjectID 一行**，每行完整 `CFTS009/010-{ObjectID}`；**移除文件名與章節號之次行**、不用頓號或逗號串接（Pei 裁定，R-8） |
| 變動量 | 283 格，全數落於 `spec`（N）欄；五個內容欄零變動 |
| 送達方式 | **覆蓋**（同 §8.1e） |
| 送達前備份 | `features/power/sandbox/delivery_backup/20260821T094127Z/` |
| 送達後核對 | 來源與目的地位元組相同 |

實況：

```
row 10（SWE-PM-001）        row 187（SWE-PM-073）
CFTS009-4941354            CFTS010-4942354
CFTS009-4941355
CFTS009-4941357
CFTS009-4941358
CFTS009-4941360
CFTS009-4941453
```

驗證：283/283 之**每一行**皆匹配 `^CFTS0(09|10)-\d{7}$`；
文件名殘留 0、逗號／頓號 0；每列行數即該 leaf 於 SYS2 所引 ObjectID 數
（1 個 125 列、2 個 40 列、⋯、13 個 11 列）；五個內容欄零變動；
逐格 diff 283 格全落 N 欄；lint036 A–N 十五項為零、P=23（未變）。

> **§8.1e 之格式已被本節取代**：該節所載「多個 ObjectID 以 `, ` 續列、
> 原 `{檔名}_{章節號}` 保留為次行」係依 canon §10.7 原條文；
> Pei 於 2026-08-21 直接裁定改為一值一行且不附檔名章節（**R-8**），
> canon §10.7 之「排列」段已同步更新。
> §8.1e 之**來源鏈與鏈驗證仍然有效** —— 本次僅改呈現格式，
> ObjectID 之取值方式未變。

### 8.1g 送達紀錄（第六次，2026-08-21）—— 四個長欄位全面改寫（16–21 包）

| 項 | 值 |
|---|---|
| 交付副本 | `features/power/sandbox/b19/pm_19.xlsx` |
| SHA256 | `b4dd5ca0c0f02394117e52d4c8b34274`（前一版 `94419a2f…`） |
| 位元組 | 148,415 |
| 內容 | 284 列 —— TC 283 條（`-001`–`-283`）＋ `SWE-PM-089` 留白列 1（row 230） |
| 變更 | **`pre`／`input`／`proc`／`er` 四欄全面改寫，283 列全數觸及**（軌 A 56／軌 B 197／軌 C 30）。依 R-1 v3（訊號 `$MESSAGE.Signal$` ＋ DBC `VAL_` 標籤）、R-9（PC 一條件一行一編號）、R-10（空白正規化）、R-11（一觀察點一步驟、須寫應觀察值、Input 一律 `NA`）、R-12(a)（工具行置末）、R-13（規格訊號名保留）。PROC 步數 422 → 606 |
| `test_item` | **內容零變動**；僅 163 格之不可見字元正規化（NBSP／全形空格／行尾空白，R-10(a)） |
| `spec_reference` | **零變動** |
| 送達目的地 | `/Users/peihe/Work/02_Project_R1LR/10_Reviewing/00_TestCase/ASW-R2/Power Management/` |
| 送達方式 | **覆蓋** `…_PowerManagement_20260821.xlsx`（Pei 指定；同 §8.1e／8.1f） |
| 送達前備份 | `features/power/sandbox/delivery_backup/20260821T124700Z/`（原目錄三檔） |
| 送達後核對 | **來源與目的地位元組相同**（同一 SHA256） |
| 授權 | Pei 逐字「授權」（2026-08-21）—— 即 §8.4 所要求之**重新取得**，其效力範圍為本次 283 條之改寫後內容 |
| 寫回路徑 | `pm_19.xlsx` 由 `surgical_save()` 產出（b16→b19 逐包）；送達為位元組複製，未重新存檔。全域無 `Workbook.save()` |
| ChangeHistory | 本次未增列 |

**送達後驗證 14 項全數 PASS**：壓縮成員（42）未變、分頁清單（10）未變、
合併儲存格（5 ranges）未變、凍結窗格未變、欄寬未變、DV 含 x14（3）未變、
條件式格式未變、資料列數 284（留白列仍為 row 230）、B 欄序號逐列相符、
`tc_id` 逐列相符、`req_id` 逐列相符、`spec_reference` 逐列相符、
首列 `-001`／末列 `-283`、**lint036 A–N 十五項為零**（P=10，全落於
`test_item` 括號下半，各包明令不動）。

`inputs/` 之原始檔未被改動 —— SHA256 仍為 `ce93174794d0d43c…`（G0 台帳）。

#### ⚠ 本版之已知限制（隨交付一併聲明）

| 項 | 內容 |
|---|---|
| `PENDING: DR-PW20` | rows 73／74／119／245 各一格 —— 原文僅載轉態目標之**類別**（`another value`／`different from SNA`），依路線 (c) 不推定 |
| `PENDING: DR-PW22` | row 291 之 PROC 2／ER 2 —— geolocation pop-up 與 disclaimer 之**擇一判準原文未載** |
| **DR-PW21（High）** | row 72 之 `PowerModeSts_Telematic` 為規格明載而兩份 DBC 皆無同名。依 **R-13** 保留原文名，**該列不可執行至訊號層**。若上游確認其即 `STATUS_BH_BCM1.PowerModeSts`（VAL_ 與原文二值逐字相符），該列須改寫 |
| **A-PM15** | rows 270／271／275–282 共 8 列之 PROC 1 為抽象動作（`Bring the HU to …`）—— `CFTS009-4941950`／`4941952` 未載觸發來源，**不可執行至訊號層** |
| **A-PM13／A-PM14** | rows 13／265–268 五列同文；row 181 ≡ row 293。依 §8.2.1 TC 側不合併不刪列，拆併屬 Pei |
| lint P=10 | 全落於 `test_item` 括號下半之三件組殘留（R-1 v1 舊式）。該半依 R-6 為 verbatim 之延伸，各包明令不動 |

### 8.1h 送達紀錄（第七次，2026-08-24）—— 面向切分 389 列 ＋ 內容三項（25–29 包）

| 項 | 值 |
|---|---|
| 交付副本 | `features/power/sandbox/b29/pm_29.xlsx` |
| SHA256 | `35305835796a3982ed74b52eb30d563461ffebe254a2a4a55390e8d411730480` |
| 位元組 | 169,266 |
| 內容 | **399 列 —— TC 389 條（`-001`–`-389`）＋ `SWE-PM-089` 留白列 1（row 334）** |
| 變更 | 相對 §8.1g（283 條）：**(1) 面向切分**（25–26 包，283 → 389 條，`tc_id` 全數重新指派）；**(2) 括號下半收斂為 V2**（27–28 包）；**(3) 內容三項**（29 包）—— 主詞 `TLM`→`HU` 828 處／內部變數行為化 95 步＋45 前提行／`Front_Panel_OnOff.Req` → 電源鍵 9 步 |
| 送達目的地 | `/Users/peihe/Work/02_Project_R1LR/10_Reviewing/00_TestCase/ASW-R2/Power Management/` |
| 送達方式 | **新檔名並存**（同 §8.1a）—— `…_PowerManagement_20260824.xlsx`；`…_20260820.xlsx` 與 `…_20260821.xlsx` 原地保留，未覆蓋 |
| 送達前備份 | `features/power/sandbox/delivery_backup/20260824T075848Z/`（原目錄三檔，R-P228） |
| 送達後核對 | **來源與目的地位元組相同**（同一 SHA256） |
| 授權 | Pei 於 2026-08-24 逐字指示「跳過 3 4 也是 拜託快點寫回」—— 即 §8.4 所要求之重新取得，其效力範圍為本次 389 條之內容 |
| 寫回路徑 | `pm_29.xlsx` 由 `surgical_save()` 產出（b19→b29 逐包）；送達為位元組複製，未重新存檔。全域無 `Workbook.save()` |
| 版號 | **維持現狀**，未標 `(Revise2)`（Pei 2026-08-24 裁定不需要） |
| TestRail 對照表 | **未產出**（Pei 2026-08-24 裁定不需要）。`tc_id` 於 pm_26–29 間完全相同，惟**相對 283 條版為全數重新指派** |
| ChangeHistory | 本次未增列 |

**送達後驗證 PASS**：來源與目的地 SHA256 相同、壓縮成員（42）未變、
分頁清單（10）未變、合併儲存格（5 ranges）未變、凍結窗格未變、欄寬（36）未變、
資料列數 389、首列 `-001`／末列 `-389`、**lint036 A–N 含 I-sibling 十五項為零**（P=10、U=32）。
x14 DV 由 `R10:R221` 變為 `R10:R325` —— **列數增加所致，非結構破壞**。

#### ⚠ 本版之已知限制（隨交付一併聲明）

§8.1g 之限制表除下列變動外仍然有效：

| 項 | 內容 |
|---|---|
| **③ 389 列人讀覆核未執行** | Pei 於 2026-08-24 明示跳過。29 包上繳 §七-2 所列之 **40 步／38 列**（`Antitheft_Activation.Req`／`VPLastStatus` 行為化）**改變了受測情境**——例 row 45「進 Standby 後按電源鍵」會破壞被測狀態本身；該疑慮**成立且未逐列改判即交付** |
| **④ Excel 實開抽驗未執行** | Pei 於 2026-08-24 明示跳過。本版之驗證**僅及於機制與結構**（同 47 包 §八第 1 項之限制），未經人眼於 Excel 實際開啟確認版面 |
| `PENDING: DR-PW23` | **22 格**（11 步 ×（proc＋er），rows 65／93／94／101／120／157／158／159／163／165／168）—— `RemStartFail` 無 HMI／DBC／診斷指令之觀察途徑，行為化不可行 |
| `PENDING: DR-PW20`／`DR-PW22` | 沿 §8.1g，列號隨切分位移 |
| **DR-PW24（Low）** | `Front_Panel_OnOff.Req` 與 `$ICSPowerButton$` 之對應未確認；若確認非同一對象，已改寫之 9 步需回查 |
| **DR-PW25（Low）** | HMI 設定條目名 `timeout setting`／`auto switch-on setting` 未對 HMI 規格查證；`Timeout1` 與 `SwitchOff_Timeout_Setting.Req` 是否同一條目亦僅為推論。影響 B1 之 30 步 |
| **套不進句式 243 筆** | 內部變數於 pre／proc／er 之殘留（`Antitheft_Result.Info` 28 步、`Phone_Call.Info` 58 行等），29 包依「不得自創」未改，逐筆見 `features/power/scripts/b29/plan.json` 之 `unfit` |
| **裸讀式殘留 2 行** | rows 67／78 之 `Read VPLastStatus … the value held before the disconnection` —— 行為化句式需先知 ON／OFF，該值兩者皆非 |
| **同列重複行 5 處** | rows 95–98 之 pre、row 237 之 proc —— `Timeout1` 與 `SwitchOff_Timeout_Setting.Req` 映至同一 HMI 條目所致，未去重（29 包 §三-2） |
| **雙重電源鍵按壓 3 列** | rows 149／201／202 之 proc 連續兩步皆為 `Press the HU power button` —— 規則 C 與 B2 各自套句式之後果，未合併（29 包 §三-6） |
| **括號下半與 proc 不同調 77 列** | 依 **R-16** 為明訂可接受（括號下半為需求側摘要，無需與 proc 逐字同調），非缺陷 |

### 8.2 寫回後之十一項驗證（R-P310(三)）—— **全數 PASS**

DV（含 x14）未變、合併儲存格未變、條件式格式未變、分頁清單未變、
壓縮成員清單未變、凍結窗格未變（G95）、欄寬未變（G95）、
B 欄序號逐列相符、列序依 `(SWE-PM ID, split_index)`、最終 `tc_id` 逐列相符、
**來源原始檔未被改動**。

另以獨立讀取複驗交付副本：TC 283 列、留白 1 列、留白列之 `req_id` 為 `SWE-PM-089`、
首列 `…-001`、末列 `…-283`、**`test_item` 三段格式 283 / 283 相符**。

### 8.3 ⚠ 客戶目錄原先為空白樣板

送達前，客戶目錄之該檔 SHA256 為 `ce93174794d0d43c…` —— 即 **G0 台帳所登記之空白樣板**，
與本專案 `inputs/` 之唯讀來源位元組相同。
**即客戶目錄先前未有任何已填寫之版本。**

前一版 260 條副本（SHA256 `dc0d2ee0d46e7518…`，261 列）
**位於 repo 之 `output/superseded/`，從未送達客戶目錄**。
故 R-P322 所載之「260 / 260 = 100% 位移」警語，
**就本目錄而言無比對對象** —— 惟若客戶另有管道取得該版，
換算依據為 `data/tcid_three_gen_54.md`。

### 8.4 授權之效力範圍（R-P309 之同型）

本次送達所本之授權，其所及者為**授權當時之 283 條內容**。
**若其後內容再有實質變動**（TC 增減、四個長欄位之改寫、`req_id` 或 Test Set 之變動），
**該授權不及於變動後之版本，須重新取得。**
