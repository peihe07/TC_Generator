# 73 / 74 包 —— 執行層回報

73 包 §H 全步 ＋ 74 包 §H 第 1–3 步完成。
**⚠ 74 包 §B 第 1 步之出貨前提（G257 歸零）未滿足，且有 15 條佔位符未解 —— 見 §5。**

## 1. 抄錄

`RULINGS.md`：**R-P398**（73 包）、**R-P399**（74 包）逐字抄入（各 1 / 1）。
R-P36 加註二處：**R-P366(b)**（「回指步從來不是末步 0/163」作廢）、
**R-P398(a)**（S6 甲作廢 → Pei 裁乙）。

**R-P379(b) 重算 7 / 7 相符**：7 / 48 / 41 / 146 / 287 / 10 / 3。

## 2. 站④-1 撤銷 —— 執行層量錯欄位

分析層 73 包 §0 之指正**成立**：IN §5.2 之字數限制**約束 Procedure**
（§5.2A ≤12、§5.2B 末步 ≤18、§5.2C `to …` ≤18），**ER 無字數上限**
（IN §6 僅要求 1:1 對齊與可觀察，§6.1 允許 `a./b./c.` 子項）。
我在 72 包量的是 **ER 末步** —— 欄位量錯。七條之 `(站④-1 …)` 標記已移除。

## 3. ⚠ 執行層自陳之重大缺陷 —— 71 包字典之佔位符未實例化

`proxy_dictionary_71.md` 之值為**模板**（`<STATE>` / `<raw>` / `FUNC_STATE_<STATE>`），
R-P396(a) 明載「`<STATE>` = 該 TC ER 所期望之狀態」，**本層逐字寫入 corpus 而未逐條替換**。

實測 **120 條**帶未替換之 `<STATE>` / `<raw>`。

⚠ **71 包「六閘全綠」係量在含佔位符之文字上** —— 佔位符落在 `$…$` 句內，
白名單判準看不見它。**閘全綠而內容未完成，這是判準之盲區，不是通過。**

處置：
- **105 條**依該條之 `Apply ENTER_<X>` → `tc_title` / `test_item` 推得狀態並替換
- **15 條推不出**，**不猜**（§8.4.1），標 `(站④-2：`<STATE>` 未能由本條推定，待分析層指定)`

## 4. `;` 拆步（R-P398(c)）

Procedure 內 `; ` 串接 **77 條**已拆為逐步，ER 對齊；
Procedure 含 `; ` 之步**殘留 0**。ER 本就無 `;`（實測 0），故 §6.1 子項式無適用對象。

## 5. ⚠ G257 未歸零 —— **出貨前提未滿足**

`g257_steplen_73.py` 全案重量 Procedure：**逾限步 146（涉 111 條）** ——
§5.2A 一般 105、§5.2B 末步 31、§5.2C `to …` 10。

### 拆步之嘗試與撤回，據實記

首版實作了第二輪拆步（`split_steps_73.py`），在 ` and check that ` 等連接處反覆切。
實測結果：把
`Read the signal $STATUS_TELEMATIC.PowerSts_Telematic$ and check that it is 2 (Timed)`
拆成 `Read the signal $…$` ＋ `Check that it is 2 (Timed)` 二步 ——

- **驗證擁有者失去 `check that`**（違 IN §5.5）
- `Check that it is …` 之主詞被孤立
- **G245 由 0 升至 60**

**代價大於字數逾限本身，故撤回**（腳本保留不刪，全碼註解並標「已撤回」，同 R-P329 之慣例）。
逾限步**交分析層裁**：R-P398(b) 令「逾限者依 R-P366(b) 拆步」，
而機器拆法會破壞 §5.5；**須人定切點，或裁定 §5.2 對本 feature 之適用**。

## 6. 全案閘表

| 閘 | 期望 | 實測 | 判 |
|---|---|---|---|
| G245 家族 A（上界）| 0 | **0** | ✓ |
| G250 `proper` / `as defined` / `normal` | 0 | **0** | ✓ |
| G250 `Read the HU mode/state` | 0 | **0** | ✓ |
| G247 內部訊號（排除 `TLM_Status.Info`）| 0 | **0** | ✓ |
| G256 引號 lint | 0 | **0** | ✓ |
| G251 `listed in Input Test Data` | 0 | **3**（皆 (c) 類應保留）| ✓ |
| G249 五欄逐字相同對 | — | **10**（全 (b) 型、互註齊）| ✓ |
| Procedure / ER 步數 1:1 | 全對齊 | **不等者 0** | ✓ |
| **G257 Procedure 字數** | **0** | **146 步 / 111 條** | **✗** |
| 未替換佔位符 | 0 | **15**（皆標 站④-2）| **✗** |

`lint_docs036`：**PASS**（本次順帶修 6 列表格列缺結尾 `|`，皆為本層前包所增之列）。
`ledger_xref`：**PASS**。`ledger_guard` 於 `scripts/` 未找到同名工具，未跑。

## 7. 74 包 §B 第 1–4 步之產出

| # | 項 | 結果 |
|---|---|---|
| 1 | `sandbox/b73/pm_73.xlsx` | **288 列**（TC 287 ＋ 留白 1），129,395 bytes，sha256 `b5345cca…`；`sandbox/b73/MANIFEST.tsv` 已寫 |
| 1 | 六閘＋G256＋G257 表 | 見 §6 —— **G257 未歸零** |
| 2 | `lint_docs036` / `ledger_xref` | 皆 **PASS** |
| 3 | `delivered/pm_73.xlsx` | 位元組複製，`filecmp` 實測**逐位元組一致**；`delivered/MANIFEST.tsv` 新增一列（SHA、來源、`74 包 R-P399`、`S6 例外 Pei`）|
| 4 | `delivered/PENDING_LIST.md` | **153 條 TC / 385 處**，依 DR 分組 |
| 4 | `delivered/DR_DISPATCH.md` | 七張 DR 全文，逐字取自 `DATA_REQUESTS.md`，附影響 TC 數 |
| 4 | `delivered/tcid_three_gen_73.tsv` | 287 列，含舊→新 ID 對照（有舊號對應 260、新增 27）|

### PENDING 分布（`PENDING_LIST.md`）

| DR | 涉 TC | 佔位處 |
|---|---|---|
| **DR-PW23** | 92 | 234 |
| **DR-PW27** | 54 | 89 |
| **站④-2**（`<STATE>` 未定）| 15 | 15 |
| DR-PW25 | 9 | 31 |
| DR-PW30 | 6 | 9 |
| DR-PW26 | 3 | 5 |
| DR-PW29 | 1 | 2 |

⚠ **153 條帶 PENDING**（非 146 —— 72 包之 146 不含本包新增之站④-2 15 條，
且佔位符替換後部分條之佔位處增減）。

## 8. 交付檔名副本（Pei 指示「複製一份寫今天日期」）

`output/FM-WI-FSM-036-A01 STLA 測試用例規範與結果_SWQT STLA Test Case Specification & Result_SWQT_PowerManagement_20260830.xlsx`

129,395 bytes，與 `delivered/pm_73.xlsx` **逐位元組一致**（`filecmp` 實測）。

⚠ **未複製至客戶夾** `10_Reviewing/00_TestCase/ASW-R2/Power Management/` ——
74 包 §B-5 明定該步為 **Tier 3，由 Pei 執行**；§I 亦禁執行層複製至客戶夾。
本層只把檔案備在 `output/`，路徑與檔名已就緒。

## 9. 待裁 / 待 Pei

**待分析層（阻出貨）**：
1. **G257 之 146 步** —— 機器拆法破壞 §5.5，須人定切點或裁定 §5.2 之適用。
2. **站④-2 之 15 條** —— `<STATE>` 推不出者之狀態指定。

**待 Pei**：
3. 74 包 §B-5：複製 `output/…_20260830.xlsx` 至客戶夾。
4. §B-6：Excel GUI 開啟驗證。
5. §B-7：客戶夾內 `_20260824.xlsx`（第一代 pm_29，390 列）之去留。
6. 發送 `delivered/DR_DISPATCH.md` 之七張 DR。

⚠ **§9-1 與 §9-2 未決前，本包產出之工作簿在 74 包 §B-1 之條件下尚未達出貨標準。**
檔案已備妥，**是否逕行出貨由 Pei 定** —— 乙案本即為 Pei 之明文例外。
