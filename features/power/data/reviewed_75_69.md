# 已改寫 75 條 —— 站④ 目視包（69 包 / R-P394(d)）

> `sandbox/b55/pm_55.xlsx` 尚未生成，本檔先以現行 `generated/batch_*.json` 出。
> **逐字，不判定、不摘要。** 抽查結果可隨時叫停 B5（R-P394(d)）。

> 共 **75** 條。改寫依據見各條 Remarks 與 `reasoning_note`。

| # | tc_id | req_id | 改寫依據（Remarks）|
|---|---|---|---|
| 1 | `NR1L-PowerManagement-004` | `SWE-PM-071` | (R-P386 第一批) |
| 2 | `NR1L-PowerManagement-005` | `SWE-PM-072` | (R-P386 第一批) |
| 3 | `NR1L-PowerManagement-011` | `SWE-PM-073` | (R-P386 第一批) |
| 4 | `NR1L-PowerManagement-012` | `SWE-PM-073` | (R-P386 第一批) |
| 5 | `NR1L-PowerManagement-017` | `SWE-PM-057` | (R-P386 第一批) |
| 6 | `NR1L-PowerManagement-018` | `SWE-PM-057` | (R-P386 第一批) |
| 7 | `NR1L-PowerManagement-019` | `SWE-PM-057` | (R-P386 第一批) |
| 8 | `NR1L-PowerManagement-020` | `SWE-PM-060` | (R-P386 第一批) |
| 9 | `NR1L-PowerManagement-021` | `SWE-PM-060` | (R-P386 第一批) |
| 10 | `NR1L-PowerManagement-027` | `SWE-PM-063` | (R-P383) |
| 11 | `NR1L-PowerManagement-031` | `SWE-PM-065` | (R-P383) |
| 12 | `NR1L-PowerManagement-049` | `SWE-PM-012` | (R-P386 第一批) |
| 13 | `NR1L-PowerManagement-050` | `SWE-PM-012` | (R-P386 第一批) |
| 14 | `NR1L-PowerManagement-055` | `SWE-PM-013` | (R-P386 第二批) |
| 15 | `NR1L-PowerManagement-057` | `SWE-PM-014` | (R-P376 丁案；原驗 RemStartFail 內部值，改驗其下游效果) |
| 16 | `NR1L-PowerManagement-065` | `SWE-PM-014` | (R-P376 丁案；原驗 RemStartFail 內部值，改驗其下游效果) |
| 17 | `NR1L-PowerManagement-081` | `SWE-PM-021` | (R-P386 第二批) |
| 18 | `NR1L-PowerManagement-100` | `SWE-PM-028` | (R-P386 第一批) |
| 19 | `NR1L-PowerManagement-101` | `SWE-PM-028` | (R-P386 第一批) |
| 20 | `NR1L-PowerManagement-103` | `SWE-PM-029` | (R-P386 第一批) |
| 21 | `NR1L-PowerManagement-104` | `SWE-PM-029` | (R-P386 第一批) |
| 22 | `NR1L-PowerManagement-116` | `SWE-PM-036` | (R-P376 丁案；原驗 RemStartFail / VPLastStatus 內部值，改驗其下游效果) |
| 23 | `NR1L-PowerManagement-117` | `SWE-PM-037` | (R-P383) |
| 24 | `NR1L-PowerManagement-118` | `SWE-PM-039` | (R-P386 第一批) |
| 25 | `NR1L-PowerManagement-119` | `SWE-PM-039` | (R-P386 第一批) |
| 26 | `NR1L-PowerManagement-120` | `SWE-PM-039` | (R-P386 第一批) |
| 27 | `NR1L-PowerManagement-121` | `SWE-PM-039` | (R-P386 第一批) |
| 28 | `NR1L-PowerManagement-123` | `SWE-PM-041` | (R-P386 第一批) |
| 29 | `NR1L-PowerManagement-125` | `SWE-PM-042` | (R-P386 第二批) |
| 30 | `NR1L-PowerManagement-143` | `SWE-PM-048` | (R-P386 第一批) |
| 31 | `NR1L-PowerManagement-148` | `SWE-PM-053` | (R-P386 第一批) |
| 32 | `NR1L-PowerManagement-149` | `SWE-PM-054` | (R-P386 第一批) (R-P357(b)：與 NR1L-PowerManagement-192 五欄逐字相同而 Req ID 不同（SWE-PM-054 / SWE-PM-101），二列皆保留不得合併；互註對方 tc_id) |
| 33 | `NR1L-PowerManagement-150` | `SWE-PM-054` | (R-P386 第一批) (R-P357(b)：與 NR1L-PowerManagement-193 五欄逐字相同而 Req ID 不同（SWE-PM-054 / SWE-PM-101），二列皆保留不得合併；互註對方 tc_id) |
| 34 | `NR1L-PowerManagement-151` | `SWE-PM-054` | (R-P386 第一批) (R-P357(b)：與 NR1L-PowerManagement-194 五欄逐字相同而 Req ID 不同（SWE-PM-054 / SWE-PM-101），二列皆保留不得合併；互註對方 tc_id) |
| 35 | `NR1L-PowerManagement-152` | `SWE-PM-054` | (R-P386 第一批) (R-P357(b)：與 NR1L-PowerManagement-195 五欄逐字相同而 Req ID 不同（SWE-PM-054 / SWE-PM-101），二列皆保留不得合併；互註對方 tc_id) |
| 36 | `NR1L-PowerManagement-155` | `SWE-PM-056` | (R-P386 第一批) (R-P357(b)：與 NR1L-PowerManagement-185 五欄逐字相同而 Req ID 不同（SWE-PM-056 / SWE-PM-097），二列皆保留不得合併；互註對方 tc_id) |
| 37 | `NR1L-PowerManagement-156` | `SWE-PM-058` | (R-P386 第一批) |
| 38 | `NR1L-PowerManagement-169` | `SWE-PM-075` | (R-P386 第一批) |
| 39 | `NR1L-PowerManagement-172` | `SWE-PM-076` | (R-P383) |
| 40 | `NR1L-PowerManagement-182` | `SWE-PM-093` | (R-P386 第二批) |
| 41 | `NR1L-PowerManagement-185` | `SWE-PM-097` | (R-P386 第一批) (R-P357(b)：與 NR1L-PowerManagement-155 五欄逐字相同而 Req ID 不同（SWE-PM-056 / SWE-PM-097），二列皆保留不得合併；互註對方 tc_id) |
| 42 | `NR1L-PowerManagement-186` | `SWE-PM-098` | (R-P386 第二批) |
| 43 | `NR1L-PowerManagement-187` | `SWE-PM-099` | (R-P386 第二批) |
| 44 | `NR1L-PowerManagement-191` | `SWE-PM-100` | (R-P386 第二批) |
| 45 | `NR1L-PowerManagement-192` | `SWE-PM-101` | (R-P386 第一批) (R-P357(b)：與 NR1L-PowerManagement-149 五欄逐字相同而 Req ID 不同（SWE-PM-054 / SWE-PM-101），二列皆保留不得合併；互註對方 tc_id) |
| 46 | `NR1L-PowerManagement-193` | `SWE-PM-101` | (R-P386 第一批) (R-P357(b)：與 NR1L-PowerManagement-150 五欄逐字相同而 Req ID 不同（SWE-PM-054 / SWE-PM-101），二列皆保留不得合併；互註對方 tc_id) |
| 47 | `NR1L-PowerManagement-194` | `SWE-PM-101` | (R-P386 第一批) (R-P357(b)：與 NR1L-PowerManagement-151 五欄逐字相同而 Req ID 不同（SWE-PM-054 / SWE-PM-101），二列皆保留不得合併；互註對方 tc_id) |
| 48 | `NR1L-PowerManagement-195` | `SWE-PM-101` | (R-P386 第一批) (R-P357(b)：與 NR1L-PowerManagement-152 五欄逐字相同而 Req ID 不同（SWE-PM-054 / SWE-PM-101），二列皆保留不得合併；互註對方 tc_id) |
| 49 | `NR1L-PowerManagement-202` | `SWE-PM-103` | (R-P386 第二批) |
| 50 | `NR1L-PowerManagement-216` | `SWE-PM-106` | (R-P386 第一批) |
| 51 | `NR1L-PowerManagement-217` | `SWE-PM-107` | (R-P386 第一批) |
| 52 | `NR1L-PowerManagement-218` | `SWE-PM-108` | (R-P386 第一批) |
| 53 | `NR1L-PowerManagement-222` | `SWE-PM-111` | (R-P386 第二批) |
| 54 | `NR1L-PowerManagement-223` | `SWE-PM-111` | (R-P386 第二批) |
| 55 | `NR1L-PowerManagement-224` | `SWE-PM-113` | (R-P383) (R-P386 第二批) |
| 56 | `NR1L-PowerManagement-228` | `SWE-PM-078` | (R-P386 第二批) |
| 57 | `NR1L-PowerManagement-229` | `SWE-PM-078` | (R-P386 第二批) |
| 58 | `NR1L-PowerManagement-230` | `SWE-PM-079` | (R-P386 第二批) |
| 59 | `NR1L-PowerManagement-233` | `SWE-PM-081` | (R-P386 第二批) |
| 60 | `NR1L-PowerManagement-234` | `SWE-PM-081` | (R-P386 第二批) |
| 61 | `NR1L-PowerManagement-235` | `SWE-PM-081` | (R-P386 第二批) |
| 62 | `NR1L-PowerManagement-236` | `SWE-PM-082` | (R-P386 第二批) |
| 63 | `NR1L-PowerManagement-237` | `SWE-PM-082` | (R-P386 第二批) |
| 64 | `NR1L-PowerManagement-238` | `SWE-PM-082` | (R-P386 第二批) |
| 65 | `NR1L-PowerManagement-242` | `SWE-PM-084` | (R-P386 第二批) |
| 66 | `NR1L-PowerManagement-243` | `SWE-PM-084` | (R-P386 第二批) |
| 67 | `NR1L-PowerManagement-249` | `SWE-PM-087` | (R-P386 第二批) |
| 68 | `NR1L-PowerManagement-250` | `SWE-PM-088` | (R-P386 第二批) |
| 69 | `NR1L-PowerManagement-262` | `SWE-PM-001` | (R-P383) |
| 70 | `NR1L-PowerManagement-271` | `SWE-PM-004` | (R-P383) |
| 71 | `NR1L-PowerManagement-281` | `SWE-PM-007` | (R-P386 第二批) |
| 72 | `NR1L-PowerManagement-284` | `SWE-PM-075` | (R-P393(c) §8.3 拆分增列；本條與 NR1L-PowerManagement-169 同錨點) |
| 73 | `NR1L-PowerManagement-285` | `SWE-PM-075` | (R-P393(c) §8.3 拆分增列；本條與 NR1L-PowerManagement-169 同錨點) |
| 74 | `NR1L-PowerManagement-286` | `SWE-PM-087` | (R-P393(c) §8.3 拆分增列；本條與 NR1L-PowerManagement-249 同錨點) |
| 75 | `NR1L-PowerManagement-287` | `SWE-PM-093` | (R-P393(c) §8.3 拆分增列；本條與 NR1L-PowerManagement-182 同錨點) |

## `NR1L-PowerManagement-004`　`SWE-PM-071`　Standard screen shown after StandardScreen_Time

**Test Item**

```
TLM boot requires following timings:
After SplashScreen_Time the splash screen is loaded and shown on TLM display (only if TLM has not to pass to Standby status nor to Bench status: in these cases no splash screen has to be shown);
 After StandardScreen_Time the standard screen is visualized on TLM screen

(read the TLM screen content before and after StandardScreen_Time -> The standard screen is not visualized before StandardScreen_Time has elapsed, and it is visualized once that time has passed)
```

**Pre-Conditions**

```
1. A suspend-resume boot sequence is available on the bench
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Start the suspend-resume boot sequence with video recording running
2. PENDING: DR-PW30 SplashScreen_Time 之值 —— read the HU screen at that time and check that the "Splash Screen" is shown on it
3. PENDING: DR-PW30 StandardScreen_Time 之值 —— read the HU screen before and after that time and check that the "Standard Screen" is shown on it only after that time
```

**Expected Result**

```
1. The boot sequence is recorded
2. PENDING: DR-PW30 SplashScreen_Time
3. PENDING: DR-PW30 StandardScreen_Time
```

**Remarks**

```
(R-P386 第一批)
```

## `NR1L-PowerManagement-005`　`SWE-PM-072`　Events during boot are buffered without loss

**Test Item**

```
Any event occurring during the boot must be recognized by TLM and then TLM has to behave and process it according to the transitions described in par. “TLM_Status.Info setting” while the boot is still completing. TLM must buffer the events and process them as soon as possible, depending on boot timings.

(read the TLM_Status transitions during the remainder of the boot -> Every buffered event is processed before the boot sequence completes and none remains pending at boot completion)
```

**Pre-Conditions**

```
1. An event injection tool is connected to the bench
```

**Input Test Data**

```
Ignition values during boot: 2 (Ignition_Off), 4 (Ignition_On), 2 (Ignition_Off)
```

**Test procedure**

```
1. Start the TLM boot sequence and send each ignition value listed in Input Test Data in turn while the boot is still completing ($STATUS_BH_BCM1.OperationalModeSts$, DR-PW26)
2. Let the boot complete
3. Read the signal $STATUS_TELEMATIC.PowerSts_Telematic$ and check that it corresponds to the last ignition value that was sent
```

**Expected Result**

```
1. Each ignition value is registered without a bus error while the boot is still completing
2. The boot completes
3. The signal value $STATUS_TELEMATIC.PowerSts_Telematic$ corresponds to the last ignition value that was sent
```

**Remarks**

```
(R-P386 第一批)
```

## `NR1L-PowerManagement-011`　`SWE-PM-073`　Continuing call transferred to head set under Load Shed

**Test Item**

```
When STATUS_LIN.PN14_LS_Actv=[1h] and STATUS_LIN.PN14_LS_Lvl7= [1h] signals are received by the TLM,  the TLM shall immediately reduce the maximum volume level to 20 for Ecall/ACN/Chimes/Beeps/Alerts and if the volume was greater, send the AUD_LVL signal with the updated volume level. If Ecall/ACN/chimes mode is not active, TLM shall be muted. ICS module shall power down. Under fault condition of missing load shed signals on the CAN bus, the last values of load shed signals shall be used until load shed signal broadcast resumes. If the load shed signals do not recover, the on-going load shed action shall be maintained for the rest of current ignition key cycle. The TLM shall transfer the call(not-Ecall/ACN call)  to the head set in case a continuing call is still active

While in BODY ON or BODY OFF-TIMED mode, when TLM receives STATUS_LIN.Batt_ST_Crit=[1h], the TLM shall mimimize current withdraw while keeping only the display on and controls active for HVAC controls, and active phone for ACN. TLM shall immediately reduce the maximum volume level to 20 for Ecall/ACN/Chimes/Beeps/Alerts  and if the volume was greater, send the AUD_LVL signal with the updated volume level. If Ecall/ACN/chimes mode is not active, TLM shall be muted. The TLM shall transfer the call (not-Ecall/ACN call) to the head set in case a continuing call is still active. Unless defined otherwise, TLM shall stay in this state until either voltage out of range conditions are satisfied  or shall go back to normal behavior 10 seconds after STATUS_LIN.Batt_ST_Crit becomes [0h].

(read the call audio routing -> The continuing call is routed to the head set and is not dropped)
```

**Pre-Conditions**

```
1. The bench is an Atlantis High configuration
2. A LIN and CAN simulation tool is connected
3. A non-Ecall non-ACN call is active and continuing
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Send the signal $STATUS_LIN.PN14_LS_Actv$ = 1 (Active)
2. Send the signal $STATUS_LIN.PN14_LS_Lvl7$ = 1 (Active)
3. Read the HU speakers and check that the call audio is no longer present on them
4. Read the "Call Screen" on the paired phone and check that it still shows the call as connected
```

**Expected Result**

```
1. The TLM accepts the signal without a bus error
2. The TLM accepts the second signal without a bus error
3. The call audio is no longer present on the HU speakers
4. The "Call Screen" on the paired phone still shows the call as connected
```

**Remarks**

```
(R-P386 第一批)
```

## `NR1L-PowerManagement-012`　`SWE-PM-073`　Continuing call transferred to head set under Battery Critical

**Test Item**

```
When STATUS_LIN.PN14_LS_Actv=[1h] and STATUS_LIN.PN14_LS_Lvl7= [1h] signals are received by the TLM,  the TLM shall immediately reduce the maximum volume level to 20 for Ecall/ACN/Chimes/Beeps/Alerts and if the volume was greater, send the AUD_LVL signal with the updated volume level. If Ecall/ACN/chimes mode is not active, TLM shall be muted. ICS module shall power down. Under fault condition of missing load shed signals on the CAN bus, the last values of load shed signals shall be used until load shed signal broadcast resumes. If the load shed signals do not recover, the on-going load shed action shall be maintained for the rest of current ignition key cycle. The TLM shall transfer the call(not-Ecall/ACN call)  to the head set in case a continuing call is still active

While in BODY ON or BODY OFF-TIMED mode, when TLM receives STATUS_LIN.Batt_ST_Crit=[1h], the TLM shall mimimize current withdraw while keeping only the display on and controls active for HVAC controls, and active phone for ACN. TLM shall immediately reduce the maximum volume level to 20 for Ecall/ACN/Chimes/Beeps/Alerts  and if the volume was greater, send the AUD_LVL signal with the updated volume level. If Ecall/ACN/chimes mode is not active, TLM shall be muted. The TLM shall transfer the call (not-Ecall/ACN call) to the head set in case a continuing call is still active. Unless defined otherwise, TLM shall stay in this state until either voltage out of range conditions are satisfied  or shall go back to normal behavior 10 seconds after STATUS_LIN.Batt_ST_Crit becomes [0h].

(read the call audio routing -> The continuing call is routed to the head set and is not dropped)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. The TLM is in BODY ON mode
3. A non-Ecall non-ACN call is active and continuing
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Send the signal $STATUS_LIN.Batt_ST_Crit$ = 1 (True)
2. Read the HU speakers and check that the call audio is no longer present on them
3. Read the "Call Screen" on the paired phone and check that it still shows the call as connected
```

**Expected Result**

```
1. The TLM accepts the signal without a bus error
2. The call audio is no longer present on the HU speakers
3. The "Call Screen" on the paired phone still shows the call as connected
```

**Remarks**

```
(R-P386 第一批)
```

## `NR1L-PowerManagement-017`　`SWE-PM-057`　Timeout1 options follow PROXI "Switch_Off_Time" set to 20 minutes

**Test Item**

```
IF "Switch_Off_Time" parameter  is set  to "20 minutes"  then the user can select SwitchOff_Timeout_Setting.Req to "00 min" OR to "20 min" in TLM menu; so Timeout1 is equal to "00 min" OR "20 minutes" respectively.

(select each offered value in turn and read Timeout1 -> Timeout1 reads "00 min" after the first selection and "20 minutes" after the second)
```

**Pre-Conditions**

```
1. An LTM High Radio is absent from the bench configuration
2. The PROXI parameter "Switch_Off_Time" is at 20 minutes
3. The TLM is in Full-Operation status
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. PENDING: DR-PW25 —— TLM 選單中該設定項之名稱與進入路徑
2. PENDING: DR-PW25 —— 讀該設定項所列之可選值（數與名）
```

**Expected Result**

```
1. PENDING: DR-PW25
2. PENDING: DR-PW25
```

**Remarks**

```
(R-P386 第一批)
```

## `NR1L-PowerManagement-018`　`SWE-PM-057`　Timeout1 options follow PROXI "Switch_Off_Time" set to 60 minutes

**Test Item**

```
IF "Switch_Off_Time" parameter  is set  to "20 minutes"  then the user can select SwitchOff_Timeout_Setting.Req to "00 min" OR to "20 min" in TLM menu; so Timeout1 is equal to "00 min" OR "20 minutes" respectively.

(select each offered value in turn and read Timeout1 -> Timeout1 reads "00 min" after the first selection and "60 minutes" after the second)
```

**Pre-Conditions**

```
1. An LTM High Radio is absent from the bench configuration
2. The PROXI parameter "Switch_Off_Time" is at 60 minutes
3. The TLM is in Full-Operation status
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. PENDING: DR-PW25 —— TLM 選單中該設定項之名稱與進入路徑
2. PENDING: DR-PW25 —— 讀該設定項所列之可選值（數與名）
```

**Expected Result**

```
1. PENDING: DR-PW25
2. PENDING: DR-PW25
```

**Remarks**

```
(R-P386 第一批)
```

## `NR1L-PowerManagement-019`　`SWE-PM-057`　Timeout1 options follow PROXI "Switch_Off_Time" set to 180 minutes

**Test Item**

```
IF "Switch_Off_Time" parameter  is set  to "20 minutes"  then the user can select SwitchOff_Timeout_Setting.Req to "00 min" OR to "20 min" in TLM menu; so Timeout1 is equal to "00 min" OR "20 minutes" respectively.

(select each offered value in turn and read Timeout1 -> Timeout1 reads "00 min" after the first selection and "180 minutes" after the second)
```

**Pre-Conditions**

```
1. An LTM High Radio is absent from the bench configuration
2. The PROXI parameter "Switch_Off_Time" is at 180 minutes
3. The TLM is in Full-Operation status
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. PENDING: DR-PW25 —— TLM 選單中該設定項之名稱與進入路徑
2. PENDING: DR-PW25 —— 讀該設定項所列之可選值（數與名）
```

**Expected Result**

```
1. PENDING: DR-PW25
2. PENDING: DR-PW25
```

**Remarks**

```
(R-P386 第一批)
```

## `NR1L-PowerManagement-020`　`SWE-PM-060`　LTM or ETM Radio offers one timeout parameter

**Test Item**

```
For LTM/ETM, the user can set one parameter, by means of Auto_SwitchOn_Setting.Req. For other Radios the user can set two parameters, by means of SwitchOff_Timeout_Setting.Req and Auto_SwitchOn_Setting.Req signals, selectable from TLM menu.

(read the parameters offered for user selection -> Auto_SwitchOn_Setting.Req is the only parameter offered and SwitchOff_Timeout_Setting.Req is absent)
```

**Pre-Conditions**

```
1. An LTM Radio is present in the bench configuration
2. The TLM is in Full-Operation status
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. PENDING: DR-PW25 —— TLM 選單中該設定項之名稱與進入路徑
2. PENDING: DR-PW25 —— 讀該設定項所列之可選值（數與名）
```

**Expected Result**

```
1. PENDING: DR-PW25
2. PENDING: DR-PW25
```

**Remarks**

```
(R-P386 第一批)
```

## `NR1L-PowerManagement-021`　`SWE-PM-060`　Radio other than LTM or ETM offers two timeout parameters

**Test Item**

```
For LTM/ETM, the user can set one parameter, by means of Auto_SwitchOn_Setting.Req. For other Radios the user can set two parameters, by means of SwitchOff_Timeout_Setting.Req and Auto_SwitchOn_Setting.Req signals, selectable from TLM menu.

(read the parameters offered for user selection -> SwitchOff_Timeout_Setting.Req and Auto_SwitchOn_Setting.Req are both offered for selection)
```

**Pre-Conditions**

```
1. A Radio other than LTM or ETM is present in the bench configuration
2. The TLM is in Full-Operation status
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. PENDING: DR-PW25 —— TLM 選單中該設定項之名稱與進入路徑
2. PENDING: DR-PW25 —— 讀該設定項所列之可選值（數與名）
```

**Expected Result**

```
1. PENDING: DR-PW25
2. PENDING: DR-PW25
```

**Remarks**

```
(R-P386 第一批)
```

## `NR1L-PowerManagement-027`　`SWE-PM-063`　Bluetooth calls can be made and received in Timed state

**Test Item**

```
In Timed state, it is possible either to make and to receive one or more bluetooth phone calls according to following logics that depend on Timeout1 and on MaxCallTimeout time parameters.

(read the call audio routing and the TLM state -> Both calls were served and the TLM remains in Timed state)
```

**Pre-Conditions**

```
1. A paired bluetooth phone is available on the bench
2. The TLM is in Timed state
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Place an outgoing bluetooth call from the paired phone through the TLM
2. Read the paired phone screen and check that it shows the call as connected, and check that the call audio is present on the HU speakers
3. End that call and let the paired phone place an incoming call to the HU
4. Read the HU screen and check that the "Incoming Call" pop-up is shown on it, then answer it
5. Read the signal $STATUS_TELEMATIC.PowerSts_Telematic$ and check that it is 2 (Timed)
```

**Expected Result**

```
1. The outgoing call is placed from the paired phone
2. The paired phone shows the call as connected and the call audio is present on the HU speakers
3. The incoming call reaches the HU
4. The "Incoming Call" pop-up is shown on the HU screen and the call is answered
5. The signal value $STATUS_TELEMATIC.PowerSts_Telematic$ = 2 (Timed) is received
```

**Remarks**

```
(R-P383)
```

## `NR1L-PowerManagement-031`　`SWE-PM-065`　Further calls are still managed within Timeout1

**Test Item**

```
In this case, TLM is still able to manage other possible phone calls within Timeout1 expiration.

(read the call audio routing and the TLM state -> The TLM remains in Timed state while the second call runs)
```

**Pre-Conditions**

```
1. A paired bluetooth phone is available on the bench
2. Apply ENTER_TIMED
3. $BCM_FD_27.Comfort_Enable_Time$ is at a value other than 0 (DR-PW26)
4. One call has already ended before that value has elapsed
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Place a second bluetooth call from the paired phone while $BCM_FD_27.Comfort_Enable_Time$ has not yet elapsed
2. Answer the call and check that the call audio is present on the HU speakers
3. Read the signal $STATUS_TELEMATIC.PowerSts_Telematic$ and check that it is 2 (Timed)
```

**Expected Result**

```
1. The second bluetooth call reaches the HU before $BCM_FD_27.Comfort_Enable_Time$ has elapsed
2. The call is answered and the call audio is present on the HU speakers
3. The signal value $STATUS_TELEMATIC.PowerSts_Telematic$ = 2 (Timed) is received
```

**Remarks**

```
(R-P383)
```

## `NR1L-PowerManagement-049`　`SWE-PM-012`　User settings are restored after a battery reconnection

**Test Item**

```
After a battery reconnection and also when TLM has to exit INIT state (as soon as the voltage is limited within certain thresholds), TLM is able to work properly again and it has to restore the last user settings and the last variables values: VPLastStatus, SwitchOffSetting.Req, Auto_SwitchOn_Setting.Req shall be restored to their values before the battery disconnection / battery reset

(read the three stored variables -> VPLastStatus, SwitchOffSetting.Req and Auto_SwitchOn_Setting.Req read their values before the battery disconnection)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. VPLastStatus, SwitchOffSetting.Req and Auto_SwitchOn_Setting.Req hold known values
3. The battery is disconnected
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Apply ENTER_FULL_OPERATION and read the signal $STATUS_TELEMATIC.PowerSts_Telematic$, recording the value as State_before
2. Disconnect the battery from the HU, then reconnect it
3. Send the signal $STATUS_BH_BCM1.OperationalModeSts$ = 4 (Ignition_On) (DR-PW26), then read the signal $STATUS_TELEMATIC.PowerSts_Telematic$ and check that it equals State_before
4. PENDING: DR-PW25 SwitchOffSetting.Req 與 Auto_SwitchOn_Setting.Req 之設定項名與讀取方法
```

**Expected Result**

```
1. State_before is recorded
2. The battery is disconnected and reconnected
3. The signal value $STATUS_TELEMATIC.PowerSts_Telematic$ equals State_before
4. PENDING: DR-PW25 SwitchOffSetting.Req / Auto_SwitchOn_Setting.Req
```

**Remarks**

```
(R-P386 第一批)
```

## `NR1L-PowerManagement-050`　`SWE-PM-012`　TLM starts from Sleep state after leaving INIT

**Test Item**

```
Then, TLM has to behave according to requirements of par. "TLM_Status.Info and $Telematic_Power$ signal setting", setting TLM_Status.Info to "Sleep" first and starting from Sleep state.

(read TLM_Status.Info and the state machine -> TLM_Status.Info reads "Sleep" and the TLM starts from Sleep state)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. The battery has just been reconnected
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Disconnect the battery from the HU
2. Reconnect the battery
3. Read the bus trace and check that the first $STATUS_TELEMATIC$ frame transmitted after the reconnection carries $STATUS_TELEMATIC.PowerSts_Telematic$ = 0 (Sleep)
```

**Expected Result**

```
1. The battery is disconnected
2. The battery is reconnected
3. The first $STATUS_TELEMATIC$ frame after the reconnection carries $STATUS_TELEMATIC.PowerSts_Telematic$ = 0 (Sleep)
```

**Remarks**

```
(R-P386 第一批)
```

## `NR1L-PowerManagement-055`　`SWE-PM-013`　AMP, ICS and DTV are off while chime audio stays active

**Test Item**

```
This status is related to TLM OFF. AMP/ICS/DTV shall be OFF. Audio for ANC, ACN, and chimes (if equipped) shall be active in this state)

(read the AMP, ICS and DTV power states and the audio paths -> AMP, ICS and DTV are OFF while audio for ANC, ACN and chimes is active)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. The TLM is in Partial Operation with AMP, ICS and DTV equipped
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Apply ENTER_PARTIAL_OPERATION
2. Read the signal $STATUS_TELEMATIC.PowerSts_Telematic$ and check that it is 7 (Partial_Operation)
3. Apply FUNC_STATE_PARTIAL_OPERATION and check each of its sub-items
4. Send the signal $PARK_INFO.ChimeActivation_LHF$ = 1 (Active) and check that the chime is audible on the left hand front speaker
```

**Expected Result**

```
1. The HU is in Partial Operation
2. The signal value $STATUS_TELEMATIC.PowerSts_Telematic$ = 7 (Partial_Operation) is received
3. FUNC_STATE_PARTIAL_OPERATION holds:
   a. No audio source is playing and no amplifier output is present
   b. The HU display is off, except for the HMI Antitheft Screens (CFTS009-4941457)
   c. PENDING: DR-PW27 BoosterOUT OFF / antenna supply ON 位準值
   d. A USB device is not enumerated and the AUX input does not play
4. The chime is audible on the left hand front speaker
```

**Remarks**

```
(R-P386 第二批)
```

## `NR1L-PowerManagement-057`　`SWE-PM-014`　Remote Start ends at ignition off: RemStartFail is set true

**Test Item**

```
IF TLM_Status.Info and $Telematic_Power$ == "Full-Operation" AND STATUS_BH_BCM2.RemStActvSts has a transition  from "Remote Start Active" to "Remote Start Not Active"THEN IF LTM_OperationalModeSts.Info is equal to "Ignition Pre Off" OR to "Ignition Off", TLM has to set RemStartFail = "True" THEN IF Phone_Call.Info == "Not Active", TLM has to set RemStartFail ="False" AND TLM_Status.Info and $Telematic_Power$ to "Standby" value and it passes to TLM Standby state.IF Phone_Call.Info == "Active" TLM has to set TLM_Status.Info and $Telematic_Power$ to "Timed" value and it passes to TLM Timed state.In this case, TLM has to stay in this state until Phone_Call.Info becomes equal to "Not_Active", OR at maximum until MaxCallTimeout expiration. See par. “Phone call management in Timed state” .ELSE IF LTM_OperationalModeSts.Info is not equal to "Ignition Pre Off" OR to "Ignition Off"  THEN TLM has to set RemStartFail = "False" AND TLM has to stay in the original state (Full Operation).IF TLM_Status.Info and $Telematic_Power$ == "Full-Operation"AND signal LTM_OperationalModeSts.Info has a transition to "Ignition Pre Off" OR to "Ignition Off" valueAND STATUS_BH_BCM2.RemStActvSts is equal to "Remote Start Not Active"AND RemStartFail == "False"THENaccording to a time setting selectable in the TLM menu it is possible to have two different behaviours

(read $STATUS_TELEMATIC.PowerSts_Telematic$ -> The TLM passes to Standby)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. $STATUS_TELEMATIC.PowerSts_Telematic$ = 4 (Full_Operation)
3. $STATUS_BH_BCM2.RemStActvSts$ = 1 (Remote Start Active)
4. No phone call is in progress on the bench
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Send the signal $STATUS_BH_BCM1.OperationalModeSts$ = 2 (Ignition_Off)
2. Send the signal $STATUS_BH_BCM2.RemStActvSts$ = 0 (Remote Start Not Active)
3. Read the signal $STATUS_TELEMATIC.PowerSts_Telematic$ and check that it is 1 (Standby)
```

**Expected Result**

```
1. The signal value $STATUS_BH_BCM1.OperationalModeSts$ = 2 (Ignition_Off) is received
2. The signal value $STATUS_BH_BCM2.RemStActvSts$ = 0 (Remote Start Not Active) is received
3. The signal value $STATUS_TELEMATIC.PowerSts_Telematic$ = 1 (Standby) is received
```

**Remarks**

```
(R-P376 丁案；原驗 RemStartFail 內部值，改驗其下游效果)
```

## `NR1L-PowerManagement-065`　`SWE-PM-014`　Remote Start ends at ignition pre off: RemStartFail is set true

**Test Item**

```
IF TLM_Status.Info and $Telematic_Power$ == "Full-Operation" AND STATUS_BH_BCM2.RemStActvSts has a transition  from "Remote Start Active" to "Remote Start Not Active"THEN IF LTM_OperationalModeSts.Info is equal to "Ignition Pre Off" OR to "Ignition Off", TLM has to set RemStartFail = "True" THEN IF Phone_Call.Info == "Not Active", TLM has to set RemStartFail ="False" AND TLM_Status.Info and $Telematic_Power$ to "Standby" value and it passes to TLM Standby state.IF Phone_Call.Info == "Active" TLM has to set TLM_Status.Info and $Telematic_Power$ to "Timed" value and it passes to TLM Timed state.In this case, TLM has to stay in this state until Phone_Call.Info becomes equal to "Not_Active", OR at maximum until MaxCallTimeout expiration. See par. “Phone call management in Timed state” .ELSE IF LTM_OperationalModeSts.Info is not equal to "Ignition Pre Off" OR to "Ignition Off"  THEN TLM has to set RemStartFail = "False" AND TLM has to stay in the original state (Full Operation).IF TLM_Status.Info and $Telematic_Power$ == "Full-Operation"AND signal LTM_OperationalModeSts.Info has a transition to "Ignition Pre Off" OR to "Ignition Off" valueAND STATUS_BH_BCM2.RemStActvSts is equal to "Remote Start Not Active"AND RemStartFail == "False"THENaccording to a time setting selectable in the TLM menu it is possible to have two different behaviours

(read $STATUS_TELEMATIC.PowerSts_Telematic$ -> The TLM passes to Standby)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. $STATUS_TELEMATIC.PowerSts_Telematic$ = 4 (Full_Operation)
3. $STATUS_BH_BCM2.RemStActvSts$ = 1 (Remote Start Active)
4. No phone call is in progress on the bench
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Send the signal $STATUS_BH_BCM1.OperationalModeSts$ = 10 (Ignition_Pre_Off)
2. Send the signal $STATUS_BH_BCM2.RemStActvSts$ = 0 (Remote Start Not Active)
3. Read the signal $STATUS_TELEMATIC.PowerSts_Telematic$ and check that it is 1 (Standby)
```

**Expected Result**

```
1. The signal value $STATUS_BH_BCM1.OperationalModeSts$ = 10 (Ignition_Pre_Off) is received
2. The signal value $STATUS_BH_BCM2.RemStActvSts$ = 0 (Remote Start Not Active) is received
3. The signal value $STATUS_TELEMATIC.PowerSts_Telematic$ = 1 (Standby) is received
```

**Remarks**

```
(R-P376 丁案；原驗 RemStartFail 內部值，改驗其下游效果)
```

## `NR1L-PowerManagement-081`　`SWE-PM-021`　Rear camera enable in Idle keeps Idle with video only

**Test Item**

```
IF TLM_Status.Info and $Telematic_Power$ == "Idle" AND PROXI parameter Rear_View_Camera == "Present" AND Rear_Camera_Enable.Info passes from "False" to "True"THENTLM stays in Idle state but allows its screen only to show the rear view camera video on its screen.Refer to VF551 for details about video availability requirements on TLM screen state.

(read TLM_Status.Info and the screen content -> The screen shows the rear view camera video and nothing else)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. Apply ENTER_IDLE
3. PROXI Rear_View_Camera = "Present"
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. PENDING: DR-PW23 Rear_Camera_Enable.Info 之驅動方法（自 "False" 轉 "True"）
2. Read the HU screen and check that the "Rear View Camera" video is shown on it
3. Read the signal $STATUS_TELEMATIC.PowerSts_Telematic$ and check that it is still 3 (Idle)
```

**Expected Result**

```
1. PENDING: DR-PW23 Rear_Camera_Enable.Info
2. The "Rear View Camera" video is shown on the HU screen
3. The signal value $STATUS_TELEMATIC.PowerSts_Telematic$ = 3 (Idle) is still received
```

**Remarks**

```
(R-P386 第二批)
```

## `NR1L-PowerManagement-100`　`SWE-PM-028`　Antitheft success with a zero timeout takes Timeout1 from PROXI

**Test Item**

```
IF SwitchOff_Timeout_Setting.Req == 00 min or ( If Auto_SwitchOn_Setting.Req =="Active ", when  Timeout1 == 00 MIN" for LTM High Radio): THENTLM has to set Timeout1 to the value specified by PROXI parameter “Switch_Off_Time” (for example 20 minutes if Switch_Off_Time == 20 minutes), only for this case,  restoring it to "00 minutes" at next Ignition On event.

(read Timeout1 and then trigger an Ignition On event -> Timeout1 reads "00 minutes" again at the next Ignition On event)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. SwitchOff_Timeout_Setting.Req reads "00 min"
3. Switch_Off_Time reads 20 minutes
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. PENDING: DR-PW23 SwitchOff_Timeout_Setting.Req 之設定方法（設為 "00 min"）
2. Apply ENTER_TIMED and read the signal $STATUS_TELEMATIC.PowerSts_Telematic$ and check that it is 2 (Timed)
3. Hold for the PROXI Switch_Off_Time value, then read the signal $STATUS_TELEMATIC.PowerSts_Telematic$ and check that it is 1 (Standby)
4. PENDING: DR-PW23 Antitheft_Result.Info 之觀察方法（antitheft 成功）
```

**Expected Result**

```
1. PENDING: DR-PW23 SwitchOff_Timeout_Setting.Req
2. The signal value $STATUS_TELEMATIC.PowerSts_Telematic$ = 2 (Timed) is received
3. The signal value $STATUS_TELEMATIC.PowerSts_Telematic$ = 1 (Standby) is received after the PROXI Switch_Off_Time value has elapsed
4. PENDING: DR-PW23 Antitheft_Result.Info
```

**Remarks**

```
(R-P386 第一批)
```

## `NR1L-PowerManagement-101`　`SWE-PM-028`　Antitheft success on LTM High takes Timeout1 from PROXI

**Test Item**

```
IF SwitchOff_Timeout_Setting.Req == 00 min or ( If Auto_SwitchOn_Setting.Req =="Active ", when  Timeout1 == 00 MIN" for LTM High Radio): THENTLM has to set Timeout1 to the value specified by PROXI parameter “Switch_Off_Time” (for example 20 minutes if Switch_Off_Time == 20 minutes), only for this case,  restoring it to "00 minutes" at next Ignition On event.

(read Timeout1 and then trigger an Ignition On event -> Timeout1 reads "00 minutes" again at the next Ignition On event)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. An LTM High Radio is present in the bench configuration
3. Auto_SwitchOn_Setting.Req reads "Active " and Timeout1 reads "00 MIN"
4. Switch_Off_Time reads 20 minutes
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. PENDING: DR-PW23 SwitchOff_Timeout_Setting.Req 之設定方法（設為 "00 min"）
2. Apply ENTER_TIMED and read the signal $STATUS_TELEMATIC.PowerSts_Telematic$ and check that it is 2 (Timed)
3. Hold for the PROXI Switch_Off_Time value, then read the signal $STATUS_TELEMATIC.PowerSts_Telematic$ and check that it is 1 (Standby)
4. PENDING: DR-PW23 Antitheft_Result.Info 之觀察方法（antitheft 成功）
```

**Expected Result**

```
1. PENDING: DR-PW23 SwitchOff_Timeout_Setting.Req
2. The signal value $STATUS_TELEMATIC.PowerSts_Telematic$ = 2 (Timed) is received
3. The signal value $STATUS_TELEMATIC.PowerSts_Telematic$ = 1 (Standby) is received after the PROXI Switch_Off_Time value has elapsed
4. PENDING: DR-PW23 Antitheft_Result.Info
```

**Remarks**

```
(R-P386 第一批)
```

## `NR1L-PowerManagement-103`　`SWE-PM-029`　Timeout1 follows Switch_Off_Time when the setting is zero

**Test Item**

```
IF SwitchOff_Timeout_Setting.Req == 00 min  THENTLM has to set Timeout1 to the value specified by PROXI parameter “Switch_Off_Time” (for example 20 minutes if Switch_Off_Time == 20 minutes), only for this case,  restoring it to "00 minutes" at next Ignition On event.

(read Timeout1 and then trigger an Ignition On event -> Timeout1 reads "00 minutes" again at the next Ignition On event)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. SwitchOff_Timeout_Setting.Req reads "00 min"
3. Switch_Off_Time reads 20 minutes
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. PENDING: DR-PW23 SwitchOff_Timeout_Setting.Req 之設定方法（設為 "00 min"）
2. Apply ENTER_TIMED and read the signal $STATUS_TELEMATIC.PowerSts_Telematic$ and check that it is 2 (Timed)
3. Hold for the PROXI Switch_Off_Time value, then read the signal $STATUS_TELEMATIC.PowerSts_Telematic$ and check that it is 1 (Standby)
```

**Expected Result**

```
1. PENDING: DR-PW23 SwitchOff_Timeout_Setting.Req
2. The signal value $STATUS_TELEMATIC.PowerSts_Telematic$ = 2 (Timed) is received
3. The signal value $STATUS_TELEMATIC.PowerSts_Telematic$ = 1 (Standby) is received after the PROXI Switch_Off_Time value has elapsed
```

**Remarks**

```
(R-P386 第一批)
```

## `NR1L-PowerManagement-104`　`SWE-PM-029`　Timeout1 follows PwrAccDelayAct when the setting is zero

**Test Item**

```
IF SwitchOff_Timeout_Setting.Req == 00 min  THEN TLM has to set Timeout1 to the value specified by $PwrAccDelayAct$ (for example 10  minutes if $PwrAccDelayAct$ == 10 minutes), only for this case,  restoring it to "00 minutes" at next Ignition  On event.

(read Timeout1 and then trigger an Ignition On event -> Timeout1 reads "00 minutes" again at the next Ignition On event)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. SwitchOff_Timeout_Setting.Req reads "00 min"
3. $PwrAccDelayAct$ reads 10 minutes
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. PENDING: DR-PW23 SwitchOff_Timeout_Setting.Req 之設定方法（設為 "00 min"）
2. Apply ENTER_TIMED and read the signal $STATUS_TELEMATIC.PowerSts_Telematic$ and check that it is 2 (Timed)
3. Hold for the $BCM_FD_27.Comfort_Enable_Time$ (DR-PW26) value, then read the signal $STATUS_TELEMATIC.PowerSts_Telematic$ and check that it is 1 (Standby)
```

**Expected Result**

```
1. PENDING: DR-PW23 SwitchOff_Timeout_Setting.Req
2. The signal value $STATUS_TELEMATIC.PowerSts_Telematic$ = 2 (Timed) is received
3. The signal value $STATUS_TELEMATIC.PowerSts_Telematic$ = 1 (Standby) is received after the $BCM_FD_27.Comfort_Enable_Time$ (DR-PW26) value has elapsed
```

**Remarks**

```
(R-P386 第一批)
```

## `NR1L-PowerManagement-116`　`SWE-PM-036`　Remote start from Timed passes the TLM to Partial Operation

**Test Item**

```
IF TLM_Status.Info and $Telematic_Power$ == "Timed"AND signal STATUS_BH_BCM2.RemStActvSts has a transition from "Remote Start Not Active" to “Remote Start Active”THENTLM has to set RemStartFail ="False" AND VPLastStatus = "On"AND TLM_Status.Info and $Telematic_Power$ to “Partial-Operation” value and it passes to TLM Partial Operation state.

(read $STATUS_TELEMATIC.PowerSts_Telematic$ -> The TLM passes to Partial Operation)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. Apply ENTER_TIMED
3. $STATUS_BH_BCM2.RemStActvSts$ = 0 (Remote Start Not Active)
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Send the signal $STATUS_BH_BCM2.RemStActvSts$ = 1 (Remote Start Active)
2. Read the signal $STATUS_TELEMATIC.PowerSts_Telematic$ and check that it is 7 (Partial_Operation)
```

**Expected Result**

```
1. The signal value $STATUS_BH_BCM2.RemStActvSts$ = 1 (Remote Start Active) is received
2. The signal value $STATUS_TELEMATIC.PowerSts_Telematic$ = 7 (Partial_Operation) is received
```

**Remarks**

```
(R-P376 丁案；原驗 RemStartFail / VPLastStatus 內部值，改驗其下游效果)
```

## `NR1L-PowerManagement-117`　`SWE-PM-037`　Call end in Timed with a failed remote start passes the TLM to Standby

**Test Item**

```
IF TLM_Status.Info and $Telematic_Power$ == "Timed" AND PhoneCall.Info becames "not Active"AND RemStartFail ==“True”THENTLM has to set RemStartFail  to “False” value and  TLM_Status.Info and $Telematic_Power$ to “Standby” value and it passes to Standby state.

(read the remote start outcome flag and the TLM state -> TLM_Status.Info and $Telematic_Power$ read "Standby" and the TLM passes to Standby state)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. TLM_Status.Info and $Telematic_Power$ read "Timed"
3. RemStartFail reads "True"
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. PENDING: DR-PW23 PhoneCall.Info 之驅動方法（使其轉為 "not Active"）
2. Read the signal $STATUS_TELEMATIC.PowerSts_Telematic$ and check that it is 1 (Standby)
3. PENDING: DR-PW23 RemStartFail 之觀察方法
```

**Expected Result**

```
1. PENDING: DR-PW23 PhoneCall.Info
2. The signal value $STATUS_TELEMATIC.PowerSts_Telematic$ = 1 (Standby) is received
3. PENDING: DR-PW23 RemStartFail
```

**Remarks**

```
(R-P383)
```

## `NR1L-PowerManagement-118`　`SWE-PM-039`　An SNA operational mode is handled as an ignition off event

**Test Item**

```
IF TLM receives signal LTM_OperationalModeSts.Info equal to "SNA" value  THEN TLM has to behave as an Ignition Pre Off or Ignition Off event occurs, according to par. "TLM Operative state management".

(read the TLM state against the operative state management rules -> The TLM behaves as for an Ignition Pre Off or Ignition Off event)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. The TLM is in an Ignition On working condition
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Apply ENTER_FULL_OPERATION, send the signal $STATUS_BH_BCM1.OperationalModeSts$ = 2 (Ignition_Off) (DR-PW26), then read the signal $STATUS_TELEMATIC.PowerSts_Telematic$ and record the value as State_ignoff
2. Apply ENTER_FULL_OPERATION again
3. Send the signal $STATUS_BH_BCM1.OperationalModeSts$ = 15 (SNA) (DR-PW26), then read the signal $STATUS_TELEMATIC.PowerSts_Telematic$ and check that it is the same value as State_ignoff
```

**Expected Result**

```
1. State_ignoff is recorded
2. The HU is in Full-Operation again
3. The signal value $STATUS_TELEMATIC.PowerSts_Telematic$ after the SNA value is the same as State_ignoff
```

**Remarks**

```
(R-P386 第一批)
```

## `NR1L-PowerManagement-119`　`SWE-PM-039`　A zero switch off timeout loads Timeout1 from the PROXI value

**Test Item**

```
IF TLM_Status.Info was equal to "Full-Operation" AND SwitchOff_Timeout_Setting.Req was equal to "00 min" or (Auto_SwitchOn_Setting.Req == Active for LTM High Radio), then TLM has to set Timeout1 to the "Switch_Off_Time" PROXI value.

(read Timeout1 against the configured parameter -> Timeout1 reads the "Switch_Off_Time" PROXI value)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. TLM_Status.Info was equal to "Full-Operation"
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. PENDING: DR-PW23 SwitchOff_Timeout_Setting.Req 之設定方法
2. Apply ENTER_FULL_OPERATION
3. Send the signal $STATUS_BH_BCM1.OperationalModeSts$ = 2 (Ignition_Off) (DR-PW26)
4. Read the signal $STATUS_TELEMATIC.PowerSts_Telematic$ and check that it is 2 (Timed), then hold for the PROXI Switch_Off_Time value and check that it is 1 (Standby)
```

**Expected Result**

```
1. PENDING: DR-PW23 SwitchOff_Timeout_Setting.Req
2. The HU is in Full-Operation
3. The signal value $STATUS_BH_BCM1.OperationalModeSts$ = 2 (Ignition_Off) is received
4. The signal value $STATUS_TELEMATIC.PowerSts_Telematic$ = 2 (Timed) is received, and $STATUS_TELEMATIC.PowerSts_Telematic$ = 1 (Standby) is received after the PROXI Switch_Off_Time value has elapsed
```

**Remarks**

```
(R-P386 第一批)
```

## `NR1L-PowerManagement-120`　`SWE-PM-039`　Auto switch on active on LTM High Radio loads Timeout1 from the PROXI value

**Test Item**

```
IF TLM_Status.Info was equal to "Full-Operation" AND SwitchOff_Timeout_Setting.Req was equal to "00 min" or (Auto_SwitchOn_Setting.Req == Active for LTM High Radio), then TLM has to set Timeout1 to the "Switch_Off_Time" PROXI value.

(read Timeout1 against the configured parameter -> Timeout1 reads the "Switch_Off_Time" PROXI value)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. TLM_Status.Info was equal to "Full-Operation"
3. The unit is an LTM High Radio
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. PENDING: DR-PW23 SwitchOff_Timeout_Setting.Req 之設定方法
2. Apply ENTER_FULL_OPERATION
3. Send the signal $STATUS_BH_BCM1.OperationalModeSts$ = 2 (Ignition_Off) (DR-PW26)
4. Read the signal $STATUS_TELEMATIC.PowerSts_Telematic$ and check that it is 2 (Timed), then hold for the PROXI Switch_Off_Time value and check that it is 1 (Standby)
```

**Expected Result**

```
1. PENDING: DR-PW23 SwitchOff_Timeout_Setting.Req
2. The HU is in Full-Operation
3. The signal value $STATUS_BH_BCM1.OperationalModeSts$ = 2 (Ignition_Off) is received
4. The signal value $STATUS_TELEMATIC.PowerSts_Telematic$ = 2 (Timed) is received, and $STATUS_TELEMATIC.PowerSts_Telematic$ = 1 (Standby) is received after the PROXI Switch_Off_Time value has elapsed
```

**Remarks**

```
(R-P386 第一批)
```

## `NR1L-PowerManagement-121`　`SWE-PM-039`　Only TLM menu items are guaranteed in the Timed status

**Test Item**

```
IF TLM passes to Timed status due to this two conditions, THEN only TLM menu items (not related to vehicle setup) must be guaranteed. See TLM HMI documents for TLM items.

(read the offered items against the TLM HMI documents -> Only TLM menu items that are not related to vehicle setup are guaranteed)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. The TLM passed to Timed status due to the two conditions of this clause
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Apply ENTER_TIMED
2. PENDING: DR-PW27 TLM HMI documents —— 該態下應不可用之 vehicle setup 項目清單
```

**Expected Result**

```
1. The HU is in Timed
2. PENDING: DR-PW27 TLM HMI documents
```

**Remarks**

```
(R-P386 第一批)
```

## `NR1L-PowerManagement-123`　`SWE-PM-041`　No TLM function is available in the TLM off with network on status

**Test Item**

```
No TLM, FPDM, AMP, ICS, and DTV functionality is available.

(read the FPDM, AMP, ICS and DTV functions -> No TLM, FPDM, AMP, ICS and DTV functionality is available)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. The TLM is in an Ignition Off working condition
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Apply ENTER_STANDBY
2. Read the signal $STATUS_TELEMATIC.PowerSts_Telematic$ and check that it is 1 (Standby)
3. Apply FUNC_STATE_STANDBY and check each of its sub-items
```

**Expected Result**

```
1. The HU is in Standby
2. The signal value $STATUS_TELEMATIC.PowerSts_Telematic$ = 1 (Standby) is received
3. FUNC_STATE_STANDBY holds:
   a. No audio source is playing on the HU speakers
   b. No amplifier output is present on the HU speakers
   c. The HU display is off, except for the HMI Antitheft Screens (CFTS009-4941457)
   d. PENDING: DR-PW27 BoosterOUT / antenna supply OFF 位準值
   e. A USB device inserted on the bench is not enumerated and the AUX input does not play on the HU speakers
```

**Remarks**

```
(R-P386 第一批)
```

## `NR1L-PowerManagement-125`　`SWE-PM-042`　No TLM function is available in the TLM off with network off status

**Test Item**

```
No TLM, FPDM AMP, ICS, and DTV functionality is available.

(read the FPDM, AMP, ICS and DTV functions -> No TLM, FPDM AMP, ICS and DTV functionality is available)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. The TLM is in an Ignition Off working condition
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Apply ENTER_STANDBY, then stop all bus activity on Body CAN and let the network go to sleep
2. PENDING: DR-PW26 Sleep 態之觀察方法（CAN 睡眠後無法以 CAN 讀 $STATUS_TELEMATIC.PowerSts_Telematic$）
3. Apply FUNC_STATE_SLEEP and check each of its sub-items
```

**Expected Result**

```
1. The Body CAN goes to sleep
2. PENDING: DR-PW26 Sleep 態之觀察方法
3. FUNC_STATE_SLEEP holds:
   a. No audio source is playing and no amplifier output is present
   b. The HU display is off, except for the HMI Antitheft Screens (CFTS009-4941457)
   c. PENDING: DR-PW27 BoosterOUT / antenna supply OFF 位準值
   d. A USB device is not enumerated and the AUX input does not play
```

**Remarks**

```
(R-P386 第二批)
```

## `NR1L-PowerManagement-143`　`SWE-PM-048`　The ex-factory default selects recall last with the last status on

**Test Item**

```
Default:         The ex-factory default must be "Auto_SwitchOn_Setting.Req == Recall_Last" AND VPLastStatus == On.

(read the stored last status -> VPLastStatus reads "On")
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. The TLM carries the ex-factory configuration
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. PENDING: DR-PW25 —— TLM 選單中該設定項之名稱與進入路徑
2. PENDING: DR-PW25 —— 讀該設定項所列之可選值（數與名）
```

**Expected Result**

```
1. PENDING: DR-PW25
2. PENDING: DR-PW25
```

**Remarks**

```
(R-P386 第一批)
```

## `NR1L-PowerManagement-148`　`SWE-PM-053`　The vehicle brand logo screen follows the brand configuration parameter

**Test Item**

```
TLM has to read Brand_Configuration_2 PROXI parameter in order to show the vehicle brand logo screen.

(read the shown logo against the configured parameter -> The vehicle brand logo shown matches the Brand_Configuration_2 PROXI parameter)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. The TLM carries a configured brand parameter
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Set VC_VEH_BRAND = the brand under test (DR-PW28)
2. Apply ENTER_FULL_OPERATION and let the brand logo screen be shown
3. Read the "Brand Logo Screen" and check which of the named logos is shown on it
```

**Expected Result**

```
1. The brand configuration is applied
2. The brand logo screen is shown
3. The logo shown on the "Brand Logo Screen" is the one named for this configuration
```

**Remarks**

```
(R-P386 第一批)
```

## `NR1L-PowerManagement-149`　`SWE-PM-054`　No audio brand without SDARS shows the vehicle brand logo only

**Test Item**

```
- IF SDARS_Presence == "Absent" AND Audio_Brand == "No Audio Brand"THEN TLM has to show the vehicle brand logo only, that depends on Brand_Configuration_2 parameter value;

(read the shown logos -> The vehicle brand logo that depends on the Brand_Configuration_2 parameter value is shown alone)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. SDARS_Presence reads "Absent"
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Set VC_VEH_BRAND = the brand under test (DR-PW28)
2. Apply ENTER_FULL_OPERATION and let the brand logo screen be shown
3. Read the "Brand Logo Screen" and check which of the named logos is shown on it
```

**Expected Result**

```
1. The brand configuration is applied
2. The brand logo screen is shown
3. The logo shown on the "Brand Logo Screen" is the one named for this configuration
```

**Remarks**

```
(R-P386 第一批) (R-P357(b)：與 NR1L-PowerManagement-192 五欄逐字相同而 Req ID 不同（SWE-PM-054 / SWE-PM-101），二列皆保留不得合併；互註對方 tc_id)
```

## `NR1L-PowerManagement-150`　`SWE-PM-054`　Beats brand white without SDARS adds the Beats logo

**Test Item**

```
- IF SDARS_Presence == "Absent" AND Audio_Brand == "Beats Brand White"THEN TLM has to show the Beats Brand White logo in addition to the vehicle brand logo;

(read the shown logos -> The Beats Brand White logo is shown in addition to the vehicle brand logo)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. SDARS_Presence reads "Absent"
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Set VC_VEH_BRAND = the brand under test (DR-PW28)
2. Apply ENTER_FULL_OPERATION and let the brand logo screen be shown
3. Read the "Brand Logo Screen" and check which of the named logos is shown on it
```

**Expected Result**

```
1. The brand configuration is applied
2. The brand logo screen is shown
3. The logo shown on the "Brand Logo Screen" is the one named for this configuration
```

**Remarks**

```
(R-P386 第一批) (R-P357(b)：與 NR1L-PowerManagement-193 五欄逐字相同而 Req ID 不同（SWE-PM-054 / SWE-PM-101），二列皆保留不得合併；互註對方 tc_id)
```

## `NR1L-PowerManagement-151`　`SWE-PM-054`　SDARS present without audio brand adds the Sirius logo

**Test Item**

```
- IF SDARS_Presence == "Present" AND Audio_Brand == "No Audio Brand"THEN TLM has to show the Sirius logo in addition to the vehicle brand logo;

(read the shown logos -> The Sirius logo is shown in addition to the vehicle brand logo)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. SDARS_Presence reads "Present"
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Set VC_VEH_BRAND = the brand under test (DR-PW28)
2. Apply ENTER_FULL_OPERATION and let the brand logo screen be shown
3. Read the "Brand Logo Screen" and check which of the named logos is shown on it
```

**Expected Result**

```
1. The brand configuration is applied
2. The brand logo screen is shown
3. The logo shown on the "Brand Logo Screen" is the one named for this configuration
```

**Remarks**

```
(R-P386 第一批) (R-P357(b)：與 NR1L-PowerManagement-194 五欄逐字相同而 Req ID 不同（SWE-PM-054 / SWE-PM-101），二列皆保留不得合併；互註對方 tc_id)
```

## `NR1L-PowerManagement-152`　`SWE-PM-054`　SDARS present with beats brand white adds both logos

**Test Item**

```
- IF SDARS_Presence == "Present" AND Audio_Brand == "Beats Brand White"THEN TLM has to show both the Sirius and the Beats logos in addition to the vehicle brand logo.

(read the shown logos -> Both the Sirius and the Beats logos are shown in addition to the vehicle brand logo)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. SDARS_Presence reads "Present"
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Set VC_VEH_BRAND = the brand under test (DR-PW28)
2. Apply ENTER_FULL_OPERATION and let the brand logo screen be shown
3. Read the "Brand Logo Screen" and check which of the named logos is shown on it
```

**Expected Result**

```
1. The brand configuration is applied
2. The brand logo screen is shown
3. The logo shown on the "Brand Logo Screen" is the one named for this configuration
```

**Remarks**

```
(R-P386 第一批) (R-P357(b)：與 NR1L-PowerManagement-195 五欄逐字相同而 Req ID 不同（SWE-PM-054 / SWE-PM-101），二列皆保留不得合併；互註對方 tc_id)
```

## `NR1L-PowerManagement-155`　`SWE-PM-056`　The Fiat Latam startup animation replaces the vehicle brand logo

**Test Item**

```
If DID "Startup Animation Selection" = [Fiat Latam] then the HU shall replace the vehicle brand logo with the Fiat Latam Logo regardless of the value of $VC_Veh_Brand$

(read the shown logo against the configured brand -> The Fiat Latam Logo replaces the vehicle brand logo regardless of the configured brand)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. The HU carries a configured vehicle brand
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Write the DID Startup Animation Selection = Fiat Latam
2. Set VC_VEH_BRAND = a value other than Fiat (DR-PW28)
3. Apply ENTER_FULL_OPERATION and read the "Brand Logo Screen", and check that the "Fiat Latam Logo" is shown on it in place of the "Vehicle Brand Logo"
```

**Expected Result**

```
1. The DID is written
2. The brand configuration is applied
3. The "Fiat Latam Logo" is shown on the "Brand Logo Screen" in place of the "Vehicle Brand Logo"
```

**Remarks**

```
(R-P386 第一批) (R-P357(b)：與 NR1L-PowerManagement-185 五欄逐字相同而 Req ID 不同（SWE-PM-056 / SWE-PM-097），二列皆保留不得合併；互註對方 tc_id)
```

## `NR1L-PowerManagement-156`　`SWE-PM-058`　The ex-factory default sets a zero switch off timeout

**Test Item**

```
Default:The ex-factory default must be "SwitchOff_Timeout_Setting.Req == 00 MIN" and  (Auto_SwitchOn_Setting.Req =="Active", and  Timeout1 == 00 MIN" for LTM High Radio)

(read the auto switch on parameter and Timeout1 -> Auto_SwitchOn_Setting.Req reads "Active" and Timeout1 reads "00 MIN")
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. The TLM carries the ex-factory configuration
3. The unit is an LTM High Radio
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. PENDING: DR-PW25 —— TLM 選單中該設定項之名稱與進入路徑
2. PENDING: DR-PW25 —— 讀該設定項所列之可選值（數與名）
```

**Expected Result**

```
1. PENDING: DR-PW25
2. PENDING: DR-PW25
```

**Remarks**

```
(R-P386 第一批)
```

## `NR1L-PowerManagement-169`　`SWE-PM-075`　The HU leaves Timed one minute after the FOTA pop-up is left untouched

**Test Item**

```
If the HU Transitions to Timed mode due to the condition described in CFTS009-1809, the HU shall transition to Standby mode after: -1 minute has passed without the user interacting with the pop-up -The FOTA pop up is dismissed -$ACCDlyAct$ transitions from active to inactive

(read the HU mode after the idle period -> The HU transitions to Standby mode after 1 minute has passed)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. The HU is in Timed mode due to the condition described in CFTS009-1809
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. PENDING: DR-PW27 CFTS057 —— FOTA update available 之建立方法
2. Apply ENTER_TIMED
3. Read the HU screen and check that the "FOTA update available" pop-up is shown
4. Hold for 60000 ms with no user interaction, then read the HU screen and check that the "FOTA update available" pop-up is still shown on it
5. Read the signal $STATUS_TELEMATIC.PowerSts_Telematic$ and check that it is 1 (Standby)
```

**Expected Result**

```
1. PENDING: DR-PW27 CFTS057
2. The HU is in Timed
3. The "FOTA update available" pop-up is shown on the HU screen
4. The "FOTA update available" pop-up is still shown on the HU screen after 60000 ms with no user interaction
5. The signal value $STATUS_TELEMATIC.PowerSts_Telematic$ = 1 (Standby) is received
```

**Remarks**

```
(R-P386 第一批)
```

## `NR1L-PowerManagement-172`　`SWE-PM-076`　A ten second power button press performs a radio reset and saves logs

**Test Item**

```
the HU shall reset both the main CPU and the CAN micro at the time of the reset

(read both processors -> b. The CAN micro resets at the time of the reset)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. The HU is not installing a firmware image
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Press and hold the H/K "Power" button for 10 seconds consecutively
2. Read the HU screen and check that it goes dark and then shows the "Splash Screen" again
3. Read the bus trace and check that the $STATUS_TELEMATIC$ message stops being transmitted and then resumes
```

**Expected Result**

```
1. The H/K "Power" button is held for 10 seconds consecutively
2. The HU screen goes dark and then shows the "Splash Screen" again
3. The $STATUS_TELEMATIC$ message stops being transmitted on the bus trace and then resumes
```

**Remarks**

```
(R-P383)
```

## `NR1L-PowerManagement-182`　`SWE-PM-093`　A second start-up animation waits for the wakeup cycle or thirty minutes

**Test Item**

```
While HU is in SLEEP MODE, STANDBY MODE, or in PARTIAL OPERATION MODE and when driver door ajar status ($Door_Ajar_Status$) is changed to CLOSED, HU shall play a start-up animation as defined per HMI. The startup animation will be unique for each vehicle or/and brand as defined per PDO/HMI. If driver door is not present or removed for current vehicle ($DriverDoorOnOffSts$ = [DOOR_OFF]), or if HU changes mode (due to ignition event or to due to HU power mode status change to BODY ON or to TIMED MODE while driver door ajar status ($Door_Ajar_Status$) is OPEN, the HU shall skip start-up animation. While HU is playing a start-up animation and HU changes mode (due to ignition event or to HU power mode status changes to BODY ON or to TIMED MODE) or if an ignition crank event ($PowerMode$ = [IGN_START ]), then HU shall cancel current start-up animation and switch required power mode as defined. Once a start-up animation is played, HU shall not play the next start-up animation until the next CAN wakeup cycle OR at least 30 minutes passed from last time the start-up animation was played ;whichever is greater; given all other conditions are met for startup animation to play as defined here.

(read the screen against the elapsed time -> A start-up animation plays again only at the next CAN wakeup cycle or after 30 minutes, whichever is greater)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. The HU has just played a start-up animation
3. All other conditions for the animation hold
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Set Door_Ajar_Status = "Open" then "Closed" again within the same CAN wakeup cycle (DR-PW28)
2. Read the HU screen and check that the "Start-up Animation" is not played on it
3. Hold for 1800000 ms, repeat the door event, then read the HU screen and check that the "Start-up Animation" is played on it
```

**Expected Result**

```
1. The door event is registered within the same CAN wakeup cycle
2. The "Start-up Animation" is not played on the HU screen
3. After 1800000 ms the "Start-up Animation" is played on the HU screen
```

**Remarks**

```
(R-P386 第二批)
```

## `NR1L-PowerManagement-185`　`SWE-PM-097`　The Fiat Latam startup animation selection replaces the vehicle brand logo

**Test Item**

```
If DID "Startup Animation Selection" = [Fiat Latam] then the HU shall replace the vehicle brand logo with the Fiat Latam Logo regardless of the value of $VC_Veh_Brand$

(read the shown logo against the configured brand -> The Fiat Latam Logo replaces the vehicle brand logo regardless of the configured brand)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. The HU carries a configured vehicle brand
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Write the DID Startup Animation Selection = Fiat Latam
2. Set VC_VEH_BRAND = a value other than Fiat (DR-PW28)
3. Apply ENTER_FULL_OPERATION and read the "Brand Logo Screen", and check that the "Fiat Latam Logo" is shown on it in place of the "Vehicle Brand Logo"
```

**Expected Result**

```
1. The DID is written
2. The brand configuration is applied
3. The "Fiat Latam Logo" is shown on the "Brand Logo Screen" in place of the "Vehicle Brand Logo"
```

**Remarks**

```
(R-P386 第一批) (R-P357(b)：與 NR1L-PowerManagement-155 五欄逐字相同而 Req ID 不同（SWE-PM-056 / SWE-PM-097），二列皆保留不得合併；互註對方 tc_id)
```

## `NR1L-PowerManagement-186`　`SWE-PM-098`　The always setting plays a startup sound with the animation

**Test Item**

```
If $Themed_Sound$ = [Fiat Latam] and the "Welcome Onboard Sound" setting is set to "Always", the HU startup animation shall be accompanied by a startup sound that begins at the same time.

(read the audio output against the animation start -> A startup sound accompanies the animation and begins at the same time)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. Set Themed_Sound = "Fiat Latam" (DR-PW28)
3. Select "Welcome Onboard Sound" = "Always"
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Start video and audio recording on the bench
2. Apply ENTER_FULL_OPERATION and let the startup animation play
3. Read the recording and check that the startup sound is present on the HU speakers starting at the same time as the first frame of the "Start-up Animation"
```

**Expected Result**

```
1. The recording is running
2. The startup animation plays
3. On the recording, the startup sound starts at the same time as the first frame of the animation
```

**Remarks**

```
(R-P386 第二批)
```

## `NR1L-PowerManagement-187`　`SWE-PM-099`　The once a day setting plays the startup sound on the first startup of the day

**Test Item**

```
If $Themed_Sound$ = [Fiat Latam] AND the "Welcome Onboard Sound" setting is set to "Once a Day" AND the HU has not yet played the startup sound yet that day, the HU startup animation shall be accompanied by a startup sound that begins at the same time.

(read the audio output against the animation start -> A startup sound accompanies the animation and begins at the same time)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. Set Themed_Sound = "Fiat Latam" (DR-PW28)
3. Select "Welcome Onboard Sound" = "Once a day"
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Start video and audio recording on the bench
2. Apply ENTER_FULL_OPERATION and let the startup animation play
3. Read the recording and check that the startup sound is present on the HU speakers starting at the same time as the first frame of the "Start-up Animation" on the first startup of the day
```

**Expected Result**

```
1. The recording is running
2. The startup animation plays
3. On the recording, the startup sound starts at the same time as the first frame of the animation on the first startup of the day
```

**Remarks**

```
(R-P386 第二批)
```

## `NR1L-PowerManagement-191`　`SWE-PM-100`　The never setting plays no startup sound with the animation

**Test Item**

```
If $Themed_Sound$ = [Fiat Latam] AND the "Welcome Onboard Sound" setting is set to "Never", the HU startup animation shall not be accompanied by a startup sound that begins at the same time.

(read the audio output against the animation start -> No startup sound accompanies the animation)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. Set Themed_Sound = "Fiat Latam" (DR-PW28)
3. Select "Welcome Onboard Sound" = "Never"
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Start video and audio recording on the bench
2. Apply ENTER_FULL_OPERATION and let the startup animation play
3. Read the recording and check that no startup sound is present on the HU speakers while the "Start-up Animation" runs
```

**Expected Result**

```
1. The recording is running
2. The startup animation plays
3. On the recording, no startup sound is played while the animation runs
```

**Remarks**

```
(R-P386 第二批)
```

## `NR1L-PowerManagement-192`　`SWE-PM-101`　No audio brand without SDARS shows the vehicle brand logo only

**Test Item**

```
- IF SDARS_Presence == "Absent" AND Audio_Brand == "No Audio Brand"THEN TLM has to show the vehicle brand logo only, that depends on Brand_Configuration_2 parameter value;

(read the shown logos -> The vehicle brand logo that depends on the Brand_Configuration_2 parameter value is shown alone)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. SDARS_Presence reads "Absent"
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Set VC_VEH_BRAND = the brand under test (DR-PW28)
2. Apply ENTER_FULL_OPERATION and let the brand logo screen be shown
3. Read the "Brand Logo Screen" and check which of the named logos is shown on it
```

**Expected Result**

```
1. The brand configuration is applied
2. The brand logo screen is shown
3. The logo shown on the "Brand Logo Screen" is the one named for this configuration
```

**Remarks**

```
(R-P386 第一批) (R-P357(b)：與 NR1L-PowerManagement-149 五欄逐字相同而 Req ID 不同（SWE-PM-054 / SWE-PM-101），二列皆保留不得合併；互註對方 tc_id)
```

## `NR1L-PowerManagement-193`　`SWE-PM-101`　Beats brand white without SDARS adds the Beats logo

**Test Item**

```
- IF SDARS_Presence == "Absent" AND Audio_Brand == "Beats Brand White"THEN TLM has to show the Beats Brand White logo in addition to the vehicle brand logo;

(read the shown logos -> The Beats Brand White logo is shown in addition to the vehicle brand logo)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. SDARS_Presence reads "Absent"
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Set VC_VEH_BRAND = the brand under test (DR-PW28)
2. Apply ENTER_FULL_OPERATION and let the brand logo screen be shown
3. Read the "Brand Logo Screen" and check which of the named logos is shown on it
```

**Expected Result**

```
1. The brand configuration is applied
2. The brand logo screen is shown
3. The logo shown on the "Brand Logo Screen" is the one named for this configuration
```

**Remarks**

```
(R-P386 第一批) (R-P357(b)：與 NR1L-PowerManagement-150 五欄逐字相同而 Req ID 不同（SWE-PM-054 / SWE-PM-101），二列皆保留不得合併；互註對方 tc_id)
```

## `NR1L-PowerManagement-194`　`SWE-PM-101`　SDARS present without audio brand adds the Sirius logo

**Test Item**

```
- IF SDARS_Presence == "Present" AND Audio_Brand == "No Audio Brand"THEN TLM has to show the Sirius logo in addition to the vehicle brand logo;

(read the shown logos -> The Sirius logo is shown in addition to the vehicle brand logo)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. SDARS_Presence reads "Present"
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Set VC_VEH_BRAND = the brand under test (DR-PW28)
2. Apply ENTER_FULL_OPERATION and let the brand logo screen be shown
3. Read the "Brand Logo Screen" and check which of the named logos is shown on it
```

**Expected Result**

```
1. The brand configuration is applied
2. The brand logo screen is shown
3. The logo shown on the "Brand Logo Screen" is the one named for this configuration
```

**Remarks**

```
(R-P386 第一批) (R-P357(b)：與 NR1L-PowerManagement-151 五欄逐字相同而 Req ID 不同（SWE-PM-054 / SWE-PM-101），二列皆保留不得合併；互註對方 tc_id)
```

## `NR1L-PowerManagement-195`　`SWE-PM-101`　SDARS present with beats brand white adds both logos

**Test Item**

```
- IF SDARS_Presence == "Present" AND Audio_Brand == "Beats Brand White"THEN TLM has to show both the Sirius and the Beats logos in addition to the vehicle brand logo.

(read the shown logos -> Both the Sirius and the Beats logos are shown in addition to the vehicle brand logo)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. SDARS_Presence reads "Present"
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Set VC_VEH_BRAND = the brand under test (DR-PW28)
2. Apply ENTER_FULL_OPERATION and let the brand logo screen be shown
3. Read the "Brand Logo Screen" and check which of the named logos is shown on it
```

**Expected Result**

```
1. The brand configuration is applied
2. The brand logo screen is shown
3. The logo shown on the "Brand Logo Screen" is the one named for this configuration
```

**Remarks**

```
(R-P386 第一批) (R-P357(b)：與 NR1L-PowerManagement-152 五欄逐字相同而 Req ID 不同（SWE-PM-054 / SWE-PM-101），二列皆保留不得合併；互註對方 tc_id)
```

## `NR1L-PowerManagement-202`　`SWE-PM-103`　ICS stays available while DTV is off in this status

**Test Item**

```
This status is related to TLM audio is OFF. TLM shall allow only Splash Screen visualization on its display.  ICS functionalities are available.  DTV shall be OFF.

(read the ICS functions and the DTV -> DTV is OFF)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. The TLM is in an Ignition On Engine On working condition
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Apply ENTER_IDLE
2. Read the signal $STATUS_TELEMATIC.PowerSts_Telematic$ and check that it is 3 (Idle)
3. Read the HU screen and check that only the "Splash Screen" is shown on it and no audio source is playing on the HU speakers
4. Touch the screen and read the bus trace, and check that $TELEMATIC_FD_5.CM_TCH_STAT$ is 1 (TCH_PSD) and that $TELEMATIC_FD_5.CM_TCH_X_COORD$ and $TELEMATIC_FD_5.CM_TCH_Y_COORD$ carry the touch coordinates
```

**Expected Result**

```
1. The HU is in Idle
2. The signal value $STATUS_TELEMATIC.PowerSts_Telematic$ = 3 (Idle) is received
3. Only the "Splash Screen" is shown on the HU screen and no audio source is playing on the HU speakers
4. The signal value $TELEMATIC_FD_5.CM_TCH_STAT$ = 1 (TCH_PSD) is received together with the touch coordinates on $TELEMATIC_FD_5.CM_TCH_X_COORD$ and $TELEMATIC_FD_5.CM_TCH_Y_COORD$
```

**Remarks**

```
(R-P386 第二批)
```

## `NR1L-PowerManagement-216`　`SWE-PM-106`　The SOS button variant selects the SOS disclaimer text

**Test Item**

```
For all variations of the disclaimer screen and geolocation pop up listed below, if the configuration parameter $Ecall_Button_Variant$ = [SOS] the HU shall use the SOS text for the disclaimer.

(read the disclaimer wording -> The HU uses the SOS text for the disclaimer)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. Set VC_VEH_BRAND = a value other than "Maserati" (DR-PW28)
3. Set TBM_Present = "Present" (DR-PW28)
4. PROXI Ecall_Button_Variant = the variant for this market
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Apply ENTER_FULL_OPERATION and let the HU reach the disclaimer presentation
2. Read the "Disclaimer" screen text and check that it contains "SOS"
```

**Expected Result**

```
1. The HU reaches the disclaimer presentation
2. The "Disclaimer" screen text contains "SOS"
```

**Remarks**

```
(R-P386 第一批)
```

## `NR1L-PowerManagement-217`　`SWE-PM-107`　The help button variant replaces the SOS text in the disclaimer

**Test Item**

```
For all variations of the disclaimer screen and geolocation pop up listed below, if the configuration parameter $Ecall_Button_Variant$ = [Help] the HU shall replace the "SOS" text with the "Help" version of the disclaimer.

(read the disclaimer wording -> The HU replaces the "SOS" text with the "Help" version of the disclaimer)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. Set VC_VEH_BRAND = a value other than "Maserati" (DR-PW28)
3. Set TBM_Present = "Present" (DR-PW28)
4. PROXI Ecall_Button_Variant = the variant for this market
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Apply ENTER_FULL_OPERATION and let the HU reach the disclaimer presentation
2. Read the "Disclaimer" screen text and check that it contains "Help" and does not contain "SOS"
```

**Expected Result**

```
1. The HU reaches the disclaimer presentation
2. The "Disclaimer" screen text contains "Help" and does not contain "SOS"
```

**Remarks**

```
(R-P386 第一批)
```

## `NR1L-PowerManagement-218`　`SWE-PM-108`　A non Maserati brand shows the core disclaimer once every thirty ignition cycles

**Test Item**

```
If $VC_VEH_BRAND$ <> [Maserati] the R1 Head Unit shall only show the core disclaimer screen once every 30 ignition cycles

(read the screen across the cycles -> The core disclaimer screen is shown only once every 30 ignition cycles)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. $VC_VEH_BRAND$ reads a value other than "Maserati"
```

**Input Test Data**

```
Ignition cycles: 31 consecutive Ignition Off -> Ignition On cycles
```

**Test procedure**

```
1. Repeat the ignition cycle listed in Input Test Data 31 times, sending $STATUS_BH_BCM1.OperationalModeSts$ = 2 (Ignition_Off) then = 4 (Ignition_On) (DR-PW26)
2. After each cycle, read the HU screen and record whether the "Disclaimer" screen is shown
3. Count the cycles in which the "Disclaimer" screen was shown
```

**Expected Result**

```
1. All 31 ignition cycles are registered without a bus error
2. The presence of the "Disclaimer" screen is recorded for each of the 31 cycles
3. The "Disclaimer" screen was shown in exactly 2 cycles: cycle 1 and cycle 31
```

**Remarks**

```
(R-P386 第一批)
```

## `NR1L-PowerManagement-222`　`SWE-PM-111`　A missing TBM adds the ADAS text to the disclaimer

**Test Item**

```
For all screen sizes except 7 inch If $VC_VEH_BRAND$ <> [Maserati] AND ($TBM_Present$ = [Not Present] OR$Country_Code$  does not require SOS or Geolocation) then the HU shall add the ADAS text to the disclaimer

(read the disclaimer wording -> The HU adds the ADAS text to the disclaimer)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. The screen size is other than 7 inch
3. Set VC_VEH_BRAND = a value other than "Maserati" (DR-PW28)
4. Set TBM_Present = "Not Present" (DR-PW28)
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Apply ENTER_FULL_OPERATION and let the HU reach the disclaimer presentation
2. Read the "Disclaimer" screen text and check the added text
3. PENDING: DR-PW27 HMI disclaimer wording —— the ADAS text 之逐字定義
```

**Expected Result**

```
1. The HU reaches the disclaimer presentation
2. The "Disclaimer" screen text is read
3. PENDING: DR-PW27 HMI disclaimer wording
```

**Remarks**

```
(R-P386 第二批)
```

## `NR1L-PowerManagement-223`　`SWE-PM-111`　A country not requiring SOS or geolocation adds the ADAS text

**Test Item**

```
For all screen sizes except 7 inch If $VC_VEH_BRAND$ <> [Maserati] AND ($TBM_Present$ = [Not Present] OR$Country_Code$  does not require SOS or Geolocation) then the HU shall add the ADAS text to the disclaimer

(read the disclaimer wording -> The HU adds the ADAS text to the disclaimer)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. The screen size is other than 7 inch
3. Set VC_VEH_BRAND = a value other than "Maserati" (DR-PW28)
4. PROXI Country_Code = a market that requires neither SOS nor geolocation
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Apply ENTER_FULL_OPERATION and let the HU reach the disclaimer presentation
2. Read the "Disclaimer" screen text and check the added text
3. PENDING: DR-PW27 HMI disclaimer wording —— the ADAS text 之逐字定義
```

**Expected Result**

```
1. The HU reaches the disclaimer presentation
2. The "Disclaimer" screen text is read
3. PENDING: DR-PW27 HMI disclaimer wording
```

**Remarks**

```
(R-P386 第二批)
```

## `NR1L-PowerManagement-224`　`SWE-PM-113`　A geolocation and SOS market adds the ADAS and SOS text

**Test Item**

```
For all screen sizes except 7 inch If $VC_VEH_BRAND$ <> [Maserati] AND $TBM_Present$ = [Present]  AND $Country_Code$ requires geolocation and SOS in the disclaimer then the HU shall add the ADAS and SOS to the geolocation pop-up or disclaimer. See HMI for different statup conditions to determine when to add geolocation + SOS Pop-up or add geolocation and SOS text to Disclaimer.

(read the shown wording -> The HU adds the ADAS and SOS to the geolocation pop-up or disclaimer)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. The screen size is other than 7 inch
3. Set VC_VEH_BRAND = a value other than "Maserati" (DR-PW28)
4. Set TBM_Present = "Present" (DR-PW28)
5. PROXI Country_Code = a market that requires geolocation and SOS in the disclaimer
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Apply ENTER_FULL_OPERATION and let the HU reach the disclaimer presentation
2. Read the HU screen and check whether the "Disclaimer" screen or the geolocation pop-up is shown
3. PENDING: DR-PW27 HMI disclaimer wording（ADAS ＋ SOS 之逐字文字）
```

**Expected Result**

```
1. The HU reaches the disclaimer presentation
2. Either the "Disclaimer" screen or the geolocation pop-up is shown
3. PENDING: DR-PW27 HMI disclaimer wording
```

**Remarks**

```
(R-P383) (R-P386 第二批)
```

## `NR1L-PowerManagement-228`　`SWE-PM-078`　A none special package falls back to the brand default theme

**Test Item**

```
If $VC_SpecialPKG$ = [none] or indicates a value that is not supported by the HU, the default theme based on the $VC_VEH_BRAND$ signal shall be used. See the latest version of [PDO Theme Configuration] for the default value definition.

(read the applied theme against the brand signal -> The default theme based on the $VC_VEH_BRAND$ signal is used)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. Set VC_VEH_BRAND = the configured brand (DR-PW28)
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Set VC_SpecialPKG = "none" (DR-PW28)
2. Read the "Theme" applied on the HU screen
3. PENDING: DR-PW27 [PDO Theme Configuration] —— 該品牌之預設 theme
```

**Expected Result**

```
1. The special package value is applied
2. The applied theme is read from the HU screen
3. PENDING: DR-PW27 [PDO Theme Configuration]
```

**Remarks**

```
(R-P386 第二批)
```

## `NR1L-PowerManagement-229`　`SWE-PM-078`　An unsupported special package falls back to the brand default theme

**Test Item**

```
If $VC_SpecialPKG$ = [none] or indicates a value that is not supported by the HU, the default theme based on the $VC_VEH_BRAND$ signal shall be used. See the latest version of [PDO Theme Configuration] for the default value definition.

(read the applied theme against the brand signal -> The default theme based on the $VC_VEH_BRAND$ signal is used)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. Set VC_VEH_BRAND = the configured brand (DR-PW28)
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Set VC_SpecialPKG = an unsupported value (DR-PW28)
2. Read the "Theme" applied on the HU screen
3. PENDING: DR-PW27 [PDO Theme Configuration] —— 該品牌之預設 theme
```

**Expected Result**

```
1. The special package value is applied
2. The applied theme is read from the HU screen
3. PENDING: DR-PW27 [PDO Theme Configuration]
```

**Remarks**

```
(R-P386 第二批)
```

## `NR1L-PowerManagement-230`　`SWE-PM-079`　An unsupported CAN value on a branded element uses the PDO default

**Test Item**

```
For all of the PDO branded elements listed, if the CAN signals referenced indicate a value that is not supported by the HU, the default value defined by PDO shall be used. See the latest version of [PDO Theme Configuration] and the "Theme Name" defaults for the values to use.

(read the shown element -> The default value defined by PDO is used for that branded element)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. The HU is displaying a PDO branded element
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Send an unsupported CAN value for the branded element
2. Read the "PDO Branded Element" shown on the HU screen
3. PENDING: DR-PW27 [PDO Theme Configuration] —— 該元件之預設值與元件清單
```

**Expected Result**

```
1. The unsupported value is sent
2. The shown element is read
3. PENDING: DR-PW27 [PDO Theme Configuration]
```

**Remarks**

```
(R-P386 第二批)
```

## `NR1L-PowerManagement-233`　`SWE-PM-081`　The Chrysler brand selects the Chrysler font

**Test Item**

```
The HU shall use $VC_VEH_BRAND$ to determine the correct font to display. Valid values are as follows:$VC_VEH_BRAND$  = [Chrysler] shall indicate Chrysler font. $VC_VEH_BRAND$  = [Dodge] shall indicate Dodge font. $VC_VEH_BRAND$  = [Jeep] shall indicate Jeep font. $VC_VEH_BRAND$  = [Alpha Romeo] shall indicate Alfa Romeo font. $VC_VEH_BRAND$  = [Fiat] shall indicate Fiat font. (DEFAULT)$VC_VEH_BRAND$  = [Lancia] shall indicate Lancia font. $VC_VEH_BRAND$  = [Maserati] shall indicate Maserati font. $VC_VEH_BRAND$  = [Ram] shall indicate Ram font.$VC_VEH_BRAND$  = [Abarth] shall indicate Abarth font.$VC_VEH_BRAND$  = [Opel] shall indicate Opel font.$VC_VEH_BRAND$  = [Vauxhall] shall indicate Vauxhall font.$VC_VEH_BRAND$  = [Citroen] shall indicate Citroen font.$VC_VEH_BRAND$  = [Peugeot] shall indicate Peugeot font.

(read the displayed font -> The HU displays the Chrysler font)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. The HU is displaying branded text
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Set VC_VEH_BRAND = "Chrysler" (DR-PW28)
2. Read the branded text on the HU screen and check that it is rendered in the "Chrysler font"
```

**Expected Result**

```
1. The brand configuration is applied
2. The branded text is rendered in the "Chrysler font"
```

**Remarks**

```
(R-P386 第二批)
```

## `NR1L-PowerManagement-234`　`SWE-PM-081`　The Jeep brand selects the Jeep font

**Test Item**

```
The HU shall use $VC_VEH_BRAND$ to determine the correct font to display. Valid values are as follows:$VC_VEH_BRAND$  = [Chrysler] shall indicate Chrysler font. $VC_VEH_BRAND$  = [Dodge] shall indicate Dodge font. $VC_VEH_BRAND$  = [Jeep] shall indicate Jeep font. $VC_VEH_BRAND$  = [Alpha Romeo] shall indicate Alfa Romeo font. $VC_VEH_BRAND$  = [Fiat] shall indicate Fiat font. (DEFAULT)$VC_VEH_BRAND$  = [Lancia] shall indicate Lancia font. $VC_VEH_BRAND$  = [Maserati] shall indicate Maserati font. $VC_VEH_BRAND$  = [Ram] shall indicate Ram font.$VC_VEH_BRAND$  = [Abarth] shall indicate Abarth font.$VC_VEH_BRAND$  = [Opel] shall indicate Opel font.$VC_VEH_BRAND$  = [Vauxhall] shall indicate Vauxhall font.$VC_VEH_BRAND$  = [Citroen] shall indicate Citroen font.$VC_VEH_BRAND$  = [Peugeot] shall indicate Peugeot font.

(read the displayed font -> The HU displays the Jeep font)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. The HU is displaying branded text
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Set VC_VEH_BRAND = "Jeep" (DR-PW28)
2. Read the branded text on the HU screen and check that it is rendered in the "Jeep font"
```

**Expected Result**

```
1. The brand configuration is applied
2. The branded text is rendered in the "Jeep font"
```

**Remarks**

```
(R-P386 第二批)
```

## `NR1L-PowerManagement-235`　`SWE-PM-081`　The Fiat brand selects the default Fiat font

**Test Item**

```
The HU shall use $VC_VEH_BRAND$ to determine the correct font to display. Valid values are as follows:$VC_VEH_BRAND$  = [Chrysler] shall indicate Chrysler font. $VC_VEH_BRAND$  = [Dodge] shall indicate Dodge font. $VC_VEH_BRAND$  = [Jeep] shall indicate Jeep font. $VC_VEH_BRAND$  = [Alpha Romeo] shall indicate Alfa Romeo font. $VC_VEH_BRAND$  = [Fiat] shall indicate Fiat font. (DEFAULT)$VC_VEH_BRAND$  = [Lancia] shall indicate Lancia font. $VC_VEH_BRAND$  = [Maserati] shall indicate Maserati font. $VC_VEH_BRAND$  = [Ram] shall indicate Ram font.$VC_VEH_BRAND$  = [Abarth] shall indicate Abarth font.$VC_VEH_BRAND$  = [Opel] shall indicate Opel font.$VC_VEH_BRAND$  = [Vauxhall] shall indicate Vauxhall font.$VC_VEH_BRAND$  = [Citroen] shall indicate Citroen font.$VC_VEH_BRAND$  = [Peugeot] shall indicate Peugeot font.

(read the displayed font -> The HU displays the Fiat font that the specification marks as DEFAULT)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. The HU is displaying branded text
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Set VC_VEH_BRAND = "Fiat" (DR-PW28)
2. Read the branded text on the HU screen and check that it is rendered in the "Fiat font"
```

**Expected Result**

```
1. The brand configuration is applied
2. The branded text is rendered in the "Fiat font"
```

**Remarks**

```
(R-P386 第二批)
```

## `NR1L-PowerManagement-236`　`SWE-PM-082`　The Chrysler brand selects the Chrysler App icon

**Test Item**

```
The HU shall use the $VC_VEH_BRAND$  signal to determine the correct App icon to display.$VC_VEH_BRAND$  = [Chrysler] shall indicate Chrysler App icon. $VC_VEH_BRAND$  = [Dodge] shall indicate Dodge App icon. $VC_VEH_BRAND$  = [Jeep] shall indicate Jeep App icon.$VC_VEH_BRAND$  = [Alpha Romeo] shall indicate Alfa Romeo App icon.$VC_VEH_BRAND$  = [Fiat] shall indicate Fiat App icon. (DEFAULT)$VC_VEH_BRAND$  = [Lancia] shall indicate Lancia App icon. $VC_VEH_BRAND$  = [Maserati] shall indicate Maserati App icon.$VC_VEH_BRAND$  = [Ram] shall indicate Ram App icon.$VC_VEH_BRAND$  = [Abarth] shall indicate Abarth App icon.$VC_VEH_BRAND$  = [Opel] shall indicate Opel font App icon.$VC_VEH_BRAND$  = [Vauxhall] shall indicate Vauxhall App icon.$VC_VEH_BRAND$  = [Citroen] shall indicate Citroen App icon.$VC_VEH_BRAND$  = [Peugeot] shall indicate Peugeot App icon.

(read the displayed App icon -> The HU displays the Chrysler App icon)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. The HU is displaying the App icon
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Set VC_VEH_BRAND = "Chrysler" (DR-PW28)
2. Read the App Drawer on the HU screen and check that the "Chrysler App icon" is shown on it
```

**Expected Result**

```
1. The brand configuration is applied
2. The "Chrysler App icon" is shown in the App Drawer
```

**Remarks**

```
(R-P386 第二批)
```

## `NR1L-PowerManagement-237`　`SWE-PM-082`　The Jeep brand selects the Jeep App icon

**Test Item**

```
The HU shall use the $VC_VEH_BRAND$  signal to determine the correct App icon to display.$VC_VEH_BRAND$  = [Chrysler] shall indicate Chrysler App icon. $VC_VEH_BRAND$  = [Dodge] shall indicate Dodge App icon. $VC_VEH_BRAND$  = [Jeep] shall indicate Jeep App icon.$VC_VEH_BRAND$  = [Alpha Romeo] shall indicate Alfa Romeo App icon.$VC_VEH_BRAND$  = [Fiat] shall indicate Fiat App icon. (DEFAULT)$VC_VEH_BRAND$  = [Lancia] shall indicate Lancia App icon. $VC_VEH_BRAND$  = [Maserati] shall indicate Maserati App icon.$VC_VEH_BRAND$  = [Ram] shall indicate Ram App icon.$VC_VEH_BRAND$  = [Abarth] shall indicate Abarth App icon.$VC_VEH_BRAND$  = [Opel] shall indicate Opel font App icon.$VC_VEH_BRAND$  = [Vauxhall] shall indicate Vauxhall App icon.$VC_VEH_BRAND$  = [Citroen] shall indicate Citroen App icon.$VC_VEH_BRAND$  = [Peugeot] shall indicate Peugeot App icon.

(read the displayed App icon -> The HU displays the Jeep App icon)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. The HU is displaying the App icon
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Set VC_VEH_BRAND = "Jeep" (DR-PW28)
2. Read the App Drawer on the HU screen and check that the "Jeep App icon" is shown on it
```

**Expected Result**

```
1. The brand configuration is applied
2. The "Jeep App icon" is shown in the App Drawer
```

**Remarks**

```
(R-P386 第二批)
```

## `NR1L-PowerManagement-238`　`SWE-PM-082`　The Fiat brand selects the default Fiat App icon

**Test Item**

```
The HU shall use the $VC_VEH_BRAND$  signal to determine the correct App icon to display.$VC_VEH_BRAND$  = [Chrysler] shall indicate Chrysler App icon. $VC_VEH_BRAND$  = [Dodge] shall indicate Dodge App icon. $VC_VEH_BRAND$  = [Jeep] shall indicate Jeep App icon.$VC_VEH_BRAND$  = [Alpha Romeo] shall indicate Alfa Romeo App icon.$VC_VEH_BRAND$  = [Fiat] shall indicate Fiat App icon. (DEFAULT)$VC_VEH_BRAND$  = [Lancia] shall indicate Lancia App icon. $VC_VEH_BRAND$  = [Maserati] shall indicate Maserati App icon.$VC_VEH_BRAND$  = [Ram] shall indicate Ram App icon.$VC_VEH_BRAND$  = [Abarth] shall indicate Abarth App icon.$VC_VEH_BRAND$  = [Opel] shall indicate Opel font App icon.$VC_VEH_BRAND$  = [Vauxhall] shall indicate Vauxhall App icon.$VC_VEH_BRAND$  = [Citroen] shall indicate Citroen App icon.$VC_VEH_BRAND$  = [Peugeot] shall indicate Peugeot App icon.

(read the displayed App icon -> The HU displays the Fiat App icon that the specification marks as DEFAULT)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. The HU is displaying the App icon
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Set VC_VEH_BRAND = "Fiat" (DR-PW28)
2. Read the App Drawer on the HU screen and check that the "Fiat App icon" is shown on it
```

**Expected Result**

```
1. The brand configuration is applied
2. The "Fiat App icon" is shown in the App Drawer
```

**Remarks**

```
(R-P386 第二批)
```

## `NR1L-PowerManagement-242`　`SWE-PM-084`　The recirc icon follows the PROXI parameters on the Atlantis architecture

**Test Item**

```
CUSW/AtlLo/AtlMi/AtlHi:The HU shall use $VC_VEH_LINE$ and the$Car_Shape_Configuration$ and $Number_of_Doors$ PROXI parameters to determine the correct recirc icon to display. See HMI release and PDO graphics files for ICON assignments.PNET: The HU shall use the $VC_VEH_LINE$ and the $VC_BODY_STYLE$ signals to determine the correct recirc icon to display. See HMI release and PDO graphics files for ICON assignments

(read the shown recirc icon -> The recirc icon matches the assignment for that vehicle line and car shape)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. The HU runs the CUSW or Atlantis architecture
3. The climate screen showing the recirc icon is reachable
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Apply the configuration: PROXI Car_Shape_Configuration and PROXI Number_of_Doors
2. Read the climate screen and check which "Recirc Icon" is shown on it
3. PENDING: DR-PW27 HMI release —— 該組態所對應之 recirc icon 指派
```

**Expected Result**

```
1. The configuration is applied
2. The "Recirc Icon" shown on the climate screen is read
3. PENDING: DR-PW27 HMI release
```

**Remarks**

```
(R-P386 第二批)
```

## `NR1L-PowerManagement-243`　`SWE-PM-084`　The recirc icon follows the body style signal on the PowerNet architecture

**Test Item**

```
CUSW/AtlLo/AtlMi/AtlHi:The HU shall use $VC_VEH_LINE$ and the$Car_Shape_Configuration$ and $Number_of_Doors$ PROXI parameters to determine the correct recirc icon to display. See HMI release and PDO graphics files for ICON assignments.PNET: The HU shall use the $VC_VEH_LINE$ and the $VC_BODY_STYLE$ signals to determine the correct recirc icon to display. See HMI release and PDO graphics files for ICON assignments

(read the shown recirc icon -> The recirc icon matches the assignment for that vehicle line and body style)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. The HU runs the PNET architecture
3. The climate screen showing the recirc icon is reachable
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Apply the configuration: Set VC_BODY_STYLE = the body style under test (DR-PW28)
2. Read the climate screen and check which "Recirc Icon" is shown on it
3. PENDING: DR-PW27 HMI release —— 該組態所對應之 recirc icon 指派
```

**Expected Result**

```
1. The configuration is applied
2. The "Recirc Icon" shown on the climate screen is read
3. PENDING: DR-PW27 HMI release
```

**Remarks**

```
(R-P386 第二批)
```

## `NR1L-PowerManagement-249`　`SWE-PM-087`　A non M240 vehicle line falls back to the brand seat graphic

**Test Item**

```
If $VC_VEH_LINE$ = [M240] The HU Shall use the M240 seat graphics.  If $VC_VEH_LINE$ <> [M240]The HU shall use $VC_VEH_BRAND$ to determine the correct settings seat graphic display.

(read the shown seat graphic against the brand signal -> The HU uses $VC_VEH_BRAND$ to determine the settings seat graphic)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. The seat settings screen is reachable
3. Set VC_VEH_BRAND = the configured brand (DR-PW28)
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Set VC_VEH_LINE = a value other than M240 (DR-PW28)
2. Read the seat settings screen and check which "Seat Graphic" is shown on it
3. PENDING: DR-PW27 —— 非 M240 之 seat graphic 指派
```

**Expected Result**

```
1. The vehicle line is applied
2. The shown "Seat Graphic" is read
3. PENDING: DR-PW27
```

**Remarks**

```
(R-P386 第二批)
```

## `NR1L-PowerManagement-250`　`SWE-PM-088`　The performance gauges follow the vehicle line signal

**Test Item**

```
The HU shall use the $VC_VEH_LINE$ signal to determine the correct performance gauges to display. See HMI release and PDO graphics files for performance gauge assignments.

(read the shown gauges -> The performance gauges match the assignment for that vehicle line)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. The performance gauges screen is reachable
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Set VC_VEH_LINE = the vehicle line under test (DR-PW28)
2. Read the performance gauges screen and check which "Performance Gauges" are shown on it
3. PENDING: DR-PW27 —— 該 vehicle line 之 gauges 指派
```

**Expected Result**

```
1. The vehicle line is applied
2. The shown "Performance Gauges" are read
3. PENDING: DR-PW27
```

**Remarks**

```
(R-P386 第二批)
```

## `NR1L-PowerManagement-262`　`SWE-PM-001`　Full-Operation holds in each listed ignition working condition

**Test Item**

```
All TLM, AMP/ICS/DTV functionalities are available.

(read TLM_Status.Info after each one -> The TLM stays ON with all TLM, AMP, ICS and DTV functionalities available in each of the listed conditions)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
The TLM is in Full-Operation
```

**Input Test Data**

```
Ignition working conditions: Ignition Pre_Start, Ignition Start, Ignition Cranking, Ignition On Engine On
```

**Test procedure**

```
1. Apply each ignition working condition listed in Input Test Data in turn by sending $STATUS_BH_BCM1.OperationalModeSts$ (DR-PW26)
2. After each one, read the signal $STATUS_TELEMATIC.PowerSts_Telematic$ and check that it is 4 (Full_Operation)
3. After each one, check that the audio active source keeps playing on the HU speakers
```

**Expected Result**

```
1. Each listed ignition working condition is registered without a bus error
2. The signal value $STATUS_TELEMATIC.PowerSts_Telematic$ = 4 (Full_Operation) is received after each one
3. The audio active source keeps playing on the HU speakers after each one
```

**Remarks**

```
(R-P383)
```

## `NR1L-PowerManagement-271`　`SWE-PM-004`　Timed keeps the TLM on with all functionalities available for a limited time

**Test Item**

```
TLM AMP/ICS/DTV functionalities are available only for a limited time.

(read the TLM state again after Timeout1 has elapsed -> The availability is limited in time and does not continue past Timeout1)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. $STATUS_BH_BCM1.OperationalModeSts$ = 2 (Ignition_Off) (DR-PW26)
3. Apply ENTER_TIMED
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Read the signal $STATUS_TELEMATIC.PowerSts_Telematic$ and check that it is 2 (Timed), and check that the audio active source keeps playing on the HU speakers
2. Hold for the $BCM_FD_27.Comfort_Enable_Time$ value with no phone call active
3. Read the signal $STATUS_TELEMATIC.PowerSts_Telematic$ and check that it is 1 (Standby)
```

**Expected Result**

```
1. The signal value $STATUS_TELEMATIC.PowerSts_Telematic$ = 2 (Timed) is received and the audio active source keeps playing on the HU speakers
2. The $BCM_FD_27.Comfort_Enable_Time$ value elapses with no phone call active
3. The signal value $STATUS_TELEMATIC.PowerSts_Telematic$ = 1 (Standby) is received
```

**Remarks**

```
(R-P383)
```

## `NR1L-PowerManagement-281`　`SWE-PM-007`　Bench powers the amplifier, booster, antennas and MCU on

**Test Item**

```
Bench
LTM plays the audio active source (Tuner, USB, AUX_IN or Phone Call, etc)
ON (Not muted)
ON DCSD follows behavior related to intensity and display status as defined in CFTS020 and VF668 DCSD sends touch coordinates
ON
ON Refer to {CFTS024} for further details about Antenna power supply
ON Refer to {VF654} for further details about Antenna power supply
ON (if present) Refer to {VF652} for further details about USB presence
ON (if present) Refer to {VF652} for further details about AUX_IN presence

(read the USB and AUX MCU states -> The USB and AUX MCU are ON when present)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
The ignition working condition is Ignition Off
The Engineering Line is activated
TLM_Status.Info reads "Bench"
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Apply ENTER_BENCH
2. Read the signal $STATUS_TELEMATIC.PowerSts_Telematic$ and check that it is 6 (Bench)
3. Read the HU speakers and check that the audio active source is playing on them
4. PENDING: DR-PW27 BoosterOUT / analog and digital antenna supply 之 ON 位準值 —— measure the voltage at each output and check it against that level
5. Insert a USB device and check that it is enumerated and can be played, and check that the AUX input plays on the HU speakers
```

**Expected Result**

```
1. The HU is in Bench
2. The signal value $STATUS_TELEMATIC.PowerSts_Telematic$ = 6 (Bench) is received
3. The audio active source is playing on the HU speakers
4. PENDING: DR-PW27 BoosterOUT / antenna supply ON 位準值
5. The USB device is enumerated and can be played, and the AUX input plays on the HU speakers
```

**Remarks**

```
(R-P386 第二批)
```

## `NR1L-PowerManagement-284`　`SWE-PM-075`　A dismissed FOTA pop up passes the TLM to Standby

**Test Item**

```
If the HU Transitions to Timed mode due to the condition described in CFTS009-1809, the HU shall transition to Standby mode after: -1 minute has passed without the user interacting with the pop-up -The FOTA pop up is dismissed -$ACCDlyAct$ transitions from active to inactive

(read the HU mode after the idle period -> The HU transitions to Standby mode after 1 minute has passed)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. The HU is in Timed mode due to the condition described in CFTS009-1809
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. PENDING: DR-PW27 CFTS057 —— FOTA update available 之建立方法
2. Apply ENTER_TIMED
3. Read the HU screen and check that the "FOTA update available" pop-up is shown
4. Dismiss the "FOTA update available" pop-up through the HMI, then read the HU screen and check that it is no longer shown on it
5. Read the signal $STATUS_TELEMATIC.PowerSts_Telematic$ and check that it is 1 (Standby)
```

**Expected Result**

```
1. PENDING: DR-PW27 CFTS057
2. The HU is in Timed
3. The "FOTA update available" pop-up is shown
4. The pop-up is no longer shown on the HU screen
5. The signal value $STATUS_TELEMATIC.PowerSts_Telematic$ = 1 (Standby) is received
```

**Remarks**

```
(R-P393(c) §8.3 拆分增列；本條與 NR1L-PowerManagement-169 同錨點)
```

## `NR1L-PowerManagement-285`　`SWE-PM-075`　An accessory delay going inactive passes the TLM to Standby

**Test Item**

```
If the HU Transitions to Timed mode due to the condition described in CFTS009-1809, the HU shall transition to Standby mode after: -1 minute has passed without the user interacting with the pop-up -The FOTA pop up is dismissed -$ACCDlyAct$ transitions from active to inactive

(read the HU mode after the idle period -> The HU transitions to Standby mode after 1 minute has passed)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. The HU is in Timed mode due to the condition described in CFTS009-1809
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. PENDING: DR-PW27 CFTS057 —— FOTA update available 之建立方法
2. Apply ENTER_TIMED with $BCM_FD_27.Comfort_Enable_Act$ = 1 (DR-PW26)
3. Read the HU screen and check that the "FOTA update available" pop-up is shown
4. Send the signal $BCM_FD_27.Comfort_Enable_Act$ = 0 (DR-PW26)
5. Read the signal $STATUS_TELEMATIC.PowerSts_Telematic$ and check that it is 1 (Standby)
```

**Expected Result**

```
1. PENDING: DR-PW27 CFTS057
2. The HU is in Timed
3. The "FOTA update available" pop-up is shown
4. The signal value $BCM_FD_27.Comfort_Enable_Act$ = 0 is received
5. The signal value $STATUS_TELEMATIC.PowerSts_Telematic$ = 1 (Standby) is received
```

**Remarks**

```
(R-P393(c) §8.3 拆分增列；本條與 NR1L-PowerManagement-169 同錨點)
```

## `NR1L-PowerManagement-286`　`SWE-PM-087`　An M240 vehicle line uses the M240 seat graphics

**Test Item**

```
If $VC_VEH_LINE$ = [M240] The HU Shall use the M240 seat graphics.  If $VC_VEH_LINE$ <> [M240]The HU shall use $VC_VEH_BRAND$ to determine the correct settings seat graphic display.

(read the shown seat graphic against the brand signal -> The HU uses $VC_VEH_BRAND$ to determine the settings seat graphic)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. The seat settings screen is reachable
3. Set VC_VEH_BRAND = the configured brand (DR-PW28)
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Set VC_VEH_LINE = "M240" (DR-PW28)
2. Read the seat settings screen and check that the "M240 seat graphics" are shown on it
```

**Expected Result**

```
1. The vehicle line is applied
2. The "M240 seat graphics" are shown on the seat settings screen
```

**Remarks**

```
(R-P393(c) §8.3 拆分增列；本條與 NR1L-PowerManagement-249 同錨點)
```

## `NR1L-PowerManagement-287`　`SWE-PM-093`　A start-up animation plays again in the next wakeup cycle

**Test Item**

```
While HU is in SLEEP MODE, STANDBY MODE, or in PARTIAL OPERATION MODE and when driver door ajar status ($Door_Ajar_Status$) is changed to CLOSED, HU shall play a start-up animation as defined per HMI. The startup animation will be unique for each vehicle or/and brand as defined per PDO/HMI. If driver door is not present or removed for current vehicle ($DriverDoorOnOffSts$ = [DOOR_OFF]), or if HU changes mode (due to ignition event or to due to HU power mode status change to BODY ON or to TIMED MODE while driver door ajar status ($Door_Ajar_Status$) is OPEN, the HU shall skip start-up animation. While HU is playing a start-up animation and HU changes mode (due to ignition event or to HU power mode status changes to BODY ON or to TIMED MODE) or if an ignition crank event ($PowerMode$ = [IGN_START ]), then HU shall cancel current start-up animation and switch required power mode as defined. Once a start-up animation is played, HU shall not play the next start-up animation until the next CAN wakeup cycle OR at least 30 minutes passed from last time the start-up animation was played ;whichever is greater; given all other conditions are met for startup animation to play as defined here.

(read the screen against the elapsed time -> A start-up animation plays again only at the next CAN wakeup cycle or after 30 minutes, whichever is greater)
```

**Pre-Conditions**

```
1. A LIN and CAN simulation tool is connected
2. The HU has just played a start-up animation
3. All other conditions for the animation hold
```

**Input Test Data**

```
NA
```

**Test procedure**

```
1. Set Door_Ajar_Status = "Open" then "Closed" again within the same CAN wakeup cycle (DR-PW28)
2. Read the HU screen and check that the "Start-up Animation" is not played on it
3. PENDING: DR-PW26 Sleep 態之觀察方法 —— let the Body CAN go to sleep and wake it again to start a new wakeup cycle
4. Repeat the door event and read the HU screen, and check that the "Start-up Animation" is played on it
```

**Expected Result**

```
1. The door event is registered within the same CAN wakeup cycle
2. The "Start-up Animation" is not played
3. PENDING: DR-PW26 Sleep 態之觀察方法
4. The "Start-up Animation" is played on the HU screen in the new wakeup cycle
```

**Remarks**

```
(R-P393(c) §8.3 拆分增列；本條與 NR1L-PowerManagement-182 同錨點)
```
