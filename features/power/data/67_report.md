# 67 包 —— 執行層回報

§H 六步全部完成（第 3 步之刺激候選供料後依指示停在該項；其餘照做）。

## 1. G255 —— 本包數字（R-P379(c)）

67 包條文未預寫數字（§J 明示「第一批 tc_id 數由執行層依 (a) 清單計」）。
執行層所計與其來源：

| 數 | 來源 | 值 |
|---|---|---|
| 第一批 tc_id | R-P391(a) 清單逐一展開 | **37**（corpus 內全數存在，37/37）|
| `VC_*` 相異名 | CFTS009 文字層 `\$VC_[A-Za-z0-9_]+\$` 去重 | **9** |
| `$VC_VEH_BRAND$` 出現 | 同上計次 | **277** |
| 刺激候選（chime） | forms 二本 DBC 依規格用語掃描 | **38**（全數有 `CM_`）|
| 家族 K 殘留 | `family_k_59.py` | 158 → **132** |

## 2. R-P389(a) —— 四名之查詢結果（§H 第 2 步）

**六處逐字查詢，全部 0 命中**：BHCAN2 `SG_`、FDCAN8 `SG_`、PROXI `Format`、
LID `Logical Identifier`、SR26 全 3 分頁、SR24 全 8 分頁。

⚠ **且非四名之個別缺漏，是整個 `VC_*` 命名空間不存在於任一參考檔**（A-PW367）：

| 檔 | `VC_` 前綴之名 |
|---|---|
| PROXI `Format` | **0** |
| LID `Logical Identifier` | **0** |
| BHCAN2 `SG_` | **0** |
| FDCAN8 `SG_` | **0** |

而 CFTS009 內 `$VC_*$` 有 **9 個相異名**，`$VC_VEH_BRAND$` 出現 **277 次**。

依 R-G13 第 3 項（涵蓋範圍是否本應包含之）**無法認定任一檔本應包含之**，
故記「**未查得**」而非「查無」，**不登 `LOOKUP_MISSES` M-n**。
處置依 R-P389(c)：R-13 保留規格原名，**開 DR-PW28**。

附帶記：規格自身有大小寫變體（`$VC_Veh_Brand$` 2 次、`$VC_SpecialPkg_IC$` 1 次），
**R-7 之單一拼法在此無所適從**，併入 DR-PW28 詢問。

## 3. R-P390 —— 刺激候選供料（§H 第 3 步，該項停）

落檔 `data/stimulus_candidates_67.md`。**執行層不擇定。**

| 主題 | 候選 | 無 `CM_` 者 |
|---|---|---|
| chime（`-055`）| **38** | **0** |
| ICS 面板觸控回應（`-202`）| 見該檔 | 見該檔 |

逐候選列 `MESSAGE.Signal`、`CM_` 註解全文、發送／接收節點、`VAL_`。
**全部候選皆有 `CM_`**，故 R-P390(b) 之「`CM_` 無明述者不選」有據可判，待分析層擇定。

## 4. R-P391(a) —— 第一批 37 條改寫（§H 第 4 步）

`apply_batch1_67.py` → **37 / 37 命中**，落於五個 batch 檔。
JSON round-trip 逐位元組一致。

**R-P389(c) 解開了品牌視覺類** —— `$VC_*$` 以 `Set VC_VEH_BRAND = <值> (DR-PW28)`
書寫（不加 `$`、不加 `PROXI`），故 `-148`／`-149~152`／`-192~195`／`-155`／`-185`
本批即可做，不必等 DR 回覆。

### 閘門（該批範圍）

| 閘 | 期望 | 實測 |
|---|---|---|
| G245（家族 A，判準 = `remeasure_55.py` 上界，R-P362）| 0 | **0** ✓ |
| G250（`proper` / `as defined` / `normal`）| 0 | **0** ✓ |
| G250（`Read the HU mode/state`）| 0 | **0** ✓ |
| G251（`listed in Input Test Data`）| 0 | **2** —— `-005` / `-218`，**皆 (c) 類應保留者** ✓ |

### ⚠ 二處補正（`fix_batch1_67.py`），據實記

**(a) G245 首輪殘留 14 條，成因為具名 UI 元件未加引號。**
R-P384(b) 之「具名不以引號為要件」指**抽取**判準；**IN §11 之 TC 書寫規則仍要求加引號**。
二者不衝突（一為抽取、一為書寫），**本層首輪誤把抽取判準套到書寫上**。
補正後為 `"Brand Logo Screen"`、`"Fiat Latam Logo"`、`"Vehicle Brand Logo"`、
`"Standard Screen"`、`"Call Screen"`、`"FOTA update available"` 等加引號，G245 歸零。
另二處措辭改寫入白名單：`-011`/`-012` 之手機端由「音訊在手機」改為
`"Call Screen"` 顯示通話已接通 (ii)；`-118` 之比較步併入前一步之 check。

**(b) `-155` / `-185` 成為五欄逐字相同（11 → 12 對）—— 不是回歸，是 R-P357(b) 之 (b) 型。**
二條之 `test_item` **本就逐字相同**（同一需求文字掛 `SWE-PM-056` 與 `SWE-PM-097` 兩個 SWRA ID），
先前只因舊 Procedure 措辭不同而未撞上五欄鍵。**Req ID 不同 → 依 R-P357(b) 二列皆保留、不得合併。**

⚠ **順帶發現：R-P357(b) 之「Remarks 互註對方 tc_id」全案從未施作**（B5 凍結期間無人做）。
本包一併補全 **12 對 / 24 處**互註。

### 家族 K 判準之補（`family_k_59.py`）

`-218` 依 66 包 §1 表應為 (c) 類（`Repeat … 31 times`），
惟 `DATASET_RE` 原式只認 `in turn` / `each of` / `one at a time` 等，**看不到 `Repeat … N times`**。
已補入該式 —— 其為「一步多輪、輪間並列」，屬 R-P373(a) 之第 3 類。
補後分類：**(a) 118 / (b) 10 / (c) 4**。

## 5. 新開 DR 三張

| DR | 級別 | 內容 |
|---|---|---|
| **DR-PW28** | High | `VC_*` 命名空間（9 名）與 `$TBM_Present$` 之實體載體、值域、大小寫變體 |
| **DR-PW29** | Low | `CFTS009-4941453` 之 `Full-Operation` / `Timed` 各二列，`Source` 欄不同，是否對應不同 HU 變體（R-P391(c)）|
| **DR-PW30** | Medium | `SplashScreen_Time` / `StandardScreen_Time` **之值全案未載**（僅 `CFTS010-4942337` 一處提及且無值）；影響 `-004` |

DR-PW27 已於 66 包擴為七項總表。

## 6. 未做者，據實列

- **`-169` 之 §8.3 拆分**（三個離開條件各一 TC）—— 66 包 §1 表所令，本包只改寫現行條，**未拆**。
- **`-249` 補 M240 支、`-182` 拆二、`-222`/`-223` 分支** —— 同屬拆分，**未做**；
  其中 `-249`／`-222`／`-223` 屬第二批，`-182` 繫 `ENTER_SLEEP`（PENDING DR-PW26）。
- **第二批**（R-P391(b)）—— 依禁區不得先做。

## 7. 現況（`pending_recount_67.tsv`）

| 項 | 值 |
|---|---|
| corpus | 283 條 |
| 第一批已改寫 | **37** |
| corpus 內 `PENDING` 文字 | **20**（66 包 2 → 本包 20）|
| 家族 K 殘留 | **132**（158 → 132）|
| 五欄逐字相同對 | **12**（含新撞上之 `-155`/`-185`）|
| R-P357(b) 互註 | **24 處 / 12 對**（本包補全）|
| 丁案已改寫 | 3 |
| G0 | 素材 9/9 ＋ 參考庫 7/7 |

⚠ **PENDING 由 2 升至 20** —— 第一批中 `-017`~`-021`／`-143`／`-156`（DR-PW25）、
`-100`~`-104`／`-119`／`-120`（DR-PW23）、`-121`／`-169`（DR-PW27）、`-004`（DR-PW30）、
`-049`（DR-PW25）、`-123`（DR-PW27）等依 §1 表所令佔位。**S6 甲案下該 20 條不出貨。**

## 8. 待裁

1. **刺激候選之擇定**（§3）：chime 38 個候選皆有 `CM_`，待分析層擇。
2. **§8.3 拆分是否本批補做**（§6）：`-169` 之三個離開條件。
3. **R-P357(b) 互註補全之追認**（§4(b)）：本層逕行補了 12 對 24 處，屬既有條文之未施作項，請確認。
