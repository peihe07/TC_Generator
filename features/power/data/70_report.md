# 70 包 —— 執行層回報

§H 三步完成，依第 3 步「停」停在 token 供料之後。

## 1. G255 —— 本包四數與重算（R-P379(c)）

**4 / 4 相符**：104 / 72 / 83 / 119（以現行 corpus 重跑）。

## 2. 抄錄

`RULINGS.md`：**R-P395** 逐字抄入（1 / 1）。R-P36 加註二處：
**R-P394(b) 之丁案項結案**（0 條，依 R-P376(c) 嚴格判準）；
**R-P381 / R-P384** 之人讀**單位**由「名」改為「原子 token」（非取消人讀）。

## 3. B5 剩餘 72 條（§H 第 2 步）—— **B5 全數完成，212 / 212**

八條規則施行：R1 17、R3 32、R4 4、R5 3、**R6 18**、R7 2。

⚠ R6（未查得 `$X$` → 去 `$` ＋ `(DR-PW28)`）本批 **18 條**，
遠高於前二批之 5 —— 剩餘 72 條集中於品牌／組態類，`$VC_*$` 密度高，與 A-PW367 一致。

### 全案六閘（287 條）

| 閘 | 期望 | 實測 | 判 |
|---|---|---|---|
| G250 `proper` / `as defined` / `normal` | 0 | **0** | ✓ |
| G251 `listed in Input Test Data` | 0 | **3** —— 皆 (c) 類應保留（`-005`/`-218`/`-262`）| ✓ |
| G249 五欄逐字相同對 | — | **12**，全 (b) 型，互註齊 | ✓ |
| G250 `Read the HU mode/state` | 0 | **13** | ✗ |
| G245 家族 A（上界）| 0 | **168** | ✗ |
| G247 Proc/ER 非 PENDING 之內部訊號句 | 0 | **53** | ✗ |

三閘未歸零，成因同 69 包 §5：**複合觀察目標需逐 token 之代理量**。
本包依 R-P395(a) 供料，**不自行指定**。

### 本包重蹈之處，據實記

R7 之標記於本批**再度**嵌入被禁措辭（`PENDING: DR-PW27 \`normal\` …`），
本層於 69 包已自陳過同一錯並修正腳本之輸出，惟**修正只作用於當時已存在之條**，
新跑之 72 條又產生 2 處。已清理。
⚠ **教訓**：修了資料未修產生器 —— 與 A-PW366（顯示截斷作資料鍵）同族之操作面錯誤。

## 4. 原子 token 供料（§H 第 3 步）—— **R-P395 之前提成立**

`data/composite_tokens_70.md`。

| 項 | 值 |
|---|---|
| 母體（三閘未歸零之條）| **170** |
| **相異原子 token** | **59** |
| token 總出現 | 271 |
| 代表錨點段落（逐字附全文）| 29 |

**170 條 → 59 個 token，收斂近 3 倍。** 分析層 §0 之判斷（「原子種類有限」）**成立**。

出現次數前十：`screen` 50、`TLM_Status.Info` 40、`$Telematic_Power$` 28、
`TLM state` 21、`antitheft request` 19、`HU mode` 13、`VPLastStatus` 9、
`applied theme` 7、`audio path` 6、`AMP` 5。

供料**不判定、不預填代理量**；**不合併同義**（`HU mode` 與 `TLM state` 分列二 token）。
末欄「既裁之解」**非本層指定**，僅引既有條文供對照 ——
6 個 token 已有既裁（`TLM_Status.Info` / `$Telematic_Power$` → R-P368、
`$Radio_Theme$` → R-P368、`$PowerMode$` → R-P385(b)、
`LTM_OperationalModeSts.Info` → R-P368、`shown Splash Screen` → R-P387(b)），
**其餘 53 個待 71 包裁**。

⚠ 若該 6 個既裁 token 於 71 包確認可直接套用，
其涉及 `TLM_Status.Info`(40) ＋ `$Telematic_Power$`(28) ＋ `TLM state`(21，同義待判) 等
高頻項，**可望一次消去 G245 之大部分**。本層不預估比例。

## 5. 現況（`pending_recount_70.tsv`）

| 項 | 值 |
|---|---|
| corpus | 287 |
| **已改寫累計** | **287 / 287（100%）** |
| 　其中 B5（R-P394）| 212 |
| corpus 內 `PENDING` | **125** |
| G245 / G250-G / G247 | 168 / 13 / 53 |
| G250-D / G251 / G249 | 0 / 3（皆應保留）/ 12（皆 (b) 型）|

**全 corpus 已無未經配方處理之條。** 剩餘工作為三閘之 token 字典套用。

## 6. 待裁

1. **59 個 token 之代理量字典**（71 包，R-P395(b)）—— 53 個待裁，6 個已有既裁待確認。
2. **同義判定**：`HU mode` / `TLM state` / `TLM_Status.Info` / `$Telematic_Power$`
   四者是否同物 —— 依 §I 本層未合併，四者分列。
3. **站④ 抽查** 75 條仍在 Pei 手上（`reviewed_75_69.md`）；
   B5 之 212 條若需目視包，本層可同法產出。
