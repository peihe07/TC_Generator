# 72 包 —— 執行層回報

§H 第 1、2、4 步完成；第 3 步（Excel GUI 開啟驗證）為 Pei 之手動項。

## 1. G255 / G256 表（R-P379(c) / R-P397(b)）

### G255 —— 本包數字與重算，**4 / 4 相符**

287 / 146 / 10 / 3（來源 `71_report.md` §4–§6，以現行 corpus 重跑）。

### G256 —— IN §11 引號 lint（**新閘**）

| | |
|---|---|
| 判準 | **以全案已加引號之具名元件為字典**（20 個），凡該名以**未加引號**之形式出現者即違規 |
| 範圍 | Procedure／ER；**`test_item` 上半 verbatim 不掃、不改**（R-6）|
| 首跑 | **49 句 / 49 條** |
| 補正後 | **0** ✓ |

首跑分布：`splash screen` 29、`brand logo screen` 9、`start-up animation` 7、
`seat graphic` 2、`M240 seat graphics` 1、`performance gauges` 1。

**判準之設計**：只認**同一名在同一 corpus 內時而加引號、時而未加**之不一致，
**不判斷「某泛稱是否應為具名」**（那是分析層之事）。
初版以「名詞清單」為判準時得 168 句 / 112 條，其中多數為
`the HU screen`（載體泛稱）、`the VR button`、`"Incoming Call" pop-up`（名已加引號、
其後為類別名詞）等**非違規**者 —— 已改為現行判準。

## 2. A-PW370 —— 引號規則連續四次靠人記而漏

執行層三次（67 包批一 15 條、68 包批二 15 條、71 包字典 6 條）＋
分析層一次（71 包 §1 表之 `played animation` 條目，**未加引號且含所禁之 `normal`**）。

**根因**：R-P384(b)「具名不以引號為要件」為**抽取**判準，IN §11 為**書寫**規則，
二者不衝突而**易被當成同一件事**；每次都是事後由 G245 掃出再補。

**教訓與 A-PW366 同族：規則若只寫在條文而不寫進閘，重犯是必然而非偶然。**

## 3. §H 第 2 步 —— sandbox 工作簿與目視包

### `sandbox/b72/pm_72.xlsx`

| | |
|---|---|
| 存在 | **是** |
| bytes | **128,143** |
| sha256 | `d333c520aa000221a2e87966553240a80b3bef54330e39c1b4c79d2182c69a94` |
| 資料列 | **288** = TC **287** ＋ `SWE-PM-089` 留白 1（R-P141）|
| 分頁 | 10 個，與來源同 |

寫入路徑 **`surgical_save()`**（同 27 包），**全域無 `Workbook.save()`**；
來源為 `inputs/` 之原始工作簿，**位元組複製後才寫**（`sha256` 相等已 assert）。
**未複製至 `delivered/`**（R-P397 / §I）。

⚠ **一處判斷請確認**：R-P397(a) 令「SHA 記 MANIFEST」，惟同條又令不複製至 `delivered/`。
本層**未動 `delivered/MANIFEST.tsv`**（該檔為交付台帳，登錄未交付之檔恐生誤解），
改出 **`sandbox/b72/MANIFEST.tsv`** 記其 SHA 與「未交付」之註。請確認取徑。

### `data/reviewed_287_72.md`

| | |
|---|---|
| 存在 | **是**，bytes **390,614** |
| 條數 | **287**（取代 `reviewed_75_69.md`）|
| 內容 | tc_id／req_id／五欄／Remarks，**逐字，不判定、不摘要** |

## 4. 全案閘門現況（`pending_recount_72.tsv`）

| 閘 | 期望 | 實測 | 判 |
|---|---|---|---|
| G245 家族 A（上界）| 0 | **0** | ✓ |
| G250 `proper` / `as defined` / `normal` | 0 | **0** | ✓ |
| G250 `Read the HU mode/state` | 0 | **0** | ✓ |
| G247 內部訊號（排除 `TLM_Status.Info`）| 0 | **0** | ✓ |
| **G256 引號 lint（新）** | 0 | **0** | ✓ |
| G251 `listed in Input Test Data` | 0 | **3** —— 皆 (c) 類應保留 | ✓ |
| G249 五欄逐字相同對 | — | **10**，全 (b) 型、互註齊 | ✓ |
| G0 | 全過 | 素材 9/9 ＋ 參考庫 7/7 | ✓ |

`PENDING` **146**（未變 —— 本包無內容性改寫，僅引號補正）。

## 5. §B DR 發送清單 —— 執行層之補充

分析層列 7 張。本層補三項機讀事實供 Pei 核對：

| DR | 影響（`generated/` 逐條掃）|
|---|---|
| **DR-PW28** | `VC_*` 於 corpus 之 Procedure／Pre-Condition 引用 —— **影響面最廣**；CFTS009 內 `$VC_VEH_BRAND$` 一名即 277 次 |
| **DR-PW23** | 內部訊號 12 名之佔位，為 `PENDING` 146 條之最大宗 |
| **DR-PW27** | 七項文件；其中 CFTS024／VF654 之位準值影響**全部 11 個 `FUNC_STATE_<STATE>` 片段**之 (v) 類子項 |

⚠ **146 / 287（51%）之 PENDING 全繫於這 7 張 DR。**
S6 甲案下該 146 條不出貨 —— **無 DR 回覆即無出貨**，此為現況之唯一瓶頸。

## 6. 待 Pei（三項，皆 Tier 3）

1. **站④ 抽查**：`sandbox/b72/pm_72.xlsx`（Excel 開）＋ `data/reviewed_287_72.md`。
   §H 第 3 步之 GUI 驗證為手動項，本層未代行。
2. **DR 是否已送**：§B 七張逐張核對；未送者請發。
3. **S6**：甲（等）／乙（分段出貨 141 條、146 條附清單）／丙（逐名審）。不開口即甲。

另請確認 §3 之 MANIFEST 取徑。
