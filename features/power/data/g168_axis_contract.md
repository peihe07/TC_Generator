# G168 —— `distinguishing_axis` 契約檢查（R-P247）

> **⚠ §4.6 之契約原文於本庫查無**，`duplicate_of` 欄亦不存在於任何 TC；
> 故 `axis` 之**列舉值清單無從取得，執行層不自行擬定**。
> 本閘只檢可查證之結構（C1–C5）。

## 一、結構違規（C1–C4）—— **254** 條

| tc | 違規 |
|---|---|
| `NR1L-PowerManagement-011` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-012` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-013` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-014` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-015` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-016` | C6 `axis="branch"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-017` | C6 `axis="branch"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-018` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-019` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-020` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-021` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-022` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-023` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-024` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-025` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-026` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-027` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-028` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-029` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-030` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-031` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-032` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-033` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-034` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-035` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-036` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-037` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-038` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-039` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-040` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-041` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-042` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-043` | C6 `axis="trigger"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-044` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-045` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-046` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-047` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-048` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-049` | C6 `axis="branch"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-050` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-051` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-052` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-053` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-054` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-055` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-056` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-057` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-058` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-059` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-060` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-061` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-062` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-063` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-064` | C6 `axis="branch"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-065` | C6 `axis="branch"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-066` | C6 `axis="trigger"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-067` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-068` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-069` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-070` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-071` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-072` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-073` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-074` | C6 `axis="trigger"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-075` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-076` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-077` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-078` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-079` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-080` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-081` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-082` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-083` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-084` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-085` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-086` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-087` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-088` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-089` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-090` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-091` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-092` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-093` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-094` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-095` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-096` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-097` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-098` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-099` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-100` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-101` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-102` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-103` | C6 `axis="branch"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-104` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-105` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-106` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-107` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-108` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-109` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-110` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-111` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-112` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-113` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-114` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-115` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-116` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-117` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-118` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-119` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-120` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-121` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-122` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-123` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-124` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-125` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-126` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-127` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-128` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-129` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-130` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-131` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-132` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-133` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-134` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-135` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-136` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-137` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-138` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-139` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-140` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-141` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-142` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-143` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-144` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-145` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-146` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-147` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-148` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-149` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-150` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-151` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-152` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-153` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-154` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-155` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-156` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-157` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-158` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-159` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-160` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-161` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-162` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-163` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-164` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-165` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-166` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-167` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-168` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-169` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-170` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-171` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-172` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-173` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-174` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-175` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-176` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-177` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-178` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-179` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-180` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-181` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-182` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-183` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-184` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-185` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-186` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-187` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-188` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-189` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-190` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-191` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-192` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-193` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-194` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-195` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-196` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-197` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-198` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-199` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-200` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-201` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-202` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-203` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-204` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-205` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-206` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-207` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-208` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-209` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-210` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-211` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-212` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-213` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-214` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-215` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-216` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-217` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-218` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-219` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-220` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-221` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-222` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-223` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-224` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-225` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-226` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-227` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-228` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-229` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-230` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-231` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-232` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-233` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-234` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-235` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-236` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-237` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-238` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-239` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-240` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-241` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-242` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-243` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-244` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-245` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-246` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-247` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-248` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-249` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-250` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-251` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-252` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-253` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-254` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-255` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-256` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-257` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-258` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-259` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-260` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-261` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-262` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-263` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |
| `NR1L-PowerManagement-264` | C6 `axis="behaviour"` 不在 §4.6 之六值列舉內 |

**C4 之現況**：`axis="none"` 者 **0** 條 ——該項現**不觸發**，故其正確性**未經本批語料檢驗**（fixture 已另行證明其會 FAIL）。

## 二、`axis` 之值分布

| axis | 條數 |
|---|---|
| `behaviour` | 245 |
| `trigger_state` | 6 |
| `branch` | 6 |
| `timing` | 3 |
| `trigger` | 3 |
| `input_data` | 1 |

## 三、C5 同 leaf 內 `delta` 逐字相同 —— **1** 組

> **觸發不等於違規** —— 逐組之判定屬人工。

| leaf | TC | `delta` |
|---|---|---|
| `SWE-PM-071` | `…-002`、`…-003` | 本條驗抑制分支：轉往 Standby 或 Bench 時不得顯示 splash，與 -01 為互斥條件 |
