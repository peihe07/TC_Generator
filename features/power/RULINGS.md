# RULINGS — Power (FW036)

Pei 之裁決與分析層自裁條文之逐字登記。條文一律照錄（R19-2：原文貼入，
不改寫、不摘要），執行層之回報另起段落。本檔為 Power 之裁決權威；
跨 feature 條文承接時註明來源包。

---

## 第一輪 — 下放包 01（`docs/handoff/01_intake.md` §A）

> 01 包之步驟 5（抄入 RULINGS.md）因該包停於步驟 2 而未執行。
> 於 02 包一併補抄，條文逐字照錄，狀態欄反映 02 包之異動。

```
[R-P1] SWE-PM-089 之來源對應先留空。
       不臆測、不套用鄰近 leaf 之來源、不以 SWE1-PM-ANT-008 反查填補。
       該 leaf 仍計入 115 母體，列為 RD-1 待決。
       裁決者 Pei，逐字依據：「那題記得先留空 其他可以繼續」。
```
**狀態：live。** 02 包實測 G3 = 114/115，唯一失敗者即 `SWE-PM-089`
（其 `Source Requirement ID` = `SWE1-PM-ANT-008`，非 SYS2 命名空間）。未填補。

```
[R-P2] 命名。feature 目錄 = features/power；
       workbook Test Group = "Power Management"；
       tc_id 方案 = NR1L-PowerManagement-{NNN}。
       裁決者 Pei，逐字依據：「是繼續」（回應 #5 提案）。
```
**狀態：live。**

```
[R-P3] spec_mode = B（純文字層 + 章節 regex）。
       三份 .docx 經 magic bytes 實測皆非 OOXML 亦非 OLE，
       為 Markdown 純文字轉檔。禁止以 python-docx / olefile /
       zipfile 讀取；一律以 UTF-8 文字讀入。
```
**狀態：撤回（R-P9）。** 事實前提為假 —— 原始檔實測兩份 OOXML、一份 OLE2。
改立 R-P3′，見 R-P9。

```
[R-P4] Power Management 之規格來源為兩份 CFTS：
       CFTS009 Wake-up and Power-up、CFTS010 Power Down。
       任何宣稱「規格來源」之陳述必須同時涵蓋兩份，
       單引一份即為不完整。
```
**狀態：live。** 02 包實測 G4 = 111 / 3 / 1，兩域互斥，證實雙來源。

```
[R-P5] Layer 2 之 `Power State` 與 `Power State Reporting` 合併，
       為單一 Test Set `Power State`，64 leaf。
       Layer 3 隨之合併，含 CFTS009 §1.6.2.1.15
       （`TLM_Status.Info` / `$Telematic_Power$` 訊號上報）。
       裁決者 Pei，逐字依據：「E-1 合併」。
```
**狀態：live，惟 leaf 數待重裁。** 02 包重算 Power State = **62**，非 64。
見 `docs/upstream/02_rebaseline.md` §五。依 §E 末，不逕行改寫。

```
[R-P6] Layer 2 之 `Power Down`（3 leaf）**保留獨立 Test Set**，
       不因低於 §4.1.3 健康門檻而合併。
       裁決者 Pei，逐字依據：「E-2 保留獨立 Test Set」。
```
**狀態：live。** 02 包重算 Power Down = 3，相符。

```
[R-P7] 範圍界定：037 之 115 leaf 為本 feature 唯一驗證母體。
       SYS2 反向缺口（未被 037 引用之 SW/System 需求）
       與 SYS2 匯出之收錄規則（`Sys-RA-PM-0197`–`0206` 斷點、
       CFTS009 本文未被引用之 547 條）**不追、不問、不列 RD-1**。
       DR-PW2 撤回。
       裁決者 Pei，逐字依據：「#1/#3 不需」。
```
**狀態：live。**

> **註記（R-P36 首次適用，05 包加註）**
> 註記（04 包實測，A-PW19）：本條內嵌之「547 條」為失效值。
> 以原始檔重測，CFTS009 本文未被引用之需求錨點實為 669 條。
> 量測條件見 04 上繳包 §六。原文保留，依 R-P36 不改。

```
[R-P8] Priority 之判定來源：依 **TC 實際所寫之測項內容**
       套 §10.2 之 rubric 判定 P0–P3。
       037 `Priority` 欄之 `High` / `Medium`（91 / 24）
       **不具映射權威**，不得以之推導 priority。
       DR-PW4 撤回。
       裁決者 Pei，逐字依據：「#4 依照所寫測項去判定」。
```
**狀態：live。**

---

## 第二輪 — 下放包 02（`docs/handoff/02_rebaseline.md` §A）

```
[R-P9]  R-P3 撤回。其事實前提為假 —— 分析層所測之「三份純文字」
        係 Project 附件之轉換產物，非磁碟上之原始文件。
        改立 R-P3′：spec_mode = D（二進位文件抽取）。
        讀取方式依實測 magic bytes 決定：
          50 4B 03 04（OOXML .docx）→ zipfile 或 python-docx
          D0 CF 11 E0（OLE2 .doc）  → macOS 內建 textutil 轉換
        R-P3 對 python-docx / olefile / zipfile 之禁令一併解除。
        裁決者 Pei，逐字依據：「照你的建議」（回應 Q1）。
```
**執行層回報：已落實。** CFTS009（`50 4B 03 04`）以 `zipfile` +
`word/document.xml` 抽出；CFTS010（`D0 CF 11 E0`）以 `textutil -convert html`
抽出。SYS3 依 01 包 §B 不讀。

```
[R-P10] 三份純文字衍生物明示為不可得：無版本、無產生紀錄、
        無法重現，一律不得再作為任何量測之來源。
        CFTS 本文相關之一切數字，均須自原始檔重新產生。
        裁決者 Pei，逐字依據：「照你的建議」（回應 Q2）。
```
**執行層回報：已落實。** 02 包所有 CFTS 相關數字（G3、G5b、G8、G9、§E 重算）
皆自原始檔重新產生，無一沿用 01 包之值。

```
[R-P11] G8 / G9 四數（CFTS009 需求錨點 904 / 章節錨點 172；
        CFTS010 需求錨點 148 / 章節錨點 92）作廢。
        自原始檔重測後改寫 §D 期望值，不視為停止條件。
        理由：原值非 baseline，係自失效來源量得，
        改寫不構成「自行調參數遷就」。
        此例外僅適用於本次 rebaseline，不得類推。
        裁決者 Pei，逐字依據：「照你的建議」（回應 Q3）。
```
**執行層回報：已重測。** G8 = 需求錨點 904 / 章節錨點 **196**；
G9 = 需求錨點 148 / 章節錨點 92。四數中三數與舊值相同；
CFTS009 章節錨點由 172 改為 196，差額 24 項經獨立佐證為真標題
（pStyle 7 ×15、pStyle 8 ×9），舊值 172 係衍生物粗體處理造成之漏算。

```
[R-P12] A-PW07 撤回（「三份 .docx 實為 Markdown 純文字」為假；
        副檔名與內容實為相符）。
        改登 A-PW08：01 下放包 §B 之「真實格式」欄與 bytes 欄
        與原始檔不符，源於以衍生物冒充原始檔。
        另記：CFTS010 之原始檔副檔名為 .doc，非 .docx；
        01 包中「三份 .docx」之表述在檔名層即為錯誤。
        裁決者 Pei，逐字依據：「照你的建議」（回應 Q4）。
```
**執行層回報：已落實於 ANOMALIES.md。**

```
[R-P13] 台帳通則（跨 feature 適用，canon 候選）：
        凡經任何轉換之素材，台帳須同時登記
        （a）原始檔之完整路徑、bytes、SHA256 全 64 碼
        （b）衍生物之 bytes、SHA256 全 64 碼
        （c）轉換工具與完整轉換指令
        三者缺一，該素材不得作為量測來源。
        裁決者 Pei，逐字依據：「照你的建議」（回應 Q5）。
```
**執行層回報：已落實。** 見 `docs/upstream/02_rebaseline.md` §二（原始檔七份）
與 §三（衍生物三份，含轉換指令）。

```
[R-P14] 閃點表結構修正：
        （a）新增 G0「素材身分驗證」為前置閘 ——
             全部素材之原始檔 SHA256 相符方得進入 G1 以後。
             G0 不通過時，G1 以後一律不執行、不回報。
        （b）G11 移除。其期望值與 §E 同源，
             以 G11 驗 §E 為循環論證，不構成獨立驗證。
        （c）§E 之「已定版」與「本表實際只由單一來源支撐、
             不是交集」並存之張力，維持登記，不由實測覆蓋。
        裁決者 Pei，逐字依據：「照你的建議」（回應 Q6）。
```
**執行層回報：已落實。** G0 = 7/7 通過；G11 未執行、未回報；
§E 之張力維持登記，重算結果僅上繳不改寫（§E 實測 62/24/16/8/3 + 未歸類 1）。

---

## 第三輪 — 下放包 03（`docs/handoff/03_framework_inputs.md` §A）

```
[R-P15] 主章節判定規則：
        （a）解析至單一章節之 leaf —— 自動歸屬該章節。
        （b）解析至多個相異章節之 leaf —— **不以任何演算法決定**。
             逐條列出其章節集合，逐條由 Pei 裁定，記入 RULINGS.md。
        禁止以出現次數、章節深度、token 順序、章節號大小
        或任何 tie-break 自動指派。
        理由：此為判斷而非計算；02 包之不符即源於將判斷
        隱藏於未書面化之 tie-break。
        裁決者 Pei，逐字依據：「都照你建議」（回應 Q1(a)）。
```
**執行層回報：已落實。** 11 個跨章節 leaf 之素材見 `data/multi_chapter_leaves.md`
（B1），未附任何建議歸屬。單章節之 103 個 leaf 依 (a) 自動歸屬。
**待 Pei 逐條裁定 11 條。**

```
[R-P16] §E Layer 3 之 CFTS009 §1.8.1 刪除。實測 0 leaf 落點，
        係分析層轉抄時多寫。
        裁決者 Pei，逐字依據：「都照你建議」（回應 Q1(c)）。
```
**執行層回報：已落實於 §E，但前提須複核。** 「實測 0 leaf 落點」僅在
02 包之主章節規則下成立。實測 `SWE-PM-057` 之章節集合含
**CFTS009 §1.8.1.1.1（ID 1 Description）**，出現 3 次，與其他兩章節同票。
即 §1.8.1 並非無 leaf 觸及，而是在該規則下未勝出。
依 R-P15(b)，`SWE-PM-057` 之歸屬尚待裁定 —— 若裁定為 §1.8.1.1.1，
則 R-P16 之刪除前提不成立。**登記為 A-PW14，待 Q1 裁定後回頭確認。**

```
[R-P17] 文字層定義追認為條文：
        每段同時產出 plain 與 bold 兩種序列化；
        §C rule 1（章節錨點）套用於 plain，
        §C rule 2（需求錨點）套用於 bold，兩者依段落索引對齊。
        理據：CFTS009 標題以段落樣式 pStyle 1–8 表達（run 層無粗體），
        CFTS010 標題為 run 層粗體；單一序列化不可能同時滿足兩條正則。
        佐證：該定義下 CFTS009 得 196 章節錨點，196 項全部具
        heading pStyle 1–8 無一例外。
        併同追認：G8 章節錨點期望值 172 → **196**（172 為衍生物漏算）。
        裁決者 Pei，逐字依據：「都照你建議」（回應 Q2）。
```
**執行層回報：已落實。** G8 = 904 / 196、G9 = 148 / 92，皆 PASS。
定義已寫入 `scripts/extract_textlayer.py` 之 docstring。
**附帶發現**：本定義套用於 SYS3 SYSAD 時 rule 1 匹配數為 **0**（G16）——
該文件之標題不含字面章節號（Word 自動編號），非序列化問題。見 B3。

```
[R-P18] G6 拆為兩閘，並訂正 §C 座標：
        G6a = 錨點鏈第一段，SYS2 CFTS009 欄內 token 可抽取率
        G6b = 錨點鏈第三段，該 token 可解析至 CFTS 章節之比率
        §C 之 SYS2 CFTS009 讀取座標改為 r2–**r339**，
        並註明 r339（NRL-142587）為無 Feature-ID 之 Heading 列。
        01/02 包之 G6「336/337」係將第一段與第三段混為一談，作廢。
        裁決者 Pei，逐字依據：「都照你建議」（回應 Q3）。
```
**執行層回報：已落實，且診斷獲得實測確證。**
G6a = **337 / 338**（r339 唯一無 token 者）。r339 之 `Type` 欄實測為
`Heading`，`Sys-RA-Feature-ID` 欄為空 —— 與條文描述完全相符。
G6b = 列層 **336 / 337**，唯一失敗者 `Sys-RA-PM-0334`（`4942087`）。
**此數精確還原 01 包之「336/337 失敗者 Sys-RA-PM-0334」** ——
證實該數量的是第三段而非第一段，R-P18 之診斷正確。
token 層 438 / 439；其中 81 個 token 係經**章節錨點 id** 路徑解析
（Sys-RA 直接指向章節而非需求錨點），見 A-PW15。

```
[R-P19] 02 下放包 §A 末句與 §H 步驟 8 之「七條」訂正為「六條」。
        §A 實際區塊數為 6（R-P9–R-P14），§J 之「六條」為正確值。
        裁決者 Pei，逐字依據：「都照你建議」（回應 Q4）。
```
**執行層回報：已訂正。** 訂正後重驗：§A fenced block = 6，
全檔「七條」出現 0 次、「六條」3 處一致（§A 末句、§H 步驟 8、§J）。

```
[R-P20] SYS3 SYSAD 納入閱讀範圍。R-P3′ 後技術障礙已消失。
        其 §4.x 元件分解為目前唯一可能提供**獨立**分組來源之文件
        （§E 已自承 037 Requirement Title 無分組價值），
        直接關係 Layer 2 邊界之可交叉驗證性。
        本條僅解除「不讀」之限制，不改變其對 TC 內容不具權威之地位（§8.1）。
        裁決者 Pei，逐字依據：「都照你建議」（回應 Q5）。
```
**執行層回報：已讀。** 見 `data/sys3_chapters.md`（B3）。
47 個 heading（Heading1 ×9、Heading2 ×38），§4「系統架構設計」下
36 個 Heading2。**未據此調整 §E 之任何分組**（B3 禁區）。

```
[R-P21] 037 之 SYS2 Traceability 與 Excluded NRLs (HW-only) 兩分頁
        補入 §C 讀取座標並補設閘門。
        A-PW03 / A-PW04 / A-PW05 三條 anomaly 全部宣稱此二分頁之內容，
        卻自 01 包以來從未經執行層複驗。
        裁決者 Pei，逐字依據：「都照你建議」（回應 Q6）。
```
**執行層回報：已落實。** 座標實測：`SYS2 Traceability` 表頭 r1、資料 r2–r34
（33 列全非空）；`Excluded NRLs (HW-only)` 表頭 r1、資料 r2–r27（26 列全非空）。
三條 anomaly 複驗結果見 `docs/upstream/03_framework_inputs.md` §六：
A-PW03 成立（且應加強）、A-PW04 逐字成立、**A-PW05 描述有誤須修正**。

```
[R-P22] 跨多章節 leaf 之非主章節不得靜默丟棄。
        補設閘門驗證：被丟棄之次章節是否已被其他 leaf 覆蓋。
        未覆蓋者須逐一登記，否則 Layer 3 之涵蓋宣稱不成立。
        裁決者 Pei，逐字依據：「都照你建議」（回應 Q7）。
```
**執行層回報：已落實，結果嚴重。** G14 實測：被丟棄之相異次章節 10 個，
其中 **9 個未被任何 leaf 之主章節覆蓋**，包含 `Stolen Vehicle Mode`、
`Logistic Standby` / `Logistic Sleep` / `Logistic Idle`、
`ICS Wakeup Reasons by POWER Button Pressed` 等實質功能章節。
在現行主章節作法下，這 9 章不會產生任何 TC。登記為 **A-PW16**。

```
[R-P23] R-P13 台帳通則增補第（d）欄：轉換工具之版本與 OS 版本。
        現況 textutil 版本隨 macOS 更新而變，同一指令跨機器
        未必產生同一雜湊；本機 Darwin 25.5.0 之可重現性已驗，
        跨機器未驗。
        裁決者 Pei，逐字依據：「都照你建議」（回應 Q8）。
```
**執行層回報：已落實。** 第（d）欄實測值：
macOS 26.5.2（build 25F84）／Darwin 25.5.0 arm64；
`/usr/bin/textutil` 173,088 B、mtime 2025-06-25（系統二進位，無版本字串）；
Python 3.10.13；openpyxl 3.1.5。見 03 上繳包 §三。

---

## 第四輪 — 下放包 04（`docs/handoff/04_framework_lock.md` §A）

```
[R-P24] 一個 leaf 得對應多個 Layer 3 章節。
        Layer 3 記錄該 leaf 觸及之全部相異章節，不擇一。
        Layer 2（Test Set）仍為單值，需擇一。
        理由：Layer 3 為文件內部之導航與涵蓋單位，不入工作簿（§4.1.5），
        無「一列一值」之約束；R-P15 將 Layer 2 之欄位限制
        誤套於 Layer 3，該前提本身未經驗證（03 §九第 1 項）。
        R-P15(b) 之「逐條裁定」範圍因此收窄為僅 Test Set 歸屬，
        Layer 3 之章節集合改為自動記全集。
        裁決者 Pei，逐字依據：「照建議吧」（回應 03 Q5）。
```
**執行層回報：已落實。** `data/layer3_full.tsv` —— **140 列**，涵蓋 114 leaf，
**G13b = 46 個相異章節（PASS）**。每 leaf 章節數分布：1 章 ×103、2 章 ×1、
3 章 ×7、4 章 ×2、6 章 ×1。產生指令 `python features/power/scripts/build_layer3.py`。

```
[R-P25] R-P16 撤回。
        撤回理由並非「§1.8.1 無 leaf」為單純誤測，而是
        **規則已廢止而其結論留存**：
        SWE-PM-057 觸及 §1.8.1.1.1，與 §1.6.2.1.17、§1.6.3.1.1
        三方同票（各 3 次）；「§1.8.1 零落點」僅在 02 包
        「同票取最深」之 tie-break 下成立（深度 5 勝 4），
        而該 tie-break 已由 R-P15 廢止。
        此與 R-P18 所訂正之 G6 錯誤同型：以一個層級或一條已失效之
        規則所得之結果，斷言另一層級之事實。
        §E Layer 3 之 §1.8.1 刪除線移除，改記 §1.8.1.1.1。
        裁決者 Pei，逐字依據：「照建議吧」（回應 03 Q2）。
```
**執行層回報：已落實。** `docs/handoff/01_intake.md` §E 之 Power State 列
已由 `~~§1.8.1~~` 改為 **`§1.8.1.1.1`**。A-PW14 標記為已處置。

```
[R-P26] 11 條跨章節 leaf 之處置：
        （a）Layer 3 —— 十一條全部依 R-P24 自動記全集，無需裁定。
        （b）Layer 2 Test Set —— 僅 SWE-PM-008 與 SWE-PM-057
             兩條之歸屬會改變分布。分析層已獨立重算確認算術閉合。
             此二條依 R-P15(b) 逐條由 Pei 裁定。
        （c）SWE-PM-057 之裁定素材已於 03 §七備齊，本包不重複產出。
             SWE-PM-008 之素材見 §B1。
        裁決者 Pei，逐字依據：「照建議吧」（回應 03 Q1）。
```
**執行層回報：已落實。** （a）11 條之 Layer 3 已入 `layer3_full.tsv`。
（b）（c）`SWE-PM-008` 素材見 `data/b1_swepm008.md`，未附建議歸屬。
**待 Pei 裁定二條。**

```
[R-P27] A-PW16 之 9 個未覆蓋章節：不併入既有 Test Set、
        不另立 Test Set、不宣告不涵蓋。
        先查清其是否為真實 coverage hole ——
        Stolen Vehicle Mode 與三個 Logistic 狀態為實質功能，
        「它們沒有 TC」比「它們該歸哪個 Test Set」重要。
        判定素材見 §B2。
        裁決者 Pei，逐字依據：「照建議吧」（回應 03 Q4）。
```
**執行層回報：已落實。** `data/b2_uncovered_chapters.md`，逐章判讀結果：
涵蓋 1、涵蓋（有一分支例外）1、部分涵蓋 5、未涵蓋 1、無法判定 1。
**未涵蓋者為 §1.6.2.1.4 Stolen Vehicle Mode** —— 惟該章 `4941400` 逐字為
「the R1 HU shall not enter stolen vehicle mode under any condition」，
其 `Radio` 欄含 `R1L`（本專案車型），而描述進入條件之 `4941399` 之 `Radio` 欄
不含 R1L。此為並陳之事實，非處置建議。

```
[R-P28] 量測章節層之反向缺口。
        R-P7 所裁「不追 SYS2 反向缺口」指需求層，
        章節層不在其射程內，且從未量測。
        CFTS009 196 章 + CFTS010 92 章 = 288 章，
        現被觸及者 46 章（16%）。須產出未被觸及之 242 章清單並分類。
        A-PW16 之 9 章為此問題之子集。
        裁決者 Pei，逐字依據：「照建議吧」（回應 03 Q6）。
```
**執行層回報：已量測（G17）。** 288 章中被觸及 46、未觸及 **242**。
分類（依是否含需求錨點）：**含需求錨點者 171 章，共 499 個需求錨點**；
無需求錨點之純標題／定義殼 71 章。被觸及之 46 章含 553 個錨點。
**須一併呈報**：未觸及章節之 499 個錨點，依定義全部屬於「未被 037 引用」者，
即 **R-P28 之章節層缺口完全內含於 R-P7 已裁定不追之需求層缺口**（實測未被引用
CFTS009 669 + CFTS010 145 = 814 個）。R-P28 之前提「章節層不在 R-P7 射程內」
形式上成立，但其內容與 R-P7 所免除者重疊。詳見 04 上繳包 §六。

```
[R-P29] A-PW05 訂正（分析層逐字指定）。
        原描述「SYS2 Traceability 用 SWE1-PM-TLM-001..033 / -ANT-」
        之 -ANT- 部分為假：該分頁 33 列中含 ANT 者 0 筆。
        SWE1-PM-ANT-008 僅出現於 SWE1 Requirements 分頁
        SWE-PM-089 之 Source Requirement ID 欄，即 A-PW01 所指者。
        分析層將 A-PW01 之證據誤植於 A-PW05。

        訂正後描述逐字如下：
        「037 內部 id 命名空間不一致：SWE1 Requirements 分頁用
         SWE-PM-001..115（115 筆連續）；SYS2 Traceability 分頁用
         SWE1-PM-TLM-001..033（33 列，前綴分布單一）。
         該分頁 SWE-PM- 出現 0 次，兩套互不對應。
         實測附註：SWE1-PM-ANT- 命名空間不在本分頁，
         其唯一出處為 SWE-PM-089 之 Source Requirement ID 欄（見 A-PW01）。」

        核心主張（兩套命名空間互不對應）維持成立，僅證據出處訂正。
        裁決者 Pei，逐字依據：「好 出」。
```
**執行層回報：已逐字替換。** 訂正前後全文見 04 上繳包 §七。新增 A-PW18 登記誤植本身。

```
[R-P30] A-PW03 加註（分析層逐字指定）。
        原描述僅指出「涵蓋範圍不足」。實測顯示分類亦不實。
        加註逐字如下：
        「加註（03 §六複驗）：分頁名為 Excluded NRLs (HW-only)，
         但 26 列之 SW/HW/System 欄實測為 HW 18 / Information 4 /
         Out of Scope 2 / Heading 1 / 空白 1。
         故『(HW-only)』在分類上亦不實，不僅是涵蓋範圍不足；
         原描述只指出後者。」
        A-PW04 經逐字複驗成立（NRL-994xx 0 次、Sys-RA-PD 0 次、
        Sys-RA-PM- 76 次），不動。
        裁決者 Pei，逐字依據：「好 出」。
```
**執行層回報：已逐字加註。** A-PW04 未動。

```
[R-P31] A-PW06（037 Sub Categorization 詞彙漂移）補驗。
        其宣稱「不可作分批判準」，而分批規劃即將用到（DECISIONS.md §7），
        卻自 01 包以來從未由執行層複驗。
        03 包之 R-P21 僅點名 A-PW03/04/05 三條，為分析層之疏漏。
        裁決者 Pei，逐字依據：「照建議吧」（回應 03 Q7）。
```
**執行層回報：已複驗（G18），逐字成立。** 實測值域恰為 5 值，計數逐一相符：
`HMI` 36、`Service\nHMI` 35、`Service` 27、`HMI Service` 16、`HMI/Service` 1，
合計 115。原描述無須訂正。附註：`Service\nHMI` 之分隔為換行字元 U+000A。

```
[R-P32] 04 包安排 SYS3 SYSAD §4.x 與 §E 之交叉比對。
        R-P20 之理由為「§4.x 為目前唯一可能提供獨立分組來源之文件」，
        03 包依 B3 禁區只取素材未做比對，故 R-P20 僅完成一半，
        §E「不是交集、只由單一來源支撐」之弱點原封不動。
        切入點：03 §七實測 SYS3 §4「動態行為 Dynamic Behavior」
        下有七個狀態子節，與 Power State 所轄之 TLM 狀態直接可比。
        本包完成比對後，該弱點方得結論（無論結論為支持或不支持）。
        裁決者 Pei，逐字依據：「照建議吧」（回應 03 Q8）。
```
**執行層回報：已完成比對（G20），結論為否。** 見 `data/b3_sys3_crosscheck.md`。
關鍵實測：SYS3 文字層含 `Sys-RA-PM-` token **630 個出現、272 個相異**，故可連結；
但 549 個歸屬中 **444（81%）落在兩個分配矩陣節**（§4.28 230、§4.3 214），
而 R-P32 所指定之切入點 —— 「動態行為」七個狀態子節（§4.30–§4.36）——
其 Sys-RA token 數 **全為 0**。名稱七項全對得上，卻無任何 leaf 可被指派。
**SYS3 不構成 §4.1.2 之第二來源**；§E 之弱點確認成立且無法以 SYS3 消除。
登記為 A-PW20。

---

---

## 第五輪 — 下放包 05（`docs/handoff/05_testset_lock.md` §A）

```
[R-P33] `SWE-PM-008` 之 Test Set = **Power State**。
        依據：Requirement Title 為 `Logistic Mode`；
        Description 全文僅述 HW supplier 通知
        Logistic Idle / Logistic Standby / Logistic Sleep、
        子元件確保無功能可用並準備關機；
        Verification Method 為改 `STATUS_BH_BCM1.PowerModeSts`
        至 `Logistic_Mode_On` 後 HU 關機。**全文無一字涉及 timeout。**
        02 包演算法將其判入 Timeout Settings，係因 §1.6.7.1 得 4 票
        高於三個 Logistic 章節各自之 3 / 2 / 2；
        該規則將合計 7 個命中之同一能力叢集拆散，
        並使票數最高之章節恰為與本 leaf 內容最無關者。
        裁決者 Pei，逐字依據：「是照你建議 去做」。
```
**執行層回報：已落實（G22 PASS）。** `data/leaf_testset.tsv` 該列依據欄記 `R-P33`。

```
[R-P34] `SWE-PM-057` 之 Test Set = **Timeout Settings**。
        依據：Description 為讀 PROXI 參數 `Switch_Off_Time`、
        設定至 `SwitchOff_Timeout_Setting.Req`，值域 00 / 20 / 60 / 180 min。
        行為與 Test Set 之能力分群判準（§4.2：同一 Test Set
        應共用 setup 與 UI 進入路徑）一致。
        已知反面：§1.6.2.1.17 標題 `Proxi Parameters management`
        與 leaf 標題 `Proxi Parameter management` 近乎逐字；
        本裁決採**行為勝過名稱**。此張力登記，不隱藏。
        裁決者 Pei，逐字依據：「是照你建議 去做」。
```
**執行層回報：已落實（G22 PASS）。** 條文所載之張力於 §E 註記中一併保留。

```
[R-P35] §E 定版為 **63 / 24 / 16 / 8 / 3**，合計 114
        （＋ `SWE-PM-089` 留空 = 115）。
        Power State 63 / Startup Display 24 / Branding and Theme 16 /
        Timeout Settings 8 / Power Down 3。
        原 64 / 24 / 16 / 7 / 3 係在「兩條皆歸 Power State」之
        隱含假設下產生，該假設從未經裁定。
        R-P5 / R-P6 所裁者為「合併」與「Power Down 獨立」，
        非此五個數字，故本次定版不牴觸該二條。
        §E 標題由「待定版」改為「**已定版（R-P35）**」。
        裁決者 Pei，逐字依據：「是照你建議 去做」。
```
**執行層回報：已定版（G21 PASS）。** `python features/power/scripts/build_testsets.py`
實測 63 / 24 / 16 / 8 / 3，合計 114，無未歸類 Test Set。
該腳本**不含任何 tie-break**，遇未裁定之跨 Test Set leaf 即 `SystemExit`。
**併同登記一項清單缺漏**：§E 之 Layer 3 列未涵蓋父章節 `§1.6.2.1`（9 leaf 觸及）
與 `§1.6.2.1.17`（1 leaf）。二者於指派時作為「非 Test Set 候選」登記，
不影響分布數字。見 A-PW24。

```
[R-P36] **裁決條文不可變（跨 feature 通則，canon 候選）。**
        已落檔之裁決條文，其內文一字不得事後修改，
        包含其中內嵌之數值、引用與敘述。
        該等內容若嗣後經實測證明失效，處置方式為
        **於該條文之下新增註記**，載明失效項、實測值、量測條件與發現包次，
        原文保留。
        理由：逐字記錄之價值來自不可變性；
        若允許回頭改寫裁決內文，則「逐字依據」失去意義，
        且無從區分「當時如此裁定」與「事後如此改寫」。
        適用範例：R-P7 條文內嵌「547 條」，04 包實測為 669（A-PW19）——
        **R-P7 原文不改**，於其下加註。
        裁決者 Pei，逐字依據：「是照你建議 去做」（回應 04 Q5）。
```
**執行層回報：已首次適用，G27 PASS。** 三處加註（R-P7、A-PW05、04 下放包 §前言），
註記皆為新增段落。加註前後各計原文 SHA256，**三處雜湊完全相同**，
證原文位元組未變。詳見 05 上繳包 §三。

```
[R-P37] R-P28 之章節層反向缺口調查**停止**。
        04 §六已證明該缺口完全內含於 R-P7 所裁定不追之
        需求層缺口，續行即繞過已生效之裁決。
        G17 之量測結果（288 章中未觸及 242；含需求錨點 171 章 / 499 錨點；
        純標題殼 71 章）保留為紀錄，不再擴充、不再據以行動。
        G17 自 06 包起自閘門表移除。
        裁決者 Pei，逐字依據：「是照你建議 去做」（回應 04 Q4）。
```
**執行層回報：已停止。** 本包未對章節層缺口做任何新量測或擴充。
G17 之 04 包結果原樣保留於 04 上繳包 §六。06 包移除為分析層之動作，本包僅記錄。

```
[R-P38] B2 判讀單位訂正並重做。
        現行 B2 以「章 vs leaf」比對，正確單位為
        **「被引用之錨點 vs leaf」**。
        04 §九第 1 項已示範其脆弱：§1.6.2.1.4 初判「未涵蓋」，
        補查引用層後發現該章在範圍內之唯一需求為一條否定需求，
        判定結論未變而其意義完全改變；其餘八章未做同等深度複查。
        九章全部重做，「該 leaf 實際引用了該章的哪幾個錨點」
        改為**必填欄位**，非事後補查。
        A-PW16 之處置（04 Q2）與 §1.6.2.1 之處置（04 Q3）
        暫緩至重做完成後再裁。
        裁決者 Pei，逐字依據：「是照你建議 去做」（回應 04 Q6）。
```
**執行層回報：已重做（G23 PASS），且訂正單位後有一章判定實質改變。**
`data/b2_v2_uncovered_chapters.md`（61,432 bytes），舊檔標註取代不刪。
**§1.6.2.1.15.1 由「部分涵蓋」改為「涵蓋」** —— v1 所據之 `4941661`（ICS POWER
按鍵 wakeup 路徑）與 `4941662` **未被任何 leaf 引用**；實際被引用者為 `4941663`，
其內文全文為「In "Timed Mode" the Customer setting screens shall be disabled.」，
`Radio` 欄 `R1L-R, R1L`，與 `SWE-PM-004` Description 末句逐句對應。
另實測：九章合計 31 個錨點，**僅 18 個被引用**；§1.6.2.1.14 之 8 個錨點中僅 1 個被引用。

```
[R-P39] 清點 CFTS 本文之嵌入物件。
        04 §九第 3 項實測 §1.6.2.1 之文字層僅有兩個
        `WrapperResource` inline RTF 參照，即規格內容可藏於
        R-P17 文字層定義看不見之處。
        全文之嵌入物件從未清點，故 G8 = 904 所代表之規格覆蓋率
        目前無上界保證。
        本包清點兩份 CFTS 全文之嵌入物件數量與分布。
        **本包不解 RTF、不改 R-P17 之文字層定義** ——
        清點結果為 06 包之裁定素材。
        裁決者 Pei，逐字依據：「是照你建議 去做」（回應 04 Q7）。
```
**執行層回報：已清點（G24），結論與條文之推測形態不同。**
CFTS009 `.docx` 實測**無 `word/embeddings/`**、`w:object` / `w:drawing` /
`w:pict` / `o:OLEObject` 各 **0** 個、`word/media/` 僅 1 個頁首圖。
即 `…inline.rtf WrapperResource` **是純字面文字，不是嵌入物件的錨** ——
其所指之 RTF 資源**未隨文件匯出**。
**故並非「藏在看不見之處」，而是「不存在於交付文件之中」。**
實測 CFTS009 16 處、CFTS010 15 處，合計 **31 處**，分布於 **16 個章節**（各 8 章），
其中 8 章之非錨點內文 < 200 字元。登記為 A-PW23（框架依實測訂正）。

```
[R-P40] 量測被引用 item 之 `EE Architecture` 分布。
        04 §九第 4 項已驗車型軸無虞（被引用之 238 個 item
        全為 `allSys` 151 或含 `R1L` 87，零例外），
        但 `EE Architecture`（Atlantis High / Mid）軸未做同等檢查。
        FW036 有七個車型欄（03 包 G12 實測 c21–c27）於 Phase 4 待填。
        01 包 §K 第 2 項所指之 140 個 Atlantis 錨點位於**未被引用**之池中
        且已由 R-P7 免除，與本條所指之**被引用**池不同，不得混為一談。
        裁決者 Pei，逐字依據：「是照你建議 去做」（回應 04 Q8）。
```
**執行層回報：已量測（G25）。** 238 個被引用 item 全數帶 `EE Architecture` 欄，
無一缺漏。值組：`Atlantis High, Atlantis Mid` 162、`All` 61、
**`Atlantis Mid` 單值 13**、`Atlantis High, Atlantis Mid, CUSW, PowerNet` 1、
**`Atlantis High` 單值 1**。
**無任何被引用 item 落在兩世代之外**；224 / 238 兩世代通用，14 個為單世代專屬。
逐 leaf 層級：114 個 leaf 中僅 **2** 個之 item 聯集僅含單一世代。

```
[R-P41] B2 之事實部分腳本化；037 各欄相異值數補測。
        （a）B2 之章節全文、被引用錨點、leaf Description 三項
             為機械可得，須以腳本產生（`build_b2.py`），
             僅保留判讀欄為人工填寫。
             B1 / B3 同理補腳本（`build_b1.py` 已存在，B3 補 `build_b3.py`）。
        （b）G19 已證 037 十八欄零空值，但非空不等於有鑑別力。
             補測各欄之相異值數。
             已知 `Categorization` 為單一值（即 G2 之內容，且被判為 PASS）——
             此類欄位在分批、優先級、追溯上與空欄實際效果相同。
        裁決者 Pei，逐字依據：「是照你建議 去做」（回應 04 §九第 5、6 項）。
```
**執行層回報：已落實（G26）。**
（a）新增 `build_b2.py`（判讀欄以 `JUDGEMENTS` 常數保存，隨腳本版控）、
`build_b3.py`、`build_layer3.py`、`build_testsets.py`；
另**自行補** `build_b4_b5.py`（B4 / B5 為本包新產出，條文未點名，
但為貫徹「素材須可重現」之精神一併腳本化）。
（b）相異值數 = 1 者 **3 欄**：`Release Version`、`Categorization`、`Feasibility`；
≥ 100 者 **4 欄**：`SWE-Requirement ID` 115、`Requirement Description` 115、
`Verification Method` 114、`Source Requirement ID` 112。
**附帶查出**：§E「本分組之已知弱點」稱 `Requirement Title` 出現「20+ 種」、
「`Timeout` 7、`Phone Call` 5 為僅有例外」—— 實測 **99 種**、僅出現 1 次者 94 種，
且另有 `Splash Screen logo visualization` 4、`Power down` 3、`FOTA` 2 三組重複。
該節結論（無分組價值）不受影響，反而更強。登記為 A-PW25。


---

## 第六輪 — 下放包 06（`docs/handoff/06_scope_boundary.md` §A）

```
[R-P42] **錨點層範圍上界**（跨 feature 通則，canon 候選）。
        某 leaf 之 TC 範圍上界為「該 leaf 之 Source Requirement ID
        所實際引用之需求錨點」。
        未被引用之錨點，其行為**一律不測**，即使該錨點與被引用者
        位於同一章節、甚至僅一行之隔。
        本條非新規則，係 §8.2.1（不得擴張至 sibling Req）與
        §8.4.2（不得測當前 spec 未擁有者）在錨點層之直接套用：
        章節是規格作者的編排單位，錨點才是 SYS2 / 037 的引用單位，
        涵蓋性判斷必須以引用單位為準。
        未被引用之錨點若確有行為缺口，屬**RD 之分解問題**，
        走 DATA_REQUEST，不由 TC 作者吸收，不寫入 TC，
        不於 `reasoning` 欄以「為求完整」為由納入。
        依據：05 包 §4.1 —— §1.6.2.1.15.1 之判定於單位訂正後
        由「部分涵蓋」翻為「涵蓋」，v1 所依據之兩個錨點
        `4941661` / `4941662` 未被任何 leaf 引用。
        判讀單位錯誤會產出**實質錯誤**之 coverage hole，非僅意義改變。
        裁決者 Pei，逐字依據：「好照你建議」（回應 05 Q3）。
```
**執行層回報：已落實為本包之判讀基準。** B1 之 leaf 層交叉即依本條區分「範圍內」與「範圍外」：
31 處懸空參照中 29 處落在未被引用之錨點下，依本條**不測、不發 DR**，僅 2 處入 DR-PW6。
A-PW27 已逐一登記部分涵蓋 5 章之未被引用錨點。

```
[R-P43] A-PW16 九章依 R-P42 逐章落地：
        （a）涵蓋 3 章（§1.6.2.1.15.1、§1.6.3.1.1、§1.8.1.1.1）—— 無待辦。
        （b）部分涵蓋 5 章（§1.6.2.1.9 / .10 / .11 / .14、§1.6.2.1.4 除外）——
             未被引用之錨點**不測**，逐一登記於 ANOMALIES，不發 DR。
        （c）**未涵蓋 1 章：§1.6.2.1.4 Stolen Vehicle Mode** ——
             其**有**被引用之錨點（1 個，`SWE-PM-003`）卻判為未涵蓋，
             性質與（b）不同：不是「範圍外」，是「範圍內而未被涵蓋」。
             → 發 DR。
        （d）無法判定 1 章：§1.6.2.1 —— 併入 R-P44。
        裁決者 Pei，逐字依據：「好照你建議」（回應 05 Q1）。
```
**執行層回報：已逐章落地。**（a）3 章無待辦。（b）5 章之未被引用錨點依 R-P42 不測，
逐一登記於 A-PW27（§1.6.2.1.4 之 `4941399`、§1.6.2.1.9 之 `4941429`、§1.6.2.1.14 之 7 個、
§1.6.2.1.15.1 之 `4941661`/`4941662`、§1.8.1.1.1 之 `4941813`/`4941816`）。
（c）§1.6.2.1.4 → **DR-PW5**（High）。（d）§1.6.2.1 → 併入 DR-PW6。

```
[R-P44] §1.6.2.1 之兩個被引用錨點皆為懸空 `WrapperResource` 參照，
        內容不存在於交付文件中，**無法據以撰寫 TC**。
        與全文 31 處懸空參照併為同一 DR（見 R-P45）。
        該章觸及 9 個 leaf（`SWE-PM-001`–`009`），為影響面最大之單一章節。
        裁決者 Pei，逐字依據：「好照你建議」（回應 05 Q2）。
```
**執行層回報：已併入 DR-PW6。** B1 交叉確認該章之兩個被引用錨點 `4941354` / `4941355`
即為 31 處中唯二落在被引用錨點下者，觸及 9 個 leaf，全屬 Power State ——
**條文所稱「影響面最大之單一章節」經實測為「唯一有影響面之章節」**。

```
[R-P45] 懸空 `WrapperResource` 參照發 DR，
        **但須先完成 leaf 層交叉再發**。
        現況只知 31 處分布於 16 章（CFTS009 16 處、CFTS010 15 處），
        不知牽連幾個 leaf、幾個被引用錨點。
        DR 若不能講清影響面，收件方無從評估優先序。
        交叉內容見 §B1。
        裁決者 Pei，逐字依據：「好照你建議」（回應 05 Q4）。
```
**執行層回報：已完成交叉後發 DR（G29）。** 交叉結果大幅收窄影響面：
31 處 / 16 章 → **受影響之被引用錨點 2 個、leaf 9 個 / 114、Test Set 1 個（Power State）**。
據此將 DR-PW6 之 Urgency 定為 **Medium** 而非 High（不阻斷 framework，
且僅影響 9 個 leaf 之 `specification_reference` 而非其可否撰寫）。
**R-P45「先交叉再發」之要求直接改變了 Urgency 判定** —— 若依 31 處 / 16 章之表面數字，
本應判為 High。

```
[R-P46] A-PW24 之兩處缺漏**補列**於 §E Layer 3：
        CFTS009 `§1.6.2.1`（TLM algorithm requirements，9 leaf 觸及）
        與 `§1.6.2.1.17`（Proxi Parameters management，1 leaf 觸及）。
        R-P35 所鎖定者為**分布數字**，非 Layer 3 章節清單；
        補列不動 63 / 24 / 16 / 8 / 3。
        `§1.6.2.1` 補列於 Power State，`§1.6.2.1.17` 補列於 Timeout Settings
        （依 R-P34 之 `SWE-PM-057` 歸屬）。
        補列後 G13b 之 46 不變（該二章原已在 `layer3_full.tsv` 內，
        缺漏僅在 §E 之人工清單）——此點須實測確認。
        裁決者 Pei，逐字依據：「好照你建議」（回應 05 Q5）。
```
**執行層回報：已補列，G13b 仍為 46（PASS）。** §E Power State 之 Layer 3 增列 `§1.6.2.1`、
Timeout Settings 增列 `§1.6.2.1.17`。`layer3_full.tsv` 重跑仍為 140 列 / 46 章 ——
證條文所稱「缺漏僅在 §E 之人工清單」為真。G21 複驗 63/24/16/8/3 未受影響。

```
[R-P47] A-PW25（§E 弱點段「20+ 種／僅有例外」，實測 99 種／另有三組）
        **走註記，原文不改**。
        §E 弱點段非裁決條文，但屬歷史紀錄，同樣適用 R-P36 之精神。
        且訂正後結論更強（99 種比 20+ 種更無分組價值），
        保留原文對照具價值。
        裁決者 Pei，逐字依據：「好照你建議」（回應 05 Q6）。
```
**執行層回報：已加註，原文位元組未變（G31 PASS）。**
原文 SHA256 `1f737d7c1498002e56a4c50e4b33d5d130608d4d7c479e614a6839f7437d1ca6`，
加註後同值。**惟過程中曾一度將該段自句中切斷，已修復並重驗** —— 詳見 06 上繳包 §六。

```
[R-P48] R-P39「不得解 RTF」之範圍釐清：
        指**不解析 RTF 之內容**，**不涵蓋**「檢視 OLE2 之 storage 目錄結構」。
        `olefile` 已由 R-P3′ 解禁，列出 storage 清單屬清點，非解內容。
        CFTS010 之嵌入物件清點應據此取**上界**，
        05 包所得之 15 處為 `textutil html` 輸出之下界。
        裁決者 Pei，逐字依據：「好照你建議」（回應 05 Q7）。
```
**執行層回報：已據此取上界（G30），結果為 0。**
`olefile` 未安裝；依 CLAUDE.md「優先用內建函式庫」，以標準函式庫自行解析 MS-CFB 之
標頭、FAT 與目錄鏈（`scripts/build_ole_census.py`），**只讀目錄項，未讀任何 stream 內容**。
實測 14 個目錄項（root 1 / stream 10 / storage 3），**無 `ObjectPool`、無 `\x01Ole`、
無 `_`-起始之物件 storage**。即 CFTS010 之嵌入物件數為 **0**，此為確定值而非下界。
與 CFTS009 同 —— 二份文件皆零嵌入物件。

```
[R-P49] `Verification Criteria` / `Verification Method` 兩欄補設品質閘。
        二欄為 Phase 4 產生 TC 之直接輸入，現有閘門僅驗「非空」（G19）
        與「相異值數」（G26），**未驗其是否可執行**。
        反例：`SWE-PM-008` 之 Verification Criteria 全文為
        「Vehicle equiped with CAN」（含拼字錯誤），不含任何可執行條件。
        非空不等於可用 —— 此與 G19 / G26 為同一教訓。
        閘門定義見 §D 之 G28。
        裁決者 Pei，逐字依據：「好照你建議」（回應 05 Q8）。
```
**執行層回報：已量測，G28 基線建立。**
VC 單欄不可執行 **2 / 115**（`SWE-PM-007`、`SWE-PM-008`）、
VM 單欄不可執行 **0 / 115**、**二欄合觀不可執行 0 / 115**。
條文所舉之反例 `SWE-PM-008` 確為兩筆之一。
**因二欄合觀無不可執行者，DR-PW7 之 Urgency 定為 Low（不阻斷任何 leaf）。**
量測含 6 筆人工覆寫（2 筆確認不可執行、4 筆訂正正則之偽陰性），皆附理由隨腳本版控。

```
[R-P50] Layer 3 邊界之定性（採納 05 §8.1 之判斷）。
        Layer 3 之 46 章係錨點鏈之投影，非獨立之範圍界定。
        本 feature 之規格涵蓋範圍實由 **SYS2 之收錄規則**決定，
        而該規則已由 R-P7 裁定不追。
        故此非「該驗而未驗」之缺口，而是**已被裁決放棄驗證之範圍問題**，
        登記為**長期已知限制**，不列待辦、不再探查。
        Phase 4 之交付說明須明載：
        「本 feature 之規格涵蓋範圍由 SYS2 收錄規則決定，
         Layer 3 為其投影，非獨立界定。」
        逐字採納執行層之表述：
        「若日後有人問『為什麼某章沒有 TC』，正確答案是 R-P7，不是 Layer 3。」
        裁決者 Pei，逐字依據：「好照你建議」（回應 05 §8.1）。
```
**執行層回報：已登記為長期已知限制。** `docs/INDEX.md` §「長期已知限制」已載，
並逐字採納條文所定之表述。Phase 4 交付說明之明載義務併同登記，待 Phase 4 執行。

```
[R-P51] 05 §8.2 第 4 項（B2 判讀無第二意見）登記為**已知限制，不另立機制**。
        Phase 4 撰寫 TC 時，作者必然再次接觸同一批被引用錨點，
        該接觸即為天然之第二次判讀。
        另立覆核機制之成本高於收益。
        惟 Phase 4 若於該接觸中發現與 B2 v2 判讀相左者，
        **須登記為 anomaly，不得逕行採用任一方**。
        裁決者 Pei，逐字依據：「好照你建議」（回應 05 §8.2 第 4 項）。
```
**執行層回報：已登記。** B2 v2 之判讀以 `build_b2.py` 之 `JUDGEMENTS` 常數版控，
Phase 4 若發現相左者須登 anomaly 之義務併同登記。

---

## 待裁

- **DR-PW1**（`SWE-PM-089` 上游來源，High）—— 阻斷該 leaf 之 TC。
- **DR-PW5**（§1.6.2.1.4 之否定需求由誰承接，High）。
- **DR-PW6**（31 處懸空參照，Medium）—— 影響 `SWE-PM-001`–`009` 九個 leaf 之
  `specification_reference`。
- **DR-PW3**（`4942087` 歸屬，Medium）、**DR-PW7**（VC 欄品質，Low）—— 皆不阻斷。
- **A-PW29 之用途轉向**：G25 之 EE Architecture 分布（Mid 單值 13、High 單值 1）
  為 Phase 4 填 FW036 c21–c27 之判準來源，與 R30-3 / R30-4（車型欄留白）之
  既有政策併存與否，待裁。
