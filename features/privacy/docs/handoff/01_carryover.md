# 下放包 01 — 跨 feature 條文承接 + Privacy 基準確認

分析層 → 執行層。2026-08-13。

**背景**：`features/` 下現僅存 `privacy/`，其餘五個 feature 目錄與 repo 根
`output/` 已不在。分析層不對其原因作任何陳述（未量測）。本包之作用是把
**仍然生效且與 Privacy 有關**的跨 feature 條文承接過來，其餘一律不追。

原擬之下放包 08（AMFM 路徑）因目錄不存在未能寫入，內容中與 Privacy 無關
之部分（Media 基準爭議、AMFM/Home/SXM 之歷史比對）**全部作廢，不再處理**。

---

## 1. 承接條文（分析層自裁，即時生效）

```text
[RULING] R22 — 稽核方法學條文（承接自 07 包上繳，2026-08-13）

R22-1  稽核之時態限縮
  裁：hash 稽核只證明「現在相符」，不證明「從未被覆寫」。
      若某檔曾被覆寫、隨後又自客戶樹重新複製，稽核仍顯示 MATCH。
      結論措辭一律限縮為現在式。
  §5a：**稽核之時態必須寫明。「相符」是現在式陳述，「未曾發生」是
      完成式陳述，前者不蘊含後者。**

R22-2  NO_COUNTERPART 之語意
  裁：其嚴格語意為「客戶樹內**無同名檔**」，**不等於「無對應檔」**。
      先例：SXM 之 inputs 工作簿 SHA256 cd876c202c71… 即 FW036 空白
      範本，對應檔存在但客戶樹內檔名為 …_SWQT_20260121.xlsx，
      basename 索引不命中。
  §5a：**以檔名為索引之比對，其陰性結果只能陳述索引層事實，
      不得升格為內容層結論。**

R22-3  白名單 vs 既存違規清單之性質差異
  裁：`tests/` 之寫回路徑 ratchet 測試中，白名單為**永久豁免**，
      既存違規清單為**待清償之債**。
      入白名單需要「確定不會成為交付件」之積極證據；
      不確定者一律留在債務清單（誤列於此之代價僅為多修一處）。
      `scripts/translate_xlsx.py` 與 `backend/api_server.py:2370`
      維持 ACTIVE，不入白名單。

R22-4  ratchet 測試之三項實作 —— 追認
  (a) 雙向約束（清單陳舊亦 FAIL）：追認。無人修剪之 baseline 會停止
      代表任何東西。
  (b) 以每檔呼叫數而非行號為判準：追認。封存標頭使行號位移。
  (c) 作業順序得由執行層調換並回報：追認，立為通則 ——
      **下放包所列作業順序若與其內容之相互影響衝突，執行層得調換
      並於上繳包載明。**

R22-5  分類承載因果假設時之處置
  裁：當分類架構本身承載因果假設時，**未經裁定不得施加分類**；
      應停在原始量測並回報。
```

---

## 2. Privacy 基準確認（唯讀，必辦）

`features/privacy/inputs/` 現為本 repo **唯一**尚存之 inputs 目錄，
其基準完整性直接決定 Privacy 全部後續比對之可信度。

作業：

1. 對 `features/privacy/inputs/` 全部檔案計 SHA256
2. 與客戶樹對照，範圍**限定** `/Users/peihe/Work/02_Project_R1LR/` 之下
3. 逐檔列出**所有**同名候選及其 SHA256（依 A-PV04：同名檔內容可不同）
4. 判定四類並分開計數：
   `MATCH` / `OUT_OF_TREE`（僅命中該樹外）/ `NO_MATCH` / `NO_COUNTERPART`
5. 結論措辭套用 R22-1 之現在式限縮；`NO_COUNTERPART` 之說明套用 R22-2
6. 已知先例可直接引用，不必重測：V6_R2 之 SHA256
   `49dd3c31405fb0c34d4cf11048d325d6f047c0d333c10248310e72c57e194fbb`

**全程唯讀** —— 不得移動、改名、覆寫、刪除任一檔案。

---

## 3. 待 Pei 裁定（一項）

```text
[PENDING RULING] R22-6 — backend/api_server.py:2410 就地覆寫

事實：該行 wb.save(source_path) 以 openpyxl 存檔路徑覆寫**來源檔本身**。
      07 包稽核觸發停手條件，程式碼未改。

現況變化使其升級：features/privacy/inputs/ 已是本 repo 唯一之
      inputs 目錄。該路徑一旦被觸發於其中任一檔（FW036 空白範本、
      V6_R2、037、CFTS022、SYSAD 等），Privacy 之基準即毀，
      且無從察覺 —— 基準與受測物一起被改。

建議：改為寫入新檔並回傳新路徑，永不覆寫來源。
      **界定**：本項解除的是「基準被毀」之危害，**不解除**該路徑
      仍以 openpyxl 存檔所產生之「輸出受損」缺陷；後者續留 ratchet
      債務清單，不得藉本項順手改為 xlsx_surgical。

裁：[ ] 照建議  [ ] 暫不動，僅登記  [ ] 其他：____
```

未簽署前，執行層**不得**修改 `backend/api_server.py`。

---

## 4. 執行層作業

1. 貼入 §1 全文至 `features/privacy/RULINGS.md`（R22；占用則停手回報）
2. 執行 §2 之基準確認，結果寫入上繳包
3. 更新 `features/privacy/PLAYBOOK.md` §6 狀態板 —— 現況 P0/P1 已完成
   而狀態板七列全為未勾選，`workbook_state`、`leaves`、`targets`、
   `Open PENDING` 各欄皆空。依 `RECON.md` 與 `INTAKE.md` 之實測值填入，
   **不得以本包或記憶為來源**
4. §3 待簽前不動 `backend/api_server.py`

**不做**：不追查已消失之 feature 目錄、不重建任何歷史往返包、
不執行任何 git 操作、不動 canon 本體。

---

## 5. 停手條件

1. `RULINGS.md` R22 編號已占用 → 停止貼入 R22，續行第 2–3 項
2. §2 出現任一 `NO_MATCH` 或 `OUT_OF_TREE` → **停止第 3 項狀態板更新**，
   續行回報。理由：基準未驗明時，狀態板不應宣告 P1 完成
3. §2 之比對需要任何寫入權限 → 停止該項並回報

---

## 6. 上繳包要求

寫入 `features/privacy/docs/upstream/01_carryover.md`，須含：

1. §2 之逐檔表與四類計數彙總，含量測條件逐項
2. §4.3 狀態板更新後之全文
3. **「本包是否仍有該驗而未驗者」之獨立判斷** —— 不得省略

---

## 7. 本包產生之新條文清單（自檢表）

- [x] R22-1 稽核時態限縮 —— §1，區塊形式
- [x] R22-2 NO_COUNTERPART 語意 —— §1，區塊形式
- [x] R22-3 白名單 vs 債務清單 —— §1，區塊形式
- [x] R22-4 ratchet 三項追認 + 作業順序得調換 —— §1，區塊形式
- [x] R22-5 分類承載因果假設時停在原始量測 —— §1，區塊形式
- [ ] R22-6 api_server.py:2410 —— §3，**未簽署**
