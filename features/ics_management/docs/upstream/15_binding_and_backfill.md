# 上繳包 15 — 世代錯配、綁定與讀者、12 處回填（2026-08-30）

對應下放包：`docs/handoff/15_binding_and_backfill.md`
**開工時重測之 sha256（R-ICS17(e)）＝ `b5a9dad86adc783fe0758772c211f57e66b171d9c13f9b37186ba31467ea6571`**
—— 與執行層自身記錄相符，未停。

**本包 git 執行次數 0**（含唯讀）。
**【E26 觸發】** —— 依令**未進入作業 B（綁定）與作業 C（回填）**。
E28／E29／E1／E9／E18 未觸發；E27／E30 因 B／C 未執行而**未到判定點**。

**故：本包未綁定 BHCAN2、未建立 `verify_reference_binding.py`、未回填任何佔位。**

---

## §1 裁決指紋＋前提驗證＋圍籬 diff

### 1.1 前提驗證 —— **P1～P4 全部相符**

| # | 前提 | 實測 | 判 |
|---|---|---|---|
| P1 | 相異 46、`## R-ICS` 53 行；無重複條號 | **相異 46、53 行**；無 DUPLICATE | 相符（**E18 未觸發**）|
| P2 | A-ICS 至 99、相異 99、無缺口；DR 21／21 | **A-ICS99**／99／無缺口；**DR 21**／21／無缺口 | 相符 |
| P3 | `holder: analysis-A`、`released: null` | 同 | 相符 |
| P4 | 圍籬 diff 新增 `R-ICS45`＋`R-ICS46` 二條、刪除 0 | **新增二條、刪除 0** | 相符 |

**A-ICS99 之拿法（預期數自快照時點取得）本次生效** —— P4 首度相符。

### 1.2 圍籬 diff（對 `14_rulings_snapshot.md`，未動用 git）

快照自證：檔頭載值＝本文實算，**相同**。
diff **新增 110 行、刪除 0 行**；新增 `##` 標題 **`R-ICS45`、`R-ICS46`**；刪除 **無**。

---

## §2 作業 A — 世代錯配（`docs/reports/15_dbc_generation_diff.md`）

### 2-1 【E29 未觸發】dbc 確為四支

自 `forms/` 與各 feature `inputs/` 全樹重建，無第五支。
四支之 sha256（前 16）：A `46cb73f3db62ac9f`／B `9ef1ec9830fc8018`／
C `51c8fd6092925071`／D `2a86c4bf3e670d71`。

### 2-2 已用訊號全集 —— **17 個**（自列舉長度取得）

`Radio_btn0~4`（5）、`Radio_Knob1_DIR/VAL`、`Radio_Knob2_DIR/VAL`（4）、`RQ_DISP_INTS`、
`DCSD_DISP_STAT`／`DCSD_Enter`／`DCSD_Screen_Off`／`DCSD_VOLKNOB_DIR`／`DCSD_VOLKNOB_VAL`（5）、
`TGW_DISP_STATSts`、`PowerSts_Telematic`。

### 2-3 【核心結果】世代之間唯一的差異在發收方

| 比對面 | 結果 |
|---|---|
| 起始位元 | **17／17 全部相同** |
| 長度 | **17／17 全部相同** |
| 位元組序、factor／offset、值域、單位 | **17／17 全部相同** |
| **`VAL_` 列舉** | **17／17 逐字相同** |
| 發方／收方 | **17／17 全部不同** |

**下放包 §7 所慮之風險經量測不成立**：
「位元佈局或值域悄悄不同」→ **不成立**；
「回填的卻是錯的值，而且錯得看不出來」→ **不成立**。

發收方之差為閘道轉發之必然：B 檔 `ICS`／`DCSD` 發 → `SGW`；A 檔 `SGW` 轉發 → `ETM,LTM`。
**同一條鏈之上下游兩段，非矛盾。**

### 2-4 差異分類（四類）

| 類 | 實數 |
|---|---|
| 四支一致 | **0** |
| 僅 R1 與 R4／R5 有差 | **0** |
| 僅存於 BHCAN 族（A、B），FDCAN 族查無 | **15** |
| 四支皆載，承載訊息隨匯流排而異（`TGW_DISP_STATSts`、`PowerSts_Telematic`）| **2** |

**分類名稱之更正並具名**：機械分桶原將 15 個標為「僅存於某世代」，**該標籤誤導** ——
它們同時存在於 A（R1）與 B（R4）二支 BHCAN，只是不在二支 FDCAN。
**差別在匯流排族，不在世代。**

### 2-5 【E26 觸發】受影響之既有 TC

| 面 | 受影響條數 |
|---|---|
| **交付欄**（`pre_conditions`／`input_test_data`／`test_procedure`／`expected_result`）| **0 條** |
| **`reasoning`** | **3 條** |

**交付欄 0 條之依據**：31 條之交付欄**無一條斷言任何訊號之發送或接收節點**，
寫法一律為 `Read the signal $MESSAGE.Signal$ ... on the CAN trace`；
而位元佈局與 `VAL_` 四支相同，故其訊號名、位元位置、值與標籤在任一世代上皆成立。

**`reasoning` 3 條之依據**：`b03` 之三條逐字載

> `SG_ RQ_DISP_INTS : 55|8@0+ (0.5,0) [0|100] "%" DCSD`，**發送節點為 SGW 而非 ICS**（已具名，見 upstream-04 §三）

該行末 `DCSD` 為收方，與「發送節點為 SGW」合起來**逐字取自 B 檔（R4_BHCAN）**。
而於 Pei 所裁之 A 檔，`RQ_DISP_INTS` 為 **`ETM`→`SGW`**
—— **發送節點是 ETM（本 DUT 側）不是 SGW，收方是 SGW 不是 DCSD。**

### 2-6 E26 之判定 —— 依字面停下，但性質與下放包設想不同

| | 下放包 §7 所慮 | 實測 |
|---|---|---|
| 位元佈局悄悄不同 | 擔心 | **不成立**（17／17 相同）|
| `VAL_` 悄悄不同 | 擔心 | **不成立**（17／17 逐字相同）|
| 交付欄之值填錯而看不出來 | 擔心 | **不成立**（受影響 0 條）|
| **`reasoning` 引述之 DBC 原行過時** | **未預料** | **成立（3 條）** |

**執行層依 E26 字面停下，未進入 B／C，且不自行判斷此差異是否足以放行。**

**供分析層裁決之判斷材料**：E26 之立法理由為
「綁定與回填皆以**訊號解析正確**為前提」。**訊號解析經量測正確**
（位元佈局、值域、`VAL_` 四支相同；交付欄受影響 0 條）。
實際觸發者是 `reasoning` 中一行引述過時，性質不同且較輕。
**若分析層認為立法理由已滿足，作業 B／C 可於下一包直接執行，本包之量測不需重做。**

---

## §3 作業 B、C —— **未執行**（E26）

| 作業 | 狀態 |
|---|---|
| B：綁定 BHCAN2 ＋ 建 `verify_reference_binding.py` ＋ 納入 gate | **未執行** |
| C：12 處佔位回填 | **未執行** |

故：`feature.yaml` `reference:` 仍 **10 鍵**（未變 11）；`forms/FORMS.md` **一字未改**；
佔位仍 **18 處／14 條**；TC 仍 31 條；錨變動 0。
**E27（既有 10 鍵 sha 是否相符）與 E30（`(supporting observation)` 之改動）未到判定點。**

**禁區自證**：`feature.yaml` sha `ea6ed546d8a8aa19c5fa…`、`FORMS.md` sha `e127ca66826f1aede3a1…`
—— 與開工前相同。`features/display/`／`features/vehicle_setting/` **變動 0 處**。

---

## §4 作業 D — TC 2／TC 4 之前提建立法（`docs/reports/15_tc2_tc4_precondition.md`）

### 4-1 【E28 未觸發】二條之錨並非 `4819144`／`4820117`

| TC | 錨 | 主詞 |
|---|---|---|
| TC 2 `Power hardkey pressed at Telematic Power full operation` | `CFTS020-4819561` | **HU** |
| TC 4 `Power hardkey pressed at Telematic Power idle` | `CFTS020-4819564` | **HU** |

主詞為 `HU`（本 DUT）而非 `ICS`（面板）→ **E28 未觸發**。

### 4-2 前提之建立法 —— **第三種：根本未寫**

下放包所列三種可能之實測結果：

| 可能 | 判 |
|---|---|
| 以 CAN 輸入餵入 | **否** —— TC 2 六步、TC 4 五步逐步核對，**無任何一步提及 `$Telematic_Power$`** |
| 以電源模式／PROXI／其他設定建立 | **否** —— `pre_conditions` 未載建立法，`input_test_data` 為 `NA` |
| **根本未寫如何建立** | **是** |

`$Telematic_Power$` 於二條中各出現 **1 次**，皆在 `pre_conditions` 第 3 項，
且為狀態陳述（`is in the "Full_Operation" state`），無對應建立步驟。

### 4-3 故 A-ICS91 之「餵不進去」**可能不是本二條之問題**

A-ICS91 預設了「前提是以 CAN 餵入建立的」。**實測顯示二條從未指定任何建立方法。**

- 若日後指定為「以 CAN 餵入」→ A-ICS91 成立，二條需改。
- 若指定為其他方法 → A-ICS91 **與本二條無關**，二條只是缺一項建立說明。
- **現況：未指定，二者皆未定。**

**執行層不推定何者為真**，亦未查 IN §4.4 對 Pre-Condition 是否須附建立步驟之規定
（列為未驗項）。

### 4-4 【對 upstream-14 之更正】A-ICS91 之論據所引者非本二條之錨

upstream-14 §3-3 以 `4819144`／`4820117`（主詞為 ICS 面板、內容為 Diagnostics 之啟閉）
論證「規格說 ICS 接收 `$Telematic_Power$`」。**實測：該二物件於 31 條之
`specification_reference` 中命中 0 —— 未被任何 TC 引為錨。**

本二條之 `$Telematic_Power$` 來自其錨 `4819561`／`4819564` 之**條件子句**，主詞為 HU，
**不是「ICS 依其啟閉 Diagnostics」那條需求**。
upstream-14 未作此區分，**於此具名更正**。A-ICS91 之論據需重新指認。

### 4-5 另二項

- 二條之 `$Telematic_Power$` 為**裸符號**，非 IN §8.7.5(a) 之 `$MESSAGE.Signal$` 式，
  與同檔 `$CLIMATIC_PANEL.Radio_btn0$` 之寫法不一致。**只列不改**。
- 二條之 `pre_conditions` 第 2 項為
  `A CAN trace tool is connected and able to log the ICS button status messages`
  —— **只提 log（讀），未提 inject（寫）**。台架能力面本身即未涵蓋餵入。

---

## §5 作業 E — 常設自檢集

| 項 | 結果 |
|---|---|
| 圍籬 diff | **+110／−0**；新增 `R-ICS45`、`R-ICS46` 二條（**P4 相符**）|
| 候選篩 | 原始 **140**／殘餘 **66**／**殘餘率 47%**（前六包 53／53／43／52／47／47%）|
| 未錨定斷言 | **3＋6**，不變 |
| `selfcheck_b01.py` | PASS —— 機檢 19 項 FAIL 0 |
| `verify_verbatim_b01.py` | PASS —— **31／31** |
| `pending_census.py` | **18 處／14 條**（未回填）|
| `ledger_guard.py` | 前後 exit 0，**逐字相同** |
| gate | **仍為四支**（第五支 `verify_reference_binding` 因 B 未執行而未建）；前後**逐字相同**，差皆 0 |
| 快照 | `docs/reports/15_rulings_snapshot.md` **已產出**，53 錨點 |

開工前 gate 基線為 5 支紅（含 `lint_docs036`，成因在 `features/power/`，非本包所致，**未修**）。

---

## §6 預期數字對照（下放包 §5，20 項）

| # | 項 | 預期 | 實測 | 判 |
|---|---|---|---|---|
| 1 | `ledger_guard` 開工前 | exit 0；53 行（相異 46）、A-ICS 99、DR 21／21 | 全數相符 | 相符 |
| 2 | 圍籬 diff | 新增二條、刪除 0 | **新增二條、刪除 0** | 相符 |
| 3 | 已用訊號全集 | 實測值 | **17 個** | 相符 |
| 4 | 差異分類四類 | 各有實數 | 0／0／15／2 | 相符 |
| 5 | 受影響之既有 TC | **0**（非 0 → E26 停）| **交付欄 0、`reasoning` 3** | **不符 → E26 觸發** |
| 6 | `feature.yaml` 鍵數 | 10 → 11 | **10（未變）** | **未執行（E26）** |
| 7 | `FORMS.md` 變動 | 僅一列一欄 | **0 處** | **未執行（E26）** |
| 8 | `verify_reference_binding` 首跑 | 11／11 | **未建立** | **未執行（E26）** |
| 9 | `display`／`vehicle_setting` 變動 | 0 處 | **0 處** | 相符 |
| 10 | 回填處數 | 實測值 | **0** | **未執行（E26）** |
| 11 | 回填後佔位總數 | 實測值 | **18（未變）** | **未執行（E26）** |
| 12 | `verify_verbatim` | 31／31 | **31／31** | 相符 |
| 13 | TC 總數 | 31 | **31** | 相符 |
| 14 | Test Set 相異值 | 5 | **5** | 相符 |
| 15 | 錨變動 | 0 處 | **0 處** | 相符 |
| 16 | 作業 D | 二條全文；三分之一；錨逐條列 | 齊；**第三種**；錨為 `4819561`／`4819564` | 相符 |
| 17 | 候選篩 | 二數並報＋殘餘率 | 140／66／**47%** | 相符 |
| 18 | 五支 gate | 差皆 0 | **四支**（第五支未建）；差皆 0 | **部分（因 E26）** |
| 19 | **git 執行次數** | **0** | **0** | 相符 |
| 20 | 快照 | 已產出 | **已產出** | 相符 |

**11 項相符、1 項不符（#5，即 E26）、6 項因 E26 未執行、1 項部分。**

---

## §7 未結 DR 清單（21 條）

| DR | 現況 | 本包新事實 |
|---|---|---|
| DR-ICS1／3／11／14／17／19／21 | OPEN | — |
| DR-ICS2 | OPEN | B5 所繫 |
| DR-ICS4 | OPEN | 1 處佔位 |
| DR-ICS5／7／10／15 | 可結 | — |
| DR-ICS6 | OPEN | 5 處佔位 |
| **DR-ICS8** | OPEN | **12 處佔位未回填（E26）**；惟其回填所需之訊號解析經本包量測**確認正確** |
| DR-ICS9 | OPEN | V1／V2／V3 所繫；**無佔位故不會自行浮出** |
| DR-ICS12 | 追蹤件 | — |
| DR-ICS13 | 分析層已標「可結」 | 執行層未動 |
| DR-ICS16 | 匯流排軸已結（BHCAN2）| 綁定未執行（E26）|
| DR-ICS18 | 告知／追認件 | — |
| DR-ICS20 | OPEN | G2／G3 效力所繫 |

---

## §8 結果三分法

| 分類 | 項 |
|---|---|
| **改對了** | `15_dbc_generation_diff.md`／`15_tc2_tc4_precondition.md` 二份報告；`15_rulings_snapshot.md` 回凍基準快照 |
| **核實無誤** | 四支 dbc（無第五支）；已用訊號 17 個；**位元佈局／值域／`VAL_` 四支 17／17 相同**；交付欄受影響 0 條；TC 2／TC 4 之錨為 `4819561`／`4819564` 且主詞為 HU；`4819144`／`4820117` 未被任何 TC 引為錨 |
| **正確地不動** | **未綁定 BHCAN2、未建讀者、未回填任何佔位、未納 gate**（E26 停）；`feature.yaml`／`FORMS.md` 一字未改（sha 自證）；`features/display/`／`features/vehicle_setting/` 0 處；TC 2／TC 4 一字未改；未改 `reasoning` 中過時之引述行（不在本包授權內）；未修 `features/power/` 之 gate 紅；未對任何 DR 結案；五簿一字未寫；**git 0 次** |

---

## §9 建議登錄之 anomaly（編號由分析層取）

1. **【E26 之實體】三條 b03 之 `reasoning` 逐字引述 R4 檔之 DBC 原行，
   該引述於裁定之 BHCAN2 上不正確**（`RQ_DISP_INTS` 實為 `ETM`→`SGW`，非 `SGW`→`DCSD`）。
2. **【對 upstream-14 之更正】A-ICS91 之論據所引之 `4819144`／`4820117`
   並非 TC 2／TC 4 之錨，且未被任何 TC 引用。** A-ICS91 需重新指認論據。
3. **TC 2／TC 4 之 `$Telematic_Power$` 前提未寫任何建立方法** ——
   A-ICS91 之「餵不進去」因此可能與本二條無關。
4. **世代之間唯一差異在發收方**：位元佈局與 `VAL_` 四支 17／17 相同 ——
   A-ICS93 所慮之數值面風險**不成立**，建議改述。
5. **15 個訊號之差別在匯流排族（BHCAN vs FDCAN），不在世代** ——
   「世代錯配」之命名本身可能誤導。
6. **已綁之 R5_FDCAN8 對本線 17 個已用訊號只貢獻 2 個**，其餘 15 個全來自 R4_BHCAN。
7. `Radio_btn0` 於 A 檔收方**僅 `LTM`**（其餘八個為 `ETM,LTM`），意義未查。
8. TC 2／TC 4 之 `$Telematic_Power$` 為**裸符號**，非 `$MESSAGE.Signal$` 式。
9. 二條之 `pre_conditions` 只載 CAN trace 之 **log** 能力，未載 **inject** 能力。

**本包未產生任何新裁決條文，未自取任何編號。**

---

## §10 獨立判斷

### 10-1 本包是否仍有該驗而未驗者 —— **有，三項**

1. **其餘 29 條之 Pre-Condition 是否同樣「只陳述狀態、不寫建立法」未查。**
   若普遍如此，§4-2 之發現不是 TC 2／TC 4 所獨有，而是全批之體例問題。
2. **IN §4.4 對 Pre-Condition 是否須附建立步驟之規定未查** ——
   未查則無法判「未寫建立法」是否為缺陷。
3. **四支 dbc 之全檔差異仍未盤點**（本包只比 17 個已用訊號）。
   upstream-14 §10-1 已列此項，本包仍未做。

### 10-2 【下放包 §6 指定】DR-ICS8 是否可標「可結」（只建議不裁）

**建議：不可標可結，但其阻因已從「缺件」降為「程序」。**

**理由**：DR-ICS8 之缺件是 `TGW_DISP_STAT` 之訊號實體。該缺件**實質上已補齊** ——
b14 判定同一物（`TGW_DISP_STATSts`＠`BO_ 1500 TELEMATIC_DISPLAY2`），
b15 復以四支 dbc 交叉確認其位元佈局與 `VAL_` 在任一世代上皆相同。
**知識面已完備，回填之技術前提全數齊備。**

**但 12 處佔位一處未回填**（E26 停），故 DR-ICS8 之**實際效果尚未產生**。
標「可結」而佔位仍在，會使 `pending_census` 與 DR 台帳互相矛盾。

**殘餘之處與所繫**：

| 殘餘 | 處數 | 所繫 |
|---|---|---|
| 不涉 TC 2／TC 4 之 `$TGW_DISP_STAT$` 佔位 | **實測待作業 C 執行時確定**（本包未執行 §3 之複驗步驟）| 僅繫於 E26 之放行 |
| TC 2／TC 4 所佔者 | 同上 | 繫於 A-ICS91 之重新指認（§4-4）與前提建立法之指定（§4-2）|

**建議之路徑**：分析層若認 E26 之立法理由已滿足（§2-6），
下一包直接執行作業 B／C，回填後 DR-ICS8 即可裁可結；
TC 2／TC 4 之二處則依 §4 之新事實另行處置 —— **它們的阻因已不是「餵不進去」，
而是「從未寫過怎麼餵」**，後者是可以在不解 A-ICS91 的情況下先寫清楚的。

---

## §11 引用清單

R-17(a)(c)(e)(f)、R-G40、R-G41(a)（全案）；R-ICS1 ~ R-ICS46（實測 53 行／相異 46）；
A-ICS72、A-ICS91、A-ICS93、A-ICS94、A-ICS98、A-ICS99；A-DM14；
DR-ICS1 ~ DR-ICS21；R-G13、R-G17、R-G18、R-G25；
FO §8.2、FO §8.4、FO §8.5；IN §4.4（未查，列為未驗）、IN §8.7.5、IN §9、IN §10.7、IN §11。
