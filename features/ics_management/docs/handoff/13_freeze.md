# 下放包 13 — 收尾包：probe 三值化、SYS2 反向掃、凍結記錄（2026-08-30）

## §0 背景與量測時點

upstream-12 已審結：19 項相符、1 項部分不符（#13，根因為執行層 probe 之布林合桶）。
**git 執行 0 次**；快照法首次圍籬 diff 落地，**並抓到下放包 §0 之一項不符**
（正本 `R-ICS40` 非既存，寫於 b11 完工之後）。

**本包為 ICS 線凍結前之最後一包。** 其存在理由是 upstream-12 §11-2 之判斷：
「規格面已到底」之前提「可量之物皆由 CFTS020 到得了」，**已被 §3-5 推翻**
（SYS2 有 23 列在案 FR 無 CFTS020 來源）。凍結前須把這個盲區掃一次，
以及把「87 個」之題目界定清楚——二件皆不依賴上游。

### 本輪分析層之寫入（R-ICS26(b)）

- `RULINGS.md`：**R-ICS42**
- `ANOMALIES.md`：**A-ICS77 ~ A-ICS83**（含分析層之誤三則：A-ICS81、A-ICS83，及 A-ICS77 之未複驗）
- `DATA_REQUESTS.md`：DR-ICS16 補線索（PDT27 dbc）；**DR-ICS18 未動**（87 併問之計畫已撤銷，R-ICS42(c)）

### 對 upstream-12 之裁決（摘要）

| 事項 | 裁 |
|---|---|
| §3-2「87 個」實為 §1.4 × 86 ＋ §1.5 × 1 | R-ICS42(a)；A-ICS74 改述；**probe 三值化**（R-ICS42(b)）|
| §3-1 E15（86／87 為 SYS2 所收）| 依 SYS2 自身分類處置：OoS／Information 者不入驗證範圍；唯一 FR `4819353` 查覆蓋（R-ICS42(d)）|
| §3-4 SYS2 於 Associated 分支只收 §1.18 | **強化 R-ICS39**，採為旁證 |
| §3-5 SYS2 23 列無 CFTS020 來源 | **A-ICS78【重】**；本包作業 B 反向掃（R-ICS42(e)）|
| §4-2 b06 加 §1.18 錨 | 合式，追溯非反噬（R-ICS42(g)）|
| §4-3 第三種表達判 Disassociated | 採為 R-ICS37(a) 旁證，非決定性；A-ICS76 描述三處為分析層之誤（R-ICS42(f)）|
| §4-4 PDT27 dbc 可填 SGW 盲區 | 登記不綁定；本包唯讀量其對 DR-ICS16 之填補（R-ICS42(h)）|
| §1.2 R-ICS40 正本時序 | A-ICS70 更正；已直接問 Pei（R-ICS42(i)）|
| `$Touchscreen_ICS$` 第四路 | 登記不走（R-ICS42(j)）|

### 前提（量測時點 2026-08-30）

| # | 前提 | 驗法 |
|---|---|---|
| P1 | `RULINGS.md`：相異 ruling_id **42**、錨點總數 **49**（七組 v1／v2）；無重複 | `ledger_guard.py` |
| P2 | `ANOMALIES.md` 至 **A-ICS83**、相異 83、無缺口；DR 主登記表 **20 列**、相異 20、無缺口 | 同上 |
| P3 | `ANALYSIS_LOCK.md` `holder: analysis-A`、`released: null` | 同上 |

P1 為分析層自算。不符以實測為準並具名，不停工；重複 ruling_id → E18。

---

## §1 禁區

- **git 全數不執行，唯讀亦不可。** 圍籬 diff 以 `docs/reports/12_rulings_snapshot.md` 為基準。
- **不改任何錨、不生成任何 TC、不動任何 reasoning。** 本包純量測＋工具修。
- **probe 三值化不得改變軸層輸出**（回歸檢查同 b11 作業 C）。
- **`PDT27_E2A_R1_BHCAN2.dbc` 只讀不綁定**：不改 `feature.yaml`、不改 `FORMS.md`。
- **不對 23 列作任何「應否納入」之判斷**（R-ICS42(e)）。
- 不對任何 DR 結案；分析層五簿一字不寫；不自取編號。

---

## §2 裁決引用

**R-ICS42(b)(d)(e)(h)** 為本包主據；R-ICS39、R-ICS38(a)(d)、R-ICS37(a)、R-ICS2 v2、
R-ICS32(c)＋R-ICS34、R-ICS26(a)(b)、R-ICS17(e)(f)、R-ICS29(f)。

---

## §3 作業清單

### 作業 A — probe 三值化與全域三層交叉重算（R-ICS42(b)）

`cfts020_probe` 之 `variant_fits_dut` 改為三值：`Disassociated`／`Associated`／`Unclassified`。

- **回歸**：軸層輸出與 b12 逐字相同；`variant` 欄之值域改變**須逐物件列出**。
- **重算全域三層交叉表**（upstream-11 §4-3 之 138／29／87 全部作廢），
  以三值 × 範圍層重列，每格給實數與所屬章節分佈。
- `4819353`（§1.4 唯一 FR，→ `NRL-52850`）**查現有 31 條之覆蓋**：有／無／部分，逐字引其本文。
  未覆蓋者**登記為覆蓋缺口，不生成**。

---

### 作業 B — SYS2 反向掃：23 列無來源 FR（A-ICS78／R-ICS42(e)）

出 `docs/reports/13_sys2_reverse_scan.md`。

以 SYS2 為起點（**不是 CFTS020**），23 列逐一列：

1. `Document ID`／`Category`／子分類／`SYS2 MD Feedback` 逐字；
2. 其內容屬何層：HW 供應商介面（I2C 位址、觸控訊息時序、電氣參數）／
   HMI 軟體側行為／不可判；
3. 本 DUT（Disassociated、LTM）與該列之關係：該列描述之介面是 HU↔DCSD 之間、
   抑或 DCSD 內部；
4. 是否有任一列為 **HMI 軟體側可驗證之行為**。

**同時擴掃**：SYS2 全表中，來源欄非空但**不指向 CFTS020／CFTS022** 之列
（指向 CFTS019、HMI L&F、或其他），逐列列其來源文件與是否已在 `spec-index/sources/` 內。
這是「掃描起點盲區」之完整盤點，不只 23 列。

**只列不裁。E19**：若有任一列為 HMI 軟體側可驗證之行為，**停下回報**——範圍屬 Pei。

---

### 作業 C — `PDT27_E2A_R1_BHCAN2.dbc` 對 DR-ICS16 之填補程度（唯讀）

出 `docs/reports/13_pdt27_dbc_vs_dr16.md`。

DR-ICS16 之問：本 DUT 於哪條匯流排觀察 `$TGW_DISP_STAT$` 與 `$Telematic_Power$`？

- 該 dbc 中此二訊號之承載訊息、發送節點、接收節點逐字；
- 與二綁定 DBC（B-CAN／CAN-FD）之對照：同一訊號於三 DBC 之發收方矩陣；
- 若本 DUT（`LTM`）於該 dbc 明列為收方，**這是否足以定台架觀察點** —— 只答「足／不足／部分」並給理由，**不採認**。
- 該 dbc 對 b03 之 12 處 `$TGW_DISP_STAT$` 佔位之**潛在回收數**（只估）。

**只讀不綁定。** 綁定待 A-DM14。

---

### 作業 D — 凍結記錄（**本包之交付物**）

出 `docs/reports/13_freeze_record.md`。凍結記錄須讓**下次解凍之人不必重讀十三包**即可接手：

1. **現況數字**：TC 31、錨行總數、佔位 18 處／14 條、Test Set 5、未錨定斷言 3＋6、
   候選篩基線、DR 20 條之狀態分佈。
2. **不可現狀出貨之條**：逐條列（V1／V2／V3、B5 ＋ 本包若有新增），各附其阻因與所繫 DR。
3. **掛帳三件＋本包新增**：
   - §1.4 之 86 物件依 SYS2 分類處置（R-ICS42(d)）；`4819353` 覆蓋結果
   - DR-ICS18 追認風險（上游否認則 009 ＋ 15 條加錨退回）
   - G5 押後（A-ICS60）
   - 23 列反向掃之結果（作業 B）
   - PDT27 dbc 綁定待 A-DM14（作業 C）
   - A-ICS80（R-ICS40 正本來源）待 Pei 答
4. **解凍觸發條件**：DR-ICS16／9／2／20／18 各自回覆後應做之事，逐條對映。
5. **解凍時第一件事**：`ledger_guard` ＋ 圍籬 diff 對本包快照。

---

### 作業 E — 常設自檢集

1. 圍籬 diff 對 `12_rulings_snapshot.md`：**預期新增非 0 行**（R-ICS42），刪除 0 行。
2. 候選篩：二數並報＋殘餘率（前四包 53／53／43／52%）。
3. 未錨定斷言：3＋6，不變。
4. 四支 gate 開工前／完工後；差皆 0。
5. 開工時重測本包 sha256。
6. 完工時存 `docs/reports/13_rulings_snapshot.md` —— **此即凍結基準快照**。

---

## §4 停下回報條件

沿 E1／E9／E18，並：

- **E19**：作業 B 發現任一列為 HMI 軟體側可驗證之行為 → 停下（範圍屬 Pei）。
- **E20**：作業 A 之三值化改變任何物件之**軸層**判定 → 停下（回歸失敗）。
- **E21**：作業 C 發現該 dbc 與二綁定 DBC 對同一訊號之發收方**矛盾** → 停下（不得自行調和）。

---

## §5 預期數字（審查基準）

**凡有列舉者，數字自列舉長度取得。**

| # | 項 | 預期 |
|---|---|---|
| 1 | `ledger_guard` 開工前 | exit 0；錨點 **49**（相異 **42**）、A-ICS **83**、DR **20／20**；無重複 |
| 2 | 圍籬 diff | 新增非 0、刪除 **0** |
| 3 | 錨變動／TC 新增／reasoning 變動 | **0／0／0** |
| 4 | TC 總數 | **31** |
| 5 | 佔位總數 | **18** |
| 6 | 作業 A 軸層回歸 | **逐字相同**；`variant` 值域改變者逐物件列出 |
| 7 | 作業 A 三層交叉表 | 三值 × 範圍層，每格實數；**§1.4 × 86 落於 `Unclassified`** |
| 8 | `4819353` 覆蓋 | 有／無／部分，附逐字 |
| 9 | 作業 B | 23 列逐一四欄；擴掃之非 CFTS020／022 來源列數 |
| 10 | 作業 C | 三 DBC 發收方矩陣；足／不足／部分；潛在回收數 |
| 11 | 作業 D | 凍結記錄五節齊備 |
| 12 | 候選篩 | 二數並報＋殘餘率 |
| 13 | 未錨定斷言 | **3＋6** |
| 14 | Test Set 相異值 | **5** |
| 15 | 四支 gate | 差**皆 0** |
| 16 | **git 指令執行次數** | **0** |
| 17 | 快照 | `13_rulings_snapshot.md` 已產出（凍結基準）|

---

## §6 上繳包要求

`docs/upstream/13_freeze.md`。沿 upstream-12 之節構，並須含：

- §1 裁決指紋（R-ICS1～R-ICS42）＋前提驗證＋圍籬 diff
- 作業 A～E 之結果
- **§n 未結 DR 清單（20 條）**
- **結果三分法**
- **獨立判斷**：凍結記錄是否足以讓不曾讀過十三包之人接手；若不足，缺什麼
- 建議登錄之 anomaly（編號由分析層取）

---

## §7 本包在整條線上的位置

**這是 ICS 的收尾包。** 做完三件不依賴上游的事——probe 修好、盲區掃過、
DR-ICS16 的新線索量過——留一份凍結記錄，然後讓位給 PM 的 389 列。

十三包、31 條、42 條裁決、83 則 anomaly、20 條 DR。
其中最值錢的一件不是任何一條 TC，是 A-ICS78：**掃描起點本身會有盲區**。
這件事要帶去 PM、SU、DD。
