# 下放包 11 — 009 與三缺口生成、NBSP 重跑、變體層 probe（2026-08-29）

> **本檔取代同路徑之前一版**（原「SYS2 收錄面判讀」版）。
> 取代事由：Pei 於 b11 下派後、執行前裁定「**§1.18 之 29 個物件算數**」（R-ICS39），
> 原作業 A 之判讀失去決策用途，依 R-ICS39(i) 撤除。
> **若執行層已依前一版開工，立即停下並改依本版**；已產出之內容不丟棄，隨包上繳。

## §0 背景與量測時點

upstream-10 已審結（14／15 相符，1 項為禁區違規）。本輪落 **R-ICS37**、**R-ICS38**、
**A-ICS62 ~ A-ICS69**、DR-ICS18 再改問、DR-ICS19 新登。

隨後 **Pei 裁定：§1.18 之 29 個物件對本 DUT 算數**（R-ICS39）。
A-ICS63 依（ii）定案並結案。**兩件事就此分層固定**：

| 層 | 結論 | 依據 |
|---|---|---|
| **變體層** | 本 DUT 為 `Disassociated`（Silver Box，外接 DCSD） | 量測（R-ICS37(a)），十項證據脫離二節，46 : 0 |
| **範圍層** | §1.18 之 29 物件**算數**，二節真並存 | **Pei 裁定**（R-ICS39），依 SYS2 之收錄 |

**變體是硬體事實，算數是範圍決定，二者不同層、不互相推導。**
§1.8 仍為變體正解，27 條錨全部有效一條不撤（R-ICS39(b)）。

### 本輪分析層之寫入（R-ICS26(b)）

- `RULINGS.md`：**R-ICS37**、**R-ICS38**、**R-ICS39**
- `ANOMALIES.md`：**A-ICS62 ~ A-ICS69**（A-ICS63 已依 R-ICS39 結案）
- `DATA_REQUESTS.md`：DR-ICS18 降為告知／追認件、**DR-ICS19** 新登、催件排序重排

### 前提（量測時點 2026-08-29）

| # | 前提 | 驗法 |
|---|---|---|
| P1 | `RULINGS.md`：相異 ruling_id **39**、錨點總數 **46**（`R-ICS2`／`19`／`22`／`23`／`25`／`33`／`35` 七者各 v1／v2） | `ledger_guard.py` |
| P2 | `ANOMALIES.md` 至 **A-ICS69**、相異 69、無缺口；DR 主登記表 **19 列**、相異 19、無缺口 | 同上 |
| P3 | `ANALYSIS_LOCK.md` `holder: analysis-A`、`released: null` | 同上 |

P1 為分析層自算。不符以實測為準並具名，不停工。

---

## §1 禁區

- **git 全數不執行，唯讀指令亦不可**（含 `git status`／`diff`／`log`／**`show`**）。
  自證未改檔用 `ls`／`shasum`／`get_file_info`。**對所有並行實例一體適用。**
  圍籬 diff 依 **R-ICS38(a) 快照法**；**本包無前快照，標「無基準，不判」，非停工事由**。
- **既有 27 條之 `specification_reference` 一條不改**（R-ICS39(d)）。
  加錨押後至 b12，其前置為本包作業 B 之 NBSP 重跑。
  **本包量出「應加哪些錨」，但不加。**
- **G5 維持押後、不生成**（阻因 A-ICS60，與 R-ICS39 無關）。
- **不得對 DR-ICS18／DR-ICS19 作結案**；DR-ICS13 於 009 通過自檢後方得標「可結」，
  **不得於生成當下即結**。
- **不得新立 Test Set**：若新 TC 需要現有五個以外之 Test Set，
  **停下回報**（E12），不得逕立 —— Test Set 屬 framework 之 Layer 2（IN §4.1）。
- 分析層五簿一字不寫；不自取 `A-`／`DR-` 編號；`{VF601}` 不納源；
  `$TGW_DISP_STAT$` 12 處佔位維持；08 之腳本與報告不回改。

---

## §2 裁決引用

**R-ICS39(c)(d)(e)(f)** 為本包之主據；R-ICS37(b)、R-ICS38(a)(c)(d)(e)、
R-ICS36(e)(f)、R-ICS2 v2、R-ICS31(a)(b)、R-ICS32(c)＋R-ICS34、
R-ICS26(a)(b)、R-ICS17(e)(f)、R-ICS29(f)；IN §4.1／§4.3.1／§8.4.1／§10.7。

---

## §3 作業清單

### 作業 A — 生成 009 與 G2／G3／G9（**四條，本線八包來第一次新生成**）

#### A-1 先導：009（Back_Button）

母條：`CFTS020-4821704`（行為，主詞 TLM＝本 DUT）＋ `CFTS020-4821681`（LID 清單）。

- **先寫 009 一條，自檢通過後才寫 G2／G3／G9。** 這是先導閘（IN 之 pilot 精神），
  不另設回報點 —— 但若 009 之自檢有任一項 FAIL，**停下回報**（E13）。
- `specification_reference` 依 IN §10.7：一 ObjectID 一行、ID 升序、前綴逐行重述。
  **本條之錨為 §1.18 家族，不併列 §1.8**（§1.8 無 Back_Button 之判適用母條）。

#### A-2 G2、G3、G9

依 upstream-09 之缺口清點與 upstream-10 §3-3 之判定。
**G3 之 `Tbutton` 取 `100 msec`**（R-ICS36(f)，來源為 §1.8 定義塊）——
**reasoning 須註明其來源為 §1.8 而非 §1.18**（R-ICS35 v1(e)）。

#### A-3 四條共通之硬性要求

- `test_item` **必含下半括號**（IN §4.3.1）：上半為需求原句 verbatim（≤ 50 token），
  下半為測試目的／情境標籤，獨立成行 `(...)`。**缺括號下半即 FAIL，不得出**。
- 缺件一律 `PENDING: DR-{n} <缺件名>`，**不得留空、不得填 NA**（IN §8.4.3）；
  新增之 PENDING 須計入本包之佔位數並具名。
- 訊號寫法依 IN §8.7.5 v3：`$MESSAGE.Signal$ = <raw> (<label>)`；
  DBC 查無者依 (d)(g) 保留來源名稱、登 DR。
- Input Test Data 以 `NA` 為常態（R-1 v2）。
- 尾句號、雙引號、Design Method、Priority 依 IN §11／§12／§10.2。

---

### 作業 B — NBSP 重跑與加錨集合之重測（**b12 加錨之前置**）

出 `docs/reports/11_s118_coverage_gap_rerun.md`。

1. **NBSP 正規化**（R-ICS38(c)）：新寫後繼腳本，**不回改 `s118_compare_08.py`**。
2. **重跑 09 之覆蓋清點**，**二數並報**：09 原判（有覆蓋 9／部分 4／無覆蓋 5）
   與重跑後之判；逐條標出判定改變者，特別是 `4821702` 所涉之
   b04-06／b05-01／b05-02。
3. **重測加錨集合**：於「二節皆有母條」之判準下（R-ICS39(c) 並列雙錨），
   逐條列出 **27 條中哪些應加 §1.18 之錨、加哪一個 ObjectID**。
   複驗 08 之錨層 11／4 → 13／2 與 TC 層 13／7。
   **只列不加**（R-ICS39(d)）。輸出須為 b12 可直接執行之逐條指示表。
4. **本款不及 `test_item` 上半之 verbatim**（R-ICS31(b) 不變）。

---

### 作業 C — probe 加變體層（R-ICS38(d)／A-ICS67）

`cfts020_probe` 之適用性判定分列為**軸層**與**變體層**二旗標，不得合併。

- 回溯報 §1.18 之 29 物件於二層下之判定：
  **軸層＝適用、變體層＝Associated 分支** —— 而依 R-ICS39 其**範圍上仍算數**。
  故第三個欄位為**範圍層**，值取自裁決（`R-ICS39`），不由 probe 推導。
- **三層須分列，不得合併為單一旗標。** 這正是 A-ICS63 之教訓所在。
- **軸層輸出不得改變**：回歸檢查，除新增欄位外應與 b10 逐字相同。

---

### 作業 D — CFTS022 之變體軸量測（R-ICS38(e)／A-ICS68）

出 `docs/reports/11_cfts022_variant_axis.md`。

CFTS022 是否亦分 Associated／Disassociated 軸？7 條純 CFTS022 錨之 TC
（b01-01~05、b06-01／02）是否受影響？

量測：CFTS022 屬性軸值域全列；`Associated`／`Disassociated`／`Silver Box`／`DCSD`
之全文出現與逐字；該 7 條所錨之 6 個物件
（`4914956`／`57`／`58`／`74`／`75`／`76`／`4914993`）逐一之變體面判定。

**只列不裁。** 若判「受影響」，**停下回報**（E10），不得改錨。

---

### 作業 E — 常設自檢集

1. **候選篩**（R-ICS34）：二數並報＋殘餘率＋衍生載具詞逐個印出。
   **本包為 R-ICS34(c) 三包計數之第三包** —— > 60% 須具名並提重議。
2. **未錨定斷言人工複核**（R-ICS32(c)）：現況 3（弱驗證）＋6（已標明）；
   **新增之四條須一併納入複核**。
3. `selfcheck_b01.py`、`verify_verbatim_b01.py`、`pending_census.py`、
   `ledger_guard.py`：開工前／完工後各一跑；四支 gate 差值報出。
4. 開工時重測本包 sha256（R-ICS17(e)）。
5. **快照法首次落地**（R-ICS38(a)）：完工時存 `docs/reports/11_rulings_snapshot.md`。
   **本包不作圍籬 diff**（無前快照）。

---

## §4 停下回報條件

沿 E1／E3／E4，並：

- **E9**（R-ICS38(b)）：**遇條文互斥一律停下回報，不得自行擇一。**
- **E10**：作業 D 判 CFTS022 之 7 條受變體軸影響 → 停下。
- **E12**：新 TC 需要現有五個以外之 Test Set → 停下，**不得逕立**。
- **E13**：009 之自檢有任一項 FAIL → 停下，**不續寫 G2／G3／G9**。
- **E14**：作業 B 若重測出**與 08 之 13／7 不同之 TC 層數字** → 停下回報，
  不得逕以新數字作為 b12 之加錨基礎。

---

## §5 預期數字（審查基準）

| # | 項 | 預期 |
|---|---|---|
| 1 | `ledger_guard` 開工前 | exit 0；錨點 **46**（相異 **39**）、A-ICS **69**、DR **19／19** 無缺口 |
| 2 | 圍籬 diff | **「無基準，不判」**（快照法首包），非停工事由 |
| 3 | **新生成之 TC** | **4 條**（009、G2、G3、G9）|
| 4 | **既有 27 條之 `specification_reference` 變動** | **0 處** |
| 5 | TC 總數 | **27 → 31** |
| 6 | 四條之 `test_item` 括號下半 | **4／4 齊備**，且括號內容互不逐字相同 |
| 7 | `verify_verbatim` | **31／31** |
| 8 | Test Set 相異值 | **5**（不變）—— 若須新增，E12 停下 |
| 9 | `$TGW_DISP_STAT$` 佔位 | **12**（不變）|
| 10 | 全批佔位總數 | **17 ＋ 本包新增之 PENDING 數**（逐處具名）|
| 11 | 作業 B | 二數並報；判定改變者逐條列；**加錨指示表為 b12 可直接執行之形式** |
| 12 | 作業 C | **三層分列**（軸層／變體層／範圍層）；軸層與 b10 逐字相同 |
| 13 | 作業 D | 6 個 CFTS022 物件逐一之變體面判定 |
| 14 | 候選篩 | 二數並報＋殘餘率；**第三包計數** |
| 15 | 未錨定斷言 | 3＋6 ＋ 新四條之複核結果 |
| 16 | `ledger_guard` 完工後 | exit 0；與開工前逐字相同 |
| 17 | 四支 gate | 差**皆 0** |
| 18 | **git 指令執行次數** | **0**（含唯讀）|
| 19 | 快照 | `docs/reports/11_rulings_snapshot.md` 已產出 |

---

## §6 上繳包要求

`docs/upstream/11_generation_and_rerun.md`。沿 upstream-10 之節構，並須含：

- §1 裁決指紋（R-ICS1～R-ICS39，七組 v1／v2）＋前提驗證
- 作業 A～E 之結果；**四條 TC 之全文**
- **§n 未結 DR 清單（19 條）**
- **結果三分法**
- **獨立判斷**：本包是否仍有該驗而未驗者；
  以及 —— **009 之錨是否應併列 §1.8 之任何物件**（只建議不裁）
- 建議登錄之 anomaly（編號由分析層取）

---

## §7 本包在整條線上的位置

**八包以來第一次真的寫 TC。**

錨層在 b10 收斂（27 條 0 變動，終局）；範圍層在本輪由 Pei 裁定（§1.18 算數）。
兩層都定了，生成才有依據 —— 009 從 b03 卡到現在，母條就是 `4821704`，
它一直在那裡，缺的是「§1.18 算不算數」這個裁定。

**風險已具名**（R-ICS39(h)）：上游若否認 SYS2 之收錄，009 與 b12 之加錨都要退回。
但那是一件可回復的事，而繼續等是不可回復的時間。
