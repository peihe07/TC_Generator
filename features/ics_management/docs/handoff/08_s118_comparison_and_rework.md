# 下放包 08 — §1.18 對比、005 生成、if-any 改寫（2026-08-29）

## §0 背景與量測時點

upstream-07 已審結，八項作業全數收下。b06 之 E1 停工判為正確（A-ICS45 為分析層之誤）。
分析層即裁 **R-ICS19 v2／R-ICS25 v2／R-ICS31／R-ICS32／R-ICS33**；
`R-ICS19`、`R-ICS25` 依 R-ICS17(d) 改題為 `v1` 並加註記（**註記寫於圍籬外**，依 R-ICS19 v2(a)）；
登 **A-ICS46～A-ICS50**；DR-ICS13／16／17 三條改述（見 §3-0）。

### 本輪分析層之寫入（R-ICS26(b)）

`RULINGS.md`（R-ICS19 v1/v2、R-ICS25 v1/v2、R-ICS31、R-ICS32、R-ICS33）、
`ANOMALIES.md`（A-ICS46～50）、`DATA_REQUESTS.md`（DR-ICS13／16／17 之狀態欄）。

**MCP 中斷之具名**：本輪分析層寫入期間 MCP 逾時二次。R-ICS32／R-ICS33 之首次寫入
**未落**（以 `get_file_info` ＋ 尾端實測確認最後一條為 R-ICS31 後才補寫），**無重號**。
此事實依 R-ICS26(b) 具名，並提醒執行層：`ledger_guard` 開工前那次實跑之結果為準。

### 前提（量測時點 2026-08-29 17:2x）

| # | 前提 | 驗法 |
|---|---|---|
| P1 | `RULINGS.md`：相異 ruling_id **33**、錨點總數 **38**（`R-ICS2`／`22`／`23`／`19`／`25` 五者各 v1／v2） | `ledger_guard.py` |
| P2 | `ANOMALIES.md` 至 **A-ICS50**；DR 主登記表 **17 列、相異 17、無缺口** | 同上 |
| P3 | `ANALYSIS_LOCK.md` `holder: analysis-A`、`released: null` | 同上 |

**P1 為分析層自算**（上一輪自算與實測一致，本輪同法）。不符以實測為準並具名，不停工。

## §1 禁區

沿下放包 07 §1，並補：
- **不得改任何既有 TC 之 `specification_reference` 至 §1.18**（R-ICS33(a)）。
- **不得自行指定畫面名**（R-ICS30(b)）；if-any 改寫只動 Pre-Condition 與 ER 結構。
- `$TGW_DISP_STAT$` 12 處佔位**維持**，除非作業 A 於 §1.18 找到適用之母條並經停下回報後另裁。
- 009 **不生成**（R-ICS25 v2(a)）。

## §2 裁決引用

R-ICS33（作業 A）、R-ICS31(c)（作業 B）、R-ICS32(b)（作業 C）、R-ICS32(a)（作業 D）、
R-ICS32(c)（作業 E）、R-ICS2 v2（全部適用性判定）、R-ICS19 v2(b)（sha8 比對程序）。

## §3 作業清單

### 作業 A — §1.8 vs §1.18 逐物件對比（R-ICS33(b)，最重）

出 `docs/reports/08_s118_vs_s18.md`：

1. 二節各自之**適用物件清單**（依 R-ICS2 v2，四欄判定 R-DD23／24）。
2. **行為面之重疊與差異**：以行為（按鍵資料傳送／旋鈕資料傳送／顯示狀態／stuck 等）
   為列，二節之對應物件為欄。
3. **現有 25 條之每一個錨**，於 §1.18 是否有更具體之對應物件；有則列其 ObjectID 與逐字。
4. **特別查二項**（R-ICS33(e)）：
   - `$TGW_DISP_STAT$`／`$DCSD_DISP_STAT$` 之發送與觀察面（可能解 DR-ICS16）
   - `Back_Button` 之 HU／ICS 側行為母條（可能解 DR-ICS13）
5. 依 R-ICS33(c) 就三種可能結果**分別估其影響**（①§1.8 為正 ②§1.18 為正、25 條須重錨
   ③二節並存），含**受影響 TC 條數**之實數。
6. **只列不裁、不改錨。** 若查得 §1.18 有解 DR-ICS16／13 之母條，**停下回報**（E2），
   不逕行生成或改寫。

### 作業 B — 005 生成（R-ICS31(c)）

E4 於本件未真正觸發（屬性比對以值集合為準）。
005 → Test Set `Volume Control`，上半取 `CFTS022-4914993` 逐字，
錨 `CFTS022-4914993`，訊號 `$CLIMATIC_PANEL.Radio_btn4$`（值依 `VAL_` 逐字）。
條數不預設，依 partial failure 拆分。

### 作業 C — if-any 四行改寫（R-ICS32(b)）

B6／Scroll／Tune／N1 各一行 ER。改為條件式：
Pre-Condition 增「已定義該動作之畫面」之限定（畫面名仍 `PENDING: DR-ICS6`），
ER 再斷言差異。**不得自行指定畫面名**；改寫後該四行應不再列為未錨定斷言。

### 作業 D — 不作為三行標弱驗證（R-ICS32(a)）

B3／I1／I2 各一行。**內容不改**，於各該 TC 之 reasoning 增一句：
「來源未承諾該不作為必有可觀察之無變化」，並於體檢報告標為弱驗證。

### 作業 E — 常設項落地（R-ICS32(c)）

1. 候選篩腳本（ER 實詞對所錨來源句之涵蓋度）納入每包必跑之自檢集。
2. 人工複核之判準表寫於腳本檔頭，沿 `gen_pre_delivery_07.py` 之體例。
3. 對全批（含本包新增）重跑一次，出 `docs/reports/08_pre_delivery_check.md`。

## §4 掃描條件

沿 upstream-07 §0 全部。另：屬性三軸比對以**值集合**為準、順序不計（R-ICS31(a)）；
verbatim 仍為逐字（R-ICS31(b)）；章節樹須輸出至 `§1.x` 三層並標出與 feature 同名／近名之節（A-ICS50）。

## §5 預期數字

| # | 項 | 預期 |
|---|---|---|
| 1 | `ledger_guard` 開工前 | exit 0；錨點 38（相異 33）、A-ICS 50、DR 17／相異 17 |
| 2 | §1.18 適用物件 | 29（upstream-07 §13-2 之數，本包複驗；不符即以實測為準） |
| 3 | §1.8 vs §1.18 對比表 | 行為面逐列有對應；25 條之錨逐一有判 |
| 4 | 三種結果之影響估 | 各含受影響 TC 條數之實數 |
| 5 | 005 之 TC | ≥ 1 |
| 6 | 009 之 TC | **0**（凍結） |
| 7 | if-any 未錨定斷言 | 4 → 0 |
| 8 | 不作為未錨定斷言 | 3 → 3（保留，標弱驗證） |
| 9 | 未錨定斷言總數 | 7 → 3 |
| 10 | `$TGW_DISP_STAT$` 佔位 | 12（不變，除非 E2 觸發後另裁） |
| 11 | Test Set 相異值 | **5，不變** |
| 12 | `ledger_guard` 完工後 | exit 0；差異依 R-ICS26(a) 處理 |
| 13 | 四支 gate | 差皆 0 |

## §6 升級條件

- **E1**：`ledger_guard` 開工前 exit 1 → 停。
- **E2**：作業 A 查得 §1.18 有解 DR-ICS16 或 DR-ICS13 之母條 → **停下回報，不逕行改寫**。
- **E3**：作業 A 顯示 §1.8 之某物件於 §1.18 有**內容衝突**（非僅更具體）之對應物 → 停下回報。
- **E4**：`4914993` 於新版之三軸值集合與舊版不同 → 停（本包已知相同，此為防迴歸）。
- **E5**：if-any 改寫後仍無法脫離未錨定斷言（即 Pre-Condition 之限定本身即未錨定）→ 具名回報。
- **E6**：任一作業須改動禁區所列之檔。

## §7 上繳要求

1. 沿既有體例；§0 含本檔重測 sha256。
2. §1 列 R-ICS1～R-ICS33 全部 sha8（五組 v1／v2 並列）；
   **sha8 不符時依 R-ICS19 v2(b)**：差異行全為圍籬外註記者不停，具名即可。
3. 附 `ledger_guard` 前後二次實跑。
4. §預期數字逐項對 §5 之 13 項，相符者亦列。
5. §DR 狀態實測表：逐條回報，特別是 DR-ICS13／16／17 三條改述後之現況。
6. §獨立判斷：續答「哪幾條可現狀出貨」；並回答——
   **若 §1.18 為正而 25 條須重錨，以本 feature 現況估，重錨之工作量與風險為何。**
