# 下放包 18 —— 切斷循環指涉、判準之偽陰與 doc-sync 之錨

- 日期：2026-08-24
- 方向：分析層（Claude Project）→ 執行層（Claude Code）
- feature slug：`power_moding`
- 對應上繳：`features/power_moding/docs/upstream/18_break_the_circle.md`
- 前一包：[17_scope_of_inventory.md](17_scope_of_inventory.md)
  （上繳 [../upstream/17_scope_of_inventory.md](../upstream/17_scope_of_inventory.md)）

---

## 一、17 包之覆核結果 —— **通過**

三條停止條件全未觸發；章 8 方向二 8/8 逐字命中、新漏 0；
`shasum -c` 5/5 OK；質疑型候選 23／65；十支檢查全綠。

**四項特別記明**：

1. **§3.2 為本輪最有價值之發現** —— A-PMH03 原記「PDF 為
   `Startup R1Low Only`」，而**該連續字串於 PDF 中不存在**。
   以 `get_text("blocks")` 查出其為**頁眉（y=21.6 之 `Startup`）
   與節標題（y=469.3 之 `R1Low Only`）兩個獨立文字物件之串接**。
   **「拼字錯誤」之歸因本身就是一個未經查證之推論**，
   而它被沿用了兩包。
2. **§4.6 之連帶觸發** —— 為 `check_granularity.py` 加 `LIMITS` 後
   `--check-doc-sync` 立即 FAIL，**R-PMH42 第二次實地兌現**，
   且執行層具名其為誤報（程式被改而門檻未動）而非分岔。
3. **§7 之數字更正** —— 下放包記「四份素材」，實為**五筆**
   （036 母本 ＋ 036 客戶複本 ＋ 037 ＋ SYS1 ＋ PDF）。我寫錯，採認。
4. **§6.1 之限度陳述** —— 「只在其前」為空集，執行層明說
   「這證明的是**在這份規格裡**沒發生，不是窗口方向之選擇無關緊要」。

---

## 二、§12 第 2 項之循環指涉 —— **可切斷，方法已在手上**

執行層自陳：R-PMH51 記為「已結清」係因章 8 補做完成，
**而 9.1／11.1 之結論仍建於 13 包之 6-gram 30% 門檻，
該門檻正是 §5.4 第 3 項所列之未受檢驗代理量** —— 二者互相指涉。

**該循環之切斷點，執行層自己已經做出來了，只是沒認出來。**

看章 8 之實跑輸出：

```
--- 方向二（PDF → SYS1）---
  #  逐字命中         覆蓋率
  1  是        100.0%
  …  （8 句全部）
  方向二真漏候選（覆蓋 < 30%）：0 句
```

**8 句全部「逐字命中」，門檻一次都沒有被用到。**
6-gram 只在「逐字未命中」之殘餘上才起作用 —— 而章 8 之殘餘為空。

**故判定之權威是二值之逐字命中，門檻只是殘餘之分流器。**
只要把這個分工寫成條文，9.1／11.1 就不必倚賴門檻：
對其建錨重跑，凡逐字命中者即定案，**殘餘一律人讀，不得由門檻自動判為「非漏」**。

→ R-PMH66。**循環自此切斷** —— 判定不再建於未受檢驗之代理量上。

---

## 三、§12 第 3 項 —— 分析層即第二來源，**我覆核了，並查出一個偽陰**

執行層自陳「我就是那個『人』，而我同時也是寫判準的人」，
`VERDICT` 至少有 must-hit D，本項沒有等價驗證。**分析層即其第二來源。**

### 3.1 對其 6 個偽陽之判定：**逐條同意**

| 條號 | 其偽陽理由 | 分析層 |
|---|---|---|
| R-PMH64 | 判準之自命中（條文列舉標記清單本身） | **同意** —— 結構性偽陽，無法以判準排除 |
| R-PMH10 | 「取代」為被質疑者之標記 | **同意** |
| R-PMH16 | 「不符」指其自保之已知反例（Comfort） | **同意** |
| R-PMH17 | 追認型，「取代」為引用 R-PMH15 | **同意** |
| R-PMH26 | 「不成立」在其 (d) 之條件句 | **同意** |
| R-PMH55 | 「不成立」指 ER 之斷言條件 | **同意** |

### 3.2 但其偽陰確實存在 —— **實測一例：R-PMH20**

執行層 §12 第 4 項自陳「判準之偽陰率我沒有量測，因為量測它需要一份
已知正解之清單」。**不必先有正解清單，反例一個就夠。**

**R-PMH20 未進候選 23，而它是質疑型。** 其依據段逐字：

> 依據：A-PMH11（分頁層量測寫成全簿結論，**實測全簿為 5 組而非 4 組**）；
> 以及 R-PMH10 之成因（四份樣本寫成「語料 5/5」，實際母體未定義）。

它質疑了執行層 14 包 §2.2 之陳述，**形態與 R-PMH51／R-PMH59 完全相同**。
**其未命中之原因是它用了「而非」，而 15 個標記裡沒有這個詞。**

同型可疑者另有 **R-PMH21**（「二者皆為正常結果……**非內容差異**」）。

### 3.3 處置：補標記 ＋ 抽樣估偽陰率

**只補標記不夠** —— 補完之後，仍無人知道還有多少種措詞未被列舉
（此即 A-PMH08／A-PMH13 之列舉式判準形態，在本 repo 已出現第四次）。

**故同時要求抽樣**：自**非候選**之條文中隨機抽 10 條人讀，
逐條判其是否為質疑型；命中數即偽陰率之估計，**與判準一併回報**。

→ R-PMH67。

---

## 四、§12 第 5 項 —— doc-sync 之錨取錯了層級

執行層具名其粗度：以**整支程式之 SHA256** 為錨，故任何編輯（含純註解）
皆使文件失效，而門檻一字未動。其結論尖銳：

> 這會訓練出「重跑 emit 再貼上」之反射動作，**而該反射正是使 R-PMH42
> 失效之途徑**：若某日門檻真的變了，重貼之動作與本次完全相同，
> **沒有任何東西會提醒那次不一樣**。

**該分析成立，且其修法是明確的：錨應取門檻表本身，不取整支程式。**

`--emit-thresholds` 之輸出即門檻表之正規形式；以**該輸出之 SHA256** 為錨，
則：

| 情形 | 現行（程式雜湊） | 修正後（門檻表雜湊） |
|---|---|---|
| 加註解／加 `LIMITS` | **FAIL**（誤報） | PASS |
| 改門檻值 | FAIL | **FAIL**（真報） |
| 改門檻之計算方式而值不變 | FAIL | PASS ⚠ |

**第三列即修正後之殘餘盲區**，須具名寫入 `LIMITS`：
門檻表雜湊守的是**值**，不是**產生該值之邏輯**。

→ R-PMH68。

---

## 五、裁決條文（逐條抄入 `RULINGS.md`）

```
R-PMH66（判定為二值，門檻只分流殘餘）
規格比對之判定以**逐字命中**（二值）為權威。相似度門檻
（6-gram 覆蓋率等）**不得作為判定之依據**，其唯一用途為
**將逐字未命中之殘餘分流供人讀**。

具體拘束三項：
(a) 逐字命中者即定案為「非漏」，不再計算任何相似度；
(b) 逐字未命中者一律進入殘餘；**殘餘不得由門檻自動判為「非漏」** ——
    門檻只決定人讀之優先順序，不決定結論；
(c) 殘餘之每一句，其最終判定須有人讀之具名結論，不得只留門檻之數值。

依據：17 包 §三之章 8 實跑 —— 方向二 8 句**全部逐字命中**，
6-gram 門檻一次都未被用到。判定之權威本即在二值之逐字命中，
門檻只是殘餘之分流器；**此分工一經明載，9.1／11.1 即不必倚賴
未受檢驗之門檻**（16 包 §12 第 2 項所指之循環指涉，自此切斷）。
```

```
R-PMH67（列舉式判準須附偽陰之抽樣估計）
以標記列舉為基礎之判準（R-PMH57 之前綴、R-PMH64 之質疑型標記、
R-PMH45／R-PMH49 之互斥對），其回報**須含偽陰之抽樣估計**：

自**未命中**之母體中隨機抽 N 條（N >= 10）人讀，逐條判其是否應命中；
命中數即偽陰率之估計，與判準、偽陽數一併回報。

**補標記不構成本條之滿足** —— 補完之後仍無人知道還有多少種措詞未被列舉。
抽樣之作用不在補齊，在於**使「不知道還漏多少」變成一個有數字之陳述**。

已知偽陰（實測，18 包 §3.2）：**R-PMH20 未進 R-PMH64 之候選 23**，
其依據段逐字為「實測全簿為 5 組**而非** 4 組」——
形態與 R-PMH51／R-PMH59 相同，未命中之原因是「而非」不在 15 個標記內。
同型可疑者另有 R-PMH21（「**非**內容差異」）。

標記清單應補 `而非`／`並非`／`過時`／`失效`／`無來源`／`湊得`，
**惟補標記與抽樣估計二者皆須為之，不得以其一代替其二。**
```

```
R-PMH68（doc-sync 之錨取門檻表，不取整支程式）
`--check-doc-sync` 之錨為 `--emit-thresholds` **輸出之 SHA256**，
不為實作該門檻之程式檔之 SHA256。

理由：以整支程式為錨者，任何編輯（含純註解、含新增與門檻無關之常數）
皆使文件失效，而門檻一字未動。該誤報會訓練出「重跑 emit 再貼上」之
反射動作，**而該反射正是使本檢查失效之途徑** —— 門檻真變之日，
重貼之動作與誤報之日完全相同，無任何東西提醒其不同（17 包 §12 第 5 項）。

**殘餘盲區須具名寫入 `LIMITS`**：門檻表之雜湊守的是**值**，
不是**產生該值之邏輯** —— 改計算方式而值不變者，本檢查不會察覺。
```

---

## 六、作業步驟

1. **抄錄** —— §五之 R-PMH66 ~ R-PMH68 逐字抄入 `RULINGS.md`，附核對表
   （依 R-PMH41 驗命中數）。

2. **9.1 與 11.1 之建錨重跑（R-PMH66）** —— 以
   `chapter_bidirectional.py` 對章 9、章 11 建錨並雙向重跑：
   - 判定以**逐字命中**為之；
   - 殘餘（逐字未命中者）**逐句人讀並具名結論**，不得只留覆蓋率數值；
   - 章 9 之結論須與 A-PMH14 之「狀態矩陣整表缺失」一致；不一致則停。
   **此步驟同時結清 R-PMH51 之最後兩側，並解除 16 §12 第 2 項之循環。**

3. **章錨之分割檢查（17 §12 第 1 項）** —— 對全部已建錨之章，
   驗其**覆蓋且不重疊**：相鄰兩章之錨互為邊界，
   全 PDF 之 p8–p11 文字須被章區間完全覆蓋，且任兩章區間不得相交。
   **未被任何章覆蓋之文字逐段列出** —— 那正是「錨之外的第 7 個 marker」
   會藏身之處。

4. **R-PMH67 之落實** —— 補六個標記後重跑 `challenge_rulings.py`，
   並自**未命中**之條文中隨機抽 10 條人讀，回報偽陰率之估計。
   **抽樣須可重現**（記其亂數種子或抽樣方法）。

5. **R-PMH68 之落實** —— `--check-doc-sync` 之錨改為
   `--emit-thresholds` 輸出之 SHA256；`LIMITS` 增其殘餘盲區。
   **附兩項故意失敗**：
   (a) 改門檻值而不重貼 → 須 FAIL；
   (b) **加一行純註解 → 須 PASS**（證明誤報已消除）。

6. **§5.4 六項中之第 1 項（TSV 之 SYS1 來源）** —— 依 17 §14 第 3 項，
   其與 lint §3.4 直接相關。**本輪只做量測，不改**：
   對 48 leaf 逐一比對 `layer3_sections.tsv` 之 `section_title`
   與 PDF 對應段落之首句，回報不符者。
   **若不符數為 0，該項可降為低風險；不為 0 則須裁。**

---

## 七、停止條件

canon §0 六條，另加本包三條：

7. 步驟 2 之章 9 結論與 A-PMH14 之「狀態矩陣整表缺失」不一致
8. 步驟 3 之分割檢查發現未被任何章覆蓋之文字**且其中含 marker**
9. 步驟 5 之 (b)（純註解 → 須 PASS）未通過

**本包零寫回工作簿。本包未由分析層授權提交**（R-PMH65）。
**不得改動 `scripts/new_feature.py`、`docs/runtime/` 下任何檔案、
任何他 feature 之檔案。**

---

## 八、上繳包要求（`docs/upstream/18_break_the_circle.md`）

1. §五三條之抄錄核對表（含命中數）
2. 章 9／章 11 之雙向重跑全表 ＋ 殘餘之逐句人讀結論
3. 章錨分割檢查之輸出（覆蓋、重疊、未覆蓋文字）
4. 補標記後之候選清單 ＋ **偽陰抽樣之 10 條與其判定**
5. doc-sync 改錨後之兩項故意失敗實跑
6. TSV `section_title` vs PDF 首句之 48 列比對
7. lint 全跑輸出
8. 未結 DR 清單
9. **「本包是否仍有該驗而未驗者」之獨立判斷** —— 不得省略
10. 建議之 commit 訊息與 pathspec（**不執行**）＋ R-G6 之揭露表

---

## 九、待 Pei

| # | 事項 | 阻斷 |
|---|---|---|
| 1 | **三筆 DR 之發出** —— **執行層已第五度重申**。DR-PMH2 於 17 包再獲佐證（A-PMH14 之矛盾已更正，其正確判定即「狀態矩陣整表缺失」） | **DR-PMH1 阻斷交付** |
| 2 | 17／18 之 commit 授權 | 否 |
| 3 | §5.4 其餘五項之處置（第 1 項已排入本包步驟 6） | Phase 5 |
| 4 | Q10、`PROFILE_INTEGRATION.md` | 否 |

---

## 十、本包產生之新條文清單（自檢表）

| 條號 | 主旨 | 已以可貼區塊出現於 §五 |
|---|---|---|
| R-PMH66 | 判定為二值（逐字命中），門檻只分流殘餘且殘餘須人讀 | ✅ |
| R-PMH67 | 列舉式判準須附偽陰之抽樣估計；補標記不構成滿足 | ✅ |
| R-PMH68 | doc-sync 之錨取門檻表輸出，不取整支程式 | ✅ |

三條各管一事。
