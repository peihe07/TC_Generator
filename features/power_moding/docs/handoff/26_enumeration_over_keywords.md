# 下放包 26 —— 斷言之二分、掃描母體改為全枚舉與限定之合併上限

- 日期：2026-08-25
- 方向：分析層（Claude Project）→ 執行層（Claude Code）
- feature slug：`power_moding`
- 對應上繳：`features/power_moding/docs/upstream/26_enumeration_over_keywords.md`
- 前一包：[25_limitation_union.md](25_limitation_union.md)
  （上繳 [../upstream/25_limitation_union.md](../upstream/25_limitation_union.md)）

---

## 一、25 包之覆核結果 —— **通過**

三條抄錄逐位相符；`-007` 七項限定全在且 lint 30/30；
ER1～ER5 逐斷言掃描牴觸 0；字級座標與分析層逐項相符；斷言／ER 比 1.28。

**三項特別記明**：

1. **§3.2 之處置** —— `r46`／`r47` 納入限定**而 A-PMH22 不結清**，
   並記「若日後有 TC 之斷言涉及 headunit mode 之靜音行為，**本則即復活**」。
   **繞過一個歧義而不假裝它消失了。**
2. **§6 之含意自陳** —— 23 包之 18 條判定「**有 5 個斷言未被單獨對照，
   佔 22%；其涵蓋率為 78%，非 100%**」。
   **一個剛通過的停止條件，其通過本身被標註了限度。**
3. **§12 第 2 項** —— 自陳「**R-PMH94 之 (a)~(c) 並未給『不需掃描』這個出口，
   我自行加了一個例外**」。

---

## 二、§12 第 2 項 —— **你加的那個出口是對的，但它該是條文**

執行層對 ER1／ER2 判「不需反向掃描」，理由為：
其斷言之標的為「**測試過程中該事件未發生**」，非 SUT 之行為；
而素材所述者皆為「某事件發生後 SUT 如何」，**無共同謂詞可取相反值**。

**該理由成立。** 反向掃描之目的是找「與 SUT 行為斷言取相反值之素材陳述」，
而**測試執行斷言根本不在素材之論域內** —— 素材不描述測試員做了什麼。

**但執行層之自陳也對**：R-PMH94 沒有給這個出口，**而它自己加了一個**。
**加得對不影響「自行加例外」這件事之性質** ——
下一次若加錯，同樣不會有東西攔它。

→ R-PMH97，把出口寫進條文，並附其判別法與具名義務。

---

## 三、§12 第 4 項 —— **關鍵詞之列舉，正解是不要用關鍵詞**

執行層量了 15 個同義表述（規格與矩陣皆 0 命中）並自陳
**「那只證明『我想到的 15 個都不在』…… 這不是隨機抽樣，是我自己出的題目」**。

**其自評正確，而補更多同義詞不會解決它** —— 與「非漏」之情形完全同型
（24 包 §7.2：漏掉的可能正是用最多的那個）。

### 3.1 R-PMH91 之作法可以移植

R-PMH91 解決記法之列舉，靠的不是列更多禁用詞，
而是**把母體換成一個依構造即封閉之集合**（各檢查之判定表），
再要求其每一項以四詞之一作結。

**同一手法可移植到斷言掃描**：

| | 現行 | **改為** |
|---|---|---|
| 母體 | **關鍵詞命中之行／格** | **全部行／全部有值格** |
| 關鍵詞之角色 | 決定何者進入母體 | **僅決定人讀之先後** |
| 偽陰 | 未被想到之表述**永不進入母體** | **不存在** —— 每一行／格皆須具名記法 |

**`matrix_vs_chapter.py` 早已是這個形態** —— 其對章 7／8／10／11
各以 **30 列全具名**，不以關鍵詞篩選。
**而 `spec_assertion_scan.py` 不是。** 兩支同時存在於本 feature，
**一支已經解決了另一支還在犯的問題。**

### 3.2 母體之規模可估

| 側 | 規模 |
|---|---|
| 規格 | PDF 全文之行（`pm.txt` 約 580 行；扣封面與圖標籤後之敘述行為其子集） |
| 矩陣 | **事件列之有值格 174**（21 包 §3 已量） |

**每一斷言 × 174 格**在人讀上不可行，**故須分兩層**：
機器先以**謂詞層**（而非關鍵詞）粗篩 —— 例如該格之動詞是否涉及
顯示／音訊／電源／狀態轉移 —— 再由人讀其入選者。
**惟粗篩之落選者仍須逐格具名其落選理由**，
否則只是把關鍵詞換了個名字。

→ R-PMH98。

---

## 四、§12 第 3 項 —— 合併之風險，設上限而非禁止

執行層自陳七項限定壓縮於四步，並自評
**「若某項因合併而在執行時被忽略，其後果與刪去相同」**。

**§5.2 之字數上限使完全不合併不可行**（七項七步，加上原有三步為十步，
且每步僅一項時其字數遠低於上限，形式冗贅）。

**故不禁止合併，而設可驗之拘束**：每步至多兩項、ER 逐項複述、
lint 驗七項之字串各出現一次。**現行之四步（2/2/2/1）已合於此。**

→ R-PMH99。

---

## 五、裁決條文（逐條抄入 `RULINGS.md`）

```
R-PMH97（斷言之二分：SUT 行為斷言 vs 測試執行斷言）
ER 之斷言分二類：

  **SUT 行為斷言** —— 其標的為受測系統之行為或狀態
                    （`The announcement is heard`／`no pop-up is displayed`）。
                    **須依 R-PMH94 逐斷言反向掃描。**
  **測試執行斷言** —— 其標的為測試過程中測試員之作為或不作為
                    （`No ON/OFF key press occurs`／`No door is opened`）。
                    **不入反向掃描之母體。**

判別法：該斷言之主語是否為 SUT（或其部件）。
主語為測試員之操作者，屬後者。

**不入母體者須逐條具名其歸類與理由**，並寫入該檢查之常數
（非只寫於上繳包）；**未具名而略過者，視為未掃描。**

依據：25 包 §4.1 —— 執行層對 `-007` 之 ER1／ER2 判「不需反向掃描」，
其理由成立（素材不描述測試員做了什麼，故無共同謂詞），
**而 R-PMH94 之 (a)~(c) 未給該出口，執行層自行加之**（25 包 §12 第 2 項自陳）。
本條使該出口成為條文，**加得對與否不影響「自行加例外」之性質** ——
下次若加錯，同樣不會有東西攔它。
```

```
R-PMH98（斷言掃描之母體為全枚舉，關鍵詞降為排序輔助）
斷言之反向掃描（R-PMH90／R-PMH93／R-PMH94），其母體為
**規格之全部敘述行**與**素材之全部有值格**，
**不得為關鍵詞命中之子集**。

關鍵詞之角色由「決定何者進入母體」降為「**決定人讀之先後**」。

規模不可行時得以**謂詞層粗篩**分兩層（機器先篩、人讀其入選者），
**惟粗篩之落選者仍須逐格／逐行具名其落選理由** ——
否則只是把關鍵詞換了個名字。

依據：25 包 §5 之 15 個同義表述量測命中 0／0，執行層自陳
**「那只證明『我想到的 15 個都不在』…… 這不是隨機抽樣，是我自己出的題目」**
（25 包 §12 第 4 項）。其形態同於 24 包 §7.2 之「非漏」——
**漏掉的可能正是用最多的那個**，補列舉不解決。

**本 feature 已有正解之實例**：`matrix_vs_chapter.py` 對章 7／8／10／11
各以 30 列**全具名**，不以關鍵詞篩選；
**而 `spec_assertion_scan.py` 仍以關鍵詞篩母體** ——
**兩支同時存在，一支已經解決了另一支還在犯的問題。**
```

```
R-PMH99（限定合併之上限與其可驗性）
事件層限定（R-PMH87／R-PMH94）得合併於同一 procedure 步驟，
**惟須同時滿足三項**：

(a) **每步至多兩項**；
(b) **ER 逐項複述** —— 合併之步驟，其 ER 須將各項分列或以連接詞明列，
    使每一項各自可判；
(c) **lint 須驗各限定項之字串於 procedure 中各出現一次**
    （出現 0 次或 2 次以上皆 FAIL）。

**不禁止合併之理由**：canon §5.2 之字數上限使「一項一步」於本例產生
十步且每步字數遠低於上限，形式冗贅。

依據：25 包 §3.3 —— 七項限定壓縮於四步（2/2/2/1），
執行層自評**「若某項因合併而在執行時被忽略，其後果與刪去相同」**
（25 包 §12 第 3 項）。**現行之四步合於本條。**
```

---

## 六、作業步驟

1. **抄錄** —— §五之 R-PMH97 ~ R-PMH99 逐字抄入 `RULINGS.md`，附核對表
   （依 R-PMH41 驗命中數）。

2. **5 個未單獨對照之斷言（25 §12 第 1 項）** ——
   `-001` ER1／ER3、`-002` ER2、`-004` ER2／ER3，
   依 R-PMH94 逐斷言對照，**並依 R-PMH97 先判其二分**
   （測試執行斷言者具名歸類，不入母體）。
   **發現牴觸即停並上呈。**

3. **`batch_er_vs_matrix.py` 改以斷言為單位（R-PMH94 之連帶）** ——
   母體由 18 條 ER 改為 23 個斷言（`-007` 另計），
   重跑並回報記法分布。**涵蓋率由 78% 應升為 100%。**

4. **`spec_assertion_scan.py` 之母體改造（R-PMH98）** ——
   自關鍵詞命中改為全枚舉：
   - 矩陣側：**174 個事件列有值格**全部入母體；
   - 規格側：PDF 之全部敘述行入母體（封面、頁碼、圖標籤得排除，
     **惟排除規則須具名且逐行可查**）；
   - 得以謂詞層粗篩分兩層，**落選者逐項具名其落選理由**。
   **本輪先做矩陣側（174 格），規格側之全枚舉列為下輪**
   —— 二者規模不同，一次做完風險過高。
   **改造後對 `-007` 之四個斷言重跑**，回報記法分布與其與現行結果之差異。

5. **`L160` 之 `Note:` 適用範圍（25 §12 第 5 項）** ——
   併入 `DR-PMH7` 之問題全文（其已含 `Else: Mute Active` 之記法問題，
   二者同為「素材之記法未定義」之類）。
   **若 `DR-PMH7` 已發出則另開 `DR-PMH8`。**

6. **`-007` 之七項限定依 R-PMH99(c) 加 lint 檢查** ——
   驗七項字串各出現一次，附 must-hit：
   刪去任一項 → 須 FAIL；重複任一項 → 須 FAIL。

7. **章 12 × 矩陣之全對照（25 §12 第 6 項）** ——
   除 `r16` 外之 29 列。章 9 因 `DR-PMH5` 未答而暫不做。

---

## 七、停止條件

canon §0 六條，另加本包三條：

7. 步驟 2 之 5 個斷言對照發現**新的**牴觸（未經登記者）
8. 步驟 4 之矩陣側全枚舉（174 格）對 `-007` 之四斷言，
   其結果與現行（關鍵詞篩選）**不同**
   —— 若不同，代表關鍵詞篩選確曾漏掉東西，須逐項回報
9. 步驟 6 之 must-hit（刪去任一項）**未 FAIL**

**本包零寫回工作簿。本包未由分析層授權提交**（R-PMH65）。
**ch 9（`Power Transitions`）不得開批**；**batch 1 不得提交寫回**
（步驟 2、3 未完成前）。
**不得改動 `scripts/new_feature.py`、`docs/runtime/`、任何他 feature 之檔案。**

---

## 八、上繳包要求（`docs/upstream/26_enumeration_over_keywords.md`）

1. §五三條之抄錄核對表（含命中數）
2. **步驟 2 之 5 個斷言對照表**（含 R-PMH97 之二分歸類）
3. 步驟 3 之 23 斷言記法分布 ＋ 涵蓋率
4. **步驟 4 之矩陣側全枚舉結果**（174 格）＋ 與關鍵詞篩選之差異
5. 步驟 5 之 DR 增補
6. 步驟 6 之 lint 新檢查 ＋ 兩項 must-hit 實跑
7. 步驟 7 之章 12 全對照表
8. 由程式產生之檢查總表
9. 未結 DR 清單（狀態依 R-PMH82）
10. **「本包是否仍有該驗而未驗者」之獨立判斷** —— 不得省略
11. 建議之 commit 訊息與 pathspec（**不執行**）＋ R-G6 之揭露表

---

## 九、待 Pei

| # | 事項 | 阻斷 |
|---|---|---|
| 1 | 三筆 DR 之發出 ＋ 日期與對象（`DR-PMH7` 建議併問 `Else: Mute Active` 與 `L160` 之 `Note:` 二者之適用範圍） | `DR-PMH5` 阻斷 ch 9 |
| 2 | 章 9 × 矩陣之全對照（待 `DR-PMH5`）；9.1 之 profile 例外 | ch 9 開批 |
| 3 | 17 §5.4 其餘五項；Q10、`PROFILE_INTEGRATION.md` | 否 |

---

## 十、本包產生之新條文清單（自檢表）

| 條號 | 主旨 | 已以可貼區塊出現於 §五 |
|---|---|---|
| R-PMH97 | 斷言之二分；測試執行斷言不入掃描母體，須具名 | ✅ |
| R-PMH98 | 掃描母體為全枚舉，關鍵詞降為排序輔助 | ✅ |
| R-PMH99 | 限定合併之上限（每步至多兩項）＋ ER 逐項複述 ＋ lint 字串檢查 | ✅ |

三條各管一事。
