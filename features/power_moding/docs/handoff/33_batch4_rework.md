# 下放包 33 —— batch 4 之重做（限定樣板、`-035` 之矛盾、`NA`）

- 日期：2026-08-25
- 方向：分析層（Claude Project）→ 執行層（Claude Code）
- feature slug：`power_moding`
- 對應上繳：`features/power_moding/docs/upstream/33_batch4_rework.md`
- 前一包：[32_batch4.md](32_batch4.md)
  （上繳 [../upstream/32_batch4.md](../upstream/32_batch4.md)）
- **本包為 batch 4 之第二輪**（R-PMH120 之上限）

---

## 一、32 包之覆核 —— **程序面通過，產出面不通過**

三條抄錄逐位相符；`n_leaf` = 46 全項重跑；章 7 全枚舉 28/28、未判定 0；
停止條件 8 未觸發（12 leaf 全在 p8）；A-PMH28 立；lint 32/32 × 4。

**三項特別記明**：

1. **§停止條件 7 之處置** —— 量出「只有一句真的漏，而它已由 `-024` 承載」，
   並拒絕補進 `-025`／`-026`。其理由逐字為
   **「那會使追溯欄記載一個該條並未驗證的句子，是把檢查做綠而不是把事做對」**
   —— **採認，並升為條文**（R-PMH122）。
2. **`--limit-must-hit` 母體之自查** —— 迴圈寫死 `("batch01","batch02")`，
   batch 3／4 從未進過該錨點；**上繳草稿一度寫成 33/33 而實測 19/19，
   在報出前自己量了一次才發現**。
   其歸因精準：**「28 §5.2 一般化之未竟處 —— 當時只改了期望值的來源，
   沒改母體的來源」**（R-PMH124）。
3. **`A-PMH27` 之狀態詞用 `ACCEPTED` 而非下放包所令之 `RESOLVED`** ——
   **你對，我錯**（R-PMH123）。

---

## 二、batch 4 之人讀覆核 —— **三項須改**

### 2.1 【嚴重】限定之依據為十四條逐字相同之樣板，其中九條無逾時斷言

十四條之 `reasoning` 皆含**一字不差**之同一段：

> 「`SU9.1)` …與**本條之逾時斷言**同謂詞取相反值」

**逐條實查其 ER**：

| 有逾時／時長斷言（**5**） | `-024`（1.5s）／`-026`（1.5s）／`-028`（3s）／`-029`（10s）／`-032`（計次） |
|---|---|
| **無任何逾時斷言（9）** | `-025`／`-027`／`-030`／`-031`／`-033`／`-034`／`-035`／`-036`／`-037` |

`-030` 之 ER 為「觸發組合」、`-031`／`-035` 為「不顯示動畫」、
`-036` 為「跨螢幕同步」—— **其 ER 中無任何逾時**，
而其 `reasoning` 宣稱「本條之逾時斷言」。

**違反二條**：
- **R-PMH94** —— 限定須**逐斷言**導出；樣板不是導出；
- **R-PMH43** —— 陳述須有證據；**該句於九條為不成立之陳述**。

**九條之限定依 §8.5 為不必要之窄化，須移除。**
**五條之限定保留**，其 `reasoning` 須逐條具名**該條之哪一個 ER 斷言**與
`SU9.1)` 取相反值（不得再用「本條之逾時斷言」之泛稱）。

### 2.2 【嚴重】`-035` 之限定與其步驟指涉同一顆實體按鍵

```
1. Do not press the Power Off or Screen Off hard key
2. Press the power button to turn the head unit on
```

其 `reasoning` 稱「步驟 1 禁的是 Power Off／Screen Off，步驟 2 按的是電源鍵之 On」。

**`SU9.)` 之逐字為 `Pressing "Screen Off" or "Power Off" hard key`** ——
**該鍵即電源鍵**：關機時按下為 On、開機時按下為 Off。
**測試員讀到的是「不要按這顆鍵」而後「按這顆鍵」。**

且該條**無逾時斷言**（§2.1 之九條之一）→ **依 §2.1 移除後，矛盾自然消失。**

### 2.3 【中】`input_test_data` 為 `"N/A"`，前三批為 `"NA"`

canon §4.5 逐字為 `set Input Test Data to **NA**`。
batch 1／2／3 皆為 `"NA"`，**batch 4 十四條皆為 `"N/A"`**。

**四批之間值不一致，而 lint 32/32 全綠。**

→ 十四條改 `"NA"`。**其檢查之有無見 §四之解凍判斷。**

### 2.4 其餘逐項通過

`DS4.1)` 照原文抄；`test_item` 上半皆為 `source_clause` 之逐字子句；
`-025`／`-026`、`-036`／`-037` 之拆分依 §8.2.2 正確
（其預期結果相反／其為兩個獨立分支）；
`-034` 之三個後果不拆依 §5.7 正確且其與 §8.2.2 之界線已具名；
`source_clause_origin` 十四條全為 `spec_pdf p8`。

---

## 三、`-006` 三分之依據 —— **037 記了，在 `Requirement Title` 欄**

執行層 §10 第 3 項自陳「037 依何而分，台帳沒有記」。

**分析層實測 037 之 `Analysis Report`**：

| leaf | `Requirement Title` |
|---|---|
| `-006-01` | `Start-up Animation Duration and Trigger` |
| `-006-02` | `Shut-down Animation Duration` |
| `-006-03` | `Shut-down Animation Trigger Conditions` |

**分法是 037 的，不是執行層之讀法**（§8.2：RD 為需求單位之權威）。
**其與本批之三條逐條相符**（`-028` 啟動時序／`-029` 關機時長／`-030` 關機觸發）。

**缺的是 `layer3_sections.tsv` 未帶該欄** —— 故執行層所看之處看不到。
（同理 `-008-01/-02`、`-009-01/-02` 之分法亦記於該欄，本批之對應亦相符。）

→ R-PMH125。

---

## 四、`input_test_data` 之檢查 —— **不解凍**

§2.3 為實測缺陷（R-PMH104(a) 之字面條件成立），**惟分析層判不解凍**：

| | |
|---|---|
| 其為**值之不一致**，非**規則之違反** —— canon §4.5 之 `NA` 是一個例示，非明列之禁止式 | |
| 其**可由既有之「必填欄無空」與 profile §3 之欄位規則涵蓋**，惟需擴其判準 → **即新增檢查項** | |
| 其**後果為交付欄位之字面不齊**，不使任何 TC 之驗證內容錯誤 | |

**R-PMH104(a) 之「有誤」須為使 TC 內容錯誤者**，此項不是。
→ **修正十四條之值，不新增檢查。** 其一致性由 §五步驟 5 之一次性核對確認。

---

## 五、裁決條文（逐條抄入 `RULINGS.md`）

```
R-PMH122（追溯欄不得記載該條未驗證之句子）
`source_clause` 與 `specification_reference` 所載者，**須為該條實際驗證之句子**。

為使某項檢查通過而補入該條並未驗證之句子，**禁止** ——
其使追溯欄記載一件不實之事，**是把檢查做綠而不是把事做對**。

**檢查若因此不過，錯在檢查之對象指認，不在產出。**

依據：32 包停止條件 7 逐字為「`-001-01`／`-001-02` 之 `source_clause`
未含 `SU1.)` 之漏句子句」，而該子句實由 **`-024`** 承載
——**分析層以 leaf 指稱承載者，而承載者為 TC**；
`-001-01` 之另一條（`-025`）與 `-001-02`（`-026`）各驗別句，
補入即為不實。執行層拒絕補入並兩面並陳，**其處置正確**（32 包 §停止條件 7）。

**連帶**：停止條件之對象若為某一句之承載，**須以 TC 指稱，不得以 leaf 指稱**
—— 一個 leaf 可有多條 TC，各驗其不同之句。
```

```
R-PMH123（`A-PMH27` 之狀態詞採 `ACCEPTED`）
`A-PMH27`（`-002` 判 out of scope）之狀態詞為 **`ACCEPTED`**，非 `RESOLVED`。
**採認執行層 32 包之更正，分析層 32 包步驟 2 所令之 `RESOLVED` 撤回。**

其三項理由皆成立：
(a) 該缺口之**事實未消失** —— CFTS009 仍未持有，該行為仍無 TC；
(b) **R-PMH74 對同形態用的即 `ACCEPTED`**（`SU9.)`／`SU9.1)` 經裁定不補）；
(c) **R-PMH121(c) 之「已知未決清單」取 `ACCEPTED` 方成立** ——
    標 `RESOLVED` 者沒有理由出現在一份未決清單上。

**同理適用於 `-023`／`-028` 之狀態詞**，執行層須一併核對其一致。
```

```
R-PMH124（一般化須同時及於期望值與母體）
既有檢查之一般化（R-PMH107 所稱「不凍結且為義務」者），
**須同時及於其期望值之來源與其母體之來源**。

只改期望值而母體仍為寫死之列舉者，**新資料永不進入該檢查**，
而該檢查仍會報 PASS —— 其形態為「檢查通過而未檢查」。

實施：一般化後須以**新批之資料**實跑一次，並比對其母體規模之變化；
規模未變者即為未竟。

依據：32 包 §兩件我自己抓到的 第 1 項 —— `--limit-must-hit` 之迴圈寫死
`("batch01","batch02")`，batch 3／4 從未進過該錨點；
**上繳草稿一度寫成 33/33 而實測 19/19**。
執行層之歸因逐字為「**28 §5.2 一般化之未竟處 —— 當時只改了期望值的來源，
沒改母體的來源**」。
```

```
R-PMH125（`layer3_sections.tsv` 增 `requirement_title` 欄）
`data/layer3_sections.tsv` 增一欄 `requirement_title`，其值逐字取自 037
`Analysis Report` 之 `Requirement Title` 欄。

**理由**：037 之子項分法（`-006-01`／`-02`／`-03` 等）**記載於該欄**，
而台帳未帶之，致「037 依何而分」在台帳上不可見
（32 包 §10 第 3 項，執行層自陳其三分為「我的讀法」）。

**分析層實測**（037 `Analysis Report`）：
  `-006-01` = `Start-up Animation Duration and Trigger`
  `-006-02` = `Shut-down Animation Duration`
  `-006-03` = `Shut-down Animation Trigger Conditions`
**分法為 037 之分法，非執行層之讀法**（canon §8.2：RD 為需求單位之權威）。

**連帶義務**：增欄後，各批之每一 TC 須驗其射程與其 leaf 之
`requirement_title` 相符，不符者具名。
```

```
R-PMH126（限定之依據須逐條導出，不得為樣板）
事件層限定之 `reasoning`，**須逐條具名該條之哪一個 ER 斷言與素材取相反值**。

**逐字相同之樣板不構成 R-PMH94 之「逐斷言導出」** ——
其於某些條上必為不成立之陳述（R-PMH43）。

泛稱（「本條之逾時斷言」「本條之音訊斷言」）**不得用於未實際含該斷言之條**；
該條若無與素材取相反值之斷言，**其限定即為 §8.5 之不必要窄化，須移除**。

依據：batch 4 之十四條 `reasoning` 皆含一字不差之
「與**本條之逾時斷言**同謂詞取相反值」，而逐條實查其 ER，
**九條無任何逾時斷言**（`-025`／`-027`／`-030`／`-031`／`-033`／`-034`／
`-035`／`-036`／`-037`）。
其中 `-035` 更因該限定而與其步驟 2 指涉同一顆實體按鍵
（`SU9.)` 之 `"Power Off" hard key` 即電源鍵），
**使測試員讀到「不要按這顆鍵」而後「按這顆鍵」**。
```

---

## 六、作業步驟

1. **抄錄** —— §五之 R-PMH122 ~ R-PMH126 逐字抄入 `RULINGS.md`，附核對表。

2. **限定之逐條重導（R-PMH126）** ——
   (a) 九條之限定**移除**（其 procedure 步驟 1、ER1 一併刪，步數重編）；
   (b) 五條之限定**保留**，其 `reasoning` 改為具名該條之**該一個** ER 斷言
       （如 `-024` 之 ER4 `Each splash screen times out after 1.5 seconds`）；
   (c) `limits` 欄由十四筆減為**五筆**；
   (d) **R-PMH99(c) 之字串檢查隨之** —— 其讀該批之限定清單，
       清單減少即其射程減少，**非新增亦非移除檢查項**。

3. **`input_test_data` 十四條改 `"NA"`（§2.3）** —— **不新增檢查**（§四）；
   四批之值一次性核對並回報（`NA` 應為 43 條全數）。

4. **`A-PMH27`／`-023`／`-028` 之狀態詞核對（R-PMH123）** ——
   三者一致為 `ACCEPTED`，其於 `PENDING-ON-DR` 與「已知未決清單」之
   出現理由具名。

5. **`layer3_sections.tsv` 增 `requirement_title`（R-PMH125）** ——
   48 列全數；增欄後對四批之每一 TC 驗其射程與其 leaf 之 title 相符，
   **不符者具名**。

6. **`--limit-must-hit` 之母體一般化（R-PMH124）** ——
   母體改讀 `generated/` 之全部批次檔，
   **實跑後回報其母體規模之變化**（19 → 應為修正後之實際值）。

7. **lint 全跑（四批）** —— 檢查項數應維持 **32**。

---

## 七、停止條件

canon §0 六條，另加本包三條：

7. 步驟 2(a) 移除後，九條之 procedure 與 ER 步數不再 1:1
8. 步驟 5 發現任一 TC 之射程與其 leaf 之 `requirement_title` **不符**
9. 步驟 6 之母體規模**未變**（依 R-PMH124 即為未竟）

**本包零寫回工作簿。本包未由分析層授權提交**（R-PMH65）。
**apparatus 維持凍結** —— §四已判 `input_test_data` 一項不解凍。
**不得改動 `scripts/new_feature.py`、`docs/runtime/`、任何他 feature 之檔案。**

---

## 八、上繳包要求（`docs/upstream/33_batch4_rework.md`）

1. §五五條之抄錄核對表（含命中數）
2. **修正後之 batch 4 全文（14 條）** ＋ `limits` 五筆 ＋ lint 輸出
3. 步驟 3 之四批 `NA` 核對（43 條）
4. 步驟 4 之三筆狀態詞核對
5. 步驟 5 之 TSV 增欄 ＋ 四批之 title 相符核對
6. 步驟 6 之母體規模變化
7. 由程式產生之檢查總表
8. 未結 DR 清單
9. **「本包是否仍有該驗而未驗者」之獨立判斷** —— 不得省略
10. 建議之 commit 訊息與 pathspec（**不執行**）＋ R-G6 之揭露表

---

## 九、待 Pei

| # | 事項 | 阻斷 |
|---|---|---|
| 1 | **A-PMH28（流程圖之規範性）** —— 其提案 (a) 自 01 包放到現在。**五類行為全部無 TC 覆蓋，其中 `toggle them one after another` 直接落在 `-024`／`-026`／`-033`／`-034` 之標的內**。分析層提案：**比照 R-PMH55(b)，登記為覆蓋缺口並併入 `DR-PMH8` 第六問**，不為其撰寫 TC | 否，但其為本批最大之已知缺口 |
| 2 | **R-PMH121 之核可**（DR 未覆之交付截止規則） | 交付日之處置 |
| 3 | **`DR-PMH8`（現 5 問，擬增第六問）之發出** | 否 |
| 4 | 9.1 之 profile 例外；17 §5.4 其餘五項；Q10 | Phase 6／7 前 |

---

## 十、本包產生之新條文清單（自檢表）

| 條號 | 主旨 | 已以可貼區塊出現於 §五 |
|---|---|---|
| R-PMH122 | 追溯欄不得記載該條未驗證之句子；停止條件須以 TC 指稱承載者 | ✅ |
| R-PMH123 | `A-PMH27` 採 `ACCEPTED`；分析層之 `RESOLVED` 撤回 | ✅ |
| R-PMH124 | 一般化須同時及於期望值與母體 | ✅ |
| R-PMH125 | TSV 增 `requirement_title`；037 為分法之權威 | ✅ |
| R-PMH126 | 限定之依據須逐條導出，不得為樣板 | ✅ |

五條各管一事。**本包未新增任何檢查程式或檢查項**（符合 R-PMH104）。
