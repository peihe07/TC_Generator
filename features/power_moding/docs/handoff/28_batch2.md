# 下放包 28 —— 覆核線之收束、apparatus 凍結與 batch 2 開批

- 日期：2026-08-25
- 方向：分析層（Claude Project）→ 執行層（Claude Code）
- feature slug：`power_moding`
- 對應上繳：`features/power_moding/docs/upstream/28_batch2.md`
- 前一包：[27_verdict_for_every_cell.md](27_verdict_for_every_cell.md)
  （上繳 [../upstream/27_verdict_for_every_cell.md](../upstream/27_verdict_for_every_cell.md)）

---

## 一、27 包之覆核結果 —— **通過**

三條抄錄逐位相符；174 格全部入判定表；`--cell-must-hit` 兩項全過；
切分複核 23 → 26（+13%）；PC 之 24 個斷言牴觸 0；
lint 32/32；「未實測」由 8 降為 4。

**§3.3 為本輪最重要之一項**：`-003` ER2 之
`No user input is given while the disclaimer screen times out`
—— 以 ` and ` 為據時整條被歸為測試執行斷言而**不入母體**；
改以謂詞為準後，後半 `the disclaimer screen times out` 為 SUT 斷言。

> **連接詞決定切分時，一個 SUT 斷言可以整個消失在一個測試執行斷言背後。**

R-PMH101 之立條依據於同輪兌現。

---

## 二、**覆核線該收了 —— 用數字說**

### 2.1 產出與投入之對照

| 項 | 值 |
|---|---|
| Phase 4 開批（12 包） | 2026-08-24 |
| **迄今之往返** | **16 輪**（12 → 27） |
| **迄今產出之 TC** | **8 條，7 leaf** |
| **未動之 leaf** | **41 / 48**（`Splash Screen` 3、`Startup Animation` 9、`Startup Sounds` 6、`Power Transitions` 7、`Power Off Behavior` 8、`Voice Assistant Key` 5、`Off Road Plus` 2、外加 `Disclaimer Screen` 已完成 7） |
| 同期新增之裁決 | R-PMH50 → **R-PMH102**（**53 條**） |
| 同期新增之檢查程式 | 11 支 |
| **每輪之自評缺口** | **穩定 6 項** |
| **每輪結清之缺口** | **約 3 項** |

**缺口之產生率高於結清率，且已維持十六輪。**

### 2.2 但這不是白做的 —— 十六輪抓到了什麼

| 輪 | 實質發現 |
|---|---|
| 13 | batch 1 六類違規（單步驟 TC、`observe` 禁用動詞 8 處、造值） |
| 13 | A-PMH03 之 7.1 **漏句**（非重排）—— 單向比對之缺陷 |
| 14 | `SU9.)`／`SU9.1)` **不在 48 leaf 內** |
| 15 | marker 前綴漏 `DS`（分母 30 → 31） |
| 19 | 9.1 之 PDF 原句損壞（`aofnd`） |
| 22 | pop-up 組由「未對照」改判「牴觸」 |
| 24 | ER4(b) 之 `r45` 牴觸 —— 限定 4 → 7 項 |
| 27 | `-003` ER2 之 SUT 斷言**從未被掃** |

**每一項都是真的，且多數會直接產出錯誤之 TC。**

### 2.3 而 27 包之六項自評，其性質變了

| # | 自評 | 性質 |
|---|---|---|
| 1 | 21 個「待判定」被計入「已判定」之總數 | **計數失真 —— 實質** |
| 4 | PC 側只做關鍵詞式否定，全枚舉未做 | **母體未全 —— 實質** |
| 6 | `test_procedure` 之歸類未逐條驗證 | **歸類未驗 —— 實質** |
| 2 | 切分之連接詞清單仍是列舉 | 檢查可更細 |
| 3 | `SPLIT_REVIEW` 無第二來源 | 檢查可更細 |
| 5 | 規格側全枚舉未做（母體已量 235 行） | 已排程 |

**三項實質、三項「檢查可更細」。**
**十六輪以來，第一次出現「多數自評不指向產出可能有錯」。**

### 2.4 真正之阻斷不在這條線上

**`DR-PMH5`／`6`／`7` 之發出日期欄已第六次空著。**
ch 9（`Power Transitions`，7 leaf）因 `DR-PMH5` 而凍結 —— **而該 DR 從未發出。**

**十六輪之覆核不能解決一個需要對外發文才能解決的問題。**

---

## 三、裁決條文（逐條抄入 `RULINGS.md`）

```
R-PMH103（覆核線之收束判準）
一條覆核線（同一批產出之反覆查驗）於**該輪之自評項多數不指向
「產出可能有錯」而指向「檢查可更細」**時結束。

判別：逐項問「若此項不修，是否可能使某條 TC 之內容錯誤？」
  是 → 實質項，須處理；
  否 → 精化項，入 Phase 5 之待辦，**不阻斷開批**。

**精化項不因其未結清而使該批不可用** —— 其登記於 `DECISIONS.md`
之 KNOWN-INCOMPLETE，附其風險陳述。

依據：28 包 §2.3 —— 27 包之六項自評中三項實質、三項精化，
為 Phase 4 開批十六輪以來首次多數不指向產出。
同期產出 8 條 TC（7/48 leaf）而新增 53 條裁決、11 支檢查程式，
**缺口之產生率高於結清率且已維持十六輪**（§2.1）。
```

```
R-PMH104（apparatus 凍結）
自本包起，**不再新增檢查程式或檢查項**，除下列二情形：

(a) 某條已產出之 TC 經**實測**有誤（非「可能有誤」），且該誤為現行
    檢查所不能攔者；
(b) Pei 裁定。

現有 32 項 lint、13 支檢查程式、四詞記法、must-hit 註冊機制**全部保留並繼續執行**；
**凍結者為其增長，非其運作。**

**理由**：檢查之邊際產出已隨輪次遞減（§2.2 之發現集中於 13–24 包，
25–27 包所查出者為「檢查可更細」），而其成本為每輪一個完整往返。

**本條可由任一實測之 TC 缺陷解凍** —— 屆時新增之檢查須指名其所攔之
該項缺陷，不得泛化。
```

---

## 四、作業步驟

1. **抄錄** —— §三之 R-PMH103／R-PMH104 逐字抄入 `RULINGS.md`，附核對表。

2. **27 §12 之三項實質項（僅此三項，其餘入 KNOWN-INCOMPLETE）** ——
   (a) **第 1 項**：`popup`／`popup_after` 之 21 格改記
       **`待定義`**（其列層記法在 `matrix_vs_chapter.VERDICT`），
       使 `verdict_form.py` 之統計不再把「待判定」算入「已判定」；
   (b) **第 4 項**：PC 之 24 個斷言依 R-PMH98／R-PMH100 改為**全枚舉**
       （174 格全入判定表，落選記 `未對照` ＋ 謂詞域理由）；
   (c) **第 6 項**：`test_procedure` 之每一步驟依 R-PMH97 **逐條**判其二分，
       歸類與理由寫入常數；**`-007` 步驟 5 之「SUT 能接收該報導」是否為
       SUT 斷言，須具名判定**。

3. **其餘三項入 `DECISIONS.md` 之 KNOWN-INCOMPLETE**（R-PMH103）——
   切分之連接詞列舉／`SPLIT_REVIEW` 無第二來源／規格側全枚舉未做。
   **各附一句風險陳述**，不再排程。

4. **batch 1 之覆核結束宣告** —— 於 `DECISIONS.md` 記明：
   batch 1（8 條，7 leaf）之覆核線依 R-PMH103 結束；
   其殘餘項為上開三項精化 ＋ `-007` 之 `L160` 待確認（`DR-PMH7`）。
   **batch 1 仍不得寫回工作簿** —— 其阻斷改為單一項：
   **`tc_id` 為 provisional，待全 48 leaf 完成後單次指派**（12 包 §五）。

5. **batch 2 之產出 —— `Startup Sounds`（ch 8，6 leaf）** ——
   選批理由三項，皆可查：
   - **章 8 之材料最乾淨**：雙向複驗新漏 **0**（17 包）、
     矩陣全對照牴觸 **0**（22 包）、marker 6/6 全在 SYS1（14 包）；
   - **不受任何 DR 阻斷**（`DR-PMH5` 阻 ch 9、`DR-PMH6`／`7` 不阻批）；
   - 其 6 leaf 為 `SSND 1)`～`SSND 3)` 之展開，
     **含明確之三值列舉**（`Always`／`Once a Day`／`Never`），
     依 profile §4 之 split policy 各自成條 ＋ 一條負向。

   產出要求：
   - 每 leaf 必附 `source_clause`，**取自 PDF**（R-PMH50）；
   - 依 R-PMH94／R-PMH97／R-PMH101 **逐斷言**導出限定與掃描；
   - `tc_id` 續為 provisional；
   - **零寫回工作簿**。

6. **batch 2 之 lint 與掃描** —— 現有 32 項全跑；
   `spec_assertion_scan` 對 batch 2 之各斷言重跑（**不新增檢查**，R-PMH104）。

---

## 五、停止條件

canon §0 六條，另加本包三條：

7. batch 2 之任一斷言掃描發現**牴觸**（不限於新的 —— 其為新產出，
   任一牴觸皆須先解）
8. 步驟 2(c) 發現 `test_procedure` 之某步驟實為 SUT 斷言
   **且其未經任何掃描**
9. **本包新增任何檢查程式或檢查項**（R-PMH104 之自我檢查）

**本包零寫回工作簿。本包未由分析層授權提交**（R-PMH65）。
**ch 9（`Power Transitions`）不得開批**（`DR-PMH5` 未 `ANSWERED`）。
**不得改動 `scripts/new_feature.py`、`docs/runtime/`、任何他 feature 之檔案。**

---

## 六、上繳包要求（`docs/upstream/28_batch2.md`）

1. §三二條之抄錄核對表（含命中數）
2. 步驟 2 之三項實質項處理結果
3. 步驟 3 之 KNOWN-INCOMPLETE 三項（各附風險陳述）
4. 步驟 4 之覆核結束宣告
5. **batch 2 之 6 leaf、其 TC 全文、`source_clause`、逐斷言掃描**
6. lint 全跑（**32 項，不得增減**）
7. 由程式產生之檢查總表
8. 未結 DR 清單
9. **「本包是否仍有該驗而未驗者」之獨立判斷** —— 不得省略
10. 建議之 commit 訊息與 pathspec（**不執行**）＋ R-G6 之揭露表

---

## 七、待 Pei

| # | 事項 | 阻斷 |
|---|---|---|
| 1 | **三筆 DR 之發出** —— 發出日期欄已**第六次**空著；`DR-PMH5` 凍結 ch 9 之 7 leaf。**十六輪之覆核解決不了一個需要對外發文才能解決的問題** | **ch 9 之 7 leaf** |
| 2 | **R-PMH103／R-PMH104 之核可** —— 覆核線收束與 apparatus 凍結。**若你認為應繼續深化，一句話即可撤回本二條** | batch 2 之開批 |
| 3 | 9.1 之 `source_clause` 例外是否寫入 profile；17 §5.4 其餘五項；Q10 | 否 |

---

## 八、本包產生之新條文清單（自檢表）

| 條號 | 主旨 | 已以可貼區塊出現於 §三 |
|---|---|---|
| R-PMH103 | 覆核線之收束判準（實質項 vs 精化項） | ✅ |
| R-PMH104 | apparatus 凍結；其解凍條件 | ✅ |

二條各管一事。**本包未新增任何檢查程式或檢查項**（自檢：符合 R-PMH104）。
