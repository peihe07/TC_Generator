# 下放包 04 —— R-PMH10 證據基礎之更正、母體判準與機器檢查補實

- 日期：2026-08-23
- 方向：分析層（Claude Project）→ 執行層（Claude Code）
- feature slug：`power_moding`
- 對應上繳：`features/power_moding/docs/upstream/04_corpus_and_assertions.md`
- 前一包：[03_testgroup_and_dv.md](03_testgroup_and_dv.md) ＋
  [03a_pei_rulings.md](03a_pei_rulings.md)
  （上繳 [../upstream/03_testgroup_and_dv.md](../upstream/03_testgroup_and_dv.md)，已覆核）

---

## 一、03 包之覆核結果

**通過。** 十三節齊備，九條停止條件逐條有實據，`RULINGS.md` 前 12 條複驗
SHA256 全同。四項特別記明：

1. **§3 之自我更正為本輪最重要之一項** —— 「三方衝突」是我在 02 包覆核時
   照單轉述所造成之假問題。執行層指出其成因是把**建議**（`QS Suggestion`
   標題逐字為「25/10/15 QS確認後建議」）與**現況**、把 **037 的 Priority 欄**
   與 **036 的 Test Case Priority 欄**並列成三方。此更正成立。
2. **§6.1 之對照向（id-suffix 判準只得 27，漏 21）**證明 R-PMH1 之判準選對了，
   且證明另一個看似合理之判準會靜默漏掉 21 個 leaf。此為 R-G7-1 之正確用法。
3. **§8.2 之 A-PMH09（客戶那份帶 AMFM 血緣）為本輪最有價值之發現** ——
   但其表述須更正，見 §三。
4. **§9 五項自評誠實，其中第 5 項（`write_back` 無機器檢查）自陳
   「目前只是文字修補」而未自我核可為 RESOLVED** —— 通則 8 之正確應用。

---

## 二、分析層之獨立複驗（三項，皆自行實測）

### 2.1 母本 DV —— 相符，但口徑過廣（→ A-PMH11）

**量測條件**：對 `forms/…_20260817_ext.xlsx` 之 zip 成員
`xl/worksheets/*.xml` **全部**（非僅 sheet6）以正規式抽
`<dataValidation>` 與 `<x14:dataValidation>`。

`sheet6`（`Test Case Specification`）之三組 legacy ＋ 一組 x14
**與上繳所報逐字相符**，含 `AF` 之前導空白（`formula1` 逐字為
`"Pass, Fail, Pending,Block,NA"`）與 `P10:Q1411` 跨兩欄之 sqref。

**但另有一組未被報告**：

| 分頁 | sqref | type | formula1 |
|---|---|---|---|
| `sheet5`（`Product Document 記錄封面頁`） | `B7:C7` | `list` | `"Confidential, Top Secret"` |

上繳 §2.2 末句逐字為「**母本之 DV 總數 = 4 組**（legacy 3 ＋ x14 1），
**除此之外無其他 DV**」。該量測之範圍是**單一分頁**，而該句之量詞是
**整本活頁簿**。實測全簿為 **5 組**。

**不是量錯，是量詞與量測範圍不一致** —— 登記為 A-PMH11，並立 R-PMH20。

### 2.2 `priority` 欄分布 —— 相符

**量測條件**：四份交付件，`P` 欄（rev C），r10 起 `D` 欄非空之列。

| 交付件 | 列 | P0 | P1 | P2 | P3 | 空 | 逸出 |
|---|---|---|---|---|---|---|---|
| User Profiles | 189 | 38 | 66 | 71 | 14 | 0 | 0 |
| Comfort | 466 | 325 | 132 | 8 | 0 | 1 | 0 |
| Time Management | 59 | 30 | 27 | 2 | 0 | 0 | 0 |
| Power Management | 284 | 180 | 59 | 7 | 37 | 1 | 0 |

**與上繳逐格相符，逸出 0。停止條件 7 確未觸發。**

### 2.3 A-PMH09 —— 推論方向對，但**結論須更正**

上繳 §9 第 3 項請分析層判斷是否值得查 AMFM 側。**已查。**

**量測條件**：`/Users/peihe/Work/…/ASW-R2/AM:FM/…_SWQT_AMFM_20260810.xlsx`
（我方交付件），`openpyxl` 唯讀。

| 項 | AMFM 交付件實測 | 客戶那份（01 包所測） |
|---|---|---|
| 欄數 | **34（rev C）** | 35 |
| `Cover!D6` 版本 | **`C`** | `A` |
| `ChangeHistory` ver C | **表單原文**（「新增欄位：預估測試時間(分鐘)」） | **被覆寫**為寫回註記 |
| `D5` 範圍 Scope | **`SWE1_AMFM_FM-WI-FSM-037-A03 STLA 報告_SWRA STLA Report_SWRA_20260323`** | **空白** |
| 資料列 | 298 | 48 |
| `D5:F5` 合併 | 有 | 有 |

**更正**：「客戶那份**衍生自 AMFM 交付件**」**不成立** —— AMFM 之交付件為
34 欄、ChangeHistory 未被覆寫、`D5` 已填。二者不是同一份之複本。

**成立者**：客戶那份帶 **AMFM 中繼產物**之血緣（某個寫回註記已寫入、
但未進入交付之工作副本），其後被清空內容並貼入 48 列 037 資料。
證據仍是那三項，只是其來源被指向中繼態而非交付態。

**故無須回報 AMFM feature** —— 其交付件乾淨，無缺陷可報。
執行層之提案 (c) **不執行**，理由記於本節。

---

## 三、**R-PMH10 之證據基礎不完整 —— 須上呈 Pei 重裁 Q3**

### 3.1 反例

上一段 §2.3 之量測順帶推翻了 R-PMH10 之依據：

> R-PMH10 逐字：「依據為實測：已交付件 User Profiles／Comfort／
> Time Management／Power Management 四份之該三欄皆空，母本亦空 ——
> **語料 5/5 無一填寫**。」

**AMFM 20260810 之 `D5` 非空**，且其值正是**指向該 feature 之 SWRA 報告全名**
（`SWE1_AMFM_FM-WI-FSM-037-A03 STLA 報告_SWRA STLA Report_SWRA_20260323`）。
該件是**我方交付件**，就在同一個 `ASW-R2` 樹下（`AM:FM/`），
**我當初取樣四份時漏了它**。

### 3.2 更嚴重者：母體從未被定義

我以 `**/*036*.xlsx` 掃 `ASW-R2` 全樹，得 **28 個檔**，其中包含：

- 中間版本（`(Review)`／`(Refine)`／`(Revise)`／`(done)`／`_Rebuilt`）
- 同一夾之多版本（Power Management 有 `20260820` 與 `20260821` 兩份；
  Notifications HMI 有三份；Engineering Mode 有六份）
- 非交付態產物（`REF/036_pre_writeback_20260823.xlsx`、
  `VF230_V1_R5/output/…`）

**「已交付件 5/5」這個比率之分母從未被定義**，而 R-G8 明訂缺判準之比率
不予採認 —— **我自己違反了我在 R-PMH14 裡剛寫下的東西**。R-PMH14 談的是
「分母要能分辨候選各案」，而這裡連分母是哪些檔都沒界定。

### 3.3 處置

- **R-PMH10 現狀維持有效**（D3／D4／D5 留空），**但標 `[PEI-REOPEN]`** ——
  Pei 於 2026-08-23 就 Q3 所作之「留空」裁定，係基於我提供之不完整語料，
  **我不得自行改判**；
- 本包步驟 2 先以 R-PMH19 之母體判準取全量實測，**取得完整語料後再請 Pei 裁**；
- 在 Pei 重裁前，**D3／D4／D5 一律不寫入任何值**（與現行 R-PMH10 之行為相同，
  故本項不阻斷任何後續步驟）。

---

## 四、裁決條文（逐條抄入 `RULINGS.md`）

```
R-PMH19（已交付件語料之母體判準）
以「已交付件」為據之任何陳述，其母體為：`ASW-R2` 樹下符合下列全部條件
之 036 檔案 ——

(a) 位於某一交付夾之根層（不在 `REF/`、`output/`、`validation/`
    或任何子目錄內）；
(b) 檔名不含中間態標記：`(Review)`、`(Revise)`、`(Refine)`、`(done)`、
    `_Rebuilt`、`pre_writeback`；
(c) 同一交付夾內有多份符合 (a)(b) 者，取檔名日期最大之一份，
    其餘列為「同夾舊版」並具名排除。

陳述時須同時載明：母體清單（逐檔全路徑）、依 (a)(b)(c) 各排除幾份、
以及分子之計數方式。未載明者，其比率不予採認（R-G8）。
```

```
R-PMH20（量詞與量測範圍須一致）
任何帶全稱量詞之陳述（「全部」「總數」「除此之外無」「N/N」），
其量測範圍必須等同該量詞所涵蓋之範圍。

分頁層之量測不得寫成活頁簿層之結論；單一 feature 之量測不得寫成
全案之結論；樣本之量測不得寫成母體之結論。

若量測範圍小於量詞，二者擇一修正：縮小量詞，或擴大量測。
不得以「實務上不會有別的」為由保留較大之量詞。

依據：A-PMH11（分頁層量測寫成全簿結論，實測全簿為 5 組而非 4 組）；
以及 R-PMH10 之成因（四份樣本寫成「語料 5/5」，實際母體未定義）。
```

```
R-PMH21（規格文字量不作完整性判準）
規格 PDF 之抽取字元數不得作為規格完整性、抽取正確性或版本一致性之判準。

`pymupdf` 得 15,618 chars 而 `pdftotext -layout` 正規化後得 15,167 chars
（差 451，約 3%）—— 二者皆為正常結果，差異來自換行與空白之正規化策略，
非內容差異。

本條不要求追查該 3% 之成因。完整性之判準為
`outline_map.json` 之 29/29 章節命中與 48/48 leaf 全解，該二者已成立。
```

```
R-PMH22（write_back 之機器檢查）
`write_back` 之 `mode: append` 與 `first_row: 10` 須有機器可執行之檢查，
於每次寫回前自動驗證，失敗即中止寫回。

最低要求三項：
(a) 目標分頁自 `first_row` 起至寫回前之最後一列，`D` 欄全空
    （BLANK 之前提仍成立）；
(b) 寫回之起始列 == `first_row`，不得由任何其他來源推導
    （特別是 `outline_map.json` 之 `row_036_customer`，該欄記的是
    客戶那份之列號）；
(c) 寫回後之列數 == 寫回前之列數 ＋ 本批 TC 數。

`feature.yaml` 之註解與本條文本身**都不構成本條之滿足**（通則 8：
文字修補不構成 RESOLVED）。本條之 RESOLVED 條件為：檢查已實作、
且已以一次**故意失敗**之測試證明其會攔下（檢查項須確認其在該階段
確實可能失敗）。
```

```
R-PMH23（客戶那份之可用範圍收緊）
客戶交付夾之 `…_PowerModingHMI_20260819.xlsx`（R-PMH7 所定之「來源複本」）
之下列分頁**一律不得取用**：

  `Cover 封面`、`ChangeHistory 修訂履歷`、`Product Document 記錄封面頁`、
  `Cover_old`、`ChangeHistory_old`

理由：其 `ChangeHistory` ver C 已被他 feature（AMFM）之中繼寫回註記覆寫，
`Cover!D6` 版本標為 `A` 而其 ChangeHistory 有 A/B/C 三列（檔案自相矛盾）。
取用其任一格，等同把他 feature 之修訂履歷帶入本 feature 之交付物。

R-PMH7 所稱之「附屬分頁」自本條起明確限定為三頁：
`Reference`、`QS Suggestion`、`Test Case Framework`。
```

---

## 五、作業步驟

1. **抄錄** —— §四五條（R-PMH19 ~ R-PMH23）逐字抄入 `RULINGS.md`，附核對表。
   R-PMH10 條後加 `[PEI-REOPEN]` 標記與其理由（**原文不改字**）。
   新增 A-PMH11（量詞與量測範圍不一致），A-PMH09 之內文依 §2.3 更正其結論段
   並標 RESOLVED（成因已查明、影響為零、無須外部回報）。

2. **D3／D4／D5 全母體實測（本包首要）** —— 依 R-PMH19 建立母體：
   先以 `**/*036*.xlsx` 掃 `ASW-R2` 全樹取得候選全集（分析層實測為 28 檔，
   **須自行重掃，不得沿用**），再依 (a)(b)(c) 逐條排除並具名列出被排除者。

   對母體每一檔實測並列表：`D3`／`D4`／`D5` 之值（逐字，空者記 `<empty>`）、
   欄數、`Cover!D6` 版本、資料列數。

   **回報格式須讓 Pei 能直接據以裁定**：非空者逐一列出其 `D5` 全文，
   並標注該值指向何物（SWRA 報告全名／規格文件名／其他）。
   **不提案、不歸納慣例** —— Q3 之重裁屬 Pei。

3. **`write_back` 檢查之實作（R-PMH22）** —— 於本 feature `scripts/` 下
   實作三項檢查，並附**故意失敗之測試**各一：
   (a) 在 `first_row` 之前插一列假資料 → 檢查須攔下；
   (b) 令起始列 ≠ `first_row` → 檢查須攔下；
   (c) 令寫回後列數少一 → 檢查須攔下。
   三項皆須實際跑過並貼出其攔下之輸出。**未經故意失敗驗證者不得標 RESOLVED。**

4. **未引用 outline 之機器產出** —— 產出 `data/uncited_sections.tsv`：
   52 個 outline 中未被任何 leaf 引用之 23 項，逐項含
   `outline_number`／`title`／`層級`／`分類`（章節層節點／Assumptions／
   圖片佔位／其他）。
   **分類欄以規則產生（可重跑），不得以人讀結論填寫。**
   `classify_uncited_sections.py` 若不存在則新寫一支，置於本 feature
   `scripts/` 下；**不得改動 `scripts/recon.py` 或任何共用腳本**。
   產出後與上繳 §7.3C 之人讀結論比對，**不符者以機器產出為準並回報差異**。

5. **A-PMH11 之全簿 DV 複驗** —— 對母本**全部**分頁重掃 DV
   （legacy ＋ x14），列出全簿清單並標明每組所屬分頁。
   同法對客戶那份掃一次，回報二者差異。
   上繳 §2.2 之結論句依 R-PMH20 改寫為分頁層陳述。

6. **`Q` 與 `AF` 兩欄之瑕疵登記** —— 依上繳 §9 第 2 項，登記為 A-PMH12：
   `Q`（Estimated Test Time）套用 `"P0,P1,P2,P3"` 下拉、
   `AF` 列舉含前導空白。**本包不提解法**，但須於 `DECISIONS.md` 標為
   Phase 6／7 之前置阻斷項（首次填值前必須處理）。

7. **`INDEX.md` 與 `PLAYBOOK.md` §6 狀態板同步** —— Phase 1 已完成，
   下一步為 Phase 2／3（framework）。狀態板須反映 `[PEI-REOPEN]` 之 Q3。

---

## 六、停止條件

canon §0 六條，另加本包三條：

7. R-PMH19 之母體建立後，符合條件之檔數 < 5（母體過小則任何比率皆無意義，
   停並回報實際檔數與排除明細）
8. 步驟 3 之三項故意失敗測試有任一項**未被攔下**
9. 步驟 4 之機器分類與 §7.3C 之人讀結論不符，且差異涉及「帶實質需求文字
   之節點被判為未引用」

**本包零寫回工作簿。** 全部改狀態 git 屬 Pei（R-G5）。
**不得改動 `scripts/new_feature.py`**（03a §四之禁止項，仍在）。

---

## 七、上繳包要求（`docs/upstream/04_corpus_and_assertions.md`）

1. §四五條之抄錄核對表 ＋ R-PMH10 之 `[PEI-REOPEN]` 標記落實證明
   （原文 SHA256 未變）
2. 步驟 2 之母體清單（逐檔全路徑）、排除明細（依 (a)(b)(c) 各幾份）、
   `D3`／`D4`／`D5` 全量表、非空者之逐字全文
3. 步驟 3 之三支檢查程式碼路徑 ＋ 三次故意失敗之實際輸出
4. `data/uncited_sections.tsv` ＋ 與人讀結論之比對結果
5. 步驟 5 之全簿 DV 清單（母本／客戶那份，分頁標明）＋ 差異
6. A-PMH11／A-PMH12 之登記
7. `INDEX.md`／`PLAYBOOK.md` §6 之更新
8. **「本包是否仍有該驗而未驗者」之獨立判斷** —— 不得省略
9. 建議之 commit 訊息與 pathspec（**不執行**）＋ R-G6 之唯讀／改狀態分列

---

## 八、本包產生之新條文清單（自檢表）

| 條號 | 主旨 | 已以可貼區塊出現於 §四 |
|---|---|---|
| R-PMH19 | 已交付件語料之母體判準（三條排除規則 ＋ 揭露義務） | ✅ |
| R-PMH20 | 量詞與量測範圍須一致 | ✅ |
| R-PMH21 | 規格文字量不作完整性判準 | ✅ |
| R-PMH22 | `write_back` 之機器檢查（含故意失敗之驗證要求） | ✅ |
| R-PMH23 | 客戶那份之封面五頁禁用 | ✅ |

五條各管一事。

**待 Pei 者一項**：Q3 之重裁（D3／D4／D5 是否留空），俟步驟 2 之全母體
實測回報後進行。**在此之前該三欄一律不寫入，故不阻斷任何步驟。**
