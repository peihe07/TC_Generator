# 下放包 17 —— 「只列不改」之界線、質疑型條文之母體與 A-PMH14 之修正

- 日期：2026-08-24
- 方向：分析層（Claude Project）→ 執行層（Claude Code）
- feature slug：`power_moding`
- 對應上繳：`features/power_moding/docs/upstream/17_scope_of_inventory.md`
- 前一包：[16_verdict_blindspot.md](16_verdict_blindspot.md)
  （上繳 [../upstream/16_verdict_blindspot.md](../upstream/16_verdict_blindspot.md)）

---

## 一、16 包之覆核結果 —— **通過**

三條停止條件全未觸發；must-hit D（`SU` 改判 `noise` → 升為須人讀）攔下；
兩份萃取 marker 集合逐項相等；lint 30/30；七支檢查全綠。

**三項特別記明**：

1. **§4.2 為本輪最有價值之發現** —— 候選集合本身隨萃取器而變
   （PyMuPDF 多出 `Loading`／`each` 兩個候選，源自 p9 流程圖標籤之
   閱讀順序差異）。執行層之總結精準：
   **「14 包之前提是前綴清單 → 15 包移到候選形態 → 16 包移到萃取器；
   每一階都消滅了下一階的錯，也都留下自己這一階的前提。」**
2. **§2.2 之分離本身是結果** —— 三個 `xref`（`CR`／`CTS`／`DCR`）被旗標
   而九個 `noise` 零命中，且執行層具名其為**已知偽陽形態**而非判定有誤。
   未把偽陽當成發現，也未因偽陽而放寬判準。
3. **§4.4 之勘誤** —— 自查出「該工具差異早於 03 包上繳 §6.2 即已記載
   （明載為不同工具之差異，非衝突）」，即 **15 包是未回查既有記載**。
   **一個疑慮被提出兩次而第一次的答案還在檔案裡。**

---

## 二、§10 第 3 項 —— **執行層是對的，我的指示劃錯了界線**

執行層依「只列不改」之令未動 A-PMH14，但據實記明其認為該改：

> `ANOMALIES.md` 現在同時寫著「9.1 條列再流維持」與「9.1 之狀態矩陣全缺」。
> **只列不改使該矛盾繼續留在檔案裡**，任何人下次讀 A-PMH14 之結語
> 都會讀到那句「維持」。

**指認成立。我的步驟 5 把兩件不同的事綁在同一個「只列不改」之下**：

| 事 | 適用「只列不改」？ |
|---|---|
| **判準單向套用之盤點清單** —— 一份待辦，其項目尚未決定如何處置 | **是** —— 未經裁定即改動，等同自行決定處置 |
| **`ANOMALIES.md` 中一句已知不實之陳述** —— 一個現存之錯誤，非待辦 | **否** —— 它每被讀一次就誤導一次 |

**且該矛盾已由既有條文所涵蓋**：R-PMH45（同檔內互斥狀態陳述須一致）與
R-PMH43（不實陳述須更正）。**我的「只列不改」實際上暫停了兩條既有條文。**

→ R-PMH63。**A-PMH14 之修正列為本包步驟 2。**

---

## 三、§10 第 4 項 —— 質疑型條文之母體，判準我給

執行層自陳：R-PMH62 之回溯只查了四條，而 `RULINGS.md` 現有 62 條，
**「我沒有一個可以自動判定『哪些條文屬於質疑型』之判準」**。

**該判準可機械化，且其材料已在條文內。** 本 repo 之條文一律含「依據」段，
而質疑型條文之依據段必然載明其所推翻之對象。判準如下：

**條文之依據段含下列任一標記者，即為質疑型**：

```
不成立／作廢／撤回／改判／取代／推翻／未套用／誤用／
判錯／不符／矛盾／由…查出／之錯／之缺陷／之瑕疵
```

以此掃 `RULINGS.md` 全 62 條，其命中集合即 R-PMH62 之回溯母體。
**該判準會有偽陽**（例如條文只是引用他處之錯作為例證），
**故其輸出為候選清單，逐條由人確認** —— 與 `VERDICT` 之處理相同。

→ R-PMH64。

---

## 四、我自己違反了 R-PMH48 —— 而且該條不夠

16 包 §八／§十記「14／15／16 三包之提交**未授權**」，
而 14／15 已於 2026-08-24 經 Pei 授權並提交（`99b4269`）。

**R-PMH48 是我立的條**，其內容為「下放包不得記載 git 之**提交狀態**，
若需觸及只寫**授權與否**」。我寫的是「未授權」—— 形式上合規。

**但該條不夠**：**授權亦為 Pei 之行為，同樣發生在撰包時點之外，
同樣會過時。** R-PMH48 只擋了「已否提交」，沒擋「已否授權」，
而二者過時之機制完全相同。

**正解**：下放包只得記載**分析層自身之行為** ——
「本包未由分析層授權提交」為分析層可知且永不過時之陳述；
「Pei 是否已授權」則永遠須由執行層於執行時實地確認。

→ R-PMH65。

---

## 五、裁決條文（逐條抄入 `RULINGS.md`）

```
R-PMH63（「只列不改」之適用範圍）
「只列不改」之指示，其適用範圍限於**尚未決定處置之待辦盤點**；
**不適用於檔案中已知不實或自相矛盾之陳述**。

判別：該項若為「一件待決定之事」→ 只列不改，等候裁定；
該項若為「一句現存於檔案而每被讀一次就誤導一次之話」→ **立即更正**，
其更正方式依 R-PMH44（原文保留、附勘誤或結論改寫並保留原句）。

下放包不得以「只列不改」之措詞暫停 R-PMH43（不實陳述須更正）
或 R-PMH45（同檔內互斥狀態須一致）—— 若確有暫停之必要，
須具名該二條並載其理由。

依據：17 包步驟 5 之「只列不改」使 `ANOMALIES.md` 之 A-PMH14 同時寫著
「9.1 條列再流維持」與「9.1 之狀態矩陣全缺」而未被更正；
由執行層於 16 包 §10 第 3 項指出（17 包 §二）。
```

```
R-PMH64（質疑型條文之機械判準）
R-PMH62 之回溯自套，其母體以下列判準自 `RULINGS.md` 產生，不得人工挑選：

條文之依據段含下列任一標記者，即列為質疑型之候選 ——
`不成立`／`作廢`／`撤回`／`改判`／`取代`／`推翻`／`未套用`／`誤用`／
`判錯`／`不符`／`矛盾`／`由…查出`／`之錯`／`之缺陷`／`之瑕疵`

該判準會有偽陽（條文僅引他處之錯為例證者），**故其輸出為候選清單，
逐條由人確認**，與 `VERDICT` 之處理相同（R-PMH57／R-PMH61）。

判準與其偽陽率須於每次回溯時一併回報；**候選數為 0 者視為判準失效**，
不得視為「無質疑型條文」。

依據：16 包 §10 第 4 項 —— 執行層自陳其回溯只查了四條而無自動判定之判準，
故所交清單之完整性無法主張。
```

```
R-PMH65（下放包只得記載分析層自身之行為）
下放包對 git 之記載，限於**分析層自身之行為**：
「本包未由分析層授權提交」為可知且永不過時之陳述。

**不得記載 Pei 之授權狀態**（「未授權」「已授權」）——
授權為 Pei 之行為，發生於撰包時點之外，與提交狀態同樣會過時。
Pei 是否已授權，一律由執行層於執行時實地確認並於上繳回報。

本條擴充 R-PMH48：該條只擋了「已否提交」，未擋「已否授權」，
而二者過時之機制完全相同。

依據：16 包 §八／§十記「14／15／16 三包之提交未授權」，
而 14／15 已於同日經 Pei 授權並提交（`99b4269`）——
該陳述形式上合於 R-PMH48 而實質上仍然過時（17 包 §四）。
```

---

## 六、作業步驟

1. **抄錄** —— §五之 R-PMH63 ~ R-PMH65 逐字抄入 `RULINGS.md`，附核對表
   （依 R-PMH41 驗命中數）。

2. **A-PMH14 之修正（R-PMH63）** —— 其結語現稱
   「9.1／11.1 —— 條列再流，**維持**（本輪方向二未在其上查出新漏）」，
   而同一則之新漏 2 查出的正是 9.1 之狀態矩陣全缺。
   依 R-PMH44 之形態：**原句保留**，其後加更正段載明
   （a）9.1 之「維持」不成立，其成因為沿用 12 包「其餘三則不變」之語
   而未對其套用同一判準（R-PMH62 之形態）；
   （b）11.1 之「維持」**經 16 包 §5.1 確認為已做雙向複驗且成立**，不受影響；
   （c）**outline `8` 至今未做雙向複驗**，其「拼字」之歸因不得引用（R-PMH51）。

3. **outline `8` 之雙向複驗（R-PMH51 之未套用側）** ——
   對章 8（`SSND 1)`～`SSND 3)`，6 個 marker）以雙向法比對 PDF 與 SYS1：
   - 方向一：SYS1 之每一句是否出現於 PDF；
   - **方向二：PDF 之每一句是否出現於 SYS1**；
   - 逐則回報二方向之結果，漏句者逐句列出逐字內容與頁次。
   **此為本包最高優先** —— 章 8 之 6 leaf 屬 `Startup Sounds` 組，
   為 batch 2 之候選，其材料完整性須先於開批確定。

4. **R-PMH52 之擴及四支檢查（§5.3 之單向套用）** ——
   `check_granularity.py`／`check_write_back.py`／`marker_coverage.py`
   三支之輸出各增「本檢查未涵蓋之範圍」具名段
   （`check_state_consistency.py` 已有 `EXCLUDED` 具名，補齊格式即可）。
   **具名之內容須為該檢查之限度，不得寫成一般性免責。**
   `marker_coverage.py` 之限度已於 16 §2.2／§4.2 寫成文字，
   **本輪改為程式輸出**。

5. **R-PMH64 之回溯（母體版）** —— 依判準自 `RULINGS.md` 65 條產生候選清單，
   逐條確認為質疑型或偽陽，對確認者檢查其是否曾被回頭套用於支持該質疑之
   其他項。**回報判準之命中數、偽陽數、以及未套用之項。**
   本輪**只列不改** —— 但依 R-PMH63，若其中任一項為「檔案中已知不實之陳述」
   而非「待決定之事」，**該項須立即更正並具名**。

6. **語氣檢查之對照窗口（16 §10 第 1 項）** —— 增取「其前」180 字元之
   對照窗口，比較兩窗口之旗標集合；不同者具名。
   **只回報，不改現行窗口之選擇。**

7. **`MANIFEST.sha256` 之複驗（16 §10 第 2 項）** —— 跑 `shasum -c`，
   確認四份素材（尤其 PDF 本體）自 01 包搬入後未變。

---

## 七、停止條件

canon §0 六條，另加本包三條：

7. 步驟 3 之章 8 雙向複驗發現**任一新漏句**
8. 步驟 5 之候選清單為 0（依 R-PMH64 視為判準失效）
9. 步驟 7 之 `shasum -c` 有任一份不符

**本包零寫回工作簿。本包未由分析層授權提交**（R-PMH65）。
**不得改動 `scripts/new_feature.py`、`docs/runtime/` 下任何檔案、
任何他 feature 之檔案。**

---

## 八、上繳包要求（`docs/upstream/17_scope_of_inventory.md`）

1. §五三條之抄錄核對表（含命中數）
2. A-PMH14 之修正全文（原句保留之證明）
3. **章 8 之雙向複驗全表** —— 逐則、逐方向、漏句逐字
4. 四支檢查之限度具名段（實際輸出）
5. R-PMH64 之候選清單 ＋ 命中／偽陽數 ＋ 未套用項 ＋ 依 R-PMH63 立即更正者
6. 語氣檢查之雙窗口對照
7. `shasum -c` 之輸出
8. lint 全跑輸出
9. 未結 DR 清單
10. **「本包是否仍有該驗而未驗者」之獨立判斷** —— 不得省略
11. 建議之 commit 訊息與 pathspec（**不執行**）＋ R-G6 之揭露表

---

## 九、待 Pei

| # | 事項 | 阻斷 |
|---|---|---|
| 1 | **三筆 DR 之發出**（DR-PMH1／2／3）—— **執行層已第四度重申**。DR-PMH2 本輪多一項證據：A-PMH14 對 9.1 之記載自相矛盾，而 DR-PMH2 所索取者正是該矩陣 | **DR-PMH1 阻斷交付** |
| 2 | 16／17 之 commit 授權 | 否 |
| 3 | 16 §8 條件 9 之三項是否排入 Phase 5 前 —— **本包已排入步驟 3、4**，餘 A-PMH03 之 `8` 亦在步驟 3 | 否 |
| 4 | Q10、`PROFILE_INTEGRATION.md` | 否 |

---

## 十、本包產生之新條文清單（自檢表）

| 條號 | 主旨 | 已以可貼區塊出現於 §五 |
|---|---|---|
| R-PMH63 | 「只列不改」限於待辦盤點，不適用於已知不實之陳述 | ✅ |
| R-PMH64 | 質疑型條文之機械判準（回溯自套之母體） | ✅ |
| R-PMH65 | 下放包只得記載分析層自身之行為（擴充 R-PMH48） | ✅ |

三條各管一事。
