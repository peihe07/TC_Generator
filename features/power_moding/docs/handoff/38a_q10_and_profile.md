# 下放包 38a —— Q10 定案與 profile §1.2 之落檔授權（與 38 同一往返）

- 日期：2026-08-25
- 方向：分析層（Claude Project）→ 執行層（Claude Code）
- **本檔不另佔往返編號** —— 與 [38_phase5.md](38_phase5.md) 同屬第 38 輪
- 38 本文未改一字

---

## 一、Pei 之裁定（2026-08-25，逐字）

> 「1 不要」
> 「但只有這裡是例外啊 那就寫吧」

即：**Q10 不填**；**profile 增 §1.2 記 9.1 之例外**（R-PMH75 維持有效）。

**解讀之確認**：第二句採前述之（甲）＋寫入 —— **不撤回 R-PMH75**，
9.1 之 `source_clause` 繼續取 SYS1；所寫者為使 profile 與 `RULINGS.md` 一致。

---

## 二、裁決條文（逐條抄入 `RULINGS.md`）

```
R-PMH145（Q10 定案 —— `Product Document 記錄封面頁` 不填）
`Product Document 記錄封面頁` **整張分頁不填**，維持母本現況（僅標籤、值全空）。

Pei 於 2026-08-25 裁定。

**其語料與 R-PMH27 之不同須記明**：D5 之語料為 9 空／7 非空，
本項為 **4 空／12 填** —— **本裁定明知其為語料之少數側**。
其依據非多數，而為三項成本：
(a) 五個欄位之字串須由我方自擬（`B3` `new R1L`(8) vs `NR1L`(4)、`B4` 文件名、
    `B5` `V1.0`(11) vs `Initial`(1)、`B8` 日期格式、`A13:D13` 修訂列）；
(b) `B4` 取檔名即依賴上游命名，**與 R-PMH26(d) 相衝**；
    其值不得自客戶那份複製，**與 R-PMH23 相衝**；
(c) `B7` 帶 DV 而 `check_write_back` 之三項不涵蓋之。

**其為交付揭露事項** —— 入 R-PMH132(b) 之「已知未決清單」一節，
載明「本欄依裁定留空，與 12/16 之語料多數不同」，
**使交付方一望即知其為選擇而非遺漏**。
```

```
R-PMH146（profile §1.2 之落檔 —— 一次性授權）
Pei 於 2026-08-25 授權於
`docs/runtime/profiles/FW036_R1L_PowerModing_Profile.md` **增一節 §1.2**，
其內容為 38a §三之全文，**逐字，一次**。

**明文不授權**：該檔之其他任何節、`docs/runtime/` 下之其他任何檔案。
**R-PMH46 之一次性授權已用畢，本條為第二次且獨立**。

**驗證義務**：寫入後以逐字比對證明其與 §三之區塊相同，
並附該檔之前後 SHA256 與行數變化；**§1 原文不得改一字**。

**其必要性**：R-PMH75（9.1 之 `source_clause` 取 SYS1）只記於 `RULINGS.md`，
而 profile §1 仍載「判讀基準為 PDF、SYS1 為追溯用」。
**profile 為給未來讀者與其他 session 之規則書** ——
依其字面「修正」該 8 條之 `source_clause_origin`，
**恰會把 A-PMH16 所查出、SYS1 已刪之舊文字放回**
（`for 60 seconds`／`the radio should shut Off`）。

**本條不改變 R-PMH75 之效力**，只使規則書與實作一致。
```

---

## 三、profile §1.2 之逐字（**加於 §1.1 之後、§2 之前**）

```markdown
### 1.2 R-PMH50 之例外 —— outline 9.1 [ADD]

outline `9.1`（`SWE1-HMI-PM-018-01`～`-05`，5 leaf）之 `source_clause`
**取自 SYS1 匯出，非 PDF** —— R-PMH50 於此反轉。

依據：R-PMH75（Pei 裁定 2026-08-25，`DR-PMH4` 之答覆「以刪掉之後的為主」）。
PDF 之該句本身損壞（含 `aofnd`、兩個主謂結構相連而無連接詞、
新舊兩版疊寫），而 SYS1 之版本恰為刪去舊文字後之定稿。

`source_clause_origin` 逐字為 `sys1_export 9.1`。

**R-PMH50 於其餘 40 leaf 維持不變** —— 本例外**只此一處**。

⚠ **不得依 §1 之通則「修正」此 5 leaf**：依 §1 改回 PDF，
即把 A-PMH16 所查出、SYS1 已刪之舊文字放回
（`for 60 seconds`／`the radio should shut Off the`），
**其為已被裁定排除之內容**。

**承擔之風險已具名**（R-PMH75）：`the radio should shut Off`
（逾時後收音機關機）不會有任何一條 TC 驗到；
若上游日後主張該行為仍屬需求，ch 9 之覆蓋即有缺口。
```

---

## 四、對 38 包之影響

| 38 之節 | 原狀態 | 本檔之後 |
|---|---|---|
| §四 步驟 6（寫回之欄位） | 未提 `Product Document` | **明定不填**（R-PMH145）；寫回之範圍限 `Test Case Specification` 一分頁 |
| §七 之後兩輪 | 39 輪含 Q10 與 profile 例外 | **二者於本輪解決**，39 輪只餘 `Cover 封面` 署名欄與 17 §5.4 其餘五項 |
| §八 待 Pei 第 2、3 項 | 待裁 | **已裁** |
| 其餘步驟 | | 不受影響 |

### 4.1 增列之作業步驟

8. **profile §1.2 之落檔（R-PMH146）** —— 依 §三逐字，一次；
   附前後 SHA256、行數變化、逐字比對證明；**§1 原文不得改一字**。
   **`PROFILE_INTEGRATION.md` 仍不得動**（R-PMH46 之明文不授權，至今未解）。

9. **Q10 之交付揭露登記（R-PMH145）** —— 入「已知未決清單」，
   載其「依裁定留空，與 12/16 之語料多數不同」。

---

## 五、本檔產生之新條文清單（自檢表）

| 條號 | 主旨 | 已以可貼區塊出現於 §二 |
|---|---|---|
| R-PMH145 | Q10 定案：不填；其為語料少數側之選擇，須揭露 | ✅ |
| R-PMH146 | profile §1.2 之一次性落檔授權 | ✅ |

二條各管一事。**本檔未新增任何檢查程式或檢查項**（符合 R-PMH104）。

**待 Pei 者一項**：`DR-PMH8`（9 問 ＋ 更正句）之發出 ＋ 日期與對象。
