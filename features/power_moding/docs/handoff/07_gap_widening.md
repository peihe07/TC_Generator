# 下放包 07 —— tie-break、granularity 檢查與跨 feature 缺口擴查

- 日期：2026-08-24
- 方向：分析層（Claude Project）→ 執行層（Claude Code）
- feature slug：`power_moding`
- 對應上繳：`features/power_moding/docs/upstream/07_gap_widening.md`
- 前一包：[06_framework_proposal.md](06_framework_proposal.md)
  （上繳 [../upstream/06_framework_proposal.md](../upstream/06_framework_proposal.md)，已覆核）

---

## 一、06 包之覆核結果

**通過。** 九條停止條件逐條有實據，母體 16 ＋ 量測時點齊備，
Layer 2 複算 48/48 八組全符、餘數 0，R-PMH27 原文 `e6e14fc0a96c1ccc` 未變。

四項特別記明：

1. **§2.4 之 (c) 平手** —— R-PMH31 把 `_Rebuilt` 自 (b) 移除後，該檔改由 (c)
   排除，而 (c) 之判準是「檔名日期最大」，兩檔同為 `20260816` ——
   **(c) 無鑑別力，實際取捨由排序之實作細節決定**。
   **我上一包移除 (b) 之兩項時，沒有檢查移除後由誰接手、接手者是否接得住。**
   這是條文修訂之連帶檢查缺失，記為分析層之瑕疵。
2. **§4.1 指出我 06 §六之前提不成立** —— 我寫「若已涵蓋，(ii) 成立且無缺口；
   若未涵蓋，則為真缺口」，實測為**零命中**，故 (ii) 之形態由
   「已由他 feature 涵蓋故不重複」變為「**兩邊都沒有**」。指認正確。
3. **§5.2 指出 Q10 之範圍被我低估** —— 不是一格，是**一整張分頁**，
   且填寫呈**全有全無**（12 檔整張填、4 檔一格未填）。
   我 06 §七寫「`B7`」，低估了範圍。
4. **§6 五項自評皆有實質**，尤以第 3 項（零命中只限 `features/power` 284 條）
   之量詞自我限定為 R-PMH20 之正確應用。

---

## 二、§4.2 之 283 vs 284 —— **已解，是口徑差異，非任何一方有誤**

執行層實測 `…_PowerManagement_20260821.xlsx` 之 `D` 欄非空資料列為 **284**，
而我 06 §六引 `features/power` 之交付數為 **283**，並註明「差 1 之成因未追」。

**分析層之既有量測即可解**：我於 03 包 §2 對同一檔之實測為
「`Test Group` 欄 `Power Management` **283 / 283**，**另 1 列為 `SWE-PM-089` 留白列**」。

即：

| 口徑 | 值 |
|---|---|
| `D` 欄非空之**資料列數** | **284** |
| 其中具 `Test Group` 之列（＝ TC 數） | **283** |
| 留白列（`SWE-PM-089`，有 req id 無 TC） | 1 |

**兩個數字都對，量的是不同東西。** 執行層之「未追成因」與「不改他 feature
之數字」處置正確；成因在本包補上，**不需任何人改動 `features/power`**。

**A-PMH13 §4.2 之口徑註可據此結案**（由 PENDING 之附帶項改為已釐清）。

---

## 三、Layer 2 之 granularity 檢查（canon §4.1.3）—— 分析層執行，**PASS**

06 §5 明令「不定 granularity」屬分析層，執行層據此未驗（其 §6 第 2 項自陳）。
**本節即該檢查**，比照 R-VF44 之前例逐項驗，並附 must-hit／must-not-hit 錨點。

**量測條件**：對象為 06 §5.1 之 8 組 Test Set；分母為 48 個 leaf；
「規模」以 leaf 數計（TC 數尚未生成，不以之為據）。

| 判準 | 錨點（must-not-hit） | 本 feature 實測 | 結果 |
|---|---|---|---|
| **過細**：Test Set 數 ≈ TC/leaf 數，Test Set 欄淪為 TC ID 之副本 | 一 RD parent 一組 → 若成立，組數應接近 29（outline 數）或 48 | **8 組**，8/48 = 0.17 | **PASS** |
| **過細**：存在只含 1 個 leaf 之組 | 任一組 = 1 | **最小組 = 3**（`Splash Screen`／`Off Road Plus`） | **PASS** |
| **過粗**：存在收容簇（`Misc`／`General`／`Unclassified`／`Other`） | 組名命中該四詞任一 | **零命中** | **PASS** |
| **過粗**：單組吃掉過半 leaf | 任一組 > 24 | **最大組 = 9**（`Startup Animation`），9/48 = 0.19 | **PASS** |
| **決策測試**：filter 後為有意義之簇（非 1 條、非全簿） | 組規模落在 [2, 24] 之外 | 全部 8 組落在 **[3, 9]** | **PASS** |
| **共用 setup／UI 進入路徑** | 同組內之觸發情境互不相干 | 逐組見下 | **PASS** |

**共用情境之逐組陳述**（§4.2「同一 Test Set 應共用 setup 與 UI 進入路徑」）：

| Test Set | 共用之觸發情境 |
|---|---|
| `Splash Screen` | 駕駛門關閉／CAN BUS cycle 起始時之畫面呈現 |
| **#2（待命名）** | 免責畫面顯示中之呈現與互動 |
| `Startup Animation` | 動畫播放之條件、次數與跨螢幕同步 |
| `Startup Sounds` | 開機／道別音效之設定與音量 |
| `Power Transitions` | IGN／電源狀態轉換時之行為 |
| `Power Off Behavior` | Power Button Off 狀態下之他功能介入 |
| `Voice Assistant Key` | VR 硬鍵長按 |
| `Off Road Plus` | Off Road+ 硬控之按壓 |

**分布**：8 組／平均 6.0／最小 3／最大 9／標準差 2.2。
**無任一判準落在失敗側。** 本檢查於 `framework.md` 定版時一併記入。

> ⚠ 此檢查之限制須明說：**它驗的是 leaf 分布，不是 TC 分布**。
> TC 生成後若某組 TC 數暴增（例如 `Startup Animation` 之 9 個 leaf 展開為
> 30+ 條），granularity 須重驗。**列為 Phase 4 之複驗項。**

---

## 四、裁決條文（抄入 `RULINGS.md`）

```
R-PMH32（(c) 平手時不擇一）
R-PMH19 (c)「同一交付夾內取檔名日期最大之一份」遇日期相同而無從分辨時，
**不得擇一**，亦不得由排序之實作細節決定。

處置為：該夾之全部平手候選並列，依 R-PMH29 做敏感度陳述 ——
各候選導致同一結論者，該夾照常計入母體並記明「其代表檔未定，
但結論對此不敏感」；不同候選導致不同結論者，停並上呈。

**不得以「資料列較多」「mtime 較晚」「檔名較規範」為 tie-break** ——
該三者皆是在判定他 feature 之交付態，而該判定不在本判準之授權範圍內
（R-PMH31 之同一理由）。

依據：`Engineering Mode` 夾之 `EngeeringMode_20260816`(211 列) 與
`EngMode_20260816_Rebuilt`(527 列) 檔名日期相同，(c) 無鑑別力
（06 包上繳 §2.4）。
```

```
R-PMH33（條文修訂之連帶檢查）
自任何判準移除一項條件時，須逐一檢查該項原本排除之對象改由何者接手，
並實測其接手後之結果與移除前相同。

僅確認「移除後總數不變」不足 —— 總數相同而代表檔改變者，
其後續量測即不同（R-PMH32 所指之情形）。

依據：R-PMH31 自 (b) 移除 `_Rebuilt` 與 `(done)` 時，分析層只驗了
母體仍為 16，未檢查 `_Rebuilt` 改由 (c) 接手後 (c) 是否接得住 ——
實測 (c) 在該夾為平手。
```

---

## 五、待 Pei 裁定（三項，其一阻斷）

### Q11（**阻斷 Layer 2 定版**）—— Test Set #2 之命名

| 案 | 值 | 代價 |
|---|---|---|
| **（甲）分析層提案** | `Disclaimer Screen` | 與 §4.2「不得重複 Test Group 字樣」字面相衝 |
| （乙） | `Acceptance Screen` | 合 §4.2，但 `Acceptance` 非規格用語（§8.4.1 精神：不造詞） |
| （丙） | 併入 `Splash Screen`，成 7 組 | 合 §4.2，但該組成 10 leaf、混兩個 FROP，且 §三之 granularity 須重驗 |

提甲之理由：§4.2 禁重複是為避免冗餘（`Bluetooth` 群下寫 `Bluetooth
Connection`），而此處 Test Group 是**交付夾標籤**而非能力名。
惟此為 canon 之字面例外，**分析層不自行豁免**。

### A-PMH13 —— `SWE1-HMI-PM-028` 之處置（Phase 4 前）

**查證後之事實**：該 leaf 內文逐字為 `OFF2.) Please refer to CFTS009 for
complete behavior.`，本身無可驗證行為；`features/power` 之 284 資料列
**零命中**（八組檢索式，唯一 `wake up` 命中經人工複核不相關）。
**兩邊都沒有 —— 是全案缺口，不是分工。**

| 案 | 處置 | 代價 |
|---|---|---|
| （i） | 寫一條 TC 驗「該轉介所指之行為與 CFTS009 一致」 | **違 §8.4.2** —— 行為定義在外部規格，且該規格屬已交付之 `features/power` |
| （ii） | 判為 out of scope，於 `reasoning` 記為 coverage gap | 需 Pei 核可：R-PMH1 之範圍為 48 個 Functional Requirement **全集**，排除任一者即動到範圍 |
| （iii） | 開 DR 詢問上游該 leaf 是否應存在於本報告 | 不排除任何項，但需等回覆 |
| **（分析層提案）** | **（ii）＋（iii）併行，且該列仍寫入工作簿並揭露** | 比照 Vehicle Setting 之 R-VF12（460/1087 out of SWE.1 scope **須揭露**，不靜默丟棄）；欄位以 `PENDING: DR-PMH{n} …` 形態填，不留空白 |

### Q10 —— `Product Document 記錄封面頁`（Phase 7 前）

執行層實測後，**範圍是一整張分頁而非一格**，且呈全有全無（12 檔整張填、
4 檔一格未填）。若裁為須填，需增補 11 項，其中：

- **5 項須你給定字串**（`B3` 專案代號 `new R1L`(8) vs `NR1L`(4) 兩種並存、
  `B4` 文件名、`B5` 版本 `V1.0`(11) vs `Initial`(1)、`B8` 日期格式不一、
  `A13:D13` 修訂列）
- **1 項與 R-PMH26(d) 相衝**（`B4` 取檔名即依賴上游命名）
- **1 項需擴充機器檢查**（`B7` 之 DV 約束不在現行 `check_write_back.py` 三項內）
- **1 項與 R-PMH23 相衝**（值不得自客戶那份複製）

**分析層提案：不填**，與 R-PMH27 同一理由 —— 語料雖多數有填，但其填法
分歧且需五個由我方自擬之字串；母本現況為空，不填即為忠實於母本。
**惟 12/16 之語料強度高於 D5 之 7/16，此項比 Q3 更接近「慣例」，故仍請你裁。**

---

## 六、作業步驟（皆不受 Q11 阻斷）

1. **抄錄** —— §四之 R-PMH32／R-PMH33 逐字抄入 `RULINGS.md`，附核對表。
   R-PMH19 (c) 條後加 R-PMH32 之附註（**原文不改字**）。
   A-PMH13 之 §4.2 口徑註依本包 §二結案（283 = TC 數、284 = 資料列數、
   差額為 `SWE-PM-089` 留白列），**不改 `features/power` 之任何檔案**。

2. **A-PMH13 之跨 feature 擴查**（清償 06 上繳 §6 第 3 項）——
   零命中之結論目前僅限 `features/power` 之 284 條。對 R-PMH24 母體之
   **其餘 15 個交付件**檢索同一組標的（`OFF2`／`off road`／`Off Road+`／
   `Power Off State` ＋ `launch`／`hard control`），回報各檔之命中數。
   **唯讀，不得修改任何他 feature 之檔案。**
   結論句依 R-PMH20 限定量詞（「本次量測之 16 個交付件中」）。

3. **母本 `Cover 封面` 之複驗**（清償 06 上繳 §6 第 5 項）——
   04 包 §8.1 之三項（`D6=C`、核准者／審查者有值、作者欄空）本輪重測，
   確認其為**被複驗**而非被取代（通則 5）。

4. **`framework.md` 補記 granularity 檢查** —— 將本包 §三之六項判準、
   錨點與實測值逐項寫入，並註明其限制（驗的是 leaf 分布非 TC 分布，
   Phase 4 須重驗）。**Test Set #2 仍記 `<PENDING Q11>`，不預填。**

5. **(c) 平手之複驗** —— 依 R-PMH32 重跑母體篩選，確認
   `Engineering Mode` 夾之處理改為「並列 ＋ 敏感度陳述」，
   母體仍為 **16**，且該夾之代表檔在輸出中標為「未定」。

---

## 七、停止條件

canon §0 六條，另加本包三條：

7. 步驟 5 之母體 ≠ 16
8. 步驟 2 之擴查發現任一他 feature **已涵蓋** `-028` 所指行為
   （若已涵蓋，A-PMH13 之提案形態即須改寫，停並回報）
9. 步驟 3 之複驗結果與 04 包 §8.1 不符

**本包零寫回工作簿。** 全部改狀態 git 屬 Pei（R-G5）。
**不得改動 `scripts/new_feature.py`；不得修改任何他 feature 之檔案。**

---

## 八、上繳包要求（`docs/upstream/07_gap_widening.md`）

1. §四二條之抄錄核對表 ＋ R-PMH19 附註三之落實證明（原文 SHA256 未變）
2. 步驟 2 之 15 檔檢索結果表 ＋ 依 R-PMH20 限定量詞之結論句
3. 步驟 3 之 `Cover 封面` 複驗結果（與 04 包逐項對照）
4. 更新後之 `framework.md`（含 granularity 節）
5. 步驟 5 之母體複驗（含量測時點，R-PMH30）
6. **「本包是否仍有該驗而未驗者」之獨立判斷** —— 不得省略
7. 建議之 commit 訊息與 pathspec（**不執行**）＋ R-G6 之唯讀／改狀態分列

---

## 九、本包產生之新條文清單（自檢表）

| 條號 | 主旨 | 已以可貼區塊出現於 §四 |
|---|---|---|
| R-PMH32 | (c) 平手時不擇一，改敏感度處置；三種 tie-break 明文禁止 | ✅ |
| R-PMH33 | 條文修訂之連帶檢查（移除一項須驗接手者接得住） | ✅ |

二條各管一事。

**待 Pei 者三項**：Q11（阻斷 Layer 2 定版）、A-PMH13 之處置、Q10。
