# 下放包 11 —— 已發生變更之舉證、勘誤方式與互斥狀態一致性

- 日期：2026-08-24
- 方向：分析層（Claude Project）→ 執行層（Claude Code）
- feature slug：`power_moding`
- 對應上繳：`features/power_moding/docs/upstream/11_claim_evidence.md`
- 前一包：[10_profile_draft.md](10_profile_draft.md)
  （上繳 [../upstream/10_profile_draft.md](../upstream/10_profile_draft.md)，已覆核）

---

## 一、10 包之覆核結果

**通過。停止條件 8 之觸發為正確觸發。**

`--check-doc-sync` 之 RESOLVED 三條件全部滿足，且其**上線第一秒即在真實
情境下攔下一次真分岔**（新增該檢查本身即改動程式，文件雜湊當場過時）——
**這比任何合成測試都有力**。profile 草案核對零不符：17 條 `R-PMH`、
5 條 `A-PMH` 皆存在，欄位九鍵、數值 12 項、Test Set 八組、九詞條逐字全符，
另唯讀複驗了草案 §7 對 Power profile 之五項聲明。

---

## 二、`framework.md:7` —— 責任在我，而且我在覆核時放過了它

### 2.1 事實鏈

| 環節 | 誰 | 發生了什麼 |
|---|---|---|
| 08a 步驟 8 之指示 | **分析層（我）** | 令「三處 `<PENDING Q11>` 改為 `Disclaimer Screen`」＋「狀態由未定版改為定版」—— **兩件事寫在同一步驟內，未指明其替換順序會互相破壞** |
| 執行 | 執行層 | 先換佔位符，再換該行 → 第二次 `.replace()` 靜默未命中 |
| 驗證 | 執行層 | 驗佔位符殘留 = 0 → 通過（**驗的是代理量**） |
| 上繳 §11.3(a) | 執行層 | 逐字寫「狀態由「未定版」改為 **定版**」 |
| **覆核** | **分析層（我）** | **讀到該句、接受、核可 08a 通過** |

**我的失誤有二**：
一、指示把兩個會互相破壞之替換寫在同一步驟；
二、**覆核時憑一句「已改為定版」之敘述即核可，未要求任何實測輸出** ——
而 08a §3.1 是我自己寫的，其中對 R-PMH18 之三字串驗證我**明確要求了
「須貼出實測輸出」**，對「狀態改為定版」卻沒有。
**同一份下放包內，我對兩件事採了不同的舉證標準。**

### 2.2 結果：`framework.md` 自相矛盾維持兩輪

第 7 行「未定版」、第 24 行「定版」，跨 08a 與 09 兩輪並存。
09 包我改對了第 24 行之標題卻沒看見第 7 行 —— **而 09 §7 第 2 項那句
「此形態只修了這一處，未回頭掃描」之自陳所預告的東西，
就在同一個檔案的第 7 行。**

### 2.3 這比 09 §6.1 那一項嚴重，執行層之分級正確

09 §6.1 是「驗了、通過了、但驗的不是要驗的那件事」；
**本項是「報告了一件沒做到的事」** —— 一句對已發生變更之正面陳述，
而該變更從未發生。**前者是驗證設計缺陷，後者是紀錄不實**
（非故意，但效果相同：往後任何人讀 08a 上繳都會相信那件事做了）。

---

## 三、裁決條文（逐條抄入 `RULINGS.md`）

```
R-PMH43（已發生變更之舉證）
上繳包中任何對「已發生之變更」所作之陳述（「已改為 X」「已撤除」
「已定版」「已更新」），須附該變更之實測輸出 —— before/after、
`grep -n` 命中、或等效之可重跑證據。**敘述本身不構成證據。**

覆核方（分析層）不得僅憑敘述核可此類陳述；缺證據者退回，
不得因其看來合理而放行。

下放包對同一包內各項變更所要求之舉證標準須一致 —— 對某項要求「須貼出
實測輸出」而對另一項不要求，即為指示缺陷。

依據：08a 上繳 §11.3(a) 逐字稱「狀態由『未定版』改為定版」，
而該變更從未發生（`framework.md:7` 至 10 包回掃時仍為「未定版」）；
分析層於覆核時讀到該句並核可（10 包 §二）。
```

```
R-PMH44（已提交之上繳／下放包之更正方式）
已提交之上繳包或下放包**原文一字不改**。發現其含不實或過時陳述時，
以**勘誤附註**處理：

(a) 於該檔末追加 `## 勘誤` 節，載明：被更正之節號與原句逐字、
    正確之事實、發現該誤之輪次與其證據；
(b) 於 `docs/INDEX.md` 該輪次列標記「含勘誤」；
(c) **不得刪除、不得改寫原句** —— 原句連同勘誤並存，方能看出當時
    相信了什麼、何時發現不是。

追加勘誤節本身是對檔案之修改，須列入該輪之 pathspec 並揭露。

依據：08a 上繳 §11.3(a) 之誤稱（10 包 §4.2）；本 repo 對 `RULINGS.md`
之既有作法（原文不改字、以附註承接，R-P36 形態）擴及往返包。
```

```
R-PMH45（同檔內互斥狀態陳述之一致性）
同一檔案內對同一對象所作之**互斥狀態陳述**須一致，並以可執行之檢查驗之。

最低限度之互斥對：`定版`/`未定版`、`PENDING`/`RESOLVED`、
`待裁`/`已裁`/`已結清`、`wired: true`/`wired: false`。

檢查方式：對每一互斥對，掃全檔取其全部出現位置；同時出現兩側者即失敗，
並列出行號與逐字內容。**不得以「總數為 0」代替**（R-PMH41 末段）。

此檢查之價值在於它抓的是**替換未命中之結果**，而非替換之過程 ——
歷史上已無 before 可查之替換，其殘留仍會被它抓到。

依據：`framework.md` 第 7 行「未定版」與第 24 行「定版」跨 08a、09
兩輪並存（10 包 §4.3）。
```

---

## 四、作業步驟

1. **抄錄** —— §三之 R-PMH43 ~ R-PMH45 逐字抄入 `RULINGS.md`，附核對表
   （抄錄本身依 R-PMH41 驗命中數）。

2. **08a 上繳之勘誤附註（R-PMH44）** —— 於
   `docs/upstream/08_criterion_repair.md` 檔末追加 `## 勘誤` 節，
   更正其 §11.3(a) 之「狀態由『未定版』改為定版」一句。
   **原文一字不改**，並於 `docs/INDEX.md` 該輪次列標「含勘誤」。
   附原文 SHA256 之變化揭露（追加節必然改變全檔雜湊，
   須明示所改者為「檔案」而非「該節原文」）。

3. **互斥狀態一致性檢查之實作（R-PMH45）** —— 於本 feature `scripts/` 下
   實作 `check_state_consistency.py`（或併入既有腳本），涵蓋
   §三所列四組互斥對，掃描標的為
   `framework.md`／`feature.yaml`／`DECISIONS.md`／`PLAYBOOK.md`／
   `RULINGS.md`／`ANOMALIES.md`。
   - **須以一次故意失敗證明其會攔下**：在暫存副本上把
     `framework.md:7` 改回「未定版」→ 檢查須 FAIL 並列出第 7、24 兩行；
   - 範圍向：現行六檔須全 PASS；
   - **`RULINGS.md` 之特殊處理須明說** —— 該檔本就同時含
     「PENDING」與「RESOLVED」等字（不同 anomaly 之狀態），
     故其互斥判定須以**同一對象**為單位（同一 `A-PMH{n}` 之狀態），
     不得以全檔字串共現判定。此點若無法乾淨實作，**標「該檔未納入」
     並具名，不得放寬判準後宣稱通過**。

4. **`--check-doc-sync` 之強化（R-PMH41 之應用，非新條）** ——
   現行只驗雜湊，**雜湊是代理量**：手改 `framework.md` 之門檻數值而不動
   雜湊行，檢查會 PASS。改為**比對 `--emit-thresholds` 之輸出與文件門檻表
   之正規化文字**（去除行首空白與表格對齊空白後逐字比對）。
   附故意失敗：手改文件中一個門檻值 → 檢查須 FAIL。

5. **profile 草案** —— **仍不得寫入**（待 Pei 核可）。
   本輪不做任何與 profile 相關之動作。

---

## 五、停止條件

canon §0 六條，另加本包三條：

7. 步驟 3 之故意失敗未被攔下，或範圍向有任一檔 FAIL
8. 步驟 4 之故意失敗未被攔下
9. 步驟 3 之 `RULINGS.md` 處理採「放寬判準後宣稱通過」而非「具名排除」

**本包零寫回工作簿。改狀態 git 零次**（10 包之提交尚未授權）。
**不得寫入 `docs/runtime/profiles/`；不得改動 `scripts/new_feature.py`；
不得修改任何他 feature 之檔案**（含 10 包 §8 所見之
`features/power/docs/internal_var_observability.md` —— 該檔屬併行 session，
**不得觸碰、不得入 pathspec**）。

---

## 六、上繳包要求（`docs/upstream/11_claim_evidence.md`）

1. §三三條之抄錄核對表（含命中數）
2. 步驟 2 之勘誤節全文 ＋ `INDEX.md` 標記 ＋ 雜湊變化之揭露
3. 步驟 3 之檢查實作 ＋ 故意失敗與範圍向之實跑輸出 ＋ `RULINGS.md` 之處理方式
4. 步驟 4 之強化後輸出 ＋ 故意失敗實跑
5. **「本包是否仍有該驗而未驗者」之獨立判斷** —— 不得省略
6. 建議之 commit 訊息與 pathspec（**不執行**）＋ R-G6 之唯讀／改狀態分列

---

## 七、待 Pei 裁定 —— **兩項阻斷 Phase 4，已阻斷兩輪**

| # | 事項 | 阻斷 |
|---|---|---|
| **profile 草案** | 10 包 §四全文。**核對零不符**（10 上繳 §5：17 條 R-PMH ＋ 5 條 A-PMH 皆存在、欄位九鍵、數值 12 項、Test Set 八組全符）。核可後由執行層寫入 `docs/runtime/profiles/FW036_R1L_PowerModing_Profile.md` | **是** |
| **A-PMH13** | `-028` 之處置。提案 **(ii)＋(iii) 併行**：判 out of scope、開 DR、該列仍寫入工作簿並揭露（比照 R-VF12），格式沿用 Comfort 之 `[BLOCKED-SPEC] Owner: {CFTS}`。**連帶**：若裁 (ii)，profile §0／§2 之「48 leaf」須加註「其中 1 條為揭露列」 | **是** |
| **commit 授權** | 10 包之異動（08／08a／09 已提交為 `931053f`）＋ 11 包 | 否 |
| Q10 | `Product Document 記錄封面頁`（提案不填；profile §3.10 已預留） | 否，Phase 7 前 |
| — | A-PMH06 canon 層（`new_feature.py` 樣板） | 否，PENDING-CANON |

**Phase 4（TC 生成）除上開二項外無其他前置。**

---

## 八、本包產生之新條文清單（自檢表）

| 條號 | 主旨 | 已以可貼區塊出現於 §三 |
|---|---|---|
| R-PMH43 | 已發生變更之陳述須附實測證據；覆核方不得憑敘述核可；同包舉證標準須一致 | ✅ |
| R-PMH44 | 已提交之往返包原文不改字，以檔末勘誤節處理 | ✅ |
| R-PMH45 | 同檔內互斥狀態陳述之一致性檢查（四組互斥對） | ✅ |

三條各管一事。
