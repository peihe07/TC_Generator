# 下放包 09 —— must-hit 隔離度、G1 門檻之推導與門檻單一來源

- 日期：2026-08-24
- 方向：分析層（Claude Project）→ 執行層（Claude Code）
- feature slug：`power_moding`
- 對應上繳：`features/power_moding/docs/upstream/09_threshold_derivation.md`
- 前一包：[08](08_criterion_repair.md) ＋ [08a](08a_q11_and_git.md)
  （上繳 [../upstream/08_criterion_repair.md](../upstream/08_criterion_repair.md)，已覆核）

---

## 一、08 包之覆核結果

**通過。** 九條停止條件逐條有實據；五個 must-hit 指定判準全部如期 FAIL；
範圍向全 PASS；`framework.md` 定版且四檔佔位符殘留 0；
三字串大小寫敏感驗證通過（差異字元位置 11：`s` vs `S`）。

四項特別記明：

1. **§0 之主動更正** —— 我在 08 §5.1 寫「06 與 07 至今未提交」，
   而該陳述於執行時已不成立（`a345ca8` 已落地）。執行層未沉默沿用，
   而是先更正再往下做。**撰包時點早於提交所致，非任何一方有誤**，
   其處置正確。
2. **§11.2 之「不執行第二次提交」** —— R-PMH37 之標的已由 Pei 另行完成，
   執行層逐項比對 `a345ca8` 之訊息、路徑數、路徑集合、時點四項後，
   認定授權標的已達成而**不再執行**，並援引該條「明文不授權第二次提交」。
   **這是授權條文被正確地當成上限而非義務。**
   `git log -1` 因併行 session 推進而已非該提交，改以
   `git show --name-only a345ca8` 查核 —— 量測方法之替換有交代。
3. **§2.3 之方法決定**：首版規則「指定以外之判準 FAIL ⇒ 錨點失敗」過嚴，
   自行檢出並改為「連帶不使錨點失敗，但須記隔離度」。**追認，見 §二。**
4. **§7 第 2 項自陳 G1 之門檻無來源** —— **該門檻是我在 08 §2.1 給的**，
   指認正確，見 §三。

---

## 二、§2.3 之方法決定 —— **追認，並升為條文**

執行層之判斷成立：**must-hit 之職責是證明「該判準會 FAIL」，
其他判準一併 FAIL 不否定此事**；但隔離度影響證明力，須具名。

**追認其兩處執行時修正亦正確**：A3 補回 G5（下放包原文即列二者，
是執行層首版漏抄）、A4 之 `Misc` 由 1 leaf 改為 2 leaf 以隔離 G3。

**惟 §7 第 3 項自陳「A1／A2 之無法隔離只被論述、未被證明」——
該證明存在且可機器化**，見 R-PMH38(c)。

**鴿籠論證**：`n` 個 leaf 分為 `k` 組，若每組規模 ≥ 2 則須 `n ≥ 2k`。
故 `k > floor(n/2)` 時**必有單 leaf 組**，G2 必然 FAIL。
- A1：`k=29 > floor(48/2)=24` → G2 必 FAIL；
- A2：`k=48 > 24` → G2 必 FAIL。

**二者之連帶為可算之結構性連帶，非構造疏失，亦非論述。**

---

## 三、G1 之門檻 `0.35` 無來源 —— 我的缺陷，且該判準缺隔離錨點

### 3.1 兩個問題，須分開修

**問題一：`0.35` 是我湊的。** 它剛好讓 `8/48=0.167` 通過、`29/48=0.604` 失敗，
但 **0.35 與 0.4、0.5 之間我給不出理由**。執行層指出「A1 之 0.604 對 0.5
門檻同樣會 FAIL」—— 即現有錨點**分辨不出 0.35 與 0.5**，
依 R-PMH14 之鑑別力口徑，該錨點不足以支持 `0.35`。

**問題二：G1 從未被單獨證明有效。** 五個錨點中，A1／A2 觸發 G1 時
**同時觸發 G2／G5**（結構性連帶）。故**沒有任何錨點證明 G1 會在
G2／G5 皆通過之情形下獨立 FAIL** —— 依 R-PMH35(c)，G1 目前應標
「未實測」而非 PASS。

### 3.2 G1 確實不可省 —— 它抓別的判準抓不到的東西

存在 G2／G4／G5 全通過而仍過細之組態：
**48 leaf 分為 20 組（每組 2–3）** —— `min=2`（G2 ✅）、`max=3`（G4 ✅）、
全落 `[2,24]`（G5 ✅），而 `20/48 = 0.417` 之過細正是 canon §4.1.3 所指
「Test Set 欄淪為 TC ID 欄之副本」。**故 G1 須保留，但要給它來源與錨點。**

### 3.3 門檻之推導（取代湊數）

canon §4.1.3 之決策測試逐字為：「filter 後是否得到**有意義之簇** ——
不是一條，也不是整本」。**「不是一條」在 G2 已由 `min ≥ 2` 承接；
G1 承接的是其平均意義**：

> 若**平均**每組不足 3 個 leaf，則過濾該 Test Set 之結果多為 1–2 列，
> 其索引價值已與逐條列舉無異。

故 **G1 之門檻取 `組數 / leaf 數 ≤ 1/3`（平均組規模 ≥ 3）**，
其來源為「簇之最小有意義規模為 3」，與 G2 之 `≥ 2`（成組之最低要求）
分工明確：**G2 管單組下限，G1 管全域平均**。

本 feature 實測 `8/48 = 0.1667 ≤ 0.3333` **PASS**（餘裕 2 倍）。

### 3.4 新增隔離錨點 A6

| 錨點 | 構造 | 應 FAIL | 預期 |
|---|---|---|---|
| **A6** | 48 leaf 分為 **20 組**（8 組 ×3 ＋ 12 組 ×2） | **G1（且僅 G1）** | `20/48 = 0.4167 > 0.3333`；`min=2` G2✅；`max=3` G4✅；全落 `[2,24]` G5✅；組名不含收容簇 G3✅ |

**A6 為 G1 之隔離證明**；未跑 A6 前，G1 依 R-PMH35(c) 標「未實測」。

---

## 四、裁決條文（逐條抄入 `RULINGS.md`）

```
R-PMH38（must-hit 錨點之隔離度）
must-hit 錨點之通過條件為「**其指定判準如期 FAIL**」。
指定以外之判準一併 FAIL **不使該錨點失敗**，但須記其隔離度，
因隔離度決定該錨點之證明力：

  **隔離**      —— 僅指定判準 FAIL。可單獨證明該判準有效。
  **結構性連帶** —— 連帶之 FAIL 可由構造參數以算式推出者。
                  須寫出該算式，不得以文字論述代替。
  **未隔離**    —— 連帶之 FAIL 既非指定、亦無算式可推。
                  該錨點不足以證明任一判準有效，須另構造隔離錨點。

任一判準若無任何「隔離」或「結構性連帶」之錨點，該判準依 R-PMH35(c)
標「未實測」，不得標 PASS。

現行錨點之隔離度：
  A1（29 組）／A2（48 組）—— **結構性連帶**。算式：n 個 leaf 分 k 組，
    每組規模 >= 2 須 n >= 2k；故 k > floor(n/2) 時必有單 leaf 組，
    G2 必然 FAIL。A1: 29 > floor(48/2)=24；A2: 48 > 24。
  A3／A4／A5 —— **隔離**。
```

```
R-PMH39（G1 之門檻與其來源）
granularity 判準 G1 之門檻為 `組數 / leaf 數 <= 1/3`（即平均組規模 >= 3），
**取代 08 包 §2.1 所給之 0.35**。

來源：canon §4.1.3 之決策測試「filter 後須得有意義之簇 —— 不是一條，
也不是整本」。其「不是一條」由 G2（`min >= 2`）承接單組下限；
G1 承接其平均意義 —— 平均每組不足 3 個 leaf 時，過濾結果多為 1–2 列，
索引價值與逐條列舉無異。

`0.35` 之作廢理由：該值係湊得，且現有錨點對 `0.35` 與 `0.5` 無鑑別力
（A1 之 0.604 對兩者皆 FAIL），依 R-PMH14 不足以支持之。

G1 不得省略 —— 存在 G2／G4／G5 全通過而仍過細之組態（48 leaf 分 20 組，
每組 2–3），其過細正是 canon §4.1.3 所指「Test Set 欄淪為 TC ID 欄之副本」。
該組態即 G1 之隔離錨點 A6。
```

```
R-PMH40（判準門檻之單一來源）
判準之門檻只有一個來源：**實作該判準之程式**。

文件（`framework.md`／`DECISIONS.md`／handoff／upstream）中出現之門檻數值，
須由程式輸出產生，或加一致性檢查驗其與程式相同；
**兩份獨立維護之副本一律視為缺陷** —— 程式改而文件未改（或反之）
不會被任何檢查發現。

實施：`check_granularity.py` 增 `--emit-thresholds` 輸出門檻表，
文件之門檻節由該輸出貼入並附產生時之程式 SHA256；
或增一檢查比對文件中之數值與程式常數。二擇一，於上繳說明所採者。

依據：08 包上繳 §7 第 4 項自陳 `framework.md` 之 granularity 節與
`check_granularity.py` 之門檻為兩份獨立副本（與 A-PMH12 同型：
宣告與實際分離）。
```

---

## 五、作業步驟

1. **抄錄** —— §四之 R-PMH38 ~ R-PMH40 逐字抄入 `RULINGS.md`，附核對表。

2. **G1 門檻改為 `1/3` 並實作 A6** ——
   - `check_granularity.py` 之 G1 常數改為 `1/3`（以分數或 `1.0/3.0` 表示，
     不得寫 `0.33`）；
   - 新增錨點 A6（20 組：8 組 ×3 ＋ 12 組 ×2），**須 G1 單獨 FAIL、
     G2／G3／G4／G5 全 PASS**；
   - 六個錨點重跑，各記其隔離度（依 R-PMH38 之三級）；
   - **A1／A2 之結構性連帶須以算式在程式中判定**
     （`k > floor(n/2)` ⇒ G2 必 FAIL），不得以字串註解代替；
   - 範圍向重跑：現行 8 組於 G1–G5 全 PASS（`0.1667 ≤ 0.3333`）。

3. **門檻單一來源（R-PMH40）** —— 二擇一實施，於上繳說明所採者與其驗證輸出。

4. **`Test Set` 欄之機器檢索**（清償 08 §7 第 1 項）——
   對母體 15 個有內容交付件，將 A-PMH13 之七組檢索式擴及 `Test Set` 欄
   （以表頭文字定位），回報命中。**唯讀。**
   結論句依 R-PMH34(c) 更新盲區聲明。

5. **profile 之備料** —— 讀 `docs/runtime/profiles/` 之既有 profile，
   回報：檔名清單、各檔之節結構（標題層級與節名）、`[OVERRIDE]` 與 `[ADD]`
   之寫法與其引用 canon 節號之格式、以及各 profile 之行數。
   **只備料，不撰寫本 feature 之 profile** —— 其草擬屬分析層，Pei 核可。

6. **`framework.md` 之 granularity 節依 R-PMH39／R-PMH40 更新** ——
   門檻改 `1/3` 並記其推導；六錨點與隔離度；門檻節依 R-PMH40 之所採方式產生。

---

## 六、停止條件

canon §0 六條，另加本包三條：

7. A6 未使 G1 單獨 FAIL（即 G2/G3/G4/G5 有任一併同 FAIL）
8. 六錨點中有任一指定判準未 FAIL，或範圍向有任一判準 FAIL
9. 步驟 4 之 `Test Set` 欄檢索發現任一他 feature 有 Off Road 相關之 Test Set

**本包零寫回工作簿。** **改狀態 git 零次** —— R-PMH37 已用畢失效，
08 與 09 之提交**尚未授權**（見 §七）。
**不得改動 `scripts/new_feature.py`；不得修改任何他 feature 之檔案。**

---

## 七、待 Pei 裁定

| # | 事項 | 阻斷 |
|---|---|---|
| **A-PMH13** | `-028` 之處置 —— **Phase 4 之唯一前置**。分析層提案 **(ii)＋(iii) 併行**：判 out of scope、開 DR、**該列仍寫入工作簿並揭露**。**執行層另查得現成前例**：Comfort 之 `Remarks` 使用 `[BLOCKED-SPEC] Owner: {CFTS}` 形態標註跨規格歸屬（08 包 §5），可直接沿用而不必自創格式 | **是**（首批可先行，不含 `-028`） |
| **08／09 之 commit 授權** | R-PMH37 為一次性且已用畢。08 之 pathspec 見其上繳 §11.6（11 路徑），09 之待本包上繳後產生。**是否比照 R-PMH37 逐包窄口授權，或改為常規授權執行層自行提交** | 否 |
| Q10 | `Product Document 記錄封面頁`（提案不填） | 否，Phase 7 前 |
| — | profile `FW036_R1L_PowerModing_Profile.md` 尚未撰寫 | Phase 4 前 |

---

## 八、上繳包要求（`docs/upstream/09_threshold_derivation.md`）

1. §四三條之抄錄核對表
2. 六錨點之實跑輸出（含 A6）＋ 各錨點之隔離度 ＋ A1/A2 之算式判定輸出
3. 範圍向輸出（G1 門檻 `1/3` 下）
4. R-PMH40 之所採方式與其驗證輸出
5. 步驟 4 之 `Test Set` 欄檢索結果 ＋ 更新後之盲區聲明
6. 步驟 5 之 profile 備料
7. 更新後之 `framework.md` granularity 節
8. **「本包是否仍有該驗而未驗者」之獨立判斷** —— 不得省略
9. 建議之 commit 訊息與 pathspec（**不執行**）＋ R-G6 之唯讀／改狀態分列

---

## 九、本包產生之新條文清單（自檢表）

| 條號 | 主旨 | 已以可貼區塊出現於 §四 |
|---|---|---|
| R-PMH38 | must-hit 之隔離度三級，結構性連帶須有算式 | ✅ |
| R-PMH39 | G1 門檻改 `1/3` 並附推導；`0.35` 作廢；G1 不可省之理由 | ✅ |
| R-PMH40 | 判準門檻之單一來源 | ✅ |

三條各管一事。R-PMH39 為**取代型**，其取代之值與作廢理由已於條內載明。
