# 下放包 — Projection：DR#14 答覆處置（A-PJ61）

> 交付對象：Claude Code
> 觸發：Pei 於 2026-08-12 答覆 DR#14 (b) —— **否**
> 授權層級：Tier 1
> **本包與 `HANDOFF_phase7_writeback.md` 並行，不取代之。**

---

## 0. 答覆

> **DR#14 (b)**：Atlantis Mid 車型（VF ProMaster 637 / Commander 598 / Renegade 5210 / Toro 2261 / Fastack 376）是否在 R1LR SWQT 驗證範圍內？
>
> **Pei 裁決：否。**

依此，B5（Knob 42 列）之阻塞分為兩部分：

```
Atl-Mid  5 車型 × 6 列 = 30 列  → 範圍外，本包處置
Atl-Hi   HDCC27 / DT27 × 6 列 = 12 列  → 仍阻塞，見 §3
```

---

## 1. R-P75｜範圍外標記之 Remarks 窄口

A-PJ61 已預見此情形：既有之範圍外標記（`Not in ASW-R1 Release Scope`，77 列先例）位於 `Remarks` 欄，而該欄依 profile §1 凍結。

> **R-P75**
> `Remarks` 欄開啟窄口，**當且僅當**下列四條同時成立：
>
> 1. 該列屬 Atl-Mid 車型之 30 列白名單（依 `PROXI VC_Veh_Line` 值為 `376` / `637` / `598` / `5210` / `2261`）
> 2. 變更形式為**純附加**——原內容完全保留，於其後附加固定字串，不得刪除或改寫任何既有字元
> 3. 附加之字串為固定形式，不得逐列變化：
>    ```
>    Vehicle line out of R1LR SWQT scope (DR#14, 2026-08-12)
>    ```
>    原欄非空者，以換行分隔後附加；原欄為空者直接寫入。
> 4. 逐列記錄至 `data/remarks_scope_gate.log.json`（列號、原內容、附加後內容）
>
> **窄口不擴及任何其他欄、任何其他列。**
>
> **理由**：本簿自身之範圍標記慣例即置於 `Remarks`（77 列先例）。若僅記於交付文件而不入簿，執行者將遇到一個無法解析之 PROXI 前置條件而無任何說明——**那正是本專案存在的理由所在之缺陷**。標記入簿使該列自我說明。
>
> **設計比照 R-P12（L-PJ4 窄口）**：白名單 + 固定形式 + 純附加 + 逐列記錄。兩者共同確立本 feature 之凍結欄例外形式——**凍結欄之例外一律為窄口，不得為一般授權**。

### 連帶：L-PJ4 之凍結欄雜湊比對

D-2 之 34 欄雜湊須新增第三項授權例外：

| 例外 | 列數 | 依據 |
|---|---|---|
| `Expected Result` 純刪除 | 6（r424–r429） | R-P12 |
| `Test Case Author` 補值 | 40 | R-P19 / R-P54 |
| **`Remarks` 純附加** | **30** | **R-P75（本條）** |

三者皆須逐列列出並附 diff，不得僅以 log 代替。

---

## 2. R-P76｜30 列之 `Pre-Conditions` 不修訂

> Atl-Mid 之 30 列既已判定範圍外，其 `PROXI VC_Veh_Line = <車型代號>` **不予修訂**。
>
> 理由：
> 1. 該值之正確形式須依各車型自身之 PROXI 檔（R-P47：PROXI 為單車型文件），而該 5 台車之 PROXI 未取得亦無須取得（範圍外）
> 2. 依 R-P35，範圍外列仍須修訂——但 R-P35 之適用前提是「文字可依既有來源寫實」。本情形之來源已確定**不會取得**，與 R-P35 所指之「執行阻塞不豁免文字修訂」性質不同
> 3. 強行修訂等同編造（O-4）
>
> 30 列於 D-5 之依據編號更新為 **`A-PJ45 / DR#14 / R-P75 / R-P76`**，分類維持「正確地不動」。
>
> **A-PJ45 部分結案**：Atl-Mid 之 30 列由本條處置；Atl-Hi 之 12 列見 §3，A-PJ45 維持 PENDING 至該部分結案。

---

## 3. 仍阻塞：Atl-Hi 12 列（DR#18）

`HDCC27` 與 `DT27` 兩值仍不在 `Vehicle_Line_Configuration` 之列舉內——列舉僅有 `HDCC = 130` 與 `DT = 124`，**`27` 後綴之語意未明**。

該 2 車型在範圍內（Atl-Hi），故不適用 R-P75／R-P76。

> **DR#18（新開）**
> `PROXI VC_Veh_Line = HDCC27` 與 `= DT27` 中之 `27` 後綴語意為何？
> - 若 `27` 為程式代號或年式標記，而配置值應為 `130` / `124` → 依 R-P8′ 形式更正為 `Car_Configuration_15.Vehicle_Line_Configuration = 130 (HDCC)`
> - 若 `27` 為值之一部分而列舉不完整 → 屬 canon §5a 第九條家族，須取得完整列舉
> - 若為測試矩陣欄標題之沿用（同 A-PJ45） → 12 列比照 R-P76 不修訂，另需裁定標記方式
>
> **服務列數**：12（Knob）
> **Urgency**：Medium —— 不阻塞 Phase 7 寫回（該 12 列本即「正確地不動」），但阻塞 A-PJ45 結案與最終交付之完整性聲明。

---

## 4. 執行

本包之作業併入 `HANDOFF_phase7_writeback.md` 之執行順序：

```
第 1 步  併入：R-P75 / R-P76 落檔；檢查表 v4 之 D-2 新增第三項授權例外
第 2 步  不變
第 3 步  不變
第 4 步  W-2 新增：Remarks 純附加 30 列（含於複本 W-1 ~ W-7 全流程）
第 5 步  不變
第 6 步  【Pei 已於 2026-08-12 明示放行】對交付用 xlsx 執行 W-1 ~ W-7
```

**第 6 步之放行已取得。** 第 1 ~ 5 步全部通過後，得逕行第 6 步，**不需再次請示**；但第 4 步（複本全流程）若任一項未通過，仍須停下回報，不得跳過。

---

## 5. 上繳要求（併入原包 §7）

原包七項之外新增：

8. R-P75 窄口之 30 列逐列 diff（原內容 / 附加後內容），及 `remarks_scope_gate.log.json`
9. D-2 三項授權例外之逐列清單（6 + 40 + 30 = 76 列）
10. DR#18 之 12 列現況確認（維持不動、依據編號正確）
11. **交付用 xlsx 之寫回結果**：W-1 ~ W-7 逐項、外部重算統計值、最終檔案 SHA256

---

## 6. 本包產生之新條文清單（A-PJ53 要求）

| 編號 | 形式 | 位置 |
|---|---|---|
| R-P75 | 可貼區塊 | §1 |
| R-P76 | 可貼區塊 | §2 |
| DR#18 | 可貼區塊 | §3 |

落檔：R-P75 / R-P76 → `DECISIONS.md §0.21`；DR#18 → `DATA_REQUESTS.md`；A-PJ45 部分結案註記、A-PJ61 CLOSED by R-P75 → `ANOMALIES.md`；Remarks 窄口 → `profile`（與 R-P12 窄口並列於同一節，標明「凍結欄之例外一律為窄口」）。

**不 commit。**
