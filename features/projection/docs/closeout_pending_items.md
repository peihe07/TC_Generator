# Close-out 待審項 — `.gitignore` diff / tag annotation / commit 分組

> 產出日期：2026-08-12
> 依據：`HANDOFF_closeout_disposition.md` §9 第 2、3、7、8 項
>       + `HANDOFF_delivery_precheck.md` §3
> **`.gitignore` 兩處修改僅產出 diff，未寫入。git 一律只準備不執行。**

---

## 1. `.gitignore` 修改 diff（§9 第 2、3 項｜**待 Pei 追認，未寫入**）

檔案：`features/projection/.gitignore`

### 1.1 新增 `backup/`（R-P89）

```diff
@@ 檔首（第 1–3 行）@@
 # Customer source files - never commit
 inputs/
+
+# Backups of customer workbooks - same policy as inputs/. R-P78 requires the
+# write-back backups be kept, so exclusion is the only available fix: the two
+# .bak.xlsx files are byte-identical copies of the customer workbook, and the
+# delivery-target backup is a copy of what was in the customer review folder.
+backup/
```

**現況**：`features/projection/backup/` 內有 **3 份**（截至送達後）：

| 檔 | SHA256 | size |
|---|---|---|
| `…20260812T081932Z.bak.xlsx` | `11579c9b…` | 572,672 |
| `…20260812T084057Z.bak.xlsx` | `11579c9b…` | 572,672 |
| `…delivery-target.20260812T101458Z.bak.xlsx` | `11579c9b…` | 572,672 |

三份皆為客戶工作簿之完整副本。目錄狀態為 `??`（未被忽略），
**`git add -A` 會直接提交客戶原始檔**。

### 1.2 `pcts_ui` 改為入庫 + 理由文字更正（R-P90）

```diff
@@ 現行第 25–27 行 @@
-# PCTS Verifier uiautomator dumps: 320 KB of single-line XML regenerated by
-# re-dumping the app. No diff value, and nothing in them is human-authored.
-data/pcts_ui/
+# PCTS Verifier uiautomator dumps ARE tracked (R-P90). The earlier exclusion
+# asked the wrong question: not "can this kind of data be produced again" but
+# "can the observation that produced it be reproduced". R-P11 binds the gate to
+# one specific combination - Pixel 10 / Android 16 / PCTS Verifier
+# 5.1-prod.922397802 - which may not survive device retirement, OS upgrade, or
+# an apk version being pulled. L-PJ7 reads pcts_evidence.json's status to decide
+# whether 23 rows may be edited; if that status is ever challenged, these dumps
+# are the only physical evidence. 297 KB is not a burden.
+#   Evidence is valuable because it records a moment, not because it records a
+#   kind of fact.
```

**現況**：14 份 XML，**297 KB**（`du -sh` 顯示 320K 為區塊配置，實際檔案總和 297 KB），
`git ls-files` 回傳 0。

> **執行層附註**：R-P90 之通則（「產生它的那次觀測能否重現」）與現行 `.gitignore`
> 對另三份 `*_sections.json` 的保留理由**不衝突** —— 那三份留庫的理由是
> 「traceability lookup，diff 是 spec export 位移的訊號」，屬**現行性**價值；
> `pcts_ui` 留庫的理由是**取證性**價值。兩種理由並存，不需統一。

### 1.3 未動之項（確認現行政策維持）

| 項 | 現況 | 本輪處置 |
|---|---|---|
| `inputs/` | 排除 | **維持** |
| `data/sysad_sections.json` | 排除（bleeds SYSAD paragraph body text） | **維持** |
| `data/er_divergence.json` / `er_narrow_gate.log.json` | 排除（embeds ER/Procedure verbatim） | **維持** |
| `batches/` | 排除（embeds PRE/PROC verbatim） | **維持**；去 prose 之稽核軌跡尚未產出（見 §3 組 6） |

> `data/remarks_scope_gate.log.json` 為本輪新增，**其結構與 `er_narrow_gate.log.json`
> 同型**（含 `before` / `after` 之工作簿逐字內容）。依現行政策應一併排除，
> 但**現行 `.gitignore` 未涵蓋它**。
>
> ```diff
> @@ 現行第 21–23 行 @@
>  data/sysad_sections.json
>  data/er_divergence.json
>  data/er_narrow_gate.log.json
> +data/remarks_scope_gate.log.json
> ```
>
> **這是本輪新增的第三處缺口**，與 `backup/` 同因：新產物未同步套用既有政策。
> 一併列入待審。

---

## 2. tag annotation 全文（§9 第 7 項）

```
tag  fw036-projection-refine-v1
```

```
FW036 R1L Projection — test case refinement v1

交付檔        NR1L_GEN1(HDCC)_Ver_20260813.xlsx  (sheet TestResults)
交付 SHA256   b16debb7bc609e39044803760171cf1d2b583fd1ed8a4cd2602e82029c8c6b67
交付檔大小    574,700 bytes
基準 SHA256   11579c9b3b8e56eb9f25a06acd2ce9281409286248a37b327be4732cc0bdede9
基準檔大小    572,672 bytes

交付位置      /Users/peihe/Work/02_Project_R1LR/10_Reviewing/00_TestCase/
              Projection/CP:AA:iPod/NR1L_GEN1(HDCC)_Ver_20260813.xlsx
送達日期      2026-08-12（R-P88 授權後執行；送達前已驗證該位置之現有檔案
              SHA256 == 基準檔，未被他人修改）

資料列        559 → 565（刪除追溯列 r562、表尾補 7 條）
leaf 覆蓋     165 / 171（未覆蓋：SWE1-PROJ-146，依 R-P18 不補列）
變更列        既有 63 列（Pre-Conditions / Test procedure）
              + 授權例外 76 列（Expected Result 6 / Test Case Author 40
                                / Remarks 30）
補列          7 條（NR1L-PROJ-560 ~ 566；5 條完整 TC + 2 條 BLOCKED 佔位）

驗證          D-1 ~ D-11 全綠；W-0 ~ W-9 單一次完整執行
              全簿公式 775 → 781，逐格內容比對
              外部重算（LibreOffice headless）12 項統計值全中

OPEN DR       #2  Est_Range_BEV 之正式 LID（2 列）
              #8  CFTS025-4660 需求本文（24 列 + leaf 146）
              #12 mobile GAL 操作文件 / logcat 過濾條件（4 + 3 列）
              #13 量測設備規格（10 列）
              #15 客戶專屬手機 APP（1 列）
              #16 SWE1-PROJ-190/195 是否撤銷（2 列）
              #17 BLOCKED 佔位列之統計口徑（2 列）
              #18 HDCC27 / DT27 之 27 後綴語意（12 列）
              #19 SYSAD 版本落差（81 列）

⚠ 交付檔不在版本歷史中（inputs/ 依客戶原始檔政策排除）。
  本 tag 之樹不含交付檔；交付版本以上列 SHA256 綁定。
  checkout 此 tag 會得到一個沒有交付檔的樹。
```

**tag 由 Pei 執行。**

---

## 3. commit 分組最終清單（§9 第 8 項，依 §7 更正）

**組 2（framework Part V）已移除** —— 該檔早於本輪入庫，`git status` 未列出。
**組 3 訊息已更正**為實際變更範圍。

### 組 1 — canon + profile

```
docs: add canon numeric-discipline and handoff contract, Projection profile
```

| 檔 | 狀態 |
|---|---|
| `docs/fw036/FEATURE_ONBOARDING.md` | M — §5a 第 11~16 條 + 條號索引 |
| `docs/runtime/profiles/FW036_R1L_Projection_Profile.md` | M — 檢查表 v4（D-1~D-11）、W-0~W-9、兩處窄口 |

### 組 3 — 治理文件

```
docs(projection): record R-P54..R-P91 and A-PJ52..A-PJ74
```

| 檔 | 狀態 |
|---|---|
| `features/projection/DECISIONS.md` | M — §0.18 ~ §0.25 |
| `features/projection/ANOMALIES.md` | M — A-PJ52 ~ A-PJ74 |
| `features/projection/DATA_REQUESTS.md` | M — DR#15 ~ DR#19 |
| `features/projection/docs/*.md` | ?? 15 份（7 份下放包 + 8 份報告） |
| `docs/fw036/RD1_projection_submission.md` | ?? |
| `docs/fw036/RESYNC_proposal_projection_closeout.md` | ?? |

> 訊息中的 `R-P54..R-P91` 與 `A-PJ52..A-PJ74` 為**本次變更之實際範圍** ——
> R-P1~R-P53 與 A-PJ01~A-PJ51 早已入庫。

### 組 4 — `data/*.json`

```
feat(projection): add Layer 3 derivations, signal map, and evidence
```

**建議分兩類處理**（未自行決定）：

**4a 稽核憑證（建議入庫）**

| 檔 | 理由 |
|---|---|
| `data/signal_map.json` (M) | D-8 拆欄後之三欄語意 |
| `data/carplay_addendum_sections.json` (M) | 29 → 284 節重建 |
| `data/d5_blocked_rows.json` (M) | 75 列阻塞對照 |
| `data/d6_append_34cols.json` | R-P79 補列 34 欄判定 |
| `data/d11_dv_compliance.json` | D-11 |
| `data/specref_resolution.json` | R-P73 真解析 |
| `data/w7_expected.json` | **W-7「預期值先算後比」之憑證** |
| `data/stepR_writeback.json` | 交付版之寫回逐項結果 |
| `data/stepM_writeback.json` | **作廢版之記錄 —— A-PJ69 的證據，保留** |
| `data/d8_artifact_manifest.json` / `d7_proc_excerpt_diff.json` / `b5_vehicle_split.json` / `lpj1_resolution.json` | 各檢查項明細 |

**4b 逐版中間態（建議只留最終版）**

`data/dryrun_v2.json` ~ `dryrun_v5.json` — 其差異已完整記錄於各版報告，
最終版為 `dryrun_v6.json`。

**4c 依現行政策應排除**

`data/remarks_scope_gate.log.json` — 見 §1.3。

### 組 5 — scripts

```
feat(projection): centralise lint matchers and measurement conditions (R-P49, R-P65)
```

| 檔 | 狀態 |
|---|---|
| `scripts/lint_defs.py` | M — 量測條件收編、`FROZEN_EXCEPTIONS`、`SPEC_ANCHOR`、`resolve_spec_anchor` |
| `scripts/writeback.py` | ?? — W-0 ~ W-9 + 備份還原 |
| `scripts/dryrun_v6.py` | ?? — **最終版** |
| `scripts/dryrun_v3.py` / `dryrun_v4.py` | ?? — **建議刪除**（中間版；v5 已改名為 v6） |

> 本組單獨成 commit 之理由（R-P49 / R-P65 的重點就是單一實作，獨立 commit 使
> 日後 blame 直接指向該裁決）成立且有實測支持：本輪四項過程缺陷全部源於量測條件
> 分散假設。

### 組 6 — batch 稽核軌跡

```
docs(projection): add Phase 5 batch audit records
```

**尚不成立。** `batches/` 現受排除，理由為含逐字 prose（非「可再生」）。
`.gitignore` 已寫出解法：輸出 `_meta` + `row` / `tc_id` / `verdict` / `reason` /
`changed` 而**不含 `before` / `after` 全文**。

**該去 prose 版本尚未產出**，需授權新增產物（例如 `data/batch_audit_trail.json`）。

---

## 4. 未執行之操作

- 未 `git add` / `git commit` / `git tag`
- **未修改 `.gitignore`**（§1 三處皆為 diff 提案）
- 未刪除任何檔案（§3 之精簡建議未執行）
- 未產出 batch 去 prose 稽核軌跡
