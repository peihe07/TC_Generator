# 下放包 09 — vsm_v43：K-6 量測 —— 171 × 295 逐列文本比對（R-VT21(c)）

日期：2026-09-02　取號：`docs/handoff/` 實測有 00–08，取 09
台帳不重生；DR 不代發；不寫工作簿；**不動暫代母體、Layer 2、已凍 b1**（R-VT21(c)）。
目的：定 K-6 —— `VF655_V43_R3` 171 列與暫代母體 295 列是同一批需求之兩次匯出、互補之兩批、還是兩版次；量測後 Pei 併裁 K-5／K-6。

## 一、W 清單（全程字面）

**W-1 逐列文本比對（171 × 295）**：`Description` 兩級比對——(a) 逐字全等；(b) 正規化後全等（`_x000D_`→空白、連續空白摺一、strip）。輸出四類計數＋清單：兩級皆等（同列雙份）／僅正規化等／不等但同 chapter-內容鍵（top-token 集 Jaccard ≥ 0.8，列為「近似對」並附分數）／無對應。41 列 DocID 空批同法跑一份（41 × 295）。

**W-2 覆蓋聯集圖**：以 08 包之 42 節表為框，逐節列「295 批命中組／171 批命中組／41 批命中組」三欄與各命中數——判 (a) 兩批是否互補（各覆蓋規格之不同節）；(b) 295∪171∪41 是否覆蓋 42 節全部（含 `1.11.1.1.11`／`.12`／`.28`／`.29` 四節）。

**W-3 版次線索**：171 批之 `Description` 與 295 批近似對之逐字 diff 抽樣（前 10 對，逐字並列）——差異型態（訊號名改版？措辭改版？全同？）如實列出，不定性。

**W-4 K-8 補測**：`1.11.1.1.35` 節內文 token ∩ `01.13.*` 三章節組（295 批）與 171 批同前綴組之 token 集；命中列出，不硬配。

**W-5 `Sys-RA-VF665_V43_VSM-nnn` 編號連續性**（上繳 06 §八-5(b) 未竟項順做）：507 列之 nnn 分布——三批（295／171／41）各自之號段是否成段、是否交錯；跳號清單。

## 二、E

| # | 項 | 判準 |
|---|---|---|
| E65 | W-1 四類計數 | 四類合計 = 171（41 批同理 = 41） |
| E66 | W-2 覆蓋 | 42 節逐節三欄齊；聯集覆蓋數觀測 |
| E67 | 近似對之 Jaccard | 逐對附分數；門檻 0.8 固定不調 |
| E68 | W-5 號段 | 三批號段圖 |

## 三、上繳（`docs/upstream/09_k6_measure.md`）

四類清單（全等對逐對列 ID 對映）；覆蓋聯集表；diff 抽樣逐字；K-8 結果；E65–E68；**不含任何處置建議之外的母體變更**；獨立判斷（此包之判斷特別重要：三種假說——雙份匯出／互補批／兩版次——逐一以量測結果對質）；gate_all 歸因。

## 四、升級條件

任何對暫代母體、Layer 2、b1 之寫入；Jaccard 門檻被調整；比對結果與 08 包 §三數字矛盾而未歸因。
