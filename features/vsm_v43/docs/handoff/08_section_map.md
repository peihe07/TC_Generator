# 下放包 08 — vsm_v43：42 節章節對應全表驗證（R-VT20(a)）；通過則 b1 補 spec 錨並凍結

日期：2026-09-02
取號：`docs/handoff/` 實測有 00–07，取 08
對象：執行層。00–07 包續有效。台帳不重生；DR 不代發；不寫工作簿。

## 一、W-1 —— 42 節全表（token 共現法，全程字面）

對 V43 R4 規格 `1.11.1.1.*` 推算之 42 節，逐節建表：

| 欄 | 內容 |
|---|---|
| 推算章節號 | 上繳 07 W-A 之 outline 兄弟序法 |
| 標題逐字 | heading 段原文 |
| 節內 token 集 | 該節起訖段之內文中，符合 v5 事實表訊號名或 PROXI `Format`／SYSRA 內文之 `[A-Z][A-Za-z0-9_]{3,}(\.[A-Za-z]\w*)?` 具名 token（詞界式，R-VT20(e)） |
| 候選 chapter 組 | SYSRA `01.11.01.01.*` 各組之 token 集（自 Description，詞界式）與該節交集數最大者 |
| 命中數／次高組命中數 | 兩數並列（差距即置信度） |

判準（R-VT20(a)）：**偏移全表一致**（候選組號 − 推算節號 = 常數）**且 token 支持 ≥ 40／42 節** → 判成立。
無 token 之節（若有）列「無詞可證」，不計入分母亦不硬配，逐節列出。
ELSE 對應範圍：對 b1 之 para 1028↔1046 逐字列出中間全部段落之 IF/ELSE 結構，判 ELSE 之轄域（字面縮排／樣式證據，無則記「不可判」）。

## 二、W-2 —— 依判定分支

**成立** → b1_ambient 11 條逐條加 spec 錨（`Vehicle_Setup_Management_by_VP-LTM_R1L_TBM_VF665_V43_R4_{對應節號}` 一行在前，Sys-RA 錨在後；檔名 token 化以實際檔名為準，逐字回報所用前綴）；`leaves_interim.tsv` 加 `spec_section` 欄（295 列全填，無對應者空並列數）；framework Layer 3 節加對應表落點註。b1 凍結聲明。
**不成立** → 只上繳全表與不成立理由；b1 以 Sys-RA 錨凍結。

## 三、E

| # | 項 | 判準 |
|---|---|---|
| E57 | 42 節表完整 | 42 列；每列兩命中數並列 |
| E58 | 偏移常數 | 觀測（成立則須全表同值） |
| E59 | token 支持節數 | 觀測；≥40 判成立 |
| E60 | 成立時 b1 之 spec 錨 | 11/11，前綴逐字一致 |
| E61 | E38–E45／E51 重跑（若改 b1） | 全過 |

## 四、上繳（`docs/upstream/08_section_map.md`）

全表；判定與依據；ELSE 轄域判讀；分支產出；E57–E61；gate_all 歸因。

## 五、升級條件

偏移非常數而強判成立；任何「無詞可證」節被硬配；spec 錨前綴出現拼寫變體。
