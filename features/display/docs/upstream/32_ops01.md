# 上繳包 32 —— 停止條件 87 觸發：適用條文 172 條，逾 30，未生成 `ops-01`

- 日期：2026-08-26
- 方向：執行層（Claude Code）→ 分析層（Claude Project）
- 對應下放：`features/display/docs/handoff/32_ops01.md`
- **停止條件 87 觸發**：T1b 之適用條文 **172 條** > 30 → **停，不生成**
- T2／T3 **未執行**；`pilot-01`／`rvc-01` 一字未動
- §三之五項附帶步驟**全數執行**
- **git 未執行**（§五為建議）

---

## 摘要

| 任務 | 結果 |
|---|---|
| T1a | 三條需求文逐字取得；**兩項既有阻斷立即現形**（見 §1.2） |
| T1b | **172 條**（R-G37 新判準，適用本專案）→ **停止條件 87 觸發** |
| T1c／T1d | 於停手範圍內**已量**（母體之形狀，供重新界定），見 §二 |
| T2 生成 | **未執行** |
| T3 機器檢查 | **未執行**（無產出可檢） |
| §三.1–.6 | 全數執行 |

**172 這個數字有一半是我的詞表造成的** —— 見 §2.2。
本層**不自行縮窄母體重跑**（那等於自行改變任務範圍），
但已量出三種界定之數字供分析層擇一。

---

## 一、T1a —— 三條需求文

### 1.1 逐字全文

```text
SWE1-DM-001（037 r8）Sub Categorization: State Management
  Title: Display Operative State Management [ON/OFF/Wakeup] - ON/OFF states
  Source Requirement ID: SYS-DISP-001    Priority: High
  Verification Method: Functional Test, Integration Test
  > The Display Management software shall manage display operative states as
    DISPLAY_ON and DISPLAY_OFF based on system operational requests and timeout
    conditions.The software shall send appropriate display state and brightness
    requests to DCSD during state transition handling.

SWE1-DM-002（037 r9）Sub Categorization: Wake-up Management
  Title: Display Operative State Management [ON/OFF/Wakeup] - Touch Based WakeUp
  Source Requirement ID: SYS-DISP-002    Priority: High
  Verification Method: Functional Test
  > The Display Management software shall transition display state from
    DISPLAY_OFF to DISPLAY_ON when valid touch coordinates are received from
    DCSD.The software shall restore previous display brightness and active HMI
    context after wake-up event handling.

SWE1-DM-003（037 r10）Sub Categorization: Startup & Wake-up Handling
  Title: Display Operative State Management [ON/OFF/Wakeup] - Sleep and Splash
  Source Requirement ID: SYS-DISP-003    Priority: Medium
  Verification Method: Functional Test, Integration Test
  > The Display Management software shall support Splash Screen handling during
    system startup and sleep-to-wakeup transition sequence.The software shall
    resume normal HMI operation after Splash timeout completion or operational
    state transition completion.
```

三條之 `SYS2 Traceability` 分別為 r2／r3／r4，`SYS-RA-DISP-001/002/003`，
`NRL` 皆空。**數值＋單位 0 處、`$Signal$` token 0 個、外部引用 0 處**
（R-DM27 之八條全稱，於此三條再度確認）。

### 1.2 兩項既有阻斷在本組立即現形

**(a) `DISPLAY_ON`／`DISPLAY_OFF` 與 DBC 逐字不等 —— DR-DM8。**
037 三條全用 `DISPLAY_ON`／`DISPLAY_OFF`；DBC `DCSD_DISP_STAT` 之
`VAL_` 為 `1 "ON"`／`0 "OFF"`，規格側另有 `[DISP_ON]`／`[DISP_OFF]`
之別名（A-DM35）。**三者兩兩逐字不等。**
DR-DM8 問的正是「是否為同一狀態」，**SENT 未答**。

> **這使 001／002 之 ER 幾乎必然只能驗行為，不能寫 raw** ——
> 除非該條所引之 CFTS 條文自己用了短拼法。逐條判定屬 T1c，
> 而 T1c 之母體現已停手。

**(b) 003 之 Splash 時段 —— DR-DM1。**
037 之 003 逐字含 `Splash timeout completion`，而 CFTS_020 之 splash
時段一律轉指 `{CFTS009-722}`（R-DM8 之再判定、上繳 06 已實測）。
**DR-DM1 SENT 未答。** 003 之時長面向本輪即可預見為 deferred。

---

## 二、T1b —— 172 條，停止條件 87 觸發

### 2.1 詞表及其非窮盡性（B9／B11 之具名）

```text
# 詞表（**逐項自 037 三條需求文之逐字內容導出**；非窮盡）
  DCSD Screen ON   ← 001「display operative states … DISPLAY_ON」
  DCSD Screen OFF  ← 001「… DISPLAY_OFF」
  touch to turn on ← 002「valid touch coordinates … wake-up」
  touch coord      ← 002「valid touch coordinates are received from DCSD」
  splash           ← 003「Splash Screen handling during system startup」
  power up seq     ← 003「system startup」
  sleep/wake       ← 003「sleep-to-wakeup transition sequence」
  intensity req    ← 001「brightness requests to DCSD」
  disp stat        ← 001「display state … requests to DCSD」

  **非窮盡性具名**：詞表由需求文之名詞片語人工對譯為規格用語
  （如 `DISPLAY_ON` → `'DCSD Screen ON' state`）。**該對譯是判斷，非量測**
  （B9／B11 之同型）。規格若以本詞表未涵蓋之措辭表述同一行為，本掃描會漏。
```

**詞表由需求文之名詞片語人工對譯為規格用語**（如 `DISPLAY_ON` →
`'DCSD Screen ON' state`）。**該對譯是判斷，非量測** ——
同 B9（停止條件 58 之詞表）與 B11（R-G33 之 token）之形態。
差別在 B11 已由 R-DM53 結構性解決（token 移到宣告端），
**而本處之對譯無宣告端可移** —— 037 之需求文不會為我們標註規格用語。

### 2.2 結果與其形狀

```text
# 命中（R-G37 新判準，適用本專案）= **172** 條
  停止條件 87（逾 30 條）：**觸發**

## Artifact Type
  Subsystem Functional Requirement: 169
  Description: 3

## T1d —— 診斷章（§1.4.x）vs 行為章
  診斷章 = 10   行為章 = 162

## 章節分布
   14  §1.8.2.6 Touch Screen Event Communication and X-Y Coord S
   11  §1.11.2.2 DCSD Display Hot Behavior
   10  §1.11.2.3 Touch Screen Event Communication and X-Y Coord S
    6  §1.8.1.1.3 HU behavior in response to ICS SCREEN OFF hardke
    5  §1.4.3.1 DCSD Behavior when receiving Implausible Signal 
    5  §1.8.1.1.1 HU behavior in response to ICS POWER hardkey pre
    5  §1.8.2.1.2.1 Rear Camera Events
    5  §1.15.3.2.1 Rear Camera Events
    4  §1.8.2.1.4.1 Rear Camera Events
    4  §1.8.2.3.1 Screen Off Button
    4  §1.8.2.5.2 Multi-stage' DCSD Display Hot Algorithm
```

**172 > 30 → 停止條件 87 觸發，停手不生成。**

**但這個數字有一半是我的詞表造成的**，量測如下：

```text
# 母體之三種界定（皆為 R-G37 新判準、適用本專案）
  (A) 主題詞 **或** 訊號提及詞（本輪 T1b 所用）: **172**
  (B) 僅主題詞（訊號提及不計）              : **88**
  (C) 主題詞且不含任何訊號提及              : **18**

# 為何 (A) 這麼大：兩個訊號提及詞太寬
  `disp stat` 126 條 —— 凡提及 $DCSD_DISP_STAT$／$TGW_DISP_STAT$ 者皆命中，
  含 Display Hot（熱管理，pilot-01 之範圍）與 Rear Camera（rvc-01 之範圍）。
  即 **(A) 之母體與另兩批重疊**。
    與 rvc-01 重疊（RVC）: 48
    與 pilot-01 重疊（Display Hot）: 18

# (B) 之章節分布（88 條）
    5  §1.4.3.1 DCSD Behavior when receiving Implausible Signa
    3  §1.8.1.1.1 HU behavior in response to ICS POWER hardkey p
    3  §1.8.1.1.3 HU behavior in response to ICS SCREEN OFF hard
    3  §1.8.2.1.2.1 Rear Camera Events
    3  §1.8.2.1.3.1 Rear Camera Interrupts
    3  §1.8.2.1.4.1 Rear Camera Events
    3  §1.11.1.1.2 HU behavior in response to DCSD SCREEN OFF har
    3  §1.11.2.1.1 HU and DCSD Startup Sequence
    3  §1.11.2.1.2.1 Rear Camera Events
    3  §1.11.2.1.3.1 Rear Camera Interrupts
    3  §1.11.2.1.4.1 Rear Camera Events
    3  §1.15.3.2.1 Rear Camera Events
    3  §1.15.3.3.1 Rear Camera Interrupts
    3  §1.15.3.4.1 Rear Camera Events

# 三 leaf 之對應（依主題詞歸屬，只分類不判覆蓋）
  SWE1-DM-001: 37（詞命中計，一條可計入多 leaf）
  SWE1-DM-002: 34（詞命中計，一條可計入多 leaf）
  SWE1-DM-003: 26（詞命中計，一條可計入多 leaf）
```

**根因**：我的九個詞裡有三個是**訊號提及詞**
（`disp stat` 126／`intensity req` 77／`touch coord` 38），
**凡提及該訊號者皆命中** —— 而 `$DCSD_DISP_STAT$` 是本 feature
三個批次共用的訊號。故 (A) 之母體**與另兩批重疊**：
**RVC 48 條、Display Hot 18 條**。

三種界定之數字：

| 界定 | 條數 | 停止條件 87 |
|---|---:|---|
| (A) 主題詞 **或** 訊號提及詞（本輪所用） | **172** | **觸發** |
| (B) 僅主題詞 | **88** | 仍逾 30 |
| (C) 主題詞且不含任何訊號提及 | **18** | 未逾 30 |

**本層不自行擇一** —— 縮窄母體等於自行改變任務範圍，
而下放包 §2.2 T1b 明文「判準之關鍵詞由三條需求文之逐字內容導出」，
我導出的就是這九個。**擇一屬 Tier 2。**

### 2.3 T1c／T1d —— 於停手範圍內已量者

**T1d（診斷章 vs 行為章）**：診斷章 **10** 條、行為章 **162** 條。
兩章確實皆有材料，故若日後生成，`reasoning` 須依 §2.2 T1d 具名其分屬
（停止條件 90 之標的）。

**T1c（拼法判定）未逐條做** —— 其為 172 條之逐條作業，
而母體本身待重新界定，先做會白做。**此為 R-G38(b) 之具名不驗理由。**

### 2.4 一項供分析層參考之觀察（不構成建議）

(B) 之 88 條其章節分布之前段大量為 `Rear Camera Events`／
`Rear Camera Interrupts`（每節 3 條、共五個架構副本）——
**因為 `sleep/wake` 一詞命中了 `wake` 而 RVC 諸條含 `wake up the DCSD`
之類語句**。若分析層採 (B)，該重疊仍在。

(C) 之 18 條則已排除全部訊號提及，**其代價是會漏掉「以訊號描述狀態
轉換」之條文** —— 而 001 之需求文逐字正是「send appropriate display
state and brightness requests to DCSD」，**那些條文恰是它要的**。

**三種界定各有其漏，本層不代擇。**

---

## 三、§三之五項附帶步驟（全數執行）

### 3.1 A-DM33 加註 ＋ A14 併入

**加註已於 31 輪執行**（上繳 31 §四），本輪複核其在：
記明母體限於 `§1.11.2.2`、以舊判準為之、`{4819273}` 不在其任一組內、
**「`1.11.2.2` 不是本專案 Display Hot 條文之全部」**，並標「登為 A14」。

**A14 至此併入 A-DM33 之加註，不另立異常。** A 類仍列 A14 為索取項
（其標的為「Display Hot 之條文母體究竟有幾組」，屬 DR-DM10(a)）。

### 3.2 DR-DM9(b) 增附件三

已入 `DATA_REQUESTS.md`。逐字：

> **附件三（下放包 32 §三.2）—— 第三種書寫形態**：`{4819344}`
> （`§1.4.2.1 Other Requirements`，適用本專案）逐字為
> `When the HU has a loss of communication condition with the ICS, the HU shall set TGW_DISP_STAT = [Fh: sna].`
> —— **十六進位形態 `[Fh: sna]`**，與 `TGW_DISP_STATSts` 之 `VAL_ 15 "SNA"` 對得上。
> **單一用例，本層未外推**（R-DM48）。
> 即規格對 HU 側值至少有三種書寫：長拼法 `[DISP_NORMAL]`、
> 雙記法 `DISP_NORMAL / Normal_mode`、十六進位 `[Fh: sna]`。
> **請上游一併說明何者為權威。**

### 3.3 DR-DM4 增附件二

已入。逐字：

> **附件二（下放包 32 §三.3）—— 3 位號亦出現於診斷章**：`{4819273}`
> （`§1.4.1.2.6`，`[Radio:R1M, R1H, R1L, R1L-R] [EE Architecture:All]`，
> **適用本專案**）逐字為
> `Execute 'Display is Hot' portion of DCSD Display Hot Algorithm - See CFTS013-629.`
> —— 即 **3 位號不只出現在 `1.11.2.2`，也出現在診斷章，且該條適用本專案**。
> 該條被舊適用性判準排除十輪（A-DM40／R-G37）。

### 3.4 `PLAYBOOK.md` 新增 §5a 寫作慣例（**建議，非條文**）

新增「§5a.1 全稱句之上方緊接其所據之輸出」，節首明標
**「本節之項為慣例，不是裁決條文；違反不觸發任何停止條件」**，
並附兩例之對照表（29 §2.2 之 `All`、31 §5.2 初稿之「主題為診斷行為」）
與「其最易失守之處為回溯檢查類文件」。

### 3.5 B26 併入 A-DM37

`{4819233}`（`Ethernet Cable`）／`{4819234}`（空字串）已以增列形態
併入 A-DM37 之同一條登記，**不另立異常**。B26 自 B 類移除。

### 3.6 `docs/INDEX.md` 已更新。

---

## 四、未驗項分流（A／B，R-G29）——**依 R-G38 篩過**

### 4.1 R-G38 之適用紀錄

| 候選 | 素材在手 | 無待裁阻斷 | 無停止條件禁止 | 不需另立判準 | 處置 |
|---|---|---|---|---|---|
| 「172 之形狀未量」 | 是 | 是 | 是 | 是 | **當場驗** → §2.2 |
| 「T1c 逐條拼法未做」 | 是 | **否**（母體待重新界定） | — | — | 不驗，理由具名 |
| 「三種界定該擇何者」 | 是 | **否**（Tier 2） | — | — | 不驗，理由具名 |
| 「`ops-01` 未生成」 | — | — | **否**（停止條件 87） | — | 不驗，R-G38(c) |

### 4.2 A 類

| 編號 | 項 | 阻斷什麼 | 索取中 |
|---|---|---|---|
| A1／A2／A13／A14 | CFTS_020 與 CFTS_013 之關係（四項同源） | 004／005；Display Hot 母體 | DR-DM10(a)＋補充一、二 |
| A3 | HU 側值標籤（**現已知三種書寫形態**） | `rvc-01` 之 HU 側；**001／002 之 ER** | DR-DM9(b)＋附件三 |
| A4 | `Cat. SL` 之位置 | 涉 SL 之仲裁 | DR-DM2(a) |
| A5 | `sysad_allocation.tsv` | 全 8 leaf 之追溯欄 | DR-DM3 |
| A9 | 倒車檔訊號 | 007 之觸發面向 | DR-DM11 |
| A10 | DR-DM4 之標的（**現有兩個附件**） | DR-DM4 之答覆 | 已重擬，待發 |
| A12 | 007／008 之區分軸 | `rvc-01` 之 `leaf_id` | DR-DM12，待發 |
| **A15** | **`DISPLAY_ON`／`DISPLAY_OFF` 與 `ON`／`OFF`／`DISP_ON`／`DISP_OFF` 是否同一狀態** | **001／002 之全部 ER 能否寫 raw** | **DR-DM8（SENT 未答）** |
| **A16** | **`ops-01` 之母體界定（172／88／18 三選一）** | **第三批之生成本身** | **本包 §2.2，待分析層裁** |

A15／A16 為本輪新增。**A15 不是新問題** —— DR-DM8 早已 SENT；
本輪只是第一次遇到它直接擋住一批 TC，故升入 A 類具名。

### 4.3 B 類

| 編號 | 項 | 狀態 |
|---|---|---|
| B1–B25、B27 | 見上繳 25–31 | 不變 |
| ~~B26~~ | `{4819233}`／`{4819234}` | **本輪併入 A-DM37，移除** |
| **B28** | **本批詞表之對譯無宣告端可移** | B11 由 R-DM53 結構性解決（token 移到宣告端），**而 037 之需求文不會標註規格用語**，故此類對譯只能具名，不能消滅 |
| **B29** | **`sleep/wake` 一詞命中 RVC 諸條之 `wake up the DCSD`** | (B) 界定下之已知偽陽性；若採 (B) 須先處理 |

B28／B29 為本輪新增。

---

## 五、建議之 commit 訊息與 pathspec（**未執行**）

```bash
git add \
  features/display/ANOMALIES.md \
  features/display/DATA_REQUESTS.md \
  features/display/BACKLOG.md \
  features/display/PLAYBOOK.md \
  features/display/docs/INDEX.md \
  features/display/docs/handoff/32_ops01.md \
  features/display/docs/upstream/32_ops01.md
```

```text
feat(display): survey the operative state material and stop at 172 clauses

- the term list derived from the three requirement texts matches 172 clauses
  under the corrected applicability predicate, over the thirty the handoff
  set as the stopping point, so no batch was generated
- measure why: three of the nine terms match any clause that mentions a
  signal, and DCSD_DISP_STAT is shared with both existing batches, so the
  population overlaps them by 48 RVC clauses and 18 Display Hot ones
- give the analysis layer three populations to choose between, 172, 88 and
  18, each with its own known blind spot, and pick none of them
- record A15: the leaves say DISPLAY_ON and DISPLAY_OFF, the DBC says ON and
  OFF, and the spec says DISP_ON, so DR-DM8 now blocks a batch rather than a
  column
- attach {4819344} to DR-DM9(b): the spec writes HU-side values three ways,
  including the hex form [Fh: sna]
- attach {4819273} to DR-DM4: the three-digit numbering also appears in the
  fault chapter, in a clause that does apply here
- add a writing convention to the playbook: put the output a universal
  claim rests on directly above it
```

> `generated/` 本輪**無新增**（`ops-01` 未生成）。
> `feature.yaml`／036 母本未變更，皆不入。

---

## 六、本包是否仍有該驗而未驗者 —— 獨立判斷

**兩項，皆已依 R-G38(b) 具名理由。**

1. **T1c 之逐條拼法判定未做。** 不驗之理由：母體待重新界定（A16），
   對 172 條逐條做而母體隨後縮成 18 條，其中 154 條之工作會白做。
   **但須具名一項風險**：若分析層擇 (A)，這 172 條之逐條拼法
   仍得做，**而屆時我會是在一個已知過寬的母體上做逐條判斷** ——
   過寬之母體不只是浪費，它會讓「這條到底屬不屬於 001」變成
   每一條都要判一次的事。

2. **三種界定之擇一未做。** 不驗之理由：屬 Tier 2。
   **惟本層之觀察須記**：(C) 之 18 條排除了全部訊號提及，
   **而 001 之需求文逐字要求「send appropriate display state and
   brightness requests to DCSD」—— 那些正是被排除掉的條文。**
   即 **(C) 最小，但它漏掉的恰好是 001 的主要動作。**
