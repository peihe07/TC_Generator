# 下放包 27 —— A8 解除（六列已驗明）、STALE 修法裁定（乙案）、DR-DM9 重擬

- 日期：2026-08-25
- 方向：分析層（Claude Project）→ 執行層（Claude Code）
- 對應上繳：`docs/upstream/27_stale_fix.md`
- **本包對交付物之推進：R-G33(d) 交付前 gate 之修復；24-4／24-5 解封**（R-G31）
- **前置（已查證）**：上繳包 26 已回；停止條件 66／67 之觸發皆已覆核；
  `pilot-01.json` 經 STALE 測試後逐字元還原；綁定 `entries: 11`／11 of 11

---

## 一、上繳包 26 之覆核

**核可。兩條停止條件之觸發皆正確，且皆為「檢查抓到東西」。** 逐項處置如下。

### 1.1 A8 —— 「全列 `All`」是我的過度宣稱；六列已驗明，實質影響為零

停止條件 67 之觸發正確：下放包 24 §一寫 `EE Architecture = All（全列）`，
實測為 `All` 26／空 5／`PowerNet` 1。**「全列」為我未逐列驗就寫下的
總括語** —— 與「Safety attributes 不帶該欄」（R-DM46）同型：
理由句比量測走得遠。

執行層 §十第 2 項自陳「我把『停手』執行成了『不再往下看一眼』」。
**那六列分析層已代為驗明**（自 Project 附件複本；執行層依步驟 1 重算）：

| 列 | Document ID | Category | EE | 內容 |
|---|---|---|---|---|
| r8 | CFTS013-752 | Information | 空 | 修訂履歷 |
| r9 | （`0`） | Information | 空 | 修訂履歷表頭 |
| r12 | CFTS013-1192 | Information | 空 | **`The TBM shall do this or that`**（樣板殘句） |
| r17 | CFTS013-1196 | **Heading** | **PowerNet** | `HU requirements`（章名） |
| r18 | CFTS013-1197 | Information | 空 | **`The HU shall dipslay xxxxxxxx`**（樣板殘句，含錯字） |
| r19 | CFTS013-1194 | Information | 空 | `0` |

**而 11 條 Functional Requirement 之 `EE Architecture` 為 `All` 11/11。**

即：不符的六列**全為 Information／Heading**，其中三列（r12/r18/r19）
明顯是撰寫殘渣（`do this or that`、`dipslay xxxxxxxx`、`0`）。
需求本體無一受影響。

**處置**：
(a) 下放包 24 §一之該列**更正**為
    `All 26／空 5／PowerNet 1；11 條 FR 全為 All`，於 24 包檔內
    以修正註記形態加註（不改原文，R-TM13），註明其為分析層之過度宣稱。
(b) **A8 解除**。24-5／24-4 之前提恢復。
(c) r12／r18／r19 之樣板殘句以 **A-DM37**（LOW）登記 ——
    其為 Information 列，不入任何 TC，不阻斷；記其存在
    使日後讀該檔者不誤認為需求。

### 1.2 A7 —— STALE 修法：**裁定乙案**

§5.2 之確診成立且要害：

> 它抓得到的，正好不是 R-G33(d) 要防的那一種。

三選項中**裁定乙**（`deferred` 項不刪除，解除以標記為之）。
理由採認執行層 §5.5 之判斷並補其未估之量：

- 本案每一條機器保證（sidecar、綁定、核對表、順序驗證）都靠
  「留下可比對之痕跡」成立，**刪除本質上不留痕跡** —— 乙案把
  「解除」變成一次寫入
- 甲案回到詞表由人給（B9／B11 復發）；丙案多一份要同步的檔，
  其時效無人保證（R-G33(d) 之同型問題再現）
- **規模之估**（執行層 §十第 1 項自陳未估者）：deferred 項之上限為
  「leaf 數 × 每 leaf 之獨立面向數」。本 feature 8 leaf，
  以每 leaf 三個面向計亦不過二十餘項，**陣列只增不減之堆積
  在本案規模下不構成代價**。他 feature 沿用時若逾百項再議。

條文見 §三 R-DM54。**修畢須重跑 R-G25 之誘發測試**（步驟 2）——
上輪之測試在指定情境下 FAIL，修後同一情境必須 PASS。

### 1.3 A-DM35 —— 補件成立，我的質疑前提為誤

我在 26 §2.4 質疑「逐字解得」與 R-DM48 相衝，其前提是規格側
只用 `[DISP_ON]`／`[DISP_REAR_CAMERA]` 一套拼法。**實測推翻了前提**：
規格對 `$DCSD_DISP_STAT$` 同時使用兩套，其中六個標籤
（`[OFF]` 85 次、`[ON]` 53、`[BLANK]` 20、`[RR_CMRA]` 72、
`[DISP_HOT]` 46、`[SNA]` 8）**逐字解得** DBC `VAL_`。

段落 1742 之一句兩訊號（`$TGW_DISP_STAT$ = [DISP_REAR_CAMERA]` 與
`$DCSD_DISP_STAT$ = [RR_CMRA]`）是決定性的：
**`[DISP_*]` 長拼法是 HU 側訊號的值域，`[RR_CMRA]` 等短拼法是
DCSD 側的** —— 兩套拼法各有其主。

**判定採認**：相容非相衝；**判定落在條款層級**（該條逐字用哪一套拼法，
決定該 TC 可否寫 raw）。本批三條一字不必改 —— #3 所引之 `{4820287}`
用的是解不得的 `[DISP_ON]`，其只驗行為之處置事後仍然正確。

**A3 之縮小自此有據**：用短拼法之條款（007／008 之 RVC 諸條）
其訊號值**不受 DR-DM9 阻斷**；用長拼法之條款（`{4820287}`／
`{4820289}` 第四動作）仍受阻，待 DR-DM9(a)。

### 1.4 DR-DM9 重擬 —— 採執行層三問，補一件附件

依 §三建議重擬為三問，**由 Pei 發出**：

- (a) `[DISP_ON]`／`[DISP_OFF]` 與 `[ON]`／`[OFF]` 是否為同一狀態
  之兩種書寫（規格內部之別名），或為不同狀態
- (b) `$TGW_DISP_STAT$`（HU 側）之值標籤（`[DISP_NORMAL]`／
  `[DISP_REAR_CAMERA]`／`[DISP_OFF]`…）對其 DBC `VAL_` 之對應
- (c) 規格自帶之雙記法何者為權威

**附件**：§2.1 之計數表全文（八個標籤 × 命中次數 × DBC 判定），
使上游一眼看見問題之形狀。原 DR-DM9 之文字不刪，
以「重擬（下放包 27）」註記接續。

### 1.5 其餘三項

- **B15（計數須具名口徑）**：採認。以指標形態註於 ledger 之 R-G16 條下
  （「量測條件」自此明文包含計數口徑：儲存格計／出現次數計），
  不改原條文。
- **A-DM36（`copy` 檔）**：登記正確。該檔於量測時已不存在 ——
  若係 Pei 所刪，即 26a §二之甲案已執行，**待 Pei 一句確認後結案**。
  B14（目錄普查非常設）維持 B 類。
- **§十第 3 項（「自陳不等於處置」）**：逐字採認，不另立條 ——
  R-G29 之分流已含其處置路徑（A 類次包處理），本次之一輪延遲
  即該機制之正常運作；再加條文只會加重 11–14 輪那型的自我增殖。

---

## 二、作業步驟

1. **A8 之收尾**：獨立重算 §1.1 之六列表與「FR 11/11 為 All」；
   相符則於 24 包檔內加修正註記（R-TM13 形態）、登記 **A-DM37**、
   宣告 A8 解除。不符則停（停止條件 67 續用）。
2. **STALE 修法（R-DM54，乙案）**：
   - `check_disclosure.py` 改讀 `lifted` 語意：
     `MISSING` 之母體 = 未解除項；`STALE` 之母體 = **已解除項**
     （同 leaf 之 lifted token 出現於括號下半即 STALE）；
     跨 leaf 之誤置檢查維持
   - **重跑 R-G25 誘發測試**：對 `multi-stage` 一項**於拋棄式複本**
     標 `lifted: true` → `STALE` 須報 1 且指名 TC#3 → 還原 →
     還原後連跑兩次逐字元一致（停止條件 66 續用：未報 1 即停）
3. **24-5**：`reference:` 增 `cfts013_sysra`
   （sha256 以磁碟實測 `1036b2af…` 為準），綁定 **12 項**、
   `entries: 12`（停止條件 62 恢復適用）。
4. **24-4**：`popup_priority.tsv` 之來源登記，**依 R-G36 機器抽取**
   （`{CFTS013-937}` 全文與 `PU0130` 之逐字指名），sidecar 之
   `generated_by` 為腳本。
5. 抄錄 §三 **R-DM54** 入 `features/display/RULINGS.md`（置放依 R-G34）；
   R-G16 之口徑指標置於 ledger 該條下（非 fence）。
6. `DATA_REQUESTS.md`：DR-DM9 之重擬（§1.4，含附件表）；
   `ANOMALIES.md`：A-DM35 加「已採認，判定落在條款層級」之結案註記。
7. 更新 `docs/INDEX.md`。

**仍不寫回 036 母本。**

---

## 三、裁決條文

```
R-DM54（deferred 之解除以標記為之，不以刪除為之 —— STALE 修法乙案）
`deferred` 陣列（R-DM53 之四鍵物件）自本條起**只增不減**。
項之解除不刪除該物件，改增三鍵：

  lifted      true
  lifted_at   解除日期（YYYY-MM-DD）
  lifted_by   解除依據（DR 答覆／裁定之編號，逐字）

`check_disclosure.py` 之兩向據此重定義：
  MISSING —— 未解除項（無 `lifted` 或 `lifted: false`）之 token
             不見於其 leaf 各 TC 之括號下半
  STALE   —— **已解除項**之 token 仍見於其 leaf 任一 TC 之括號下半
  跨 leaf 誤置之檢查維持既有行為

理由（上繳 26 §5.2 之確診）：原實作之 STALE 候選集自當前陣列建，
被整個移出陣列之 token 從此無人檢查 ——
**它抓得到的，正好不是 R-G33(d) 要防的那一種**（deferred 解除之
實際形態即是移出）。乙案把「解除」變成一次**寫入**：token 永遠
在候選集內，且解除之時點與依據成為可查之紀錄 —— 與本案每一條
機器保證（sidecar、綁定、核對表、順序驗證）同一理路：
**留下可比對之痕跡；刪除本質上不留痕跡。**

規模之界：本 feature 8 leaf，deferred 項之堆積以每 leaf 三面向計
不逾二十餘項，不構成代價。他 feature 沿用本條時若逾百項，
屆時再議歸檔機制，**不得預先為此複雜化**。

配套：本條生效後，R-G25 之誘發測試須以「標 lifted」之形態重跑
一次並 PASS（上輪以「移除」形態測試 FAIL，即本條之立條原因）。
```

---

## 四、停止條件

沿用 1–68（62 恢復適用），另加：

69. 步驟 2 之誘發測試若 `STALE` 仍未報 1，或還原後兩次不一致 →
    停並回報（乙案之實作即不成立，回到 §5.5 重議）。
70. 步驟 3 之 `cfts013_sysra` sha256 若與 `1036b2af…` 不符 →
    停並回報（表示磁碟檔與 26 輪量測之檔已不同一份）。

**全部 git 操作屬 Pei。**

---

## 五、上繳包要求（`docs/upstream/27_stale_fix.md`）

1. §1.1 六列表之獨立重算；24 包修正註記全文；A-DM37 全文；A8 解除宣告
2. R-DM54 抄錄核對表；修後 `check_disclosure.py` 之誘發測試全輸出
   （lifted → STALE=1 → 還原 → 兩次一致）
3. 綁定 12 項輸出（`entries: 12`）
4. `popup_priority.tsv` 之產出與 sidecar
5. DR-DM9 重擬全文（含附件表）；A-DM35 結案註記
6. 未驗項分流（A／B，R-G29）
7. 建議之 commit 訊息與 pathspec（不執行）
