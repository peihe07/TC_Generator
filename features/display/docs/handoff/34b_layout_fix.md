# 下放包 34b —— 附件：交付版面修正（Pei 2026-08-26 裁定「乙」）

- 性質：34 包之附件。補記入 `docs/upstream/34_closeout.md`，不另出上繳。
- TC 內容一字不動；本包為**版面**修正。

## 一、裁定（乙）

工作簿骨架 = **037 之 8 條需求，`SWE1-DM-001` → `-008` 升冪**。

| 規則 | 內容 |
|---|---|
| 排序 | 全部列依 Requirement ID 升冪；同一需求之多條 TC 依其行為軸序接續 |
| 無 TC 之需求 | **`SWE1-DM-006` 補一列**：D 欄填 `SWE1-DM-006`，**其餘欄一律留空**（Pei：留空就好——G／H 亦不填） |
| TC ID | 依新列序重編 `TC-DM-001` … `TC-DM-023`（O 欄全 NEW，重編無代價） |

新版列布局（10 起）：

```
r10–18  SWE1-DM-001 × 9        r19–21  SWE1-DM-002 × 3
r22–23  SWE1-DM-003 × 2        r24–25  SWE1-DM-004 × 2
r26     SWE1-DM-005 × 1        r27     SWE1-DM-006（空列，僅 D 欄）
r28–30  SWE1-DM-007 × 3        r31–33  SWE1-DM-008 × 3
```

## 二、執行

1. 自 `generated/*.json` 重建寫回（XML 外科式，來源母本不動），
   依 §一之序；輸出覆蓋 `output/` 同名檔。
2. 全套重驗：完整性八項計數、回讀 24 列 × 15 欄
   （**含 r27 空列之驗證：D 欄有值、其餘欄為空**）、
   合併 lint（母體 = 23 條 TC 列；**r27 非 TC 列，不入 lint 母體，
   排除須具名**）、`check_disclosure.py` 雙向、來源 sha 不變、綁定 13/13。
3. 覆蓋總表與 §六 pathspec 之補記。

## 三、防再犯（一句，不立條——R-DM57 凍結）

`write_back_036.py` 之列序自本包起以 **Requirement ID 升冪**為預設；
批次序僅為生成時之內部順序，不得出現在交付版面。
記入 `PLAYBOOK.md` 交付慣例節。
