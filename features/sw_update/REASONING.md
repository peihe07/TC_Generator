# REASONING — 逐 TC 之撰寫理由（暫行落點）

> ## ⚠ **本檔為暫行處置，其落點待裁**
>
> 歷來多條裁定令「記於 `reasoning`」（R-SU35(b)2、R-SU42、`006`／`007` 之情態差、
> D-4 之 `shalll`、T54b…），**而 `feature.yaml` 查無 `reasoning` 欄之映射**；
> 036 母本最接近者為 **`AH` Remarks 備註**，而各批自 pilot v3 起**一律未寫 `AH`**。
>
> **故該等內容至今只存在於腳本註解與台帳中，不隨 TC 走。**
> 交付時之審閱者拿到的是工作簿，不是本 repo。
>
> **三個可能之落點（待裁，不在逕行條件內 —— 其涉交付欄集）**：
> **(甲)** 啟用 `AH` 欄承載；**(乙)** 本檔，交付時附上；**(丙)** 明記 reasoning 不進交付物。
>
> **本輪暫採 (乙)** —— 其為三者中唯一「不動交付欄集且內容不遺失」者。
> **裁定後若取 (甲)，本檔之內容逐列搬入 `AH` 即可；若取 (丙)，本檔改為內部文件。**

---

## batch 1（`Silent Update`）

### `newR1L-SU-001` ← `SWE1-FOTA-175`

- **判定核心之限定**：末行之 `while no safety-related notification condition applies`
  依 **R-SU43 v2(a)** 加註 —— `CFTS057-4907477` 為
  `SHALL NOT be notified **unless** necessary for safety requirements`，
  **明文許可之他值**；不加該限定則本列會判一個合規系統為 fail。
- **其前提掛 `PENDING: DR-SU1`**（安全條件清單未知）。

### `newR1L-SU-002` ← `SWE1-FOTA-176`（facet A）

- **原措辭含造值**：`at any point of the session` **不見於 037 `176` 全文**，
  為分析層所加之強化語，其效果為把一個有例外之規則寫成無例外者。**已刪。**
- 限定與前提同 `001`。
- **保留 `The recorded screen content contains no …` 之句式** ——
  其一，維持與 R-SU36 之連結（該內容為 `as continuous video capture` 所錄）；
  其二，實測若改為 `is displayed on the head unit`，`I-cross` 之
  `IX_NEG` 即不命中，本列會整列掉出跨列比對之範圍。

### `newR1L-SU-003` ← `SWE1-FOTA-176`（facet B）

- **情態問題（待 DR-SU1）**：`4907477` 用 `allow`（許可），
  而本列 ER 斷言「通知**被顯示**」—— **即把許可讀成義務**。
  一個在安全條件成立時選擇不通知之系統並未違反 `allow`。
  **縱使安全條件清單到手，本問仍在。**
- 觀測手段明文化為 `as continuous video capture`，**雖其 ER 為肯定式**
  （R-SU36(a) 不強制）—— 理由為所驗通知之最短持續時間未知（DR-SU1）。

### `newR1L-SU-004` ← `SWE1-FOTA-177`

- 觀測步驟由 `Record every SW Update screen …` 改為
  `Record the head unit screen content as continuous video capture …`
  —— **`every` 是一個宣稱不是一個動作**（R-SU36(c)）。
- 其 proc 3／ER 3 為連帶改動：原文說「所錄下之**各個畫面**」，
  而步驟 2 已不再產生一組畫面而是一段錄影，不改則二步互相矛盾。

### `newR1L-SU-005` ← `SWE1-FOTA-183`

- ⚠ **判定核心失格，成因為規格自身抵觸**（R-SU43 v2(a)）：
  其錨 `4907485`（4.7.3.2 第 3 步）令「完成時顯示成功通知」，
  而同章 `4907477` 令「靜默期間不得通知，除非安全所需」——
  **success notification 是一種 notification**，而本列 pre 明載套件為 Silent Update。
  **改寫解決不了，併入 DR-SU1 之問 (ii)。**

### `newR1L-SU-006` ← `SWE1-FOTA-180`｜`newR1L-SU-007` ← `SWE1-FOTA-182`

- **情態差之記明**：037 為 `shalll not`／`shall not`，
  CFTS `4907482`／`4907484` 為 `MAY NOT display`。
  **SWE.6 以 037 為需求本文**，TC 依 037 之強度撰寫；不改二者、不發 DR。
- ⚠ **`MAY NOT` 於英文有歧義**（「得不顯示」／「不得顯示」）。
  **本列之 R-SU43 通過繫於「037 為需求本文」這一條** —— 若日後改以 CFTS 為準，須重驗。
- `006` 之 `test_item` 上半 **verbatim 保留 037 之 `shalll`**（D-4，拼寫殘留）——
  R-4 僅允句首大寫之正規化，拼寫不在其列。

### `newR1L-SU-008` ← `SWE1-FOTA-184`

- **第三型（R-SU32(iii)），掛 `PENDING: DR-SU2`**：其三類違例
  （prompt／progress notification／confirmation screen）**逐類皆已有專屬 TC**
  （`001`／`006`／`007`），而其獨有之宣稱 `across the three phases`
  **不可觀測**（階段界線於 HU 外部無表徵）。
- 所求為**區辨手段**；若無，其驗證應**併入 `175`**（須上游確認，R-SU32(d)）。

### `newR1L-SU-009` ← `SWE1-FOTA-179`｜`newR1L-SU-010` ← `SWE1-FOTA-181`

- **第三型，掛 `PENDING: DR-SU2`**。`009` 之後果與 `175` 完全相同（不可區辨）；
  `010` 之限定詞 `immediately` **不可量**（下載完成時點無觀測通道，規格未給閾值）。
- ⚠ **`181` 不屬 105 列** —— **R-SU32 v2(e) 之首例**：
  語形判準未攔下它，而它同樣不可完整驗證。

---

## batch 2a（`Interruption Handling`）

### `newR1L-SU-011` ← `315`｜`newR1L-SU-014` ← `318`

- **第四型（R-SU39）：觸發手段不可得** —— 其外部後果**可觀測**
  （版本未變、HU 可操作），缺者為**使該條件發生之手段**
  （`315` 需 socket 層錯誤注入；`318` 需事故偵測訊號而本 feature 未綁 DBC）。
- **DR-SU2(d) 求觸發手段，DR-SU4 求判定準據 —— 二者不互相取代**，
  一者到手而另一者未到，本列仍不可交付。
- ⚠ **不得以「模擬碰撞訊號」充數**（`318`）：訊號名與值域皆無來源，寫之即造值。

### `newR1L-SU-011`–`016`（六列共通）

- **判定核心降為記錄**（R-SU43(f)）：原斷言 `Version_after equals Version_initial`
  **不是規格所允許之唯一結果** —— `4907673` Table 4-6 對「中斷落於 deployment 階段」
  建議 `Complete the deployment`（**版本會變**），
  故依該建議實作之**合規**系統會被原 ER 判 fail。
- **判定核心改掛 `PENDING: DR-SU4`**；版本之讀取與記錄保留為證據。
- ⚠ **暫態**：六列之 Final Step 現字面相同，各列之 (a2) 觸發側狀態退回 ER 前段
  —— **違 R-SU41 全條**（(a) 區分退回前置、(b) Final Step 相同）。
  **DR-SU4 回覆後須依 R-SU41(c) 重建區分並重跑遮蔽測試。**
- `016` 之 ER 第 3 行改為 `The host system connector is disconnected`
  —— 原句斷言 HU 之狀態，**需消歧「HU 是被拔者還是觀察者」**；
  改寫後陳述連接器之狀態，**繞開該消歧而非回答它**（`4907340` 之多主機讀法有據而未消歧）。

### `newR1L-SU-015` ← `319`

- `test_item` 上半 **verbatim 保留 D-1 之缺字**（`the handling of condition`，條件名脫落）。
- **錨不取路徑 A 之首選**：首選 `4907380`（章 4.5.4.1，分 0.174）與本列無關，
  其成因為 D-1 之缺字使本列無詞可共；錨取 **GT-A1 已裁之 `4907671`**。
  **本列為 R-SU14 v5「不取首選為錨」之最強實例。**

### `newR1L-SU-017` ← `313`

- **統攝列，餘量為空**（R-SU37 v2(b)）：其 Description 二句拆解後
  全部由 `315`–`320` 與 `358` 承擔；其餘量（協調行為本身）
  **於其自身 Description 中無文字依據可支撐獨立之驗證點**。
- 曾被考慮而否決之餘量：「多個中斷條件併發時處理錯亂」——
  **問 1 否**（`313` 未提併發）、**問 2 是**（併發屬本組他列）。**推想不是需求。**
- `specification_reference` 取其自證六錨（R-SU15(b)）。

---

## batch 3（`Update HMI`）

### `newR1L-SU-018`–`020` ← `SWE1-FOTA-130`

- **拆三之依據**：三語言為三個獨立之部分失效（法語正常而西語不正常時，
  單一 TC 之判決不可辨）。**肯定式全稱，依 R-SU33(d) 需逐 X 確認**，
  不適用觀測窗法。三 TC 同 trace `130`。
- ⚠ **TC 數與資料軸綁定**：語言集若擴增，TC 數隨之變動而 037 一字未改。
  **現行 N=3 之依據為 037 該列自身之列舉。**

### `newR1L-SU-021` ← `SWE1-FOTA-131`

- **掛 `PENDING: DR-SU5`**：其驗證單元為「伺服器所設之類型決定所適用之流程」，
  **一個 campaign 只有一個類型，故本質上需二次執行**。
  而原第 3 步之「還原至更新前版本」**不可行且不可確認**：
  素材全無降版之需求或程序；`Rollback Protection` 為本 feature 之一個 Test Set，
  **其需求即防止回退**；縱可回退，「與更新前等價」無需求可據以確認，
  **而二次之差異須可歸因於類型方有意義**。
- 措辭改為「回到**可比之起始狀態**」—— **不預設其手段為降版**，
  使 DR 之請求不被一個未經驗證之作法所侷限。
- ⚠ **`131` 之 s4（跨更新類型之一致互動流程）本批未涵蓋** ——
  037 該列標題即以此為名。**見 DR-SU5 請求 2。**

### `newR1L-SU-022` ← `SWE1-FOTA-132`

- **上半取 s3＋s4（相鄰二句）**：s3 即 ER 第 4 行前半之驗證點，s4 即其後半 ——
  **上半與判定對象逐句對應**。原取 s2＋s4 時，**被驗的那句不在上半內**。
- s2（SWMC 查詢客戶接受狀態）為其前置行為，其**狀態**由 pre_conditions 第 3 行承載。
  ⚠ **其行為本身是否仍有 TC 涵蓋，見上繳包 36 §5 之自評。**

### `newR1L-SU-023`／`024` ← `SWE1-FOTA-133`

- **拆二之依據**：「顯示」與「可互動」為二個獨立之部分失效
  （連結顯示而不可點選時，單一 TC 之判決不可辨）。二 TC 同 trace `133`。

### `newR1L-SU-025` ← `SWE1-FOTA-134`

- **標點依 037 原文逐字保留**：`to the user ␣.` 之句點前空格、
  `“Install”`／`“Schedule Later”` 之彎引號（U+201C／U+201D）。
  **R-4 僅允句首大寫之正規化，標點不在其列** —— **非我方打錯。**
- 上半取 s3＋s4，**已刪除自 s2 移接之時間子句** `After completion of the download,`
  —— 原文中該子句修飾「SWMC 提供細節給 HMI」，移接後主詞變為 HMI，
  **產生一個原文沒有的句子（造句非摘句）**。下載完成之時點由 procedure 第 2 步承載。
- ⚠ **引號不一致**：`test_procedure`／`expected_result` 依下放包 41 §3.2 之全文
  用**直引號**，而 `test_item` 為彎引號 —— **同一 TC 內同一標籤二種寫法**。
  執行層依 T32b 未改寫，**待裁**。
- **情態差**：CFTS `4907662` 為 `SHOULD provide`，037 為 `shall provide`；
  依 SWE.6 以 037 為準，差異記此，不改二者、不發 DR。

### `newR1L-SU-026`／`027` ← `SWE1-FOTA-136`

- **配對之依據**（IN §7）：枚舉之支援情形須配至少一個未支援之負向 TC。
- **其區分位於判定對象內**（`offers … an option` vs `offers … no option`），
  滿足 R-SU41(b)。
- 標點依 037 原文逐字保留（`Silent Install flag ␣.`）。

---

## ROV-A（`ROV Installation` 首四列）

### `newR1L-SU-028` ← `SWE1-FOTA-090`

- **`PU0303` 於本 TC 僅為時點指標，不是驗證對象**（下放包 43 §3.1(i)）——
  其作用是告訴測試者「更新已成功、可以切 Body OFF 了」。
  **該彈窗本身之驗證屬 `SWE1-FOTA-088`**（IN §8.2.1，不擴入 sibling）。
- **取彈窗而不取 `$FOTA_Status$` 之依據**（同 §3.1(ii)）：
  後者為 CarPropertyManager 之車輛屬性，**台架不可觀測**（R-SU25(b)）；
  前者之對應關係**載於彈窗清單**（`PU0303` Description 逐字：
  `Shown after a successful update.`），**非推想**。
- `test_item` 上半之 `$FOTA_Status$` 記法**依 037 逐字保留**（R-S4）——
  **上半是需求原文，步驟與 ER 是可執行之描述，二者不必同形。**

### `newR1L-SU-029` ← `SWE1-FOTA-092`

- proc 3 之限定子句 `while $FOTA_Status$ = [Installing FOTA Update]` **已刪** ——
  其為不可觀測之限定，**且刪後 ER 3 仍可判**（錄影中有無安裝進度畫面）。
- 上半保留該訊號寫法（037 逐字）。

### `newR1L-SU-030`／`031` ← `SWE1-FOTA-093`／`094`

- **第四型（R-SU39）：觸發手段不可得** —— 其觀測面（`"Reverted"`／
  `"Walk Home Scenario"` 二彈窗）明確，缺者為**使更新失敗之手段**。
  DR-SU2(d) 之第四型段由 2 列增為 4 列。
- **二者須可互相區辨**：`093` 要求回退**成功**、`094` 要求**失敗未回退**。
  若上游只給「使更新失敗」之單一手段而不能控制回退之成敗，**二列仍不可分**。

---

## 未起草者之預先記錄

### `SWE1-FOTA-104`／`105`／`106` —— 彈窗編號於**二側皆為佔位**

037 引 `PUXXX3`（`104`／`105`）與 `PUxxx1`（`106`，**大小寫與前者不一致**）。

**實測**：二者於 `Pop Up List HMI R1 (26PI).xlsx` **皆非任何一列之 `ID Number`**，
只出現於他列 `Description` 之敘述文字中
（`show TBM Update Popup (PUXXX1)`／`TBM Ongoing Update Popup (PUXXX3)`；
另有 `PUXXX2`、`PUXXX1 b`、`PUXXX3 b`）。

> ### ⚠ **與 A-SU3 形態相同而處置相反，不得比照**
>
> **A-SU3**：一個**已存在**之彈窗被寫錯編號（`PU971` → `PU0971`）——**可推定正解**。
> **本項**：一個**尚未被指派編號**之彈窗 —— **沒有正解可推。**
>
> 二者於清單中皆表現為「查無」。**若台帳只記「查無」，日後會有人比照 A-SU3
> 去推一個不存在的編號。**

**處置（下放包 43 §二 #2）**：其 ER **以彈窗之功能描述指稱**
（如 `the schedule update pop-up`），**不引任何編號**；
**不開 DR**（不阻斷交付），列 `DELIVERY_CHECKLIST.md` **D-9**。

---

## ROV-B（強制更新彈窗之互動流程）

### 共通 —— 三個車輛屬性一律不入 procedure／ER

`$FOTA_Status$`／`$FOTA_Delay$`／`$FOTA_Install$` 皆為 CarPropertyManager 之
車輛屬性，**台架不可觀測**（R-SU25(b)，同 `028` 之成因）。
其於 `test_item` 上半**依 037 逐字保留**（R-S4）——
**上半是需求原文，步驟與 ER 是可執行之描述，二者不必同形。**

**`$FOTA_Delay$` 之 `[Not_Prohibited]`（底線）與 `[Not Prohibited]`（空格）
為同一值之二種寫法**（下放包 44 §一 #2）。其依據為**二側皆有**：
037 側 `097` 用底線、`101` 用空格；**CFTS 側 `4907880` 用底線、`4907884` 用空格**
（執行層實測）。**故其值域為二值（`Prohibited`／`Not Prohibited`），非三值。**
`test_item` 依各列原文逐字，**不統一**。

**pre_conditions 之拆行**：下放包原文以 `and` 併二條件於同一行
（`… campaign is staged … and the … pop-up is displayed …`），觸發 lint 之 R 檢查
（R-9(a)：一行一條件）。**執行層拆為二行**（不改條件、不改單元，逕行）。

### `newR1L-SU-033` ← `SWE1-FOTA-099`

- **安裝進度畫面於本 TC 僅為「安裝已開始」之可觀測表徵，本 TC 不驗該畫面** ——
  其驗證屬 `SWE1-FOTA-092`（IN §8.2.1）。
- 取此表徵而不取 `$FOTA_Install$ = [Accepted]` 之依據：後者不可觀測（同上）。

### `newR1L-SU-034`／`035` ← `SWE1-FOTA-100`（拆二）

- **拆二之依據**（IN §8.2.2）：逾時正常而取消不正常時，單一 TC 之判決不可辨。
- **句之分配**（037 `100` 共五句）：
  - `034`（逾時）取 **s1 ＋ s3**；`035`（取消）取 **s1 ＋ s2**。
  - **s2 委派 `035`、s3 委派 `034`** —— **非「未涵蓋」，是二 TC 各持其一**。
    `034` 之上半為**非相鄰句之摘取**（跳過 s2），依下放包 41 §二-1 之先例
    （`131` 之 s1＋s5）為合法摘句。
  - ⚠ **s4／s5 二句無 TC 涵蓋**：其內容為「收到取消或逾時通知後，
    將 `$FOTA_Install$` 設為 `[Not Accepted]` 再重設為 `Nothing to report`」——
    **全屬車輛屬性之內部狀態變化，不可觀測**。
    **依 `132` s2 之先例記為間接涵蓋**：其失效必然表現於 s2／s3 之外部後果
    （安裝仍開始），故 `034`／`035` 之 ER 已間接及之。
- **不寫逾時之秒數** —— 037 稱 `the configured timeout` 而未給值，
  寫任何秒數即造值（§8.4.1）。其可觀測之界為「彈窗關閉」。

### `newR1L-SU-036` ← `SWE1-FOTA-101`

- **範圍委派**：本列末句之二個分支 —— `FOTA_Delay = Prohibited` 屬
  **`SWE1-FOTA-102`**（`037`）、`FOTA_Status ≠ Waiting` 屬 **`SWE1-FOTA-109`**
  —— **本 TC 不涵蓋**（IN §8.2.1）。

### `newR1L-SU-037` ← `SWE1-FOTA-102`

- **不取首選為錨**（R-SU14 v5）：路徑 A 之**首選為 `4907884`，分 0.566**，
  而其述**允許**取消或忽略之條件 —— **與本列之規定相反**。
  錨取候選 **#4 `4907885`（分 0.407）**。
- ⚠ **本例比 `177` 難抓**：`177` 之首選分僅 0.174（低分易察覺），
  **本列之首選分 0.566 而內容相反** —— **高分而錯**。

---

## ROV-C ＋ ROV-D（**執行層自行起草**，下放包 45 之新分工）

### 共通 —— 屬性逐個判，不按批次排除

下放包 46 §二 #3 之四項裁定 ＋ 執行層自判一項（`$OperationalModeSts$`，待追認）。
**`$Speedometer$` 與 `$OperationalModeSts$` 為車輛之物理／模式狀態，台架可控可讀；
`$FOTA_Status$` 為軟體內部狀態，不可觀測。二者只因都寫成 `$…$` 而看似同族。**

### `newR1L-SU-039` ← `SWE1-FOTA-089`

- **車速鎖定行為本身屬 `CFTS022` 之 `4915105`，為外部規格**（IN §8.4.2 不涵蓋）。
  其依據為錨 `4907907` 逐字：`…implement the HU vehicle speed lockout behavior
  defined in Requirement ID 4915105 present in CFTS022…`
- ⚠ **`4915105` 於 `ANCHOR_POOL.md` 有登記** —— 惟其身分為
  **「內文交叉引用之出處」而非 CFTS_57 之物件**（語料中查無）。**外部性成立。**
- 本列所擁有者為「車速 > 0 時判定為行進中並通知 HMI」，
  其可觀測之後果為**安裝不啟動**。

### `newR1L-SU-041` ← `SWE1-FOTA-107` —— ⚠ **本批最不確定之一列**

- **其餘量之判斷（待裁）**：037 四句全為內部行為，而「排程時間之決定」已委派
  `103`／`104`／`105`。**執行層判其餘量為「所顯示之剩餘時間算得對」** ——
  該三列驗的是「開啟排程畫面／可選時間」，**無一列驗其計算之正確性**。
- **不寫任何秒數或誤差**（§8.4.1）—— 037 與 CFTS 皆無精度值。
- ⚠ **判定核心用了 `corresponds to`，那是我寫的不是規格寫的**。
  **「不寫誤差」與「寫一個模糊詞」是兩件事，而本列是後者。**
  其攔截完全依賴抽驗（見上繳包 40 §7）。

### `newR1L-SU-042` ← `SWE1-FOTA-108` —— **不拆**

- 其條件為二個連線判準之析取（`$LTE_Status$` 與 `$Cellsignal$`）。
  **形式上為獨立之部分失效（IN §8.2.2 疑須拆），惟二者在台架上無法獨立產生**
  —— 遮蔽行動網路會使二條件同時成立。
  **拆之即得二個 procedure 完全相同之 TC，落入 R-SU32(iii)。**
- **`[3G OR 4G OR H_Plus]` 與 `[0 OR 1 OR SNA]` 同為析取式之列舉**
  （下放包 46 §二 #4 僅點名後者，執行層依同一理由處理前者）。

### `newR1L-SU-043` ← `SWE1-FOTA-109` —— **不拆**

- 下放包 46 §三 預判「若各分支之畫面不同疑須拆」。**實測其多分支只在 s3**，
  **而 s3 已由 46 §三 委派 `092`–`095`**；s2 為單一條件（`<> [Waiting]`）。
  **委派之後只剩一個驗證單元。**

### `newR1L-SU-044` ← `SWE1-FOTA-088`

- **錨 `4907906` 之首選分僅 0.268**（機制 3 之門檻為 ≤ 0.267，**幾乎被攔**），
  **而其內容逐字命中**（`display pop-up, PU0303 after a successful FOTA update at
  Body ON mode`）。**其為「低分而對」，與 `037` 之「高分而錯」互為反面。**
- `PU0303` 於本列為**被驗者**；於 `028` 為**時點指標** —— 同一彈窗，二種用法。

### `newR1L-SU-045` ← `SWE1-FOTA-095`

- `PU0416` 為被驗者。Body ON 模式之判定委派 `088`（`044`）。

---

## 更正 —— 「整列全文」規則之出處

下放包 48 §1.1 記其出處為「下放包 45 §一」。**實測全 handoff 目錄，
其唯一出處為 `40_batch3_update_hmi.md:22`（下放包 40 §一-4）**：

> 4. **分析層之改寫指令一律給整列全文**，不再給逐句差分
>    （成因：下放包 39 §3.1 之逐句差分丟掉 `recorded screen content`）

45 §一 為 Pei 之分工裁定（「311 列起草 21 列用了 44 包」），非本規則。
**§1.1 之實質判斷完全正確**（規則立過、47 §三 又違、為同一種誤之第二次）——
**只是其自己說的「第一次是規則還沒立；第二次是規則立了而我沒守」**
**要成立，得指得到規則是在哪一包立的。**

**自本包起：凡改寫一律整列全文，無例外**（含「只改一行」之情形）。

---

## batch 4（ROV-E ＋ `USB Update`，執行層自行起草）

### 共通 —— **本批之自信度標記**（下放包 47 §四之分層抽驗）

高 2（`050`／`051`）／中 3（`046`／`048`／`049`）／**低 3**（`047`／`052`／`053`）。
**低＋中 = 6 列，即本批之抽驗組成**（≥5 不需補足；未逾 8 不觸發退回訊號）。

### `newR1L-SU-046`／`047` ← `104`／`105` —— ⚠ **其動力系集合為包含關係**

`104` 之集合 `[BEV] or [PHEV]` 為 `105` 之 `[BEV] or [PHEV] or [FCEV] or [REPB]`
**之真子集**，而二列之外部後果（顯示排程更新彈窗）**完全相同**。

**若二 TC 皆取 BEV 測試，其 procedure 與 ER 將逐字相同 —— 落入 R-SU32(iii)。**

**執行層之處置（待裁）**：`047` 取 `104` 所無之 **FCEV／REPB**，
使二者之 pre 與判定對象相異（`on this BEV or PHEV vehicle` vs
`on this FCEV or REPB vehicle`）。**其區分落在判定對象內**（R-SU41(c)）。

**⚠ 更正（下放包 49 §二 #2）**：該取值**不是執行層自定，取自規格** ——
`104` 之錨 `4907887` 為 `[BEV] or [PHEV]`、`105` 之錨 `4907888` 為
`[BEV] or [PHEV] or [FCEV] or [REPB]`，**其差即在 FCEV／REPB 二型**。
**自信度由 `低` 升 `中`。**

併記：CFTS 有二條同形而集合不同之條文（`4907887`／`4907888`），
**其為規格自身之重複或演進遺留，非我方之問題。**

### `newR1L-SU-048` ← `106`

- **錨為二個物件**：`4907890` 與 `4907891` **同分 0.640 且文字幾乎相同**
  （皆為 `When the HU receives $FOTA_Cancellation_Reason$, the HU shall display
  "Conditions Not Met" (PUxxx1) pop-up with the cancellation r…`）——
  **CFTS 側之重複條文**。取二者，待抽驗確認。
- **ER 以彈窗之功能描述指稱，不引編號**（下放包 43 §二 #2）——
  `PUxxx1` 於 037 與彈窗清單**二側皆為佔位**。

### `newR1L-SU-049` ← `080`（**105 列**）

- 其為本批唯一之 105 列。**依 R-SU29(b) 逐列求其外部後果，取得**：
  **「無 Wi-Fi 而更新仍完成」** —— 該後果可觀測（版本改變 ＋ 設定顯示無 Wi-Fi）。
  **內部之 USB 2.0 介面、Connectivity Manager 之組態皆不入 ER。**
- **故本列不掛 `PENDING`** —— 105 列不等於不可寫（R-SU32 v2(e) 之正交性）。

### `newR1L-SU-052`／`053` ← `083`／`084` —— **第三型，與 `081`／`082` 不可區辨**

**037 有二對近乎重複之列**：

| 對 | 其差別 |
|---|---|
| `081` vs `083` | `081`：二服務**各自**送 Arbiter；`083`：二者先送 WiFi Update Service **再轉送** |
| `082` vs `084` | 同上之路由差別 |

**其差別全在內部路由，外部後果完全相同** ——
`081`／`083` 皆為「取版本較高者部署」；`082`／`084` 皆為「同版本時取 FOTA」。

**依 R-SU32(iii) 掛第三型 `PENDING`，入 DR-SU2(c)**，
其所求為**區辨手段**（能觀測版本資訊經哪一條路徑送達 Arbiter），
**或上游確認該二對為重複之需求列而得併**（同 DR-SU3 之形）。

⚠ **執行層不逕自判其為重複** —— 二者之 `Source Requirement ID` 不同，
**合併需求單元屬上游**（R-SU32(d)）。


---

## batch 5（`Update Policy` 之逕行 12 列）

### 共通 —— **更新類型之判準**（下放包 49 §二 #1）

> 同一行為在**不同更新類型**下之規定，**不構成重疊** ——
> 一個系統可以 Silent 正確而 Critical 錯誤，故二者之判決可相異。

**且該區分須在判定對象內**（R-SU41(a)）——
故 `061`（`030`）之 ER 為 `…contains no download confirmation screen
**for this critical update**`，`065`（`037`）為 `…offers no reject control
**for this critical update**`。**類型不只在 pre，也在判定對象。**

### ⚠ `036` 由「逕行」改列「待裁」—— 下放包 49 §二 #1 之類型判定有誤

其令 `030`／`031`／`036`／`037` 逕行，理由為「**其為 Critical 而既有者為 Silent**」。
**實測 037 `SWE1-FOTA-036` 全文明寫 `classified as **Silent Update**`**，
且其內容（`shall not trigger the SW Update HMI for notifications, progress prompts`）
**與 `001`（`175`）／`002`（`176`A）相同**。

**依 §二 #1 自身之判準（行為同且類型亦同 → 待裁），`036` 應列待裁。**

### ⚠ `027`／`033` —— 執行層另判之組內相交

- **`027`**（Critical 不得拒絕 ＋ 安全條件前延後安裝）
- **`033`**（Critical 忽略 bearer 規則）
- **`037`**（Critical：覆寫 bearer 規則 ＋ 強制流程 ＋ 不提供 Reject、僅可延後）

**`037` 同時涵蓋 `027` 與 `033` 之行為，且三者皆為 Critical。**
依 §二 #1「同組內二列相交 → 列待裁」，`027`／`033` 列待裁，`037` 逕行
（其為下放包明令逕行者）。

### `newR1L-SU-054` ← `010`（105 列而不掛 `PENDING`）

其情境為「**無任何已設定之 Wi-Fi**」—— **TBM 為唯一可用之承載**，
故「更新仍完成」即足以證其走了 TBM。**bearer 之選擇於此情境下由排除法可證。**

### `newR1L-SU-056`／`058`／`059` ← `012`／`014`／`015` —— **第二型 `PENDING`**

三列之判定核心皆繫於「**用哪一條網路**」，而 **bearer 於 HU 外部無表徵**：

| TC | 其欲驗者 | 為何不可觀測 |
|---|---|---|
| `056`（`012`） | Body OFF 時由 TBM 切至 Wi-Fi | 二者皆使下載繼續，外部無別 |
| `058`（`014`） | Critical 於 Body ON **即使 Wi-Fi 可用仍走 TBM** | Wi-Fi 可用而走 TBM，其差全在 bearer |
| `059`（`015`） | Body OFF→ON 後由 TBM 續傳 | 同上 |

**與 `054` 之別**：`054` 之情境使 TBM 成為唯一可能，**其餘三列則二者皆可能**。
**故同一個「bearer 不可觀測」，在 `054` 可由排除法繞過，在三列不可。**

### `newR1L-SU-060` ← `028` —— **第四型（觸發手段不可得）**

其觸發為「Wi-Fi 連線或下載**一週內**未完成」。**等待一週不可行**，
而**加速該計時器之手段無來源**。DR 之請求為**觸發手段**，非觀測手段。

⚠ **不寫任何秒數或替代閾值** —— 037 之 `one week` 為規格值，
**縮短它即改變被驗之需求**（§8.4.1）。

### `newR1L-SU-062` ← `031` —— 其類型來自錨而非列文

**037 `031` 全文查無任何更新類型**（`Critical`／`Silent`／`Regular` 皆無）。
其 Critical 身分**來自錨 `4907471` 之章別（4.7.3.1 Critical Updates）**，
**而該錨為 GT-B 已裁**（三證齊備）。

⚠ **此依賴須明記**：若日後該錨被推翻，本列之類型判定隨之失效，
**而 §二 #1 之判準（類型不同即不相交）於本列即不再成立。**

### `newR1L-SU-063` ← `034`

其判定對象為**安裝之先後**（critical 裝了、regular 在其 pending 期間未裝），
**非優先序之內部分類**。須同時佈置二個不同類型之 campaign。

---

## 抽驗制度 —— **「標記與追溯」**（下放包 49 §二 #6 之正名）

**本節原稱「誘因設計」，自下放包 49 §二 #6 起正名為「標記與追溯」。**

> 其功能為**記錄**，不是**行為引導**。
> **稱之為誘因會使人以為制度已自我保護，而它其實依賴一個未被寫下的前提：
> 執行層想把事情做對。** —— **該前提現寫下了。**

**罰則保留**（標 `高` 而錯 → 該批全退 ＋ 其後三批全隨機抽 8），
**惟其性質記為「留下記錄」而非嚇阻。**

### 指標：標 `高` 之列於抽驗中被挑錯之比率

| 批 | 標 `高` 之列 | 進抽驗者 | **被挑錯** | 累計比率 |
|---|---:|---:|---:|---|
| batch 4 | 2（`050`／`051`） | 0 | 0 | — |
| batch 5 | 2（`055`／`061`） | 0 | **待分析層抽驗** | — |
| **累計** | **4** | **0** | **0** | **基數為 0，尚不可計** |

**其為指標而非罰則，不需假設任何動機。**

---

## `W` 檢查之 (a) 已裁段（下放包 49 §二 #4）

**八列入 (a)，其後各包只報列數。(a) 段之列若 ER 再有改動即回 (b) 段**（下放包 48 §二）。

| TC | 命中之詞 | 裁定依據 |
|---|---|---|
| `001`／`005`／`008`／`010`／`022`／`050`／**`054`**／**`060`** | `differs from`／`equals` | **版本字串之相異／相等為精確比較，無精度問題**（`054`／`060` 依下放包 50 §二 #5 增入） |
| `039` | `greater than` | **與零比較為精確** |
| `041` | `equals` | **精度由畫面粒度給出**（下放包 47 §三） |

**出處：下放包 49 §二 #4。**

---

## 待裁登記（`Update Policy` 之 5 列）＋ 判準補句

**下放包 50 §二 #1／#2 追認**：`036`（Silent，非 Critical）、
`027`／`033`（皆與 `037` 相交且同為 Critical）改列待裁；連同 `025`／`026` 共 **5 列**。

### 類型認定之判準（下放包 50 §二 #3 補入）

> 類型之認定得取自**列文**或**已裁之錨**（GT 或分析層已裁者）；
> **未裁之錨不得用於認定類型** —— 該列之型別繫於錨，
> 而錨之正確性又繫於內容之相符，**以未裁之錨定型別即為循環**（同 R-SU20(d)）。

`062`（`031`）之逕行據此成立：其錨 `4907471` 為 **GT-B 已裁**（三證齊備）。

---

## 抽驗制度之改制（下放包 50 §二 #4）

> **`PENDING` 與 `低` 信心標記是同一件事的兩種記法** ——
> 其不確定性已由 `PENDING` **顯式承載**，不需再由抽驗攔一次。
> **重複計數會使訊號失真。**

**三項**：
1. **抽驗組成排除已掛 `PENDING` 之列**（其仍列於標記表，只是不進抽驗、不計入訊號）
2. **退回訊號只看 `低`**：扣除 `PENDING` 後之 `低` **逾 3 列**即退回；`中` 全數納入抽驗但不觸發退回
3. batch 5 依新制重算：扣除後 `低` = **0**、`中` = **6** → **不觸發，不退回**

**其同時解掉 B-19**（閾值固定而組成隨批量增長）—— 新閾值只數 `低` 且排除 `PENDING`，
**不隨批量線性增長**。

### 標記表（batch 5，增「是否已掛 `PENDING`」欄）

| TC | 037 | 自信度 | **已掛 `PENDING`** | 進抽驗？ |
|---|---|---|:--:|:--:|
| `054` | `010` | 中 | — | ✅ |
| `055` | `011` | 高 | — | — |
| `056` | `012` | 低 | **✅** | ❌（排除） |
| `057` | `013` | 中 | — | ✅ |
| `058` | `014` | 低 | **✅** | ❌ |
| `059` | `015` | 低 | **✅** | ❌ |
| `060` | `028` | 低 | **✅** | ❌ |
| `061` | `030` | 高 | — | — |
| `062` | `031` | 中 | — | ✅ |
| `063` | `034` | 中 | — | ✅ |
| `064` | `035` | 中 | — | ✅ |
| `065` | `037` | 中 | — | ✅ |

**抽驗組成 6 列（全部 `中`）；扣除後 `低` = 0，訊號不觸發。**


---

## batch 7（`Wi-Fi Download`，28 列）—— T66（下放包 54）

### 一、五個門檻列之來源與逐字值（**IN §8.7.1 於本 feature 首次實際套用**）

| TC | 037 列 | 門檻之逐字值（037 原文） | 其於 TC 中之量法 |
|---|---|---|---|
| `SU-086` | `043` | `within 15 seconds`（Host → Client） | 連續錄影之時戳差（R-SU36）：`Time_client` − `Time_confirm` ≤ 15 s |
| `SU-096` | `054` | `within 15 seconds`（Client → Host） | 同上：`Time_host` − `Time_exit` ≤ 15 s；Host Mode 之表徵取**熱點恢復可用** |
| `SU-104` | `065` | `5 consecutive Wi-Fi connection attempts` | 以無網際網路之 AP 使下載連續失敗 5 次，判其自可選清單移除 |
| `SU-107` | `069` | `$IBS_SOC$ > [65]` | 以 `$IBS_SOC$` = 60% 佈置負向面，判其不啟動下載 |
| `SU-109` | `071` | `within 3 minutes` | 時戳差：`Time_next` − `Time_start` ≥ 3 min |

> **五列之值全部逐字取自 037，無一為我方自訂**（§8.4.1）。
> **`057` 之 `30 minutes` 不在本表** —— 其於 037 與嵌入物件 `4908702` 之**起算點不一致**，
> 二者皆為上游文件，**我方無權擇一**（下放包 54 §二 #1），該列不起草。

### 二、`$IBS_SOC$` 之可觀測性 —— 比照 `$Speedometer$`

電池 SOC 為**車輛之物理狀態**，台架可控亦可讀，**與 FOTA 內部狀態不同族**
（下放包 46 §二 #3 對 `$Speedometer$` 之裁定）。故 `069`／`070` 之 pre 與 ER
得引其值（60%／80%），不比照 `$FOTA_Status$` 排除。

### 三、`062` 改判第三型 —— **遮蔽測試第二次抓到起草者**

首版之 `062` 判定核心取「訊號強度為主要準則」，
**其 Final Step 與 `060` 逐字相同** —— 二列所驗者皆為「連上訊號較強之網路」。

> **`062` 之唯一區分是排除清單，而排除清單於外部無任何表徵。**
> 依 R-SU32(iii)，**其為不可區辨**，非「可寫而寫得不好」。

**故 `062` 改掛第三型**（Final Step 為 `PENDING`，所求為**區辨手段**），入 DR-SU2(c)。

> **本項與 batch 6 之共通形態函式不同**：那次是**寫法**製造了相同，
> **這次是內容本來就相同** —— 而**二次皆由遮蔽測試發現，lint 皆為全綠。**

### 四、統攝殘餘（R-SU37 v2）—— 不掛 `PENDING` 之二處

| TC | 037 列 | 殘餘 |
|---|---|---|
| `SU-083` | `040` | `notify subscribed modules that software download via Wi-Fi has been enabled` —— 服務間之通知，無外部後果 |
| `SU-089` | `047` | 四個儲存欄位（SSID／security type／encryption type／passphrase）**於外部無逐項表徵**；判定核心取**再次連線不再要求密碼** |

**二者皆為「主句可驗、子句無面」** —— 依 R-SU37 v2 記為殘餘，**不整列掛起**。

### 五、intake 規範增一則 —— **由圖找列**（下放包 54 §二 #5）

> **本則已於 2026-08-30 升格為全域條文 `R-G28`**（下放包 55；`FO §9.2`）。
> **feature 側不重述其內容，一律引用** —— 見 `FO §9.2 R-G28`
> 與本 feature 之 `ANCHOR_POOL.md` 附記、BACKLOG B-25。
>
> 其於本 feature 之實施記錄：上繳包 46（六物件之 intake 與由圖找列）、
> 上繳包 47 §1.3（客戶素材目錄之盤點）。

### 六、偏向指標（下放包 52 §二 #5(a)，**每包一列**）

| 批 | 標的組 | 已起草 105 比率 | 剩餘 105 比率 | **偏向量** |
|---|---|---:|---:|---:|
| batch 6 | `Integrity Verification` ＋ `Deployment Conditions` | 22%（17/75） | 37%（88/236） | **15 pt** |
| **batch 7** | **`Wi-Fi Download`（105 比率 17%）** | **20%（21/103）** | **40%（84/208）** | **20 pt** |

> ⚠ **偏向擴大了 5 個百分點** —— 本批之標的是**至今 105 比率最低**的一組（17%）。
> **配額（下放包 52 §二 #5(c)：batch 7 必含 `Status Reporting`）並未在本批適用**，
> 其成因見上繳包 47 §7：**該批（T64）至今未執行**。

---

## batch 8（配額批：`Status Reporting` ＋ `FOTA Overview`，13 列）—— T64（下放包 52）

### 一、⚠ 105 分類之**第三種結局**：表徵存在，而在**車外**

`Status Reporting` 七列中六列（`330`／`331`／`333`／`334`／`339`／`358`）
其 037 之 `Verification Criteria` **明白寫著觀測點** ——
`reported to the OTA server`／`received by the OTA server`。

> **它們不是「沒有表徵」（第二型），也不是「表徵為他列所轄」（第三型）。**
> **它們的表徵在 OTA 伺服器上，而伺服器側之可及性從未確立。**

**我方至今之台架假設只涵蓋「在伺服器上佈置 campaign」**
（各批之 pre 皆有 `An update package is staged on the OTA Server`），
**而「讀取伺服器所收到的 session report」是另一回事** ——
**能佈置不蘊含能讀取**（同 R-SU26 與 R-SU40 之「全覽做過不蘊含值域已知」之形）。

**故六列一律掛 `PENDING`，其請求為：伺服器側之讀取手段。**
**不逕自假設有伺服器主控台可看** —— 該假設聽起來很合理，而它沒有來源（§8.4.1）。

### 二、`337` vs `010`（`181`）之判定對象比對（T64c，下放包 52 §二 #3）

| TC | 037 | 判定對象 |
|---|---|---|
| `SU-010` | `181` | **其間無使用者互動** —— 否定式：下載完成與安裝之間沒有任何使用者輸入 |
| `SU-075` | `337` | **部署流程啟動** —— 肯定式：收到 SWMC 之部署通知後部署開始 |

**判定對象不同，二列皆維持**（依下放包 52 §二 #3 之判準）。

> **惟其重疊須記明**：`337` 所驗之「部署於下載完成後啟動」，
> **在 `010` 之步驟序列中是其前提** —— 二者不是互斥，是**一個包含另一個之前半**。
> **若日後 `010` 之 `immediately` 得到閾值而改寫，本重疊須重判。**

### 三、起草規範增二則（T64e）

1. **共通形態函式**（下放包 52 §二 #6）：凡以共通函式或樣板產出**三列以上**之
   Final Step 者，**產出後必跑遮蔽測試**，不待批次末之全簿測試。
   **其代價低（一次比對），而其攔截之對象是 lint 全綠、`I-sibling` 與 `I-cross` 皆看不見者。**
2. **句首缺字之處置**（下放包 52 §二 #2）：037 之缺字落在句首時，
   **`test_item` 上半 verbatim 保留，lint 之 `J` 隨之為 1，其成因逐包揭露**；
   **不得以 R-4 之「句首大寫正規化」補之** —— 該款預設句首那個字是完整的。
   **（batch 8 另遇句尾截斷，登 D-6，處置同此：逐字保留並列待裁。）**

### 四、`W` 之 (a) 段（T64b，下放包 52 §二 #1）

batch 6 之九處新命中（`equals Version_initial` 族）**併入 (a) 已裁段**，
**(a) 段由 10 列增為 18 列**；其後只報列數，不逐列陳述。
**batch 7／batch 8 之 `W` 皆為 0**，(a) 段列數不變。

### 五、偏向指標（續 batch 7 之表）

| 批 | 標的組 | 已起草 105 比率 | 剩餘 105 比率 | **偏向量** |
|---|---|---:|---:|---:|
| batch 6 | `Integrity Verification`＋`Deployment Conditions` | 22% | 37% | 15 pt |
| batch 7 | `Wi-Fi Download`（17%） | 20% | 40% | **20 pt** |
| **batch 8** | **`Status Reporting`（71%）＋`FOTA Overview`（33%）** | **24%（28/116）** | **39%（77/195）** | **15 pt** |

> **配額有效：偏向由 20 pt 收回 15 pt。** 其代價見上繳包 48 §7
> —— **13 列中僅 4 列無 `PENDING`。**

---

## batch 9（`Session Management` 13 列，配額批）—— T69g（下放包 57）

### 一、選組：**其 105 比率 38% 高於母體，而 51 §二 之優先序本不該選它**

| 判準 | 本批 |
|---|---|
| 51 §二 #1（105 低者優先） | ❌ **未依** —— 剩餘各組中 `TBM Reflash`(7%)／`HU FOTA via TBM`(6%) 才是最低者 |
| 52 §二 #5(b) 配額（每三批含一 105 ≥ 33% 之組） | ✅ **依之，且於配額窗第一批即滿足** |
| 批量 12–20 | ✅ 13 |
| 避開 bearer 族／大組 | ✅ |

> **二個判準在此互相衝突，我取配額而非優先序 #1**，
> 其理由是**偏向指標**：batch 7 之偏向量曾擴至 20 pt，
> **若本批再取 7% 之組，其只會更大**。**列待裁（上繳包 57 §5）。**

### 二、⚠ 本組之三對重複表述（入 DR-SU3）

| 對 | 其差 |
|---|---|
| `350`／`368` | 皆為「前提不滿足即佇列」；`368` 多一段 **TC client 之轉送** |
| `351`／`369` | 皆為「server-initiated 與 vehicle-initiated 同流程」；`369` 多同一段轉送 |
| `353`／`337` | `353` 後半（下載完成後啟動部署流程）**已由 `337` 驗** |

**三對之外部後果皆相同**，其差全在**內部路由**——
**與下放包 49 §二 #3 所裁之 `081`／`083`、`082`／`084` 同型**（DR-SU3 (ii) 之族）。

### 三、`361` —— **第四型而其可觀測面存在**

`361`（背景執行不阻塞前景）之判定核心**可觀測**：更新流程執行中，
調台與開設定選單仍可用。**缺者只有觸發**（server-initiated 之推送）。

> **這是第四型之標準形態，而本批有三列同型**（`351`／`361`／`369`）——
> **其所缺者是同一件事：使 OTA 伺服器主動向本車發起 session 之手段。**
> 三列共用同一個 `PENDING` 之請求，**故其解一次到位**。

### 四、偏向指標（續）

| 批 | 標的組 | 已起草 | 剩餘 | **偏向量** |
|---|---|---:|---:|---:|
| batch 6 | Integrity Verification＋Deployment Conditions | 22% | 37% | 15 pt |
| batch 7 | Wi-Fi Download（17%） | 20% | 40% | **20 pt** |
| batch 8 | Status Reporting（71%）＋FOTA Overview（33%） | 24% | 39% | 15 pt |
| **batch 9** | **Session Management（38%）** | **26%（33/129）** | **40%（72/182）** | **14 pt** |

**連二批收斂（20 → 15 → 14 pt），惟剩餘之 105 比率仍為 40%，未下降。**

---

## batch 10（`TBM Reflash` 14 列）—— T70h（下放包 58）

### 一、選組：配額窗已滿足，依 51 §二 #1 取 105 最低者

| 判準 | 值 |
|---|---|
| **105 比率** | **7%（1／14）** —— 剩餘各組中最低（`HU FOTA via TBM` 6% 但為 36 列，逾批量上限） |
| **偏向量預估** | **18 pt**（取之前 14 pt）—— **實測結果與預估相同** |
| 機制 3 攔下 | **0 列** —— 本組錨定風險為至今最低 |
| 批量 | 14（12–20） |

> **本批之選擇使偏向量由 14 pt 升為 18 pt** —— 其為配額制之預期行為：
> **配額批壓低偏向，低 105 之批推高它，二者交替。**
> **重點不在單批之值，在其是否收斂** —— 現為 15／20／15／14／**18**，**未收斂。**

### 二、⚠ `123`／`124` —— 又一對「其差只在內部狀態值」

二列所令之動作**逐字相同**（清除全部 TBM FOTA 彈窗與狀態列），
其差為 `$TBMUpdate$` 之 `No_Updates_Available` 與 `No_Update` 二值 ——
**而該值於外部無表徵**。`124` 掛第三型入 DR-SU2(c)。

**本 feature 之第四種同型**（前三為 `060`／`062`、`331`／`332`、`350`／`368` 等）——
惟**其成因與前三不同**：前三之差在**內部路由**，本對之差在**內部狀態值**。
**二者皆為「外部後果相同而條文分列」，其請求同屬 DR-SU3 之改寫後問法。**

### 三、`119`／`121` 與 `120`／`122` —— **可區分，因其 UI 元件不同**

同為 `Update_End` 觸發，`119` 驗**成功彈窗**、`121` 驗**結束畫面**；
`Update_Fail` 之 `120`（回復成功彈窗）與 `122`（失敗畫面）亦然。
**彈窗與畫面是二個可分別觀察之物**，故不構成不可區辨。

> **與 `123`／`124` 之對照即其判準**：
> **UI 元件不同 → 可區分；只有內部狀態值不同 → 不可區分。**

### 四、`116` 之未覆蓋 facet（**本 feature 無 `COVERAGE_GAPS.md`，記於此**）

037 `116` 之三個使用者動作（`Update Later`／**忽略**／**關閉**）為
**三個可獨立觸發之情境**。本批之 TC 取 `Update Later`；
**「忽略」與「關閉」二者未起草** —— 其為 IN §8.2.2 之可拆而未拆，
**非不可觀測，只是未做**。**若本 feature 立 `COVERAGE_GAPS.md`，此為其第一項。**

### 五、偏向指標（續）

| 批 | 標的組 | 本批 105 比率 | **窗目標／窗內累計** | **主指標：剩餘 105 比率** | 次：偏向量 |
|---|---|---:|---|---:|---:|
| batch 7 | Wi-Fi Download | 17% | （舊制） | 40% | 20 pt |
| batch 8 | Status Reporting＋FOTA Overview | 52% | （舊制配額批） | 39% | 15 pt |
| batch 9 | Session Management | 38% | （舊制配額批） | 40% | 14 pt |
| batch 10 | TBM Reflash | 7% | （舊制） | **42%** | 18 pt |
| **batch 11** | **Session Flows** | **62%** | **42%／62%（1/3 批）✅** | **40%（↓）** | 12 pt |

> **配額之形式（下放包 60 §三 #4）**：目標值**固定於窗起始之剩餘比率**（本窗 42%），
> 判準為**窗內前 k 批之合計 ≥ 目標**，**目標只在窗界動，不在窗內動**。
> **並用**：配額看窗（規劃用）／**主指標「剩餘比率須逐批下降」看每批（監測用）** ——
> 主指標是**不能靠一批超標買斷**的要求。

---

## 起草規範增二則 —— T71f（下放包 59 §三 #4、#5）

### 一、可區辨性之判準：**UI 元件 vs 內部狀態值**

> **UI 元件不同 → 可區分；只有內部狀態值不同 → 不可區分。**

| 對 | 其差 | 判定 |
|---|---|---|
| `119`／`121` | 成功**彈窗** vs 結束**畫面** | ✅ 可區分（二個可分別觀察之物） |
| `120`／`122` | 回復成功**彈窗** vs 失敗**畫面** | ✅ 可區分 |
| `123`／`124` | `$TBMUpdate$` 之 `No_Updates_Available` vs `No_Update` | ❌ **不可區分** —— 其所令之動作逐字相同，差在無外部表徵之內部值 |

### 二、D-7 之取材界線（結構層缺陷之處置）

```
規範句與其後之取值註腳，以「未斷句即接列舉」為界。
註腳（如 `$OperationalModeSts$` 之取值表）不入 `test_item` 上半；
其存在與其界記於 `DESCRIPTION_DEFECTS.md`。

D-1～D-6 為字元層（缺字／拼錯／截斷），D-7 為結構層（規範與註腳未分開）
—— 二者之處置不同：字元層 verbatim 保留，結構層須劃界並記其界。
```

---

## batch 11（`Session Flows` 16 列）—— T71h（下放包 59）

### 一、新配額之首批

| | 值 |
|---|---|
| 窗（batch 11–13）起始剩餘 105 比率 | **42%** |
| **本批 105 比率** | **62%（10／16）** |
| 窗內累計（第 1／3 批） | **62%** —— **已高於目標** |
| 主指標：**剩餘 105 比率** | **42% → 40%** —— **五批以來首次下降** |
| 次指標：偏向量 | 18 pt → **12 pt** |

> **新指標第一次就給出了舊指標給不出的資訊**：舊制之偏向量在 batch 8／9 也曾下降，
> **而剩餘比率當時一直是 39–40%，沒有動。** 本批是**剩餘比率第一次真的下降**。

### 二、⚠ 至今最大之一組重複表述：**五列講同一件事**

`274`／`275`／`276`（本批）＋ `347`／`348`（batch 9）——
**五列皆為「輪詢間隔／輪詢參數之組態」**：

| 037 列 | 其述 |
|---|---|
| `347` | 支援可組態之輪詢間隔，預設 24 小時 |
| `348` | 支援以小時為單位之組態與再組態 |
| `274` | 維持可組態之輪詢參數並於有連線時週期通訊 |
| `275` | 支援自伺服器參數組態輪詢間隔並套用 |
| `276` | 支援可自伺服器更新之輪詢參數並於週期 session 套用 |

**其外部後果完全相同（車輛依所設之間隔輪詢），差別全在敘述之切面**
（預設值／單位／維持／來源／套用時機）。**入 DR-SU3。**

**另一對**：`272`／`277` —— 皆為「經事件介面接收伺服器發起之 session」，
`277` 多「通知 WiFiUpdateService」一段（內部）。

### 三、`019` —— **105 列而可寫**（R-SU32 v2(e) 之又一例）

其判定核心為**否定式之外部後果**：`$PowerMode$` 非 IGN_RUN 且非引擎自動停止時，
**經內建數據機之下載不啟動** —— 可觀測、可觸發（點火置於 ACC）。

### 四、lint 首版之 `H=1`／`W=1`

`SU-160`（`275`）之 ER 用了 `matches` —— **下放包 47 §二 #6 所增之關係模糊詞**。
**已改為 `is the value sent by the OTA Server`。**

> **該詞是我自己寫的，而它正是我方在 47 包請分析層加進詞表的那一個。**
> **詞表擴充之後第一次抓到的人是我。**

---

## batch 12（`Update Agent` 14 列）—— T72f（下放包 60）

### 一、窗內第 2 批

| | 值 |
|---|---|
| 本批 105 比率 | **57%（8／14）** |
| **窗內累計（2／3 批）** | **60%（18／30）**，目標 42% ✅ |
| **主指標：剩餘 105 比率** | **40% → 38%（續降）** |
| 次指標：偏向量 | 12 → **8 pt** |

### 二、⚠ 本組之性質不同於前十一批：**其述者為「能力」而非「行為」**

`375`（安裝後之映像與參考映像相同）、`379`（永不 brick）、`382` 後半
（結果映像與目標映像相符）—— **三列皆為全稱或不可取樣之命題**。

> **R-SU43(f) 之案例三例並見**：
> **一次執行不能證實「永不 brick」**；
> **而若把 ER 寫成「更新後系統仍可開機」，它會永遠通過** ——
> **那正是 PLAYBOOK (41)／(46) 所記之「查了但那個查法不可能失敗」。**
>
> **故三列一律掛 `PENDING`，其請求為「該能力於單次台架執行中之判準」**，
> **而非「觀測手段」** —— 二者不同：後者問「去哪裡看」，前者問「看到什麼算通過」。

### 三、`380` 為本批唯一可寫，且其 ER 須為**析取式**

037 載 `resume **or** safely terminate` —— **二支皆為合格行為**。
依 R-SU43 v2（判定核心不得斷言規格明文允許為他者之值），
其 ER 寫為「**開機後或續行安裝、或顯示更新前之版本**」。

**斷電為可注入之中斷**（台架可斷可復），**故本列不落入第四型。**

### 四、⚠ 新增二對重複表述

| 對 | 其差 |
|---|---|
| `371`／`169`（batch 11） | 皆為「依 deployment metadata 選定安裝器」，`169` 另含「轉送至 Update Engine」一段 |
| `382` 前半／`172`（batch 6） | 皆為「來源版本不符即拒絕」 |

**入 DR-SU3（第四形態：敘述切面）。**

### 五、`W=1` 為 (b) 段新命中，**其措辭來自 037**

`SU-171`（`375`）之 ER 含 `identical to`，而 `test_item` 上半無數值 ——
**該片語為 037 原文（`identical to the reference deployment image`）**，
依 R-4 逐字保留。**列 (b) 段之新命中，待人裁。**

---

## batch 13（`Interruption Handling` 12 ＋ `Telematics Client` 5）—— T73f（下放包 61）

### 一、窗結算（batch 11–13）

| 批 | 標的組 | 本批 105 比率 | 主指標：剩餘 105 比率 |
|---|---|---:|---:|
| batch 11 | Session Flows | 62% | 40% |
| batch 12 | Update Agent | 57% | 38% |
| **batch 13** | **Interruption Handling ＋ Telematics Client** | **76%** | **33.1%** |
| **窗累計** | — | **66%（31／47）** vs 目標 **42%** ✅ | **偏向量 1 pt** |

**下窗（batch 14–16）之目標 = 33%**（本窗結束時之剩餘比率）。

> **主指標三批連降 42 → 40 → 38 → 33%，偏向量由 18 pt 收至 1 pt。**
> **舊制五批未動之數，新制三批降了 9 個百分點。**

### 二、退回訊號拆二之首次適用（下放包 61 §三 #4）

| 訊號 | 本批 |
|---|---:|
| **品質訊號**（`低` 之未承載理由**非**錨定） | **0 列** → 不退回 |
| **錨定訊號**（其理由為機制 3 之錨） | **3 列**（`321`／`327`／`329`）→ **不觸發退回，全數納入抽驗** |

**`REASONING.md` 之標記表自本批起記「`低` 之成因分類」**（`True` = 由 `PENDING` 承載／
`anchor` = 錨定成因／其餘 = 品質成因）。

### 三、析取式之二分（T73e，下放包 61 §三 #6）

> **R-SU43(e) 所禁之析取式，指「我方為涵蓋全部合規結果而自造之析取」。**
> **規格自身載有 `or` 者（如 `380` 之 `resume or safely terminate`），ER 照抄其析取為正**
> —— 其非我方之涵蓋，是規格之規定。
> **二者之別**：前者使 TC 不驗任何事；**後者所驗者即「二支之外皆為不合格」。**

**`W` 之 (a) 已裁段增 `375`**（`identical to` 為 037 原文，同 `041` 之 `equals`）——
**(a) 段由 18 列增為 19 列。**

### 四、⚠ 本批之三組相交

| 組 | 其差 |
|---|---|
| `360`／`324`／`325`／`328` | `360` 為「偵測—保存—續傳」之統攝式表述，三列各持其一 |
| `365`／`366`／`367` | 三列共用同一句「自 TC client 收受並轉送」，其差僅在後半 |
| `322`／`346`（batch 6） | `346` 驗「空間不足則不啟動」，`322` 驗「偵測後中止並回報」 |

**前二組入 DR-SU3；第三組之判定對象不同，二列皆維持。**

---

## batch 14（`Client Architecture` 前 16 列）—— T74d（下放包 62）

### 一、⚠ 首次拆組，其拆法為**列序**

`Client Architecture` 共 35 列，**逾批量上限 20**。本批取**列序之前 16 列**，
**不依內容挑選** —— **免得又一次「挑容易的」**（B-23 之教訓）。其餘 19 列隨後續二批。

### 二、下窗（14–16）第 1 批

| | 值 |
|---|---|
| 本批 105 比率 | **38%（6／16）**，目標 33% ✅ |
| 窗累計（1／3） | 38% |
| **主指標：剩餘 105 比率** | **33.1% → 32.4%** |
| 訊號拆二 | 品質 0 → 不退回；錨定 4 列全數納入抽驗 |

### 三、DR-SU6 由 3 列增為 6 列

`195`（不得依賴特定匯流排實作）／`198`（`no unintended DTCs`）／`212`（HMI 可移植性）
—— 依下放包 62 §三 #2 之判別問句：

> **「若這一次跑出來是好的，它證明了什麼？」** 三者皆答「**證明這一次沒事**」。

**`198` 另有一項獨立缺口**：`unintended` 之界未載 ——
**即使有判準，也還需要一份「哪些 DTC 算 intended」之清單。**

### 四、「依 metadata 選定並呼叫安裝器」—— **同一件事之第四次出現**

`169`（batch 11）／`371`（batch 12）／`209`／`253`（batch 14）——
**四列分屬三組（`Session Flows`／`Update Agent`／`Client Architecture`）**，
**其外部後果完全相同。** 入 DR-SU3。

**併記 `207`／`208` 之機器證據**：二列之路徑 A 首選錨**為同一物件 `4907322`** ——
**其同源不需人眼判斷，錨自己指出來了。**

---

## 拆組之常規（T75e，下放包 63 §三 #4）

```
逾批量上限之組，其拆分一律依 037 之列序，不依內容挑選。
理由：依內容挑選會在「難易」上產生選擇，而該選擇不可見於任何指標
（B-23：偏向不只使難列堆積，還使對母體性質之判斷失真）。
列序是外生的 —— 它由上游文件決定，不由我方決定。
```

## batch 15（`Client Architecture` 第 17–28 列）—— T75f

| | 值 |
|---|---|
| 本批 105 比率 | **58%（7／12）**，目標 33% ✅ |
| 窗累計（2／3） | **46%（13／28）** |
| **主指標：剩餘 105 比率** | **32.4% → 29.0%** |
| 可交付 | **0 列** —— 本批 12 列全部掛 `PENDING` |
| 037 覆蓋 | **218／311 = 70%** |

**⚠ 本批之重複表述密度為至今最高**：`255`／`256`（**同錨 `4907332`**）、
`260`／`268`（OMA-DM）、**`264`／`265` 為「選定安裝器」之第五、六次**
（前四次 `169`／`371`／`209`／`253`）。

**DR-SU6 增 `257`（獨立於 OS 與 flash driver）／`267`（RBUA 之可移植性）** → **8 列**。

---

## batch 16（三組收尾，14 列）—— T76f（下放包 64）

### 一、窗結算（batch 14–16）

| 批 | 標的 | 本批 105 比率 | 主指標：剩餘 105 比率 |
|---|---|---:|---:|
| batch 14 | Client Architecture 前 16 | 38% | 32.4% |
| batch 15 | Client Architecture 17–28 | 58% | 29.0% |
| **batch 16** | **CA 末 7 ＋ Update Policy 5 ＋ Configurable Parameters 2** | **43%** | **26.6%** |
| **窗累計** | — | **45%（19／42）** vs 目標 33% ✅ | — |

**下窗（17–19）目標 = 27%。037 覆蓋 232／311 = 75%。**
**剩餘四組**：`HU FOTA via TBM` 36、`Deployment Flow` 26、`Bearer Selection` 16、`Wi-Fi Download` 1（`057`，待裁）。

### 二、⚠ `Client Architecture` 之交付率為全案最低（下放包 64 §三 #5 令記）

**35 列跨三批，可交付僅 2 列（`203`／`213`，皆為 HMI 之顯示）。**

> **其成因在該組之性質，不在起草品質**：
> 35 列中大宗為**架構性陳述**（介面之定義、平台無關性、可移植性、
> 背景執行、資源用量、模組間之分派）——
> **其驗證面在內部或在「一次執行不能否證」之層次**：
> 本組貢獻了 DR-SU6 之 13 列中之 **7 列**（`195`／`212`／`257`／`267`／`269`／`270`／`282`／`283`／`284` 之多數）。
>
> **記此免得日後讀者以為是我方寫壞了。**

### 三、`Update Policy` 五列之待裁狀態解除

`025`／`026`／`027`／`033`／`036` 自下放包 49／50 起列待裁，
**本批依已裁之判準起草**（Critical 之自動啟動與無 opt-out 可觀測；
分類與 bearer 偏好無外部表徵）。**`036` 為 Silent Update 組之統攝式表述，入 DR-SU3。**

---

## batch 17（`Deployment Flow` 前 16 列）—— T77e（下放包 65）

| | 值 |
|---|---|
| 本批 105 比率 | **31%（5／16）**，下窗目標 27% ✅ |
| **主指標：剩餘 105 比率** | **26.6% → 25.4%** |
| **可交付** | **12 列**（全案單批最高） |
| `PENDING` | 11 行 |
| **037 覆蓋** | **248／311 = 80%** |

### 一、⚠ 四個門檻列，其中一個與既有之 30 分鐘**不同源**

| 037 列 | 門檻（逐字） |
|---|---|
| `143` | `a maximum period of 30 minutes after key-off` |
| `145` | `$IBS_SOC$ < [80]` |
| `146` | `$IBS_SOC_ACCURACY$ = [0] or [SNA]` |
| `147` | `only when $PowerMode$ = [IGN_OFF]` |
| `149`／`150` | 彈窗自動解除上限 **20 次** |

> **`143` 之 30 分鐘自 key-off 起算；`057` 之 30 分鐘自 session 起算；
> `4908702` 之 30 分鐘自 timed mode 到期起算 —— 三個 30 分鐘，三個起算點。**
> **`143` 與 `057` 二者皆為 037 之文字，其並存不構成矛盾**（分屬不同情境），
> **而其與嵌入物件之第三個 30 分鐘仍為 DR（下放包 54 §二 #1）。**

### 二、`145` 之 037 原文有斷字缺陷 —— 登 **D-8**

`the WiFi Update Servic/USB Update Service e` —— **`Service` 之 `e` 被推到句中**。
**其不觸發 `J`（首字為 `If`），故 lint 攔不到** ——
**只能由逐字抄寫時發現，而我是在修另一個錯時才看到它。**

### 三、`148` 之 `PU0304` 只在標題欄

037 之 Description 只寫 `the pop-up`，**編號在標題**。
**故 ER 不引該編號**（同下放包 43 §二 #2 之處置）。
**⚠ 本項為「標題有而條文無」之首例** —— 起草時只讀 Description 者會錯過它，
**只讀標題者會把它當成條文之一部分。**

---

## batch 18（`Deployment Flow` 餘 10 ＋ `HU FOTA via TBM` 前 6）—— T78f（下放包 66）

| | 值 |
|---|---|
| 本批 105 比率 | **19%（3／16）** vs 目標 27% ❌ |
| 窗累計（2／3） | 25%（8／32） |
| **主指標：剩餘 105 比率** | **25.4% → 27.7%（上升）** |
| 可交付 | 7 列｜037 覆蓋 **264／311 = 85%** |

### 一、⚠ 目標未達之成因是**可選之組已窮**，非選組

剩餘三組：`HU FOTA via TBM` 30（**6%**）、`Bearer Selection` 16（**62%，待 DR-SU2(a)**）、`057` 1。
**扣除 Bearer 後，剩餘 31 列之 105 列僅 2 列** ——
**batch 19 之 105 比率上限 6%，窗累計上限 21%，無論如何選都達不到 27%。**
**`Bearer Selection` 一組即佔剩餘 105 列之 13／15。**

### 二、`157`／`162`／`167` —— 三列同源之最大一組

**同組、同批、皆為「安裝失敗狀態經 installer status callback 回報」**，
其差僅在回報者之層級（Update Engine／SW Updater Service／Update Engine **Manager**）。入 DR-SU3。

### 三、`165` 我讀錯而由停止條件攔下

首版讀成「安裝成功之通知」，**其判定核心實為完成後依 `$PowerMode$` 決定續行或入睡**。
**`test_item` 上半非逐字之停止條件攔下** —— 改寫後判定核心為「畫面熄滅」。

### 四、`105` 比率解釋力之限縮（T78c 之結論）

> **105 比率解釋跨組可交付率之大部分變異（R² = 0.645，n = 19 組）。
> 加入「第四型%」與「DR-SU6%」二因子後 R² = 0.722，增幅 +0.077 < 0.1 ——
> **依否證條件，「難度為三軸」之說不成立。**
> `Integrity Verification`（第四型 75%）與 `Client Architecture`（DR-SU6 29%）
> **各有一個具名之成因，而那不是一個可推廣的維度。**

---

## batch 19（`HU FOTA via TBM` 前 16／餘 30）—— T79d（下放包 67）

| | 值 |
|---|---|
| 本批 105 比率 | **0%（0／16）** |
| **窗結算（17–19）** | **17%（8／48）** vs 目標 27% —— **依下放包 67 §四 #3 記為「因可選組窮盡而未達標」，不記為偏向** |
| 主指標：剩餘 105 比率 | **41.9%**（剩 31 列，其中 `Bearer Selection` 佔 105 列 13／13） |
| 可交付 | 4 列｜`PENDING` 48 行 |
| **037 覆蓋** | **280／311 = 90%** |

### 一、⚠ 主指標之上升是**分母縮小**所致，非偏向

剩餘 31 列 = `Bearer Selection` 16（**105 列 13**）＋ `HU FOTA via TBM` 14（105 列 0）＋ `057` 1。
**其比率 41.9% 已不是一個「偏向」之量** —— **它現在只是在說「剩下的就是 Bearer」。**

> **指標之射程到此為止**：當剩餘只剩一個被 DR 阻斷之組時，
> **剩餘 105 比率不再量偏向，它量的是那一個 DR 的大小。**

### 二、`225`／`226`／`227`／`230` —— `$FOTA_MASTER.*` 之四列同型

其值由 FOTA Master 送出，**台架設定其值之手段未確立** —— 四列之觸發皆掛 `PENDING`，
**而其畫面後果皆可觀測**。**其為第四型之一個新子形態：值來自他 ECU。**

**⚠ 措辭之處置**：`P` 檢查（R-1 v3(a)：賦值須帶 DBC `VAL_` 標籤）於首版命中 16 行 ——
**其成因為我在 `PENDING` 佔位中寫了 `$signal$ = value` 之賦值形**。
**已改為散文指稱**（「使 FOTA Master 回報無 FOTA 事件」），
**037 原文之賦值形則保留於 `test_item` 上半（該欄不受 `P` 拘束）。**

### 三、`233`／`234` —— **候選清單之首次起草驗證**

`DUP_CANDIDATES.md` 之第二高分對（0.882），**其錨同為 `4907779`** ——
**起草後確認二列同源**（皆為自 DD metadata 抽取 TBM 更新資訊，其差在所抽之欄位）。
**候選清單於本批首次被驗證為真陽性。**
