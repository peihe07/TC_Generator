# 下放包 05 —— Phase 0/1 結案確認、三項體例裁定、Phase 3 framework 前置量測

- 日期：2026-08-27
- 方向：分析層 → 執行層
- 前一包：`04_round_close.md`；對應上繳：`docs/upstream/04_framework_survey.md`
- 裁定狀態：R-SU8（RULINGS 現行版判準）、R-SU9（recon 產物重生條件）——
  分析層即裁；併記格第二 id、狀態板逾範圍 —— 採認執行層處置

---

## 一、上繳包 03 審查判定

**收，Phase 0/1 結案確認。** 三項體例問題逐項處分如下，皆為執行層
主動聲明之處置，無隱藏動作。

### 1.1 併記格第二 id（334／361／507）—— 採認，不另裁

執行層之依據正確：R-SU5 v2 (b) 已定本欄不進入任何 TC 欄位，
故三 id 之歸屬不影響任何產出。台帳全集數（unique 366，含該三 id）
為準。不另發裁決，`ANOMALIES.md` 之註明保留。

### 1.2 T16 刪檔重生 `DECISIONS.md` —— 本次採認，並立 R-SU9 定其條件

本次無內容遺失（檔為 recon 產物、簽核欄仍為未簽佔位），採認。
惟「刪檔以繞過保護機制」不可成為默認作法 —— 立 R-SU9 定判準。

### 1.3 T17 逾範圍填狀態板 —— 採認

Phase 0/1 全結而狀態板留空會與事實脫節，填實為正。未動 P2–P7 為正。
逾範圍主動聲明之作法本身即應維持。

### 1.4 自評所提之 RULINGS 版本判讀風險 —— 有效，立 R-SU8

執行層指出之風險成立：`RULINGS.md` 現同存 R-SU5 v1／v2 與
R-SU7 v1／v2，自動化讀取若無判準會讀到已撤銷之 565／478／135。
此為 R-TM13（不刪除、加註保留）之必然副作用，須以判準補其缺口。

---

## 二、裁決條文（抄入 RULINGS.md，逐字）

```
R-SU8（RULINGS.md 之現行版判準與索引）

`RULINGS.md` 依 R-TM13 保留被取代之條文（不刪除、不改寫）。
為免自動化或人工讀取誤用已撤銷之值，定判準二項：

(a) 同一條號有多個版本區塊時，**v 字尾最大者為現行**；
    無 v 字尾者視為 v1。被取代之版本僅供沿革查考，
    其所載之任何數值、形態陳述、拘束一律不得引用。

(b) `RULINGS.md` **檔首須維持現行版索引表**：條號、現行版、
    一句話主旨、來源下放包。每次 append 新版本時同步更新該表；
    索引表與條文區塊不一致時，**以條文區塊為準**，並即修索引。

本條之目的為讀取安全，不改變 R-TM13 之保留義務。
```

```
R-SU9（recon 產物之重生條件）

`scripts/recon.py` 於 `DECISIONS.md` 已存在時改寫 `DECISIONS.new.md`
（A-TM15 機制），其目的為保護**人手內容**。判準：

(a) 既有檔之簽核欄全為未簽佔位（`[PROPOSED]`／`[PEI]` 未填）
    且無任何人手編輯痕跡者，得刪檔令 recon 就地重生，
    **但須於上繳包揭露「刪檔重生」與其判定依據**。
(b) 任一項已簽、或含人手撰寫之內容者，**不得刪檔** ——
    保留 `.new` 並逐項人工合併，合併結果附 diff 上繳。
(c) 判定不確定時一律走 (b)。

本條同理適用於其他「已存在即改寫 .new」之腳本產物。
```

---

## 三、Phase 3 前置：framework 量測（T18）

IN §4.1 之三層框架由分析層起草，惟起草所需之兩個來源分群
須先實測。**分析層側之 037／SYS1 附件複本已不在場**（本 Project
附件已更換為他 feature 之素材），故本輪之量測全部由執行層執行，
分析層依其回報起草 framework。

**本包不產出 framework，不產出任何 TC。**

| # | 任務 |
|---|---|
| T18a | **037 分群實測**：以 `Categorization == Heading` 之 45 列為分節點，依文件序走訪，輸出每個 Heading 之：`SWE1-FOTA-{n}`、標題原文、其下所轄之 Functional + NFR 列數、所轄列之 id 範圍。末附「所轄列數總和 = 311」之閉合檢查（不閉合即停並回報缺口列） |
| T18b | **SYS1 分群實測**：`Basic Report` 之 28 個頂層章，各章之：`Outline Number`、`Description` 首句、其下二／三層節數與各節之 Outline Number 清單 |
| T18c | **CFTS_57 章節實測**：87 個章節物件之 ObjectID、章節號、標題原文、其下所轄需求物件（487 之分布）計數 |
| T18d | **三源對照草表**：以 T18a 之 45 個 Heading 為主鍵，逐項標註其在 T18b（SYS1 章）與 T18c（CFTS 章）之對應候選（依標題語意），**對應不明者標 `?` 不強配**。此為草料非結論 —— 分析層據以起草 Layer 2；執行層不得逕定 Test Set 名稱 |
| T18e | **Sub Categorization 交叉表**：45 個 Heading × `{Service, HMI, blank}` 之列數分布（HMI 87 列集中於哪些 Heading，關係到 Layer 2 之 UI 入口路徑一致性，IN §4.1.3） |

輸出置於 `features/sw_update/docs/upstream/04_framework_survey.md`；
量測腳本若新增，置於 `features/sw_update/scripts/`。

---

## 四、任務彙總

| # | 任務 |
|---|---|
| T-抄 | R-SU8、R-SU9 逐字 append 入 `RULINGS.md`；依 R-SU8(b) **新建檔首現行版索引表**（7 條現行 + 2 條留存，格式自定但須含四欄）。程式回讀逐字元核對法照舊 |
| T18a–e | §三 之五項量測 |

**不在本輪**：framework 定稿、錨定協定、任何 TC、寫回、git。

---

## 五、上繳包要求（`docs/upstream/04_framework_survey.md`）

1. T-抄 核對結果 + 索引表全文
2. T18a–e 逐項量測結果（原始輸出；T18a 須含閉合檢查）
3. 未結 DR 清單
4. 獨立自評
5. 量測條件揭露（R-G8）
