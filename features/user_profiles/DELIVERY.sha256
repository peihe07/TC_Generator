# DELIVERY — features/user_profiles/output/ 產出台帳（append-only）
# ─────────────────────────────────────────────────────────────────────────
# 形式沿用 Comfort（42 包 §一之未決 4 自裁）。記帳規則：
# **逐次追加，不覆蓋、不刪除、不改寫既有條目。** 每產出一份 xlsx 就在檔尾
# 新增一個 ENTRY 區塊。舊條目即使其檔案已從 output/ 消失，該區塊仍留著 ——
# 台帳記的是「產出過什麼」，不是「現在有什麼」。
#
# 為什麼存在：`output/` 是 gitignored（`.gitignore` 之 `output/`），
# 產出永遠不進版控。沒有台帳的話，「那份交付件到底是什麼」只能事後拼湊。
#
# `type` 欄（沿用 Comfort 29 §5.2）：
#   produced          —— output/ 之產出物
#   folder-attachment —— 置入客戶交付夾之附件
#
# 驗證（自 features/user_profiles/ 執行）：
#     shasum -a 256 -c --ignore-missing DELIVERY.sha256
#
# **旗標的代價**：`--ignore-missing` 使本台帳無法偵測「產出被刪除」。
# 它驗的是「還在磁碟上的產出有沒有被動過」。**台帳綠燈不等於產出俱在。**
# ─────────────────────────────────────────────────────────────────────────
#
# ── 42 輪：**尚無 ENTRY。**
#
# 寫回程式（`scripts/write_back.py`）已就位並通過 7/7 方向性案例，
# 但 **未決 1（T:Z 七個車型欄）無依據**，`--write` 受閘拒絕執行。
# 自我測試之探針檔全部落在 scratchpad，**不屬產出物，不入本台帳** ——
# 台帳之記錄單位是「一次 `--write` 的輸出」，探針不是。
#
# 第一個 ENTRY 於未決 1 得依據後產生。

# ── ENTRY 001 — 2026-08-19 — 第一次寫回：全語料 189 條
#
#   type : produced
#   來源 : inputs/…_SWQT_20260817_ext.xlsx（6372fb6be02f48dc3a3e091a60d2e2b3cf26d8704c27e25d79b7c9516fb825b2）
#   內容 : row 10–198，共 **189 列 = 189 條 TC**，無留空列
#   列序 : 依 Requirement or Design ID（leaf id）遞增 —— Comfort 96 §1 之同型
#   欄   : 寫 14 欄（D F G H I J K L M N P R S AH）；
#          O／AA／Q／AB 與 T:Z 七欄**留空**，其依據見 feature.yaml 之 why 欄
#   閘   : verify_dv_integrity 四項全綠（zip members 48=48、x14 節點 1=1、
#          sqref R10:R1411 不變、legacy 4=4）；WB-0／WB-5／WB-6 全綠；
#          寫回前 lint_tcs 189/0 與全部 audit 綠
#   交付 : **未交付**。本 ENTRY 只記「產出過什麼」；
#          送客戶目錄屬 Pei（Tier 3）
#   餘   : 12 條（163–165、174–182）之覆核未完，其修正於下次重寫回落地
#          —— 版本以 ENTRY 區分，不以「最終版」名之
#   size : 229859 bytes
#
6ca2438ae5fe03391e5e790685b9287658444785eb584c645fe17c522c34ae43  output/FM-WI-FSM-036-A01 STLA 測試用例規範與結果_SWQT STLA Test Case Specification & Result_SWQT_UserProfiles_20260819_full.xlsx

# ── ENTRY 002 — 2026-08-19 — 第二次寫回：remarks 全刪 ＋ TC-167 之 ER4
#
#   type : produced
#   來源 : inputs/…_SWQT_20260817_ext.xlsx（6372fb6be02f48dc3a3e091a60d2e2b3cf26d8704c27e25d79b7c9516fb825b2）
#   內容 : row 10–198，共 **189 列 = 189 條 TC**，無留空列
#   對 ENTRY 001 之差異（兩項，皆為 53 包所指示）：
#     一、**AH（remarks）欄 189 列全部清空** —— Pei 裁示（53 包 §一）。
#         其對外載體改為 output/DELIVERY_NOTE.md 之留白清單。
#     二、**TC-167（8.1）之 ER4** 由「Tutorials begin …」改為
#         「Video Bank 標題 “Tutorials”、副標 “Learn about new features” 顯示」
#         —— 「開始」不是可觀察之狀態。
#   未改 : A-UP14 之三個 popup id 依我方 spec 生成（53 包 §二.2），
#          差異登記於 ANOMALIES.md 與 DATA_REQUESTS.md §0.1
#   欄   : 寫 13 欄（D F G H I J K L M N P R S）；
#          **O／AA／AH 與 Q／AB 及 T:Z 七欄留空**，依據見 feature.yaml 之 why
#   閘   : 八項交付前自檢全綠（見 53 上繳 §3）；18 支閘全綠
#   交付 : **未交付**。送客戶目錄屬 Pei（Tier 3）
#   size : 202475 bytes
#
91e1de077568d87a25c7195c903894e20c880926b1bfb9d5cf129f2d409365e1  output/FM-WI-FSM-036-A01 STLA 測試用例規範與結果_SWQT STLA Test Case Specification & Result_SWQT_UserProfiles_20260819_noremarks.xlsx

# ── ENTRY 003 — 2026-08-19 — 第三次寫回：Test Item 之兩段結構
#
#   type : produced
#   來源 : inputs/…_SWQT_20260817_ext.xlsx（6372fb6be02f48dc…）
#   內容 : row 10–198，共 **189 列 = 189 條 TC**，無留空列
#   對 ENTRY 002 之差異（一項）：
#     **I 欄（Test Item）改為兩段** —— 首段 tc_title（形態不變）、
#     第二段括號內一句「本條在測什麼」（55 包 §一，Pei 確認之規格）。
#     第二段之來源為各條 reasoning 之「驗證目標」句改寫為英文，
#     逐條落於 data/test_item_part2.tsv（以 tc_id 為鍵，189 列）。
#   未改 : remarks 仍全空（ENTRY 002 之裁定）；A-UP14 仍依我方 spec 生成
#   閘   : 九項交付前自檢全綠（**新增第 i 項：Test Item 兩段**）；
#          TI-1／TI-2／TI-3 首跑對 ENTRY 002 之現況 **189 列全紅**，修正後 0
#   交付 : **未交付**。送客戶目錄屬 Pei（Tier 3）
#   size : 206116 bytes
#
1e4dbf2a828d9177c6ccacdfe62e92f62414f7afdc603cb9932706510c1eb677  output/FM-WI-FSM-036-A01 STLA 測試用例規範與結果_SWQT STLA Test Case Specification & Result_SWQT_UserProfiles_20260819_testitem.xlsx

# ── ENTRY 004 — **未產出。**
#
# 56 包 §三曾指定 ENTRY 004（AD-1 之改寫 ＋ TC-167 之引用欄 ＋ 18 支閘 ＋ 四份 pack）。
# 其落地前 57 包到達，Pei 指示「AD-1 與 AE-1 合併為同一次重出」（57 包 §四，G-J），
# **故 ENTRY 004 從未產出，其內容併入 ENTRY 005**。
# 本區塊只為說明台帳之號次跳空 —— 台帳 append-only，不回頭改號。

# ── ENTRY 005 — 2026-08-20 — 第四次寫回：AD-1（第二段之資訊量）＋ AE-1（孤兒值與 §4.5）
#
#   type : produced
#   來源 : inputs/…_SWQT_20260817_ext.xlsx（6372fb6be02f48dc…）
#   內容 : row 10–198，共 **189 列 = 189 條 TC**，無留空列
#   對 ENTRY 003 之差異（兩組，合併於同一次重出）：
#     一、**AD-1** —— `I` 欄第二段之資訊量。35 條列待判，人工判讀後
#         **31 條改寫**（帶進該條 ER 所斷言而首段未載之具體內容，
#         來源仍為 reasoning 之驗證目標句與其 ER，不另行構思）；
#         3 條（036／041／060）判為**殘餘**，其 ER 亦無首段未載之內容，
#         改寫將構成自行構思，逾越授權，故不動並具名；
#         1 條（176）為代理判準之偽陽（第二段已帶「12-character」，
#         而數字為判準之盲區），維持原樣。
#     二、**AE-1** —— `K` 欄（Input Test Data）之孤兒值與 §4.5 欄位歸屬。
#         IT-1 首跑 6 處紅：
#           `163`／`166` 屬**互動資料**（按下畫面之時點）→ 移入 `L` 欄步驟，
#                        `K` 欄改 `NA`；`about` 之模糊語隨移入去除（§1.2）
#           `004`／`100` 屬**獨立資料集**（PLP 表列項）未被引用 → 補入綁定引用
#           `175`／`176` 屬**邊界值**未被引用 → 補入步驟（`zero characters`／
#                        `twelve characters`／`thirteenth character`）
#         重跑後 IT-1 **0 處**。IT-2（§4.5 欄位歸屬）留 1 處待判（`049`），
#         人工判為**保留** —— 其值為資料本身（一組 4 位 PIN），非互動。
#   未改 : remarks 仍全空（ENTRY 002 之裁定）；A-UP14 仍依我方 spec 生成；
#          **TC-167 之 specification_reference 仍為具名缺口** ——
#          Tutorials L&F 之 PDF 未落 `inputs/`（56 包 §三.2：未落則不再列為待辦）
#   閘   : **十項**交付前自檢全綠（**新增第 j 項：IT**）；
#          IT-1／IT-2 立於 `audit_consistency`（56 → 65 方向性案例）；
#          `audit_second_segment` 13/13（G-K 之三條原文以字面固定，
#          改寫落地後證明仍在）；全閘自我測試綠；
#          `verify_dv_integrity` 違規 0（zip 48=48、x14 節點 1、sqref R10:R1411）
#   交付 : **未交付**。送客戶目錄屬 Pei（Tier 3）
#   size : 206499 bytes
#
570eb7cd8d1049de0e24f897c4acc376ed6dfd2b9089795991ae799aabbac5f5  output/FM-WI-FSM-036-A01 STLA 測試用例規範與結果_SWQT STLA Test Case Specification & Result_SWQT_UserProfiles_20260820_itemdata.xlsx

# ── ENTRY 006 — 2026-08-20 — 第五次寫回：Test Item 兩段之間空一行
#
#   type : produced
#   來源 : inputs/…_SWQT_20260817_ext.xlsx（6372fb6be02f48dc…）
#   內容 : row 10–198，共 **189 列 = 189 條 TC**，無留空列
#   對 ENTRY 005 之差異（一項，Pei 2026-08-20 指示）：
#     **I 欄（Test Item）之兩段之間改為空一行** ——
#     由 `<tc_title>\n(<句>)` 改為 `<tc_title>\n\n(<句>)`。
#     改在 `scripts/test_item_part2.py` 之 `compose()`，**單點**；
#     空行只影響呈現，**不影響任何閘** —— `TI-1`／`G3`／`audit_second_segment`
#     之切段皆先濾掉空行。已於 `audit_delivery_fields` 加一條方向性案例
#     釘住現行形態（17/17），使日後有人拿掉空行時不會以為兩種寫法等價。
#   空白之複查（同輪，Pei 指示「去掉多餘的空格，包括數字前面」）：
#     交付 13 欄 × 189 列全掃 —— **行尾空白 0、整格前後空白 0、
#     連續兩個以上空格 0、全形空格 0、不斷行空格 0、tab 0、
#     數字前異常空白 0**。**未動任何一格。**
#     兩處「看似多餘」者經查皆為刻意，逐項具名於 59 上繳 §2：
#       (1) `design_method` 之 `(Functional based ; no specific technique)`
#           —— 分號前之空格**出自母本「下拉選單」工作表之逐字值**，
#           改了即與 DV 不符；那是客戶的枚舉字串，不是我方的排版
#       (2) `M` 欄 23 行之三格縮排（`   a.` `   b.` …，見 TC-039／013／076）
#           —— §6.1 之子層列舉，且 `K-3` 之 `SUBLIST = ^\s+[a-z]\.\s`
#           **以該縮排為抑制條件**：拿掉縮排，三條會轉紅且層級消失
#   未改 : remarks 仍全空；A-UP14 仍依我方 spec 生成；
#          `TC-167` 之 specification_reference 仍為具名缺口
#   閘   : 十項交付前自檢全綠；`audit_delivery_fields` **17/17**（新增空行案例）；
#          全閘自我測試綠；`verify_dv_integrity` 違規 0
#   交付 : **未交付**。送客戶目錄屬 Pei（Tier 3）
#   size : 206495 bytes
#
cbd04af652727ae49c18969e87969d195388b6610329ea5c204634e2c01ed3fb  output/FM-WI-FSM-036-A01 STLA 測試用例規範與結果_SWQT STLA Test Case Specification & Result_SWQT_UserProfiles_20260820_itemgap.xlsx
