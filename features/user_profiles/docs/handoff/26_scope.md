# 26 下放包 — 範圍界定（Pei 2026-08-18 裁定）與 RD 拆分

## 裁決條文（逐字）

```text
R-U56 範圍界定 —— SWE 有寫就有，沒寫就不理
      037 之 180 leaf 母體即本 feature 之驗證範圍上界。
      **spec 有內容而 037 未為其產出 leaf 者，不生成 TC、不列覆蓋缺口、
      不向上游索取釐清。**
      我方不代 037 決定「什麼該是需求」——那屬 SWE.1／SWE.5 之職掌
      （§8.2「TC 作者不得重新分解、合併或發明 RD 項目」之延伸）。

      據此關閉：
        DR #3（spec 3.1–3.5、10.1、11.1、11.2 之上游覆蓋缺口）
              → **不送出**。
              惟 3.1–3.5 之**使用**不受影響：R-U22／R-U46 裁定其為
              PROF-001-01（SWE 有寫之 leaf）之 in-scope 依據，繼續併列 3.x。
              10.1／11.1／11.2 不生成 TC，狀態改為 **OUT-OF-SCOPE**（非缺口）。
        DR #7（9.1.1 另一側之大螢幕版面無 leaf）
              → **不送出**，狀態 OUT-OF-SCOPE。
              「037 在 8.8 對螢幕尺寸切兩個 leaf、在 9.1.1 只切一個」
              之佐證仍留檔，但**不因其不一致而代其補**。

      A-UP02 之分類由「上游覆蓋缺口」改為 **OUT-OF-SCOPE（已裁）**，
      **不關閉其記載** —— 事實仍在檔，只是不再是待辦。

      **本條為 feature-level。** 其形態顯為 feature-agnostic，
      惟範圍界定屬 Pei，**升為全域須 Pei 另行一句**；
      分析層不以本條之必然結果自推跨 feature 授權（R-U13）。
```

**本包產生之新條文清單（自檢）**：R-U56 ✓（以區塊形式出現）

## RD 之拆分（分析層自陳：25 包將四題捆為一群是錯的）

四題不是同一種東西：

| # | 問的是 | 依 R-U56 |
|---|---|---|
| #3 | SWE **沒切**的東西怎麼辦 | **關閉** |
| #7 | SWE **沒切**的東西怎麼辦 | **關閉** |
| **#5** | SWE **有切**之 leaf（085／088），其條文之 label 覆寫範圍怎麼讀 | **續送** |
| **#6** | SWE **有切**之 leaf（088 條件 2），其情境可否佈署 | **續送** |

#5／#6 之共同點是：**leaf 存在、TC 已生成、答案會改變已生成之內容**。
#3／#7 則是「該不該有 leaf」—— 那不是我方的問題。

## 作業

1. **R-U56 入庫** —— 逐字追加於 `RULINGS.md`；profile 之範圍節加註指向。

2. **`DATA_REQUESTS.md` 更新**
   - DR #3：狀態改 **CLOSED — OUT-OF-SCOPE (R-U56)**，保留其全部實測記載
     （3.1–3.5 可讀之證據、形態同 Comfort R-C16 之比對）作為歷史，
     **並明記「3.1–3.5 之使用不受本次關閉影響」**
   - DR #7：同上；其「8.8 切兩個、9.1.1 切一個」之佐證保留
   - #5／#6：改為**獨立送出**，不再標「併 DR #3 送出」

3. **`ANOMALIES.md` 更新** —— A-UP02 分類改 OUT-OF-SCOPE（已裁），不關閉記載。

4. **連帶掃描**：全批 78 條之 `reasoning`／`remarks` 中，
   凡以「上游覆蓋缺口」「037 未涵蓋」「無對應 leaf」等語描述者，
   逐條列出並依 R-U56 改述 —— **不得留下把 OUT-OF-SCOPE 寫成待辦之句子**。
   命中 0 亦須回報（R-G10 之餘數精神）。

5. **RD 查詢單（#5／#6）** —— 出英文可寄版本，各含：
   - spec 出處與逐字原文
   - 座標複位之證據（#5 之 `****` 位於 x=101.4／y=275.9–286.7、
     該列於 y=289.8、表中其餘列無 `****`）
   - **答案不同會導致什麼**（#5：`VARIANT_LABEL_OVERRIDES` 之適用範圍與
     9.1／9.2 之 ER；#6：TC-077 是否為不可佈署之條文條件）
   - 附記：A-UP11（037 之 title 於 12.8／12.8.1 七個 leaf 錯位）——
     **僅供上游參考，非索取**，且明記我方已依 Description 為準、不受影響
   落 `docs/upstream/26_rd_queries.md`，**由 Pei 寄出**（Tier 3）。

6. 25 包之作業 A（`075` 加引號、反向自檢、`remarks` ↔ `specification_reference`
   一致性閘、A-UP13 併入第三批）與作業 B（ch4 取樣清單）**照原指示**。

## 不在本包授權範圍

- 任何寫入性 git（R-G5／R-G12）
- 寫回工作簿（R-U14）
- 第三批之生成 —— 待取樣清單覆核
- 依 R-U56 自行為他 feature 認定範圍 —— 本條為 feature-level

## 上繳

`docs/upstream/26_scope.md` ＋ `docs/upstream/26_rd_queries.md`，
更新 `docs/INDEX.md`，附獨立判斷。
