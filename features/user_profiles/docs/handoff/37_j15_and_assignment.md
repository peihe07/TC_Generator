# 37 下放包 — J-15 作業 3 補做、指派檢查與第四批收尾

**本包無裁決條文。** 36 輪上繳**核可**。

## 一、`20_batch03.md` —— 已由分析層加註，不必再等裁示

執行層拒改下放包（分析層之物）是對的，故由本層加註。
標頭已寫入該檔首，**內容一字未改**：
`[SUPERSEDED / NOT EXECUTED]` ＋ 明列其「defect 0」不成立 ＋
五項作業之後續落點 ＋ 保留理由。

**採 `26_rd_queries.md` 之 WITHDRAWN 先例**：標頭是加註，不是回溯編輯；
「已結輪次不回溯編輯」管的是**內容**，不是**其效力狀態之標示**。

### 對執行層之查證，兩點回應

1. **「我無法證明它是誰寫的」** —— 這句寫得對。
   `docs/handoff/` 無作者欄、git 首次提交者是把未追蹤檔一併納入者，
   **兩項證據都不指向作者**。時序（早 2 分鐘）只支持「它是前一版」之推論，
   **而你把推論標成推論**。
2. **「五項中四項已由後續下放包以不同編號重新提出並完成」** ——
   這才是這次查證真正的產出。**未執行 ≠ 遺漏**；
   逐條查完才知道實際只漏一項。

## 二、作業

### 1. J-15 作業 3（**唯一真正之遺漏**）

複核 11 輪 PLP 位置指涉盲區掃描之**其餘 16 條命中**，
逐條判其是否為「**結論對、理由錯**」之同型。

**其非空談之證據**：34 輪之 `phrase_of` bug 正是「為對的結論給錯的理由」——
同一形態已在本 feature 出現過一次。

逐條回報：命中之 leaf、其當時之結論、其當時之理由、複核判定。
**判定為「理由錯」者，即使結論不變，亦須具名並改述。**

### 2. 指派檢查（36 輪 §5 第 3 項，成因未除）

Y-1 掃描抓得到**跨 leaf 群**之誤指，**抓不到同群內指錯**
（如 `030-01` 誤指 `030-02`）—— 本輪之兩處指錯即由此而生。

**補**：`remarks`／`reasoning` 中提及之 `NR1L-UserProfiles-NNN`，
其所對應之 leaf 須與該句所稱之關係相符
（配對 → 兩者為同一節之正／反；委派 → 被指者之 leaf 確為該行為之承擔者）。
`tc_id` → leaf 之對映由生成器之號碼指派表取得，**不靠人眼**。
含方向性案例；**首跑不論紅綠皆回報**。

### 3. 第四批覆核用全文之餘 5 條

`109`／`110`／`111`（`26a` 檔首）與 `122`／`123`（`26b` 檔首）——
**本層下一輪讀畢**。執行層不需另出檔，**惟若該五條因本輪之修正而內容變動，
請於上繳具名其變動處**，以免我讀到舊版。

### 4. 兩項待裁（承 34／35 輪，本輪一併裁）

- **`TC-003` 之 ER 越界**：三條 TC 斷言同一組事實 —— **維持現狀，不改**。
  §5.7 允許同一觸發之多個結果併於一條；三條各有其 leaf，
  其 ER 之重疊部分為**共用觀察點**，非越界。
  惟於 `TC-003` 之 remarks 具名「本條之核心斷言為 X，其餘為情境確認」，
  使覆核者不必推測何者為主。
- **PU0580 四條未指定其開關**：**指定為「已開啟」**，寫入 pre-condition。
  理由：未指定則 popup 出現與否不確定，測試結果不可重現（§2）；
  而「關閉時不出現」若為條文所述之另一分支，**應另立 TC 而非留白**。
  查 5.3.1 之 `(if turned on for that Profile)` 是否有對應 leaf，
  無則依 R-U56 為 OUT-OF-SCOPE，具名。

### 5. 重跑全閘，貼輸出

## 不在本包授權範圍

- 任何寫入性 git（R-G5／R-G12）
- **寄出 RD 查詢單** —— Tier 3，屬 Pei
- 寫回工作簿（R-U14）
- 第五批之取樣與生成 —— 待第四批覆核完成
- 刪除任何檔

## 上繳

`docs/upstream/37_j15_and_assignment.md`，更新 `docs/INDEX.md`，附獨立判斷。
