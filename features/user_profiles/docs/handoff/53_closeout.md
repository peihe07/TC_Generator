# 53 下放包 — 收尾：ENTRY 002 ＋ close-out（一輪完成）

**本包無新裁決條文。** 52 輪上繳**核可**。

**本輪為本 feature 之最後一個作業輪次。** 其後僅餘 Pei 之交付動作。

## 一、remarks —— **全刪**（Pei 已裁，不再等 51 包之量測）

Pei 裁示「全刪」。分析層曾陳其代價（24 條未涵蓋聲明、
`TC-176`／`TC-140`／`TC-155` 之設置理由），Pei 未改口，**該裁定成立**。

**51 包之三分類量測不再執行** —— 該量測之目的為供 Pei 於甲／乙／丙／丁擇一，
**選擇已作出，量測即無標的**。51 包作廢，於其檔首加
`[SUPERSEDED by 53 — Pei 已裁全刪，量測無標的]`，內容不改。

**作業**：
- 交付件之 `remarks` 欄 **189 列全部清空**
- **資訊不消失**：`reasoning` 不交付但仍在語料；review pack 四份仍在；
  **`48_delivery_note.md` 之 28 條留白清單為唯一對外載體，須完整保留並擴充**
  （見 §三）

## 二、本輪一併落地之修正（**一次重出，ENTRY 002**）

1. **`TC-167` 之 ER4** —— 補可觀察形式（Video Bank 標題 `Tutorials`、
   副標 `Learn about new features`），`specification_reference` 併列 Tutorials L&F。
   **不驗影片內容、播放控制、4 支之數目。**
2. **`A-UP14`**（PU1089／1090／1091 之角色記載，兩份上游不一致）——
   **依我方 spec 之記載生成，不改 TC**；Pop Up List 之差異登記為 anomaly
   並併入 RD 查詢單。**若任一條之判定會因此翻轉**，依 41 包 §四之授權
   逕行修正並具名（**僅限判定，不含措辭**）。
3. **餘 3 條之覆核**（`165`／`181`／`182`）—— 分析層於本輪執行期間讀畢；
   **若有 defect，併入本次重出**，不另開輪次。

## 三、交付說明之擴充（`48_delivery_note.md`）

remarks 全刪後，它是**唯一**對外說明「哪裡沒驗、為什麼」之載體。須含：

1. 現有 28 條留白清單（保留）
2. **新增**：Tutorials L&F `INTR2.)`（下載既有 profile 時 Tutorials 不顯示），
   037 未產出需求項，本次未涵蓋（52 包 §3.2）
3. **新增**：缺件清單 **3 項** —— `PU1087`／`PU1088` 之內文、
   RD #5（label 覆寫範圍）、RD #6（車輛組合可佈署性）；
   各附「卡住哪幾條 TC」與「我方之替代作法」
4. **保留**：覆蓋率之讀法 ——
   > 全覆蓋（180 / 180）說的是 037 之每個 leaf 都有一條 TC，
   > 不是每個 leaf 都驗完了。覆蓋率是分母的性質，不是分子的品質。
5. **新增**：`A-UP14` 之上游記載不一致

## 四、close-out（**本輪完成，不另開輪次**）

### 4.1 canon 重新同步（Operating Charter 明載之義務）

本 feature 產出 **R-G1～R-G12 十二條全域條文** 與 **G-A～G-K 十一項常規**，
現散於 feature 之 `RULINGS.md` 與 profile。

**作業**：依 09 包 A 類升格之作法，寫入
`docs/fw036/FEATURE_ONBOARDING.md` 之全域段，
feature 側改為引用。**逐條註明其來源條號與 feature。**
**不同步，下一個 feature 又從零重推 —— 那正是 09 輪只做了一半的事。**

### 4.2 素材狀態之判準（採 52 輪獨立判斷 1）

> **沒有路徑的「到齊」不算到齊。**

`DATA_REQUESTS.md` 之每一項附**其在檔案系統上之路徑與 SHA**；
「到齊」定義為 `shasum -c` 對得上。記入 profile。

### 4.3 `data/*.tsv` 與 `outline_map.json` 之指紋（採 52 輪獨立判斷 3）

現僅 `BASELINE.sha256` 保護其**位元組**，
**無物保護「它與 PDF 現況一致」** —— 而 `TC-017`／`074` 之正確性正來自
07 輪之補句表。**加與 PDF 之一致性檢查**，或**具名該缺口於 close-out 報告**。

### 4.4 「先查他 feature」之常規擴充（採 52 輪獨立判斷 4）

由「他 feature 之**交付件**」擴為「他 feature 之**全部素材**（含 `inputs/`）」。
理由：`TC-169` 自第六批起寫「逐步對映不在我方輸入內」，
**而那份輸入一直在 `features/comfort/inputs/` 裡，卡了五十輪。**

### 4.5 狀態板與索引

`PLAYBOOK.md` §6 狀態板、`docs/INDEX.md` 全輪次索引、
`ANOMALIES.md`／`DATA_REQUESTS.md`／`RULINGS.md` 之最終狀態。

## 五、產出

- **ENTRY 002**：`shasum -c` 全綠並附輸出；18 支閘全綠
- **`DELIVERY_NOTE.md`**（`output/` 內之副本同步更新）
- **close-out 報告**：`docs/upstream/53_closeout.md`

## 六、不在本包授權範圍

- **交付（送客戶目錄）** —— Tier 3，**屬 Pei**
- 任何寫入性 git（R-G5／R-G12）—— 備完整指令清單供 Pei 執行
- 寄出 RD 查詢單 —— 屬 Pei

## 七、Pei 於本輪後所餘之事項

1. **交付** —— `output/` 之 ENTRY 002
2. **git** —— 執行層備妥之指令清單
3. **`R-U17`** —— 刪 `inputs/` 之 spec 副本（一行）
4. **RD v2 ＋ #8 ＋ A-UP14** —— 可於交付後寄出（R-U57：不回頭改形式差異）
