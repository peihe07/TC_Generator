# 49 下放包 — remarks 之外部可讀性、批次落地與收尾

**本包無裁決條文。** 48 輪上繳**核可**。ENTRY 001 成立。

## 一、採納兩項

### G-I：`\b` 不適用於中文 —— 記入 profile 之判準節

`\b(pending|TBD|待判|未決)\b` 之中文詞永遠比不到，
而語料側掃描數十輪皆綠。**與 X-1（`Activate` 未入詞表）同類：
閘在跑，但它看不到那一類輸入。**

已 grep 17 支確認無其他同型（0 命中），該步驟正確。
**記入 profile**：凡判準含 `\b` 而其詞表有非 ASCII 者，
一律改以 `(?<![\u4e00-\u9fff])`／字串包含判定，並附中文命中之方向性案例。

### G-J：修正一律成批落地（採 48 輪之提醒）

分析層 48 包所稱「改一條之成本 0.04 秒」**只涵蓋機器之部分**。
本輪三處淨化連帶三份 pack 重出、`28_provenance4` 重標、待判登記表重判三次。

**餘 12 條之覆核發現一次落地**，不逐條處理。
本層將於下一輪讀畢 `163`–`165`、`174`–`182` 後一併下放。

## 二、remarks 之六類內部指涉 —— 先查 Comfort，再論處置

執行層之判斷成立：該六類**承載覆核依據**（刪去 §8.4.1，
讀者無從知道那是規範要求或個人選擇），刪除等於 189 條 remarks 之實質變更。

**惟在論處置之前，先做 G-H 之那一步**（44 包所立、T:Z 已驗有效之常規）：

**作業**：唯讀查 `features/comfort` 之**交付件** remarks 欄，
量下列各類之出現數與其形態：
- canon 節號（`§8.4.1` 等）
- 內部掃描／閘代號
- 裁決條號（`R-C…`）、異常編號（`A-CF…`）
- 輪次包號、DR／RD 編號

**判讀**：
- **Comfort 之交付件已含同類** → 先例成立，本 feature 沿用，不另出版本；
  於交付說明具名該先例
- **未含或形態不同** → 屬交付形式（Tier 3），
  備妥「另出對外版」之成本估計（影響條數、需改寫之句型、
  是否可機械轉寫）後送 Pei 裁

**兩份 feature 用同一份表單母本**（Comfort 亦 rev C），
G-H 之邊界成立，該先例可用。

## 三、產出閘之改判準 —— 核可並記為判例

42 輪之閘寫「`vehicle_columns is None` 即拒絕」，
而它要表達的是「**決定了沒有**」。44 輪定為留空後值仍為 `None`，閘不轉綠。

改為問 `feature.yaml` 三項是否**已決定且 `why` 非空** —— 正確。

> **「決定了留空」與「還沒想好」在 yaml 裡長得一樣，只有 `why` 分得開。**

此即 G-C（宣告與生效之分離）之延伸：`why` 不是註解，**它是狀態**。
記為判例，並確認 `--force` 仍未加入。

## 四、作業

1. G-I：`\b` 之判準改寫 ＋ 中文方向性案例；記入 profile
2. §二：唯讀查 Comfort 交付件之 remarks，依判讀分支處置
3. §三：記為判例
4. **本輪不重生成、不重寫回** —— 待餘 12 條之覆核發現一併落地（G-J）

## 五、不在本包授權範圍

- **交付（送客戶目錄）** —— Tier 3，屬 Pei
- remarks 之對外轉寫 —— 待 §二之查證結果
- 任何寫入性 git（R-G5／R-G12）
- 寫入他 feature 之檔（§二為唯讀）

## 六、上繳

`docs/upstream/49_remarks_scope.md`，更新 `docs/INDEX.md`，附獨立判斷。

## 七、現況

| 項 | 值 |
|---|---|
| 交付件 | `output/…_UserProfiles_20260819_full.xlsx`，189 條，ENTRY 001 |
| 已覆核 | 177 / 189（餘 12）|
| 閘 | 17 支 ＋ write_back 自我測試 12 |
| 擋交付者 | **無**（技術面）；remarks 之對外可讀性待查證 |
| 待 Pei | **交付與否**、R-U17、RD v2＋#8 |
