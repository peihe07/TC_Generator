# 11a 下放包 — 10 輪之兩個決定題（Pei 2026-08-17 裁定，逐字）

10 輪之前置 1–3 **尚未覆核** —— 其上繳包未落檔（見 R-U48）。
本包僅裁兩個決定題與取樣清單。

```text
R-U46 PLP 引用判準 —— 採聯集，並明列盲區
      甲（該 leaf 所引 spec section 之 pdf_text 含 PLP 字樣）
      ∪ 乙（該 leaf 自身之 037 Description／VC 含 PLP 字樣）
      = 併列 3.x 之自動判準。
      駁回單採甲：甲會排除 PROF-001-01，
      **而該條正是催生 R-U22 與 R-U39(2) 之起因；
      判準把自己的起因排除掉，即判準不對。**
      **盲區**：以位置指涉表述之引用（"above"／"listed above"／
      "see list of linked content"）不由自動判準決定。
      處置：掃出全部含位置指涉之 leaf，逐條人工判讀，
      結果逐條記於 DECISIONS.md，**不併入自動判準**。
      4.1（PRACC1.: "see list of linked content above"）為已知首例，
      其併列 3.x 成立。
      `build_batch_context` 之 `PLP_ENABLED` 於本條裁定後啟用。

R-G11 可測判準須同時聲明其盲區（全域慣例）
      立任何可測判準時，須一併聲明它抓不到什麼，
      並指定盲區之處置路徑（人工判讀／另立判準／接受漏失）。
      **未聲明盲區之判準，其「命中數」不得被當作「全集」。**
      理由：R-U39(2) 以可測判準取代具名個案是對的，
      而可測判準本身仍可能漏掉非字面之引用 ——
      兩者是相繼的兩層，不是後者取代前者。
      來源：10 輪之 PLP 掃描（4.1 以 "above" 指涉 PLP 表）。

R-U47 pilot 取樣清單 —— 核可
      8 組各 2、相異 16／16、餘數 0（R-G10 已驗）。
      四項必含之覆蓋 {Service B: 4, must_carry: 3, 4.1.1: 1, PLP: 2}
      全數 ≥ 1，成立。
      **must_carry 七條中 pilot 僅覆蓋三條（9.3.2／9.8／11.4）** ——
      pilot 不要求全覆蓋，惟**餘四條須登記追蹤**，
      並於第一批正式批次前各至少覆蓋一次。
      初版 PROF-045 換為 PROF-048（Test Set 歸屬不符）之更動，
      經記錄，核可。

R-U48 上繳包之落檔要求（重申）
      10 輪之上繳須寫入 `docs/upstream/10_pilot.md` 方為交付；
      **聊天貼文不算，亦不得作為覆核之依據。**
      **生成不得在落檔前開始。**
      本輪貼文於傳輸中多處截斷（含 `git add` 指令斷於
      `features/user_profiles/BA`），**該截斷版不得被執行或引用**；
      git 指令清單須自落檔後之檔案讀取完整版。
```

## 本包產生之新條文清單（自檢）

- R-U46 ✓　R-G11 ✓　R-U47 ✓　R-U48 ✓（皆以區塊形式出現）
