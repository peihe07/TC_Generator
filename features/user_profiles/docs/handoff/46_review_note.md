# 46 下放包 — 第六批覆核（第一段）：缺陷 0，並記覆核命中率之趨勢

**本包無裁決條文。** 45 輪上繳**核可**。

## 覆核進度

| pack | 條數 | 本層讀畢 | 缺陷 |
|---|---|---|---|
| `24a` | 11 | 是 | 0（另生 AA-1，屬 process）|
| `24b` | 11 | 是 | **1**（AB-1，`TC-154`）|
| `33a` | 17 | **8**（`166`–`173`）| **0** |
| `33b` | 16 | 否 | — |

**累計 155 / 189；餘 34 條。**

## 本段之三處記錄（非缺陷）

1. **`TC-173` 之對照** —— `avatar` **不得**重複而 `username` **得**重複，
   兩規則同在 ch8 而方向相反。故其 ER3 須明說「兩個都在」：
   只驗輸入被接受，一個**接受輸入卻在儲存時把第二個改名**之實作會通過。
2. **`TC-169` 之「上游文件依賴」分辨正確** ——
   8.3 之 `Specific popups can be found in the HMI Pop Up List`，
   其逐步對映不在我方輸入內。remarks 明寫「**不援引 R-U56**
   （本 leaf 存在，缺的是規則之權威文件）」——
   **Z-1 當初要分的正是這條界線，本次為自行分對。**
3. **`TC-172` 之前提使「下一個可用」與「第一個」不同** ——
   否則一個永遠 highlight 第一個（含已被使用者）之實作會通過。

## 覆核命中率之趨勢（本層自記，尚不作結論）

| 批 | 條數 | 人讀所生之內容缺陷 |
|---|---|---|
| 第一批（`017`–`044`）| 28 | 3（C-1／C-2／C-3）|
| 第三批（`079`–`108`）| 30 | 4（T-1／U-1／V-1／W-1）|
| 第四批（`109`–`134`）| 26 | 4（X-1／X-2／Y-1／Z-1）|
| 第五批（`135`–`156`）| 22 | **1**（AB-1）|
| 第六批（前 8 條）| 8 | **0** |

**趨勢已現，但不足以據以縮減。** 餘 34 條讀畢後，若第六批之總數仍為 0–1，
本層將提出**改為針對性覆核**之依據（只讀掃描標為待判者 ＋ 兩成抽樣）；
**在此之前維持 100%**（G-E）。

**不以趨勢預先放行** —— 第五批之 AB-1 正是在「前 7 條缺陷 0」之後才出現的。

## 作業

**本輪無新作業。** 45 輪之 AB-1 修正、G-F／G-G／G-H 落地照原指示執行；
本層續讀 `33a` 餘 9 條與 `33b` 16 條。

**惟一項提醒**：AB-1 之修正會動到 `TC-154`，
其所屬之 `24b` pack 指紋將因此失效 —— 依 G-F／G-G，
上繳時附 `--verify` 結果即可，**該條已由本層讀畢，不需重讀**。

## 不在本包授權範圍

- 交付、RD 寄出 —— 屬 Pei
- 任何寫入性 git（R-G5／R-G12）

## 上繳

併入 45 輪之上繳即可（`docs/upstream/45_fingerprints.md`），
或另立 `46_note.md`，具名所擇。
