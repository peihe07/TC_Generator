# 28 下放包 — RD v2 核可、四項裁示與第三批放行

**本包無裁決條文。** 27 輪上繳**核可**。

## 一、RD 查詢單 v2 —— **核可，可寄**

X-1／X-2／X-3 三處皆改，且兩處改得比我要求的更好：

- **X-1**：不只列滿七條，還把「一致」之三條分表列出並各加註 ——
  收件者不必回頭自己數。`125-01`／`125-02` 之「標題只涵蓋描述之一部分」
  亦具名 —— **對外文件不把「不完全精準」寫成「一致」**，這一步是對的。
- **X-2**：不只刪掉未證明之主張，還以七條實查**反證**它
  （`125-03`／`125-04` 取自 12.8.1、`126-01`／`126-02` 取自 12.8 ——
  兩組互相取用，不是單向 +1），並明寫「我方之證據無法顯示其成因」。
  **明說證據到不了哪裡，比留白更不容易被讀成疏漏。**
- **X-3**：附上 169 列 namespace 與 135 id 命中率之驗證 ——
  **讓對方不必接受我方之宣稱**。其對風險之估計（八個月落差＋CR 號，
  對方合理反應是先要我方更新再回答，兩題各多耗一個往返）比我寫的具體。

`26_rd_queries.md` 加 WITHDRAWN 標頭而不刪，版本塊明記「Version 1 was never
sent」—— 記載正確。

**本件可送 Pei 寄出。**

## 二、四項裁示

### R-1 —— 對外文件之最小閘（27 輪 §6 第 1 項，採納）

自陳「內部九支閘，對外文件一支都沒有，而它的精確度標準更高」成立。

**立最小閘**：文內敘述之筆數，須與其所附表列之列數一致
（`seven` vs 4 列即應轉紅）。**只做這一項**，不擴及語意 ——
語意之審查留給人工，但數字不符是可測的，不該再靠人眼。
含方向性案例。

### R-2 —— `ANOMALIES.md` 之「整體位移」措辭：**加註，不刪原文**

27 輪 §2.3 具名待示。**依 R-G4-1 之先例**：原文保留
（那是 19 輪當時之判讀，其上下文已載明依據），
另起一行註明 27 輪之修正與其反證。

**理由**：刪掉就看不出「曾經主張過一個未經證明的模式」——
而那正是本輪最值得記住的事。

### R-3 —— Q-1 引號界線（25 輪 §5 第 1 項）：**不擴張**

界線定為：**測試者會在畫面上逐字讀到的一句文字，或可點擊元件之 label
→ 加引號**；**我方以清單形式轉錄之表格列項 → 不加**。

據此 `TC-075`（spec prose 之顯示描述）須加、已加；
`TC-039`（15 列）與 `TC-013`（4 列）**不必改**。

**盲區具名**：Q-1 之 ≥7 詞閾值係看了結果才定（N=6 得 19／N=7 得 8／N=8 得 7），
**短於 7 詞之未加引號顯示文字，本掃描看不見**（R-G11）。

### R-4 —— A-UP13 三行為：**維持附掛，但批界定義隨之修訂**

三者落在 ch6／ch7，執行層已具名其為附掛而非主體，並指出
「若不具名，批界之定義就被悄悄改掉」—— 判斷正確。

**維持附掛**（25 包「不再延後」不變），惟批次記錄一律寫成
**「第三批 ＝ ch4 剩餘 26 ＋ A-UP13 附掛 3」**，
不寫「第三批 ＝ ch4」。**批界是被修訂，不是被稀釋。**

## 三、第三批取樣清單 —— **核可，放行生成**

30 條（ch4 26 ＋ `009` 負向配對 1 ＋ A-UP13 附掛 3），
`tc_id` `079`–`108`，leaf 覆蓋 72 → **100 / 180**。

切分依據逐 leaf 檢查、一葉一 TC 為預設，成立。
`002-02` 之 R-U27 寫作限制先具名（**本批唯一帶上游未決事項生成者**），
`005` 之順序斷言（須能區分「有存到」與「存在載入之前」）亦先具名 ——
**在生成前指出會犯的地方，比生成後再修有效**。

## 作業

1. R-1：對外文件最小閘（含方向性案例）
2. R-2：`ANOMALIES.md` 加註
3. R-3：Q-1 界線寫入 profile；`039`／`013` 不改
4. R-4：批次記錄之批界寫法
5. **第三批生成** —— 30 條，落 `generated/`，**不寫回工作簿**（R-U14）
   - `002-02`：popup 內文不寫（R-U27）
   - `005`：順序斷言之 ER 須可區分
   - 委派一律指名 leaf id（D-1）
   - PLP 併列之代價聲明隨引用欄同讀（J-1）
6. 生成後跑全部閘，並出**第三批之 ER 出處對照**（含 `pre_conditions` 之
   字面值與變體範圍層級）

## 不在本包授權範圍

- 任何寫入性 git（R-G5／R-G12）
- **寄出 RD 查詢單** —— Tier 3，屬 Pei
- 寫回工作簿（R-U14）
- 第四批之取樣 —— 待第三批覆核

## 上繳

`docs/upstream/28_batch03.md` ＋ `docs/upstream/28_provenance4.md`，
更新 `docs/INDEX.md`，附獨立判斷。第三批 TC 全文附上或指明路徑。
