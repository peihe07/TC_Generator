# 08b 下放包 — 作業指示（執行層）

裁決見 `08a_rulings.md`（R-U35～R-U38、R-G7-1）。07 輪核可，無退回項。

**本輪為 Phase 1 收尾。** 完成後 framework 定稿，方進 Phase 2。

## 作業

1. **條文入庫** — R-U35～R-U38、R-G7-1 逐字追加於 `RULINGS.md`；
   R-G7-1 置 R-G7 之後並標明修訂關係，**R-G7 原文不改**。

2. **R-U36 字內斷字全量掃描（先辦）**
   掃 21 頁文字層，量 `spa ces` 形態（詞內插入空白）之出現節數與字元數。
   量測條件須自陳：如何判定「詞內空白」而非正常斷詞
   （建議：以英文詞典或詞長分布為據，並列出判準之偽陽性風險）。
   據此**重報 C 組**，附分子定義（R-G8）。
   先辦之理由：其結果會改變 C 之終值，
   而補句表是否還有遺漏取決於這個數。

3. **R-U37 消歧反向驗證**
   人工構造至少一組近似段落注入，證明 difflib 消歧選中正確者。
   含 R-G7 之對照向。選錯即為缺陷，**改判準不改案例**。

4. **R-U35 落地**
   - `outline_map` 之 `pdf_text` 確立為生成基準；
     `text` 欄標註「追溯用，非判讀依據」
   - 補句表七條加 `must_carry` 欄與 `affected_field` 欄
   - 9.3.2 之 variant 進 feature profile；lint 規則落地並**實跑**
     （造一條含 "Stellantis Account" 之假 TC 證明它會 FAIL —— R-G7）
   - Table CPA2／EDPR1 之 PDF 出處頁次登記

5. **R-U38 登記** — 2.1 登為 R-U25 之已知例外，不試抽。

6. **framework 定稿**（作業 2–5 完成後）
   Layer 1／2／3 依 R-U20、R-U19；生成集合 133；
   須載明其判讀依據面為 PDF 側（R-U25／R-U35）。
   定稿後回報，**仍待分析層覆核方為定案**。

## 不在本包授權範圍

- 整份重建 `outline_map.json`（R-U31 駁回，僅增欄）
- 修改 `text` 欄之內容（僅加註標示）
- 寫入 comfort 或任何他 feature 之檔（R-U24／R-U30）
- 刪除 `inputs/` 之 spec 副本（R-U17，屬 Pei）
- 任何 git 操作（R-G5）
- 寫回實作（R-U14：x14 DV gate 未立前不得開工）
- **TC 生成** —— framework 未經覆核前不得起批

## 上繳

`docs/upstream/08_phase1_close.md`，更新 `docs/INDEX.md`，
附「本包是否仍有該驗而未驗者」之獨立判斷。
每個比率附分子定義（R-G8）；對照向未全綠者逐項追因（R-G7-1）；
動作清單與 git 陳述逐項對照（R-G6）。

**另附 Phase 1 收尾清單**：自 01 輪至本輪之全部未驗項、記載限制、
永久限制，彙整成一張表，標明各自之現況（已清／待清／永久）。
Phase 2 起不再回頭翻前七包。

## 承前之未決

- **A-UP09 / R-U14** —— x14 DV gate 未立，寫回不得開工
- **R-U17** —— `inputs/` spec 副本之刪除待 Pei
- **DR #3**（上游覆蓋缺口）、**DR #4**（PU1087／1088 之 popup 內文，MEDIUM）
- **N-XF01** —— comfort 孤兒檔，待 Comfort 下次開輪次
- **永久限制** —— home 之 `lint_tcs` 危害無法觀察（inputs/ 已清空）
