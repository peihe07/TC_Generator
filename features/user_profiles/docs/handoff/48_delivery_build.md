# 48 下放包 — 解除產出封鎖，出交付件

**本包無裁決條文。**

## 一、封鎖解除（分析層自陳之疏漏）

42 包起之「**不得產出交付件 —— 未決 1 未定前不得為之**」，
其前提於 **44 輪已消滅**（T:Z 依 Comfort 先例定為留空，
`applied: false` ＋ `why` 三項實測依據）。

**分析層未同步收回該封鎖，致其多掛四輪。** 現予解除。

**餘 12 條之覆核不構成產出之前提**：改一條 TC 之成本為重生成 ＋ 重寫回，
而寫回走 `patch_sheet_xml` 為 0.04 秒。
**為 12 條未讀而壓住整份產出，是把便宜的事排在貴的事之前。**

## 二、作業

### 1. 產出交付件（`output/`）

- 母本：`inputs/…_SWQT_20260817_ext.xlsx`（**不得覆寫**）
- 189 條，列序**依 Requirement ID**（非批次序）
- `vehicle_columns=None`（T:Z 七欄留空）
- 檔名與位置沿用 Comfort 之台帳形式（42 包未決 4 已自裁）
- **寫回後必跑**：`verify_dv_integrity.py`（DV-1／DV-2 兩向）、
  涵蓋檢查、全部 lint 與 audit
- 建 `DELIVERY.sha256` 之 ENTRY，`shasum -c` 全綠並附輸出
- **交付欄位淨化**：`[spec-derived]` 等來源類別標記、
  `pending`／`待判` 等內部狀態字樣**不得出現於任何交付欄位**；
  逐欄掃描並回報命中數（**命中 0 亦回報**）

### 2. 交付前自檢（逐項回報，不得以「已檢查」帶過）

| # | 項 |
|---|---|
| a | 189 列全在，`tc_id` 無重號、無跳號 |
| b | 列序依 Requirement ID |
| c | 必填欄無空值；`priority` 皆為 P0–P3；`design_method` 皆在下拉詞彙內 |
| d | 多行欄位之換行為 `\n`，無 `\r\n` 混用 |
| e | 無 emoji；無方括號（除 D-UP22-01 之 profile 例外，且經 G19 對照）|
| f | 無行尾句點（`pre_conditions`／`input_test_data`／`test_procedure`／`expected_result`）|
| g | x14 DV 節點數與 zip member 集合與母本相符 |
| h | 內部狀態字樣掃描結果 |

### 3. 交付說明（隨產出件附上）

須載明 42 包 §三之句：

> **全覆蓋（180 / 180）說的是 037 之每個 leaf 都有一條 TC，
> 不是每個 leaf 都驗完了。覆蓋率是分母的性質，不是分子的品質。**

並附**已具名之留白清單**（各批 remarks 中「本條不保證」之處），
使覆核者知道哪些側未被涵蓋。

### 4. 續行（不擋產出）

- 餘 12 條（`163`–`165`、`174`–`182`）之覆核由分析層進行
- 其所生之修正，於下一次重生成 ＋ 重寫回一併落地
- **產出件版本以 `DELIVERY.sha256` 之 ENTRY 區分**，不以「最終版」名之

## 三、不在本包授權範圍

- **交付（送客戶目錄）** —— Tier 3，**屬 Pei**。
  本包只產出至 `features/user_profiles/output/`
- 任何寫入性 git（R-G5／R-G12）
- 覆寫母本

## 四、上繳

`docs/upstream/48_delivery_build.md`，更新 `docs/INDEX.md`，附獨立判斷。
**§二之八項自檢逐項附實測輸出。**
