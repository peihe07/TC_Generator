# 11b 下放包 — 作業指示（執行層）

裁決見 `11a_rulings.md`（R-U46～R-U48、R-G11）。

**順序不可調換**：落檔 → 分析層覆核前置 1–3 → 才生成。

## 作業

1. **落檔 10 輪之上繳包（先辦，其餘全部等它）**
   將 10 輪之前置 1–3 與作業 5 之全部內容寫入
   `docs/upstream/10_pilot.md`，含：
   - 前置 1 之六條標記改動（逐條原文與加註後之首行）
   - 前置 2 之 `git check-ignore` 結果、`shasum -c` 7/7 輸出、
     **完整未截斷之 git 指令清單**
   - 前置 3 之六項自檢，含判準改兩次之經過（v1 掃全檔／v2 掃生產路徑／
     v3 改問不變量）
   - 作業 5 之 PLP 掃描三種讀法之完整命中清單
   - 取樣清單 16 列之完整理由欄
   聊天貼文於傳輸中截斷，**不得以貼文為據回填** —— 一律自實際產物重出。

2. **條文入庫** — R-U46～R-U48、R-G11 逐字追加。

3. **R-U46 落地**
   - 啟用 `PLP_ENABLED`
   - 併列 3.x 之 leaf = 甲 ∪ 乙 之聯集，清單具名回報
   - **盲區掃描**：掃全部 180 leaf 之 pdf_text，
     列出含位置指涉（`above`／`listed above`／`see list`／`see table`
     等）之 leaf，逐條人工判讀其是否指向 PLP 表或其他表，
     結果逐條記 `DECISIONS.md`
   - 4.1（PRACC1.）之併列 3.x 已裁定成立，直接落地

4. **R-U47 之追蹤登記** — must_carry 七條中 pilot 未覆蓋之四條，
   登記於 `DATA_REQUESTS.md` 或 `DECISIONS.md`（擇一，具名說明擇何處），
   標明「第一批正式批次前各至少覆蓋一次」。

5. **停** — 作業 1–4 完成後上繳，**不生成 TC**。
   分析層覆核前置 1–3 與本包產出後，方另下生成指令。

## 不在本包授權範圍

- **生成 TC**（R-U48：落檔與覆核前不得開始）
- 執行 `git add`／`commit`（屬 Pei；本輪僅備完整清單）
- 寫回工作簿（R-U14）
- 寫入他 feature 之檔（R-U24／R-U30／R-U44）

## 上繳

`docs/upstream/11_plp_and_pilot_prep.md`（作業 2–4），
`docs/upstream/10_pilot.md`（作業 1，補落檔），
更新 `docs/INDEX.md`。
附「本包是否仍有該驗而未驗者」之獨立判斷。
判準之盲區須逐項聲明（R-G11）；清單式分類以餘數驗證（R-G10）；
動作清單與 git 陳述逐項對照（R-G6）。
