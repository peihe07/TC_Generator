# 上繳 25 — Q-1、兩個閘、A-UP13 歸屬，與第三批取樣清單

- 產出層：執行層｜2026-08-18｜對象：分析層
- 下放包：`25_batch03_sample.md`（**無裁決條文**）
- **本輪未執行任何 git**；**未寫回工作簿**（R-U14）；**第三批未生成**（僅取樣清單）
- 語料：**78 條，未變動**

## 0. 全閘現況

| 項 | 結果 |
|---|---|
| `lint_tcs.py` 語料 | 78 條，**違規 0** |
| `lint_tcs.py --self-test` | **60 / 60**（＋4：G20）|
| `lint_variant_labels.py` 反向／語料 | 11 / 11 ／ 78 條違規 0 |
| `build_batch_context.py --selfcheck` | 8 / 8 |
| `render_spec_region.py --regression` | 7 / 7 |
| `audit_consistency.py` | K-3 0／K-4a 0／K-4b 0／**Q-1 7 處待判** |
| `audit_consistency.py --self-test` | **23 / 23**（＋3：Q-1）|
| `audit_variant_pairs.py` ／ `--self-test` | 違規 0 ／ 7 / 7 |
| `audit_delegation.py` ／ `--self-test` | 紅 0 ／ 黃 8 ／ 8 / 8 |
| `scan_override_notes.py --check` | 與 TSV 一致 |

---

# A. Q-1 與兩個自陳缺口

## 1. Q-1 —— `TC-075` 加引號，並反向自檢全批

### 1.1 修正

`TC-075` 之 ER2 已改為
`The Connected Account row reads “Save your preferences to the cloud and access
them from vehicle to vehicle.”, with no Uconnect.com subscription clause…`

### 1.2 反向自檢：**G18 查不到的那一半**

G18 查「引號**內**之字面值溯不溯得到源」；**它查不到「該加而未加」**。
本輪補反向掃描（`audit_consistency.py` 之 **Q-1**）：
ER 中**引號外**之連續 ≥7 詞若逐字見於被引之節，即列待判。

### 1.3 掃描結果 **7 處，逐條判皆非缺陷**

| tc_id | 命中 | 判 |
|---|---|---|
| `013` | `two columns labeled connected account and local` | 行為敘述（該畫面有兩欄），非顯示值 |
| `018` | `to the left of the edit profile` | 版面位置之敘述 |
| `021` | `and viewing info of what is linked` | spec 自己之散文列舉（受限動作），非 UI 標籤 |
| `037` | `a page of general info of what` | 行為敘述（顯示一個頁面）|
| `054` | `a lock symbol combined with the profile` | 圖示之敘述，非文字值 |
| `062` | `glove box lock button is greyed out` | 行為敘述 |
| `063` | `function is not available while in valet` | **PU0833 之內容 spec 未給**，ER 為條文散文之轉述而非引用該 popup 之字 |

**共同形態**：七處都是**行為敘述**與 spec 措辭相同，
而非「螢幕上出現的那串字」。§11 要加引號者是後者。

### 1.4 **一個真正的邊界案，及本輪為它立的界線**

掃描原本會命中第八處 —— `TC-039` 之第 m 列
（`Save your preferences to the cloud…`），**與 Q-1 所指者是同一串字**。

那一串在 `075` 裡要加引號，在 `039` 裡卻沒加。**兩者差在哪？**

| 形態 | 例 | 加引號？ |
|---|---|---|
| **散文中內嵌**之顯示文字 | `075`「The row reads “…”」、`055`、`072` | **是** |
| **逐列轉錄**之表格內容 | `039` 之 `a.`–`o.`、`013` 之 `a.`–`d.` | 否 —— **列表形式本身即標示其為轉錄** |

**這個界線不是我本輪發明的 —— 語料早已一致遵循它**：
`013` 之畫面標題與兩欄說明（散文）加了引號，而其 Table CPA2 四列（轉錄）沒加。
本輪只是把它**寫下來並落為掃描判準**（子層列舉行不計入）。

**盲區（R-G11）**：**此界線是我讀語料歸納的，非 canon 明文。**
若分析層認為轉錄列亦須加引號，`TC-039`（15 列）與 `TC-013`（4 列）**皆須改** ——
請於下包明示。**我不自行擴張。**

### 1.5 方向性案例（＋3，共 23 / 23）

紅向取 **`075` 之原形**（散文內嵌而未加引號）；
綠向取**同句加了引號**與**轉錄子層行**兩種 —— 後者守住 §1.4 之界線。

---

## 2. A-2 —— `remarks` ↔ `specification_reference` 一致性閘（**G20**）

### 2.1 為何非立不可

**同一形狀已發生兩次**：

| | 情形 |
|---|---|
| **C-2**（20 包）| 18 輪之文字說「無界前基準線」，而 `design_method` 欄掛 BVA |
| **P-3**（24 包）| `TC-072` 之 remarks 說「併列 12.3.1 與 12.6」，而引用欄只有 12.3.1 |

**兩次都是「改了一處而未掃同一條的其他記載」，兩次都靠人工覆核才發現。**

### 2.2 判準（**只取「宣稱有引用」之語境**）

> `remarks` 中在**宣稱引用**之語境（`併列`／`已列`／`故列`／`列該`…）下
> 出現之節號，須是 `specification_reference` 之子集；多出者即紅。

**刻意不取所有節號** —— remarks 常需**說明某節為何不列**
（`TC-072` 修正後正是如此）。若把所有節號都算宣稱，
**這條閘會逼人刪掉正確的說明** —— 那比不設閘更糟。
故另設「不列／未列／屬多引／移出」之否定語境，其鄰近節號不計。

### 2.3 方向性案例（＋4，共 60 / 60）

| 案例 | 期望 |
|---|---|
| **P-3 之原形**：稱「併列 12.3.1 與 12.6」而引用欄只有 12.3.1 | **紅** |
| **其修正後之形**：說明 12.6 **為何不列** | 綠 |
| 宣稱併列且引用欄確實含之 | 綠 |
| 只是提及節號，無引用之宣稱 | 綠 |

**第二條是護欄**：它守住這條閘不會反過來懲罰正確的說明。

---

## 3. A-3 —— A-UP13 之歸屬：**我 23 輪的記載不準確，本輪更正**

25 包要求三個行為併入第三批取樣。查 037 之 180 leaf 母體以定其歸屬，
結果**推翻我自己 23 輪的結論**：

| # | 行為 | 23 輪記為 | **實況** |
|---|---|---|---|
| 1 | 6.2.1：客製化或刪除後預設 profile 之消失 | 無人覆蓋 | **確實無人覆蓋** |
| 2 | 7.2.1：`More Options` → Edit Profile tab | 無人覆蓋 | **有專屬 leaf** `SWE1-HMI-PROF-059-02`，**尚未取樣** |
| 3 | 7.2.1：選別的 profile → 顯示新 welcome popup | 無人覆蓋 | **有專屬 leaf** `SWE1-HMI-PROF-059-03`，**尚未取樣** |

**成因**：23 輪我查了 7.2 與 7.2.1 之**條文**，
**卻沒查 7.2.1 有幾個 leaf** —— 該節有三個（`059-01`／`-02`／`-03`），
而我只看到自己那條所屬的 `059-01`。

**故第 2、3 項是 (b) 類之待兌現承諾，不是覆蓋缺口** ——
23 輪之「三個行為無任何 TC 覆蓋」**過度悲觀，本輪更正**。
`TC-007` 之 reasoning 已改為指名 `059-02`／`059-03`。

**第 1 項才是真正的缺口**：6.2.1 僅一個 leaf（`SWE1-HMI-PROF-048`），
其 description 之後半（`until a user customizes or deletes it`）無 TC。

**一併記一個反面，以防下一次假委派**：
第 1 項**不可**委派予 `SWE1-HMI-PROF-007-02`（4.5，`Recreate Default Driver1
if Customized and Deleted`）—— 該 leaf 驗的是「客製化後刪除**全部** profile
→ Driver 1 **重建**」，與「客製化後該預設 profile **不再是預設**」是兩件事。
**兩者措辭極近，正是下一次假委派最可能的落點。**

---

# B. 第三批取樣清單（**先回報，不生成**）

## 1. 範圍與批界

| 項 | 數 |
|---|---|
| 180 leaf 中已覆蓋 | 72 |
| 餘 | 108（ch4 **26**／ch5 38／ch6 9／ch7 12／ch8 23）|
| **第三批主體** | **ch4 剩餘 26 leaf**（實測與 25 包所估相符）|
| 附掛 | **3 項**（A-UP13）|

**ch4 之 26 leaf 逐條列名**（依節次）：

| 節 | leaf |
|---|---|
| 4.1 | `001-02`、`001-03` |
| 4.1.1 | `002-01`、`002-02` |
| 4.2 | `003` |
| 4.3 | `004-01`、`004-02`、`004-03`、`004-04` |
| 4.3.1 | `005` |
| 4.4 | `006-01`、`006-02`、`006-03` |
| 4.5 | `007-01`、`007-02`、`007-03` |
| 4.5.1 | `008` |
| 4.5.2 | `009` |
| 4.5.3 | `010-01`、`010-02` |
| 4.5.3.1 | `011` |
| 4.5.4 | `012` |
| 4.6 | `013` |
| 4.6.1 | `014` |
| 4.6.2 | `015` |
| 4.6.3 | `016` |

## 2. 三項必含（具名）

### 2.1 待兌現之 (b) 類委派 —— **本批兌現 2 / 5**

| 委派方 | 被指名之 leaf | 節 | 本批兌現？ |
|---|---|---|---|
| `TC-001` | `001-02`（啟用時回復）| 4.1 | **是** |
| `TC-001` | `001-03`（不可用項目跳過）| 4.1 | **是** |
| `TC-005` | `047` | 6.2 | 否 —— ch6，待後批 |
| `TC-009` | `073-02`／`073-03` | 8.7 | 否 —— ch8，待後批 |
| `TC-007` | `059-02`／`059-03` | 7.2.1 | **是（附掛，見 §3）** |

**未兌現者須留在承諾狀態，不得因本批未涵蓋而被讀成已處理。**

### 2.2 §7 列舉配對 —— **須額外造者 1 條**

ch4 之多數配對**037 已自行切為兩個 leaf**，天然成對，不需額外造：

| 節 | 配對 | 形態 |
|---|---|---|
| 4.4 | `006-01`（載入上次）↔ `006-02`／`006-03`（被 key fob／記憶座椅覆寫）| 037 已切 |
| 4.5.3 | `010-01`（有其他 profile 可用）↔ `010-02`（無）| 037 已切 |
| 4.6 | `013`（按鈕預設在狀態列）↔ `016`（被移除後之 highlight）| 037 已切 |

**須額外造者**：

> **`009`（4.5.2）之負向配對。** 其條文為
> `The memory seat preferences can be swapped between Driver Profiles,
> **but there will always be one Driver Profile per memory seat position**`。
> 「**永遠只有一個**」是全稱 —— **只驗互換成功，一個允許兩個 profile 連到
> 同一座椅位置的實作會通過**。須配一條「嘗試使同一座椅位置連上第二個
> profile → 不成立」之負向。

### 2.3 變體對造之新 axis —— **本批為 0，且這是查過的**

V-1 之母體 6 個 axis 落於 **6.1／8.1／9.1／10.3.1／11.4**，
**ch4 一個都沒有**（`data/override_notes_m3.tsv` 逐條複驗）。

**故第三批無新 axis。** 具名於此，是為使「0」不被讀成「沒查」。

**兩個 `pending` axis 仍未兌現**：`r1h-cpa-6.1`（leaf `046`）與
`r1h-cpa-8.1`（leaf `065`）—— 皆不在 ch4，**待第四批**。
`audit_variant_pairs` 之絆線仍在（該 leaf 一旦生成 TC 而未配對造即轉紅）。

## 3. A-UP13 三行為之歸屬（**附掛項，皆不在 ch4**）

| 行為 | 歸入 leaf | 節 | 形態 |
|---|---|---|---|
| 1 | `SWE1-HMI-PROF-048` | 6.2.1 | **已覆蓋 leaf 之第二條 TC**（同 `gen_pairs` 之形態，非新 leaf）|
| 2 | `SWE1-HMI-PROF-059-02` | 7.2.1 | 新 leaf |
| 3 | `SWE1-HMI-PROF-059-03` | 7.2.1 | 新 leaf |

**具名其為附掛而非主體**：第三批之批界是「ch4 剩餘」，
而三者落在 ch6／ch7。**若不具名，批界之定義就被悄悄改掉了** ——
往後「第三批 = ch4」這句話會對不上實際內容。

依 25 包「不再延後」之指示納入；
**若分析層認為應維持批界純淨、三者改列第四批，本清單不因此失效**（扣掉三列即可）。

## 4. 估計條數與其切分依據

| 來源 | 條數 |
|---|---|
| ch4 之 26 leaf，一葉一 TC（§8.2.1）| 26 |
| `009` 之負向配對（§2.2）| 1 |
| A-UP13 附掛（§3）| 3 |
| **合計（估）** | **30** |

**tc_id 範圍**：`079`–`108`（承 078）。
**批後 leaf 覆蓋**：72 → **100 / 180**。

### 4.1 切分依據 —— **一葉一 TC 為預設，逐項檢查有無須拆**

| leaf | 是否須拆 | 依據 |
|---|---|---|
| `004-01`～`004-04` | **否** | 4.3 之三個啟用途徑（HU／記憶座椅／key fob）037 已切為三個 leaf，一葉一 TC 即足 |
| `007-03` | **否** | 其 `(unless there are 2 or more memory seat buttons)` 以 pre-condition 固定為「少於 2 個記憶座椅按鈕」；≥2 之情形由 `008`（4.5.1）承擔 |
| `008` | **否** | 條文之例以 2 個按鈕為說明，pre-condition 固定為 2 |
| `013` | **否** | 「按鈕預設在狀態列」與「圖示隨 profile 變」為同一觸發之兩個觀察點，併為一條之兩個 ER；移除後之行為屬 `016` |
| `002-02` | **否，但有寫作限制** | 見 §4.2 |

### 4.2 **`002-02` 之寫作限制（R-U27）—— 須先講清楚，否則生成時會犯**

`002-02`（4.1.1）之 description 為
`When user pressing Yes in pop-up PU_0118, PU1087 is displayed. PU1088 shows
when restore completes.`

**DR #4 仍未到齊** —— 缺的是 `PU1087`／`PU1088` 之 **popup 內文**。
依 **R-U27**：該 TC **得以生成**（觸發條件、顯示與否、流程分支皆可驗），
**惟 popup 內文之逐字 ER 不寫**，不得以鄰近 PU id 推定內容（§8.4.1）。

故其 ER 只能寫「`PU1087` 顯示」而**不能寫該 popup 上寫什麼**。
**這是本批唯一帶著上游未決事項生成的 leaf**，故先具名。

### 4.3 其他須於生成時遵守者

- **4.1／4.3 之 PLP 併列**（D-UP17-01）：`specification_reference` 得併列
  `3.1`–`3.5`，**但覆蓋率不得以引用欄推定**（J-1）——
  其代價聲明須隨引用欄一起被讀到
- **委派一律指名 leaf id**（profile §3.6 / D-1）—— 本批預期會產生數處委派
  （如 `007-03` → `008`），**寫成節號會被 D-1 判紅**
- **`005`（4.3.1）之「儲存後才載入」為順序斷言** —— ER 須能區分
  「有存到」與「存在載入之前」，否則一個先載入後儲存之實作會通過

---

## 5. 本包是否仍有該驗而未驗者（獨立判斷）

| # | 項 | 分類 | 說明 |
|---|---|---|---|
| 1 | **引號界線為歸納所得，非 canon 明文** | **本輪之判準風險** | §1.4：若判為轉錄列亦須加引號，`039`（15 列）與 `013`（4 列）皆須改。**待明示，不自行擴張** |
| 2 | **G20 只查 `remarks`，不查 `reasoning`** | 判準盲區 | `reasoning` 內亦常提節號（如本輪新增之數處）。**擴及 reasoning 會大量誤報**（其本就在論述他節），故未擴 —— 但這表示同形缺陷若落在 reasoning，本閘仍看不到 |
| 3 | **Q-1 之 ≥7 詞閾值是調出來的** | 判準盲區 | N=6 得 19 處、N=7 得 8 處、N=8 得 7 處。**7 是我看了結果才定的** —— 短於 7 詞之未加引號顯示文字，本掃描看不見 |
| 4 | **A-UP13 第 1 項仍無 TC** | 待第三批 | 本輪只定歸屬，**未生成** |
| 5 | **第三批之取樣清單未經第二人覆核** | **分析層待辦** | 依 25 包 C，生成待清單覆核 |
| 6 | **`pending` 兩 axis 未兌現** | 待第四批 | 絆線仍在 |
| 7 | **D-3 之 8 處黃仍靠人工** | 承前 | |
| 8 | A-UP09／R-U14、DR #3–#7、R-U17、N-XF01、A-UP10、A-UP11 是否回報上游 | 承前 | 擋 Phase 6 寫回 |

---

## 6. 動作清單（R-G6）

| # | 動作 | 對象 | git |
|---|---|---|---|
| 1 | 檔案編輯 | `scripts/gen_pairs.py`（Q-1 之引號）| 否 |
| 2 | 檔案編輯 | `scripts/audit_consistency.py`（**Q-1 反向掃描** ＋3 案）| 否 |
| 3 | 檔案編輯 | `scripts/lint_tcs.py`（**G20** ＋4 案）| 否 |
| 4 | 檔案編輯 | `scripts/gen_pilot.py`（`TC-005`／`TC-007` 之委派歸屬更正）| 否 |
| 5 | 檔案追加 | `ANOMALIES.md`（A-UP13 歸屬確認）| 否 |
| 6 | 檔案重生成 ×21 | `generated/`（pilot ＋ pairs；**內容變動者 3 條**：`005`／`007`／`075`）| 否 |
| 7 | 檔案新建 | `docs/upstream/25_batch03_sample.md`（本檔）| 否 |
| 8 | 檔案編輯 | `docs/INDEX.md` | 否 |
| 9 | 程式執行 | ch4 母體查詢、Q-1 閾值校準、生成 ×2、全部閘、五支 audit 之 `--self-test` | 否 |

**本輪未執行任何 git**：寫入性與唯讀皆無。

**未動**：工作簿（**未寫回**）、`inputs/`、`forms/`、`feature.yaml`、`framework.md`、
`RULINGS.md`、`DECISIONS.md`、`DATA_REQUESTS.md`、`BASELINE.sha256`、`data/`、
`scripts/gen_batch01.py`、`gen_batch02.py`、`lint_variant_labels.py`、
`render_spec_region.py`、`build_batch_context.py`、`audit_variant_pairs.py`、
`audit_delegation.py`、`scan_override_notes.py`、**他 feature 之任何檔**、
`docs/fw036/`、`docs/runtime/profiles/`。

**第三批未生成** —— 依 25 包 C，待本清單覆核。
