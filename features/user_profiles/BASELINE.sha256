# BASELINE — User Profiles 素材基準（inputs/ ＋ spec-index/ 之 Personal Account 三件）
#
# 為什麼存在：inputs/ 是 gitignored（本 feature .gitignore:2），素材本身永遠
# 不進版控。2026-08-13 實測，amfm / home / media / projection 四個 feature 的
# inputs/ 已全部清空，而 git 無從還原（那些檔從未被追蹤）。本檔是那件事之後
# 留下的防線：素材可以消失，雜湊留在版控裡。
#
# 涵蓋範圍依 01b 作業項 2 明示之 **R-C20（Comfort）比照**：
# 「涵蓋以來源為準，『還沒用到』不是不保護的理由。」故本檔涵蓋 inputs/ 之
# 全部檔案，加上 spec-index 之 Personal Account 三件（xlsx export / JSON /
# PDF）—— 後者因 `.gitignore` 之 `spec-index/cache/*` 同樣不入版控，
# 而 R-U3 已裁定該 export 為本 feature 之 spec 基線，是唯一之 spec 來源。
#
# 量測條件：SHA256（`shasum -a 256`），對整個檔案位元組流，非工作表內容雜湊。
# 路徑相對於 `features/user_profiles/`。
#
# 驗證（自 features/user_profiles/ 執行）：
#     shasum -a 256 -c BASELINE.sha256
# macOS 的 shasum 會對本註解區塊印 "WARNING: N lines are improperly
# formatted"，那是 perl 版的行為，exit code 仍為 0，逐檔仍印 OK。
#
# 更新時機：素材有增減或經裁決換版時，連同裁決編號一併更新本檔。
#
# **2026-08-17 建檔（Phase 0，下放包 01b 作業項 2）**：
# inputs/ 現為 **1 檔** —— 依 R-G1／R-U7 置入之 036 母本複本。
# 母本本身留在 `forms/`，未被覆寫（本輪全程未執行 openpyxl save）。
# **037 Analysis Report 尚未到齊**（A-UP04），到齊後須連同本檔一併更新。
#
# **2026-08-17 更新（03 輪，下放包 03 作業項 1／4）—— inputs/ 由 1 檔增為 3 檔**：
#   ＋ 037 Analysis Report（DR #1 到齊，A-UP04 之解除依據）
#   ＋ SYS1 Personal Account spec 之 inputs/ 副本
#
# **037 之採認前置**：Phase 0 之全部 037 側數字係分析層於 **Project 附件副本**
# 上量得，**未比雜湊**（下放包 03 §素材現況自陳）。本輪一律以 `inputs/` 這份
# 重新實測，不沿用 —— 其 SHA256 為 `9d176dde…`，**於本檔首次落錨**。
# 大小相同不等於內容相同；在有雜湊之前，那兩份是兩個未經比對的物件。
#
# **037 之檔名與 DR #1 所列者不同**：DR 列
# `FM-WI-FSM-037-A03 N1L SWE1 Personal Account HMI V0.1 STLA 報告.xlsx`，
# 實得者以連字號分隔且 `PersonalAccount` 無空白。**檔名是 DR 之 pattern，
# 不是該檔之身分** —— 身分由下方之 SHA256 承擔。
#
# **兩份 spec 之同一性（03 作業項 5，只驗不處置）**：
# `inputs/` 與 `spec-index/cache/` 之 R1L-R export **SHA256 完全相同**
# （`368d5874…`）。**兩者皆列於本檔**，各自以其路徑受檢 ——
# 相同不是合併的理由，處置屬 Tier 2／3（R-U17 提案），本輪不刪不搬不改引用。
# R-U3 之 spec 基線引用路徑維持 `spec-index/`。

6372fb6be02f48dc3a3e091a60d2e2b3cf26d8704c27e25d79b7c9516fb825b2  inputs/FM-WI-FSM-036-A01 STLA 測試用例規範與結果_SWQT STLA Test Case Specification & Result_SWQT_20260817_ext.xlsx
368d5874aa23e49c007251ece84de8901f873a7f51ab09b788122b164d365b05  ../../spec-index/cache/SYS1_HMI_Personal_Account_HMI_Logic_and_Flow_R1L-R_(February_10_2023).xlsx
920b2cbca378a1c851bb0521711f1b076fdfdb68939337d3faf30c81a869c48e  ../../spec-index/cache/SYS1_HMI_Personal_Account_HMI_Logic_and_Flow_R1L-R_(February_10_2023).json
25553b9b5c63834eb916c82db2f6951982c5e8123410c9a221b46e5da2eb37a7  ../../spec-index/sources/Personal Account HMI Logic and Flow R1L-R (February 10 2023).pdf

# ---- 03 輪新增（2026-08-17）----
9d176ddef6d013539bd33e8a74e8b67d01fba232486aaac9eedad109a783eedb  inputs/FM-WI-FSM-037-A03-N1L-SWE1-PersonalAccount-HMI-V0.1 STLA 報告.xlsx
368d5874aa23e49c007251ece84de8901f873a7f51ab09b788122b164d365b05  inputs/SYS1_HMI_Personal_Account_HMI_Logic_and_Flow_R1L-R_(February_10_2023).xlsx

# ---- 10 輪新增（2026-08-17，R-U45）----
# **data/outline_map.json 自本輪起納入版控並受本檔保護。**
# 其何以不再是「可重建之衍生物」：該檔含 `pdf_text`（生成階段之判讀基準，
# R-U25／R-U35(a)）、`divergence`（07 輪逐節稽核之結果）與 `__meta__` ——
# **三者皆非 build_outline_map.py 所能產出**，重建成本為 07／08 兩輪之工作量。
# `.gitignore` 之例外依 R-U13「先裁後改」由 R-U45 授權後方加。
a70f21817c2d93c49429a4bcc23f2e39b769f901669c9581efacc46e0a8cfa98  data/outline_map.json

# ── 52 輪：Pop Up List（DR #4 之權威文件；R-U9 之候選，本輪確認採用）
#
# **它不是新到的檔** —— repo 內原已有兩份：
#   features/comfort/inputs/…（SHA b0827f02…，即 R-U9 所指名之候選）
#   docs/test/Player/SPEC/…（SHA fdc8196a…，位元組不同而 PU 內容相同）
# 本輪之複本逐位元組取自前者（`cmp` 相同）。
#
# **身分以內容確認，不以檔名**：52 包所載之量測（全三分頁、原始字串相異
# 1341 個 PU id、無 PU1087／PU1088、PU1089–1091 在）於本複本上逐項重現
# —— 原始字串 1341、正規化後 1339（差 2 為 `PU_0129` 之類之異寫）。
#
# **R-U9 之單一來源要求未達成**：該條要求「採用後移入 spec-index/」，
# 而移動 comfort 之檔屬寫入他 feature（R-U24／R-U30 禁止）。
# 現況為三份並存，已具名上報（52 上繳 §1.3）。
b0827f02c1a0a69b266fd92287557326b4e565942616dc49cca31672e4afa30b  inputs/Pop Up List HMI R1 SR24 Post 2A (Dec 15, 2023).xlsx
