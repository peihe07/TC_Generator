# 上繳包 16 —— R-DD20 v3／R-DD24 落地、A-DD11、DR-DD8 插句、DR-DD9 降級、T25a–c（補跑）

- 日期：2026-08-29
- 方向：執行層 → 分析層 ＋ Pei（§六 出貨閘）
- 對應下放包：`19_ship_ready.md` §七（T-抄／T-登／T25a／T25b／T25c）
- **⚠ 本包為補跑** —— 下放包 19 未於其當輪執行，本包在**上繳 17 之後**才落
  （上繳 17 §三 已報該缺口）。**編號依其所屬之下放包，非依落檔時序**
- 本輪**未寫回工作簿、未執行 git、未動 tsv、未生成 `-025`~`-028`**；共用路徑未寫入一字

> **三件事**：
> **T25b 得 `24 ＋ 0 ＋ 4 ＝ 28`** —— 上繳 17 §三所報之缺口已補，
> **未結 DR 中自此無阻斷出貨者**。
> **`R-DD20` 已成三版**（v1／v2 留存、v3 現行）；v1 `00912428`、v2 `5ae8694f`
> **二者之 `sha8` 皆與其落檔輪相同**，即留存版逐位元未動。
> **R-DD24 之實作早於其條文** —— (a)(b) 已於包 18 T24c 落地，
> 本輪只是把條文補上並逐條覆核；**這個順序本身值得記**（§四）。

---

## 一、T-抄

### 1.1 二條落檔

| 條號 | 來源 | 字元數 | 落檔 | 逐字元差異 | `sha8`（試跑）|
|---|---|---|---|---|---|
| **R-DD20 v3** | 下放包 19 §二 | **1091** | 1 次 | **0** | `1f2aef6d` |
| **R-DD24** | 下放包 19 §四 | **322** | 1 次 | **0** | `8fcae198` |

### 1.2 R-DD20 之三版錨點（R-DD14(a) 之體例延伸）

| 圍籬首行 | 錨點 | 狀態 |
|---|---|---|
| `R-DD20（…丁案…）` | `## R-DD20 v1` | 留存 |
| `R-DD20 v2（…）` | **`## R-DD20 v2`**（本輪由 `## R-DD20` 改題）| **本輪轉留存** |
| `R-DD20 v3（…）` | `## R-DD20` | **現行** |

`scripts/t17a_add_anchors.py` 之 `ANCHOR_OVERRIDE` 同步改為三列。

> **三版與 R-DD6 之二版式不同之處**：二版時「留存版加 `v1`、現行版裸名」即足；
> 三版起**中間版必須具名**（`R-DD20 v2`），否則無從與 v1 區辨。
> 故 `"R-DD20 v2": "R-DD20 v2"` 這一列雖與預設同值，**仍明寫** ——
> 它記錄的是「此版已非現行」，不是一個可省略的恆等式。

**條數與停止值同步**：

| | 上輪（包 20 後）| **本輪** |
|---|---|---|
| 索引現行 | 24 | **25**（新增 R-DD24）|
| 索引留存 | 2 | **3**（新增 `R-DD20`（v2））|
| 錨點（`^## R-DD`）| 26 | **28** |
| **停止條件 2 之值** | 26 | **28** |

**指紋試跑**（共用路徑，跑後還原；`git status` 對 `docs/fw036/` 無變更）：

| ruling_id | ancestor | sha8 | 狀態 |
|---|---|---|---|
| R-DD20 | `R-DD20 v1` | `00912428` | **與包 16 落檔時相同** |
| R-DD20 | `R-DD20 v2` | `5ae8694f` | **與包 17 落檔時相同** |
| R-DD20 | `R-DD20` | **`1f2aef6d`** | 新增（v3）|
| R-DD24 | `R-DD24` | **`8fcae198`** | 新增 |

**既有各列之 `sha8` 全未變。** 即本輪只加不改，二個留存版逐位元未動。

### 1.3 留存表新增 `R-DD20`（v2）一列，逐條列其失效值

> (c) 只書「不變」，其所指之字串在 v1（中文佔位，違 IN §1）——
> **本身未載任何可執行之字串**；且其 `PENDING: DR-DD9` 之缺件認定於 v3 被撤
> （037 本以通稱書之，上游未擬給指令，IN §8.4.3 之佔位義務於本欄不發生）；
> **無 (d) 之 DR-DD9 降級**

**上繳 17 §一-3 所報之歧義至此消滅** —— v3(c) 載有可執行之字串，不再指向他版。

---

## 二、T-登

### 2.1 A-DD11 建條（INFORMATIONAL）

`ANOMALIES.md` 新增 `## [A-DD11]`，載二變體逐字、出處、及**不回修任何 TC** 之判定：

| 變體 | 出處 | 關係 |
|---|---|---|
| `Feature not available while the vehicle is in motion` | `HMI Settings List` `Settings` r666 c7 | 與 profile §2.1 觀察面 B **逐字相同**（源 HMI spec p4）|
| `Function not available while vehicle is in motion` | 同檔 r151 c7 | **不同**：`Function`／無 `the` |

**不回修、不改 profile、不登 DR** —— 該不一致存在於**未綁之文件內**。

條中另答一個下放包明命但未給理由的點：**何以不入 marker 登記表**？

> marker 登記表之每一列課予**生成義務**（用及某物即須標）。
> 本條為 INFORMATIONAL，**不課任何生成義務** ——
> 入表即等於要求 TC 標一個沒有假設內容的 marker。**登記表只登有義務者。**

### 2.2 DR-DD8 文稿插句

| | 來源 | 字元數 | 落檔 | 逐字元差異 |
|---|---|---|---|---|
| 插句 | 下放包 19 §三-1 | **445** | 1 次（`Request:` 段後）| **0** |

其餘文稿**逐字不動**（已核）。

### 2.3 DR-DD9 降緩發

| 欄 | 舊 | 新 |
|---|---|---|
| 等級 | **必發** | **緩發**（R-DD20 v3(d)）|
| Batch impact | 「`-002` 之 `$` 指令行留 `PENDING: DR-DD9`」| 「`-002` 依 R-DD20 v3(c) 已可出貨；process 之具名為**品質改善項**」|
| 文稿 | — | **不動**（其二問仍成立）|

**發送清單同步**：必發 DD1／DD5／DD6／**DD8**（四筆）；緩發 DD2／DD4／DD7／**DD9**。

### 2.4 §2.4 拘束之全帳 grep —— 另清出二處

| # | 位置 | 舊 | 新 |
|---|---|---|---|
| 1 | 發送清單「必發」列之說明 | 「DD8 卡 `-120`／`-121` 之負向側；DD9 卡 `-001`／`-002` …」| DD8 改 `-013`／`-015`（依包 17 §四 10-5）；**DD9 移出該列**；加「四筆皆不阻斷出貨」|
| 2 | 「八筆未發送」下 | 無 | 加一句：**自 R-DD20 v3(c)(d) 起，未結 DR 中已無阻斷出貨者** |

> 第 1 項之 DD8 範圍是**同一處過期陳述之第四次**（包 17 §四 10-5 裁定後，
> 上繳 14 §2.2 改了 CG、上繳 17 §2.4 改了索引列、**本輪才改到發送清單之說明欄**）。
> **同一個裁定在三份不同的表裡各有一份副本，改一處不會連動。**
> 這是 §2.4 之 grep 拘束存在的理由，也是它至今仍在抓到東西的原因。

---

## 三、T25a —— `-002` 依 R-DD20 v3(c) 重生成

### 3.1 改動

```diff
  2. Terminate the DD process in the test environment
- $ PENDING: DR-DD9 <DD process termination command>
```

**只去 `$` 行；業務行、ER、其餘三則之任何欄位一字未改。**
生成器之 `reasoning` 同步改述其依據（v3(c) 之「台架程序層級、非規格缺件」）。

### 3.2 `-002` 之現行全文（四交付欄）

```
pre_conditions:
1. The signal $STATUS_CCAN3.VehicleSpeedVSOSig$ is transmitted on the bus at 0 (0.0000 km/h)

input_test_data:
NA

test_procedure:
1. Bring the HU through the Body OFF power down [ASSUMPTION A-DD10]
2. Terminate the DD process in the test environment
3. Start CAN activity on Body CAN to wake the HU [ASSUMPTION A-DD10]
4. Open "DND Customize auto reply message" and check that it opens

expected_result:
1. The HU periodic messages are no longer present on the bus
2. The DD process is no longer running in the test environment
3. The HU broadcasts all its periodic messages within 400 msec of wakeup
4. "DND Customize auto reply message" opens and its view is displayed
```

**業務行 `Terminate the DD process in the test environment` 仍為 037 r10 c18 逐字**
（首字母大寫為排版正規化，實詞未改）—— v3(c) 未改此點。

### 3.3 27 檢

| 產物 | 則數 | 結果 |
|---|---|---|
| pilot | 4 | 25 PASS ／ 2 N/A ／ 0 WARN ／ **0 FAIL** |
| B1 | 10 | 25 PASS ／ 2 N/A ／ 0 WARN ／ **0 FAIL** |
| B2 | 8 | 25 PASS ／ 2 N/A ／ 0 WARN ／ **0 FAIL** |
| **Body Off Init** | 2 | **25 PASS ／ 2 N/A ／ 0 WARN ／ 0 FAIL** |

**Body Off Init 之 N/A 由 1 回到 2** —— 即 T25c 之檢 8（§五）。

---

## 四、R-DD24 —— 條文晚於其實作，本輪逐條覆核

**(a)(b) 已於下放包 18 §四 10-6(a)(b) 落地**（上繳 15 §四），本輪只補條文。逐條覈對：

| 條 | 要求 | 現行實作 | 符 |
|---|---|---|---|
| (a) | first-match 指派須回傳**是否為正面命中**；落 fallback 一律 WARN | `_want()` 回傳三元組 `(方法, 分支, 是否 fallback)`；`FALLBACK` 非空即 WARN | ✅ |
| (b) | WARN 之 detail 須列出**所有未命中之判準** | `**三判準皆未命中**（非 fault、無二值對、無 ER 極性對、條件 N 項）—— 落於序末之 fallback，非正面判定` | ✅ |
| (c) | 覆核自此問四欄 | 本包 §四 即以四欄問；上繳 15 §四-2 已指出三欄不足 | ✅ |

**覆核之實跑**（重跑上繳 15 之 R 注入）：

```
[WARN]  13 §12  …
   001 → 功能測試 (Functional based ; no specific technique)（**三判準皆未命中**
        （非 fault、無二值對、無 ER 極性對、條件 0 項）—— 落於序末之 fallback，非正面判定）；
   002 → 狀態轉換 (State Transition Testing)（ER 對同一組實詞 ['hu','messages','periodic']
        分作否定與肯定之斷言（A→B）（門檻 3 為手定，見 R-G8 界線））；
   不符 first-match 無；皆為下拉選單實值 True　**fallback 命中 ['001']，須人審**
RESULT: PASS 24 ／ N/A 2 ／ WARN 1 ／ FAIL 0　（共 27 檢）
```

**(b) 之「列出所有未命中之判準」逐項對得上**：非 fault、無二值對、無 ER 極性對、條件 0 項。

### ⚠ 值得記一句 —— 條文晚於實作，這一次沒事，但不總是沒事

R-DD24 之 (a)(b) 是**執行層先做、分析層後裁**（10-6 之要求 → 實作 → 本輪落條）。
**本件相符，是因為條文照著實作寫。**

> **反過來的情形才是風險**：若條文在實作之後獨立寫出，
> 二者不符時**沒有任何機制會發現** —— 自檢驗的是產物，不是「實作是否合條文」。
> **本輪之逐條覈對是人做的，不是機器做的。**

---

## 五、T25c —— 檢 8 回到 N/A，且其 detail 書「無適用對象」

`-002` 不再有指令行，故檢 8 之 `cli` 為空，走 N/A 分支：

```
[N/A ]   8 §5.4  CLI／tooling 步驟採 description + `$` 指令格式；並比對步驟起首動詞…（T23b）
   2 則、共 7 步驟，以 `$ ` 起首之指令行 0 —— §5.4 **無適用對象**；
   步驟起首動詞 ['Bring','Open','Start','Terminate']；語料聯集 [...]（ACCESS […]／
   STIMULUS […]／**未分類 ['Bring','Read','Terminate']**，未分類者不參與 RE_ACCESS）；
   聯集外 無；語料之人審出處 下放包 18 §三 10-3
```

**確認三點**（下放包 19 T25c 所命）：

1. **書「無適用對象」**，非沿用前輪之 PASS 敘述（四判準之結果）—— ✅ 符 R-DD23 之推論
2. **數據為本產物導出**：`2 則、共 7 步驟`（`-001` 3 ＋ `-002` 4），非殘值 —— ✅
3. **未分類名單與人審出處仍逐次列印**（10-3 之防漏洞拘束不因回 N/A 而失效）—— ✅

> **這一項是本輪唯一一個「檢由 PASS 退回 N/A」的地方。**
> 退回時最容易發生的是**留著上一輪的 PASS 敘述** ——
> 那會變成「宣稱驗過了一個已經不存在的東西」。
> R-DD23 之推論（無適用對象時不得書無主詞之肯定斷言）正是為此。

---

## 六、T25b —— 出貨就緒盤點

```
已產出 24　＝　可出貨 24 ＋ 不得出貨 0
範圍外（profile §5 ∩ framework Part II，二來源一致）4：['025', '026', '027', '028']
閉合：可出貨 24 ＋ 不得出貨 0 ＋ 範圍外 4 ＝ 28　對 28 leaf：✅ 閉合
已產出∩範圍外（須為空）：空 ✓
既未產出亦未範圍外（須為空）：空 ✓
```

`exit 0`。**上繳 17 §三所報之 `23 ＋ 1` 缺口已補。**

**唯一變動之列**：

```
newR1L-DD-D002  002  Body Off Init  P1  A-DD10   **不可**（PENDING: DR-DD9）  →  可
```

> **下放包 19 §七 T25b 書「4 凍結」，本輪書「4 範圍外」** ——
> 該四列已由**下放包 20 之 R-DD25(b)** 自凍結改判範圍外，
> 且盤點之判準已於上繳 17 §五 隨之改讀。**本輪照現況書，不照下放包 19 之當時措辭。**

---

## 七、未結 DR 清單（本包後）

| DR | 狀態 | 等級 | 阻斷 | 台帳項 |
|---|---|---|---|---|
| **DR-DD1** | DRAFTED（確認件）| **必發**（文件缺陷）| **無** | A-DD1（CLOSED-BY-SCOPE）|
| DR-DD2 | DRAFTED | 緩發 | 無 | A-DD2 |
| ~~DR-DD3~~ | **RESOLVED** | — | — | ~~A-DD5~~ |
| DR-DD4 | PARTIALLY ANSWERED | 緩發 | 無 | A-DD6 |
| **DR-DD5** | DRAFTED | **必發** | 無（marker 式）| A-DD8 |
| **DR-DD6** | DRAFTED | **必發** | 無（marker 式）| A-DD9 |
| DR-DD7 | DRAFTED | 緩發 | 無 | A-DD7 |
| **DR-DD8** | DRAFTED（**本輪插句**）| **必發** | 無 | [CG-DD1] |
| **DR-DD9** | DRAFTED（文稿不動）| **緩發**（本輪降級）| **無** | A-DD10 |

**八筆未發送；DD3 已結案。**
**未結 DR 中已無阻斷出貨者** —— DD9 為最後一筆，本輪撤其佔位並降級。

**台帳項現況**：A-DD1 `CLOSED-BY-SCOPE`；A-DD2／A-DD6～A-DD10 `OPEN`；
**A-DD11 `INFORMATIONAL`**（本輪新立）；A-DD3／A-DD5 已結；
[CG-DD1] `OPEN`、[CG-DD2] `RECORDED`。

---

## 八、獨立自評

### 8.1 我做對的

1. **三版錨點時，把中間版明寫成 `"R-DD20 v2": "R-DD20 v2"`。**
   那一列與 `.get(id, id)` 之預設同值，刪掉程式照跑。
   但 `ANCHOR_OVERRIDE` 是 R-DD14(a) 之**體例載體** ——
   留著它才看得出「此版已非現行」是一個被決定過的事實，不是預設值。
2. **T25b 照現況書「4 範圍外」，沒有照下放包 19 之「4 凍結」。**
   下放包 19 成文時該四列確實是凍結；包 20 已改判。**補跑一包舊任務時，
   要照現況書，不照它成文時的世界。**
3. **A-DD11 之「何以不入 marker 登記表」是我補的理由。**
   下放包只命「不加」，未給理由。條目若只寫「不加」，
   下一個人加進去時不會知道自己違反了什麼。
4. **R-DD24 逐條覈對而非只落條。** 它的實作早於條文，
   「應該相符」很容易變成「假定相符」。跑了 R 注入把 (b) 的輸出貼出來對。
5. **§2.4 之 grep 又抓到 DD8 範圍之第四份副本。** 同一個裁定散在三張表裡，
   前三輪各改一處，都以為改完了。

### 8.2 我做糙的

1. **`-002` 之 `reasoning` 我是用正則定位後整段替換的**，非逐字元比對後替換。
   該欄非交付欄、不受 R-DD22 拘束，且生成後已重跑 27 檢 ——
   **但那不等於我確認了替換的邊界正確**。這是本輪唯一一處未經逐字元回證的編輯。
2. **R-DD24 之覈對表三列全 ✅，是我讀條文對程式碼讀出來的**，
   **沒有任何機器在檢「實作是否合條文」**（§四之末段）。這是一個結構性缺口，
   我指出了但沒有提議解法。
3. **本包編號 16 落在 17 之後**，`docs/upstream/` 之檔名序自此與時序不符。
   我照下放包所命之檔名寫（`16_ship_ready.md`），並在抬頭註明 ——
   **但目錄列出來時看不到那個註**。
4. **A-DD11 之二變體我沒有實測**，逐字取自下放包 19 §1.2。
   該查證為分析層所做（`copy_file_user_to_claude` 取副本），
   執行層**未複測** —— 與 A-DD1 當年之複測待遇不同。

### 8.3 我拒絕做的

1. **不改二個留存版（v1／v2）之圍籬一字**（R-TM13）—— `sha8` 為證。
2. **不改 DR-DD9 之文稿**（下放包 19 §三-2 明命「文稿本身不動」）。
3. **不把 A-DD11 加入 marker 登記表**（下放包 19 §七 明命）。
4. **不因「本輪 4 產物全綠」而略去 T25c 之逐項確認** —— 檢 8 由 PASS 退 N/A
   是最容易留下過期敘述的地方，逐項驗過才寫。
5. **不寫回工作簿** —— §六 為 Tier 3 之權。

### 8.4 一件我原本會漏的

**我原本會在 T25a 之後直接跑 27 檢，看到全綠就收工，不去看檢 8 的 detail。**

T25c 是下放包**單獨列出來的一項任務** —— 它不驗任何產物，只驗**一個檢的敘述**。
若下放包沒有列它，我不會去看：27 檢全綠、`-002` 可出貨、盤點閉合，
**每一個數字都對**。

而檢 8 的 N/A 敘述若沿用了上一輪的 PASS 文字（「含指令行之 TC／步驟…四判準違規 無」），
它會宣稱驗過了一個**已經不存在的指令行**。

> **全綠不會告訴你「綠的理由是不是還成立」。**
> 這一輪之所以沒事，是因為 N/A 分支與 PASS 分支在程式碼裡本來就是分開寫的 ——
> **不是因為我檢查過。**

---

## 九、量測條件揭露（R-G8）

### 9.1 分子與分母

| 所書 | 分子 | 分母 | 方法 |
|---|---|---|---|
| 「25 PASS ／ 2 N/A ／ 0 WARN ／ 0 FAIL」×4 | 各檢 verdict | 27 檢 | `SC_ARTIFACT=<檔> python3 scripts/selfcheck_tcs.py` |
| 「24 ＋ 0 ＋ 4 ＝ 28」| `PENDING` 命中／範圍外名單 | 24 已產出 ＋ 4 範圍外 | `scripts/t24e_ship_gate.py`，exit 0 |
| 「錨點 28 ／ 現行 25 ／ 留存 3」| `grep -c` | `RULINGS.md` | `^## R-DD` ／ `^\| R-DD` ／ 留存表列數 |
| 「逐字元差異 0」×3 | 落檔 vs 下放包 | R-DD20 v3 1091／R-DD24 322／DR-DD8 插句 445 字元 | Python 字串全等 |
| 留存版未動 | `sha8` 比對 | v1 `00912428`／v2 `5ae8694f` | `rulings_hash.py` 試跑後還原 |

### 9.2 已知界線

1. **`-002` 之 `reasoning` 未經逐字元回證**（§8.2-1）—— 本輪唯一一處。
2. **無機器檢驗「實作是否合條文」**（§8.2-2、§四末段）。
3. **A-DD11 之二變體未經執行層複測**（§8.2-4）—— 逐字取自下放包。
4. **上繳包檔名序自本包起與時序不符**（§8.2-3）。
5. 前輪各項界線續存：檢 13 之門檻 3、`NEG`／`STOP` 手列、marker 表為登記式、
   語言檢例外 3 未被行使、範圍外區間正則只認區間式。

### 9.3 本輪未量測者

1. 寫回工作簿之任何面 —— 未寫回。
2. `-025`~`-028` —— 範圍外，不生成。
3. `HMI Settings List`／`DTCs Matrix` 之查證 —— 分析層所做，執行層未複測。
4. profile §5 第 175 行之別字（上繳 17 §四）—— 分析層自辦，本輪未再核。

---

## 十、待分析層／Pei

| # | 事項 | 為何須分析層／Pei |
|---|---|---|
| **10-1** | **§六 出貨閘（甲／乙）** —— 材料**至此完備**：24 可出貨、0 不得出貨、4 範圍外、**無阻斷出貨之 DR**。分析層意見為乙 | **寫回為 Tier 3 之權** |
| **10-2** | **無機器檢驗「實作是否合條文」**（§四末段、§8.2-2）—— R-DD24 之覈對由人做。是否要求某種形式之對照 | 品保結構之事 |
| 10-3 | **A-DD11 之二變體未經執行層複測**（§8.2-4）—— 是否要求複測 | 是否值得（該檔未綁） |
| 10-4 | **上繳包檔名序與時序不符**（§8.2-3）—— 是否於 `docs/INDEX` 類之處加註 | 台帳體例 |
| 10-5 | profile §5 第 175 行之別字（上繳 17 §10-2）—— 仍待分析層改 | T26c 分析層自辦 |

**本輪未動**：工作簿、git、`docs/fw036/`、`features/power/`、`docs/runtime/`、
`-025`~`-028`；`generated/` 中僅 `batch_body_off_init.json` 之 `-002` 去一行。
