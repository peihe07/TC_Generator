# 下放包 12 —— T17b 停止採認、R-DD15~R-DD17、五項久懸結案、T18／批次 B1

- 日期：2026-08-28
- 方向：分析層 → 執行層 ＋ Pei（§二 tsv 解除）
- 前一包：`11_rulings_anchor.md`；對應上繳：`08_pilot_rev.md`（含續篇）
- **pilot 通過**：22 檢 20 PASS／2 N/A（有據）／0 FAIL；D1–D6 ＋ §5.2 六處全修
- **本包開批次**：見 §六

---

## 一、上繳包 08 審查判定 —— 收

### 1.1 T17b 之停止 —— **正確，且停在最難停的地方**

前四步全過、14 錨點驗訖、寫入只差一個動作、寫下去 `--check` 立刻轉綠。
**而綠的代價是把 popup 未 commit 之 107 行插入固化為基線。**

**§15.4 記入案例（本輪最高價值之一項）**：

> 「popup 線 R-POP18~20」按**名稱**歸因會打勾通過；
> 逐行比 **sha** 才看到 `R-POP18`（`18ddf460`→`bfbb3167`）與
> `R-POP20`（`7ac862b3`→`2894aa0c`）**本體已改寫** ——
> 現行 tsv 裡那三條與重生出來的那三條**不是同一份東西**。

**拘束（全域，自本包起）**：凡以「已知項」為由放行之差異，
**歸因鍵須為內容雜湊，不得為識別名**。名稱相同而雜湊相異者，
一律視為未知項處理。

> 下放包 11 §三之停止條件寫的是「popup 線 R-POP18~20」—— **以名稱書之**。
> 執行層若照字面打勾即通過。**是它改用 sha 才擋下來。條件寫得不夠精確是我的事。**

### 1.2 §12.2 錨點命名 —— 推導法會靜默對調

R-DD6 之 v1／v2，其**圍籬首行**（條文自身之標題，寫於下放時）與
**索引識別**不同步：v1 之首行為 `R-DD6（…）`、v2 之首行為 `R-DD6 v2（…）`。
由首行推導即把二者對調，**而兩種結果都跑得出 14 個錨點，停止條件抓不到。**

改用明示對照表為正確。**成因在分析層**：包 08 §二令「v1 轉留存」時，
未同時指定其標題行之處置。

### 1.3 §14 之減法

148 → 144 **不是「已轉換 4 條」**：本 feature 轉出 11（−11）、
`sw_update` 期間新增 7（+7），淨減 4。**不逐一核算即會誤讀。**
合 R-DD10(c)（書計數須書母體判準與排除項）之精神。

### 1.4 §2.5 之三項判斷性檢

第 9／11／12 項非純字串比對而明說其為判斷 —— **正確**。
**把判斷偽裝成量測比不做還糟**，此語採為一般拘束。

---

## 二、tsv 解除 —— 分析層傾向丙，**待 Pei**

三案見上繳 §13.6。**分析層意見：丙（由 popup 線統一重生）。**

理由：重生為一次寫全，**由變動最大之線執行，其餘線只需驗自己那幾列**；
且僅該線知悉自身是否定稿。本線 14 列已可被任何一次重生正確收錄，**無時序要求**。

**本線續行之條件**：popup 線 commit 並重生後，本線重跑第 5 步逐行歸因，
差異若僅剩本線 14 列即寫入。**在此之前 T17b 維持停止。**

`--check` 現行 FAIL 為**已知且已歸因**（本線 14 列未收 ＋ popup 9 項未收），
非未知態 —— 提請 Pei 知會 popup 線時一併說明。

---

## 三、裁決條文（R-DD15／R-DD16／R-DD17）

```
R-DD15（§5.2 之 word 計法）

IN §5.2 未定義 word 之計法。本 feature 採：

(a) 去除編號前綴（`^\d+\.\s*`）後，以空白切分之 token 數。
(b) 訊號名 `$MESSAGE.Signal$` 計 1；`(8.0625 km/h)` 計 2；
    帶引號之 UI 標籤依其內含空白計（`"Pairing (1st time)"` 計 3）。
(c) 本計法對含單位與引號標籤之步驟**偏嚴**（同語意之步驟字數高於英文散文）。
    **偏嚴為刻意** —— 偏嚴不會放過違規，偏寬會。
(d) **不得為使步驟合格而改計法**。逾限時改步驟，不改尺。
（Pei 下放，分析層即裁，下放包 12 §三）
```

```
R-DD16（輸出鍵名二制之處置）

IN §10.1 所列鍵名（tc_title／specification_reference／split_flag／
split_reason）與本專案既有 pilot／寫回形制（test_item／spec_reference）不同。

(a) **鍵名依既有寫回形制**：`test_item`／`spec_reference`。
    該二者為工具契約（寫回器所消費者），非 ASPICE 內容之要求；
    逕改鍵名將斷開寫回管線。
(b) **`split_flag`／`split_reason` 仍須輸出** —— 該二者承載資訊
    （某 leaf 是否被拆為多 TC 及其理由），非命名之別。
    未拆者 `split_flag: false`、`split_reason: "NA"`。
(c) 若寫回器拒受未知鍵，**回報，不得逕自刪鍵** ——
    刪之即湮滅 §8.2.2 之拆分紀錄。
（Pei 下放，分析層即裁，下放包 12 §三）
```

```
R-DD17（Pre-Condition 之訊號源行）

四則 pilot 之 PC 現書
`The vehicle is stationary and $…$ is transmitted at 0 (0.0000 km/h)`。
其前半（`The vehicle is stationary`）與後半之 raw 0 重複陳述同一事實，
且前半為環境敘述而非 spec 觸發條件（IN §8.5）。

裁定：PC 之訊號源行**只書訊號源本身**，形如
`The signal $MESSAGE.Signal$ is transmitted on the bus at <raw> (<物理值>)`。

理由：如此該行純為 §4.5-1 之環境資料（外部訊號源），
**不再兼作對觸發條件之主張** —— 上繳 §1.2 所指
「-009 為四則中唯一非嚴格觸發條件者」之疑點，隨之消解：
四則之該行一律為環境資料，其是否兼為觸發條件，
由 037 原文於 test_item 與 Procedure 承載，不由 PC 承載。
（Pei 下放，分析層即裁，下放包 12 §三）
```

---

## 四、五項久懸之結案

| # | 事項 | 處分 |
|---|---|---|
| 3 | **輸出鍵名二制**（上繳 07 §10-5、08 §11-3 二度提出）| **R-DD16**。**分析層之誤：二度未答。** 執行層依既有形制辦並揭露為正確 |
| 4 | `-009` 之 PC 依 §8.5 是否應刪 | **R-DD17** —— 不刪，改寫為純訊號源行，疑點隨之消解 |
| 5 | §5.2 之 word 計法 | **R-DD15** —— 採執行層之計法，並明定偏嚴為刻意、不得改尺 |
| 8 | **R-DD14 未列入 T-抄** | **分析層之漏列。** 執行層未逕抄為正確（抄則錨點 15，與停止條件 2 之 14 牴觸）。本包補 T-抄，停止條件同步更新為 **15**（14 現行 ＋ 1 留存）|
| 9 | `R-DD6` v1／v2 於 tsv 同 id | **接受現況，不改條號。** 現行 tsv 已有 17 組同 id（`R-C27`／`R-C4`／`R-TM9` 等），工具印警告而二列皆收錄，**無資料遺失**。`vehicle_setting` 之 prime 法可分離，惟其為條號改寫，**代價高於效益**。索引留存列須並列二者之 `sha8`（`f28ee265` v1／`a5cbaf9c` v2），**使其以雜湊可辨** —— 合 §1.1 之拘束 |

**§13.5 之 `superseded` 4 條**：非本線範圍，未追查為正確。
提請 Pei 於全域重生時一併知悉。

---

## 五、pilot 之最終修訂（T18a）

pilot 已通過，**惟依 R-DD16／R-DD17 尚有二處形式修訂**：

| # | 修訂 |
|---|---|
| T18a-1 | 四則 PC 依 **R-DD17** 改寫為純訊號源行 |
| T18a-2 | 四則補 `split_flag: false`／`split_reason: "NA"`（**R-DD16(b)**）。若寫回器拒受，回報不刪 |

修訂後重跑 22 檢，**第 3 項（§4.4／§8.5）之判準須含 R-DD17 之形式**。

---

## 六、批次 B1（T18b／T18c）

pilot 已證形制可行。**開批次生成**，惟先過一道訊號閘：

### 6.1 T18b —— 訊號涵蓋掃描（B1 之前置）

掃 leaf `-001`~`-008`、`-013`~`-016`（12 leaf）之 037 全欄，
擷取全部 `$...$` token，與 profile §3 之五項比對，輸出：

- **已涵蓋者**（可逕用 profile §3 之寫法）
- **未涵蓋者**（逐項列出，含其所在 leaf 與 037 原文脈絡）

> **`-001`／`-002`（Body OFF 初始化）為重點** —— 其觸發為電源域事件，
> profile §3 五項中無對應者。**若掃出新訊號，該二 leaf 不入 B1**，
> 另循 R-DD5／R-DD6 v2 之四庫查對流程，查無者登 DR。

### 6.2 T18c —— B1 生成（範圍依 T18b 之結果）

| 情形 | B1 範圍 |
|---|---|
| T18b 全數已涵蓋 | `-001`~`-008` ＋ `-013`~`-016`（12 leaf） |
| `-001`／`-002` 掃出未涵蓋訊號 | **B1 = `-003`~`-008` ＋ `-013`~`-016`（10 leaf）**；`-001`／`-002` 待四庫查對 |
| 其他 leaf 亦掃出未涵蓋 | 逐一剔除，**剔除者列表回報，不臆造施加路徑** |

**拘束**（承 pilot，不重述已證者）：

1. **自檢骨幹為 IN §9 十七項**（＋追加項），下放包所列一律為額外
2. `-003`／`-005`／`-007`／`-013`／`-015` 用及 §3.1 raw 者標 `[ASSUMPTION A-DD6]`
3. `-013`／`-015`（Lockout Tables）之取樣 feature 依 profile §2.1，
   **黃標三項與 NAV 系不取樣**；`-013`／`-015` 之取樣**得與 pilot 相異**，
   惟須於 reasoning 載明所據之 p7 列
4. **解鎖方向**（`-005`／`-013` 之 ≤3 MPH）用 raw **77**；
   BVA 之另一側（`78`／`128`）**除非 037 明書，否則不擴入**（§8.2.1）
5. priority 依 profile §4；**不得因批次而重判**
6. **只生成，不寫回、不 git**；產物置 `generated/`

---

## 七、任務彙總

| # | 任務 |
|---|---|
| T-抄 | **R-DD14**（包 11 §一，前輪漏列）＋ **R-DD15／R-DD16／R-DD17**（本包 §三）逐字入 `RULINGS.md`，並依 R-DD14 加標題錨點。**錨點數應為 18**（17 現行 ＋ 1 留存）—— 停止條件 2 之數值更新為 **18** |
| T-登 | 索引留存列並列 `R-DD6` v1／v2 之 `sha8`（§四-9）|
| T18a | pilot 四則依 R-DD17／R-DD16(b) 修訂，重跑 22 檢 |
| T18b | 訊號涵蓋掃描（§6.1）|
| T18c | B1 生成（§6.2，範圍依 T18b）|
| — | **T17b 維持停止**，待 popup 線（§二）|

**不在本輪**：寫回工作簿、git、他 feature 之 `RULINGS.md`、`-017`~`-028`。

## 八、上繳包要求（`docs/upstream/09_batch_b1.md`）

T-抄核對（18 錨點）、T-登結果、T18a 修訂後四則 ＋ 22 檢、
T18b 掃描原始輸出（含未涵蓋者之 037 脈絡）、T18c 之 B1 全文 ＋ 自檢、
未結 DR 清單、獨立自評、R-G8 揭露。
