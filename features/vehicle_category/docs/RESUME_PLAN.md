# 續作計畫 —— Vehicle Category（DR 回覆後）

> **建檔於下放包 33 §四（T169）。本檔為計畫，非執行紀錄 ——
> 建檔時未執行任何一項。**
>
> 由來：分析層在 DR 回覆前已無可推進之項目，
> **趁記憶尚在把續作寫成可執行之計畫**，免得回覆到時重新推導。

- 建檔：2026-08-27
- 逐字採下放包 33 §四；執行層之更新以加註為之，原文不改（R-TM13）

---

分析層在 DR 回覆前已無可推進之項目。**趁記憶尚在，把續作寫成可執行之計畫**，
免得回覆到時重新推導。置 `docs/RESUME_PLAN.md`。

### 4.1 依 DR 分流

| DR 回覆 | 續作 | 影響面 |
|---|---|---|
| **DR-VC9(二)**（三筆 leaf 之地位）| 判為需求 → b 段 3 筆生成（`007-01`／`013-04`／`025-01`）。**其來源皆 < 60 字元**，適用 profile §8 之短來源處置（上半取完整句 ＋ 人工複核，下放包 19 §1.1）<br>判為誤登 → 三筆自 117 母體移出，**R-VC3 之母體修訂**（Tier 2，須另裁），表 A／表 B／`verify_partn.py`／framework 之 leaf 數全部重編 | 大 |
| **DR-VC3**（17 節是否應補）| 「不補」→ 表 B 定稿，第 6／7 批 3 筆照現行邊界生成<br>**「應補」→ 章 8／9 另立 `Cabrio Rooftop`，framework 8 組 → 9 組**（R-VC16(c)），表 A／表 B／`verify_partn.py` 重編；14.2／§15 併入 `Brake Service`；16.2.1／16.2.2 併入 `Cabrio Widget`；§11.9 群併入 `Settings List`（R-VC16(d)）| **最大** |
| **DR-VC10**（`PU0091` 之字／`061` 路徑）| 7 處 PENDING 結案 | 中 |
| **DR-VC9(一)**（PDO Graphics／`table above`）| 3 處 PENDING 結案（`011`／`012-03`／`014`）| 小 |
| **DR-VC1**（`PUXXXX`）| 1 處 PENDING 結案（`021`）| 小 |
| **DR-VC8**（鎖定門檻）| 1 處 PENDING 結案（`033-01`），並依 R-VC18 另裁是否補拆 boundary 2–3 筆 | 小 |
| **DR-VC2／DR-VC4／DR-VC5／DR-VC6** | 不阻斷任何批次 | 無 |
| **`QS Suggestion` 第 4 項** | 若已生效 → **120 筆 priority 全數重映射**，R-VC11／13／14 之定案基礎失效（IN §10.2 明文衝突）| **全面** |

### 4.2 全部 PENDING 結案後之出貨序

```
1. 12 處 PENDING 全數結案（或 Pei 依 IN §8.4.3 裁定降轉 NA）
2. 表 A 定稿、表 B 定稿（R-VC3 之出貨門檻二表）
3. 丙″ 產出交付本（TC ID 依 R-VC28 `newR1L-VC-001…`）
4. 側檔對工作簿之對應驗證（上繳包 28 §4.2 之未機器化項）
5. `DELIVERY_CHECKLIST` 五項人工驗收（Pei）—— 其中第 5 項母體為 12 處
6. `DELIVERY_NOTE.md` 撰寫（含未結項之揭露）
7. Pei 之 git 操作
```

### 4.3 續作時之必讀

```
接手者於續作前須讀：
  RULINGS.md（R-VC1~R-VC29）、ANOMALIES.md（A-VC1~A-VC21）、
  DATA_REQUESTS.md（DR-VC1~DR-VC10）、REVISIONS.md（REV-01~REV-14）、
  framework.md、profile、PLAYBOOK §7、本計畫

**並依 R-VC29 對任何將修訂之條文先跑 `ledger_xref`。**
```

---

---

## 4.4 執行進度加註（下放包 34，T177；原文不改，R-TM13）

**b 段與第 6／7 批已自待辦移除** —— 其裁定與生成見下：

| 原待辦 | 現況 |
|---|---|
| **DR-VC9(二) → b 段 3 筆** | ✅ **已生成**（R-VC30，Pei 2026-08-27 裁定 (a)：裁為需求）。`007-01`／`013-04` 併回第 1 批（24 筆）、`025-01` 併回第 3 批（17 筆）。**DR-VC9(二) 之查證維持發送**；回覆若與裁定相反，依 §4.1 之該列由 Pei 再裁 |
| **DR-VC3 → 第 6／7 批 3 筆** | ⚠ **2 筆已生成**（`065-01` 第 6 批、`066` 第 7 批）；**`065-02` 留置** —— 勘查命中停止條件 (b)，見上繳包 34 §3。DR-VC3 對表 B 四處與 framework 組數之影響**不變** |

**剩餘待辦（DR 回覆前無可推進者）**：

1. ~~**`065-02` 之 CONT 處置**~~ ✅ **已解**（下放包 35 §二：profile §9.3.1
   佔位剝除取材通則；上繳包 35）—— **生成側自此無任何待辦**
2. **14 處 PENDING 結案**（原 12 ＋ `013-04` 之 DR-VC9 ＋ `065-02` 之 DR-VC10）
3. 表 B 定稿（四處待 DR-VC3）
4. **Excel 人工驗收**（Pei）—— 寫回前最後一道
5. 126 筆之一次寫回 —— **126 筆已齊**（117/117 leaf）

> §4.2 之出貨序**不變**，惟其第 1 項之母體由 12 處改為 **14 處**。

### 4.5 生成側終數（下放包 35 收斂後）

```
117 / 117 leaf 全數處置    TC 126 筆    八批回歸 22 checked / 0 failed ×8
```

**全案剩三道門**：
~~Excel 驗收（Pei）→ 寫回工作版 →~~ PENDING 結案後出貨。

### 4.6 寫回工作版已完成（2026-08-27，Pei 直接指示；上繳包 36）

`output/…_VehicleCategory_20260827_working.xlsx`，126 列，驗收 8 項全過。
**側檔↔工作簿之對應已機器化並 PASS**（包 28 §4.2 之未機器化項至此關閉）。

**剩二道門**：
1. **Excel 人工驗收**（Pei）—— **改對工作版跑**，非 `/tmp` 之 3 筆探針
2. PENDING 14 處結案 ＋ 表 B 定稿 → 出貨版

---

## 5. 本檔之維護

- **每次續作後更新**：已完成者標 ✅ 並記其上繳包編號；新增者附其由來。
- 原文不改（R-TM13）—— 變動以加註為之，使「當初怎麼計畫」與
  「後來怎麼做」可區分。
