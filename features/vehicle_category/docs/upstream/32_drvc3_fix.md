# 上繳包 32 —— Vehicle Category：DR-VC3 更正（時效）＋ 四項（T164–T168）

- 日期：2026-08-27
- 對應下放：`docs/handoff/32_drvc3_fix.md`
  （SHA256 `edde35b6c6c868a3d8e3935bb6b026d76f8fa9576df4ae339dcf5a1daa54e12b`，155 行）
- **T164 已完成 —— DR-VC3 現在可發。**
- 未寫回交付本、未進行任何 git 操作。

---

## 0. T164（時效）—— 先報這一項

**`DATA_REQUESTS.md` DR-VC3 之「發出前須知」段已改寫。**
裁定措辭**逐字落入**（SHA256 `637198b224135005…`，`in` 比對通過），
原措辭依 R-TM13 保留並加註作廢。

### 0.1 修改前後逐字對照

```diff
--- DATA_REQUESTS.md (前)
+++ DATA_REQUESTS.md (後)
@@ -71,8 +71,49 @@
   之衍生 PDF 所寫，**不得引為本 DR 之措辭依據，亦不得寫入表 B**；
   發出時一律採 T17 重驗後之內容。
-- **T17 之實質發現（發出前須知）**：18 節中 **§15 與 §10.1／10.2 之關鍵內容
-  在 repo 之權威素材中不可得** —— 其為圖檔，SYS1 `Description` 僅存
-  `(image: imageNN.png)` 佔位。故本 DR 對該三節之提問應改為
-  「該節內容僅存於圖，SYS1 匯出未帶文字」，而非引述任何摘要文字。
+- **T17 之實質發現（發出前須知）** —— **本段已依 R-VC27 改寫（下放包 32 §1.1，
+  2026-08-27）。改寫後之措辭逐字如下**：
+
+```
+T17 之實質發現（發出前須知）—— 逐節分列（R-VC27 修訂，2026-08-27）
+
+  §15  —— SYS1 `Description` 僅存標題句
+           「Electronic Park Brake Service Mode Pop-up」
+           ＋ 三個圖佔位（image20/21/22）。其實質內容不可得。
+           本 DR 對本節之提問：請提供該三圖之內容。
+
+  §10.1／§10.2 —— **文字部分已有，不需索取。**
+           SYS1 `Description` 載有：
+             「Refer to the HMI Settings list for settings location.」
+             「All four Aux switches (Aux 1 Aux 2, Aux 3, and Aux 4)
+               can be used simultaneously」
+           另有四個圖佔位（image9–12）。
+           本 DR 對此二節之提問：
+           (a) 該四圖之內容；
+           (b) 上開第二句是否即為此二節之完整需求，
+               抑或圖中另載其他規則。
+
+  其餘 14 節 —— 依原措辭。
+
+原段（作 §15／§10.1／§10.2 三節皆「僅存於圖，SYS1 匯出未帶文字」）
+係據 R-VC12 二(a) 而寫，該款已由 **R-VC27 作廢**（A-VC20）。
+原文不刪，於本段末加註「原措辭作廢，見 R-VC27」（R-TM13）。
+```
+
+  > **原措辭（R-TM13，不刪）**：
+  >
+  > 「**T17 之實質發現（發出前須知）**：18 節中 **§15 與 §10.1／10.2 之關鍵內容
+  > 在 repo 之權威素材中不可得** —— 其為圖檔，SYS1 `Description` 僅存
+  > `(image: imageNN.png)` 佔位。故本 DR 對該三節之提問應改為
+  > 「該節內容僅存於圖，SYS1 匯出未帶文字」，而非引述任何摘要文字。」
+  >
+  > **原措辭作廢，見 R-VC27。** 其對 **§15 成立**，對 **§10.1／§10.2 不成立**
+  > —— 該二節之 SYS1 `Description` 載有
+  > `All four Aux switches (Aux 1 Aux 2, Aux 3, and Aux 4) can be used
+  > simultaneously`，為可讀、可測之需求敘述（A-VC20，上繳包 31 §0）。
+  >
+  > **⚠ 本段標著「發出前須知」且明文指示本 DR 之措辭** ——
+  > 其錯誤之影響不限於 repo 內部：照原措辭發出，
+  > 會 (a) 索取一份我方已有之內容、(b) 把真正的缺口（圖內內容）錯誤放大。
+  > **DR-VC3 之發送狀態與已發時之更正函，屬 Pei（Tier 3）**（下放包 32 §1.2）。
 
 ## DR-VC4 —— VF507 / VF352 兩份未附文件
```

### 0.2 三項落實之確認

| 要求（下放包 §1.1）| 落實 |
|---|---|
| 逐節分列之新措辭 | ✅ 逐字，未增刪一字 |
| 原文不刪 | ✅ 以引用區塊完整保留 |
| 末加註「原措辭作廢，見 R-VC27」| ✅ 並補記其**對 §15 成立、對 §10.1／§10.2 不成立** |

**另加一段執行層之提醒**（非裁定所要求，但屬本件之要害）：
該段標著「發出前須知」且明文指示本 DR 之措辭 ——
**其錯誤之影響不限於 repo 內部**。已於加註中記明二項後果
（索取已有之內容／把真正的缺口錯誤放大），
並註明發送狀態與更正函屬 Pei（Tier 3）。

**DR-VC3 之發送狀態我不知道，也無從查** —— §1.2 之分流由你裁。

---

## 1. T165 —— PLAYBOOK §7.6

「條文修訂後之波及檢查，**先查對外文件**」逐字入 §7（建議位階）。

**併記本次之順序恰好相反**：R-VC27 修了 `RULINGS.md` 與 `data/tableB_draft.md`，
**未修 `DATA_REQUESTS.md`** —— 先修了不出門的，漏了要出門的。

**另補一張本 feature 之對外文件清單**（裁定未要求，波及檢查時逐一查）：

| 檔 | 何時離開 repo |
|---|---|
| `DATA_REQUESTS.md` | **DR 發出時**（其措辭直接進入信件）|
| `docs/DELIVERY_CHECKLIST.md` | 交付審查時 |
| 交付本 xlsx（尚未產出）| 出貨時 |
| 表 A／表 B | **隨交付本出貨**（R-VC3 之出貨門檻二表）|
| `docs/REASONING_sidecar.md` | 隨交付本出貨 |

**已記明本節無承載者。** 與 §7.1／§7.3／§7.4／§7.5 同為紀律 ——
**本節現為第五條紀律類條文，五條皆無承載者。**

---

## 2. T166 —— `DECISIONS.md` 加註

於「DR 七筆全未結」一行下方加註（簽署本文不改，R-TM13／A-TM15）：

> （截至簽署日 2026-08-26 為七筆；現為十筆，見 `DATA_REQUESTS.md`）

**另補二事**（裁定未列，但同屬該行之過時範圍）：
- **同批 A 亦已自四項擴為六項**（＋ DR-VC8、DR-VC9(二)）—— 該行下半亦過時。
- 加一句通則：**本節為時點紀錄**，其各行於簽署日為真、不隨後續變動更新，
  現況一律以 `DATA_REQUESTS.md` 與 `ANOMALIES.md` 為準。

> 只加「七 → 十」而不動同批 A 那半句，下一個讀者仍會讀到過時的四項。

---

## 3. T167 —— 第三段盲區

`scripts/ledger_xref.py` 之模組說明補記三段分工：

```
跨檔          —— 本工具 ＋ `xref_triage`（DR-VC3 之真牴觸即由此抓到）
同檔、同條目內 —— PLAYBOOK §7.5（加註時複核該條各款），紀律，無承載者
同檔、跨條目   —— **無人管，已知第三段盲區**
```

並記明下放包 31 §4.1 之不做第三判準**裁定不變**，
此處僅補記其為盲區（前記僅提「同條目內」而漏了「跨條目」）。

---

## 4. T168 —— 七項判定之落檔

| 標的 | 判定 | 是否有敘述已過時 | 處置 |
|---|---|---|---|
| `§8.3` | 同一事實之不同面 | **否** —— `tableB_draft.md:37` 已載其逐字全文，註 1 記其補入依據 | **無需落檔** |
| `§16.2.1` | 同一事實之不同面 | **否** —— 「有內容」與「037 未涵蓋」為同一事實之兩半 | **無需落檔** |
| `§16.2.2` | 同一事實之不同面 | **否** —— 同上 | **無需落檔** |
| `R-VC19` | 同一事實之不同面 | **否** —— **實測其 fence 外已有加註**：「(a) 已擴充、(d) 已作廢 —— 見 R-VC23」。A-VC15 所述之「(c) 無承載者」與該加註並存不悖 | **無需落檔** |
| `R-VC4` | 非牴觸 | **否** —— 七處全部一致（`spec_reference` 逐字取 037 `HMI Source ID` 欄原值）| **無需落檔** |
| `DR-VC2` | 時點差異 | **是** —— `DECISIONS.md:118` | **已落檔（T166）** |
| `§2` | 噪音 | **是（工具側）** —— owner 表不含 `framework`／`profile` 形態 | **已落檔**：`ledger_xref.py` 之 OWNER 處加記「已知殘餘」，並註明**本輪不修**之理由（一次只動一個變數）|

**七項中五項無需落檔、二項已落。**

> `R-VC19` 一項我原以為需要加註（(d) 已由 R-VC23 作廢），
> **實測發現加註早已存在**（下放包 17 §3.2 時所加）。
> **這一次「先查再說」的順序是對的** —— 若照推定去加，會加出重複。

---

## 5. 六批回歸（本包未動生成物）

```
pilot_glovebox                     22 checked / 0 failed
batch1_category_structure          22 checked / 0 failed
batch2_settings_list               22 checked / 0 failed
batch3_controls                    22 checked / 0 failed
batch4_settings_behavior           22 checked / 0 failed
batch5_ignition_availability       22 checked / 0 failed
```

`ledger_xref` 五斷言仍全過，候選數不變（90）——
本包之改動皆在說明與加註，未動判準。

---

## 6. 量測條件揭露（R-G8）

- **T164 之逐字比對**為 `in` 比對（裁定之 code fence 全文是否為
  `DATA_REQUESTS.md` 之子串），**非 `diff -q`** ——
  後者需二個等長檔案，此處為片段落入長檔。
- **DR-VC3 之發送狀態未查、無從查** —— §1.2 之分流未執行。
- **T168 之「無需落檔」為否定性判斷**，其母體為
  `xref_triage_review.tsv` 之保留行（該七標的共 26 行）。
  **第一層剔除之 536 處未讀** —— 若某過時敘述落在被剔除的「純編號引用」裡，
  本輪看不到。
- `§2` 之殘餘**未實測其實際筆數** —— 只知其成因。

---

## 7. 待你裁

1. **DR-VC3 之發送狀態**（§1.2 分流）—— 未發則現可發；已發則補更正函
2. `§2` 之 owner 殘餘是否另輪修（§4）
3. Tier 3：其餘九筆 DR 之回覆

---

## 8. 進度

**117 leaf 中 112 筆已收斂，TC 累計 120 筆。** 生成側停於 DR-VC3／DR-VC9(二)。
表 A 完成、表 B 草稿（四處待 DR-VC3）、`reasoning` 側檔 120 筆、
TC ID 已裁（R-VC28）、丙″ 六項全過、交付前清單五項建檔。

**紀律類條文現為五條**（PLAYBOOK §7.1／§7.3／§7.4／§7.5／§7.6），
**五條皆無承載者** —— 其中 §7.4 於寫入後**一包之內即失效一次**（上繳包 31 §1.1）。
