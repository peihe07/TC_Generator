# 上繳包 20 —— Vehicle Category：CONT 二層防護 —— **self-test 攔下，停**

- 日期：2026-08-26
- 對應下放：`docs/handoff/20_batch3_tc.md`
  （SHA256 `eb7bf492d1678326f0d8f8225ec6ea317407f6fc497a248661251b8b411f39eb`，196 行）
  ＋ `docs/handoff/20a_cont_guard.md`
  （SHA256 `b61cbb66094e111d0380e2eee7105a55a3819b00a90d0e96545f8674350ecc14`，120 行）
- **結論：T108／T109／T111 完成。T110 停於第二層之 self-test。
  T112／T113（生成第 3 批）未執行。**
- 未寫回工作簿、未進行任何 git 操作。

---

## 0. 一頁摘要

| # | 任務 | 結果 |
|---|---|---|
| T108 | PLAYBOOK §7.1.1 self-test 前置 | ✅ |
| T109 | R-VC24／R-VC25 抄錄 | ✅ 逐字（`619d02720c3d1bc3`／`399842a0a3b4ab07`）|
| T110-1 | 二層檢查 ＋ self-test | ✅ 實作完成 |
| T110-2~5 | 全表首跑／登記／驗證／納入常駐 | ⚠️ **停** —— 第二層 self-test 不過 |
| T111 | DR-VC9 第三問 | ✅ |
| T112／T113 | 生成第 3 批 16 筆 ＋ 收斂 | ⏸ **未執行** |

**停點：self-test 第 3 個斷言（第二層之已知標的）FAIL。
而它抓到的是真的 —— 且抓到的不是登記錯，是判準之前提不成立。**

---

## 1. T108／T109

**PLAYBOOK §7.1.1**：已知標的驗證改為掃描器之自帶前置斷言，
「**先」由紀律變為程式結構 —— 不先跑就跑不了**。新寫者一律適用、既有不回溯。

**R-VC24／R-VC25** 逐字抄入 `RULINGS.md`，byte-level diff 通過。

---

## 2. T110 —— 停點

### 2.1 self-test 之四個斷言

```
cont_guard —— self-test 前置（PLAYBOOK §7.1.1）
  self-test 1  第一層(b) 已知標的 019-02 應為候選  PASS  命中=['reference','short']
  self-test 2  第一層(a) 反向 017 不應為候選        PASS  命中=無
  self-test 3  第二層(b) 第 1 批四筆登記應全過      **FAIL**
               母體=4 不符=[('SWE1-HMI-VC-013-02','2.6.3',
                            'For four or more features, display the first two',
                            '片段非該節之子串')]
  self-test 4  第二層(a) 反向 012-03→§2.5 應 FAIL   PASS

**self-test 未全過 —— 不輸出正式結果，非零碼退出。**
EXIT=2
```

**第一次跑就攔下。** 這正是 §7.1.1 要的形態 ——
若照舊（寫完、跑全表、看結果），我會先看到一張漂亮的候選清單，
第二層之問題要等到有人細看才會發現。

### 2.2 診斷 —— 登記是對的，判準之前提不成立

| | 逐字 |
|---|---|
| **037 `013-02`** | `For four or more features, display the first two features as single banners, the next two features as half banners in the same row**.**` |
| **SYS1 §2.6.3 第 2 句** | `For four or more features, … in the same row**, and follow with remaining features as tiles below the half banners.**` |

**037 把 `, and follow with…` 改成了 `.`** —— 這不是「切一段」，是**改寫**。

四筆之子串關係實測：

| leaf | 037 片段 ⊆ SYS1 節 |
|---|---|
| `012-02` | ✅ |
| `012-03` | ✅ |
| `013-03` | ✅ |
| **`013-02`** | ❌ |

**登記之 SYS1 節（2.6.3）是正確的** —— 錯的是第二層之前提
「續行型之定義即『037 之片段出自 SYS1 之完整句』，**片段必為其來源之子串**」。
`013-02` 是**被編輯過的片段**，不是純片段。

> 此即第 1 批勘查（上繳包 14 §2.2）已記之發現：
> 「**037 在 `013-02` 句中補了句號並刪去 `and`**，使其看似完整句。」
> 當時記為「只讀 037 不比對 SYS1 會誤讀」；
> **現在它以另一種形態回來 —— 使一個正確的登記通不過內容驗證。**

下放包 20a §2.3 曾預期 `013-02` 為第一層之偽陰性
（首字大寫 `F`，三特徵皆不命中）並註「其已在 CONT 表，**不受影響**」。
**第一層之判斷正確，但第二層受影響** —— 該預期只涵蓋了第一層。

### 2.3 我沒有做的兩件事

1. **未放寬第二層判準使其通過**（例如改為 token 重疊率門檻）——
   那會引入閾值，而本 feature 已三次判定閾值「量錯了東西」
   （停止條件 87／第 14 項之編輯距離／R-G39）。
   且**放寬會使 self-test 4（反向）之鑑別力一併下降**。
2. **未把 `013-02` 移出 CONT 表或移入排除清單** ——
   它確實是續行型（其第 16 項比對之標的 SYS1 §2.6.3 第 2 句正確且已 PASS），
   移走會使第 16 項對其 N/A，**失去現有保護**。

### 2.4 三條出路（**未擇一，待裁**）

| 出路 | 內容 | 代價 |
|---|---|---|
| 甲 | CONT 表增一欄 `fragment_kind`：`verbatim`（純片段，適用子串驗證）／`edited`（037 改寫過，**第二層改驗其與 SYS1 之首段對齊**：037 片段去尾標點後為 SYS1 句之前綴）| 判準仍為零閾值；但需區分二類，且「改寫」之形態未必只有末尾 |
| 乙 | 第二層改驗**前綴關係**（037 片段去尾標點 ⊆ SYS1 句之開頭），對四筆皆適用 | 單一判準、零閾值；惟對「中段被改寫」者無效 |
| 丙 | 第二層降為**警示**（列出不符者但不 FAIL），內容驗證交人工 | 最省事，但風險 2（表之內容錯）就沒有機器承載者 —— **等於這一層白做** |

**我的判斷是乙**：實測四筆中 `013-02` 之改寫恰在句尾（`, and…` → `.`），
而**續行型之切點本來就在句尾**（前半／續行），故前綴關係是這類形態之
自然不變量。甲之 `fragment_kind` 需要人工標註，而標註本身無檢查 ——
那是把 CONT 表之原始問題（靠記性）搬到新欄位上。

**但這是 Tier 2，我不自裁。**

### 2.5 第一層之診斷跑（其二個 self-test 皆通過）

第二層仍為阻斷狀態，**未繞過**。以下為僅跑第一層之診斷，明確標示：

**全 117 leaf 之 037 掃描：候選 16 筆**

| 特徵 | leaf | 批次 |
|---|---|---|
| `continuation` | `012-03`、`013-03` | 第 1 批（已在 CONT）|
| **`reference`** | `019-02` | **第 3 批** |
| **`reference`** | `038-02`、`038-03` | **第 4 批** |
| **`reference`** | `058-02`、`064-02` | **第 5 批** |
| `short` 單獨命中 | `001-03`／`004`／`005`／`007-01`／`013-04`／`018`／`025-01`／`028-02`／`046-01` | 各批 |

**兩項要緊之結論**：

1. **`reference` 型五筆全在未生成之批次（3／4／5）** ——
   **無一落於已收斂之 pilot／第 1 批／第 2 批**。
   20a §2.3 之「提前現形、一次做完」在此兌現：
   第 4、5 批之四筆在其勘查之前就已浮現。
2. **`short` 單獨命中者多為偽陽性** —— 如
   `The tab name for the Controls tab is Controls.`（`004`）、
   `Select a list item by pressing on it.`（`046-01`）——
   **是完整可讀之短句，非片段亦非指涉**。
   `short` 之門檻（60 字元）借自 profile §8，其用途是「短來源之子串保護力弱」，
   **與「是否為片段」是兩件事**。本層借用它會過度觸發。

> 已收斂批次之 `short` 命中（`001-03`／`004`／`005`／`028-02`／`046-01`）
> **不構成回溯需求** —— 其上半皆為完整句，第 16 項對其 N/A 屬正確。

---

## 3. T111 —— DR-VC9 第三問

已加入 `VC-014` 之 `(See table above)`，並逐字載明三項 SYS1 實測
（§3 僅章標題無表／唯一之表在 §3.9 之後方／§2.4 之表與 Controls 無關），
其措辭刻意寫成「**查過了、沒有**」而非「我們沒找到」。

阻斷範圍已分列：**(二) 阻斷三筆 b 段**；**(一)(三) 不阻斷**，
三筆帶 PENDING 生成且字串各異。

---

## 4. T112／T113 —— 未執行

依「先檢查、後登記、登記後以檢查證明通過」之順序，**T110 未通過即不進 T112**。

且第 3 批之 `019-02` 其上半取 SYS1 整段之處置**依賴 CONT 登記**（T110-3），
該登記在第二層判準定案前不宜落。

---

## 5. 待你裁

1. **§2.4 之第二層判準**（甲／乙／丙，我建議乙）。定案後：
   T110-2~5 續跑、`019-02` 登記、第 3 批生成。
2. **第一層之 `short` 特徵是否保留** —— 其偽陽性率高（16 筆中 9 筆單靠它命中，
   全為完整短句）。移除則 `019-02`／`058-02` 仍由 `reference` 命中，
   **不影響偵測力**；保留則每批需人工排除數筆。
3. 同批 A（六項）、DR-VC3、DR-VC9(一)（Tier 3）。

---

## 6. 量測條件揭露（R-G8）

### 第一層之偽陰性（20a §2.1 末段之實證）

三特徵之外的形態偵測不到。**`013-02` 即活例**：其首字為大寫 `F`、
非代名詞起首、長度 131 字元 —— **三特徵皆不命中**，
它在 CONT 表是因為第 1 批勘查之 SYS1 對照抓到，**不是特徵偵測**。

**故 SYS1 對照（勘查 (d) 項）不因本層而免除** —— 本層是底網，不是替代。
20a §2.3 已預告此點，本輪以實測證實。

### 第二層之判準（現行，待裁）

子串比對，零閾值。**其前提為「037 之片段未經改寫」** ——
該前提於 `013-02` 不成立（§2.2）。**這不是量測誤差，是判準之適用範圍問題。**

### `short` 門檻之借用

60 字元借自 profile §8。**二者用途不同**：profile §8 之 60 是
「子串判準之保護力界線」，本層之 60 是「疑似片段」——
**同一個數字承擔二個不同的判準，是一個未被言明的耦合**。
若 profile §8 日後改門檻，本層會跟著變而無人察覺。
**建議二者脫鉤**（本輪未改，一併待裁）。
