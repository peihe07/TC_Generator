# 上繳包 17 —— Vehicle Category：R-VC23 落地 ＋ 第 2 批勘查（T95–T98）

- 日期：2026-08-26
- 對應下放：`docs/handoff/17_batch2_survey.md`
  （SHA256 `fce62945dd651e184b5b5121a600cfb79710d467354507ef10f46c69616a6245`，232 行）
- **結論：T95–T98 四項全數完成。勘查完成，未生成任何 TC。**
- 未寫回工作簿、未進行任何 git 操作。

---

## 0. 一頁摘要

| # | 任務 | 結果 |
|---|---|---|
| T95 | 機讀行格式固定 | ✅ profile §5.1.2，五項固定 |
| T96 | 第 2 批勘查 a–g | ✅ 五項風險**四確認一歸因於我方**；記法比下放包所列**多三種** |
| T97 | R-VC23 抄錄 ＋ R-VC19 加註 | ✅ 逐字（1,540 B／29 行／`256e156f8b6b0441`）|
| T98 | 第 7b 項逐 token 重寫 | ✅ 雙向實測；**過程中修掉一個潛伏當機** |

**三件請你先看**：
1. **§4.1(1) 之截斷是你那邊的量測問題，不是 037 之異常** —— 二筆在 037 內
   皆為完整句（273／247 字元，以句號結尾）。見 §2.1。
2. **來源記法實際有五種，下放包列了二種** —— 另三種為直雙引號、直單引號、
   彎單引號。且 `051-03`／`053` **同一字串在二欄用不同記法**。見 §2.5。
3. **第 8 項硬編 `TCS[10]`**，批次少於 11 筆即當機。修掉了。見 §5.3。

---

## 1. T95 —— 機讀行格式（profile §5.1.2）

五項固定：前綴逐字 `values(`、參數名與模板之洞逐字相同、
分隔符 ` = `、值以 ` | ` 分隔且值本身不得含 `|`、一常數一行不續行。

理由逐字記入：**若格式不固定，日後另一個帶參數常數會出現第二種寫法 ——
分歧從值域移到了格式**，而 §5.2(a) 之「二處不得分歧」是對值域說的，
攔不到格式分歧。

---

## 2. T96 —— 第 2 批勘查（30 leaf）

### 2.1 §4.1(1) 截斷 —— **歸因：讀取端，非 037**

| leaf | 037 `Description` 長度 | 結尾 | 判 |
|---|---|---|---|
| `VC-046-05` | **273 字元** | `…switches to next level within settings or changes the settings.` | **完整** |
| `VC-051-03` | **247 字元** | `…taken back to the settings screen that was rejected.` | **完整** |

**二筆在 037 儲存格內皆為完整句，以句號結尾。**
截斷發生於分析層之讀取／顯示端。

**不登記 A** —— 037 無異常。

> 這與 PLAYBOOK §7.2 所記之第六件（對截斷至 74 字元之顯示字串跑正則）
> **同源，且是第二次**。§7.2 之措辭為「複核腳本之掃描標的不得為顯示用之
> 截斷字串」—— 本次不是跑正則而是下裁，但根因同一：**在截斷後的字串上
> 作判斷**。建議 §7.2 之範圍由「掃描標的」擴為「任何判讀之輸入」，
> **但那是你的 PLAYBOOK 條目，我不代改。**

### 2.2 §4.1(2) `VC-048-02` —— **確認為拆分候選**

```
Confirmation tone shall be played whenever a settings value is modified,
with the exception these settings: Clock Hours / Minutes, Audio Settings,
Phone/Bluetooth Settings.
```

§8.3 壓力測試：**若只有部分行為失效，判準是否仍明確？**
—— 「一般設定無確認音」與「例外設定卻有確認音」為二個獨立失效，
單一 TC 下皆判 fail 而**分不出是哪一個**。且 IN §7 明文要求
列舉之支援項須配對至少一個不支援之負向 TC。

**拆為 2 筆**：(a) 一般設定變更 → 有確認音；(b) 三類例外設定變更 → 無確認音。

> 本筆即 FROP = `Audio Management` 之唯一一筆。**表 A 之揭露須標其
> 落於第 2 批，且拆後為 2 筆 TC 而非 1 筆。**

### 2.3 §4.1(3) 500 ms —— 確認 spec-sourced，且為 sibling

`VC-049`（200 ms／Clock 等五類）與 `VC-050`（500 ms／Brightness）
皆逐字載於 037，可用。

**括號下半以適用對象區分**（Clock 等五類 vs Brightness），
**不以速率區分** —— 採認下放包之判斷：速率是後果不是情境。

**500 ms 之 boundary 不拆為獨立 TC**，改於同一 TC 內以步驟涵蓋
（短於門檻之按壓不連續增減／超過門檻則連續）。理由：
二者為同一規則之二側，拆開後各自之 ER 皆不完整。

### 2.4 §4.1(4) `HMI Settings List` —— **確載其內容，不需 PENDING**

實測該素材（`inputs/`，已在手）：

| 分頁 | 列數 | 內容 |
|---|---|---|
| `Settings` | 1,015 | `List Item` 欄；**頂層 category 36 個**（`n. Name` 形態）；**編號 list item 318 筆** |
| `Brand-Specific Names` | 1,001 | category → 各品牌名 |

- `VC-040`／`VC-041` 所需之 **category 層級結構：存在**（36 個頂層 category）
- `VC-044` 所需之 **list item 順序：存在**（列序即順序，且帶編號）

**三筆皆不需 `PENDING`。** 與第 1 批 `VC-011` 之 `the table`
（未具名）不同 —— 本三筆明確指名該素材，且該素材確載其內容。

> 附記：該表之 category 編號有重複與跳序（二個 `1.`、
> `4. Units` 出現在 `3. Entertainment Screens` 之前）。
> **為來源自身之編號，不影響本三筆之可測性**（其驗的是「取自該表」，
> 非「編號連續」）。僅回報。

### 2.5 §4.1(5) 三筆帶圖 —— 文字層殘餘**足以構成可測內容**

| leaf | 圖 | 文字層殘餘 | 判 |
|---|---|---|---|
| `VC-040` | image13 | `If the Settings List layout includes a Left Menu Rail, the HMI Settings List category should be the titles of the Left Menu Rail.` | **足夠** |
| `VC-041` | image14 | `If the Settings List layout does not include a Left Menu Rail, the HMI Settings List categories should be the first level of the Settings.` | **足夠** |
| `VC-053` | image15 | `If a Setting has a Definition, an "info icon"（“i”）will appear on the line next to the setting.` | **足夠** |

三者之圖皆為輔助說明，**照常生成，不比照 `VC-013-04`**。

### 2.6 ⚠ 來源記法：實際五種，下放包列了二種

T96(e) 全批掃描（30 leaf 之 `Title` ＋ `Description`）：

| 記法 | leaf 數 | leaf |
|---|---|---|
| 彎雙引號 `“…”` | 3 | `042-01`、`043`、`053` |
| **直雙引號 `"…"`** | 2 | `047-03`（`"down"`）、`047-04`（`"releases"`／`"released"`）|
| **直單引號 `'…'`** | 2 | `051-03`、`053` |
| 方括號 `[…]` | 1 | `043`（`[Example: Language (English) >]`）|
| **彎單引號 `‘…’`** | 1 | `051-03`（`‘Setting not saved, please try again’`）|

**下放包 §3.1 列了彎雙引號與方括號二種；另三種為本掃描所得。**

#### 一項新形態 —— **同一字串在二欄用不同記法**

| leaf | `Description` | `Title` |
|---|---|---|
| `051-03` | `‘Setting not saved, please try again’`（彎單）| `'Setting not saved, please try again'`（直單）|
| `053` | `“info icon”`／`“i”`（彎雙）| `'i'`（直單）|

**A-VC10 記的是二欄之資訊量不對稱；這是二欄之記法不對稱。**

其後果落在 R-VC23(c)：**取哪一欄作 verbatim 上半，就必須保留哪一欄之記法**
—— 二者不可混用。生成時須逐筆決定取哪一欄並保持一致。
**未立新 A**（其為 A-VC10 之同一根源：Title 為 037 作者之改寫），
但生成時之風險已記於此。

### 2.7 T96(f) 拆分候選 —— 2 筆需拆，共 +2 TC

逐筆施 §8.3 壓力測試：

| leaf | 判 | 依據 |
|---|---|---|
| **`048-02`** | **拆 2** | §2.2 —— 正向 ＋ 三類例外，二個獨立失效；IN §7 之負向配對 |
| **`046-05`** | **拆 2** | 二個獨立行為：(i) 旋鈕轉動／方向鍵**移動游標**、(ii) 按下旋鈕**選取並進入次層**。前者成後者敗時，單一 TC 判準不明 |
| `047-03` | 不拆 | 「按下進入 down state → 轉動增減」為**一個互動序列**，拆開後前半之 ER 不完整 |
| `049`／`050` | 不拆 | 500 ms 之二側為同一規則之二面（§2.3）|
| `042-01` | 不拆 | 「截斷 → 改箭號 → 選項下推」為一個後果鏈 |
| 其餘 24 筆 | 不拆 | 各為單一規則 |

### 2.8 T96(g) TC 數預估

**30 leaf → 32 TC**（+2 由 §2.7 之二筆拆分）。

- 需 `PENDING` 者：**0 筆**（§2.4 已解 `HMI Settings List` 之疑）
- 需保留（b 段）者：**0 筆** —— 30 筆之地位皆無疑
- **第 2 批無 b 段**，R-VC22 之分段不適用

> **第 1 批之預估「無需拆分」事後成立；本批預估 +2，不得因前批而鬆懈** ——
> 下放包 §4.1(2) 之提醒已照辦，逐筆施壓而非沿用前批之結論。

---

## 3. T97 —— R-VC23 與 R-VC19 加註

R-VC23 抄入 `RULINGS.md`，byte-level diff 逐字一致
（1,540 B／29 行／`256e156f8b6b0441`）。

**R-VC19 原文未刪**，於其 fence 外加註：(a) 已擴充、(d) 已作廢、
(b)(c) 不變且更形吃重、(c) 為通則化後之唯一實質保護且須逐 token 不得抽樣。

---

## 4. T98 —— 第 7b 項逐 token 重寫

### 4.1 新判準

token 樣式表為**開放式**（法式／彎雙／彎單／方括號／角括號／直單），
逐 token 比對其是否逐字出現於該 leaf 之 037 `Title`／`Description`，
**或 SYS1 之對應文字**（R-VC7；續行型 leaf 之上半取自 SYS1，其 token 亦須可溯）。

### 4.2 雙向實測

**(a) 反向 —— 注入來源沒有的方括號 → FAIL**

```
 7b  ...  **FAIL**
     保留 token 7 個；未對上來源 1 個 [('SWE1-HMI-VC-026-01', '[Example: not in source]')]
exit: 1
```

**(b) 已知標的 —— `VC-043` 之真實 `[Example: Language (English) >]` → 不觸發**

```
 7b  ...  PASS
     保留 token 2 個；未對上來源 0 個
```

該探針之 2 個 token 為 `“>”` 與 `[Example: Language (English) >]`，
**皆對得上 037 之 `Description`**。與下放包 §3.3 之預期一致。

**(c) 回歸** —— pilot 6 token／第 1 批 0 token，皆 PASS，二批仍 19/0。

### 4.3 ⚠ 過程中修掉一個潛伏當機

(b) 之探針初跑 `IndexError`。追因：第 8 項寫

```python
exact = pat in TCS[10]["test_procedure"]     # 硬編位置索引
```

**`TCS[10]` 是硬編之位置索引** —— 只在該批恰有 ≥11 筆、
且 `VC-033-01` 恰在第 11 位時才對。pilot（12 筆）與第 1 批（22 筆）
恰好都滿足，**故一直沒現形**；1 筆之探針一跑就當。

改為依 `leaf_id` 查，位置無關。

> **這一件不是判準錯，是實作錯，且是潛伏的** ——
> 前八件皆在判準層（太嚴／太鬆／輸入截斷），本件在資料存取層。
> 它之所以被抓到，是因為 PLAYBOOK §7.1(b) 要我拿一個**單筆探針**去跑；
> 若 (b) 只用既有批次驗，它會繼續潛伏到某個 10 筆以下的批次。

---

## 5. 待你裁

1. **PLAYBOOK §7.2 之範圍**（§2.1）—— 由「掃描標的」擴為「任何判讀之輸入」。
   我不代改你的條目。
2. **第 2 批之生成授權** —— 30 leaf → 32 TC，無 PENDING、無 b 段。
3. 同批 A（六項）、DR-VC3、DR-VC9(一) 之發送（Tier 3）。

---

## 6. 量測條件揭露（R-G8）

- **§2.1 之歸因**為讀 037 儲存格之完整值並量其長度與結尾字元，
  **非推論**。二筆皆 `endswith('.')` 為 True。
- **§2.4 之 category 計數**以正則 `^\d+\.\s+\S` 掃 `List Item` 欄。
  **偽陰性**：不合該形態之 category（如無編號者）不計入。
  其 36 之數僅為「確有層級結構」之佐證，**非該表之權威 category 數**。
- **§2.6 之記法掃描**以八個樣式之正則為之。
  **偽陰性**：樣式表外之記法（如 `「…」`、`｢…｣`）掃不到。
  R-VC23 通則化後，**掃不到即等於未受保護** —— 第 7b 項只驗它認得的 token。
  這是通則化之代價：條文不再限制記法，但檢查器仍受其樣式表所限。
- **§2.7 之拆分判定為人工施壓測試**，無機械證據。
  其判準（「部分行為失效時判準是否仍明確」）本身需語意判斷。
