# 上繳包 31 —— Vehicle Category：8 項判定 ＋ owner 修正（T160–T163）

- 日期：2026-08-27
- 對應下放：`docs/handoff/31_xref_verdict.md`
  （SHA256 `4b70e02ce46c49cde33fe77ddc37ec45ee9fb98b50ea8b6a37257433fb245f8a`，175 行）
- **結論：T160–T163 全數完成。八項中 **一項為真牴觸，且時效緊迫**。**
- 未修改任何台帳之實質內容、未寫回、未進行任何 git 操作。

---

## 0. ⚠⚠ 請先看這一段 —— DR-VC3 正在發送，而它帶著一句已被推翻的話

**`DATA_REQUESTS.md` 第 71–76 行，DR-VC3 之「T17 之實質發現（發出前須知）」**：

> **T17 之實質發現（發出前須知）**：18 節中 **§15 與 §10.1／10.2 之關鍵內容
> 在 repo 之權威素材中不可得** —— 其為圖檔，SYS1 `Description` 僅存
> `(image: imageNN.png)` 佔位。故本 DR 對該三節之提問**應改為**
> 「該節內容僅存於圖，SYS1 匯出未帶文字」，而非引述任何摘要文字。

**該敘述對 §15 成立，對 §10.1／§10.2 不成立**（A-VC20 已證，R-VC27 已裁）：
該二節之 SYS1 `Description` 含
`All four Aux switches (Aux 1 Aux 2, Aux 3, and Aux 4) can be used simultaneously`
—— 一句可讀、可測之需求敘述。

**而這一段標著「發出前須知」，且明文指示本 DR 之措辭應怎麼寫。**
**DR-VC3 在你正在發送的十筆之中。**

若照此發出，上游會被告知那二節沒有文字 —— 於是
**(a)** 我們去要一份已經有的東西、
**(b)** 真正的缺口（圖內內容）被錯誤地擴大範圍描述。

**執行層未修改**（T160 明文「只判定不修改」，真牴觸之處置由分析層另裁）。
**但若 DR-VC3 已發出或即將發出，這一段須先攔下。**

> **這也更正我自己**：上繳包 29 §4.3 我把 `§10.1`／`§10.2` 標為
> 「**已知牴觸（A-VC20），已由 R-VC27 解**」。**「已解」是錯的** ——
> R-VC27 修的是 R-VC12 二(a) 與表 B，**DR-VC3 這一段從未被碰過**。
> 我當時是憑「R-VC27 已裁」推定其涵蓋範圍，**沒有回去讀 DR-VC3**。

---

## 1. T162 —— owner 修正（先做，因 `2`／`4` 之判定依賴它）

### 1.1 ⚠ 更正上繳包 30 §4(3) 之成因診斷

包 30 我寫：owner 正則「**實測未吃到**，因該行之 `§3.3` 前為
`下放包 29 §3.3` 而 `§` 與數字間無空白、且樣式與全形／半形空白不符」。

**那是錯的。正則一直吃得到。** 實測 `data/ledger_xref.tsv`：

```
3.3  下放包  ANOMALIES.md  1112
3.3  下放包  ANOMALIES.md  1114
```

**`owner` 欄正確標了 `下放包`** —— **是標了而未用**：
上一版只把 owner 記進欄位，從未據以剔除，於是噪音照樣進候選表。

**這是同一形態之第三次**：看到症狀（噪音還在）就推因（正則壞了），
**沒去驗那個因**。前二次見 PLAYBOOK §7.4（x14 DV 之開閉標籤、圖片之副檔名）
—— 而 §7.4 正是我上一包剛寫進去的。

### 1.2 修正之範圍

**只動一個變數**（下放包 §3.2）：`owner` 命中之 `section` 提及**不記錄**。
`NEG`／`POS` 語彙表與第一層剔除類別**一律未動**。

self-test 增為**五**個斷言：

```
ledger_xref —— self-test 前置（PLAYBOOK §7.1.1）
  self-test 1  (b) §10.1 應同時出現於 DATA_REQUESTS 與 RULINGS  PASS  命中 23 處，檔 ['ANOMALIES.md', 'DATA_REQUESTS.md', 'RULINGS.md', 'data/tableB_draft.md', 'docs/REVISIONS.md']
  self-test 2  (a) 反向 §99.99 應零命中                        PASS  命中 0 處
  self-test 3  (b) R-VC12 應跨 ≥2 個台帳                       PASS  檔 ['ANOMALIES.md', 'DATA_REQUESTS.md', 'RULINGS.md', 'data/tableB_draft.md', 'docs/REVISIONS.md', 'docs/runtime/profiles/FW036_R1L_VehicleCategory_Profile.md', 'framework.md']
  self-test 4  (b) 「下放包 29 §3.3」須被判 owner 並剔除      PASS  剔除總數 183
  self-test 5  (a) 反向 裸 §3.3（A-VC5 本文）須保留          PASS  保留 4 處
```

### 1.3 修前修後差異表

| 量 | 修前 | 修後 | 差 |
|---|---|---|---|
| mentions | 1055 | 876 | -179 |
| targets | 182 | 158 | -24 |
| candidates | 107 | 90 | -17 |
| review_t | 61 | 52 | -9 |
| priority_t | 18 | 15 | -3 |

**owner 剔除 181 處**，擁有者分布：
`下放包` 73／`上繳包` 47／`IN` 46／`FO` 7／`canon` 3／`包` 3／`PLAYBOOK` 2。

### 1.4 消失者與新增者

| | 標的 |
|---|---|
| **高優先消失（18 → 15）** | `section:3.3`、`section:4`、`section:6` |
| **高優先新增** | **無** |
| **無解決標記者消失（9 → 8）** | `section:4` |
| **無解決標記者新增** | **無** |

**三者皆為噪音之具體證據**：`§3.3` 之十行中多行來自「下放包 29 §3.3」；
`§4`／`§6` 同型（「包 4」「§6.1」之截段與他文件節號）。

**新增為 0** —— 即**沒有真候選原本被噪音淹沒**。

---

## 2. T160 —— 八項逐筆判定

**母體**：修正後之 `xref_triage_review.tsv` 保留行，**逐行讀原檔全文**
（不以 tsv 之 400 字元截斷欄下判定，PLAYBOOK §7.2）。

| # | 標的 | 判定 | 依據 |
|---|---|---|---|
| 1 | **`§8.3`** | **非牴觸-同一事實之不同面** | `tableB_draft.md:37` **已載其逐字全文**（`A graphic representation of the vehicle status will be present on pop up …`），註 1 記其依 R-VC12 二(c) 補入。NEG 側為註 1 之「於下放包 01 §4.2(b) 之摘要中**漏列**」—— **漏列者為下放包 01 之摘要，補入者為表 B**，二者非同一標的之相反陳述。**條文與產出一致。** |
| 2 | **`§15`** | **真牴觸（見 §0）** | `DATA_REQUESTS.md:71–76`（DR-VC3）現存「§15 **與 §10.1／10.2** 之關鍵內容…僅存 `(image:)` 佔位」與「本 DR 之提問應改為『SYS1 匯出未帶文字』」。**對 §15 成立、對 §10.1／§10.2 不成立**。表 B 與 RULINGS 已依 R-VC27 改，**DR-VC3 未改**。 |
| 3 | **`§16.2.1`** | **非牴觸-同一事實之不同面** | 僅二處：`DATA_REQUESTS.md:65`（18 節「有實質需求內容**而 037 無對應需求**」）與 `tableB_draft.md:50`（逐字內容）。「有內容」與「037 未涵蓋」**是同一件事的兩半**，表 B 記的正是這個。 |
| 4 | **`§16.2.2`** | **非牴觸-同一事實之不同面** | 同上，`DATA_REQUESTS.md:65` ＋ `tableB_draft.md:51`。 |
| 5 | **`R-VC19`** | **非牴觸-同一事實之不同面** | NEG 側為 A-VC15（`lint036.py --profile` 不讀 profile → **R-VC19(c) 無承載者**）；POS 側為 profile §1（「逐字採 R-VC19 (a)–(d)」）。**(c) 無工具**與**(a)–(d) 已抄入**並存不悖 —— 前者說工具、後者說文本。 |
| 6 | **`R-VC4`** | **非牴觸** | 七處全部一致：`spec_reference` **逐字取 037 `HMI Source ID` 欄原值**（RULINGS／DECISIONS／framework／profile 四處同文）。NEG 側 `ANOMALIES.md:242` 為 A-VC6 之 `dict.get` 崩潰敘述（「預設值僅在鍵不存在時生效；R-VC4 明文要求該鍵為 `null`」）—— **那是在解釋崩潰成因，不是對 R-VC4 的相反陳述**。 |
| 7 | **`DR-VC2`** | **非牴觸-時點差異**（惟有一個可讀性風險）| `DECISIONS.md:118` 作「**DR 七筆全未結**（DR-VC1 ~ DR-VC7）」，而現有**十筆**。該行於簽署時正確，DECISIONS 為**時點紀錄**。**惟該節無「截至某日」之標記** —— 今日之讀者會讀到七。**提案**：比照包 25 之表 A 加註，於該節加一行時點標記（**本輪未執行**，T160 只判定）。 |
| 8 | **`§2`** | **非牴觸-噪音** | 四處分別為 `framework.md §2`（RULINGS:775、framework:25）與 `profile §2`（profile:41、135）—— **皆為他文件之節號**。`owner` 之擁有者表現含 `IN`／`FO`／`PLAYBOOK`／`canon`／`下放包`／`上繳包`／`包`，**不含 `framework`／`profile`／`.md` 形態**，故未被剔除。**修正之殘餘**，非牴觸。 |

**`§4` 不在表內** —— 其於 T162 修正後已自高優先消失（§1.4）。

### 2.1 統計

| 判定 | 數 |
|---|---|
| **真牴觸** | **1**（`§15`／DR-VC3）|
| 非牴觸-同一事實之不同面 | 5（`§8.3`／`§16.2.1`／`§16.2.2`／`R-VC19`／`DR-VC2`）|
| 非牴觸 | 1（`R-VC4`）|
| 非牴觸-噪音 | 1（`§2`）|
| 無法判定 | 0 |

---

## 3. T161／T163

- **PLAYBOOK §7.5**（下放包 §4.2 逐字）：條文之某款被作廢／修訂／加註時，
  須讀**該條全文**並複核其餘各款，於加註中記明。
  併記下放包 §4.1 之「為何不做第三判準」與**本節無承載者**。
- **盲區逐字記入二處**：`scripts/ledger_xref.py` 之模組說明、
  **profile §12**（新增）。profile §12 另記**二個判準與制度之衝突**
  （對立語彙 vs R-TM13；跨檔要求 vs 同條目內牴觸）。

> §7.5 之立意在本輪即被印證：**`§15` 這一筆之所以還在，正是因為
> R-VC27 修 R-VC12 二(a) 時沒有回頭掃「還有誰引用了這個前提」。**
> 只是 §7.5 治的是**同條目內**，而 DR-VC3 是**另一個檔**——
> **本輪之真牴觸不在 §7.5 之射程內**，它是被 `ledger_xref` 抓到的。
> 二個補網各管一段，記明。

---

## 4. 量測條件揭露（R-G8）

- **八項判定皆讀原檔全文**，非 tsv 之截斷欄（PLAYBOOK §7.2）。
- **判定之母體限於 `xref_triage_review.tsv` 之保留行** ——
  第一層剔除之 536 處未讀。若某牴觸之二端皆被判為「純編號引用」，本輪看不到。
- **`§2` 之噪音殘餘未修**（owner 表不含 `framework`／`profile`）——
  屬 `ledger_xref` 之再一次改動，下放包 §3.2 明文「一次只動一個變數」，
  **本輪不動**。記為已知殘餘。
- **DR-VC3 之真牴觸為單點發現** —— 我未掃「還有哪些檔引用了
  『§10.1／10.2 僅存圖佔位』這個前提」之全部。已掃者為
  `僅存|只存|未帶文字|不可得` 之樣式，命中 20 行，其中現存而未加註者
  **僅 DR-VC3 之二行**；`RULINGS.md:396`／`400` 已由 R-VC27 加註作廢、
  `docs/REVISIONS.md:81`／`89` 為 REV-07 之歷史紀錄。
  **但樣式為列舉式，同義而不同詞者抓不到。**

---

## 5. 待你裁

1. **⚠ DR-VC3 之「發出前須知」段**（§0）—— 發送中，**時效緊迫**
2. `DECISIONS.md:118` 之時點標記（§2 第 7 項之提案）
3. `§2` 之 owner 殘餘是否修（§4 第 3 點）
4. Tier 3：十筆 DR 之回覆

---

## 6. 進度

**117 leaf 中 112 筆已收斂，TC 累計 120 筆。** 生成側停於 DR-VC3／DR-VC9(二)。
表 A 完成、表 B 草稿（四處待 DR-VC3）、`reasoning` 側檔 120 筆、
TC ID 已裁（R-VC28）、寫回方案丙″ 六項全過、交付前清單五項已建檔。
