# 上繳包 36 —— Vehicle Category：收線

- 日期：2026-08-27
- 對應下放：`docs/handoff/36_closeout.md`
  （SHA256 `f2f65357a3e102cfc70a43c665f1c80374715d913b3d5973b77b8fd21c540e75`，61 行）
- **本包無生成、無寫回。三項記明 ＋ 一項更名。**
- 未進行任何 git 操作。

---

## 1. ⚠ 工作版之寫回**先於授權** —— 記明

下放包 34 §四明文「寫回之授權在下一包，待收斂 ＋ Excel 驗收」；
包 35 未含寫回授權。**我在 Pei 直接指示下寫了。**

Pei 為 Tier 3，其指示即授權 —— **但那不改變一件事**：
**下放包 34 所設之二個前提（收斂、Excel 驗收），第二個當時未過，現在也還沒過。**

結果無害（母本未變、產物在 gitignore 之 `output/`、八項驗收全過），
**授權邊界不因結果無害而失效**。依下放包 36 §二，記於此一句，不立 A。

---

## 2. ⚠ 我造成一個號碼碰撞，已更名

寫回工作版時我把上繳包取名 `36_writeback_working.md` —— 當時無下放包 36。
下放包 36 落檔後，**主號 36 有二個上繳包**。

**已更名為 `35a_writeback_working.md`**（沿 `20a` 之字母後綴慣例：
補充包不佔主號；其內容對應 Pei 之直接指示而非編號下放包，本就不該佔主號）。

**PLAYBOOK §7.7 之二項檢查規制「編號包之寫入」，規制不到「上繳包自行取號」**
—— 已記為該節之已知缺口。二項檢查是給分析層寫下放包用的，
執行層自行為一份無下放包對應之產出取號時，它不在場。

---

## 3. 出貨版改用外科式：採認，且本包不預作

| 版 | 方法 | 狀態 |
|---|---|---|
| 工作版 | 丙″ 產物 | **已產出，不重產**（下放包 36 §1.1）|
| 出貨版 | **外科式**（移植 `display/scripts/write_back_036.py` 之法）| **PENDING 結案後才做，本包不預作** |

**新增之三個驗收量**（丙″ 六項量不到者）自出貨版起併入：
`zip 部件數`、`t="shared"` 之出現數、`xl/media` 之檔數。
本次工作版之實測值為 **52／0／9**，母本為 **48／1401／2**。

**`RESUME_PLAN` 之出貨序第 3 項未改** —— 下放包 36 §四明文
「由下次開機首包更新（本包不遙改）」。

---

## 4. PLAYBOOK §7.3.1 —— 「答案已在 repo 裡」第三次

逐字入檔：

```
方案評估時，先掃他 feature 之 `scripts/` 有無同題之既有實作。
```

併記三次之分布：**前二次在台帳**（`ledger_xref` 已管）、
**本次在跨 feature 之 `scripts/`**（已知盲區之又一段）。

**不造工具、不開任務**（下放包 §1.2 明文）——
`ledger_xref` 之母體為本 feature 之 9 份台帳；擴至跨 feature 之原始碼
是另一個量級的事，其收益未經評估。**本節為紀律，無承載者。**

---

## 5. ⚠ `delivered/MANIFEST.tsv` —— 請你確認有無第二條線

該檔於本輪出現（**僅表頭，零資料列**），**非我所建**。
我未寫入它（其語意為「已交付」，而本輪產出為工作版）。

**下放包 36 §三之提醒逐字轉達**：

> 其出現疑為併行 session —— Pei 收線前請確認無第二條線開著
> （display 事件之教訓：一 feature 一線）。

**本 session 對 `features/vehicle_category/` 之全部寫入皆有紀錄**
（上繳包 27–36）；該檔不在其中。

---

## 6. 現況

```
117 / 117 leaf        TC 126 筆        八批回歸 22 checked / 0 failed ×8
工作版已落地           側檔↔工作簿 PASS      PENDING 14 處
DELIVERY.sha256 ENTRY 001（shasum -c OK）
```

**剩三件全在 Pei**：

1. **Excel 驗收** —— 對 `output/…_working.xlsx`（126 列）跑
   `DELIVERY_CHECKLIST` 第 1／2 項。**不需任何 session 開著。**
2. **git commit**
3. **十筆 DR 之回覆** ＋ `QS Suggestion` 第 4 項之狀態

**下次開機讀序**（下放包 36 §四）：
`docs/RESUME_PLAN.md` → 下放包 36 §一（出貨版方法）→ `RULINGS.md` 尾段。

---

## 7. 量測條件揭露（R-G8）

- **本包未跑任何驗證** —— §6 之數字全部引自上繳包 35／35a 之實測，
  非本輪重測。
- `delivered/MANIFEST.tsv` 之「非我所建」為**否定性判斷**，
  其依據為本 session 之寫入紀錄；**若有未經我手之寫入路徑，該判斷看不到**。
- 更名為 `git mv`，內容未改一字（除檔頭之編號與一段成因說明）。
