# 上繳包 36 —— Vehicle Category：126 筆寫回工作版

- 日期：2026-08-27
- **對應下放：無** —— 本輪為 **Pei 之直接指示**（2026-08-27：「寫回工作版」），
  非編號下放包。**NN 配對於此中斷，記明**（PLAYBOOK §7.7 之二項檢查
  規制編號包，本輪不適用）。
- 前一包：`docs/handoff/35_final_leaf.md`／`docs/upstream/35_final_leaf.md`
- **母本未被寫入**（SHA256 前後實測 `6372fb6be02f48dc` 未變）。
- 未進行任何 git 操作。

---

## 0. 產出

```
output/FM-WI-FSM-036-A01 STLA 測試用例規範與結果_SWQT STLA Test Case
Specification & Result_SWQT_VehicleCategory_20260827_working.xlsx
```

- SHA256 `30a824a34421b834c7d8ab3c6764ba91…`
- 232873 bytes
- **工作版** —— 14 處 PENDING 依 IN §8.4.3 內嵌佔位、表 B 仍為草稿。
  **出貨版仍卡 PENDING 結案**，此門檻不因本輪改變。

---

## 1. ⚠ 一件要先講：Excel 驗收尚未過，而我寫了

下放包 34 §四明文：**寫回之授權…待本包收斂 ＋ Pei 之 Excel 驗收**。
該驗收**仍未回報**（掛五包）。

Pei 於本輪直接指示「寫回工作版」—— 我照做，
**但那一道門沒有因此變成過了**。

**建議：Excel 驗收改對本檔跑，不對 `/tmp` 之探針跑。**
理由：探針為 **3 筆假資料**，本檔為 **126 筆真資料**；
「開啟不報修復」與「`R10:R1411` 下拉可用」在後者上驗才有意義 ——
且 R 欄現在**每一列都有值**（探針只有 3 列）。

已更新 `docs/DELIVERY_CHECKLIST.md` 第 1／2 項之標的為本檔。

---

## 2. 方法與驗收

方法為**丙″**（上繳包 29 §一，六項全過）：openpyxl 寫入 → 自母本注入
`<extLst>` 與 `printerSettings` → 補宣告 `xmlns:xr` → 重打包。

```
步驟 1 —— 寫入 126 列 × 14 欄
步驟 2／3 —— 注入 extLst ＋ printerSettings，補宣告 xmlns:xr
步驟 4 —— 重打包

步驟 5 —— 驗收
 #  項                                          判
--------------------------------------------------------------------------------
 1  x14:dataValidation 1 條                     PASS
    母本 1 → 輸出 1
 2  extLst 1 個                                 PASS
    母本 1 → 輸出 1
 3  標準 dataValidation 3 條                      PASS
    母本 3 → 輸出 3
 4  printerSettings 5 個                        PASS
    母本 5 → 輸出 5
 5  126 列 × 14 欄逐格與 JSON 相符                    PASS
    不符 0 格 無
 6  B 欄公式未覆寫                                   PASS
    B10 = '=IF(ISBLANK($D10),"",ROW()-9)'
 7  第 136 列（末列之後）仍為空                           PASS
    D 欄值 None
 8  TC ID 連續且無重複（R-VC28）                       PASS
    newR1L-VC-001 … newR1L-VC-126；相異 126
--------------------------------------------------------------------------------
母本 SHA256 前 6372fb6be02f48dc → 後 6372fb6be02f48dc  **未變**
8 checked / 0 failed

輸出：output/FM-WI-FSM-036-A01 STLA 測試用例規範與結果_SWQT STLA Test Case Specification & Result_SWQT_VehicleCategory_20260827_working.xlsx  (232873 bytes)
**工作版** —— 14 處 PENDING 內嵌，表 B 仍為草稿；出貨版仍卡 PENDING 結案。
```

**新增三項驗收**（丙″ 原六項之外，本輪針對真資料所加）：

| 項 | 為何加 |
|---|---|
| **B 欄公式未覆寫** | `B10:B1411` 預填 `=IF(ISBLANK($D10),"",ROW()-9)`；覆寫即毀序號欄 |
| **第 136 列仍為空** | 證明沒有多寫一列（off-by-one 之典型） |
| **TC ID 連續且無重複** | R-VC28 之直接驗證 |

---

## 3. TC ID（R-VC28）

`newR1L-VC-001` … `newR1L-VC-126`，**依工作簿列序**連續、無重複。

**列序 ＝ spec section 序**（`2.2` → `16.2`，66 個 section），
同一 section 內依 `leaf_id`、同一 leaf 內依批內原序（拆分筆各佔一號）。

首列 `SWE1-HMI-VC-001-01`（§2.2）、末列 `SWE1-HMI-VC-066`（§16.2）。

---

## 4. ⚠ 側檔對工作簿之驗證 —— 上繳包 28 §4.2 之未機器化項，現已機器化

包 28 §4.2 曾記：

> 側檔與**工作簿**之對應**尚未驗**（工作簿未產出）。現驗的是側檔對六批 JSON。
> 寫回實作時須補「側檔鍵集合 == 工作簿 D 欄 ∪ 拆分序」之驗證 ——
> §2.2 第三項要的是那一個，本輪只能做到前半。

**現已補入 `build_reasoning_sidecar.py --verify`**：

```
對應驗證：鍵 126 個（其中拆分之第 2 筆以上 9 個）；工作簿 已比對；PASS
```

**其價值在於「不同源」**：一邊是六批 JSON、一邊是 xlsx 之**實際儲存格**。
包 28 §7 自陳「側檔自 JSON 推導，故相符對本輪必然成立」——
**這一項不是必然成立的那種。**

工作簿不存在時本項回傳空（非 FAIL）—— 其產出在寫回之後，記明。

**已知之等價前提**：側檔之 `#n` 為**批內序**、工作簿之為**簿內序**，
二者相等係因**同一 leaf 之全部 TC 必在同一批**。
該前提現行成立，**若日後有 leaf 之拆分筆跨批，本項會誤報** —— 記明。

---

## 5. 第二讀者（LibreOffice）—— **其涵蓋範圍比看起來小**

`soffice --headless --convert-to csv` 返回碼 0，套件可被另一實作開啟。

**但其匯出僅第一個分頁（`Cover_old`），非 TC 分頁** ——
產出之 csv 僅 248 bytes。
**故它只證明「套件結構可讀」，完全不證明 126 列之內容。**

126 列之證據是**驗收第 5 項**（openpyxl 重開輸出檔、逐格對 JSON，
126 × 14 = 1764 格，不符 0）。

**Excel 之驗收仍未做**（§1）。

---

## 5.1 ⚠⚠ 兩件實測結果 —— 丙″ 之輸出與母本差得比六項驗收所顯示的多

### (1) 結構實測

| 量 | 母本 | 輸出 |
|---|---|---|
| x14 DV／extLst／classic DV／printerSettings | 1／1／3／5 | **1／1／3／5** ✅ |
| **zip 部件** | 48 | **52**（6 消失／10 新增）|
| **共用公式 `t="shared"`** | **1401** | **0** |
| **`xl/media`** | 2 | **9** |
| 消失者 | — | `calcChain.xml`、`comments1.xml`、`vmlDrawing1.vml`、`sharedStrings.xml`、`image2.jpeg`、`sheet8.xml.rels` |

**共用公式那一項要看清楚**：openpyxl 把 1401 個 `t="shared"` 參照**全部展開為獨立公式**。
B 欄逐列回讀（B10／B11／B135／B1411）**與母本字串相同** ——
**行為保留，結構不同**。丙″ 之六項驗收**量不到這一項**。

### (2) ⚠ `features/display` 已經有一個更保守的寫回法，而它就在同一個 repo 裡

`features/display/scripts/write_back_036.py` 之模組說明逐字：

> **不用 openpyxl 存檔** —— openpyxl 重寫整份工作簿，會丟失
> `x14:dataValidation`、drawing 關聯等其未完整支援之部件。本腳本直接改
> `xl/worksheets/sheetN.xml` 之目標儲存格，其餘部件**逐 byte 原樣重打包**。

其 `DELIVERY.sha256` ENTRY 001 之結構驗證：

> zip 成員 **48 → 48（零增零減）**；classic DV 4 → 4；x14 DV 2 → 2；
> worksheets 9 → 9；drawings 6 → 6；rels 16 → 16 —— **逐項相等**

**而我在上繳包 27 §3.4(g.3) 寫的是**：

> **丙案未實測** —— 其可行性、樣式索引之處理、`sharedStrings` 之增補
> 皆未驗證。本包不主張它可行。

**丙案不但可行，它已經被實作、跑過、寫進另一個 feature 的交付台帳了。**
我沒有去看。

**這是同一種失誤的第三次**：
DR-VC6 之實測佐證欄早就記對了（A-VC20）、R-VC19 之加註早就在了（T168，那次查了）、
**丙案早就有實作了**（本次）。**三次的共同形態：答案已經在 repo 裡，而我沒查。**

`display` 之說明另載二件我未遇到但值得記：
- **B 欄為共用公式之宿主**（`<f t="shared" ref="B11:B74" si="0">`）——
  賦值會毀去宿主使其餘列之 `si` 無定義。我未寫 B 欄，故未觸發；
  **但 openpyxl 之展開等於把整個 shared 機制拆了**，只是拆得「均勻」而未壞。
- **樣式保留**：其外科式改寫保留原 `s=` 屬性與 `<row>` 之屬性。

### (3) 我沒有自行改用外科式

**丙″ 為分析層所裁**（下放包 29 §2.3），且其八項驗收全過、資料正確。
改換方法屬 Tier 2。**本節為資訊，不是提案** ——
惟其資訊足以使該裁定重新被考慮，故列為 §8 之待裁第一項。

---

## 5.2 `DELIVERY.sha256` 補建 —— 本 feature 原本沒有

`output/` 由 `.gitignore` 排除（第 20 行），**產出永遠不進版控**。
而本 feature **沒有 `DELIVERY.sha256`** —— 產出第一份 xlsx 時才發現。

已依 `features/display/DELIVERY.sha256` 之格式補建（其承 privacy 之 R27-1／R27-2），
ENTRY 001 記本次工作版，含上開結構實測。**已 `shasum -c` 驗證 OK。**

**其旗標之代價逐字承接**：`--ignore-missing` 使台帳
**無法偵測「產出被刪除」** —— 台帳綠燈不等於產出俱在。

> 另：`delivered/MANIFEST.tsv` 於本輪出現（僅表頭），**非我所建** ——
> 疑為併行 session。本輪**未寫入該檔**：其欄位語意為「已交付」，
> 而本輪產出為**工作版**，寫入會誤述其狀態。

---

## 6. 量測條件揭露（R-G8）

- **母本只做 `read_bytes()` 與唯讀載入**，SHA256 前後實測未變。
- **中間檔已刪**（`_step1.xlsx`／`_step1_written.xlsx`）——
  `output/` 只留最終檔。
- **驗收第 5 項之比對為 openpyxl 讀回**；
  **openpyxl 讀不到的東西（如 x14 DV）不在該項之範圍** ——
  x14 由第 1 項以 XML 計數驗。
- **未驗**：Excel 之開啟行為、下拉之**可用性**、版面目視、欄位對位之語意
  —— 皆為 `DELIVERY_CHECKLIST` 之人工項。
- **`(image:` 樣式之限制不變**（上繳包 35 §7）：若匯出另有他種插圖標記，
  §9.3.1 之剝除看不到。

---

## 7. 現況

```
117 / 117 leaf    TC 126 筆    八批回歸 22 checked / 0 failed ×8
工作版已產出      側檔↔工作簿 PASS      PENDING 14 處
```

**全案剩二道門**：

1. **Excel 人工驗收**（Pei）—— 改對本檔跑
2. **PENDING 14 處結案**（DR-VC10 8／DR-VC9 4／DR-VC1 1／DR-VC8 1）
   ＋ 表 B 定稿（DR-VC3）→ 出貨版

**十筆 DR 未結。**
