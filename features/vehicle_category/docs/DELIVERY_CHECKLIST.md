# 交付前人工驗收清單 —— Vehicle Category

> **本檔為清單，非執行紀錄。** 建於下放包 30 §三（T159）——
> DR 發送中、交付本尚不可產出，先列出**交付前必須人工做而機器不能代**的事，
> 免得屆時臨時想。
>
> **本輪只建檔，未執行任何一項。**

- 建檔：2026-08-27，下放包 30 §三
- 交付本之產出方案：**丙″**（上繳包 29 §一，六項全過）

---

## 五項

| # | 項 | 誰做 | 為何機器不能代 | 狀態 |
|---|---|---|---|---|
| 1 | **以 Excel 開啟丙″ 之輸出，確認不報修復** | Pei（或有 Excel 之環境）| LibreOffice headless 返回碼 0 **不等於** Excel；GUI 應用程式不由執行層啟動 | ☐ 未執行 |
| 2 | **確認 `R10:R1411` 之設計方法下拉在 Excel 中可下拉、選項正確** | Pei | x14 DV 之**存在**（實測 1 條）與其**可用性**是兩件事 | ☐ 未執行 |
| 3 | **版面目視**：橫幅圖位置、凍結窗格、列高 | Pei | 無基準可自動比對 | ☐ 未執行 |
| 4 | **抽樣 5 筆 TC 之欄位對位**（D／E／F／G／H…）| Pei | 逐格相符已驗，但**「填對欄」與「填對意思」不同** | ☐ 未執行 |
| 5 | **7 處 PENDING 之最終處置確認** | Pei（Tier 3）| IN §8.4.3：含 PENDING **不得出貨**，須結案或裁定降轉 NA | ☐ 未執行 |

---

## 各項之現有材料

### 第 1／2 項 —— 丙″ 之輸出

- 產出方式：`scripts/probe_writeback_c2.py`（**目前為 3 筆假資料之可行性驗證**，
  非交付本）。
- 已驗六項：x14 DV 1 條、extLst 1 個、標準 DV 3 條、printerSettings 5 個、
  資料 14 欄逐格相符、可被 openpyxl 開啟。
- **未驗**：Excel 開啟之行為；下拉之**可用性**。

### 第 3 項 —— 版面

- 母本實測（上繳包 27 §3.1／包 29 §1.3）：
  **凍結窗格無**、條件式格式無、資料區合併儲存格無；
  資料格樣式已預設（`wrap_text`、垂直置中、細框線）；
  TC 分頁之橫幅圖錨於 `twoCellAnchor` `row 0`／`col 1–2`（820×121）。
- 丙″ 之輸出**未動 media 與 drawing**，但 openpyxl 會把 2 圖複製為 9 檔
  （**內容逐位元組相同**，見包 29 §1.4）。

### 第 4 項 —— 欄位對位

- 映射表見上繳包 27 §3.2：**14 欄有來源、13 欄無**。
- `C`（Polarion ID）**不在 TC JSON 內**，須自 `data/recon_leaf_to_section.tsv` 帶入
  —— **抽樣時請特別看這一欄**。
- `E`（TestRail ID）、`O`、`Q`、`T`–`Z`、`AA`、`AB`–`AH` 無來源，應留空。
- `F`（Test Case ID）依 **R-VC28**：`newR1L-VC-001` … `newR1L-VC-120`。

### 第 5 項 —— 7 處 PENDING

全部為第 5 批，皆指向 **DR-VC10**：

| leaf | 位置 | 標記 |
|---|---|---|
| `061` | Procedure | `PENDING: DR-VC10 Software Updates entry path in Key Off` |
| `062-01` | ER | `PENDING: DR-VC10 PU0091 popup string` |
| `062-02`（×2，拆分）| Procedure | 同上 |
| `063-01` | ER | 同上 |
| `063-02`（×2，拆分）| Procedure | 同上 |

**另有既有批次之 PENDING**（收斂第 8b 項之 `pending_scope` 所載）：
pilot 1 處（DR-VC8）、第 1 批 2 處（DR-VC9）、第 3 批 2 處（DR-VC9／DR-VC1）。
**全簿合計 12 處，非 7 處** —— 7 為第 5 批之數。
**第 5 項之驗收母體應為全簿 12 處。**

---

## ⚠ 本清單之限制

- **五項為下放包 30 §三所列**，執行層未增刪。
  若交付前另有人工項（如與他 feature 之交付本並置檢查），**本清單看不到**。
- 各項之「狀態」欄須於執行時逐項更新；**本檔不自動追蹤**。
- 第 5 項之母體經執行層更正為 **12 處**（見上），
  下放包原文作 7 處 —— 該數為第 5 批之數，非全簿。
