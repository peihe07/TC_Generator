# 下放包 27 —— Vehicle Category：出貨門檻二表 ＋ Phase 6 前置勘查

- 日期：2026-08-27
- 方向：分析層（Claude Project）→ 執行層（Claude Code）
- 對應上繳：`features/vehicle_category/docs/upstream/27_deliverable_prep.md`
- 前一包：`docs/handoff/26_batch5_close.md`
- **NN 檢查**：寫入前已 `list_directory`，`docs/handoff/` 止於 `26_`，無碰撞。
- **第 5 批 21 筆收斂確認（22/0，六批回歸全綠）。生成側停於 112/117。**
- **本包授權不倚賴 DR 之尾段前置工作。不授權寫回工作簿。**

---

## 一、上繳包 26 之覆核

**核可。** 二項具名：

1. **第 18 項之回溯實測 —— 抓到的是歷史上真的存在過的那一筆。**
   §1.2 對「移除 8b」之母體回跑得
   `第8項 引用不存在之「第8b項」`。self-test 用的是構造標的（插入第 99 項），
   而這一步用的是**真實發生過的錯誤**。
   **且 §8 誠實標明它是解析後移除、非真的刪腳本再跑，並主張其等價** ——
   等價性主張與同一次執行之別，標得對。
2. **§3 之自我觀察比該加註本身更有價值**：
   > 「那個理由是對的，但它是從結果反推的 —— 我當時沒有一個判準能說明
   > 為什麼點火狀態可循環，而彈窗不行。」

   **結論對而依據不足，是比結論錯更難自己發現的狀態** ——
   因為它不會被任何檢查叫住。能自己指出來，是這條線上少見的事。

---

## 二、尾段之裁定：**做二表 ＋ Phase 6 前置**

採認 §7.2 之判斷。**生成側停了不等於這條路停了** ——
表 A／表 B 為 R-VC3 所裁之出貨門檻，其編製不倚賴 DR-VC3／DR-VC9(二)。

### 2.1 表 A（FROP 跨域揭露）—— 完全不倚賴 DR

**承 REV-14，以 leaf 母體之實測編製，不得沿用下放包 24 §3.1 之敘述。**

已知（上繳包 24 §3.1 實測，須重測確認）：

```
章 13 之 16 leaf ＝ Power Management 12 ＋ Vehicle Settings 4
  （VS 四筆為 062-01／062-02／063-01／063-02）
另 FROP = Audio Management 1 筆（048-02，§12.3.2，第 2 批）
```

**編製要求**：

| 項 | 要求 |
|---|---|
| 母體 | **117 leaf**（R-VC15：逐項標母體）|
| 內容 | 每筆跨域 leaf 之 req_id、section、FROP 值、所屬 Test Set、所屬批次、其 TC 筆數 |
| 拆分 | 一 leaf 對多 TC 者逐筆列其 TC（`062-02`／`063-02` 各 2）|
| 對照 | **另附 145 列母體之 FROP 分布**，並明文二母體之差異（R-VC16(e) 之加註即為此而設）|
| 出處 | 037 第 8 欄逐列讀，非抽樣；方法與範圍揭露 |

### 2.2 表 B（覆蓋落差揭露）—— 結構與清單可先備，**措辭待 DR-VC3**

17 節之清單、每節之 SYS1 `Description` 引錄、其所屬章 —— **皆可先備**。

**待 DR-VC3 者只有三處**：
- 章 8／9（Cabrio 本體 7 節）之最終處置（「應補」則另立 `Cabrio Rooftop`，
  R-VC16(c)）
- 14.2／§15（`Brake Service` 之待補節）
- 16.2.1／16.2.2（`Cabrio Widget` 之待補節）

**先備之拘束**：
- 三節（§15／§10.1／§10.2）之「內容」欄一律書
  **「該節內容僅存於圖，SYS1 匯出未帶文字」**，不得寫入任何摘要文字
  （R-VC12 二(a)）
- §8.3 之內容補入 SYS1 `Description` 之該句（R-VC12 二(c)）
- 其餘 13 節之摘要**其效力僅及於「與 SYS1 相符」，非「與規格原件相符」**
  （R-VC12 二(d)，FO §3 Mode A 之盲點）—— 該限制須寫在表上，不是只寫在上繳包
- **標為草稿**，檔名或表頭明示其最終措辭待 DR-VC3

### 2.3 Phase 6 前置勘查 —— **只勘查，不寫回**

036 母本已在 `inputs/`
（`FM-WI-FSM-036-A01 STLA 測試用例規範與結果…_20260817_ext.xlsx`）。

| # | 勘查項 |
|---|---|
| a | 母本之分頁結構、各分頁之用途、資料列起始列號 |
| b | **欄位映射**：TC JSON 之 10 個 key ＋ `test_item`／`test_group`／`test_set` 對應母本之哪些欄；**逐欄列出，無對應者標明** |
| c | **既有內容之狀態**：母本現有幾列資料、是否為空模板、若有既有 TC 其來源為何 |
| d | **下拉／驗證清單**：哪些欄有資料驗證（data validation）、其清單來源（同表 range／外部 range／自訂）—— **此為寫回之最大風險，PM 線曾因下拉毀損而需外科式修改** |
| e | 合併儲存格、凍結窗格、條件式格式之分布 |
| f | TC ID 之既有形態（`{project}-{abbr}-{NNN}`，IN §10.3）與其現有最大序號 |
| g | 寫回方式之候選與各自風險（`openpyxl` 全量重寫／逐格寫入／XML 外科式修改），**只列不選** |

**勘查後停，回報，不寫回、不修改母本任何一格。**

> (d)(g) 為本項之核心。`openpyxl` 之全量重寫會丟失 data validation
> 與部分格式 —— 這不是假設，是本 repo 他線之既有教訓。
> **先知道母本裡有什麼，再談怎麼寫。**

---

## 三、五筆 DR 之發送清單（供 Pei 一次處理，Tier 3）

| DR／A | 對象 | 問 | 阻斷 |
|---|---|---|---|
| **同批 A（六項）** | **037 作者** | DR-VC2（Source Requirement ID 之來源）、DR-VC7（欄 18 Priority 之判準）、A-VC2（封面 Reviewer／Date）、A-VC10（Title 與 Description 之分工，四面）、DR-VC8（`033-01` 鎖定門檻：`After three` vs `more than three`）、**DR-VC9(二)**（三筆純交叉引用 leaf 之地位：`007-01`／`013-04`／`025-01`）| **b 段 3 筆**（DR-VC9(二)）；`033-01` 之 boundary 拆分（DR-VC8）|
| **DR-VC3** | 規格作者 | 表 B 之 17 節是否應補 | **第 6／7 批 3 筆**；`framework.md` §2 之組數（R-VC16(c)）；表 B 之最終措辭 |
| **DR-VC9(一)** | 規格作者 | PDO Graphics 素材；`VC-014` 之 `(See table above)` 指涉（**已實測 SYS1 §3.1 上方無表**）| `011`／`012-03`／`014` 之 PENDING |
| **DR-VC10** | 規格作者＋彈窗清單維護者 | (一) `PU0091` 之字（規格作 `Feature`／彈窗清單與設定清單作 `Function`，句點有無亦異）；(二) `061` 之進入路徑（**已實測 SYS1 三節與設定清單皆無，清單委派至我方未持有之 Logic and Flow**）| 第 5 批 7 處 PENDING |
| DR-VC1 | 規格作者 | `VC-021` 之 `PUXXXX`（規格原文即字面 X）| `021` 之 PENDING |

**發送順序之建議（若不能一次全發）**：
1. **DR-VC9(二)** —— 阻斷 b 段 3 筆，且與同批 A 同對象，一封信解決
2. **DR-VC3** —— 阻斷第 6／7 批且會動到 framework 組數，**其影響面最大**
3. 其餘三筆不阻斷批次（皆以 PENDING 內嵌），可後發

> 若 DR-VC3 回覆為「應補」，章 8／9 另立 `Cabrio Rooftop` 將使
> framework 由 8 組變 9 組，**表 A／表 B 與 `verify_partn.py` 皆須重編**。
> 這是尾段唯一會回頭改動已收斂結構的分支 —— 早問早定。

---

## 四、執行層任務

| # | 任務 |
|---|---|
| T142 | 表 A 依 §2.1 編製，置 `docs/` 或 `data/`（形式由執行層定）|
| T143 | 表 B 草稿依 §2.2 更新（承既有 `tableB_draft.md`），標明待 DR-VC3 之三處 |
| T144 | Phase 6 前置勘查（§2.3 之 a–g），**只讀不寫** |

**不在本輪範圍**：寫回工作簿、b 段生成、第 6／7 批、
任何對 036 母本之修改。

---

## 五、上繳包要求

1. T142–T144 逐項結果
2. 表 A 全表；表 B 草稿之異動處
3. Phase 6 勘查表（a–g），**(d) 之 data validation 清單為重點**
4. 量測條件揭露（R-G8）

---

> **十筆 DR 全未結，生成側已停。** 本包之三項做完後，
> 若 DR 仍未回，這條線將只剩「等」—— 屆時再議是否有其他不倚賴 DR 之工作。
