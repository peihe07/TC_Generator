# 下放包 34 —— Vehicle Category：b 段裁定 (a) ＋ 第 6／7 批 ＋ 126 筆之寫回時序

- 日期：2026-08-27
- 方向：分析層（Claude Project）→ 執行層（Claude Code）
- 對應上繳：`features/vehicle_category/docs/upstream/34_tail_batches.md`
- 前一包：`docs/handoff/33_resume_plan.md`
- **NN 檢查**：寫入前已 `list_directory`，`docs/handoff/` 止於 `33_`，無碰撞。
- 裁定：Pei 2026-08-27 —— **b 段三筆採 (a)（裁為需求，生成）**
- **本包授權：b 段 3 筆 ＋ 第 6／7 批 3 筆之生成。不授權寫回（時序見 §四）。**

---

## 一、R-VC30（裁決條文，逐字抄入 `RULINGS.md`）

```
R-VC30（b 段三筆之地位：裁為需求，生成）

（Pei 2026-08-27 裁定，選項 (a)。）

`SWE1-HMI-VC-007-01`／`VC-013-04`／`VC-025-01` 三筆，
**裁為需求 leaf，維持於 117 母體，生成 TC**。

DR-VC9(二) 之查證**維持發送** —— 裁定權在本專案（Tier 3），
查證為資訊補強。上游回覆若與本裁定相反（確認為表頭誤登），
依 `RESUME_PLAN.md` §4.1 之相應分支**由 Pei 再裁**
（屆時之處置：刪 3 筆 TC ＋ R-VC3 母體修訂 117 → 114）。

三筆之驗證定位（防與 sibling 重複，IN §8.2.1）：

  `007-01` —— **全集層**：Vehicle Tab 之全集與 SYS1 §2.4 表相符
              （頁籤數量正確、無表外多餘頁籤）。
              `-02`~`-05` 為**逐列層**（各列之名稱與位置）——
              全集之完整性與逐列之正確性為不同驗證點，不重複。
  `025-01` —— 同構：Controls 按鈕之全集與 SYS1 §3.9 表相符；
              `-02`~`-05` 為逐列層。
  `013-04` —— 直向 Dashboard 版面與 PDO graphics 相符。
              PDO graphics 不在素材（DR-VC9(一)）——
              本筆為**殼 TC**，帶 `PENDING: DR-VC9 PDO graphics`。

即：**三筆中二筆為實 TC**（其表皆在 SYS1，第 1／3 批已用），
僅 `013-04` 為殼。
```

> 這比裁定時預想的好 —— (a) 之成本原估「3 筆殼」，
> 實測 `007-01`／`025-01` 之表**都在手**，全集比對可執行。
> 真殼只有一筆。

---

## 二、b 段 3 筆之生成規格

| 項 | 拘束 |
|---|---|
| 歸批 | `007-01`／`013-04` 併回**第 1 批** JSON、`025-01` 併回**第 3 批** JSON（R-VC22(d)：補生成併入原批）|
| 短來源 | 三筆皆 < 60 字元（profile §8）：上半取**完整句**、**分析層人工複核**（上繳時我逐筆讀）|
| `007-01`／`025-01` | 全集層之 Procedure：進入該頁籤／畫面 → 逐一記錄呈現之項 → 與 SYS1 表之全集比對（§5.6 之 baseline 形態：記錄為 recorded list，ER 比對其與表相符且無多餘）。reasoning 載明與 `-02`~`-05` 之層次分工（§8.2.1）|
| `013-04` | Pre-Condition：`DISPLAY_PORTRAIT`（profile §6）；Procedure：顯示 Dashboard → 記錄版面 → 比對 `PENDING: DR-VC9 PDO graphics`；reasoning 載明其為 R-VC30 所裁之殼、與 `013-01`~`-03`（已測之具體版面規則）之分工 |
| 括號下半 | 與各自批內既有 sibling 以 a＋b 全集重驗兩兩不同（R-VC22(d)）|
| 收斂 | 第 1 批重跑（24 筆）、第 3 批重跑（17 筆），全項；第 15 項之母體隨之更新 |

---

## 三、第 6／7 批：修正置末裁定，勘查＋生成併包

### 3.1 置末裁定之修正（記 REV-15）

下放包 13 §4.2 將二批置末，理由「重審前生成，其結論可能被推翻」。
**逐筆細算後修正**：DR-VC3 回「應補」之後果是**追加**（新組、新 leaf），
不改此 3 筆之 037 內容 —— **TC 本體兩種回覆下皆成立**。
會動者僅 Cabrio Widget 之 reasoning 委派敘述（一句 Revise）
與 Test Set 歸屬之萬一重劃。

**當時未做逐筆分析即整批置末** —— 置末本身無害（也確實等到了現在），
但其理由書寫過寬。記 REV-15。

### 3.2 勘查＋生成併包之條件

3 leaf 小批，格式基線已全部就位（六批收斂）。**勘查仍做（R-VC21 末句），
但不停輪** —— 除非命中停止條件：

```
勘查中發現下列任一 → 停並回報，不生成：
  (a) 拆分候選（R-VC26 之問法逐筆施）
  (b) 新的 CONT 形態（三處置類皆不合）
  (c) 已知 DR 之外的 PENDING 需求
  (d) 來源記法之驗證異常（token 對不上來源列）
  (e) Title 越界之行為主張型（R-VC24）
```

### 3.3 生成規格

| 項 | 拘束 |
|---|---|
| 批檔 | `generated/batch6_brake_service.json`（2 筆）／`generated/batch7_cabrio_widget.json`（1 筆）|
| Test Set | `Brake Service`／`Cabrio Widget`（framework §2 現行邊界）|
| Cabrio Widget 之委派 | reasoning 載明：widget 開啟之 Cabrio 功能屬章 8／9（037 未涵蓋，表 B）；**「若 DR-VC3 回『應補』，本委派之標的改指 `Cabrio Rooftop` 組」** —— 預先寫明兩態，回覆後只需確認不需改寫 |
| Brake Service 之邊界 | 僅測 037 已涵蓋之字面；§14.2／§15 之待補內容不得預納（§8.4.2）|
| 收斂 | 各批全項；六批回歸 → **八批回歸** |

---

## 四、寫回時序：**126 筆齊後一次寫**

TC ID 依 R-VC28 為工作簿**列序**連續編號。先寫 120 筆再插入 6 筆，
**後續 ID 全部位移** —— 故：

```
本包之 6 筆全數收斂 → 126 筆依 section 序排列 → 丙″ 一次寫回（工作版）
```

工作版之定義照前輪：12 處 PENDING 內嵌照 §8.4.3 佔位、表 B 仍為草稿。
**出貨版仍卡 PENDING 結案** —— 此門檻不因本包改變。

**寫回之授權在下一包** —— 待本包收斂 ＋ **Pei 之 Excel 驗收**
（`/tmp/vc_writeback_c/`：開啟不報修復、R10:R1411 下拉可用）。
**該驗收仍未回報，它是寫回前的最後一道。**

---

## 五、執行層任務

| # | 任務 |
|---|---|
| T173 | 抄錄 R-VC30 入 `RULINGS.md`（接 R-VC29 之後），byte-level diff |
| T174 | b 段 3 筆生成（§二），併回原批，二批重跑收斂 |
| T175 | 第 6／7 批勘查（§3.2 停止條件）→ 未命中則生成（§3.3）|
| T176 | 八批回歸全綠 |
| T177 | `REVISIONS.md` 記 REV-15（§3.1）；`RESUME_PLAN.md` 更新（b 段與第 6／7 批自待辦移除，剩餘 = PENDING 結案 ＋ 表 B 定稿 ＋ 寫回）|

---

## 六、上繳包要求

1. T173–T177 逐項結果
2. **6 筆 TC 全文**（b 段 3 筆之上半供分析層人工複核 —— profile §8）
3. 第 1／3 批重跑 ＋ 第 6／7 批收斂 ＋ 八批回歸全輸出
4. 量測條件揭露（R-G8）

---

> **117/117 leaf 在本包收斂後全數處置完畢**（114 實 TC 涵蓋 ＋ 12 處 PENDING
> 佔位 ＋ 1 殼）。TC 預計 126 筆（勘查若觸發拆分則另計）。
> Tier 3：**Excel 驗收**（寫回前最後一道）、十筆 DR 之回覆、
> `QS Suggestion` 狀態查詢。
